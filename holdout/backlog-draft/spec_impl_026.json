{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_026",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_026",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--retry-failed` flag to `pi-sparkle resume --run <id>` that re-executes only FAILED children of a completed-with-failures run (COMPLETED children keep their results; the flag is a no-op with a note when nothing failed). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/main.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_026"
  }
}
