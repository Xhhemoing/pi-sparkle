{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_test_012",
  "family": "testing",
  "tasks": [
    {
      "id": "tsk_test_012",
      "role": "tester",
      "objective": "In the worktree at the sealed base commit, author a test file for src/pi-adapter/file-credential-store.ts covering: FileCredentialStore save/read/delete round-trip, 0600 publish mode, and group/world-readable refusal on POSIX. The tests must pass against the base commit as-is with `npx tsx --test <your file>`. Write under test/unit/ mirroring the repo's existing layout and import style (.js suffixes, node:test)."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "mutant-kill",
    "custodianRef": "oracle_test_012"
  }
}
