{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_test_001",
  "family": "testing",
  "tasks": [
    {
      "id": "tsk_test_001",
      "role": "tester",
      "objective": "In the worktree at the sealed base commit, author a test file for src/routing/r1.ts covering: routeR1 tier selection: cold-start fallback, quality-floor filtering, cheapest-above-floor pick, and the version-identity fail-closed skip. The tests must pass against the base commit as-is with `npx tsx --test <your file>`. Write under test/unit/ mirroring the repo's existing layout and import style (.js suffixes, node:test)."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "mutant-kill",
    "custodianRef": "oracle_test_001"
  }
}
