{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_025",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_025",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--max-parallel <n>` flag to `pi-sparkle run --children <spec>` capping concurrent children (default unchanged; n<1 is a named argv error). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/main.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_025"
  }
}
