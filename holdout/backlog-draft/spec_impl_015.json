{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_015",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_015",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--format csv` export option to `pi-sparkle pref export` (header row `scope,scopeKey,key,value,explicit,recurrence`; the existing JSON stays default). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/pref.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_015"
  }
}
