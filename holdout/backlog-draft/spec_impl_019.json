{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_019",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_019",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--reveal` flag to `pi-sparkle models list` that prints each provider's credential source (env var name or `auth.json`), never the secret bytes. The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/models.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_019"
  }
}
