{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_027",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_027",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--from <nodeId>` flag to `pi-sparkle resume --run <id>` restarting the flowchart from a named node (downstream dependencies re-run; completed upstream results are reused; unknown node ids are a named error). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/main.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_027"
  }
}
