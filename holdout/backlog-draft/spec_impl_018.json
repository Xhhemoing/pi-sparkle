{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_018",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_018",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--verify` flag to `pi-sparkle auth status` that re-checks each stored credential's file mode and reports drift (0600 expected; drift is WARN, never auto-chmod). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/auth.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_018"
  }
}
