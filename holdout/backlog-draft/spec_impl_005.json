{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_005",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_005",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--quiet` flag to `pi-sparkle doctor` that prints only FAIL lines (exit codes unchanged; PASS/WARN suppressed from stdout but still counted in `--json`). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/doctor.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_005"
  }
}
