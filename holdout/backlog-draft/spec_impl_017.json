{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_017",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_017",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--tag <key=value>` repeat flag to `pi-sparkle episode events` filtering to events whose payload carries the tag (unknown keys match nothing, not an error). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/episode.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_017"
  }
}
