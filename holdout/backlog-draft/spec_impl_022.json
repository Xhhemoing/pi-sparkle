{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_022",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_022",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--output <file>` flag to `pi-sparkle commits preview` writing the preview JSON to disk (stdout still prints the human summary; the file is written atomically). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/commits.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_022"
  }
}
