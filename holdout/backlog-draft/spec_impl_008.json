{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_008",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_008",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--stats` flag to `pi-sparkle inspect --run <id>` printing per-child duration, attempts, and outcome as a table (no new events; derive from the existing event log). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/inspect.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_008"
  }
}
