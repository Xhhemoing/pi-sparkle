{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_014",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_014",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--policy <file.json>` flag to `pi-sparkle retain` reading a retention policy from disk (schema-validated; a malformed policy fails closed with the file named). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/retain.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_014"
  }
}
