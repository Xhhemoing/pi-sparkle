{
  "$schema": "f6-taskSpec-v1",
  "id": "spec_impl_012",
  "family": "implementation",
  "tasks": [
    {
      "id": "tsk_impl_012",
      "role": "implementer",
      "objective": "In the worktree at the sealed base commit, add a `--dry-run` flag to `pi-sparkle delete --episode <id>` with the same contract (cascade members listed, nothing removed). The change must keep `corepack pnpm run build && corepack pnpm run lint && corepack pnpm run typecheck && node scripts/run-tests.mjs` green. Stay inside src/cli/delete.ts and its test file; do not refactor neighbours."
    }
  ],
  "allowedModels": [
    "claude-fable-5",
    "gpt-5.6-sol",
    "kimi-k3"
  ],
  "oracle": {
    "kind": "hidden-acceptance-tests",
    "custodianRef": "oracle_impl_012"
  }
}
