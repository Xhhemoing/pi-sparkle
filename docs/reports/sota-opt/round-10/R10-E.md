MODEL_SLUG=claude-fable-5-thinking-xhigh

# R10-E：`src/learning/` 第十遍复查报告（Round 10）

**战役:** 全库持久 SOTA 优化 Round 10 / R10-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `8d598f5`（含 S10-A-1、S10-B-1..3 排除已入表）
**分支:** `cursor/r10-e-learning-tenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第十组新角度枚举为空（有理由的
空枚举，未铸造任何 S10-E-\* ID），切片维持关闭（MORE_OPTIMA=no）。**

1. **零漂移双重确认**：切片 10 个文件（合计 1770 行）自 R1-E 裁决基线
   （`adb20d7`）至本轮基线（`8d598f5`）**逐字节未变**——
   `git diff adb20d7..HEAD -- src/learning/` 为 0 行、
   `git log adb20d7..HEAD -- src/learning/` 零提交，十轮全程无人触及该目录。
   且 R9-E 基线（`0f5c824`）之后的全部提交（R9-E/F/G/H/I/J 报告摄取、
   Round 9 收口、R10-A/B 报告与派单）仅触及 `docs/reports/sota-opt/`——
   `git diff --stat 0f5c824..HEAD -- src/ test/` 为空，**src 与 test 一行
   未动**。R9-E 的全部规模测量、调用面图景与 S1-E-1..8 / S2-E-1..7 /
   S3-E-1..5 / S4-E-1..3 / S5-E-1..5 / S6-E-1..5 / S7-E-1..5 / S8-E-1..3 /
   S9-E-1..3 共 44 项切片排除裁决原样成立、原样继承。
2. **本 VM 锚点/矩阵复测（未拷贝 R8-E/R9-E 数字）**：SLICE-CPU 总量上界
   锚点八次运行 **10.9–11.3µs/run**（collect 7.4–7.5µs、outcomes 1.4µs、
   diagnose 1.11–1.14µs、bandit-build 1.0µs）——距落地线（≥10ms）
   **889–919×**，即使把切片 CPU 清零也远不达门槛。本 VM 比 R9-E 的 VM 快
   （collect 7.4–7.5 vs 13.6–13.8µs，切片逐字节未变故为环境差异非代码差异）；
   按历史最保守口径（R7-E outcomes 带 7.0–7.3µs 替换本轮 outcomes 项）总量
   仍仅 ~16.8–17.2µs，**≥581×** 低于门槛，两种口径结论一致。配置态 live 面：
   `applyLearnedRouting` 非空策略 **264–289ns/task**（R9-E 258–280 同带）、
   空策略 **112–120ns/task**（R9-E 120–126 同带）；`loadLearnedRouting`
   全新 root 25.1–36.5µs、现实 registry **75.5–83.4µs**（R8-E 80–85 /
   R9-E 85.9–97.8 同级，亚 ms 一次性）。全部 VERDICT 行（确定性等价/夹具
   检查）八次运行逐位一致，3/3 sanity 检查全过（§1、附录 A）。
3. **配置态 × 命令类矩阵复核无洞**：全部切片进入点行号逐条 grep 复核，
   **与 R8-E/R9-E 记录逐一相同、无新增调用方、无新增命令类**
   （`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
   `run/flowchart-run:681/712`、`track/loop:88/172`）。S8-E-1 双载入
   （cli/main + flowchart-run）按令保护未动。
4. **第十组新角度枚举为空（§2）**：按「结构位置 × 消除机制」双向扫描
   第十遍全量重读的 10 个源文件，识别出的全部形态要么是既有排除 ID 的
   换名/换位点重现（双 Map.get=S1-E-2、verdict 双分派链=S9-E-3 族、
   小集合扫描=R6/R7-E 收口族、谓词整理=S7-E-4 族），要么被硬不变量明令
   封死（两遍结构、I/O 契约面、S8-E-1 双载入、registry fail-open），
   要么物理上无收益（`outcomeKindFromResult` 死形参属纯重构零运行时成本）。
   **未铸造任何 S10-E-\* 新 ID，无新增排除提议。**
5. **验证面**：切片相关套件全绿（**48 tests / 2 suites / 48 pass / 0 fail**，
   Node 22.14.0）。零代码改动下全部硬不变量天然满足。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与
双归因未动；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故
未提交 `scripts/round10-r10e-equivalence-sim.ts`（锚点脚本全文进附录 A，
遵守 R7-F/R8-C/R9-I 无赢家纪律）。本切片在其输出契约与数据面语义下维持
SOTA——第十遍复查确认：**剩余的全部 ms 级余量仍全部在被排除表点名保护的
I/O 契约面上**，切片级收口条件（R3-E §7 … R9-E §7）依然成立。
**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行）本轮第十遍全量实际读码，未依赖前九轮
  记忆。清单经 `wc -l src/learning/*.ts` 确认恰为 10 个文件，无多无少。
  上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、`feedback/`、
  `persist/`、`privacy/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表，含 S10-A-1、
  S10-B-1..3）→ round-10/PLAN.md → round-09/PLAN.md → round-09/R9-E.md →
  R8-E … R1-E → 10 个源文件。
- 基线漂移检查：`git diff adb20d7..HEAD -- src/learning/` 为 0 行且
  `git log adb20d7..HEAD -- src/learning/` 无提交——切片自 R1-E 裁决基线起
  十轮逐字节未变。`0f5c824..8d598f5`（R9-E 之后）的全部提交仅为
  `docs/reports/sota-opt/` 文档，`git diff --stat 0f5c824..HEAD -- src/
  test/` 为空——前九轮全部测量与裁决原样成立。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S9-\*、S10-A-1、
  S10-B-1..3 条目。特别地：
  - **S8-E-1 双 `loadLearnedRouting`**（`cli/main:708` + `flowchart-run:712`
    与 `track/loop:88` + `flowchart-run:712`）按令保护，未做任何去重分析
    之外的动作，本轮仅复核其行号与频次（§1）。
  - **S9-E-2 pass-2 预过滤为负优化**——本轮未再尝试任何两遍结构的工作集
    缩减变体；两遍语义按硬不变量维持。
  - **S9-E-1 死存储收口**——本轮死存储第二位点搜索结果见 §2 第 1 行
    （找到的是死形参非死存储，纯重构零收益，不立项）。
  - **S9-E-3 分派树合一**——本轮发现的 verdict 双分派链（§2 第 2 行）
    识别为同族表达式内变体，拒列。
- **换名重提检查**（识别为既有方案换名/换位点，**未列为新候选**）：
  - `collectSignalsFromEvents` JUDGE_DECISION 分支条件 spread 内
    `roleByTask.get()` / `familyByTask.get()` 各求值两次
    （`signals:218-223`）= **S1-E-2 双 Map.get 同位点重现**，拒列；
  - JUDGE_DECISION 的 score 三元链与 outcomeKind 三元链对同一 verdict
    双分派（`signals:195,204-209`）= **S9-E-3 跨函数分派树合一族**的
    表达式内变体（ns 级、同输入双树），拒列；
  - `familyFromRouted` 对 FAMILIES 的 `includes` 线性扫描换 Set
    （`from-episode:287-289`）= R6-E/R7-E 小集合扫描收口族（S6-E-1
    同域），拒列；
  - `isCompleteRoute` 四组 typeof+trim 的短路顺序重排
    （`from-episode:275-285`）= S7-E-4 循环不变量/谓词整理族，拒列；
  - `outcomesFromRoutedRun` 每 TASK_RESULT 一次 `Date.parse`
    （`from-episode:237`）= X1-1 域，拒列；
  - `runAutoAdaptLoop` 的 `events ?? []` / `extraSignals ?? []` 与
    JUDGE_DECISION evidenceIds 经 baseSignal `?? []` 的共享冻结空单例
    = S1-A-7 / S7-B-5 可观察身份族（R8-E §0、R9-E §0 已拒），维持拒列；
  - bandit reward 循环 `kind === "human"` 冗余析取、`readBanditFile`
    ENOENT catch 微整理 = R9-E §0 无 ID 收口，维持拒列。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  双 LCB（beta+normal）/ 双归因（logit+prob-add）在位；提升
  proposal-first；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；
  bandit/learned registry I/O 的 fsync 与完整性重哈希未触碰；corrupt
  registry fail-closed（S2-I-1 / S9-G-1 族）未触碰；`applyCascadeRetry`
  公开载荷形状未触碰；两遍 `collectSignalsFromEvents` 语义未触碰；
  未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

R7-I 教训（默认态夹具可能掩盖配置态主路径）按令复查。src 自 `0f5c824` 起
一行未动，故 R8-E/R9-E §1 矩阵表结构原样成立；本轮逐条 grep 复核全部切片
进入点行号，**与 R9-E 记录逐一相同、无新增调用方**：

| 命令类 × 配置态 | 切片进入点（本轮行号复核） | 频次与规模 |
| --- | --- | --- |
| `adapt auto --run` | `runAutoAdaptFromEvents` @ `cli/adapt:188` → `runAutoAdaptLoop` | 每进程一次，E≈41 |
| `adapt auto --project` | `runAutoAdaptLoop` @ `cli/adapt:205`（空事件表，S4-E-1 面） | 每进程一次 |
| `adapt learn --run` | `proposeRoutingFromRoutedEvents` @ `cli/adapt:168`（S2-E-2 面） | 每进程一次 |
| 普通 `run` | `loadLearnedRouting` @ `cli/main:708`（S2-I-1 已裁决） | 每进程一次 |
| `run --children` | `cli/main:708` 载入 → `applyLearnedRouting` @ `routing/assign:102` 每任务 → `startFlowchartRun` 内 `flowchart-run:712` 第二次载入（S8-E-1，勿去重）→ `applyLearnedRouting` @ `flowchart-run:681` 每节点 → `runAutoAdaptLoop` @ `cli/main:783` 一次 | 双载入各一次；任务/节点 ≤10/几十 |
| `track`（startTrackedRun） | `track/loop:88` 载入 → 每任务 `applyLearnedRouting` → `flowchart-run:712` 第二次载入（同 S8-E-1）→ `runAutoAdaptLoop` @ `track/loop:172` 一次 | 同上 |
| flowchart resume / replay | 各单次载入（无双载入，R8-E/R9-E 复核维持） | 每恢复一次 |
| SPARKLE_AUTO_ADAPT=0 | 仍收集（persistSignals + updateProjectBandit 照付），仅跳过 propose；R5-E 裁决维持 | 不变 |
| 测试专用面 | `patterns` / `attribution` / `signatures` / `compareSignatures` / `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `parseObservedSignal`(直调) / `proposeRoutingFromAssignments`(deprecated) 交叉检索复核仍零生产调用方 | 不进热路径 |

矩阵结论：**无新洞**。配置态给本切片的全部新增工作仍是每任务/每节点一次
`applyLearnedRouting` + 每 run 至多两次 `loadLearnedRouting`（S8-E-1 已
点名保护），均在本轮实测锚点覆盖内。

SLICE-CPU 锚点本 VM 重测（八次运行区间；Node 22.14.0；seeds
`0xea10e1`–`0xea10e5`；夹具镜像 R8-E/R9-E 生成器，R1-A 组成 E=41）：

```text
collect=7.4-7.5us  outcomes=1.4us  diagnose=1.11-1.14us  bandit-build=1.0us
total in-slice CPU ~10.9-11.3us per full auto-adapt run
vs landing bar >=10000us  ->  889-919x below EVEN IF ZEROED
configured-state: applyLearnedRouting(avoid=10, M=10) = 264-289ns/task （R9-E 258-280 同带）
                  applyLearnedRouting(空策略)        = 112-120ns/task （R9-E 120-126 同带）
                  loadLearnedRouting fresh(ENOENT)   = 25.1-36.5us/次
                  loadLearnedRouting 现实 registry    = 75.5-83.4us/次 （亚 ms 一次性，S8-E-1 带）
```

注：本 VM 快于 R8-E/R9-E 的 VM（collect 7.4–7.5µs vs 13.6–13.8µs；切片
逐字节未变，为环境差异非代码差异）。本轮 diagnose 夹具（seed `0xea10e3`）
与 R9-E 夹具组成不同（1.11–1.14µs vs 0.84–0.93µs，夹具差异非代码差异）；
outcomes 夹具为 retry-heavy 变体 1.4µs。按历史最保守口径（R7-E outcomes 带
7.0–7.3µs 替换）总量 ~16.8–17.2µs，仍 **≥581×** 低于门槛。两种口径下结论
一致：**锚点复核成立，该切片不存在不推翻既有排除就能达门槛的候选。**
全部 VERDICT 行（夹具规模、live 面 sanity、registry 夹具产出激活策略）
八次运行逐位一致，3/3 sanity 检查全过。

## 2. 第十组新角度枚举：有理由的空枚举

方法：对第十遍全量重读的 10 个源文件按「结构位置 × 消除机制」双向扫描
（死代码/死数据 / 冗余求值 / 分派整形 / 数据结构替换 / 分配避免 / I/O
契约面 / 结构语义变换），每个形态先对 EXCLUSIONS 全表 + 九轮 E 报告做
同机制先例检索，只有「排除表未覆盖且理论收益可达数十 ms」者才立项。
本轮扫描结果：**零个可立项形态**，逐类裁决如下（均未铸造新 ID——按方法论，
排除行换名/换位点重现与已关闭类的机制变体不重复立项）：

| 形态（本轮第十遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **死代码第二位点搜索**：`outcomeKindFromResult` 的 `outcome` 形参声明后无任何读者（`from-episode:291-298`，函数体只读 `verification`） | 0ns——死形参非死存储：私有单态函数经 JIT 内联后实参传递免费，消除属纯重构无任何运行时收益 | 不立项：无收益路径即无候选资格；S9-E-1 的死存储收口维持（routes 局部载荷外无「有写无读」位点，本形参是「有传无读」，删之为纯重构且触碰内部签名无益） | R9-E「LEARN_EVALUATION_PLAN 双定义=纯重构拒列」同式 |
| JUDGE_DECISION 分支 score 三元链 + outcomeKind 三元链对同一 `verdict` 双分派（`signals:195,204-209`），可合一为单 switch 同时产出两值 | 每 JUDGE_DECISION 至多 ~2 次冗余字符串比较，ns 级 | 换名重提拒列：S9-E-3（scoreTaskResult × taskSuccessFromResult 跨函数分派树合一，1.2–1.6ns/call 淘汰）的表达式内小型化变体——同一「同输入双分派树合一」机制，收益量级更低 | S9-E-3 族 |
| JUDGE_DECISION 条件 spread 内 `roleByTask.get()` / `familyByTask.get()` 各双重求值（`signals:218-223`） | 每 JUDGE_DECISION 2 次冗余 Map.get，ns 级 | 换名重提拒列：S1-E-2（双 Map.get 去重，R1-E 实测噪声带）同位点重现 | S1-E-2 |
| `familyFromRouted` 对 8 元素 FAMILIES 数组的 `includes` 线性扫描换 Set（`from-episode:287-289`） | 每 TASK_RESULT 一次 ≤8 步扫描 → O(1)，ns 级 | 已关闭类的机制变体拒列：小集合线性扫描在本切片与邻切片经 R6-E/R7-E 多位点裁决（Set 构建成本 > 扫描节省，且 8 元素扫描单次 <10ns） | S6-E-1 域 + R6/R7-E 小集合收口 |
| `isCompleteRoute` 四组 typeof+trim 短路顺序重排 / 提升（`from-episode:275-285`） | 每 MODEL_ROUTED 次 ns 级 | 换名重提拒列：谓词整理/循环不变量提升族（R9-E §0 已对同族无 ID 收口） | S7-E-4 族 |
| 两遍 `collectSignalsFromEvents` 的任何结构变换（合遍/预过滤/空快路径/工作集缓存） | pass 1+2 合计 ~7.5µs/run 全部 | 三向封死 + 硬不变量明令：合遍改变 PROJECT_DISCOVERED 后置发现语义（S1-E-1）；预过滤为理论+实测双重**负**优化（S9-E-2，E=400 五次全负）；空事件快路径生产不可达（S4-E-1）。派单明令两遍语义不动 | S1-E-1 / S9-E-2 / S4-E-1 + 硬不变量 |
| I/O 契约面全体：fsync 丢弃、完整性重哈希跳过、no-change 跳写、`saveAdaptationRegistry` 跳过、双载入去重、corrupt registry fail-open、readdir 并行化、惰性载入 | 切片相关全部 ms 级余量所在（本轮实测 registry 载入 75.5–83.4µs/次、写侧更贵） | 排除行逐字重现 + 硬不变量明令封死：全部数十 µs~ms 级形态均在排除表点名保护的 I/O 契约面上 | X0-3 / S2-E-1 / S2-E-4 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1 / S1-E-4/5 / S2-I-1 / S9-G-1 族 |
| `outcomesFromRoutedRun` 每 TASK_RESULT 一次 `Date.parse(event.occurredAt)`（`from-episode:237`）缓存/预解析 | 每 TASK_RESULT ~百 ns | 换名重提拒列：X1-1 域（Date.parse 缓存）第一轮即封死 | X1-1 |
| 测试专用面任何优化（`patterns` / `attribution` / `signatures` 三文件 + `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `compareSignatures`） | 不进任何生产路径 | 本轮交叉检索复核仍零生产调用方，自动拒列 | S1-E-7 / X2-6 族 |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（十轮）、src/test 自 R9-E 基线零漂移、调用位点行号全数复核一致（§1） | — |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可达数十 ms
落地线的候选。九个角度类（单遍化、分配避免、正则整形、数据结构替换、
I/O 重叠、形状单态化、死存储、工作集缩减、分派树合一）经 R1-E..R9-E
逐类点名收口后，第十遍读码未发现任何第十类机制：切片总 CPU 10.9–11.3µs
（本 VM）意味着**即使发明一个能清零全切片 CPU 的新机制也距门槛 889 倍**，
唯一物理上可达 ms 的位置全部是被硬不变量与排除表双重保护的 I/O 契约面。

## 3. 逐文件收口（前九轮收口之上的本轮新检查点）

| 文件 | 本轮第十遍新视角检查 | 结论 |
| --- | --- | --- |
| `signals.ts` | verdict 双分派链（§2 行 2）与条件 spread 双 get（§2 行 3）识别为换名，拒列；两遍结构维持硬不变量；S1-E-1/2/3、S2-E-5/7、S3-E-1/5、S4-E-1/3、S5-E-2/3、S6-E-1/2/5、S7-E-1/2/5、S8-E-2、S9-E-2/3 维持 | 无候选 |
| `from-episode.ts` | `outcomeKindFromResult` 死形参（§2 行 1）纯重构零收益不立项；`familyFromRouted` includes（§2 行 4）、`isCompleteRoute` 谓词序（§2 行 5）、Date.parse（§2 行 8）换名拒列；`applyCascadeRetry` 载荷形状按令未触碰；S2-E-2、S7-E-3、S9-E-1 维持 | 无候选 |
| `task-success.ts` | S9-E-3 涉及面维持；`copyDefinedBinding`+`present()` 空白字段契约实施点不动；S2-E-7、S8-E-2 覆盖维持 | 无候选 |
| `auto-loop.ts` | `events/extraSignals ?? []` 空单例拒列维持（§0）；I/O 契约边全点名维持（S1-E-4/5、S2-E-1、S3-E-2、S4-E-2、S5-E-1/5）；ingestSubagentDirectory readdir 序传播为既有预期行为非候选 | 无候选 |
| `bandit-store.ts` | 吸收析取、ENOENT catch 微整理拒列维持（§0）；fsync/重哈希按令未触碰；S2-E-3/4、S3-E-4、S6-E-3/4、X1-2 维持 | 无候选 |
| `diagnostics.ts` | 本轮 diagnose 锚点 1.11–1.14µs（夹具差异非代码差异）；`unique`/`mode` helper 族维持；S1-E-6、S3-E-3、S4-E-3、S5-E-4 维持 | 无候选 |
| `learned-routing.ts` | 空策略面锚点 112–120ns/task 复核（R9-E 同带）；读路径完整性重哈希拒列维持（S8-D-2 同式 + CAS 契约）；corrupt registry fail-closed 未触碰；S8-E-1（双载入，勿去重）、S8-E-3、S2-E-6、X1-1 维持 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | 本轮交叉检索复核仍零生产调用方（仅测试使用）；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下切片相关套件基线复核，全绿（Node 22.14.0）：

```bash
npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0
```

锚点脚本（临时置于 `/tmp/r10e-anchor.mts`，未入库以遵守无赢家纪律；
完整源码见附录 A，seeds `0xea10e1`–`0xea10e5`）共 **8 次独立运行**，每次
3/3 sanity 检查全过，全部确定性 VERDICT 行跨 8 次运行**逐位一致**。
代表性一次运行：

```text
VERDICT fixtures: E=41 collected=7 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=7.4us outcomes=1.4us diagnose=1.11us bandit-build=1.0us | total in-slice CPU ~11.0us per run vs landing bar >=10000us (912x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=283ns/task | empty policy=113ns/task -> x10 tasks/run = 2.83us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=27.1us | realistic registry (baseline+1 candidate)=75.5us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

计时跨 8 次运行汇总：SLICE-CPU 总量 10.9–11.3µs 稳定（889–919×）；
`applyLearnedRouting` 非空 264–289ns/task、空策略 112–120ns/task 稳定；
`loadLearnedRouting` fresh 25.1–36.5µs、现实 registry 75.5–83.4µs 稳定。
本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐形态有理由；第十遍读码未发现前九轮
  九个角度类之外的任何新机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令封死的
  I/O 契约面上；CPU 面即使清零也距门槛 889–919×）。
- 矩阵空洞：无（调用位点行号与 R8-E/R9-E 逐一相同；配置态 live 面与
  载入面锚点全数复测在带内）。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R9-E §7：SLICE-CPU 锚点失效（全切片 CPU
增长 ≥2 个量级，本轮复核值 10.9–11.3µs、历史保守口径 ~16.8–17.2µs）或
任一 I/O 契约排除（X0-3 / S2-E-1/4 / S1-G-1 / S1-E-4/5 / S4-E-2 / S5-E-5 /
S6-E-3/4 / S8-E-1）被正式推翻，或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

锚点脚本全文（`/tmp/r10e-anchor.mts`，`npx tsx /tmp/r10e-anchor.mts`）：

```ts
/**
 * R10-E deterministic anchor re-measurement (tenth pass over src/learning/).
 * No fresh candidates survived enumeration (empty tenth-group enumeration,
 * like R9-I), so this script only re-anchors on THIS VM:
 *   (1) the SLICE-CPU total upper-bound anchor (R3-E..R9-E lineage), and
 *   (2) the configured-state x command-class matrix cells named by the
 *       dispatch: applyLearnedRouting nonempty vs empty policy, and
 *       loadLearnedRouting fresh root (ENOENT) vs realistic registry.
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xea10e1 - 0xea10e5.
 * Fixture generators mirror R8-E/R9-E (R1-A composition, E~41).
 * VERDICT lines are deterministic (bit-identical across runs); BENCH lines
 * carry timing.
 */
import { mkdtemp } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { performance } from "node:perf_hooks";
import {
  collectSignalsFromEvents,
  type ObservedSignal
} from "/workspace/src/learning/signals.js";
import { diagnoseModelProjectIssues } from "/workspace/src/learning/diagnostics.js";
import { outcomesFromRoutedRun } from "/workspace/src/learning/from-episode.js";
import {
  applyLearnedRouting,
  ensureRoutingBaseline,
  loadLearnedRouting,
  routingPolicyContent,
  routingPolicyIdentity,
  type LearnedRoutingPolicy
} from "/workspace/src/learning/learned-routing.js";
import { createBanditState, recordReward, type BanditState } from "/workspace/src/routing/bandit.js";
import { oneHotDistribution } from "/workspace/src/routing/catalog-model.js";
import {
  loadAdaptationRegistryOrNew,
  saveAdaptationRegistry
} from "/workspace/src/adaptation/promotion.js";
import { AGENT_ROLES } from "/workspace/src/domain/roles.js";
import type { ProjectId } from "/workspace/src/domain/ids.js";
import type { Event } from "/workspace/src/run/events.js";
import type { TaskOutcome, VerificationKind } from "/workspace/src/protocol/v1.js";
import type { IsoTimestamp } from "/workspace/src/domain/timestamp.js";
import type { TaskFamily } from "/workspace/src/task/taxonomy.js";

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}`);
  }
}
function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

const NOW = "2026-08-24T17:00:00.000Z" as IsoTimestamp;
const FAMILIES_LOCAL: readonly TaskFamily[] = [
  "edit", "test", "review", "plan", "research", "refactor", "deploy", "unknown"
];
const OUTCOMES: readonly TaskOutcome[] = ["SUCCESS", "PARTIAL", "FAILURE", "CANCELLED"];
const VERIFS: readonly VerificationKind[] = ["PASSED", "FAILED", "UNOBSERVED"];
const ROLES = ["actor", "critic", "tester", "planner", "scout", "reviewer"] as const;
const ANSWERS = ["lgtm", "no, revert this", "please also add coverage", "可以", "不行 错误", "hmm"];

/* R1-A composition fixture (E~41): mirrors R8-E/R9-E generators. */
function genCollectEvents(rng: () => number, length: number): Event[] {
  const out: unknown[] = [];
  const taskIds = Array.from({ length: 10 }, (_, i) => `tsk_${i}0000000`);
  out.push({ type: "PROJECT_DISCOVERED", payload: { project: { id: "prj_simsim01", rootPath: "/tmp/x" } } });
  for (let i = 0; i < length; i += 1) {
    const roll = rng();
    const taskId = pick(rng, taskIds);
    if (roll < 0.25) {
      out.push({
        type: "MODEL_ROUTED",
        payload: {
          taskId,
          model: pick(rng, ["cheap", "premium", "mid"]),
          role: pick(rng, ROLES),
          ...(rng() < 0.8 ? { family: pick(rng, FAMILIES_LOCAL) } : {}),
          ...(rng() < 0.8 ? { modelVersion: "v1" } : {}),
          ...(rng() < 0.8 ? { featureVersion: "fv1" } : {})
        }
      });
    } else if (roll < 0.5) {
      out.push({
        type: "CHILD_MESSAGE",
        payload: {
          message: {
            type: "TASK_RESULT",
            taskId,
            runId: "run_simsim01",
            outcome: pick(rng, OUTCOMES),
            verification: { kind: pick(rng, VERIFS) },
            summary: pick(rng, ["tests passed", "did the work\n  with details", "failed to compile", ""]),
            evidenceIds: rng() < 0.6 ? ["evd_00000001"] : []
          }
        }
      });
    } else if (roll < 0.6) {
      out.push({ type: "USER_ANSWER", runId: "run_simsim01", payload: { answer: pick(rng, ANSWERS) } });
    } else if (roll < 0.7) {
      out.push({
        type: "JUDGE_DECISION",
        runId: "run_simsim01",
        payload: {
          taskId,
          verdict: pick(rng, ["APPROVED", "REJECTED", "NEEDS_USER_DECISION"] as const),
          evidenceIds: rng() < 0.5 ? ["evd_00000002"] : []
        }
      });
    } else {
      out.push({ type: pick(rng, ["LEDGER_UPDATED", "TASK_STATUS_CHANGED", "RUN_STARTED"] as const), payload: {} });
    }
  }
  return out as Event[];
}

function genRoutedEvents(rng: () => number, length: number): Event[] {
  const out: unknown[] = [];
  const taskIds = Array.from({ length: 10 }, (_, i) => `tsk_${i}0000000`);
  const models = ["cheap", "premium", "mid"];
  out.push({
    type: "PROJECT_DISCOVERED",
    runId: "run_simsim01",
    occurredAt: NOW,
    payload: { project: { id: "prj_simsim01", rootPath: "/tmp/proj-a" } }
  });
  for (let i = 0; i < length; i += 1) {
    const roll = rng();
    const taskId = pick(rng, taskIds);
    if (roll < 0.28) {
      const model = pick(rng, models);
      const complete = rng() < 0.85;
      out.push({
        type: "MODEL_ROUTED",
        runId: "run_simsim01",
        occurredAt: NOW,
        payload: {
          taskId,
          model,
          role: pick(rng, ["actor", ...AGENT_ROLES]),
          eligibleModels: models,
          behaviorDistribution: oneHotDistribution(models, model),
          ...(complete ? { family: pick(rng, FAMILIES_LOCAL) } : {}),
          ...(complete ? { featureVersion: "fv1" } : {}),
          ...(complete ? { modelVersion: "v1" } : {}),
          ...(complete ? { agentRole: pick(rng, [...AGENT_ROLES]) } : {})
        }
      });
    } else if (roll < 0.36) {
      out.push({
        type: "TASK_RETRY",
        runId: "run_simsim01",
        occurredAt: NOW,
        taskId: rng() < 0.8 ? taskId : undefined,
        payload: { nextModel: rng() < 0.85 ? pick(rng, [...models, "fresh"]) : undefined }
      });
    } else if (roll < 0.62) {
      out.push({
        type: "CHILD_MESSAGE",
        runId: "run_simsim01",
        occurredAt: NOW,
        payload: {
          message: {
            type: "TASK_RESULT",
            taskId,
            runId: "run_simsim01",
            outcome: pick(rng, OUTCOMES),
            verification: { kind: pick(rng, VERIFS) },
            summary: "tests passed",
            evidenceIds: rng() < 0.6 ? ["evd_00000001"] : []
          }
        }
      });
    } else {
      out.push({
        type: pick(rng, ["USER_ANSWER", "LEDGER_UPDATED", "RUN_STARTED"] as const),
        runId: "run_simsim01",
        occurredAt: NOW,
        payload: { answer: "lgtm" }
      });
    }
  }
  return out as Event[];
}

function genSignal(rng: () => number): ObservedSignal {
  const criterion = pick(rng, ["taskSuccess", "userAcceptance", "policyCompliance", undefined] as const);
  return {
    source: pick(rng, ["user", "subagent", "deterministic"] as const),
    kind: pick(rng, ["human", "peer", "judge", "deterministic"] as const),
    projectId: pick(rng, ["prj_a0000000", "prj_b0000000"]) as ProjectId,
    score: Math.floor(rng() * 101),
    boundary: "execution",
    summary: "s",
    createdAt: NOW,
    evidenceIds: [],
    ...(rng() < 0.85 ? { modelId: pick(rng, ["m1", "m2", "m3"]) } : {}),
    ...(criterion !== undefined ? { criterion } : {}),
    ...(rng() < 0.7 ? { family: pick(rng, ["edit", "test", "review"]) } : {}),
    ...(rng() < 0.8 ? { outcomeKind: pick(rng, ["PASS", "FAIL"] as const) } : {})
  };
}

/* Verbatim in-lock bandit build replica (R2-E S2-E-3 reference form). */
function banditBuild(previous: BanditState | undefined, signals: readonly ObservedSignal[]): BanditState {
  const arms = new Set(previous?.arms ?? []);
  for (const signal of signals) {
    if (signal.modelId !== undefined && signal.modelId.trim() !== "") arms.add(signal.modelId);
  }
  const armList = [...arms];
  let state = createBanditState(armList);
  if (previous !== undefined) {
    const pulls: Record<string, number> = {};
    const rewardSum: Record<string, number> = {};
    for (const arm of armList) {
      pulls[arm] = previous.pulls[arm] ?? 0;
      rewardSum[arm] = previous.rewardSum[arm] ?? 0;
    }
    state = {
      arms: armList,
      pulls,
      rewardSum,
      explorationsUsed: previous.explorationsUsed,
      highRiskExplorations: previous.highRiskExplorations
    };
  }
  for (const signal of signals) {
    if (signal.criterion !== "taskSuccess") continue;
    if (signal.source === "user" || signal.kind === "human" || signal.kind !== "deterministic") continue;
    if (signal.modelId === undefined || !state.arms.includes(signal.modelId)) continue;
    if (signal.outcomeKind === "PASS") state = recordReward(state, signal.modelId, 1);
    else if (signal.outcomeKind === "FAIL") state = recordReward(state, signal.modelId, 0);
  }
  return state;
}

/* ================================================================
 * SECTION 1: SLICE-CPU anchor + live-face configured-state cells.
 * ================================================================ */
{
  const events = genCollectEvents(mulberry32(0xea10e1), 40);
  const routed = genRoutedEvents(mulberry32(0xea10e2), 40);
  const sigRng = mulberry32(0xea10e3);
  const signals = Array.from({ length: 12 }, () => genSignal(sigRng));
  const models10 = Array.from({ length: 10 }, (_, i) => `m${i}`);
  const prevSeed = Array.from({ length: 30 }, () => genSignal(sigRng));
  const previous = banditBuild(undefined, prevSeed);

  const collected = collectSignalsFromEvents(events, {});
  const outs = outcomesFromRoutedRun(routed);
  const diag = diagnoseModelProjectIssues(signals);
  console.log(
    `VERDICT fixtures: E=${events.length} collected=${collected.length} routedE=${routed.length} outcomes=${outs.length} S=${signals.length} diagnoseGroups=${diag.length} prevArms=${previous.arms.length}`
  );
  check("collect fixture non-trivial", collected.length > 3);
  check("outcomes fixture non-trivial", outs.length > 0);

  const collect = bench(() => void collectSignalsFromEvents(events, {}), 20000);
  const outcomes = bench(() => void outcomesFromRoutedRun(routed), 20000);
  const diagnose = bench(() => void diagnoseModelProjectIssues(signals), 40000);
  const bandit = bench(() => void banditBuild(previous, signals), 40000);
  const total = collect + outcomes + diagnose + bandit;
  console.log(
    `BENCH SLICE-CPU anchor re-verify: collect=${(collect * 1e3).toFixed(1)}us outcomes=${(outcomes * 1e3).toFixed(1)}us diagnose=${(diagnose * 1e3).toFixed(2)}us bandit-build=${(bandit * 1e3).toFixed(1)}us | total in-slice CPU ~${(total * 1e3).toFixed(1)}us per run vs landing bar >=10000us (${Math.round(10 / total)}x below even if zeroed)`
  );

  // Configured-state live face: nonempty learned policy vs empty policy.
  const catalog10 = models10;
  const learned10: LearnedRoutingPolicy = {
    primaryModelId: "m0",
    avoid: Array.from({ length: 10 }, (_, i) => ({
      modelId: `m${i % 5}`,
      reason: "r",
      ...(i % 2 === 0 ? { family: ["edit", "test", "review", "plan", "research"][i % 5]! } : {})
    })),
    prefer: [{ family: "edit", modelId: "m1" }]
  };
  const learnedEmpty: LearnedRoutingPolicy = { primaryModelId: "m0", avoid: [], prefer: [] };
  const appliedNon = applyLearnedRouting("edit", catalog10, "m2", learned10);
  const appliedEmpty = applyLearnedRouting("edit", catalog10, "m2", learnedEmpty);
  console.log(
    `VERDICT live-face sanity: nonempty allowed=${appliedNon.allowedModels.length} preferred=${appliedNon.preferredModel} | empty allowed=${appliedEmpty.allowedModels.length} preferred=${appliedEmpty.preferredModel}`
  );
  const applyCost = bench(() => void applyLearnedRouting("edit", catalog10, "m2", learned10), 100000);
  const applyEmptyCost = bench(() => void applyLearnedRouting("edit", catalog10, "m2", learnedEmpty), 100000);
  console.log(
    `BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=${(applyCost * 1e6).toFixed(0)}ns/task | empty policy=${(applyEmptyCost * 1e6).toFixed(0)}ns/task -> x10 tasks/run = ${(applyCost * 10 * 1e3).toFixed(2)}us/run (live face with learned policy loaded)`
  );
}

/* ================================================================
 * SECTION 2: configured-state cells — loadLearnedRouting fresh vs
 * realistic registry (production call-site absolute anchor; same
 * fixture as R8-E S8-E-1 / R9-E).
 * ================================================================ */
{
  const freshRoot = await mkdtemp(join(tmpdir(), "r10e-fresh-"));
  const freshCost = await benchAsync(async () => {
    await loadLearnedRouting(freshRoot, "/tmp/proj-r10e");
  }, 200);

  const stateRoot = await mkdtemp(join(tmpdir(), "r10e-reg-"));
  const projectRoot = "/tmp/proj-r10e";
  const registry = await loadAdaptationRegistryOrNew(stateRoot);
  const identity = routingPolicyIdentity(projectRoot);
  const parent = ensureRoutingBaseline(registry, identity, "premium", "r10e-anchor");
  const policy: LearnedRoutingPolicy = {
    primaryModelId: "premium",
    avoid: [{ modelId: "cheap", family: "edit", reason: "meanScore 0.30 over 6 samples" }],
    prefer: [{ family: "edit", modelId: "premium" }]
  };
  registry.createCandidate({
    identity,
    content: routingPolicyContent(policy),
    parentVersionId: parent.versionId,
    author: { kind: "detector", identity: "r10e-anchor" },
    evaluationPlan: { stages: ["static", "replay"], metrics: ["task-success", "cost"], planVersion: 1 }
  });
  await saveAdaptationRegistry(stateRoot, registry);
  const loaded = await loadLearnedRouting(stateRoot, projectRoot);
  check("matrix registry fixture yields an active policy", loaded !== undefined);
  const realCost = await benchAsync(async () => {
    await loadLearnedRouting(stateRoot, projectRoot);
  }, 200);
  console.log(
    `BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=${(freshCost * 1e3).toFixed(1)}us | realistic registry (baseline+1 candidate)=${(realCost * 1e3).toFixed(1)}us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)`
  );
}

console.log(`VERDICT total sanity checks: ${checks}, failures: ${failures}`);
if (failures > 0) {
  console.error(`${failures} SANITY CHECK(S) FAILED`);
  process.exit(1);
}
console.log("ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)");
```
