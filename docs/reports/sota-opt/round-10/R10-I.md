MODEL_SLUG=claude-fable-5-thinking-xhigh

# R10-I：CLI / Pi adapter / config / telemetry 第十遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `ca85ed8`（派单 tip，含 S10-A-1 / S10-B-1..3 / S10-C-1..3 / S10-D-1..2 排除）。报告撰写前再次 `git fetch origin`：tip 已前移至 `d3266fe`，增量仅为 R10-H（eval 切片）报告吞并——**未铸造新 ID、EXCLUSIONS 逐字未变**（`git diff ca85ed8..d3266fe -- EXCLUSIONS.md` 空），与本切片无碰撞，本分支维持基于 `ca85ed8`。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，`git diff 8dee7fb..HEAD -- src/cli src/pi-adapter src/config src/telemetry` 为空——**零漂移，连续第三轮字节不变**；`package.json` / `pnpm-lock.yaml` 亦零漂移。
- **切片清单核实（派单指令项）：25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致。R8-I 的「~28（cli 16）」维持 R9-I 判定：**头部笔误计数而非文件漂移**，git 为权威，全表见 §0。
- 前置阅读按令执行：README → EXCLUSIONS 全表（含 S10-A/B/C/D 全系新行）→ round-10/PLAN → round-09/PLAN + R9-I → R8-I ～ R1-I → 25 个切片源文件全量实际读码。
- 分支：`cursor/r10-i-cli-tenth-pass-83a1`（已推送，未开 PR）。
- 环境：**双 Node 版本实测**——VM 默认 22.14.0 与 nvm 22.22.2（engines ≥22.19 合规）。本 VM 细节：`/usr/bin/node` **不存在**，22.14.0 唯一入口是 `/exec-daemon/node` shim（R9-I 遮蔽警告再次成立；基准全部显式传二进制全路径）。pnpm 10.17.1，`pnpm install --frozen-lockfile`（本 VM node_modules 初始缺失，冷装）+ `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**，与 R7-I/R8-I/R9-I 同版本；exports 图逐项复核仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`），**无与模型表版本锁定的轻量 provider-id 子路径——S8-I-1 的唯一重开条件连续第三轮未满足**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S10-I-\* ID（诚实空枚举，与 R9-I 同结局），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第十组枚举而非 R9-I 剩余项清理；空枚举的证据面比 R9-I 更深一层（两个此前仅以散文关闭的机制变体本轮补齐了直接测量，见第 3 点）：

1. **零漂移三重确认（连续第三轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变、pi-ai 0.84.1 exports 图未变）、基线前移增量（`ca85ed8..d3266fe` 仅 R10-H 文档，EXCLUSIONS 未变）。R8-I 关闭矩阵与 R9-I 抽测所依据的结构前提在本基线逐项成立。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字）**：R9-I 5 格承重集 × 2 Node 版本全部 exit 0，结构逐格同构：地板 55.6/51.9 ms，**custom−builtin 差 children +47.1 / +36.6、track +56.0 / +26.4 ms（22.14/22.22）**——与 R9-I 带（+53.9/+26.9、+49.4/+28.2）同号、同量级、同 Node 版本梯度。进程内冷载锚点：`providers/all` 71.2 ms@22.14 / 28.7 ms@22.22（warm ~0.1 ms）；per-provider 表 anthropic 5.1/3.3、openai 2.7/1.0 ms——S7-I-1 的 per-provider 优势（~9-28×）与 custom 态回退归属完好（§1）。矩阵中唯一剩余的数十 ms 结构仍是 custom/mixed 态 `providers/all` 失手回退；其跳过形态持有 documented soundness counterexample（S8-I-1）且被硬不变量封死，重开条件本轮复核仍未满足。
3. **第十组新角度枚举为空（§2）**：以「机制分类 × 排除表覆盖」双向扫描，全部形态要么是排除行逐字重现，要么是已关闭类的机制变体。其中两个 R9-I 仅以散文归类关闭的变体，本轮以新的直接测量收紧裁决而**不重开、不铸 ID**（Rename/re-site 纪律）：
   - **跨进程版本键控磁盘快照 builtin 目录**（custom 失手时读快照替代 `providers/all` 导入）：本轮新证据表明该候选在**可表示性上成立**——解析后的 `Model` 对象为纯数据（11 个顶层字段全部 string/boolean/number/object，0 个函数字段，JSON 往返稳定，样本 333 B），量级上限（71.2/28.7 ms 减去快照读取 ~1-3 ms）**本可过量级门槛**；但在健全性上三重违规：(a) 版本字符串不内容锁定模型表（pnpm patch / postinstall / workspace override 可同版本改表）→ 惰性面读副本、同步面读活模块，**正是 S8-I-1 的 lazy-vs-sync 分歧禁区**，且重开条件明文要求由 pi-ai **自身**提供版本锁定导出，自制键不构成该锁；(b) 快照有效性无法在不加载 `providers/all` 的前提下核验（`getBuiltinModelDataGeneratedAt` 也在模块内——鸡生蛋）；(c) state-root 新增缓存文件类违反数据面契约 + 损坏快照静默回退属 S2-I-1/S3-I-6/S9-G-1 已否决的 fail-open 读取族。裁决：维持 R9-I「S8-I-1 同族 + 新数据面契约双重违规」归类，可表示性由假设升级为实测，**不铸 ID**。
   - **进程内失手记忆化**（`builtinModelLazy` 对 per-provider 解析失败 Map 记忆）：直接测量失败解析成本——首次 ~1.9-2.0 ms、重复失手中位 **~0.10 ms**（Node 已在 package-reader 层缓存失败解析），custom 态每次目录构建重复失手 ≤2 次 → 收益亚毫秒级，µs 族否决。确认 R9-I「物理上无收益」归类，**不铸 ID**。
   - 其余全部角度（含 track 路径重复构建排查——`track/loop.ts` 目录构建恰好一次，无 S1-I 形态残留）逐条映射到排除行，见 §2.3。
4. **验证面**：三个已落地赢家的等价性仿真在本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**）；切片相关子集 131 用例（130 pass / 1 skip / 0 fail）与全测试套件在 22.22.2 全绿（**1168 pass / 0 fail / 1 skip**）。22.14.0 下 doctor 的 engines 预检 fail-closed 仍是设计行为，非基线破损（R9-I §3 记录在案，本轮不重复展开）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（file-credential-store / auth-session 仅只读证据）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交任何 sim/harness 脚本（全文进附录 A，遵守 R7-F/R8-C/R8-F 纪律）。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X0-\*…X4-\* / S1-\*…S9-\* / S10-A-1 / S10-B-1..3 / S10-C-1..3 / S10-D-1..2 条目。I 切片逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：`--children` 的 smartChildPlan 目录复用（main.ts L726-729 注释与复用逻辑原样）、Pi runtime 点用处 `await import`、12 个分支独占分派模块惰性导入、per-provider 惰性模型表全部在位——零漂移即字节级证明，三个等价性仿真复跑通过为行为级证明。S4-I 败者编号为 **S4-I-2..5**（无 S4-I-1）、S5-I 赢家为 **S5-I-1**，按派单口径记录。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅复核其重开条件——pi-ai 0.84.1 exports 图 7 项无轻量 provider-id 子路径，未满足，维持淘汰。硬不变量「`providers/all` 失手回退 + throw-outside-catch 不动」逐字在位（`listed-model-lazy.ts` L34-37 未动）。§2.1 的磁盘快照变体经新测量后仍判入其同族，未借道重开。
  - **未重开 S8-I-2**（预热 providers/all——ESM 实例化为主线程 CPU 无重叠窗口）、**S8-I-3**（enabled 循环 Promise.all 化——µs 级 + 未知 id 错误选择分歧）：本轮扫描中「并行 per-provider 导入」形态直接映射到 S8-I-3 行，未立项。
  - **未重开 S6-I-1..3**（剩余独占边惰性 / compile-cache / 分派拆分）、**S7-I-2..4**（preferences 水合 / 配置读取并行 / per-provider 表推广到一次性命令）、**S5-I-2..5**、**S4-I-2..5**、**S3-I-1..6**、**S2-I-1..6**、**S1-I-1..8**。特记：flowchart 路径 `buildLiveCatalogConfig` 双构建（main.ts L569 + L583 router 内）本轮读码再见，即 **S1-I-1**（~190 µs 噪声）逐字重现，未立项；`loadLearnedRouting` 下沉 = S2-I-1、stdout 批量 = S3-I-4、冲突检查提升 = S4-I-4，均逐字映射。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项（S1-I-5 / S7-I-4 维持关闭）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价；双 LCB / 双归因在位；提升 proposal-first；CAS / 凭据 / 数据面契约 / 公开签名不动；`listed-model.ts` 同步公开面与惰性面经 `listed-model-common.ts` 的字节等价关系未动；未改任何测试与阈值。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR。

## 1. 本 VM 复测：5 格承重抽测 + 进程内锚点（双 Node，未拷贝历轮数字）

R9-I 抽测集原样复刻（configured 态夹具、每迭代全新拷贝 state-root、2 预热 + 10 测量、显式二进制全路径；harness 全文附录 A.1）。

### 1.1 抽测中位数（ms，spawn-to-exit，全部 exit 0）

| 格 | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|
| `--version`（地板） | 55.6 (min 53.6, p90 56.3) | 51.9 (49.9, 53.8) |
| run-children@builtin | 106.0 (103.0, 110.7) | 107.5 (105.3, 109.8) |
| run-track@builtin | 124.0 (119.4, 130.8) | 125.2 (122.6, 133.8) |
| run-children@custom | **153.1** (146.5, 156.8) | **144.1** (138.5, 147.0) |
| run-track@custom | **180.0** (171.9, 187.8) | **151.6** (147.9, 154.1) |

- **custom−builtin 差**：children **+47.1 / +36.6**、track **+56.0 / +26.4** ms（22.14/22.22）。R9-I 带 +53.9/+26.9 与 +49.4/+28.2，R8-I 带 +53.8/+28.6 与 +46.2/+29.2——三轮同号、同量级、同 Node 版本梯度。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
- **builtin 态目录构建**比地板贵 +50.4/+55.6（children）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。
- **Node 版本项**：22.22.2 的节省集中在触发 `providers/all` 的 custom 格（−9.0 children / −28.4 track），地板与 builtin 格 ±4 ms 内——`getPackageScopeConfig` 病理属 Node 版本项的历轮判断继续吻合。环境项，非切片代码角度。

### 1.2 进程内冷载锚点（附录 A.2 脚本）

| 锚点 | 22.14.0 | 22.22.2 |
|---|---|---|
| `providers/all` cold import | 71.2 ms | 28.7 ms |
| `providers/all` warm（第二次 await） | 0.057 ms | 0.096 ms |
| per-provider `anthropic.models` cold | 5.1 ms | 3.3 ms |
| per-provider `openai.models` cold | 1.0–2.7 ms | 1.0 ms |
| custom id 失手解析（`providers/customco.models` 失败 import）首次 | 1.875 ms | 2.004 ms |
| 同上，重复失手中位（N=50） | **0.102 ms** | **0.103 ms** |

- per-provider 优势 ~9-28×（71.2/5.1 ～ 28.7/1.0），S7-I-1 归属完好；custom 格增量与 `providers/all` cold 同量级，回退归属自洽。
- **重复失手 ~0.1 ms** 是 §2.2 记忆化裁决的直接证据：Node 的 package-reader 已缓存失败解析，仅首次失手支付 ~2 ms 完整 exports 遍历。

### 1.3 pi-ai 数据面探针（§2.1 裁决证据，附录 A.3 脚本）

- `providers/all` 导出 9 项**全部为函数**（builtinModels / builtinProviders / getBuiltinModel / getBuiltinModelDataGeneratedAt / …）。`builtinModels()` 返回值仅为 **178 B 存根**（7 键 / 5 模型 / 含 2 个函数值），且 `getBuiltinModel("anthropic","claude-haiku-4-5") !== builtinModels().anthropic["claude-haiku-4-5"]`——**权威表只经 getBuiltinModel / per-provider `*_MODELS` 可达**，无单一可序列化导出。
- 解析后的 `Model` 对象为纯数据：11 个顶层字段（id/name/api/provider/baseUrl/reasoning/input/cost/contextWindow/maxTokens/compat）无函数值，JSON 333 B 且往返稳定。
- `getBuiltinModelDataGeneratedAt()` = 1786081986539——数据代时间戳存在，但**导出自 providers/all 模块内部**：任何以它作缓存键的方案必须先加载被规避的模块本身。

## 2. 第十组新角度枚举（诚实为空，未铸 ID）

### 2.1 跨进程版本键控磁盘快照（custom 失手时读快照替代 providers/all）——不铸 ID，维持 S8-I-1 同族关闭

R9-I 以散文将其归入「S8-I-1『绕开回退权威源』同族 + 新数据面缓存契约」。本轮不满足于归类，把裁决建立在测量上：

- **可表示性（新实测，此前为假设）**：`Model` 对象纯数据、JSON 往返稳定（§1.3）——快照在技术上可构造，候选不能以「无法表示」廉价否决。
- **量级（过门槛）**：收益上限 = `providers/all` cold（71.2/28.7 ms）− 快照读取+解析（全表估 ~400 KB，~1-3 ms）≈ **25-65 ms**，数十 ms 级，量级门槛本可通过。
- **健全性（三重独立违规，任一即死）**：
  1. **lazy-vs-sync 分歧窗口**：同步面（`listed-model.ts`）读活模块，惰性面读副本。版本字符串不内容锁定表（pnpm patch / postinstall / workspace override 同版本改表可行），同版本异表**不可检测**——检测需加载 `providers/all` 本身（`getBuiltinModelDataGeneratedAt` 亦在模块内，§1.3），鸡生蛋。这正是 S8-I-1 的 kill zone；且重开条件明文要求版本锁定导出**由 pi-ai 自身提供**，自制键不构成该锁。
  2. **数据面契约**：state-root 新增缓存文件类（或构建期产物）违反「CAS / 数据面契约不动」。
  3. **fail-open 读取族**：损坏/过期快照静默回退权威源 = S2-I-1 / S3-I-6 / S9-G-1 已三次否决的模式族。
- **裁决**：机制变体确认判入 S8-I-1 同族，Rename/re-site 纪律下**不铸新 ID**；本节测量使该关闭从归类升级为带证据的封死。

### 2.2 进程内失手记忆化（builtinModelLazy 失败解析 Map 化）——不铸 ID，µs 族

- R9-I 散文归类「物理上无收益」。本轮直接测量（§1.2）：首次失手 ~1.9-2.0 ms **不可避免**（至少探测一次），记忆化只省重复失手，而重复失手已被 Node package-reader 缓存压到 **~0.10 ms/次**；custom 态每次目录构建 enabled 循环重复失手 ≤2 次 → 总收益 **<0.3 ms**。
- µs 族否决（S8-I-3 同量级族），确认 R9-I 归类，**不铸新 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现或量级即死）

| 扫描角度 | 处置 |
|---|---|
| track 路径重复目录构建 | **本轮读码排查为无**：`track/loop.ts` L81 构建恰好一次，main.ts track 分支不预构建；S1-I 形态无残留 |
| flowchart 路径双 `buildLiveCatalogConfig` | S1-I-1 逐字（~190 µs，第二次构建全走 warm 模块缓存） |
| providers.json 双读 | S1-I-2 逐字（~60 µs） |
| enabled 循环并行 per-provider 导入 | S8-I-3 逐字（µs + 未知 id 错误选择分歧） |
| providers/all 预热重叠 | S8-I-2 逐字（主线程 CPU 无重叠窗口） |
| custom id 跳回退 | S8-I-1 硬禁，重开条件未满足（§1.3） |
| compile-cache / 分派拆分 / 剩余独占边 | S6-I-1..3 逐字 |
| dist 布局向 getPackageScopeConfig 开刀 | S5-I-2..5 族；且为 Node 版本项（§1.1） |
| 一次性命令类（models/auth/doctor/inspect/episode/commits/adapt） | S1-I-4 / S7-I-4 / S2-I-5 / S3-I-4 类；一次性 CLI 噪声按派单直接否决 |
| 凭据面（auth status --all 等） | S1-I-5 围栏，只读证据 |
| loadLearnedRouting / loadProvidersConfig 死载荷下沉 | S2-I-1 / S3-I-3 逐字（损坏时错误路径发散） |
| buildInvocation / invocationError / parseModelRef / parseProvidersConfig 微优化 | S1-I-7 / S2-I-3 / S3-I-1 / S3-I-5 / S2-I-6 逐字（实测更慢或 ns 级） |
| veteran 态 invocations.jsonl 线性成本 | 校准成本在 `src/routing/cost-calibration.ts`，切片外；R3-I 量化 + R7-I 关闭记载维持 |

## 3. 验证面

- **等价性仿真（已落地赢家，未削弱）**：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（其 part D 探针同时复证 builtin 构建后 providers/all 保持冷、custom 回退命中缓存）。
- **测试**：切片相关子集（unit cli/config/telemetry + pi-boundary + live-catalog + integration cli/pi-adapter/track-loop）**131 用例：130 pass / 1 skip / 0 fail**；全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I 计数逐字相同。
- **引擎注记**：22.14.0 的 doctor engines 预检 fail-closed 为设计行为（R9-I §3 记录），本轮未重复展开；本 VM `/usr/bin/node` 不存在，22.14.0 仅经 `/exec-daemon/node` 可达，已在环境节记录以防后续轮次误判。

## 附录 A：测量 harness 全文（未提交为脚本，遵守无赢家纪律）

### A.1 5 格抽测 harness（/tmp/r10i-bench/spot.mjs，`node spot.mjs <node二进制全路径>` 分别以两版本执行）

```javascript
// R10-I spot re-measure: floor + builtin/custom children+track cells (R8-I/R9-I harness style).
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r10i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.argv[2];
const WARMUP = 2, ITERS = 10;

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
for (const [name, cfg] of Object.entries(states)) {
  const dir = join(FIX, `state-${name}`);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
}
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
const CHILDREN = join(FIX, "children.json");
writeFileSync(CHILDREN, JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");

const cells = [
  { id: "version", args: () => ["--version"], state: undefined },
  ...["builtin", "custom"].flatMap((state) => [
    { id: `run-children@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] },
    { id: `run-track@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"] }
  ])
];
function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, `state-${state}`), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 120000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-FAIL ${cell.id}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

### A.2 进程内锚点探针（工作区根目录下临时文件执行后删除；bare specifier 需从 /workspace 解析——/tmp 下执行会 ERR_MODULE_NOT_FOUND）

```javascript
import { performance } from "node:perf_hooks";
const label = process.version;
// (1) 失手解析成本（重复 N 次同进程）
{
  const N = 50; const times = [];
  for (let i = 0; i < N; i++) {
    const t0 = performance.now();
    try { await import("@earendil-works/pi-ai/providers/customco.models"); } catch { /* expected miss */ }
    times.push(performance.now() - t0);
  }
  times.sort((a, b) => a - b);
  console.log(`${label} miss-resolution: first=${times[times.length - 1].toFixed(3)}ms median=${times[Math.floor(N / 2)].toFixed(3)}ms min=${times[0].toFixed(3)}ms`);
}
// (2) providers/all cold/warm
{
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const dt = performance.now() - t0;
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  console.log(`${label} providers/all import: cold=${dt.toFixed(1)}ms warm=${(performance.now() - t1).toFixed(3)}ms`);
}
// per-provider 表：for (const p of ["anthropic","openai"]) await import(`@earendil-works/pi-ai/providers/${p}.models`) 同法计时
```

### A.3 pi-ai 数据面探针（§1.3 / §2.1 证据）

```javascript
const all = await import("@earendil-works/pi-ai/providers/all");
// 导出全为函数：Object.keys(all).map(k => typeof all[k]) → 9 × "function"
const stub = all.builtinModels();          // 178 B 存根、含 2 个函数值——非权威表
const m = all.getBuiltinModel("anthropic", "claude-haiku-4-5");
console.log(m === stub.anthropic?.["claude-haiku-4-5"]); // false：权威表只经 getBuiltinModel 可达
// Model 纯数据性：Object.entries(m) 无函数字段；JSON.stringify(m) 333 B 且 JSON.parse 往返稳定
console.log(all.getBuiltinModelDataGeneratedAt());       // 1786081986539——键源在被规避模块内部（鸡生蛋）
```
