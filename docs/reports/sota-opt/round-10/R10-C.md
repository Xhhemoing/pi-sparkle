MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 10 / R10-C：离线路由切片第十遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 10 / R10-C（Round 1–9 同区第十遍，叠在
  已落地的 S7-C 之上）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `253d589`（含 R10-A /
  S10-A-1 与 R9-G / S9-G-1..3 排除合入；`git log -1 -- src/routing/` 显示
  切片最后一次改动仍为 `183df9b`「land S7-C support-only eta」——其后基线
  区间仅文档更新，本轮全部测量在该形态之上，未回退未重写）
- **分支:** `cursor/r10-c-offline-routing-tenth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件清点确认，共 2,039 行；含 S7-C eta 站点、S6-C switch 与 S4-C/S5-C solve 现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动。** 第十组从未点名角度枚举出
三个新候选与一个组合封顶探针：一个经理论直接淘汰并以数值反例探针
坐实（S10-C-3），两个可测项经理论 + 确定性仿真（**12,420 项逐位检查
× 3 次独立全量运行全绿**，另有前置两候选形态 9,468 项 × 3 次独立运行
全绿；两形态确定性输出行 md5 跨运行逐字节一致）+ 真实规模基准裁决后
淘汰：

- **S10-C-1（DKL）** irls 每行 stamp 扫描改独立键代表行列表（每次拟合
  按首现行序收集一次代表行，此后每次迭代只对独立键循环计算 eta/mu，
  再以行循环经 `keys[i]` 回填行缓冲）：逐位平凡成立（同一代表行支撑
  驱动同一升序支撑和，计算只读 beta；回填在该键写入之后才读），但省
  流量上界即亚带——行访问 **3,586,400 次/报告** × 每次省
  (1 Int32 load + 1 compare) = 7,172,800 ops/报告 ≈ **~7.2 ms @1ns**，
  低于带 ~5×；三次干净两路 **+16.2/+11.2/+16.7 ms
  （1.024×/1.017×/1.025×）**全部带内。与 S2-C（键去重本体）、S6-C-6
  （戳拷贝）、S9-C-3（行缓冲消除）互异：本候选是去重判定的**循环控制
  结构**层，此前从未点名。
- **S10-C-2（AKD）** APC off 值在单列扫描内按规范键去重（首个活跃行
  付 O(p) slice+zero+dot，同键后续行复用存储的 double）：逐位由 S2-C
  论证在新站点成立——同键行向量逐项相同 → off 为同一 double，逐行
  `sum += on − off` 每项每值每序不变；但节省池是已排除 S8-C-2 实测
  天花板（22.2–23.6 ms，带内）的**严格子集**——只省重复对的 O(p)
  （本夹具活跃对重复率 58.6%）；组件级实测 **~18.15 ms/报告**，三次
  干净两路 **+12.5/+11.1/+12.0 ms（1.019×/1.017×/1.018×）**带内。
  与 S8-C-2（off 改支撑求和，省全部对）机制不同、不构成重开，但被其
  天花板数学封顶。
- **S10-C-3（SYM）** solveSymmetric 对称 Schur 补镜像（消元只算上/下
  三角一半，另一半从 `m[row][k]` 镜像）：理论直接淘汰，不立仿真道——
  （i）部分主元行交换在消元中途破坏对称性，镜像需交换追踪 + 回退
  路径（平行实现，X2-1 改写先例反面）；（ii）即使无交换，镜像值
  fl(fl(x/p)·y) 与独立计算值 fl(fl(y/p)·x) 对一般 double 逐位不等
  （除法舍入不与乘法交换）——X2-3 数值路径冻结同族。附带数值探针
  坐实：**10,000 组随机 (x,y,p) 中 3,458 组逐位不等**（首例
  x=2.069274090230465, y=3.3415430765599012, p=2.522030453570187 →
  2.7416673339237967 vs 2.7416673339237962）。
- **CMB（DKL+AKD 叠加探针，切片内最大候选组合）**：三次干净两路
  **+30.5/+28.5/+8.8 ms（1.047×/1.044×/1.013×）**——最大组合仍不
  越带，且跨独立进程摆幅（8.8↔30.5）本身即带级。两个亚带候选叠加
  也无法跨越落地线，组合出路封顶。

线束锚点（§2.1）：多路赛马内**逐位相同**的 production 导入 vs ctl
复刻差 **+32.3/−2.6/+39.6 ms** 跨运行翻符号——±35 ms 噪声带在本 VM
的直接演示（两条lane每一位都相同，计时差纯为环境噪声）。

重剖析（§1）确认 S7-C 后成本模型在本 VM 复现：`solveSymmetric` 自身
**61.3%（~421.9 ms/报告）**、`irls` 自身 **20.4%（~140.7 ms）**——与
R9-C 的 61.5%/~430 与 18.7%/~131 同带；其余各池（编排 ~29.5、sigmoid
~29.2、防御拷贝 ~18.3、APC ~16.7、GC ~6.9 ms/报告）**每一个即使整池
清零也低于 ±35 ms 噪声带**（R9-C 同结论，逐池复测未漂移）。本轮把
irls 去重判定的循环控制结构（DKL）、APC off 站点的键去重出路（AKD）
与 solve 的对称镜像出路（SYM）点名关闭，并以 CMB 探针关闭「亚带候选
组合越带」的最后出路。**在当前排除表、逐位契约与 ±35 ms 噪声带下，
本切片不存在不经表所有者层级契约变更（公开异步签名 / 构建面 / X2-3
数值路径解冻）即可达落地线的候选**——收口声明与 R8-C/R9-C 同强，且
本轮额外封顶了组合出路。

小形状矩阵复核（R7-I 教训）：3 模型 × 3 项目 / 120 行档整报告中位
**20.6 ms**——该规模下整条报告低于噪声带，调用矩阵在小配置态一侧
整体收口。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11），live 面零文件改动。仓库变更仅本报告一个文件；无生产赢家故
未提交新 scripts 资产（败者仿真全文进附录，遵守 R7-C/R8-C/R9-C
先例）。

## 0. 范围与约束遵守

- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表，含
  S10-A-1 与 S9-G-1..3；X2-1 改写后行）→ round-10/PLAN.md +
  round-09/R9-C.md → R8-C … R1-C 与 R7-C-LAND.md → 9 个切片源文件
  全量重读。
- 基线漂移检查：基线 `253d589` 相对 R9-C 基线 `195cb53` 仅文档合入
  （R9-C..G 报告、R10 PLAN、R10-A 报告与 S10-A-1 排除行）；
  `git log -1 -- src/routing/` 仍为 `183df9b`（S7-C 落地本身），S7-C
  落地形态（去重分支内支撑升序直加 + 前提注释 + S6-C switch 累加）
  原样在位，本轮未触碰未重写。既有回归资产七连全绿且检查数逐一匹配
  锚定值（§5），基线无漂移。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径；
  S5-C-5/7、S6-C-1..7、S7-C-1..4、S8-C-1..4、S9-C-1..4 及 X* /
  S1-*..S9-* / S10-A-1 全系未重提。S10-C-2 与 S8-C-2 的区分见 §2
  （机制不同：键去重 vs 支撑求和；且被其天花板封顶而非重测同方案）；
  S10-C-1 与 S2-C/S6-C-6/S9-C-3 的区分见 §2（循环控制结构 vs 键去重
  本体 / 戳拷贝 / 行缓冲）。
- 硬不变量全部满足（生产零 diff 下天然成立）：双 LCB 与双归因两路
  一行未删；promotion proposal-first；阈值 / 测试 / 公开签名 / 数据面
  契约不动；单线程逐位审计基底不动（无线程化尝试）；±0.0 夹具逐位
  维持。
- 环境：Node 22.22.2（VM 默认 22.14.0 低于 engines ≥22.19.0，nvm
  切换并前置 PATH，与 R1-C..R9-C 同处理）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`。

## 1. 重剖析：S7-C 后成本模型（实测，未拷贝 R9-C 数字）

V8 `--cpu-prof`（perf 夹具 rows=400/p=60/bootstrap=200，2 次预热 + 7 次
测量；工作进程采样 6,197.2 ms / 9 次报告调用，Node 22.22.2）。本 VM
生产每报告 7 次测量（独立进程，sorted）：649.3 652.7 654.7 **656.8**
659.2 661.0 679.0，中位 **656.8 ms**；剖析运行内中位 ~663 ms。

| 函数（自身时间） | 采样合计 | 折合 /报告 | 占比 |
| --- | --- | --- | --- |
| `solveSymmetric`（lin-alg） | 3797.3 ms | **~421.9 ms** | **61.3%** |
| `irls` 自身 | 1266.0 ms | **~140.7 ms** | **20.4%** |
| `fitLogitAdditive` 自身（编排+bootstrap） | 265.3 ms | ~29.5 ms | 4.3% |
| `sigmoid` | 263.0 ms | ~29.2 ms | 4.2% |
| lin-alg 匿名回调（防御拷贝） | 164.9 ms | ~18.3 ms | 2.7% |
| `averagePredictiveComparison` | 150.5 ms | ~16.7 ms | 2.4% |
| GC | 62.4 ms | ~6.9 ms | 1.0% |

与 R9-C 剖面（solve 61.5%/~430、irls 18.7%/~131）逐池同带——S7-C 后
成本模型在本 VM 稳定复现，未漂移。带下各池维持 R8-C/R9-C 裁决：每池
即使整池清零也 < ±35 ms 带（防御拷贝池 ~18.3 ms 复测与 R4-C S4-C-3
裁决「拷贝存在性=契约本体」一致，整池消除也不越带）。

站点核算（仿真插桩，同夹具）：fits=201、IRLS 迭代 8,966、p=60、
n=400、s̄=4.457、独立键（基础行）=167；行访问 **3,586,400 次/报告**
（S10-C-1 每次省 1 Int32 load + 1 compare = 7,172,800 ops/报告，
~7.2 ms @1ns——上界即封顶）；APC 全列扫（59 非截距列）活跃对 1,383、
键去重后独立对 573（重复率 58.6%），×201 次扫描 = S10-C-2 的节省池，
组件实测 90.31 µs/扫描 → **~18.15 ms/报告** < S8-C-2 天花板
22.2–23.6 ms < ±35 ms 带。

**收口复核**：两个带上池逐项对锁仍闭合——`solveSymmetric` 七类分解
（R6-C §2.5）由 X2-3 / S4-C / V8 / S5-C 族 / S4-C-1 / S4-C-2 /
S6-C-7 / S5-C-6 / S5-C-5 / S4-C-3 / S8-C-4 覆盖，本轮补关对称镜像
出路（S10-C-3，理论 + 反例探针）；`irls` 的 w/z/wz 公式体 + switch
分派（S6-C）+ eta 支撑和（S7-C）+ 戳拷贝（S6-C-6）+ 行缓冲
（S9-C-3）+ 钳制形态（S9-C-4）之外，本轮补关去重判定的循环控制结构
（S10-C-1）。APC 站点在 S8-C-2（支撑求和）之外补关键去重出路
（S10-C-2）。架构级出口（线程化 / WASM）维持 R9-C 理论关闭
（S9-C-1/2）。组合出路以 CMB 探针实测封顶（§2）。

## 2. 候选总表（S10-C-1..3 + CMB 探针，全部淘汰）

本轮第十组从未点名角度：irls 去重判定的循环控制结构、APC off 站点的
键去重、solve 对称镜像、以及亚带候选的最大组合封顶。可测候选先过
排除表相邻行区分，再过逐位仿真（附录脚本：终版 12,420 项 × 3 次独立
全量运行 + 前置形态 9,468 项 × 3 次，确定性行 md5 跨运行一致；决斗
模式 250 项 × 9 次独立进程），最后过真实规模基准（组件级 + 多路赛马
+ 三次干净两路决斗）。

| ID | 候选 | 理论 | 仿真等价 | 真实规模基准 | 裁决 |
| --- | --- | --- | --- | --- | --- |
| S10-C-1 | irls 每行 stamp 扫描改独立键代表行列表（DKL）：每次拟合按首现行序收集代表行一次，此后每次迭代对代表行循环计算 `etaByKey`/`muByKey`，行循环经 `keys[i]` 回填 | 逐位平凡：同代表行支撑 → 同升序支撑和（S7-C 引理同前提：0/1 设计 + +0.0 起始 + 有限 beta，均未变）；计算只读 beta，回填在该键写入后才读；首现序 = stamp 扫描的计算序，逐键逐值逐序不变 | ✅ 全电池（24 直接 irls 夹具 s=1..8 + ±0.0/β=0/空支撑对抗 + 53 全报告用例 + CMB 叠加道）逐位 × 3 次运行（另前置形态 × 3 次） | 省流量上界 7,172,800 ops/报告 ~**7.2 ms** @1ns < 带 ~5×；多路 +14.5/+16.7/+13.2 ms；三次干净两路 **+16.2/+11.2/+16.7 ms（1.024×/1.017×/1.025×）** | 淘汰：上界即亚带，实测全部带内（贴带不越带先例 S8-C-2）。与 S2-C（键去重本体）、S6-C-6（戳拷贝）、S9-C-3（行缓冲）互异：此前从未点名循环控制结构层。重开条件：迭代数或 n/独立键比增长 ≥1 个数量级使 stamp 流量越带 |
| S10-C-2 | APC off 值按规范键去重（AKD）：单列扫描内首个活跃行付 O(p) slice+zero+dot，同键后续行复用存储 double；`averagePredictiveComparison` 内部签名扩 keys+scratch（模块私有，公开签名不动） | 逐位由 S2-C 论证在新站点成立：同键行向量逐项相同 → slice+zero+dot 同 double；逐行 `sum += on − off` 每项每值每序不变，仅改 off 的取得方式（算 vs 读同值） | ✅ 全电池 + 专用对抗（5 组系数 × 30 重复密集向量含空支撑 × 13 列全扫 + 缺失列 + 空行集，全 Object.is）逐位 × 3 次运行 | 组件级全列扫 delta 90.31 µs × 201 = **~18.15 ms/报告**（重复率 58.6%）；多路 +14.1/+15.0/+22.6 ms；三次干净两路 **+12.5/+11.1/+12.0 ms（1.019×/1.017×/1.018×）** | 淘汰：节省池是 S8-C-2 实测天花板（22.2–23.6 ms，带内）的严格子集（只省重复对 O(p)），数学封顶带内；实测同证。机制与 S8-C-2 不同（键去重 vs 支撑求和跳列），不构成重开。重开条件：与 S8-C-2 同（APC 站点占比越带——即使重复率→1 天花板仍为 S8-C-2 量级） |
| S10-C-3 | solveSymmetric 对称 Schur 补镜像（SYM）：消元只算一半，`m[k][row]` 从 `m[row][k]` 镜像，期望砍近半消元乘加 | 理论不可行：(i) 部分主元行交换中途破坏对称性，镜像需交换追踪 + 非对称回退（平行实现）；(ii) 镜像值 fl(fl(x/p)·y) ≠ 独立值 fl(fl(y/p)·x)（除法舍入不与乘法交换）——X2-3 数值路径冻结同族 | 不立仿真道；数值反例探针坐实：**3,458/10,000 组随机 (x,y,p) 逐位不等**（首例 →2.7416673339237967 vs …62） | — | 理论淘汰。重开条件：表所有者层级 X2-3 数值路径解冻 + 放弃逐位契约（不预期） |
| —（CMB 探针，不立独立 ID） | S10-C-1 + S10-C-2 叠加（切片内最大候选组合） | 两编辑站点不相交（irls 体内 vs APC 体内），叠加逐位性 = 两候选逐位性之合取 | ✅ 全电池含 CMB 独立道逐位 × 3 次运行 | 多路 +29.9/+31.6/+33.7 ms；三次干净两路 **+30.5/+28.5/+8.8 ms（1.047×/1.044×/1.013×）** | 组合出路封顶：最大组合三次全部带内且摆幅（8.8↔30.5）即带级；「贴带不越带」（S8-C-2 先例）适用。无独立重开条件（随成员 ID） |

### 2.1 线束锚点

多路赛马内 ctl（逐字 183df9b/253d589 复刻入参数化线束）vs 生产导入
（逐位相同，仿真每次运行全报告比对确认）：**+32.3/−2.6/+39.6 ms**
——跨三次独立全量运行翻符号，两 lane 每一位相同下计时差纯为环境
噪声，±35 ms 带在本 VM 直接演示。本轮线束注入 irls+solve+APC 三件
（R9-C 为两件，锚点 +39.8/+41.1/+43.6 同量级）；各变体与 ctl 同线束
对比，差异恰为单站点编辑，不受注入代价影响。

## 3. 全切片裁决（9 文件）

| 文件 | 裁决(一行) |
| --- | --- |
| `offline-logit.ts` | 零改动。S10-C-1/2 实测淘汰（§2）；S7-C 落地形态（支撑升序 eta + 前提注释）逐字维持；S1-C/S2-C/S3-C/S6-C 落地形态维持；S8-C-1/2/3、S9-C-3/4、S7-C-1..4、S5-C-5/7 全部未重提 |
| `lin-alg.ts` | 零字节改动。S10-C-3 理论淘汰 + 反例探针（§2）；S4-C/S5-C 落地形态维持；S4-C-3 防御拷贝裁决复核维持（整池 ~18.3 ms 亚带）；七类分解 + 对称镜像出路全部闭合 |
| `posterior.ts` | C1/B1/A2 落地形态；S1-C-2/3/4/5 维持；`betacf`/`lnGamma`/二分 80 次数值路径锁定（X1-3/X3-4）；本轮重读无新候选 |
| `offline-prob-add.ts` | S1-C-6/7 维持；公式冻结（双归因 X0-11 一翼），全函数亚 ms；无新候选 |
| `r1.ts` | S1-C-10/X1-4/X1-6 维持；M≤10 常数域；无新候选 |
| `r1-shadow-report.ts` | 主路径 A2/B1 后形态；merge 路径仓内不可达（S1-C-2/3）；X1-5 维持；无新候选 |
| `propensity.ts` | S1-C-8/X3-5 维持；n 数百级、每报告一次；无新候选 |
| `bandit.ts` | S1-C-9/S1-A-8 维持；不可变契约拷贝保留（X4-2 同类）；无新候选 |
| `shadow.ts` | X4-2/S1-C-9 维持；drift/预算扣减 O(1)；无新候选 |

## 4. 新增排除 ID（请并入全局 EXCLUSIONS.md「本战役新增」）

| ID | 方案 | 原因 |
| --- | --- | --- |
| S10-C-1 | irls stamp 扫描改独立键代表行列表（DKL） | 逐位等价成立且已仿真证明，但省流量上界 ~7.2 ms/报告 < ±35 ms 带 ~5×；三次干净两路 +16.2/+11.2/+16.7 ms 全部带内。重开条件：迭代数或 n/独立键比增长 ≥1 个数量级 |
| S10-C-2 | APC off 值按规范键去重（AKD） | 逐位等价成立（S2-C 论证新站点）且已仿真证明，但节省池为 S8-C-2 天花板（22.2–23.6 ms，带内）严格子集；组件实测 ~18.15 ms、三次干净两路 +12.5/+11.1/+12.0 ms 带内。重开条件：与 S8-C-2 同 |
| S10-C-3 | solveSymmetric 对称 Schur 补镜像（SYM） | 理论不可行：部分主元行交换破坏对称性（镜像需平行回退实现）；镜像值 fl(fl(x/p)·y) ≠ fl(fl(y/p)·x)，反例探针 3,458/10,000 逐位不等——X2-3 同族。重开条件：X2-3 数值路径解冻（不预期） |

切片级收口条件：R6-C §2.5 七类分解 + S7-C 落地 + S8-C 跨函数死卫类
+ R9-C 微结构与架构出口关闭之后，本轮把 irls 去重循环控制结构、APC
键去重出路、solve 对称镜像出路点名关闭，并以 CMB 探针实测封顶组合
出路（最大组合 +8.8~+30.5 ms 带内）。**在当前排除表、逐位契约与
±35 ms 噪声带下，本切片无可达落地线的候选**；推翻该声明需要
（a）指出七类 + 历轮微结构类 + 本轮三出路之外的新成本类，或（b）表
所有者层级的契约变更（公开异步签名 / 构建面开放 / X2-3 数值路径
解冻——均不预期），或（c）现实规模位移 ≥1 个数量级使 S8-C-1/2、
S9-C-3 或 S10-C-1/2 的重开条件成立。

## 5. 测试与验证

环境：Node 22.22.2（nvm；VM 默认低于 engines）、pnpm。生产代码零改动，
测试文件零改动。

```bash
pnpm typecheck && pnpm lint && pnpm build    # ✓ 全绿（零 diff 基线自证）
pnpm test                                    # ✓ 全套通过（pass 1168 / fail 0 / skipped 1）

# 本轮仿真（临时脚本，未入库——无生产赢家，全文见附录）
npx tsx /tmp/r10c-sim.mts                   # ✓ 12,420 项逐位 × 3 次独立运行，确定性行 md5 一致
                                            #   （前置 DKL/AKD 两道形态 9,468 项 × 3 次同全绿）
npx tsx /tmp/r10c-sim.mts --duel ctl,dkl    # ✓ 250 项 + 决斗计时（×3 独立进程）
npx tsx /tmp/r10c-sim.mts --duel ctl,akd    # ✓ 250 项 + 决斗计时（×3 独立进程）
npx tsx /tmp/r10c-sim.mts --duel ctl,cmb    # ✓ 250 项 + 决斗计时（×3 独立进程）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 658.5 ms（1.12× vs frozen S6-C），与父端复跑
# （659.1/744.6，1.13×）同带。
```

代表性一次全量运行输出（终版，含 CMB 道）：

```text
scenario 1 (direct irls bitwise equivalence, 24 fixtures x {DKL} vs verbatim landed-S7-C irls/solve; support sizes: 1,2,3,4,5,6,7,8)
scenario 1b (adversarial signed-zero / beta=0 / empty-support fixtures x {DKL})
scenario 1c (AKD adversarial component checks: 5 coefficient sets x 30 duplicate-heavy vectors incl. empty support, x 13 APC columns; all Object.is; c1-sweep dedup hits on fixture: 7)
scenario 1d (solve drift anchor: 25 matrices x {prod} vs verbatim landed solve; SYM probe: 3458/10000 random (x,y,p) triples give fl(fl(x/p)*y) != fl(fl(y/p)*x); first: x=2.069274090230465 y=3.3415430765599012 p=2.522030453570187 -> 2.7416673339237967 vs 2.7416673339237962)
scenario 2 (full-report bitwise equivalence, 53 cases x {production, DKL, AKD, CMB} vs verbatim landed-S7-C pipeline)
S10-C-2 bench APC full sweep (59 names, 1383 active pairs, 573 distinct-by-key pairs = 58.6% dup fraction, p=60): slice+dot=240.9us per-key-dedup=150.6us delta=90.31us -> per report (x201 sweeps) ~18.15ms (S8-C-2 full-elimination ceiling was 22.16-23.59ms, in-band)
S10-C-1 bound: stamp-scan traffic = rowVisits x (1 Int32 load + 1 compare); see site accounting below — at 1ns/op the whole class is single-digit ms (strictly below the S9-C-3 row-buffer class ceiling ~14ms, itself sub-band)
site accounting: fits=201 irlsIterations=8966 p=60 n=400 sBar=4.457 distinctKeys(base)=167; row visits/report=3586400 (S10-C-1 drops 1 Int32 load + 1 compare each = 7172800 ops/report, ~7.2ms at 1ns/op)
  shape small (3 models x 3 projects, 120 rows): median 20.6 ms/report
  shape perf (6 models x 8 projects, 400 rows): median 724.3 ms/report
perf fixture (rows=400, bootstrap=200), median of 7 interleaved reps (multi-way, relative order only):
  ctl (landed S7-C)    719.8 ms  (1.00x vs ctl)
  production           687.5 ms  (1.05x vs ctl)
  VAR-DKL              705.3 ms  (1.02x vs ctl)
  VAR-AKD              705.7 ms  (1.02x vs ctl)
  VAR-CMB              689.9 ms  (1.04x vs ctl)

ALL EQUIVALENCE CHECKS PASSED (12420 bitwise checks)
```

其余两次精确留档运行的多路表（相对序带内一致）：

```text
run B:  ctl 684.8 | production 687.4 (1.00x) | VAR-DKL 668.1 (1.02x) | VAR-AKD 669.8 (1.02x) | VAR-CMB 653.2 (1.05x)
run C:  ctl 728.7 | production 689.1 (1.06x) | VAR-DKL 715.5 (1.02x) | VAR-AKD 706.1 (1.03x) | VAR-CMB 695.0 (1.05x)
前置形态（DKL/AKD 两道，9,468 项）代表运行：
run A': ctl 723.7 | production 681.1 (1.06x) | VAR-DKL 708.9 (1.02x) | VAR-AKD 707.4 (1.02x)
```

决斗输出（独立进程，每对 ×3）：

```text
duel ctl vs dkl: ctl=683.7ms dkl=667.4ms delta=16.2ms (1.024x)
duel ctl vs dkl: ctl=682.3ms dkl=671.1ms delta=11.2ms (1.017x)
duel ctl vs dkl: ctl=681.5ms dkl=664.9ms delta=16.7ms (1.025x)
duel ctl vs akd: ctl=662.9ms akd=650.4ms delta=12.5ms (1.019x)
duel ctl vs akd: ctl=670.6ms akd=659.6ms delta=11.1ms (1.017x)
duel ctl vs akd: ctl=672.1ms akd=660.1ms delta=12.0ms (1.018x)
duel ctl vs cmb: ctl=681.8ms cmb=651.3ms delta=30.5ms (1.047x)
duel ctl vs cmb: ctl=677.5ms cmb=649.0ms delta=28.5ms (1.044x)
duel ctl vs cmb: ctl=695.0ms cmb=686.2ms delta=8.8ms (1.013x)
```

## 附录：确定性仿真脚本（完整，可复现）

```ts
/**
 * Round 10 / R10-C deterministic equivalence + benchmark simulation
 * (offline routing slice, tenth pass). NOT committed unless a production
 * winner lands — otherwise full source goes into the R10-C report appendix.
 *
 * Adjudicated lanes (all measured against CTL = verbatim 183df9b/253d589
 * production irls/solve/on-prob/APC, i.e. the landed S7-C form; every
 * already-excluded edit UNAPPLIED):
 *
 *   PROD  production import (equivalence anchor; must match CTL bitwise)
 *   DKL   S10-C-1 candidate: the irls per-row stamp scan is replaced by a
 *         per-fit distinct-key list — representatives collected once per fit
 *         in first-occurrence order, each iteration computes eta/mu once per
 *         distinct key over the representative row's support, then a plain
 *         row loop fills the eta/mu row buffers through keys[i]. Bitwise
 *         trivially: the same representative row's support drives the same
 *         ascending support sum for every key, computations only read beta,
 *         and the row fills read the per-key doubles after every write.
 *         Scan-elimination sibling of the excluded S9-C-3 (buffer
 *         elimination); traffic bound is the per-row stamp load+compare.
 *   AKD   S10-C-2 candidate: averagePredictiveComparison dedups the off
 *         value per canonical key within one column sweep — same-key rows
 *         hold identical vectors, so slice+zero+dot yields the identical
 *         double; the first active row with a key computes it, duplicates
 *         reuse the stored double, and the per-row `sum += on - off` order
 *         is unchanged. Bitwise by the S2-C argument at a new site. Bounded
 *         above by the excluded S8-C-2 ceiling (22.2-23.6 ms, in-band):
 *         dedup saves the per-pair O(p) work only for duplicate pairs.
 *   CMB   DKL + AKD stacked (maximal in-scope composition) — bounds whether
 *         the two individually sub-band candidates jointly cross the band.
 *
 * Theory-rejected candidate with NO lane:
 *   S10-C-3 (SYM) solveSymmetric symmetric Schur-complement mirroring
 *         (halve the elimination update, mirror m[k][row] from m[row][k]) —
 *         (i) partial-pivot row swaps destroy symmetry mid-elimination, so
 *         the mirror needs swap tracking + a fallback path (parallel
 *         implementation), and (ii) even in the swap-free case the mirrored
 *         value fl(fl(x/p)*y) differs bitwise from the independently
 *         computed fl(fl(y/p)*x) for generic doubles (division rounding
 *         does not commute through multiplication) — X2-3 family. The probe
 *         below concretizes counterexamples.
 *
 * Modes:
 *   (default)          equivalence battery + instrumentation + micro benches
 *                      + in-process multi-way race (relative order only)
 *   --duel A,B         clean two-lane: exactly two racers, 7 interleaved
 *                      reps, median (run in 3+ independent processes)
 *
 * Run: npx tsx /tmp/r10c-sim.mts [--duel ctl,dkl]
 */

import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.js";
import { solveSymmetric } from "/workspace/src/routing/lin-alg.js";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import type { AttributionReport, OfflineRow } from "/workspace/src/routing/offline-types.js";

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}

interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}

interface FitResult {
  readonly coefficients: readonly number[] | null;
}

type SolveImpl = (a: readonly (readonly number[])[], b: readonly number[]) => number[] | null;

type IrlsImpl = (
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number,
  solve: SolveImpl
) => FitResult;

interface ApcScratch {
  readonly stamp: Int32Array;
  readonly off: Float64Array;
  mark: number;
}

type ApcImpl = (
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  scratch: ApcScratch | null,
  coefficients: readonly number[],
  onProbabilities: readonly number[],
  column: string
) => number;

interface Pipeline {
  readonly irls: IrlsImpl;
  readonly solve: SolveImpl;
  readonly apc: ApcImpl;
  readonly wantsScratch: boolean;
}

function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}

function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}

function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);

  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);

  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);

  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });

  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}

function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}

function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}

/* ------------------------------------------------------------------- */
/* Shared S6-C accumulation body (verbatim in every lane).              */
/* ------------------------------------------------------------------- */

function accumulate(
  xtwx: number[][],
  xtwz: number[],
  active: readonly number[],
  w: number,
  wz: number
): void {
  switch (active.length) {
    case 5: {
      const a0 = active[0]!;
      const a1 = active[1]!;
      const a2 = active[2]!;
      const a3 = active[3]!;
      const a4 = active[4]!;
      xtwz[a0] = xtwz[a0]! + wz;
      const r0 = xtwx[a0]!;
      r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
      xtwz[a1] = xtwz[a1]! + wz;
      const r1 = xtwx[a1]!;
      r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
      xtwz[a2] = xtwz[a2]! + wz;
      const r2 = xtwx[a2]!;
      r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
      xtwz[a3] = xtwz[a3]! + wz;
      const r3 = xtwx[a3]!;
      r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
      xtwz[a4] = xtwz[a4]! + wz;
      const r4 = xtwx[a4]!;
      r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
      break;
    }
    case 4: {
      const a0 = active[0]!;
      const a1 = active[1]!;
      const a2 = active[2]!;
      const a3 = active[3]!;
      xtwz[a0] = xtwz[a0]! + wz;
      const r0 = xtwx[a0]!;
      r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
      xtwz[a1] = xtwz[a1]! + wz;
      const r1 = xtwx[a1]!;
      r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
      xtwz[a2] = xtwz[a2]! + wz;
      const r2 = xtwx[a2]!;
      r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
      xtwz[a3] = xtwz[a3]! + wz;
      const r3 = xtwx[a3]!;
      r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
      break;
    }
    case 3: {
      const a0 = active[0]!;
      const a1 = active[1]!;
      const a2 = active[2]!;
      xtwz[a0] = xtwz[a0]! + wz;
      const r0 = xtwx[a0]!;
      r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
      xtwz[a1] = xtwz[a1]! + wz;
      const r1 = xtwx[a1]!;
      r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
      xtwz[a2] = xtwz[a2]! + wz;
      const r2 = xtwx[a2]!;
      r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
      break;
    }
    case 2: {
      const a0 = active[0]!;
      const a1 = active[1]!;
      xtwz[a0] = xtwz[a0]! + wz;
      const r0 = xtwx[a0]!;
      r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
      xtwz[a1] = xtwz[a1]! + wz;
      const r1 = xtwx[a1]!;
      r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
      break;
    }
    default: {
      for (let ai = 0; ai < active.length; ai++) {
        const a = active[ai]!;
        xtwz[a] = xtwz[a]! + wz;
        const rowA = xtwx[a]!;
        for (let bi = 0; bi < active.length; bi++) {
          const b = active[bi]!;
          rowA[b] = rowA[b]! + w;
        }
      }
    }
  }
}

/* ------------------------------------------------------------------- */
/* CTL irls: verbatim current production irls (landed S7-C form),       */
/* parameterized only by the solve implementation.                      */
/* ------------------------------------------------------------------- */

const irlsCtl: IrlsImpl = (design, rows, vectors, supports, keys, keySpace, maxIter, solve) => {
  void vectors;
  const p = design.names.length;
  const n = rows.length;
  const eta = new Array<number>(n).fill(0);
  const mu = new Array<number>(n).fill(0);
  const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
  const xtwz: number[] = new Array<number>(p).fill(0);
  const stamp = new Int32Array(keySpace);
  const etaByKey = new Float64Array(keySpace);
  const muByKey = new Float64Array(keySpace);
  let beta = new Array<number>(p).fill(0);
  for (let iter = 0; iter < maxIter; iter++) {
    const mark = iter + 1;
    for (let i = 0; i < n; i++) {
      const key = keys[i]!;
      if (stamp[key] !== mark) {
        stamp[key] = mark;
        let value = 0;
        const active = supports[i]!;
        for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
        etaByKey[key] = value;
        muByKey[key] = sigmoid(value);
      }
      eta[i] = etaByKey[key]!;
      mu[i] = muByKey[key]!;
    }
    for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
    xtwz.fill(0);
    for (let i = 0; i < n; i++) {
      const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
      const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
      const wz = w * z;
      accumulate(xtwx, xtwz, supports[i]!, w, wz);
    }
    for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
    const next = solve(xtwx, xtwz);
    if (next === null) return { coefficients: null };
    const delta = next.map((value, index) => value - beta[index]!);
    const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
    beta = next;
    if (!beta.every(Number.isFinite)) return { coefficients: null };
    if (l2 < TOL) break;
  }
  return { coefficients: beta };
};

/* DKL (S10-C-1): identical except the per-row stamp scan is replaced by a
 * per-fit distinct-key representative list. Representatives are collected
 * once per fit in first-occurrence row order; every iteration computes
 * eta/mu once per distinct key over the representative row's support (the
 * identical ascending support sum on the identical support array contents),
 * then a plain row loop fills the eta/mu row buffers through keys[i]. The
 * computations only read beta and write the per-key arrays; the fills only
 * read the per-key arrays after every write — same doubles, fewer stamp
 * loads/compares. */
const irlsDkl: IrlsImpl = (design, rows, vectors, supports, keys, keySpace, maxIter, solve) => {
  void vectors;
  const p = design.names.length;
  const n = rows.length;
  const eta = new Array<number>(n).fill(0);
  const mu = new Array<number>(n).fill(0);
  const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
  const xtwz: number[] = new Array<number>(p).fill(0);
  const seen = new Int32Array(keySpace);
  const reps: number[] = [];
  for (let i = 0; i < n; i++) {
    const key = keys[i]!;
    if (seen[key] === 0) {
      seen[key] = 1;
      reps.push(i);
    }
  }
  const etaByKey = new Float64Array(keySpace);
  const muByKey = new Float64Array(keySpace);
  let beta = new Array<number>(p).fill(0);
  for (let iter = 0; iter < maxIter; iter++) {
    for (let ri = 0; ri < reps.length; ri++) {
      const i = reps[ri]!;
      const key = keys[i]!;
      let value = 0;
      const active = supports[i]!;
      for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
      etaByKey[key] = value;
      muByKey[key] = sigmoid(value);
    }
    for (let i = 0; i < n; i++) {
      const key = keys[i]!;
      eta[i] = etaByKey[key]!;
      mu[i] = muByKey[key]!;
    }
    for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
    xtwz.fill(0);
    for (let i = 0; i < n; i++) {
      const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
      const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
      const wz = w * z;
      accumulate(xtwx, xtwz, supports[i]!, w, wz);
    }
    for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
    const next = solve(xtwx, xtwz);
    if (next === null) return { coefficients: null };
    const delta = next.map((value, index) => value - beta[index]!);
    const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
    beta = next;
    if (!beta.every(Number.isFinite)) return { coefficients: null };
    if (l2 < TOL) break;
  }
  return { coefficients: beta };
};

/* ------------------------------------------------------------------- */
/* CTL solve: verbatim current production solveSymmetric.               */
/* ------------------------------------------------------------------- */

const solveCtl: SolveImpl = (a, b) => {
  const n = b.length;
  if (a.length !== n || a.some((row) => row.length !== n)) {
    throw new DomainValidationError("solveSymmetric requires a square matrix matching b");
  }
  const m: number[][] = a.map((row) => [...row]);
  const x: number[] = [...b];
  const eps = 1e-12;

  for (let col = 0; col < n; col++) {
    let pivotRow = col;
    let pivotAbs = Math.abs(m[col]![col]!);
    for (let row = col + 1; row < n; row++) {
      const abs = Math.abs(m[row]![col]!);
      if (abs > pivotAbs) {
        pivotAbs = abs;
        pivotRow = row;
      }
    }
    if (pivotAbs < eps) return null;
    if (pivotRow !== col) {
      const tmp = m[col]!;
      m[col] = m[pivotRow]!;
      m[pivotRow] = tmp;
      const tb = x[col]!;
      x[col] = x[pivotRow]!;
      x[pivotRow] = tb;
    }
    const colArr = m[col]!;
    const pivot = colArr[col]!;
    const xCol = x[col]!;
    for (let row = col + 1; row < n; row++) {
      const rowArr = m[row]!;
      const factor = rowArr[col]! / pivot;
      if (factor === 0) continue;
      let k = col;
      const stop = n - 3;
      for (; k < stop; k += 4) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
        rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
        rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
      }
      for (; k < n; k++) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
      }
      x[row] = x[row]! - factor * xCol;
    }
  }

  const solution = new Array<number>(n).fill(0);
  for (let row = n - 1; row >= 0; row--) {
    const rowArr = m[row]!;
    let sum = x[row]!;
    for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
    const diag = rowArr[row]!;
    if (Math.abs(diag) < eps) return null;
    solution[row] = sum / diag;
  }
  for (const value of solution) {
    if (!Number.isFinite(value)) return null;
  }
  return solution;
};

/* ------------------------------------------------------------------- */
/* APC lanes.                                                           */
/* ------------------------------------------------------------------- */

/* CTL APC: verbatim production (keys/scratch ignored). */
const apcCtl: ApcImpl = (design, rows, vectors, _supports, _keys, _scratch, coefficients, onProbabilities, column) => {
  let sum = 0;
  const columnIdx = design.columnIndex.get(column);
  if (columnIdx !== undefined && columnIdx !== 0) {
    for (let i = 0; i < rows.length; i++) {
      if (vectors[i]![columnIdx] === 0) continue;
      const on = onProbabilities[i]!;
      const offVector = vectors[i]!.slice();
      offVector[columnIdx] = 0;
      const off = sigmoid(dot(coefficients, offVector));
      sum += on - off;
    }
  }
  return rows.length === 0 ? 0 : sum / rows.length;
};

/* AKD (S10-C-2): off value deduped per canonical key within one sweep.
 * Same-key rows hold identical vector contents, so slice+zero+dot yields
 * the identical double; the first active row with a key pays the O(p) copy
 * and dot, duplicates reuse the stored double. The per-row addition
 * `sum += on - off` keeps every term, value, and order unchanged. */
const apcAkd: ApcImpl = (design, rows, vectors, _supports, keys, scratch, coefficients, onProbabilities, column) => {
  let sum = 0;
  const columnIdx = design.columnIndex.get(column);
  if (columnIdx !== undefined && columnIdx !== 0) {
    const s = scratch!;
    s.mark += 1;
    const mark = s.mark;
    for (let i = 0; i < rows.length; i++) {
      if (vectors[i]![columnIdx] === 0) continue;
      const on = onProbabilities[i]!;
      const key = keys[i]!;
      let off: number;
      if (s.stamp[key] !== mark) {
        s.stamp[key] = mark;
        const offVector = vectors[i]!.slice();
        offVector[columnIdx] = 0;
        off = sigmoid(dot(coefficients, offVector));
        s.off[key] = off;
      } else {
        off = s.off[key]!;
      }
      sum += on - off;
    }
  }
  return rows.length === 0 ? 0 : sum / rows.length;
};

/* ---------------------- rest of the verbatim pipeline ---------------------- */

const onProb = (vectors: readonly number[][], coefficients: readonly number[]): number[] =>
  vectors.map((vector) => sigmoid(dot(coefficients, vector)));

function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}

function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}

function pushValue(map: Map<string, number[]>, key: string, value: number): void {
  const list = map.get(key);
  if (list === undefined) map.set(key, [value]);
  else list.push(value);
}

function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

function fitWith(
  pipeline: Pipeline,
  rows: readonly OfflineRow[],
  options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
): AttributionReport {
  const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
  const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
  const seed = options?.seed ?? SEED_DEFAULT;

  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const effects: Array<{ name: string; point: number; lcb: number; ucb: number }> = [];

  if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
  }

  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const scratch: ApcScratch | null = pipeline.wantsScratch
    ? { stamp: new Int32Array(baseRows.length), off: new Float64Array(baseRows.length), mark: 0 }
    : null;
  const fit = pipeline.irls(design, baseRows, vectors, supports, keys, baseRows.length, maxIter, pipeline.solve);
  if (fit.coefficients === null) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
  }

  const onProbabilities = onProb(vectors, fit.coefficients);
  const pointEffects = new Map<string, number>();
  for (const name of design.names) {
    if (name === "intercept") continue;
    pointEffects.set(
      name,
      pipeline.apc(design, baseRows, vectors, supports, keys, scratch, fit.coefficients, onProbabilities, name)
    );
  }
  for (const ref of design.referenceLevels) {
    if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
      pointEffects.set(`${ref.factor}:${ref.name}`, 0);
    }
  }

  const random = rng(seed);
  const draws = new Map<string, number[]>();
  let successful = 0;
  for (let draw = 0; draw < bootstrapDraws; draw++) {
    const sample: Row[] = [];
    const sampleVectors: number[][] = [];
    const sampleSupports: number[][] = [];
    const sampleKeys: number[] = [];
    for (let i = 0; i < baseRows.length; i++) {
      const index = Math.floor(random() * baseRows.length);
      sample.push(baseRows[index]!);
      sampleVectors.push(vectors[index]!);
      sampleSupports.push(supports[index]!);
      sampleKeys.push(keys[index]!);
    }
    if (sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1)) continue;
    const bootFit = pipeline.irls(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter, pipeline.solve);
    if (bootFit.coefficients === null) continue;
    successful += 1;
    const sampleOnProbabilities = onProb(sampleVectors, bootFit.coefficients);
    for (const [name] of pointEffects.entries()) {
      const value = pipeline.apc(
        design,
        sample,
        sampleVectors,
        sampleSupports,
        sampleKeys,
        scratch,
        bootFit.coefficients,
        sampleOnProbabilities,
        name
      );
      pushValue(draws, name, value);
    }
  }

  for (const [name, point] of pointEffects.entries()) {
    const values = draws.get(name) ?? [];
    if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
    }
    values.sort((a, b) => a - b);
    effects.push({
      name,
      point,
      lcb: percentile(values, 0.025),
      ucb: percentile(values, 0.975)
    });
  }

  const n = baseRows.length;
  const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
  const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
  const muLcb = betaQuantileLcb(muPosterior, 0.05);
  const models = new Set(baseRows.map((r) => r.modelVersion)).size;
  const projects = new Set(baseRows.map((r) => r.projectId)).size;
  const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
  const containsZero = (e: { lcb: number; ucb: number }): boolean =>
    e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
  const interactionsFor = (prefix: string): Array<{ name: string; lcb: number; ucb: number }> =>
    effects.filter((e) => e.name.startsWith(`w:${prefix}`));

  let diagnosis: AttributionReport["diagnosis"] = "uncertain";
  const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
  if (
    effects.some(
      (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
    )
  ) {
    diagnosis = "model-problem";
  } else if (
    effects.some(
      (e) =>
        e.name.startsWith("v:") &&
        e.lcb < -ATTRIBUTION_EFFECT &&
        effects
          .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
          .every(containsZero)
    )
  ) {
    diagnosis = "project-problem";
  } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
    diagnosis = "interaction-only";
  } else if (scenarioHard) {
    diagnosis = "scenario-hard";
  }

  return {
    estimator: "logit-additive",
    rowsUsed: baseRows.length,
    effects,
    diagnosis,
    reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
    writesActivePointer: false
  };
}

/* ------------------------------ harness ------------------------------ */

let checksPassed = 0;
let failures = 0;

function check(label: string, ok: boolean): void {
  if (ok) checksPassed++;
  else {
    failures++;
    process.stderr.write(`FAIL ${label}\n`);
  }
}

function compareReports(label: string, expected: AttributionReport, actual: AttributionReport): void {
  check(`${label}.estimator`, expected.estimator === actual.estimator);
  check(`${label}.rowsUsed`, expected.rowsUsed === actual.rowsUsed);
  check(`${label}.diagnosis`, expected.diagnosis === actual.diagnosis);
  check(`${label}.reason`, expected.reason === actual.reason);
  check(`${label}.writesActivePointer`, expected.writesActivePointer === actual.writesActivePointer);
  check(`${label}.effects.length`, expected.effects.length === actual.effects.length);
  if (expected.effects.length === actual.effects.length) {
    for (let i = 0; i < expected.effects.length; i++) {
      const e = expected.effects[i]!;
      const g = actual.effects[i]!;
      check(`${label}.effects[${i}].name`, e.name === g.name);
      check(`${label}.effects[${i}].point`, Object.is(e.point, g.point));
      check(`${label}.effects[${i}].lcb`, Object.is(e.lcb, g.lcb));
      check(`${label}.effects[${i}].ucb`, Object.is(e.ucb, g.ucb));
    }
  }
}

function compareFits(label: string, expected: FitResult, actual: FitResult): void {
  const en = expected.coefficients === null;
  const an = actual.coefficients === null;
  check(`${label}.null`, en === an);
  if (expected.coefficients !== null && actual.coefficients !== null) {
    check(`${label}.length`, expected.coefficients.length === actual.coefficients.length);
    if (expected.coefficients.length === actual.coefficients.length) {
      for (let i = 0; i < expected.coefficients.length; i++) {
        check(`${label}[${i}]`, Object.is(expected.coefficients[i], actual.coefficients[i]));
      }
    }
  }
}

function compareSolutions(label: string, expected: number[] | null, actual: number[] | null): void {
  check(`${label}.null`, (expected === null) === (actual === null));
  if (expected !== null && actual !== null) {
    check(`${label}.length`, expected.length === actual.length);
    if (expected.length === actual.length) {
      for (let i = 0; i < expected.length; i++) {
        check(`${label}[${i}]`, Object.is(expected[i], actual[i]));
      }
    }
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}

function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRow[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function allUniqueRows(r: () => number, count: number): OfflineRow[] {
  const rows: OfflineRow[] = [];
  for (let i = 0; i < count; i++) {
    rows.push({
      scenarioId: `s${i % 2}`,
      modelVersion: `m${i}`,
      projectId: `p${i}`,
      y: r() < 0.6 ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function batteryCases(): Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];

  for (let i = 0; i < 40; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, {
      scenarios: 2,
      models: 3,
      projects: 3,
      rows: 60,
      passRate: 0.55,
      pipeInModel: true,
    }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  cases.push({
    rows: allUniqueRows(r, 36),
    options: { bootstrap: 40, seed: 23 },
  });
  {
    const oneKey: OfflineRow[] = [];
    for (let i = 0; i < 30; i++) {
      oneKey.push({
        scenarioId: "s0",
        modelVersion: "m0",
        projectId: "p0",
        y: i % 3 === 0 ? 0 : 1,
        occurredAtMs: 1_000 + i,
      });
    }
    cases.push({ rows: oneKey, options: { bootstrap: 40, seed: 31 } });
  }
  cases.push({
    rows: [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1, occurredAtMs: 1_000 },
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 0, occurredAtMs: 1_001 },
    ],
    options: { bootstrap: 30, seed: 5 },
  });
  return cases;
}

const out = (line: string): void => {
  process.stdout.write(`${line}\n`);
};

const fail = (line: string): void => {
  process.stderr.write(`${line}\n`);
  process.exitCode = 1;
};

const CTL: Pipeline = { irls: irlsCtl, solve: solveCtl, apc: apcCtl, wantsScratch: false };
const LANE_DKL: Pipeline = { irls: irlsDkl, solve: solveCtl, apc: apcCtl, wantsScratch: false };
const LANE_AKD: Pipeline = { irls: irlsCtl, solve: solveCtl, apc: apcAkd, wantsScratch: true };
/* CMB: DKL + AKD stacked — the maximal in-scope candidate composition, to
 * bound whether the two sub-band candidates can jointly cross the band. */
const LANE_CMB: Pipeline = { irls: irlsDkl, solve: solveCtl, apc: apcAkd, wantsScratch: true };

const VARIANTS: Array<[string, Pipeline]> = [
  ["DKL", LANE_DKL],
  ["AKD", LANE_AKD],
  ["CMB", LANE_CMB],
];

/* -------- scenario 1: direct irls bitwise equivalence (s = 1..8) -------- */

function directDesign(p: number): Design {
  return {
    names: Array.from({ length: p }, (_, i) => `c${i}`),
    columnIndex: new Map(),
    referenceLevels: [],
    build(): number[] {
      throw new Error("direct-irls fixtures never call design.build");
    }
  };
}

function directKeys(vectors: readonly number[][]): number[] {
  const seen = new Map<string, number>();
  const keys = new Array<number>(vectors.length);
  for (let i = 0; i < vectors.length; i++) {
    const sig = vectors[i]!.join(",");
    const canonical = seen.get(sig);
    if (canonical === undefined) {
      seen.set(sig, i);
      keys[i] = i;
    } else {
      keys[i] = canonical;
    }
  }
  return keys;
}

function scenarioDirectIrls(): void {
  const r = fixtureRng(0xac01);
  let fixtures = 0;
  const supportSizes = new Set<number>();
  for (let t = 0; t < 24; t++) {
    const p = 6 + Math.floor(r() * 7);
    const n = 5 + Math.floor(r() * 36);
    const vectors: number[][] = [];
    const rows: Row[] = [];
    for (let j = 0; j < n; j++) {
      if (j > 0 && r() < 0.35) {
        const k = Math.floor(r() * j);
        vectors.push(vectors[k]!);
      } else {
        const vec = new Array<number>(p).fill(0);
        vec[0] = 1;
        const extra = (j + t) % 8;
        const cols = new Set<number>();
        while (cols.size < Math.min(extra, p - 1)) {
          cols.add(1 + Math.floor(r() * (p - 1)));
        }
        for (const c of cols) vec[c] = 1;
        vectors.push(vec);
      }
      rows.push({ scenarioId: "s", modelVersion: "m", projectId: "p", y: r() < 0.5 ? 1 : 0 });
    }
    const supports = computeSupports(vectors);
    for (const s of supports) supportSizes.add(s.length);
    const keys = directKeys(vectors);
    const design = directDesign(p);
    const maxIter = t % 3 === 0 ? 1 : t % 3 === 1 ? 3 : 50;
    const expected = irlsCtl(design, rows, vectors, supports, keys, n, maxIter, solveCtl);
    compareFits(`direct[${t}].DKL`, expected, irlsDkl(design, rows, vectors, supports, keys, n, maxIter, solveCtl));
    fixtures++;
  }
  for (let s = 1; s <= 6; s++) {
    check(`direct.support-size-${s}-covered`, supportSizes.has(s));
  }
  out(
    `scenario 1 (direct irls bitwise equivalence, ${fixtures} fixtures x {DKL} vs ` +
      `verbatim landed-S7-C irls/solve; support sizes: ${[...supportSizes].sort((a, b) => a - b).join(",")})`
  );
}

/* -- scenario 1b: ±0.0 / beta=0 / empty-support adversarial irls fixtures -- */

function scenarioSignedZeroAdversarial(): void {
  // beta0-truncated: maxIter=1 pins the beta=0 first iteration.
  {
    const p = 8;
    const vectors: number[][] = [];
    const rows: Row[] = [];
    for (let j = 0; j < 12; j++) {
      const vec = new Array<number>(p).fill(0);
      vec[0] = 1;
      vec[1 + (j % (p - 1))] = 1;
      vectors.push(vec);
      rows.push({ scenarioId: "s", modelVersion: "m", projectId: "p", y: (j % 2) as 0 | 1 });
    }
    const supports = computeSupports(vectors);
    const keys = directKeys(vectors);
    const design = directDesign(p);
    const expected = irlsCtl(design, rows, vectors, supports, keys, rows.length, 1, solveCtl);
    compareFits("adv.beta0.DKL", expected, irlsDkl(design, rows, vectors, supports, keys, rows.length, 1, solveCtl));
  }
  // negative-beta: an all-fail dummy drives strongly negative coefficients.
  {
    const p = 6;
    const vectors: number[][] = [];
    const rows: Row[] = [];
    for (let j = 0; j < 40; j++) {
      const vec = new Array<number>(p).fill(0);
      vec[0] = 1;
      const bad = j % 2 === 0;
      if (bad) vec[1] = 1;
      else vec[2] = 1;
      if (j % 3 === 0) vec[3] = 1;
      vectors.push(vec);
      rows.push({ scenarioId: "s", modelVersion: "m", projectId: "p", y: bad ? 0 : 1 });
    }
    const supports = computeSupports(vectors);
    const keys = directKeys(vectors);
    const design = directDesign(p);
    const expected = irlsCtl(design, rows, vectors, supports, keys, rows.length, 50, solveCtl);
    compareFits("adv.negbeta.DKL", expected, irlsDkl(design, rows, vectors, supports, keys, rows.length, 50, solveCtl));
  }
  // empty-support: an all-zero vector (s = 0) pins the lemma endpoint.
  {
    const p = 5;
    const vectors: number[][] = [];
    const rows: Row[] = [];
    for (let j = 0; j < 10; j++) {
      const vec = new Array<number>(p).fill(0);
      if (j !== 4) {
        vec[0] = 1;
        vec[1 + (j % (p - 1))] = 1;
      }
      vectors.push(vec);
      rows.push({ scenarioId: "s", modelVersion: "m", projectId: "p", y: (j % 2) as 0 | 1 });
    }
    const supports = computeSupports(vectors);
    const keys = directKeys(vectors);
    const design = directDesign(p);
    const expected = irlsCtl(design, rows, vectors, supports, keys, rows.length, 3, solveCtl);
    compareFits("adv.emptysup.DKL", expected, irlsDkl(design, rows, vectors, supports, keys, rows.length, 3, solveCtl));
  }
  out("scenario 1b (adversarial signed-zero / beta=0 / empty-support fixtures x {DKL})");
}

/* ---- scenario 1c: AKD adversarial component checks (dedup-heavy) ---- */

function scenarioAkdComponents(): void {
  const r = fixtureRng(0xac03);
  const coefSets: Array<[string, number[]]> = [
    ["all-negative", Array.from({ length: 12 }, (_, j) => -(j + 1) * 0.37)],
    ["mixed-sign", Array.from({ length: 12 }, (_, j) => (j % 2 === 0 ? 1 : -1) * (j + 0.5))],
    ["tiny", Array.from({ length: 12 }, (_, j) => (j % 2 === 0 ? 1 : -1) * 5e-324 * (j + 1))],
    ["zeros", Array.from({ length: 12 }, () => 0)],
    ["random", Array.from({ length: 12 }, () => r() * 8 - 4)],
  ];
  // Duplicate-heavy vector set: ~40% of rows share a reference with an
  // earlier row (same canonical key), plus an all-zero row (empty support).
  const vectors: number[][] = [];
  for (let i = 0; i < 30; i++) {
    if (i > 0 && r() < 0.4) {
      vectors.push(vectors[Math.floor(r() * i)]!);
      continue;
    }
    const vec = new Array<number>(12).fill(0);
    if (i !== 7) vec[0] = 1;
    const extra = i % 6;
    const cols = new Set<number>();
    while (cols.size < extra) cols.add(1 + Math.floor(r() * 11));
    for (const c of cols) vec[c] = 1;
    vectors.push(vec);
  }
  const supports = computeSupports(vectors);
  const keys = directKeys(vectors);
  const design = {
    names: Array.from({ length: 12 }, (_, i) => (i === 0 ? "intercept" : `c${i}`)),
    columnIndex: new Map(Array.from({ length: 12 }, (_, i) => [i === 0 ? "intercept" : `c${i}`, i] as const)),
    referenceLevels: [],
    build(): number[] {
      throw new Error("component fixtures never call design.build");
    }
  } satisfies Design;
  const rows: Row[] = vectors.map((_, i) => ({
    scenarioId: "s",
    modelVersion: "m",
    projectId: "p",
    y: (i % 2) as 0 | 1
  }));

  const scratch: ApcScratch = { stamp: new Int32Array(rows.length), off: new Float64Array(rows.length), mark: 0 };
  let dedupHits = 0;
  for (const [name, coef] of coefSets) {
    const on = onProb(vectors, coef);
    for (const column of design.names) {
      const expectedApc = apcCtl(design, rows, vectors, supports, keys, null, coef, on, column);
      const actualApc = apcAkd(design, rows, vectors, supports, keys, scratch, coef, on, column);
      check(`akd-comp[${name}].apc[${column}]`, Object.is(expectedApc, actualApc));
    }
    check(
      `akd-comp[${name}].apc[missing]`,
      Object.is(
        apcCtl(design, rows, vectors, supports, keys, null, coef, on, "nope"),
        apcAkd(design, rows, vectors, supports, keys, scratch, coef, on, "nope")
      )
    );
  }
  // Count dedup hits on this fixture for the report (per column sweep).
  {
    const columnIdx = 1;
    const seenKeys = new Set<number>();
    for (let i = 0; i < rows.length; i++) {
      if (vectors[i]![columnIdx] === 0) continue;
      if (seenKeys.has(keys[i]!)) dedupHits++;
      else seenKeys.add(keys[i]!);
    }
  }
  check(
    "akd-comp.empty-rows",
    Object.is(
      apcCtl(design, [], [], [], [], null, coefSets[0]![1], [], "c1"),
      apcAkd(design, [], [], [], [], scratch, coefSets[0]![1], [], "c1")
    )
  );
  out(
    `scenario 1c (AKD adversarial component checks: ${coefSets.length} coefficient sets x ` +
      `30 duplicate-heavy vectors incl. empty support, x 13 APC columns; all Object.is; ` +
      `c1-sweep dedup hits on fixture: ${dedupHits})`
  );
}

/* -- scenario 1d: solve drift anchor + SYM (S10-C-3) counterexample probe -- */

function scenarioSolveAnchorAndSym(): void {
  const r = fixtureRng(0xac02);
  let fixtures = 0;
  const runOne = (label: string, a: number[][], b: number[]): void => {
    compareSolutions(`solve[${label}].prod`, solveCtl(a, b), solveSymmetric(a, b));
    fixtures++;
  };
  for (let t = 0; t < 20; t++) {
    const n = 1 + Math.floor(r() * 12);
    const a: number[][] = [];
    for (let i = 0; i < n; i++) {
      const row: number[] = [];
      for (let j = 0; j < n; j++) row.push(r() * 4 - 2);
      a.push(row);
    }
    if (t % 3 === 0 && n >= 2) a[n - 1]![0] = 1e6;
    if (t % 5 === 0) {
      for (let i = 0; i < n; i++) for (let j = 0; j < i; j++) a[i]![j] = a[j]![i]!;
    }
    const b = Array.from({ length: n }, () => r() * 2 - 1);
    runOne(`rand${t}`, a, b);
  }
  runOne("singular2", [[1, 2], [2, 4]], [1, 2]);
  runOne("nan-diag", [[Number.NaN, 1], [1, 2]], [1, 1]);
  runOne("signed-zero", [[2, -0, 1], [-0, 3, -0], [1, -0, 4]], [-0, 1, -0]);
  runOne("n0", [], []);
  runOne("n1", [[3]], [6]);
  {
    let msgCtl = "";
    let msgProd = "";
    try { solveCtl([[1, 2]], [1, 2]); } catch (e) { msgCtl = (e as Error).message; }
    try { solveSymmetric([[1, 2]], [1, 2]); } catch (e) { msgProd = (e as Error).message; }
    check("solve.throw.same", msgCtl !== "" && msgCtl === msgProd);
  }

  // SYM probe: the mirrored elimination update fl(fl(x/p)*y) must equal the
  // independently computed fl(fl(y/p)*x) for the halving to be bitwise. It
  // does not: division rounding does not commute through multiplication.
  let mismatches = 0;
  let first: string | null = null;
  const probes = 10000;
  for (let t = 0; t < probes; t++) {
    const x = r() * 8 - 4;
    const y = r() * 8 - 4;
    const p = r() * 4 + 0.1;
    const mirrored = (x / p) * y;
    const independent = (y / p) * x;
    if (!Object.is(mirrored, independent)) {
      mismatches++;
      if (first === null) {
        first = `x=${x} y=${y} p=${p} -> ${mirrored} vs ${independent}`;
      }
    }
  }
  check("sym.counterexample-found", mismatches > 0);
  out(
    `scenario 1d (solve drift anchor: ${fixtures} matrices x {prod} vs verbatim landed solve; ` +
      `SYM probe: ${mismatches}/${probes} random (x,y,p) triples give fl(fl(x/p)*y) != fl(fl(y/p)*x); ` +
      `first: ${first})`
  );
}

/* -------------- scenario 2: full-report bitwise equivalence -------------- */

function scenarioEquivalence(): void {
  const cases = batteryCases();
  for (const [index, testCase] of cases.entries()) {
    const expected = fitWith(CTL, testCase.rows, testCase.options);
    compareReports(`R10C-prod[${index}]`, expected, fitLogitAdditive(testCase.rows, testCase.options));
    for (const [name, pipeline] of VARIANTS) {
      compareReports(`R10C-${name}[${index}]`, expected, fitWith(pipeline, testCase.rows, testCase.options));
    }
  }
  out(
    `scenario 2 (full-report bitwise equivalence, ${cases.length} cases x {production, DKL, AKD, CMB} ` +
      `vs verbatim landed-S7-C pipeline)`
  );
}

/* ------------------- scenario 3: component benchmarks ------------------- */

function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

function perfRows(): OfflineRow[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, {
    scenarios: 4,
    models: 6,
    projects: 8,
    rows: 400,
    passRate: 0.6,
  });
}

const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

function scenarioComponents(): void {
  const rows = perfRows();
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irlsCtl(design, baseRows, vectors, supports, keys, baseRows.length, MAX_ITER_DEFAULT, solveCtl);
  const coef = fit.coefficients!;
  const p = design.names.length;

  // S10-C-2 component: APC full sweep (59 non-intercept names), slice+dot
  // per active pair vs per-key dedup, on the real fixture shapes.
  {
    const on = onProb(vectors, coef);
    const names = design.names.filter((name) => name !== "intercept");
    let activePairs = 0;
    let distinctPairs = 0;
    for (const name of names) {
      const columnIdx = design.columnIndex.get(name)!;
      const seenKeys = new Set<number>();
      for (let i = 0; i < baseRows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        activePairs++;
        if (!seenKeys.has(keys[i]!)) {
          seenKeys.add(keys[i]!);
          distinctPairs++;
        }
      }
    }
    const scratch: ApcScratch = { stamp: new Int32Array(baseRows.length), off: new Float64Array(baseRows.length), mark: 0 };
    // Bitwise check on the real fixture before timing.
    for (const name of names) {
      check(
        `S10C2.sweep[${name}]`,
        Object.is(
          apcCtl(design, baseRows, vectors, supports, keys, null, coef, on, name),
          apcAkd(design, baseRows, vectors, supports, keys, scratch, coef, on, name)
        )
      );
    }
    const ctlCost = bench(() => {
      for (const name of names) apcCtl(design, baseRows, vectors, supports, keys, null, coef, on, name);
    }, 300);
    const akdCost = bench(() => {
      for (const name of names) apcAkd(design, baseRows, vectors, supports, keys, scratch, coef, on, name);
    }, 300);
    out(
      `S10-C-2 bench APC full sweep (${names.length} names, ${activePairs} active pairs, ` +
        `${distinctPairs} distinct-by-key pairs = ${((1 - distinctPairs / activePairs) * 100).toFixed(1)}% dup fraction, p=${p}): ` +
        `slice+dot=${(ctlCost * 1e3).toFixed(1)}us per-key-dedup=${(akdCost * 1e3).toFixed(1)}us ` +
        `delta=${((ctlCost - akdCost) * 1e3).toFixed(2)}us -> per report (x201 sweeps) ` +
        `~${((ctlCost - akdCost) * 201).toFixed(2)}ms ` +
        `(S8-C-2 full-elimination ceiling was 22.16-23.59ms, in-band)`
    );
  }

  // S10-C-1 arithmetic bound: the stamp scan pays 1 typed load + 1 compare
  // per row visit; DKL replaces that with distinct-key loop control.
  {
    out(
      `S10-C-1 bound: stamp-scan traffic = rowVisits x (1 Int32 load + 1 compare); ` +
        `see site accounting below — at 1ns/op the whole class is single-digit ms ` +
        `(strictly below the S9-C-3 row-buffer class ceiling ~14ms, itself sub-band)`
    );
  }
}

/* --------------------------- site accounting --------------------------- */

function siteAccounting(): void {
  const rows = perfRows();
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const p = design.names.length;
  const n = baseRows.length;

  let fits = 0;
  let iterations = 0;
  const countingIrls: IrlsImpl = (d, r2, v, s, k, ks, mi, sv) => {
    fits++;
    const inner: SolveImpl = (a2, b2) => {
      iterations++;
      return sv(a2, b2);
    };
    return irlsCtl(d, r2, v, s, k, ks, mi, inner);
  };
  const countingPipeline: Pipeline = { irls: countingIrls, solve: solveCtl, apc: apcCtl, wantsScratch: false };
  fitWith(countingPipeline, rows, PERF_OPTIONS);

  const sBar = supports.reduce((acc, s) => acc + s.length, 0) / supports.length;
  const rowVisits = iterations * n;
  const distinctBase = new Set(keys).size;
  out(
    `site accounting: fits=${fits} irlsIterations=${iterations} p=${p} n=${n} sBar=${sBar.toFixed(3)} ` +
      `distinctKeys(base)=${distinctBase}; row visits/report=${rowVisits} ` +
      `(S10-C-1 drops 1 Int32 load + 1 compare each = ${rowVisits * 2} ops/report, ` +
      `~${(rowVisits * 2 / 1e6).toFixed(1)}ms at 1ns/op)`
  );

  // Configured-state matrix probe (R7-I lesson): small shape stays sub-band.
  const shapes: Array<[string, OfflineRow[]]> = [
    ["small (3 models x 3 projects, 120 rows)", randomRows(fixtureRng(0x51), { scenarios: 2, models: 3, projects: 3, rows: 120, passRate: 0.6 })],
    ["perf (6 models x 8 projects, 400 rows)", rows],
  ];
  for (const [label, shapeRows] of shapes) {
    const times: number[] = [];
    fitWith(CTL, shapeRows, PERF_OPTIONS);
    for (let i = 0; i < 3; i++) {
      const t0 = performance.now();
      fitWith(CTL, shapeRows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    times.sort((a, b) => a - b);
    out(`  shape ${label}: median ${times[1]!.toFixed(1)} ms/report`);
  }
}

/* --------------------------- performance fixture --------------------------- */

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

function perfFixtureMultiway(): void {
  const rows = perfRows();

  const expected = fitWith(CTL, rows, PERF_OPTIONS);
  compareReports("perf-fixture.prod", expected, fitLogitAdditive(rows, PERF_OPTIONS));
  for (const [name, pipeline] of VARIANTS) {
    compareReports(`perf-fixture.${name}`, expected, fitWith(pipeline, rows, PERF_OPTIONS));
  }

  const racers: Array<[string, () => AttributionReport]> = [
    ["ctl (landed S7-C)", (): AttributionReport => fitWith(CTL, rows, PERF_OPTIONS)],
    ["production", (): AttributionReport => fitLogitAdditive(rows, PERF_OPTIONS)],
    ["VAR-DKL", (): AttributionReport => fitWith(LANE_DKL, rows, PERF_OPTIONS)],
    ["VAR-AKD", (): AttributionReport => fitWith(LANE_AKD, rows, PERF_OPTIONS)],
    ["VAR-CMB", (): AttributionReport => fitWith(LANE_CMB, rows, PERF_OPTIONS)],
  ];
  const times = racers.map(() => [] as number[]);
  for (let rep = 0; rep < 7; rep++) {
    for (let v = 0; v < racers.length; v++) {
      const t0 = performance.now();
      racers[v]![1]();
      times[v]!.push(performance.now() - t0);
    }
  }
  const ctlMs = median(times[0]!);
  out(`perf fixture (rows=400, bootstrap=200), median of 7 interleaved reps (multi-way, relative order only):`);
  for (let v = 0; v < racers.length; v++) {
    const ms = median(times[v]!);
    out(`  ${racers[v]![0].padEnd(20)} ${ms.toFixed(1)} ms  (${(ctlMs / ms).toFixed(2)}x vs ctl)`);
  }
}

/* ------------------------------ duel mode ------------------------------ */

const LANES: Record<string, (rows: readonly OfflineRow[]) => AttributionReport> = {
  ctl: (rows) => fitWith(CTL, rows, PERF_OPTIONS),
  prod: (rows) => fitLogitAdditive(rows, PERF_OPTIONS),
  dkl: (rows) => fitWith(LANE_DKL, rows, PERF_OPTIONS),
  akd: (rows) => fitWith(LANE_AKD, rows, PERF_OPTIONS),
  cmb: (rows) => fitWith(LANE_CMB, rows, PERF_OPTIONS),
};

function duel(a: string, b: string): void {
  const rows = perfRows();
  const laneA = LANES[a]!;
  const laneB = LANES[b]!;
  compareReports(`duel.${a}-vs-${b}`, laneA(rows), laneB(rows));
  const ta: number[] = [];
  const tb: number[] = [];
  for (let rep = 0; rep < 7; rep++) {
    let t0 = performance.now();
    laneA(rows);
    ta.push(performance.now() - t0);
    t0 = performance.now();
    laneB(rows);
    tb.push(performance.now() - t0);
  }
  const ma = median(ta);
  const mb = median(tb);
  out(
    `duel ${a} vs ${b}: ${a}=${ma.toFixed(1)}ms ${b}=${mb.toFixed(1)}ms ` +
      `delta=${(ma - mb).toFixed(1)}ms (${(ma / mb).toFixed(3)}x)`
  );
  if (failures > 0) {
    fail(`\n${failures} EQUIVALENCE CHECK(S) FAILED (${checksPassed} passed)`);
  } else {
    out(`duel equivalence: ${checksPassed} bitwise checks passed`);
  }
}

/* ------------------------------- main ------------------------------- */

const duelArg = process.argv.indexOf("--duel");
if (duelArg !== -1) {
  const [a, b] = (process.argv[duelArg + 1] ?? "").split(",");
  if (a === undefined || b === undefined || !(a in LANES) || !(b in LANES)) {
    fail(`usage: --duel <${Object.keys(LANES).join("|")}>,<lane>`);
  } else {
    duel(a, b);
  }
} else {
  scenarioDirectIrls();
  scenarioSignedZeroAdversarial();
  scenarioAkdComponents();
  scenarioSolveAnchorAndSym();
  scenarioEquivalence();
  scenarioComponents();
  siteAccounting();
  perfFixtureMultiway();
  if (failures > 0) {
    fail(`\n${failures} EQUIVALENCE CHECK(S) FAILED (${checksPassed} passed)`);
  } else {
    out(`\nALL EQUIVALENCE CHECKS PASSED (${checksPassed} bitwise checks)`);
  }
}
```

MORE_OPTIMA=no
BRANCH=cursor/r10-c-offline-routing-tenth-pass-83a1
