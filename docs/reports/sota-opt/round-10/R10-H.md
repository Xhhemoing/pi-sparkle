MODEL_SLUG=claude-fable-5-thinking-xhigh

# R10-H：`src/evaluation/` + `src/requirement/` + `src/review/` + `src/rubric/` 第十遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 10 / R10-H
**基线:** `cursor/sota-persistent-opt-83a1` @ `004aa20`（独占 tip，含 S10-A-1 / S10-B-1..3 / S10-C-1..3 排除全表；R9-I / R10-E / R10-F 空枚举收口）
**分支:** `cursor/r10-h-eval-tenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十遍枚举为空：无新候选、零新 ID、切片零代码改动、切片维持关闭。**
切片 21 个文件（1750 行，本轮 `wc -l` 复核一致；8+7+4+2 清单逐目录确认）
自 R1-H 基线（`fd437a9`）以来**逐字节未变**（`git diff fd437a9..004aa20
-- src/{evaluation,requirement,review,rubric}/` 为空，十遍全程零 diff），
且 R9-H 基线（`af7a423`）之后 `src/` **零提交**（`git log af7a423..004aa20
-- src/` 为空——其间全部提交均为 R9-G/R9-H/R9-J/R9-I 与 R10 各波报告及
排除表文档；R10-A/B/C/E/F 全数无落地）⇒ 生产调用方地图**可证不变**
（最强形式），本轮全库 import 交叉检索再次独立确认（8 个导入位点与
R3-H…R9-H 完全一致；`src/evaluation/` 全部 8 文件、
`review/{pairwise,reconcile,critic}.ts`、`src/rubric/` 及
`applyRoutingScoreUpdate`、`buildContractFromSources` 仍零生产调用方——
本轮定向 rg 证实其值级引用全部落在切片自身与 `test/`）。按指令在本 VM
重测「配置态（3 档）× 命令类（6 类）× 执行层（热/冷）」满格矩阵
（§1.3–§1.5，三次独立运行）：热层默认态 **9.9–10.1µs**/run、配置态 A
**8.2–9.0µs**、配置态 B **4.0–4.6µs**（两档配置态三次全部 ≤ 默认态 ⇒
R8-H/R9-H「无配置态悬崖、默认锚点是保守上界」在本 VM 复现；绝对值落在
十轮历史跨 VM 带 3.9–10µs 的上沿，属测量环境差异而非调用图变更——
§1.2 已证零变更）；晋升类 18.2–20.2ns；四个零生产调用方命令类
（evaluate 1.12–1.17µs / critique 0.62–0.64µs / pairwise 2.0–2.2µs /
reconcile 68–73ns）**全部 ≤2.2µs**——调用图未变下即便未来接线也不达
落地线（数十~数百 ms）；冷层首调默认 1706–1715µs、配置 1628–1651µs
once-per-process（与 R6-H…R9-H 同带，否决类归属不变）。第十遍在完整
排除表（S1-H-1..9、S2-H-1..7、S3-H-1..4、S4-H-1..3、S5-H-1..3、
S6-H-1..4、S7-H-1..3、S8-H-1..3、S9-H-1..2 及九轮 40+ 处不立 ID 收口）
之上以**导出面完备性审计**收口：切片全部 30+ 个值级导出逐一对账（§2），
每一个都已有裁决记录或落入已判家族；本轮新检视的六处从未点名微观位点
（excerpt SlicedString 驻留、PATH_RE /g lastIndex 安全性、createRubric
时间戳路径、validateEvaluatorScope String() 分配、三个 adapter 输入
校验合取重排、classifyDiffScope 控制流去糖）全部以既有排除/契约/量级
直接覆盖，**无一达到立 ID 门槛**（§2.2）。诚实结论：枚举为空，不硬凑
ID。未重开任何 X* / S1-* … S9-* / S10-A/B/C-* 条目；S5-H-1 守卫经本轮
diff + 读码双复核**原样保留**（§0 换名重提检查）；S8-H-3 全决议快路径
**未做任何形态**（fail-open 反例维持钉死）。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/evaluation/`（8 文件：evaluator / delivery-adapter /
  diff-adapter / ownership / precedence / check-adapter / adapters /
  types）、`src/requirement/`（7 文件：normalizer / provenance /
  precedence / extractor / heuristic / coverage / critic）、
  `src/review/`（4 文件：pairwise / self-review / reconcile / critic）、
  `src/rubric/`（2 文件：registry / types）计 21 文件全量第十遍实际
  读码，未依赖前九轮记忆。上下游 `track/{clarify,loop,plan,primary-split}.ts`、
  `run/{supervisor,coordinator,flowchart-run}.ts`、
  `adaptation/{promotion,promotion-rules}.ts` 只读取证，一行未改。
  切片外其余树零触碰。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表 529 行，含
  S10-A-1 / S10-B-1..3 / S10-C-1..3）/ round-10/PLAN.md /
  round-09/PLAN.md + R9-H.md / R8-H … R1-H。
- **换名重提检查**（逐条核对，全部未列为新候选、未触碰）：
  - **S5-H-1（必须保留项）**：`detectConflicts` 的分配前守卫方向两面
    钉死维持（S5-H-1 显式预守卫付扫描费 / S6-H-1 序贯守卫付布局费）。
    现行 `requirement/precedence.ts` L24 的
    `if (fast.length === 0 || slow.length === 0) return [];` 早退经
    本轮读码 + `git diff fd437a9..HEAD -- src/requirement/precedence.ts`
    （空）双复核**原样在位、逐字节未变**。本轮对该函数零候选、零
    「优化掉」提案。
  - **S8-H-3（永不重开项）**：`assertCoverageAllowsStart` 未做任何
    全决议快路径（包括任何变形：resolved-superset 预判、gated 分支
    条件跳过、blocking 预扫描短路）。schema 合法 `default:""` /
    `options:[""]` 的 fail-open 反例维持钉死；「resolved ⇒ 非
    blocking」仍是无处强制的不变量。本轮 `coverage.ts` 零 diff。
  - S8-H-1：`PairwiseInput` 全形状（含 comment 双字段）未收窄；盲评
    链 PIC 二态教训未重演。S9-H-1/S9-H-2：`self-review.ts` 三函数
    （assertNotSelfReview / applyRoutingScoreUpdate /
    assertCanPromoteFromReview）裁决记录完备，本轮零重提（死中间体
    = 逃逸分析地板；spread 单态化 = 晋升频次墙）。
  - S1-H-9 / S2-H-4 / S3-H-4：`changeSetsEqual` 三面钉死维持，零候选。
    S1-H-8 / S2-H-6：`registerRubric` copy-on-write 维持
    （`{...DEFAULT_REGISTRY}` 浅拷贝别名安全性依赖全部写路径整体替换
    registry，本轮读码再确认成立）。S6-H-2/3、S1-H-1/2、S4-H-1：
    coverage 六面钉死维持。S7-H-1/2/3：异步机器 / signals 活契约 /
    JIT 预热零重提。S6-H-4：角色对象每调用新建维持。X4-9 / X0-5 /
    X0-6：维持排除。
  - S10-A-1 / S10-B-1..3 / S10-C-1..3：均不在本切片，未触碰、未换名
    重提（S10-B-2 的「死钳位消除」与本切片无对应位点；S10-C-* 的
    IRLS/APC 站点在 `routing/`，切片外）。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价、双 LCB 与双归因
  保留、阈值/CAS/凭据/数据面契约/公开签名不变、fail-open 零引入、
  测试未改，天然成立。不声称 Outcome-supported，Checkpoint F-PROD
  仍开放（ADR-005）。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，按指令经 nvm
  切换 v22.22.2；`pnpm install --frozen-lockfile` 后 `pnpm build` /
  `pnpm typecheck` 全绿（与 R1-J §3 既知环境注记一致）。

## 1. 基线不变性、调用图复核与满格矩阵重测

1. **切片逐字节未变**：`git diff fd437a9..004aa20 --
   src/{evaluation,requirement,review,rubric}/` 输出为空；同范围
   `git log` 零提交（十遍全程零 diff）。21 文件 1750 行逐目录计数与
   R1-H 清单一致。R1-H 逐函数下界表、R2-H 调用图上界、R3-H 重复工作
   枚举、R4-H 三类角度收口、R5-H 三层级收口、R6-H 执行层级收口、
   R7-H 机器成本三面收口、R8-H 配置态矩阵收口、R9-H 命令类矩阵满格 +
   逃逸分析地板与全部 S*-H-* 排除继承有效。
2. **调用图可证不变（最强形式）**：`git log af7a423..004aa20 -- src/`
   **为空**——R9-H 基线以来全部提交均为 `docs/reports/` 文档（R9-G/
   R9-H/R9-I/R9-J 报告、R10 全部波次报告与排除表并入）⇒ 生产调用方
   地图数学上不变。本轮全库 import 检索双确认（8 位点，频次逐一
   复核）：`assertCoverageAllowsStart` ←
   `run/{supervisor,coordinator,flowchart-run}.ts`（每 run 启动一次，
   且仅当 `input.contract !== undefined`）；`extractHeuristicContract`
   ← `track/clarify.ts`（每 run 一次）；`applyPrecedence` ←
   `track/loop.ts`（每 run 一次，`"user-first"`）；`shouldScout` ←
   `track/plan.ts`（每 run 一次）；`assertCanPromoteFromReview` ←
   `adaptation/promotion-rules.ts`（每晋升一次）；其余全部为类型导入
   （`adaptation/promotion.ts` 的 ReviewerKind、`track/primary-split.ts`
   的 HeuristicHabits）。test-only 面定向 rg：`runBlindPairwisePair` /
   `createEvaluationRecord` / `reconcileReviews` /
   `createCriticObservation` / `applyRoutingScoreUpdate` /
   `buildContractFromSources` / `registerRubric` / `classifyDiffScope` /
   `selectHighestPrecedence` 的值级引用仅存在于切片自身与
   `test/unit/`、`test/integration/`。
3. **热层三态锚点重测**（三次运行，本 VM，Node 22.22.2）：

```text
CEILING default  (9-pass anchor shape, Q=0):        9858 / 9967 / 10130 ns/run
CEILING configured A (habits 三键全设, Q=1 gated):   8957 / 8194 / 8481 ns/run
CEILING configured B (vague objective, Q=3 gated):   3996 / 4645 / 4008 ns/run
CEILING promotion class: assertCanPromoteFromReview = 19.3 / 18.2 / 20.2 ns/promotion
```

   两档配置态三次全部 ≤ 默认态（gated 重映射真实执行：production
   assume-defaults 流恒传覆盖全部问题的 `resolvedQuestionIds`）——
   R8-H/R9-H「无配置态悬崖、默认锚点是保守上界」在本 VM 复现。默认态
   绝对值（9.9–10.1µs）贴十轮历史跨 VM 带（3.9–10µs）上沿，属慢 VM
   环境差异而非调用图变更（§1.2 已证零变更）；量级结论不变：整个热面
   在 µs 级，低于落地线 **3–4 个数量级**。
4. **命令类矩阵剩余格重测**（R9-H 首锚的四个零生产调用方类，本轮
   三次复测）：

```text
CEILING evaluate class  (TEST-ONLY): createEvaluationRecord(3 criteria) = 1121 / 1132 / 1168 ns/call
CEILING critique class  (chain 内):  critiqueContract(real contract)    =  637 /  615 /  622 ns/call
CEILING pairwise class  (TEST-ONLY): runBlindPairwisePair               = 2102 / 2037 / 2156 ns/call
CEILING reconcile class (TEST-ONLY): reconcileReviews(n=2)              =   73 /   68 /   69 ns/call
```

   与 R9-H 锚点带完全重叠（evaluate 1105–1216ns / critique 611–622ns /
   pairwise 1975–2003ns / reconcile 69–71ns）。指令要求的判据成立：
   **调用图未变下即便把这四个面全部接线，当前规模每类 ≤2.2µs，距
   落地线仍差 4+ 个数量级**。「配置态（3 档）× 命令类（6 类）×
   执行层（热/冷）」矩阵在本 VM 无未测格。
5. **冷层锚点复测（默认 + 配置双态）**：dist 产物、新进程、模块加载
   预扣除后，三生产入口首调合计中位数三次：默认态
   **1706 / 1715 / 1715 µs**（区间 [1662, 1841]µs）、配置态
   **1628 / 1651 / 1631 µs**（区间 [1597, 1738]µs）——与 R6-H
   1.65–1.71ms / R7-H 1.72–1.76ms / R8-H 1.6–1.7ms / R9-H 1.6–1.8ms
   同带；once-per-process 否决类归属不变（S7-H-3 已证该层无净正
   对策）。

## 2. 第十遍枚举：导出面完备性审计 → 诚实空结果

### 2.1 导出面对账（切片全部值级导出 × 裁决记录）

第十遍的新收口角度：不再按成本面（前九轮已穷尽单函数 / 函数间 /
跨模块 / 调用图 / 模块图 / 执行层级 / 调用机器 / 配置态 / 命令类 +
编译器地板十个面），而是按**导出面完备性**对账——切片 30+ 个值级
导出逐一核对是否存在从未被任何轮次检视过的函数体。结果：**零遗漏**。

| 导出 | 裁决记录 |
| --- | --- |
| `checkCoverageGate` / `coverageMatrixFromTasks` / `assertCoverageAllowsStart` / `isSkipContract` | S1-H-1/2、S4-H-1、S6-H-2/3、S8-H-3 六面钉死；isSkipContract 为 O(assumptions) some，R1-H 下界表覆盖 |
| `extractHeuristicContract` / `heuristicExtractor` / `heuristicCritic` / `isVague` / `namedTargets` / `shouldScout` | S1-H-3、S3-H-3、S6-H-4、S7-H-1、S8-H-2 + R2-H/R5-H/R7-H/R8-H no-ID 收口（数据字面量、[...sources] 双拷贝、habits ?? {}、roleId trim、isVague 词计数） |
| `buildContractCandidate` | S2-H-1/2、S3-H-1、S7-H-1 + R7-H minimumConfidence 常量校验 no-ID |
| `normalizeSources` / `createTrustedSource` / `buildContractFromSources` | S2-H-7、S7-H-2 + R5-H slice(0,200) no-ID + R6-H freeze no-ID；buildContractFromSources 零生产调用方（本轮 rg 复核） |
| `critiqueContract` | S2-H-3（负优化）、S3-H-2（正确变体仍噪声） |
| `detectConflicts` / `applyPrecedence` | S1-H-5、S4-H-2、**S5-H-1（守卫保留）**、S6-H-1 + R6-H toLowerCase CSE no-ID |
| `findUnsourcedItems` | R1-H「单遍即输出」+ R5-H 两遍并单遍 no-ID（X3-2 族） |
| `createEvaluationRecord` / `canEvaluatorScoreCriterion` / `validateEvaluatorScope` | S1-H-7 + R5-H evidence 双读 no-ID + R9-H 惰性链化拒列；后两者为 O(1)/单比较 test-only 面（R1-H §4.4 统一裁决） |
| `CheckAdapter` / `createCheckAdapter` / `changeSetsEqual` / `hashArtifact` | S1-H-9、S2-H-4、S3-H-4、S5-H-3 + R9-H attributionMetadata 全分支活性复核（本轮读码再确认：metadata 在全部到达路径被消费） |
| `DeliveryAdapter` / `DiffAdapter` / 工厂函数 | R1-H 常数分支收口 + R7-H 无状态单例化拒列（S6-H-4 换位点 + test-only） |
| `classifyDiffScope` | X4-9 维持 + 分类次序为归因契约（R1-H/R5-H） |
| `EVIDENCE_PRECEDENCE` / `getPrecedenceWeight` / `comparePrecedence` / `selectHighestPrecedence` | X1-4/S1-A-8 三元表族 + S2-H-5 折叠携带（closed） |
| `blindPairwiseCompare` / `runBlindPairwisePair` | 双物质比较协议本体（R1-H）+ S8-H-1 三重钉死 + R1-H remap "ab" 冗余展开 no-ID |
| `reconcileReviews` | S1-H-6 + R9-H 空 dissent Set 跳过拒列 |
| `createCriticObservation` | R2-H hasPass 融合 no-ID + R9-H score 条件键单态化拒列（S9-D-1 机制 + PIC 纪律 + test-only 三重） |
| `applyRoutingScoreUpdate` / `assertCanPromoteFromReview` | S9-H-1（逃逸分析地板）/ S9-H-2（晋升频次墙）——review 切片零未检视结构（R9-H §3.2） |
| `registerRubric` / `getActiveRubric` / `listRubrics` / `resetRubricRegistry` / `createRubric` | S1-H-8 反例 + S2-H-6 + Θ(字段) 构造（R1-H）；getActive/list 为 O(1)/O(R) 直读 |

### 2.2 本轮新检视的六处微观位点（全部不立 ID）

逐文件重扫中定向核查了六处**从未被点名**的位点，全部以既有排除/
契约/量级直接覆盖，无一达到「理论 + 仿真 + 落地线 + 无反例」四重
门槛，故**不立任何 S10-H-* ID**（与 R9-I / R10-E / R10-F 的空枚举
收口同型）：

1. `normalizer.ts` excerpt 的 SlicedString 驻留：`content.slice(0,200)`
   在 V8 下对 >200 字符输入产生 SlicedString，其父串在
   `contract.sourceRefs` 存活期间被钉住——这是**内存驻留**细节而非
   运行时间：生产源恒为单句 CLI objective（<200 字符时 V8 全范围
   slice 返回原串，零分配零驻留，R5-H 已证）；「修复」（显式扁平化
   拷贝）反而**增加**运行时工作，且战役门槛第 3 条只认落地线上的
   运行时间收益。零流量 + 负运行时 + 门槛外，三重不立。
2. `heuristic.ts` PATH_RE 的 `/gi` + `String.prototype.match` lastIndex
   安全性复核：match 对全局正则内部重置 lastIndex，无 X0-6 类状态
   危险；该正则已模块级提升——**现状已是正确形态，无候选**（取证性
   复核，非提案）。
3. `rubric/types.ts` `createRubric` 用 `new Date().toISOString()` 而非
   `domain/timestamp.nowIso()`：行为逐字节同构（同一 ISO 格式），
   统一化是零性能内容的风格重构 + test-only 面。不立。
4. `evaluator.ts` `validateEvaluatorScope` 每调用 `String(rubric.version)`
   分配：单比较 test-only 面，ns 级（S1-A-8 表族量级邻域）。不立。
5. 三个 adapter 的输入校验合取重排（`isCommandResult` /
   `isDeliveryEvidence` / `isDiffScope` 的 typeof 链短路序）：边界
   校验次序是防御契约实施点 + S1-B-3 短路重排族 + 零生产调用方。不立。
6. `ownership.ts` `classifyDiffScope` 的 `classified` 旗标换 labeled
   continue：控制流去糖（S8-H-2 家族）+ test-only + X4-9 邻域。不立。

### 2.3 收口陈述

十遍合计：单函数、函数间、跨模块、调用图、模块图、执行层级、调用
机器、配置态、命令类、编译器地板、导出面完备性**十一个面**全部有
实测锚点或对账记录。本切片在当前调用图下**结构上不存在**达门槛
候选；R9-H 的总门槛更新维持并按本 VM 数字刷新：任何未来候选须先
推翻满格矩阵实测上界——热层默认 **9.9–10.1µs/run**（本 VM；配置态
更低 ⇒ 默认锚点是保守上界）、四个零调用方命令类 ≤2.2µs、冷层
**1.6–1.7ms once-per-process**（否决类）。重开该切片的唯一前提维持
R4-H…R9-H 收口原文：调用图变更（evaluation/review/rubric 面接入每
turn 热路径，或合同规模增长 ≥2 个量级）。

## 3. 逐文件收口（第十遍新视角，其余与 R1-H…R9-H 一致）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `requirement/normalizer.ts` | excerpt SlicedString 驻留（§2.2-1，内存面不立）；S2-H-7、S7-H-2 维持 | 无新候选 |
| `requirement/heuristic.ts` | PATH_RE lastIndex 安全性取证（§2.2-2，现状正确）；S3-H-3、S6-H-4、S7-H-1、S8-H-2 维持 | 无新候选 |
| `requirement/precedence.ts` | **S5-H-1 守卫在位复核（L24 早退，零 diff）**；S1-H-5、S4-H-2、S6-H-1 维持 | 无新候选 |
| `requirement/coverage.ts` | **S8-H-3 零形态复核（无任何全决议快路径）**；S1-H-1/2、S4-H-1、S6-H-2/3 维持 | 无新候选 |
| `requirement/extractor.ts` / `critic.ts` / `provenance.ts` | S2-H-1/2、S3-H-1/2、S7-H-1 维持；导出面对账零遗漏 | 无新候选 |
| `evaluation/evaluator.ts` | validateEvaluatorScope String() 分配（§2.2-4，不立）；S1-H-7 维持 | 无新候选 |
| `evaluation/check-adapter.ts` | isCommandResult 合取序（§2.2-5，不立）；changeSetsEqual 三面 + S5-H-3 维持；attributionMetadata 活性再确认 | 无新候选 |
| `evaluation/delivery-adapter.ts` / `diff-adapter.ts` | isDeliveryEvidence/isDiffScope 合取序（§2.2-5，不立）；常数分支守卫维持 | 无新候选 |
| `evaluation/ownership.ts` | classified 旗标去糖（§2.2-6，不立）；X4-9 维持 | 无新候选 |
| `evaluation/precedence.ts` / `adapters.ts` / `types.ts` | 三元表族 + 纯类型面维持；S2-H-5 维持 | 无新候选 |
| `review/pairwise.ts` / `reconcile.ts` / `critic.ts` / `self-review.ts` | S8-H-1、S1-H-6、S9-H-1/2 维持；四文件十轮合计零未检视结构 | 无新候选 |
| `rubric/registry.ts` / `types.ts` | createRubric 时间戳路径（§2.2-3，不立）；S1-H-8 反例 + S2-H-6 维持 | 无新候选 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 5. 测试

零代码改动下切片相关套件基线复核，全绿（Node 22.22.2 via nvm，满足
engines >=22.19.0；系统 Node 22.14.0 过低的既知环境注记同 R1-J §3；
`pnpm install --frozen-lockfile` 后 `pnpm build` / `pnpm typecheck`
全绿）：

```bash
npx tsx --test "test/unit/requirement/*.test.ts" "test/unit/evaluation/*.test.ts" \
  "test/unit/review/*.test.ts" "test/integration/m3/checkpoint-d.test.ts" \
  "test/integration/m3/coverage-gate.test.ts" \
  "test/integration/m3/requirement-extraction.test.ts" \
  "test/integration/m4/delivery-evidence.test.ts"
# tests 93 / suites 13 / pass 93 / fail 0（与 R1-H…R9-H 同套件同计数）
```

锚点重测（临时脚本 `/tmp/r10h-anchor.mts`，无赢家、无候选裁决，
故未入库以从 round01–09 仅赢家 sim 入库的仓库惯例；完整源码见附录，
冷层探针以 `pnpm build` 后的 dist 产物为准）代表性一次运行：

```text
CEILING default (9-pass anchor shape, Q=0): extractHeuristicContract=8912ns + run-start gate(Q=0, no options)=696ns + applyPrecedence=250ns = 9858ns once-per-run production total
CEILING configured A (habits set, Q=1 gated): extractHeuristicContract=8047ns + run-start gate(Q=1, all resolved)=697ns + applyPrecedence=212ns = 8957ns once-per-run production total
CEILING configured B (vague, Q=3 gated): extractHeuristicContract=3419ns + run-start gate(Q=3, all resolved)=466ns + applyPrecedence=111ns = 3996ns once-per-run production total
CEILING promotion class: assertCanPromoteFromReview=19.3ns once per promotion (production)
CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=1121ns/call
CEILING critique class (inside run-start chain): critiqueContract(real contract)=637ns/call
CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=2102ns/call
CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=73ns/call
COLD first-call production total (dist, fresh process, default): median=1706µs range=[1678, 1738]µs — once per PROCESS (veto class)
COLD first-call production total (dist, fresh process, configured (habits + Q=1 gated)): median=1628µs range=[1597, 1700]µs — once per PROCESS (veto class)

ALL ANCHOR PROBES COMPLETED
```

三次独立运行方向与量级一致（默认 9858/9967/10130ns、配置 A
8957/8194/8481ns、配置 B 3996/4645/4008ns、晋升 19.3/18.2/20.2ns、
evaluate 1121/1132/1168ns、critique 637/615/622ns、pairwise
2102/2037/2156ns、reconcile 73/68/69ns、冷默认 1706/1715/1715µs、
冷配置 1628/1651/1631µs）。

## 6. 新增排除 ID

**无。** 第十遍枚举为空——切片导出面完备对账零遗漏，六处新检视微观
位点全部落入既有排除/契约/量级收口（§2.2），不硬凑 ID。与 R9-I /
R10-E / R10-F 的空枚举收口同型。

## 附录：锚点重测脚本（完整，可复现）

运行方式：`pnpm build` 后保存为任意 `.mts` 并 `npx tsx <file>`（仓库
根目录，依赖已装；冷层探针需要 dist 产物存在）。本轮无候选裁决，
脚本仅重建满格矩阵锚点。

```ts
/**
 * R10-H deterministic anchor re-verification (tenth pass).
 * No fresh candidates survived enumeration (empty tenth group), so this
 * script only re-establishes, on THIS VM, the full measurement matrix that
 * R8-H/R9-H closed:
 *   - hot tier, three states: default (Q=0), configured A (habits set, Q=1
 *     gated), configured B (vague objective, Q=3 gated). The production
 *     assume-defaults track flow always passes resolvedQuestionIds covering
 *     ALL questions, so the gated re-map materially executes.
 *   - promotion class (production, once per promotion).
 *   - the four zero-production-caller command classes first anchored in
 *     R9-H: evaluate / critique / pairwise / reconcile.
 *   - cold tier once-per-process first-call totals on dist, default AND
 *     configured shapes (5 fresh processes each, median).
 * Everything is measured against production imports; no replicas are needed
 * because no candidate edits are adjudicated.
 */
import { performance } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import {
  extractHeuristicContract,
  type HeuristicHabits
} from "/workspace/src/requirement/heuristic.js";
import {
  assertCoverageAllowsStart,
  type CoverageTaskRef,
  type CoverageStartOptions
} from "/workspace/src/requirement/coverage.js";
import { applyPrecedence } from "/workspace/src/requirement/precedence.js";
import { critiqueContract } from "/workspace/src/requirement/critic.js";
import { assertCanPromoteFromReview } from "/workspace/src/review/self-review.js";
import { runBlindPairwisePair, type PairwiseInput } from "/workspace/src/review/pairwise.js";
import { reconcileReviews } from "/workspace/src/review/reconcile.js";
import { createEvaluationRecord } from "/workspace/src/evaluation/evaluator.js";
import { createRubric } from "/workspace/src/rubric/types.js";
import type { EpisodeId, TaskId } from "/workspace/src/domain/ids.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

/* Hot tier, three states (R8-H/R9-H anchor shapes). */
async function measureChain(label: string, objective: string, habits: HeuristicHabits | undefined): Promise<void> {
  const args = { objective, ...(habits !== undefined ? { habits } : {}) };
  const chain = await benchAsync(async () => {
    await extractHeuristicContract(args);
  }, 3000);
  const prod = await extractHeuristicContract(args);
  const real = prod.contract;
  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
  const options: CoverageStartOptions | undefined =
    real.questions.length > 0
      ? { resolvedQuestionIds: real.questions.map((question) => question.id) }
      : undefined;
  const gate = bench(() => assertCoverageAllowsStart(real, tasks, options), 50000);
  const precedence = bench(() => void applyPrecedence(real, "user-first"), 100000);
  const total = chain + gate + precedence;
  log(
    `CEILING ${label}: extractHeuristicContract=${(chain * 1e6).toFixed(0)}ns + run-start gate(Q=${real.questions.length}${options !== undefined ? ", all resolved" : ", no options"})=${(gate * 1e6).toFixed(0)}ns + applyPrecedence=${(precedence * 1e6).toFixed(0)}ns = ${(total * 1e6).toFixed(0)}ns once-per-run production total`
  );
}
{
  await measureChain(
    "default (9-pass anchor shape, Q=0)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    undefined
  );
  await measureChain(
    "configured A (habits set, Q=1 gated)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    { requireTests: true, preferReview: false, askBeforeWrite: true }
  );
  await measureChain("configured B (vague, Q=3 gated)", "fix bug please", { askBeforeWrite: true });
}

/* Command-class anchors beyond run-start. */
{
  const promo = bench(
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    200000
  );
  log(`CEILING promotion class: assertCanPromoteFromReview=${(promo * 1e6).toFixed(1)}ns once per promotion (production)`);

  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const evaluate = bench(() => void createEvaluationRecord(evalInput), 50000);
  log(`CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=${(evaluate * 1e6).toFixed(0)}ns/call`);

  const prod = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const critique = bench(() => void critiqueContract(prod.contract), 50000);
  log(`CEILING critique class (inside run-start chain): critiqueContract(real contract)=${(critique * 1e6).toFixed(0)}ns/call`);

  const pairInput: PairwiseInput = {
    episodeId: "ep_00000001" as EpisodeId,
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pairwise = bench(() => void runBlindPairwisePair(pairInput, () => 0.7), 20000);
  const pair = runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  const reconcile = bench(() => void reconcileReviews(two), 100000);
  log(`CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=${(pairwise * 1e6).toFixed(0)}ns/call`);
  log(`CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=${(reconcile * 1e6).toFixed(0)}ns/call`);
}

/* Cold tier on dist, default + configured (5 fresh processes each). */
{
  const mkScript = (configured: boolean): string => `
    const { performance } = await import("node:perf_hooks");
    const { extractHeuristicContract } = await import("/workspace/dist/requirement/heuristic.js");
    const { assertCoverageAllowsStart } = await import("/workspace/dist/requirement/coverage.js");
    const { applyPrecedence } = await import("/workspace/dist/requirement/precedence.js");
    const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
    const args = ${"{"} objective${"}"};
    ${configured ? 'const cfgArgs = { objective, habits: { requireTests: true, preferReview: false, askBeforeWrite: true } };' : ""}
    const t0 = performance.now();
    const prod = await extractHeuristicContract(${configured ? "cfgArgs" : "args"});
    const tasks = Array.from({ length: 5 }, (_, i) => ({
      id: "tsk_0000000" + i,
      acceptanceCriteria: prod.contract.acceptanceCriteria.map((c) => ({ id: c.id }))
    }));
    ${configured
      ? "assertCoverageAllowsStart(prod.contract, tasks, { resolvedQuestionIds: prod.contract.questions.map((q) => q.id) });"
      : "assertCoverageAllowsStart(prod.contract, tasks);"}
    applyPrecedence(prod.contract, "user-first");
    const t1 = performance.now();
    process.stdout.write((t1 - t0).toFixed(3));
  `;
  const median = (xs: number[]): number => {
    const sorted = [...xs].sort((a, b) => a - b);
    return sorted[Math.floor(sorted.length / 2)] ?? Number.NaN;
  };
  for (const [label, configured] of [
    ["default", false],
    ["configured (habits + Q=1 gated)", true]
  ] as const) {
    const samples: number[] = [];
    for (let i = 0; i < 5; i += 1) {
      const proc = spawnSync(process.execPath, ["--input-type=module", "-e", mkScript(configured)], {
        encoding: "utf8"
      });
      if (proc.status !== 0) {
        check(`cold probe runs (${label})`, false, proc.stderr);
        break;
      }
      samples.push(Number(proc.stdout.trim()));
    }
    log(
      `COLD first-call production total (dist, fresh process, ${label}): median=${(median(samples) * 1e3).toFixed(0)}µs range=[${(Math.min(...samples) * 1e3).toFixed(0)}, ${(Math.max(...samples) * 1e3).toFixed(0)}]µs — once per PROCESS (veto class)`
    );
    check(`cold probes produced numbers (${label})`, samples.length === 5 && samples.every(Number.isFinite));
  }
}

if (failures > 0) {
  process.stderr.write(`\n${failures} check(s) FAILED\n`);
  process.exit(1);
}
log("\nALL ANCHOR PROBES COMPLETED");
```

MORE_OPTIMA=no
BRANCH=cursor/r10-h-eval-tenth-pass-83a1
