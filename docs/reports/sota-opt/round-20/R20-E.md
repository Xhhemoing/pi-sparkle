MODEL_SLUG=claude-fable-5-thinking-xhigh

# R20-E：`src/learning/` 第二十遍复查报告（Round 20）

**战役:** 全库持久 SOTA 优化 Round 20 / R20-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `c1e4424`（含 R20-D 空枚举摄取与本派单；R20-B / R20-C 在飞未触碰、未等待）
**分支:** `cursor/r20-e-learning-twentieth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第二十组新角度枚举为空（有理由的
空枚举，未铸造任何 S20-E-\* ID，与 R10-E..R19-E 同口径），切片维持关闭
（MORE_OPTIMA=no）。本轮首次补齐一条 R1-E..R19-E 从未测过的完整性轴——
「多进程并发面（E 自有共享态 = bandit 数据面）」：前十九份 E 报告里的
「并发」字样全部只是 S8-E-1 的保留理由（并发 promote 新鲜性）或进程内
I/O 编排已裁面（S1-E-5 / S4-E-2），从未有任何一轮对 E 切片唯一的跨进程
共享态——`bandit.json` + 切片内唯一锁调用点（`bandit-store.ts:44`，
首手复核：不传 options，默认 timeoutMs=5000 / retryMs=10）——给出过
竞争排队、可串行化、撕裂读的实测价格。R20-D 刚关掉 D 面（adaptation
registry 锁：tmp+fsync+rename 原子落盘）；E 面结构上不同——bandit 写
是就地 `writeFile`（无 tmp+rename、无 fsync）。本轮以真实 dist/ 产物
子进程五段关死（6 次独立套件运行 = 3 × v22.14.0 + 3 × v22.22.2，每次
12/12 sanity 全绿）：**

1. **bandit 锁竞争排队量化税（C1）**：真实 `updateProjectBandit` 单飞
   事务中位 **0.65–1.07 ms**；持锁 H ∈ {3,5,20,35} ms 网格下竞争侧
   整事务中位 H=3 → 11.55–13.13、H=5 → 11.59–13.21、H=20 →
   22.55–33.58、H=35 → 43.50–44.16 ms——等待呈 retryMs=10 的轮询阶梯
   （量化税 = 中位 − H − 单飞 ≈ **1.9–12.5 ms/竞争事务**，典型 ≤1 个
   retry 量子，H=20 有一次中位落双量子），与 R20-D 在同一锁协议上的
   6.4–12.4 ms 带交叉核对成立。互斥见证 **192/192**（4 格 × 8 rep ×
   6 运行：竞争重叠 8/8 全格engaged，竞争者事务完成时刻 ≥ 持有者释放
   时刻全数成立）。
2. **N 路护航（C2）**：N ∈ {2,4,8} 同时真实更新，makespan 中位阶梯
   N=2 → 2.23–13.93（亚 ms 真实持锁使部分 rep 根本撞不上）、N=4 →
   22.68–34.40、N=8 → **63.95–73.79 ms** ≈ (N−1)×(retryMs+ε)。N=8 格
   数字上进落地区间，但要求同项目同 stateRoot 上 **8 个 run 进程同时
   收尾**——auto-adapt 每 run 进程恰一次 + 操作者 CLI 串行，稳态流量
   恰为零（R20-D S1c / S12-B-2 同判的零流量假设格）。
3. **可串行化见证 + 锁消除反事实（C3）**：4 进程 × 25 次真实锁内事务
   → 守恒精确（totalPulls=100/100、逐臂 25/25，6/6 运行；100 次跨进程
   锁事务 makespan 80–86 ms）。**锁消除反事实**（逐字同体、仅去掉
   `withExclusiveFileLock`）：**丢失更新 88–99 / 100（6/6 运行）**——
   锁是正确性地板而非可削开销，「省锁」类候选被事实性封死。
4. **写风暴下的无锁读者撕裂读普查（C4）**：200 臂状态（bandit.json
   9,381 B），真实写管线风暴 **1,382–1,793 次写/s**；无锁读者
   （`loadProjectBandit`——本轮普查：src/ 与 test/ **均零调用方**，
   死导出面）在风暴窗内 **5.5–6.0% 读到 undefined**（806–967 /
   13,949–16,956；分类探针：截断后空窗 6–34 次、部分 JSON 1–25 次、
   ENOENT 0），静默窗 0 / 12,414–14,654。与 D 面结构对照：registry
   save 走 tmp+fsync+rename，R20-D S1d 撕裂 0/240；E 面就地写**确实
   可撕**——但生产读全部在锁内（唯一生产读在 `updateProjectBandit`
   锁内），fail-soft catch 把撕裂降级为 undefined 属设计事实，无锁读
   生产流量为零。读延迟 p50 0.096–0.124 / p95 0.114–0.201 ms 两臂
   轻度膨胀，同带级。
5. **双进程完整 auto-adapt 环（C5）**：真实 `runAutoAdaptLoop`
   （extraSignals → persistSignals feedback jsonl → bandit 锁 →
   diagnose → registry 锁 propose 全链）单飞稳态中位 **2.58–3.45 ms**；
   双进程并发同 rep 每进程中位 **13.33–14.32 ms**、makespan
   15.02–16.16 ms、竞争开销 **10.71–11.32 ms ≈ 恰一个 retry 量子**
   （两把锁的等待相互重叠）——与 C1 量化税带算术一致。跨进程去重
   见证：候选 `created=true` 在双进程对上**恰出现一次**（registry 锁内
   contentHash find），6/6 运行；单飞臂同样恰一次。

**天花板裁决（频率 × 单价 × 属地三端封死，R20-D 同式）**：频率端——
竞争面要求 ≥2 个 run 进程在同项目同 stateRoot 同时收尾，稳态流量恰零；
单价端——真实碰撞税 10.71–11.32 ms/碰撞，低于落地线（数十–数百 ms），
且它是锁协议契约地板（retryMs 阶梯）而非可削开销；属地端——
retryMs/timeoutMs 默认值在 `persist/file-lock.ts`（J 切片；X0-7 阈值
邻域），锁消除被 C3 反事实封死（88–99% 丢失更新），写管线原子化/加
fsync 是健壮性功能工作（X0-2）且受益面流量为零，无变更跳写 = S2-E-4、
mkdir 提升 = S6-E-3、紧凑序列化 = S6-E-4、fs.watch 唤醒 = S9-G-2 族、
persist∥bandit 并行 = S4-E-2 逐字维持。**该轴无缝隙，收口，无新 ID。**

其余与前十九轮同构的复核全数成立：切片二十遍零 diff；SLICE-CPU 本夹具
**17.7–19.3 µs/run**（本轮构成 collected=20 / outcomes=1 /
diagnoseGroups=1），R17-E 种子对调复现 collected=15 / outcomes=4 且总量
**18.7–18.9 µs 逐点落在 R17-E 带 18.5–20.2 之内**（且在 R19-E cmp17 带
18.5–19.7 内）；配置态 live 面与载入面锚点全数在历史带内；`pnpm gate`
在 engines 合规 Node v22.22.2 全绿（1168 pass / 0 fail / 1 skipped，
78 suites，计数与 R14-E..R19-E 逐一相同）；切片套件 48/48 双版本过。
S8-E-1 / S9-E-2 / S13-B-1 未以任何形态触碰。不声称 Outcome-supported；
Checkpoint F-PROD 仍开放（ADR-005）。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行，`wc -l` 与 R14-E..R19-E 逐字节相同）
  本轮第二十遍全量实际读码，未依赖前十九轮记忆。上下游 `adaptation/`、
  `persist/file-lock.ts`、`feedback/store.ts`、`routing/`、`run/`、
  `track/`、`cli/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表 546 行，含
  全部 S\*-E-\* 行与 S8-E-1 / S9-E-2 / S13-B-1）→ PROGRESS.md →
  round-20/PLAN.md + round-19/PLAN.md → round-19/R19-E.md（空枚举；
  输入表示 + 事件排序）→ R18-E（冷层 + 字宽）→ R17-E（拒绝路径 +
  逐事件类价表）→ R16-E / R15-E / R14-E / R13-E..R10-E 收口段（透镜
  与先例核对）→ 10 个源文件 → 刚合入的 R20-A / R20-D（辖区无交，
  R20-D 的 D 面并发轴为本轮 E 面轴的方法论先例）。R20-B / R20-C 在
  其他分支在跑，未等待、未触碰其报告与分支。
- **基线漂移十重确认**：`git diff adb20d7..HEAD -- src/learning` 为
  **0 行**、`git log adb20d7..HEAD -- src/learning` 为 **0 提交**——
  二十轮全程无人触及该目录。且 R19-E 基线（`d95564d`）之后的全部提交
  （R19-E..J 摄取、Round 19 收口、Round 20 开轮、R20-A/R20-D 摄取与
  R20-B/C/E 派单）**仅触及 `docs/reports/sota-opt/`**——
  `git diff --stat d95564d..HEAD -- src/ test/` 为空、name-only 扫描
  非 docs 路径为零。R1-E..R19-E 的全部规模测量、调用面图景与
  S1-E-1..8 / S2-E-1..7 / S3-E-1..5 / S4-E-1..3 / S5-E-1..5 /
  S6-E-1..5 / S7-E-1..5 / S8-E-1..3 / S9-E-1..3 共 44 项切片排除裁决
  （R10-E..R19-E 未铸 ID，其空枚举收口一并继承，含 R13-E 三处无名
  微观、R14-E 规模/JIT、R15-E GC/A-A、R16-E 累积状态/载荷形态、
  R17-E 异常路径/事件类构成、R18-E 冷层/字符宽度、R19-E 输入表示/
  事件排序共十二维）原样成立、原样继承。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S19 时代条目。特别地：
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护：本轮仅复核其行号与
    频次（§1，与 R19-E 逐一相同），**未做任何去重分析**；本轮并发轴
    与其保留理由（并发 promote 下的新鲜性）同向——C5 实证跨进程写者
    真实存在，双载入各自读到当时快照正是该理由的机制本体。
  - **S9-E-2**（pass-2 预过滤负优化）未触碰；本轮轴不涉两遍结构。
  - **S13-B-1**（B 切片辖区）未以任何变体重提：`applyLearnedRouting`
    本体**零改动**（切片零 diff 下必然）；重开条件（本体成本 ≥2 个
    量级，R16-E 量化 |avoid| ≈ 965–1,063）未触发；本轮轴不触及 apply
    内部（apply 是纯函数，无跨进程共享态，不在并发面上）。
  - **R13-E 三处无名微观**位点逐字未变（零 diff 下必然），维持裁决，
    未补铸 ID。
  - **R14-E..R19-E 十二轴未复铸**：派单明令勿补铸（R19-E 输入表示/
    事件排序、R18-E 冷层/CJK、R17-E 拒配路径/类价表、R16-E 累积状态/
    载荷形态）。本轮轴与十二维全部无交——前十九轮全部测量都是
    **一次一个进程**；本轮测的是**进程共存**（竞争、可串行化、撕裂），
    R19-D/R20-D 的进程**启动**层（boot+import）只作子进程夹具开销，
    未记账、未重铸。
  - R10-E..R19-E 未铸 S10..S19-E-\*，本轮亦未事后发明任何此类编号；
    R20-A（A 辖区 resident-state/驻留链）与 R20-D（D 辖区并发面）
    空枚举与本切片无交——**本轮不是 R20-D 的重铸**：R20-D 定价的是
    adaptation registry 锁（D 数据面、原子 rename 落盘），本轮定价的
    是 bandit 锁 + bandit.json（E 自有数据面、就地写、切片内唯一锁
    调用点），两面在锁路径、数据面布局、写管线原子性上结构不同
    （C4 撕裂普查给出实测差异：5.5–6.0% vs R20-D 的 0/240）。
- **换名重提检查**：本轮新定位形态的换名检查全部并入 §2.2 表格逐行
  给出，并逐一对照 EXCLUSIONS 全表 + 十九轮 E 报告 + 十轮空枚举面。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  稀疏 → 已批准 R0 基线；双 LCB / 双归因在位；晋升 proposal-first；无
  阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；fail-open 家族
  关死；隐藏模块缓存（X1-1）未引入；bandit/learned registry I/O 的
  fsync 与完整性重哈希未触碰；corrupt registry fail-closed 未触碰；
  两遍 `collectSignalsFromEvents` 语义未触碰；X0-3 维持；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；
  未 force-push；仅提交并推送本分支上的本报告一个文件。
- 环境：本 VM 系统默认 node v22.14.0（`/exec-daemon/node`，PATH 前置
  遮蔽 nvm——R12-E §0 起连续记录的同一环境事实），nvm 另装 v22.22.2
  （engines `>=22.19.0` 仅后者合规）。锚点与并发轴在两个版本各测
  （§1/§2）；`pnpm gate` 以 v22.22.2 为准（PATH 前置 nvm bin 后
  `node --version` 复核为 v22.22.2）。依赖经
  `pnpm install --frozen-lockfile` 安装（pnpm 10.17.1）；并发轴子进程
  跑 `pnpm build` 产出的真实 `dist/` 产物（R20-D 同协议）。
- 新轴测量注意事项（本轮实际踩过、供后轮复用）：(a) 跨进程同步必须用
  屏障文件（子进程 READY → 父触发 go 文件），子进程**boot+import 约
  百 ms**，任何「父看到持有者获取后再 spawn 竞争者」的次序都会错过
  竞争窗——竞争者必须先起好、驻留在 1 ms 轮询上。(b) H=0（亚轮询
  量子持锁）格在该协议下无法确定性 engage（持有者在竞争者屏障唤醒前
  已释放，首测 0/8 overlapped），从网格移除；真实亚 ms 持锁的碰撞由
  C2（N=2）与 C5 真实事务对撞覆盖。(c) 计时一律取子进程**进程内**
  `performance.timeOrigin + performance.now()`（排除 boot；跨进程可
  比，同机时钟）；互斥见证以 ±0.5 ms 容差判定。(d) C3 反事实的丢失
  更新数逐运行波动（88–99/100），断言只锁「锁内臂精确守恒」这一确定
  性事实，反事实臂只报告不作硬断言。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

src 自 `d95564d` 起一行未动，故 R8-E..R19-E §1 矩阵表结构原样成立；
本轮逐条 rg 复核全部切片进入点行号，**与 R19-E 记录逐一相同、无新增
调用方、无新增命令类**（`cli/adapt:168/188/205`、`cli/main:708/783`、
`routing/assign:102`、`run/flowchart-run:681/712`、`track/loop:88/172`；
resume/replay 经 `flowchartForSupervisor` @ `flowchart-run:880/1021`
各单次载入；`run --flowchart` 在 `:708` 载入前返回，无双载入）。切片外
消费者复核与 R19-E 逐一相同（`adaptation/eval-routing.ts:223` 仅
`parseLearnedRoutingPolicy`、`track/primary-split.ts:3` 类型导入、
`privacy/record-classes.ts` 文档字符串、`approval-profile.ts:44`
docstring 假阳性）。**本轮矩阵新增一列首手事实（并发轴 C0）**：
`updateProjectBandit` 生产调用方恰一处（`auto-loop.ts:89`，每 run 进程
至多一次、由 `signals.some(modelId!==undefined)` 门控）；
`loadProjectBandit` 在 src/ 与 test/ **均零调用方**（死导出面，先前
按 test-only 记录，本轮收紧为零调用方）；切片内唯一锁调用点
`bandit-store.ts:44` **不传 options**，默认 timeoutMs=5000 /
retryMs=10（与 R20-D 钉死的 D 面四调用点同为默认参数）。

SLICE-CPU 锚点本 VM 重测（**16 次运行**：8 × Node v22.14.0 + 8 ×
Node v22.22.2；seeds `0xe20e01`（collect）/ `0xe20e02`（routed）/
`0xe20e03`（signals）——R12-E..R19-E 拒绝采样同协议，三个首选种子
全部一次通过（collected=20>3、outcomes=1>0、diagnoseGroups=1≥1）；
探针记录 seed→(collected,outcomes,diagnoseGroups)：01→(20,4,1)、
02→(19,1,0)、03→(21,3,1)、04→(13,3,0)、05→(18,3,0)、06→(15,1,0)；
夹具镜像 R8-E..R19-E 生成器，R1-A 组成 E=41）：

```text
collect=14.4-16.0us  outcomes=1.5-1.6us  diagnose=1.02-1.09us  bandit-build=0.7-0.8us
total in-slice CPU ~17.7-19.3us per full auto-adapt run （两 Node 版本重叠带：
  v22.14.0 17.8-19.3 / v22.22.2 17.7-18.5，版本差异不可分辨）
vs landing bar >=10000us  ->  517-564x below EVEN IF ZEROED
带间比对：本轮构成 collected=20 / outcomes=1 / diagnoseGroups=1 与 R19-E
  （14/3/2）、R17-E（15/4/1）均不同，按派单令补跑 6 次 R17-E 种子比对运行
  （0xe17e01/02/03 + R17-E NOW）：复现 collected=15 / outcomes=4 /
  diagnoseGroups=1，总量 18.7-18.9us 逐点落在 R17-E 带 18.5-20.2 之内、
  也在 R19-E cmp17 带 18.5-19.7 之内（collect 12.8-13.1 / outcomes 4.0-4.2 /
  diagnose 1.00-1.08 / bandit 0.8 与 R17-E 12.7-14.1 / 4.0-4.3 / 0.98-1.11 /
  0.7-0.8 逐项同带）——带差即构成差：本夹具 outcomes=1（cmp17 为 4）使
  outcomes 段 1.5-1.6 vs 4.0-4.2（逐 outcome 绑定价一致），collected=20
  （cmp17 为 15）使 collect 段 14.4-16.0 vs 12.8-13.1（逐发射价一致）——
  R17-E 价格表在第三个构成点上继续给出同向价签算术，无需重derive
configured-state: applyLearnedRouting(avoid=10, M=10) = 257-283ns/task （R13-E 258-312 /
                                                        R17-E 257-290 / R18-E 268-314 /
                                                        R19-E 256-312 同带）
                  applyLearnedRouting(空策略)        = 111-126ns/task （R15-E..R19-E
                                                        110-140 同带）
                  loadLearnedRouting fresh(ENOENT)   = 25.0-36.0us/次
                  loadLearnedRouting 现实 registry    = 75.3-119.1us/次 （R8-E 80-85 /
                                                        R15-E..R19-E 74.3-134.5 同带，
                                                        I/O 抖动，亚 ms 一次性，S8-E-1 带）
```

全部确定性 VERDICT 行 16 次运行**逐位一致**（`E=41 collected=20
routedE=41 outcomes=1 S=12 diagnoseGroups=1 prevArms=3`），3/3 sanity
×16 全过；cmp17 6 次 VERDICT 行逐位一致且与 R17-E 夹具行逐字相同。
**锚点复核成立：该切片不存在不推翻既有排除就能达门槛的候选。**

## 2. 本轮实扫新轴 + 第二十组新角度枚举：有理由的空枚举

### 2.0 新轴 C：多进程并发面（本切片首次；E 自有共享态 = bandit 数据面）

R1-E..R19-E 全部测量都在**单进程**内完成：CPU/规模/分配/协议/累积态/
载荷形态/异常面/构成面/执行层级/字宽/输入表示/输入次序十二维没有任何
一维变动过「有几个进程在同一共享态上」。E 切片拥有一块自有跨进程共享
态：`bandit.json`（advisory adaptation-plane、per-project）——它的
读-改-写事务由切片内唯一锁调用点保护（`bandit-store.ts:44`，默认参数），
落盘走**就地 `writeFile`**（与 D 面 registry save 的 tmp+fsync+rename
结构不同）。当两个 run 进程在同项目上同时收尾时（每 run 进程恰一次
auto-adapt），该面发生什么，从未有过价格。本轮以真实 dist/ 子进程 +
文件屏障同步五段定价（6 次套件运行 = 3 × v22.14.0 + 3 × v22.22.2，
12/12 sanity ×6 全绿；C1-C5 数字见结论 1–5，此处不重抄），关键机制
事实与裁决：

- **C1/C2（排队与护航）**：等待由 `persist/file-lock.ts` 的
  retryMs=10 轮询阶梯量化（本轮量化税 1.9–12.5 ms/竞争事务与 R20-D
  的 6.4–12.4 交叉核对，同一协议在两个数据面上给出同带价格）。收割
  这笔税的全部旋钮在切片外或被排除：调小 retryMs = J 切片默认值 +
  X0-7 阈值邻域；E 调用点自传 options = 共享锁协议的行为分歧 + 零
  竞争流量下零收益；事件驱动唤醒 = S9-G-2 fs.watch 同族。
- **C3（可串行化）**：锁内臂精确守恒（6/6），锁消除反事实丢 88–99%
  更新——「省锁/缓存跨进程状态」类候选不是慢与快的问题，是**错**的
  问题（X1-1 隐藏缓存族在跨进程写者存在下必然陈旧，本轮给出实测
  反例底座）。
- **C4（撕裂读）**：E 面就地写在风暴下对无锁读者可撕（5.5–6.0%
  undefined，分类探针证实主体是 O_TRUNC 后空窗 + 少量部分 JSON），
  D 面原子 rename 为 0/240（R20-D S1d）——结构差异实测坐实。但
  (a) 生产读全部在锁内（C0 普查：唯一生产读在 `updateProjectBandit`
  锁内；`loadProjectBandit` 零调用方），撕裂窗无生产观察者；
  (b) `readBanditFile` 的 fail-soft catch 把撕裂降级为 undefined =
  「无先验状态」，advisory 面按设计容忍；(c) 把就地写改成
  tmp+rename/加 fsync 是**健壮性功能工作**（X0-2），不是保行为优化
  ——它增加 syscalls（负价），受益面流量为零。**无候选，仅记录结构
  事实。**
- **C5（真实碰撞全链）**：双进程完整 auto-adapt 碰撞总税
  10.71–11.32 ms ≈ 恰一个 retry 量子——feedback jsonl 追加（J 面，
  O_APPEND 无锁）不贡献竞争，bandit 锁与 registry 锁的等待相互重叠
  而非叠加；账目与 C1 一致，无隐藏第二笔税。跨进程 propose 去重
  （contentHash find 在 registry 锁内）见证恰一次 created——
  proposal-first 语义在并发下自洽，无需也不可加去重旁路。

**裁决**：该轴在频率（稳态并发流量恰零）× 单价（碰撞税 ~11 ms <
落地线）× 属地（锁协议 J 切片 / X0-7；原子化 X0-2；其余全部撞既有
排除行）三端封死，**不产生候选**。「进程数」维度就此与前十二维正交
封口：成本 = f(单进程价格表) + 碰撞数 × (~1 retry 量子)，碰撞数生产
恒零。

### 2.1 第二十组枚举表

方法：本轮代码侧透镜「**跨进程共享态**」（新轴的对偶）：对第二十遍
全量重读的 10 个源文件逐函数问 (a) 哪些位点触碰跨进程共享态（锁、
共享文件、追加日志），任何省锁/缓存/批合并/原子性变更是否等价且可能
越线；(b) 纯函数位点（无共享态）在并发维度上是否存在任何新收益位；
同时按惯例对本轮逐字新点名的位置做换名检查（对照 EXCLUSIONS 全表 +
十九轮 E 报告 + 十轮空枚举面）。扫描结果：**零个可立项形态**，逐类
裁决如下（均未铸造新 ID）：

| 形态（本轮第二十遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **bandit 锁消除 / 缩窄临界区**（`bandit-store:44` 整事务在锁内，读-改-写-落盘全包） | 竞争时 ~11ms/碰撞；单飞时锁 glue µs 级 | **反事实实测封死拒列（不铸 ID）**：C3 锁消除臂丢失更新 88–99/100；缩窄（锁外读、锁内写）同样破坏读-改-写原子性，属同一反事实；单飞锁 glue 被 0.65–1.07ms 事务价包含，深度亚 ms | 硬不变量 + 本轮 C3 |
| **跨 run 进程内 bandit 状态缓存**（模块级缓存 previous 免锁内重读） | 锁内 readFile ~百 µs | 换名重提拒列：X1-1 隐藏模块缓存族；且 C3 实证跨进程写者存在 ⇒ 缓存必然陈旧，本轮为 X1-1 补上实测反例底座 | X1-1 + 本轮 C3 |
| **bandit 无变更跳写 / 写批合并** | 亚 ms/run | 换名重提拒列：S2-E-4（无变更跳写，持久化契约）逐字维持；跨进程批合并需进程间协调 = 不存在的基础设施 + X0-3 保存时机邻域 | S2-E-4 / X0-3 |
| **`updateProjectBandit` 每写 mkdir(recursive) 消除/提升** | 22.7–23.7µs/call（R6-E 实测） | 换名重提拒列：S6-E-3 逐字维持（S5-G-1 同型，外部清理自愈语义） | S6-E-3 |
| **bandit.json 紧凑序列化**（去 pretty-print 缩小写窗） | 写窗与撕裂率小幅缩小 | 换名重提拒列：S6-E-4 逐字维持（S4-G-6 同型磁盘字节发散）；本轮 C4 顺带给出其并发面收益上界 = 零生产观察者的撕裂率 | S6-E-4 + 本轮 C4 |
| **就地 writeFile 改 tmp+fsync+rename / 加 fsync**（对齐 D 面原子性，消灭 C4 撕裂窗） | 撕裂 5.5–6.0%→0（仅对无锁读者） | **属地 + 算术双封死拒列**：健壮性功能工作（X0-2），非保行为优化——增加 syscalls 是负价；受益面（无锁读者）生产流量为零（C0 普查 `loadProjectBandit` 零调用方）；崩溃中写丢 bandit = advisory 面 fail-soft 设计事实 | X0-2 + 本轮 C0/C4 |
| **锁参数调优**（retryMs 调小削量化税 / timeoutMs 调整） | 1.9–12.5ms/竞争事务 | 属地拒列：默认值在 `persist/file-lock.ts`（J 切片）+ X0-7 阈值邻域；E 调用点自传 options = 共享协议行为分歧；竞争流量恒零使收益期望恒零 | X0-7 / R20-D 同判 |
| **锁等待事件驱动化**（fs.watch lock 文件消失即重试） | 每碰撞 ≤1 retry 量子 | 换名重提拒列：S9-G-2 fs.watch 同族（R20-D 同判）；零流量 | S9-G-2 族 |
| **bandit 锁与 registry 锁合并 / auto-loop 两腿单锁化** | 每碰撞省一次锁往返 | 拒列：跨数据面锁耦合拓宽两个临界区（负价）；保存时机语义 X0-3 邻域；C5 实证两锁等待本就重叠，无第二笔税可省 | X0-3 + 本轮 C5 |
| **persistSignals ∥ updateProjectBandit 编排重叠** | 317–443µs（R4-E 实测） | 换名重提拒列：S4-E-2 逐字维持（双故障错误非确定）；本轮 C5 的 jsonl 追加零竞争事实不改变其单进程裁决 | S4-E-2 |
| **auto-loop 前置门控外提 / bandit 调用条件收紧**（无奖励信号时免锁免 I/O） | 亚 ms/run | 换名重提拒列：`signals.some(modelId!==undefined)` 门已存在（`auto-loop:88`）；再收紧（无 taskSuccess 时跳过）= 丢失 arms 注册的行为变更 + S2-E-4 跳写换名 | S2-E-4 + 行为等价性 |
| **`loadProjectBandit` 死导出清理** | 0ns 运行时 | 不立项：无收益路径即无候选资格；公开签名保留（R19-E「死导出」同款裁决）；本轮仅把其调用方普查从 test-only 收紧为零调用方 | 硬不变量（公开签名） |
| **bandit 按臂分片 / 数据面布局重构**（缩小竞争粒度） | 零流量下零收益 | 拒列：数据面布局变更（X0-2 + G/J 面 NAMESHAPE/BYTESHAPE 禁移植邻域）；竞争流量恒零 | X0-2 + R20 PLAN 禁令 |
| **纯函数位点并发维度普查**（collect/outcomes/diagnose/apply/patterns 等全体） | — | 无共享态即无并发收益位：worker 并行化 = S9-C-1 同族契约级不可行（公开同步签名 + 确定性基底）；十二维已有裁决原样覆盖 | S9-C-1 族 + R14-E..R19-E |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（二十轮）、src/test 自 R19-E 基线零漂移、调用位点行号全数复核一致（§1） | — |

**提议排除 ID：无（本轮无「新增排除 ID」表——空枚举未铸任何
S20-E-\*）。** 切片在现行排除表与硬不变量下不存在可达数十 ms 落地线
的候选。本切片「无落地余量」证明自此覆盖 **CPU + 规模 + 分配 + 协议 +
累积状态 + 载荷形态 + 异常面 + 构成面 + 执行层级 + 字符宽度 + 输入
表示 + 输入次序 + 进程共存（本轮）十三个维度**。

## 3. 逐文件收口（前十九轮收口之上的本轮新检查点）

| 文件 | 本轮第二十遍新视角检查 | 结论 |
| --- | --- | --- |
| `bandit-store.ts` | **并发面首次全谱定价**：单飞事务 0.65–1.07ms（在带）；竞争量化税 1.9–12.5ms；锁消除反事实丢 88–99% 更新（锁 = 正确性地板）；就地写撕裂窗 5.5–6.0%（无锁读者，零生产流量）；`loadProjectBandit` 收紧为零调用方；锁调用点默认参数钉死；fsync/mkdir/序列化/跳写按令未触碰 | 无候选 |
| `auto-loop.ts` | C5 全链碰撞税 10.71–11.32ms ≈ 一个 retry 量子（两锁等待重叠，无隐藏第二笔）；`updateProjectBandit` 门控（`:88`）与唯一调用点（`:89`）首手复核；S1-E-4/5、S2-E-1、S4-E-2、X0-3 维持 | 无候选 |
| `from-episode.ts` | propose 腿的 registry 锁属 D 面（R20-D 已价），本切片只持调用边；跨进程 contentHash 去重见证（恰一次 created）在 `proposeRoutingFromOutcomes` 同构生效；既有拒列全维持 | 无候选 |
| `learned-routing.ts` | apply/load 复测在带（257–283ns / 25.0–36.0 / 75.3–119.1µs）；apply 纯函数无并发面；S8-E-1 未做去重分析，其新鲜性理由获 C5 跨进程写者实证支撑；S13-B-1 边界零触碰 | 无候选 |
| `signals.ts` / `task-success.ts` / `diagnostics.ts` | 纯函数面，无跨进程共享态，无并发收益位；本夹具构成（collected=20 / outcomes=1 / diagnoseGroups=1）经 cmp17 对调证实带差即构成差；十二维既有裁决维持 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | test-only 面复核仍零生产调用方，自动拒列；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核（依赖 `pnpm install
--frozen-lockfile`，pnpm 10.17.1）：

```bash
# Node v22.22.2（nvm bin PATH 前置；node --version 复核 v22.22.2）
pnpm gate        # typecheck ✓ + lint ✓ + test ✓ + build ✓
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（计数与 R14-E..R19-E 逐一相同）

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.14.0 与 v22.22.2 均过）
```

锚点脚本（临时置于 `/tmp/r20e-anchor.mts`，未入库以遵守无赢家纪律；
构造见附录 A）共 **16 次独立运行**（8 × v22.14.0 + 8 × v22.22.2），
每次 3/3 sanity 全过，确定性 VERDICT 行跨 16 次运行**逐位一致**；另
**6 次 R17-E 种子比对运行**（cmp17 模式）VERDICT 行 6/6 逐位一致且与
R17-E 夹具行逐字相同。代表性一次运行（v22.14.0）：

```text
VERDICT fixtures: E=41 collected=20 routedE=41 outcomes=1 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=14.9us outcomes=1.5us diagnose=1.02us bandit-build=0.7us | total in-slice CPU ~18.1us per run vs landing bar >=10000us (552x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=283ns/task | empty policy=116ns/task -> x10 tasks/run = 2.83us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=30.8us | realistic registry (baseline+1 candidate)=83.8us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

并发轴套件（`/tmp/r20e-conc.mts` + 6 个子进程脚本，未入库；构造见
附录 A）共 **6 次独立套件运行**（3 × v22.14.0 + 3 × v22.22.2），每次
12/12 sanity 全过（互斥见证 32/32/运行、C3 锁内守恒、C5 双臂恰一次
created、C4 双臂读者推进）。代表性一次运行（v22.22.2）：

```text
BENCH C1 solo real updateProjectBandit: ms=0.52-2.94 median=0.71
BENCH C1 contended H=3ms: op ms=11.07-13.15 median=11.93 | tax(median-solo)=11.22ms | overlapped=8/8 exclusionOk=8/8
BENCH C1 contended H=20ms: op ms=21.52-33.53 median=22.93 | tax(median-solo)=22.22ms | overlapped=8/8 exclusionOk=8/8
BENCH C2 convoy N=8: makespan ms=63.15-66.28 median=64.09
VERDICT C3 locked conservation: totalPulls=100/100 perArmExact=4/4 elapsed=82ms
VERDICT C3 nolock counterfactual: totalPulls=7/100 lostUpdates=93 elapsed=13ms
VERDICT C4 fixture: banditJsonBytes=9381
BENCH C4 quiet:  READER total=13297 apiUndefined=0 rawEmpty=0 rawParseError=0 rawEnoent=0 p50=0.105 p95=0.138
BENCH C4 storm: STORM writes=2791 elapsed=2000.8 | READER total=14418 apiUndefined=806 rawEmpty=7 rawParseError=9 rawEnoent=0 p50=0.119 p95=0.197
BENCH C5 full-loop: solo ms=2.56-4.58 median=2.90 | dual per-proc ms=3.28-17.36 median=14.22 | dual makespan ms=14.22-17.36 median=15.25 | contention overhead(median dual-proc - solo)=11.32ms
VERDICT total sanity checks: 12, failures: 0
ALL CONC AXIS CHECKS PASSED
```

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round20-r20e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R19-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2.1 空枚举，逐形态有理由；第二十遍以「跨进程
  共享态」透镜普查新点名的全部维度落进实测封死区（碰撞税 ~11ms ×
  零流量、锁消除 = 正确性灾难、撕裂窗零生产观察者）、辖区外
  （锁协议 J 切片 / 原子化 X0-2 功能工作）或既有排除确认；新轴本身
  以「无候选」收口，未发现第十四个机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令
  封死的 I/O 契约面、once-per-run CLI 面或切片外辖区上；CPU 温态面
  即使清零也距门槛 517–564×）。
- 矩阵空洞：无（调用位点行号与 R8-E..R19-E 逐一相同；消费者集合与
  R19-E 逐一相同并新增 C0 首手钉死三事实——updateProjectBandit 单
  调用点、loadProjectBandit 零调用方、锁调用点默认参数；配置态 live
  面与载入面锚点全数复测在带内；双 Node 版本交叉不可分辨；本轮带
  17.7–19.3 由 6 次种子对调运行实证归因为构成效应——R17-E 种子在
  本 VM 复现 18.7–18.9，逐点落在 R17-E 带 18.5–20.2 内）。
- 并发假说（「进程共存维度藏着未测余量或未见风险」）：以五段价格表
  关闭（量化税 1.9–12.5ms/竞争事务且流量恒零；N=8 护航 63.95–73.79ms
  是零流量假设格；锁内守恒 6/6 精确、锁消除丢 88–99%；撕裂窗
  5.5–6.0% 仅对零调用方的无锁读 API 可见；全链碰撞税 ≈ 恰一个 retry
  量子、两锁等待重叠无叠加）。「单进程价格表 × 碰撞数」因子化完成，
  碰撞数生产恒零。
- S13-B-1 边界：`applyLearnedRouting` 本体零改动；重开条件
  （|avoid| ≈ 965–1,063，R16-E 量化）未触发、生产写入方不可触发；
  R13-E 三处无名微观未补铸未重开。S8-E-1 未做任何去重分析，其保留
  理由（并发 promote 新鲜性）本轮获得跨进程写者存在性的实测支撑。
- 跨职权观察（非候选、非 ID，仅记录）：(a) bandit.json 就地写 + 无
  fsync 意味着崩溃恰落在写窗内可留下截断文件，下次锁内读经 fail-soft
  降级为「无先验」= bandit 状态静默重置——advisory 面按设计容忍；
  若未来 bandit 升级为决策依据面，原子化（tmp+rename）应作为功能
  工作立项（X0-2，非本战役职权）。(b) `loadProjectBandit` 是零调用方
  死导出，若未来获得无锁生产调用方，C4 的 5.5–6.0% 撕裂率立即变成
  正确性工单；仅记录。(c) 锁协议的量化税（~1 retry 量子/碰撞）若
  未来出现真实并发调度器（同项目并行 tracked run 成为受支持流），
  可寻址旋钮在 `persist/file-lock.ts`（J 切片），非 E。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R19-E §6（含 R14-E 规模判据、R15-E
分配/GC/噪声地板判据、R16-E 累积状态与载荷形态判据、R17-E 异常面与
构成面判据、R18-E 执行层级与字符宽度判据、R19-E 表示面与次序面判据），
并新增一条可标定判据：**(c) 并发面判据**——若生产结构使同项目同
stateRoot 上的并发 auto-adapt 进程数稳态 ≥2（例如并行 tracked run
成为受支持流），碰撞税 ≈ 10.7–11.3 ms × 每 run 碰撞概率开始计入每 run
预算，届时优先复测 C5 并把旋钮问题移交 J 切片锁协议；或 bandit.json
获得任何无锁生产读者（届时 C4 撕裂率是正确性问题而非性能问题）；或
任一 I/O 契约排除（X0-3 / S2-E-1/4 / S1-G-1 / S1-E-4/5 / S4-E-2 /
S5-E-5 / S6-E-3/4 / S8-E-1）被正式推翻，或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

主锚点脚本全文（`/tmp/r20e-anchor.mts`，`npx tsx /tmp/r20e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R20-E；
`NOW = "2026-08-25T07:30:00.000Z"`；SECTION 1 的 seeds
`0xe20e01`（collect）/ `0xe20e02`（routed）/ `0xe20e03`（signals）——
拒绝采样：三个首选种子一次通过（collected=20>3、outcomes=1>0、
diagnoseGroups=1≥1），无顺延；探针记录
seed→(collected,outcomes,diagnoseGroups)：01→(20,4,1)、02→(19,1,0)、
03→(21,3,1)、04→(13,3,0)、05→(18,3,0)、06→(15,1,0)；SECTION 2 的
`mkdtemp` 前缀 `r20e-fresh-`/`r20e-reg-` 与 detector identity
`r20e-anchor`。另加 CLI 开关 `probe`（打印候选种子构成三元组）与
`cmp17`（置换为 R17-E 的 NOW（`02:30`）与 seeds `0xe17e01/02/03`、
前缀 `r20e-cmp17-`、identity `r20e-anchor-cmp17`，用于 R15-E 协议的
种子对调构成归因运行）。其余逐字同 R10-E 附录 A。

并发轴套件（`/tmp/r20e-conc.mts` 编排器 + 5 个子进程脚本，子进程一律
import `/workspace/dist/`（`pnpm build` 真实产物），`NODE_BIN` 环境变量
选择子进程 node 版本；同步用屏障文件（子进程打印 READY 后 1ms 轮询
`existsSync(go 文件)`），计时用子进程进程内
`performance.timeOrigin + performance.now()`）：

- `r20e-child-holder.mjs`：合成持有者——对 E 锁路径
  （`<banditPath>.lock`）调用真实 `withExclusiveFileLock`（dist、默认
  参数），锁内 sleep H ms，serve 模式逐 rep 打印
  `ACQUIRED i <t>` / `RELEASED i <t>`。
- `r20e-child-updater.mjs`：真实生产事务——dist
  `updateProjectBandit`，固定信号（1 条 deterministic taskSuccess
  PASS，modelId=本子进程臂），serve 模式逐 rep 打印
  `OP i start=<t> end=<t> ms=<x>`（opsPerRep 可调，C3 用 25）。
- `r20e-child-nolock.mjs`：C3 反事实——`updateProjectBandit` 锁内体
  逐字复刻、仅去掉 `withExclusiveFileLock`（banditPath/readBanditFile
  同形）。仅用于丢失更新见证，永不是候选。
- `r20e-child-reader.mjs`：C4 无锁读者——dist `loadProjectBandit`
  循环 durationMs，计 total/apiUndefined 与延迟分位；API miss 后立即
  用原始 readFile 分类（空串 / JSON.parse 抛错 / ENOENT）。
- `r20e-child-storm.mjs`：C4 写风暴——真实 `updateProjectBandit`
  紧循环 durationMs，打印写计数。
- `r20e-child-loop.mjs`：C5 全链——dist `runAutoAdaptLoop`，
  extraSignals = 6 条 deterministic taskSuccess FAIL（modelId
  =badmodel≠primary、带 episodeId）使 actionable 分支 engage（feedback
  6 追加 + bandit 锁 + registry 锁 propose 全链）；serve 模式逐 rep
  打印 `LOOP i start end ms collected issues created reason`。
- 编排器段落：C1（solo 8 rep；H∈{3,5,20,35} 各 8 rep，holder/updater
  常驻 serve、逐 rep 屏障（先 `-h-i` 等 ACQUIRED 再 `-u-i`）、互斥
  见证 opStart<released ⇒ opEnd≥released−0.5ms；H=0 格因亚轮询量子
  持锁无法确定性 engage 而移除，见 §0 注意事项 (b)）；C2（N∈{2,4,8}
  各 4 rep 共屏障，makespan=max(end)−min(start)）；C3（锁臂 4×25 →
  读终态断言精确守恒；反事实臂同形不断言只报告）；C4（200 臂状态
  9,381B；静默读者 1.5s → 风暴写者+读者同窗 2s）；C5（solo 6 rep、
  dual 2×6 rep 共屏障；rep0 弃计入含 createCandidate；断言两臂各恰
  一次 created=true）。sanity 12 项/运行。

16 次主锚点运行原始区间（8 × v22.14.0 + 8 × v22.22.2）：

```text
SLICE-CPU 总量:
  v22.14.0: 18.1 / 19.1 / 19.3 / 18.0 / 18.0 / 18.4 / 17.8 / 18.1 us
  v22.22.2: 18.5 / 17.7 / 18.1 / 18.3 / 18.2 / 18.1 / 18.5 / 18.3 us
collect:    14.4-16.0 us（v22.14.0 14.5-16.0 / v22.22.2 14.4-15.2）
outcomes:   1.5-1.6 us（outcomes=1 构成）
diagnose:   1.02-1.09 us
bandit:     0.7-0.8 us
apply(non): 257-283 ns/task（v22.14.0 257-283 / v22.22.2 266-276）
apply(空):  111-126 ns/task
load fresh: 25.0-36.0 us
load real:  75.3-119.1 us
VERDICT 行: 16/16 逐位一致；sanity 3/3 x16 全过
```

6 次 R17-E 种子比对运行（cmp17）原始值（3 × v22.14.0 + 3 × v22.22.2）：

```text
总量: 18.9 / 18.8 / 18.8 | 18.8 / 18.7 / 18.9 us —— 逐点落在 R17-E 带
  18.5-20.2 之内（也在 R19-E cmp17 带 18.5-19.7 内）
collect 12.8-13.1 / outcomes 4.0-4.2 / diagnose 1.00-1.08 / bandit 0.8
  —— 与 R17-E 逐项同带（12.7-14.1 / 4.0-4.3 / 0.98-1.11 / 0.7-0.8）
VERDICT 行: E=41 collected=15 routedE=41 outcomes=4 S=12 diagnoseGroups=1
  prevArms=3 —— 6/6 逐位一致，与 R17-E 夹具行逐字相同
```

6 次并发轴套件运行原始区间（3 × v22.14.0 + 3 × v22.22.2）：

```text
C1 solo median:   0.81 / 1.06 / 0.79 | 0.71 / 1.07 / 0.65 ms（band 0.45-5.12）
C1 contended median（tax = median − solo median）:
  H=3:  11.55-13.13（10.90-12.07）   H=5:  11.59-13.21（10.82-12.15）
  H=20: 22.55-33.58（21.49-32.52；5/6 落 22.55-23.60，1 次双量子 33.58）
  H=35: 43.50-44.16（42.79-43.67）
  量化税（median − H − solo）: 全格 1.9-12.5 ms —— R20-D 6.4-12.4 交叉核对
  互斥见证: overlapped 8/8 全格全运行；exclusionOk 192/192
C2 makespan median: N=2 2.23-13.93（部分 rep 亚 ms 持锁撞不上）
                    N=4 22.68-34.40    N=8 63.95-73.79（全带 42.81-78.36）
C3 locked:  totalPulls=100/100 perArmExact=4/4（6/6 运行），makespan 80-86 ms
C3 nolock:  lostUpdates 99 / 97 / 88 | 93 / 95 / 95（每运行 /100）
C4 quiet:   reads 12414-14654，undefined 0；p50 0.096-0.117 / p95 0.114-0.148 ms
C4 storm:   writes 2763-3586 每 2s（1382-1793/s）；undefined 806-967 /
            13949-16956（5.5-6.0%）；分类 空串 6-34 / 部分 JSON 1-25 /
            ENOENT 0；p50 0.101-0.124 / p95 0.154-0.201 ms
C5 solo median:     2.58-3.45 ms（rep0 弃计入）
C5 dual per-proc:   13.33-14.32 ms；makespan 15.02-16.16 ms
C5 碰撞开销:        10.71-11.32 ms ≈ 1 retry 量子
C5 created=true:    solo 恰 1 次、dual 对上恰 1 次（6/6 运行）
sanity 12/12 x6 全过；两版本同带
```
