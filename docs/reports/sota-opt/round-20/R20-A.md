MODEL_SLUG=claude-fable-5-thinking-xhigh

# R20-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第二十遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 20 / R20-A（Round 1–19 同区第二十遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `5a7d2fa`（含 R19-F/G 空枚举收口与 R19-J / R20-A 派出）
**分支:** `cursor/r20-a-tracking-twentieth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S20-A-\* ID，切片零代码改动；
整片预算收口第十八次复核成立，并新增两条实测轴——「驻留状态基数维 M
（gate.openMinors）定价」补上 R16-A「输入维度全覆盖」声明的量化盲区
（该普查只覆盖生产流量非零的维度 {E, C, R, V, S, L, codes, G}；M 是
TrackingAssessment payload 中最后一个 schema 不封顶、parse 合法、却因
from-child 路径恒传 `openMinors: []` 而生产恒零的集合维——连同 minor
文本字节一起，本轮给出实测斜率与穿越点），「多 turn 驻留链面
（window.previous + prevSummaryHash + minors 累积/升级）定价」补上公开
runTrackingTurn / rollSummary API 的最后一个从未定价的面：二十遍搜查史
全部 turn 测量都在 from-child 的 fresh-window 形态上，previous 链面
（hashSummary 唯一调用点 roller.ts:68，R9-A 以来生产零流量事实）、
mergeOpenMinors 连击算术与 minor-escalated 软门从未有过实测价。两轴
合并后，turn 格的**驻留状态维（单命令基数 M + 跨 turn 链 K）自此全部
有实测价**，且两轴均只产出归档证人与既有拒绝记录的数据补充，未产出
可落地候选。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R19-A 至本轮基线
（`5a7d2fa`）**逐字节未变**（`git diff 7acb666..5a7d2fa -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，二十遍全程零 diff；
清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2）并全部实际重读）。
调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..5a7d2fa -- src/` **完全为空**（跨 R10…R20 开轮十一段
全部为 docs 提交），生产调用方经全库 grep 复核仍为 `supervisor.ts:483`
（applyTrackingGate/nextTrackingSeq；两个 settle 调用点 601/711 均不传
trackingAssessment ⇒ 生产 O(1) no-op，第十一次复验）/ `coordinator.ts:444` /
`flowchart-run.ts:320`（applyChildThreeLine），每子结果一次（~5 次/run），
事件表几十级（41）。R19-A 的 ~66–75 µs/run 预算天花板经本轮在当前 VM
（Node v22.22.2；本 VM 默认 v22.14.0，按 engines >=22.19.0 经 nvm 切换）
实测复核为 **~64–80 µs/run**（五格锚点矩阵三次独立运行 12.9–16.1
µs/gate，与 R19-A 13.2–15.1 / R18-A 12.7–14.8 同带略宽；两个 no-contract
格 15.4–16.1 复现 R11-A 以来的「PIC/基准次序」现象，单格横向比较不具
意义；量级结论不变——稳态下即使切片零成本，节省上界 ~0.08 ms/run，仍比
落地线低约三个量级）。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
数据补充（3 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项 +
R15-A 5 项 + R16-A 4 项 + R17-A 4 项 + R18-A 4 项 + R19-A 2 项换名检查
全部复核维持，未追认任何 S12…S19-A-\* ID），按令一律不铸新 ID。作为
空枚举的实证支撑，本轮新落档两组数据（§1，三次独立运行稳定；fuzz 计数
三次逐位一致）：

1. **驻留状态基数维 M：gate.openMinors 定价（axis 9a）**：
   `parseGateDecision` 既不封顶 minors 条数也不封顶单条 id/text 字节
   （asString 仅非空校验）⇒ M 与 minor 文本字节是切片仅存的两个
   schema 双不封顶维度，且 R16-A 普查未覆盖（生产恒零）。**生产 M≡0
   引理（F1a）**：from-child 路径每个 window 恒 `openMinors: []` 且无
   previous，evaluateGates 把 input.openMinors 原样回填 gate.openMinors
   ⇒ 700/700 applied assessment 全部 M=0（分布级证人，三次逐位一致）。
   **契约存活面（F1b）**：M ∈ {1,10,100} JSON 往返
   parseTrackingAssessment 全部接受、e2e 追加形态与 M=0 逐位一致
   （+1 TA、payload 按引用携带 minors）、**hashAssessment 对 M 与
   minor 文本字节逐位不变**（哈希面序列化 gate 仅取 {codes, kind}，
   hashSummary 完全不序列化 openMinors——M 双旁路全部哈希地板，与
   R16-A V 轴同构）。**定价（E=41 fresh-apply，三次运行区间）**：
   M ∈ {0,10,100,1000} 全程 6.16–6.32 / 6.64–6.88 / 9.80–10.68 /
   40.51–49.34 µs，线性拟合 **34.4–43.2 ns/minor，其中写侧
   parseTrackingAssessment（parseOpenMinor 循环）承载 96–100%**，
   哈希地板全程平坦（1761–2026 ns 区间内无 M 斜率）；穿越 10 ms/run
   线需 **M ≈ 46,187–58,043**（生产 M=0，任何现实携带量如 M=100 仅
   +3.6–4.4 µs/gate）。**minor 文本字节维斜率为零**（M=100 ×
   {16, 1024, 16384}B 三格 9.57–11.85 µs 无序差，拟合 −0.96~+0.01
   ps/char/minor）：apply 路径对 minor.text 的唯一触碰是 asString 的
   `trim() === ""` 恒等检查（V8 对非空白首尾 O(1)），不拷贝、不哈希
   ——该维**结构性平坦，无穿越点**（与 R16-A L 轴的 schema 封顶封闭
   同级但机制不同）。
2. **多 turn 驻留链面：previous 链 + minors 升级定价（axis 9b）**：
   runTrackingTurn 的 previous 面（含 hashSummary 链、mergeOpenMinors
   连击、minor-escalated 软门）生产零流量（F1a 附带证人：700/700
   summary 无 prevSummaryHash）但全部 parse/API 合法。**契约证人
   （F1c，三次逐位一致）**：20-turn 链上 `summary_k.prevSummaryHash ≡
   hashSummary(summary_{k-1})` 逐环可独立重算；UNOBSERVED 态 minors
   **逐位透明**（P/score/codes/prevSummaryHash 流与无 minors 链完全
   相等，10 turn）；verified-true minors 使 P 精确下沉
   count × minorPDip（0.03，钳位 0——规格回声，零阈值触碰）；升级三腿
   全部按规格触发（复观察在第 3 turn 首次升级 = consecutiveTurns 达 2；
   3 个 verified 立即；touchesConstraint / userRejected 立即；
   UNOBSERVED 永不），升级门 parse 合法 e2e ⇒ soft/minor-escalated ⇒
   queue_analysis（+3: TA+GT+RUN_BLOCKED）。**定价（三次运行区间）**：
   fresh-window turn（生产形态）1941–2111 ns；with-previous turn
   3311–3475 ns（M=0）——增量 ~1.3–1.5 µs 中 hashSummary 隔离实测
   1085–1109 ns 为主体，其余为 mergeConstraints/uniqueStrings×2/
   O(Q·D) filter；每携带一个 minor 加 ~72–93 ns/turn（M=10 格
   4158–4407 ns）；**K-turn 链积分线性无悬崖**（K ∈ {5,25,100} 摊销
   3043–3264 / 3277–3455 / 3280–3464 ns/turn，100-turn 长会话全程仅
   328–346 µs）；**escalated 格 7.55–7.80 µs**，落在 R19-A
   queue_analysis 带（7.70–8.50）边缘重叠——directive 维在 minors
   触发臂上无悬崖。链面唯一的哈希成本（hashSummary）天花板
   ~1.1 µs/turn，任何缓存/增量化候选撞 X1-1/S5-G-2/S5-H-3/X0-6
   （§0-2 拒列）。

R12-A 分解的五大组件占比（hashAssessment×3 ~39–46% / validateEvent
~19–23% / turn ~12–15% / prescore×2 ~9–12% / O(E) 五扫描+拷贝
~1.9–2.2%）在本 VM 逐一复现（§1 表；share 分母取当轮 contract 格，
run3 该格 15.1 µs 处于 PIC 高沿故占比略降，组件绝对值三次同带），
R18-A skip 定价与 R19-A replay 定价抽测复现同带（guard-3 skip
1804–1982 ns = 13.0–14.2% of apply；replay find#1 3128–3297 ns =
20.7–25.6% of apply）。第二十遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；未触碰在飞的
  R19-I / R19-J 分支与文件。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表 545 行，含
  S13-B-1；R12-A…R19-A 空枚举未铸 ID，确认）/ PROGRESS.md /
  round-20/PLAN.md / round-19/PLAN.md / round-19/R19-A.md /
  R18-A…R10-A 收口逐份。EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md
  零触碰。
- 环境：本 VM 默认 Node v22.14.0，按 engines（>=22.19.0）经 nvm 切至
  v22.22.2 后 `pnpm install --frozen-lockfile`，全部测量在 v22.22.2
  完成。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或数据补充，**一律不铸 S20-A-\* ID**。R12-A 13 项、R13-A 8 项、
  R14-A 3 项、R15-A 5 项、R16-A 4 项、R17-A 4 项、R18-A 4 项、R19-A
  2 项换名检查全部复核维持，不再逐条重列；以下 3 项为本轮**新记录**）：
  1. **写侧 minors 校验削减/信任调用方**（在 events.ts 写侧屏障内跳过
     或抽样 parseGateDecision 对 gate.openMinors 的 O(M) 逐项校验，
     使 M 斜率的 96–100% 承载项消失）——**fail-open 反例**：损坏
     minor（非法 status / 负 consecutiveTurns / 非布尔
     touchesConstraint/userRejected / 空 id/text）将静默入表并在
     后续读者（升级判定、lightMinorCount）上发散；= S4-G-2 写侧屏障
     族 + R12-A §0-13 信任调用方本体 + R18-A §0-3 拒绝路径廉价化
     整类；且 M≡0 生产零流量（F1a 700/700），斜率 34–43 ns/minor
     只存在于假想调用方上，拒列；
  2. **hashSummary 链缓存/增量化**（链面上 turn k 对 summary_{k-1}
     整体重哈希——理论上可跨 turn 缓存上一轮已算的 hash 或增量维护
     排序数组）——缓存 = X1-1 隐藏模块状态本体 + S5-G-2 读侧 memoize
     族；增量 hash = S5-H-3 族；排序面 = X0-6/X1-2/S5-A-2 锁死；且
     hashSummary 生产零流量（roller.ts:68 唯一调用点，previous ≡
     undefined，R9-A 事实第十一次复验），天花板 ~1.1 µs/turn，拒列；
  3. **with-previous 合并管道融合/Set 化**（mergeConstraints Map 重建 +
     uniqueStrings ×2 + unresolved×confirmed O(Q·D) filter +
     mergeOpenMinors 单遍融合）= S1-A-4/X4-7 Set 化家族 + R17-A §0-2
     （roller O(Q·D) 第四站点）的 previous 侧换位点 + S2-A-1（roller
     快路径 12 µs/episode 拒列）同域；整 with-previous turn 全程仅
     3.3–3.6 µs 且生产零流量，拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  escalated/soft 按结局类特化 = R19-A 结局维收口条款本体（本轮
  escalated 格 7.55–7.80 µs 在 queue band 内，只是其数据补充）；
  shouldEscalateMinors 三遍融合 = S1-A-6 本体（本轮 M 轴给出其规模
  斜率上界，仍零生产流量）；gates/roller 的 openMinors 透传享元 =
  S1-A-7/S7-B-5 可观察身份族；lightMinorCount filter 计数器化 =
  S2-J-6 同构 + turn 内部 R13-A 全点名；minorPDip/软阈值面 = X0-7
  冻结；第二次 prescore = S1-A-2；evidenceRefsOf 双算 = S2-A-2；
  死滚轮尾 = S9-A-1；幂等双 find/死扫描 = S8-A-1 / S11-A-2；写侧
  validateEvent = S4-G-2 族（实现在 events.ts，G 切片）；哈希面 =
  X1-2/S5-H-3/S5-A-2 锁死；human-score 正则面 = X0-6 + S7-A-1 +
  R14-A §0-2；`every×includes` Set 化 = S1-A-4/X4-7（C/R ≈ 10³ 重开
  条件，R15-A 轴 1）；turn 恒定输入常量折叠 = R12-A §0-11 伞 +
  R16-A §0-2；skip 守卫次序/享元/prescore 复用 = R18-A §0-1/2/4
  维持；replay 快路径/重算去重 = R19-A §0-1/2 维持。
- 未重开任何 X\* / S1-\* … S19-\* 条目。R19-A 两轴（重投递面、
  wait_user 指令类）**只做复核所需的抽测**（replay find#1 一格同带
  复现，见 §1；wait_user 格不重测）；R18-A 两轴（skip 组成、拒绝终点
  普查）只抽测 guard-3 skip 一格；R17-A 两轴（事件表组成 mix、冷进程
  预算）、R16-A 三轴（L/V/S）、R15-A 三轴（C/R、GC、JIT）、R14-A
  两轴（E、max-codes）**不补铸、不重测超出复核所需的部分**；
  S11-A-1 / S11-A-2 / S10-A-1 / S9-A-1 / S8-A-1..3 / S7-A-1..4 /
  X0-4/X2-4 / X0-6 / S6-A-1 专项复核：本轮零触碰、零重提，全部维持
  关闭。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足——F1c 的 minorPDip/升级腿证人是对
  既有阈值行为的**回声验证**，未改任何阈值、未改任何测试。不声称
  Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。无赢家 ⇒
  零生产改动，未提交任何仿真脚本（按令不建
  `scripts/round20-r20a-equivalence-sim.ts`），仿真全文见本报告附录。
  不开 PR。

## 1. 预算支配论证复核 + 两条新轴（方法第 1、2 步）

R3-A §3 … R19-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..5a7d2fa -- <切片>` 为空，逐字节
   一致（二十遍全程零 diff）——R1-A…R19-A 全部测量与排除按令转移，但
   数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨十一段）**：`git diff 89f0455..5a7d2fa --
   src/` **完全为空**（R10 开轮以来全部提交均在 docs/ 下）。生产调用方
   三点位（supervisor.ts:483 / coordinator.ts:444 /
   flowchart-run.ts:320）、settle 调用点 601/711 不传 assessment
   （首行 `if (assessment === undefined) return`）、
   `runTrackingTurn` 唯一 src/ 调用方 from-child.ts:81（window 恒
   fresh：无 previous、`openMinors: []`）、`hashSummary` 生产零流量
   （roller.ts:68 唯一调用点）、`parseTrackingAssessment` 唯一 src/
   调用方 events.ts:738、isolation.ts 双函数与 `executionAuthority`
   零 src/ 生产调用方，经本轮全库 grep 全部复验，无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               15.4      15.8      15.9  µs/gate (+1 event)
PASSED x contract(3)               12.9      13.9      15.1  µs/gate (+1 event)
FAILED x no-contract               16.0      16.1      15.8  µs/gate (+3 events)
FAILED x contract(3)               14.0      14.2      13.8  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.6      14.9      14.4  µs/gate (+3 events)
=> ~64–80 µs/run（稳态热）
```

与 R19-A（13.2–15.1 µs/gate ⇒ 66–75 µs/run）同带略宽；五格三次运行
**无悬崖、无空洞**。no-contract 双格 15.4–16.1 复现 R11-A 以来的
「PIC/基准次序」现象（run3 contract 格 15.1 亦落在该漂移沿上），单格
横向比较不具意义。稳态下即使把整个切片优化到零成本，节省上界
~0.08 ms/run，仍比战役落地线（数十~数百 ms 或复杂度类下降）低
**约三个量级**。**支配论证（稳态口径）第十八次复核成立**；生产冷态
口径维持 R17-A 轴 6b 裁决（[2.7, 8.5] ms/run once-per-run，不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R19-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R19-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1959–1970 ns 每次 ⇒ **5.88–5.91 µs** | 5.85–6.02 µs | ~39–46% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 347–370 ns + 第三次 hash） | **2.91–2.96 µs** | 2.80–3.03 µs | ~19–23% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.84–1.98 µs** | 1.71–1.85 µs | ~12–15% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 704–836 ns 每次 ⇒ **1.41–1.67 µs** | 1.33–1.57 µs | ~9–12% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 78–79 + seq 46–54 + 双幂等 find 39–41×2 + status 50–51 + `[...events]` 25–28 ns） | **282–287 ns** | 314–323 ns | **~1.9–2.2%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以当轮 contract 格（12.9–15.1 µs）计；run3 该格处于 PIC 高沿故
占比区间下探。`applyTrackingGate` 整体隔离实测 5.74–6.20 µs，与
R19-A（6.11–6.24）同带略宽。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次运行区间）：guard-3 skip（FAILED 零 evidence refs）1804–1982 ns
= 13.0–14.2% of apply（R18-A 13.6–14.6% 同带）；replay find#1
（GT idempotencyKey 命中，E=44）3128–3297 ns = 20.7–25.6% of apply
（R19-A 22.0–22.5% 同带略宽）——组成天花板 {apply, skip, replay}
两个半边继续成立，不重测其余格。

**本轮新轴 9a：驻留状态基数维 M（gate.openMinors 条数 + minor 文本
字节）**（fresh-apply，E=41，三次运行区间）：

```text
定价格（applyTrackingGate 全程 | 写侧 parse 隔离 | hashAssessment 隔离）:
  M=   0    6.16–6.32 µs | 0.30–0.33 µs | 1961–2026 ns
  M=  10    6.64–6.88 µs | 0.67–0.84 µs | 1789–1942 ns
  M= 100    9.80–10.68 µs | 3.84–4.72 µs | 1788–1832 ns
  M=1000   40.51–49.34 µs | 34.72–41.95 µs | 1761–1836 ns
  => 斜率 34.4–43.2 ns/minor；写侧 parse 承载 96–100%；
     哈希地板全程平坦（M 双旁路 hashAssessment 与 hashSummary）；
     10 ms/run 穿越点 M ≈ 46,187–58,043（生产 M=0）

minor 文本字节维（M=100 固定，text ∈ {16, 1024, 16384}B）:
  三格 9.57–11.85 µs 无序差；拟合 −0.96 ~ +0.01 ps/char/minor
  => 结构性零斜率：apply 路径唯一触碰是 asString 的 trim() === ""
     恒等检查（V8 非空白首尾 O(1)），不拷贝、不哈希、无穿越点

生产 M≡0 引理（F1a）: from-child 恒 openMinors: [] 且无 previous；
  evaluateGates 原样回填 ⇒ 700/700 applied assessment 全 M=0
契约存活面（F1b）: M ∈ {1,10,100} JSON 往返 parse 接受；e2e 追加形态
  与 M=0 逐位一致（+1 TA，payload 按引用携带 minors）；hashAssessment
  对 M 与文本字节逐位不变
```

四个结论：

1. **R16-A 覆盖声明的量化补全**：输入维度实测覆盖自此扩为
   {E, C, R, V, S, L, codes, G} ∪ {M, minor-text}——前者是生产流量
   维（R16-A 收口），后两者是**契约存活但生产恒零**的维（与 R19-A
   wait_user 格同类）。M 是切片继 V 之后第二个「双旁路哈希地板」的
   集合维；minor-text 是切片第一个**结构性零斜率**维（L 轴靠 schema
   封顶封闭，S 轴有 0.5 ns/char/扫描 斜率，本维连斜率都不存在）。
2. **斜率承载项唯一**：96–100% 在写侧 parseOpenMinor 循环（types.ts
   实现、events.ts:738 调用）——「削减该循环」候选是 §0-1 fail-open
   拒列；其余站点（mergeOpenMinors / lightMinorCount filter /
   shouldEscalateMinors / gates 透传 / roller 携带）在 fresh-apply
   路径全部 O(1)（生产 window minors 恒空，assessment.gate.openMinors
   不回流 turn）。
3. **无规模乘数、无悬崖**：M 斜率线性（四格拟合残差在带宽内），穿越
   10 ms/run 需 M ≈ 4.6–5.8×10⁴——高于任何现实 minors 会话（几个）
   约 4 个量级；文本维无穿越点。
4. **不产生候选**：削减写侧校验 = §0-1；minors 进哈希面 = 哈希契约
   变更（X1-2 面）；schema 加 M 上限 = 阈值/schema 面变更（X0-7 类，
   且是功能变更非保行为优化）。

**本轮新轴 9b：多 turn 驻留链面（window.previous + prevSummaryHash +
minors 累积/升级）**（runTrackingTurn 公开面隔离，三次运行区间）：

```text
fresh-window turn（生产形态）                    1941–2111 ns
with-previous turn（M=0 携带）                   3311–3475 ns
with-previous turn（M=3 携带）                   3421–3606 ns
with-previous turn（M=10 携带）                  4158–4407 ns
hashSummary 隔离（生产形状 summary）             1085–1109 ns
K-turn 链积分（摊销/turn）:
  K=  5   3043–3264 ns/turn（15.2–16.3 µs 全程）
  K= 25   3277–3455 ns/turn（81.9–86.4 µs 全程）
  K=100   3280–3464 ns/turn（328.0–346.4 µs 全程）
escalated 格（soft/minor-escalated ⇒ queue_analysis, +3）
                                                 7.55–7.80 µs/gate-apply

契约证人（F1c，三次逐位一致）:
  20-turn 哈希链逐环可独立重算（prevSummaryHash ≡ hashSummary(prev)）
  UNOBSERVED minors 逐位透明（P/score/codes/hash 流与无 minors 链相等）
  verified dip = count × 0.03 精确（钳位 0；规格回声）
  升级三腿按规格触发：复观察第 3 turn（consecutiveTurns 达 2）/
    3-verified 立即 / touches|rejected 立即 / UNOBSERVED 永不
  升级门 parse 合法 e2e ⇒ queue_analysis +3（reasonCode=minor-escalated）
```

四个结论：

1. **链面组成定价完整**：with-previous 增量 ~1.3–1.5 µs/turn，主体是
   hashSummary（~1.1 µs，唯一链面哈希成本），其余为
   mergeConstraints Map 重建 + uniqueStrings ×2 + O(Q·D) filter +
   mergeOpenMinors；每携带 minor 加 ~72–93 ns/turn。**K 链积分线性
   无悬崖**：100-turn 长驻会话全程 328–346 µs，穿越 10 ms 需
   K ≈ 3×10³ turn——比任何现实会话长约 2 个量级，且该面生产流量
   为零（from-child 恒 fresh window）。
2. **directive 维在 minors 触发臂上闭合**：escalated 格 7.55–7.80 µs
   落在 R19-A queue_analysis 带（7.70–8.50）边缘重叠——soft 门经
   minor-escalated 触发与经 soft-threshold 触发同价，R19-A 结局维
   收口在第四个触发臂上复核成立。
3. **minors 语义三分层钉死（F1c）**：UNOBSERVED＝纯载荷（逐位透明、
   永不升级、不入哈希）；verified-true＝分值活跃（P 精确下沉
   count×0.03）；verified+{复观察≥2 turn | ≥3 个 | touches |
   rejected}＝升级触发。三层全部是规格行为的回声验证，零阈值触碰。
4. **不产生候选**：hashSummary 缓存/增量 = §0-2；合并管道融合 =
   §0-3；escalated 特化 = R19-A 结局维条款本体。

**重开条件全景更新（在 R19-A §1 表基础上）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| **M（openMinors 条数）** | **0** | **≈ 4.6–5.8×10⁴（写侧 parse 承载）** | **~4+（对任何现实携带量）** | **本轮** |
| **minor-text（字节）** | **0** | **无穿越点（结构性零斜率：trim 恒等 + 哈希双旁路）** | **—** | **本轮** |
| G（gates/run，caller 侧） | ~5 | ≈ 654–763 | ~2.1 | R16-A 锚点算术 |
| G 组成（apply/skip/replay 全混合） | 全 apply | 混入只降不升 | — | R18-A + R19-A |
| **K（驻留链 turn 数，previous 面）** | **0（恒 fresh）** | **≈ 3×10³ turn（3.3–3.5 µs/turn 线性）** | **~2（对任何现实会话）** | **本轮** |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run，无重复乘数 | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–13.3 µs/fault，无重复乘数 | — | R18-A |
| 重投递面（once-per-redelivery） | 0 次/run | ~2.8–3.3 µs/次，无重复乘数 | — | R19-A + 本轮抽测 |

C 轴（~2.5 个量级，R15-A）仍是切片内部最近的规模悬崖，第一优先重开
对象不变（S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）。本轮两轴
关闭的是**驻留状态维**：R14–R16 关规模维、R17 关组成/温度维、R18 关
组成/故障维、R19 关结局维之后，turn 格仅剩的未定价状态载体
（单命令 minors 基数 M、minors 文本字节、跨 turn previous 链 K）至此
全部有实测价——切片的输入格、结局格、状态格三张格网自此**无一格
无价**。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 3 项 + R12-A…R19-A
43 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、数据
补充或本体，按「换名不铸 ID」纪律未列任何 S20-A-\* 候选。空枚举与
R12-A…R19-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第二十遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `types.ts` | **M 轴斜率承载项定位**（9a：parseOpenMinor 循环承载 96–100%，34–43 ns/minor；削减 = §0-1 fail-open 拒列）；**minor-text 零斜率证明**（asString trim 恒等 O(1)，无拷贝无哈希）；**哈希双旁路证人**（hashAssessment 序列化 gate 仅 {codes,kind}，hashSummary 不序列化 openMinors——M 与 V 同构绕过全部哈希地板）；hashSummary 隔离定价 1085–1109 ns（链面唯一哈希成本，缓存/增量 = §0-2 拒列）；R12-A F1 哈希天花板 + R16-A L/V/S + S2-A-5 / X1-2 / S5-H-3 / S1-A-8 / X0-5 维持 | 无新候选 |
| `turn.ts` | **链面组成定价**（9b：fresh 1941–2111 vs with-previous 3311–3475 ns；mergeOpenMinors 连击算术 F1c 证人——复观察 consecutiveTurns 精确 +1/turn；合并管道融合 = §0-3 拒列）；lightMinorCount filter 与 minorPDip 管道 = R13-A 全点名 + X0-7 冻结；R13-A 八子组件剖面维持（本轮整体 1.84–1.98 µs 同带）；R16-A §0-2/§0-4 维持 | 无新候选 |
| `roller.ts` | **previous 面首次定价**（hashSummary 调用点 roller.ts:68 生产零流量第十一次复验；with-previous 增量 ~1.3–1.5 µs/turn 主体在 hashSummary）；O(Q·D) 第四站点 = R17-A §0-2 维持（previous 侧换位点 §0-3 拒列）；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `gates.ts` | **升级三腿分布级证人**（F1c：复观察第 3 turn / 3-verified / touches|rejected 立即 / UNOBSERVED 永不——shouldEscalateMinors 全分支首次被驱动）；escalated 格 7.55–7.80 µs 在 queue band（R19-A 结局维第四触发臂复核）；S1-A-6 融合 = 本体维持（M 轴给出规模上界，零生产流量）；S7-A-3 / S8-A-3 / R13-A §0-3 享元自败维持 | 无新候选 |
| `gate-apply.ts` | **M 轴 e2e 定价宿主**（9a 四格 6.2–49.3 µs；追加形态对 M 逐位不变 F1b）；escalated 格 e2e（+3 形态 reasonCode=minor-escalated）；replay find#1 抽测 3128–3297 ns 同带复现（R19-A 8a 维持）；S11-A-2 / S8-A-1 / S2-A-4 / R15-A §0-1 replay 死字段维持 | 无新候选 |
| `child-tracking.ts` | guard-3 skip 抽测 1804–1982 ns 同带复现（R18-A 7a 维持）；skip 守卫次序/享元 = R18-A §0-1/2 维持；seq 单调性引理 = R19-A 维持（本轮 F1b 复用其 fresh 路径恒 miss 事实） | 无新候选 |
| `from-child.ts` | **生产 M≡0 / 链恒 fresh 引理宿主**（F1a 700/700：window 恒 `openMinors: []` 无 previous ⇒ assessment.gate.openMinors 恒空、summary 恒无 prevSummaryHash——两条新轴的零流量根源即本文件 73–80 行 window 字面量）；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `prescore.ts` | minorPDip 管道规格回声（F1c dip = count×0.03 精确，钳位 0）；lightMinorCount 注入路径 O(M) 仅在假想 caller；C/R 轴维持 R15-A 穿越点；S1-A-4 / X4-7 / S6-A-1 / S8-A-3 / R12-A §0-2..5 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（humanInput {} ⇒ extractHumanScore({}) 短路；链面 F1c 亦不触 human 臂）；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 维持 | 无新候选 |
| `combined-score.ts` | 链面 score 管道复核（escalated 格 score 由 P 直传，combineScore 短路臂）；TypeError 终点归类维持（R18-A 7b 防御纵深）；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |
| `config.ts` | minorPDip 0.03 冻结面（F1c 规格回声，零触碰）；applyUserThreshold throw 终点归类维持（R18-A 7b）；X0-7 维持 | 无新候选 |
| `analysis.ts` | S8-A-2 维持（零 src/ 生产调用方 + sanitize choke point，本轮 grep 复验） | 无新候选 |
| `isolation.ts` / `index.ts` | O(1) 恒等函数 / 纯再导出；`executionAuthority` 与 isolation 双函数零 src/ 生产调用方复验；runTrackingTurn/rollSummary/mergeOpenMinors 经 index.ts 导出 = 9b 轴契约存活面的载体；惰性 import = R12-A §0-10 维持 | 无新候选 |

**对 R19-A 声明的裁决**：结局维（replay 端点 + directive 类补全）
收口在其维度上成立；本轮把搜查转向**驻留状态维（单命令 M 基数 +
跨 turn 链 K）**，两处新数据**全部只产出归档证人与既有拒绝记录的数据
补充，未产出可落地候选**。收口的预言力第二十遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R19-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F1a: 700 trials, 700 applied, all gate.openMinors=[] & no prevSummaryHash
F1b: M in {1,10,100} parse-legal e2e; hash invariant in M & text bytes
F1c: 20-turn hash chain link-recomputable; UNOBSERVED transparent x10;
     dip=count*0.03 exact; escalation legs per spec; escalated e2e +3
anchor: 15.4 / 12.9 / 16.0 / 14.0 / 14.6 µs/gate => ~64–77 µs/run
decomposition: hash×3=5911ns | validateEvent=2962ns | turn=1913ns
    | prescore×2=1430ns | 5 scans+copy=287ns | applyTrackingGate=5.74µs
spot: skip guard-3 1804ns (14.0%) | replay find#1 3297ns (25.6%)
9a: M=0/10/100/1000: 6.32/6.64/10.50/47.67 µs => 41.3 ns/minor
    (parse 99%); hash flat 2026->1836ns; crossing M ~= 48,215
    text 16/1024/16384B: 11.85/10.99/10.27 µs => -0.96 ps/char/minor
9b: fresh 1994 | prev M=0 3475 | M=3 3606 | M=10 4407 ns
    hashSummary 1108ns; K=5/25/100: 3264/3284/3307 ns/turn
    escalated 7.80 µs
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F1a/F1b/F1c: 计数逐位同 run 1
anchor: 15.8 / 13.9 / 16.1 / 14.2 / 14.9 µs/gate => ~70–80 µs/run
decomposition: hash×3=5890ns | validateEvent=2910ns | turn=1983ns
    | prescore×2=1672ns | 5 scans+copy=282ns | applyTrackingGate=6.20µs
spot: skip guard-3 1982ns (14.2%) | replay find#1 3174ns (22.8%)
9a: 6.18/6.69/10.68/49.34 µs => 43.2 ns/minor (parse 96%);
    hash flat 1967->1761ns; crossing M ~= 46,187
    text: 10.38/10.34/10.39 µs => +0.01 ps/char/minor
9b: fresh 2111 | prev M=0 3441 | M=3 3595 | M=10 4328 ns
    hashSummary 1085ns; K=5/25/100: 3196/3455/3464 ns/turn
    escalated 7.55 µs
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F1a/F1b/F1c: 计数逐位同 run 1
anchor: 15.9 / 15.1 / 15.8 / 13.8 / 14.4 µs/gate => ~69–79 µs/run
decomposition: hash×3=5878ns | validateEvent=2916ns | turn=1839ns
    | prescore×2=1408ns | 5 scans+copy=282ns | applyTrackingGate=5.76µs
spot: skip guard-3 1961ns (13.0%) | replay find#1 3128ns (20.7%)
9a: 6.16/6.88/9.80/40.51 µs => 34.4 ns/minor (parse 100%);
    hash flat 1961->1810ns; crossing M ~= 58,043
    text: 9.61/9.60/9.57 µs => -0.02 ps/char/minor
9b: fresh 1941 | prev M=0 3311 | M=3 3421 | M=10 4158 ns
    hashSummary 1109ns; K=5/25/100: 3043/3277/3280 ns/turn
    escalated 7.76 µs
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立仿真运行 F1a/F1b/F1c 计数逐位一致（700 trial 全 applied、
M 三档 parse 合法、20-turn 链逐环、10-turn 透明流、dip 三档、升级
四腿确定性重现）；锚点五格三次同带无悬崖；9a 四格 + 文本三格与 9b
八格三次运行区间紧。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S20-A-\*）。换名检查 3 项新记录见 §0（1–3），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件本轮**新增两条收口**：

1. **驻留状态基数维（M + minor-text）收口**：切片仅存的两个 schema
   双不封顶维度自此有实测价——M 斜率 34.4–43.2 ns/minor（写侧
   parseOpenMinor 循环承载 96–100%；哈希双旁路；10 ms/run 穿越点
   M ≈ 4.6–5.8×10⁴）、minor-text **结构性零斜率**（trim 恒等 +
   不拷贝不哈希，无穿越点）。重开条件：(a) 任何生产调用方开始携带
   非空 openMinors（届时最先越线的是写侧 parse 循环，候选先撞
   S4-G-2 写侧屏障契约与 §0-1 fail-open 反例），或 (b) minors 进入
   hashAssessment / hashSummary 序列化面（哈希契约变更，需全区
   重审），或 (c) schema 为 minors 加上限（功能变更，非本战役事项）。
2. **驻留链面（previous / K 维）收口**：runTrackingTurn 公开面的
   previous 链全部有实测价——with-previous 3311–3475 ns/turn
   （增量主体 hashSummary 1085–1109 ns）、每 minor 携带 +72–93
   ns/turn、K 积分线性无悬崖（10 ms 穿越 K ≈ 3×10³）、escalated 格
   7.55–7.80 µs 在 queue band（R19-A 结局维第四触发臂）。重开条件：
   `src/` 出现向 runTrackingTurn 传 previous 的生产调用方（长驻
   tracker 会话接线），届时 hashSummary 链成为每 turn 热成本，
   §0-2（缓存/增量化）需按新流量重审——但其 X1-1/S5-G-2/S5-H-3
   反例独立于流量存在，预期结局是「修调用方持有 hash 而非切片加
   缓存」。C 轴（~2.5 个量级，R15-A）仍是切片内部最近的规模悬崖，
   第一优先重开对象不变（S1-A-4/X4-7 家族 `every×includes` 站点
   Set 化，现含 roller 第四站点）。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf20a01`–`0xf20a02`。

```ts
/**
 * R20-A deterministic anchor + decomposition + NEW-axis simulation
 * (twentieth pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R19-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R19-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation,
 *       5-scan O(E) inventory), plus compact spot re-confirmations of
 *       the R18-A skip pricing (guard-3) and the R19-A replay pricing
 *       (find#1) for composition-ceiling continuity;
 *   (c) NEW axis 9a: RESIDENT-STATE cardinality dimension M
 *       (gate.openMinors). R16-A declared input-dimension full coverage
 *       over {E, C, R, V, S, L, codes, G} -- but that census only
 *       covered dimensions with nonzero production flow. M is the last
 *       schema-unbounded collection in the TrackingAssessment payload
 *       (parseGateDecision caps neither the minors count nor the
 *       per-minor id/text byte length), it is parse-legal end to end,
 *       and it is production-zero-flow ONLY because the from-child path
 *       constructs every window with openMinors: [] and no previous
 *       (evaluateGates then echoes input.openMinors into gate.openMinors,
 *       so the appended assessment always carries M=0). Like the V axis
 *       (R16-A), M bypasses ALL hash floors: hashAssessment serializes
 *       gate as {codes, kind} only and hashSummary does not serialize
 *       openMinors at all. This axis (i) proves the M=0 production lemma
 *       distribution-level, (ii) proves parse-legality + hash invariance
 *       + purity for M >> 0, (iii) prices the fresh-apply path at
 *       M in {0, 10, 100, 1000} and isolates the slope carrier
 *       (write-side parseTrackingAssessment / parseOpenMinor), and
 *       (iv) prices the minor-text byte sub-dimension (expected ~zero
 *       slope: asString does typeof + trim identity check only).
 *   (d) NEW axis 9b: MULTI-TURN resident-chain face (window.previous +
 *       prevSummaryHash + minors accumulation/escalation). Every prior
 *       pass measured the from-child fresh-window form; the stateful
 *       face of the public runTrackingTurn/rollSummary API (previous
 *       summary threading, hashSummary chain -- the only call site of
 *       hashSummary, roller.ts:68, production zero-flow since R9-A --
 *       mergeOpenMinors consecutiveTurns arithmetic, and the
 *       minor-escalated soft gate) has never been priced. This axis
 *       prices the with-previous turn, the isolated hashSummary, the
 *       K-turn chain integral (K in {5, 25, 100}), and the escalated
 *       e2e applyTrackingGate cell (soft/minor-escalated ->
 *       queue_analysis directive), closing the resident-state dimension
 *       of the turn lattice.
 *
 * Archival-fact fuzz:
 *   F1a: production M===0 lemma. Seeded fuzz over from-child shapes:
 *        every applied assessment carries gate.openMinors.length === 0.
 *   F1b: M parse-legality + hash bypass + purity. For M in {1,10,100}:
 *        JSON round-trip through parseTrackingAssessment accepts;
 *        hashAssessment is bitwise invariant in M (and in minor text
 *        bytes); e2e applyTrackingGate applies with the same append
 *        shape as M=0 and the appended TA payload carries the minors.
 *   F1c: chain-face witnesses. (i) hash-chain recomputability:
 *        summary_k.prevSummaryHash === hashSummary(summary_{k-1}) for
 *        every k; (ii) UNOBSERVED-status minors are fully transparent:
 *        P / score / codes / prevSummaryHash streams are bitwise equal
 *        with and without them; (iii) verified-true minors dip P by
 *        exactly count * minorPDip (0.03, clamped at 0) -- spec echo,
 *        no threshold touched; (iv) escalation legs fire exactly per
 *        spec: re-observation escalates first at turn 3 (consecutive
 *        turns reaches 2), 3 distinct verified minors escalate
 *        immediately, touchesConstraint / userRejected escalate
 *        immediately; the escalated gate is soft/minor-escalated and
 *        maps to queue_analysis (+3 events) end to end.
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf20a01..0xf20a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  hashSummary,
  parseTrackingAssessment,
  type OpenMinor,
  type RollingSummary,
  type TrackingAssessment,
  type TrackingWindow
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary = opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran";
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

function mkMinor(
  index: number,
  opts?: {
    status?: OpenMinor["status"];
    consecutiveTurns?: number;
    touchesConstraint?: boolean;
    userRejected?: boolean;
    textBytes?: number;
  }
): OpenMinor {
  return {
    id: `minor_${String(index).padStart(6, "0")}`,
    text:
      opts?.textBytes !== undefined
        ? "m".repeat(opts.textBytes)
        : `minor observation ${index}`,
    status: opts?.status ?? "UNOBSERVED",
    consecutiveTurns: opts?.consecutiveTurns ?? 0,
    touchesConstraint: opts?.touchesConstraint ?? false,
    userRejected: opts?.userRejected ?? false
  };
}

function withMinors(
  assessment: TrackingAssessment,
  minors: readonly OpenMinor[]
): TrackingAssessment {
  return { ...assessment, gate: { ...assessment.gate, openMinors: minors } };
}

function gateCommandFor(
  kind: "PASSED" | "FAILED",
  table: readonly Event[],
  opts?: { taskId?: string; minors?: readonly OpenMinor[] }
): { assessment: TrackingAssessment; hash: string; seq: number } {
  const mk = mkChild(kind, { taskId: opts?.taskId });
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({
    observation: obs,
    episodeId: "ep_bench",
    runId: String(RUN_ID)
  });
  if (!decision.apply) throw new Error("fixture must apply");
  const assessment =
    opts?.minors === undefined ? decision.assessment : withMinors(decision.assessment, opts.minors);
  return {
    assessment,
    hash: hashAssessment(assessment),
    seq: nextTrackingSeq(table)
  };
}

/* ============================================================
 * F1a: production M === 0 lemma (from-child path always carries
 * gate.openMinors === [], seeded distribution-level witness).
 * ============================================================ */
{
  const rng = mulberry32(0xf20a01);
  let applied = 0;
  for (let trial = 0; trial < 700; trial += 1) {
    const kind = pick(rng, ["PASSED", "FAILED"] as const);
    const mk = mkChild(kind, {
      taskId: `tsk_f_${trial}`,
      claimSuccessWithCheckGap: rng() < 0.25
    });
    const obs = observationFromChild(mk.child, mk.spec, rng() < 0.5 ? mk.contract : undefined);
    const decision = assessChildObservation({
      observation: obs,
      episodeId: "ep_bench",
      runId: String(RUN_ID)
    });
    if (decision.apply) {
      applied += 1;
      check(
        `F1a production M=0 trial ${trial}`,
        decision.assessment.gate.openMinors.length === 0
      );
      check(
        `F1a turn window minors empty trial ${trial}`,
        decision.turn.summary.openMinors.length === 0 &&
          decision.turn.summary.prevSummaryHash === undefined
      );
    }
  }
  console.log(
    `F1a: 700 fuzz trials, ${applied} applied; every applied assessment carries gate.openMinors=[] ` +
      `and a fresh (previous-less, prevSummaryHash-less) summary -- production M=0 / chain-less lemma holds`
  );
}

/* ============================================================
 * F1b: M >> 0 is parse-legal, hash-bypassing, and pure e2e.
 * ============================================================ */
{
  const base = gateCommandFor("PASSED", events41, { taskId: "tsk_m" });
  const baseHash = base.hash;
  for (const M of [1, 10, 100]) {
    const minors = Array.from({ length: M }, (_, i) => mkMinor(i));
    const augmented = withMinors(base.assessment, minors);
    // hash bypass: hashAssessment ignores gate.openMinors entirely
    check(`F1b hash invariant in M=${M}`, hashAssessment(augmented) === baseHash);
    // parse-legality end to end (JSON round trip)
    const pojo = JSON.parse(JSON.stringify(augmented)) as unknown;
    let parsed: TrackingAssessment | undefined;
    try {
      parsed = parseTrackingAssessment(pojo);
    } catch {
      parsed = undefined;
    }
    check(`F1b M=${M} parse-legal`, parsed !== undefined);
    check(
      `F1b M=${M} parse preserves minors`,
      parsed !== undefined && parsed.gate.openMinors.length === M
    );
    // e2e apply with the same append shape as M=0
    let c = 900000;
    const out = applyTrackingGate({
      events: events41,
      assessment: augmented,
      assessmentHash: baseHash,
      expectedSeq: base.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
    check(
      `F1b M=${M} e2e apply shape`,
      out.result.applied === true &&
        out.result.directive === "none" &&
        out.events.length - events41.length === 1
    );
    const ta = out.events[out.events.length - 1] as unknown as {
      type: string;
      payload: { assessment: TrackingAssessment };
    };
    check(
      `F1b M=${M} TA payload carries minors by reference`,
      ta.type === "TRACKING_ASSESSMENT" && ta.payload.assessment.gate.openMinors.length === M
    );
  }
  // minor-text bytes are also hash-invariant
  const fatText = withMinors(base.assessment, [mkMinor(0, { textBytes: 4096 })]);
  check("F1b hash invariant in minor text bytes", hashAssessment(fatText) === baseHash);
  console.log(
    "F1b: M in {1,10,100} parse-legal e2e (JSON round trip accepted, minors preserved), " +
      "hashAssessment bitwise invariant in M and in minor text bytes (hash floor fully bypassed), " +
      "apply shape identical to M=0 (+1 TA), TA payload carries minors -- M is contract-live"
  );
}

/* ============================================================
 * F1c: chain-face witnesses (hash chain, transparency, dip echo,
 * escalation legs).
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);

function chainWindow(
  previous: RollingSummary | undefined,
  openMinors: readonly OpenMinor[]
): TrackingWindow {
  return {
    ...(previous !== undefined ? { previous } : {}),
    contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
    toolSituations: prescoreInputP.toolSituations,
    constraints: observationP.constraints,
    unresolvedDecisions: ["q_alpha", "q_beta"],
    confirmedDecisions: [],
    openMinors
  };
}

function chainTurn(
  previous: RollingSummary | undefined,
  openMinors: readonly OpenMinor[]
): ReturnType<typeof runTrackingTurn> {
  return runTrackingTurn({
    window: chainWindow(previous, openMinors),
    prescoreInput: prescoreInputP,
    humanInput: {},
    gateFacts: { deterministicFail: false }
  });
}

{
  // (i) hash-chain recomputability over a 20-turn chain
  let previous: RollingSummary | undefined;
  const summaries: RollingSummary[] = [];
  for (let k = 0; k < 20; k += 1) {
    const turn = chainTurn(previous, []);
    summaries.push(turn.summary);
    if (k === 0) {
      check("F1c turn 0 has no prevSummaryHash", turn.summary.prevSummaryHash === undefined);
    } else {
      check(
        `F1c hash chain link k=${k}`,
        turn.summary.prevSummaryHash === hashSummary(summaries[k - 1] as RollingSummary)
      );
    }
    previous = turn.summary;
  }

  // (ii) UNOBSERVED minors are fully transparent (P / score / codes /
  // prevSummaryHash streams bitwise equal with and without them)
  let prevA: RollingSummary | undefined;
  let prevB: RollingSummary | undefined;
  const carried = Array.from({ length: 10 }, (_, i) => mkMinor(i));
  for (let k = 0; k < 10; k += 1) {
    const a = chainTurn(prevA, []);
    const b = chainTurn(prevB, k === 0 ? carried : (prevB?.openMinors ?? []));
    check(
      `F1c UNOBSERVED transparency k=${k}`,
      a.P === b.P &&
        a.score === b.score &&
        a.gate.kind === b.gate.kind &&
        a.summary.anomalyCodes.length === b.summary.anomalyCodes.length &&
        a.summary.prevSummaryHash === b.summary.prevSummaryHash
    );
    check(`F1c UNOBSERVED carried k=${k}`, k === 0 || b.summary.openMinors.length === 10);
    prevA = a.summary;
    prevB = b.summary;
  }

  // (iii) verified-true minors dip P by exactly count * 0.03 (spec echo)
  const clean = chainTurn(undefined, []);
  for (const count of [1, 2, 3]) {
    const verified = Array.from({ length: count }, (_, i) =>
      mkMinor(i, { status: "verified-true" })
    );
    const dipped = chainTurn(undefined, verified);
    const expected = Number(Math.max(0, clean.P - count * 0.03).toFixed(4));
    check(
      `F1c verified dip count=${count}`,
      dipped.P === expected,
      `${dipped.P} vs ${expected}`
    );
  }

  // (iv) escalation legs
  // leg 1: re-observation escalates first at turn 3 (consecutiveTurns hits 2)
  let prev: RollingSummary | undefined;
  let minor = [mkMinor(0, { status: "verified-true" })];
  let firstEscalation = -1;
  for (let k = 1; k <= 5; k += 1) {
    const turn = chainTurn(prev, minor);
    if (firstEscalation === -1 && turn.gate.kind !== "none") {
      firstEscalation = k;
      check(
        "F1c escalation leg 1 gate shape",
        turn.gate.kind === "soft" && turn.gate.codes[0] === "minor-escalated"
      );
    }
    prev = turn.summary;
    minor = turn.summary.openMinors.map((item) => ({ ...item }));
  }
  check("F1c escalation leg 1 fires at turn 3", firstEscalation === 3, String(firstEscalation));
  // leg 2: three distinct verified minors escalate immediately
  const three = chainTurn(undefined, [
    mkMinor(0, { status: "verified-true" }),
    mkMinor(1, { status: "verified-true" }),
    mkMinor(2, { status: "verified-true" })
  ]);
  check(
    "F1c escalation leg 2 (3 verified)",
    three.gate.kind === "soft" && three.gate.codes[0] === "minor-escalated"
  );
  // leg 3: touchesConstraint / userRejected escalate immediately
  const touches = chainTurn(undefined, [
    mkMinor(0, { status: "verified-true", touchesConstraint: true })
  ]);
  const rejected = chainTurn(undefined, [
    mkMinor(0, { status: "verified-true", userRejected: true })
  ]);
  check(
    "F1c escalation leg 3 (touchesConstraint / userRejected)",
    touches.gate.kind === "soft" &&
      touches.gate.codes[0] === "minor-escalated" &&
      rejected.gate.kind === "soft" &&
      rejected.gate.codes[0] === "minor-escalated"
  );
  // UNOBSERVED minors never escalate regardless of count / consecutive
  const inert = chainTurn(undefined, [
    mkMinor(0, { consecutiveTurns: 9, touchesConstraint: true, userRejected: true }),
    mkMinor(1, { consecutiveTurns: 9 }),
    mkMinor(2, { consecutiveTurns: 9 }),
    mkMinor(3, { consecutiveTurns: 9 })
  ]);
  check("F1c UNOBSERVED never escalates", inert.gate.kind === "none");

  // escalated command is parse-legal e2e and maps to queue_analysis (+3)
  const escalatedCmd = ((): { assessment: TrackingAssessment; hash: string; seq: number } => {
    const verified = [mkMinor(0, { status: "verified-true", touchesConstraint: true })];
    const turn = chainTurn(undefined, verified);
    if (turn.gate.kind !== "soft") throw new Error("escalated fixture must be soft");
    const base = gateCommandFor("PASSED", events41, { taskId: "tsk_esc" });
    const assessment: TrackingAssessment = {
      ...base.assessment,
      // rebuild the score fields exactly as from-child would from this turn
      prescore: turn.P,
      score: turn.score,
      gate: turn.gate
    };
    return { assessment, hash: hashAssessment(assessment), seq: nextTrackingSeq(events41) };
  })();
  const pojo = JSON.parse(JSON.stringify(escalatedCmd.assessment)) as unknown;
  let parsedEsc: TrackingAssessment | undefined;
  try {
    parsedEsc = parseTrackingAssessment(pojo);
  } catch {
    parsedEsc = undefined;
  }
  check("F1c escalated assessment parse-legal", parsedEsc !== undefined);
  let c = 950000;
  const escOut = applyTrackingGate({
    events: events41,
    assessment: escalatedCmd.assessment,
    assessmentHash: escalatedCmd.hash,
    expectedSeq: escalatedCmd.seq,
    policyVersion: "track-v1",
    nowIso: NOW_ISO,
    generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
  });
  const escAppended = escOut.events.slice(events41.length);
  check(
    "F1c escalated e2e -> queue_analysis (+3: TA+GT+RUN_BLOCKED)",
    escOut.result.applied === true &&
      escOut.result.directive === "queue_analysis" &&
      escAppended.length === 3 &&
      escAppended[1]!.type === "GATE_TRANSITION" &&
      (escAppended[1] as unknown as { payload: { reasonCode: string } }).payload.reasonCode ===
        "minor-escalated" &&
      escAppended[2]!.type === "RUN_BLOCKED"
  );
  console.log(
    "F1c: 20-turn hash chain recomputable link by link; UNOBSERVED minors bitwise transparent " +
      "(P/score/codes/prevSummaryHash streams equal, 10 turns); verified dip = count*0.03 exact; " +
      "escalation legs fire per spec (re-observation at turn 3 / 3-verified / touches / rejected; " +
      "UNOBSERVED never); escalated gate parse-legal e2e -> queue_analysis (+3); zero violations"
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R19-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    anchorUs.push(one * 1e3);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition of the PASSED x contract(3) cell
 * (R12-A..R19-A methodology, 5-scan O(E) inventory) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side, M=0): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)})`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const cmdGT = gateCommandFor("FAILED", events41, { taskId: "tsk_replay_gt" });
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: cmdGT.assessment,
      assessmentHash: cmdGT.hash,
      expectedSeq: cmdGT.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: cmdGT.assessment,
        assessmentHash: cmdGT.hash,
        expectedSeq: cmdGT.seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }
}

/* ============================================================
 * NEW axis 9a: resident-state cardinality dimension M
 * (gate.openMinors count + minor-text bytes; fresh-apply pricing).
 * ============================================================ */
{
  console.log("--- NEW axis 9a: M (gate.openMinors) pricing (fresh apply, E=41) ---");
  const cells = [0, 10, 100, 1000] as const;
  const gateUs: number[] = [];
  const parseUs: number[] = [];
  const hashUs: number[] = [];
  for (const M of cells) {
    const minors = Array.from({ length: M }, (_, i) => mkMinor(i));
    const cmd = gateCommandFor("PASSED", events41, {
      taskId: "tsk_m_axis",
      minors: M === 0 ? undefined : minors
    });
    let appended = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyTrackingGate({
        events: events41,
        assessment: cmd.assessment,
        assessmentHash: cmd.hash,
        expectedSeq: cmd.seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      appended = out.events.length - events41.length;
    }, M >= 1000 ? 4000 : 20000);
    check(`9a M=${M} apply shape`, appended === 1);
    gateUs.push(one * 1e3);
    const pojo = JSON.parse(JSON.stringify(cmd.assessment)) as unknown;
    const tP = bench(() => void parseTrackingAssessment(pojo), M >= 1000 ? 10000 : 50000);
    parseUs.push(tP * 1e3);
    const tH = bench(() => void hashAssessment(cmd.assessment), 100000);
    hashUs.push(tH * 1e3);
    console.log(
      `  M=${String(M).padStart(4)}: applyTrackingGate ${(one * 1e3).toFixed(2)}us | parse ${(tP * 1e3).toFixed(2)}us | hashAssessment ${(tH * 1e6).toFixed(0)}ns`
    );
  }
  const gateSlope = ((gateUs[3]! - gateUs[0]!) * 1e3) / 1000;
  const parseSlope = ((parseUs[3]! - parseUs[0]!) * 1e3) / 1000;
  const perGateBudgetUs = 2000; // 10 ms/run at ~5 gates/run
  const crossM = ((perGateBudgetUs - gateUs[0]!) * 1e3) / gateSlope;
  console.log(
    `  => slope ${gateSlope.toFixed(1)} ns/minor (parse carrier ${parseSlope.toFixed(1)} ns/minor = ${((parseSlope / gateSlope) * 100).toFixed(0)}%); hash floor flat (${(hashUs[0]! * 1e3).toFixed(0)} -> ${(hashUs[3]! * 1e3).toFixed(0)}ns); 10ms/run crossing M ~= ${Math.round(crossM).toLocaleString("en-US")}`
  );
  // minor-text byte sub-dimension (expected ~zero slope on the apply path)
  const textCells = [16, 1024, 16384] as const;
  const textUs: number[] = [];
  for (const bytes of textCells) {
    const minors = Array.from({ length: 100 }, (_, i) => mkMinor(i, { textBytes: bytes }));
    const cmd = gateCommandFor("PASSED", events41, { taskId: "tsk_m_text", minors });
    const one = bench(() => {
      let c = 100;
      void applyTrackingGate({
        events: events41,
        assessment: cmd.assessment,
        assessmentHash: cmd.hash,
        expectedSeq: cmd.seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    }, 10000);
    textUs.push(one * 1e3);
    console.log(`  M=100 x text=${String(bytes).padStart(5)}B: applyTrackingGate ${(one * 1e3).toFixed(2)}us`);
  }
  const textSlopePs = ((textUs[2]! - textUs[0]!) * 1e6) / (100 * (16384 - 16));
  console.log(
    `  => minor-text byte slope ~${textSlopePs.toFixed(2)} ps/char/minor (asString trim identity; no copy, no hash)`
  );
}

/* ============================================================
 * NEW axis 9b: multi-turn resident-chain face pricing
 * (with-previous turn, hashSummary, K-turn integral, escalated cell).
 * ============================================================ */
{
  console.log("--- NEW axis 9b: resident-chain face pricing (runTrackingTurn public face) ---");
  // fresh-window baseline (production form)
  const tFresh = bench(() => void chainTurn(undefined, []), 100000);
  console.log(`  fresh-window turn (production form): ${(tFresh * 1e6).toFixed(0)}ns`);
  // with-previous cells at M in {0, 3, 10}
  const prevBase = chainTurn(undefined, []).summary;
  for (const M of [0, 3, 10]) {
    const minors = Array.from({ length: M }, (_, i) => mkMinor(i));
    const seeded = chainTurn(undefined, minors).summary;
    const tPrev = bench(() => void chainTurn(seeded, seeded.openMinors), 50000);
    console.log(
      `  with-previous turn (M=${String(M).padStart(2)} carried): ${(tPrev * 1e6).toFixed(0)}ns`
    );
  }
  // isolated hashSummary (the only chain-face hash cost, roller.ts:68)
  const tHashSummary = bench(() => void hashSummary(prevBase), 100000);
  console.log(`  hashSummary isolated (production-shape summary): ${(tHashSummary * 1e6).toFixed(0)}ns`);
  // K-turn chain integrals
  for (const K of [5, 25, 100]) {
    const reps = K >= 100 ? 200 : K >= 25 ? 800 : 4000;
    const tChain = bench(() => {
      let previous: RollingSummary | undefined;
      for (let k = 0; k < K; k += 1) {
        previous = chainTurn(previous, previous?.openMinors ?? []).summary;
      }
    }, reps);
    console.log(
      `  K=${String(K).padStart(3)} chain integral: ${(tChain * 1e3).toFixed(1)}us total = ${((tChain * 1e6) / K).toFixed(0)}ns/turn amortized`
    );
  }
  // escalated e2e cell (soft/minor-escalated -> queue_analysis, +3 events)
  {
    const verified = [mkMinor(0, { status: "verified-true", touchesConstraint: true })];
    const turn = chainTurn(undefined, verified);
    if (turn.gate.kind !== "soft") throw new Error("escalated fixture must be soft");
    const base = gateCommandFor("PASSED", events41, { taskId: "tsk_esc_bench" });
    const assessment: TrackingAssessment = {
      ...base.assessment,
      prescore: turn.P,
      score: turn.score,
      gate: turn.gate
    };
    const hash = hashAssessment(assessment);
    const seq = nextTrackingSeq(events41);
    let directive = "";
    let appended = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyTrackingGate({
        events: events41,
        assessment,
        assessmentHash: hash,
        expectedSeq: seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      directive = out.result.directive;
      appended = out.events.length - events41.length;
    }, 20000);
    check("9b escalated cell shape", directive === "queue_analysis" && appended === 3);
    console.log(
      `  escalated cell (soft/minor-escalated -> queue_analysis, +3): ${(one * 1e3).toFixed(2)}us/gate-apply`
    );
  }
  console.log(
    "  => the resident-chain face is priced: with-previous turn adds ~hashSummary + merge costs, " +
      "K-chain is linear with no cliff, escalated cell sits in the queue_analysis band; no candidate"
  );
}

void mulberry32(0xf20a02);

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
