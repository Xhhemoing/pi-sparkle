MODEL_SLUG=claude-fable-5-thinking-xhigh

# R20-J：数据面切片（cluster / privacy / preferences / episode / persist / track / context / feedback）第二十遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 20 / R20-J
**基线:** `cursor/sota-persistent-opt-83a1` @ `497c312`（独占 tip，含 R20-A..R20-G 七区摄取——全部空枚举——与 R20-H/I/J 派发注记；R20-H 与 R20-I 在飞未触碰）
**分支:** `cursor/r20-j-persist-twentieth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第二十遍枚举为空：无达门槛候选、零新 ID、零生产代码改动、切片维持
关闭（MORE_OPTIMA=no）。**
切片 29 个文件（3655 行，本轮 `wc -l` 复核与 R9-J..R19-J 一致；
3+3+7+5+2+4+2+3 清单逐目录确认恰 29 文件）自 J1 落地（`fb41417`）以来
**逐字节未变**（本轮核对 `git diff fb41417..HEAD -- <29 文件>` 输出为
0 行——J1 之后经 R2-J..R19-J 与本轮共十九遍复查累计零后续代码改动）。
R19-J 基线（`5a7d2fa`）之后 `git log 5a7d2fa..HEAD -- src/` **零提交**
（其间 7 个提交全部为 `docs/reports/` 文档——R19-I/R19-J 摄取、
Round 19 收口、R20-A..R20-G 各波摄取/派发及 R20-H/I/J 派发；R20 前七区
全部空枚举，无一代码落地）⇒ 生产调用图对本切片**可证不变（最强形式，
第十二连）**。按指令在本 VM 重锚配置态 I/O 地板矩阵（三遍 × 各 3 次
运行，§1.3）：preferences `saveToDisk` 非空策略 **125.0–226.7µs/写**
（50 观察档，R19-J 137.1–236.8 / R18-J 128.9–222.7µs 带重叠）～
**420.2–571.8µs/写**（500 观察档，R19-J 453.2–555.5 / R18-J
406.9–525.5µs 带重叠）；jsonl 追加 fsync=false **59.4–73.2µs**
（R17-J 63.6–77.8 / R14-J 61.9–98.5µs 带重叠；下缘 59.4 略低于历史
下沿 = 本遍存储态略快，与 R19-J「整体略偏高」反号互证漂移是 VM 存储
态非代码）/ fsync=true **228.7–446.8µs**（R17-J 226.2–455.9µs 带内）；
配置态删除级联 N=200、20 命中 match=**636.4–763.5µs**（R18-J
619.4–772.6µs 带内，R19-J 664.2–987.4µs 带重叠）/
no-match=**267.0–371.5µs**（R19-J 284.6–375.7 / R17-J 282.6–389.5µs
带重叠，下缘 −17.6µs 落 R15-J A/A 级联噪声 ±168.7–232.8µs/次之内）；
`buildProjectContextIndex` **40.4–41.1µs/构建**（R19-J 40.3–40.7 /
R18-J 40.4–41.5µs 带重叠）；track plan CPU 计数器 **1.03–1.10µs/plan**
（R19-J 1.03–1.13µs 带内）/ 默认 randomUUID **2.51–2.68µs/plan**
（R19-J 2.50–2.71µs 带内）——CPU 三格逐一在带而 I/O 五格整体持平或
略低于 R19-J（与零 diff 互证），**I/O 支配判据第二十次成立**。J1 落地
代码本轮 `scripts/r1j-equivalence-sim.ts` 重跑全绿（**2468 项逐位
检查，reference 10190.8ms → current 3.5ms = 2926.6×**）。第二十遍在
完整排除表（54 个已闭 S\*-J-\* ID + J1 + R10-J..R19-J 十轮空枚举收口）
之上以 Round 20 兄弟区透镜在 J 的一次首落地收口：**输入类别组成剂量
面（R20-F outcome 流组成轴的 J 切片对应物）**——历轮全部 J fixture
都钉死类别配比（级联恒 10% 命中、loop-eval 恒 ~25% tombstone、
redaction e2e 恒全净 body、episode 流恒 open/close 形）而只变规模
（R14-J）、每记录字节（R16-J）、次序与重数（R19-J）；本轮固定 N、
字节总量**与**次序，只变**类别混合比**，问每一类成员的边际价格与
组成悬崖：(C1) 级联命中率剂量 h∈{0,20,100,200}/200（字节补偿构造，
命中/非命中 episodeId 同宽）——零剂量地板 377.2–521.0µs（两笔
fail-closed 读 + 全店扫描 + L119 早退，W-C1a 字节不动点见证两轮
成立），0→20 组成阶跃 **+443.8/+360.4µs**（中位对中位，两轮）=
S6-J-1 串行两笔契约写的入场价本体，写台地内边际每命中
**−0.432/−0.348µs**（负号 = 噪声 + h=200 时 180 条 body 剥除令重写轻
~22KB 抵消剥除拷贝 CPU）⇒ **唯一组成悬崖是 0→1 命中边界即幂等契约
写相位本体（P0 Q2），台地内类别配比零价**；(C2) J1 站点
`evaluatePreferenceLoop` tombstone 密度剂量 d∈{0,25,50,90,100}%
（N=6000 同一数组，仅 Set 成员变）——全程单调近线性，满密度仅
零密度的 **1.18–1.21×**，全摆幅边际 **35.0/38.8ns/tombstone 观察**
（两轮），W-C2 确定性与 W-C2' 满密度语义（无状态存活自身迭代 ⇒
correction/reversal 恒零）见证成立 ⇒ **J1 的 O(1) undo 在密度维度上
最优**（边际常数 ns 级且与 map 尺寸无关，被替换的逐删除重 fold 参考
实现正是 2926.6× 的分母）；(C3) redaction 脏 body 比率剂量
r∈{0,50,100}%（等长脏/净 body，替换非删除保字节均等）——纯 CPU 剂量
边际 **395/526ns/脏记录**（split/join 重组本体；筛扫描因生产策略
needles 恒非空而剂量无关），r=50 落中点近线性无悬崖，e2e
appendFeedback 两带完全重合（r=0 75.8–103.2 vs r=100 68.9–99.6µs）⇒
**亚 µs CPU 剂量沉没于 59.4–73.2µs jsonl 地板之下**，剥除语义为
S2-J-9/S3-J-1 契约域；(C4) `reduceEpisodeEvents` 事件类型组成
（E=1001 固定）——ATTACH 重 1539.2–1564.4µs vs WAITING 重
19.1–85.7µs vs MIXED 413.6–419.2µs：**同事件数下类别混合比独立移动
价格 ~60–68×**；attach 剂量梯 R∈{250,500,1000,2000} 每事件
425–431→797–820→1542–1557→3012–3069ns 逐级倍增 ⇒ 总代价
**O(R²)**（每 RUN_ATTACHED 的 O(R) `runIds.includes` 重复卫 + O(R)
展开拷贝），10ms 越线外推 R\*≈2.6k attach 事件——但该 reducer
**零生产调用方**（grep 再证：src 内仅 manager.ts 定义 + replay.ts
消费，后者两导出即 R10-J 零调用方面）且生产事件流单集数 attach
（E=9 实测 562–607ns/reduce 全额），按指令判据「零生产流量 + 幻想
规模十毫秒 = 空收口」**不铸 ID**（组成响应与重开前提录档 §2.5）。
四胞全部得出「无候选」（组成阶跃 = 契约写相位本体；密度面 J1 已
最优；脏比面沉没 I/O 地板下；类型混合面零流量幻想规模），按纪律
不铸任何 S20-J-\* ID。指令点名的换名重提检查（J1 / S5-J-3 / S6-J-1 /
S8-J-2 钉死项读码逐条在位；R19-J 重投递剂量 + 次序置换、R18-J skip
普查与整命令组成账目、R17-J 拒绝普查与冷预算、R16-J SYSCENSUS-J 与
payload 字节轴、R14-J 规模越线数字均未重测未补铸；R18-G BYTESHAPE /
R19-G NAMESHAPE / R20-G XPROC / R20-D、R20-E 锁面设计未移植——
file-lock.ts 零 J 切片生产调用方 grep 再证，其 retryMs 梯已由 D/E 自
调用方定价，未重铸）逐条复核全部原样在位（§0）。`pnpm gate` 全绿
（1169 测试 / 78 套件 / 1168 通过 / 1 跳过 / 0 失败）。诚实结论：
枚举为空，不硬凑 ID，与 R10-J..R19-J 及 R20-A..G 空枚举收口同型。
现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/cluster/`（3：mailbox / spawn / host）、`src/privacy/`
  （3：deletion / state-layout / record-classes）、`src/preferences/`
  （7：loop-eval / export / precedence / materialize / service / store /
  types）、`src/episode/`（5：manager / store / closure / events /
  replay）、`src/persist/`（2：file-lock / jsonl）、`src/track/`
  （4：primary-split / plan / clarify / loop）、`src/context/`
  （2：index / packet）、`src/feedback/`（3：redaction / store / types）
  共 **29 文件 3655 行**全量第二十遍实际读码，未依赖前十九轮记忆。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表 545 行）/
  PROGRESS.md / round-20/PLAN.md（含 A–G 七区收口注记与 H/I/J 派单
  注记）/ R19-J.md（空枚举；重投递剂量面 + 输入次序置换面）/
  R18-J.md（空枚举；skip 普查 22 位点 + 整命令组成对账三命令）/
  R17-J.md（空枚举；拒绝路径普查 46 终点 + 冷进程预算 4.0–4.8ms）/
  R16-J.md（空枚举；SYSCENSUS-J + payload 字节形态轴）；R15-J /
  R14-J / R13-J / R12-J / R11-J 收口段略读确认不重铸。J1 未重做
  （本轮重跑其等价仿真全绿 2926.6×）；
  `scripts/r1j-equivalence-sim.ts` 未改动。未开 PR；未编辑
  EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未触碰在飞
  R20-H / R20-I 分支与文件。零生产编辑 ⇒ 按指令**未提交任何新仿真
  脚本**（新轴 harness 源码与 sha256 入附录 B，锚点 harness 出处入
  附录 A，工作区临时件已在 gate 前移出工作树）。
- **指令点名的换名重提检查**（零 diff ⇒ J1 以来逐字节一致，全部落地
  形态数学上原样；本轮读码逐条复核，未列为新候选）：
  - **J1**：`loop-eval.ts` 每主体增量 fold + O(1) tombstone undo 落地
    形态逐字节未变；等价仿真 2468 项逐位全绿（2926.6×）。本轮 C2 胞
    直接测 J1 落地站点的 tombstone **密度剂量响应**——测量性收口
    （证 J1 密度最优），落地形态零改动，且 W-C2 证明每剂量两次评估
    输出逐位相同。
  - **S5-J-3（fail-closed 钉死项）**：`cascadeFeedbackTombstones`
    L111 的 `readFeedbackTombstoneIds` 仍在匹配循环**之前**无条件执行
    ——两读 fail-closed 顺序零形态改动。C1 零剂量地板（377–521µs）
    的构成正是该两读 + 全店扫描；读序在组成剂量面同样最优（读价与
    剂量无关，写价才有组成阶跃）。
  - **S6-J-1（partial-delete 钉死项）**：L121–124 的
    `writeFeedbackRecords` → tombstone 落盘仍为**串行两写**，零并行化/
    攒批/延迟变形。C1 的 0→20 组成阶跃（+443.8/+360.4µs）正是该串行
    两写的入场价直测——阶跃 = 契约本体，无变形空间。
  - **S8-J-2**：`persist/jsonl.ts` L17–22 fsync 路径仍为 appendFile 后
    独立 `open("a")`+sync+close 双句柄形态，一字未动。C3 e2e 胞经
    `appendFeedback` 走 fsync=false 调用方形态（原样）；四调用方
    fsync 旗标零 src 提交下数学上不变。
  - **R19-J 重投递剂量 + 输入次序置换（指令明令勿重铸）**：未重测
    未补铸。本轮全部 fixture 显式钉死两维：每载荷投递恰一次（无重
    数变量），命中/成员位置全程均匀铺设（C1 取 `i % (200/h) === 0`
    均匀位——R19-J B1 已证位置盲，位置维不再是变量）。本轮唯一
    变量是**类别混合比**，与重数、次序两维正交。
  - **R18-J skip 普查 + 整命令组成账目（指令明令勿重铸）**：22 个
    skip 位点与三命令对账未重测未补铸。本轮 C1 h=0 胞与 R18-J
    `deleteEpisodeRecords-noop` 胞**终点不同**：R18-J 在**空店**上
    定价 `records.length === 0` skip（58–61µs）；本轮 h=0 在**满店
    零命中**上定价 L119 `cascaded.length === 0` 早退（377–521µs，
    含全店读扫）——这是组成剂量轴的零剂量端点，与 S0c NO-MATCH 锚
    胞同面（fixture 字节形不同故带略高，267–372 vs 377–521µs，差落
    读扫字节量），按「锚面重测非新 ID」纪律仅列数字不铸 ID。
  - **R17-J 拒绝普查 + 冷进程预算（指令明令勿重铸）**：46 终点与冷
    预算未重测未补铸。本轮四胞全部走**非拒绝路径**（W-C4 显式断言
    无 failClosed；C4 attach 流 runId 逐一互异不触重复卫拒绝）。
  - **R16-J SYSCENSUS-J 与 payload 字节形态轴（指令明令勿补铸）**：
    未重测未补铸。本轮剂量构造显式做到**字节总量逐剂量相同**：C1
    命中/非命中 episodeId 同宽 10 字符（"eps_hit000"/"eps_other0"）
    且 id 定宽；C3 脏/净 body 等长（needle 字符替换非删除，
    `DIRTY_BODY.length === CLEAN_BODY.length` 内联断言）——字节维
    不是变量。
  - **R15-J A/A 与引擎代轴（指令明令勿补铸）**：未重测未补铸——本轮
    仅引用其协议噪声地板（级联 ±168.7–232.8µs/次、fsync
    ±45.4–174.8µs/追加）判读 S0b/S0c 带缘与 C1 台地内负斜率
    （§1.3 / §2.2）。
  - **R14-J 规模越线数字（指令明令勿重铸）**：未重测未补铸；本轮
    C1/C2/C3 全部 fixture 固定在既有协议规模（N=200 店、N=6000
    loop-eval、1000 记录池、2000 追加）。C4 attach 剂量梯
    R∈{250..2000} 的用途是**组成响应的指数辨识**（把 ATTACH 重 vs
    WAITING 重的 ~60–68× 差归因到每 attach 的两个 O(R) 操作），
    站点是 R14-J 生产路径规模矩阵从未覆盖的零调用方 reducer 面
    （R10-J 域）；越线数字（R\*≈2.6k）仅作幻想规模否决依据录档，
    不铸任何规模 ID。
  - **R18-G BYTESHAPE / R19-G NAMESHAPE / R20-G XPROC / R20-D、R20-E
    锁面（指令明令勿移植）**：未移植——无 CJK/emoji/行长/值长变量
    （BYTESHAPE 域，本轮字节逐剂量恒定），无路径深度/组件长度/扇出
    变量（NAMESHAPE 域），无跨进程变量（XPROC 域，四胞全部单进程），
    无锁协议测量（`withExclusiveFileLock` 本轮 grep 再证生产调用方
    全在 pi-adapter / learning / adaptation / cli 四个他区文件，
    J 切片内零调用位点——D/E 已自调用方定价其 retryMs 梯，无 J 拥有
    的未定价锁面可寻）。
  - **S2-J-7（级联无匹配早退，已闭）**：L119 早退原样；C1 h=0 胞
    直测其满店零命中价格（组成轴零剂量端点），引用不重开。
  - **S2-J-9 / S3-J-1 / S9-J-1（redaction 契约族，已闭）**：C3 的
    剥除语义（split/join 重组、恒拷贝、classes 判定）全部为其契约
    实施点，W-C3 见证 secret/pii 分类与剥除后不含 needle，引用不
    重开。
  - **R11-J/R12-J/R13-J 无名微观（指令明令勿补铸）**：
    `context/index.ts` 双扫、`cluster/host.ts` send() 死复核、spawn 链
    双 trim、`mailbox.claimRole` own-mail 重取重存、
    `packet.compileContextPacket` 双遍循环、deletion `statExists`
    catch-all 探针、store `loadFromDisk` 双探针——全部原样在位、
    未补铸。
  - R10-J 零调用方导出收口维持：`evaluatePreferenceLoop`（本轮 grep
    再证：src 内仅定义点）/ `reduceEpisodeEvents` 消费链
    （replay.ts 两导出零生产调用方——C4 裁决的流量前提）/
    `exportForDataset` / `materializeView` / `findConflicts` /
    `durableRecordClassById` / `precedence.ts` 整模块等零生产调用方
    面维持（零 src 提交 ⇒ 数学上不变，§1.2）；S7-J-6 saveToDisk 每写
    `mkdirSync` 自愈保留；S2-J-1 / S1-J-7 / S9-J-1..4 原样。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价（R1 未接线）、
  双 LCB 与双归因保留（X0-11）、晋升 proposal-first、无
  Outcome-supported 声明（Checkpoint F-PROD 仍开放，ADR-005 / X0-1）、
  阈值/测试/CAS/凭据/数据面契约/公开签名不变、**fsync 未去**、
  删除级联 fail-closed 两读顺序与串行两写未动、fail-open 家族全部
  保持关闭，天然成立。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，经 nvm 切换
  v22.22.2 为规范引擎（与 R17-J..R19-J 同注记；本 VM 另有
  `/exec-daemon/node` 遮蔽 PATH 前段，显式前置 `$NVM_BIN` 后
  `node --version` = v22.22.2）；仿真与门禁全绿（§6）。

## 1. 基线不变性、调用图复核与 I/O 地板重锚

1. **切片逐字节未变**：`git diff fb41417..HEAD -- src/cluster
   src/privacy src/preferences src/episode src/persist src/track
   src/context src/feedback` 输出为空（29 文件 3655 行，二十遍全程 J1
   之外零 diff）。R1-J…R19-J 全部逐函数收口与 54 个 S\*-J-\* 已闭 ID +
   R10-J..R19-J 十轮无 ID 收口继承有效。
2. **调用图可证不变（最强形式，第十二连）**：`git log 5a7d2fa..HEAD
   -- src/` **零提交**——R19-J 基线以来 7 个提交全部为 `docs/reports/`
   文档（R19-I/R19-J 摄取、Round 19 收口、R20-A..G 摄取/派发及
   R20-H/I/J 派发）⇒ 生产调用方地图数学上不变。R17-J §1.2 的全量
   re-grep 复证在零 src 提交下继承有效；本轮抽样复核
   `evaluatePreferenceLoop` src 内仅定义点、`reduceEpisodeEvents`
   仅 manager.ts 定义 + replay.ts 消费（R10-J 零调用方面）、
   `withExclusiveFileLock` 生产调用方全在他区四文件（J 内零位点）
   全部原样。
3. **配置态 I/O 地板矩阵重锚**（三遍 × 各 3 次运行，本 VM，规范引擎
   Node 22.22.2；harness 与 R12-J 附录 A 逐字节同源仅换 `r20j-`
   前缀，sha256 见附录 A）：

```text
S0a  saveToDisk 非空策略 50 观察档:  186.0/205.1/125.0 | 226.7/173.3/154.1 | 217.4/201.2/149.8 us/write → 带 125.0–226.7µs（R19-J 137.1–236.8 / R18-J 128.9–222.7µs 带重叠）
S0a' 同上 500 观察档:  512.8/420.2/571.8 | 528.0/427.0/422.1 | 523.5/447.7/444.6 us/write → 带 420.2–571.8µs（R19-J 453.2–555.5 / R18-J 406.9–525.5µs 带重叠——地板随存量伸缩，I/O + 全量 stringify 支配判据第二十次成立）
S0b  jsonl append fsync=false (2000 次):  73.2/64.2/64.1 | 63.9/64.8/59.4 | 65.3/65.8/61.8 us → 带 59.4–73.2µs（R17-J 63.6–77.8 / R14-J 61.9–98.5µs 带重叠；下缘 59.4 略低于历史下沿——本遍存储态略快，与 R19-J 整体偏高反号互证漂移属 VM 非代码）
S0b  jsonl append fsync=true  (300 次):   270.9/256.8/446.8 | 297.0/330.6/284.1 | 232.3/228.7/274.5 us → 带 228.7–446.8µs（R17-J 226.2–455.9µs 带内；R15-J A/A fsync 协议噪声 ±45.4–174.8µs/追加覆盖带宽）
S0c  删除级联 N=200、20 命中 MATCH:    733.6/713.5/639.1 | 711.5/636.4/655.1 | 763.5/680.9/670.0 us → 带 636.4–763.5µs（R18-J 619.4–772.6µs 带内；R19-J 664.2–987.4µs 带重叠）
S0c  删除级联 N=200 NO-MATCH:          275.5/267.0/282.7 | 371.5/278.2/315.3 | 281.1/275.0/313.9 us → 带 267.0–371.5µs（R19-J 284.6–375.7 / R17-J 282.6–389.5µs 带重叠；下缘 −17.6µs 落 R15-J A/A 级联噪声 ±168.7–232.8µs/次之内）
S0d  buildProjectContextIndex: 40.5/40.4/40.4 | 40.8/40.8/41.1 | 40.7/40.5/40.6 us → 带 40.4–41.1µs（R19-J 40.3–40.7 / R18-J 40.4–41.5µs 带重叠）
S0e  track plan CPU: 计数器 gen 1.09/1.09/1.10 | 1.09/1.09/1.10 | 1.03/1.04/1.03 us/plan → 带 1.03–1.10µs（R19-J 1.03–1.13µs 带内）
     默认 randomUUID gen: 2.64/2.62/2.64 | 2.68/2.68/2.68 | 2.56/2.58/2.51 us/plan → 带 2.51–2.68µs（R19-J 2.50–2.71µs 带内）
```

   判读：CPU 三格（S0d/S0e）逐一在历史带内而 I/O 五格整体持平或略低
   于 R19-J（与零 diff 互证：R19-J 偏高、本轮持平略低，双向漂移
   正是 VM 存储协议噪声非代码漂移的形状，R15-J A/A 地板完全覆盖各
   带缘）——**「同路径 µs/ns CPU 为 I/O 地板支配即拒」判据在本 VM
   第二十次成立**。未复制任何前轮数字，全部本 VM 重测。
4. **J1 等价仿真重跑**（生产脚本，未改动）：
   `pnpm exec tsx scripts/r1j-equivalence-sim.ts` → 场景 1（16 例
   确定性边界）64 项 + 场景 2（600 例随机化）2400 项 + 性能夹具
   （N=6000 多主体 ~25% tombstone）reference 10190.8ms → current
   3.5ms（**2926.6×**；reference 逐元素访问 92,590,419 次/评估）。
   `ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)`。

## 2. 第二十遍枚举：输入类别组成剂量面（四胞 + 六见证）→ 全部「无候选」→ 诚实空结果

### 2.1 收口角度

前十九轮已按十九个互不重复的收口角度穷尽：逐函数下界（R1-J）、
调用图（R2-J）、语义融合反例（R3-J）、并行化竞态（R4-J）、fail-open
反例（R5-J）、崩溃序/攒批反例（R6-J）、配置态热路径（R7-J）、fsync
句柄面（R8-J）、原型键等价 + 短路死分支（R9-J）、零调用方导出审计
（R10-J）、结构位点 × 消除机制矩阵（R11-J）、跨函数重复纯计算普查
（R12-J）、fs 动词 × Node API 形态普查（R13-J）、规模越线标定 + 相位
分解（R14-J）、测量学基底 A/A + 引擎代际（R15-J）、内核边界 syscall
普查 + payload 字节形态（R16-J）、fail-closed 拒绝路径普查 + 冷进程
预算（R17-J）、skip 路径普查 + 整命令组成对账（R18-J）、at-least-once
重投递剂量 + 输入次序置换（R19-J）。本轮取 Round 20 兄弟区已落地的
**组成透镜**（R20-F 的 outcome 流组成完备性轴——工作负载组成维度）
在 J 首次落地：历轮全部 J fixture 都把输入**类别混合比**钉死为单一
配置——级联恒 10% 命中（i%10）、loop-eval 恒 ~25% tombstone（i%4）、
redaction e2e 恒全净 body、episode 流恒 open/close 形——而规模
（R14-J）、每记录字节（R16-J）、投递重数与位置/次序（R19-J）都已
各自独立闭合。**类别配比是最后一个从未独立变过的输入维度**：固定
N、字节总量与次序，只变某一类成员的占比，问（i）每一类成员的边际
价格是多少；（ii）配比空间内是否存在组成悬崖（若有，「按类分流/
预筛」即成候选；若无，该轴闭合）。与全部已闭轴正交：配比不是规模
（N 恒定）、不是字节形（逐剂量字节总量相同，见 §0 R16-J 条）、不是
次序（位置均匀铺设恒定，R19-J B1 位置盲已证）、不是重数（每载荷恰
一次投递）。四胞覆盖切片四条真实数据面：删除级联命中率（C1）、J1
站点 tombstone 密度（C2）、redaction 脏 body 比率（C3）、episode
事件类型混合（C4）。R20-G XPROC / R20-D、R20-E 锁面按令未移植
（单进程、零锁测量，file-lock.ts 零 J 内调用位点 grep 再证）。

### 2.2 C1：级联命中率剂量（h ∈ {0,20,100,200}/200，字节补偿）

方法：200 条记录逐剂量字节总量相同（id 定宽 `fb-XXX`；episodeId 取
"eps_hit000"/"eps_other0" 同宽 10 字符；body/summary 逐 i 相同），
命中均匀铺设（`i % (200/h) === 0`，位置维钉死于 R19-J B1 已证盲
型）；每剂量 3 次运行 × 30 迭代 × 两轮，迭代间不计时重播种。见证
内联断言（附录 B）：W-C1a 零剂量后 records.jsonl 与 tombstones.json
**逐字节不变**（无写相位）；W-C1b 侧车恰 +h 个新 id。

```text
       run1（us/cascade × 3 运行）           run2
h=  0: 474.1 / 377.2 / 393.2                521.0 / 461.4 / 406.7   （W-C1a 两轮 30×3 迭代全过）
h= 20: 964.8 / 837.0 / 697.1                790.2 / 889.2 / 821.8   （W-C1b 侧车 +20）
h=100: 671.1 / 656.4 / 683.2                831.3 / 658.8 / 795.0   （W-C1b 侧车 +100）
h=200: 759.8 / 687.8 / 759.3                864.9 / 759.2 / 728.9   （W-C1b 侧车 +200）
台地内边际（中位 h=200 − 中位 h=20，/180）: −0.432 | −0.348 us/命中
0→20 组成阶跃（中位对中位）: +443.8 | +360.4 us
```

**裁决（无候选）**：
1. **剂量响应是阶跃函数不是斜坡**：零剂量地板 377.2–521.0µs =
   S5-J-3 两笔 fail-closed 读（records 全店 + tombstones 侧车）+
   200 条 map 扫描 + L119 早退，W-C1a 证其零写；任何 h>0 立即跳上
   写台地（656.4–964.8µs），台地内 h=20/100/200 三带完全重合、
   边际 −0.432/−0.348µs/命中 = 零（两轮同号为负，量级远小于 R15-J
   A/A 级联噪声 ±168.7–232.8µs/次；负号的结构成因：h=200 时 180 条
   body 各 ~124B 被剥除 ⇒ records 重写轻 ~22KB，字节减免抵消剥除
   拷贝 `{...record, body: undefined}` 的 CPU）。
2. **唯一组成悬崖 = 0→1 命中边界 = 幂等契约写相位入场价本体**：
   阶跃 +360–444µs 恰为 S6-J-1 串行两笔契约写（records 全量重写 +
   tombstones 侧车重写），P0 Q2「re-asserts the tombstones」的实施
   形态，R19-J A1 已证其重投递字节不动点。写相位不可省（契约）、
   不可攒批/并行（S6-J-1 钉死）、台地内类别配比零价 ⇒ **配比空间内
   无任何可寻址预算**。「按命中率预筛」类候选被结构否决：预筛本身
   就是同一遍 O(N) 扫描（早退路径已存在且已闭 S2-J-7）。
3. h=0 端点与 S0c NO-MATCH 锚胞同面（fixture 字节形不同故带略高，
   §0 R18-J 条），h=20 端点与 S0c MATCH 锚胞同面（964.8 高缘落
   R19-J MATCH 带 664.2–987.4µs 内）——剂量轴两端与二十轮锚面自洽。
   **该胞不产生候选。**

### 2.3 C2：J1 站点 tombstone 密度剂量（d ∈ {0,25,50,90,100}%，N=6000）

方法：同一 6000 观察数组（60 主体、createdAt 严格单调、字节恒定），
仅 tombstone Set 成员按 `i % 100 < d` 变化；每剂量 3 次运行 × 40
迭代 × 两轮。见证：W-C2 每剂量两次评估输出逐位相同；W-C2' 满密度
语义——无状态存活自身迭代 ⇒ correctionCost=0、reversalEvents=0、
fit=1（内联断言成立）。

```text
        run1（us/evaluate × 3）             run2                      report（两轮相同）
d=  0%: 1194.8 / 1144.4 / 1138.9           1155.2 / 1102.5 / 1099.8   fit=1, forgetting=0
d= 25%: 1278.9 / 1281.8 / 1278.0           1248.2 / 1239.6 / 1248.0   forgetting=21
d= 50%: 1306.6 / 1300.7 / 1302.2           1270.4 / 1403.0 / 1269.1   forgetting=52
d= 90%: 1572.5 / 1343.2 / 1340.7           1635.0 / 1323.4 / 1310.5   forgetting=784（首 rep 离群，rep2–3 在带）
d=100%: 1353.8 / 1354.2 / 1355.6           1335.1 / 1338.2 / 1333.7   forgetting=1200
全摆幅边际（中位 d=100 − 中位 d=0，/6000）: 35.0 | 38.8 ns/tombstone 观察
满密度/零密度比: 1.183× | 1.211×
```

**裁决（无候选）**：
1. **J1 在密度维度上最优**：密度响应单调近线性、满密度仅 1.18–1.21×
   零密度、边际 35.0/38.8ns/tombstone 观察且与 map 尺寸无关——这
   正是 J1 的 O(1) undo（每 tombstone 命中执行一次常数 Map 撤销）
   的密度签名。被 J1 替换的逐删除重 fold 参考实现在该维度是
   O(N)/tombstone（仿真直证：92,590,419 次逐元素访问、2926.6×
   分母）。**不存在任何按密度分流的更优形态**：d=0 时 tombstone
   probe 是两次 Set.has 未命中（已是下界），d=100 时每观察一次
   undo（语义必需，forgetting=1200 是正确输出）。
2. 剂量改变的是**语义输出**（forgettingEvents 0→21→52→784→1200），
   不是可消除的开销——密度税即遗忘事件核算本体。d=90 两轮首 rep
   离群（1572.5/1635.0）为 GC/预热噪声，rep2–3 回带（1310.5–1343.2
   与 d=100 带连续）。**该胞不产生候选**（J1 密度最优的测量性
   收口）。

### 2.4 C3：redaction 脏 body 比率剂量（r ∈ {0,50,100}%，等长构造）

方法：脏/净 body **等长**（needle 字符替换非删除：`sk-`→`zk_`、
`api_key`→`aqi_kev`，内联断言 `DIRTY_BODY.length === CLEAN_BODY.length`）；
生产策略（redactPII=true、maxBodyChars=400、4 needles）。纯 CPU 胞
1000 记录池 × 100000 次；e2e 胞 `appendFeedback` 2000 次/运行。
见证 W-C3：脏 body 输出 classes 含 "secret" 且剥除后不含任何
needle；净 body 走 pii-only 分类且 redacted=true（断言成立）。

```text
纯 CPU redactFeedback（ns/record × 3 运行）:
  r=  0%: 1336 / 1274 / 1267        1157 / 1135 / 1117
  r= 50%: 1435 / 1424 / 1405        1429 / 1497 / 1393
  r=100%: 1700 / 1669 / 1666        1706 / 1661 / 1659
  边际（中位 r=100 − 中位 r=0）: +395 | +526 ns/脏记录；r=50 落中点（1424 vs 1471.5 | 1429 vs 1398）= 近线性无悬崖
e2e appendFeedback（us/append × 3 运行）:
  r=  0%: 93.0 / 79.9 / 75.8        85.9 / 89.6 / 103.2
  r=100%: 99.6 / 86.2 / 91.0        86.8 / 74.1 / 68.9   → 两带完全重合（75.8–103.2 vs 68.9–99.6）
```

**裁决（无候选）**：
1. **脏比剂量是亚 µs CPU、沉没于 I/O 地板之下**：每脏记录边际
   +395/+526ns = `stripForbidden` 的 split/join 重组本体（仅在命中
   needle 时发生分段重装）；筛扫描本身剂量无关（生产策略 needles
   恒非空 ⇒ split 扫描每记录必走，无匹配时返回单段近零开销）。e2e
   带完全重合直证：该剂量在 59.4–73.2µs jsonl 地板 + stringify 之下
   不可测。
2. **无预筛可加**：在 split/join 前加 `includes()` 预筛 = 对净 body
   重复一遍同长度扫描（split 无匹配已是单遍）、对脏 body 净增一遍
   ——结构性净负。剥除语义（恒拷贝、classes 判定、双 needle 大小写
   臂）全部为 S2-J-9 / S3-J-1 / S9-J-1 已闭契约实施点。
   **该胞不产生候选。**

### 2.5 C4：episode 事件类型组成（E=1001 固定 + attach 剂量梯 + 生产规模）

方法：固定事件总数 E=1001，只变类型混合（ATTACH 重 / WAITING 重 /
MIXED 交错）；attach 剂量梯 R∈{250,500,1000,2000} 辨识指数；生产
规模胞 E=9（4 attach + 4 waiting）。见证 W-C4：三种组成终态断言
（attach 计数 = 1000 / status=WAITING_FOR_USER / 混合 500+WAITING）
且全程无 failClosed（非拒绝路径，与 R17-J 域不相交）。

```text
组成三型（E=1001，us/reduce × 3 运行 × 两轮）:
  ATTACH 重  : 1545.2 / 1539.2 / 1540.5     1564.4 / 1546.3 / 1554.3
  WAITING 重 :   84.3 /   19.1 /   22.7       85.7 /   19.3 /   26.1   （首 rep 预热离群）
  MIXED 交错 :  413.6 / 413.7 / 417.3        419.0 / 419.2 / 417.6
  → 同 E 下类别混合比独立移动价格 ~60–68×（ATTACH 中位 1540.5|1554.3 vs WAITING 中位 22.7|26.1）
attach 剂量梯（ns/event × 3 运行 × 两轮）:
  R= 250: 428 / 430 / 431        425 / 428 / 425
  R= 500: 805 / 797 / 799        820 / 811 / 809
  R=1000: 1542 / 1546 / 1545     1556 / 1557 / 1557
  R=2000: 3034 / 3012 / 3020     3069 / 3042 / 3053
  → 每事件价随 R 逐级 ×1.86–1.96 ⇒ 总代价 O(R²)；10ms 越线外推 R*≈2.6k attach 事件
  内部交叉核验: MIXED（500 attach）中位 413.7|419.0µs ≈ 梯 R=500 全额 399.5–410.8µs + 500 条 waiting 线性余量 —— 价格由 attach 计数单独设定，与总 E 无关
生产规模（E=9，4 attach + 4 waiting）: 603 / 595 / 569 | 607 / 580 / 562 ns/reduce（全额）
```

**裁决（无候选，指令判据直接适用）**：
1. **组成响应真实且可解**：每 RUN_ATTACHED 执行两个 O(R) 操作——
   `runIds.includes(runId)` 重复卫 + `[...runIds, runId]` 不可变
   拷贝——合计 O(R²)，梯上 1.86–1.96× 逐级倍增与理论指数吻合；
   WAITING 事件仅常数字段替换（19.1–26.1µs/1001 事件 ≈ 20–26ns/
   事件）。同 E 下类型混合比移动价格 ~60–68×，是本切片首次直测的
   组成敏感面。
2. **但该面零生产流量 + 幻想规模**：`reduceEpisodeEvents` 生产
   调用方为零（grep 再证：src 内仅 manager.ts 定义 + replay.ts
   消费，replay 两导出即 R10-J 已闭零调用方面；episode-bind 与 CLI
   仅引 manager 构造器不引 reducer）；生产事件流每 episode 单集数
   attach（E=9 全额 562–607ns）。10ms 越线需单 episode 攒 ~2.6k
   attach 事件且 reducer 被接进生产热路径——两个前提今日都不成立。
   按指令判据「**零生产流量 + 仅幻想规模十毫秒 = 空收口**」不铸
   ID。重开前提录档：若 replay 面接线生产（R9-J..R19-J 收口原文的
   调用图变更前提）**且** episode-run 扇出真实增长 2–3 个数量级，
   彼时候选是重复卫 Set 化 + 终端一次性数组构建——属数据面契约
   变更（runIds 不可变快照语义），须走契约评审非微观优化。
   **该胞不产生候选。**

### 2.6 其余重扫形状对账

零 src 提交下数学上不可能出现新形状（§1.2）；本轮全量读码以组成
剂量透镜做第二十遍普查，浮出物全部落已闭域（§0 换名检查表逐条）：
级联写侧 `[...tombstones].sort()` 每投递重排序（I/O 地板内 µs CPU +
S6-J-1 契约相位成员；C1 台地内零边际直证其不可测）、
`redactFeedback` 恒拷贝与 classes 数组构造（S2-J-9/S9-J-1 域，C3
sink 值 360000/540000/720000 即 classes 计数的剂量响应——语义输出
非开销）、`recordPreference` 的 `observations.filter` recurrence
孪生（S1-J-2/S2-J-6 域）、claimRole own-mail 重取重存（R12-J
位点）、`reduceEpisodeEvents` runIds 展开 + includes 二次型（本轮
C4 定价，零流量幻想规模空收口，上条）。抽样复核与 R19-J §2.4
对账表逐条一致。

### 2.7 收口陈述

二十遍合计，本切片在当前调用图下**结构上不存在**达门槛候选：
（i）三条 I/O 路径地板 125.0–763.5µs，其上任何等价 CPU 重写不可
寻址（S0a–S0c 二十轮复现）；（ii）纯 CPU 面每 run 合计个位
µs~41µs，距落地线 3–5 个数量级（S0d–S0e）；（iii）R17-J 已证拒绝
面、R18-J 已证无操作面、R19-J 已证重执行面与次序面均无可寻址预算；
（iv）本轮组成面证明：**类别配比维度上不存在从未点名的可寻址
预算**——级联的唯一组成悬崖是幂等契约写相位的入场价本体（0→1 命中
阶跃 +360–444µs，台地内配比零价 ±0.4µs/命中）；J1 站点密度响应
35.0–38.8ns/tombstone 观察即 O(1) undo 的下界签名（满密度仅
1.18–1.21×）；redaction 脏比边际 +395/+526ns/记录沉没于 jsonl 地板
之下且无预筛可加；episode 类型混合面的 O(R²) 组成响应（同 E 移价
~60–68×）落在零生产流量的 R10-J 零调用方面上、越线规模（R\*≈2.6k）
为幻想规模——按指令判据空收口；（v）语义面（fail-closed 两读、
串行两写、fsync、恒拷贝 redaction、幂等重申写、recurrence 证据、锁
协议、崩溃序）全部是钉死反例/明文契约的实施点，无合法变形。重开
本切片的唯一前提维持 R9-J..R19-J 收口原文：调用图变更（test-only 面
接线生产热路径——本轮新增 C4 具体化：replay/reducer 面接线 + 扇出
增长 2–3 个数量级时走契约评审）、或 preferences/feedback 存量真实
增长到 R14-J 标定的 N\* 量级 / R16-J 标定的 V\*/B\* 字节量级——彼时
候选是持久化形态变更（原子改名写、增量落盘）或 REDACTION 策略
变更，须走数据面契约评审而非微观优化。

## 3. 逐文件收口（第二十遍新视角，其余与 R1-J…R19-J 一致）

| 文件 | 本轮组成剂量普查核查 | 结论 |
| --- | --- | --- |
| `cluster/mailbox.ts` | 邮件类别单一（role-cast），无组成维度可变；R19-J A4/B3 重数与位置面已闭，R12-J 位点原样 | 无新候选 |
| `cluster/spawn.ts` | spawn 参数无类别混合面（配额拒绝 = R17-J 域）；双 trim 原样 | 无新候选 |
| `cluster/host.ts` | register/send 无类别配比输入；R11-J 死复核位点原样 | 无新候选 |
| `privacy/deletion.ts` | C1 主体：命中率剂量阶跃 +360–444µs = S6-J-1 写相位入场价本体，台地内配比零价（−0.43/−0.35µs/命中落 A/A 噪声）；W-C1a 零剂量字节不动点两轮 30×3 全过；h=0/h=20 端点与 S0c 两锚胞自洽；S5-J-3 两读顺序在组成面同样最优 | 无新候选 |
| `privacy/state-layout.ts` | 纯路径构造；无组成输入 | 无 |
| `privacy/record-classes.ts` | 静态类别表本体（查表 O(1)）；零生产调用方维持 | 无新候选 |
| `preferences/loop-eval.ts` | **J1 在位，仿真 2468 项全绿（2926.6×）**；C2 密度剂量直证 J1 密度最优（满密度 1.18–1.21×、边际 35.0/38.8ns/tombstone 观察、与 map 尺寸无关）；W-C2/W-C2' 两轮成立 | 无新候选 |
| `preferences/export.ts` | 零调用方维持；导出无类别分流路径 | 无新候选 |
| `preferences/precedence.ts` | 整模块零生产调用方维持 | 整面关闭 |
| `preferences/materialize.ts` | 聚合对 explicit/inferred 类别的处理是语义权重非开销分流 | 无新候选 |
| `preferences/service.ts` | 纯委托层 | 无 |
| `preferences/store.ts` | explicit/inferred 组成变的是 recurrence 语义（S1-J-2/S2-J-6 域）非价格；S0a 双档重锚 125.0–571.8µs；S7-J-6 自愈原样 | 无新候选 |
| `preferences/types.ts` | 类型定义 | 无 |
| `episode/manager.ts` | C4 主体：类型混合移价 ~60–68×（同 E）、attach 面 O(R²)（includes 卫 + 展开拷贝）、生产规模 E=9 全额 562–607ns——零生产调用方 + 幻想规模 ⇒ 按指令空收口，重开前提录档 §2.5；重复事件拒绝 = R17-J 域未触 | 无新候选 |
| `episode/store.ts` | 事件追加对类型盲（序列化字节定价，R16-J 域）；fsync=false 调用方形态原样 | 无新候选 |
| `episode/closure.ts` | decideClosure 对事件类别的分支 = 语义判定本体；S2-J-8 / S7-J-3 / S9-J-4 维持 | 无新候选 |
| `episode/events.ts` | 类型定义（C4 的类别枚举源） | 无 |
| `episode/replay.ts` | reducer 唯一 src 消费点；两导出零生产调用方维持 = C4 空收口的流量前提 | 无新候选 |
| `persist/file-lock.ts` | 零 J 内调用位点（grep 再证：生产调用方全在 pi-adapter/learning/adaptation/cli 四他区文件）；retryMs 梯 R20-D/E 已自调用方定价，按令未重铸 | 无新候选 |
| `persist/jsonl.ts` | 追加对行内容类别盲（C3 e2e 直证：脏/净带完全重合）；S8-J-2 双句柄零改动；S0b 双档重锚 59.4–446.8µs | 无新候选 |
| `track/primary-split.ts` | 纯组装层 | 无 |
| `track/plan.ts` | S0e 重锚 1.03–1.10µs/plan；children 类别由 schema 定义（组成即语义） | 无新候选 |
| `track/clarify.ts` | applyAnswers 对问题类别保序处理（语义）；双视图 miss 面 = R18-J 域 | 无新候选 |
| `track/loop.ts` | 转发下游；S6-J-4 / S1-J-5 / S2-J-11 / S4-J-2 维持 | 无新候选 |
| `context/index.ts` | facts 置信类别（HIGH/MEDIUM/LOW）分桶是输出契约本体；S0d 重锚 40.4–41.1µs；R11-J 位点未补铸 | 无新候选 |
| `context/packet.ts` | 候选/省略分类 = 输出语义；R12-J 位点未补铸；S1-J-7 原位在位 | 无新候选 |
| `feedback/redaction.ts` | C3 主体：脏比边际 +395/+526ns/记录（split/join 重组本体）、近线性无悬崖、e2e 沉没 I/O 地板下、无预筛可加；W-C3 secret/pii 分类见证两轮成立；S2-J-9 / S3-J-1 / S9-J-1 维持 | 无新候选 |
| `feedback/store.ts` | C1/C3 e2e 宿主；级联两相实施点维持（S5-J-3 / S6-J-1）；追加对记录类别盲 | 无新候选 |
| `feedback/types.ts` | 类型定义 | 无 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。切片自 R1-J 落地 J1
（`fb41417`）起经 R2-J..R19-J 与本轮 R20-J 十九遍复查累计零后续代码
改动，逐字节一致。

## 5. 新增排除 ID

**无。** 第二十遍枚举为空——组成剂量面四胞均为测量性裁决而非生产
代码形态：C1 的组成阶跃是 S6-J-1 契约写相位入场价直测（台地内配比
零价）、C2 是 J1 密度最优性的收尾测量、C3 是 S2-J-9 契约族的剂量
定价（沉没 I/O 地板下）、C4 是零流量 R10-J 面上的组成响应录档
（幻想规模按指令空收口）。四组数字（阶跃 +360.4~+443.8µs / 台地
边际 −0.43~−0.35µs/命中；密度边际 35.0/38.8ns/观察、满密度
1.18–1.21×；脏比边际 +395/+526ns/记录、e2e 带重合；类型混合
~60–68×、梯指数 1.86–1.96×/级、R\*≈2.6k、生产 E=9 全额 562–607ns）
录档供后续轮次引用；均不构成任何既有条目的重开证据。R14-J 规模
数字、R15-J A/A 与引擎轴、R16-J SYSCENSUS-J 与字节轴、R17-J 拒绝
普查与冷预算、R18-J skip 普查与命令对账、R19-J 重投递与次序面按
指令未重铸；R18-G BYTESHAPE / R19-G NAMESHAPE / R20-G XPROC /
R20-D、R20-E 锁面未移植。与 R10-J..R19-J 及 R20-A..G 的空枚举收口
同型。

## 6. 测试

零代码改动下相关基线复核，全绿（规范引擎 Node v22.22.2 via nvm，
满足 engines >=22.19.0；系统 Node 22.14.0 过低 + 本 VM
`/exec-daemon/node` PATH 遮蔽的环境注记见 §0）：

```bash
pnpm exec tsx scripts/r1j-equivalence-sim.ts
# ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)
# perf fixture: reference 10190.8 ms -> current 3.5 ms (2926.6x)
pnpm gate   # typecheck + lint + test + build 全绿
# tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1（全套件，
# 含 cluster/privacy/preferences/episode/persist/track/context/feedback）
```

锚点重锚 harness 与组成剂量轴 harness（临时脚本，无赢家、无新 ID，
按 round01–19 仅赢家 sim 入库的仓库惯例未入库；出处与 sha256 见附录
A/B，工作区临时件已在 gate 前移出工作树——dev lint 的 no-console
规则对树内临时脚本报错，移出后 gate 全绿，生产文件零 lint 问题）。
锚点三遍、组成轴两轮，方向与量级一致（数字见 §1.3 / §2.2–2.5）。

## 附录 A：I/O 地板矩阵 harness 出处（可复现）

脚本与 R12-J 附录 A 归档件**逐字节同源**：自
`docs/reports/sota-opt/round-12/R12-J.md` 附录 A（```ts 围栏内全文）
原样提取为仓库根 `tmp-r20j-anchor.ts`，将其中 `r12j-` 临时目录前缀
替换为 `r20j-`，sha256 =
`d68b389c496d6fd5c4ddf113cc7920d7a8c9d813ff9a2b349befca68a6c7caf6`
（无赢家按纪律不入库，临时文件已移出工作树）。运行方式：
`pnpm exec tsx tmp-r20j-anchor.ts`。三遍输出已列入 §1.3。

## 附录 B：输入类别组成剂量面 harness（完整，可复现）与两轮输出

`tmp-r20j-composition.ts`（sha256
`013dbb4e40e7a90f496e35da4b33f95bb19a824ed04a2d59b20602e5cb55f089`；
无赢家按纪律不入库，工作区临时件已移出）。运行
`pnpm exec tsx tmp-r20j-composition.ts`。W-C1a/W-C1b/W-C2/W-C2'/
W-C3/W-C4 为内联断言的结构性见证（零剂量字节不动点 / 侧车 +h 计数 /
逐位确定性 / 满密度语义 / 剥除与分类语义 / 组成终态与非拒绝）。

```ts
/**
 * R20-J new-axis harness: input class-composition dose face.
 * J-slice counterpart of the R20-F outcome-stream-composition cell. Every
 * previous J round fixed the class-mix ratio of its fixtures (cascade 10%
 * hits, loop-eval 25% tombstones, redaction all-clean bodies, episode
 * streams open+close shaped) while varying scale (R14-J), per-record bytes
 * (R16-J), order and multiplicity (R19-J). This harness fixes N, byte
 * totals AND order, and varies only the CLASS MIX, asking what the marginal
 * price of one class member is and whether any composition cliff opens an
 * addressable candidate.
 * Cells:
 *   C1 deletion-cascade hit-ratio dose h in {0,20,100,200}/200 with
 *      byte-compensated episode ids ("eps_hit000" vs "eps_other0", both
 *      10 chars; record ids fixed-width) -> marginal per-hit price +
 *      zero-dose no-write fixpoint witness + sidecar dose witness.
 *   C2 evaluatePreferenceLoop (J1 site) tombstone-density dose
 *      d in {0,25,50,90,100}% at N=6000: identical observation array,
 *      only Set membership varies -> J1 O(1)-undo density response.
 *   C3 redaction dirty-body ratio r in {0,50,100}% with equal-length
 *      dirty/clean bodies (needle chars substituted, not removed) ->
 *      pure-CPU redactFeedback dose + e2e appendFeedback dose +
 *      strip witness.
 *   C4 reduceEpisodeEvents event-type composition at fixed E=1001
 *      (ATTACH-heavy / WAITING-heavy / MIXED) + attach-dose ladder
 *      R in {250,500,1000,2000} + production-scale cell E=9.
 * Temporary script: not committed (no production winner -> report only).
 */
import { mkdtempSync, rmSync, mkdirSync, writeFileSync, readFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join, dirname } from "node:path";

import { cascadeFeedbackTombstones } from "./src/privacy/deletion.js";
import { appendFeedback, feedbackLogPath, feedbackTombstonesPath } from "./src/feedback/store.js";
import { redactFeedback } from "./src/feedback/redaction.js";
import { evaluatePreferenceLoop } from "./src/preferences/loop-eval.js";
import type { PreferenceObservation } from "./src/preferences/types.js";
import { reduceEpisodeEvents } from "./src/episode/manager.js";
import type { EpisodeEvent } from "./src/episode/events.js";
import type { FeedbackRecord } from "./src/feedback/types.js";
import type { ProjectEpisode } from "./src/domain/episode.js";
import type { EpisodeId, ProjectId, RunId } from "./src/domain/ids.js";
import type { IsoTimestamp } from "./src/domain/timestamp.js";

const hr = (): bigint => process.hrtime.bigint();
const usOf = (a: bigint, b: bigint): number => Number(b - a) / 1000;
const f = (x: number): string => x.toFixed(x < 10 ? 2 : 1);
const median = (xs: number[]): number => [...xs].sort((a, b) => a - b)[Math.floor(xs.length / 2)]!;

// ------------------------------------------------------------------ C1
/**
 * 200 records; exactly h hits at uniform positions (position-blindness is
 * R19-J B1). Byte totals identical across doses: fixed-width ids, and the
 * episodeId field is "eps_hit000" (hit) or "eps_other0" (non-hit), both
 * 10 chars, every other field identical per index i.
 */
function seedDose(stateRoot: string, h: 0 | 20 | 100 | 200): void {
  const log = feedbackLogPath(stateRoot);
  mkdirSync(dirname(log), { recursive: true });
  const lines: string[] = [];
  let hits = 0;
  for (let i = 0; i < 200; i++) {
    const hit = h !== 0 && i % (200 / h) === 0;
    if (hit) hits += 1;
    lines.push(
      JSON.stringify({
        id: `fb-${String(i).padStart(3, "0")}`,
        episodeId: hit ? "eps_hit000" : "eps_other0",
        kind: "human",
        rubricVersion: "r1",
        score: 50 + (i % 40),
        evidenceRefs: [],
        redacted: false,
        createdAt: "2026-08-25T00:00:00.000Z",
        body: `free text feedback payload number ${String(i).padStart(3, "0")} `.repeat(3),
        summary: `summary ${String(i).padStart(3, "0")}`
      })
    );
  }
  if (hits !== h) throw new Error(`dose must place exactly ${h} hits, got ${hits}`);
  writeFileSync(log, lines.join("\n") + "\n", "utf8");
  writeFileSync(
    feedbackTombstonesPath(stateRoot),
    JSON.stringify(["fb-pre-a", "fb-pre-b", "fb-pre-c", "fb-pre-d", "fb-pre-e"], null, 2) + "\n",
    "utf8"
  );
}

async function cellCascadeDose(): Promise<Record<number, number>> {
  console.log("[C1 cascade hit-ratio dose (N=200 fixed, byte-compensated; h = 0/20/100/200)]");
  const medians: Record<number, number> = {};
  for (const h of [0, 20, 100, 200] as const) {
    const results: number[] = [];
    const all: number[] = [];
    for (let run = 0; run < 3; run++) {
      const stateRoot = mkdtempSync(join(tmpdir(), "r20j-dose-"));
      seedDose(stateRoot, h);
      await cascadeFeedbackTombstones(stateRoot, "eps_warm00" as EpisodeId); // warm no-match
      const iters = 30;
      let total = 0n;
      for (let i = 0; i < iters; i++) {
        seedDose(stateRoot, h); // untimed re-seed
        const beforeRecords = h === 0 ? readFileSync(feedbackLogPath(stateRoot), "utf8") : "";
        const beforeTombs = h === 0 ? readFileSync(feedbackTombstonesPath(stateRoot), "utf8") : "";
        const t0 = hr();
        const out = await cascadeFeedbackTombstones(stateRoot, "eps_hit000" as EpisodeId);
        const t1 = hr();
        if (out.length !== h) throw new Error(`expected ${h} cascaded, got ${out.length}`);
        if (h === 0) {
          // W-C1a: zero-dose leaves both files byte-identical (no write phase).
          if (
            readFileSync(feedbackLogPath(stateRoot), "utf8") !== beforeRecords ||
            readFileSync(feedbackTombstonesPath(stateRoot), "utf8") !== beforeTombs
          ) {
            throw new Error("W-C1a violated: zero-dose cascade must not write");
          }
        }
        total += t1 - t0;
      }
      // W-C1b: sidecar gained exactly h new ids (all ids distinct by seed).
      const tombs = JSON.parse(readFileSync(feedbackTombstonesPath(stateRoot), "utf8")) as string[];
      const newTombs = tombs.filter((id) => !id.startsWith("fb-pre-")).length;
      if (newTombs !== h) throw new Error(`W-C1b violated: expected +${h} sidecar ids, got ${newTombs}`);
      results.push(Number(total) / 1000 / iters);
      all.push(Number(total) / 1000 / iters);
      rmSync(stateRoot, { recursive: true, force: true });
    }
    medians[h] = median(all);
    console.log(`  h=${String(h).padStart(3)}: ${results.map(f).join(" / ")} us/cascade  (W-C1${h === 0 ? "a: no-write fixpoint held" : "b: sidecar +" + h}) `);
  }
  const slope = (medians[200]! - medians[20]!) / 180;
  console.log(
    `  marginal per-hit price (median h=200 minus median h=20, /180): ${slope.toFixed(3)} us/hit; zero-dose floor = two fail-closed reads (S5-J-3 order)`
  );
  return medians;
}

// ------------------------------------------------------------------ C2
interface LoopFixture {
  readonly observations: readonly PreferenceObservation[];
}

function makeObservations(): LoopFixture {
  const N = 6000;
  const SUBJECTS = 60;
  const scopes = ["user", "project", "role"] as const;
  const observations: PreferenceObservation[] = [];
  const base = Date.parse("2026-01-01T00:00:00.000Z");
  for (let i = 0; i < N; i++) {
    const s = i % SUBJECTS;
    const valuePick = (i + s) % 5;
    observations.push({
      id: `evd_${String(i).padStart(4, "0")}`,
      scope: scopes[s % 3]!,
      scopeKey: `sk${s}`,
      key: `k${s % 12}`,
      value: valuePick < 2 ? `v${valuePick}` : valuePick === 2 ? i % 7 : valuePick === 3 ? true : "v9",
      evidenceEpisodeId: `eps_${i.toString(36)}` as EpisodeId,
      weight: 1.0,
      createdAt: new Date(base + i * 1000).toISOString() as IsoTimestamp,
      explicit: i % 5 === 4,
      recurrenceCount: 1
    });
  }
  return { observations };
}

function tombstonesAtDensity(observations: readonly PreferenceObservation[], densityPct: number): Set<string> {
  const tombstones = new Set<string>();
  observations.forEach((obs, i) => {
    if (i % 100 < densityPct) tombstones.add(obs.id);
  });
  return tombstones;
}

function cellLoopEvalDensity(): void {
  console.log("[C2 evaluatePreferenceLoop (J1 site) tombstone-density dose (N=6000 fixed array; d = 0/25/50/90/100%)]");
  const { observations } = makeObservations();
  for (const d of [0, 25, 50, 90, 100]) {
    const tombstones = tombstonesAtDensity(observations, d);
    // W-C2: determinism witness — two evaluations bit-identical.
    const r1 = JSON.stringify(evaluatePreferenceLoop(observations, tombstones));
    const r2 = JSON.stringify(evaluatePreferenceLoop(observations, tombstones));
    if (r1 !== r2) throw new Error("W-C2 violated: reports must be deterministic");
    if (d === 100) {
      const report = JSON.parse(r1) as { correctionCost: number; reversalEvents: number; fit: number };
      // W-C2': full-density semantics — no state ever survives its own
      // iteration, so no correction/reversal can be observed.
      if (report.correctionCost !== 0 || report.reversalEvents !== 0 || report.fit !== 1) {
        throw new Error("W-C2' violated: d=100% must observe no corrections/reversals");
      }
    }
    const results: number[] = [];
    let sink = 0;
    for (let run = 0; run < 3; run++) {
      for (let i = 0; i < 5; i++) sink += evaluatePreferenceLoop(observations, tombstones).correctionCost;
      const iters = 40;
      const t0 = hr();
      for (let i = 0; i < iters; i++) sink += evaluatePreferenceLoop(observations, tombstones).correctionCost;
      const t1 = hr();
      results.push(usOf(t0, t1) / iters);
    }
    console.log(`  d=${String(d).padStart(3)}%: ${results.map(f).join(" / ")} us/evaluate  [report=${r1}] [sink=${sink}]`);
  }
}

// ------------------------------------------------------------------ C3
const DIRTY_BODY =
  "release notes body: token sk-EXAMPLE0001 plus api_key reference inline; " +
  "the remainder of this feedback body is ordinary prose that pads the record " +
  "to a fixed byte width for the dose comparison.";
// Equal length, no needle matches: substitute (not remove) the needle chars.
const CLEAN_BODY = DIRTY_BODY.replace("sk-", "zk_").replace("api_key", "aqi_kev");

function recordOf(id: string, dirty: boolean): FeedbackRecord {
  return {
    id,
    episodeId: "eps_dose00" as EpisodeId,
    kind: "human",
    rubricVersion: "r1",
    score: 77,
    evidenceRefs: [],
    redacted: false,
    createdAt: "2026-08-25T00:00:00.000Z" as FeedbackRecord["createdAt"],
    body: dirty ? DIRTY_BODY : CLEAN_BODY,
    summary: "fixed-width summary text"
  };
}

const PRODUCTION_POLICY = {
  redactPII: true,
  maxBodyChars: 400,
  forbiddenSubstrings: ["sk-", "api_key", "API_KEY", "BEGIN PRIVATE"]
} as const;

async function cellRedactionDose(): Promise<void> {
  console.log("[C3 redaction dirty-body ratio dose (equal-length bodies; r = 0/50/100%)]");
  if (DIRTY_BODY.length !== CLEAN_BODY.length) throw new Error("byte parity violated");
  // W-C3: strip semantics witness.
  {
    const dirtyOut = redactFeedback(recordOf("fb-w-d", true), PRODUCTION_POLICY);
    const cleanOut = redactFeedback(recordOf("fb-w-c", false), PRODUCTION_POLICY);
    if (
      !dirtyOut.decision.classes.includes("secret") ||
      dirtyOut.feedback.body === undefined ||
      dirtyOut.feedback.body.includes("sk-") ||
      dirtyOut.feedback.body.includes("api_key")
    ) {
      throw new Error("W-C3 violated: dirty body must have needles stripped");
    }
    if (
      cleanOut.decision.classes.includes("secret") ||
      cleanOut.decision.classes.join(",") !== "pii" ||
      cleanOut.feedback.redacted !== true
    ) {
      throw new Error("W-C3 violated: clean body must carry pii-only redaction (production policy)");
    }
  }
  // (a) pure CPU dose.
  for (const r of [0, 50, 100]) {
    const records: FeedbackRecord[] = [];
    for (let i = 0; i < 1000; i++) {
      records.push(recordOf(`fb-${String(i).padStart(4, "0")}`, (i % 100) < r));
    }
    const results: number[] = [];
    let sink = 0;
    for (let run = 0; run < 3; run++) {
      for (let i = 0; i < 20000; i++) sink += redactFeedback(records[i % 1000]!, PRODUCTION_POLICY).decision.classes.length;
      const N = 100000;
      const t0 = hr();
      for (let i = 0; i < N; i++) sink += redactFeedback(records[i % 1000]!, PRODUCTION_POLICY).decision.classes.length;
      const t1 = hr();
      results.push((usOf(t0, t1) / N) * 1000); // ns per record
    }
    console.log(`  CPU redactFeedback r=${String(r).padStart(3)}%: ${results.map((x) => x.toFixed(0)).join(" / ")} ns/record  [sink=${sink}]`);
  }
  // (b) e2e appendFeedback dose (production path: redact + stringify + jsonl append).
  for (const r of [0, 100]) {
    const stateRoot = mkdtempSync(join(tmpdir(), "r20j-redact-"));
    const results: number[] = [];
    for (let run = 0; run < 3; run++) {
      const iters = 2000;
      for (let i = 0; i < 100; i++) await appendFeedback(stateRoot, recordOf(`fb-warm-${run}-${i}`, r === 100));
      const t0 = hr();
      for (let i = 0; i < iters; i++) await appendFeedback(stateRoot, recordOf(`fb-${run}-${i}`, r === 100));
      const t1 = hr();
      results.push(usOf(t0, t1) / iters);
    }
    console.log(`  e2e appendFeedback r=${String(r).padStart(3)}%: ${results.map(f).join(" / ")} us/append`);
    rmSync(stateRoot, { recursive: true, force: true });
  }
}

// ------------------------------------------------------------------ C4
function openedEvent(): EpisodeEvent {
  const episode: ProjectEpisode = {
    id: "eps_compose01" as EpisodeId,
    projectId: "prj_compose01" as ProjectId,
    objective: "composition dose fixture",
    contractVersion: 1,
    runIds: [],
    startedAt: "2026-08-25T00:00:00.000Z" as IsoTimestamp,
    status: "OPEN",
    acceptance: [],
    evidenceRefs: []
  };
  return { type: "EPISODE_OPENED", episode, occurredAt: episode.startedAt };
}

function attachEvent(i: number): EpisodeEvent {
  return {
    type: "RUN_ATTACHED",
    episodeId: "eps_compose01" as EpisodeId,
    runId: `run_${String(i).padStart(4, "0")}` as RunId,
    attachedAt: "2026-08-25T00:00:01.000Z" as IsoTimestamp
  };
}

function waitingEvent(i: number): EpisodeEvent {
  return {
    type: "EPISODE_WAITING",
    episodeId: "eps_compose01" as EpisodeId,
    reason: `waiting reason ${String(i).padStart(4, "0")}`,
    requiredEvidence: [],
    occurredAt: "2026-08-25T00:00:02.000Z" as IsoTimestamp
  };
}

function timeReduce(events: readonly EpisodeEvent[], iters: number): number[] {
  const results: number[] = [];
  let sink = 0;
  for (let run = 0; run < 3; run++) {
    for (let i = 0; i < Math.max(2, Math.floor(iters / 10)); i++) sink += reduceEpisodeEvents(events).events.length;
    const t0 = hr();
    for (let i = 0; i < iters; i++) sink += reduceEpisodeEvents(events).events.length;
    const t1 = hr();
    results.push(usOf(t0, t1) / iters);
  }
  if (sink === 0) throw new Error("sink must be positive");
  return results;
}

function cellReduceComposition(): void {
  console.log("[C4 reduceEpisodeEvents event-type composition (fixed E=1001) + attach-dose ladder + production scale]");
  const compositions: readonly [string, EpisodeEvent[]][] = [
    ["ATTACH-heavy (1 open + 1000 attach)", [openedEvent(), ...Array.from({ length: 1000 }, (_, i) => attachEvent(i))]],
    ["WAITING-heavy (1 open + 1000 waiting)", [openedEvent(), ...Array.from({ length: 1000 }, (_, i) => waitingEvent(i))]],
    [
      "MIXED (1 open + 500 attach/500 waiting interleaved)",
      [
        openedEvent(),
        ...Array.from({ length: 1000 }, (_, i) => (i % 2 === 0 ? attachEvent(i / 2) : waitingEvent((i - 1) / 2)))
      ]
    ]
  ];
  for (const [label, events] of compositions) {
    const state = reduceEpisodeEvents(events);
    // W-C4: composition-specific final-state witnesses; no rejection taken.
    if (state.failClosed) throw new Error(`W-C4 violated: ${label} must not fail closed`);
    if (label.startsWith("ATTACH") && state.episode?.runIds.length !== 1000) throw new Error("W-C4 attach count");
    if (label.startsWith("WAITING") && state.episode?.status !== "WAITING_FOR_USER") throw new Error("W-C4 waiting status");
    if (label.startsWith("MIXED") && (state.episode?.runIds.length !== 500 || state.episode?.status !== "WAITING_FOR_USER")) {
      throw new Error("W-C4 mixed state");
    }
    const results = timeReduce(events, 50);
    console.log(`  ${label}: ${results.map(f).join(" / ")} us/reduce (E=1001)`);
  }
  // attach-dose ladder: per-event cost must expose the O(R^2) spread+includes structure.
  for (const R of [250, 500, 1000, 2000]) {
    const events = [openedEvent(), ...Array.from({ length: R }, (_, i) => attachEvent(i))];
    const results = timeReduce(events, Math.max(10, Math.floor(20000 / R)));
    const perEvent = results.map((x) => (x / (R + 1)) * 1000);
    console.log(
      `  attach ladder R=${String(R).padStart(4)}: ${results.map(f).join(" / ")} us/reduce = ${perEvent.map((x) => x.toFixed(0)).join(" / ")} ns/event`
    );
  }
  // production-scale composition (episodes attach 1-few runs).
  const realistic = [openedEvent(), ...Array.from({ length: 4 }, (_, i) => attachEvent(i)), ...Array.from({ length: 4 }, (_, i) => waitingEvent(i))];
  const results = timeReduce(realistic, 20000);
  console.log(`  production-scale (E=9, 4 attach + 4 waiting): ${results.map((x) => (x * 1000).toFixed(0)).join(" / ")} ns/reduce`);
}

async function main(): Promise<void> {
  console.log(`node ${process.version}`);
  await cellCascadeDose();
  cellLoopEvalDensity();
  await cellRedactionDose();
  cellReduceComposition();
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
```

两轮实测输出（Node 22.22.2）：

```text
--- run1 ---
[C1 cascade hit-ratio dose (N=200 fixed, byte-compensated; h = 0/20/100/200)]
  h=  0: 474.1 / 377.2 / 393.2 us/cascade  (W-C1a: no-write fixpoint held)
  h= 20: 964.8 / 837.0 / 697.1 us/cascade  (W-C1b: sidecar +20)
  h=100: 671.1 / 656.4 / 683.2 us/cascade  (W-C1b: sidecar +100)
  h=200: 759.8 / 687.8 / 759.3 us/cascade  (W-C1b: sidecar +200)
  marginal per-hit price (median h=200 minus median h=20, /180): -0.432 us/hit; zero-dose floor = two fail-closed reads (S5-J-3 order)
[C2 evaluatePreferenceLoop (J1 site) tombstone-density dose (N=6000 fixed array; d = 0/25/50/90/100%)]
  d=  0%: 1194.8 / 1144.4 / 1138.9 us/evaluate  [report={"fit":1,"correctionCost":0,"forgettingEvents":0,"reversalEvents":0}] [sink=0]
  d= 25%: 1278.9 / 1281.8 / 1278.0 us/evaluate  [report={"fit":1,"correctionCost":0,"forgettingEvents":21,"reversalEvents":0}] [sink=0]
  d= 50%: 1306.6 / 1300.7 / 1302.2 us/evaluate  [report={"fit":1,"correctionCost":0,"forgettingEvents":52,"reversalEvents":0}] [sink=0]
  d= 90%: 1572.5 / 1343.2 / 1340.7 us/evaluate  [report={"fit":1,"correctionCost":0,"forgettingEvents":784,"reversalEvents":0}] [sink=0]
  d=100%: 1353.8 / 1354.2 / 1355.6 us/evaluate  [report={"fit":1,"correctionCost":0,"forgettingEvents":1200,"reversalEvents":0}] [sink=0]
[C3 redaction dirty-body ratio dose (equal-length bodies; r = 0/50/100%)]
  CPU redactFeedback r=  0%: 1336 / 1274 / 1267 ns/record  [sink=360000]
  CPU redactFeedback r= 50%: 1435 / 1424 / 1405 ns/record  [sink=540000]
  CPU redactFeedback r=100%: 1700 / 1669 / 1666 ns/record  [sink=720000]
  e2e appendFeedback r=  0%: 93.0 / 79.9 / 75.8 us/append
  e2e appendFeedback r=100%: 99.6 / 86.2 / 91.0 us/append
[C4 reduceEpisodeEvents event-type composition (fixed E=1001) + attach-dose ladder + production scale]
  ATTACH-heavy (1 open + 1000 attach): 1545.2 / 1539.2 / 1540.5 us/reduce (E=1001)
  WAITING-heavy (1 open + 1000 waiting): 84.3 / 19.1 / 22.7 us/reduce (E=1001)
  MIXED (1 open + 500 attach/500 waiting interleaved): 413.6 / 413.7 / 417.3 us/reduce (E=1001)
  attach ladder R= 250: 107.4 / 107.8 / 108.3 us/reduce = 428 / 430 / 431 ns/event
  attach ladder R= 500: 403.2 / 399.5 / 400.1 us/reduce = 805 / 797 / 799 ns/event
  attach ladder R=1000: 1543.8 / 1547.6 / 1546.7 us/reduce = 1542 / 1546 / 1545 ns/event
  attach ladder R=2000: 6070.3 / 6027.6 / 6042.1 us/reduce = 3034 / 3012 / 3020 ns/event
  production-scale (E=9, 4 attach + 4 waiting): 603 / 595 / 569 ns/reduce
--- run2 ---
[C1] h=0: 521.0 / 461.4 / 406.7  h=20: 790.2 / 889.2 / 821.8
  h=100: 831.3 / 658.8 / 795.0  h=200: 864.9 / 759.2 / 728.9 us/cascade
  marginal: -0.348 us/hit（W-C1a/W-C1b 同样全过）
[C2] d=0: 1155.2 / 1102.5 / 1099.8  d=25: 1248.2 / 1239.6 / 1248.0
  d=50: 1270.4 / 1403.0 / 1269.1  d=90: 1635.0 / 1323.4 / 1310.5
  d=100: 1335.1 / 1338.2 / 1333.7 us/evaluate（report 与 run1 逐位相同）
[C3] CPU r=0: 1157 / 1135 / 1117  r=50: 1429 / 1497 / 1393  r=100: 1706 / 1661 / 1659 ns/record
  e2e r=0: 85.9 / 89.6 / 103.2  r=100: 86.8 / 74.1 / 68.9 us/append
[C4] ATTACH 1564.4 / 1546.3 / 1554.3  WAITING 85.7 / 19.3 / 26.1  MIXED 419.0 / 419.2 / 417.6 us/reduce
  ladder R=250: 425 / 428 / 425  R=500: 820 / 811 / 809  R=1000: 1556 / 1557 / 1557  R=2000: 3069 / 3042 / 3053 ns/event
  production-scale E=9: 607 / 580 / 562 ns/reduce
```

MORE_OPTIMA=no
BRANCH=cursor/r20-j-persist-twentieth-pass-83a1
