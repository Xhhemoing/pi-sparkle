MODEL_SLUG=claude-fable-5-thinking-xhigh

# R20-I：CLI / Pi adapter / config / telemetry 第二十遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `497c312`（派单 tip，含 R20-E/F/G 收口与 R20-H/I/J 派单；H/J 在飞，未触碰）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- <25 文件>` 为空（`--stat` 0 行）——**零漂移，自 `8dee7fb` 起连续第十三轮字节不变（按派单 R9-I 起算口径即第十二连轮，同一事实）**。`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet` 通过）；切片相关测试面零漂移（`git diff --quiet 8dee7fb..HEAD -- test/` 通过）。基线增量核查：`6e3e9c6`（R19-I 基线）→ `497c312` 共 8 个提交，全部 `docs(sota-opt)` 收口/派单（`git diff --stat -- . ':!docs'` 为 0 行）；**EXCLUSIONS.md 该区间逐字节未变**（`git diff --quiet` 通过），对 I 切片无新约束行。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致（合计 4708 行，与 R19-I 计数逐字相同）。
- 前置阅读按令执行：README → EXCLUSIONS 全表 → PROGRESS → round-20/PLAN（含 R20-A…R20-G 七切片空枚举收口摘要；H/J 在飞未等待）→ R19-I（十一连空 + 配置输入基数轴 E≈130-160 越线点）→ R18-I（children 基数轴 C≈2400）→ R17-I（拒绝路径六端点）→ R16-I / R15-I / R14-I / R13-I / R12-I / R11-I / R10-I / R9-I 逐份重读。邻区禁铸 ID 逐一定位核实：**FITQ**（R19-C 拟合粒度）、**NAMESHAPE**（R19-G 目录扇出几何）、**SCALEX**（R20-C 剂量响应）、**XPROC**（R20-G 跨进程干涉）——本轮新轴与四者机制、站点、切片均不同（见 §2.1），未换名重提。
- 分支：`cursor/r20-i-cli-twentieth-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（`/exec-daemon/node`，与 R9-I～R19-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。pnpm 10.17.1，`pnpm install --frozen-lockfile`（store 温装 1.8 s）+ `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**（package.json 钉死 + frozen-lockfile 内容寻址）；exports 图仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第十三轮未满足（本轮文件级直接确认；R13-I 已证文件级检查即判决性检查，未重复四 specifier 执行级探针）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S20-I-\* ID（诚实空枚举，与 R9-I～R19-I 同结局，十二连空），切片维持关闭（MORE_OPTIMA=no。）** 本轮是全新第二十组枚举而非历轮剩余项清理。R14-I 钉死配置空间维度、R15-I 钉死测量学维度、R16-I 钉死命令类覆盖与采样归因维度、R17-I 钉死错误端点定价维度、R18-I 钉死 children 负载输入规模维度、R19-I 钉死配置输入基数维度；本轮补一块与全部历轮正交的砖——**累积遥测状态维度（`runtime/invocations.jsonl` 记录基数 N）**：十九轮以来所有承重格夹具都是**全新拷贝的 state-root**，`invocations.jsonl` 恒不存在（ENOENT → 空数组）——而生产 CLI 每完成一次外部模型调用就向该文件追加一行（main.ts L620 writer），且**每个 configured 态建 router 的命令类每次 spawn 都整文件重读+逐行校验**（`loadInvocationsFromStateRoot`：JSON.parse × N + `isInvocation` × N，后者即本切片 `telemetry/model-invocation.ts` 的 `invocationError`）。「随使用量累积的运行时状态」这一输入维度从未被任何轮次定价；R19-I 放大的是配置输入文件（providers.json）、R18-I 放大的是工作负载输入文件（children.json），本轴放大的是**使用历史本身**。该轴为测量收口、未产出候选，但产出一个重要的诚实更新：

1. **零漂移五重确认（连续第十三轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变；pi-ai 0.84.1 exports 图 7 项未变；`dist/providers/` 仍恰 39 表，本 VM 直接计数）、测试面（零变动）、基线增量（`6e3e9c6..497c312` 共 8 提交全 docs，EXCLUSIONS 字节不变）、调用图前提（S5-I-1 目录快照复用注释与站点 main.ts L726-729/L751、track/loop.ts L81/L155 逐字在位——本轮为新轴做了直接重读确认）。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字，派单明令项）**：R9-I 5 格承重集 × 2 Node 版本全部 exit 0，结构逐格同构：地板 55.0/51.5 ms，**custom−builtin 差 children +46.5 / +24.0、track +48.8 / +24.1 ms（22.14/22.22）**——与 R19-I 带（+47.6/+26.4、+48.4/+23.9）、R18-I 带（+49.5/+26.4、+55.2/+25.0）及更早十轮同号、同量级、同 Node 版本梯度（**十三轮同构**）；带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内。builtin 态目录构建比地板贵 +51.3/+52.1（children，双引擎），与 R19-I（+54.6/+54.6）紧邻。归属未变。
3. **新轴（§1.3）：累积 invocations.jsonl 基数标定（I 切片首次；使用历史状态维度）**。端到端新增 6 格（双引擎全 exit 0）：相对 lineage custom 格，N=1000 条 **+4.8/+2.7**、N=5000 条 **+17.8/+20.6**、N=20000 条 **+54.7/+58.2** ms（22.14/22.22）——线性、无悬崖；builtin 态同价（N=5000 时 +19.2/+24.4，证明成本在装载+校验而非目录匹配）；track 命令类同站点同价（+14.3/+17.1）。**冷进程单次装载直接归因**（附录 A.3）：N=1k/5k/20k 冷装载中位 4.98/18.66/57.19（22.14）与 5.75/19.07/55.34 ms（22.22）——端到端边际 ≈ 恰好一次冷装载，**无隐藏二次读**（S5-I-1 快照复用在 children/track 两承重类的代码级复核 + 数字级吻合双证）。进程内分解（附录 A.2）：warm 每记录 JSON.parse ~0.81 µs + `isInvocation` ~0.52-0.61 µs，全装载 warm 1.9-3.3 µs/条、冷进程 2.8-5.8 µs/条；`calibrateCatalogConfig` 的 M×N 匹配环在 N=50000 也仅 ≤1.74 ms（pinned 全匹配形态同价）。**10 ms 越线点 N\* ≈ 2300-2700 条（日志 ~0.65-0.77 MB），四种测法（双引擎 × 端到端/冷装载）互相印证**。
4. **诚实更新派单前提**：「本切片唯一数十 ms 结构是 custom−builtin 回退」在累积态下不再是唯一——N ≳ 2300 的 state-root 每次 configured spawn 多付 >10 ms、N=20000 付 ~55-58 ms，且该尺度**非幻想规模**：writer 无轮转、`deleteRunRecords` 只删 `runtime/runs/<runId>/`、`deleteEpisodeRecords` 只删 episode 文件——**没有任何现存工具会删或截断 `runtime/invocations.jsonl`**（record-classes 声明 retention run-scoped/delete-files，但两把删除刀都碰不到该路径），长寿命 state-root 靠使用即可自然到达。**但它不产生候选**：每条记录对应一次已完成的真实外部模型调用（秒级延迟 + 真实计费），N\*=2400 意味着该 state-root 已吸收 ≥ 数十分钟的付费模型时间，10 ms/spawn 相对量低 4-5 个数量级（R8-I 健全性反例的同族相对成本裁决）；且全部消除形态逐一映射到既有围栏（§2.3）：尾窗/轮转/压实=数据面语义与 retention 契约变更（派单硬禁），跨进程解析缓存=R10-I 磁盘快照族+S8-I-1 围栏+CAS，跳过/弱化校验=record-classes「malformed invocation records fail closed at validateInvocation」恢复契约本体，进程内去重=S5-I-1 已落地（本轮双证无残留二次读），`invocationError` 微观=µs 族（合法记录必走全部检查、重排无效），M×N 单遍化=N=50k 也 ≤1.74 ms 噪声族。
5. **附带功能性观测（散文记录，非候选、不铸 ID）**：现行 writer（`pi-executor.buildInvocation`）写 `config.model = identity.modelId`（裸 id）且 `config.modelVersion = options.modelVersion`（全仓 src/ 无任何调用方设置该选项——grep 直接确认），而 `calibrateCatalogRates` 按目录行匹配要求 `inv.config.model === model.id`（目录 id 是 `provider/model` 全引用）**且** `inv.config.modelVersion === model.version`（强制非空 string）——两个条件对 writer 产出的记录**结构性双落空**，平滑分支在现行写读配对下恒为惰性（`aggregateCatalogObserved` 亦按注释设计跳过 unpinned）。这是路由/遥测契约面的 id 空间对齐问题，不是性能问题（pinned 全匹配形态实测同价），处置权在 D/H 区契约面；与 R19-G **NAMESHAPE**（runs 目录扇出几何）机制、站点均不同，不换名重提、不铸 ID，仅记录供后续轮次/契约所有者引用。
6. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**；r7i part D 载荷探针复证 builtin 构建后 providers/all 保持冷 62.0 ms、custom 回退命中缓存 1.4 ms）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R19-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动，无 r20i 新仿真（派单：仅落地代码才须新增）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读读码，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交任何 scripts/（全部 harness 源码进附录 A，遵守 R7-F/R8-C/R9-I～R19-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README 未触碰；未触碰在飞 R20-H / R20-J 分支与文件。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S19-\* 条目；R9-I～R19-I 空枚举维持，未事后发明 S9-I-\* ～ S19-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：`--children` 复用 smartChildPlan 已校准目录（main.ts L726-729/L751 原样——本轮新轴归因把该站点数字级双证一遍）、Pi runtime 点用处 `await import`（S4-I 败者为 **S4-I-2..5**，无 S4-I-1）、12 条分支独占分派模块惰性导入（赢家 ID **S5-I-1**）、per-provider 惰性模型表（S7-I-1）——零漂移即字节级证明，三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅做文件级重开条件确认（`all.models.js` 不存在 + exports 图 7 项 + 39 表计数，§2.2），未满足，维持淘汰；未构造替代注册表、未触碰回退路径。
  - **未重铸 R19-I 配置输入基数轴**（E×P×K、E≈130-160 越线点）——本轮 custom 夹具维持 lineage P=1/K=2/E=2，未放大任何配置维；**未重铸 R18-I children 基数轴**（C≈2400）——children 夹具维持 C=2；**未重铸 R17-I 拒绝路径**——未测任何 exit-1 端点。新轴放大的是第三种输入（使用历史累积状态），与三者的输入文件/维度两两正交。
  - **未换名重提邻区 ID**：FITQ（R19-C，offline 拟合粒度分布）/ NAMESHAPE（R19-G，`runtime/runs/` 目录扇出几何——注意其对象是**目录条目数**，本轴对象是**单文件行数**，读取站点也不同：EventStore 目录行走 vs `loadInvocationsFromStateRoot` 单文件整读）/ SCALEX（R20-C，C 区池量剂量响应）/ XPROC（R20-G，跨进程 I/O 干涉定价）——四者的测量对象、切片归属、消除机制与本轴均无交集；XPROC 的并发干涉角在本轴下的对应问题（并发 spawn 同时读同一大 jsonl）被 R20-G「run 面存储无锁、几何免疫」收口覆盖，未重测。
  - **未重开 S8-I-2 / S8-I-3 / S7-I-2..4 / S6-I-1..3 / S5-I-2..5 / S4-I-2..5 / S3-I-1..6 / S2-I-1..6 / S1-I-1..8**；R10-I～R19-I 全部无 ID 收口维持（跨进程磁盘快照、失手记忆化、自制 union、root/compat 替代源、`all.models` 探针、肥 builtin 态、A/A 地板/引擎代/堆足迹、flowchart 格+采样归因、六端点、C 轴、E×P×K 轴），本轮既未重测也未补铸；其结构前提（39 表、exports 图、pi-ai 版本、调用图）本轮复核未变。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价；双 LCB / 双归因在位；提升 proposal-first（Checkpoint F-PROD 开放）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`providers/all` 失手回退 + throw-outside-catch 在位；未改任何测试。**新轴全程零代码改动、零行为差异**——所有测量都是对现行装载路径的直接观测 + 夹具状态注入（向夹具 state-root 预写合法/生产同形 jsonl），未触碰 writer、reader、校验器任何一行。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；未触碰在飞 R20-H / R20-J 分支与文件。

## 1. 本 VM 复测：5 格承重抽测 + 新轴 6 格（双 Node，未拷贝历轮数字）

R9-I～R19-I 抽测集原样复刻并**新增 6 个累积遥测状态格**（configured 态夹具、每迭代全新拷贝 state-root、2 预热 + 10 测量、显式二进制全路径，遵守 R7-I 配置态 × 命令类纪律；harness 全文附录 A.1）。

### 1.1 抽测中位数（ms，spawn-to-exit，全部 exit 0）

| 格 | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|
| `--version`（地板） | 55.0 (min 53.3, p90 58.3) | 51.5 (48.7, 54.0) |
| run-children@builtin（C=2） | 106.3 (103.0, 108.8) | 103.6 (101.2, 104.3) |
| run-track@builtin | 124.7 (122.3, 128.3) | 120.2 (118.7, 123.9) |
| run-children@custom（P=1/K=2/E=2） | **152.8** (144.5, 159.4) | **127.6** (123.8, 130.3) |
| run-track@custom | **173.5** (162.0, 178.7) | **144.3** (141.0, 147.0) |
| run-children@custom-inv1k（N=1000，本轮新格） | 157.6 (150.5, 166.1) | 130.3 (129.0, 132.8) |
| run-children@custom-inv5k（N=5000，本轮新格） | **170.6** (168.5, 179.3) | **148.2** (145.3, 150.8) |
| run-children@custom-inv20k（N=20000，本轮新格） | **207.5** (202.9, 219.9) | **185.8** (178.7, 200.6) |
| run-children@custom-inv5k-pinned（N=5000 全匹配形态） | 170.9 (163.7, 179.8) | 151.5 (145.9, 156.4) |
| run-children@builtin-inv5k（N=5000，builtin 目录） | 125.5 (124.1, 127.2) | 128.0 (121.3, 134.6) |
| run-track@custom-inv5k（N=5000，track 命令类） | 187.8 (176.8, 193.5) | 161.4 (159.4, 164.3) |

- **custom−builtin 差**：children **+46.5 / +24.0**、track **+48.8 / +24.1** ms（22.14/22.22）。R19-I 带 +47.6/+26.4 与 +48.4/+23.9，R18-I 带 +49.5/+26.4 与 +55.2/+25.0，R17-I 带 +47.3/+24.7 与 +46.5/+21.8，更早十轮见 R19-I §1.1 汇编——**十三轮同号、同量级、同 Node 版本梯度**。带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内（22.14 单窗 maxAbs 34.5 ms、22.22 8.7-10.4 ms），无新结构。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
- **builtin 态目录构建**比地板贵 +51.3/+52.1（children，双引擎）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。本 VM 地板（55.0/51.5）与 R19-I VM（53.9/51.2）紧邻，格间结构不变。

### 1.2 新轴的生产流证明（先证流量，再证数字）

- **writer**：`main.ts` L620——`run` 家族每次外部模型调用完成后 `appendFile(runtime/invocations.jsonl, JSON行)`（经 `pi-executor.ts` L193 `recordInvocation` 校验后）。一行 ≈ 283 B（实测 1000 条 277 KB）。
- **readers（每 spawn 一次，整文件）**：`loadInvocationsFromStateRoot`（routing/cost-calibration.ts L88-108：readFile → split("\n") → 逐行 JSON.parse → 逐行 `isInvocation`，后者即本切片 `invocationError` 全量 4×isId + 2×HASH 正则 + ISO 正则 + Date.parse）。CLI 侧消费站点：run --children（main.ts L356 smartChildPlan）、run --track（track/loop.ts L81）、run --flowchart / resume / answer（main.ts L583/L1067/L1189 `createCalibratedCliModelRouter`）、pause / inject（pause.ts L65 / inject.ts L84 同函数）——**configured 态所有建 router 的命令类**。每 spawn 恰一次：children 路径 S5-I-1 快照复用（L726-729 注释 + L751 `createModelRouter(catalog)`）、track 路径同型（loop.ts L155）——代码级复核 + §1.3 冷装载数字级吻合双证，**无残留二次读**。
- **无删除/轮转**：`deleteRunRecords` 只删 `runtime/runs/<runId>/` 子树（privacy/deletion.ts L59-66）；`deleteEpisodeRecords` 只删 `runtime/episodes/<id>*.jsonl` 两形态（L81-89）；record-classes L126-136 为 model-invocation 声明 retention run-scoped / deletion delete-files，但**两把删除刀的路径都不含 `runtime/invocations.jsonl`**——该文件在现行工具面下只增不减，长寿命 state-root 的 N 随使用单调累积。十九轮以来全部夹具 N=0（全新 state-root，ENOENT → 空数组，成本恰为一次 stat 失败）——该维度是最后一个从未注入的输入平面。

### 1.3 轴 1：累积 invocations.jsonl 基数标定（本轮新证据，不铸 ID）

**(a) 端到端边际**（相对各自 N=0 基线格，ms，22.14/22.22）：

| N（条 / 文件大小） | children@custom | children@builtin | children@custom-pinned | track@custom |
|---|---|---|---|---|
| 1000 / 277 KB | **+4.8 / +2.7** | — | — | — |
| 5000 / 1383 KB | **+17.8 / +20.6** | +19.2 / +24.4 | +18.1 / +23.9 | +14.3 / +17.1 |
| 20000 / 5532 KB | **+54.7 / +58.2** | — | — | — |

线性、无悬崖；**builtin 目录同价**（记录一条也匹配不上目录行，仍付同额边际）→ 成本在装载+校验，不在匹配；**pinned 全匹配形态同价**（差 +0.3/+3.3，A/A 包络内）→ M×N 平滑分支非成本站点；**track 命令类同站点同价** → 命令类无关，站点唯一。N=1000 边际在 R15-I A/A 包络内、N=5000 起越出 22.22 包络（8.7-10.4 ms）——但归因不依赖包络判读，由 (b) 直接测定。

**(b) 冷进程单次装载直接归因**（附录 A.3：全新进程只执行一次 `loadInvocationsFromStateRoot`，10 进程取中位；ms）：

| N | 22.14.0 | 22.22.2 |
|---|---|---|
| 1000 | 4.98 (min 4.56) | 5.75 (5.37) |
| 5000 | 18.66 (17.77) | 19.07 (18.46) |
| 20000 | 57.19 (53.19) | 55.34 (54.00) |

端到端边际 ≈ **恰好一次冷装载**（+54.7 vs 57.19、+58.2 vs 55.34；5k/1k 各格同带）——无隐藏二次读，S5-I-1 复用完好。冷进程每记录 2.8-5.8 µs（短环 JIT 未升阶 + 一次性分配），warm 收敛到 1.9-2.1 µs。

**(c) 进程内站点分解**（附录 A.2，warm 制度 best-of-median，双引擎几乎逐位重合；22.22 行示）：

| N | full 装载 | readFile | split | JSON.parse×N | isInvocation×N | cal-unpinned | cal-pinned |
|---|---|---|---|---|---|---|---|
| 100 | 0.33 | 0.07 | 0.00 | 0.09 | 0.06 | 0.01 | 0.03 |
| 1000 | 2.15 | 0.19 | 0.02 | 0.82 | 0.54 | 0.01 | 0.02 |
| 5000 | 10.43 | 1.24 | 0.09 | 4.07 | 2.71 | 0.05 | 0.10 |
| 20000 | 37.61 | 4.54 | 0.47 | 16.27 | 10.86 | 0.66 | 0.64 |
| 50000 | 124.70 | 11.97 | 1.15 | 40.38 | 26.96 | 1.70 | 1.11 |

（22.14 同表：full 0.32/2.21/10.04/36.94/103.81，parse 0.81-0.86 µs/条、validate 0.52-0.61 µs/条逐位同带。）每记录 warm：**JSON.parse ~0.81 µs（~43%）、`isInvocation` ~0.54 µs（~29%）**，余量为 readFile/split/trim/数组构造。`calibrateCatalogConfig`（真实 custom 态目录 M=4 行：`customco/custom-big`、`customco/custom-small`、`cheap`、`premium` 别名）在 **N=50000 也仅 ≤1.74 ms，pinned 全匹配（每条走满平滑算术）同价** ——M×N 环即死于 µs 族。

**(d) 越线点与保真门**：10 ms 越线 **N\* ≈ 2300-2700 条（~0.65-0.77 MB）**——冷装载内插 2276-2468、端到端内插 2600-2631，双引擎 × 两测法互证。该尺度**可由累积到达**（见 §1.2 无删除证明；对比 R18-I C≈2400 是「单个 children.json 写 2400 个子任务」的幻想规模，本轴 N=2400 只需该 state-root 历史上完成过 2400 次模型调用）。**但相对成本裁决同 R8-I 健全性反例族**：每条记录 = 一次已完成的秒级付费模型调用，N\*=2400 背后是 ≥ 数十分钟的累计模型墙钟时间，10 ms/spawn 低 4-5 个数量级；N=20000（~55-58 ms/spawn，与 custom−builtin 结构同量级）背后是数小时/数百美元级的使用史。成本存在、可测、线性、会继续长——但它是 retention 政策问题（数据面契约，派单硬禁改动），不是切片代码低效（每记录 µs 级已贴底），见 §2.3 消除形态逐一围栏映射。

## 2. 第二十组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 输入规模定价），每形态先对 EXCLUSIONS 全表 + 十九轮 I 报告 + 邻区四禁铸 ID 做同机制先例检索。

### 2.1 新轴候选性自查——测量收口，不铸 ID

- **轴 1（累积遥测基数标定）**是既有装载路径在使用历史维度上的直接观测：零行为差异、零代码改动。其产出是「线性 + N\*≈2300-2700 + 每记录 µs 级贴底 + M×N 环 µs 族 + 无二次读 + 无删除工具」的裁决数字集，不是优化。正交性：与 R19-I（配置输入文件 providers.json，解析+权威查询税）、R18-I（工作负载文件 children.json，assignTasks 超线性）、R14-I（builtin 配置空间，模块装载交叉点）的输入平面两两不同；与邻区 NAMESHAPE（目录条目扇出，EventStore 行走）对象与站点不同；与 SCALEX（C 区池量响应）、FITQ（拟合粒度）、XPROC（跨进程干涉）切片与机制不同。pinned/unpinned 双形态、builtin/custom 双目录、children/track 双命令类的六格设计即双归因纪律本体。
- **附带功能性观测不铸 ID**（结论第 5 条）：writer/reader 的 id 空间双落空（裸 modelId vs 目录全引用；modelVersion 恒 undefined vs 强制非空 version）使平滑分支结构性惰性——**性能上已被 pinned 同价格证明无关**（正因如此它留在散文而不进候选），功能归属在契约面。

### 2.2 S8-I-1 重开条件文件级再确认——不铸 ID

- `dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 **0.84.1**（package.json + frozen-lockfile 内容寻址）。R13-I 已证文件级检查对四 specifier 结局有判决性（文件不存在 ⇒ ERR_MODULE_NOT_FOUND 族必然），本轮依派单「仅在必要时重测」纪律不重复执行级探针。
- **裁决**：重开条件连续第十三轮未满足；所需的是 pi-ai 上游导出，不是仓内改动。此项为审计证据，非机制候选——**不铸 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级即死）

| 扫描角度 | 处置 |
|---|---|
| **invocations.jsonl 尾窗读取 / 记录数上限 / 轮转 / 压实 / 按 run 分片**（本轴消除形态 #1） | 数据面语义变更本体：校准与 catalog-observed 的样本集改变 = 行为差异；retention 是 record-classes 契约（privacy 面所有权），派单硬禁数据面/阈值改动。非候选 |
| **跨进程解析结果缓存（mtime/size 键控）**（消除形态 #2） | R10-I 磁盘快照族无 ID 收口逐字重现（lazy-vs-sync 分歧 + 数据面 + CAS 三重违反）+ S8-I-1 围栏；非候选 |
| **进程内装载去重**（消除形态 #3） | S5-I-1 已落地物本体；本轮代码级（L726-729/L751、loop.ts L81/L155）+ 数字级（端到端边际 ≈ 恰一次冷装载）双证无残留二次读。无事可做 |
| **跳过/弱化 `isInvocation` 装载校验**（消除形态 #4） | record-classes L135「malformed invocation records fail closed at validateInvocation」恢复契约本体；且校验仅 0.52-0.61 µs/条（29%），跳过也够不到装载线。非候选 |
| **`invocationError` 微观（检查重排 / 正则合并 / Date.parse 短路）**（消除形态 #5） | µs 族即死：合法记录必走全部检查（重排只影响非法记录的首错报文 = 行为差异）；ISO 正则已在 Date.parse 前短路；S2-I-3 邻域。非候选 |
| **`calibrateCatalogRates` M×N → 单遍分组**（消除形态 #6） | 实测 N=50000 × M=4 仅 ≤1.74 ms（pinned 全匹配同价）；µs-低 ms 族。非候选 |
| **「无可匹配记录时跳过装载」**（消除形态 #7） | 先验不可判定（匹配性取决于记录内容；外部嵌入方可设 options.modelVersion 产出 pinned 记录）——跳过 = 语义分歧。非候选 |
| **流式/分块解析替代整读** | 总 CPU 不变（readFile 仅 ~12%），µs 族重排。非候选 |
| custom id 跳回退 / 自制 union / 磁盘快照 / 失手 memo | S8-I-1 硬禁 + R10-I/R11-I 三重围栏；重开条件本轮文件级仍未满足（§2.2）；派单明令不得铸 rename ID |
| 配置输入基数族（fat providers.json / S1-I-8 Map / 失手 memo E 维 / enabled 并行） | R19-I 轴收口逐字引用（E≈130-160、E×K≳2×10⁵ 越线点，fat-E ≥99.5% 语义强制税）；本轮未重测未补铸 |
| children 基数族（assignments.find 换 Map） | S1-I-3 + R18-I C≈2400 收口；本轮 C=2 未触 C 维 |
| 拒绝路径 / 错误端点 | R17-I 六端点收口；本轮未测 exit-1 端点 |
| flowchart 命令类 / 采样归因 / 模块图普查 | R16-I 收口，前提未变（39 表 / exports / 版本本轮复核） |
| 肥 builtin 态预取交叉点 / A/A 地板 / 引擎代 / 堆足迹 | R14-I / R15-I 收口；本轮带比对直接引用其 MDE 包络 |
| flowchart 双 build / providers.json 双读 / 死载荷下沉 / pause-inject 免 router / compile-cache / dist 布局 / 一次性命令类 / 凭据面 / 解析微观 / barrel 隐藏边 / root-compat 替代源 / per-provider 探针短路 / 预热重叠 | S1-I-1/2、S2-I-1..6、S3-I-1..6、S4-I-2..5、S5-I-2..5、S6-I-1..3、S7-I-2..4、S8-I-2/3、S1-I-4..8、R11-I/R12-I/R13-I 无 ID 收口逐字重现——零漂移下前提逐项成立，本轮 25 文件重读未发现任何新站点脱离既有覆盖物 |
| telemetry 其余面（INVOCATION_CALL_OUTCOMES includes→Set / compareRunToRun） | 表长 4 / 诊断聚合一次性，S1-D-8 族噪声——本轮新轴恰好把 telemetry 文件在生产流上的**真实热站点**（`invocationError` × N 装载税）第一次定价：0.54 µs/条，µs 族贴底 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖、pi-ai exports、测试面、基线增量五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可落地候选；本轮把派单前提「唯一数十 ms 结构 = custom−builtin 回退」诚实更新为「累积态下存在第二个数十 ms 线性结构（N≳2300 的 invocations.jsonl 装载税）」，但其全部消除形态收敛到既有围栏（数据面 retention 契约 / R10-I 缓存族 / S5-I-1 已落地 / fail-closed 校验契约 / µs 族），每记录成本已贴 Node 原生解析地板（0.81 µs JSON.parse 不可再降、0.54 µs 契约校验不可省），**结构上无仓内合法改动空间**——它的解药是 retention 政策（契约面决策），不是代码。

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx；无代码落地故按派单为自愿加测）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part B：1220 builtin 行 × 39 provider lazy===sync；part D 载荷探针：builtin 构建后 providers/all 保持冷 62.0 ms、custom 回退命中缓存 1.4 ms；part E：configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单项，Node 22.22.2）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**（与 R9-I～R19-I 计数逐字相同）、`pnpm build` 绿。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **构建**：`pnpm install --frozen-lockfile`（1.8 s 温装）+ `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动；无赢家故无新仿真脚本入库；三个 harness（spot/inproc/coldload）均为 /tmp 临时件，全文进附录 A，未进 lint 面。
- **引擎注记**：22.14.0 的 doctor engines 预检 fail-closed 为设计行为（R9-I §3 记录在案）；nvm 22.22.2 为 gate/仿真引擎。依 R15-I 轴 1 纪律，候选验收须在 ≥22.19 引擎上做——本轮无候选，抽测数字用于十三轮带比对（双引擎同号），新轴归因不依赖包络判读（冷装载直测）。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；新轴以测量收口关闭使用历史状态维度缺口；S8-I-1 文件级第十三轮 blocked；附带功能性观测移交契约面、不铸 ID）。
- 剩余赢家：无（custom−builtin 结构维持 S8-I-1 健全性反例封死；新发现的第二个数十 ms 结构——累积 invocations.jsonl 装载税——每记录成本贴 Node 原生地板、每 spawn 恰一次已由 S5-I-1 保证、全部消除形态映射既有围栏，解药是 retention 契约决策非仓内代码）。
- 矩阵空洞：无（R8-I 78 格全覆盖；承重格十三轮同构复现 + 本轮补入累积状态 6 格——builtin/custom × pinned/unpinned × children/track 的归因完备组合；R7-I 配置态 × 命令类纪律全程遵守；配置基数维持 R19-I 收口、children 基数维持 R18-I 收口、错误端点维持 R17-I 收口、命令类与归因维持 R16-I 收口、测量学维持 R15-I 收口、肥 builtin 态维持 R14-I 收口）。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 11 格抽测 harness（/tmp/r20i-bench/spot.mjs，`<node二进制全路径> spot.mjs <同一路径>` 分别以两版本执行；承重 5 格与 R19-I A.1 逐字同构 + 新增 6 个累积遥测状态格）

```javascript
// R20-I spot re-measure: floor + builtin/custom children+track bearing cells
// (R9-I..R19-I harness lineage) + NEW axis: accumulated telemetry state —
// runtime/invocations.jsonl cardinality N. Every prior round's fixtures used
// FRESH state roots (invocations.jsonl absent -> ENOENT -> []); the
// accumulated-invocation-log dimension was never end-to-end priced in I.
// Production flow: main.ts:620 appends one JSON line per model call;
// smartChildPlan (run --children), track/loop.ts (run --track), and
// createCalibratedCliModelRouter (run --flowchart/resume/answer/pause/inject)
// all re-read + re-validate the WHOLE log at startup via
// loadInvocationsFromStateRoot (JSON.parse x N + isInvocation x N).
// deleteRunRecords/deleteEpisodeRecords never remove the file -> N accumulates.
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r20i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.argv[2];
const WARMUP = 2, ITERS = 10;

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
// NEW axis: invocation record generators.
// "unpinned" mirrors pi-executor.buildInvocation exactly: bare modelId,
// modelVersion absent (options.modelVersion is never set by any caller), so
// calibrateCatalogRates / aggregateCatalogObserved skip every record after
// load+validate. This is the production-real shape today.
const HEX = "0123456789abcdef";
const hex8 = (i) => Array.from({ length: 8 }, (_, k) => HEX[(i * 7 + k * 13 + 5) % 16]).join("");
const BASE_MS = Date.UTC(2026, 7, 20);
function unpinnedRecord(i) {
  return {
    id: `inv_r20i${String(i).padStart(6, "0")}`,
    taskId: `tsk_t${i % 7}`, runId: `run_r${i % 13}`, agentInstanceId: `agt_a${i % 3}`,
    config: { provider: "customco", model: i % 2 === 0 ? "custom-big" : "custom-small", parameterHash: hex8(i) },
    responseHash: hex8(i + 1),
    tokensIn: 1200 + (i % 900), tokensOut: 350 + (i % 400),
    latencyMs: 1800 + (i % 2500),
    occurredAt: new Date(BASE_MS + i * 1000).toISOString()
  };
}
// "pinned" is the hypothetical future-writer shape that actually matches
// catalog rows (config.model = catalogId, modelVersion = listed.version =
// custom model id) -> exercises the full M x N smoothing branch. Upper bound.
function pinnedRecord(i) {
  const big = i % 2 === 0;
  return {
    ...unpinnedRecord(i),
    config: {
      provider: "customco",
      model: big ? "customco/custom-big" : "customco/custom-small",
      modelVersion: big ? "custom-big" : "custom-small",
      parameterHash: hex8(i)
    }
  };
}
function jsonl(n, gen) {
  const lines = [];
  for (let i = 0; i < n; i++) lines.push(JSON.stringify(gen(i)));
  return lines.join("\n") + "\n";
}
const seeded = [
  ["custom-inv1k", "custom", jsonl(1000, unpinnedRecord)],
  ["custom-inv5k", "custom", jsonl(5000, unpinnedRecord)],
  ["custom-inv20k", "custom", jsonl(20000, unpinnedRecord)],
  ["custom-inv5k-pinned", "custom", jsonl(5000, pinnedRecord)],
  ["builtin-inv5k", "builtin", jsonl(5000, unpinnedRecord)]
];
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
for (const [name, cfg] of Object.entries(states)) {
  const dir = join(FIX, `state-${name}`);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
}
for (const [name, base, log] of seeded) {
  const dir = join(FIX, `state-${name}`);
  cpSync(join(FIX, `state-${base}`), dir, { recursive: true });
  writeFileSync(join(dir, "runtime", "invocations.jsonl"), log);
}
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
const CHILDREN = join(FIX, "children.json");
writeFileSync(CHILDREN, JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");

const childrenArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN];
const trackArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"];
const cells = [
  // Bearing set (R9-I..R19-I lineage).
  { id: "version", args: () => ["--version"], state: undefined },
  { id: "run-children@builtin", state: "builtin", args: childrenArgs },
  { id: "run-track@builtin", state: "builtin", args: trackArgs },
  { id: "run-children@custom", state: "custom", args: childrenArgs },
  { id: "run-track@custom", state: "custom", args: trackArgs },
  // NEW axis: accumulated invocations.jsonl cardinality.
  { id: "run-children@custom-inv1k", state: "custom-inv1k", args: childrenArgs },
  { id: "run-children@custom-inv5k", state: "custom-inv5k", args: childrenArgs },
  { id: "run-children@custom-inv20k", state: "custom-inv20k", args: childrenArgs },
  { id: "run-children@custom-inv5k-pinned", state: "custom-inv5k-pinned", args: childrenArgs },
  { id: "run-children@builtin-inv5k", state: "builtin-inv5k", args: childrenArgs },
  { id: "run-track@custom-inv5k", state: "custom-inv5k", args: trackArgs }
];
function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, `state-${state}`), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 180000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-FAIL ${cell.id}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

原始输出（全部格 exit 0）：

```
== v22.14.0 ==
version: median=55.0ms min=53.3 p90=58.3 exit=0
run-children@builtin: median=106.3ms min=103.0 p90=108.8 exit=0
run-track@builtin: median=124.7ms min=122.3 p90=128.3 exit=0
run-children@custom: median=152.8ms min=144.5 p90=159.4 exit=0
run-track@custom: median=173.5ms min=162.0 p90=178.7 exit=0
run-children@custom-inv1k: median=157.6ms min=150.5 p90=166.1 exit=0
run-children@custom-inv5k: median=170.6ms min=168.5 p90=179.3 exit=0
run-children@custom-inv20k: median=207.5ms min=202.9 p90=219.9 exit=0
run-children@custom-inv5k-pinned: median=170.9ms min=163.7 p90=179.8 exit=0
run-children@builtin-inv5k: median=125.5ms min=124.1 p90=127.2 exit=0
run-track@custom-inv5k: median=187.8ms min=176.8 p90=193.5 exit=0
== v22.22.2 ==
version: median=51.5ms min=48.7 p90=54.0 exit=0
run-children@builtin: median=103.6ms min=101.2 p90=104.3 exit=0
run-track@builtin: median=120.2ms min=118.7 p90=123.9 exit=0
run-children@custom: median=127.6ms min=123.8 p90=130.3 exit=0
run-track@custom: median=144.3ms min=141.0 p90=147.0 exit=0
run-children@custom-inv1k: median=130.3ms min=129.0 p90=132.8 exit=0
run-children@custom-inv5k: median=148.2ms min=145.3 p90=150.8 exit=0
run-children@custom-inv20k: median=185.8ms min=178.7 p90=200.6 exit=0
run-children@custom-inv5k-pinned: median=151.5ms min=145.9 p90=156.4 exit=0
run-children@builtin-inv5k: median=128.0ms min=121.3 p90=134.6 exit=0
run-track@custom-inv5k: median=161.4ms min=159.4 p90=164.3 exit=0
```

### A.2 进程内站点分解（/tmp/r20i-bench/inproc.mjs，双引擎各执行一次）

```javascript
// R20-I in-process anchor: decompose loadInvocationsFromStateRoot cost at
// invocation-log cardinality N. Stages: readFile / split / JSON.parse x N /
// isInvocation x N (I-owned telemetry/model-invocation.ts invocationError),
// plus calibrateCatalogConfig over the REAL custom-state catalog
// (buildLiveCatalogConfig on a customco fixture), unpinned (production shape,
// zero smoothing matches) vs pinned (hypothetical full-match upper bound).
import { mkdirSync, rmSync, writeFileSync } from "node:fs";
import { readFile } from "node:fs/promises";
import { join } from "node:path";
import { performance } from "node:perf_hooks";
import { loadInvocationsFromStateRoot, calibrateCatalogConfig } from "/workspace/dist/routing/cost-calibration.js";
import { isInvocation } from "/workspace/dist/telemetry/model-invocation.js";
import { buildLiveCatalogConfig } from "/workspace/dist/cli/model-catalog.js";

const ROOT = "/tmp/r20i-bench/inproc-fix";
const HEX = "0123456789abcdef";
const hex8 = (i) => Array.from({ length: 8 }, (_, k) => HEX[(i * 7 + k * 13 + 5) % 16]).join("");
const BASE_MS = Date.UTC(2026, 7, 20);
function unpinnedRecord(i) { /* 与 A.1 逐字相同 */ }
function pinnedRecord(i) { /* 与 A.1 逐字相同 */ }
const customProvider = { /* 与 A.1 逐字相同 */ };
rmSync(ROOT, { recursive: true, force: true });
mkdirSync(join(ROOT, "runtime"), { recursive: true });
writeFileSync(join(ROOT, "runtime", "providers.json"), JSON.stringify({
  version: 1, enabled: ["customco/custom-big", "customco/custom-small"],
  primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider]
}, null, 2) + "\n");
const catalog = await buildLiveCatalogConfig(ROOT); // real custom-state catalog (M rows incl. default aliases)
console.log(`catalog rows M=${catalog.models.length}: ${catalog.models.map((m) => m.id).join(", ")}`);

function median(xs) { const s = [...xs].sort((a, b) => a - b); return s[Math.floor(s.length / 2)]; }
async function timeAsync(reps, fn) {
  const ts = [];
  for (let r = 0; r < reps + 2; r++) {
    const t0 = performance.now();
    await fn();
    if (r >= 2) ts.push(performance.now() - t0);
  }
  return median(ts);
}
function timeSync(reps, fn) {
  const ts = [];
  for (let r = 0; r < reps + 2; r++) {
    const t0 = performance.now();
    fn();
    if (r >= 2) ts.push(performance.now() - t0);
  }
  return median(ts);
}

const Ns = [100, 1000, 5000, 20000, 50000];
for (const N of Ns) {
  const reps = N <= 1000 ? 50 : N <= 5000 ? 20 : N <= 20000 ? 10 : 5;
  const lines = [];
  for (let i = 0; i < N; i++) lines.push(JSON.stringify(unpinnedRecord(i)));
  const raw = lines.join("\n") + "\n";
  const logPath = join(ROOT, "runtime", "invocations.jsonl");
  writeFileSync(logPath, raw);
  const bytes = Buffer.byteLength(raw);

  const tFull = await timeAsync(reps, () => loadInvocationsFromStateRoot(ROOT));
  const tRead = await timeAsync(reps, () => readFile(logPath, "utf8"));
  const tSplit = timeSync(reps, () => raw.split("\n"));
  const parts = raw.split("\n").filter((l) => l.trim() !== "");
  const tParse = timeSync(reps, () => { for (const l of parts) JSON.parse(l); });
  const parsed = parts.map((l) => JSON.parse(l));
  const tValidate = timeSync(reps, () => { let ok = 0; for (const p of parsed) if (isInvocation(p)) ok++; return ok; });
  const invocations = await loadInvocationsFromStateRoot(ROOT);
  const tCalUnpinned = timeSync(reps, () => calibrateCatalogConfig(catalog, invocations));
  const pinnedParsed = [];
  for (let i = 0; i < N; i++) pinnedParsed.push(pinnedRecord(i));
  const tCalPinned = timeSync(reps, () => calibrateCatalogConfig(catalog, pinnedParsed));

  const perRecUs = (ms) => ((ms * 1000) / N).toFixed(2);
  console.log(
    `N=${N} (${(bytes / 1024).toFixed(0)}KB): full=${tFull.toFixed(2)}ms ` +
    `[read=${tRead.toFixed(2)} split=${tSplit.toFixed(2)} parse=${tParse.toFixed(2)} validate=${tValidate.toFixed(2)}] ` +
    `cal-unpinned=${tCalUnpinned.toFixed(2)}ms cal-pinned=${tCalPinned.toFixed(2)}ms ` +
    `| per-record: parse=${perRecUs(tParse)}us validate=${perRecUs(tValidate)}us full=${perRecUs(tFull)}us`
  );
}
```

原始输出：

```
== v22.22.2 ==
catalog rows M=4: customco/custom-big, customco/custom-small, cheap, premium
N=100 (28KB): full=0.33ms [read=0.07 split=0.00 parse=0.09 validate=0.06] cal-unpinned=0.01ms cal-pinned=0.03ms | per-record: parse=0.86us validate=0.59us full=3.31us
N=1000 (277KB): full=2.15ms [read=0.19 split=0.02 parse=0.82 validate=0.54] cal-unpinned=0.01ms cal-pinned=0.02ms | per-record: parse=0.82us validate=0.54us full=2.15us
N=5000 (1383KB): full=10.43ms [read=1.24 split=0.09 parse=4.07 validate=2.71] cal-unpinned=0.05ms cal-pinned=0.10ms | per-record: parse=0.81us validate=0.54us full=2.09us
N=20000 (5532KB): full=37.61ms [read=4.54 split=0.47 parse=16.27 validate=10.86] cal-unpinned=0.66ms cal-pinned=0.64ms | per-record: parse=0.81us validate=0.54us full=1.88us
N=50000 (13830KB): full=124.70ms [read=11.97 split=1.15 parse=40.38 validate=26.96] cal-unpinned=1.70ms cal-pinned=1.11ms | per-record: parse=0.81us validate=0.54us full=2.49us
== v22.14.0 ==
catalog rows M=4: customco/custom-big, customco/custom-small, cheap, premium
N=100 (28KB): full=0.32ms [read=0.06 split=0.00 parse=0.09 validate=0.06] cal-unpinned=0.01ms cal-pinned=0.03ms | per-record: parse=0.86us validate=0.61us full=3.18us
N=1000 (277KB): full=2.21ms [read=0.32 split=0.02 parse=0.81 validate=0.53] cal-unpinned=0.01ms cal-pinned=0.02ms | per-record: parse=0.81us validate=0.53us full=2.21us
N=5000 (1383KB): full=10.04ms [read=1.12 split=0.09 parse=4.05 validate=2.62] cal-unpinned=0.06ms cal-pinned=0.09ms | per-record: parse=0.81us validate=0.52us full=2.01us
N=20000 (5532KB): full=36.94ms [read=3.90 split=0.47 parse=16.36 validate=10.51] cal-unpinned=0.71ms cal-pinned=0.46ms | per-record: parse=0.82us validate=0.53us full=1.85us
N=50000 (13830KB): full=103.81ms [read=11.53 split=1.13 parse=40.52 validate=26.10] cal-unpinned=1.74ms cal-pinned=1.12ms | per-record: parse=0.81us validate=0.52us full=2.08us
```

### A.3 冷进程单次装载归因（/tmp/r20i-bench/coldload.mjs + coldload-driver.mjs）

```javascript
// coldload.mjs — R20-I cold-load attribution: one loadInvocationsFromStateRoot
// call in a FRESH process (no JIT warmup), timed alone. Parent spawns this
// K times and medians. Distinguishes "cold per-record overhead" from "hidden
// second read" when comparing against the end-to-end spot deltas.
import { performance } from "node:perf_hooks";
import { loadInvocationsFromStateRoot } from "/workspace/dist/routing/cost-calibration.js";

const root = process.argv[2];
const t0 = performance.now();
const invocations = await loadInvocationsFromStateRoot(root);
const dt = performance.now() - t0;
console.log(`${dt.toFixed(2)} ${invocations.length}`);
```

```javascript
// coldload-driver.mjs — fresh-spawn coldload.mjs 10x per N per node binary.
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const ROOT = "/tmp/r20i-bench/coldload-fix";
// unpinnedRecord/hex8/BASE_MS 与 A.1 逐字相同
const nodeBin = process.argv[2];
for (const N of [1000, 5000, 20000]) {
  const dir = join(ROOT, `n${N}`);
  rmSync(dir, { recursive: true, force: true });
  mkdirSync(join(dir, "runtime"), { recursive: true });
  const lines = [];
  for (let i = 0; i < N; i++) lines.push(JSON.stringify(unpinnedRecord(i)));
  writeFileSync(join(dir, "runtime", "invocations.jsonl"), lines.join("\n") + "\n");
  const times = [];
  let count;
  for (let r = 0; r < 10; r++) {
    const res = spawnSync(nodeBin, ["/tmp/r20i-bench/coldload.mjs", dir], { encoding: "utf8" });
    const [ms, n] = res.stdout.trim().split(" ");
    times.push(Number(ms));
    count = n;
  }
  times.sort((a, b) => a - b);
  console.log(`N=${N}: cold-load median=${times[Math.floor(times.length / 2)].toFixed(2)}ms min=${times[0].toFixed(2)} loaded=${count}`);
}
```

原始输出：

```
== v22.22.2 ==
N=1000: cold-load median=5.75ms min=5.37 loaded=1000
N=5000: cold-load median=19.07ms min=18.46 loaded=5000
N=20000: cold-load median=55.34ms min=54.00 loaded=20000
== v22.14.0 ==
N=1000: cold-load median=4.98ms min=4.56 loaded=1000
N=5000: cold-load median=18.66ms min=17.77 loaded=5000
N=20000: cold-load median=57.19ms min=53.19 loaded=20000
```

MORE_OPTIMA=no
