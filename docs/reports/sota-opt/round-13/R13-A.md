MODEL_SLUG=claude-fable-5-thinking-xhigh

# R13-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第十三遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 13 / R13-A（Round 1–12 同区第十三遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `f669e99`（含 R12-G 收口与 Round 13 开轮）
**分支:** `cursor/r13-a-tracking-thirteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S13-A-\* ID，切片零代码改动；
整片预算收口第十一次复核成立，并新增「turn 内部剖面」把 R12-A 合同地板
分解中最后一个聚合块（完整 runTrackingTurn，~13%/gate）下探到逐子组件级。**
切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R12-A 至本轮基线
（`f669e99`）**逐字节未变**（`git diff 7acb666..f669e99 -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，十三遍全程零 diff）。
调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来 `git diff
89f0455..f669e99 -- src/` **完全为空**（跨 R10/R11/R12/R13 开轮四段全部为
docs 提交），生产调用方经全库 grep 复核仍为 `supervisor.ts:483`
（applyTrackingGate/nextTrackingSeq；两个 settle 调用点 601/711 均不传
trackingAssessment ⇒ 生产 O(1) no-op，R9-A 事实第五次复验）/
`coordinator.ts:444` / `flowchart-run.ts:320`（applyChildThreeLine），每
子结果一次（~5 次/run），事件表几十级（41）。R12-A 的 ~70–92 µs/run 预算
天花板经本轮在当前 VM（Node v22.22.2）实测复核为 **~70–81 µs/run**
（2×2 锚点矩阵三次独立运行，四格 14.1–16.1 µs/gate，与 R12-A 同带且带宽
更窄；量级结论不变——即使切片零成本，节省上界 ~0.08 ms/run，仍比落地线
低约三个量级）。

按令对 R12-A「逐组件全部落在已关 ID 或契约裁决上」的声明做了**结构位置 ×
消除机制满矩阵**第十三遍扫描：**未找到 R12-A 漏掉的站点**。本轮枚举出的
全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点（8 项新记录，见
§0；R12-A 的 13 项换名检查全部维持换名拒绝，未追认任何 S12-A-\* ID），
按令一律不铸新 ID。作为空枚举的实证支撑，本轮新落档 **turn 内部剖面**
（§1，三次独立运行稳定）：生产锚点格里 1.78–1.95 µs/gate 的完整
runTrackingTurn 中，约 40–44% 是含 spread 克隆的第二次 `computePrescore`
（S1-A-2）、约 18–19% 是死滚轮尾 `rollSummary`（S9-A-1，含 S4-A-1 的
window spread 与 `[...codes]` 拷贝）、约 7–8% 是常量 human/score 管道
（S7-A-1/S7-A-4/S6-A-1）、evaluateGates none 路径仅 27–30 ns
（S1-A-6/S7-A-3），mergeOpenMinors/collectEvidence 各 ~42–45 ns——
**turn 内部无一子组件未点名**，且 FAILED 侧 packet 构造增量三次运行符号
翻转（+125/−55/+277 ns）＝噪声级。R12-A 分解的五大组件占比
（hashAssessment×3 ~41–42% / validateEvent ~20–21% / turn ~13% /
prescore×2 ~10–11% / O(E) 扫描合计 ~1.6%）在本 VM 逐一复现。第十三遍
收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：roller / analysis / gates / config /
  combined-score / from-child / index / turn / isolation / human-score /
  prescore / types）+ `src/run/child-tracking.ts` + `src/run/gate-apply.ts`，
  **清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2），全部实际
  重读**。未触碰其余 `src/run/`（G 切片）、`src/supervisor/` 及任何其他
  切片。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表，含 S12-B-1..2 /
  S12-C-1 / S12-D-1..2；R12-A / R12-E / R12-F / R12-G / R12-H 空枚举未铸
  ID，确认）/ PROGRESS.md / round-13/PLAN.md / round-12/PLAN.md +
  R12-A.md / round-11/R11-A.md / round-10/R10-A.md … round-01/R1-A.md。
  R12-I / R12-J 在其他分支在跑，未等待、未触碰其报告。
- 环境：本 VM 默认 Node v22.14.0，按 engines（>=22.19.0）切至 v22.22.2
  后 `pnpm install --frozen-lockfile`，全部测量在 v22.22.2 完成。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点，**一律不铸 S13-A-\* ID**。R12-A 的 13 项换名检查【常量哈希排序
  置换、死 `cappedByHardFail`、P 代数约分、DimensionScore 形状单态化、
  dimension() 条件 spread、trim 探针、asString 首字符卫、nextTrackingSeq
  强度削减、currentGateStatus 频次重排、惰性 import、turn 恒定输入常量
  折叠伞、写侧 validateEvent 跳过、三 hash 去重】全部复核维持，不再逐条
  重列；以下 8 项为本轮**新记录**的换名检查）：
  1. `combineScore` **幂等二次舍入跳过**（unobserved/无明显问题路径
     `roundScore(input.P)` 对已是 `Number(x.toFixed(4))` 产物的 P 是恒等
     ——本轮 F1 在 4850 个生产形态 P 值上逐位证实 `turn.score === P`）
     = S6-A-1 反例域「任何改写/快路径」blanket（R11-A §0 已点名该
     blanket），且量级内含于 combine 全链 123–151 ns（上界 ~0.7 µs/run），
     拒列；
  2. `hashAssessment` **dimensions 排序比较器 localeCompare 换码点比较**
     （本轮 F2 事实：已验证域——6 个 PrescoreDimensionId——36 个有序对
     两比较器全体同号 ⇒ 域内哈希字节不变；域外非 ASCII 反例
     `"\u00e4-dim"` vs `"z-dim"` 两比较器发散 ⇒ 对 schema 非法但可强转的
     输入哈希**字节**改变，而哈希字节是持久化 idempotencyKey/CAS 的基底）
     = S3-B-3 机制换位点 + S9-D-4/S12-D-1 编码角落 fail-open 族；收益
     上界是 R12-A F1 天花板（272–283 ns/hash 含全部 map+sort 机器）的
     真子集，比落地线低约四个量级，拒列；
  3. gates.ts **门控决策对象按 (kind,codes,askUser) 享元/单例化**（none
     路径每 turn 分配新对象）= S6-B-2/S7-B-5 可观察身份族换位点；且返回
     携带 `openMinors: input.openMinors` 随输入变化，享元自败，拒列；
  4. `extractHumanScore` **空输入早退卫**（无 list 无 userText 直接返回
     unobserved）= S7-A-1 整管道常量折叠的真子集；实测整管道 12–14 ns
     已近调用下界，拒列；
  5. gate-apply **幂等双 find 次序交换**（TRACKING_ASSESSMENT find 提到
     GATE_TRANSITION find 之前）= **不等价**：门已应用的重放上两者同时
     命中（GATE_TRANSITION 与其成对 TRACKING_ASSESSMENT 总是共存），
     交换后返回形状丢失 transitionId——S1-A-9 乱序发散结构同型；死扫描
     本体已被 S8-A-1 裁决，拒列；
  6. `[...input.events]`+push **换预分配定长数组/索引写** = S2-A-4 追加
     构造微观族（spread→concat 已测慢 3.7×）+ R1-A 不可变拷贝地板本体
     （本轮实测 27–28 ns 即地板），拒列；
  7. `computePrescore` 六个维度谓词函数的 **input 属性读 CSE/局部提升**
     = S5-J-5 CSE 族（~10 ns/查）+ S1-A-4 循环融合已测更慢的同域，拒列；
  8. `coverageOutcome`/`constraintOutcome` 的 **every×includes Set 化**
     （O(R·C)→O(R+C)）= S1-A-4「Set 化实测更慢」本体 + 现实 R,C ≤ 3
     （X4-7 个位数域），拒列。
- 未重开任何 X\* / S1-\* … S12-\* 条目。S11-A-1（apply 守卫常量折叠，
  非 S10-A-1）、S11-A-2（none 路径死扫描，落地需公开 GateApplyResult
  收窄）、S10-A-1 / S9-A-1 / S7-A-1（平行 turn 路径/公开旗标结构）、
  S8-A-1（fresh-seq 幂等扫描）、X0-4/X2-4（签名/单遍合并）、X0-6
  （human-score 正则缓存）、S6-A-1（toFixed 舍入）、S7-A-4（combineScore
  代数）专项复核：本轮零触碰、零重提，全部维持关闭。R12-A 空枚举未铸
  S12-A-\*，本轮未追认、未补铸。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无命令
  权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足。不声称 Outcome-supported；Checkpoint
  F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，仿真只留在本报告附录，
  不入库（方法第 5 条）。

## 1. 预算支配论证复核 + turn 内部剖面（方法第 1、2 步）

R3-A §3 … R12-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..f669e99 -- <切片>` 为空，逐字节
   一致（十三遍全程零 diff）——R1-A…R12-A 全部测量与排除按令转移，但
   数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨四段）**：`git diff 89f0455..f669e99 -- src/`
   **完全为空**（38 个变更文件全部在 docs/ 下）。生产调用方、supervisor
   settle no-op（601/711 不传 assessment，settleSupervisedOutcome 首行
   `if (assessment === undefined) return`）、hashSummary 生产零流量
   （roller.ts:68 唯一调用点且 from-child 恒无 previous）、
   `parseTrackingAssessment` 唯一 `src/` 调用方为 events.ts:738（写侧
   屏障）、`GateApplyResult.runStatus/applied/transitionId` 零 `src/`
   读者（S11-A-2 字段级事实）、`proposeFromAnomaly`/`sanitizePacketForAnalysis`/
   isolation 面零 `src/` 生产调用方（S8-A-2 事实）、`src/track/` 对切片
   零导入七项事实经本轮全库 grep 全部复验。

**2×2 锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run）：**

```text
                          run1      run2      run3
PASSED x no-contract      15.9      15.5      16.0  µs/gate (+1 event)
PASSED x contract(3)      14.1      14.4      14.5  µs/gate (+1 event)
FAILED x no-contract      15.8      16.0      16.1  µs/gate (+3 events)
FAILED x contract(3)      14.3      14.4      14.3  µs/gate (+3 events)
=> ~70–81 µs/run
```

与 R12-A（14.1–18.5 µs/gate ⇒ 70–92 µs/run）同带且带宽更窄（本轮首测格
噪声较小）；R11-A 以来的「no-contract 格略贵于 contract 格」PIC/基准次序
现象复现，单格横向比较不具意义；四格三次运行**无悬崖、无空洞**。即使把
整个切片优化到零成本，节省上界 ~0.08 ms/run，仍比战役落地线（数十~数百
ms 或复杂度类下降）低**约三个量级**。**支配论证第十一次复核成立。**

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次运行
区间；与 R12-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R12-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1954–1978 ns 每次 ⇒ **5.86–5.93 µs** | 5.85–6.03 µs | ~41–42% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 306–326 ns + 第三次 hash） | **2.92–2.95 µs** | 2.90–2.99 µs | ~20–21% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（本轮 §「turn 内部剖面」下探） | **1.78–1.95 µs** | 1.73–2.07 µs | ~13% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 692–802 ns 每次 ⇒ **1.39–1.60 µs** | 1.39–1.70 µs | ~10–11% | S1-A-2 |
| O(E) 扫描合计（episodeIdFromEvents 65 + nextTrackingSeq 53–54 + 双幂等 find 40–41×2 + `[...events]` 拷贝 27–28 ns） | **226–228 ns** | ~220–224 ns | **~1.6%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |
| `observationFromChild` 等杂项 | 56–59 ns | 56–82 ns | <1% | S1-B-5 分配噪声族 |

\* 以 14.1–14.5 µs 锚点格计；`applyTrackingGate` 整体隔离实测
6.23–6.27 µs，与 R12-A（6.23 µs）逐位同带。

**本轮新落档：turn 内部剖面**（R12-A 表中最后一个聚合块的逐子组件隔离
测量，from-child 生产形态，三次运行区间）：

| turn 子组件 | 实测/turn | 占 turn* | 锁定裁决 |
| --- | --- | --- | --- |
| 第二次 `computePrescore`（含 `{...prescoreInput, lightMinorCount}` spread 克隆） | **713–846 ns** | ~40–44% | S1-A-2（重复 prescore 本体）+ S3-A-2（克隆站点） |
| `rollSummary`（死滚轮尾；wrapped 含 window spread + `[...codes]`） | core 364–368 / wrapped **327–348 ns** | ~18–19% | S9-A-1（死输出尾）+ S4-A-1（死 openMinors 覆盖/克隆）+ S2-A-1/S7-A-2（内部微观） |
| `hasObviousHumanProblem` + `combineScore`（含幂等二次舍入） | **123–151 ns** | ~7–8% | S7-A-1（常量管道）+ S7-A-4（代数）+ S6-A-1（舍入域，§0-1） |
| `mergeOpenMinors([], [])` | 42–43 ns | ~2% | S3-A-4 + §0-11 常量折叠伞（R12-A） |
| `collectEvidence`（1 个 toolSituation） | 42–45 ns | ~2% | S1-B-5 分配噪声族 |
| `evaluateGates`（none 路径） | 27–30 ns | ~2% | S1-A-6 / S7-A-3 / S10-A-1 |
| `extractHumanScore({})` | 12–14 ns | <1% | S7-A-1 + X0-6（正则面未触碰） |
| 残差（调用/解构/`readersInvoked`/输出组装 + 隔离误差） | ~430–490 ns | ~25% | 函数体骨架，无可点名结构 |

\* 隔离和 1.29–1.47 µs vs 整体 1.78–1.95 µs；隔离测量固有偏低（R12-A
同现象）。方法学记录：wrapped rollSummary 三次运行均略**低于** core
（−20~−40 ns），而 wrapped 严格多做工作 ⇒ 逐子组件隔离误差约为数十 ns
（PIC/基准位点效应），该粒度以下的任何「候选」先天不可测。FAILED 侧
turn（硬门 + wakeAnalysis packet 构造）与 PASSED 侧的差量三次运行
**符号翻转**（+125 / −55 / +277 ns）＝ packet 构造在噪声带内，S1-B-5
族裁决获得直接证据。

剖面的三个方法论结论：

1. **turn 内部无未点名结构**：八个子组件逐一落在已关 ID 上；理论可折叠
   体量恰好就是四轮前已裁死的两个 ID——S1-A-2（含克隆 ~0.7–0.85 µs）与
   S9-A-1（~0.33–0.35 µs）——其落地面（公开注入口风险 / 平行 turn 路径
   或公开旗标）在 R2-A/R9-A/R10-A/R11-A 已四度钉死。R12-A 表中的
   「~13% turn」聚合块至此完成组件级闭合。
2. **收口粒度谱系延续**：S10-A-1 门控结局（~20 ns）→ S11-A-1 守卫
   （~14 ns）→ R12-A 哈希置换天花板（~272 ns/hash 被契约整体锁死）→
   本轮 turn 子组件（12–151 ns，全部有名）——粒度每下探一层，新结构
   持续为零，且已低于本 VM 的逐组件隔离误差（数十 ns）。
3. **可支配残差账目对齐**：S1-A-2 + S9-A-1 合计 ~1.0–1.2 µs/gate 与
   R12-A「~18% 可折叠体量」记账一致（本轮 ~17–19%）；其余 ~81–83% 是
   哈希/schema/扫描/骨架地板，全部契约锁定。

**F1/F2 归档事实**（5000 组 seeded fuzz + 穷举域检查，三次运行逐位一致）：

- **F1**：4850 个 apply 路径上 (i) turn 内部 prescore 的 P 与外层
  `computePrescore` **逐位相等**（S1-A-2 纯重复证人，spread 克隆不改
  数值）；(ii) `turn.human` 恒 `{kind:"unobserved"}`（S7-A-1 常量管道
  证人）；(iii) `turn.score === prescore.P` **逐位相等** ⇒ combineScore
  对已舍入 P 的二次 `roundScore` 在全部生产形态上是恒等（§0-1 换名检查
  的记账证人；全域幂等性由 toFixed 语义给出，但按 S6-A-1 域纪律不作为
  落地依据）。
- **F2**：6 个维度 id 的全部 36 个有序对上
  `sign(localeCompare) === sign(码点比较)` ⇒ 比较器交换在已验证域内哈希
  字节不变；域外反例（`"\u00e4-dim"` vs `"z-dim"`：locale −1 vs 码点 +1）
  证明交换对可强转的非法输入改变哈希字节 ⇒ §0-2 拒列的编码角落证据。
  未来任何「哈希排序比较器替换/特化」提案可直接引用本事实 + S3-B-3/
  S9-D-4 族否决。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 8 项 + R12-A 13 项复核
维持）均为既有排除 ID 的换名/换位点，按「换名不铸 ID」纪律未列任何
S13-A-\* 候选。空枚举与 R12-A / R12-E / R12-F / R12-G / R12-H 同为战役
允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第十三遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `combined-score.ts` | 幂等二次舍入跳过（§0-1，F1 证人——本文件十三遍来最后一个未量化角度，已归档恒等事实）；isMachineScore 边界防御保留维持；S6-A-1 / S7-A-4 反例收口维持 | 无新候选 |
| `types.ts` | 哈希排序比较器换码点（§0-2，F2 证人）；R12-A F1 常量置换天花板维持；S1-A-8/X0-5 parse 面、S2-A-5/S5-A-2/S3-B-3 哈希面、asString 卫（R12-A §0-7）维持；hashSummary 生产零流量维持 | 无新候选 |
| `gates.ts` | 决策对象享元（§0-3，openMinors 载荷自败）；本轮剖面实测 none 路径 27–30 ns；S1-A-6 / S7-A-3 / S10-A-1 / S8-A-3 收口维持 | 无新候选 |
| `human-score.ts` | 空输入早退卫（§0-4，实测 12–14 ns 近下界）；X0-6 / S1-A-3 / R3-A 对偶面 / R5-A 频次重排 / S7-A-1 整管道死维持 | 无新候选 |
| `gate-apply.ts` | 幂等双 find 次序交换（§0-5，不等价反例：命中对共存时 transitionId 丢失）；追加预分配形式（§0-6）；写侧 validateEvent（R12-A §0-12，~21% 地板）与三 hash 去重（§0-13，~42% 地板）维持；S11-A-2 / S2-A-4 / X2-4/X0-4/S1-A-1/S1-A-9/S8-A-1（合计 ~1.6%）/ FAIL_CLOSED 兜底维持；executionAuthority 零调用方零计算维持 | 无新候选 |
| `prescore.ts` | 维度谓词属性读 CSE（§0-7）；every×includes Set 化（§0-8）；R12-A §0-2..5（cappedByHardFail/P 代数/形状单态化/条件 spread）维持；S1-A-4 / S3-A-3 / S6-A-1 / S8-A-3 收口维持 | 无新候选 |
| `turn.ts` | **内部剖面八子组件全点名**（§1 新表）；readersInvoked 双分配与 `previous?.openMinors ?? []` 空数组 = S1-B-5 族；恒定输入常量折叠伞（R12-A §0-11）维持；S1-A-7 / S2-A-3 / S3-A-2/4 / S4-A-1 / S5-A-1 / S7-A-1 / S9-A-1 / S10-A-1 收口维持 | 无新候选 |
| `roller.ts` | 剖面量化死尾 core 364–368 / wrapped 327–348 ns（含隔离误差方法学记录）；S9-A-1 / S1-A-5 / S2-A-1 / S3-A-1 / S7-A-2 维持 | 无新候选 |
| `from-child.ts` | FAIL 守卫（line 70）活性维持（R12-A fuzz 事实）；S1-A-2 / S2-A-2/6 / S3-A-3 / S4-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十一层收口维持 | 无新候选 |
| `child-tracking.ts` | skipped 死分配 = S7-A-3 族维持（R12-A 已录）；nextTrackingSeq 强度削减（R12-A §0-8）；批量门控 API = X0-4+X1-2 维持；S4-A-2 / S6-G-7 维持 | 无新候选 |
| `analysis.ts` | S8-A-2 维持（test-only + sanitize choke point，本轮 grep 复验零 src/ 生产调用方） | 无新候选 |
| `isolation.ts` / `config.ts` / `index.ts` | O(1) 谓词 / 常量 / 纯再导出；零 src/ 生产调用方复验；惰性 import = R12-A §0-10 维持 | 无新候选 |

**对 R12-A 声明的裁决**：「逐组件全部落在已关 ID 或契约裁决上，无一残余
未点名」在其组件粒度上成立；本轮把粒度再下探一层（turn 内部八子组件 +
combineScore 舍入恒等 + 哈希比较器域一致性），三处新事实**全部只产出
归档证人而非可落地候选**，且新粒度（12–151 ns/子组件）已低于本 VM 逐
组件隔离误差（wrapped/core 倒挂 −20~−40 ns 的直接测量）。收口的预言力
第十三遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家不
落仿真文件，遵守方法第 5 条；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R12-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm typecheck` / `pnpm lint` / `pnpm build` 全绿
（Node v22.22.2）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F1: 5000 fuzz trials, 4850 apply paths; turn-internal prescore bitwise-dup, human constant-unobserved, score re-round identity
F2: 36 ordered id pairs locale==codepoint on validated domain; off-domain counterexample diverges (locale=-1, codepoint=1)
anchor: 15.9 / 14.1 / 15.8 / 14.3 µs/gate => ~70–80 µs/run
decomposition: hash×3=5934ns | validateEvent=2946ns | turn=1856ns | prescore×2=1568ns | scans+copy=228ns | applyTrackingGate whole=6.25µs
turn interior: merge=43 | 2nd-prescore=806 | human=12 | combine=145 | gates=27 | collect=42 | roll(core/wrapped)=368/348 ns | sum=1423 vs whole=1856ns | FAILED delta=+125ns
ALL CHECKS PASSED (deterministic, seeded)

run 2:
anchor: 15.5 / 14.4 / 16.0 / 14.4 µs/gate => ~72–80 µs/run
decomposition: hash×3=5862ns | validateEvent=2920ns | turn=1949ns | prescore×2=1603ns | scans+copy=226ns
turn interior: merge=42 | 2nd-prescore=846 | human=13 | combine=151 | gates=28 | collect=45 | roll=367/340 ns | sum=1465 vs whole=1949ns | FAILED delta=-55ns (SIGN FLIP)
ALL CHECKS PASSED (deterministic, seeded)

run 3:
anchor: 16.0 / 14.5 / 16.1 / 14.3 µs/gate => ~71–81 µs/run
decomposition: hash×3=5911ns | validateEvent=2939ns | turn=1778ns | prescore×2=1385ns | scans+copy=228ns
turn interior: merge=43 | 2nd-prescore=713 | human=14 | combine=123 | gates=30 | collect=45 | roll=364/327 ns | sum=1294 vs whole=1778ns | FAILED delta=+277ns
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立运行 F1/F2 结论逐位一致（4850 apply、恒等/常量/同号事实确定性
重现）；剖面各子组件区间紧（2nd prescore 713–846、roll wrapped 327–348、
combine 123–151 ns）；FAILED packet 差量符号翻转＝噪声裁决本体；锚点四格
三次同带无悬崖。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S13-A-\*）。换名检查 8 项新记录见 §0（1–8），归档
于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件全部维持不变：整片预算支配论证的重开条件仍为 run 事件表或每
turn 集合规模增长 ≥2–3 个量级（届时 S1-A-1、S2-A-1、S2-A-3、S4-A-1、
S7-A-2、S8-A-1(B)、S9-A-1、S10-A-1、S11-A-1、S11-A-2 可凭既有等价性
证据优先重开——但 R12-A 分解与本轮剖面表明届时首先该重开的是哈希/验证
地板的量级复核：扫描面即使解冻也仅占 ~1.6%，turn 可折叠面 ~17–19% 且
落地结构被四度钉死）。F1 舍入恒等事实随 prescore 舍入规格（toFixed(4)）
或 combineScore 分支结构改变而自动失效；F2 域一致性事实随
PrescoreDimensionId 枚举扩张（尤其引入非 ASCII id）而自动失效。

## 附录：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.ts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xd13a01`–`0xd13a02`。

```ts
/**
 * R13-A deterministic anchor + decomposition simulation (thirteenth pass).
 * No fresh candidate cleared the rename check this round, so there is NO
 * candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R12-A lineage, gate-outcome x contract 2x2);
 *   (b) re-verifies the R12-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation);
 *   (c) NEW this round: decomposes the LAST aggregated block of that
 *       decomposition -- the full runTrackingTurn (~13%/gate) -- into its
 *       interior components, each measured in isolation over the exact
 *       production shapes, to show every sub-component maps onto an
 *       already-closed ID (S1-A-2 duplicate prescore incl. the spread
 *       clone, S9-A-1 dead roller tail, S7-A-1 constant human/score
 *       pipe, S1-A-6/S7-A-3 gates, X0-6-guarded human-score regexes).
 *
 * Archival-fact fuzz used by this round's adjudication:
 *   F1: from-child path identity witnesses. For every observation passing
 *       the entry guard: turn-internal prescore P/quality/coverage are
 *       BITWISE equal to the outer computePrescore (S1-A-2 is pure
 *       duplication, incl. the {...prescoreInput, lightMinorCount} spread),
 *       turn.human is always {kind:"unobserved"} (S7-A-1 constant pipe),
 *       and turn.score === prescore.P bitwise -- i.e. combineScore's
 *       re-rounding of the already-rounded P is an IDENTITY on this path
 *       (toFixed(4) idempotency; any skip/fast-path = S6-A-1 domain,
 *       rename-rejected).
 *   F2: hashAssessment's dimensions sort comparator (localeCompare) agrees
 *       in SIGN with UTF-16 codepoint comparison on the ENTIRE validated
 *       domain (6 PrescoreDimensionId values, exhaustive ordered pairs);
 *       an off-domain counterexample (non-ASCII strings) shows the two
 *       comparators diverge there => a comparator swap would change hash
 *       BYTES for schema-illegal-but-castable inputs (persisted
 *       idempotencyKey face) -- S3-B-3 mechanism transplant + S9-D-4
 *       encoding-corner family, rename-rejected, fact archived.
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xd13a01..0xd13a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn, mergeOpenMinors } from "/workspace/src/tracking/turn.js";
import { rollSummary } from "/workspace/src/tracking/roller.js";
import { evaluateGates } from "/workspace/src/tracking/gates.js";
import {
  extractHumanScore,
  hasObviousHumanProblem
} from "/workspace/src/tracking/human-score.js";
import { combineScore } from "/workspace/src/tracking/combined-score.js";
import {
  assessChildObservation,
  prescoreInputFromObservation,
  type ChildObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment,
  type TrackingWindow
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-24T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

/* ============================================================
 * Shared: realistic event tables (R3-A..R12-A harness lineage).
 * ============================================================ */
function buildEventTable(opts: { attached: boolean; filler: number }): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (opts.attached) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < opts.filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(kind: "PASSED" | "FAILED"): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  return {
    child: {
      taskId: "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary: "tests ran",
      evidenceIds: ["evd_1"],
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: ["evd_1"] } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "tests pass" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "keep scope" },
        { id: "con_1", description: "no new deps" },
        { id: "con_2", description: "tests stay green" }
      ]
    } as unknown as RequirementContract
  };
}

function genObservation(rng: () => number, trial: number): ChildObservation {
  const kind = pick(rng, ["PASSED", "FAILED"] as const);
  return {
    taskId: `tsk_${trial % 7}`,
    role: pick(rng, ["implementer", "tester", "scout", "worker"]),
    outcome: pick(rng, ["SUCCESS", "PARTIAL", "FAILURE", "CANCELLED", "TIMEOUT"] as const),
    summary: pick(rng, [
      "tests passed",
      "child completed the task",
      "",
      "wip",
      "verified output",
      "all checks succeed"
    ]),
    evidenceIds: rng() < 0.75 ? [`evd_${trial}`, "evd_shared"] : [],
    artifactIds: rng() < 0.5 ? [`art_${trial}`] : [],
    verification: { kind, evidenceIds: rng() < 0.8 ? [`evd_v_${trial}`] : [] },
    requiredChecks: rng() < 0.5 ? (rng() < 0.5 ? ["test"] : ["chk_0", "chk_1"]) : [],
    constraints: Array.from({ length: Math.floor(rng() * 5) }, (_, i) => ({
      id: `c_${i}`,
      text: `constraint ${i}`,
      kind: "constraint" as const,
      mandatory: true as const
    }))
  };
}

/* ============================================================
 * F1: from-child path identity witnesses over the REAL code.
 * ============================================================ */
{
  const rng = mulberry32(0xd13a01);
  let applies = 0;
  for (let trial = 0; trial < 5000; trial += 1) {
    const observation = genObservation(rng, trial);
    const outer = computePrescore(prescoreInputFromObservation(observation));
    const decision = assessChildObservation({ observation, episodeId: "ep_x", runId: "run_x" });
    if (!decision.apply) continue;
    applies += 1;
    // S1-A-2 pure-duplication witness: the turn-internal prescore
    // ({...prescoreInput, lightMinorCount: 0} spread) is bitwise-identical
    // to the outer computePrescore on P (and the assessment fields carry
    // the outer quality/coverage which must match the turn-visible P).
    check(
      "F1 (turn.P bitwise-equal to outer prescore.P)",
      Object.is(decision.turn.P, outer.P),
      `trial ${trial}: ${decision.turn.P} vs ${outer.P}`
    );
    check(
      "F1 (assessment prescore/quality/coverage = outer display/quality/coverage)",
      Object.is(decision.assessment.prescore, outer.displayPrescore) &&
        Object.is(decision.assessment.quality, outer.quality) &&
        Object.is(decision.assessment.coverage, outer.coverage),
      `trial ${trial}`
    );
    // S7-A-1 constant-pipe witness: human is always unobserved here.
    check(
      "F1 (turn.human always unobserved on from-child path)",
      decision.turn.human.kind === "unobserved",
      `trial ${trial}: ${JSON.stringify(decision.turn.human)}`
    );
    // toFixed(4) idempotency witness: combineScore's re-round of the
    // already-rounded P is an identity => turn.score === P bitwise.
    check(
      "F1 (turn.score === prescore.P bitwise; re-round is identity)",
      Object.is(decision.turn.score, outer.P),
      `trial ${trial}: ${decision.turn.score} vs ${outer.P}`
    );
  }
  console.log(
    `F1: 5000 fuzz trials, ${applies} apply paths; turn-internal prescore bitwise-dup, human constant-unobserved, score re-round identity`
  );
}

/* ============================================================
 * F2: hashAssessment dimensions-sort comparator domain agreement.
 * ============================================================ */
{
  const DIMENSION_IDS = [
    "evidence-consistency",
    "scope-safety",
    "check-coverage",
    "constraint-retention",
    "progress-vs-stall",
    "narrative-coherence"
  ] as const;
  const sign = (n: number): number => (n < 0 ? -1 : n > 0 ? 1 : 0);
  let pairs = 0;
  for (const a of DIMENSION_IDS) {
    for (const b of DIMENSION_IDS) {
      pairs += 1;
      const locale = sign(a.localeCompare(b));
      const codepoint = a < b ? -1 : a > b ? 1 : 0;
      check("F2 (localeCompare sign == codepoint sign on validated id domain)", locale === codepoint, `${a} vs ${b}`);
    }
  }
  // Off-domain counterexample: comparator swap changes order (and thus hash
  // bytes) for schema-illegal-but-castable non-ASCII strings.
  const offA = "\u00e4-dim"; // "ä-dim"
  const offB = "z-dim";
  const localeOff = sign(offA.localeCompare(offB));
  const codepointOff = offA < offB ? -1 : offA > offB ? 1 : 0;
  check("F2 (off-domain divergence exists: locale != codepoint)", localeOff !== codepointOff, `${localeOff} vs ${codepointOff}`);
  console.log(
    `F2: ${pairs} ordered id pairs locale==codepoint on validated domain; off-domain counterexample diverges (locale=${localeOff}, codepoint=${codepointOff})`
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all four matrix
 * shapes so the anchor cells measure steady-state JIT tiers.
 * ============================================================ */
const events41 = buildEventTable({ attached: true, filler: 39 });
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind);
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R12-A lineage): applyChildThreeLine
 * end-to-end at real scale (41-event table), 2x2 full matrix.
 * ============================================================ */
{
  const cells: Array<{ label: string; kind: "PASSED" | "FAILED"; withContract: boolean }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind);
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition of the PASSED x contract(3) cell
 * (R12-A methodology, re-anchored on this VM).
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tCopy = bench(() => void [...events41], 200000);
  const tObs = bench(() => void observationFromChild(mkP.child, mkP.spec, mkP.contract), 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate: caller+CAS+write-side): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side, in validateEvent): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole (parse + 3rd hash inside): ${ns(tVev)}`);
  console.log(`  episodeIdFromEvents scan (E=41): ${ns(tEpisode)}`);
  console.log(`  nextTrackingSeq scan (E=41): ${ns(tSeq)}`);
  console.log(`  idempotency find GATE_TRANSITION miss (E=41): ${ns(tFindGT)}`);
  console.log(`  idempotency find TRACKING_ASSESSMENT miss (E=41): ${ns(tFindTA)}`);
  console.log(`  [...events] immutable append copy (E=41): ${ns(tCopy)}`);
  console.log(`  observationFromChild: ${ns(tObs)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full (contains S9-A-1 dead tail): ${ns(tTurn)}`);
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tCopy;
  console.log(`  SUM O(E) scans + copy: ${ns(scans)}`);

  /* ============================================================
   * NEW (thirteenth pass): turn-interior decomposition. Splits the
   * runTrackingTurn block (~13%/gate in R12-A's table) into its interior
   * components, isolated over the exact from-child production shapes.
   * ============================================================ */
  const windowP: TrackingWindow = {
    contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
    toolSituations: prescoreInputP.toolSituations,
    constraints: observationP.constraints,
    unresolvedDecisions: [],
    confirmedDecisions: [],
    openMinors: []
  };
  const tMerge = bench(() => void mergeOpenMinors([], windowP.openMinors), 500000);
  const tSpreadPrescore = bench(
    () => void computePrescore({ ...prescoreInputP, lightMinorCount: 0 }),
    200000
  );
  const tHuman = bench(() => void extractHumanScore({}), 500000);
  const humanP = extractHumanScore({});
  const tCombine = bench(
    () =>
      void combineScore({
        P: decisionP.prescore.P,
        human: humanP,
        obviousProblem: hasObviousHumanProblem(humanP)
      }),
    500000
  );
  const openMinorsP = mergeOpenMinors([], windowP.openMinors);
  const tGates = bench(
    () =>
      void evaluateGates({
        P: decisionP.prescore.P,
        score: decisionP.turn.score,
        human: humanP,
        config: {
          version: 1,
          softThreshold: 0.55,
          hardFailCap: 0.3,
          unobservedHighCap: 0.54,
          minorPDip: 0.03
        },
        deterministicFail: false,
        ownershipEscape: false,
        claimedVerificationWithoutChecks: false,
        repeatedNoProgress: false,
        userRejectStop: false,
        safetyRejected: false,
        openMinors: openMinorsP
      }),
    500000
  );
  // collectEvidence replica (private in turn.ts) over the production window
  const tCollect = bench(() => {
    const refs = new Set<string>();
    for (const tool of windowP.toolSituations) {
      for (const id of tool.evidenceIds) refs.add(id);
      for (const id of tool.artifactIds) refs.add(id);
      for (const hash2 of tool.hashes) refs.add(hash2);
    }
    void [...refs];
  }, 500000);
  const evidenceP = (() => {
    const refs = new Set<string>();
    for (const tool of windowP.toolSituations) {
      for (const id of tool.evidenceIds) refs.add(id);
      for (const id of tool.artifactIds) refs.add(id);
      for (const hash2 of tool.hashes) refs.add(hash2);
    }
    return [...refs];
  })();
  const tRoll = bench(
    () =>
      void rollSummary({
        window: { ...windowP, openMinors: openMinorsP },
        prescore: decisionP.prescore.P,
        human: humanP,
        score: decisionP.turn.score,
        anomalyCodes: [],
        evidenceRefs: evidenceP,
        openMinors: openMinorsP
      }),
    200000
  );
  // roll wrapper cost incl. the {...window, openMinors} spread and [...codes]
  const tRollWrapped = bench(() => {
    const anomalyCodes = [...decisionP.turn.gate.codes];
    void rollSummary({
      window: { ...windowP, openMinors: openMinorsP },
      prescore: decisionP.prescore.P,
      human: humanP,
      score: decisionP.turn.score,
      anomalyCodes,
      evidenceRefs: evidenceP,
      openMinors: openMinorsP
    });
  }, 200000);
  // FAILED-side turn (hard gate + wakeAnalysis packet build)
  const mkF = mkChild("FAILED");
  const observationF = observationFromChild(mkF.child, mkF.spec, mkF.contract);
  const prescoreInputF = prescoreInputFromObservation(observationF);
  const tTurnFailed = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationF.role}`, `task ${observationF.taskId}`],
          toolSituations: prescoreInputF.toolSituations,
          constraints: observationF.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputF,
        humanInput: {},
        gateFacts: { deterministicFail: true }
      }),
    100000
  );

  console.log("--- NEW: turn-interior decomposition (from-child production shapes) ---");
  console.log(`  mergeOpenMinors([], []): ${ns(tMerge)}`);
  console.log(`  computePrescore({...spread, lightMinorCount:0}) [2nd prescore, S1-A-2]: ${ns(tSpreadPrescore)}`);
  console.log(`  extractHumanScore({}) [S7-A-1 pipe]: ${ns(tHuman)}`);
  console.log(`  hasObviousHumanProblem + combineScore [S7-A-1/S7-A-4/S6-A-1]: ${ns(tCombine)}`);
  console.log(`  evaluateGates (none path) [S1-A-6/S7-A-3]: ${ns(tGates)}`);
  console.log(`  collectEvidence (1 toolSituation): ${ns(tCollect)}`);
  console.log(`  rollSummary core [S9-A-1 dead tail body]: ${ns(tRoll)}`);
  console.log(`  rollSummary wrapped (+window spread + [...codes]) [S9-A-1 + S4-A-1]: ${ns(tRollWrapped)}`);
  const interiorSum = tMerge + tSpreadPrescore + tHuman + tCombine + tGates + tCollect + tRollWrapped;
  console.log(`  SUM interior components: ${ns(interiorSum)} (vs whole runTrackingTurn ${ns(tTurn)})`);
  console.log(`  runTrackingTurn FAILED side (hard gate + packet build): ${ns(tTurnFailed)} (delta vs PASSED = ${ns(tTurnFailed - tTurn)})`);
}

void mulberry32(0xd13a02);

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
