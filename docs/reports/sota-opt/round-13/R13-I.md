MODEL_SLUG=claude-fable-5-thinking-xhigh

# R13-I：CLI / Pi adapter / config / telemetry 第十三遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `8f706ca`（派单 tip，含 R13-A/C/D/E/F 空枚举收口与 R13-B 的 S13-B-1 淘汰；G/H 在飞，未触碰）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，`git diff 8dee7fb..HEAD -- src/cli src/pi-adapter src/config src/telemetry` 为空——**零漂移，连续第六轮字节不变**。全仓 `src/` 自 `8dee7fb` 后仅 `src/experiments/{canary,plan,shadow}.ts` 与 `src/routing/offline-logit.ts` 变动（S7-C / S7-F 已落地项），全部切片外；`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet` 通过）；切片相关测试面零漂移（`git diff --stat 8dee7fb..HEAD -- test/` 为空）。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致，`grep -c '\.ts$'` 合计 25。R8-I 的「~28 / cli 16」维持 R9-I～R12-I 判定：头部笔误计数而非文件漂移，git 为权威，全表见 §0。
- 前置阅读按令执行：README → EXCLUSIONS 全表（含 S13-B-1 新行）→ round-13/PLAN → round-12/PLAN + R12-I → R11-I → R10-I → R9-I（三连空枚举）→ R8-I（78 格关闭矩阵）→ R7-I / R5-I / R4-I（落地赢家）按需 → 25 个切片源文件全量实际重读（含凭据面两文件的只读读码，未测量未立项）。
- 分支：`cursor/r13-i-cli-thirteenth-pass-83a1`（已推送，未开 PR）。
- 环境：**双 Node 版本实测**——VM 默认 22.14.0（唯一入口 `/exec-daemon/node` shim，与 R9-I～R12-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规）。pnpm 10.17.1，`pnpm install --frozen-lockfile`（本 VM node_modules 初始缺失，store 温装 2.1s）+ `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**，与 R7-I～R12-I 同版本；exports 图逐项复核仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`），**无与模型表版本锁定的轻量 provider-id 子路径——S8-I-1 的唯一重开条件连续第六轮未满足**（本轮以 `./providers/all.models` 文件级直接探针补齐该审计的最后一个旁路形态，见 §2.1）。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S13-I-\* ID（诚实空枚举，与 R9-I / R10-I / R11-I / R12-I 同结局，五连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第十三组枚举而非历轮剩余项清理。R12-I 已把 pi-ai 0.84.1 的 7 个导出全部特征化；本轮在其上补一块此前从未直接测量的砖——重开条件文本中「等价 `./providers/all.models` 导出」一臂的**文件级直接探针**（历轮由 342 文件目录清点侧证，本轮首次让通配 specifier 本身失败作证），至此重开条件的两臂（`models.generated` 三 specifier + `all.models`）在本 VM 双 Node 上全部 blocked：

1. **零漂移五重确认（连续第六轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变——frozen-lockfile 内容寻址使包内容漂移在数学上不可能；pi-ai 0.84.1 exports 图未变；`dist/providers/` 仍恰 39 个 `.models.js` 表，本 VM 直接计数）、测试面（切片相关测试目录自 `8dee7fb` 零变动）、生产调用图（本轮再 grep：`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` 失手回退 / `runtime.ts` 点用处 / `listed-model.ts` 同步面，`model-catalog.ts` 命中为 S7-I-1 注释非导入；pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方）、基线增量（`8f706ca` 相对 R12-I 基线 `84aa66f` 仅 R12-G/H/I/J 与 R13 系文档收口 + 切片外 S13-B 判定，EXCLUSIONS 对 I 切片无新约束行）。R8-I 关闭矩阵、R9-I～R12-I 抽测所依据的结构前提在本基线逐项成立。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字，派单明令项）**：R9-I 5 格承重集 × 2 Node 版本全部 exit 0，结构逐格同构：地板 53.8/50.4 ms，**custom−builtin 差 children +45.5 / +23.7、track +51.7 / +21.5 ms（22.14/22.22）**——与 R12-I 带（+51.3/+29.1、+58.7/+24.1）、R11-I 带（+45.9/+22.8、+48.0/+23.4）、R10-I 带（+47.1/+36.6、+56.0/+26.4）、R9-I 带（+53.9/+26.9、+49.4/+28.2）同号、同量级、同 Node 版本梯度（**六轮同构**）。进程内冷载锚点：`providers/all` 50.8-60.7 ms@22.14 / 30.4-31.8 ms@22.22（warm 0.064-0.099 ms）；per-provider 表 anthropic 3.4/2.4、openai 1.4/0.9 ms（优势 ~9-25×）；失手解析首次 1.755/1.830 ms、重复失手中位 **0.101/0.095 ms**（四度复证 R10-I 记忆化否决）。S7-I-1 per-provider 优势与 custom 态回退归属完好（§1）。
3. **第十三组新角度枚举为空（§2）**，其中一项为本轮新测量、仍不铸 ID：
   - **`providers/all.models` 文件级可达性探针（§2.1）**：`@earendil-works/pi-ai/providers/all.models` 经 `./providers/*` 通配导出可解析为 specifier，但 `dist/providers/all.models.js` 不存在——直接 import 双 Node 一致 **ERR_MODULE_NOT_FOUND**；`ls` 亦确认无此文件。加上三种 `models.generated` specifier 本 VM 复测仍全部 blocked（ERR_INVALID_MODULE_SPECIFIER / ERR_PACKAGE_PATH_NOT_EXPORTED ×2，双 Node 一致），**S8-I-1 重开条件的两臂（`models.generated` 导出 / 等价 `all.models` 导出）在 0.84.1 内均被直接测量封死**，无任何旁路。此项为重开条件审计而非机制候选——未构造替代注册表、未触碰回退路径，不属自制 union / 磁盘快照 / memo 三重围栏中的任何 rename——不铸 ID。
   - 其余全部角度逐条映射到排除行或历轮无 ID 收口（§2.2），无一越过落地线；本轮两个从未点名的微观形态（`import.meta.resolve` 同步预探测 miss、`buildLiveCatalogConfig` 默认别名行 spread 复制）均为换名拒绝（S4-I-5 族 / S1-I-8 邻域 µs 族），不铸 ID。
4. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**；r7i part B 1220 builtin 行 × 39 provider lazy===sync；part D 载荷探针复证 builtin 构建后 providers/all 保持冷 60.1 ms、custom 回退命中缓存 2.1 ms）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R12-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读证据，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交 `scripts/round13-r13i-equivalence-sim.ts`（全部 harness/探针源码进附录 A，遵守 R7-F/R8-C/R9-I～R12-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md 未触碰。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S12-\* / S13-B-1 条目；R9-I / R10-I / R11-I / R12-I 空枚举维持，未事后发明 S9-I-\* ～ S12-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：`--children` 复用 smartChildPlan 已校准目录（main.ts L726-729 原样）、Pi runtime 点用处 `await import`（main.ts L149 / auth-session 函数体内；S4-I 败者为 **S4-I-2..5**，无 S4-I-1）、12 条分支独占分派模块惰性导入（赢家 ID **S5-I-1**）、per-provider 惰性模型表（`listed-model-lazy.ts` + `model-catalog.ts` L58）——零漂移即字节级证明，三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅对其**重开条件**做旁路封死审计（§2.1）——`all.models` 文件级探针 + 三 specifier 复测后确认 0.84.1 无任何导出形态的轻量注册表，未满足，维持淘汰。硬不变量「`providers/all` 失手回退 + throw-outside-catch」逐字在位（`listed-model-lazy.ts` L34-37 未动）。探针是重开条件证据，不是绕道重开，更不是自制 union（R11-I 三重围栏）/ 磁盘快照（R10-I）/ 进程内 memo（本轮重复失手 0.095-0.101 ms 四度复证 µs 族）的 rename——本轮未构造任何替代注册表。
  - **未重开 S8-I-2**（预热无重叠窗口：ESM 实例化主线程 CPU）、**S8-I-3**（enabled 循环 Promise.all——µs + 未知 id 错误选择分歧）、**S7-I-2..4**、**S6-I-1..3**、**S5-I-2..5**、**S4-I-2..5**、**S3-I-1..6**、**S2-I-1..6**、**S1-I-1..8**。本轮 25 文件重读逐字再确认（未立项）：flowchart 双 `buildLiveCatalogConfig`（S1-I-1，main L569+L583）、providers.json 双读（S1-I-2，main L609 + createExecutor kind==="pi" L131）、loadLearnedRouting/loadProvidersConfig 死载荷下沉（S2-I-1/S3-I-3）、`smartChildPlan` assignments.find（S1-I-3）、双 createAgentProfileRegistry（S3-I-2）、stdout 批量（S3-I-4）、冲突检查提升（S4-I-4）、describeSparkleModel try/catch 预探测（S4-I-5）。
  - **R10-I / R11-I / R12-I 无 ID 收口维持**：跨进程版本键控磁盘快照、进程内失手记忆化、进程内自制 39 表 union（三重围栏：硬不变量明令 + 键表不被 pi-ai 版本锁定 + 相等性不加载 providers/all 不可核验）、回退后 per-provider 探针短路（≤3.5 ms/provider，仅 mixed 态可达）、root `.` / `./compat` 作替代源（R12-I：无表 + 严格更重双重不成立）。派单明令「自制 union / 磁盘快照 / memo 三重围栏维持」——本轮一项未铸。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项（S1-I-5 / S7-I-4 维持关闭）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；稀疏→已批准 R0 基线；双 LCB / 双归因在位（X0-11）；提升 proposal-first（Checkpoint F-PROD 开放，ADR-005/X0-1）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`listed-model.ts` 同步公开面与惰性面经 `listed-model-common.ts` 字节等价关系未动；`providers/all` 失手回退 + throw-outside-catch 在位；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未推送专属分支；未触碰在飞 R13-G / R13-H 分支与文件。

## 1. 本 VM 复测：5 格承重抽测 + 进程内锚点（双 Node，未拷贝历轮数字）

R9-I～R12-I 抽测集原样复刻（configured 态夹具、每迭代全新拷贝 state-root、2 预热 + 10 测量、显式二进制全路径，遵守 R7-I 配置态 × 命令类纪律；harness 全文附录 A.1）。

### 1.1 抽测中位数（ms，spawn-to-exit，全部 exit 0）

| 格 | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|
| `--version`（地板） | 53.8 (min 52.7, p90 55.8) | 50.4 (49.1, 52.0) |
| run-children@builtin | 105.9 (104.1, 111.9) | 103.7 (101.9, 105.2) |
| run-track@builtin | 122.6 (118.0, 126.1) | 121.5 (119.4, 124.4) |
| run-children@custom | **151.4** (145.6, 163.8) | **127.4** (126.2, 129.9) |
| run-track@custom | **174.3** (167.0, 184.5) | **143.0** (138.0, 147.8) |

- **custom−builtin 差**：children **+45.5 / +23.7**、track **+51.7 / +21.5** ms（22.14/22.22）。R12-I 带 +51.3/+29.1 与 +58.7/+24.1，R11-I 带 +45.9/+22.8 与 +48.0/+23.4，R10-I 带 +47.1/+36.6 与 +56.0/+26.4，R9-I 带 +53.9/+26.9 与 +49.4/+28.2——**六轮同号、同量级、同 Node 版本梯度**。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
- **builtin 态目录构建**比地板贵 +52.1/+53.3（children）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。本 VM 地板与 R11-I VM（53.0）几乎一致、比 R12-I VM（58.4）快 ~8%，属 VM 个体差，格间结构不变。
- **Node 版本项**：22.22.2 的节省集中在触发 `providers/all` 的 custom 格（−24.0 children / −31.3 track），地板与 builtin 格 ±3.4 ms 内——`getPackageScopeConfig` 病理属 Node 版本项的历轮判断第六轮吻合。环境项，非切片代码角度。

### 1.2 进程内冷载锚点（附录 A.2 脚本，工作区根执行，测毕删除）

| 锚点 | 22.14.0 | 22.22.2 |
|---|---|---|
| `providers/all` cold import（3 次） | 60.7 / 50.8 / 60.1 ms | 31.3 / 30.4 / 31.8 ms |
| `providers/all` warm（第二次 await） | 0.064-0.070 ms | 0.097-0.099 ms |
| per-provider `anthropic.models` cold | 3.4 ms | 2.4 ms |
| per-provider `openai.models` cold | 1.4 ms | 0.9 ms |
| `providers/all`（双表已载后） | 53.3 ms | 28.4 ms |
| custom id 失手解析首次 | 1.755 ms | 1.830 ms |
| 同上，重复失手中位（N=49） | **0.101 ms** | **0.095 ms** |

- per-provider 优势 ~9-25×，S7-I-1 归属完好；custom 格增量与 `providers/all` cold 同量级，回退归属自洽。
- 重复失手 ~0.10 ms 四度复证 R10-I：Node package-reader 已缓存失败解析，记忆化物理无收益（<0.3 ms/构建）。

## 2. 第十三组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类），每形态先对 EXCLUSIONS 全表 + 十二轮 I 报告做同机制先例检索。R12-I 已把 7 导出全部特征化；本轮把重开条件的「等价导出」一臂推进到文件级直接测量，是该审计的最后一块砖。

### 2.1 `providers/all.models` 文件级可达性探针——不铸 ID，S8-I-1 重开条件两臂全封死

S8-I-1 重开条件的措辞历轮收紧为「pi-ai 自身 ship 版本锁定的 `models.generated`（**或等价 `./providers/all.models`**）」。`models.generated` 三 specifier 的不可达 R11-I 已直接测量；但 `all.models` 一臂此前仅由 R8-I 的 342 文件目录清点侧证——通配导出 `./providers/*` 使 `@earendil-works/pi-ai/providers/all.models` 成为**合法可解析 specifier**，若上游在补丁版本内静默补上该文件，目录清点结论会过期而 exports 图不变。本轮直接测量（附录 A.3 脚本，双 Node）：

- **`@earendil-works/pi-ai/providers/all.models` → ERR_MODULE_NOT_FOUND**（双 Node 一致）：specifier 经通配导出解析成功、目标文件 `dist/providers/all.models.js` 不存在。`ls` 直接确认无此文件；`dist/providers/` 仍恰 **39 个** `.models.js`（本 VM 直接计数，与 R11-I union 探针的 39 表一致）。
- **三种 `models.generated` specifier 本 VM 复测**：`providers/../models.generated` → ERR_INVALID_MODULE_SPECIFIER；`dist/models.generated.js` 与 `models.generated` → ERR_PACKAGE_PATH_NOT_EXPORTED（双 Node 一致，与 R11-I/R12-I 逐字同结局）。
- **裁决**：重开条件的两臂（`models.generated` 导出物 / 等价 `all.models` 聚合表）在 0.84.1 内均被直接测量证伪，加上 R12-I 的 root/compat 特征化（无表 + 更重）与 exports 图 7 项复核，「重开物是否已在包内以任何形态 ship」第六轮完备否定。此项为审计证据，非机制候选：未构造替代注册表、未触碰回退路径——**不铸 ID**。

### 2.2 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级即死）

| 扫描角度 | 处置 |
|---|---|
| custom id 跳回退 / 自制 union / 磁盘快照 / 进程内失手 memo | S8-I-1 硬禁 + R11-I/R10-I 三重围栏无 ID 收口；重开条件本轮两臂审计后仍未满足；派单明令不得铸 rename ID |
| **`import.meta.resolve` 同步预探测替代 try/catch miss**（本轮从未点名微观） | **换名拒绝不铸 ID**：机制上是 S4-I-5（describeSparkleModel try/catch 换预探测，平行实现风险）的惰性面镜像；量级上被 §1.2 直接定界——首次失手 1.8 ms 至少付一次（resolve 同样遍历 exports），重复失手已被 Node 缓存到 ~0.10 ms，custom 态每构建 ≤2 次重复 → 收益 <0.3 ms，µs 族 |
| **`buildLiveCatalogConfig` 默认别名行 spread 复制**（fastRow/primaryRow 补 `cheap`/`premium` 行，本轮从未点名微观） | **换名拒绝不铸 ID**：每目录构建 ≤2 次对象 spread（~11 字段），ns-µs 级；S1-I-8（resolveListedModel 自定义路径先构建再 find）邻域微观族，深度低于落地线两个数量级 |
| root / compat 作替代注册表源 | R12-I 直接测量封死（无表 + 严格更重，双重不成立） |
| providers/all 预热重叠 | S8-I-2 逐字（ESM 实例化主线程 CPU，无重叠窗口） |
| enabled 循环并行 per-provider 导入 | S8-I-3 逐字（µs + 未知 id 错误选择分歧） |
| 回退后 per-provider 探针短路（mixed 态） | R11-I §2.2 无 ID 收口（≤3.5 ms/provider，仅 mixed 态可达；跨调用穿线撞 X1-1） |
| pi-adapter barrel（`index.ts`）隐藏静态边 | 本轮再 grep 为无：全 src/ 无生产导入方；providers/all 的 src 内边仅 lazy 回退 / runtime 点用处 / 同步面（model-catalog.ts 命中为注释） |
| flowchart 路径双 `buildLiveCatalogConfig` | S1-I-1 逐字（第二次构建全走 warm 模块缓存，~190 µs；custom 态第二次付 warm 0.06-0.10 ms 而非二次冷载——§1.2 warm 锚点旁证） |
| providers.json 双读（run：main L609 + createExecutor pi 分支） | S1-I-2 逐字（~60 µs） |
| loadLearnedRouting / loadProvidersConfig 死载荷下沉 | S2-I-1 / S3-I-3 逐字（损坏时错误路径发散） |
| `smartChildPlan` assignments.find 换 Map / 双 registry | S1-I-3 / S3-I-2 逐字（children 个位数 / 8.4 µs） |
| pause/inject 免 router / 免 learned | S2-I-2 / S9-G-1 逐字 |
| resume/answer 预读去重 / PAUSED 探测降级 | S2-I-4 / S4-I-2 逐字 |
| compile-cache / 分派拆分 / 剩余独占边 | S6-I-1..3 逐字（once-per-process 亚两位数 ms） |
| dist 布局向 getPackageScopeConfig 开刀 / 引擎升级 | S5-I-2..5 族；Node 版本项（§1.1 梯度第六轮旁证），环境项非切片角度 |
| 一次性命令类（models/auth/doctor/inspect/episode/commits/adapt/pref/delete） | S1-I-4 / S7-I-4 / S2-I-5 / S3-I-4 类；once-per-process 噪声按派单直接否决 |
| 凭据面（auth status --all、credential store 微观） | S1-I-5 / S7-I-4 围栏，只读证据 |
| buildInvocation / invocationError / parseModelRef / parseProvidersConfig / parseCliErrorJson 微观 | S1-I-7 / S2-I-3 / S3-I-1 / S3-I-5 / S2-I-6 / S1-I-6 逐字（实测更慢或 ns 级） |
| pi-executor / cluster-tools 执行面微观 | 网络 I/O 支配的 live 面；µs 级，无复杂度类角度 |
| telemetry INVOCATION_CALL_OUTCOMES includes→Set | 表长 4，S1-D-8 族噪声 |
| writeAtomicJson 去 fsync / doctor-overlay jsonl 扫描 | S9-G-3 族（不得去 fsync）/ S3-J-4 族 + 一次性诊断类 |
| `builtinModelLazy` 动态 specifier / `_MODELS` 键过滤微观 | 命中路径被表解析成本（0.9-3.4 ms）支配、失手路径 ~0.1 ms；µs 族 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖（含包内容内容寻址锁定）、pi-ai exports、测试面、调用图五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选；唯一数十 ms 结构（custom/mixed 态 providers/all 失手回退）的全部消除形态收敛到同一个上游前提——pi-ai ship 轻量权威子路径——该前提的两种成立形态本轮均被文件级直接测量排除。

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**（part C：./model-catalog.js 在 main.ts 共 5 个共享调用点复核）；`round07-r7i-equivalence-sim.ts` **80 / 0**（part B：1220 builtin 行 × 39 provider lazy===sync；part C：17 组失手/custom/对抗用例；part D 载荷探针：builtin 构建后 providers/all 保持冷 60.1 ms、custom 回退命中缓存 2.1 ms；part E：configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单强制项）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2（与 R9-I～R12-I 计数逐字相同）、`pnpm build` 绿。
- **CLI smoke（派单项）**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **引擎注记**：22.14.0 的 doctor engines 预检 fail-closed 为设计行为（R9-I §3 记录在案），本轮不重复展开；本 VM 22.14.0 仅经 `/exec-daemon/node` 可达。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动，无 landed-sim 之外的额外 gate 需求；无赢家故未提交 `scripts/round13-r13i-equivalence-sim.ts`。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；本轮新测量的 `all.models` 文件级探针封死重开条件最后一个未直测旁路；两个从未点名微观均为换名拒绝 + µs 族）。
- 剩余赢家：无（唯一数十 ms 结构被 S8-I-1 健全性反例 + 硬不变量封死；重开条件两臂的包内旁路已被完备排除——所需的是 pi-ai 上游导出，不是仓内改动）。
- 矩阵空洞：无（R8-I 78 格全覆盖；本轮承重格六轮同构复现；R7-I 配置态 × 命令类纪律全程遵守——configured 态夹具、custom/builtin 双态、children/track 双命令类；mixed 态增量收益维持 R11-I 定界 ≤3.5 ms/provider）。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 5 格抽测 harness（/tmp/r13i-bench/spot.mjs，`<node二进制全路径> spot.mjs <同一路径>` 分别以两版本执行）

```javascript
// R13-I spot re-measure: floor + builtin/custom children+track cells (R9-I..R12-I harness style).
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r13i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.argv[2];
const WARMUP = 2, ITERS = 10;

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
for (const [name, cfg] of Object.entries(states)) {
  const dir = join(FIX, `state-${name}`);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
}
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
const CHILDREN = join(FIX, "children.json");
writeFileSync(CHILDREN, JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");

const cells = [
  { id: "version", args: () => ["--version"], state: undefined },
  ...["builtin", "custom"].flatMap((state) => [
    { id: `run-children@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] },
    { id: `run-track@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"] }
  ])
];
function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, `state-${state}`), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 120000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-FAIL ${cell.id}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

原始输出（run 命令均为 `--executor` 缺省下的域内校验路径，全部 exit 0）：

```
== v22.14.0 ==
version: median=53.8ms min=52.7 p90=55.8 exit=0
run-children@builtin: median=105.9ms min=104.1 p90=111.9 exit=0
run-track@builtin: median=122.6ms min=118.0 p90=126.1 exit=0
run-children@custom: median=151.4ms min=145.6 p90=163.8 exit=0
run-track@custom: median=174.3ms min=167.0 p90=184.5 exit=0
== v22.22.2 ==
version: median=50.4ms min=49.1 p90=52.0 exit=0
run-children@builtin: median=103.7ms min=101.9 p90=105.2 exit=0
run-track@builtin: median=121.5ms min=119.4 p90=124.4 exit=0
run-children@custom: median=127.4ms min=126.2 p90=129.9 exit=0
run-track@custom: median=143.0ms min=138.0 p90=147.8 exit=0
```

### A.2 / A.3 进程内锚点 + 可达性探针（§1.2 / §2.1 证据；临时置于 /workspace 根以获得 node_modules 解析，测毕删除）

```javascript
import { performance } from "node:perf_hooks";
const label = process.version;
const which = process.argv[2];
if (which === "all") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const dt = performance.now() - t0;
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  console.log(`${label} providers/all: cold=${dt.toFixed(1)}ms warm=${(performance.now() - t1).toFixed(3)}ms`);
} else if (which === "tables") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/anthropic.models");
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/openai.models");
  const t2 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const t3 = performance.now();
  console.log(`${label} anthropic.models=${(t1 - t0).toFixed(1)}ms openai.models=${(t2 - t1).toFixed(1)}ms providers/all-after-tables=${(t3 - t2).toFixed(1)}ms`);
} else if (which === "miss") {
  const N = 50; const times = [];
  for (let i = 0; i < N; i++) {
    const t0 = performance.now();
    try { await import("@earendil-works/pi-ai/providers/customco.models"); } catch { /* expected miss */ }
    times.push(performance.now() - t0);
  }
  const first = times[0];
  const rest = times.slice(1).sort((a, b) => a - b);
  console.log(`${label} miss-resolution: first=${first.toFixed(3)}ms repeat-median=${rest[Math.floor(rest.length / 2)].toFixed(3)}ms min=${rest[0].toFixed(3)}ms`);
} else if (which === "traversal") {
  for (const spec of [
    "@earendil-works/pi-ai/providers/../models.generated",
    "@earendil-works/pi-ai/dist/models.generated.js",
    "@earendil-works/pi-ai/models.generated",
    "@earendil-works/pi-ai/providers/all.models"
  ]) {
    try {
      await import(spec);
      console.log(`${label} REACHABLE: ${spec}`);
    } catch (e) {
      console.log(`${label} blocked: ${spec} -> ${e.code ?? e.message.slice(0, 60)}`);
    }
  }
}
```

原始输出：

```
v22.14.0 providers/all: cold=60.7 / 50.8 / 60.1 ms  warm=0.064-0.070 ms
v22.22.2 providers/all: cold=31.3 / 30.4 / 31.8 ms  warm=0.097-0.099 ms
v22.14.0 anthropic.models=3.4ms openai.models=1.4ms providers/all-after-tables=53.3ms
v22.22.2 anthropic.models=2.4ms openai.models=0.9ms providers/all-after-tables=28.4ms
v22.14.0 miss-resolution: first=1.755ms repeat-median=0.101ms min=0.069ms
v22.22.2 miss-resolution: first=1.830ms repeat-median=0.095ms min=0.069ms
v22.14.0 blocked: @earendil-works/pi-ai/providers/../models.generated -> ERR_INVALID_MODULE_SPECIFIER
v22.14.0 blocked: @earendil-works/pi-ai/dist/models.generated.js -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/models.generated -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/providers/all.models -> ERR_MODULE_NOT_FOUND   ← 本轮新增第四探针
v22.22.2 blocked: (同上四行，双 Node 一致)
```

MORE_OPTIMA=no
