MODEL_SLUG=claude-fable-5-thinking-xhigh

# R13-H：`src/evaluation/` + `src/requirement/` + `src/review/` + `src/rubric/` 第十三遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 13 / R13-H
**基线:** `cursor/sota-persistent-opt-83a1` @ `525a052`（独占 tip，含 R13-A/C/D/E 空枚举收口与 R13-B 的 S13-B-1 排除摄取；R13-F/G 在飞，未触碰）
**分支:** `cursor/r13-h-eval-thirteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十三遍枚举为空：无达门槛候选、零新 ID、切片零代码改动、切片维持关闭
（MORE_OPTIMA=no）。**
切片 21 个文件（1750 行 = 655+712+309+74，本轮 `wc -l` 逐目录复核一致；
8+7+4+2 清单逐目录确认恰 21 文件）自 R1-H 基线（`fd437a9`）以来**逐字节
未变**（`git diff fd437a9..525a052 --
src/{evaluation,requirement,review,rubric}/` 为 0 行、同范围 `git log`
为 **0 提交**，十三遍全程零 diff——指令要求的空 diff 检查通过），且
R12-H 基线（`ea1b214`）之后 `src/` **零提交**（`git log
ea1b214..525a052 -- src/` 为空——其间全部 11 个提交均为 R12-F/G/H/I/J
摄取与 Round 13 各波派单/摄取文档；R13-A/B/C/D/E 全数无代码落地，
S13-B-1 仅入排除表）⇒ 生产调用方地图**可证不变**（最强形式），本轮
全库 import 交叉检索再次独立确认（9 个切片外导入位点与 R3-H…R12-H
完全一致：7 个值级 = `assertCoverageAllowsStart`×3 /
`extractHeuristicContract` / `applyPrecedence` / `shouldScout` /
`assertCanPromoteFromReview`，2 个纯类型 = promotion.ts ReviewerKind /
primary-split.ts HeuristicHabits；`src/evaluation/` 全部 8 文件、
`review/{pairwise,reconcile,critic}.ts`、`src/rubric/` 仍零生产调用方）。
按指令在本 VM 重锚「配置态（3 档）× 命令类（6 类）× 执行层（热/冷）」
满格矩阵（§1.3–§1.5，三次独立运行，Node v22.22.2）：热层默认态
**9.3–10.4µs**/run（居十三轮历史跨 VM 带 3.9–10µs 上沿，与 R10-H
9.9–10.1µs 同段；一次 10.37µs 轻微越带上缘属本 VM 抖动，§1.2 已证调用
图零变更，量级类属不变）、配置态 A **7.4–8.2µs**（三次全低于默认态）、
配置态 B **4.1–4.6µs**（三次全低于默认态）——**无配置态悬崖**、默认
锚点仍是保守口径；晋升类 18.1–18.7ns；四个零生产调用方命令类
（evaluate 1.10–1.16µs / critique 0.62–0.65µs / pairwise 2.0–2.2µs /
reconcile 61–68ns）**全部 ≤2.2µs**；冷层首调默认 1685–1849µs、配置
1647–1662µs once-per-process（与 R6-H…R12-H 同带，否决类归属不变）。
第十三遍在完整排除表（S1-H-1..9 … S9-H-1..2 及 R10-H/R11-H/R12-H 三轮
空枚举收口共 50+ 处不立 ID 记录）之上以一个从未用过的完备性轴收口：
**规模-越线标定（scale-crossover calibration）**（§2.1）——前十二轮
全部在固定真实规模上测锚点，本轮把三个生产面各自沿输入规模十进制
扫描、拟合增长律、实测钉出「到达落地线（数十 ms）所需的规模」，用
实测曲线校准 R4-H…R12-H 收口原文里的重开条件（「合同规模增长 ≥2 个
量级」）：Face R（extractor 正则面，objective 长度 ×1/×8/×64/×512）
线性、尾段斜率 15.8–16.3ns/char，10ms 越线点在 **~61–63 万字符**
（真实 CLI 句子的 ~9,000 倍）；Face T（run-start 门任务面，
T=5/50/500/5000）线性、越线点 **~4.1–5.7 万任务**（真实 5 任务计划的
~8,000–11,000 倍）；Face C（门准则面，合成 schema 合法合同
C=2/20/200/1000，全部过生产 `validateRequirementContract`）实测出
**已知 S1-H-1/S4-H-1 辖区的 Object.keys-每准则二次方**（拟合指数
2.53–2.61，超二次部分来自每准则 O(C) 键数组物化的分配/GC 增长），
10ms 越线点 **C≈482–503**；关键校准结论：重开条件触发点 C=200（+2
个量级）实测门成本仅 641–683µs，仍比 10ms 落地地板低 **≥14×** ⇒
**重开条件先于落地线可达点触发，标定保守正确、二次方不穿底**；同时
量化了 S1-H-1 拒列的规模条件性——若合同规模真到 C~500，S1-H-1 的
Set 提升将成为真赢家，但那是重开领地而非今日流量（生产 heuristic
恒产 C≤2）。三个 fail-closed 卫在每个规模点均实测保持（26 项确定性
sanity 检查三次运行全通过）。另做 H 切片首次**双引擎热锚**（dist 新
进程，nvm v22.22.2 vs 系统 v22.14.0，镜像 R13-E 双 Node 交叉锚定）：
12.9–13.5µs 同带、双向差 ≤4.5%，**无引擎版本悬崖**。结构位置 × 消除
机制扫描本轮**未发现任何从未点名的位点**——第十三遍读码找到的全部
微观位点逐一对表后均为换名重提或已关家族站点换位（§0 十五项审计，
含本轮新识别的 reconcile.ts L39 Set-from-map 第四站点 = R12-H P2
家族「三站点一并收口」的站点换位，按指令不铸 ID、不做探针），是
十三轮以来首次**候选化之前即全数拒列**（与 R12-G 首次完全空枚举
同型的收口强度）。诚实结论：枚举为空，不硬凑 ID。未重开任何 X* /
S1-* … S12-* / S13-B-1 条目；S5-H-1 守卫经本轮 diff + 读码双复核
**原样保留**；S8-H-3 全决议快路径**未做任何形态**；S8-H-1
PairwiseInput 未收窄。`pnpm gate` 全绿。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/evaluation/`（8 文件：evaluator / delivery-adapter /
  diff-adapter / ownership / precedence / check-adapter / adapters /
  types）、`src/requirement/`（7 文件：normalizer / provenance /
  precedence / extractor / heuristic / coverage / critic）、
  `src/review/`（4 文件：pairwise / self-review / reconcile / critic）、
  `src/rubric/`（2 文件：registry / types）计 21 文件全量第十三遍实际
  读码，未依赖前十二轮记忆。上下游 `domain/contract.ts`、
  `track/{clarify,loop,plan,primary-split}.ts`、
  `run/{supervisor,coordinator,flowchart-run}.ts`、
  `adaptation/{promotion,promotion-rules}.ts` 只读取证，一行未改。
  切片外其余树零触碰。在飞 R13-F / R13-G 分支与文件未触碰、未等待。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表 546 行，含
  S13-B-1）/ round-13/PLAN.md / round-12/PLAN.md + R12-H.md /
  R11-H / R10-H（空枚举）/ R9-H / R8-H 按需（R10-H §2.1 导出面对账、
  R11-H §2.1 私有面对账、R12-H §2.1 剖面归账为本轮换名检查基底）。
  EXCLUSIONS.md / PROGRESS.md / PLAN.md 未编辑。
- **换名重提检查**（第十三遍读码发现的全部微观位点逐条对表，全部在
  候选化之前拒列，零探针、零仿真、零触碰）：
  - **S5-H-1（必须保留项）**：`requirement/precedence.ts` L24 的
    `if (fast.length === 0 || slow.length === 0) return [];` 分配前
    守卫经本轮读码 + `git diff fd437a9..HEAD --
    src/requirement/precedence.ts`（空）双复核**原样在位、逐字节
    未变**。本轮对该函数零候选、零「优化掉」提案。
  - **S8-H-3（永不重开项）**：`assertCoverageAllowsStart` 未做任何
    全决议快路径（含 resolved-superset 预判、gated 分支条件跳过、
    Q=0 免 `resolved` Set / 免 gated 拷贝、blocking 预扫描短路全部
    变形）。`coverage.ts` 零 diff。§2.1 Face C 的规模扫描只测生产
    函数原样执行，未引入任何变体。
  - **S8-H-1（永不重开项）**：`PairwiseInput` 全形状（含 comment
    双字段）未收窄；PIC 二态教训（S8-A-3 / S8-E-2 / S9-D-1 同族）
    未重演。
  - **S9-H-1 / S9-H-2**：`self-review.ts` 三函数零重提（死中间体 =
    逃逸分析地板；spread 单态化 = 晋升频次墙）。
  - `isVague` 的 `split(/\s+/)+filter` 换手写词计数早退 = **R2-H/
    R5-H/R7-H/R8-H no-ID 收口原条目**（R10-H §2 列表明文含「isVague
    词计数」；R5-H 已判 S1-B-2 类；R7-H 已判多正则合并单遍）。本轮
    重新独立发现后识别为换名，拒列。
  - `reconcile.ts` L39 `Array.from(new Set(dissent.map(...)))` 的
    Set-from-map 中间数组 = **R12-H P2 家族第四站点**（P2 裁决原文
    「三站点一并收口，不再逐点重提」+ 面零生产调用方 + n=2 真实
    规模）。按指令「closed-family site-swaps 不铸 ID」拒列。
  - `checkCoverageGate` L27 `Object.keys(...).includes` 与 L28
    `?.length` 的联合守卫「消除 includes 只留 ?.length」= **S4-H-1
    fail-open 家族更廉价形态**（去掉 own-key 判定后原型链键穿透：
    准则 id 为 `"constructor"` 时 `m["constructor"]?.length === 1`
    把从未插入的准则误判为已覆盖）。拒列，守卫原样。
  - `assertAuthorityGrounding` 的 sourcesByRef Map 在空 authority 下
    死构造 = **S2-H-2 原条目本体**（空 authority 早退，~44ns 已裁决）。
  - `SMALLEST_CHANGE` 每调用 spread 提升 = R2-H no-ID + R8-H 换名
    拒列记录；vague 分支问题对象字面量模块级提升 = S6-H-4 /
    S7-B-5 可观察身份改变家族。
  - `createEvaluationRecord` 的 `input.evidence[criterion.id]` 双读
    = R5-H evidence 双读 no-ID；聚合谓词三遍 = S1-H-7。
  - `remapToOriginalIds` "ab" 分支冗余展开 = R1-H no-ID；
    `getPrecedenceWeight` reduce 内双调用 = R2-H no-ID + S2-H-5；
    `createCriticObservation` hasPass 融合 = R2-H no-ID、score 条件键
    单态化 = R9-H 拒列；`heuristicCritic` omissions 双拷贝 = S4-H-3；
    `habits ?? {}` 空对象 = R10-H no-ID；`objective.trim()` 与
    isVague 内重复 trim = R6-H（S1-H-3 家族）；q-scope/q-write 恒假
    `questions.some` 守卫 = R4-H §2 注 no-ID（对未来分支扩展的防御）。
  - `changeSetsEqual` 三面（S1-H-9 / S2-H-4 / S3-H-4）、
    `registerRubric` copy-on-write（S1-H-8 反例 + S2-H-6；别名安全性
    本轮读码再确认：全部写路径整体替换 registry）、adapter
    async→sync（S7-H-1 + X0-4）、normalizeSources 默认 origin 守卫
    （S2-H-7）、q-tests 子集正则短路（S3-H-3）、S1-H-3 双
    namedTargets、excerpt slice（R5-H no-ID + R10-H SlicedString）、
    extractSignals 三正则（R1-H no-ID）、X4-9 changeSet Set 化、
    classifyDiffScope 控制流去糖（R10-H）、diff-adapter FAIL 路径死
    布尔化（R11-H L1）、coverage 冗余重存（R11-H L2）、
    buildContractFromSources 融合（R11-H L3）、orphans 防御拷贝
    （R12-H P1）、Set-from-map 三站点（R12-H P2）、vague 死
    wantsReview（R12-H P3）：全部维持，零候选。
  - **S13-B-1 / S12-B-1..2 / S12-C-1 / S12-D-1..2（其他切片）**：均
    不在本切片，未触碰、未换名重提（本切片无 learned 层、无
    (family, preferredModel) 记忆化对应物——extractor/critic 角色对
    的「批内记忆化」形态即 S6-H-4 已判家族，且本切片每 run 恰一次
    调用，无批内重复可收拢）。
  - R10-H / R11-H / R12-H 未铸 S10-H-* / S11-H-* / S12-H-*，本轮亦
    未事后发明任何此类编号，不为空枚举轮补铸 ID。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价（R1 未接线）、
  稀疏 → 已批准 R0 基线、双 LCB（beta+normal）与双归因
  （logit+prob-add）保留（X0-11）、晋升 proposal-first、阈值/CAS/
  凭据/数据面契约/公开签名不变、DEFAULT_REGISTRY 未就地变异、
  fail-open 零引入、测试未改，天然成立。不声称 Outcome-supported，
  Checkpoint F-PROD 仍开放（ADR-005 / X0-1）。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，且本 VM
  `/exec-daemon/node`（v22.14.0）在 PATH 前段遮蔽 nvm bin（与 R11-H /
  R12-H §0 注记同型）；测量与测试前已显式把 nvm v22.22.2 bin 置于
  PATH 首并复核 `node --version` = v22.22.2。
  `pnpm install --frozen-lockfile`（pnpm 10.17.1）后 `pnpm build` 绿。
- 未开 PR；未 force-push；未推送专属分支；仅提交并推送工作分支
  `cursor/r13-h-eval-thirteenth-pass-83a1`。无赢家 ⇒ 未提交
  `scripts/round13-r13h-equivalence-sim.ts`（按指令），锚点/规模扫描
  脚本全文在附录。

## 1. 基线不变性、调用图复核与满格矩阵重锚

1. **切片逐字节未变**：`git diff fd437a9..525a052 --
   src/{evaluation,requirement,review,rubric}/` 输出为 0 行（`--stat`
   同空），同范围 `git log` 为 0 提交（十三遍全程零 diff——指令要求
   的空 diff 检查通过）。21 文件 1750 行逐目录计数（655/712/309/74）
   与 R1-H 清单一致。R1-H 逐函数下界表、R2-H 调用图上界、R3-H 重复
   工作枚举、R4-H 三类角度收口、R5-H 三层级收口、R6-H 执行层级收口、
   R7-H 机器成本三面收口、R8-H 配置态矩阵收口、R9-H 命令类矩阵满格 +
   逃逸分析地板、R10-H 导出面完备性审计、R11-H 私有面完备性审计、
   R12-H 热链成本剖面归账与全部 S*-H-* 排除继承有效。
2. **调用图可证不变（最强形式）**：`git log ea1b214..525a052 -- src/`
   **为空**——R12-H 基线以来全部 11 个提交均为 `docs/reports/` 文档
   （R12-F/G/H/I/J 摄取、Round 12 收口、Round 13 全部派单/摄取；
   R13-A/B/C/D/E 无代码落地，S13-B-1 仅入排除表）⇒ 生产调用方地图
   数学上不变。本轮全库 import 交叉检索独立双确认：切片外 `src/`
   引用切片的文件恰 9 个，与 R3-H…R12-H 完全一致——值级 7 处：
   `assertCoverageAllowsStart` ← `run/supervisor.ts` /
   `run/coordinator.ts` / `run/flowchart-run.ts`（每 run 启动一次，仅
   当 `input.contract !== undefined`）；`extractHeuristicContract` ←
   `track/clarify.ts`（每 run 一次）；`applyPrecedence` ←
   `track/loop.ts`（每 run 一次，`"user-first"`）；`shouldScout` ←
   `track/plan.ts`（每 run 一次）；`assertCanPromoteFromReview` ←
   `adaptation/promotion-rules.ts`（每晋升一次）；纯类型 2 处
   （`adaptation/promotion.ts` ReviewerKind、`track/primary-split.ts`
   HeuristicHabits）。`src/evaluation/` 全部 8 文件、
   `review/{pairwise,reconcile,critic}.ts`、`src/rubric/` 及
   `applyRoutingScoreUpdate`、`buildContractFromSources` 的切片外
   `src/` 值级引用**为零**——未把假想接线当今日流量。
3. **热层三态锚点重锚**（三次独立运行，本 VM，Node 22.22.2；测量
   条件与 R10-H…R12-H 严格同构——benchAsync 单次预热 + 3000 计时，
   规模扫描块放在锚点之后以免额外预热污染可比性）：

```text
CEILING default  (9-pass anchor shape, Q=0):        9599 / 10371 / 9348 ns/run
CEILING configured A (habits 三键全设, Q=1 gated):   8196 /  7447 / 7470 ns/run
CEILING configured B (vague objective, Q=3 gated):   4640 /  4138 / 4594 ns/run
CEILING promotion class: assertCanPromoteFromReview = 18.7 / 18.1 / 18.3 ns/promotion
```

   默认态 9.3–10.4µs 居十三轮历史跨 VM 带（3.9–10µs）上沿、与 R10-H
   本带（9.9–10.1µs）同段；单次 10.37µs 轻微越带上缘属本 VM 抖动
   （§1.2 已证调用图零变更，chain 分量三次 8414–9352ns 与 R11-H 带
   8189–9085 重叠）。两档配置态三次**全部低于**默认态 ⇒
   R8-H…R12-H「无配置态悬崖、默认锚点是保守上界」在本 VM 复现。
   量级类结论不变：整个热面 µs 级，低于落地线 **3–4 个数量级**。
   gated 重映射真实执行（production assume-defaults 流恒传覆盖全部
   问题的 `resolvedQuestionIds`）。
4. **命令类矩阵剩余格重锚**（R9-H 首锚的四个零生产调用方类，本轮
   三次复测）：

```text
CEILING evaluate class  (TEST-ONLY): createEvaluationRecord(3 criteria) = 1123 / 1160 / 1104 ns/call
CEILING critique class  (chain 内):  critiqueContract(real contract)    =  616 /  646 /  622 ns/call
CEILING pairwise class  (TEST-ONLY): runBlindPairwisePair               = 2033 / 2150 / 2036 ns/call
CEILING reconcile class (TEST-ONLY): reconcileReviews(n=2)              =   61 /   65 /   68 ns/call
```

   与 R9-H…R12-H 锚点带一致（critique 本轮三次全落 ~620ns 峰，即
   R11-H/R12-H 双峰之高峰——JIT 分层状态，量级类属不变）。判据成立：
   **调用图未变下即便把这四个面全部接线，当前规模每类 ≤2.2µs，距
   落地线仍差 4+ 个数量级**。「配置态（3 档）× 命令类（6 类）×
   执行层（热/冷）」矩阵在本 VM 无未测格。
5. **冷层锚点复测（默认 + 配置双态）**：dist 产物、新进程、模块加载
   预扣除后，三生产入口首调合计中位数三次：默认态
   **1761 / 1849 / 1685 µs**（区间 [1656, 2085]µs）、配置态
   **1662 / 1662 / 1647 µs**（区间 [1597, 1774]µs）——与 R6-H
   1.65–1.71ms … R12-H 1.6–1.7ms 同带；once-per-process 否决类归属
   不变（S7-H-3 已证该层无净正对策）。
6. **双引擎热锚（H 切片首做，镜像 R13-E 双 Node 交叉锚定）**：dist
   新进程、500 预热 + 3000 计时，默认链：

```text
DUAL-ENGINE nvm v22.22.2:   13481 / 13359 / 13360 ns/run
DUAL-ENGINE system v22.14.0: 12901 / 13067 / 13476 ns/run
```

   两引擎同带、双向差 ≤4.5% ⇒ **无引擎版本悬崖**（dist 新进程口径
   高于 tsx 进程内锚点属 JIT 分层状态差异，口径内自洽）。

## 2. 第十三遍枚举：规模-越线标定（新轴）→ 诚实空结果

### 2.1 规模-越线标定（scale-crossover calibration，本轮新轴）

前十二轮的十三个完备性面（单函数 / 函数间 / 跨模块 / 调用图 /
模块图 / 执行层级 / 调用机器 / 配置态 / 命令类 / 编译器地板 /
导出面 / 私有面 / 成本剖面归账）全部在**固定真实规模**上测量；
R4-H…R12-H 的收口原文把重开条件定为「合同规模增长 ≥2 个量级」，但
该阈值从未被实测曲线校准过。第十三遍补上这最后一块：把三个生产面
各自沿输入规模十进制扫描、拟合增长律、钉出到达落地线（10ms 取保守
地板）所需的实测规模，回答两个从未直接回答的问题：**(a) 有没有任何
面在重开阈值以下就出现超线性悬崖？(b) 重开条件是否先于落地线可达点
触发（标定是否保守正确）？** 全部测生产导出函数原样执行（合成合同
过生产 `validateRequirementContract`，schema 合法），26 项确定性
fail-closed/sanity 检查三次运行全通过。三次运行代表值：

```text
Face R（extractor 正则面，objective 长度）：
  x1(67ch)=5337–5611ns  x8(585ch)=14198–15715ns  x64(4729ch)=81267–97870ns  x512(37881ch)=598127–617418ns
  尾段斜率 15.8–16.3ns/char → 10ms 越线点 ~613,539–633,327 字符（真实 CLI 句子的 ~8,892–9,179 倍）；线性，无悬崖
Face T（run-start 门任务面，C=2）：
  T=5:538–750ns  T=50:6059–6146ns  T=500:45404–47556ns  T=5000:880411–1205698ns
  尾段斜率 176–241ns/task → 10ms 越线点 ~41,470–56,792 任务（真实 5 任务计划的 ~8,294–11,358 倍）；线性，无悬崖
Face C（门准则面，合成合同 C=2/20/200/1000，tasks=5）：
  gate: C=2:586–638ns  C=20:9267–9872ns  C=200:641464–683425ns  C=1000:39552985–42957687ns
  critique: 497–609 / 927–1011 / 8003–8812 / 41544–47550 ns（线性）
  precedence: 129–144 / 1243–1306 / 11950–12143 / 65628–69195 ns（线性）
  gate 增长 ×10(20→200)=×65.8–71.1、×5(200→1000)=×58.2–67.0 → 拟合指数 2.53–2.61
  10ms 越线点 C≈482–503
```

三个收口事实：

1. **Face R / Face T 线性、无悬崖**：越线点都在真实规模的 ~10⁴ 倍，
   远超任何现实形态（objective 是一句 CLI 命令、任务计划个位数）。
   即使 ×512 档（3.8 万字符 objective）全链也只有 ~0.6ms。
2. **Face C 的二次方被实测证实且被重开条件正确包住**：gate 的
   超线性来自 `checkCoverageGate` 每准则物化
   `Object.keys(requirementToTasks)` 的 O(C²) 比较 + O(C²) 键数组
   分配（S1-H-1 / S4-H-1 已判辖区；指数 2.53–2.61 的超二次部分是
   分配/GC 增长）。10ms 越线点 C≈482–503；而重开条件触发点 C=200
   （较生产 C=2 增 2 个量级）实测仅 641–683µs，比 10ms 地板低
   **≥14×**（拟合保守口径亦 ≥6×）⇒ **重开条件先于落地线可达点
   触发，「≥2 个量级」标定保守正确，二次方不穿底**。这同时量化了
   S1-H-1 拒列的规模条件性：若调用图变更使合同规模到 C~500，
   S1-H-1 的 Set 提升（连同 S6-H-2 的 taskToChecks 死计算）将成为
   真赢家——但那正是收口原文的重开领地，今日零流量（生产
   heuristic 恒产 C≤2，wantsTests 时 C=2）。
3. **fail-closed 在每个规模点保持**：每档 T 与每档 C 都实测「去掉
   一个准则的覆盖 ⇒ `assertCoverageAllowsStart` 抛
   DomainValidationError」（26 项检查含 8 项 fail-closed 断言），
   规模扫描未发现任何随规模退化的门语义。

规模轴由此闭合：**没有任何面在重开阈值以下越线**；本切片「结构上
不存在达门槛候选」的十二轮结论现在有了沿规模维度的实测曲线支撑，
而不只是固定点锚点。

### 2.2 结构位置 × 消除机制扫描：零新位点 → 候选化之前全数拒列

第十三遍全格对账（结构位置：入口守卫 / 循环体 / 返回构造 / 跨调用
重复 / 数据字面量 / 正则求值 / 防御校验 / 拷贝语义 / 异步机器 /
模块加载 × 消除机制：死码 / CSE / 融合 / 快路径 / 记忆化 / 表示
变换 / 去糖 / 惰性 / 可变化 / 复杂度类）。与 R10-H（6 处）、R11-H
（3 处）、R12-H（3 处）逐轮递减的「从未点名位点」序列一致收敛：
**本轮为 0 处**——第十三遍读码发现的每一个微观位点都在 §0 的十五项
换名审计中对到了既有 ID / no-ID 收口记录（其中唯一接近新颖的
reconcile.ts L39 Set-from-map 是 R12-H P2 家族的第四站点换位，按
指令「closed-family site-swaps 不铸 ID」在候选化之前拒列，未做
探针）。1750 行 × 13 遍后，切片的可优化面（导出 + 私有 + 剖面 +
规模）四向闭合。**不立任何 S13-H-* ID**（与 R10-H / R11-H / R12-H /
R13-A / R13-C / R13-D / R13-E 空枚举收口同型）。

### 2.3 收口陈述

十三遍合计：单函数、函数间、跨模块、调用图、模块图、执行层级、调用
机器、配置态、命令类、编译器地板、导出面完备性、私有面完备性、成本
剖面归账、**规模-越线标定**十四个面全部有实测锚点或对账记录。本切片
在当前调用图下**结构上不存在**达门槛候选。总门槛按本 VM 数字刷新：
任何未来候选须先推翻满格矩阵实测上界——热层默认 **9.3–10.4µs/run**
（3000-rep 保守口径）、四个零调用方命令类 ≤2.2µs、冷层
**1.6–1.8ms once-per-process**（否决类）、双引擎无悬崖。重开该切片
的唯一前提维持 R4-H…R12-H 收口原文并经本轮实测标定：调用图变更
（evaluation/review/rubric 面接入每 turn 热路径，或合同规模增长
≥2 个量级——本轮已证该阈值在 Face C 二次方下仍先于 10ms 落地地板
触发，标定无需收紧）。

## 3. 逐文件收口（第十三遍新视角，其余与 R1-H…R12-H 一致）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `requirement/coverage.ts` | **Face T / Face C 规模扫描宿主（§2.1）**：二次方实测证实且重开条件包住；includes 守卫消除识别为 S4-H-1 fail-open 家族拒列（§0，"constructor" 原型链反例）；S8-H-3 零形态、S1-H-1/2、S6-H-2/3、R11-H L2、R12-H P1/P2 维持 | 无新候选 |
| `requirement/heuristic.ts` | **Face R 规模扫描宿主（§2.1）**：线性无悬崖；isVague 词计数识别为 R2-H/R5-H/R7-H/R8-H no-ID 换名拒列（§0）；SMALLEST_CHANGE 提升 = R2-H/R8-H 拒列；S1-H-3、S3-H-3、S6-H-4、S7-H-1、S8-H-2、R12-H P3 维持 | 无新候选 |
| `requirement/precedence.ts` | **S5-H-1 守卫在位复核（L24 早退，零 diff）**；Face C precedence 线性（65.6–69.2µs @C=1000 仍低于落地线两个量级）；S1-H-5、S4-H-2、S6-H-1 维持 | 无新候选 |
| `requirement/extractor.ts` | sourcesByRef 空 authority 死构造识别为 S2-H-2 本体（§0）；S1-H-4、S2-H-1、S3-H-1、S6-H-4、S7-H-1、R10-H roleId trim 维持 | 无新候选 |
| `requirement/critic.ts` / `provenance.ts` | Face C critique 线性（41.5–47.6µs @C=1000）；S2-H-3、S3-H-2 维持；findUnsourcedItems L30 = R12-H P2 站点维持 | 无新候选 |
| `requirement/normalizer.ts` | S2-H-7、S7-H-2、R5-H slice、R10-H SlicedString、R11-H L3 维持 | 无新候选 |
| `evaluation/evaluator.ts` | evidence 双读识别为 R5-H no-ID 换名（§0）；S1-H-7、INDEPENDENCE_CLASSES 表族（R1-H）、R10-H String() 分配维持；evaluate 类锚点复测同带 | 无新候选 |
| `evaluation/check-adapter.ts` / `delivery-adapter.ts` / `diff-adapter.ts` | changeSetsEqual 三面、S5-H-3、attributionMetadata 活性、R10-H 合取序、R11-H L1 维持 | 无新候选 |
| `evaluation/ownership.ts` / `precedence.ts` / `adapters.ts` / `types.ts` | X4-9、R10-H 去糖、getPrecedenceWeight 双调用 = R2-H/S2-H-5 换名（§0）、纯类型面维持 | 无新候选 |
| `review/pairwise.ts` / `reconcile.ts` / `critic.ts` / `self-review.ts` | remap "ab" = R1-H 换名；**reconcile L39 = R12-H P2 家族第四站点换位，不铸 ID（§0/§2.2）**；createCriticObservation 双记录 = R2-H/R9-H；S8-H-1、S1-H-6、S9-H-1/2 维持；pairwise/reconcile 锚点复测同带 | 无新候选 |
| `rubric/registry.ts` / `types.ts` | S1-H-8 反例 + S2-H-6 + R10-H createRubric 时间戳取证维持；DEFAULT_REGISTRY 别名安全第五次复核成立 | 无新候选 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。无仿真脚本入库（无赢家，遵循
round01–12 仅赢家 sim 入库惯例 + 本轮指令明文；完整脚本见附录）。

## 5. 测试与门

`pnpm gate` 全绿（Node 22.22.2 via nvm，满足 engines >=22.19.0；
系统 Node 22.14.0 过低 + `/exec-daemon/node` PATH 遮蔽的环境注记见
§0；`pnpm install --frozen-lockfile` 后）：

```bash
pnpm gate   # typecheck ✓ + lint ✓ + test: tests 1169 / pass 1168 / fail 0 / skipped 1 ✓ + build ✓
```

切片相关套件基线复核，全绿：

```bash
npx tsx --test "test/unit/requirement/*.test.ts" "test/unit/evaluation/*.test.ts" \
  "test/unit/review/*.test.ts" "test/integration/m3/checkpoint-d.test.ts" \
  "test/integration/m3/coverage-gate.test.ts" \
  "test/integration/m3/requirement-extraction.test.ts" \
  "test/integration/m4/delivery-evidence.test.ts"
# tests 93 / suites 13 / pass 93 / fail 0（与 R1-H…R12-H 同套件同计数）
```

锚点 + 规模扫描（临时脚本 `/tmp/r13h-sim.mts`，无赢家故未入库；完整
源码见附录；冷层/双引擎探针以 `pnpm build` 后 dist 产物为准）三次
独立运行，26 项确定性 fail-closed/sanity 检查全部通过、裁决结论逐位
一致（默认锚点三次 9599/10371/9348ns、配置 A 8196/7447/7470ns、配置
B 4640/4138/4594ns；晋升类 18.7/18.1/18.3ns；evaluate
1123/1160/1104ns、critique 616/646/622ns、pairwise 2033/2150/2036ns、
reconcile 61/65/68ns；Face R 尾斜率 15.9/16.3/15.8ns/char、Face T 尾
斜率 211/241/176ns/task、Face C 拟合指数 2.55/2.53/2.61、越线点
503/501/482；冷层默认 1761/1849/1685µs、配置 1662/1662/1647µs；双
引擎 22.22.2: 13481/13359/13360ns vs 22.14.0: 12901/13067/13476ns）。

## 6. 新增排除 ID

**无。** 第十三遍枚举为空——结构位置 × 消除机制扫描零新位点（全部
在候选化之前对表拒列，§0 十五项换名审计），新轴规模-越线标定证明
没有任何面在重开阈值以下越线（§2.1）。不硬凑 ID，不为站点换位铸 ID。
与 R10-H / R11-H / R12-H / R13-A / R13-C / R13-D / R13-E 的空枚举
收口同型。

## 附录：确定性锚点 + 规模-越线标定脚本（完整，可复现）

运行方式：`pnpm build` 后保存为任意 `.mts` 并 `npx tsx <file>`（仓库
根目录，依赖已装；冷层/双引擎探针需要 dist 产物存在）。无赢家 ⇒ 无
随机 fuzz（无候选可裁决），26 项 sanity/fail-closed 检查全为固定输入
确定性断言，三次运行为精确重执行。按惯例不入库。

```ts
/**
 * R13-H deterministic anchor re-verification + scale-crossover calibration
 * (thirteenth pass).
 *
 * No fresh candidate survived enumeration (every micro-site found by the
 * thirteenth read is already family-covered by the exclusion table or a
 * prior no-ID closure — see report §0/§2), so this script (a) re-establishes,
 * on THIS VM, the full measurement matrix that R8-H..R12-H closed — hot tier
 * three states (default Q=0 / configured A habits+Q=1 gated / configured B
 * vague+Q=3 gated; the production assume-defaults track flow always passes
 * resolvedQuestionIds covering ALL questions so the gated re-map materially
 * executes), promotion class, the four zero-production-caller command
 * classes (evaluate / critique / pairwise / reconcile), and cold-tier
 * once-per-process first-call totals on dist (default AND configured,
 * 5 fresh processes each) — and (b) runs the thirteenth pass's NEW
 * completeness axis: SCALE-CROSSOVER CALIBRATION. All twelve prior axes
 * measured the chain at fixed real scale; this axis sweeps each production
 * face across input-scale decades, fits the growth law, and empirically pins
 * the crossover scale at which the face would reach the landing line
 * (tens of ms), thereby calibrating the standing reopen condition
 * ("contract scale growth >= 2 orders of magnitude", R4-H..R12-H closure
 * wording). This is measurement of the reopen threshold, NOT a claim that
 * hypothetical scale is today's traffic: no candidate is proposed at
 * synthetic scale.
 *
 *   Face R (regex/extractor): objective length sweep x1/x8/x64/x512 —
 *          the extractor slice-side (42-47% of the chain per R12-H profile)
 *          is regex + literal construction over the objective string.
 *   Face T (gate/tasks): task count sweep 5/50/500/5000 into
 *          assertCoverageAllowsStart with the production contract (C=2).
 *   Face C (gate/criteria): synthetic schema-legal contracts with
 *          C = 2/20/200/1000 acceptance criteria (each sourced), 5 tasks
 *          covering all criteria — empirically verifies the known
 *          checkCoverageGate quadratic (Object.keys(...).includes per
 *          criterion, S1-H-1 / S4-H-1 territory) and pins its crossover.
 *          Fail-closed behavior is asserted at every scale point.
 *
 * Also (c): a dual-engine hot anchor (dist, fresh process, v22.14.0
 * /exec-daemon/node vs nvm v22.22.2) — H-slice first, mirroring R13-E's
 * dual-Node cross anchoring — to show no engine-version cliff.
 *
 * Reference = production imports everywhere (all swept functions are
 * exported); synthetic contracts are passed through the production
 * validateRequirementContract so every scale point is schema-legal. With no
 * candidate to adjudicate there is no randomized fuzz this round: every
 * sanity/fail-closed check below is fully deterministic (fixed inputs),
 * so three runs are exact re-executions.
 */
import { performance } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import { existsSync } from "node:fs";
import {
  extractHeuristicContract,
  type HeuristicHabits
} from "/workspace/src/requirement/heuristic.js";
import {
  assertCoverageAllowsStart,
  checkCoverageGate,
  coverageMatrixFromTasks,
  type CoverageTaskRef,
  type CoverageStartOptions
} from "/workspace/src/requirement/coverage.js";
import { applyPrecedence, detectConflicts } from "/workspace/src/requirement/precedence.js";
import { critiqueContract } from "/workspace/src/requirement/critic.js";
import { validateRequirementContract, type RequirementContract } from "/workspace/src/domain/contract.js";
import { assertCanPromoteFromReview } from "/workspace/src/review/self-review.js";
import { runBlindPairwisePair, type PairwiseInput } from "/workspace/src/review/pairwise.js";
import { reconcileReviews } from "/workspace/src/review/reconcile.js";
import { createEvaluationRecord } from "/workspace/src/evaluation/evaluator.js";
import { createRubric } from "/workspace/src/rubric/types.js";
import type { EpisodeId, TaskId } from "/workspace/src/domain/ids.js";

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean, detail?: string): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

/* ================================================================
 * Hot tier, three states (R8-H..R12-H anchor shapes). Measured FIRST so
 * anchor conditions stay comparable to R10-H/R11-H/R12-H (benchAsync's
 * single warm call + 3000 timed reps, no extra chain executions before).
 * ================================================================ */
async function measureChain(label: string, objective: string, habits: HeuristicHabits | undefined): Promise<void> {
  const args = { objective, ...(habits !== undefined ? { habits } : {}) };
  const chain = await benchAsync(async () => {
    await extractHeuristicContract(args);
  }, 3000);
  const prod = await extractHeuristicContract(args);
  const real = prod.contract;
  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
  const options: CoverageStartOptions | undefined =
    real.questions.length > 0
      ? { resolvedQuestionIds: real.questions.map((question) => question.id) }
      : undefined;
  const gate = bench(() => assertCoverageAllowsStart(real, tasks, options), 50000);
  const precedence = bench(() => void applyPrecedence(real, "user-first"), 100000);
  const total = chain + gate + precedence;
  log(
    `CEILING ${label}: extractHeuristicContract=${(chain * 1e6).toFixed(0)}ns + run-start gate(Q=${real.questions.length}${options !== undefined ? ", all resolved" : ", no options"})=${(gate * 1e6).toFixed(0)}ns + applyPrecedence=${(precedence * 1e6).toFixed(0)}ns = ${(total * 1e6).toFixed(0)}ns once-per-run production total`
  );
}
{
  await measureChain(
    "default (9-pass anchor shape, Q=0)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    undefined
  );
  await measureChain(
    "configured A (habits set, Q=1 gated)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    { requireTests: true, preferReview: false, askBeforeWrite: true }
  );
  await measureChain("configured B (vague, Q=3 gated)", "fix bug please", { askBeforeWrite: true });
}

/* ================================================================
 * Command-class anchors beyond run-start.
 * ================================================================ */
{
  const promo = bench(
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    200000
  );
  log(`CEILING promotion class: assertCanPromoteFromReview=${(promo * 1e6).toFixed(1)}ns once per promotion (production)`);

  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const evaluate = bench(() => void createEvaluationRecord(evalInput), 50000);
  log(`CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=${(evaluate * 1e6).toFixed(0)}ns/call`);

  const prod = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const critique = bench(() => void critiqueContract(prod.contract), 50000);
  log(`CEILING critique class (inside run-start chain): critiqueContract(real contract)=${(critique * 1e6).toFixed(0)}ns/call`);

  const pairInput: PairwiseInput = {
    episodeId: "ep_00000001" as EpisodeId,
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pairwise = bench(() => void runBlindPairwisePair(pairInput, () => 0.7), 20000);
  const pair = runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  const reconcile = bench(() => void reconcileReviews(two), 100000);
  log(`CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=${(pairwise * 1e6).toFixed(0)}ns/call`);
  log(`CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=${(reconcile * 1e6).toFixed(0)}ns/call`);
}

/* ================================================================
 * NEW AXIS: scale-crossover calibration.
 * ================================================================ */

/* Face R: objective-length sweep on the full production chain. */
{
  const clause = "fix the login retry bug in src/auth/session.ts and keep tests green";
  const factors = [1, 8, 64, 512] as const;
  const reps = [3000, 1000, 150, 20] as const;
  const rows: { chars: number; ns: number }[] = [];
  for (let i = 0; i < factors.length; i += 1) {
    const objective = Array.from({ length: factors[i] as number }, () => clause).join("; then ");
    const t = await benchAsync(async () => {
      await extractHeuristicContract({ objective });
    }, reps[i] as number);
    // Sanity: the scaled objective still yields a valid non-vague contract
    // with one file deliverable per repeated clause.
    const out = await extractHeuristicContract({ objective });
    check(
      `Face R sanity (x${factors[i]}: deliverables scale with named targets)`,
      out.contract.deliverables.length === 1 + (factors[i] as number),
      `got ${out.contract.deliverables.length}`
    );
    rows.push({ chars: objective.length, ns: t * 1e6 });
    log(`SCALE R (objective x${factors[i]}, ${objective.length} chars): extractHeuristicContract=${(t * 1e6).toFixed(0)}ns/run (${(t * 1e6 / objective.length).toFixed(1)}ns/char)`);
  }
  const last = rows[rows.length - 1] as { chars: number; ns: number };
  const perChar = last.ns / last.chars;
  const cross10ms = Math.round(10e6 / perChar);
  log(`SCALE R crossover: at measured tail slope ${perChar.toFixed(1)}ns/char the chain reaches 10ms only at ~${cross10ms.toLocaleString("en-US")}-char objectives (~${Math.round(cross10ms / 69).toLocaleString("en-US")}x the real CLI sentence) — growth is linear, no cliff below the reopen threshold`);
}

/* Face T: task-count sweep into the run-start gate (production contract, C=2). */
{
  const prod = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const real = prod.contract;
  const counts = [5, 50, 500, 5000] as const;
  const reps = [20000, 5000, 500, 50] as const;
  const rows: { t: number; ns: number }[] = [];
  for (let i = 0; i < counts.length; i += 1) {
    const tasks: CoverageTaskRef[] = Array.from({ length: counts[i] as number }, (_, k) => ({
      id: `tsk_${String(k).padStart(8, "0")}` as TaskId,
      acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
    }));
    const t = bench(() => assertCoverageAllowsStart(real, tasks), reps[i] as number);
    // Fail-closed sanity at scale: dropping coverage of one criterion must throw.
    const uncovered = tasks.map((task) => ({ ...task, acceptanceCriteria: task.acceptanceCriteria.slice(0, 1) }));
    let threw = false;
    try {
      assertCoverageAllowsStart(real, uncovered);
    } catch {
      threw = true;
    }
    check(`Face T fail-closed at T=${counts[i]}`, threw);
    rows.push({ t: counts[i] as number, ns: t * 1e6 });
    log(`SCALE T (tasks=${counts[i]}, C=2): assertCoverageAllowsStart=${(t * 1e6).toFixed(0)}ns/run (${(t * 1e6 / (counts[i] as number)).toFixed(1)}ns/task)`);
  }
  const last = rows[rows.length - 1] as { t: number; ns: number };
  const perTask = last.ns / last.t;
  const cross10ms = Math.round(10e6 / perTask);
  log(`SCALE T crossover: at measured tail slope ${perTask.toFixed(1)}ns/task the gate reaches 10ms only at ~${cross10ms.toLocaleString("en-US")} tasks (~${Math.round(cross10ms / 5).toLocaleString("en-US")}x the real 5-task plan) — linear, no cliff`);
}

/* Face C: synthetic criteria-count sweep (schema-legal, production-validated).
 * This is the face that carries the KNOWN quadratic: checkCoverageGate
 * materializes Object.keys(requirementToTasks) per criterion (S1-H-1 /
 * S4-H-1 closed territory at real scale C=2). */
function syntheticContract(criteriaCount: number): RequirementContract {
  const src = [{ kind: "message" as const, ref: "src-1" }];
  return validateRequirementContract({
    schemaVersion: 1,
    objective: "synthetic scale probe objective for crossover calibration",
    deliverables: [{ id: "d-1", description: "d", artifactKind: "diff", sourceRefs: src }],
    constraints: [],
    nonGoals: [],
    acceptanceCriteria: Array.from({ length: criteriaCount }, (_, i) => ({
      id: `ac-${i + 1}`,
      description: `criterion ${i + 1}`,
      observableCheck: `automated check ${i + 1}`,
      sourceRefs: src
    })),
    assumptions: [],
    questions: [],
    authority: [],
    sourceRefs: src
  });
}
{
  const counts = [2, 20, 200, 1000] as const;
  const reps = [50000, 10000, 200, 10] as const;
  const critReps = [50000, 10000, 1000, 200] as const;
  const rows: { c: number; gateNs: number; critNs: number; precNs: number }[] = [];
  for (let i = 0; i < counts.length; i += 1) {
    const contract = syntheticContract(counts[i] as number);
    check(`Face C contract is schema-legal (C=${counts[i]})`, contract.acceptanceCriteria.length === (counts[i] as number));
    const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, k) => ({
      id: `tsk_0000000${k}` as TaskId,
      acceptanceCriteria: contract.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
    }));
    // Sanity: gate passes when fully covered, and the raw gate result is clean.
    const matrix = coverageMatrixFromTasks(contract, tasks);
    const gateResult = checkCoverageGate(contract, matrix);
    check(`Face C gate ok at C=${counts[i]}`, gateResult.ok && gateResult.uncoveredCriteria.length === 0);
    // Fail-closed sanity: removing coverage of the last criterion throws.
    const uncovered = tasks.map((task) => ({
      ...task,
      acceptanceCriteria: task.acceptanceCriteria.slice(0, (counts[i] as number) - 1)
    }));
    let threw = false;
    try {
      assertCoverageAllowsStart(contract, uncovered);
    } catch {
      threw = true;
    }
    check(`Face C fail-closed at C=${counts[i]}`, threw);
    check(`Face C detectConflicts empty at C=${counts[i]}`, detectConflicts(contract).length === 0);
    const gate = bench(() => assertCoverageAllowsStart(contract, tasks), reps[i] as number);
    const crit = bench(() => void critiqueContract(contract), critReps[i] as number);
    const prec = bench(() => void applyPrecedence(contract, "user-first"), critReps[i] as number);
    rows.push({ c: counts[i] as number, gateNs: gate * 1e6, critNs: crit * 1e6, precNs: prec * 1e6 });
    log(
      `SCALE C (criteria=${counts[i]}, tasks=5): run-start gate=${(gate * 1e6).toFixed(0)}ns | critiqueContract=${(crit * 1e6).toFixed(0)}ns | applyPrecedence=${(prec * 1e6).toFixed(0)}ns`
    );
  }
  // Quadratic verification on the gate: successive x10 in C should approach
  // x100 in time once the per-criterion Object.keys materialization dominates.
  const r1 = (rows[2] as { gateNs: number }).gateNs / (rows[1] as { gateNs: number }).gateNs; // C 20 -> 200
  const r2 = (rows[3] as { gateNs: number }).gateNs / (rows[2] as { gateNs: number }).gateNs; // C 200 -> 1000 (x5)
  const alpha = Math.log(r2) / Math.log(5);
  log(`SCALE C growth: gate x10 (20->200) = x${r1.toFixed(1)}; x5 (200->1000) = x${r2.toFixed(1)} => fitted exponent ~${alpha.toFixed(2)} (known Object.keys-per-criterion quadratic, S1-H-1/S4-H-1 territory)`);
  const last = rows[rows.length - 1] as { c: number; gateNs: number };
  const k = last.gateNs / (last.c * last.c);
  const crossC = Math.round(Math.sqrt(10e6 / k));
  log(`SCALE C crossover: on the fitted quadratic the gate reaches 10ms at C~${crossC.toLocaleString("en-US")} criteria (real production C=2; reopen condition "scale +2 orders" = C~200 stays ~${(10e6 / (k * 200 * 200)).toFixed(0)}x below the 10ms landing floor => calibration holds, quadratic does NOT undercut it)`);
}

/* ================================================================
 * Cold tier on dist, default + configured (5 fresh processes each).
 * ================================================================ */
const mkColdScript = (configured: boolean): string => `
  const { performance } = await import("node:perf_hooks");
  const { extractHeuristicContract } = await import("/workspace/dist/requirement/heuristic.js");
  const { assertCoverageAllowsStart } = await import("/workspace/dist/requirement/coverage.js");
  const { applyPrecedence } = await import("/workspace/dist/requirement/precedence.js");
  const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
  const args = ${"{"} objective${"}"};
  ${configured ? 'const cfgArgs = { objective, habits: { requireTests: true, preferReview: false, askBeforeWrite: true } };' : ""}
  const t0 = performance.now();
  const prod = await extractHeuristicContract(${configured ? "cfgArgs" : "args"});
  const tasks = Array.from({ length: 5 }, (_, i) => ({
    id: "tsk_0000000" + i,
    acceptanceCriteria: prod.contract.acceptanceCriteria.map((c) => ({ id: c.id }))
  }));
  ${configured
    ? "assertCoverageAllowsStart(prod.contract, tasks, { resolvedQuestionIds: prod.contract.questions.map((q) => q.id) });"
    : "assertCoverageAllowsStart(prod.contract, tasks);"}
  applyPrecedence(prod.contract, "user-first");
  const t1 = performance.now();
  process.stdout.write((t1 - t0).toFixed(3));
`;
{
  const median = (xs: number[]): number => {
    const sorted = [...xs].sort((a, b) => a - b);
    return sorted[Math.floor(sorted.length / 2)] ?? Number.NaN;
  };
  for (const [label, configured] of [
    ["default", false],
    ["configured (habits + Q=1 gated)", true]
  ] as const) {
    const samples: number[] = [];
    for (let i = 0; i < 5; i += 1) {
      const proc = spawnSync(process.execPath, ["--input-type=module", "-e", mkColdScript(configured)], {
        encoding: "utf8"
      });
      if (proc.status !== 0) {
        check(`cold probe runs (${label})`, false, proc.stderr);
        break;
      }
      samples.push(Number(proc.stdout.trim()));
    }
    log(
      `COLD first-call production total (dist, fresh process, ${label}): median=${(median(samples) * 1e3).toFixed(0)}µs range=[${(Math.min(...samples) * 1e3).toFixed(0)}, ${(Math.max(...samples) * 1e3).toFixed(0)}]µs — once per PROCESS (veto class)`
    );
    check(`cold probes produced numbers (${label})`, samples.length === 5 && samples.every(Number.isFinite));
  }
}

/* ================================================================
 * Dual-engine hot anchor (H-slice first; mirrors R13-E's dual-Node cross
 * anchoring): default chain on dist, warm + 3000 timed reps, one fresh
 * process per engine.
 * ================================================================ */
{
  const hotScript = `
    const { performance } = await import("node:perf_hooks");
    const { extractHeuristicContract } = await import("/workspace/dist/requirement/heuristic.js");
    const { assertCoverageAllowsStart } = await import("/workspace/dist/requirement/coverage.js");
    const { applyPrecedence } = await import("/workspace/dist/requirement/precedence.js");
    const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
    const run = async () => {
      const prod = await extractHeuristicContract({ objective });
      const tasks = Array.from({ length: 5 }, (_, i) => ({
        id: "tsk_0000000" + i,
        acceptanceCriteria: prod.contract.acceptanceCriteria.map((c) => ({ id: c.id }))
      }));
      assertCoverageAllowsStart(prod.contract, tasks);
      applyPrecedence(prod.contract, "user-first");
    };
    for (let i = 0; i < 500; i += 1) await run();
    const t0 = performance.now();
    for (let i = 0; i < 3000; i += 1) await run();
    process.stdout.write((((performance.now() - t0) / 3000) * 1e6).toFixed(0));
  `;
  const engines: [string, string][] = [["nvm v22.22.2", process.execPath]];
  if (existsSync("/exec-daemon/node")) engines.push(["system v22.14.0", "/exec-daemon/node"]);
  for (const [label, bin] of engines) {
    const proc = spawnSync(bin, ["--input-type=module", "-e", hotScript], { encoding: "utf8" });
    if (proc.status !== 0) {
      check(`dual-engine probe runs (${label})`, false, proc.stderr);
      continue;
    }
    log(`DUAL-ENGINE hot default chain (dist, fresh process, ${label}): ${proc.stdout.trim()}ns/run`);
  }
}

if (failures > 0) {
  process.stderr.write(`\n${failures} of ${checks} check(s) FAILED\n`);
  process.exit(1);
}
log(`\nALL ${checks} EQUIVALENCE/SANITY CHECKS PASSED (deterministic, seeded)`);
```

MORE_OPTIMA=no
BRANCH=cursor/r13-h-eval-thirteenth-pass-83a1
