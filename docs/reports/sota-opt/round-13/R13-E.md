MODEL_SLUG=claude-fable-5-thinking-xhigh

# R13-E：`src/learning/` 第十三遍复查报告（Round 13）

**战役:** 全库持久 SOTA 优化 Round 13 / R13-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `49841da`（含 R13-B 摄取与 S13-B-1 排除已入表）
**分支:** `cursor/r13-e-learning-thirteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第十三组新角度枚举为空（有理由的
空枚举，未铸造任何 S13-E-\* ID，与 R10-E / R11-E / R12-E 同口径），切片维持
关闭（MORE_OPTIMA=no）。**

1. **零漂移三重确认**：切片 10 个文件（`wc -l` 确认恰 10 文件、合计 1770 行，
   无多无少）自 R1-E 裁决基线（`adb20d7`）至本轮基线（`49841da`）**逐字节
   未变**——`git diff adb20d7..HEAD -- src/learning/` 为 0 行、
   `git log adb20d7..HEAD -- src/learning/` 为 **0 提交**，十三轮全程无人触及
   该目录。且 R12-E 基线（`f661a87`）与 R10-E 基线（`8d598f5`）之后的全部
   提交（R12-C..J 收口、Round 12 收口、R13-A/R13-B 摄取与 R13-C/D/E 派单）
   **仅触及 `docs/reports/sota-opt/`**——`git diff --stat f661a87..HEAD --
   src/ test/` 与 `git diff --stat 8d598f5..HEAD -- src/ test/` 均为空，
   src 与 test 一行未动。R1-E..R12-E 的全部规模测量、调用面图景与
   S1-E-1..8 / S2-E-1..7 / S3-E-1..5 / S4-E-1..3 / S5-E-1..5 / S6-E-1..5 /
   S7-E-1..5 / S8-E-1..3 / S9-E-1..3 共 44 项切片排除裁决（R10-E / R11-E /
   R12-E 未铸 ID，其空枚举收口一并继承）原样成立、原样继承。
2. **本 VM 锚点/矩阵重锚（未拷贝 R12-E 数字，遵派单令；本轮独有：同 VM
   双 Node 版本交叉锚定）**：SLICE-CPU 总量上界锚点 **24 次运行**（16 次
   Node v22.14.0 + 8 次 Node v22.22.2，engines `>=22.19.0` 仅后者合规）
   **13.5–15.9µs/run**（collect 10.2–12.4µs、outcomes 1.5–1.7µs、diagnose
   1.00–1.40µs、bandit-build 0.7–0.9µs；两版本同带，版本差异不可分辨）——
   距落地线（≥10ms）**628–738×**，即使把切片 CPU 清零也远不达门槛，在历史带
   （~11–24µs/run）之内。按历史最保守合成口径（R7-E outcomes 带 7.0–7.3µs +
   R12-E diagnose 分组上界 3.83–3.92µs 同时替换本轮两项）总量 ~21.7–24.4µs，
   仍 **≥409×** 低于门槛。三种口径结论一致。配置态 live 面：
   `applyLearnedRouting` 非空策略 **258–312ns/task**（R11-E 254–281 /
   R12-E 258–295 同带）、空策略 **111–132ns/task**（R12-E 111–128 同带）；
   `loadLearnedRouting` 全新 root 24.6–42.4µs、现实 registry
   **76.1–88.6µs**（R8-E 80–85 / R12-E 76.1–83.2 同带，亚 ms 一次性；另有
   1 次 126.3µs 首运行冷缓存离群，紧随 registry 写盘之后，24 次中仅此一次）。
   全部确定性 VERDICT 行 24 次运行**逐位一致**，3/3 sanity 检查全过
   （§1、附录 A）。
3. **配置态 × 命令类矩阵复核无洞**：全部切片进入点行号逐条 grep 复核，
   **与 R8-E..R12-E 记录逐一相同、无新增调用方、无新增命令类**
   （`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
   `run/flowchart-run:681/712`、`track/loop:88/172`；resume/replay 经
   `flowchartForSupervisor` @ `flowchart-run:880/1021` 各单次载入；
   `run --flowchart` @ `cli/main:580` 在 `:708` 载入前返回，无双载入）。
   S8-E-1 双载入按令保护，未做任何去重分析。切片外唯一其余消费者仍为
   `adaptation/eval-routing.ts:223` 的 `parseLearnedRoutingPolicy`
   （离线 eval 面，D 切片辖区）。
4. **第十三组新角度枚举为空（§2）**：本轮新透镜为「函数内已建结构复用 ×
   中间分配普查」（R13-B 跨界记忆化键域透镜的**被调侧镜像**——B 侧问
   「调用点能否收拢重复调用」，本轮问「函数体内已付费构建的结构是否被
   旁路、每次调用制造哪些中间分配」）。对第十三遍全量重读的 10 个源文件
   逐函数普查，定位出三个此前十二轮从未逐字点名的结构位置
   （`updateProjectBandit` 奖励环成员判断旁路同作用域已建 `arms` Set、
   `applyLearnedRouting` avoided-Set 的 filter+map 双中间数组、
   `routingPolicyIdentity` 构造字面量经 `parseProjectId` 全量再校验），
   全部落进已关闭机制类或落地线算术封死区。被调侧记忆化整族（
   `stableProjectKey` / `truncate` / `applyLearnedRouting` 结果）由 X1-1
   与 S13-B-1 辖区双重封死。**未铸造任何 S13-E-\* 新 ID，无新增排除提议；
   S13-B-1 未以任何形态在本切片重提；`applyLearnedRouting` 内部零改动
   （S13-B-1 重开条件「本体成本增长 ≥2 个量级」未被触碰，亦未被实现）。**
5. **验证面**：`pnpm gate` 在 engines 合规的 Node v22.22.2 上**全绿**
   （typecheck ✓ / lint ✓ / test **1168 pass / 0 fail / 1 skipped**（共
   1169）/ build ✓）。切片相关套件 **48 tests / 2 suites / 48 pass /
   0 fail**（两个 Node 版本均过）。注：系统默认 node 为 v22.14.0
   （违反 engines `>=22.19.0`），在其上 `doctor.test.ts` 的 preflight
   检查按设计 fail-closed（1 fail）——纯环境现象非代码回归，切换
   nvm v22.22.2 后消失；R12-E §0 已记录同一 engines 事实。零代码改动下
   全部硬不变量天然满足。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与
双归因未动；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故
未提交 `scripts/round13-r13e-equivalence-sim.ts`（锚点脚本差异面进附录 A，
遵守 R7-F/R8-C/R9-I/R10-E/R11-E/R12-E 无赢家纪律）。本切片在其输出契约与
数据面语义下维持 SOTA——第十三遍复查确认：**剩余的全部 ms 级余量仍全部在
被排除表点名保护的 I/O 契约面上**，切片级收口条件（R3-E §7 … R12-E §6）
依然成立。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行）本轮第十三遍全量实际读码，未依赖前十二轮
  记忆。上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、
  `feedback/`、`persist/`、`privacy/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表，含 S13-B-1）→
  round-13/PLAN.md → round-12/PLAN.md + round-12/R12-E.md（空枚举）→
  R11-E / R10-E（空枚举）→ R13-B（S13-B-1 判决书与辖区边界）→ 10 个
  源文件。R13-C / R13-D 在其他分支在跑，未等待、未触碰其报告与分支。
- 基线漂移检查（§结论 1）：切片零提交零 diff；`f661a87..49841da` 与
  `8d598f5..49841da` 的 src/test 均零 diff——前十二轮全部测量与裁决
  原样成立。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S12-\*、S13-B-1 条目。
  特别地：
  - **S13-B-1**（B 切片，本轮新入表）未以任何变体在本切片重提：其消除
    机制整个落在切片外调用点（`assign.ts:100–105` 的批内记忆化），本切片
    辖区只有被调方 `applyLearnedRouting` 本体——本轮对其**零改动**
    （切片零 diff 下必然），其单次成本锚点 258–312ns/task 与 R13-B §2d
    「本体从不是成本主体」的机制性结论一致；重开条件（本体成本增长
    ≥2 个量级）既未触碰亦未实现。被调侧记忆化变体见 §2 行 4 裁决。
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护，本轮仅复核其行号与频次
    （§1），未做任何去重分析。并发 promote 下的新鲜性仍是其保留理由。
  - **S9-E-2 pass-2 预过滤是负优化（E=400 五次全负）**——本轮未再尝试
    任何两遍结构的工作集缩减变体；两遍语义按硬不变量维持。
  - **S9-E-1 / S9-E-3 / X2-6 / X0-3 / X0-8** 及派单点名的全部「勿重述」
    条目未以任何新 ID 重述（§2 逐条换名检查）。
  - R10-E / R11-E / R12-E 未铸 S10-E-\* / S11-E-\* / S12-E-\*，本轮亦未
    事后发明任何此类编号。
- **换名重提检查**（识别为既有方案换名/换位点，**未列为新候选**；R10-E §0
  七条、R11-E §2 四条、R12-E §2 五条换名裁决全部原样维持，此处不再复抄）：
  本轮新定位形态的换名检查全部并入 §2 表格逐行给出，并逐一对照
  S1-E-\* … S9-E-\* 全表、S13-B-1 与 R10-E/R11-E/R12-E 三轮空枚举面。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  稀疏 → 已批准 R0 基线；双 LCB（beta+normal）/ 双归因（logit+prob-add）
  在位；晋升 proposal-first；无阈值变更；CAS / 凭据 / 数据面契约 / 公开
  签名不动；fail-open / 隐藏模块缓存（X1-1）未引入；bandit/learned
  registry I/O 的 fsync 与完整性重哈希未触碰；corrupt registry
  fail-closed（S2-I-1 / S9-G-1 族）未触碰；两遍
  `collectSignalsFromEvents` 语义未触碰；X0-3（auto-loop 与 from-episode
  不合并）维持；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未 force-push；
  未推送专属分支；仅提交并推送本分支上的本报告一个文件。
- 环境：本 VM 系统默认 node v22.14.0（`/exec-daemon/node`，PATH 前置遮蔽
  nvm），nvm 另装 v22.22.2（engines `>=22.19.0` 仅后者合规）。锚点在两个
  版本各测（§1，同带）；`pnpm gate` 以 v22.22.2 为准（§4）。依赖经
  `pnpm install --frozen-lockfile` 全新安装（pnpm 10.17.1）。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

R7-I 教训（默认态夹具可能掩盖配置态主路径）按令复查。src 自 `8d598f5` 起
一行未动，故 R8-E..R12-E §1 矩阵表结构原样成立；本轮逐条 grep 复核全部
切片进入点行号，**与 R12-E 记录逐一相同、无新增调用方**：

| 命令类 × 配置态 | 切片进入点（本轮行号复核） | 频次与规模 |
| --- | --- | --- |
| `adapt auto --run` | `runAutoAdaptFromEvents` @ `cli/adapt:188` → `runAutoAdaptLoop` | 每进程一次，E≈41 |
| `adapt auto --project` | `runAutoAdaptLoop` @ `cli/adapt:205`（空事件表，S4-E-1 面） | 每进程一次 |
| `adapt learn --run` | `proposeRoutingFromRoutedEvents` @ `cli/adapt:168`（S2-E-2 面） | 每进程一次 |
| 普通 `run` | `loadLearnedRouting` @ `cli/main:708`（S2-I-1 已裁决） | 每进程一次 |
| `run --flowchart` | `startFlowchartRun` @ `cli/main:580` → `flowchartForSupervisor` @ `flowchart-run:747→712` 单次载入（在 `cli/main:708` 之前返回，无双载入） | 每进程一次 |
| `run --children` | `cli/main:708` 载入 → `applyLearnedRouting` @ `routing/assign:102` 每任务 → `startFlowchartRun`（@ `cli/main:748`）内 `flowchartForSupervisor` @ `flowchart-run:747→712` 第二次载入（S8-E-1，勿去重）→ `applyLearnedRouting` @ `flowchart-run:681` 每节点 → `runAutoAdaptLoop` @ `cli/main:783` 一次 | 双载入各一次；任务/节点 ≤10/几十 |
| `track`（startTrackedRun） | `track/loop:88` 载入 → 每任务 `applyLearnedRouting` → `startFlowchartRun` @ `track/loop:152` → `flowchart-run:712` 第二次载入（同 S8-E-1）→ `runAutoAdaptLoop` @ `track/loop:172` 一次 | 同上 |
| flowchart resume / replay | `flowchartForSupervisor` @ `flowchart-run:880` / `:1021` 各单次载入（无双载入，R8-E..R12-E 复核维持） | 每恢复一次 |
| SPARKLE_AUTO_ADAPT=0 | 仍收集（persistSignals + updateProjectBandit 照付），仅跳过 propose；R5-E 裁决维持 | 不变 |
| 测试专用面 | `patterns` / `attribution` / `signatures` / `compareSignatures` / `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `parseObservedSignal`(直调) / `proposeRoutingFromAssignments`(deprecated) / `saveLearnedRouting`(deprecated, throw-only) / `learnedRoutingPath`(死导出) 交叉检索复核仍零生产调用方 | 不进热路径 |

切片外其余消费者复核：`adaptation/eval-routing.ts:223` 仅调用
`parseLearnedRoutingPolicy`（离线 eval 面，D 切片）；`track/primary-split.ts:3`
仅导入类型；`privacy/record-classes.ts` 仅文档字符串提及路径布局。
**矩阵结论：无新洞**。配置态给本切片的全部新增工作仍是每任务/每节点一次
`applyLearnedRouting` + 每 run 至多两次 `loadLearnedRouting`（S8-E-1 已
点名保护），均在本轮实测锚点覆盖内。

SLICE-CPU 锚点本 VM 重测（**24 次运行**：16 × Node v22.14.0 + 8 ×
Node v22.22.2；seeds `0xe13e01`–`0xe13e03`；夹具镜像 R8-E..R12-E 生成器，
R1-A 组成 E=41）：

```text
collect=10.2-12.4us  outcomes=1.5-1.7us  diagnose=1.00-1.40us  bandit-build=0.7-0.9us
total in-slice CPU ~13.5-15.9us per full auto-adapt run （两 Node 版本同带，不可分辨）
vs landing bar >=10000us  ->  628-738x below EVEN IF ZEROED
最保守合成口径（R7-E outcomes 7.0-7.3us + R12-E diagnose 分组上界 3.83-3.92us 同替）
  -> ~21.7-24.4us，仍 >=409x 低于门槛
configured-state: applyLearnedRouting(avoid=10, M=10) = 258-312ns/task （R11-E 254-281 / R12-E 258-295 同带）
                  applyLearnedRouting(空策略)        = 111-132ns/task （R12-E 111-128 同带）
                  loadLearnedRouting fresh(ENOENT)   = 24.6-42.4us/次
                  loadLearnedRouting 现实 registry    = 76.1-88.6us/次 （R8-E 80-85 / R12-E 76.1-83.2 同带，
                                                        亚 ms 一次性，S8-E-1 带；另 1 次 126.3us 首运行冷缓存离群）
```

注：本轮夹具（seed 组 `0xe13e01`–`0xe13e03`）组成为 collected=14 /
outcomes=2 / diagnoseGroups=1（与 R11-E 的 15/2/1 接近、与 R12-E 的
19/3/0 不同，夹具差异非代码差异）；diagnose 分组路径（分组 / reduce /
filter / unique / mode / sort）经 diagnoseGroups=1 全数走到（R11-E 同
协议），保守合成口径另引 R12-E 分组上界 3.83–3.92µs 替换。全部确定性
VERDICT 行（夹具规模、live 面 sanity、registry 夹具产出激活策略）24 次
运行**逐位一致**，3/3 sanity 检查全过。**锚点复核成立：该切片不存在
不推翻既有排除就能达门槛的候选。**

## 2. 第十三组新角度枚举：有理由的空枚举

方法：本轮新透镜「**函数内已建结构复用 × 中间分配普查**」——R13-B 跨界
记忆化键域透镜（产出 S13-B-1）的被调侧镜像：对第十三遍全量重读的 10 个
源文件逐函数问两件事：(a) 函数体内已付过构建费的数据结构（Set/Map/数组）
是否在同一作用域被更贵的等价探针旁路；(b) 每次调用制造哪些可消除的中间
分配（filter/map 链、spread 链、构造字面量再校验）。每个形态先对
EXCLUSIONS 全表 + 十二轮 E 报告（含 R10-E/R11-E/R12-E 三轮空枚举面）做
同机制先例检索（换名检查），只有「排除表未覆盖且理论收益可达数十 ms 或
复杂度类下降」者才立项。本轮扫描结果：**零个可立项形态**，逐类裁决如下
（均未铸造新 ID——按 R9-E..R12-E 方法论，排除行换名/换位点重现与已关闭类
的机制变体不重复立项；落地线算术直接封死的形态无候选资格）：

| 形态（本轮第十三遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **`updateProjectBandit` 奖励环成员判断旁路已建 Set**（`bandit-store:72`：`state.arms.includes(signal.modelId)` 逐信号 O(A) 线性扫描，而同作用域 `:46-52` 已付费构建的 `arms` Set（`state.arms === [...arms]`，includes 与 Set.has 同为 SameValueZero）可 O(1) 等价替换）——此前十二轮从未逐字点名 | 每合格信号一次 O(A)→O(1)，S≤12、A≈3 ⇒ 每 run 数十 ns | 落地线算术封死 + 已关闭类机制变体拒列：函数内已建 Set 复用族在邻切片两度闭合（S2-F-4 全实验仅 ~0.8%、S7-B-6 每批一次 ns 级）；且整个 `updateProjectBandit` 在排它文件锁内被 read+write I/O 支配（S3-E-4 实测结论、S6-E-3 mkdir 单项 22.7µs 即 >400× 于此收益） | S2-F-4 / S7-B-6 族 + S3-E-4 / S6-E-3 |
| **`applyLearnedRouting` avoided-Set 构造的双中间数组**（`learned-routing:198-202`：`new Set(learned.avoid.filter(...).map(...))` 每次调用制造 filter 数组 + map 数组两个中间分配，可融合为单 for-of + Set.add，输出逐位等价）——空 avoid 快路径（R11-E 行 3）已裁决，非空侧融合从未单列 | 非空策略全额 258–312ns/task 即天花板；×≤10 任务 + 几十节点 ⇒ <3µs/run | 已关闭类机制变体拒列 + 落地线算术封死：filter+map 融合族三度实测阴性（S2-E-6 n=10 实测更慢、S1-A-4 实测更慢、S6-B-1 40–73µs 淘汰）；**本条按 S13-B-1 辖区纪律显式声明：`applyLearnedRouting` 内部零改动，本体成本零增长（重开条件未触碰）**；调用点记忆化 = S13-B-1（B 辖区，不重提） | S2-E-6 / S1-A-4 / S6-B-1 族 + S13-B-1 边界 |
| **`routingPolicyIdentity` 构造字面量全量再校验**（`learned-routing:43`：`prj_${stableProjectKey(...)}` 是受控哈希构造串，仍经切片外 `parseProjectId` 契约实施点全量校验）——从未逐字点名 | 每 run ≤4 次（双载入 ×2 + propose 路径），每次 ns~百 ns ⇒ 亚 µs | 防御纵深删除族拒列：R12-E 行 2（`outcomesFromRoutedRun` 内部字面量再校验）同族第二位点；绕过需平行非校验构造器 = X1-2 第二入口；且 `parseProjectId` 在切片外（domain/ids，只读区）；载入面整体被派单列为 reject（R11-E 行 1 真子集论证同式） | R12-E §2 行 2 / S2-D-5 / S3-H-1 族 + X1-2 |
| **被调侧记忆化整族**（`stableProjectKey` 按 projectRoot / `truncate` 按 text / `applyLearnedRouting` 按 (family,preferredModel) 结果缓存）——R13-B 透镜镜像扫描的正面清点 | 每 run 各 ≤4 次 / ≤S 次 / ≤10+几十次，全部 ns~亚 µs | 双重辖区封死：模块级缓存 = X1-1（R12-E 行 1 对 stableProjectKey 已裁决）；调用点批内记忆化 = S13-B-1 辖区（B 切片已裁决其无收益池：learned 本体 ~百 ns/任务，memo 键构造 + Map 哈希吃掉差额——该机制性结论同样封死本切片一切「把 apply 结果缓存起来」的变体） | X1-1 / S13-B-1 + R13-B §2d |
| `runAutoAdaptLoop` 三路拼接拷贝、`persistSignals` filter 双求值、some-探针跨函数去重、`ingestSubagentDirectory` 顺序读 | ns~µs 级 | 换名重提拒列：R12-E 行 13 / S1-E-4 / R11-E 行 4 / S1-E-5 逐字维持 | S1-E-4/5 / S5-E-3 族 |
| `optimizedPolicy` filter+map 双遍、`proposeAndMaybePromote` 的 `candidatesFor().find()` 线性扫描 | 每 run 一次，ns 级且在 registry 锁内被 load/save I/O 支配 | 换名重提拒列：S2-E-6 逐字同位点；S1-D-2 换调用侧（R12-E 行 12 维持）；X0-3 保护两函数主体不合并 | S2-E-6 / S1-D-2 / X0-3 |
| `collectSignalsFromEvents` pass-1 五 Map / JUDGE_DECISION 双 Map.get / verdict 双分派链 / 条件 spread 形状 | pass 1+2 合计 ~10.2–12.4µs/run 全部 | 三向封死 + 硬不变量明令：合遍（S1-E-1）/ 预过滤（S9-E-2 负优化）/ 空快路径（S4-E-1）/ 惰性构建（R12-E 行 4）；S1-E-2 / R10-E 行 2 / S8-E-2 逐字维持 | S1-E-1 / S9-E-2 / S4-E-1 / S1-E-2 / S8-E-2 + 硬不变量 |
| `bandit-store` 奖励环不可变链批量化 / 吸收析取 / mkdir / 紧凑序列化 / no-change 跳写 | 每 run ≤µs 级或 I/O 契约面 | 换名重提拒列：R12-E 行 3（X1-2 + I/O 支配）；R9-E/R10-E 吸收析取无 ID 收口；S6-E-3/4、S2-E-4 逐字维持 | R12-E §2 行 3 / S6-E-3/4 / S2-E-4 |
| `from-episode` 内部字面量再校验 / Date.parse / familyFromRouted includes / isCompleteRoute 谓词序 / applyCascadeRetry / seen-Set 单探针 / outcomeKindFromResult 死形参 | ns 级 | 全部维持 R10-E..R12-E 拒列，本轮读码确认位点未变（切片零 diff 下必然） | R10-E §2 / R11-E §2 / R12-E §2 |
| `scoreTaskResult` 尾部 `return 70` / 死导出清理（`saveLearnedRouting` / `proposeRoutingFromAssignments` / `learnedRoutingPath`） | 0ns 运行时 | 不立项：无收益路径即无候选资格；公开签名必须保留（硬不变量）；R11-E 行 5 / R12-E 行 5 同式 | 硬不变量（公开签名） |
| 两遍 `collectSignalsFromEvents` 的任何结构变换（合遍/预过滤/空快路径/惰性构建/工作集缓存） | pass 1+2 全部 | 三向封死 + 硬不变量明令 | S1-E-1 / S9-E-2 / S4-E-1 + 硬不变量 |
| I/O 契约面全体：fsync 丢弃、完整性重哈希跳过、no-change 跳写、双载入去重、corrupt registry fail-open、readdir 并行化、惰性载入、mkdir 提升、紧凑序列化 | 切片相关全部 ms 级余量所在（本轮实测 registry 载入 76.1–88.6µs/次、写侧更贵） | 排除行逐字重现 + 硬不变量明令封死：全部数十 µs~ms 级形态均在排除表点名保护的 I/O 契约面上。特别地 `routingPolicyContent` 的 pretty-print 格式参与 contentHash ⇒ 任何序列化变更破坏 CAS 契约，比 S6-E-4 更硬 | X0-3 / S2-E-1 / S2-E-4 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1 / S1-E-4/5 / S2-I-1 / S9-G-1 族 + CAS |
| 测试专用面任何优化（`patterns` / `attribution` / `signatures` 三文件 + `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `compareSignatures`） | 不进任何生产路径 | 本轮交叉检索复核仍零生产调用方，自动拒列 | S1-E-7/8 / X2-6 族 |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（十三轮）、src/test 自 R10-E 基线零漂移、调用位点行号全数复核一致（§1） | — |

**提议排除 ID：无（本轮无「新增排除 ID」表——空枚举未铸任何 S13-E-\*）。**
切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选。前十二轮已
逐类收口的九个角度类（单遍化、分配避免、正则整形、数据结构替换、I/O 重叠、
形状单态化、死存储、工作集缩减、分派树合一）之外，第十三遍以被调侧
「结构复用 × 分配普查」透镜新探的三个结构位置 + 记忆化整族清点全部落进
已关闭机制类或落地线算术封死区：切片总 CPU 13.5–15.9µs（本 VM 双 Node
版本）意味着**即使发明一个能清零全切片 CPU 的新机制也距门槛 628 倍以上**
（最保守合成口径 ≥409×），唯一物理上可达 ms 的位置全部是被硬不变量与
排除表双重保护的 I/O 契约面。

## 3. 逐文件收口（前十二轮收口之上的本轮新检查点）

| 文件 | 本轮第十三遍新视角检查 | 结论 |
| --- | --- | --- |
| `bandit-store.ts` | **奖励环 `state.arms.includes` 旁路已建 `arms` Set（§2 行 1）判 S2-F-4/S7-B-6 族 + 锁内 I/O 支配拒列**；不可变链批量化 = R12-E 行 3 维持；吸收析取、ENOENT catch 维持；fsync/重哈希/mkdir/序列化按令未触碰；S2-E-3/4、S3-E-4、S6-E-3/4、X1-2 维持 | 无候选 |
| `learned-routing.ts` | **avoided-Set filter+map 双中间数组（§2 行 2）判融合族三阴性先例 + 落地线拒列（内部零改动，S13-B-1 边界显式遵守）**；**`routingPolicyIdentity` 构造字面量再校验（§2 行 3）判 R12-E 行 2 同族第二位点拒列**；`stableProjectKey`/apply 结果记忆化（§2 行 4）判 X1-1 + S13-B-1 辖区拒列；`routingPolicyContent` pretty-print 参与 contentHash 判 CAS 契约维持；读路径完整性重哈希拒列维持（S8-D-2 同式）；S8-E-1（双载入，勿去重）、S8-E-3、S2-E-6、X1-1 维持；R11-E 载入校验微观/空 avoid 快路径、R12-E 死导出拒列维持 | 无候选 |
| `signals.ts` | `truncate` 记忆化并入 §2 行 4 拒列；pass-1 五 Map / 双 Map.get / verdict 双分派 / 条件 spread 维持既有拒列；两遍结构维持硬不变量；S1-E-1/2/3、S2-E-5/7、S3-E-1/5、S4-E-1/3、S5-E-2/3、S6-E-1/2/5、S7-E-1/2/5、S8-E-2、S9-E-2/3 维持 | 无候选 |
| `auto-loop.ts` | 函数内结构复用普查无新位点（`signals` 数组唯一遍历者各自必要）；三路拼接、filter 双求值、some-探针、顺序读维持既有拒列（§2 行 5）；I/O 契约边全点名维持（S1-E-4/5、S2-E-1、S3-E-2、S4-E-2、S5-E-1/5）；LEARN_EVALUATION_PLAN 双定义维持 R9-E 纯重构拒列 | 无候选 |
| `from-episode.ts` | `routes` Map 无旁路探针（复用普查阴性）；内部字面量再校验 / Date.parse / includes / 谓词序 / seen-Set / 死形参维持 R10-E..R12-E 拒列（§2 行 9）；`applyCascadeRetry` 载荷形状按令未触碰；S2-E-2、S7-E-3、S9-E-1 维持 | 无候选 |
| `task-success.ts` | `copyDefinedBinding`+`present()` 无中间分配可消（条件 spread = S8-E-2 已裁决面）；S9-E-3、S2-E-7 覆盖维持 | 无候选 |
| `diagnostics.ts` | `groups` Map 无旁路探针；`group.map().filter()` 中间数组 = 融合族（S1-E-6 十倍更慢先例）拒列；冗余 Map.set = S3-E-3 维持；`unique`/`mode` 小集合族维持；本轮 diagnose 锚点 1.00–1.40µs（分组路径 diagnoseGroups=1 全数走到） | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | 本轮交叉检索复核仍零生产调用方（仅测试使用）；`clusterSignatures` O(n²) / `BOUNDARY_ORDER.indexOf` 比较器等全部自动拒列；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核（依赖
`pnpm install --frozen-lockfile` 全新安装，pnpm 10.17.1）：

```bash
# Node v22.22.2（nvm；engines >=22.19.0 合规）
pnpm gate        # typecheck ✓ + lint ✓ + test ✓ + build ✓
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.14.0 与 v22.22.2 均过）
```

环境注记：系统默认 node 为 v22.14.0（`/exec-daemon/node` 在 PATH 中遮蔽
nvm），违反 engines `>=22.19.0`；在其上 `pnpm gate` 的
`test/unit/cli/doctor.test.ts`「doctor reports developer preview …」按设计
fail-closed（doctor preflight 以 `process.versions.node` 对照 engines，
1 fail / 1168 pass）——纯环境现象非代码回归，切换 nvm v22.22.2 后全绿。
本报告 gate 结论以 engines 合规版本为准。

锚点脚本（临时置于 `/tmp/r13e-anchor.mts`，未入库以遵守无赢家纪律；
完整差异面见附录 A，seeds `0xe13e01`–`0xe13e03`）共 **24 次独立运行**
（16 × v22.14.0 + 8 × v22.22.2），每次 3/3 sanity 检查全过，全部确定性
VERDICT 行跨 24 次运行**逐位一致**。代表性一次运行（v22.22.2）：

```text
VERDICT fixtures: E=41 collected=14 routedE=41 outcomes=2 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=10.5us outcomes=1.6us diagnose=1.07us bandit-build=0.7us | total in-slice CPU ~13.8us per run vs landing bar >=10000us (724x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=258ns/task | empty policy=112ns/task -> x10 tasks/run = 2.58us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=26.9us | realistic registry (baseline+1 candidate)=85.6us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

计时跨 24 次运行汇总：SLICE-CPU 总量 13.5–15.9µs 稳定（628–738×）；
`applyLearnedRouting` 非空 258–312ns/task、空策略 111–132ns/task 稳定；
`loadLearnedRouting` fresh 24.6–42.4µs、现实 registry 76.1–88.6µs 稳定
（+1 次 126.3µs 首运行冷缓存离群）。本轮无候选立项，故无败者仿真；无
新增等价性仿真脚本入库（未提交 `scripts/round13-r13e-equivalence-sim.ts`）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐形态有理由；第十三遍以「函数内已建结构
  复用 × 中间分配普查」被调侧透镜新探的三个结构位置 + 记忆化整族清点全部
  落进已关闭机制类或落地线算术封死区，未发现第十个机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令封死的
  I/O 契约面上；CPU 面即使清零也距门槛 628–738×，最保守合成口径 ≥409×）。
- 矩阵空洞：无（调用位点行号与 R8-E..R12-E 逐一相同；配置态 live 面与
  载入面锚点全数复测在带内；本轮加测同 VM 双 Node 版本交叉，版本差异
  不可分辨）。
- S13-B-1 边界：`applyLearnedRouting` 本体零改动，重开条件（本体成本增长
  ≥2 个量级）未触碰亦未实现；本切片未铸任何与其平行的 learned-routing
  记忆化 ID。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R12-E §6：SLICE-CPU 锚点失效（全切片 CPU
增长 ≥2 个量级，本轮复核值 13.5–15.9µs、最保守合成口径 ~21.7–24.4µs）或
任一 I/O 契约排除（X0-3 / S2-E-1/4 / S1-G-1 / S1-E-4/5 / S4-E-2 / S5-E-5 /
S6-E-3/4 / S8-E-1）被正式推翻，或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

锚点脚本全文（`/tmp/r13e-anchor.mts`，`npx tsx /tmp/r13e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下四处替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R13-E；
`NOW = "2026-08-24T21:30:00.000Z"`；SECTION 1 的 seeds
`0xe13e01`/`0xe13e02`/`0xe13e03`；SECTION 2 的 `mkdtemp` 前缀
`r13e-fresh-`/`r13e-reg-` 与 detector identity `r13e-anchor`。其余逐字同
R10-E 附录 A（含 `genCollectEvents` / `genRoutedEvents` / `genSignal` /
`banditBuild` 四个生成器与全部 BENCH/VERDICT 逻辑），输出行格式与本报告
§1/§4 完全一致。

24 次运行原始区间（16 × v22.14.0 + 8 × v22.22.2）：

```text
SLICE-CPU 总量:
  v22.14.0 批一: 14.7 / 13.8 / 13.6 / 14.0 / 13.8 / 15.9 / 13.8 / 14.3 us
  v22.14.0 批二: 14.0 / 14.1 / 13.5 / 13.6 / 13.6 / 13.8 / 13.7 / 14.0 us
  v22.22.2:      13.8 / 13.7 / 13.8 / 13.6 / 13.9 / 13.9 / 14.0 / 13.8 us
collect:    10.2-12.4 us（12.4 为批一 run 6 单次；其余 10.2-10.9）
outcomes:   1.5-1.7 us
diagnose:   1.00-1.40 us（1.40 为批一 run 1 首次；其余 1.00-1.20）
bandit:     0.7-0.9 us
apply(non): 258-312 ns/task（312 为批一 run 7 单次；其余 258-287）
apply(空):  111-132 ns/task
load fresh: 24.6-42.4 us
load real:  76.1-88.6 us（+1 次 126.3us：批一 run 1，registry 写盘后
            首次测量的冷缓存离群，24 次中仅此一次）
VERDICT 行: 24/24 逐位一致；sanity 3/3 x24 全过
```
