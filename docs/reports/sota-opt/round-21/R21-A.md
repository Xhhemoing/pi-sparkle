MODEL_SLUG=claude-fable-5-thinking-xhigh

# R21-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第二十一遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 21 / R21-A（Round 1–20 同区第二十一遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `d4e2273`（含 R20-H/I/J 空枚举收口、Round 20 关轮 `da13bc6` 与禁单补记）
**分支:** `cursor/r21-a-tracking-twenty-first-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S21-A-\* ID，切片零代码改动；
整片预算收口第十九次复核成立，并新增一条实测轴——「H 输入面
（humanInput）定价」补上公开 `runTrackingTurn` API 四个输入参数中最后
一个从未定价的参数：window 的驻留态面由 R20-A（轴 9a/9b：M / minor-text /
K 链）收口，prescoreInput 的规模与载荷面由 R14-A（E）/ R15-A（C/R）/
R16-A（L/V/S）收口，gateFacts 的布尔覆写面由锚点矩阵 + R18-A skip +
R19-A 结局格收口——而 humanInput 在二十遍搜查史上只被测过空输入短路
（R13-A turn 剖面 `human=12–14 ns`，即 `extractHumanScore({})` 的
trim-空串早退），其活跃臂（userText 文本梯子、CountableList ratio 臂、
H 参与 combineScore 混合、H 触发的三条门腿）从未有过实测价。本轮给出
类格、字节斜率 H_L、匹配基数 H_m、清单基数 H_N、H-active 整 turn 与
写侧 parse 变体差五组定价。该面生产流量为零**由构造钉死**
（from-child.ts:84 恒传 `humanInput: {}` 且 from-child window 无
userText ⇒ `turn.human ≡ {kind:"unobserved"}`——S7-A-1 常量管道引理，
本轮 F1a 700/700 分布级再证），但**契约存活**（userText/list 是公开
API 输入；产出的 HumanSignal 原样进入 TRACKING_ASSESSMENT payload，
四变体 parse 合法且对 hashAssessment 逐位不可见——继 R16-A V 轴与
R20-A M 轴之后第三个「哈希双旁路」载荷域家族成员）。轴上全部角度
经换名检查判为既有排除 ID 的本体或换位点（2 项新记录，见 §0），
**只产出归档证人与数据补充，未产出可落地候选**——生产零流量 +
仅幻想规模越线（文本 ~1.2 MB / 清单 ~4.7×10⁴ 项）按令即空收口。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R20-A 至本轮基线
（`d4e2273`）**逐字节未变**（`git diff 7acb666..d4e2273 -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，二十一遍全程零
diff；清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2）并全部
实际重读）。调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..d4e2273 -- src/` **完全为空**（跨 R10…R21 开轮十二段
全部为 docs 提交），生产调用方经本轮全库 grep 复核仍为
`supervisor.ts:483`（applyTrackingGate/nextTrackingSeq；两个 settle
调用点 601/711 均不传 trackingAssessment ⇒ 生产 O(1) no-op，第十二次
复验）/ `coordinator.ts:444` / `flowchart-run.ts:320`
（applyChildThreeLine），每子结果一次（~5 次/run），事件表几十级（41）。
R20-A 的 ~64–80 µs/run 预算天花板经本轮在当前 VM（Node v22.22.2；本 VM
默认 v22.14.0 且 `/exec-daemon/node` 垫片遮蔽 nvm，按 engines >=22.19.0
经 PATH 前置 nvm v22.22.2）实测复核为 **~66–95 µs/run**（五格锚点矩阵
三次独立运行 13.2–19.1 µs/gate；上沿 95 由 run1 no-contract 首格单点
19.1 拉出——R11-A 以来「PIC/基准次序」现象在本轮 F1b/F1c 多形态
assessment 预热后更明显，除该孤点外三次运行为 13.2–16.2 µs/gate ⇒
~66–81 µs/run，与 R20-A 64–80 同带；单格横向比较不具意义；量级结论
不变——稳态下即使切片零成本，节省上界 ~0.07–0.10 ms/run，仍比落地线
低约三个量级）。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
数据补充（2 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项 +
R15-A 5 项 + R16-A 4 项 + R17-A 4 项 + R18-A 4 项 + R19-A 2 项 +
R20-A 3 项换名检查全部复核维持，未追认任何 S12…S20-A-\* ID），按令
一律不铸新 ID。作为空枚举的实证支撑，本轮新落档一组数据（§1，三次
独立运行稳定；fuzz 计数三次逐位一致）：

**H 输入面定价（axis 10）**：`extractHumanScore` 类格呈**双峰结构**——
三条廉价臂（空输入短路 17–24 ns、锚定前缀早退 short-confirm 34–42 /
requirement-only 65–71 ns、list ratio 臂 54–60 ns）对**文本梯子地板
0.62–0.74 µs**（凡通过两个锚定前缀测试的非空文本，均须支付
extractTenPoint 每调用 2× `new RegExp` 构造 189–191 ns——R14-A §0-2 /
X0-6 本体的首次隔离定价，占地板 ~26–30%——加 2× matchAll 与至多 3 个
短规则非锚定扫描）。**H_L 字节斜率 1.65–1.66 ns/char 线性无悬崖**
（no-match 散文 64B→256KB 四格），10 ms/run 穿越点 userText ≈
**1.2 MB**；digit-dense 最坏形态 3.06–3.18×（5.2–5.4 ns/char，穿越
~371–383 KB）。**H_m 匹配基数 ~85.7–91.7 ns/match**（fen-dense
m ∈ {2,64,1024}；>1 个分数标记先全量收集后按歧义拒绝塌缩为
unobserved——F1d 钉死），穿越 m ≈ 2.2–2.3×10⁴。**H_N 清单基数
42.2–43.0 ns/item**（ratio 臂 N ∈ {3,100,1000,10000}），穿越
N ≈ 46,542–47,356——与 R20-A M 轴（34.4–43.2 ns/minor）同带，
「每项对象扫描」价的第二个独立证人。**H-active 整 turn 全部留在
turn 带内无悬崖**（生产 1823–1992 / no-match 1KB 4409 上界 /
ten-point 软门 2716 / whole-reject 硬门 2680 / list-ratio 硬门
1948 ns——list 臂不付文本梯子，与生产格同价）。写侧
parseHumanSignal 四变体差为常数 ≤~40 ns（固定字段，无规模维）。
契约证人：F1b 四变体哈希逐位不变 + parse 往返保形 + e2e 追加形态
与生产逐位同构（+1 TA、payload 按引用携带变体）；F1c 11 类提取表 +
ratio 对文本的优先权 + 混合公式回声（obvious-problem 时 score ≡
round(0.7·min(H,P)+0.3·max(H,P))，mark≥8 时 P-恒等）+ **H 触发三门腿
e2e 形态**（whole-reject ⇒ hard/user-reject-stop/askUser ⇒ wait_user
+3；security 未同意项 ⇒ hard/permission-security-reject ⇒
queue_analysis +3；低分「3分」⇒ score 0.51 < 0.55 ⇒
soft/soft-threshold ⇒ queue_analysis——**只验形态不重定价**，directive
定价维持 R19-A 7.85–8.57 µs 带，不补铸）；F1d 歧义拒绝面
（>1 slash / >1 fen / slash-fen 冲突 / 越界 11分与 13.5/10 全部塌缩
unobserved，一致时接受）。

R12-A 分解的五大组件占比（hashAssessment×3 ~38–45% / validateEvent
~19–22% / turn ~12–16% / prescore×2 ~9–14% / O(E) 五扫描+拷贝
~1.8–2.1%）在本 VM 逐一复现（§1 表），R18-A skip 定价与 R19-A replay
定价抽测复现同带（guard-3 skip 1961–2583 ns = 13.0–19.5% of apply，
run3 上沿为单次抖动、组成结论不变；replay find#1 3057–3416 ns =
20.1–25.8% of apply）。第二十一遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；未触碰在飞的
  R21-B / R21-C 分支与文件，未触碰已收口的 R20-\* 报告。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表 545 行，含
  S13-B-1；R12-A…R20-A 空枚举未铸 ID，确认）/ PROGRESS.md /
  round-21/PLAN.md / round-20/R20-A.md / round-19/R19-A.md /
  round-18/R18-A.md。EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md /
  README.md 零触碰。
- 环境：本 VM 默认 Node v22.14.0（`/exec-daemon/node` 垫片遮蔽 nvm
  的 `nvm use`），按 engines（>=22.19.0）以 PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 切至 v22.22.2 后
  `pnpm install --frozen-lockfile`（pnpm 10.17.1），全部测量在
  v22.22.2 完成。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或数据补充，**一律不铸 S21-A-\* ID**。R12-A 13 项、R13-A 8 项、
  R14-A 3 项、R15-A 5 项、R16-A 4 项、R17-A 4 项、R18-A 4 项、R19-A
  2 项、R20-A 3 项换名检查全部复核维持，不再逐条重列；以下 2 项为
  本轮**新记录**）：
  1. **extractHumanScore 文本梯子五扫描合并单遍组合正则**（把
     NEGATION / TEN_POINT_SLASH / TEN_POINT_FEN / 三个短规则的非锚定
     扫描合并为一个多分支正则或手写单遍扫描，摊平 0.62–0.74 µs
     地板）——= S6-E-2（E 区 scoreUserAnswer 双正则合并：廉价形式
     负向优先 vs 首位置匹配发散、忠实 matchAll 形式慢 3–12×）的
     跨切片同族 + 本切片的优先序契约可观察（分数类先于短规则类、
     operation-reject > named-error-continue > whole-reject 的梯子
     次序、以及「>1 匹配先收集后拒绝」的歧义语义——合并后须在单遍中
     复刻全部优先序与计数语义，廉价形式不等价）；且生产零流量
     （F1a：humanInput ≡ {}），拒列；
  2. **extractRatio agreedIds Set 削减/计数器化**（N 小时以数组直扫
     或双向计数替代 `new Set(agreedIds)` + 逐项 has）= S1-A-4/X4-7
     小集合 Set 家族的 ratio 臂换位点；42.2–43.0 ns/item 斜率只存在
     于假想 N 上（现实验收清单个位数，N=3 格全程 54–60 ns），拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  extractTenPoint 每调用 2× RegExp 构造提升/缓存 = R14-A §0-2 本体 =
  X0-6（/g lastIndex 状态风险；本轮首次隔离定价 189–191 ns/调用，
  纯归因数据补充）；no-match 预探针/matchAll 早退 = S1-A-3 本体
  （「早退/惰性『分』匹配」；hit 侧省 miss 侧付探针税 = S7-E-2 恒等
  快路径同型）；文本梯子按类频次重排 = R5-A 频次重排本体（R13-A
  已录维持）；from-child 路径 human 管道常量折叠 = S7-A-1 本体
  （F1a 即其分布级证人）；按 text 记忆化 extractHumanScore = X1-1
  隐藏缓存本体；combineScore 混合代数重排 = S7-A-4 本体（舍入后
  可观察发散）；H 变体进哈希面 = 哈希契约变更（X1-2 面，R16-A
  哈希域清单归档维持）；schema 加 userText/list 上限 = X0-7 类
  阈值/schema 面功能变更；第二次 prescore = S1-A-2；幂等双 find/
  死扫描 = S8-A-1 / S11-A-2；写侧 validateEvent = S4-G-2 族（实现在
  events.ts，G 切片）；`every×includes` Set 化 = S1-A-4/X4-7
  （C/R ≈ 10³ 重开条件，R15-A 轴 1）；turn 恒定输入常量折叠 =
  R12-A §0-11 伞 + R16-A §0-2；skip 守卫次序/享元/prescore 复用 =
  R18-A §0-1/2/4 维持；replay 快路径/重算去重 = R19-A §0-1/2 维持；
  hashSummary 缓存/增量与 with-previous 管道融合 = R20-A §0-2/3
  维持。
- 未重开任何 X\* / S1-\* … S20-\* 条目。R20-A 两轴（M 基数、驻留链
  K）**不补铸、不重测**（其 gate.openMinors / minor-text /
  window.previous 数据按令转移）；R19-A 两轴（重投递面、wait_user
  指令类）只做复核所需的抽测（replay find#1 一格同带复现，见 §1；
  wait_user 格**不重定价**——F1c 三门腿只验 e2e 形态并引用 R19-A
  带）；R18-A 两轴（skip 组成、拒绝终点普查）只抽测 guard-3 skip
  一格；R17-A…R14-A 各轴不补铸、不重测超出复核所需的部分；
  S11-A-1 / S11-A-2 / S10-A-1 / S9-A-1 / S8-A-1..3 / S7-A-1..4 /
  X0-4/X2-4 / X0-6 / S6-A-1 专项复核：本轮零触碰、零重提，全部维持
  关闭。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足——F1c 的混合公式/门腿证人是对既有
  阈值与门控行为的**回声验证**，未改任何阈值、未改任何测试；
  「H/score 不写路由」在本轮 F1b/F1c 的 e2e 形态中逐位可见
  （H 变体只进 TA payload 与 gate 映射，不触任何路由面）。不声称
  Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。无赢家 ⇒
  零生产改动，未提交任何仿真脚本（按令不建
  `scripts/round21-r21a-equivalence-sim.ts`），仿真全文见本报告附录。
  不开 PR。

## 1. 预算支配论证复核 + 一条新轴（方法第 1、2 步）

R3-A §3 … R20-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..d4e2273 -- <切片>` 为空，逐字节
   一致（二十一遍全程零 diff）——R1-A…R20-A 全部测量与排除按令转移，
   但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨十二段）**：`git diff 89f0455..d4e2273 --
   src/` **完全为空**（R10 开轮以来全部提交均在 docs/ 下）。生产调用方
   三点位（supervisor.ts:483 / coordinator.ts:444 /
   flowchart-run.ts:320）、settle 调用点 601/711 不传 assessment
   （首行 `if (assessment === undefined) return`）、
   `parseTrackingAssessment` 唯一 src/ 调用方 events.ts:738、
   `runTrackingTurn` 唯一 src/ 调用方 from-child.ts:81（humanInput 恒
   `{}`，window 无 userText ⇒ H 面生产零流量）、`extractHumanScore` /
   `hashSummary` 切片外零 src/ 生产调用方、isolation.ts 双函数与
   `executionAuthority` 零 src/ 生产调用方，经本轮全库 grep 全部复验，
   无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               19.1      15.5      15.3  µs/gate (+1 event)
PASSED x contract(3)               15.4      13.6      13.2  µs/gate (+1 event)
FAILED x no-contract               15.5      16.0      16.2  µs/gate (+3 events)
FAILED x contract(3)               13.8      13.8      14.0  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.5      14.5      14.8  µs/gate (+3 events)
=> ~66–95 µs/run（稳态热；除 run1 首格 19.1 孤点外 ~66–81）
```

与 R20-A（12.9–16.1 µs/gate ⇒ 64–80 µs/run）同带略宽；五格三次运行
**无悬崖、无空洞**。run1 no-contract 首格 19.1 是 R11-A 以来
「PIC/基准次序」现象的本轮形态（本轮仿真在预热前多驱动了 F1b/F1c 的
四变体与三门腿 assessment 形状，PIC 污染更重；run2/3 同格回落
15.3–15.5），单格横向比较不具意义。稳态下即使把整个切片优化到零成本，
节省上界 ~0.07–0.10 ms/run，仍比战役落地线（数十~数百 ms 或复杂度类
下降）低**约三个量级**。**支配论证（稳态口径）第十九次复核成立**；
生产冷态口径维持 R17-A 轴 6b 裁决（[2.7, 8.5] ms/run once-per-run，
不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R20-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R20-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1936–1973 ns 每次 ⇒ **5.81–5.92 µs** | 5.88–5.91 µs | ~38–45% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 298–353 ns + 第三次 hash） | **2.91 µs** | 2.91–2.96 µs | ~19–22% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.82–2.10 µs** | 1.84–1.98 µs | ~12–16% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 700–911 ns 每次 ⇒ **1.40–1.82 µs** | 1.41–1.67 µs | ~9–14% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 76–78 + seq 46 + 双幂等 find 38–40×2 + status 50–51 + `[...events]` 26–28 ns） | **278–280 ns** | 282–287 ns | **~1.8–2.1%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以当轮 contract 格（13.2–15.4 µs）计。`applyTrackingGate` 整体
隔离实测 5.77–5.90 µs，与 R20-A（5.74–6.20）同带。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次运行区间）：guard-3 skip（FAILED 零 evidence refs）1961–2583 ns
= 13.0–19.5% of apply（R20-A 13.0–14.2%；run3 的 2583/19.5% 为单次
抖动上沿，run1/2 为 1961–1997，skip 仍严格便宜于 apply，组成结论
不变）；replay find#1（GT idempotencyKey 命中，E=44）3057–3416 ns =
20.1–25.8% of apply（R20-A 20.7–25.6% 同带）——组成天花板
{apply, skip, replay} 两个半边继续成立，不重测其余格。

**本轮新轴 10：H 输入面（humanInput：文本梯子 + ratio 臂 + H-active
turn + 写侧变体）**（extractHumanScore 隔离 + runTrackingTurn 公开面，
三次运行区间）：

```text
类格（extractHumanScore 隔离）:
  empty {}（生产短路，R13-A 连续性）            17–24 ns
  short-confirm（'ok'，锚定早退）               34–42 ns
  requirement-only（锚定早退）                  65–71 ns
  list ratio N=3（ratio 臂，跳过全部正则）      54–60 ns
  ---- 文本梯子地板（通过两个锚定前缀测试即支付） ----
  ten-point slash（'8/10'）                     648–670 ns
  ten-point fen（'这次给 3分'）                 660–679 ns
  operation-reject                              664–682 ns
  named-error-continue                          637–651 ns
  whole-reject（'回滚'）                        621–635 ns
  no-match 44B 散文（走完全梯子）               666–736 ns
  归因: extractTenPoint 每调用 2× new RegExp 构造 189–191 ns
        （= 地板 ~26–30%；R14-A §0-2 / X0-6 本体，拒列不动）

H_L 字节斜率（no-match 散文，10 ms/run 预算 ⇒ 每 turn 2 ms）:
  64B 0.71–0.73 | 1KB 2.32–2.38 | 16KB 27.80–27.96 |
  256KB 434.18–436.04 µs
  => 1.65–1.66 ns/char 线性无悬崖；穿越点 userText ≈ 1.2 MB
  digit-dense 16KB 85.52–88.36 µs = 3.06–3.18×（5.2–5.4 ns/char，
  穿越 ~371–383 KB）——两个梯子正则的回溯压力形态，仍线性类

H_m 匹配基数（fen-dense，>1 标记先收集后歧义拒绝）:
  m=2 0.84–0.90 | m=64 6.35–6.82 | m=1024 88.46–94.63 µs
  => ~85.7–91.7 ns/match；穿越 m ≈ 2.2–2.3×10⁴

H_N 清单基数（ratio 臂）:
  N=3 0.10–0.11 | 100 1.91–1.97 | 1000 20.08–21.20 |
  10000 422.29–429.67 µs
  => 42.2–43.0 ns/item；穿越 N ≈ 46,542–47,356
     （与 R20-A M 轴 34.4–43.2 ns/minor 同带——「每项对象扫描」价
      的第二个独立证人）

H-active 整 turn（runTrackingTurn 公开面）:
  生产 {}（短路）1823–1992 | no-match 1KB 4208–4409 |
  ten-point '3分'（软门）2579–2716 | whole-reject（硬门）2547–2680 |
  list ratio N=3（安全硬门）1804–1948 ns
  => 全部留在 turn 带内；最大增量 +2.3–2.4 µs（1KB 散文）；
     list 臂不付文本梯子，与生产格同价

写侧 parseHumanSignal 变体差（parseTrackingAssessment 整体）:
  unobserved 301–317 | ratio 315–344 | ten-point 306–315 |
  short-rule 314–336 ns => 常数 ≤~40 ns，固定字段无规模维

生产零流量引理（F1a）: from-child.ts:84 恒 humanInput {} 且 window 无
  userText ⇒ 700/700 applied assessment 与 turn.human 全部
  {kind:"unobserved"}（S7-A-1 分布级再证，三次逐位一致）
契约存活面（F1b）: 四变体 hashAssessment 逐位不变（哈希双旁路，
  继 V/M 后第三例）；JSON 往返 parse 保形；e2e 追加形态与生产逐位
  同构（+1 TA，payload 按引用携带变体）
语义回声（F1c/F1d，三次逐位一致）: 11 类提取表精确；ratio 对文本
  优先权；混合公式 score ≡ round(0.7·min(H,P)+0.3·max(H,P))
  （obvious-problem 时）/ P-恒等（mark≥8）；H 触发三门腿 e2e 形态
  （whole-reject ⇒ wait_user +3 / security 拒 ⇒ queue_analysis +3 /
  '3分' ⇒ 0.51 < 0.55 ⇒ soft-threshold ⇒ queue_analysis——只验形态，
  定价维持 R19-A）；歧义面（>1 slash / >1 fen / 冲突 / 越界）全部
  塌缩 unobserved，一致时接受
```

五个结论：

1. **turn 输入参数格网闭合**：runTrackingTurn 的四个输入参数
   （window / prescoreInput / gateFacts / humanInput）自此全部有实测
   价——R20-A 收口的是驻留状态维（M/minor-text/K），本轮收口的是
   最后一个参数整面。R16-A 输入维度实测覆盖自此扩为
   {E, C, R, V, S, L, codes, G} ∪ {M, minor-text, K} ∪
   {H_L, H_m, H_N}——后五者全部是「契约存活但生产恒零」维。
2. **双峰结构 + 地板承载项唯一**：文本梯子地板 0.62–0.74 µs 的
   ~26–30% 是 extractTenPoint 每调用 2× RegExp 构造（R14-A §0-2 /
   X0-6 本体），其余为 5 个非锚定扫描；「摊平地板」的两条路
   （构造提升、单遍合并）分别撞 X0-6 与 S6-E-2 同族 + 优先序契约
   （§0-1），全部拒列。
3. **无规模乘数、无悬崖**：H_L 线性 1.65–1.66 ns/char（digit-dense
   3.06–3.18× 仍线性），H_m ~86–92 ns/match，H_N 42–43 ns/item；
   三个穿越点（~1.2 MB / ~2.2×10⁴ / ~4.7×10⁴）分别比任何现实用户
   消息/评分/验收清单高约 4–5、3–4、4 个量级。
4. **H 触发臂与结局格接线一致**：三门腿把 H 面接入 R19-A 的
   directive 格（wait_user / queue_analysis ×2）与 R18-A 的组成
   天花板——H-active turn 最贵 4.4 µs，加上 R19-A wait_user 格
   7.9–8.6 µs 仍在既有带内，组成结论不受 H 面影响。
5. **不产生候选**：单遍合并 = §0-1；ratio Set 削减 = §0-2；RegExp
   构造提升 = R14-A §0-2 / X0-6；预探针 = S1-A-3 / S7-E-2；频次
   重排 = R5-A；常量折叠 = S7-A-1；记忆化 = X1-1；哈希面/schema
   面 = X1-2 / X0-7。生产零流量 + 幻想规模越线 ⇒ 按令空收口。

**重开条件全景更新（在 R20-A §1 表基础上新增三行）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| M（openMinors 条数） | 0 | ≈ 4.6–5.8×10⁴ | ~4+ | R20-A |
| minor-text（字节） | 0 | 无穿越点（结构性零斜率） | — | R20-A |
| K（驻留链 turn 数） | 0（恒 fresh） | ≈ 3×10³ turn | ~2 | R20-A |
| **H_L（userText 字节）** | **0（恒 undefined）** | **≈ 1.2 MB 散文 / 371–383 KB digit-dense** | **~4–5** | **本轮** |
| **H_m（分数标记数）** | **0** | **≈ 2.2–2.3×10⁴** | **~3–4** | **本轮** |
| **H_N（list 项数）** | **0（恒无 list）** | **≈ 4.65–4.74×10⁴** | **~4** | **本轮** |
| G（gates/run，caller 侧） | ~5 | ≈ 654–763 | ~2.1 | R16-A 锚点算术 |
| G 组成（apply/skip/replay 全混合） | 全 apply | 混入只降不升 | — | R18-A + R19-A |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–13.3 µs/fault | — | R18-A |
| 重投递面（once-per-redelivery） | 0 次/run | ~2.8–3.4 µs/次 | — | R19-A + 本轮抽测 |

C 轴（~2.5 个量级，R15-A）仍是切片内部最近的规模悬崖，第一优先重开
对象不变（S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）。本轮关闭的
是**输入参数维**：R14–R16 关规模/载荷维、R17 关组成/温度维、R18 关
组成/故障维、R19 关结局维、R20 关驻留状态维之后，公开 turn 面的
输入参数格网（window / prescoreInput / gateFacts / humanInput）自此
**无一参数无价**。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 2 项 + R12-A…R20-A
46 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、数据
补充或本体，按「换名不铸 ID」纪律未列任何 S21-A-\* 候选。空枚举与
R12-A…R20-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第二十一遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `human-score.ts` | **H 面首次定价宿主**（axis 10：类格双峰——三廉价臂 17–71 ns vs 文本梯子地板 0.62–0.74 µs；H_L 1.65–1.66 ns/char 无悬崖、digit-dense 3.06–3.18×；H_m ~86–92 ns/match 且歧义拒绝先收集后塌缩 F1d；H_N 42–43 ns/item）；**地板归因**（2× 每调用 RegExp 构造 189–191 ns = R14-A §0-2 / X0-6 本体首次隔离定价，拒列不动）；单遍合并 = §0-1（S6-E-2 同族 + 优先序契约）；ratio Set 削减 = §0-2；预探针 = S1-A-3 本体；频次重排 = R5-A 本体；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 维持 | 无新候选 |
| `turn.ts` | **H-active turn 定价**（生产 1823–1992 vs 最贵 H 形态 4409 ns，全带内；userText 双源合并 `humanInput.userText ?? window.userText` 单 trim 无双算）；H 触发臂接线证人（userRejectStop/safetyRejected 从 human 变体派生，F1c 三门腿）；R13-A 八子组件剖面维持（本轮整体 1.82–2.10 µs 同带）；R20-A §0-3 with-previous 管道维持；R12-A §0-11 / R16-A §0-2 维持 | 无新候选 |
| `combined-score.ts` | **混合公式回声证人**（F1c：obvious-problem 时 score ≡ round(0.7·min+0.3·max) 精确、mark≥8 时 P-恒等——首次以 H-active 输入驱动该臂）；S7-A-4 代数重排 / S6-A-1 舍入 / TypeError 防御纵深维持 | 无新候选 |
| `types.ts` | **H 变体哈希双旁路证人**（F1b：hashAssessment 对 human 四变体逐位不变——继 V（R16-A）/M（R20-A）后第三例，R16-A 哈希域清单归档的 H 行首次实测）；写侧 parseHumanSignal 四变体差 ≤~40 ns 常数（固定字段无规模维）；R20-A M 轴 / R16-A L/V/S / S2-A-5 / X1-2 / S5-H-3 / S1-A-8 / X0-5 维持 | 无新候选 |
| `gates.ts` | H 触发三门腿分布级证人（F1c：user-reject-stop 硬门 askUser=true / permission-security-reject 硬门 / soft-threshold 软门经 H 分数下沉触发——门本体零改动零重提）；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 维持 | 无新候选 |
| `gate-apply.ts` | H 变体 e2e 纯性宿主（F1b：+1 TA 形态对变体逐位同构；F1c 三门腿 +3 形态只验形态不重定价，R19-A directive 格维持）；replay find#1 抽测 3057–3416 ns 同带复现（R19-A 8a 维持）；S11-A-2 / S8-A-1 / S2-A-4 / R15-A §0-1 维持 | 无新候选 |
| `child-tracking.ts` | guard-3 skip 抽测 1961–2583 ns 复现（R18-A 7a 维持；run3 上沿为抖动，skip < apply 不变）；skip 守卫次序/享元 = R18-A §0-1/2 维持；seq 单调性引理 = R19-A 维持 | 无新候选 |
| `from-child.ts` | **H 面生产零流量根源宿主**（F1a 700/700：from-child.ts:84 恒 `humanInput: {}` 且 window 字面量无 userText ⇒ assessment.human 恒 unobserved——axis 10 零流量引理即本文件；S7-A-1 常量管道本体维持）；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `prescore.ts` | H 面不触 prescore（extractHumanScore 与 computePrescore 正交，F1c 门腿中 P 恒由 prescore 独立算出——「H 不写 P」规格回声）；C/R 轴维持 R15-A 穿越点；S1-A-4 / X4-7 / S6-A-1 / S8-A-3 / R12-A §0-2..5 维持 | 无新候选 |
| `roller.ts` | H 变体经 summary.human 原样携带（rollSummary 不触 H 内容——hashSummary 亦不序列化 human，R16-A 哈希域清单维持）；R20-A previous 面 / R17-A O(Q·D) / S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `config.ts` | softThreshold 0.55 冻结面（F1c leg3 的 0.51 < 0.55 是回声证人，零触碰）；applyUserThreshold throw 终点归类维持（R18-A 7b）；X0-7 维持 | 无新候选 |
| `analysis.ts` | S8-A-2 维持（零 src/ 生产调用方 + sanitize choke point，本轮 grep 复验；AnomalyPacket.H 字段由 humanScoreValue 派生——H 面对 analysis 的唯一投影，wakeAnalysis 生产触发臂已由既有格覆盖） | 无新候选 |
| `isolation.ts` / `index.ts` | O(1) 恒等函数 / 纯再导出；`executionAuthority` 与 isolation 双函数零 src/ 生产调用方复验；extractHumanScore/hasObviousHumanProblem/humanScoreValue 经 index.ts 导出 = axis 10 契约存活面的载体；惰性 import = R12-A §0-10 维持 | 无新候选 |

**对 R20-A 声明的裁决**：驻留状态维（M + K）收口在其维度上成立；
R20-A 结语「输入格、结局格、状态格三张格网无一格无价」的量化边界是
「生产流量非零或驻留态」维——humanInput 作为公开 turn 面的第四个
输入参数不在其中，本轮补上。两处新数据**全部只产出归档证人与既有
拒绝记录的数据补充，未产出可落地候选**。收口的预言力第二十一遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R20-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F1a: 700 trials, 700 applied, all human={kind:"unobserved"} (turn too)
F1b: 4 variants hash-invariant, parse round-trip preserved, e2e +1 TA
F1c: 11-class table exact; ratio precedence; blend formula echo;
     3 gate legs e2e (wait_user / queue_analysis x2, shape only)
F1d: >1 slash / >1 fen / conflict / out-of-range all unobserved
anchor: 19.1 / 15.4 / 15.5 / 13.8 / 14.5 µs/gate => ~69–95 µs/run
decomposition: hash×3=5820ns | validateEvent=2914ns | turn=2097ns
    | prescore×2=1822ns | 5 scans+copy=280ns | applyTrackingGate=5.89µs
spot: skip guard-3 1997ns (13.0%) | replay find#1 3088ns (20.1%)
axis10 class: {}=19 | confirm=42 | req-only=68 | slash=648 | fen=662
    | op-rej=681 | named=643 | whole=621 | no-match44B=670 | listN3=57 ns
    | 2×RegExp ctor=190ns
axis10 H_L: 64B/1K/16K/256K: 0.71/2.35/27.96/436.04 µs
    => 1661.1 ps/char; crossing ~1.2 MB; digit-dense 16K 85.52µs (×3.06)
axis10 H_m: m=2/64/1024: 0.84/6.35/88.46 µs
axis10 H_N: N=3/100/1K/10K: 0.11/1.96/21.12/429.67 µs
    => 43.0 ns/item; crossing N ≈ 46,542
axis10 turn: prod 1992 | no-match1K 4409 | fen 2716 | whole 2680
    | list 1948 ns
axis10 parse variants: 317/344/312/334 ns
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F1a/F1b/F1c/F1d: 计数逐位同 run 1
anchor: 15.5 / 13.6 / 16.0 / 13.8 / 14.5 µs/gate => ~68–80 µs/run
decomposition: hash×3=5807ns | validateEvent=2908ns | turn=1823ns
    | prescore×2=1401ns | 5 scans+copy=278ns | applyTrackingGate=5.77µs
spot: skip guard-3 1961ns (14.4%) | replay find#1 3057ns (22.5%)
axis10 class: {}=17 | confirm=34 | req-only=65 | slash=650 | fen=660
    | op-rej=664 | named=637 | whole=623 | no-match44B=666 | listN3=54 ns
    | 2×RegExp ctor=189ns
axis10 H_L: 0.71/2.32/27.80/434.76 µs => 1656.2 ps/char;
    crossing ~1.2 MB; digit-dense 16K 88.36µs (×3.18)
axis10 H_m: 0.84/6.82/94.63 µs
axis10 H_N: 0.11/1.91/20.08/422.29 µs => 42.2 ns/item;
    crossing N ≈ 47,356
axis10 turn: prod 1833 | no-match1K 4208 | fen 2579 | whole 2547
    | list 1804 ns
axis10 parse variants: 301/328/306/314 ns
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F1a/F1b/F1c/F1d: 计数逐位同 run 1
anchor: 15.3 / 13.2 / 16.2 / 14.0 / 14.8 µs/gate => ~66–81 µs/run
decomposition: hash×3=5919ns | validateEvent=2908ns | turn=1889ns
    | prescore×2=1417ns | 5 scans+copy=280ns | applyTrackingGate=5.90µs
spot: skip guard-3 2583ns (19.5%) | replay find#1 3416ns (25.8%)
axis10 class: {}=24 | confirm=39 | req-only=71 | slash=670 | fen=679
    | op-rej=682 | named=651 | whole=635 | no-match44B=736 | listN3=60 ns
    | 2×RegExp ctor=191ns
axis10 H_L: 0.73/2.38/27.88/434.18 µs => 1653.9 ps/char;
    crossing ~1.2 MB; digit-dense 16K 87.75µs (×3.15)
axis10 H_m: 0.90/6.40/88.99 µs
axis10 H_N: 0.10/1.97/21.20/424.74 µs => 42.5 ns/item;
    crossing N ≈ 47,083
axis10 turn: prod 1823 | no-match1K 4275 | fen 2589 | whole 2572
    | list 1832 ns
axis10 parse variants: 302/315/315/336 ns
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立仿真运行 F1a/F1b/F1c/F1d 计数逐位一致（700 trial 全 applied、
四变体哈希/往返/追加形态、11 类提取表、三门腿 e2e、歧义五格确定性
重现）；锚点五格三次同带无悬崖（run1 首格孤点已归因 PIC/次序）；
axis 10 十格类格 + 四格 H_L + 三格 H_m + 四格 H_N + 五格 turn +
四格写侧变体三次运行区间紧。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S21-A-\*）。换名检查 2 项新记录见 §0（1–2），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件本轮**新增一条收口**：

**H 输入面（H_L / H_m / H_N 三维）收口**：公开 runTrackingTurn 的
第四个输入参数自此有实测价——文本梯子地板 0.62–0.74 µs/调用（2×
RegExp 构造承载 ~26–30%）、H_L 1.65–1.66 ns/char（穿越 ~1.2 MB；
digit-dense 最坏 3.06–3.18×，穿越 ~371–383 KB）、H_m ~86–92 ns/match
（穿越 ~2.2–2.3×10⁴）、H_N 42–43 ns/item（穿越 ~4.65–4.74×10⁴）、
H-active turn 上界 +2.3–2.4 µs、写侧变体差 ≤~40 ns 常数。重开条件：
(a) 任何生产调用方开始向 runTrackingTurn / from-child 传非空
humanInput（用户反馈接线，届时最先越线的是文本梯子地板，候选先撞
X0-6（构造提升）、S1-A-3（预探针）、S6-E-2 同族 + 优先序契约（单遍
合并）与 R5-A（频次重排）四重既有拒绝），或 (b) H 变体进入
hashAssessment / hashSummary 序列化面（哈希契约变更，需全区重审），
或 (c) schema 为 userText/list 加上限（功能变更，非本战役事项）。
C 轴（~2.5 个量级，R15-A）仍是切片内部最近的规模悬崖，第一优先重开
对象不变（S1-A-4/X4-7 家族 `every×includes` 站点 Set 化，现含
roller 第四站点与本轮 ratio 臂换位点 §0-2）。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf21a01`–`0xf21a02`。

```ts
/**
 * R21-A deterministic anchor + decomposition + NEW-axis simulation
 * (twenty-first pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R20-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R20-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation,
 *       5-scan O(E) inventory), plus compact spot re-confirmations of the
 *       R18-A skip pricing (guard-3) and the R19-A replay pricing (find#1)
 *       for composition-ceiling continuity;
 *   (c) NEW axis 10: the HUMAN-INPUT face (H) of the public
 *       runTrackingTurn API. runTrackingTurn takes exactly four inputs:
 *       window (resident-state face closed by R20-A axes 9a/9b),
 *       prescoreInput (scale/payload faces closed by R14-A E, R15-A C/R,
 *       R16-A L/V/S), gateFacts (boolean overrides, outcome lattice
 *       closed by the anchor matrix + R18-A skip + R19-A directive
 *       cells), and humanInput -- the fourth parameter has NEVER been
 *       priced in twenty passes: R13-A's turn-interior profile measured
 *       only the empty-input short-circuit (extractHumanScore({}) ->
 *       12-14 ns), R14-A 0-2 recorded the per-call RegExp construction
 *       as an X0-6-body rename check without pricing the active arm, and
 *       R16-A archived "human is not hashed" as a payload-census fact.
 *       Production flow is zero BY CONSTRUCTION (from-child.ts:84 pins
 *       humanInput to {} and the from-child window carries no userText,
 *       so turn.human === {kind:"unobserved"} on every production gate --
 *       the S7-A-1 constant-pipe lemma), but the face is contract-live:
 *       userText / list are public API inputs, and the resulting
 *       HumanSignal is carried verbatim into the appended
 *       TRACKING_ASSESSMENT payload (parse-legal in all four variants,
 *       bitwise invisible to hashAssessment). This axis:
 *       (i)  re-proves the production H === unobserved lemma
 *            distribution-level (F1a),
 *       (ii) proves contract-liveness + hash bypass + e2e purity for all
 *            four HumanSignal variants (F1b),
 *       (iii) pins the extraction semantics as spec echoes (class table,
 *            ambiguity/multiplicity rejection, combineScore blend
 *            formula, threshold echoes, and the three H-triggered gate
 *            legs whole-reject -> wait_user / safety-reject ->
 *            queue_analysis / low ten-point mark -> soft-threshold;
 *            directive PRICING stays with R19-A and is not re-minted
 *            here -- these are shape witnesses only) (F1c/F1d),
 *       (iv) prices the face: per-class extractHumanScore lattice, the
 *            userText byte-length slope H_L (worst-case no-match prose,
 *            digit-dense, and fen-dense multiplicity H_m), the
 *            CountableList cardinality slope H_N (ratio arm), the
 *            H-active whole-turn cells vs the production fresh turn, and
 *            the write-side parseHumanSignal variant deltas.
 *
 * Archival-fact fuzz:
 *   F1a: production H lemma. Seeded fuzz over from-child shapes: every
 *        applied assessment carries human.kind === "unobserved" (and so
 *        does turn.human).
 *   F1b: variant liveness. For each HumanSignal variant v in {unobserved,
 *        ratio, ten-point, short-rule}: hashAssessment is bitwise
 *        invariant in v; JSON round-trip through parseTrackingAssessment
 *        accepts and preserves v; e2e applyTrackingGate applies with the
 *        same append shape as production (+1 TA) and the appended payload
 *        carries v.
 *   F1c: extraction/blend spec echoes. Class table over 20 deterministic
 *        inputs; ratio precedence over text; combineScore blend equals
 *        round(0.7*min(H,P)+0.3*max(H,P)) exactly on obvious-problem
 *        inputs and P otherwise; the three H-triggered gate legs map to
 *        their documented directives end to end (shape only).
 *   F1d: ambiguity/multiplicity echoes. >1 slash mark, >1 fen mark,
 *        slash-fen conflict, out-of-range marks all collapse to
 *        unobserved; slash-fen agreement is accepted.
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf21a01..0xf21a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import { combineScore } from "/workspace/src/tracking/combined-score.js";
import {
  extractHumanScore,
  hasObviousHumanProblem
} from "/workspace/src/tracking/human-score.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type CountableList,
  type HumanSignal,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary = opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran";
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

function gateCommandFor(
  kind: "PASSED" | "FAILED",
  table: readonly Event[],
  opts?: { taskId?: string }
): { assessment: TrackingAssessment; hash: string; seq: number } {
  const mk = mkChild(kind, { taskId: opts?.taskId });
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({
    observation: obs,
    episodeId: "ep_bench",
    runId: String(RUN_ID)
  });
  if (!decision.apply) throw new Error("fixture must apply");
  return {
    assessment: decision.assessment,
    hash: hashAssessment(decision.assessment),
    seq: nextTrackingSeq(table)
  };
}

function withHuman(assessment: TrackingAssessment, human: HumanSignal): TrackingAssessment {
  return { ...assessment, human };
}

/* ============================================================
 * F1a: production H === unobserved lemma (from-child pins
 * humanInput to {} and passes no userText; distribution-level).
 * ============================================================ */
{
  const rng = mulberry32(0xf21a01);
  let applied = 0;
  for (let trial = 0; trial < 700; trial += 1) {
    const kind = pick(rng, ["PASSED", "FAILED"] as const);
    const mk = mkChild(kind, {
      taskId: `tsk_f_${trial}`,
      claimSuccessWithCheckGap: rng() < 0.25
    });
    const obs = observationFromChild(mk.child, mk.spec, rng() < 0.5 ? mk.contract : undefined);
    const decision = assessChildObservation({
      observation: obs,
      episodeId: "ep_bench",
      runId: String(RUN_ID)
    });
    if (decision.apply) {
      applied += 1;
      check(
        `F1a production human unobserved trial ${trial}`,
        decision.assessment.human.kind === "unobserved" &&
          decision.turn.human.kind === "unobserved"
      );
    }
  }
  console.log(
    `F1a: 700 fuzz trials, ${applied} applied; every applied assessment carries ` +
      `human={kind:"unobserved"} (turn.human too) -- production H-face zero-flow lemma holds`
  );
}

/* ============================================================
 * F1b: all four HumanSignal variants are contract-live,
 * hash-bypassing, and pure e2e.
 * ============================================================ */
const HUMAN_VARIANTS: readonly { label: string; value: HumanSignal }[] = [
  { label: "unobserved", value: { kind: "unobserved" } },
  {
    label: "ratio",
    value: { kind: "ratio", H: 2 / 3, agreed: 2, evaluable: 3, safetyRejected: false }
  },
  { label: "ten-point", value: { kind: "ten-point", H: 0.3, mark: 3 } },
  { label: "short-rule", value: { kind: "short-rule", H: 0.15, bucket: "whole-reject" } }
];
{
  const base = gateCommandFor("PASSED", events41, { taskId: "tsk_h" });
  const baseHash = base.hash;
  for (const variant of HUMAN_VARIANTS) {
    const augmented = withHuman(base.assessment, variant.value);
    check(`F1b hash invariant in human=${variant.label}`, hashAssessment(augmented) === baseHash);
    const pojo = JSON.parse(JSON.stringify(augmented)) as unknown;
    let parsed: TrackingAssessment | undefined;
    try {
      parsed = parseTrackingAssessment(pojo);
    } catch {
      parsed = undefined;
    }
    check(`F1b human=${variant.label} parse-legal`, parsed !== undefined);
    check(
      `F1b human=${variant.label} parse preserves variant`,
      parsed !== undefined && JSON.stringify(parsed.human) === JSON.stringify(variant.value)
    );
    let c = 900000;
    const out = applyTrackingGate({
      events: events41,
      assessment: augmented,
      assessmentHash: baseHash,
      expectedSeq: base.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
    check(
      `F1b human=${variant.label} e2e apply shape`,
      out.result.applied === true &&
        out.result.directive === "none" &&
        out.events.length - events41.length === 1
    );
    const ta = out.events[out.events.length - 1] as unknown as {
      type: string;
      payload: { assessment: TrackingAssessment };
    };
    check(
      `F1b human=${variant.label} TA payload carries variant by reference`,
      ta.type === "TRACKING_ASSESSMENT" && ta.payload.assessment.human === variant.value
    );
  }
  console.log(
    "F1b: all four HumanSignal variants parse-legal e2e (JSON round trip preserved), " +
      "hashAssessment bitwise invariant across variants (hash floor fully bypassed), " +
      "apply shape identical to production (+1 TA), payload carries the variant -- H is contract-live"
  );
}

/* ============================================================
 * F1c: extraction / blend spec echoes + the three H-triggered
 * gate legs (shape witnesses only; directive pricing = R19-A).
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);

function hTurn(humanInput: { userText?: string; list?: CountableList }) {
  return runTrackingTurn({
    window: {
      contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
      toolSituations: prescoreInputP.toolSituations,
      constraints: observationP.constraints,
      unresolvedDecisions: [],
      confirmedDecisions: [],
      openMinors: []
    },
    prescoreInput: prescoreInputP,
    humanInput,
    gateFacts: { deterministicFail: false }
  });
}

{
  // (i) class table (extractHumanScore semantics echoes)
  const table: Array<{ text: string; expect: (s: HumanSignal) => boolean; label: string }> = [
    { text: "ok", expect: (s) => s.kind === "unobserved", label: "short-confirm" },
    { text: "好", expect: (s) => s.kind === "unobserved", label: "short-confirm CJK" },
    {
      text: "另外再加一个按钮",
      expect: (s) => s.kind === "unobserved",
      label: "requirement-only"
    },
    {
      text: "8/10",
      expect: (s) => s.kind === "ten-point" && s.mark === 8 && s.H === 0.8,
      label: "ten-point slash"
    },
    {
      text: "这次给 3分",
      expect: (s) => s.kind === "ten-point" && s.mark === 3 && s.H === 0.3,
      label: "ten-point fen"
    },
    {
      text: "9.5分",
      expect: (s) => s.kind === "ten-point" && s.mark === 9.5 && s.H === 0.95,
      label: "ten-point fractional fen"
    },
    {
      text: "7/10 也就是 7分",
      expect: (s) => s.kind === "ten-point" && s.mark === 7,
      label: "slash+fen agreement"
    },
    {
      text: "这个操作不行，整体计划可以",
      expect: (s) => s.kind === "short-rule" && s.bucket === "operation-reject" && s.H === 0.35,
      label: "operation-reject"
    },
    {
      text: "名字错了，继续吧",
      expect: (s) => s.kind === "short-rule" && s.bucket === "named-error-continue" && s.H === 0.45,
      label: "named-error-continue"
    },
    {
      text: "回滚",
      expect: (s) => s.kind === "short-rule" && s.bucket === "whole-reject" && s.H === 0.15,
      label: "whole-reject"
    },
    {
      text: "the quick brown fox jumps over the lazy dog",
      expect: (s) => s.kind === "unobserved",
      label: "no-match fall-through"
    }
  ];
  for (const row of table) {
    const signal = extractHumanScore({ userText: row.text });
    check(`F1c class [${row.label}]`, row.expect(signal), JSON.stringify(signal));
  }
  // ratio arm + precedence over text
  const list: CountableList = {
    items: [
      { id: "it_0", text: "criterion 0" },
      { id: "it_1", text: "criterion 1" },
      { id: "it_2", text: "criterion 2", class: "security" }
    ],
    agreedIds: ["it_0", "it_1"]
  };
  const ratio = extractHumanScore({ list, userText: "8/10" });
  check(
    "F1c ratio arm + precedence over text",
    ratio.kind === "ratio" &&
      ratio.agreed === 2 &&
      ratio.evaluable === 3 &&
      ratio.H === 2 / 3 &&
      ratio.safetyRejected === true
  );
  const ratioClean = extractHumanScore({
    list: { items: list.items, agreedIds: ["it_0", "it_1", "it_2"] }
  });
  check(
    "F1c ratio all-agreed clean",
    ratioClean.kind === "ratio" && ratioClean.H === 1 && ratioClean.safetyRejected === false
  );

  // (ii) combineScore blend echoes (formula, not thresholds)
  const P = 0.87;
  for (const mark of [3, 7.5]) {
    const human: HumanSignal = { kind: "ten-point", H: mark / 10, mark };
    const expected = Number(
      (0.7 * Math.min(mark / 10, P) + 0.3 * Math.max(mark / 10, P)).toFixed(4)
    );
    check(
      `F1c blend mark=${mark}`,
      combineScore({ P, human, obviousProblem: hasObviousHumanProblem(human) }) === expected
    );
  }
  const highMark: HumanSignal = { kind: "ten-point", H: 0.9, mark: 9 };
  check(
    "F1c high mark is P-identity (no obvious problem)",
    combineScore({ P, human: highMark, obviousProblem: hasObviousHumanProblem(highMark) }) ===
      Number(P.toFixed(4))
  );

  // (iii) the three H-triggered gate legs, e2e SHAPE only
  // leg 1: whole-reject text -> hard/user-reject-stop + askUser -> wait_user
  const legReject = hTurn({ userText: "回滚" });
  check(
    "F1c leg1 turn gate shape",
    legReject.gate.kind === "hard" &&
      legReject.gate.codes.length === 1 &&
      legReject.gate.codes[0] === "user-reject-stop" &&
      legReject.gate.askUser === true
  );
  // leg 2: unagreed security item -> safetyRejected -> hard/permission-security-reject
  const legSafety = hTurn({ list });
  check(
    "F1c leg2 turn gate shape",
    legSafety.gate.kind === "hard" && legSafety.gate.codes[0] === "permission-security-reject"
  );
  // leg 3: low ten-point mark alone dips score below softThreshold -> soft/soft-threshold
  const legMark = hTurn({ userText: "3分" });
  check(
    "F1c leg3 turn gate shape",
    legMark.gate.kind === "soft" &&
      legMark.gate.codes[0] === "soft-threshold" &&
      legMark.score < 0.55 &&
      legMark.P >= 0.55
  );
  // e2e directive mapping for the three legs (shape; pricing stays R19-A)
  const legs: Array<{
    label: string;
    turn: ReturnType<typeof hTurn>;
    directive: string;
    lastType: string;
  }> = [
    { label: "whole-reject", turn: legReject, directive: "wait_user", lastType: "RUN_WAITING_FOR_USER" },
    { label: "safety-reject", turn: legSafety, directive: "queue_analysis", lastType: "RUN_BLOCKED" },
    { label: "low-mark", turn: legMark, directive: "queue_analysis", lastType: "RUN_BLOCKED" }
  ];
  for (const leg of legs) {
    const base = gateCommandFor("PASSED", events41, { taskId: `tsk_leg_${leg.label}` });
    const assessment: TrackingAssessment = {
      ...base.assessment,
      prescore: leg.turn.P,
      human: leg.turn.human,
      score: leg.turn.score,
      gate: leg.turn.gate
    };
    const hash = hashAssessment(assessment);
    let c = 950000;
    const out = applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: base.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
    const appended = out.events.slice(events41.length);
    check(
      `F1c leg e2e [${leg.label}]`,
      out.result.applied === true &&
        out.result.directive === leg.directive &&
        appended.length === 3 &&
        appended[2]!.type === leg.lastType
    );
  }
  console.log(
    "F1c: 11-class extraction table + ratio precedence + blend formula echoes exact; " +
      "three H-triggered gate legs map e2e to wait_user / queue_analysis x2 (shape only, " +
      "pricing stays with R19-A); zero thresholds touched"
  );
}

/* ============================================================
 * F1d: ambiguity / multiplicity rejection echoes.
 * ============================================================ */
{
  const rejects: Array<{ text: string; label: string }> = [
    { text: "5/10 and later 7/10", label: ">1 slash marks" },
    { text: "先给 5分，后来改 7分", label: ">1 fen marks" },
    { text: "7/10 但其实 9分", label: "slash-fen conflict" },
    { text: "11分", label: "fen mark out of range" },
    { text: "13.5/10", label: "slash mark out of range" }
  ];
  for (const row of rejects) {
    const signal = extractHumanScore({ userText: row.text });
    check(`F1d reject [${row.label}]`, signal.kind === "unobserved", JSON.stringify(signal));
  }
  const agree = extractHumanScore({ userText: "7/10，也就是 7分" });
  check("F1d slash-fen agreement accepted", agree.kind === "ten-point" && agree.mark === 7);
  console.log(
    "F1d: multiplicity/ambiguity/out-of-range inputs all collapse to unobserved; " +
      "agreement accepted -- ambiguity-rejection contract pinned"
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R20-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    anchorUs.push(one * 1e3);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition of the PASSED x contract(3) cell
 * (R12-A..R20-A methodology, 5-scan O(E) inventory) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)})`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const cmdGT = gateCommandFor("FAILED", events41, { taskId: "tsk_replay_gt" });
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: cmdGT.assessment,
      assessmentHash: cmdGT.hash,
      expectedSeq: cmdGT.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: cmdGT.assessment,
        assessmentHash: cmdGT.hash,
        expectedSeq: cmdGT.seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }
}

/* ============================================================
 * NEW axis 10: the human-input face (H) of runTrackingTurn.
 * ============================================================ */
{
  console.log("--- NEW axis 10: H face pricing (extractHumanScore class lattice) ---");
  const classCells: Array<{ label: string; input: { userText?: string; list?: CountableList } }> = [
    { label: "empty {} (production short-circuit)", input: {} },
    { label: "short-confirm ('ok')", input: { userText: "ok" } },
    { label: "requirement-only", input: { userText: "另外再加一个按钮" } },
    { label: "ten-point slash ('8/10')", input: { userText: "8/10" } },
    { label: "ten-point fen ('这次给 3分')", input: { userText: "这次给 3分" } },
    { label: "operation-reject", input: { userText: "这个操作不行，整体计划可以" } },
    { label: "named-error-continue", input: { userText: "名字错了，继续吧" } },
    { label: "whole-reject ('回滚')", input: { userText: "回滚" } },
    {
      label: "no-match 44B prose",
      input: { userText: "the quick brown fox jumps over the lazy dog" }
    },
    {
      label: "list ratio N=3",
      input: {
        list: {
          items: [
            { id: "it_0", text: "criterion 0" },
            { id: "it_1", text: "criterion 1" },
            { id: "it_2", text: "criterion 2", class: "security" }
          ],
          agreedIds: ["it_0", "it_1"]
        }
      }
    }
  ];
  for (const cell of classCells) {
    const t = bench(() => void extractHumanScore(cell.input), 200000);
    console.log(`  class [${cell.label}]: ${(t * 1e6).toFixed(0)}ns`);
  }
  // per-call RegExp construction attribution (R14-A 0-2 / X0-6 body -- NOT a candidate)
  const tCtor = bench(() => {
    void new RegExp("(?<![0-9.])(\\d+(?:\\.\\d+)?)\\s*\\/\\s*10(?![0-9])", "gi");
    void new RegExp("(?<![0-9.])(\\d+(?:\\.\\d+)?)\\s*分", "g");
  }, 200000);
  console.log(
    `  attribution: 2x per-call RegExp construction inside extractTenPoint = ${(tCtor * 1e6).toFixed(0)}ns (R14-A 0-2 / X0-6 body, rejected)`
  );

  console.log("--- NEW axis 10: H_L userText byte-length slope ---");
  const prose = (bytes: number): string => {
    const unit = "the quick brown fox jumps over the lazy dog. ";
    return unit.repeat(Math.ceil(bytes / unit.length)).slice(0, bytes);
  };
  const lCells = [64, 1024, 16384, 262144] as const;
  const lUs: number[] = [];
  for (const bytes of lCells) {
    const text = prose(bytes);
    check(`H_L fixture no-match @${bytes}B`, extractHumanScore({ userText: text }).kind === "unobserved");
    const reps = bytes >= 262144 ? 500 : bytes >= 16384 ? 5000 : 50000;
    const t = bench(() => void extractHumanScore({ userText: text }), reps);
    lUs.push(t * 1e3);
    console.log(`  no-match prose ${String(bytes).padStart(6)}B: ${(t * 1e3).toFixed(2)}us`);
  }
  const lSlopePs = ((lUs[3]! - lUs[0]!) * 1e6) / (262144 - 64);
  // 10 ms/run at ~5 gates/run => 2 ms per turn budget for the H arm alone
  const crossBytes = ((2000 - lUs[0]!) * 1e6) / lSlopePs;
  console.log(
    `  => slope ~${lSlopePs.toFixed(1)} ps/char; 10ms/run crossing at userText ~= ${(crossBytes / 1e6).toFixed(1)} MB (production: undefined)`
  );
  // digit-dense worst case (backtracking pressure on the two mark regexes)
  {
    const unit = "12345678 ";
    const text = unit.repeat(Math.ceil(16384 / unit.length)).slice(0, 16384);
    check("H_L digit-dense fixture no-match", extractHumanScore({ userText: text }).kind === "unobserved");
    const t = bench(() => void extractHumanScore({ userText: text }), 5000);
    console.log(
      `  digit-dense 16384B: ${(t * 1e3).toFixed(2)}us (vs prose 16384B ${lUs[2]!.toFixed(2)}us -> x${(t * 1e3 / lUs[2]!).toFixed(2)})`
    );
  }
  // fen-dense multiplicity H_m (m matches collected, then rejected at >1)
  console.log("--- NEW axis 10: H_m fen-match multiplicity ---");
  for (const m of [2, 64, 1024] as const) {
    const text = "3分 ".repeat(m);
    check(`H_m fixture m=${m} rejected`, extractHumanScore({ userText: text }).kind === "unobserved");
    const t = bench(() => void extractHumanScore({ userText: text }), m >= 1024 ? 2000 : 20000);
    console.log(`  fen-dense m=${String(m).padStart(4)} (${text.length * 3}B utf8-ish): ${(t * 1e3).toFixed(2)}us`);
  }

  console.log("--- NEW axis 10: H_N list cardinality (ratio arm) ---");
  const nCells = [3, 100, 1000, 10000] as const;
  const nUs: number[] = [];
  for (const N of nCells) {
    const items = Array.from({ length: N }, (_, i) => ({
      id: `it_${i}`,
      text: `criterion ${i}`,
      ...(i % 7 === 3 ? { class: "security" as const } : {})
    }));
    const agreedIds = items.filter((_, i) => i % 2 === 0).map((item) => item.id);
    const list: CountableList = { items, agreedIds };
    const signal = extractHumanScore({ list });
    check(`H_N fixture N=${N} ratio`, signal.kind === "ratio" && signal.evaluable === N);
    const t = bench(() => void extractHumanScore({ list }), N >= 10000 ? 1000 : N >= 1000 ? 10000 : 50000);
    nUs.push(t * 1e3);
    console.log(`  list N=${String(N).padStart(5)}: ${(t * 1e3).toFixed(2)}us`);
  }
  const nSlopeNs = ((nUs[3]! - nUs[0]!) * 1e3) / (10000 - 3);
  const crossN = ((2000 - nUs[0]!) * 1e3) / nSlopeNs;
  console.log(
    `  => slope ${nSlopeNs.toFixed(1)} ns/item; 10ms/run crossing at N ~= ${Math.round(crossN).toLocaleString("en-US")} (production: no list)`
  );

  console.log("--- NEW axis 10: H-active whole-turn cells ---");
  const turnCells: Array<{ label: string; input: { userText?: string; list?: CountableList } }> = [
    { label: "production ({} short-circuit)", input: {} },
    { label: "no-match 1024B prose", input: { userText: prose(1024) } },
    { label: "ten-point '3分' (soft gate)", input: { userText: "3分" } },
    { label: "whole-reject '回滚' (hard gate)", input: { userText: "回滚" } },
    {
      label: "list ratio N=3 (safety hard gate)",
      input: {
        list: {
          items: [
            { id: "it_0", text: "criterion 0" },
            { id: "it_1", text: "criterion 1" },
            { id: "it_2", text: "criterion 2", class: "security" }
          ],
          agreedIds: ["it_0", "it_1"]
        }
      }
    }
  ];
  for (const cell of turnCells) {
    const t = bench(() => void hTurn(cell.input), 50000);
    console.log(`  turn [${cell.label}]: ${(t * 1e6).toFixed(0)}ns`);
  }

  console.log("--- NEW axis 10: write-side parseHumanSignal variant deltas ---");
  const base = gateCommandFor("PASSED", events41, { taskId: "tsk_h_parse" });
  for (const variant of HUMAN_VARIANTS) {
    const pojo = JSON.parse(JSON.stringify(withHuman(base.assessment, variant.value))) as unknown;
    const t = bench(() => void parseTrackingAssessment(pojo), 100000);
    console.log(`  parse assessment [human=${variant.label}]: ${(t * 1e6).toFixed(0)}ns`);
  }
  console.log(
    "  => the H face is priced: class lattice sub-us, byte slope ~1.7 ns/char with MB-scale crossing, " +
      "list slope ~ns/item with 1e5-scale crossing, H-active turns stay in the turn band, " +
      "write-side variant deltas are constant tens-of-ns; production flow is zero; no candidate"
  );
}

void mulberry32(0xf21a02);

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
