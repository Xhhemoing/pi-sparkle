MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 16 / R16-C：离线路由切片第十六遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 16 / R16-C（Round 1–15 同区第十六遍，叠在
  已落地的 S7-C 之上）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `391495d`（R16-A 空枚举合入 +
  R16-C 派单后的独占分支最新 tip，与派单预期一致；
  `git log -1 -- src/routing/` 显示切片最后一次生产改动**仍为 `183df9b`
  「perf(routing): land S7-C support-only eta」**，其后基线区间仅文档更新。
  `git diff 183df9b..HEAD --` 对 9 个切片文件为**空**（stat 输出为空、exit 0），
  本轮全部测量在该形态之上，未回退未重写。R16-B / R15-J 在飞，未等待、
  未触碰其分支与文件）
- **分支:** `cursor/r16-c-offline-routing-sixteenth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十六组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第十六遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R15-C 的全部无号关闭
探针）后，本轮找到**两个从未点名的微观站点**、**一处零池备忘站点**并
新增**两条测量轴**（R15-A/B/C 先例），全部以关闭探针（不铸号）收口
——没有任何一个达到接受门槛：

- **ICOL（累加 switch 截距列常量折叠，关闭探针，不铸号）**——
  `build()` 无条件写 `vec[0]=1` 且 `computeSupports` 按 j 升序推入，
  故**每一行的 `active[0]` 恒等于 0**（模块内可证不变量，本轮在 perf
  夹具全 400 行上断言核实）。把 case 2..5 体内的 `active[0]` 装载替换
  为字面量 0 是结构性逐位等价（同目标、同浮点操作、同顺序，只删一次
  整数元素装载/分派行访问；53 例四道电池全绿 × 5 次运行）。从未有 ID
  点名过「支撑首元素 = 截距列常量」这一位置（SWORD 是标签序、STORD
  是体内语句序、S6-C-5 是 length 提升、MUCSE 是 mu/eta 重复读——均非
  元素装载的不变量常量折叠）。五次位置交替决斗 delta **+11.7 / +1.5 /
  +34.7 / +20.3 / −4.9 ms（landed 减 icol）——第五次运行符号翻转**，
  且最大读数 +34.7 出自可见漂移窗口（该窗口双道 rep 均漂至 750–780
  区间；run D 亦含 729 漂移 rep）；干净窗口读数 +11.7/+1.5 落在
  AAFLR 地板（±1.5，R15-C）与先验算术类（每分派行访问一次 L1 热整数
  装载 ≈ 1–3 ns × 3,586,400 = **3.6–10.8 ms 类**）之间。即使按面值
  全收，34.7 < 35 仍是「贴带不落地」；符号不稳判为深亚带微效应 +
  漂移伪信号，关闭。
- **SFILL（solve 回代输出数组 `fill(0)` 死存储，关闭探针，不铸
  号）**——R15-C 零池注记只覆盖了 `solution` 的**分配**（返回值本
  体）；`fill(0)` 本身从未点名。死存储可证：回代自 row=n−1 降序写满
  全部下标，每次读 `solution[k]`（k>row）都命中已写元素，尾部
  isFinite 扫描只读已写元素。独立池计时 **8,966 次 fill(60) =
  0.598–0.626 ms/报告封顶**，亚带 ~56×；结构对价（跳过 fill）把输出
  数组退化为 HOLEY 元素类别（已知 V8 反模式），五次决斗 delta
  **−1.6 / −7.6 / +2.9 / +5.3 / +0.8 ms 符号翻转** = 纯抖动。算术
  关闭。
- **ITERX（IRLS 收敛普查，新轴，非候选）**——perf 夹具 201 拟合中
  **56 个经 l2 < 1e-8 提前收敛、145 个打满 maxIter=50、0 个奇异**
  （三次正典运行逐位一致）；迭代四分位 q1=45 / med=50 / q3=50 /
  max=50。坐实 solve 池（473–485 ms，占 66.5–67.9%）线性依赖于
  **冻结的 8,966 次迭代**——任何「减迭代」思路（warm start、TOL/初
  值变更、bootstrap 续拟合）都改变浮点序列，被逐位契约排除（X1-3/
  X2-3 族的收敛侧镜像），收敛缩短类无可受理道，本轴一次性收口。
- **COLDX（一次性进程 JIT 预热份额，新轴，非候选）**——把**本进程
  第一次**生产报告（import 完成后、任何切片调用之前计时）与稳态中位
  对比：预热份额 **46.9–80.5 ms（占中位 6.8–11.8%，五次运行）**。
  该份额名义上越带，但**无切片内可受理变换**：进程内预热只搬移成本
  不消除成本（S7-H-3 在他区实测预热为净负），跨进程代码缓存是构建面
  （S6-I-2 已关），且预热后的稳态池本身全部冻结。模块加载/解析成本
  在 import 时支付，属 I 区辖域，不计入本轴。轴留档校准：C 区一切
  「整报告中位」口径均为稳态口径，一次性调用另付一笔亚 100 ms 的
  引擎预热，两口径都无新道。
- **PLSEG（prob-add diagnose 项目环自引用过滤 + 死参，零池备忘，
  不铸号）**——`diagnose()` 项目环用
  `key.endsWith("|" + lastSegment(key))` 过滤交互对：lastSegment 取
  的是**被测键自己**的尾段，谓词对含 "|" 的键恒真，故该过滤对每个
  欠佳项目算出同一对集（项目名甚至未被读取）；`lastSegment` 另携带
  死 `_input` 形参（IRLSDP 同类）。整个 `fitProbabilityAdditive`
  报告在生产形状（1 场景 × 6 模型 × 8 项目 × 400 行）实测
  **1.144–1.188 ms**，站点池为零——语义/重构问题移交表所有者备忘
  （本战役只落性能赢家，行为一位不动），非候选。

除上述站点与轴外，第十六遍逐行重读**零新发现**：其余每一个执行站点
都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 / 结构交换负
优化）。三大池地板与组合天花板复核见 §3：本 VM 上 solve 与生产中位
整体上漂 ~2–6%（473.3–484.8 / 676.5–686.3，仍在 R14-C 带 657.6–689.3
内），APC 池 48.5–50.9 **漂出 R14-C 重锚定带 53.0–55.6 下沿**，按
R14-C §3 末处置规则随池重锚地板：组合天花板本 VM 直测
**12.1–23.8 ms（三次）< 35**，组合关闭维持且更紧。小形状档整报告
中位 **31.4–33.0 ms**，整条报告低于噪声带（R7-I 矩阵义务履行）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11），live 面零文件改动。仓库变更仅本报告一个文件；无生产赢家故
未提交 `scripts/round16-r16c-equivalence-sim.ts`（败者/锚点仿真全文进
附录，遵守 R7-C..R15-C 先例）。

## 0. 范围与约束遵守

- 读序（强制）已履行：README → EXCLUSIONS.md（完整表，含 S12-C-1 与
  S13-B-1）→ round-16/PLAN.md → R15-C.md（空枚举确认，无 S15-C-\*；
  STORD/RIDGE 关闭探针与 AAFLR/GCAX 轴记名）→ R14-C.md（空枚举；
  SWORD/XF64/IRLSDP/CMPH 与 APCX/TIMER/FLOORX 重锚定记名）→
  R13-C（PIVCSE/FINTAIL/REFZ/TSORT/DEG）与 R12-C（S12-C-1 +
  FZERO/PIVSWAP/BASE/SAMP/MUCSE）、R11-C（RID/CNT/AUGF/EXP/ALLOC/
  DMAP/COPY/SCAN）探针表逐条核对 → 9 个切片源文件全量重读。
- 基线漂移检查：基线 `391495d` 相对 R15-C 基线 `d148d12` 仅文档合入
  （R15-F/G/H/I 与 R16-A 空枚举收口、R16 PLAN 与派单）；
  `git log -1 -- src/routing/` 仍为 `183df9b`（S7-C 落地本身），
  `git diff 183df9b..HEAD --` 对 9 文件为空（stat 空输出、exit 0），
  落地形态原样在位。既有回归资产七连全绿且检查数逐一匹配锚定值（§5）。
- 调用图确认（派单强制项，本轮 rg 复核）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 消费（双归因两路在位）；
  `solveSymmetric` 仅被 `offline-logit.ts` 导入；`src/supervisor/`
  对 9 个切片文件的导入为**零**——live = R0 等价维持，R1 未接线，
  `src/supervisor/model-router.ts` 零字节未触。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S10-\*、S12-C-1 全系未重提、未立道、
  未重测；R13-C 五个、R14-C 四个、R15-C 两个无名微观
  （PIVCSE/FINTAIL/REFZ/TSORT/DEG/SWORD/XF64/IRLSDP/CMPH/STORD/
  RIDGE）未重探未补铸未换址重提；R15-C AAFLR/GCAX 两轴未补铸
  （本轮决斗读数按 AAFLR 已留档地板 ±1.5 ms 校准，未重测 A/A；GC 轴
  未重测）；RID/CNT 归约重排反例原样引用；嵌套序 × 布局组合空间
  （S4-C-2 + S5-C-1..4 + S12-C-1）未重开（SFILL 是回代**输出数组死
  存储**站点，非任何表示/嵌套形态）；CMB 组合未重堆叠（本轮 FLOORX
  重测遵循 R14-C 规则，是地板测量非堆叠候选，S8-C-2/S10-C-2/
  S2-C-4/S4-C-4 全部维持关闭）。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动；X2-3 数值路径未触；单线程逐位审计基底不动。
- 环境：Node 22.22.2（VM 默认 22.14.0 低于 engines ≥22.19.0，nvm
  切换并前置 PATH，与 R1-C..R15-C 同处理）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`。

## 1. 重剖析：S7-C 后成本模型（本 VM 实测，未拷贝历轮数字）

测量器为**仪表化逐字复刻管线**（与 183df9b 生产 irls/solve/on-prob/
APC 逐语句相同，仅在调用边界加 performance.now() 与计数器，浮点操作/
操作数/顺序零改动），其忠实性先经 **53 例全形状电池 ×{仪表道,
纯净落地道, 纯净 ICOL 道, 纯净 SFILL 道} vs 生产 `fitLogitAdditive`
导入逐位全绿**（Object.is 含符号零）再采信读数；生产中位数由**未仪表
化生产导入**直接测得。perf 夹具与 R10-C..R15-C 完全一致
（fixtureRng(0xbeef)，4 场景 × 6 模型 × 8 项目，rows=400，
passRate=0.6，bootstrap=200, seed=20260818 → p=60，201 拟合，
8,966 次 IRLS 迭代）。

生产整报告中位（每次独立进程 7 测量取中位，三次正典运行 + 两次补充
运行）：**684.5 / 676.5 / 686.3（正典 A/B/C）；677.1 / 672.8（补充
D/E）**——高于派单 R15-C 带 660.6–667.1 约 1.4–3.9%，但仍在 R14-C
带 657.6–689.3 内（同 VM 独立旁证：r7c 回归资产锚点 **669.0 ms =
1.13× vs 冻结 S6-C ctl 754.3**，与中位同带）；判为 VM 微架构漂移，
solve 池同幅上漂（见下），非切片回归。

仪表化池表（perf 形状，每次运行内 3 次仪表化取中位；三次正典运行
读数并列）：

| 池 | run A | run B | run C | 占比 | 站点计数（精确） |
| --- | --- | --- | --- | --- | --- |
| 总计 | 722.0 | 701.7 | 714.2 | 100% | fits=201 |
| `solveSymmetric` | **480.4** | **473.3** | **484.8** | **66.5–67.9%** | 8,966 次调用 |
| `irls` 自身（扣 solve） | **178.4** | **166.3** | **164.5** | **23.0–24.7%** | 8,966 迭代 / 3,586,400 行访问 / 1,249,652 sigmoid |
| APC 全站点 | 49.5 | 48.5 | 50.9 | 6.9–7.1% | 277,606 活跃对 / 12,259 次调用 |
| `onProbabilitiesFor` | 7.1 | 7.2 | 7.2 | 1.0% | 80,400 sigmoid+dot |
| bootstrap 采样 | 1.8 | 2.2 | 1.8 | 0.2–0.3% | —（SAMP 池 R12-C 已封顶） |
| 排序/分位尾 | 2.2 | 1.6 | 1.6 | 0.2–0.3% | 12,200 draws-map ops |
| 基行投影 | 0.006 | 0.005 | 0.006 | ~0% | 每报告 1 次（R12-C BASE 已封顶） |
| 编排余量（build/supports/keys） | 2.7 | 2.7 | 3.4 | 0.4–0.5% | — |

除 solve / irls / APC 外每池整池清零也低于 ±35 ms 带（R9-C..R15-C
同结论，逐池复测未漂移）；solve 与 irls 为落地线唯二宿主。本轮新增
仪表计数（本轮探针用）：**支撑尺寸分派普查 s1=0 / s2=63,324 /
s3=280,467 / s4=1,210,220 / s5=2,032,389**（3,586,400 行访问全部落
在 case 2..5，default 滚动环在 perf 形状零执行——S6-C 分派覆盖率的
直接旁证；ICOL 可及访问 = 全量）；**收敛普查 56 收敛 / 145 打满 /
0 奇异，q1=45 med=50 q3=50 max=50**（ITERX 轴）。**APC 池本 VM 三次
读数 48.5–50.9 漂出 R14-C 重锚定带 53.0–55.6 的下沿**——按 R14-C
§3 末处置规则随池重锚地板（§3，天花板 12.1–23.8 < 35，关闭更紧）。
小形状档（3 模型 × 3 项目 / 120 行，bootstrap=200）整报告中位
**31.6 / 33.0 / 32.9（D 31.4）ms**——小配置态整条报告低于噪声带，
调用矩阵小态一侧整体收口（R7-I 矩阵义务履行）。

生产自确定性复核：3 seed × 各 2 次运行全字段 Object.is 全绿（含于
§5 的 11,709 项检查 × 5 次运行）。

## 2. 第十六组枚举（诚实结果：空；两个无号关闭探针 + 一处零池备忘 + 两条新轴）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过。** 从未点名的站点全部在下表关闭（无一铸号；探针数字
为本 VM 实测，出处为附录测量彩排三次正典运行 + 两次补充运行）：

| 站点/轴（从未点名） | 关闭方式 | 证据 |
| --- | --- | --- |
| ICOL：累加 switch case 2..5 体内 `active[0]` 装载折叠为字面量 0（截距列恒居支撑首位） | 五次决斗符号翻转 + 最大读数出自漂移窗口且贴带不越带 + 先验算术类深亚带 | 不变量在 perf 夹具全行断言成立（`supports.every(s => s[0]===0)`）；结构性逐位（同目标/同浮点/同序，仅删一次整数装载；53 例 × 5 运行电池全绿）；决斗 delta **+11.7 / +1.5 / +34.7 / +20.3 / −4.9 ms**——run E 符号翻转、run C/D 窗口可见漂移（双道 rep 漂至 729–780）；干净读数落在 AAFLR 地板与 1–3 ns/装载 × 3,586,400 = **3.6–10.8 ms 算术类**之间；面值最大 34.7 < 35（贴带不落地） |
| SFILL：solve 回代输出数组 `new Array(n).fill(0)` 死存储消除 | 算术上界深度亚带 + 结构对价为元素类别退化 + 决斗符号翻转 | 死存储证明：回代 row=n−1..0 先写后读、isFinite 尾扫只读已写元素；独立池 **8,966 × fill(60) = 0.598–0.626 ms/报告封顶**，亚带 ~56×；跳过 fill ⇒ HOLEY 输出数组（V8 反模式）；五次决斗 **−1.6 / −7.6 / +2.9 / +5.3 / +0.8 ms** 符号翻转 = 纯抖动 |
| ITERX：IRLS 收敛普查（新轴，非候选） | 收敛缩短类一次性收口，无可受理道 | **56 收敛 / 145 打满 maxIter=50 / 0 奇异**（确定性，跨运行逐位一致）；q1=45 med=50 q3=50 max=50；solve 池 473–485 ms 线性依赖 8,966 次冻结迭代——warm start / TOL / 初值 / 续拟合全部改变浮点序列（X1-3/X2-3 收敛侧镜像），无逐位合法的减迭代形态 |
| COLDX：一次性进程 JIT 预热份额（新轴，非候选） | 名义越带但无切片内变换 | 首报告 vs 稳态中位：**46.9–80.5 ms（6.8–11.8%，五次运行）**；进程内预热只搬移成本（S7-H-3 他区实测净负）、跨进程缓存属构建面（S6-I-2 已关）、稳态池本身全冻结；模块加载在 import 支付属 I 区。轴留档：C 区中位口径 = 稳态口径 |
| PLSEG：prob-add `diagnose()` 项目环自引用 endsWith 过滤（项目不变式）+ `lastSegment` 死 `_input` 形参 | 零池 + IRLSDP 类重构备忘 | 整 `fitProbabilityAdditive` 报告（1 场景 × 6 模型 × 8 项目 × 400 行）实测 **1.144–1.188 ms**，站点池为零；`key.endsWith("|"+lastSegment(key))` 对含 "|" 键恒真 ⇒ 项目环对每个欠佳项目复算同一对集且未读项目名——语义问题移交表所有者，性能面无候选（行为一位不动） |

以上无一构成候选：ICOL 是删一条 L1 热整数装载的常量折叠，符号不稳
（五取一翻转）且面值最大读数贴带不越带，先验算术类 3.6–10.8 ms 深亚
带——按 AAFLR 地板与漂移窗口证据校准后判为深亚带微效应 + 漂移伪信
号；SFILL 池 0.6 ms 且结构对价自带元素类别退化；ITERX/COLDX 是测量
轴不是变换；PLSEG 池为零。**故第十六组候选集为空，不铸 S16-C-1 及
后续编号**，遵循 R11-C..R15-C 无号关闭先例。

**枚举收口：** 除上表外，第十六遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖；本轮
重读零新发现——posterior 的 80 次二分 × betacf 每报告一次为亚 ms
且 X1-3 冻结替换；prob-add 的 kappaS/cell 双算是 S1-C-6 本体、
诊断双层 entries 在 PLSEG 锚定的 1.14–1.19 ms 整报告池内；
propensity 的 `isFabricatedPositiveSupport` 三谓词扫描属 S1-C-8
同族遍数常数域；bandit/shadow 微观分配是 S1-C-9/X4-2 辖区；
estimateFingerprint 是 S1-C-4 本体）。`offline-logit.ts` 与
`lin-alg.ts` 的剩余站点经本轮两个新位置关闭后，四分类（重命名 /
亚带 / 逐位违例 / 结构交换负优化）继续穷尽。顺带坐实的零池观察
（不铸号，非探针）：perf 形状下累加 switch 的 default 滚动环执行
次数恰为 0（s1=0 普查），S6-C 直线化分派在生产形状覆盖率 100%；
eta 支撑和环里的 `active[0]`（同一不变量）由 sigmoid 去重压到
1,249,652 次执行，其常量折叠形态被同一 ICOL 算术类覆盖（滚动环内
折叠还需重构环结构，S6-C-3/4 辖区）。本轮核实换名再提交拒列：零
——没有任何已关 ID 的换址/换名形态在本轮出现。

## 3. 三大池地板与组合出路复核

- **solve 池 473.3–484.8 ms（本 VM，高于 R15-C 带 452.8–453.8 约
  4.5–6.8%，与生产中位同幅同向 = VM 漂移非回归）**：消元主体 ≈
  6.45 亿次冻结顺序浮点乘减/报告。R6-C §2.5 七类分解 + S10-C-3 +
  S12-C-1（嵌套序，稳定 ~3.6× 负优化）+ R11-C COPY + R12-C FZERO/
  PIVSWAP + S5-C-6 + R13-C PIVCSE/FINTAIL + R14-C XF64 之后，本轮
  SFILL 关闭回代输出数组的最后一个从未点名位置（fill 死存储，
  0.6 ms 封顶）。ITERX 坐实池量线性依赖 8,966 次冻结迭代，收敛
  缩短类无逐位合法形态。触碰地板本体的机制只剩 SIMD/线程
  （S9-C-1/2 + 不变量禁止）与数值路径改道（X2-3 冻结）。
- **irls 自身池 164.5–178.4 ms**：可省项全部已关闭（S2-C/S6-C/S7-C
  落地本体之上：S10-C-1 决斗带内、S9-C-3 符号翻转、S6-C-6 上界
  2–4、S5-C-7 无正决斗、S9-C-4、S4-C-5/6、S2-C-3、S8-C-3、R11-C
  RID/CNT/EXP/ALLOC、R12-C MUCSE、R14-C SWORD/IRLSDP、R15-C
  STORD/RIDGE）。本轮 ICOL 关闭 case 体内元素装载的不变量常量折叠
  （最后一个从未点名的装载类位置：标签序 SWORD、体内语句序 STORD、
  首元素装载 ICOL 至此三面收口）；s1=0 普查坐实分派全覆盖。
- **APC 池 48.5–50.9 ms（本 VM，漂出 R14-C 重锚定带 53.0–55.6 的
  下沿 ~5–9%）**：按 R14-C §3 末处置规则随池重锚地板（这不是重跑
  已关 ID 的决斗，也不构建任何堆叠候选道）：APCX 无计时器独立道
  **54.6–57.8 ms**（基形状近似，sink=7.309 与 R14-C 逐位同值 =
  跨轮确定性旁证）；FLOORX 最廉价逐位合法形态（S8-C-2 型支撑升序
  off 和——仅作地板测量，S8-C-2/S10-C-2/S2-C-4/S4-C-4 全部维持
  关闭）地板 **31.5–42.5 ms** ⇒ **组合天花板 = 池 − 地板 =
  12.1–23.8 ms（三次读数）< 35**。池下漂坐实可省份额进一步收缩，
  组合关闭在本 VM 上更强而非更弱，与 R14-C 直测 13.6–21.9 与
  S8-C-2 历史决斗 22.2–23.6 同带互证。R13-C REFZ 参考水平份额
  不受影响。
- **预热‑稳态两口径（本轮新轴收口）**：COLDX 46.9–80.5 ms 预热份额
  （6.8–11.8%）+ R15-C GCAX 5.89–5.93 ms 收集器份额 + R11-C ALLOC
  2.0–3.5 ms 分配封顶——引擎侧三条轴（编译预热/回收停顿/分配）
  全部无切片内可受理变换；稳态口径下三大池冻结结论不受影响。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测 +8.8~+30.5 带级
  摆幅带内），维持关闭。本轮两个探针无一是稳定越带正项（ICOL 符号
  翻转、SFILL 0.6 ms 封顶），无新成员可入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建面
开放 / X2-3 数值路径解冻 / 单线程审计基底变更——均不预期）；
（b）现实规模位移 ≥1 个数量级使亚带池放大越带（ICOL/SFILL 的重开
条件同此；ICOL 另附条件：在安静窗口取得符号稳定且越带的读数，且须
先按 AAFLR 复核当窗地板）；或（c）指出「重命名 / 亚带 / 逐位违例 /
结构交换负优化」四分类之外的第五类成本——十六遍枚举未再发现。

## 4. 重命名检查（派单强制项）

- **ICOL vs SWORD / STORD / S6-C / S6-C-1..5 / MUCSE / S7-C**：
  SWORD（R14-C 无号）是 case **标签排列**次序；STORD（R15-C 无号）
  是 case 体内**语句交错**次序；S6-C 是直线化分派本体、S6-C-1/2 是
  子集特化、S6-C-3/4 是滚动/展开结构、S6-C-5 是 `active.length`
  **长度**读提升；MUCSE（R12-C 无号）是 w/z 公式内 mu/eta **浮点值**
  重复读；S7-C 落地本体是 eta 改支撑求和。ICOL 只问「`active[0]`
  这一**整数元素装载**可否按模块内不变量（截距列恒居首）折叠为
  常量」——不动标签、不动语句序、不动长度读、不动任何浮点。该结构
  位置（支撑首元素的不变量常量折叠）此前无任何 ID 或探针覆盖，非
  重命名；以五次决斗符号翻转 + 漂移窗口证据 + 贴带不越带 + 算术类
  深亚带关闭，不铸号。
- **SFILL vs COPY / ALLOC / FINTAIL / S8-C-4 / R15-C 零池注记**：
  COPY（R11-C 无号）是 solve **输入防御拷贝**契约封闭；ALLOC 是
  irls 缓冲分配；FINTAIL（R13-C 无号）是尾部 isFinite **检查环**；
  S8-C-4 是对角 eps 卫死码；R15-C 零池注记覆盖 `solution` 的**分配**
  （返回值本体）。SFILL 是该数组 `fill(0)` 的**死存储**本身——从未
  点名；以 0.6 ms 池封顶 + HOLEY 对价 + 决斗符号翻转关闭，不铸号。
- **ITERX vs EXP / S8-C-3 / X1-3**：EXP 是 sigmoid 池算术、S8-C-3
  是收敛侧有限性检查 DCE、X1-3 是二分迭代替换禁令。ITERX 不提任何
  变换——它是**收敛结构普查轴**，把「减迭代」整类钉死在逐位契约
  外。轴无重开语义，不铸号。
- **COLDX vs GCAX / AAFLR / S7-H-3 / S6-I-2**：GCAX 是收集器停顿
  份额、AAFLR 是决斗地板；COLDX 是**编译预热份额**——第三条引擎侧
  观测轴，与前两轴正交；S7-H-3（预热净负）与 S6-I-2（编译缓存 =
  构建面）是它引用的既有关闭，不重开。轴不铸号。
- **PLSEG vs IRLSDP / S1-C-6 / X0-5**：IRLSDP 是 irls 死 `vectors`
  形参；PLSEG 是 prob-add `lastSegment` 死 `_input` 形参 + 项目环
  自引用过滤的**项目不变式**——不同站点、同一「零池纯重构」分类，
  按 IRLSDP 先例记备忘不铸号；S1-C-6 的 kappaS/cell 双算未被重提。
- **FLOORX 重测 vs S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 / CMB 禁令**：
  本轮重测**只因池漂出带**（R14-C 处置规则），产出仅用于天花板差值
  算术；不重跑任何已关 ID 决斗、不构建堆叠候选、不产出可落地形态，
  四个成员 ID 与 CMB 禁令全部维持原状。
- **换名再提交拒列（本轮核实）**：零。R13-C 五个、R14-C 四个、
  R15-C 两个无名微观未重探、未补铸、未换址重提；S10-C-1..3 /
  S12-C-1 / RID / CNT 零重提；AAFLR/GCAX 轴未补铸；嵌套序 × 布局
  组合空间未触碰。

## 5. 测试与验证

环境：Node 22.22.2（nvm + PATH 前置；VM 默认低于 engines）、
pnpm 10.17.1。生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录）
npx tsx /tmp/r16c-anchor.mts                # ✓ 11,709 项逐位 × 5 次运行全绿
                                            #   （53 例 ×{仪表道,落地道,ICOL 道,SFILL 道} 忠实性电池
                                            #    + COLDX 夹具逐位同一性 + ICOL 支撑首位不变量断言
                                            #    + 3 seed × 2 自确定性 + perf 夹具逐位
                                            #    + prob-add 自确定性）
                                            #   正典 A/B/C + 补充 D/E（ICOL 符号稳定性加测）
npx tsx /tmp/r16c-apcfloor.mts              # ✓ APC 地板重锚定加测 × 3（APCX/FLOORX，
                                            #   遵循 R14-C 池漂出带处置规则）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 669.0 ms（1.13× vs 冻结 S6-C ctl 754.3），与 §1
# 生产中位（672.8–686.3）同带。
```

代表性一次测量彩排全量输出（正典 run A；run B/C 数字见 §1 池表与
§2，run D/E 为 ICOL/SFILL 补充读数）：

```text
scenario 1 (four-lane bitwise fidelity, 53 cases x {instrumented, plain-landed, plain-icol, plain-sfill} vs production fitLogitAdditive import)
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)
  shape small (3 models x 3 projects, 120 rows): production median 31.6 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 684.5 ms/report (7 reps)
probe COLDX: first-ever production report in this process 765.0 ms vs steady-state median 684.5 ms -> one-shot JIT/warm-up share 80.5 ms (11.8% of the median; module load/parse excluded, paid at import)
instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):
  total                 722.0 ms
  solveSymmetric        480.4 ms  (66.5%)  [8966 calls]
  irls minus solve      178.4 ms  (24.7%)  [8966 iterations, 3586400 row visits, 1249652 irls sigmoids]
  onProbabilitiesFor    7.1 ms  (1.0%)  [80400 sigmoid+dot]
  APC (all sweeps)      49.5 ms  (6.9%)  [277606 active pairs, 12259 calls]
  bootstrap sampling    1.8 ms  (0.2%)
  sort/percentile tail  2.2 ms  (0.3%)  [12200 draws-map ops]
  base-row projection   0.006 ms  (once/report)
  outside pools (build/supports/keys/base copies) 2.7 ms  (0.4%)
  fits 201
probe ITERX: per-fit IRLS iteration census — 56 fits converged (l2 < 1e-8), 145 capped at maxIter=50, 0 null; iteration quartiles q1=45 med=50 q3=50 max=50
  -> the solve pool (480.4 ms) is linear in the 8966 frozen iterations; any iteration-count reduction (warm starts, TOL/init changes) alters the float sequence — excluded by the bitwise contract, so the convergence-shortening class carries no admissible lane
probe ICOL counts: switch dispatches by support size [s1=0, s2=63324, s3=280467, s4=1210220, s5=2032389] of 3586400 row visits — ICOL-eligible (s2..s5) visits drop exactly one integer element load each
probe ICOL: landed 687.4 ms vs icol 675.7 ms (delta +11.7 ms landed-minus-icol; 6 position-alternated interleaved reps each after warmup)
  landed reps: 688.1 686.1 687.4 760.3 684.2 682.4
  icol reps: 675.4 675.7 674.7 683.8 681.5 672.3
  -> structural bitwise: active[0] === 0 on every row (vec[0]=1 always; supports ascend), invariant checked over the perf fixture; bound: one integer load per dispatched visit = SWORD-scale ±3.6 ms, sub-band ~10x
probe SFILL: landed 690.3 ms vs sfill 691.9 ms (delta -1.6 ms landed-minus-sfill; 6 position-alternated interleaved reps each after warmup)
  landed reps: 683.3 690.3 682.4 683.5 762.5 756.8
  sfill reps: 682.7 684.0 682.9 691.9 761.1 752.4
  -> standalone fill pool: 8,966 fill(0) calls on a 60-slot array = 0.598 ms/report ceiling (median of 6 passes; sink=0.0); the structural counterparty is a HOLEY-kind output array — dead-store proof: back-substitution writes rows n-1..0 before any read, and the isFinite sweep reads only written elements
probe PLSEG: whole fitProbabilityAdditive report (1 scenario x 6 models x 8 projects, 400 rows) median 1.144 ms — bounds diagnose()'s project-invariant pair filter and the dead lastSegment(_input) parameter at zero pool (IRLSDP-class refactor memo, not a lane)

ALL EQUIVALENCE CHECKS PASSED (11709 bitwise checks)
```

其余四次运行的探针行（关键数字）：

```text
run B: prod median 676.5 (small 33.0); COLDX 73.5 (10.9%); ICOL +1.5 (landed 693.9 vs icol 692.4);
       SFILL -7.6 (landed 686.4 vs sfill 694.1); fill pool 0.599; PLSEG 1.144
run C: prod median 686.3 (small 32.9); COLDX 46.9 (6.8%); ICOL +34.7 (landed 758.4 vs icol 723.7;
       双道 rep 漂至 750-780 区间的漂移窗口——landed reps 692.2 694.3 753.9 771.9 758.4 761.7 /
       icol reps 690.6 722.3 723.7 780.5 770.5 699.6); SFILL +2.9; fill pool 0.624; PLSEG 1.188
run D: prod median 677.1 (small 31.4); COLDX 49.8 (7.4%); ICOL +20.3 (landed 695.5 vs icol 675.2;
       landed 含 729.4 漂移 rep); SFILL +5.3; fill pool 0.626; PLSEG 1.148
run E: prod median 672.8; COLDX 55.2 (8.2%); ICOL -4.9 (landed 718.1 vs icol 723.0——符号翻转);
       SFILL +0.8
APC 地板重锚定加测 × 3: APCX 57.8 / 54.6 / 55.3; FLOORX 36.8 / 42.5 / 31.5;
       ceiling 21.0 / 12.1 / 23.8 (全部 < 35; sink=7.309 与 R14-C 逐位同值)
ITERX 与 ICOL counts 在全部五次运行逐位一致（确定性计数）。
```

## 6. 新增排除 ID：无

第十六组候选集为空。ICOL / SFILL 为关闭探针，ITERX / COLDX 为测量
轴，PLSEG 为零池重构备忘，遵循 R11-C..R15-C 先例不铸号；重开条件均
为现实规模/设计结构位移 ≥1 个数量级（不预期），ICOL 另附「安静窗口
符号稳定且越带 + 先复核 AAFLR 地板」条件，ITERX/COLDX 作为轴无重开
语义。ICOL 的五次读数（四正一负、幅度 23× 摆动、最大值出自漂移窗
口）已按 AAFLR 地板校准留档：若后续轮次在同类装载折叠位置观察到
符号稳定且越带的读数，应先复核当窗地板再立道。

## 附录：测量彩排脚本（完整，可复现）

### A. 主彩排 `/tmp/r16c-anchor.mts`

```ts
/**
 * Round 16 / R16-C measurement re-anchor + sixteenth-group closure probes
 * (offline routing slice, sixteenth pass). NOT committed — the sixteenth-
 * group enumeration carries no candidate lanes that reach the acceptance bar;
 * full source goes into the R16-C report appendix (R7-C..R15-C precedent).
 *
 * Discipline (same as R11-C..R15-C):
 *   1. re-anchor the post-S7-C profile on THIS VM: production median
 *      ms/report on the perf shape (400 rows, 6 models x 8 projects,
 *      bootstrap=200) and the small shape, x-checked by an INSTRUMENTED
 *      verbatim pipeline copy (timers/counters only; every float op, value,
 *      and order identical — validated bitwise against the production
 *      import over the full 53-case battery before its numbers are used);
 *   2. derive the per-pool cost table (solve / irls-other / on-prob / APC /
 *      sampling / tail / base-copy) plus site counts, now including a
 *      per-support-size dispatch census (for ICOL arithmetic) and a per-fit
 *      iteration census (ITERX axis);
 *   3. close the sixteenth-group never-named sites (no lanes reach the bar;
 *      all arithmetic/mechanism closures, no IDs minted):
 *        ICOL   the accumulation switch's `active[0]` load is provably the
 *               constant 0 on every row (build() always writes vec[0]=1 and
 *               computeSupports pushes indices ascending, so the intercept
 *               column 0 heads every support). Specializing a0 -> literal 0
 *               in the case 2..5 bodies drops one integer element load per
 *               dispatched row visit; every float op, target, and order is
 *               unchanged (structurally bitwise). Expected duel: jitter
 *               within the R15-C AAFLR floor; arithmetic bound is the
 *               SWORD-scale per-visit ~ns class (sub-band ~10x)
 *        SFILL  the back-substitution output array `new Array(n).fill(0)`
 *               in solveSymmetric is a dead store: rows are written from
 *               n-1 down to 0 and each read solution[k] (k>row) hits an
 *               already-written element; the final isFinite sweep reads
 *               only written elements. R15-C closed the ALLOCATION as the
 *               return value itself; the fill was never named. Standalone
 *               pool: one fill(60) per solve x 8,966 solves. The structural
 *               counterparty (skip the fill) leaves a HOLEY-kind array — a
 *               known element-kind regression — so the lane is measured as
 *               a duel too (expected jitter/negative), and the pool bound
 *               closes it either way
 *   4. NEW AXES this round (R15-A/B/C precedent of adding observability
 *      angles):
 *        ITERX  per-fit IRLS iteration census — converged (l2 < TOL) vs
 *               maxIter-capped fits, iteration quartiles. Documents that
 *               ~2/3 of the report (the solve pool) is linear in a frozen
 *               iteration count: any "fewer iterations" idea (warm starts,
 *               looser TOL, better initialization) changes the float
 *               sequence and is excluded by the bitwise contract, so the
 *               axis closes the convergence-shortening class without a lane
 *        COLDX  JIT-cold first-report share — the FIRST production report
 *               in this process (timed before any other slice call) vs the
 *               steady-state median. Bounds what any in-process warm-up
 *               trick could ever touch (pre-warming only MOVES this cost;
 *               S7-H-3 measured priming as net-negative in another slice;
 *               cross-process code caching is the build face, S6-I-2)
 *   5. PLSEG (note, not a probe): offline-prob-add diagnose()'s project
 *      loop filters interaction pairs with key.endsWith("|" + lastSegment
 *      (key)) — self-referential, so the filter is project-invariant and
 *      recomputes the same pair set for every underperforming project;
 *      lastSegment also carries a dead `_input` parameter (IRLSDP class).
 *      The whole fitProbabilityAdditive report at production scale is
 *      sub-ms (anchored below), so the pool is zero — refactor/semantics
 *      memo for the table owner, not a lane.
 *   6. re-check production self-determinism across 3 seeds x 2 runs.
 *
 * Run: npx tsx /tmp/r16c-anchor.mts
 */

import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.js";
import { fitProbabilityAdditive } from "/workspace/src/routing/offline-prob-add.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import type { AttributionReport, OfflineRow } from "/workspace/src/routing/offline-types.js";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.js";

/* --------------------------- shared constants --------------------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}

interface FitResult {
  readonly coefficients: readonly number[] | null;
}

type SolveImpl = (a: readonly (readonly number[])[], b: readonly number[]) => number[] | null;

type IrlsImpl = (
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
) => FitResult;

function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}

function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/* ---------------- COLDX: JIT-cold first-report, timed FIRST --------------- *
 * This block runs before any other call into the production import, so the
 * timing captures the interpreter/baseline-tier cost of the very first
 * production report in this process (module load/parse already paid at
 * import time — that is the I slice's territory, footnoted in the report). */

function fixtureRngEarly(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function perfRowsWith(r: () => number): OfflineRow[] {
  const scenarios = Array.from({ length: 4 }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: 6 }, (_, i) => `model-${i}`);
  const projects = Array.from({ length: 8 }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < 400; i++) {
    const modelVersion = models[Math.floor(r() * models.length)]!;
    const rate = modelVersion === models[0] ? 0.6 * 0.4 : 0.6;
    rows.push({
      scenarioId: scenarios[Math.floor(r() * scenarios.length)]!,
      modelVersion,
      projectId: projects[Math.floor(r() * projects.length)]!,
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

const COLD_ROWS = perfRowsWith(fixtureRngEarly(0xbeef));
const tCold0 = performance.now();
fitLogitAdditive(COLD_ROWS, { bootstrap: 200, seed: SEED_DEFAULT });
const COLD_FIRST_MS = performance.now() - tCold0;

/* ------------- verbatim production pipeline building blocks -------------- *
 * Transcribed from src/routing/offline-logit.ts and src/routing/lin-alg.ts
 * at the 183df9b-landed state. The instrumented lane's ONLY additions are
 * STATS counter increments and performance.now() reads at call boundaries;
 * no float operation, operand, or ordering differs. The plain lanes (landed,
 * ICOL a0->0 specialization, SFILL fill-free solve) carry no timers at all.
 * Every lane is validated bitwise against the production import over the
 * full 53-case battery before any number is reported. */

const STATS = {
  fits: 0,
  fitsConverged: 0,
  fitsCapped: 0,
  fitsNull: 0,
  perFitIterations: [] as number[],
  sizeCounts: new Array<number>(8).fill(0),
  irlsMs: 0,
  irlsIterations: 0,
  solveCalls: 0,
  solveMs: 0,
  sigmoidIrls: 0,
  rowVisits: 0,
  onProbMs: 0,
  onProbSigmoids: 0,
  apcMs: 0,
  apcActivePairs: 0,
  apcCalls: 0,
  samplingMs: 0,
  drawsMapOps: 0,
  tailMs: 0,
  baseCopyMs: 0,
  totalMs: 0,
  reset(): void {
    this.fits = 0; this.fitsConverged = 0; this.fitsCapped = 0; this.fitsNull = 0;
    this.perFitIterations = []; this.sizeCounts = new Array<number>(8).fill(0);
    this.irlsMs = 0; this.irlsIterations = 0; this.solveCalls = 0;
    this.solveMs = 0; this.sigmoidIrls = 0; this.rowVisits = 0; this.onProbMs = 0;
    this.onProbSigmoids = 0; this.apcMs = 0; this.apcActivePairs = 0;
    this.apcCalls = 0; this.samplingMs = 0; this.drawsMapOps = 0;
    this.tailMs = 0; this.baseCopyMs = 0; this.totalMs = 0;
  }
};

function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);

  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);

  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);

  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });

  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}

function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}

function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}

/* verbatim landed solveSymmetric (lin-alg.ts) parameterized ONLY by whether
 * the back-substitution output array is pre-filled (landed) or left unfilled
 * (SFILL probe — structurally bitwise: rows are written n-1..0 before any
 * read, and the final isFinite sweep reads only written elements). */
function makeSolve(fillVariant: "landed" | "nofill"): SolveImpl {
  const prefill = fillVariant === "landed";
  return (a, b) => {
    const n = b.length;
    if (a.length !== n || a.some((row) => row.length !== n)) {
      throw new DomainValidationError("solveSymmetric requires a square matrix matching b");
    }
    const m: number[][] = a.map((row) => [...row]);
    const x: number[] = [...b];
    const eps = 1e-12;

    for (let col = 0; col < n; col++) {
      let pivotRow = col;
      let pivotAbs = Math.abs(m[col]![col]!);
      for (let row = col + 1; row < n; row++) {
        const abs = Math.abs(m[row]![col]!);
        if (abs > pivotAbs) {
          pivotAbs = abs;
          pivotRow = row;
        }
      }
      if (pivotAbs < eps) return null;
      if (pivotRow !== col) {
        const tmp = m[col]!;
        m[col] = m[pivotRow]!;
        m[pivotRow] = tmp;
        const tb = x[col]!;
        x[col] = x[pivotRow]!;
        x[pivotRow] = tb;
      }
      const colArr = m[col]!;
      const pivot = colArr[col]!;
      const xCol = x[col]!;
      for (let row = col + 1; row < n; row++) {
        const rowArr = m[row]!;
        const factor = rowArr[col]! / pivot;
        if (factor === 0) continue;
        let k = col;
        const stop = n - 3;
        for (; k < stop; k += 4) {
          rowArr[k] = rowArr[k]! - factor * colArr[k]!;
          rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
          rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
          rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
        }
        for (; k < n; k++) {
          rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        }
        x[row] = x[row]! - factor * xCol;
      }
    }

    const solution = prefill ? new Array<number>(n).fill(0) : new Array<number>(n);
    for (let row = n - 1; row >= 0; row--) {
      const rowArr = m[row]!;
      let sum = x[row]!;
      for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
      const diag = rowArr[row]!;
      if (Math.abs(diag) < eps) return null;
      solution[row] = sum / diag;
    }
    for (const value of solution) {
      if (!Number.isFinite(value)) return null;
    }
    return solution;
  };
}

const solveLanded = makeSolve("landed");
const solveNoFill = makeSolve("nofill");

/* Shared preamble/epilogue for the irls lanes. The lanes differ ONLY in
 * (a) instrumentation, (b) which solve variant they call (SFILL probe), and
 * (c) whether the accumulation switch's case bodies read `active[0]` or use
 * the literal 0 (ICOL probe): build() writes vec[0]=1 unconditionally and
 * computeSupports pushes indices in ascending j, so active[0] === 0 on every
 * row — the specialization touches no float op, target, or order. The
 * default case (support sizes outside 2..5) is verbatim in both variants. */

function makeIrls(
  bodyVariant: "landed" | "icol",
  solveImpl: SolveImpl,
  instrumented: boolean
): IrlsImpl {
  return (design, rows, _vectors, supports, keys, keySpace, maxIter) => {
    const tIrls0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.fits += 1;
    const p = design.names.length;
    const n = rows.length;
    const eta = new Array<number>(n).fill(0);
    const mu = new Array<number>(n).fill(0);
    const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
    const xtwz: number[] = new Array<number>(p).fill(0);
    const stamp = new Int32Array(keySpace);
    const etaByKey = new Float64Array(keySpace);
    const muByKey = new Float64Array(keySpace);
    let beta = new Array<number>(p).fill(0);
    let iterationsRun = 0;
    let convergedEarly = false;
    for (let iter = 0; iter < maxIter; iter++) {
      iterationsRun = iter + 1;
      if (instrumented) {
        STATS.irlsIterations += 1;
        STATS.rowVisits += n;
      }
      const mark = iter + 1;
      for (let i = 0; i < n; i++) {
        const key = keys[i]!;
        if (stamp[key] !== mark) {
          stamp[key] = mark;
          if (instrumented) STATS.sigmoidIrls += 1;
          let value = 0;
          const active = supports[i]!;
          for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
          etaByKey[key] = value;
          muByKey[key] = sigmoid(value);
        }
        eta[i] = etaByKey[key]!;
        mu[i] = muByKey[key]!;
      }
      for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
      xtwz.fill(0);
      if (bodyVariant === "landed") {
        for (let i = 0; i < n; i++) {
          const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
          const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
          const wz = w * z;
          const active = supports[i]!;
          if (instrumented) STATS.sizeCounts[Math.min(active.length, 7)] += 1;
          switch (active.length) {
            case 5: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              const a4 = active[4]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
              xtwz[a4] = xtwz[a4]! + wz;
              const r4 = xtwx[a4]!;
              r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
              break;
            }
            case 4: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
              break;
            }
            case 3: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
              break;
            }
            case 2: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
              break;
            }
            default: {
              for (let ai = 0; ai < active.length; ai++) {
                const a = active[ai]!;
                xtwz[a] = xtwz[a]! + wz;
                const rowA = xtwx[a]!;
                for (let bi = 0; bi < active.length; bi++) {
                  const b = active[bi]!;
                  rowA[b] = rowA[b]! + w;
                }
              }
            }
          }
        }
      } else {
        for (let i = 0; i < n; i++) {
          const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
          const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
          const wz = w * z;
          const active = supports[i]!;
          switch (active.length) {
            case 5: {
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              const a4 = active[4]!;
              xtwz[0] = xtwz[0]! + wz;
              const r0 = xtwx[0]!;
              r0[0] = r0[0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[0] = r1[0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[0] = r2[0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[0] = r3[0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
              xtwz[a4] = xtwz[a4]! + wz;
              const r4 = xtwx[a4]!;
              r4[0] = r4[0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
              break;
            }
            case 4: {
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              xtwz[0] = xtwz[0]! + wz;
              const r0 = xtwx[0]!;
              r0[0] = r0[0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[0] = r1[0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[0] = r2[0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[0] = r3[0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
              break;
            }
            case 3: {
              const a1 = active[1]!;
              const a2 = active[2]!;
              xtwz[0] = xtwz[0]! + wz;
              const r0 = xtwx[0]!;
              r0[0] = r0[0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[0] = r1[0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[0] = r2[0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
              break;
            }
            case 2: {
              const a1 = active[1]!;
              xtwz[0] = xtwz[0]! + wz;
              const r0 = xtwx[0]!;
              r0[0] = r0[0]! + w; r0[a1] = r0[a1]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[0] = r1[0]! + w; r1[a1] = r1[a1]! + w;
              break;
            }
            default: {
              for (let ai = 0; ai < active.length; ai++) {
                const a = active[ai]!;
                xtwz[a] = xtwz[a]! + wz;
                const rowA = xtwx[a]!;
                for (let bi = 0; bi < active.length; bi++) {
                  const b = active[bi]!;
                  rowA[b] = rowA[b]! + w;
                }
              }
            }
          }
        }
      }
      for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
      let next: number[] | null;
      if (instrumented) {
        const tSolve0 = performance.now();
        next = solveImpl(xtwx, xtwz);
        STATS.solveMs += performance.now() - tSolve0;
        STATS.solveCalls += 1;
      } else {
        next = solveImpl(xtwx, xtwz);
      }
      if (next === null) {
        if (instrumented) {
          STATS.fitsNull += 1;
          STATS.perFitIterations.push(iterationsRun);
          STATS.irlsMs += performance.now() - tIrls0;
        }
        return { coefficients: null };
      }
      const delta = next.map((value, index) => value - beta[index]!);
      const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
      beta = next;
      if (!beta.every(Number.isFinite)) {
        if (instrumented) {
          STATS.fitsNull += 1;
          STATS.perFitIterations.push(iterationsRun);
          STATS.irlsMs += performance.now() - tIrls0;
        }
        return { coefficients: null };
      }
      if (l2 < TOL) {
        convergedEarly = true;
        break;
      }
    }
    if (instrumented) {
      if (convergedEarly) STATS.fitsConverged += 1;
      else STATS.fitsCapped += 1;
      STATS.perFitIterations.push(iterationsRun);
      STATS.irlsMs += performance.now() - tIrls0;
    }
    return { coefficients: beta };
  };
}

const irlsInstrumented = makeIrls("landed", solveLanded, true);
const irlsLanded = makeIrls("landed", solveLanded, false);
const irlsIcol = makeIrls("icol", solveLanded, false);
const irlsSfill = makeIrls("landed", solveNoFill, false);

function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}

function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}

function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}

function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

/**
 * Full pipeline parameterized by irls lane. instrumented=true additionally
 * times onProb/APC/sampling/tail/baseCopy pools (timers only).
 */
function makeFit(irlsImpl: IrlsImpl, instrumented: boolean) {
  const onProb = (vectors: readonly number[][], coefficients: readonly number[]): number[] => {
    if (!instrumented) return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    const t0 = performance.now();
    const result = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    STATS.onProbMs += performance.now() - t0;
    STATS.onProbSigmoids += vectors.length;
    return result;
  };

  const apc = (
    design: Design,
    rows: readonly Row[],
    vectors: readonly number[][],
    coefficients: readonly number[],
    onProbabilities: readonly number[],
    column: string
  ): number => {
    const t0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.apcCalls += 1;
    let sum = 0;
    const columnIdx = design.columnIndex.get(column);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < rows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        if (instrumented) STATS.apcActivePairs += 1;
        const on = onProbabilities[i]!;
        const offVector = vectors[i]!.slice();
        offVector[columnIdx] = 0;
        const off = sigmoid(dot(coefficients, offVector));
        sum += on - off;
      }
    }
    const result = rows.length === 0 ? 0 : sum / rows.length;
    if (instrumented) STATS.apcMs += performance.now() - t0;
    return result;
  };

  const pushValue = (map: Map<string, number[]>, key: string, value: number): void => {
    if (instrumented) STATS.drawsMapOps += 1;
    const list = map.get(key);
    if (list === undefined) map.set(key, [value]);
    else list.push(value);
  };

  return function fit(
    rows: readonly OfflineRow[],
    options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
  ): AttributionReport {
    const tTotal0 = instrumented ? performance.now() : 0;
    const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
    const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
    const seed = options?.seed ?? SEED_DEFAULT;

    const tBase0 = instrumented ? performance.now() : 0;
    const baseRows: Row[] = rows.map((r) => ({
      scenarioId: r.scenarioId,
      modelVersion: r.modelVersion,
      projectId: r.projectId,
      y: r.y
    }));
    if (instrumented) STATS.baseCopyMs += performance.now() - tBase0;
    const effects: Array<{ name: string; point: number; lcb: number; ucb: number }> = [];

    if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
    }

    const design = buildDesign(baseRows);
    const vectors = baseRows.map((r) => design.build(r));
    const supports = computeSupports(vectors);
    const keys = canonicalRowKeys(baseRows);
    const fit = irlsImpl(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
    if (fit.coefficients === null) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
    }

    const onProbabilities = onProb(vectors, fit.coefficients);
    const pointEffects = new Map<string, number>();
    for (const name of design.names) {
      if (name === "intercept") continue;
      pointEffects.set(
        name,
        apc(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
      );
    }
    for (const ref of design.referenceLevels) {
      if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
        pointEffects.set(`${ref.factor}:${ref.name}`, 0);
      }
    }

    const random = rng(seed);
    const draws = new Map<string, number[]>();
    let successful = 0;
    for (let draw = 0; draw < bootstrapDraws; draw++) {
      const tSample0 = instrumented ? performance.now() : 0;
      const sample: Row[] = [];
      const sampleVectors: number[][] = [];
      const sampleSupports: number[][] = [];
      const sampleKeys: number[] = [];
      for (let i = 0; i < baseRows.length; i++) {
        const index = Math.floor(random() * baseRows.length);
        sample.push(baseRows[index]!);
        sampleVectors.push(vectors[index]!);
        sampleSupports.push(supports[index]!);
        sampleKeys.push(keys[index]!);
      }
      const collapsed = sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1);
      if (instrumented) STATS.samplingMs += performance.now() - tSample0;
      if (collapsed) continue;
      const bootFit = irlsImpl(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
      if (bootFit.coefficients === null) continue;
      successful += 1;
      const sampleOnProbabilities = onProb(sampleVectors, bootFit.coefficients);
      for (const [name] of pointEffects.entries()) {
        const value = apc(
          design,
          sample,
          sampleVectors,
          bootFit.coefficients,
          sampleOnProbabilities,
          name
        );
        pushValue(draws, name, value);
      }
    }

    const tTail0 = instrumented ? performance.now() : 0;
    for (const [name, point] of pointEffects.entries()) {
      const values = draws.get(name) ?? [];
      if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
        return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
      }
      values.sort((a, b) => a - b);
      effects.push({
        name,
        point,
        lcb: percentile(values, 0.025),
        ucb: percentile(values, 0.975)
      });
    }

    const n = baseRows.length;
    const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
    const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
    const muLcb = betaQuantileLcb(muPosterior, 0.05);
    const models = new Set(baseRows.map((r) => r.modelVersion)).size;
    const projects = new Set(baseRows.map((r) => r.projectId)).size;
    const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
    const containsZero = (e: { lcb: number; ucb: number }): boolean =>
      e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
    const interactionsFor = (prefix: string): Array<{ name: string; lcb: number; ucb: number; point: number }> =>
      effects.filter((e) => e.name.startsWith(`w:${prefix}`));

    let diagnosis: AttributionReport["diagnosis"] = "uncertain";
    const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
    if (
      effects.some(
        (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
      )
    ) {
      diagnosis = "model-problem";
    } else if (
      effects.some(
        (e) =>
          e.name.startsWith("v:") &&
          e.lcb < -ATTRIBUTION_EFFECT &&
          effects
            .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
            .every(containsZero)
      )
    ) {
      diagnosis = "project-problem";
    } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
      diagnosis = "interaction-only";
    } else if (scenarioHard) {
      diagnosis = "scenario-hard";
    }

    const report: AttributionReport = {
      estimator: "logit-additive",
      rowsUsed: baseRows.length,
      effects,
      diagnosis,
      reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
      writesActivePointer: false
    };
    if (instrumented) {
      STATS.tailMs += performance.now() - tTail0;
      STATS.totalMs += performance.now() - tTotal0;
    }
    return report;
  };
}

const fitInst = makeFit(irlsInstrumented, true);
const fitPlain = makeFit(irlsLanded, false);
const fitIcol = makeFit(irlsIcol, false);
const fitSfill = makeFit(irlsSfill, false);

/* ------------------------------ harness ------------------------------ */

let checksPassed = 0;
let failures = 0;

function check(label: string, ok: boolean): void {
  if (ok) checksPassed++;
  else {
    failures++;
    process.stderr.write(`FAIL ${label}\n`);
  }
}

function compareReports(label: string, expected: AttributionReport, actual: AttributionReport): void {
  check(`${label}.estimator`, expected.estimator === actual.estimator);
  check(`${label}.rowsUsed`, expected.rowsUsed === actual.rowsUsed);
  check(`${label}.diagnosis`, expected.diagnosis === actual.diagnosis);
  check(`${label}.reason`, expected.reason === actual.reason);
  check(`${label}.writesActivePointer`, expected.writesActivePointer === actual.writesActivePointer);
  check(`${label}.effects.length`, expected.effects.length === actual.effects.length);
  if (expected.effects.length === actual.effects.length) {
    for (let i = 0; i < expected.effects.length; i++) {
      const e = expected.effects[i]!;
      const g = actual.effects[i]!;
      check(`${label}.effects[${i}].name`, e.name === g.name);
      check(`${label}.effects[${i}].point`, Object.is(e.point, g.point));
      check(`${label}.effects[${i}].lcb`, Object.is(e.lcb, g.lcb));
      check(`${label}.effects[${i}].ucb`, Object.is(e.ucb, g.ucb));
    }
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}

function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRow[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function allUniqueRows(r: () => number, count: number): OfflineRow[] {
  const rows: OfflineRow[] = [];
  for (let i = 0; i < count; i++) {
    rows.push({
      scenarioId: `s${i % 2}`,
      modelVersion: `m${i}`,
      projectId: `p${i}`,
      y: r() < 0.6 ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function batteryCases(): Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];

  for (let i = 0; i < 40; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, {
      scenarios: 2,
      models: 3,
      projects: 3,
      rows: 60,
      passRate: 0.55,
      pipeInModel: true,
    }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  cases.push({
    rows: allUniqueRows(r, 36),
    options: { bootstrap: 40, seed: 23 },
  });
  {
    const oneKey: OfflineRow[] = [];
    for (let i = 0; i < 30; i++) {
      oneKey.push({
        scenarioId: "s0",
        modelVersion: "m0",
        projectId: "p0",
        y: i % 3 === 0 ? 0 : 1,
        occurredAtMs: 1_000 + i,
      });
    }
    cases.push({ rows: oneKey, options: { bootstrap: 40, seed: 31 } });
  }
  cases.push({
    rows: [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1, occurredAtMs: 1_000 },
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 0, occurredAtMs: 1_001 },
    ],
    options: { bootstrap: 30, seed: 5 },
  });
  return cases;
}

const out = (line: string): void => {
  process.stdout.write(`${line}\n`);
};

function perfRows(): OfflineRow[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, {
    scenarios: 4,
    models: 6,
    projects: 8,
    rows: 400,
    passRate: 0.6,
  });
}

const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------- scenario 0: COLDX fixture identity + support invariant --------- */

function scenarioColdAndInvariant(): void {
  // The COLDX rows were built by an inlined copy of the perf fixture (it had
  // to run before any other slice call). Verify the copy is byte-identical
  // to the canonical fixture so the cold timing is on the same workload.
  const canonical = perfRows();
  check("R16C-coldx.rows-length", COLD_ROWS.length === canonical.length);
  let same = COLD_ROWS.length === canonical.length;
  for (let i = 0; same && i < canonical.length; i++) {
    const a = COLD_ROWS[i]!;
    const b = canonical[i]!;
    same =
      a.scenarioId === b.scenarioId &&
      a.modelVersion === b.modelVersion &&
      a.projectId === b.projectId &&
      a.y === b.y &&
      a.occurredAtMs === b.occurredAtMs;
  }
  check("R16C-coldx.rows-bitwise", same);

  // ICOL premise: every support starts with column 0 (the intercept). Verify
  // across the whole perf fixture.
  const baseRows: Row[] = canonical.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  check(
    "R16C-icol.support-head-invariant",
    supports.every((s) => s.length >= 1 && s[0] === 0)
  );
}

/* --------- scenario 1: four-lane bitwise fidelity battery --------- */

function scenarioEquivalence(): void {
  const cases = batteryCases();
  for (const [index, testCase] of cases.entries()) {
    const expected = fitLogitAdditive(testCase.rows, testCase.options);
    compareReports(`R16C-inst[${index}]`, expected, fitInst(testCase.rows, testCase.options));
    compareReports(`R16C-plain[${index}]`, expected, fitPlain(testCase.rows, testCase.options));
    compareReports(`R16C-icol[${index}]`, expected, fitIcol(testCase.rows, testCase.options));
    compareReports(`R16C-sfill[${index}]`, expected, fitSfill(testCase.rows, testCase.options));
  }
  out(
    `scenario 1 (four-lane bitwise fidelity, ${cases.length} cases x {instrumented, plain-landed, plain-icol, plain-sfill} ` +
      `vs production fitLogitAdditive import)`
  );
}

/* -------- scenario 2: production self-determinism across 3 seeds -------- */

function scenarioDeterminism(): void {
  const r = fixtureRng(0xd16c);
  const rows = randomRows(r, { scenarios: 3, models: 4, projects: 5, rows: 150, passRate: 0.6 });
  for (const seed of [101, 202, 303]) {
    const first = fitLogitAdditive(rows, { bootstrap: 60, seed });
    const second = fitLogitAdditive(rows, { bootstrap: 60, seed });
    compareReports(`R16C-det[seed=${seed}]`, first, second);
  }
  out("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)");
}

/* ------------- scenario 3: profile re-anchor + pool table + axes ------------- */

function scenarioProfile(): void {
  const smallRows = randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
  {
    const times: number[] = [];
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(smallRows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape small (3 models x 3 projects, 120 rows): production median ${median(times).toFixed(1)} ms/report (3 reps)`);
  }

  {
    const rows = perfRows();
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    const med = median(times);
    out(`  shape perf (6 models x 8 projects, 400 rows): production median ${med.toFixed(1)} ms/report (7 reps)`);
    out(
      `probe COLDX: first-ever production report in this process ${COLD_FIRST_MS.toFixed(1)} ms vs steady-state median ` +
        `${med.toFixed(1)} ms -> one-shot JIT/warm-up share ${(COLD_FIRST_MS - med).toFixed(1)} ms ` +
        `(${(((COLD_FIRST_MS - med) / med) * 100).toFixed(1)}% of the median; module load/parse excluded, paid at import)`
    );
  }

  // Instrumented pool table on the perf shape (3 runs; medians reported).
  const rows = perfRows();
  const expected = fitLogitAdditive(rows, PERF_OPTIONS);
  const runs: Array<Record<string, number>> = [];
  let iterCensus: { converged: number; capped: number; nulls: number; q1: number; med: number; q3: number; max: number } | null = null;
  let sizeCensus: number[] = [];
  for (let rep = 0; rep < 3; rep++) {
    STATS.reset();
    const actual = fitInst(rows, PERF_OPTIONS);
    if (rep === 0) {
      compareReports("R16C-perf-fixture", expected, actual);
      const sorted = [...STATS.perFitIterations].sort((a, b) => a - b);
      iterCensus = {
        converged: STATS.fitsConverged,
        capped: STATS.fitsCapped,
        nulls: STATS.fitsNull,
        q1: sorted[Math.floor(sorted.length * 0.25)]!,
        med: sorted[Math.floor(sorted.length * 0.5)]!,
        q3: sorted[Math.floor(sorted.length * 0.75)]!,
        max: sorted[sorted.length - 1]!,
      };
      sizeCensus = [...STATS.sizeCounts];
    }
    runs.push({
      totalMs: STATS.totalMs, irlsMs: STATS.irlsMs, solveMs: STATS.solveMs,
      onProbMs: STATS.onProbMs, apcMs: STATS.apcMs, samplingMs: STATS.samplingMs,
      tailMs: STATS.tailMs, baseCopyMs: STATS.baseCopyMs,
      fits: STATS.fits, irlsIterations: STATS.irlsIterations, solveCalls: STATS.solveCalls,
      sigmoidIrls: STATS.sigmoidIrls, rowVisits: STATS.rowVisits,
      onProbSigmoids: STATS.onProbSigmoids, apcActivePairs: STATS.apcActivePairs,
      apcCalls: STATS.apcCalls, drawsMapOps: STATS.drawsMapOps,
    });
  }
  const med = (key: string): number => median(runs.map((r) => r[key]!));
  const total = med("totalMs");
  const solveMs = med("solveMs");
  const irlsOther = med("irlsMs") - solveMs;
  const rest = total - med("irlsMs") - med("onProbMs") - med("apcMs") - med("samplingMs") - med("tailMs");
  out(`instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):`);
  out(`  total                 ${total.toFixed(1)} ms`);
  out(`  solveSymmetric        ${solveMs.toFixed(1)} ms  (${(100 * solveMs / total).toFixed(1)}%)  [${med("solveCalls")} calls]`);
  out(`  irls minus solve      ${irlsOther.toFixed(1)} ms  (${(100 * irlsOther / total).toFixed(1)}%)  [${med("irlsIterations")} iterations, ${med("rowVisits")} row visits, ${med("sigmoidIrls")} irls sigmoids]`);
  out(`  onProbabilitiesFor    ${med("onProbMs").toFixed(1)} ms  (${(100 * med("onProbMs") / total).toFixed(1)}%)  [${med("onProbSigmoids")} sigmoid+dot]`);
  out(`  APC (all sweeps)      ${med("apcMs").toFixed(1)} ms  (${(100 * med("apcMs") / total).toFixed(1)}%)  [${med("apcActivePairs")} active pairs, ${med("apcCalls")} calls]`);
  out(`  bootstrap sampling    ${med("samplingMs").toFixed(1)} ms  (${(100 * med("samplingMs") / total).toFixed(1)}%)`);
  out(`  sort/percentile tail  ${med("tailMs").toFixed(1)} ms  (${(100 * med("tailMs") / total).toFixed(1)}%)  [${med("drawsMapOps")} draws-map ops]`);
  out(`  base-row projection   ${med("baseCopyMs").toFixed(3)} ms  (once/report)`);
  out(`  outside pools (build/supports/keys/base copies) ${rest.toFixed(1)} ms  (${(100 * rest / total).toFixed(1)}%)`);
  out(`  fits ${med("fits")}`);
  if (iterCensus !== null) {
    out(
      `probe ITERX: per-fit IRLS iteration census — ${iterCensus.converged} fits converged (l2 < 1e-8), ` +
        `${iterCensus.capped} capped at maxIter=50, ${iterCensus.nulls} null; iteration quartiles ` +
        `q1=${iterCensus.q1} med=${iterCensus.med} q3=${iterCensus.q3} max=${iterCensus.max}`
    );
    out(
      `  -> the solve pool (${solveMs.toFixed(1)} ms) is linear in the ${med("irlsIterations")} frozen iterations; ` +
        `any iteration-count reduction (warm starts, TOL/init changes) alters the float sequence — excluded by the ` +
        `bitwise contract, so the convergence-shortening class carries no admissible lane`
    );
  }
  out(
    `probe ICOL counts: switch dispatches by support size [s1=${sizeCensus[1]}, s2=${sizeCensus[2]}, s3=${sizeCensus[3]}, ` +
      `s4=${sizeCensus[4]}, s5=${sizeCensus[5]}] of ${med("rowVisits")} row visits — ICOL-eligible (s2..s5) visits drop ` +
      `exactly one integer element load each`
  );
}

/* --------- scenario 4: sixteenth-group closure probes (no lanes) --------- */

function duel(
  label: string,
  lhs: (rows: readonly OfflineRow[], options?: { maxIter?: number; bootstrap?: number; seed?: number }) => AttributionReport,
  rhs: (rows: readonly OfflineRow[], options?: { maxIter?: number; bootstrap?: number; seed?: number }) => AttributionReport,
  lhsName: string,
  rhsName: string,
  rows: OfflineRow[]
): void {
  lhs(rows, PERF_OPTIONS); // warmup lhs
  rhs(rows, PERF_OPTIONS); // warmup rhs
  const lhsTimes: number[] = [];
  const rhsTimes: number[] = [];
  for (let rep = 0; rep < 6; rep++) {
    if (rep % 2 === 0) {
      let t0 = performance.now();
      rhs(rows, PERF_OPTIONS);
      rhsTimes.push(performance.now() - t0);
      t0 = performance.now();
      lhs(rows, PERF_OPTIONS);
      lhsTimes.push(performance.now() - t0);
    } else {
      let t0 = performance.now();
      lhs(rows, PERF_OPTIONS);
      lhsTimes.push(performance.now() - t0);
      t0 = performance.now();
      rhs(rows, PERF_OPTIONS);
      rhsTimes.push(performance.now() - t0);
    }
  }
  const lhsMed = median(lhsTimes);
  const rhsMed = median(rhsTimes);
  const delta = lhsMed - rhsMed;
  out(
    `probe ${label}: ${lhsName} ${lhsMed.toFixed(1)} ms vs ${rhsName} ${rhsMed.toFixed(1)} ms ` +
      `(delta ${(delta >= 0 ? "+" : "") + delta.toFixed(1)} ms ${lhsName}-minus-${rhsName}; ` +
      `6 position-alternated interleaved reps each after warmup)`
  );
  out(`  ${lhsName} reps: ${lhsTimes.map((t) => t.toFixed(1)).join(" ")}`);
  out(`  ${rhsName} reps: ${rhsTimes.map((t) => t.toFixed(1)).join(" ")}`);
}

function scenarioProbes(): void {
  const rows = perfRows();

  // ICOL: landed case bodies (read active[0]) vs a0 -> literal 0. Structurally
  // bitwise (same targets, same float ops, same order; only an integer element
  // load is dropped). Arithmetic bound: one ~ns-class load per dispatched row
  // visit — the SWORD arithmetic (±1 ns x 3.59M visits = ±3.6 ms), sub-band
  // ~10x. Duel expected inside the R15-C AAFLR floor (±1.5 ms) or deep
  // sub-band either way.
  duel("ICOL", fitPlain, fitIcol, "landed", "icol", rows);
  out(
    `  -> structural bitwise: active[0] === 0 on every row (vec[0]=1 always; supports ascend), invariant checked ` +
      `over the perf fixture; bound: one integer load per dispatched visit = SWORD-scale ±3.6 ms, sub-band ~10x`
  );

  // SFILL: landed solve (solution prefilled with 0) vs fill-free solve. The
  // fill is a provable dead store; skipping it leaves a HOLEY-kind output
  // array (known V8 element-kind regression), so the duel's expected outcome
  // is jitter or a negative for the fill-free lane. The standalone pool bound
  // closes the site regardless.
  duel("SFILL", fitPlain, fitSfill, "landed", "sfill", rows);
  {
    // Standalone pool: one fill(60) per solve x 8,966 solves/report.
    const n = 60;
    const buf = new Array<number>(n).fill(0);
    let sink = 0;
    for (let rep = 0; rep < 2; rep++) {
      for (let it = 0; it < 8966; it++) buf.fill(0);
      sink += buf[59]!;
    }
    const passMs: number[] = [];
    for (let rep = 0; rep < 6; rep++) {
      const t0 = performance.now();
      for (let it = 0; it < 8966; it++) buf.fill(0);
      passMs.push(performance.now() - t0);
      sink += buf[0]!;
    }
    out(
      `  -> standalone fill pool: 8,966 fill(0) calls on a 60-slot array = ${median(passMs).toFixed(3)} ms/report ` +
        `ceiling (median of 6 passes; sink=${sink.toFixed(1)}); the structural counterparty is a HOLEY-kind output ` +
        `array — dead-store proof: back-substitution writes rows n-1..0 before any read, and the isFinite sweep ` +
        `reads only written elements`
    );
  }

  // PLSEG context anchor: the whole fitProbabilityAdditive report at a
  // production-like single-scenario shape (prob-add requires exactly one
  // scenario). Sub-ms whole-report time bounds every site inside it,
  // including diagnose()'s project-invariant pair filter and lastSegment's
  // dead `_input` parameter (IRLSDP-class refactor memo, zero perf pool).
  {
    const r = fixtureRng(0x9add);
    const single = randomRows(r, { scenarios: 1, models: 6, projects: 8, rows: 400, passRate: 0.55 });
    const first = fitProbabilityAdditive(single);
    const second = fitProbabilityAdditive(single);
    compareReports("R16C-plseg-selfdet", first, second);
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitProbabilityAdditive(single);
      times.push(performance.now() - t0);
    }
    out(
      `probe PLSEG: whole fitProbabilityAdditive report (1 scenario x 6 models x 8 projects, 400 rows) median ` +
        `${median(times).toFixed(3)} ms — bounds diagnose()'s project-invariant pair filter and the dead ` +
        `lastSegment(_input) parameter at zero pool (IRLSDP-class refactor memo, not a lane)`
    );
  }
}

/* --------------------------------- main --------------------------------- */

scenarioColdAndInvariant();
scenarioEquivalence();
scenarioDeterminism();
scenarioProfile();
scenarioProbes();

if (failures > 0) {
  process.stderr.write(`${failures} CHECK(S) FAILED\n`);
  process.exitCode = 1;
} else {
  out(`\nALL EQUIVALENCE CHECKS PASSED (${checksPassed} bitwise checks)`);
}
```

### B. APC 地板重锚定加测 `/tmp/r16c-apcfloor.mts`

```ts
/**
 * R16-C addendum: APC pool floor re-anchor on THIS VM (R14-C APCX/FLOORX
 * method, applied because the instrumented APC pool reading 48.5-50.9 ms
 * drifted BELOW the R14-C re-anchored band 53.0-55.6 — R14-C's disposition
 * rule re-anchors the floor whenever the pool exits the band). This is a
 * floor measurement, NOT a lane: S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 stay
 * closed and no stacked candidate is built (CMB stays closed).
 *
 * Run: npx tsx /tmp/r16c-apcfloor.mts
 *
 * Structure: rebuild the perf fixture (fixtureRng(0xbeef), 4x6x8, 400 rows),
 * run the verbatim landed base fit (irls rolled accumulation + production
 * solveSymmetric import) to obtain base-fit coefficients, then time
 *   APCX   the verbatim APC sweep (slice + full dot) standalone, 60 reps
 *   FLOORX the cheapest bitwise-legal sweep (S8-C-2-type support-ascending
 *          off sum skipping the contrast column), 60 reps
 * and report pool, floor, and ceiling = pool - floor per report (x201 fits).
 * Three runs of this script produced (this VM):
 *   APCX 57.8 / 54.6 / 55.3 ms; FLOORX 36.8 / 42.5 / 31.5 ms;
 *   ceiling 21.0 / 12.1 / 23.8 ms — all < 35; sink=7.309 matches R14-C
 *   bitwise (cross-round determinism witness).
 * Full source follows the R14-C appendix APCX/TIMER/FLOORX block verbatim
 * except that solveSymmetric is the production import and the sweep-rep
 * count is 60; omitted here for length — the three output lines above are
 * the complete addendum evidence.
 */
```

MORE_OPTIMA=no
BRANCH=cursor/r16-c-offline-routing-sixteenth-pass-83a1
