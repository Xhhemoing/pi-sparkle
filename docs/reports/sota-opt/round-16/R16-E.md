MODEL_SLUG=claude-fable-5-thinking-xhigh

# R16-E：`src/learning/` 第十六遍复查报告（Round 16）

**战役:** 全库持久 SOTA 优化 Round 16 / R16-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `53e1284`（含 R16-B 空枚举摄取与本派单）
**分支:** `cursor/r16-e-learning-sixteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第十六组新角度枚举为空（有理由的
空枚举，未铸造任何 S16-E-\* ID，与 R10-E..R15-E 同口径），切片维持关闭
（MORE_OPTIMA=no）。本轮把 R16-A 的「payload 形态轴」方法移植到本切片并
新增一条本切片首创的「跨 run 累积状态轴」——十五轮全部锚点都只对每 run
输入规模（E/S/分配/协议噪声）测量，从未对切片每 run 重读的持久累积状态
（bandit arms、registry candidates）与 schema 不封顶的配置载荷
（policy 基数、自由文本字节）标定过穿越点。两轴共六个维度全部给出实测
穿越点或结构性封闭，其中最近的悬崖（registry candidates 双载入形态
C\* ≈ 927–1,067）位于 D 切片辖区的 O(C) 快照解析上，本切片辖区内无对应
候选。**

1. **零漂移六重确认**：切片 10 个文件（`wc -l` 确认恰 10 文件、合计 1770
   行，与 R14-E/R15-E 逐字节相同）自 R1-E 裁决基线（`adb20d7`）至本轮基线
   （`53e1284`）**逐字节未变**——`git diff adb20d7..HEAD -- src/learning/`
   为 0 行、`git log adb20d7..HEAD -- src/learning/` 为 **0 提交**，十六轮
   全程无人触及该目录。且 R15-E 基线（`d148d12`）之后的全部提交（R15-E..J
   摄取、Round 15 收口、Round 16 开轮、R16-A/B 摄取与 R16-C/D/E 派单）
   **仅触及 `docs/reports/sota-opt/`**——`git log d148d12..HEAD --name-only`
   非 docs 路径为零，`git diff --stat d148d12..HEAD -- src/ test/` 为空。
   R1-E..R15-E 的全部规模测量、调用面图景与 S1-E-1..8 / S2-E-1..7 /
   S3-E-1..5 / S4-E-1..3 / S5-E-1..5 / S6-E-1..5 / S7-E-1..5 / S8-E-1..3 /
   S9-E-1..3 共 44 项切片排除裁决（R10-E..R15-E 未铸 ID，其空枚举收口一并
   继承，含 R13-E 三处无名微观、R14-E 规模越线/JIT 双轴、R15-E GC/分配与
   A/A 地板双轴）原样成立、原样继承。
2. **本 VM 锚点/矩阵重锚（未拷贝 R15-E 数字，遵派单令；同 VM 双 Node 版本
   交叉，延续 R13-E..R15-E 协议）**：SLICE-CPU 总量上界锚点 **16 次运行**
   （8 × Node v22.14.0 + 8 × Node v22.22.2，engines `>=22.19.0` 仅后者
   合规）**19.0–22.0µs/run**（collect 14.9–17.0µs、outcomes 2.3–2.7µs、
   diagnose 0.87–1.22µs、bandit-build 0.7–1.2µs；两版本同带）——距落地线
   （≥10ms）**455–527×**，即使把切片 CPU 清零也远不达门槛，在历史带
   （~11–24µs/run）之内，且完整落在 R14-E 带（17.2–19.2）与 R15-E 带
   （20.9–22.4）的包络之间：本轮夹具 collected=17（R14-E 15 / R15-E 21），
   collect 读数随构成单调居中（15.0–17.0µs），与 R15-E §1 已裁的构成效应
   同向一致，无需另做种子比对运行。按历史最保守合成口径（R7-E outcomes 带
   7.0–7.3µs + R12-E diagnose 分组上界 3.83–3.92µs 同时替换本轮两项）总量
   ~25.9–30.1µs，仍 **≥332×** 低于门槛。配置态 live 面：
   `applyLearnedRouting` 非空策略 **269–294ns/task**（R13-E 258–312 /
   R14-E 256–303 / R15-E 258–298 同带）、空策略 **111–128ns/task**
   （R15-E 110–125 同带）；`loadLearnedRouting` 全新 root 25.1–37.7µs、
   现实 registry **76.6–84.8µs**（R8-E 80–85 / R15-E 76.9–85.0 同带，
   亚 ms 一次性）。全部确定性 VERDICT 行 16 次运行**逐位一致**
   （`E=41 collected=17 routedE=41 outcomes=2 S=12 diagnoseGroups=1
   prevArms=3`），3/3 sanity 检查全过（§1、附录 A）。
3. **配置态 × 命令类矩阵复核无洞**：全部切片进入点行号逐条 rg 复核，
   **与 R8-E..R15-E 记录逐一相同、无新增调用方、无新增命令类**
   （`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
   `run/flowchart-run:681/712`、`track/loop:88/172`；resume/replay 经
   `flowchartForSupervisor` @ `flowchart-run:880/1021` 各单次载入；
   `run --flowchart` @ `cli/main:580` 在 `:708` 载入前返回，无双载入）。
   S8-E-1 双载入按令保护，未做任何去重分析（新轴 A2 只测其成本形态）。
   切片外消费者以模块路径精确检索复核：`adaptation/eval-routing.ts:223`
   仅 `parseLearnedRoutingPolicy`（离线 eval 面，D 切片辖区）、
   `track/primary-split.ts:3` 类型导入、`privacy/record-classes.ts:212/224`
   文档字符串、`adaptation/approval-profile.ts:44` docstring 假阳性
   （R14-E 已裁）——**消费者集合与 R15-E 逐一相同，无新增调用边**。
4. **本轮两条实扫新轴（§2 前置）**：(a) **跨 run 累积状态轴**（本切片
   首创；不同于 R14-E 的每 run 输入规模轴）：bandit arms 单调只增
   （`new Set(previous?.arms ?? [])` ∪ 本 run modelIds，从不剪枝），每 run
   全量重建 + 序列化 + 读回均 O(A)——阶梯 A∈{3..10⁴} 六次运行标定
   **A\*(10ms) ≈ 6,571–6,925 arms**（生产 A=3 的 ~3.3–3.4 个量级；目录
   M≤10 时代级流失也只到 10² 级）、A\*(50ms) ≈ 34,176–36,890；registry
   candidates 每 distinct contentHash 累积一个且切片从不回收——阶梯
   C∈{1..10³} 标定载入面斜率 4,659–5,372 ns/candidate，
   **C\*(10ms/run) ≈ 1,857–2,141（单载入）/ 927–1,067（S8-E-1 双载入
   形态）**——全切片史上最近悬崖（~3.0–3.3 个量级），但 O(C) 全部落在
   `loadAdaptationRegistry` 快照解析（`adaptation/`，D 切片辖区）；本
   切片辖区内的 in-lock `candidatesFor().find` 仅 21.9–25.1ns/candidate
   （穿越需 ~4.0–4.6×10⁵ candidates）。(b) **配置载荷形态轴**（R16-A
   方法移植）：`parseLearnedRoutingPolicy` 对 avoid/prefer 数组长度
   **不设上限**（N=10⁴ 实测过真实 parser 往返）——apply 斜率
   61.2–63.0 ns/entry/task，**10ms/run 穿越需 |avoid| ≈ 16,404–16,973
   （生产 ≤10 的 ~3.2 个量级）**；同一阶梯首次量化 **S13-B-1 重开条件
   （本体成本 ≥2 个量级 ≈ ≥27µs/task）折合 |avoid| ≈ 965–1,063**——
   生产写入方结构性有界（optimizedPolicy ≤ 失败 (project,model) 组数
   ≤ M；policyFromOutcomes ≤ M×8 家族对），重开条件不可从生产写入方
   触发；自由文本字节（answer/summary/subagent text 上游仅查非空，
   schema 开放）三站点斜率 8.9–11.3 ns/char，**T\*(10ms) ≈ 0.885–1.12M
   字符（生产 10¹–10² 的 ~3.6–4.0 个量级）**。两轴六维度全部收口，
   不产生任何新候选。
5. **第十六组新角度枚举为空（§2）**：本轮新透镜为「**累积状态 × 配置
   载荷形态**」（两条测量轴的代码侧对偶：逐函数问 (a) 哪些位点每 run
   重走跨 run 累积状态、其重走消除是否在本切片辖区且能越线；(b) 哪些
   位点对 schema 不封顶载荷做逐项走查、其特化是否等价且能越线）。对
   第十六遍全量重读的 10 个源文件逐函数普查：全部命中位点落在被点名
   排除保护（R13-E 无名微观 #1/#2、S1-D-2、S2-E-3/4、S2-E-5、S1-E-3、
   S7-E-1/2、S13-B-1 边界、S8-E-1）、切片外辖区（D 切片载入面、C 切片
   `routing/bandit.ts` 内核、数据面 bandit.json）或落地线算术封死区
   （全部穿越点 ≥3 个量级）。**未铸造任何 S16-E-\* 新 ID，无新增排除
   提议；S13-B-1 未以任何形态重提（applyLearnedRouting 内部零改动，
   其重开条件本轮首次量化且未触发）；R13-E 三处无名微观未补铸；
   R14-E/R15-E 四轴未复铸。**
6. **验证面**：`pnpm gate` 在 engines 合规的 Node v22.22.2 上**全绿**
   （typecheck ✓ / lint ✓ / test **1168 pass / 0 fail / 1 skipped**（共
   1169，与 R14-E/R15-E 计数逐一相同）/ build ✓）。切片相关套件
   **48 tests / 2 suites / 48 pass / 0 fail**（两个 Node 版本均过）。
   系统默认 node 仍为 v22.14.0（违反 engines `>=22.19.0`，R12-E §0 起
   连续记录的同一环境事实）；gate 结论以 v22.22.2 为准。零代码改动下
   全部硬不变量天然满足。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与
双归因未动；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故
未提交任何 sim/锚点脚本（差异面与新轴脚本测量段进附录 A，遵守
R7-F/R8-C/R9-I/R10-E..R15-E 无赢家纪律）。本切片在其输出契约与数据面语义
下维持 SOTA——第十六遍复查确认：**剩余的全部 ms 级余量仍全部在被排除表
点名保护的 I/O 契约面或切片外辖区上**，切片级收口条件（R3-E §7 …
R15-E §6）依然成立。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行）本轮第十六遍全量实际读码，未依赖前
  十五轮记忆。上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、
  `protocol/`、`privacy/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表 546 行，含
  S8-E-1 / S9-E-2 / S13-B-1）→ round-16/PLAN.md → round-15/R15-E.md
  （空枚举）→ round-14/R14-E.md（空枚举）→ 10 个源文件。R16-C / R16-D 在
  其他分支在跑，未等待、未触碰其报告与分支。
- 基线漂移检查（§结论 1）：切片零提交零 diff；`d148d12..53e1284` 全部
  提交仅触及 `docs/reports/sota-opt/`——前十五轮全部测量与裁决原样成立。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S15-\* 条目。特别地：
  - **S13-B-1**（B 切片辖区）未以任何变体重提：本切片辖区只有被调方
    `applyLearnedRouting` 本体，本轮对其**零改动**（切片零 diff 下必然）；
    新轴 B1 只经公开函数从外部测量，并**首次量化其重开条件**：本体成本
    增长 ≥2 个量级（≥~27µs/task，对 258–312ns 谱系锚点）折合
    |avoid| ≈ 965–1,063——生产写入方有界（≤M×8），条件未触发、不可从
    生产侧触发；fat 形态只能经人工撰写内容 + proposal-first 人工晋升
    进入，届时按 S13-B-1 行内既备设计重开即可。
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护，本轮仅复核其行号与频次
    （§1）并在新轴 A2 中把其成本形态作为「双载入 = C\* 减半」计入穿越点
    （927–1,067 vs 1,857–2,141），未做任何去重分析。并发 promote 下的
    新鲜性仍是其保留理由。
  - **S9-E-2**（pass-2 预过滤负优化）未触碰：本轮未做任何两遍结构的
    工作集缩减变体；两遍语义按硬不变量维持。
  - **R13-E 三处无名微观**（bandit `includes` 旁路已建 arms Set @
    `bandit-store:72`、apply avoided-Set filter+map 双中间数组 @
    `learned-routing:198-202`、`routingPolicyIdentity` 字面量再校验 @
    `learned-routing:43`）：位点本轮逐字复核未变（零 diff 下必然），
    维持 R13-E 裁决，**未补铸 ID、未重开**；新轴旁证：即使 arms 累积到
    A\*=6.6k，重走主体也在 `createBanditState`/`recordReward`
    （`routing/bandit.ts`，C 切片辖区）与 Record 建表上，`includes` 旁路
    的可得份额仍是 ns 级。
  - **R14-E 规模越线/JIT 双轴与 R15-E GC/分配、A/A 地板双轴未复铸**：
    在 src/test 零漂移下原样继承（派单明令勿复铸）；本轮两条新轴
    （累积状态、载荷形态）为不同测量维度，无交叠——R14-E 的 E\* 是
    「每 run 输入事件数」，本轮 A\*/C\* 是「跨 run 累积状态基数」，
    本轮 T\* 是「单事件字节数」（R14-E §2 行 4 对字符串管道只做了
    机制类拒列，从未标定穿越点）。
  - R10-E..R15-E 未铸 S10..S15-E-\*，本轮亦未事后发明任何此类编号；
    R16-A / R16-B 空枚举（各自辖区）与本切片无交，仅继承 R16-A 的
    **payload 形态轴方法**作为新轴 B 的协议先例。
- **换名重提检查**：本轮新定位形态的换名检查全部并入 §2 表格逐行给出，
  并逐一对照 S1-E-\* … S9-E-\* 全表、S13-B-1、R10-E..R15-E 六轮空枚举面
  （其换名裁决全部原样维持，不再复抄）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  稀疏 → 已批准 R0 基线；双 LCB / 双归因在位；晋升 proposal-first；无
  阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；fail-open / 隐藏
  模块缓存（X1-1）未引入；bandit/learned registry I/O 的 fsync 与完整性
  重哈希未触碰；corrupt registry fail-closed 未触碰；两遍
  `collectSignalsFromEvents` 语义未触碰；X0-3 维持；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未
  force-push；仅提交并推送本分支上的本报告一个文件。
- 环境：本 VM 系统默认 node v22.14.0（`/exec-daemon/node`，PATH 前置
  遮蔽 nvm），nvm 另装 v22.22.2（engines `>=22.19.0` 仅后者合规）。锚点
  与两条新轴在两个版本各测（§1/§2，同带）；`pnpm gate` 以 v22.22.2 为准
  （§4）。依赖经 `pnpm install --frozen-lockfile` 安装（pnpm 10.17.1）。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

R7-I 教训（默认态夹具可能掩盖配置态主路径）按令复查。src 自 `d148d12` 起
一行未动（且自 `85b04ba` 起 src/test 累计零 diff 的 R14-E/R15-E 论证链
原样有效），故 R8-E..R15-E §1 矩阵表结构原样成立；本轮逐条 rg 复核全部
切片进入点行号，**与 R15-E 记录逐一相同、无新增调用方**：

| 命令类 × 配置态 | 切片进入点（本轮行号复核） | 频次与规模 |
| --- | --- | --- |
| `adapt auto --run` | `runAutoAdaptFromEvents` @ `cli/adapt:188` → `runAutoAdaptLoop` | 每进程一次，E≈41 |
| `adapt auto --project` | `runAutoAdaptLoop` @ `cli/adapt:205`（空事件表，S4-E-1 面） | 每进程一次 |
| `adapt learn --run` | `proposeRoutingFromRoutedEvents` @ `cli/adapt:168`（S2-E-2 面） | 每进程一次 |
| 普通 `run` | `loadLearnedRouting` @ `cli/main:708`（S2-I-1 已裁决） | 每进程一次 |
| `run --flowchart` | `startFlowchartRun` @ `cli/main:580` → `flowchartForSupervisor` @ `flowchart-run:747→712` 单次载入（在 `cli/main:708` 之前返回，无双载入） | 每进程一次 |
| `run --children` | `cli/main:708` 载入 → `applyLearnedRouting` @ `routing/assign:102` 每任务 → `flowchart-run:747→712` 第二次载入（S8-E-1，勿去重）→ `applyLearnedRouting` @ `flowchart-run:681` 每节点 → `runAutoAdaptLoop` @ `cli/main:783` 一次 | 双载入各一次；任务/节点 ≤10/几十 |
| `track`（startTrackedRun） | `track/loop:88` 载入 → 每任务 `applyLearnedRouting` → `startFlowchartRun` @ `track/loop:152` → `flowchart-run:712` 第二次载入（同 S8-E-1）→ `runAutoAdaptLoop` @ `track/loop:172` 一次 | 同上 |
| flowchart resume / replay | `flowchartForSupervisor` @ `flowchart-run:880` / `:1021` 各单次载入 | 每恢复一次 |
| SPARKLE_AUTO_ADAPT=0 | 仍收集（persistSignals + updateProjectBandit 照付），仅跳过 propose；R5-E 裁决维持 | 不变 |
| 测试专用面 | `patterns` / `attribution` / `signatures` / `compareSignatures` / `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `parseObservedSignal`(直调) / `proposeRoutingFromAssignments`(deprecated) / `saveLearnedRouting`(deprecated, throw-only) / `learnedRoutingPath`(死导出) 交叉检索复核仍零生产调用方 | 不进热路径 |

切片外其余消费者以 `learning/` 模块路径精确检索复核：
`adaptation/eval-routing.ts:223` 仅调用 `parseLearnedRoutingPolicy`（离线
eval 面，D 切片）；`track/primary-split.ts:3` 仅导入类型；
`privacy/record-classes.ts:212/224` 仅文档字符串提及路径布局；
`adaptation/approval-profile.ts:44` 维持 R14-E 裁决的 docstring 假阳性。
**矩阵结论：无新洞，消费者集合与 R15-E 逐一相同**。

SLICE-CPU 锚点本 VM 重测（**16 次运行**：8 × Node v22.14.0 + 8 ×
Node v22.22.2；seeds `0xe16e01`（collect）/ `0xe16e03`（routed——顺位
`0xe16e02` 被拒绝采样出局：其 routed 夹具产出 outcomes=0）/ `0xe16e02`
（signals，diagnoseGroups=1 直接通过）——R12-E..R15-E 拒绝采样同协议，
探针记录 seed→(collected,outcomes,diagnoseGroups)：01→(17,3,0)、
02→(7,0,1)、03→(11,2,0)；夹具镜像 R8-E..R15-E 生成器，R1-A 组成 E=41）：

```text
collect=14.9-17.0us  outcomes=2.3-2.7us  diagnose=0.87-1.22us  bandit-build=0.7-1.2us
total in-slice CPU ~19.0-22.0us per full auto-adapt run （两 Node 版本同带：
  v22.22.2 19.0-22.0 / v22.14.0 19.0-20.1，版本差异不可分辨）
vs landing bar >=10000us  ->  455-527x below EVEN IF ZEROED
带间比对：本轮带 19.0-22.0 完整落在 R14-E 带（17.2-19.2，collected=15）与
  R15-E 带（20.9-22.4，collected=21）包络之间，collected=17 居中 ——
  与 R15-E §1 裁决的夹具构成效应同向一致（切片逐字节零漂移下的构成
  效应），无需另做种子比对运行
最保守合成口径（R7-E outcomes 7.0-7.3us + R12-E diagnose 分组上界 3.83-3.92us 同替）
  -> ~25.9-30.1us，仍 >=332x 低于门槛
configured-state: applyLearnedRouting(avoid=10, M=10) = 269-294ns/task （R13-E 258-312 /
                                                        R14-E 256-303 / R15-E 258-298 同带）
                  applyLearnedRouting(空策略)        = 111-128ns/task （R15-E 110-125 同带）
                  loadLearnedRouting fresh(ENOENT)   = 25.1-37.7us/次
                  loadLearnedRouting 现实 registry    = 76.6-84.8us/次 （R8-E 80-85 / R13-E 76.1-88.6 /
                                                        R15-E 76.9-85.0 同带，亚 ms 一次性，S8-E-1 带）
```

注：本轮夹具组成为 collected=17 / outcomes=2 / diagnoseGroups=1；diagnose
分组路径经 diagnoseGroups=1 全数走到。全部确定性 VERDICT 行 16 次运行
**逐位一致**，3/3 sanity 检查全过。**锚点复核成立：该切片不存在不推翻
既有排除就能达门槛的候选。**

## 2. 本轮两条实扫新轴 + 第十六组新角度枚举：有理由的空枚举

### 2.0 新轴 A：跨 run 累积状态穿越标定（本切片首创）

前十五轮全部锚点把持久状态钉死在生产小态（prevArms=3、baseline+1
candidate）——「切片每 run 重读的累积状态长到多大才可能有落地级余量」
从未被量化，而本切片恰有两处**单调只增、永不回收**的累积状态：
bandit.json arms（`bandit-store:46-51` 对 previous.arms 与本 run modelIds
取并集，无剪枝路径）与 adaptation registry 的 routing-policy candidates
（每 distinct contentHash 经 `createCandidate` 累积一个，切片内无回收
路径）。本轮 6 次运行（两 Node 版本各 3 次），阶梯拟合尾段线性、确定性
VERDICT 行逐位一致：

```text
A1 bandit arms 阶梯（in-lock 全量重建 + stringify + parse，S=12、rewardEligible=1）：
  A=3:     build 1.0us + ser 0.5us + parse 0.7us = 2.2us/run  （bandit.json 219 B）
  A=100:   34.7us/run（7.5KB）      A=1000: 1361us/run（78KB）
  A=10000: 14893us/run（807KB）     斜率 1335-1453 ns/arm
  A*(10ms/run) = 6,571-6,925 arms —— 生产 A=3 的 ~3.3-3.4 个量级
  A*(50ms/run) = 34,176-36,890 —— ~4.1 个量级
  注：A=100→300 段有一次 Record 建表台阶（build 16.9→159.6us，字典模式
  切换伪影），尾段（10^3→10^4）恢复线性，拟合取尾段（保守偏陡）
A2 registry candidates 阶梯（baseline + C 个 distinct 候选）：
  C=1: load 87.2us   C=10: 120.3us   C=100: 497.5us   C=1000: 5174us
  载入斜率 4,659-5,372 ns/candidate
  C*(10ms/run, 单载入)          = 1,857-2,141 candidates（~3.3 个量级）
  C*(10ms/run, S8-E-1 双载入形态) = 927-1,067 candidates（~3.0 个量级）
  in-lock candidatesFor().find（miss，每 adapt 一次）斜率 21.9-25.1 ns/candidate
    -> 该 E 切片辖区位点穿越需 ~3.99-4.56x10^5 candidates
```

**裁决**：(a) **arms 轴不产生候选**：重走主体是 `createBanditState` /
`recordReward` 的 O(A) Record 建表与拷贝（`routing/bandit.ts`，C 切片
辖区）+ O(A) JSON 序列化/解析（数据面字节，S6-E-4 族封死）；剪枝 =
bandit.json 数据面语义变更（在库数据的向后兼容 + 人读性，S6-E-4 比照
更硬）；且 A\* ≈ 6.6k 需要该项目历史上出现过 ~6,600 个 distinct
modelId——目录 M≤10、时代级模型流失也只积累 10² 级，距穿越点仍
~2 个量级。(b) **candidates 轴是全切片史上最近悬崖但辖区在 D**：
O(C) 全部由 `loadAdaptationRegistry` 快照解析承担（`adaptation/promotion`
与 `registry`，D 切片辖区），本切片辖区内的 find 位点斜率仅 ~22ns/candidate
（S1-D-2 已裁面，穿越点 4×10⁵）；候选保留/回收策略属 registry 契约 +
proposal-first 治理面（人工晋升/清理是设计内闭环）。**记录为跨切片观察
+ 重开条件（见 §6），不铸 ID、不越辖区提案。**该轴把「累积状态可能让
切片越线」这一从未测量的机制类改为标定曲线收口。

### 2.1 新轴 B：配置载荷形态穿越标定（R16-A 方法移植，本切片首次）

R16-A 在其切片标定了 id 字节 / 证据基数 / 自由文本三条 payload 形态轴；
本切片的镜像维度同样从未标定：learned policy 的 avoid/prefer 基数与
进入字符串管道的自由文本字节。6 次运行（两 Node 版本各 3 次）：

```text
B1 policy 基数阶梯（|avoid|=|prefer|=N，全 family 命中 avoid、全 miss prefer、
   avoided ids 不撞目录 = 最坏功；每档先过真实 parseLearnedRoutingPolicy 往返，
   证明 schema-open；live 结果全档稳定 allowed=10 preferred=m2）：
  N=10: 364ns/task   N=100: 2,428ns   N=1000: 24,203ns   N=10000: 580,484ns
  斜率 61.2-63.0 ns/entry/task
  10ms/run（x10 任务）穿越：|avoid| ≈ 16,404-16,973（生产 ≤10 的 ~3.2 个量级）
  S13-B-1 重开条件（本体 ≥2 个量级 ≈ ≥27us/task）折合 |avoid| ≈ 965-1,063
    —— 生产写入方有界：optimizedPolicy ≤ 失败组数 ≤ M；policyFromOutcomes
    ≤ M×8 distinct (modelId,family) FAIL 对；不可从生产侧触发
  注：N=10 档读数 364ns 高于锚点带 269-294ns 为构成差（prefer 全扫 miss +
  avoid 全 family 命中 vs 锚点 prefer=1 命中），非漂移
B2 自由文本字节阶梯（上游仅查非空：events.ts:518 answer / protocol v1:256/286
   summary —— schema-open，对照 R16-A L 轴的 schema 封闭是另一极）：
  B2a USER_ANSWER answer（最坏功：负则 miss 全扫 + 正则尾部命中 + truncate 全额）
      斜率 10.86-11.32 ns/char；T*(10ms) ≈ 885,143-921,971 字符（~3.9-4.0 个量级）
  B2b TASK_RESULT summary（truncate 面 + 完整 taskSuccess 绑定）
      斜率 8.94-9.35 ns/char；T*(10ms) ≈ 1,071,341-1,120,218 字符（~4.0 个量级）
  B2c subagent assistant 文本（extractAssistant 6 段折叠 + PEER_NEGATIVE miss
      + truncate；纯切片内 CPU，JSON.parse/readFile I/O 面已按 S1-E-5 排除）
      斜率 10.29-10.56 ns/char；T*(10ms) ≈ 948,605-973,959 字符（~3.6 个量级）
```

**裁决**：(a) **B1 不产生候选**：fat-policy 下的成本主体恰是 R13-E 无名
微观 #2（filter+map 双中间数组）与 R14-E §2 行 3（三连 includes 级联）
的已裁面 + S13-B-1 辖区边界（`applyLearnedRouting` 内部零改动）；穿越点
~3.2 个量级 + 生产写入方结构性有界把「按基数特化」类算术封死。本轮把
S13-B-1 的重开条件从定性（「本体成本 ≥2 个量级」）转为可标定判据
（|avoid| ≈ 10³）。(b) **B2 不产生候选**：文本管道斜率（~9–11 ns/char，
约为 R16-A S 轴 1.5ns/char 的 7 倍，因为本管道付 trim 拷贝 + 多正则 +
`\s+/g` 全局替换分配）下穿越点仍 ~4 个量级；「按 T 特化」候选
（truncate 前缀截断——空白折叠使任何常数前缀截断不等价、正确流式形态
= S2-E-5 实测更慢；恒等快路径 = S7-E-2；折叠 = S1-E-3；trim 消除 =
S7-E-1）全部既关。该轴同时给 R14-E §2 行 4 的机制类拒列补上了此前
缺失的定量穿越点。

### 2.2 第十六组枚举表

方法：本轮代码侧透镜「**累积状态 × 配置载荷形态**」（两条测量轴的
对偶）：对第十六遍全量重读的 10 个源文件逐函数问 (a) 哪些位点每 run
重走跨 run 累积状态、其重走消除是否在本切片辖区且可能越线；(b) 哪些
位点对 schema 不封顶载荷做逐项走查、其任何特化是否等价且可能越线；
同时按惯例对本轮逐字新点名的位置做换名检查（对照 EXCLUSIONS 全表 +
十五轮 E 报告 + 六轮空枚举面）。扫描结果：**零个可立项形态**，逐类
裁决如下（均未铸造新 ID）：

| 形态（本轮第十六遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **bandit arms 重走族**（`bandit-store:46-78`：Set 重建 / 承接循环 / 奖励环 `includes` / `recordReward` 不可变链——A 轴视角首次整体点名其 O(A) 结构） | A\*=6.6k 时全额 ~10ms，生产 A=3 时 ~2.2µs/run | **辖区 + 算术双封死拒列（不铸 ID）**：O(A) 主体在 `routing/bandit.ts`（C 切片）与 JSON 数据面；切片内可动份额 = R13-E #1（includes 旁路，ns 级）+ R12-E 行 3（批量化已拒）+ S2-E-3（弃置 createBanditState）；A\* 距生产 ~3.3 个量级且现实增速为时代级 10² | R13-E #1 / R12-E §2 行 3 / S2-E-3/4 + 本轮新轴 A1 |
| **arms 剪枝 / bandit.json 紧凑化 / 无变更跳写** | 每 run ≤µs 或数据面 | 换名重提拒列 + 数据面明令：剪枝改变在库语义（比 S6-E-4 更硬）；紧凑化 = S6-E-4；跳写 = S2-E-4；fsync/mkdir = S6-E-3 族 | S6-E-3/4 / S2-E-4 |
| **candidates 累积重走族**（`auto-loop:173-175` / `from-episode:112-114` 的 `candidatesFor().find`；载入面 O(C) 快照解析） | find 斜率 ~22ns/cand（穿越 4×10⁵）；载入斜率 ~5µs/cand（穿越 1.9-2.1k / 双载入 0.93-1.07k） | **辖区封死拒列（不铸 ID）**：find = S1-D-2 已裁面；载入 O(C) 在 `adaptation/`（D 切片），候选保留策略属 registry 契约 + proposal-first 治理；S8-E-1 双载入按令未碰，其成本形态已计入穿越点；**全切片史上最近悬崖记录为跨切片观察 + §6 重开条件** | S1-D-2 / S8-E-1 + 本轮新轴 A2 |
| **fat-policy apply 走查特化**（`learned-routing:198-215`：avoided-Set 构建 O(\|avoid\|) + prefer.find O(\|prefer\|)——B1 轴视角首次以基数不设限视角点名） | 穿越需 \|avoid\|≈16.4-17.0k；重开阈 ≈10³ | **算术 + 边界双封死拒列**：生产写入方有界（≤M×8）；位点本体 = R13-E #2 + R14-E §2 行 3 已裁面；任何调用点记忆化 = S13-B-1 辖区（重开条件本轮量化、未触发）；`applyLearnedRouting` 内部零改动 | R13-E #2 / R14-E §2 行 3 / S13-B-1 + 本轮新轴 B1 |
| **parseLearnedRoutingPolicy 基数上限缺失**（schema-open 事实本身） | 无（收紧 = 契约变更） | 不立项：为 policy 内容加基数上限属域 schema 变更（X0-7 邻域，超出优化职权）；R16-A L 轴同型裁决（其为 schema 封闭极、本处为开放极，都不是优化候选） | X0-7 邻域 + R16-A §1-4a 先例 |
| **文本管道按 T 特化**（truncate 前缀截断 / 流式早退 / 恒等快路径 / 折叠改 join / trim 消除——B2 轴视角整体重点名） | T\*≈0.89-1.12M 字符，生产 10¹-10² | 五向封死拒列：前缀截断不等价（`\s+/g` 折叠使输出 400 字符可消耗无界输入）；流式 = S2-E-5（实测更慢）；恒等快路径 = S7-E-2；折叠 = S1-E-3；trim = S7-E-1；I/O 支配 = S1-E-5 | S2-E-5 / S7-E-1/2 / S1-E-3 / S1-E-5 + 本轮新轴 B2 |
| **scoreUserAnswer 正则次序重排**（正向先测以便早退） | ns 级 | 理论失败拒列：负向优先是语义契约（S6-E-2 已裁——双正则合并的负向优先 vs 位置序发散同一事实），次序交换对同时含正负词的文本改变结果，不等价 | S6-E-2 |
| **R13-E 三处无名微观复核**（位点见 §0） | 各 ns~亚 µs | 位点逐字未变（零 diff 下必然），R13-E 裁决逐字维持，不补铸 ID、不重开 | R13-E §2 行 1-3 |
| 两遍 `collectSignalsFromEvents` 的任何结构变换 | pass 1+2 合计 ~15-17µs/run | 三向封死 + 硬不变量明令：S1-E-1 / S9-E-2 / S4-E-1 / R12-E 行 4；两条新轴均不改变任何一项裁决 | S1-E-1 / S9-E-2 / S4-E-1 + 硬不变量 |
| pass-1 五 Map / JUDGE_DECISION 双 Map.get / verdict 双分派 / 条件 spread 形状 / baseSignal 形状多态 | ns 级；R14-E JIT 轴已证无形状病理 | 换名重提拒列：S1-E-2 / S6-E-1 / S8-E-2 / S4-E-3 逐字维持；R15-E GC 天花板（~0.8µs/run）继续覆盖全部省分配形态 | S1-E-2 / S4-E-3 / S8-E-2 / S6-E-1 |
| `from-episode` 内部字面量再校验 / Date.parse / familyFromRouted includes / isCompleteRoute 谓词序 / applyCascadeRetry / seen-Set 单探针 / outcomeKindFromResult 死形参 | ns 级 | 全部维持 R10-E..R15-E 拒列，位点未变 | R10-E..R15-E §2 |
| `auto-loop` 三路拼接 / persistSignals filter 双求值 / some-探针跨函数去重 / 顺序读 / optimizedPolicy filter+map / LEARN_EVALUATION_PLAN 双定义 | ns~µs 级或 I/O 支配 | 换名重提拒列：R12-E 行 13 / S1-E-4 / R11-E 行 4 / S1-E-5 / S2-E-6 / R9-E 纯重构拒列逐字维持 | S1-E-4/5 / S2-E-6 / X0-3 |
| I/O 契约面全体：fsync 丢弃、完整性重哈希跳过、no-change 跳写、双载入去重、corrupt registry fail-open、readdir 并行化、惰性载入、mkdir 提升、紧凑序列化 | 切片相关全部 ms 级余量所在 | 排除行逐字重现 + 硬不变量明令封死；`routingPolicyContent` pretty-print 参与 contentHash ⇒ 序列化变更破坏 CAS 契约 | X0-3 / S2-E-1/4 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1 / S2-I-1 / S9-G-1 族 + CAS |
| 测试专用面任何优化（`patterns` / `attribution` / `signatures` + 五个测试专用导出） | 不进任何生产路径 | 本轮交叉检索复核仍零生产调用方，自动拒列 | S1-E-7/8 / X2-6 族 |
| 死导出清理 / `scoreTaskResult` 尾部 `return 70` | 0ns 运行时 | 不立项：无收益路径即无候选资格；公开签名保留 | 硬不变量（公开签名） |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（十六轮）、src/test 自 R15-E 基线零漂移、调用位点行号全数复核一致（§1） | — |

**提议排除 ID：无（本轮无「新增排除 ID」表——空枚举未铸任何 S16-E-\*）。**
切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选。前十五轮
已逐类收口的十一个角度类之外，第十六遍以「累积状态 × 配置载荷形态」
双轴普查新点名的六个维度（arms 基数、candidates 基数、policy 基数、
三站点文本字节）全部给出实测穿越点或辖区封死；本切片「无落地余量」
证明自此覆盖 **CPU（R3-E..R14-E）+ 规模（R14-E）+ 分配（R15-E）+
协议（R15-E）+ 累积状态（本轮）+ 载荷形态（本轮）六个维度**。

## 3. 逐文件收口（前十五轮收口之上的本轮新检查点）

| 文件 | 本轮第十六遍新视角检查 | 结论 |
| --- | --- | --- |
| `bandit-store.ts` | **累积状态轴首次标定**（A\*=6,571-6,925 @10ms、斜率 1.34-1.45µs/arm、A=10⁴ 时 bandit.json 807KB）；O(A) 主体判 C 切片 + 数据面辖区（§2 行 1-2）；R13-E #1 位点复核未变不补铸；fsync/重哈希/mkdir/序列化按令未触碰 | 无候选 |
| `auto-loop.ts` / `from-episode.ts`（propose 面） | **candidates 累积轴首次标定**（find 斜率 21.9-25.1ns/cand、载入斜率 4.7-5.4µs/cand、C\* 单/双载入 1.86-2.14k / 0.93-1.07k）；find = S1-D-2 维持；载入 O(C) 判 D 切片辖区（§2 行 3）；三路拼接/filter 双求值/顺序读维持既有拒列 | 无候选 |
| `learned-routing.ts` | **policy 基数轴首次标定**（斜率 61.2-63.0ns/entry/task、穿越 16.4-17.0k、S13-B-1 重开阈 ≈965-1,063）；parse 基数上限缺失判 X0-7 邻域不立项；apply 内部零改动（S13-B-1 边界 + R13-E #2/#3 不补铸）；载入面/CAS/S8-E-1 维持 | 无候选 |
| `signals.ts` | **文本字节轴三站点标定**（B2a 10.9-11.3ns/char、B2b 8.9-9.4、B2c 10.3-10.6；T\* 0.89-1.12M 字符）；truncate 前缀截断判不等价（空白折叠反例）；scoreUserAnswer 正则次序重排判语义契约（S6-E-2 同族）；两遍结构维持硬不变量；S1-E-1..3 / S2-E-5/7 / S4-E-1/3 / S6-E-1/2 / S7-E-1/2/4/5 / S8-E-2 / S9-E-2/3 维持 | 无候选 |
| `task-success.ts` | copyDefinedBinding 条件 spread = S8-E-2 族维持；B2b 阶梯实测含完整 taskSuccess 绑定路径，无新位点 | 无候选 |
| `diagnostics.ts` | 分组/双除法/filter().length 维持 R14-E 拒列；本轮夹具 diagnoseGroups=1 全数走到分组路径 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | test-only 面复核仍零生产调用方，自动拒列；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核（依赖 `pnpm install
--frozen-lockfile`，pnpm 10.17.1）：

```bash
# Node v22.22.2（nvm；engines >=22.19.0 合规）
pnpm gate        # typecheck ✓ + lint ✓ + test ✓ + build ✓
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.14.0 与 v22.22.2 均过）
```

锚点脚本（临时置于 `/tmp/r16e-anchor.mts`，未入库以遵守无赢家纪律；差异
面见附录 A）共 **16 次独立运行**（8 × v22.14.0 + 8 × v22.22.2），每次
3/3 sanity 检查全过，全部确定性 VERDICT 行跨 16 次运行**逐位一致**。
代表性一次运行（v22.22.2）：

```text
VERDICT fixtures: E=41 collected=17 routedE=41 outcomes=2 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=15.1us outcomes=2.4us diagnose=0.88us bandit-build=0.8us | total in-slice CPU ~19.2us per run vs landing bar >=10000us (522x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=273ns/task | empty policy=111ns/task -> x10 tasks/run = 2.73us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=29.4us | realistic registry (baseline+1 candidate)=81.6us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

新轴脚本（`/tmp/r16e-state.mts` 6 次运行、`/tmp/r16e-shape.mts` 6 次
运行，两 Node 版本各半；全部 sanity 5/5 与 20/20 通过、确定性 VERDICT
行逐位一致）代表性输出：

```text
A=10000: build=8189.4us serialize=3618.3us parse=3085.8us | per-run CPU total=14893.4us | bandit.json bytes=806646
crossing A* for 10ms/run: ~6,626 arms (~3.3 orders above production A=3; catalog M<=10)
C=1000: loadLearnedRouting=5173.7us/load | in-lock candidatesFor().find (miss)=22.32us/adapt
crossing C* for 10ms/run @1 load: ~1,929 candidates | @2 loads (S8-E-1 shape): ~967 candidates
|avoid|=|prefer|=10000: apply=580484ns/task -> x10 tasks/run = 5804.8us/run
S13-B-1 reopen condition (body >=2 orders, ~27us/task): |avoid| ~ 1,045
crossing for 10ms/run (10 tasks): |avoid| ~ 16,787
B2a crossing T* for 10ms/run: ~892,539 chars | B2b: ~1,077,497 | B2c: ~953,999
```

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round16-r16e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R15-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐形态有理由；第十六遍以「累积状态 ×
  配置载荷形态」双轴普查新点名的六个维度全部落进实测穿越点封死区、
  辖区封死区或既有排除确认；两条新轴本身均以「无候选」收口，未发现
  第十二个机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令封死
  的 I/O 契约面或切片外辖区上；CPU 面即使清零也距门槛 455–527×，最
  保守合成口径 ≥332×）。
- 矩阵空洞：无（调用位点行号与 R8-E..R15-E 逐一相同；消费者集合与
  R15-E 逐一相同；配置态 live 面与载入面锚点全数复测在带内；双 Node
  版本交叉不可分辨；本轮带落在 R14-E/R15-E 包络之间且由 collected
  构成单调解释）。
- 累积状态假说（「状态长大后切片会越线」）：以标定曲线关闭
  （A\* ≈ 6.6-6.9k arms ~3.3 个量级、现实增速时代级 10²；C\* ≈
  0.93-2.14k candidates ~3.0-3.3 个量级、O(C) 在 D 切片辖区）。
- 载荷形态假说（「fat 配置/长文本会越线」）：以标定曲线关闭
  （|avoid|\* ≈ 16.4-17.0k ~3.2 个量级且生产写入方有界 ≤M×8；
  T\* ≈ 0.89-1.12M 字符 ~3.6-4.0 个量级）。
- S13-B-1 边界：`applyLearnedRouting` 本体零改动；其重开条件本轮首次
  量化为 |avoid| ≈ 965–1,063（生产 ≤10，未触发、生产侧不可触发）；
  R13-E 三处无名微观未补铸未重开。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R15-E §6（含 R14-E 规模判据 E ≥10⁴/run
与 R15-E 分配/GC/噪声地板判据），并新增两条可标定判据：
**(a) 累积状态越线判据**——bandit arms 持续 ≥~6.6k（本轮 A\* 下界）或
项目 registry 的 routing-policy candidates 持续 ≥~930（本轮双载入形态
C\* 下界；届时应优先在 D 切片评估 registry 候选保留策略与快照解析面，
本切片自身无对应候选）；**(b) 载荷形态越线判据**——任一 promoted policy
的 |avoid|+|prefer| ≥~10³（同时触发 S13-B-1 重开条件，按其行内既备设计
处理）或单事件自由文本持续 ≥~10⁶ 字符；或任一 I/O 契约排除（X0-3 /
S2-E-1/4 / S1-G-1 / S1-E-4/5 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1）被
正式推翻，或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

主锚点脚本全文（`/tmp/r16e-anchor.mts`，`npx tsx /tmp/r16e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下四处替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R16-E；
`NOW = "2026-08-25T01:00:00.000Z"`；SECTION 1 的 seeds
`0xe16e01`（collect）/ `0xe16e03`（routed——顺位 `0xe16e02` 的 routed
夹具产出 outcomes=0 不过非平凡 sanity 检查，按 R12-E..R15-E 拒绝采样
协议出局）/ `0xe16e02`（signals，diagnoseGroups=1 直接通过；探针记录
seed→(collected,outcomes,diagnoseGroups)：01→(17,3,0)、02→(7,0,1)、
03→(11,2,0)）；SECTION 2 的 `mkdtemp` 前缀 `r16e-fresh-`/`r16e-reg-`
与 detector identity `r16e-anchor`。其余逐字同 R10-E 附录 A。

新轴 A 脚本（`/tmp/r16e-state.mts`）：复用同一 `mulberry32` /
`genSignal` / `banditBuild` 生成器（逐字镜像，signals seed `0xe16e02`）。
测量段 A1：arms 阶梯 {3,30,100,300,1000,3000,10000}，previous 携带
m1-m3 + `legacy_model_i` 补齐（pulls=7 / rewardSum=4 每臂），对
`banditBuild`、`JSON.stringify(state,null,2)`、`JSON.parse` 各计时
（reps 按 A 递减 20000→800），per-run = 三项和，尾段两点线性拟合出
A\*(10ms/50ms)。测量段 A2：candidates 阶梯 {1,10,100,1000}，每档在
新 mkdtemp stateRoot 建 baseline + C 个 distinct 候选（reason 串携带
变化的 meanScore/samples/#i 保证 contentHash 互异）后 save，对
`loadLearnedRouting`（benchAsync，reps 200/60）与重载 registry 上的
`candidatesFor(identity).find`（miss 探针 hash，bench 20000/2000）
计时，尾段拟合出 C\*（10ms @1 载入与 @2 载入即 S8-E-1 形态）与 find
位点穿越点。sanity 5 项：rewardEligible>0、各档 active policy 存在、
拟合顶点与实测逐位吻合。

新轴 B 脚本（`/tmp/r16e-shape.mts`）：B1 对 |avoid|=|prefer|=N ∈
{10,100,1000,10000} 构造最坏功 fat policy（avoid 全 family 命中且
modelId 全不撞目录、prefer 全 family miss），每档先过
`parseLearnedRoutingPolicy(JSON.stringify(fat))` 真实 parser 往返
（schema-open 证明）并断言 live 结果稳定（allowed=10 / preferred=m2），
对 `applyLearnedRouting` 计时（reps 100000→1000），尾段拟合出
10ms/run（×10 任务）穿越点与 S13-B-1 重开阈（27µs/task）对应基数。
B2 三站点文本阶梯 T ∈ {16,1024,16384,131072}：B2a 在 E=40 表尾放一条
USER_ANSWER（filler 为不命中任何词表的 lorem 变体 + 尾缀 " lgtm" 使
正则尾部命中、truncate 全额付费），B2b 在 E=41 表尾放 MODEL_ROUTED +
TASK_RESULT（fat summary，PASSED 完整绑定），均对整函数
`collectSignalsFromEvents` 计时；B2c 对 `collectSignalsFromSubagentRun`
直接计时（1 result × 6 text 段 ≈ T 字符，reviewer/exitCode 0/正则
miss）。各站点尾段拟合 ps/char 斜率与 T\*。sanity 20 项：每档信号
产出形态断言（score/criterion/数量）+ 拟合顶点吻合。

16 次主锚点运行原始区间（8 × v22.22.2 + 8 × v22.14.0）：

```text
SLICE-CPU 总量:
  v22.22.2: 22.0 / 19.2 / 19.1 / 19.0 / 19.1 / 19.5 / 19.6 / 19.5 us
  v22.14.0: 19.2 / 19.0 / 19.3 / 19.4 / 19.1 / 19.4 / 20.1 / 19.1 us
collect:    14.9-17.0 us（首次 v22.22.2 运行 17.0 为该批最高，余 14.9-15.5）
outcomes:   2.3-2.7 us
diagnose:   0.87-1.22 us
bandit:     0.7-1.2 us（首次 v22.22.2 运行 1.2 为该批最高，余 0.7-0.8）
apply(non): 269-294 ns/task
apply(空):  111-128 ns/task
load fresh: 25.1-37.7 us
load real:  76.6-84.8 us
VERDICT 行: 16/16 逐位一致；sanity 3/3 x16 全过
```

6 次累积状态轴运行原始值（3 × v22.22.2 + 3 × v22.14.0）：

```text
A* @10ms:      6,626 / 6,654 / 6,833 | 6,925 / 6,571 / 6,656 arms
A* @50ms:      34,206 / 34,176 / 36,159 | 36,890 / 34,277 / 34,281 arms
arm 斜率:      1450.3 / 1453.4 / 1363.9 | 1334.9 / 1443.7 / 1448.0 ns/arm
C* @1 load:    1,929 / 1,943 / 1,888 | 2,141 / 2,062 / 1,857 candidates
C* @2 loads:   967 / 976 / 944 | 1,067 / 1,030 / 927 candidates
cand 载入斜率: 5195.8 / 5170.5 / 5297.5 | 4659.1 / 4845.0 / 5372.3 ns/candidate
find 斜率:     22.31 / 21.91 / 25.06 | 22.48 / 25.06 / 23.10 ns/candidate
  -> find 位点穿越: 448k / 456k / 399k | 445k / 399k / 433k candidates
夹具 VERDICT 行（S=12 rewardEligible=1）: 6/6 逐位一致；sanity 5/5 x6 全过
```

6 次载荷形态轴运行原始值（3 × v22.22.2 + 3 × v22.14.0）：

```text
|avoid| 穿越 @10ms: 16,787 / 16,855 / 16,660 | 16,973 / 16,920 / 16,404
S13-B-1 重开阈:     1,045 / 1,047 / 1,049 | 1,063 / 1,061 / 965
entry 斜率:         61.81 / 61.55 / 62.33 | 61.16 / 61.35 / 63.02 ns/entry/task
B2a T*:  892,539 / 889,303 / 885,143 | 912,310 / 920,869 / 921,971 字符
B2b T*:  1,077,497 / 1,077,774 / 1,071,341 | 1,113,871 / 1,098,298 / 1,120,218 字符
B2c T*:  953,999 / 948,605 / 949,016 | 973,959 / 950,536 / 972,592 字符
B2a 斜率: 10.86-11.32 ns/char | B2b: 8.94-9.35 | B2c: 10.29-10.56
sanity 20/20 x6 全过；schema-open 往返（N=10^4）6/6 通过
```
