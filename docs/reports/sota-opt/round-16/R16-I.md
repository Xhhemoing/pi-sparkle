MODEL_SLUG=claude-fable-5-thinking-xhigh

# R16-I：CLI / Pi adapter / config / telemetry 第十六遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `6720960`（派单 tip，含 R16-A…R16-F 空枚举收口；G/H 在飞，未触碰）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- src/cli src/pi-adapter src/config src/telemetry` 为空（`--stat` 0 行）——**零漂移，连续第九轮字节不变**。全仓 `src/` 自 `8dee7fb` 后仍仅 `src/experiments/{canary,plan,shadow}.ts` 与 `src/routing/offline-logit.ts` 变动（S7-C / S7-F 已落地项），全部切片外；`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet` 通过）；切片相关测试面零漂移（`git diff --quiet 8dee7fb..HEAD -- test/` 通过）。基线增量核查：`2932c41`（R15-I 基线）→ `6720960` 非 docs 变更 0 个文件（`git diff --stat -- . ':!docs'` 为空），**EXCLUSIONS.md 在该区间逐字节未变**（`git diff --quiet` 通过），对 I 切片无新约束行。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致（R8-I「~28 / cli 16」维持历轮判定：头部笔误，git 为权威）。
- 前置阅读按令执行：README → EXCLUSIONS 全表（546 行，含全部 S\*-I-\* 行、S8-I-1 与 S13-B-1 尾行）→ round-16/PLAN（含 R16-A…R16-F 六切片空枚举收口摘要与 I 切片行）→ R15-I（七连空 + spawn A/A 地板 + 引擎代格 + 堆足迹格）→ R14-I（六连空 + 肥配置态交叉不存在证明）→ R13-I / R12-I / R11-I / R10-I / R9-I 按需 → **25 个切片源文件全量实际重读**（含凭据面两文件 `file-credential-store.ts` / `auth-session.ts` 的只读读码，未测量未立项）。
- 分支：`cursor/r16-i-cli-sixteenth-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（唯一入口 `/exec-daemon/node` shim，与 R9-I～R15-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。R15-I 轴 2 已把引擎代格（Node 24.19.0）收口且判「22.22→24 无增量红利」，本轮不重装 24、不补铸该格。pnpm 10.17.1，`pnpm install --frozen-lockfile`（本 VM node_modules 初始缺失，store 温装 1.8s）+ `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**（package.json 钉死 + frozen-lockfile 内容寻址），与 R7-I～R15-I 同版本；exports 图逐项复核仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第九轮未满足，两臂本轮第四次直接测量再确认（§2.2）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S16-I-\* ID（诚实空枚举，与 R9-I～R15-I 同结局，八连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第十六组枚举而非历轮剩余项清理。R14-I 钉死配置空间维度（肥态）、R15-I 钉死测量学维度（MDE / 引擎代 / 内存）；本轮补两块此前从未直接测量的砖——**命令类维度的抽测集缺口（`run --flowchart`）与 custom 格增量的采样归因（模块图普查 + CPU 剖析）**，两轴全部为测量收口、无一产出候选：

1. **零漂移五重确认（连续第九轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变——frozen-lockfile 内容寻址使包内容漂移数学上不可能；pi-ai 0.84.1 exports 图 7 项未变；`dist/providers/` 仍恰 39 表，本 VM 直接计数）、测试面（切片相关测试目录自 `8dee7fb` 零变动）、生产调用图（本轮再 grep：`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` 失手回退 L36 / `runtime.ts` 点用处 L20 / `listed-model.ts` 同步面 L1，`model-catalog.ts` 命中为 S7-I-1 注释非导入；pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方）、基线增量（`2932c41..6720960` 非 docs 零文件，EXCLUSIONS 字节不变）。R8-I 关闭矩阵、R9-I～R15-I 抽测所依据的结构前提在本基线逐项成立。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字，派单明令项）**：R9-I 5 格承重集 × 2 Node 版本全部 exit 0，结构逐格同构：地板 54.2/51.0 ms，**custom−builtin 差 children +49.7 / +26.3、track +51.7 / +22.5 ms（22.14/22.22）**——与 R15-I 带（+56.3/+39.7、+65.9/+23.2）、R14-I 带（+52.2/+26.7、+55.1/+23.7）及更早五轮同号、同量级、同 Node 版本梯度（**九轮同构**）；带缘漂移全部落在 R15-I 轴 1 的 A/A 噪声包络内（22.14 单窗 maxAbs 34.5 ms、22.22 8.7-10.4 ms），无新结构。进程内冷载锚点：`providers/all` 47.8-63.7 / 30.9-31.9 ms（22.14/22.22；warm 0.063-0.102 ms）；per-provider 表 anthropic 3.1/2.5、openai 1.4/0.8 ms（优势 ~9-25×）；custom id 失手解析首次 1.784/1.949 ms、重复失手中位 **0.097/0.096 ms**（七度复证 R10-I 记忆化否决）。S7-I-1 per-provider 优势与 custom 态回退归属完好（§1）。
3. **第十六组新角度枚举为空（§2）**，其中两轴为本轮新测量、全部不铸 ID：
   - **轴 1（§1.3）：`run --flowchart` 命令类抽测格（抽测集缺口收口）**。R8-I 78 格矩阵关闭时覆盖过 flowchart 命令类，但 R9-I～R15-I 的每轮抽测集只重测 children/track 双命令类——第三个校准目录消费者（`runCommand` L569 flowchart 分支，经 `buildLiveCatalogConfig` → `createCalibratedCliModelRouter` 同一条 S7-I-1 路径）九轮从未进过抽测集。本轮补测 4 格（builtin/custom × 22.14/22.22，`--executor fake` 单节点图，全 exit 0）：builtin 85.3/80.1、custom 127.1/103.3 ms，**custom−builtin 差 +41.8/+23.2 ms——与 children/track 带同号同量级**，逐格与 `providers/all` cold 锚点自洽。裁决：custom 格增量是命令类无关的一次性 `providers/all` 失手回退（第三命令类直接证人），无悬崖、无新结构、无候选；flowchart@builtin 比 children@builtin 便宜 ~24 ms 是该分支跳过 learned-routing/smartChildPlan/adapt 子树的既有设计（导入图差异，非优化空间）。
   - **轴 2（§1.4）：custom 格增量采样归因（模块图普查 + CPU 剖析，I 切片首次）**。静态普查：`providers/all` ESM 图实为 **181 模块 / 1003 KiB**（修正历轮「~40 模块」速记——40 是 provider 模块数，全图 181），最大单文件 `providers/data/openrouter.json` 132.5 KiB 仅占 13.2%，**39 个 `.models.js` 表合计仅 15 KiB（1.4%）**——S7-I-1 的数据/机器比首次量化：惰性面用 1.4% 的字节承载 100% 的目录构建需求。动态归因（`--cpu-prof`，run-children@custom vs @builtin，22.22）：custom 格采样 136.0 ms 中 **pi-ai 包自身代码 self time 仅 0.8%（1.1 ms）**，node 内部 78.9%；custom−builtin 增量逐站点分解为 `compileSourceTextModule` +6.4、`realpathSync` +7.6、JSON 模块策略（`jsonStrategy`，即 data/\*.json 装载）+3.2、`getPackageScopeConfig` +2.2、URL/package_json_reader/ModuleWrap 各 ~2.1 ms——**全部是 Node ESM 装载机（resolve/read/compile/instantiate）摊在 181 模块图上，无任何单一可从切片代码个别规避的未点名热点**；repo dist/ self time 双格恰同（27.6 vs 27.7 ms），增量 100% 在装载侧。这是 S8-I-2（ESM 实例化主线程 CPU、无重叠窗口）的第四证人（前三：否决实测、24 代实测、堆足迹格），也把「回退成本可在图内局部优化」的假设从采样面封死。`realpathSync` 站点对应的 `--preserve-symlinks` 类干预是引擎/bin 面项（S5-I-5 / S6-I-2 家族，且 pnpm 符号链接布局下不健全），非切片候选、不铸 ID。
   - 其余全部角度逐条映射到排除行或历轮无 ID 收口（§2.3），无一越过落地线；R13-I 两处无名微观（`import.meta.resolve` 预探针、alias-row spread）本轮重读逐字在位（`listed-model-lazy.ts` L21-38 try/catch miss 结构未动、`model-catalog.ts` L72/L79 原样），维持换名拒绝、不补铸；R14-I 肥配置态交叉不存在证明与 R15-I 三轴（A/A 地板、引擎代格、堆足迹格）未重铸、未重测（其前提——39 表计数、exports 图、pi-ai 版本——本轮复核未变）。
4. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**；r7i part B 1220 builtin 行 × 39 provider lazy===sync；part D 载荷探针复证 builtin 构建后 providers/all 保持冷 58.5-61.4 ms、custom 回退命中缓存 1.4 ms）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R15-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读证据，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交 `scripts/round16-r16i-equivalence-sim.ts`（全部 harness/探针源码进附录 A，遵守 R7-F/R8-C/R9-I～R15-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md 未触碰；未触碰在飞 R16-G / R16-H 分支与文件。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S15-\* 条目；R9-I～R15-I 空枚举维持，未事后发明 S9-I-\* ～ S15-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：`--children` 复用 smartChildPlan 已校准目录（main.ts L726-729 原样）、Pi runtime 点用处 `await import`（main.ts L149 / auth-session 函数体内；S4-I 败者为 **S4-I-2..5**，无 S4-I-1）、12 条分支独占分派模块惰性导入（赢家 ID **S5-I-1**；main.ts L1417-1455 dispatch 站点 + L668 track/loop 边逐字在位）、per-provider 惰性模型表（`listed-model-lazy.ts` + `model-catalog.ts` L58）——零漂移即字节级证明，三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅对其**重开条件**做第四轮直接测量再确认（§2.2）——四 specifier 全 blocked（双 Node 逐字）+ `all.models.js` 文件级不存在 + 39 表计数不变，未满足，维持淘汰。硬不变量「`providers/all` 失手回退 + throw-outside-catch」逐字在位（`listed-model-lazy.ts` L34-37 未动）。本轮两条新轴均为测量收口而非机制候选——轴 1 是既有加载路径在第三命令类上的直接观测（零行为差异）、轴 2 是同一路径的采样归因（未构造任何替代注册表、未改变任何加载决策）——与自制 union / 磁盘快照 / memo 三重围栏（R10-I / R11-I）中的任何 rename 无涉。
  - **未重开 S8-I-2**（预热无重叠窗口——轴 2 的 CPU 剖析为其第四证人：增量 100% 是主线程装载机 self time）、**S8-I-3**（enabled 循环 Promise.all——µs + 未知 id 错误选择分歧）、**S7-I-2..4**、**S6-I-1..3**、**S5-I-2..5**、**S4-I-2..5**、**S3-I-1..6**、**S2-I-1..6**、**S1-I-1..8**。本轮 25 文件重读逐字再确认（未立项）：flowchart 双 `buildLiveCatalogConfig`（S1-I-1，main L569+L583）、providers.json 双读（S1-I-2，main L609 + createExecutor kind==="pi" L131）、loadLearnedRouting/loadProvidersConfig 死载荷下沉（S2-I-1/S3-I-3，main L708/L609）、`smartChildPlan` assignments.find（S1-I-3，main L372）、双 createAgentProfileRegistry（S3-I-2，main L286+L753）、stdout 批量（S3-I-4）、冲突检查提升（S4-I-4）、describeSparkleModel try/catch 预探测（S4-I-5）、setDefaultModels 双 parseModelRef（S3-I-5，providers-config L100-104）、parseCliErrorJson reverse（S1-I-6）。
  - **R10-I ～ R15-I 无 ID 收口维持**：跨进程版本键控磁盘快照、进程内失手记忆化（本轮重复失手中位 0.096-0.097 ms 七度复证 µs 族）、进程内自制 39 表 union（三重围栏）、回退后 per-provider 探针短路（≤3.5 ms/provider，仅 mixed 态可达）、root `.` / `./compat` 作替代源（R12-I）、`all.models` 文件级探针（R13-I）、肥配置态交叉不存在证明（R14-I）、**A/A 噪声地板 / 引擎代格 / 堆足迹格（R15-I，不补铸不重测——本轮所有跨轮带缘比较直接引用其 MDE 数字）**。**R13-I 两处无名微观维持不铸**：`import.meta.resolve` 同步预探测（S4-I-5 族镜像 + 失手锚点七度定界 <0.3 ms/构建）、`buildLiveCatalogConfig` 默认别名行 spread（model-catalog.ts L72/L79 原样，ns-µs 级 S1-I-8 邻域）。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项（S1-I-5 / S7-I-4 维持关闭）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；稀疏→已批准 R0 基线；双 LCB / 双归因在位（X0-11）；提升 proposal-first（Checkpoint F-PROD 开放，ADR-005/X0-1）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`listed-model.ts` 同步公开面与惰性面经 `listed-model-common.ts` 字节等价关系未动；`providers/all` 失手回退 + throw-outside-catch 在位；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未触碰在飞 R16-G / R16-H 分支与文件。

## 1. 本 VM 复测：5 格承重抽测 + 进程内锚点 + 两条新轴（双 Node，未拷贝历轮数字）

R9-I～R15-I 抽测集原样复刻并**新增 flowchart 双格**（configured 态夹具、每迭代全新拷贝 state-root、2 预热 + 10 测量、显式二进制全路径，遵守 R7-I 配置态 × 命令类纪律；harness 全文附录 A.1）。

### 1.1 抽测中位数（ms，spawn-to-exit，全部 exit 0）

| 格 | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|
| `--version`（地板） | 54.2 (min 53.2, p90 55.1) | 51.0 (49.2, 56.8) |
| run-children@builtin | 107.9 (103.5, 111.0) | 104.2 (102.1, 107.8) |
| run-track@builtin | 125.8 (121.7, 126.9) | 122.6 (119.5, 125.2) |
| run-flowchart@builtin（本轮新格，§1.3） | 85.3 (82.0, 92.3) | 80.1 (77.3, 81.0) |
| run-children@custom | **157.6** (149.5, 172.5) | **130.5** (126.5, 133.5) |
| run-track@custom | **177.5** (167.5, 198.1) | **145.1** (141.1, 148.3) |
| run-flowchart@custom（本轮新格，§1.3） | **127.1** (122.1, 128.7) | **103.3** (100.0, 107.3) |

- **custom−builtin 差**：children **+49.7 / +26.3**、track **+51.7 / +22.5** ms（22.14/22.22）。R15-I 带 +56.3/+39.7 与 +65.9/+23.2，R14-I 带 +52.2/+26.7 与 +55.1/+23.7，R13-I 带 +45.5/+23.7 与 +51.7/+21.5，R12-I 带 +51.3/+29.1 与 +58.7/+24.1，R11-I 带 +45.9/+22.8 与 +48.0/+23.4，R10-I 带 +47.1/+36.6 与 +56.0/+26.4，R9-I 带 +53.9/+26.9 与 +49.4/+28.2——**九轮同号、同量级、同 Node 版本梯度**。本轮全部带缘偏移（如 22.22 children 从 R15-I 的 +39.7 回到 +26.3）落在 R15-I 轴 1 的 A/A 包络内（22.22 单窗 maxAbs 8.7-10.4 ms、多窗聚合更紧；22.14 单窗 maxAbs 34.5 ms），无一需要新解释。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
- **builtin 态目录构建**比地板贵 +53.7/+53.2（children，双引擎）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。本 VM 地板（54.2/51.0）与 R15-I VM（55.6/52.4）紧邻，格间结构不变。

### 1.2 进程内冷载锚点（附录 A.2 脚本，工作区根执行，测毕删除）

| 锚点 | 22.14.0 | 22.22.2 |
|---|---|---|
| `providers/all` cold import（3 次） | 63.7 / 60.3 / 47.8 ms | 31.9 / 30.9 / 31.6 ms |
| `providers/all` warm（第二次 await） | 0.063-0.071 ms | 0.097-0.102 ms |
| per-provider `anthropic.models` cold | 3.1 ms | 2.5 ms |
| per-provider `openai.models` cold | 1.4 ms | 0.8 ms |
| `providers/all`（双表已载后） | 43.3 ms | 28.0 ms |
| custom id 失手解析首次 | 1.784 ms | 1.949 ms |
| 同上，重复失手中位（N=49） | **0.097 ms** | **0.096 ms** |

- per-provider 优势 ~9-25×，双引擎成立，S7-I-1 归属完好；custom 格增量与 `providers/all` cold 同量级，回退归属自洽。
- 重复失手 ~0.096-0.097 ms 七度复证 R10-I：Node package-reader 已缓存失败解析，记忆化物理无收益（<0.3 ms/构建）。

### 1.3 轴 1：`run --flowchart` 命令类抽测格（本轮新证据，不铸 ID）

R8-I 关闭矩阵覆盖过 flowchart 命令类，但 R9-I 定义的每轮抽测承重集（version + children/track × builtin/custom）九轮从未含它——第三个 `createCalibratedCliModelRouter` 消费者的 custom 态增量一直只有结构推断（同一条 `buildLiveCatalogConfig` 路径）而无直接跨轮数字。本轮补测（单节点图 `allowedModels:["cheap"]` 走恒在别名行、`--executor fake` 确定性完成，夹具见附录 A.1）：

- **custom−builtin 差 +41.8 / +23.2 ms（22.14/22.22）**——与 children（+49.7/+26.3）、track（+51.7/+22.5）同号同量级，Node 版本梯度同构（22.14 增量 ≈ providers/all cold@22.14，22.22 增量 ≈ cold@22.22）。**回退成本命令类无关的判定拿到第三命令类直接证人。**
- flowchart@builtin 比 children@builtin 便宜 23-24 ms：该分支不做 `loadLearnedRouting` / `smartChildPlan` / adapt 循环（main.ts L568-601 提前 return，学习/自适应子树不进导入图）——既有分支设计的导入图差异，非未开发的优化空间（对其继续惰性化即 S6-I-1 域，已排除）。
- **裁决：无候选、不铸 ID。** 抽测集的命令类缺口就此关闭；后续轮次抽测承重集可继续用 children/track 双格（flowchart 格与其同分布已被直接证明）。

### 1.4 轴 2：custom 格增量采样归因——模块图普查 + CPU 剖析（本轮新证据，不铸 ID）

历轮对 custom 格增量的归属证据是端到端差分 + 进程内 import 计时（锚点级）；「增量内部是否存在单一可从切片代码规避的未点名热点」一直只有 S8-I-2 的机制论证。本轮两步直接测量（脚本与原始输出见附录 A.3/A.4）：

**(a) 静态普查**：从 `dist/providers/all.js` 沿相对 import 边闭包遍历——**181 模块 / 1003 KiB**（修正历轮「~40 模块」速记：40 是 provider 模块数，含 API 机器与数据文件的全图是 181）。字节分布：最大单文件 `providers/data/openrouter.json` 132.5 KiB（13.2%），前 12 名合计 ~54%（六个 data JSON + 五个 API 模块 + models.js），**无 >14% 的单一站点**；39 个 `.models.js` 表合计仅 **15 KiB（1.4%）**——S7-I-1 的数据/机器比首次量化：目录构建所需的纯数据只占全图 1.4%，其余 98.6% 是 custom 失手回退被迫连带装载的 API/auth 机器与聚合数据。

**(b) 动态归因**（`node --cpu-prof`，run-children@custom vs @builtin 各一次全新 spawn，22.22）：

| 粗桶 | custom（136.0 ms 采样） | builtin（105.5 ms 采样） |
|---|---|---|
| node 内部 + GC/parse | 107.3 ms（78.9%） | 77.8 ms（73.7%） |
| repo dist/ | 27.6 ms（20.3%） | 27.7 ms（26.3%） |
| pi-ai 包代码 | **1.1 ms（0.8%）** | 0（未进采样） |

- 增量细站点（custom−builtin）：`compileSourceTextModule` 16.0−9.6=+6.4、`realpathSync` +7.6（builtin 侧不进 top14）、JSON 模块策略 `jsonStrategy` +3.2（即 §(a) 六个 data JSON 的装载通道）、`getPackageScopeConfig` +2.2、`URL`/`package_json_reader`/`ModuleWrap` 各 ~2.1 ms——**全部是 Node ESM 装载机在 181 模块图上的摊销，无任何集中于单一模块、可被切片代码绕开的热点**；pi-ai 包自身求值 self time 仅 0.8%。
- **repo dist/ self time 双格恰同（27.6 vs 27.7 ms）**——增量 100% 在装载侧，切片内命令逻辑对 custom 态零额外成本；「在切片代码里找 custom 态计算浪费」这一整类假设被采样面直接清空。
- 派生观察两条，均非候选：`realpathSync` 对应的 `--preserve-symlinks` 类干预是引擎/bin 面项（S5-I-5 / S6-I-2 家族；且 pnpm 内容寻址 store 靠符号链接布局，该旗标不健全），不立 ID；JSON 数据文件与 API 模块均为 pi-ai 包内部相对边，exports 图外不可从仓内选择性装载（S8-I-1 四探针同一围栏）。
- **裁决：无候选、不铸 ID。** 此轴是 S8-I-2 的第四证人（主线程 CPU、无重叠窗口、跨代不降），并把 R15-I 轴 3（堆足迹 ~2.4-2.7 MiB）的时间侧对应物钉死：唯一消除形态仍是 S8-I-1 被封死的跳过。

## 2. 第十六组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 命令类覆盖 / 采样归因），每形态先对 EXCLUSIONS 全表 + 十五轮 I 报告做同机制先例检索。R14-I 收口配置空间维度、R15-I 收口测量学维度；本轮收口**命令类覆盖维度**（轴 1）与**采样归因维度**（轴 2）。

### 2.1 两条新轴的候选性自查——全部测量收口，不铸 ID

- **轴 1（flowchart 格）**是既有加载路径在第三命令类上的直接观测：零行为差异、零代码改动，其产出是「回退成本命令类无关」的直接证人与抽测集缺口关闭，不是优化。
- **轴 2（普查 + 剖析）**是归因基础设施：181 模块图的字节与 self-time 分布证明增量无单一可规避热点、切片内逻辑零额外成本——巩固 S8-I-1/S8-I-2 围栏而非产生新机制；两条派生观察（preserve-symlinks、JSON 数据边）分别落入 S5-I-5 族（环境项）与 S8-I-1 围栏（exports 图外不可达）。

### 2.2 S8-I-1 重开条件两臂第四轮直接测量再确认——不铸 ID

- 文件级：`dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 0.84.1（package.json + frozen-lockfile 内容寻址）。
- 执行级：四 specifier 探针在 22.14 / 22.22 双引擎逐字同结局（附录 A.2 原始输出：`providers/../models.generated` → ERR_INVALID_MODULE_SPECIFIER；`dist/models.generated.js` / `models.generated` → ERR_PACKAGE_PATH_NOT_EXPORTED；`providers/all.models` → ERR_MODULE_NOT_FOUND）——0.84.1 内无任何导出形态的轻量权威注册表，无任何旁路。
- **裁决**：重开条件连续第九轮未满足；所需的是 pi-ai 上游导出，不是仓内改动。此项为审计证据，非机制候选——未构造替代注册表、未触碰回退路径——**不铸 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级即死）

| 扫描角度 | 处置 |
|---|---|
| custom id 跳回退 / 自制 union / 磁盘快照 / 进程内失手 memo | S8-I-1 硬禁 + R11-I/R10-I 三重围栏无 ID 收口；重开条件本轮四测仍未满足；派单明令不得铸 rename ID |
| custom 先查（`customProviders.find` 前置于 builtin 探测） | S8-I-1 同一健全性反例的次序镜像：per-provider 表缺失 + id 撞名时 custom 遮蔽 builtin，惰性面与同步面（`listed-model.ts` L41-45 builtin 先行）分歧——rename 拒绝 |
| 图内选择性装载（只装 data JSON / 跳过 API 模块） | 轴 2 派生角度即死：全部是 pi-ai 包内部相对边，exports 图外不可达（§2.2 四探针同围栏）；无 >14% 单一站点可截 |
| `--preserve-symlinks` / realpath 缓存类引擎干预 | S5-I-5 / S6-I-2 族（bin/引擎面，切片外）；pnpm 符号链接布局下不健全——观察记录，不立 ID |
| 多 provider 阈值预取 providers/all | R14-I §2.1 全域支配证明（交叉点不存在），本轮前提复核未变，不重测不补铸 |
| `import.meta.resolve` 同步预探测替代 try/catch miss（R13-I 无名微观 #1） | 维持换名拒绝不铸 ID：S4-I-5 惰性面镜像；§1.2 失手锚点七度定界（首次 1.8-1.9 ms 至少付一次、重复失手 ~0.096-0.097 ms、custom 态每构建 ≤2 次重复）→ 收益 <0.3 ms，µs 族 |
| `buildLiveCatalogConfig` 默认别名行 spread(R13-I 无名微观 #2) | 维持换名拒绝不铸 ID：每目录构建 ≤2 次 ~11 字段对象 spread（L72/L79），ns-µs 级 S1-I-8 邻域 |
| root / compat 作替代注册表源 | R12-I 直接测量封死（无表 + 严格更重，双重不成立） |
| providers/all 预热重叠 | S8-I-2 逐字；轴 2 CPU 剖析为第四证人：增量 100% 主线程装载机 self time，无可重叠窗口 |
| enabled 循环并行 per-provider 导入 | S8-I-3 逐字（µs + 未知 id 错误选择分歧） |
| 回退后 per-provider 探针短路（mixed 态） | R11-I §2.2 无 ID 收口（≤3.5 ms/provider，仅 mixed 态可达；跨调用穿线撞 X1-1） |
| pi-adapter barrel（`index.ts`）隐藏静态边 | 本轮再 grep 为无：全 src/ 无生产导入方；providers/all 的 src 内边仅 lazy 回退 / runtime 点用处 / 同步面 |
| flowchart 路径双 `buildLiveCatalogConfig` | S1-I-1 逐字（第二次构建全走 warm 模块缓存，~190 µs；warm 锚点 0.063-0.102 ms + 轴 1 flowchart 格端到端自洽双旁证） |
| flowchart 分支学习子树继续惰性化 | S6-I-1 域逐字（分支独占残余边组 −2.2~−4.6 ms，无病态红利）；轴 1 观测到的 builtin 差是既有设计非空间 |
| providers.json 双读（run：main L609 + createExecutor pi 分支 L131） | S1-I-2 逐字（~60 µs） |
| loadLearnedRouting / loadProvidersConfig 死载荷下沉 | S2-I-1 / S3-I-3 逐字（损坏时错误路径发散） |
| `smartChildPlan` assignments.find 换 Map / 双 registry | S1-I-3 / S3-I-2 逐字（children 个位数 / 8.4 µs） |
| pause/inject 免 router / 免 learned | S2-I-2 / S9-G-1 逐字 |
| resume/answer 预读去重 / PAUSED 探测降级 | S2-I-4 / S4-I-2 逐字 |
| compile-cache / 分派拆分 / 剩余独占边 | S6-I-1..3 逐字（once-per-process 亚两位数 ms） |
| dist 布局向 getPackageScopeConfig 开刀 / 引擎升级钉版 | S5-I-2..5 族；R15-I 轴 2 已实测 22.22 取尽该红利、24 无增量——环境项非切片角度，本轮剖析中该站点仅 +2.2 ms 亦旁证 |
| 一次性命令类（models/auth/doctor/inspect/episode/commits/adapt/pref/delete） | S1-I-4 / S7-I-4 / S2-I-5 / S3-I-4 类；once-per-process 噪声按派单直接否决 |
| 凭据面（auth status --all、credential store 微观） | S1-I-5 / S7-I-4 围栏，只读证据 |
| buildInvocation / invocationError / parseModelRef / parseProvidersConfig / parseCliErrorJson / setDefaultModels 微观 | S1-I-7 / S2-I-3 / S3-I-1 / S3-I-5 / S2-I-6 / S1-I-6 逐字（实测更慢或 ns 级） |
| pi-executor / cluster-tools 执行面微观 | 网络 I/O 支配的 live 面；µs 级，无复杂度类角度 |
| telemetry INVOCATION_CALL_OUTCOMES includes→Set / compareRunToRun 微观 | 表长 4，S1-D-8 族噪声 / 诊断聚合一次性 |
| writeAtomicJson 去 fsync / doctor-overlay jsonl 扫描 | S9-G-3 族（不得去 fsync）/ S3-J-4 族 + 一次性诊断类 |
| `builtinModelLazy` 动态 specifier / `_MODELS` 键过滤微观 | 命中路径被表解析成本（0.8-3.1 ms）支配、失手路径 ~0.096 ms；µs 族 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖（含包内容内容寻址锁定）、pi-ai exports、测试面、调用图五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选；唯一数十 ms 结构（custom/mixed 态 providers/all 失手回退）的全部消除形态收敛到同一个上游前提——pi-ai ship 轻量权威子路径——该前提的两种成立形态维持文件级 blocked（本轮第四次直接确认）；本轮两条新轴进一步证明该结构命令类无关（轴 1）、其内部无任何可从切片代码个别规避的热点且切片内逻辑对 custom 态零额外成本（轴 2）。

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part B：1220 builtin 行 × 39 provider lazy===sync；part C：17 组失手/custom/对抗用例；part D 载荷探针：builtin 构建后 providers/all 保持冷 58.5-61.4 ms、custom 回退命中缓存 1.4 ms；part E：configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单项）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2（与 R9-I～R15-I 计数逐字相同）、`pnpm build` 绿。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **引擎注记**：22.14.0 的 doctor engines 预检 fail-closed 为设计行为（R9-I §3 记录在案）；本 VM 22.14.0 仅经 `/exec-daemon/node` 可达；nvm 22.22.2 为 gate/仿真引擎。依 R15-I 轴 1 纪律，候选验收须在 ≥22.19 引擎 + 交错窗上做——本轮无候选，抽测数字仅用于九轮带比对（多窗中位数，双引擎同号）。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动，无 landed-sim 之外的额外 gate 需求；无赢家故未提交 `scripts/round16-r16i-equivalence-sim.ts`；两枚临时探针（anchor/census）测毕即删，未进 lint 面。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；本轮两条新轴以测量收口关闭命令类覆盖缺口与采样归因缺口；S8-I-1 两臂四测维持 blocked；两个历轮无名微观维持换名拒绝）。
- 剩余赢家：无（唯一数十 ms 结构被 S8-I-1 健全性反例 + 硬不变量封死；重开条件两臂的包内旁路已被 R12-I/R13-I/R14-I 完备排除、本轮复测维持——所需的是 pi-ai 上游导出，不是仓内改动；轴 2 证明该结构内部亦无局部可规避热点，切片内逻辑对 custom 态零额外成本）。
- 矩阵空洞：无（R8-I 78 格全覆盖；本轮承重格九轮同构复现 + 抽测集补入第三命令类双格；R7-I 配置态 × 命令类纪律全程遵守——configured 态夹具、builtin/custom 双态、children/track/flowchart 三命令类；肥态维持 R14-I 收口、mixed 态增量收益维持 R11-I 定界 ≤3.5 ms/provider、测量学三格维持 R15-I 收口）。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 7 格抽测 harness（/tmp/r16i-bench/spot.mjs，`<node二进制全路径> spot.mjs <同一路径>` 分别以两版本执行；与 R15-I A.1 逐字同构 + 新增 flowchart 夹具与双格）

```javascript
// R16-I spot re-measure: floor + builtin/custom children+track cells
// (R9-I..R15-I harness style) + NEW axis: run --flowchart cells (the third
// calibrated-catalog command class, never in prior rounds' spot sets).
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r16i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.argv[2];
const WARMUP = 2, ITERS = 10;

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
for (const [name, cfg] of Object.entries(states)) {
  const dir = join(FIX, `state-${name}`);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
}
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
const CHILDREN = join(FIX, "children.json");
writeFileSync(CHILDREN, JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");
// New axis fixture: one-node flowchart on the always-present "cheap" alias row.
const FLOW = join(FIX, "flow.json");
writeFileSync(FLOW, JSON.stringify({
  id: "bench-flow",
  nodes: [
    { id: "only", taskId: "tsk_only", role: "actor", objective: "Do the work", modelPolicy: { allowedModels: ["cheap"] }, confidenceThreshold: 0.7, approvalRequired: false }
  ],
  edges: []
}) + "\n");

const cells = [
  { id: "version", args: () => ["--version"], state: undefined },
  ...["builtin", "custom"].flatMap((state) => [
    { id: `run-children@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] },
    { id: `run-track@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"] },
    { id: `run-flowchart@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--flowchart", FLOW, "--executor", "fake"] }
  ])
];
function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, `state-${state}`), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 120000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-FAIL ${cell.id}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

原始输出（run 命令均为 `--executor` 缺省或 fake 下的域内校验/确定性执行路径，全部 exit 0）：

```
== v22.14.0 ==
version: median=54.2ms min=53.2 p90=55.1 exit=0
run-children@builtin: median=107.9ms min=103.5 p90=111.0 exit=0
run-track@builtin: median=125.8ms min=121.7 p90=126.9 exit=0
run-flowchart@builtin: median=85.3ms min=82.0 p90=92.3 exit=0
run-children@custom: median=157.6ms min=149.5 p90=172.5 exit=0
run-track@custom: median=177.5ms min=167.5 p90=198.1 exit=0
run-flowchart@custom: median=127.1ms min=122.1 p90=128.7 exit=0
== v22.22.2 ==
version: median=51.0ms min=49.2 p90=56.8 exit=0
run-children@builtin: median=104.2ms min=102.1 p90=107.8 exit=0
run-track@builtin: median=122.6ms min=119.5 p90=125.2 exit=0
run-flowchart@builtin: median=80.1ms min=77.3 p90=81.0 exit=0
run-children@custom: median=130.5ms min=126.5 p90=133.5 exit=0
run-track@custom: median=145.1ms min=141.1 p90=148.3 exit=0
run-flowchart@custom: median=103.3ms min=100.0 p90=107.3 exit=0
```

### A.2 进程内锚点 + 可达性探针（§1.2 / §2.2 证据；临时置于 /workspace 根以获得 node_modules 解析，测毕删除；与 R15-I A.2 的 all/tables/miss/traversal 四模式逐字同构，heap 模式为 R15-I 轴 3 收口项本轮未复跑）

```javascript
// R16-I in-process anchors + reachability probes (temporary; deleted after measurement).
import { performance } from "node:perf_hooks";
const label = process.version;
const which = process.argv[2];
if (which === "all") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const dt = performance.now() - t0;
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  console.log(`${label} providers/all: cold=${dt.toFixed(1)}ms warm=${(performance.now() - t1).toFixed(3)}ms`);
} else if (which === "tables") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/anthropic.models");
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/openai.models");
  const t2 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const t3 = performance.now();
  console.log(`${label} anthropic.models=${(t1 - t0).toFixed(1)}ms openai.models=${(t2 - t1).toFixed(1)}ms providers/all-after-tables=${(t3 - t2).toFixed(1)}ms`);
} else if (which === "miss") {
  const N = 50; const times = [];
  for (let i = 0; i < N; i++) {
    const t0 = performance.now();
    try { await import("@earendil-works/pi-ai/providers/customco.models"); } catch { /* expected miss */ }
    times.push(performance.now() - t0);
  }
  const first = times[0];
  const rest = times.slice(1).sort((a, b) => a - b);
  console.log(`${label} miss-resolution: first=${first.toFixed(3)}ms repeat-median=${rest[Math.floor(rest.length / 2)].toFixed(3)}ms min=${rest[0].toFixed(3)}ms`);
} else if (which === "traversal") {
  for (const spec of [
    "@earendil-works/pi-ai/providers/../models.generated",
    "@earendil-works/pi-ai/dist/models.generated.js",
    "@earendil-works/pi-ai/models.generated",
    "@earendil-works/pi-ai/providers/all.models"
  ]) {
    try {
      await import(spec);
      console.log(`${label} REACHABLE: ${spec}`);
    } catch (e) {
      console.log(`${label} blocked: ${spec} -> ${e.code ?? e.message.slice(0, 60)}`);
    }
  }
}
```

原始输出（anchors：每模式全新进程；traversal 双引擎逐字一致）：

```
v22.14.0 providers/all: cold=63.7 / 60.3 / 47.8 ms  warm=0.063-0.071 ms
v22.22.2 providers/all: cold=31.9 / 30.9 / 31.6 ms  warm=0.097-0.102 ms
v22.14.0 anthropic.models=3.1ms openai.models=1.4ms providers/all-after-tables=43.3ms
v22.22.2 anthropic.models=2.5ms openai.models=0.8ms providers/all-after-tables=28.0ms
v22.14.0 miss-resolution: first=1.784ms repeat-median=0.097ms min=0.072ms
v22.22.2 miss-resolution: first=1.949ms repeat-median=0.096ms min=0.072ms
v22.14.0 blocked: @earendil-works/pi-ai/providers/../models.generated -> ERR_INVALID_MODULE_SPECIFIER
v22.14.0 blocked: @earendil-works/pi-ai/dist/models.generated.js -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/models.generated -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/providers/all.models -> ERR_MODULE_NOT_FOUND
v22.22.2 blocked: (同上四行，双 Node 逐字一致)
```

### A.3 providers/all 图静态普查（§1.4(a) 证据；临时置于 /workspace 根，测毕删除）

```javascript
// R16-I axis 2 part A: static census of the providers/all ESM graph.
// Walks relative import specifiers from dist/providers/all.js and reports
// module count, total bytes, and the largest files — checking whether the
// fallback cost has a single dominating site or is spread across the graph.
import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";

const ROOT = "/workspace/node_modules/@earendil-works/pi-ai/dist";
const start = resolve(ROOT, "providers/all.js");
const seen = new Map();
const queue = [start];
const IMPORT_RE = /(?:^|\n)\s*(?:import|export)\s[^"']*?["']([^"']+)["']|import\(["']([^"']+)["']\)/g;
while (queue.length > 0) {
  const file = queue.pop();
  if (seen.has(file)) continue;
  let text;
  try {
    text = readFileSync(file, "utf8");
  } catch {
    continue;
  }
  seen.set(file, Buffer.byteLength(text));
  for (const match of text.matchAll(IMPORT_RE)) {
    const spec = match[1] ?? match[2];
    if (spec === undefined || !spec.startsWith(".")) continue;
    queue.push(resolve(dirname(file), spec));
  }
}
const rows = [...seen.entries()].sort((a, b) => b[1] - a[1]);
const total = rows.reduce((acc, [, bytes]) => acc + bytes, 0);
console.log(`providers/all graph: ${rows.length} modules, ${(total / 1024).toFixed(0)} KiB total`);
for (const [file, bytes] of rows.slice(0, 12)) {
  console.log(`  ${(bytes / 1024).toFixed(1).padStart(8)} KiB  ${(100 * bytes / total).toFixed(1).padStart(5)}%  ${file.slice(ROOT.length + 1)}`);
}
const models = rows.filter(([f]) => f.endsWith(".models.js"));
const modelBytes = models.reduce((acc, [, b]) => acc + b, 0);
console.log(`  of which ${models.length} *.models.js tables: ${(modelBytes / 1024).toFixed(0)} KiB (${(100 * modelBytes / total).toFixed(1)}%)`);
```

原始输出：

```
providers/all graph: 181 modules, 1003 KiB total
     132.5 KiB   13.2%  providers/data/openrouter.json
      64.1 KiB    6.4%  providers/data/vercel-ai-gateway.json
      58.8 KiB    5.9%  api/openai-completions.js
      52.5 KiB    5.2%  api/openai-codex-responses.js
      45.1 KiB    4.5%  providers/data/amazon-bedrock.json
      44.4 KiB    4.4%  api/anthropic-messages.js
      28.8 KiB    2.9%  api/openai-responses-shared.js
      25.3 KiB    2.5%  providers/data/opencode.json
      24.5 KiB    2.4%  models.js
      22.9 KiB    2.3%  api/mistral-conversations.js
      21.8 KiB    2.2%  providers/data/huggingface.json
      20.9 KiB    2.1%  providers/data/cloudflare-ai-gateway.json
  of which 39 *.models.js tables: 15 KiB (1.4%)
```

### A.4 CPU 剖析归因 harness（/tmp/r16i-bench/prof.mjs；§1.4(b) 证据）

```javascript
// R16-I axis 2 part B: --cpu-prof attribution of one run-children@custom spawn.
// Aggregates self time by script URL group to check whether the custom-cell
// increment hides a single unnamed hot site addressable from slice code.
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, cpSync, readdirSync, readFileSync } from "node:fs";
import { join } from "node:path";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r16i-bench";
const FIX = join(ROOT, "fixtures");
const PROF = join(ROOT, "prof");
const nodeBin = process.argv[2];
const state = process.argv[3] ?? "custom";

rmSync(PROF, { recursive: true, force: true });
mkdirSync(PROF, { recursive: true });
const sd = join(ROOT, `prof-state-${state}`);
rmSync(sd, { recursive: true, force: true });
cpSync(join(FIX, `state-${state}`), sd, { recursive: true });
const env = { ...process.env };
delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
const r = spawnSync(nodeBin, ["--cpu-prof", "--cpu-prof-dir", PROF, CLI,
  "run", "--project", join(FIX, "project"), "--objective", "bench objective",
  "--state-root", sd, "--children", join(FIX, "children.json")],
  { env, encoding: "utf8", timeout: 120000 });
if (r.status !== 0) { console.error(`FAIL: ${(r.stderr ?? "").slice(0, 300)}`); process.exit(1); }

const file = readdirSync(PROF).find((f) => f.endsWith(".cpuprofile"));
const prof = JSON.parse(readFileSync(join(PROF, file), "utf8"));
const byNode = new Map(prof.nodes.map((n) => [n.id, n]));
const self = new Map();
const deltas = prof.timeDeltas ?? [];
for (let i = 0; i < prof.samples.length; i++) {
  const id = prof.samples[i];
  self.set(id, (self.get(id) ?? 0) + (deltas[i] ?? 0));
}
function groupOf(url, fn) {
  if (url === "" || url.startsWith("node:")) return `node-internal:${url || "(vm)"}${fn ? "::" + fn : ""}`;
  if (url.includes("@earendil-works/pi-ai")) return "pi-ai:" + url.split("pi-ai/")[1];
  if (url.includes("/dist/")) return "dist:" + url.split("/dist/")[1];
  if (url.includes("node_modules")) return "dep:" + url.split("node_modules/")[1];
  return url;
}
const byGroup = new Map();
const byCoarse = new Map();
for (const [id, us] of self) {
  const n = byNode.get(id);
  const url = n.callFrame.url ?? "";
  const g = groupOf(url, n.callFrame.functionName);
  byGroup.set(g, (byGroup.get(g) ?? 0) + us);
  const coarse = url.includes("pi-ai") ? "pi-ai package" : url.includes("/dist/") ? "repo dist/" : (url === "" || url.startsWith("node:")) ? "node internals + GC/parse" : "other deps";
  byCoarse.set(coarse, (byCoarse.get(coarse) ?? 0) + us);
}
const total = [...self.values()].reduce((a, b) => a + b, 0);
console.log(`${nodeBin.includes("nvm") ? "v22.22" : "v22.14"} run-children@${state} total sampled=${(total / 1000).toFixed(1)}ms`);
console.log("coarse buckets:");
for (const [g, us] of [...byCoarse.entries()].sort((a, b) => b[1] - a[1])) {
  console.log(`  ${(us / 1000).toFixed(1).padStart(7)}ms ${(100 * us / total).toFixed(1).padStart(5)}%  ${g}`);
}
console.log("top 14 fine sites:");
for (const [g, us] of [...byGroup.entries()].sort((a, b) => b[1] - a[1]).slice(0, 14)) {
  console.log(`  ${(us / 1000).toFixed(1).padStart(7)}ms ${(100 * us / total).toFixed(1).padStart(5)}%  ${g.slice(0, 110)}`);
}
```

原始输出（22.22.2，custom 与 builtin 各一次全新 spawn）：

```
v22.22 run-children@custom total sampled=136.0ms
coarse buckets:
    107.3ms  78.9%  node internals + GC/parse
     27.6ms  20.3%  repo dist/
      1.1ms   0.8%  pi-ai package
top 14 fine sites:
     18.4ms  13.5%  node-internal:(vm)::(idle)
     16.0ms  11.7%  node-internal:node:internal/modules/esm/utils::compileSourceTextModule
     13.4ms   9.8%  node-internal:(vm)::(program)
      7.6ms   5.6%  node-internal:node:fs::realpathSync
      5.3ms   3.9%  node-internal:node:internal/bootstrap/realm::compileForInternalLoader
      4.3ms   3.1%  dist:routing/live-cascade.js
      3.2ms   2.4%  node-internal:(vm)::(garbage collector)
      3.2ms   2.3%  dist:supervisor/flowchart-supervisor.js
      3.2ms   2.3%  node-internal:node:internal/modules/esm/translators::jsonStrategy
      2.2ms   1.6%  node-internal:(vm)::getPackageScopeConfig
      2.1ms   1.6%  node-internal:node:internal/url::URL
      2.1ms   1.6%  node-internal:node:internal/modules/package_json_reader::get exports
      2.1ms   1.6%  dist:supervisor/flowchart-snapshot.js
      2.1ms   1.6%  node-internal:(vm)::ModuleWrap
---
v22.22 run-children@builtin total sampled=105.5ms
coarse buckets:
     77.8ms  73.7%  node internals + GC/parse
     27.7ms  26.3%  repo dist/
top 14 fine sites:
     18.4ms  17.4%  node-internal:(vm)::(idle)
      9.6ms   9.1%  node-internal:node:internal/modules/esm/utils::compileSourceTextModule
      7.8ms   7.4%  node-internal:(vm)::(program)
      6.9ms   6.5%  node-internal:(vm)::(garbage collector)
      4.3ms   4.1%  dist:routing/live-cascade.js
      4.3ms   4.1%  dist:supervisor/flowchart-supervisor.js
      4.2ms   4.0%  node-internal:node:internal/bootstrap/realm::compileForInternalLoader
      2.1ms   2.0%  dist:run/flowchart-run.js
      2.1ms   2.0%  dist:cli/main.js
      2.1ms   2.0%  node-internal:node:internal/modules/esm/resolve::defaultResolve
      2.1ms   2.0%  node-internal:(vm)::internalModuleStat
      2.1ms   2.0%  dist:run/child-tracking.js
      2.1ms   2.0%  node-internal:node:internal/url::getPathFromURLPosix
      1.3ms   1.2%  node-internal:node:internal/modules/esm/assert
```

MORE_OPTIMA=no
