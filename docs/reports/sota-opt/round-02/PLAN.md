# Round 2 分区（10 子代理，3-VM 分波）

模型一律 `claude-fable-5-thinking-xhigh`。基线：最新 `cursor/sota-persistent-opt-83a1`（含 Round 1 已合入成果）。

Round 1 已对十区做完第一遍。本轮在完整排除表之上再搜一遍：只接受排除表未覆盖、且理论+仿真达门槛的新更优解。禁止重开 S1-* / X* 条目。

分区与 Round 1 相同（R2-A … R2-J），报告写入 `docs/reports/sota-opt/round-02/`。
