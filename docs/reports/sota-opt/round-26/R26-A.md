MODEL_SLUG=claude-fable-5-thinking-xhigh

# R26-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第二十六遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 26 / R26-A（Round 1–25 同区第二十六遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `a9ebd8d`（开工于 `f953f92`——含 R25-H 空枚举收口、Round 26 开卷与 R26-A 派出；运行中 exclusive 快进一笔 docs-only 提交 `a9ebd8d`：R25-I 空枚举收口 + process-environment coordinate 禁移植扩栏 + R26-B 派出，按令 fast-forward 至新 tip，冻结主张全部在 `a9ebd8d` 复验，并对新禁移植栏做了专项划界（§0）；R25-J 在飞未触碰）
**分支:** `cursor/r26-a-tracking-twenty-sixth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S26-A-\* ID，切片零代码改动；
整片预算收口第二十四次复核成立，并新增一条实测轴——「环境耦合 /
collation-locale 面」（axis 15）补上二十五遍搜查史里最后一个从未受控的
输入坐标：**进程本身**。R14–R25 依次剂量了事件规模（R14/R15）、载荷
长度（R16）、组成与温度（R17/R18）、结局（R19）、驻留态（R20）、H 面
（R21）、导出面（R22）、轴间耦合（R23）、字段计数（R24）与字符串内容类
（R25）——全程**进程环境恒定**。而切片唯一的环境权威耦合（F6a 普查：
14 文件零 Date / Math.random / process.\* / performance / timers /
crypto / Intl.\* / toLocale\* token）恰是 types.ts 两处
`String.prototype.localeCompare`：:489 hashAssessment 维度 id 排序
（生产热 ×3 哈希腿/gate，切片第一大组件内）与 :516 hashSummary
operations 排序（生产零流量）。无参 localeCompare 咨询进程默认 locale
（ICU 读 LC_ALL/LANG）：6-id 闭集**不等于 locale 稳定**——捷克/斯洛伐克
把二合字母 "ch" 排在 h 后，"check-coverage" 对 "constraint-retention"
与 "evidence-consistency" 双双反序（F6b：15 对 × 27-locale 电池，恰
{cs, cs-CZ, sk} 偏离码元序，恰在含 check-coverage 的 2 对；其余 24
locale 全 15 对同码元序）。跨进程实证（F6c，LANG 实剂量真子进程）：
同一 assessment **值**逐字节 locale 不变（3/3 子进程复铸 JSON 相同），
但 cs_CZ 下 hashAssessment=63c67708 ≠ en_US/C/parent 的 e3b9e20；en
写入的 TRACKING_ASSESSMENT 在 cs_CZ 下被 validateEvent **fail-closed
拒绝**（"payload.assessmentHash mismatch"）——冻结哈希/幂等契约是
**环境耦合**的，账本只在同 collation 环境间可移植（且 LC_ALL=C 不还原
码元序而是 ICU 别名到 en-US）。进程内一致性无恙（F6d：每个受剂量 LANG
下全链条照常 apply 并复验自身事件）——风险面严格是跨环境移植性
（fail-closed 可用性归档事实），非性能洞、非 fail-open。定价三组
（三次独立运行区间）：**15a** 比较器 collation tax（码元替身
bench-only，en-US 下 40/40 种子化排列先证哈希相等再定价）
−14.1~+41.4 ns/hash ⇒ −211~+621 ns/run，噪声级无稳定方向，且 F6c
证明替身在 cs/sk 默认环境下**非哈希保持**——S3-B-3 / R6-A / R7-A 拒绝
从「换名/无收益」加强为「跨环境功能变更」；**15b** 环境本体剂量
（LANG=en_US/cs_CZ/C 三态 × 生产面）：hashAssessment cs/en
×1.66–1.71（1867–1936 vs 3181–3211 ns）、锚点格 ×1.17–1.23——裁剪
collator（cs）令 V8 localeCompare 快路径失效落入 ICU 全比较，生产默认
（en/C 族）恰坐最便宜格；**15c** hashSummary 比较器站点
ICU-engagement 类剂量（固定 n=64、12 字符名）：ASCII 16076–16239 ns、
case-mix ×0.99–1.00、two-byte ×2.41–2.43（39009–39067 ns）——双字节
操作数把比较器逐出 V8 单字节快路径入全 ICU collation（与 R25-A 14b
剂量的 stringify 编码类 ×1.01–1.04 站点不同、机制不同、操作数不同），
站点生产零流量（F6e）。全部可想摊平路径经换名检查逐一判为既有排除
ID 本体或功能性变更（4 项新记录，见 §0），**只产出归档证人与环境
坐标格网的闭合，未产出可落地候选**，按令即空收口。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R25-A 至本轮基线
（`a9ebd8d`）**逐字节未变**（`git diff 7acb666..a9ebd8d -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，二十六遍全程零
diff；清单核对恰 14 文件（12 + 2）并全部实际重读；开工 tip `f953f92`
与快进后 tip `a9ebd8d` 两点均验空）。调用面维持最强冻结并再延一轮：
R10-A 基线（`89f0455`）以来 `git diff 89f0455..a9ebd8d -- src/`
**完全为空**（跨 R10…R26 开轮十七段全部为 docs 提交），生产调用方经
本轮全库 grep 复核仍为 `supervisor.ts:483`（applyTrackingGate/
nextTrackingSeq；两个 settle 调用点 601/711 均不传 trackingAssessment
⇒ 生产 O(1) no-op，第十七次复验，本轮源码级重读
`settleSupervisedOutcome` 首行 `if (assessment === undefined) return`）
/ `coordinator.ts:444` / `flowchart-run.ts:320`（applyChildThreeLine），
每子结果一次（~5 次/run），事件表几十级（41）。R25-A 的预算天花板
（~69–89 µs/run；89 为次序孤点）经本轮在当前 VM（Node v22.22.2，
PATH 前置 nvm，遮蔽 `/exec-daemon/node` 垫片的默认 v22.14.0）实测
复核为 **~69–84 µs/run**（五格锚点矩阵三次独立运行 13.7–16.8
µs/gate，与 R25-A 69–89 / R24-A 67–84 / R23-A 70–82 重叠同带；本轮
**无孤点上沿**，最高格为 run1 FAILED×no-contract 16.8）。量级结论
不变——稳态下即使切片零成本，节省上界 ~0.07–0.08 ms/run，仍比落地线
低约三个量级。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
功能性变更（4 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项
+ R15-A 5 项 + R16-A 4 项 + R17-A 4 项 + R18-A 4 项 + R19-A 2 项 +
R20-A 3 项 + R21-A 2 项 + R22-A 5 项 + R23-A 5 项 + R24-A 5 项 +
R25-A 5 项共 68 项换名检查全部复核维持，未追认任何 S12…S25-A-\*
ID），按令一律不铸新 ID。

R12-A 分解的五大组件占比（hashAssessment×3 ~40–41% / validateEvent
~18–19% / turn ~11–12% / prescore×2 ~8.7–9.2% / O(E) 五扫描+拷贝
~1.8–2.0%）在本 VM 逐一复现（§1 表），R18-A skip 定价与 R19-A replay
定价抽测复现同带（guard-3 skip 1876–2004 ns = 12.9–13.3% of apply；
replay find#1 3194–3255 ns = 21.0–22.0% of apply）。第二十六遍收口，
**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；未触碰在飞的
  R25-J 分支与文件（R25-I 于本轮运行中由 exclusive 收口合入，其
  报告仅按令追读以做划界，分支未触碰），未触碰已收口的 R25-\* 报告。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表 545 行，含
  S13-B-1；R12-A…R25-A 空枚举未铸 ID，确认）/ PROGRESS.md /
  round-26/PLAN.md（A 段 + 全部 plan-ban 轴）/ R25-A / R24-A /
  R23-A / R22-A；运行中 exclusive 快进后追读 R25-I.md 全文做划界。
  EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
- 环境：本 VM 默认 Node v22.14.0（`/exec-daemon/node` 垫片遮蔽 nvm），
  按 engines（>=22.19.0）以 PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 切至 v22.22.2 后
  `pnpm install --frozen-lockfile`（pnpm 10.17.1），全部测量在
  v22.22.2 完成。
- **不移植禁令逐项核对**：R25-B task-face optional-field presence
  topology（B）/ R25-D identity-coordinate face（D）/ R25-F
  plan-header identity-geometry face（F）/ R25-G injection
  command-kind face（G）/ R25-H consumer-stage string-representation
  census（H）/ **R25-I process-environment coordinate（I；见下条
  专项划界）**/ R24-B M=1 catalog collapse / R24-D approval-authority
  face / R24-F state serialization-boundary provenance face / R24-H
  operand-residency census / R24-I adaptation-state coordinate 均未
  移植为 A ID。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN /
  KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB /
  OCCX / JB / RUNLIM / VALCLASS / CG / AGEX 未编号。
- **与 R25-I「process-environment coordinate」的专项划界**（该禁移植
  栏由运行中 exclusive 快进（`a9ebd8d`）带入，与本轮已在测的 axis 15
  名义相邻，逐字复核后判定**非移植非补铸**）：R25-I 的受控变量是
  I 切片配置输入面的**显式环境变量块**（`PI_*`/`SPARKLE_*` 作为
  config 读取的坐标），其报告全文零提及 locale/LANG/LC_ALL（本轮
  逐字复核），其装具从未剂量 collation；axis 15 的受控变量是进程的
  **隐式环境状态**——语言内建 `localeCompare` 经 ICU 咨询的默认
  collation locale，切片内零 `process.*` 读取（F6a 普查），证人是
  跨进程哈希分歧与 fail-closed 拒验（冻结契约面），后果面是账本
  移植性而非配置行为，生产面是 A 切片哈希腿而非 I 切片 config 装载。
  坐标、站点、证人、后果四不同；名义重叠仅在「环境」一词。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或功能性变更，**一律不铸 S26-A-\* ID**。R12-A…R25-A 共 68 项
  换名检查全部复核维持，不再逐条重列；以下 4 项为本轮**新记录**，
  全部由 axis 15 的环境视角引出）：
  1. **hashAssessment 比较器换码元序**（`localeCompare` →
     `<`/`>` 码元比较，15a 的直接摊平路）= **S3-B-3 / R6-A / R7-A
     家族本体**（排序/比较器换名家族既有拒绝），且本轮**加强**：
     F6c 证明该替身在 cs/sk 默认环境下**非哈希保持**——同一
     assessment 换比较器改哈希值 = 冻结哈希契约的功能性变更；15a
     实测 tax 本身 −14.1~+41.4 ns/hash 噪声级无稳定方向，拒列；
  2. **钉死显式 locale**（`localeCompare(b, "en")` 或模块级
     `Intl.Collator("en")` 复用）= 对 en 族环境哈希保持，但对 cs/sk
     默认环境**改变既有账本的复验结局**（功能性变更——这是移植性
     硬化提案不是性能优化，按 proposal-first 归 parent 决策面，本
     报告只归档事实）；性能上显式 collator 路径慢于默认快路径
     （15b cs 格 ×1.66–1.71 即 ICU 引擎价的实测下界），拒列；
  3. **6-id 闭集排序结果记忆化 / 预排序常量表**（比较器只作用于
     闭集 ⇒ 排一次缓存复用）= **X1-1 / S8-G-2 memo 禁令本体**（哈希
     输入静默缓存家族）+ R12-A §0-11 常量折叠伞；且该缓存若跨模块
     生命周期共享，会把 F6c 的环境分歧固化成单进程内快照（错误
     方向），拒列；
  4. **进程级 locale 归一化**（启动时设 `LC_ALL` 或部署文档强制
     collation 环境）= 切片外变更（不在 14 文件内），属部署/数据面
     契约的环境要求条款；且 F6c 证明 `LC_ALL=C` 在 Node/ICU 下只是
     别名到 en-US 而非码元序——归一化目标本身依赖 ICU 回退语义。
     报告级归档，拒列为 A 切片 ID。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  幂等双 find/死扫描 = S8-A-1 / S11-A-2；episode/seq/status/copy 五
  扫描逐条 = X0-4/X2-4/S1-A-1/S1-A-9/R1-A 清单（R12-A 起锁定）；写侧
  validateEvent = S4-G-2 族（实现在 events.ts，G 切片）；第二次
  prescore = S1-A-2；turn 恒定输入常量折叠 = R12-A §0-11 伞 +
  R16-A §0-2；skip 守卫次序/享元/prescore 复用 = R18-A §0-1/2/4
  维持；replay 快路径/重算去重 = R19-A §0-1/2 维持；hashSummary
  缓存/增量 = R20-A §0-2/3 维持；H 面 = R21-A 维持；分析-隔离面 =
  R22-A §0-1..5 维持；G×E 积分账本五路 = R23-A §0-1..5 维持；
  输入面字段粒度五路 = R24-A §0-1..5 维持；内容类五路 = R25-A
  §0-1..5 维持。
- 未重开任何 X\* / S1-\* … S25-\* 条目。**R25-A value-domain / 字符串
  内容类普查不补铸、不重测**——axis 15 全程固定全部字符串内容为生产
  纹理 ASCII，只动进程环境；15c 虽用双字节名，但其受控变量是
  **比较器站点的 ICU-engagement 类**（hashSummary :516 的比较器
  操作数），R25-A 14b 剂量的是 **stringify 编码类**（hashAssessment
  :501 的序列化载荷，turnId 非比较器操作数）——站点不同、机制不同、
  操作数不同，且 R25-A 三消费者清单（正则扫描 / JSON 哈希 / 相等
  扫描）从未包含 collation 比较器；**R24-A residual input-face
  list-dimension 不补铸**（axis 15 全程固定计数）；**R23-A G×E
  自耦合积分账本不补铸**（全程固定 E=41）；**R22-A 公开导出普查
  不补铸**（hashSummary 切片外零生产调用方作为既有事实引用）；
  **R21-A H 输入面不补铸**（humanInput ≡ `{}`）；**R20-A openMinors
  M 轴与驻留链 K 不补铸**（F6e 只把 K=0 作为可达性事实复证）；
  R19/R18 各一格组成连续性抽测；R17/R16/R15/R14 均不重测。
  S11-A-1 / S11-A-2 / S10-A-1 / S9-A-1 / S8-A-1..3 / S7-A-1..4 /
  X0-4/X2-4 / X0-6 / S6-A-1 专项复核：本轮零触碰、零重提，全部
  维持关闭。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点五格
  （gate-outcome × contract × max-codes）覆盖；配置态逐面复核——
  (a) settle 带/不带 assessment：生产两调用点 601/711 均不带
  （O(1) no-op 源码级复验），带 assessment 臂即 applyTrackingGate
  本体（已价）；(b) trackingConfig 覆写/applyUserThreshold：生产
  from-child 恒走 DEFAULT（R22-A config 双臂已价，X0-7 阈值冻结
  维持）；(c) maxItems 截断态 = S2-A-1 已拒面；(d) policyVersion 为
  透传字符串无分支；(e) **本轮新增：环境态本身**（LANG=en_US /
  cs_CZ / C 三态 × 锚点格与 hash 腿 = 15b 矩阵）——进程环境首次
  成为受控配置坐标，无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足——axis 15 只经公开
  hashAssessment/hashSummary/validateEvent/applyChildThreeLine/
  applyTrackingGate 剂量，F6a–F6e 是对环境权威面/序稳定性/契约耦合/
  进程内一致性/可达性的**归档证人**，未改任何阈值、未改任何测试；
  F6c 的移植性事实**只归档不修复**（任何修复路径都是冻结契约变更，
  须按 proposal-first 走 parent 决策面）。不声称 Outcome-supported；
  Checkpoint F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，未提交
  任何仿真脚本（按令不建 repo scripts），仿真全文见本报告附录。
  不开 PR。

## 1. 预算支配论证复核 + 一条新轴（方法第 1、2 步）

R3-A §3 … R25-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..a9ebd8d -- <切片>` 为空，逐字节
   一致（二十六遍全程零 diff；开工 tip `f953f92` 与快进后 tip
   `a9ebd8d` 两点均验空）——R1-A…R25-A 全部测量与排除按令转移，
   但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨十七段）**：`git diff 89f0455..a9ebd8d --
   src/` **完全为空**（R10 开轮以来全部提交均在 docs/ 下）。生产调用方
   三点位（supervisor.ts:483 / coordinator.ts:444 /
   flowchart-run.ts:320）、settle 调用点 601/711 不传 assessment
   （首行 `if (assessment === undefined) return`）经本轮全库 grep +
   源码级重读复验，无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               16.1      15.7      15.1  µs/gate (+1 event)
PASSED x contract(3)               15.5      14.9      14.5  µs/gate (+1 event)
FAILED x no-contract               16.8      16.4      15.5  µs/gate (+3 events)
FAILED x contract(3)               14.7      13.9      13.7  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.8      14.3      14.2  µs/gate (+3 events)
=> ~69–84 µs/run（稳态热；本轮无孤点上沿，最高格 run1 FAILED×no-contract 16.8）
```

与 R25-A（13.9–17.9 µs/gate ⇒ ~69–89 µs/run，89 为次序孤点）/
R24-A（67–84）/ R23-A（70–82）重叠同带；五格三次运行**无悬崖、无
空洞**。本轮仿真在锚点前驱动了 F6c/F6d 的 LANG 子进程与 15 系剂量，
PIC 污染路径与 R21-A…R25-A 同型但本轮未拉出孤点。稳态下即使把整个
切片优化到零成本，节省上界 ~0.07–0.08 ms/run，仍比战役落地线（数十~
数百 ms 或复杂度类下降）低**约三个量级**。**支配论证（稳态口径）
第二十四次复核成立**；生产冷态口径维持 R17-A 轴 6b 裁决（[2.7, 8.5]
ms/run once-per-run，不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R25-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R25-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1969–2060 ns 每次 ⇒ **5.91–6.18 µs** | 5.67–5.90 µs | ~40–41% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 309–338 ns + 第三次 hash） | **2.77–2.81 µs** | 3.01–3.14 µs | ~18–19% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.69–1.72 µs** | 1.72–1.91 µs | ~11–12% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 665–676 ns 每次 ⇒ **1.33–1.35 µs** | 1.35–1.58 µs | ~8.7–9.2% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 78–79 + seq 46–49 + 双幂等 find 39–41×2 + status 50–52 + `[...events]` 26–29 ns） | **282–287 ns**（⇒ 6.87–6.99 ns/event @E=41） | 279–284 ns | **~1.8–2.0%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以当轮 contract 格（14.5–15.5 µs）计。`applyTrackingGate` 整体
隔离实测 5.81–6.04 µs，与 R25-A（5.92–6.21）/ R24-A（5.84–5.95）
同带。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次运行区间）：guard-3 skip（FAILED 零 evidence refs）1876–2004 ns
= 12.9–13.3% of apply（R25-A 13.0–13.3% 同带）；replay find#1
（GT idempotencyKey 命中，E=44）3194–3255 ns = 21.0–22.0% of apply
（R25-A 20.0–20.1% 邻带）——组成天花板 {apply, skip, replay} 两个半边
继续成立，不重测其余格。

**本轮新轴 15：环境耦合 / collation-locale 面**（确定性剂量经公开
hashAssessment / hashSummary / validateEvent / 生产铸造面（mkChild →
observationFromChild → assessChildObservation）→ applyChildThreeLine
驱动，固定 E=41、固定全部字符串内容与计数（与 R25-A 内容类、R24-A
计数维正交）；环境剂量用 **LANG/LC_ALL 实剂量真子进程**（tsx CLI
spawn，非 mock）；三次运行区间）：

```text
证人（F6a–F6e 钉死，三次运行证人行逐字节一致）:
  F6a: 环境权威普查——14 文件零 Date / Math.random / process.* /
    performance / hrtime / timers / crypto / Intl.* / toLocale* token；
    唯一环境耦合 = String.prototype.localeCompare 恰两站点
    [types.ts:489（hashAssessment 维度 id 排序，生产热 ×3 腿/gate）,
    types.ts:516（hashSummary operations 排序，生产零流量）]；切片
    其余全部字符串操作（正则 /i 简单大小写折叠、toFixed、trim、
    JSON.stringify、默认 Array.prototype.sort 码元比较）按 spec
    locale 无关——gate.codes 与 hashSummary 各 id 列表排序用默认
    比较器，构造上 locale 稳定
  F6b: 闭域跨 locale 序证人——6 维度 id 全 15 对 × 27-locale 电池
    （en/en-US/cs/cs-CZ/sk/de/sv/da/fi/nb/hu/tr/pl/lt/et/is/fr/es/
    ja/zh/ko/ru/el/he/ar/th/vi）：恰 {cs, cs-CZ, sk} 偏离码元序，
    恰在含 "check-coverage" 的 2 对（捷/斯把二合字母 "ch" 排 h 后
    ⇒ check-coverage 排到 constraint-retention 与
    evidence-consistency 之后）；其余 24 locale 全 15 对同码元序。
    闭集 ≠ locale 稳定
  F6c: 跨进程环境证人（LANG=en_US.UTF-8 / cs_CZ.UTF-8 / C 实剂量
    真子进程，生产铸造 + 写侧复验）——assessment 值逐字节 locale
    不变（3/3 子进程复铸 JSON 与父进程相同：铸造路径零环境耦合）；
    hashAssessment：en_US=e3b9e20 = C(→ICU 回退别名 en-US) =
    parent，但 cs_CZ=63c67708（同一 assessment 不同哈希；cs 下排序
    [constraint-retention, evidence-consistency, check-coverage,
    narrative-coherence, progress-vs-stall, scope-safety]）；en 写入
    的 TRACKING_ASSESSMENT 事件在 cs_CZ 下被 validateEvent
    fail-closed 拒绝（"Invalid Event: payload.assessmentHash
    mismatch: does not match hashAssessment(assessment)"）。冻结
    哈希/幂等契约是环境耦合的：账本只在同 collation 环境间可移植；
    LC_ALL=C 不还原码元序（POSIX C ≠ 码元，ICU 别名 en-US）
  F6d: 进程内一致性证人——每个受剂量 LANG 下全链条
    （applyChildThreeLine over 41 事件表）照常 apply 并复验自身全部
    事件（en/cs/C: applied=true, validate ok）——风险面严格是跨环境
    移植性（fail-closed 可用性），永不进程内完整性、永不 fail-open；
    单环境部署不受影响
  F6e: 生产可达性普查——每个生产 assessment（PASSED/FAILED 双臂）
    都带全 6 id 维度向量 ⇒ locale 敏感对在每个生产哈希里（非边缘
    格）；hashSummary 站点（:516）生产零流量（from-child 窗口无
    previous ⇒ prevSummaryHash undefined，R20-A K=0 复证）

15a  比较器 collation tax（码元替身 bench-only，en-US 下 40/40
     种子化维度排列先证哈希相等再定价；替身永不落地）:
  production hashAssessment 1969–2060 ns vs 码元替身 1983–2071 ns
  ⇒ tax −14.1 / +41.4 / −10.7 ns/hash（三次运行）×3 腿 ×5 gate
    ⇒ −211~+621 ns/run 上界——噪声级、无稳定方向
  ⇒ 且 F6c 证明替身在 cs/sk 默认环境下非哈希保持：换比较器 =
    冻结契约的跨环境功能变更（S3-B-3/R6-A/R7-A 拒绝加强，§0-1）

15b  环境本体剂量（LANG 三态 × 生产面；子进程协议 20k warm/200k
     reps hash、1.5k/5k anchor，冷起子进程数值系统性高于父进程锚点
     矩阵，仅作同协议跨 locale 比较）:
  hashAssessment   en_US 1872–1936ns | C 1867–1906ns | cs_CZ
                   3181–3211ns（cs/en ×1.66–1.71）
  锚点格 PASSED×contract(3)
                   en_US 17.1–18.4µs | C 16.3–17.7µs | cs_CZ
                   20.4–21.6µs（cs/en ×1.17–1.23）
  ⇒ 裁剪 collator（cs 定制排序表）令 V8 localeCompare 默认 locale
    快路径失效落入 ICU 全比较；en 与 C 同带（F6c 别名证人的性能
    回声）；生产默认环境（en/C 族 CI/服务器）恰坐最便宜格

15c  hashSummary 比较器站点 ICU-engagement 类剂量（固定 n=64 ops、
     固定 12 字符名、种子化乱序；受控变量 = 比较器操作数的编码类，
     与 R25-A 14b 的 stringify 载荷类不同站点不同机制）:
  ASCII 16076–16239ns | case-mix ×0.99–1.00 | two-byte
  39009–39067ns（×2.41–2.43）
  ⇒ 双字节操作数把 localeCompare 逐出 V8 单字节快路径入全 ICU
    collation（对照：R25-A 14b stringify 编码类仅 ×1.01–1.04）；
    站点生产零流量（F6e），纯公开 API 面定价；比较器换码元 =
    R6-A 原始的 locale 内分歧拒绝 + 本轮 §0-1 跨环境加强
```

五个结论：

1. **输入坐标系最后一维闭合**：R14–R25 把切片的全部显式输入
   （事件表、参数、载荷、组成、结局、驻留、H、导出、耦合、计数、
   内容）剂量殆尽，进程环境是唯一从未受控的隐式坐标——本轮补上：
   环境权威普查（F6a 恰两站点）、闭域序稳定性（F6b 24/27）、契约
   耦合（F6c）、一致性（F6d）、可达性（F6e）与三组定价（15a–15c）。
2. **归档移植性事实（非性能洞、非 fail-open）**：冻结哈希/幂等契约
   经 localeCompare 耦合进程默认 collation——同一 assessment 在
   cs/sk 族环境下哈希不同，跨环境搬运账本被 validateEvent
   fail-closed 拒绝。这是**可用性/移植性**归档事实：任何修复
   （显式 locale 钉死 / 码元序规范化）都是冻结契约变更，按
   proposal-first 归 parent 决策面（§0-2/§0-4），本轮只归档不修复。
3. **环境是真实性能坐标但生产坐最便宜格**：cs 族环境锚点格
   ×1.17–1.23、hash 腿 ×1.66–1.71——若生产部署落入裁剪 collator
   族才实体化，且届时摊平路仍是已拒家族（§0-1/2/3），无候选。
4. **hashSummary 比较器 two-byte 类 ×2.41–2.43**——站点生产零流量
   （R20-A previous 面），纯公开 API 面定价，零牵引。
5. **不产生候选**：比较器换码元 = S3-B-3/R6-A/R7-A 加强（§0-1）；
   显式 locale = 功能性变更 + proposal 面（§0-2）；闭集排序缓存 =
   X1-1/S8-G-2（§0-3）；进程级归一化 = 切片外 + 别名语义（§0-4）。
   15a 实测证明换比较器连纯性能收益都不存在（噪声级）。

**重开条件全景更新（在 R25-A §1 表基础上新增两行）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| M（openMinors 条数） | 0 | ≈ 4.6–5.8×10⁴ | ~4+ | R20-A |
| K（驻留链 turn 数） | 0（恒 fresh） | ≈ 3×10³ turn | ~2 | R20-A |
| H_L / H_m / H_N（humanInput 三维） | 0 | ≈ 1.2 MB / 2.2–2.3×10⁴ / 4.65–4.74×10⁴ | ~3–5 | R21-A |
| T（analysis content 操作基数） | 0（面零流量） | ≈ 1.50–1.51×10⁵ | ~5 | R22-A |
| G（gates/run，固定 E 线性算术） | ~5 | ≈ 645–714 | ~2.1 | R16-A 系 |
| G（gates/run，含 G×E 自耦合二次项） | ~5 | ≈ 341–344 | ~1.8 | R23-A |
| N_op / N_cl / W×O / Q×D / CF / Cc/Rt 对角线外 | 1 / 1 / 0 / 0 / 2 / 原点 | 见 R24-A 表 | ~4.3–5.5 / 面须先获流量 | R24-A |
| claim 内容类 / turnId 转义编码类 / includes 分歧纹理 | 最便宜格 | 见 R25-A 表 | ~4–4.7 / — | R25-A |
| **环境坐标（collation locale）** | **en/C 族（最便宜格）** | **非字节量轴：部署态坐标——cs/sk 族默认环境实体化 ×1.17–1.23/锚点格、×1.66–1.71/hash 腿；摊平路全为已拒家族** | **—** | **本轮** |
| **hashSummary 比较器 ICU-engagement 类** | **站点零流量** | **面须先获生产流量（R20-A previous 面）；two-byte 类 ×2.41–2.43** | **面须先获流量** | **本轮** |
| G 组成（apply/skip/replay 全混合） | 全 apply | 混入只降不升 | — | R18-A + R19-A |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–15.2 µs/fault | — | R18-A + R22-A |
| 重投递面（once-per-redelivery） | 0 次/run | ~2.8–3.4 µs/次 | — | R19-A + R23-A F3d 形态 |

**可操作重开优先级不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7
家族 `every×includes` 站点 Set 化）仍是第一优先重开对象。本轮
关闭的是**环境坐标维**：R14–R16 关规模/载荷维、R17 关组成/温度维、
R18 关组成/故障维、R19 关结局维、R20 关驻留状态维、R21 关输入参数
维、R22 关公开导出维、R23 关轴间耦合维、R24 关字段粒度维、R25 关
内容类维之后，**显式输入坐标系之外的隐式坐标（进程环境）自此
有价**——且额外产出一条冻结契约的移植性归档事实（F6c）供 parent
决策面引用。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 4 项 + R12-A…R25-A
68 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、功能性
变更或数据补充，按「换名不铸 ID」纪律未列任何 S26-A-\* 候选。空枚举
与 R12-A…R25-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第二十六遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `types.ts` | **axis 15 主宿主**（:489/:516 恰两处 localeCompare = 切片唯一环境耦合，F6a；15a 码元替身 tax −14.1~+41.4 ns/hash 噪声级；15c 比较器 ICU-engagement 类 two-byte ×2.41–2.43 零流量站点；F6c 哈希契约环境耦合归档）；比较器换码元 = §0-1（S3-B-3/R6-A/R7-A 加强）拒列；显式 locale 钉死 = §0-2（功能性变更 + proposal 面）拒列；闭集排序缓存 = §0-3（X1-1/S8-G-2）拒列；hash×3 地板复现 5.91–6.18 µs；X1-1/S8-G-2 memo 禁维持；S2-A-5 / X1-2 / S5-H-3 / S5-A-2 / S1-A-8 / X0-5 维持 | 无新候选 |
| `prescore.ts` | 本轮零环境角度（SUCCESS_CLAIM 正则 /i 是 spec 级简单大小写折叠，locale 无关——F6a 普查行覆盖；includes 相等是码元语义无 collation）；R25-A 14a/14c 内容类格维持；S1-A-3 / S1-A-4/X4-7 / S6-A-1 / S8-A-3 维持；C 轴可操作重开第一优先不变 | 无新候选 |
| `turn.ts` | F6e hashSummary 零流量复证宿主（runTrackingTurn → rollSummary 无 previous ⇒ prevSummaryHash undefined，R20-A K=0）；turn 内两处默认 sort（anomalyCodes 等）码元比较器 locale 无关（F6a/F6b 注记）；R13-A 八子组件 / R12-A §0-11 / R16-A §0-2 / R24-A 13a/13e / R25-A 14a turn 格维持 | 无新候选 |
| `from-child.ts` | **F6c 值不变性证人源**（3/3 LANG 子进程经 observationFromChild → assessChildObservation 复铸逐字节相同 JSON：铸造路径零环境耦合、零 localeCompare）；F6e 全 6 id 双臂可达性普查源；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `gate-apply.ts` | F6c 写侧拒验回声宿主（TRACKING_ASSESSMENT 哈希复验在 events.ts validateEvent，G 切片；apply 面 idempotencyKey `${hash}:${seq}` 的哈希环境耦合随 F6c 归档）；15b 锚点格环境剂量承载（cs ×1.17–1.23）；O(E) 清单 / S11-A-2 / S8-A-1 / S2-A-4 / R15-A §0-1 / R23-A §0-1..5 维持 | 无新候选 |
| `child-tracking.ts` | 15b 环境剂量锚点宿主 + F6d 进程内一致性证人（每 LANG 全链 applied=true + 自validate ok）；锚点/分解宿主复测同带；guard-3 skip 抽测 1876–2004 ns 复现（R18-A 7a 维持）；字符串透传无环境分支 | 无新候选 |
| `roller.ts` | hashSummary 唯一切片内调用方（previous 面零流量维持，R20-A；15c 剂量经公开 hashSummary 直驱不经 roller 生产路径）；其 sort 站点用默认比较器 locale 无关（F6a）；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `gates.ts` | 本轮零环境角度（布尔/数值面，零字符串比较；F6a 普查行）；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（humanInput ≡ `{}`）；其正则 /i 同 spec 级折叠 locale 无关（F6a）；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 / R21-A 维持 | 无新候选 |
| `combined-score.ts` | 本轮零环境角度（标量对输入；toFixed 按 spec locale 无关——非 toLocaleString，F6a 注记）；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |
| `config.ts` | 配置态矩阵复核宿主（生产恒 DEFAULT；R22-A 双臂价维持）；X0-7 阈值冻结维持；零环境读取（对照：R25-I 的 I 切片 env-var 配置面在本切片不存在——F6a 零 process.\*，§0 划界） | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方）；其 JSON.stringify 无 localeCompare（F6a）；S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R22-A 隔离三函数价维持；index.ts 纯再导出零逻辑；惰性 import = R12-A §0-10 维持；零环境 token（F6a） | 无新候选 |

**对 R25-A 声明的裁决**：「字符串输入面计数 × 长度 × 内容类三维格网
自此全部有价」在其维度上成立；R25-A 的三消费者清单（正则扫描 / JSON
哈希 / 相等扫描）枚举的全部是**内容敏感**消费者——切片还有一个
**环境敏感**消费者（collation 比较器）不在其中，且整个战役从未把
进程环境本身作为受控变量，本轮补上并闭合（F6a–F6e 五组证人 +
15a–15c 三组定价）。本轮全部新数据**只产出归档证人、环境坐标格网
闭合与一条冻结契约移植性事实，未产出可落地候选**。收口的预言力
第二十六遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R25-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致；退出码 0）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F6a: 14 文件零环境 token；恰两处 localeCompare [types.ts:489, :516]
F6b: 27-locale 电池恰 {cs, cs-CZ, sk} 偏离、恰 2 对含 check-coverage
F6c: en=C=parent=e3b9e20 | cs_CZ=63c67708 | cs 拒验 fail-closed
     （"payload.assessmentHash mismatch"）| 值不变 3/3
F6d: 每 LANG 进程内 applied=true + 自验 ok | F6e: 6/6 id 双臂 +
     hashSummary 零流量（prevSummaryHash undefined）
15b: hash en 1879 | C 1906 | cs 3181ns (×1.69)；anchor en 17.1 |
     C 16.9 | cs 21.0µs (×1.23)
anchor: 16.1 / 15.5 / 16.8 / 14.7 / 14.8 µs/gate => ~73–84 µs/run
decomposition: hash×3=6180ns | validateEvent=2806ns | turn=1704ns
    | prescore×2=1353ns | 5 scans+copy=282ns (6.89 ns/event)
    | applyTrackingGate=5.85µs
spot: skip guard-3 2004ns (12.9%) | replay find#1 3255ns (21.0%)
15a: prod 2060ns vs 码元替身 2071ns => tax −10.7ns/hash（−160ns/run）
     ；40/40 排列哈希相等（en-US）
15c: ASCII 16239 | case-mix 16081 (×0.99) | two-byte 39067ns (×2.41)
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F6a–F6e: 证人行逐字节同 run 1
15b: hash en 1936 | C 1870 | cs 3211ns (×1.66)；anchor en 17.2 |
     C 17.7 | cs 20.4µs (×1.18)
anchor: 15.7 / 14.9 / 16.4 / 13.9 / 14.3 µs/gate => ~69–82 µs/run
decomposition: hash×3=6110ns | validateEvent=2770ns | turn=1690ns
    | prescore×2=1331ns | 5 scans+copy=282ns (6.87 ns/event)
    | applyTrackingGate=6.04µs
spot: skip guard-3 1979ns (13.3%) | replay find#1 3194ns (21.4%)
15a: prod 2037ns vs 码元替身 1995ns => tax +41.4ns/hash（+621ns/run）
15c: ASCII 16148 | case-mix 15922 (×0.99) | two-byte 39009ns (×2.42)
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F6a–F6e: 证人行逐字节同 run 1
15b: hash en 1872 | C 1867 | cs 3203ns (×1.71)；anchor en 18.4 |
     C 16.3 | cs 21.6µs (×1.17)
anchor: 15.1 / 14.5 / 15.5 / 13.7 / 14.2 µs/gate => ~69–77 µs/run
decomposition: hash×3=5907ns | validateEvent=2805ns | turn=1723ns
    | prescore×2=1332ns | 5 scans+copy=287ns (6.99 ns/event)
    | applyTrackingGate=5.81µs
spot: skip guard-3 1876ns (12.9%) | replay find#1 3200ns (22.0%)
15a: prod 1969ns vs 码元替身 1983ns => tax −14.1ns/hash（−211ns/run）
15c: ASCII 16076 | case-mix 16032 (×1.00) | two-byte 39047ns (×2.43)
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立仿真运行 F6a–F6e 证人行逐字节一致（环境权威普查/序稳定性/
契约耦合哈希值/拒验消息/一致性/可达性确定性重现——含 e3b9e20 与
63c67708 两个哈希值三次逐字节相同）；锚点五格三次同带无悬崖、无
孤点；15b 环境剂量三次同向（cs 恒最贵、en/C 同带）；15a tax 三次
异号（噪声级裁决稳固）；15c 类比三次区间宽 ≤0.02。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S26-A-\*）。换名检查 4 项新记录见 §0（1–4），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md；本轴轴名
（环境耦合 / collation-locale 面）按令**不加 EXCLUSIONS 行**，由
parent 决定 plan-ban。

重开条件本轮**新增一条收口**：

**环境耦合 / collation-locale 收口**：切片输入坐标系的最后一维
（进程环境）自此有价——唯一环境耦合站点恰两处（F6a）、闭域序稳定性
24/27 locale（F6b：偏离恰 {cs, cs-CZ, sk} × 含 check-coverage 的
2 对）、冻结哈希契约环境耦合 + fail-closed 拒验归档（F6c：e3b9e20
vs 63c67708）、进程内一致性无恙（F6d）、生产可达性全覆盖 +
hashSummary 零流量（F6e）、三组定价（15a 比较器 tax 噪声级且替身
跨环境非哈希保持 / 15b 环境本体 cs 族 ×1.17–1.23 锚点、×1.66–1.71
hash 腿 / 15c 零流量站点 two-byte ×2.41–2.43）。重开条件：
(a) 生产部署开始**跨 collation 环境共享账本**（多主机/容器 locale
不齐）——届时移植性硬化按 proposal-first 走 parent 决策面（显式
locale 钉死或哈希前码元序规范化都是**冻结契约变更**，先撞
S3-B-3/R6-A/R7-A 家族与数据面契约冻结，且须带迁移方案）；
(b) 生产环境默认 locale 落入裁剪 collator 族（cs/sk）——性能面
×1.17–1.23 实体化，但摊平路仍是已拒家族（§0-1/2/3），预期仍无
候选；(c) hashSummary 站点获得生产流量（R20-A previous 面重开
条件）且 operations 名进入双字节类——届时 15c 的 ×2.41–2.43 与
R20-A M/K 轴一并重审。**可操作重开第一优先不变**：C 轴（~2.5 个
量级，R15-A；S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf26a01`–`0xf26a02`。F6c/15b 的 LANG 剂量以
`spawnSync` 起真子进程（`node_modules/tsx/dist/cli.mjs`），子进程源码
由脚本写入临时目录。

```ts
/**
 * R26-A deterministic anchor + decomposition + NEW-axis simulation
 * (twenty-sixth pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R25-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R25-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation, 5-scan
 *       O(E) inventory), plus compact spot re-confirmations of the R18-A
 *       skip pricing (guard-3) and the R19-A replay pricing (find#1);
 *   (c) NEW axis 15: the AMBIENT-ENVIRONMENT COUPLING census
 *       (collation-locale face). Twenty-five passes priced the slice's
 *       inputs by scale (R14/R15), payload length (R16), composition
 *       (R17/R18), outcome (R19), residency state (R20), human face
 *       (R21), export face (R22), axis coupling (R23), field count (R24)
 *       and string content class (R25) -- always with the PROCESS
 *       ENVIRONMENT held fixed. The process's ambient configuration
 *       (the Intl default locale) was never the controlled variable,
 *       anywhere in the campaign. It matters here because the slice's
 *       ONLY ambient-authority coupling (F6a census: zero Date /
 *       Math.random / process.* / timers / crypto across all 14 files)
 *       is String.prototype.localeCompare at exactly two sites:
 *         - types.ts:489 hashAssessment dimension-id sort -- production
 *           hot (x3 hash legs/gate, the slice's #1 component); and
 *         - types.ts:516 hashSummary operations sort -- zero production
 *           flow (from-child windows carry no `previous`, R20-A K=0).
 *       localeCompare with no arguments consults the process default
 *       locale (ICU reads LC_ALL/LANG). The dimension ids are a closed
 *       6-value union, but closed does NOT mean locale-stable: Czech and
 *       Slovak collate the digraph "ch" as a separate letter after "h",
 *       which reorders "check-coverage" against BOTH
 *       "constraint-retention" and "evidence-consistency". The axis
 *       therefore (1) witnesses cross-locale order stability over the
 *       whole closed domain against a 27-locale battery, (2) witnesses
 *       the cross-process consequence on the frozen hash contract with
 *       real LANG-dosed child processes (same assessment, different
 *       hashAssessment value; fail-closed validateEvent rejection of a
 *       ledger written under the other environment), (3) prices the
 *       collation comparator inside hashAssessment against a
 *       codeunit-comparator replica (bench-only replica, never landed),
 *       (4) doses the environment itself on the production surface
 *       (anchor cell + hash micro under LANG=en_US/cs_CZ/C children),
 *       and (5) doses the ICU-engagement class of the zero-flow
 *       hashSummary comparator site (ASCII vs case-mix vs two-byte
 *       names at fixed count and length -- the COMPARATOR path, distinct
 *       from R25-A 14b which dosed the stringify encoding class).
 *
 * Archival-fact witnesses (deterministic, seeded):
 *   F6a: ambient-authority census. The 14 slice files contain zero
 *        ambient tokens except exactly two localeCompare sites in
 *        types.ts (489/516). Everything else the slice does (regex /i
 *        simple case folding, toFixed, trim, JSON.stringify, Number,
 *        Array.prototype.sort default comparator) is locale-independent
 *        by spec.
 *   F6b: closed-domain cross-locale order witness. All 15 pairs of the
 *        6 dimension ids, against Intl.Collator(loc) for a 27-locale
 *        battery: exactly {cs, cs-CZ, sk} diverge from codeunit order,
 *        exactly on the 2 pairs involving "check-coverage" (the ch
 *        digraph); every other locale agrees with codeunit order on all
 *        15 pairs. gate.codes / hashSummary id-list sorts use the
 *        default Array sort comparator (codeunit) and are
 *        locale-independent by construction.
 *   F6c: cross-process environment witness (LANG-dosed real children,
 *        production mint + gate + write-side validateEvent):
 *        the assessment VALUE minted by the production from-child path
 *        is bitwise locale-invariant (child re-mints, JSON identical),
 *        but its hashAssessment differs under LANG=cs_CZ.UTF-8; a
 *        TRACKING_ASSESSMENT event written under en_US is rejected
 *        fail-closed by validateEvent under cs_CZ
 *        ("payload.assessmentHash mismatch"); LC_ALL=C resolves to
 *        en-US (ICU fallback), i.e. POSIX C does not restore codeunit
 *        order, it aliases en-US.
 *   F6d: within-process consistency witness. Under every dosed LANG the
 *        full production chain (applyChildThreeLine over the 41-event
 *        table) applies its gate and re-validates its own events --
 *        the risk surface is strictly cross-environment portability
 *        (fail-closed availability), never in-process integrity.
 *   F6e: production reachability census. Every production assessment
 *        carries all 6 dimension ids (the from-child mint emits the
 *        fixed 6-dimension vector), so the locale-sensitive pair is in
 *        EVERY production hash, not an edge case; the hashSummary site
 *        is zero-flow (from-child window has no `previous`).
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf26a01..0xf26a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { spawnSync } from "node:child_process";
import { readFileSync, writeFileSync, mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore, type PrescoreInput } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  hashSummary,
  parseTrackingAssessment,
  type SummaryHashInput,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";
import { hash32 } from "/workspace/src/domain/hash.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    summary?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary =
    opts?.summary ?? (opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran");
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

/** Production-texture prescore input (mirrors prescoreInputFromObservation
 *  for a PASSED tester child with contract(3)). */
function basePrescoreInput(): PrescoreInput {
  const constraints = [
    { id: "con_0", text: "constraint 0 text", kind: "constraint" as const, mandatory: true as const },
    { id: "con_1", text: "constraint 1 text", kind: "constraint" as const, mandatory: true as const },
    { id: "con_2", text: "constraint 2 text", kind: "constraint" as const, mandatory: true as const }
  ];
  return {
    claims: ["tests ran"],
    toolSituations: [
      {
        name: "task-result",
        exitCode: 0,
        wrote: true,
        escaped: false,
        artifactIds: ["art_1"],
        evidenceIds: ["evd_1"],
        hashes: []
      }
    ],
    writePaths: [],
    ownedPaths: [],
    requiredChecks: ["chk_0"],
    completedChecks: ["chk_0"],
    constraints,
    retainedConstraintIds: ["con_0", "con_1", "con_2"],
    progressed: true,
    stalledTurns: 0,
    independentEvidence: true
  };
}

const DIMENSION_IDS = [
  "evidence-consistency",
  "scope-safety",
  "check-coverage",
  "constraint-retention",
  "progress-vs-stall",
  "narrative-coherence"
] as const;

/* ============================================================
 * F6a: ambient-authority census (static, over the 14 slice files).
 * ============================================================ */
{
  const sliceFiles = [
    "analysis.ts",
    "combined-score.ts",
    "config.ts",
    "from-child.ts",
    "gates.ts",
    "human-score.ts",
    "index.ts",
    "isolation.ts",
    "prescore.ts",
    "roller.ts",
    "turn.ts",
    "types.ts"
  ].map((f) => `/workspace/src/tracking/${f}`);
  sliceFiles.push("/workspace/src/run/child-tracking.ts", "/workspace/src/run/gate-apply.ts");
  check("F6a slice manifest is exactly 14 files", sliceFiles.length === 14);
  const ambientTokens: Array<{ label: string; re: RegExp }> = [
    { label: "Date", re: /\bDate\b/ },
    { label: "Math.random", re: /Math\.random/ },
    { label: "process.*", re: /\bprocess\./ },
    { label: "performance", re: /\bperformance\b/ },
    { label: "hrtime", re: /hrtime/ },
    { label: "timers", re: /setTimeout|setInterval|queueMicrotask/ },
    { label: "crypto", re: /\bcrypto\b/ },
    { label: "Intl.*", re: /\bIntl\./ },
    { label: "toLocale*", re: /toLocale/ }
  ];
  let ambientHits = 0;
  let localeCompareSites: string[] = [];
  for (const file of sliceFiles) {
    const text = readFileSync(file, "utf8");
    for (const token of ambientTokens) {
      if (token.re.test(text)) {
        ambientHits += 1;
        console.error(`F6a ambient token ${token.label} found in ${file}`);
      }
    }
    const lines = text.split("\n");
    lines.forEach((line, i) => {
      if (line.includes("localeCompare")) localeCompareSites.push(`${file.split("/").pop()}:${i + 1}`);
    });
  }
  check("F6a zero ambient tokens outside collation", ambientHits === 0);
  check(
    "F6a exactly two localeCompare sites, both in types.ts",
    localeCompareSites.length === 2 && localeCompareSites.every((s) => s.startsWith("types.ts:")),
    localeCompareSites.join(",")
  );
  console.log(
    `F6a: ambient-authority census over the 14 slice files -- zero Date / Math.random / ` +
      `process.* / performance / timers / crypto / Intl.* / toLocale* tokens; the slice's ONLY ` +
      `ambient coupling is String.prototype.localeCompare at exactly two sites ` +
      `[${localeCompareSites.join(", ")}] (hashAssessment dimension-id sort, production-hot x3/gate; ` +
      `hashSummary operations sort, zero production flow). Regex /i simple case folding, toFixed, ` +
      `trim, JSON.stringify and default Array sort are locale-independent by spec.`
  );
}

/* ============================================================
 * F6b: closed-domain cross-locale order witness (27-locale battery).
 * ============================================================ */
{
  const battery = [
    "en", "en-US", "cs", "cs-CZ", "sk", "de", "sv", "da", "fi", "nb", "hu", "tr",
    "pl", "lt", "et", "is", "fr", "es", "ja", "zh", "ko", "ru", "el", "he", "ar", "th", "vi"
  ];
  const cu = (a: string, b: string): number => (a < b ? -1 : a > b ? 1 : 0);
  const divergentLocales = new Map<string, string[]>();
  for (const loc of battery) {
    const coll = new Intl.Collator(loc);
    const divergent: string[] = [];
    for (let i = 0; i < DIMENSION_IDS.length; i += 1) {
      for (let j = i + 1; j < DIMENSION_IDS.length; j += 1) {
        const a = DIMENSION_IDS[i]!;
        const b = DIMENSION_IDS[j]!;
        if (Math.sign(coll.compare(a, b)) !== Math.sign(cu(a, b))) divergent.push(`${a}<>${b}`);
      }
    }
    if (divergent.length > 0) divergentLocales.set(loc, divergent);
  }
  const expectDivergent = ["cs", "cs-CZ", "sk"];
  check(
    "F6b exactly cs/cs-CZ/sk diverge from codeunit order",
    [...divergentLocales.keys()].sort().join(",") === [...expectDivergent].sort().join(","),
    [...divergentLocales.keys()].join(",")
  );
  for (const [loc, pairs] of divergentLocales) {
    check(
      `F6b ${loc} diverges exactly on the two check-coverage pairs`,
      pairs.length === 2 && pairs.every((p) => p.includes("check-coverage")),
      pairs.join(",")
    );
  }
  console.log(
    `F6b: closed-domain cross-locale order witness -- 15 id pairs x 27-locale battery: exactly ` +
      `{cs, cs-CZ, sk} diverge from codeunit order, exactly on the 2 pairs involving ` +
      `"check-coverage" (Czech/Slovak collate the digraph "ch" as a letter after "h", so ` +
      `"check-coverage" sorts after BOTH "constraint-retention" and "evidence-consistency"); ` +
      `all 24 other locales agree with codeunit order on all 15 pairs. The closed 6-id union is ` +
      `NOT locale-stable. gate.codes and the hashSummary id-list sorts use the default Array ` +
      `comparator (codeunit) and are locale-independent by construction.`
  );
}

/* ============================================================
 * F6c/F6d + axis 15b/15d: cross-process LANG-dosed children
 * (witness + production-surface environment dose).
 * ============================================================ */
const childResults = new Map<
  string,
  {
    locale: string;
    sign: number;
    sortedIds: string[];
    assessmentJson: string;
    hash: string;
    validateForeign: string;
    ownApplied: boolean;
    ownValidateOk: boolean;
    hashNs: number;
    anchorUs: number;
  }
>();
{
  const mk = mkChild("PASSED");
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: String(RUN_ID) });
  if (!decision.apply) throw new Error("F6c fixture must apply");
  const parentAssessment = decision.assessment;
  const parentHash = hashAssessment(parentAssessment);
  const parentJson = JSON.stringify(parentAssessment);
  // parent-minted TRACKING_ASSESSMENT event (write-side artifact to re-validate abroad)
  const parentEvent = {
    id: "evt_00000100",
    schemaVersion: 1,
    occurredAt: NOW_ISO,
    runId: String(RUN_ID),
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: {
      assessment: parentAssessment,
      assessmentHash: parentHash,
      seq: nextTrackingSeq(events41)
    }
  };
  const dir = mkdtempSync(join(tmpdir(), "r26a-"));
  const argPath = join(dir, "arg.json");
  writeFileSync(
    argPath,
    JSON.stringify({
      child: mk.child,
      spec: mk.spec,
      contract: mk.contract,
      parentJson,
      parentHash,
      parentEvent
    })
  );
  const childPath = join(dir, "child.mts");
  writeFileSync(
    childPath,
    `
import { readFileSync } from "node:fs";
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { assessChildObservation } from "/workspace/src/tracking/from-child.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import { hashAssessment } from "/workspace/src/tracking/types.js";

const arg = JSON.parse(readFileSync(process.argv[2]!, "utf8"));
const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x";
const obs = observationFromChild(arg.child, arg.spec, arg.contract);
const decision = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: RUN_ID });
if (!decision.apply) throw new Error("child mint must apply");
const assessment = decision.assessment;
const assessmentJson = JSON.stringify(assessment);
const hash = hashAssessment(assessment);
let validateForeign = "ok";
try {
  validateEvent(arg.parentEvent as Event);
} catch (error) {
  validateForeign = error instanceof Error ? error.message : String(error);
}
// own-mint within-process chain over a 41-event table
const events: Event[] = [];
let idc = 0;
const nid = (): EventId => ("evt_" + String(idc++).padStart(8, "0")) as EventId;
events.push({ id: nid(), schemaVersion: 1, occurredAt: NOW_ISO, runId: RUN_ID, type: "RUN_STARTED", actor: "system", payload: { title: "bench" } } as unknown as Event);
events.push({ id: nid(), schemaVersion: 1, occurredAt: NOW_ISO, runId: RUN_ID, type: "RUN_ATTACHED", actor: "supervisor", payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO } } as unknown as Event);
for (let i = 0; i < 39; i += 1) {
  events.push({ id: nid(), schemaVersion: 1, occurredAt: NOW_ISO, runId: RUN_ID, type: "CHILD_MESSAGE", actor: "child", payload: { taskId: "tsk_" + (i % 5), content: "..." } } as unknown as Event);
}
let c = 100;
const out = applyChildThreeLine({
  events,
  child: arg.child,
  spec: arg.spec,
  contract: arg.contract,
  nowIso: NOW_ISO,
  generateEventId: () => ("evt_" + String(c++).padStart(8, "0")) as EventId
});
const ownApplied = out.result.applied;
let ownValidateOk = true;
try {
  for (const event of out.events.slice(events.length)) validateEvent(event);
} catch {
  ownValidateOk = false;
}
// production-surface bench under this environment
for (let w = 0; w < 20000; w += 1) void hashAssessment(assessment);
let t0 = performance.now();
for (let i = 0; i < 200000; i += 1) void hashAssessment(assessment);
const hashNs = ((performance.now() - t0) / 200000) * 1e6;
const benchAnchor = (): number => {
  let cc = 100;
  const r = applyChildThreeLine({
    events,
    child: arg.child,
    spec: arg.spec,
    contract: arg.contract,
    nowIso: NOW_ISO,
    generateEventId: () => ("evt_" + String(cc++).padStart(8, "0")) as EventId
  });
  return r.events.length;
};
for (let w = 0; w < 1500; w += 1) void benchAnchor();
t0 = performance.now();
for (let i = 0; i < 5000; i += 1) void benchAnchor();
const anchorUs = ((performance.now() - t0) / 5000) * 1e3;
const ids = ["evidence-consistency","scope-safety","check-coverage","constraint-retention","progress-vs-stall","narrative-coherence"];
console.log("R26A_CHILD_RESULT " + JSON.stringify({
  locale: new Intl.Collator().resolvedOptions().locale,
  sign: Math.sign("check-coverage".localeCompare("constraint-retention")),
  sortedIds: [...ids].sort((l, r) => l.localeCompare(r)),
  assessmentJson,
  hash,
  validateForeign,
  ownApplied,
  ownValidateOk,
  hashNs,
  anchorUs
}));
`
  );
  const doses = ["en_US.UTF-8", "cs_CZ.UTF-8", "C"];
  for (const dose of doses) {
    const res = spawnSync(
      process.execPath,
      ["/workspace/node_modules/tsx/dist/cli.mjs", childPath, argPath],
      {
        env: { ...process.env, LANG: dose, LC_ALL: dose },
        encoding: "utf8",
        timeout: 120000
      }
    );
    const line = (res.stdout ?? "").split("\n").find((l) => l.startsWith("R26A_CHILD_RESULT "));
    check(`F6c child under LANG=${dose} produced a result`, line !== undefined, res.stderr ?? "");
    if (line !== undefined) {
      childResults.set(dose, JSON.parse(line.slice("R26A_CHILD_RESULT ".length)));
    }
  }
  const en = childResults.get("en_US.UTF-8");
  const cs = childResults.get("cs_CZ.UTF-8");
  const posix = childResults.get("C");
  if (en && cs && posix) {
    check("F6c en child resolves en-US", en.locale === "en-US", en.locale);
    check("F6c cs child resolves cs-CZ", cs.locale === "cs-CZ", cs.locale);
    check("F6c C child aliases en-US (ICU fallback)", posix.locale === "en-US", posix.locale);
    check(
      "F6c assessment VALUE is locale-invariant (mint path has no ambient coupling)",
      en.assessmentJson === parentJson && cs.assessmentJson === parentJson && posix.assessmentJson === parentJson
    );
    check("F6c en child hash equals parent hash", en.hash === parentHash, `${en.hash} vs ${parentHash}`);
    check("F6c C child hash equals parent hash", posix.hash === parentHash);
    check(
      "F6c cs child hash DIFFERS for the same assessment (frozen-contract divergence)",
      cs.hash !== parentHash,
      cs.hash
    );
    check("F6c cs sign flips (+1)", cs.sign === 1 && en.sign === -1);
    check(
      "F6c cs sorted order moves check-coverage after constraint-retention and evidence-consistency",
      cs.sortedIds.indexOf("check-coverage") > cs.sortedIds.indexOf("constraint-retention") &&
        cs.sortedIds.indexOf("check-coverage") > cs.sortedIds.indexOf("evidence-consistency") &&
        en.sortedIds.indexOf("check-coverage") < en.sortedIds.indexOf("constraint-retention")
    );
    check("F6c en child re-validates the en-written event", en.validateForeign === "ok", en.validateForeign);
    check(
      "F6c cs child REJECTS the en-written event fail-closed",
      cs.validateForeign.includes("assessmentHash mismatch"),
      cs.validateForeign
    );
    check("F6d cs child within-process chain applies its own gate", cs.ownApplied && cs.ownValidateOk);
    check("F6d en child within-process chain applies its own gate", en.ownApplied && en.ownValidateOk);
    console.log(
      `F6c: cross-process environment witness -- assessment VALUE bitwise locale-invariant ` +
        `(3/3 children re-mint the identical JSON); hashAssessment: en_US=${en.hash} ` +
        `C(->en-US)=${posix.hash} parent=${parentHash} but cs_CZ=${cs.hash} (DIFFERENT hash for ` +
        `the same assessment; sorted id order under cs: [${cs.sortedIds.join(", ")}]); ` +
        `write-side validateEvent under cs_CZ rejects the en-written TRACKING_ASSESSMENT ` +
        `fail-closed ("${cs.validateForeign}"). Frozen hash/idempotency contract is ` +
        `environment-coupled: a ledger is portable only between same-collation environments. ` +
        `Fail-closed (availability/portability risk), never fail-open.`
    );
    console.log(
      `F6d: within-process consistency -- under every dosed LANG the full production chain ` +
        `applies its gate and re-validates its own events (en: applied=${en.ownApplied}, ` +
        `cs: applied=${cs.ownApplied}); single-environment deployments are unaffected.`
    );
    console.log(
      `axis 15b (environment dose on the production surface): hashAssessment ` +
        `en_US ${en.hashNs.toFixed(0)}ns | C ${posix.hashNs.toFixed(0)}ns | cs_CZ ${cs.hashNs.toFixed(0)}ns ` +
        `(cs/en x${(cs.hashNs / en.hashNs).toFixed(2)}); anchor cell PASSED x contract(3) ` +
        `en_US ${en.anchorUs.toFixed(1)}us | C ${posix.anchorUs.toFixed(1)}us | cs_CZ ${cs.anchorUs.toFixed(1)}us ` +
        `(cs/en x${(cs.anchorUs / en.anchorUs).toFixed(2)})`
    );
  }
}

/* ============================================================
 * F6e: production reachability census.
 * ============================================================ */
{
  const mk = mkChild("PASSED");
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: String(RUN_ID) });
  if (!decision.apply) throw new Error("F6e fixture must apply");
  const ids = decision.assessment.dimensions.map((d) => d.id);
  check(
    "F6e every production assessment carries all 6 dimension ids",
    ids.length === 6 && DIMENSION_IDS.every((id) => ids.includes(id))
  );
  const mkF = mkChild("FAILED");
  const obsF = observationFromChild(mkF.child, mkF.spec, mkF.contract);
  const decisionF = assessChildObservation({ observation: obsF, episodeId: "ep_bench", runId: String(RUN_ID) });
  check(
    "F6e FAILED-arm assessment also carries all 6 ids",
    decisionF.apply && decisionF.assessment.dimensions.length === 6
  );
  // hashSummary zero-flow witness: the from-child window has no `previous`,
  // so rollSummary computes no prevSummaryHash (site 516 unreached).
  const prescoreInput = prescoreInputFromObservation(obs);
  const turn = runTrackingTurn({
    window: {
      contextFacts: [`role ${obs.role}`, `task ${obs.taskId}`],
      toolSituations: prescoreInput.toolSituations,
      constraints: obs.constraints,
      unresolvedDecisions: [],
      confirmedDecisions: [],
      openMinors: []
    },
    prescoreInput,
    humanInput: {},
    gateFacts: { deterministicFail: false }
  });
  check("F6e hashSummary site zero-flow on the production path", turn.summary.prevSummaryHash === undefined);
  console.log(
    `F6e: production reachability census -- every production assessment (PASSED and FAILED arms) ` +
      `carries the full 6-id dimension vector, so the locale-sensitive pair sits in EVERY ` +
      `production hash (not an edge case); the hashSummary comparator site (types.ts:516) is ` +
      `zero-flow on the production path (from-child windows carry no previous => ` +
      `prevSummaryHash undefined; R20-A K=0 re-witnessed).`
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R25-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    anchorUs.push(one * 1e3);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition (R12-A..R25-A methodology) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)}) => ~${((scans * 1e6) / 41).toFixed(2)} ns/event at E=41`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const mkGT = mkChild("FAILED", { taskId: "tsk_replay_gt" });
    const obsGT = observationFromChild(mkGT.child, mkGT.spec, mkGT.contract);
    const decGT = assessChildObservation({
      observation: obsGT,
      episodeId: "ep_bench",
      runId: String(RUN_ID)
    });
    if (!decGT.apply) throw new Error("replay fixture must apply");
    const hashGT = hashAssessment(decGT.assessment);
    const seqGT = nextTrackingSeq(events41);
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: decGT.assessment,
      assessmentHash: hashGT,
      expectedSeq: seqGT,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: decGT.assessment,
        assessmentHash: hashGT,
        expectedSeq: seqGT,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }

  /* ============================================================
   * NEW axis 15a: collation-comparator tax inside hashAssessment
   * (bench-only codeunit replica; equality proven before pricing).
   * ============================================================ */
  {
    const hashAssessmentCodeunit = (a: TrackingAssessment): string => {
      const payload = {
        coverage: a.coverage,
        dimensions: [...a.dimensions]
          .map((dimension) => ({ id: dimension.id, verdict: dimension.verdict }))
          .sort((left, right) => (left.id < right.id ? -1 : left.id > right.id ? 1 : 0)),
        episodeId: a.episodeId,
        gate: {
          codes: [...a.gate.codes].sort(),
          kind: a.gate.kind
        },
        prescore: a.prescore,
        quality: a.quality,
        runId: a.runId,
        score: a.score,
        turnId: a.turnId
      };
      return hash32(JSON.stringify(payload));
    };
    // equality witness under the CURRENT (en-US) environment: production
    // assessment + 40 seeded dimension-order permutations, PASSED + FAILED arms
    const rng = mulberry32(0xf26a01);
    let equal = 0;
    const arms: TrackingAssessment[] = [assessment];
    const mkF = mkChild("FAILED");
    const obsF = observationFromChild(mkF.child, mkF.spec, mkF.contract);
    const decF = assessChildObservation({ observation: obsF, episodeId: "ep_bench", runId: String(RUN_ID) });
    if (decF.apply) arms.push(decF.assessment);
    for (let trial = 0; trial < 40; trial += 1) {
      const base = arms[trial % arms.length]!;
      const permuted = [...base.dimensions];
      for (let i = permuted.length - 1; i > 0; i -= 1) {
        const j = Math.floor(rng() * (i + 1));
        [permuted[i], permuted[j]] = [permuted[j]!, permuted[i]!];
      }
      const variant: TrackingAssessment = { ...base, dimensions: permuted };
      if (hashAssessment(variant) === hashAssessmentCodeunit(variant)) equal += 1;
    }
    check("15a codeunit replica hash-equal under en-US on 40 seeded permutations", equal === 40, `${equal}/40`);
    for (let w = 0; w < 20000; w += 1) void hashAssessmentCodeunit(assessment);
    const tCu = bench(() => void hashAssessmentCodeunit(assessment), 200000);
    const deltaNs = (tHash - tCu) * 1e6;
    console.log(
      `--- NEW axis 15a: collation tax inside hashAssessment (codeunit replica, bench-only) ---`
    );
    console.log(
      `  production hashAssessment ${(tHash * 1e6).toFixed(0)}ns vs codeunit replica ${(tCu * 1e6).toFixed(0)}ns ` +
        `=> comparator tax ${deltaNs.toFixed(1)}ns/hash x3 legs x5 gates => ${(deltaNs * 15).toFixed(0)}ns/run upper bound ` +
        `(replica hash-equal ONLY under codeunit-agreeing locales -- F6c shows the swap is NOT ` +
        `equivalent under cs/sk default environments: S3-B-3 / R6-A / R7-A rejection now ` +
        `strengthened from "rename" to "not hash-preserving cross-environment")`
    );
  }

  /* ============================================================
   * NEW axis 15c: ICU-engagement class dose at the zero-flow
   * hashSummary comparator site (fixed count n=64, fixed length 12).
   * ============================================================ */
  {
    const mkSummary = (names: string[]): SummaryHashInput => ({
      schemaVersion: 1,
      constraints: [],
      unresolvedQuestions: [],
      confirmedDecisions: [],
      operations: names.map((name) => ({
        name,
        wrote: false,
        escaped: false,
        artifactIds: [],
        evidenceIds: [],
        hashes: []
      })),
      prescore: 0.5,
      human: { kind: "unobserved" },
      score: 0.5,
      anomalyCodes: [],
      evidenceRefs: [],
      openMinors: [],
      omissions: [],
      failClosed: false
    });
    const N_OPS = 64;
    const LEN = 12;
    const rng = mulberry32(0xf26a02);
    const shuffle = <T,>(xs: T[]): T[] => {
      for (let i = xs.length - 1; i > 0; i -= 1) {
        const j = Math.floor(rng() * (i + 1));
        [xs[i], xs[j]] = [xs[j]!, xs[i]!];
      }
      return xs;
    };
    const ascii = shuffle(
      Array.from({ length: N_OPS }, (_, i) => `tool${String(i).padStart(LEN - 4, "0")}`)
    );
    const caseMix = shuffle(
      Array.from({ length: N_OPS }, (_, i) =>
        (i % 2 === 0 ? "Tool" : "tool") + String(i).padStart(LEN - 4, "0")
      )
    );
    const twoByte = shuffle(
      Array.from({ length: N_OPS }, (_, i) => "\u5de5\u5177" + String(i).padStart(LEN - 2, "0"))
    );
    const sAscii = mkSummary(ascii);
    const sCase = mkSummary(caseMix);
    const sTwo = mkSummary(twoByte);
    for (const s of [sAscii, sCase, sTwo]) for (let w = 0; w < 2000; w += 1) void hashSummary(s);
    const tAscii = bench(() => void hashSummary(sAscii), 20000);
    const tCase = bench(() => void hashSummary(sCase), 20000);
    const tTwo = bench(() => void hashSummary(sTwo), 20000);
    console.log(
      `--- NEW axis 15c: ICU-engagement class dose at the hashSummary comparator site (zero-flow) ---`
    );
    console.log(
      `  n=${N_OPS} ops, ${LEN}-char names: ASCII ${(tAscii * 1e6).toFixed(0)}ns | case-mix ${(tCase * 1e6).toFixed(0)}ns ` +
        `(x${(tCase / tAscii).toFixed(2)}) | two-byte ${(tTwo * 1e6).toFixed(0)}ns (x${(tTwo / tAscii).toFixed(2)}) ` +
        `-- the two-byte class bails the V8 one-byte localeCompare fast path into full ICU ` +
        `collation on the COMPARATOR (distinct from R25-A 14b which dosed the stringify ` +
        `encoding class at ~x1.01-1.04); site has zero production flow (F6e), public-API-face ` +
        `pricing only; comparator swap here is R6-A's original within-locale divergence rejection`
    );
  }
}

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
