MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 26 / R26-C：离线路由切片第二十六遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 26 / R26-C（Round 1–25 同区第二十六遍，叠在
  已落地的 S7-C 之上）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `f0bcdbb`（测量期起点为
  `b5b2763`——R25-J 合入 + Round 25 关轮 + R26-C 派单后的独占分支 tip；测量完成后、
  提交期间独占分支两次快进：先至 `8b0c3de`（R26-A / R26-B 两份空枚举合入 +
  R26-D/E 派单），再至 `f0bcdbb`（R26-D 空枚举合入 + R26-F 派单），每次均按派单
  规则 rebase 本分支并重验冻结主张。`git diff b5b2763..f0bcdbb --name-only`
  = 5 个文件（PROGRESS.md / round-26 PLAN.md / R26-A.md / R26-B.md / R26-D.md），
  全部位于 `docs/reports/sota-opt/`，**零 `src/` 触碰**。`git log -1 --
  src/routing/` 显示切片最后一次生产改动**仍为 `183df9b`「perf(routing): land
  S7-C support-only eta」**；每次 rebase 前后 `git diff 183df9b..HEAD --` 对 9 个
  切片文件均为**空**（stat 空输出、exit 0），本轮全部测量在该形态之上，未回退
  未重写；测量树与 `f0bcdbb` 的 `src/` 逐字节相同 ⇒ 全部读数在新基线下原样有效。
  新合入的 R26-A collation-locale face、R26-B privacy / provider-policy
  constraint-leg face、R26-D injection-seam default-arm face 三条禁移植轴已与
  本轮 PRICEX 轴做对象域切割（零交集，见 §4 增行）。R26-E / R26-F 在飞，
  未等待、未触碰其分支与文件。
- **分支:** `cursor/r26-c-offline-routing-twenty-sixth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第二十六组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第二十六遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R25-C 的全部无号关闭
探针与轴，含 R25-C 的 AGEX、R24-C 的 OCCX、R23-C 的 ORDX、R22-C 的
SEEDX、R21-C 的 YMIX、R20-C 的 SCALEX、R19-C 的 FITQ）后，本轮**从未
点名站点数为零**——R18-C「TAILG 是尾部段最后一个从未点名位置」的判定
经 R19-C..R25-C 与本轮连续八遍逐行核实成立：每一个执行站点都能指到
既有 ID、无号探针、零池备忘或池算术覆盖。本轮新增**一条完备性轴**并
按规执行**一次天花板重测**，全部以轴/地板测量（不铸号）收口——没有
任何候选道达到接受门槛：

- **PRICEX（货币成本坐标 / 价格几何剂量响应，新轴，非候选；C 区首次）**
  ——R25-C 把切片公开**输入行**（(三元组, y, occurredAtMs) 观测行）的
  全坐标定价完毕；但切片公开输入面还有**不在观测行上的一枚数值坐标**：
  货币成本。它恰好以三种形态落在三张契约脸上——归因面**按类型缺席**
  （`OfflineRow` 无成本字段），propensity 面**声明而永不读取**
  （`PropensityLogEntry.costUsd`），R1/影子面**活消费**
  （`R0Decision.candidates[].estimatedCostUsd` → `costOf` /
  `cheaperEstimate` / `tierIndex` + `selectedCost`）——而**历轮 C 区
  全部夹具都把价格向量钉死在无平局升序梯 `0.001*(idx+1)`（R1-C..
  R25-C 逐轮核实），从未移动、从未计为维度**。PRICEX 冻结体量 /
  层名集合 / 结局组成 / 发射次序 / 占据 / 种子 / 时间戳（全 fresh）/
  目录基数 M=10 / options / config / nowMs，只动价格向量的
  {数值量级, 平局结构, 排序方向}，分三面定价：
  **A 面（归因面，唯一能触 ±35 ms 带的池族）**——货币坐标**按类型
  缺席**：`OfflineRow` = {scenarioId, modelVersion, projectId, y,
  occurredAtMs} 无成本字段；`offline-logit.ts` / `offline-prob-add.ts` /
  `lin-alg.ts` / `posterior.ts` / `bandit.ts` / `offline-types.ts`
  六文件 cost/usd/price 标识符计数**全零**（rg -ic 本轮复核）——
  合同内剂量不存在，**按构造关闭**（比 AGEX 的逐位零响应更强：AGEX
  的时间字段在场但被投影丢弃，PRICEX 的货币字段在该面根本无处安放）。
  **A′ 面（propensity 面）**——`costUsd` 在公开输入类型上声明
  （`propensity.ts:11`）而**切片内零读点**（`computeOverlapDiagnostics` /
  `validateCounterfactualReport` / `isFabricatedPositiveSupport` 均不
  消费，rg + 逐行核实）：五个值剂量（anchor / 0 / 1e12 / 5e-324 /
  1/3）对 N=200 日志的诊断输出**逐位相同**（四运行全绿），墙钟
  0.5–0.6 µs/次调用（µs 级本身即派单拒绝类；2.8 µs 单点为首触抖动）。
  **B 面（R1/影子面，切片内唯三活价格消费站点）**——冻结 160 键
  （8 族 × 2 角色 × 10 模型版本）× 25 obs/键（20 PASS + 5 FAIL[model]，
  全 fresh，**八剂量共享同一个观测数组对象**——对象同一性，强于逐位）
  × 480 episode × M=10，每剂量的 `R0Decision` 由**生产 `routeR0` 导入**
  构建（合同内输入按构造成立；R0/policy 在切片外、构建在计时窗外），
  八个价格剂量 {asc(历史钉死值逐字), desc, alltie, pairtie, int,
  micro(1e-12 级), spread(10^-5..10^4 十一个数量级), subn(5e-324
  次正规)}：**结构见证四运行逐位一致**——后验计数**全部剂量恒等**
  （powCalls=4,000、quantileCalls=estimateComputes=160、
  cacheHits=4,640、ribCalls=betacfCalls=12,800=80×160、
  **betacfIters=76,800 八剂量恒等**——AGEX 里唯一随剂量移动的计数
  在价格轴下纹丝不动：价格不是后验输入，双轴正交的双向见证）；
  价格计数恒等（comparisons=4,320=9×480、costOfCalls=8,640=18×480、
  costFindHops=30,240=63×480——**find 跳数由位置决定而非价格决定**，
  八剂量恒等断言）；**唯一剂量响应计数是平局路径**：tieBranches /
  tierIndexCalls / tierIndexHops = 0/0/0（六个无平局剂量）→
  480/960/480（pairtie）→ **4,320/8,640/21,600（alltie 极端）**；
  **恒等折叠定理八剂量断言**（rightWins=0：R0 排序契约下 reduce
  累积元恒为 candidates[0]，`selection == r0.selection` 逐 episode
  ×480 全过——desc 剂量赢家如约翻到 model-9，语义随剂量按契约移动
  而墙钟不动）；**每键后验估计 160 键 × 7 字段跨剂量逐位等于锚剂量**
  + **每 episode 估计多重集 480 全等锚剂量**（价格只能置换估计数组
  的呈现序，不能触及任何数值）；复刻道 vs 生产道每剂量逐位全绿后才
  采信读数。定价（四运行）：整工作载 prod 4.75–6.55 ms（最坏单剂量
  读数 6.55 [run B alltie] = **×5.3 低于 35 ms 带**；逐运行最坏剂量
  分别为 subn / subn / alltie / int——除平局池外无剂量签名）；价格
  可归因差 = 最坏−最好 0.53 / 0.69 / 1.70 / 1.09 ms = **×20.5–×65.6
  低于带**；唯一响应池 price-reduce 0.191–0.313 ms（最坏读数 0.313
  [alltie] = **×112 低于带**——21,600 次 tierIndex 跳仅值 ≈0.10 ms）。
  **真实入口面（R7-I 教训，整命令面）**：生产 `buildR1ShadowReport`
  导入逐剂量（routeR0 + 切片 + gatedComparisonReport），整入口墙钟
  5.37–7.41 ms（最坏 7.41 [run C desc] = **×4.7 低于带**）；整报告
  自确定性 + **配对恒等定理**（480 对全部 r0==r1、零 fallback、零
  invoked）每剂量断言全绿；**desc 恒为整入口最坏（4/4 运行）而工作
  载道无此签名** ⇒ 签名住在切片外 `routeR0` 的升序排序吃降序输入
  （切片外辖区），且差值 0.81–1.70 ms 本身 ×20+ 亚带。**货币坐标在
  三张脸上全部无越线点（归因面按构造、propensity 面逐位见证、R1 面
  直测封顶）**。候选道形族三重拒绝（见 §2）；价格几何本身是**调用方
  数据不是代码自由度**。轴一次性收口，非候选。
- **APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠）**
  ——本轮四次读数 APC 池 **47.3 / 46.2 / 46.0 / 46.5 ms**，其中两次
  正典读数（46.0 / 46.2）落在 R24-C 重锚定带 46.4–53.3 **下沿以下
  0.2–0.4 ms** ⇒ 按 R14-C 处置规则**随读重锚为本轮带 46.0–47.3**
  （与 R24-C 带在 [46.4, 47.3] 交叠——本 VM 类 APC 侧略快，方向与
  r7c 锚点 659.9 vs R25-C 666.3 一致，非切片改动：切片零 diff）；
  天花板重测每次运行执行：APCX 无计时器独立道 **55.7 / 54.7 / 60.2 /
  59.4 ms**（与 R25-C 58.4–62.8 及 R24-C 55.7–57.5 两带交叠），
  FLOORX 最廉价逐位合法形态地板 **34.4 / 34.7 / 37.3 / 39.9 ms** ⇒
  **组合天花板 = 21.3 / 20.0 / 22.9 / 19.5 ms < 35**（与 R25-C 带
  20.8–25.6 在 [20.8, 22.9] 交叠；19.5 在 R16-C..R25-C 历史读数域
  [12.1, 25.6] 之内），组合关闭维持。**sink=7.309 与 floorSink=7.309
  八读数全同，且与 R14-C/R16-C..R25-C 逐位同值**（派单强制的跨轮
  确定性旁证成立）。

除上述轴与天花板重测外，第二十六遍逐行重读**零新发现**：其余每一个
执行站点都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 /
结构交换负优化）+ R18-C 补充的 once-per-fault 拒绝类。三大池地板与
组合天花板复核见 §3：本 VM 上生产中位 **663.0–669.9 ms/报告（四次
运行 663.7 / 664.3 / 663.0 / 669.9；三次正典 663.0–669.9）**——与
R25-C 带 663.2–681.5（正典 663.2–672.8）在 [663.2, 669.9] 宽交叠
（663.0 低于 R25-C 正典下沿 0.2 ms = 亚噪声，方向与 APC 池微快一致；
同 VM 独立旁证：r7c 回归资产锚点 **production 659.9 ms = 1.11× vs
冻结 S6-C ctl 734.4**、r1c 锚点 **production 665.1 ms = 2.31× vs
参考 1536.9**，均在带邻域互证）；非切片回归或加速（切片对 `183df9b`
零 diff）。小形状档整报告中位 **32.2–33.9 ms**，整条报告低于噪声带
（R7-I 矩阵义务履行）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11，`writeAttributionPair` 两路调用本轮 rg 复核在位），live 面零
文件改动（`src/supervisor/` 对 9 文件导入为零，本轮 rg 复核）。仓库
变更仅本报告一个文件；无生产赢家故未提交 `/tmp/r26c/r26c-anchor.mts`
（败者/锚点仿真全文进附录，遵守 R7-C..R25-C 先例）。

## 0. 范围与约束遵守

- 读序（强制）已履行：README → EXCLUSIONS.md（完整表，含全部
  S\*-C-\* 行与 S12-C-1、S13-B-1、X1-3/X3-4/X3-5/X4-2 posterior/
  propensity/bandit 行）→ PROGRESS.md → round-26/PLAN.md（C 段 +
  全部 plan-ban 轴）→ R25-C.md（空枚举确认，无 S25-C-\*；AGEX 轴
  收口与「公开输入行全坐标定价完毕」裁决域勘定——该裁决域为观测行
  坐标，货币坐标不在其上，见 §4 PRICEX vs AGEX 行）→ R24-C.md
  （OCCX）/ R23-C.md（ORDX）/ R22-C.md（SEEDX）轴定义核对（经
  EXCLUSIONS 完整表逐条比对，证非重铸）→ 运行中快进合入的
  R26-A / R26-B 新轴段与更新版 PLAN 禁项（collation-locale face /
  privacy-provider-policy constraint-leg face——对象域均与 PRICEX
  无交，见 §4 增行，零移植）→ 9 个切片源文件全量重读 +
  `offline-types.ts` / `outcomes.ts` / `r0.ts` / `policy.ts` /
  `capability-registry.ts` 契约面跳读（货币字段的入口与消费图勘定，
  见 §2）。
- 基线漂移检查（含运行中两次快进）：测量期基线 `b5b2763` 相对 R25-C
  基线 `cb07c72` 为 docs(sota-opt) 提交（R25-F..R25-J 五份空枚举合入
  + Round 25 关轮 + R26-A/B/C 派单）；测量完成后独占分支先快进至
  `8b0c3de`（R26-A / R26-B 空枚举合入 + R26-D/E 派单），提交后又快进
  至 `f0bcdbb`（R26-D 空枚举合入 + R26-F 派单），每次均按派单规则
  rebase 本分支——`git diff b5b2763..f0bcdbb --name-only` = 5 个
  文件全部 `docs/reports/sota-opt/`，零 `src/` 触碰；每次 rebase
  前后 `git log -1 -- src/routing/` 均为 `183df9b`（S7-C 落地本身），
  `git diff 183df9b..HEAD --` 对 9 文件均为空（stat 空输出、exit 0），
  落地形态原样在位 ⇒ 测量树与新基线 `src/` 逐字节相同，全部读数原样
  有效。三份新合入报告的新轴段已核对（R26-A collation-locale face =
  进程 locale 环境 × `localeCompare` 站点，A 区字符串比较面；R26-B
  privacy / provider-policy constraint-leg face = `privacyRequired`
  × `privacyClass` × `providerPolicy` 资格腿，B 区 live 资格面；
  R26-D injection-seam default-arm face = `RegistryOptions` 生产默认
  时钟/id 注入臂 vs 确定性注入臂，D 区注入缝面——对象域均与 PRICEX
  的「数值价格几何」无交，见 §4 增行），零移植零补铸。既有回归资产
  七连全绿且检查数逐一匹配锚定值（§5）。
- 调用图确认（派单强制项，本轮 rg 复核）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 的 `writeAttributionPair`
  消费（双归因两路在位）；`src/supervisor/` 对 9 个切片文件的导入为
  **零**——live = R0 等价维持，R1 未接线，`src/supervisor/model-router.ts`
  零字节未触。
- 货币坐标消费图（本轮新勘定，PRICEX 轴的对象域）：切片内 cost/usd/
  price 标识符的**全部**出现点 = `r1.ts` 的 `costOf`（`estimatedCostUsd`
  线性 find）+ `cheaperEstimate`（成本比较 + 平局分支）+ `tierIndex`
  （平局仲裁线性 findIndex），`r1-shadow-report.ts` 的 `selectedCost`
  （每对一次查找），`propensity.ts:11` 的 `costUsd`（声明而零读点），
  `shadow.ts` 的 `comparisonBudgetUsd` / `comparisonCostUsd`
  （**config 面货币**——预算/门控配置，不属输入数据轴域，本轮明示
  不剂量化，与 R24-I adaptation-state coordinate 同面切割，诚实划界
  不越权）；`offline-logit.ts` / `offline-prob-add.ts` / `lin-alg.ts` /
  `posterior.ts` / `bandit.ts` / `offline-types.ts` **零出现**（rg -ic
  全零）——货币坐标恰好三张脸：归因面类型死、propensity 面声明死、
  R1/影子面活。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S25-\*（含 S12-C-1，重开条件：无）
  未重提、未立道、未重测；R13-C..R25-C 二十五个无名关闭（PIVCSE/
  FINTAIL/REFZ/TSORT/DEG/SWORD/XF64/IRLSDP/CMPH/STORD/RIDGE/ICOL/
  SFILL/NSQRT/PMV/OSTZ/TAILG/REJX/FITQ/SCALEX/YMIX/SEEDX/ORDX/OCCX/
  **AGEX**）未重探未补铸未换址重提——**SCALEX 未重测、YMIX 未重测、
  SEEDX 未重测、ORDX 未重测、OCCX 未重测、AGEX 未重测**（PRICEX
  全部剂量点都在冻结体量 4,000 obs / 480 episode、冻结层名集合、
  冻结结局组成（每键 20/5 冻结）、冻结发射次序、冻结占据轮廓、
  无 RNG、冻结时间戳（全 fresh = nowMs，八剂量共享**同一个观测数组
  对象**——对象同一性断言强于逐位）、冻结 options 与 config 上，
  未触任何体量剂量、组成剂量、种子剂量、次序剂量、占据剂量或时间
  剂量——PRICEX 的每个剂量动的是**不在观测行上的价格向量**，观测
  多重集八剂量恒为同一对象；七轴两两正交，§4）；本轮测量器**不含**
  FITQ 逐抽取计时器（FITQ 重开条件——bootstrap 抽取数或拟合规模
  位移 ≥1 个数量级——未被触发：options 全程默认值）；ITERX/COLDX/
  AAFLR/GCAX 四轴未补铸（B 面读数取自全剂量预热后的稳态窗口，首触
  效应不进入读数；IRLS 迭代锚定值 8,966 在 §1 原样复核）；**IDG 未
  铸未移植**（PRICEX 各剂量断言模型 id 集合冻结——id-set-frozen
  逐剂量断言，零字符串移动）；R25-A value-domain / string
  content-class census、R25-B task-face optional-field presence
  topology、R25-D identity-coordinate face、R25-F plan-header
  identity-geometry face、R25-G injection command-kind face、R25-H
  consumer-stage string-representation census、R25-I
  process-environment coordinate、R24-I adaptation-state coordinate、
  R24-H operand-residency census、M=1 catalog collapse、
  approval-authority face、serialization-boundary provenance face、
  **COTARG** / JB / RUNLIM / CALLB / CKMIX / SHAPEK / KFAN /
  TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / VALCLASS / CG 均未移植
  （派单点名禁项，宿主与机制均在他区；PRICEX 与其中最近邻者的对象域
  切割见 §4）；**R26-A collation-locale face / R26-B
  privacy-provider-policy constraint-leg face / R26-D injection-seam
  default-arm face（运行中快进新增禁项）未移植**（§4 增行）；
  RID/CNT 归约重排反例原样引用；嵌套序 × 布局
  组合空间（S4-C-2 + S5-C-1..4 + S12-C-1）未重开；CMB 组合未重堆叠
  （本轮 APCX/FLOORX 重测系派单强制天花板复测；PRICEX 各剂量点只测
  落地形态一条道，零变体决斗）；PLSEG/PMV/TAILG 备忘未重提。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动；X2-3 数值路径未触；单线程逐位审计基底不动；fail-open 族
  维持关闭；无 fsync / 完整性再哈希移除（本切片不涉盘，天然成立）。
- 环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0——回到
  R1-C..R25-C 的处理；VM 默认 22.14.0 未使用）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`（`pnpm gate` 全绿，见 §5）。

## 1. 重剖析：S7-C 后成本模型（本 VM 实测，未拷贝历轮数字）

测量器为**仪表化逐字复刻管线**（与 183df9b 生产 irls/solve/on-prob/
APC 逐语句相同，仅在调用边界加 performance.now() 与计数器，浮点操作/
操作数/顺序零改动），其忠实性先经 **53 例全形状电池 ×{仪表道, 纯净
落地道} vs 生产 `fitLogitAdditive` 导入逐位全绿**（Object.is 含符号零）
再采信读数；posterior/R1 面沿用 R25-C 引入的**仪表化逐字复刻道**
（outcomes/posterior/r1 三文件逐语句复刻 + BSTAT 计数器，本轮在 r1
价格消费站点增加 PSTAT 计数器——comparisons / costOfCalls /
costFindHops / tieBranches / tierIndexCalls / tierIndexHops /
rightWins / reduceMs，浮点路径零改动），其忠实性在 **PRICEX 的每一个
价格剂量点**对生产 `prepareR1Observations` / `estimateForKey` /
`routeR1` 导入逐位全绿（480 episode × 10 估计 × 全字段 + 160 键 ×
7 字段，每剂量）后才采信读数；生产中位数、B 面工作载读数与真实入口
读数均由**未仪表化生产导入**直接测得。perf 夹具与 R10-C..R25-C 完全
一致（fixtureRng(0xbeef)，4 场景 × 6 模型 × 8 项目，rows=400,
passRate=0.6，bootstrap=200, seed=20260818 → p=60，201 拟合，
8,966 次 IRLS 迭代）。

本轮共四次全量运行：run 0 为**预演运行**（脚本与正典运行**逐字节
相同**——预演后零改动，四次运行同一脚本），run A / run B / run C 为
正典运行。四次运行全部计入处置依据，不作剔除；每次运行 6,277 项
逐位检查全绿。

生产整报告中位（每次运行内 7 测量取中位）：**663.7 / 664.3 /
663.0 / 669.9 ms**——四读数带 663.0–669.9 与 R25-C 带 663.2–681.5
（正典 663.2–672.8）在 [663.2, 669.9] 宽交叠，按派单口径随读重锚为
本轮带 **663.0–669.9（正典同）**；663.0 低于 R25-C 正典下沿 0.2 ms
= 亚噪声（同运行 7-rep 散布 660.9–678.4 同量级），方向与本 VM 类
APC 侧微快一致（同 VM 独立旁证：r7c 回归资产锚点 **production
659.9 ms = 1.11× vs 冻结 S6-C ctl 734.4**、r1c 锚点 **production
665.1 ms = 2.31× vs 参考 1536.9**，互证）；判为 VM 微架构漂移带内
往返，非切片回归或加速（切片对 `183df9b` 零 diff）。

仪表化池表（perf 形状，每次运行内 3 次仪表化取中位；三次正典运行
读数并列，预演读数注记）：

| 池 | run A | run B | run C | 占比 | 站点计数（精确） |
| --- | --- | --- | --- | --- | --- |
| 总计 | 666.5 | 665.1 | 666.5 | 100% | fits=201 |
| `solveSymmetric` | **453.2** | **451.6** | **451.4** | **67.7–68.0%** | 8,966 次调用 |
| `irls` 自身（扣 solve） | **151.4** | **150.8** | **152.3** | **22.7–22.8%** | 8,966 迭代 / 3,586,400 行访问 / 1,249,652 sigmoid |
| APC 全站点 | 46.2 | 46.0 | 46.5 | 6.9–7.0% | 277,606 活跃对 / 12,259 次调用 |
| `onProbabilitiesFor` | 6.8 | 6.6 | 6.8 | 1.0% | 80,400 sigmoid+dot |
| bootstrap 采样 | 1.7 | 2.1 | 1.7 | 0.3% | —（SAMP 池 R12-C 已封顶） |
| 排序/分位尾 | 1.5 | 1.5 | 1.6 | 0.2% | 12,200 draws-map ops |
| 基行投影 | ~0.003 | ~0.003 | ~0.003 | ~0% | 每报告 1 次（R12-C BASE 已封顶） |
| 编排余量（build/supports/keys） | 5.6 | 6.5 | 6.2 | 0.8–1.0% | —（差值余项注记同 R24-C） |
| 抽取结局计数 | 200/0/0 | 200/0/0 | 200/0/0 | — | successful/collapsed/singular，四运行逐位一致 |

预演读数（同口径）：run 0 总计 665.8 / solve 451.5 / irls-other
151.9 / APC 47.3——预演与正典四池零级差，无 R24-C 预演型全池上移
事件。

除 solve / irls / APC 外每池整池清零也低于 ±35 ms 带（R9-C..R25-C
同结论，逐池复测未漂移）；solve 与 irls 为落地线唯二宿主。全部站点
计数与历轮锚定值逐一相同（8,966 / 3,586,400 / 1,249,652 / 277,606 /
12,259 / 12,200 / 80,400 / 201——跨轮确定性旁证）。**APC 池本 VM
四次读数 46.0–47.3，其中两次正典读数（46.0 / 46.2）落在 R24-C 重
锚定带 46.4–53.3 下沿以下 0.2–0.4 ms** ⇒ 按 R14-C 处置规则**随读
重锚为本轮带 46.0–47.3**（与 R24-C 带在 [46.4, 47.3] 交叠——本 VM
类 APC 侧微快，与 r7c ctl 734.4 vs R25-C 745.1 同向，非切片改动）；
派单强制的天花板重测仍在每次运行执行（§3，天花板 19.5–22.9 < 35，
关闭维持且 sink/floorSink 八读数逐位同值）。小形状档（3 模型 ×
3 项目 / 120 行，bootstrap=200）整报告中位 **33.9 / 32.9 / 33.1 ms**
（正典；预演 32.2）——小配置态整条报告低于噪声带，调用矩阵小态
一侧整体收口（R7-I 矩阵义务履行）。

生产自确定性复核：3 seed × 各 2 次运行全字段 Object.is 全绿（含于
§5 的 6,277 项检查 × 4 次运行）。

## 2. 第二十六组枚举（诚实结果：空；零新站点 + 一条新轴 + 一次按规天花板重测）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过，且本轮从未点名站点数为零**（连续第八遍；探针数字为
本 VM 实测，出处为附录测量彩排三次正典运行 + 一次预演）：

| 站点/轴 | 关闭方式 | 证据 |
| --- | --- | --- |
| 从未点名站点普查：**零**（连续第八遍） | 逐行三问全覆盖 | 26 遍扫描把 9 文件每一个执行站点指到既有覆盖物：offline-logit 的编排头（DEG/BASE）、irls 全站点（S2-C/S3-C/S6-C/S7-C 落地体 + S10-C-1/S9-C-3/S9-C-4/S6-C-5/6/S5-C-7/S4-C-5/6/MUCSE/SWORD/STORD/RIDGE/ICOL/IRLSDP + 收敛尾 S2-C-3/S8-C-3/NSQRT + ITERX/FITQ）、solve 全站点（COPY/S4-C-3/XF64 + PIVCSE/S5-C-6 + PIVSWAP + FZERO + S4-C/S5-C 落地体 + S12-C-1 + S6-C-7 + SFILL/FINTAIL + S8-C-4）、APC（S1-C-1/S2-C-2/S2-C-4/S4-C-4/S8-C-2/S10-C-2 + FLOORX 天花板）、on-prob（S7-C-4/S8-C-1）、采样环（SAMP/S3-C-3/S2-C-5）、抽取效应环（S7-C-1/DMAP）、尾部效应环（TSORT/CMPH/REFZ/TAILG）、诊断尾（R15-C 零池注记）、posterior（X1-3/X3-4/S1-C-7/OSTZ/S1-C-4 + 已落地 memo + R25-C AGEX decay-scan/lcb-quantile 直测封顶）、prob-add（S1-C-6/PMV/PLSEG）、propensity（S1-C-8/X3-5 遍数常数域——**其中 `costUsd` 字段本轮 PRICEX A′ 面证明声明而零读点，逐位见证**）、r1（X1-4/S1-C-10/S1-C-2/3——**其中三个价格消费站点 costOf/cheaperEstimate/tierIndex 的池由本轮 PRICEX B 面 price-reduce 0.191–0.313 ms 直测封顶，含 21,600 跳的 alltie 极端**）、bandit/shadow（S1-C-9/X4-2）、r1-shadow-report（X1-5 + REJX 终点定价 + **selectedCost 由 PRICEX 真实入口面封顶**）——R18-C「TAILG 是尾部段最后一个从未点名位置」的判定经 R19-C..R25-C 与本遍连续八轮核实成立；观测行六维经 SCALEX/YMIX/SEEDX/ORDX/OCCX/AGEX 关死后本轮六轴零位移，**货币维度经本轮 PRICEX 关死** |
| PRICEX：货币成本坐标 / 价格几何剂量响应（新轴，非候选；C 区首次，补齐切片公开输入面在冻结契约下**不在观测行上**的数值坐标——`R0Decision.candidates[].estimatedCostUsd` 与 `PropensityLogEntry.costUsd`；历轮夹具钉死无平局升序梯 `0.001*(idx+1)` 从未移动） | A 面按构造关闭（类型级缺席）+ A′ 面逐位零响应见证 + B 面后验计数八剂量恒等（含 betacfIters——AGEX 的唯一响应计数在价格轴下不动）+ 恒等折叠定理逐剂量断言 + 唯一响应池 price-reduce 直测 ×112 亚带 + 整工作载亚带 ×5.3–6.3 + 真实入口亚带 ×4.7–5.1 + 每剂量逐位保真 + 候选道形族三重拒绝 + desc 整入口签名诚实记名并归址切片外 | **A 面**（归因面）：`OfflineRow` = {scenarioId, modelVersion, projectId, y, occurredAtMs}——无成本字段；六文件 cost/usd/price 标识符 rg -ic 全零 ⇒ 合同内剂量**不存在**，按构造关闭（强于 AGEX 型逐位零响应：字段无处安放）。**A′ 面**（propensity 面）：`costUsd` 声明于公开输入类型（`propensity.ts:11`）而切片内零读点（三消费函数 rg + 逐行核实）；五值剂量（anchor=0.001i / 0 / 1e12 / 5e-324 / 1/3）× N=200 日志 → `computeOverlapDiagnostics` 输出逐位相同（四运行全绿）；墙钟 0.5–0.6 µs/call（µs 级 = 派单拒绝类，2.8 µs 单点为首触抖动）。**B 面**（八剂量 asc/desc/alltie/pairtie/int/micro/spread/subn @ 冻结 160 键 × 25 obs/键全 fresh × 480 episode × M=10 × nowMs=1.756e12 × DEFAULT config，观测数组八剂量同一对象，R0 由生产 routeR0 导入构建、契约六断言/剂量：M-frozen / id-set-frozen / all-eligible / cost-ascending / tier-order==candidates-order / cost-values-exact-bitwise）：**后验计数八剂量恒等断言**——powCalls=4,000、quantileCalls=estimateComputes=160、cacheHits=4,640、ribCalls=betacfCalls=12,800、**betacfIters=76,800 恒等**（价格不是后验输入的计数级证明；与 AGEX 的剂量响应计数恰为互补，双轴正交双向见证）；**价格计数恒等断言**——comparisons=4,320、costOfCalls=8,640、costFindHops=30,240（=63×480，find 跳数位置决定：tier 序 == candidates 序 ⇒ 每 episode 跳数 9×1+(2+..+10)=63 与价格无关，含 desc）；**唯一剂量响应计数 = 平局路径**：tieBranches/tierIndexCalls/tierIndexHops 0/0/0（asc/desc/int/micro/spread/subn）→ 480/960/480（pairtie）→ 4,320/8,640/21,600（alltie）；**恒等折叠定理**（rightWinsCost=rightWinsTie=0 八剂量断言：R0 升序契约下 reduce 累积元恒为 candidates[0]；selection==r0.selection 逐 episode ×480×8 剂量全过——desc 赢家如约 model-9、其余 model-0，全部 selected-cheapest=480/480，wellSampledKeys=160/160，语义随剂量按契约移动）；**每键估计 160×7 字段与每 episode 估计多重集 ×480 跨剂量逐位等于锚剂量**（价格只能置换估计数组呈现序）；复刻道 vs 生产道逐位 + 生产自确定性 + 计数三 rep 确定性每剂量断言。定价（四运行）：整工作载 4.75–6.55 ms（最坏读数 6.55 [run B alltie] = ×5.3 亚带；逐运行最坏剂量 subn/subn/alltie/int 无稳定签名）；价格可归因差 0.53/0.69/1.70/1.09 ms = ×20.5–×65.6 亚带；**唯一响应池 price-reduce 0.191–0.313 ms**（最坏 alltie 0.313 = ×112 亚带——21,600 次 tierIndex 跳全价 ≈0.10 ms；无平局几何 ≈0.20 ms 为纯比较+find 底价）。**真实入口面**：生产 `buildR1ShadowReport` 导入（routeR0 + 切片 + gatedComparisonReport 整命令面）逐剂量整入口 5.37–7.41 ms（最坏 desc 7.41 [run C] = ×4.7 亚带）；整报告自确定性 + 配对恒等定理（480 对 r0==r1、零 fallback、零 invoked）每剂量断言；**desc 恒为整入口最坏（4/4 运行，6.80–7.41）而 B 面工作载道无 desc 签名（desc 读数 4.94–6.17 居中）** ⇒ 签名住在切片外 `routeR0` 升序排序吃降序输入的排序功（切片外辖区），差值 0.81–1.70 ms 自身 ×20+ 亚带。**收口算术：价格几何对切片的全部触达 = 一次 reduce 的比较/查找/平局仲裁（price-reduce 池）+ selectedCost 每对一次查找；平局极端 alltie 把该池从 ≈0.20 抬到 ≈0.31 ms = 每报告位移 ≈0.11 ms；价格单独驱动 35 ms 位移需 ≈35/0.11 ≈ ×318 的平局仲裁流量 ≈ 15.3 万 episode/报告 = 合成工作载（480，已 ≥ 仓内一切调用方一个数量级以上）的 ×318——货币维度无越线点（与 SCALEX..AGEX 六轴同判且裕量最大）**。候选道形族三重拒绝：「costOf 线性 find 换 Map/预建成本数组」——宿主池 price-reduce ≤0.313 ms = 亚带 ×112（S9-C-4 型深亚带拒绝），且 costFindHops=63/episode 位置恒等证明 find 功与价格无关、无胖尾可救；「tierIndex 平局仲裁预建 rank 表」——同宿主池亚带，且生产几何（历史钉死 asc 梯）**无平局** ⇒ tie 路径流量恒零，收益仅存在于合成 alltie 剂量 = 为不存在的调用方数据加基础设施；「cheaperEstimate 比较器改写/短路重排」——改变平局语义或比较次序 = 行为/逐位违例（X1-4 域）。价格几何本身是**调用方数据不是代码自由度**——切片内不存在以价格为对象的合法变换。重开仅当 R0Decision/PropensityLogEntry 货币字段契约本身变更（公开签名面，不预期）。轴一次性收口，无候选道 |
| APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠；池 46.0–47.3 两读数低于 R24-C 带下沿 ⇒ 按 R14-C 规则随读重锚为 46.0–47.3，交叠 [46.4, 47.3]） | 天花板仍亚带 + sink 逐位同值 | APCX **55.7 / 54.7 / 60.2 / 59.4**（与 R25-C 58.4–62.8 及 R24-C 55.7–57.5 两带交叠）；FLOORX **34.4 / 34.7 / 37.3 / 39.9** ⇒ 天花板 **21.3 / 20.0 / 22.9 / 19.5 < 35**（与 R25-C 带 20.8–25.6 在 [20.8, 22.9] 交叠；全部读数在 R16-C..R25-C 历史域 [12.1, 25.6] 内），距带最近读数仍差 12.1 ms；**sink=7.309 与 floorSink=7.309 八读数全同，且与 R14-C/R16-C..R25-C 逐位同值**——派单强制的 APC 关闭维持条件（sink 逐位匹配）成立；S8-C-2/S10-C-2/S2-C-4/S4-C-4 与 CMB 禁令全部维持原状 |

以上无一构成候选：从未点名普查结果为零（无新站点可立道）；PRICEX
是货币坐标定价轴不是变换（各剂量点只测落地形态一条道，零变体决斗
——其全部潜在候选族被三重拒绝直接排除：深亚带宿主池 + 生产几何
零平局流量 + 逐位/行为违例）；APCX/FLOORX 是按规天花板测量不是
变换。**故第二十六组候选集为空，不铸 S26-C-1 及后续编号**，遵循
R11-C..R25-C 无号关闭先例。

**枚举收口：** 除上表外，第二十六遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖——本轮
PRICEX 把 r1 价格站点与 propensity costUsd 字段从「池算术覆盖」
升级为「输入维度直测封顶」）。R25-C 的「公开输入行全坐标定价完毕」
裁决域为 (三元组, y, occurredAtMs) 观测行；本轮把裁决域扩到**切片
公开输入面的全部数值坐标**：观测行六维（SCALEX/YMIX/SEEDX/ORDX/
OCCX/AGEX）+ 决策面货币维（**本轮 PRICEX**）——至此**切片公开输入
面上不存在从未定价的数值坐标**。仍不剂量化、不铸名的坐标（诚实
划界）：options 坐标（maxIter/bootstrap——§3(a) 契约面）、config
坐标（halfLife/minSamples/priors/lcbKind + `shadow.ts` 的
comparisonBudgetUsd/comparisonCostUsd **config 面货币**——与 R24-I
adaptation-state coordinate 同面，派单禁移植域）、字符串身份坐标
（IDG 冻结）——三者均不属输入数据轴域。本轮核实换名再提交拒列：
零——没有任何已关 ID 的换址/换名形态在本轮出现。

## 3. 三大池地板与组合出路复核

- **solve 池 451.4–453.2 ms（本 VM 三正典读数；预演 451.5，与
  R25-C 带 452.7–454.5 及 R24-C 带 450.8–460.9 交叠）**：消元主体
  ≈ 6.45 亿次冻结顺序浮点乘减/报告。R6-C §2.5 七类分解 + S10-C-3 +
  S12-C-1（嵌套序，稳定 ~3.6× 负优化，重开条件：无）+ R11-C COPY +
  R12-C FZERO/PIVSWAP + S5-C-6 + R13-C PIVCSE/FINTAIL + R14-C XF64 +
  R16-C SFILL 之后，本轮**零新位置**。ITERX 坐实池量线性依赖 8,966
  次冻结迭代（本轮锚定值逐位复核），FITQ 坐实拟合粒度无胖尾，
  SCALEX 坐实其 K 轴 α=2.75，YMIX/SEEDX/ORDX 坐实其每迭代单价组成/
  种子/次序不变，OCCX 坐实其占据响应律，AGEX 坐实时间维零触达，
  **PRICEX（本轮）坐实货币维度对该池零触达**：货币字段在归因面按
  类型缺席（合同内剂量不存在），IRLS/solve 全部站点计数本轮恒等
  复核。观测行六维 + 货币维七个输入维度都只会让该池占比更高或持平，
  收敛缩短类无逐位合法形态。触碰地板本体的机制只剩 SIMD/线程
  （S9-C-1/2 + 不变量禁止）与数值路径改道（X2-3 冻结）。
- **irls 自身池 150.8–152.3 ms（预演 151.9；与 R25-C 带 153.1–154.8
  下邻接、与 R24-C 带 151.7–156.6 交叠——本 VM 类微快，与 APC 池
  同向诚实记名）**：可省项全部已关闭（S2-C/S6-C/S7-C 落地本体之上：
  S10-C-1 决斗带内、S9-C-3 符号翻转、S6-C-6 上界 2–4、S5-C-7 无正
  决斗、S9-C-4、S4-C-5/6、S2-C-3、S8-C-3、R11-C RID/CNT/EXP/ALLOC、
  R12-C MUCSE、R14-C SWORD/IRLSDP、R15-C STORD/RIDGE、R16-C ICOL、
  R17-C NSQRT）。货币维度对该池零触达（A 面类型级缺席）。本轮零
  新位置。
- **APC 池 46.0–47.3 ms（本 VM 四读数；两次正典 46.0/46.2 低于
  R24-C 带 46.4–53.3 下沿 0.2–0.4 ms ⇒ 按 R14-C 处置规则随读重锚
  为本轮带 46.0–47.3，与 R24-C 带交叠 [46.4, 47.3]，本 VM 类 APC
  侧微快、非切片改动）**：派单强制天花板重测照常执行（这不是重跑
  已关 ID 的决斗，也不构建任何堆叠候选道）：APCX 无计时器独立道
  **54.7–60.2 ms**（基形状近似，**sink=7.309 与 R14-C/R16-C..R25-C
  逐位同值** = 跨轮确定性旁证，派单强制项成立）；FLOORX 最廉价逐位
  合法形态地板 **34.4–39.9 ms** ⇒ **组合天花板 = 池独立道 − 地板 =
  19.5–22.9 ms（四次读数）< 35**。十一轮天花板读数 12.1–23.8 /
  13.8–21.6 / 12.1–20.9 / 13.9–24.0 / 17.4–21.6 / 12.5–19.8 /
  17.1–21.8 / 14.6–16.0 / 15.9–22.7 / 20.8–25.6 / 19.5–22.9 稳定
  亚带，组合关闭维持。R13-C REFZ 参考水平份额不受影响。PRICEX 的
  计数见证补强该闭合：APC 活跃对 277,606 本轮恒等——货币维度无法
  触达 APC 池（类型级）。
- **引擎侧三轴（R15-C/R16-C 收口，本轮未重测未补铸）**：COLDX
  预热份额、GCAX 收集器份额、ALLOC 分配封顶——三轴均无切片内可
  受理变换，稳态口径下三大池冻结结论不受影响；本轮中位口径仍为
  稳态口径（PRICEX 各剂量点取自全剂量预热后的稳态窗口，首触效应
  不进入读数）。
- **拒绝面（R18-C REJX 轴收口，本轮未重测未补铸）**：31 终点
  once-per-fault 定价原样引用；拒绝路径不构成第五类成本；PRICEX 的
  B 面八剂量全部 480/480 selected-cheapest（sparse/below-floor/
  hysteresis 分支零流量——夹具即为此设计：全 fresh 全 wellSampled
  使价格消费站点看到极大流量），A′ 面五剂量诊断路径零拒绝分支——
  货币维度在全剂量下拒绝/退化分支零流量。四分类 + once-per-fault
  拒绝类继续穷尽。
- **posterior/R1 面（R25-C AGEX 直测 + 本轮 PRICEX 直测双封顶）**：
  整影子工作载 4.75–6.55 ms @ 160 键 × 480 episode × M=10（≥ 仓内
  一切调用方一个数量级以上），其中价格可动池 price-reduce
  0.191–0.313 ms——**该面整体离带 ×5.3–6.3，价格可归因份额离带
  ×112+**；真实入口整命令面 5.37–7.41 ms 离带 ×4.7–5.1。与 R10-C
  「七文件亚 ms 级」裁决兼容且更强（输入维度直测而非池算术外推）。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测带级摆幅带内），
  维持关闭。本轮探针无一是稳定越带正项（从未点名普查为零、PRICEX
  是货币坐标定价、APCX/FLOORX 是天花板测量不是候选），无新成员可
  入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建
面开放 / X2-3 数值路径解冻 / 单线程审计基底变更 / maxIter=50、
tol=1e-8、bootstrap/seed 契约、rows/OutcomeObservation 输入契约、
R0Decision/PropensityLogEntry 货币字段契约（含 R0 升序排序契约——
恒等折叠定理的前提）变更——均不预期，前者属阈值/测试面禁区、后者
属公开签名面）；（b）现实规模位移触达 SCALEX 标定的越线点（最尖锐
on-prob K\*=2.6×、名义收益口径 ≥~3.4×；APC 组合外推 ~1.4× 需按
R14-C 规则在该剂量重跑地板——今日生产流量在 perf 夹具当量及以下，
均未触达；结局组成维度经 YMIX、重采样身份维度经 SEEDX、呈现次序
维度经 ORDX、占据维度经 OCCX、时间维度经 AGEX、**货币维度经本轮
PRICEX** 证明**均无越线点存在**：归因面按构造零响应，R1 面价格可
归因位移 ≈0.11 ms/480 episode 需 ≈15.3 万 episode/报告 = 合成工作
载 ×318、仓内真实调用方 3 个数量级以上）；或（c）指出「重命名 /
亚带 / 逐位违例 / 结构交换负优化 / once-per-fault 拒绝」之外的
第六类成本——二十六遍枚举未再发现。

## 4. 重命名检查（派单强制项）

- **PRICEX vs AGEX（R25-C）**：AGEX 动**观测行上的时间字段值**
  （`occurredAtMs`），机制是衰减权重 → 后验质量 → betacf 收敛迭代 +
  阈值控制流（唯一响应计数 betacfIters）；PRICEX 冻结全部时间戳
  （八剂量全 fresh = nowMs，观测数组对象同一），动的是**不在观测行
  上的价格向量**，机制是比较/线性查找/平局仲裁控制流（唯一响应计数
  tieBranches/tierIndexHops），且 **betacfIters=76,800 八剂量恒等
  断言**——AGEX 的响应计数在价格轴下纹丝不动、PRICEX 的响应计数在
  AGEX 里恒零（R25-C 夹具单模型无 reduce 平局），双向正交见证。对象
  （时间 vs 货币）、宿主（posterior 池 vs price-reduce 池）、机制
  均不同。非重命名，AGEX 未重测（零时间剂量触碰）。
- **PRICEX vs SCALEX（R20-C）/ YMIX（R21-C）/ SEEDX（R22-C）/
  OCCX（R24-C）**：四轴全部作用在**观测行多重集**上（体量 / 结局
  组成 / 重采样身份 / 格胞占据）；PRICEX 的八个剂量共享**同一个
  观测数组对象**（对象同一性断言，强于逐位冻结）——观测行多重集
  零移动，每键 20/5 组成、4,000 obs 体量、无 RNG、占据轮廓全部
  恒等。非重命名，四轴未重测。
- **PRICEX vs ORDX（R23-C）**：ORDX 置换**同一个观测多重集的发射
  次序**（posterior 输入面呈现序）；PRICEX 冻结观测发射序（同一
  数组对象），desc 剂量下 candidates 数组的重排**不是独立置换的
  呈现序**，而是冻结的 R0 升序排序契约在移动价格向量下的**像**
  （逐剂量断言 candidates-cost-ascending + cost-values-exact——
  次序由价格函数决定，是剂量的组成部分而非第二个自由度）；且估计
  多重集跨剂量逐位等于锚剂量断言坐实「价格只能置换估计数组呈现序、
  不能触及数值」。对象（观测发射序 vs 价格向量）、面（posterior
  输入 vs R0 决策输入）不同。非重命名，ORDX 未重测。
- **PRICEX vs FITQ（R19-C）/ ITERX（R16-C）**：两者对象都是**归因面
  IRLS 池**（拟合粒度墙钟分布 / 迭代计数普查）；PRICEX 的 A 面恰好
  证明货币坐标在归因面**按类型缺席**（无剂量可施），B 面宿主是
  r1 的 price-reduce 池（不同池、不同函数）。FITQ 重开条件
  （bootstrap 抽取数/拟合规模位移 ≥1 个数量级）未被触发：本轮
  options 全程默认。非重命名。
- **PRICEX vs M=1 catalog collapse（R24-B，派单禁移植项）**：该轴
  塌缩**目录基数 M**（候选个数）；PRICEX 冻结 M=10 恒等断言
  （M-frozen 逐剂量）+ id 集合冻结断言，只动价格**数值**的量级/
  平局/方向几何。坐标（基数 vs 数值几何）不同。非移植。
- **PRICEX vs R25-A value-domain / string content-class census
  （派单禁移植项）**：R25-A 的轴对象是**字符串字节内容类**——固定
  计数、固定长度下字节是什么（regex 扫描 / JSON 转义 / includes
  纹理），宿主机制是逐字节扫描成本；PRICEX 的对象是 **double 型
  数值坐标**（货币量级/平局结构），机制是浮点比较 + 数组查找 +
  平局分支——**零字符串字节移动**（id-set-frozen 逐剂量断言）、
  零扫描纹理、零长度移动。字段类不同、机制不同、面不同：非移植、
  非重命名。（若未来有人试图以「数值 value-domain」之名重提本轴：
  本报告已把货币维度三面直测/构造封顶，重提即撞 PRICEX 收口。）
- **PRICEX vs R26-A collation-locale face（运行中快进新增禁移植
  项）**：R26-A 的对象是**进程 locale 环境 × `localeCompare` 站点**
  （字符串比较语义对环境的耦合，A 区）；PRICEX 剂量路径内**零字符串
  比较移动**——r1 的成本比较是数值 `<`/`>`，平局仲裁走 tierIndex
  位置查找而非字符串序；R0 排序的 modelId 平局腿是 code-unit
  `<`/`>`（非 localeCompare）且在切片外、id 集合冻结。零 locale
  耦合、零环境剂量（process env 未触，R25-I 域同样零触）。非移植。
- **PRICEX vs R26-B privacy / provider-policy constraint-leg face
  （运行中快进新增禁移植项）**：R26-B 的对象是**资格腿四态**
  （`privacyRequired` × `privacyClass` × `providerPolicy`，B 区
  live 资格面）；PRICEX 冻结全部资格坐标（privacyRequired=
  "cloud-general"、providerPolicy="approved" 常量、all-eligible
  逐剂量断言——资格腿零流量零移动），只动已合格候选的价格数值。
  非移植。
- **PRICEX vs R26-D injection-seam default-arm face（运行中第二次
  快进新增禁移植项）**：R26-D 的对象是**注入缝默认臂**
  （`RegistryOptions` 生产默认 `nowIso` 时钟 × `createResourceVersionId`
  id 链 vs 历轮确定性注入臂，D 区适配注册面）；PRICEX 全程走确定性
  显式实参（`nowMs=1.756e12` 常量显式传参——C 切片 posterior/r1 的
  时钟本就是显式参数无默认臂；零 id 生成、零 uuid、零注入缝触碰），
  剂量对象是价格**数值**不是注入臂选择。面（注入缝 vs 输入数据值域）、
  机制（默认臂链路成本 vs 比较/平局控制流）均不同。非移植。
- **PRICEX vs X1-4 / S1-C-2/3 / S1-C-10（r1 站点既有 ID）**：三者
  是 r1 面**代码变换**裁决；PRICEX 是**输入轴定价**，零道立起零
  决斗重跑——其候选道形族（find→Map、tier rank 预建、比较器改写）
  在 §2 被三重拒绝：宿主池 price-reduce ≤0.313 ms 亚带 ×112 +
  生产几何（历史 asc 梯）平局流量恒零 + 比较器改写属行为/逐位违例
  （X1-4 域）。计数见证（costFindHops=63/episode 恒等）是新剂量点
  的打印，不重测任何已关变换。非重命名。
- **PRICEX vs shadow.ts 的 comparisonBudgetUsd/comparisonCostUsd 与
  RouteRequest.budgetUsd/fixedCostUsd**：四者是 **config/请求配置面
  货币**（预算与门控参数，前两者在切片内 shadow 配置、后两者在切片
  外 policy 请求面）；PRICEX 明示只剂量化**输入数据面货币**
  （estimatedCostUsd / costUsd），config 面货币本轮冻结不剂量化
  不铸名（§2 枚举收口段诚实划界）——恰为避开 R24-I
  adaptation-state coordinate（配置态面，派单禁移植）的裁决域。
- **APCX/FLOORX 重测 vs S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 / CMB
  禁令**：本轮重测系**派单强制天花板复测**（池两读数低于带下沿故
  按规随读重锚为 46.0–47.3），产出仅用于天花板差值算术与 sink 逐位
  核对；不重跑任何已关 ID 决斗、不构建堆叠候选、不产出可落地形态，
  四个成员 ID 与 CMB 禁令全部维持原状。
- **换名再提交拒列（本轮核实）**：零。R13-C..R25-C 二十五个无名
  关闭（含 TAILG/REJX/FITQ/SCALEX/YMIX/SEEDX/ORDX/OCCX/AGEX）未
  重探、未补铸、未换址重提；S10-C-1..3 / S12-C-1 / RID / CNT 零
  重提；ITERX/COLDX/AAFLR/GCAX 四轴未补铸；嵌套序 × 布局组合空间
  未触碰；X2-1 重写维持排除（S7-C 的 0/1 形态即落地形态，未另起
  平行实现）；COTARG 与全部他区无名轴（含 R25-B/D/F/G/H/I 六条 +
  R24-B/D/F/H 四条 + R26-A/B/D 三条新增）零移植。

## 5. 测试与验证

环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0）、pnpm 10.17.1、
`pnpm install --frozen-lockfile`。生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录）
npx tsx /tmp/r26c/r26c-anchor.mts           # ✓ 6,277 项逐位 × 3 次正典运行全绿
                                            #   （53 例 ×{仪表道,落地道} 忠实性电池
                                            #    + 3 seed × 2 自确定性 + perf 夹具逐位
                                            #    + PRICEX A′ 面五值剂量诊断输出逐位
                                            #    + PRICEX B 面八价格剂量：R0 契约六断言/剂量
                                            #      + 复刻道 vs 生产导入逐位（480 episode ×
                                            #      10 估计全字段 + 160 键 × 7 字段/剂量）
                                            #      + 跨剂量估计不变断言（键级 + episode 级）
                                            #      + 恒等折叠定理 + 后验/价格计数恒等断言
                                            #      + 计数三 rep 确定性断言
                                            #    + 真实入口自确定性 + 配对恒等定理/剂量
                                            #    + APCX/FLOORX 天花板重测每次运行内嵌）
                                            #   另有 1 次预演运行同样 6,277 项全绿
                                            #   （预演与正典脚本逐字节相同，零改动）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 659.9 ms（1.11× vs 冻结 S6-C ctl 734.4）；r1c 锚点：
# production 665.1 ms（2.31× vs 参考 1536.9）——与 §1 生产正典中位带
# （663.0–669.9）互证。
```

代表性一次测量彩排全量输出（正典 run A；run B/C 与预演数字见 §1
池表与 §2）：

```text
scenario 1 (two-lane bitwise fidelity, 53 cases x {instrumented, plain-landed} vs production fitLogitAdditive import)
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)
  shape small (3 models x 3 projects, 120 rows): production median 33.9 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 664.3 ms/report (7 reps)
  perf reps: 670.2 661.8 662.5 663.9 664.3 666.4 677.1
instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):
  total                 666.5 ms
  solveSymmetric        453.2 ms  (68.0%)  [8966 calls]
  irls minus solve      151.4 ms  (22.7%)  [8966 iterations, 3586400 row visits, 1249652 irls sigmoids]
  onProbabilitiesFor    6.8 ms  (1.0%)  [80400 sigmoid+dot]
  APC (all sweeps)      46.2 ms  (6.9%)  [277606 active pairs, 12259 calls]
  bootstrap sampling    1.7 ms  (0.3%)
  sort/percentile tail  1.5 ms  (0.2%)  [12200 draws-map ops]
  base-row projection   0.003 ms  (once/report)
  outside pools (build/supports/keys/base copies) 5.6 ms  (0.8%)
  fits 201  draws successful=200 collapsed=0 singular=0
probe PRICEX (monetary-cost coordinate / price-geometry dose response; NEW axis, not a lane):
  face A (attribution face — the only +-35 ms-capable pools): the money coordinate is ABSENT BY TYPE (OfflineRow carries no cost field; zero cost/usd/price identifiers in offline-logit.ts / offline-prob-add.ts / lin-alg.ts / posterior.ts) — no in-contract dose exists; closes by construction
  face A' (propensity face): PropensityLogEntry.costUsd is declared on the public input type and never read by any slice function — five value doses, all outputs must be bitwise identical:
    computeOverlapDiagnostics wall (N=200 logs, per call): anchor:0.6us zero:0.5us huge:0.6us subn:0.5us frac:0.5us
  face B (R1/shadow face — the slice's ONLY live price consumers: costOf/cheaperEstimate/tierIndex + selectedCost):
    frozen dims: keys=160 (8 fam x 2 roles x 10 model versions), obs/key=25 (20 PASS + 5 FAIL[model], frozen order, ALL fresh), episodes=480, M=10, nowMs=1756000000000, config=DEFAULT; only the estimatedCostUsd vector moves (per-dose R0 from the production routeR0 import)
    dose price=asc: prod 5.34 ms/workload (replica 6.12; prepare 1.95 / decay-scan 0.06 / lcb-quantile 1.24 / price-reduce 0.197 ms); tieBranches=0 tierIndexCalls=0 tierIndexHops=0; selection: model-0=480 (selected-cheapest=480/480); wellSampledKeys=160/160; betacfIters=76800
    dose price=desc: prod 4.94 ms/workload (replica 6.01; prepare 0.70 / decay-scan 0.06 / lcb-quantile 1.22 / price-reduce 0.196 ms); tieBranches=0 tierIndexCalls=0 tierIndexHops=0; selection: model-9=480 (selected-cheapest=480/480); wellSampledKeys=160/160; betacfIters=76800
    dose price=alltie: prod 5.25 ms/workload (replica 5.91; prepare 0.67 / decay-scan 0.05 / lcb-quantile 1.21 / price-reduce 0.290 ms); tieBranches=4320 tierIndexCalls=8640 tierIndexHops=21600; selection: model-0=480 (selected-cheapest=480/480); wellSampledKeys=160/160; betacfIters=76800
    dose price=pairtie: prod 4.86 ms/workload (replica 5.89; prepare 0.71 / decay-scan 0.05 / lcb-quantile 1.21 / price-reduce 0.209 ms); tieBranches=480 tierIndexCalls=960 tierIndexHops=480; selection: model-0=480 (selected-cheapest=480/480); wellSampledKeys=160/160; betacfIters=76800
    dose price=int: prod 5.23 ms/workload (replica 4.82; prepare 0.68 / decay-scan 0.05 / lcb-quantile 1.21 / price-reduce 0.191 ms); tieBranches=0 tierIndexCalls=0 tierIndexHops=0; selection: model-0=480 (selected-cheapest=480/480); wellSampledKeys=160/160; betacfIters=76800
    dose price=micro: prod 4.95 ms/workload (replica 5.78; prepare 0.69 / decay-scan 0.05 / lcb-quantile 1.22 / price-reduce 0.235 ms); tieBranches=0 tierIndexCalls=0 tierIndexHops=0; selection: model-0=480 (selected-cheapest=480/480); wellSampledKeys=160/160; betacfIters=76800
    dose price=spread: prod 5.27 ms/workload (replica 5.81; prepare 0.66 / decay-scan 0.05 / lcb-quantile 1.22 / price-reduce 0.200 ms); tieBranches=0 tierIndexCalls=0 tierIndexHops=0; selection: model-0=480 (selected-cheapest=480/480); wellSampledKeys=160/160; betacfIters=76800
    dose price=subn: prod 5.55 ms/workload (replica 6.06; prepare 0.66 / decay-scan 0.05 / lcb-quantile 1.21 / price-reduce 0.197 ms); tieBranches=0 tierIndexCalls=0 tierIndexHops=0; selection: model-0=480 (selected-cheapest=480/480); wellSampledKeys=160/160; betacfIters=76800
    price-dose arithmetic (35 ms band): worst dose subn 5.55 ms (x6.3 below band); worst-minus-best delta 0.69 ms (x50.7 below band); worst price-reduce pool 0.290 ms [alltie] (x121 below band); whole-pool zeroing bounded by the worst dose itself
  face B real entry (production buildR1ShadowReport import per dose — routeR0 + slice + gatedComparisonReport; whole-command surface, R7-I lesson):
    whole-entry wall (480 episodes, median of 5, ms): asc:6.52 desc:6.94 alltie:5.89 pairtie:5.88 int:5.37 micro:5.39 spread:5.40 subn:5.66
    real-entry arithmetic: worst desc 6.94 ms (x5.0 below band); worst-minus-best 1.57 ms; in-slice share bounded by the workload lane above
probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) 0.272 ms/fit -> x201 fits = 54.7 ms/report (base-shape approximation; sink=7.309)
probe FLOORX: cheapest bitwise-legal sweep 0.173 ms/fit -> x201 fits = 34.7 ms/report floor; APC combination ceiling on THIS VM = 20.0 ms (floorSink=7.309)
APC pool median this run: 46.2 ms (R24-C band 46.4-53.3, maintained through R25-C; re-anchor band only if outside)

ALL EQUIVALENCE CHECKS PASSED (6277 bitwise checks)
```

其余两次正典运行与一次预演的关键数字：

```text
run B: prod median 663.0 (small 32.9; perf reps 678.4 660.9 663.2 663.0 662.7 664.8 662.5);
       pools solve 451.6 / irls-other 150.8 / APC 46.0;
       PRICEX face B prod: asc 6.41 / desc 6.17 / alltie 6.55 / pairtie 5.73 / int 4.84 /
       micro 5.70 / spread 6.38 / subn 5.11 ms; worst alltie 6.55 (x5.3 below band);
       worst-minus-best delta 1.70 ms (x20.5 below band); worst price-reduce 0.313 [alltie];
       real entry: 6.80/6.80/6.40/6.34/6.12/6.14/5.97/6.59; worst desc 6.80 (x5.1);
       APCX 60.2 / FLOORX 37.3 / ceiling 22.9
run C: prod median 669.9 (small 33.1; perf reps 674.1 669.0 669.8 669.9 669.5 670.4 674.0);
       pools solve 451.4 / irls-other 152.3 / APC 46.5;
       PRICEX face B prod: 5.24/5.37/5.34/4.75/5.85/5.07/4.91/5.35 ms;
       worst int 5.85 (x6.0 below band); worst-minus-best delta 1.09 ms (x32.0 below band);
       worst price-reduce 0.303 [alltie];
       real entry: 6.58/7.41/5.91/5.71/5.71/5.81/6.04/6.04; worst desc 7.41 (x4.7);
       APCX 59.4 / FLOORX 39.9 / ceiling 19.5
预演 run 0: prod median 663.7 (perf reps 662.5-669.3; small 32.2); pools solve 451.5 /
       irls-other 151.9 / APC 47.3; PRICEX face B prod 5.67-6.20 ms; worst subn 6.20 (x5.6);
       delta 0.53 (x65.6); worst price-reduce 0.301 [alltie];
       real entry 6.17-6.98, worst desc 6.98 (x5.0); APCX 55.7 / FLOORX 34.4 / ceiling 21.3;
       6,277 项全绿
PRICEX 结构量（四运行逐位一致）: tieBranches/tierIndexCalls/tierIndexHops =
       0/0/0 (asc,desc,int,micro,spread,subn) / 480/960/480 (pairtie) /
       4320/8640/21600 (alltie — 唯一剂量响应计数族);
       selection: desc → model-9 x480, 其余七剂量 → model-0 x480
       (全部 selected-cheapest=480/480, wellSampledKeys=160/160 — 语义随剂量按契约移动);
       后验计数八剂量恒等 (断言): powCalls=4000, quantileCalls=estimateComputes=160,
       cacheHits=4640, ribCalls=betacfCalls=12800=80x160, betacfIters=76800
       (AGEX 的唯一响应计数在价格轴下不动 — 价格不是后验输入);
       价格计数恒等 (断言): comparisons=4320=9x480, costOfCalls=8640=18x480,
       costFindHops=30240=63x480 (find 跳数位置决定, 含 desc);
       恒等折叠定理: rightWins=0 八剂量, selection==r0.selection x480x8;
       每键估计 160x7 字段与每 episode 估计多重集 x480 跨剂量逐位等于锚剂量;
       A' 面五值剂量诊断输出逐位相同 (每运行)
APC 天花板重测（每次运行内嵌 × 4）: APCX 55.7/54.7/60.2/59.4; FLOORX 34.4/34.7/37.3/39.9;
       ceiling 21.3/20.0/22.9/19.5 (全部 < 35; sink=7.309 与 floorSink=7.309
       八读数全同，且与 R14-C/R16-C..R25-C 逐位同值);
       APC 池四读数 46.0-47.3, 两读数低于 R24-C 带下沿 0.2-0.4 ms ⇒ 随读重锚 46.0-47.3
       (与 R24-C 带交叠 [46.4, 47.3])
全部站点计数（8,966 / 3,586,400 / 1,249,652 / 277,606 / 12,259 / 12,200 /
80,400 / 201）在四次运行逐位一致（确定性计数）。
```

## 6. 新增排除 ID：无

第二十六组候选集为空。从未点名站点普查结果为零（连续第八遍；R18-C
「最后一个从未点名位置」判定经 R19-C..R25-C 与本轮八遍核实），PRICEX
为货币成本坐标 / 价格几何剂量响应轴（重开条件：R0Decision /
PropensityLogEntry 货币字段契约或 R0 升序排序契约本身变更——属公开
签名面，不预期。货币维度本身**无越线点**：归因面按类型缺席（合同内
剂量不存在，按构造关闭），propensity 面声明而零读点（五值剂量逐位
见证），R1 面唯一响应池 price-reduce 0.191–0.313 ms ×112 亚带、
唯一响应计数为平局路径（21,600 跳极端全价 ≈0.10 ms）、后验计数含
betacfIters 八剂量恒等（价格不是后验输入的计数级证明），价格可归因
位移需 ≈15.3 万 episode/报告才触 35 ms 带 = 合成工作载 ×318、仓内
真实调用方 3 个数量级以上；切片公开输入面至此全数值坐标定价完毕——
观测行六维（SCALEX 体量 / YMIX 组成 / SEEDX 身份 / ORDX 次序 /
OCCX 占据 / AGEX 时间）+ 决策面货币维（PRICEX）），APCX/FLOORX 为
按规天花板重测（池 46.0–47.3 两读数低于 R24-C 带下沿 ⇒ 随读重锚，
交叠 [46.4, 47.3]；天花板 19.5–22.9 < 35 稳定亚带，sink 逐位同值）。

## 附录：测量彩排脚本（完整，可复现）

### 主彩排 `/tmp/r26c/r26c-anchor.mts`

（四次运行——1 预演 + 3 正典——所用脚本逐字节相同，预演后零改动。）

```ts
/**
 * Round 26 / R26-C measurement re-anchor + twenty-sixth-group closure probes
 * (offline routing slice, twenty-sixth pass). NOT committed — the twenty-
 * sixth-group enumeration carries no candidate lanes that reach the
 * acceptance bar; full source goes into the R26-C report appendix
 * (R7-C..R25-C precedent).
 *
 * Discipline (same as R11-C..R25-C):
 *   1. re-anchor the post-S7-C profile on THIS VM: production median
 *      ms/report on the perf shape (400 rows, 6 models x 8 projects,
 *      bootstrap=200) and the small shape, x-checked by an INSTRUMENTED
 *      verbatim pipeline copy (timers/counters only; every float op, value,
 *      and order identical — validated bitwise against the production
 *      import over the full 53-case battery before its numbers are used);
 *   2. derive the per-pool cost table (solve / irls-other / on-prob / APC /
 *      sampling / tail / base-copy) plus site counts;
 *   3. PRICEX (new completeness axis for C; monetary-cost coordinate /
 *      price-geometry dose response — the first never-dosed coordinate of
 *      the C-slice public input surface OUTSIDE the observation row whose
 *      seven coordinates R25-C closed: strings (IDG frozen), y/outcome
 *      (YMIX), volume (SCALEX), order (ORDX), occupancy (OCCX), resample
 *      identity (SEEDX), time (AGEX). The slice's public input surface
 *      additionally carries MONEY: R1Input.r0.candidates[].estimatedCostUsd
 *      (read by r1.ts costOf/cheaperEstimate/tierIndex and by
 *      r1-shadow-report.ts selectedCost) and PropensityLogEntry.costUsd
 *      (declared but never read inside the slice). Every C-round fixture
 *      that ever built a catalog pinned the price vector (R25-C face-B
 *      fixture: estimatedCostUsd = 0.001*(idx+1)) and no round ever moved
 *      it or counted it as a dimension. PRICEX freezes volume, level-name
 *      sets, outcome composition, emission order, occupancy, seed, time
 *      (all observations fresh at nowMs), options, config, catalog
 *      cardinality (M=10 every dose), model ids/versions, and eligibility,
 *      and moves ONLY the numeric price vector:
 *        face A (attribution face; the only pools that can reach the
 *          +-35 ms band): the money coordinate is ABSENT BY TYPE — the
 *          attribution input rows carry no cost field at all (rg census:
 *          zero cost/usd/price identifiers in offline-logit.ts,
 *          offline-prob-add.ts, lin-alg.ts, posterior.ts), so no
 *          in-contract dose exists and the face closes by construction
 *          (stronger than AGEX face A, where the field existed and was
 *          dropped at entry);
 *        face A' (propensity face): PropensityLogEntry.costUsd is the
 *          slice's only present-but-never-read money field — five value
 *          doses must leave computeOverlapDiagnostics /
 *          validateCounterfactualReport / isFabricatedPositiveSupport
 *          bitwise identical;
 *        face B (R1/shadow face; the slice's ONLY live price consumers):
 *          a frozen 160-key x 25-obs/key x 480-episode x M=10 shadow
 *          workload (the r1-shadow-report inner pattern: shared index
 *          prepared once, routeR1 per episode; all keys wellSampled and
 *          above the floor, so the price-consuming reduce sees maximal
 *          traffic: 9 comparisons/episode) dosed over eight price
 *          geometries {asc (the historical pin), desc, alltie, pairtie,
 *          int, micro, spread, subn}; per-dose R0 decisions are produced
 *          by the PRODUCTION routeR0 import (out-of-slice, constructed
 *          outside the timed window), so every input is in-contract by
 *          construction; deterministic witnesses: price-counter table
 *          (comparisons / costOf calls / find hops / tie branches /
 *          tierIndex calls+hops / right-wins), posterior count identities
 *          (powCalls / quantileCalls / estimateComputes / cacheHits /
 *          betacfIters all dose-invariant — price cannot reach the
 *          posterior pool), per-key estimates bitwise EQUAL TO THE ANCHOR
 *          DOSE across all doses, per-episode estimate MULTISETS equal to
 *          anchor (price only permutes presentation order), selection ==
 *          r0.selection every episode every dose (the identity-fold
 *          theorem: R0's cost-ascending output contract pre-sorts the
 *          reduce's traffic, so the distinct-cost reduce never replaces
 *          its accumulator and price values reach the slice's control
 *          flow only through the tie predicate), and per-dose bitwise
 *          fidelity of the instrumented verbatim replica lane against the
 *          production prepareR1Observations/estimateForKey/routeR1
 *          imports; plus a REAL-ENTRY lane: the production
 *          buildR1ShadowReport import per dose (routeR0 + slice +
 *          gatedComparisonReport; whole-command surface, R7-I lesson).
 *      It builds no lane and re-runs no closed duel (no SCALEX size dose,
 *      YMIX composition dose, SEEDX seed dose, ORDX order dose, OCCX
 *      occupancy dose, or AGEX time dose is touched — every PRICEX dose
 *      shares ONE frozen observation array object and asserts the catalog
 *      id set and cardinality frozen).
 *   4. APC pool disposition: R24-C re-anchored the APC band to 46.4-53.3 ms
 *      on its VM class and R25-C maintained it. The ceiling re-measure
 *      (APCX/FLOORX, R14-C method verbatim: 5 warmup + 60 timed sweeps,
 *      sink printed to 3 decimals) runs in every canonical run; the R14-C
 *      disposition rule re-anchors the BAND only if the instrumented pool
 *      exits 46.4-53.3. The sink must match 7.309 bitwise (cross-round
 *      determinism witness, dispatch-mandated).
 *   5. re-check production self-determinism across 3 seeds x 2 runs.
 *
 * AGEX / OCCX / ORDX / SEEDX / YMIX / SCALEX / FITQ / TAILG / REJX / NSQRT /
 * PMV / OSTZ / ICOL / SFILL / ITERX / COLDX / AAFLR / GCAX / STORD / RIDGE
 * are NOT re-probed and NOT re-minted (dispatch order). COTARG and the
 * unnamed sibling axes (process-environment coordinate, consumer-stage
 * string-representation census, injection command-kind face, plan-header
 * identity-geometry face, identity-coordinate face, task-face
 * optional-field presence topology, value-domain / string content-class
 * census, M=1 catalog collapse, approval-authority face, state
 * serialization-boundary provenance face, operand-residency census,
 * adaptation-state coordinate) are NOT ported (dispatch order). PRICEX
 * moves a NUMERIC DATA coordinate (what an arm costs) through documented
 * comparison semantics — it does not move catalog cardinality (M=10
 * asserted every dose, distinct from the banned M=1 catalog collapse), any
 * identifier string byte, any string length or content class, any optional-
 * field presence, any config or options value, or any observation field.
 * ShadowConfig.comparisonBudgetUsd / comparisonCostUsd are CONFIG-face
 * money (the R25-C options/config boundary keeps them out of the input-
 * data axis domain); bandit.ts carries no money field (rg census).
 *
 * Run: npx tsx /tmp/r26c/r26c-anchor.mts
 */

import { fitProbabilityAdditive } from "/workspace/src/routing/offline-prob-add.js";
import {
  DEFAULT_POSTERIOR_CONFIG,
  estimateForKey,
  prepareR1Observations,
} from "/workspace/src/routing/posterior.js";
import type { PosteriorConfig } from "/workspace/src/routing/posterior.js";
import { routeR1 } from "/workspace/src/routing/r1.js";
import type { R1Decision } from "/workspace/src/routing/r1.js";
import type { OutcomeObservation } from "/workspace/src/routing/outcomes.js";
import type { ModelDescriptor } from "/workspace/src/routing/capability-registry.js";
import type { R0Decision } from "/workspace/src/routing/r0.js";
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.js";
import type { AttributionReport, OfflineRow } from "/workspace/src/routing/offline-types.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import {
  computeOverlapDiagnostics,
  isFabricatedPositiveSupport,
  validateCounterfactualReport,
} from "/workspace/src/routing/propensity.js";
import type { PropensityLogEntry } from "/workspace/src/routing/propensity.js";
import { buildR1ShadowReport } from "/workspace/src/routing/r1-shadow-report.js";
import type { FrozenR1ShadowEpisode, R1ShadowReportInput } from "/workspace/src/routing/r1-shadow-report.js";
import { routeR0 } from "/workspace/src/routing/r0.js";
import type { R0Config } from "/workspace/src/routing/r0.js";
import type { RouteRequest } from "/workspace/src/routing/policy.js";

/* --------------------------- shared constants --------------------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}

interface FitResult {
  readonly coefficients: readonly number[] | null;
}

type IrlsImpl = (
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
) => FitResult;

function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}

function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/* ------------- verbatim production pipeline building blocks -------------- *
 * Transcribed from src/routing/offline-logit.ts and src/routing/lin-alg.ts
 * at the 183df9b-landed state. The instrumented lane's ONLY additions are
 * STATS counter increments and performance.now() reads at call boundaries;
 * no float operation, operand, or ordering differs. The plain lane carries
 * no timers at all. Both lanes are validated bitwise against the production
 * import over the full 53-case battery — and additionally at every OCCX
 * dose — before any number is reported. */

const STATS = {
  fits: 0,
  irlsMs: 0,
  irlsIterations: 0,
  solveCalls: 0,
  solveMs: 0,
  sigmoidIrls: 0,
  rowVisits: 0,
  onProbMs: 0,
  onProbSigmoids: 0,
  apcMs: 0,
  apcActivePairs: 0,
  apcCalls: 0,
  samplingMs: 0,
  drawsMapOps: 0,
  tailMs: 0,
  baseCopyMs: 0,
  totalMs: 0,
  collapsedDraws: 0,
  singularDraws: 0,
  successfulDraws: 0,
  reset(): void {
    this.fits = 0; this.irlsMs = 0; this.irlsIterations = 0; this.solveCalls = 0;
    this.solveMs = 0; this.sigmoidIrls = 0; this.rowVisits = 0; this.onProbMs = 0;
    this.onProbSigmoids = 0; this.apcMs = 0; this.apcActivePairs = 0;
    this.apcCalls = 0; this.samplingMs = 0; this.drawsMapOps = 0;
    this.tailMs = 0; this.baseCopyMs = 0; this.totalMs = 0;
    this.collapsedDraws = 0; this.singularDraws = 0; this.successfulDraws = 0;
  }
};

function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);

  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);

  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);

  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });

  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}

function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}

function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}

/* verbatim landed solveSymmetric (lin-alg.ts) — plain, no timers */
function solvePlain(a: readonly (readonly number[])[], b: readonly number[]): number[] | null {
  const n = b.length;
  if (a.length !== n || a.some((row) => row.length !== n)) {
    throw new DomainValidationError("solveSymmetric requires a square matrix matching b");
  }
  const m: number[][] = a.map((row) => [...row]);
  const x: number[] = [...b];
  const eps = 1e-12;

  for (let col = 0; col < n; col++) {
    let pivotRow = col;
    let pivotAbs = Math.abs(m[col]![col]!);
    for (let row = col + 1; row < n; row++) {
      const abs = Math.abs(m[row]![col]!);
      if (abs > pivotAbs) {
        pivotAbs = abs;
        pivotRow = row;
      }
    }
    if (pivotAbs < eps) return null;
    if (pivotRow !== col) {
      const tmp = m[col]!;
      m[col] = m[pivotRow]!;
      m[pivotRow] = tmp;
      const tb = x[col]!;
      x[col] = x[pivotRow]!;
      x[pivotRow] = tb;
    }
    const colArr = m[col]!;
    const pivot = colArr[col]!;
    const xCol = x[col]!;
    for (let row = col + 1; row < n; row++) {
      const rowArr = m[row]!;
      const factor = rowArr[col]! / pivot;
      if (factor === 0) continue;
      let k = col;
      const stop = n - 3;
      for (; k < stop; k += 4) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
        rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
        rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
      }
      for (; k < n; k++) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
      }
      x[row] = x[row]! - factor * xCol;
    }
  }

  const solution = new Array<number>(n).fill(0);
  for (let row = n - 1; row >= 0; row--) {
    const rowArr = m[row]!;
    let sum = x[row]!;
    for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
    const diag = rowArr[row]!;
    if (Math.abs(diag) < eps) return null;
    solution[row] = sum / diag;
  }
  for (const value of solution) {
    if (!Number.isFinite(value)) return null;
  }
  return solution;
}

/* The lanes differ ONLY in instrumentation (counters + timers). Verbatim
 * landed accumulation switch in both. */
function makeIrls(instrumented: boolean): IrlsImpl {
  return (design, rows, _vectors, supports, keys, keySpace, maxIter) => {
    const tIrls0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.fits += 1;
    const p = design.names.length;
    const n = rows.length;
    const eta = new Array<number>(n).fill(0);
    const mu = new Array<number>(n).fill(0);
    const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
    const xtwz: number[] = new Array<number>(p).fill(0);
    const stamp = new Int32Array(keySpace);
    const etaByKey = new Float64Array(keySpace);
    const muByKey = new Float64Array(keySpace);
    let beta = new Array<number>(p).fill(0);
    for (let iter = 0; iter < maxIter; iter++) {
      if (instrumented) {
        STATS.irlsIterations += 1;
        STATS.rowVisits += n;
      }
      const mark = iter + 1;
      for (let i = 0; i < n; i++) {
        const key = keys[i]!;
        if (stamp[key] !== mark) {
          stamp[key] = mark;
          if (instrumented) STATS.sigmoidIrls += 1;
          let value = 0;
          const active = supports[i]!;
          for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
          etaByKey[key] = value;
          muByKey[key] = sigmoid(value);
        }
        eta[i] = etaByKey[key]!;
        mu[i] = muByKey[key]!;
      }
      for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
      xtwz.fill(0);
      for (let i = 0; i < n; i++) {
        const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
        const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
        const wz = w * z;
        const active = supports[i]!;
        switch (active.length) {
          case 5: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            const a3 = active[3]!;
            const a4 = active[4]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
            xtwz[a3] = xtwz[a3]! + wz;
            const r3 = xtwx[a3]!;
            r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
            xtwz[a4] = xtwz[a4]! + wz;
            const r4 = xtwx[a4]!;
            r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
            break;
          }
          case 4: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            const a3 = active[3]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
            xtwz[a3] = xtwz[a3]! + wz;
            const r3 = xtwx[a3]!;
            r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
            break;
          }
          case 3: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
            break;
          }
          case 2: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
            break;
          }
          default: {
            for (let ai = 0; ai < active.length; ai++) {
              const a = active[ai]!;
              xtwz[a] = xtwz[a]! + wz;
              const rowA = xtwx[a]!;
              for (let bi = 0; bi < active.length; bi++) {
                const b = active[bi]!;
                rowA[b] = rowA[b]! + w;
              }
            }
          }
        }
      }
      for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
      let next: number[] | null;
      if (instrumented) {
        const tSolve0 = performance.now();
        next = solvePlain(xtwx, xtwz);
        STATS.solveMs += performance.now() - tSolve0;
        STATS.solveCalls += 1;
      } else {
        next = solvePlain(xtwx, xtwz);
      }
      if (next === null) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      const delta = next.map((value, index) => value - beta[index]!);
      const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
      beta = next;
      if (!beta.every(Number.isFinite)) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      if (l2 < TOL) break;
    }
    if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
    return { coefficients: beta };
  };
}

const irlsInstrumented = makeIrls(true);
const irlsPlain = makeIrls(false);

function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}

function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}

function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}

function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

/* betaQuantileLcb replica (posterior.ts verbatim) — keeps the replica lanes
 * self-contained exactly like R14-C..R23-C. */
function lnGamma(z: number): number {
  const g = 7;
  const c = [
    0.99999999999980993, 676.5203681218851, -1259.1392167224028, 771.32342877765313,
    -176.61502916214059, 12.507343278686905, -0.13857109526572012, 9.9843695780195716e-6,
    1.5056327351493116e-7
  ];
  if (z < 0.5) {
    return Math.log(Math.PI / Math.sin(Math.PI * z)) - lnGamma(1 - z);
  }
  const x = z - 1;
  let a = c[0]!;
  for (let i = 1; i < g + 2; i++) a += c[i]! / (x + i);
  const t = x + g + 0.5;
  return 0.5 * Math.log(2 * Math.PI) + (x + 0.5) * Math.log(t) - t + Math.log(a);
}

function lnBetaFunction(a: number, b: number): number {
  return lnGamma(a) + lnGamma(b) - lnGamma(a + b);
}

function betacf(a: number, b: number, x: number): number {
  const maxIt = 200;
  const eps = 3e-12;
  const qab = a + b;
  const qap = a + 1;
  const qam = a - 1;
  let c = 1;
  let d = 1 - (qab * x) / qap;
  if (Math.abs(d) < 1e-30) d = 1e-30;
  d = 1 / d;
  let h = d;
  for (let m = 1; m <= maxIt; m++) {
    const m2 = 2 * m;
    let aa = (m * (b - m) * x) / ((qam + m2) * (a + m2));
    d = 1 + aa * d;
    if (Math.abs(d) < 1e-30) d = 1e-30;
    c = 1 + aa / c;
    if (Math.abs(c) < 1e-30) c = 1e-30;
    d = 1 / d;
    h *= d * c;
    aa = (-(a + m) * (qab + m) * x) / ((a + m2) * (qap + m2));
    d = 1 + aa * d;
    if (Math.abs(d) < 1e-30) d = 1e-30;
    c = 1 + aa / c;
    if (Math.abs(c) < 1e-30) c = 1e-30;
    d = 1 / d;
    const del = d * c;
    h *= del;
    if (Math.abs(del - 1) < eps) break;
  }
  return h;
}

function regularizedIncompleteBeta(x: number, a: number, b: number, lnBeta: number): number {
  if (x <= 0) return 0;
  if (x >= 1) return 1;
  const prefix = Math.exp(a * Math.log(x) + b * Math.log(1 - x) - lnBeta);
  if (x < (a + 1) / (a + b + 2)) {
    return (prefix * betacf(a, b, x)) / a;
  }
  return 1 - (prefix * betacf(b, a, 1 - x)) / b;
}

function inverseRegularizedIncompleteBeta(p: number, a: number, b: number): number {
  const lnBeta = lnBetaFunction(a, b);
  let lo = 0;
  let hi = 1;
  for (let i = 0; i < 80; i++) {
    const mid = (lo + hi) / 2;
    if (regularizedIncompleteBeta(mid, a, b, lnBeta) > p) hi = mid;
    else lo = mid;
  }
  return (lo + hi) / 2;
}

function betaQuantileLcbReplica(posterior: { alpha: number; beta: number }, p = 0.05): number {
  if (!(p > 0 && p < 1)) return p <= 0 ? 0 : 1;
  if (posterior.alpha <= 0 || posterior.beta <= 0) return 0;
  return inverseRegularizedIncompleteBeta(p, posterior.alpha, posterior.beta);
}

function makeFit(irlsImpl: IrlsImpl, instrumented: boolean) {
  const onProb = (vectors: readonly number[][], coefficients: readonly number[]): number[] => {
    if (!instrumented) return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    const t0 = performance.now();
    const result = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    STATS.onProbMs += performance.now() - t0;
    STATS.onProbSigmoids += vectors.length;
    return result;
  };

  const apc = (
    design: Design,
    rows: readonly Row[],
    vectors: readonly number[][],
    coefficients: readonly number[],
    onProbabilities: readonly number[],
    column: string
  ): number => {
    const t0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.apcCalls += 1;
    let sum = 0;
    const columnIdx = design.columnIndex.get(column);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < rows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        if (instrumented) STATS.apcActivePairs += 1;
        const on = onProbabilities[i]!;
        const offVector = vectors[i]!.slice();
        offVector[columnIdx] = 0;
        const off = sigmoid(dot(coefficients, offVector));
        sum += on - off;
      }
    }
    const result = rows.length === 0 ? 0 : sum / rows.length;
    if (instrumented) STATS.apcMs += performance.now() - t0;
    return result;
  };

  const pushValue = (map: Map<string, number[]>, key: string, value: number): void => {
    if (instrumented) STATS.drawsMapOps += 1;
    const list = map.get(key);
    if (list === undefined) map.set(key, [value]);
    else list.push(value);
  };

  return function fit(
    rows: readonly OfflineRow[],
    options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
  ): AttributionReport {
    const tTotal0 = instrumented ? performance.now() : 0;
    const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
    const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
    const seed = options?.seed ?? SEED_DEFAULT;

    const tBase0 = instrumented ? performance.now() : 0;
    const baseRows: Row[] = rows.map((r) => ({
      scenarioId: r.scenarioId,
      modelVersion: r.modelVersion,
      projectId: r.projectId,
      y: r.y
    }));
    if (instrumented) STATS.baseCopyMs += performance.now() - tBase0;
    const effects: Array<{ name: string; point: number; lcb: number; ucb: number }> = [];

    if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
    }

    const design = buildDesign(baseRows);
    const vectors = baseRows.map((r) => design.build(r));
    const supports = computeSupports(vectors);
    const keys = canonicalRowKeys(baseRows);
    const fit = irlsImpl(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
    if (fit.coefficients === null) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
    }

    const onProbabilities = onProb(vectors, fit.coefficients);
    const pointEffects = new Map<string, number>();
    for (const name of design.names) {
      if (name === "intercept") continue;
      pointEffects.set(
        name,
        apc(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
      );
    }
    for (const ref of design.referenceLevels) {
      if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
        pointEffects.set(`${ref.factor}:${ref.name}`, 0);
      }
    }

    const random = rng(seed);
    const draws = new Map<string, number[]>();
    let successful = 0;
    for (let draw = 0; draw < bootstrapDraws; draw++) {
      const tSample0 = instrumented ? performance.now() : 0;
      const sample: Row[] = [];
      const sampleVectors: number[][] = [];
      const sampleSupports: number[][] = [];
      const sampleKeys: number[] = [];
      for (let i = 0; i < baseRows.length; i++) {
        const index = Math.floor(random() * baseRows.length);
        sample.push(baseRows[index]!);
        sampleVectors.push(vectors[index]!);
        sampleSupports.push(supports[index]!);
        sampleKeys.push(keys[index]!);
      }
      const collapsed = sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1);
      if (instrumented) STATS.samplingMs += performance.now() - tSample0;
      if (collapsed) {
        if (instrumented) STATS.collapsedDraws += 1;
        continue;
      }
      const bootFit = irlsImpl(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
      if (bootFit.coefficients === null) {
        if (instrumented) STATS.singularDraws += 1;
        continue;
      }
      successful += 1;
      if (instrumented) STATS.successfulDraws += 1;
      const sampleOnProbabilities = onProb(sampleVectors, bootFit.coefficients);
      for (const [name] of pointEffects.entries()) {
        const value = apc(
          design,
          sample,
          sampleVectors,
          bootFit.coefficients,
          sampleOnProbabilities,
          name
        );
        pushValue(draws, name, value);
      }
    }

    const tTail0 = instrumented ? performance.now() : 0;
    for (const [name, point] of pointEffects.entries()) {
      const values = draws.get(name) ?? [];
      if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
        return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
      }
      values.sort((a, b) => a - b);
      effects.push({
        name,
        point,
        lcb: percentile(values, 0.025),
        ucb: percentile(values, 0.975)
      });
    }

    const n = baseRows.length;
    const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
    const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
    const muLcb = betaQuantileLcbReplica(muPosterior, 0.05);
    const models = new Set(baseRows.map((r) => r.modelVersion)).size;
    const projects = new Set(baseRows.map((r) => r.projectId)).size;
    const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
    const containsZero = (e: { lcb: number; ucb: number }): boolean =>
      e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
    const interactionsFor = (prefix: string): Array<{ name: string; lcb: number; ucb: number; point: number }> =>
      effects.filter((e) => e.name.startsWith(`w:${prefix}`));

    let diagnosis: AttributionReport["diagnosis"] = "uncertain";
    const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
    if (
      effects.some(
        (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
      )
    ) {
      diagnosis = "model-problem";
    } else if (
      effects.some(
        (e) =>
          e.name.startsWith("v:") &&
          e.lcb < -ATTRIBUTION_EFFECT &&
          effects
            .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
            .every(containsZero)
      )
    ) {
      diagnosis = "project-problem";
    } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
      diagnosis = "interaction-only";
    } else if (scenarioHard) {
      diagnosis = "scenario-hard";
    }

    const report: AttributionReport = {
      estimator: "logit-additive",
      rowsUsed: baseRows.length,
      effects,
      diagnosis,
      reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
      writesActivePointer: false
    };
    if (instrumented) {
      STATS.tailMs += performance.now() - tTail0;
      STATS.totalMs += performance.now() - tTotal0;
    }
    return report;
  };
}

const fitInst = makeFit(irlsInstrumented, true);
const fitPlain = makeFit(irlsPlain, false);

/* ------------------------------ harness ------------------------------ */

let checksPassed = 0;
let failures = 0;

function check(label: string, ok: boolean): void {
  if (ok) checksPassed++;
  else {
    failures++;
    process.stderr.write(`FAIL ${label}\n`);
  }
}

function compareReports(label: string, expected: AttributionReport, actual: AttributionReport): void {
  check(`${label}.estimator`, expected.estimator === actual.estimator);
  check(`${label}.rowsUsed`, expected.rowsUsed === actual.rowsUsed);
  check(`${label}.diagnosis`, expected.diagnosis === actual.diagnosis);
  check(`${label}.reason`, expected.reason === actual.reason);
  check(`${label}.writesActivePointer`, expected.writesActivePointer === actual.writesActivePointer);
  check(`${label}.effects.length`, expected.effects.length === actual.effects.length);
  if (expected.effects.length === actual.effects.length) {
    for (let i = 0; i < expected.effects.length; i++) {
      const e = expected.effects[i]!;
      const g = actual.effects[i]!;
      check(`${label}.effects[${i}].name`, e.name === g.name);
      check(`${label}.effects[${i}].point`, Object.is(e.point, g.point));
      check(`${label}.effects[${i}].lcb`, Object.is(e.lcb, g.lcb));
      check(`${label}.effects[${i}].ucb`, Object.is(e.ucb, g.ucb));
    }
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}

function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRow[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function allUniqueRows(r: () => number, count: number): OfflineRow[] {
  const rows: OfflineRow[] = [];
  for (let i = 0; i < count; i++) {
    rows.push({
      scenarioId: `s${i % 2}`,
      modelVersion: `m${i}`,
      projectId: `p${i}`,
      y: r() < 0.6 ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function batteryCases(): Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];

  for (let i = 0; i < 40; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, {
      scenarios: 2,
      models: 3,
      projects: 3,
      rows: 60,
      passRate: 0.55,
      pipeInModel: true,
    }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  cases.push({
    rows: allUniqueRows(r, 36),
    options: { bootstrap: 40, seed: 23 },
  });
  {
    const oneKey: OfflineRow[] = [];
    for (let i = 0; i < 30; i++) {
      oneKey.push({
        scenarioId: "s0",
        modelVersion: "m0",
        projectId: "p0",
        y: i % 3 === 0 ? 0 : 1,
        occurredAtMs: 1_000 + i,
      });
    }
    cases.push({ rows: oneKey, options: { bootstrap: 40, seed: 31 } });
  }
  cases.push({
    rows: [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1, occurredAtMs: 1_000 },
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 0, occurredAtMs: 1_001 },
    ],
    options: { bootstrap: 30, seed: 5 },
  });
  return cases;
}

const out = (line: string): void => {
  process.stdout.write(`${line}\n`);
};

function perfRows(): OfflineRow[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, {
    scenarios: 4,
    models: 6,
    projects: 8,
    rows: 400,
    passRate: 0.6,
  });
}

const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------- scenario 1: two-lane bitwise fidelity battery --------- */

function scenarioEquivalence(): void {
  const cases = batteryCases();
  for (const [index, testCase] of cases.entries()) {
    const expected = fitLogitAdditive(testCase.rows, testCase.options);
    compareReports(`R26C-inst[${index}]`, expected, fitInst(testCase.rows, testCase.options));
    compareReports(`R26C-plain[${index}]`, expected, fitPlain(testCase.rows, testCase.options));
  }
  out(
    `scenario 1 (two-lane bitwise fidelity, ${cases.length} cases x {instrumented, plain-landed} ` +
      `vs production fitLogitAdditive import)`
  );
}

/* -------- scenario 2: production self-determinism across 3 seeds -------- */

function scenarioDeterminism(): void {
  const r = fixtureRng(0xd19c);
  const rows = randomRows(r, { scenarios: 3, models: 4, projects: 5, rows: 150, passRate: 0.6 });
  for (const seed of [101, 202, 303]) {
    const first = fitLogitAdditive(rows, { bootstrap: 60, seed });
    const second = fitLogitAdditive(rows, { bootstrap: 60, seed });
    compareReports(`R26C-det[seed=${seed}]`, first, second);
  }
  out("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)");
}

/* ------------- scenario 3: profile re-anchor + pool table ------------- */

let APC_POOL_MEDIAN = 0;

interface PoolReading {
  totalMs: number;
  irlsMs: number;
  solveMs: number;
  onProbMs: number;
  apcMs: number;
  samplingMs: number;
  tailMs: number;
  baseCopyMs: number;
  fits: number;
  irlsIterations: number;
  solveCalls: number;
  sigmoidIrls: number;
  rowVisits: number;
  onProbSigmoids: number;
  apcActivePairs: number;
  apcCalls: number;
  drawsMapOps: number;
  collapsedDraws: number;
  singularDraws: number;
  successfulDraws: number;
}

function snapshotStats(): PoolReading {
  return {
    totalMs: STATS.totalMs, irlsMs: STATS.irlsMs, solveMs: STATS.solveMs,
    onProbMs: STATS.onProbMs, apcMs: STATS.apcMs, samplingMs: STATS.samplingMs,
    tailMs: STATS.tailMs, baseCopyMs: STATS.baseCopyMs,
    fits: STATS.fits, irlsIterations: STATS.irlsIterations, solveCalls: STATS.solveCalls,
    sigmoidIrls: STATS.sigmoidIrls, rowVisits: STATS.rowVisits,
    onProbSigmoids: STATS.onProbSigmoids, apcActivePairs: STATS.apcActivePairs,
    apcCalls: STATS.apcCalls, drawsMapOps: STATS.drawsMapOps,
    collapsedDraws: STATS.collapsedDraws, singularDraws: STATS.singularDraws,
    successfulDraws: STATS.successfulDraws,
  };
}

function scenarioProfile(): void {
  const smallRows = randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
  {
    const times: number[] = [];
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(smallRows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape small (3 models x 3 projects, 120 rows): production median ${median(times).toFixed(1)} ms/report (3 reps)`);
  }

  {
    const rows = perfRows();
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape perf (6 models x 8 projects, 400 rows): production median ${median(times).toFixed(1)} ms/report (7 reps)`);
    out(`  perf reps: ${times.map((t) => t.toFixed(1)).join(" ")}`);
  }

  // Instrumented pool table on the perf shape (3 runs; medians reported).
  const rows = perfRows();
  const expected = fitLogitAdditive(rows, PERF_OPTIONS);
  const runs: PoolReading[] = [];
  for (let rep = 0; rep < 3; rep++) {
    STATS.reset();
    const actual = fitInst(rows, PERF_OPTIONS);
    if (rep === 0) {
      compareReports("R26C-perf-fixture", expected, actual);
    }
    runs.push(snapshotStats());
  }
  const med = (key: keyof PoolReading): number => median(runs.map((r) => r[key]));
  const total = med("totalMs");
  const solveMs = med("solveMs");
  const irlsOther = med("irlsMs") - solveMs;
  const rest = total - med("irlsMs") - med("onProbMs") - med("apcMs") - med("samplingMs") - med("tailMs");
  APC_POOL_MEDIAN = med("apcMs");
  out(`instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):`);
  out(`  total                 ${total.toFixed(1)} ms`);
  out(`  solveSymmetric        ${solveMs.toFixed(1)} ms  (${(100 * solveMs / total).toFixed(1)}%)  [${med("solveCalls")} calls]`);
  out(`  irls minus solve      ${irlsOther.toFixed(1)} ms  (${(100 * irlsOther / total).toFixed(1)}%)  [${med("irlsIterations")} iterations, ${med("rowVisits")} row visits, ${med("sigmoidIrls")} irls sigmoids]`);
  out(`  onProbabilitiesFor    ${med("onProbMs").toFixed(1)} ms  (${(100 * med("onProbMs") / total).toFixed(1)}%)  [${med("onProbSigmoids")} sigmoid+dot]`);
  out(`  APC (all sweeps)      ${med("apcMs").toFixed(1)} ms  (${(100 * med("apcMs") / total).toFixed(1)}%)  [${med("apcActivePairs")} active pairs, ${med("apcCalls")} calls]`);
  out(`  bootstrap sampling    ${med("samplingMs").toFixed(1)} ms  (${(100 * med("samplingMs") / total).toFixed(1)}%)`);
  out(`  sort/percentile tail  ${med("tailMs").toFixed(1)} ms  (${(100 * med("tailMs") / total).toFixed(1)}%)  [${med("drawsMapOps")} draws-map ops]`);
  out(`  base-row projection   ${med("baseCopyMs").toFixed(3)} ms  (once/report)`);
  out(`  outside pools (build/supports/keys/base copies) ${rest.toFixed(1)} ms  (${(100 * rest / total).toFixed(1)}%)`);
  out(`  fits ${med("fits")}  draws successful=${med("successfulDraws")} collapsed=${med("collapsedDraws")} singular=${med("singularDraws")}`);
}

/* --------- scenario 4: PRICEX monetary-cost (price-geometry) dose response --- *
 * NEW completeness axis (C-first; the first never-dosed coordinate of the
 * C-slice public input surface outside the observation row closed by
 * R25-C: strings/y/volume/order/occupancy/seed/time). Face A closes by
 * type-level absence (no money field exists on the attribution entries);
 * face A' witnesses the propensity face's present-but-never-read costUsd
 * field bitwise; face B prices the R1/shadow face (the slice's only live
 * price consumers: costOf / cheaperEstimate / tierIndex + selectedCost).
 * No identifier string moves; no count moves; no y/outcome flips; no
 * seed, order, occupancy, time, options, config, or nowMs movement; the
 * catalog id set and cardinality M=10 are asserted frozen at every dose. */

const NOW_MS = 1_756_000_000_000; // frozen 2026 epoch anchor

function scenarioPricexFaceA(): void {
  out(`probe PRICEX (monetary-cost coordinate / price-geometry dose response; NEW axis, not a lane):`);
  out(
    `  face A (attribution face — the only +-35 ms-capable pools): the money coordinate is ABSENT BY TYPE ` +
      `(OfflineRow carries no cost field; zero cost/usd/price identifiers in offline-logit.ts / ` +
      `offline-prob-add.ts / lin-alg.ts / posterior.ts) — no in-contract dose exists; closes by construction`
  );
  out(
    `  face A' (propensity face): PropensityLogEntry.costUsd is declared on the public input type and ` +
      `never read by any slice function — five value doses, all outputs must be bitwise identical:`
  );
  const N_LOGS = 200;
  const mkLogs = (cost: (i: number) => number): PropensityLogEntry[] =>
    Array.from({ length: N_LOGS }, (_, i) => ({
      episodeHash: `ep-${i}`,
      modelId: `model-${i % 10}`,
      behaviorProbability: i % 10 === 0 ? 1 : 0,
      targetProbability: i % 10 === 0 ? 1 : 0,
      observedUtility: i % 2 === 0 ? 1 : 0,
      costUsd: cost(i),
      guardrailBreach: false,
    }));
  const COST_DOSES_A: ReadonlyArray<{ name: string; cost: (i: number) => number }> = [
    { name: "anchor", cost: (i) => 0.01 * (i + 1) },
    { name: "zero", cost: () => 0 },
    { name: "huge", cost: () => 1e15 },
    { name: "subn", cost: () => 5e-324 },
    { name: "frac", cost: (i) => (i + 0.5) * 1e-3 },
  ];
  const anchorLogs = mkLogs(COST_DOSES_A[0]!.cost);
  const anchorDiag = computeOverlapDiagnostics(anchorLogs);
  const anchorReport = validateCounterfactualReport({
    reportVersion: 1,
    candidate: "cand",
    baseline: "base",
    claims: ["仿真证据"],
    diagnostics: anchorDiag,
  });
  const anchorFab = isFabricatedPositiveSupport(anchorLogs);
  for (const dose of COST_DOSES_A) {
    const logs = mkLogs(dose.cost);
    check(
      `R26C-pricexA'[${dose.name}].stamps-finite-nonnegative`,
      logs.every((l) => Number.isFinite(l.costUsd) && l.costUsd >= 0)
    );
    const diag = computeOverlapDiagnostics(logs);
    check(
      `R26C-pricexA'[${dose.name}].diagnostics-bitwise`,
      diag.totalActions === anchorDiag.totalActions &&
        diag.eligibleActions === anchorDiag.eligibleActions &&
        Object.is(diag.minPropensity, anchorDiag.minPropensity) &&
        Object.is(diag.maxPropensity, anchorDiag.maxPropensity) &&
        diag.supportOk === anchorDiag.supportOk &&
        Object.is(diag.effectiveSampleSize, anchorDiag.effectiveSampleSize) &&
        diag.estimatorId === anchorDiag.estimatorId &&
        diag.invalidReason === anchorDiag.invalidReason
    );
    const report = validateCounterfactualReport({
      reportVersion: 1,
      candidate: "cand",
      baseline: "base",
      claims: ["仿真证据"],
      diagnostics: diag,
    });
    check(
      `R26C-pricexA'[${dose.name}].validation-bitwise`,
      report.valid === anchorReport.valid &&
        report.status === anchorReport.status &&
        report.reasons.join("\u0000") === anchorReport.reasons.join("\u0000")
    );
    check(`R26C-pricexA'[${dose.name}].fabricated-support-bitwise`, isFabricatedPositiveSupport(logs) === anchorFab);
  }
  // wall: whole propensity pool at N=200, per-dose medians (mus level; the
  // pool is a constant-pass sub-ms domain — S1-C-8/X3-5 closed it long ago).
  const wallLines: string[] = [];
  for (const dose of COST_DOSES_A) {
    const logs = mkLogs(dose.cost);
    const times: number[] = [];
    for (let rep = 0; rep < 5; rep++) {
      const t0 = performance.now();
      for (let k = 0; k < 200; k++) computeOverlapDiagnostics(logs);
      times.push((performance.now() - t0) / 200);
    }
    wallLines.push(`${dose.name}:${(median(times) * 1000).toFixed(1)}us`);
  }
  out(`    computeOverlapDiagnostics wall (N=200 logs, per call): ${wallLines.join(" ")}`);
}

/* ------------------------------- face B -------------------------------- *
 * Instrumented verbatim replicas of outcomes.ts / posterior.ts / r1.ts at
 * the 183df9b-landed state. The replica lane's ONLY additions are BSTAT
 * counter increments and performance.now() reads at call boundaries; no
 * float operation, operand, or ordering differs. The replica lane is
 * validated bitwise against the production prepareR1Observations /
 * estimateForKey / routeR1 imports AT EVERY DOSE before its numbers are
 * used. */

const BSTAT = {
  powCalls: 0,
  betacfCalls: 0,
  betacfIters: 0,
  ribCalls: 0,
  lnGammaCalls: 0,
  quantileCalls: 0,
  estimateComputes: 0,
  cacheHits: 0,
  prepareMs: 0,
  updMs: 0,
  lcbMs: 0,
  reset(): void {
    this.powCalls = 0; this.betacfCalls = 0; this.betacfIters = 0; this.ribCalls = 0;
    this.lnGammaCalls = 0; this.quantileCalls = 0; this.estimateComputes = 0;
    this.cacheHits = 0; this.prepareMs = 0; this.updMs = 0; this.lcbMs = 0;
  },
};

function outcomeKeyB(parts: {
  readonly taskFamily: string;
  readonly role: string;
  readonly modelVersion: string;
  readonly featureVersion: string;
}): string {
  return `${parts.taskFamily}|${parts.role}|${parts.modelVersion}|${parts.featureVersion}`;
}

function isInformativeOutcomeB(observation: OutcomeObservation): boolean {
  return observation.outcome === "PASS" || observation.outcome === "FAIL";
}

function observationsForR1B(observations: readonly OutcomeObservation[]): OutcomeObservation[] {
  return observations.filter((row) => {
    if (row.criterion !== "taskSuccess" || !isInformativeOutcomeB(row)) return false;
    if (row.source !== "deterministic-check") return false;
    if (row.outcome === "FAIL" && row.failureClass !== "model") return false;
    return true;
  });
}

function updatePosteriorB(
  config: PosteriorConfig,
  observations: readonly OutcomeObservation[],
  nowMs: number
): { alpha: number; beta: number } {
  const t0 = performance.now();
  let alpha = config.priorAlpha;
  let beta = config.priorBeta;
  for (const observation of observations) {
    if (!isInformativeOutcomeB(observation)) continue;
    const ageMs = Math.max(0, nowMs - observation.occurredAtMs);
    BSTAT.powCalls += 1;
    const weight = Math.pow(2, -ageMs / config.halfLifeMs);
    if (observation.outcome === "PASS") alpha += weight;
    else beta += weight;
  }
  BSTAT.updMs += performance.now() - t0;
  return { alpha, beta };
}

function posteriorMeanB(posterior: { alpha: number; beta: number }): number {
  const total = posterior.alpha + posterior.beta;
  return total > 0 ? posterior.alpha / total : 0;
}

function posteriorVarianceB(posterior: { alpha: number; beta: number }): number {
  const total = posterior.alpha + posterior.beta;
  if (total <= 0) return 0;
  return (posterior.alpha * posterior.beta) / (total * total * (total + 1));
}

function lnGammaB(z: number): number {
  BSTAT.lnGammaCalls += 1;
  const g = 7;
  const c = [
    0.99999999999980993, 676.5203681218851, -1259.1392167224028, 771.32342877765313,
    -176.61502916214059, 12.507343278686905, -0.13857109526572012, 9.9843695780195716e-6,
    1.5056327351493116e-7
  ];
  if (z < 0.5) {
    return Math.log(Math.PI / Math.sin(Math.PI * z)) - lnGammaB(1 - z);
  }
  const x = z - 1;
  let a = c[0]!;
  for (let i = 1; i < g + 2; i++) a += c[i]! / (x + i);
  const t = x + g + 0.5;
  return 0.5 * Math.log(2 * Math.PI) + (x + 0.5) * Math.log(t) - t + Math.log(a);
}

function lnBetaFunctionB(a: number, b: number): number {
  return lnGammaB(a) + lnGammaB(b) - lnGammaB(a + b);
}

function betacfB(a: number, b: number, x: number): number {
  BSTAT.betacfCalls += 1;
  const maxIt = 200;
  const eps = 3e-12;
  const qab = a + b;
  const qap = a + 1;
  const qam = a - 1;
  let c = 1;
  let d = 1 - (qab * x) / qap;
  if (Math.abs(d) < 1e-30) d = 1e-30;
  d = 1 / d;
  let h = d;
  for (let m = 1; m <= maxIt; m++) {
    BSTAT.betacfIters += 1;
    const m2 = 2 * m;
    let aa = (m * (b - m) * x) / ((qam + m2) * (a + m2));
    d = 1 + aa * d;
    if (Math.abs(d) < 1e-30) d = 1e-30;
    c = 1 + aa / c;
    if (Math.abs(c) < 1e-30) c = 1e-30;
    d = 1 / d;
    h *= d * c;
    aa = (-(a + m) * (qab + m) * x) / ((a + m2) * (qap + m2));
    d = 1 + aa * d;
    if (Math.abs(d) < 1e-30) d = 1e-30;
    c = 1 + aa / c;
    if (Math.abs(c) < 1e-30) c = 1e-30;
    d = 1 / d;
    const del = d * c;
    h *= del;
    if (Math.abs(del - 1) < eps) break;
  }
  return h;
}

function ribB(x: number, a: number, b: number, lnBeta: number): number {
  BSTAT.ribCalls += 1;
  if (x <= 0) return 0;
  if (x >= 1) return 1;
  const prefix = Math.exp(a * Math.log(x) + b * Math.log(1 - x) - lnBeta);
  if (x < (a + 1) / (a + b + 2)) {
    return (prefix * betacfB(a, b, x)) / a;
  }
  return 1 - (prefix * betacfB(b, a, 1 - x)) / b;
}

function invB(p: number, a: number, b: number): number {
  const lnBeta = lnBetaFunctionB(a, b);
  let lo = 0;
  let hi = 1;
  for (let i = 0; i < 80; i++) {
    const mid = (lo + hi) / 2;
    if (ribB(mid, a, b, lnBeta) > p) hi = mid;
    else lo = mid;
  }
  return (lo + hi) / 2;
}

function betaQuantileLcbB(posterior: { alpha: number; beta: number }, p = 0.05): number {
  BSTAT.quantileCalls += 1;
  if (!(p > 0 && p < 1)) return p <= 0 ? 0 : 1;
  if (posterior.alpha <= 0 || posterior.beta <= 0) return 0;
  return invB(p, posterior.alpha, posterior.beta);
}

function lowerConfidenceBoundB(
  config: PosteriorConfig,
  posterior: { alpha: number; beta: number },
  kind: "normal" | "beta-quantile" = config.lcbKind
): number {
  const t0 = performance.now();
  let result: number;
  if (kind === "beta-quantile") {
    result = betaQuantileLcbB(posterior, 0.05); // oneSidedTail(config.lcbZ) === 0.05 in production
  } else {
    const mean = posteriorMeanB(posterior);
    const sd = Math.sqrt(posteriorVarianceB(posterior));
    result = Math.max(0, mean - config.lcbZ * sd);
  }
  BSTAT.lcbMs += performance.now() - t0;
  return result;
}

function weightedSampleSizeB(config: PosteriorConfig, posterior: { alpha: number; beta: number }): number {
  return posterior.alpha + posterior.beta - config.priorAlpha - config.priorBeta;
}

function isWellSampledB(config: PosteriorConfig, posterior: { alpha: number; beta: number }): boolean {
  return weightedSampleSizeB(config, posterior) >= config.minSamples;
}

function groupObservationsByKeyB(
  observations: readonly OutcomeObservation[]
): ReadonlyMap<string, readonly OutcomeObservation[]> {
  const groups = new Map<string, OutcomeObservation[]>();
  for (const observation of observations) {
    const key = outcomeKeyB(observation);
    const group = groups.get(key);
    if (group === undefined) {
      groups.set(key, [observation]);
    } else {
      group.push(observation);
    }
  }
  return groups;
}

interface R1KeyEstimateB {
  readonly key: string;
  readonly alpha: number;
  readonly beta: number;
  readonly mean: number;
  readonly lcb: number;
  readonly samples: number;
  readonly wellSampled: boolean;
}

interface PreparedB {
  readonly byKey: ReadonlyMap<string, readonly OutcomeObservation[]>;
  readonly estimateCache: Map<string, { readonly fingerprint: string; readonly estimate: R1KeyEstimateB }>;
}

function prepareR1ObservationsB(observations: readonly OutcomeObservation[]): PreparedB {
  const t0 = performance.now();
  const prepared: PreparedB = {
    byKey: groupObservationsByKeyB(observationsForR1B(observations)),
    estimateCache: new Map(),
  };
  BSTAT.prepareMs += performance.now() - t0;
  return prepared;
}

function estimateFingerprintB(config: PosteriorConfig, nowMs: number): string {
  return `${nowMs}|${config.priorAlpha}|${config.priorBeta}|${config.halfLifeMs}|${config.minSamples}|${config.lcbZ}|${config.lcbKind}`;
}

function estimateForKeyB(
  prepared: PreparedB,
  config: PosteriorConfig,
  nowMs: number,
  parts: { readonly taskFamily: string; readonly role: string; readonly modelVersion: string; readonly featureVersion: string }
): R1KeyEstimateB {
  const key = outcomeKeyB(parts);
  const fingerprint = estimateFingerprintB(config, nowMs);
  const hit = prepared.estimateCache.get(key);
  if (hit !== undefined && hit.fingerprint === fingerprint) {
    BSTAT.cacheHits += 1;
    return hit.estimate;
  }
  BSTAT.estimateComputes += 1;
  const posterior = updatePosteriorB(config, prepared.byKey.get(key) ?? [], nowMs);
  const estimate: R1KeyEstimateB = {
    key,
    alpha: posterior.alpha,
    beta: posterior.beta,
    mean: posteriorMeanB(posterior),
    lcb: lowerConfidenceBoundB(config, posterior),
    samples: weightedSampleSizeB(config, posterior),
    wellSampled: isWellSampledB(config, posterior),
  };
  prepared.estimateCache.set(key, { fingerprint, estimate });
  return estimate;
}

const DEFAULT_POSTERIOR_CONFIG_B: PosteriorConfig = {
  priorAlpha: 1,
  priorBeta: 1,
  halfLifeMs: 24 * 60 * 60 * 1000,
  minSamples: 5,
  lcbZ: 1.96,
  lcbKind: "beta-quantile",
};
const DEFAULT_QUALITY_FLOOR_B = 0.55;
const DEFAULT_HYSTERESIS_MARGIN_B = 0.02;

interface R1EstimateB extends R1KeyEstimateB {
  readonly modelId: string;
}

interface R1DecisionB {
  readonly selection: string | undefined;
  readonly estimates: readonly R1EstimateB[];
  readonly reason: string;
  readonly exploratory: false;
  readonly fallback: boolean;
}

interface R1InputB {
  readonly r0: R0Decision;
  readonly role: string;
  readonly featureVersion: string;
  readonly models: readonly ModelDescriptor[];
  readonly observations: PreparedB;
  readonly config?: Partial<PosteriorConfig> | undefined;
  readonly nowMs: number;
  readonly qualityFloor?: number | undefined;
  readonly hysteresisMargin?: number | undefined;
  readonly previousModelId?: string | undefined;
}

const PSTAT = {
  comparisons: 0,
  costOfCalls: 0,
  costFindHops: 0,
  tieBranches: 0,
  tierIndexCalls: 0,
  tierIndexHops: 0,
  rightWinsCost: 0,
  rightWinsTie: 0,
  reduceMs: 0,
  reset(): void {
    this.comparisons = 0; this.costOfCalls = 0; this.costFindHops = 0;
    this.tieBranches = 0; this.tierIndexCalls = 0; this.tierIndexHops = 0;
    this.rightWinsCost = 0; this.rightWinsTie = 0; this.reduceMs = 0;
  },
};

function costOfB(r0: R0Decision, modelId: string): number {
  PSTAT.costOfCalls += 1;
  return (
    r0.candidates.find((row) => {
      PSTAT.costFindHops += 1;
      return row.modelId === modelId;
    })?.estimatedCostUsd ?? Number.POSITIVE_INFINITY
  );
}

function tierIndexB(r0: R0Decision, modelId: string): number {
  PSTAT.tierIndexCalls += 1;
  if (r0.selection === modelId) return 0;
  const fallback = r0.fallbacks.indexOf(modelId);
  PSTAT.tierIndexHops += fallback === -1 ? r0.fallbacks.length : fallback + 1;
  return fallback === -1 ? Number.MAX_SAFE_INTEGER : fallback + 1;
}

function cheaperEstimateB(left: R1EstimateB, right: R1EstimateB, r0: R0Decision): R1EstimateB {
  PSTAT.comparisons += 1;
  const leftCost = costOfB(r0, left.modelId);
  const rightCost = costOfB(r0, right.modelId);
  if (rightCost < leftCost) {
    PSTAT.rightWinsCost += 1;
    return right;
  }
  if (rightCost > leftCost) return left;
  PSTAT.tieBranches += 1;
  const leftIndex = tierIndexB(r0, left.modelId);
  const rightIndex = tierIndexB(r0, right.modelId);
  if (rightIndex < leftIndex) {
    PSTAT.rightWinsTie += 1;
    return right;
  }
  return left;
}

function routeR1B(input: R1InputB): R1DecisionB {
  const config: PosteriorConfig = { ...DEFAULT_POSTERIOR_CONFIG_B, ...input.config };
  const qualityFloor = input.qualityFloor ?? DEFAULT_QUALITY_FLOOR_B;
  const hysteresisMargin = input.hysteresisMargin ?? DEFAULT_HYSTERESIS_MARGIN_B;
  const request = input.r0.request;
  void request;

  if (input.r0.selection === undefined) {
    return {
      selection: undefined,
      estimates: [],
      reason: "no eligible model; R0 refused",
      exploratory: false,
      fallback: false,
    };
  }

  const prepared = input.observations;
  const tierIds = [input.r0.selection, ...input.r0.fallbacks];
  const estimates: R1EstimateB[] = [];
  const modelsById = new Map(input.models.map((m) => [m.modelId, m]));

  for (const modelId of tierIds) {
    const model = modelsById.get(modelId);
    const estimate = estimateForKeyB(prepared, config, input.nowMs, {
      taskFamily: input.r0.request.taskFamily,
      role: input.role,
      modelVersion: model?.version ?? modelId,
      featureVersion: input.featureVersion,
    });
    estimates.push({ modelId, ...estimate });
  }

  const sampled = estimates.filter((e) => e.wellSampled);
  if (sampled.length === 0) {
    return {
      selection: input.r0.selection,
      estimates,
      reason: "no well-sampled estimate; conservative R0 baseline selected",
      exploratory: false,
      fallback: true,
    };
  }

  const aboveFloor = sampled.filter((e) => e.lcb >= qualityFloor);
  if (aboveFloor.length === 0) {
    return {
      selection: input.r0.selection,
      estimates,
      reason: "no well-sampled model above quality floor; conservative R0 baseline selected",
      exploratory: false,
      fallback: true,
    };
  }

  const tReduce = performance.now();
  const cheapest = aboveFloor.reduce((acc, current) => cheaperEstimateB(acc, current, input.r0));
  PSTAT.reduceMs += performance.now() - tReduce;
  const previous = aboveFloor.find((e) => e.modelId === input.previousModelId);
  if (
    previous !== undefined &&
    previous.modelId !== cheapest.modelId &&
    cheapest.lcb < qualityFloor + hysteresisMargin
  ) {
    return {
      selection: previous.modelId,
      estimates,
      reason: `r1-hysteresis: previous selection still above floor (${previous.lcb.toFixed(4)})`,
      exploratory: false,
      fallback: false,
    };
  }

  return {
    selection: cheapest.modelId,
    estimates,
    reason: `cheapest above quality floor ${qualityFloor} (LCB ${cheapest.lcb.toFixed(4)})`,
    exploratory: false,
    fallback: false,
  };
}

/* --------------------------- face B fixtures ---------------------------- */

const B_FAMILIES = 8;
const B_ROLES = 2;
const B_MODELS = 10;
const B_OBS_PER_KEY = 25;
const B_EPISODES = 480;
const B_FEATURE = "fv-1";
const B_KEYS = B_FAMILIES * B_ROLES * B_MODELS; // 160

const R0_CONFIG_B: R0Config = { confidenceGate: 0.9, cascade: false, policyVersion: "r0-frozen" };

interface PriceDose {
  readonly name: string;
  readonly cost: (m: number) => number;
}

/** Only the numeric price vector moves; ids, versions, eligibility, and M=10
 * are frozen. asc is the historical pinned geometry (0.001*(idx+1), the
 * R25-C face-B fixture value verbatim). */
const PRICE_DOSES: readonly PriceDose[] = [
  { name: "asc", cost: (m) => 0.001 * (m + 1) },
  { name: "desc", cost: (m) => 0.001 * (B_MODELS - m) },
  { name: "alltie", cost: () => 0.004 },
  { name: "pairtie", cost: (m) => 0.001 * (Math.floor(m / 2) + 1) },
  { name: "int", cost: (m) => m + 1 },
  { name: "micro", cost: (m) => (m + 1) * 1e-12 },
  { name: "spread", cost: (m) => Math.pow(10, m - 5) },
  { name: "subn", cost: (m) => (m + 1) * 5e-324 },
];

/** contextNeeded=1e6 and outputCostPerMTok=0 make the production
 * estimateCostUsd equal dose.cost(m) bitwise: (1e6/1e6)*c + (0/1e6)*0 = c. */
function requestForFamilyPrice(taskFamily: string): RouteRequest {
  return {
    taskFamily,
    privacyRequired: "cloud-general",
    requiredCapabilities: [],
    contextNeeded: 1_000_000,
    outputNeeded: 0,
    budgetUsd: 1_000_000,
    deadlineMs: 60_000,
    highRisk: false,
  };
}

function modelsPrice(dose: PriceDose): ModelDescriptor[] {
  return Array.from({ length: B_MODELS }, (_, m) => ({
    modelId: `model-${m}`,
    providerId: "prov-fixed",
    version: `mv-${m}`,
    capabilities: [],
    providerPolicy: "approved" as const,
    inputCostPerMTok: dose.cost(m),
    outputCostPerMTok: 0,
    latencyMsPer1K: 100,
  }));
}

/** Per-dose R0 decisions come from the PRODUCTION routeR0 import — the input
 * to the slice is in-contract by construction. Built outside the timed
 * window (R0/policy are out-of-slice). */
function r0ForFamilyPrice(dose: PriceDose, taskFamily: string): R0Decision {
  return routeR0(R0_CONFIG_B, modelsPrice(dose), requestForFamilyPrice(taskFamily));
}

/** ONE frozen observation array shared by every dose (object identity, not
 * just bitwise): 160 keys x 25 obs (20 PASS + 5 FAIL[model] at j%5==0, frozen
 * order), all fresh (occurredAtMs = nowMs), so every key is wellSampled with
 * LCB 0.637405 > floor+margin and the price-consuming reduce sees maximal
 * traffic (9 comparisons/episode). */
function buildObservationsFresh(): OutcomeObservation[] {
  const observations: OutcomeObservation[] = [];
  for (let f = 0; f < B_FAMILIES; f++) {
    for (let role = 0; role < B_ROLES; role++) {
      for (let m = 0; m < B_MODELS; m++) {
        for (let j = 0; j < B_OBS_PER_KEY; j++) {
          const fail = j % 5 === 0;
          observations.push({
            taskFamily: `family-${f}`,
            role: `role-${role}`,
            modelId: `model-${m}`,
            modelVersion: `mv-${m}`,
            featureVersion: B_FEATURE,
            criterion: "taskSuccess",
            outcome: fail ? "FAIL" : "PASS",
            occurredAtMs: NOW_MS,
            source: "deterministic-check",
            ...(fail ? { failureClass: "model" as const } : {}),
          });
        }
      }
    }
  }
  return observations;
}

interface EpisodeSpecB {
  readonly family: string;
  readonly role: string;
}

function episodesB(): EpisodeSpecB[] {
  return Array.from({ length: B_EPISODES }, (_, e) => ({
    family: `family-${e % B_FAMILIES}`,
    role: `role-${Math.floor(e / B_FAMILIES) % B_ROLES}`,
  }));
}

function runWorkloadProd(
  observations: readonly OutcomeObservation[],
  models: readonly ModelDescriptor[],
  r0ByFamily: ReadonlyMap<string, R0Decision>,
  episodes: readonly EpisodeSpecB[]
): { decisions: R1Decision[]; ms: number } {
  const t0 = performance.now();
  const prepared = prepareR1Observations(observations);
  const decisions: R1Decision[] = [];
  for (const ep of episodes) {
    decisions.push(
      routeR1({
        r0: r0ByFamily.get(ep.family)!,
        role: ep.role,
        featureVersion: B_FEATURE,
        models,
        observations: prepared,
        nowMs: NOW_MS,
      })
    );
  }
  return { decisions, ms: performance.now() - t0 };
}

function runWorkloadReplica(
  observations: readonly OutcomeObservation[],
  models: readonly ModelDescriptor[],
  r0ByFamily: ReadonlyMap<string, R0Decision>,
  episodes: readonly EpisodeSpecB[]
): { decisions: R1DecisionB[]; ms: number } {
  const t0 = performance.now();
  const prepared = prepareR1ObservationsB(observations);
  const decisions: R1DecisionB[] = [];
  for (const ep of episodes) {
    decisions.push(
      routeR1B({
        r0: r0ByFamily.get(ep.family)!,
        role: ep.role,
        featureVersion: B_FEATURE,
        models,
        observations: prepared,
        nowMs: NOW_MS,
      })
    );
  }
  return { decisions, ms: performance.now() - t0 };
}

function sameDecisionLists(
  a: readonly (R1Decision | R1DecisionB)[],
  b: readonly (R1Decision | R1DecisionB)[]
): boolean {
  if (a.length !== b.length) return false;
  for (let e = 0; e < a.length; e++) {
    const x = a[e]!;
    const y = b[e]!;
    if (
      x.selection !== y.selection ||
      x.reason !== y.reason ||
      x.fallback !== y.fallback ||
      x.exploratory !== y.exploratory ||
      x.estimates.length !== y.estimates.length
    ) {
      return false;
    }
    for (let k = 0; k < x.estimates.length; k++) {
      const p = x.estimates[k]!;
      const q = y.estimates[k]!;
      if (
        p.modelId !== q.modelId ||
        p.key !== q.key ||
        !Object.is(p.alpha, q.alpha) ||
        !Object.is(p.beta, q.beta) ||
        !Object.is(p.mean, q.mean) ||
        !Object.is(p.lcb, q.lcb) ||
        !Object.is(p.samples, q.samples) ||
        p.wellSampled !== q.wellSampled
      ) {
        return false;
      }
    }
  }
  return true;
}

interface BCounters {
  powCalls: number;
  betacfCalls: number;
  betacfIters: number;
  ribCalls: number;
  lnGammaCalls: number;
  quantileCalls: number;
  estimateComputes: number;
  cacheHits: number;
  prepareMs: number;
  updMs: number;
  lcbMs: number;
}

function snapshotB(): BCounters {
  return {
    powCalls: BSTAT.powCalls, betacfCalls: BSTAT.betacfCalls, betacfIters: BSTAT.betacfIters,
    ribCalls: BSTAT.ribCalls, lnGammaCalls: BSTAT.lnGammaCalls, quantileCalls: BSTAT.quantileCalls,
    estimateComputes: BSTAT.estimateComputes, cacheHits: BSTAT.cacheHits,
    prepareMs: BSTAT.prepareMs, updMs: BSTAT.updMs, lcbMs: BSTAT.lcbMs,
  };
}

function scenarioPricexFaceB(): void {
  out(`  face B (R1/shadow face — the slice's ONLY live price consumers: costOf/cheaperEstimate/tierIndex + selectedCost):`);
  out(
    `    frozen dims: keys=${B_KEYS} (${B_FAMILIES} fam x ${B_ROLES} roles x ${B_MODELS} model versions), ` +
      `obs/key=${B_OBS_PER_KEY} (20 PASS + 5 FAIL[model], frozen order, ALL fresh), episodes=${B_EPISODES}, M=${B_MODELS}, ` +
      `nowMs=${NOW_MS}, config=DEFAULT; only the estimatedCostUsd vector moves (per-dose R0 from the production routeR0 import)`
  );
  const families = Array.from({ length: B_FAMILIES }, (_, f) => `family-${f}`);
  const observations = buildObservationsFresh(); // ONE object for every dose
  const episodes = episodesB();
  const modelsByDose = new Map(PRICE_DOSES.map((d) => [d.name, modelsPrice(d)] as const));
  const r0ByDoseFamily = new Map(
    PRICE_DOSES.map((d) => [d.name, new Map(families.map((f) => [f, r0ForFamilyPrice(d, f)] as const))] as const)
  );

  // Structural assertions: M frozen, ids frozen, all eligible, candidates
  // cost-ascending with modelId tie-break (the R0 output contract), tier
  // order == candidates order.
  const idSetAnchor = [...modelsByDose.get("asc")!.map((m) => m.modelId)].sort().join(",");
  for (const dose of PRICE_DOSES) {
    const r0 = r0ByDoseFamily.get(dose.name)!.get("family-0")!;
    check(`R26C-pricexB[${dose.name}].M-frozen==${B_MODELS}`, r0.candidates.length === B_MODELS);
    check(
      `R26C-pricexB[${dose.name}].id-set-frozen`,
      [...r0.candidates.map((c) => c.modelId)].sort().join(",") === idSetAnchor
    );
    check(`R26C-pricexB[${dose.name}].all-eligible`, r0.candidates.every((c) => c.eligible));
    let sorted = true;
    for (let i = 1; i < r0.candidates.length; i++) {
      const a = r0.candidates[i - 1]!;
      const b = r0.candidates[i]!;
      if (
        a.estimatedCostUsd > b.estimatedCostUsd ||
        (a.estimatedCostUsd === b.estimatedCostUsd && a.modelId > b.modelId)
      ) {
        sorted = false;
      }
    }
    check(`R26C-pricexB[${dose.name}].candidates-cost-ascending (R0 sort contract)`, sorted);
    check(
      `R26C-pricexB[${dose.name}].tier-order==candidates-order`,
      r0.selection === r0.candidates[0]!.modelId &&
        r0.fallbacks.length === B_MODELS - 1 &&
        r0.fallbacks.every((id, i) => id === r0.candidates[i + 1]!.modelId)
    );
    check(
      `R26C-pricexB[${dose.name}].cost-values-exact`,
      r0.candidates.every((c) => {
        const m = Number(c.modelId.slice("model-".length));
        return Object.is(c.estimatedCostUsd, dose.cost(m));
      })
    );
  }

  // Steady-state warmup over every dose (COLDX stays closed; first-touch
  // effects do not enter readings).
  for (const dose of PRICE_DOSES) {
    const models = modelsByDose.get(dose.name)!;
    const r0ByFamily = r0ByDoseFamily.get(dose.name)!;
    runWorkloadProd(observations, models, r0ByFamily, episodes);
    runWorkloadReplica(observations, models, r0ByFamily, episodes);
  }
  BSTAT.reset();
  PSTAT.reset();

  const doseLines: string[] = [];
  const doseTotals: Array<{ name: string; prodMs: number; reduceMs: number }> = [];
  let anchorKeyEstimates: R1KeyEstimateB[] | undefined;
  let anchorEpisodeSigs: string[] | undefined;
  let anchorBetacfIters: number | undefined;

  const estimateSig = (d: R1Decision | R1DecisionB): string =>
    [...d.estimates]
      .sort((a, b) => (a.modelId < b.modelId ? -1 : a.modelId > b.modelId ? 1 : 0))
      .map(
        (e) =>
          `${e.modelId}\u0000${e.key}\u0000${e.alpha}\u0000${e.beta}\u0000${e.mean}\u0000${e.lcb}\u0000${e.samples}\u0000${e.wellSampled}`
      )
      .join("\u0001");

  for (const dose of PRICE_DOSES) {
    const models = modelsByDose.get(dose.name)!;
    const r0ByFamily = r0ByDoseFamily.get(dose.name)!;

    // (a) bitwise fidelity: replica lane vs production lane AT THIS DOSE.
    const prodOnce = runWorkloadProd(observations, models, r0ByFamily, episodes);
    const replicaOnce = runWorkloadReplica(observations, models, r0ByFamily, episodes);
    check(`R26C-pricexB[${dose.name}].replica-vs-production-bitwise`, sameDecisionLists(prodOnce.decisions, replicaOnce.decisions));

    // (b) per-key estimate fidelity + CROSS-DOSE invariance (price cannot
    // reach the posterior: per-key estimates must equal the anchor dose
    // bitwise, all 160 keys, all fields).
    const preparedProd = prepareR1Observations(observations);
    const preparedRepl = prepareR1ObservationsB(observations);
    const keyEstimates: R1KeyEstimateB[] = [];
    let keyFieldsSame = true;
    let wellSampledCount = 0;
    for (let f = 0; f < B_FAMILIES; f++) {
      for (let role = 0; role < B_ROLES; role++) {
        for (let m = 0; m < B_MODELS; m++) {
          const parts = {
            taskFamily: `family-${f}`,
            role: `role-${role}`,
            modelVersion: `mv-${m}`,
            featureVersion: B_FEATURE,
          };
          const pe = estimateForKey(preparedProd, DEFAULT_POSTERIOR_CONFIG, NOW_MS, parts);
          const re = estimateForKeyB(preparedRepl, DEFAULT_POSTERIOR_CONFIG_B, NOW_MS, parts);
          if (
            pe.key !== re.key ||
            !Object.is(pe.alpha, re.alpha) ||
            !Object.is(pe.beta, re.beta) ||
            !Object.is(pe.mean, re.mean) ||
            !Object.is(pe.lcb, re.lcb) ||
            !Object.is(pe.samples, re.samples) ||
            pe.wellSampled !== re.wellSampled
          ) {
            keyFieldsSame = false;
          }
          if (re.wellSampled) wellSampledCount += 1;
          keyEstimates.push(re);
        }
      }
    }
    check(`R26C-pricexB[${dose.name}].per-key-estimates-bitwise (160 keys x 7 fields)`, keyFieldsSame);
    if (anchorKeyEstimates === undefined) {
      anchorKeyEstimates = keyEstimates;
    } else {
      let sameAsAnchor = true;
      for (let k = 0; k < keyEstimates.length; k++) {
        const a = anchorKeyEstimates[k]!;
        const b = keyEstimates[k]!;
        if (
          a.key !== b.key ||
          !Object.is(a.alpha, b.alpha) ||
          !Object.is(a.beta, b.beta) ||
          !Object.is(a.mean, b.mean) ||
          !Object.is(a.lcb, b.lcb) ||
          !Object.is(a.samples, b.samples) ||
          a.wellSampled !== b.wellSampled
        ) {
          sameAsAnchor = false;
        }
      }
      check(`R26C-pricexB[${dose.name}].per-key-estimates==anchor (price cannot reach the posterior)`, sameAsAnchor);
    }

    // (c) per-episode estimate MULTISETS equal to anchor (price only permutes
    // the presentation order of the estimates array), and the identity-fold
    // theorem: selection == r0.selection at every episode.
    const episodeSigs = prodOnce.decisions.map(estimateSig);
    if (anchorEpisodeSigs === undefined) {
      anchorEpisodeSigs = episodeSigs;
    } else {
      check(
        `R26C-pricexB[${dose.name}].estimate-multisets==anchor (480 episodes)`,
        episodeSigs.length === anchorEpisodeSigs.length && episodeSigs.every((s, i) => s === anchorEpisodeSigs![i])
      );
    }
    check(
      `R26C-pricexB[${dose.name}].selection==r0.selection (identity fold under the R0 sort contract)`,
      prodOnce.decisions.every((d, e) => d.selection === r0ByFamily.get(episodes[e]!.family)!.selection)
    );

    // (d) production self-determinism at this dose.
    const prodTwice = runWorkloadProd(observations, models, r0ByFamily, episodes);
    check(`R26C-pricexB[${dose.name}].production-self-determinism`, sameDecisionLists(prodOnce.decisions, prodTwice.decisions));

    // (e) counter determinism: 3 replica reps, counters bitwise-identical.
    const counterReps: Array<{ b: BCounters; comparisons: number; costOfCalls: number; costFindHops: number; tieBranches: number; tierIndexCalls: number; tierIndexHops: number; rightWinsCost: number; rightWinsTie: number; reduceMs: number }> = [];
    const replicaTimes: number[] = [];
    for (let rep = 0; rep < 3; rep++) {
      BSTAT.reset();
      PSTAT.reset();
      const run = runWorkloadReplica(observations, models, r0ByFamily, episodes);
      replicaTimes.push(run.ms);
      counterReps.push({
        b: snapshotB(),
        comparisons: PSTAT.comparisons,
        costOfCalls: PSTAT.costOfCalls,
        costFindHops: PSTAT.costFindHops,
        tieBranches: PSTAT.tieBranches,
        tierIndexCalls: PSTAT.tierIndexCalls,
        tierIndexHops: PSTAT.tierIndexHops,
        rightWinsCost: PSTAT.rightWinsCost,
        rightWinsTie: PSTAT.rightWinsTie,
        reduceMs: PSTAT.reduceMs,
      });
    }
    const c0 = counterReps[0]!;
    const countsSame = counterReps.every(
      (c) =>
        c.b.powCalls === c0.b.powCalls &&
        c.b.betacfCalls === c0.b.betacfCalls &&
        c.b.betacfIters === c0.b.betacfIters &&
        c.b.ribCalls === c0.b.ribCalls &&
        c.b.lnGammaCalls === c0.b.lnGammaCalls &&
        c.b.quantileCalls === c0.b.quantileCalls &&
        c.b.estimateComputes === c0.b.estimateComputes &&
        c.b.cacheHits === c0.b.cacheHits &&
        c.comparisons === c0.comparisons &&
        c.costOfCalls === c0.costOfCalls &&
        c.costFindHops === c0.costFindHops &&
        c.tieBranches === c0.tieBranches &&
        c.tierIndexCalls === c0.tierIndexCalls &&
        c.tierIndexHops === c0.tierIndexHops &&
        c.rightWinsCost === c0.rightWinsCost &&
        c.rightWinsTie === c0.rightWinsTie
    );
    check(`R26C-pricexB[${dose.name}].count-determinism (3 replica reps)`, countsSame);

    // (f) posterior count identities: EVERY posterior count is dose-invariant
    // (the analogue of AGEX where only betacfIters could move — here nothing
    // may move: price is not a posterior input).
    check(`R26C-pricexB[${dose.name}].powCalls==${B_KEYS * B_OBS_PER_KEY}`, c0.b.powCalls === B_KEYS * B_OBS_PER_KEY);
    check(`R26C-pricexB[${dose.name}].quantileCalls==${B_KEYS}`, c0.b.quantileCalls === B_KEYS);
    check(`R26C-pricexB[${dose.name}].estimateComputes==${B_KEYS}`, c0.b.estimateComputes === B_KEYS);
    check(
      `R26C-pricexB[${dose.name}].cacheHits==${B_EPISODES * B_MODELS - B_KEYS}`,
      c0.b.cacheHits === B_EPISODES * B_MODELS - B_KEYS
    );
    check(`R26C-pricexB[${dose.name}].ribCalls==80*quantiles`, c0.b.ribCalls === 80 * c0.b.quantileCalls);
    check(`R26C-pricexB[${dose.name}].betacfCalls==ribCalls`, c0.b.betacfCalls === c0.b.ribCalls);
    if (anchorBetacfIters === undefined) {
      anchorBetacfIters = c0.b.betacfIters;
    } else {
      check(`R26C-pricexB[${dose.name}].betacfIters==anchor`, c0.b.betacfIters === anchorBetacfIters);
    }

    // (g) price-counter identities: find hops are position-determined (tier
    // order == candidates order), so they are dose-invariant too; the ONLY
    // dose-responsive counts are the tie-path counts.
    check(`R26C-pricexB[${dose.name}].comparisons==${9 * B_EPISODES}`, c0.comparisons === 9 * B_EPISODES);
    check(`R26C-pricexB[${dose.name}].costOfCalls==${18 * B_EPISODES}`, c0.costOfCalls === 18 * B_EPISODES);
    check(`R26C-pricexB[${dose.name}].costFindHops==${63 * B_EPISODES}`, c0.costFindHops === 63 * B_EPISODES);
    check(`R26C-pricexB[${dose.name}].rightWins==0 (identity fold)`, c0.rightWinsCost === 0 && c0.rightWinsTie === 0);
    check(`R26C-pricexB[${dose.name}].tierIndexCalls==2*tieBranches`, c0.tierIndexCalls === 2 * c0.tieBranches);

    // (h) production-lane wall time: 5 timed reps, median.
    const prodTimes: number[] = [];
    for (let rep = 0; rep < 5; rep++) {
      prodTimes.push(runWorkloadProd(observations, models, r0ByFamily, episodes).ms);
    }
    const prodMs = median(prodTimes);
    doseTotals.push({ name: dose.name, prodMs, reduceMs: c0.reduceMs });

    // (i) decision census.
    const selHist = new Map<string, number>();
    for (const d of prodOnce.decisions) selHist.set(d.selection ?? "none", (selHist.get(d.selection ?? "none") ?? 0) + 1);
    const selStr = [...selHist.entries()].map(([k, v]) => `${k}=${v}`).join(" ");
    const cheapestCount = prodOnce.decisions.filter((d) => d.reason.startsWith("cheapest above quality floor")).length;

    doseLines.push(
      `    dose price=${dose.name}: prod ${prodMs.toFixed(2)} ms/workload (replica ${median(replicaTimes).toFixed(2)}; ` +
        `prepare ${c0.b.prepareMs.toFixed(2)} / decay-scan ${c0.b.updMs.toFixed(2)} / lcb-quantile ${c0.b.lcbMs.toFixed(2)} / ` +
        `price-reduce ${c0.reduceMs.toFixed(3)} ms); tieBranches=${c0.tieBranches} tierIndexCalls=${c0.tierIndexCalls} ` +
        `tierIndexHops=${c0.tierIndexHops}; selection: ${selStr} (selected-cheapest=${cheapestCount}/${B_EPISODES}); ` +
        `wellSampledKeys=${wellSampledCount}/${B_KEYS}; betacfIters=${c0.b.betacfIters}`
    );
  }

  for (const line of doseLines) out(line);

  // (j) dose arithmetic vs the 35 ms offline-logit band.
  const worst = doseTotals.reduce((a, b) => (b.prodMs > a.prodMs ? b : a));
  const best = doseTotals.reduce((a, b) => (b.prodMs < a.prodMs ? b : a));
  const delta = worst.prodMs - best.prodMs;
  const worstReduce = doseTotals.reduce((a, b) => (b.reduceMs > a.reduceMs ? b : a));
  out(
    `    price-dose arithmetic (35 ms band): worst dose ${worst.name} ${worst.prodMs.toFixed(2)} ms ` +
      `(x${(35 / worst.prodMs).toFixed(1)} below band); worst-minus-best delta ${delta.toFixed(2)} ms ` +
      `(x${(35 / Math.max(delta, 1e-9)).toFixed(1)} below band); worst price-reduce pool ${worstReduce.reduceMs.toFixed(3)} ms ` +
      `[${worstReduce.name}] (x${(35 / Math.max(worstReduce.reduceMs, 1e-9)).toFixed(0)} below band); ` +
      `whole-pool zeroing bounded by the worst dose itself`
  );
}

/* -------- scenario 4c: PRICEX real-entry lane (production command surface) -------- */

function scenarioPricexRealEntry(): void {
  out(
    `  face B real entry (production buildR1ShadowReport import per dose — routeR0 + slice + gatedComparisonReport; ` +
      `whole-command surface, R7-I lesson):`
  );
  const observations = buildObservationsFresh();
  const episodes: FrozenR1ShadowEpisode[] = episodesB().map((ep, e) => ({
    episodeHash: `ep-${e}`,
    taskFamily: ep.family,
    role: ep.role,
    request: requestForFamilyPrice(ep.family),
    taskSuccess: e % 2 === 0 ? ("PASS" as const) : ("FAIL" as const),
  }));
  const lines: string[] = [];
  const totals: Array<{ name: string; ms: number }> = [];
  for (const dose of PRICE_DOSES) {
    const input: R1ShadowReportInput = {
      episodes,
      models: modelsPrice(dose),
      r0Config: R0_CONFIG_B,
      featureVersion: B_FEATURE,
      nowMs: NOW_MS,
      observations,
    };
    const first = buildR1ShadowReport(input);
    const second = buildR1ShadowReport(input);
    check(
      `R26C-pricexReal[${dose.name}].whole-report-determinism`,
      JSON.stringify(first) === JSON.stringify(second)
    );
    check(
      `R26C-pricexReal[${dose.name}].pairs-identity-theorem (r0==r1, no fallback)`,
      first.pairs.length === B_EPISODES &&
        first.pairs.every((p) => p.r0ModelId === p.r1ModelId && p.r1Fallback === false && p.invoked === false)
    );
    const times: number[] = [];
    for (let rep = 0; rep < 5; rep++) {
      const t0 = performance.now();
      buildR1ShadowReport(input);
      times.push(performance.now() - t0);
    }
    const ms = median(times);
    totals.push({ name: dose.name, ms });
    lines.push(`${dose.name}:${ms.toFixed(2)}`);
  }
  out(`    whole-entry wall (480 episodes, median of 5, ms): ${lines.join(" ")}`);
  const worst = totals.reduce((a, b) => (b.ms > a.ms ? b : a));
  const best = totals.reduce((a, b) => (b.ms < a.ms ? b : a));
  out(
    `    real-entry arithmetic: worst ${worst.name} ${worst.ms.toFixed(2)} ms (x${(35 / worst.ms).toFixed(1)} below band); ` +
      `worst-minus-best ${(worst.ms - best.ms).toFixed(2)} ms; in-slice share bounded by the workload lane above`
  );
}

/* ------ scenario 5: APCX/FLOORX ceiling re-measure (R14-C method) ------- */

function scenarioApcFloor(): void {
  const rows = perfRows();
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irlsPlain(design, baseRows, vectors, supports, keys, baseRows.length, MAX_ITER_DEFAULT);
  const coefficients = fit.coefficients!;
  const onProbabilities = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
  const names = design.names.filter((name) => name !== "intercept");
  let apcSink = 0;
  const sweepReps = 60;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tSweep0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perSweepMs = (performance.now() - tSweep0) / sweepReps;
  out(
    `probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) ` +
      `${perSweepMs.toFixed(3)} ms/fit -> x201 fits = ${(perSweepMs * 201).toFixed(1)} ms/report ` +
      `(base-shape approximation; sink=${apcSink.toFixed(3)})`
  );

  let floorSink = 0;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tFloor0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perFloorMs = (performance.now() - tFloor0) / sweepReps;
  out(
    `probe FLOORX: cheapest bitwise-legal sweep ${perFloorMs.toFixed(3)} ms/fit -> x201 fits = ` +
      `${(perFloorMs * 201).toFixed(1)} ms/report floor; APC combination ceiling on THIS VM = ` +
      `${(perSweepMs * 201 - perFloorMs * 201).toFixed(1)} ms (floorSink=${floorSink.toFixed(3)})`
  );
}
/* --------------------------------- main --------------------------------- */

scenarioEquivalence();
scenarioDeterminism();
scenarioProfile();
scenarioPricexFaceA();
scenarioPricexFaceB();
scenarioPricexRealEntry();
scenarioApcFloor();

out(`APC pool median this run: ${APC_POOL_MEDIAN.toFixed(1)} ms (R24-C band 46.4-53.3, maintained through R25-C; re-anchor band only if outside)`);

if (failures > 0) {
  process.stderr.write(`${failures} CHECK(S) FAILED\n`);
  process.exitCode = 1;
} else {
  out(`\nALL EQUIVALENCE CHECKS PASSED (${checksPassed} bitwise checks)`);
}
```
