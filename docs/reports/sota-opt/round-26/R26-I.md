MODEL_SLUG=claude-fable-5-thinking-xhigh

# R26-I：CLI / Pi adapter / config / telemetry 第二十六遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `ee00c81`（派单独占 tip，含 R26-F 收口与 R26-I 派单）；运行中独占分支快进至 `1ecff7b`（R26-G 收口 + R26-J 派单），本分支已按派单令**同步快进（rebase）到 `1ecff7b`**——增量 `ee00c81..1ecff7b` 仅 1 个提交且全部为 docs（`git diff --stat -- . ':!docs'` 0 行），EXCLUSIONS.md 该区间逐字节未变，切片与 dist 不受影响（全部测量在 `ee00c81` 构建的 dist 上完成，二者切片字节相同——快进后 `git diff 8dee7fb..HEAD -- <25 文件>` 重验仍为 0 行）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- <25 文件>` 为空（0 行）——**零漂移，自 `8dee7fb` 起连续第十九轮字节不变**。`package.json` / `pnpm-lock.yaml` / 切片相关测试面零漂移（`git diff --quiet 8dee7fb..HEAD -- package.json pnpm-lock.yaml test/` 通过，快进后复验通过）。基线增量核查：`3ecbd94`（R25-I 收官时 tip）→ `1ecff7b` 共 **9 个提交，全部 `docs(sota-opt)` 收口/派单**（`git diff --stat -- . ':!docs'` 0 行）；**EXCLUSIONS.md 全区间逐字节未变**（`git diff --quiet` 通过），对 I 切片无新约束行。快进吸收的 R26-G 为父代理已收口分支（其归档轴「edge condition-kind face」属 G 辖区 flowchart 边条件种类，与本轮新轴对象无交集；R26-J 在飞，未触碰其分支与文件）。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致。
- 前置阅读按令执行：README → EXCLUSIONS 全表（S1-I / S4-I-2..5 / S5-I-1..5 / S7-I-1..4 / S8-I-1..3 逐条定位）→ PROGRESS → round-26/PLAN（A–F 六切片收口摘要 + G/H 在飞行 + I 派单行）→ R25-I（进程环境坐标）→ R24-I（适应态坐标）→ R23-I（`.pi/` 树）→ R22-I（run 记录回读）。兄弟轴禁移植逐一核读：R26-F input storage-state face / R26-E **RTL** / R26-C **PRICEX** / R26-D injection-seam default-arm face / R26-A collation-locale face / R26-B privacy / provider-policy constraint-leg face / R25-H consumer-stage census / R25-G injection command-kind face / R25-F plan-header identity-geometry face / R25-D identity-coordinate face / R25-B task-face optional-field presence topology / R25-A value-domain content-class census / **NAMESHAPE**（R19-G 路径几何，plan-ban 名录项）——本轮新轴与其对象、站点、机制在 §2.1 逐项划界。
- 分支：`cursor/r26-i-cli-twenty-sixth-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（`/exec-daemon/node`，与 R9-I～R25-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。pnpm 10.17.1，`pnpm install --frozen-lockfile` + `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**（钉死 + frozen-lockfile）；exports 图仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`，本 VM 直接枚举）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第十九轮未满足（两臂文件级 blocked 按派单原样维持，未重复执行级探针）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S26-I-\* ID（诚实空枚举，与 R9-I～R25-I 同结局，十九连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第二十六组枚举而非历轮剩余项清理。R25-I 收官时宣告「六个输入平面 + 四个配置态坐标系全部收口」——本轮发现并收口 spawn 面三条输入通道中**最后一条从未被剂量的通道：argv 表面形态面（argv surface-form face）——参数向量自身的句法几何**。spawn 面恰有三条输入通道：环境块（R25-I 第四配置态坐标收口）、路径/名字几何（**NAMESHAPE**，R19-G 收口且为 plan-ban 名录项）、argv 向量本身。历轮全部 harness 对每格 argv 只用**一种规范序列化**（长旗标、空格分隔值、单一固定旗标次序、`--version` 单一别名），向量的**句法形态**（旗标次序置换 × 分隔形式 `--flag=value` vs `--flag value` × 分派别名选择 `version|--version|-V` × 重复旗标出现）从未做过受控变量——R7-I「固定夹具遮蔽主路径」教训在第三条 spawn 通道上的对应物（本轮证实该通道**平坦**：遮蔽处无结构，收口即完成）。该轴剂量的是**句法**，全部值钉在承重规范值上；配置值的**来源拓扑**（flag/env/providers/default 语义面）是 R25-I 已收口对象，本轮明令不补铸——**未设任何 `--primary-model` / `--fast-model` 格**。该轴为测量收口、未产出 I 切片候选，四个裁决性发现：

1. **零漂移五重确认（连续第十九轮）**：切片源码（`git diff 8dee7fb..HEAD` 空，快进前后各验一次）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变；pi-ai 0.84.1 exports 图 7 项未变；39 表直接计数；`all.models.js` 不存在）、测试面（零变动）、基线增量（`3ecbd94..1ecff7b` 共 9 提交全 docs，EXCLUSIONS 字节不变）、调用图前提（S5-I-1 dispatch 站点 **13 条 `await import`** 直接计数在位；S7-I-1 站点 model-catalog.ts L53-58 注释与站点原样；`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` L36 失手回退 / `runtime.ts` L20 点用处 / `listed-model.ts` L1 同步面【grep 直接重验，注释行除外恰 3 边】；pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方【grep 计数 0】；本轮新增前提计数：**parseArgs 站点恰 23 处分布于 9 个切片文件**——main.ts 9 / models.ts 4 / auth.ts 3 / commits.ts 2 / adapt.ts、doctor.ts、episode.ts、inject.ts、pause.ts 各 1）。
2. **本 VM 锚点复测（三次运行 × 双引擎，未拷贝历轮数字，派单明令项）**：R9-I 承重集 + flowchart 双态格全部 exit 0，**custom−builtin 差 children +43.4~+61.7 / +24.0~+27.8、track +48.5~+52.6 / +25.7~+36.9、flowchart-flat64 +47.1~+49.2 / +12.0~+28.2 ms（22.14/22.22，三次运行带）**——与 R25-I 带（children +37.0~+50.7 / +20.9~+25.8、track +45.7~+53.1 / +23.6~+25.6、flat64 +42.3~+52.5 / +15.1~+24.9）同号、同量级、同 Node 版本梯度（**十九轮同构**）。带交叠核对：children/flowchart 双引擎与 track@22.14 四个带对 R25-I 带直接相交；track@22.22 带 [+25.7,+36.9] 与 R25-I [+23.6,+25.6] 在最近点相距仅 0.1 ms（run2 +25.7 vs 带顶 +25.6，即同结构贴带），run1/run3 的 +7.3~+11.3 ms 上浮与该格绝对中位（157.0/163.6 vs R25-I 143.8-152.2）同步抬升，落在 R15-I 轴 1 的 A/A 单窗噪声包络内——无新结构、无新空洞。地板 56.5-58.8 / 51.5-55.8 ms 与 R25-I（54.6-58.8 / 52.9-53.5）几乎同位。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退；r7i part D 本轮复跑第三线互证（builtin 构建后 providers/all 保持冷 **69.0 ms**、custom 回退命中缓存 **3.4 ms**）。
3. **新轴（§1.2-§1.4）：argv 表面形态标定（I 切片首次；第三条 spawn 通道收口）**。门在承重路径上恰有两层：`main()` 分派 switch（slice 自有；`version|--version|-V` 与 `help|--help|-h` 各三别名臂）与各命令头部的 `node:util` `parseArgs` strict 调用（23 站点；接受两种分隔形式、任意旗标次序、非 multiple 旗标 last-wins 重复）。结构判决先于测量并被测量证实：**parseArgs 在任何 slice 逻辑运行前把全部被接受形态归一化为同一 `values` 记录，形态不可能翻任何下游腿**。9 个新格（3 命令类 × 2 配置态交叉 + 别名双格；双引擎 × 三次运行全部 exit 0）裁决数字：
   - **进程内解析段全形态同价（µs 级 once-per-command）**：run 表规范形 4.02-4.21 µs/call、`=` 形 4.00-4.03、逆序 3.92-4.00、重复 last-wins 4.48-4.57（双引擎并示）——**形态间最大差 ~0.56 µs**，在 spawn 尺度不可见。resume 表现实档（3 重复 + csv）3.83-3.90 µs；对抗档 200 重复 `--selected` 63.2-63.6 µs 仍为 µs 族 once-per-command。slice 自有 `collectSelectedActionIds` 现实档 348-366 ns、对抗档 5.9-6.0 µs。整分派别名臂三形同价（`--version`/`-V`/`version` = 6.7-7.4 µs/call，含每调用 readFileSync(package.json)；`help`/`--help` = 221-267 ns/call）。
   - **e2e 形态剂量全部 ≈0（符号翻转）**：eqForm −16.8~+5.3、shuffled −13.9~+10.1、dupSame −6.0~+1.0、track mixedShuffled −8.7~+4.3、flowchart shuffled −1.7~+12.9、别名双格 −2.9~+4.1 ms（相对各自同态规范格；双引擎三运行）——与进程内 ≤0.6 µs 形态差逐位相容；配置态 × 命令类交叉（builtin/custom × children/track/flowchart）无一格显示形态结构。
   - **行为见证 28 项检查 × 双引擎全 0 失败**：W1 次序不变性（逆序 ≡ 规范序，builtin+custom 各 3 运行）、W2 分隔形式不变性（`=` 形 ≡ 空格形）、W3 别名不变性（version 三形 stdout 逐字节相同 ×3 + help 三形 ×3）、W4 重复 last-wins（同值重复 e2e 规范化逐字相同 + 进程内 parseArgs 异值 last-wins 确定性）、W5 `--selected` 几何（重复序保持、首现去重、CSV trim、repeats-before-csv 合并序、双缺省 undefined vs 显式空 `[]`——slice 自有 `collectSelectedActionIds` 语义全钉住）。
4. **诚实注记 + 契约面移交注记（散文记录，不铸 ID）**：argv 面错误分类学存在**三分不对称**（只读观察，非候选）：未知旗标（engine `TypeError` 经外层 catch）打印 `stage: execute`；缺必填旗标（slice 自查 cliFail）打印 `stage: parse-args`；多余位置参数（engine 拒绝）打印 `stage: execute`——同一 argv 面的三类错误报两种 stage 标签。R17-I 已把这三个端点全部定价贴地板（错误路径无性能项）；stage 标签属错误元数据/契约外观问题——**移交契约所有者，移交不代行，不铸 ID**。
5. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**，22.22.2 + tsx）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R25-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动，无 r26i 新仿真（派单：仅落地代码才须新增）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读读码，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交任何 scripts/（全部 harness 源码进附录 A，遵守 R7-F/R8-C/R9-I～R25-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README 未触碰；未触碰在飞 R26-J 与已收口 R26-G/R26-H 分支与文件。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S25-\* 条目；R9-I～R25-I 空枚举维持，未事后发明 S9-I-\* ～ S25-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：零漂移即字节级证明（站点逐项重验，结论第 1 条），三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅做文件级重开条件确认（§2.2），未满足，维持淘汰。**两臂文件级 blocked 按派单原样维持。**
  - **与 R25-I「进程环境坐标」的划界（本区自有轴，plan-ban 不补铸）**：其对象是配置值的**来源拓扑语义面**（env 键存在性 × flag/env/providers/default 优先级链——哪个源赢、赢家值把目录翻到哪条腿）；本轴对象是 argv 通道的**句法面**（同一组值的不同序列化形态），全部值钉死在承重规范值上。为杜绝补铸，本轮**明令不设 `--primary-model` / `--fast-model` 格**（那是 R25-I 优先级链的 flag 臂，属其来源拓扑对象）；环境块全程钉在全缺席角（R25-I 的坐标不重剂量）。对象、机制、可变维三不同。
  - **与 NAMESHAPE（R19-G，plan-ban 名录项）的划界**：其对象是 I/O 契约动词价格函数中的**路径名自由度**（state-root 深度 × 组件字节长 × 目录扇出）；本轴 argv 里的路径**值**（--project/--state-root/--children）字节钉死与历轮相同，只变旗标的**排列与分隔句法**。未剂量任何路径几何维。
  - **与 R17-I「拒绝路径定价」的划界**：其定价了 argv 面的三个拒绝端点（未知子命令、缺旗标、旗标冲突——全部贴地板）；本轴只剂量**被接受形态**的价格与等价性，拒绝侧仅引用 R17-I 收口、未重定价（§1.4 的 stage 标签观察是元数据外观，非价格）。
  - **与 R25-A「value-domain / string content-class census」的划界（禁移植轴）**：其对象是固定长度下字符串的**字节内容类**（A 辖区进程内值域）；本轴所有 token 的字节内容不变，只变 token 边界与排列。
  - **与 R21-E / R22-B「标识符几何」的划界**：其对象是域标识符的长度/结构几何；本轴不触任何标识符，children.json / flowchart 规格逐字节与历轮相同。
  - **与 R16-I「命令类覆盖」的划界**：其可变维是**哪些命令**进矩阵；本轴固定承重命令类、变**同一命令的向量序列化**。
  - **与 R25-G「injection command-kind face」的划界（禁移植轴）**：其对象是 G 辖区 inject 缝的命令种类分派；本轴别名三格（`version|--version|-V`）是**同一 handler 的表面 token 选择**（三 token 进同一 case 臂），非命令种类。
  - **与 R26-A「collation-locale face」/ R26-F「input storage-state face」/ R26-E RTL / R26-C PRICEX / R26-D injection-seam / R26-B constraint-leg 的划界（禁移植轴）**：本轴无 locale 剂量（parseArgs 无 locale 敏感比较）、无 V8 容器完整性态剂量（args 数组均为新鲜 packed 字面量）、不触 applyLearnedRouting 终态腿 / 货币成本坐标 / RegistryOptions 注入臂 / privacyRequired 约束腿。
  - **未换名移植其余兄弟轴**（R25-H consumer-stage census / R25-F plan-header identity-geometry / R25-D identity-coordinate / R25-B presence topology / R24-B M=1 collapse / R24-D approval-authority / R24-F provenance / R24-H operand-residency / **AGEX** / **CG** / **JB** / **VALCLASS** / **COTARG** / **FITQ** / **SCALEX** / **YMIX** / **SEEDX** / **ORDX** / **OCCX** / **TERMCLASS** / **SCHEDWIN** / **XPROC** / **SHAPEK** / **KFAN** / **NVG** / **RUNLIM** / **CKMIX** / **CALLB** / R26-G edge condition-kind face）——逐一定位核过，与本轴对象机制无交集。
  - **未重铸 R24-I 适应态坐标**（全部夹具无 registry.json）/ **R23-I `.pi/` 树平面**（项目无 `.pi/` 树）/ **R22-I run 记录回读平面**（无 inspect/commits/episode 规模格）/ **R21-I flowchart 平面**（flat64 规格维持承重形态未扩展）/ **R20-I 累积遥测 N 轴**（invocations.jsonl 不存在）/ **R19-I E×P×K**（custom 夹具维持 P=1/K=2/E=2）/ **R18-I children 基数轴**（C=2）/ **R14-I providers 配置空间**（builtin/custom 夹具逐字节沿用）。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价；双 LCB / 双归因在位；提升 proposal-first（无任何提升）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`providers/all` 失手回退 + throw-outside-catch 在位；无 fsync / 完整性再哈希削除；fail-open 族保持关闭；未改任何测试。**新轴全程零代码改动、零行为注入**——所有测量都是对既有 parseArgs/dispatch 门的直接观测 + 文档化公开 CLI 语法面（USAGE 记载的旗标与别名）的合法形态注入。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；未 force-push 独占分支；未触碰在飞 R26-J 分支与文件。

## 1. 本 VM 复测：7 格承重再锚 + 新轴 9 格（双 Node × 三运行，未拷贝历轮数字）

R9-I～R25-I 抽测集承重面复刻并**新增 9 个 argv 形态格**（每迭代全新拷贝 state-root、基底 env 显式清除 `PI_PROVIDER`/`PI_MODEL`/`PI_API_KEY`/`PI_FAST_MODEL`/`SPARKLE_AUTO_ADAPT`/`PI_THINKING_LEVEL`【环境坐标钉在 R25-I 已收口的全缺席角】、2 预热 + 10 测量、显式二进制全路径；harness 全文附录 A.2，三次运行原始输出附录 A.5）。

### 1.1 承重集三运行中位数带（ms，spawn-to-exit；22.14.0 / 22.22.2）

| 格 | run1 | run2 | run3 |
|---|---|---|---|
| `--version`（地板） | 57.3 / 51.5 | 56.5 / 55.8 | 58.8 / 52.5 |
| run-children@builtin | 112.1 / 108.7 | 110.2 / 115.0 | 117.1 / 109.5 |
| run-track@builtin | 128.2 / 124.1 | 135.6 / 130.8 | 138.9 / 126.7 |
| run-children@custom | 155.5 / 132.7 | 171.9 / 142.8 | 170.1 / 136.8 |
| run-track@custom | 177.3 / 157.0 | 184.1 / 156.5 | 191.5 / 163.6 |
| run-flowchart@custom-flat64 | 266.6 / 236.6 | 263.0 / 250.0 | 265.4 / 250.7 |
| run-flowchart@builtin-flat64 | 217.4 / 224.6 | 215.8 / 221.8 | 218.3 / 226.0 |

- **custom−builtin 差（三运行带）**：children **+43.4~+61.7 / +24.0~+27.8**、track **+48.5~+52.6 / +25.7~+36.9**、flowchart-flat64 **+47.1~+49.2 / +12.0~+28.2** ms（22.14/22.22）。对 R25-I 带逐类核对（派单明令「先再锚定后判洞」项）：children 双引擎、flowchart 双引擎、track@22.14 五个带**直接相交**；track@22.22 带与 R25-I 带在最近点相距 0.1 ms（run2 +25.7 vs R25-I 带顶 +25.6——同结构贴带），run1/run3 上浮 +7.3~+11.3 ms 与该格绝对中位同步抬升（157.0/163.6 vs R25-I 143.8-152.2），属 R15-I 轴 1 的 A/A 单窗噪声包络内偏移（22.14 单窗 maxAbs ~34.5 ms 量级），三运行同号、无新结构——**十九轮同构，无新空洞**。
- 全部 exit 0，三运行 × 双引擎无一失败。归属第三线互证：r7i part D 本轮复跑（附录 A.6）——builtin 构建后 providers/all 保持冷 **69.0 ms**、custom 回退命中缓存 **3.4 ms**。

### 1.2 新轴的生产流证明（先证门存在与流量，再证遮蔽史，再证数字）

- **门与流量**：argv 向量是每次 CLI 调用的必经输入；其消费门恰两层，全部在 I 切片内：
  1. `main.ts` **L1405-1486** `main(argv)`：slice 自有分派 switch——`case "version": case "--version": case "-V"`（L1456-1460）与 `case "help": case "--help": case "-h"`（L1461-1465）各三别名臂进同一 handler；12 个子命令臂 + unknown 臂。
  2. 各命令头部 `node:util` `parseArgs` strict 调用（**23 站点 / 9 文件**；run 命令表在 main.ts L514-532，14 个选项）：接受 `--flag value` 与 `--flag=value` 两种分隔形式、任意旗标次序、非 multiple 旗标 last-wins、`multiple: true`（resume/answer 的 `--selected`）重复累积；归一化输出 `values` 记录后 slice 逻辑才开始。slice 自有的形态敏感消费仅 `collectSelectedActionIds`（flowchart-io.ts L99-122：repeats-before-csv 合并序 + 首现去重 + trim）。
- **遮蔽史（R7-I 教训第三 spawn 通道）**：R8-I 78 格矩阵与 R9-I…R25-I 全部抽测 harness 对每格 argv 只用一种序列化（长旗标 + 空格分隔 + 单一次序；地板格只用 `--version` 一个别名）——**向量句法形态从未进过任何矩阵**。spawn 面三通道中环境块已由 R25-I 收口、路径名几何已由 R19-G（NAMESHAPE）收口，argv 形态是最后一条无价签通道。
- **正交性**：剂量只改 token 排列/分隔/别名/重复；全部值（路径、objective、children/flowchart 文件）字节钉死；providers.json / `.pi/` 树 / registry.json / invocations.jsonl / children C=2 / flowchart 规格 / 环境块全部维持既有坐标不动。

### 1.3 轴 1：argv 表面形态标定（本轮新证据，不铸 ID）

**(a) 端到端形态剂量（三运行中位数差，相对各自同态规范格，ms，22.14/22.22）**：

| 格（相对基线） | run1 | run2 | run3 | 判决 |
|---|---|---|---|---|
| children@builtin+eqForm − children@builtin | −0.4 / +4.7 | −1.8 / −6.6 | +0.5 / +0.3 | ≈0，符号翻转 |
| children@custom+eqForm − children@custom | +5.3 / +3.8 | −16.8 / −9.9 | −5.8 / −2.5 | ≈0，符号翻转 |
| children@builtin+shuffled − children@builtin | −1.9 / +5.2 | −0.9 / −7.3 | +1.2 / −1.0 | ≈0，符号翻转 |
| children@custom+shuffled − children@custom | +1.0 / +10.1 | −13.9 / −1.1 | −8.6 / −1.3 | ≈0，符号翻转 |
| children@builtin+dupSame − children@builtin | −0.3 / +1.0 | −0.9 / −1.3 | −6.0 / +0.2 | ≈0，符号翻转 |
| track@builtin+mixedShuffled − track@builtin | −0.7 / +4.0 | −5.7 / +4.3 | −8.7 / +2.4 | ≈0，符号翻转 |
| flowchart@custom+shuffled − flowchart@custom | −1.7 / +9.8 | +12.9 / +11.9 | +4.2 / −0.4 | ≈0，符号翻转 |
| version-shortV − version | −0.9 / +4.1 | −1.4 / −3.5 | −2.9 / −0.5 | 别名同价 |
| version-word − version | −1.1 / +3.4 | −1.3 / −3.2 | −1.7 / −0.3 | 别名同价 |

配置态 × 命令类交叉（builtin/custom × children/track/flowchart）无一格显示形态结构；全部 exit 0。

**(b) 进程内解析段分解**（附录 A.3；双引擎并示；22.14/22.22）：

| 段 | 辖区 | 22.14.0 | 22.22.2 |
|---|---|---|---|
| parseArgs run 表规范形（8 token） | node:util/engine（表为 slice 定义） | 4.21 µs | 4.02 µs |
| parseArgs run 表 `=` 形（4 token） | 同上 | 4.03 µs | 4.00 µs |
| parseArgs run 表逆序（8 token） | 同上 | 3.92 µs | 4.00 µs |
| parseArgs run 表重复 last-wins（12 token） | 同上 | 4.48 µs | 4.57 µs |
| parseArgs resume 表现实档（3 重复 + csv） | 同上 | 3.90 µs | 3.83 µs |
| parseArgs resume 表对抗档（200 重复） | 同上 | 63.2 µs | 63.6 µs |
| **collectSelectedActionIds 现实档（3+3，1 dup）** | **cli(I)** | **348 ns** | **366 ns** |
| collectSelectedActionIds 对抗档（200→150 去重） | cli(I) | 6.04 µs | 5.94 µs |
| main([--version]) 整分派（含 readFileSync package.json） | cli(I) | 7.36 µs | 6.88 µs |
| main([-V]) / main([version]) 整分派 | cli(I) | 7.17 / 7.07 µs | 7.15 / 6.70 µs |
| main([help]) / main([--help]) 整分派（USAGE 常量写出） | cli(I) | 221 / 242 ns | 223 / 267 ns |

- **形态间最大差 ~0.56 µs/命令**（dup 12-token vs 逆序 8-token）——e2e 符号翻转带（±17 ms 抖动）与之逐位相容：µs 级形态差在 spawn 尺度物理不可见。
- **I 切片份额**：分派 switch 是分支常数（help 臂全额 221-267 ns 即其上界见证）；选项表是传给 parseArgs 的字面量（构造成本已含在 3.9-4.6 µs 内）；slice 自有形态敏感段仅 `collectSelectedActionIds` 348-366 ns。token 化/归一化/strict 检查在 `node:util` 引擎辖区。**无任何段构成 I 候选。**

**(c) 行为见证**（附录 A.4；双引擎，28 检查全 0 失败）：

- **W1 次序不变性**：children run 逆序旗标 ≡ 规范序（builtin + custom × 3 运行，输出规范化逐字相同）。
- **W2 分隔形式不变性**：`--flag=value` 全形 ≡ `--flag value` 全形（builtin + custom × 3 运行）。
- **W3 别名不变性**：`--version`/`-V`/`version` stdout 逐字节相同且全 exit 0；`help`/`--help`/`-h` 同。
- **W4 重复 last-wins**：同值重复 e2e ≡ 规范格（3 运行）；进程内异值重复 parseArgs 取末值且跨调用确定。
- **W5 `--selected` 几何**：`(["b","a","b"], " c , a ,, d ") → ["b","a","c","d"]`（重复序保持 + 首现去重 + trim + repeats-before-csv）；双缺省 → `undefined`、显式空 csv → `[]`（slice 文档化语义逐条钉住）。

### 1.4 argv 面错误分类学不对称注记（只读观察，移交契约所有者，不铸 ID）

同一 argv 面的三类用户错误报两种 stage 标签（本轮直接取证）：未知旗标 `--no-such-flag` → engine `TypeError`（ERR_PARSE_ARGS_UNKNOWN_OPTION）经 `main()` 外层 catch（L1478-1485 `error instanceof DomainValidationError ? "validation" : "execute"`）打印 **`stage: execute`**；多余位置参数同路径 **`stage: execute`**；缺必填旗标（runCommand L535-542 slice 自查）打印 **`stage: parse-args`**。三端点价格 R17-I 已收口（全贴地板，无性能项）；stage 标签是否应统一为 parse-args/validation 属错误元数据契约判定——**移交，不代行，非性能项**（统一它是行为可观察变更，非保行为优化）。

## 2. 第二十六组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 输入规模定价 / spawn 通道补全），每形态先对 EXCLUSIONS 全表 + 二十五轮 I 报告 + 兄弟禁移植轴做同机制先例检索。

### 2.1 新轴候选性自查——测量收口，不铸 ID

- **轴 1（argv 表面形态）**是既有 parseArgs/dispatch 门在第三条 spawn 通道上的直接观测：零行为注入、零代码改动。产出是「全形态同价（≤0.6 µs/命令）+ e2e 符号翻转 ≈0 + 28 项行为见证 + 分派别名三形同价」的裁决数字集，不是优化。
- **派单空转卫自查**：本轴有真实生产流（旗标次序/分隔形式/别名是用户每次调用的自由选择；USAGE 文档化公开语法面）且填补的是历轮 harness 单一序列化制造的测量盲区（每轮 spot.mjs 的 args 数组可指认）——不属「零生产流 + 仅幻想尺度」空转型。不铸候选的理由是三重的：解析段 µs 级 once-per-command（验收线明文 REJECT 类）、形态间差 ≤0.6 µs 无可收割梯度、token 化归一化在 node:util 引擎辖区（slice 只拥有表和 switch）。

### 2.2 S8-I-1 重开条件文件级再确认——不铸 ID

- `dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 **0.84.1**。依派单「两臂文件级 blocked」纪律不重复执行级探针。
- **裁决**：重开条件连续第十九轮未满足；r7i part D 复跑（69.0 / 3.4 ms）再度坐实回退结构的健全性价格。审计证据，非机制候选——**不铸 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级/辖区即死）

| 扫描角度 | 处置 |
|---|---|
| **parseArgs 换手写解析器 / 预编译选项表** | 平行实现族（X1-2 同型）+ V8/engine builtin 反例族（S7-C-2 / S8-B-2 先例：builtin 已特化）+ 3.9-4.6 µs once-per-command 验收线 REJECT 类。非候选 |
| **23 处选项表字面量提升为模块常量** | S1-B-7 逐字同型（V8 已优化对象字面量）+ 构造成本含在 µs 级解析段内。非候选 |
| **packageVersion() 缓存 / 构建期内联版本号** | X1-1 隐藏缓存族 + S6-I-2 构建面冻结同族 + 整臂 6.7-7.4 µs once-per-command。非候选 |
| **USAGE 惰性构建 / help 臂瘦身** | 模块级模板常量一次构建；help 整分派 221-267 ns。无可减物 |
| **分派 switch 按频率重排 / 别名臂合并** | S1-B-3 族短路重排噪声；三别名臂已是同一 case fallthrough，零成本。非候选 |
| **collectSelectedActionIds Set/微观重构** | 现实档 348-366 ns（S1-J-3 族 ns 级）；对抗档 6 µs once-per-command。非候选 |
| **`--selected` multiple 解析对抗档** | 63 µs @200 重复——线性、once-per-command、对抗规模假设。非候选 |
| **argv 面拒绝端点瘦身 / stage 标签统一** | 价格 R17-I 收口（全贴地板）；标签统一是行为可观察契约变更——§1.4 移交注记 |
| **dup last-wins 快路径 / 归一化缓存** | 形态差 ≤0.6 µs 无收割空间；跨调用缓存撞 X1-1。非候选 |
| 双装载去重 / 装载下沉 / 部分解析 / 完整性跳过 / 轮转工具 / JSON.parse / status stdout / save 攒批 | R24-I §2.3 十一行收口维持（适应态平面零角未动，前提未变） |
| 环境门读提升 / env-providers 优先级 / kill-switch / 表 import 段 / 拒绝腿 | R25-I §2.3 收口维持（本轮环境块钉全缺席角未再剂量） |
| ingest/回读/flowchart 规模/配置基数/children 基数族 | R23-I / R22-I / R21-I / R19-I / R18-I 收口维持，本轮未触其规模格 |
| 拒绝路径 / 错误端点 / 肥 builtin 态 / A/A 地板 / 引擎代 / 堆足迹 / 采样归因 / 测量学 | R17-I / R14-I / R15-I / R16-I 收口维持；本轮带比对直接引用其 MDE 包络 |
| flowchart 双 build / providers.json 双读 / pause-inject 免 router / compile-cache / dist 布局 / 一次性命令类 / 凭据面 / 解析微观 / barrel 隐藏边 / root-compat 替代源 / per-provider 探针短路 / 预热重叠 | S1-I-1/2、S2-I-1..6、S3-I-1..6、S4-I-2..5、S5-I-2..5、S6-I-1..3、S7-I-2..4、S8-I-2/3、R11-I/R12-I/R13-I 无 ID 收口逐字重现——零漂移下前提逐项成立，本轮 25 文件重读未发现任何新站点脱离既有覆盖物 |
| telemetry / config 残余面 | R20-I 热站点定价贴底维持；S1-D-8 / S3-I-5 族噪声维持 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖、pi-ai exports、测试面、基线增量五重零漂移（头部核查，快进后复验） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可落地候选；本轮把 I 切片 **spawn 面第三条也是最后一条输入通道（argv 向量句法形态）**收口：全形态同价 ≤0.6 µs/命令、e2e 符号翻转 ≈0、28 项行为见证（次序/分隔/别名/last-wins/selected 几何全钉住）、分派别名三形同价。**I 切片自此六个输入平面 + 四个配置态坐标系 + spawn 面三通道（环境块 R25-I × 路径几何 R19-G × argv 形态本轮）全部收口。**

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx；无代码落地故按派单为自愿加测）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part D 载荷探针复证 builtin 构建后 providers/all 保持冷 69.0 ms、custom 回退命中缓存 3.4 ms；part E configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单项，Node 22.22.2）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**（与 R9-I～R25-I 计数逐字相同）、`pnpm build` 绿。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动；无赢家故无新仿真脚本入库；四个 harness/探针（genfix/spot/inproc/equiv）均为 /tmp 临时件，全文进附录 A，未进 lint 面。
- **引擎注记**：22.14.0 双引擎并测用于十九轮带比对；nvm 22.22.2 为 gate/仿真引擎（engines ≥22.19 纪律，R15-I/S5-I-5 在案）。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；新轴以测量收口关闭 spawn 面最后一条输入通道；S8-I-1 文件级第十九轮 blocked）。
- 剩余赢家：无（custom−builtin 结构维持 S8-I-1 健全性反例封死；argv 形态面的全部成本段——分派 switch 分支常数 / parseArgs µs 级引擎段 / collectSelectedActionIds 348-366 ns——无一构成候选；全部消除形态映射 X1-1 / X1-2 / S1-B-7 / S1-B-3 / S1-J-3 / S6-I-2 / R17-I 既有围栏）。
- 矩阵空洞：无（承重格十九轮同构复现 + 本轮补入 argv 形态 9 格——{builtin,custom} × {children,track,flowchart} × 形态{canonical,eqForm,shuffled,dupSame,mixedShuffled} 的归因完备组合 + 别名双格；R7-I 纪律在第三 spawn 通道上重演并收口，结论是平坦）。
- 跨辖区移交注记一条已在结论第 4 条 / §1.4 记录（契约所有者辖区：argv 面三类错误的 stage 标签三分不对称），非 I 候选。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 夹具生成（/tmp/r26i-bench/genfix.mjs）

```javascript
// R26-I fixture generation: bearing-set re-anchor states (R9-I..R25-I lineage).
// The NEW axis this round is the argv surface-form face — the syntactic
// geometry of the argument vector itself (flag order permutation, separator
// form --flag=value vs --flag value, dispatch alias choice, duplicate flag
// occurrence) with every VALUE held at the bearing canonical ones. Every
// prior I-round harness serialized each cell's argv exactly one way
// (long-form flags, space-separated values, one fixed order), so the vector's
// FORM was pinned across all 25 rounds of matrices. State fixtures here are
// byte-identical in shape to R25-I appendix A.1 (default / builtin / custom
// provider states; env block stays at the all-unset corner — R25-I closed
// that coordinate and it is not re-dosed).
import { mkdirSync, rmSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const ROOT = "/tmp/r26i-bench";
const FIX = join(ROOT, "fixtures");

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
function writeState(name, cfg) {
  const dir = join(FIX, name);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  if (cfg !== undefined) {
    writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
  }
  return dir;
}
writeState("state-builtin", states.builtin);
writeState("state-custom", states.custom);
writeState("state-default", undefined);

// Children spec (R9-I..R25-I lineage, C=2).
writeFileSync(join(FIX, "children.json"), JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");

// Flowchart fixtures (R21-I flat shapes; bearing cells only).
function flatFlow(n) {
  const nodes = [];
  for (let i = 0; i < n; i++) {
    nodes.push({
      id: `n${i}`, taskId: `tsk_f${i}`, role: "actor",
      objective: `Step ${i} of the benchmark flow`,
      modelPolicy: { allowedModels: ["cheap"] },
      confidenceThreshold: 0.7, approvalRequired: false
    });
  }
  return { id: `bench-flat-${n}`, nodes, edges: [] };
}
function resultsFor(n) {
  const out = {};
  for (let i = 0; i < n; i++) {
    out[`n${i}`] = {
      outcome: "SUCCESS", confidence: 0.9,
      evidenceIds: [`evd_f${i}`],
      facts: [{ key: `fact-${i}`, value: `value ${i}`, confidence: 0.8 }]
    };
  }
  return out;
}
writeFileSync(join(FIX, "flow-flat64.json"), JSON.stringify(flatFlow(64)) + "\n");
writeFileSync(join(FIX, "results-flat64.json"), JSON.stringify(resultsFor(64)) + "\n");

// Project scaffold (R9-I..R25-I shape; no .pi tree, no adaptation registry —
// R23-I / R24-I planes stay closed at their zero corner).
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
console.log("FIXTURES_WRITTEN");
```

### A.2 16 格抽测 harness（/tmp/r26i-bench/spot.mjs，分别以两版本二进制执行 × 3 运行）

```javascript
// R26-I spot measure: R9-I..R25-I bearing set (floor + builtin/custom x
// children/track/flowchart re-anchor) + NEW axis: argv surface-form face.
// Every prior I-round harness serialized each cell's argument vector exactly
// one way: long-form flags, space-separated values, one fixed flag order,
// the "--version" alias for the floor cell. The vector's syntactic FORM —
// the third spawn-face channel after the env block (closed by R25-I) and the
// path/name geometry (NAMESHAPE, closed by R19-G and plan-banned) — was never
// a controlled variable in any round. Doors on the bearing path:
//   main.ts main(): slice-owned dispatch switch with alias arms
//     ("version" | "--version" | "-V"; "help" | "--help" | "-h");
//   runCommand L514-532 (and 12 sibling sites): node:util parseArgs in strict
//     mode — accepts --flag value and --flag=value forms, any flag order,
//     last-wins duplicate handling for non-multiple options.
// Structural prediction under test: parseArgs normalizes every accepted
// surface form to the same `values` record before any slice logic runs, so
// form cells must price ~zero on every config-state x command-class cell and
// produce normalized-identical output (witnesses in equiv.mjs). All VALUES
// stay at bearing canonical ones — this axis doses syntax only, never the
// config-source topology (that semantic plane is R25-I's, not re-dosed).
// usage: <node-binary> spot.mjs
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r26i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.execPath;
const WARMUP = 2, ITERS = 10;

const PROJ = join(FIX, "project");
const CHILDREN = join(FIX, "children.json");

// Canonical serializations (R9-I..R25-I lineage — byte-identical token order).
const childrenArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN];
const trackArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"];
const flowResultsArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--flowchart", join(FIX, "flow-flat64.json"), "--results", join(FIX, "results-flat64.json")];

// Surface-form doses (same values, different syntax).
const childrenEqForm = (s) => ["run", `--project=${PROJ}`, "--objective=bench objective", `--state-root=${s}`, `--children=${CHILDREN}`];
const childrenShuffled = (s) => ["run", "--children", CHILDREN, "--state-root", s, "--objective", "bench objective", "--project", PROJ];
const childrenDupSame = (s) => ["run", "--project", PROJ, "--project", PROJ, "--objective", "bench objective", "--objective", "bench objective", "--state-root", s, "--children", CHILDREN];
const trackMixedShuffled = (s) => ["run", "--assume-defaults", "--track", `--state-root=${s}`, "--objective=bench objective", `--project=${PROJ}`];
const flowShuffled = (s) => ["run", "--results", join(FIX, "results-flat64.json"), "--flowchart", join(FIX, "flow-flat64.json"), "--state-root", s, "--objective", "bench objective", "--project", PROJ];

const cells = [
  // Bearing set re-anchor (R9-I..R25-I lineage; canonical forms).
  { id: "version", args: () => ["--version"], state: undefined },
  { id: "run-children@builtin", state: "state-builtin", args: childrenArgs },
  { id: "run-track@builtin", state: "state-builtin", args: trackArgs },
  { id: "run-children@custom", state: "state-custom", args: childrenArgs },
  { id: "run-track@custom", state: "state-custom", args: trackArgs },
  { id: "run-flowchart@custom-flat64", state: "state-custom", args: flowResultsArgs },
  { id: "run-flowchart@builtin-flat64", state: "state-builtin", args: flowResultsArgs },
  // NEW axis: dispatch alias arms of the floor cell (same handler, different
  // surface token; "-V" and bare "version" were never spawned in any round).
  { id: "version-shortV", args: () => ["-V"], state: undefined },
  { id: "version-word", args: () => ["version"], state: undefined },
  // NEW axis: separator-form dose across config states (=-form halves the
  // token count 9 -> 5; parseArgs splits on the first "=").
  { id: "run-children@builtin+eqForm", state: "state-builtin", args: childrenEqForm },
  { id: "run-children@custom+eqForm", state: "state-custom", args: childrenEqForm },
  // NEW axis: flag-order permutation dose across config states.
  { id: "run-children@builtin+shuffled", state: "state-builtin", args: childrenShuffled },
  { id: "run-children@custom+shuffled", state: "state-custom", args: childrenShuffled },
  // NEW axis: duplicate-flag occurrence (last-wins, same values; 9 -> 13 tokens).
  { id: "run-children@builtin+dupSame", state: "state-builtin", args: childrenDupSame },
  // NEW axis: command-class cross — track with mixed =-form + boolean-first
  // order; flowchart with fully reversed flag order at custom state.
  { id: "run-track@builtin+mixedShuffled", state: "state-builtin", args: trackMixedShuffled },
  { id: "run-flowchart@custom-flat64+shuffled", state: "state-custom", args: flowShuffled }
];

function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, state), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    delete env.PI_FAST_MODEL; delete env.SPARKLE_AUTO_ADAPT; delete env.PI_THINKING_LEVEL;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 180000 });
    const dt = performance.now() - t0;
    exit = r.status;
    const want = cell.expectExit ?? 0;
    if (i === 0 && r.status !== want) console.error(`  FIRST-UNEXPECTED ${cell.id}: exit=${r.status} ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

### A.3 进程内解析段分解（/tmp/r26i-bench/inproc.mjs，双引擎各执行一次）

```javascript
// R26-I in-process decomposition of the argv surface-form face.
// Prices each parse-plane segment the form cells exercise:
//   (a) node:util parseArgs over the run command's exact option table
//       (replica of main.ts L514-532) at four surface forms of the SAME
//       bearing values: canonical space-form (9 tokens), =-form (5 tokens),
//       reversed order (9 tokens), duplicate last-wins (13 tokens);
//   (b) the resume command's option table with `multiple: true` --selected at
//       realistic (3 repeats + csv) and adversarial (200 repeats) geometry,
//       plus the slice-owned collectSelectedActionIds merge on top;
//   (c) whole-dispatch price of the alias arms: main(["--version"]) vs
//       main(["-V"]) vs main(["version"]) and main(["help"]) vs
//       main(["--help"]) against an in-memory CliIo sink (this includes the
//       readFileSync(package.json) the version handler performs per call —
//       an upper bound on the dispatch switch itself, which is branch-only).
// The I-slice share of the surface-form face is the dispatch switch plus the
// option tables handed to parseArgs; tokenization/normalization is
// node:util engine territory.
import { performance } from "node:perf_hooks";
import { parseArgs } from "node:util";
import { collectSelectedActionIds } from "/workspace/dist/cli/flowchart-io.js";
import { main } from "/workspace/dist/cli/main.js";

function bench(label, n, fn) {
  for (let i = 0; i < Math.min(n, 2000); i++) fn(i);
  const t0 = performance.now();
  let sink = 0;
  for (let i = 0; i < n; i++) sink += fn(i);
  const per = ((performance.now() - t0) / n) * 1e6;
  console.log(`${label}: ${per.toFixed(1)}ns/call (sink=${sink})`);
}

// (a) run-command option table (replica of main.ts runCommand L514-532).
const runOptions = {
  project: { type: "string" },
  objective: { type: "string" },
  "state-root": { type: "string" },
  executor: { type: "string" },
  children: { type: "string" },
  flowchart: { type: "string" },
  results: { type: "string" },
  track: { type: "boolean", default: false },
  "primary-model": { type: "string" },
  "fast-model": { type: "string" },
  "assume-defaults": { type: "boolean", default: false },
  answers: { type: "string" },
  "public-prior": { type: "string" },
  "require-public-prior": { type: "boolean", default: false }
};
const PROJ = "/tmp/r26i-bench/fixtures/project";
const S = "/tmp/r26i-bench/work/x";
const CH = "/tmp/r26i-bench/fixtures/children.json";
const canonical = ["--project", PROJ, "--objective", "bench objective", "--state-root", S, "--children", CH];
const eqForm = [`--project=${PROJ}`, "--objective=bench objective", `--state-root=${S}`, `--children=${CH}`];
const reversed = ["--children", CH, "--state-root", S, "--objective", "bench objective", "--project", PROJ];
const dupSame = ["--project", PROJ, "--project", PROJ, "--objective", "bench objective", "--objective", "bench objective", "--state-root", S, "--children", CH];
const N = 200000;
bench("parseArgs run-table canonical (8 tokens)", N, () => parseArgs({ args: canonical, options: runOptions }).values.track === false ? 1 : 0);
bench("parseArgs run-table =-form (4 tokens)", N, () => parseArgs({ args: eqForm, options: runOptions }).values.track === false ? 1 : 0);
bench("parseArgs run-table reversed (8 tokens)", N, () => parseArgs({ args: reversed, options: runOptions }).values.track === false ? 1 : 0);
bench("parseArgs run-table dup last-wins (12 tokens)", N, () => parseArgs({ args: dupSame, options: runOptions }).values.track === false ? 1 : 0);

// (b) resume-table multiple:true geometry + slice-owned merge.
const resumeOptions = {
  run: { type: "string" },
  "state-root": { type: "string" },
  supervised: { type: "boolean", default: false },
  executor: { type: "string" },
  results: { type: "string" },
  selected: { type: "string", multiple: true },
  "selected-ids": { type: "string" },
  text: { type: "string" },
  unpause: { type: "boolean", default: false }
};
const realistic = ["--run", "run_x", "--selected", "act_1", "--selected", "act_2", "--selected", "act_3", "--selected-ids", "act_4, act_5 ,act_2"];
const adversarial = ["--run", "run_x"];
for (let i = 0; i < 200; i++) adversarial.push("--selected", `act_${i % 150}`);
bench("parseArgs resume-table realistic (3 repeats + csv)", N, () => parseArgs({ args: realistic, options: resumeOptions }).values.selected.length);
bench("parseArgs resume-table adversarial (200 repeats)", 20000, () => parseArgs({ args: adversarial, options: resumeOptions }).values.selected.length);
{
  const parsedRealistic = parseArgs({ args: realistic, options: resumeOptions }).values;
  bench("collectSelectedActionIds realistic (3+3, 1 dup)", N, () => collectSelectedActionIds(parsedRealistic.selected, parsedRealistic["selected-ids"]).length);
  const parsedAdv = parseArgs({ args: adversarial, options: resumeOptions }).values;
  bench("collectSelectedActionIds adversarial (200 -> 150 dedup)", 20000, () => collectSelectedActionIds(parsedAdv.selected, undefined).length);
}

// (c) whole-dispatch alias arms against an in-memory sink (upper bound on the
// switch: version arms re-read package.json per call; help arms write USAGE).
function sinkIo() { let bytes = 0; return { stdout: (t) => { bytes += t.length; }, stderr: (t) => { bytes += t.length; }, bytes: () => bytes }; }
for (const arm of [["--version"], ["-V"], ["version"], ["help"], ["--help"]]) {
  const io = sinkIo();
  let n = 20000;
  for (let i = 0; i < 2000; i++) await main(arm, io);
  const t0 = performance.now();
  for (let i = 0; i < n; i++) await main(arm, io);
  const per = ((performance.now() - t0) / n) * 1e6;
  console.log(`main([${arm.join(",")}]) whole-dispatch: ${per.toFixed(0)}ns/call (bytes=${io.bytes()})`);
}
```

原始输出：

```
== v22.14.0 ==
parseArgs run-table canonical (8 tokens): 4207.9ns/call (sink=200000)
parseArgs run-table =-form (4 tokens): 4028.9ns/call (sink=200000)
parseArgs run-table reversed (8 tokens): 3923.8ns/call (sink=200000)
parseArgs run-table dup last-wins (12 tokens): 4483.5ns/call (sink=200000)
parseArgs resume-table realistic (3 repeats + csv): 3900.3ns/call (sink=600000)
parseArgs resume-table adversarial (200 repeats): 63157.5ns/call (sink=4000000)
collectSelectedActionIds realistic (3+3, 1 dup): 348.0ns/call (sink=1000000)
collectSelectedActionIds adversarial (200 -> 150 dedup): 6037.0ns/call (sink=3000000)
main([--version]) whole-dispatch: 7362ns/call (bytes=132000)
main([-V]) whole-dispatch: 7168ns/call (bytes=132000)
main([version]) whole-dispatch: 7072ns/call (bytes=132000)
main([help]) whole-dispatch: 221ns/call (bytes=130988000)
main([--help]) whole-dispatch: 242ns/call (bytes=130988000)
== v22.22.2 ==
parseArgs run-table canonical (8 tokens): 4022.3ns/call (sink=200000)
parseArgs run-table =-form (4 tokens): 3996.6ns/call (sink=200000)
parseArgs run-table reversed (8 tokens): 3997.5ns/call (sink=200000)
parseArgs run-table dup last-wins (12 tokens): 4566.8ns/call (sink=200000)
parseArgs resume-table realistic (3 repeats + csv): 3828.6ns/call (sink=600000)
parseArgs resume-table adversarial (200 repeats): 63602.9ns/call (sink=4000000)
collectSelectedActionIds realistic (3+3, 1 dup): 366.3ns/call (sink=1000000)
collectSelectedActionIds adversarial (200 -> 150 dedup): 5940.4ns/call (sink=3000000)
main([--version]) whole-dispatch: 6877ns/call (bytes=132000)
main([-V]) whole-dispatch: 7148ns/call (bytes=132000)
main([version]) whole-dispatch: 6701ns/call (bytes=132000)
main([help]) whole-dispatch: 223ns/call (bytes=130988000)
main([--help]) whole-dispatch: 267ns/call (bytes=130988000)
```

### A.4 行为见证（/tmp/r26i-bench/equiv.mjs，双引擎各执行一次）

```javascript
// R26-I behavioral witnesses for the argv surface-form face.
//   W1 order invariance: children run with fully reversed flag order must be
//      normalized-identical to the canonical order (builtin + custom states).
//   W2 separator-form invariance: --flag=value serialization must be
//      normalized-identical to --flag value (builtin + custom states).
//   W3 alias invariance: --version / -V / version produce byte-identical
//      stdout and exit 0; help / --help / -h produce byte-identical USAGE.
//   W4 duplicate last-wins: duplicated flags with identical values are
//      normalized-identical to canonical e2e; in-process parseArgs probe
//      proves deterministic last-wins for differing duplicate values.
//   W5 --selected geometry: repeat order preserved, first-occurrence dedup,
//      CSV whitespace trim, repeats-before-csv merge order (slice-owned
//      collectSelectedActionIds semantics), all deterministic across runs.
// Each e2e witness runs 3x; ids and state paths are normalized before diff.
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, cpSync } from "node:fs";
import { join } from "node:path";
import { parseArgs } from "node:util";
import { collectSelectedActionIds } from "/workspace/dist/cli/flowchart-io.js";

const CLI = "/workspace/dist/cli/main.js";
const FIX = "/tmp/r26i-bench/fixtures";
const WORK = "/tmp/r26i-bench/equiv-work";
const PROJ = join(FIX, "project");
const CHILDREN = join(FIX, "children.json");

function normalize(text, stateDir) {
  return text
    .replaceAll(stateDir, "<STATE>")
    .replace(/run_[0-9a-f-]+/g, "run_X")
    .replace(/ep_[0-9a-f-]+/g, "ep_X")
    .replace(/evd_[0-9a-f-]+/g, "evd_X")
    .replace(/cnd_[0-9a-f-]+/g, "cnd_X")
    .replace(/agt_[0-9a-f-]+/g, "agt_X");
}
function runArgs(state, args, tag, iter) {
  const dir = join(WORK, `${tag}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, state), dir, { recursive: true });
  const env = { ...process.env };
  delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
  delete env.PI_FAST_MODEL; delete env.SPARKLE_AUTO_ADAPT; delete env.PI_THINKING_LEVEL;
  const r = spawnSync(process.execPath, [CLI, ...args(dir)], { env, encoding: "utf8", timeout: 180000 });
  if (r.status !== 0) throw new Error(`${tag}-${iter} exit=${r.status}: ${r.stderr}`);
  return normalize(r.stdout, dir);
}
function bare(args) {
  const r = spawnSync(process.execPath, [CLI, ...args], { encoding: "utf8", timeout: 180000 });
  return { out: r.stdout, err: r.stderr, status: r.status };
}

const canonical = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN];
const eqForm = (s) => ["run", `--project=${PROJ}`, "--objective=bench objective", `--state-root=${s}`, `--children=${CHILDREN}`];
const shuffled = (s) => ["run", "--children", CHILDREN, "--state-root", s, "--objective", "bench objective", "--project", PROJ];
const dupSame = (s) => ["run", "--project", PROJ, "--project", PROJ, "--objective", "bench objective", "--objective", "bench objective", "--state-root", s, "--children", CHILDREN];

rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
let checks = 0, failures = 0;
function assertEq(a, b, label) {
  checks++;
  if (a !== b) { failures++; console.error(`FAIL ${label}`); }
}

for (let i = 0; i < 3; i++) {
  for (const state of ["state-builtin", "state-custom"]) {
    const base = runArgs(state, canonical, `${state}-canon`, i);
    // W1 order invariance
    assertEq(base, runArgs(state, shuffled, `${state}-shuffled`, i), `W1 order ${state} run${i}`);
    // W2 separator-form invariance
    assertEq(base, runArgs(state, eqForm, `${state}-eqform`, i), `W2 eq-form ${state} run${i}`);
    // W4 duplicate last-wins (same values) e2e
    if (state === "state-builtin") {
      assertEq(base, runArgs(state, dupSame, `${state}-dupsame`, i), `W4 dup-same ${state} run${i}`);
    }
  }
}

// W3 alias invariance (single-shot; deterministic outputs, no state).
{
  const v1 = bare(["--version"]), v2 = bare(["-V"]), v3 = bare(["version"]);
  assertEq(v1.out, v2.out, "W3 version vs -V stdout");
  assertEq(v1.out, v3.out, "W3 version vs word stdout");
  assertEq(`${v1.status},${v2.status},${v3.status}`, "0,0,0", "W3 version exits");
  const h1 = bare(["help"]), h2 = bare(["--help"]), h3 = bare(["-h"]);
  assertEq(h1.out, h2.out, "W3 help vs --help stdout");
  assertEq(h1.out, h3.out, "W3 help vs -h stdout");
  assertEq(`${h1.status},${h2.status},${h3.status}`, "0,0,0", "W3 help exits");
}

// W4b in-process last-wins determinism for differing duplicate values.
{
  const options = { objective: { type: "string" }, project: { type: "string" } };
  const parsed = parseArgs({ args: ["--objective", "first", "--project", "p", "--objective", "second"], options });
  assertEq(parsed.values.objective, "second", "W4b parseArgs last-wins");
  const again = parseArgs({ args: ["--objective", "first", "--project", "p", "--objective", "second"], options });
  assertEq(parsed.values.objective, again.values.objective, "W4b last-wins deterministic");
}

// W5 --selected merge geometry (slice-owned semantics, deterministic).
{
  const merged = collectSelectedActionIds(["b", "a", "b"], " c , a ,, d ");
  assertEq(JSON.stringify(merged), JSON.stringify(["b", "a", "c", "d"]), "W5 order+dedup+trim+merge");
  const repeatsOnly = collectSelectedActionIds(["z", "y"], undefined);
  assertEq(JSON.stringify(repeatsOnly), JSON.stringify(["z", "y"]), "W5 repeat order preserved");
  const csvOnly = collectSelectedActionIds(undefined, "k,j");
  assertEq(JSON.stringify(csvOnly), JSON.stringify(["k", "j"]), "W5 csv order preserved");
  assertEq(collectSelectedActionIds(undefined, undefined), undefined, "W5 omitted-both is undefined");
  assertEq(JSON.stringify(collectSelectedActionIds(undefined, " , ")), "[]", "W5 explicit-empty csv is []");
}

console.log(`equiv checks=${checks} failures=${failures}`);
if (failures > 0) process.exit(1);
```

输出（22.22.2 与 22.14.0 各一次）：

```
equiv checks=28 failures=0
equiv checks=28 failures=0
```

### A.5 三次运行原始输出（spot-all.txt）

```
### run1
== v22.14.0 ==
version: median=57.3ms min=55.1 p90=58.3 exit=0
run-children@builtin: median=112.1ms min=107.2 p90=116.3 exit=0
run-track@builtin: median=128.2ms min=120.1 p90=135.9 exit=0
run-children@custom: median=155.5ms min=148.3 p90=170.0 exit=0
run-track@custom: median=177.3ms min=169.1 p90=190.1 exit=0
run-flowchart@custom-flat64: median=266.6ms min=252.1 p90=273.9 exit=0
run-flowchart@builtin-flat64: median=217.4ms min=205.7 p90=228.1 exit=0
version-shortV: median=56.4ms min=54.4 p90=58.0 exit=0
version-word: median=56.2ms min=54.4 p90=56.7 exit=0
run-children@builtin+eqForm: median=111.7ms min=106.5 p90=118.6 exit=0
run-children@custom+eqForm: median=160.8ms min=151.9 p90=169.3 exit=0
run-children@builtin+shuffled: median=110.2ms min=107.2 p90=116.4 exit=0
run-children@custom+shuffled: median=156.5ms min=152.9 p90=170.7 exit=0
run-children@builtin+dupSame: median=111.8ms min=105.9 p90=114.7 exit=0
run-track@builtin+mixedShuffled: median=127.5ms min=121.6 p90=130.9 exit=0
run-flowchart@custom-flat64+shuffled: median=264.9ms min=244.2 p90=272.3 exit=0
== v22.22.2 ==
version: median=51.5ms min=49.1 p90=53.8 exit=0
run-children@builtin: median=108.7ms min=103.2 p90=116.7 exit=0
run-track@builtin: median=124.1ms min=118.3 p90=129.9 exit=0
run-children@custom: median=132.7ms min=129.6 p90=135.0 exit=0
run-track@custom: median=157.0ms min=146.2 p90=172.6 exit=0
run-flowchart@custom-flat64: median=236.6ms min=224.7 p90=247.7 exit=0
run-flowchart@builtin-flat64: median=224.6ms min=212.3 p90=240.7 exit=0
version-shortV: median=55.6ms min=53.2 p90=59.1 exit=0
version-word: median=54.9ms min=52.2 p90=56.4 exit=0
run-children@builtin+eqForm: median=113.4ms min=109.5 p90=118.0 exit=0
run-children@custom+eqForm: median=136.5ms min=134.2 p90=140.3 exit=0
run-children@builtin+shuffled: median=113.9ms min=110.3 p90=118.9 exit=0
run-children@custom+shuffled: median=142.8ms min=135.7 p90=146.8 exit=0
run-children@builtin+dupSame: median=109.7ms min=104.7 p90=116.4 exit=0
run-track@builtin+mixedShuffled: median=128.1ms min=121.5 p90=132.6 exit=0
run-flowchart@custom-flat64+shuffled: median=246.4ms min=234.9 p90=260.5 exit=0
### run2
== v22.14.0 ==
version: median=56.5ms min=55.7 p90=59.4 exit=0
run-children@builtin: median=110.2ms min=108.9 p90=114.2 exit=0
run-track@builtin: median=135.6ms min=128.3 p90=142.3 exit=0
run-children@custom: median=171.9ms min=147.7 p90=183.2 exit=0
run-track@custom: median=184.1ms min=172.9 p90=187.5 exit=0
run-flowchart@custom-flat64: median=263.0ms min=250.7 p90=272.3 exit=0
run-flowchart@builtin-flat64: median=215.8ms min=207.2 p90=224.8 exit=0
version-shortV: median=55.1ms min=52.3 p90=56.3 exit=0
version-word: median=55.2ms min=53.0 p90=55.8 exit=0
run-children@builtin+eqForm: median=108.4ms min=104.6 p90=111.3 exit=0
run-children@custom+eqForm: median=155.1ms min=144.4 p90=163.5 exit=0
run-children@builtin+shuffled: median=109.3ms min=107.7 p90=111.0 exit=0
run-children@custom+shuffled: median=158.0ms min=147.0 p90=166.4 exit=0
run-children@builtin+dupSame: median=109.3ms min=105.3 p90=111.9 exit=0
run-track@builtin+mixedShuffled: median=129.9ms min=127.5 p90=135.0 exit=0
run-flowchart@custom-flat64+shuffled: median=275.9ms min=267.4 p90=286.6 exit=0
== v22.22.2 ==
version: median=55.8ms min=52.0 p90=57.5 exit=0
run-children@builtin: median=115.0ms min=106.3 p90=123.3 exit=0
run-track@builtin: median=130.8ms min=126.3 p90=135.1 exit=0
run-children@custom: median=142.8ms min=137.5 p90=147.2 exit=0
run-track@custom: median=156.5ms min=150.7 p90=162.5 exit=0
run-flowchart@custom-flat64: median=250.0ms min=237.4 p90=262.8 exit=0
run-flowchart@builtin-flat64: median=221.8ms min=209.1 p90=228.0 exit=0
version-shortV: median=52.3ms min=51.6 p90=54.0 exit=0
version-word: median=52.6ms min=50.5 p90=54.1 exit=0
run-children@builtin+eqForm: median=108.4ms min=102.5 p90=110.6 exit=0
run-children@custom+eqForm: median=132.9ms min=124.8 p90=136.0 exit=0
run-children@builtin+shuffled: median=107.7ms min=103.5 p90=116.1 exit=0
run-children@custom+shuffled: median=141.7ms min=131.6 p90=148.3 exit=0
run-children@builtin+dupSame: median=113.7ms min=104.7 p90=118.1 exit=0
run-track@builtin+mixedShuffled: median=135.1ms min=127.4 p90=138.3 exit=0
run-flowchart@custom-flat64+shuffled: median=261.9ms min=250.6 p90=272.1 exit=0
### run3
== v22.14.0 ==
version: median=58.8ms min=55.5 p90=71.1 exit=0
run-children@builtin: median=117.1ms min=114.0 p90=119.4 exit=0
run-track@builtin: median=138.9ms min=131.0 p90=150.2 exit=0
run-children@custom: median=170.1ms min=154.3 p90=179.4 exit=0
run-track@custom: median=191.5ms min=182.0 p90=200.9 exit=0
run-flowchart@custom-flat64: median=265.4ms min=256.4 p90=277.9 exit=0
run-flowchart@builtin-flat64: median=218.3ms min=208.0 p90=227.8 exit=0
version-shortV: median=55.9ms min=55.3 p90=56.6 exit=0
version-word: median=57.1ms min=54.8 p90=59.7 exit=0
run-children@builtin+eqForm: median=117.6ms min=108.5 p90=127.0 exit=0
run-children@custom+eqForm: median=164.3ms min=158.4 p90=172.0 exit=0
run-children@builtin+shuffled: median=118.3ms min=113.6 p90=124.5 exit=0
run-children@custom+shuffled: median=161.5ms min=144.3 p90=170.5 exit=0
run-children@builtin+dupSame: median=111.1ms min=107.8 p90=120.8 exit=0
run-track@builtin+mixedShuffled: median=130.2ms min=122.4 p90=134.8 exit=0
run-flowchart@custom-flat64+shuffled: median=269.6ms min=255.3 p90=278.4 exit=0
== v22.22.2 ==
version: median=52.5ms min=50.5 p90=53.3 exit=0
run-children@builtin: median=109.5ms min=108.0 p90=110.6 exit=0
run-track@builtin: median=126.7ms min=124.5 p90=128.2 exit=0
run-children@custom: median=136.8ms min=133.1 p90=138.8 exit=0
run-track@custom: median=163.6ms min=157.4 p90=176.5 exit=0
run-flowchart@custom-flat64: median=250.7ms min=239.2 p90=255.4 exit=0
run-flowchart@builtin-flat64: median=226.0ms min=218.1 p90=236.3 exit=0
version-shortV: median=52.0ms min=50.3 p90=53.4 exit=0
version-word: median=52.2ms min=50.6 p90=53.6 exit=0
run-children@builtin+eqForm: median=109.8ms min=107.6 p90=113.1 exit=0
run-children@custom+eqForm: median=134.3ms min=126.2 p90=137.7 exit=0
run-children@builtin+shuffled: median=108.5ms min=106.1 p90=110.5 exit=0
run-children@custom+shuffled: median=135.5ms min=129.1 p90=141.8 exit=0
run-children@builtin+dupSame: median=109.7ms min=105.5 p90=119.9 exit=0
run-track@builtin+mixedShuffled: median=129.1ms min=119.9 p90=138.7 exit=0
run-flowchart@custom-flat64+shuffled: median=250.3ms min=232.7 p90=256.6 exit=0
```

### A.6 stage 标签取证（smoke）与 r7i part D 复跑摘录

```
$ node dist/cli/main.js run --project <proj> --objective x --state-root <s> --children <spec> --no-such-flag
error: Unknown option '--no-such-flag'
  command: run
  stage: execute            # engine TypeError via outer catch (L1478-1485)

$ node dist/cli/main.js run
error: run requires --project <path> and --objective <text>
  command: run
  stage: parse-args         # slice-owned cliFail (L536-542)

$ node dist/cli/main.js run --project <proj> stray-positional --objective x
error: Unexpected argument 'stray-positional'. This command does not take positional arguments
  command: run
  stage: execute            # engine reject via outer catch
```

（三端点价格 R17-I 已收口贴地板；stage 三分不对称为 §1.4 移交注记。）

```
r7i part D: load probe — builtin build leaves providers/all cold (69.0ms), custom fallback loads it (3.4ms cache-hit)
r7i part E: configured run --children battery — normalized-identical across rounds
checks=80 failures=0
```

MORE_OPTIMA=no
