# R27-H：evaluation/requirement/review/rubric 切片第二十七遍——中途撞轴降级存档（属性来源面 → R27-F）+ 新轴执行基质面（execution-substrate face，战役首创）定价收口——空枚举

- 基线：`cursor/sota-persistent-opt-83a1` @ `d2d51b3`（开工时 exclusive tip——R27-E 空枚举收口铸 **RVL** + 本 R27-H 派发）；运行中 exclusive **三波** docs-only 快进：第一波 `4fafd0c`（R27-F 空枚举收口——**输入访问路径机制面**（input access-path mechanics face，F 切片 `src/experiments/`）——+ R27-I 派发），第二波 `efed712`（R27-G 空枚举收口——**join 策略面**（join-policy face，G 切片）——+ R27-J 派发），第三波于成稿提交前吸收两个提交 `2994d2b`（R27-I 空枚举收口——**进程输出通道配置面**（process output-channel disposition face，I 切片 spawn fd 表通道）——+ Round 28 开卷）与 `9e51aa9`（R27-J 空枚举收口——**SIDEC** feedback 删除面侧车存量携带（J 切片）——+ R28-B 派发）。每波 `git diff <prev>..<tip> --stat` 复核均仅触及 `docs/reports/sota-opt/`，`src/` 零变化；均按令 fast-forward 本分支、重验 21 文件冻结、并把新收口兄弟轴加入非移植清单（§4）。第一波快进（R27-F）与本轮先建的候选轴**撞轴**，处置见 §2；本轮全部测量在 `efed712` 上完成，第三波增量 `src/` 零变化 ⇒ 测量在字节相同的切片与 dist 上完成，全部有效。
- 分支：`cursor/r27-h-eval-twenty-seventh-pass-83a1`；仓库变更仅本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` = 本文件）。
- 模型：claude-fable-5-thinking-xhigh。
- 环境：Node v22.22.2（`engines: >=22.19.0` 满足）、tsx 4.23.12（内嵌 esbuild 0.28.2）、TypeScript 5.9.3（target ES2024 / module NodeNext）、pnpm 10.17.1。
- 前置阅读：README、EXCLUSIONS 全表、PROGRESS、round-27/PLAN（H 段 + 全部 plan-ban 轴，快进前后三版）、R26-H 全文（本切片上一轴 realm 面 + 中途撞轴降级先例——本轮§2 处置的模板）、R25-H / R24-H / R23-H / R22-H 关键段（证非补铸）、R27-E（RVL）全文、快进后 R27-F / R27-G 全文核读。

## 结论

**空枚举。零候选。不铸任何 S27-H-\* ID。** 这是 R12-H 起第十六个连续 H 空枚举。

1. **中途撞轴（§2，附录 C 存档）**：本轮先建先测的候选轴「消费级属性来源面」（六臂 PD/AC/NP/PITK/PX/PXH：own data / own accessor / null-prototype own / prototype-inherited / 透明 Proxy / Reflect-handler Proxy，剂量消费级记录容器的属性服务机制）在三轮测量收尾前遭遇 exclusive 快进 `4fafd0c` 摄取 **R27-F 输入访问路径机制面**——描述符类（data vs accessor）× 查找基（own / prototype-hosted / null-prototype）× 膜（none vs Proxy），与本轴六臂逐坐标同构（PD=data×own×无膜；AC=accessor×own×无膜；NP=data×null-proto×无膜；PITK=data×proto-hosted×无膜；PX/PXH=data×own×Proxy 两种 handler 形态）。F 先收口进 exclusive = campaign-first 归 F。按 R26-H 先例（其存储类普查撞 R26-F 同款处置）**降级存档**：数据与脚本随附录 C 留档，不主张、不铸 ID、不发明 ALLCAPS 昵称、不移植。存档读数即便未撞轴亦为 µs 拒收类（最重孤立臂 9.09–9.29 µs/run）。
2. **新轴（§3，战役首创，已定价收口）**：**执行基质面（execution-substrate face）**——同一 21 文件源码的**两条生产真实发射管线**作为受控变量：安装位二进制（`bin.pi-sparkle` → `dist/cli/main.js`，tsc ES2024 发射，普通 `node`）vs 开发位 CLI 与整个测试面（`dev`/`cli`/`test` 脚本 → `tsx src/...`，esbuild 按需变换）。二十六遍 H 的热层测量全部钉在单一基质坐标（tsx/src）；dist 自 R21-H 起仅作冷层探针载具、从未与 src 臂对照。三轮 × 每轮 12 个进程隔离子进程（每臂 6，T/D 交错）：**热层 11 腿 33/33 类内**（跨基质比 1.004–1.239，臂内 A/A 噪声地板 1.00–1.25，|Δ| 0.2–726 ns——全部亚 µs 拒收类）；**冷层基质税 +174–183 ms/进程**（tsx 装载器引导 + 全图变换）——量级达 tens–hundreds ms 但类别为 once-per-process CLI 税 = 明令拒收类，且其中 H 切片 9 模块仅占 +8.2–8.6 ms。**36 个子进程的语义奇偶校验 sha256 逐字节一致**（14 段输出，含 S5-H-1 语义三元组）——基质不泄漏进任何语义字节。轴完备定价并收口，无候选。
3. 锚点全数重测（§1）：默认态链 9005–9493 ns（R26-H 带 9215–9751 / 历史跨 VM 带 ~3.9–10.4 µs 内）；配置 A 7097–8241 ns、配置 B 3927–4733 ns；promotion 18.3–19.3 ns；四个零生产调用方命令类 ≤2.07 µs；冷首调 dist 中位 1636–1650 µs。生产调用面与 21 文件冻结零漂移。
4. 勘误两条（§0）：(a) R26-H「21 文件零 `instanceof` / `Array.isArray` / `getPrototypeOf` / `.constructor` 站点」声称过宽——实为 7 个 `Array.isArray` 站点（全部在门/precedence 消费级之外，realm-blind，不影响 R26-H realm 面判定，但普查行需更正）；(b) 切片三文件经由**切片外**域工厂 `createEventId()`/`nowIso()`（`src/domain/{ids,timestamp}.ts`）铸易变身份字段（`evt_` nonce + ISO 时钟）——21 文件自身零直接 Date/random 站点，跨进程语义奇偶校验须且仅须掩蔽这两类字段。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；无阈值 / 测试 / 公开签名 / 数据面变更。无赢家故未向仓库提交任何脚本资产（两组驱动全文进附录 A/C 并附 sha256，临时文件已删——附录 D）。

## §0 范围、读序、冻结与不变量核查

- **文件普查**：8+7+4+2 = 21 文件在位；`wc -l` 655（evaluation）+712（requirement）+309（review）+74（rubric）= **1750**，与 R26-H 逐组一致。
- **冻结**：`git diff fd437a9..HEAD -- <21 文件>` 输出为空——在开工 tip `d2d51b3`、快进 `4fafd0c`、快进 `efed712`、快进 `9e51aa9` 四个 tip 上各核一次，均空。二十七遍 H 未触 `src/`。
- **S5-H-1**：`src/requirement/precedence.ts` L24 `if (fast.length === 0 || slow.length === 0) return [];` 逐字在位。语义三元组在**两个基质**上各 attested（fast-only → []、slow-only → []、fast+slow → 恰 1 conflict；附录 A 子驱动 checks，36 进程全过）。发射存活性（§3.4）：tsc 打印器把该单行 if 折为两行（条件与 `return [];` 逐字保留、语义不变），esbuild 逐字保留单行——两发射均含守卫，check 三轮全过。
- **生产调用面复核**（rg import 搜索，本 tip 实测）：`adaptation/{promotion,promotion-rules}` 仅 import `review/self-review`；`run/{coordinator,flowchart-run,supervisor}` + `track/{clarify,loop,plan,primary-split}` import requirement 模块。`src/evaluation/` 全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、`src/rubric/` 两文件**零生产调用方**——与 R3-H…R26-H 恒定。
- **门禁**：`pnpm gate` 于测量 tip 上全绿——typecheck ✓、lint ✓、test ✓（**tests 1169：1168 pass / 1 skipped / 0 fail / 78 suites**）、build ✓（build 产物 `dist/` 即 §3 dist 臂被测物；`dist/` 在 .gitignore，构建不脏仓库）。
- **勘误 (a)**：21 文件内 `Array.isArray` 共 **7 站点**——`requirement/provenance.ts` L17、L19；`evaluation/diff-adapter.ts` L74–77；`evaluation/check-adapter.ts` L101（`instanceof` / `getPrototypeOf` / `.constructor` 确为零）。R26-H 的零声称应缩窄为「门/precedence 消费级模块零品牌检查站点」——7 站点全部位于 provenance 记账与两个零生产调用方适配器的边界校验，且 `Array.isArray` realm-blind，R26-H realm 面判定不受影响。
- **勘误/机制注记 (b)**：`evaluation/evaluator.ts` L84/L94、`review/pairwise.ts` L44/L50、`review/critic.ts` L33/L38/L60/L65 经由域工厂 `createEventId()` / `nowIso()`（切片外 `src/domain/{ids,timestamp}.ts`）铸 `evt_` nonce 与 `createdAt` ISO 时钟；21 文件自身零直接时钟/随机站点（rg `Date.now|new Date|Math.random|randomUUID|crypto|hrtime`：零命中）。跨进程/跨基质语义比对必须掩蔽恰这两类字段（附录 A 子驱动 `canon()`），其余全部输出字节要求逐字一致——36 进程实测一致。

## §1 锚点重测（本 VM，历史仪器：tsx/src 基质、进程内、附录 C 父驱动三轮）

| 锚 | run1 | run2 | run3 | R26-H 参照带 |
|---|---|---|---|---|
| 默认态链（extract+gate+precedence，Q=0） | 9493 ns | 9005 ns | 9450 ns | 9215–9751 ns（历史跨 VM ~3.9–10.4 µs） |
| 配置 A（habits，Q=1 gated） | 7828 ns | 7097 ns | 8241 ns | ~7.00–7.84 µs（run3 略出上沿，VM 漂移注记） |
| 配置 B（vague，Q=3 gated） | 3939 ns | 4733 ns | 3927 ns | ~3.98–4.81 µs |
| promotion 类（生产） | 18.4 ns | 19.3 ns | 18.3 ns | ~17.8–19.9 ns |
| evaluate 类（零生产调用方） | 1094 ns | 1085 ns | 1136 ns | 1.102–1.155 µs |
| critique 类（run-start 链内） | 593 ns | 605 ns | 601 ns | 0.599–0.623 µs |
| pairwise 类（零生产调用方） | 2036 ns | 2057 ns | 2069 ns | 1.992–2.624 µs |
| reconcile 类（零生产调用方） | 66 ns | 66 ns | 73 ns | 64–67 ns |
| 冷首调 dist 默认（11 进程中位） | 1650 µs | 1636 µs | 1641 µs | 1669–1748 µs（同 VM 类，once-per-process 拒收） |
| 冷首调 dist 配置（11 进程中位） | 1605 µs | 1599 µs | 1587 µs | — |

三轮 CHECKS 37/37 全过。默认态仍是保守锚；配置态 × 命令类矩阵（R7-I 义务）全数复测且全部落在历史带附近。四个零生产调用方命令类 ≤2.07 µs——即便被生产接线也不构成候选池。

## §2 中途撞轴：消费级属性来源面 → 降级存档（不主张、不铸 ID、不移植）

- **本轮先建的候选轴**：消费级记录容器（contract / criterion / assumption / question / task / task-criterion / options）的**属性服务机制**六臂——PD（own data，历轮 100% 夹具坐标 + 生产 100% 构造形态）、AC（own accessor getter）、NP（null-prototype own data）、PITK（prototype-inherited）、PX（透明 Proxy）、PXH（Reflect-handler Proxy）。novelty 自查当时通过：历轮 Proxy 仅作死区追踪（S6-H-2/S7-H-2 形态），accessor/inherited/null-proto 从未作操作数类剂量。驱动建成并完成三轮测量（进程隔离 RX 六子 + 冷层 11 进程 + PI-CT 行为探针 + ROT 污染视图 + E0 残留探针）。
- **撞轴判定**：快进 `4fafd0c` 摄取 R27-F——**输入访问路径机制面**：描述符类（data vs accessor）× 查找基（own-literal / prototype-hosted / null-prototype）× 膜（none / 透明 Proxy），在 F 切片操作数上定价。本轴六臂与该三维格逐坐标同构（映射见结论第 1 条）；受控变量同为「调用方提供容器的属性访问机理」，仅切片不同。**同一机制格在另一切片再定价 = 移植**，plan-ban 明令禁止。F 先收口 = campaign-first 归 F。
- **处置（R26-H 先例逐条执行）**：降级存档。数据与脚本随附录 C 留档；不以任何量值主张「轴收口」；不铸 ID；不发明 ALLCAPS 昵称；本报告不把该面列为本轮新轴。
- **存档要点**（不判定，仅供 F 轴族横向印证）：RX 进程隔离 P1（默认门+precedence）PD 1294–1449 ns/run，最重臂 PXH 9090–9290 ns/run（比 6.3–7.2×）；P3（配置态、仅 options 记录剂量）全臂比 ≤1.68——剂量面越窄税越薄；**PI-CT 行为探针**：原型继承 contract 结构类型合法，但门的 `{...contract}` own-enumerable 展开缝丢继承字段 → **确定性 TypeError = fail-closed 拒收方向**（与 R27-F 的 PITK 行为观测同向，独立复现、不移植其结论）；E0 进程内 IC 残留 +505–532 ns（61.5–64.3%）——印证进程隔离纪律的必要性。最重孤立臂 9.29 µs/run，需 ~1076 次消费执行才越 10 ms 地板（调用图每 run 恰执行一次组合）——即便未撞轴亦为 µs 拒收类。

## §3 新轴：执行基质面（execution-substrate face，战役首创，已定价收口）

### §3.1 定义与生产通量

受控变量：**执行 H 切片热义务的 JavaScript 由哪条发射/装载管线产出**。`package.json` 把两条管线都船运为生产真实入口，作用于同一批 21 个源文件：

- **dist 臂**：`tsc -p tsconfig.build.json`（TS 5.9.3，target ES2024，module NodeNext，rootDir src → outDir dist）→ 磁盘 `dist/**/*.js`，普通 `node` 执行。生产入口：`bin.pi-sparkle: "dist/cli/main.js"`（安装位二进制）；`files` 数组只船运 `dist`——**npm 安装用户 100% 走此基质**。
- **src 臂**：`tsx` 4.23.12 装载器钩子（内嵌 esbuild 0.28.2）按需变换 `src/**/*.ts` → 内存 ESM，`node --import tsx` 执行。生产入口：`dev` / `cli` 脚本（开发位 CLI）与 `test`（`tsx --test`，**整个测试面**）——仓库内开发与 CI 100% 走此基质。

两臂子进程夹具字节、reps、pass 结构、GC 屏障逐字相同；唯一变量 = 发射管线（tsc 打印器 emit vs esbuild transform）+ 装载路径（磁盘 .js vs 钩子内存变换）。

### §3.2 新颖性与正交性（逐条钉死）

- **新颖性**：rg 全库 `docs/reports/sota-opt/`——26 遍 H 的热层测量全部单基质（`npx tsx` on src）；`dist` 仅以 R21-H「QM/QT/冷层探针需要 dist」式的**载具注记**出现（R22-E 提及 tsx cli 路径同为运行注记），**无一轮把基质本身作受控变量对照定价**。本轮兄弟 A–G 亦无一碰基质维。
- **正交性 pins**：全部操作数坐标钉死在历史锚形——own data / 普通对象字面量 / packed+extensible / 主 realm / canon 字符串 / 默认基数（C=2、Q=0、T=5）/ 单一构造位点。逐邻座钉死：**vs §2 撞轴族（R27-F 访问路径机制面）**——那是操作数的属性访问机理，本轴操作数全程 PD 钉死、剂量的是**代码工件**，零交集；**vs R27-G join-policy face**——G 切片 join 语义，H 切片无 join 位点；**vs R27-A 数值类 / R27-B 批基数+前奏拦截 / R27-C PROBX / R27-D 树几何 / R27-E RVL**——无数值类、批基数、概率坐标、策略树、TASK_RESULT 裁决格剂量；**vs R26-H realm 面**——两臂同主 realm、同一 Node 二进制 v22.22.2（被剂量的是 JS 文本与装载钩子，非 VM 也非跨 realm 来源）；**vs R26-F 存储态 / R25-H 字符串表示 / R24-H 驻留 / R23-H 成员混合 / R22-H 调用上下文**——均钉死；**vs SHAPEK**——每臂单形状宇宙、进程隔离，无形状混合剂量；**vs 引擎版本/进程环境坐标**——Node 版本、env、argv 两臂逐字相同（仅 `R27H_SUBSTRATE` 选臂变量）。
- **静态发射普查**（结构定性）：21 文件 src=1771 计数行（`split("\n")` 含末行；`wc -l` 1750）/ 55113 B → tsc-dist=1122 行 / 42839 B → esbuild-emit=1097 行 / 36257 B。类型擦除后两发射器输出规模相近；tsc 重打印重排行、esbuild 保留原行架。S5-H-1 守卫行两发射语义逐字保留（§0）。

### §3.3 方法

- **子驱动**（附录 A2，310 行）：`R27H_SUBSTRATE` 选臂 → **imports 计时普查**（9 个 H 模块逐模块动态 import 计时，once-per-process）→ **冷首调**（extract→gate→precedence 首链）→ **语义奇偶签名**（14 段输出 canon 化后 sha256：三态契约、门通过路 + 拒收路（未解决 question 的确定性错误消息）、precedence 双策略（无冲突 + 冲突在场各双策略）、S5-H-1 三元组、critique / evaluate / pairwise / reconcile / promotion；勘误 (b) 的两类易变域工厂字段掩蔽，其余全字节入签名）→ **热层 11 腿**（chainDefault / gateDefault / precDefault / chainConfA / gateConfA / chainConfB 各 5 pass，promotion / evaluate / critique / pairwise / reconcile 各 3 pass；pass 间 GC 屏障；2000 次预热）。
- **父驱动**（附录 A1，258 行）：静态普查 + S5-H-1 发射存活 check → **12 子进程 T,D 交错生成**（每臂 6，进程隔离、每子全新 IC 宇宙），spawn-to-exit 墙钟计时 → 奇偶判定：12 sha 必须唯一 → 热层判定：臂内（6 子中位数的）跨基质比 vs 臂内偶/奇半样 **A/A 噪声地板**（类内判据：比 ≤ max(1.10, 1.5×地板) 或 |Δ| < 1 µs）→ 冷层汇总。
- **三次独立运行**（run1/2/3，各 12 子进程 = 共 36 子进程）。

### §3.4 结果（三轮全过：CHECKS 51/51 ×3；ADJUDICATIONS 11/11 类内 ×3）

**奇偶**：36/36 子进程语义签名 sha256 = `51eb658d4effc94325741c02700a17efdbe2462ba7695951d9ec603d05a2517d`，跨基质、跨轮逐字节一致。基质不泄漏进任何语义输出字节；S5-H-1 三元组两基质 attested ×36。

**热层**（每格 run1/run2/run3；读数 = 每臂 6 子进程各自中位数的臂中位，ns/call）：

| 腿 | 跨基质比 | A/A 地板 | max\|Δ\| | 方向 |
|---|---|---|---|---|
| chainDefault | 1.056 / 1.116 / 1.080 | 1.034 / 1.050 / 1.005 | 726 ns | src<dist ×3 |
| gateDefault | 1.053 / 1.042 / 1.057 | 1.028 / 1.002 / 1.026 | 30 ns | src<dist ×3 |
| precDefault | 1.019 / 1.021 / 1.019 | 1.016 / 1.008 / 1.005 | 5 ns | src<dist ×3 |
| chainConfA | 1.045 / 1.051 / 1.061 | 1.274 / 1.056 / 1.064 | 298 ns | 混合 |
| gateConfA | 1.069 / 1.058 / 1.058 | 1.023 / 1.027 / 1.010 | 45 ns | src<dist ×3 |
| chainConfB | 1.064 / 1.123 / 1.004 | 1.080 / 1.076 / 1.119 | 437 ns | 混合 |
| promotion | 1.215 / 1.156 / 1.008 | 1.077 / 1.117 / 1.245 | 4.5 ns | src<dist ×3 |
| evaluate | 1.046 / 1.004 / 1.005 | 1.018 / 1.007 / 1.006 | 43 ns | 混合 |
| critique | 1.069 / 1.055 / 1.092 | 1.005 / 1.028 / 1.031 | 24 ns | src<dist ×3 |
| pairwise | 1.031 / 1.039 / 1.006 | 1.032 / 1.025 / 1.025 | 70 ns | src<dist ×3 |
| reconcile | 1.218 / 1.239 / 1.193 | 1.044 / 1.175 / 1.117 | 13 ns | src<dist ×3 |

33/33 判定类内。诚实方向注记：多数腿三轮一致 **src（esbuild-emit）≤ dist（tsc-emit）**——chainDefault 三轮稳定快 5–11%（|Δ| 354–726 ns）。方向真实且可复现，但绝对量亚 µs/call = 拒收类；且「把船运工件从 tsc 发射换 esbuild 发射」是构建管线与产物（声明/映射文件）变更，非本切片代码变更，收益天花板即上表 |Δ|——不构成候选。

**冷层（once-per-process 拒收类，完备性定价）**：

| 量 | dist 臂 | src 臂 | 基质税（src−dist） |
|---|---|---|---|
| 9 个 H 模块 import | 5.1 / 5.1 / 5.1 ms | 13.3 / 13.6 / 13.4 ms | **+8.2 / +8.6 / +8.3 ms** |
| 冷首调（extract+gate+precedence） | 1725 / 1715 / 1717 µs | 1603 / 1620 / 1623 µs | −122 / −95 / −94 µs |
| 进程墙钟（spawn→exit） | 721 / 726 / 725 ms | 904 / 900 / 901 ms | **+183 / +174 / +176 ms** |

- 进程级基质税 +174–183 ms**量级达标但类别不达标**：once-per-process CLI 税 = 明令拒收类。且其构成主体是 tsx 装载器引导 + **切片外**全依赖图变换——H 切片 9 模块仅占 +8.2–8.6 ms（src 臂逐模块中位：heuristic 7.2 ms、pairwise 1.8、evaluator 1.1、coverage 1.1，其余 ≤0.7）。
- 冷首调 src 臂反快 ~95–122 µs（方向注记：esbuild 已变换代码在 import 阶段完成解析，dist 臂首链承担更多磁盘 .js 惰性编译错位）——once-per-process 类，不深究不主张。

### §3.5 判定

轴已完备定价并收口：热层亚 µs、冷层 once-per-process，两层均拒收类。**零候选，不铸 ID。** 按令不把「execution-substrate face」写入 EXCLUSIONS 表——父级计划禁令。

## §4 枚举收口（不补铸 / 不移植逐条对照）

- **具名禁令逐条遵守**：FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL——本轮两轴（新轴 + 存档轴）均不触及任何一条的受控变量（SHAPEK 与 realm/存储态邻界已在 §3.2 与附录 C 头注逐条钉死）。
- **无名计划禁令逐条遵守**：R26-H 操作数 realm 面（本轴单 realm 全程）；R25-H 字符串表示普查（全臂扁平 canon 串）；R24-H 驻留普查（共享池、身份钉死）；R23-H 注册表成员混合（默认注册表）；R22-H 调用上下文（固定调用几何）；R26-F 输入存储态面（packed+extensible 断言）；M=1 目录坍缩 / 审批权威面 / 状态序列化边界来源面 / 适应态坐标 / 值域-字符串内容类普查 / task 面可选字段在场拓扑 / 身份坐标面 / 计划标头身份几何面 / 注入命令种类面 / 进程环境坐标（env/argv 两臂逐字同）/ 排序locale面 / 隐私-供应商政策约束腿面 / 注入缝默认臂面 / 边条件种类面 / argv 表面形式面——全部域外或坐标钉死。
- **同轮兄弟轴不移植**：R27-A 标量数值类面（无数值类剂量）；R27-B 批基数底+前奏拦截面（域外）；R27-C **PROBX**（无概率/重要性权重物理对象；PATH_RE 维持 R16-H 拒收，本切片无程序化/对抗目标且无 ≥3.2K 无斜杠载荷）；R27-D 政策内容树几何面（域外）；R27-E **RVL**（本切片无 TASK_RESULT 铸造位点）；**R27-F 输入访问路径机制面**（撞轴处置见 §2——降级存档而非移植，本轮新轴操作数访问路径全程钉死 PD）；**R27-G join 策略面**（H 切片无 join 位点）；**R27-I 进程输出通道配置面**（成稿前快进摄取；I 切片 spawn fd 表通道——本切片 21 文件零 spawn / 零 stdio 写站点，本轮新轴两臂子进程 stdio 接线逐字相同【父驱动 spawnSync 默认双 pipe】非受控变量，零交集）；**R27-J SIDEC** feedback 删除面侧车存量携带（成稿前快进摄取；J 切片墓碑侧车契约——本切片无墓碑/侧车物理对象，零交集）。
- 本轮新轴昵称不入 EXCLUSIONS 表（父级所有权）。

## §5 门禁与仓库状态

- `pnpm gate` 全绿（§0）：typecheck ✓ lint ✓ test ✓（1168 pass / 1 skipped / 0 fail / 78 suites）build ✓。
- 仓库变更仅本报告一个文件；`scripts/` 零新增；无赢家故无 production 变更、无测试变更、无阈值变更。S5-H-1 在位且两基质 attested。
- 驱动脚本仅存 `/tmp` 与本报告附录（A/C），已按附录 D 清单删除。

## 附录 A：执行基质面驱动（完整，可复现；无赢家按纪律不入库）

### A1 父驱动 `/tmp/r27h-substrate.mts`（258 行，sha256 `024a5069ad5a56a2da1a3621c6d478cca7c4f5e7207d3955620fe9fc1e7ec480`）

运行：`pnpm build`（或 `pnpm gate`）后 `npx tsx /tmp/r27h-substrate.mts`。

```ts
/**
 * R27-H parent driver: EXECUTION-SUBSTRATE FACE (twenty-seventh pass — the
 * pass's NEW completeness axis, opened after the first candidate axis
 * collided with the mid-run R27-F ingest and was demoted to archive).
 *
 * Axis: WHICH EMIT/LOADER PIPELINE produced the JavaScript that executes the
 * H-slice's hot obligations. package.json ships BOTH substrates as
 * production-real entry points for the same 21 source files:
 *   dist arm  bin.pi-sparkle -> dist/cli/main.js  (tsc -p tsconfig.build.json,
 *             target ES2024, NodeNext; plain `node`)
 *   src arm   dev/cli/test scripts -> `tsx src/...` (esbuild transform,
 *             loader hooks; `node --import tsx`)
 * All 26 prior H passes measured exactly one substrate coordinate for the hot
 * tier (tsx/src; dist appeared only as R21-H's uncontrasted cold-probe
 * vehicle). The substrate has never been a controlled variable.
 *
 * Pinned coordinates (orthogonality to every named/unnamed plan-ban axis):
 * operand construction own-data/plain/packed (R27-F access-path face is the
 * OPERAND'S access mechanics — here operands are pinned PD and the dosed
 * variable is the CODE artifact), realm main-only (R26-H), string bytes canon
 * (R25-H), residency/identity fixed (R24-H), membership default (R23-H),
 * call-context fixed (R22-H), storage-state packed+extensible (R26-F), no
 * numeric-class / cardinality / tree-geometry / verdict-lattice / probability
 * dose (R27-A/B/D/E RVL/C PROBX), join policy not applicable (R27-G is
 * G-slice join semantics; H-slice has no join sites).
 *
 * Legs:
 *  CENSUS   static emit census: src TS bytes/lines vs tsc-dist JS bytes/lines
 *           vs esbuild-transform JS bytes/lines for all 21 files.
 *  SPAWN    12 children interleaved T,D,T,D,... (6 per arm), each a fresh
 *           process; parent times spawn-to-exit wall clock (startup tax).
 *  PARITY   all 12 parity sha256 values must be byte-identical (substrate
 *           must not leak into any semantic output byte, incl. S5-H-1 triple).
 *  HOT      per-leg cross-substrate ratio of arm medians, adjudicated against
 *           the within-arm A/A split noise floor.
 *  COLD     importMs + coldFirstCallUs + process wall clock per arm
 *           (once-per-process veto class; priced for completeness).
 *
 * Run: pnpm build first (gate), then `npx tsx /tmp/r27h-substrate.mts`.
 */
import { spawnSync } from "node:child_process";
import { readFileSync, existsSync } from "node:fs";
import { transformSync } from "/workspace/node_modules/.pnpm/esbuild@0.28.2/node_modules/esbuild/lib/main.js";

const FILES = [
  "evaluation/adapters",
  "evaluation/check-adapter",
  "evaluation/delivery-adapter",
  "evaluation/diff-adapter",
  "evaluation/evaluator",
  "evaluation/ownership",
  "evaluation/precedence",
  "evaluation/types",
  "requirement/coverage",
  "requirement/critic",
  "requirement/extractor",
  "requirement/heuristic",
  "requirement/normalizer",
  "requirement/precedence",
  "requirement/provenance",
  "review/critic",
  "review/pairwise",
  "review/reconcile",
  "review/self-review",
  "rubric/registry",
  "rubric/types"
];

function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function median(xs: number[]): number {
  const s = [...xs].sort((a, b) => a - b);
  return s[Math.floor(s.length / 2)] ?? Number.NaN;
}
let failures = 0;
let checks = 0;
function check(name: string, ok: boolean, detail?: string): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
let adjudications = 0;
let breaches = 0;
function adjudicate(name: string, ok: boolean, detail: string): void {
  adjudications += 1;
  if (!ok) breaches += 1;
  log(`ADJUDICATE ${ok ? "in-class" : "BREACH"} ${name}: ${detail}`);
}

/* ================= CENSUS ================= */
let srcBytes = 0;
let srcLines = 0;
let distBytes = 0;
let distLines = 0;
let esbBytes = 0;
let esbLines = 0;
let esbuildErrors = 0;
for (const f of FILES) {
  const srcPath = `/workspace/src/${f}.ts`;
  const distPath = `/workspace/dist/${f}.js`;
  check(`dist exists: ${f}.js`, existsSync(distPath));
  const src = readFileSync(srcPath, "utf8");
  const dist = readFileSync(distPath, "utf8");
  srcBytes += Buffer.byteLength(src);
  srcLines += src.split("\n").length;
  distBytes += Buffer.byteLength(dist);
  distLines += dist.split("\n").length;
  try {
    /* tsx's esbuild transform coordinates: TS loader, ESM, no minify. */
    const esb = transformSync(src, { loader: "ts", format: "esm" }).code;
    esbBytes += Buffer.byteLength(esb);
    esbLines += esb.split("\n").length;
  } catch {
    esbuildErrors += 1;
  }
}
check("esbuild transformed all 21", esbuildErrors === 0);
log(
  `CENSUS 21 files: src=${srcLines} lines/${srcBytes} B; tsc-dist=${distLines} lines/${distBytes} B; esbuild-emit=${esbLines} lines/${esbBytes} B`
);
/* Structural emit diff on the S5-H-1 host file: does either emit alter the
 * guard line? */
{
  const src = readFileSync("/workspace/src/requirement/precedence.ts", "utf8");
  const dist = readFileSync("/workspace/dist/requirement/precedence.js", "utf8");
  const esb = transformSync(src, { loader: "ts", format: "esm" }).code;
  /* tsc's printer re-lines the single-line if (condition and `return [];`
   * land on separate lines); the guard must survive SEMANTICALLY in both
   * emits: same condition, immediately followed by `return [];`. */
  const guardRe = /if \(fast\.length === 0 \|\| slow\.length === 0\)\s*return \[\];/;
  check("S5-H-1 guard exact single-line in src", src.includes("if (fast.length === 0 || slow.length === 0) return [];"));
  check("S5-H-1 guard survives tsc-dist emit", guardRe.test(dist));
  check("S5-H-1 guard survives esbuild emit", guardRe.test(esb));
  log("S5-H-1 guard survives both emits (tsc re-lines the if; condition+return preserved verbatim)");
}

/* ================= SPAWN ================= */
type ChildResult = {
  substrate: "dist" | "src";
  importMs: number;
  perModuleMs: [string, number][];
  coldFirstCallUs: number;
  paritySha: string;
  medians: Record<string, number>;
  passes: Record<string, number[]>;
  checks: number;
  failures: number;
  wallMs: number;
};
const N_PER_ARM = 6;
const children: ChildResult[] = [];
const order: ("src" | "dist")[] = [];
for (let i = 0; i < N_PER_ARM; i += 1) order.push("src", "dist"); /* T,D interleave */
for (const sub of order) {
  const argv =
    sub === "dist"
      ? ["--expose-gc", "/tmp/r27h-substrate-child.mjs"]
      : ["--expose-gc", "--import", "tsx", "/tmp/r27h-substrate-child.mjs"];
  const t0 = performance.now();
  const res = spawnSync(process.execPath, argv, {
    cwd: "/workspace",
    env: { ...process.env, R27H_SUBSTRATE: sub },
    encoding: "utf8",
    timeout: 120000
  });
  const wallMs = performance.now() - t0;
  check(`child ${sub} exit 0`, res.status === 0, `status=${res.status} stderr=${res.stderr?.slice(0, 400)}`);
  const line = (res.stdout ?? "").split("\n").find((l) => l.startsWith("RESULT "));
  check(`child ${sub} emitted RESULT`, line !== undefined);
  if (line !== undefined) {
    const parsed = JSON.parse(line.slice(7)) as Omit<ChildResult, "wallMs">;
    children.push({ ...parsed, wallMs });
  }
}

/* ================= PARITY ================= */
const shas = new Set(children.map((c) => c.paritySha));
check("cross-substrate parity: all 12 children byte-identical semantic outputs", shas.size === 1, [...shas].join(","));
log(`PARITY sha256(all semantic outputs incl. S5-H-1 triple) = ${[...shas].join(",")} across ${children.length} children (dist x${children.filter((c) => c.substrate === "dist").length}, src x${children.filter((c) => c.substrate === "src").length})`);
const childChecks = children.reduce((a, c) => a + c.checks, 0);
const childFailures = children.reduce((a, c) => a + c.failures, 0);
check("all child-internal checks passed", childFailures === 0, `${childFailures}/${childChecks}`);

/* ================= HOT ADJUDICATION ================= */
const LEGS = [
  "chainDefault",
  "gateDefault",
  "precDefault",
  "chainConfA",
  "gateConfA",
  "chainConfB",
  "promotion",
  "evaluate",
  "critique",
  "pairwise",
  "reconcile"
];
function armMedian(sub: "dist" | "src", leg: string): number {
  return median(children.filter((c) => c.substrate === sub).map((c) => c.medians[leg]!));
}
function aaRatio(sub: "dist" | "src", leg: string): number {
  const arm = children.filter((c) => c.substrate === sub);
  const a = median(arm.filter((_, i) => i % 2 === 0).map((c) => c.medians[leg]!));
  const b = median(arm.filter((_, i) => i % 2 === 1).map((c) => c.medians[leg]!));
  return Math.max(a, b) / Math.min(a, b);
}
log("--- EXECUTION-SUBSTRATE FACE: hot tier (ns/call medians of per-child medians; 6 fresh processes per arm) ---");
for (const leg of LEGS) {
  const d = armMedian("dist", leg);
  const s = armMedian("src", leg);
  const ratio = Math.max(d, s) / Math.min(d, s);
  const dir = d <= s ? "dist<=src" : "src<dist";
  const floor = Math.max(aaRatio("dist", leg), aaRatio("src", leg));
  log(
    `HOT ${leg}: dist=${d.toFixed(1)}ns src=${s.toFixed(1)}ns cross-ratio=${ratio.toFixed(3)} (${dir}); A/A-floor=${floor.toFixed(3)}`
  );
  /* In-class iff the cross-substrate ratio is within 1.5x of the within-arm
   * A/A noise floor OR the absolute delta is sub-µs-scale per call. */
  const absDeltaNs = Math.abs(d - s);
  adjudicate(
    `substrate ${leg}`,
    ratio <= Math.max(1.10, floor * 1.5) || absDeltaNs < 1000,
    `cross=${ratio.toFixed(3)} floor=${floor.toFixed(3)} |delta|=${absDeltaNs.toFixed(1)}ns`
  );
}

/* ================= COLD / STARTUP (once-per-process veto class) ================= */
log("--- EXECUTION-SUBSTRATE FACE: once-per-process layer (veto class; priced for completeness) ---");
for (const sub of ["dist", "src"] as const) {
  const arm = children.filter((c) => c.substrate === sub);
  const imp = arm.map((c) => c.importMs);
  const cold = arm.map((c) => c.coldFirstCallUs);
  const wall = arm.map((c) => c.wallMs);
  log(
    `COLD ${sub}: import(9 H-modules)=${median(imp).toFixed(1)}ms [${Math.min(...imp).toFixed(1)},${Math.max(...imp).toFixed(1)}]; first-call=${median(cold).toFixed(0)}us [${Math.min(...cold).toFixed(0)},${Math.max(...cold).toFixed(0)}]; process-wall=${median(wall).toFixed(0)}ms [${Math.min(...wall).toFixed(0)},${Math.max(...wall).toFixed(0)}]`
  );
}
{
  const dWall = median(children.filter((c) => c.substrate === "dist").map((c) => c.wallMs));
  const sWall = median(children.filter((c) => c.substrate === "src").map((c) => c.wallMs));
  const dImp = median(children.filter((c) => c.substrate === "dist").map((c) => c.importMs));
  const sImp = median(children.filter((c) => c.substrate === "src").map((c) => c.importMs));
  log(
    `COLD substrate tax (src-arm minus dist-arm): process-wall +${(sWall - dWall).toFixed(0)}ms; H-module import +${(sImp - dImp).toFixed(1)}ms — ONCE-PER-PROCESS (reject class per acceptance bar)`
  );
  /* Per-module top contributors on the src arm */
  const srcArm = children.filter((c) => c.substrate === "src");
  const agg = new Map<string, number[]>();
  for (const c of srcArm) for (const [p, ms] of c.perModuleMs) agg.set(p, [...(agg.get(p) ?? []), ms]);
  const rows = [...agg.entries()].map(([p, ms]) => [p, median(ms)] as const).sort((a, b) => b[1] - a[1]);
  log(`COLD src-arm per-module import medians (ms): ${rows.map(([p, ms]) => `${p}=${ms.toFixed(1)}`).join(" ")}`);
}

log(`CHECKS ${checks - failures}/${checks} passed; ADJUDICATIONS ${adjudications - breaches}/${adjudications} in-class (${breaches} BREACHED)`);
if (failures > 0) process.exit(1);
```

### A2 子驱动 `/tmp/r27h-substrate-child.mjs`（310 行，sha256 `153ab025c21af3ac250dcf0668430af0237bf3b9b2d72f733d959e57d972f77f`）

```js
/**
 * R27-H substrate child: prices the H-slice production chain under ONE
 * execution substrate, selected by env:
 *   R27H_SUBSTRATE=dist -> tsc-compiled ES2024 JS from dist/ (installed-bin
 *                          arm: `bin.pi-sparkle` -> dist/cli/main.js), run
 *                          under plain `node`.
 *   R27H_SUBSTRATE=src  -> esbuild-transformed TS from src/ (dev-CLI arm:
 *                          `pnpm dev|cli|test` -> `tsx src/...`), run under
 *                          `node --import tsx`.
 * Identical fixture bytes, identical reps, identical pass structure; the ONLY
 * dosed variable is the emit/loader pipeline that produced the executing JS.
 * Every child emits a cross-substrate parity sha256 over all semantic outputs
 * (incl. the S5-H-1 attestation triple); the parent requires byte identity.
 */
import { performance } from "node:perf_hooks";
import { createHash } from "node:crypto";

const SUB = process.env.R27H_SUBSTRATE;
if (SUB !== "dist" && SUB !== "src") throw new Error(`bad R27H_SUBSTRATE: ${SUB}`);
const BASE = SUB === "dist" ? "/workspace/dist" : "/workspace/src";
const EXT = SUB === "dist" ? "js" : "ts";
const mod = (p) => `${BASE}/${p}.${EXT}`;

const forceGc = globalThis.gc;
function gcBarrier() {
  if (forceGc !== undefined) {
    forceGc();
    forceGc();
  }
}
let failures = 0;
let checks = 0;
function check(name, ok) {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL [${SUB}] ${name}\n`);
  }
}
function median(xs) {
  const s = [...xs].sort((a, b) => a - b);
  return s[Math.floor(s.length / 2)];
}
function bench(fn, reps) {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn, reps) {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}
function passes(fn, reps, n) {
  const out = [];
  for (let i = 0; i < n; i += 1) {
    gcBarrier();
    out.push(bench(fn, reps) * 1e6); /* ns */
  }
  return out;
}
async function passesAsync(fn, reps, n) {
  const out = [];
  for (let i = 0; i < n; i += 1) {
    gcBarrier();
    out.push((await benchAsync(fn, reps)) * 1e6); /* ns */
  }
  return out;
}
/* Deterministic canonical stringify: sorted keys, handles arrays/null.
 * Masks the two volatile identity fields minted OUTSIDE the 21-file slice by
 * the domain event factories (`createEventId()` -> evt_<nonce>, `nowIso()` ->
 * createdAt): they are per-call wall-clock/nonce identity, not substrate
 * semantics. Everything else must be byte-identical across substrates. */
function canon(v) {
  if (v === null || typeof v !== "object") return JSON.stringify(v);
  if (Array.isArray(v)) return `[${v.map(canon).join(",")}]`;
  const keys = Object.keys(v).sort();
  return `{${keys
    .map((k) => {
      const val = v[k];
      if (k === "createdAt" && typeof val === "string") return `${JSON.stringify(k)}:"MASKED-CLOCK"`;
      if (k === "id" && typeof val === "string" && val.startsWith("evt_")) return `${JSON.stringify(k)}:"MASKED-NONCE"`;
      return `${JSON.stringify(k)}:${canon(val)}`;
    })
    .join(",")}}`;
}

/* ---- IMPORT CENSUS (cold, once-per-process; substrate-visible layer) ---- */
const perModule = [];
async function timedImport(p) {
  const t0 = performance.now();
  const m = await import(mod(p));
  perModule.push([p, performance.now() - t0]);
  return m;
}
const tImp0 = performance.now();
const heuristic = await timedImport("requirement/heuristic");
const coverage = await timedImport("requirement/coverage");
const precedence = await timedImport("requirement/precedence");
const rcritic = await timedImport("requirement/critic");
const selfReview = await timedImport("review/self-review");
const pairwiseMod = await timedImport("review/pairwise");
const reconcileMod = await timedImport("review/reconcile");
const evaluatorMod = await timedImport("evaluation/evaluator");
const rubricTypes = await timedImport("rubric/types");
const importMs = performance.now() - tImp0;

/* ---- COLD FIRST-CALL (once-per-process; before any warmup) ---- */
const OBJ = "fix the login retry bug in src/auth/session.ts and keep tests green";
function tasksFor(contract, count = 5) {
  const tasks = [];
  for (let i = 0; i < count; i += 1) {
    const acs = [];
    for (const criterion of contract.acceptanceCriteria) acs.push({ id: criterion.id });
    tasks.push({ id: `tsk_0000000${i}`, acceptanceCriteria: acs });
  }
  return tasks;
}
const tCold0 = performance.now();
const first = await heuristic.extractHeuristicContract({ objective: OBJ });
const coldContract = first.contract;
coverage.assertCoverageAllowsStart(coldContract, tasksFor(coldContract), undefined);
precedence.applyPrecedence(coldContract, "user-first");
const coldFirstCallUs = (performance.now() - tCold0) * 1e3;

/* ---- PARITY SIGNATURE (all semantic outputs, canonical bytes) ---- */
const sigParts = [];
{
  const canonDefault = (await heuristic.extractHeuristicContract({ objective: OBJ })).contract;
  check("canon default shape Q=0 C=2", canonDefault.questions.length === 0 && canonDefault.acceptanceCriteria.length === 2);
  sigParts.push(canon(canonDefault));
  const confA = (
    await heuristic.extractHeuristicContract({
      objective: OBJ,
      habits: { requireTests: true, preferReview: false, askBeforeWrite: true }
    })
  ).contract;
  sigParts.push(canon(confA));
  const confB = (await heuristic.extractHeuristicContract({ objective: "fix bug please", habits: { askBeforeWrite: true } })).contract;
  sigParts.push(canon(confB));

  /* gate outcomes: pass path + fail-closed path (unresolved question) */
  sigParts.push(canon(coverage.assertCoverageAllowsStart(canonDefault, tasksFor(canonDefault), undefined) ?? "void"));
  let gateErr = "none";
  try {
    coverage.assertCoverageAllowsStart(confB, tasksFor(confB), undefined);
  } catch (e) {
    gateErr = String(e && e.message);
  }
  sigParts.push(canon(gateErr));

  /* precedence outputs, two policies */
  sigParts.push(canon(precedence.applyPrecedence(canonDefault, "user-first")));
  sigParts.push(canon(precedence.applyPrecedence(canonDefault, "spec-first")));

  /* S5-H-1 attestation triple across substrate (fast-only -> [], slow-only
   * -> [], fast+slow -> exactly 1 conflict) */
  const mkC = (checks_) => ({
    id: "req_00000001",
    rawText: "t",
    goals: [],
    constraints: [],
    acceptanceCriteria: checks_.map((oc, i) => ({
      id: `ac_0000000${i}`,
      description: "d",
      observableCheck: oc,
      sourceRefs: []
    })),
    assumptions: [],
    questions: [],
    provenance: []
  });
  const fastOnly = precedence.detectConflicts(mkC(["response is fast", "check completes < 10ms"]));
  const slowOnly = precedence.detectConflicts(mkC(["batch job is slow", "runs > 1000ms"]));
  const both = precedence.detectConflicts(mkC(["response is fast", "batch job is slow"]));
  /* and the conflict-present precedence path, both policies */
  sigParts.push(canon(precedence.applyPrecedence(mkC(["response is fast", "batch job is slow"]), "user-first")));
  sigParts.push(canon(precedence.applyPrecedence(mkC(["response is fast", "batch job is slow"]), "spec-first")));
  check("S5-H-1 fast-only -> []", fastOnly.length === 0);
  check("S5-H-1 slow-only -> []", slowOnly.length === 0);
  check("S5-H-1 fast+slow -> exactly 1", both.length === 1);
  sigParts.push(canon([fastOnly, slowOnly, both]));

  /* critique + evaluate + pairwise + reconcile + promotion */
  sigParts.push(canon(rcritic.critiqueContract(canonDefault)));
  const rubric = rubricTypes.createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001",
    evaluator: { kind: "deterministic", version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const record = evaluatorMod.createEvaluationRecord(evalInput);
  check("evaluate deterministic FAIL on missing evidence", record.overall === "FAIL");
  sigParts.push(canon(record));
  const pairInput = {
    episodeId: "ep_00000001",
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pair = pairwiseMod.runBlindPairwisePair(pairInput, () => 0.7);
  check("pairwise identity-stable consensus a", pair.reconciliation.consensus === "a");
  sigParts.push(canon(pair));
  sigParts.push(canon(reconcileMod.reconcileReviews([pair.first, pair.swapped])));
  let promoOk = true;
  try {
    selfReview.assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" });
  } catch {
    promoOk = false;
  }
  check("promotion peer distinct allowed", promoOk);
  sigParts.push(canon(promoOk));
}
const paritySha = createHash("sha256").update(sigParts.join("\n")).digest("hex");
if (process.env.R27H_DEBUG_SIG === "1") {
  sigParts.forEach((p, i) => {
    const h = createHash("sha256").update(p).digest("hex").slice(0, 12);
    process.stdout.write(`SIGPART ${i} ${h} ${p.slice(0, 120)}\n`);
  });
}

/* ---- HOT LEGS (steady-state; the adjudicated readings) ---- */
const N_PASSES = 5;
const results = {};
{
  const canonDefault = (await heuristic.extractHeuristicContract({ objective: OBJ })).contract;
  const tasks = tasksFor(canonDefault);
  /* warm */
  for (let i = 0; i < 2000; i += 1) {
    coverage.assertCoverageAllowsStart(canonDefault, tasks, undefined);
    precedence.applyPrecedence(canonDefault, "user-first");
  }
  results.chainDefault = await passesAsync(async () => {
    await heuristic.extractHeuristicContract({ objective: OBJ });
  }, 1500, N_PASSES);
  results.gateDefault = passes(() => coverage.assertCoverageAllowsStart(canonDefault, tasks, undefined), 20000, N_PASSES);
  results.precDefault = passes(() => void precedence.applyPrecedence(canonDefault, "user-first"), 50000, N_PASSES);

  const argsA = { objective: OBJ, habits: { requireTests: true, preferReview: false, askBeforeWrite: true } };
  results.chainConfA = await passesAsync(async () => {
    await heuristic.extractHeuristicContract(argsA);
  }, 1500, N_PASSES);
  const confA = (await heuristic.extractHeuristicContract(argsA)).contract;
  const tasksA = tasksFor(confA);
  const optA = { resolvedQuestionIds: confA.questions.map((q) => q.id) };
  results.gateConfA = passes(() => coverage.assertCoverageAllowsStart(confA, tasksA, optA), 20000, N_PASSES);

  const argsB = { objective: "fix bug please", habits: { askBeforeWrite: true } };
  results.chainConfB = await passesAsync(async () => {
    await heuristic.extractHeuristicContract(argsB);
  }, 1500, N_PASSES);

  results.promotion = passes(
    () => selfReview.assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    100000,
    3
  );
  const rubric = rubricTypes.createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001",
    evaluator: { kind: "deterministic", version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  results.evaluate = passes(() => void evaluatorMod.createEvaluationRecord(evalInput), 20000, 3);
  results.critique = passes(() => void rcritic.critiqueContract(canonDefault), 20000, 3);
  const pairInput = {
    episodeId: "ep_00000001",
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  results.pairwise = passes(() => void pairwiseMod.runBlindPairwisePair(pairInput, () => 0.7), 10000, 3);
  const pair = pairwiseMod.runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  results.reconcile = passes(() => void reconcileMod.reconcileReviews(two), 50000, 3);
}

const out = {
  substrate: SUB,
  node: process.version,
  importMs: Number(importMs.toFixed(3)),
  perModuleMs: perModule.map(([p, ms]) => [p, Number(ms.toFixed(3))]),
  coldFirstCallUs: Number(coldFirstCallUs.toFixed(1)),
  paritySha,
  medians: Object.fromEntries(Object.entries(results).map(([k, v]) => [k, Number(median(v).toFixed(1))])),
  passes: Object.fromEntries(Object.entries(results).map(([k, v]) => [k, v.map((x) => Number(x.toFixed(1)))])),
  checks,
  failures
};
process.stdout.write(`RESULT ${JSON.stringify(out)}\n`);
if (failures > 0) process.exit(1);
```

## 附录 B：执行基质面三次运行原始输出（逐字）

### B1 run1

```text
CENSUS 21 files: src=1771 lines/55113 B; tsc-dist=1122 lines/42839 B; esbuild-emit=1097 lines/36257 B
S5-H-1 guard survives both emits (tsc re-lines the if; condition+return preserved verbatim)
PARITY sha256(all semantic outputs incl. S5-H-1 triple) = 51eb658d4effc94325741c02700a17efdbe2462ba7695951d9ec603d05a2517d across 12 children (dist x6, src x6)
--- EXECUTION-SUBSTRATE FACE: hot tier (ns/call medians of per-child medians; 6 fresh processes per arm) ---
HOT chainDefault: dist=6969.1ns src=6272.2ns cross-ratio=1.111 (src<dist); A/A-floor=1.031
ADJUDICATE in-class substrate chainDefault: cross=1.111 floor=1.031 |delta|=696.9ns
HOT gateDefault: dist=549.1ns src=507.0ns cross-ratio=1.083 (src<dist); A/A-floor=1.038
ADJUDICATE in-class substrate gateDefault: cross=1.083 floor=1.038 |delta|=42.1ns
HOT precDefault: dist=213.6ns src=211.3ns cross-ratio=1.011 (src<dist); A/A-floor=1.008
ADJUDICATE in-class substrate precDefault: cross=1.011 floor=1.008 |delta|=2.3ns
HOT chainConfA: dist=4725.2ns src=4978.6ns cross-ratio=1.054 (dist<=src); A/A-floor=1.094
ADJUDICATE in-class substrate chainConfA: cross=1.054 floor=1.094 |delta|=253.4ns
HOT gateConfA: dist=689.1ns src=637.9ns cross-ratio=1.080 (src<dist); A/A-floor=1.144
ADJUDICATE in-class substrate gateConfA: cross=1.080 floor=1.144 |delta|=51.2ns
HOT chainConfB: dist=4078.0ns src=3451.1ns cross-ratio=1.182 (src<dist); A/A-floor=1.179
ADJUDICATE in-class substrate chainConfB: cross=1.182 floor=1.179 |delta|=626.9ns
HOT promotion: dist=21.2ns src=21.6ns cross-ratio=1.019 (dist<=src); A/A-floor=1.143
ADJUDICATE in-class substrate promotion: cross=1.019 floor=1.143 |delta|=0.4ns
HOT evaluate: dist=951.3ns src=958.7ns cross-ratio=1.008 (dist<=src); A/A-floor=1.007
ADJUDICATE in-class substrate evaluate: cross=1.008 floor=1.007 |delta|=7.4ns
HOT critique: dist=272.3ns src=265.8ns cross-ratio=1.024 (src<dist); A/A-floor=1.015
ADJUDICATE in-class substrate critique: cross=1.024 floor=1.015 |delta|=6.5ns
HOT pairwise: dist=1907.2ns src=1831.9ns cross-ratio=1.041 (src<dist); A/A-floor=1.011
ADJUDICATE in-class substrate pairwise: cross=1.041 floor=1.011 |delta|=75.3ns
HOT reconcile: dist=59.1ns src=57.2ns cross-ratio=1.033 (src<dist); A/A-floor=1.113
ADJUDICATE in-class substrate reconcile: cross=1.033 floor=1.113 |delta|=1.9ns
--- EXECUTION-SUBSTRATE FACE: once-per-process layer (veto class; priced for completeness) ---
COLD dist: import(9 H-modules)=5.1ms [5.0,5.1]; first-call=1725us [1702,1757]; process-wall=721ms [708,727]
COLD src: import(9 H-modules)=13.3ms [12.7,13.8]; first-call=1603us [1591,1632]; process-wall=904ms [883,911]
COLD substrate tax (src-arm minus dist-arm): process-wall +183ms; H-module import +8.2ms — ONCE-PER-PROCESS (reject class per acceptance bar)
COLD src-arm per-module import medians (ms): requirement/heuristic=6.9 review/pairwise=1.8 evaluation/evaluator=1.1 requirement/coverage=1.1 review/self-review=0.9 requirement/precedence=0.6 rubric/types=0.4 requirement/critic=0.2 review/reconcile=0.2
CHECKS 51/51 passed; ADJUDICATIONS 11/11 in-class (0 BREACHED)
```

### B2 run2

```text
CENSUS 21 files: src=1771 lines/55113 B; tsc-dist=1122 lines/42839 B; esbuild-emit=1097 lines/36257 B
S5-H-1 guard survives both emits (tsc re-lines the if; condition+return preserved verbatim)
PARITY sha256(all semantic outputs incl. S5-H-1 triple) = 51eb658d4effc94325741c02700a17efdbe2462ba7695951d9ec603d05a2517d across 12 children (dist x6, src x6)
--- EXECUTION-SUBSTRATE FACE: hot tier (ns/call medians of per-child medians; 6 fresh processes per arm) ---
HOT chainDefault: dist=6990.9ns src=6264.9ns cross-ratio=1.116 (src<dist); A/A-floor=1.050
ADJUDICATE in-class substrate chainDefault: cross=1.116 floor=1.050 |delta|=726.0ns
HOT gateDefault: dist=537.1ns src=515.3ns cross-ratio=1.042 (src<dist); A/A-floor=1.002
ADJUDICATE in-class substrate gateDefault: cross=1.042 floor=1.002 |delta|=21.8ns
HOT precDefault: dist=214.2ns src=209.7ns cross-ratio=1.021 (src<dist); A/A-floor=1.008
ADJUDICATE in-class substrate precDefault: cross=1.021 floor=1.008 |delta|=4.5ns
HOT chainConfA: dist=4967.7ns src=5222.4ns cross-ratio=1.051 (dist<=src); A/A-floor=1.056
ADJUDICATE in-class substrate chainConfA: cross=1.051 floor=1.056 |delta|=254.7ns
HOT gateConfA: dist=685.0ns src=647.7ns cross-ratio=1.058 (src<dist); A/A-floor=1.027
ADJUDICATE in-class substrate gateConfA: cross=1.058 floor=1.027 |delta|=37.3ns
HOT chainConfB: dist=3986.7ns src=3550.2ns cross-ratio=1.123 (src<dist); A/A-floor=1.076
ADJUDICATE in-class substrate chainConfB: cross=1.123 floor=1.076 |delta|=436.5ns
HOT promotion: dist=23.7ns src=20.5ns cross-ratio=1.156 (src<dist); A/A-floor=1.117
ADJUDICATE in-class substrate promotion: cross=1.156 floor=1.117 |delta|=3.2ns
HOT evaluate: dist=941.8ns src=938.4ns cross-ratio=1.004 (src<dist); A/A-floor=1.007
ADJUDICATE in-class substrate evaluate: cross=1.004 floor=1.007 |delta|=3.4ns
HOT critique: dist=274.6ns src=260.4ns cross-ratio=1.055 (src<dist); A/A-floor=1.028
ADJUDICATE in-class substrate critique: cross=1.055 floor=1.028 |delta|=14.2ns
HOT pairwise: dist=1867.0ns src=1796.7ns cross-ratio=1.039 (src<dist); A/A-floor=1.025
ADJUDICATE in-class substrate pairwise: cross=1.039 floor=1.025 |delta|=70.3ns
HOT reconcile: dist=65.9ns src=53.2ns cross-ratio=1.239 (src<dist); A/A-floor=1.175
ADJUDICATE in-class substrate reconcile: cross=1.239 floor=1.175 |delta|=12.7ns
--- EXECUTION-SUBSTRATE FACE: once-per-process layer (veto class; priced for completeness) ---
COLD dist: import(9 H-modules)=5.1ms [5.0,5.2]; first-call=1715us [1699,1748]; process-wall=726ms [718,731]
COLD src: import(9 H-modules)=13.6ms [12.7,14.0]; first-call=1620us [1598,1637]; process-wall=900ms [892,904]
COLD substrate tax (src-arm minus dist-arm): process-wall +174ms; H-module import +8.6ms — ONCE-PER-PROCESS (reject class per acceptance bar)
COLD src-arm per-module import medians (ms): requirement/heuristic=7.2 review/pairwise=1.8 evaluation/evaluator=1.1 requirement/coverage=1.1 review/self-review=0.7 requirement/precedence=0.6 rubric/types=0.4 requirement/critic=0.2 review/reconcile=0.2
CHECKS 51/51 passed; ADJUDICATIONS 11/11 in-class (0 BREACHED)
```

### B3 run3

```text
CENSUS 21 files: src=1771 lines/55113 B; tsc-dist=1122 lines/42839 B; esbuild-emit=1097 lines/36257 B
S5-H-1 guard survives both emits (tsc re-lines the if; condition+return preserved verbatim)
PARITY sha256(all semantic outputs incl. S5-H-1 triple) = 51eb658d4effc94325741c02700a17efdbe2462ba7695951d9ec603d05a2517d across 12 children (dist x6, src x6)
--- EXECUTION-SUBSTRATE FACE: hot tier (ns/call medians of per-child medians; 6 fresh processes per arm) ---
HOT chainDefault: dist=6734.4ns src=6235.0ns cross-ratio=1.080 (src<dist); A/A-floor=1.005
ADJUDICATE in-class substrate chainDefault: cross=1.080 floor=1.005 |delta|=499.4ns
HOT gateDefault: dist=544.3ns src=514.8ns cross-ratio=1.057 (src<dist); A/A-floor=1.026
ADJUDICATE in-class substrate gateDefault: cross=1.057 floor=1.026 |delta|=29.5ns
HOT precDefault: dist=214.7ns src=210.7ns cross-ratio=1.019 (src<dist); A/A-floor=1.005
ADJUDICATE in-class substrate precDefault: cross=1.019 floor=1.005 |delta|=4.0ns
HOT chainConfA: dist=5190.5ns src=4892.4ns cross-ratio=1.061 (src<dist); A/A-floor=1.064
ADJUDICATE in-class substrate chainConfA: cross=1.061 floor=1.064 |delta|=298.1ns
HOT gateConfA: dist=690.4ns src=652.6ns cross-ratio=1.058 (src<dist); A/A-floor=1.010
ADJUDICATE in-class substrate gateConfA: cross=1.058 floor=1.010 |delta|=37.8ns
HOT chainConfB: dist=3587.0ns src=3602.5ns cross-ratio=1.004 (dist<=src); A/A-floor=1.119
ADJUDICATE in-class substrate chainConfB: cross=1.004 floor=1.119 |delta|=15.5ns
HOT promotion: dist=23.8ns src=23.6ns cross-ratio=1.008 (src<dist); A/A-floor=1.245
ADJUDICATE in-class substrate promotion: cross=1.008 floor=1.245 |delta|=0.2ns
HOT evaluate: dist=944.4ns src=948.8ns cross-ratio=1.005 (dist<=src); A/A-floor=1.006
ADJUDICATE in-class substrate evaluate: cross=1.005 floor=1.006 |delta|=4.4ns
HOT critique: dist=282.3ns src=258.4ns cross-ratio=1.092 (src<dist); A/A-floor=1.031
ADJUDICATE in-class substrate critique: cross=1.092 floor=1.031 |delta|=23.9ns
HOT pairwise: dist=1872.8ns src=1862.2ns cross-ratio=1.006 (src<dist); A/A-floor=1.025
ADJUDICATE in-class substrate pairwise: cross=1.006 floor=1.025 |delta|=10.6ns
HOT reconcile: dist=63.0ns src=52.8ns cross-ratio=1.193 (src<dist); A/A-floor=1.117
ADJUDICATE in-class substrate reconcile: cross=1.193 floor=1.117 |delta|=10.2ns
--- EXECUTION-SUBSTRATE FACE: once-per-process layer (veto class; priced for completeness) ---
COLD dist: import(9 H-modules)=5.1ms [5.0,5.7]; first-call=1717us [1680,1757]; process-wall=725ms [716,734]
COLD src: import(9 H-modules)=13.4ms [12.9,14.7]; first-call=1623us [1593,1641]; process-wall=901ms [895,904]
COLD substrate tax (src-arm minus dist-arm): process-wall +176ms; H-module import +8.3ms — ONCE-PER-PROCESS (reject class per acceptance bar)
COLD src-arm per-module import medians (ms): requirement/heuristic=7.1 review/pairwise=1.8 requirement/coverage=1.2 evaluation/evaluator=1.1 review/self-review=0.7 requirement/precedence=0.6 rubric/types=0.4 requirement/critic=0.2 review/reconcile=0.2
CHECKS 51/51 passed; ADJUDICATIONS 11/11 in-class (0 BREACHED)
```

## 附录 C：降级存档——消费级属性来源面（撞轴 R27-F；不主张、不铸 ID、不移植）

撞轴前完成的三轮测量与全部驱动随此留档（R26-H 附录 C 同款处置）。存档数据不参与本轮任何判定；§2 的「存档要点」仅为 F 轴族横向印证。夹具坐标：六臂 PD/AC/NP/PITK/PX/PXH 剂量消费级记录容器；RX 主读数进程隔离（每臂全新子进程，2000 PD 预热 + 5×4096 单次使用本臂集合）；P1=默认门+precedence、P2=仅 precedence、P3=配置态仅 options 记录剂量；PI-CT 行为探针与 ROT 进程内污染视图仅留档不判定。三轮 CHECKS 37/37（父）+ 每臂 236/236（子）全过；ADJUDICATIONS 12/15 类内（3 BREACH 为预设类界被突破的诚实记录——AC/PITK 超出 plain-lattice 3× 预设界，属存档观测非候选）。

### C1 父驱动 `/tmp/r27h-provenance.mts`（484 行，sha256 `4773e4985b95654830122ebb64e165fd2a92fdbde6869dc8421690ce2df22ecc`）

运行：`pnpm build` 后 `node --allow-natives-syntax --expose-gc --import tsx /tmp/r27h-provenance.mts`。

```ts
/**
 * R27-H parent driver: deterministic anchor re-verification + CONSUMER-STAGE
 * PROPERTY-PROVENANCE FACE (twenty-seventh pass — the pass's NEW
 * completeness axis).
 *
 * Axis: WHICH ACCESS PATH serves each consumed property of the structurally
 * typed record operands read by the consumer stages (run-start gate +
 * precedence): own enumerable DATA property on a plain object (all 26 prior
 * rounds of fixtures, and 100% of production constructions — rg src/ for
 * `new Proxy|Object.create|defineProperty`: zero hits), vs own ACCESSOR
 * (getter-backed), PROTOTYPE-INHERITED data, NULL-PROTOTYPE own data, or
 * PROXY-mediated access ([[Get]]/[[OwnPropertyKeys]] through an exotic
 * object). TS structural typing admits all five at every consumer seam;
 * prior campaign Proxy usage was deadness TRACING (S6-H-2 / S7-H-2), never
 * a priced operand class; rg over docs/reports/sota-opt for an operand-class
 * dose of accessors/prototypes/proxies: zero hits.
 *
 * Pinned coordinates (orthogonality to every named/unnamed plan-ban axis):
 * realm main-only (R26-H), elements kind packed + integrity extensible
 * (R26-F), string bytes canon (R25-H/R25-A), field presence all-present
 * (R25-B), cardinality default C=1/Q=0/T=5 (R13-H), shape homogeneous per
 * arm (SHAPEK is mix-ratio in the data-shape lattice; provenance moves the
 * property KIND, one construction site per arm), call geometry fixed
 * (R22-H), sync-only (R21-H), no serialization boundary (R24-F), no argv /
 * env / locale / policy-leg / verdict-lattice / numeric-class / tree-
 * geometry dose (R26-I / R25-I / R26-A / R26-B / R27-E RVL / R27-A / R27-D).
 *
 * Legs:
 *  ANCHOR   full-grid matrix re-anchor FIRST (clean-IC), R8-H..R26-H shapes.
 *  E0-PRE   steady canon before any exotic object exists in-process.
 *  PI-CT    behavior probe: contract-side prototype-inherited records are
 *           structurally legal but the gate's own-enumerable spread seam
 *           ({...contract}) drops inherited fields -> deterministic
 *           TypeError = REFUSAL (fail-closed direction), attested.
 *  ROT      in-process rotation view (contamination view per R26-F/R26-H
 *           methodology; logged, never adjudicated).
 *  E0-POST  IC-residue probe after the Proxy excursion.
 *  RX       PRIMARY: process-isolated children, one per arm
 *           (PD/AC/NP/PITK/PX/PXH), warmed on 2000 PD sets, then 3x4096
 *           single-use own-arm sets; legs P1 (default gate+precedence),
 *           P2 (precedence only), P3 (configured Q=1, options record only).
 *  COLD     once-per-process first-call totals on dist (veto class).
 *
 * Run: pnpm build, then
 *   node --allow-natives-syntax --expose-gc --import tsx /tmp/r27h-provenance.mts
 */
import { performance } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import { types } from "node:util";
import {
  extractHeuristicContract,
  type HeuristicHabits
} from "/workspace/src/requirement/heuristic.js";
import {
  assertCoverageAllowsStart,
  type CoverageTaskRef
} from "/workspace/src/requirement/coverage.js";
import { applyPrecedence, detectConflicts } from "/workspace/src/requirement/precedence.js";
import { critiqueContract } from "/workspace/src/requirement/critic.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { assertCanPromoteFromReview } from "/workspace/src/review/self-review.js";
import { runBlindPairwisePair, type PairwiseInput } from "/workspace/src/review/pairwise.js";
import { reconcileReviews } from "/workspace/src/review/reconcile.js";
import { createEvaluationRecord } from "/workspace/src/evaluation/evaluator.js";
import { createRubric } from "/workspace/src/rubric/types.js";
import type { EpisodeId, TaskId } from "/workspace/src/domain/ids.js";

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean, detail?: string): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
let adjudications = 0;
let breaches = 0;
function adjudicate(name: string, ok: boolean, detail: string): void {
  adjudications += 1;
  if (!ok) breaches += 1;
  log(`ADJUDICATE ${ok ? "in-class" : "BREACH"} ${name}: ${detail}`);
}
const forceGc: (() => void) | undefined = (globalThis as { gc?: () => void }).gc;
function gcBarrier(): void {
  if (forceGc !== undefined) {
    forceGc();
    forceGc();
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function median(xs: number[]): number {
  const sorted = [...xs].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)] ?? Number.NaN;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}
let sink = 0;

/* V8 probes (--allow-natives-syntax), compiled at runtime. */
type Probe = (a: unknown) => boolean;
function nativeProbe(name: string): Probe | undefined {
  try {
    const fn = new Function("a", `return %${name}(a)`) as Probe;
    fn([]);
    return fn;
  } catch {
    return undefined;
  }
}
const hasHoley = nativeProbe("HasHoleyElements");
const hasDict = nativeProbe("HasDictionaryElements");
check("native elements-kind probes available", hasHoley !== undefined && hasDict !== undefined);
function isPackedNonDict(a: unknown): boolean {
  return hasHoley !== undefined && hasDict !== undefined && !hasHoley(a) && !hasDict(a);
}

const OBJ = "fix the login retry bug in src/auth/session.ts and keep tests green";

function buildArgs(objective: string, habits: HeuristicHabits | undefined) {
  return { objective, ...(habits !== undefined ? { habits } : {}) };
}
/* Task-shape builders as pinned free functions (R26-H protocol note 1:
 * Array.from-arrow callees drift to HOLEY inner .map allocations when
 * optimized; plain for+push sidesteps the drift entirely). */
function tasksFor(contract: RequirementContract, count = 5): CoverageTaskRef[] {
  const tasks: { id: TaskId; acceptanceCriteria: { id: string }[] }[] = [];
  for (let i = 0; i < count; i += 1) {
    const acs: { id: string }[] = [];
    for (const criterion of contract.acceptanceCriteria) acs.push({ id: criterion.id });
    tasks.push({ id: `tsk_0000000${i}` as TaskId, acceptanceCriteria: acs });
  }
  return tasks as CoverageTaskRef[];
}

/* ================================================================
 * ANCHOR: hot tier, three states — measured FIRST, before ANY exotic
 * object (accessor/null-proto/inherited/proxy) exists in this process.
 * ================================================================ */
async function measureChain(
  label: string,
  objective: string,
  habits: HeuristicHabits | undefined
): Promise<number> {
  const args = buildArgs(objective, habits);
  const chain = await benchAsync(async () => {
    await extractHeuristicContract(args);
  }, 3000);
  const prod = await extractHeuristicContract(args);
  const real = prod.contract;
  const tasks = tasksFor(real);
  const options =
    real.questions.length > 0
      ? { resolvedQuestionIds: real.questions.map((question) => question.id) }
      : undefined;
  const gate = bench(() => assertCoverageAllowsStart(real, tasks, options), 50000);
  const precedence = bench(() => void applyPrecedence(real, "user-first"), 100000);
  const total = chain + gate + precedence;
  log(
    `CEILING ${label}: extractHeuristicContract=${(chain * 1e6).toFixed(0)}ns + run-start gate(Q=${real.questions.length}${options !== undefined ? ", all resolved" : ", no options"})=${(gate * 1e6).toFixed(0)}ns + applyPrecedence=${(precedence * 1e6).toFixed(0)}ns = ${(total * 1e6).toFixed(0)}ns once-per-run production total`
  );
  return total;
}
{
  await measureChain("default (9-pass anchor shape, Q=0)", OBJ, undefined);
  await measureChain("configured A (habits set, Q=1 gated)", OBJ, {
    requireTests: true,
    preferReview: false,
    askBeforeWrite: true
  });
  await measureChain("configured B (vague, Q=3 gated)", "fix bug please", {
    askBeforeWrite: true
  });
}

/* ================================================================
 * Command-class anchors beyond run-start (R9-H..R26-H mirror).
 * ================================================================ */
{
  const promo = bench(
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    200000
  );
  log(`CEILING promotion class: assertCanPromoteFromReview=${(promo * 1e6).toFixed(1)}ns once per promotion (production)`);

  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const evaluate = bench(() => void createEvaluationRecord(evalInput), 50000);
  log(`CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=${(evaluate * 1e6).toFixed(0)}ns/call`);

  const prod = await extractHeuristicContract({ objective: OBJ });
  const critique = bench(() => void critiqueContract(prod.contract), 50000);
  log(`CEILING critique class (inside run-start chain): critiqueContract(real contract)=${(critique * 1e6).toFixed(0)}ns/call`);

  const pairInput: PairwiseInput = {
    episodeId: "ep_00000001" as EpisodeId,
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pairwise = bench(() => void runBlindPairwisePair(pairInput, () => 0.7), 20000);
  const pair = runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  const reconcile = bench(() => void reconcileReviews(two), 100000);
  log(`CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=${(pairwise * 1e6).toFixed(0)}ns/call`);
  log(`CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=${(reconcile * 1e6).toFixed(0)}ns/call`);
  check("pairwise identity-stable scores agree across swap", pair.reconciliation.consensus === "a");
  const biased = runBlindPairwisePair(pairInput, () => 0.7, { bindScoresToSlots: true });
  check("pairwise slot-bound scores reconcile to uncertain", biased.reconciliation.consensus === "uncertain");
  const record = createEvaluationRecord(evalInput);
  check("evaluate deterministic FAIL on missing evidence", record.overall === "FAIL");
  check(
    "evaluate per-criterion outcomes",
    record.scores.map((s) => s.outcome).join(",") === "PASS,PASS,FAIL"
  );
}

/* ================================================================
 * NEW AXIS: CONSUMER-STAGE PROPERTY-PROVENANCE FACE (in-parent legs)
 * ================================================================ */
log("--- CONSUMER-STAGE PROPERTY-PROVENANCE FACE (E0-PRE / PI-CT behavior probe / ROT contamination view / E0-POST residue / RX process-isolated primary / COLD) ---");

const canon = (await extractHeuristicContract({ objective: OBJ })).contract;
check("canon default shape Q=0 C=2", canon.questions.length === 0 && canon.acceptanceCriteria.length === 2);

/* Record builders (mirror of the RX child, kept in sync by hand; the
 * adjudicated numbers come from the child — these exist for the in-parent
 * views and the PI-CT behavior probe). */
type Fields = [string, unknown][];
const REFLECT_HANDLER = {
  get: Reflect.get,
  has: Reflect.has,
  ownKeys: Reflect.ownKeys,
  getOwnPropertyDescriptor: Reflect.getOwnPropertyDescriptor
};
function mkPD(fields: Fields): Record<string, unknown> {
  const o: Record<string, unknown> = {};
  for (let i = 0; i < fields.length; i += 1) o[fields[i]![0]] = fields[i]![1];
  return o;
}
function mkAC(fields: Fields): Record<string, unknown> {
  const o: Record<string, unknown> = {};
  for (let i = 0; i < fields.length; i += 1) {
    const v = fields[i]![1];
    Object.defineProperty(o, fields[i]![0], { get: () => v, enumerable: true, configurable: true });
  }
  return o;
}
function mkNP(fields: Fields): Record<string, unknown> {
  const o: Record<string, unknown> = Object.create(null);
  for (let i = 0; i < fields.length; i += 1) o[fields[i]![0]] = fields[i]![1];
  return o;
}
function mkPI(fields: Fields): Record<string, unknown> {
  const p: Record<string, unknown> = {};
  for (let i = 0; i < fields.length; i += 1) p[fields[i]![0]] = fields[i]![1];
  return Object.create(p) as Record<string, unknown>;
}
function mkPX(fields: Fields): Record<string, unknown> {
  return new Proxy(mkPD(fields), {});
}
function mkPXH(fields: Fields): Record<string, unknown> {
  return new Proxy(mkPD(fields), REFLECT_HANDLER);
}
const MK: Record<string, (fields: Fields) => Record<string, unknown>> = {
  PD: mkPD,
  AC: mkAC,
  NP: mkNP,
  PI: mkPI,
  PX: mkPX,
  PXH: mkPXH
};
function buildContractArm(mech: string, src: RequirementContract): RequirementContract {
  const mk = MK[mech]!;
  const criteria: unknown[] = [];
  for (const c of src.acceptanceCriteria) {
    criteria.push(
      mk([
        ["id", c.id],
        ["description", c.description],
        ["observableCheck", c.observableCheck],
        ["sourceRefs", c.sourceRefs]
      ])
    );
  }
  const assumptions: unknown[] = [];
  for (const a of src.assumptions) {
    assumptions.push(
      mk([
        ["id", a.id],
        ["statement", a.statement],
        ["source", a.source]
      ])
    );
  }
  return mk([
    ["schemaVersion", src.schemaVersion],
    ["objective", src.objective],
    ["deliverables", src.deliverables],
    ["constraints", src.constraints],
    ["nonGoals", src.nonGoals],
    ["acceptanceCriteria", criteria],
    ["assumptions", assumptions],
    ["questions", []],
    ["authority", src.authority],
    ["sourceRefs", src.sourceRefs]
  ]) as unknown as RequirementContract;
}
function buildTasksArm(mech: string, src: RequirementContract): CoverageTaskRef[] {
  const mk = MK[mech]!;
  const tasks: unknown[] = [];
  for (let i = 0; i < 5; i += 1) {
    const acs: unknown[] = [];
    for (const c of src.acceptanceCriteria) acs.push(mk([["id", c.id]]));
    tasks.push(
      mk([
        ["id", `tsk_0000000${i}`],
        ["acceptanceCriteria", acs]
      ])
    );
  }
  return tasks as CoverageTaskRef[];
}
function buildSetArm(arm: string, src: RequirementContract) {
  const cMech = arm === "PITK" ? "PD" : arm;
  const tMech = arm === "PITK" ? "PI" : arm;
  return { contract: buildContractArm(cMech, src), tasks: buildTasksArm(tMech, src) };
}
function consumeP1(set: { contract: RequirementContract; tasks: CoverageTaskRef[] }): void {
  assertCoverageAllowsStart(set.contract, set.tasks, undefined);
  sink += applyPrecedence(set.contract, "user-first").acceptanceCriteria.length;
}

/* E0-PRE: steady canon (same fixture, 50000 reps) BEFORE any exotic
 * object exists in this process. */
const e0Fixture = { contract: canon, tasks: tasksFor(canon) };
const e0Pre = bench(() => consumeP1(e0Fixture), 50000);
log(`E0-PRE steady canon (pre-exotic): ${(e0Pre * 1e6).toFixed(0)}ns/run`);

/* PI-CT behavior probe (deterministic, not priced): contract-side
 * prototype-inherited records — the gate's {...contract} spread seam is
 * own-enumerable-only, so inherited fields are dropped and the gate THROWS
 * (refusal = fail-closed direction), while the precedence stage (pure
 * [[Get]] reads) stays byte-identical. */
{
  const inherited = buildContractArm("PI", canon);
  check(
    "PI-CT: inherited contract satisfies reads ([[Get]] through proto)",
    (inherited as { objective?: unknown }).objective === canon.objective &&
      !Object.hasOwn(inherited, "objective")
  );
  let outcome = "";
  try {
    assertCoverageAllowsStart(inherited, tasksFor(canon), undefined);
    outcome = "PASSED-GATE";
  } catch (e) {
    outcome = e instanceof TypeError ? "TYPEERROR" : `OTHER:${String(e)}`;
  }
  check("PI-CT: gate REFUSES inherited contract deterministically (TypeError at spread seam)", outcome === "TYPEERROR", outcome);
  const prec = applyPrecedence(inherited, "user-first");
  check(
    "PI-CT: precedence stage is provenance-blind on the same operand (alias return, byte-identical reads)",
    prec === inherited && JSON.stringify(detectConflicts(inherited)) === JSON.stringify(detectConflicts(canon))
  );
  log("PI-CT behavior probe: structural-type-legal inherited contract -> gate deterministic TypeError (REFUSAL, fail-closed direction); precedence stage provenance-blind. Seam: {...contract} own-enumerable spread in assertCoverageAllowsStart.");
}

/* ROT: in-process rotation (contamination view — logged, not adjudicated). */
{
  const ARMS = ["PD", "AC", "NP", "PITK", "PX", "PXH"];
  const POOL = 2048;
  const results: Record<string, number> = {};
  const pools = new Map<string, { contract: RequirementContract; tasks: CoverageTaskRef[] }[]>();
  for (const arm of ARMS) {
    const pool: { contract: RequirementContract; tasks: CoverageTaskRef[] }[] = [];
    for (let i = 0; i < POOL; i += 1) pool.push(buildSetArm(arm, canon));
    pools.set(arm, pool);
  }
  gcBarrier();
  for (const arm of ARMS) {
    const pool = pools.get(arm)!;
    const t0 = performance.now();
    for (let i = 0; i < POOL; i += 1) consumeP1(pool[i]!);
    results[arm] = ((performance.now() - t0) / POOL) * 1e6;
  }
  log(
    `ROT in-process rotation view (CONTAMINATION — not adjudicated): ${ARMS.map((a) => `${a}=${results[a]!.toFixed(0)}ns`).join(" ")}`
  );
}

/* E0-POST: residue on the SAME canon fixture after the exotic excursion. */
const e0Post = bench(() => consumeP1(e0Fixture), 50000);
log(
  `E0-POST steady canon (post-exotic): ${(e0Post * 1e6).toFixed(0)}ns/run (residue ${(e0Post - e0Pre) >= 0 ? "+" : ""}${((e0Post - e0Pre) * 1e6).toFixed(0)}ns = ${(((e0Post - e0Pre) / e0Pre) * 100).toFixed(1)}%)`
);

/* ================================================================
 * RX PRIMARY: process-isolated children, one per arm.
 * ================================================================ */
{
  const ARMS = ["PD", "AC", "NP", "PITK", "PX", "PXH"];
  const rx: Record<string, Record<string, number>> = {};
  for (const arm of ARMS) {
    const res = spawnSync(
      process.execPath,
      ["--allow-natives-syntax", "--expose-gc", "/tmp/r27h-rx-child.mjs", arm],
      { encoding: "utf8", timeout: 120000 }
    );
    check(`RX child ${arm} exit 0`, res.status === 0, `status=${res.status} stderr=${res.stderr}`);
    process.stdout.write(res.stdout);
    const legs: Record<string, number> = {};
    for (const line of res.stdout.split("\n")) {
      const m = /^RX (\w+) (P\d): .* median=(\d+)ns\/run/.exec(line);
      if (m) legs[m[2]!] = Number(m[3]);
    }
    rx[arm] = legs;
  }
  for (const leg of ["P1", "P2", "P3"]) {
    const base = rx["PD"]![leg];
    if (base === undefined) continue;
    const ratios = ARMS.map((a) => `${a}=${rx[a]![leg] === undefined ? "?" : (rx[a]![leg]! / base).toFixed(2)}`).join(" ");
    log(`RX ${leg} ratios vs PD: ${ratios}`);
    for (const arm of ["AC", "NP", "PITK"]) {
      const r = rx[arm]![leg]! / base;
      adjudicate(`RX ${leg} ${arm} plain-lattice class (<3x PD)`, r < 3, `ratio=${r.toFixed(2)}`);
    }
    for (const arm of ["PX", "PXH"]) {
      const r = rx[arm]![leg]! / base;
      adjudicate(`RX ${leg} ${arm} trap-dispatch class (<10x PD)`, r < 10, `ratio=${r.toFixed(2)}`);
    }
  }
  const worst = Math.max(...ARMS.flatMap((a) => Object.values(rx[a]!)));
  log(
    `RX worst isolated arm reading: ${worst.toFixed(0)}ns/run -> ${(1e7 / worst).toFixed(0)} consumer executions needed to cross the 10ms floor (call graph executes the composite once per run)`
  );
}

/* ================================================================
 * COLD: once-per-process first-call totals on dist (veto class).
 * ================================================================ */
{
  for (const state of ["default", "configured"]) {
    const totals: number[] = [];
    for (let i = 0; i < 11; i += 1) {
      const res = spawnSync(process.execPath, ["/tmp/r27h-cold-child.mjs", state], {
        encoding: "utf8",
        timeout: 60000
      });
      check(`cold child ${state} #${i} exit 0`, res.status === 0, res.stderr);
      const m = /first-call chain=(\d+)us/.exec(res.stdout);
      if (m) totals.push(Number(m[1]));
    }
    log(
      `COLD ${state}: median=${median(totals).toFixed(0)}us range=[${Math.min(...totals)}, ${Math.max(...totals)}]us over ${totals.length} fresh processes (once-per-process veto class)`
    );
  }
}

log(`CHECKS ${checks - failures}/${checks} passed${failures > 0 ? ` (${failures} FAILED)` : ""}; ADJUDICATIONS ${adjudications - breaches}/${adjudications} in-class${breaches > 0 ? ` (${breaches} BREACHED)` : ""}; sink=${sink}`);
process.exit(failures === 0 ? 0 : 1);
```

### C2 RX 子驱动 `/tmp/r27h-rx-child.mjs`（458 行，sha256 `c175d131e2c699befaaf9d967433fcfa8128935eab70fe64eed8ad71c4061e27`）

```js
/**
 * R27-H RX child: one process = one arm of the CONSUMER-STAGE
 * PROPERTY-PROVENANCE FACE (twenty-seventh pass — the pass's NEW
 * completeness axis).
 *
 * Axis: WHICH ACCESS PATH serves each consumed property of the structurally
 * typed record operands the consumer stages (run-start gate + precedence)
 * read. Twenty-six rounds of H fixtures (and every production construction
 * site — rg src/ for `new Proxy|Object.create|defineProperty`: zero hits)
 * built every record as a plain object literal whose consumed properties are
 * OWN ENUMERABLE DATA properties reachable in the fast in-object/descriptor
 * path. The public surface is structurally typed, so records whose
 * properties are own ACCESSORS (getter-backed), PROTOTYPE-INHERITED data,
 * NULL-PROTOTYPE own data, or PROXY-mediated ([[Get]]/[[OwnPropertyKeys]]
 * through an exotic object) are protocol-legal inputs whose price was never
 * a controlled variable in any round of any slice (prior Proxy uses were
 * deadness-TRACING tools — S6-H-2 / S7-H-2 — never a priced operand class).
 *
 * Pinned coordinates (orthogonality, attested per pool):
 *   realm          main only (R26-H axis)          — getPrototypeOf identity
 *   elements kind  packed, non-dict (R26-F axis)   — %HasHoleyElements etc.
 *   integrity      extensible (R26-F axis)         — !isFrozen
 *   string bytes   canon references (R25-H/R25-A)  — value equality
 *   field presence all present (R25-B axis)        — defined witness
 *   cardinality    C=1,Q=0,T=5 default shape       — R13-H scale pinned
 *   shape mix      homogeneous per arm (SHAPEK)    — single builder per arm
 * Containers (arrays) stay PLAIN/main-realm/packed in EVERY arm — only the
 * RECORD operands move (contract, criterion, assumption, question, task,
 * task-criterion, options records).
 *
 * Arms: PD (plain own-data; 26-round production class / A-A base through the
 * same builder architecture), AC (own accessor properties), NP
 * (null-prototype own data), PITK (tasks-side records prototype-inherited
 * data; contract plain — the behavior-preserving inherited dose), PX
 * (records wrapped in empty-handler Proxy: exotic-dispatch floor), PXH
 * (Proxy with materialized Reflect traps: full trap tax).
 *
 * Protocol (R26-F/R26-H): process isolation per arm — property-provenance
 * doses genericize ICs, so in-process rotation is a contamination view. This
 * child warms the consumer stages with 2000 PD single-use sets (production
 * IC state), GC-barriers, then prices 3 passes x 4096 single-use own-arm
 * sets per leg. Legs: P1 default-state gate+precedence composite, P2
 * precedence-only, P3 configured Q=1 three-arg gate with ONLY the options
 * record dosed.
 *
 * Run: node --allow-natives-syntax --expose-gc /tmp/r27h-rx-child.mjs <ARM>
 */
import { performance } from "node:perf_hooks";
import { types } from "node:util";
import { extractHeuristicContract } from "/workspace/dist/requirement/heuristic.js";
import { assertCoverageAllowsStart } from "/workspace/dist/requirement/coverage.js";
import { applyPrecedence, detectConflicts } from "/workspace/dist/requirement/precedence.js";

const ARM = process.argv[2];
const ARMS = ["PD", "AC", "NP", "PITK", "PX", "PXH"];
if (!ARMS.includes(ARM)) {
  process.stderr.write(`usage: r27h-rx-child.mjs <${ARMS.join("|")}>\n`);
  process.exit(2);
}

let failures = 0;
let checks = 0;
function check(name, ok, detail) {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL [${ARM}] ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
const forceGc = globalThis.gc;
function gcBarrier() {
  if (forceGc) {
    forceGc();
    forceGc();
  }
}
function log(line) {
  process.stdout.write(`${line}\n`);
}
function median(xs) {
  const s = [...xs].sort((a, b) => a - b);
  return s[Math.floor(s.length / 2)];
}

/* V8 native probes, compiled at runtime (realm/exotic-agnostic). */
function nativeProbe(name) {
  try {
    const fn = new Function("a", `return %${name}(a)`);
    fn([]);
    return fn;
  } catch {
    return undefined;
  }
}
const hasHoley = nativeProbe("HasHoleyElements");
const hasDict = nativeProbe("HasDictionaryElements");
check("native elements-kind probes available", hasHoley !== undefined && hasDict !== undefined);
function isPackedNonDict(a) {
  return hasHoley !== undefined && hasDict !== undefined && !hasHoley(a) && !hasDict(a);
}
function storagePinned(a) {
  return isPackedNonDict(a) && !Object.isFrozen(a);
}

/* ---------------------------------------------------------------- */
/* Canon: the real production default contract, built once. String   */
/* primitives from this object are reused by reference in all arms.  */
/* ---------------------------------------------------------------- */
const OBJ = "fix the login retry bug in src/auth/session.ts and keep tests green";
const canon = (await extractHeuristicContract({ objective: OBJ })).contract;
check("canon default shape Q=0 C=2", canon.questions.length === 0 && canon.acceptanceCriteria.length === 2);
const canonA = (
  await extractHeuristicContract({
    objective: OBJ,
    habits: { requireTests: true, preferReview: false, askBeforeWrite: true }
  })
).contract;
check("canon configured-A shape Q=1 undefaulted", canonA.questions.length === 1 && canonA.questions[0].default === undefined);

/* ---------------------------------------------------------------- */
/* Record builders per provenance mechanism. Field values are read   */
/* from canon (string reuse); every consumed field present.          */
/* ---------------------------------------------------------------- */
const REFLECT_HANDLER = {
  get: Reflect.get,
  has: Reflect.has,
  ownKeys: Reflect.ownKeys,
  getOwnPropertyDescriptor: Reflect.getOwnPropertyDescriptor
};
function mkPD(fields) {
  const o = {};
  for (let i = 0; i < fields.length; i += 1) o[fields[i][0]] = fields[i][1];
  return o;
}
function mkAC(fields) {
  const o = {};
  for (let i = 0; i < fields.length; i += 1) {
    const v = fields[i][1];
    Object.defineProperty(o, fields[i][0], { get: () => v, enumerable: true, configurable: true });
  }
  return o;
}
function mkNP(fields) {
  const o = Object.create(null);
  for (let i = 0; i < fields.length; i += 1) o[fields[i][0]] = fields[i][1];
  return o;
}
function mkPI(fields) {
  const p = {};
  for (let i = 0; i < fields.length; i += 1) p[fields[i][0]] = fields[i][1];
  return Object.create(p);
}
function mkPX(fields) {
  return new Proxy(mkPD(fields), {});
}
function mkPXH(fields) {
  return new Proxy(mkPD(fields), REFLECT_HANDLER);
}
const MK = { PD: mkPD, AC: mkAC, NP: mkNP, PI: mkPI, PX: mkPX, PXH: mkPXH };

/* Operand-set builders. mech = record mechanism for the dosed side(s). */
function buildContract(mech, src) {
  const mk = MK[mech];
  const criteria = [];
  for (const c of src.acceptanceCriteria) {
    criteria.push(
      mk([
        ["id", c.id],
        ["description", c.description],
        ["observableCheck", c.observableCheck],
        ["sourceRefs", c.sourceRefs]
      ])
    );
  }
  const assumptions = [];
  for (const a of src.assumptions) {
    assumptions.push(
      mk([
        ["id", a.id],
        ["statement", a.statement],
        ["source", a.source]
      ])
    );
  }
  const questions = [];
  for (const q of src.questions) {
    questions.push(
      mk([
        ["id", q.id],
        ["question", q.question],
        ["options", q.options]
      ])
    );
  }
  return mk([
    ["schemaVersion", src.schemaVersion],
    ["objective", src.objective],
    ["deliverables", src.deliverables],
    ["constraints", src.constraints],
    ["nonGoals", src.nonGoals],
    ["acceptanceCriteria", criteria],
    ["assumptions", assumptions],
    ["questions", questions],
    ["authority", src.authority],
    ["sourceRefs", src.sourceRefs]
  ]);
}
function buildTasks(mech, src, covered) {
  const mk = MK[mech];
  const tasks = [];
  for (let i = 0; i < 5; i += 1) {
    const acs = [];
    if (covered) {
      for (const c of src.acceptanceCriteria) acs.push(mk([["id", c.id]]));
    }
    tasks.push(
      mk([
        ["id", `tsk_0000000${i}`],
        ["acceptanceCriteria", acs]
      ])
    );
  }
  return tasks;
}
function buildOptions(mech, ids) {
  return MK[mech]([["resolvedQuestionIds", ids]]);
}
/* Arm -> mechanism per operand side. PITK doses ONLY the task records
 * (the behavior-preserving inherited dose; contract-side inherited is the
 * PI-CT behavior probe, adjudicated in the parent). */
function buildSetP1(arm, src) {
  const cMech = arm === "PITK" ? "PD" : arm;
  const tMech = arm === "PITK" ? "PI" : arm;
  return { contract: buildContract(cMech, src), tasks: buildTasks(tMech, src, true) };
}
function buildContractP2(arm, src) {
  return buildContract(arm === "PITK" ? "PI" : arm, src);
}
function buildSetP3(arm, src) {
  /* P3 doses ONLY the options record; contract+tasks stay PD. */
  return {
    contract: buildContract("PD", src),
    tasks: buildTasks("PD", src, true),
    options: buildOptions(arm === "PITK" ? "PI" : arm, [src.questions[0].id])
  };
}

/* ---------------------------------------------------------------- */
/* Consumers (the production composite).                             */
/* ---------------------------------------------------------------- */
let sink = 0;
function consumeP1(set) {
  assertCoverageAllowsStart(set.contract, set.tasks, undefined);
  sink += applyPrecedence(set.contract, "user-first").acceptanceCriteria.length;
}
function consumeP2(contract) {
  sink += applyPrecedence(contract, "user-first").acceptanceCriteria.length;
}
function consumeP3(set) {
  assertCoverageAllowsStart(set.contract, set.tasks, set.options);
  sink += 1;
}

/* ---------------------------------------------------------------- */
/* Attestation: byte-identical behavior + per-pool provenance pins.  */
/* ---------------------------------------------------------------- */
function jsonOf(x) {
  return JSON.stringify(x);
}
{
  /* Behavior parity vs PD on this arm's own mechanism. */
  const pd = buildSetP1("PD", canon);
  const own = buildSetP1(ARM, canon);
  let pdThrew;
  let ownThrew;
  try {
    assertCoverageAllowsStart(pd.contract, pd.tasks, undefined);
    pdThrew = "";
  } catch (e) {
    pdThrew = String(e);
  }
  try {
    assertCoverageAllowsStart(own.contract, own.tasks, undefined);
    ownThrew = "";
  } catch (e) {
    ownThrew = String(e);
  }
  check("gate verdict parity (covered)", pdThrew === "" && ownThrew === "");
  check(
    "precedence output JSON parity",
    jsonOf(applyPrecedence(own.contract, "user-first")) === jsonOf(applyPrecedence(pd.contract, "user-first"))
  );
  /* Uncovered refusal: same message bytes (fail-closed parity). */
  const pdU = { contract: buildContract("PD", canon), tasks: buildTasks("PD", canon, false) };
  const ownU = {
    contract: buildContract(ARM === "PITK" ? "PD" : ARM, canon),
    tasks: buildTasks(ARM === "PITK" ? "PI" : ARM, canon, false)
  };
  let mPd = "";
  let mOwn = "";
  try {
    assertCoverageAllowsStart(pdU.contract, pdU.tasks, undefined);
  } catch (e) {
    mPd = String(e);
  }
  try {
    assertCoverageAllowsStart(ownU.contract, ownU.tasks, undefined);
  } catch (e) {
    mOwn = String(e);
  }
  check("gate refusal message byte parity (uncovered)", mPd !== "" && mPd === mOwn, `${mPd} vs ${mOwn}`);
  /* S5-H-1 triple semantics on this arm's mechanism (contract-side mech;
   * PITK contract is PD there, still attested). */
  const mech = ARM === "PITK" ? "PD" : ARM;
  const mkC = (checks) =>
    buildContract(mech, {
      ...canon,
      acceptanceCriteria: checks.map((oc, i) => ({
        id: `x-${i}`,
        description: "d",
        observableCheck: oc,
        sourceRefs: canon.acceptanceCriteria[0].sourceRefs
      }))
    });
  check("S5-H-1 fast-only -> []", detectConflicts(mkC(["must be fast"])).length === 0);
  check("S5-H-1 slow-only -> []", detectConflicts(mkC(["may be slow"])).length === 0);
  const both = mkC(["must be fast", "may be slow"]);
  check("S5-H-1 fast+slow -> exactly 1 conflict", detectConflicts(both).length === 1);
  check(
    "conflicted precedence JSON parity",
    jsonOf(applyPrecedence(both, "user-first")) ===
      jsonOf(
        applyPrecedence(
          buildContract("PD", {
            ...canon,
            acceptanceCriteria: [
              { id: "x-0", description: "d", observableCheck: "must be fast", sourceRefs: canon.acceptanceCriteria[0].sourceRefs },
              { id: "x-1", description: "d", observableCheck: "may be slow", sourceRefs: canon.acceptanceCriteria[0].sourceRefs }
            ]
          }),
          "user-first"
        )
      )
  );
  /* P3 parity: gate passes with dosed options record. */
  const p3 = buildSetP3(ARM, canonA);
  let p3Threw = "";
  try {
    assertCoverageAllowsStart(p3.contract, p3.tasks, p3.options);
  } catch (e) {
    p3Threw = String(e);
  }
  check("P3 configured gate verdict parity", p3Threw === "");
}
function pinOptionsRecord(set, label) {
  /* P3 pools: contract+tasks stay PD; ONLY the options record is dosed. */
  const c = set.contract;
  const o = set.options;
  check(`${label} P3 contract stays plain PD`, Object.getPrototypeOf(c) === Object.prototype && Object.hasOwn(c, "objective"));
  check(`${label} P3 options field present`, o.resolvedQuestionIds !== undefined);
  check(`${label} P3 options ids array pinned`, storagePinned(o.resolvedQuestionIds) && Object.getPrototypeOf(o.resolvedQuestionIds) === Array.prototype);
  if (ARM === "PD") {
    check(`${label} P3 PD options own data`, Object.getOwnPropertyDescriptor(o, "resolvedQuestionIds")?.value !== undefined);
  } else if (ARM === "AC") {
    check(`${label} P3 AC options accessor`, typeof Object.getOwnPropertyDescriptor(o, "resolvedQuestionIds")?.get === "function");
  } else if (ARM === "NP") {
    check(`${label} P3 NP options null-proto`, Object.getPrototypeOf(o) === null);
  } else if (ARM === "PITK") {
    check(`${label} P3 PITK options inherited`, !Object.hasOwn(o, "resolvedQuestionIds") && o.resolvedQuestionIds !== undefined);
  } else {
    check(`${label} P3 ${ARM} options proxy`, types.isProxy(o));
  }
}
function pinSet(set, label) {
  const c = set.contract;
  const consumedArrays = [c.acceptanceCriteria, c.assumptions, c.questions, set.tasks];
  for (const a of consumedArrays) {
    check(`${label} container storage pinned (packed, non-dict, extensible)`, storagePinned(a));
    check(`${label} container realm pinned (main %Array.prototype%)`, Object.getPrototypeOf(a) === Array.prototype);
  }
  check(`${label} field presence pinned`, c.acceptanceCriteria[0].id !== undefined && set.tasks[0].id !== undefined);
  check(`${label} string bytes pinned (canon values)`, c.objective === canon.objective);
  const rec = c.acceptanceCriteria[0];
  const task = set.tasks[0];
  if (ARM === "PD") {
    check(`${label} PD: own data prop`, Object.getOwnPropertyDescriptor(rec, "id")?.value !== undefined);
    check(`${label} PD: not a proxy`, !types.isProxy(c) && !types.isProxy(task));
  } else if (ARM === "AC") {
    check(`${label} AC: own accessor prop`, typeof Object.getOwnPropertyDescriptor(rec, "id")?.get === "function");
    check(`${label} AC: contract accessor`, typeof Object.getOwnPropertyDescriptor(c, "acceptanceCriteria")?.get === "function");
  } else if (ARM === "NP") {
    check(`${label} NP: null prototype`, Object.getPrototypeOf(c) === null && Object.getPrototypeOf(rec) === null);
    check(`${label} NP: own data`, Object.getOwnPropertyDescriptor(rec, "id")?.value !== undefined);
  } else if (ARM === "PITK") {
    check(`${label} PITK: task props inherited`, !Object.hasOwn(task, "id") && task.id !== undefined);
    check(`${label} PITK: contract plain`, Object.getPrototypeOf(c) === Object.prototype && Object.hasOwn(c, "objective"));
  } else {
    check(`${label} ${ARM}: records are proxies`, types.isProxy(c) && types.isProxy(rec) && types.isProxy(task));
    check(`${label} ${ARM}: proxy proto delegates to main %Object.prototype%`, Object.getPrototypeOf(c) === Object.prototype);
  }
}

/* ---------------------------------------------------------------- */
/* Warm to production IC state: 2000 PD single-use sets per leg.     */
/* ---------------------------------------------------------------- */
{
  const warm1 = [];
  const warm2 = [];
  const warm3 = [];
  for (let i = 0; i < 2000; i += 1) warm1.push(buildSetP1("PD", canon));
  for (let i = 0; i < 2000; i += 1) warm2.push(buildContractP2("PD", canon));
  for (let i = 0; i < 2000; i += 1) warm3.push(buildSetP3("PD", canonA));
  for (let i = 0; i < 2000; i += 1) consumeP1(warm1[i]);
  for (let i = 0; i < 2000; i += 1) consumeP2(warm2[i]);
  for (let i = 0; i < 2000; i += 1) consumeP3(warm3[i]);
}
gcBarrier();

/* ---------------------------------------------------------------- */
/* Timed passes: 3 x 4096 single-use own-arm sets per leg.           */
/* ---------------------------------------------------------------- */
const N = 4096;
function timeLeg(build, consume, legLabel) {
  const perPass = [];
  for (let pass = 0; pass < 5; pass += 1) {
    const pool = [];
    for (let i = 0; i < N; i += 1) pool.push(build(i));
    if (legLabel === "P1") {
      pinSet(pool[0], `${legLabel} pool[0]`);
      pinSet(pool[1], `${legLabel} pool[1]`);
      pinSet(pool[N - 1], `${legLabel} pool[last]`);
    } else if (legLabel === "P3") {
      pinOptionsRecord(pool[0], `${legLabel} pool[0]`);
      pinOptionsRecord(pool[N - 1], `${legLabel} pool[last]`);
    }
    gcBarrier();
    const t0 = performance.now();
    for (let i = 0; i < N; i += 1) consume(pool[i]);
    const t1 = performance.now();
    perPass.push(((t1 - t0) / N) * 1e6);
  }
  log(
    `RX ${ARM} ${legLabel}: passes=[${perPass.map((p) => p.toFixed(0)).join(",")}]ns median=${median(perPass).toFixed(0)}ns/run`
  );
  return median(perPass);
}
timeLeg((i) => buildSetP1(ARM, canon), consumeP1, "P1");
timeLeg((i) => {
  const c = buildContractP2(ARM, canon);
  if (i === 0) {
    check("P2 contract mechanism witness", ARM === "PD" ? Object.hasOwn(c, "objective") : true);
  }
  return c;
}, consumeP2, "P2");
timeLeg((i) => buildSetP3(ARM, canonA), consumeP3, "P3");

log(`CHECKS [${ARM}] ${checks - failures}/${checks} passed${failures > 0 ? ` (${failures} FAILED)` : ""}; sink=${sink}`);
process.exit(failures === 0 ? 0 : 1);
```

### C3 冷层子驱动 `/tmp/r27h-cold-child.mjs`（36 行，sha256 `0c47eece6abcb43c82d62427de0b04efce121b035a44173a14c4782deb938d04`）

```js
/**
 * R27-H cold-layer child: once-per-process first-call production chain
 * total on dist (veto-class anchor, R6-H..R26-H mirror).
 * Run: node /tmp/r27h-cold-child.mjs <default|configured>
 */
import { performance } from "node:perf_hooks";
import { extractHeuristicContract } from "/workspace/dist/requirement/heuristic.js";
import { assertCoverageAllowsStart } from "/workspace/dist/requirement/coverage.js";
import { applyPrecedence } from "/workspace/dist/requirement/precedence.js";

const state = process.argv[2] ?? "default";
const input =
  state === "configured"
    ? {
        objective: "fix the login retry bug in src/auth/session.ts and keep tests green",
        habits: { requireTests: true, preferReview: false, askBeforeWrite: true }
      }
    : { objective: "fix the login retry bug in src/auth/session.ts and keep tests green" };

const t0 = performance.now();
const candidate = await extractHeuristicContract(input);
const contract = candidate.contract;
const tasks = [];
for (let i = 0; i < 5; i += 1) {
  const acs = [];
  for (const c of contract.acceptanceCriteria) acs.push({ id: c.id });
  tasks.push({ id: `tsk_0000000${i}`, acceptanceCriteria: acs });
}
const options =
  contract.questions.length > 0
    ? { resolvedQuestionIds: contract.questions.map((q) => q.id) }
    : undefined;
assertCoverageAllowsStart(contract, tasks, options);
const out = applyPrecedence(contract, "user-first");
const t1 = performance.now();
process.stdout.write(`COLD ${state} first-call chain=${((t1 - t0) * 1e3).toFixed(0)}us (sink=${out.acceptanceCriteria.length})\n`);
```

### C4 三轮运行原始输出（逐字）

```text
CEILING default (9-pass anchor shape, Q=0): extractHeuristicContract=8509ns + run-start gate(Q=0, no options)=733ns + applyPrecedence=251ns = 9493ns once-per-run production total
CEILING configured A (habits set, Q=1 gated): extractHeuristicContract=6919ns + run-start gate(Q=1, all resolved)=698ns + applyPrecedence=212ns = 7828ns once-per-run production total
CEILING configured B (vague, Q=3 gated): extractHeuristicContract=3344ns + run-start gate(Q=3, all resolved)=480ns + applyPrecedence=114ns = 3939ns once-per-run production total
CEILING promotion class: assertCanPromoteFromReview=18.4ns once per promotion (production)
CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=1094ns/call
CEILING critique class (inside run-start chain): critiqueContract(real contract)=593ns/call
CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=2036ns/call
CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=66ns/call
--- CONSUMER-STAGE PROPERTY-PROVENANCE FACE (E0-PRE / PI-CT behavior probe / ROT contamination view / E0-POST residue / RX process-isolated primary / COLD) ---
E0-PRE steady canon (pre-exotic): 828ns/run
PI-CT behavior probe: structural-type-legal inherited contract -> gate deterministic TypeError (REFUSAL, fail-closed direction); precedence stage provenance-blind. Seam: {...contract} own-enumerable spread in assertCoverageAllowsStart.
ROT in-process rotation view (CONTAMINATION — not adjudicated): PD=3180ns AC=7092ns NP=3952ns PITK=10196ns PX=5583ns PXH=9136ns
E0-POST steady canon (post-exotic): 1360ns/run (residue +532ns = 64.3%)
RX PD P1: passes=[1791,1692,1449,1322,1354]ns median=1449ns/run
RX PD P2: passes=[317,297,279,311,292]ns median=297ns/run
RX PD P3: passes=[1366,1383,1318,1010,1441]ns median=1366ns/run
CHECKS [PD] 236/236 passed; sink=112400
RX AC P1: passes=[6538,5049,5061,4866,5121]ns median=5061ns/run
RX AC P2: passes=[603,700,599,716,588]ns median=603ns/run
RX AC P3: passes=[1945,1791,1865,1556,1921]ns median=1865ns/run
CHECKS [AC] 236/236 passed; sink=112400
RX NP P1: passes=[5078,3719,4070,3591,3616]ns median=3719ns/run
RX NP P2: passes=[451,389,356,410,416]ns median=410ns/run
RX NP P3: passes=[1845,1750,1759,1514,1799]ns median=1759ns/run
CHECKS [NP] 236/236 passed; sink=112400
RX PITK P1: passes=[9914,8275,8009,8143,8181]ns median=8181ns/run
RX PITK P2: passes=[2352,2400,2199,2194,2188]ns median=2199ns/run
RX PITK P3: passes=[2210,2026,2123,1811,2152]ns median=2123ns/run
CHECKS [PITK] 236/236 passed; sink=112400
RX PX P1: passes=[6064,5149,5398,5076,5002]ns median=5149ns/run
RX PX P2: passes=[359,359,356,363,357]ns median=359ns/run
RX PX P3: passes=[1905,1682,1785,1491,1767]ns median=1767ns/run
CHECKS [PX] 236/236 passed; sink=112400
RX PXH P1: passes=[10209,9178,9260,9166,9132]ns median=9178ns/run
RX PXH P2: passes=[461,428,432,442,432]ns median=432ns/run
RX PXH P3: passes=[1938,1764,1803,1571,1812]ns median=1803ns/run
CHECKS [PXH] 236/236 passed; sink=112400
RX P1 ratios vs PD: PD=1.00 AC=3.49 NP=2.57 PITK=5.65 PX=3.55 PXH=6.33
ADJUDICATE BREACH RX P1 AC plain-lattice class (<3x PD): ratio=3.49
ADJUDICATE in-class RX P1 NP plain-lattice class (<3x PD): ratio=2.57
ADJUDICATE BREACH RX P1 PITK plain-lattice class (<3x PD): ratio=5.65
ADJUDICATE in-class RX P1 PX trap-dispatch class (<10x PD): ratio=3.55
ADJUDICATE in-class RX P1 PXH trap-dispatch class (<10x PD): ratio=6.33
RX P2 ratios vs PD: PD=1.00 AC=2.03 NP=1.38 PITK=7.40 PX=1.21 PXH=1.45
ADJUDICATE in-class RX P2 AC plain-lattice class (<3x PD): ratio=2.03
ADJUDICATE in-class RX P2 NP plain-lattice class (<3x PD): ratio=1.38
ADJUDICATE BREACH RX P2 PITK plain-lattice class (<3x PD): ratio=7.40
ADJUDICATE in-class RX P2 PX trap-dispatch class (<10x PD): ratio=1.21
ADJUDICATE in-class RX P2 PXH trap-dispatch class (<10x PD): ratio=1.45
RX P3 ratios vs PD: PD=1.00 AC=1.37 NP=1.29 PITK=1.55 PX=1.29 PXH=1.32
ADJUDICATE in-class RX P3 AC plain-lattice class (<3x PD): ratio=1.37
ADJUDICATE in-class RX P3 NP plain-lattice class (<3x PD): ratio=1.29
ADJUDICATE in-class RX P3 PITK plain-lattice class (<3x PD): ratio=1.55
ADJUDICATE in-class RX P3 PX trap-dispatch class (<10x PD): ratio=1.29
ADJUDICATE in-class RX P3 PXH trap-dispatch class (<10x PD): ratio=1.32
RX worst isolated arm reading: 9178ns/run -> 1090 consumer executions needed to cross the 10ms floor (call graph executes the composite once per run)
COLD default: median=1650us range=[1623, 1717]us over 11 fresh processes (once-per-process veto class)
COLD configured: median=1605us range=[1580, 1681]us over 11 fresh processes (once-per-process veto class)
CHECKS 37/37 passed; ADJUDICATIONS 12/15 in-class (3 BREACHED); sink=224580
```

```text
CEILING default (9-pass anchor shape, Q=0): extractHeuristicContract=8055ns + run-start gate(Q=0, no options)=704ns + applyPrecedence=247ns = 9005ns once-per-run production total
CEILING configured A (habits set, Q=1 gated): extractHeuristicContract=6198ns + run-start gate(Q=1, all resolved)=686ns + applyPrecedence=214ns = 7097ns once-per-run production total
CEILING configured B (vague, Q=3 gated): extractHeuristicContract=4103ns + run-start gate(Q=3, all resolved)=517ns + applyPrecedence=113ns = 4733ns once-per-run production total
CEILING promotion class: assertCanPromoteFromReview=19.3ns once per promotion (production)
CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=1085ns/call
CEILING critique class (inside run-start chain): critiqueContract(real contract)=605ns/call
CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=2057ns/call
CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=66ns/call
--- CONSUMER-STAGE PROPERTY-PROVENANCE FACE (E0-PRE / PI-CT behavior probe / ROT contamination view / E0-POST residue / RX process-isolated primary / COLD) ---
E0-PRE steady canon (pre-exotic): 821ns/run
PI-CT behavior probe: structural-type-legal inherited contract -> gate deterministic TypeError (REFUSAL, fail-closed direction); precedence stage provenance-blind. Seam: {...contract} own-enumerable spread in assertCoverageAllowsStart.
ROT in-process rotation view (CONTAMINATION — not adjudicated): PD=3167ns AC=7034ns NP=3929ns PITK=10239ns PX=5535ns PXH=9208ns
E0-POST steady canon (post-exotic): 1326ns/run (residue +505ns = 61.5%)
RX PD P1: passes=[1844,1350,1294,1294,1272]ns median=1294ns/run
RX PD P2: passes=[263,261,248,241,242]ns median=248ns/run
RX PD P3: passes=[1368,1042,1312,991,1458]ns median=1312ns/run
CHECKS [PD] 236/236 passed; sink=112400
RX AC P1: passes=[6437,5211,5015,4970,5023]ns median=5023ns/run
RX AC P2: passes=[683,670,657,692,663]ns median=670ns/run
RX AC P3: passes=[1876,1704,1729,1494,1803]ns median=1729ns/run
CHECKS [AC] 236/236 passed; sink=112400
RX NP P1: passes=[5006,3621,3523,3512,3532]ns median=3532ns/run
RX NP P2: passes=[430,382,382,380,420]ns median=382ns/run
RX NP P3: passes=[1864,1759,1713,1498,1731]ns median=1731ns/run
CHECKS [NP] 236/236 passed; sink=112400
RX PITK P1: passes=[9917,8187,8069,7976,8024]ns median=8069ns/run
RX PITK P2: passes=[2284,2388,2223,2211,2263]ns median=2263ns/run
RX PITK P3: passes=[2190,2104,2041,1743,2151]ns median=2104ns/run
CHECKS [PITK] 236/236 passed; sink=112400
RX PX P1: passes=[6440,5102,5149,5016,4988]ns median=5102ns/run
RX PX P2: passes=[390,356,359,365,368]ns median=365ns/run
RX PX P3: passes=[1934,1846,1499,1754,1496]ns median=1754ns/run
CHECKS [PX] 236/236 passed; sink=112400
RX PXH P1: passes=[10354,9290,9288,9383,9261]ns median=9290ns/run
RX PXH P2: passes=[469,438,434,436,444]ns median=438ns/run
RX PXH P3: passes=[1865,1854,1734,1494,1716]ns median=1734ns/run
CHECKS [PXH] 236/236 passed; sink=112400
RX P1 ratios vs PD: PD=1.00 AC=3.88 NP=2.73 PITK=6.24 PX=3.94 PXH=7.18
ADJUDICATE BREACH RX P1 AC plain-lattice class (<3x PD): ratio=3.88
ADJUDICATE in-class RX P1 NP plain-lattice class (<3x PD): ratio=2.73
ADJUDICATE BREACH RX P1 PITK plain-lattice class (<3x PD): ratio=6.24
ADJUDICATE in-class RX P1 PX trap-dispatch class (<10x PD): ratio=3.94
ADJUDICATE in-class RX P1 PXH trap-dispatch class (<10x PD): ratio=7.18
RX P2 ratios vs PD: PD=1.00 AC=2.70 NP=1.54 PITK=9.13 PX=1.47 PXH=1.77
ADJUDICATE in-class RX P2 AC plain-lattice class (<3x PD): ratio=2.70
ADJUDICATE in-class RX P2 NP plain-lattice class (<3x PD): ratio=1.54
ADJUDICATE BREACH RX P2 PITK plain-lattice class (<3x PD): ratio=9.13
ADJUDICATE in-class RX P2 PX trap-dispatch class (<10x PD): ratio=1.47
ADJUDICATE in-class RX P2 PXH trap-dispatch class (<10x PD): ratio=1.77
RX P3 ratios vs PD: PD=1.00 AC=1.32 NP=1.32 PITK=1.60 PX=1.34 PXH=1.32
ADJUDICATE in-class RX P3 AC plain-lattice class (<3x PD): ratio=1.32
ADJUDICATE in-class RX P3 NP plain-lattice class (<3x PD): ratio=1.32
ADJUDICATE in-class RX P3 PITK plain-lattice class (<3x PD): ratio=1.60
ADJUDICATE in-class RX P3 PX trap-dispatch class (<10x PD): ratio=1.34
ADJUDICATE in-class RX P3 PXH trap-dispatch class (<10x PD): ratio=1.32
RX worst isolated arm reading: 9290ns/run -> 1076 consumer executions needed to cross the 10ms floor (call graph executes the composite once per run)
COLD default: median=1636us range=[1619, 1702]us over 11 fresh processes (once-per-process veto class)
COLD configured: median=1599us range=[1578, 1700]us over 11 fresh processes (once-per-process veto class)
CHECKS 37/37 passed; ADJUDICATIONS 12/15 in-class (3 BREACHED); sink=224580
```

```text
CEILING default (9-pass anchor shape, Q=0): extractHeuristicContract=8463ns + run-start gate(Q=0, no options)=734ns + applyPrecedence=253ns = 9450ns once-per-run production total
CEILING configured A (habits set, Q=1 gated): extractHeuristicContract=7354ns + run-start gate(Q=1, all resolved)=676ns + applyPrecedence=211ns = 8241ns once-per-run production total
CEILING configured B (vague, Q=3 gated): extractHeuristicContract=3346ns + run-start gate(Q=3, all resolved)=469ns + applyPrecedence=112ns = 3927ns once-per-run production total
CEILING promotion class: assertCanPromoteFromReview=18.3ns once per promotion (production)
CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=1136ns/call
CEILING critique class (inside run-start chain): critiqueContract(real contract)=601ns/call
CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=2069ns/call
CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=73ns/call
--- CONSUMER-STAGE PROPERTY-PROVENANCE FACE (E0-PRE / PI-CT behavior probe / ROT contamination view / E0-POST residue / RX process-isolated primary / COLD) ---
E0-PRE steady canon (pre-exotic): 814ns/run
PI-CT behavior probe: structural-type-legal inherited contract -> gate deterministic TypeError (REFUSAL, fail-closed direction); precedence stage provenance-blind. Seam: {...contract} own-enumerable spread in assertCoverageAllowsStart.
ROT in-process rotation view (CONTAMINATION — not adjudicated): PD=3100ns AC=7705ns NP=3658ns PITK=9449ns PX=5468ns PXH=9100ns
E0-POST steady canon (post-exotic): 1323ns/run (residue +509ns = 62.5%)
RX PD P1: passes=[1749,1537,1248,1252,1279]ns median=1279ns/run
RX PD P2: passes=[294,250,251,248,245]ns median=250ns/run
RX PD P3: passes=[1370,1319,1318,970,1410]ns median=1319ns/run
CHECKS [PD] 236/236 passed; sink=112400
RX AC P1: passes=[6438,5444,5154,5116,5094]ns median=5154ns/run
RX AC P2: passes=[658,635,685,619,653]ns median=653ns/run
RX AC P3: passes=[1980,1773,1871,1553,1818]ns median=1818ns/run
CHECKS [AC] 236/236 passed; sink=112400
RX NP P1: passes=[5136,3757,3615,3617,3592]ns median=3617ns/run
RX NP P2: passes=[444,386,373,374,374]ns median=374ns/run
RX NP P3: passes=[1870,1706,1787,1459,1916]ns median=1787ns/run
CHECKS [NP] 236/236 passed; sink=112400
RX PITK P1: passes=[10010,8243,8147,8237,8133]ns median=8237ns/run
RX PITK P2: passes=[2357,2369,2307,2166,2187]ns median=2307ns/run
RX PITK P3: passes=[2211,2092,2275,1785,2286]ns median=2211ns/run
CHECKS [PITK] 236/236 passed; sink=112400
RX PX P1: passes=[6081,5072,5089,4986,4970]ns median=5072ns/run
RX PX P2: passes=[372,343,340,353,345]ns median=345ns/run
RX PX P3: passes=[1999,1679,1744,1494,1728]ns median=1728ns/run
CHECKS [PX] 236/236 passed; sink=112400
RX PXH P1: passes=[10153,8992,9090,9143,9022]ns median=9090ns/run
RX PXH P2: passes=[470,430,434,436,445]ns median=436ns/run
RX PXH P3: passes=[1865,1798,1778,1481,1740]ns median=1778ns/run
CHECKS [PXH] 236/236 passed; sink=112400
RX P1 ratios vs PD: PD=1.00 AC=4.03 NP=2.83 PITK=6.44 PX=3.97 PXH=7.11
ADJUDICATE BREACH RX P1 AC plain-lattice class (<3x PD): ratio=4.03
ADJUDICATE in-class RX P1 NP plain-lattice class (<3x PD): ratio=2.83
ADJUDICATE BREACH RX P1 PITK plain-lattice class (<3x PD): ratio=6.44
ADJUDICATE in-class RX P1 PX trap-dispatch class (<10x PD): ratio=3.97
ADJUDICATE in-class RX P1 PXH trap-dispatch class (<10x PD): ratio=7.11
RX P2 ratios vs PD: PD=1.00 AC=2.61 NP=1.50 PITK=9.23 PX=1.38 PXH=1.74
ADJUDICATE in-class RX P2 AC plain-lattice class (<3x PD): ratio=2.61
ADJUDICATE in-class RX P2 NP plain-lattice class (<3x PD): ratio=1.50
ADJUDICATE BREACH RX P2 PITK plain-lattice class (<3x PD): ratio=9.23
ADJUDICATE in-class RX P2 PX trap-dispatch class (<10x PD): ratio=1.38
ADJUDICATE in-class RX P2 PXH trap-dispatch class (<10x PD): ratio=1.74
RX P3 ratios vs PD: PD=1.00 AC=1.38 NP=1.35 PITK=1.68 PX=1.31 PXH=1.35
ADJUDICATE in-class RX P3 AC plain-lattice class (<3x PD): ratio=1.38
ADJUDICATE in-class RX P3 NP plain-lattice class (<3x PD): ratio=1.35
ADJUDICATE in-class RX P3 PITK plain-lattice class (<3x PD): ratio=1.68
ADJUDICATE in-class RX P3 PX trap-dispatch class (<10x PD): ratio=1.31
ADJUDICATE in-class RX P3 PXH trap-dispatch class (<10x PD): ratio=1.35
RX worst isolated arm reading: 9090ns/run -> 1100 consumer executions needed to cross the 10ms floor (call graph executes the composite once per run)
COLD default: median=1641us range=[1621, 1720]us over 11 fresh processes (once-per-process veto class)
COLD configured: median=1587us range=[1576, 1684]us over 11 fresh processes (once-per-process veto class)
CHECKS 37/37 passed; ADJUDICATIONS 12/15 in-class (3 BREACHED); sink=224580
```

## 附录 D：临时资产 sha256 清单与删除记录

全部测量资产仅存在于 `/tmp`，未向仓库提交任何一个；本报告归档其全文（附录 A/C）或关键输出（附录 B/C4）后**全部删除**。

| 资产 | sha256 | 归档位置 |
|---|---|---|
| r27h-substrate.mts（新轴父驱动，258 行） | `024a5069ad5a56a2da1a3621c6d478cca7c4f5e7207d3955620fe9fc1e7ec480` | 附录 A1 全文 |
| r27h-substrate-child.mjs（新轴子驱动，310 行） | `153ab025c21af3ac250dcf0668430af0237bf3b9b2d72f733d959e57d972f77f` | 附录 A2 全文 |
| r27h-sub-run1.txt | `2b64b99d7a115602a52754309ce760117ca9a82a5593b75ed4ee56ee9d48aef5` | 附录 B1 逐字 |
| r27h-sub-run2.txt | `d917eae73e2e44f53e01f4ab6ff4b1b53f6ecb584d49982befb2a166f0381730` | 附录 B2 逐字 |
| r27h-sub-run3.txt | `abcee6760e98acd8c5aeed3a98c4347eb30d1aac9173d7b80bcbb4121c1b634a` | 附录 B3 逐字 |
| r27h-provenance.mts（存档轴父驱动，484 行） | `4773e4985b95654830122ebb64e165fd2a92fdbde6869dc8421690ce2df22ecc` | 附录 C1 全文 |
| r27h-rx-child.mjs（存档轴 RX 子驱动，458 行） | `c175d131e2c699befaaf9d967433fcfa8128935eab70fe64eed8ad71c4061e27` | 附录 C2 全文 |
| r27h-cold-child.mjs（存档轴冷层子驱动，36 行） | `0c47eece6abcb43c82d62427de0b04efce121b035a44173a14c4782deb938d04` | 附录 C3 全文 |
| r27h-run1.txt | `06beabf2f77e9a7e5c8b0ebdc6d0faa418ee2409148755cd799c34ae0bbfba77` | 附录 C4 逐字 |
| r27h-run2.txt | `e5045e2f4ef9f6040e7a11f5a75d53abf1abe0880dcc6e2431168f61e0123ae1` | 附录 C4 逐字 |
| r27h-run3.txt | `4d6e3731a378270332e0518cfaec862a6e7ac877d415df0a2bff7071d86cdf02` | 附录 C4 逐字 |
| r27h-smoke.txt（存档轴首次干净运行，内容同 run 系列格式） | `f6042e742e727a36c64c4d47ea87673c9e72a2b285a9771cac00cf71855d3885` | sha 留档 |
| r27h-gate.log（pnpm gate 全量输出） | `ce8b146b261ec2ea5d7b578fd409711cdadd2565ec62f91fba29fa59b592a793` | §0 摘要（1168 pass / 1 skipped / 0 fail / 78 suites，GATE_EXIT=0） |
| r27h-sig-dist.txt / r27h-sig-src.txt（奇偶签名逐段调试快照） | `f20ec5deb8dbae3004598d1ac1e353c84b8cf89ab4a6cc1c2540f196d06406ff` / `1fefe0040a1dc40cd29d5fd39be43eb25a8a1ec4072ca90187eed701370e61a8` | §0 勘误 (b) 定位记录 |

删除记录：上表全部文件已于报告提交前自 `/tmp` 删除（含本附录装配用临时片段）。
