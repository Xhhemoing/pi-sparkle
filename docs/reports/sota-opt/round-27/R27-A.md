MODEL_SLUG=claude-fable-5-thinking-xhigh

# R27-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第二十七遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 27 / R27-A（Round 1–26 同区第二十七遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `9e90035`（R26-I 空枚举收口 + Round 27 开卷；本报告全部测量在该 tip 上完成——测量期两次 fetch 均停在 `9e90035`。**成稿推送后 exclusive docs-only 快进一笔 `c4f4462`**：R26-J 空枚举收口（新铸命名轴 **DELINV**，J 切片偏好持久面删除坐标）+ R27-B 派出，按令 rebase 至新 tip；增量 `9e90035..c4f4462` 经 `git diff --stat` 核对**全部为 docs/reports/sota-opt/ 文件、`src/` 零行**，快进后 14 文件 `git diff 7acb666..HEAD` 复验仍为 **0 行**——两 tip 上切片字节相同，全部测量原样有效；DELINV 已补入 §0 不移植栏；R26-H 在飞未合入、未触碰）
**分支:** `cursor/r27-a-tracking-twenty-seventh-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S27-A-\* ID，切片零代码改动；
整片预算收口第二十五次复核成立，并新增一条实测轴——「标量数值值类面 /
scalar numeric value-class face」（axis 16）补上值平面的另一半：R25-A
剂量了字符串输入的**内容类**（正则扫描 / JSON 转义 / 相等扫描三消费者，
全程数值字段钉死生产值），R26-A 剂量了进程环境（collation locale，全程
数值恒定），而**数值字段装的是哪一个 double**（同字段数、同字符串、同
环境下的值类坐标）从未是战役任何一轮的受控变量。切片恰有四个值类敏感的
数值消费操作类（F7a 普查）：① hashAssessment 站点 JSON.stringify 内的
number→string 最短往返格式化（每哈希恰 4 个 unit-score 数 ×3 腿/gate，
切片第一大组件内）+ 零流量 hashSummary/analysis-content 站点；②
`Number(x.toFixed(4))` 舍入恰 5 站点（prescore.ts:69/79/82/83、
combined-score.ts:28）；③ IEEE-754 算术腿（quality×coverage、dip 乘法、
0.7·min+0.3·max 混合——次正规操作数可逐出硬件快路径）；④ 整数腿（幂等键
`${hash}:${seq}` 模板插值、payload.seq === 比较、nextTrackingSeq 的
seq+1/Math.max、parse 侧 Number.isInteger/isFinite 门）。全切片零 bigint /
parseInt / parseFloat / toPrecision / toExponential / toLocaleString。
剂量类：SMI 整数 {0,1}、生产 4dp 格网短小数、二进小数、17 有效位全宽
double、次正规（5e-324 是 parse 合法的：有限且在 [0,1]）、ε 边界，及
seq 的 SMI/int32/2^53 量级梯。定价五组（三次独立运行区间）：**16a**
hashAssessment 数类谱——int01 生产格 1932–1934 ns/hash 恒为最便宜格，
最贵合法类 ε 边界 ×1.25–1.29（2414–2496 ns；stringify 隔离显微镜内
格式化增量 303–309 ns/hash，载荷 428→502 字符），17 位类 ×1.24–1.27，
次正规 ×1.12–1.17，全类全跑 ⇒ 最坏合法对抗面 +7.2~+8.4 µs/run（预算
~10%，量级仍差落地线 ~4 个数量级）；**16b** toFixed(4) 输入类剂量——
五个合法类平坦 104–119 ns（无对抗放大面），次正规反而最快（87–92 ns，
短指数路径）；**16c** 算术类腿——混合臂次正规操作数 +34~+36 ns/op
（×1.29–1.30，硬件慢路径实测存在但每 gate 仅 1 次 ⇒ ~+170 ns/run 噪声
级），prescore dip 臂次正规平坦（714–719 vs 725–730 ns）；**16d** seq
量级梯——nextTrackingSeq 平坦（43–57 ns 无量级罚），幂等键插值 SMI
5.5–5.7 ns vs HeapNumber 类 12.7–16.0 ns（+7~+10 ns），apply 整体
beyond-int32 格 +0.4 µs/gate——但生产 seq 恒小 SMI（F7b），2^31 格需
21 亿事件不可达；**16e** e2e 回声——全 17 位最坏类 apply 7.01–7.21 µs
vs 生产格 6.24–6.31 µs（+0.77–0.91 µs/gate ⇒ +3.9–4.6 µs/run）。契约角
证人（F7c，三次运行逐字节一致）：-0 是哈希不可见的（stringify "0"，
±0 同哈希、JSON 往返静默正规化 -0→+0 不移哈希——冻结契约把零的符号商掉，
与 R26-A collation 面相反是**移植安全**的）；非有限 score 哈希可表示
（NaN/Infinity 均序列化 "null" 且塌缩同哈希）但写侧 fail-closed 拒绝
（"must be a finite number in [0, 1]"，无非有限字节可达账本）；哈希面
对 double **值精确**（0.3 ≠ 0.1+0.2 两哈希，17 位/次正规 JSON 往返
逐位保真，全程零量化）。生产铸造普查（F7b + F7d）：铸造路径只产
Number(x.toFixed(4)) 格网值（700 种子化格网试验 × 4 字段 = 2800/2800
全部 ≤4 小数位、无 -0，字母表恰 40 个不同值）——**生产恒坐最便宜格式化
类**，贵类只经公开 API（构造 assessment）可达。全部可想摊平路径经换名
检查逐一判为既有排除 ID 本体或冻结契约功能性变更（5 项新记录，见 §0），
**只产出归档证人与数值值类格网的闭合，未产出可落地候选**，按令即空
收口。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R26-A 至本轮基线
（`9e90035`）**逐字节未变**（`git diff 7acb666..9e90035 -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，二十七遍全程零
diff；清单核对恰 14 文件（12 + 2）并全部实际重读，`wc -l` 合计 1796 行；
成稿后快进 tip `c4f4462` 上 `git diff 7acb666..HEAD` 复验同为空——两点
均验空）。调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..c4f4462 -- src/` **完全为空**（跨 R10…R27 开轮十八段
全部为 docs 提交），生产调用方经本轮全库 grep 复核仍为
`src/run/supervisor.ts:483`（applyTrackingGate/nextTrackingSeq；两个
settle 调用点 601/711 均经 settleSupervisedOutcome 首行
`if (assessment === undefined) return`（:482）⇒ 生产 O(1) no-op，第十八
次复验）/ `src/run/coordinator.ts:444` / `src/run/flowchart-run.ts:320`
（applyChildThreeLine），每子结果一次（~5 次/run），事件表几十级（41）。
R26-A 的预算天花板（~69–84 µs/run）经本轮在当前 VM（Node v22.22.2，
PATH 前置 nvm）实测复核为 **~68–84 µs/run**（五格锚点矩阵三次独立运行
13.6–16.7 µs/gate，与 R26-A 69–84 / R25-A 69–89 / R24-A 67–84 重叠
同带；本轮无孤点上沿，最高格为 run2 PASSED×no-contract 16.7）。量级
结论不变——稳态下即使切片零成本，节省上界 ~0.07–0.08 ms/run，仍比落地
线低约三个量级。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
功能性变更（5 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项
+ R15-A 5 项 + R16-A 4 项 + R17-A 4 项 + R18-A 4 项 + R19-A 2 项 +
R20-A 3 项 + R21-A 2 项 + R22-A 5 项 + R23-A 5 项 + R24-A 5 项 +
R25-A 5 项 + R26-A 4 项共 72 项换名检查全部复核维持，未追认任何
S12…S26-A-\* ID），按令一律不铸新 ID。

R12-A 分解的五大组件占比（hashAssessment×3 ~39–40% / validateEvent
~19% / turn ~11–12% / prescore×2 ~9.2–9.3% / O(E) 五扫描+拷贝
~1.9%）在本 VM 逐一复现（§1 表），R18-A skip 定价与 R19-A replay
定价抽测复现同带（guard-3 skip 1942–2020 ns = 13.2–13.9% of apply；
replay find#1 3174–3210 ns = 21.8% of apply）。第二十七遍收口，
**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；未触碰在飞的
  R26-H 分支与文件（R26-J 于本轮成稿推送后由 exclusive 收口合入，其
  报告仅按令追读以做划界，分支未触碰），未触碰已收口的 R26-\* 报告。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表，含 S13-B-1；
  R12-A…R26-A 空枚举未铸 ID，确认）/ PROGRESS.md / round-27/PLAN.md
  （A 段 + 全部 plan-ban 轴）/ round-26/PLAN.md / R26-A / R26-I /
  R25-A / R24-A / R23-A / R22-A / R21-A / R20-A；成稿后 exclusive
  快进带入 R26-J，按令追读其 DELINV 轴定义做划界（见下条）。
  EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
- 环境：本 VM PATH 前置 `~/.nvm/versions/node/v22.22.2/bin`
  （Node v22.22.2，满足 engines >=22.19.0），依赖已装
  （`pnpm install --frozen-lockfile` 基线），全部测量在 v22.22.2 完成。
  `package-lock.json` 未出现，无需处理。
- **不移植禁令逐项核对**：R26-B privacy / provider-policy
  constraint-leg face（B）/ R26-D injection-seam default-arm face（D）/
  R26-F input storage-state face（F）/ R26-G edge condition-kind face
  （G）/ **R26-I argv surface-form face（I；argv 句法几何——flag 序 ×
  `--flag=value`/`--flag value` × 别名 × last-wins，纯 CLI 面，切片
  零 argv 读取，F7a 普查行零 process.\* 复证不可移植）**/ R25-B
  task-face optional-field presence topology / R25-D
  identity-coordinate face / R25-F plan-header identity-geometry face /
  R25-G injection command-kind face / R25-H consumer-stage
  string-representation census / R25-I process-environment coordinate /
  R24-B M=1 catalog collapse / R24-D approval-authority face / R24-F
  state serialization-boundary provenance face / R24-H
  operand-residency census / R24-I adaptation-state coordinate 均未
  移植为 A ID。**R26-J DELINV（成稿后快进带入的中途新增栏）**：J 切片
  偏好持久面删除坐标（`deleteObservation` 变异动词 × 删除目标位置 ×
  持久墓碑），宿主是 preferences store 的存储变异面——A 切片零存储、
  零删除动词、事件表 append-only（`[...events]` 不可变拷贝），坐标
  在本切片无宿主，不可移植、不补铸、不抢先编号（PLAN 已由 parent
  更新为全切片禁令，照令遵守）。FITQ / NAMESHAPE / SCALEX / XPROC /
  YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX /
  CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG /
  PRICEX / RTL / DELINV 未编号。R26-H 运行全程未合入。
- **与三个名义近邻的专项划界**（axis 16 逐字复核后判定**非移植非
  补铸**）：
  1. **vs R25-A「value-domain / string content-class census」**（prior
     A 轴）：R25-A 的受控变量是**字符串**输入的内容类（claim 文本 ×
     正则扫描、turnId × JSON 转义编码类、id × includes 相等扫描——三
     消费者清单全部内容敏感、全部字符串操作数），其 14a–14e 全程把
     prescore/score/seq 等**数值字段钉死在生产值**，报告明言只覆盖
     值平面的字符串半边。axis 16 的受控变量是**数值**字段装的哪一个
     double（字符串全程钉死生产纹理），消费者是格式化器（stringify
     数字臂）、舍入器（toFixed）、算术单元与整数插值——四个消费类
     没有一个在 R25-A 的三消费者清单里。操作数类型、消费者、机制
     三不同；名义重叠仅在「值」一词。
  2. **vs R26-A「collation-locale face」**（prior A 轴）：R26-A 的
     受控变量是进程隐式环境（ICU 默认 collation），操作数与值全程
     恒定；axis 16 全程单环境（en/C 族最便宜格，R26-A 15b 已价），
     动的是值本身。环境坐标 vs 值坐标正交。
  3. **vs S6-A-1（toFixed 移除既有拒绝）**：S6-A-1 拒绝的是**变换
     本身**（把 `Number(x.toFixed(4))` 换成乘除舍入/删除——功能性
     变更）；axis 16b 定价的是同一站点的**输入值类**（哪类 double
     进 toFixed），不提议改动任何站点。16b 实测五类平坦 104–119 ns
     恰好反向加固 S6-A-1：该站点对输入类不敏感，无可摊平面。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或功能性变更，**一律不铸 S27-A-\* ID**。R12-A…R26-A 共 72 项
  换名检查全部复核维持，不再逐条重列；以下 5 项为本轮**新记录**，
  全部由 axis 16 的数值视角引出）：
  1. **哈希面数值量化/重格式化**（对 17 位/次正规类先 toFixed(4) 或
     自定 formatter 再 stringify，摊平 16a 最贵格的直接路）= 冻结
     哈希契约的**功能性变更本体**：F7c-iii 证明哈希面对 double 值
     精确（0.3 ≠ 0.1+0.2 两哈希、17 位往返逐位保真）——量化会改变
     parse 合法输入的既有哈希值 = 改 CAS/幂等键 = 数据面契约变更，
     且 F7b 证明生产恒在最便宜格 ⇒ 生产收益恒 0，拒列；
  2. **闭字母表 number→string 缓存**（F7d 的 40 值生产字母表 ⇒
     数字串记忆化/预format 表）= **X1-1 / S8-G-2 memo 禁令本体**
     （哈希输入静默缓存家族）+ R12-A §0-11 常量折叠伞；且 16a 显示
     生产格 formatter 增量本就 0 ns（int01 基线），缓存无差可省，
     拒列；
  3. **toFixed(4) 换乘除舍入/删除**（16b 站点的强度削减）= **S6-A-1
     本体**（既有拒绝维持）；16b 实测输入类平坦（104–119 ns，无贵
     格）进一步压灭动机——变换无收益面，拒列；
  4. **次正规操作数冲刷**（混合臂/算术腿前 flush-to-zero 或
     `Math.max(x, MIN_NORMAL)` 规避 16c 的 ×1.29 慢路径）= 改变
     parse 合法输入的计算结果（次正规 H/P 是 isMachineScore 合法值，
     冲刷改 score 值 ⇒ 改哈希 ⇒ 契约变更）+ 站点每 gate 仅 1 次
     （+34 ns/op = ~170 ns/run 噪声级）；X0-7 阈值冻结连带（score
     语义不可动），拒列；
  5. **seq 类型收窄/幂等键整数化**（把 `${hash}:${seq}` 换二进制键
     或 seq 钳位 int32，摊平 16d 的 HeapNumber 格）= 幂等键格式是
     **数据面契约**（S8-A-1 / S11-A-2 幂等家族连带，账本既有键不可
     重写）；且生产 seq 恒小 SMI（F7b），beyond-int32 格需 2^31 事件
     不可达（E 轴 R14-A 穿越点 ~10⁵ 早已先爆），插值差 +7~+10 ns
     噪声级，拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  幂等双 find/死扫描 = S8-A-1 / S11-A-2；episode/seq/status/copy 五
  扫描逐条 = X0-4/X2-4/S1-A-1/S1-A-9/R1-A 清单（R12-A 起锁定）；写侧
  validateEvent = S4-G-2 族（实现在 events.ts，G 切片）；第二次
  prescore = S1-A-2；turn 恒定输入常量折叠 = R12-A §0-11 伞 +
  R16-A §0-2；skip 守卫次序/享元/prescore 复用 = R18-A §0-1/2/4
  维持；replay 快路径/重算去重 = R19-A §0-1/2 维持；hashSummary
  缓存/增量 = R20-A §0-2/3 维持；H 面 = R21-A 维持；分析-隔离面 =
  R22-A §0-1..5 维持；G×E 积分账本五路 = R23-A §0-1..5 维持；
  输入面字段粒度五路 = R24-A §0-1..5 维持；内容类五路 = R25-A
  §0-1..5 维持；collation 面四路 = R26-A §0-1..4 维持。
- 未重开任何 X\* / S1-\* … S26-\* 条目。**R25-A 字符串内容类普查
  不补铸、不重测**——axis 16 全程固定全部字符串为生产纹理 ASCII，
  只动数值字段的值（§0 划界 1）；**R26-A collation-locale 面不补铸、
  不重测**——全程单环境，locale 恒 en 族（划界 2）；**R24-A residual
  input-face list-dimension 不补铸**（全程固定字段计数）；**R23-A
  G×E 自耦合积分账本不补铸**（全程固定 E=41）；**R22-A 公开导出普查
  不补铸**（hashSummary/analysis 站点零流量作为既有事实引用，16a 的
  贵类剂量走 hashAssessment 生产站点）；**R21-A H 输入面不补铸**
  （humanInput 除 16c 混合臂公开 API 剂量外恒 `{}`，且 16c 剂量的是
  H **值的数类**非 H 面形状——R21-A 剂量的是 humanInput 三维输入
  拓扑）；**R20-A openMinors M 轴与驻留链 K 不补铸**（恒 M=0/K=0）；
  R19/R18 各一格组成连续性抽测；R17…R14 均不重测。S11-A-1 / S11-A-2 /
  S10-A-1 / S9-A-1 / S8-A-1..3 / S7-A-1..4 / X0-4/X2-4 / X0-6 /
  S6-A-1 专项复核：本轮零触碰、零重提（S6-A-1 获 16b 反向加固，
  见划界 3），全部维持关闭。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点五格
  （gate-outcome × contract × max-codes）覆盖；配置态逐面复核——
  (a) settle 带/不带 assessment：生产两调用点 601/711 均不带
  （O(1) no-op 源码级复验 :482 首行早退）；(b) trackingConfig 覆写/
  applyUserThreshold：生产 from-child 恒走 DEFAULT（R22-A config
  双臂已价，X0-7 阈值冻结维持）；(c) maxItems 截断态 = S2-A-1 已拒
  面；(d) policyVersion 为透传字符串无分支；(e) **本轮新增：数值
  值类本身成为受控配置坐标**（4 数字段 × 6 值类 × 4 消费类 + seq
  4 量级格 = 16a–16e 格网）——无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足——axis 16 只经公开 hashAssessment /
  parseTrackingAssessment / validateEvent / computePrescore /
  combineScore / applyTrackingGate / nextTrackingSeq /
  applyChildThreeLine 剂量，F7a–F7d 是对数值消费面/生产值类/契约角/
  格网可达性的**归档证人**，未改任何阈值、未改任何测试；F7c 的 -0
  商掉与非有限 fail-closed 均为**契约既有行为的归档**（非新裁决、
  非修复）。不声称 Outcome-supported；Checkpoint F-PROD 仍开放
  （ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本（按令不建
  repo scripts），仿真全文见本报告附录。不开 PR。

## 1. 预算支配论证复核 + 一条新轴（方法第 1、2 步）

R3-A §3 … R26-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..9e90035 -- <切片>` 为空，逐字节
   一致（二十七遍全程零 diff；成稿后快进 tip `c4f4462` 复验同为空，
   测量 tip 与推送 tip 两点均验空）——R1-A…R26-A 全部测量与排除按令
   转移，但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨十八段）**：`git diff 89f0455..c4f4462 --
   src/` **完全为空**（R10 开轮以来全部提交均在 docs/ 下，含快进
   增量 `9e90035..c4f4462` 的 R26-J 收口一笔）。生产调用方
   三点位（supervisor.ts:483 / coordinator.ts:444 /
   flowchart-run.ts:320）、settle 调用点 601/711 不传 assessment
   （:482 首行 `if (assessment === undefined) return`）经本轮全库
   grep + 源码级重读复验，无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               15.8      16.7      16.5  µs/gate (+1 event)
PASSED x contract(3)               14.6      14.7      14.7  µs/gate (+1 event)
FAILED x no-contract               15.7      15.4      15.4  µs/gate (+3 events)
FAILED x contract(3)               13.8      13.6      13.7  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.2      14.3      14.2  µs/gate (+3 events)
=> ~68–84 µs/run（稳态热；本轮无孤点上沿，最高格 run2 PASSED×no-contract 16.7）
```

与 R26-A（13.7–16.8 µs/gate ⇒ ~69–84 µs/run）/ R25-A（69–89）/
R24-A（67–84）重叠同带；五格三次运行**无悬崖、无空洞**。稳态下即使把
整个切片优化到零成本，节省上界 ~0.07–0.08 ms/run，仍比战役落地线
（数十~数百 ms 或复杂度类下降）低**约三个量级**。**支配论证（稳态
口径）第二十五次复核成立**；生产冷态口径维持 R17-A 轴 6b 裁决
（[2.7, 8.5] ms/run once-per-run，不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R26-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R26-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1916–1943 ns 每次 ⇒ **5.75–5.83 µs** | 5.91–6.18 µs | ~39–40% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 301–345 ns + 第三次 hash） | **2.78–2.79 µs** | 2.77–2.81 µs | ~19% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.69–1.71 µs** | 1.69–1.72 µs | ~11–12% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 675–686 ns 每次 ⇒ **1.35–1.37 µs** | 1.33–1.35 µs | ~9.2–9.3% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 73–77 + seq 46 + 双幂等 find 38–41×2 + status 48–50 + `[...events]` 27 ns） | **273–277 ns**（⇒ 6.65–6.75 ns/event @E=41） | 282–287 ns | **~1.9%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以当轮 contract 格（14.6–14.7 µs）计。`applyTrackingGate` 整体
隔离实测 5.75–5.96 µs，与 R26-A（5.81–6.04）/ R25-A（5.92–6.21）
同带。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次运行区间）：guard-3 skip（FAILED 零 evidence refs）1942–2020 ns
= 13.2–13.9% of apply（R26-A 12.9–13.3% 邻带）；replay find#1
（GT idempotencyKey 命中，E=44）3174–3210 ns = 21.8% of apply
（R26-A 21.0–22.0% 同带）——组成天花板 {apply, skip, replay} 两个半边
继续成立，不重测其余格。

**本轮新轴 16：标量数值值类面 / scalar numeric value-class face**
（确定性剂量经公开 hashAssessment / parseTrackingAssessment /
validateEvent / computePrescore / combineScore / applyTrackingGate /
nextTrackingSeq / applyChildThreeLine 驱动，固定 E=41、固定全部字符串
内容与字段计数与进程环境（与 R25-A 内容类、R24-A 计数维、R26-A 环境维
三重正交），只动数值字段装的 double；每格测量前全类预热扫平 PIC/JIT
偏置（方法学注记：预热前首测类会吃 recompile 罚，已消）；三次运行
区间）：

```text
证人（F7a–F7d 钉死，三次运行证人行逐字节一致，sha256 同值）:
  F7a: 数值消费普查——toFixed 恰 5 站点 [combined-score.ts:28,
    prescore.ts:69, prescore.ts:79, prescore.ts:80, prescore.ts:83]；
    Number.isInteger parse 腿 [types.ts:337, :340, :381]；
    Number.isFinite 腿 [config.ts:24, human-score.ts:94, types.ts:16,
    :302]；Number() 文本转换恰在 human-score 十分制臂（生产零流量）；
    number-bearing JSON.stringify = hashAssessment（4 unit-score
    数 ×3 腿/gate，生产热）+ hashSummary（score + 每 op exitCode，
    零流量）+ analysis content（零流量）；数字模板插值 =
    gate-apply.ts:35 幂等键 `${hash}:${seq}`；全切片零 bigint /
    parseInt / parseFloat / toPrecision / toExponential / fround /
    toLocaleString
  F7b: 生产数类普查——铸造路径（PASSED/FAILED × claim-gap 臂）放上
    哈希面的每一个数都是 ≤4 小数位短小数（Number(x.toFixed(4)) 格网；
    实测值 {0, 0.3, 0.4, 0.5, 0.8, 1}），即最便宜的最短往返格式化类
    （SMI 与短 double）；seq 恒小 SMI；exitCode 字母表 {0,1}；生产
    路径**不可能铸出**次正规/17 位/-0/非有限值
  F7c: 契约角证人——(i) -0 哈希不可见（stringify "0"）：score=-0
    孪生与 +0 同哈希、parse 接受、e2e gate 照常 apply + 写侧复验 ok、
    JSON 往返静默正规化 -0→+0 不移哈希——冻结契约把零的符号商掉
    （移植安全，与 R26-A collation 面相反）；(ii) 非有限 score 哈希
    可表示（NaN 与 Infinity 均序列化 "null" 且塌缩同一哈希）但写侧
    fail-closed 拒绝（parse 门 "must be a finite number in [0, 1]"）
    ——永不 fail-open，无非有限字节可达账本；(iii) 哈希面对 double
    值精确：0.3 vs 0.1+0.2 两哈希，17 位/次正规/ε 值 parse 合法且
    JSON 往返逐位保真（最短往返）——哈希面全程零量化
  F7d: 生产格网可达性——700 种子化 prescore 输入格网试验 ×
    P/quality/coverage/displayPrescore 四发射面 = 2800/2800 全部
    在 [0,1]、≤4 小数位、无 -0；不同值恰 40 个：
    Number(x.toFixed(4)) 把生产值字母表闭合在便宜格式化类上；
    贵类（17 位、次正规）只经公开 API（构造 assessment）可达，
    永不被铸造

16a  hashAssessment 数类谱（4 unit-score 数/哈希 × 6 值类；同步
     stringify-only 显微镜隔离格式化增量；载荷字符数为机制证人）:
  int01（生产 PASSED 格）  1932–1934 ns/hash ×1.00  428 字符  格式化增量 0（基线）
  grid-4dp（生产纹理）     2102–2185 ns/hash ×1.09–1.13  440 字符  +116~+126 ns
  dyadic-short             2099–2178 ns/hash ×1.09–1.13  439 字符  +112~+122 ns
  seventeen-digit          2396–2465 ns/hash ×1.24–1.27  497 字符  +290~+311 ns
  subnormal(5e-324)        2170–2256 ns/hash ×1.12–1.17  448 字符  +165~+171 ns
  epsilon-boundary         2414–2496 ns/hash ×1.25–1.29  502 字符  +303~+309 ns
  ⇒ ×3 腿 ×5 gate：生产 int01 ~29.0 µs/run，最坏合法类（ε 边界全
    字段）36.2–37.4 µs/run ⇒ 对抗面上界 +7.2~+8.4 µs/run（预算
    ~10%）；机制 = 最短往返 dtoa 位数（Grisu/Ryū 慢路径）+ 载荷
    +69~+74 字符的 hash32 逐字符腿，两者由显微镜分离（格式化增量
    ≈ 总增量的 40–45%，其余是更长串的哈希扫描）
16b  toFixed(4) 输入类剂量（combineScore P 臂 + 隔离
     Number(x.toFixed(4)) 显微镜）:
  int 1 104–112 ns | grid-4dp 110–119 | repeating 2/3 108–112 |
  seventeen-digit 108–112 | one-minus-eps 110–114 |
  subnormal 5e-324 86–92（最快：短指数路径）
  ⇒ 五个合法类平坦（±7 ns 带内），toFixed 站点无对抗放大面——
    反向加固 S6-A-1（变换无收益、输入类也无收益）
16c  算术类腿（次正规操作数，公开路径）:
  混合臂 0.7·min+0.3·max：H 正常 118.7–121.2 ns | H 17 位
  114.9–116.3 | H 次正规 5e-324 153.2–154.0（×1.29–1.30，硬件
  慢路径实测存在）| H 次正规 1e-310 153.2–153.9
  prescore dip 臂：count 0（生产）726–730 | count 1 725–727 |
  次正规 dip 714–719 ⇒ 平坦
  ⇒ 次正规罚 +34~+36 ns/op × 每 gate 1 次 ⇒ ~+170 ns/run 噪声级；
    且需公开 API 构造次正规 H（生产 H 面恒 unobserved，R21-A）
16d  seq 量级梯（gate-apply 幂等键插值 + nextTrackingSeq）:
  applyTrackingGate：SMI 5（生产）5.94–6.00 µs | int32-max
  5.94–6.00 | beyond-int32 2^31 6.35–6.45 | max-safe 2^53-1
  6.38–6.46（HeapNumber 格 +0.4 µs/gate）
  隔离 `${hash}:${seq}` 插值：SMI 5.5–5.7 ns | HeapNumber 类
  12.4–16.0 ns（+7~+10 ns）
  nextTrackingSeq over E=41：43–57 ns 全量级平坦（Math.max/加法
  无量级罚）
  ⇒ 生产 seq 恒小 SMI（F7b）；beyond-int32 格需 2^31 事件——E 轴
    10 ms 穿越点 ~10⁵（R14-A）早爆 4 个数量级，格不可达
16e  e2e 回声（applyTrackingGate 全 17 位最坏类 vs 生产格）:
  生产格 6.24–6.31 µs vs 全 17 位 7.01–7.21 µs ⇒ +0.77–0.91
  µs/gate ×5 ⇒ +3.9–4.6 µs/run（附着在 2 条 gate 内哈希腿 +
  写侧 parse 上，与 16a 谱一致）
```

五个结论：

1. **值平面自此双半闭合**：R25-A 关字符串内容类维、R26-A 关环境维
   之后，数值字段的值类坐标（哪一个 double）是值平面最后一个未受控
   半边——本轮补上：数值消费普查（F7a 四消费类恰站点清单）、生产数类
   普查（F7b 最便宜格）、契约角（F7c 三证人）、格网闭合（F7d 40 值
   字母表）与五组定价（16a–16e）。
2. **生产恒坐最便宜格式化类**：Number(x.toFixed(4)) 把铸造字母表
   闭合在 ≤4 小数位短小数（F7d 2800/2800），17 位/次正规/ε 类只经
   公开 API 可达——16a 的 ×1.25–1.29 谱是**纯公开 API 对抗面定价**
   （+7.2~+8.4 µs/run 上界，预算 ~10%，距落地线 ~4 个量级），零生产
   牵引。
3. **归档契约角事实（非性能洞、非 fail-open）**：-0 被冻结契约商掉
   （哈希不可见，JSON 往返正规化不移哈希——与 R26-A collation 面
   相反是移植安全的）；非有限值哈希可表示但写侧 fail-closed；哈希面
   对 double 值精确零量化。三者均为既有行为归档，无需修复、无候选。
4. **toFixed/算术/seq 三面平坦**：16b 输入类 ±7 ns 无放大面（S6-A-1
   反向加固）；16c 次正规罚 ~170 ns/run 噪声级且 H 面生产零流量；
   16d nextTrackingSeq 全量级平坦、HeapNumber 格不可达。
5. **不产生候选**：哈希面量化 = 契约功能性变更（§0-1）；闭字母表
   缓存 = X1-1/S8-G-2（§0-2）；toFixed 强度削减 = S6-A-1（§0-3）；
   次正规冲刷 = 契约变更 + 噪声级（§0-4）；seq 收窄/键整数化 =
   幂等契约 + 不可达格（§0-5）。

**重开条件全景更新（在 R26-A §1 表基础上新增一行）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| M（openMinors 条数） | 0 | ≈ 4.6–5.8×10⁴ | ~4+ | R20-A |
| K（驻留链 turn 数） | 0（恒 fresh） | ≈ 3×10³ turn | ~2 | R20-A |
| H_L / H_m / H_N（humanInput 三维） | 0 | ≈ 1.2 MB / 2.2–2.3×10⁴ / 4.65–4.74×10⁴ | ~3–5 | R21-A |
| T（analysis content 操作基数） | 0（面零流量） | ≈ 1.50–1.51×10⁵ | ~5 | R22-A |
| G（gates/run，固定 E 线性算术） | ~5 | ≈ 645–714 | ~2.1 | R16-A 系 |
| G（gates/run，含 G×E 自耦合二次项） | ~5 | ≈ 341–344 | ~1.8 | R23-A |
| N_op / N_cl / W×O / Q×D / CF / Cc/Rt 对角线外 | 1 / 1 / 0 / 0 / 2 / 原点 | 见 R24-A 表 | ~4.3–5.5 / 面须先获流量 | R24-A |
| claim 内容类 / turnId 转义编码类 / includes 分歧纹理 | 最便宜格 | 见 R25-A 表 | ~4–4.7 / — | R25-A |
| 环境坐标（collation locale） | en/C 族（最便宜格） | 非字节量轴：部署态坐标 | — | R26-A |
| hashSummary 比较器 ICU-engagement 类 | 站点零流量 | 面须先获生产流量 | 面须先获流量 | R26-A |
| **数值值类（4 数字段 × 值类）** | **toFixed(4) 格网（最便宜格式化类，F7b/F7d）** | **非计数轴：最坏合法对抗面（ε/17 位全字段）+7.2~+8.4 µs/run = 预算 ~10%，穿越 10 ms 需每 gate 哈希腿 ×~120——值类无此杠杆；摊平路全为已拒家族（§0-1..5）** | **~4** | **本轮** |
| **seq 量级格（HeapNumber 类）** | **恒小 SMI** | **格不可达（需 2^31 事件，E 轴早爆 4 个量级）** | **—** | **本轮** |
| G 组成（apply/skip/replay 全混合） | 全 apply | 混入只降不升 | — | R18-A + R19-A |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–15.2 µs/fault | — | R18-A + R22-A |
| 重投递面（once-per-redelivery） | 0 次/run | ~2.8–3.4 µs/次 | — | R19-A + R23-A F3d 形态 |

**可操作重开优先级不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7
家族 `every×includes` 站点 Set 化）仍是第一优先重开对象。本轮
关闭的是**数值值类维**：R14–R16 关规模/载荷维、R17 关组成/温度维、
R18 关组成/故障维、R19 关结局维、R20 关驻留状态维、R21 关输入参数
维、R22 关公开导出维、R23 关轴间耦合维、R24 关字段粒度维、R25 关
字符串内容类维、R26 关环境维之后，**值平面的数值半边（哪一个
double）自此有价**——且额外产出三条契约角归档事实（F7c：-0 商掉 /
非有限 fail-closed / 值精确零量化）供后续轮次与 parent 决策面引用。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 5 项 + R12-A…R26-A
72 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、功能性
变更或数据补充，按「换名不铸 ID」纪律未列任何 S27-A-\* 候选。空枚举
与 R12-A…R26-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第二十七遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `types.ts` | **axis 16 主宿主**（hashAssessment 站点 stringify 数字臂 = 16a 谱宿主，int01 生产格 ×1.00 恒最便宜、ε 边界 ×1.25–1.29 纯对抗面；isMachineScore/isInteger/isFinite parse 门 = F7a 腿清单 + F7c 契约角证人宿主：-0 商掉/非有限 fail-closed/值精确）；哈希面量化 = §0-1 拒列；闭字母表数字串缓存 = §0-2（X1-1/S8-G-2）拒列；hash×3 地板复现 5.75–5.83 µs；X1-1/S8-G-2 memo 禁维持；R26-A 两处 localeCompare 站点零触碰（不重测）；S2-A-5 / X1-2 / S5-H-3 / S5-A-2 / S1-A-8 / X0-5 维持 | 无新候选 |
| `prescore.ts` | 16b toFixed 输入类剂量四站点宿主（:69/79/80/83——平坦 ±7 ns 无放大面）+ 16c dip 臂次正规剂量宿主（714–730 ns 平坦）+ F7d 格网闭合发射源（700 试验 × 4 面 = 2800/2800 ≤4dp）；toFixed 强度削减 = §0-3（S6-A-1）拒列；S1-A-3 / S1-A-4/X4-7 / S6-A-1 / S8-A-3 维持；C 轴可操作重开第一优先不变 | 无新候选 |
| `combined-score.ts` | 16b 第五 toFixed 站点（:28）+ 16c 混合臂次正规剂量宿主（0.7·min+0.3·max，H 次正规 ×1.29–1.30 = 切片唯一实测硬件慢路径，+34~+36 ns/op 噪声级）；次正规冲刷 = §0-4 拒列；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |
| `gate-apply.ts` | 16d seq 量级梯宿主（幂等键 `${hash}:${seq}` 插值 SMI 5.5–5.7 vs HeapNumber 12.4–16.0 ns；nextTrackingSeq 43–57 ns 全量级平坦；beyond-int32 格 +0.4 µs/gate 不可达）+ 16e e2e 回声宿主（全 17 位 +0.77–0.91 µs/gate）；seq 收窄/键整数化 = §0-5 拒列；O(E) 清单 / S11-A-2 / S8-A-1 / S2-A-4 / R15-A §0-1 / R23-A §0-1..5 维持 | 无新候选 |
| `from-child.ts` | F7b 生产数类普查发射源（PASSED/FAILED × claim-gap 臂全部铸 ≤4dp 短小数 {0, 0.3, 0.4, 0.5, 0.8, 1}，seq 小 SMI，无 -0/非有限/次正规可铸）；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `child-tracking.ts` | 锚点/分解宿主复测同带（五格 13.6–16.7 µs/gate）；guard-3 skip 抽测 1942–2020 ns 复现（R18-A 7a 维持）；数值透传无值类分支 | 无新候选 |
| `turn.ts` | 16c 剂量经公开 combineScore 直驱不经 turn 生产路径改动；R13-A 八子组件 / R12-A §0-11 / R16-A §0-2 / R24-A 13a/13e / R25-A 14a turn 格维持；runTrackingTurn 复现 1.69–1.71 µs | 无新候选 |
| `roller.ts` | hashSummary 站点零流量维持（R20-A；其 score/exitCode 数字臂在 F7a 清单内注记为零流量站点，16a 谱不移植到零流量面）；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `gates.ts` | 数值比较腿（score vs softThreshold/hardFailCap）为标量 compare，值类不改变分支价（16b/16c 平坦带旁证）；X0-7 阈值冻结维持；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（humanInput ≡ `{}`）；其 Number() 文本转换腿在 F7a 清单（十分制臂）；16c 的次正规 H 剂量经公开 combineScore 传参不经本文件生产路径；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 / R21-A 维持 | 无新候选 |
| `config.ts` | 配置态矩阵复核宿主（生产恒 DEFAULT；R22-A 双臂价维持）；isFinite 守卫腿在 F7a 清单（:24）；X0-7 阈值冻结维持 | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方）；其 stringify 数字臂（score/P/H/exitCode）在 F7a 清单注记为零流量站点；S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R22-A 隔离三函数价维持；index.ts 纯再导出零逻辑；惰性 import = R12-A §0-10 维持；零数值消费站点（F7a） | 无新候选 |

**对 R26-A 声明的裁决**：「显式输入坐标系之外的隐式坐标（进程环境）
自此有价」在其维度上成立；但 R25-A + R26-A 合起来仍只覆盖了值平面的
字符串半边 + 环境坐标——**数值字段装哪一个 double** 既非内容类（操作数
是字符串）也非环境（坐标是进程），是第三个正交维，且其四个消费类
（格式化器/舍入器/算术单元/整数插值）没有一个在既有三消费者清单里。
本轮补上并闭合（F7a–F7d 四组证人 + 16a–16e 五组定价）。本轮全部新
数据**只产出归档证人、数值值类格网闭合与三条契约角事实，未产出可
落地候选**。收口的预言力第二十七遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R26-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致；退出码 0）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F7a–F7d: 证人行逐字节一致（三 run sha256 同值）；F7b 生产字母表
    {0, 0.3, 0.4, 0.5, 0.8, 1}；F7c -0 同哈希 + NaN/Inf 塌缩同哈希
    且写侧 fail-closed + 0.3≠0.1+0.2；F7d 2800/2800 ≤4dp、40 值
anchor: 15.8 / 14.6 / 15.7 / 13.8 / 14.2 µs/gate => ~69–79 µs/run
decomposition: hash×3=5749ns | validateEvent=2780ns | turn=1685ns
    | prescore×2=1373ns | 5 scans+copy=277ns (6.75 ns/event)
    | applyTrackingGate=5.96µs
spot: skip guard-3 2020ns (13.9%) | replay find#1 3174ns (21.8%)
16a: int01 1932 ×1.00 | grid-4dp 2178 ×1.13 | dyadic 2178 ×1.13 |
     17-digit 2455 ×1.27 | subnormal 2256 ×1.17 | ε 2483 ×1.29 ns/hash
16b: 109.0 / 114.2 / 111.4 / 110.4 / 112.8 / 89.3 ns（平坦，次正规最快）
16c: mix H 正常 121.2 | 17 位 116.0 | 次正规 153.6/153.4 ns；dip 平坦
16d: apply SMI 5.99 | int32 5.94 | 2^31 6.35 | 2^53 6.38 µs；
     interp 5.5 vs 13.5/13.3/12.7 ns；nextSeq 54/47/46/43 ns
16e: 生产 6.30 vs 全 17 位 7.21 µs => +0.91 µs/gate（+4.6 µs/run）
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F7a–F7d: 证人行逐字节同 run 1
anchor: 16.7 / 14.7 / 15.4 / 13.6 / 14.3 µs/gate => ~68–84 µs/run
decomposition: hash×3=5808ns | validateEvent=2785ns | turn=1699ns
    | prescore×2=1369ns | 5 scans+copy=276ns (6.72 ns/event)
    | applyTrackingGate=5.75µs
spot: skip guard-3 1942ns (13.2%) | replay find#1 3210ns (21.8%)
16a: int01 1932 ×1.00 | grid-4dp 2102 ×1.09 | dyadic 2099 ×1.09 |
     17-digit 2396 ×1.24 | subnormal 2170 ×1.12 | ε 2414 ×1.25 ns/hash
16b: 107.3 / 118.0 / 111.4 / 111.2 / 110.8 / 88.3 ns（平坦，次正规最快）
16c: mix H 正常 118.9 | 17 位 116.3 | 次正规 154.0/153.9 ns；dip 平坦
16d: apply SMI 5.98 | int32 5.97 | 2^31 6.39 | 2^53 6.38 µs；
     interp 5.5 vs 14.4/12.7/12.4 ns；nextSeq 56/48/46/41 ns
16e: 生产 6.24 vs 全 17 位 7.01 µs => +0.77 µs/gate（+3.9 µs/run）
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F7a–F7d: 证人行逐字节同 run 1
anchor: 16.5 / 14.7 / 15.4 / 13.7 / 14.2 µs/gate => ~69–82 µs/run
decomposition: hash×3=5829ns | validateEvent=2780ns | turn=1709ns
    | prescore×2=1349ns | 5 scans+copy=273ns (6.65 ns/event)
    | applyTrackingGate=5.76µs
spot: skip guard-3 1992ns (13.5%) | replay find#1 3208ns (21.8%)
16a: int01 1934 ×1.00 | grid-4dp 2185 ×1.13 | dyadic 2178 ×1.13 |
     17-digit 2465 ×1.27 | subnormal 2248 ×1.16 | ε 2496 ×1.29 ns/hash
16b: 111.7 / 118.9 / 112.3 / 112.3 / 113.8 / 91.9 ns（平坦，次正规最快）
16c: mix H 正常 118.7 | 17 位 114.9 | 次正规 153.2/153.2 ns；dip 平坦
16d: apply SMI 6.00 | int32 6.00 | 2^31 6.45 | 2^53 6.46 µs；
     interp 5.7 vs 16.0/15.8/16.0 ns；nextSeq 57/51/52/43 ns
16e: 生产 6.31 vs 全 17 位 7.21 µs => +0.90 µs/gate（+4.5 µs/run）
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立仿真运行 F7a–F7d 证人行逐字节一致（数值消费普查/生产字母表/
契约角哈希行为/格网闭合确定性重现——头 4 行 sha256 三 run 同值）；
锚点五格三次同带无悬崖、无孤点；16a 数类谱三次同序（int01 恒最便宜、
ε 恒最贵 ×1.25–1.29）；16b 三次平坦；16c 次正规罚三次同带
（153.2–154.0 ns）；16d 量级梯三次同形（nextSeq 平坦、HeapNumber
apply 格 +0.4 µs）；16e 回声三次同向（+0.77–0.91 µs/gate）。空枚举
裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S27-A-\*）。换名检查 5 项新记录见 §0（1–5），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md；本轴轴名
（标量数值值类面 / scalar numeric value-class face）按令**不加
EXCLUSIONS 行**，由 parent 决定 plan-ban。

重开条件本轮**新增一条收口**：

**标量数值值类收口**：值平面的数值半边（数值字段装哪一个 double）
自此有价——数值消费普查恰四类（F7a：stringify 数字臂/toFixed 5 站点/
算术腿/整数插值，零 bigint/parseInt/toLocale\*）、生产数类普查
（F7b：铸造恒 ≤4dp 短小数 + 小 SMI seq，贵类不可铸）、契约角三证人
（F7c：-0 哈希不可见且往返正规化移植安全 / 非有限哈希可表示但写侧
fail-closed / 哈希面值精确零量化）、格网闭合（F7d：toFixed(4) 字母表
恰 40 值 2800/2800）、五组定价（16a 数类谱 ε ×1.25–1.29 对抗上界
+7.2~+8.4 µs/run / 16b toFixed 输入类平坦 / 16c 次正规算术罚 ~170
ns/run 噪声级 / 16d seq 量级平坦 + 不可达 HeapNumber 格 / 16e e2e
回声 +3.9–4.6 µs/run）。重开条件：(a) 哈希面契约变更（若 parent
决策面未来按 proposal-first 变更序列化格式——如定点编码——则 16a 谱
须重价，且 -0/非有限角须重裁）；(b) 生产铸造路径脱离 toFixed(4)
格网（新增不经 Number(x.toFixed(4)) 的数值字段直上哈希面——届时
17 位类从对抗面变生产面，16a 的 ×1.24–1.27 实体化 ⇒ +~6–8 µs/run，
量级仍差落地线 ~3 个数量级，预期仍无候选）；(c) H 面获得生产流量
（R21-A 重开条件）且人类输入允许任意 double——届时 16c 次正规罚与
16b 一并重审；(d) seq 语义变更允许大整数（如全局单调 ID）——届时
16d 的 HeapNumber 格实体化但仍 +0.4 µs/gate 噪声级。**可操作重开
第一优先不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7 家族
`every×includes` 站点 Set 化）。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf27a01`–`0xf27a03`。脚本 sha256：
`ed4d97b42ef9f5eb1ed45ca15273499bab16653b5f0a9151367d8771edea9964`
（1015 行）。三次官方运行原始输出已核对入 §5；临时文件
（/tmp/r27a-sim.mts、/tmp/r27a-run{1,2,3}.txt）报告推送后删除。

```ts
/**
 * R27-A deterministic anchor + decomposition + NEW-axis simulation
 * (twenty-seventh pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R26-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R26-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation, 5-scan
 *       O(E) inventory), plus compact spot re-confirmations of the R18-A
 *       skip pricing (guard-3) and the R19-A replay pricing (find#1);
 *   (c) NEW axis 16: the SCALAR NUMERIC VALUE-CLASS face. Twenty-six
 *       passes priced the slice's inputs by scale (R14/R15), payload
 *       length (R16), composition (R17/R18), outcome (R19), residency
 *       (R20), human face (R21), export face (R22), axis coupling (R23),
 *       field count (R24), STRING content class (R25) and the ambient
 *       collation locale (R26) -- always with every number-typed input
 *       held fixed at its production value. The numeric half of the value
 *       plane (WHICH double a number field carries, at fixed field count
 *       and fixed strings) was never the controlled variable, anywhere in
 *       the campaign. It matters here because the slice has four distinct
 *       number-consuming operation classes whose per-value price is
 *       value-class dependent:
 *         - number->string shortest-roundtrip formatting inside
 *           JSON.stringify at the hashAssessment site (types.ts:485-501,
 *           exactly 4 unit-score numbers per hash x3 legs/gate -- the
 *           slice's #1 component), plus the zero-flow hashSummary and
 *           analysis-content stringify sites;
 *         - Number(x.toFixed(4)) rounding at 5 sites (prescore.ts:69/79/
 *           82/83, combined-score.ts:28);
 *         - IEEE-754 arithmetic legs (quality*coverage, dip multiply,
 *           0.7*min+0.3*max mix) where subnormal operands can leave the
 *           hardware fast path; and
 *         - integer legs: seq template-literal interpolation in the
 *           idempotency key (gate-apply.ts:35), payload.seq === compares,
 *           nextTrackingSeq Math.max(seq+1), and the parse-side
 *           Number.isInteger / Number.isFinite validation gates.
 *       The dose classes: SMI integers {0,1}, production 4dp-grid short
 *       decimals, dyadic shorts, full 17-significant-digit doubles,
 *       subnormals (5e-324 is parse-legal: finite and in [0,1]), and for
 *       seq the SMI/int32/2^53 magnitude ladder.
 *
 * Archival-fact witnesses (deterministic, seeded):
 *   F7a: numeric-consumer census. Static scan of the 14 slice files for
 *        every number-consuming operation class (toFixed / number-bearing
 *        JSON.stringify / template interpolation of numbers / isInteger /
 *        isFinite / Number() conversion / arithmetic on input numbers),
 *        and absence of bigint / parseInt / parseFloat / toPrecision /
 *        toExponential / Math.fround / bit-twiddling on VALUE-carrying
 *        numbers (hash32's charCode loop is out-of-slice, domain/hash.ts).
 *   F7b: production number-class census. Drive the production mint
 *        (PASSED/FAILED x contract arms) and check every number that
 *        reaches the hash face serializes to <= 6 chars with <= 4
 *        fraction digits (the toFixed(4) grid), i.e. production sits in
 *        the cheapest formatting classes; seq is a small SMI; exitCode
 *        is 0/1; no subnormal, no 17-digit, no -0, no non-finite value
 *        is mintable on the production path.
 *   F7c: contract-corner witnesses on the frozen hash face:
 *        (i)  -0 is hash-INVISIBLE (JSON.stringify(-0) === "0"): the
 *             score=-0 twin hashes identically to score=+0, parse accepts
 *             -0 (isMachineScore(-0) is true), the e2e gate applies and
 *             write-side revalidates OK, and a JSON round trip silently
 *             canonicalizes -0 -> +0 WITHOUT changing the hash -- the
 *             frozen contract quotients out the sign of zero (portability
 *             -safe, unlike the R26-A collation face);
 *        (ii) non-finite scores are hash-REPRESENTABLE (stringify emits
 *             "null", hashAssessment returns a hash) but write-REJECTED:
 *             applyTrackingGate passes its CAS equality (same object,
 *             same hash) and then validateEvent rejects fail-closed at
 *             the parse gate ("must be a finite number in [0, 1]") --
 *             never fail-open, no non-finite byte reaches the ledger;
 *        (iii) value-exactness: 0.3 vs 0.30000000000000004 are distinct
 *             parse-legal scores with distinct hashes; 17-digit and
 *             subnormal values survive JSON round trips bitwise
 *             (shortest-roundtrip) -- the hash contract is value-exact
 *             over doubles, no quantization anywhere at the hash face.
 *   F7d: production grid reachability. Seeded fuzz over the full
 *        prescore-input outcome lattice: every emitted P / quality /
 *        coverage / displayPrescore is in [0,1] and serializes with
 *        <= 4 fraction digits (Number(x.toFixed(4)) closure witness);
 *        count distinct emitted values (the production value alphabet).
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf27a01..0xf27a03.
 * Run: npx tsx /tmp/r27a-sim.mts   (repo root, deps installed, Node >=22.19)
 */
import { readFileSync } from "node:fs";
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore, type PrescoreInput } from "/workspace/src/tracking/prescore.js";
import { combineScore } from "/workspace/src/tracking/combined-score.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    summary?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary =
    opts?.summary ?? (opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran");
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

/* ============================================================
 * F7a: numeric-consumer census (static, over the 14 slice files).
 * ============================================================ */
{
  const sliceFiles = [
    "analysis.ts",
    "combined-score.ts",
    "config.ts",
    "from-child.ts",
    "gates.ts",
    "human-score.ts",
    "index.ts",
    "isolation.ts",
    "prescore.ts",
    "roller.ts",
    "turn.ts",
    "types.ts"
  ].map((f) => `/workspace/src/tracking/${f}`);
  sliceFiles.push("/workspace/src/run/child-tracking.ts", "/workspace/src/run/gate-apply.ts");
  check("F7a slice manifest is exactly 14 files", sliceFiles.length === 14);
  const absentTokens: Array<{ label: string; re: RegExp }> = [
    { label: "bigint", re: /\bBigInt\b|\bbigint\b/ },
    { label: "parseInt/parseFloat", re: /parseInt|parseFloat/ },
    { label: "toPrecision/toExponential", re: /toPrecision|toExponential/ },
    { label: "Math.fround/clz32", re: /Math\.fround|Math\.clz32/ },
    { label: "toLocaleString", re: /toLocaleString/ }
  ];
  let absentHits = 0;
  const toFixedSites: string[] = [];
  const isIntegerSites: string[] = [];
  const isFiniteSites: string[] = [];
  const numberConvSites: string[] = [];
  for (const file of sliceFiles) {
    const text = readFileSync(file, "utf8");
    for (const token of absentTokens) {
      if (token.re.test(text)) {
        absentHits += 1;
        console.error(`F7a forbidden numeric token ${token.label} found in ${file}`);
      }
    }
    const short = file.split("/").pop();
    const lines = text.split("\n");
    lines.forEach((line, i) => {
      if (line.includes(".toFixed(")) toFixedSites.push(`${short}:${i + 1}`);
      if (line.includes("Number.isInteger")) isIntegerSites.push(`${short}:${i + 1}`);
      if (line.includes("Number.isFinite")) isFiniteSites.push(`${short}:${i + 1}`);
      if (/\bNumber\(/.test(line)) numberConvSites.push(`${short}:${i + 1}`);
    });
  }
  check("F7a zero forbidden numeric tokens", absentHits === 0);
  check(
    "F7a exactly 5 toFixed sites (prescore x4 + combined-score x1)",
    toFixedSites.length === 5 &&
      toFixedSites.filter((s) => s.startsWith("prescore.ts:")).length === 4 &&
      toFixedSites.filter((s) => s.startsWith("combined-score.ts:")).length === 1,
    toFixedSites.join(",")
  );
  check(
    "F7a Number.isInteger legs live only in types.ts (parse side)",
    isIntegerSites.length === 3 && isIntegerSites.every((s) => s.startsWith("types.ts:")),
    isIntegerSites.join(",")
  );
  check(
    "F7a Number.isFinite legs in types.ts x2 + config.ts x1",
    isFiniteSites.filter((s) => s.startsWith("types.ts:")).length === 2 &&
      isFiniteSites.filter((s) => s.startsWith("config.ts:")).length === 1 &&
      isFiniteSites.filter((s) => s.startsWith("human-score.ts:")).length === 1,
    isFiniteSites.join(",")
  );
  console.log(
    `F7a: numeric-consumer census over the 14 slice files -- toFixed sites [${toFixedSites.join(", ")}]; ` +
      `Number.isInteger parse legs [${isIntegerSites.join(", ")}]; Number.isFinite legs [${isFiniteSites.join(", ")}]; ` +
      `Number() text conversion sites [${numberConvSites.join(", ")}] (human-score ten-point arm, zero production flow); ` +
      `number-bearing JSON.stringify sites: types.ts hashAssessment (4 unit scores x3 legs/gate, production-hot) + ` +
      `types.ts hashSummary (score + per-op exitCode, zero flow) + analysis.ts content (score/P/H/exitCode, zero flow); ` +
      `number template interpolation: gate-apply.ts:35 idempotencyKey \`\${hash}:\${seq}\`; ` +
      `zero bigint / parseInt / parseFloat / toPrecision / toExponential / fround / toLocaleString anywhere.`
  );
}

/* ============================================================
 * F7b: production number-class census (mint-path numbers).
 * ============================================================ */
const shortDecimal = (x: number): boolean => {
  const s = String(x);
  if (!/^-?\d+(\.\d+)?$/.test(s)) return false;
  const frac = s.split(".")[1] ?? "";
  return frac.length <= 4;
};
{
  const numbersSeen = new Map<string, number>();
  for (const kind of ["PASSED", "FAILED"] as const) {
    for (const withGap of [false, true]) {
      const mk = mkChild(kind, { claimSuccessWithCheckGap: withGap });
      const obs = observationFromChild(mk.child, mk.spec, mk.contract);
      const dec = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: String(RUN_ID) });
      if (!dec.apply) continue;
      const a = dec.assessment;
      numbersSeen.set(`prescore:${kind}:${withGap}`, a.prescore);
      numbersSeen.set(`quality:${kind}:${withGap}`, a.quality);
      numbersSeen.set(`coverage:${kind}:${withGap}`, a.coverage);
      numbersSeen.set(`score:${kind}:${withGap}`, a.score);
      const ex = dec.prescore.dimensions.find((d) => d.value !== undefined)?.value;
      if (ex !== undefined) numbersSeen.set(`dimvalue:${kind}`, ex);
    }
  }
  let allShort = true;
  let allUnit = true;
  for (const [, v] of numbersSeen) {
    if (!shortDecimal(v)) allShort = false;
    if (!(v >= 0 && v <= 1) || Object.is(v, -0)) allUnit = false;
  }
  check("F7b every mint-path hash-face number is a <=4-fraction-digit short decimal", allShort);
  check("F7b every mint-path number is in [0,1], none is -0", allUnit);
  const seq = nextTrackingSeq(events41);
  check("F7b production seq is a small SMI", Number.isInteger(seq) && seq >= 0 && seq < 2 ** 30, String(seq));
  const mkP = mkChild("PASSED");
  const pi = prescoreInputFromObservation(observationFromChild(mkP.child, mkP.spec, mkP.contract));
  check(
    "F7b production exitCode alphabet is {0,1}, stalledTurns 0",
    pi.toolSituations.every((t) => t.exitCode === 0 || t.exitCode === 1) && pi.stalledTurns === 0
  );
  console.log(
    `F7b: production number-class census -- every number the mint path puts on the hash face ` +
      `(prescore/quality/coverage/score across PASSED/FAILED x claim-gap arms) is a short decimal with ` +
      `<= 4 fraction digits (the Number(x.toFixed(4)) grid; values seen: ` +
      `${[...new Set([...numbersSeen.values()])].sort((l, r) => l - r).join(", ")}), i.e. the CHEAPEST ` +
      `shortest-roundtrip formatting classes (SMI integers and short doubles); seq is a small SMI; ` +
      `exitCode alphabet {0,1}; no subnormal / 17-digit / -0 / non-finite value is mintable on the production path.`
  );
}

/* ============================================================
 * F7c: contract-corner witnesses on the frozen hash face.
 * ============================================================ */
{
  const mk = mkChild("PASSED");
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const dec = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: String(RUN_ID) });
  if (!dec.apply) throw new Error("F7c fixture must apply");
  const base = dec.assessment;

  // (i) -0 is hash-invisible and write-legal
  const zeroTwin: TrackingAssessment = { ...base, score: 0 };
  const negZeroTwin: TrackingAssessment = { ...base, score: -0 };
  check("F7c(i) -0 twin hashes identically to +0 twin", hashAssessment(negZeroTwin) === hashAssessment(zeroTwin));
  check("F7c(i) parse accepts -0 (isMachineScore(-0) true)", Object.is(parseTrackingAssessment(JSON.parse(JSON.stringify({ ...negZeroTwin }))).score, 0));
  const parsedNegZero = parseTrackingAssessment({ ...negZeroTwin });
  check("F7c(i) in-memory parse PRESERVES -0 (no normalization inside the slice)", Object.is(parsedNegZero.score, -0));
  check("F7c(i) JSON round trip canonicalizes -0 -> +0 without changing the hash", Object.is(JSON.parse(JSON.stringify(-0)), 0) && hashAssessment(parsedNegZero) === hashAssessment(zeroTwin));
  {
    let c = 900;
    const out = applyTrackingGate({
      events: events41,
      assessment: negZeroTwin,
      assessmentHash: hashAssessment(negZeroTwin),
      expectedSeq: nextTrackingSeq(events41),
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
    check("F7c(i) e2e gate applies the -0 twin and write-side revalidates OK", out.result.applied === true);
  }

  // (ii) non-finite: hash-representable, write-rejected fail-closed
  const nanTwin: TrackingAssessment = { ...base, prescore: Number.NaN };
  const infTwin: TrackingAssessment = { ...base, score: Number.POSITIVE_INFINITY };
  const nanHash = hashAssessment(nanTwin);
  check("F7c(ii) hashAssessment REPRESENTS NaN (stringify emits null, hash returned)", typeof nanHash === "string" && nanHash.length > 0);
  check(
    "F7c(ii) NaN and Infinity twins collapse to the same hash (both serialize as null)",
    nanHash === hashAssessment({ ...base, prescore: Number.POSITIVE_INFINITY }) &&
      hashAssessment(infTwin) === hashAssessment({ ...base, score: Number.NaN })
  );
  for (const [label, twin] of [
    ["NaN prescore", nanTwin],
    ["Infinity score", infTwin]
  ] as const) {
    let threw = "";
    try {
      let c = 900;
      applyTrackingGate({
        events: events41,
        assessment: twin,
        assessmentHash: hashAssessment(twin),
        expectedSeq: nextTrackingSeq(events41),
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    } catch (error) {
      threw = error instanceof Error ? error.message : String(error);
    }
    check(
      `F7c(ii) ${label} passes CAS but is REJECTED fail-closed at the write gate`,
      threw.includes("must be a finite number in [0, 1]"),
      threw
    );
  }

  // (iii) value-exactness of the hash face
  const a03: TrackingAssessment = { ...base, score: 0.3 };
  const a03eps: TrackingAssessment = { ...base, score: 0.1 + 0.2 };
  check("F7c(iii) 0.3 vs 0.30000000000000004 are distinct parse-legal scores with distinct hashes", hashAssessment(a03) !== hashAssessment(a03eps));
  const exactClasses = [0.7368421052631579, 5e-324, 1 - Number.EPSILON / 2, 0.1 + 0.2, Number.EPSILON];
  let roundTripExact = 0;
  for (const v of exactClasses) {
    const rt = JSON.parse(JSON.stringify({ v })).v as number;
    if (Object.is(rt, v)) roundTripExact += 1;
    void parseTrackingAssessment({ ...base, score: v }); // parse-legal (finite, in [0,1])
  }
  check("F7c(iii) 17-digit / subnormal / epsilon values survive JSON round trips bitwise", roundTripExact === exactClasses.length, `${roundTripExact}/${exactClasses.length}`);
  console.log(
    `F7c: contract-corner witnesses -- (i) -0 is hash-INVISIBLE (stringify "0"): score=-0 twin hashes ` +
      `identically to +0, parse accepts it, the e2e gate applies + write-side revalidates OK, and a JSON ` +
      `round trip canonicalizes -0 -> +0 without moving the hash (the frozen contract quotients the sign ` +
      `of zero -- portability-safe, unlike the R26-A collation face); (ii) non-finite scores are ` +
      `hash-representable (NaN and Infinity both serialize as null and COLLAPSE to one hash) but ` +
      `write-rejected fail-closed at validateEvent's parse gate ("must be a finite number in [0, 1]") -- ` +
      `never fail-open, no non-finite byte reaches the ledger; (iii) the hash face is value-EXACT over ` +
      `doubles: 0.3 vs 0.1+0.2 hash apart, 17-digit/subnormal/epsilon values are parse-legal and survive ` +
      `JSON round trips bitwise (shortest-roundtrip) -- no quantization anywhere at the hash face.`
  );
}

/* ============================================================
 * F7d: production grid reachability (seeded fuzz over the
 * prescore-input outcome lattice; toFixed(4) closure witness).
 * ============================================================ */
{
  const rng = mulberry32(0xf27a01);
  const distinct = new Set<number>();
  let allShort = 0;
  const TRIALS = 700;
  for (let t = 0; t < TRIALS; t += 1) {
    const nTools = Math.floor(rng() * 3);
    const tools = Array.from({ length: nTools }, (_, i) => ({
      name: `tool_${i}`,
      exitCode: rng() < 0.5 ? 0 : 1,
      wrote: rng() < 0.5,
      escaped: rng() < 0.15,
      artifactIds: [],
      evidenceIds: [],
      hashes: []
    }));
    const nChecks = Math.floor(rng() * 3);
    const required = Array.from({ length: nChecks }, (_, i) => `chk_${i}`);
    const completed = required.filter(() => rng() < 0.7);
    const nCons = Math.floor(rng() * 3);
    const constraints = Array.from({ length: nCons }, (_, i) => ({
      id: `con_${i}`,
      text: `constraint ${i}`,
      kind: "constraint" as const,
      mandatory: true as const
    }));
    const retained = constraints.map((c) => c.id).filter(() => rng() < 0.8);
    const input: PrescoreInput = {
      claims: rng() < 0.5 ? ["all checks passed"] : rng() < 0.5 ? ["tests ran"] : [],
      toolSituations: tools,
      writePaths: rng() < 0.2 ? ["/a/b"] : [],
      ownedPaths: rng() < 0.5 ? ["/a/b"] : [],
      requiredChecks: required,
      completedChecks: completed,
      constraints,
      retainedConstraintIds: retained,
      progressed: rng() < 0.6 ? true : rng() < 0.5 ? false : "UNOBSERVED" as const,
      stalledTurns: Math.floor(rng() * 3),
      independentEvidence: rng() < 0.5,
      ...(rng() < 0.3 ? { lightMinorCount: Math.floor(rng() * 4) } : {})
    };
    const out = computePrescore(input);
    for (const v of [out.P, out.quality, out.coverage, out.displayPrescore]) {
      distinct.add(v);
      if (shortDecimal(v) && v >= 0 && v <= 1 && !Object.is(v, -0)) allShort += 1;
    }
  }
  check("F7d toFixed(4) closure: every emitted score is a short decimal in [0,1]", allShort === TRIALS * 4, `${allShort}/${TRIALS * 4}`);
  console.log(
    `F7d: production grid reachability -- ${TRIALS} seeded prescore-input lattice trials emit ` +
      `${distinct.size} distinct values across P/quality/coverage/displayPrescore, all in [0,1], all ` +
      `<= 4 fraction digits, none -0 (${allShort}/${TRIALS * 4}): Number(x.toFixed(4)) closes the ` +
      `production value alphabet over the cheap formatting classes; expensive classes (17-digit, ` +
      `subnormal) are reachable ONLY through the public API (crafted assessments), never minted.`
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R26-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    anchorUs.push(one * 1e3);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition (R12-A..R26-A methodology) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)}) => ~${((scans * 1e6) / 41).toFixed(2)} ns/event at E=41`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const mkGT = mkChild("FAILED", { taskId: "tsk_replay_gt" });
    const obsGT = observationFromChild(mkGT.child, mkGT.spec, mkGT.contract);
    const decGT = assessChildObservation({
      observation: obsGT,
      episodeId: "ep_bench",
      runId: String(RUN_ID)
    });
    if (!decGT.apply) throw new Error("replay fixture must apply");
    const hashGT = hashAssessment(decGT.assessment);
    const seqGT = nextTrackingSeq(events41);
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: decGT.assessment,
      assessmentHash: hashGT,
      expectedSeq: seqGT,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: decGT.assessment,
        assessmentHash: hashGT,
        expectedSeq: seqGT,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }

  /* ============================================================
   * NEW axis 16a: number->string formatting class spectrum at the
   * hashAssessment site (4 unit scores per hash, x3 legs/gate).
   * All variants are parse-legal crafted assessments (public API).
   * ============================================================ */
  {
    const classes: Array<{ label: string; values: [number, number, number, number] }> = [
      { label: "int01 (production PASSED)", values: [1, 1, 1, 1] },
      { label: "grid-4dp (production texture)", values: [0.6667, 0.75, 0.6, 0.8] },
      { label: "dyadic-short", values: [0.5, 0.25, 0.125, 0.5] },
      {
        label: "seventeen-digit",
        values: [0.7368421052631579, 0.30000000000000004, 0.3333333333333333, 0.9999999999999999]
      },
      { label: "subnormal", values: [5e-324, 1e-310, 5e-324, 1e-310] },
      { label: "epsilon-boundary", values: [Number.EPSILON, 1 - Number.EPSILON / 2, Number.EPSILON, 1 - Number.EPSILON / 2] }
    ];
    console.log("--- NEW axis 16a: stringify number-class spectrum at hashAssessment (4 scores/hash) ---");
    const payloadOf = (variant: TrackingAssessment): Record<string, unknown> => ({
      coverage: variant.coverage,
      dimensions: [...variant.dimensions].map((d) => ({ id: d.id, verdict: d.verdict })).sort((l, r) => l.id.localeCompare(r.id)),
      episodeId: variant.episodeId,
      gate: { codes: [...variant.gate.codes].sort(), kind: variant.gate.kind },
      prescore: variant.prescore,
      quality: variant.quality,
      runId: variant.runId,
      score: variant.score,
      turnId: variant.turnId
    });
    const variants = classes.map((cls) => ({
      cls,
      variant: {
        ...assessment,
        prescore: cls.values[0],
        quality: cls.values[1],
        coverage: cls.values[2],
        score: cls.values[3]
      } as TrackingAssessment
    }));
    // pre-warm sweep across ALL classes (kills first-class JIT/PIC ordering)
    for (let w = 0; w < 20000; w += 1) {
      for (const { variant } of variants) void hashAssessment(variant);
    }
    let baseNs = 0;
    let baseStrNs = 0;
    for (const { cls, variant } of variants) {
      void parseTrackingAssessment({ ...variant }); // parse-legal witness
      const t = bench(() => void hashAssessment(variant), 200000);
      const frozen = payloadOf(variant);
      const tStr = bench(() => void JSON.stringify(frozen), 200000);
      if (cls.label.startsWith("int01")) {
        baseNs = t * 1e6;
        baseStrNs = tStr * 1e6;
      }
      const payloadLen = JSON.stringify(frozen).length;
      console.log(
        `  ${cls.label}: ${(t * 1e6).toFixed(0)}ns/hash (x${(t * 1e6 / baseNs).toFixed(2)} vs int01; payload ${payloadLen} chars; ` +
          `stringify-only microscope ${(tStr * 1e6).toFixed(0)}ns, formatter delta ${(tStr * 1e6 - baseStrNs).toFixed(0)}ns) ` +
          `=> x3 legs x5 gates = ${(t * 1e6 * 15).toFixed(0)}ns/run`
      );
    }
  }

  /* ============================================================
   * NEW axis 16b: toFixed(4) input-class dose at the rounding sites
   * (combineScore public API + bench-only Number(x.toFixed(4))
   * microscope; production P is post-round already).
   * ============================================================ */
  {
    const classes: Array<{ label: string; v: number }> = [
      { label: "int 1", v: 1 },
      { label: "grid-4dp 0.6667", v: 0.6667 },
      { label: "repeating 2/3", v: 2 / 3 },
      { label: "seventeen-digit 0.7368...", v: 0.7368421052631579 },
      { label: "one-minus-eps", v: 1 - Number.EPSILON / 2 },
      { label: "subnormal 5e-324", v: 5e-324 }
    ];
    console.log("--- NEW axis 16b: toFixed(4) input-class dose (combineScore P-arm + isolated microscope) ---");
    // pre-warm sweep across ALL classes (kills first-class JIT/PIC ordering)
    for (let w = 0; w < 20000; w += 1) {
      for (const cls of classes) {
        void combineScore({ P: cls.v, human: { kind: "unobserved" }, obviousProblem: false });
        void Number(cls.v.toFixed(4));
      }
    }
    for (const cls of classes) {
      const tCombine = bench(
        () => void combineScore({ P: cls.v, human: { kind: "unobserved" }, obviousProblem: false }),
        200000
      );
      const tFixed = bench(() => void Number(cls.v.toFixed(4)), 200000);
      console.log(
        `  ${cls.label}: combineScore(P) ${(tCombine * 1e6).toFixed(1)}ns | isolated Number(x.toFixed(4)) ${(tFixed * 1e6).toFixed(1)}ns`
      );
    }
  }

  /* ============================================================
   * NEW axis 16c: IEEE-754 arithmetic-class legs (subnormal operands
   * through the public mix and dip paths).
   * ============================================================ */
  {
    console.log("--- NEW axis 16c: arithmetic-class legs (subnormal operands, public paths) ---");
    const mixClasses: Array<{ label: string; H: number }> = [
      { label: "H normal 0.5", H: 0.5 },
      { label: "H seventeen-digit", H: 0.7368421052631579 },
      { label: "H subnormal 5e-324", H: 5e-324 },
      { label: "H subnormal 1e-310", H: 1e-310 }
    ];
    for (let w = 0; w < 20000; w += 1) {
      for (const cls of mixClasses) {
        void combineScore({ P: 0.6667, human: { kind: "ten-point", H: cls.H, mark: 5 }, obviousProblem: true });
      }
    }
    for (const cls of mixClasses) {
      const t = bench(
        () =>
          void combineScore({
            P: 0.6667,
            human: { kind: "ten-point", H: cls.H, mark: 5 },
            obviousProblem: true
          }),
        200000
      );
      console.log(`  mix arm 0.7*min+0.3*max [${cls.label}]: ${(t * 1e6).toFixed(1)}ns`);
    }
    const dipClasses: Array<{ label: string; count: number }> = [
      { label: "count 0 (production)", count: 0 },
      { label: "count 1 (normal dip)", count: 1 },
      { label: "count 1e-320 (subnormal dip)", count: 1e-320 }
    ];
    for (const cls of dipClasses) {
      const input: PrescoreInput = { ...prescoreInputP, lightMinorCount: cls.count };
      const t = bench(() => void computePrescore(input), 200000);
      console.log(`  prescore dip arm [${cls.label}]: ${(t * 1e6).toFixed(0)}ns/prescore`);
    }
  }

  /* ============================================================
   * NEW axis 16d: seq integer magnitude class at gate-apply
   * (idempotency-key interpolation + find compares + validateEvent
   * isInteger leg + nextTrackingSeq Math.max arithmetic).
   * ============================================================ */
  {
    console.log("--- NEW axis 16d: seq magnitude class at gate-apply ---");
    const seqClasses: Array<{ label: string; seq: number }> = [
      { label: "SMI 5 (production)", seq: 5 },
      { label: "int32-max 2^31-1", seq: 2 ** 31 - 1 },
      { label: "beyond-int32 2^31", seq: 2 ** 31 },
      { label: "max-safe 2^53-1", seq: 2 ** 53 - 1 }
    ];
    // pre-warm sweep across ALL seq classes
    for (let w = 0; w < 2000; w += 1) {
      for (const cls of seqClasses) {
        let c = 100;
        void applyTrackingGate({
          events: events41,
          assessment,
          assessmentHash: hash,
          expectedSeq: cls.seq,
          policyVersion: "track-v1",
          nowIso: NOW_ISO,
          generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
        });
        void `${hash}:${cls.seq}`;
      }
    }
    for (const cls of seqClasses) {
      let applied = false;
      const t = bench(() => {
        let c = 100;
        applied = applyTrackingGate({
          events: events41,
          assessment,
          assessmentHash: hash,
          expectedSeq: cls.seq,
          policyVersion: "track-v1",
          nowIso: NOW_ISO,
          generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
        }).result.applied;
      }, 20000);
      check(`16d fresh apply at seq class [${cls.label}]`, applied);
      const tKey = bench(() => void `${hash}:${cls.seq}`, 200000);
      console.log(
        `  ${cls.label}: applyTrackingGate ${(t * 1e3).toFixed(2)}us | isolated \`\${hash}:\${seq}\` ${(tKey * 1e6).toFixed(1)}ns`
      );
    }
    // nextTrackingSeq over a table whose TA/GT rows carry large seqs
    for (const cls of seqClasses) {
      const bigTable = [...events41];
      let c2 = 500;
      const applied = applyTrackingGate({
        events: bigTable,
        assessment,
        assessmentHash: hash,
        expectedSeq: cls.seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c2++).padStart(8, "0")}` as EventId
      });
      const t = bench(() => void nextTrackingSeq(applied.events), 200000);
      check(
        `16d nextTrackingSeq exact at [${cls.label}]`,
        nextTrackingSeq(applied.events) === cls.seq + 1
      );
      console.log(`  nextTrackingSeq over table with seq=${cls.label}: ${(t * 1e6).toFixed(0)}ns`);
    }
  }

  /* ============================================================
   * NEW axis 16e: e2e echo -- worst formatting class vs production
   * grid twin through the whole applyTrackingGate pipeline.
   * ============================================================ */
  {
    const worst: TrackingAssessment = {
      ...assessment,
      prescore: 0.7368421052631579,
      quality: 0.30000000000000004,
      coverage: 0.3333333333333333,
      score: 0.9999999999999999
    };
    const worstHash = hashAssessment(worst);
    for (let w = 0; w < 2000; w += 1) {
      for (const [a, h] of [
        [assessment, hash],
        [worst, worstHash]
      ] as const) {
        let c = 100;
        void applyTrackingGate({
          events: events41,
          assessment: a,
          assessmentHash: h,
          expectedSeq: seq,
          policyVersion: "track-v1",
          nowIso: NOW_ISO,
          generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
        });
      }
    }
    const tProd = bench(() => {
      let c = 100;
      void applyTrackingGate({
        events: events41,
        assessment,
        assessmentHash: hash,
        expectedSeq: seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    }, 20000);
    const tWorst = bench(() => {
      let c = 100;
      void applyTrackingGate({
        events: events41,
        assessment: worst,
        assessmentHash: worstHash,
        expectedSeq: seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    }, 20000);
    console.log(
      `--- NEW axis 16e: e2e echo -- applyTrackingGate production-grid ${(tProd * 1e3).toFixed(2)}us vs ` +
        `all-17-digit worst class ${(tWorst * 1e3).toFixed(2)}us (delta ${((tWorst - tProd) * 1e3).toFixed(2)}us/gate ` +
        `x5 gates => ${((tWorst - tProd) * 5 * 1e3).toFixed(1)}us/run; surcharge rides the 2 in-gate hash legs + write-side parse)`
    );
  }
}

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
