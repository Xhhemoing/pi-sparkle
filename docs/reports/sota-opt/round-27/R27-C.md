MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 27 / R27-C：离线路由切片第二十七遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 27 / R27-C（Round 1–26 同区第二十七遍，叠在
  已落地的 S7-C 之上）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `134e596`（测量期起点为
  `69fc043`——R26-H 合入 + Round 26 关轮 + R27-C 派单后的独占分支 tip；测量完成
  后、提交期间独占分支三次快进：先至 `4a65f8c`（R27-A 空枚举合入 + R27-D
  派单），再至 `b6052fb`（R27-B 空枚举合入 + R27-E 派单），又至 `134e596`
  （R27-D 空枚举合入 + R27-F 派单），每次均按派单规则 rebase 本分支并重验冻结
  主张。`git diff 69fc043..134e596 --name-only` = 5 个文件（PROGRESS.md /
  round-27 PLAN.md / R27-A.md / R27-B.md / R27-D.md），全部位于
  `docs/reports/sota-opt/`，**零 `src/` 触碰**。`git log -1 -- src/routing/`
  显示切片最后一次生产改动**仍为 `183df9b`「perf(routing): land S7-C
  support-only eta」**；每次 rebase 前后 `git diff 183df9b..HEAD --` 对 9 个切片
  文件均为**空**（stat 空输出、exit 0），本轮全部测量在该形态之上，未回退未
  重写；测量树与 `134e596` 的 `src/` 逐字节相同 ⇒ 全部读数在新基线下原样有效。
  新合入的 R27-A「标量数值值类面 / scalar numeric value-class face」、R27-B
  「批基数轴底部 + 每调用批不变前奏截距面 / batch-cardinality axis bottom +
  per-call batch-invariant prelude intercept face」、R27-D「策略内容树几何面 /
  policy-content tree-geometry face」三条禁移植轴已与本轮 PROBX 轴做对象域
  切割（宿主/机制/响应变量三重无交，见 §4 增行）。R27-E / R27-F 在飞，未等待、
  未触碰其分支与文件。
- **分支:** `cursor/r27-c-offline-routing-twenty-seventh-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，wc -l = 170/168/311/585/254/156/88/130/177 = 2,039；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第二十七组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第二十七遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R26-C 的全部无号关闭
探针与轴，含 R26-C 的 PRICEX、R25-C 的 AGEX、R24-C 的 OCCX、R23-C 的
ORDX、R22-C 的 SEEDX、R21-C 的 YMIX、R20-C 的 SCALEX、R19-C 的 FITQ）
后，本轮**从未点名站点数为零**——R18-C「TAILG 是尾部段最后一个从未
点名位置」的判定经 R19-C..R26-C 与本轮连续九遍逐行核实成立：每一个
执行站点都能指到既有 ID、无号探针、零池备忘或池算术覆盖。本轮新增
**一条完备性轴**并按规执行**一次天花板重测**，全部以轴/地板测量
（不铸号）收口——没有任何候选道达到接受门槛：

- **PROBX（策略概率坐标 / 重要性权重几何剂量响应，新轴，非候选；
  C 区首次）**——R25-C 把观测行六维、R26-C 把货币维定价完毕后，切片
  公开输入面还剩**最后一对从未剂量化的活读数值坐标**：
  `PropensityLogEntry.behaviorProbability`（μ）与
  `.targetProbability`（π）。它们是 propensity 面三消费函数
  （`computeOverlapDiagnostics` / `isFabricatedPositiveSupport` /
  `validateCounterfactualReport`）的**核心活输入**——重要性权重
  w=π/μ、支持/越界闩锁、ESS、one-hot 伪造检测全部由这一对坐标驱动
  ——而**历轮 C 区每一个构建过 propensity 日志的夹具（含 R26-C
  PRICEX A′ 面）都把它钉死在文档化确定性 one-hot live 形态
  μ=π=(i%10===0?1:0)，从未移动、从未计为维度**。PROBX 冻结体量
  （N=200 恒等断言）/ id 字符串 / 发射次序 / 构建路径（同一
  Array.from 字面形——存储态冻结）/ costUsd（PRICEX 型无平局升序
  锚梯 0.01·(i+1)，**逐剂量对象级断言恒等**——与 PRICEX 恰互为
  冻结补集）/ observedUtility / guardrailBreach / estimatorId /
  claims / config，只动 (μ, π) 值对，分四面定价：
  **A 面（归因/posterior/R1 面，唯一能触 ±35 ms 带的池族）**——
  策略概率坐标**按类型缺席**：`OfflineRow` / `OutcomeObservation` /
  `R1Input` 无任何策略概率字段；rg 普查
  `behaviorProbability|targetProbability` 在 `src/` 的出现点**仅
  `propensity.ts` 一个文件（5 处）**——合同内剂量不存在，**按构造
  关闭**（PRICEX A 面同型）。
  **B 面（live 面——切片内唯二概率消费函数）**——八个 (μ, π) 联合
  几何剂量 {onehot（锚，文档化生产 live 形态）, uniform, hetero,
  spike（重要性权重爆炸 / ESS 塌缩）, wsubn（次正规权重，sumSquares
  下溢归零支）, inf（除以次正规 → Infinity → Inf/Inf=NaN ESS）,
  oob（越界被处理输入，range 闩锁 + violation 腿）, viol（one-hot
  支持违例，目标质量落在未选臂）}，每剂量**预注册**分支/计数期望
  （pushes / rangeFails / violations / everyScans / someScans /
  filterScans 六计数 + supportOk / ESS 等价类 / min/max 逐位 /
  invalid-marker 自有属性 / 检测器布尔 / 验证器 reasons 数）并由
  **仪表化逐字复刻道**（PSTAT/FSTAT 计数器，浮点操作/操作数/顺序
  零改动）**逐剂量逐位核准后**才采信：八剂量全部命中预注册值
  （四运行 ×3 自确定性调用全绿）——ESS 等价类按剂量走完
  {===20, ===200, Cauchy–Schwarz 严格内点, 塌缩至 (1,1.001),
  下溢归零支 ===0, **Inf/Inf 传播 NaN**, ≈100, 全零权重 ===0}
  全谱。定价（N=200 ≥ 仓内唯一调用方——单元测试 N≤4——的 50×；
  **`src/` 生产调用方为零**，rg 普查三消费函数仓内仅
  `propensity.ts` 自身 + `test/unit/routing/propensity.test.ts`）：
  `computeOverlapDiagnostics` 六个常规几何剂量 0.72–1.71 µs/call，
  wsubn/inf 8.13–8.25 µs/call（次正规算术钳制 ×6–11——**µs 级
  本身即派单拒绝类**）；`isFabricatedPositiveSupport`
  0.17–1.24 µs/call；`validateCounterfactualReport`
  0.017–0.273 µs/call（故障腿 once-per-fault REJX 类）。
  **体量天花板探针**（N=20,000 = 100× 锚，天花板算术非剂量——体量
  不是本轴坐标，SCALEX 不补铸）：最坏形态 0.890–0.924 ms/call ⇒
  触 35 ms 带需 **37.9–39.1 次调用/报告，而生产通量为 0 次调用/
  报告**（无 `src/` 调用方）。
  **A′ 面（声明死字段收口）**——propensity 行仅剩的两枚声明而零
  读点数值：`observedUtility` 五值剂量（anchor / undefined / 1e12 /
  5e-324 / 1/3）+ 弃用可选 `propensity` 五值剂量（0 / 1 / 5e-324 /
  0.5 / mirror-onehot，字段物化经 hasOwnProperty 断言），10 剂量 ×
  {诊断, 验证, 伪造检测} 输出**全部逐位等于锚**（四运行全绿）——
  把 PRICEX 的 costUsd 零读见证扩展到整行：**至此
  `PropensityLogEntry` 的每一枚数值字段要么被活剂量化要么被死见证**。
  **C 面（验证器数值输入面）**——`CounterfactualReport` 的活读数值
  腿恰两条：`reportVersion`（!== 比较）与
  `diagnostics.effectiveSampleSize`（< minESS 比较 + 故障腿
  toFixed(2) once-per-fault）；ESS 五剂量 {pass, below, subn, NaN,
  +Inf} → reasons {0,1,1,0,0}（**NaN/Inf ESS 穿过 < min 门——
  NaN<2 与 Inf<2 均为 false，冻结契约的语义事实，诚实记名非候选：
  行为/阈值面禁区**）；version 四剂量 {1, 99, 1.5, NaN} → reasons
  {0,1,1,1}；验证器死数值四枚（totalActions / eligibleActions /
  minPropensity / maxPropensity）五剂量补丁（含 all-four 极端）
  验证输出**逐位等于锚**。腿定价：pass 0.142–0.154 µs、ess-fault
  0.107–0.112 µs、version-fault 0.028–0.029 µs——全部亚 µs +
  once-per-fault。**策略概率坐标在四张脸上全部无越线点（归因面按
  构造、live 面 µs 域直测封顶 + 100× 天花板算术 + 零生产通量、
  死字段面逐位见证、验证器面亚 µs 封顶）**。候选道形族三重拒绝
  （见 §2）；概率几何本身是**调用方数据不是代码自由度**。轴一次性
  收口，非候选。
- **APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠）**
  ——本轮四次读数 APC 池 **46.8 / 46.4 / 46.9 / 46.1 ms**，全部落在
  R26-C 重锚定带 **46.0–47.3 之内** ⇒ 带维持，无需重锚；天花板重测
  每次运行执行：APCX 无计时器独立道 **53.8 / 55.6 / 59.2 / 53.9 ms**
  （与 R26-C 54.7–60.2 带交叠），FLOORX 最廉价逐位合法形态地板
  **38.0 / 34.6 / 40.7 / 34.0 ms** ⇒ **组合天花板 = 15.7 / 21.0 /
  18.5 / 19.9 ms < 35**（与 R26-C 带 19.5–22.9 在 [19.5, 21.0]
  交叠；15.7 在 R16-C..R26-C 历史读数域 [12.1, 25.6] 之内），组合
  关闭维持。**sink=7.309 与 floorSink=7.309 八读数全同，且与
  R14-C/R16-C..R26-C 逐位同值**（派单强制的跨轮确定性旁证成立）。

除上述轴与天花板重测外，第二十七遍逐行重读**零新发现**：其余每一个
执行站点都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 /
结构交换负优化）+ R18-C 补充的 once-per-fault 拒绝类。三大池地板与
组合天花板复核见 §3：本 VM 上生产中位 **660.6–668.4 ms/报告（四次
运行 668.4 / 660.6 / 665.4 / 664.8；三次正典 660.6–665.4）**——与
R26-C 带 663.0–669.9 在 [663.0, 668.4] 宽交叠（正典下沿 660.6 低于
R26-C 带下沿 2.4 ms，与同运行 7-rep 散布 659.9–675.1 同量级 = 亚
噪声，按 R14-C 处置规则**随读重锚为本轮带 660.6–668.4**；方向与本
VM 类整体微快一致，同 VM 独立旁证：r7c 回归资产锚点 **production
658.9 ms = 1.12× vs 冻结 S6-C ctl 738.3**（R26-C：659.9 / 734.4）、
r1c 锚点 **production 663.6 ms = 2.30× vs 参考 1527.7**（R26-C：
665.1 / 1536.9），均同向互证）；非切片回归或加速（切片对 `183df9b`
零 diff）。小形状档整报告中位 **32.2–32.9 ms**，整条报告低于噪声带
（R7-I 矩阵义务履行）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11，`writeAttributionPair` 两路调用本轮 rg 复核在位），live 面零
文件改动（`src/supervisor/` 对 9 文件导入为零，本轮 rg 复核）。仓库
变更仅本报告一个文件；无生产赢家故未提交 `/tmp/r27c/r27c-anchor.mts`
（败者/锚点仿真全文进附录，遵守 R7-C..R26-C 先例）。

## 0. 范围与约束遵守

- 读序（强制）已履行：README → EXCLUSIONS.md（完整表，含全部
  S\*-C-\* 行与 S12-C-1、S13-B-1、X1-3/X3-4/X3-5/X4-2 posterior/
  propensity/bandit 行）→ PROGRESS.md → round-27/PLAN.md（C 段 +
  全部 plan-ban 轴）→ R26-C.md（空枚举确认，无 S26-C-\*；PRICEX 轴
  收口与「切片公开输入面全数值坐标定价完毕」裁决域勘定——该裁决域
  为观测行六维 + 决策面货币维，**propensity 行的 (μ, π) 概率对不在
  其上**：PRICEX 的 A′ 面恰以钉死 (μ, π) 为前提剂量 costUsd，本轮
  PROBX 以钉死 costUsd 为前提剂量 (μ, π)——互为冻结补集，见 §4）→
  R26-H.md（consumer-stage operand-realm face 判例与 R26-F 撞轴
  降级教训——本轮轴对象为输入值几何非容器 realm/存储态，零交集）→
  R25-C.md（AGEX）→ R24-C.md（OCCX）/ R23-C.md（ORDX）/
  R22-C.md（SEEDX）/ R21-C（YMIX）/ R20-C（SCALEX）/ R19-C（FITQ）
  轴定义核对（经 EXCLUSIONS 完整表逐条比对，证非重铸）→ 运行中
  快进合入的 R27-A / R27-B / R27-D 新轴段与更新版 PLAN 禁项（scalar
  numeric value-class face、batch-cardinality bottom + prelude
  intercept face、policy-content tree-geometry face——对象域与
  PROBX 的切割见 §4 增行，零移植）→
  9 个切片源文件全量重读 + `offline-types.ts` / `outcomes.ts` /
  `r0.ts` / `policy.ts` 契约面跳读（概率字段的入口与消费图勘定，
  见下）。
- 基线漂移检查（含运行中三次快进）：测量期基线 `69fc043` 相对 R26-C
  基线 `f0bcdbb` 为 docs(sota-opt) 提交链（R26-E..R26-J 空枚举合入 +
  Round 26 关轮 + R27-A/B/C 派单）；测量完成后独占分支先快进至
  `4a65f8c`（R27-A 空枚举合入 + R27-D 派单），提交后又两次快进至
  `b6052fb`（R27-B 空枚举合入 + R27-E 派单）与 `134e596`（R27-D
  空枚举合入 + R27-F 派单），每次均按派单规则 rebase 本分支——
  `git diff 69fc043..134e596 --name-only` = 5 个文件全部
  `docs/reports/sota-opt/`（PROGRESS.md / round-27 PLAN.md /
  R27-A.md / R27-B.md / R27-D.md），零 `src/` 触碰；每次 rebase
  前后 `git log -1 -- src/routing/` 均为 `183df9b`（S7-C 落地
  本身），`git diff 183df9b..HEAD --` 对 9 文件均为空（stat 空
  输出、exit 0），落地形态原样在位 ⇒ 测量树与新基线 `src/` 逐字节
  相同，全部读数原样有效。三份新合入报告的新轴段已核对（R27-A
  scalar numeric value-class face = A 区 tracking 消费面「数值字段
  装哪一个 double」的每标量值类价格普查；R27-B batch-cardinality
  axis bottom + per-call batch-invariant prelude intercept face =
  B 区 live 面批基数 N 轴底部 {0,1,2,3,5,8,16} 扫描 + 每调用批
  不变前奏截距仿射分解；R27-D policy-content tree-geometry face =
  D 区 adaptation 面策略内容树几何——三者对象域均与 PROBX 的
  「(μ, π) 联合几何 → 分支流/ESS 等价类/估计器语义」无交，见 §4
  增行），零移植零补铸。既有回归资产七连全绿且检查数逐一匹配锚定
  值（§5）。
- 调用图确认（派单强制项，本轮 rg 复核）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 的 `writeAttributionPair`
  消费（双归因两路在位）；`src/supervisor/` 对 9 个切片文件的导入为
  **零**——live = R0 等价维持，R1 未接线，`src/supervisor/model-router.ts`
  零字节未触。
- 概率坐标消费图（本轮新勘定，PROBX 轴的对象域）：
  `behaviorProbability|targetProbability` 在 `src/` 的**全部**出现点
  = `propensity.ts` 一个文件（5 处：类型声明两行 + `behaviorMu` /
  `targetPi` 两访问器 + 文档注释）；三消费函数
  （`computeOverlapDiagnostics` / `isFabricatedPositiveSupport` /
  `validateCounterfactualReport`）的仓内出现点仅 `propensity.ts`
  自身 + `test/unit/routing/propensity.test.ts`（N≤4 的单元夹具）
  ——**`src/` 生产调用方为零**（生产通量 0 次调用/报告）。
  `src/experiments/replay.ts` 的 `propensity` / `propensityFor` /
  `propensities` 字段是其**自有本地类型**（frozen-episode 离线
  台账，实验区），不导入 `src/routing/propensity.ts`、不进本切片
  任何函数——异面零耦合，诚实记名不越权。归因/posterior/R1 面：
  `OfflineRow` / `OutcomeObservation` / `R1Input` **无任何策略概率
  字段**（类型级缺席，PROBX A 面）。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S26-\*（含 S12-C-1，重开条件：无）
  未重提、未立道、未重测；R13-C..R26-C 二十六个无名关闭（PIVCSE/
  FINTAIL/REFZ/TSORT/DEG/SWORD/XF64/IRLSDP/CMPH/STORD/RIDGE/ICOL/
  SFILL/NSQRT/PMV/OSTZ/TAILG/REJX/FITQ/SCALEX/YMIX/SEEDX/ORDX/OCCX/
  AGEX/**PRICEX**）未重探未补铸未换址重提——**SCALEX 未重测、YMIX
  未重测、SEEDX 未重测、ORDX 未重测、OCCX 未重测、AGEX 未重测、
  PRICEX 未重测**（PROBX 全部剂量点都在冻结体量 N=200、冻结 id
  字符串、冻结发射次序、冻结构建路径、无 RNG、无时间戳消费、
  **costUsd 逐剂量对象级断言钉死在 PRICEX 型锚梯**、冻结
  observedUtility / estimatorId / claims / config 上，未触任何体量
  剂量、组成剂量、种子剂量、次序剂量、占据剂量、时间剂量或价格
  剂量——PROBX 的每个剂量动的是 (μ, π) 值对，其余坐标八剂量恒等
  断言；八轴两两正交，§4）；体量天花板探针（N=20,000）是**池天花板
  算术**不是体量剂量（单形态单读数、无 α 标定、无跨体量决斗——
  SCALEX 的 K 轴口径未重测）；本轮测量器**不含** FITQ 逐抽取计时器
  （FITQ 重开条件——bootstrap 抽取数或拟合规模位移 ≥1 个数量级——
  未被触发：options 全程默认值）；ITERX/COLDX/AAFLR/GCAX 四轴未补铸
  （B 面读数取自全剂量预热后的稳态窗口，首触效应不进入读数；IRLS
  迭代锚定值 8,966 在 §1 原样复核）；**IDG 未铸未移植**（PROBX 各
  剂量断言 episodeHash/modelId 字符串集合冻结——frozen-coordinates
  逐剂量断言，零字符串移动）；R25-A value-domain / string
  content-class census、R25-B task-face optional-field presence
  topology、R25-D identity-coordinate face、R25-F plan-header
  identity-geometry face、R25-G injection command-kind face、R25-H
  consumer-stage string-representation census、R25-I
  process-environment coordinate、R24-I adaptation-state coordinate、
  R24-H operand-residency census、M=1 catalog collapse、
  approval-authority face、serialization-boundary provenance face、
  **COTARG** / JB / RUNLIM / CALLB / CKMIX / SHAPEK / KFAN /
  TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / **VALCLASS** / CG /
  **RTL** / **DELINV** 均未移植（派单点名禁项，宿主与机制均在他区；
  PROBX 与其中最近邻者的对象域切割见 §4）；R26-A collation-locale
  face / R26-B privacy-provider-policy constraint-leg face / R26-D
  injection-seam default-arm face / R26-F input storage-state face /
  R26-G edge condition-kind face / R26-H consumer-stage operand-realm
  face / R26-I argv surface-form face 未移植（PROBX 剂量全程同 realm
  原生数组、同构建路径、零 argv、零 locale、零注入缝——存储态与
  realm 坐标冻结，见 §4）；**R27-A scalar numeric value-class face
  / R27-B batch-cardinality bottom + prelude intercept face /
  R27-D policy-content tree-geometry face（运行中三次快进新增禁项）
  未移植**（§4 增行）；RID/CNT 归约重排反例
  原样引用；嵌套序 × 布局组合空间（S4-C-2 + S5-C-1..4 + S12-C-1）
  未重开；CMB 组合未重堆叠（本轮 APCX/FLOORX 重测系派单强制天花板
  复测；PROBX 各剂量点只测落地形态一条道，零变体决斗）；PLSEG/PMV/
  TAILG 备忘未重提。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动（**C 面的 NaN/Inf-ESS 语义事实仅记名不改动**——验证器行为
  与阈值均为禁区）；X2-3 数值路径未触；单线程逐位审计基底不动；
  fail-open 族维持关闭；无 fsync / 完整性再哈希移除（本切片不涉盘，
  天然成立）。
- 环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0——回到
  R1-C..R26-C 的处理；VM 默认 22.14.0 未使用）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`（`pnpm gate` 全绿，见 §5）。

## 1. 重剖析：S7-C 后成本模型（本 VM 实测，未拷贝历轮数字）

测量器为**仪表化逐字复刻管线**（与 183df9b 生产 irls/solve/on-prob/
APC 逐语句相同，仅在调用边界加 performance.now() 与计数器，浮点操作/
操作数/顺序零改动），其忠实性先经 **53 例全形状电池 ×{仪表道, 纯净
落地道} vs 生产 `fitLogitAdditive` 导入逐位全绿**（Object.is 含符号零）
再采信读数；propensity 面新增**仪表化逐字复刻道**
（`computeOverlapDiagnosticsReplica` + PSTAT 计数器——scanned /
rangeFails / violations / pushes，`isFabricatedPositiveSupportReplica`
+ FSTAT 计数器——everyScans / someScans / filterScans，浮点路径零
改动），其忠实性在 **PROBX 的每一个 (μ, π) 剂量点**对生产导入
9 字段逐位全绿（Object.is 含符号零/NaN + invalidReason 自有属性
断言，每剂量）后才采信读数；生产中位数与全部 PROBX 墙钟读数均由
**未仪表化生产导入**直接测得。perf 夹具与 R10-C..R26-C 完全一致
（fixtureRng(0xbeef)，4 场景 × 6 模型 × 8 项目，rows=400,
passRate=0.6，bootstrap=200, seed=20260818 → p=60，201 拟合，
8,966 次 IRLS 迭代）。

本轮共四次全量运行：run 0 为**预演运行**（脚本与正典运行**逐字节
相同**——预演后零改动，四次运行同一脚本），run A / run B / run C 为
正典运行。四次运行全部计入处置依据，不作剔除；每次运行 6,538 项
逐位检查全绿。

生产整报告中位（每次运行内 7 测量取中位）：**668.4 / 660.6 /
665.4 / 664.8 ms**——四读数带 660.6–668.4 与 R26-C 带 663.0–669.9
在 [663.0, 668.4] 宽交叠，正典下沿 660.6 低于 R26-C 带下沿 2.4 ms
（同运行 7-rep 散布 659.9–671.3 同量级 = 亚噪声），按 R14-C 处置
规则**随读重锚为本轮带 660.6–668.4（正典 660.6–665.4）**；方向与
本 VM 类整体微快一致（同 VM 独立旁证：r7c 回归资产锚点 **production
658.9 ms = 1.12× vs 冻结 S6-C ctl 738.3**（R26-C：659.9/734.4）、
r1c 锚点 **production 663.6 ms = 2.30× vs 参考 1527.7**（R26-C：
665.1/1536.9），同向互证）；判为 VM 微架构漂移带内往返，非切片回归
或加速（切片对 `183df9b` 零 diff）。

仪表化池表（perf 形状，每次运行内 3 次仪表化取中位；三次正典运行
读数并列，预演读数注记）：

| 池 | run A | run B | run C | 占比 | 站点计数（精确） |
| --- | --- | --- | --- | --- | --- |
| 总计 | 663.2 | 673.9 | 666.6 | 100% | fits=201 |
| `solveSymmetric` | **450.8** | **453.9** | **454.1** | **67.4–68.1%** | 8,966 次调用 |
| `irls` 自身（扣 solve） | **152.6** | **152.5** | **150.7** | **22.4–23.0%** | 8,966 迭代 / 3,586,400 行访问 / 1,249,652 sigmoid |
| APC 全站点 | 46.4 | 46.9 | 46.1 | 6.9–7.0% | 277,606 活跃对 / 12,259 次调用 |
| `onProbabilitiesFor` | 6.8 | 6.8 | 6.8 | 1.0% | 80,400 sigmoid+dot |
| bootstrap 采样 | 1.8 | 1.7 | 2.0 | 0.3% | —（SAMP 池 R12-C 已封顶） |
| 排序/分位尾 | 1.5 | 1.5 | 1.5 | 0.2% | 12,200 draws-map ops |
| 基行投影 | ~0.004 | ~0.003 | ~0.003 | ~0% | 每报告 1 次（R12-C BASE 已封顶） |
| 编排余量（build/supports/keys） | 3.3 | 10.5 | 5.4 | 0.5–1.6% | —（差值余项注记同 R24-C） |
| 抽取结局计数 | 200/0/0 | 200/0/0 | 200/0/0 | — | successful/collapsed/singular，四运行逐位一致 |

预演读数（同口径）：run 0 总计 672.2 / solve 454.2 / irls-other
150.8 / APC 46.8——预演与正典四池零级差，无 R24-C 预演型全池上移
事件。

除 solve / irls / APC 外每池整池清零也低于 ±35 ms 带（R9-C..R26-C
同结论，逐池复测未漂移）；solve 与 irls 为落地线唯二宿主。全部站点
计数与历轮锚定值逐一相同（8,966 / 3,586,400 / 1,249,652 / 277,606 /
12,259 / 12,200 / 80,400 / 201——跨轮确定性旁证）。**APC 池本 VM
四次读数 46.1–46.9，全部落在 R26-C 重锚定带 46.0–47.3 之内** ⇒ 带
维持，无需重锚；派单强制的天花板重测仍在每次运行执行（§3，天花板
15.7–21.0 < 35，关闭维持且 sink/floorSink 八读数逐位同值）。小形状
档（3 模型 × 3 项目 / 120 行，bootstrap=200）整报告中位 **32.9 /
32.3 / 32.5 ms**（正典；预演 32.2）——小配置态整条报告低于噪声带，
调用矩阵小态一侧整体收口（R7-I 矩阵义务履行）。

生产自确定性复核：3 seed × 各 2 次运行全字段 Object.is 全绿（含于
§5 的 6,538 项检查 × 4 次运行）。

## 2. 第二十七组枚举（诚实结果：空；零新站点 + 一条新轴 + 一次按规天花板重测）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过，且本轮从未点名站点数为零**（连续第九遍；探针数字为
本 VM 实测，出处为附录测量彩排三次正典运行 + 一次预演）：

| 站点/轴 | 关闭方式 | 证据 |
| --- | --- | --- |
| 从未点名站点普查：**零**（连续第九遍） | 逐行三问全覆盖 | 27 遍扫描把 9 文件每一个执行站点指到既有覆盖物：offline-logit 的编排头（DEG/BASE）、irls 全站点（S2-C/S3-C/S6-C/S7-C 落地体 + S10-C-1/S9-C-3/S9-C-4/S6-C-5/6/S5-C-7/S4-C-5/6/MUCSE/SWORD/STORD/RIDGE/ICOL/IRLSDP + 收敛尾 S2-C-3/S8-C-3/NSQRT + ITERX/FITQ）、solve 全站点（COPY/S4-C-3/XF64 + PIVCSE/S5-C-6 + PIVSWAP + FZERO + S4-C/S5-C 落地体 + S12-C-1 + S6-C-7 + SFILL/FINTAIL + S8-C-4）、APC（S1-C-1/S2-C-2/S2-C-4/S4-C-4/S8-C-2/S10-C-2 + FLOORX 天花板）、on-prob（S7-C-4/S8-C-1）、采样环（SAMP/S3-C-3/S2-C-5）、抽取效应环（S7-C-1/DMAP）、尾部效应环（TSORT/CMPH/REFZ/TAILG）、诊断尾（R15-C 零池注记）、posterior（X1-3/X3-4/S1-C-7/OSTZ/S1-C-4 + 已落地 memo + R25-C AGEX 直测封顶）、prob-add（S1-C-6/PMV/PLSEG）、propensity（S1-C-8/X3-5 遍数常数域——**其中 (μ, π) 活读对、observedUtility/`propensity` 死字段、验证器数值输入面本轮全部由 PROBX 四面直测/见证封顶**）、r1（X1-4/S1-C-10/S1-C-2/3 + R26-C PRICEX price-reduce 封顶）、bandit/shadow（S1-C-9/X4-2——**其宿主池本轮以 selectArm 244–255 ns / recordReward 165–179 ns 划界读数再证 ns 域**）、r1-shadow-report（X1-5 + REJX 终点定价 + PRICEX selectedCost 封顶）——R18-C「TAILG 是尾部段最后一个从未点名位置」的判定经 R19-C..R26-C 与本遍连续九轮核实成立；观测行六维（SCALEX/YMIX/SEEDX/ORDX/OCCX/AGEX）与货币维（PRICEX）关死后本轮七轴零位移，**propensity 行概率对经本轮 PROBX 关死** |
| PROBX：策略概率坐标 / 重要性权重几何剂量响应（新轴，非候选；C 区首次，补齐切片公开输入面**最后一对活读数值坐标**——`PropensityLogEntry.behaviorProbability`（μ）与 `.targetProbability`（π）；历轮全部 propensity 夹具钉死文档化 one-hot live 形态 μ=π=(i%10===0?1:0) 从未移动） | A 面按构造关闭（类型级缺席）+ B 面八剂量预注册计数/等价类全命中 + 仪表复刻逐剂量逐位 + µs 域直测封顶 + 100× 体量天花板算术 + 零生产通量 + A′ 面 10 剂量逐位零响应见证（整行数值字段收口）+ C 面活腿亚 µs 封顶 + 死数值五剂量逐位见证 + 候选道形族三重拒绝 | **A 面**（归因/posterior/R1 面）：`OfflineRow` / `OutcomeObservation` / `R1Input` 无任何策略概率字段；rg 普查 `behaviorProbability\|targetProbability` 在 `src/` 仅 `propensity.ts` 5 处 ⇒ 合同内剂量**不存在**，按构造关闭（PRICEX A 面同型）。**B 面**（八剂量 onehot/uniform/hetero/spike/wsubn/inf/oob/viol @ N=200 冻结、id/次序/构建路径/costUsd（PRICEX 锚梯逐剂量对象级断言）/utility/estimator/config 全冻结）：**预注册计数全命中**——pushes {20,200,200,200,200,200,100,20}、rangeFails {0,0,0,0,0,0,200,0}、violations {0,0,0,0,0,0,100,20}、检测器扫描三计数逐剂量精确（含 every/some 短路语义：onehot 2/1/200、uniform 200/200/0、oob 1/200/0）；**ESS 等价类全谱走完**——onehot ===20、uniform ===200、hetero Cauchy–Schwarz 严格内点、spike 塌缩 (1,1.001)（单条 μ=1e-6 → w=1e6 把 ESS 从 200 压到 ≈1.0004）、wsubn ===0（π=5e-324 → sumSquares 下溢归零支）、**inf NaN**（μ=5e-324 → w=Inf → Inf/Inf）、oob ≈100、viol ===0（全零权重）；supportOk {T,T,T,T,T,T,F,F} + invalid-marker 自有属性逐剂量断言；验证器 reasons {0,0,0,1,1,0,1,2} 全命中；复刻道 vs 生产道 9 字段逐位（Object.is 含 NaN/符号零）+ 生产 ×3 自确定性每剂量全绿（四运行）。定价：`computeOverlapDiagnostics` 常规几何 0.72–1.71 µs/call、**wsubn/inf 8.13–8.25 µs/call（次正规算术钳制 ×6–11——整池仍 µs 域 = 派单拒绝类）**；`isFabricatedPositiveSupport` 0.17–1.24 µs；`validateCounterfactualReport` 0.017–0.273 µs（故障腿 once-per-fault REJX 类）。**体量天花板探针**（N=20,000 = 100× 锚；天花板算术非体量剂量，SCALEX 不补铸）：最坏形态 0.890–0.924 ms/call ⇒ 触 35 ms 带需 **37.9–39.1 次调用/报告；生产通量 = 0 次调用/报告**（三消费函数 `src/` 调用方为零，仓内唯一调用方为 N≤4 单元测试；`src/experiments/replay.ts` 的 propensity 字段系自有本地类型零耦合）。**A′ 面**：observedUtility 五值剂量（anchor/undef/1e12/5e-324/1/3）+ 弃用 `propensity` 五值剂量（0/1/5e-324/0.5/mirror，hasOwnProperty 物化断言），10 剂量 × 三消费函数输出**逐位等于锚**（四运行）——`PropensityLogEntry` 整行数值字段至此活剂量化或死见证完毕。**C 面**：活读数值腿恰两条（reportVersion !==、effectiveSampleSize < + toFixed(2) 故障腿）；ESS 五剂量 reasons {0,1,1,0,0}——**NaN/Inf ESS 穿过 < min 门（NaN<2=false, Inf<2=false）系冻结契约语义事实，诚实记名非候选（行为/阈值禁区）**；version 四剂量 reasons {0,1,1,1}；死数值四枚（totalActions/eligibleActions/minPropensity/maxPropensity）五剂量补丁（含 all-four）验证输出逐位等于锚；腿定价 pass 0.142–0.154 / ess-fault 0.107–0.112 / version-fault 0.028–0.029 µs——亚 µs + once-per-fault。**收口算术：概率几何对切片的全部触达 = 三消费函数（生产通量零）；最坏可构造单价（次正规联合比值 @ 100× 体量）0.924 ms/call，触带需 ≈38 调用/报告而生产为 0——每条腿离带 ≥3 个数量级**。候选道形族三重拒绝：「三遍权重归约融合单遍（sum/sumSquares/扫描合并）」——宿主池 µs 域亚带 ×4,000+（S9-C-4 型深亚带拒绝），且零生产通量 = 为不存在的调用方省 µs；「isFabricatedPositiveSupport 的 some+filter 双扫合一/短路重排」——同宿主亚带 + 短路计数语义为契约行为（every/some 早停已是生产语义，重排属行为面）；「诊断结果按 logs 引用 memo 化」——零重复调用生产模式（零调用方），数组变异下语义风险 = 行为违例域。概率几何本身是**调用方数据不是代码自由度**——切片内不存在以 (μ, π) 为对象的合法变换。重开仅当 PropensityLogEntry 概率字段契约变更或三消费函数被接进 ≥38 调用/报告 × N≥20,000 的生产环路（公开签名/接线面，不预期）。轴一次性收口，无候选道 |
| APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠；池 46.1–46.9 四读数全部落在 R26-C 带 46.0–47.3 内 ⇒ 带维持无需重锚） | 天花板仍亚带 + sink 逐位同值 | APCX **53.8 / 55.6 / 59.2 / 53.9**（与 R26-C 54.7–60.2 带交叠）；FLOORX **38.0 / 34.6 / 40.7 / 34.0** ⇒ 天花板 **15.7 / 21.0 / 18.5 / 19.9 < 35**（与 R26-C 带 19.5–22.9 在 [19.5, 21.0] 交叠；全部读数在 R16-C..R26-C 历史域 [12.1, 25.6] 内），距带最近读数仍差 14.0 ms；**sink=7.309 与 floorSink=7.309 八读数全同，且与 R14-C/R16-C..R26-C 逐位同值**——派单强制的 APC 关闭维持条件（sink 逐位匹配）成立；S8-C-2/S10-C-2/S2-C-4/S4-C-4 与 CMB 禁令全部维持原状 |

以上无一构成候选：从未点名普查结果为零（无新站点可立道）；PROBX
是策略概率坐标定价轴不是变换（各剂量点只测落地形态一条道，零变体
决斗——其全部潜在候选族被三重拒绝直接排除：µs 域深亚带宿主池 +
零生产通量 + 行为/语义违例）；APCX/FLOORX 是按规天花板测量不是
变换。**故第二十七组候选集为空，不铸 S27-C-1 及后续编号**，遵循
R11-C..R26-C 无号关闭先例。

**枚举收口：** 除上表外，第二十七遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖——本轮
PROBX 把 propensity 三消费函数从「池算术覆盖」升级为「输入维度四面
直测封顶」）。R26-C 的「切片公开输入面全数值坐标定价完毕」裁决域为
观测行六维 + 决策面货币维；本轮把裁决域扩到 **propensity 行与验证器
报告面的全部数值坐标**：(μ, π) 活读对（PROBX B 面）+ observedUtility
/ 弃用 propensity 死字段（A′ 面）+ reportVersion / effectiveSampleSize
活腿与四枚验证器死数值（C 面）——至此**切片公开输入面上不存在从未
定价的数值坐标：观测行六维 + 货币维 + 概率对 + 验证器报告面全数值
腿**。仍不剂量化、不铸名的坐标（诚实划界）：options 坐标
（maxIter/bootstrap——§3(a) 契约面）、config 坐标（halfLife/
minSamples/priors/lcbKind + bandit/shadow 的 seed/epsilon/
explorationBudget/comparisonBudgetUsd/comparisonCostUsd——config
面，与 R24-I adaptation-state coordinate 同面，派单禁移植域；本轮
selectArm/recordReward 划界读数只证宿主池 ns 域，零 config 剂量）、
`TaskFeatures.contextTokens/outputTokens`（bandit 面声明而切片内
零读点的探索面数值——宿主池 ns 域划界封顶，不另立轴）、字符串身份
坐标（IDG 冻结）——均不属输入数据轴域。本轮核实换名再提交拒列：
零——没有任何已关 ID 的换址/换名形态在本轮出现。

## 3. 三大池地板与组合出路复核

- **solve 池 450.8–454.1 ms（本 VM 三正典读数；预演 454.2，与
  R26-C 带 451.4–453.2 及 R25-C 带 452.7–454.5 交叠）**：消元主体
  ≈ 6.45 亿次冻结顺序浮点乘减/报告。R6-C §2.5 七类分解 + S10-C-3 +
  S12-C-1（嵌套序，稳定 ~3.6× 负优化，重开条件：无）+ R11-C COPY +
  R12-C FZERO/PIVSWAP + S5-C-6 + R13-C PIVCSE/FINTAIL + R14-C XF64 +
  R16-C SFILL 之后，本轮**零新位置**。ITERX 坐实池量线性依赖 8,966
  次冻结迭代（本轮锚定值逐位复核），FITQ 坐实拟合粒度无胖尾，
  SCALEX 坐实其 K 轴 α=2.75，YMIX/SEEDX/ORDX 坐实其每迭代单价组成/
  种子/次序不变，OCCX 坐实其占据响应律，AGEX 坐实时间维零触达，
  PRICEX 坐实货币维零触达，**PROBX（本轮）坐实概率维度对该池零
  触达**：策略概率字段在归因面按类型缺席（合同内剂量不存在），
  IRLS/solve 全部站点计数本轮恒等复核。八个输入维度都只会让该池
  占比更高或持平，收敛缩短类无逐位合法形态。触碰地板本体的机制
  只剩 SIMD/线程（S9-C-1/2 + 不变量禁止）与数值路径改道（X2-3
  冻结）。
- **irls 自身池 150.7–152.6 ms（预演 150.8；与 R26-C 带 150.8–152.3
  及 R24-C 带 151.7–156.6 交叠）**：可省项全部已关闭（S2-C/S6-C/
  S7-C 落地本体之上：S10-C-1 决斗带内、S9-C-3 符号翻转、S6-C-6
  上界 2–4、S5-C-7 无正决斗、S9-C-4、S4-C-5/6、S2-C-3、S8-C-3、
  R11-C RID/CNT/EXP/ALLOC、R12-C MUCSE、R14-C SWORD/IRLSDP、R15-C
  STORD/RIDGE、R16-C ICOL、R17-C NSQRT）。概率维度对该池零触达
  （A 面类型级缺席）。本轮零新位置。
- **APC 池 46.1–46.9 ms（本 VM 四读数；全部落在 R26-C 重锚定带
  46.0–47.3 之内 ⇒ 带维持）**：派单强制天花板重测照常执行（这不是
  重跑已关 ID 的决斗，也不构建任何堆叠候选道）：APCX 无计时器独立
  道 **53.8–59.2 ms**（基形状近似，**sink=7.309 与 R14-C/
  R16-C..R26-C 逐位同值** = 跨轮确定性旁证，派单强制项成立）；
  FLOORX 最廉价逐位合法形态地板 **34.0–40.7 ms** ⇒ **组合天花板 =
  池独立道 − 地板 = 15.7–21.0 ms（四次读数）< 35**。十二轮天花板
  读数 12.1–23.8 / 13.8–21.6 / 12.1–20.9 / 13.9–24.0 / 17.4–21.6 /
  12.5–19.8 / 17.1–21.8 / 14.6–16.0 / 15.9–22.7 / 20.8–25.6 /
  19.5–22.9 / 15.7–21.0 稳定亚带，组合关闭维持。R13-C REFZ 参考
  水平份额不受影响。PROBX 的 A 面见证补强该闭合：APC 活跃对
  277,606 本轮恒等——概率维度无法触达 APC 池（类型级）。
- **引擎侧三轴（R15-C/R16-C 收口，本轮未重测未补铸）**：COLDX
  预热份额、GCAX 收集器份额、ALLOC 分配封顶——三轴均无切片内可
  受理变换，稳态口径下三大池冻结结论不受影响；本轮中位口径仍为
  稳态口径（PROBX 各剂量点取自全剂量预热后的稳态窗口，首触效应
  不进入读数）。
- **拒绝面（R18-C REJX 轴收口，本轮未重测未补铸）**：31 终点
  once-per-fault 定价原样引用；拒绝路径不构成第五类成本；PROBX 的
  C 面故障腿（ess-fault 0.107–0.112 µs 含模板字面量 + toFixed(2)、
  version-fault 0.028–0.029 µs）就是该拒绝类的两枚新剂量点打印
  ——once-per-fault + 亚 µs，类判定原样成立；oob/viol 剂量的闩锁
  腿（rangeFails/violations 分支）逐条计数定价，整池仍 µs 域。
  四分类 + once-per-fault 拒绝类继续穷尽。
- **propensity 面（本轮 PROBX 四面直测封顶）**：三消费函数在合成
  N=200（≥ 仓内唯一调用方 50×）下整面 ≤10 µs/call，最坏可构造
  单价（次正规联合比值 @ N=20,000 = 100×）0.890–0.924 ms/call，
  触带需 ≈38 调用/报告而**生产通量为 0**——该面整体离带 ≥3 个
  数量级。与 R10-C「七文件亚 ms 级」裁决兼容且更强（输入维度直测
  而非池算术外推）。探索面（bandit/shadow）划界读数 selectArm
  244–255 ns / recordReward 165–179 ns 再证 S1-C-9/X4-2 的 ns 域
  裁决。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测带级摆幅带内），
  维持关闭。本轮探针无一是稳定越带正项（从未点名普查为零、PROBX
  是概率坐标定价、APCX/FLOORX 是天花板测量不是候选），无新成员可
  入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建
面开放 / X2-3 数值路径解冻 / 单线程审计基底变更 / maxIter=50、
tol=1e-8、bootstrap/seed 契约、rows/OutcomeObservation 输入契约、
PropensityLogEntry 概率/货币字段契约变更——均不预期，前者属阈值/
测试面禁区、后者属公开签名面）；（b）现实规模位移触达 SCALEX 标定
的越线点（最尖锐 on-prob K\*=2.6×、名义收益口径 ≥~3.4×；APC 组合
外推 ~1.4× 需按 R14-C 规则在该剂量重跑地板——今日生产流量在 perf
夹具当量及以下，均未触达；结局组成维度经 YMIX、重采样身份维度经
SEEDX、呈现次序维度经 ORDX、占据维度经 OCCX、时间维度经 AGEX、
货币维度经 PRICEX、**概率维度经本轮 PROBX** 证明**均无越线点
存在**：归因面按构造零响应，propensity 面最坏单价 0.924 ms ×
生产通量 0 = 零位移，触带需为不存在的调用方凭空造出 ≈38 调用/
报告 × 100× 体量）；或（c）指出「重命名 / 亚带 / 逐位违例 / 结构
交换负优化 / once-per-fault 拒绝」之外的第六类成本——二十七遍
枚举未再发现。

## 4. 重命名检查（派单强制项）

- **PROBX vs PRICEX（R26-C，直接前驱 C 轴）**：PRICEX 动**货币
  数值**（estimatedCostUsd/costUsd 的量级/平局/方向几何），机制是
  比较/线性查找/平局仲裁控制流，宿主是 r1 price-reduce 池与
  propensity 的 costUsd 死字段；PROBX 冻结全部货币坐标（**costUsd
  逐剂量对象级断言钉死在 PRICEX 型锚梯**），动的是**概率值对
  (μ, π)**，机制是重要性权重比值算术 + 支持/越界闩锁 + ESS 等价类
  + 检测器布尔，宿主是 propensity 三消费函数。两轴互为冻结补集
  （PRICEX A′ 面钉死 (μ, π) 剂量 costUsd；PROBX 钉死 costUsd 剂量
  (μ, π)）——双向正交见证。对象、宿主池、机制均不同。非重命名，
  PRICEX 未重测（零价格剂量触碰）。
- **PROBX vs AGEX（R25-C）/ SCALEX（R20-C）/ YMIX（R21-C）/
  SEEDX（R22-C）/ ORDX（R23-C）/ OCCX（R24-C）**：六轴全部作用在
  **归因面观测行多重集**上（时间 / 体量 / 结局组成 / 重采样身份 /
  呈现次序 / 格胞占据）；PROBX 的剂量对象在 propensity 行上，归因
  面经 A 面证明**按类型无处安放概率坐标**——观测行零移动（本轮
  夹具与计数 8,966/3,586,400/…/201 恒等复核）。体量天花板探针
  N=20,000 是**池天花板算术**（单形态、无 α 标定、无跨体量决斗）
  不是 SCALEX 型体量剂量。非重命名，六轴未重测。
- **PROBX vs FITQ（R19-C）/ ITERX（R16-C）**：两者对象都是**归因面
  IRLS 池**（拟合粒度墙钟分布 / 迭代计数普查）；PROBX 的宿主是
  propensity 消费池（不同池、不同函数），A 面恰好证明概率坐标在
  归因面类型级缺席。FITQ 重开条件（bootstrap 抽取数/拟合规模位移
  ≥1 个数量级）未被触发：本轮 options 全程默认。非重命名。
- **PROBX vs R27-A scalar numeric value-class face（运行中快进新增
  禁移植项）**：R27-A 的对象是**单枚标量数值字段「装哪一个
  double」的值类价格普查**，宿主是 A 区 tracking 消费面的四类数值
  消费操作（stringify 数字臂 / toFixed / 算术腿 / 整数插值），响应
  变量是每标量格式化/算术单价；PROBX 的对象是 **(μ, π) 二元联合
  几何**，宿主是 C 区 propensity 消费函数，响应变量是**控制流计数
  （rangeFails/violations 闩锁、pushes 权重装载、every/some 短路
  深度）、ESS 等价类（Cauchy–Schwarz 内点 / 塌缩 / 下溢归零 /
  Inf/Inf-NaN 传播）与检测器/验证器布尔语义**——估计器语义响应，
  不是每标量值类单价普查。次正规每操作成本类在本轮仅作为联合比值
  的池内后果出现（8.2 vs 0.7–1.3 µs，µs 域亚带 ×4,000）并按池
  算术定价，未按值类普查口径重铸；剂量路径内零数字→字符串移动
  （C 面故障腿 toFixed 系 once-per-fault REJX 类单独定价）。切片、
  宿主、机制、响应变量四重不同。非移植、非重命名。
- **PROBX vs R27-B batch-cardinality axis bottom + per-call
  batch-invariant prelude intercept face（运行中第二次快进新增禁
  移植项）**：R27-B 的对象是 **B 区 live 面批基数 N 轴的底部**
  （N ∈ {0,1,2,3,5,8,16} 扫描）与**每调用批不变前奏截距**（仿射
  分解出的 per-call 固定功），坐标是**基数**、机制是截距/斜率
  分离；PROBX 冻结体量（**N=200 恒等断言逐剂量**），坐标是
  **(μ, π) 值对**、机制是重要性权重几何与分支流——零 N 底部扫描、
  零截距分解（体量天花板探针是 N=20,000 单形态池算术，无 N 底部
  格、无仿射拟合、无 per-call 前奏分离）。切片（B live vs C
  offline propensity）、坐标（基数 vs 值）、机制（截距分解 vs
  估计器语义响应）均不同。非移植。
- **PROBX vs R27-D policy-content tree-geometry face（运行中第三次
  快进新增禁移植项）**：R27-D 的对象是 **D 区 adaptation 面策略
  内容的树几何**（内容树的深度/分叉/悬崖腿形态）；PROBX 的剂量
  对象是**平坦日志行上的一对 double**——零树结构、零嵌套深度剂量
  （PropensityLogEntry 为单层记录，八剂量共享同一构建字面形）、
  零 adaptation 面触碰。切片、宿主、坐标类型（树形态 vs 标量值对）
  均不同。非移植。
- **PROBX vs VALCLASS（R24-J，派单点名禁重铸项）**：VALCLASS 的
  对象是 **J 区持久面偏好值的联合类型类组成**（string/int/double/
  bool/mix），机制是 dtoa/parse 序列化单价；PROBX 全程零序列化、
  全剂量为固定类型内存 double，响应是控制流/ESS 等价类。非重铸。
- **PROBX vs R25-A value-domain / string content-class census（派单
  禁移植项）**：R25-A 的轴对象是**字符串字节内容类**（其数值字段
  全程钉死生产值）；PROBX 零字符串字节移动（episodeHash/modelId
  冻结，frozen-coordinates 逐剂量断言），动的是 double 值对。恰为
  互补面（R25-A 定字符串内容类、R27-A 定标量数值类、PROBX 定 C 区
  概率对联合几何），三者对象域两两无交。非移植。
- **PROBX vs R26-F input storage-state face / R26-H consumer-stage
  operand-realm face（H 区撞轴降级教训在案）**：R26-F 的对象是
  输入容器的**存储态**（打包/字典模式等），R26-H 的对象是消费容器
  的 **V8 原生 realm**；PROBX 全部剂量共享同一构建路径（同一
  Array.from 字面形——存储态冻结）、同 realm 原生数组与普通对象
  （零 vm/foreign-realm 操作数），只动值。两坐标均为本轴冻结面。
  非移植，且未重蹈 H 首铸撞 R26-F 的覆辙（轴对象为值几何非容器
  形态）。
- **PROBX vs R26-A collation-locale face / R26-I argv surface-form
  face / R25-I process-environment coordinate**：PROBX 剂量路径内
  零 localeCompare、零 argv、零 process env 触碰（数值比较全为
  `<`/`>`/`===`，正则仅验证器 claims 腿且 claims 冻结空数组）。
  非移植。
- **PROBX vs R26-B privacy constraint-leg face / R26-D injection-seam
  default-arm face / R26-G edge condition-kind face / R24-I
  adaptation-state coordinate / M=1 catalog collapse 等他区禁项**：
  宿主与机制均在他区（live 资格面 / 注入缝 / 边条件 / 配置态 /
  目录基数）；PROBX 零资格腿、零注入缝（三消费函数无时钟无 id 生成
  无默认臂）、零 config 剂量（DEFAULT_REPORT_VALIDATION 常量原样、
  bandit/shadow config 冻结——划界读数用 greedy 常量 config 只证
  宿主池 ns 域）、M 无关。非移植。
- **PROBX vs S1-C-8 / X3-5（propensity 站点既有 ID）**：两者是
  propensity 面**代码变换**裁决（遍数常数域）；PROBX 是**输入轴
  定价**，零道立起零决斗重跑——其候选道形族（归约融合 / some+filter
  合一 / memo 化）在 §2 被三重拒绝：µs 域深亚带宿主池 + 零生产
  通量 + 行为/语义违例。计数见证（PSTAT/FSTAT 六计数）是新剂量点
  的打印，不重测任何已关变换。非重命名。
- **PROBX vs REJX（R18-C）**：REJX 是拒绝终点 once-per-fault 定价
  轴；PROBX 的 C 面故障腿与 oob/viol 闩锁腿**引用**该拒绝类做类
  判定（两枚新剂量点打印 0.028–0.112 µs），不重测 31 终点、不
  重铸轴。非重命名。
- **PROBX vs `TaskFeatures.contextTokens/outputTokens` 与 bandit
  reward / shadow 预算数值**：四者是**探索/config 面数值**（bandit
  声明零读点字段与配置面）；PROBX 明示只剂量化 **propensity 行与
  验证器报告面**的数值坐标，探索面以宿主池划界读数（selectArm
  244–255 ns / recordReward 165–179 ns，S1-C-9/X4-2 域）封顶不
  另立轴（§2 枚举收口段诚实划界）——恰为避开 R24-I
  adaptation-state coordinate（配置态面，派单禁移植）的裁决域。
- **APCX/FLOORX 重测 vs S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 / CMB
  禁令**：本轮重测系**派单强制天花板复测**（池四读数 46.1–46.9
  全部在 R26-C 带内故带维持），产出仅用于天花板差值算术与 sink
  逐位核对；不重跑任何已关 ID 决斗、不构建堆叠候选、不产出可落地
  形态，四个成员 ID 与 CMB 禁令全部维持原状。
- **换名再提交拒列（本轮核实）**：零。R13-C..R26-C 二十六个无名
  关闭（含 TAILG/REJX/FITQ/SCALEX/YMIX/SEEDX/ORDX/OCCX/AGEX/
  PRICEX）未重探、未补铸、未换址重提；S10-C-1..3 / S12-C-1 / RID /
  CNT 零重提；ITERX/COLDX/AAFLR/GCAX 四轴未补铸；嵌套序 × 布局
  组合空间未触碰；X2-1 重写维持排除（S7-C 的 0/1 形态即落地形态，
  未另起平行实现）；COTARG / VALCLASS / CALLB / SHAPEK / KFAN /
  JB / RUNLIM / CKMIX / TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE /
  CG / RTL / DELINV 与全部他区无名轴（R24-B/D/F/H/I、R25-A/B/D/
  F/G/H/I、R26-A/B/D/F/G/H/I 十九条 + **R27-A / R27-B / R27-D 新增
  三条**）零移植。

## 5. 测试与验证

环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0）、pnpm 10.17.1、
`pnpm install --frozen-lockfile`。生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录）
npx tsx /tmp/r27c/r27c-anchor.mts           # ✓ 6,538 项逐位 × 3 次正典运行全绿
                                            #   （53 例 ×{仪表道,落地道} 忠实性电池
                                            #    + 3 seed × 2 自确定性 + perf 夹具逐位
                                            #    + PROBX B 面八 (μ,π) 剂量：冻结坐标断言/剂量
                                            #      + 仪表复刻道 vs 生产导入 9 字段逐位
                                            #      + PSTAT/FSTAT 六计数预注册全命中
                                            #      + ESS 等价类/supportOk/invalid-marker/
                                            #        检测器布尔/验证器 reasons 预注册断言
                                            #      + 生产 ×3 自确定性/剂量
                                            #    + A′ 面 10 死字段剂量 × 三消费函数逐位等锚
                                            #    + C 面 ESS/version 活腿 + 四死数值五剂量逐位
                                            #    + 体量天花板探针 + 探索面划界读数
                                            #    + APCX/FLOORX 天花板重测每次运行内嵌）
                                            #   另有 1 次预演运行同样 6,538 项全绿
                                            #   （预演与正典脚本逐字节相同，零改动）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 658.9 ms（1.12× vs 冻结 S6-C ctl 738.3）；r1c 锚点：
# production 663.6 ms（2.30× vs 参考 1527.7）——与 §1 生产正典中位带
# （660.6–665.4）互证。
```

代表性一次测量彩排全量输出（正典 run A；run B/C 与预演数字见 §1
池表与 §2）：

```text
scenario 1 (two-lane bitwise fidelity, 53 cases x {instrumented, plain-landed} vs production fitLogitAdditive import)
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)
  shape small (3 models x 3 projects, 120 rows): production median 32.9 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 660.6 ms/report (7 reps)
  perf reps: 667.7 659.9 660.0 661.4 660.3 660.6 671.3
instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):
  total                 663.2 ms
  solveSymmetric        450.8 ms  (68.0%)  [8966 calls]
  irls minus solve      152.6 ms  (23.0%)  [8966 iterations, 3586400 row visits, 1249652 irls sigmoids]
  onProbabilitiesFor    6.8 ms  (1.0%)  [80400 sigmoid+dot]
  APC (all sweeps)      46.4 ms  (7.0%)  [277606 active pairs, 12259 calls]
  bootstrap sampling    1.8 ms  (0.3%)
  sort/percentile tail  1.5 ms  (0.2%)  [12200 draws-map ops]
  base-row projection   0.004 ms  (once/report)
  outside pools (build/supports/keys/base copies) 3.3 ms  (0.5%)
  fits 201  draws successful=200 collapsed=0 singular=0
probe PROBX (policy-probability coordinate / importance-weight geometry dose response; NEW axis, not a lane):
  face A (attribution/posterior/R1 faces): the probability coordinate is ABSENT BY TYPE (OfflineRow / OutcomeObservation / R1Input carry no policy-probability field; rg census in report) — no in-contract dose exists; closes by construction
  face B (live face — the slice's only probability consumers): eight (mu, pi) joint-geometry doses, N=200 frozen, costUsd frozen at the PRICEX anchor vector, ids/order/utility/estimator frozen:
    computeOverlapDiagnostics wall (N=200, per call): onehot:0.72us uniform:1.32us hetero:1.23us spike:1.26us wsubn:8.16us inf:8.13us oob:0.89us viol:0.73us
    isFabricatedPositiveSupport wall (N=200, per call): onehot:1.24us uniform:0.47us hetero:0.36us spike:0.75us wsubn:0.74us inf:0.36us oob:0.17us viol:0.33us
    validateCounterfactualReport wall (per call; fault legs are once-per-fault REJX class): onehot:0.135us uniform:0.017us hetero:0.017us spike:0.273us wsubn:0.081us inf:0.018us oob:0.117us viol:0.233us
  face A' (declared-dead completion): observedUtility + deprecated `propensity` — five value doses each, all outputs bitwise identical to the onehot anchor:
    (10 doses x {diagnostics, validation, fabrication-detector} all bitwise-identical to anchor)
  face C (validator numeric-input face): reportVersion + effectiveSampleSize live legs; dead numerics witnessed:
    validator legs (per call): pass=0.145us ess-fault=0.108us version-fault=0.028us (fault legs once-per-fault)
  volume-ceiling probe (N=20,000 = 100x anchor, per call): onehot:0.106ms uniform:0.250ms wsubn:0.911ms inf:0.924ms
    band arithmetic: worst shape 0.924 ms/call -> 37.9 calls/report needed to reach the 35 ms band; production flux is ZERO calls/report (no src/ caller)
  exploration-face pool ceiling (demarcation support): selectArm greedy M=10 250 ns/call
  exploration-face pool ceiling (demarcation support): recordReward M=10 172 ns/call
probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) 0.276 ms/fit -> x201 fits = 55.6 ms/report (base-shape approximation; sink=7.309)
probe FLOORX: cheapest bitwise-legal sweep 0.172 ms/fit -> x201 fits = 34.6 ms/report floor; APC combination ceiling on THIS VM = 21.0 ms (floorSink=7.309)
APC pool median this run: 46.4 ms (R26-C band 46.0-47.3; re-anchor per R14-C rule only if outside)

ALL EQUIVALENCE CHECKS PASSED (6538 bitwise checks)
```

其余两次正典运行与一次预演的关键数字：

```text
run B: prod median 665.4 (small 32.3; perf reps 668.3 665.7 665.4 662.0 662.6 667.4 663.5);
       pools solve 453.9 / irls-other 152.5 / APC 46.9;
       PROBX diag walls: onehot 0.82 / uniform 1.33 / hetero 1.25 / spike 1.71 /
       wsubn 8.25 / inf 8.22 / oob 0.89 / viol 0.75 us;
       validator legs pass 0.142 / ess-fault 0.112 / version-fault 0.028 us;
       ceiling probe worst 0.915 ms [wsubn] -> 38.2 calls/report; selectArm 244 ns / recordReward 179 ns;
       APCX 59.2 / FLOORX 40.7 / ceiling 18.5
run C: prod median 664.8 (small 32.5; perf reps 667.6 662.9 662.4 665.5 662.9 664.8 675.1);
       pools solve 454.1 / irls-other 150.7 / APC 46.1;
       PROBX diag walls: onehot 0.73 / uniform 1.32 / hetero 1.24 / spike 1.24 /
       wsubn 8.16 / inf 8.19 / oob 0.89 / viol 0.74 us;
       validator legs pass 0.154 / ess-fault 0.108 / version-fault 0.029 us;
       ceiling probe worst 0.916 ms [inf] -> 38.2 calls/report; selectArm 255 ns / recordReward 165 ns;
       APCX 53.9 / FLOORX 34.0 / ceiling 19.9
预演 run 0: prod median 668.4 (perf reps 666.1-673.5; small 32.2); pools solve 454.2 /
       irls-other 150.8 / APC 46.8; PROBX diag walls onehot 0.73 - inf 8.18 us 同构;
       validator legs 0.142/0.107/0.029 us; ceiling probe worst 0.895 ms [wsubn] -> 39.1 calls;
       selectArm 250 ns / recordReward 171 ns; APCX 53.8 / FLOORX 38.0 / ceiling 15.7;
       6,538 项全绿
PROBX 结构量（四运行逐位一致）: pushes {20,200,200,200,200,200,100,20} /
       rangeFails {0,0,0,0,0,0,200,0} / violations {0,0,0,0,0,0,100,20}
       (onehot/uniform/hetero/spike/wsubn/inf/oob/viol 序);
       ESS 等价类: ===20 / ===200 / CS-内点 / (1,1.001) 塌缩 / ===0 下溢 /
       NaN (Inf/Inf) / ~100 / ===0 全零权重 — 预注册全命中;
       supportOk {T,T,T,T,T,T,F,F}; 检测器 {F,T,T,T,T,T,F,F} 含短路扫描计数
       (onehot 2/1/200, uniform 200/200/0, oob 1/200/0 等) 逐剂量精确;
       验证器 reasons {0,0,0,1,1,0,1,2}; NaN/Inf ESS 穿过 < min 门 (语义事实, 非候选);
       A' 面 10 死字段剂量与 C 面四死数值五剂量全部逐位等锚 (每运行);
       复刻道 vs 生产道 9 字段逐位 + 生产 ×3 自确定性每剂量全绿
APC 天花板重测（每次运行内嵌 × 4）: APCX 53.8/55.6/59.2/53.9; FLOORX 38.0/34.6/40.7/34.0;
       ceiling 15.7/21.0/18.5/19.9 (全部 < 35; sink=7.309 与 floorSink=7.309
       八读数全同，且与 R14-C/R16-C..R26-C 逐位同值);
       APC 池四读数 46.1-46.9 全部在 R26-C 带 46.0-47.3 内 ⇒ 带维持
全部站点计数（8,966 / 3,586,400 / 1,249,652 / 277,606 / 12,259 / 12,200 /
80,400 / 201）在四次运行逐位一致（确定性计数）。
```

## 6. 新增排除 ID：无

第二十七组候选集为空。从未点名站点普查结果为零（连续第九遍；R18-C
「最后一个从未点名位置」判定经 R19-C..R26-C 与本轮九遍核实），PROBX
为策略概率坐标 / 重要性权重几何剂量响应轴（重开条件：
PropensityLogEntry 概率字段契约变更，或三消费函数被接进 ≥38 调用/
报告 × N≥20,000 的生产环路——属公开签名/接线面，不预期。概率维度
本身**无越线点**：归因面按类型缺席（合同内剂量不存在，按构造关闭），
live 面八 (μ, π) 剂量 µs 域直测封顶（最坏次正规几何 8.25 µs/call @
N=200）、100× 体量天花板 0.924 ms/call × **生产通量 0 调用/报告 =
零位移**，死字段面 10 剂量逐位见证，验证器面活腿亚 µs + 死数值逐位
见证；切片公开输入面至此全数值坐标定价完毕——观测行六维（SCALEX
体量 / YMIX 组成 / SEEDX 身份 / ORDX 次序 / OCCX 占据 / AGEX 时间）
+ 决策面货币维（PRICEX）+ **propensity 行概率对与验证器报告面全
数值腿（本轮 PROBX）**），APCX/FLOORX 为按规天花板重测（池 46.1–46.9
四读数全部在 R26-C 带 46.0–47.3 内 ⇒ 带维持；天花板 15.7–21.0 < 35
稳定亚带，sink 逐位同值）。

## 附录：测量彩排脚本（完整，可复现；sha256 `f9627edf13d24c7398785cbee00c98d3748965a9c7243402efad4579b2d4ecb7`）

### 主彩排 `/tmp/r27c/r27c-anchor.mts`

（四次运行——1 预演 + 3 正典——所用脚本逐字节相同，预演后零改动。）

```ts
/**
 * Round 27 / R27-C measurement re-anchor + twenty-seventh-group closure probes
 * (offline routing slice, twenty-seventh pass). NOT committed — the twenty-
 * seventh-group enumeration carries no candidate lanes that reach the
 * acceptance bar; full source goes into the R27-C report appendix
 * (R7-C..R26-C precedent).
 *
 * Discipline (same as R11-C..R26-C):
 *   1. re-anchor the post-S7-C profile on THIS VM: production median
 *      ms/report on the perf shape (400 rows, 6 models x 8 projects,
 *      bootstrap=200) and the small shape, x-checked by an INSTRUMENTED
 *      verbatim pipeline copy (timers/counters only; every float op, value,
 *      and order identical — validated bitwise against the production
 *      import over the full 53-case battery before its numbers are used);
 *   2. derive the per-pool cost table (solve / irls-other / on-prob / APC /
 *      sampling / tail / base-copy) plus site counts;
 *   3. PROBX (new completeness axis for C; policy-probability coordinate /
 *      importance-weight geometry dose response — the LAST never-dosed
 *      live-read numeric coordinate pair of the C-slice public input
 *      surface after R25-C closed the observation row (strings IDG-frozen,
 *      y YMIX, volume SCALEX, order ORDX, occupancy OCCX, resample identity
 *      SEEDX, time AGEX) and R26-C closed the money coordinate (PRICEX:
 *      estimatedCostUsd live on the R1 face, costUsd declared-dead on the
 *      propensity face). The propensity face's input row carries TWO MORE
 *      live-read numeric coordinates that no round ever moved:
 *      PropensityLogEntry.behaviorProbability (mu) and .targetProbability
 *      (pi) — every fixture that ever built propensity logs (R26-C PRICEX
 *      face A' included) pinned them to the deterministic one-hot live
 *      shape mu = pi = (i % 10 === 0 ? 1 : 0) and never counted them as a
 *      dimension. PROBX freezes volume (N=200 every dose), id strings,
 *      emission order, construction path (same Array.from — storage state
 *      frozen), costUsd (PRICEX anchor vector, asserted bitwise per dose),
 *      observedUtility, guardrailBreach, estimatorId, claims, and config,
 *      and moves ONLY the (mu, pi) value pair:
 *        face A (attribution/posterior/R1 faces): the probability
 *          coordinate is ABSENT BY TYPE — OfflineRow / OutcomeObservation /
 *          R1Input carry no policy-probability field (rg census in the
 *          report); no in-contract dose exists; closes by construction;
 *        face B (live face; the slice's ONLY probability consumers):
 *          computeOverlapDiagnostics + isFabricatedPositiveSupport — eight
 *          (mu, pi) joint-geometry doses {onehot(anchor, the documented
 *          production live shape), uniform, hetero, spike(importance-weight
 *          explosion / ESS collapse), wsubn(subnormal weights, sumSquares
 *          underflow), inf(division by subnormal -> Infinity -> NaN ESS),
 *          oob(out-of-range handled inputs), viol(one-hot support
 *          violation)} with preregistered branch/counter expectations,
 *          an instrumented verbatim replica (counters only) validated
 *          bitwise per dose, production self-determinism, and per-dose
 *          wall pricing at N=200 (>= 50x the repo's only callers) plus a
 *          100x volume-ceiling probe (N=20,000, ceiling arithmetic only —
 *          volume is NOT a dosed coordinate of this axis);
 *        face A' (declared-dead completion): observedUtility and the
 *          deprecated optional `propensity` field are declared on the
 *          public input row and never read by any slice function — five
 *          value doses each must leave all three consumers bitwise
 *          identical (extends the PRICEX costUsd witness to the whole
 *          row: every numeric field of PropensityLogEntry is now either
 *          dosed-live or witnessed-dead);
 *        face C (validator numeric-input face): CounterfactualReport
 *          .reportVersion and .diagnostics.effectiveSampleSize are the
 *          validator's own live-read numeric inputs (fault legs are
 *          once-per-fault REJX class; NaN/Infinity/subnormal value-class
 *          doses witness the comparison legs); totalActions /
 *          eligibleActions / minPropensity / maxPropensity are
 *          validator-dead numerics (five-dose bitwise witness).
 *      Exploration-face numerics (bandit reward, TaskFeatures token
 *      counts, epsilon/seed/budget config, the rng function face) are
 *      DEMARCATED, not dosed — different coordinate class (utility /
 *      config / function faces), host pool bandit/shadow is an ns-us
 *      domain (S1-C-9 / X4-2); a single pool-ceiling wall line supports
 *      the demarcation. Validator config (minEffectiveSampleSize /
 *      supportedReportVersion) stays frozen (config face, R24-I family).
 *   4. APCX/FLOORX ceiling retest (dispatch-mandated): standalone
 *      timer-free APC lane vs cheapest bitwise-legal floor lane; sink
 *      checksums must match R14-C/R16-C..R26-C bitwise (7.309).
 *
 * Every wall-clock number is read AFTER the instrumented lane passes the
 * full bitwise battery in the same process run. Node: v22.22.2 (nvm),
 * tsx loader, same flags as R10-C..R26-C.
 */
import type { OutcomeObservation } from "/workspace/src/routing/outcomes.js";
import type { ModelDescriptor } from "/workspace/src/routing/capability-registry.js";
import type { R0Decision } from "/workspace/src/routing/r0.js";
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.js";
import type { AttributionReport, OfflineRow } from "/workspace/src/routing/offline-types.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import {
  computeOverlapDiagnostics,
  isFabricatedPositiveSupport,
  validateCounterfactualReport,
} from "/workspace/src/routing/propensity.js";
import type { PropensityLogEntry } from "/workspace/src/routing/propensity.js";
import { buildR1ShadowReport } from "/workspace/src/routing/r1-shadow-report.js";
import type { FrozenR1ShadowEpisode, R1ShadowReportInput } from "/workspace/src/routing/r1-shadow-report.js";
import { routeR0 } from "/workspace/src/routing/r0.js";
import type { R0Config } from "/workspace/src/routing/r0.js";
import type { RouteRequest } from "/workspace/src/routing/policy.js";

/* --------------------------- shared constants --------------------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}

interface FitResult {
  readonly coefficients: readonly number[] | null;
}

type IrlsImpl = (
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
) => FitResult;

function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}

function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/* ------------- verbatim production pipeline building blocks -------------- *
 * Transcribed from src/routing/offline-logit.ts and src/routing/lin-alg.ts
 * at the 183df9b-landed state. The instrumented lane's ONLY additions are
 * STATS counter increments and performance.now() reads at call boundaries;
 * no float operation, operand, or ordering differs. The plain lane carries
 * no timers at all. Both lanes are validated bitwise against the production
 * import over the full 53-case battery before any number is reported. */

const STATS = {
  fits: 0,
  irlsMs: 0,
  irlsIterations: 0,
  solveCalls: 0,
  solveMs: 0,
  sigmoidIrls: 0,
  rowVisits: 0,
  onProbMs: 0,
  onProbSigmoids: 0,
  apcMs: 0,
  apcActivePairs: 0,
  apcCalls: 0,
  samplingMs: 0,
  drawsMapOps: 0,
  tailMs: 0,
  baseCopyMs: 0,
  totalMs: 0,
  collapsedDraws: 0,
  singularDraws: 0,
  successfulDraws: 0,
  reset(): void {
    this.fits = 0; this.irlsMs = 0; this.irlsIterations = 0; this.solveCalls = 0;
    this.solveMs = 0; this.sigmoidIrls = 0; this.rowVisits = 0; this.onProbMs = 0;
    this.onProbSigmoids = 0; this.apcMs = 0; this.apcActivePairs = 0;
    this.apcCalls = 0; this.samplingMs = 0; this.drawsMapOps = 0;
    this.tailMs = 0; this.baseCopyMs = 0; this.totalMs = 0;
    this.collapsedDraws = 0; this.singularDraws = 0; this.successfulDraws = 0;
  }
};

function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);

  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);

  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);

  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });

  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}

function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}

function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}

/* verbatim landed solveSymmetric (lin-alg.ts) — plain, no timers */
function solvePlain(a: readonly (readonly number[])[], b: readonly number[]): number[] | null {
  const n = b.length;
  if (a.length !== n || a.some((row) => row.length !== n)) {
    throw new DomainValidationError("solveSymmetric requires a square matrix matching b");
  }
  const m: number[][] = a.map((row) => [...row]);
  const x: number[] = [...b];
  const eps = 1e-12;

  for (let col = 0; col < n; col++) {
    let pivotRow = col;
    let pivotAbs = Math.abs(m[col]![col]!);
    for (let row = col + 1; row < n; row++) {
      const abs = Math.abs(m[row]![col]!);
      if (abs > pivotAbs) {
        pivotAbs = abs;
        pivotRow = row;
      }
    }
    if (pivotAbs < eps) return null;
    if (pivotRow !== col) {
      const tmp = m[col]!;
      m[col] = m[pivotRow]!;
      m[pivotRow] = tmp;
      const tb = x[col]!;
      x[col] = x[pivotRow]!;
      x[pivotRow] = tb;
    }
    const colArr = m[col]!;
    const pivot = colArr[col]!;
    const xCol = x[col]!;
    for (let row = col + 1; row < n; row++) {
      const rowArr = m[row]!;
      const factor = rowArr[col]! / pivot;
      if (factor === 0) continue;
      let k = col;
      const stop = n - 3;
      for (; k < stop; k += 4) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
        rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
        rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
      }
      for (; k < n; k++) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
      }
      x[row] = x[row]! - factor * xCol;
    }
  }

  const solution = new Array<number>(n).fill(0);
  for (let row = n - 1; row >= 0; row--) {
    const rowArr = m[row]!;
    let sum = x[row]!;
    for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
    const diag = rowArr[row]!;
    if (Math.abs(diag) < eps) return null;
    solution[row] = sum / diag;
  }
  for (const value of solution) {
    if (!Number.isFinite(value)) return null;
  }
  return solution;
}

/* The lanes differ ONLY in instrumentation (counters + timers). Verbatim
 * landed accumulation switch in both. */
function makeIrls(instrumented: boolean): IrlsImpl {
  return (design, rows, _vectors, supports, keys, keySpace, maxIter) => {
    const tIrls0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.fits += 1;
    const p = design.names.length;
    const n = rows.length;
    const eta = new Array<number>(n).fill(0);
    const mu = new Array<number>(n).fill(0);
    const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
    const xtwz: number[] = new Array<number>(p).fill(0);
    const stamp = new Int32Array(keySpace);
    const etaByKey = new Float64Array(keySpace);
    const muByKey = new Float64Array(keySpace);
    let beta = new Array<number>(p).fill(0);
    for (let iter = 0; iter < maxIter; iter++) {
      if (instrumented) {
        STATS.irlsIterations += 1;
        STATS.rowVisits += n;
      }
      const mark = iter + 1;
      for (let i = 0; i < n; i++) {
        const key = keys[i]!;
        if (stamp[key] !== mark) {
          stamp[key] = mark;
          if (instrumented) STATS.sigmoidIrls += 1;
          let value = 0;
          const active = supports[i]!;
          for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
          etaByKey[key] = value;
          muByKey[key] = sigmoid(value);
        }
        eta[i] = etaByKey[key]!;
        mu[i] = muByKey[key]!;
      }
      for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
      xtwz.fill(0);
      for (let i = 0; i < n; i++) {
        const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
        const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
        const wz = w * z;
        const active = supports[i]!;
        switch (active.length) {
          case 5: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            const a3 = active[3]!;
            const a4 = active[4]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
            xtwz[a3] = xtwz[a3]! + wz;
            const r3 = xtwx[a3]!;
            r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
            xtwz[a4] = xtwz[a4]! + wz;
            const r4 = xtwx[a4]!;
            r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
            break;
          }
          case 4: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            const a3 = active[3]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
            xtwz[a3] = xtwz[a3]! + wz;
            const r3 = xtwx[a3]!;
            r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
            break;
          }
          case 3: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
            break;
          }
          case 2: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
            break;
          }
          default: {
            for (let ai = 0; ai < active.length; ai++) {
              const a = active[ai]!;
              xtwz[a] = xtwz[a]! + wz;
              const rowA = xtwx[a]!;
              for (let bi = 0; bi < active.length; bi++) {
                const b = active[bi]!;
                rowA[b] = rowA[b]! + w;
              }
            }
          }
        }
      }
      for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
      let next: number[] | null;
      if (instrumented) {
        const tSolve0 = performance.now();
        next = solvePlain(xtwx, xtwz);
        STATS.solveMs += performance.now() - tSolve0;
        STATS.solveCalls += 1;
      } else {
        next = solvePlain(xtwx, xtwz);
      }
      if (next === null) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      const delta = next.map((value, index) => value - beta[index]!);
      const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
      beta = next;
      if (!beta.every(Number.isFinite)) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      if (l2 < TOL) break;
    }
    if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
    return { coefficients: beta };
  };
}

const irlsInstrumented = makeIrls(true);
const irlsPlain = makeIrls(false);

function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}

function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}

function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}

function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

/* betaQuantileLcb replica (posterior.ts verbatim) — keeps the replica lanes
 * self-contained exactly like R14-C..R23-C. */
function lnGamma(z: number): number {
  const g = 7;
  const c = [
    0.99999999999980993, 676.5203681218851, -1259.1392167224028, 771.32342877765313,
    -176.61502916214059, 12.507343278686905, -0.13857109526572012, 9.9843695780195716e-6,
    1.5056327351493116e-7
  ];
  if (z < 0.5) {
    return Math.log(Math.PI / Math.sin(Math.PI * z)) - lnGamma(1 - z);
  }
  const x = z - 1;
  let a = c[0]!;
  for (let i = 1; i < g + 2; i++) a += c[i]! / (x + i);
  const t = x + g + 0.5;
  return 0.5 * Math.log(2 * Math.PI) + (x + 0.5) * Math.log(t) - t + Math.log(a);
}

function lnBetaFunction(a: number, b: number): number {
  return lnGamma(a) + lnGamma(b) - lnGamma(a + b);
}

function betacf(a: number, b: number, x: number): number {
  const maxIt = 200;
  const eps = 3e-12;
  const qab = a + b;
  const qap = a + 1;
  const qam = a - 1;
  let c = 1;
  let d = 1 - (qab * x) / qap;
  if (Math.abs(d) < 1e-30) d = 1e-30;
  d = 1 / d;
  let h = d;
  for (let m = 1; m <= maxIt; m++) {
    const m2 = 2 * m;
    let aa = (m * (b - m) * x) / ((qam + m2) * (a + m2));
    d = 1 + aa * d;
    if (Math.abs(d) < 1e-30) d = 1e-30;
    c = 1 + aa / c;
    if (Math.abs(c) < 1e-30) c = 1e-30;
    d = 1 / d;
    h *= d * c;
    aa = (-(a + m) * (qab + m) * x) / ((a + m2) * (qap + m2));
    d = 1 + aa * d;
    if (Math.abs(d) < 1e-30) d = 1e-30;
    c = 1 + aa / c;
    if (Math.abs(c) < 1e-30) c = 1e-30;
    d = 1 / d;
    const del = d * c;
    h *= del;
    if (Math.abs(del - 1) < eps) break;
  }
  return h;
}

function regularizedIncompleteBeta(x: number, a: number, b: number, lnBeta: number): number {
  if (x <= 0) return 0;
  if (x >= 1) return 1;
  const prefix = Math.exp(a * Math.log(x) + b * Math.log(1 - x) - lnBeta);
  if (x < (a + 1) / (a + b + 2)) {
    return (prefix * betacf(a, b, x)) / a;
  }
  return 1 - (prefix * betacf(b, a, 1 - x)) / b;
}

function inverseRegularizedIncompleteBeta(p: number, a: number, b: number): number {
  const lnBeta = lnBetaFunction(a, b);
  let lo = 0;
  let hi = 1;
  for (let i = 0; i < 80; i++) {
    const mid = (lo + hi) / 2;
    if (regularizedIncompleteBeta(mid, a, b, lnBeta) > p) hi = mid;
    else lo = mid;
  }
  return (lo + hi) / 2;
}

function betaQuantileLcbReplica(posterior: { alpha: number; beta: number }, p = 0.05): number {
  if (!(p > 0 && p < 1)) return p <= 0 ? 0 : 1;
  if (posterior.alpha <= 0 || posterior.beta <= 0) return 0;
  return inverseRegularizedIncompleteBeta(p, posterior.alpha, posterior.beta);
}

function makeFit(irlsImpl: IrlsImpl, instrumented: boolean) {
  const onProb = (vectors: readonly number[][], coefficients: readonly number[]): number[] => {
    if (!instrumented) return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    const t0 = performance.now();
    const result = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    STATS.onProbMs += performance.now() - t0;
    STATS.onProbSigmoids += vectors.length;
    return result;
  };

  const apc = (
    design: Design,
    rows: readonly Row[],
    vectors: readonly number[][],
    coefficients: readonly number[],
    onProbabilities: readonly number[],
    column: string
  ): number => {
    const t0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.apcCalls += 1;
    let sum = 0;
    const columnIdx = design.columnIndex.get(column);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < rows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        if (instrumented) STATS.apcActivePairs += 1;
        const on = onProbabilities[i]!;
        const offVector = vectors[i]!.slice();
        offVector[columnIdx] = 0;
        const off = sigmoid(dot(coefficients, offVector));
        sum += on - off;
      }
    }
    const result = rows.length === 0 ? 0 : sum / rows.length;
    if (instrumented) STATS.apcMs += performance.now() - t0;
    return result;
  };

  const pushValue = (map: Map<string, number[]>, key: string, value: number): void => {
    if (instrumented) STATS.drawsMapOps += 1;
    const list = map.get(key);
    if (list === undefined) map.set(key, [value]);
    else list.push(value);
  };

  return function fit(
    rows: readonly OfflineRow[],
    options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
  ): AttributionReport {
    const tTotal0 = instrumented ? performance.now() : 0;
    const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
    const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
    const seed = options?.seed ?? SEED_DEFAULT;

    const tBase0 = instrumented ? performance.now() : 0;
    const baseRows: Row[] = rows.map((r) => ({
      scenarioId: r.scenarioId,
      modelVersion: r.modelVersion,
      projectId: r.projectId,
      y: r.y
    }));
    if (instrumented) STATS.baseCopyMs += performance.now() - tBase0;
    const effects: Array<{ name: string; point: number; lcb: number; ucb: number }> = [];

    if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
    }

    const design = buildDesign(baseRows);
    const vectors = baseRows.map((r) => design.build(r));
    const supports = computeSupports(vectors);
    const keys = canonicalRowKeys(baseRows);
    const fit = irlsImpl(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
    if (fit.coefficients === null) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
    }

    const onProbabilities = onProb(vectors, fit.coefficients);
    const pointEffects = new Map<string, number>();
    for (const name of design.names) {
      if (name === "intercept") continue;
      pointEffects.set(
        name,
        apc(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
      );
    }
    for (const ref of design.referenceLevels) {
      if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
        pointEffects.set(`${ref.factor}:${ref.name}`, 0);
      }
    }

    const random = rng(seed);
    const draws = new Map<string, number[]>();
    let successful = 0;
    for (let draw = 0; draw < bootstrapDraws; draw++) {
      const tSample0 = instrumented ? performance.now() : 0;
      const sample: Row[] = [];
      const sampleVectors: number[][] = [];
      const sampleSupports: number[][] = [];
      const sampleKeys: number[] = [];
      for (let i = 0; i < baseRows.length; i++) {
        const index = Math.floor(random() * baseRows.length);
        sample.push(baseRows[index]!);
        sampleVectors.push(vectors[index]!);
        sampleSupports.push(supports[index]!);
        sampleKeys.push(keys[index]!);
      }
      const collapsed = sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1);
      if (instrumented) STATS.samplingMs += performance.now() - tSample0;
      if (collapsed) {
        if (instrumented) STATS.collapsedDraws += 1;
        continue;
      }
      const bootFit = irlsImpl(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
      if (bootFit.coefficients === null) {
        if (instrumented) STATS.singularDraws += 1;
        continue;
      }
      successful += 1;
      if (instrumented) STATS.successfulDraws += 1;
      const sampleOnProbabilities = onProb(sampleVectors, bootFit.coefficients);
      for (const [name] of pointEffects.entries()) {
        const value = apc(
          design,
          sample,
          sampleVectors,
          bootFit.coefficients,
          sampleOnProbabilities,
          name
        );
        pushValue(draws, name, value);
      }
    }

    const tTail0 = instrumented ? performance.now() : 0;
    for (const [name, point] of pointEffects.entries()) {
      const values = draws.get(name) ?? [];
      if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
        return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
      }
      values.sort((a, b) => a - b);
      effects.push({
        name,
        point,
        lcb: percentile(values, 0.025),
        ucb: percentile(values, 0.975)
      });
    }

    const n = baseRows.length;
    const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
    const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
    const muLcb = betaQuantileLcbReplica(muPosterior, 0.05);
    const models = new Set(baseRows.map((r) => r.modelVersion)).size;
    const projects = new Set(baseRows.map((r) => r.projectId)).size;
    const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
    const containsZero = (e: { lcb: number; ucb: number }): boolean =>
      e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
    const interactionsFor = (prefix: string): Array<{ name: string; lcb: number; ucb: number; point: number }> =>
      effects.filter((e) => e.name.startsWith(`w:${prefix}`));

    let diagnosis: AttributionReport["diagnosis"] = "uncertain";
    const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
    if (
      effects.some(
        (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
      )
    ) {
      diagnosis = "model-problem";
    } else if (
      effects.some(
        (e) =>
          e.name.startsWith("v:") &&
          e.lcb < -ATTRIBUTION_EFFECT &&
          effects
            .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
            .every(containsZero)
      )
    ) {
      diagnosis = "project-problem";
    } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
      diagnosis = "interaction-only";
    } else if (scenarioHard) {
      diagnosis = "scenario-hard";
    }

    const report: AttributionReport = {
      estimator: "logit-additive",
      rowsUsed: baseRows.length,
      effects,
      diagnosis,
      reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
      writesActivePointer: false
    };
    if (instrumented) {
      STATS.tailMs += performance.now() - tTail0;
      STATS.totalMs += performance.now() - tTotal0;
    }
    return report;
  };
}

const fitInst = makeFit(irlsInstrumented, true);
const fitPlain = makeFit(irlsPlain, false);

/* ------------------------------ harness ------------------------------ */

let checksPassed = 0;
let failures = 0;

function check(label: string, ok: boolean): void {
  if (ok) checksPassed++;
  else {
    failures++;
    process.stderr.write(`FAIL ${label}\n`);
  }
}

function compareReports(label: string, expected: AttributionReport, actual: AttributionReport): void {
  check(`${label}.estimator`, expected.estimator === actual.estimator);
  check(`${label}.rowsUsed`, expected.rowsUsed === actual.rowsUsed);
  check(`${label}.diagnosis`, expected.diagnosis === actual.diagnosis);
  check(`${label}.reason`, expected.reason === actual.reason);
  check(`${label}.writesActivePointer`, expected.writesActivePointer === actual.writesActivePointer);
  check(`${label}.effects.length`, expected.effects.length === actual.effects.length);
  if (expected.effects.length === actual.effects.length) {
    for (let i = 0; i < expected.effects.length; i++) {
      const e = expected.effects[i]!;
      const g = actual.effects[i]!;
      check(`${label}.effects[${i}].name`, e.name === g.name);
      check(`${label}.effects[${i}].point`, Object.is(e.point, g.point));
      check(`${label}.effects[${i}].lcb`, Object.is(e.lcb, g.lcb));
      check(`${label}.effects[${i}].ucb`, Object.is(e.ucb, g.ucb));
    }
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}

function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRow[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function allUniqueRows(r: () => number, count: number): OfflineRow[] {
  const rows: OfflineRow[] = [];
  for (let i = 0; i < count; i++) {
    rows.push({
      scenarioId: `s${i % 2}`,
      modelVersion: `m${i}`,
      projectId: `p${i}`,
      y: r() < 0.6 ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function batteryCases(): Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];

  for (let i = 0; i < 40; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, {
      scenarios: 2,
      models: 3,
      projects: 3,
      rows: 60,
      passRate: 0.55,
      pipeInModel: true,
    }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  cases.push({
    rows: allUniqueRows(r, 36),
    options: { bootstrap: 40, seed: 23 },
  });
  {
    const oneKey: OfflineRow[] = [];
    for (let i = 0; i < 30; i++) {
      oneKey.push({
        scenarioId: "s0",
        modelVersion: "m0",
        projectId: "p0",
        y: i % 3 === 0 ? 0 : 1,
        occurredAtMs: 1_000 + i,
      });
    }
    cases.push({ rows: oneKey, options: { bootstrap: 40, seed: 31 } });
  }
  cases.push({
    rows: [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1, occurredAtMs: 1_000 },
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 0, occurredAtMs: 1_001 },
    ],
    options: { bootstrap: 30, seed: 5 },
  });
  return cases;
}

const out = (line: string): void => {
  process.stdout.write(`${line}\n`);
};

function perfRows(): OfflineRow[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, {
    scenarios: 4,
    models: 6,
    projects: 8,
    rows: 400,
    passRate: 0.6,
  });
}

const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------- scenario 1: two-lane bitwise fidelity battery --------- */

function scenarioEquivalence(): void {
  const cases = batteryCases();
  for (const [index, testCase] of cases.entries()) {
    const expected = fitLogitAdditive(testCase.rows, testCase.options);
    compareReports(`R27C-inst[${index}]`, expected, fitInst(testCase.rows, testCase.options));
    compareReports(`R27C-plain[${index}]`, expected, fitPlain(testCase.rows, testCase.options));
  }
  out(
    `scenario 1 (two-lane bitwise fidelity, ${cases.length} cases x {instrumented, plain-landed} ` +
      `vs production fitLogitAdditive import)`
  );
}

/* -------- scenario 2: production self-determinism across 3 seeds -------- */

function scenarioDeterminism(): void {
  const r = fixtureRng(0xd19c);
  const rows = randomRows(r, { scenarios: 3, models: 4, projects: 5, rows: 150, passRate: 0.6 });
  for (const seed of [101, 202, 303]) {
    const first = fitLogitAdditive(rows, { bootstrap: 60, seed });
    const second = fitLogitAdditive(rows, { bootstrap: 60, seed });
    compareReports(`R27C-det[seed=${seed}]`, first, second);
  }
  out("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)");
}

/* ------------- scenario 3: profile re-anchor + pool table ------------- */

let APC_POOL_MEDIAN = 0;

interface PoolReading {
  totalMs: number;
  irlsMs: number;
  solveMs: number;
  onProbMs: number;
  apcMs: number;
  samplingMs: number;
  tailMs: number;
  baseCopyMs: number;
  fits: number;
  irlsIterations: number;
  solveCalls: number;
  sigmoidIrls: number;
  rowVisits: number;
  onProbSigmoids: number;
  apcActivePairs: number;
  apcCalls: number;
  drawsMapOps: number;
  collapsedDraws: number;
  singularDraws: number;
  successfulDraws: number;
}

function snapshotStats(): PoolReading {
  return {
    totalMs: STATS.totalMs, irlsMs: STATS.irlsMs, solveMs: STATS.solveMs,
    onProbMs: STATS.onProbMs, apcMs: STATS.apcMs, samplingMs: STATS.samplingMs,
    tailMs: STATS.tailMs, baseCopyMs: STATS.baseCopyMs,
    fits: STATS.fits, irlsIterations: STATS.irlsIterations, solveCalls: STATS.solveCalls,
    sigmoidIrls: STATS.sigmoidIrls, rowVisits: STATS.rowVisits,
    onProbSigmoids: STATS.onProbSigmoids, apcActivePairs: STATS.apcActivePairs,
    apcCalls: STATS.apcCalls, drawsMapOps: STATS.drawsMapOps,
    collapsedDraws: STATS.collapsedDraws, singularDraws: STATS.singularDraws,
    successfulDraws: STATS.successfulDraws,
  };
}

function scenarioProfile(): void {
  const smallRows = randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
  {
    const times: number[] = [];
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(smallRows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape small (3 models x 3 projects, 120 rows): production median ${median(times).toFixed(1)} ms/report (3 reps)`);
  }

  {
    const rows = perfRows();
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape perf (6 models x 8 projects, 400 rows): production median ${median(times).toFixed(1)} ms/report (7 reps)`);
    out(`  perf reps: ${times.map((t) => t.toFixed(1)).join(" ")}`);
  }

  // Instrumented pool table on the perf shape (3 runs; medians reported).
  const rows = perfRows();
  const expected = fitLogitAdditive(rows, PERF_OPTIONS);
  const runs: PoolReading[] = [];
  for (let rep = 0; rep < 3; rep++) {
    STATS.reset();
    const actual = fitInst(rows, PERF_OPTIONS);
    if (rep === 0) {
      compareReports("R27C-perf-fixture", expected, actual);
    }
    runs.push(snapshotStats());
  }
  const med = (key: keyof PoolReading): number => median(runs.map((r) => r[key]));
  const total = med("totalMs");
  const solveMs = med("solveMs");
  const irlsOther = med("irlsMs") - solveMs;
  const rest = total - med("irlsMs") - med("onProbMs") - med("apcMs") - med("samplingMs") - med("tailMs");
  APC_POOL_MEDIAN = med("apcMs");
  out(`instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):`);
  out(`  total                 ${total.toFixed(1)} ms`);
  out(`  solveSymmetric        ${solveMs.toFixed(1)} ms  (${(100 * solveMs / total).toFixed(1)}%)  [${med("solveCalls")} calls]`);
  out(`  irls minus solve      ${irlsOther.toFixed(1)} ms  (${(100 * irlsOther / total).toFixed(1)}%)  [${med("irlsIterations")} iterations, ${med("rowVisits")} row visits, ${med("sigmoidIrls")} irls sigmoids]`);
  out(`  onProbabilitiesFor    ${med("onProbMs").toFixed(1)} ms  (${(100 * med("onProbMs") / total).toFixed(1)}%)  [${med("onProbSigmoids")} sigmoid+dot]`);
  out(`  APC (all sweeps)      ${med("apcMs").toFixed(1)} ms  (${(100 * med("apcMs") / total).toFixed(1)}%)  [${med("apcActivePairs")} active pairs, ${med("apcCalls")} calls]`);
  out(`  bootstrap sampling    ${med("samplingMs").toFixed(1)} ms  (${(100 * med("samplingMs") / total).toFixed(1)}%)`);
  out(`  sort/percentile tail  ${med("tailMs").toFixed(1)} ms  (${(100 * med("tailMs") / total).toFixed(1)}%)  [${med("drawsMapOps")} draws-map ops]`);
  out(`  base-row projection   ${med("baseCopyMs").toFixed(3)} ms  (once/report)`);
  out(`  outside pools (build/supports/keys/base copies) ${rest.toFixed(1)} ms  (${(100 * rest / total).toFixed(1)}%)`);
  out(`  fits ${med("fits")}  draws successful=${med("successfulDraws")} collapsed=${med("collapsedDraws")} singular=${med("singularDraws")}`);
}
/* --------- scenario 4: PROBX policy-probability coordinate dose response --- *
 * NEW completeness axis (C-first; the last never-dosed live-read numeric
 * coordinate pair of the C-slice public input surface). Face A closes by
 * type-level absence (no policy-probability field exists on OfflineRow /
 * OutcomeObservation / R1Input — rg census in the report); face B doses the
 * (mu, pi) joint geometry against the slice's only probability consumers
 * (computeOverlapDiagnostics / isFabricatedPositiveSupport) with an
 * instrumented verbatim replica, preregistered branch/counter expectations,
 * and per-dose pricing; face A' witnesses the two remaining declared-dead
 * numerics (observedUtility, deprecated `propensity`) bitwise; face C doses
 * the validator's own numeric inputs (reportVersion, effectiveSampleSize)
 * and witnesses its dead numerics. No identifier string moves; no count,
 * order, occupancy, seed, time, money, options, or config movement; the
 * costUsd vector is asserted frozen at the PRICEX anchor at every dose. */

import { createBanditState, recordReward, selectArm } from "/workspace/src/routing/bandit.js";
import type { TaskFeatures } from "/workspace/src/routing/bandit.js";

const N_PROB = 200;

type Diag = ReturnType<typeof computeOverlapDiagnostics>;
type Report = Parameters<typeof validateCounterfactualReport>[0];

function mkProbLogs(
  mu: (i: number) => number,
  pi: (i: number) => number,
  n: number = N_PROB,
  extra?: { observedUtility?: (i: number) => number | undefined; propensity?: (i: number) => number }
): PropensityLogEntry[] {
  return Array.from({ length: n }, (_, i) => ({
    episodeHash: `ep-${i}`,
    modelId: `model-${i % 10}`,
    behaviorProbability: mu(i),
    targetProbability: pi(i),
    observedUtility: extra?.observedUtility !== undefined ? extra.observedUtility(i) : i % 2 === 0 ? 1 : 0,
    costUsd: 0.01 * (i + 1), // PRICEX anchor vector — frozen at every PROBX dose
    guardrailBreach: false,
    ...(extra?.propensity !== undefined ? { propensity: extra.propensity(i) } : {}),
  }));
}

/* instrumented verbatim replica of computeOverlapDiagnostics — counters only;
 * every float op, operand, and order identical to src/routing/propensity.ts */
const PSTAT = {
  scanned: 0,
  rangeFails: 0,
  violations: 0,
  pushes: 0,
  reset(): void {
    this.scanned = 0; this.rangeFails = 0; this.violations = 0; this.pushes = 0;
  },
};

function behaviorMuR(entry: PropensityLogEntry): number {
  return entry.behaviorProbability;
}

function targetPiR(entry: PropensityLogEntry): number {
  return entry.targetProbability;
}

function computeOverlapDiagnosticsReplica(
  logs: readonly PropensityLogEntry[],
  estimatorId: "paired" | "ips" | "snips" | "dr" = "snips"
): Diag {
  if (logs.length === 0) {
    return {
      totalActions: 0,
      eligibleActions: 0,
      minPropensity: 0,
      maxPropensity: 0,
      supportOk: false,
      effectiveSampleSize: 0,
      estimatorId,
      invalidReason: "INVALID_ESTIMATE",
    };
  }
  let min = Number.POSITIVE_INFINITY;
  let max = Number.NEGATIVE_INFINITY;
  let overlapOk = true;
  const weights: number[] = [];
  for (const log of logs) {
    PSTAT.scanned++;
    const mu = behaviorMuR(log);
    const pi = targetPiR(log);
    min = Math.min(min, mu);
    max = Math.max(max, mu);
    if (mu < 0 || mu > 1 || pi < 0 || pi > 1) {
      PSTAT.rangeFails++;
      overlapOk = false;
    }
    if (pi > 0 && mu <= 0) {
      PSTAT.violations++;
      overlapOk = false;
    }
    if (mu > 0) {
      PSTAT.pushes++;
      weights.push(pi / mu);
    }
  }
  const sum = weights.reduce((acc, w) => acc + w, 0);
  const sumSquares = weights.reduce((acc, w) => acc + w * w, 0);
  const effectiveSampleSize = sumSquares > 0 ? (sum * sum) / sumSquares : 0;
  const supportOk = overlapOk;
  return {
    totalActions: logs.length,
    eligibleActions: logs.length,
    minPropensity: min,
    maxPropensity: max,
    supportOk,
    effectiveSampleSize,
    estimatorId,
    ...(supportOk ? {} : { invalidReason: "INVALID_ESTIMATE" as const }),
  };
}

/* instrumented verbatim replica of isFabricatedPositiveSupport — counters only */
const FSTAT = {
  everyScans: 0,
  someScans: 0,
  filterScans: 0,
  reset(): void {
    this.everyScans = 0; this.someScans = 0; this.filterScans = 0;
  },
};

function isFabricatedPositiveSupportReplica(logs: readonly PropensityLogEntry[]): boolean {
  if (logs.length < 2) return false;
  const allStrictlyPositive = logs.every((row) => {
    FSTAT.everyScans++;
    return behaviorMuR(row) > 0 && behaviorMuR(row) <= 1;
  });
  const notOneHot =
    !logs.some((row) => {
      FSTAT.someScans++;
      return behaviorMuR(row) === 1;
    }) ||
    logs.filter((row) => {
      FSTAT.filterScans++;
      return behaviorMuR(row) === 1;
    }).length !== 1;
  return allStrictlyPositive && notOneHot;
}

function compareDiag(label: string, expected: Diag, actual: Diag): void {
  check(`${label}.totalActions`, expected.totalActions === actual.totalActions);
  check(`${label}.eligibleActions`, expected.eligibleActions === actual.eligibleActions);
  check(`${label}.minPropensity`, Object.is(expected.minPropensity, actual.minPropensity));
  check(`${label}.maxPropensity`, Object.is(expected.maxPropensity, actual.maxPropensity));
  check(`${label}.supportOk`, expected.supportOk === actual.supportOk);
  check(`${label}.ess`, Object.is(expected.effectiveSampleSize, actual.effectiveSampleSize));
  check(`${label}.estimatorId`, expected.estimatorId === actual.estimatorId);
  check(`${label}.invalidReason`, expected.invalidReason === actual.invalidReason);
  check(
    `${label}.invalidReason-own-property`,
    Object.prototype.hasOwnProperty.call(expected, "invalidReason") ===
      Object.prototype.hasOwnProperty.call(actual, "invalidReason")
  );
}

interface ProbDose {
  readonly name: string;
  readonly mu: (i: number) => number;
  readonly pi: (i: number) => number;
  readonly expPushes: number;
  readonly expRangeFails: number;
  readonly expViolations: number;
  readonly expSupportOk: boolean;
  readonly essCheck: (ess: number) => boolean;
  readonly essClass: string;
  readonly expMin: number;
  readonly expMax: number;
  readonly expFab: boolean;
  readonly expEvery: number;
  readonly expSome: number;
  readonly expFilter: number;
  readonly expReasons: number;
  readonly expValid: boolean;
  readonly expStatus: "ok" | "INVALID_ESTIMATE";
}

const PROB_DOSES: readonly ProbDose[] = [
  {
    name: "onehot", // anchor: documented deterministic-live shape; the shape every historical fixture pinned
    mu: (i) => (i % 10 === 0 ? 1 : 0),
    pi: (i) => (i % 10 === 0 ? 1 : 0),
    expPushes: 20, expRangeFails: 0, expViolations: 0, expSupportOk: true,
    essCheck: (e) => e === 20, essClass: "===20",
    expMin: 0, expMax: 1,
    expFab: false, expEvery: 2, expSome: 1, expFilter: 200,
    expReasons: 0, expValid: true, expStatus: "ok",
  },
  {
    name: "uniform",
    mu: () => 0.1,
    pi: () => 0.1,
    expPushes: 200, expRangeFails: 0, expViolations: 0, expSupportOk: true,
    essCheck: (e) => e === 200, essClass: "===200",
    expMin: 0.1, expMax: 0.1,
    expFab: true, expEvery: 200, expSome: 200, expFilter: 0,
    expReasons: 0, expValid: true, expStatus: "ok",
  },
  {
    name: "hetero",
    mu: (i) => (i % 5 === 0 ? 0.9 : 0.3),
    pi: (i) => (i % 3 === 0 ? 0.6 : 0.2),
    expPushes: 200, expRangeFails: 0, expViolations: 0, expSupportOk: true,
    essCheck: (e) => e > 0 && e < 200, essClass: "0<ess<200 (Cauchy-Schwarz strict)",
    expMin: 0.3, expMax: 0.9,
    expFab: true, expEvery: 200, expSome: 200, expFilter: 0,
    expReasons: 0, expValid: true, expStatus: "ok",
  },
  {
    name: "spike", // importance-weight explosion: single mu=1e-6 entry collapses ESS to ~1
    mu: (i) => (i === 0 ? 1e-6 : 1),
    pi: () => 1,
    expPushes: 200, expRangeFails: 0, expViolations: 0, expSupportOk: true,
    essCheck: (e) => e > 1 && e < 1.001, essClass: "1<ess<1.001 (ESS collapse)",
    expMin: 1e-6, expMax: 1,
    expFab: true, expEvery: 200, expSome: 2, expFilter: 200,
    expReasons: 1, expValid: false, expStatus: "INVALID_ESTIMATE",
  },
  {
    name: "wsubn", // subnormal weights: sumSquares underflows to 0 -> ess=0 branch
    mu: () => 1,
    pi: () => 5e-324,
    expPushes: 200, expRangeFails: 0, expViolations: 0, expSupportOk: true,
    essCheck: (e) => e === 0, essClass: "===0 (sumSquares underflow branch)",
    expMin: 1, expMax: 1,
    expFab: true, expEvery: 200, expSome: 1, expFilter: 200,
    expReasons: 1, expValid: false, expStatus: "INVALID_ESTIMATE",
  },
  {
    name: "inf", // division by subnormal -> Infinity weights -> Inf/Inf = NaN ESS
    mu: () => 5e-324,
    pi: () => 1,
    expPushes: 200, expRangeFails: 0, expViolations: 0, expSupportOk: true,
    essCheck: (e) => Number.isNaN(e), essClass: "NaN (Inf/Inf propagation)",
    expMin: 5e-324, expMax: 5e-324,
    expFab: true, expEvery: 200, expSome: 200, expFilter: 0,
    expReasons: 0, expValid: true, expStatus: "ok",
  },
  {
    name: "oob", // out-of-range handled inputs: range latch + violation legs
    mu: (i) => (i % 2 === 0 ? 1.5 : -0.25),
    pi: () => 0.5,
    expPushes: 100, expRangeFails: 200, expViolations: 100, expSupportOk: false,
    essCheck: (e) => Math.abs(e - 100) < 1e-9, essClass: "~100 (uniform weights over half)",
    expMin: -0.25, expMax: 1.5,
    expFab: false, expEvery: 1, expSome: 200, expFilter: 0,
    expReasons: 1, expValid: false, expStatus: "INVALID_ESTIMATE",
  },
  {
    name: "viol", // one-hot support violation: target mass on unselected arm
    mu: (i) => (i % 10 === 0 ? 1 : 0),
    pi: (i) => (i % 10 === 1 ? 1 : 0),
    expPushes: 20, expRangeFails: 0, expViolations: 20, expSupportOk: false,
    essCheck: (e) => e === 0, essClass: "===0 (all pushed weights are 0)",
    expMin: 0, expMax: 1,
    expFab: false, expEvery: 2, expSome: 1, expFilter: 200,
    expReasons: 2, expValid: false, expStatus: "INVALID_ESTIMATE",
  },
];

function reportFor(diag: Diag): Report {
  return {
    reportVersion: 1,
    candidate: "cand",
    baseline: "base",
    claims: [],
    diagnostics: diag,
  };
}

function scenarioProbxLive(): void {
  out(`probe PROBX (policy-probability coordinate / importance-weight geometry dose response; NEW axis, not a lane):`);
  out(
    `  face A (attribution/posterior/R1 faces): the probability coordinate is ABSENT BY TYPE ` +
      `(OfflineRow / OutcomeObservation / R1Input carry no policy-probability field; rg census in report) ` +
      `— no in-contract dose exists; closes by construction`
  );
  out(
    `  face B (live face — the slice's only probability consumers): eight (mu, pi) joint-geometry doses, ` +
      `N=200 frozen, costUsd frozen at the PRICEX anchor vector, ids/order/utility/estimator frozen:`
  );
  const anchorLogs = mkProbLogs(PROB_DOSES[0]!.mu, PROB_DOSES[0]!.pi);
  for (const dose of PROB_DOSES) {
    const logs = mkProbLogs(dose.mu, dose.pi);
    // frozen-coordinate assertions (everything but the (mu, pi) pair)
    check(`R27C-probx[${dose.name}].n-frozen`, logs.length === N_PROB);
    check(
      `R27C-probx[${dose.name}].frozen-coordinates`,
      logs.every(
        (l, i) =>
          l.episodeHash === anchorLogs[i]!.episodeHash &&
          l.modelId === anchorLogs[i]!.modelId &&
          Object.is(l.costUsd, anchorLogs[i]!.costUsd) &&
          Object.is(l.observedUtility, anchorLogs[i]!.observedUtility) &&
          l.guardrailBreach === anchorLogs[i]!.guardrailBreach &&
          !Object.prototype.hasOwnProperty.call(l, "propensity")
      )
    );
    // production output + instrumented replica bitwise + counters
    const prod = computeOverlapDiagnostics(logs);
    PSTAT.reset();
    const repl = computeOverlapDiagnosticsReplica(logs);
    compareDiag(`R27C-probx[${dose.name}].replica`, prod, repl);
    check(`R27C-probx[${dose.name}].scanned`, PSTAT.scanned === N_PROB);
    check(`R27C-probx[${dose.name}].pushes`, PSTAT.pushes === dose.expPushes);
    check(`R27C-probx[${dose.name}].rangeFails`, PSTAT.rangeFails === dose.expRangeFails);
    check(`R27C-probx[${dose.name}].violations`, PSTAT.violations === dose.expViolations);
    // preregistered structural expectations
    check(`R27C-probx[${dose.name}].supportOk`, prod.supportOk === dose.expSupportOk);
    check(`R27C-probx[${dose.name}].ess-class(${dose.essClass})`, dose.essCheck(prod.effectiveSampleSize));
    check(`R27C-probx[${dose.name}].min`, Object.is(prod.minPropensity, dose.expMin));
    check(`R27C-probx[${dose.name}].max`, Object.is(prod.maxPropensity, dose.expMax));
    check(
      `R27C-probx[${dose.name}].invalid-marker`,
      dose.expSupportOk
        ? prod.invalidReason === undefined && !Object.prototype.hasOwnProperty.call(prod, "invalidReason")
        : prod.invalidReason === "INVALID_ESTIMATE"
    );
    // production self-determinism (3 calls bitwise)
    compareDiag(`R27C-probx[${dose.name}].det2`, prod, computeOverlapDiagnostics(logs));
    compareDiag(`R27C-probx[${dose.name}].det3`, prod, computeOverlapDiagnostics(logs));
    // validator response on this dose's diagnostics (claims frozen empty)
    const validation = validateCounterfactualReport(reportFor(prod));
    check(`R27C-probx[${dose.name}].reasons`, validation.reasons.length === dose.expReasons);
    check(`R27C-probx[${dose.name}].valid`, validation.valid === dose.expValid);
    check(`R27C-probx[${dose.name}].status`, validation.status === dose.expStatus);
    // fabrication detector: replica counters + production agreement
    FSTAT.reset();
    const fabRepl = isFabricatedPositiveSupportReplica(logs);
    const fabProd = isFabricatedPositiveSupport(logs);
    check(`R27C-probx[${dose.name}].fab`, fabProd === dose.expFab && fabRepl === fabProd);
    check(`R27C-probx[${dose.name}].fab-every`, FSTAT.everyScans === dose.expEvery);
    check(`R27C-probx[${dose.name}].fab-some`, FSTAT.someScans === dose.expSome);
    check(`R27C-probx[${dose.name}].fab-filter`, FSTAT.filterScans === dose.expFilter);
  }
  // per-dose wall pricing: whole consumer pool at N=200 (>= 50x the repo's
  // only callers, which are unit tests with N<=4; zero src/ production callers)
  const diagLines: string[] = [];
  const fabLines: string[] = [];
  const valLines: string[] = [];
  for (const dose of PROB_DOSES) {
    const logs = mkProbLogs(dose.mu, dose.pi);
    const report = reportFor(computeOverlapDiagnostics(logs));
    {
      const times: number[] = [];
      for (let rep = 0; rep < 7; rep++) {
        const t0 = performance.now();
        for (let k = 0; k < 400; k++) computeOverlapDiagnostics(logs);
        times.push((performance.now() - t0) / 400);
      }
      diagLines.push(`${dose.name}:${(median(times) * 1000).toFixed(2)}us`);
    }
    {
      const times: number[] = [];
      for (let rep = 0; rep < 7; rep++) {
        const t0 = performance.now();
        for (let k = 0; k < 400; k++) isFabricatedPositiveSupport(logs);
        times.push((performance.now() - t0) / 400);
      }
      fabLines.push(`${dose.name}:${(median(times) * 1000).toFixed(2)}us`);
    }
    {
      const times: number[] = [];
      for (let rep = 0; rep < 7; rep++) {
        const t0 = performance.now();
        for (let k = 0; k < 2000; k++) validateCounterfactualReport(report);
        times.push((performance.now() - t0) / 2000);
      }
      valLines.push(`${dose.name}:${(median(times) * 1000).toFixed(3)}us`);
    }
  }
  out(`    computeOverlapDiagnostics wall (N=200, per call): ${diagLines.join(" ")}`);
  out(`    isFabricatedPositiveSupport wall (N=200, per call): ${fabLines.join(" ")}`);
  out(`    validateCounterfactualReport wall (per call; fault legs are once-per-fault REJX class): ${valLines.join(" ")}`);
}

/* face A': the two remaining declared-dead numerics of the propensity row.
 * With the whole (mu, pi) lattice at the onehot anchor, five value doses on
 * observedUtility and on the deprecated optional `propensity` field must
 * leave all three consumers bitwise identical (extends the PRICEX costUsd
 * zero-read witness to the whole row). */
function scenarioProbxDeadFields(): void {
  const anchor = PROB_DOSES[0]!;
  const anchorLogs = mkProbLogs(anchor.mu, anchor.pi);
  const anchorDiag = computeOverlapDiagnostics(anchorLogs);
  const anchorVal = validateCounterfactualReport(reportFor(anchorDiag));
  const anchorFab = isFabricatedPositiveSupport(anchorLogs);
  out(
    `  face A' (declared-dead completion): observedUtility + deprecated \`propensity\` — five value doses each, ` +
      `all outputs bitwise identical to the onehot anchor:`
  );
  const UTIL_DOSES: ReadonlyArray<{ name: string; util: (i: number) => number | undefined }> = [
    { name: "anchor", util: (i) => (i % 2 === 0 ? 1 : 0) },
    { name: "undef", util: () => undefined },
    { name: "huge", util: () => 1e12 },
    { name: "subn", util: () => 5e-324 },
    { name: "third", util: () => 1 / 3 },
  ];
  for (const dose of UTIL_DOSES) {
    const logs = mkProbLogs(anchor.mu, anchor.pi, N_PROB, { observedUtility: dose.util });
    const diag = computeOverlapDiagnostics(logs);
    compareDiag(`R27C-probxDead[util=${dose.name}]`, anchorDiag, diag);
    const val = validateCounterfactualReport(reportFor(diag));
    check(
      `R27C-probxDead[util=${dose.name}].validation`,
      val.valid === anchorVal.valid && val.status === anchorVal.status &&
        val.reasons.join("\u0000") === anchorVal.reasons.join("\u0000")
    );
    check(`R27C-probxDead[util=${dose.name}].fab`, isFabricatedPositiveSupport(logs) === anchorFab);
  }
  const PROP_DOSES: ReadonlyArray<{ name: string; prop: (i: number) => number }> = [
    { name: "zero", prop: () => 0 },
    { name: "one", prop: () => 1 },
    { name: "subn", prop: () => 5e-324 },
    { name: "half", prop: () => 0.5 },
    { name: "mirror", prop: (i) => (i % 10 === 0 ? 1 : 0) },
  ];
  for (const dose of PROP_DOSES) {
    const logs = mkProbLogs(anchor.mu, anchor.pi, N_PROB, { propensity: dose.prop });
    check(
      `R27C-probxDead[prop=${dose.name}].field-present`,
      logs.every((l) => Object.prototype.hasOwnProperty.call(l, "propensity"))
    );
    const diag = computeOverlapDiagnostics(logs);
    compareDiag(`R27C-probxDead[prop=${dose.name}]`, anchorDiag, diag);
    const val = validateCounterfactualReport(reportFor(diag));
    check(
      `R27C-probxDead[prop=${dose.name}].validation`,
      val.valid === anchorVal.valid && val.status === anchorVal.status &&
        val.reasons.join("\u0000") === anchorVal.reasons.join("\u0000")
    );
    check(`R27C-probxDead[prop=${dose.name}].fab`, isFabricatedPositiveSupport(logs) === anchorFab);
  }
  out(`    (10 doses x {diagnostics, validation, fabrication-detector} all bitwise-identical to anchor)`);
}

/* face C: the validator's own numeric inputs. effectiveSampleSize and
 * reportVersion are live-read; totalActions / eligibleActions /
 * minPropensity / maxPropensity are validator-dead (five-dose witness).
 * Fault legs (template literal + toFixed) are once-per-fault REJX class. */
function scenarioProbxValidator(): void {
  out(`  face C (validator numeric-input face): reportVersion + effectiveSampleSize live legs; dead numerics witnessed:`);
  const baseDiag: Diag = {
    totalActions: 4,
    eligibleActions: 4,
    minPropensity: 0.5,
    maxPropensity: 0.5,
    supportOk: true,
    effectiveSampleSize: 4,
    estimatorId: "snips",
    invalidReason: undefined,
  };
  const ESS_DOSES: ReadonlyArray<{ name: string; ess: number; expReasons: number; expValid: boolean; expStatus: "ok" | "INVALID_ESTIMATE" }> = [
    { name: "pass", ess: 4, expReasons: 0, expValid: true, expStatus: "ok" },
    { name: "below", ess: 0.4, expReasons: 1, expValid: false, expStatus: "INVALID_ESTIMATE" },
    { name: "subn", ess: 5e-324, expReasons: 1, expValid: false, expStatus: "INVALID_ESTIMATE" },
    { name: "nan", ess: Number.NaN, expReasons: 0, expValid: true, expStatus: "ok" },
    { name: "inf", ess: Number.POSITIVE_INFINITY, expReasons: 0, expValid: true, expStatus: "ok" },
  ];
  for (const dose of ESS_DOSES) {
    const val = validateCounterfactualReport(reportFor({ ...baseDiag, effectiveSampleSize: dose.ess }));
    check(
      `R27C-probxVal[ess=${dose.name}]`,
      val.reasons.length === dose.expReasons && val.valid === dose.expValid && val.status === dose.expStatus
    );
    const again = validateCounterfactualReport(reportFor({ ...baseDiag, effectiveSampleSize: dose.ess }));
    check(
      `R27C-probxVal[ess=${dose.name}].det`,
      again.valid === val.valid && again.status === val.status && again.reasons.join("\u0000") === val.reasons.join("\u0000")
    );
  }
  const VERSION_DOSES: ReadonlyArray<{ name: string; version: number; expReasons: number }> = [
    { name: "pass", version: 1, expReasons: 0 },
    { name: "mismatch", version: 99, expReasons: 1 },
    { name: "frac", version: 1.5, expReasons: 1 },
    { name: "nan", version: Number.NaN, expReasons: 1 },
  ];
  for (const dose of VERSION_DOSES) {
    const val = validateCounterfactualReport({ ...reportFor(baseDiag), reportVersion: dose.version });
    check(`R27C-probxVal[ver=${dose.name}]`, val.reasons.length === dose.expReasons && val.valid === (dose.expReasons === 0));
  }
  // validator-dead numerics: five doses each leave validation bitwise identical
  const anchorVal = validateCounterfactualReport(reportFor(baseDiag));
  const DEAD_DOSES: ReadonlyArray<{ name: string; patch: Partial<Diag> }> = [
    { name: "totalActions", patch: { totalActions: -5 } },
    { name: "eligibleActions", patch: { eligibleActions: 1e12 } },
    { name: "minPropensity", patch: { minPropensity: Number.NaN } },
    { name: "maxPropensity", patch: { maxPropensity: 5e-324 } },
    { name: "all-four", patch: { totalActions: 0, eligibleActions: -1, minPropensity: 1e300, maxPropensity: -1e300 } },
  ];
  for (const dose of DEAD_DOSES) {
    const val = validateCounterfactualReport(reportFor({ ...baseDiag, ...dose.patch }));
    check(
      `R27C-probxVal[dead=${dose.name}]`,
      val.valid === anchorVal.valid && val.status === anchorVal.status &&
        val.reasons.join("\u0000") === anchorVal.reasons.join("\u0000")
    );
  }
  // fault-leg pricing (once-per-fault class): pass leg vs ESS-fault leg vs version-fault leg
  const passReport = reportFor(baseDiag);
  const essFault = reportFor({ ...baseDiag, effectiveSampleSize: 0.4 });
  const verFault = { ...reportFor(baseDiag), reportVersion: 99 };
  const lane = (report: Report): number => {
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      for (let k = 0; k < 2000; k++) validateCounterfactualReport(report);
      times.push((performance.now() - t0) / 2000);
    }
    return median(times) * 1000;
  };
  out(
    `    validator legs (per call): pass=${lane(passReport).toFixed(3)}us ess-fault=${lane(essFault).toFixed(3)}us ` +
      `version-fault=${lane(verFault).toFixed(3)}us (fault legs once-per-fault)`
  );
}

/* volume-ceiling probe: N=20,000 (100x anchor) on the four worst shapes.
 * Volume is NOT a dosed coordinate of PROBX (the axis moves values only);
 * this is ceiling arithmetic for the pool, SCALEX-style pool bounding. */
function scenarioProbxCeiling(): void {
  const shapes: ReadonlyArray<{ name: string; dose: ProbDose }> = [
    { name: "onehot", dose: PROB_DOSES[0]! },
    { name: "uniform", dose: PROB_DOSES[1]! },
    { name: "wsubn", dose: PROB_DOSES[4]! },
    { name: "inf", dose: PROB_DOSES[5]! },
  ];
  const lines: string[] = [];
  let worst = 0;
  for (const shape of shapes) {
    const logs = mkProbLogs(shape.dose.mu, shape.dose.pi, 20_000);
    const times: number[] = [];
    for (let rep = 0; rep < 5; rep++) {
      const t0 = performance.now();
      for (let k = 0; k < 20; k++) computeOverlapDiagnostics(logs);
      times.push((performance.now() - t0) / 20);
    }
    const ms = median(times);
    worst = Math.max(worst, ms);
    lines.push(`${shape.name}:${ms.toFixed(3)}ms`);
  }
  out(`  volume-ceiling probe (N=20,000 = 100x anchor, per call): ${lines.join(" ")}`);
  out(
    `    band arithmetic: worst shape ${worst.toFixed(3)} ms/call -> ${(35 / worst).toFixed(1)} calls/report ` +
      `needed to reach the 35 ms band; production flux is ZERO calls/report (no src/ caller)`
  );
  // exploration-face demarcation support: the bandit/shadow pool is an ns-us
  // domain (S1-C-9 / X4-2); reward / token / epsilon / seed / rng coordinates
  // live there and are demarcated, not dosed.
  const arms = Array.from({ length: 10 }, (_, i) => `arm-${i}`);
  let state = createBanditState(arms);
  for (let i = 0; i < 10; i++) state = recordReward(state, `arm-${i}`, (i % 3) / 2);
  const features: TaskFeatures = {
    featureVersion: "v1",
    taskFamily: "fam0",
    role: "role0",
    contextTokens: 1_000,
    outputTokens: 200,
    capabilities: [],
  };
  const greedyConfig = { seed: 1, explorationBudget: 0, epsilon: 0 };
  const rngStub = (): number => 0.999;
  {
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      for (let k = 0; k < 20_000; k++) selectArm(state, greedyConfig, features, false, rngStub);
      times.push(((performance.now() - t0) / 20_000) * 1e6);
    }
    out(`  exploration-face pool ceiling (demarcation support): selectArm greedy M=10 ${median(times).toFixed(0)} ns/call`);
  }
  {
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      for (let k = 0; k < 4_000; k++) recordReward(state, "arm-3", 0.5);
      times.push(((performance.now() - t0) / 4_000) * 1e6);
    }
    out(`  exploration-face pool ceiling (demarcation support): recordReward M=10 ${median(times).toFixed(0)} ns/call`);
  }
}
function scenarioApcFloor(): void {
  const rows = perfRows();
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irlsPlain(design, baseRows, vectors, supports, keys, baseRows.length, MAX_ITER_DEFAULT);
  const coefficients = fit.coefficients!;
  const onProbabilities = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
  const names = design.names.filter((name) => name !== "intercept");
  let apcSink = 0;
  const sweepReps = 60;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tSweep0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perSweepMs = (performance.now() - tSweep0) / sweepReps;
  out(
    `probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) ` +
      `${perSweepMs.toFixed(3)} ms/fit -> x201 fits = ${(perSweepMs * 201).toFixed(1)} ms/report ` +
      `(base-shape approximation; sink=${apcSink.toFixed(3)})`
  );

  let floorSink = 0;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tFloor0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perFloorMs = (performance.now() - tFloor0) / sweepReps;
  out(
    `probe FLOORX: cheapest bitwise-legal sweep ${perFloorMs.toFixed(3)} ms/fit -> x201 fits = ` +
      `${(perFloorMs * 201).toFixed(1)} ms/report floor; APC combination ceiling on THIS VM = ` +
      `${(perSweepMs * 201 - perFloorMs * 201).toFixed(1)} ms (floorSink=${floorSink.toFixed(3)})`
  );
}
/* --------------------------------- main --------------------------------- */

scenarioEquivalence();
scenarioDeterminism();
scenarioProfile();
scenarioProbxLive();
scenarioProbxDeadFields();
scenarioProbxValidator();
scenarioProbxCeiling();
scenarioApcFloor();

out(`APC pool median this run: ${APC_POOL_MEDIAN.toFixed(1)} ms (R26-C band 46.0-47.3; re-anchor per R14-C rule only if outside)`);

if (failures > 0) {
  process.stderr.write(`${failures} CHECK(S) FAILED\n`);
  process.exitCode = 1;
} else {
  out(`\nALL EQUIVALENCE CHECKS PASSED (${checksPassed} bitwise checks)`);
}
```
