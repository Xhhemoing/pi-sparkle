MODEL_SLUG=claude-fable-5-thinking-xhigh

# R18-E：`src/learning/` 第十八遍复查报告（Round 18）

**战役:** 全库持久 SOTA 优化 Round 18 / R18-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `b1534b0`（含 R18-B 空枚举摄取与本派单）
**分支:** `cursor/r18-e-learning-eighteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第十八组新角度枚举为空（有理由的
空枚举，未铸造任何 S18-E-\* ID，与 R10-E..R17-E 同口径），切片维持关闭
（MORE_OPTIMA=no）。本轮首次补齐两条从未测过的完整性轴——(a) 冷层/
每进程一次定价轴：十七轮全部锚点只测过 JIT 温态稳态，而生产 CLI 每进程
只调各切片进入点一次；本轮在生产执行面（`dist/` 编译产物 + 裸 node）实测
首调价：进入点首调 107–1,047µs（首/温比 112–457×）、每进程冷合成
2.23–2.50ms（= 温态 141–163×，但仍距落地线 4.0–4.5×），切片 10 文件
边际 import 2.19–3.71ms（外依赖闭包 18.5–21.5ms 不属本切片）——冷层
全额上界 ~4.4–6.2ms/进程 < 10ms 落地线，且属派单明拒的 once-per-run
CLI 面；「温态锚点低估单发进程真实成本两个量级」的残余怀疑就此以实价
关闭。(b) 字符宽度/CJK 语料定价轴：R16-E B2 只测过 ASCII lorem 填充，
词表 CJK 臂（不行/错误/可以）与非 ASCII 扫描行为从未定价；本轮三站 ×
三语料 × 三尺寸实测：CJK 每字符价反而比 ASCII 便宜 2.4–3.1×，混合语料
最贵也仅 ASCII 的 1.18–1.36×，最坏斜率 13.61ns/char，触及 10ms 需单事件
文本 ≥~73.5 万字符（超生产载荷 3 个量级以上）——「非 ASCII 语料可能
病态慢」假说就此以实价关闭。**

1. **零漂移八重确认**：切片 10 个文件（`wc -l` 确认恰 10 文件、合计 1770
   行，与 R14-E..R17-E 逐字节相同）自 R1-E 裁决基线（`adb20d7`）至本轮
   基线（`b1534b0`）**逐字节未变**——`git diff adb20d7..HEAD --
   src/learning/` 为 0 行、`git log adb20d7..HEAD -- src/learning/` 为
   **0 提交**，十八轮全程无人触及该目录。且 R17-E 基线（`5d939af`）之后
   的全部提交（R17-E..J 摄取、Round 17 收口、Round 18 开轮、R18-A/B 摄取
   与 R18-C/D/E 派单）**仅触及 `docs/reports/sota-opt/`**——
   `git log 5d939af..HEAD --name-only` 非 docs 路径为零。R1-E..R17-E 的
   全部规模测量、调用面图景与 S1-E-1..8 / S2-E-1..7 / S3-E-1..5 /
   S4-E-1..3 / S5-E-1..5 / S6-E-1..5 / S7-E-1..5 / S8-E-1..3 / S9-E-1..3
   共 44 项切片排除裁决（R10-E..R17-E 未铸 ID，其空枚举收口一并继承，含
   R13-E 三处无名微观、R14-E 规模/JIT 双轴、R15-E GC/A-A 双轴、R16-E
   累积状态/载荷形态双轴、R17-E 异常路径/事件类构成双轴）原样成立、
   原样继承。
2. **本 VM 锚点/矩阵重锚（未拷贝 R17-E 数字，遵派单令；同 VM 双 Node
   版本交叉，延续 R13-E..R17-E 协议）**：SLICE-CPU 总量上界锚点 **16 次
   运行 + 1 次同场 A/A 复认**（8 × Node v22.14.0 + 8 × Node v22.22.2 +
   复认 1 × v22.22.2；engines `>=22.19.0` 仅后者合规）
   **12.4–14.8µs/run**（collect 7.6–9.3µs、outcomes 3.1–3.7µs、diagnose
   0.89–1.18µs、bandit-build 0.7–0.9µs；v22.14.0 12.4–14.2 / v22.22.2
   13.1–14.8 重叠带，版本差异不可分辨）——距落地线（≥10ms）
   **678–809×**，即使把切片 CPU 清零也远不达门槛。本轮带**低于** R17-E
   带（18.5–20.2）为纯夹具构成差：本轮种子构成 collected=8 / outcomes=3
   （历史 E 系最小），R17-E 为 collected=15 / outcomes=4。构成归因按
   R15-E 种子对调协议在本 VM 实证闭环：**12 次 R17-E 种子比对运行**
   （`0xe17e01/02/03` + R17-E NOW）复现 collected=15 / outcomes=4，
   其中静场 9 次（6 × v22.14.0 + 3 × v22.22.2）总量 **18.8–19.4µs——
   逐点落在 R17-E 带 18.5–20.2 之内**（collect 12.8–13.4 / outcomes
   4.0–4.3 与 R17-E 逐项同带）；早场 3 次 21.1–22.2µs 略超带（会话
   负载噪声，构成 VERDICT 行仍逐位一致）。同 VM 同场种子对调即复现
   带差全额 ⇒ 带差 100% 构成效应，与 R17-E 价格表（最贵类 result-\*
   1.0–1.2µs/事件 × collected 差 7）算术相符。配置态 live 面：
   `applyLearnedRouting` 非空策略 **268–314ns/task**（R13-E 258–312 /
   R14-E 256–303 / R15-E 258–298 / R16-E 269–294 / R17-E 257–290
   同带）、空策略 **116–140ns/task**（R15-E..R17-E 110–128 邻带）；
   `loadLearnedRouting` 全新 root 29.4–49.8µs、现实 registry
   **80.7–134.5µs**（R8-E 80–85 / R15-E..R17-E 74.3–89.1 同带偏上，
   I/O 抖动，亚 ms 一次性）。全部确定性 VERDICT 行 17 次运行**逐位
   一致**（`E=41 collected=8 routedE=41 outcomes=3 S=12 diagnoseGroups=1
   prevArms=3`），3/3 sanity 检查全过（§1、附录 A）。
3. **配置态 × 命令类矩阵复核无洞**：全部切片进入点行号逐条 rg 复核，
   **与 R17-E 记录逐一相同、无新增调用方、无新增命令类**
   （`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
   `run/flowchart-run:681/712`、`track/loop:88/172`；resume/replay 经
   `flowchartForSupervisor` @ `flowchart-run:880/1021` 各单次载入；
   `run --flowchart` @ `cli/main:580` 在 `:708` 载入前返回，无双载入）。
   S8-E-1 双载入按令保护，未做任何去重分析。切片外消费者复核与 R17-E
   逐一相同（`adaptation/eval-routing.ts:223` 仅
   `parseLearnedRoutingPolicy`、`track/primary-split.ts:3` 类型导入、
   `privacy/record-classes.ts:212/224` 文档字符串、
   `adaptation/approval-profile.ts:44` docstring 假阳性）。测试专用面
   （`patterns`/`attribution`/`signatures` + 五个测试专用导出）本轮
   交叉检索复核仍零生产调用方。
4. **本轮两条实扫新轴（§2 前置）**：(a) **冷层/每进程一次定价轴**
   （本切片首次；R3-E..R17-E 全部锚点在预热后测温态稳态，而生产 CLI
   每进程对 collect/outcomes/diagnose/bandit 各调一次、apply 调 ~10 次
   ——首调走 V8 解释层，从未定价）：6 次运行（两 Node 版本各 3 次，
   每次独立进程 = 独立冷样本），生产执行面 = `dist/` 编译 JS + 裸
   node（包 bin 即 `dist/cli/main.js`）。首调价：collect **893–1,047µs**
   （首/温 112–134×）、outcomes 817–890µs（253–283×）、diagnose
   194–227µs（172–238×）、bandit-build 156–306µs（206–409×）、apply
   107–124µs（365–457×，第二调即跌至 6.3–6.7µs）。按生产调形（四进入点
   各首调一次 + apply 首调+次调+8×次层）合成**每进程冷价 2,234–2,500µs
   = 温态 141–163×，但仍距落地线 4.0–4.5×**。import 面拆分：切片 10
   文件边际 import **2.19–3.71ms**，外依赖闭包 18.5–21.5ms 不属本切片
   ——冷层全额上界（边际 import + 冷合成）**~4.4–6.2ms/进程 < 10ms**，
   且整体属派单明拒的 once-per-run CLI 噪声面；可行减法（V8 code
   cache / snapshot / 惰性 import）全部是运行时基建或 X1-1 隐藏模块
   缓存族，不在切片职权。(b) **字符宽度/CJK 语料定价轴**（本切片首次；
   R16-E B2 只测 ASCII lorem 字节规模，`USER_NEGATIVE`/`USER_POSITIVE`/
   `PEER_NEGATIVE` 词表的 CJK 臂与非 ASCII 扫描从未定价）：三站
   （A：USER_ANSWER→`scoreUserAnswer` 双正则全扫+truncate；B：
   TASK_RESULT summary→truncate+完整 binding；C：子代理 assistant 文本
   →extractAssistant+PEER_NEGATIVE 未命中扫描+truncate）+ 直调未命中扫
   （S），× 三语料（ascii/cjk/mixed）× 三尺寸（1K/16K/128K 字符），
   6 次运行 54 项 sanity 全过：**ascii 斜率 9.12–11.36ns/char，cjk
   3.23–4.34ns/char（便宜 2.4–3.1×），mixed 12.23–13.61ns/char（最贵，
   ≤ascii 的 1.36×）**；最坏穿越点 T\*(10ms) = mixed 站 A ~**73.5–74.5
   万字符/事件**（cjk 站 S 最好 ~1,363–1,374 万字符）——距生产文本
   规模 3 个量级以上，且 CJK 方向为负（更便宜），无任何按宽度特化的
   收益位。
5. **第十八组新角度枚举为空（§2）**：本轮新透镜为「**执行层级 × 字符
   宽度**」（两条测量轴的代码侧对偶：逐函数问 (a) 哪些位点扛着不成
   比例的首调/冷层价、其任何重构是否等价且可能越线；(b) 哪些文本扫描
   位点对字符宽度敏感、其任何按宽度特化是否等价且可能越线）。对第十八
   遍全量重读的 10 个源文件逐函数普查：全部命中形态落在运行时基建
   辖区（V8 tiering/code cache）、派单明拒面（once-per-run CLI 噪声）、
   被点名排除保护（X1-1 隐藏模块缓存、S9-E-2 预过滤负优化、S12-D-1
   预探针族、S1-E-3/S2-E-5/S7-E-2 truncate 族、X0-2 行为变更）或落地线
   算术封死区（冷层全额 4.4–6.2ms、宽度穿越点 ≥73.5 万字符）。**未
   铸造任何 S18-E-\* 新 ID，无新增排除提议；S13-B-1 未以任何形态重提
   （`applyLearnedRouting` 内部零改动，重开条件 |avoid|≈965–1,063 维持
   未触发）；S8-E-1 未做去重分析；S9-E-2 未触碰；R13-E 三处无名微观
   未补铸；R14-E..R17-E 八轴未复铸。**
6. **验证面**：`pnpm gate` 在 engines 合规的 Node v22.22.2 上**全绿**
   （typecheck ✓ / lint ✓ / test **1168 pass / 0 fail / 1 skipped**（共
   1169 / 78 suites，与 R14-E..R17-E 计数逐一相同）/ build ✓）。切片
   相关套件 **48 tests / 2 suites / 48 pass / 0 fail**（v22.14.0 与
   v22.22.2 均过）。系统默认 node 仍为 v22.14.0（`/exec-daemon/node`
   PATH 前置遮蔽 nvm，违反 engines `>=22.19.0`，R12-E §0 起连续记录的
   同一环境事实）；gate 结论以 v22.22.2 为准。零代码改动下全部硬不变量
   天然满足。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与
双归因未动；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故
未提交任何 sim/锚点脚本（差异面与新轴脚本测量段进附录 A，遵守
R7-F/R8-C/R9-I/R10-E..R17-E 无赢家纪律）。本切片在其输出契约与数据面语义
下维持 SOTA——第十八遍复查确认：**剩余的全部 ms 级余量仍全部在被排除表
点名保护的 I/O 契约面、once-per-run CLI 面或切片外辖区上**，切片级收口
条件（R3-E §7 … R17-E §6）依然成立。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行）本轮第十八遍全量实际读码，未依赖前
  十七轮记忆。上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、
  `protocol/`、`privacy/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表 545 行，含全部
  S\*-E-\* 行与 S8-E-1 / S9-E-2 / S13-B-1）→ PROGRESS.md →
  round-18/PLAN.md → round-17/R17-E.md（空枚举；异常路径 + 事件类构成）
  → round-16/R16-E.md（空枚举；累积状态 + 载荷形态）→ round-15/R15-E.md
  （空枚举；GC/A-A）→ R14-E/R13-E（按需，透镜与先例核对）→ 10 个源
  文件。R18-C / R18-D 在其他分支在跑，未等待、未触碰其报告与分支。
- 基线漂移检查（§结论 1）：切片零提交零 diff；`5d939af..b1534b0` 全部
  提交仅触及 `docs/reports/sota-opt/`——前十七轮全部测量与裁决原样成立。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S17 时代条目。特别地：
  - **S13-B-1**（B 切片辖区）未以任何变体重提：本切片辖区只有被调方
    `applyLearnedRouting` 本体，本轮对其**零改动**（切片零 diff 下必然）；
    其重开条件（本体成本 ≥2 个量级，R16-E 量化为 |avoid| ≈ 965–1,063）
    未触发、生产写入方（≤M×8）不可触发；本轮冷层轴只测其首调价
    （107–124µs，一次性 tiering 成本），不触及本体结构。
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护，本轮仅复核其行号与频次
    （§1），未做任何去重分析。并发 promote 下的新鲜性仍是其保留理由。
  - **S9-E-2**（pass-2 预过滤负优化）未触碰：本轮宽度轴的「按语料预探针
    再扫描」形态在 §2.2 行 5 以 S9-E-2/S12-D-1 换名拒列，未做实验重开。
  - **R13-E 三处无名微观**（bandit `includes` 旁路已建 arms Set @
    `bandit-store:72`、apply avoided-Set filter+map 双中间数组 @
    `learned-routing:198-202`、`routingPolicyIdentity` 字面量再校验 @
    `learned-routing:43`）：位点本轮逐字复核未变（零 diff 下必然），
    维持 R13-E 裁决，**未补铸 ID、未重开**。
  - **R14-E 规模/JIT、R15-E GC/A-A、R16-E 累积状态/载荷形态、R17-E
    异常路径/事件类构成八轴未复铸**：在 src/test 零漂移下原样继承
    （派单明令勿复铸）；本轮两条新轴（执行层级、字符宽度）为不同测量
    维度，无交叠——R14-E JIT 轴测的是**温态优化健康度**（deopt 计数），
    本轮 A 轴测的是**首调解释层价格**（tier-up 之前）；R16-E B2 轴测的
    是**ASCII 字节规模斜率**，本轮 B 轴测的是**字符宽度类别的斜率差**
    （尺寸固定对照、宽度变化）。
  - R10-E..R17-E 未铸 S10..S17-E-\*，本轮亦未事后发明任何此类编号；
    R18-A / R18-B 空枚举（各自辖区）与本切片无交，仅继承 R18-B 的
    **语料宽度方法**作为 B 轴协议先例。
- **换名重提检查**：本轮新定位形态的换名检查全部并入 §2.2 表格逐行给出，
  并逐一对照 EXCLUSIONS 全表 + 十七轮 E 报告 + 八轮空枚举面（其换名裁决
  全部原样维持，不再复抄）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  稀疏 → 已批准 R0 基线；双 LCB / 双归因在位；晋升 proposal-first；无
  阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；fail-open / 隐藏
  模块缓存（X1-1）未引入；bandit/learned registry I/O 的 fsync 与完整性
  重哈希未触碰；corrupt registry fail-closed 未触碰；两遍
  `collectSignalsFromEvents` 语义未触碰；X0-3 维持；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未
  force-push；仅提交并推送本分支上的本报告一个文件。
- 环境：本 VM 系统默认 node v22.14.0（`/exec-daemon/node`，PATH 前置
  遮蔽 nvm），nvm 另装 v22.22.2（engines `>=22.19.0` 仅后者合规）。锚点
  与两条新轴在两个版本各测（§1/§2，同带或标注版本差）；`pnpm gate` 以
  v22.22.2 为准（§4）。依赖经 `pnpm install --frozen-lockfile` 安装
  （pnpm 10.17.1）；冷层轴运行前 `pnpm build` 产出 `dist/`（gate 内建
  build 同产物）。
- 冷层轴测量注意事项（首次成文，供后轮复用）：(a) 每进程只有一份首调
  样本，冷区间必须跨进程聚合（本轮每 Node 版本 3 个独立进程）；(b)
  夹具生成先于全部计时窗，生成器只预热生成器代码、不预热切片代码；
  (c) bandit 的 `previous` 状态必须手写字面量而非由温 `banditBuild`
  派生，否则首调窗已被隐式预热（本轮曾据此修正一次，见附录 A）；(d)
  collect 输出含 `nowIso()` 壁钟 `createdAt`，跨调等价断言须剥离该字段
  后比较（本轮曾据此修正一次假阴性，见附录 A）。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

R7-I 教训（默认态夹具可能掩盖配置态主路径）按令复查。src 自 `5d939af` 起
一行未动（且自 `d148d12` 起 src/test 累计零 diff 的 R15-E..R17-E 论证链
原样有效），故 R8-E..R17-E §1 矩阵表结构原样成立；本轮逐条 rg 复核全部
切片进入点行号，**与 R17-E 记录逐一相同、无新增调用方**：

| 命令类 × 配置态 | 切片进入点（本轮行号复核） | 频次与规模 |
| --- | --- | --- |
| `adapt auto --run` | `runAutoAdaptFromEvents` @ `cli/adapt:188` → `runAutoAdaptLoop` | 每进程一次，E≈41 |
| `adapt auto --project` | `runAutoAdaptLoop` @ `cli/adapt:205`（空事件表，S4-E-1 面） | 每进程一次 |
| `adapt learn --run` | `proposeRoutingFromRoutedEvents` @ `cli/adapt:168`（S2-E-2 面） | 每进程一次 |
| 普通 `run` | `loadLearnedRouting` @ `cli/main:708`（S2-I-1 已裁决） | 每进程一次 |
| `run --flowchart` | `startFlowchartRun` @ `cli/main:580` → `flowchartForSupervisor` @ `flowchart-run:747→712` 单次载入（在 `cli/main:708` 之前返回，无双载入） | 每进程一次 |
| `run --children` | `cli/main:708` 载入 → `applyLearnedRouting` @ `routing/assign:102` 每任务 → `startFlowchartRun` @ `cli/main:748` → `flowchart-run:747→712` 第二次载入（S8-E-1，勿去重）→ `applyLearnedRouting` @ `flowchart-run:681` 每节点 → `runAutoAdaptLoop` @ `cli/main:783` 一次 | 双载入各一次；任务/节点 ≤10/几十 |
| `track`（startTrackedRun） | `track/loop:88` 载入 → 每任务 `applyLearnedRouting` → `startFlowchartRun` @ `track/loop:152` → `flowchart-run:712` 第二次载入（同 S8-E-1）→ `runAutoAdaptLoop` @ `track/loop:172` 一次 | 同上 |
| flowchart resume / replay | `flowchartForSupervisor` @ `flowchart-run:880` / `:1021` 各单次载入 | 每恢复一次 |
| SPARKLE_AUTO_ADAPT=0 | 仍收集（persistSignals + updateProjectBandit 照付），仅跳过 propose；R5-E 裁决维持 | 不变 |
| 测试专用面 | `patterns` / `attribution` / `signatures` / `compareSignatures` / `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `parseObservedSignal`(直调) / `proposeRoutingFromAssignments`(deprecated) / `saveLearnedRouting`(deprecated, throw-only) / `learnedRoutingPath`(死导出) 交叉检索复核仍零生产调用方 | 不进热路径 |

切片外其余消费者以 `learning/` 模块路径精确检索复核：
`adaptation/eval-routing.ts:223` 仅调用 `parseLearnedRoutingPolicy`（离线
eval 面，D 切片）；`track/primary-split.ts:3` 仅导入类型；
`privacy/record-classes.ts:212/224` 仅文档字符串提及路径布局；
`adaptation/approval-profile.ts:44` 维持 R14-E 裁决的 docstring 假阳性
（「auto-loop never CAS-promotes」，非调用边）。**矩阵结论：无新洞，
消费者集合与 R17-E 逐一相同**。

SLICE-CPU 锚点本 VM 重测（**16 次运行 + 1 次同场 A/A 复认**：8 ×
Node v22.14.0 + 8 × Node v22.22.2 + 复认 1 × v22.22.2；seeds
`0xe18e01`（collect）/ `0xe18e02`（routed）/ `0xe18e03`（signals）——
R12-E..R17-E 拒绝采样同协议，三个生成器按序首选种子全部一次通过、零
出局，探针记录 seed→(collected,outcomes,diagnoseGroups)：01→(8,1,0)、
02→(13,3,3)、03→(18,2,1)；夹具镜像 R8-E..R17-E 生成器，R1-A 组成 E=41）：

```text
collect=7.6-9.3us  outcomes=3.1-3.7us  diagnose=0.89-1.18us  bandit-build=0.7-0.9us
total in-slice CPU ~12.4-14.8us per full auto-adapt run （两 Node 版本重叠带：
  v22.14.0 12.4-14.2 / v22.22.2 13.1-14.8，版本差异不可分辨）
vs landing bar >=10000us  ->  678-809x below EVEN IF ZEROED
带间比对：本轮带低于 R17-E 带 18.5-20.2 为纯构成差——本轮种子构成
  collected=8 / outcomes=3（历史 E 系最小；R14-E 15 / R15-E 21 / R16-E 17 /
  R17-E 15）。R15-E 种子对调协议在本 VM 实证闭环：12 次 R17-E 种子
  （0xe17e01/02/03 + R17-E NOW）比对运行复现 collected=15 / outcomes=4，
  静场 9 次总量 18.8-19.4us 逐点落在 R17-E 带内（collect 12.8-13.4 /
  outcomes 4.0-4.3 逐项同带；早场 3 次 21.1-22.2 略超带，会话负载噪声，
  构成 VERDICT 行 12/12 逐位一致）。同 VM 同场种子对调复现带差全额
  ⇒ 带差 100% 构成效应，与 R17-E 价格表算术相符（最贵类 result-*
  1.0-1.2us/事件 x collected 差 7 ≈ collect 差 4.5-5.5us + outcomes
  差 0.6-1.0us = 带差 5-6.5us）
configured-state: applyLearnedRouting(avoid=10, M=10) = 268-314ns/task （R13-E 258-312 /
                                                        R14-E 256-303 / R15-E 258-298 /
                                                        R16-E 269-294 / R17-E 257-290 同带）
                  applyLearnedRouting(空策略)        = 116-140ns/task （R15-E 110-125 /
                                                        R16-E 111-128 / R17-E 110-122 邻带）
                  loadLearnedRouting fresh(ENOENT)   = 29.4-49.8us/次
                  loadLearnedRouting 现实 registry    = 80.7-134.5us/次 （R8-E 80-85 /
                                                        R15-E..R17-E 74.3-89.1 同带偏上，
                                                        I/O 抖动，亚 ms 一次性，S8-E-1 带）
```

注：本轮夹具组成为 collected=8 / outcomes=3 / diagnoseGroups=1；diagnose
分组路径经 diagnoseGroups=1 全数走到。全部确定性 VERDICT 行 17 次运行
**逐位一致**，3/3 sanity 检查全过。**锚点复核成立：该切片不存在不推翻
既有排除就能达门槛的候选。**

## 2. 本轮两条实扫新轴 + 第十八组新角度枚举：有理由的空枚举

### 2.0 新轴 A：冷层/每进程一次定价（本切片首次）

R3-E..R17-E 十五轮锚点谱系全部在 JIT 预热后测温态稳态（`bench()` 先跑
一次再计时 N 万次）；而生产形态是**每 CLI 进程对每个切片进入点只调一次**
（apply 例外，~10 次/进程）——首调走 V8 解释层 + IC 冷启动，其价格从未
被标定，「温态锚点低估单发进程真实成本」是十七轮报告链上最后一个未定价
的残余怀疑。本轮在生产执行面定价：`dist/` 编译产物（包 bin 即
`dist/cli/main.js` 同产物）+ 裸 node（无 tsx 转译器）；每次脚本运行是
一个独立进程 = 一份独立冷样本，6 个进程（两 Node 版本各 3 个）：

```text
A1 首调/次调/次层/温态四层价（us；6 进程区间；夹具同锚点 0xe18e01/02/03）:
  collectSignalsFromEvents(E=41):  first=893-1,047  second=171-211  next8=29.7-31.5
                                   warm=7.63-7.98   first/warm=112-134x
  outcomesFromRoutedRun(E=41):     first=817-890    second=32.3-33.3 next8=17.7-18.2
                                   warm=3.12-3.32   first/warm=253-283x
  diagnoseModelProjectIssues(S=12):first=194-227    second=66.7-107.4 next8=6.9-8.9
                                   warm=0.94-1.18   first/warm=172-238x
  banditBuild(S=12,prevArms=3):    first=156-306    second=59.1-103.1 next8=6.5-8.3
                                   warm=0.74-0.76   first/warm=206-409x
  applyLearnedRouting(avoid=10):   first=107-124    second=6.3-6.7   next8=2.9-4.7
                                   warm=0.27-0.29   first/warm=365-457x（次调即跌 17x）
A2 每进程冷合成（生产调形：四进入点各首调一次 + apply 首调+次调+8x次层）:
  cold=2,234-2,500us vs warm=15.2-16.0us | cold/warm=141-163x
  vs landing bar >=10000us: 仍 4.0-4.5x below EVEN AT COLD TIER
A3 import 面拆分（先温外依赖闭包再量切片边际；6 进程）:
  外依赖闭包（adaptation/domain/routing/run/protocol/... 26 直接依赖模块
  的传递闭包）=18.5-21.5ms —— 不属本切片
  切片 10 文件边际 import=2.19-3.71ms（v22.22.2 2.19-2.65 / v22.14.0
  3.22-3.71；1770 源行的 parse+eval 份额）
A4 冷层全额上界 = 切片边际 import + 冷合成 ~ 4.4-6.2ms/进程 < 10ms 落地线
sanity: 4/4 x6 全过（冷/次调输出等价断言含 createdAt 剥离；夹具 VERDICT
  行与锚点逐位一致）
```

**裁决**：(a) **冷层价不产生候选**：冷合成 2.23–2.50ms 距落地线仍
4.0–4.5×，冷层全额上界 4.4–6.2ms 仍在线下；且该成本整体属派单验收标准
**明拒的 once-per-run CLI 噪声面**——即使算术越线也不具立项资格。(b)
**冷层减法全部不在切片职权**：首调贵在 V8 tiering（解释层执行 + IC/
反馈向量冷启动），不在切片算法——任何切片内重构（拆函数、改分派、
预热调用）都不能消除首次解释执行；真正的减法（V8 code cache /
startup snapshot / 编译产物预热）是运行时/部署基建，且「CLI 启动时
预热调用」形态是净负优化（温价照付 + 冷价照付）。惰性/延迟 import =
X1-1 隐藏模块缓存族；把 `learned-routing` 与 `adaptation/promotion`
解耦以缩闭包 = 公开契约重构（X0-4 族 + D 切片辖区）。(c) **残余怀疑
就此关闭**：温态锚点确实低估单发进程 141–163×——但诚实定价后冷层
仍不达线，且方向盘不在本切片手里。该轴把「一遍 CLI 的真实成本」从
推断变成了四层价格表（first/second/next8/warm），供后轮直接引用。

### 2.1 新轴 B：字符宽度/CJK 语料定价（本切片首次）

R16-E B2 只用 ASCII lorem 填充测过单事件字节规模斜率；切片词表的 CJK 臂
（`USER_NEGATIVE` 的 不行/错误、`USER_POSITIVE` 的 可以、`PEER_NEGATIVE`
同族）与正则/truncate 管线对非 ASCII 语料的扫描行为从未定价——「非
ASCII 语料可能病态慢（宽字符边界、Unicode 路径）」是载荷形态轴留下的
未测臂。本轮三站 × 三语料 × 三尺寸（语料单元精心避开全部词表关键词，
miss-scan 断言 `scoreUserAnswer` 返回 undefined）：

```text
B 斜率表（ns/char；6 次运行区间；站 A=USER_ANSWER 全管线、站 B=TASK_RESULT
  summary 全管线、站 C=子代理 assistant 文本全管线、站 S=scoreUserAnswer 直调
  未命中扫描；尺寸 1K/16K/128K 字符，斜率取 16K->128K 段）:
           站 A          站 B          站 C          站 S
  ascii    11.07-11.36   9.12-9.28     10.40-10.55   1.76-1.78
  cjk      4.07-4.34     3.23-3.49     3.52-3.67     0.73
  mixed    13.42-13.61   12.23-12.42   12.94-13.28   1.32-1.34
宽度比: cjk/ascii = 0.32-0.39（CJK 便宜 2.4-3.1x）
        mixed/ascii = 1.18-1.36（混合最贵，边界过渡密度效应）
穿越点 T*(10ms)（达 10ms 所需单事件字符数）:
  最坏 = mixed 站 A ~734,868-744,925 chars；全表最好 = cjk 站 S
  ~13,631,225-13,743,994 chars —— 全部距生产文本规模 3 个量级以上
  （且 TASK_RESULT summary 入库前 truncate(400)，USER_ANSWER 为人手输入）
sanity: 54 项/次 x6 全过（3 语料 x 3 尺寸 x 3 站产出断言 + miss-scan；
  站 A score=90 userAcceptance、站 B taskSuccess PASS score=90 且存储
  summary<=400、站 C peer score=70 恒定），确定性输出逐位一致
```

**裁决**：(a) **CJK 方向为负收益**：CJK 每字符价仅 ASCII 的 0.32–0.39×
——`\b`/`\w` 是 ASCII 类，CJK 文本提供的边界/交替候选位更少，扫描推进
更快；「非 ASCII 病态慢」假说被实测反向关闭，不存在收益位。(b) **混合
语料溢价不产生候选**：mixed 最贵也仅 ascii 的 1.18–1.36×（ASCII↔CJK
过渡各造一个 `\b` 候选位），绝对量 12.2–13.6ns/char，穿越点 ~73.5 万
字符/事件——生产不可达（summary 有 truncate(400) 上限，answer 为人手
输入，assistant 文本经 extractAssistant 单事件级处理）。(c) **按宽度
特化全部换名封死**：扫描前语料预探针（先测 charset 再选路径）=
S12-D-1 预探针族 + S9-E-2 预过滤负优化换名；先 truncate 再扫描 =
语义变更（打分对全文、truncate 仅存储面，X0-2 行为变更）；删词表
CJK 臂 = R7-E 已裁不等价（反例 `"x可以x"` 命中：ASCII `\w` 与 CJK
毗邻产生边界）——R17-E 跨职权观察原样维持，本轮为其补上语料价：
CJK 臂在场与否对扫描斜率的影响藏在交替表编译内，实测三语料斜率已
覆盖其上界。**该轴把载荷形态证明从「ASCII 字节规模」补全到「字符
宽度类别」，R16-E B2 + 本轮 B 合并覆盖字节 × 宽度全平面。**

### 2.2 第十八组枚举表

方法：本轮代码侧透镜「**执行层级 × 字符宽度**」（两条测量轴的对偶）：
对第十八遍全量重读的 10 个源文件逐函数问 (a) 哪些位点扛着不成比例的
首调/冷层价、其任何重构是否等价且可能越线；(b) 哪些文本扫描位点对
字符宽度敏感、其任何按宽度特化是否等价且可能越线；同时按惯例对本轮
逐字新点名的位置做换名检查（对照 EXCLUSIONS 全表 + 十七轮 E 报告 +
八轮空枚举面）。扫描结果：**零个可立项形态**，逐类裁决如下（均未
铸造新 ID）：

| 形态（本轮第十八遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **冷层重构**（拆小函数助解释层、collect 双遍拆分助 tier-up、分派树改形助 IC 预热——A 轴视角首次整体定价 first/warm 112–457×） | 每进程 2.23–2.50ms 冷合成 | **辖区 + 验收标准双封死拒列（不铸 ID）**：首调价在 V8 tiering 不在切片算法，切片内重构不能消除首次解释执行；once-per-run CLI 噪声是派单明拒面；且冷合成距线仍 4.0–4.5× | 派单验收标准 + 本轮新轴 A1/A2 |
| **CLI 启动预热调用**（进程起点先对四进入点各打一发假调用换 tier-up） | 表面上把冷价移出关键路径 | **净负优化拒列**：预热调用的温价与真调用的冷价照付（合计更高），且假调用需假夹具 = 行为面新增；S9-E-2 负优化同式 | S9-E-2 同式 + 本轮新轴 A1 |
| **惰性/延迟/条件 import**（切片模块按命令类懒载、`learned-routing` 与 `adaptation/promotion` 解耦缩闭包） | 切片边际 import 2.19–3.71ms/进程 | **排除行逐字封死拒列**：X1-1 隐藏模块缓存族（fail-open 角落 + 首用抖动转嫁）；解耦 = 公开契约重构（X0-4 族，实现在 D 切片辖区）；且外依赖闭包 18.5–21.5ms 不属本切片，边际份额已在线下 | X1-1 / X0-4 + 本轮新轴 A3 |
| **V8 code cache / startup snapshot / 编译产物预热** | 冷层全额 4.4–6.2ms/进程 | 运行时/部署基建，非切片代码；本战役职权是保行为源码优化 | 辖区外（记录为跨职权观察） |
| **扫描前语料宽度预探针**（先测 charset 再选正则路径/跳扫描） | mixed 与 ascii 斜率差 ~2–4ns/char | 换名重提拒列：S12-D-1 预探针族（fail-open 角落）+ S9-E-2 预过滤负优化；且探针本身要全扫一遍 charset = 自付斜率 | S12-D-1 / S9-E-2 + 本轮新轴 B |
| **先 truncate 再扫描**（用 400 上限先斩文本再进 scoreUserAnswer/PEER_NEGATIVE） | 把斜率上界从全文压到 400 字符 | **不等价拒列**：打分语义对全文（关键词可在 400 后出现），truncate 仅存储面；改扫描窗口 = 行为变更 | X0-2 / S1-E-3/S2-E-5/S7-E-2 truncate 族 |
| **词表 CJK 臂删除/重排**（USER_NEGATIVE/USER_POSITIVE/PEER_NEGATIVE 中 不行/错误/可以） | 交替表缩短（编译后近零） | **不等价拒列**：R7-E 反例 `"x可以x"` 命中维持；本轮 B 轴为其补语料价——三语料斜率已覆盖交替表上界，删臂无可测收益 | R7-E S7-E-1 等价论证 + X0-2 + 本轮新轴 B |
| **CJK 快路径**（检测纯 CJK 文本走简化扫描） | CJK 已便宜 2.4–3.1×，无收益位 | 方向为负的特化：为更快的输入建快路径 = 纯复杂度增量；预探针族同前 | 本轮新轴 B（实测反向） |
| **apply 首调层坍缩**（利用次调即跌 17× 的 tier 特征重排 10 次调用顺序） | first 107–124µs 只付一次，已是现状 | 测量事实非候选：生产调形本就只付一次首调（A2 合成已按此计价）；无重排空间 | 本轮新轴 A1 |
| **diagnose/bandit 首调窗内 second 仍高**（diagnose second 66.7–107.4µs、bandit second 59.1–103.1µs——二调仍未 tier-up） | 每进程一次性 ~120–210µs | 同 V8 tiering 辖区；且两函数每进程各只调一次，second 层生产不发生 | 本轮新轴 A1 |
| **JUDGE_DECISION 双 Map.get / verdict 双分派 / 条件 spread 形状 / baseSignal 形状多态 / 分派重排** | ns 级 | 换名重提拒列：S1-E-2 / S9-E-3 族 / S8-E-2 / S4-E-3 / S3-E-1 逐字维持；R14-E JIT 轴与 R15-E GC 天花板继续覆盖 | S1-E-2 / S9-E-3 / S8-E-2 / S4-E-3 / S3-E-1 |
| `from-episode` 内部字面量再校验 / Date.parse / familyFromRouted includes / isCompleteRoute 谓词序 / applyCascadeRetry / seen-Set 单探针 / outcomeKindFromResult 死形参 / 环内 try/catch | ns 级或实测 ~0 | 全部维持 R10-E..R17-E 拒列（含 R17-E A4 零成本 try 证实），位点未变 | R10-E..R17-E §2 |
| `auto-loop` 三路拼接 / persistSignals filter 双求值 / 顺序读 / optimizedPolicy filter+map / candidatesFor().find | ns~µs 级或 I/O 支配 | 换名重提拒列：R12-E 行 13 / S1-E-4 / S1-E-5 / S2-E-6 / S1-D-2 / X0-3 逐字维持 | S1-E-4/5 / S2-E-6 / S1-D-2 / X0-3 |
| `bandit-store` / `readBanditFile` corrupt JSON catch；`parseLearnedRoutingPolicy` / `parseObservedSignal` 拒绝门族 | 每 run 至多一次，µs 级 | 数据面 fail-soft/fail-closed 契约本体；R17-E 异常轴价格表 + S6-E-3/4 / S2-E-4 / X0-5 全维持 | R17-E 新轴 A + S6-E-3/4 / X0-5 |
| I/O 契约面全体：fsync 丢弃、完整性重哈希跳过、no-change 跳写、双载入去重、corrupt registry fail-open、readdir 并行化、惰性载入、mkdir 提升、紧凑序列化 | 切片相关全部 ms 级余量所在 | 排除行逐字重现 + 硬不变量明令封死；`routingPolicyContent` pretty-print 参与 contentHash ⇒ 序列化变更破坏 CAS 契约 | X0-3 / S2-E-1/4 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1 / S2-I-1 / S9-G-1 族 + CAS |
| 测试专用面任何优化（`patterns` / `attribution` / `signatures` + 五个测试专用导出） | 不进任何生产路径 | 本轮交叉检索复核仍零生产调用方，自动拒列 | S1-E-7/8 / X2-6 族 |
| 死导出清理 / `scoreTaskResult` 尾部 `return 70` | 0ns 运行时 | 不立项：无收益路径即无候选资格；公开签名保留 | 硬不变量（公开签名） |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（十八轮）、src/test 自 R17-E 基线零漂移、调用位点行号全数复核一致（§1） | — |

**提议排除 ID：无（本轮无「新增排除 ID」表——空枚举未铸任何 S18-E-\*）。**
切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选。前十七轮
已逐类收口的十三个角度类之外，第十八遍以「执行层级 × 字符宽度」双轴
普查新点名的全部维度（5 个函数 × 4 层执行价 + 4 站 × 3 语料宽度价）
全部给出实测价格与辖区/验收标准/算术封死；本切片「无落地余量」证明自此
覆盖 **CPU（R3-E..R14-E）+ 规模（R14-E）+ 分配（R15-E）+ 协议（R15-E）+
累积状态（R16-E）+ 载荷形态（R16-E）+ 异常面（R17-E）+ 构成面（R17-E）+
执行层级（本轮）+ 字符宽度（本轮）十个维度**。

## 3. 逐文件收口（前十七轮收口之上的本轮新检查点）

| 文件 | 本轮第十八遍新视角检查 | 结论 |
| --- | --- | --- |
| `signals.ts` | **宽度价首次标定**（三站三语料斜率表：cjk 便宜 2.4–3.1×、mixed ≤1.36×、最坏穿越点 ~73.5 万字符）；词表 CJK 臂补语料价（R7-E `"x可以x"` 等价论证维持）；collect 首调价 893–1,047µs（首/温 134×，V8 辖区）；先 truncate 再扫描判不等价；S1-E-1..3 / S2-E-5/7 / S4-E-1/3 / S6-E-1/2/5 / S7-E-1/2/4/5 / S8-E-2 / S9-E-2/3 维持 | 无候选 |
| `from-episode.ts` | outcomes 首调价 817–890µs（253–283×，V8 辖区）；温态 3.1–3.7µs 在带；环内 try/catch 维持 R17-E A4 零成本裁决；既有拒列全维持 | 无候选 |
| `learned-routing.ts` | apply 四层价首次标定（first 107–124µs → second 6.3–6.7µs 即跌 17×，生产 10 任务冷调形 ~155µs/进程 vs 温态 2.7–3.1µs）；load fresh/real 复测在带（29.4–49.8 / 80.7–134.5µs）；解耦 promotion 依赖缩 import 闭包判 X0-4 族；apply 内部零改动（S13-B-1 边界 + R13-E #2/#3 不补铸）；S8-E-1 维持 | 无候选 |
| `task-success.ts` | 在 collect 冷路径内合并计价（无独立进入点）；binding 逐项维持 S8-E-2/S9-E-3 已裁面 | 无候选 |
| `diagnostics.ts` | 首调价 194–227µs、次调 66.7–107.4µs（每进程一次，second 层生产不发生）；本轮夹具 diagnoseGroups=1 全数走到分组路径（0.89–1.18µs 在带）；R14-E 双除法/filter().length 拒列维持 | 无候选 |
| `bandit-store.ts` | in-lock 构建复制体首调价 156–306µs（tiering 辖区；测量法修正：previous 手写字面量以保首调真冷，见 §0）；corrupt JSON fail-soft 维持契约本体裁决；fsync/重哈希/mkdir/序列化按令未触碰 | 无候选 |
| `auto-loop.ts` | 进入点编排面；切片边际 import 2.19–3.71ms 含其份额（1770 行 parse+eval）；惰性载入判 X1-1；三路拼接/filter 双求值/顺序读维持既有拒列 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | test-only 面复核仍零生产调用方，自动拒列；其 import 份额只在测试进程发生；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核（依赖 `pnpm install
--frozen-lockfile`，pnpm 10.17.1）：

```bash
# Node v22.22.2（nvm；engines >=22.19.0 合规）
pnpm gate        # typecheck ✓ + lint ✓ + test ✓ + build ✓
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.14.0 与 v22.22.2 均过）
```

锚点脚本（临时置于 `/tmp/r18e-anchor.mts`，未入库以遵守无赢家纪律；差异
面见附录 A）共 **17 次独立运行**（8 × v22.14.0 + 8 × v22.22.2 + 同场
A/A 复认 1 × v22.22.2），每次 3/3 sanity 检查全过，全部确定性 VERDICT
行跨 17 次运行**逐位一致**；另 **12 次 R17-E 种子比对运行**（cmp17
模式）构成 VERDICT 行 12/12 逐位一致且与 R17-E 夹具行逐字相同。代表性
一次运行（v22.14.0）：

```text
VERDICT fixtures: E=41 collected=8 routedE=41 outcomes=3 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=7.6us outcomes=3.1us diagnose=0.92us bandit-build=0.8us | total in-slice CPU ~12.4us per run vs landing bar >=10000us (809x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=280ns/task | empty policy=122ns/task -> x10 tasks/run = 2.80us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=33.1us | realistic registry (baseline+1 candidate)=91.5us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

新轴脚本（`/tmp/r18e-cold.mjs` 6 进程、`/tmp/r18e-import-marginal.mjs`
6 进程、`/tmp/r18e-cjk.mts` 6 次运行，两 Node 版本各半；sanity 4/4 与
54/54 全过、确定性 VERDICT 行逐位一致）代表性输出：

```text
COLD import (dist learning subtree union, plain node production face): 19.1ms
COLD collectSignalsFromEvents(E=41): first=1046.9us second=194.2us next8=31.4us warm=7.82us | first/warm=134.0x
COLD outcomesFromRoutedRun(E=41): first=889.9us second=32.8us next8=18.2us warm=3.15us | first/warm=282.8x
COLD diagnoseModelProjectIssues(S=12): first=227.0us second=107.4us next8=7.2us warm=0.95us | first/warm=238.1x
COLD banditBuild(S=12,prevArms=3): first=182.0us second=59.1us next8=6.5us warm=0.75us | first/warm=242.3x
COLD applyLearnedRouting(avoid=10,M=10): first=123.7us second=6.6us next8=3.0us warm=0.27us | first/warm=457.2x
COLD once-per-process composite (collect+outcomes+diagnose+bandit + apply x10 tiered): cold=2499.9us vs warm=15.4us | cold/warm=162.6x | vs landing bar >=10000us: 4.0x below even at cold tier
COLD import split: out-of-slice dep closure=19.1ms | slice 10-file marginal=2.65ms
BENCH mixed station A: T=1K 13.3us | T=16K 195.7us | T=128K 1738.0us | slope=13.45ns/char | T*(10ms)~743,621 chars
BENCH cjk station B: T=1K 3.8us | T=16K 33.4us | T=128K 406.6us | slope=3.25ns/char | T*(10ms)~3,073,264 chars
```

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round18-r18e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R17-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐形态有理由；第十八遍以「执行层级 ×
  字符宽度」双轴普查新点名的全部维度落进辖区外（V8 tiering/部署基建）、
  验收标准明拒面（once-per-run CLI）、实测定价封死区或既有排除确认；
  两条新轴本身均以「无候选」收口，未发现第十四个机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令封死
  的 I/O 契约面、once-per-run CLI 面或切片外辖区上；CPU 温态面即使
  清零也距门槛 678–809×，冷层全额上界 4.4–6.2ms 亦在线下）。
- 矩阵空洞：无（调用位点行号与 R8-E..R17-E 逐一相同；消费者集合与
  R17-E 逐一相同；配置态 live 面与载入面锚点全数复测在带内；双 Node
  版本交叉不可分辨；本轮带 12.4–14.8 由 12 次种子对调运行实证归因为
  构成效应——R17-E 种子在本 VM 复现 18.8–19.4，逐点落在 R17-E 带内）。
- 冷层假说（「温态锚点低估单发进程真实成本两个量级」）：以四层价格表
  关闭（首/温 112–457×、冷合成 141–163× 属实——但冷合成 2.23–2.50ms
  距线仍 4.0–4.5×，冷层全额上界 4.4–6.2ms 仍在线下；减法在 V8/部署
  基建辖区，且 once-per-run CLI 面是派单明拒项）。
- 宽度假说（「非 ASCII 语料可能病态慢」）：以斜率表反向关闭（CJK 便宜
  2.4–3.1×、mixed 最贵 ≤1.36× ascii、最坏穿越点 ~73.5 万字符/事件距
  生产 3 个量级；R16-E B2 字节轴 + 本轮宽度轴合并覆盖载荷全平面）。
- S13-B-1 边界：`applyLearnedRouting` 本体零改动；重开条件
  （|avoid| ≈ 965–1,063，R16-E 量化）未触发、生产侧不可触发；R13-E
  三处无名微观未补铸未重开。
- 跨职权观察（非候选、非 ID）：(a) V8 code cache / startup snapshot 可
  一次性回收每进程 ~4.4–6.2ms 切片冷层价（外依赖闭包另有 18.5–21.5ms）
  ——属部署/运行时基建，不在本战役保行为源码优化职权，仅记录；(b)
  `\b` 包裹的 CJK 词表项孤立文本永不匹配（R7-E 已成文，R17-E 复认，
  本轮补语料价）——产品侧若欲中文答复参与打分属功能变更（X0-2），
  仅记录。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R17-E §6（含 R14-E 规模判据、R15-E
分配/GC/噪声地板判据、R16-E 累积状态与载荷形态判据、R17-E 异常面与
构成面判据），并新增两条可标定判据：**(a) 执行层级判据**——若生产
形态从单发 CLI 进程变为长驻守护进程/服务（温态支配，现有温锚点即
治理），或落地线口径改为接纳每进程 ms 级一次性成本（届时应与运行时
基建侧共推 code cache，而非切片内重构）；**(b) 字符宽度判据**——若
生产单事件文本规模逼近 ~73.5 万字符（按本轮 mixed 站 A 最坏斜率折算
10ms），或词表增长使扫描斜率较本轮基准恶化 ≥10×；或任一 I/O 契约排除
（X0-3 / S2-E-1/4 / S1-G-1 / S1-E-4/5 / S4-E-2 / S5-E-5 / S6-E-3/4 /
S8-E-1）被正式推翻，或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

主锚点脚本全文（`/tmp/r18e-anchor.mts`，`npx tsx /tmp/r18e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下四处替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R18-E；
`NOW = "2026-08-25T03:45:00.000Z"`；SECTION 1 的 seeds
`0xe18e01`（collect）/ `0xe18e02`（routed）/ `0xe18e03`（signals）——
三个生成器按序首选种子全部一次通过拒绝采样（collected=8>3、
outcomes=3>0、diagnoseGroups=1≥1），零出局；探针记录
seed→(collected,outcomes,diagnoseGroups)：01→(8,1,0)、02→(13,3,3)、
03→(18,2,1)；SECTION 2 的 `mkdtemp` 前缀 `r18e-fresh-`/`r18e-reg-`
与 detector identity `r18e-anchor`。另加 CLI 开关 `cmp17`：置换为
R17-E 的 NOW（`02:30`）与 seeds `0xe17e01/02/03`、前缀
`r18e-cmp17-`、identity `r18e-anchor-cmp17`，用于 R15-E 协议的种子
对调构成归因运行。其余逐字同 R10-E 附录 A。

新轴 A 脚本（`/tmp/r18e-cold.mjs`，`node /tmp/r18e-cold.mjs`，纯 JS）：
against `dist/` 编译产物 + 裸 node（生产执行面）。顶层 `await import`
计时 7 个 dist 模块（4 切片进入点 + bandit/catalog-model/roles）为
union import 价；夹具生成器逐字镜像锚点（同 seeds），**夹具先于全部
计时窗构建**（生成器只预热生成器代码）；`banditBuild` 的 `previous`
为手写字面量（prevArms=3 同锚点派生形状）——初版由温 `banditBuild`
派生导致首调窗被隐式预热，已修正。`coldTiers()` 对每函数依次计
first（首调）/ second（次调）/ next8（再 8 调均值）/ warm（bench
2–10 万次），并输出 first/warm 比；合成 = c.first + o.first + d.first +
b.first + (a.first + a.second + 8×a.next8)（生产调形：四进入点各一次 +
apply ~10 次分层计价；初版误按 10×first 计，已修正）。等价断言：冷/
次调输出 JSON 相等，collect 剥离 `createdAt`（`nowIso()` 壁钟字段跨调
合法不同，初版未剥离致假阴性，已修正）。sanity 4 项。每进程一份冷
样本，两 Node 版本各跑 3 进程。

新轴 A 补充脚本（`/tmp/r18e-import-marginal.mjs`）：phase 1
`Promise.all` 预热切片 10 文件的全部 26 个直接外部依赖模块（传递闭包
随之温载：adaptation/approval-profile、candidate、promotion、registry、
resource；domain/errors、hash、ids、record、roles、timestamp；
feedback/store、types；persist/file-lock；preferences/service；
privacy/state-layout；protocol/v1；routing/assign、bandit、
catalog-model、failure-class、outcomes；run/episode-bind、events、
event-store；task/taxonomy），phase 2 再 import 切片 10 文件计边际——
边际 delta 即切片自身 parse+eval 份额上界。两 Node 版本各 3 进程。

新轴 B 脚本（`/tmp/r18e-cjk.mts`，`npx tsx /tmp/r18e-cjk.mts`）：三语料
单元（ASCII lorem 64 字符 / 千字文 CJK 32 字符 / 混合 72 字符，均精心
避开全部词表关键词），`filler(unit, size)` 重复裁剪到 1K/16K/128K。
三站夹具：A = discovery + USER_ANSWER（filler + " lgtm" 尾缀，断言
score=90 userAcceptance 恰 1 信号）；B = discovery + 完整 MODEL_ROUTED +
TASK_RESULT（summary=filler，断言 taskSuccess PASS score=90 且存储
summary≤400）；C = 子代理 raw（6 段 text part 拼 filler，断言 peer
score=70 恰 1 信号）；S = `scoreUserAnswer(filler)` 直调（断言
undefined 未命中）。54 项 sanity 兼共享 JIT 预热（另 400 轮 × 3 语料 ×
4 站 1K 预热）后按站/语料/尺寸计时（reps 20000/2000/300），斜率取
16K→128K 段，T\*(10ms) = 10ms ÷ 斜率。两 Node 版本各 3 次。

17 次主锚点运行原始区间（8 × v22.14.0 + 8 × v22.22.2 + 复认 1 ×
v22.22.2）：

```text
SLICE-CPU 总量:
  v22.14.0: 12.4 / 14.2 / 12.4 / 13.3 / 13.2 / 13.1 / 12.5 / 12.9 us
  v22.22.2: 14.5 / 14.8 / 14.7 / 14.4 / 13.1 / 13.2 / 14.3 / 14.4 us + 复认 12.4
collect:    7.6-9.3 us（v22.14.0 7.6-8.6 / v22.22.2 7.9-9.3）
outcomes:   3.1-3.7 us
diagnose:   0.89-1.18 us
bandit:     0.7-0.9 us
apply(non): 268-314 ns/task（v22.14.0 272-314 / v22.22.2 268-314）
apply(空):  116-140 ns/task
load fresh: 29.4-49.8 us
load real:  80.7-134.5 us
VERDICT 行: 17/17 逐位一致；sanity 3/3 x17 全过
```

12 次 R17-E 种子比对运行（cmp17）原始值：

```text
静场 9 次（6 x v22.14.0 + 3 x v22.22.2）:
  总量: 18.8 / 19.0 / 19.4 / 19.0 / 18.8 / 18.8 | 19.0 / 18.8 / 19.4 us
    —— 逐点落在 R17-E 带 18.5-20.2 之内
  collect 12.8-13.4 / outcomes 4.0-4.3 / diagnose 1.03-1.14 / bandit 0.7-0.8
    —— 与 R17-E 逐项同带（12.7-14.1 / 4.0-4.3 / 0.98-1.11 / 0.7-0.8）
早场 3 次（会话负载噪声）: 22.1 / 21.1 / 22.2 us（collect 14.3-15.5）——略超带
VERDICT 行: E=41 collected=15 routedE=41 outcomes=4 S=12 diagnoseGroups=1
  prevArms=3 —— 12/12 逐位一致，与 R17-E 夹具行逐字相同
```

6 进程冷层轴运行原始值（3 × v22.22.2 + 3 × v22.14.0）：

```text
import union:    19.1 / 17.0 / 17.0 | 21.8 / 20.1 / 21.7 ms
collect first:   1046.9 / 994.4 / 917.8 | 953.1 / 921.2 / 893.3 us
outcomes first:  889.9 / 839.2 / 841.2 | 817.1 / 821.6 / 868.5 us
diagnose first:  227.0 / 202.4 / 200.9 | 197.5 / 196.6 / 193.6 us
bandit first:    182.0 / 181.3 / 306.2 | 187.9 / 156.0 / 157.0 us
apply first:     123.7 / 107.0 / 123.1 | 113.9 / 108.8 / 116.8 us
冷合成:          2499.9 / 2358.5 / 2433.2 | 2305.9 / 2233.7 / 2258.7 us
温合成:          15.4 / 15.9 / 15.5 | 15.4 / 15.2 / 16.0 us（cold/warm 140.9-162.6x）
sanity 4/4 x6 全过；夹具 VERDICT 行与锚点逐位一致
import 拆分（补充脚本 6 进程）:
  外依赖闭包:    18.5 / 18.6 / 19.1 | 20.6 / 21.4 / 21.5 ms
  切片边际:      2.19 / 2.25 / 2.65 | 3.22 / 3.43 / 3.71 ms
```

6 次字符宽度轴运行原始区间（3 × v22.22.2 + 3 × v22.14.0；斜率 ns/char）：

```text
ascii:  站 A 11.07-11.36 / 站 B 9.12-9.28 / 站 C 10.40-10.55 / 站 S 1.76-1.78
cjk:    站 A 4.07-4.34  / 站 B 3.23-3.49 / 站 C 3.52-3.67  / 站 S 0.73
mixed:  站 A 13.42-13.61 / 站 B 12.23-12.42 / 站 C 12.94-13.28 / 站 S 1.32-1.34
T*(10ms) 全表: 最坏 mixed 站 A 734,868-744,925 chars；最好 cjk 站 S
  13,631,225-13,743,994 chars
sanity 54/54 x6 全过（确定性产出断言 + miss-scan）；两版本同带
```
