MODEL_SLUG=claude-fable-5-thinking-xhigh

# R18-I：CLI / Pi adapter / config / telemetry 第十八遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `1d42a17`（派单 tip，含 R18-C 收口与 R18-I 派单；G/H 在飞，未触碰）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- src/cli src/pi-adapter src/config src/telemetry` 为空（`--stat` 0 行）——**零漂移，连续第十一轮字节不变**。`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet` 通过）；切片相关测试面零漂移（`git diff --quiet 8dee7fb..HEAD -- test/` 通过）。基线增量核查：`fb5d9a2`（R17-I 基线）→ `1d42a17` 共 10 个提交，全部 `docs(sota-opt)` 收口/派单（`git diff --stat -- . ':!docs'` 为空，非 docs 零文件）；**EXCLUSIONS.md 该区间逐字节未变**（`git diff --stat` 空；545 行 wc -l 口径），对 I 切片无新约束行（最后 I 相关行仍是 S8-I-3）。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致（合计 4708 行）。
- 前置阅读按令执行：README → EXCLUSIONS 全表（含全部 S\*-I-\* 行与 S13-B-1 尾行）→ PROGRESS → round-18/PLAN（含 R18-A…R18-F 六切片空枚举收口摘要与 I 切片行）→ R17-I（九连空 + fail-closed 拒绝路径定价六端点）→ R16-I（flowchart 抽测格 + 181 模块/1003 KiB 采样归因）→ R15-I / R14-I / R13-I 收口段落逐份重读 → **25 个切片源文件全量实际重读**（含凭据面两文件 `file-credential-store.ts` / `auth-session.ts` 的只读读码，未测量未立项）。
- 分支：`cursor/r18-i-cli-eighteenth-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（唯一入口 `/exec-daemon/node` shim，与 R9-I～R17-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。R15-I 轴 2 已收口引擎代格（24 无增量红利），本轮不重装 24、不补铸。pnpm 10.17.1，`pnpm install --frozen-lockfile`（本 VM node_modules 初始缺失，store 温装 1.8 s）+ `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**（package.json 钉死 + frozen-lockfile 内容寻址），与 R7-I～R17-I 同版本；exports 图逐项复核仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第十一轮未满足，两臂本轮第六次直接测量再确认（§2.2）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S18-I-\* ID（诚实空枚举，与 R9-I～R17-I 同结局，十连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第十八组枚举而非历轮剩余项清理。R14-I 钉死配置空间维度、R15-I 钉死测量学维度、R16-I 钉死命令类覆盖与采样归因维度、R17-I 钉死错误端点定价维度；本轮补最后一块十七轮从未直接测量的正交砖——**输入负载规模维度（children spec 基数 C）**：自 R8-I 以来所有承重格与关闭矩阵的 children 夹具恒为 C=2，S1-I-3（`assignments.find` 换 Map）的否决依据「children 个位数」从未有过越线点标定；兄弟切片的同型轴（R13-H 规模-越线标定、R14-A/D/E/J 规模越线）在 I 一直缺席。该轴为测量收口、未产出候选：

1. **零漂移五重确认（连续第十一轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变——frozen-lockfile 内容寻址使包内容漂移数学上不可能；pi-ai 0.84.1 exports 图 7 项未变；`dist/providers/` 仍恰 39 表，本 VM 直接计数）、测试面（切片相关测试目录自 `8dee7fb` 零变动）、生产调用图（本轮再 grep：`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` 失手回退 L36 / `runtime.ts` 点用处 L20 / `listed-model.ts` 同步面 L1，`model-catalog.ts` 命中为 S7-I-1 注释非导入；pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方）、基线增量（`fb5d9a2..1d42a17` 全 docs，EXCLUSIONS 字节不变）。R8-I 关闭矩阵、R9-I～R17-I 抽测所依据的结构前提在本基线逐项成立。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字，派单明令项）**：R9-I 5 格承重集 × 2 Node 版本全部 exit 0，结构逐格同构：地板 54.3/51.4 ms，**custom−builtin 差 children +49.5 / +26.4、track +55.2 / +25.0 ms（22.14/22.22）**——与 R17-I 带（+47.3/+24.7、+46.5/+21.8）、R16-I 带（+49.7/+26.3、+51.7/+22.5）及更早八轮同号、同量级、同 Node 版本梯度（**十一轮同构**）；带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内。builtin 态目录构建比地板贵 +54.0/+55.6（children，双引擎），与 R17-I（+54.6/+53.9）紧邻。进程内冷载锚点：`providers/all` 46.0-79.5 / 31.0-31.7 ms（22.14/22.22；warm 0.059-0.078 ms）；per-provider 表 anthropic 3.5/2.5、openai 2.2/0.9 ms（优势 ~9-25×）；custom id 失手解析首次 1.959/2.014 ms、重复失手中位 **0.097/0.102 ms**（九度复证 R10-I 记忆化否决）。S7-I-1 per-provider 优势与 custom 态回退归属完好（§1）。
3. **第十八组新角度枚举为空（§2）**，其中一轴为本轮新测量、不铸 ID：
   - **轴 1（§1.3）：children spec 基数越线标定（I 切片首次；输入负载规模维度）**。端到端新增 C=10 / C=50 双格（builtin 态，双引擎全 exit 0）：children@builtin C=2→10→50 为 108.3→144.4→410.5（22.14）与 107.0→147.8→452.3 ms（22.22），**边际成本 ~4.5-7.6 ms/child**。进程内阶梯（C ∈ {2,10,50,200,1000,4000}，逐站点 best-of-5）把切片内三个 O(C)/O(C²) 站点直接定价：`parseChildSpec` 线性 ~0.44 µs/task（C=4000 仅 1.76 ms）；compile 预处理 flatMap 全程亚 ms；**唯一超线性站点是 S1-I-3 的 `assignments.find` 二次扫描**——C=4000 时 find 形态 31.4/31.7 ms vs Map 反事实 3.13/3.14 ms，**差 28.3/28.6 ms；二次拟合的 10 ms 越线点在 C ≈ 2400**，比生产 children 基数（个位数）高约三个数量级。C=50 时切片内三站点合计 **0.25/0.25 ms**，对端到端 C=2→50 增量（+302.2/+345.3 ms）占比 **~0.08%**——每 child ~6-7 ms 的边际成本以减法归属于切片外（run/ 子代执行 + persist/ 逐事件 jsonl 追加与逐 child readAll 门控，G/J 契约地板，fsync 族关闭在案）。**无候选、不铸 ID**；S1-I-3 的否决依据自此有直接越线点数字。
   - 其余全部角度逐条映射到排除行或历轮无 ID 收口（§2.3），无一越过落地线。R17-I 拒绝路径定价六端点、R16-I flowchart 格与采样归因、R15-I 三轴、R14-I 肥态证明、R13-I 两处无名微观（`import.meta.resolve` 预探针本轮重读 `listed-model-lazy.ts` L21-38 原样、alias-row spread `model-catalog.ts` L72/L79 原样）均未重铸、未重测（其前提——39 表计数、exports 图、pi-ai 版本——本轮复核未变）。
4. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**；r7i part D 载荷探针复证 builtin 构建后 providers/all 保持冷 60.1 ms、custom 回退命中缓存 1.4 ms）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R17-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读证据，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交 `scripts/round18-r18i-equivalence-sim.ts`（全部 harness/探针源码进附录 A，遵守 R7-F/R8-C/R9-I～R17-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md 未触碰；未触碰在飞 R18-G / R18-H 分支与文件。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S17-\* 条目（含 S13-B-1）；R9-I～R17-I 空枚举维持，未事后发明 S9-I-\* ～ S17-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：`--children` 复用 smartChildPlan 已校准目录（main.ts L726-729 原样）、Pi runtime 点用处 `await import`（main.ts L149 / auth-session 函数体内；S4-I 败者为 **S4-I-2..5**，无 S4-I-1）、12 条分支独占分派模块惰性导入（赢家 ID **S5-I-1**；main.ts L1417-1455 dispatch 站点 + L668 track/loop 边逐字在位）、per-provider 惰性模型表（`listed-model-lazy.ts` + `model-catalog.ts` L58）——零漂移即字节级证明，三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅对其**重开条件**做第六轮直接测量再确认（§2.2）——四 specifier 全 blocked（双 Node 逐字）+ `all.models.js` 文件级不存在 + 39 表计数不变 + exports 图 7 项不变，未满足，维持淘汰。硬不变量「`providers/all` 失手回退 + throw-outside-catch」逐字在位（`listed-model-lazy.ts` L34-37 未动）。本轮新轴是既有 `--children` 路径在输入基数维度上的直接观测（零行为差异、零代码改动），未构造替代注册表、未触碰回退路径——与自制 union / 磁盘快照 / memo 三重围栏（R10-I / R11-I）中的任何 rename 无涉，与 R17-I unknown-model 拒绝路径的 providers/all 消费判定无涉（本轮全部 exit-0 格）。
  - **未重开 R17-I 拒绝路径收口**：六个拒绝端点定价（五个贴 `--version` 地板 + unknown-model@children = 地板 + 一次 providers/all 权威源查询）不补铸、不重测；本轮抽测集回归 exit-0 承重格 + 新规模格。
  - **未重开 S8-I-2**（预热无重叠窗口）、**S8-I-3**（enabled 循环 Promise.all）、**S7-I-2..4**、**S6-I-1..3**、**S5-I-2..5**、**S4-I-2..5**、**S3-I-1..6**、**S2-I-1..6**、**S1-I-1..8**。本轮 25 文件重读逐字再确认（未立项）：flowchart 双 `buildLiveCatalogConfig`（S1-I-1，main L569+L583）、providers.json 双读（S1-I-2，main L609 + createExecutor kind==="pi" L131）、loadLearnedRouting/loadProvidersConfig 死载荷下沉（S2-I-1/S3-I-3，main L708/L609）、`smartChildPlan` assignments.find（S1-I-3，main L372——本轮轴 1 对其否决依据做越线点标定，见 §1.3）、双 createAgentProfileRegistry（S3-I-2，main L286+L753）、stdout 批量（S3-I-4）、冲突检查提升（S4-I-4）、describeSparkleModel try/catch 预探测（S4-I-5）、setDefaultModels 双 parseModelRef（S3-I-5，providers-config L100-104）、parseCliErrorJson reverse（S1-I-6）、buildInvocation +=（S1-I-7）。
  - **R10-I ～ R17-I 无 ID 收口维持**：跨进程版本键控磁盘快照、进程内失手记忆化（本轮重复失手中位 0.097-0.102 ms 九度复证 µs 族）、进程内自制 39 表 union（三重围栏）、回退后 per-provider 探针短路（≤3.5 ms/provider，仅 mixed 态可达）、root `.` / `./compat` 作替代源（R12-I）、`all.models` 文件级探针（R13-I）、肥配置态交叉不存在证明（R14-I）、A/A 噪声地板 / 引擎代格 / 堆足迹格（R15-I）、flowchart 抽测格 + 181 模块/1003 KiB 采样归因（R16-I）、**fail-closed 拒绝路径六端点定价（R17-I，不补铸不重测）**。**R13-I 两处无名微观维持不铸**：`import.meta.resolve` 同步预探测（S4-I-5 族镜像 + 失手锚点九度定界 <0.3 ms/构建）、`buildLiveCatalogConfig` 默认别名行 spread（ns-µs 级 S1-I-8 邻域）。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项（S1-I-5 / S7-I-4 维持关闭）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；稀疏→已批准 R0 基线；双 LCB / 双归因在位（X0-11）；提升 proposal-first（Checkpoint F-PROD 开放，ADR-005/X0-1）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`listed-model.ts` 同步公开面与惰性面经 `listed-model-common.ts` 字节等价关系未动；`providers/all` 失手回退 + throw-outside-catch 在位；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；未触碰在飞 R18-G / R18-H 分支与文件。

## 1. 本 VM 复测：5 格承重抽测 + 进程内锚点 + 新轴（双 Node，未拷贝历轮数字）

R9-I～R17-I 抽测集原样复刻并**新增 2 个 children 基数格**（configured 态夹具、每迭代全新拷贝 state-root、2 预热 + 10 测量、显式二进制全路径，遵守 R7-I 配置态 × 命令类纪律；harness 全文附录 A.1）。

### 1.1 抽测中位数（ms，spawn-to-exit，全部 exit 0）

| 格 | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|
| `--version`（地板） | 54.3 (min 52.9, p90 56.5) | 51.4 (49.3, 52.5) |
| run-children@builtin（C=2） | 108.3 (106.6, 110.9) | 107.0 (102.9, 111.0) |
| run-track@builtin | 125.7 (120.8, 128.4) | 129.1 (126.6, 131.6) |
| run-children@custom | **157.8** (149.7, 165.1) | **133.4** (129.1, 138.4) |
| run-track@custom | **180.9** (171.4, 195.4) | **154.1** (147.9, 157.5) |
| run-children@builtin-c10（本轮新格，§1.3） | 144.4 (139.7, 147.5) | 147.8 (143.2, 163.2) |
| run-children@builtin-c50（本轮新格，§1.3） | **410.5** (405.1, 418.6) | **452.3** (439.7, 480.2) |

- **custom−builtin 差**：children **+49.5 / +26.4**、track **+55.2 / +25.0** ms（22.14/22.22）。R17-I 带 +47.3/+24.7 与 +46.5/+21.8，R16-I 带 +49.7/+26.3 与 +51.7/+22.5，R15-I 带 +56.3/+39.7 与 +65.9/+23.2，R14-I 带 +52.2/+26.7 与 +55.1/+23.7，R13-I 带 +45.5/+23.7 与 +51.7/+21.5，R12-I 带 +51.3/+29.1 与 +58.7/+24.1，R11-I 带 +45.9/+22.8 与 +48.0/+23.4，R10-I 带 +47.1/+36.6 与 +56.0/+26.4，R9-I 带 +53.9/+26.9 与 +49.4/+28.2——**十一轮同号、同量级、同 Node 版本梯度**。带缘偏移（含本轮 track@builtin 22.22 略高于 22.14 的 +3.4 ms 交叉）全部落在 R15-I 轴 1 的 A/A 噪声包络内（22.14 单窗 maxAbs 34.5 ms、22.22 8.7-10.4 ms），无新结构。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
- **builtin 态目录构建**比地板贵 +54.0/+55.6（children，双引擎）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。本 VM 地板（54.3/51.4）与 R17-I VM（54.0/51.7）紧邻，格间结构不变。

### 1.2 进程内冷载锚点（附录 A.2 脚本，工作区根执行，测毕删除）

| 锚点 | 22.14.0 | 22.22.2 |
|---|---|---|
| `providers/all` cold import（3 次） | 57.1 / 79.5 / 46.0 ms | 31.0 / 31.5 / 31.7 ms |
| `providers/all` warm（第二次 await） | 0.059-0.077 ms | 0.065-0.078 ms |
| per-provider `anthropic.models` cold | 3.5 ms | 2.5 ms |
| per-provider `openai.models` cold | 2.2 ms | 0.9 ms |
| `providers/all`（双表已载后） | 54.4 ms | 26.9 ms |
| custom id 失手解析首次 | 1.959 ms | 2.014 ms |
| 同上，重复失手中位（N=49） | **0.097 ms** | **0.102 ms** |

- per-provider 优势 ~9-25×，双引擎成立，S7-I-1 归属完好；custom 格增量与 `providers/all` cold 同量级，回退归属自洽（22.14 的 79.5 ms 单样本为 A/A 包络内 VM 抖动，同轮另两样本 46.0-57.1 与历轮带重叠）。
- 重复失手 ~0.097-0.102 ms 九度复证 R10-I：Node package-reader 已缓存失败解析，记忆化物理无收益（<0.3 ms/构建）。

### 1.3 轴 1：children spec 基数越线标定（本轮新证据，不铸 ID）

十七轮以来所有端到端夹具的 children spec 恒为 **C=2**（R8-I 78 格关闭矩阵与 R9-I～R17-I 每轮抽测集皆然）；S1-I-3（`assignments.find` 换 Map，main.ts L372）的否决依据「children 个位数」、S3-I-4（stdout 批量）的「实测更慢」都建立在该基数上。输入负载规模维度在 A/D/E/H/J 各有兄弟收口（R13-H 规模-越线标定、R14-A/D/E/J 规模越线），在 I 从未直接测量。本轮两步补齐（spawn 格见 §1.1 下两行；进程内阶梯脚本与原始输出见附录 A.2 `scale` 模式）：

**(a) 端到端基数格**：children@builtin C=2→10→50 中位 108.3→144.4→410.5（22.14）/ 107.0→147.8→452.3 ms（22.22）。边际成本 **C≤10 带 ~4.5-5.1、C≤50 带 ~6.7-7.6 ms/child**（轻度超线性），双引擎同构、全 exit 0。

**(b) 进程内切片站点阶梯**（C ∈ {2,10,50,200,1000,4000}，逐站点 best-of-5，双引擎数字几乎逐位重合）：

| C | parseChildSpec 复刻 | assignTasks（切片外） | find 形态（S1-I-3 站点） | Map 反事实 | compile 预处理 | 纯 find 扫描 |
|---|---|---|---|---|---|---|
| 2 | 0.101/0.120 | 0.068/0.056 | 0.009/0.007 | 0.007/0.007 | 0.003/0.002 | 0.001/0.001 |
| 10 | 0.106/0.117 | 0.129/0.122 | 0.031/0.028 | 0.018/0.018 | 0.003/0.003 | 0.002/0.002 |
| 50 | 0.132/0.121 | 0.379/0.378 | 0.109/0.112 | 0.087/0.087 | 0.014/0.014 | 0.025/0.025 |
| 200 | 0.233/0.256 | 1.633/1.313 | 0.355/0.312 | 0.177/0.175 | 0.055/0.053 | 0.173/0.145 |
| 1000 | 0.759/0.771 | 3.772/3.946 | 2.951/3.177 | 0.760/0.764 | 0.395/0.336 | 3.903/3.607 |
| 4000 | 1.756/1.762 | 9.710/9.527 | **31.407/31.747** | 3.127/3.138 | 0.195/0.213 | 28.697/28.321 |

（单位 ms，22.14/22.22；`parseChildSpec` 为 main.ts L273-341 逐字形状复刻含 per-task `registry.resolve` 与 `parseTaskId`；find 形态为 main.ts L371-379 逐字形状复刻含 `liveCascadePlanFromAssignment`；Map 反事实即 S1-I-3 被排除候选的形态；compile 预处理为 main.ts L733-745 flatMap 复刻。）

- **切片内唯一超线性站点是 S1-I-3 的二次扫描**：C=4000 时 find−Map 差 **28.28/28.61 ms**（纯 find 扫描 28.3-28.7 ms 即其全部主体；cascade 构造在 Map 反事实中线性 ~3.1 ms）；C=1000 时差仅 2.19/2.41 ms。按二次律拟合（a·C²，a ≈ 1.77-1.79 × 10⁻⁶ ms），**10 ms 越线点在 C ≈ 2400**——比生产 children 基数（个位数；本仓 README 示例与全部测试夹具 ≤ ~6）高约**三个数量级**。`parseChildSpec` 严格线性 ~0.44 µs/task；compile 预处理全程亚 ms。
- **端到端边际成本 ≥99.8% 在切片外**：C=50 时切片内三站点合计 0.25/0.25 ms（加上切片外 assignTasks 也仅 ~0.63 ms），对端到端 C=2→50 增量 +302.2/+345.3 ms 占比 **~0.08%**（减法归属）。每 child ~6-7 ms 的主体是 run/ 子代执行编排与 persist/ 逐事件 jsonl 追加、逐 child readAll 门控（G/J 切片契约地板；fsync 去除族 S9-G-3/S8-J-2 关闭在案，逐 child readAll 是 S6-G-7/S1-G-1 证据边界契约）。边际成本的轻度超线性（4.5→6.7 / 5.1→7.6 ms/child）同样不在切片内（阶梯直测切片站点在该区间近线性且亚 ms），与事件日志随 child 增长的切片外重读自洽——记录为归属观察，不外推为候选。
- **候选性裁决（不铸 ID）**：(a) S1-I-3 换 Map 在生产基数下维持排除（C=2 时 find 0.007-0.009 vs Map 0.007 ms，零差），其重开条件自此有直接数字——children 基数需增长至 ~10³ 量级（越线点 C≈2400），届时 Map 反事实的等价性证据与形态已在本节备档；(b) 「按基数分派 find/Map 双形态」是为零流量假设格引入分支复杂度，S12-B-2/S13-B-1 同型先例（条件式落地物按未触发处理）；(c) 端到端每 child 成本的消除形态全部落在 G/J 切片契约（不可去 fsync、不可跳过读校验），非 I 切片候选；(d) 高 C 下 stdout 行数增长的批量化即 S3-I-4（实测更慢 + CliIo 调用次数可观测），维持排除。**输入负载规模维度就此收口**；后续轮次引用本节即可把「children 规模化瘦身」类候选直接判为越线点外推或切片外契约。

## 2. 第十八组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 输入规模定价），每形态先对 EXCLUSIONS 全表 + 十七轮 I 报告做同机制先例检索。R14-I 收口配置空间、R15-I 收口测量学、R16-I 收口命令类覆盖与采样归因、R17-I 收口错误端点定价；本轮收口**输入负载规模维度**（轴 1）。

### 2.1 新轴候选性自查——测量收口，不铸 ID

- **轴 1（基数标定）**是既有 `--children` 成功路径在输入规模维度上的直接观测：零行为差异、零代码改动。其产出是「切片内唯一超线性站点的越线点 C≈2400 + 端到端边际成本 ≥99.8% 切片外」的裁决数字，不是优化；唯一在理论上可达数十 ms 的形态（S1-I-3 换 Map @ C≥2400）是零生产流量假设格，维持排除并首次量化其重开条件。

### 2.2 S8-I-1 重开条件两臂第六轮直接测量再确认——不铸 ID

- 文件级：`dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 0.84.1（package.json + frozen-lockfile 内容寻址）。
- 执行级：四 specifier 探针在 22.14 / 22.22 双引擎逐字同结局（附录 A.2 原始输出：`providers/../models.generated` → ERR_INVALID_MODULE_SPECIFIER；`dist/models.generated.js` / `models.generated` → ERR_PACKAGE_PATH_NOT_EXPORTED；`providers/all.models` → ERR_MODULE_NOT_FOUND）——0.84.1 内无任何导出形态的轻量权威注册表，无任何旁路。
- **裁决**：重开条件连续第十一轮未满足；所需的是 pi-ai 上游导出，不是仓内改动。此项为审计证据，非机制候选——未构造替代注册表、未触碰回退路径——**不铸 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级即死）

| 扫描角度 | 处置 |
|---|---|
| custom id 跳回退 / 自制 union / 磁盘快照 / 进程内失手 memo | S8-I-1 硬禁 + R11-I/R10-I 三重围栏无 ID 收口；重开条件本轮六测仍未满足；派单明令不得铸 rename ID |
| `smartChildPlan` assignments.find 换 Map（本轮新标定的镜像形态） | S1-I-3 维持排除：生产基数 C=2 零差；越线点 C≈2400 为零生产流量假设格（§1.3；S12-B-2/S13-B-1 条件式落地物同型） |
| children 基数分派双形态 / 按 C 阈值切换数据结构 | 零流量假设格引入分支复杂度；等价证据已备档但落地条件即重开条件，未触发 |
| 高 C 下 stdout 批量 / 输出瘦身 | S3-I-4 逐字（实测慢 85-86 µs + CliIo 调用次数可观测）；本轮 C=50 格显示输出体量非主体 |
| 每 child 事件 I/O 摊薄（攒批/去 fsync/跳读校验） | 切片外（run/ + persist/，G/J 契约）；S9-G-3 / S8-J-2 / S5-G-5 / S6-G-7 fail-closed 族关闭在案 |
| `parseChildSpec` 微观（per-task registry.resolve 提升 / 单遍化） | 线性 ~0.44 µs/task，C=4000 才 1.76 ms；µs 族即死；registry 双建即 S3-I-2（8.4 µs） |
| unknown-model 拒绝 / 错误端点瘦身 | R17-I 六端点定价收口：五端点贴地板、unknown-model 消费 = S8-I-1 健全性反例逐字同型；不补铸 |
| `run --flowchart` 命令类 / 采样归因 / 模块图普查 | R16-I 轴 1/轴 2 收口，前提未变，不重测不补铸 |
| 多 provider 阈值预取 providers/all / 肥配置态 | R14-I 全域支配证明（交叉点不存在），前提复核未变 |
| A/A 地板 / 引擎代 / 堆足迹再测 | R15-I 三轴收口；本轮带比对直接引用其 MDE 包络 |
| `import.meta.resolve` 同步预探测（R13-I 无名微观 #1） | 维持换名拒绝：S4-I-5 族镜像 + 失手锚点九度定界 <0.3 ms/构建 |
| `buildLiveCatalogConfig` 默认别名行 spread（R13-I 无名微观 #2） | 维持换名拒绝：每构建 ≤2 次 ~11 字段 spread（L72/L79），ns-µs 级 S1-I-8 邻域 |
| root / compat 作替代注册表源 | R12-I 直接测量封死（无表 + 严格更重） |
| providers/all 预热重叠 / enabled 循环并行 | S8-I-2 / S8-I-3 逐字（R16-I CPU 剖析为 S8-I-2 第四证人） |
| 回退后 per-provider 探针短路（mixed 态） | R11-I 无 ID 收口（≤3.5 ms/provider；跨调用穿线撞 X1-1） |
| pi-adapter barrel（`index.ts`）隐藏静态边 | 本轮再 grep 为无：全 src/ 无生产导入方 |
| flowchart 双 `buildLiveCatalogConfig` / providers.json 双读 | S1-I-1 / S1-I-2 逐字（warm 锚点 0.059-0.078 ms 旁证） |
| loadLearnedRouting / loadProvidersConfig 死载荷下沉 | S2-I-1 / S3-I-3 逐字（损坏时错误路径发散） |
| pause/inject 免 router / 免 learned；resume/answer 预读去重 / PAUSED 探测降级 | S2-I-2 / S9-G-1 / S2-I-4 / S4-I-2 逐字 |
| compile-cache / 分派拆分 / 剩余独占边 | S6-I-1..3 逐字（once-per-process 亚两位数 ms） |
| dist 布局 / 引擎钉版 | S5-I-2..5 族；R15-I 轴 2 已实测 22.22 取尽红利、24 无增量 |
| 一次性命令类（models/auth/doctor/inspect/episode/commits/adapt/pref/delete） | S1-I-4 / S7-I-4 / S2-I-5 / S3-I-4 类；once-per-process 噪声按派单直接否决 |
| 凭据面（auth status --all、credential store 微观） | S1-I-5 / S7-I-4 围栏，只读证据 |
| buildInvocation / invocationError / parseModelRef / parseProvidersConfig / parseCliErrorJson / setDefaultModels 微观 | S1-I-7 / S2-I-3 / S3-I-1 / S3-I-5 / S2-I-6 / S1-I-6 逐字（实测更慢或 ns 级） |
| pi-executor / cluster-tools 执行面微观 | 网络 I/O 支配的 live 面；µs 级，无复杂度类角度 |
| telemetry INVOCATION_CALL_OUTCOMES includes→Set / compareRunToRun 微观 | 表长 4，S1-D-8 族噪声 / 诊断聚合一次性 |
| writeAtomicJson 去 fsync / doctor-overlay jsonl 扫描 | S9-G-3 族（不得去 fsync）/ S3-J-4 族 + 一次性诊断类 |
| `builtinModelLazy` 动态 specifier / `_MODELS` 键过滤微观 | 命中路径被表解析成本（0.9-3.5 ms）支配、失手路径 ~0.10 ms；µs 族 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖、pi-ai exports、测试面、调用图五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选；唯一数十 ms 结构（custom/mixed 态 providers/all 失手回退）的全部消除形态收敛到同一个上游前提——pi-ai ship 轻量权威子路径——该前提的两种成立形态维持文件级 blocked（本轮第六次直接确认）；本轮新轴进一步证明输入规模维度亦无切片内候选（唯一超线性站点越线点在生产基数三个数量级之外，端到端边际成本 ≥99.8% 是切片外 I/O 契约地板）。

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part A 结构边 7 检查；part D 载荷探针：builtin 构建后 providers/all 保持冷 60.1 ms、custom 回退命中缓存 1.4 ms）。
- **`pnpm gate`（派单项）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2（与 R9-I～R17-I 计数逐字相同）、`pnpm build` 绿。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **引擎注记**：22.14.0 的 doctor engines 预检 fail-closed 为设计行为（R9-I §3 记录在案）；nvm 22.22.2 为 gate/仿真引擎。依 R15-I 轴 1 纪律，候选验收须在 ≥22.19 引擎 + 交错窗上做——本轮无候选，抽测数字仅用于十一轮带比对（多窗中位数，双引擎同号）。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动；无赢家故未提交 `scripts/round18-r18i-equivalence-sim.ts`；临时探针（`r18i-probe.mjs`）测毕即删（gate 前删除，未进 lint 面）。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；本轮新轴以测量收口关闭输入负载规模缺口；S8-I-1 两臂六测维持 blocked；两个历轮无名微观维持换名拒绝）。
- 剩余赢家：无（唯一数十 ms 结构被 S8-I-1 健全性反例 + 硬不变量封死；输入规模维度的唯一超线性站点越线点 C≈2400 为零生产流量假设格；重开条件所需的是 pi-ai 上游导出或 children 基数增长三个数量级，均非仓内改动）。
- 矩阵空洞：无（R8-I 78 格全覆盖；承重格十一轮同构复现 + 抽测集本轮补入基数双格；R7-I 配置态 × 命令类纪律全程遵守——configured 态夹具、builtin/custom 双态、children/track 双命令类 + C=2/10/50 三基数；肥态维持 R14-I 收口、测量学维持 R15-I 收口、命令类与归因维持 R16-I 收口、错误端点维持 R17-I 收口）。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 7 格抽测 harness（/tmp/r18i-bench/spot.mjs，`<node二进制全路径> spot.mjs <同一路径>` 分别以两版本执行；承重 5 格与 R17-I A.1 逐字同构 + 新增 C=10/C=50 基数格）

```javascript
// R18-I spot re-measure: floor + builtin/custom children+track cells
// (R9-I..R17-I harness lineage) + NEW axis: children-spec cardinality cells
// (C=10 / C=50 at builtin — every prior round's spot fixture was C=2;
// the input-workload scale dimension was never end-to-end priced in I).
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r18i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.argv[2];
const WARMUP = 2, ITERS = 10;

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
for (const [name, cfg] of Object.entries(states)) {
  const dir = join(FIX, `state-${name}`);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
}
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
// Children fixtures: the C=2 lineage fixture plus the new C=10 / C=50 scale
// fixtures (roles cycle over real AgentRoles; independent tasks — dependency
// shape belongs to the graph compiler outside this slice).
const ROLES = ["implementer", "reviewer", "tester", "scout", "worker"];
function childrenSpec(count) {
  const tasks = [];
  for (let i = 0; i < count; i++) {
    tasks.push({
      id: `tsk_c${String(i).padStart(4, "0")}`,
      role: ROLES[i % ROLES.length],
      objective: `Do scale-cell work item ${i}`
    });
  }
  return { tasks };
}
const CHILDREN = join(FIX, "children.json");
writeFileSync(CHILDREN, JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");
const CHILDREN10 = join(FIX, "children-10.json");
writeFileSync(CHILDREN10, JSON.stringify(childrenSpec(10)) + "\n");
const CHILDREN50 = join(FIX, "children-50.json");
writeFileSync(CHILDREN50, JSON.stringify(childrenSpec(50)) + "\n");

const cells = [
  // Bearing set (R9-I..R17-I lineage).
  { id: "version", args: () => ["--version"], state: undefined },
  ...["builtin", "custom"].flatMap((state) => [
    { id: `run-children@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] },
    { id: `run-track@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"] }
  ]),
  // NEW axis: children-spec cardinality (builtin state, same command class).
  { id: "run-children@builtin-c10", state: "builtin", args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN10] },
  { id: "run-children@builtin-c50", state: "builtin", args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN50] }
];
function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, `state-${state}`), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 180000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-FAIL ${cell.id}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

原始输出（全部格 exit 0）：

```
== v22.14.0 ==
version: median=54.3ms min=52.9 p90=56.5 exit=0
run-children@builtin: median=108.3ms min=106.6 p90=110.9 exit=0
run-track@builtin: median=125.7ms min=120.8 p90=128.4 exit=0
run-children@custom: median=157.8ms min=149.7 p90=165.1 exit=0
run-track@custom: median=180.9ms min=171.4 p90=195.4 exit=0
run-children@builtin-c10: median=144.4ms min=139.7 p90=147.5 exit=0
run-children@builtin-c50: median=410.5ms min=405.1 p90=418.6 exit=0
== v22.22.2 ==
version: median=51.4ms min=49.3 p90=52.5 exit=0
run-children@builtin: median=107.0ms min=102.9 p90=111.0 exit=0
run-track@builtin: median=129.1ms min=126.6 p90=131.6 exit=0
run-children@custom: median=133.4ms min=129.1 p90=138.4 exit=0
run-track@custom: median=154.1ms min=147.9 p90=157.5 exit=0
run-children@builtin-c10: median=147.8ms min=143.2 p90=163.2 exit=0
run-children@builtin-c50: median=452.3ms min=439.7 p90=480.2 exit=0
```

### A.2 进程内锚点 + 可达性探针 + 基数阶梯（§1.2 / §1.3(b) / §2.2 证据；临时置于 /workspace 根以获得 node_modules 解析，gate 前删除；all/tables/miss/traversal 四模式与 R17-I A.2 逐字同构，`scale` 模式为本轮新增）

```javascript
// R18-I in-process anchors + reachability probes + NEW axis: children-spec
// cardinality ladder (temporary; deleted after measurement).
import { performance } from "node:perf_hooks";
import { mkdirSync, rmSync, writeFileSync } from "node:fs";
import { readFile } from "node:fs/promises";
import { join } from "node:path";
const label = process.version;
const which = process.argv[2];
if (which === "all") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const dt = performance.now() - t0;
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  console.log(`${label} providers/all: cold=${dt.toFixed(1)}ms warm=${(performance.now() - t1).toFixed(3)}ms`);
} else if (which === "tables") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/anthropic.models");
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/openai.models");
  const t2 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const t3 = performance.now();
  console.log(`${label} anthropic.models=${(t1 - t0).toFixed(1)}ms openai.models=${(t2 - t1).toFixed(1)}ms providers/all-after-tables=${(t3 - t2).toFixed(1)}ms`);
} else if (which === "miss") {
  const N = 50; const times = [];
  for (let i = 0; i < N; i++) {
    const t0 = performance.now();
    try { await import("@earendil-works/pi-ai/providers/customco.models"); } catch { /* expected miss */ }
    times.push(performance.now() - t0);
  }
  const first = times[0];
  const rest = times.slice(1).sort((a, b) => a - b);
  console.log(`${label} miss-resolution: first=${first.toFixed(3)}ms repeat-median=${rest[Math.floor(rest.length / 2)].toFixed(3)}ms min=${rest[0].toFixed(3)}ms`);
} else if (which === "traversal") {
  for (const spec of [
    "@earendil-works/pi-ai/providers/../models.generated",
    "@earendil-works/pi-ai/dist/models.generated.js",
    "@earendil-works/pi-ai/models.generated",
    "@earendil-works/pi-ai/providers/all.models"
  ]) {
    try {
      await import(spec);
      console.log(`${label} REACHABLE: ${spec}`);
    } catch (e) {
      console.log(`${label} blocked: ${spec} -> ${e.code ?? e.message.slice(0, 60)}`);
    }
  }
} else if (which === "scale") {
  // NEW axis: slice-attributable cost ladder for the --children input scale.
  // Replicates the O(C)/O(C^2) sites in main.ts verbatim shape:
  //   (1) parseChildSpec (main L273-341): JSON.parse + per-task validation
  //   (2) smartChildPlan routed mapping (main L371-379): assignments.find
  //       quadratic (the S1-I-3 site), plus a Map counterfactual pricing the
  //       excluded candidate's ceiling at each C.
  //   (3) compile prep flatMap (main L733-745).
  // assignTasks / calibrate are timed separately as out-of-slice context.
  const { createAgentProfileRegistry, defaultAgentProfiles } = await import("./dist/agents/registry.js");
  const { DomainValidationError } = await import("./dist/domain/errors.js");
  const { parseTaskId, isArtifactId } = await import("./dist/domain/ids.js");
  const { isAgentRole } = await import("./dist/domain/roles.js");
  const { assignTasks } = await import("./dist/routing/assign.js");
  const { liveCascadePlanFromAssignment } = await import("./dist/routing/live-cascade.js");
  const { buildLiveCatalogConfig } = await import("./dist/cli/model-catalog.js");
  const { calibrateCatalogFromState } = await import("./dist/routing/cost-calibration.js");

  const SR = "/tmp/r18i-bench/scale-state";
  rmSync(SR, { recursive: true, force: true });
  mkdirSync(join(SR, "runtime"), { recursive: true });
  writeFileSync(join(SR, "runtime", "providers.json"), JSON.stringify({
    version: 1,
    enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"],
    primary: "anthropic/claude-fable-5",
    fast: "anthropic/claude-haiku-4-5",
    customProviders: []
  }, null, 2) + "\n");

  // Verbatim-shape replica of main.ts parseChildSpec (L273-341).
  async function parseChildSpecReplica(path) {
    const raw = await readFile(path, "utf8");
    let parsed;
    try {
      parsed = JSON.parse(raw);
    } catch (error) {
      throw new DomainValidationError(`Invalid child spec ${path}: ${error instanceof Error ? error.message : String(error)}`);
    }
    if (typeof parsed !== "object" || parsed === null || !Array.isArray(parsed.tasks)) {
      throw new DomainValidationError("Child spec must be { \"tasks\": [...] }");
    }
    const registry = createAgentProfileRegistry(defaultAgentProfiles());
    const tasks = parsed.tasks;
    const seen = new Set();
    return tasks.map((entry, index) => {
      if (typeof entry !== "object" || entry === null) {
        throw new DomainValidationError(`Child task ${index} must be an object`);
      }
      const task = entry;
      const taskId = parseTaskId(task.id);
      if (seen.has(taskId)) throw new DomainValidationError(`Duplicate child task id: ${taskId}`);
      seen.add(taskId);
      if (typeof task.role !== "string" || !isAgentRole(task.role)) {
        throw new DomainValidationError(`Child task ${taskId}: role must be a known AgentRole`);
      }
      if (typeof task.objective !== "string" || task.objective.trim() === "") {
        throw new DomainValidationError(`Child task ${taskId}: objective must be a non-empty string`);
      }
      const acceptanceCriteria = Array.isArray(task.acceptanceCriteria)
        ? task.acceptanceCriteria.map((criterion) => {
            if (typeof criterion !== "object" || criterion === null) {
              throw new DomainValidationError(`Child task ${taskId}: acceptanceCriteria must be objects`);
            }
            const c = criterion;
            if (typeof c.id !== "string" || c.id === "" || typeof c.description !== "string" || c.description === "") {
              throw new DomainValidationError(`Child task ${taskId}: acceptanceCriteria need {id, description}`);
            }
            return { id: c.id, description: c.description };
          })
        : [];
      const inputArtifactIds = Array.isArray(task.inputArtifactIds)
        ? task.inputArtifactIds.map((id) => {
            if (!isArtifactId(id)) throw new DomainValidationError(`Child task ${taskId}: invalid inputArtifactId`);
            return id;
          })
        : [];
      const limits = task.limits;
      const profile = registry.resolve(task.role);
      const dependsOn = Array.isArray(task.dependsOn) ? task.dependsOn.map((id) => parseTaskId(id)) : undefined;
      return {
        taskId,
        role: task.role,
        objective: task.objective,
        profile,
        inputArtifactIds,
        acceptanceCriteria,
        limits: {
          maxAttempts: typeof limits?.maxAttempts === "number" ? limits.maxAttempts : 1,
          timeoutMs: typeof limits?.timeoutMs === "number" ? limits.timeoutMs : 60_000,
          maxWallTimeMs: typeof limits?.maxWallTimeMs === "number" ? limits.maxWallTimeMs : 3_600_000
        },
        ...(dependsOn !== undefined ? { dependsOn } : {})
      };
    });
  }

  const ROLES = ["implementer", "reviewer", "tester", "scout", "worker"];
  function specFile(count) {
    const tasks = [];
    for (let i = 0; i < count; i++) {
      tasks.push({ id: `tsk_c${String(i).padStart(5, "0")}`, role: ROLES[i % ROLES.length], objective: `Do scale work item ${i}` });
    }
    const path = join("/tmp/r18i-bench", `scale-children-${count}.json`);
    writeFileSync(path, JSON.stringify({ tasks }) + "\n");
    return path;
  }

  const catalog = await calibrateCatalogFromState(await buildLiveCatalogConfig(SR, { primaryModelId: "anthropic/claude-fable-5", fastModelId: "anthropic/claude-haiku-4-5" }), SR);
  const bestOf = (fn, n = 5) => {
    let best = Infinity;
    for (let i = 0; i < n; i++) {
      const t0 = performance.now();
      fn();
      const dt = performance.now() - t0;
      if (dt < best) best = dt;
    }
    return best;
  };
  console.log(`${label} scale ladder (best-of-5 per site, ms):`);
  console.log("C\tparse\tassign*\tfind-map\tmap-cf\tcompile-prep\tfind-only");
  for (const C of [2, 10, 50, 200, 1000, 4000]) {
    const path = specFile(C);
    let parseBest = Infinity;
    for (let i = 0; i < 5; i++) {
      const t0 = performance.now();
      await parseChildSpecReplica(path);
      const dt = performance.now() - t0;
      if (dt < parseBest) parseBest = dt;
    }
    const children = await parseChildSpecReplica(path);
    const assignable = children.flatMap((child) =>
      isAgentRole(child.role) ? [{ taskId: child.taskId, role: child.role, objective: child.objective }] : []
    );
    let assignments;
    const assignBest = bestOf(() => { assignments = assignTasks({ tasks: assignable, catalog }); });
    // (2) routed mapping replica — S1-I-3 site verbatim shape.
    const findMapBest = bestOf(() => {
      children.map((child) => {
        const assignment = assignments.find((item) => item.taskId === child.taskId);
        if (assignment === undefined) return child;
        return { ...child, assignedModel: assignment.decision.model, cascade: liveCascadePlanFromAssignment(assignment, catalog) };
      });
    });
    // Map counterfactual (the S1-I-3 excluded candidate) — same outputs.
    const mapCfBest = bestOf(() => {
      const byId = new Map(assignments.map((item) => [item.taskId, item]));
      children.map((child) => {
        const assignment = byId.get(child.taskId);
        if (assignment === undefined) return child;
        return { ...child, assignedModel: assignment.decision.model, cascade: liveCascadePlanFromAssignment(assignment, catalog) };
      });
    });
    // Pure find quadratic (no cascade), isolating the scan itself.
    const findOnlyBest = bestOf(() => {
      for (const child of children) assignments.find((item) => item.taskId === child.taskId);
    });
    // (3) compile prep flatMap replica (main L733-745).
    const catalogIds = catalog.models.map((model) => model.id);
    const routed = children.map((child) => {
      const assignment = assignments.find((item) => item.taskId === child.taskId);
      return assignment === undefined ? child : { ...child, assignedModel: assignment.decision.model };
    });
    const prepBest = bestOf(() => {
      routed.flatMap((child) => {
        if (!isAgentRole(child.role)) return [];
        return [{
          taskId: child.taskId, role: child.role, objective: child.objective,
          ...(child.dependsOn !== undefined ? { dependsOn: child.dependsOn } : {}),
          allowedModels: catalogIds,
          ...(child.assignedModel !== undefined ? { preferredModel: child.assignedModel } : {})
        }];
      });
    });
    console.log(`${C}\t${parseBest.toFixed(3)}\t${assignBest.toFixed(3)}\t${findMapBest.toFixed(3)}\t${mapCfBest.toFixed(3)}\t${prepBest.toFixed(3)}\t${findOnlyBest.toFixed(3)}`);
  }
}
```

原始输出（anchors：每模式全新进程，`all` 每引擎 3 次；traversal 双引擎逐字一致；scale 双引擎各一次）：

```
v22.14.0 providers/all: cold=57.1 / 79.5 / 46.0 ms  warm=0.059-0.077 ms
v22.22.2 providers/all: cold=31.0 / 31.5 / 31.7 ms  warm=0.065-0.078 ms
v22.14.0 anthropic.models=3.5ms openai.models=2.2ms providers/all-after-tables=54.4ms
v22.22.2 anthropic.models=2.5ms openai.models=0.9ms providers/all-after-tables=26.9ms
v22.14.0 miss-resolution: first=1.959ms repeat-median=0.097ms min=0.063ms
v22.22.2 miss-resolution: first=2.014ms repeat-median=0.102ms min=0.060ms
v22.14.0 blocked: @earendil-works/pi-ai/providers/../models.generated -> ERR_INVALID_MODULE_SPECIFIER
v22.14.0 blocked: @earendil-works/pi-ai/dist/models.generated.js -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/models.generated -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/providers/all.models -> ERR_MODULE_NOT_FOUND
v22.22.2 blocked: (同上四行，双 Node 逐字一致)
v22.14.0 scale ladder (best-of-5 per site, ms):
C	parse	assign*	find-map	map-cf	compile-prep	find-only
2	0.101	0.068	0.009	0.007	0.003	0.001
10	0.106	0.129	0.031	0.018	0.003	0.002
50	0.132	0.379	0.109	0.087	0.014	0.025
200	0.233	1.633	0.355	0.177	0.055	0.173
1000	0.759	3.772	2.951	0.760	0.395	3.903
4000	1.756	9.710	31.407	3.127	0.195	28.697
v22.22.2 scale ladder (best-of-5 per site, ms):
C	parse	assign*	find-map	map-cf	compile-prep	find-only
2	0.120	0.056	0.007	0.007	0.002	0.001
10	0.117	0.122	0.028	0.018	0.003	0.002
50	0.121	0.378	0.112	0.087	0.014	0.025
200	0.256	1.313	0.312	0.175	0.053	0.145
1000	0.771	3.946	3.177	0.764	0.336	3.607
4000	1.762	9.527	31.747	3.138	0.213	28.321
```

MORE_OPTIMA=no
