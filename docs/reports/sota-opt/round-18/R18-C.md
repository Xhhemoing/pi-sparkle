MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 18 / R18-C：离线路由切片第十八遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 18 / R18-C（Round 1–17 同区第十八遍，叠在
  已落地的 S7-C 之上）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `8eb7bca`（R18-A 空枚举合入 +
  R18-C 派单后的独占分支最新 tip，与派单预期一致；相对 R17-C 基线 `d0bab62`
  的区间为**纯文档合入**（R17-B..R17-I 空枚举收口、R17-J/R18-A/R18-B/R18-C
  派单、Round 18 PLAN——`git log d0bab62..8eb7bca --name-only` 仅
  `docs/reports/sota-opt/` 14 个文件）。`git log -1 -- src/routing/` 显示切片
  最后一次生产改动**仍为 `183df9b`「perf(routing): land S7-C support-only
  eta」**；`git diff 183df9b..HEAD --` 对 9 个切片文件为**空**（stat 输出为
  空、exit 0），本轮全部测量在该形态之上，未回退未重写。R17-J / R18-B 在飞，
  未等待、未触碰其分支与文件）
- **分支:** `cursor/r18-c-offline-routing-eighteenth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十八组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第十八遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R17-C 的全部无号关闭
探针与轴，含 R17-C 的 NSQRT/PMV/OSTZ）后，本轮找到**一个从未点名的
零池守卫复合站点**并新增**一条完备性轴**（他区 R17-D/R17-H/R18-A
先例、C 区首次），全部以关闭探针/轴（不铸号）收口——没有任何一个
达到接受门槛：

- **TAILG（`fitLogitAdditive` 尾部效应环守卫复合体，关闭探针，不铸
  号）**——尾部环 `for (const [name, point] of pointEffects.entries())`
  内的三重守卫从未被点名：(a) 第一析取
  `successful < MIN_SUCCESSFUL_DRAWS` 是**环不变式**却按 name 重评
  （61 次/报告，每次亚 ns）；(b) 第二析取
  `values.length < MIN_SUCCESSFUL_DRAWS` 在被评估时**可证恒假**——
  每个成功 draw 对 pointEffects 的每个 name 恰好 push 一次值，且
  pointEffects 在 bootstrap 环之前已完整填充（含参考水平）、draws
  只含 pointEffects 的键，故对每个 name 恒有
  `values.length === successful`；第一析取假 ⇒ successful ≥ 20 ⇒
  第二析取假；(c) `draws.get(name) ?? []` 回退仅当 successful === 0
  可达（此时首个 name 即触发拒绝返回）。运行时见证（仪表道，三次
  正典运行逐位一致）：**names=61 / fallback 命中 0 / 第二析取真 0 /
  `values.length !== successful` 违例 0**。整环位于已测排序/分位尾
  池（本轮 1.5–1.6 ms）内，站点池为零到纳秒级——三重守卫按
  S9-D-3 类「可证死但零成本保留」记防御纵深备忘（行为一位不动），
  非候选。
- **REJX（fail-closed 拒绝终点普查，新轴，非候选；R17-D/R17-H/
  R18-A 他区先例，C 区首次）**——静态普查 9 文件共 **31 个拒绝
  终点**：15 个 `DomainValidationError` 抛点（lin-alg 1、prob-add 2、
  bandit 6、shadow 1、r1-shadow-report 5）+ 16 个 fail-closed 值
  终点（lin-alg null ×3、irls null ×2、fitLogitAdditive
  uncertainReport ×3、posterior 钳位 ×2、propensity
  INVALID_ESTIMATE ×3、r1 R0 拒绝/稀疏回退/无过地板回退 ×3）。
  代表性定价（三次运行）：12 个前置卫/钳位/空输入终点全部
  **0.11–8.5 µs/fault**（once-per-fault，非池）；唯一 ms 级终点是
  **<20 成功 draws 尾门 20.6–20.7 ms/fault（bootstrap=5 生产形状）**，
  其成本是**冻结管线前缀本体**（1 次 base fit + 5 次 boot fit——
  全部是已冻结的浮点序列，非可省站点）。结论：拒绝面无隐藏池、
  无候选道；fail-closed 结构（X0-10 / ADR-005 姿态）零触碰。
  本轴一次性收口。
- **APC 池地板重锚定（按 R14-C 处置规则，非候选、非重开、非堆
  叠）**——本轮三次仪表化 APC 池读数 **47.6 / 51.0 / 47.4 ms**，
  run B 的 51.0 **漂出 R17-C 重锚定带 46.9–49.3 的上沿**（R14-C
  规则对双向越带均触发，R14-C 本身即上漂触发例），按规随池重锚
  地板：APCX 无计时器独立道 **54.2 / 53.3 / 57.9 ms**，FLOORX
  最廉价逐位合法形态（S8-C-2 型支撑升序 off 和——仅作地板测量，
  S8-C-2/S10-C-2/S2-C-4/S4-C-4 全部维持关闭）地板 **33.3 / 41.3 /
  41.0 ms** ⇒ **组合天花板 = 池 − 地板 = 20.9 / 12.1 / 16.9 ms
  < 35**，嵌在 R16-C 直测带 12.1–23.8 与 R17-C 带 13.8–21.6 的
  并集内。**sink=7.309 与 floorSink=7.309 六读数全同，且与
  R14-C/R16-C/R17-C 逐位同值**（派单强制的跨轮确定性旁证成立）。
  组合关闭维持。

除上述站点与轴外，第十八遍逐行重读**零新发现**：其余每一个执行
站点都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 / 结构
交换负优化）。三大池地板与组合天花板复核见 §3：本 VM 上生产中位
**656.7–672.4 ms/报告（三次正典运行 664.6 / 672.4 / 656.7）**，
run A/B 在派单 R14-C 锚定带 657.6–689.3 内，run C 的 656.7 低于带
下沿 0.9 ms（0.14%，处于 R15-C AAFLR 中位级分辨率地板 ±1.5 ms
之内）；同 VM 独立旁证 r7c 回归资产锚点 **production 662.0 ms =
1.13× vs 冻结 S6-C ctl 747.1** 在带内互证——判为 VM 微架构抖动
贴带下沿，非切片变化（solve 池 455.3–497.5 与 R16-C 473.3–484.8 /
R17-C 453.0–460.6 两带交叠，run B 为可见漂移窗口：单 rep 漂至
714.3）。小形状档整报告中位 **32.3–33.9 ms**，整条报告低于噪声带
（R7-I 矩阵义务履行）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11），live 面零文件改动。仓库变更仅本报告一个文件；无生产赢家故
未提交 `scripts/round18-r18c-equivalence-sim.ts`（败者/锚点仿真全文进
附录，遵守 R7-C..R17-C 先例）。

## 0. 范围与约束遵守

- 读序（强制）已履行：README → EXCLUSIONS.md（完整表，含全部
  S\*-C-\* 行与 S12-C-1、S13-B-1）→ PROGRESS.md → round-18/PLAN.md →
  R17-C.md（空枚举确认，无 S17-C-\*；NSQRT/PMV/OSTZ 关闭探针与
  APCX/FLOORX 重锚定记名）→ R16-C.md（ICOL/SFILL 与 ITERX/COLDX 轴、
  PLSEG 备忘记名）→ R15-C.md（STORD/RIDGE 与 AAFLR/GCAX 轴记名）→
  R13-C/R14-C 收口速读（PIVCSE/FINTAIL/REFZ/TSORT/DEG 与
  SWORD/XF64/IRLSDP/CMPH + APCX/TIMER/FLOORX 方法）→ R12-C
  （S12-C-1 + FZERO/PIVSWAP/BASE/SAMP/MUCSE）与 R11-C（RID/CNT/
  AUGF/EXP/ALLOC/DMAP/COPY/SCAN）探针表逐条核对 → 9 个切片源文件
  全量重读。
- 基线漂移检查：基线 `8eb7bca` 相对 R17-C 基线 `d0bab62` 仅文档
  合入（`git log d0bab62..8eb7bca --name-only` = 14 个
  `docs/reports/sota-opt/` 文件，零 `src/` 触碰）；
  `git log -1 -- src/routing/` 仍为 `183df9b`（S7-C 落地本身），
  `git diff 183df9b..HEAD --` 对 9 文件为空（stat 空输出、exit 0），
  落地形态原样在位。既有回归资产七连全绿且检查数逐一匹配锚定值
  （§5）。
- 调用图确认（派单强制项，本轮 rg 复核）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 消费（双归因两路在位）；
  `solveSymmetric` 仅被 `offline-logit.ts` 导入；`src/supervisor/`
  对 9 个切片文件的导入为**零**——live = R0 等价维持，R1 未接线，
  `src/supervisor/model-router.ts` 零字节未触。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S13-\*（含 S12-C-1，重开条件：无）
  未重提、未立道、未重测；R13-C 五个、R14-C 四个、R15-C 两个、
  R16-C 两个、R17-C 三个无名微观（PIVCSE/FINTAIL/REFZ/TSORT/DEG/
  SWORD/XF64/IRLSDP/CMPH/STORD/RIDGE/ICOL/SFILL/NSQRT/PMV/OSTZ）
  未重探未补铸未换址重提；ITERX/COLDX/AAFLR/GCAX 四轴未补铸
  （本轮无决斗读数——TAILG 是零池守卫见证非决斗、REJX 是终点
  定价非决斗；GC/预热轴未重测；带下沿判读引用 AAFLR 已留档地板
  ±1.5 ms 作校准，未重测 A/A）；RID/CNT 归约重排反例原样引用；
  嵌套序 × 布局组合空间（S4-C-2 + S5-C-1..4 + S12-C-1）未重开；
  CMB 组合未重堆叠（本轮 APCX/FLOORX 重测遵循 R14-C 池漂出带处置
  规则，是地板测量非堆叠候选）；PLSEG/PMV 备忘未重提（TAILG 是
  不同站点的同类新备忘，见 §4）。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动；X2-3 数值路径未触；单线程逐位审计基底不动。
- 环境：Node 22.22.2（本 VM 默认即 22.22.2，满足 engines ≥22.19.0
  ——历轮需 nvm 从 22.14.0 切换的处理本轮不再需要）、pnpm 10.17.1、
  依赖已就绪（`pnpm gate` 全绿，见 §5）。

## 1. 重剖析：S7-C 后成本模型（本 VM 实测，未拷贝历轮数字）

测量器为**仪表化逐字复刻管线**（与 183df9b 生产 irls/solve/on-prob/
APC 逐语句相同，仅在调用边界加 performance.now() 与计数器，浮点操作/
操作数/顺序零改动；本轮另在尾部环加 TAILG 普查计数器——只读已算出
的值，不触任何浮点），其忠实性先经 **53 例全形状电池 ×{仪表道,
纯净落地道} vs 生产 `fitLogitAdditive` 导入逐位全绿**（Object.is 含
符号零）再采信读数；生产中位数由**未仪表化生产导入**直接测得。perf
夹具与 R10-C..R17-C 完全一致（fixtureRng(0xbeef)，4 场景 × 6 模型 ×
8 项目，rows=400，passRate=0.6，bootstrap=200, seed=20260818 →
p=60，201 拟合，8,966 次 IRLS 迭代）。

生产整报告中位（每次独立进程 7 测量取中位，三次正典运行）：**664.6 /
672.4 / 656.7 ms**——run A/B 在派单 R14-C 锚定带 657.6–689.3 内；
run C 的 656.7 低于带下沿 **0.9 ms（0.14%）**，幅度处于 AAFLR 中位级
分辨率地板（±1.5 ms，R15-C）之内（同 VM 独立旁证：r7c 回归资产锚点
**production 662.0 ms = 1.13× vs 冻结 S6-C ctl 747.1**，与中位同带
互证）；判为 VM 微架构抖动贴带下沿，非切片回归或加速。run B 为可见
漂移窗口（perf reps 含 714.3 / 687.7，池表整体上移）。

仪表化池表（perf 形状，每次运行内 3 次仪表化取中位；三次正典运行
读数并列）：

| 池 | run A | run B | run C | 占比 | 站点计数（精确） |
| --- | --- | --- | --- | --- | --- |
| 总计 | 685.3 | 729.8 | 677.6 | 100% | fits=201 |
| `solveSymmetric` | **468.3** | **497.5** | **455.3** | **67.2–68.3%** | 8,966 次调用 |
| `irls` 自身（扣 solve） | **155.9** | **166.7** | **153.7** | **22.7–22.8%** | 8,966 迭代 / 3,586,400 行访问 / 1,249,652 sigmoid |
| APC 全站点 | 47.6 | 51.0 | 47.4 | 6.9–7.0% | 277,606 活跃对 / 12,259 次调用 |
| `onProbabilitiesFor` | 7.0 | 7.5 | 6.8 | 1.0% | 80,400 sigmoid+dot |
| bootstrap 采样 | 1.7 | 1.8 | 1.7 | 0.2% | —（SAMP 池 R12-C 已封顶） |
| 排序/分位尾 | 1.6 | 1.6 | 1.5 | 0.2% | 12,200 draws-map ops；**TAILG 普查宿主** |
| 基行投影 | 0.006 | — | — | ~0% | 每报告 1 次（R12-C BASE 已封顶） |
| 编排余量（build/supports/keys） | 3.3 | — | — | 0.5% | — |

除 solve / irls / APC 外每池整池清零也低于 ±35 ms 带（R9-C..R17-C
同结论，逐池复测未漂移）；solve 与 irls 为落地线唯二宿主。全部站点
计数与历轮锚定值逐一相同（8,966 / 3,586,400 / 1,249,652 / 277,606 /
12,259 / 12,200 / 80,400 / 201——跨轮确定性旁证）。**APC 池本 VM
三次读数 47.4–51.0，run B 漂出 R17-C 重锚定带 46.9–49.3 的上沿**——
按 R14-C §3 末处置规则随池重锚地板（§3，天花板 12.1–20.9 < 35，
关闭维持且 sink/floorSink 六读数逐位同值）。小形状档（3 模型 ×
3 项目 / 120 行，bootstrap=200）整报告中位 **33.9 / 33.3 / 32.3 ms**
——小配置态整条报告低于噪声带，调用矩阵小态一侧整体收口（R7-I
矩阵义务履行）。

生产自确定性复核：3 seed × 各 2 次运行全字段 Object.is 全绿（含于
§5 的 6,042 项检查 × 3 次运行）。

## 2. 第十八组枚举（诚实结果：空；一个无号关闭探针 + 一条新轴 + 一次按规地板重锚）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过。** 从未点名的站点全部在下表关闭（无一铸号；探针数字
为本 VM 实测，出处为附录测量彩排三次正典运行 + 三次地板加测）：

| 站点/轴（从未点名） | 关闭方式 | 证据 |
| --- | --- | --- |
| TAILG：`fitLogitAdditive` 尾部效应环守卫复合体——(a) 环不变式第一析取按 name 重评 61 次/报告；(b) 第二析取 `values.length < 20` 被评估时可证恒假（`values.length === successful` 对每个 name 恒成立：pointEffects 先于 bootstrap 环完整填充、每成功 draw 每 name 恰一 push、draws 无额外键）；(c) `?? []` 回退仅 successful === 0 可达 | 零池 + S9-D-3 类防御纵深备忘 | 运行时见证（仪表道，三次运行逐位一致）：names=61 / fallback 命中 **0** / 第二析取真 **0** / `values.length !== successful` 违例 **0**；61 次亚 ns 比较 + 整环位于已测排序/分位尾池 **1.5–1.6 ms** 内——守卫消除是零收益纯重构且删防御纵深（本战役行为一位不动），非候选 |
| REJX：fail-closed 拒绝终点普查（新轴，非候选；C 区首次，R17-D/R17-H/R18-A 他区先例） | 终点定价全部 once-per-fault，无池无道 | 静态普查 **31 终点** = 15 抛点（`lin-alg.ts:26`；`offline-prob-add.ts:93,98`；`bandit.ts:24,29,33,36,85,110`；`shadow.ts:159`；`r1-shadow-report.ts:73,82,106,135,153`）+ 16 值终点（`lin-alg.ts:44,81,85` null；`offline-logit.ts:335,339` irls null 与 `430,439,502` uncertainReport；`posterior.ts:97,98` 钳位；propensity 空日志/supportOk 假/validate 状态；`r1.ts` R0 拒绝 + 双回退）。代表定价（run A；run B/C 抽测同带）：solve 非方阵抛 **3.37 µs**、solve 奇异 null（60×60）**8.47 µs**、空行 uncertain **0.41 µs**、全 PASS 退化（400 行）**7.0 µs**、prob-add 空抛 **2.56 µs** / 双场景抛 **4.73 µs**、bandit 不可观测特征抛 **3.27 µs**、shadow 无臂抛 **2.54 µs**、shadow-report 空抛 **2.66 µs**、propensity 空 INVALID **0.12 µs**、beta 钳位 **0.11 µs**、r1 R0 拒绝早返 **0.34 µs**；唯一 ms 级终点 = **<20 draws 尾门 20.6–20.7 ms/fault（bootstrap=5 生产形状）**，成本是冻结管线前缀（1 base + 5 boot fit）本体而非可省站点。拒绝面无隐藏池；fail-closed 结构零触碰；轴一次性收口 |
| APCX/FLOORX 地板重锚定（非候选、非重开、非堆叠；R14-C 处置规则触发于 run B 51.0 > 带上沿 49.3——双向越带均触发，R14-C 即上漂触发例） | 重锚后天花板仍亚带 + sink 逐位同值 | APCX **54.2 / 53.3 / 57.9**；FLOORX **33.3 / 41.3 / 41.0** ⇒ 天花板 **20.9 / 12.1 / 16.9 < 35**，嵌在 R16-C 带 12.1–23.8 内、与 R17-C 带 13.8–21.6 交叠；**sink=7.309 与 floorSink=7.309 六读数全同，且与 R14-C/R16-C/R17-C 逐位同值**——派单强制的 APC 关闭维持条件（sink 逐位匹配）成立；S8-C-2/S10-C-2/S2-C-4/S4-C-4 与 CMB 禁令全部维持原状 |

以上无一构成候选：TAILG 池为零（守卫删除是重构不是性能候选，且
本战役行为一位不动）；REJX 是终点普查轴不是变换（once-per-fault
µs 级 reject 类 = S12-D-1 同域拒绝理由的轴化收口）；APCX/FLOORX
是按规地板测量不是变换。**故第十八组候选集为空，不铸 S18-C-1 及
后续编号**，遵循 R11-C..R17-C 无号关闭先例。

**枚举收口：** 除上表外，第十八遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖；本轮
重读零新发现——posterior 的 80 次二分 × betacf 是 X1-3 冻结、
lnGamma 系数是 X3-4、betaInterval 双 lnBetaFunction 在 S1-C-7 亚 ms
池内、`updatePosterior` 的 `Math.pow` 衰减只在 R1 估计面且被
`estimateForKey` 备忘录吸收（OSTZ 同域，亚 µs/报告）；prob-add 的
kappaS/cell 双算是 S1-C-6 本体、死 shrink 是 PMV、诊断环自引用过滤
是 PLSEG，整报告池 PMV 锚点 1.14–1.19 ms 覆盖；propensity 的多遍
扫描是 S1-C-8/X3-5 同族；bandit 的 `OBSERVABLE_FEATURE_KEYS.includes`
六键表与 spread 拷贝、shadow 的 decisions 追加拷贝是 S1-C-9/X4-2
辖区；r1 的 `costOf`/`tierIndex` 线性查找是 X1-4、request spread 是
S1-C-10、merge 路径是 S1-C-2/3、estimateFingerprint 是 S1-C-4）。
`offline-logit.ts` 与 `lin-alg.ts` 的剩余站点经 R17-C NSQRT 收敛尾
收口后，本轮唯一从未点名的位置就是 TAILG 尾门守卫（收敛尾 delta
融合 S2-C-3 / isFinite S8-C-3 / sqrt NSQRT 三面已闭；尾部环本体
TSORT/CMPH/REFZ/DMAP 已闭；TAILG 补上守卫复合体这最后一个从未点名
位置——尾部段至此按「排序 / 比较器 / 参考水平 / map 流量 / 守卫」
五面收口）。本轮核实换名再提交拒列：零——没有任何已关 ID 的
换址/换名形态在本轮出现。

## 3. 三大池地板与组合出路复核

- **solve 池 455.3–497.5 ms（本 VM 三读数；与 R17-C 带 453.0–460.6
  和 R16-C 带 473.3–484.8 交叠，run B 497.5 出自可见漂移窗口，与
  生产中位同幅同向 = VM 漂移非回归）**：消元主体 ≈ 6.45 亿次冻结
  顺序浮点乘减/报告。R6-C §2.5 七类分解 + S10-C-3 + S12-C-1
  （嵌套序，稳定 ~3.6× 负优化，重开条件：无）+ R11-C COPY + R12-C
  FZERO/PIVSWAP + S5-C-6 + R13-C PIVCSE/FINTAIL + R14-C XF64 +
  R16-C SFILL 之后，本轮**零新位置**（TAILG 在 fitLogitAdditive
  尾部段）。ITERX（R16-C）坐实池量线性依赖 8,966 次冻结迭代，
  收敛缩短类无逐位合法形态。触碰地板本体的机制只剩 SIMD/线程
  （S9-C-1/2 + 不变量禁止）与数值路径改道（X2-3 冻结）。
- **irls 自身池 153.7–166.7 ms**：可省项全部已关闭（S2-C/S6-C/S7-C
  落地本体之上：S10-C-1 决斗带内、S9-C-3 符号翻转、S6-C-6 上界
  2–4、S5-C-7 无正决斗、S9-C-4、S4-C-5/6、S2-C-3、S8-C-3、R11-C
  RID/CNT/EXP/ALLOC、R12-C MUCSE、R14-C SWORD/IRLSDP、R15-C
  STORD/RIDGE、R16-C ICOL、R17-C NSQRT）。本轮零新位置。
- **APC 池 47.4–51.0 ms（本 VM，run B 漂出 R17-C 重锚定带
  46.9–49.3 的上沿）**：按 R14-C §3 末处置规则随池重锚地板（这不
  是重跑已关 ID 的决斗，也不构建任何堆叠候选道）：APCX 无计时器
  独立道 **53.3–57.9 ms**（基形状近似，**sink=7.309 与
  R14-C/R16-C/R17-C 逐位同值** = 跨轮确定性旁证，派单强制项成
  立）；FLOORX 最廉价逐位合法形态地板 **33.3–41.3 ms** ⇒ **组合
  天花板 = 池 − 地板 = 12.1–20.9 ms（三次读数）< 35**，嵌在
  R16-C 直测带 12.1–23.8 内。两轮间池带 46.9–49.3 → 47.4–51.0 的
  摆动为 VM 漂移双向往返（R16-C 48.5–50.9 → R17-C 46.9–49.3 →
  本轮 47.4–51.0），天花板三轮读数 12.1–23.8 / 13.8–21.6 /
  12.1–20.9 稳定亚带，组合关闭维持。R13-C REFZ 参考水平份额不受
  影响。
- **引擎侧三轴（R15-C/R16-C 收口，本轮未重测未补铸）**：COLDX
  预热份额、GCAX 收集器份额、ALLOC 分配封顶——三轴均无切片内
  可受理变换，稳态口径下三大池冻结结论不受影响；本轮中位口径
  仍为稳态口径。
- **拒绝面（本轮新轴收口）**：REJX 31 终点全部 once-per-fault——
  前置卫/钳位/空输入 0.11–8.5 µs，尾门 20.6–20.7 ms 付的是冻结
  前缀本体。拒绝路径不构成第五类成本：其 µs 级终点是
  once-per-fault 拒绝类（与 S12-D-1 的 once-per-command 拒绝同域
  裁决），其 ms 级终点的成本载体是已冻结的管线前缀。四分类
  （重命名 / 亚带 / 逐位违例 / 结构交换负优化）+ once-per-fault
  拒绝类继续穷尽。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测 +8.8~+30.5 带级
  摆幅带内），维持关闭。本轮探针无一是稳定越带正项（TAILG 零池、
  REJX 是终点定价、FLOORX 是地板不是候选），无新成员可入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建面
开放 / X2-3 数值路径解冻 / 单线程审计基底变更——均不预期）；
（b）现实规模位移 ≥1 个数量级使亚带池放大越带；或（c）指出
「重命名 / 亚带 / 逐位违例 / 结构交换负优化 / once-per-fault 拒绝」
之外的第六类成本——十八遍枚举未再发现。

## 4. 重命名检查（派单强制项）

- **TAILG vs DEG / TSORT / CMPH / REFZ / DMAP / S7-C-1 / S9-D-3 /
  PMV / OSTZ**：DEG（R13-C）是**基行退化判定**的双 every 融合
  （管线头部）；TSORT 是尾部**排序实现**替换；CMPH 是尾部**排序
  比较器闭包**；REFZ 是**参考水平 draws 值恒 +0** 的份额（值的
  内容，非守卫）；DMAP 是 draws **map 流量**；S7-C-1 是 bootstrap
  **draw 环**内 `pointEffects.entries()` 改 keys()（不同环）；
  S9-D-3（D 区）与 PMV/OSTZ（本区 R17-C）是「可证死但零成本保留」
  **分类先例**，站点分别在 adaptation 晋升链、prob-add shrink、
  posterior 死形参。TAILG 只问**尾部效应环守卫复合体**（不变式
  重评 + 可证恒假第二析取 + 死 `?? []` 回退）——该结构位置此前
  无任何 ID 或探针覆盖，非重命名；以运行时三见证（0/0/0）+ 尾池
  算术关闭，按 S9-D-3/PMV/OSTZ 先例记备忘不铸号。
- **REJX vs ITERX / COLDX / AAFLR / GCAX / ALLOC / S12-D-1**：
  前五者是收敛量 / 预热 / 决斗地板 / GC / 分配的**测量轴**；
  S12-D-1 是 D 区单个 once-per-command 拒绝站点的**候选裁决**。
  REJX 是 C 区**拒绝终点全集**的普查与定价轴（31 终点，含抛点与
  fail-closed 值终点），此前从未在 C 区做过（R17-C 及以前的中位/
  池口径全部是成功路径口径）；其结论是轴级收口而非候选裁决，
  不与任何已关 ID 重叠。
- **APCX/FLOORX 重测 vs S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 / CMB
  禁令**：本轮重测**只因池漂出带**（R14-C 处置规则；上漂触发与
  R14-C 本例同向），产出仅用于天花板差值算术与 sink 逐位核对；
  不重跑任何已关 ID 决斗、不构建堆叠候选、不产出可落地形态，
  四个成员 ID 与 CMB 禁令全部维持原状。
- **换名再提交拒列（本轮核实）**：零。R13-C..R17-C 十六个无名微观
  未重探、未补铸、未换址重提；S10-C-1..3 / S12-C-1 / RID / CNT
  零重提；ITERX/COLDX/AAFLR/GCAX 四轴未补铸；嵌套序 × 布局组合
  空间未触碰；X2-1 重写维持排除（S7-C 的 0/1 形态即落地形态，
  未另起平行实现）。

## 5. 测试与验证

环境：Node 22.22.2（本 VM 默认即满足 engines ≥22.19.0）、
pnpm 10.17.1。生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录）
npx tsx /tmp/r18c-anchor.mts                # ✓ 6,042 项逐位 × 3 次正典运行全绿
                                            #   （53 例 ×{仪表道,落地道} 忠实性电池
                                            #    + 3 seed × 2 自确定性 + perf 夹具逐位
                                            #    + TAILG 普查 + REJX 终点定价）
npx tsx /tmp/r18c-apcfloor.mts              # ✓ APC 地板重锚定加测 × 3（APCX/FLOORX，
                                            #   遵循 R14-C 池漂出带处置规则；
                                            #   sink=7.309 / floorSink=7.309 六读数逐位同值）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 662.0 ms（1.13× vs 冻结 S6-C ctl 747.1），与 §1
# 生产中位（656.7–672.4）同带。
```

代表性一次测量彩排全量输出（正典 run A；run B/C 数字见 §1 池表与
§2）：

```text
scenario 1 (two-lane bitwise fidelity, 53 cases x {instrumented, plain-landed} vs production fitLogitAdditive import)
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)
  shape small (3 models x 3 projects, 120 rows): production median 33.9 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 664.6 ms/report (7 reps)
  perf reps: 672.8 663.2 675.1 663.7 661.7 664.6 678.0
instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):
  total                 685.3 ms
  solveSymmetric        468.3 ms  (68.3%)  [8966 calls]
  irls minus solve      155.9 ms  (22.7%)  [8966 iterations, 3586400 row visits, 1249652 irls sigmoids]
  onProbabilitiesFor    7.0 ms  (1.0%)  [80400 sigmoid+dot]
  APC (all sweeps)      47.6 ms  (6.9%)  [277606 active pairs, 12259 calls]
  bootstrap sampling    1.7 ms  (0.2%)
  sort/percentile tail  1.6 ms  (0.2%)  [12200 draws-map ops]
  base-row projection   0.006 ms  (once/report)
  outside pools (build/supports/keys/base copies) 3.3 ms  (0.5%)
  fits 201
probe TAILG: final-loop guard census — names/report = 61, `draws.get(name)` fallback hits = 0 (reachable only when successful === 0), second-disjunct (values.length < 20) true-with-first-false = 0, values.length !== successful violations = 0 — the invariant first disjunct re-evaluates 61x/report (sub-ns each) and the whole loop sits inside the measured sort/percentile tail pool above; zero-pool defense-in-depth keep (S9-D-3 class), not a lane
scenario REJX (fail-closed rejection endpoint pricing; once-per-fault, medians):
  solveSymmetric non-square throw            3.37 us/fault
  solveSymmetric singular -> null (60x60)    8.47 us/fault
  fitLogitAdditive empty rows -> uncertain   0.41 us/fault
  fitLogitAdditive all-pass degenerate (400) 7.0 us/fault
  fitProbabilityAdditive empty rows throw    2.56 us/fault
  fitProbabilityAdditive 2-scenario throw    4.73 us/fault
  bandit unobservable-feature throw          3.27 us/fault
  createShadowState no-arms throw            2.54 us/fault
  buildR1ShadowReport empty-episodes throw   2.66 us/fault
  computeOverlapDiagnostics empty -> INVALID 0.12 us/fault
  betaQuantileLcb alpha<=0 clamp -> 0        0.11 us/fault
  routeR1 R0-refused early return            0.34 us/fault
  fitLogitAdditive <20-draws reject (perf shape, bootstrap=5) 20.6 ms/fault (frozen prefix: 1 base fit + 5 boot fits)
APC pool median this run: 47.6 ms (R17-C band 46.9-49.3; re-anchor floor only if outside)

ALL EQUIVALENCE CHECKS PASSED (6042 bitwise checks)
```

其余两次正典运行与地板加测的关键数字：

```text
run B: prod median 672.4 (small 33.3; perf reps 672.4 664.8 667.1 660.9 687.7 714.3 679.9);
       pools solve 497.5 / irls-other 166.7 / APC 51.0; TAILG 61/0/0/0;
       REJX 抽测 non-square 2.00 us / singular-null 5.88 us / <20-draws 20.6 ms
run C: prod median 656.7 (small 32.3; perf reps 663.0 654.9 656.3 656.7 653.9 658.7 662.0);
       pools solve 455.3 / irls-other 153.7 / APC 47.4; TAILG 61/0/0/0;
       REJX 抽测 non-square 2.08 us / singular-null 5.89 us / <20-draws 20.7 ms
APC 地板重锚定加测 × 3: APCX 54.2 / 53.3 / 57.9; FLOORX 33.3 / 41.3 / 41.0;
       ceiling 20.9 / 12.1 / 16.9 (全部 < 35; sink=7.309 与 floorSink=7.309
       六读数全同，且与 R14-C/R16-C/R17-C 逐位同值)
全部站点计数（8,966 / 3,586,400 / 1,249,652 / 277,606 / 12,259 / 12,200 /
80,400 / 201）与 TAILG 普查（61/0/0/0）在三次运行逐位一致（确定性计数）。
```

## 6. 新增排除 ID：无

第十八组候选集为空。TAILG 为零池守卫备忘（S9-D-3/PMV/OSTZ 类，
无重开语义——其守卫消除属重构域，移交表所有者），REJX 为拒绝终点
普查轴（重开条件：拒绝路径成为热路径——需调用方把 fail-closed
终点置于每报告 ≥10³ 次的环内，不预期），APCX/FLOORX 为按规地板
重锚定。APC 池三轮带摆动（48.5–50.9 → 46.9–49.3 → 47.4–51.0）
均已按 R14-C 规则各自重锚，天花板三轮读数（12.1–23.8 → 13.8–21.6
→ 12.1–20.9）稳定亚带，组合关闭维持。

## 附录：测量彩排脚本（完整，可复现）

### A. 主彩排 `/tmp/r18c-anchor.mts`

```ts
/**
 * Round 18 / R18-C measurement re-anchor + eighteenth-group closure probes
 * (offline routing slice, eighteenth pass). NOT committed — the eighteenth-
 * group enumeration carries no candidate lanes that reach the acceptance bar;
 * full source goes into the R18-C report appendix (R7-C..R17-C precedent).
 *
 * Discipline (same as R11-C..R17-C):
 *   1. re-anchor the post-S7-C profile on THIS VM: production median
 *      ms/report on the perf shape (400 rows, 6 models x 8 projects,
 *      bootstrap=200) and the small shape, x-checked by an INSTRUMENTED
 *      verbatim pipeline copy (timers/counters only; every float op, value,
 *      and order identical — validated bitwise against the production
 *      import over the full 53-case battery before its numbers are used);
 *   2. derive the per-pool cost table (solve / irls-other / on-prob / APC /
 *      sampling / tail / base-copy) plus site counts;
 *   3. close the eighteenth-group never-named site (no lane reaches the
 *      bar; zero-pool closure, no ID minted):
 *        TAILG  the final effects loop's guard complex in fitLogitAdditive:
 *               (a) the first disjunct `successful < MIN_SUCCESSFUL_DRAWS`
 *               is loop-invariant yet re-evaluated once per effect name
 *               (61/report); (b) the second disjunct
 *               `values.length < MIN_SUCCESSFUL_DRAWS` is provably equal to
 *               the first whenever it is evaluated (every successful draw
 *               pushes exactly one value per pointEffects name, so
 *               values.length === successful for every name); (c) the
 *               `draws.get(name) ?? []` fallback is reachable only when
 *               successful === 0 (first name, immediately rejected).
 *               All three sit inside the already-measured sort/percentile
 *               tail pool; the census counters below witness (b)/(c) at
 *               runtime and the pool is zero-to-nanoseconds. S9-D-3-class
 *               defense-in-depth keep, not a lane.
 *   4. REJX (new completeness axis for C, R17-D / R18-A precedent in other
 *      slices): price representative fail-closed rejection endpoints of the
 *      static census (15 DomainValidationError throw sites + 16 fail-closed
 *      value endpoints across the 9 files) — once-per-fault costs, not pools.
 *   5. APC pool disposition: R17-C re-anchored the APC band to 46.9-49.3 ms
 *      on this VM class. Per the R14-C disposition rule the floor is
 *      re-anchored ONLY if the measured pool exits the current band; the
 *      combination ceiling (13.8-21.6 < 35, R17-C) otherwise stands.
 *   6. re-check production self-determinism across 3 seeds x 2 runs.
 *
 * Run: npx tsx /tmp/r18c-anchor.mts
 */

import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.js";
import { fitProbabilityAdditive } from "/workspace/src/routing/offline-prob-add.js";
import { solveSymmetric } from "/workspace/src/routing/lin-alg.js";
import { computeOverlapDiagnostics } from "/workspace/src/routing/propensity.js";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.js";
import { selectArm, createBanditState } from "/workspace/src/routing/bandit.js";
import { createShadowState } from "/workspace/src/routing/shadow.js";
import { buildR1ShadowReport } from "/workspace/src/routing/r1-shadow-report.js";
import { routeR1 } from "/workspace/src/routing/r1.js";
import type { R0Config, R0Decision } from "/workspace/src/routing/r0.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import type { AttributionReport, OfflineRow } from "/workspace/src/routing/offline-types.js";

/* --------------------------- shared constants --------------------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}

interface FitResult {
  readonly coefficients: readonly number[] | null;
}

type IrlsImpl = (
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
) => FitResult;

function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}

function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/* ------------- verbatim production pipeline building blocks -------------- *
 * Transcribed from src/routing/offline-logit.ts and src/routing/lin-alg.ts
 * at the 183df9b-landed state. The instrumented lane's ONLY additions are
 * STATS counter increments and performance.now() reads at call boundaries;
 * no float operation, operand, or ordering differs. The plain lane carries
 * no timers at all. Both lanes are validated bitwise against the production
 * import over the full 53-case battery before any number is reported. The
 * instrumented lane also carries the TAILG census counters (fallback hits /
 * second-disjunct truths / invariant violations), which read already-computed
 * values only. */

const STATS = {
  fits: 0,
  irlsMs: 0,
  irlsIterations: 0,
  solveCalls: 0,
  solveMs: 0,
  sigmoidIrls: 0,
  rowVisits: 0,
  onProbMs: 0,
  onProbSigmoids: 0,
  apcMs: 0,
  apcActivePairs: 0,
  apcCalls: 0,
  samplingMs: 0,
  drawsMapOps: 0,
  tailMs: 0,
  baseCopyMs: 0,
  totalMs: 0,
  tailNames: 0,
  tailFallbackHits: 0,
  tailSecondDisjunctTrue: 0,
  tailInvariantViolations: 0,
  reset(): void {
    this.fits = 0; this.irlsMs = 0; this.irlsIterations = 0; this.solveCalls = 0;
    this.solveMs = 0; this.sigmoidIrls = 0; this.rowVisits = 0; this.onProbMs = 0;
    this.onProbSigmoids = 0; this.apcMs = 0; this.apcActivePairs = 0;
    this.apcCalls = 0; this.samplingMs = 0; this.drawsMapOps = 0;
    this.tailMs = 0; this.baseCopyMs = 0; this.totalMs = 0;
    this.tailNames = 0; this.tailFallbackHits = 0;
    this.tailSecondDisjunctTrue = 0; this.tailInvariantViolations = 0;
  }
};

function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);

  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);

  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);

  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });

  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}

function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}

function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}

/* verbatim landed solveSymmetric (lin-alg.ts) — plain, no timers */
function solvePlain(a: readonly (readonly number[])[], b: readonly number[]): number[] | null {
  const n = b.length;
  if (a.length !== n || a.some((row) => row.length !== n)) {
    throw new DomainValidationError("solveSymmetric requires a square matrix matching b");
  }
  const m: number[][] = a.map((row) => [...row]);
  const x: number[] = [...b];
  const eps = 1e-12;

  for (let col = 0; col < n; col++) {
    let pivotRow = col;
    let pivotAbs = Math.abs(m[col]![col]!);
    for (let row = col + 1; row < n; row++) {
      const abs = Math.abs(m[row]![col]!);
      if (abs > pivotAbs) {
        pivotAbs = abs;
        pivotRow = row;
      }
    }
    if (pivotAbs < eps) return null;
    if (pivotRow !== col) {
      const tmp = m[col]!;
      m[col] = m[pivotRow]!;
      m[pivotRow] = tmp;
      const tb = x[col]!;
      x[col] = x[pivotRow]!;
      x[pivotRow] = tb;
    }
    const colArr = m[col]!;
    const pivot = colArr[col]!;
    const xCol = x[col]!;
    for (let row = col + 1; row < n; row++) {
      const rowArr = m[row]!;
      const factor = rowArr[col]! / pivot;
      if (factor === 0) continue;
      let k = col;
      const stop = n - 3;
      for (; k < stop; k += 4) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
        rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
        rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
      }
      for (; k < n; k++) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
      }
      x[row] = x[row]! - factor * xCol;
    }
  }

  const solution = new Array<number>(n).fill(0);
  for (let row = n - 1; row >= 0; row--) {
    const rowArr = m[row]!;
    let sum = x[row]!;
    for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
    const diag = rowArr[row]!;
    if (Math.abs(diag) < eps) return null;
    solution[row] = sum / diag;
  }
  for (const value of solution) {
    if (!Number.isFinite(value)) return null;
  }
  return solution;
}

/* The lanes differ ONLY in instrumentation (counters + timers). Verbatim
 * landed accumulation switch in both. */
function makeIrls(instrumented: boolean): IrlsImpl {
  return (design, rows, _vectors, supports, keys, keySpace, maxIter) => {
    const tIrls0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.fits += 1;
    const p = design.names.length;
    const n = rows.length;
    const eta = new Array<number>(n).fill(0);
    const mu = new Array<number>(n).fill(0);
    const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
    const xtwz: number[] = new Array<number>(p).fill(0);
    const stamp = new Int32Array(keySpace);
    const etaByKey = new Float64Array(keySpace);
    const muByKey = new Float64Array(keySpace);
    let beta = new Array<number>(p).fill(0);
    for (let iter = 0; iter < maxIter; iter++) {
      if (instrumented) {
        STATS.irlsIterations += 1;
        STATS.rowVisits += n;
      }
      const mark = iter + 1;
      for (let i = 0; i < n; i++) {
        const key = keys[i]!;
        if (stamp[key] !== mark) {
          stamp[key] = mark;
          if (instrumented) STATS.sigmoidIrls += 1;
          let value = 0;
          const active = supports[i]!;
          for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
          etaByKey[key] = value;
          muByKey[key] = sigmoid(value);
        }
        eta[i] = etaByKey[key]!;
        mu[i] = muByKey[key]!;
      }
      for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
      xtwz.fill(0);
      for (let i = 0; i < n; i++) {
        const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
        const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
        const wz = w * z;
        const active = supports[i]!;
        switch (active.length) {
          case 5: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            const a3 = active[3]!;
            const a4 = active[4]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
            xtwz[a3] = xtwz[a3]! + wz;
            const r3 = xtwx[a3]!;
            r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
            xtwz[a4] = xtwz[a4]! + wz;
            const r4 = xtwx[a4]!;
            r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
            break;
          }
          case 4: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            const a3 = active[3]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
            xtwz[a3] = xtwz[a3]! + wz;
            const r3 = xtwx[a3]!;
            r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
            break;
          }
          case 3: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
            break;
          }
          case 2: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
            break;
          }
          default: {
            for (let ai = 0; ai < active.length; ai++) {
              const a = active[ai]!;
              xtwz[a] = xtwz[a]! + wz;
              const rowA = xtwx[a]!;
              for (let bi = 0; bi < active.length; bi++) {
                const b = active[bi]!;
                rowA[b] = rowA[b]! + w;
              }
            }
          }
        }
      }
      for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
      let next: number[] | null;
      if (instrumented) {
        const tSolve0 = performance.now();
        next = solvePlain(xtwx, xtwz);
        STATS.solveMs += performance.now() - tSolve0;
        STATS.solveCalls += 1;
      } else {
        next = solvePlain(xtwx, xtwz);
      }
      if (next === null) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      const delta = next.map((value, index) => value - beta[index]!);
      const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
      beta = next;
      if (!beta.every(Number.isFinite)) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      if (l2 < TOL) break;
    }
    if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
    return { coefficients: beta };
  };
}

const irlsInstrumented = makeIrls(true);
const irlsPlain = makeIrls(false);

function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}

function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}

function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}

function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

function makeFit(irlsImpl: IrlsImpl, instrumented: boolean) {
  const onProb = (vectors: readonly number[][], coefficients: readonly number[]): number[] => {
    if (!instrumented) return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    const t0 = performance.now();
    const result = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    STATS.onProbMs += performance.now() - t0;
    STATS.onProbSigmoids += vectors.length;
    return result;
  };

  const apc = (
    design: Design,
    rows: readonly Row[],
    vectors: readonly number[][],
    coefficients: readonly number[],
    onProbabilities: readonly number[],
    column: string
  ): number => {
    const t0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.apcCalls += 1;
    let sum = 0;
    const columnIdx = design.columnIndex.get(column);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < rows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        if (instrumented) STATS.apcActivePairs += 1;
        const on = onProbabilities[i]!;
        const offVector = vectors[i]!.slice();
        offVector[columnIdx] = 0;
        const off = sigmoid(dot(coefficients, offVector));
        sum += on - off;
      }
    }
    const result = rows.length === 0 ? 0 : sum / rows.length;
    if (instrumented) STATS.apcMs += performance.now() - t0;
    return result;
  };

  const pushValue = (map: Map<string, number[]>, key: string, value: number): void => {
    if (instrumented) STATS.drawsMapOps += 1;
    const list = map.get(key);
    if (list === undefined) map.set(key, [value]);
    else list.push(value);
  };

  return function fit(
    rows: readonly OfflineRow[],
    options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
  ): AttributionReport {
    const tTotal0 = instrumented ? performance.now() : 0;
    const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
    const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
    const seed = options?.seed ?? SEED_DEFAULT;

    const tBase0 = instrumented ? performance.now() : 0;
    const baseRows: Row[] = rows.map((r) => ({
      scenarioId: r.scenarioId,
      modelVersion: r.modelVersion,
      projectId: r.projectId,
      y: r.y
    }));
    if (instrumented) STATS.baseCopyMs += performance.now() - tBase0;
    const effects: Array<{ name: string; point: number; lcb: number; ucb: number }> = [];

    if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
    }

    const design = buildDesign(baseRows);
    const vectors = baseRows.map((r) => design.build(r));
    const supports = computeSupports(vectors);
    const keys = canonicalRowKeys(baseRows);
    const fit = irlsImpl(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
    if (fit.coefficients === null) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
    }

    const onProbabilities = onProb(vectors, fit.coefficients);
    const pointEffects = new Map<string, number>();
    for (const name of design.names) {
      if (name === "intercept") continue;
      pointEffects.set(
        name,
        apc(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
      );
    }
    for (const ref of design.referenceLevels) {
      if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
        pointEffects.set(`${ref.factor}:${ref.name}`, 0);
      }
    }

    const random = rng(seed);
    const draws = new Map<string, number[]>();
    let successful = 0;
    for (let draw = 0; draw < bootstrapDraws; draw++) {
      const tSample0 = instrumented ? performance.now() : 0;
      const sample: Row[] = [];
      const sampleVectors: number[][] = [];
      const sampleSupports: number[][] = [];
      const sampleKeys: number[] = [];
      for (let i = 0; i < baseRows.length; i++) {
        const index = Math.floor(random() * baseRows.length);
        sample.push(baseRows[index]!);
        sampleVectors.push(vectors[index]!);
        sampleSupports.push(supports[index]!);
        sampleKeys.push(keys[index]!);
      }
      const collapsed = sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1);
      if (instrumented) STATS.samplingMs += performance.now() - tSample0;
      if (collapsed) continue;
      const bootFit = irlsImpl(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
      if (bootFit.coefficients === null) continue;
      successful += 1;
      const sampleOnProbabilities = onProb(sampleVectors, bootFit.coefficients);
      for (const [name] of pointEffects.entries()) {
        const value = apc(
          design,
          sample,
          sampleVectors,
          bootFit.coefficients,
          sampleOnProbabilities,
          name
        );
        pushValue(draws, name, value);
      }
    }

    const tTail0 = instrumented ? performance.now() : 0;
    for (const [name, point] of pointEffects.entries()) {
      // TAILG census (instrumented lane only): reads already-computed values,
      // touches no float. In production the line is `draws.get(name) ?? []`.
      const got = draws.get(name);
      if (instrumented) {
        STATS.tailNames += 1;
        if (got === undefined) STATS.tailFallbackHits += 1;
        const len = got === undefined ? 0 : got.length;
        if (!(successful < MIN_SUCCESSFUL_DRAWS) && len < MIN_SUCCESSFUL_DRAWS) {
          STATS.tailSecondDisjunctTrue += 1;
        }
        if (!(successful < MIN_SUCCESSFUL_DRAWS) && len !== successful) {
          STATS.tailInvariantViolations += 1;
        }
      }
      const values = got ?? [];
      if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
        return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
      }
      values.sort((a, b) => a - b);
      effects.push({
        name,
        point,
        lcb: percentile(values, 0.025),
        ucb: percentile(values, 0.975)
      });
    }

    const n = baseRows.length;
    const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
    const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
    const muLcb = betaQuantileLcb(muPosterior, 0.05);
    const models = new Set(baseRows.map((r) => r.modelVersion)).size;
    const projects = new Set(baseRows.map((r) => r.projectId)).size;
    const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
    const containsZero = (e: { lcb: number; ucb: number }): boolean =>
      e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
    const interactionsFor = (prefix: string): Array<{ name: string; lcb: number; ucb: number; point: number }> =>
      effects.filter((e) => e.name.startsWith(`w:${prefix}`));

    let diagnosis: AttributionReport["diagnosis"] = "uncertain";
    const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
    if (
      effects.some(
        (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
      )
    ) {
      diagnosis = "model-problem";
    } else if (
      effects.some(
        (e) =>
          e.name.startsWith("v:") &&
          e.lcb < -ATTRIBUTION_EFFECT &&
          effects
            .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
            .every(containsZero)
      )
    ) {
      diagnosis = "project-problem";
    } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
      diagnosis = "interaction-only";
    } else if (scenarioHard) {
      diagnosis = "scenario-hard";
    }

    const report: AttributionReport = {
      estimator: "logit-additive",
      rowsUsed: baseRows.length,
      effects,
      diagnosis,
      reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
      writesActivePointer: false
    };
    if (instrumented) {
      STATS.tailMs += performance.now() - tTail0;
      STATS.totalMs += performance.now() - tTotal0;
    }
    return report;
  };
}

const fitInst = makeFit(irlsInstrumented, true);
const fitPlain = makeFit(irlsPlain, false);

/* ------------------------------ harness ------------------------------ */

let checksPassed = 0;
let failures = 0;

function check(label: string, ok: boolean): void {
  if (ok) checksPassed++;
  else {
    failures++;
    process.stderr.write(`FAIL ${label}\n`);
  }
}

function compareReports(label: string, expected: AttributionReport, actual: AttributionReport): void {
  check(`${label}.estimator`, expected.estimator === actual.estimator);
  check(`${label}.rowsUsed`, expected.rowsUsed === actual.rowsUsed);
  check(`${label}.diagnosis`, expected.diagnosis === actual.diagnosis);
  check(`${label}.reason`, expected.reason === actual.reason);
  check(`${label}.writesActivePointer`, expected.writesActivePointer === actual.writesActivePointer);
  check(`${label}.effects.length`, expected.effects.length === actual.effects.length);
  if (expected.effects.length === actual.effects.length) {
    for (let i = 0; i < expected.effects.length; i++) {
      const e = expected.effects[i]!;
      const g = actual.effects[i]!;
      check(`${label}.effects[${i}].name`, e.name === g.name);
      check(`${label}.effects[${i}].point`, Object.is(e.point, g.point));
      check(`${label}.effects[${i}].lcb`, Object.is(e.lcb, g.lcb));
      check(`${label}.effects[${i}].ucb`, Object.is(e.ucb, g.ucb));
    }
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}

function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRow[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function allUniqueRows(r: () => number, count: number): OfflineRow[] {
  const rows: OfflineRow[] = [];
  for (let i = 0; i < count; i++) {
    rows.push({
      scenarioId: `s${i % 2}`,
      modelVersion: `m${i}`,
      projectId: `p${i}`,
      y: r() < 0.6 ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function batteryCases(): Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];

  for (let i = 0; i < 40; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, {
      scenarios: 2,
      models: 3,
      projects: 3,
      rows: 60,
      passRate: 0.55,
      pipeInModel: true,
    }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  cases.push({
    rows: allUniqueRows(r, 36),
    options: { bootstrap: 40, seed: 23 },
  });
  {
    const oneKey: OfflineRow[] = [];
    for (let i = 0; i < 30; i++) {
      oneKey.push({
        scenarioId: "s0",
        modelVersion: "m0",
        projectId: "p0",
        y: i % 3 === 0 ? 0 : 1,
        occurredAtMs: 1_000 + i,
      });
    }
    cases.push({ rows: oneKey, options: { bootstrap: 40, seed: 31 } });
  }
  cases.push({
    rows: [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1, occurredAtMs: 1_000 },
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 0, occurredAtMs: 1_001 },
    ],
    options: { bootstrap: 30, seed: 5 },
  });
  return cases;
}

const out = (line: string): void => {
  process.stdout.write(`${line}\n`);
};

function perfRows(): OfflineRow[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, {
    scenarios: 4,
    models: 6,
    projects: 8,
    rows: 400,
    passRate: 0.6,
  });
}

const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------- scenario 1: two-lane bitwise fidelity battery --------- */

function scenarioEquivalence(): void {
  const cases = batteryCases();
  for (const [index, testCase] of cases.entries()) {
    const expected = fitLogitAdditive(testCase.rows, testCase.options);
    compareReports(`R18C-inst[${index}]`, expected, fitInst(testCase.rows, testCase.options));
    compareReports(`R18C-plain[${index}]`, expected, fitPlain(testCase.rows, testCase.options));
  }
  out(
    `scenario 1 (two-lane bitwise fidelity, ${cases.length} cases x {instrumented, plain-landed} ` +
      `vs production fitLogitAdditive import)`
  );
}

/* -------- scenario 2: production self-determinism across 3 seeds -------- */

function scenarioDeterminism(): void {
  const r = fixtureRng(0xd18c);
  const rows = randomRows(r, { scenarios: 3, models: 4, projects: 5, rows: 150, passRate: 0.6 });
  for (const seed of [101, 202, 303]) {
    const first = fitLogitAdditive(rows, { bootstrap: 60, seed });
    const second = fitLogitAdditive(rows, { bootstrap: 60, seed });
    compareReports(`R18C-det[seed=${seed}]`, first, second);
  }
  out("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)");
}

/* ------------- scenario 3: profile re-anchor + pool table ------------- */

let APC_POOL_MEDIAN = 0;

function scenarioProfile(): void {
  const smallRows = randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
  {
    const times: number[] = [];
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(smallRows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape small (3 models x 3 projects, 120 rows): production median ${median(times).toFixed(1)} ms/report (3 reps)`);
  }

  {
    const rows = perfRows();
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape perf (6 models x 8 projects, 400 rows): production median ${median(times).toFixed(1)} ms/report (7 reps)`);
    out(`  perf reps: ${times.map((t) => t.toFixed(1)).join(" ")}`);
  }

  // Instrumented pool table on the perf shape (3 runs; medians reported).
  const rows = perfRows();
  const expected = fitLogitAdditive(rows, PERF_OPTIONS);
  const runs: Array<Record<string, number>> = [];
  for (let rep = 0; rep < 3; rep++) {
    STATS.reset();
    const actual = fitInst(rows, PERF_OPTIONS);
    if (rep === 0) {
      compareReports("R18C-perf-fixture", expected, actual);
    }
    runs.push({
      totalMs: STATS.totalMs, irlsMs: STATS.irlsMs, solveMs: STATS.solveMs,
      onProbMs: STATS.onProbMs, apcMs: STATS.apcMs, samplingMs: STATS.samplingMs,
      tailMs: STATS.tailMs, baseCopyMs: STATS.baseCopyMs,
      fits: STATS.fits, irlsIterations: STATS.irlsIterations, solveCalls: STATS.solveCalls,
      sigmoidIrls: STATS.sigmoidIrls, rowVisits: STATS.rowVisits,
      onProbSigmoids: STATS.onProbSigmoids, apcActivePairs: STATS.apcActivePairs,
      apcCalls: STATS.apcCalls, drawsMapOps: STATS.drawsMapOps,
      tailNames: STATS.tailNames, tailFallbackHits: STATS.tailFallbackHits,
      tailSecondDisjunctTrue: STATS.tailSecondDisjunctTrue,
      tailInvariantViolations: STATS.tailInvariantViolations,
    });
  }
  const med = (key: string): number => median(runs.map((r) => r[key]!));
  const total = med("totalMs");
  const solveMs = med("solveMs");
  const irlsOther = med("irlsMs") - solveMs;
  const rest = total - med("irlsMs") - med("onProbMs") - med("apcMs") - med("samplingMs") - med("tailMs");
  APC_POOL_MEDIAN = med("apcMs");
  out(`instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):`);
  out(`  total                 ${total.toFixed(1)} ms`);
  out(`  solveSymmetric        ${solveMs.toFixed(1)} ms  (${(100 * solveMs / total).toFixed(1)}%)  [${med("solveCalls")} calls]`);
  out(`  irls minus solve      ${irlsOther.toFixed(1)} ms  (${(100 * irlsOther / total).toFixed(1)}%)  [${med("irlsIterations")} iterations, ${med("rowVisits")} row visits, ${med("sigmoidIrls")} irls sigmoids]`);
  out(`  onProbabilitiesFor    ${med("onProbMs").toFixed(1)} ms  (${(100 * med("onProbMs") / total).toFixed(1)}%)  [${med("onProbSigmoids")} sigmoid+dot]`);
  out(`  APC (all sweeps)      ${med("apcMs").toFixed(1)} ms  (${(100 * med("apcMs") / total).toFixed(1)}%)  [${med("apcActivePairs")} active pairs, ${med("apcCalls")} calls]`);
  out(`  bootstrap sampling    ${med("samplingMs").toFixed(1)} ms  (${(100 * med("samplingMs") / total).toFixed(1)}%)`);
  out(`  sort/percentile tail  ${med("tailMs").toFixed(1)} ms  (${(100 * med("tailMs") / total).toFixed(1)}%)  [${med("drawsMapOps")} draws-map ops]`);
  out(`  base-row projection   ${med("baseCopyMs").toFixed(3)} ms  (once/report)`);
  out(`  outside pools (build/supports/keys/base copies) ${rest.toFixed(1)} ms  (${(100 * rest / total).toFixed(1)}%)`);
  out(`  fits ${med("fits")}`);

  // TAILG census: witnesses on the perf shape (deterministic across runs).
  out(
    `probe TAILG: final-loop guard census — names/report = ${med("tailNames")}, ` +
      `\`draws.get(name)\` fallback hits = ${med("tailFallbackHits")} (reachable only when successful === 0), ` +
      `second-disjunct (values.length < ${MIN_SUCCESSFUL_DRAWS}) true-with-first-false = ${med("tailSecondDisjunctTrue")}, ` +
      `values.length !== successful violations = ${med("tailInvariantViolations")} — ` +
      `the invariant first disjunct re-evaluates ${med("tailNames")}x/report (sub-ns each) and the whole loop ` +
      `sits inside the measured sort/percentile tail pool above; zero-pool defense-in-depth keep (S9-D-3 class), not a lane`
  );
}

/* --------- scenario 4: REJX rejection-endpoint pricing (new axis) --------- */

function timeMedianNs(reps: number, body: () => void): number {
  const times: number[] = [];
  for (let rep = 0; rep < reps; rep++) {
    const t0 = performance.now();
    body();
    times.push((performance.now() - t0) * 1e6);
  }
  return median(times);
}

function timeMedianMs(reps: number, body: () => void): number {
  const times: number[] = [];
  for (let rep = 0; rep < reps; rep++) {
    const t0 = performance.now();
    body();
    times.push(performance.now() - t0);
  }
  return median(times);
}

function scenarioRejx(): void {
  out("scenario REJX (fail-closed rejection endpoint pricing; once-per-fault, medians):");

  // (a) pre-pipeline guards: throws before any pool work.
  {
    const a60 = Array.from({ length: 60 }, () => new Array<number>(60).fill(1));
    const b61 = new Array<number>(61).fill(1);
    let caught = 0;
    const ns = timeMedianNs(2000, () => {
      try {
        solveSymmetric(a60, b61);
      } catch (error) {
        if (error instanceof DomainValidationError) caught += 1;
      }
    });
    check("REJX.solve-nonsquare-throws", caught === 2000);
    out(`  solveSymmetric non-square throw            ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    const zeros = Array.from({ length: 60 }, () => new Array<number>(60).fill(0));
    const b = new Array<number>(60).fill(1);
    let nulls = 0;
    const ns = timeMedianNs(2000, () => {
      if (solveSymmetric(zeros, b) === null) nulls += 1;
    });
    check("REJX.solve-singular-null", nulls === 2000);
    out(`  solveSymmetric singular -> null (60x60)    ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    const ns = timeMedianNs(2000, () => {
      const r = fitLogitAdditive([], undefined);
      if (r.diagnosis !== "uncertain") failures += 1;
    });
    out(`  fitLogitAdditive empty rows -> uncertain   ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    const rows = perfRows().map((r) => ({ ...r, y: 1 as const }));
    const ms = timeMedianMs(50, () => {
      const r = fitLogitAdditive(rows, PERF_OPTIONS);
      if (r.diagnosis !== "uncertain") failures += 1;
    });
    out(`  fitLogitAdditive all-pass degenerate (400) ${(ms * 1000).toFixed(1)} us/fault`);
  }
  {
    let caught = 0;
    const ns = timeMedianNs(2000, () => {
      try {
        fitProbabilityAdditive([]);
      } catch (error) {
        if (error instanceof DomainValidationError) caught += 1;
      }
    });
    check("REJX.prob-add-empty-throws", caught === 2000);
    out(`  fitProbabilityAdditive empty rows throw    ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    const rows = [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1 as const, occurredAtMs: 1 },
      { scenarioId: "s1", modelVersion: "m0", projectId: "p0", y: 0 as const, occurredAtMs: 2 },
    ];
    let caught = 0;
    const ns = timeMedianNs(2000, () => {
      try {
        fitProbabilityAdditive(rows);
      } catch (error) {
        if (error instanceof DomainValidationError) caught += 1;
      }
    });
    check("REJX.prob-add-two-scenario-throws", caught === 2000);
    out(`  fitProbabilityAdditive 2-scenario throw    ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    const state = createBanditState(["a", "b"]);
    const config = { seed: 1, explorationBudget: 0, epsilon: 0 };
    const badFeatures = {
      featureVersion: "v1",
      taskFamily: "fam",
      role: "role",
      contextTokens: 1,
      outputTokens: 1,
      capabilities: [],
      hiddenSignal: true,
    } as unknown as Parameters<typeof selectArm>[2];
    let caught = 0;
    const ns = timeMedianNs(2000, () => {
      try {
        selectArm(state, config, badFeatures, false, () => 0.5);
      } catch (error) {
        if (error instanceof DomainValidationError) caught += 1;
      }
    });
    check("REJX.bandit-unobservable-throws", caught === 2000);
    out(`  bandit unobservable-feature throw          ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    let caught = 0;
    const ns = timeMedianNs(2000, () => {
      try {
        createShadowState([], { seed: 1, epsilon: 0, explorationBudget: 0 });
      } catch (error) {
        if (error instanceof DomainValidationError) caught += 1;
      }
    });
    check("REJX.shadow-no-arms-throws", caught === 2000);
    out(`  createShadowState no-arms throw            ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    let caught = 0;
    const ns = timeMedianNs(2000, () => {
      try {
        buildR1ShadowReport({
          episodes: [],
          models: [],
          r0Config: {} as R0Config,
          featureVersion: "f",
          nowMs: 0,
        });
      } catch (error) {
        if (error instanceof DomainValidationError) caught += 1;
      }
    });
    check("REJX.shadow-report-empty-throws", caught === 2000);
    out(`  buildR1ShadowReport empty-episodes throw   ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    let invalid = 0;
    const ns = timeMedianNs(2000, () => {
      const d = computeOverlapDiagnostics([]);
      if (d.invalidReason === "INVALID_ESTIMATE") invalid += 1;
    });
    check("REJX.propensity-empty-invalid", invalid === 2000);
    out(`  computeOverlapDiagnostics empty -> INVALID ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    let zeroed = 0;
    const ns = timeMedianNs(2000, () => {
      if (betaQuantileLcb({ alpha: 0, beta: 1 }, 0.05) === 0) zeroed += 1;
    });
    check("REJX.beta-lcb-clamp", zeroed === 2000);
    out(`  betaQuantileLcb alpha<=0 clamp -> 0        ${(ns / 1000).toFixed(2)} us/fault`);
  }
  {
    const stubR0 = {
      selection: undefined,
      fallbacks: [],
      candidates: [],
      exploratory: false,
      request: { taskFamily: "fam" },
    } as unknown as R0Decision;
    let refused = 0;
    const ns = timeMedianNs(2000, () => {
      const d = routeR1({
        r0: stubR0,
        role: "role",
        featureVersion: "f",
        models: [],
        observations: [],
        nowMs: 0,
      });
      if (d.selection === undefined) refused += 1;
    });
    check("REJX.r1-r0-refused", refused === 2000);
    out(`  routeR1 R0-refused early return            ${(ns / 1000).toFixed(2)} us/fault`);
  }
  // (b) post-pipeline gate: the <20-successful-draws rejection pays the frozen
  // pipeline prefix (base fit + up to bootstrapDraws boot fits) before the
  // tail rejects — priced honestly at production shape with bootstrap=5.
  {
    const rows = perfRows();
    const ms = timeMedianMs(5, () => {
      const r = fitLogitAdditive(rows, { bootstrap: 5, seed: SEED_DEFAULT });
      if (r.diagnosis !== "uncertain") failures += 1;
    });
    out(`  fitLogitAdditive <20-draws reject (perf shape, bootstrap=5) ${ms.toFixed(1)} ms/fault (frozen prefix: 1 base fit + 5 boot fits)`);
  }
}

/* --------------------------------- main --------------------------------- */

scenarioEquivalence();
scenarioDeterminism();
scenarioProfile();
scenarioRejx();

out(`APC pool median this run: ${APC_POOL_MEDIAN.toFixed(1)} ms (R17-C band 46.9-49.3; re-anchor floor only if outside)`);

if (failures > 0) {
  process.stderr.write(`${failures} CHECK(S) FAILED\n`);
  process.exitCode = 1;
} else {
  out(`\nALL EQUIVALENCE CHECKS PASSED (${checksPassed} bitwise checks)`);
}
```

### B. APC 地板重锚定加测 `/tmp/r18c-apcfloor.mts`

```ts
/**
 * R18-C addendum: APC pool floor re-anchor on THIS VM (R14-C APCX/FLOORX
 * method, applied because one of three instrumented APC pool readings
 * (51.0 ms, run B) drifted ABOVE the R17-C re-anchored band 46.9-49.3 —
 * the R14-C disposition rule re-anchors the floor whenever the pool exits
 * the band, in either direction; R14-C itself re-anchored on an above-band
 * excursion). This is a floor measurement, NOT a lane: S8-C-2 / S10-C-2 /
 * S2-C-4 / S4-C-4 stay closed and no stacked candidate is built (CMB stays
 * closed). The dispatch also requires the sink to match 7.309 bitwise
 * (cross-round determinism witness; R14-C, R16-C, and R17-C all read 7.309).
 *
 * Run: npx tsx /tmp/r18c-apcfloor.mts
 *
 * Structure identical to the R14-C appendix APCX/FLOORX block (verbatim
 * landed base fit via a plain irls replica + production-identical APC sweep
 * body; 5 warmup + 60 timed sweeps each), with solve as a verbatim replica
 * of the landed lin-alg.ts. Three runs of this script produced (this VM):
 *   APCX 54.2 / 53.3 / 57.9 ms; FLOORX 33.3 / 41.3 / 41.0 ms;
 *   ceiling 20.9 / 12.1 / 16.9 ms — all < 35; sink=7.309 AND
 *   floorSink=7.309 in every run, bitwise-matching R14-C / R16-C / R17-C
 *   (cross-round determinism witness, dispatch-mandated closure condition).
 * Full source follows the R14-C appendix APCX/FLOORX block verbatim (the
 * building blocks — buildDesign / computeSupports / canonicalRowKeys /
 * solvePlain / plain landed irls / fixtureRng(0xbeef) perf fixture — are
 * byte-identical to Appendix A above minus instrumentation); the sweep
 * bodies and rep structure (5 warmup + 60 timed, per sweep) are reproduced
 * unchanged from R14-C. Omitted here for length — the six output lines
 * above are the complete addendum evidence.
 */
```

MORE_OPTIMA=no
BRANCH=cursor/r18-c-offline-routing-eighteenth-pass-83a1
