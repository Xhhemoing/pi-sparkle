MODEL_SLUG=claude-fable-5-thinking-xhigh

# R18-H：`src/evaluation/` + `src/requirement/` + `src/review/` + `src/rubric/` 第十八遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 18 / R18-H
**基线:** `cursor/sota-persistent-opt-83a1` @ `fa268cc`（独占 tip，含 R18-A/B/D/E/F 空枚举收口与本次派单；R18-C/R18-G 在飞，未等待、未触碰）
**分支:** `cursor/r18-h-eval-eighteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十八遍枚举为空：无达门槛候选、零新 ID、切片零代码改动、切片维持关闭
（MORE_OPTIMA=no）。**
切片 21 个文件（1750 行 = 655+712+309+74，本轮 `wc -l` 逐目录复核一致；
8+7+4+2 清单逐目录确认恰 21 文件，与派单「evaluation 是 8 不是 9」吻合）
自 R1-H 基线（`fd437a9`）以来**逐字节未变**（`git diff fd437a9..HEAD --
src/evaluation src/requirement src/review src/rubric` 为 0 行、同范围
`git log` 为 **0 提交**，十八遍全程零 diff——指令要求的空 diff 检查
通过），且 R17-H 基线（`a8625dd`）之后 `src/` **零提交**（`git log
a8625dd..HEAD -- src/` 为空——其间全部 10 个提交 `--name-only` 逐一
确认仅触及 `docs/reports/sota-opt/`：R17-F/G/H/I/J 摄取、Round 17 收口
与 Round 18 全部派单/摄取；R18-A/B/D/E/F 全数空枚举无代码落地）⇒ 生产
调用方地图**可证不变**（最强形式），本轮全库 import 交叉检索再次独立
确认（9 个切片外导入位点与 R3-H…R17-H 完全一致：7 个值级 =
`assertCoverageAllowsStart`×3 / `extractHeuristicContract` /
`applyPrecedence` / `shouldScout` / `assertCanPromoteFromReview`，2 个
纯类型 = promotion.ts ReviewerKind / primary-split.ts HeuristicHabits；
`src/evaluation/` 全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、
`src/rubric/` 仍零生产调用方）。按指令在本 VM 重锚满格矩阵（§1.3–§1.5，
三次独立运行，Node v22.22.2）：热层默认态 **9.40–9.55µs**/run（与派单
点名的 R17-H 热默认锚 9.17–10.18µs 交叠同带——三次 9550/9414/9402ns
全部带内，离散度 <1.6%；居十八轮历史跨 VM 带 3.9–10.4µs 上段）、配置态
A **6.9–8.4µs**（三次全低于同轮默认态）、配置态 B **3.9–4.7µs**（三次
全低于默认态）——**无配置态悬崖**、默认锚点仍是保守口径；晋升类
17.7–18.6ns；四个零生产调用方命令类（evaluate 1.10–1.13µs / critique
0.27–0.61µs / pairwise 2.04–2.05µs / reconcile 62–67ns）**全部
≤2.1µs**；冷层首调默认 1670–1706µs、配置 1628–1665µs
once-per-process（与 R6-H…R17-H 同带，否决类归属不变）。第十八遍在
完整排除表（S1-H-1..9 … S9-H-1..2 及 R10-H…R17-H 八轮空枚举收口共
60+ 处不立 ID 记录）之上以一个从未用过的完备性轴收口：**生产可达
休眠分支普查（production-reachable dormant-branch census）**
（§2.1–§2.3，镜像 R18-A skip-path 组成定价与 R18-B「有限 RoutingLimits
配置态：十七轮预算/截止分支零执行」轴在本切片的落地）——前十七轮的
十八个完备性面全部只透过**三个夹具态**（默认 / 配置 A / 配置 B）驱动
热链，而生产链里存在若干**普通生产输入即可到达、但三态从未执行**的
分支：q-tests 追问推入（heuristic.ts L64–70——非 vague 且不含 tests
关键词的 objective，可论证是现实中最常见的态；默认锚 objective 恰好
含 "tests" 而遮蔽了它）、q-scope 推入（L71–77，rename 族 / tiny 族
objective 激活——同时激活前几轮记为「生产形态下不可达」的
namedTargets 二次调用）、零目标与多目标 deliverable 臂、vague ×
requireTests 组合，以及 **skip-contract 门快路径**（生产可达：
`run/episode-bind.ts` 对 plain run 默认构造 skip 合同，三个 run 入口
每次启动都走它）。本轮补上：(a) **满格生产配置态网格**——27 个原始
habit 组合（requireTests / preferReview / askBeforeWrite 各
{缺省,true,false}）× 7 个 objective 类 = **189 格逐格定价**（前十七轮
只定价过其中 3 格），三次运行全格 **2.3–7.3µs**、全局最大格恒为
O1 anchor × aw=T（6834–7313ns）、**低于同轮默认锚**——满格无悬崖，
十七轮三态锚被证保守；(b) **7 个具名休眠态锚级定价**（与十七轮锚
严格同协议）：D1 plain **4.25–4.94µs** / D1q qa **4.35–4.84µs** /
D2a rename 族 scope-ask **4.63–4.70µs**（namedTargets 二次调用激活态）
/ D2b tiny 族 **4.39–4.43µs** / D3 三目标 **2.93–2.94µs** / D4
vague+requireTests **3.69–3.75µs**、G1 skip 合同门快路径
**11.9–12.5ns**/run-start（plain run 生产默认路径首次定价）、G2
非 skip 极小合同全门 **268–287ns**——全部低于默认锚、距数十 ms 落地线
**3–4 个数量级**；(c) **13 项分支覆盖 attestation**（全部经生产输出
形状观察，零插桩零复刻）证明枚举出的每一个生产可达分支格至少被一个
网格格执行（观察到恰 10 个互异 question 集合）；(d) **4 类零生产流量
休眠分支语义 attestation**（spec-first/latest-first 胜者臂、预置
default 问题快路径、空 options "resolved" 回退、调用方自带 sources
臂）——只证语义不定价，零流量 ⇒ 零收益池，不铸 ID。普查发现的唯一
语义角落（"qa" 使 wantsTests 为真但 q-tests 推入守卫不含 qa ⇒ 两者
并存）是行为观察，非保行为优化位点。结构位置 × 消除机制扫描其余全部
微观位点逐一对表均为换名重提或已关家族站点换位（§0 审计；本轮**零
从未点名结构位点**——收敛序列 R15-H 0 → R16-H 1 → R17-H 0 → 本轮
0）。诚实结论：枚举为空，不硬凑 ID。未重开任何 X* / S1-* … S17-*
条目；S5-H-1 守卫经本轮 diff + 读码双复核**原样保留、逐字节未变**，
三项语义 attestation ×3 运行全过；S8-H-3 全决议快路径**未做任何
形态**；S8-H-1 PairwiseInput 未收窄；R17-H 拒绝路径普查**未补铸未
重定价**（本轮全部是成功路径态，与错误路径轴正交）；R16-H PATH_RE
回溯拒列**未重开**（重开条件 (a)/(b) 均未触发：`src/` 零提交 ⇒
objective 无新程序化/对抗来源，无 ≥3.2K 字符无斜杠 token 段真实输入
类出现）；R16-H 载荷基底 × 形态格、R15-H A/A 决斗地板、R14-H 引擎底
轴未重测未复铸。`pnpm gate` 全绿。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/evaluation/`（8 文件：adapters / check-adapter /
  delivery-adapter / diff-adapter / evaluator / ownership / precedence /
  types）、`src/requirement/`（7 文件：coverage / critic / extractor /
  heuristic / normalizer / precedence / provenance）、`src/review/`
  （4 文件：critic / pairwise / reconcile / self-review）、`src/rubric/`
  （2 文件：registry / types）计 21 文件全量第十八遍实际读码，未依赖
  前十七轮记忆。上下游 `domain/contract.ts`（DecisionQuestion 形状
  取证：options 可空、default 可预置均 schema 合法，供 §2.3 Z2/Z3
  构造）、`run/episode-bind.ts`（skip 合同生产形状取证，供 §2.1 G1/G2
  构造——`contractFromObjective(objective, skipped)` 且
  `opts.skipContract !== false` 默认为 skip ⇒ plain run 的门调用
  **默认走 skip 快路径**）、`track/{clarify,loop,plan,primary-split}.ts`
  （生产 habit 通道取证：`habitsFromPreferences` 每键仅布尔时注入 ⇒
  27 个原始组合全部生产可达）、`run/{supervisor,coordinator,
  flowchart-run}.ts`、`adaptation/{promotion,promotion-rules}.ts` 只读
  取证，一行未改。切片外其余树零触碰。在飞 R18-C / R18-G 分支与文件
  未触碰、未等待。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表 546 行，含
  每一条 S*-H-* 与 S5-H-1、S13-B-1）/ PROGRESS.md / round-18/PLAN.md /
  round-17/R17-H.md（空枚举；33 条拒绝终点普查；重锚 9.17–10.18µs）/
  round-16/R16-H.md（空枚举；PATH_RE 候选化后拒列 + 重开条件；基底 ×
  形态格）/ round-15/R15-H.md（空枚举；A/A 决斗地板）/
  round-14/R14-H.md（空枚举；引擎底轴）收口段逐一复核。
  EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md 未编辑。
- **换名重提检查**（第十八遍读码发现的全部微观位点逐条对表；全部在
  候选化之前拒列，零代码触碰。本轮普查轴定价的 189 格 + 8 个具名态
  均为**既有生产函数原样执行**——不是新结构位点，不铸 ID）：
  - **S5-H-1（必须保留项）**：`requirement/precedence.ts` L24 的
    `if (fast.length === 0 || slow.length === 0) return [];` 分配前
    守卫经本轮读码 + `git diff fd437a9..HEAD --
    src/requirement/precedence.ts`（0 行）双复核**原样在位、逐字节
    未变**。本轮对该函数零候选、零「优化掉」提案；§2.4 仿真含三项
    确定性语义 attestation（fast-only → [] / slow-only → [] /
    fast+slow → 单冲突全 id），三次运行全过（428 项检查 ×3 全绿）。
  - **S8-H-3（永不重开项）**：`assertCoverageAllowsStart` 未做任何
    全决议快路径。`coverage.ts` 零 diff。§2.1 G1/G2 与 §2.2 网格门腿
    定价只跑生产函数原样执行（含 skip 快路径与满门路径），未引入
    任何变体。
  - **S8-H-1（永不重开项）**：`PairwiseInput` 全形状（含 comment
    双字段）未收窄；PIC 形状污染家族未重演。
  - **R17-H 不重提项**：33 条 fail-closed 拒绝终点普查（17 throw
    2.5–12.1µs + 16 非抛出 115–941ns）**不补铸、不重定价、不重测**。
    本轮普查轴与其正交：全部 189 格 + 8 具名态均为**成功路径**执行
    （门全过、无 throw）；唯一触门失败的探针是 §2.3 Z3b 的
    fail-closed 对照 attestation（布尔断言，无定价），属 R17-H T8 腿
    语义面复证而非重定价。
  - **R16-H 不重开项**：PATH_RE 分支 1 二次回溯拒列维持——重开条件
    (a) objective 程序化/对抗来源、(b) ≥~3.2K 字符无斜杠 `[\w.-]` 段
    真实载荷，经本轮取证均未触发（`git log a8625dd..HEAD -- src/` 为
    空 ⇒ `cli/main.ts` objective 到达通道未变）；载荷基底 × 形态格
    不补铸、不重测。**本轮网格 objective 全部为现实短句（10¹–10²
    字符）**，未做任何形态/长度扫描——普查轴变的是**决策分支**，
    不是字符串形态，与 R16-H 轴正交。
  - **S1-H-3 家族（namedTargets/shouldAskScope 二次调用）的活化
    注记**：R4-H…R17-H 记「生产形态下 shouldAskScope 首行 shouldScout
    短路使二次调用不可达」——该表述对**已锚三态**成立；本轮普查证明
    rename 族 / tiny 族 objective（普通生产输入）使 shouldScout 为假
    ⇒ namedTargets 在 shouldAskScope 内与 L85 各执行一次（二次调用
    **活化**）。活化态全链实测 4.39–4.70µs（D2a/D2b，§2.1）⇒ 二次
    调用增量仍为亚 µs 噪声级，S1-H-3 的噪声类裁决**在活化态下仍然
    成立，不构成重开**——普查修正的是可达性表述，不是裁决结论。
  - **"qa" 语义角落（本轮新观察，非候选）**：`wantsTests` 正则含
    `qa` 而 q-tests 推入守卫的正则仅 `tests?|coverage` ⇒ "qa"
    objective 下 ac-tests/c-tests 与 q-tests 追问**并存**（BC12
    attestation 证实）。这是行为语义观察：两正则本就语义独立
    （S6-E-5 同哲学），统一它们会改变可观察行为（问题集合变化），
    出保行为范围 ⇒ 不是优化位点，不铸 ID，仅存档。
  - `heuristic.ts`（本轮普查宿主，除上述活化/角落注记外）：L46/L194
    objective 双 trim = R6-H no-ID（S1-H-3 家族）；L48/L64 双正则
    子集短路 = S3-H-3；SMALLEST_CHANGE 每调用 spread（L105）=
    R2-H/R8-H 拒列；vague 分支问题字面量提升 = S6-H-4 / S7-B-5 可
    观察身份家族；isVague split+filter 词计数 = R2-H/R5-H/R7-H/R8-H
    no-ID；q-scope/q-write `questions.some` 守卫 = R4-H no-ID（本轮
    普查在 Q≥1 态下真实执行该 some，价随格测得，仍亚 µs）；正则
    字面量提升 = S1-B-1 / S3-E-5 家族 + R1-H no-ID；每调用
    extractor/critic 角色对（L187–188）= S6-H-4；异步机器 = S7-H-1 +
    X0-4；`[...critique.omissions]` = S4-H-3；`[...sources]` 防御
    spread（L186）= R2-H no-ID；`habits ?? {}` = R10-H no-ID；vague
    路死 wantsReview = R12-H P3；PATH_RE 回溯形态 = R16-H 拒列维持。
  - `coverage.ts`：L22 orphans 防御 spread = R12-H P1；L27
    `Object.keys(...).includes` 每准则 = S1-H-1 / S4-H-1 辖区
    （Face C 二次方 R13-H 已标定）；L52 Set-from-map = R12-H P2；
    L61 owners 取-改-存回写 = R1-H 下界表覆盖；L83–89 gated 合同
    拷贝 = S1-H-2 / S6-H-3（本轮 Q≥1 网格格真实执行 gated 重映射，
    价随格测得）；taskToChecks 死计算 = S6-H-2；L92–98 throw 消息
    装配 = R14-H 拒列 + R17-H T7–T9 定价维持；isSkipContract 线性
    some = R15-H 查表排除维持（本轮 G1 给其真值路径首次锚级定价
    11.9–12.5ns，语义面从未变）。
  - `requirement/precedence.ts`（守卫外）：双 filter 序贯早退 =
    S6-H-1；`conflict.ids.includes` 与双 filter Set 化 = S1-H-5；
    `[...fast, ...slow]` 中间拼接 = 冲突侧零生产流量 + S1-H-5 家族
    （本轮 `detectConflicts(real).length === 0` 三次再证）；
    spec-first/latest-first 胜者臂 = 零生产流量（loop.ts 恒传
    "user-first"，本轮 Z1 语义 attestation 收口，不定价不铸 ID）。
  - `extractor.ts`：L68 roleId 双 trim = R10-H no-ID；L75
    `[...input.sources]` 二次 spread = R2-H no-ID；L83 sourceRefs
    Set = S2-H-1；L122 空 authority 下 sourcesByRef Map 死构造 =
    S2-H-2 本体；L80 生产链双 validate = S3-H-1；grounding.find 换
    Map = S1-H-4；validateConfidence 常量校验 = R15-H 查表排除维持；
    三扩展点异步机器 = S7-H-1 + X0-4；caller-provided sources 臂 =
    零生产流量（clarify 恒不传 sources，本轮 Z4 attestation 收口）。
  - `requirement/critic.ts` / `provenance.ts`：四遍装配 = S2-H-3 /
    S3-H-2；critique 与 detectConflicts 的 toLowerCase 双算 =
    S4-H-2；findUnsourcedItems L30 Set-from-map = R12-H P2 镜像
    站点；isSourced `Array.isArray` 数据面守卫 = R4-H no-ID。
  - `normalizer.ts`：excerpt slice(0,200) = R5-H no-ID + R16-H
    SLICED 基底胞收口维持；extractSignals 三正则 = R1-H no-ID；
    signals 生产组合死输出 = S7-H-2；默认 origin 守卫 = S2-H-7；
    buildContractFromSources = R11-H L3；两条 origin-kind throw =
    R17-H T10/T11 定价维持。
  - `evaluation/evaluator.ts`：`input.evidence[criterion.id]` 双读 =
    R5-H no-ID；聚合三遍 = S1-H-7；INDEPENDENCE_CLASSES includes =
    R1-H 表族；`String()` 分配 = R10-H no-ID；两条输入守卫 throw =
    R17-H T12/T13 定价维持；canEvaluatorScoreCriterion /
    validateEvaluatorScope = test-only。
  - `evaluation/check-adapter.ts` / `delivery-adapter.ts` /
    `diff-adapter.ts`：changeSetsEqual 三面 = S1-H-9 / S2-H-4 /
    S3-H-4；hashArtifact 拼接 = S5-H-3；16 条非抛出拒绝返回 = R17-H
    A1–A15 定价维持；输入校验合取序 = R10-H no-ID；FAIL 路径死
    `hasEpisodeOwned` = R11-H L1。`ownership.ts`：
    `context.changeSet.includes` = X4-9；控制流去糖 = R10-H no-ID。
    `evaluation/precedence.ts`：reduce 内 comparePrecedence 双调用 =
    R2-H no-ID + S2-H-5；三元素表 find = R1-H 表族。
  - `review/pairwise.ts`：remap "ab" 分支冗余展开 = R1-H no-ID；
    presentInput "ba" 对象构造 = 命令类天花板辖区（≤2.1µs）。
    `review/reconcile.ts`：三遍 filter = S1-H-6；L39 Set-from-map =
    R12-H P2 第四站点维持。`review/critic.ts`：hasPass 融合 =
    R2-H no-ID；score 条件键单态化 = R9-H 拒列；actor-defense
    ABSTAIN = R17-H A16 定价维持。`review/self-review.ts`：S9-H-1 /
    S9-H-2 维持；四条 fail-closed throw = R17-H T14–T17 定价维持。
  - `rubric/registry.ts`：copy-on-write = S1-H-8 反例 + S2-H-6；
    `{ ...DEFAULT_REGISTRY }` 浅拷贝别名安全性第十次读码复核成立。
    `rubric/types.ts`：createRubric 时间戳 = R10-H 取证维持。
  - **S13-B-1 / S12-* / S11-*（其他切片）**：均不在本切片，未触碰、
    未换名重提。R17-H 拒绝路径普查、R16-H 基底 × 形态格、R15-H A/A
    地板、R14-H 引擎底轴不重测不补铸（派单明令）。
  - R10-H…R17-H 未铸 S10-H-* … S17-H-*，本轮亦未事后发明任何此类
    编号，不为空枚举轮补铸 ID。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价（R1 未接线）、
  稀疏 → 已批准 R0 基线、双 LCB（beta+normal）与双归因
  （logit+prob-add）保留（X0-11）、晋升 proposal-first、阈值/CAS/
  凭据/数据面契约/公开签名不变、DEFAULT_REGISTRY 未就地变异、
  fail-open 零引入、测试未改，天然成立。不声称 Outcome-supported，
  Checkpoint F-PROD 仍开放（ADR-005 / X0-1）。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，且本 VM
  `/exec-daemon/node`（v22.14.0）在 PATH 前段遮蔽 nvm bin（与
  R11-H…R17-H §0 注记同型）；测量与测试前已显式把 nvm v22.22.2 bin
  置于 PATH 首并复核 `node --version` = v22.22.2。
  `pnpm install --frozen-lockfile`（pnpm 10.17.1）后 `pnpm build` 绿。
- 未开 PR；未 force-push；未触碰专属分支；仅提交并推送工作分支
  `cursor/r18-h-eval-eighteenth-pass-83a1`。无赢家 ⇒ 未提交任何
  仿真脚本（按指令），锚点/普查测量脚本全文在附录。

## 1. 基线不变性、调用图复核与满格矩阵重锚

1. **切片逐字节未变**：`git diff fd437a9..HEAD -- src/evaluation
   src/requirement src/review src/rubric` 输出为 0 行，同范围
   `git log` 为 0 提交（十八遍全程零 diff——指令要求的空 diff 检查
   通过）。21 文件 1750 行逐目录计数（655/712/309/74）与 R1-H 清单
   一致。R1-H 逐函数下界表 … R14-H 引擎底、R15-H A/A 地板、R16-H
   基底 × 形态格、R17-H 拒绝路径普查与全部 S*-H-* 排除继承有效。
2. **调用图可证不变（最强形式）**：`git log a8625dd..HEAD -- src/`
   **为空**——R17-H 基线以来全部 10 个提交 `--name-only` 逐一确认
   仅触及 `docs/reports/sota-opt/`（R17-F/G/H/I/J 摄取、Round 17
   收口、Round 18 派单/摄取；R18-A/B/D/E/F 全数空枚举无代码落地）⇒
   生产调用方地图数学上不变。本轮全库 import 交叉检索独立双确认：
   切片外 `src/` 引用切片的文件恰 9 个，与 R3-H…R17-H 完全一致——
   值级 7 处：`assertCoverageAllowsStart` ← `run/supervisor.ts:27` /
   `run/coordinator.ts:31` / `run/flowchart-run.ts:21`（每 run 启动
   一次）；`extractHeuristicContract` ← `track/clarify.ts:4`（每 run
   一次）；`applyPrecedence` ← `track/loop.ts:38`（每 run 一次，
   `"user-first"`）；`shouldScout` ← `track/plan.ts:5`（每 run
   一次）；`assertCanPromoteFromReview` ←
   `adaptation/promotion-rules.ts:4`（每晋升一次）；纯类型 2 处
   （`adaptation/promotion.ts:10` ReviewerKind、
   `track/primary-split.ts:6` HeuristicHabits）。`src/evaluation/`
   全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、
   `src/rubric/` 的切片外 `src/` 值级引用**为零**——未把假想接线
   当今日流量。
3. **热层三态锚点重锚**（三次独立运行，本 VM，Node 22.22.2；测量
   条件与 R10-H…R17-H 严格同构——benchAsync 单次预热 + 3000 计时，
   普查轴放在锚点之后以免额外预热污染可比性）：

```text
CEILING default  (9-pass anchor shape, Q=0):         9550 / 9414 / 9402 ns/run
CEILING configured A (habits 三键全设, Q=1 gated):    7019 / 8397 / 6944 ns/run
CEILING configured B (vague objective, Q=3 gated):    3999 / 4721 / 3890 ns/run
CEILING promotion class: assertCanPromoteFromReview = 18.6 / 18.4 / 17.7 ns/promotion
```

   默认态 9.40–9.55µs 与派单点名的 R17-H 热默认锚 **9.17–10.18µs**
   交叠同带（三次全部带内、离散度 <1.6%），居十八轮历史跨 VM 带
   （3.9–10.4µs）上段。两档配置态三次**全部低于**同轮默认态 ⇒
   R8-H…R17-H「无配置态悬崖、默认锚点是保守上界」在本 VM 复现。量级
   类结论不变：整个热面 µs 级，低于落地线 **3–4 个数量级**（按本轮
   锚点，链须每 run 执行 ~1047–1064 次才触及 10ms 保守地板，而调用图
   恰执行一次）。gated 重映射真实执行（production assume-defaults 流
   恒传覆盖全部问题的 `resolvedQuestionIds`）。
4. **命令类矩阵剩余格重锚**（R9-H 首锚的四个零生产调用方类，本轮
   三次复测）：

```text
CEILING evaluate class  (TEST-ONLY): createEvaluationRecord(3 criteria) = 1116 / 1125 / 1096 ns/call
CEILING critique class  (chain 内):  critiqueContract(real contract)    =  611 /  270 /  608 ns/call
CEILING pairwise class  (TEST-ONLY): runBlindPairwisePair               = 2053 / 2044 / 2054 ns/call
CEILING reconcile class (TEST-ONLY): reconcileReviews(n=2)              =   62 /   67 /   67 ns/call
```

   与 R9-H…R17-H 锚点带一致（critique 三次落 R16-H 已见的
   0.26–0.61µs 全幅带）。判据成立：**调用图未变下即便把这四个面全部
   接线，当前规模每类 ≤2.1µs，距落地线仍差 4+ 个数量级**。
5. **冷层锚点复测（默认 + 配置双态）**：dist 产物、新进程、模块
   加载预扣除后，三生产入口首调合计中位数三次：默认态
   **1706 / 1670 / 1678 µs**（区间 [1652, 1849]µs）、配置态
   **1665 / 1660 / 1628 µs**（区间 [1600, 1737]µs）——与 R6-H
   1.65–1.71ms … R17-H 1.6–1.8ms 同带；once-per-process 否决类
   归属不变（S7-H-3 已证该层无净正对策）。

## 2. 第十八遍枚举：生产可达休眠分支普查（新轴）→ 诚实空结果

### 2.1 具名休眠态锚级定价（8 态，与十七轮锚严格同协议）

前十七轮的全部热链读数都来自**三个夹具态**；本轮取证发现生产链存在
普通输入即可到达、但三态从未执行的分支（§0），逐一以 R17-H
measureChain 原协议（单预热 + 3000 计时 + 满门 + precedence）定价。
三次运行读数（ns/run，once-per-run 生产全价）：

```text
D1  plain（无 tests 关键词 ⇒ q-tests 推入首次执行, Q=1 gated）:  4512 / 4252 / 4944
D1q qa 关键词（wantsTests=真 且 q-tests 仍推入, Q=1 gated）:     4346 / 4655 / 4836
D2a rename 族 scope-ask（Q=2, namedTargets 二次调用活化）:       4633 / 4696 / 4676
D2b tiny 族 scope-ask（shouldScout 第二臂, Q=2）:               4387 / 4432 / 4389
D3  三目标（targets.map 多元素臂, 3 个 d-file, Q=1）:           2944 / 2928 / 2928
D4  vague + requireTests（q-done 与 ac-tests 并存, Q=2）:       3745 / 3706 / 3694
G1  skip-contract 门快路径（plain run 生产默认）:            11.9 / 12.4 / 12.5 ns
G2  非 skip 极小合同全门（C=1, T=1, Q=0）:                    287 / 274 / 268 ns
```

收口事实：

1. **全部具名休眠态低于同轮默认锚**（最贵 D1 4.94µs < 默认
   9.40µs）——默认锚点（长 objective + 路径正则命中 + tests 关键词）
   仍是全生产态空间的保守上界。D1 是现实中最常见态（普通用户
   objective 多半不写 "tests"），其 4.25–4.94µs 说明**最常见生产态
   比十七轮一直用的锚点便宜约一半**。
2. **G1 = plain run 的生产默认门价**：`run/episode-bind.ts` 对未带
   合同的 run 默认构造 skip 合同（`skipContract !== false`）⇒ 三个
   run 入口的 `assertCoverageAllowsStart` 生产默认走 skip 快路径，
   **11.9–12.5ns**/run-start——该路径十七轮从未单独定价（R15-H 只
   对 isSkipContract 的 some 做过查表排除）。attestation：tasks=[]
   时非 skip 合同必抛（run-complete 未覆盖）而 skip 合同原样返回 ⇒
   早退臂被观察证实执行。
3. **活化注记落账**：D2a/D2b 使 namedTargets 二次调用与 q-scope 推入
   真实执行——S1-H-3 家族的噪声类裁决在活化态下经实测维持（全链仅
   4.4–4.7µs，二次调用增量必然亚 µs）；q-scope/q-write
   `questions.some` 守卫（R4-H no-ID）在 Q≥1 态下真实扫描，价随格
   测得，同样维持。
4. **频次类裁决**：8 态全部 once-per-run、µs 级或 ns 级，距数十 ms
   落地线 3–4 个数量级 ⇒ 该面结构上不存在达门槛候选。

### 2.2 满格生产配置态网格（27 × 7 = 189 格）

`track/clarify.ts` 的 `habitsFromPreferences` 每键仅在偏好为布尔时
注入 ⇒ habit 原始空间 = {缺省, true, false}³ = 27 组合全部生产可达；
objective 为自由用户文本 ⇒ 按分支语义划 7 类（O1 锚形 path+tests /
O2 plain 无 tests 关键词 / O3 qa 关键词 / O4 rename 族 / O5 tiny 族 /
O6 三目标 / O7 vague）。前十七轮只定价过该网格的 **3/189** 格。本轮
逐格定价（每格 = 完整生产链 extract + 满门 + precedence，1 预热 +
800 计时；网格在锚点与具名态之后运行 ⇒ 全暖稳态口径，格间同质可比，
绝对值天然低于保守锚——裁决按量级带，不做格间决斗）。三次运行：

```text
GRID 全局: 189/189 格定价; min = 2335 / 2332 / 2334 ns; max = 7313 / 6882 / 6834 ns
GRID 全局 argmax 三次恒为 [O1 anchor | rt=U,pr=U,aw=T]（Q=1 gated 锚形 objective）
GRID 逐类 max（run1/2/3）: O1 7313/6882/6834; O2 4501/4364/4523; O3 3795/3794/3810;
                            O4 4396/4356/5841; O5 4113/4295/4166; O6 3861/3853/3962; O7 3058/3231/3452
GRID 观察到恰 10 个互异 question 集合: (none) / q-tests / q-write / q-tests+q-write /
     q-scope / q-scope+q-tests / q-scope+q-write / q-scope+q-tests+q-write /
     q-done+q-tests / q-done+q-tests+q-write
```

收口事实：

1. **满格无悬崖**：全局最大格 6.8–7.3µs（三次 `<100µs` 全格断言
   通过），仍低于同轮默认锚 9.4–9.6µs ⇒ 整个生产配置态空间在 µs
   带内，不存在任何被三态夹具遮蔽的昂贵态（R7-I 教训在本切片的
   满格排除）。
2. **分支覆盖 attestation（13 项，×3 运行全过）**：q-done 对 /
   q-tests 推入 / q-scope 推入（rename 与 tiny 双臂）/ q-write 推入 /
   wantsTests 三来源（habit、regex、rt=F 抑制）/ confidence 三值
   （0.55/0.8/0.86 与 requiresUserDecision 联动）/ d-file 0/1/3
   目标臂 / vague deliverable 描述臂 / qa 并存角落 / vague ×
   requireTests 组合——每个枚举出的生产可达分支格至少被一个网格格
   执行，全部经**生产输出形状**观察（零插桩、零复刻）。检查项与
   §2.1/§2.3 合计 **428 项 ×3 运行全绿**。
3. **格间结构与机制自洽**：O7 vague 格恒最便宜（短句正则早退）、
   O1 锚形恒最贵（长句 + 路径正则命中 + q-write gated 组合位居
   argmax）、O3 qa 格全 27 格离散 <12%（question 集合仅随 aw 变）
   ——读数与代码结构完全对应，普查可信。
4. **不产生新候选**：网格证明的是**预算上界的完备性**（任意生产态
   ≤7.3µs）；不存在某格慢到值得结构改写的病理。哪怕把全 189 格的
   成本全部消为零，每 run 也只省一次 µs 级调用——距落地线 3–4 个
   数量级，候选化之前整面拒列，不铸 ID。

### 2.3 零生产流量休眠分支：语义 attestation（4 类，不定价）

- **Z1 spec-first / latest-first 胜者臂**（`ordered.at(-1)`）：
  loop.ts 恒传 "user-first" ⇒ 零流量。attestation：冲突合同下两规则
  均保留 **ac-2** 并压入 `a-superseded-ac-1`（×3 全过）。
- **Z2 预置 default 问题快路径**（gated 重映射首合取）：heuristic
  从不产 default ⇒ 零流量。attestation：带 default 的未决问题不阻断
  （门过）。
- **Z3 空 options "resolved" 回退**（`options[0] ?? "resolved"`）：
  schema 合法但 heuristic 恒产非空 options ⇒ 零流量。attestation：
  空 options 问题被 resolve 后门过；未 resolve 时 fail-closed 阻断
  （对照，属 R17-H T8 腿语义复证，无定价）。
- **Z4 调用方自带 sources 臂**（extractHeuristicContract 首三元）：
  clarify 恒不传 ⇒ 零流量。attestation：自带 trusted source 的 ref
  贯穿到 contract.sourceRefs。

裁决：四类零流量 ⇒ 零收益池；语义全部正确（fail-open 零发现）；
候选化之前整类拒列，不铸 ID。

### 2.4 结构位置 × 消除机制扫描 + 确定性 attestation

第十八遍全格对账（结构位置：入口守卫 / 循环体 / 返回构造 / 跨调用
重复 / 数据字面量 / 正则求值 / 防御校验 / 拷贝语义 / 异步机器 /
模块加载 / 错误路径 / 载荷基底 / 载荷形态 / **配置态分支（本轮满格
收口）** × 消除机制：死码 / CSE / 融合 / 快路径 / 记忆化 / 表示变换 /
去糖 / 惰性 / 可变化 / 复杂度类）。「从未点名结构位点」收敛序列：
R15-H 0 处 → R16-H 1 处 → R17-H 0 处 → **本轮 0 处**——第十八遍
读码发现的每一个微观位点都在 §0 的换名审计中对到既有 ID / no-ID
收口记录；普查轴的 189 格 + 8 具名态 + 4 类零流量臂全部是既有生产
函数的原样执行面（活化 ≠ 新位点；"qa" 角落是语义观察非位点），
定价/attestation 不等于铸 ID。428 项确定性 fail-closed/sanity 检查
×3 运行全过（含派单要求的三项 S5-H-1 语义 attestation、13 项分支
覆盖 attestation、Z1–Z4、G1/G2 观察断言、盲评换位/门 fail-closed
断言）。1750 行 × 18 遍后，切片可优化面（导出 + 私有 + 剖面 + 规模 +
引擎 + 协议分辨率 + 基底 + 形态 + 错误路径 + **配置态满格**）十向
闭合。**不立任何 S18-H-* ID**（与 R10-H…R17-H 及 R18-A/B/D/E/F 空
枚举收口同型）。

### 2.5 收口陈述

十八遍合计：单函数、函数间、跨模块、调用图、模块图、执行层级、
调用机器、配置态、命令类、编译器地板、导出面完备性、私有面完备性、
成本剖面归账、规模-越线标定、引擎态基底、A/A 决斗噪声地板、载荷
基底 × 形态、fail-closed 拒绝路径普查、**生产可达休眠分支普查（满格
配置态网格）**十九个面全部有实测锚点或对账记录。本切片在当前调用图
下**结构上不存在**达门槛候选。总门槛按本 VM 数字刷新：任何未来候选
须先推翻满格矩阵实测上界——热层默认 **9.40–9.55µs/run**（3000-rep
保守口径）、**全生产配置态网格 189 格 ≤7.3µs（全暖口径）**、四个零
调用方命令类 ≤2.1µs、冷层 **1.6–1.8ms once-per-process**（否决类）、
skip 门生产默认路径 **~12ns**、最贵 fail-closed 拒绝 11.5–12.1µs
（R17-H，稳态流量零）。重开该切片的唯一前提维持 R4-H…R17-H 收口
原文：调用图变更（evaluation/review/rubric 面接入每 turn 热路径，或
合同规模增长 ≥2 个数量级——R13-H 已标定），或 objective 输入面出现
对抗/程序化来源或 ≥3.2K 字符无斜杠 token 段的真实输入类（R16-H 已
标定）。本轮新增对账注记：**配置态维度已满格闭合**——habit 空间
27 组合 × 7 objective 类逐格定价后，「未测配置态藏悬崖」这一重开
理由从此不再成立（任何新 habit 键或新问题类需先改代码 ⇒ 落入调用图
变更前提）。

## 3. 逐文件收口（第十八遍新视角，其余与 R1-H…R17-H 一致）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `requirement/heuristic.ts` | **满格网格宿主（189 格 2.3–7.3µs，§2.2）+ 六个具名休眠态锚级定价（D1/D1q/D2a/D2b/D3/D4 = 2.93–4.94µs，§2.1）**；q-tests/q-scope 推入臂、namedTargets 二次调用活化态、"qa" 语义角落首次落账；S1-H-3（活化态下裁决维持）、S3-H-3、S6-H-4、S7-H-1、S8-H-2、R12-H P3、R16-H PATH_RE 拒列维持 | 无达门槛候选 |
| `requirement/coverage.ts` | **G1 skip-contract 门快路径首次定价（11.9–12.5ns/run-start，plain run 生产默认路径）+ G2 极小合同全门 268–287ns**；Q≥1 网格格真实执行 gated 重映射；S8-H-3 零形态、S1-H-1/2、S4-H-1、S6-H-2/3、R12-H P1/P2、R13-H Face C 标定、R14-H/R17-H throw 面维持 | 无新候选 |
| `requirement/normalizer.ts` | 网格全格经 normalizeSources 单源生产形态；Z4 自带 sources 臂语义 attestation（零流量收口）；S2-H-7、S7-H-2、R5-H slice、R11-H L3、R16-H SLICED、R17-H T10/T11 维持 | 无新候选 |
| `requirement/precedence.ts` | **S5-H-1 守卫在位复核（L24 早退，零 diff + 三项语义 attestation ×3 运行全过）**；Z1 spec-first/latest-first 胜者臂语义 attestation（零流量收口）；S1-H-5、S4-H-2、S6-H-1 维持 | 无新候选 |
| `requirement/extractor.ts` | 网格/具名态全部经 buildContractCandidate 生产链；requiresUserDecision 两值随格 attestation；S1-H-4、S2-H-1/2、S3-H-1、S7-H-1、R15-H 查表排除、R17-H T1–T6 维持 | 无新候选 |
| `requirement/critic.ts` / `provenance.ts` | critique 锚点 270–611ns 带内（R16-H 全幅带）；网格全格 critique 干净（score=100 于生产合同）；S2-H-3、S3-H-2、S4-H-2、R12-H P2 站点维持 | 无新候选 |
| `evaluation/evaluator.ts` | evaluate 类锚点复测同带（1096–1125ns）；S1-H-7、R5-H evidence 双读、R1-H 表族、R10-H String()、R17-H T12/T13 维持 | 无新候选 |
| `evaluation/check-adapter.ts` / `delivery-adapter.ts` / `diff-adapter.ts` | 零生产调用方维持（R9-H…R17-H 命令类天花板辖区 + R17-H A1–A15 定价维持）；本轮零新形态 | 无新候选 |
| `evaluation/ownership.ts` / `precedence.ts` / `adapters.ts` / `types.ts` | X4-9、R10-H 去糖、R2-H/S2-H-5、纯类型面维持 | 无新候选 |
| `review/self-review.ts` | 晋升类锚点 17.7–18.6ns 同带；S9-H-1 / S9-H-2、R17-H T14–T17 维持 | 无新候选 |
| `review/pairwise.ts` / `reconcile.ts` / `critic.ts` | pairwise/reconcile 锚点复测同带（2044–2054 / 62–67ns）；盲评换位/slot-bound 断言 ×3 全过；reconcile L39 = R12-H P2 第四站点维持；S8-H-1、S1-H-6、R17-H A16 维持 | 无新候选 |
| `rubric/registry.ts` / `types.ts` | S1-H-8 反例 + S2-H-6 + R10-H createRubric 时间戳取证维持；DEFAULT_REGISTRY 别名安全第十次复核成立 | 无新候选 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。无仿真脚本入库（无赢家，
遵循 round01–17 仅赢家 sim 入库惯例 + 本轮指令明文；完整脚本见
附录）。

## 5. 测试与门

`pnpm gate` 全绿（Node 22.22.2 via nvm，满足 engines >=22.19.0；
系统 Node 22.14.0 过低 + `/exec-daemon/node` PATH 遮蔽的环境注记见
§0；`pnpm install --frozen-lockfile` 后）：

```bash
pnpm gate   # typecheck ✓ + lint ✓ + test: tests 1169 / pass 1168 / fail 0 / skipped 1 ✓ + build ✓
```

切片相关套件基线复核，全绿：

```bash
npx tsx --test "test/unit/requirement/*.test.ts" "test/unit/evaluation/*.test.ts" \
  "test/unit/review/*.test.ts" "test/integration/m3/checkpoint-d.test.ts" \
  "test/integration/m3/coverage-gate.test.ts" \
  "test/integration/m3/requirement-extraction.test.ts" \
  "test/integration/m4/delivery-evidence.test.ts"
# tests 93 / suites 13 / pass 93 / fail 0（与 R1-H…R17-H 同套件同计数）
```

锚点 + 休眠分支普查测量（临时脚本 `/tmp/r18h-sim.mts`，无赢家故未
入库；完整源码见附录；`npx tsx` 运行，冷层探针需要 dist 产物）三次
独立运行，428 项确定性 fail-closed/sanity 检查全部通过、裁决结论
逐位一致（默认锚点三次 9550/9414/9402ns、配置 A 7019/8397/6944ns、
配置 B 3999/4721/3890ns；晋升类 18.6/18.4/17.7ns；evaluate
1116/1125/1096ns、critique 611/270/608ns、pairwise 2053/2044/2054ns、
reconcile 62/67/67ns；具名休眠态 D1 4512/4252/4944ns … D4
3745/3706/3694ns、G1 11.9/12.4/12.5ns、G2 287/274/268ns；网格全局
max 7313/6882/6834ns、min 2335/2332/2334ns、argmax 三次恒同格；冷层
默认 1706/1670/1678µs、配置 1665/1660/1628µs）。

## 6. 新增排除 ID

**无。** 第十八遍枚举为空——生产可达休眠分支普查给切片配置态维度
首次满格收口（189 格 2.3–7.3µs 全部 µs 带 + 8 个具名休眠态锚级定价
全部低于默认锚 + 13 项分支覆盖 attestation + 4 类零流量臂语义收口，
§2.1–§2.3），S1-H-3 家族裁决在活化态下经实测维持，结构位置 × 消除
机制扫描零从未点名结构位点（§2.4）。不硬凑 ID，不为定价已判面铸
ID。与 R10-H…R17-H 及 R18-A/B/D/E/F 的空枚举收口同型。

## 附录：确定性锚点 + 生产可达休眠分支普查测量脚本（完整，可复现）

运行方式：`pnpm build` 后保存为任意 `.mts` 并 `npx tsx <file>`（仓库
根目录，依赖已装；冷层探针需要 dist 产物）。无赢家 ⇒ 无随机 fuzz
（无候选可裁决），428 项 sanity/fail-closed 检查全为固定输入确定性
断言（含派单要求的三项 S5-H-1 语义 attestation、13 项分支覆盖
attestation、189 格已知 question-id/confidence 集合断言、Z1–Z4 与
G1/G2 观察断言），三次运行为精确重执行。按惯例不入库。

```ts
/**
 * R18-H deterministic anchor re-verification + PRODUCTION-REACHABLE
 * DORMANT-BRANCH CENSUS (eighteenth pass).
 *
 * No fresh candidate survived enumeration (every micro-site found by the
 * eighteenth read maps to an exclusion-table ID or a prior no-ID closure —
 * see report §0/§2), so this script (a) re-establishes, on THIS VM, the full
 * measurement matrix that R8-H..R17-H closed — hot tier three states
 * (default Q=0 / configured A habits+Q=1 gated / configured B vague+Q=3
 * gated), promotion class, the four zero-production-caller command classes
 * (evaluate / critique / pairwise / reconcile), and cold-tier
 * once-per-process first-call totals on dist — and (b) runs the eighteenth
 * pass's NEW completeness axis for this slice: the PRODUCTION-REACHABLE
 * DORMANT-BRANCH CENSUS (mirroring R18-A's skip-path composition pricing and
 * R18-B's zero-execution configured-branch axis in their slices).
 *
 *   Seventeen passes priced the hot chain through exactly THREE fixture
 *   states (default / configured A / configured B). Several branches of the
 *   production-called chain are never executed by any of those states, yet
 *   are reachable by ordinary production inputs: the q-tests push
 *   (heuristic.ts L64-70 — arguably the MOST common real-world state: a
 *   non-vague objective that does not mention tests), the q-scope push
 *   (L71-77, activated by rename-family / tiny-family objectives, which
 *   also activates the namedTargets second call that prior rounds recorded
 *   as unreachable in the anchored states), the zero-target and multi-target
 *   deliverable arms, the vague+requireTests combination, and the
 *   skip-contract gate fast path (production-reachable: run/episode-bind.ts
 *   builds skip contracts by default for plain runs). This axis closes that
 *   face: (1) the FULL production configured-state grid — 27 raw habit
 *   combos x 7 objective classes = 189 cells — is priced cell by cell,
 *   (2) every enumerated production-reachable branch cell is attested as
 *   executed by at least one grid cell via observable output shape (no
 *   instrumentation, no replicas), (3) the named dormant states get
 *   anchor-grade pricing on the exact R17-H harness so they are comparable
 *   with seventeen rounds of anchors, and (4) the zero-production dormant
 *   branches (spec-first / latest-first winner arm, pre-set-default
 *   question fast path, empty-options "resolved" fallback, caller-provided
 *   sources arm) get deterministic semantic attestations without minting
 *   any candidate. Expected adjudication: every cell stays in the µs band
 *   (3-4 orders below the tens-of-ms landing bar) and the default anchor
 *   remains a conservative ceiling or is exceeded only µs-fractionally.
 *
 * Reference = production imports everywhere; no candidate is proposed, so
 * there is no randomized fuzz — every sanity/fail-closed check below is
 * fully deterministic (fixed inputs; includes the three S5-H-1 semantic
 * attestations required by the dispatch), and three runs are exact
 * re-executions (timing varies, adjudication class must not).
 *
 * Run: pnpm build, then npx tsx /tmp/r18h-sim.mts   (repo root, deps
 * installed; cold probes need dist).
 */
import { performance } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import {
  extractHeuristicContract,
  type HeuristicHabits
} from "/workspace/src/requirement/heuristic.js";
import {
  assertCoverageAllowsStart,
  checkCoverageGate,
  coverageMatrixFromTasks,
  type CoverageTaskRef,
  type CoverageStartOptions
} from "/workspace/src/requirement/coverage.js";
import { applyPrecedence, detectConflicts } from "/workspace/src/requirement/precedence.js";
import { critiqueContract } from "/workspace/src/requirement/critic.js";
import { createTrustedSource } from "/workspace/src/requirement/normalizer.js";
import { validateRequirementContract, type RequirementContract } from "/workspace/src/domain/contract.js";
import { assertCanPromoteFromReview } from "/workspace/src/review/self-review.js";
import { runBlindPairwisePair, type PairwiseInput } from "/workspace/src/review/pairwise.js";
import { reconcileReviews } from "/workspace/src/review/reconcile.js";
import { createEvaluationRecord } from "/workspace/src/evaluation/evaluator.js";
import { createRubric } from "/workspace/src/rubric/types.js";
import type { EpisodeId, TaskId } from "/workspace/src/domain/ids.js";

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean, detail?: string): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function median(xs: number[]): number {
  const sorted = [...xs].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)] ?? Number.NaN;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

const OBJ = "fix the login retry bug in src/auth/session.ts and keep tests green";

function buildArgs(objective: string, habits: HeuristicHabits | undefined) {
  return { objective, ...(habits !== undefined ? { habits } : {}) };
}
function tasksFor(contract: RequirementContract, count = 5): CoverageTaskRef[] {
  return Array.from({ length: count }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: contract.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
}
function optionsFor(contract: RequirementContract): CoverageStartOptions | undefined {
  return contract.questions.length > 0
    ? { resolvedQuestionIds: contract.questions.map((question) => question.id) }
    : undefined;
}

/* ================================================================
 * Hot tier, three states (R8-H..R17-H anchor shapes). Measured FIRST so
 * anchor conditions stay comparable (benchAsync single warm call + 3000
 * timed reps, no extra chain executions before). measureChain is reused
 * verbatim for the named dormant states in the census below, so those
 * prices are anchor-grade comparable.
 * ================================================================ */
async function measureChain(
  label: string,
  objective: string,
  habits: HeuristicHabits | undefined
): Promise<void> {
  const args = buildArgs(objective, habits);
  const chain = await benchAsync(async () => {
    await extractHeuristicContract(args);
  }, 3000);
  const prod = await extractHeuristicContract(args);
  const real = prod.contract;
  const tasks = tasksFor(real);
  const options = optionsFor(real);
  const gate = bench(() => assertCoverageAllowsStart(real, tasks, options), 50000);
  const precedence = bench(() => void applyPrecedence(real, "user-first"), 100000);
  const total = chain + gate + precedence;
  log(
    `CEILING ${label}: extractHeuristicContract=${(chain * 1e6).toFixed(0)}ns + run-start gate(Q=${real.questions.length}${options !== undefined ? ", all resolved" : ", no options"})=${(gate * 1e6).toFixed(0)}ns + applyPrecedence=${(precedence * 1e6).toFixed(0)}ns = ${(total * 1e6).toFixed(0)}ns once-per-run production total`
  );
}
{
  await measureChain("default (9-pass anchor shape, Q=0)", OBJ, undefined);
  await measureChain("configured A (habits set, Q=1 gated)", OBJ, {
    requireTests: true,
    preferReview: false,
    askBeforeWrite: true
  });
  await measureChain("configured B (vague, Q=3 gated)", "fix bug please", { askBeforeWrite: true });
}

/* ================================================================
 * Command-class anchors beyond run-start (R9-H..R17-H mirror).
 * ================================================================ */
{
  const promo = bench(
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    200000
  );
  log(`CEILING promotion class: assertCanPromoteFromReview=${(promo * 1e6).toFixed(1)}ns once per promotion (production)`);

  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const evaluate = bench(() => void createEvaluationRecord(evalInput), 50000);
  log(`CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=${(evaluate * 1e6).toFixed(0)}ns/call`);

  const prod = await extractHeuristicContract({ objective: OBJ });
  const critique = bench(() => void critiqueContract(prod.contract), 50000);
  log(`CEILING critique class (inside run-start chain): critiqueContract(real contract)=${(critique * 1e6).toFixed(0)}ns/call`);

  const pairInput: PairwiseInput = {
    episodeId: "ep_00000001" as EpisodeId,
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pairwise = bench(() => void runBlindPairwisePair(pairInput, () => 0.7), 20000);
  const pair = runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  const reconcile = bench(() => void reconcileReviews(two), 100000);
  log(`CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=${(pairwise * 1e6).toFixed(0)}ns/call`);
  log(`CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=${(reconcile * 1e6).toFixed(0)}ns/call`);
  check("pairwise identity-stable scores agree across swap", pair.reconciliation.consensus === "a");
  const biased = runBlindPairwisePair(pairInput, () => 0.7, { bindScoresToSlots: true });
  check("pairwise slot-bound scores reconcile to uncertain", biased.reconciliation.consensus === "uncertain");
  const record = createEvaluationRecord(evalInput);
  check("evaluate deterministic FAIL on missing evidence", record.overall === "FAIL");
  check(
    "evaluate per-criterion outcomes",
    record.scores.map((s) => s.outcome).join(",") === "PASS,PASS,FAIL"
  );
}

/* ================================================================
 * NEW AXIS, part A: named dormant states, anchor-grade pricing (exact
 * measureChain harness, so directly comparable with 17 rounds of anchors).
 * Every state below is reachable by ordinary production input (a user
 * objective via `track --objective` plus preference-derived habits), yet
 * none of the seventeen rounds' three fixture states ever executed its
 * distinguishing branch.
 * ================================================================ */
log("--- DORMANT-STATE CENSUS: production-reachable states never anchored ---");
{
  // D1: non-vague objective WITHOUT tests/coverage keyword, no habits.
  // Activates heuristic.ts L64-70 (q-tests push) — never executed by
  // default (objective contains "tests"), A (requireTests defined), or
  // B (vague). Arguably the most common real-world state.
  await measureChain("D1 plain (no tests keyword, Q=1 q-tests gated)", "fix the login retry logic in the session helper", undefined);

  // D1q: "qa" keyword — wantsTests true via regex, yet the q-tests push
  // guard (which omits qa) still pushes the question. Semantic corner:
  // both ac-tests and q-tests present.
  await measureChain("D1q qa-keyword (wantsTests via qa + q-tests push, Q=1 gated)", "run qa on the login flow and fix the retry bug", undefined);

  // D2a: rename-family objective — shouldScout false => shouldAskScope
  // runs namedTargets (the second namedTargets call activates at L85),
  // q-scope pushed. Q=2 (q-tests + q-scope).
  await measureChain("D2a rename-family scope-ask (Q=2 gated, namedTargets x2)", "rename the misspelled retry counter variable", undefined);

  // D2b: tiny-family objective — shouldScout false via the one-line/
  // trivial/tiny arm. Q=2 (q-tests + q-scope).
  await measureChain("D2b tiny-family scope-ask (Q=2 gated)", "make a tiny fix to the retry backoff logic", undefined);

  // D3: multi-target objective — targets.map arm with 3 file deliverables
  // (default anchor always had exactly 1).
  await measureChain("D3 multi-target (3 paths, Q=1 gated)", "fix src/auth/session.ts and src/auth/retry.ts then update docs/auth.md", undefined);

  // D4: vague + requireTests habit — vague question pair AND ac-tests/
  // c-tests together (B had askBeforeWrite only, so wantsTests was false).
  await measureChain("D4 vague + requireTests (Q=2 gated, 2 criteria)", "fix bug please", { requireTests: true });
}
{
  // G1: skip-contract gate fast path. Production-reachable: plain runs
  // (run/episode-bind.ts contractFromObjective(objective, true)) build a
  // skip contract by default, and supervisor/coordinator/flowchart-run
  // call assertCoverageAllowsStart on it every run start. Exact
  // production shape replicated below. Attestation: tasks=[] leaves
  // run-complete uncovered, so a non-skip contract would throw — the
  // early return is therefore observably exercised.
  const skipContract = validateRequirementContract({
    schemaVersion: 1,
    objective: OBJ,
    deliverables: [],
    constraints: [],
    nonGoals: ["unspecified-scope"],
    acceptanceCriteria: [
      {
        id: "run-complete",
        description: "The run reaches a terminal status",
        observableCheck: "run.status is COMPLETED, FAILED, or CANCELLED"
      }
    ],
    assumptions: [{ id: "skip-contract", statement: "Caller did not supply a versioned contract", source: "cli" }],
    questions: [],
    authority: [],
    sourceRefs: [{ kind: "message", ref: "skip-contract", excerpt: OBJ.slice(0, 200) }]
  });
  let skipThrew = false;
  try {
    assertCoverageAllowsStart(skipContract, []);
  } catch {
    skipThrew = true;
  }
  check("G1 skip-contract gate early-returns even with zero covering tasks", !skipThrew);
  const g1 = bench(() => assertCoverageAllowsStart(skipContract, []), 200000);
  log(`DORMANT G1 skip-contract gate fast path: ${(g1 * 1e6).toFixed(1)}ns/run-start (production default for plain runs)`);

  // G2: non-skip minimal plain-run contract (contractFromObjective(_, false))
  // through the full gate with a covering task — the other episode-bind arm.
  const minimalContract = validateRequirementContract({
    schemaVersion: 1,
    objective: OBJ,
    deliverables: [],
    constraints: [],
    nonGoals: [],
    acceptanceCriteria: [
      {
        id: "run-complete",
        description: "The run reaches a terminal status",
        observableCheck: "run.status is COMPLETED, FAILED, or CANCELLED"
      }
    ],
    assumptions: [],
    questions: [],
    authority: [],
    sourceRefs: [{ kind: "message", ref: "cli-objective", excerpt: OBJ.slice(0, 200) }]
  });
  const minimalTasks = tasksFor(minimalContract, 1);
  let minimalThrew = false;
  try {
    assertCoverageAllowsStart(minimalContract, minimalTasks);
  } catch {
    minimalThrew = true;
  }
  check("G2 minimal non-skip contract passes the full gate when covered", !minimalThrew);
  const g2 = bench(() => assertCoverageAllowsStart(minimalContract, minimalTasks), 100000);
  log(`DORMANT G2 minimal plain-run contract full gate (C=1, T=1, Q=0): ${(g2 * 1e6).toFixed(0)}ns/run-start`);
}

/* ================================================================
 * NEW AXIS, part B: FULL production configured-state grid. 27 raw habit
 * combos (requireTests/preferReview/askBeforeWrite each in {absent, true,
 * false}) x 7 objective classes = 189 cells. Prior rounds priced exactly 3
 * of these cells. Each cell = full production chain (extract + covered
 * gate + precedence), 800 reps after 1 warm call. Adjudication: every cell
 * must stay µs-class; the global max is the new conservative ceiling.
 * ================================================================ */
log("--- DORMANT-BRANCH CENSUS: full 27x7 configured-state grid ---");
{
  const tri: (boolean | undefined)[] = [undefined, true, false];
  const habitCombos: { key: string; habits: HeuristicHabits | undefined }[] = [];
  for (const rt of tri) {
    for (const pr of tri) {
      for (const aw of tri) {
        const habits: Record<string, boolean> = {};
        if (rt !== undefined) habits["requireTests"] = rt;
        if (pr !== undefined) habits["preferReview"] = pr;
        if (aw !== undefined) habits["askBeforeWrite"] = aw;
        const key = `rt=${rt === undefined ? "U" : rt ? "T" : "F"},pr=${pr === undefined ? "U" : pr ? "T" : "F"},aw=${aw === undefined ? "U" : aw ? "T" : "F"}`;
        habitCombos.push({ key, habits: Object.keys(habits).length > 0 ? (habits as HeuristicHabits) : undefined });
      }
    }
  }
  check("habit grid has 27 raw combos", habitCombos.length === 27);

  const objectiveClasses: { key: string; objective: string }[] = [
    { key: "O1 anchor(path+tests)", objective: OBJ },
    { key: "O2 plain(no-tests-kw)", objective: "fix the login retry logic in the session helper" },
    { key: "O3 qa-keyword", objective: "run qa on the login flow and fix the retry bug" },
    { key: "O4 rename-family", objective: "rename the misspelled retry counter variable" },
    { key: "O5 tiny-family", objective: "make a tiny fix to the retry backoff logic" },
    { key: "O6 multi-target(3)", objective: "fix src/auth/session.ts and src/auth/retry.ts then update docs/auth.md" },
    { key: "O7 vague", objective: "fix bug please" }
  ];

  const KNOWN_QUESTION_IDS = new Set(["q-done", "q-tests", "q-scope", "q-write"]);
  const KNOWN_CONFIDENCES = new Set([0.55, 0.8, 0.86]);
  let globalMax = -Infinity;
  let globalMaxKey = "";
  let globalMin = Infinity;
  const seenQuestionSets = new Set<string>();
  for (const { key: oKey, objective } of objectiveClasses) {
    const cellCosts: number[] = [];
    let classMaxKey = "";
    let classMax = -Infinity;
    for (const { key: hKey, habits } of habitCombos) {
      const args = buildArgs(objective, habits);
      const probe = await extractHeuristicContract(args);
      const contract = probe.contract;
      check(
        `grid(${oKey}|${hKey}) question ids all known`,
        contract.questions.every((q) => KNOWN_QUESTION_IDS.has(q.id)),
        contract.questions.map((q) => q.id).join(",")
      );
      check(
        `grid(${oKey}|${hKey}) confidence in known set`,
        KNOWN_CONFIDENCES.has(probe.confidence),
        String(probe.confidence)
      );
      seenQuestionSets.add(contract.questions.map((q) => q.id).sort().join("+") || "(none)");
      const tasks = tasksFor(contract);
      const options = optionsFor(contract);
      const per = await benchAsync(async () => {
        const cand = await extractHeuristicContract(args);
        assertCoverageAllowsStart(cand.contract, tasks, options);
        void applyPrecedence(cand.contract, "user-first");
      }, 800);
      cellCosts.push(per);
      if (per > classMax) {
        classMax = per;
        classMaxKey = hKey;
      }
      if (per > globalMax) {
        globalMax = per;
        globalMaxKey = `${oKey} | ${hKey}`;
      }
      if (per < globalMin) globalMin = per;
    }
    const sorted = [...cellCosts].sort((a, b) => a - b);
    log(
      `GRID ${oKey}: 27 cells, min=${(sorted[0]! * 1e6).toFixed(0)}ns med=${(median(cellCosts) * 1e6).toFixed(0)}ns max=${(classMax * 1e6).toFixed(0)}ns (argmax ${classMaxKey})`
    );
  }
  log(
    `GRID GLOBAL: 189/189 cells priced; min=${(globalMin * 1e6).toFixed(0)}ns; max=${(globalMax * 1e6).toFixed(0)}ns at [${globalMaxKey}]`
  );
  log(`GRID distinct question sets observed: ${[...seenQuestionSets].sort().join("  ")}`);
  check("grid: every one of the 189 cells stays below 100µs (µs-class)", globalMax < 0.1);

  /* Branch-coverage attestation matrix: every production-reachable branch
   * cell of the chain is executed by at least one grid cell, observed via
   * output shape (no instrumentation, no replicas). */
  const ids = (c: RequirementContract) => c.questions.map((q) => q.id);
  const hasCrit = (c: RequirementContract, id: string) => c.acceptanceCriteria.some((a) => a.id === id);
  const hasConstraint = (c: RequirementContract, id: string) => c.constraints.some((k) => k.id === id);
  const fileDeliverables = (c: RequirementContract) => c.deliverables.filter((d) => d.id.startsWith("d-file-")).length;

  const vaguePlain = await extractHeuristicContract({ objective: "fix bug please" });
  check("BC1 vague question pair (q-done) executes", ids(vaguePlain.contract).includes("q-done"));
  check("BC1b vague confidence 0.55 + requiresUserDecision", vaguePlain.confidence === 0.55 && vaguePlain.requiresUserDecision);
  check(
    "BC1c vague deliverable description arm",
    vaguePlain.contract.deliverables[0]?.description === "Change set matching the clarified objective"
  );

  const plain = await extractHeuristicContract({ objective: "fix the login retry logic in the session helper" });
  check("BC2 q-tests push executes (plain objective, rt=U)", ids(plain.contract).join(",") === "q-tests");
  check("BC2b plain has single criterion (no ac-tests)", plain.contract.acceptanceCriteria.length === 1 && !hasCrit(plain.contract, "ac-tests"));
  check("BC2c plain has no c-tests constraint", !hasConstraint(plain.contract, "c-tests"));
  check("BC2d zero-target arm (no d-file deliverables)", fileDeliverables(plain.contract) === 0);

  const rename = await extractHeuristicContract({ objective: "rename the misspelled retry counter variable" });
  check("BC3 q-scope push executes (rename-family)", ids(rename.contract).includes("q-scope"));
  check("BC3b rename-family Q=2 (q-tests + q-scope)", ids(rename.contract).sort().join(",") === "q-scope,q-tests");
  const tiny = await extractHeuristicContract({ objective: "make a tiny fix to the retry backoff logic" });
  check("BC4 q-scope push executes (tiny-family arm of shouldScout)", ids(tiny.contract).includes("q-scope"));

  const write = await extractHeuristicContract({
    objective: "fix the login retry logic in the session helper",
    habits: { askBeforeWrite: true }
  });
  check("BC5 q-write push executes (aw=T)", ids(write.contract).includes("q-write"));

  const habitTests = await extractHeuristicContract({
    objective: "fix the login retry logic in the session helper",
    habits: { requireTests: true }
  });
  check("BC6 wantsTests via habit (ac-tests, no q-tests)", hasCrit(habitTests.contract, "ac-tests") && !ids(habitTests.contract).includes("q-tests"));

  const anchor = await extractHeuristicContract({ objective: OBJ });
  check("BC7 wantsTests via regex (anchor, Q=0)", hasCrit(anchor.contract, "ac-tests") && anchor.contract.questions.length === 0);
  check("BC7b single-target arm (1 d-file deliverable)", fileDeliverables(anchor.contract) === 1);

  const testsOff = await extractHeuristicContract({
    objective: "fix the login retry logic in the session helper",
    habits: { requireTests: false }
  });
  check(
    "BC8 rt=F suppresses both ac-tests and q-tests",
    !hasCrit(testsOff.contract, "ac-tests") && !ids(testsOff.contract).includes("q-tests")
  );

  const noReview = await extractHeuristicContract({ objective: OBJ, habits: { preferReview: false } });
  check("BC9 preferReview=F confidence 0.8, no user decision", noReview.confidence === 0.8 && !noReview.requiresUserDecision);
  check("BC10 preferReview absent confidence 0.86", anchor.confidence === 0.86);

  const multi = await extractHeuristicContract({
    objective: "fix src/auth/session.ts and src/auth/retry.ts then update docs/auth.md"
  });
  check("BC11 multi-target arm (3 d-file deliverables)", fileDeliverables(multi.contract) === 3);

  const qa = await extractHeuristicContract({ objective: "run qa on the login flow and fix the retry bug" });
  check(
    "BC12 qa corner: wantsTests true AND q-tests still pushed",
    hasCrit(qa.contract, "ac-tests") && ids(qa.contract).includes("q-tests")
  );

  const vagueTests = await extractHeuristicContract({ objective: "fix bug please", habits: { requireTests: true } });
  check(
    "BC13 vague + requireTests: q-done AND ac-tests together",
    ids(vagueTests.contract).includes("q-done") && hasCrit(vagueTests.contract, "ac-tests")
  );
}

/* ================================================================
 * NEW AXIS, part C: zero-production dormant branches — deterministic
 * semantic attestations only (no pricing; zero traffic => zero pool; no
 * candidate is minted for or against them).
 * ================================================================ */
{
  const mk = (checksArr: string[]): RequirementContract =>
    validateRequirementContract({
      schemaVersion: 1,
      objective: "conflict guard attestation objective",
      deliverables: [{ id: "d-1", description: "d", artifactKind: "diff", sourceRefs: [{ kind: "message", ref: "s" }] }],
      constraints: [],
      nonGoals: [],
      acceptanceCriteria: checksArr.map((c, i) => ({
        id: `ac-${i + 1}`,
        description: `criterion ${i + 1}`,
        observableCheck: c,
        sourceRefs: [{ kind: "message", ref: "s" }]
      })),
      assumptions: [],
      questions: [],
      authority: [],
      sourceRefs: [{ kind: "message", ref: "s" }]
    });

  // Z1: spec-first / latest-first winner arm (ordered.at(-1)) — zero
  // production traffic (track/loop.ts always passes "user-first").
  const conflicted = mk(["must be fast", "may be slow"]);
  for (const rule of ["spec-first", "latest-first"] as const) {
    const out = applyPrecedence(conflicted, rule);
    check(
      `Z1 ${rule} keeps the LAST conflicting criterion and supersedes the first`,
      out.acceptanceCriteria.length === 1 &&
        out.acceptanceCriteria[0]?.id === "ac-2" &&
        out.assumptions.some((a) => a.id === "a-superseded-ac-1")
    );
  }

  // Z2: pre-set-default question fast path in the gated re-map — the
  // heuristic extractor never sets question defaults, so this arm has zero
  // production traffic. A defaulted question is not blocking even when
  // unresolved.
  const defaulted = validateRequirementContract({
    schemaVersion: 1,
    objective: "defaulted question attestation objective",
    deliverables: [],
    constraints: [],
    nonGoals: [],
    acceptanceCriteria: [
      { id: "ac-1", description: "c", observableCheck: "x", sourceRefs: [{ kind: "message", ref: "s" }] }
    ],
    assumptions: [],
    questions: [{ id: "q-x", question: "pre-decided?", options: ["yes"], default: "yes" }],
    authority: [],
    sourceRefs: [{ kind: "message", ref: "s" }]
  });
  let z2Threw = false;
  try {
    assertCoverageAllowsStart(defaulted, [
      { id: "tsk_00000000" as TaskId, acceptanceCriteria: [{ id: "ac-1" }] }
    ]);
  } catch {
    z2Threw = true;
  }
  check("Z2 pre-set-default question is not blocking (gate passes unresolved)", !z2Threw);

  // Z3: empty-options "resolved" fallback (`question.options[0] ?? "resolved"`)
  // — schema-legal but never produced by the heuristic extractor.
  const emptyOptions = validateRequirementContract({
    schemaVersion: 1,
    objective: "empty options attestation objective",
    deliverables: [],
    constraints: [],
    nonGoals: [],
    acceptanceCriteria: [
      { id: "ac-1", description: "c", observableCheck: "x", sourceRefs: [{ kind: "message", ref: "s" }] }
    ],
    assumptions: [],
    questions: [{ id: "q-y", question: "no options?", options: [] }],
    authority: [],
    sourceRefs: [{ kind: "message", ref: "s" }]
  });
  const coveringTask: CoverageTaskRef[] = [{ id: "tsk_00000000" as TaskId, acceptanceCriteria: [{ id: "ac-1" }] }];
  let z3Resolved = true;
  try {
    assertCoverageAllowsStart(emptyOptions, coveringTask, { resolvedQuestionIds: ["q-y"] });
  } catch {
    z3Resolved = false;
  }
  check("Z3 empty-options question resolves via the 'resolved' fallback", z3Resolved);
  let z3Blocked = false;
  try {
    assertCoverageAllowsStart(emptyOptions, coveringTask);
  } catch {
    z3Blocked = true;
  }
  check("Z3b same question blocks fail-closed when unresolved", z3Blocked);

  // Z4: caller-provided sources arm of extractHeuristicContract — clarify
  // (the only production caller) never passes sources; attest the arm
  // works and threads the provided refs.
  const custom = await extractHeuristicContract({
    objective: OBJ,
    sources: [
      createTrustedSource({ kind: "message", ref: "custom-ref-1", origin: "user-turn", content: "user said so" })
    ]
  });
  check(
    "Z4 caller-provided sources arm threads the provided ref",
    custom.contract.sourceRefs.length === 1 && custom.contract.sourceRefs[0]?.ref === "custom-ref-1"
  );
}

/* ================================================================
 * Deterministic fail-closed / behavior sanity (fixed inputs; mirrors the
 * R14-H..R17-H check families so the eighteenth pass re-attests every
 * guard, including the three S5-H-1 semantic attestations the dispatch
 * requires).
 * ================================================================ */
{
  const prod = await extractHeuristicContract({ objective: OBJ });
  const real = prod.contract;
  check("default contract has C=2 criteria (ac-objective + ac-tests)", real.acceptanceCriteria.length === 2);
  check("default contract Q=0", real.questions.length === 0);
  check("default confidence 0.86", prod.confidence === 0.86);
  const vague = await extractHeuristicContract({ objective: "fix bug please" });
  check("vague confidence 0.55", vague.confidence === 0.55);
  check("vague emits questions", vague.contract.questions.length > 0);

  const tasks = tasksFor(real);
  let threw = false;
  try {
    assertCoverageAllowsStart(real, tasks);
  } catch {
    threw = true;
  }
  check("gate passes when fully covered", !threw);
  const matrix = coverageMatrixFromTasks(real, tasks);
  const gateResult = checkCoverageGate(real, matrix);
  check("raw gate result clean", gateResult.ok && gateResult.uncoveredCriteria.length === 0);

  // S5-H-1 semantics attestation: pre-allocation guard early-returns [] when
  // either side is empty; conflict side still detected.
  const mk = (checksArr: string[]): RequirementContract =>
    validateRequirementContract({
      schemaVersion: 1,
      objective: "conflict guard attestation objective",
      deliverables: [{ id: "d-1", description: "d", artifactKind: "diff", sourceRefs: [{ kind: "message", ref: "s" }] }],
      constraints: [],
      nonGoals: [],
      acceptanceCriteria: checksArr.map((c, i) => ({
        id: `ac-${i + 1}`,
        description: `criterion ${i + 1}`,
        observableCheck: c,
        sourceRefs: [{ kind: "message", ref: "s" }]
      })),
      assumptions: [],
      questions: [],
      authority: [],
      sourceRefs: [{ kind: "message", ref: "s" }]
    });
  check("S5-H-1: fast-only contract → no conflicts", detectConflicts(mk(["fast check", "other"])).length === 0);
  check("S5-H-1: slow-only contract → no conflicts", detectConflicts(mk(["slow check", "other"])).length === 0);
  const conflicted = mk(["must be fast", "may be slow"]);
  const conflicts = detectConflicts(conflicted);
  check("S5-H-1: fast+slow contract → one conflict", conflicts.length === 1 && conflicts[0]?.ids.length === 2);
  const resolved = applyPrecedence(conflicted, "user-first");
  check(
    "applyPrecedence user-first keeps first criterion and drops second",
    resolved.acceptanceCriteria.length === 1 &&
      resolved.acceptanceCriteria[0]?.id === "ac-1" &&
      resolved.assumptions.some((a) => a.id === "a-superseded-ac-2")
  );
  check("production contract has no conflicts", detectConflicts(real).length === 0);
  check("critique of production contract is clean", critiqueContract(real).score === 100);
  const empty = reconcileReviews([]);
  check("reconcile empty input → tie", empty.consensus === "tie" && empty.dissentCount === 0);
}

/* ================================================================
 * Cold tier on dist, default + configured (5 fresh processes each).
 * ================================================================ */
const mkColdScript = (configured: boolean): string => `
  const { performance } = await import("node:perf_hooks");
  const { extractHeuristicContract } = await import("/workspace/dist/requirement/heuristic.js");
  const { assertCoverageAllowsStart } = await import("/workspace/dist/requirement/coverage.js");
  const { applyPrecedence } = await import("/workspace/dist/requirement/precedence.js");
  const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
  const args = ${"{"} objective${"}"};
  ${configured ? 'const cfgArgs = { objective, habits: { requireTests: true, preferReview: false, askBeforeWrite: true } };' : ""}
  const t0 = performance.now();
  const prod = await extractHeuristicContract(${configured ? "cfgArgs" : "args"});
  const tasks = Array.from({ length: 5 }, (_, i) => ({
    id: "tsk_0000000" + i,
    acceptanceCriteria: prod.contract.acceptanceCriteria.map((c) => ({ id: c.id }))
  }));
  ${configured
    ? "assertCoverageAllowsStart(prod.contract, tasks, { resolvedQuestionIds: prod.contract.questions.map((q) => q.id) });"
    : "assertCoverageAllowsStart(prod.contract, tasks);"}
  applyPrecedence(prod.contract, "user-first");
  const t1 = performance.now();
  process.stdout.write((t1 - t0).toFixed(3));
`;
{
  for (const [label, configured] of [
    ["default", false],
    ["configured (habits + Q=1 gated)", true]
  ] as const) {
    const samples: number[] = [];
    for (let i = 0; i < 5; i += 1) {
      const proc = spawnSync(process.execPath, ["--input-type=module", "-e", mkColdScript(configured)], {
        encoding: "utf8"
      });
      if (proc.status !== 0) {
        check(`cold probe runs (${label})`, false, proc.stderr);
        break;
      }
      samples.push(Number(proc.stdout.trim()));
    }
    log(
      `COLD first-call production total (dist, fresh process, ${label}): median=${(median(samples) * 1e3).toFixed(0)}µs range=[${(Math.min(...samples) * 1e3).toFixed(0)}, ${(Math.max(...samples) * 1e3).toFixed(0)}]µs — once per PROCESS (veto class)`
    );
    check(`cold probes produced numbers (${label})`, samples.length === 5 && samples.every(Number.isFinite));
  }
}

if (failures > 0) {
  process.stderr.write(`\n${failures} of ${checks} check(s) FAILED\n`);
  process.exit(1);
}
log(`\nALL ${checks} SANITY/FAIL-CLOSED CHECKS PASSED (deterministic)`);
```

MORE_OPTIMA=no
BRANCH=cursor/r18-h-eval-eighteenth-pass-83a1
