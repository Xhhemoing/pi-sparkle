MODEL_SLUG=claude-fable-5-thinking-xhigh

# R17-H：`src/evaluation/` + `src/requirement/` + `src/review/` + `src/rubric/` 第十七遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 17 / R17-H
**基线:** `cursor/sota-persistent-opt-83a1` @ `a8625dd`（独占 tip，含 R17-A/B/C/D/E 空枚举收口与本次派单；R17-F/G 在飞，未等待、未触碰）
**分支:** `cursor/r17-h-eval-seventeenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十七遍枚举为空：无达门槛候选、零新 ID、切片零代码改动、切片维持关闭
（MORE_OPTIMA=no）。**
切片 21 个文件（1750 行 = 655+712+309+74，本轮 `wc -l` 逐目录复核一致；
8+7+4+2 清单逐目录确认恰 21 文件，与派单「evaluation 是 8 不是 9」吻合）
自 R1-H 基线（`fd437a9`）以来**逐字节未变**（`git diff fd437a9..HEAD --
src/evaluation src/requirement src/review src/rubric` 为 0 行、同范围
`git log` 为 **0 提交**，十七遍全程零 diff——指令要求的空 diff 检查
通过），且 R16-H 基线（`af3b087`）之后 `src/` **零提交**（`git log
af3b087..HEAD -- src/` 为空——其间全部 10 个提交 `--name-only` 逐一
确认仅触及 `docs/reports/sota-opt/`：R16-F/G/H/I/J 摄取、Round 16 收口
与 Round 17 全部派单/摄取；R17-A/B/C/D/E 全数空枚举无代码落地）⇒ 生产
调用方地图**可证不变**（最强形式），本轮全库 import 交叉检索再次独立
确认（9 个切片外导入位点与 R3-H…R16-H 完全一致：7 个值级 =
`assertCoverageAllowsStart`×3 / `extractHeuristicContract` /
`applyPrecedence` / `shouldScout` / `assertCanPromoteFromReview`，2 个
纯类型 = promotion.ts ReviewerKind / primary-split.ts HeuristicHabits；
`src/evaluation/` 全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、
`src/rubric/` 仍零生产调用方）。按指令在本 VM 重锚满格矩阵（§1.3–§1.5，
三次独立运行，Node v22.22.2）：热层默认态 **9.17–10.18µs**/run（与派单
点名的 R16-H 热默认锚 9.35–9.78µs 交叠同带——三次 10183/9399/9165ns
中两次落带内/带缘、一次 10.18µs 居 R13-H 本带 9.3–10.4µs 内，属十七轮
历史跨 VM 带 3.9–10.4µs 上段；离散度 <10%）、配置态 A **7.4–8.4µs**
（三次全低于同轮默认态）、配置态 B **4.0–4.3µs**（三次全低于默认态）
——**无配置态悬崖**、默认锚点仍是保守口径；晋升类 17.6–19.4ns；四个
零生产调用方命令类（evaluate 1.12–1.16µs / critique 0.64–0.66µs /
pairwise 2.03–2.06µs / reconcile 65–77ns）**全部 ≤2.1µs**；冷层首调
默认 1675–1750µs、配置 1611–1642µs once-per-process（与 R6-H…R16-H
同带，否决类归属不变）。第十七遍在完整排除表（S1-H-1..9 … S9-H-1..2
及 R10-H…R16-H 七轮空枚举收口共 60+ 处不立 ID 记录）之上以一个从未
用过的完备性轴收口：**fail-closed 拒绝路径普查（rejection-path
census）**（§2.1–§2.2，镜像 R17-B 拒绝路径定价 / R17-D 28 条终点
fail-closed 普查 / R17-E 拒绝定价表在本切片的落地）——前十六轮的
十七个完备性面全部记账**成功路径**（热默认链永不抛错；R12-H 剖面与
R14-H 引擎底都是成功流账目）；R14-H 曾点名门 throw 路径消息装配但
**未定价**，切片其余 throw 端点与三个 adapter 的非抛出拒绝返回从未被
系统性枚举。本轮补上：**33 条切片内拒绝终点全枚举 + 逐条语义
attestation + 逐条定价**——(a) **17 条 throw 端点**（fail-closed
中止：extractor 角色/置信度/authority 六条、coverage 门三条、
normalizer 来源两条、evaluator 输入两条、self-review 双站点四条），
单次拒绝全价 **2.5–12.1µs**（最贵 = T5 inference-confidence 经完整
buildContractCandidate 链拒绝 11.5–12.1µs；同步 throw 地板 ~2.5µs =
DomainValidationError 构造 + 栈捕获的引擎级成本，T12/T13 函数头守卫
即该地板；端点价格序与抛点在链中的位置完全一致，机制自洽）——每条
生产成功流**零次执行**、至多每被拒命令一次（fail-closed 中止后命令
终止），全部低于数十 ms 落地线 **3–4 个数量级**；(b) **16 条非抛出
拒绝返回**（CheckAdapter 5 / DeliveryAdapter 6 / DiffAdapter 4 /
review critic actor-defense ABSTAIN 1），**115–941ns**/call，全部在
R9-H…R16-H 命令类天花板辖区之内（零生产调用方）。该轴不重开 R14-H
throw 路径拒列（本轮**未优化任何错误路径，只定价**；「省栈捕获/懒
装配消息」类候选在定价数字下直接被频次类 + X1-1 家族封死，不铸
ID）。orphans 消息段经取证确认经由 `assertCoverageAllowsStart`
**不可达**（`coverageMatrixFromTasks` 恒返回空 orphanRequirements），
其语义以直接 `checkCoverageGate` 调用 attestation 收口。结构位置 ×
消除机制扫描其余全部微观位点逐一对表均为换名重提或已关家族站点换位
（§0 审计；本轮**零从未点名结构位点**——收敛序列 R13-H 0 → R14-H 1 →
R15-H 0 → R16-H 1 → 本轮 0；普查轴定价的是 R14-H 已点名未定价面，
非新结构位点）。诚实结论：枚举为空，不硬凑 ID。未重开任何 X* /
S1-* … S16-* 条目；S5-H-1 守卫经本轮 diff + 读码双复核**原样保留、
逐字节未变**，三项语义 attestation ×3 运行全过；S8-H-3 全决议快路径
**未做任何形态**；S8-H-1 PairwiseInput 未收窄；R16-H PATH_RE 回溯
拒列**未重开**（重开条件 (a)/(b) 均未触发：`src/` 零提交 ⇒ objective
无新程序化/对抗来源，无 ≥3.2K 字符无斜杠 token 段真实输入类出现）；
R16-H 载荷基底 × 形态格、R15-H A/A 决斗地板、R14-H 引擎底轴未重测
未复铸。`pnpm gate` 全绿。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/evaluation/`（8 文件：adapters / check-adapter /
  delivery-adapter / diff-adapter / evaluator / ownership / precedence /
  types）、`src/requirement/`（7 文件：coverage / critic / extractor /
  heuristic / normalizer / precedence / provenance）、`src/review/`
  （4 文件：critic / pairwise / reconcile / self-review）、`src/rubric/`
  （2 文件：registry / types）计 21 文件全量第十七遍实际读码，未依赖
  前十六轮记忆。上下游 `domain/contract.ts`（AuthorityGrant 形状取证，
  供 §2.1 T6 端点构造）、`domain/errors.ts`、
  `track/{clarify,loop,plan,primary-split}.ts`、
  `run/{supervisor,coordinator,flowchart-run}.ts`、
  `adaptation/{promotion,promotion-rules}.ts` 只读取证，一行未改。
  切片外其余树零触碰。在飞 R17-F / R17-G 分支与文件未触碰、未等待。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表 546 行，含
  每一条 S*-H-* 与 S5-H-1、S13-B-1）/ PROGRESS.md / round-17/PLAN.md /
  round-16/R16-H.md（空枚举；PATH_RE 候选化后拒列 + 重开条件；基底 ×
  形态格）/ round-15/R15-H.md（空枚举；A/A 决斗地板）/
  round-14/R14-H.md（空枚举；引擎底轴 + throw 路径点名不定价）/
  R10-H…R13-H 收口段逐一复核（空枚举确认，未重铸）。EXCLUSIONS.md /
  PROGRESS.md / 任何 PLAN.md 未编辑。
- **换名重提检查**（第十七遍读码发现的全部微观位点逐条对表；全部在
  候选化之前拒列，零代码触碰。本轮普查轴定价的 33 条终点均为**已
  点名或已属既有辖区**的面——不是新结构位点，不铸 ID）：
  - **S5-H-1（必须保留项）**：`requirement/precedence.ts` L24 的
    `if (fast.length === 0 || slow.length === 0) return [];` 分配前
    守卫经本轮读码 + `git diff fd437a9..HEAD --
    src/requirement/precedence.ts`（0 行）双复核**原样在位、逐字节
    未变**。本轮对该函数零候选、零「优化掉」提案；§2.3 仿真含三项
    确定性语义 attestation（fast-only → [] / slow-only → [] /
    fast+slow → 单冲突全 id），三次运行全过（122 项检查 ×3 全绿）。
  - **S8-H-3（永不重开项）**：`assertCoverageAllowsStart` 未做任何
    全决议快路径。`coverage.ts` 零 diff。§2.1 门端点定价只跑生产
    函数原样执行（喂被拒输入），未引入任何变体。
  - **S8-H-1（永不重开项）**：`PairwiseInput` 全形状（含 comment
    双字段）未收窄；PIC 形状污染家族未重演。
  - **R16-H 不重开项**：PATH_RE 分支 1 二次回溯拒列维持——重开条件
    (a) objective 程序化/对抗来源、(b) ≥~3.2K 字符无斜杠 `[\w.-]` 段
    真实载荷，经本轮取证均未触发（`git log af3b087..HEAD -- src/` 为
    空 ⇒ `cli/main.ts` objective 到达通道未变）；载荷基底 × 形态格
    不补铸、不重测。**本轮形态/长度轴零探针**——普查轴与 R16-H 轴
    正交（错误路径 vs 载荷形态）。
  - **R14-H 已点名面的处置**：coverage.ts L92–98 门 throw 路径错误
    消息装配 = R14-H 唯一点名 + 三重拒列（fail-closed 频次类 + 消息
    格式被单元/集成测试双重断言 X0-5 族 + 融合家族）。本轮**不重提
    该拒列、不做任何优化形态**，仅按普查轴给其首次实测价（T7–T9 =
    3.5–3.9µs/被拒启动，§2.1）——数字证实 R14-H 拒列的量级判断
    （距落地线 3+ 个数量级）。
  - **普查轴定价数字不产生新候选**：throw 端点的 ~2.5µs 同步地板由
    DomainValidationError 构造 + 栈捕获支配（T12/T13 函数头守卫即
    地板值）——「调 Error.stackTraceLimit / 缓存错误对象 / 懒装配
    消息」类想法分别撞 X1-1（模块级隐藏状态）、错误对象身份/栈新鲜
    契约与 R14-H 拒列本体，且全部端点稳态流量为零 ⇒ 收益恒 0，在
    候选化之前作为一类拒列，不铸 ID。
  - `heuristic.ts`：L46/L194 objective 双 trim = R6-H no-ID
    （S1-H-3 家族）；L48/L64 双正则子集短路 = S3-H-3；L85
    namedTargets 与 shouldAskScope 内二次调用 = S1-H-3（生产形态下
    shouldAskScope 首行 `shouldScout` 短路使二次调用不可达，本轮读码
    再确认）；SMALLEST_CHANGE 每调用 spread（L105）= R2-H/R8-H
    拒列；vague 分支问题字面量提升 = S6-H-4 / S7-B-5 可观察身份家族；
    isVague split+filter 词计数 = R2-H/R5-H/R7-H/R8-H no-ID；
    q-scope/q-write `questions.some` 守卫 = R4-H no-ID；正则字面量
    提升 = S1-B-1 / S3-E-5 家族 + R1-H no-ID；每调用 extractor/critic
    角色对（L187–188）= S6-H-4；异步机器 = S7-H-1 + X0-4；
    `[...critique.omissions]` = S4-H-3；`[...sources]` 防御 spread
    （L186）= R2-H no-ID；`habits ?? {}` = R10-H no-ID；vague 路死
    wantsReview = R12-H P3；PATH_RE 回溯形态 = R16-H 拒列维持。
  - `coverage.ts`：L22 orphans 防御 spread = R12-H P1；L27
    `Object.keys(...).includes` 每准则 = S1-H-1 / S4-H-1 辖区
    （Face C 二次方 R13-H 已标定、越线点 C≈482–503 先被重开条件
    包住）；L52 Set-from-map = R12-H P2；L61 owners 取-改-存回写 =
    R1-H 下界表覆盖；L83–89 gated 合同拷贝 = S1-H-2 / S6-H-3；
    taskToChecks 死计算 = S6-H-2；L92–98 throw 消息装配 = R14-H
    拒列维持（本轮仅定价，见上）。
  - `requirement/precedence.ts`（守卫外）：双 filter 序贯早退 =
    S6-H-1；`conflict.ids.includes` 与双 filter Set 化 = S1-H-5；
    `[...fast, ...slow]` 中间拼接 = 冲突侧零生产流量 + S1-H-5 家族
    （本轮 `detectConflicts(real).length === 0` 三次再证）。
  - `extractor.ts`：L68 roleId 双 trim = R10-H no-ID；L75
    `[...input.sources]` 二次 spread = R2-H no-ID；L83 sourceRefs
    Set = S2-H-1；L122 空 authority 下 sourcesByRef Map 死构造 =
    S2-H-2 本体；L80 生产链双 validate = S3-H-1；grounding.find 换
    Map = S1-H-4；validateConfidence 常量校验 = R15-H 查表排除维持
    （本轮 T3/T4/T5 给其三条拒绝腿定价，语义面从未变）；三扩展点
    异步机器 = S7-H-1 + X0-4。
  - `requirement/critic.ts` / `provenance.ts`：四遍装配 = S2-H-3 /
    S3-H-2；critique 与 detectConflicts 的 toLowerCase 双算 =
    S4-H-2；findUnsourcedItems L30 Set-from-map = R12-H P2 镜像
    站点；isSourced `Array.isArray` 数据面守卫 = R4-H no-ID。
  - `normalizer.ts`：excerpt slice(0,200) = R5-H no-ID + R16-H
    SLICED 基底胞正面收口维持；extractSignals 三正则 = R1-H no-ID；
    signals 生产组合死输出 = S7-H-2；默认 origin 守卫 = S2-H-7；
    buildContractFromSources = R11-H L3；assertOriginMatchesKind
    两条 throw = 本轮 T10/T11 定价（plain Error 面，语义未变）。
  - `evaluation/evaluator.ts`：`input.evidence[criterion.id]` 双读 =
    R5-H no-ID；聚合三遍 = S1-H-7；INDEPENDENCE_CLASSES includes =
    R1-H 表族；`String()` 分配 = R10-H no-ID；两条输入守卫 throw =
    本轮 T12/T13 定价；canEvaluatorScoreCriterion /
    validateEvaluatorScope = test-only。
  - `evaluation/check-adapter.ts`：changeSetsEqual 三面 = S1-H-9 /
    S2-H-4 / S3-H-4；hashArtifact 拼接 = S5-H-3；attributionMetadata
    活性本轮读码再确认；五条拒绝返回 = 本轮 A1–A5 定价（命令类
    天花板辖区）。`delivery-adapter.ts` / `diff-adapter.ts`：输入
    校验合取序 = R10-H no-ID；FAIL 路径死 `hasEpisodeOwned` =
    R11-H L1；拒绝返回 = A6–A15 定价。`ownership.ts`：
    `context.changeSet.includes` = X4-9；控制流去糖 = R10-H no-ID。
    `evaluation/precedence.ts`：reduce 内 comparePrecedence 双调用 =
    R2-H no-ID + S2-H-5；三元素表 find = R1-H 表族。
  - `review/pairwise.ts`：remap "ab" 分支冗余展开 = R1-H no-ID；
    presentInput "ba" 对象构造 = 命令类天花板辖区（≤2.1µs）。
    `review/reconcile.ts`：三遍 filter = S1-H-6；L39 Set-from-map =
    R12-H P2 第四站点维持。`review/critic.ts`：hasPass 融合 =
    R2-H no-ID；score 条件键单态化 = R9-H 拒列；actor-defense
    ABSTAIN = 本轮 A16 定价。`review/self-review.ts`：S9-H-1 /
    S9-H-2 维持；四条 fail-closed throw = 本轮 T14–T17 定价。
  - `rubric/registry.ts`：copy-on-write = S1-H-8 反例 + S2-H-6；
    `{ ...DEFAULT_REGISTRY }` 浅拷贝别名安全性第九次读码复核成立。
    `rubric/types.ts`：createRubric 时间戳 = R10-H 取证维持。
  - **S13-B-1 / S12-* / S11-*（其他切片）**：均不在本切片，未触碰、
    未换名重提。R16-H 基底 × 形态格、R15-H A/A 地板、R14-H 引擎底轴
    不重测不补铸（派单明令）。
  - R10-H…R16-H 未铸 S10-H-* … S16-H-*，本轮亦未事后发明任何此类
    编号，不为空枚举轮补铸 ID。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价（R1 未接线）、
  稀疏 → 已批准 R0 基线、双 LCB（beta+normal）与双归因
  （logit+prob-add）保留（X0-11）、晋升 proposal-first、阈值/CAS/
  凭据/数据面契约/公开签名不变、DEFAULT_REGISTRY 未就地变异、
  fail-open 零引入、测试未改，天然成立。不声称 Outcome-supported，
  Checkpoint F-PROD 仍开放（ADR-005 / X0-1）。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，且本 VM
  `/exec-daemon/node`（v22.14.0）在 PATH 前段遮蔽 nvm bin（与
  R11-H…R16-H §0 注记同型）；测量与测试前已显式把 nvm v22.22.2 bin
  置于 PATH 首并复核 `node --version` = v22.22.2。
  `pnpm install --frozen-lockfile`（pnpm 10.17.1）后 `pnpm build` 绿。
- 未开 PR；未 force-push；未触碰专属分支；仅提交并推送工作分支
  `cursor/r17-h-eval-seventeenth-pass-83a1`。无赢家 ⇒ 未提交任何
  仿真脚本（按指令），锚点/普查测量脚本全文在附录。

## 1. 基线不变性、调用图复核与满格矩阵重锚

1. **切片逐字节未变**：`git diff fd437a9..HEAD -- src/evaluation
   src/requirement src/review src/rubric` 输出为 0 行，同范围
   `git log` 为 0 提交（十七遍全程零 diff——指令要求的空 diff 检查
   通过）。21 文件 1750 行逐目录计数（655/712/309/74）与 R1-H 清单
   一致。R1-H 逐函数下界表 … R14-H 引擎底、R15-H A/A 地板、R16-H
   基底 × 形态格与全部 S*-H-* 排除继承有效。
2. **调用图可证不变（最强形式）**：`git log af3b087..HEAD -- src/`
   **为空**——R16-H 基线以来全部 10 个提交 `--name-only` 逐一确认
   仅触及 `docs/reports/sota-opt/`（R16-F/G/H/I/J 摄取、Round 16
   收口、Round 17 派单/摄取；R17-A/B/C/D/E 全数空枚举无代码落地）⇒
   生产调用方地图数学上不变。本轮全库 import 交叉检索独立双确认：
   切片外 `src/` 引用切片的文件恰 9 个，与 R3-H…R16-H 完全一致——
   值级 7 处：`assertCoverageAllowsStart` ← `run/supervisor.ts:27` /
   `run/coordinator.ts:31` / `run/flowchart-run.ts:21`（每 run 启动
   一次）；`extractHeuristicContract` ← `track/clarify.ts:4`（每 run
   一次）；`applyPrecedence` ← `track/loop.ts:38`（每 run 一次，
   `"user-first"`）；`shouldScout` ← `track/plan.ts:5`（每 run
   一次）；`assertCanPromoteFromReview` ←
   `adaptation/promotion-rules.ts:4`（每晋升一次）；纯类型 2 处
   （`adaptation/promotion.ts:10` ReviewerKind、
   `track/primary-split.ts:6` HeuristicHabits）。`src/evaluation/`
   全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、
   `src/rubric/` 的切片外 `src/` 值级引用**为零**——未把假想接线
   当今日流量。
3. **热层三态锚点重锚**（三次独立运行，本 VM，Node 22.22.2；测量
   条件与 R10-H…R16-H 严格同构——benchAsync 单次预热 + 3000 计时，
   普查轴放在锚点之后以免额外预热污染可比性）：

```text
CEILING default  (9-pass anchor shape, Q=0):        10183 / 9399 / 9165 ns/run
CEILING configured A (habits 三键全设, Q=1 gated):    7407 / 8408 / 8329 ns/run
CEILING configured B (vague objective, Q=3 gated):    3967 / 4099 / 4333 ns/run
CEILING promotion class: assertCanPromoteFromReview = 19.4 / 17.6 / 18.4 ns/promotion
```

   默认态 9.17–10.18µs 与派单点名的 R16-H 热默认锚 **9.35–9.78µs**
   交叠同带（两次落带内/带缘，一次 10.18µs 居 R13-H 本带 9.3–10.4µs
   之内），居十七轮历史跨 VM 带（3.9–10.4µs）上段。两档配置态三次
   **全部低于**同轮默认态 ⇒ R8-H…R16-H「无配置态悬崖、默认锚点是
   保守上界」在本 VM 复现。量级类结论不变：整个热面 µs 级，低于落地
   线 **3–4 个数量级**（按本轮锚点，链须每 run 执行 ~982–1091 次才
   触及 10ms 保守地板，而调用图恰执行一次）。gated 重映射真实执行
   （production assume-defaults 流恒传覆盖全部问题的
   `resolvedQuestionIds`）。
4. **命令类矩阵剩余格重锚**（R9-H 首锚的四个零生产调用方类，本轮
   三次复测）：

```text
CEILING evaluate class  (TEST-ONLY): createEvaluationRecord(3 criteria) = 1116 / 1164 / 1130 ns/call
CEILING critique class  (chain 内):  critiqueContract(real contract)    =  640 /  664 /  645 ns/call
CEILING pairwise class  (TEST-ONLY): runBlindPairwisePair               = 2032 / 2058 / 2064 ns/call
CEILING reconcile class (TEST-ONLY): reconcileReviews(n=2)              =   65 /   66 /   77 ns/call
```

   与 R9-H…R16-H 锚点带一致。判据成立：**调用图未变下即便把这四个
   面全部接线，当前规模每类 ≤2.1µs，距落地线仍差 4+ 个数量级**。
   矩阵在本 VM 无未测格。
5. **冷层锚点复测（默认 + 配置双态）**：dist 产物、新进程、模块
   加载预扣除后，三生产入口首调合计中位数三次：默认态
   **1750 / 1681 / 1675 µs**（区间 [1657, 1836]µs）、配置态
   **1633 / 1642 / 1611 µs**（区间 [1597, 1700]µs）——与 R6-H
   1.65–1.71ms … R16-H 1.6–1.8ms 同带；once-per-process 否决类
   归属不变（S7-H-3 已证该层无净正对策）。

## 2. 第十七遍枚举：fail-closed 拒绝路径普查（新轴）→ 诚实空结果

### 2.1 throw 端点普查（17 条，fail-closed 中止类）

前十六轮的十七个完备性面全部记账成功路径；R14-H 点名门 throw 消息
装配但按拒列原则未做探针，其余 throw 端点从未被枚举。本轮按 R17-D
28 条终点普查的方法在本切片全枚举：**17 条 throw 端点**，每条经
(1) 错误类断言（DomainValidationError vs plain Error）、(2) 消息
正则 attestation、(3) 全价定价（被拒调用的整调用成本——与 R17-D
「最贵拒绝」同口径）。三次运行读数（ns/rejected-call）：

```text
T1  extractor 空 roleId（buildContractCandidate 头部）:      5091 / 5208 / 5058
T2  extractor=critic 同 roleId:                              5263 / 4854 / 5001
T3  extraction confidence 越界（经完整 extract 后拒）:        8097 / 8066 / 8020
T4  minimumConfidence 参数越界:                              8237 / 8148 / 8394
T5  inference confidence 非法（链最深处拒）:                 12076 / 11478 / 12002  ← 最贵
T6  authority grant 无可信 grounding:                        9576 / 9858 / 9921
T7  门阻启动（uncovered 消息腿，R14-H 点名位首次定价）:       3838 / 3749 / 3831
T8  门阻启动（blocking 消息腿）:                             3543 / 3457 / 3583
T9  门阻启动（uncovered+blocking 组合最长可达消息）:          3933 / 3816 / 3934
T10 normalizer user-turn origin × file kind（plain Error）:  2990 / 2886 / 2959
T11 normalizer approved-spec origin × message kind:          3196 / 3096 / 3198
T12 createEvaluationRecord 空 artifactId（函数头守卫）:       2594 / 2508 / 2525
T13 createEvaluationRecord 未知 independence class:          2636 / 2767 / 2631
T14 applyRoutingScoreUpdate self-by-kind:                    3518 / 3509 / 3464
T15 applyRoutingScoreUpdate self-by-identity:                3543 / 3502 / 3477
T16 assertCanPromoteFromReview self-by-kind:                 2871 / 2783 / 2807
T17 assertCanPromoteFromReview self-by-identity:             2834 / 2825 / 2814
```

收口事实：

1. **最贵拒绝 11.5–12.1µs（T5）**，= 完整 buildContractCandidate 链
   跑到 inference 校验处 + 错误构造；**同步 throw 地板 ~2.5µs**
   （T12/T13 函数头守卫，几乎零前置工作）= DomainValidationError
   构造 + 栈捕获 + unwind 的引擎级成本。端点价格随抛点在链中的位置
   单调（T1/T2 头部 ~5µs < T3/T4 extract 后 ~8µs < T6 grounding 后
   ~9.6–9.9µs < T5 critique+inference 后 ~12µs），机制自洽——普查
   读数可信。
2. **频次类裁决**：全部 17 条是 fail-closed 中止——非法 extractor
   配置、self-review 企图、门阻启动都使命令终止；生产成功流零次
   执行、至多每被拒命令一次。即便最贵端点也距 10ms 保守落地地板
   **~830×**、距数十 ms 落地线 3–4 个数量级 ⇒ 按验收线属
   once-per-run 否决类，**该面结构上不存在达门槛候选**。
3. **不产生新候选**：地板由错误对象构造支配（切片外引擎面）；「省
   栈捕获/缓存错误对象/懒装配消息」分别撞 X1-1、错误新鲜性契约与
   R14-H 拒列本体，且收益池恒零（零稳态流量）——候选化之前作为
   一类拒列，不铸 ID。R14-H 对门消息装配的三重拒列由 T7–T9 数字
   **事后证实**（3.5–3.9µs/被拒启动，其中成功腿门成本 ~0.7µs，
   消息+错误增量 ~2.8–3.2µs——全在引擎错误地板量级）。
4. **orphans 腿取证**：`coverageMatrixFromTasks` 恒返回
   `orphanRequirements: []` ⇒ throw 消息的 `orphans=` 段经
   `assertCoverageAllowsStart` **不可达**（仓内无第二个 matrix 生产
   者，本轮 rg 再证）；其语义以直接 `checkCoverageGate` 合成 matrix
   attestation 收口（拒绝正确、orphans 原样透传）。该死段不构成
   候选：删除撞公开 CoverageGateResult 形状 + 防御纵深（S2-H-7 同
   哲学），量级为消息装配子项的子项。

### 2.2 非抛出拒绝返回普查（16 条，零调用方命令类辖区）

三个 adapter 与 review critic 的拒绝形态（ABSTAIN / FAIL /
UNOBSERVED）从未被逐条定价——它们零生产调用方（R9-H…R16-H 命令类
天花板辖区），本轮一并收口。三次运行读数（ns/call，每条含 outcome +
reason 正则 attestation）：

```text
A1  check ABSTAIN 非法输入:        206 / 153 / 188      A9  delivery FAIL reopen:        185 / 149 / 158
A2  check FAIL cwd 不匹配:         399 / 384 / 405      A10 delivery FAIL 人工拒绝:      167 / 168 / 180
A3  check FAIL 陈旧 revision:      343 / 332 / 344      A11 delivery UNOBSERVED 未记录:  156 / 167 / 174
A4  check FAIL 陈旧 changeSet:     546 / 430 / 460      A12 diff ABSTAIN 非法输入:       138 / 132 / 144
A5  check FAIL exit!=0:            486 / 470 / 468      A13 diff FAIL unrelated:         270 / 257 / 262
A6  delivery UNOBSERVED null:      122 / 115 / 128      A14 diff FAIL unknown 所有权:    255 / 251 / 273
A7  delivery ABSTAIN 非法输入:     120 / 118 / 130      A15 diff UNOBSERVED 空:          236 / 232 / 287
A8  delivery FAIL rollback:        140 / 145 / 152      A16 critic ABSTAIN actor 防御:   941 / 912 / 919
```

全部 **115–941ns**、最贵 A16（含 createEventId + nowIso）。裁决：
零生产调用方 + 即便全部接线也在命令类 ≤2.1µs 天花板之内 + 距落地线
4+ 个数量级 ⇒ 无候选，不铸 ID。changeSet 陈旧腿（A4，最贵 check
拒绝）的双 Set 构造 = S1-H-9 / S2-H-4 / S3-H-4 已判三面，读数证实
其量级判断。

### 2.3 结构位置 × 消除机制扫描 + 确定性 attestation

第十七遍全格对账（结构位置：入口守卫 / 循环体 / 返回构造 / 跨调用
重复 / 数据字面量 / 正则求值 / 防御校验 / 拷贝语义 / 异步机器 /
模块加载 / **错误路径（本轮定价收口）** / 载荷基底 / 载荷形态 ×
消除机制：死码 / CSE / 融合 / 快路径 / 记忆化 / 表示变换 / 去糖 /
惰性 / 可变化 / 复杂度类）。「从未点名结构位点」收敛序列：R13-H
0 处 → R14-H 1 处 → R15-H 0 处 → R16-H 1 处 → **本轮 0 处**——
第十七遍读码发现的每一个微观位点都在 §0 的换名审计中对到既有 ID /
no-ID 收口记录；普查轴的 33 条终点全部属已点名或既有辖区面（R14-H
门 throw / R15-H 查表排除的 validateConfidence 拒绝腿 / 命令类天花板
辖区的 adapter 拒绝面），定价不等于铸 ID。122 项确定性
fail-closed/sanity 检查 ×3 运行全过（含派单要求的三项 S5-H-1 语义
attestation、17 条 throw 端点错误类+消息断言、16 条拒绝返回
outcome+reason 断言、orphans 直接调用 attestation、盲评换位/门
fail-closed 断言）。1750 行 × 17 遍后，切片可优化面（导出 + 私有 +
剖面 + 规模 + 引擎 + 协议分辨率 + 基底 + 形态 + **错误路径**）九向
闭合。**不立任何 S17-H-* ID**（与 R10-H…R16-H 及 R17-A/B/C/D/E 空
枚举收口同型）。

### 2.4 收口陈述

十七遍合计：单函数、函数间、跨模块、调用图、模块图、执行层级、
调用机器、配置态、命令类、编译器地板、导出面完备性、私有面完备性、
成本剖面归账、规模-越线标定、引擎态基底、A/A 决斗噪声地板、载荷
基底 × 形态、**fail-closed 拒绝路径普查**十八个面全部有实测锚点或
对账记录。本切片在当前调用图下**结构上不存在**达门槛候选。总门槛按
本 VM 数字刷新：任何未来候选须先推翻满格矩阵实测上界——热层默认
**9.17–10.18µs/run**（3000-rep 保守口径）、四个零调用方命令类
≤2.1µs、冷层 **1.6–1.8ms once-per-process**（否决类）、**最贵
fail-closed 拒绝 11.5–12.1µs/被拒命令（稳态流量零）、同步 throw
引擎地板 ~2.5µs、非抛出拒绝 ≤941ns**。重开该切片的唯一前提维持
R4-H…R16-H 收口原文：调用图变更（evaluation/review/rubric 面接入
每 turn 热路径，或合同规模增长 ≥2 个量级——R13-H 已标定），或
objective 输入面出现对抗/程序化来源或 ≥3.2K 字符无斜杠 token 段的
真实输入类（R16-H 已标定）。本轮新增对账注记：**错误路径面不构成
独立重开前提**——即使拒绝频次增长若干数量级（意味着上游大规模送
非法输入），修复点在上游输入面而非本切片错误装配。

## 3. 逐文件收口（第十七遍新视角，其余与 R1-H…R16-H 一致）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `requirement/extractor.ts` | **六条 throw 端点定价（T1–T6：5.0–12.1µs/被拒调用，链位置单调自洽，§2.1）**；validateConfidence 三腿 = R15-H 查表排除维持；S1-H-4、S2-H-1/2、S3-H-1、S7-H-1 维持 | 无达门槛候选 |
| `requirement/coverage.ts` | **门 throw 三腿首次定价（T7–T9 = 3.5–3.9µs/被拒启动，R14-H 三重拒列获数字证实）**；orphans 腿经 assertCoverageAllowsStart 不可达取证 + 直接调用 attestation；S8-H-3 零形态、S1-H-1/2、S4-H-1、S6-H-2/3、R12-H P1/P2、R13-H Face C 标定维持 | 无新候选 |
| `requirement/normalizer.ts` | **两条 origin-kind throw 定价（T10/T11 = 2.9–3.2µs，plain Error 面）**；S2-H-7、S7-H-2、R5-H slice、R11-H L3、R16-H SLICED 收口维持 | 无新候选 |
| `requirement/heuristic.ts` | 三态锚点重锚同带（§1.3）；PATH_RE 回溯拒列**未重开**（重开条件未触发取证）；S1-H-3、S3-H-3、S6-H-4、S7-H-1、S8-H-2、R12-H P3 维持 | 无新候选 |
| `requirement/precedence.ts` | **S5-H-1 守卫在位复核（L24 早退，零 diff + 三项语义 attestation ×3 运行全过）**；applyPrecedence 腿 114–253ns 同带；S1-H-5、S4-H-2、S6-H-1 维持 | 无新候选 |
| `requirement/critic.ts` / `provenance.ts` | critique 锚点 640–664ns 带内；S2-H-3、S3-H-2、S4-H-2、R12-H P2 站点维持 | 无新候选 |
| `evaluation/evaluator.ts` | **两条输入守卫 throw 定价（T12/T13 = 2.5–2.8µs = 同步 throw 引擎地板）**；evaluate 类锚点复测同带（1116–1164ns）；S1-H-7、R5-H evidence 双读、R1-H 表族、R10-H String() 维持 | 无新候选 |
| `evaluation/check-adapter.ts` / `delivery-adapter.ts` / `diff-adapter.ts` | **15 条非抛出拒绝返回定价（A1–A15 = 115–546ns，§2.2）**；A4 陈旧 changeSet 腿证实 S1-H-9/S2-H-4/S3-H-4 量级判断；attributionMetadata 活性、R10-H 合取序、R11-H L1 维持 | 无新候选 |
| `evaluation/ownership.ts` / `precedence.ts` / `adapters.ts` / `types.ts` | X4-9、R10-H 去糖、R2-H/S2-H-5、纯类型面维持 | 无新候选 |
| `review/self-review.ts` | **四条 fail-closed throw 定价（T14–T17 = 2.8–3.5µs，双站点 × 双触发全覆盖）**；S9-H-1 / S9-H-2 维持 | 无新候选 |
| `review/pairwise.ts` / `reconcile.ts` / `critic.ts` | pairwise/reconcile 锚点复测同带（2032–2064 / 65–77ns）；**critic actor-defense ABSTAIN 定价（A16 = 912–941ns）**；reconcile L39 = R12-H P2 第四站点维持；S8-H-1、S1-H-6 维持 | 无新候选 |
| `rubric/registry.ts` / `types.ts` | S1-H-8 反例 + S2-H-6 + R10-H createRubric 时间戳取证维持；DEFAULT_REGISTRY 别名安全第九次复核成立 | 无新候选 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。无仿真脚本入库（无赢家，
遵循 round01–16 仅赢家 sim 入库惯例 + 本轮指令明文；完整脚本见
附录）。

## 5. 测试与门

`pnpm gate` 全绿（Node 22.22.2 via nvm，满足 engines >=22.19.0；
系统 Node 22.14.0 过低 + `/exec-daemon/node` PATH 遮蔽的环境注记见
§0；`pnpm install --frozen-lockfile` 后）：

```bash
pnpm gate   # typecheck ✓ + lint ✓ + test: tests 1169 / pass 1168 / fail 0 / skipped 1 ✓ + build ✓
```

切片相关套件基线复核，全绿：

```bash
npx tsx --test "test/unit/requirement/*.test.ts" "test/unit/evaluation/*.test.ts" \
  "test/unit/review/*.test.ts" "test/integration/m3/checkpoint-d.test.ts" \
  "test/integration/m3/coverage-gate.test.ts" \
  "test/integration/m3/requirement-extraction.test.ts" \
  "test/integration/m4/delivery-evidence.test.ts"
# tests 93 / suites 13 / pass 93 / fail 0（与 R1-H…R16-H 同套件同计数）
```

锚点 + 拒绝路径普查测量（临时脚本 `/tmp/r17h-sim.mts`，无赢家故未
入库；完整源码见附录；`npx tsx` 运行，冷层探针需要 dist 产物）三次
独立运行，122 项确定性 fail-closed/sanity 检查全部通过、裁决结论
逐位一致（默认锚点三次 10183/9399/9165ns、配置 A 7407/8408/8329ns、
配置 B 3967/4099/4333ns；晋升类 19.4/17.6/18.4ns；evaluate
1116/1164/1130ns、critique 640/664/645ns、pairwise 2032/2058/2064ns、
reconcile 65/66/77ns；普查最贵 throw T5 12076/11478/12002ns、同步
地板 T12 2594/2508/2525ns、最贵非抛出 A16 941/912/919ns；冷层默认
1750/1681/1675µs、配置 1633/1642/1611µs）。

## 6. 新增排除 ID

**无。** 第十七遍枚举为空——fail-closed 拒绝路径普查给切片错误面
首次完整定价（17 条 throw 端点 2.5–12.1µs + 16 条非抛出拒绝
115–941ns，全部零稳态流量或零调用方，§2.1–§2.2），R14-H 门 throw
拒列获数字证实，结构位置 × 消除机制扫描零从未点名结构位点（§2.3）。
不硬凑 ID，不为定价已判面铸 ID。与 R10-H…R16-H 及 R17-A/B/C/D/E 的
空枚举收口同型。

## 附录：确定性锚点 + fail-closed 拒绝路径普查测量脚本（完整，可复现）

运行方式：`pnpm build` 后保存为任意 `.mts` 并 `npx tsx <file>`（仓库
根目录，依赖已装；冷层探针需要 dist 产物）。无赢家 ⇒ 无随机 fuzz
（无候选可裁决），122 项 sanity/fail-closed 检查全为固定输入确定性
断言（含派单要求的三项 S5-H-1 语义 attestation、33 条拒绝终点的错误
类/消息/outcome 断言），三次运行为精确重执行。按惯例不入库。

```ts
/**
 * R17-H deterministic anchor re-verification + fail-closed rejection-path
 * census (seventeenth pass).
 *
 * No fresh candidate survived enumeration (every micro-site found by the
 * seventeenth read maps to an exclusion-table ID or a prior no-ID closure —
 * see report §0/§2), so this script (a) re-establishes, on THIS VM, the full
 * measurement matrix that R8-H..R16-H closed — hot tier three states
 * (default Q=0 / configured A habits+Q=1 gated / configured B vague+Q=3
 * gated; the production assume-defaults track flow always passes
 * resolvedQuestionIds covering ALL questions so the gated re-map materially
 * executes), promotion class, the four zero-production-caller command
 * classes (evaluate / critique / pairwise / reconcile), and cold-tier
 * once-per-process first-call totals on dist — and (b) runs the seventeenth
 * pass's NEW completeness axis for this slice: the FAIL-CLOSED
 * REJECTION-PATH CENSUS (mirroring R17-B's rejection pricing, R17-D's
 * 28-endpoint fail-closed census, and R17-E's rejection price table in
 * their slices).
 *
 *   Sixteen passes measured the SUCCESS paths of this slice (the hot default
 *   chain never throws; R12-H's profile decomposition and R14-H's engine
 *   lens are success-flow accounts). R14-H NAMED the coverage-gate throw
 *   path (message assembly) and rejected optimizing it without a probe;
 *   the slice's other throw endpoints and the adapters' non-throw rejection
 *   returns were never systematically enumerated or priced. This axis
 *   closes that face with numbers: every in-slice rejection endpoint is
 *   (1) enumerated, (2) semantically attested (correct error class +
 *   message prefix, or exact outcome/reason), and (3) priced per call —
 *   whole-call cost of the rejecting invocation, the honest "price of a
 *   rejection" (same framing as R17-D's most-expensive-rejection number).
 *   Expected adjudication: every endpoint is µs-class, executes zero times
 *   on the production success flow, and at most once per REJECTED command
 *   (fail-closed abort) — the once-per-run veto class of the acceptance
 *   bar. No candidate is proposed; R14-H's throw-path rejection is NOT
 *   reopened (nothing here optimizes an error path — it prices them).
 *
 * Reference = production imports everywhere; no candidate is proposed, so
 * there is no randomized fuzz — every sanity/fail-closed check below is
 * fully deterministic (fixed inputs; includes the three S5-H-1 semantic
 * attestations required by the dispatch), and three runs are exact
 * re-executions (timing varies, adjudication class must not).
 *
 * Run: pnpm build, then npx tsx /tmp/r17h-sim.mts   (repo root, deps
 * installed; cold probes need dist).
 */
import { performance } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import {
  extractHeuristicContract,
  heuristicExtractor,
  heuristicCritic,
  type HeuristicHabits
} from "/workspace/src/requirement/heuristic.js";
import {
  buildContractCandidate,
  type RequirementExtractor
} from "/workspace/src/requirement/extractor.js";
import {
  assertCoverageAllowsStart,
  checkCoverageGate,
  coverageMatrixFromTasks,
  type CoverageTaskRef,
  type CoverageStartOptions
} from "/workspace/src/requirement/coverage.js";
import { applyPrecedence, detectConflicts } from "/workspace/src/requirement/precedence.js";
import { critiqueContract } from "/workspace/src/requirement/critic.js";
import { normalizeSources, type RawSource } from "/workspace/src/requirement/normalizer.js";
import { validateRequirementContract, type RequirementContract } from "/workspace/src/domain/contract.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import {
  assertCanPromoteFromReview,
  applyRoutingScoreUpdate
} from "/workspace/src/review/self-review.js";
import { runBlindPairwisePair, type PairwiseInput } from "/workspace/src/review/pairwise.js";
import { reconcileReviews } from "/workspace/src/review/reconcile.js";
import { createCriticObservation } from "/workspace/src/review/critic.js";
import { createEvaluationRecord } from "/workspace/src/evaluation/evaluator.js";
import { createCheckAdapter } from "/workspace/src/evaluation/check-adapter.js";
import { createDeliveryAdapter } from "/workspace/src/evaluation/delivery-adapter.js";
import { createDiffAdapter } from "/workspace/src/evaluation/diff-adapter.js";
import type { AdapterContext, CommandResult } from "/workspace/src/evaluation/adapters.js";
import { createRubric } from "/workspace/src/rubric/types.js";
import type { EpisodeId, TaskId } from "/workspace/src/domain/ids.js";

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean, detail?: string): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function median(xs: number[]): number {
  const sorted = [...xs].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)] ?? Number.NaN;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}
/** Price a sync endpoint that must throw; attests error class + message on the probe call. */
function benchThrow(
  name: string,
  fn: () => void,
  reps: number,
  wantDomainError: boolean,
  messageRe: RegExp
): number {
  let caught: unknown;
  try {
    fn();
  } catch (error) {
    caught = error;
  }
  check(`${name} throws`, caught !== undefined);
  check(
    `${name} error class ${wantDomainError ? "DomainValidationError" : "Error"}`,
    wantDomainError ? caught instanceof DomainValidationError : caught instanceof Error
  );
  check(`${name} message`, caught instanceof Error && messageRe.test(caught.message), String(caught));
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) {
    try {
      fn();
    } catch {
      /* expected: fail-closed rejection */
    }
  }
  return (performance.now() - t0) / reps;
}
/** Price an async endpoint that must reject; attests error class + message on the probe call. */
async function benchThrowAsync(
  name: string,
  fn: () => Promise<void>,
  reps: number,
  messageRe: RegExp
): Promise<number> {
  let caught: unknown;
  try {
    await fn();
  } catch (error) {
    caught = error;
  }
  check(`${name} rejects`, caught !== undefined);
  check(`${name} error class DomainValidationError`, caught instanceof DomainValidationError);
  check(`${name} message`, caught instanceof Error && messageRe.test(caught.message), String(caught));
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) {
    try {
      await fn();
    } catch {
      /* expected: fail-closed rejection */
    }
  }
  return (performance.now() - t0) / reps;
}

const OBJ = "fix the login retry bug in src/auth/session.ts and keep tests green";

/* ================================================================
 * Hot tier, three states (R8-H..R16-H anchor shapes). Measured FIRST so
 * anchor conditions stay comparable (benchAsync single warm call + 3000
 * timed reps, no extra chain executions before).
 * ================================================================ */
async function measureChain(label: string, objective: string, habits: HeuristicHabits | undefined): Promise<void> {
  const args = { objective, ...(habits !== undefined ? { habits } : {}) };
  const chain = await benchAsync(async () => {
    await extractHeuristicContract(args);
  }, 3000);
  const prod = await extractHeuristicContract(args);
  const real = prod.contract;
  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
  const options: CoverageStartOptions | undefined =
    real.questions.length > 0
      ? { resolvedQuestionIds: real.questions.map((question) => question.id) }
      : undefined;
  const gate = bench(() => assertCoverageAllowsStart(real, tasks, options), 50000);
  const precedence = bench(() => void applyPrecedence(real, "user-first"), 100000);
  const total = chain + gate + precedence;
  log(
    `CEILING ${label}: extractHeuristicContract=${(chain * 1e6).toFixed(0)}ns + run-start gate(Q=${real.questions.length}${options !== undefined ? ", all resolved" : ", no options"})=${(gate * 1e6).toFixed(0)}ns + applyPrecedence=${(precedence * 1e6).toFixed(0)}ns = ${(total * 1e6).toFixed(0)}ns once-per-run production total`
  );
}
{
  await measureChain("default (9-pass anchor shape, Q=0)", OBJ, undefined);
  await measureChain("configured A (habits set, Q=1 gated)", OBJ, {
    requireTests: true,
    preferReview: false,
    askBeforeWrite: true
  });
  await measureChain("configured B (vague, Q=3 gated)", "fix bug please", { askBeforeWrite: true });
}

/* ================================================================
 * Command-class anchors beyond run-start.
 * ================================================================ */
{
  const promo = bench(
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    200000
  );
  log(`CEILING promotion class: assertCanPromoteFromReview=${(promo * 1e6).toFixed(1)}ns once per promotion (production)`);

  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const evaluate = bench(() => void createEvaluationRecord(evalInput), 50000);
  log(`CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=${(evaluate * 1e6).toFixed(0)}ns/call`);

  const prod = await extractHeuristicContract({ objective: OBJ });
  const critique = bench(() => void critiqueContract(prod.contract), 50000);
  log(`CEILING critique class (inside run-start chain): critiqueContract(real contract)=${(critique * 1e6).toFixed(0)}ns/call`);

  const pairInput: PairwiseInput = {
    episodeId: "ep_00000001" as EpisodeId,
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pairwise = bench(() => void runBlindPairwisePair(pairInput, () => 0.7), 20000);
  const pair = runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  const reconcile = bench(() => void reconcileReviews(two), 100000);
  log(`CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=${(pairwise * 1e6).toFixed(0)}ns/call`);
  log(`CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=${(reconcile * 1e6).toFixed(0)}ns/call`);
  check("pairwise identity-stable scores agree across swap", pair.reconciliation.consensus === "a");
  const biased = runBlindPairwisePair(pairInput, () => 0.7, { bindScoresToSlots: true });
  check("pairwise slot-bound scores reconcile to uncertain", biased.reconciliation.consensus === "uncertain");
  const record = createEvaluationRecord(evalInput);
  check("evaluate deterministic FAIL on missing evidence", record.overall === "FAIL");
  check(
    "evaluate per-criterion outcomes",
    record.scores.map((s) => s.outcome).join(",") === "PASS,PASS,FAIL"
  );
}

/* ================================================================
 * NEW AXIS: fail-closed rejection-path census. Every in-slice rejection
 * endpoint enumerated, semantically attested, and priced (whole-call cost
 * of the rejecting invocation). Two families:
 *   T*  — throw endpoints (fail-closed abort; DomainValidationError unless
 *         noted). Production frequency: zero on success flow, at most once
 *         per REJECTED command.
 *   A*  — non-throw rejection returns (adapters + review critic ABSTAIN/
 *         FAIL/UNOBSERVED). Zero production callers (command-class ceiling
 *         jurisdiction, R9-H..R16-H).
 * ================================================================ */
{
  log("--- REJECTION-PATH CENSUS: throw endpoints (fail-closed aborts) ---");
  const trustedSource: RawSource[] = [];
  const okExtractor = heuristicExtractor({});
  const okCritic = heuristicCritic();

  // T1 extractor.ts L68-70: blank role id.
  const t1 = await benchThrowAsync(
    "T1 buildContractCandidate blank roleId",
    async () => {
      await buildContractCandidate({
        objective: OBJ,
        sources: trustedSource,
        extractor: { ...okExtractor, roleId: "   " },
        critic: okCritic
      });
    },
    3000,
    /role ids are required/
  );
  // T2 extractor.ts L71-73: identical roles.
  const t2 = await benchThrowAsync(
    "T2 buildContractCandidate identical roleIds",
    async () => {
      await buildContractCandidate({
        objective: OBJ,
        sources: trustedSource,
        extractor: { ...okExtractor, roleId: "same-role" },
        critic: { ...okCritic, roleId: "same-role" }
      });
    },
    3000,
    /independently versioned roles/
  );
  // T3 extractor.ts L77 via validateConfidence: extraction confidence out of range.
  const badConfExtractor: RequirementExtractor = {
    roleId: "bad-conf-extractor-v1",
    async extract(input) {
      const result = await okExtractor.extract(input);
      return { ...result, confidence: 1.5 };
    }
  };
  const t3 = await benchThrowAsync(
    "T3 extraction confidence out of range",
    async () => {
      await buildContractCandidate({
        objective: OBJ,
        sources: trustedSource,
        extractor: badConfExtractor,
        critic: okCritic
      });
    },
    2000,
    /extraction confidence must be between 0 and 1/
  );
  // T4 extractor.ts L79: minimum confidence out of range.
  const t4 = await benchThrowAsync(
    "T4 minimum confidence out of range",
    async () => {
      await buildContractCandidate({
        objective: OBJ,
        sources: trustedSource,
        extractor: okExtractor,
        critic: okCritic,
        minimumConfidence: 2
      });
    },
    2000,
    /minimum confidence must be between 0 and 1/
  );
  // T5 extractor.ts L85: inference confidence invalid (NaN).
  const badInferenceExtractor: RequirementExtractor = {
    roleId: "bad-inference-extractor-v1",
    async extract(input) {
      const result = await okExtractor.extract(input);
      return {
        ...result,
        inferences: [{ statement: "latent", corroboratedSourceRefs: [], confidence: Number.NaN }]
      };
    }
  };
  const t5 = await benchThrowAsync(
    "T5 inference confidence invalid",
    async () => {
      await buildContractCandidate({
        objective: OBJ,
        sources: trustedSource,
        extractor: badInferenceExtractor,
        critic: okCritic
      });
    },
    2000,
    /inference confidence must be between 0 and 1/
  );
  // T6 extractor.ts assertAuthorityGrounding: ungrounded authority grant.
  const authorityExtractor: RequirementExtractor = {
    roleId: "authority-extractor-v1",
    async extract(input) {
      const result = await okExtractor.extract(input);
      const contract = validateRequirementContract({
        ...result.contract,
        authority: [{ scope: "fs", actions: ["write"] }]
      });
      return { ...result, contract, authorityGrounding: [] };
    }
  };
  const t6 = await benchThrowAsync(
    "T6 ungrounded authority grant",
    async () => {
      await buildContractCandidate({
        objective: OBJ,
        sources: trustedSource,
        extractor: authorityExtractor,
        critic: okCritic
      });
    },
    2000,
    /authority grant 0 requires a user or approved-project source/
  );

  const prod = await extractHeuristicContract({ objective: OBJ });
  const real = prod.contract;
  const coveredTasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
  const uncoveredTasks = coveredTasks.map((task) => ({
    ...task,
    acceptanceCriteria: task.acceptanceCriteria.slice(0, 1)
  }));
  // T7 coverage.ts L97: gate blocked — uncovered criterion (R14-H named message assembly, priced here).
  const t7 = benchThrow(
    "T7 gate blocked (uncovered criterion)",
    () => assertCoverageAllowsStart(real, uncoveredTasks),
    10000,
    true,
    /coverage gate blocked start: uncovered=/
  );
  // T8 coverage.ts L97: gate blocked — unresolved blocking decisions (vague contract, no options).
  const vague = await extractHeuristicContract({ objective: "fix bug please" });
  const vagueTasks: CoverageTaskRef[] = [
    {
      id: "tsk_00000000" as TaskId,
      acceptanceCriteria: vague.contract.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
    }
  ];
  const t8 = benchThrow(
    "T8 gate blocked (blocking decisions)",
    () => assertCoverageAllowsStart(vague.contract, vagueTasks),
    10000,
    true,
    /coverage gate blocked start: blocking=/
  );
  // T9 coverage.ts L97: worst reachable message — uncovered AND blocking at once.
  const t9 = benchThrow(
    "T9 gate blocked (uncovered + blocking combined)",
    () => assertCoverageAllowsStart(vague.contract, [
      { id: "tsk_00000000" as TaskId, acceptanceCriteria: [] }
    ]),
    10000,
    true,
    /coverage gate blocked start: uncovered=.*; blocking=/
  );

  // T10/T11 normalizer.ts origin-kind mismatch (plain Error).
  const t10 = benchThrow(
    "T10 normalizeSources user-turn origin on code kind",
    () => void normalizeSources([{ kind: "file", ref: "src/x.ts", content: "text", origin: "user-turn" }]),
    20000,
    false,
    /source origin user-turn is not valid for file/
  );
  const t11 = benchThrow(
    "T11 normalizeSources approved-spec origin on message kind",
    () => void normalizeSources([{ kind: "message", ref: "m-1", content: "text", origin: "approved-spec" }]),
    20000,
    false,
    /source origin approved-spec is not valid for message/
  );

  // T12/T13 evaluator.ts input validation.
  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 1, observableCheck: "check-1" }
  ]);
  const evalBase = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a" }
  };
  const t12 = benchThrow(
    "T12 createEvaluationRecord blank target artifactId",
    () => void createEvaluationRecord({ ...evalBase, target: { artifactId: "   " } }),
    20000,
    true,
    /evaluation target artifactId must be non-empty/
  );
  const t13 = benchThrow(
    "T13 createEvaluationRecord unknown independence class",
    () =>
      void createEvaluationRecord({
        ...evalBase,
        independenceClass: "colluding" as unknown as "paired"
      }),
    20000,
    true,
    /unknown independence class: colluding/
  );

  // T14..T17 self-review fail-closed rejections (both call sites × both triggers).
  const t14 = benchThrow(
    "T14 applyRoutingScoreUpdate self by kind",
    () =>
      void applyRoutingScoreUpdate({
        reviewerId: "rev-1",
        actorId: "act-1",
        reviewerKind: "self",
        currentScore: 0.5,
        delta: 0.1
      }),
    20000,
    true,
    /self-review cannot update routing score/
  );
  const t15 = benchThrow(
    "T15 applyRoutingScoreUpdate self by identity",
    () =>
      void applyRoutingScoreUpdate({
        reviewerId: "agent-1",
        actorId: "agent-1",
        reviewerKind: "peer",
        currentScore: 0.5,
        delta: 0.1
      }),
    20000,
    true,
    /self-review cannot update routing score/
  );
  const t16 = benchThrow(
    "T16 assertCanPromoteFromReview self by kind",
    () => assertCanPromoteFromReview({ reviewerKind: "self", reviewerId: "rev-1", actorId: "act-1" }),
    20000,
    true,
    /self-review cannot promote the actor resource/
  );
  const t17 = benchThrow(
    "T17 assertCanPromoteFromReview self by identity",
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "agent-1", actorId: "agent-1" }),
    20000,
    true,
    /self-review cannot promote the actor resource/
  );

  const throwRows: [string, number][] = [
    ["T1 blank roleId", t1],
    ["T2 identical roleIds", t2],
    ["T3 extraction confidence", t3],
    ["T4 minimum confidence", t4],
    ["T5 inference confidence", t5],
    ["T6 ungrounded authority", t6],
    ["T7 gate uncovered", t7],
    ["T8 gate blocking", t8],
    ["T9 gate uncovered+blocking", t9],
    ["T10 origin user-turn/file", t10],
    ["T11 origin approved-spec/message", t11],
    ["T12 blank artifactId", t12],
    ["T13 unknown independence class", t13],
    ["T14 routing-score self kind", t14],
    ["T15 routing-score self identity", t15],
    ["T16 promote self kind", t16],
    ["T17 promote self identity", t17]
  ];
  for (const [name, ns] of throwRows) {
    log(`REJECT ${name}: ${(ns * 1e6).toFixed(0)}ns/rejected-call`);
    check(`census throw endpoint priced (${name})`, Number.isFinite(ns) && ns > 0);
  }
  const worst = throwRows.reduce((best, row) => (row[1] > best[1] ? row : best));
  log(
    `CENSUS throw endpoints: 17 enumerated; most expensive = ${worst[0]} at ${(worst[1] * 1e6).toFixed(0)}ns/rejected-call; all fail-closed aborts, zero steady-state traffic`
  );
  check("census: every throw endpoint stays µs-class (<100µs)", throwRows.every(([, ns]) => ns < 0.1));

  // Orphan leg note: coverageMatrixFromTasks always returns orphanRequirements
  // [], so the orphans= message segment is unreachable through
  // assertCoverageAllowsStart; attest checkCoverageGate's orphan branch
  // directly (direct-call-only surface, zero production producers).
  const orphanMatrix = { ...coverageMatrixFromTasks(real, coveredTasks), orphanRequirements: ["ghost-req"] };
  const orphanResult = checkCoverageGate(real, orphanMatrix);
  check(
    "census: checkCoverageGate orphan branch rejects (direct-call-only)",
    !orphanResult.ok && orphanResult.orphans.length === 1 && orphanResult.orphans[0] === "ghost-req"
  );

  log("--- REJECTION-PATH CENSUS: non-throw rejection returns (zero-caller faces) ---");
  const context: AdapterContext = {
    episodeId: "ep_00000001" as EpisodeId,
    workingDirectory: "/workspace",
    revision: "rev-abc",
    changeSet: ["src/a.ts", "src/b.ts"]
  };
  const checkAdapter = createCheckAdapter();
  const deliveryAdapter = createDeliveryAdapter();
  const diffAdapter = createDiffAdapter();
  const okCommand: CommandResult = {
    exitCode: 0,
    stdout: "ok",
    stderr: "",
    durationMs: 12,
    command: "pnpm test",
    cwd: "/workspace",
    revision: "rev-abc",
    changeSet: ["src/a.ts", "src/b.ts"]
  };
  const asyncCells: [string, () => Promise<{ outcome: string; reason?: string }>, string, RegExp | undefined][] = [
    ["A1 check ABSTAIN invalid input", () => checkAdapter.evaluate(context, { nope: true }), "ABSTAIN", /expected CommandResult/],
    ["A2 check FAIL cwd mismatch", () => checkAdapter.evaluate(context, { ...okCommand, cwd: "/elsewhere" }), "FAIL", /outside the episode working directory/],
    ["A3 check FAIL stale revision", () => checkAdapter.evaluate(context, { ...okCommand, revision: "rev-old" }), "FAIL", /stale result for revision/],
    ["A4 check FAIL stale changeSet", () => checkAdapter.evaluate(context, { ...okCommand, changeSet: ["src/other.ts"] }), "FAIL", /stale result for change set/],
    ["A5 check FAIL exit!=0", () => checkAdapter.evaluate(context, { ...okCommand, exitCode: 2, stderr: "boom" }), "FAIL", /boom/],
    ["A6 delivery UNOBSERVED null", () => deliveryAdapter.evaluate(context, null), "UNOBSERVED", /no delivery evidence configured/],
    ["A7 delivery ABSTAIN invalid", () => deliveryAdapter.evaluate(context, { manualAcceptance: 42 }), "ABSTAIN", /expected DeliveryEvidence/],
    ["A8 delivery FAIL rollback", () => deliveryAdapter.evaluate(context, { rollbackDetected: true }), "FAIL", /rollback detected/],
    ["A9 delivery FAIL reopen", () => deliveryAdapter.evaluate(context, { reopenDetected: true }), "FAIL", /reopened after closure/],
    ["A10 delivery FAIL manual reject", () => deliveryAdapter.evaluate(context, { manualAcceptance: false }), "FAIL", /manual rejection/],
    ["A11 delivery UNOBSERVED unrecorded", () => deliveryAdapter.evaluate(context, {}), "UNOBSERVED", /not recorded/],
    ["A12 diff ABSTAIN invalid", () => diffAdapter.evaluate(context, { episodeOwned: "not-array" }), "ABSTAIN", /expected DiffScope/],
    ["A13 diff FAIL unrelated", () => diffAdapter.evaluate(context, { episodeOwned: [], unrelatedUser: ["x.ts"], generated: [], unknown: [] }), "FAIL", /unrelated user changes/],
    ["A14 diff FAIL unknown ownership", () => diffAdapter.evaluate(context, { episodeOwned: [], unrelatedUser: [], generated: [], unknown: ["y.ts"] }), "FAIL", /unknown ownership/],
    ["A15 diff UNOBSERVED empty", () => diffAdapter.evaluate(context, { episodeOwned: [], unrelatedUser: [], generated: [], unknown: [] }), "UNOBSERVED", /no episode-owned changes/]
  ];
  const rejectRows: [string, number][] = [];
  for (const [name, run, wantOutcome, reasonRe] of asyncCells) {
    const probe = await run();
    check(`${name} outcome ${wantOutcome}`, probe.outcome === wantOutcome, probe.outcome);
    if (reasonRe !== undefined) {
      check(`${name} reason`, probe.reason !== undefined && reasonRe.test(probe.reason), probe.reason);
    }
    const ns = await benchAsync(async () => {
      await run();
    }, 20000);
    rejectRows.push([name, ns]);
    log(`REJECT ${name}: ${(ns * 1e6).toFixed(0)}ns/call`);
  }
  // A16 review critic ABSTAIN on actor defense (sync).
  const criticRubric = createRubric("rub-2", "task", [
    { id: "crit-1", description: "d1", weight: 1, observableCheck: "check-1" }
  ]);
  const defenseProbe = createCriticObservation({
    episodeId: "ep_00000001" as EpisodeId,
    rubric: criticRubric,
    evidence: { "crit-1": "e" },
    actorDefense: "please pass me"
  });
  check("A16 critic ABSTAIN on actor defense", defenseProbe.overall === "ABSTAIN" && defenseProbe.scores.length === 0);
  const a16 = bench(
    () =>
      void createCriticObservation({
        episodeId: "ep_00000001" as EpisodeId,
        rubric: criticRubric,
        evidence: { "crit-1": "e" },
        actorDefense: "please pass me"
      }),
    20000
  );
  rejectRows.push(["A16 critic ABSTAIN actor defense", a16]);
  log(`REJECT A16 critic ABSTAIN actor defense: ${(a16 * 1e6).toFixed(0)}ns/call`);
  const worstA = rejectRows.reduce((best, row) => (row[1] > best[1] ? row : best));
  log(
    `CENSUS non-throw rejection returns: 16 enumerated; most expensive = ${worstA[0]} at ${(worstA[1] * 1e6).toFixed(0)}ns/call; zero production callers (command-class ceiling jurisdiction)`
  );
  check("census: every non-throw rejection stays sub-10µs", rejectRows.every(([, ns]) => ns < 0.01));
}

/* ================================================================
 * Deterministic fail-closed / behavior sanity (fixed inputs; mirrors the
 * R14-H..R16-H check families so the seventeenth pass re-attests every
 * guard, including the three S5-H-1 semantic attestations the dispatch
 * requires).
 * ================================================================ */
{
  const prod = await extractHeuristicContract({ objective: OBJ });
  const real = prod.contract;
  check("default contract has C=2 criteria (ac-objective + ac-tests)", real.acceptanceCriteria.length === 2);
  check("default contract Q=0", real.questions.length === 0);
  check("default confidence 0.86", prod.confidence === 0.86);
  const vague = await extractHeuristicContract({ objective: "fix bug please" });
  check("vague confidence 0.55", vague.confidence === 0.55);
  check("vague emits questions", vague.contract.questions.length > 0);

  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
  let threw = false;
  try {
    assertCoverageAllowsStart(real, tasks);
  } catch {
    threw = true;
  }
  check("gate passes when fully covered", !threw);
  const matrix = coverageMatrixFromTasks(real, tasks);
  const gateResult = checkCoverageGate(real, matrix);
  check("raw gate result clean", gateResult.ok && gateResult.uncoveredCriteria.length === 0);

  // S5-H-1 semantics attestation: pre-allocation guard early-returns [] when
  // either side is empty; conflict side still detected.
  const mk = (checksArr: string[]): RequirementContract =>
    validateRequirementContract({
      schemaVersion: 1,
      objective: "conflict guard attestation objective",
      deliverables: [{ id: "d-1", description: "d", artifactKind: "diff", sourceRefs: [{ kind: "message", ref: "s" }] }],
      constraints: [],
      nonGoals: [],
      acceptanceCriteria: checksArr.map((c, i) => ({
        id: `ac-${i + 1}`,
        description: `criterion ${i + 1}`,
        observableCheck: c,
        sourceRefs: [{ kind: "message", ref: "s" }]
      })),
      assumptions: [],
      questions: [],
      authority: [],
      sourceRefs: [{ kind: "message", ref: "s" }]
    });
  check("S5-H-1: fast-only contract → no conflicts", detectConflicts(mk(["fast check", "other"])).length === 0);
  check("S5-H-1: slow-only contract → no conflicts", detectConflicts(mk(["slow check", "other"])).length === 0);
  const conflicted = mk(["must be fast", "may be slow"]);
  const conflicts = detectConflicts(conflicted);
  check("S5-H-1: fast+slow contract → one conflict", conflicts.length === 1 && conflicts[0]?.ids.length === 2);
  const resolved = applyPrecedence(conflicted, "user-first");
  check(
    "applyPrecedence user-first keeps first criterion and drops second",
    resolved.acceptanceCriteria.length === 1 &&
      resolved.acceptanceCriteria[0]?.id === "ac-1" &&
      resolved.assumptions.some((a) => a.id === "a-superseded-ac-2")
  );
  check("production contract has no conflicts", detectConflicts(real).length === 0);
  check("critique of production contract is clean", critiqueContract(real).score === 100);
  const empty = reconcileReviews([]);
  check("reconcile empty input → tie", empty.consensus === "tie" && empty.dissentCount === 0);
}

/* ================================================================
 * Cold tier on dist, default + configured (5 fresh processes each).
 * ================================================================ */
const mkColdScript = (configured: boolean): string => `
  const { performance } = await import("node:perf_hooks");
  const { extractHeuristicContract } = await import("/workspace/dist/requirement/heuristic.js");
  const { assertCoverageAllowsStart } = await import("/workspace/dist/requirement/coverage.js");
  const { applyPrecedence } = await import("/workspace/dist/requirement/precedence.js");
  const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
  const args = ${"{"} objective${"}"};
  ${configured ? 'const cfgArgs = { objective, habits: { requireTests: true, preferReview: false, askBeforeWrite: true } };' : ""}
  const t0 = performance.now();
  const prod = await extractHeuristicContract(${configured ? "cfgArgs" : "args"});
  const tasks = Array.from({ length: 5 }, (_, i) => ({
    id: "tsk_0000000" + i,
    acceptanceCriteria: prod.contract.acceptanceCriteria.map((c) => ({ id: c.id }))
  }));
  ${configured
    ? "assertCoverageAllowsStart(prod.contract, tasks, { resolvedQuestionIds: prod.contract.questions.map((q) => q.id) });"
    : "assertCoverageAllowsStart(prod.contract, tasks);"}
  applyPrecedence(prod.contract, "user-first");
  const t1 = performance.now();
  process.stdout.write((t1 - t0).toFixed(3));
`;
{
  for (const [label, configured] of [
    ["default", false],
    ["configured (habits + Q=1 gated)", true]
  ] as const) {
    const samples: number[] = [];
    for (let i = 0; i < 5; i += 1) {
      const proc = spawnSync(process.execPath, ["--input-type=module", "-e", mkColdScript(configured)], {
        encoding: "utf8"
      });
      if (proc.status !== 0) {
        check(`cold probe runs (${label})`, false, proc.stderr);
        break;
      }
      samples.push(Number(proc.stdout.trim()));
    }
    log(
      `COLD first-call production total (dist, fresh process, ${label}): median=${(median(samples) * 1e3).toFixed(0)}µs range=[${(Math.min(...samples) * 1e3).toFixed(0)}, ${(Math.max(...samples) * 1e3).toFixed(0)}]µs — once per PROCESS (veto class)`
    );
    check(`cold probes produced numbers (${label})`, samples.length === 5 && samples.every(Number.isFinite));
  }
}

if (failures > 0) {
  process.stderr.write(`\n${failures} of ${checks} check(s) FAILED\n`);
  process.exit(1);
}
log(`\nALL ${checks} SANITY/FAIL-CLOSED CHECKS PASSED (deterministic)`);
```

MORE_OPTIMA=no
BRANCH=cursor/r17-h-eval-seventeenth-pass-83a1
