MODEL_SLUG=claude-fable-5-thinking-xhigh

# R17-F：`src/experiments/` 第十七遍复查报告（S7-F-1 + S7-F-2 之上，全新第十七组枚举）

- 基线：`cursor/sota-persistent-opt-83a1` @ `ddc3df1`（含 R17-E 空枚举收口与 R17-G 派发；切片最后一次改动仍是 S7-F 落地提交 `519101f`，本轮实测 `git diff 519101f..HEAD -- src/experiments/` 为空——**零漂移**，第十七轮连续第十一轮字节不变；`git log -- src/experiments/` tip 即 `519101f`）
- 切片：`src/experiments/` 全部 15 文件（attribution-report / canary / comparison-report / dataset / evaluation-card / gated-comparison / holdout / isolation / manifest / plan / replay / shadow-compare / shadow / simulation-holdout / threshold-calibration——`ls | wc -l` = **15**，逐一全量重读，未依赖历轮记忆），工作在 S1-F / S5-F / S6-F-1 / S7-F-1 / S7-F-2 之上
- 前置阅读：README、EXCLUSIONS 全表（546 行全读，含 S13-B-1 与 S1-F/S5-F/S6-F/S7-F/S8-F/S9-F 全系）、round-17/PLAN（含 R17-A/B/D 空枚举收口摘要与 F 切片专令）、R16-F、R15-F、四个邻居文件（`adaptation/reflection.ts` / `adaptation/eval-routing.ts` / `adaptation/promotion-rules.ts` / `routing/r1-shadow-report.ts`，只读未改）
- 分支：`cursor/r17-f-experiments-seventeenth-pass-83a1`（已推送，未开 PR）
- 环境：Node 22.22.2（VM 默认 22.14.0 低于 engines ≥22.19.0，按历轮同处理以 nvm PATH 前置切换）、pnpm 10.17.1、`pnpm install --frozen-lockfile`

## 结论

**诚实的空枚举：第十七组没有可立 ID 的新角度，不铸任何 S17-F-\* ID，生产代码零改动。** 本轮按令做全新第十七组枚举：从零重读 15 个源文件与四个邻居、在本 VM 重建全部锚点与四次序符号（r5f/r7f 各两次独立运行）、按「站点 ×（跳过 / 替换证明 / 换数据结构 / 换布局 / 复用 / 增量化 / 早退 / 重排 / 并行化 / 批量 / 惰性 / 规范化）」矩阵穷举候选方向后逐一对照排除表——**每一个方向都被既有条目（或其重命名/换址形态）严格覆盖，或其量级天花板经本 VM 实测深低于数十 ms 落地线**。R16-F 宣告的十二层封条（账目直测、最大行分解、外推曲线、GC 类顶、来源格、JIT 档、MDE 地板、长度格、引擎格、剖析归属、A 越线点格、编码格）经本轮复核成立。

本轮的实质产出是**三条历轮从未直测的新轴**（§1.3），与 Round 17 同轮兄弟对齐（R17-A 新增生产态冷进程预算、R17-B 新增拒绝/异常路径定价与逐任务尾部分布、R17-D 新增 fail-closed 拒绝路径普查），把「拒绝路径的量级」「冷进程首实验成本」「操作粒度尾部形状」三个此前只有间接证据的维度落到直接测量：

- **轴 1：fail-closed 拒绝路径普查 + 定价**。切片共 **142 个 `DomainValidationError` 抛出点**（生产 shadow 链 49 个：plan.ts 22 + shadow.ts 27），十六轮把接受路径逐项定价，但从未问过「一次拒绝到底多贵」。本轮在锚点规模（P=2000/A=1000）实测最贵与最廉端点（两次独立运行）：**裸 throw 地板 1.50–1.51 µs**；validate 末位重复拒绝 0.128–0.158 ms（vs 接受 0.049 ms）；**最贵拒绝 = restore 未知哈希（下标 0 失配 → 原落地循环重放 → 全 population 扫描 miss → 回退 Set 建表 + 首犯探针命名）0.308–0.474 ms**（vs 接受 restore 0.052–0.053 ms，~6–9×）；recordOutcome 未分配拒绝 0.124–0.168 ms；廉价 nowMs 拒绝在强制 restore 之上仅增 3.6–5.4 µs。**全部拒绝端点即使在锚点规模也是亚 ms，且稳态生产流量为零**（`evaluateProposalShadow` 只回灌自己 assign 过的 outcome）——拒绝侧不存在收益池，无候选。
- **轴 2：冷进程预算（首实验 vs 稳态差）**。R14-F 审计过稳态 deopt=0，但从未直测新进程里**第一次**全实验为函数编译、IC 预热、V8 字符串哈希填充（P=2000 条目的哈希首算后缓存在字符串对象内，后续 validate 全部复用）付多少。本轮 4 个全新进程实测：**冷首跑 130.6–136.5 ms vs 稳态中位 113.2–118.5 ms，冷差 16.8–22.0 ms**，第 2–3 跑即衰减到 ~125–131 ms。该成本严格 once-per-process：验收线明文排除「once-per-run CLI 噪声」类，且预热干预已被 S7-H-3（净负）封死——信息性录档，无候选。稳态中位数字同时构成锚点的第四个独立视图（与 §1.1 三视图同带）。
- **轴 3：锚点内操作粒度尾部分布**。锚点的 2000 次操作（1000 assign + 1000 recordOutcome）各付常数 validate 地板 + 随账本长度 k 增长的 O(k) 项，但从未有一轮发布过操作粒度的延迟形状。本轮逐操作计时（两轮 × 各两遍）：**assign p50 57.1–57.8 µs / record p50 55.5–56.3 µs，p90 61.0–64.1 µs，p99 70.5–178.9 µs，尾帽 max 314–1780 µs（GC 停顿）**；first10→last10 爬坡 1.18–1.28×（一个预热污染窗 0.81×）——与账目预测严格一致：validate 地板 ~50 µs + k=1000 处 O(k) 项（拷贝/扫描/rng ≈ 12 µs）。**无悬崖、无整实验视图平均掉的隐藏站点**（与 R16-F 轴 1 剖析归属封条交叉一致），操作粒度维度闭合，无候选。

至此全实验锚点（本 VM 120.5–129.6 ms 三视图带 + 轴 2 稳态第四视图 113.2–118.5，与 R16-F 118.5–133.5 / R15-F 120.7–132.3 重叠）在 R16-F 十二层封条之上再加三枚：**拒绝路径格（零流量亚 ms 定价）、冷进程格（once-per-process 类闭合）、操作粒度格（无悬崖）**。锚点与四次序符号在本 VM 两次独立运行全部复现且同号为正（§1.1），S7-F-1+2 落地单元无回归，r1f 2668 项逐位全过，实验单元套件 75 项全绿，`pnpm gate` 全绿（1168 pass / 0 fail / 1 skipped——既有 provider-smoke 凭据跳过）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；无阈值/测试/公开签名/数据面变更。仓库变更仅本报告一个文件；无赢家故未提交任何 scripts 资产（新轴测量脚本全文进附录 A，遵守 R7-F 以来纪律）。

## 0. 范围与约束遵守 + 关闭 ID 重命名核查

- 阅读顺序按令执行：README → EXCLUSIONS 全表 → round-17/PLAN → R16-F → R15-F → 15 个切片源文件全量重读 → 四个邻居文件只读。清单核对：15 文件与令中列名逐一对应，无缺无余。
- 未重开任何 X0-\*..X4-\* / S1-\* ～ S13-\* 条目。R10-F…R16-F 未铸 S10..S16-F-\*，本轮未替其补铸；本轮亦未铸 S17-F-\*。按令未重铸 R16-F 的剖析归属（`assertUniqueNonEmpty` self 67.7–68.0%）、A 越线点（10 ms 于 A\*≈1.8k–4.9k；生产 A≪1000）与编码格（two-byte +29–31%，纯夹具侧）；未重铸 R15-F 的 A/A MDE（max-abs 1.04–2.64 ms）、L 长度格（9×L 仅 +20%）与引擎代格；未重铸 R14-F 的 GC 类顶、来源格、deopt 审计。本轮枚举表直接引用其数字，未重测未重立。R17-A/B/D/E 空枚举属其他切片，本轮全部工作限于 `src/experiments/`。
- **S7-F-1 ≠ S6-F-5 重命名核查（按令重申，本轮逐字复核落地代码）**：`shadow.ts` L373–386 / `canary.ts` L147–163 的对齐循环是**同下标单次相等比较**（`assignment.episodeHash !== population[index]` 即 break），失配税为有界常数，回退是后缀级 pending-Set（S6-F-1 形态），下标 0 失配改道原落地循环（shadow L388–406 / canary L166–185 逐字保留）；S6-F-5 是 population 上的**双指针前向搜索**，失配白付 O(P) 扫描、符号随次序翻转。四种次序在本 VM 两次独立运行全部同号为正（§1.1）。本轮未提出任何双指针/子序列形态，也未提出任何 O(P) two-pointer 快路径。
- **本轮三条新轴的候选性自查**：(a) 拒绝路径轴测的全部是**零稳态流量**的 fail-closed 端点——最贵拒绝 0.47 ms 亚 ms，且「让拒绝更快」既无池也无动机（fail-closed 防御纵深是硬不变量），非候选；(b) 冷进程轴是 once-per-process 类测量——验收线明文排除该类，预热干预 S7-H-3 已封（净负），非候选；(c) 操作粒度轴是账目的分布视角复核（R16-F 轴 1 的操作粒度对偶），产出是「无悬崖、无隐藏站点」的否定性结论，非候选。三者按 R10-F 以来纪律均不立 ID。
- **其余易混形态自查**：未重提 S8-F-1 / S9-F-1（assign 站点三机制收口完成；生产 A « 1000 未触发任何重开）；未重提 S9-F-2（末位命中见证；缺席证明仍属 S1-F-8）；未重提 S9-F-3（scratch-Set 稳定负优化立档）与 S6-F-2..4 / S5-F-1..3 / S7-F-3..4 / S8-F-1..3 及 S1..S4 的 F 系全部条目；`outcomes.some` 账目引用一律用 R11-F 勘误值 1.63–1.73 ms（机制锁 S1-F-8）。轴 1 与既有条目的区分：S8-F-2/S8-F-3 定价的是**接受路径上的 trim/长度卫**（µs 级机制），轴 1 定价的是**拒绝端点本身**（throw 站点的端到端成本 + 流量为零的裁定）——物理对象不同且轴 1 不提出任何变更。轴 3 与 R16-F 轴 1 的区分：剖析归属按**函数**聚合样本，轴 3 按**操作**聚合墙钟——两个正交切面对同一账目交叉验证，均为否定性封条。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：validateExperimentPlan 每调用照跑（Ω(P) 内容重读）；每条 assignment 逐调用全检（Ω(A)）；`[...assignments]`/`[...outcomes]` 防御拷贝原样；TypeError 消息与属性读取序不变（shadow：liveAction → changedLiveAction → shadowDecision → episodeHash；canary：episodeHash 先行）；双 LCB 与双归因未动（X0-11）；无阈值变更；X1-5（shadow-compare 不并 r1-shadow-report）、X3-3/X4-1（不 fail-open 增量化 canary/shadow restore）、X4-2（不对 readonly 公开追加拷贝契约做可变 push）维持；CAS/凭据/数据面契约/公开签名未触；分析不改 in-flight run、tracking 无命令权、H/score 不写路由 PASS/FAIL；live = R0 等价（R1 不接线）、稀疏 → 已批准 R0 基线；提升 proposal-first；不声称 Outcome-supported。
- 漂移复核：`git diff 519101f..HEAD -- src/experiments/` 为空（本轮实测，diff 输出 0 字节、`--stat` 零行）——R1-F…R16-F 的全部测量与排除裁决可转移，但全部锚点仍按令在本 VM 重测（§1），未拷贝任何历轮数字。

## 1. 锚点重测 + 配置态 × 命令类矩阵 + 三条新轴（本 VM 实测）

### 1.1 全实验锚点与四次序符号（r5f/r7f 两次独立运行，未拷贝历轮数字；Node 22.22.2）

| 档位 | 本 VM 第 1 次 | 本 VM 第 2 次 | R16-F 记载带 |
| --- | --- | --- | --- |
| r5f C2 全实验（P=2000/A=1000） | 122.28 ms | 121.02 ms | 118.46–129.83 ms |
| r6f partD 全实验 | **120.50 ms** | — | 122.04 ms |
| r7f partE 全实验（fixture 略异） | **129.56 ms** | 127.95 ms | 131.84–133.50 ms |
| 2A-validate 组件 | 99.72 ms（**81.6%**） | 100.35 ms（82.9%） | 98.64–109.51 ms（83.3–84.3%） |

锚点带 120.5–129.6 ms 与 R16-F 的 118.5–133.5 / R15-F 的 120.7–132.3 重叠；轴 2 的 4 个全新进程稳态中位 113.19–118.46 ms 构成独立第四视图（比仪表化视图低 ~5 ms，方向与 R16-F 轴 2 的 A=1000 独立视图 113.1–114.6 一致）。分解算术在本 VM 数字上自洽（validate 组件 99.7–100.4 + 历轮逐项池 ≈ 114–117，与三视图带紧邻）。

四种 assignment 次序（r7f partE，S7-F-1+2 落地态 vs 编辑前冻结参考，2000 restores，两次独立运行）：

| 次序 | 第 1 次 | 第 2 次 | 符号 |
| --- | --- | --- | --- |
| prefix（锚点，全对齐） | **+45.46 ms** | **+46.94 ms** | 正 |
| half-aligned | +27.03 ms | +29.71 ms | 正 |
| scattered | +4.16 ms | +5.28 ms | 正 |
| reversed | +5.52 ms | +9.02 ms | 正 |

**全次序两轮同号为正，无符号翻转**——S7-F-1 落地单元在本 VM 复现无回归（scattered 带 +4.16~+5.28 与 R16-F 的 +3.66~+4.58 / R15-F 的 +5.74~+6.10 同带）。r6f partD（S6-F-1+S7-F 叠加视图）：prefix +126.17 / scattered +39.86 / reversed +30.01 ms，三次序全正。r7f validate 组件视图（S7-F-2 单元）：×2000 调用省 +10.12 / +11.30 ms 两轮同号。r5f C1（S5-F 单元）：has+add 152.26/148.57 → add+size 118.55/116.55 ms（+33.70/+32.02 ms，22.1%/21.6%）；C4 索引循环形式 −3.87/−1.14 ms 维持 S5-F-3 抖动带记载；C5 dataset 镜像 13.4/14.9 µs（U=2000）、141.4/138.6 µs（U=20000）维持 S5-F-1 记载。r1f 2668 项逐位检查全过（生产侧 63.1 ms，参考侧 710.6 ms，11.3×）。

### 1.2 配置态 × 命令类矩阵（R7-I 教训，逐项复核）

- **生产消费链**（rg 全仓复核，与 R16-F 逐条一致，**无新 src/ 调用方**；`from "../experiments/"` 全仓仍仅四文件）：`reflection.ts`（validateExperimentPlan + createShadowRunner，唯一 runner 生产链——本轮逐行复核 `evaluateProposalShadow` 逐字未变：按调用方 outcomes 次序 assign+recordOutcome，**无对齐契约**，每 outcome 紧随其 assign 记录——S9-F-2 末位命中前提与 S7-F-1 失配安全前提均未变；生产 createShadowRunner 不传 options，bindExperimentIsolation 早退）；`eval-routing.ts`（gatedComparisonReport / createIsolationGuard / stableStringify / replayCacheKey）；`promotion-rules.ts`（validateComparisonReport）；`r1-shadow-report.ts`（gatedComparisonReport + stripImprovementClaims + comparison-report 类型）。`createCanaryRunner`、`replayPolicy`、`runSimulationHoldout`、sealed-manifest 全链路（`validateSealedDatasetManifest`）、`HoldoutVault`、`calibrateSoftThreshold`、`compareShadowR1`、`writeAttributionPair` 仓内仍仅测试可达（本轮逐符号 rg 实测，8 个入口全部零 src/ 切片外消费者）。
- **命令类覆盖**：start（常数构造）、assign（站点三机制收口完成 S2-F-4/S8-F-1/S9-F-1；轴 3 实测 p50 57 µs 即 validate 地板 + O(k) 项）、recordOutcome（三扫融合上界即三池之和 ~3.7 ms；轴 3 p50 56 µs 同构）、restore（S7-F-1 落地形态零漂移，四次序符号 §1.1；轴 1 定价其最贵拒绝 0.31–0.47 ms）、cancel（restore + clock + 一次 spread；轴 1 廉价拒绝增量 3.6–5.4 µs 即其 clock 卫）。
- **配置态维度**：canary 块（test-only，历轮裁决对 canary 站点对称覆盖——test-only 加倍否决）、missingOutcomePolicy 三值（同成本路径）、halted 态（canonicalHaltReason 每 restore 至多一次 trim）、options.outputRoot 隔离守卫（每 runner 一次且生产不传）——历轮矩阵无空洞。
- **拒绝态维度（本轮新增，轴 1）**：142 个抛出点全普查（plan 22 / shadow 27 / canary 14 / dataset 19 / manifest 14 / holdout 11 / simulation-holdout 10 / comparison-report 8 / evaluation-card 7 / isolation 6 / replay 3 / gated-comparison 1 / attribution-report、shadow-compare、threshold-calibration 各 0）；最贵/最廉端点定价见 §1.3；稳态生产流量为零。
- **进程温度维度（本轮新增，轴 2）**：冷首跑 +16.8~+22.0 ms once-per-process，第 2–3 跑衰减完毕；稳态即历轮全部测量的前提态，无遮蔽。

### 1.3 三条新轴测量（各两次独立运行）

**轴 1：拒绝路径定价（P=2000/A=1000，5 轮取最小，两次独立运行）**

| 端点 | 第 1 次 | 第 2 次 | 对照（接受路径） |
| --- | --- | --- | --- |
| 裸 throw 地板（构造+抛+接） | 1.50 µs | 1.51 µs | — |
| validate 重复项在末位（P−1 次成功探针后 size 停滞） | 0.128 ms | 0.158 ms | 接受 validate 0.049 ms |
| **restore 未知哈希（最贵：0 失配→原循环→全 P 扫描 miss→回退建表+首犯探针）** | **0.474 ms** | **0.308 ms** | 接受 restore 0.052/0.053 ms |
| recordOutcome 未分配 episode（validate + O(A) miss + throw） | 0.168 ms | 0.124 ms | — |
| 廉价 nowMs 拒绝（cancel 路径，强制 restore 之上的增量） | +5.35 µs | +3.62 µs | — |

结论：(a) 全部拒绝端点亚 ms——最贵者也仅 ~9× 接受 restore；(b) 消息全部与生产逐字一致（脚本内断言）；(c) 稳态流量为零：`evaluateProposalShadow` 只回灌自己 assign 的 outcome，reject 只在外部注入损坏态时可达——**拒绝侧无收益池、无候选**，fail-closed 防御纵深是硬不变量不动。

**轴 2：冷进程预算（4 个全新进程，各 15 跑，稳态取第 6–15 跑中位）**

| 进程 | 冷首跑 | 第 2 跑 | 第 3 跑 | 稳态中位 | 冷差 |
| --- | --- | --- | --- | --- | --- |
| 1 | 136.48 ms | 131.09 | 130.89 | 118.46 ms | **+18.01 ms** |
| 2 | 135.20 ms | 130.35 | 128.63 | 113.19 ms | **+22.01 ms** |
| 3 | 134.26 ms | 128.99 | 127.37 | 115.09 ms | +19.18 ms |
| 4 | 130.57 ms | 126.05 | 125.62 | 113.73 ms | +16.83 ms |

结论：冷差 16.8–22.0 ms（首跑为函数编译 + IC 预热 + P 条目字符串哈希首算），2–3 跑内衰减。once-per-process 类：验收线明文排除，预热干预 S7-H-3 已封（净负）——信息性录档，**无候选**。稳态中位 113.2–118.5 ms 作为锚点第四视图录 §1.1。

**轴 3：操作粒度尾部分布（锚点内逐操作计时，两轮 × 各两遍；µs）**

| 遍 | 操作 | p50 | p90 | p99 | max（尾帽） | first10→last10 爬坡 |
| --- | --- | --- | --- | --- | --- | --- |
| 1-1 | assign | 57.7 | 64.1 | 98.7 | 1780.3 | 0.81×（预热污染窗） |
| 1-1 | record | 56.3 | 62.6 | 178.9 | 581.8 | 0.91× |
| 1-2 | assign | 57.1 | 63.5 | 74.9 | 328.5 | 1.25× |
| 1-2 | record | 55.5 | 61.0 | 172.2 | 379.6 | 0.99× |
| 2-1 | assign | 57.8 | 63.9 | 81.9 | 314.3 | **1.28×** |
| 2-1 | record | 56.2 | 61.8 | 159.1 | 375.9 | 1.18× |
| 2-2 | assign | 57.6 | 63.6 | 70.5 | 320.5 | 1.23× |
| 2-2 | record | 56.0 | 61.3 | 159.3 | 317.8 | 1.20× |

结论：(a) p50 双操作 55.5–57.8 µs 即 validate 地板（0.049–0.050 ms/次，§1.3 轴 1 对照列）加薄常数——与账目预测逐 µs 一致；(b) 爬坡 1.18–1.28×（稳态窗）恰为 k=1000 处 O(k) 项（拷贝+扫描+rng ≈ 12 µs）对地板的比值，无超线性、无悬崖；(c) 尾帽 314–1780 µs 全部是 GC 停顿形状（与 R14-F GC 类顶 / R16-F 剖析 GC 行 2.3–2.4% 双向一致：p99 以内无 GC 时 ≤~179 µs）；(d) 仪表化全实验 116.7–122.0 ms 与锚点带同（逐操作 hrtime 开销 ~0.1 µs/op 不扰动）。**操作粒度维度闭合：整实验视图没有平均掉任何站点**（与 R16-F 剖析归属封条交叉验证），无候选。

## 2. 第十七组枚举（诚实为空）：方向 → 覆盖判定全表

候选生成程序：按「站点 ×（跳过 / 替换证明 / 换数据结构 / 换布局 / 复用 / 增量化 / 早退 / 重排 / 并行化 / 批量 / 惰性 / 规范化）」矩阵穷举，再对照排除表做重命名判定。结果：**每格非空即被覆盖**。

| 方向 | 量级判断（本 VM） | 覆盖条目 / 重命名判定 |
| --- | --- | --- |
| **拒绝路径任何加速**（throw 复用、消息惰性构造、回退路径瘦身） | **轴 1 首测：最贵拒绝 0.31–0.47 ms 亚 ms；稳态流量为零** | 零池 + fail-closed 防御纵深硬不变量——不可提案域 |
| **冷进程/预热干预**（模块加载期预热、哈希预填充、IC 预热调用） | 轴 2 首测：冷差 16.8–22.0 ms 但严格 once-per-process | S7-H-3（预热净负）+ 验收线 once-per-run 排除类——不立 ID |
| **操作粒度悬崖假说**（整实验视图平均掉的每操作尖峰） | 轴 3 首测：p50 55.5–57.8 µs 平地板 + 1.18–1.28× 线性爬坡，尾帽全为 GC 形状 | 否定性封条——不存在可提案标的；与 R16-F 剖析归属封条交叉一致 |
| **「组件计时账目有隐藏站点」假说** | R16-F 轴 1 剖析归属：无未具名站点 self ≥1.5%（本轮未重测，直接引用；轴 3 操作切面交叉复核） | 账目完备性双仪器封条维持 |
| **二次方池在更大 A 下的重开**（rng 承载态 / 扫描 Set 化 / 成本增量化 / includes O(1) 化） | R16-F 轴 2：各池 A\*(10ms)≈1.8k–4.9k；生产 A « 1000，条件未触发 | S2-F-1 / S9-F-2 / S1-F-8 / S8-F-1 / S9-F-1 机制封条原样 |
| **任何贴带（≤~3 ms）候选的重测/重开** | R15-F 轴 1 MDE 地板：max-abs 1.04–2.64 ms、4–5 窗伪稳可由纯噪声产生（本轮未重测，直接引用） | 「band-overlap is not a landing」数字化封条维持——S5-F-3 / S6-F-2..3 / S7-F-3..4 等贴带条目不重开 |
| **assign 站点调用方哈希规范化（含任何 L/编码变体）** | R14-F 天花板 ≤7.2 ms + R15-F L 系数 + R16-F 编码格（本轮未重测） | R14-F 已收束；跨调用规范串表 = X1-1 |
| validate 唯一性证明的哈希工作消除/替代（任何结构） | R13-F 88.6–90.4 ms/实验 + R16-F 剖析独立复现（self 79.4–85.7 ms）；本轮 validate 组件 99.7–100.4 ms 同带 | S6-F-4 + S9-F-3 双证人；X1-1/S3-F-2；S6-F-3；S5-F-2 排序毁 first-fault——数字封条双仪器维持 |
| validate 内容重读义务侧再收割 / 常数部跳过 / 布局重构 | 地板 ~9.3–9.5 ms（R13-F）；常数部 0.94 ms（R11-F） | S5-F-3 / S6-F-3 / S6-F-4 / S7-F-2 落地形态 + S3-F-2 / X1-1 族——不可提案域 |
| restore 对齐机制再叠层（双指针子序列、delete+size、批量后缀、相位拆分、外提） | — | S6-F-5（禁令）/ S6-F-2 / S7-F-3..4；S7-F-1 ≠ S6-F-5 核查见 §0；四次序符号两轮全正（§1.1） |
| restore 对齐扫描布局重构 / 检查序重排 / 防御拷贝省略 | 扫描池 ~4.2–4.9 ms（R11-F/R16-F 同带）；属性读取序是硬不变量 | 量级 + 硬不变量——不可提案域 |
| recordOutcome 正向见证 / 查重去 O(A) / 三扫融合 / 追加拷贝可变化 / 成本增量化 | 三池之和 ~3.7 ms（R9–R11-F；轴 3 record p50 与账目一致） | S9-F-2 / S1-F-8 / X4-2 / S1-F-3 + 量级 |
| rng 闭式跳转 / 承载态 | 3.0 ms @A=1000（R16-F 轴 2 同带） | S2-F-1 + X1-1/X0-4 |
| 分配消除类一切形态 / 时钟/预算/返回对象 spread / 恒等快路径 | GC 类顶 2.35–3.08 ms（R14-F + R16-F 双仪器）；轴 3 尾帽形状三向一致 | 逐项既有封条 + 类级数字封条维持 |
| JIT/PIC/预热干预 | R14-F 稳态 deopt=0 + 本轮轴 2 冷差 once-per-process | S7-H-3 + S8-A-3/S8-E-2 先例——不立 ID |
| 引擎面干预（升级 Node、V8 旗标、钉版） | R15-F 轴 3：Node 24 混合信号无 régime 迁移（本轮未重测，直接引用） | S5-I-5（切片外）+ S9-C-2（构建面）+ S6-I-2 家族——不立 ID |
| A 线性站点首字符卫镜像 / 单元素快路径 / outcome 校验微调 | 6.8 µs / ~190 µs / ~10 µs（R8-F 记载维持） | S8-F-2 / S8-F-3 + R2-F 池 |
| canary 站点一切残余 | test-only（§1.2 消费链复核） | test-only 加倍否决 + 历轮对称覆盖 |
| treat-as-failure 替换 outcome 构造 / halted trim / 隔离守卫提升 | O(字段)/缺失条目、每 restore ≤1 trim、每 runner 一次且生产不传 options | 配置态矩阵行（§1.2）+ S3-F-4 / S3-D-3 |
| test-only 链路一切残余（replay/dataset/simulation-holdout/holdout/isolation/manifest/threshold-calibration/evaluation-card/shadow-compare/attribution-report/gated-comparison/comparison-report） | 各 µs–亚 ms，R2–R16 已逐项量化 | S1-F-1..8 / S2-F-1..6 / S3-F-1..5 / S4-F-1..3 / S5-F-1 / S6-D-5 / X1-5 / X3-2 / S3-D-3 维持；无新生产调用方（§1.2）故一切先前规模否决转移 |
| 切片内 import 边惰性化 / 模块级预热 / 并行化 / WASM | — | S5-H-2 / S6-I-1 / S11-G-1（生产收益恒 0）+ S7-H-3（预热净负）+ S9-C-1/2（X0-4 + 单线程逐位审计基底） |

**结论：可立 ID 的新角度数为零。** 与 R16-F 的差别在于：R16-F 把账目完备性（剖析归属）、重开条件规模语义（A 越线点）、夹具编码盲区三维度落到直测；本轮把 (a) **拒绝态维度**（142 抛出点普查 + 最贵/最廉端点定价——fail-closed 面首次有直接数字）、(b) **进程温度维度**（冷首实验预算——once-per-process 类的本切片数字）、(c) **操作粒度维度**（p50/p90/p99/尾帽 + 爬坡系数——整实验平均的分布对偶）三个维度首次直测。第十七遍确认：在当前排除表与逐位契约下，本切片不存在不经表所有者层级契约变更（S3-F-2 深冻结值类型、S6-F-5 次序契约化、S8-F-1/S9-F-1 A 增长阈值等既有重开条件）即可达落地线的候选。**后续轮次对本切片的复核只需重跑 §1.1 锚点 + §1.2 调用面 grep；账目直测、最大行分解、外推曲线、GC 类顶、来源格、JIT 档、MDE 地板、长度格、引擎格、剖析归属、A 越线点格、编码格、拒绝路径格、冷进程格、操作粒度格十五层封条已闭合。**

## 3. 逐文件收口（R1–R16 收口之上的本轮检查点）

| 文件 | 本轮检查点 | 结论 |
| --- | --- | --- |
| `shadow.ts` | S7-F-1 落地形态零漂移逐行复核（§0）；**轴 1 最贵拒绝宿主**（未知哈希 restore 0.31–0.47 ms，27 个抛出点普查）；**轴 3 操作粒度宿主**（assign/record p50 56–58 µs 平地板，爬坡 1.18–1.28×，无悬崖） | 无候选 |
| `canary.ts` | 对称复核（test-only 加倍否决）；14 个抛出点入普查；对齐比较领先体首与 R7-F 记载一致 | 无候选 |
| `plan.ts` | validate 组件 99.7–100.4 ms 同带复现；**轴 1 末位重复拒绝定价 0.128–0.158 ms（22 个抛出点）**；**轴 2 冷差主要宿主**（P 条目字符串哈希首算 once-per-process）；S5-F / S7-F-2 落地形态零漂移 | 无候选 |
| `dataset.ts` | S5-F-1/S4-F-1 维持（sealed 链 test-only；C5 本轮复测 13.4–14.9 µs 同带；19 个抛出点全 test-only 可达） | 无候选 |
| `simulation-holdout.ts` | S2-F-5/S3-F-5/S5-F-1 维持；requireHash 站点属 S8-F-2 同判；10 个抛出点全 test-only 可达 | 无候选 |
| `comparison-report.ts` | X3-2/S4-F-3/S1-F-5 维持；真实 N 亚 ms 预算收口维持（R4-F 量化） | 无候选 |
| `gated-comparison.ts` | S1-F-1/S2-F-6 维持；IMPROVEMENT_CLAIM 双字面量维持 R9-F「重构非优化」裁定 | 无候选 |
| `attribution-report.ts` | 无循环无新调用方、零抛出点 | 无候选 |
| `replay.ts` | 仍无生产调用方（replayCacheKey 除外——小输入每 eval 一次）；S2-F-1/2/3、S4-F-2 维持 | 无候选 |
| `isolation.ts` | S3-F-4 维持；唯一生产路径整体维持 S3-D-3 否决；生产 runner 不传 options 故 bindExperimentIsolation 早退 | 无候选 |
| `manifest.ts` | stableStringify 字节契约维持 R2-F 不提案；S6-D-5 维持；生产调用皆小输入 | 无候选 |
| `holdout.ts` | S1-F-3/X4-2 维持（追加拷贝池裁决同族互证） | 无候选 |
| `threshold-calibration.ts` | S1-F-4 维持；零抛出点 | 无候选 |
| `evaluation-card.ts` | S1-B-7 域维持 | 无候选 |
| `shadow-compare.ts` | 薄封装维持 X1-5；零抛出点 | 无候选 |

## 4. 测试

- `npx tsx scripts/round01-r1f-equivalence-sim.ts` ✓ — 2668 项逐位检查 0 失败（生产侧 63.1 ms / 参考侧 710.6 ms，11.3×；信息行结构描述指 S1-F 时代，R6-F 已披露）
- `npx tsx scripts/round05-r5f-equivalence-sim.ts` ✓ ×2 — 各 224 项 0 失败（C2 端到端 122.28 / 121.02 ms 即 §1.1 锚点）
- `npx tsx scripts/round06-r6f-equivalence-sim.ts` ✓ — 27 项 0 失败（partD 三次序全正 + 全实验 120.50 ms）
- `npx tsx scripts/round07-r7f-equivalence-sim.ts` ✓ ×2 — 各 169 项 0 失败（partE 四次序两轮全部为正 + validate +10.12/+11.30 + 全实验 129.56/127.95 ms）
- `npx tsx --test test/unit/experiments/*.test.ts` ✓ — 75 项 0 失败（10 个实验套件文件）
- `npx tsx tmp-r17f-axes.ts` ✓ ×2（干净独立运行）——三条新轴两轮同带 + 全部拒绝消息断言全过（附录 A；轴 2 由脚本自 spawn 4 个全新进程实现）
- `pnpm gate`（typecheck + lint + test + build）✓ — 1168 pass / 0 fail / 1 skipped（既有 provider-smoke 凭据跳过）。Node ≥22.19.0（engines），本 VM 以 nvm 22.22.2 运行
- 未触碰任何版本化阈值、权限、数据面契约、公开签名；未改任何测试断言；未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md
- 按纪律，新轴测量脚本未入库（全文见附录 A）；`scripts/` 零新增；未弱化任何既有仿真；无赢家故未提交 `scripts/round17-r17f-equivalence-sim.ts`

## 5. 新增排除 ID

**无。** 本轮枚举为空，未铸任何 S17-F-\* ID。本轮三条新轴均为收尾测量而非候选（轴 1 是零流量拒绝面的定价普查、轴 2 是 once-per-process 类的本切片数字、轴 3 是账目的分布对偶复核），按 R10-F..R16-F 同一纪律不立 ID。轴 1 数字（最贵拒绝 0.31–0.47 ms）与轴 2 数字（冷差 16.8–22.0 ms once-per-process）录档供后续轮次引用；两者均不构成任何既有条目的重开证据。

MORE_OPTIMA=no
BRANCH=cursor/r17-f-experiments-seventeenth-pass-83a1

## 附录 A：新轴测量脚本（完整，可复现；无赢家按纪律不入库）

保存为 `tmp-r17f-axes.ts` 后 `npx tsx tmp-r17f-axes.ts` 运行（轴 2 由脚本自 spawn `--cold` 全新进程实现）。这不是候选裁决仿真（本轮无候选进入测量门），是 §1.3 三条新轴的收尾测量。

```ts
/**
 * R17-F new-axis measurements (temporary; NOT candidate adjudications and NOT
 * committed — no winner this round). Three axes no earlier F round measured
 * directly, mirroring the Round-17 sibling practice (R17-A added a
 * cold-process production-state budget, R17-B priced rejection/anomaly paths
 * and per-task tail distributions, R17-D ran a fail-closed reject-path
 * census):
 *
 *   axis 1  fail-closed reject-path census + pricing. The slice holds 142
 *           `throw new DomainValidationError` sites (49 on the production
 *           shadow chain: plan.ts 22 + shadow.ts 27). Sixteen rounds priced
 *           the accept path exhaustively but never priced what a REJECTION
 *           costs at anchor scale: the throw itself, the most expensive
 *           validate rejection (duplicate detected at the last population
 *           index), the most expensive restore rejection (unknown assignment
 *           hash at index 0 — misaligned head, plain-loop replay, full
 *           population scan miss, fallback Set build + first-offender probe),
 *           and the recordOutcome unassigned-episode rejection after a full
 *           assignment ledger. Steady-state production traffic through every
 *           reject path is zero (the reflection chain only feeds outcomes it
 *           itself assigned), so these are informational floors, not pools.
 *
 *   axis 2  cold-process budget (first-experiment vs steady-state delta).
 *           R14-F audited steady-state deopts (=0) but no round measured what
 *           the FIRST full experiment in a fresh process pays for function
 *           compilation, IC warmup, and V8 string-hash population of the
 *           2000 population entries (string hashes are computed once and
 *           cached in the string object; every later validate reuses them).
 *           Run with --cold in a fresh process: prints run-1..run-15 and the
 *           steady median. Once-per-process class — informational only
 *           (S7-H-3 sealed warmup interventions as net-negative).
 *
 *   axis 3  per-operation tail distribution inside the anchor experiment.
 *           The anchor's 2000 operations (1000 assigns + 1000 recordOutcomes)
 *           each pay a constant validate floor (~P Set inserts) plus O(k)
 *           terms that grow with the ledger index k. No round published the
 *           per-op latency shape: p50/p90/p99/max per op type, first-vs-last
 *           growth ratio, and the tail cap. If the per-op distribution had a
 *           hidden cliff, it would name a site the whole-experiment view
 *           averages away; if it is the flat floor + gentle linear ramp the
 *           ledger predicts, the per-op dimension closes with no candidate.
 *
 * Uses the production modules directly. Run: npx tsx tmp-r17f-axes.ts
 * (axis 2 spawns itself with --cold twice, fresh processes).
 */

import { execFileSync } from "node:child_process";
import { DomainValidationError } from "./src/domain/errors.js";
import { createCandidateId, createResourceVersionId } from "./src/domain/ids.js";
import { validateExperimentPlan, type ExperimentPlan } from "./src/experiments/plan.js";
import { createShadowRunner, type ExperimentOutcome, type ShadowState } from "./src/experiments/shadow.js";

function timeMs(fn: () => void, rounds = 5): number {
  let best = Infinity;
  for (let r = 0; r < rounds; r++) {
    const t0 = process.hrtime.bigint();
    fn();
    const t1 = process.hrtime.bigint();
    best = Math.min(best, Number(t1 - t0) / 1e6);
  }
  return best;
}

/** Historical fixture shape: `ep_` + zero-padded base36 index, L=11. */
function makePopulation(size: number): string[] {
  const population: string[] = [];
  for (let i = 0; i < size; i++) {
    population.push(`ep_${i.toString(36).padStart(8, "0")}`);
  }
  return population;
}

function makeShadowPlan(population: readonly string[], maxAssignments: number): ExperimentPlan {
  return {
    planVersion: 1,
    experimentId: "exp_r17f_bench",
    mode: "shadow",
    baselineVersionId: createResourceVersionId(() => "r17fbase"),
    candidateId: createCandidateId(() => "r17fcand"),
    population,
    metrics: ["utility"],
    thresholds: { maxGuardrailBreaches: 1_000_000, maxCostUsd: 1e12 },
    budget: { maxAssignments, maxWallClockMs: 1e12 },
    randomization: { seed: 42 },
    stopPolicy: { onGuardrail: "halt", onBudgetExhausted: "halt" },
    missingOutcomePolicy: "exclude",
    canary: undefined,
  };
}

const P = 2000;
const A = 1000;

function fullExperiment(plan: ExperimentPlan, hashes: readonly string[]): ShadowState {
  const runner = createShadowRunner(plan);
  let state = runner.start(0);
  for (let k = 0; k < A; k++) {
    const h = hashes[k] as string;
    state = runner.assign(state, h, 0);
    const outcome: ExperimentOutcome = {
      episodeHash: h,
      utility: 0.5,
      costUsd: 0.01,
      guardrailBreached: false,
    };
    state = runner.recordOutcome(state, outcome, 0);
  }
  if (state.assignments.length !== A) throw new Error("unexpected assignment count");
  return state;
}

/* ------------------------------------------------------------------ */
/* axis 2 (--cold): first-experiment vs steady in a FRESH process      */
/* ------------------------------------------------------------------ */
if (process.argv.includes("--cold")) {
  const population = makePopulation(P);
  const plan = makeShadowPlan(population, A);
  validateExperimentPlan(plan);
  const hashes = population.slice(0, A);
  const runs: number[] = [];
  for (let r = 0; r < 15; r++) {
    const t0 = process.hrtime.bigint();
    fullExperiment(plan, hashes);
    const t1 = process.hrtime.bigint();
    runs.push(Number(t1 - t0) / 1e6);
  }
  const steady = [...runs.slice(5)].sort((a, b) => a - b);
  const median = steady[Math.floor(steady.length / 2)] as number;
  process.stdout.write(
    `cold run-1=${(runs[0] as number).toFixed(2)}ms run-2=${(runs[1] as number).toFixed(2)}ms ` +
      `run-3=${(runs[2] as number).toFixed(2)}ms steady-median(6..15)=${median.toFixed(2)}ms ` +
      `cold-delta=${((runs[0] as number) - median).toFixed(2)}ms\n`
  );
  process.exit(0);
}

const anchorPopulation = makePopulation(P);
const anchorPlan = makeShadowPlan(anchorPopulation, A);
validateExperimentPlan(anchorPlan);
const anchorHashes = anchorPopulation.slice(0, A);

// warm through the same JIT state
fullExperiment(anchorPlan, anchorHashes);
fullExperiment(anchorPlan, anchorHashes);

/* ------------------------------------------------------------------ */
/* axis 1 — reject-path pricing at anchor scale                        */
/* ------------------------------------------------------------------ */

// (1a) throw floor: construct + throw + catch one DomainValidationError
const THROWS = 2000;
const throwFloor = timeMs(() => {
  for (let i = 0; i < THROWS; i++) {
    try {
      throw new DomainValidationError("reject-path floor probe");
    } catch {
      /* priced */
    }
  }
}, 5);

// (1b) most expensive validate rejection: duplicate detected at the LAST
// population index (1999 successful probes, then the size counter stalls)
const dupPopulation = [...anchorPopulation];
dupPopulation[P - 1] = dupPopulation[0] as string;
const dupPlan = makeShadowPlan(dupPopulation, A);
let dupMessage = "";
const tDupValidate = timeMs(() => {
  try {
    validateExperimentPlan(dupPlan);
    throw new Error("expected duplicate rejection");
  } catch (error) {
    dupMessage = (error as Error).message;
  }
}, 5);
if (!dupMessage.startsWith("population contains a duplicate:")) {
  throw new Error(`unexpected duplicate message: ${dupMessage}`);
}

// (1c) most expensive restore rejection: unknown assignment hash at index 0.
// Aligned-prefix loop breaks immediately, the plain landed loop replays all
// A entries into the pending Set, the population scan misses (999/1000
// found), and the fallback builds the full population Set and replays the
// per-assignment probe to name the first offender.
const goodState = fullExperiment(anchorPlan, anchorHashes);
const runner = createShadowRunner(anchorPlan);
const badAssignments = [...goodState.assignments];
badAssignments[0] = { ...(badAssignments[0] as ShadowState["assignments"][number]), episodeHash: "ep_not_in_population" };
const badRestoreState = { ...goodState, assignments: badAssignments } as ShadowState;
let restoreMessage = "";
const tBadRestore = timeMs(() => {
  try {
    runner.restore(badRestoreState);
    throw new Error("expected restore rejection");
  } catch (error) {
    restoreMessage = (error as Error).message;
  }
}, 5);
if (!restoreMessage.includes("is not in the frozen population")) {
  throw new Error(`unexpected restore message: ${restoreMessage}`);
}

// (1d) recordOutcome unassigned-episode rejection after a full ledger:
// one full validate + O(A) assignments.some miss + throw
let unassignedMessage = "";
const tUnassigned = timeMs(() => {
  try {
    runner.recordOutcome(
      goodState,
      { episodeHash: anchorPopulation[A] as string, utility: 0.5, costUsd: 0.01, guardrailBreached: false },
      0
    );
    throw new Error("expected unassigned rejection");
  } catch (error) {
    unassignedMessage = (error as Error).message;
  }
}, 5);
if (!unassignedMessage.startsWith("outcome for unassigned episode:")) {
  throw new Error(`unexpected unassigned message: ${unassignedMessage}`);
}

// (1e) cheap rejection: non-finite nowMs (constant-cost assert)
let cheapMessage = "";
const tCheap = timeMs(() => {
  for (let i = 0; i < 1000; i++) {
    try {
      runner.cancel(goodState, Number.NaN);
      throw new Error("expected nowMs rejection");
    } catch (error) {
      cheapMessage = (error as Error).message;
    }
  }
}, 3);
if (cheapMessage !== "nowMs must be a finite number") {
  throw new Error(`unexpected cheap message: ${cheapMessage}`);
}

// reference: one accepted restore and one accepted validate for comparison
const tGoodRestore = timeMs(() => {
  runner.restore(goodState);
}, 5);
const tGoodValidate = timeMs(() => {
  validateExperimentPlan(anchorPlan);
}, 5);

process.stdout.write(
  `axis 1 (reject paths, P=${P}/A=${A}): throw-floor=${((throwFloor / THROWS) * 1e3).toFixed(2)}µs/throw | ` +
    `dup-at-last-validate=${tDupValidate.toFixed(3)}ms (accepted validate=${tGoodValidate.toFixed(3)}ms) | ` +
    `unknown-hash-restore=${tBadRestore.toFixed(3)}ms (accepted restore=${tGoodRestore.toFixed(3)}ms) | ` +
    `unassigned-outcome=${tUnassigned.toFixed(3)}ms | cheap-nowMs-reject=${((tCheap / 1000) * 1e3).toFixed(2)}µs\n`
);
// NOTE: cancel() on a NaN clock throws inside applyExperimentClock AFTER the
// restore, so the cheap reject includes one accepted restore; report the
// increment too.
process.stdout.write(
  `axis 1 cheap-reject increment over restore: ${(((tCheap / 1000) - tGoodRestore) * 1e3).toFixed(2)}µs\n`
);

/* ------------------------------------------------------------------ */
/* axis 3 — per-operation tail distribution inside one anchor run      */
/* ------------------------------------------------------------------ */
function percentile(sorted: readonly number[], q: number): number {
  return sorted[Math.min(sorted.length - 1, Math.floor(q * sorted.length))] as number;
}

for (let round = 0; round < 2; round++) {
  const runnerT = createShadowRunner(anchorPlan);
  let state = runnerT.start(0);
  const assignTimes: number[] = [];
  const recordTimes: number[] = [];
  for (let k = 0; k < A; k++) {
    const h = anchorHashes[k] as string;
    const t0 = process.hrtime.bigint();
    state = runnerT.assign(state, h, 0);
    const t1 = process.hrtime.bigint();
    const outcome: ExperimentOutcome = { episodeHash: h, utility: 0.5, costUsd: 0.01, guardrailBreached: false };
    const t2 = process.hrtime.bigint();
    state = runnerT.recordOutcome(state, outcome, 0);
    const t3 = process.hrtime.bigint();
    assignTimes.push(Number(t1 - t0) / 1e3);
    recordTimes.push(Number(t3 - t2) / 1e3);
  }
  const firstAssign10 = assignTimes.slice(0, 10).reduce((s, v) => s + v, 0) / 10;
  const lastAssign10 = assignTimes.slice(-10).reduce((s, v) => s + v, 0) / 10;
  const firstRecord10 = recordTimes.slice(0, 10).reduce((s, v) => s + v, 0) / 10;
  const lastRecord10 = recordTimes.slice(-10).reduce((s, v) => s + v, 0) / 10;
  const sortedAssign = [...assignTimes].sort((a, b) => a - b);
  const sortedRecord = [...recordTimes].sort((a, b) => a - b);
  process.stdout.write(
    `axis 3 run ${round + 1} assign (µs): p50=${percentile(sortedAssign, 0.5).toFixed(1)} ` +
      `p90=${percentile(sortedAssign, 0.9).toFixed(1)} p99=${percentile(sortedAssign, 0.99).toFixed(1)} ` +
      `max=${(sortedAssign[sortedAssign.length - 1] as number).toFixed(1)} ` +
      `first10=${firstAssign10.toFixed(1)} last10=${lastAssign10.toFixed(1)} ` +
      `ramp=${(lastAssign10 / firstAssign10).toFixed(2)}x\n`
  );
  process.stdout.write(
    `axis 3 run ${round + 1} record (µs): p50=${percentile(sortedRecord, 0.5).toFixed(1)} ` +
      `p90=${percentile(sortedRecord, 0.9).toFixed(1)} p99=${percentile(sortedRecord, 0.99).toFixed(1)} ` +
      `max=${(sortedRecord[sortedRecord.length - 1] as number).toFixed(1)} ` +
      `first10=${firstRecord10.toFixed(1)} last10=${lastRecord10.toFixed(1)} ` +
      `ramp=${(lastRecord10 / firstRecord10).toFixed(2)}x\n`
  );
  const total = assignTimes.reduce((s, v) => s + v, 0) + recordTimes.reduce((s, v) => s + v, 0);
  process.stdout.write(
    `axis 3 run ${round + 1} instrumented-experiment total=${(total / 1e3).toFixed(2)}ms ` +
      `(tail-cap max-op=${Math.max(sortedAssign[sortedAssign.length - 1] as number, sortedRecord[sortedRecord.length - 1] as number).toFixed(1)}µs)\n`
  );
}

/* ------------------------------------------------------------------ */
/* axis 2 — cold-process budget: spawn self with --cold, twice         */
/* ------------------------------------------------------------------ */
for (let i = 0; i < 2; i++) {
  const out = execFileSync("npx", ["tsx", "tmp-r17f-axes.ts", "--cold"], {
    cwd: process.cwd(),
    encoding: "utf8",
  });
  process.stdout.write(`axis 2 fresh-process ${i + 1}: ${out}`);
}
```

两次干净独立运行（Node 22.22.2）原始输出：

```
axis 1 (reject paths, P=2000/A=1000): throw-floor=1.50µs/throw | dup-at-last-validate=0.128ms (accepted validate=0.049ms) | unknown-hash-restore=0.474ms (accepted restore=0.052ms) | unassigned-outcome=0.168ms | cheap-nowMs-reject=56.88µs
axis 1 cheap-reject increment over restore: 5.35µs
axis 3 run 1 assign (µs): p50=57.7 p90=64.1 p99=98.7 max=1780.3 first10=79.2 last10=64.2 ramp=0.81x
axis 3 run 1 record (µs): p50=56.3 p90=62.6 p99=178.9 max=581.8 first10=68.4 last10=62.0 ramp=0.91x
axis 3 run 1 instrumented-experiment total=121.95ms (tail-cap max-op=1780.3µs)
axis 3 run 2 assign (µs): p50=57.1 p90=63.5 p99=74.9 max=328.5 first10=51.7 last10=64.6 ramp=1.25x
axis 3 run 2 record (µs): p50=55.5 p90=61.0 p99=172.2 max=379.6 first10=62.8 last10=62.1 ramp=0.99x
axis 3 run 2 instrumented-experiment total=116.65ms (tail-cap max-op=379.6µs)
axis 2 fresh-process 1: cold run-1=136.48ms run-2=131.09ms run-3=130.89ms steady-median(6..15)=118.46ms cold-delta=18.01ms
axis 2 fresh-process 2: cold run-1=135.20ms run-2=130.35ms run-3=128.63ms steady-median(6..15)=113.19ms cold-delta=22.01ms
---（第 2 次独立运行）---
axis 1 (reject paths, P=2000/A=1000): throw-floor=1.51µs/throw | dup-at-last-validate=0.158ms (accepted validate=0.049ms) | unknown-hash-restore=0.308ms (accepted restore=0.053ms) | unassigned-outcome=0.124ms | cheap-nowMs-reject=56.42µs
axis 1 cheap-reject increment over restore: 3.62µs
axis 3 run 1 assign (µs): p50=57.8 p90=63.9 p99=81.9 max=314.3 first10=51.6 last10=66.0 ramp=1.28x
axis 3 run 1 record (µs): p50=56.2 p90=61.8 p99=159.1 max=375.9 first10=53.1 last10=62.6 ramp=1.18x
axis 3 run 1 instrumented-experiment total=117.58ms (tail-cap max-op=375.9µs)
axis 3 run 2 assign (µs): p50=57.6 p90=63.6 p99=70.5 max=320.5 first10=52.6 last10=64.9 ramp=1.23x
axis 3 run 2 record (µs): p50=56.0 p90=61.3 p99=159.3 max=317.8 first10=51.2 last10=61.2 ramp=1.20x
axis 3 run 2 instrumented-experiment total=116.67ms (tail-cap max-op=320.5µs)
axis 2 fresh-process 1: cold run-1=134.26ms run-2=128.99ms run-3=127.37ms steady-median(6..15)=115.09ms cold-delta=19.18ms
axis 2 fresh-process 2: cold run-1=130.57ms run-2=126.05ms run-3=125.62ms steady-median(6..15)=113.73ms cold-delta=16.83ms
```
