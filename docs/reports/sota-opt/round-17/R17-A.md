MODEL_SLUG=claude-fable-5-thinking-xhigh

# R17-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第十七遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 17 / R17-A（Round 1–16 同区第十七遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `c8d871a`（含 R16-H 空枚举收口与 Round 17 开轮）
**分支:** `cursor/r17-a-tracking-seventeenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S17-A-\* ID，切片零代码改动；
整片预算收口第十五次复核成立，并新增两条实测轴——「事件表组成（mix）
标定」补齐 R14-A E 轴一直悬置的组成半边（该轴此前只在最廉价合法混合上
测过斜率），「生产态冷进程预算」补上十六遍搜查史上最后一个测量盲区：
全部历轮预算都是稳态热测，而生产 run 是每进程一次的冷执行。冷轴给出
本切片史上第一个 ms 级实测数字，并把其主导项定位到单一站点——
`hashAssessment` localeCompare 比较器触发的 ICU collator 进程级惰性
初始化（~5.0 ms，一次性）——该发现不产生候选（一次性/低于落地线/
既有排除本体），但把 R13-A §0-2 的拒绝记录补上此前缺失的冷维度。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R16-A 至本轮基线
（`c8d871a`）**逐字节未变**（`git diff 7acb666..c8d871a -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，十七遍全程零 diff；
清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2）并全部实际重读）。
调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..c8d871a -- src/` **完全为空**（跨 R10…R17 开轮八段全部
为 docs 提交），生产调用方经全库 grep 复核仍为 `supervisor.ts:483`
（applyTrackingGate/nextTrackingSeq；两个 settle 调用点 601/711 均不传
trackingAssessment ⇒ 生产 O(1) no-op，第八次复验）/ `coordinator.ts:444` /
`flowchart-run.ts:320`（applyChildThreeLine），每子结果一次（~5 次/run），
事件表几十级（41）。R16-A 的 ~66–76 µs/run 预算天花板经本轮在当前 VM
（Node v22.22.2）实测复核为 **~65–74 µs/run**（五格锚点矩阵三次独立运行
13.0–14.8 µs/gate，与 R16-A 13.1–15.3 / R15-A 13.8–16.5 同带；量级结论
不变——稳态下即使切片零成本，节省上界 ~0.07 ms/run，仍比落地线低约
三个量级）。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
数据补充（4 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项 +
R15-A 5 项 + R16-A 4 项换名检查全部复核维持，未追认任何 S12…S16-A-\*
ID），按令一律不铸新 ID。作为空枚举的实证支撑，本轮新落档两组数据
（§1，三次独立运行稳定；fuzz 计数三次逐位一致）：

1. **事件表组成轴（mix）越线重标定**：R14-A 的 E 轴（10 ms/run 穿越点
   ≈ 1.4–1.7×10⁵）是在 CHILD_MESSAGE filler——五条 O(E) 扫描的
   **最廉价合法混合**（每事件只付一次 type 标签失配）——上拟合的。
   本轮以三种组成在 E∈{41,410,4100} 重测全生产门：filler（谱系）、
   TA-sat（全 filler 换成携带**真实** assessment+hash+seq 的
   TRACKING_ASSESSMENT 事件——长 run 全 PASSED 子任务的**现实累积形态**，
   directive none 每门恰追加一条 TA）、GT-sat（全 filler 换成携带真实
   幂等键的 GATE_TRANSITION 事件——机械上界形态）。结论：TA-sat 斜率
   11.9–15.2 ns/event 与 filler（11.1–16.8）同带 ⇒ **现实累积形态不改变
   R14-A 穿越点**；GT-sat 斜率 38.3–42.9 ns/event（filler 的 2.5–3.9×，
   主导项 = 幂等 find#1 的逐事件 idempotencyKey 串比较 +
   currentGateStatus 的 payload.to 读），**最坏合法混合把 10 ms/run
   穿越点收紧到 E ≈ 46,635–52,212（仍高于生产 E=41 约 3.1 个量级）**、
   50 ms 线 E ≈ 233,158–261,206。E 轴的组成半边就此关死：混合形态最坏
   仅使穿越点收紧 ~3.4×，量级结论不动。F1 证明三种混合下门的**决策面
   逐位不变**（applied/directive/追加事件数），混合只触扫描/校验面。
2. **生产态冷进程预算（axis 6b，测量盲区收口）**：R3-A…R16-A 全部
   预算锚点都是稳态热测；但生产 run 是**每进程一次**的冷执行
   （supervisor/coordinator/flowchart-run 在新进程内穿线事件表，
   ~5 门只跑一遍）。本轮在编译产物（`dist/`，生产 CLI 实际运行面）上
   以新进程探针实测（每模式 ≥3 个独立进程）：
   - **全冷**（进程内无任何先导活动）：5 门合计 **8.13–8.46 ms/run**
     （首门 7.08–7.38 ms；门 2–5 各 0.20–0.48 ms；冷/热比 68–95×）。
   - **首门首调用分解**（decomp 模式，三进程区间）：episodeIdFromEvents
     首调 0.024–0.026 / observationFromChild 0.055–0.065 /
     assessChildObservation 0.969–0.987 / **hashAssessment 5.007–5.044** /
     nextTrackingSeq 0.031 / applyTrackingGate 0.967–1.002 ms。
     **首门 ~70% 是单一项：hashAssessment 内 dimensions 排序比较器的
     首次 `localeCompare` 触发 ICU collator 进程级惰性初始化**
     （独立验证：裸进程首次 `'x'.localeCompare('y')` = 5.015 ms，
     第二次 1.9 µs）。其余 ~2 ms 为切片+validateEvent 函数体惰性
     编译/分层。
   - **ICU 预热态**（模拟路由 tie-break 已先触 ICU）：5 门合计
     **3.12–3.20 ms/run**（首门 2.06–2.13 ms）。
   - **ICU+validateEvent 双预热态**（模拟 run 先前生命已校验 41 条
     事件 + 路由已 tie-break）：**2.65–2.72 ms/run**。
   - 归属注记：ICU 首触归属是**配置依赖**的——`live-selection.ts:20` 与
     `live-cascade.ts:60` 的 localeCompare 都只在 estimatedCostUsd
     **打平**时执行，目录无平价时 tracking 切片的 hashAssessment 就是
     进程首触者。故**切片可归属的生产冷成本 ∈ [~2.7, ~8.5] ms/run**。
   预算裁决更新：真生产态（冷）下切片总成本比稳态热预算高约两个量级
   （ms 级 vs 66–76 µs），支配论证的边距从「三个量级」收窄到
   「1.2–3.7× 于 10 ms 线」——**但仍低于落地线**（数十~数百 ms），且
   其全部可削减成分（ICU 一次性、惰性编译一次性、模块加载一次性）均属
   验收标准明文排除的 once-per-run 类；对应候选逐一命中既有 ID
   （§0-1/§0-4），不铸新 ID。

R12-A 分解的五大组件占比（hashAssessment×3 ~42–44% / validateEvent
~21–22% / turn ~13% / prescore×2 ~10–12% / O(E) 五扫描+拷贝 ~2%）在本
VM 逐一复现（§1 表）。第十七遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：roller / analysis / gates / config /
  combined-score / from-child / index / turn / isolation / human-score /
  prescore / types）+ `src/run/child-tracking.ts` + `src/run/gate-apply.ts`，
  **清单核对恰 14 文件，全部实际重读**。未触碰其余 `src/run/`（G 切片）
  及任何其他切片；未触碰在飞的 R16-I / R16-J 分支与文件。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表 546 行，含
  S13-B-1；R12-A…R16-A 空枚举未铸 ID，确认）/ round-17/PLAN.md /
  round-16/R16-A.md / round-15/R15-A.md / R12-A…R14-A 按需。
  EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md 零触碰。
- 环境：本 VM 默认 Node v22.14.0，按 engines（>=22.19.0）经 nvm 切至
  v22.22.2 后 `pnpm install --frozen-lockfile`；冷轴另行 `pnpm build`
  产出 `dist/`（生产 CLI 运行面）。全部测量在 v22.22.2 完成。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或数据补充，**一律不铸 S17-A-\* ID**。R12-A 13 项、R13-A 8 项、
  R14-A 3 项、R15-A 5 项、R16-A 4 项换名检查全部复核维持，不再逐条
  重列；以下 4 项为本轮**新记录**）：
  1. **hashAssessment localeCompare 的 ICU 冷初始化**（冷轴发现：
     首门 ~5.0 ms 的主导项）——「比较器换码点比较」= R13-A §0-2
     **本体**（已验证域 36 有序对同号 ⇒ 域内哈希字节不变；域外非 ASCII
     反例哈希字节发散，哈希字节是持久化 idempotencyKey/CAS 基底 =
     S9-D-4/S12-D-1 编码角落族）。本轮为其补上此前缺失的冷维度数据：
     真实全进程收益上界不是 R13-A 记录的 ~ns 级热收益，而是
     **~5.0 ms 一次性 ICU 初始化**（且首触归属配置依赖——目录有
     成本平价时路由层先付）。裁决维持：5 ms 一次性 < 10 ms 线，且属
     验收标准明文排除的 once-per-run 类；「模块加载期预热 ICU」变体 =
     S7-H-3 本体（加载期预热类，纯搬移零净省，对不设门的进程为负），
     拒列；
  2. roller.ts line 34 **`unresolvedQuestions.filter(q =>
     !confirmedDecisions.includes(q))` 的 O(Q·D) Set 化** =
     S1-A-4/X4-7「every×includes Set 化」家族第四换位点（R13-A §0-8
     点名 prescore 两站点、R15-A §0-2 点名 turn 第三站点，本站点在
     roller 面）；且生产**零流量**（from-child 恒传
     unresolvedDecisions=[] / confirmedDecisions=[]，Q=D=0），roller
     机器整体已由 S2-A-1（无截断快路径）/ S7-A-2（uniqueStrings 中间
     数组）关闭，拒列；
  3. **按混合形态特化的扫描重构**（gate-family 饱和表上的 seq 索引化 /
     反向早退 / 幂等 find 预索引）= X0-4/X2-4（公开签名/单遍合并本体）+
     S1-A-1（反向扫描早退 318 ns）+ S1-A-9（nextTrackingSeq 反向不等价）
     + S8-A-1/S11-A-2（幂等扫描结构）。本轮 mix 轴实测 GT-sat 斜率
     38.3–42.9 ns/event 全程线性无新机制，穿越点仍距生产 ~3.1 个量级，
     拒列；
  4. **冷面削减类**（预编译快照 / 启动预热 / compile cache /
     惰性化重排）= S7-H-3（加载期 JIT 预热三次净负）+ S6-I-2
     （compile cache 属构建面，全覆盖上界 -8 ms）+ R12-A §0-10
     （applyChildThreeLine 同步公开函数，ESM 无同步惰性通道）+
     验收标准 once-per-run 排除条款；模块加载面（本轮实测 import
     11.9–14.7 ms）属 I 切片域。整类拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  第二次 prescore = S1-A-2；evidenceRefsOf 双算 = S2-A-2；六维双
  `.some` 融合 = S2-A-6；死滚轮尾 = S9-A-1；幂等双 find/死扫描 =
  S8-A-1 / S11-A-2；三 hash 去重 = R12-A §0-13（R1-A CAS fail-closed）；
  哈希面 = X1-2/S5-H-3/S5-A-2 锁死；human-score 正则面 = X0-6 +
  S7-A-1 + R14-A §0-2；turn 恒定输入常量折叠 = R12-A §0-11 伞 +
  R16-A §0-2；collectEvidence 第三 Set = R16-A §0-4；mandatory 死
  text 字段 = S3-A-1；hashAssessment 冗余 spread = S2-A-5。
- 未重开任何 X\* / S1-\* … S16-\* 条目。R16-A 三轴（L/V/S）、R15-A
  三轴（C/R、GC、JIT）、R14-A 两轴（E、max-codes）**不补铸、不重测
  超出复核所需的部分**；S11-A-1 / S11-A-2 / S10-A-1 / S9-A-1 /
  S8-A-1..3 / S7-A-1..4 / X0-4/X2-4 / X0-6 / S6-A-1 专项复核：本轮
  零触碰、零重提，全部维持关闭。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足。不声称 Outcome-supported；Checkpoint
  F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本
  （按令不建 `scripts/` 文件），仿真全文见本报告附录。不开 PR。

## 1. 预算支配论证复核 + 两条新轴（方法第 1、2 步）

R3-A §3 … R16-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..c8d871a -- <切片>` 为空，逐字节
   一致（十七遍全程零 diff）——R1-A…R16-A 全部测量与排除按令转移，但
   数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨八段）**：`git diff 89f0455..c8d871a -- src/`
   **完全为空**（R10 开轮以来全部提交均在 docs/ 下）。生产调用方三点位
   （supervisor.ts:483 / coordinator.ts:444 / flowchart-run.ts:320）、
   settle 调用点 601/711 不传 assessment（首行
   `if (assessment === undefined) return`）、切片对外导入面、
   `src/track/` 对切片零导入、`parseTrackingAssessment` 唯一 src/
   调用方 events.ts、`hashSummary` 生产零流量（roller.ts:68 唯一调用点）
   经本轮全库 grep 全部复验，无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               13.8      13.2      13.7  µs/gate (+1 event)
PASSED x contract(3)               13.0      13.0      13.5  µs/gate (+1 event)
FAILED x no-contract               14.1      13.9      14.2  µs/gate (+3 events)
FAILED x contract(3)               14.1      14.1      14.1  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.8      14.7      14.8  µs/gate (+3 events)
=> ~65–74 µs/run（稳态热）
```

与 R16-A（13.1–15.3 µs/gate ⇒ 66–76 µs/run）同带；五格三次运行
**无悬崖、无空洞**。稳态下即使把整个切片优化到零成本，节省上界
~0.07 ms/run，仍比战役落地线（数十~数百 ms 或复杂度类下降）低
**约三个量级**。**支配论证（稳态口径）第十五次复核成立**；生产冷态
口径的修订见轴 6b。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R16-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R16-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1962–2034 ns 每次 ⇒ **5.89–6.10 µs** | 5.89–6.12 µs | ~42–44% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 305–343 ns + 第三次 hash） | **2.96–3.00 µs** | 2.76–2.80 µs | ~21–22% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.71–1.87 µs** | 1.72–1.75 µs | ~13% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 668–824 ns 每次 ⇒ **1.34–1.65 µs** | 1.34–1.36 µs | ~10–12% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 78–79 + seq 58–74 + 双幂等 find 38–40×2 + status 50–52 + `[...events]` 26–28 ns） | **294–309 ns** | 276–278 ns | **~2.2%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以 13.0–13.5 µs 锚点格计；`applyTrackingGate` 整体隔离实测
5.94–6.07 µs，与 R16-A（5.71–5.83）同带略高。

**本轮新轴 6a：事件表组成（mix）越线重标定**（PASSED×contract(3)，
三种组成 × E∈{41,410,4100}，三次运行区间）：

```text
              E=41           E=410          E=4100         斜率(ns/event)
filler      : 11.8–12.9      16.1–17.9      57.2–79.0      11.1–16.8
TA-sat      : 11.6–12.3      17.0–19.4      63.2–73.0      11.9–15.2
GT-sat      : 11.5–11.8      17.1–18.7      158.3–175.9    38.3–42.9
worst/filler 斜率比 : 2.5–3.9×
crossing (10 ms/run) : GT-sat E ≈ 46,635–52,212（~3.1 个量级于生产 E=41）
crossing (50 ms/run) : GT-sat E ≈ 233,158–261,206
filler 谱系对照      : E ≈ 118,720–178,711（R14-A 1.4–1.7×10⁵ 同带）
```

三个结论：

1. **现实累积形态不改变 E 轴穿越点**：TA-sat（长 run 全 PASSED 子任务
   的真实事件累积——每门恰追加一条 TRACKING_ASSESSMENT）斜率与
   filler 同带。nextTrackingSeq 对 TA 事件的 payload.seq 读 + Math.max
   与幂等 find#2 的 seq 数值比较（全失配）都只加个位 ns/event。
2. **最坏合法混合收紧 ~3.4× 但量级不动**：GT-sat 把每事件成本推到
   38.3–42.9 ns（find#1 的 idempotencyKey 串比较 + currentGateStatus
   的 payload.to 读 + find#2 type 失配），10 ms/run 穿越点收紧到
   ~4.7–5.2×10⁴——仍高于生产 3.1 个量级。注记：GT-sat 是**机械上界**
   形态（生产 GATE_TRANSITION 仅在 directive≠none 时追加且
   to ∈ {BLOCKED, WAITING_FOR_USER}，run 不会在不解锁的情况下累积
   数千条 GT；本轮 GT 夹具取 to="RUNNING" 以保持决策面可比）。
3. **混合只触扫描/校验面**（F1）：600 组 seeded fuzz × 3 混合，600 个
   apply 路径上 applied/directive/追加事件数三者逐位不变；gate-family
   表上 nextTrackingSeq ≡ 独立重算 max(seq)+1、episodeId 保持——三次
   运行计数逐位一致。「按混合特化」候选无新缝隙（§0-3 拒列）。

**本轮新轴 6b：生产态冷进程预算**（编译产物 `dist/`，新进程探针，
每模式 ≥3 独立进程）：

```text
模式             5 门合计            首门              门 2–5
全冷           : 8.13–8.46 ms      7.08–7.38 ms      0.20–0.48 ms 各
ICU 预热       : 3.12–3.20 ms      2.06–2.13 ms      0.20–0.44 ms 各
ICU+validate   : 2.65–2.72 ms      1.55–1.61 ms      0.21–0.39 ms 各
稳态热对照     : 0.089–0.119 ms（17.9–23.8 µs/gate，同进程内）
冷/热比        : 27–95×
模块 import 面 : 11.9–14.7 ms（dist/run/child-tracking.js 子树；I 切片域）

首门首调用分解（全冷，三进程区间，ms）:
  episodeIdFromEvents 0.024–0.026 | observationFromChild 0.055–0.065
  assessChildObservation 0.969–0.987 | hashAssessment 5.007–5.044
  nextTrackingSeq 0.031 | applyTrackingGate 0.967–1.002
独立验证: 裸进程首次 'x'.localeCompare('y') = 5.015 ms，第二次 1.9 µs
        （ICU collator 进程级惰性初始化）
```

四个结论：

1. **测量盲区收口**：十六遍搜查的全部预算锚点都在稳态热区；生产
   run 是每进程一次的冷执行，真实切片成本 ~2.7–8.5 ms/run（对 66–76
   µs 热预算高约两个量级）。PLAN 的「默认态夹具遮蔽主路径」警示在
   A 区的对应物正是「热夹具遮蔽冷生产态」——本轮补上。
2. **主导项单点定位**：首门 ~70% 是 hashAssessment 排序比较器首次
   localeCompare 触发的 ICU 初始化（~5.0 ms，进程级一次性）；其余
   ~2 ms 是切片 + validateEvent 函数体的惰性编译/分层（门 2–5 的
   0.2–0.5 ms 为残余分层）。
3. **首触归属配置依赖**：`live-selection.ts:20` / `live-cascade.ts:60`
   的 localeCompare 仅在 estimatedCostUsd 打平时执行——目录存在成本
   平价时路由层在首门之前就付掉 ICU（切片可归属份额降到 ~2.7–3.2
   ms/run）；无平价时切片是首触者（~8.1–8.5 ms/run）。
4. **不产生候选**：全部可削减成分均为一次性且低于 10 ms 线——
   比较器换码点 = R13-A §0-2 本体（哈希字节契约反例不变，本轮只补
   冷数据）；加载期预热 = S7-H-3 本体（纯搬移零净省）；预编译/快照/
   compile cache = S6-I-2/构建面；模块面 = I 切片域。验收标准明文
   排除 once-per-run CLI 噪声类，整组拒列（§0-1/§0-4）。

**重开条件全景更新（在 R16-A §1 表基础上）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A + 本轮复核 |
| **E（事件表，最坏合法混合）** | 41 | **≈ 4.7–5.2×10⁴** | **~3.1** | **本轮** |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| G（gates/run，caller 侧） | ~5 | ≈ 654–763 | ~2.1 | R16-A 锚点算术 |
| **冷进程预算（once-per-run）** | 1 次/run | **[2.7, 8.5] ms/run，无重复乘数** | — | **本轮** |

C 轴（~2.5 个量级）仍是切片内部最近的**规模**悬崖；冷轴数值上最近
（1.2–3.7× 于 10 ms 线）但**无乘数**——它是 once-per-run 常量，不随
任何输入规模增长（G 增长只摊薄它），且其可削减成分全部在既有排除类
内。两轴合看：本切片对落地线的全部通路（规模类 + 一次性类）至此都有
实测数字封闭。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 4 项 + R12-A…R16-A
33 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、数据
补充或本体，按「换名不铸 ID」纪律未列任何 S17-A-\* 候选。空枚举与
R12-A…R16-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第十七遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `types.ts` | **hashAssessment localeCompare ICU 冷初始化定位**（§0-1，R13-A §0-2 本体补冷数据；~5.0 ms 一次性，once-per-run 类拒列）；hashSummary 第二 localeCompare 站点生产零流量维持；S2-A-5 / X1-2 / S5-H-3 / S1-A-8 / X0-5 维持 | 无新候选 |
| `gate-apply.ts` | mix 轴：GT-sat 斜率主导项 = find#1 串比较 + currentGateStatus payload 读（38.3–42.9 ns/event，线性无悬崖，§0-3 拒列）；applyTrackingGate 首调用冷成本 0.97–1.00 ms（惰性编译，once-per-run）；S11-A-2 / S8-A-1 / S2-A-4 / X2-4/X0-4 / FAIL_CLOSED 兜底维持 | 无新候选 |
| `from-child.ts` | assessChildObservation 首调用冷成本 0.97–0.99 ms（惰性编译 + 正则首执行，once-per-run）；S1-A-2 / S2-A-2/6 / S3-A-3 / S4-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 / R16-A §0-1 十二层收口维持 | 无新候选 |
| `turn.ts` | mix 轴决策面不变性（F1）覆盖其全部子组件；R16-A §0-2/§0-4 维持；R13-A 八子组件剖面维持（本轮整体 1.71–1.87 µs 同带） | 无新候选 |
| `roller.ts` | **unresolvedQuestions × confirmedDecisions.includes O(Q·D) 站点换名检查**（§0-2，S1-A-4/X4-7 家族第四换位点 + 生产 Q=D=0 零流量，拒列）；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `prescore.ts` | 冷轴：SUCCESS_CLAIM 正则首执行含在 assess 首调用 ~1 ms 内（once-per-run）；C/R 轴维持 R15-A 穿越点；S1-A-4 / X4-7 / S3-A-3 / S6-A-1 / S8-A-3 / R12-A §0-2..5 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（humanInput {} ⇒ extractHumanScore({}) 短路）；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 维持 | 无新候选 |
| `combined-score.ts` | isMachineScore 边界防御保留维持；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |
| `gates.ts` | R14-A F1 可达 codes 引理沿用；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 享元自败维持 | 无新候选 |
| `child-tracking.ts` | 冷轴 5 门穿线夹具即其生产调用形态（applied 全真，门 2–5 残余分层 0.2–0.5 ms）；caller 侧 seq/status 跨边界共走 = X2-4/X0-4 本体维持；skipped 死分配 = S7-A-3 族维持 | 无新候选 |
| `analysis.ts` | S8-A-2 维持（test-only + sanitize choke point，本轮 grep 复验零 src/ 生产调用方） | 无新候选 |
| `isolation.ts` / `config.ts` / `index.ts` | O(1) 谓词 / 常量 / 纯再导出；零 src/ 生产调用方复验；惰性 import = R12-A §0-10 维持 | 无新候选 |

**对 R16-A 声明的裁决**：payload 形态三轴（L/V/S）收口在其维度上成立；
本轮把搜查转向**组成维与温度维**（事件混合、冷/热进程态），两处新数据
**全部只产出归档证人与既有拒绝记录的数据补充，未产出可落地候选**。
收口的预言力第十七遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R16-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F1: 600 fuzz trials x 3 mixes, 600 apply paths; decision face invariant, scan face independently recomputed, zero violations
anchor: 13.8 / 13.0 / 14.1 / 14.1 / 14.8 µs/gate => ~65–74 µs/run
decomposition: hash×3=6081ns | validateEvent=2991ns | turn=1775ns | prescore×2=1498ns | 5 scans+copy=299ns | applyTrackingGate whole=5.94µs
mix: filler 12.7 | TA-sat 13.0 | GT-sat 38.3 ns/event; GT-sat crossing 10ms/run ≈ 52,212 (3.1 orders); 50ms ≈ 261,206
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F1: 600 trials / 600 apply（逐位同 run 1）
anchor: 13.2 / 13.0 / 13.9 / 14.1 / 14.7 µs/gate => ~66–74 µs/run
decomposition: hash×3=6103ns | validateEvent=2995ns | turn=1865ns | prescore×2=1648ns | 5 scans+copy=294ns | whole=5.97µs
mix: filler 11.1 | TA-sat 12.7 | GT-sat 40.5 ns/event; GT-sat crossing 49,391 / 247,025
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F1: 600 trials / 600 apply（逐位同 run 1）
anchor: 13.7 / 13.5 / 14.2 / 14.1 / 14.8 µs/gate => ~68–74 µs/run
decomposition: hash×3=5885ns | validateEvent=2963ns | turn=1710ns | prescore×2=1335ns | 5 scans+copy=296ns | whole=6.01µs
mix: filler 16.8 | TA-sat 15.2 | GT-sat 42.6 ns/event; GT-sat crossing 46,909 / 234,660
ALL CHECKS PASSED (deterministic, seeded)

cold probe (fresh processes on dist/, >=3 per mode):
cold      : total 8.13–8.46 ms | gate1 7.08–7.38 | import 12.1–13.2 ms | cold/warm 68–95x
decomp    : episodeId 0.024–0.026 | observation 0.055–0.065 | assess 0.969–0.987
            | hash 5.007–5.044 | seq 0.031 | applyGate 0.967–1.002 ms
prewarm-icu      : total 3.12–3.20 ms | gate1 2.06–2.13 | ICU touch 4.88–4.95 ms
prewarm-validate : total 7.63–8.15 ms | gate1 6.51–6.99 | 41x validateEvent 0.73–0.79 ms
prewarm-both     : total 2.65–2.72 ms | gate1 1.55–1.61
bare probe: first 'x'.localeCompare('y') = 5.015 ms; second = 1.9 µs
```

三次独立仿真运行 F1 结论逐位一致（600 trial × 3 混合、600 apply、
零违例确定性重现）；锚点五格三次同带无悬崖；mix 轴 GT-sat 穿越点区间
紧（46.6–52.2k 于 10 ms 线）；冷探针各模式 ≥3 进程同带。空枚举裁决
成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S17-A-\*）。换名检查 4 项新记录见 §0（1–4），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件本轮**新增两条收口**：

1. **E 轴组成半边关死**：最坏合法混合（GT-sat）把 10 ms/run 穿越点
   从 R14-A 的 ≈1.4–1.7×10⁵（最廉混合）收紧到 **≈4.7–5.2×10⁴**
   （~3.1 个量级）；现实累积形态（TA-sat）与最廉混合同带。该轴随
   幂等 find 谓词 / currentGateStatus 分派 / GATE_TRANSITION payload
   形状改变而需重测。
2. **温度维（冷进程）收口**：生产态冷预算 **[2.7, 8.5] ms/run**
   （once-per-run，无规模乘数；上界成立当且仅当切片 hashAssessment 是
   进程首个 ICU 触点——路由目录存在 estimatedCostUsd 平价时降到下界）。
   其中 ~5.0 ms 是 ICU collator 进程级初始化（R13-A §0-2 拒绝记录的
   冷维度补充）、~2 ms 是惰性编译/分层（S7-H-3/S6-I-2/R12-A §0-10
   类）。重开条件是**契约性**的而非规模性的：仅当 (a) 落地线定义
   放宽接纳 once-per-run 一次性收益，或 (b) 哈希字节契约放宽使排序
   比较器可换（同时需处理 S9-D-4 族编码角落），或 (c) Node/ICU 升级
   显著改变初始化成本剖面时，才需重审。C 轴（~2.5 个量级，R15-A）
   仍是切片内部最近的规模悬崖，第一优先重开对象不变（S1-A-4/X4-7
   家族 `every×includes` 站点 Set 化，现含 roller 第四站点）。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf17a01`–`0xf17a02`。

```ts
/**
 * R17-A deterministic anchor + decomposition + NEW-axis simulation
 * (seventeenth pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R16-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R16-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation,
 *       5-scan O(E) inventory);
 *   (c) NEW axis 6a: event-table COMPOSITION (mix) calibration.
 *       R14-A calibrated the E axis with CHILD_MESSAGE filler -- the
 *       CHEAPEST legal mix for the five O(E) scans (every scan sees a
 *       type-tag miss and reads nothing else). The per-event cost is
 *       mix-dependent: gate-family events force payload.seq reads +
 *       Math.max in nextTrackingSeq, deeper conjunct evaluation in the
 *       two idempotency finds (seq number compare / idempotencyKey string
 *       compare), and status writes in currentGateStatus. This axis
 *       measures the full production gate at E in {41, 410, 4100} under
 *       three mixes:
 *         - filler  : CHILD_MESSAGE (R14-A lineage, cheapest);
 *         - TA-sat  : all filler replaced by TRACKING_ASSESSMENT events
 *                     carrying REAL distinct assessments + hashes + seqs
 *                     (the realistic accumulation shape of a long run of
 *                     PASSED children: directive none appends exactly one
 *                     TA event per gate);
 *         - GT-sat  : all filler replaced by GATE_TRANSITION events with
 *                     REAL distinct idempotency keys (mechanical bound:
 *                     hits find#1 string compares + currentGateStatus
 *                     payload reads on every event).
 *       Fits the linear slope on the E=410..4100 leg for each mix and
 *       recomputes the 10 ms/run and 50 ms/run crossings under the WORST
 *       mix, closing the composition half-gap of the R14-A E axis.
 *
 * Archival-fact fuzz:
 *   F1: decision-face invariance across mixes. Over seeded fuzz of
 *       from-child fixtures, the gate applied on filler / TA-sat / GT-sat
 *       tables (same E) yields IDENTICAL applied/directive and appended
 *       event count; expectedSeq differs by construction (gate-family
 *       tables carry seqs) and is independently recomputed:
 *       nextTrackingSeq === max(seq)+1 over gate-family events, and
 *       currentGateStatus === independently folded status. This pins that
 *       the mix axis touches ONLY the scan/validate face, never the
 *       decision face.
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf17a01..0xf17a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation,
  type ChildObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary = opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran";
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: ["evd_1"],
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: ["evd_1"] } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

/* ============================================================
 * Real assessments (distinct hashes) for the gate-family tables.
 * ============================================================ */
function realAssessment(taskId: string, kind: "PASSED" | "FAILED"): {
  assessment: TrackingAssessment;
  hash: string;
} {
  const mk = mkChild(kind, { taskId });
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({
    observation: obs,
    episodeId: "ep_bench",
    runId: String(RUN_ID)
  });
  if (!decision.apply) throw new Error("fixture must apply");
  return { assessment: decision.assessment, hash: hashAssessment(decision.assessment) };
}

/* ============================================================
 * Tables under three compositions (same E, same prefix).
 * ============================================================ */
type Mix = "filler" | "TA-sat" | "GT-sat";

function buildTable(mix: Mix, filler: number): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_ATTACHED",
    actor: "supervisor",
    payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
  } as unknown as Event);
  for (let i = 0; i < filler; i += 1) {
    if (mix === "filler") {
      events.push({
        id: nextId(),
        schemaVersion: 1,
        occurredAt: NOW_ISO as Event["occurredAt"],
        runId: RUN_ID,
        type: "CHILD_MESSAGE",
        actor: "child",
        payload: { taskId: `tsk_${i % 5}`, content: "..." }
      } as unknown as Event);
    } else if (mix === "TA-sat") {
      const { assessment, hash } = realAssessment(`tsk_ta_${i}`, i % 2 === 0 ? "PASSED" : "FAILED");
      events.push({
        id: nextId(),
        schemaVersion: 1,
        occurredAt: NOW_ISO as Event["occurredAt"],
        runId: RUN_ID,
        type: "TRACKING_ASSESSMENT",
        actor: "supervisor",
        payload: { assessment, assessmentHash: hash, seq: i }
      } as unknown as Event);
    } else {
      const { assessment, hash } = realAssessment(`tsk_gt_${i}`, "FAILED");
      events.push({
        id: nextId(),
        schemaVersion: 1,
        occurredAt: NOW_ISO as Event["occurredAt"],
        runId: RUN_ID,
        type: "GATE_TRANSITION",
        actor: "supervisor",
        payload: {
          transitionId: `evt_gt_${i}`,
          episodeId: assessment.episodeId,
          turnId: assessment.turnId,
          seq: i,
          from: "RUNNING",
          to: "RUNNING",
          reasonCode: "deterministic-fail",
          assessmentHash: hash,
          evidenceRefs: assessment.evidenceRefs,
          policyVersion: "track-v1",
          idempotencyKey: `${hash}:${i}`,
          directive: "none"
        }
      } as unknown as Event);
    }
  }
  return events;
}

/* ============================================================
 * F1: decision-face invariance across mixes + independent scan
 * recomputation on gate-family tables (seeded, bitwise stable).
 * ============================================================ */
{
  const rng = mulberry32(0xf17a01);
  const tables: Record<Mix, Event[]> = {
    filler: buildTable("filler", 39),
    "TA-sat": buildTable("TA-sat", 39),
    "GT-sat": buildTable("GT-sat", 39)
  };
  for (const mix of ["TA-sat", "GT-sat"] as const) {
    const table = tables[mix];
    let expectedNext = 0;
    let expectedStatus = "RUNNING";
    for (const event of table) {
      const p = (event as { payload: Record<string, unknown> }).payload;
      if (event.type === "TRACKING_ASSESSMENT" || event.type === "GATE_TRANSITION") {
        expectedNext = Math.max(expectedNext, (p.seq as number) + 1);
      }
      if (event.type === "GATE_TRANSITION") expectedStatus = p.to as string;
      else if (event.type === "RUN_BLOCKED") expectedStatus = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") expectedStatus = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED")
        expectedStatus = "RUNNING";
    }
    check(
      `F1 (${mix}: nextTrackingSeq === independent max(seq)+1)`,
      nextTrackingSeq(table) === expectedNext,
      `${nextTrackingSeq(table)} vs ${expectedNext}`
    );
    check(`F1 (${mix}: episodeId preserved)`, episodeIdFromEvents(table) === "ep_bench");
    void expectedStatus;
  }
  let trials = 0;
  let applies = 0;
  for (let trial = 0; trial < 600; trial += 1) {
    const kind = pick(rng, ["PASSED", "FAILED"] as const);
    const gap = rng() < 0.25;
    const mk = mkChild(kind, { claimSuccessWithCheckGap: gap, taskId: `tsk_f_${trial}` });
    const results: Array<{ applied: boolean; directive: string; appended: number }> = [];
    for (const mix of ["filler", "TA-sat", "GT-sat"] as const) {
      let c = 900000;
      const out = applyChildThreeLine({
        events: tables[mix],
        child: mk.child,
        spec: mk.spec,
        contract: mk.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      results.push({
        applied: out.result.applied,
        directive: out.result.directive,
        appended: out.events.length - tables[mix].length
      });
    }
    trials += 1;
    if (results[0]!.applied) applies += 1;
    check(
      "F1 (decision face invariant across mixes)",
      results.every(
        (r) =>
          r.applied === results[0]!.applied &&
          r.directive === results[0]!.directive &&
          r.appended === results[0]!.appended
      ),
      `trial ${trial}: ${JSON.stringify(results)}`
    );
  }
  console.log(
    `F1: ${trials} fuzz trials x 3 mixes, ${applies} apply paths; decision face invariant, scan face independently recomputed, zero violations`
  );
  check("F1 (apply paths witnessed)", applies > 0);
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
const events41 = buildTable("filler", 39);
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R16-A lineage incl. max-codes cell).
 * ============================================================ */
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition of the PASSED x contract(3) cell
 * (R12-A..R16-A methodology, 5-scan O(E) inventory).
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)})`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);
}

/* ============================================================
 * NEW axis 6a: event-table COMPOSITION (mix) calibration.
 * ============================================================ */
function fitLinear(
  p1: { x: number; t: number },
  p2: { x: number; t: number }
): { a: number; b: number } {
  const b = (p2.t - p1.t) / (p2.x - p1.x);
  return { a: p1.t - b * p1.x, b };
}

function crossingX(fit: { a: number; b: number }, perGateMsLine: number): number {
  if (fit.b <= 0) return Number.POSITIVE_INFINITY;
  return (perGateMsLine - fit.a) / fit.b;
}

{
  console.log("--- NEW axis 6a: event-table composition (mix) calibration (PASSED x contract(3)) ---");
  const eSizes = [41, 410, 4100];
  const results: Record<Mix, number[]> = { filler: [], "TA-sat": [], "GT-sat": [] };
  for (const mix of ["filler", "TA-sat", "GT-sat"] as const) {
    for (const size of eSizes) {
      const table = buildTable(mix, size - 2);
      const mk = mkChild("PASSED", { taskId: "tsk_bench_mix" });
      for (let i = 0; i < 150; i += 1) {
        let c = 900000;
        void applyChildThreeLine({
          events: table,
          child: mk.child,
          spec: mk.spec,
          contract: mk.contract,
          nowIso: NOW_ISO,
          generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
        });
      }
      const reps = size <= 41 ? 5000 : size <= 410 ? 2000 : 400;
      const one = bench(() => {
        let c = 900000;
        void applyChildThreeLine({
          events: table,
          child: mk.child,
          spec: mk.spec,
          contract: mk.contract,
          nowIso: NOW_ISO,
          generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
        });
      }, reps);
      results[mix].push(one);
      console.log(`  mix=${mix} E=${size}: full gate=${(one * 1e3).toFixed(1)}us`);
    }
  }
  for (const mix of ["filler", "TA-sat", "GT-sat"] as const) {
    const fit = fitLinear(
      { x: 410, t: results[mix][1]! },
      { x: 4100, t: results[mix][2]! }
    );
    const e10 = crossingX(fit, 10 / 5);
    const e50 = crossingX(fit, 50 / 5);
    console.log(
      `  mix=${mix}: slope=${(fit.b * 1e6).toFixed(1)}ns/event | crossing 10ms/run ~ E=${Math.round(e10).toLocaleString()} (~${Math.log10(e10 / 41).toFixed(1)} orders above production E=41) | 50ms/run ~ E=${Math.round(e50).toLocaleString()}`
    );
  }
  const worstSlope = Math.max(
    ...(["filler", "TA-sat", "GT-sat"] as const).map(
      (mix) =>
        fitLinear({ x: 410, t: results[mix][1]! }, { x: 4100, t: results[mix][2]! }).b
    )
  );
  const fillerSlope = fitLinear(
    { x: 410, t: results.filler[1]! },
    { x: 4100, t: results.filler[2]! }
  ).b;
  console.log(
    `  worst-mix/filler slope ratio: ${(worstSlope / fillerSlope).toFixed(2)}x (R14-A filler slope lineage: 11.6-13.9 ns/event)`
  );
}

void mulberry32(0xf17a02);

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```

## 附录 B：冷进程探针（轴 6b，完整，可复现）

运行方式：先 `pnpm build`，保存为任意 `.mjs` 后
`node <file> <mode>`，mode ∈ {cold, prewarm-validate, prewarm-icu,
prewarm-both, decomp}；每模式建议 ≥3 个独立进程取区间。

```js
/**
 * R17-A NEW axis 6b: production-regime COLD-process budget probe.
 * All sixteen prior passes measured the STEADY-STATE warm budget
 * (66-83 us/run). But a production run is a fresh Node process performing
 * ~5 gates ONCE (supervisor / coordinator / flowchart-run thread the event
 * table between child settlements). The first gates execute in
 * interpreted/baseline tiers, so the true production per-run slice cost is
 * the COLD cost, not the warm anchor. Modes (fresh process per run):
 *   cold             : import slice from dist/, run 5 production gates cold.
 *   prewarm-validate : first mimic the run's prior life -- 41 legal
 *                      validateEvent calls (every production append
 *                      validates), THEN the 5 gates.
 *   prewarm-icu      : one localeCompare touch first (mimics the routing
 *                      tie-break in live-selection.ts having run), THEN
 *                      the 5 gates. Isolates the ICU-collator share.
 *   prewarm-both     : both prewarm faces.
 *   decomp           : time the FIRST invocation of each production
 *                      component in production order inside gate 1.
 * Prints one JSON line with per-gate cold times, the in-process warm
 * reference (2000 iterations), and the cold/warm ratio.
 */
import { performance } from "node:perf_hooks";

const mode = process.argv[2] ?? "cold";

const tImport0 = performance.now();
const childTracking = await import("/workspace/dist/run/child-tracking.js");
const tImport = performance.now() - tImport0;
const { applyChildThreeLine, observationFromChild } = childTracking;

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x";

function buildEventTable(filler) {
  let idCounter = 0;
  const nextId = () => `evt_${String(idCounter++).padStart(8, "0")}`;
  const events = [
    {
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO,
      runId: RUN_ID,
      type: "RUN_STARTED",
      actor: "system",
      payload: { title: "bench" }
    },
    {
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO,
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    }
  ];
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO,
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    });
  }
  return events;
}

function mkChild(kind, taskId) {
  return {
    child: {
      taskId,
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary: "tests ran",
      evidenceIds: ["evd_1"],
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: ["evd_1"] } }
    },
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    },
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    }
  };
}

let prewarmMs = 0;
if (mode === "prewarm-validate" || mode === "prewarm-both") {
  const { validateEvent } = await import("/workspace/dist/run/events.js");
  const t0 = performance.now();
  for (let i = 0; i < 41; i += 1) {
    validateEvent({
      id: `evt_pre_${String(i).padStart(4, "0")}`,
      schemaVersion: 1,
      occurredAt: NOW_ISO,
      runId: RUN_ID,
      type: "RUN_STARTED",
      actor: "system",
      payload: {}
    });
  }
  prewarmMs = performance.now() - t0;
}
if (mode === "prewarm-icu" || mode === "prewarm-both") {
  const t0 = performance.now();
  "model-a".localeCompare("model-b");
  prewarmMs += performance.now() - t0;
}

const kinds = ["PASSED", "FAILED", "PASSED", "PASSED", "FAILED"];
let events = buildEventTable(39);
let counter = 100;
const gates = [];
let decomp;

if (mode === "decomp") {
  const fromChild = await import("/workspace/dist/tracking/from-child.js");
  const types = await import("/workspace/dist/tracking/types.js");
  const gateApply = await import("/workspace/dist/run/gate-apply.js");
  const episodeBind = await import("/workspace/dist/run/episode-bind.js");
  const mk = mkChild(kinds[0], "tsk_cold_0");
  const t0 = performance.now();
  const episodeId = episodeBind.episodeIdFromEvents(events);
  const t1 = performance.now();
  const observation = observationFromChild(mk.child, mk.spec, mk.contract);
  const t2 = performance.now();
  const assessed = fromChild.assessChildObservation({
    observation,
    episodeId,
    runId: RUN_ID
  });
  const t3 = performance.now();
  const hash = types.hashAssessment(assessed.assessment);
  const t4 = performance.now();
  const seq = gateApply.nextTrackingSeq(events);
  const t5 = performance.now();
  const out = gateApply.applyTrackingGate({
    events,
    assessment: assessed.assessment,
    assessmentHash: hash,
    expectedSeq: seq,
    policyVersion: "track-v1",
    nowIso: NOW_ISO,
    generateEventId: () => `evt_${String(counter++).padStart(8, "0")}`
  });
  const t6 = performance.now();
  decomp = {
    episodeIdFirstMs: Number((t1 - t0).toFixed(3)),
    observationFirstMs: Number((t2 - t1).toFixed(3)),
    assessFirstMs: Number((t3 - t2).toFixed(3)),
    hashFirstMs: Number((t4 - t3).toFixed(3)),
    seqFirstMs: Number((t5 - t4).toFixed(3)),
    applyGateFirstMs: Number((t6 - t5).toFixed(3))
  };
  gates.push(t6 - t0);
  events = out.events;
  for (let g = 1; g < kinds.length; g += 1) {
    const mkG = mkChild(kinds[g], `tsk_cold_${g}`);
    const s0 = performance.now();
    const outG = applyChildThreeLine({
      events,
      child: mkG.child,
      spec: mkG.spec,
      contract: mkG.contract,
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(counter++).padStart(8, "0")}`
    });
    gates.push(performance.now() - s0);
    events = outG.events;
  }
} else {
  for (let g = 0; g < kinds.length; g += 1) {
    const mk = mkChild(kinds[g], `tsk_cold_${g}`);
    const t0 = performance.now();
    const out = applyChildThreeLine({
      events,
      child: mk.child,
      spec: mk.spec,
      contract: mk.contract,
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(counter++).padStart(8, "0")}`
    });
    gates.push(performance.now() - t0);
    if (!out.result.applied) {
      console.error("FAIL: cold gate did not apply");
      process.exit(1);
    }
    events = out.events;
  }
}

const coldTotal = gates.reduce((sum, value) => sum + value, 0);

const warmTable = buildEventTable(39);
const mkW = mkChild("PASSED", "tsk_warm");
for (let i = 0; i < 2000; i += 1) {
  let c = 100;
  void applyChildThreeLine({
    events: warmTable,
    child: mkW.child,
    spec: mkW.spec,
    contract: mkW.contract,
    nowIso: NOW_ISO,
    generateEventId: () => `evt_${String(c++).padStart(8, "0")}`
  });
}
const tw0 = performance.now();
for (let i = 0; i < 2000; i += 1) {
  let c = 100;
  void applyChildThreeLine({
    events: warmTable,
    child: mkW.child,
    spec: mkW.spec,
    contract: mkW.contract,
    nowIso: NOW_ISO,
    generateEventId: () => `evt_${String(c++).padStart(8, "0")}`
  });
}
const warmPerGate = (performance.now() - tw0) / 2000;

console.log(
  JSON.stringify({
    mode,
    importMs: Number(tImport.toFixed(3)),
    ...(mode.startsWith("prewarm") ? { prewarmMs: Number(prewarmMs.toFixed(3)) } : {}),
    ...(decomp !== undefined ? { decomp } : {}),
    gatesMs: gates.map((value) => Number(value.toFixed(3))),
    coldTotalMs: Number(coldTotal.toFixed(3)),
    warmPerGateUs: Number((warmPerGate * 1e3).toFixed(2)),
    coldOverWarm: Number((coldTotal / (warmPerGate * 5)).toFixed(1))
  })
);
```
