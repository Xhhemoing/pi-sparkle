MODEL_SLUG=claude-fable-5-thinking-xhigh

# R17-I：CLI / Pi adapter / config / telemetry 第十七遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `fb5d9a2`（派单 tip，含 R17-F 收口；G/H 在飞，未触碰）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- src/cli src/pi-adapter src/config src/telemetry` 为空（`--stat` 0 行）——**零漂移，连续第十轮字节不变**。`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet` 通过）；切片相关测试面零漂移（`git diff --quiet 8dee7fb..HEAD -- test/` 通过）。基线增量核查：`6720960`（R16-I 基线）→ `fb5d9a2` 为 R16-G/H/I/J 与 R17-A…R17-F 空枚举收口 + 派单文档，全部 docs；EXCLUSIONS.md 对 I 切片无新约束行（新增尾行 S17 前缀为零，最后 I 相关行仍是 S8-I-3）。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致（R8-I「~28 / cli 16」维持历轮判定：头部笔误，git 为权威）。
- 前置阅读按令执行：README → EXCLUSIONS 全表（546 行，含全部 S\*-I-\* 行与 S13-B-1 尾行）→ PROGRESS → round-17/PLAN（含 R17-A…R17-F 六切片空枚举收口摘要与 I 切片行）→ R16-I（八连空 + flowchart 抽测格 + 181 模块采样归因）→ R15-I（A/A 地板 / 引擎代 / 堆足迹）→ R14-I（肥配置态交叉不存在）→ R13-I…R9-I 收口按需 → **25 个切片源文件全量实际重读**（含凭据面两文件 `file-credential-store.ts` / `auth-session.ts` 的只读读码，未测量未立项）。
- 分支：`cursor/r17-i-cli-seventeenth-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（唯一入口 `/exec-daemon/node` shim，与 R9-I～R16-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。R15-I 轴 2 已收口引擎代格（24 无增量红利），本轮不重装 24、不补铸。pnpm 10.17.1，`pnpm install --frozen-lockfile`（本 VM node_modules 初始缺失）+ `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**（package.json 钉死 + frozen-lockfile 内容寻址），与 R7-I～R16-I 同版本；exports 图逐项复核仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第十轮未满足，两臂本轮第五次直接测量再确认（§2.2）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S17-I-\* ID（诚实空枚举，与 R9-I～R16-I 同结局，九连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第十七组枚举而非历轮剩余项清理。R14-I 钉死配置空间维度、R15-I 钉死测量学维度、R16-I 钉死命令类覆盖与采样归因维度；本轮与 Round 17 同轮兄弟（R17-B/D/E/F 各自新增拒绝路径普查/定价轴）对齐，补一块 I 切片十六轮从未直接测量的砖——**fail-closed 拒绝路径的端到端定价（历轮抽测格全部 exit-0，错误端点从未进过任何 spot 集）**。该轴为测量收口、未产出候选：

1. **零漂移五重确认（连续第十轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变——frozen-lockfile 内容寻址使包内容漂移数学上不可能；pi-ai 0.84.1 exports 图 7 项未变；`dist/providers/` 仍恰 39 表，本 VM 直接计数）、测试面（切片相关测试目录自 `8dee7fb` 零变动）、生产调用图（本轮再 grep：`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` 失手回退 L36 / `runtime.ts` 点用处 L20 / `listed-model.ts` 同步面 L1，`model-catalog.ts` 命中为 S7-I-1 注释非导入；pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方）、基线增量（`6720960..fb5d9a2` 仅 docs，EXCLUSIONS 对 I 无新行）。R8-I 关闭矩阵、R9-I～R16-I 抽测所依据的结构前提在本基线逐项成立。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字，派单明令项）**：R9-I 5 格承重集 × 2 Node 版本全部 exit 0，结构逐格同构：地板 54.0/51.7 ms，**custom−builtin 差 children +47.3 / +24.7、track +46.5 / +21.8 ms（22.14/22.22）**——与 R16-I 带（+49.7/+26.3、+51.7/+22.5）、R15-I 带（+56.3/+39.7、+65.9/+23.2）及更早六轮同号、同量级、同 Node 版本梯度（**十轮同构**）；带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内。进程内冷载锚点：`providers/all` 47.8-58.2 / 30.6-32.0 ms（22.14/22.22；warm 0.063-0.101 ms）；per-provider 表 anthropic 3.5/2.5、openai 1.1/0.8 ms（优势 ~9-25×）；custom id 失手解析首次 1.756/1.829 ms、重复失手中位 **0.097/0.096 ms**（八度复证 R10-I 记忆化否决）。S7-I-1 per-provider 优势与 custom 态回退归属完好（§1）。
3. **第十七组新角度枚举为空（§2）**，其中一轴为本轮新测量、不铸 ID：
   - **轴 1（§1.3）：fail-closed 拒绝路径端到端定价（I 切片首次；R17 轮拒绝路径主题在 I 的对应物）**。六个拒绝端点 × 2 Node 全部按预期 exit 1：未知子命令 53.7/49.6、run 缺旗标 55.2/50.3、`--flowchart×--children` 冲突 56.2/51.0、损坏 providers.json@children 55.8/52.3、损坏 children.json@builtin 56.3/53.4 ms——**五个端点全部贴 `--version` 地板（54.0/51.7）**：fail-closed 排序使廉价拒绝先于任何重子树装载发生（providers.json 解析先于目录构建、children spec 解析先于 smartChildPlan 体），拒绝路径无死重可剪。唯一昂贵拒绝是 **unknown-model@children：105.6/80.1 ms = 地板 + 一次 `providers/all` 权威源查询**（进程内直测该解析链首次 55.1/30.6 ms ≈ providers/all cold 逐字同带，第二次 0.46-0.48 ms 走模块缓存；per-provider 失手 ~1.8 ms 为其小头）。该消费的唯一消除形态是「per-provider 表失手即宣告不存在、跳过权威源」——**正是 S8-I-1 的健全性反例所封死的跳过**（表缺失不证明模型不存在；惰性面将与同步面分歧、把合法 id 误判 unknown）。加上稳态流量为零（错误配置路径）+ once-per-run 类，三重否决；`parseProvidersConfig` 无效 id 拒绝 8.27/8.62 µs/call，µs 族。**无候选、不铸 ID。**
   - 其余全部角度逐条映射到排除行或历轮无 ID 收口（§2.3），无一越过落地线；R13-I 两处无名微观（`import.meta.resolve` 预探针、alias-row spread）本轮重读逐字在位（`listed-model-lazy.ts` L21-38 try/catch miss 结构未动、`model-catalog.ts` L72/L79 原样），维持换名拒绝、不补铸；R14-I 肥态证明、R15-I 三轴、R16-I 两轴均未重铸、未重测（其前提——39 表计数、exports 图、pi-ai 版本——本轮复核未变）。
4. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**；r7i part B 1220 builtin 行 × 39 provider lazy===sync；part D 载荷探针复证 builtin 构建后 providers/all 保持冷 59.9 ms、custom 回退命中缓存 1.5 ms）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R16-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读证据，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交 `scripts/round17-r17i-equivalence-sim.ts`（全部 harness/探针源码进附录 A，遵守 R7-F/R8-C/R9-I～R16-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md 未触碰；未触碰在飞 R17-G / R17-H 分支与文件。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S16-\* 条目；R9-I～R16-I 空枚举维持，未事后发明 S9-I-\* ～ S16-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：`--children` 复用 smartChildPlan 已校准目录（main.ts L726-729 原样）、Pi runtime 点用处 `await import`（main.ts L149 / auth-session 函数体内；S4-I 败者为 **S4-I-2..5**，无 S4-I-1）、12 条分支独占分派模块惰性导入（赢家 ID **S5-I-1**；main.ts L1417-1455 dispatch 站点 + L668 track/loop 边逐字在位）、per-provider 惰性模型表（`listed-model-lazy.ts` + `model-catalog.ts` L58）——零漂移即字节级证明，三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅对其**重开条件**做第五轮直接测量再确认（§2.2）——四 specifier 全 blocked（双 Node 逐字）+ `all.models.js` 文件级不存在 + 39 表计数不变，未满足，维持淘汰。硬不变量「`providers/all` 失手回退 + throw-outside-catch」逐字在位（`listed-model-lazy.ts` L34-37 未动）。本轮新轴是既有拒绝路径的直接观测（零行为差异、零代码改动），其 providers/all 消费的消除形态被裁决为 S8-I-1 逐字同型（§1.3）——与自制 union / 磁盘快照 / memo 三重围栏（R10-I / R11-I）中的任何 rename 无涉。
  - **未重开 S8-I-2**（预热无重叠窗口）、**S8-I-3**（enabled 循环 Promise.all）、**S7-I-2..4**、**S6-I-1..3**、**S5-I-2..5**、**S4-I-2..5**、**S3-I-1..6**、**S2-I-1..6**、**S1-I-1..8**。本轮 25 文件重读逐字再确认（未立项）：flowchart 双 `buildLiveCatalogConfig`（S1-I-1，main L569+L583）、providers.json 双读（S1-I-2，main L609 + createExecutor kind==="pi" L131）、loadLearnedRouting/loadProvidersConfig 死载荷下沉（S2-I-1/S3-I-3，main L708/L609）、`smartChildPlan` assignments.find（S1-I-3，main L372）、双 createAgentProfileRegistry（S3-I-2，main L286+L753）、stdout 批量（S3-I-4）、冲突检查提升（S4-I-4）、describeSparkleModel try/catch 预探测（S4-I-5）、setDefaultModels 双 parseModelRef（S3-I-5，providers-config L100-104）、parseCliErrorJson reverse（S1-I-6）、buildInvocation +=（S1-I-7）。
  - **R10-I ～ R16-I 无 ID 收口维持**：跨进程版本键控磁盘快照、进程内失手记忆化（本轮重复失手中位 0.096-0.097 ms 八度复证 µs 族）、进程内自制 39 表 union（三重围栏）、回退后 per-provider 探针短路（≤3.5 ms/provider，仅 mixed 态可达）、root `.` / `./compat` 作替代源（R12-I）、`all.models` 文件级探针（R13-I）、肥配置态交叉不存在证明（R14-I）、A/A 噪声地板 / 引擎代格 / 堆足迹格（R15-I）、**flowchart 抽测格 + 181 模块/1003 KiB 采样归因（R16-I，不补铸不重测）**。**R13-I 两处无名微观维持不铸**：`import.meta.resolve` 同步预探测（S4-I-5 族镜像 + 失手锚点八度定界 <0.3 ms/构建）、`buildLiveCatalogConfig` 默认别名行 spread（model-catalog.ts L72/L79 原样，ns-µs 级 S1-I-8 邻域）。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项（S1-I-5 / S7-I-4 维持关闭）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；稀疏→已批准 R0 基线；双 LCB / 双归因在位（X0-11）；提升 proposal-first（Checkpoint F-PROD 开放，ADR-005/X0-1）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`listed-model.ts` 同步公开面与惰性面经 `listed-model-common.ts` 字节等价关系未动；`providers/all` 失手回退 + throw-outside-catch 在位；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未触碰在飞 R17-G / R17-H 分支与文件。

## 1. 本 VM 复测：5 格承重抽测 + 进程内锚点 + 新轴（双 Node，未拷贝历轮数字）

R9-I～R16-I 抽测集原样复刻并**新增 6 个拒绝端点格**（configured 态夹具、每迭代全新拷贝 state-root、2 预热 + 10 测量、显式二进制全路径，遵守 R7-I 配置态 × 命令类纪律；harness 全文附录 A.1）。

### 1.1 抽测中位数（ms，spawn-to-exit）

| 格 | 预期 exit | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|---|
| `--version`（地板） | 0 | 54.0 (min 52.4, p90 55.3) | 51.7 (49.9, 53.2) |
| run-children@builtin | 0 | 108.6 (105.0, 117.9) | 105.6 (102.2, 108.5) |
| run-track@builtin | 0 | 126.5 (122.1, 129.7) | 122.3 (118.4, 128.2) |
| run-children@custom | 0 | **155.9** (142.4, 157.9) | **130.3** (123.9, 132.4) |
| run-track@custom | 0 | **173.0** (166.4, 191.9) | **144.1** (138.4, 146.7) |
| reject:unknown-subcommand（本轮新格，§1.3） | 1 | 53.7 (51.8, 55.8) | 49.6 (48.4, 51.3) |
| reject:run-missing-flags（新格） | 1 | 55.2 (53.0, 56.6) | 50.3 (49.2, 51.5) |
| reject:run-flag-conflict（新格） | 1 | 56.2 (53.1, 57.3) | 51.0 (49.7, 53.2) |
| reject:corrupt-providers@children（新格） | 1 | 55.8 (52.3, 57.6) | 52.3 (50.0, 54.3) |
| reject:unknown-model@children（新格） | 1 | **105.6** (101.9, 111.2) | **80.1** (79.1, 81.6) |
| reject:bad-children-json@builtin（新格） | 1 | 56.3 (54.4, 57.3) | 53.4 (51.8, 55.1) |

- **custom−builtin 差**：children **+47.3 / +24.7**、track **+46.5 / +21.8** ms（22.14/22.22）。R16-I 带 +49.7/+26.3 与 +51.7/+22.5，R15-I 带 +56.3/+39.7 与 +65.9/+23.2，R14-I 带 +52.2/+26.7 与 +55.1/+23.7，R13-I 带 +45.5/+23.7 与 +51.7/+21.5，R12-I 带 +51.3/+29.1 与 +58.7/+24.1，R11-I 带 +45.9/+22.8 与 +48.0/+23.4，R10-I 带 +47.1/+36.6 与 +56.0/+26.4，R9-I 带 +53.9/+26.9 与 +49.4/+28.2——**十轮同号、同量级、同 Node 版本梯度**。带缘偏移全部落在 R15-I 轴 1 的 A/A 包络内（22.14 单窗 maxAbs 34.5 ms、22.22 8.7-10.4 ms），无新结构。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
- **builtin 态目录构建**比地板贵 +54.6/+53.9（children，双引擎）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。本 VM 地板（54.0/51.7）与 R16-I VM（54.2/51.0）紧邻，格间结构不变。
- 尾部旁注（R17-B 逐任务尾部轴在 I 的对应观察，不另立轴）：全部 11 格 p90/median 比在 1.02-1.15，无悬崖；custom 格 22.14 尾最宽（track p90 191.9 vs 中位 173.0），与 R15-I 轴 1 的 22.14 噪声顶自洽。

### 1.2 进程内冷载锚点（附录 A.2 脚本，工作区根执行，测毕删除）

| 锚点 | 22.14.0 | 22.22.2 |
|---|---|---|
| `providers/all` cold import（3 次） | 58.2 / 47.8 / 52.9 ms | 32.0 / 31.6 / 30.6 ms |
| `providers/all` warm（第二次 await） | 0.063-0.071 ms | 0.097-0.101 ms |
| per-provider `anthropic.models` cold | 3.5 ms | 2.5 ms |
| per-provider `openai.models` cold | 1.1 ms | 0.8 ms |
| `providers/all`（双表已载后） | 57.2 ms | 28.0 ms |
| custom id 失手解析首次 | 1.756 ms | 1.829 ms |
| 同上，重复失手中位（N=49） | **0.097 ms** | **0.096 ms** |

- per-provider 优势 ~9-25×，双引擎成立，S7-I-1 归属完好；custom 格增量与 `providers/all` cold 同量级，回退归属自洽。
- 重复失手 ~0.096-0.097 ms 八度复证 R10-I：Node package-reader 已缓存失败解析，记忆化物理无收益（<0.3 ms/构建）。

### 1.3 轴 1：fail-closed 拒绝路径端到端定价（本轮新证据，不铸 ID）

十六轮以来所有抽测格全部 exit-0；R8-I 78 格关闭矩阵也只覆盖成功命令类。拒绝端点的价格一直只有结构推断（「once-per-run 类直接否决」），从未直接测量——本轮与 R17-B/D/E/F 的拒绝路径主题对齐，补上直接数字（§1.1 下半表 + 进程内分解，附录 A.2 `reject` 模式）：

- **五个廉价拒绝端点全部贴地板**（+0~+2.3 ms 于 `--version`，双引擎）：未知子命令、run 缺旗标、`--flowchart×--children` 冲突三个 parse-args 拒绝在任何配置装载前抛出；损坏 providers.json 拒绝在 `loadProvidersConfig`（main L609）抛出、先于目录构建与 executor 使用；损坏 children.json 拒绝在 `parseChildSpec`（作为 `smartChildPlan` 实参求值）抛出、先于 catalog 任何工作。**fail-closed 排序天然把廉价校验放在重子树装载之前，拒绝路径上无死重可剪**——「错误路径付了不必要的成功路径成本」这一类假设在 I 切片被直接清空。
- **唯一昂贵拒绝是 unknown-model@children**：105.6/80.1 ms = 地板 + 51.6/28.4 ms。进程内直测该链（`resolveListedModelLazy("nosuchprov", …)`）：首次 **55.1/30.6 ms ≈ providers/all cold 逐字同带**（47.8-58.2 / 30.6-32.0），第二次 0.455-0.475 ms（模块缓存）；构成 = per-provider 失手（~1.8 ms）+ **`providers/all` 权威源查询**（其余全部）+ 抛 `unknown model`。
- **候选性裁决（三重否决，不铸 ID）**：(a) 该消费的唯一消除形态是「per-provider 表失手即宣告 id 不存在、跳过权威源」——S8-I-1 健全性反例逐字同型（表缺失不证明模型不存在；未来 pi-ai 布局若不再 ship per-provider 表，跳过会把**全部合法 builtin id** 误判 unknown，惰性面与同步面分歧）；`listed-model-lazy.ts` L34-37 的 throw-outside-catch 正是防此的硬不变量。(b) 稳态流量为零：unknown-model 是配置错误一次性路径，修复后不再发生。(c) once-per-run CLI 类按验收线直接否决。此外该拒绝仍**便宜于同态成功路径**（105.6 vs custom 态成功 155.9；80.1 vs 130.3）——不存在「拒绝比成功还贵」的异常。
- `parseProvidersConfig` 无效 id 拒绝 **8.27/8.62 µs/call**（2000/2000 rejected）——µs 族，每命令至多一次。
- **裁决：无候选、不铸 ID。** 拒绝路径维度就此收口；后续轮次引用本节即可把任何「错误端点瘦身」类候选直接判为地板贴线或 S8-I-1 同型。

## 2. 第十七组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 拒绝路径定价），每形态先对 EXCLUSIONS 全表 + 十六轮 I 报告做同机制先例检索。R14-I 收口配置空间、R15-I 收口测量学、R16-I 收口命令类覆盖与采样归因；本轮收口**错误端点定价维度**（轴 1）。

### 2.1 新轴候选性自查——测量收口，不铸 ID

- **轴 1（拒绝定价）**是既有 fail-closed 路径的直接观测：零行为差异、零代码改动。其产出是「五端点贴地板 + 唯一昂贵拒绝归属权威源查询」的裁决数字，不是优化；唯一理论消除形态在 §1.3 被映射为 S8-I-1 逐字同型。

### 2.2 S8-I-1 重开条件两臂第五轮直接测量再确认——不铸 ID

- 文件级：`dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 0.84.1（package.json + frozen-lockfile 内容寻址）。
- 执行级：四 specifier 探针在 22.14 / 22.22 双引擎逐字同结局（附录 A.2 原始输出：`providers/../models.generated` → ERR_INVALID_MODULE_SPECIFIER；`dist/models.generated.js` / `models.generated` → ERR_PACKAGE_PATH_NOT_EXPORTED；`providers/all.models` → ERR_MODULE_NOT_FOUND）——0.84.1 内无任何导出形态的轻量权威注册表，无任何旁路。
- **裁决**：重开条件连续第十轮未满足；所需的是 pi-ai 上游导出，不是仓内改动。此项为审计证据，非机制候选——未构造替代注册表、未触碰回退路径——**不铸 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级即死）

| 扫描角度 | 处置 |
|---|---|
| custom id 跳回退 / 自制 union / 磁盘快照 / 进程内失手 memo | S8-I-1 硬禁 + R11-I/R10-I 三重围栏无 ID 收口；重开条件本轮五测仍未满足；派单明令不得铸 rename ID |
| unknown-model 拒绝跳过权威源查询（本轮新观测的镜像形态） | §1.3 三重否决：S8-I-1 健全性反例逐字同型 + 零稳态流量 + once-per-run 类 |
| 拒绝端点「提前抛出」重排（把校验挪到更早） | 五端点已贴地板（+0~+2.3 ms），无可提前空间；S4-I-4（冲突检查提升——错误选择发散）同族先例在案 |
| `run --flowchart` 命令类 / 采样归因 / 模块图普查 | R16-I 轴 1/轴 2 收口，前提未变，不重测不补铸 |
| 多 provider 阈值预取 providers/all | R14-I 全域支配证明（交叉点不存在），前提复核未变 |
| `import.meta.resolve` 同步预探测（R13-I 无名微观 #1） | 维持换名拒绝：S4-I-5 族镜像 + 失手锚点八度定界 <0.3 ms/构建 |
| `buildLiveCatalogConfig` 默认别名行 spread（R13-I 无名微观 #2） | 维持换名拒绝：每构建 ≤2 次 ~11 字段 spread（L72/L79），ns-µs 级 S1-I-8 邻域 |
| root / compat 作替代注册表源 | R12-I 直接测量封死（无表 + 严格更重） |
| providers/all 预热重叠 / enabled 循环并行 | S8-I-2 / S8-I-3 逐字（R16-I CPU 剖析为 S8-I-2 第四证人） |
| 回退后 per-provider 探针短路（mixed 态） | R11-I 无 ID 收口（≤3.5 ms/provider；跨调用穿线撞 X1-1） |
| pi-adapter barrel（`index.ts`）隐藏静态边 | 本轮再 grep 为无：全 src/ 无生产导入方 |
| flowchart 双 `buildLiveCatalogConfig` / providers.json 双读 | S1-I-1 / S1-I-2 逐字（warm 锚点 0.063-0.101 ms 旁证） |
| loadLearnedRouting / loadProvidersConfig 死载荷下沉 | S2-I-1 / S3-I-3 逐字（损坏时错误路径发散——本轮拒绝格直接展示该 exit-1 路径的实际价格） |
| `smartChildPlan` assignments.find 换 Map / 双 registry | S1-I-3 / S3-I-2 逐字（children 个位数 / 8.4 µs） |
| pause/inject 免 router / 免 learned | S2-I-2 / S9-G-1 逐字 |
| resume/answer 预读去重 / PAUSED 探测降级 | S2-I-4 / S4-I-2 逐字 |
| compile-cache / 分派拆分 / 剩余独占边 | S6-I-1..3 逐字（once-per-process 亚两位数 ms） |
| dist 布局 / 引擎钉版 | S5-I-2..5 族；R15-I 轴 2 已实测 22.22 取尽红利、24 无增量 |
| 一次性命令类（models/auth/doctor/inspect/episode/commits/adapt/pref/delete；含 models enable 的 assertKnownCatalogId+enableModel 双 loadProvidersConfig、episode close 的双 store readAll、commits 的 events+checkpoint 双读） | S1-I-4 / S7-I-4 / S2-I-5 / S3-I-4 类；once-per-process 噪声按派单直接否决 |
| 凭据面（auth status --all、credential store 微观） | S1-I-5 / S7-I-4 围栏，只读证据 |
| buildInvocation / invocationError / parseModelRef / parseProvidersConfig / parseCliErrorJson / setDefaultModels 微观 | S1-I-7 / S2-I-3 / S3-I-1 / S3-I-5 / S2-I-6 / S1-I-6 逐字（实测更慢或 ns 级；本轮 parse 拒绝直测 8.3-8.6 µs 补充定界） |
| pi-executor / cluster-tools 执行面微观（translatePiEvent 分派、resolveIdentity、per-execute 工具数组） | 网络 I/O 支配的 live 面；µs 级，无复杂度类角度 |
| telemetry INVOCATION_CALL_OUTCOMES includes→Set / compareRunToRun 微观 | 表长 4，S1-D-8 族噪声 / 诊断聚合一次性 |
| writeAtomicJson 去 fsync / doctor-overlay jsonl 扫描 | S9-G-3 族（不得去 fsync）/ S3-J-4 族 + 一次性诊断类 |
| `builtinModelLazy` 动态 specifier / `_MODELS` 键过滤微观 | 命中路径被表解析成本（0.8-3.5 ms）支配、失手路径 ~0.096 ms；µs 族 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖、pi-ai exports、测试面、调用图五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选；唯一数十 ms 结构（custom/mixed 态 providers/all 失手回退，本轮新增其 unknown-model 拒绝路径变体的直接定价）的全部消除形态收敛到同一个上游前提——pi-ai ship 轻量权威子路径——该前提的两种成立形态维持文件级 blocked（本轮第五次直接确认）。

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part B：1220 builtin 行 × 39 provider lazy===sync；part C：17 组失手/custom/对抗用例；part D 载荷探针：builtin 构建后 providers/all 保持冷 59.9 ms、custom 回退命中缓存 1.5 ms；part E：configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单项）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2（与 R9-I～R16-I 计数逐字相同）、`pnpm build` 绿。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **引擎注记**：22.14.0 的 doctor engines 预检 fail-closed 为设计行为（R9-I §3 记录在案）；nvm 22.22.2 为 gate/仿真引擎。依 R15-I 轴 1 纪律，候选验收须在 ≥22.19 引擎 + 交错窗上做——本轮无候选，抽测数字仅用于十轮带比对（多窗中位数，双引擎同号）。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动；无赢家故未提交 `scripts/round17-r17i-equivalence-sim.ts`；临时锚点探针（`r17i-anchor.mjs`）测毕即删，未进 lint 面。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；本轮新轴以测量收口关闭错误端点定价缺口；S8-I-1 两臂五测维持 blocked；两个历轮无名微观维持换名拒绝）。
- 剩余赢家：无（唯一数十 ms 结构被 S8-I-1 健全性反例 + 硬不变量封死；其 unknown-model 拒绝变体本轮定价并映射同一围栏；重开条件所需的是 pi-ai 上游导出，不是仓内改动）。
- 矩阵空洞：无（R8-I 78 格全覆盖；承重格十轮同构复现 + 抽测集本轮补入错误端点 6 格；R7-I 配置态 × 命令类纪律全程遵守——configured 态夹具、builtin/custom/corrupt/unknown-model 四态、children/track 双命令类 + 拒绝类；肥态维持 R14-I 收口、测量学维持 R15-I 收口、命令类覆盖与归因维持 R16-I 收口）。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 11 格抽测 harness（/tmp/r17i-bench/spot.mjs，`<node二进制全路径> spot.mjs <同一路径>` 分别以两版本执行；承重 5 格与 R16-I A.1 逐字同构 + 新增 6 个拒绝端点格）

```javascript
// R17-I spot re-measure: floor + builtin/custom children+track cells
// (R9-I..R16-I harness lineage) + NEW axis: fail-closed rejection-path cells
// (spawn-to-exit pricing of exit!=0 endpoints — never in any prior round's
// spot set, which was exit-0-only for sixteen rounds).
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r17i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.argv[2];
const WARMUP = 2, ITERS = 10;

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] },
  // NEW rejection-axis states:
  // corrupt: providers.json is not JSON -> DomainValidationError before any catalog work.
  // unknownmodel: schema-valid providers.json whose enabled id resolves nowhere ->
  //   buildLiveCatalogConfig throws `unknown model` AFTER paying per-provider miss
  //   + providers/all fallback (the authoritative-source consult).
  unknownmodel: { version: 1, enabled: ["nosuchprov/nosuch-model", "anthropic/claude-fable-5"], primary: "nosuchprov/nosuch-model", customProviders: [] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
for (const [name, cfg] of Object.entries(states)) {
  const dir = join(FIX, `state-${name}`);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
}
{
  const dir = join(FIX, "state-corrupt");
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), "{ not json !!!\n");
}
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
const CHILDREN = join(FIX, "children.json");
writeFileSync(CHILDREN, JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");
const BADCHILDREN = join(FIX, "children-bad.json");
writeFileSync(BADCHILDREN, "{ tasks: [ broken\n");

const cells = [
  // Bearing set (R9-I..R16-I lineage).
  { id: "version", args: () => ["--version"], state: undefined, expectExit: 0 },
  ...["builtin", "custom"].flatMap((state) => [
    { id: `run-children@${state}`, state, expectExit: 0, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] },
    { id: `run-track@${state}`, state, expectExit: 0, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"] }
  ]),
  // NEW axis: fail-closed rejection cells (expect exit 1).
  { id: "reject:unknown-subcommand", state: undefined, expectExit: 1, args: () => ["frobnicate"] },
  { id: "reject:run-missing-flags", state: undefined, expectExit: 1, args: () => ["run"] },
  { id: "reject:run-flag-conflict", state: undefined, expectExit: 1, args: () => ["run", "--project", PROJ, "--objective", "x", "--flowchart", "/nonexistent.json", "--children", CHILDREN] },
  { id: "reject:corrupt-providers@children", state: "corrupt", expectExit: 1, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] },
  { id: "reject:unknown-model@children", state: "unknownmodel", expectExit: 1, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] },
  { id: "reject:bad-children-json@builtin", state: "builtin", expectExit: 1, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", BADCHILDREN] }
];
function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, `state-${state}`), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit; let firstStderr = "";
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 120000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0) firstStderr = (r.stderr ?? "").split("\n").find((l) => l.startsWith("error:")) ?? "";
    if (i === 0 && r.status !== cell.expectExit) console.error(`  UNEXPECTED-EXIT ${cell.id}: got ${r.status} want ${cell.expectExit}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  const note = cell.expectExit !== 0 && firstStderr !== "" ? `  [${firstStderr.slice(0, 72)}]` : "";
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}${note}`);
}
```

原始输出（全部格 exit 与预期一致；拒绝格首迭代 stderr 校验通过）：

```
== v22.14.0 ==
version: median=54.0ms min=52.4 p90=55.3 exit=0
run-children@builtin: median=108.6ms min=105.0 p90=117.9 exit=0
run-track@builtin: median=126.5ms min=122.1 p90=129.7 exit=0
run-children@custom: median=155.9ms min=142.4 p90=157.9 exit=0
run-track@custom: median=173.0ms min=166.4 p90=191.9 exit=0
reject:unknown-subcommand: median=53.7ms min=51.8 p90=55.8 exit=1  [error: Unknown command: frobnicate]
reject:run-missing-flags: median=55.2ms min=53.0 p90=56.6 exit=1  [error: run requires --project <path> and --objective <text>]
reject:run-flag-conflict: median=56.2ms min=53.1 p90=57.3 exit=1  [error: run --flowchart is incompatible with --children]
reject:corrupt-providers@children: median=55.8ms min=52.3 p90=57.6 exit=1  [error: invalid providers.json at ...]
reject:unknown-model@children: median=105.6ms min=101.9 p90=111.2 exit=1  [error: unknown model "nosuchprov/nosuch-model"]
reject:bad-children-json@builtin: median=56.3ms min=54.4 p90=57.3 exit=1  [error: Invalid child spec .../children-bad.json: ...]
== v22.22.2 ==
version: median=51.7ms min=49.9 p90=53.2 exit=0
run-children@builtin: median=105.6ms min=102.2 p90=108.5 exit=0
run-track@builtin: median=122.3ms min=118.4 p90=128.2 exit=0
run-children@custom: median=130.3ms min=123.9 p90=132.4 exit=0
run-track@custom: median=144.1ms min=138.4 p90=146.7 exit=0
reject:unknown-subcommand: median=49.6ms min=48.4 p90=51.3 exit=1  [error: Unknown command: frobnicate]
reject:run-missing-flags: median=50.3ms min=49.2 p90=51.5 exit=1  [error: run requires --project <path> and --objective <text>]
reject:run-flag-conflict: median=51.0ms min=49.7 p90=53.2 exit=1  [error: run --flowchart is incompatible with --children]
reject:corrupt-providers@children: median=52.3ms min=50.0 p90=54.3 exit=1  [error: invalid providers.json at ...]
reject:unknown-model@children: median=80.1ms min=79.1 p90=81.6 exit=1  [error: unknown model "nosuchprov/nosuch-model"]
reject:bad-children-json@builtin: median=53.4ms min=51.8 p90=55.1 exit=1  [error: Invalid child spec .../children-bad.json: ...]
```

### A.2 进程内锚点 + 可达性探针 + 拒绝链分解（§1.2 / §1.3 / §2.2 证据；临时置于 /workspace 根以获得 node_modules 解析，测毕删除；all/tables/miss/traversal 四模式与 R16-I A.2 逐字同构，`reject` 模式为本轮新增）

```javascript
// R17-I in-process anchors + reachability probes (temporary; deleted after measurement).
import { performance } from "node:perf_hooks";
const label = process.version;
const which = process.argv[2];
if (which === "all") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const dt = performance.now() - t0;
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  console.log(`${label} providers/all: cold=${dt.toFixed(1)}ms warm=${(performance.now() - t1).toFixed(3)}ms`);
} else if (which === "tables") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/anthropic.models");
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/openai.models");
  const t2 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const t3 = performance.now();
  console.log(`${label} anthropic.models=${(t1 - t0).toFixed(1)}ms openai.models=${(t2 - t1).toFixed(1)}ms providers/all-after-tables=${(t3 - t2).toFixed(1)}ms`);
} else if (which === "miss") {
  const N = 50; const times = [];
  for (let i = 0; i < N; i++) {
    const t0 = performance.now();
    try { await import("@earendil-works/pi-ai/providers/customco.models"); } catch { /* expected miss */ }
    times.push(performance.now() - t0);
  }
  const first = times[0];
  const rest = times.slice(1).sort((a, b) => a - b);
  console.log(`${label} miss-resolution: first=${first.toFixed(3)}ms repeat-median=${rest[Math.floor(rest.length / 2)].toFixed(3)}ms min=${rest[0].toFixed(3)}ms`);
} else if (which === "traversal") {
  for (const spec of [
    "@earendil-works/pi-ai/providers/../models.generated",
    "@earendil-works/pi-ai/dist/models.generated.js",
    "@earendil-works/pi-ai/models.generated",
    "@earendil-works/pi-ai/providers/all.models"
  ]) {
    try {
      await import(spec);
      console.log(`${label} REACHABLE: ${spec}`);
    } catch (e) {
      console.log(`${label} blocked: ${spec} -> ${e.code ?? e.message.slice(0, 60)}`);
    }
  }
} else if (which === "reject") {
  // NEW axis anchor: in-process price of the unknown-model reject inside
  // buildLiveCatalogConfig's resolution chain (per-provider miss + the
  // authoritative providers/all consult before the throw), vs the corrupt-
  // providers reject (parse-time throw, no catalog work).
  const { resolveListedModelLazy } = await import("./dist/pi-adapter/listed-model-lazy.js");
  const t0 = performance.now();
  const r = await resolveListedModelLazy("nosuchprov", "nosuch-model", []);
  const t1 = performance.now();
  const r2 = await resolveListedModelLazy("nosuchprov2", "nosuch-model", []);
  const t2 = performance.now();
  console.log(`${label} unknown-model resolution: first(miss+all-fallback)=${(t1 - t0).toFixed(1)}ms second(all warm)=${(t2 - t1).toFixed(3)}ms result=${String(r)}/${String(r2)}`);
  const { parseProvidersConfig } = await import("./dist/config/providers-config.js");
  const N = 2000; let rejects = 0;
  const t3 = performance.now();
  for (let i = 0; i < N; i++) {
    try { parseProvidersConfig({ version: 1, enabled: [`p${i}`], customProviders: [] }); } catch { rejects += 1; }
  }
  const t4 = performance.now();
  console.log(`${label} parseProvidersConfig invalid-id reject: ${((t4 - t3) / N * 1000).toFixed(2)}us/call (${rejects}/${N} rejected)`);
}
```

原始输出（anchors：每模式全新进程；traversal 双引擎逐字一致）：

```
v22.14.0 providers/all: cold=58.2 / 47.8 / 52.9 ms  warm=0.063-0.071 ms
v22.22.2 providers/all: cold=32.0 / 31.6 / 30.6 ms  warm=0.097-0.101 ms
v22.14.0 anthropic.models=3.5ms openai.models=1.1ms providers/all-after-tables=57.2ms
v22.22.2 anthropic.models=2.5ms openai.models=0.8ms providers/all-after-tables=28.0ms
v22.14.0 miss-resolution: first=1.756ms repeat-median=0.097ms min=0.065ms
v22.22.2 miss-resolution: first=1.829ms repeat-median=0.096ms min=0.068ms
v22.14.0 blocked: @earendil-works/pi-ai/providers/../models.generated -> ERR_INVALID_MODULE_SPECIFIER
v22.14.0 blocked: @earendil-works/pi-ai/dist/models.generated.js -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/models.generated -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/providers/all.models -> ERR_MODULE_NOT_FOUND
v22.22.2 blocked: (同上四行，双 Node 逐字一致)
v22.14.0 unknown-model resolution: first(miss+all-fallback)=55.1ms second(all warm)=0.475ms result=undefined/undefined
v22.14.0 parseProvidersConfig invalid-id reject: 8.27us/call (2000/2000 rejected)
v22.22.2 unknown-model resolution: first(miss+all-fallback)=30.6ms second(all warm)=0.455ms result=undefined/undefined
v22.22.2 parseProvidersConfig invalid-id reject: 8.62us/call (2000/2000 rejected)
```

MORE_OPTIMA=no
