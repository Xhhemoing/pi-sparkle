MODEL_SLUG=claude-fable-5-thinking-xhigh

# R17-E：`src/learning/` 第十七遍复查报告（Round 17）

**战役:** 全库持久 SOTA 优化 Round 17 / R17-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `5d939af`（含 R17-A 空枚举摄取与本派单）
**分支:** `cursor/r17-e-learning-seventeenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第十七组新角度枚举为空（有理由的
空枚举，未铸造任何 S17-E-\* ID，与 R10-E..R16-E 同口径），切片维持关闭
（MORE_OPTIMA=no）。本轮把 R17-B 的「拒绝/异常路径定价」与「尾部分布」两个
方法首次移植到本切片——十六轮全部锚点只测过接受/快乐路径，切片内全部
throw/catch/拒绝位点的价格与逐事件类边际价格从未被标定。两轴合计 11 个
拒绝/异常测量胞 + 11 个事件类定价胞全部收口：异常面全部位点是
once-per-load fail-closed 契约门（3.1–6.4µs）、零生产流量面（2.5–3.5µs）
或生产不可达防御纵深（try/catch 非抛出开销实测符号混合 ±40–380ns =
零成本 try 证实）；事件类价格表最贵类（result-fail 1,159–1,177ns/事件）
全表饱和预算仅 46.4–47.1µs/run，且价格表以 1.01–1.05 的重构比精确解释了
R14-E/R15-E/R16-E 三轮锚点带差异的全部构成效应——十六轮口头归因的
「夹具构成效应」自此有了机械价格表证明。**

1. **零漂移七重确认**：切片 10 个文件（`wc -l` 确认恰 10 文件、合计 1770
   行，与 R14-E/R15-E/R16-E 逐字节相同）自 R1-E 裁决基线（`adb20d7`）至
   本轮基线（`5d939af`）**逐字节未变**——`git diff adb20d7..HEAD --
   src/learning/` 为 0 行、`git log adb20d7..HEAD -- src/learning/` 为
   **0 提交**，十七轮全程无人触及该目录。且 R16-E 基线（`53e1284`）之后的
   全部提交（R16-E..J 摄取、Round 16 收口、Round 17 开轮、R17-A/B 摄取与
   R17-C/D/E 派单）**仅触及 `docs/reports/sota-opt/`**——
   `git log 53e1284..HEAD --name-only` 非 docs 路径为零，
   `git diff --stat 53e1284..HEAD -- src/ test/` 为空。R1-E..R16-E 的全部
   规模测量、调用面图景与 S1-E-1..8 / S2-E-1..7 / S3-E-1..5 / S4-E-1..3 /
   S5-E-1..5 / S6-E-1..5 / S7-E-1..5 / S8-E-1..3 / S9-E-1..3 共 44 项切片
   排除裁决（R10-E..R16-E 未铸 ID，其空枚举收口一并继承，含 R13-E 三处
   无名微观、R14-E 规模越线/JIT 双轴、R15-E GC/分配与 A/A 地板双轴、
   R16-E 累积状态与载荷形态双轴）原样成立、原样继承。
2. **本 VM 锚点/矩阵重锚（未拷贝 R16-E 数字，遵派单令；同 VM 双 Node 版本
   交叉，延续 R13-E..R16-E 协议）**：SLICE-CPU 总量上界锚点 **16 次运行**
   （8 × Node v22.14.0 + 8 × Node v22.22.2，engines `>=22.19.0` 仅后者
   合规）**18.5–20.2µs/run**（collect 12.7–14.1µs、outcomes 4.0–4.3µs、
   diagnose 0.98–1.11µs、bandit-build 0.7–0.8µs；两版本同带）——距落地线
   （≥10ms）**495–542×**，即使把切片 CPU 清零也远不达门槛，在历史带
   （~11–24µs/run）之内，完整落在 R14-E 带（17.2–19.2，collected=15）与
   R16-E 带（19.0–22.0，collected=17）的邻域：本轮夹具 collected=15 与
   R14-E 相同，collect 读数 12.7–14.1 与 R15-E 的 R14-E 种子比对运行
   （13.3–13.5）同带；outcomes 读数 4.0–4.3 高于 R16-E 的 2.3–2.7 为
   构成差（本轮 outcomes=4 vs R16-E outcomes=2）——**且本轮新轴 B 的
   价格表以 1.01–1.05 重构比给出了构成效应的机械证明（§2.1），十六轮
   带差归因就此闭环**。按历史最保守合成口径（R7-E outcomes 带 7.0–7.3µs
   + R12-E diagnose 分组上界 3.83–3.92µs 同时替换本轮两项）总量
   ~24.2–26.1µs，仍 **≥382×** 低于门槛。配置态 live 面：
   `applyLearnedRouting` 非空策略 **257–290ns/task**（R13-E 258–312 /
   R14-E 256–303 / R15-E 258–298 / R16-E 269–294 同带）、空策略
   **110–122ns/task**（R15-E 110–125 / R16-E 111–128 同带）；
   `loadLearnedRouting` 全新 root 25.4–36.2µs、现实 registry
   **74.3–89.1µs**（R8-E 80–85 / R15-E 76.9–85.0 / R16-E 76.6–84.8 同带，
   亚 ms 一次性）。全部确定性 VERDICT 行 16 次运行**逐位一致**
   （`E=41 collected=15 routedE=41 outcomes=4 S=12 diagnoseGroups=1
   prevArms=3`），3/3 sanity 检查全过（§1、附录 A）。
3. **配置态 × 命令类矩阵复核无洞**：全部切片进入点行号逐条 rg 复核，
   **与 R8-E..R16-E 记录逐一相同、无新增调用方、无新增命令类**
   （`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
   `run/flowchart-run:681/712`、`track/loop:88/172`；resume/replay 经
   `flowchartForSupervisor` @ `flowchart-run:880/1021` 各单次载入；
   `run --flowchart` @ `cli/main:580` 在 `:708` 载入前返回，无双载入）。
   S8-E-1 双载入按令保护，未做任何去重分析。切片外消费者以模块路径精确
   检索复核：`adaptation/eval-routing.ts:223` 仅 `parseLearnedRoutingPolicy`
   （离线 eval 面，D 切片辖区）、`track/primary-split.ts:3` 类型导入、
   `privacy/record-classes.ts:212/224` 文档字符串、
   `adaptation/approval-profile.ts:44` docstring 假阳性（R14-E 已裁）——
   **消费者集合与 R16-E 逐一相同，无新增调用边**。测试专用面
   （`patterns`/`attribution`/`signatures` 三文件 + 五个测试专用导出）
   本轮交叉检索复核仍零生产调用方。
4. **本轮两条实扫新轴（§2 前置）**：(a) **异常/拒绝路径定价轴**（R17-B
   方法移植，本切片首次；R15-E 只测协议噪声、R16-E 只测载荷规模——切片内
   全部 throw/catch 位点的价格从未被标定）：11 个测量胞、6 次运行（两
   Node 版本各 3 次）全部 sanity 通过——`parseLearnedRoutingPolicy` 四
   拒绝形态 3.1–6.4µs/次（once-per-load fail-closed 门；首/末位
   first-fault 位置差 ~40ns/entry 与 R16-E B1 斜率同阶）；
   `parseObservedSignal` 三门位拒绝 2.5–3.5µs/次（零生产流量
   extraSignals 面）；`loadLearnedRouting` fresh-root 的 ENOENT 吞异常
   机器份额 **1.92–1.98µs = 总额的 5.1–7.1%**（每 run 至多一次且仅
   fresh root）；corrupt-registry fail-closed 重抛 63.7–73.6µs/次
   （错误频率 ~0）；`outcomesFromRoutedRun` 环内 try/catch 的非抛出
   结构开销 **6/6 次运行符号混合、单窗 max-abs 40.5–380ns（全函数）**
   ——V8 零成本 try 在本切片生产环上证实，且该 catch 经可达性分析证明
   **对 schema 合法事件不可达**（family 经 `familyFromRouted`、role 经
   `isAgentRole`、三版本字段经 `isCompleteRoute` 全部前置门控，
   criterion/source 硬编码，NaN occurredAtMs 通过 typeof number）——
   纯防御纵深，删之零收益（S9-D-3 同式）；假想抛出行定价 6.2–7.1µs/行
   （生产不可达）。(b) **逐事件类定价 + 尾部分布轴**（R17-B 尾部方法
   移植，本切片首次；十六轮全部用混合夹具，逐类边际价格从未拆解）：
   11 个事件类在共享前奏上定价，价格表 6 次运行稳定——
   **result-fail 1,159–1,177 / result-pass 1,119–1,134 /
   result-unobserved 967–988 / peer 729–750 / run-failed 632–646 /
   judge 312–349 / answer-hit 288–334 / route-complete 35–46 /
   answer-miss 34–41 / route-minimal 19–25 / noise 5–19 ns/事件**；
   尾部 max/p50 = 3.4–3.5×；**全表饱和最坏预算（40 事件全为最贵类）
   46.4–47.1µs/run，仍低于落地线 ~213×**；用价格表 × 混合夹具构成计数
   重构混合成本，**重构比 1.01–1.05**——价格表精确、构成效应机械化。
   两轴合计 22 个维度全部收口，不产生任何新候选。
5. **第十七组新角度枚举为空（§2）**：本轮新透镜为「**异常/拒绝路径 ×
   事件类构成**」（两条测量轴的代码侧对偶：逐函数问 (a) 哪些 throw/
   catch/拒绝位点存在、其重构是否等价且可能越线；(b) 哪些事件类扛着
   尾部价格、其按类特化是否等价且可能越线）。对第十七遍全量重读的 10
   个源文件逐函数普查：全部命中位点落在被点名排除保护（S12-D-1 预探针
   fail-open 族、S3-I-6 stat 探针 fail-closed 绕过族、S3-E-1 互斥分派
   抖动、S8-E-2 条件 spread、S9-E-3 分派树、S1-E-3/S2-E-5/S7-E-2
   truncate 族、S9-E-2 预过滤负优化、S4-E-1 空快路径、X0-5 错误消息
   契约、X1-2 平行实现）、硬不变量明令（fail-closed / 防御纵深 / 公开
   签名）或落地线算术封死区（最贵事件类全表饱和仅 ~47µs）。**未铸造
   任何 S17-E-\* 新 ID，无新增排除提议；S13-B-1 未以任何形态重提
   （`applyLearnedRouting` 内部零改动，其重开条件 |avoid|≈965–1,063
   维持未触发）；S8-E-1 未做去重分析；S9-E-2 未触碰；R13-E 三处无名
   微观未补铸；R14-E/R15-E/R16-E 六轴未复铸。**
6. **验证面**：`pnpm gate` 在 engines 合规的 Node v22.22.2 上**全绿**
   （typecheck ✓ / lint ✓ / test **1168 pass / 0 fail / 1 skipped**（共
   1169 / 78 suites，与 R14-E/R15-E/R16-E 计数逐一相同）/ build ✓）。
   切片相关套件 **48 tests / 2 suites / 48 pass / 0 fail**（两个 Node
   版本均过）。系统默认 node 仍为 v22.14.0（违反 engines `>=22.19.0`，
   R12-E §0 起连续记录的同一环境事实）；gate 结论以 v22.22.2 为准。
   零代码改动下全部硬不变量天然满足。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与
双归因未动；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故
未提交任何 sim/锚点脚本（差异面与新轴脚本测量段进附录 A，遵守
R7-F/R8-C/R9-I/R10-E..R16-E 无赢家纪律）。本切片在其输出契约与数据面语义
下维持 SOTA——第十七遍复查确认：**剩余的全部 ms 级余量仍全部在被排除表
点名保护的 I/O 契约面或切片外辖区上**，切片级收口条件（R3-E §7 …
R16-E §6）依然成立。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行）本轮第十七遍全量实际读码，未依赖前
  十六轮记忆。上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、
  `protocol/`、`privacy/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表 546 行，含全部
  S\*-E-\* 行与 S8-E-1 / S9-E-2 / S13-B-1）→ round-17/PLAN.md →
  round-16/R16-E.md（空枚举；累积状态 + 载荷形态）→ round-15/R15-E.md
  （空枚举；GC/A-A）→ R14-E/R13-E/R12-E/R11-E/R10-E（按需，透镜与先例
  核对）→ 10 个源文件。R17-C / R17-D 在其他分支在跑，未等待、未触碰其
  报告与分支。
- 基线漂移检查（§结论 1）：切片零提交零 diff；`53e1284..5d939af` 全部
  提交仅触及 `docs/reports/sota-opt/`——前十六轮全部测量与裁决原样成立。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S16 时代条目。特别地：
  - **S13-B-1**（B 切片辖区）未以任何变体重提：本切片辖区只有被调方
    `applyLearnedRouting` 本体，本轮对其**零改动**（切片零 diff 下必然）；
    其重开条件（本体成本 ≥2 个量级，R16-E 量化为 |avoid| ≈ 965–1,063）
    未触发、生产写入方（≤M×8）不可触发；本轮两轴均不触及 apply 内部。
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护，本轮仅复核其行号与频次
    （§1），未做任何去重分析；新轴 A 只测其 fresh/corrupt 错误路径价格，
    不触碰双载入结构。并发 promote 下的新鲜性仍是其保留理由。
  - **S9-E-2**（pass-2 预过滤负优化）未触碰：本轮未做任何两遍结构的
    工作集缩减变体；新轴 B 的「构成自适应短路」形态在 §2.2 行 10 以
    S9-E-2/S4-E-1 换名拒列，未做实验重开。
  - **R13-E 三处无名微观**（bandit `includes` 旁路已建 arms Set @
    `bandit-store:72`、apply avoided-Set filter+map 双中间数组 @
    `learned-routing:198-202`、`routingPolicyIdentity` 字面量再校验 @
    `learned-routing:43`）：位点本轮逐字复核未变（零 diff 下必然），
    维持 R13-E 裁决，**未补铸 ID、未重开**。
  - **R14-E 规模/JIT、R15-E GC/A-A、R16-E 累积状态/载荷形态六轴未复铸**：
    在 src/test 零漂移下原样继承（派单明令勿复铸）；本轮两条新轴
    （异常路径定价、事件类定价）为不同测量维度，无交叠——R15-E A/A 轴
    测的是**协议噪声**，本轮 A4 测的是**结构本身的非抛出开销**（读数
    落在 R15-E 地板之内，两轴互为印证）；R16-E B2 轴测的是**单事件
    字节规模**，本轮 B 轴测的是**事件类构成边际价格**（字节固定、
    类别变化）。
  - R10-E..R16-E 未铸 S10..S16-E-\*，本轮亦未事后发明任何此类编号；
    R17-A / R17-B 空枚举（各自辖区）与本切片无交，仅继承 R17-B 的
    **拒绝/异常路径定价与尾部分布方法**作为两条新轴的协议先例。
- **换名重提检查**：本轮新定位形态的换名检查全部并入 §2.2 表格逐行给出，
  并逐一对照 S1-E-\* … S9-E-\* 全表、S13-B-1、R10-E..R16-E 七轮空枚举面
  （其换名裁决全部原样维持，不再复抄）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  稀疏 → 已批准 R0 基线；双 LCB / 双归因在位；晋升 proposal-first；无
  阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；fail-open / 隐藏
  模块缓存（X1-1）未引入；bandit/learned registry I/O 的 fsync 与完整性
  重哈希未触碰；corrupt registry fail-closed 未触碰；两遍
  `collectSignalsFromEvents` 语义未触碰；X0-3 维持；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未
  force-push；仅提交并推送本分支上的本报告一个文件。
- 环境：本 VM 系统默认 node v22.14.0（`/exec-daemon/node`，PATH 前置
  遮蔽 nvm），nvm 另装 v22.22.2（engines `>=22.19.0` 仅后者合规）。锚点
  与两条新轴在两个版本各测（§1/§2，同带）；`pnpm gate` 以 v22.22.2 为准
  （§4）。依赖经 `pnpm install --frozen-lockfile` 安装（pnpm 10.17.1）。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

R7-I 教训（默认态夹具可能掩盖配置态主路径）按令复查。src 自 `53e1284` 起
一行未动（且自 `d148d12` 起 src/test 累计零 diff 的 R15-E/R16-E 论证链
原样有效），故 R8-E..R16-E §1 矩阵表结构原样成立；本轮逐条 rg 复核全部
切片进入点行号，**与 R16-E 记录逐一相同、无新增调用方**：

| 命令类 × 配置态 | 切片进入点（本轮行号复核） | 频次与规模 |
| --- | --- | --- |
| `adapt auto --run` | `runAutoAdaptFromEvents` @ `cli/adapt:188` → `runAutoAdaptLoop` | 每进程一次，E≈41 |
| `adapt auto --project` | `runAutoAdaptLoop` @ `cli/adapt:205`（空事件表，S4-E-1 面） | 每进程一次 |
| `adapt learn --run` | `proposeRoutingFromRoutedEvents` @ `cli/adapt:168`（S2-E-2 面） | 每进程一次 |
| 普通 `run` | `loadLearnedRouting` @ `cli/main:708`（S2-I-1 已裁决） | 每进程一次 |
| `run --flowchart` | `startFlowchartRun` @ `cli/main:580` → `flowchartForSupervisor` @ `flowchart-run:747→712` 单次载入（在 `cli/main:708` 之前返回，无双载入） | 每进程一次 |
| `run --children` | `cli/main:708` 载入 → `applyLearnedRouting` @ `routing/assign:102` 每任务 → `startFlowchartRun` @ `cli/main:748` → `flowchart-run:747→712` 第二次载入（S8-E-1，勿去重）→ `applyLearnedRouting` @ `flowchart-run:681` 每节点 → `runAutoAdaptLoop` @ `cli/main:783` 一次 | 双载入各一次；任务/节点 ≤10/几十 |
| `track`（startTrackedRun） | `track/loop:88` 载入 → 每任务 `applyLearnedRouting` → `startFlowchartRun` @ `track/loop:152` → `flowchart-run:712` 第二次载入（同 S8-E-1）→ `runAutoAdaptLoop` @ `track/loop:172` 一次 | 同上 |
| flowchart resume / replay | `flowchartForSupervisor` @ `flowchart-run:880` / `:1021` 各单次载入 | 每恢复一次 |
| SPARKLE_AUTO_ADAPT=0 | 仍收集（persistSignals + updateProjectBandit 照付），仅跳过 propose；R5-E 裁决维持 | 不变 |
| 测试专用面 | `patterns` / `attribution` / `signatures` / `compareSignatures` / `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `parseObservedSignal`(直调) / `proposeRoutingFromAssignments`(deprecated) / `saveLearnedRouting`(deprecated, throw-only) / `learnedRoutingPath`(死导出) 交叉检索复核仍零生产调用方 | 不进热路径 |

切片外其余消费者以 `learning/` 模块路径精确检索复核：
`adaptation/eval-routing.ts:223` 仅调用 `parseLearnedRoutingPolicy`（离线
eval 面，D 切片）；`track/primary-split.ts:3` 仅导入类型；
`privacy/record-classes.ts:212/224` 仅文档字符串提及路径布局；
`adaptation/approval-profile.ts:44` 维持 R14-E 裁决的 docstring 假阳性
（「auto-loop never CAS-promotes」，非调用边）。**矩阵结论：无新洞，
消费者集合与 R16-E 逐一相同**。

SLICE-CPU 锚点本 VM 重测（**16 次运行**：8 × Node v22.14.0 + 8 ×
Node v22.22.2；seeds `0xe17e01`（collect）/ `0xe17e02`（routed）/
`0xe17e03`（signals）——R12-E..R16-E 拒绝采样同协议，本轮三个生成器
按序首选种子全部一次通过、零出局，探针记录
seed→(collected,outcomes,diagnoseGroups)：01→(15,2,0)、02→(19,4,0)、
03→(16,4,1)；夹具镜像 R8-E..R16-E 生成器，R1-A 组成 E=41）：

```text
collect=12.7-14.1us  outcomes=4.0-4.3us  diagnose=0.98-1.11us  bandit-build=0.7-0.8us
total in-slice CPU ~18.5-20.2us per full auto-adapt run （两 Node 版本同带：
  v22.14.0 18.5-19.3 / v22.22.2 18.7-20.2，版本差异不可分辨）
vs landing bar >=10000us  ->  495-542x below EVEN IF ZEROED
带间比对：本轮 collected=15 与 R14-E 相同，collect 12.7-14.1 与 R15-E 的
  R14-E 种子比对运行（13.3-13.5）同带；outcomes 4.0-4.3 高于 R16-E 带
  2.3-2.7 为构成差（本轮 outcomes=4 vs R16-E outcomes=2）——本轮新轴 B
  价格表重构比 1.01-1.05 给出构成效应的机械证明（§2.1），无需另做种子
  比对运行
最保守合成口径（R7-E outcomes 7.0-7.3us + R12-E diagnose 分组上界 3.83-3.92us 同替）
  -> ~24.2-26.1us，仍 >=382x 低于门槛
configured-state: applyLearnedRouting(avoid=10, M=10) = 257-290ns/task （R13-E 258-312 /
                                                        R14-E 256-303 / R15-E 258-298 /
                                                        R16-E 269-294 同带）
                  applyLearnedRouting(空策略)        = 110-122ns/task （R15-E 110-125 /
                                                        R16-E 111-128 同带）
                  loadLearnedRouting fresh(ENOENT)   = 25.4-36.2us/次
                  loadLearnedRouting 现实 registry    = 74.3-89.1us/次 （R8-E 80-85 / R15-E 76.9-85.0 /
                                                        R16-E 76.6-84.8 同带，亚 ms 一次性，S8-E-1 带）
```

注：本轮夹具组成为 collected=15 / outcomes=4 / diagnoseGroups=1；diagnose
分组路径经 diagnoseGroups=1 全数走到。全部确定性 VERDICT 行 16 次运行
**逐位一致**，3/3 sanity 检查全过。**锚点复核成立：该切片不存在不推翻
既有排除就能达门槛的候选。**

## 2. 本轮两条实扫新轴 + 第十七组新角度枚举：有理由的空枚举

### 2.0 新轴 A：异常/拒绝路径定价（R17-B 方法移植，本切片首次）

前十六轮全部锚点与新轴只测过接受/快乐路径与协议噪声；本切片的全部
throw/catch/拒绝位点（`parseLearnedRoutingPolicy` 四个 fail-closed 门、
`parseObservedSignal` 九个 first-fault 门、`loadLearnedRouting` 的 ENOENT
吞异常通道与 corrupt-registry 重抛、`outcomesFromRoutedRun` 的环内
try/catch、`readBanditFile`/`ingestSubagentDirectory` 的 JSON.parse
catch）从未被定价。本轮 11 个测量胞、6 次运行（两 Node 版本各 3 次），
全部 14 项 sanity（每拒绝形态断言精确错误消息 + 双副本对生产函数逐字节）
通过：

```text
A1 parseLearnedRoutingPolicy（once-per-load fail-closed 门）:
  accept(N=10)=2,525-2,729ns | not-json=5,813-5,961ns  no-primary=3,053-3,160ns
  avoid[0] 失败=5,757-5,979ns  avoid[9] 失败=6,183-6,393ns
  （首/末位差 ~350-450ns = ~40ns/entry 校验走查，与 R16-E B1 61-63ns/entry
   apply 斜率同阶；not-json ≈ avoid[0] 说明 JSON.parse 抛出 + 异常机器
   ~1.9us 是拒绝主体）
A2 parseObservedSignal（零生产流量 extraSignals 面）:
  accept=262-278ns | 门 1(non-record)=2,523-2,560ns  门 5(bad-score)=3,112-3,216ns
  门 9(forge-guard)=3,493-3,548ns  （门位斜率 ~120ns/门）
A3 loadLearnedRouting 错误路径:
  fresh-root 总额=27.9-38.3us；其中吞异常机器（DomainValidationError 构造
  + throw/catch + /no registry snapshot/ 正则）=1,924-1,982ns = 5.1-7.1%
  （每 run 至多一次且仅发生在从未有 registry 的 fresh root 上）
  corrupt-registry fail-closed 重抛=63.7-73.6us/call（错误频率 ~0）
A4 outcomesFromRoutedRun 环内 try/catch 非抛出结构开销（副本对副本 A/B，
  两副本先证与生产函数输出逐字节相等）:
  6 次运行全函数单窗 delta 符号混合（+2/-6 … +5/-3），max-abs=40.5-380ns
  ——落在 R15-E A/A 地板（collect 单窗 ±217-529ns）之内 = 零成本 try 证实
A5 假想抛出行定价: parseOutcomeObservation accept=190-205ns，
  抛出行=6,177-7,076ns/行——且经可达性分析该 catch 对 schema 合法事件
  不可达（family/role/三版本字段/criterion/source 五重前置门控，
  NaN occurredAtMs 通过 typeof number 检查）
```

**裁决**：(a) **拒绝门族不产生候选**：全部拒绝路径是 once-per-load
fail-closed 契约门（生产频率 ~0：仅 corrupt/手写内容触发）或零生产流量面
（extraSignals，S5-E-3 已证零流量）；任何「先廉价预探针再 JSON.parse」=
S12-D-1 族（转义键 fail-open 角落）；任何门序重排改变 first-fault 语义
（错误消息是可断言契约，X0-5 族）。(b) **ENOENT 吞异常通道不产生候选**：
机器份额 1.9–2.0µs、每 run 至多一次；消除需拓宽 `loadAdaptationRegistry`
公开契约（X0-4 族）或 stat 预探针（S3-I-6 fail-closed 绕过族 + TOCTOU
发散），且错误消息正则通道是跨模块既定契约（`loadAdaptationRegistryOrNew`
同通道）。(c) **try/catch 结构零成本证实**：非抛出开销在协议噪声地板内，
catch 生产不可达 = 纯防御纵深，删之零收益且撤防（S9-D-3 零成本保留同式）；
前置预校验以避免假想抛出 = 复制 parser（X1-2 平行实现）+ 字面量再校验
已裁面（R10-E 谱系）。**该轴把「异常面藏着未测余量」的残余怀疑替换为
价格表：全部异常位点合计在生产路径上贡献 ~0（不可达）到 ~2µs
（fresh-root 一次），距落地线 4 个量级。**

### 2.1 新轴 B：逐事件类定价 + 尾部分布（R17-B 尾部方法移植，本切片首次）

十六轮全部 collect 锚点用混合 R1-A 夹具测总量；R14-E/R15-E/R16-E 三轮
带差异全部口头归因为「夹具构成效应」（collected=15/21/17），但逐事件类
边际价格从未被拆解测量。本轮 11 个事件类在共享前奏（discovery + 10 条
完整 MODEL_ROUTED，E=51 总长）上定价（边际价 = (类夹具成本 − 前奏成本)/40，
先对全部夹具做 1500 轮共同 JIT 预热），6 次运行（两 Node 版本各 3 次），
13 项 sanity（每类信号产出计数断言 + 空表/前奏零信号）全过、混合构成
VERDICT 行 6/6 逐位一致：

```text
B 价格表（ns/事件，6 次运行区间，降序）:
  result-fail=1,159-1,177   result-pass=1,119-1,134   result-unobserved=967-988
  peer=729-750              run-failed=632-646        judge=312-349
  answer-hit=288-334        route-complete=35-46      answer-miss=34-41
  route-minimal=19-25       noise=5-19
固定成本: empty=517-533ns  discovery-only=534-553ns  前奏(E=11)=1,791-1,912ns
尾部: max/p50 = 3.4-3.5x；全表饱和最坏预算（40 事件全为 result-fail）
  = 46.4-47.1us/run —— 仍低于落地线 ~213x
混合重构交叉检验（anchor 种子 0xe17e01，E=41，构成: answer-hit=1
  answer-miss=2 judge=5 noise=13 result-fail=5 result-pass=1
  result-unobserved=3 route-complete=3 route-minimal=7）:
  predicted=12.74-12.99us  measured=12.23-12.66us  ratio=1.01-1.05
```

**裁决**：(a) **价格表不产生候选**：最贵类 TASK_RESULT（1.12–1.18µs/事件）
的成本主体 = binding 五重条件 spread + `copyDefinedBinding` trim 探针 +
`taskSuccessFromResult` 分派 + truncate + baseSignal 十一重条件 spread——
逐项落在 S8-E-2（条件 spread，PIC 敏感）/ S9-E-3（分派树合一）/
S1-E-3、S2-E-5、S7-E-2（truncate 族）/ R15-E GC 天花板（全省分配类
~0.8µs/run）已裁面上；算术封死：生产 E=41 中 TASK_RESULT ≤26 条，整类
清零 ≤30µs；全表饱和 46.4–47.1µs 仍差落地线 213×。(b) **构成效应机械化
闭环**：重构比 1.01–1.05 证明混合成本 = 固定成本 + Σ(类价 × 类计数)，
R14-E(17.2–19.2)/R15-E(20.9–22.4)/R16-E(19.0–22.0)/本轮(18.5–20.2) 四轮
带差全部可由构成计数直接预测——十六轮反复出现的「构成效应」口头归因
自此有价格表证明，未来带间漂移可先查构成再疑代码。(c) **按类特化/分派
重排不产生候选**：四连独立 if 的扫描地板实测 5–19ns/事件（noise 类），
重排 = S3-E-1（互斥分派，实测抖动）换名；「构成自适应短路」（缺消费类
时跳 pass-2）= S9-E-2（预过滤负优化）/ S4-E-1（空快路径）换名。

### 2.2 第十七组枚举表

方法：本轮代码侧透镜「**异常/拒绝路径 × 事件类构成**」（两条测量轴的
对偶）：对第十七遍全量重读的 10 个源文件逐函数问 (a) 哪些 throw/catch/
拒绝位点存在、其任何重构是否等价且可能越线；(b) 哪些事件类扛着尾部
价格、其任何按类特化是否等价且可能越线；同时按惯例对本轮逐字新点名的
位置做换名检查（对照 EXCLUSIONS 全表 + 十六轮 E 报告 + 七轮空枚举面）。
扫描结果：**零个可立项形态**，逐类裁决如下（均未铸造新 ID）：

| 形态（本轮第十七遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **拒绝门重排/预探针**（`parseLearnedRoutingPolicy` 门序重排、JSON.parse 前廉价子串预探针、`parseObservedSignal` 九门重排——A 轴视角首次整体点名定价） | 每拒绝 3.1–6.4µs，生产频率 ~0 | **契约 + 频率双封死拒列（不铸 ID）**：first-fault 次序与错误消息是可断言契约（X0-5 族）；预探针 = S12-D-1 转义键 fail-open 族；拒绝仅发生在 corrupt/手写内容上，once-per-load | X0-5 / S12-D-1 + 本轮新轴 A1/A2 |
| **ENOENT 吞异常通道消除**（`loadLearnedRouting:119-127` throw/catch/正则改 stat 预探针或非抛出 loader 变体） | 1.9–2.0µs/run（仅 fresh root） | **辖区 + 契约双封死拒列**：需拓宽 `loadAdaptationRegistry` 公开契约（X0-4 族，且实现在 D 切片辖区）或 stat 预探针（S3-I-6 fail-closed 绕过 + TOCTOU 发散族）；错误消息正则通道是跨模块既定契约 | X0-4 / S3-I-6 / S9-G-1 族 + 本轮新轴 A3 |
| **corrupt-registry 重抛路径优化** | 63.7–73.6µs/次，频率 ~0 | fail-closed 契约本体（S2-I-1「损坏 registry 时错误路径发散」同族），任何加速/绕过都是撤防 | S2-I-1 / S9-G-1 族 + 本轮新轴 A3 |
| **`outcomesFromRoutedRun` 环内 try/catch 消除/外提**（`from-episode:227-247`） | 实测 ~0（符号混合 ±40–380ns 全函数） | **零收益 + 防御纵深拒列**：非抛出开销在 R15-E A/A 地板内 = V8 零成本 try；catch 对 schema 合法事件不可达（五重前置门控，本轮可达性分析首次成文）；删之零收益、撤类型级防线（S9-D-3 零成本保留同式）；前置预校验 = X1-2 平行 parser + 字面量再校验已裁面 | S9-D-3 / X1-2 / R10-E 谱系 + 本轮新轴 A4/A5 |
| **事件类分派重排**（`signals:139-242` 四连独立 if 按价格/频率重排；CHILD_MESSAGE 前置） | 扫描地板 5–19ns/事件 | 换名重提拒列：S3-E-1（互斥分派 = 八次测量异号纯抖动）逐字维持；价格表证明分派扫描不是成本位，类体才是 | S3-E-1 + 本轮新轴 B |
| **最贵类（TASK_RESULT）类体特化**（binding 五重 spread + copyDefinedBinding + taskSuccess 分派 + truncate + baseSignal 十一重 spread——B 轴视角首次整类定价 1.12–1.18µs/事件） | 整类清零 ≤30µs/run（生产 ≤26 条）；全表饱和 46.4–47.1µs | **算术 + 已裁面双封死拒列**：类体逐项 = S8-E-2 / S9-E-3 / S1-E-3 / S2-E-5 / S7-E-2 / R15-E GC 天花板已裁面；饱和预算距落地线 213× | S8-E-2 / S9-E-3 / S1-E-3 / S2-E-5 / S7-E-2 + 本轮新轴 B |
| **构成自适应短路**（按事件类构成跳 pass-2 / 跳 Map 构建 / 空表快路径） | pass 1+2 合计 ~12.7–14.1µs/run | 三向封死 + 硬不变量明令：S9-E-2（预过滤实测负优化）/ S4-E-1（空快路径，本轮 empty=517–533ns 与其 534–542ns 带一致）/ S1-E-1（合遍语义）；两遍语义按令未触碰 | S1-E-1 / S9-E-2 / S4-E-1 + 硬不变量 |
| **CJK 备选项「死模式」删除**（USER_NEGATIVE/USER_POSITIVE/PEER_NEGATIVE 中 `\b` 包裹的 不行/错误/可以 孤立不可匹配） | ~0ns（交替项编译后近零） | **不等价拒列**：反例 `"x可以x"` 命中（ASCII \w 与 CJK 毗邻产生边界），删除改变 .test 结果；R7-E §等价论证早已成文「孤立 CJK 关键词本就不匹配、毗邻 \w 才匹配」；使 CJK 词可独立匹配的「修复」= 行为变更（X0-2 功能工作，超出保行为优化职权，仅记录为跨职权观察） | R7-E S7-E-1 等价论证 + X0-2 |
| **answer-hit / judge 价格带重叠翻转**（288–334 vs 312–349，6 次运行排序不稳定） | 亚噪声 | 测量事实非候选：两类价差在 R15-E A/A 地板内，排序翻转即证不可证伪 | R15-E 新轴 B（地板） |
| **JUDGE_DECISION 双 Map.get / verdict 双分派 / 条件 spread 形状 / baseSignal 形状多态** | ns 级 | 换名重提拒列：S1-E-2 / S9-E-3 族 / S8-E-2 / S4-E-3 逐字维持；R14-E JIT 轴与 R15-E GC 天花板继续覆盖 | S1-E-2 / S9-E-3 / S8-E-2 / S4-E-3 |
| `from-episode` 内部字面量再校验 / Date.parse / familyFromRouted includes / isCompleteRoute 谓词序 / applyCascadeRetry / seen-Set 单探针 / outcomeKindFromResult 死形参 | ns 级 | 全部维持 R10-E..R16-E 拒列，位点未变；本轮 A5 为字面量再校验补上「接受侧 190–205ns/行」定价，仍属已裁面 | R10-E..R16-E §2 |
| `auto-loop` 三路拼接 / persistSignals filter 双求值 / 顺序读 / optimizedPolicy filter+map / candidatesFor().find | ns~µs 级或 I/O 支配 | 换名重提拒列：R12-E 行 13 / S1-E-4 / S1-E-5 / S2-E-6 / S1-D-2 / X0-3 逐字维持 | S1-E-4/5 / S2-E-6 / S1-D-2 / X0-3 |
| `bandit-store` / `readBanditFile` corrupt JSON catch（fail-soft → undefined） | 每 run 至多一次，µs 级 | 数据面 fail-soft 契约本体；R13-E #1 / R12-E 行 3 / S2-E-3/4 / S6-E-3/4 全维持 | S6-E-3/4 / S2-E-4 + 本轮新轴 A |
| I/O 契约面全体：fsync 丢弃、完整性重哈希跳过、no-change 跳写、双载入去重、corrupt registry fail-open、readdir 并行化、惰性载入、mkdir 提升、紧凑序列化 | 切片相关全部 ms 级余量所在 | 排除行逐字重现 + 硬不变量明令封死；`routingPolicyContent` pretty-print 参与 contentHash ⇒ 序列化变更破坏 CAS 契约 | X0-3 / S2-E-1/4 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1 / S2-I-1 / S9-G-1 族 + CAS |
| 测试专用面任何优化（`patterns` / `attribution` / `signatures` + 五个测试专用导出） | 不进任何生产路径 | 本轮交叉检索复核仍零生产调用方，自动拒列 | S1-E-7/8 / X2-6 族 |
| 死导出清理 / `scoreTaskResult` 尾部 `return 70` | 0ns 运行时 | 不立项：无收益路径即无候选资格；公开签名保留 | 硬不变量（公开签名） |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（十七轮）、src/test 自 R16-E 基线零漂移、调用位点行号全数复核一致（§1） | — |

**提议排除 ID：无（本轮无「新增排除 ID」表——空枚举未铸任何 S17-E-\*）。**
切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选。前十六轮
已逐类收口的十二个角度类之外，第十七遍以「异常/拒绝路径 × 事件类构成」
双轴普查新点名的全部维度（11 个异常胞 + 11 个事件类）全部给出实测价格
与契约/算术封死；本切片「无落地余量」证明自此覆盖 **CPU（R3-E..R14-E）
+ 规模（R14-E）+ 分配（R15-E）+ 协议（R15-E）+ 累积状态（R16-E）+
载荷形态（R16-E）+ 异常面（本轮）+ 构成面（本轮）八个维度**。

## 3. 逐文件收口（前十六轮收口之上的本轮新检查点）

| 文件 | 本轮第十七遍新视角检查 | 结论 |
| --- | --- | --- |
| `signals.ts` | **事件类价格表首次拆解**（11 类 5–1,177ns/事件、尾部 3.4–3.5×、饱和预算 46.4–47.1µs、重构比 1.01–1.05）；`parseObservedSignal` 九门拒绝定价 2.5–3.5µs（零生产流量面）；CJK 备选项删除判不等价（反例 `"x可以x"`，R7-E 论证复认）；分派重排 = S3-E-1 维持；两遍结构维持硬不变量；S1-E-1..3 / S2-E-5/7 / S4-E-1/3 / S6-E-1/2/5 / S7-E-1/2/4/5 / S8-E-2 / S9-E-2/3 维持 | 无候选 |
| `from-episode.ts` | **环内 try/catch 定价 + 可达性分析首次成文**（非抛出 ±40–380ns 符号混合 = 零成本；catch 对 schema 合法事件不可达 = 纯防御纵深；假想抛出行 6.2–7.1µs）；字面量再校验接受侧 190–205ns/行定价；既有拒列全维持 | 无候选 |
| `learned-routing.ts` | **拒绝门定价首次标定**（accept 2.5–2.7µs vs 拒绝 3.1–6.4µs；首/末位 first-fault 差 ~40ns/entry）；ENOENT 吞异常机器 1.9–2.0µs = fresh 总额 5.1–7.1%；corrupt 重抛 63.7–73.6µs 判 fail-closed 契约本体；apply 内部零改动（S13-B-1 边界 + R13-E #2/#3 不补铸）；S8-E-1 维持 | 无候选 |
| `task-success.ts` | TASK_RESULT 类体定价含完整 copyDefinedBinding/taskSuccess 分派路径（类价 1.12–1.18µs 的组成部分），逐项落在 S8-E-2/S9-E-3 已裁面 | 无候选 |
| `diagnostics.ts` | 本轮夹具 diagnoseGroups=1 全数走到分组路径（0.98–1.11µs 在带）；R14-E 双除法/filter().length 拒列维持 | 无候选 |
| `bandit-store.ts` | corrupt JSON fail-soft catch 判数据面契约本体（§2.2 行 13）；R13-E #1 位点复核未变不补铸；fsync/重哈希/mkdir/序列化按令未触碰 | 无候选 |
| `auto-loop.ts` | `ingestSubagentDirectory` JSON.parse catch 判 I/O 面 fail-soft 契约（S1-E-5 已裁面内）；三路拼接/filter 双求值/顺序读维持既有拒列 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | test-only 面复核仍零生产调用方，自动拒列；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核（依赖 `pnpm install
--frozen-lockfile`，pnpm 10.17.1）：

```bash
# Node v22.22.2（nvm；engines >=22.19.0 合规）
pnpm gate        # typecheck ✓ + lint ✓ + test ✓ + build ✓
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.14.0 与 v22.22.2 均过）
```

锚点脚本（临时置于 `/tmp/r17e-anchor.mts`，未入库以遵守无赢家纪律；差异
面见附录 A）共 **16 次独立运行**（8 × v22.14.0 + 8 × v22.22.2），每次
3/3 sanity 检查全过，全部确定性 VERDICT 行跨 16 次运行**逐位一致**。
代表性一次运行（v22.14.0）：

```text
VERDICT fixtures: E=41 collected=15 routedE=41 outcomes=4 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=13.0us outcomes=4.0us diagnose=1.05us bandit-build=0.8us | total in-slice CPU ~18.8us per run vs landing bar >=10000us (531x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=273ns/task | empty policy=116ns/task -> x10 tasks/run = 2.73us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=27.4us | realistic registry (baseline+1 candidate)=77.9us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

新轴脚本（`/tmp/r17e-reject.mts` 6 次运行、`/tmp/r17e-eventmix.mts` 6 次
运行，两 Node 版本各半；全部 sanity 14/14 与 13/13 通过、确定性 VERDICT
行逐位一致）代表性输出：

```text
BENCH A1 parseLearnedRoutingPolicy: accept(N=10)=2694ns | reject not-json=5940ns no-primary=3136ns avoid[0]=5946ns avoid[9]=6393ns (once-per-load fail-closed gates)
BENCH A3 loadLearnedRouting error paths: fresh-root total=38.3us | machinery micro: construct=1702ns regex=23.9ns throw+catch+regex=1943ns (share=5.1%) | corrupt-registry rethrow=64.3us/call (fail-closed, once-per-run at most)
BENCH A4 try/catch structural overhead (non-throwing path, per full call): deltas=[47.5ns, 5.6ns, -47.4ns, -4.5ns, -5.8ns, -11.8ns, -21.1ns, -1.7ns] | sign +2/-6 | max-abs=47.5ns
BENCH A5 parseOutcomeObservation: accept=198ns | hypothetical throwing row=6918ns/row (unreachable in production)
BENCH B per-event marginal price sheet (ns/event): result-fail=1159 result-pass=1119 result-unobserved=967 peer=732 run-failed=632 answer-hit=334 judge=312 answer-miss=37 route-complete=35 noise=19 route-minimal=19
BENCH B tail: max=result-fail@1159ns p50=334ns max/p50=3.5x | worst-class full production budget (40 events, all worst-class) = 46.4us/run
BENCH B reconstruction: predicted=12.80us measured=12.23us ratio=1.05 (price-sheet cross-check; taskId hit-rate and summary-shape differences explain the residual)
```

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round17-r17e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R16-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐形态有理由；第十七遍以「异常/拒绝路径
  × 事件类构成」双轴普查新点名的全部维度落进实测定价封死区、契约封死区
  或既有排除确认；两条新轴本身均以「无候选」收口，未发现第十三个机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令封死
  的 I/O 契约面或切片外辖区上；CPU 面即使清零也距门槛 495–542×，最
  保守合成口径 ≥382×）。
- 矩阵空洞：无（调用位点行号与 R8-E..R16-E 逐一相同；消费者集合与
  R16-E 逐一相同；配置态 live 面与载入面锚点全数复测在带内；双 Node
  版本交叉不可分辨；本轮带 18.5–20.2 由构成计数与价格表机械解释，
  重构比 1.01–1.05）。
- 异常面假说（「throw/catch 面藏着未测余量」）：以定价关闭（全部异常
  位点在生产路径上贡献 ~0（不可达）至 ~2µs（fresh-root 一次）；try/catch
  非抛出开销符号混合 = 零成本证实；拒绝门 3.1–6.4µs once-per-load）。
- 构成面假说（「构成漂移会让 collect 越线」）：以价格表关闭（最贵类
  1.16–1.18µs/事件、全表饱和 46.4–47.1µs/run 距落地线 213×；重构比
  1.01–1.05 使未来带间漂移可先查构成再疑代码）。
- S13-B-1 边界：`applyLearnedRouting` 本体零改动；重开条件
  （|avoid| ≈ 965–1,063，R16-E 量化）未触发、生产侧不可触发；R13-E
  三处无名微观未补铸未重开。
- 跨职权观察（非候选、非 ID）：`\b` 包裹的 CJK 词表项（不行/错误/可以）
  孤立文本永不匹配（R7-E 已成文，本轮复认）——若产品侧希望中文答复
  参与打分属功能变更（X0-2），不在本战役职权内，仅记录。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R16-E §6（含 R14-E 规模判据、R15-E
分配/GC/噪声地板判据、R16-E 累积状态与载荷形态判据），并新增两条可标定
判据：**(a) 异常面越线判据**——任一拒绝/异常路径的生产频率从 ~0 升为
每 run 常态（如上游开始常规写入 corrupt registry/policy，届时应先修
上游而非优化拒绝路径），或 try/catch 非抛出开销在未来引擎上稳定越过
R15-E 协议地板一个数量级；**(b) 构成面越线判据**——生产事件表构成使
最贵类计数 ≥~8,500 条/run（按本轮类价 1.16–1.18µs 折算 10ms），即
R14-E E\* 判据的按类精化；或任一 I/O 契约排除（X0-3 / S2-E-1/4 /
S1-G-1 / S1-E-4/5 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1）被正式推翻，
或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

主锚点脚本全文（`/tmp/r17e-anchor.mts`，`npx tsx /tmp/r17e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下四处替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R17-E；
`NOW = "2026-08-25T02:30:00.000Z"`；SECTION 1 的 seeds
`0xe17e01`（collect）/ `0xe17e02`（routed）/ `0xe17e03`（signals）——
三个生成器按序首选种子全部一次通过拒绝采样（collected=15>3、
outcomes=4>0、diagnoseGroups=1≥1），零出局；探针记录
seed→(collected,outcomes,diagnoseGroups)：01→(15,2,0)、02→(19,4,0)、
03→(16,4,1)；SECTION 2 的 `mkdtemp` 前缀 `r17e-fresh-`/`r17e-reg-`
与 detector identity `r17e-anchor`。其余逐字同 R10-E 附录 A。

新轴 A 脚本（`/tmp/r17e-reject.mts`）：复用同一 `mulberry32` /
`genRoutedEvents` 生成器（逐字镜像，routed seed `0xe17e02`）。测量段
A1：对 N=10 well-formed policy 的 `JSON.stringify` 内容及四个变体
（`"{not json"`、缺 primaryModelId、avoid[0] 缺 modelId、avoid[9] 缺
modelId）分别 bench `parseLearnedRoutingPolicy`（拒绝形态 try/catch
harness，reps 20000/50000），先断言精确错误消息。A2：对 valid signal
与三个变体（非对象、score 非数、taskSuccess 伪造门）bench
`parseObservedSignal`（reps 20000/100000）。A3：fresh mkdtemp 上
benchAsync `loadLearnedRouting`（reps 200）；同消息形状孤立 bench
`new DomainValidationError` 构造、`/no registry snapshot/` 正则、
throw+catch+regex 组合（share = 组合/总额）；再在
`adaptation/registry.json` 写入 `"{corrupt!!"` 的 mkdtemp 上 benchAsync
抛出型载入（try/catch harness，reps 200）。A4：`outcomesFromRoutedRun`
逐字副本两份（含/不含环内 try/catch），先断言两副本输出与生产函数
`JSON.stringify` 逐字节相等，3000 轮共同预热后 8 窗 A/B
（`delta = bench(带 try) − bench(不带)`，reps 4000/窗），输出逐窗
delta、符号谱、max-abs。A5：`parseOutcomeObservation` 对缺字段记录的
抛出定价（reps 20000）与 valid 行接受定价（reps 50000），并成文可达性
分析。sanity 14 项：全部拒绝形态错误消息断言 + 副本逐字节 + corrupt
重抛验证。

新轴 B 脚本（`/tmp/r17e-eventmix.mts`）：共享前奏 = discovery + 10 条
完整 MODEL_ROUTED（覆盖 tsk_0..tsk_9，E=11）；11 个类夹具 = 前奏 + 40 条
单类事件（route-complete / route-minimal / answer-hit "lgtm" /
answer-miss "hmm" / judge 三 verdict 轮转 / result-pass "tests passed" /
result-fail "failed to compile" / result-unobserved 多行 summary /
peer miss 文本 / run-failed / noise 三类型轮转）。先断言各类信号产出
计数（0/0/40/0/40/40/40/40/40/40/0）与前奏/空表零信号，1500 轮全夹具
共同 JIT 预热后测边际价 =（类夹具 − 前奏）/40（reps 20000；固定成本
empty/discovery-only/前奏另测 reps 200000/100000/40000）；尾部 =
价格表 max/p50 与全表饱和预算；重构交叉检验 = 对 anchor 混合夹具
（seed `0xe17e01`）按类计数（answer 命中性经 `scoreUserAnswer` 判定、
TASK_RESULT 按 verification 分档）求 predicted = discovery 固定成本 +
Σ(类价 × 计数)，与实测混合成本求 ratio。sanity 13 项。

16 次主锚点运行原始区间（8 × v22.14.0 + 8 × v22.22.2）：

```text
SLICE-CPU 总量:
  v22.14.0: 18.8 / 18.5 / 18.8 / 19.3 / 18.5 / 18.6 / 18.6 / 18.7 us
  v22.22.2: 18.8 / 19.3 / 19.0 / 18.8 / 18.7 / 20.2 / 18.9 / 18.8 us
collect:    12.7-14.1 us（v22.22.2 第 6 次 14.1 为全批最高，余 12.7-13.4）
outcomes:   4.0-4.3 us（v22.14.0 4.0-4.1 / v22.22.2 4.1-4.3）
diagnose:   0.98-1.11 us
bandit:     0.7-0.8 us
apply(non): 257-290 ns/task（v22.14.0 257-286 / v22.22.2 265-290）
apply(空):  110-122 ns/task
load fresh: 25.4-36.2 us
load real:  74.3-89.1 us
VERDICT 行: 16/16 逐位一致；sanity 3/3 x16 全过
```

6 次异常/拒绝路径轴运行原始值（3 × v22.14.0 + 3 × v22.22.2，交替）：

```text
A1 accept:     2694 / 2641 / 2680 / 2565 / 2525 / 2602 ns
A1 not-json:   5940 / 5872 / 5911 / 5961 / 5906 / 5813 ns
A1 no-primary: 3136 / 3107 / 3134 / 3160 / 3053 / 3147 ns
A1 avoid[0]:   5946 / 5965 / 5926 / 5914 / 5757 / 5924 ns
A1 avoid[9]:   6393 / 6317 / 6373 / 6288 / 6183 / 6293 ns
A2 accept:     264-278 ns | 门1: 2523-2560 | 门5: 3112-3216 | 门9: 3493-3535 ns
A3 fresh 总额: 38.3 / 34.8 / 30.3 / 34.2 / 27.9 / 32.8 us
A3 机器组合:   1943 / 1943 / 1982 / 1924 / 1974 / 1966 ns（share 5.1-7.1%）
A3 corrupt:    64.3 / 73.6 / 63.7 / 70.2 / 73.6 / 69.6 us
A4 max-abs:    47.5 / 380.0 / 168.1 / 52.0 / 40.7 / 40.5 ns（符号谱 +2/-6 …
               +5/-3，0/6 出现 8/8 同号）
A5 accept:     190-205 ns | 抛出行: 6918 / 6209 / 7011 / 6177 / 7076 / 6271 ns
sanity 14/14 x6 全过；副本逐字节 6/6；错误消息断言全过
```

6 次事件类定价轴运行原始值（3 × v22.14.0 + 3 × v22.22.2，交替）：

```text
result-fail:       1159 / 1171 / 1168 / 1166 / 1177 / 1168 ns/事件
result-pass:       1119 / 1130 / 1121 / 1133 / 1134 / 1130
result-unobserved: 967 / 970 / 977 / 969 / 988 / 973
peer:              732 / 729 / 738 / 735 / 750 / 733
run-failed:        632 / 637 / 637 / 640 / 643 / 646
judge:             312 / 326 / 315 / 329 / 349 / 348
answer-hit:        334 / 331 / 330 / 290 / 288 / 297
route-complete:    35 / 43 / 38 / 39 / 46 / 42
answer-miss:       37 / 34 / 35 / 35 / 37 / 41
route-minimal:     19 / 22 / 20 / 20 / 23 / 25
noise:             19 / 6 / 5 / 5 / 5 / 5
固定: empty 517-533 / discovery-only 534-553 / 前奏 1791-1912 ns
尾部: max/p50 3.4-3.5x；饱和预算 46.4 / 46.9 / 46.7 / 46.7 / 47.1 / 46.7 us
重构: predicted 12.74-12.99 / measured 12.23-12.66 us / ratio 1.01-1.05
混合构成 VERDICT 行: 6/6 逐位一致；sanity 13/13 x6 全过
```
