MODEL_SLUG=claude-fable-5-thinking-xhigh

# R17-D：`src/adaptation/` 第十七遍搜查报告（Round 1–16 同区第十七遍）

**战役:** 全库持久 SOTA 优化 Round 17 / R17-D（Round 1–16 同区第十七遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `bceb989`（R17-B 收口、R17-D 分派 tip；含 R16 全轮与 R17-A/R17-C 在飞前序）
**分支:** `cursor/r17-d-adaptation-seventeenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S17-D-\* ID，切片零代码
改动；整片预算收口第十七次复核成立，并把 R17-B 为 routing 切片开辟的
「路径完备性透镜」移植到本切片，完成 fail-closed 拒绝路径全族普查——
十六遍此前全部在**成功路径**上剖析本切片（R13-D 时间相位、R14-D
E/状态规模、R15-D 分配/GC/MDE、R16-D payload 字节形态），而
`DomainValidationError` 终端（生产命令可命中的每一条拒绝出口）从未
作为一族定价（仅两条曾顺带钉过：S12-D-1 的边界闸探针与 R15-D 的
OrNew 未中探针）。本轮对 eval / promote / rollback / retire / load
五个命令类共 **28 条终端路径**（25 条抛出终端 + 2 条设计性拒收 +
1 条未中路径）逐条定价并逐条复验 fail-closed 契约，轴一次性关死：**

1. **频率上界（结构事实）**：本切片的拒绝是**整命令终止**（单次抛出
   即中止事务，与 B 切片逐任务 refusal 不同），拒绝次数 ≤ 命令次数
   ——once-per-command 类按构造成立；生产形态语料在全部锚点上
   **0 拒绝**（20+ 次 eval、K=60 promote、K=60 rollback 全部成功），
   每一条拒绝都需要毒化夹具（毒化输入 / 篡改状态 / 契约违例）才可达。
2. **单价上界（实测）**：每一条拒绝都比它所中止的成功命令便宜。最贵
   拒绝 e12（零 PASS/FAIL 记录）**2,203–2,276 µs = 成功 e2e 的
   66–70%**——按代码序付完 assignTasks×2 屏障（S2-D-4/S10-D-2 契约
   属地）之后才抛，其「前置短路」优化**就是已关 S5-D-4**（双故障
   错误选择发散，不可重开）。次贵 e11（隔离重叠）1,335–1,546 µs
   （40–48%）。其余 eval 拒绝 5–981 µs；promote 侧 p1–p7 全部
   2.6–46.4 µs；rollback/retire 侧 0.9–8.0 µs；load 终端 27–376 µs
   （l3 = S8-D-1 明令保留的篡改跳闸按规格触发）。
3. **天花板裁决**：把全部 28 条终端的成本**清零**，稳态节省 = 0
   （生产语料零命中）、毒化命令节省 ≤ 2.28 ms/命令——距落地线
   （数十–数百 ms）一个数量级以上；且该 2.28 ms 的每一段都钉在
   已关 ID（S5-D-4 / S2-D-4 / S10-D-2 / S11-D-2 / S8-D-1 /
   S3-D-3 / S9-D-2 / S1-D-9）、契约面（rejected 台账追加、错误消息
   断言面）或解析地板上。**拒绝通道两端（频率 × 单价）同时封死，
   该轴无新缝隙。**普查同时免费复验了 25 条抛出终端全部按契约抛
   `DomainValidationError` 且消息逐条匹配（fail-closed 契约第十七次
   复核成立）。

配置态 eval 地板同 VM 复锚（指令要求）：S0c 主锚 **3.57–4.00 ms**、
同夹具 S2 成功参照 **3.25–3.33 ms**——与 R16-D 3.47–3.62 重叠，
R15-D 批测带 3.10–3.25 与 S2 参照带相接，R14-D 4.05–4.21 于上沿
相邻；历史带 3.1–4.2 内，零 diff 下带间位移是 VM/窗口方差而非代码
效应。清零切片内计算仍差落地线一个数量级——预算结论第十七次成立。

切片 14 个文件自 R1-D 基线（`82bef36`）经 R2-D … R16-D 至本轮基线
（`bceb989`）**逐字节未变**（`git diff 82bef36..HEAD --
src/adaptation/` 为空，十七遍全程零 diff；`ls` 复核恰 14 文件）。
调用面维持最强冻结并再延一轮：R16-D 基线（`02c5442`）以来
`git diff 02c5442..bceb989 -- src/` **完全为空**（期间 10 个提交全部
为 docs(sota-opt)），生产调用方经全库 grep 首手复核仍为
`cli/adapt.ts` / `learning/auto-loop.ts` / `learning/from-episode.ts` /
`learning/learned-routing.ts` 四处（`tracking/analysis.ts` 三个导入
均为 `import type`，零运行时边；`privacy/record-classes.ts` 仅路径
字符串字面量）；`createAdaptationDriftMonitor` / `paretoFront` /
`proposeCandidates` / `mutateOnce` / `retireVersion` /
`evaluateProposalShadow` 在 `src/` 生产面依旧零切片外调用方
（monitor 格维持 N/A）。R1-D…R16-D 全部测量与排除**全额转移**。
全部测量在 engines 合规 Node **v22.22.2** 完成（与 R13-D…R16-D
同引擎，带可比），三次独立运行全绿（`All checks passed.`）。

按令对排除表做换名拒绝审计：本轮逐文件重读与拒绝路径普查中识别的
全部角度经逐条检查判定为既有 ID 的换名/同方案异站、被调方/契约属地
或地板项（8 项新记录，§4；指令点名项逐条核过，§5），按「宁可空枚举
也不铸装饰性 ID」的裁决纪律**一律不铸新 ID**；R13-D §4 11 项、
R14-D §4 8 项、R15-D §4 8 项、R16-D §4 8 项换名检查未重铸。R16-D
payload 形态五轴（L schema 封闭 / H ≈4,486–4,684 / O ≈2,646–2,733 /
σ 构造封闭 / P-eval ≈249–259 KB、P-promote ≈2,615–2,698 KB）、
R15-D 分配/GC（~4.4–4.5 MB/eval；GC 硬顶 37–43 µs）与 A/A MDE
（窗口中位 |263–435| µs）、R14-D 规模越线标定（10 ms @ 17–18×）
**未重铸、未重测、原样引用**——本轮普查在固定生产形态（E=200、
9.2 KB 策略、配置态注册表）上进行，与上述轴全部正交。
S12-D-1/2、S11-D-1/2、S10-D-1/2、S9-D-4、S8-D-1..3 族未以任何形态
重提。硬不变量全程满足（零 diff 天然保持）：CAS/凭据/数据面契约、
双 LCB 与双归因、proposal-first、阈值、公开签名、fsync 与完整性
重哈希全部原样；不声称 Outcome-supported，Checkpoint F-PROD 仍开放
（ADR-005）。第十七遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/adaptation/` 全目录，**恰 14 文件**（`ls` 复核）：
  eval-routing / promotion-rules / promotion / registry / mutate /
  pareto / candidate / rollback / reflection / approval-profile /
  resource / monitor / retirement / active-pointer，**全部实际重读**
  （不靠记忆）。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表，含 S9-D-4、
  S12-D-1..2、S8-D-1..3）/ round-17/PLAN.md / round-16/R16-D.md
  （空枚举；payload 形态 L/H/O/σ/P）/ round-15/R15-D.md（空枚举；
  分配/GC + A/A MDE）。R17-B（拒绝/异常路径定价格首创，routing
  切片）作为方法学参照另读。
- 基线验证：从最新 `origin/cursor/sota-persistent-opt-83a1`
  （tip = `bceb989`）切出工作分支；`git diff 82bef36..HEAD --
  src/adaptation/` **为空**（第十七轮字节恒等）；`git diff
  02c5442..bceb989 -- src/` **为空**（自 R16-D 基线以来 `src/` 生产面
  零字节变更，期间 10 个提交全部为 docs(sota-opt)）。EXCLUSIONS.md /
  PROGRESS.md / PLAN.md 未触碰。未开 PR。未 force-push。
- 邻居只读：`src/learning/`、`src/cli/adapt.ts`、`src/routing/`、
  `src/experiments/`（isolation.ts 隔离守卫事实）、`src/domain/`
  （errors/ids/hash）、`src/tracking/analysis.ts`、
  `src/privacy/record-classes.ts`。零编辑。
- 环境：本 VM 默认 Node v22.14.0，按 engines（>=22.19.0）切至
  **v22.22.2**（nvm；与 R13-D…R16-D 同引擎）后
  `pnpm install --frozen-lockfile`；全部测量与门禁在 v22.22.2 完成。
- 硬不变量全程满足（本轮零 diff，天然满足）：分析不改 in-flight run、
  Tracking 无命令权、H/score 不写路由 PASS/FAIL、live = R0 等价、
  双 LCB（beta+normal）与双归因（logit+prob-add）保留（X0-11）、
  提升 proposal-first、无 Outcome-supported 声称（Checkpoint F-PROD
  开放，ADR-005）、阈值不动、CAS/凭据/数据面契约与公开签名不动、
  fsync 不删（S8-D-3）、restore/getActiveContent 完整性重哈希不删
  （S8-D-1..2）、fail-open 编码禁区不碰（S9-D-4 / S12-D-1 廉价形态）、
  测试未改。
- 无赢家 ⇒ 零生产改动，拒绝路径普查仿真只留在本报告附录，**不入库**
  （无 `scripts/round17-r17d-*.ts`）。

## 1. 配置态 × 命令类矩阵同 VM 复锚（指令要求的第二步；eval 地板重锚）

三次独立运行（同脚本、seeds `0xd17d01`–`0xd17d06`），构造与
R8-D…R16-D 逐字段同构（配置态：9.2 KB 路由策略 40 avoid + 40 prefer +
7 assignments；8 个 prompt 身份、5 promote、1 pending、2 rollback、
2 retire；registry.json 146.2 KB、V=15 C=9 L=11 B=18、blobs 119.5 KB）。

| 矩阵格 | 默认态 | 配置态 | 与 R16-D（同引擎 v22.22.2）比对 |
| --- | --- | --- | --- |
| load | 0.10–0.12 ms | 0.61–0.69 ms | R16-D 0.55–0.67，重叠 |
| save(+fsync) | 0.37–0.58 ms | 1.44–1.72 ms | R16-D 1.50–1.52 带内/紧邻；fsync 方差支配 |
| eval 端到端（E=200） | 4.07–4.26 ms | **3.57–4.00 ms**（同夹具 S2 成功参照 **3.25–3.33 ms**） | R16-D 3.47–3.62 与本带重叠；R15-D 批测 3.10–3.25 与 S2 参照带相接；R14-D 4.05–4.21 于上沿相邻——历史带 3.1–4.2 内。零 diff 下带间位移是 VM/窗口方差而非代码效应 |
| promote（op-only，K=60） | — | 41.1–45.3 µs | R16-D 45.5–45.8 紧邻（hash32 单次 9.6–9.9 µs，R16-D 9.6–10.0 重叠） |
| rollback（user CAS，op-only） | — | 3.1–3.3 µs | R16-D 3.1–3.3，重合 |
| monitor | N/A | N/A | 仍无生产调用方（grep 首手复核） |

**预算结论复核成立**：eval 地板 ~3.3–4.0 ms（本 VM 配置态主锚
3.57–4.00、成功参照 3.25–3.33、toy 4.07–4.26，均与历史带重叠或
紧邻）、save/fsync 支配持久化路径的图景第十七次成立。R14-D 的实测
越线标定、R15-D 的分配/GC/MDE、R16-D 的形态五轴不重测、原样引用
（§5 / §8）。

### 1b. `evaluateProposalShadow` 交叉核对（experiments 切片属地）

P=200/A=100 → 1.3–1.4 ms；P=2000/A=1000 → 122.0–123.8 ms。与 R16-D
（1.3–1.4 / 120.2–122.9 ms）同带或紧邻；`reflection.ts` 只是薄委托，
实现在 `src/experiments/`（切片外），维持锚点性质，不据此铸 ID。

### 1c. 调用图第十七次复核（只读邻居 grep 事实）

- `loadAdaptationRegistry(OrNew)` / `saveAdaptationRegistry` /
  `evalRoutingPolicy` / `promoteWithRegistry`：生产调用方仍恰为
  `cli/adapt.ts`、`learning/auto-loop.ts`、`learning/from-episode.ts`、
  `learning/learned-routing.ts` 四处——单命令单事务模式第十五次
  复验成立。
- `tracking/analysis.ts` 对本切片的三个导入均为 `import type`
  （编译期擦除，零运行时边）；`privacy/record-classes.ts` 仅含
  adaptation 路径字符串字面量。
- `createAdaptationDriftMonitor` / `paretoFront` / `proposeCandidates` /
  `mutateOnce` / `retireVersion` / `evaluateProposalShadow`：`src/`
  生产面零切片外调用方——monitor/pareto/mutate/retirement/reflection
  五模块维持「无生产调用方」格局，任何针对它们的优化收益恒为 0
  （R4-D 事实第十四次复验）。

## 2. 新格：fail-closed 拒绝路径全族普查（R17-B 路径完备性透镜在本切片的移植收口）

方法：十六遍的全部剖析都在**成功路径**上采样——时间相位（R13-D）、
规模（R14-D）、分配/GC/MDE（R15-D）、payload 形态（R16-D）都测的是
「命令走完」的成本。本切片每个命令类还各有一组 fail-closed 终端
（`DomainValidationError` 抛出点），其中只有两条曾被顺带定价
（S12-D-1 的 createCandidate 边界闸探针 9.1–10.4 µs；R15-D 的
OrNew ENOENT 探针类）。本轮以毒化夹具逐条驱动每一条可达终端，
逐条断言错误类型与消息（契约复验），并以三次独立运行定价。
每格判读以 R15-D 实测 MDE（窗口中位 0.26–0.44 ms）为噪声标尺。

### 2a. 拒绝普查（频率端）

生产形态语料在全部锚点上 **0 拒绝**：S0/S0c 的 20+ 次 eval、S0d 的
K=60 promote 与 K=60 rollback 全部成功，无一命中拒绝终端。结构事实：
本切片的拒绝是**整命令终止**——`evalRoutingPolicy` /
`promoteWithRegistry` / `rollback` / `retire` / `load` 的任何一条
校验失败即单次抛出、事务中止（无逐条目重试、无部分完成），因此
拒绝次数 ≤ 命令次数，**once-per-command 类按构造成立**。到达任何
一条拒绝都需要毒化前置（契约违例输入、损坏/篡改的状态文件、并发
交错），且每次命中都是操作者可见的命令失败——不存在「拒绝风暴」
静默积累的形态（对照 R17-B §2d 同判）。

### 2b. eval 侧拒绝定价 e1..e12（端到端，同夹具成功参照 3.25–3.33 ms）

| id | 站点（毒化夹具） | 单价（µs/reject，三运行带） | 占成功 e2e | 付费内容（钉名） |
| --- | --- | --- | --- | --- |
| e1 | candidateId 格式非法 | **5** | ~0% | 仅 id 正则——在注册表装载**之前**拒绝（最便宜终端） |
| e2 | 候选不存在 | 625–658 | 19–20% | 全量 registry 装载（S11-D-2 装载链 + S8-D-1 明令重哈希） |
| e3 | 身份 kind 错误（prompt 候选） | 622–763 | 19–23% | 装载 + 候选查找 |
| e4 | 候选策略非 JSON | 534–739 | 16–23% | 装载 + JSON.parse 尝试（S1-D-9 域） |
| e5 | forbidden 字段（浅、首键） | 607–674 | 18–21% | 装载 + walk 前缀（S9-D-4 健全形态，明令保留） |
| e6 | forbidden 字段（深、末叶） | 664–798 | 20–25% | 装载 + 候选侧全量 walk（S4-D-5/S9-D-4 上界） |
| e7 | dataset manifest ENOENT | 726–815 | 22–25% | 装载 + 双策略链 ×2 + readFile 未中 |
| e8 | manifest JSON 损坏 | 776–780 | 23–24% | + 读入 + parse 尝试 |
| e9 | episodes[0] 非法 role | 900–935 | 27–29% | + manifest parse + 1 次 episode parse |
| e10 | episodes[199] 非法 role | 953–981 | 29–30% | + 200 次 episode parse（fail-fast 按索引线性；单遍解析地板） |
| e11 | 隔离重叠（workspace=stateRoot） | 1,335–1,546 | 40–48% | + dataset 全解析 + 守卫前缀检查（S3-D-3/S9-D-2 域） |
| e12 | 零 PASS/FAIL 记录 | **2,203–2,276** | **66–70%** | 直到 assignTasks×2 屏障（S2-D-4/S10-D-2 契约属地）之后的一切——最贵拒绝；其「前置短路」= 已关 S5-D-4（双故障错误选择发散），永不重开 |

三条结构读数：(a) 拒绝单价随站点深度单调走高，与代码序逐段吻合
（e1 → e12 每级增量都能钉到具体已关 ID 或被调方属地）；(b) 最贵
拒绝 e12 仍**便宜于它所中止的成功命令**（66–70%）——本切片不存在
「拒绝比成功贵」的倒挂；(c) e6−e5 增量（27–124 µs）即候选侧全量
walk 对 walk 前缀的差，与 R16-D P 轴 walk 斜率一致（交叉核对）。

### 2c. promote 侧拒绝定价 p1..p7（op-only，无 I/O）

| id | 站点 | 单价（µs/reject） | 付费内容（钉名） |
| --- | --- | --- | --- |
| p1 | evalReport 缺失 | 2.6–3.0 | 仅候选查找——最便宜 promote 拒绝 |
| p2 | evalReport contentHash 不匹配 | 15.1 | 1× hash32 过 9.2 KB 策略（门链第一环，S1-D-7/S7-D-1 域） |
| p7 | provisional 门（acceptProvisional 未置） | 16.3–16.4 | 包装层门链至 provisional 检查 |
| p3 | review actorId 不匹配 | 42.8–43.1 | 包装门 + prepare 门经 putContent（~3× hash32） |
| p4 | 状态不可提升（rejected） | 43.1–43.3 | prepare 门至状态检查（独立夹具，状态翻转不扰共享态） |
| p6 | explicitApproval 缺失 | 45.2–46.4 | 近乎完整 preparePromotion（全部门、不铸版本）——最贵 promote 拒绝 |
| p5 | commit-CAS 交错失败 | 8.3–8.9 | commit 门 + CAS + **rejected 台账追加**（契约性突变，K=40 交错夹具逐一驱动并断言台账内 `kind:"rejected"` 落账） |

全档 ≤ 46.4 µs = 成功 promote op（41.1–45.3 µs）同量级、成功 eval
e2e 的 ~1.4%。门链每一次 hash 都是 fail-closed 验证（S1-D-7 去重可
观察发散；S7-D-1 重存 fail-closed），不可折叠——p2/p3/p6 的单价
就是门链本身按规格工作的价格。

### 2d. rollback / retire 拒绝 + 设计性拒收定价 r1..r5、t1（op-only）

| id | 站点 | 单价（µs/op） | 付费内容（钉名） |
| --- | --- | --- | --- |
| r1 | 未知回滚目标 | 4.6–4.9 | validate + versionsById 未中 |
| r2 | 目标已退役 | 5.1–5.5 | validate + versionsFor 扫描 + retired 集命中（S6-D-1 守卫域） |
| r3 | 陈旧 CAS（expected 落后于 active） | 6.8–8.0 | 全部门 + CAS 不匹配 |
| t1 | 退役活动版本 | 3.5–4.1 | 查找 + active-pointer 检查 |
| r4 | degradation 提案（ok:false，设计性拒收，非抛出） | 1.0–1.1 | 全部门 + alreadyProposed 去重（稳态零追加） |
| r5 | 幂等 no-op（active==target，非抛出） | 0.9 | validate + versionsFor 扫描 + 早退（S7-D-4 保序） |

全档 ≤ 8.0 µs——rollback/retire 的整个拒绝面是纳秒–个位微秒级，
比其成功 op（3.1–3.3 µs）同量级，无任何可寻址体量。

### 2e. load 侧终端 l1..l3

| id | 站点 | 单价（µs） | 付费内容（钉名） |
| --- | --- | --- | --- |
| l1 | OrNew ENOENT 未中（非抛出） | 27.3–29.2 | R15-D once-per-command 探针类（原样复测，带一致） |
| l2 | registry JSON 损坏 | 58–61 | 读 + parse 尝试 |
| l3 | 篡改 blob 完整性跳闸 | 359–376 | 读 + parse + 重哈希至首个不匹配——**S8-D-1 明令保留的完整性门按规格触发**（跳闸即证篡改反例被拦，fail-closed 实测复验） |

### 2f. 天花板裁决（频率 × 单价双端封死）

- **频率端**：生产语料 0 命中；每命中 ≤1 次/命令且操作者可见。
- **单价端**：28 条终端全档 0.9 µs – 2.28 ms，最贵者是其所中止
  成功命令的 66–70%——上界被成功路径预算（已第十七次收口）钉死。
- **清零假设**：全部终端成本归零，稳态节省 = 0，毒化命令节省
  ≤ 2.28 ms/命令——距落地线（数十–数百 ms）一个数量级以上。
- **逐段归因**：e12 的前置短路 = S5-D-4（永不重开）；e2..e11 的
  装载/walk/解析/守卫段 = S11-D-2 / S8-D-1 / S1-D-9 / S9-D-4 /
  S4-D-5 / S3-D-3 / S9-D-2（全部已关或明令保留）；p 族 = S1-D-7 /
  S7-D-1 门链 + 台账契约；r/t 族 = S6-D-1 / S7-D-4 域；l 族 =
  S8-D-1 明令门 + 单次解析地板。**没有一段属于未命名的新缝隙。**

**该轴收口，无新 ID。**

## 3. 候选总表

**空。** 本轮真实新增候选为零：拒绝路径普查的每一 µs 都钉在已关
ID、契约面或解析地板上（§2f 逐段归因）；频率端被 once-per-command
结构 + 生产零命中双重封死。结构位 × 消除机制矩阵经 §4 换名检查后
无一格为新。按 PLAN「先复核再找新角度，不要硬凑」与指令「宁可空
枚举也不铸装饰性 ID」，不铸任何 S17-D-\*。

## 4. 枚举路径：识别但按更名/重现规则不铸 ID 的角度（8 项新记录）

R13-D §4 11 项、R14-D §4 8 项、R15-D §4 8 项、R16-D §4 8 项换名
检查未重铸（未以任何形态重提）。本轮逐文件重读与拒绝路径普查中
新识别、判为既有 ID 换名/被调方属地/契约变更类/地板项、**未铸 ID**
的角度：

| 本轮识别的落点 | 命中的闭合条目 | 处置 |
| --- | --- | --- |
| e12 派生：零 PASS/FAIL 前置短路（屏障前先数记录，省 2.2 ms/毒化命令） | **S5-D-4 本体**（全 UNOBSERVED 前置短路——双故障错误选择发散已裁） | 永不重开 |
| e2/e3 派生：装载前候选存在性预探针（侧车索引避免全量 load+rehash，省 ~0.6 ms/毒化命令） | **S11-D-2**（装载链融合上界 72–74 µs 已裁）+ **S8-D-1**（重哈希不可跳）+ 状态面契约（单文件快照）变更；且 once-per-command µs 类 | 不铸 ID |
| e10 派生：episode 解析批量预验证/早退重排 | **单遍解析地板**（每 episode 恰一次 parse，fail-fast 已按索引线性）+ R16-D §4 manifest 流式解析同域（平台面变更） | 不铸 ID |
| e11 派生：隔离守卫前移（dataset 解析前先建守卫以更早拒绝） | **结构不可倒置**：守卫需要 episodes 的 originalWorkspace 全集，只能在解析后构造；守卫本体成本域 = **S3-D-3 / S9-D-2**（已裁） | 不铸 ID |
| p5 派生：commit-CAS 拒绝路径省略 rejected 台账追加 | **契约性突变**（审计面：CAS 失败必须落账；测试断言对象） | 永不重开 |
| l3 派生：篡改跳闸部分重哈希/抽样验证 | **S8-D-1 本体**（明令保留；篡改反例）——现行已首败即停，抽样即 fail-open | 永不重开 |
| 拒绝消息预计算/错误对象享元/惰性栈捕获族 | **R17-B 拒绝通道收口同判**：once-per-command µs 类 + 错误契约面（消息是调用方与测试的断言对象） | 不铸 ID |
| e1 派生：id 正则校验前移至 CLI 参数面（更早短路） | **切片外**（`cli/adapt.ts` 属 I 切片）+ once-per-run CLI 噪声类（5 µs 本体） | 不铸 ID |

以上之外，结构位 × 消除机制矩阵的剩余格全部为空。

## 5. 更名/重现检查（指令点名项逐条）

| 点名项 | 本轮是否触碰 | 检查结论 |
| --- | --- | --- |
| S9-D-4 廉价 `toLowerCase` 跳过（U+212A fail-open） | 否 | 未重提任何折叠变体；e5/e6 只测量健全形态 walk 在拒绝路径上的价格（0.6–0.8 ms 单价内含），不改动（永不重开） |
| S12-D-1 边界闸 JSON.parse 预探针（廉价形态同族） | 否 | createCandidate 站点未重提；普查仅引用其已定价探针（9.1–10.4 µs）为「此前唯二已钉拒绝」之一 |
| R16-D payload 形态轴（L/H/O/σ/P） | 否（不重铸/不重测） | 普查在固定生产形态（E=200、9.2 KB 策略）上进行，与形态五轴正交；L 65 字符 fail-closed、H/O/P 穿越点、σ 全域摆幅原样引用 |
| R15-D 分配/GC（4.4–4.5 MB/eval；GC 硬顶 37–43 µs）与 A/A MDE（263–435 µs） | 否（不重铸/不重测） | MDE 原样引用为普查判读标尺；l1 探针带（27.3–29.2 µs）与 R15-D 类别一致 |
| R14-D 规模越线标定（10 ms @ 17–18×；S6-D-5 50 ms @ 101–108×） | 否（不重铸/不重测） | §8 原样引用；普查固定 E=200 | 
| S8-D-1 / S8-D-2 完整性重哈希 | 否 | l3 实测其跳闸价格（359–376 µs）并复验 fail-closed 契约按规格触发；§4 把「部分重哈希/抽样」判为永不重开 |
| S8-D-3 saveAdaptationRegistry drop-fsync | 否 | save 锚点原样含 fsync（1.44–1.72 ms） |
| S5-D-4 全 UNOBSERVED 前置短路 | 否（不重开） | e12 把它测为最贵拒绝的唯一「显而易见优化」并按其闭合裁决自拒 |
| S1-D-7 / S7-D-1 promote 门链 | 否 | p2/p3/p6 单价即门链按规格工作的价格；未提任何折叠 |
| R10-D…R16-D 空枚举收口 | 否 | 未回填任何 S13-D-\* … S16-D-\* |
| X\* / S1-\* … S16-\* 全表 | 否 | 逐文件重读对表复核，零重开 |

## 6. 前后对比

无。零生产改动；切片十七遍逐字节不变（`82bef36 == bceb989` on
`src/adaptation/`）。本分支仅含本报告一个文件。

## 7. 测试与门禁

零代码改动下相关套件与全量门禁复核，全绿（Node v22.22.2，pnpm 10.17.1）：

```bash
npx tsx --test "test/unit/adaptation/**/*.test.ts" "test/acceptance/adaptive-loop.test.ts"
# tests 101 / suites 11 / pass 101 / fail 0
pnpm gate   # typecheck && lint && test && build — 全绿（exit 0；tests 1169 / pass 1168 / skipped 1 / fail 0）
```

仿真（临时脚本 `/tmp/r17d-sim.mts`，未入库——无赢家不落死代码；完整
源码见附录，seeds `0xd17d01`–`0xd17d06`，三次独立运行全部
`All checks passed.`，全部 25 条抛出终端逐次断言 `DomainValidationError`
与消息匹配，定价带逐位或同带一致）：

```text
run 1（摘录）:
S0c configured anchors: load=0.67ms save=1.44ms | eval e2e=3.57ms
S2 (ref=3.33ms): e1=5us e2=625 e6=664 e10=953 e11=1335 e12=2225 (67%)
S3: p1=3.0us p2=15.1 p6=45.6 p5=8.3 | S4: r3=8.0us t1=3.5 r5=0.9
S5: l1=28.3us l2=61 l3=359

run 2（摘录）:
S0c: load=0.61 save=1.69 | eval=4.00ms
S2 (ref=3.32ms): e1=5us e2=658 e6=688 e10=981 e11=1383 e12=2203 (66%)
S3: p1=2.6us p2=15.1 p6=46.4 p5=8.9 | S4: r3=6.8us t1=4.1 r5=0.9
S5: l1=27.3us l2=58 l3=376

run 3（摘录）:
S0c: load=0.69 save=1.72 | eval=3.58ms
S2 (ref=3.25ms): e1=5us e2=633 e6=798 e10=969 e11=1546 e12=2276 (70%)
S3: p1=2.6us p2=15.1 p6=45.2 p5=8.4 | S4: r3=7.7us t1=3.5 r5=0.9
S5: l1=29.2us l2=59 l3=360
```

## 8. 新增排除 ID

**无。** 空枚举（与 R13-D / R14-D / R15-D / R16-D / R17-B 同口径）：
本轮识别的全部角度均为既有 ID 的换名/被调方属地/契约变更类/地板项
（§4），不铸装饰性 ID。

重开条件：拒绝路径轴仅在两种情形下需要重测——(a) 某生产写入方开始
以稳态频率产出契约违例输入（结构上不可达：每次拒绝都是操作者可见的
整命令失败，无静默积累通道）；(b) 错误契约面本身变更（消息/类型/
台账形态）。单价端上界被成功路径预算钉死（拒绝 ≤ 成功 e2e 的
66–70%），故成功路径的既有重开条件自动覆盖拒绝路径：E 跨量级增长时
首先重开 S6-D-5（R14-D 裁定不变）；P-eval ≈ 249–259 KB、
O ≈ 2,646–2,733 字符、H ≈ 4,486–4,684 字符、
P-promote ≈ 2,615–2,698 KB（R16-D 裁定不变）；L/σ 轴 schema/构造
封闭不随拒绝路径变化。

## 附录：确定性仿真脚本（完整，可复现）

临时路径 `/tmp/r17d-sim.mts`，`npx tsx /tmp/r17d-sim.mts` 运行
（仓库根目录，依赖已装，Node ≥22.19）；未入库（无赢家）。seeds
`0xd17d01`–`0xd17d06`；三次独立运行全部 `All checks passed.`。

```typescript
/**
 * R17-D deterministic anchor + fail-closed rejection-path census simulation
 * (seventeenth pass).
 * (1) Re-anchors the configured-state x command-class matrix closed by
 *     R9-D..R16-D (eval toy/configured, load/save floors, promote op incl.
 *     the hashCandidateContent gate chain, rollback user-CAS op, shadow
 *     cross-check) on THIS VM (engines-compliant Node >=22.19.0, v22.22.2 --
 *     same engine as R13-D..R16-D so all bands are directly comparable).
 * (2) NEW CELL: fail-closed rejection-path census. Sixteen passes profiled
 *     this slice's SUCCESS paths in time (R13-D), scale (R14-D), memory/MDE
 *     (R15-D) and payload shape (R16-D). The rejection paths -- every
 *     DomainValidationError terminal a production command can hit -- have
 *     never been priced as a family (only two were pinned in passing:
 *     S12-D-1's boundary probe and R15-D's OrNew miss probe). This round
 *     prices ALL of them, mirroring the path-completeness lens R17-B opened
 *     for the routing slice:
 *       3a: eval-side rejects e1..e12 (invalid id, unknown candidate, wrong
 *           kind, non-JSON policy, shallow/deep forbidden field, baseline
 *           forbidden field, manifest ENOENT, manifest bad JSON, bad episode
 *           first/last, isolation overlap, zero PASS/FAIL records).
 *           Structural fact: a D reject ABORTS the whole command (unlike
 *           B's per-task refusals), so reject frequency is bounded by
 *           command frequency -- once-per-command class by construction.
 *       3b: promote-side rejects p1..p7 (missing evalReport, report-hash
 *           mismatch, review mismatch, non-promotable status, commit-CAS
 *           interleave fail, missing explicit approval, provisional gate).
 *       3c: rollback/retire rejects r1..r5, t1 (unknown target, retired
 *           target, stale CAS, degradation propose ok:false path,
 *           idempotent no-op, retire-active reject).
 *       3d: load-side terminals l1..l3 (OrNew ENOENT miss, corrupted JSON,
 *           S8-D-1 tampered-blob integrity trip).
 *     Every reject is asserted to actually throw DomainValidationError
 *     (fail-closed contract re-verification) and priced per invocation; the
 *     most expensive reject (zero-records, pays the assignTasks x2 barrier
 *     by code order) is pinned against the closed S5-D-4 front short-circuit.
 * Seeded PRNG (mulberry32) -> reproducible. Seeds: 0xd17d01 - 0xd17d06.
 * Run: npx tsx /tmp/r17d-sim.mts
 */
import { performance } from "node:perf_hooks";
import { mkdir, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { ResourceRegistry } from "/workspace/src/adaptation/registry.js";
import {
  adaptationRegistryPath,
  loadAdaptationRegistry,
  loadAdaptationRegistryOrNew,
  promoteWithRegistry,
  saveAdaptationRegistry,
  type ChangeNote,
  type PromoteInput,
  type PromotionReview
} from "/workspace/src/adaptation/promotion.js";
import {
  evalRoutingPolicy,
  parseRoutingEvalReport
} from "/workspace/src/adaptation/eval-routing.js";
import { evaluateProposalShadow } from "/workspace/src/adaptation/reflection.js";
import {
  hashCandidateContent,
  type EvaluationPlan,
  type ImprovementCandidate
} from "/workspace/src/adaptation/candidate.js";
import { retireVersion } from "/workspace/src/adaptation/retirement.js";
import type { AuthorIdentity, ResourceIdentity, ResourceVersion } from "/workspace/src/adaptation/resource.js";
import {
  createProjectId,
  type CandidateId,
  type IdGenerator,
  type ResourceVersionId
} from "/workspace/src/domain/ids.js";
import type { IsoTimestamp } from "/workspace/src/domain/timestamp.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import type { ExperimentPlan } from "/workspace/src/experiments/plan.js";
import type { ExperimentOutcome } from "/workspace/src/experiments/shadow.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

/** Price a rejection path: run `fn` reps times, require EVERY run to throw
 *  DomainValidationError whose message matches `messageRe` (fail-closed
 *  contract re-verification), return ms per rejected invocation. */
async function benchReject(
  label: string,
  fn: () => Promise<unknown> | unknown,
  messageRe: RegExp,
  reps: number
): Promise<number> {
  const runOnce = async (): Promise<void> => {
    let threw = false;
    try {
      await fn();
    } catch (error) {
      threw = error instanceof DomainValidationError && messageRe.test(error.message);
      if (!threw) {
        check(
          `${label}: rejects with DomainValidationError matching ${String(messageRe)}`,
          false,
          error instanceof Error ? `${error.constructor.name}: ${error.message}` : String(error)
        );
        throw error;
      }
    }
    if (!threw) {
      check(`${label}: path actually rejects`, false, "no error thrown");
      throw new Error(`${label}: expected rejection did not happen`);
    }
  };
  await runOnce(); // warm + first contract check
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) {
    await runOnce();
  }
  return (performance.now() - t0) / reps;
}

const NOW = "2026-08-25T12:00:00.000Z" as IsoTimestamp;
const HUMAN: AuthorIdentity = { kind: "human", identity: "operator" };
const EVAL_PLAN: EvaluationPlan = { stages: ["static", "replay"], metrics: ["utility"], planVersion: 1 };

function sequentialIds(prefix: string): IdGenerator {
  let n = 0;
  return () => {
    n += 1;
    return `${prefix}${String(n).padStart(4, "0")}`;
  };
}

const BASELINE_POLICY = JSON.stringify({ primaryModelId: "premium", avoid: [], prefer: [] });
const CANDIDATE_POLICY = JSON.stringify({
  primaryModelId: "premium",
  avoid: [{ modelId: "fast", family: "edit", reason: "deterministic FAIL on edit replay" }],
  prefer: [{ family: "test", modelId: "fast" }]
});

/* Configured-state policies (R7-I lesson; bit-identical generator to
 * R8-D..R16-D at k=1). */
function configuredPolicy(variantTag: string, k = 1): string {
  const avoid = Array.from({ length: 40 * k }, (_, i) => ({
    modelId: i % 8 === 0 ? "cheap" : `m${i % 7}`,
    family: i % 3 === 0 ? pick(() => (i % 10) / 10, ["edit", "test", "review"]) : `fam${i % 12}`,
    reason: `deterministic FAIL pattern ${i} (${variantTag}): attributable replay regression observed across paired episodes with stable family-level attribution`
  }));
  const prefer = Array.from({ length: 40 * k }, (_, i) => ({
    family: i % 4 === 0 ? "test" : `fam${i % 12}`,
    modelId: i % 4 === 0 ? "cheap" : `m${(i + 1) % 7}`
  }));
  const assignments = Array.from({ length: 7 }, (_, i) => ({
    role: ["planner", "executor", "tester", "reviewer", "scout", "debugger", "worker"][i] as string,
    model: i % 2 === 0 ? "premium" : "cheap",
    family: ["edit", "test", "review"][i % 3] as string
  }));
  if (variantTag === "candidate") {
    avoid.push({ modelId: "cheap", family: "refactor", reason: "candidate-only: deterministic FAIL on refactor replay (new attribution)" });
  }
  return JSON.stringify({ primaryModelId: "premium", avoid, prefer, assignments });
}
const CONFIGURED_BASELINE_POLICY = configuredPolicy("baseline");
const CONFIGURED_CANDIDATE_POLICY = configuredPolicy("candidate");

const ROLES = ["worker", "scout", "planner", "implementer", "reviewer", "tester", "debugger"] as const;
const OBJECTIVES = [
  "Fix the failing unit test in the adapter and rerun the suite",
  "Refactor the retry helper to remove duplicated backoff logic",
  "Review the migration PR for schema drift and unsafe defaults",
  "Investigate why the nightly benchmark regressed on large inputs",
  "Plan the rollout of the new caching layer across services"
];

function mkReview(candidateId: CandidateId, contentHash: string, tag: string): PromotionReview {
  return {
    reviewId: `rv-${tag}`,
    candidateId,
    contentHash,
    verdict: "approved",
    reviewerKind: "independent",
    reviewerId: "critic-gate",
    actorId: HUMAN.identity,
    evidenceRefs: [`review:${tag}`]
  };
}

function mkNote(scope: string, rollbackVersionId: ResourceVersionId): ChangeNote {
  return {
    scope,
    evidence: ["static"],
    guardrails: ["proposal-first"],
    rollbackVersionId
  };
}

function buildRoutingRegistry(
  tag: string,
  baselinePolicy = BASELINE_POLICY,
  candidatePolicy = CANDIDATE_POLICY
): { registry: ResourceRegistry; candidateId: CandidateId; baseline: ResourceVersion; identity: ResourceIdentity } {
  const registry = new ResourceRegistry({ now: () => NOW, generateId: sequentialIds(tag) });
  const identity: ResourceIdentity = {
    kind: "routing-policy",
    name: "learned-routing",
    scope: { kind: "project", projectId: createProjectId(() => `${tag}proj`) }
  };
  const baseline = registry.registerBaseline({ identity, content: baselinePolicy, author: HUMAN });
  const candidate = registry.createCandidate({
    identity,
    content: candidatePolicy,
    parentVersionId: baseline.versionId,
    author: HUMAN,
    evaluationPlan: EVAL_PLAN
  });
  return { registry, candidateId: candidate.candidateId, baseline, identity };
}

/* Configured-state registry (same construction as R8-D..R16-D). */
function buildConfiguredRegistry(
  tag: string,
  baselinePolicy = CONFIGURED_BASELINE_POLICY,
  candidatePolicy = CONFIGURED_CANDIDATE_POLICY
): {
  registry: ResourceRegistry;
  candidateId: CandidateId;
  baseline: ResourceVersion;
  identity: ResourceIdentity;
} {
  const built = buildRoutingRegistry(tag, baselinePolicy, candidatePolicy);
  const { registry } = built;
  const versions: { identity: ResourceIdentity; v1: ResourceVersion; v2?: ResourceVersion }[] = [];
  for (let i = 0; i < 8; i += 1) {
    const identity: ResourceIdentity = {
      kind: "prompt",
      name: `agent-prompt-${i}`,
      scope: { kind: "project", projectId: createProjectId(() => `${tag}proj`) }
    };
    const body = `# prompt ${i}\n${`Follow the review checklist, cite evidence for every claim, and keep the diff bounded to the stated scope (${i}).\n`.repeat(30 + i * 8)}`;
    const v1 = registry.registerBaseline({ identity, content: body, author: HUMAN });
    const candidate = registry.createCandidate({
      identity,
      content: `${body}\n## amendment ${i}\nAdd an explicit check for the attributable failure.\n`,
      parentVersionId: v1.versionId,
      author: HUMAN,
      evaluationPlan: EVAL_PLAN
    });
    const entry: { identity: ResourceIdentity; v1: ResourceVersion; v2?: ResourceVersion } = { identity, v1 };
    if (i < 5) {
      const result = promoteWithRegistry(registry, {
        candidateId: candidate.candidateId,
        expectedCurrentVersionId: v1.versionId,
        content: `${body}\n## amendment ${i}\nAdd an explicit check for the attributable failure.\n`,
        approvedBy: HUMAN,
        review: mkReview(candidate.candidateId, candidate.contentHash, `${tag}p${i}`),
        changeNote: mkNote(`prompt:${tag}${i}`, v1.versionId),
        explicitApproval: true
      });
      entry.v2 = result.newVersion as ResourceVersion;
    } else if (i === 5) {
      registry.beginPromotion({
        candidateId: candidate.candidateId,
        expectedCurrentVersionId: v1.versionId,
        content: `${body}\n## amendment ${i}\nAdd an explicit check for the attributable failure.\n`,
        approvedBy: HUMAN,
        review: mkReview(candidate.candidateId, candidate.contentHash, `${tag}p${i}`),
        changeNote: mkNote(`prompt:${tag}${i}`, v1.versionId),
        explicitApproval: true
      });
    }
    versions.push(entry);
  }
  const first = versions[0] as { identity: ResourceIdentity; v1: ResourceVersion; v2?: ResourceVersion };
  registry.rollback({
    identity: first.identity,
    expectedCurrentVersionId: (first.v2 as ResourceVersion).versionId,
    targetVersionId: first.v1.versionId,
    reason: "degradation",
    evidence: ["ev:configured-degradation"],
    automatic: false
  });
  const second = versions[1] as { identity: ResourceIdentity; v1: ResourceVersion; v2?: ResourceVersion };
  registry.rollback({
    identity: second.identity,
    expectedCurrentVersionId: (second.v2 as ResourceVersion).versionId,
    targetVersionId: second.v1.versionId,
    reason: "user",
    evidence: ["ev:configured-user"],
    automatic: false
  });
  registry.retire((versions[2] as { v1: ResourceVersion }).v1.versionId);
  registry.retire((versions[3] as { v1: ResourceVersion }).v1.versionId);
  return built;
}

interface DatasetShape {
  readonly successRate?: number; // default 0.7
  readonly badRoleAt?: number; // index whose role is invalid (parse reject)
  readonly workspaceOverrideAt?: { readonly index: number; readonly workspace: string };
}

async function writeDataset(
  dir: string,
  episodes: number,
  rng: () => number,
  shape?: DatasetShape
): Promise<void> {
  await mkdir(dir, { recursive: true });
  const successRate = shape?.successRate ?? 0.7;
  const eps = Array.from({ length: episodes }, (_, i) => ({
    episodeHash: `eh_${i}`,
    taskId: `tsk_e${i}`,
    role: shape?.badRoleAt === i ? "chaos-monkey" : pick(rng, ROLES),
    objective: pick(rng, OBJECTIVES),
    originalWorkspace:
      shape?.workspaceOverrideAt?.index === i ? shape.workspaceOverrideAt.workspace : "/repos/alpha",
    ...(rng() < successRate ? { taskSuccess: rng() < 0.5 ? "PASS" : "FAIL" } : {}),
    ...(rng() < 0.5 ? { taskFamily: pick(rng, ["edit", "test", "review"]) } : {})
  }));
  const manifest = { datasetId: "ds-r17d", environmentVersion: "env-1", episodes: eps };
  await writeFile(join(dir, "manifest.json"), `${JSON.stringify(manifest, null, 2)}\n`, "utf8");
}

/* ============================================================
 * Section 0: whole-slice budget re-verification, default-state anchors.
 * ============================================================ */
const workRoot = await mkdtemp(join(tmpdir(), "r17d-sim-"));
let configuredEvalReport: ReturnType<typeof parseRoutingEvalReport> | undefined;
{
  const rng = mulberry32(0xd17d01);
  const stateRoot = join(workRoot, "state");
  const { registry } = buildRoutingRegistry("b0");
  await saveAdaptationRegistry(stateRoot, registry);
  const datasetDir = join(workRoot, "dataset");
  await writeDataset(datasetDir, 200, rng);

  const loadMs = await benchAsync(async () => {
    await loadAdaptationRegistry(stateRoot);
  }, 30);
  const saveMs = await benchAsync(async () => {
    await saveAdaptationRegistry(stateRoot, registry);
  }, 30);

  const { registry: r2, candidateId } = buildRoutingRegistry("b1");
  await saveAdaptationRegistry(stateRoot, r2);
  const evalMs = await benchAsync(async () => {
    await evalRoutingPolicy({ stateRoot, candidateId, datasetDir });
  }, 20);
  console.log(
    `S0 default anchors: registry load=${loadMs.toFixed(2)}ms save(+fsync)=${saveMs.toFixed(2)}ms | adapt-eval end-to-end (E=200, toy policy)=${evalMs.toFixed(2)}ms per invocation`
  );
}

/* ============================================================
 * Section 0b: evaluateProposalShadow cross-check (experiments-slice owned).
 * ============================================================ */
{
  const mkPlan = (p: number, a: number): ExperimentPlan => ({
    planVersion: 1,
    experimentId: "exp_r17d-shadow",
    mode: "shadow",
    baselineVersionId: "rsv_r17d0001" as ResourceVersionId,
    candidateId: "cnd_r17d0002" as CandidateId,
    population: Array.from({ length: p }, (_, i) => `eh_${i}`),
    metrics: ["utility"],
    thresholds: { maxGuardrailBreaches: 1000000, maxCostUsd: 1e9 },
    budget: { maxAssignments: a + 1, maxWallClockMs: 1000000000 },
    randomization: { seed: 7 },
    stopPolicy: { onGuardrail: "halt", onBudgetExhausted: "halt" },
    missingOutcomePolicy: "exclude"
  });
  const mkOutcomes = (a: number): ExperimentOutcome[] =>
    Array.from({ length: a }, (_, i) => ({
      episodeHash: `eh_${i}`,
      utility: i % 2,
      costUsd: 0.001,
      guardrailBreached: false
    }));
  for (const [p, a, reps] of [
    [200, 100, 20],
    [2000, 1000, 3]
  ] as const) {
    const plan = mkPlan(p, a);
    const outcomes = mkOutcomes(a);
    const state = evaluateProposalShadow(plan, outcomes, 0);
    check(`S0b shadow run completes without halt (P=${p},A=${a})`, !state.halted && state.outcomes.length === a);
    const ms = bench(() => {
      evaluateProposalShadow(plan, outcomes, 0);
    }, reps);
    console.log(
      `S0b evaluateProposalShadow cross-check: P=${p} A=${a} -> ${ms.toFixed(1)}ms per call (experiments-slice owned; anchor only)`
    );
  }
}

/* ============================================================
 * Section 0c: configured-state anchors (R8-D..R16-D replica on this VM).
 * ============================================================ */
{
  const rng = mulberry32(0xd17d02);
  const stateRoot = join(workRoot, "state-configured");
  const { registry, candidateId } = buildConfiguredRegistry("c0");
  await saveAdaptationRegistry(stateRoot, registry);
  const snapshot = registry.snapshot();
  const contents = snapshot.contents ?? [];
  const totalBlobBytes = contents.reduce((sum, blob) => sum + blob.content.length, 0);
  const datasetDir = join(workRoot, "dataset-configured");
  await writeDataset(datasetDir, 200, rng);

  const loadMs = await benchAsync(async () => {
    await loadAdaptationRegistry(stateRoot);
  }, 30);
  const saveMs = await benchAsync(async () => {
    await saveAdaptationRegistry(stateRoot, registry);
  }, 30);
  const evalMs = await benchAsync(async () => {
    await evalRoutingPolicy({ stateRoot, candidateId, datasetDir });
  }, 20);
  const result = await evalRoutingPolicy({ stateRoot, candidateId, datasetDir });
  configuredEvalReport = parseRoutingEvalReport(result.report);
  const raw = await readFile(adaptationRegistryPath(stateRoot), "utf8");
  console.log(
    `S0c configured anchors (registry.json=${(raw.length / 1024).toFixed(1)}KB, V=${snapshot.versions.length} C=${snapshot.candidates.length} L=${snapshot.ledger.length} B=${contents.length} blobs=${(totalBlobBytes / 1024).toFixed(1)}KB, policy=${(CONFIGURED_CANDIDATE_POLICY.length / 1024).toFixed(1)}KB): load=${loadMs.toFixed(2)}ms save(+fsync)=${saveMs.toFixed(2)}ms | adapt-eval end-to-end (E=200, configured policy)=${evalMs.toFixed(2)}ms per invocation`
  );
}

/* ============================================================
 * Section 0d: configured-state promote and rollback OP-ONLY costs.
 * ============================================================ */
{
  const report = configuredEvalReport;
  check("S0d setup: configured eval report available", report !== undefined);
  const K = 60;
  const promoteFixtures = Array.from({ length: K }, (_, i) => {
    const { registry, candidateId, baseline } = buildConfiguredRegistry(`pm${i}x`);
    const candidate = registry.getCandidate(candidateId) as ImprovementCandidate;
    const input: PromoteInput = {
      candidateId,
      expectedCurrentVersionId: baseline.versionId,
      content: CONFIGURED_CANDIDATE_POLICY,
      approvedBy: HUMAN,
      review: mkReview(candidateId, candidate.contentHash, `pm${i}`),
      changeNote: mkNote(`routing:pm${i}`, baseline.versionId),
      explicitApproval: true,
      evalReport: {
        ...(report as NonNullable<typeof report>),
        candidateId,
        contentHash: candidate.contentHash
      }
    };
    return { registry, input };
  });
  const t0 = performance.now();
  const promoted: { registry: ResourceRegistry; newVersion: ResourceVersion; identity: ResourceIdentity; baselineId: ResourceVersionId }[] = [];
  for (const fixture of promoteFixtures) {
    const result = promoteWithRegistry(fixture.registry, fixture.input);
    promoted.push({
      registry: fixture.registry,
      newVersion: result.newVersion as ResourceVersion,
      identity: (result.newVersion as ResourceVersion).identity,
      baselineId: fixture.input.expectedCurrentVersionId
    });
  }
  const promoteUs = ((performance.now() - t0) / K) * 1e3;
  check("S0d all configured promotes succeeded", promoted.length === K && promoted.every((p) => p.newVersion !== undefined));

  const t1 = performance.now();
  for (const p of promoted) {
    p.registry.rollback({
      identity: p.identity,
      expectedCurrentVersionId: p.newVersion.versionId,
      targetVersionId: p.baselineId,
      reason: "user",
      evidence: ["ev:r17d-op-anchor"],
      automatic: false
    });
  }
  const rollbackUs = ((performance.now() - t1) / K) * 1e3;
  const hashUs = bench(() => {
    hashCandidateContent(CONFIGURED_CANDIDATE_POLICY);
  }, 3000) * 1e3;
  console.log(
    `S0d configured op-only anchors (K=${K}): promote op=${promoteUs.toFixed(1)}us | rollback(user CAS) op=${rollbackUs.toFixed(1)}us | single hash32 over the ${(CONFIGURED_CANDIDATE_POLICY.length / 1024).toFixed(1)}KB policy=${hashUs.toFixed(1)}us (x3 on the promote gate chain)`
  );
}

/* ============================================================
 * Section 1: rejection census on the configured production corpus.
 * A D-slice reject aborts the whole command (single terminal throw), so
 * reject frequency <= command frequency by construction. Verify that the
 * production-shape corpus itself produces ZERO rejects (the success anchor
 * above already proves this: 20+ eval reps, K=60 promotes, K=60 rollbacks
 * all succeeded). This section just states the structural census.
 * ============================================================ */
console.log(
  "S1 rejection census: production-shape corpus -> 0 rejects across all anchors (every reject below requires a poisoned fixture); a reject is a terminal abort of the whole command -- once-per-command class by construction"
);

/* ============================================================
 * Section 2 (NEW CELL 3a): eval-side rejection-path pricing e1..e12.
 * ============================================================ */
{
  const rng = mulberry32(0xd17d03);
  const stateRoot = join(workRoot, "state-rej");
  const { registry, candidateId, baseline, identity } = buildConfiguredRegistry("rj");

  // Poisoned candidates (created against the SAME baseline so eval reaches
  // the intended gate). createCandidate accepts these bytes -- forbidden
  // fields and non-JSON are eval-time gates, not creation-time gates.
  const shallowForbidden = JSON.stringify({
    token: "sk-not-really",
    primaryModelId: "premium",
    avoid: JSON.parse(CONFIGURED_CANDIDATE_POLICY).avoid,
    prefer: JSON.parse(CONFIGURED_CANDIDATE_POLICY).prefer
  });
  const parsedCfg = JSON.parse(CONFIGURED_CANDIDATE_POLICY) as {
    primaryModelId: string;
    avoid: unknown[];
    prefer: unknown[];
    assignments: unknown[];
  };
  const deepForbidden = JSON.stringify({
    primaryModelId: parsedCfg.primaryModelId,
    avoid: parsedCfg.avoid,
    assignments: parsedCfg.assignments,
    prefer: [...parsedCfg.prefer, { family: "zz", modelId: "m1", meta: { nested: { apikey: "x" } } }]
  });
  const cShallow = registry.createCandidate({
    identity, content: shallowForbidden, parentVersionId: baseline.versionId, author: HUMAN, evaluationPlan: EVAL_PLAN
  });
  const cDeep = registry.createCandidate({
    identity, content: deepForbidden, parentVersionId: baseline.versionId, author: HUMAN, evaluationPlan: EVAL_PLAN
  });
  const cNotJson = registry.createCandidate({
    identity, content: "not-json {{{ definitely", parentVersionId: baseline.versionId, author: HUMAN, evaluationPlan: EVAL_PLAN
  });
  // prompt-kind candidate for the wrong-kind reject
  const promptIdentity: ResourceIdentity = {
    kind: "prompt",
    name: "agent-prompt-0",
    scope: identity.scope
  };
  const promptBaseline = registry.versionsFor(promptIdentity)[0] as ResourceVersion;
  const cWrongKind = registry.createCandidate({
    identity: promptIdentity,
    content: "wrong-kind candidate body",
    parentVersionId: promptBaseline.versionId,
    author: HUMAN,
    evaluationPlan: EVAL_PLAN
  });
  await saveAdaptationRegistry(stateRoot, registry);

  const goodDataset = join(workRoot, "dataset-rej-good");
  await writeDataset(goodDataset, 200, mulberry32(0xd17d03 ^ 0x111));
  const zeroRecords = join(workRoot, "dataset-rej-zero");
  await writeDataset(zeroRecords, 200, mulberry32(0xd17d03 ^ 0x222), { successRate: 0 });
  const badFirst = join(workRoot, "dataset-rej-badfirst");
  await writeDataset(badFirst, 200, mulberry32(0xd17d03 ^ 0x333), { badRoleAt: 0 });
  const badLast = join(workRoot, "dataset-rej-badlast");
  await writeDataset(badLast, 200, mulberry32(0xd17d03 ^ 0x444), { badRoleAt: 199 });
  const overlap = join(workRoot, "dataset-rej-overlap");
  await writeDataset(overlap, 200, mulberry32(0xd17d03 ^ 0x555), {
    workspaceOverrideAt: { index: 137, workspace: stateRoot }
  });
  const badJsonDir = join(workRoot, "dataset-rej-badjson");
  await mkdir(badJsonDir, { recursive: true });
  await writeFile(join(badJsonDir, "manifest.json"), "{ not json", "utf8");
  const missingDir = join(workRoot, "dataset-rej-missing"); // never created

  // Success reference on the same state (for the "reject vs success" ratio).
  const successMs = await benchAsync(async () => {
    await evalRoutingPolicy({ stateRoot, candidateId, datasetDir: goodDataset });
  }, 20);

  const rows: { id: string; label: string; ms: number; pays: string }[] = [];
  const evalReject = async (
    id: string, label: string, req: { stateRoot: string; candidateId: string; datasetDir: string },
    re: RegExp, reps: number, pays: string
  ): Promise<void> => {
    const ms = await benchReject(`S2 ${id}`, () => evalRoutingPolicy(req), re, reps);
    rows.push({ id, label, ms, pays });
  };

  await evalReject("e1", "invalid candidateId format (pre-load)", { stateRoot, candidateId: "definitely-not-an-id", datasetDir: goodDataset }, /invalid candidate id/, 200, "id regex only -- rejects BEFORE registry load");
  await evalReject("e2", "unknown candidate", { stateRoot, candidateId: "cnd_doesnotexist0001", datasetDir: goodDataset }, /unknown candidate/, 40, "registry load (S11-D-2 chain + S8-D-1 rehash)");
  await evalReject("e3", "wrong identity kind (prompt)", { stateRoot, candidateId: cWrongKind.candidateId, datasetDir: goodDataset }, /only supports routing-policy/, 40, "registry load + candidate lookup");
  await evalReject("e4", "non-JSON candidate policy", { stateRoot, candidateId: cNotJson.candidateId, datasetDir: goodDataset }, /content is not JSON/, 40, "load + JSON.parse attempt (S1-D-9 domain)");
  await evalReject("e5", "forbidden field, shallow first key", { stateRoot, candidateId: cShallow.candidateId, datasetDir: goodDataset }, /forbidden field token/, 40, "load + walk prefix (S9-D-4 sound form)");
  await evalReject("e6", "forbidden field, deep last leaf", { stateRoot, candidateId: cDeep.candidateId, datasetDir: goodDataset }, /forbidden field apikey/, 40, "load + FULL candidate walk (S4-D-5/S9-D-4 ceiling)");
  await evalReject("e7", "dataset manifest ENOENT", { stateRoot, candidateId, datasetDir: missingDir }, /dataset manifest not found/, 40, "load + full policy chain x2 + readFile miss");
  await evalReject("e8", "dataset manifest bad JSON", { stateRoot, candidateId, datasetDir: badJsonDir }, /invalid dataset manifest JSON/, 40, "load + policy chain + read + parse attempt");
  await evalReject("e9", "bad episode at index 0", { stateRoot, candidateId, datasetDir: badFirst }, /episodes\[0\] has invalid role/, 40, "load + policy chain + manifest parse + 1 episode parse");
  await evalReject("e10", "bad episode at index 199", { stateRoot, candidateId, datasetDir: badLast }, /episodes\[199\] has invalid role/, 40, "same + 200 episode parses (fail-fast is index-linear; parse floor)");
  await evalReject("e11", "isolation overlap (workspace=stateRoot)", { stateRoot, candidateId, datasetDir: overlap }, /output root overlaps original workspace/, 40, "load + policy chain + dataset parse + guard prefix (S3-D-3/S9-D-2 domain)");
  await evalReject("e12", "zero PASS/FAIL records", { stateRoot, candidateId, datasetDir: zeroRecords }, /requires at least one episode with recorded PASS or FAIL/, 20, "EVERYTHING through the assignTasks x2 barrier (S2-D-4/S10-D-2) -- most expensive reject; early exit = closed S5-D-4");

  console.log(`S2 eval-side rejection pricing (success reference e2e=${successMs.toFixed(2)}ms):`);
  for (const row of rows) {
    console.log(
      `S2   ${row.id.padEnd(4)} ${row.label.padEnd(42)} ${(row.ms * 1e3).toFixed(0).padStart(6)}us/reject  (${((row.ms / successMs) * 100).toFixed(0).padStart(3)}% of success e2e)  [${row.pays}]`
    );
  }
  const e12 = rows.find((r) => r.id === "e12");
  check("S2 e12 is the most expensive eval reject", rows.every((r) => r.id === "e12" || r.ms <= (e12?.ms ?? 0)));
  check("S2 e1 pre-load reject is the cheapest", rows.every((r) => r.id === "e1" || r.ms >= (rows.find((x) => x.id === "e1")?.ms ?? Infinity)));
}

/* ============================================================
 * Section 3 (NEW CELL 3b): promote-side rejection-path pricing p1..p7.
 * ============================================================ */
{
  const report = configuredEvalReport as NonNullable<typeof configuredEvalReport>;
  const { registry, candidateId, baseline } = buildConfiguredRegistry("pj");
  const candidate = registry.getCandidate(candidateId) as ImprovementCandidate;
  const goodInput: PromoteInput = {
    candidateId,
    expectedCurrentVersionId: baseline.versionId,
    content: CONFIGURED_CANDIDATE_POLICY,
    approvedBy: HUMAN,
    review: mkReview(candidateId, candidate.contentHash, "pj"),
    changeNote: mkNote("routing:pj", baseline.versionId),
    explicitApproval: true,
    evalReport: { ...report, candidateId, contentHash: candidate.contentHash }
  };
  const rows: { id: string; label: string; us: number; pays: string }[] = [];
  const opReject = async (
    id: string, label: string, fn: () => unknown, re: RegExp, reps: number, pays: string
  ): Promise<void> => {
    const ms = await benchReject(`S3 ${id}`, fn, re, reps);
    rows.push({ id, label, us: ms * 1e3, pays });
  };

  await opReject("p1", "missing evalReport", () => {
    const { evalReport: _omit, ...rest } = goodInput;
    return promoteWithRegistry(registry, rest as PromoteInput);
  }, /requires evalReport/, 2000, "candidate lookup only -- cheapest promote reject");

  await opReject("p2", "eval report contentHash mismatch", () => {
    return promoteWithRegistry(registry, {
      ...goodInput,
      evalReport: { ...(goodInput.evalReport as NonNullable<PromoteInput["evalReport"]>), contentHash: "deadbeef" }
    });
  }, /contentHash must equal/, 500, "1x hash32 over the policy (gate chain first link, S1-D-7/S7-D-1 domain)");

  await opReject("p3", "review actorId mismatch", () => {
    return promoteWithRegistry(registry, {
      ...goodInput,
      review: { ...goodInput.review, actorId: "someone-else" }
    });
  }, /review must match candidate author/, 500, "wrapper gate + prepare gates through putContent (~3x hash32)");

  await opReject("p6", "explicit approval missing", () => {
    return promoteWithRegistry(registry, { ...goodInput, explicitApproval: false });
  }, /explicit approval is required/, 500, "nearly full preparePromotion (all gates, no version mint)");

  await opReject("p7", "provisional gate (acceptProvisional unset)", () => {
    const base = goodInput.evalReport as NonNullable<PromoteInput["evalReport"]>;
    return promoteWithRegistry(registry, {
      ...goodInput,
      evalReport: {
        ...base,
        comparison: {
          ...base.comparison,
          utilityDelta: { ...base.comparison.utilityDelta, provisional: true }
        }
      }
    });
  }, /provisional eval report is not approval material/, 500, "wrapper gate chain up to the provisional check");

  // p4 non-promotable status: dedicated registry so the status flip does not
  // disturb the shared fixture.
  {
    const { registry: r4, candidateId: c4, baseline: b4 } = buildConfiguredRegistry("p4");
    const cand4 = r4.getCandidate(c4) as ImprovementCandidate;
    r4.updateCandidateStatus(c4, "rejected");
    const input4: PromoteInput = {
      candidateId: c4,
      expectedCurrentVersionId: b4.versionId,
      content: CONFIGURED_CANDIDATE_POLICY,
      approvedBy: HUMAN,
      review: mkReview(c4, cand4.contentHash, "p4"),
      changeNote: mkNote("routing:p4", b4.versionId),
      explicitApproval: true,
      evalReport: { ...report, candidateId: c4, contentHash: cand4.contentHash }
    };
    await opReject("p4", "non-promotable status (rejected)", () => promoteWithRegistry(r4, input4), /status rejected cannot promote/, 500, "prepare gates through the status check");
  }

  // p5 commit-CAS interleave: begin succeeds, pointer moves under it
  // (user rollback), commit CAS fails -> appends a rejected ledger entry and
  // throws. State-mutating: K prepared fixtures, timed loop.
  {
    const K = 40;
    const fixtures = Array.from({ length: K }, (_, i) => {
      const { registry: r5, candidateId: c5, baseline: b5, identity: id5 } = buildConfiguredRegistry(`p5i${i}`);
      const cand5 = r5.getCandidate(c5) as ImprovementCandidate;
      // First promote the candidate so active moves to v2, then create a
      // second candidate from v2, begin its promotion, and roll the pointer
      // back to v1 underneath the intent.
      const first = promoteWithRegistry(r5, {
        candidateId: c5,
        expectedCurrentVersionId: b5.versionId,
        content: CONFIGURED_CANDIDATE_POLICY,
        approvedBy: HUMAN,
        review: mkReview(c5, cand5.contentHash, `p5a${i}`),
        changeNote: mkNote(`routing:p5a${i}`, b5.versionId),
        explicitApproval: true,
        evalReport: { ...report, candidateId: c5, contentHash: cand5.contentHash }
      });
      const v2 = first.newVersion as ResourceVersion;
      const second = r5.createCandidate({
        identity: id5,
        content: `${CONFIGURED_CANDIDATE_POLICY} `,
        parentVersionId: v2.versionId,
        author: HUMAN,
        evaluationPlan: EVAL_PLAN
      });
      const began = r5.beginPromotion({
        candidateId: second.candidateId,
        expectedCurrentVersionId: v2.versionId,
        content: `${CONFIGURED_CANDIDATE_POLICY} `,
        approvedBy: HUMAN,
        review: mkReview(second.candidateId, second.contentHash, `p5b${i}`),
        changeNote: mkNote(`routing:p5b${i}`, v2.versionId),
        explicitApproval: true,
        evalReport: { ...report, candidateId: second.candidateId, contentHash: second.contentHash }
      });
      r5.rollback({
        identity: id5,
        expectedCurrentVersionId: v2.versionId,
        targetVersionId: b5.versionId,
        reason: "user",
        evidence: ["ev:p5-interleave"],
        automatic: false
      });
      return { r5, intentId: began.intentId };
    });
    let casRejects = 0;
    const t0 = performance.now();
    for (const f of fixtures) {
      try {
        f.r5.commitPromotion(f.intentId);
      } catch (error) {
        if (error instanceof DomainValidationError && /CAS failed/.test(error.message)) casRejects += 1;
        else throw error;
      }
    }
    const us = ((performance.now() - t0) / K) * 1e3;
    check("S3 p5 all commits CAS-rejected", casRejects === K, `${casRejects}/${K}`);
    const lastLedger = fixtures[K - 1]!.r5.ledger();
    check(
      "S3 p5 rejected ledger entry appended (contract)",
      lastLedger.some((e) => e.kind === "rejected")
    );
    rows.push({ id: "p5", label: "commit-CAS interleave fail", us, pays: "commit gates + CAS + rejected-ledger append (contract mutation)" });
  }

  console.log("S3 promote-side rejection pricing (op-only, no I/O):");
  for (const row of rows) {
    console.log(
      `S3   ${row.id.padEnd(4)} ${row.label.padEnd(42)} ${row.us.toFixed(1).padStart(8)}us/reject  [${row.pays}]`
    );
  }
}

/* ============================================================
 * Section 4 (NEW CELL 3c): rollback / retire rejection pricing r1..r5, t1.
 * ============================================================ */
{
  const { registry, candidateId, baseline, identity } = buildConfiguredRegistry("rb");
  const report = configuredEvalReport as NonNullable<typeof configuredEvalReport>;
  const cand = registry.getCandidate(candidateId) as ImprovementCandidate;
  const promotedResult = promoteWithRegistry(registry, {
    candidateId,
    expectedCurrentVersionId: baseline.versionId,
    content: CONFIGURED_CANDIDATE_POLICY,
    approvedBy: HUMAN,
    review: mkReview(candidateId, cand.contentHash, "rb"),
    changeNote: mkNote("routing:rb", baseline.versionId),
    explicitApproval: true,
    evalReport: { ...report, candidateId, contentHash: cand.contentHash }
  });
  const v2 = promotedResult.newVersion as ResourceVersion;
  // A retired prompt version exists in the configured registry (prompt 2 v1).
  const promptIdentity2: ResourceIdentity = { kind: "prompt", name: "agent-prompt-2", scope: identity.scope };
  const retiredTarget = registry.versionsFor(promptIdentity2)[0] as ResourceVersion;
  const activePrompt2 = registry.versionsFor(promptIdentity2)[1] as ResourceVersion;

  const rows: { id: string; label: string; us: number; pays: string }[] = [];
  const push = async (id: string, label: string, fn: () => unknown, re: RegExp, reps: number, pays: string): Promise<void> => {
    const ms = await benchReject(`S4 ${id}`, fn, re, reps);
    rows.push({ id, label, us: ms * 1e3, pays });
  };

  await push("r1", "unknown rollback target", () => registry.rollback({
    identity, expectedCurrentVersionId: v2.versionId, targetVersionId: "rsv_nosuchversion01" as ResourceVersionId,
    reason: "user", evidence: ["ev:r1"], automatic: false
  }), /unknown rollback target/, 2000, "validate + versionsById miss");

  await push("r2", "retired rollback target", () => registry.rollback({
    identity: promptIdentity2, expectedCurrentVersionId: activePrompt2.versionId, targetVersionId: retiredTarget.versionId,
    reason: "user", evidence: ["ev:r2"], automatic: false
  }), /is retired and cannot receive new assignments/, 2000, "validate + versionsFor scan + retired-set hit (S6-D-1 guard domain)");

  await push("r3", "stale-CAS rollback", () => registry.rollback({
    identity, expectedCurrentVersionId: baseline.versionId, targetVersionId: baseline.versionId,
    reason: "user", evidence: ["ev:r3"], automatic: false
  }), /CAS failed/, 2000, "full gates + CAS mismatch (stale expected; active is v2)");

  await push("t1", "retire the active version", () => registry.retire(v2.versionId), /cannot retire the current active version/, 2000, "lookup + active-pointer check");

  // r4 degradation propose path (ok:false designed outcome, not a throw).
  {
    const proposeUs = bench(() => {
      const result = registry.rollback({
        identity, expectedCurrentVersionId: v2.versionId, targetVersionId: baseline.versionId,
        reason: "degradation", evidence: ["ev:r4"], automatic: false
      });
      if (result.ok !== false) throw new Error("degradation should propose, not apply");
    }, 2000) * 1e3;
    rows.push({ id: "r4", label: "degradation propose (ok:false, no CAS)", us: proposeUs, pays: "full gates + alreadyProposed dedup (steady-state no append)" });
  }

  // r5 idempotent no-op (active == target).
  {
    const noopUs = bench(() => {
      const result = registry.rollback({
        identity, expectedCurrentVersionId: v2.versionId, targetVersionId: v2.versionId,
        reason: "user", evidence: ["ev:r5"], automatic: false
      });
      if (result.ok !== true) throw new Error("no-op rollback should be ok");
    }, 2000) * 1e3;
    rows.push({ id: "r5", label: "idempotent no-op (active==target)", us: noopUs, pays: "validate + versionsFor scan + early return (S7-D-4 kept order)" });
  }

  console.log("S4 rollback/retire rejection + designed-refusal pricing (op-only):");
  for (const row of rows) {
    console.log(
      `S4   ${row.id.padEnd(4)} ${row.label.padEnd(42)} ${row.us.toFixed(1).padStart(8)}us/op  [${row.pays}]`
    );
  }
}

/* ============================================================
 * Section 5 (NEW CELL 3d): load-side terminals l1..l3.
 * ============================================================ */
{
  const emptyRoot = join(workRoot, "state-l1-empty");
  const l1Us = (await benchAsync(async () => {
    const fresh = await loadAdaptationRegistryOrNew(emptyRoot);
    if (fresh.snapshot().versions.length !== 0) throw new Error("expected empty registry");
  }, 100)) * 1e3;

  const corruptRoot = join(workRoot, "state-l2-corrupt");
  await mkdir(join(corruptRoot, "adaptation"), { recursive: true });
  await writeFile(adaptationRegistryPath(corruptRoot), "{ this is not json", "utf8");
  const l2Ms = await benchReject("S5 l2", () => loadAdaptationRegistry(corruptRoot), /invalid registry snapshot/, 100);

  const tamperRoot = join(workRoot, "state-l3-tamper");
  const { registry } = buildConfiguredRegistry("l3");
  await saveAdaptationRegistry(tamperRoot, registry);
  const rawSnapshot = JSON.parse(await readFile(adaptationRegistryPath(tamperRoot), "utf8")) as {
    contents: { hash: string; content: string }[];
  };
  rawSnapshot.contents[0]!.content = `${rawSnapshot.contents[0]!.content} TAMPERED`;
  await writeFile(adaptationRegistryPath(tamperRoot), JSON.stringify(rawSnapshot, null, 2), "utf8");
  const l3Ms = await benchReject("S5 l3", () => loadAdaptationRegistry(tamperRoot), /snapshot content hash mismatch/, 40);

  console.log(
    `S5 load-side terminals: l1 OrNew ENOENT miss=${l1Us.toFixed(1)}us (R15-D once-per-command regex probe class) | l2 corrupted JSON reject=${(l2Ms * 1e3).toFixed(0)}us (read + parse attempt) | l3 S8-D-1 tampered-blob integrity trip=${(l3Ms * 1e3).toFixed(0)}us (read + parse + rehash up to first mismatch -- the mandated integrity gate firing exactly as specified)`
  );
}

await rm(workRoot, { recursive: true, force: true });
if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nAll checks passed.");
```
