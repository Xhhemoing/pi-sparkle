MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 29 / R29-C：离线路由切片第二十九遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 29 / R29-C（Round 1–28 同区二十八遍
  之后的第二十九遍，叠在已落地的 S7-C 之上）
- **基线:** 分支创建与全部测量在 `cursor/sota-persistent-opt-83a1` @
  `ee08a83`（R28-J 空枚举合入 + Round 28 关轮 + R29-C 派单后的独占分支
  tip）；提交前独占分支快进两次至 `a23deea`（`c438879` R29-B 空枚举
  合入 + R29-D 派单，`a23deea` R29-A 空枚举合入 + R29-E 派单，均
  docs-only），报告分支已逐次 rebase 通过并在每个新 tip 上重验冻结。
  `git log -1 -- src/routing/` 显示切片最后一次生产改动**仍为 `183df9b`
  「perf(routing): land S7-C support-only eta」**；`git diff 183df9b..HEAD --`
  对 9 个切片文件为**空**（stat 空输出、exit 0，`ee08a83` / `c438879` /
  `a23deea` 三个 tip 上各验一次），两次快进均未触及 `src/`，测量形态与
  提交形态位级同一。R29-B 轴（learned-policy container face）与 R29-A
  轴（input reference-identity / aliasing-topology face）已核对并入非
  移植清单（§4），未等待、未触碰其余兄弟分支。
- **分支:** `cursor/r29-c-offline-routing-twenty-ninth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，wc -l = 170/168/311/585/254/156/88/130/177 = 2,039；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第二十九组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第二十九遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R28-C 的全部无号关闭
探针与轴，含 R28-C 的基片实例化、R27-C 的 PROBX、R26-C 的 PRICEX、
R25-C 的 AGEX、R24-C 的 OCCX、R23-C 的 ORDX、R22-C 的 SEEDX、R21-C 的
YMIX、R20-C 的 SCALEX、R19-C 的 FITQ）后，本轮**从未点名站点数为零**
（连续第十一遍）——每一个执行站点都能指到既有 ID、无号探针、零池
备忘或池算术覆盖。R26-C/R27-C 已把切片公开输入面的**全部数值坐标**
定价完毕，R28-C 又把**执行基片坐标**（解释器 + 模块管线）定价关闭；
本轮新增的完备性轴因此落在**基片之内、输入值之外的下一个执行坐标**
上——进程里除被测内核外**还执行了哪些切片内核、以何次序**：

- **process workload-composition face（进程工作负载组成面）的 C 区
  双估计器共驻实例化（新轴一次性收口，非候选；face 名归 R29-C 本轮；
  观察绰号 PAIRX——plan-ban 用，不铸 ID、不写入排除表）**——本切片
  的生产执行形态是**双归因对**：`writeAttributionPair`
  （src/experiments/attribution-report.ts）在同一进程内对同一批行
  先调 `fitLogitAdditive` 再调 `fitProbabilityAdditive`（X0-11 强制
  两路都留），且两估计器**共享 posterior.ts 的 Beta 分位机器**
  （`betaQuantileLcb` → `betacf`/`lnGamma`——logit 每报告 1 次、
  prob-add 每效应 2 次，调用画像完全不同）。而 R1-C..R28-C 的
  **每一个生产读数**都测在 `fitProbabilityAdditive` 从未执行过的
  进程里——历史锚全部是孤测形态，生产共驻形态从未剂量化。本轮三臂
  剂量（L = 历史孤测形态；PAIR = 生产交错形态，每个计时 rep 之后对
  同一冻结夹具的 4 个单场景组各调一次 prob-add，计时器外；PRE =
  异质反馈先行形态，首个 logit 调用之前先跑 25 遍 × 4 组 prob-add，
  让共享机器先在 prob-add 画像上分层编译），**runner 逐字节同一**
  （臂仅由 R29C_ARM 选择；两模块在所有臂都 import——模块图冻结，
  剂量纯粹是「姊妹内核是否执行、何时执行」），全臂共享单一基片
  （tsx+src = R28-C S 臂，基片坐标冻结），交错 3 轮 × 3 臂 = 9 个
  独立进程：
  **位面**——9/9 进程的 logit 六报告 IEEE-754 位型折叠 SHA-256
  **逐字节相同**且**与 R28-C 打印摘要 `63e74db4…a893` 逐字节同值**
  （跨轮 + 跨基片 + 跨组成三重见证：共驻移不动任何 logit 输出位）；
  prob-add 四组报告摘要 6/6 进程同值 `4e914da4…38f9`，且每进程内
  双遍重算断言相等（姊妹路自确定性）。
  **热层**——perf 形状 7-rep 中位：L 652.7 / 655.5 / 655.7，PAIR
  657.5 / 655.8 / 656.2，PRE 658.4 / 656.8 / 656.2 ms；同轮差
  PAIR−L = +4.9 / +0.3 / +0.5，PRE−L = +5.7 / +1.3 / +0.5，
  PRE−PAIR = +0.9 / +1.0 / +0.0——与 L 臂自身跨轮散布（3.1 ms）
  同量级，比率 1.0005–1.0088，**离 ±35 ms 带 ≥6×**；轮 2/3 全部
  坍缩到 ≤1.3 ms = 轮 1 读数属进程级抖动；小形状差符号翻转
  （PRE 轮 1 +2.5、轮 2 −0.9）= 纯抖动。
  **组成价**——生产对的 prob-add 腿：0.683–0.694 ms/调用（热）、
  每 pair-rep 4 组合计 2.956–2.958 ms，PRE 预热 100 调用 79.4–79.6
  ms（含冷层）⇒ 姊妹腿整池 **~3 ms/对报告，离带 ≥11×**，且属
  prob-add 自有已定价池（R10-C 裁决域）非 logit 道变化。
  **收口判定**：共驻剂量响应深亚带 + 位面全等 ⇒ **历轮全部孤测
  C 锚点在生产共驻形态下噪声内原样有效，无需重锚**；且即使响应
  越带也无切片内可落地物——对的编排（调用次序/组成）住在
  src/experiments/attribution-report.ts（F 区，9 文件之外），两路
  本身是 X0-11 契约强制。轴一次性收口，非候选。
- **APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠）**
  ——本轮四次读数（1 预演 + 3 正典）：APCX 无计时器独立道 **58.4 /
  60.6 / 60.1 / 60.5 ms**（与 R28-C 带 58.3–61.8 交叠），FLOORX
  最廉价逐位合法形态地板 **40.1 / 41.3 / 32.9 / 35.6 ms**（与 R28-C
  带 31.6–41.7 交叠）⇒ **组合天花板 = 18.3 / 19.4 / 27.2 / 24.9 ms
  < 35**——四读数带 18.3–27.2 与 R28-C 带 17.3–27.3 交叠，距 35 ms
  带最近读数仍差 **7.8 ms**，组合关闭维持。**sink=7.309 与
  floorSink=7.309 八读数全同，且与 R14-C/R16-C..R28-C 打印值逐位
  同值**；每次运行内 `Object.is(apcSink, floorSink)` 断言全绿（两道
  65 遍扫掠累加双精度逐位相等）——派单强制的跨轮确定性旁证成立。

除上述轴与天花板重测外，第二十九遍逐行重读**零新发现**：其余每一个
执行站点都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 /
结构交换负优化）+ R18-C 补充的 once-per-fault 拒绝类。生产中位与
地板复核见 §1/§3：本 VM 上生产中位 **657.1–659.6 ms/报告（四次运行
657.1 / 659.6 / 659.6 / 657.4；三次正典 657.4–659.6）**——与 R28-C
同装具族 S 臂带 656.4–660.4 全交叠（rep 级读数覆盖派单默认带
660.9–667.5 的下半段：单 rep 至 674.7），按 R14-C 处置规则随读重锚
为本轮带 657.1–659.6；判为 VM 类整体微快（同 VM 独立旁证：r7c 回归
资产锚点 **production 660.1 / 657.1 ms = 1.12× vs 冻结 S6-C ctl
740.2 / 739.1**——R28-C 读数 662.3/661.3 vs 737.9 = 1.12× 同构，
比率逐轮不动），非切片回归或加速（切片对 `183df9b` 零 diff）。小
形状档整报告中位 **29.9–30.1 ms**（R28-C：30.0–30.2——同带），
整条报告低于噪声带（R7-I 矩阵义务履行）。共驻轴 L 臂中位
（652.7–655.7）与主锚带相距 1.4–4.4 ms = 双装具互证带（R28-C 的
S 臂 vs 主锚同型间距），本身就是组成面结论的又一旁证。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11，`writeAttributionPair` 两路调用本轮 rg 复核在位——本轮轴
正是把这对**第一次作为整体执行形态计时**），live 面零文件改动
（`src/supervisor/` 对 9 文件导入为零，本轮 rg 复核）。仓库变更仅本
报告一个文件；无生产赢家故未提交 `/tmp/r29c/` 两个临时脚本（全文进
附录 A，sha256 归档后已删除）。

## 0. 范围与约束遵守

- 读序（强制）已履行：round-29/PLAN.md（C 段 + 全部 plan-ban 轴；
  独占 tip `ee08a83` 已含 R28-J ZREP 与 Round 28 关轮）→
  EXCLUSIONS.md（完整表，含全部 S\*-C-\* 行与 S12-C-1、S13-B-1、
  X1-3/X3-4/X3-5/X4-2 posterior/propensity/bandit 行）→ R28-C.md
  （空枚举确认，无 S28-C-\*；基片轴收口与「输入值轴域穷尽 + 执行
  基片坐标关闭」裁决域勘定——本轮轴因此落在基片之内的工作负载
  组成坐标上）→ R27-C.md PROBX 轴定义核对（策略概率输入坐标
  (μ, π)——经完整表逐条比对，证本轮轴非其重铸：零输入值剂量）→
  9 个切片源文件全量重读 → `src/experiments/attribution-report.ts`
  消费图现场核对（对的编排在切片外，本轮轴的对象域勘定）→ rebase
  后补读 R29-B.md（learned-policy container face）与 R29-A.md
  （input reference-identity / aliasing-topology face）合入摘要及
  更新版 PLAN.md（两轴均已列入 plan-ban 段与 C 段禁移植名单）。
- 基线漂移检查：本轮测量期起点 = 分支创建点 = `ee08a83`；测量全程
  独占分支未动，提交前复核发现两次 docs-only 快进（`c438879` R29-B
  合入 + R29-D 派单、`a23deea` R29-A 合入 + R29-E 派单），按派单
  规程逐次 rebase 报告分支通过至 `a23deea` 并重验冻结。冻结主张
  三次核验：`git diff 183df9b..<tip> --stat` 对 9 个切片文件为空
  ——分支创建时（`ee08a83`）、首次 rebase 后（`c438879`）、二次
  rebase 后（`a23deea`）各一次；两次快进仅触 docs/reports，测量
  数字无需重采。R29-A / R29-B 轴已入 §4 非移植清单。
- 调用图确认（派单强制项，本轮 rg 复核）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 的 `writeAttributionPair`
  消费（双归因两路在位；调用次序 = logit 先、prob-add 后，同进程
  同批行——本轮轴的生产形态依据）；`src/supervisor/` 对 9 个切片
  文件的导入为**零**——live = R0 等价维持，R1 未接线，
  `src/supervisor/model-router.ts` 零字节未触。
- 共驻对象勘定（本轮新轴的对象域）：`fitProbabilityAdditive` 要求
  恰一个场景（多场景抛 DomainValidationError），故生产对调用方持
  单场景行组；共享面 = posterior.ts 的 `betaQuantileLcb` →
  `inverseRegularizedIncompleteBeta` → `betacf` / `lnGamma`（两估计
  器各自 import，调用画像不同：logit 每报告 1 次 muLcb，prob-add
  每效应 2 次 betaInterval）。三臂的 prob-add 腿输入 = 冻结 perf
  夹具按 scenarioId 过滤出的 4 个单场景组（94/108/88/110 行，保序
  保身份，生产合法形态）；**被计时的 logit 对象在所有臂都是整条
  冻结 perf 夹具——零输入维位移**。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S28-\*（含 S12-C-1，重开条件：无）
  未重提、未立道、未重测；R13-C..R28-C 二十八个无名关闭（PIVCSE/
  FINTAIL/REFZ/TSORT/DEG/SWORD/XF64/IRLSDP/CMPH/STORD/RIDGE/ICOL/
  SFILL/NSQRT/PMV/OSTZ/TAILG/REJX/FITQ/SCALEX/YMIX/SEEDX/ORDX/OCCX/
  AGEX/PRICEX/PROBX/**基片实例化**）未重探未补铸未换址重提——
  **SCALEX 未重测、YMIX 未重测、SEEDX 未重测、ORDX 未重测、OCCX 未
  重测、AGEX 未重测、PRICEX 未重测、PROBX 未重测、基片轴未重测**
  （共驻轴全部剂量点都在冻结 fixture 与单一基片之上：同一
  0xbeef/0x51a1/0x5eed 构造字节、同一发射次序、同一 options、全臂
  tsx+src、同一 Node 22.22.2——logit 摘要与 R28-C 逐字节同值本身
  就是八输入维 + 基片维恒等的位级见证；三臂只换姊妹内核的执行
  有无与次序，fixture 生成代码在 runner 内逐字节同一）；FITQ 重开
  条件未触发（options 全程默认值/文档化 PERF 值）；ITERX/COLDX/
  AAFLR/GCAX 四轴未补铸（热层读数取 7-rep 中位，首 rep 704.8–718.9
  ms 冷 JIT 尾巴被中位吸收不进入读数——三臂协议同构，冷尾对差分
  自消）；嵌套序 × 布局组合空间（S4-C-2 + S5-C-1..4 + S12-C-1）未
  重开；CMB 组合未重堆叠（本轮 APCX/FLOORX 重测系派单强制天花板
  复测，FLOORX 逐位合法性 59 名额外断言全绿）；PLSEG/PMV/TAILG
  备忘未重提；config 坐标（halfLife/minSamples/priors/lcbKind +
  bandit/shadow config）维持 R27-C 诚实划界，零剂量。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动；X2-3 数值路径未触；单线程逐位审计基底不动；fail-open 族
  维持关闭；无 fsync / 完整性再哈希移除（本切片不涉盘，天然成立）。
- 环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0——回到
  R1-C..R28-C 的处理；VM 默认 22.14.0 位于 `/exec-daemon/node` 遮蔽
  PATH，已显式前置 nvm bin 修正，未用于任何读数）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`（`pnpm gate` 全绿，见 §5）。九个
  共驻进程共享同一 Node 22.22.2 与同一 tsx+src 基片（引擎代与基片
  坐标双冻结——共驻轴动的是进程工作负载组成，不是引擎或管线）。

## 1. 生产中位与锚点复核（本 VM 实测，未拷贝历轮数字）

本轮测量装具为**复刻道保真电池 + 未仪表化生产导入直测**：主锚脚本
携带落地管线的逐语句复刻道（buildDesign / computeSupports /
canonicalRowKeys / irls / onProbabilitiesFor /
averagePredictiveComparison / percentile 全文照抄，与 R28-C 附录
逐语句同一），其忠实性先经 **34 例形状电池 + perf + small 全报告 vs
生产 `fitLogitAdditive` 导入逐位全绿**（Object.is 含符号零/NaN，每
运行 2,768 项检查）再采信；生产中位数与全部共驻轴读数均由**未仪表
化生产导入**直接测得。**范围注记（诚实划界，继承 R28-C）**：本轮未
重建 R27-C 型逐池仪表化复刻管线（派单强制项为生产中位 + APC 天花板
重锚，均已履行）；池结构证据链为（a）切片对 `183df9b` 零 diff ⇒ 池
组成不可能移动，（b）站点计数在本 VM 本轮由回归资产独立重印——
r2c 打印 **3,586,400 行访问 / 8,966 次迭代 → 1,249,652 去重 dot**，
r3c 打印 **累加消乘 177,843,236 → 0**，r1c 打印 **design.build 每
拟合 358,006 次**——与 R10-C..R28-C 锚定值逐位相同（跨轮确定性
旁证）。

perf 夹具与 R10-C..R28-C 完全一致（fixtureRng(0xbeef)，4 场景 ×
6 模型 × 8 项目，rows=400, passRate=0.6，bootstrap=200,
seed=20260818 → p=60，201 拟合）；小形状档同一 0x51a1 夹具（3×3×3 /
120 行，bootstrap=200）。

本轮共四次全量运行：run 0 为**预演运行**（脚本与正典运行**逐字节
相同**——预演一次通过、预演后零改动，四次运行同一脚本，sha256 见
附录 A；本轮无任何废弃试跑），run A / run B / run C 为正典运行。
四次运行全部计入处置依据，不作剔除；每次运行 2,768 项逐位检查全绿。

生产整报告中位（每次运行内 7 测量取中位）：**657.1 / 659.6 /
659.6 / 657.4 ms**——四读数带 657.1–659.6 与 R28-C 同装具族 S 臂带
656.4–660.4 全交叠（S 臂 = 单导入 runner 形态，与本轮主锚同装具
族）；对 R28-C 主锚带 660.1–661.3 低 0.5–4.2 ms、对派单默认带
660.9–667.5 低 1.3–10.4 ms，但 rep 级读数（单 rep 至 674.7，run A
含 660.1/662.3/665.9）覆盖两带下半段，且同 VM 独立旁证 r7c 打印
**production 660.1 / 657.1 ms = 1.12× vs 冻结 S6-C ctl 740.2 /
739.1**（R28-C：662.3/661.3 vs 737.9 = 1.12× 同构，**比率逐轮
不动**）⇒ 判为 VM 类整体微快的带内往返，按 R14-C 处置规则**随读
重锚为本轮带 657.1–659.6（正典 657.4–659.6）**；非切片回归或加速
（切片对 `183df9b` 零 diff）。

各运行 7-rep 明细：

```text
run 0: median 657.1  (reps 658.3 661.7 656.4 656.8 661.4 657.1 655.4; small 30.0)
run A: median 659.6  (reps 660.1 662.3 659.2 657.9 658.8 665.9 659.6; small 30.0)
run B: median 659.6  (reps 661.3 674.7 660.5 657.2 659.6 657.5 658.6; small 30.1)
run C: median 657.4  (reps 660.7 673.9 657.4 659.4 657.4 655.7 655.1; small 29.9)
```

小形状档整报告中位 **29.9–30.1 ms**（R28-C：30.0–30.2——同带）
——小配置态整条报告低于噪声带，调用矩阵小态一侧整体收口维持
（R7-I 矩阵义务履行）。生产自确定性复核：3 seed × 各 2 次运行全
字段 Object.is 全绿（每运行含于 2,768 项）。共驻轴 L 臂（同基片
异装具：单导入 runner 无复刻电池前置）中位 652.7–655.7 与主锚带
相距 1.4–4.4 ms——双装具互证带与 R28-C（S 臂 656.4–660.4 vs 主锚
660.1–661.3）同型同阶，本身即组成面「单机位 ms 级装具项」的又一
读数。

## 2. 第二十九组枚举（诚实结果：空；零新站点 + 一条新轴 + 一次按规天花板重测）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过，且本轮从未点名站点数为零**（连续第十一遍）：

| 站点/轴 | 关闭方式 | 证据 |
| --- | --- | --- |
| 从未点名站点普查：**零**（连续第十一遍） | 逐行三问全覆盖 | 29 遍扫描把 9 文件每一个执行站点指到既有覆盖物：offline-logit 编排头（DEG/BASE）、irls 全站点（S2-C/S3-C/S6-C/S7-C 落地体 + S10-C-1/S9-C-3/S9-C-4/S6-C-5/6/S5-C-7/S4-C-5/6/MUCSE/SWORD/STORD/RIDGE/ICOL/IRLSDP + 收敛尾 S2-C-3/S8-C-3/NSQRT + ITERX/FITQ）、solve 全站点（COPY/S4-C-3/XF64 + PIVCSE/S5-C-6 + PIVSWAP + FZERO + S4-C/S5-C 落地体 + S12-C-1 + S6-C-7 + SFILL/FINTAIL + S8-C-4）、APC（S1-C-1/S2-C-2/S2-C-4/S4-C-4/S8-C-2/S10-C-2 + FLOORX 天花板）、on-prob（S7-C-4/S8-C-1）、采样环（SAMP/S3-C-3/S2-C-5）、抽取效应环（S7-C-1/DMAP）、尾部效应环（TSORT/CMPH/REFZ/TAILG）、posterior（X1-3/X3-4/S1-C-7/OSTZ/S1-C-4 + 落地 memo + AGEX 封顶）、prob-add（S1-C-6/PMV/PLSEG——**其与 logit 的共驻执行形态本轮由 PAIRX 轴直测封顶**）、propensity（S1-C-8/X3-5 + PROBX 四面封顶）、r1（X1-4/S1-C-10/S1-C-2/3 + PRICEX 封顶）、bandit/shadow（S1-C-9/X4-2 + R27-C 划界读数 ns 域）、r1-shadow-report（X1-5 + REJX + PRICEX selectedCost 封顶）——R18-C「TAILG 是尾部段最后一个从未点名位置」的判定经 R19-C..R28-C 与本遍连续十一轮核实成立；输入值轴域（R26-C/R27-C）+ 执行基片坐标（R28-C）关死后，本轮把定价域推进到基片之内的工作负载组成坐标，**站点层零位移** |
| process workload-composition face 的 C 区双估计器共驻实例化（新轴，非候选；face 名归 R29-C，观察绰号 **PAIRX**——plan-ban 用，不铸 ID、不写入排除表） | 位面 9/9 摘要全等且与 R28-C 摘要逐字节同值 + 热层三臂交错直测深亚带 + 组成价 µs–低 ms 域 + 无切片内可落地物（编排在 F 区 + X0-11 契约强制两路） | **对象**：本切片的生产执行形态 = 双归因对（`writeAttributionPair` 同进程先 logit 后 prob-add，同批行；两估计器共享 posterior.ts Beta 分位机器且调用画像不同——logit 每报告 1 次 muLcb，prob-add 每效应 2 次 betaInterval）；R1-C..R28-C 全部生产读数测在 prob-add 从未执行的进程里，共驻坐标从未剂量化。响应变量 = 整报告中位 + 全输出位型 + 姊妹腿单价。**剂量设计**：三臂 {L=孤测（历史正典形态，prob-add 永不调用）, PAIR=生产交错（每计时 rep 后对 4 个单场景组各调一次 prob-add，计时器外）, PRE=异质反馈先行（首个 logit 调用前 25 遍 × 4 组 prob-add，共享机器先在 prob-add 画像上分层）}，runner 逐字节同一（臂仅由 R29C_ARM 选择；两模块全臂 import——模块图冻结）、全臂单一基片 tsx+src、全部输入维冻结（被计时 logit 对象 = 整条冻结 perf 夹具；prob-add 腿输入 = 同夹具按 scenarioId 过滤的 4 组 94/108/88/110 行，保序保身份生产合法形态），交错 3 轮 × 3 臂 = 9 进程。**位面**：logit 六报告（perf+small+3 电池+确定性重跑）IEEE-754 位型折叠 SHA-256 —— 9/9 摘要 `63e74db4…a893` 全同且**与 R28-C 打印摘要逐字节同值**（跨轮 + 跨基片 + 跨组成三重见证：共驻移不动任何 logit 输出位，同时见证八输入维 + 基片维恒等）；prob-add 四组报告摘要 6/6 同值 `4e914da4…38f9`，每进程内双遍重算断言相等。**热层**：L 652.7/655.5/655.7，PAIR 657.5/655.8/656.2，PRE 658.4/656.8/656.2 ms；同轮差 PAIR−L +4.9/+0.3/+0.5、PRE−L +5.7/+1.3/+0.5、PRE−PAIR +0.9/+1.0/+0.0——与 L 臂自身跨轮散布 3.1 ms 同量级（比率 1.0005–1.0088），**离 ±35 ms 带 ≥6×**；轮 2/3 差全部 ≤1.3 ms ⇒ 轮 1 读数属进程级抖动非剂量响应；小形状差符号翻转（+2.5/−0.9/+0.3）= 抖动。**组成价**：prob-add 腿 0.683–0.694 ms/调用（热）、每对报告 4 组合计 2.956–2.958 ms、PRE 预热 100 调用 79.4–79.6 ms 含冷层 ⇒ 姊妹腿整池 ~3 ms/对，离带 ≥11×，且属 prob-add 自有已定价池（R10-C 每报告亚 ms–低 ms 裁决域）非 logit 道变化——once-per-pair 派单拒绝类。**收口算术**：热层最大同轮差 5.7 ms ⇒ 越带需响应再放大 6×，而轮间幅度不稳定（4.9→0.3→0.5）恰为抖动签名；即便未来响应越带，切片内也无可落地物——对的编排（次序/组成/是否共进程）住在 src/experiments/attribution-report.ts（F 区，9 文件外），两路本身 X0-11 契约强制，唯一后果是锚点重标定而非候选。**判定**：历轮全部孤测 C 锚点对生产共驻形态在噪声内原样有效；轴一次性收口。重开仅当（a）共享 posterior 机器引入跨调用状态（X1-1 域，不预期）或（b）pair 编排移入本切片 / 姊妹调用被移进 logit 计时对象内（契约/F 区事件，非本切片自由度） |
| APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠） | 天花板仍亚带 + sink 逐位同值 + FLOORX 逐位合法性 59 名断言全绿 | APCX **58.4 / 60.6 / 60.1 / 60.5**（与 R28-C 带 58.3–61.8 交叠）；FLOORX **40.1 / 41.3 / 32.9 / 35.6**（与 R28-C 带 31.6–41.7 交叠）⇒ 天花板 **18.3 / 19.4 / 27.2 / 24.9 < 35**——四读数带 18.3–27.2 与 R28-C 带 17.3–27.3 交叠，按 R14-C 规则随读重锚为本轮带 18.3–27.2，距 35 带最近读数仍差 7.8 ms，组合关闭维持；**sink=7.309 与 floorSink=7.309 八读数全同，且与 R14-C/R16-C..R28-C 打印值逐位同值**，每运行内 `Object.is(apcSink, floorSink)`（65 遍扫掠累加双精度）全绿——派单强制的 APC 关闭维持条件成立；S8-C-2/S10-C-2/S2-C-4/S4-C-4 与 CMB 禁令全部维持原状 |

以上无一构成候选：从未点名普查结果为零（无新站点可立道）；共驻轴
是执行坐标定价不是变换（三臂全部执行同一落地形态，零变体决斗——
其唯一潜在「候选族」= 改 pair 编排，属 F 区文件 + X0-11 契约禁区，
且收益上界即本轮实测同轮差 0.0–5.7 ms 亚噪声）；APCX/FLOORX 是按规
天花板测量不是变换。**故第二十九组候选集为空，不铸 S29-C-1 及后续
编号**，遵循 R11-C..R28-C 无号关闭先例。

**枚举收口：** 除上表外，第二十九遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖）。R27-C
「切片公开输入面上不存在从未定价的数值坐标」与 R28-C「执行基片
坐标无越线点」的裁决本轮零位移地维持；本轮把定价域第一次扩到
**进程工作负载组成坐标**（同进程内姊妹内核的执行有无与次序），并
证明该坐标对本切片三个响应面（位/热/组成价）全部无越线点。仍不
剂量化、不铸名的坐标（诚实划界，继承 R27-C/R28-C）：options 坐标
（maxIter/bootstrap——契约面）、config 坐标（halfLife/minSamples/
priors/lcbKind + bandit/shadow 的 seed/epsilon/explorationBudget/
comparisonBudgetUsd/comparisonCostUsd——config 面，R24-I 邻域禁
移植域）、`TaskFeatures.contextTokens/outputTokens`（探索面 ns 域
划界封顶）、字符串身份坐标（IDG 冻结）、引擎版本坐标（本轮九进程
共享 Node 22.22.2）、执行基片坐标（R28-C 关闭，本轮全臂冻结在
tsx+src 单点）、**切片内其余内核的共驻组合学**（routeR1/bandit/
shadow/propensity 互相共驻——宿主全部 ns–µs 域已划界，组合共驻
无独立越线机制，诚实记名不剂量）。本轮核实换名再提交拒列：零。

## 3. 三大池地板与组合出路复核

- **solve 池（本轮未逐池重仪表；结构证据 = 零 diff + 站点计数跨轮
  逐位）**：消元主体 ≈ 6.45 亿次冻结顺序浮点乘减/报告，8,966 次调用
  计数由 r2c 在本 VM 本轮重印逐位相同（r3c 另重印累加消乘
  177,843,236 → 0）。R6-C §2.5 七类分解 + S10-C-3 + S12-C-1（嵌套
  序，重开条件：无）+ R11-C COPY + R12-C FZERO/PIVSWAP + S5-C-6 +
  R13-C PIVCSE/FINTAIL + R14-C XF64 + R16-C SFILL 之后本轮零新位置。
  **共驻维度对该池零触达**（本轮位面见证：solve 输出经由报告位型
  9/9 摘要全等；热层同轮差 0.0–5.7 ms 若全部记在 solve 池头上也不足
  带宽 1/6）。触碰地板本体的机制只剩 SIMD/线程（S9-C-1/2 + 不变量
  禁止）与数值路径改道（X2-3 冻结）。
- **irls 自身池**：可省项全部已关闭（S2-C/S6-C/S7-C 落地本体之上：
  S10-C-1 决斗带内、S9-C-3 符号翻转、S6-C-6 上界 2–4、S5-C-7 无正
  决斗、S9-C-4、S4-C-5/6、S2-C-3、S8-C-3、R11-C RID/CNT/EXP/ALLOC、
  R12-C MUCSE、R14-C SWORD/IRLSDP、R15-C STORD/RIDGE、R16-C ICOL、
  R17-C NSQRT）。行访问/去重 dot 计数（3,586,400 / 1,249,652）本轮
  重印逐位。零新位置。
- **APC 池**：派单强制天花板重测照常执行（§2 表）：APCX 无计时器
  独立道 **58.4–60.6 ms**（**sink=7.309 与 R14-C..R28-C 逐位同值** =
  跨轮确定性旁证成立）；FLOORX 最廉价逐位合法形态地板 **32.9–41.3
  ms** ⇒ **组合天花板 = 18.3–27.2 ms（四次读数）< 35**。十四轮天花板
  读数 12.1–23.8 / 13.8–21.6 / 12.1–20.9 / 13.9–24.0 / 17.4–21.6 /
  12.5–19.8 / 17.1–21.8 / 14.6–16.0 / 15.9–22.7 / 20.8–25.6 /
  19.5–22.9 / 15.7–21.0 / 17.3–27.3 / **18.3–27.2** 稳定亚带（四读数
  全部 < 35 且带与 R28-C 带交叠），组合关闭维持。
- **引擎侧三轴（R15-C/R16-C 收口，本轮未重测未补铸）**：COLDX 预热
  份额、GCAX 收集器份额、ALLOC 分配封顶——三轴均无切片内可受理
  变换；本轮全部热层读数为 7-rep 中位稳态口径（首 rep 704.8–718.9
  ms 冷尾巴被中位吸收，且三臂协议同构使冷尾对同轮差分自消）。
  **共驻轴与 COLDX 正交**：COLDX 是同内核进程内预热份额；本轮剂量
  是姊妹内核的执行有无与次序，7-rep 中位协议全臂恒等。
- **拒绝面（R18-C REJX 轴收口，本轮未重测未补铸）**：31 终点
  once-per-fault 定价原样引用；四分类 + once-per-fault 拒绝类继续
  穷尽（本轮组成价读数按其姊妹池 + once-per-pair 拒绝类记名）。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测带级摆幅带内），
  维持关闭。本轮探针无一是稳定越带正项（普查为零、共驻轴是执行
  坐标定价、APCX/FLOORX 是天花板测量），无新成员可入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建
面开放 / X2-3 数值路径解冻 / 单线程审计基底变更 / maxIter=50、
tol=1e-8、bootstrap/seed 契约、rows/OutcomeObservation 输入契约、
X0-11 双归因契约变更——均不预期）；（b）现实规模位移触达 SCALEX
标定的越线点（今日生产流量在 perf 夹具当量及以下，未触达；八个
输入维度经 SCALEX/YMIX/SEEDX/ORDX/OCCX/AGEX/PRICEX/PROBX 证明均无
越线点，执行基片维度经 R28-C 证明热层比率 ~1.00，**工作负载组成
维度经本轮证明同轮差 0.0–5.7 ms 深亚带、位面全等**）；（c）共享
posterior 机器引入跨调用状态或 pair 编排移入切片内使共驻响应实测
越带（契约/F 区事件，非切片自由度，后果是锚点重标定而非候选）；
或（d）指出「重命名 / 亚带 / 逐位违例 / 结构交换负优化 /
once-per-fault 拒绝」之外的第六类成本——二十九遍枚举未再发现。

## 4. 重命名检查（派单强制项）

- **本轮共驻轴 vs R28-C 基片实例化 / R27-H execution-substrate face
  （派单点名禁移植/禁重铸）**：基片轴的剂量坐标是**解释器 + 模块
  管线**（tsx vs node × src vs dist——R28-C 三臂各跑不同基片）；
  本轮九进程全部钉死在单一基片（tsx+src，R28-C 的 S 臂），剂量
  坐标是**进程内执行了哪些切片内核、以何次序**——基片恒等下的
  工作负载组成，两轴正交（本轮 logit 摘要与 R28-C 逐字节同值恰是
  基片维恒等的证词）。非重铸、非移植；face 名 process
  workload-composition face 归 R29-C，观察绰号 PAIRX 仅 plan-ban
  用。
- **共驻轴 vs COLDX（R16-C）/ ITERX / AAFLR / GCAX**：COLDX 是
  **同内核进程内**预热份额轴（首 N 次拟合的 JIT 爬坡占比）；本轮
  三臂共享同一 7-rep 中位协议（冷尾吸收方式恒等），剂量的是
  **异质内核**的存在与先后——PRE 臂不是预热份额复测（它在 logit
  之前插入的是 prob-add 的**异质**调用画像，不是 logit 自身的
  预热）。ITERX（迭代计数普查）本轮只经 r2c 重印锚定值。非重铸。
- **共驻轴 vs PROBX（R27-C）/ 八输入维（SCALEX/YMIX/SEEDX/ORDX/
  OCCX/AGEX/PRICEX，全部派单点名禁重铸）**：八维全部作用在**输入
  数据值**上；本轮 fixture 冻结（同构造字节、同 options、同发射
  次序——runner 内 fixture 生成代码逐字节同一，摘要同值是八维
  恒等的位级见证），被计时 logit 对象在所有臂都是整条冻结夹具。
  prob-add 腿的按场景分组是**姊妹内核的生产合法输入形态**（其
  单场景契约强制），不进入 logit 计时对象——零输入维位移，ORDX
  （记录间序）与 SCALEX（体量）均未被触碰。非重铸，八轴未重测。
- **共驻轴 vs R27-B batch-cardinality bottom + prelude intercept
  face（PLAN 点名禁移植）**：R27-B 的剂量对象是 B 区路由入口的
  批基数（M=0/1 底部 + 前奏拦截）；本轮被计时内核的调用次数全臂
  恒等（7 rep），剂量的是姊妹内核的共驻。非移植。
- **共驻轴 vs R28-F boundary call-protocol face / R28-H boundary
  argument-slot materialization face（PLAN 点名禁移植）**：两者
  剂量调用**方式**（协议形态 / 实参槽物化形态）；本轮所有调用在
  所有臂都是同一形态的普通直接调用（实参物化逐字节同码），剂量
  的是调用**组成**（谁在同进程执行过）。非移植。
- **共驻轴 vs R29-B learned-policy container face（mid-run 合入，
  PLAN 点名禁移植）**：R29-B 的剂量对象是 B 区 learned 路由容器的
  尺寸轴底部 × 行排列 × 写手形状（avoid/prefer 行几何，宿主
  assignPlanned）；本切片 9 文件内不存在 learned-policy 容器，本轮
  零容器几何剂量——剂量对象是估计器执行组成，宿主与机制均不同。
  非移植、非重铸。
- **共驻轴 vs R29-A input reference-identity / aliasing-topology
  face（mid-run 合入，PLAN 点名禁移植）**：R29-A 的剂量坐标是
  **输入对象的引用同一性/别名拓扑本身**（A 区调用方对象共享 vs
  等值异身份）；本轮输入别名拓扑在三臂间**恒等且从未作为剂量
  变量**——被计时 logit 对象在所有臂是同一构造路径的整条夹具，
  PAIR/PRE 臂的 prob-add 腿按 scenarioId 过滤共享行对象引用恰是
  生产契约形态（`writeAttributionPair(rows)` 把同一批行对象交给
  两估计器），该共享在臂内臂间零位移。剂量变量是姊妹内核的执行
  有无与次序，不是输入别名几何。非移植、非重铸。
- **共驻轴 vs S5-I-\*/S6-I-\*（CLI 惰性化/冷导入族）与 R28-C 冷面
  读数**：本轮两模块在**所有臂**都被 import（模块图冻结），冷导入
  价不被剂量——共驻剂量从首次调用开始；once-per-process 导入类
  维持派单拒绝类记名，零重开。
- **共驻轴 vs X1-1（模块级隐藏缓存）/ X1-6（跨 episode 记忆化）**：
  本轮零缓存零记忆化——纯测量轴，三臂全部执行同一落地形态；共享
  posterior 机器的无状态性（纯函数）正是位面全等的机制前提，本轮
  未改其一字节。非重开。
- **APCX/FLOORX 重测 vs S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 / CMB
  禁令**：本轮重测系**派单强制天花板复测**，产出仅用于天花板差值
  算术与 sink 逐位核对；FLOORX 道的 59 名逐位合法性断言是对既有
  S8-C-2 引理的复核不是新道；不重跑任何已关 ID 决斗、不构建堆叠
  候选、不产出可落地形态。
- **换名再提交拒列（本轮核实）**：零。R13-C..R28-C 二十八个无名
  关闭未重探、未补铸、未换址重提；S10-C-1..3 / S12-C-1 / RID / CNT
  零重提；X2-1 重写维持排除（S7-C 的 0/1 形态即落地形态）；COTARG /
  VALCLASS / CALLB / SHAPEK / KFAN / JB / RUNLIM / CKMIX / TERMCLASS /
  SCHEDWIN / XPROC / NAMESHAPE / CG / RTL / DELINV / RVL / SIDEC /
  PWL / ZREP 与全部他区无名轴（M=1 catalog collapse、
  approval-authority、state serialization-boundary provenance、
  operand-residency、adaptation-state、identity-coordinate、
  plan-header identity-geometry、injection command-kind、
  consumer-stage string-representation / operand-realm、
  injection-seam default-arm、input storage-state、edge
  condition-kind、argv surface-form、process-environment、
  collation-locale、value-domain / string content-class、scalar
  numeric value-class、batch-cardinality bottom + prelude
  intercept、policy-content tree-geometry、input access-path
  mechanics、join-policy、process output-channel disposition、
  catalog-face row-arrangement (permutation)、input key-set
  geometry、contents-store population、boundary call-protocol、
  proposal write-path termination-leg、decision-gate resolution、
  boundary argument-slot materialization、invocation-identity /
  path-referent spelling × cwd、durable-store zero-state
  provenance、learned-policy container、input reference-identity /
  aliasing-topology）零移植。

## 5. 测试与验证

环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0）、pnpm 10.17.1、
`pnpm install --frozen-lockfile`。生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录 A）
npx tsx /tmp/r29c/r29c-anchor.mts        # ✓ 2,768 项逐位 × 4 次运行全绿
                                         #   （34 例复刻道保真电池 + perf + small
                                         #    + 3 seed × 2 自确定性
                                         #    + FLOORX 59 名逐位合法性
                                         #    + sink===floorSink 双精度断言
                                         #    + APCX/FLOORX 天花板重测内嵌）
# 共驻轴（runner 逐字节同一，臂由 R29C_ARM 选择；全臂 tsx+src；交错 3 轮）
R29C_ARM=L    npx tsx /tmp/r29c/r29c-coresidency-runner.mjs
R29C_ARM=PAIR npx tsx /tmp/r29c/r29c-coresidency-runner.mjs
R29C_ARM=PRE  npx tsx /tmp/r29c/r29c-coresidency-runner.mjs
# ✓ logitDigest 9/9 = 63e74db4ac45eed0f2a219e159b5f2d65b9ea8d962408e66858c7b459dea3893
#   （与 R28-C 打印摘要逐字节同值）
# ✓ probDigest 6/6  = 4e914da4139ed4802b1c3a4fc27a50e1d562b45ea63105e27c211799f3f438f9
#   （每进程内双遍重算断言相等）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项（design.build 358,006/拟合）
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项（3,586,400 → 1,249,652 @ 8,966 迭代）
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项（累加消乘 177,843,236 → 0）
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项 ×2 次运行
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 660.1 / 657.1 ms（1.12× vs 冻结 S6-C ctl 740.2 / 739.1）——与
# §1 生产正典中位带（657.4–659.6）互证，比率与 R28-C（1.12×）逐轮不动。
```

代表性一次测量彩排全量输出（正典 run A；run 0/B/C 数字见 §1/§2）：

```text
scenario 1 (replica-lane fidelity battery, 34 cases + perf + small, bitwise vs production import): OK
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is): OK
  shape small (3 models x 3 projects, 120 rows): production median 30.0 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 659.6 ms/report (7 reps)
  perf reps: 660.1 662.3 659.2 657.9 658.8 665.9 659.6
probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) 0.302 ms/fit -> x201 fits = 60.6 ms/report (base-shape approximation; sink=7.309)
probe FLOORX: cheapest bitwise-legal sweep 0.205 ms/fit -> x201 fits = 41.3 ms/report floor; APC combination ceiling on THIS VM = 19.4 ms (floorSink=7.309)

ALL EQUIVALENCE CHECKS PASSED (2768 bitwise checks)
```

共驻轴九进程原始读数（交错序：每轮 L→PAIR→PRE；logitDigest 九行
全同、probDigest 六行全同故各列一次；组 94/108/88/110 行 = 400）：

```text
logitDigest (all 9): 63e74db4ac45eed0f2a219e159b5f2d65b9ea8d962408e66858c7b459dea3893  (== R28-C)
probDigest (6 co-resident): 4e914da4139ed4802b1c3a4fc27a50e1d562b45ea63105e27c211799f3f438f9
round 1: L    perf 652.65 (reps 705.0 652.7 651.3 679.4 652.5 649.2 659.2; small 30.28)
         PAIR perf 657.51 (reps 718.2 657.5 653.3 676.8 647.6 656.3 658.7; small 30.38; probLeg/rep 2.956; probAdd/call 0.6928)
         PRE  perf 658.39 (reps 711.8 654.2 653.2 677.4 656.7 659.2 658.4; small 32.76; preWarm 79.4; probAdd/call 0.6858)
round 2: L 655.49 (small 30.95) / PAIR 655.78 (small 30.51; leg 2.958; call 0.6936) / PRE 656.77 (small 30.08; warm 79.6; call 0.6830)
round 3: L 655.71 (small 30.09) / PAIR 656.17 (small 30.08; leg 2.958; call 0.6936) / PRE 656.21 (small 30.39; warm 79.5; call 0.6844)
同轮差: PAIR−L +4.86/+0.30/+0.46 ms; PRE−L +5.74/+1.28/+0.49; PRE−PAIR +0.88/+0.99/+0.03
比率:   PAIR/L 1.0074/1.0005/1.0007; PRE/L 1.0088/1.0020/1.0008 — 全部深亚带（≥6× 低于 ±35 ms）
组成价: prob-add 腿 0.683–0.694 ms/调用（热）、2.956–2.958 ms/对报告（4 组）；PRE 预热 100 调用 79.4–79.6 ms
```

## 6. 新增排除 ID：无

第二十九组候选集为空。从未点名站点普查结果为零（连续第十一遍；
R18-C「最后一个从未点名位置」判定经 R19-C..R28-C 与本轮十一遍
核实）。process workload-composition face 的 C 区双估计器共驻
实例化为执行坐标定价轴（重开条件：共享 posterior 机器引入跨调用
状态（X1-1 域）或 pair 编排移入切片内使共驻响应实测越出噪声带——
均属契约/F 区事件，非切片自由度，后果是锚点重标定而非候选。共驻
维度本身**无越线点**：位面 9/9 摘要全等且与 R28-C 逐字节同值、
热层同轮差 0.0–5.7 ms 与臂内散布同量级且离带 ≥6×、组成价 ~3 ms/对
报告属姊妹自有池 once-per-pair 拒绝类；不铸 ID、不写入排除表，
face 名归 R29-C，观察绰号 PAIRX 仅 plan-ban 用）。APCX/FLOORX 为
按规天花板重测（天花板 18.3–27.2 < 35 稳定亚带，
sink=floorSink=7.309 八读数逐位同值）。生产中位随读重锚
657.1–659.6 ms（正典 657.4–659.6），与 R28-C 同装具族 S 臂带
656.4–660.4 交叠，r7c 比率 1.12× 逐轮不动。

## 附录 A：测量彩排脚本（完整，可复现；归档后临时文件已删除）

- `/tmp/r29c/r29c-anchor.mts` — sha256
  `772e0bd0f6ad05ae78fdf90c5c37ad792f9c0986317b88afc52e454464fb0ea6`
  （与 R28-C 主锚逐语句同协议，仅头注更新为 R29-C；四次运行——
  1 预演 + 3 正典——逐字节同一脚本，预演一次通过零改动，本轮无
  任何废弃试跑）
- `/tmp/r29c/r29c-coresidency-runner.mjs` — sha256
  `8e09dea0ef6c7915b39a0707a8ea904a663e84b3694c107ee077778a3c026247`
  （九进程逐字节同一；位型折叠函数与报告清单与 R28-C 基片 runner
  逐语句同一，使 logitDigest 与 R28-C 打印摘要可直接比对）
- 无其他临时文件；原始读数已全文誊入 §5。

### 主锚 `/tmp/r29c/r29c-anchor.mts`

```ts
/**
 * Round 29 / R29-C measurement re-anchor + twenty-ninth-group closure probes
 * (offline routing slice, twenty-ninth pass). NOT committed — the twenty-
 * ninth-group enumeration carries no candidate lanes that reach the
 * acceptance bar; full source goes into the R29-C report appendix.
 *
 * Protocol is verbatim R28-C (which is verbatim R14-C lineage):
 *  - replica-lane fidelity battery: a verbatim copy of the landed
 *    fitLogitAdditive pipeline (buildDesign / computeSupports /
 *    canonicalRowKeys / irls / onProbabilitiesFor /
 *    averagePredictiveComparison / percentile) compared bitwise
 *    (Object.is, includes signed zero / NaN) against the production import
 *    on a 34-case shape battery + the perf fixture + the small fixture.
 *  - production median re-anchor: perf shape (4 scenarios x 6 models x
 *    8 projects, rows=400, passRate=0.6, bootstrap=200, seed=20260818)
 *    7 reps -> median; small shape (3 x 3 x 3, 120 rows) 3 reps -> median.
 *  - production self-determinism: 3 seeds x 2 runs, all report fields
 *    Object.is.
 *  - APCX probe (dispatch-mandated ceiling re-measure): standalone
 *    timer-free APC sweep (59 non-intercept names x 400 rows, base-fit
 *    coefficients) per-fit cost x 201 fits; sink = sum of all APC values
 *    (cross-round determinism witness, expected print 7.309).
 *  - FLOORX probe: cheapest bitwise-legal sweep (S8-C-2 support-ascending
 *    off-sum form, never landed — X2-1 lemma premises hold: 0/1 design,
 *    +0.0 start, finite beta), per-name values asserted Object.is against
 *    the APCX sweep, then per-fit cost x 201 fits; combination ceiling =
 *    APCX - FLOORX must stay < 35 ms with floorSink === sink bitwise.
 *
 * The co-residency axis arms run out of the byte-identical runner
 * r29c-coresidency-runner.mjs (see report appendix), not out of this file.
 */
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.ts";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.ts";
import { solveSymmetric } from "/workspace/src/routing/lin-alg.ts";
import type { AttributionEffect, AttributionLabel, AttributionReport } from "/workspace/src/routing/offline-types.ts";

let checks = 0;
function assertSame(label: string, a: unknown, b: unknown): void {
  checks += 1;
  if (!Object.is(a, b)) {
    throw new Error(`MISMATCH ${label}: ${String(a)} !== ${String(b)}`);
  }
}
function assertTrue(label: string, v: boolean): void {
  checks += 1;
  if (!v) throw new Error(`ASSERT ${label}`);
}

/* ------------- replica lane: verbatim copy of the landed pipeline ------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}
interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}
function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}
function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);
  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);
  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);
  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });
  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}
interface FitResult {
  readonly coefficients: readonly number[] | null;
}
function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}
function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}
function irls(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
): FitResult {
  const p = design.names.length;
  const n = rows.length;
  const eta = new Array<number>(n).fill(0);
  const mu = new Array<number>(n).fill(0);
  const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
  const xtwz: number[] = new Array<number>(p).fill(0);
  const stamp = new Int32Array(keySpace);
  const etaByKey = new Float64Array(keySpace);
  const muByKey = new Float64Array(keySpace);
  let beta = new Array<number>(p).fill(0);
  for (let iter = 0; iter < maxIter; iter++) {
    const mark = iter + 1;
    for (let i = 0; i < n; i++) {
      const key = keys[i]!;
      if (stamp[key] !== mark) {
        stamp[key] = mark;
        let value = 0;
        const active = supports[i]!;
        for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
        etaByKey[key] = value;
        muByKey[key] = sigmoid(value);
      }
      eta[i] = etaByKey[key]!;
      mu[i] = muByKey[key]!;
    }
    for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
    xtwz.fill(0);
    for (let i = 0; i < n; i++) {
      const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
      const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
      const wz = w * z;
      const active = supports[i]!;
      switch (active.length) {
        case 5: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          const a4 = active[4]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
          xtwz[a4] = xtwz[a4]! + wz;
          const r4 = xtwx[a4]!;
          r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
          break;
        }
        case 4: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
          break;
        }
        case 3: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
          break;
        }
        case 2: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
          break;
        }
        default: {
          for (let ai = 0; ai < active.length; ai++) {
            const a = active[ai]!;
            xtwz[a] = xtwz[a]! + wz;
            const rowA = xtwx[a]!;
            for (let bi = 0; bi < active.length; bi++) {
              const b = active[bi]!;
              rowA[b] = rowA[b]! + w;
            }
          }
        }
      }
    }
    for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
    const next = solveSymmetric(xtwx, xtwz);
    if (next === null) return { coefficients: null };
    const delta = next.map((value, index) => value - beta[index]!);
    const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
    beta = next;
    if (!beta.every(Number.isFinite)) return { coefficients: null };
    if (l2 < TOL) break;
  }
  return { coefficients: beta };
}
function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}
function onProbabilitiesFor(
  vectors: readonly number[][],
  coefficients: readonly number[]
): number[] {
  return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
}
function averagePredictiveComparison(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  coefficients: readonly number[],
  onProbabilities: readonly number[],
  column: string
): number {
  let sum = 0;
  const columnIdx = design.columnIndex.get(column);
  if (columnIdx !== undefined && columnIdx !== 0) {
    for (let i = 0; i < rows.length; i++) {
      if (vectors[i]![columnIdx] === 0) continue;
      const on = onProbabilities[i]!;
      const offVector = vectors[i]!.slice();
      offVector[columnIdx] = 0;
      const off = sigmoid(dot(coefficients, offVector));
      sum += on - off;
    }
  }
  return rows.length === 0 ? 0 : sum / rows.length;
}
function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}
type OfflineRowInput = {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
  readonly occurredAtMs: number;
};
function fitLogitAdditiveReplica(
  rows: readonly OfflineRowInput[],
  options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
): AttributionReport {
  const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
  const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
  const seed = options?.seed ?? SEED_DEFAULT;
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const effects: AttributionEffect[] = [];
  if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
  }
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irls(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
  if (fit.coefficients === null) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
  }
  const onProbabilities = onProbabilitiesFor(vectors, fit.coefficients);
  const pointEffects = new Map<string, number>();
  for (const name of design.names) {
    if (name === "intercept") continue;
    pointEffects.set(
      name,
      averagePredictiveComparison(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
    );
  }
  for (const ref of design.referenceLevels) {
    if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
      pointEffects.set(`${ref.factor}:${ref.name}`, 0);
    }
  }
  const random = rng(seed);
  const draws = new Map<string, number[]>();
  let successful = 0;
  for (let draw = 0; draw < bootstrapDraws; draw++) {
    const sample: Row[] = [];
    const sampleVectors: number[][] = [];
    const sampleSupports: number[][] = [];
    const sampleKeys: number[] = [];
    for (let i = 0; i < baseRows.length; i++) {
      const index = Math.floor(random() * baseRows.length);
      sample.push(baseRows[index]!);
      sampleVectors.push(vectors[index]!);
      sampleSupports.push(supports[index]!);
      sampleKeys.push(keys[index]!);
    }
    if (sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1)) continue;
    const bootFit = irls(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
    if (bootFit.coefficients === null) continue;
    successful += 1;
    const sampleOnProbabilities = onProbabilitiesFor(sampleVectors, bootFit.coefficients);
    for (const [name] of pointEffects.entries()) {
      const value = averagePredictiveComparison(
        design,
        sample,
        sampleVectors,
        bootFit.coefficients,
        sampleOnProbabilities,
        name
      );
      pushValue(draws, name, value);
    }
  }
  for (const [name, point] of pointEffects.entries()) {
    const values = draws.get(name) ?? [];
    if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
    }
    values.sort((a, b) => a - b);
    effects.push({
      name,
      point,
      lcb: percentile(values, 0.025),
      ucb: percentile(values, 0.975)
    });
  }
  const n = baseRows.length;
  const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
  const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
  const muLcb = betaQuantileLcb(muPosterior, 0.05);
  const models = new Set(baseRows.map((r) => r.modelVersion)).size;
  const projects = new Set(baseRows.map((r) => r.projectId)).size;
  const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
  const containsZero = (e: AttributionEffect): boolean =>
    e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
  const interactionsFor = (prefix: string): AttributionEffect[] =>
    effects.filter((e) => e.name.startsWith(`w:${prefix}`));
  let diagnosis: AttributionLabel = "uncertain";
  const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
  if (
    effects.some(
      (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
    )
  ) {
    diagnosis = "model-problem";
  } else if (
    effects.some(
      (e) =>
        e.name.startsWith("v:") &&
        e.lcb < -ATTRIBUTION_EFFECT &&
        effects
          .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
          .every(containsZero)
    )
  ) {
    diagnosis = "project-problem";
  } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
    diagnosis = "interaction-only";
  } else if (scenarioHard) {
    diagnosis = "scenario-hard";
  }
  return {
    estimator: "logit-additive",
    rowsUsed: baseRows.length,
    effects,
    diagnosis,
    reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
    writesActivePointer: false
  };
}
function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}
function pushValue(map: Map<string, number[]>, key: string, value: number): void {
  const list = map.get(key);
  if (list === undefined) map.set(key, [value]);
  else list.push(value);
}
function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}
function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRowInput[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRowInput[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
function perfRows(): OfflineRowInput[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
}
// Same small-shape fixture as R10-C..R28-C (seed 0x51a1, 3 x 3 x 3 / 120).
function smallRows(): OfflineRowInput[] {
  const r = fixtureRng(0x51a1);
  return randomRows(r, { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
}
const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

/* ------------------------------ comparisons ------------------------------ */

function compareReports(label: string, a: AttributionReport, b: AttributionReport): void {
  assertSame(`${label}.estimator`, a.estimator, b.estimator);
  assertSame(`${label}.rowsUsed`, a.rowsUsed, b.rowsUsed);
  assertSame(`${label}.diagnosis`, a.diagnosis, b.diagnosis);
  assertSame(`${label}.reason`, a.reason, b.reason);
  assertSame(`${label}.writesActivePointer`, a.writesActivePointer, b.writesActivePointer);
  assertSame(`${label}.effects.length`, a.effects.length, b.effects.length);
  for (let i = 0; i < a.effects.length; i++) {
    const ea = a.effects[i]!;
    const eb = b.effects[i]!;
    assertSame(`${label}.effects[${i}].name`, ea.name, eb.name);
    assertSame(`${label}.effects[${i}].point`, ea.point, eb.point);
    assertSame(`${label}.effects[${i}].lcb`, ea.lcb, eb.lcb);
    assertSame(`${label}.effects[${i}].ucb`, ea.ucb, eb.ucb);
  }
}

function batteryCases(): Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];
  for (let i = 0; i < 24; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }) });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 60, passRate: 0.55, pipeInModel: true }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  return cases;
}

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------------------------------- main --------------------------------- */

function main(): void {
  // 1. Replica fidelity battery (replica lane vs production import, bitwise).
  const cases = batteryCases();
  for (let i = 0; i < cases.length; i++) {
    const c = cases[i]!;
    compareReports(`battery[${i}]`, fitLogitAdditive(c.rows, c.options), fitLogitAdditiveReplica(c.rows, c.options));
  }
  compareReports("perf-fixture", fitLogitAdditive(perfRows(), PERF_OPTIONS), fitLogitAdditiveReplica(perfRows(), PERF_OPTIONS));
  compareReports("small-fixture", fitLogitAdditive(smallRows(), PERF_OPTIONS), fitLogitAdditiveReplica(smallRows(), PERF_OPTIONS));
  console.log(`scenario 1 (replica-lane fidelity battery, ${cases.length} cases + perf + small, bitwise vs production import): OK`);

  // 2. Production self-determinism: 3 seeds x 2 runs.
  for (const seed of [SEED_DEFAULT, 7_777, 424_242]) {
    const rows = perfRows();
    compareReports(`determinism.seed=${seed}`, fitLogitAdditive(rows, { bootstrap: 200, seed }), fitLogitAdditive(rows, { bootstrap: 200, seed }));
  }
  console.log("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is): OK");

  // 3. Production median re-anchor (perf 7 reps, small 3 reps).
  const prodPerf: number[] = [];
  {
    const rows = perfRows();
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      prodPerf.push(performance.now() - t0);
    }
  }
  const prodSmall: number[] = [];
  {
    const rows = smallRows();
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      prodSmall.push(performance.now() - t0);
    }
  }
  console.log(`  shape small (3 models x 3 projects, 120 rows): production median ${median(prodSmall).toFixed(1)} ms/report (3 reps)`);
  console.log(`  shape perf (6 models x 8 projects, 400 rows): production median ${median(prodPerf).toFixed(1)} ms/report (7 reps)`);
  console.log(`  perf reps: ${prodPerf.map((v) => v.toFixed(1)).join(" ")}`);

  // 4. APCX / FLOORX ceiling re-measure (dispatch-mandated).
  const rows = perfRows();
  const baseRows: Row[] = rows.map((r) => ({ scenarioId: r.scenarioId, modelVersion: r.modelVersion, projectId: r.projectId, y: r.y }));
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irls(design, baseRows, vectors, supports, keys, baseRows.length, MAX_ITER_DEFAULT);
  assertTrue("apcx.base-fit-converged", fit.coefficients !== null);
  const coefficients = fit.coefficients!;
  const onProbabilities = onProbabilitiesFor(vectors, coefficients);
  const sweepNames = design.names.filter((name) => name !== "intercept");

  // FLOORX lane bitwise-legality proof, per name (S8-C-2 form; X2-1 lemma
  // premises hold: 0/1 entries, +0.0 start, finite beta; ascending support
  // order = the full dot's nonzero visit order). Outside the sink accumulator.
  for (const name of sweepNames) {
    let sum = 0;
    const columnIdx = design.columnIndex.get(name);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < baseRows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        const active = supports[i]!;
        let value = 0;
        for (let ai = 0; ai < active.length; ai++) {
          const j = active[ai]!;
          if (j === columnIdx) continue;
          value += coefficients[j]!;
        }
        sum += onProbabilities[i]! - sigmoid(value);
      }
    }
    const floorValue = baseRows.length === 0 ? 0 : sum / baseRows.length;
    assertSame(
      `floorx.bitwise[${name}]`,
      averagePredictiveComparison(design, baseRows, vectors, coefficients, onProbabilities, name),
      floorValue
    );
  }
  // Verbatim R14-C-lineage protocol: 5 warmup sweeps + 60 timed sweeps per
  // lane, sink accumulated across all 65 (expected cross-round print 7.309).
  const names = sweepNames;
  let apcSink = 0;
  const sweepReps = 60;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tSweep0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perSweepMs = (performance.now() - tSweep0) / sweepReps;
  console.log(
    `probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) ` +
      `${perSweepMs.toFixed(3)} ms/fit -> x201 fits = ${(perSweepMs * 201).toFixed(1)} ms/report ` +
      `(base-shape approximation; sink=${apcSink.toFixed(3)})`
  );

  let floorSink = 0;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tFloor0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perFloorMs = (performance.now() - tFloor0) / sweepReps;
  console.log(
    `probe FLOORX: cheapest bitwise-legal sweep ${perFloorMs.toFixed(3)} ms/fit -> x201 fits = ` +
      `${(perFloorMs * 201).toFixed(1)} ms/report floor; APC combination ceiling on THIS VM = ` +
      `${(perSweepMs * 201 - perFloorMs * 201).toFixed(1)} ms (floorSink=${floorSink.toFixed(3)})`
  );
  assertSame("apcx.sink===floorSink", apcSink, floorSink);
  assertTrue("ceiling<35", perSweepMs * 201 - perFloorMs * 201 < 35);

  console.log("");
  console.log(`ALL EQUIVALENCE CHECKS PASSED (${checks} bitwise checks)`);
}

main();
```

### 共驻轴 runner `/tmp/r29c/r29c-coresidency-runner.mjs`

```js
/**
 * R29-C dual-estimator co-residency axis runner (process workload-composition
 * face, C-slice instantiation). Plain-JS on purpose: THIS FILE IS
 * BYTE-IDENTICAL ACROSS ALL ARMS. The arm is selected entirely by R29C_ARM;
 * every arm runs on the single canonical measurement substrate
 * (tsx + /workspace/src, the R28-C S arm — the substrate coordinate stays
 * frozen). NOT committed; full source goes into the R29-C report appendix.
 *
 * The production execution form of this slice is the dual-attribution pair
 * (`writeAttributionPair` in src/experiments/attribution-report.ts: one
 * process runs fitLogitAdditive AND fitProbabilityAdditive, X0-11 mandates
 * both lanes stay). Every historical C anchor (R1-C..R28-C) timed
 * fitLogitAdditive in processes where fitProbabilityAdditive never executed.
 * This runner doses exactly that coordinate — WHICH sibling estimator
 * executes in-process and WHEN — with all eight input dimensions, the
 * substrate, the engine, and the module graph frozen (both modules are
 * imported in every arm; only calls differ).
 *
 * Arms (dose points of the co-residency coordinate):
 *   L    : logit only — the historical canonical measurement form
 *          (7-rep perf median protocol, no prob-add call ever).
 *   PAIR : production interleaving — each timed logit rep is followed by
 *          fitProbabilityAdditive over the 4 per-scenario groups of the
 *          same frozen perf fixture (production-legal single-scenario
 *          inputs; prob-add legs timed separately, outside the logit timer).
 *   PRE  : foreign-feedback-first — 25 sweeps x 4 groups of prob-add run
 *          BEFORE the first logit call (shared posterior machinery tiers up
 *          on prob-add's call profile first), then the L protocol.
 *
 * Emits one JSON line: hot-tier medians (perf 7 reps, small 3 reps), all
 * reps, prob-add leg costs, and two SHA-256 digests over IEEE-754 bit
 * patterns + strings: logitDigest (perf + small + 3 battery shapes + a
 * determinism re-run — identical payload and fold as the R28-C substrate
 * runner, so equality across arms AND against R28-C's printed digest is a
 * single string comparison) and probDigest (the 4 per-scenario prob-add
 * reports, computed twice and asserted equal in-process; null in arm L).
 */
import { createHash } from "node:crypto";
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.ts";
import { fitProbabilityAdditive } from "/workspace/src/routing/offline-prob-add.ts";

const arm = process.env.R29C_ARM ?? "?";
if (arm !== "L" && arm !== "PAIR" && arm !== "PRE") {
  console.error(`R29C_ARM must be L | PAIR | PRE, got ${arm}`);
  process.exit(1);
}

function fixtureRng(seed) {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick(r, items) {
  return items[Math.floor(r() * items.length)];
}
function randomRows(r, options) {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) => `model-${i}`);
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
const perfRows = () => randomRows(fixtureRng(0xbeef), { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
const smallRows = () => randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
const PERF_OPTIONS = { bootstrap: 200, seed: 20260818 };

/**
 * Production-legal prob-add inputs derived from the SAME frozen perf fixture:
 * fitProbabilityAdditive requires exactly one scenario, so the production
 * pair caller hands it single-scenario row groups. Filtering preserves row
 * order and identity; zero input-dimension movement (the timed logit object
 * stays the whole frozen fixture in every arm).
 */
function perScenarioGroups(rows) {
  const order = [];
  const byScenario = new Map();
  for (const row of rows) {
    let group = byScenario.get(row.scenarioId);
    if (group === undefined) {
      group = [];
      byScenario.set(row.scenarioId, group);
      order.push(row.scenarioId);
    }
    group.push(row);
  }
  return order.map((id) => byScenario.get(id));
}

/** Canonical walk: numbers as IEEE-754 bit-pattern hex, strings/bools/null tagged. */
function foldValue(hash, value) {
  if (typeof value === "number") {
    const buf = new ArrayBuffer(8);
    new Float64Array(buf)[0] = value;
    hash.update("n:");
    hash.update(Buffer.from(buf));
  } else if (typeof value === "string") {
    hash.update(`s:${value.length}:${value};`);
  } else if (typeof value === "boolean") {
    hash.update(`b:${value};`);
  } else if (value === null) {
    hash.update("null;");
  } else if (value === undefined) {
    hash.update("undef;");
  } else if (Array.isArray(value)) {
    hash.update(`a:${value.length}[`);
    for (const item of value) foldValue(hash, item);
    hash.update("]");
  } else {
    const keys = Object.keys(value);
    hash.update(`o:${keys.length}{`);
    for (const key of keys) {
      hash.update(`k:${key}=`);
      foldValue(hash, value[key]);
    }
    hash.update("}");
  }
}
function digestReports(reports) {
  const hash = createHash("sha256");
  for (const report of reports) foldValue(hash, report);
  return hash.digest("hex");
}
function median(values) {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)];
}

const groups = perScenarioGroups(perfRows());

// PRE arm only: foreign-feedback-first — the shared posterior machinery
// (betaQuantileLcb -> betacf / lnGamma in posterior.ts, imported by BOTH
// estimators) receives prob-add's full call profile before logit ever runs.
let preWarmMs = null;
if (arm === "PRE") {
  const t0 = performance.now();
  for (let sweep = 0; sweep < 25; sweep++) {
    for (const group of groups) fitProbabilityAdditive(group);
  }
  preWarmMs = performance.now() - t0;
}

// Hot-tier timing: perf shape 7 reps, small shape 3 reps — identical rep
// protocol in every arm; PAIR interleaves the production sibling calls
// BETWEEN timed logit reps (outside the logit timer).
const perfReps = [];
const probLegTotals = [];
{
  const rows = perfRows();
  for (let rep = 0; rep < 7; rep++) {
    const t0 = performance.now();
    fitLogitAdditive(rows, PERF_OPTIONS);
    perfReps.push(performance.now() - t0);
    if (arm === "PAIR") {
      const p0 = performance.now();
      for (const group of groups) fitProbabilityAdditive(group);
      probLegTotals.push(performance.now() - p0);
    }
  }
}
const smallReps = [];
{
  const rows = smallRows();
  for (let rep = 0; rep < 3; rep++) {
    const t0 = performance.now();
    fitLogitAdditive(rows, PERF_OPTIONS);
    smallReps.push(performance.now() - t0);
  }
}

// Bit-face payload, byte-identical fold and report list as the R28-C
// substrate runner: equality across the 9 processes (and against R28-C's
// printed digest) certifies co-residency moves no logit output bit.
const r = fixtureRng(0x5eed);
const batteryShapes = [
  { rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 60, passRate: 0.5 }), options: { bootstrap: 40, seed: 11 } },
  { rows: randomRows(r, { scenarios: 3, models: 4, projects: 4, rows: 150, passRate: 0.55 }), options: { bootstrap: 60, seed: 77 } },
  { rows: randomRows(r, { scenarios: 1, models: 2, projects: 3, rows: 45, passRate: 0.65 }), options: { bootstrap: 30, seed: 5 } },
];
const reports = [
  fitLogitAdditive(perfRows(), PERF_OPTIONS),
  fitLogitAdditive(smallRows(), PERF_OPTIONS),
  ...batteryShapes.map((c) => fitLogitAdditive(c.rows, c.options)),
  fitLogitAdditive(perfRows(), PERF_OPTIONS),
];
const logitDigest = digestReports(reports);

// Prob-add bit face (co-resident arms only — arm L must never execute the
// sibling): two clean passes over the groups, digests asserted equal
// in-process (determinism witness), plus a per-call price for the pair's
// prob-add leg.
let probDigest = null;
let probAddPerCallMs = null;
if (arm !== "L") {
  const pass1 = groups.map((group) => fitProbabilityAdditive(group));
  const pass2 = groups.map((group) => fitProbabilityAdditive(group));
  const d1 = digestReports(pass1);
  const d2 = digestReports(pass2);
  if (d1 !== d2) {
    console.error(`PROB-ADD DETERMINISM MISMATCH: ${d1} !== ${d2}`);
    process.exit(1);
  }
  probDigest = d1;
  const t0 = performance.now();
  const calls = 200;
  for (let i = 0; i < calls; i++) {
    fitProbabilityAdditive(groups[i % groups.length]);
  }
  probAddPerCallMs = (performance.now() - t0) / calls;
}

console.log(JSON.stringify({
  arm,
  node: process.version,
  perfMedianMs: Number(median(perfReps).toFixed(3)),
  smallMedianMs: Number(median(smallReps).toFixed(3)),
  perfReps: perfReps.map((v) => Number(v.toFixed(1))),
  smallReps: smallReps.map((v) => Number(v.toFixed(1))),
  groupSizes: groups.map((g) => g.length),
  preWarmMs: preWarmMs === null ? null : Number(preWarmMs.toFixed(1)),
  probLegPerRepMs: probLegTotals.length === 0 ? null : Number(median(probLegTotals).toFixed(3)),
  probAddPerCallMs: probAddPerCallMs === null ? null : Number(probAddPerCallMs.toFixed(4)),
  logitDigest,
  probDigest,
}));
```
