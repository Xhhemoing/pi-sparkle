# Round 29 分区（10 子代理，3-VM 分波）

模型一律 `claude-fable-5-thinking-xhigh`。基线：最新 `cursor/sota-persistent-opt-83a1`。

Round 1–28 已对各区做过二十八遍穷尽裁决（Round 28 已关闭，全部空枚举），并落地 S1-F / J1 / S1-C / S1-I / S2-C / S3-C / S4-C / S4-I / S5-C / S5-F / S5-I-1 / S6-C / S6-F-1 / S7-C / S7-F-1 / S7-F-2 / S7-I-1。本轮只接受排除表未覆盖、且理论+仿真达门槛的新更优解。禁止重开任何 X* / S1-* … S28-* 条目（含已合入的 S13-B-1 与 R12–R28 全部空枚举收口，含刚合入的 R27-A … R27-J / R28-A … R28-J / R29-A / R29-B / R29-C / R29-D / R29-E / R29-F / R29-G / R29-H / R29-I）。S7-C 已落地，不得另起平行实现。S7-F-1 不是 S6-F-5。S5-H-1 必须保留。禁止再编号 **FITQ** / **NAMESHAPE** / **SCALEX** / **XPROC** / **YMIX** / **SCHEDWIN** / **KFAN** / **NVG** / **SEEDX** / **TERMCLASS** / **SHAPEK** / **ORDX** / **CKMIX** / **CALLB** / **OCCX** / **JB** / **RUNLIM** / **VALCLASS** / **CG** / **AGEX** / **COTARG** / **PRICEX** / **RTL** / **DELINV** / **PROBX** / **RVL** / **SIDEC** / **PWL** / **ZREP** / **PAIRX**。空枚举轴「M=1 catalog collapse」「approval-authority face」「state serialization-boundary provenance face」「operand-residency census」「adaptation-state coordinate」「value-domain / string content-class census」「task-face optional-field presence topology」「identity-coordinate face」「plan-header identity-geometry face」「injection command-kind face」「consumer-stage string-representation census」「process-environment coordinate」「collation-locale face」「privacy / provider-policy constraint-leg face」「injection-seam default-arm face」「input storage-state face」「edge condition-kind face」「argv surface-form face」「consumer-stage operand-realm face」「scalar numeric value-class face」「batch-cardinality bottom + prelude intercept face」「policy-content tree-geometry face」「input access-path mechanics face」「join-policy face」「process output-channel disposition face」「execution-substrate face」「catalog-face row-arrangement (permutation) face」「input key-set geometry face」「C-slice numeric-kernel execution-substrate instantiation」「contents-store population face」「boundary call-protocol face」「proposal write-path termination-leg face」「decision-gate resolution face」「boundary argument-slot materialization face」「invocation-identity / path-referent spelling × cwd face」「durable-store zero-state provenance」「learned-policy container face」「input reference-identity / aliasing-topology face」「process workload-composition face」「wall-clock sampling topology / timestamp-provenance face」「registry-record string-list payload face」「boundary output-retention / superseded-snapshot liveness face」「M2 supervised-engine adjudication lattice」「coverage-relation incidence topology face」「input-file JSON serialization-form face」不铸 ID、不写入排除表，但禁止再编号或跨区移植。face 名「execution-substrate face」归 R27-H；R28-C 只是该面在 C 区数值内核上的一格剂量，勿另起绰号。**PAIRX** 是 process workload-composition face 的 C 区双估计器共驻实例化观察绰号，不写入排除表；编排位点在 F 区 `attribution-report.ts`，F 勿把共驻本身当新 ID。boundary call-protocol face 与 R22-H 调用上下文 / **CALLB** 不同面。**PWL** 是 proposal write-path termination-leg face 的观察绰号，不写入排除表。**ZREP** 是 durable-store zero-state provenance 的观察绰号，不写入排除表。

R7-I 的教训：默认态夹具会遮蔽配置态主路径。本轮须按「配置态 × 命令类」矩阵复核测量盲区。多个切片已有整片预算收口——先复核再找新角度，不要硬凑。若独占分支因兄弟切片合入而快进，rebase docs-only 并把新轴加入不移植清单。

分区与 Round 1 相同（R29-A … R29-J），报告写入 `docs/reports/sota-opt/round-29/`。

状态：第 1 波 A–I 已合入、J 运行中。Round 29 未关轮（仅 J 仍在飞）。Round 30 已开卷（A 运行中、B 本波派出）。

A 切片已合入：空枚举，未铸 S29-A-*。切片 `git diff 7acb666..HEAD` 为空。预算复核 12.2–15.7 µs/gate ⇒ ~61–80 µs/run（与 R28-A 带重叠）。本轮新增 input reference-identity / aliasing-topology face（同值子项共享引用 vs 新分配；面平坦，同号差为分配布局伪迹或整门内抵消，其余 ±35 ns）。R28-A input key-set geometry face 与 R27-A scalar numeric value-class face / R26-A collation-locale face / R25-A value-domain / string content-class census 不补铸。R29-B learned-policy container face / R28-J **ZREP** 不要移植。与 R25-D identity-coordinate face 不同面。基线 `7acb666` 空 diff 再确认。

B 切片已合入：空枚举，未铸 S29-B-*。切片 `git diff 94ed3d9..HEAD` 为空。天花板复核 M=2 8.70–9.30 / M=10 17.99–18.73 ms/eval（历史带重叠）。S12-B-2 / S13-B-1 重开条件未触发。本轮新增 learned-policy container face（learned 容器的尺寸轴底部 × 行排列 × 写手形状普查；empty−none 截距 +205~+622 µs/call 复确认 S6-B-4 不重开；prefer 行序决策承载，预索引是行为变更；avoid 序 / assignments / 幽灵剂量逐字节惰性）。R28-B catalog-face row-arrangement (permutation) face 与 R27-B batch-cardinality bottom + prelude intercept face / R26-B privacy / provider-policy constraint-leg face / R25-B task-face optional-field presence topology / R24-B M=1 catalog collapse 不补铸。R28-J **ZREP** / R29-A input reference-identity / aliasing-topology face 不要移植。基线 `94ed3d9` 空 diff 再确认。

C 切片已合入：空枚举，未铸 S29-C-*。切片 `git diff 183df9b..HEAD` 为空。生产中位复核 657.1–659.6 ms（与 R28-C 带重叠）。APC ceiling 18.3–27.2 < 35；sink=7.309 逐位。r1c–r7c 8028 / 14420 / 14730 / 24888 / 28555 / 25483 / 6193 全绿。本轮新增 process workload-composition face 的 C 区双估计器共驻实例化（**PAIRX**：logit+prob-add 同进程共驻 vs 历史独唱；digest 9/9 全等，热层 PAIR−L +0.3–4.9 ms 低于 ±35；编排在 F 区 attribution-report.ts）。**PROBX** / **PRICEX** / **AGEX** / **FITQ** 不补铸。R28-C C-slice numeric-kernel execution-substrate instantiation（face 名归 R27-H）不要移植。基线 `183df9b` 空 diff 再确认。

D 切片已合入：空枚举，未铸 S29-D-*。切片 `git diff 82bef36..HEAD` 为空。eval 地板复核 S0c 3.45–3.67 ms / 同进程锚 3.20–3.33 ms（历史带重叠）。S9-D-4 / S12-D-1 未重开。本轮新增 registry-record string-list payload face（六处算子字符串列表槽的基数 n × 项宽 w × 分区 n×w；校验 ~6.5 ns/item，越线需幻想规模）。R28-D contents-store population face 与 R27-D policy-content tree-geometry face / R26-D injection-seam default-arm face / R25-D identity-coordinate face / R24-D approval-authority face 不补铸。R29-E wall-clock sampling topology / timestamp-provenance face / R29-C **PAIRX** 不要移植。基线 `82bef36` 空 diff 再确认。

E 切片已合入：空枚举，未铸 S29-E-*。切片 `git diff adb20d7..HEAD` 为空。SLICE-CPU 复核 14.7–15.2 µs/run（cmp25 9/9 落在历史带）。S8-E-1 / S9-E-2 / S13-B-1 未重开。本轮新增 wall-clock sampling topology / timestamp-provenance face（一次采集读钟次数 × 三个 `nowIso()` 站点 × 记录共享哪次采样；~430 ns/call ⇒ 0.43–4.75 µs/run，消除形态撞 S4-E-1 / S3-J-5 / X1-1）。**PWL** / **RVL** / **RTL** / **CG** / **JB** / **NVG** 不补铸。R29-C **PAIRX** 不要移植。基线 `adb20d7` 空 diff 再确认。

F 切片已合入：空枚举，未铸 S29-F-*。切片 `git diff 519101f..HEAD` 为空。锚点复核 r5f 120.28/121.96、r6f 121.05、r7f 131.29/126.76（历史带重叠）。S7-F-1 ≠ S6-F-5。本轮新增 boundary output-retention / superseded-snapshot liveness face（边界返回后继后调用方把被替换不可变 ShadowState 保持可达多久；仅无界全保留有实税 +6.9–10.5 ms，税在调用方引用图与收集器，生产已坐最便宜角）。R28-F boundary call-protocol face 与 R27-F input access-path mechanics face / R26-F input storage-state face 不补铸。R29-C **PAIRX** 不要移植。基线 `519101f` 空 diff 再确认。

G 切片已合入：空枚举，未铸 S29-G-*。切片 `git diff 4efee23..HEAD` 为空。计算顶复核 0.290–0.298 vs I/O 93.7–97.5 ms（~323–327×）。digest `06cbcf92c098c8f0` 第二十次逐位相同，勿补铸 digest。本轮新增 M2 supervised-engine adjudication lattice（`supervisor.ts` 轮询对 judge 裁决源 × child 结局 × attempt-vs-stall 预算竞态 + orphan-lease 恢复；结构 digest `435d184d67107eb1` 12/12 全等；生产剂量为零，奇点属行为变更非优化）。R28-G decision-gate resolution face 与 R27-G join-policy face 不补铸。R29-F boundary output-retention / superseded-snapshot liveness face 不要移植。基线 `4efee23` 空 diff 再确认。

H 切片已合入：空枚举，未铸 S29-H-*。切片 `git diff fd437a9..HEAD` 为空。热层复核 8889–9147 ns（run1 落 R28-H 带，run2/3 低 1.3% 属 extract 窗口）。S5-H-1 六次见证保留。本轮新增 coverage-relation incidence topology face（任务声明验收标准列表 × 合同标准集的二分匹配几何 + resolvedQuestionIds × questions；生产稀疏形比夹具更便宜，≤0.000092 ms/run）。R28-H boundary argument-slot materialization face 与 R27-H execution-substrate face 不补铸。R29-G M2 supervised-engine adjudication lattice 不要移植。基线 `fd437a9` 空 diff 再确认。

I 切片已合入：空枚举，未铸 S29-I-*。切片 `git diff 8dee7fb..HEAD` 为空（连续第二十二轮）。custom−builtin 复核 children +44.0~+50.5 / +22.6~+25.7、track +44.0~+53.2 / +22.5~+25.6、flowchart-flat64 +41.9~+42.8 / +18.5~+21.7 ms（与 R28-I 带相交）。S8-I-1 两臂文件级 blocked。本轮新增 input-file JSON serialization-form face（同一 JSON 值在 providers.json / --children / --flowchart / --results 内的字节拼写：空白形 × unicode-escape × 对象键序；最坏承重命令全脸 ~+0.11 ms once-per-command）。R28-I invocation-identity / path-referent spelling × cwd face 与 R27-I process output-channel disposition face / R26-I argv surface-form face / **NAMESHAPE** 不补铸。基线 `8dee7fb` 空 diff 再确认。

J 切片 = 29 文件。J1 / S5-J-3 / S6-J-1 / S8-J-2 钉死。禁止去 fsync。R28-J **ZREP** 与 R27-J **SIDEC** / R26-J **DELINV** / R25-J **COTARG** / R24-J **VALCLASS** / R23-J **CALLB** / R22-J **SHAPEK** / R21-J **KFAN** 不补铸。R28-H boundary argument-slot materialization face / R28-G decision-gate resolution face / R28-E **PWL** / R27-I process output-channel disposition face / R27-G join-policy face / R29-G M2 supervised-engine adjudication lattice / R29-F boundary output-retention / superseded-snapshot liveness face / R29-H coverage-relation incidence topology face / R29-I input-file JSON serialization-form face 不要移植。file-lock retryMs 已由 D/E 定价，勿作为 J ID 补铸。基线 `fb41417` 预期空 diff。运行中。若落地：重跑 J1（2468）+ r29j。I/O 地板 R28-J：preferences 124.4–218.3 / 402.4–516.2 µs；jsonl 60.3–68.7 / 250.0–354.7 µs。
