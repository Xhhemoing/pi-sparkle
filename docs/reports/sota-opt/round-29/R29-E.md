MODEL_SLUG=claude-fable-5-thinking-xhigh

# R29-E：`src/learning/` 第二十九遍复查报告（Round 29）

**战役:** 全库持久 SOTA 优化 Round 29 / R29-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `a22efff`（开工基线 `a23deea`（R29-A 摄取 + 本派单）；运行中独占分支一次 docs-only 快进：`a23deea` → `a22efff`（R29-C「process workload-composition face / **PAIRX**」空枚举摄取 + R29-F 派出），增量复核 docs-only（`src/` 零行），本分支 rebase 跟进并重验冻结（0 字节）、其轴按 §6 补入不移植清单；R29-D 在飞未触碰、未等待）
**分支:** `cursor/r29-e-learning-twenty-ninth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第二十九组新角度枚举为空
（有理由的空枚举，未铸造任何 S29-E-\* ID，与 R10-E..R28-E 同口径），
切片维持关闭（MORE_OPTIMA=no）。本轮补齐一条 R1-E..R28-E 从未拉伸过
的完整性轴——「壁钟采样拓扑 / 时间戳来源面：一次收集运行在哪些切片内
位点读壁钟（`nowIso()` = `new Date().toISOString()`）、读几次、哪些
产出/持久记录逐位共享哪一个采样，作为受控变量」。它以「时钟被读的
次数与位置」为受控变量——这是 29 轮里唯一从未受控过的坐标：R18-E
注意事项 (d) 与 R19-E 把 `nowIso()` 当作**须剥离的干扰变量**（跨调
等价断言剥 `createdAt` 字段）；S4-E-1 在已关闭的空事件快路径形态里
**顺带**给一次省下的采样定过价；S3-J-5（J 区）关闭过 per-view
nowIso 提升这一个**形态**——没有任何一轮把采样计数、采样位置、
逐通道共享拓扑当变量控过。杠杆非变量声明：事件构成钉死（R17-E 构成
面不剂量）；值表示按字符串**值**比较、从不比较引用（R29-A 引用同一性
面不剂量）；容器形状钉死（R29-B 不剂量）；store 状态全新临时根钉死
（R28-J ZREP 不剂量）；SPARKLE_AUTO_ADAPT 逐臂钉死（R25-I 坐标不
剂量）；pi 文件损坏态仅作到达/避开采样位点的杠杆，其 I/O 与错误腿
不重价（R17-E 异常面 / R26-F 存储态面不移植）。5 次套件运行 =
3 × v22.22.2 + 2 × v22.14.0，32/32 sanity ×5 全绿，全部确定性
VERDICT 行跨版本跨运行逐位一致：**

1. **三通道采样命运普查（确定性，逐腿钉死）**：events 通道
   （`collectSignalsFromEvents`，signals.ts:137）每次调用恰 1 采样，
   与 E∈{0,12,200} 无关，全部产出信号逐位共享该采样；采样点**先于**
   零发射早退（:137 在 :158 之前）——E=0 返回 `[]` 也照付 1 采样
   （该腿的免采形态 = 已关闭的 S4-E-1）。pi 通道
   （`collectSignalsFromSubagentRun`）守卫腿（非 record / 无
   projectId，:247）**零采样**；:252 的采样对每个成功解析的 record
   恰付一次（0 results 也付），该 record 的全部信号共享之。extra
   通道（`parseObservedSignal`）**零采样**——createdAt 与 episodeId
   均为调用方全权供给。
2. **每 run 采样清单定理（collect-only 腿，确定性）**：采样数 =
   1 + F_parsed（F_parsed = 成功解析的 .json 文件数；坏 JSON、空
   文件、非 record JSON、非 .json 名全部免采）；夹具 {2 valid, 1
   malformed, 1 empty, 1 non-record, 1 .txt} → 恰 3 采样。全 W1
   failing run 总采样 = 7/run（2/2 全新根重复逐位相同）：切片内 3
   （1 collect + 2 pi 文件），切片外 4（bandit 文件锁 acquiredAt +
   adaptation registry 版本戳 + registry 文件锁——R28-E PWL 已价
   腿的邻区属地）。W2 写路径**切片内零采样**（outcome 行携带事件时
   occurredAtMs——输入来源，非壁钟；N 腿零 I/O 零采样）。
3. **持久面直通见证（字节级）**：`FeedbackRecord.createdAt` =
   `signal.createdAt` 逐字（appendFeedback 不盖戳）——extra 通道
   调用方哨兵 `1999-01-02T03:04:05.000Z` 与 events 通道采样值均逐位
   落入 records.jsonl；且 extra 通道 createdAt 仅 typeof-string 校验
   （`isIsoTimestamp` 在 domain 存在但该边界不调用，非 ISO 串
   `totally-not-a-timestamp` 原样通过）——extra 通道的时钟完全由
   调用方控制（行为可见的验证面事实，仅记录，见 §2.3 (b)）。
4. **逐项定价（once-per-run 全谱，5 采样 = 3 × v22.22.2 + 2 ×
   v22.14.0）**：`nowIso()` 423.5–434.0 ns/call；整轴预算 =
   (1+F_parsed) × ~430ns：F=0 → 0.43 µs/run（23168× 低于门槛）、
   F=2 → 1.29 µs（7723×）、F=10 → 4.75 µs（2106×）、F=100（荒谬档）
   → 43.6 µs（229×）；每个被采样文件同时必付 ~10²µs 级 readFile
   I/O，时钟份额 ~0.1%。提升反事实（1+F 收拢为每 run 1 采样）省
   F × ~430ns（F=2 → 0.86 µs）且 per-file 戳坍缩为 run 戳 =
   行为可见发散（S3-J-5 同族）。ms 分辨率坍缩观察：10000 次背靠背
   采样仅产 6 个不同 ISO 串——真实 run 的多采样拓扑在可观察面上
   经常退化为单一值，但时钟仍被读 1+F 次。**不产生候选。**

**天花板裁决（频率 × 单价 × 属地三端封死，R20-E..R28-E 同式）**：
频率端——采样全部挂在 once-per-run 收集管线上（collect 每 run 一次、
pi 文件数 = 现实个位~十位），无每任务/每事件重入放大器；单价端——
整轴全谱 0.43–4.75 µs/run（现实档），即使整轴清零也距 ≥10 ms 门槛
3–4 个数量级；属地端——`nowIso` 实现体在 `src/domain/timestamp.ts`
（切片外，换更快格式化器 = 属地拒列），采样共享/提升诸形态 =
S4-E-1 / S3-J-5 / X1-1 已关闭条目的换名或行为可见发散，extra 通道
验证补强 = 功能立项（X0-2 邻域）。**不产生候选。**

其余与前二十八轮同构的复核全数成立：切片二十九遍零 diff
（`git diff adb20d7..HEAD -- src/learning` 为 **0 字节 0 提交**，
开工、快进 rebase 后、写报告前各验一次；10 文件 1770 行 `wc -l` 与
R14-E..R28-E 相同）；SLICE-CPU 本夹具（seeds `0xe29e01/02/05`，构成
collected=18 / outcomes=5 / diagnoseGroups=1）**22.2–23.9 µs/run**
（12 次运行 = 6 × v22.22.2 + 6 × v22.14.0，版本差异不可分辨，
418–450× 低于门槛；构成比 cmp25 重——outcomes=5 使 outcomes 段
4.8–5.2 vs 1.3–1.5 即主差源，随构成单调，无新机制）；**R25-E 种子
对调（cmp25，9 次 = 6 × v22.22.2 + 3 × v22.14.0）复现 collected=14 /
outcomes=1 / diagnoseGroups=2（VERDICT 行与 R25-E..R28-E 夹具行逐字
相同），总量 14.7–15.2 µs，9/9 落在默认带 14.7–15.2 内（本会话居
下缘区；R28-E 会话居上缘区 15.0–15.2，带内位置噪声），分段 collect
11.3–11.7 / outcomes 1.3–1.5 / bandit 0.8 全在 R25-E 带内，diagnose
1.32–1.36 有 2/9 采样落 1.32 = 低于带下缘 1.33 恰 0.01 µs（表显
粒度级单侧擦边，同批其余全数带内、零 diff 下 VERDICT 逐位一致——
按 R28-E load-fresh 同例只读作价签不改带）**——默认带沿用不变；
配置态 live 面锚点全数在历史带内（apply(avoid=10,M=10) 265–284
ns/task（257–312 带内 9/9）、空策略 112–124（110–140 带内）、load
现实 registry 77.0–89.4 µs（74.3–134.5 带内 9/9）、load fresh
(ENOENT) 25.8–33.8 µs（历史并集 25.2–36.8 带内 9/9——R28-E 会话的
5/9 高位噪声本会话未再现，佐证其位置噪声判读）；历史 nonempty 格
VERDICT `allowed=5 preferred=m5` 二十九轮不变）；`pnpm gate` 在
engines 合规 Node v22.22.2 全绿（1169 tests / 78 suites / 1168 pass /
0 fail / 1 skipped，计数与 R14-E..R28-E 逐一相同）；切片套件 48/48
双版本过。S8-E-1 / S9-E-2 / S13-B-1 未以任何形态触碰。R28-E PWL、
R27-E RVL、R26-E RTL、R25-E CG、R24-E JB、R22-E NVG 未复铸（本轮不
触 propose 腿身份、不动裁决格、不触 apply 终止腿、无 TASK_RETRY
链态、join 恒完整路由、无数值类剂量——W1 failing 剖面仅作到达采样
位点的杠杆）；R23-E GS 未复铸（diagnose 零时钟、仅作管线成员复核）；
R29-A input reference-identity / aliasing-topology face、R29-B
learned-policy container face、R29-C process workload-composition
face / **PAIRX**（快进补入）、R28-A input key-set geometry face、
R28-B catalog-face row-arrangement (permutation) face、R28-D
contents-store population face、R28-F boundary call-protocol face、
R28-H boundary argument-slot materialization face、R28-J **ZREP**
均未移植。不声称 Outcome-supported；Checkpoint F-PROD 仍开放
（ADR-005）。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 /
  `auto-loop` 283 / `bandit-store` 83 / `diagnostics` 78 /
  `from-episode` 298 / `learned-routing` 216 / `patterns` 146 /
  `signals` 439 / `signatures` 66 / `task-success` 69 = 1770 行，
  `wc -l` 与 R14-E..R28-E 逐一相同）。
- 生产最后落地 `adb20d7`；`git diff adb20d7..HEAD -- src/learning/`
  空（开工、快进 rebase 后、写报告前各验一次）。
- 独占分支运行中一次 docs-only 快进：`a23deea → a22efff`（R29-C
  「process workload-composition face / **PAIRX**」空枚举摄取 +
  R29-F 派出）；`git diff a23deea..a22efff` 复核增量仅
  `docs/reports/sota-opt/`（PROGRESS + PLAN + R29-C.md，`src/` 零
  行）——未按生产变更对待，按令 rebase 跟进（本分支当时尚无提交，
  rebase 即快进），10 文件冻结在 `a22efff` 下重验 0 字节，R29-C 轴
  加入不移植清单（其对象为 C 区双估计器同进程共驻实例化——本套件
  单进程单工作负载、构成钉死，无估计器共驻剂量，无交集）。
- 硬不变量全数未触碰：分析不变更在飞 run；双 LCB / 双归因均在；
  提案先行；无阈值改动；无 fsync / 完整性再哈希删除分析立项；
  S5-H-1 / S7-F-1≠S6-F-5 / S8-E-1 / S9-E-2 / S9-D-4 / S12-B-2 /
  S12-D-1 / S13-B-1 全部维持（本轮对 `loadLearnedRouting` 零去重
  分析，apply 本体零触碰——applyLearnedRouting 全程零时钟读，
  S13-B-1 重开条件未触发）。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / PLAN.md；未开 PR；未触兄弟
  分支；package-lock.json 未出现（pnpm-lock.yaml 未改，仅本机
  `pnpm install --frozen-lockfile` 补装依赖）。
- 阅读序合规：round-29/PLAN（E 段）→ EXCLUSIONS 全表 → R28-E 全文
  （另按令读 R29-A / R29-B 结论与轴段，快进后补读 R29-C 结论与轴
  段、PLAN 增量）。

## 1. 冻结证明与 SLICE-CPU 重锚

```bash
git log --oneline -1 origin/cursor/sota-persistent-opt-83a1
# a22efff docs(sota-opt): ingest R29-C (empty enum) and dispatch R29-F
git diff adb20d7..HEAD --stat -- src/learning/   # （空输出，0 文件 0 字节）
wc -l src/learning/*.ts                          # 1770 total（同 R14-E..R28-E）
```

锚点脚本 `/tmp/r29e-anchor.mts`（构造 = R10-E 附录 A 逐字同构 +
R29-E 置换，见附录 A）共 **12 次主运行**（6 × v22.22.2 + 6 ×
v22.14.0）+ **9 次 cmp25 对调运行**（R25-E 种子 `0xe25e01/02/08` +
R25-E NOW `17:30`，6 + 3），每次 3/3 sanity 全过，确定性 VERDICT 行
各自跨运行跨版本逐位一致。代表性一次主运行（v22.22.2）：

```text
VERDICT fixtures: E=41 collected=18 routedE=41 outcomes=5 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=15.7us outcomes=4.9us diagnose=1.03us bandit-build=0.8us | total in-slice CPU ~22.3us per run vs landing bar >=10000us (448x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=274ns/task | empty policy=117ns/task -> x10 tasks/run = 2.74us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=27.6us | realistic registry (baseline+1 candidate)=84.0us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

拒绝采样审计（`probe` 开关，k=1..12 三元组）：collect 角首选
`0xe29e01` 一次通过（collected=18>3）；routed 角首选 `0xe29e02`
一次通过（outcomes=5>0）；signals 角 `0xe29e03`（diagnoseGroups=0）
与 `0xe29e04`（0）拒绝，`0xe29e05`（diagnoseGroups=1）通过——两次
拒绝，探针三元组全表见附录 A。

cmp25 对调 9 次 VERDICT 行 9/9 逐位一致且与 R25-E..R28-E 夹具行
逐字相同（`E=41 collected=14 routedE=41 outcomes=1 S=12
diagnoseGroups=2 prevArms=3`）；总量 14.8 / 14.8 / 14.9 / 14.9 /
15.2 / 15.0 | 15.1 / 14.9 / 14.7 µs vs 默认带 14.7–15.2：**9/9 带内
（下缘区；R28-E 会话为上缘区 15.0–15.2——带内位置噪声，两会话并集
恰为整带）**，分段 collect 11.3–11.7 / outcomes 1.3–1.5 / bandit
0.8 vs R25-E 带 11.3–11.9 / 1.3–1.5 / 0.8 全数在带；diagnose
1.32–1.36 vs 带 1.33–1.46：**2/9 采样（v22.22.2 一次 / v22.14.0
一次）落 1.32，低于带下缘恰 0.01 µs**——表显粒度级单侧擦边（该段
表显仅两位小数，0.01 µs = 表显最小步长；同批其余 7/9 带内、collect
与 outcomes 全带内、零 diff 下夹具逐字节相同、VERDICT 逐位一致）
——非切片漂移，按 R28-E load-fresh 位置噪声同例只读作价签记录，
不改带、不立项。SLICE-CPU 重锚判定：**默认带沿用 14.7–15.2 µs/run
不变**，本轮主夹具 22.2–23.9 µs 为新构成点（outcomes=5 使 outcomes
段 4.8–5.2 vs cmp25 的 1.3–1.5，collected=18 使 collect 段
15.6–16.7——两段皆随构成单调，无新机制；R28-E 主夹具 collected=18
但 outcomes=1，collect 13.8–14.2——种子不同、事件混合不同，构成点
不可跨种子直比，跨轮可比面唯 cmp25 且 9/9 带内）。

配置态锚点（9 采样 = 6 × v22.22.2 + 3 × v22.14.0）：apply(avoid=10,
M=10) 265–284 ns/task（R13-E..R28-E 带 257–312 内 9/9）、空策略
112–124（110–140 带内 9/9）、load 现实 registry 77.0–89.4 µs
（R15-E..R28-E 带 74.3–134.5 内 9/9）、load fresh(ENOENT) 25.8–33.8
µs（历史并集 25.2–36.8 内 **9/9**——R28-E 会话 5/9 高出上缘的
位置噪声本会话未再现，回归佐证其判读）。

## 2. 新轴：壁钟采样拓扑 / 时间戳来源面

### 2.1 定义与盲区

受控变量：**一次收集运行读壁钟的次数与位置**（`nowIso()` =
`new Date().toISOString()`，src/domain/timestamp.ts:7-9），以及
**哪些产出/持久记录逐位共享哪一个采样**。切片内时钟读位点普查
（`rg "nowIso\(" src/learning/` 钉死）：恰 3 处——

- `signals.ts:137`：`collectSignalsFromEvents` 每次调用一采样，
  **先于**两遍扫描与零发射早退（:158 `if (projectId === undefined)
  return []`）；
- `signals.ts:252`：`collectSignalsFromSubagentRun` 每个成功解析的
  record 一采样，**后于**守卫腿（:247 非 record / 无 projectId
  早退零采样）；
- `signatures.ts:41`：`createSignature` 每调一采样——test-only 面
  （本遍复核仍零生产调用方，自动拒列）。

通道来源矩阵（channel × {createdAt 来源, episodeId 来源}）：
events 通道 {每调新采样, context}；pi 通道 {每文件新采样, context}；
extra 通道 {调用方供给, 调用方供给}；W2 outcome 行 {事件时
occurredAtMs（`Date.parse(event.occurredAt)`，输入来源），恒非壁钟}。

盲区证明（为什么 28 轮没人控过这条轴）：R18-E 注意事项 (d) 与
R19-E 的跨调等价断言把 `createdAt` 当**干扰变量剥离**（「`nowIso()`
壁钟字段跨调合法不同」）——剥离即声明不受控；S4-E-1 关闭的是空事件
快路径这一个**形态**（其收益构成里含一次省下的 nowIso + 5 个 Map
分配，534–542ns 整体拒收），不是采样拓扑轴；S3-J-5（J 区）关闭的是
rebuildViews per-view nowIso 提升这一个**形态**（lastUpdated 分布
发散 + ~3.8µs），其对象在 preferences store；R21-E 标识符几何控的
是 id 串的形状不是时钟；R16-E 载荷形态控的是记录形状（createdAt
字段**在场性**属之）不是其**值来源**。没有任何一轮枚举过采样位点
集合、测过每 run 采样清单（1 + F_parsed）、把「哪些记录共享哪个
采样」当作可断言拓扑钉死过，也没有任何一轮见证过 extra 通道时钟的
调用方全权控制与持久面直通。

### 2.2 与近邻轴的区分（换名自查核心）

| 近邻 | 其受控变量 | 本轴钉死之 | 区分 |
| --- | --- | --- | --- |
| R18-E (d) / R19-E | 表示面/次序面（nowIso 作干扰变量剥离） | 采样次数/位置/共享为变量 | 剥离 ≠ 受控；本轴给被剥离者建拓扑 |
| S4-E-1（已关闭形态） | 空事件快路径（含免 1 采样 + 5 Map） | 零发射腿采样事实仅引其为形态先例 | 形态裁决 ≠ 完整性轴；本轮不重提该形态 |
| S3-J-5（J 区形态） | rebuildViews per-view nowIso 提升 | 提升反事实引其为发散先例 | preferences store 属地 + 单形态；本轴在 E 收集面 |
| **PWL**（R28-E） | propose 终止腿 × 写激活集 | W2 写路径切片内零采样（C8）；切片外锁/registry 戳属其已价腿 | 写激活集 vs 时钟读集——本轮不触腿身份 |
| **RTL**（R26-E） | apply 终止腿（live 读侧） | applyLearnedRouting 全程零时钟 | 无交集 |
| **RVL**（R27-E） | TASK_RESULT 裁决格 | 夹具 verdict 恒 FAILURE/FAILED 固定剖面（仅作杠杆） | 裁决格不剂量 |
| **CG**（R25-E） | TASK_RETRY 链深 | 无 retry 事件 | 链态 vs 采样 |
| **JB**（R24-E） | join 命运 × 绑定完整度 | 路由绑定恒完整（仅作杠杆） | 绑定不剂量 |
| **NVG**（R22-E） | 数值值类 | 采样是 ISO 字符串值；ms 坍缩为采样毗邻推论非表示剂量 | 值类不剂量 |
| **GS**（R23-E） | diagnose 消费级守卫链 | diagnose 零时钟（管线成员复核） | 无交集 |
| R29-A input reference-identity / aliasing-topology face（兄弟） | 输入引用同一性/别名拓扑 | 共享断言全部按字符串**值**逐位比较（`===` 于 string 即值比较），从不作对象引用同一性剂量 | 值同一 vs 引用同一；未移植 |
| R29-B learned-policy container face（兄弟） | learned 容器尺寸轴/行排列/写手形状 | 无容器剂量（learned 面零触碰） | 未移植 |
| R29-C process workload-composition face / **PAIRX**（兄弟，快进补入） | C 区双估计器同进程共驻实例化 | 单进程单工作负载套件，构成钉死 | 无共驻剂量；未移植 |
| R28-J **ZREP**（兄弟轮） | 持久 store 零态来源 | store 态 = 全新临时根钉死（仅作杠杆） | 零态不剂量；未移植 |
| R26-F input storage-state face（兄弟轮） | 输入文件存储态 | 坏 JSON/空文件/非 record 仅作到达/避开采样位点的杠杆，其 I/O 与错误腿不重价 | 存储态是杠杆非变量；未移植 |
| R17-E | 异常面/构成面 | catch 腿仅作免采见证，事件构成钉死 | 不重价异常路径 |
| R25-I（I 切片） | 进程环境坐标 | SPARKLE_AUTO_ADAPT 逐臂钉死（0=collect-only census / 未设=W1 全程） | env 是杠杆不剂量 |
| R20-E | 并发面 | 全程单进程零竞争 | 并发 vs 采样 |
| R21-E | 标识符几何 | fbk_ id 哈希构成不剂量（其不含 createdAt 的事实按几何轴属地不重铸） | 几何 vs 时钟 |

### 2.3 采样命运普查（确定性，5/5 运行跨版本逐位一致）

套件 `/tmp/r29e-clock.mts`（全文见附录 A）SEGMENT C 共 32 检查：
计数器 = 包裹 `Date.prototype.toISOString`（发确定性 ISO 合法序列
`BASE_MS + n`，census 后恢复原方法——由此采样→记录归属可从输出值
单独证明）：

```text
VERDICT CLOCK C1 events-channel: 1 sample per collect invocation for E in {0,12,200}; sample taken BEFORE the zero-emission early return (signals.ts:137 precedes :158); all emitted signals share that one sample bit-identically
VERDICT CLOCK C2 pi-channel unit: guard legs (non-record / no projectId, signals.ts:247) are sample-free; the sample at :252 is paid once per successfully parsed record even when 0 signals emit, and is shared by all signals of that record
VERDICT CLOCK C3 per-run manifest (collect-only leg): samples = 1 + F_parsed (F_parsed = successfully parsed .json files; malformed JSON, empty files, non-record JSON and non-.json names are sample-free); fixture {2 valid, 1 malformed, 1 empty, 1 non-record, 1 .txt} -> exactly 3 samples
VERDICT CLOCK C4 extra-channel: parseObservedSignal never reads the clock; createdAt is caller-supplied and typeof-string validated only (isIsoTimestamp exists in src/domain/timestamp.ts but is not invoked at this boundary) — the extra channel's clock is fully caller-controlled
VERDICT CLOCK C5 durable pass-through: FeedbackRecord.createdAt = signal.createdAt verbatim (appendFeedback stamps nothing) — the caller-supplied extra-channel clock and the collect-invocation sample both reach records.jsonl byte-identically
VERDICT CLOCK C6 provenance matrix: events-channel {createdAt: fresh sample per invocation, episodeId: context} | pi-channel {createdAt: fresh sample per parsed file, episodeId: context} | extra-channel {createdAt: caller, episodeId: caller} | W2 outcome rows {occurredAtMs: event time, never wall-clock}
VERDICT CLOCK C7 signatures.ts:41 samples once per createSignature call — zero production callers (test-only face, census re-verified this pass), auto-rejected as a dosing site
VERDICT CLOCK C8 W2 write path: clock-free in-slice — outcome rows carry event-time occurredAtMs (input provenance), the propose legs read no wall clock (out-of-slice registry/lock stamps are neighbour territory, R28-E PWL priced those legs)
VERDICT CLOCK C9 full W1 failing run: total clock samples = 7 per run (2/2 fresh-root repeats identical); slice-attributed = 3 (1 collect + 2 parsed pi files, by C1-C3 topology); out-of-slice = 4 (bandit file-lock acquiredAt + adaptation registry version stamps + registry file-lock — R28-E PWL / neighbour territory)
```

跨职权观察（非候选、非 ID，仅记录；均为行为可见的语义/属地面）：

- **(a) 采样先于发射判定**：events 通道在零发射腿（E=0 / 无
  projectId）也付 1 采样——其免采形态即已关闭的 S4-E-1（该形态整体
  534–542ns 拒收在案）；pi 通道相反，守卫腿免采——两通道的
  采样-守卫次序不对称是实现事实，统一之 = 单形态重提（S4-E-1 换名）
  + ns 级，仅记录。
- **(b) extra 通道时钟的调用方全权**：`parseObservedSignal` 对
  createdAt 仅 typeof-string 校验，非 ISO 串原样通过并经
  `persistSignals` → `appendFeedback` 逐字落入 records.jsonl
  （C4+C5 见证）；`isIsoTimestamp` 在 domain 存在但该边界不调用。
  补验证 = 收紧公开输入契约的功能立项（X0-2 邻域 + 数据面契约），
  仅记录。extra 通道 episodeId 同为调用方供给（context 不盖戳）——
  与 events/pi 通道的 context 盖戳不对称同属通道契约。
- **(c) 每 run 可观察戳多样性的 ms 坍缩**：ISO 串 ms 分辨率使
  背靠背采样通常逐位相同（10000 采样 → 6 个不同串）——真实 run 的
  1+F 次读在可观察面上经常退化为单一戳值；「省读」与「省可观察
  多样性」因此是两个不同命题，后者多数 run 已自然为 1。
- **(d) 全 W1 采样清单的切片外份额**：7 采样中 4 个在切片外
  （bandit 文件锁 acquiredAt（src/persist/file-lock.ts:27）、
  adaptation registry 版本戳、registry 文件锁）——属地 =
  `src/persist/` 与 `src/adaptation/`（R28-E PWL 已对其宿主腿定价，
  R24-E「属地拒列」同裁），仅记录。

### 2.4 逐项定价（5 采样 = 3 × v22.22.2 + 2 × v22.14.0）

```text
nowIso():                         423.5–434.0 ns/call
整轴预算 (1+F_parsed) × ~430ns:
  F=0（无 pi 目录，生产常态）:     0.43 µs/run —— 23168× 低于门槛
  F=2:                            1.29 µs/run —— 7723×
  F=10:                           4.75 µs/run —— 2106×
  F=100（荒谬档）:                43.6 µs/run —— 229×
提升反事实（收拢为每 run 1 采样）: 省 F × ~430ns（F=2 → 0.86 µs）
  且 per-file 戳坍缩为 run 戳 = 行为可见发散（S3-J-5 同族）
ms 坍缩观察:                      10000 背靠背采样 → 6 个不同 ISO 串
```

结构判读：(i) 全部采样挂在 once-per-run 收集管线（§2.1 位点普查 +
C3/C9 清单定理）；(ii) 每个被采样 pi 文件同时必付 readFile + JSON
解析 ~10²µs 级 I/O，时钟份额 ~0.1%——即便文件数增两个数量级，先
越线的是 I/O 不是时钟；(iii) 切片内可控份额全谱 0.43–4.75 µs/run
（现实档），距 ≥10 ms 门槛 3–4 个数量级；(iv) 全部消除/共享形态
撞已关闭条目（S4-E-1 / S3-J-5 / X1-1）或行为可见发散或切片外属地。
两 Node 版本交叉不可分辨。

### 2.5 三重收口

- **频率端**：时钟读位点仅 3 处（普查钉死），全部挂 once-per-run
  收集管线；无每任务/每事件采样位点（TASK_RESULT/JUDGE/USER_ANSWER
  行共享调用级采样，C1 断言）；W2 写路径与 apply 读路径零采样。
- **单价端**：nowIso ~430ns/call；整轴现实档 0.43–4.75 µs/run；
  即使 F=100 荒谬档也仅 43.6 µs = 229× 低于门槛。
- **属地端**：`nowIso` 实现体 = `src/domain/timestamp.ts`（切片外，
  更快格式化器 = 属地拒列）；切片外采样（锁戳/registry 戳）=
  `src/persist/` + `src/adaptation/`（R28-E PWL 宿主腿已价）；
  采样共享/提升/缓存形态 = S4-E-1 / S3-J-5 / X1-1 已关闭条目换名
  或行为可见发散；extra 通道验证补强 = 功能立项。**不产生候选。**

### 2.6 第二十九组枚举表

方法：本轮代码侧透镜「**谁读时钟、谁共享戳**」：对第二十九遍全量
重读的 10 个源文件逐函数问 (a) 哪些位置的成本/行为随采样次数与
位置变动，(b) 变动处有无等价可削形态；同时按惯例对逐字新点名位置
做换名检查（对照 EXCLUSIONS 全表 + 二十八轮 E 报告 + 十八轮空枚举
面 + 三条 R29 兄弟轴）。扫描结果：**零个可立项形态**（均未铸新
ID）：

| 形态（本轮第二十九遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| pi 通道 per-file 采样提升为每 run 单采样（经 SignalContext 传共享 createdAt） | F × ~430ns（F=2 → 0.86 µs） | 行为可见发散：per-file 戳坍缩为 run 戳（S3-J-5 同族）+ 公开 SignalContext 形状扩张；µs 级 once-per-run | S3-J-5 / X0-4 邻域 |
| events 通道采样下沉到 projectId 守卫后（零发射腿免采） | ~430ns once-per-run | 换名重提拒列：S4-E-1 空事件快路径形态已裁（其构成即含此采样） | S4-E-1 |
| nowIso 结果按 ms 记忆化 / 模块级缓存 | (1+F−1) × ~430ns | X1-1 隐藏缓存域 + 陈旧戳风险；且 ms 坍缩使可观察收益趋零（§2.3 (c)） | X1-1 |
| `new Date().toISOString()` 换手写 ISO 格式化器 | ~数百 ns/采样 | 属地拒列：实现体在 src/domain/timestamp.ts（切片外）；µs 级 | R24-E 属地拒裁同式 |
| extra 通道 createdAt 补 isIsoTimestamp 验证 | —（非优化） | 功能立项：收紧公开输入契约 + 数据面语义变更（(b) 观察） | X0-2 邻域 |
| pi 守卫腿与 events 无守卫采样次序统一 | ns 级 | 单形态重提（S4-E-1 换名）+ 两通道契约有意分立 | S4-E-1 |
| 批量后置盖戳（信号先建后统一 stamp） | ns 级 | 行为等价性取决于共享拓扑不变——实现即现行（events/pi 通道已各自共享）；重排零收益 | — |
| signatures.ts createSignature 采样消除/参数化 | ~430ns/call | test-only 面零生产调用方，自动拒列（S1-E-7/8 同域） | S1-E-7/8 |
| 切片外锁戳/registry 戳去重 | µs 级 once-per-run | 属地拒列（src/persist/ + src/adaptation/）+ PWL 宿主腿已价 | R28-E PWL / R24-E |
| 漂移带来的新时钟位点 / 新采样放大器 | — | 不存在：位点普查 3 处与历轮一致；切片二十九遍零漂移；快进增量纯 docs | — |

**提议排除 ID：无（空枚举未铸任何 S29-E-\*，亦未向 EXCLUSIONS 新增
轴昵称行——壁钟采样拓扑 / 时间戳来源面为 observation-only 轴，按令
由父代 plan-ban）。**

## 3. 逐文件复核

| 文件 | 第二十九遍时钟透镜下的复核 | 结论 |
| --- | --- | --- |
| `signals.ts` | 3 处时钟位点中的 2 处宿主（:137 / :252）；C1/C2 采样-守卫次序不对称钉死；extra 通道 createdAt/episodeId 调用方全权（C4）；S9-E-2 / S1-E-1 未触 | 无候选 |
| `auto-loop.ts` | 收集编排的采样清单定理宿主（C3/C9：1 + F_parsed）；persistSignals 直通 appendFeedback 不盖戳（C5）；ingestSubagentDirectory 坏文件免采见证；S2-E-1 / X0-3 / S5-E-5 未触 | 无候选 |
| `from-episode.ts` | W2 写路径切片内零采样（C8）；outcome 行 occurredAtMs = 事件时输入来源恒非壁钟；PWL 腿身份未触 | 无候选 |
| `learned-routing.ts` | apply/load 全程零时钟读（RTL / S8-E-1 / S13-B-1 关闭维持，零触碰） | 无候选 |
| `bandit-store.ts` | 本体零时钟；其文件锁的 acquiredAt 戳在 src/persist/（切片外，C9 (d) 归属） | 无候选 |
| `diagnostics.ts` | 零时钟；仅作管线成员复核（GS 关闭不重价） | 无候选 |
| `task-success.ts` | 零时钟；V2 语义未触（RVL 关闭） | 无候选 |
| `signatures.ts` | 第 3 处时钟位点（:41，C7）；test-only 面零生产调用方自动拒列 | 无候选 |
| `patterns.ts` / `attribution.ts` | 零时钟；test-only 面复核仍零生产调用方；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核：

```bash
# 本 VM: shell 默认 node v22.14.0（/exec-daemon 前置于 PATH）；
# engines >=22.19.0 合规版本 v22.22.2 经 ~/.nvm 提供（gate 与
# v22.22.2 批次以该 PATH 前置调度）；v22.14.0 对照批次以
# /exec-daemon/node node_modules/tsx/dist/cli.mjs 调度。
# 本环境无预装依赖：pnpm install --frozen-lockfile（锁文件未改）。
pnpm gate
# typecheck ✓ + lint ✓ + test ✓ + build ✓ （GATE_EXIT=0）
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（计数与 R14-E..R28-E 逐一相同）

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.22.2 与 v22.14.0 均过）
```

锚点脚本 12 次主运行 + 9 次 cmp25 对调全过（§1）；时钟轴套件
（`/tmp/r29e-clock.mts`，全文见附录 A）共 **5 次运行**（3 ×
v22.22.2 + 2 × v22.14.0），每次 **32/32 sanity 全过**（采样计数
断言 ×9 组 + 共享拓扑值断言 + 持久直通字节断言 + 通道来源矩阵 +
全 W1 清单确定性断言），全部 VERDICT 行跨 5 次运行跨版本逐位一致；
定价段 5 采样区间见 §2.4 与附录 A。

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round29-r29e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R28-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件
（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 仅
`docs/reports/sota-opt/round-29/R29-E.md`）。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2.6 空枚举，逐形态有理由；第二十九遍以「谁读
  时钟、谁共享戳」透镜普查新点名的全部维度落进换名重提区（S4-E-1 /
  S3-J-5 / X1-1）、属地区（nowIso 实现体 / 锁戳 / registry 戳全部
  切片外）、契约面（通道来源矩阵 / extra 通道调用方全权 / 数据面
  直通）或实测清零区（W2 写路径零采样、apply 读路径零采样、
  test-only 位点）；新轴本身以「无候选」收口，未发现第二十三个
  机制类）。
- 剩余赢家：无（整轴现实档 0.43–4.75 µs/run，荒谬档 F=100 也仅
  43.6 µs = 229× 低于门槛；CPU 温态面即使清零也距门槛 418–450×
  （本构成）/ 658–680×（cmp25 构成）；每被采样文件的 I/O 以 ~10³×
  支配时钟份额）。
- 矩阵空洞：无（调用位点行号与 R8-E..R28-E 逐一相同；配置态 live
  面与载入面锚点 9/9 全带内（load-fresh 回归带内佐证 R28-E 位置
  噪声判读）；双 Node 版本交叉不可分辨；cmp25 种子对调 9/9 VERDICT
  逐字相同、总量 9/9 带内；diagnose 段 2/9 采样 1.32 = 带下缘外
  0.01 µs 表显粒度擦边，只读价签）。
- 时钟假说的双侧完成（「采样里藏着未测的成本洞或语义洞」）：成本
  洞以清单定理 + 逐项定价关闭（1 + F_parsed 采样 × ~430ns，全谱
  µs 级 once-per-run）；语义洞以来源矩阵关闭且**有实洞**——
  采样先于发射判定的通道不对称、extra 通道时钟与 episodeId 的
  调用方全权 + 持久面直通、ms 坍缩使可观察多样性退化、全 W1 清单
  的切片外份额——全部为行为可见的契约/属地面（记录为 §2.3
  (a)–(d)，非候选）。「时间」维度自此在 E 切片收口：值来源
  （本轮）× 值表示（R19-E）× 事件时序（R17-E/R19-E 次序面）。
- S13-B-1 边界：apply 本体零触碰（且本轮证其零时钟读），重开条件
  未触发。S8-E-1 零去重分析。S9-E-2 未触碰。PWL/RVL/RTL/CG/JB/
  NVG/GS 未复铸（§2.2 逐轴区分）。
- 不移植清单（含运行中快进增量）：R29-A input reference-identity /
  aliasing-topology face（共享断言全按字符串值比较，无引用同一性
  剂量）、R29-B learned-policy container face（learned 面零触碰，
  prefer 行序未动）、R29-C process workload-composition face /
  **PAIRX**（快进补入；单进程单工作负载、无估计器共驻剂量）、
  R28-A input key-set geometry face、R28-B catalog-face
  row-arrangement (permutation) face、R28-C C-slice numeric-kernel
  execution-substrate instantiation（face 名归 R27-H）、R28-D
  contents-store population face、R28-F boundary call-protocol
  face、R28-G decision-gate resolution face、R28-H boundary
  argument-slot materialization face、R28-I invocation-identity /
  path-referent spelling × cwd face（stableProjectKey 拼写规整
  未剂量）、R28-J **ZREP**、R27-A..R27-J 与 R26-A..R26-J 及更早
  各无名面、以及全部已名轴（FITQ / NAMESHAPE / SCALEX / XPROC /
  YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK /
  ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG /
  AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC /
  PWL / ZREP / PAIRX）——逐一定位核过，与本轴对象机制无交集。
  R29-D / R29-F 在飞，未读、未等待、未移植。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R28-E §6（含 R14-E 规模判据、R15-E
分配/GC/噪声地板判据、R16-E 累积状态与载荷形态判据、R17-E 异常面与
构成面判据、R18-E 执行层级与字符宽度判据、R19-E 表示面与次序面判据、
R20-E 并发面判据、R21-E 标识符几何判据、R22-E 数值值类判据、R23-E
消费级结构判据、R24-E 铸造级连接判据、R25-E 同键链态判据、R26-E
终止腿判据、R27-E 裁决格判据、R28-E 提案腿判据），并新增一条可标定
判据：**(l) 采样拓扑判据**——若切片获得每任务/每事件级时钟位点
（脱离 once-per-run 采样清单，位点普查 §2.1 需重证）、或 pi 目录
生产文件数增至使 (1+F) × 采样价越过两位数 ms（届时先越线的 I/O 面
须一并重证）、或 extra 通道 createdAt 获得格式验证/服务端盖戳
（通道来源矩阵变更需重证 (b)）、或 `nowIso` 实现体迁入本切片
（属地翻转）、或时间戳分辨率契约变更使 ms 坍缩失效（(c) 需重证）、
或 S4-E-1 / S3-J-5 / X1-1 任一被引裁决被正式推翻，或切片/调用面
出现实际代码漂移，优先复跑时钟轴双版本套件。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

临时脚本 sha256（测量完成后已从 /tmp 删除，正文与本附录为其存档）：

```text
11a6edb8072ed093ab94c3164a9bdd1a025649c9787f4f83932167f2d09e5351  /tmp/r29e-anchor.mts
10d6009436dd674b01cbb73c3b04f9324f50f1d77ee61eb45ac892585dff35a0  /tmp/r29e-clock.mts
```

主锚点脚本全文（`/tmp/r29e-anchor.mts`，`npx tsx /tmp/r29e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R29-E；
`NOW = "2026-08-26T01:50:00.000Z"`；SECTION 1 的 seeds
`0xe29e01`（collect）/ `0xe29e02`（routed）/ `0xe29e05`（signals）——
拒绝采样：collect/routed 角首选一次通过（collected=18>3、
outcomes=5>0），signals 角 `0xe29e03`（diagnoseGroups=0）与
`0xe29e04`（0）拒绝、`0xe29e05` 通过（diagnoseGroups=1）；探针
三元组 k=1..12 由 `probe` 开关打印（01: (18,1,0) / 02: (14,5,0) /
03: (17,1,0) / 04: (16,1,0) / 05: (16,1,1) / 06: (9,1,1) / 07:
(16,4,1) / 08: (19,2,0) / 09: (14,2,1) / 10: (17,1,0) / 11:
(18,5,0) / 12: (17,2,0)）；SECTION 2 的 `mkdtemp` 前缀
`r29e-fresh-`/`r29e-reg-` 与 detector identity `r29e-anchor`。另加
CLI 开关 `probe` 与 `cmp25`（置换为 R25-E 的 NOW
（`2026-08-24T17:30`）与 seeds `0xe25e01/02/08`、前缀
`r29e-cmp25-`、identity `r29e-anchor-cmp25`——种子/NOW/前缀经模式
常量 `CMP25` 参数化，语义与 R28-E cmp25 开关逐一相同）。其余逐字
同 R10-E 附录 A。

12 次主锚点运行原始区间（6 × v22.22.2 + 6 × v22.14.0）：

```text
SLICE-CPU 总量:
  v22.22.2: 22.3 / 22.3 / 22.2 / 22.9 / 22.4 / 22.4 us
  v22.14.0: 22.7 / 23.9 / 22.6 / 22.4 / 22.2 / 22.7 us
collect:    15.6-16.7 us（collected=18 构成）  outcomes: 4.8-5.2 us（outcomes=5 构成）
diagnose:   1.01-1.32 us（diagnoseGroups=1 构成） bandit:   0.7-0.8 us
apply(non): 265-284 ns/task    apply(空): 112-124 ns/task
load fresh: 25.8-33.8 us（历史并集 25.2-36.8 带内 9/9）
load real:  77.0-89.4 us（74.3-134.5 带内 9/9）
VERDICT 行: 12/12 逐位一致；sanity 3/3 x12 全过
```

9 次 R25-E 种子比对运行（cmp25）原始值（6 × v22.22.2 + 3 × v22.14.0）：

```text
总量: 14.8 / 14.8 / 14.9 / 14.9 / 15.2 / 15.0 | 15.1 / 14.9 / 14.7 us
  —— vs 默认带 14.7-15.2：9/9 带内（下缘区）
collect 11.3-11.7 / outcomes 1.3-1.5 / diagnose 1.32-1.36 / bandit 0.8
  —— vs R25-E 11.3-11.9 / 1.3-1.5 / 1.33-1.46 / 0.8：collect 与
  outcomes 全带内；diagnose 2/9 采样 1.32 低于带下缘 0.01 us
  （表显粒度级单侧擦边，§1 价签记录）
VERDICT 行: 9/9 逐位一致且与 R25-E / R26-E / R27-E / R28-E 夹具行逐字相同
```

时钟轴套件 5 次运行原始区间（3 × v22.22.2 + 2 × v22.14.0；
v22.14.0 批次以 `/exec-daemon/node node_modules/tsx/dist/cli.mjs`
调度）：

```text
nowIso():   431.6 / 434.0 / 431.3 | 425.5 / 423.5 ns/call
C9 全 W1 总采样: 7 / 7 / 7 | 7 / 7（切片内 3 + 切片外 4，5/5 一致）
ms 坍缩:    10000 采样 → 6 / 6 / 6 | 6 / 6 个不同串
VERDICT 行: 5/5 逐位一致；sanity 32/32 x5 全过
```

时钟轴套件全文（`/tmp/r29e-clock.mts`，`npx tsx /tmp/r29e-clock.mts`）：

```ts
/**
 * R29-E new completeness axis: wall-clock sampling topology / timestamp-
 * provenance face — WHEN and HOW OFTEN one collection run reads the wall
 * clock (nowIso() = new Date().toISOString()), at which in-slice call sites,
 * and which produced / durable records share which sample, as the controlled
 * variable. Channel provenance matrix: events-channel createdAt = one fresh
 * sample per collect invocation (taken BEFORE the zero-emission early return);
 * pi-channel createdAt = one fresh sample per successfully parsed file (guard
 * legs are sample-free); extra-channel createdAt AND episodeId = caller-
 * supplied pass-through (typeof-string validated only; isIsoTimestamp exists
 * in the domain but is not invoked at this boundary); W2 propose path is
 * clock-free in-slice (outcome rows carry event-time occurredAtMs, never
 * wall-clock).
 *
 * What prior passes did NOT control: R18-E note (d) / R19-E treated nowIso()
 * as a nuisance variable to STRIP for cross-call equivalence; S4-E-1 priced
 * one avoided sample inside the closed empty-events fast-path form; S3-J-5
 * (J slice) closed the per-view nowIso hoisting FORM as behavior-visible.
 * No E pass ever made the sample count/placement/sharing the controlled
 * variable. Levers, not variables: event composition pinned (R17-E), value
 * representation pinned (R19-E — samples are compared as string VALUES,
 * never as references: reference identity is R29-A's face and is not dosed),
 * container shapes pinned (R29-B), store states pinned (R28-J ZREP), env
 * pinned per arm (R25-I), no propose-leg dosing (R28-E PWL), no apply-leg
 * dosing (R26-E RTL).
 *
 * Instrumentation: Date.prototype.toISOString is wrapped with a counting
 * recorder that emits DETERMINISTIC ISO-valid strings (BASE_MS + n ms via the
 * saved original), restored after the census segment. Segments (argv[2]
 * optional filter): C (census, deterministic VERDICT lines) | P (pricing,
 * BENCH/OBS lines).
 */
import { mkdtemp, mkdir, readFile, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { performance } from "node:perf_hooks";
import {
  collectSignalsFromEvents,
  collectSignalsFromSubagentRun,
  parseObservedSignal,
  type ObservedSignal
} from "/workspace/src/learning/signals.js";
import { runAutoAdaptLoop } from "/workspace/src/learning/auto-loop.js";
import { proposeRoutingFromOutcomes, outcomesFromRoutedRun } from "/workspace/src/learning/from-episode.js";
import { createSignature } from "/workspace/src/learning/signatures.js";
import { feedbackLogPath } from "/workspace/src/feedback/store.js";
import { adaptationRoot } from "/workspace/src/privacy/state-layout.js";
import { nowIso, isIsoTimestamp } from "/workspace/src/domain/timestamp.js";
import { oneHotDistribution } from "/workspace/src/routing/catalog-model.js";
import type { Event } from "/workspace/src/run/events.js";
import type { EpisodeId, ProjectId } from "/workspace/src/domain/ids.js";
import { access } from "node:fs/promises";

const SEG = process.argv[2] ?? "CP";
const PROJECT_ID = "prj_clk00001" as ProjectId;
const EPISODE_ID = "epi_clk00001" as EpisodeId;

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}`);
  }
}
async function exists(path: string): Promise<boolean> {
  try {
    await access(path);
    return true;
  } catch {
    return false;
  }
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

/* ---------- deterministic counting clock ---------- */
const ORIGINAL_TO_ISO = Date.prototype.toISOString;
const BASE_MS = Date.UTC(2026, 7, 26, 2, 0, 0, 0);
let clockCalls = 0;
const samples: string[] = [];
function installClock(): void {
  clockCalls = 0;
  samples.length = 0;
  // Each wall-clock read yields a distinct, deterministic, ISO-valid stamp so
  // sample->record attribution is provable from output values alone.
  Date.prototype.toISOString = function patched(this: Date): string {
    const stamp = ORIGINAL_TO_ISO.call(new Date(BASE_MS + clockCalls));
    clockCalls += 1;
    samples.push(stamp);
    return stamp;
  };
}
function restoreClock(): void {
  Date.prototype.toISOString = ORIGINAL_TO_ISO;
}

/* ---------- fixtures ---------- */
function collectEvents(withRouted: boolean): Event[] {
  const out: unknown[] = [
    { type: "PROJECT_DISCOVERED", payload: { project: { id: PROJECT_ID, rootPath: "/tmp/proj-clk" } } },
    { type: "USER_ANSWER", runId: "run_clk00001", payload: { answer: "no, revert this" } }
  ];
  if (withRouted) {
    for (let i = 1; i <= 5; i += 1) {
      out.push({
        type: "MODEL_ROUTED",
        runId: "run_clk00001",
        payload: {
          taskId: `tsk_0000000${i}`,
          model: "m-bad",
          role: "actor",
          eligibleModels: ["m-bad", "m-good"],
          behaviorDistribution: oneHotDistribution(["m-bad", "m-good"], "m-bad"),
          family: "edit",
          modelVersion: "v1",
          featureVersion: "fv1",
          agentRole: "worker"
        }
      });
      out.push({
        type: "CHILD_MESSAGE",
        runId: "run_clk00001",
        payload: {
          message: {
            type: "TASK_RESULT",
            taskId: `tsk_0000000${i}`,
            runId: "run_clk00001",
            outcome: "FAILURE",
            verification: { kind: "FAILED" },
            summary: "plain verdict probe body",
            evidenceIds: ["evd_00000001"]
          }
        }
      });
    }
  }
  return out as Event[];
}
function padEvents(events: Event[], to: number): Event[] {
  const out = [...events] as unknown[];
  while (out.length < to) out.push({ type: "LEDGER_UPDATED", payload: {} });
  return out as Event[];
}
const PI_VALID_TWO_RESULTS = JSON.stringify({
  status: "completed",
  request: { agent: "tester" },
  results: [{ agent: "tester", messages: [] }, { messages: [] }]
});
const PI_VALID_ZERO_RESULTS = JSON.stringify({ status: "completed", request: {}, results: [] });

async function makePiDir(): Promise<string> {
  const dir = await mkdtemp(join(tmpdir(), "r29e-clk-pi-"));
  await writeFile(join(dir, "a-valid.json"), PI_VALID_TWO_RESULTS, "utf8");
  await writeFile(join(dir, "b-valid.json"), PI_VALID_ZERO_RESULTS, "utf8");
  await writeFile(join(dir, "c-malformed.json"), "{not json", "utf8");
  await writeFile(join(dir, "d-empty.json"), "", "utf8");
  await writeFile(join(dir, "e-nonrecord.json"), "[1,2,3]", "utf8");
  await writeFile(join(dir, "f-skipped.txt"), PI_VALID_TWO_RESULTS, "utf8");
  return dir;
}

/* ================================================================
 * SEGMENT C: sample census + sharing topology + durable pass-through.
 * ================================================================ */
if (SEG.includes("C")) {
  installClock();

  /* ---- C1: events channel — exactly one sample per invocation, shared. ---- */
  {
    const base = collectEvents(true);
    for (const [label, events] of [
      ["E=0 (zero-emission leg)", [] as Event[]],
      ["E=12", base],
      ["E=200", padEvents(base, 200)]
    ] as const) {
      const before = clockCalls;
      const signals = collectSignalsFromEvents(events, { projectId: PROJECT_ID });
      const used = clockCalls - before;
      check(`C1 ${label}: exactly 1 clock sample`, used === 1);
      const sample = samples[before]!;
      check(
        `C1 ${label}: every emitted signal carries the single sample bit-identically`,
        signals.every((signal) => signal.createdAt === sample)
      );
      if (events.length === 0) check("C1 E=0 returns [] yet still sampled", signals.length === 0);
    }
    console.log(
      "VERDICT CLOCK C1 events-channel: 1 sample per collect invocation for E in {0,12,200}; sample taken BEFORE the zero-emission early return (signals.ts:137 precedes :158); all emitted signals share that one sample bit-identically"
    );
  }

  /* ---- C2: pi channel unit — guard legs sample-free; 1 sample per parsed record. ---- */
  {
    let before = clockCalls;
    const guardNonRecord = collectSignalsFromSubagentRun([1, 2, 3], { projectId: PROJECT_ID });
    const guardNoProject = collectSignalsFromSubagentRun(JSON.parse(PI_VALID_TWO_RESULTS), {});
    check("C2 guard legs (non-record raw / missing projectId): 0 samples", clockCalls === before);
    check("C2 guard legs return []", guardNonRecord.length === 0 && guardNoProject.length === 0);

    before = clockCalls;
    const zero = collectSignalsFromSubagentRun(JSON.parse(PI_VALID_ZERO_RESULTS), { projectId: PROJECT_ID });
    check("C2 valid raw with 0 results: 1 sample paid, 0 signals emitted", clockCalls - before === 1 && zero.length === 0);

    before = clockCalls;
    const two = collectSignalsFromSubagentRun(JSON.parse(PI_VALID_TWO_RESULTS), { projectId: PROJECT_ID });
    const sample = samples[before]!;
    check("C2 valid raw with 2 results: 1 sample shared by both signals", clockCalls - before === 1 && two.length === 2 && two.every((signal) => signal.createdAt === sample));
    console.log(
      "VERDICT CLOCK C2 pi-channel unit: guard legs (non-record / no projectId, signals.ts:247) are sample-free; the sample at :252 is paid once per successfully parsed record even when 0 signals emit, and is shared by all signals of that record"
    );
  }

  /* ---- C3: directory census on the kill-switch (collect-only) leg. ---- */
  {
    const stateRoot = await mkdtemp(join(tmpdir(), "r29e-clk-c3-"));
    const piDir = await makePiDir();
    process.env.SPARKLE_AUTO_ADAPT = "0";
    const before = clockCalls;
    const result = await runAutoAdaptLoop({
      stateRoot,
      projectRoot: "/tmp/proj-clk",
      projectId: PROJECT_ID,
      primaryModelId: "m-good",
      events: [
        { type: "PROJECT_DISCOVERED", payload: { project: { id: PROJECT_ID, rootPath: "/tmp/proj-clk" } } },
        { type: "USER_ANSWER", runId: "run_clk00001", payload: { answer: "no, revert this" } }
      ] as unknown as Event[],
      subagentRunsDir: piDir
    });
    delete process.env.SPARKLE_AUTO_ADAPT;
    const used = clockCalls - before;
    check("C3 kill-switch leg reason", result.reason === "auto-adapt disabled; collected only");
    check("C3 collected = 1 user + 2 pi signals", result.collected === 3);
    check(
      "C3 clock samples = 1 (events) + 2 (parsed pi files a,b) = 3; malformed/empty/non-record/.txt files sample-free",
      used === 3
    );
    check("C3 no feedback rows (no episodeId anywhere)", !(await exists(feedbackLogPath(stateRoot))));
    check("C3 no bandit write (no modelId on any signal)", !(await exists(join(adaptationRoot(stateRoot), "learning"))));
    console.log(
      "VERDICT CLOCK C3 per-run manifest (collect-only leg): samples = 1 + F_parsed (F_parsed = successfully parsed .json files; malformed JSON, empty files, non-record JSON and non-.json names are sample-free); fixture {2 valid, 1 malformed, 1 empty, 1 non-record, 1 .txt} -> exactly 3 samples"
    );
  }

  /* ---- C4: extra channel — clock-free pass-through, caller-controlled. ---- */
  {
    const before = clockCalls;
    const raw = {
      source: "subagent",
      kind: "peer",
      projectId: PROJECT_ID,
      score: 55,
      boundary: "review",
      summary: "sentinel extra signal",
      createdAt: "totally-not-a-timestamp",
      episodeId: EPISODE_ID
    };
    const parsed = parseObservedSignal(raw);
    check("C4 extra channel: 0 clock samples", clockCalls === before);
    check("C4 createdAt pass-through byte-identical", parsed.createdAt === "totally-not-a-timestamp");
    check("C4 the passed-through string is NOT an IsoTimestamp by the domain's own validator", !isIsoTimestamp(parsed.createdAt));
    check("C4 episodeId also caller-supplied on this channel", parsed.episodeId === EPISODE_ID);
    console.log(
      "VERDICT CLOCK C4 extra-channel: parseObservedSignal never reads the clock; createdAt is caller-supplied and typeof-string validated only (isIsoTimestamp exists in src/domain/timestamp.ts but is not invoked at this boundary) — the extra channel's clock is fully caller-controlled"
    );
  }

  /* ---- C5: durable pass-through — sentinel createdAt reaches records.jsonl byte-identically. ---- */
  {
    const stateRoot = await mkdtemp(join(tmpdir(), "r29e-clk-c5-"));
    process.env.SPARKLE_AUTO_ADAPT = "0";
    const before = clockCalls;
    const result = await runAutoAdaptLoop({
      stateRoot,
      projectRoot: "/tmp/proj-clk",
      projectId: PROJECT_ID,
      primaryModelId: "m-good",
      episodeId: EPISODE_ID,
      events: [
        { type: "PROJECT_DISCOVERED", payload: { project: { id: PROJECT_ID, rootPath: "/tmp/proj-clk" } } },
        { type: "USER_ANSWER", runId: "run_clk00001", payload: { answer: "no, revert this" } }
      ] as unknown as Event[],
      extraSignals: [
        {
          source: "subagent",
          kind: "peer",
          projectId: PROJECT_ID,
          score: 55,
          boundary: "review",
          summary: "sentinel extra signal",
          createdAt: "1999-01-02T03:04:05.000Z",
          episodeId: EPISODE_ID,
          evidenceIds: []
        } as unknown as ObservedSignal
      ]
    });
    delete process.env.SPARKLE_AUTO_ADAPT;
    const eventsSample = samples[before]!;
    check("C5 collected = 1 user + 1 extra", result.collected === 2);
    const jsonl = await readFile(feedbackLogPath(stateRoot), "utf8");
    const rows = jsonl.trim().split("\n").map((line) => JSON.parse(line) as { createdAt: string; summary: string });
    check("C5 two feedback rows persisted (both episodeId-bound)", rows.length === 2);
    const extraRow = rows.find((row) => row.summary.includes("sentinel"));
    const userRow = rows.find((row) => row.summary.startsWith("user:"));
    check("C5 caller-supplied sentinel createdAt persisted byte-identically", extraRow?.createdAt === "1999-01-02T03:04:05.000Z");
    check("C5 events-channel row persisted with the collect-invocation sample", userRow?.createdAt === eventsSample);
    console.log(
      "VERDICT CLOCK C5 durable pass-through: FeedbackRecord.createdAt = signal.createdAt verbatim (appendFeedback stamps nothing) — the caller-supplied extra-channel clock and the collect-invocation sample both reach records.jsonl byte-identically"
    );
  }

  /* ---- C6: provenance matrix (summarised from C1..C5 assertions). ---- */
  console.log(
    "VERDICT CLOCK C6 provenance matrix: events-channel {createdAt: fresh sample per invocation, episodeId: context} | pi-channel {createdAt: fresh sample per parsed file, episodeId: context} | extra-channel {createdAt: caller, episodeId: caller} | W2 outcome rows {occurredAtMs: event time, never wall-clock}"
  );

  /* ---- C7: signatures.ts createSignature (test-only face). ---- */
  {
    const before = clockCalls;
    const sig = createSignature(EPISODE_ID, "execution", { operation: "read" });
    check("C7 createSignature: 1 sample per call", clockCalls - before === 1);
    check("C7 stamp equals the sample", sig.createdAt === samples[before]);
    console.log(
      "VERDICT CLOCK C7 signatures.ts:41 samples once per createSignature call — zero production callers (test-only face, census re-verified this pass), auto-rejected as a dosing site"
    );
  }

  /* ---- C8: W2 propose path is clock-free in-slice. ---- */
  {
    const stateRoot = await mkdtemp(join(tmpdir(), "r29e-clk-c8-"));
    const before = clockCalls;
    const res = await proposeRoutingFromOutcomes({
      stateRoot,
      projectRoot: "/tmp/proj-clk",
      projectId: PROJECT_ID,
      primaryModelId: "m-good",
      outcomes: []
    });
    check("C8 W2-N zero-I/O leg: 0 clock samples", clockCalls === before);
    check("C8 W2-N reason", res.reason === "no bound taskSuccess outcomes");
    const beforeOutcomes = clockCalls;
    const outs = outcomesFromRoutedRun(collectEvents(true));
    check("C8 outcomesFromRoutedRun: 0 clock samples (occurredAtMs is event time via Date.parse)", clockCalls === beforeOutcomes && outs.length === 5);
    console.log(
      "VERDICT CLOCK C8 W2 write path: clock-free in-slice — outcome rows carry event-time occurredAtMs (input provenance), the propose legs read no wall clock (out-of-slice registry/lock stamps are neighbour territory, R28-E PWL priced those legs)"
    );
  }

  /* ---- C9: full W1 failing run — total is deterministic; slice share = 1+F. ---- */
  {
    const totals: number[] = [];
    for (let rep = 0; rep < 2; rep += 1) {
      const stateRoot = await mkdtemp(join(tmpdir(), "r29e-clk-c9-"));
      const piDir = await makePiDir();
      const before = clockCalls;
      const res = await runAutoAdaptLoop({
        stateRoot,
        projectRoot: "/tmp/proj-clk",
        projectId: PROJECT_ID,
        primaryModelId: "m-good",
        events: collectEvents(true),
        subagentRunsDir: piDir
      });
      totals.push(clockCalls - before);
      check(`C9 rep${rep} full W1 run proposes`, res.created && res.reason === "proposed routing-policy candidate");
    }
    check("C9 total clock samples deterministic across 2 fresh-root repeats", totals[0] === totals[1]);
    const sliceShare = 1 + 2;
    console.log(
      `VERDICT CLOCK C9 full W1 failing run: total clock samples = ${totals[0]} per run (2/2 fresh-root repeats identical); slice-attributed = ${sliceShare} (1 collect + 2 parsed pi files, by C1-C3 topology); out-of-slice = ${totals[0]! - sliceShare} (bandit file-lock acquiredAt + adaptation registry version stamps + registry file-lock — R28-E PWL / neighbour territory)`
    );
  }

  restoreClock();
}

/* ================================================================
 * SEGMENT P: pricing (real clock; landing-bar arithmetic).
 * ================================================================ */
if (SEG.includes("P")) {
  console.log("--- BENCH CLOCK pricing (real clock) ---");
  const perCall = bench(() => void nowIso(), 2000000);
  const ns = perCall * 1e6;
  console.log(`BENCH CLOCK nowIso(): ${ns.toFixed(1)}ns/call`);
  for (const F of [0, 2, 10, 100]) {
    const budget = (1 + F) * ns;
    console.log(
      `BENCH CLOCK per-run axis budget at F_parsed=${F}: ${(1 + F)} samples x ${ns.toFixed(1)}ns = ${(budget / 1000).toFixed(2)}us/run (landing bar >=10000us: ${Math.round(10000 / (budget / 1000))}x below even if zeroed)`
    );
  }
  console.log(
    `BENCH CLOCK hoist counterfactual (one shared sample per run instead of 1+F): saves F x ${ns.toFixed(1)}ns = ${(2 * ns / 1000).toFixed(3)}us at F=2 — and collapses per-file stamps into the run stamp (behaviour-visible, S3-J-5 family divergence): reject`
  );

  // Adjacent-sample collapse incidence (ms resolution property; OBS, not VERDICT).
  {
    const K = 10000;
    const seen = new Set<string>();
    for (let i = 0; i < K; i += 1) seen.add(nowIso());
    console.log(
      `OBS CLOCK ms-resolution collapse: ${K} back-to-back samples produced ${seen.size} distinct ISO strings (~elapsed ms + 1) — adjacent samples usually collapse to bit-identical stamps, so the observable multi-sample topology of one real run frequently degenerates to a single distinct value even though the clock is read 1+F times`
    );
  }
}

console.log(`VERDICT total sanity checks: ${checks}, failures: ${failures}`);
if (failures > 0) {
  console.error(`${failures} SANITY CHECK(S) FAILED`);
  process.exit(1);
}
console.log("ALL CLOCK SANITY CHECKS PASSED (deterministic census, no RNG)");
```

MORE_OPTIMA=no
BRANCH=cursor/r29-e-learning-twenty-ninth-pass-83a1
