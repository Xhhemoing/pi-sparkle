MODEL_SLUG=claude-fable-5-thinking-xhigh

# R22-D：`src/adaptation/` 第二十二遍搜查报告（Round 1–21 同区第二十二遍）

**战役:** 全库持久 SOTA 优化 Round 22 / R22-D（Round 1–21 同区第二十二遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `5590443`（R22-A/R22-B 收口已合入；R22-C / R21-J 在飞未触碰）
**分支:** `cursor/r22-d-adaptation-twenty-second-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S22-D-\* ID，切片零代码
改动；整片预算收口第二十二次复核成立，并关掉一条二十一遍从未定价的
完备性轴——「公开导出流普查」（public-export flow census）：R13-D…
R21-D 把切片按**命令相位**（R13-D 21 相位）、**规模剂量**（R14-D）、
**引擎状态**（R15-D）、**载荷形态**（R16-D）、**拒绝终端**（R17-D）、
**锁事务**（R18-D）、**进程边界**（R19-D）、**进程共存**（R20-D）、
**状态增长方向**（R21-D）定价，并逐轮复核五个休眠模块（monitor /
pareto / mutate / retirement / reflection）零生产调用方——但**导出面
本身**从未被当作普查对象逐一定价：切片共发布 **66 个不同的值导出**
（另有 promotion.ts 的 7 个再导出别名），首手 grep 普查其中恰
**12 个绑定**被生产面 import（`evalRoutingPolicy` /
`parseRoutingEvalReport` / `loadAdaptationRegistry` /
`loadAdaptationRegistryOrNew` / `saveAdaptationRegistry` /
`withAdaptationRegistryLock` / `promoteWithRegistry` /
`parsePromotionReview` / `rollbackActive` / `ROLLBACK_REASONS` /
`hashCandidateContent` / `isAutoAdaptEnabled`；调用方恰四处 +
tracking/analysis.ts 纯 type import），其余 **54 个**为切片内传递件或
仅测试合同载体。一个藏着重体的休眠导出会在任何调用方出现的瞬间变成
潜伏成本雷——本轴把全部休眠/传递导出在生产现实形态 + 剂量网格上
逐格定价并配正确性见证，用**实测天花板**而非「调用方计数」关死该面
（附录脚本三次独立运行全绿）：**

1. **休眠模块操作普查（S1a，八组，全部 µs 级）**：`paretoFront`
   O(n²) 支配扫描 n∈{8,64,512} → 1.9 / 9.1–9.4 / 441–1249 µs（暴力
   非支配集见证逐格相等）；`mutateOnce` 三种变异全部 8.8–15.3 µs
   （3.3 KB parent；语义后置条件 + 歧义/高风险 fail-closed 见证）；
   `proposeCandidates` E∈{8,64,512}（含重复键 + 自支持混合）→
   69.7–75.6 / 544–572 / 4063–4188 µs（自支持全拒 + boundary:pattern
   去重 + cap 见证）；`createAdaptationDriftMonitor` 稳态 observe
   2.8–3.0 µs（W=512 摊薄；W=8 首窗含 JIT 39.7–49.1 µs）、snapshot
   随**累计观测数**线性 0.3→14.4–15.8 µs、restore+observe
   7.4–38.8 µs（漂移/不漂移双见证）；retirement 三件套 17–359 ns
   （幂等 + 拒退 active + assertAssignable fail-closed 见证）；
   `PromotionService` begin+commit 两相 42.6–44.7 µs ≈ 一步
   `promoteWithRegistry`（41.2–45.1 µs）同带（确定性孪生夹具快照
   语义相等见证）；`reconstructPromotion` 全台账扫描 L=11 → 95–107
   ns、L=4096 幻想态 → 4.8 µs；`RollbackLog` append 97–191 ns /
   list(L=4096) 1.4–1.7 µs / restore(L=4096) 405–549 µs（防御拷贝 +
   全量在册见证）。
2. **微助手单价表（S1b，31 格 + 快照解析）**：唯二 >1 µs 的格是
   `assertSingleResourceBoundary`（9.2 KB 策略 15.3–16.4 µs——体内
   JSON.parse 探针）与 `assertRoutingPolicyEvalReport`（整门
   12.4–12.5 µs）——两者都已**内含**在 S0d promote op 锚
   （41.2–45.1 µs）里，不是新增成本；其余 29 格全部 ≤ 451 ns
   （身份/scope/key 助手、门谓词、parse/validate 头、approval-profile
   头、split 助手，逐格配语义见证：CAS 双向 + 陈旧拒绝 + 未知键
   fail-closed、门拒模型 actor、profile 永不自动提升、kill-switch
   语义、split 决定论 + holdout 封印、无环谱系环拒）；
   `parseRegistrySnapshot`（146 KB 配置态快照对象）36.2–37.7 µs
   ——装载链既裁域（S11-D-2）的同带复锚。
3. **10 ms 越线求解（S1c）**：普查最贵休眠格 `paretoFront(n=512)`
   441–450 µs，需 **22–23 次调用/命令**才到 10 ms——而生产调用数
   是 **0**（休眠）；全普查唯一 >10 ms 的格
   `evaluateProposalShadow(P=2000/A=1000)`（123.4–125.9 ms）是
   **experiments 切片属地**（reflection.ts 只是薄委托，交叉核对锚，
   R11-D 起历轮同判）且 P=2000 剂量本身是幻想态。
4. **本轮新钉事实（非候选）**：monitor 的内部 `observations` 数组
   **无界增长**（observe 稳态只付 O(windowSize=8) 切片但 push 不
   截断；snapshot/restore 付 O(累计观测数)）——该模块零生产调用方
   （二十二轮 grep 复核），零生产流量下无性能面；「修复」= 对休眠
   合同载体的行为变更 + 零收益，不铸 ID，记录在案（§4-3）。

**天花板裁决（频率 × 单价 × 属地三端封死）**：频率端——54 个休眠
导出生产调用数恒为 0（首手 grep），12 个生产件的每命令调用次数由
R13-D 相位账目钉死为有界常数；单价端——普查全部格在生产现实形态
落 ns–µs 级，最贵休眠格到 10 ms 需 22–23 次调用/命令而实际是 0，
唯一 ms 级格属 experiments 切片且剂量幻想；属地端——每条消除思路
撞死在既有 ID（§4：休眠模块「优化」= 死代码抛光、边界闸/门链弱化 =
fail-closed 家族（S12-D-1 邻域明文冻结）、装载链 = S11-D-2、台账
扫描 = S1-D-8 族 ns 类、验证循环弱化 = fail-open）。**该轴无新缝隙，
收口，无新 ID。**

配置态 eval 地板同 VM 复锚（指令要求，未抄旧数）：附录脚本三次运行
**3.73–3.76 ms**——与 R20-D 3.75–3.97 在 3.75–3.76 重叠、紧邻
R21-D 联合带 3.84–4.19 下沿、落入历史带 3.1–4.2；零 diff 下带间位移
是 VM/窗口方差而非代码效应。同夹具 op 锚与前三轮同带：promote op
41.2–45.1 µs（R21-D 45.1–46.8 相接于 45.1、R19-D 43.1–46.4 交叠）、
rollback op 3.0–3.3 µs（R21-D 3.2–3.4 交叠）、hash32 9.6–10.1 µs
（R21-D 9.6–9.9 交叠）。清零切片内计算仍差落地线一个数量级——预算
结论第二十二次成立。

切片 14 个文件自 R1-D 基线（`82bef36`）经 R2-D … R21-D 至本轮基线
（`5590443`）**逐字节未变**（`git diff 82bef36..HEAD --
src/adaptation` 为空，二十二遍全程零 diff；`ls` 复核恰 14 文件）。
调用面维持最强冻结并再延一轮：R21-D 基线（`6e43537`）以来
`git diff 6e43537..HEAD -- src/` **完全为空**（期间 7 个提交全部为
docs(sota-opt)），生产调用方经全库 grep 首手复核仍为
`cli/adapt.ts` / `learning/auto-loop.ts` / `learning/from-episode.ts` /
`learning/learned-routing.ts` 四处（锁调用点仍恰 cli/adapt.ts
L293/L391、from-episode L104、auto-loop L165，全默认参数——R20-D
钉住的事实原样成立）；`createAdaptationDriftMonitor` / `paretoFront`
/ `proposeCandidates` / `mutateOnce` / `retireVersion` /
`evaluateProposalShadow` 在 `src/` 生产面依旧零切片外调用方
（monitor 格维持 N/A）。R1-D…R21-D 全部测量与排除**全额转移**。
全部测量在 engines 合规 Node **v22.22.2** 完成（与 R13-D…R21-D
同引擎，带可比），附录脚本三次独立运行全绿
（`All checks passed.`）。

按令对排除表做换名拒绝审计：本轮逐文件重读与普查轴中识别的全部角度
经逐条检查判定为既有 ID 的换名/同方案异站、fail-closed 弱化家族、
死代码抛光或零收益装饰类（8 项新记录，§4；指令点名项逐条核过，
§5），按「宁可空枚举也不铸装饰性 ID」的裁决纪律**一律不铸新 ID**。
R21-D 状态增长方向面（本轴零老化驱动、零深度扫掠——S1a 剂量是对
休眠导出的**调用次数/输入尺寸**剂量，不动注册表状态；配置态锚夹具
原样复用 V=15 形态）、R20-D 多进程并发面（全程单进程、零锁计时）、
R19-D 冷进程模块图（零子进程、零 import 计时）**未重铸、未重测、
原样引用**。R22-B 标识符几何（G×T）与 R21-E IDG **未移植**（本轴
全部夹具 id 为定长顺序串，无任何 id 长度自变量）；FITQ / NAMESHAPE
/ SCALEX / XPROC / YMIX / SCHEDWIN 未以任何形态重提。S12-D-1/2、
S11-D-1/2、S10-D-1/2、S9-D-4、S8-D-1..3 族未以任何形态重提。
S12-B-2 仍未触发（`eval-routing.ts` L353-354 首手复核仍只传
`learned`，无 `prior`，未从 D 面强落）。硬不变量全程满足（零 diff
天然保持）：CAS/凭据/数据面契约、双 LCB 与双归因、proposal-first、
阈值、公开签名、fsync 与完整性重哈希全部原样；不声称
Outcome-supported，Checkpoint F-PROD 仍开放（ADR-005）。
第二十二遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/adaptation/` 全目录，**恰 14 文件**（`ls` 复核）：
  eval-routing / promotion-rules / promotion / registry / mutate /
  pareto / candidate / rollback / reflection / approval-profile /
  resource / monitor / retirement / active-pointer，**全部实际重读**
  （不靠记忆，逐行对全排除表）。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表，含 S9-D-4、
  S12-D-1..2、S8-D-1..3、X0-7 阈值邻域、S1-D-\*/S2-D-\*/S3-D-\* 结构
  拷贝族）/ PROGRESS.md / round-22/PLAN.md / round-21/R21-D.md
  （空枚举；状态增长方向面）/ round-20/R20-D.md（空枚举；多进程
  并发面）及 R19-D…R8-D 各轮收口逐份核对；R22-A / R22-B 收口报告与
  Round 22 ban-list 核读。
- 基线验证：从最新 `cursor/sota-persistent-opt-83a1`
  （tip = `5590443`，含 R22-A/R22-B ingest 与 R22-C/D 派单）切出
  工作分支；`git diff 82bef36..HEAD -- src/adaptation` **为空**
  （第二十二轮字节恒等）；`git diff 6e43537..HEAD -- src/` **为空**
  （自 R21-D 基线以来 `src/` 生产面零字节变更，期间 7 个提交全部为
  docs(sota-opt)）。EXCLUSIONS.md / PROGRESS.md / PLAN.md 未触碰。
  未开 PR。未 force-push。R22-C / R21-J 在飞，未触碰其分支与报告
  文件。
- 邻居只读：`src/cli/adapt.ts`（promote L320 与两处锁调用点
  L293/L391；status/eval 无锁读）、`src/learning/`（auto-loop L165、
  from-episode L104、learned-routing 的单 routing-policy 身份）、
  `src/tracking/analysis.ts`（纯 type import 复核）、
  `src/domain/ids.ts`、`src/experiments/`（shadow 交叉核对的类型
  来源）。零编辑。
- 环境：本 VM 默认 Node v22.14.0，按 engines（>=22.19.0）切至
  **v22.22.2**（nvm；与 R13-D…R21-D 同引擎）后
  `pnpm install --frozen-lockfile`（pnpm 10.17.1）；全部测量与门禁
  在 v22.22.2 完成。
- 硬不变量全程满足（本轮零 diff，天然满足）：双 LCB（beta+normal）
  与双归因（logit+prob-add）保留（跨切片，未从本面弱化）、提升
  proposal-first、无 Outcome-supported 声称（Checkpoint F-PROD
  开放，ADR-005）、阈值不动、CAS/凭据/数据面契约与公开签名不动、
  fail-closed 不 fail-open（普查中的每一个 fail-closed 终端只作
  见证，未提任何弱化）、fsync 不删（S8-D-3；save 锚全程含 fsync）、
  完整性重哈希不删（S8-D-1..2）、fail-open 编码禁区不碰（S9-D-4 /
  S12-D-1 廉价形态永不重开；`eval-routing.ts` 的 `key.toLowerCase()`
  站点本轮首手复核仍无 ASCII 短路变体）、S12-B-2 未触发（L353-354
  仍只传 `learned`）、测试未改。
- 无赢家 ⇒ 零生产改动，普查仿真只留在本报告附录，**不入库**
  （无 `scripts/round22-r22d-*.ts`）。

## 1. 配置态 × 命令类矩阵同 VM 复锚（指令要求；eval 地板重锚，未抄旧数）

附录脚本三次独立运行（seeds `0xd22d01`–`0xd22d02` 起），构造与
R8-D…R21-D 逐字段同构（配置态：9.2 KB 路由策略 40 avoid + 40 prefer
+ 7 assignments；8 个 prompt 身份、5 promote、1 pending、2 rollback、
2 retire；registry.json 146.2 KB、V=15 C=9 L=11 B=18、blobs
119.5 KB）。

| 矩阵格 | 默认态 | 配置态 | 与 R20-D / R21-D（同引擎 v22.22.2）比对 |
| --- | --- | --- | --- |
| load | 0.11–0.13 ms | 0.57–0.60 ms | R20-D 0.56–0.79 于 0.57–0.60 重叠；R21-D 0.70–0.90 紧邻上方 |
| save(+fsync) | 0.39–0.77 ms | 1.25–1.82 ms | R21-D 1.51–1.69 内含；R20-D 1.43–1.77 交叠；fsync 方差支配 |
| eval 端到端（E=200） | 4.15–4.32 ms | **3.73–3.76 ms** | 与 R20-D 3.75–3.97 在 3.75–3.76 重叠；紧邻 R21-D 联合带 3.84–4.19 下沿；历史带 3.1–4.2 内。零 diff 下带间位移是 VM/窗口方差而非代码效应 |
| promote（op-only，K=60） | — | 41.2–45.1 µs | R21-D 45.1–46.8 相接于 45.1、R19-D 43.1–46.4 交叠 |
| rollback（user CAS，op-only） | — | 3.0–3.3 µs | R21-D 3.2–3.4 交叠、R20-D 3.1–4.1 内 |
| hash32（9.2 KB 策略单次） | — | 9.6–10.1 µs | R21-D 9.6–9.9 交叠、R20-D 9.7–9.9 内含 |
| monitor | N/A | N/A | 仍无生产调用方（grep 首手复核） |

**预算结论复核成立**：eval 地板 ~3.7–4.2 ms、save/fsync 支配持久化
路径的图景第二十二次成立。R14-D 的实测越线标定、R15-D 的分配/GC/
MDE、R16-D 的形态五轴、R17-D 的拒绝路径普查、R18-D 的锁事务账目、
R19-D 的冷进程模块图、R20-D 的多进程并发面、R21-D 的状态增长方向面
不重测、原样引用（§5 / §8）。

### 1b. `evaluateProposalShadow` 交叉核对（experiments 切片属地）

P=200/A=100 → 1.3 ms；P=2000/A=1000 → 123.4–125.9 ms。与 R21-D
（1.4 / 119.7–125.1 ms）同带；`reflection.ts` 只是薄委托，实现在
`src/experiments/`（切片外），维持锚点性质，不据此铸 ID。本格同时是
普查（§2）里唯一 >10 ms 的格——属地与剂量双重排除。

### 1c. 调用图第二十二次复核（只读邻居 grep 事实）

- `loadAdaptationRegistry(OrNew)` / `saveAdaptationRegistry` /
  `evalRoutingPolicy` / `promoteWithRegistry` /
  `withAdaptationRegistryLock`：生产调用方仍恰为 `cli/adapt.ts`、
  `learning/auto-loop.ts`、`learning/from-episode.ts`、
  `learning/learned-routing.ts` 四处——单命令单事务模式第二十次
  复验成立；锁调用点行号与 R21-D 逐处相同（L293/L391/L104/L165，
  全默认参数），`src/` 零字节变更下天然保持。
- **本轴新钉事实（普查静态半）**：切片 66 个值导出（+7 再导出
  别名）中生产 import 的绑定恰 12 个（结论节列全）；
  `tracking/analysis.ts` 三行全部 `import type`（零运行时流量）；
  余下 54 个导出的生产调用数为 **0**（切片内传递件或仅测试）。
- `createAdaptationDriftMonitor` / `paretoFront` / `proposeCandidates`
  / `mutateOnce` / `retireVersion` / `evaluateProposalShadow`：`src/`
  生产面零切片外调用方——monitor/pareto/mutate/retirement/reflection
  五模块维持「无生产调用方」格局（R4-D 事实第十九次复验）。

## 2. 新轴：公开导出流普查（休眠/传递导出逐格定价 + 见证）

方法：二十一遍的定价全部以**生产主路径**为索引——命令、规模、
状态、载荷、锁、进程、增长方向——而从未把**导出面本身**当普查
对象：休眠导出零流量所以历轮只记「无调用方」一句话，从未给出
「如果被调用会付多少」的实测天花板。这留下一类真实缝隙：藏着重体
（意外 O(n²)、隐藏 I/O、重验证循环）的休眠导出会在未来任何调用方
出现的瞬间变成主路径成本，而届时没有任何既有锚能预警。本轴分三段
关死：(2a) 静态普查（66 值导出 → 12 生产件 + 54 休眠/传递件，首手
grep）；(2b) 休眠模块操作在生产现实形态 + 剂量网格上逐格定价，
每格配语义正确性见证（S1a 八组）；(2c) 微助手单价表把剩余全部值
导出一网打尽（S1b 31 格）；(2d) 10 ms 越线求解对齐验收线。判读
标尺沿用历轮验收线（数十–数百 ms 稳态；µs/ns 与 once-per-run 是
拒绝类）。

### 2a. 静态普查（首手 grep，三类分拣）

| 类别 | 计数 | 明细 |
| --- | --- | --- |
| 生产 import 绑定 | **12** | 结论节列全；调用方恰 cli/adapt.ts、learning/auto-loop.ts、learning/from-episode.ts、learning/learned-routing.ts 四处 |
| 纯 type import | 3 处 | tracking/analysis.ts（ImprovementCandidate / ResourceRegistry / ResourceIdentity）——零运行时流量 |
| 休眠/传递值导出 | **54** | 五个整模块（monitor / pareto / mutate / retirement / reflection 的全部值导出）+ registry/promotion/rollback/candidate/resource/active-pointer/approval-profile/promotion-rules 的非生产件 |

### 2b. 休眠模块操作定价（S1a 八组；三运行带；全部配语义见证）

| 格 | 形态/剂量 | 单价（三运行带） | 见证 |
| --- | --- | --- | --- |
| `paretoFront` | n=8 / 64 / 512（5 目标随机点） | 1.9 µs / 9.1–9.4 µs / 441–1249 µs | 暴力 O(n²) 参考实现非支配集逐元素相等（front=7/28/104） |
| `mutateOnce` append | 3.3 KB parent | 9.3–10.1 µs | 追加块字节精确 + contentHash 自洽 |
| `mutateOnce` replace-section | 同上 | 8.8–9.1 µs | 段内替换到位 + 邻段不动；歧义段 fail-closed |
| `mutateOnce` adjust-parameter | 同上 | 12.4–15.3 µs | 参数值改写 + 旧值消失；高风险族拓扑 fail-closed |
| `proposeCandidates` | E=8 / 64 / 512（含重复键 + 自支持混合，uncapped） | 69.7–75.6 / 544–572 / 4063–4188 µs（7/52/410 提案） | 自支持全拒计数相等 + boundary:patternKey 去重基数相等 + 谱系/哈希逐提案 + cap=3 强制 |
| `createAdaptationDriftMonitor` | W=8 / 64 / 512 次 observe（windowSize=8 默认） | 稳态 observe 2.8–3.0 µs；snapshot 0.3→14.4–15.8 µs（O(累计观测)）；restore+observe 7.4–38.8 µs | 稳定首窗不漂移 + 多数未见窗按轴宣告漂移 + snapshot/restore 闭环 |
| retirement 三件套 | 配置态 V=15 | retireVersion 340–359 ns / assertAssignable 130–136 ns / isRetired 17–25 ns | 幂等 + 拒退 active（fail-closed）+ retired 上抛 |
| `PromotionService` | begin+commit 两相（K=40） | 42.6–44.7 µs | intentId==intentIdFor + 与一步 promote 的确定性孪生快照语义相等 |
| `reconstructPromotion` | L=11 / 4096 | 95–107 ns / 4.8 µs | 最后 promoted 项字段相等（两种 L） |
| `RollbackLog` | append / list(2) / list(4096) / restore(4096) | 97–191 ns / 16–28 ns / 1.4–1.7 µs / 405–549 µs | last() 最新项 + list() 防御拷贝 + restore 全量在册 |

判读：全部格在生产现实形态落 ns–µs；ms 级只出现在幻想剂量
（n=512 帕累托点、E=512 证据、L=4096 台账——生产形态分别是个位数、
个位数、L=11）。W=8 的 observe 首窗数字（39.7–49.1 µs）是冷摊
JIT，稳态由 W=512 摊薄给出（2.8–3.0 µs）——两读法都离验收线三个
数量级以上。

### 2c. 微助手单价表（S1b：31 格 + 快照解析；其余全部值导出）

前二（唯二 >1 µs）：`assertSingleResourceBoundary`（9.2 KB 策略）
15.3–16.4 µs（体内 JSON.parse 探针，S12-D-1 邻域只作见证不提变体）、
`assertRoutingPolicyEvalReport`（整门链）12.4–12.5 µs——两者每次
promote 各走一次，**已内含**在 S0d promote op 锚（41.2–45.1 µs）
里。第三名 `adaptationRegistryPath` 437–451 ns；其余 28 格全部
≤ 451 ns（`casActivePointer` 命中/`assertExpectedActive`/
`resourceIdentityKey`/`identityEquals`/`scopeEquals`/门谓词/
`candidateError`/`isCandidate`/`validateCandidate`/
`validateChangeNote`/`validatePromotionReview`/
`assertExplicitApprovalActor`/`parsePromotionReview`/
`parseRoutingEvalReport`/`parseRollbackLedgerEntry`/
`validateRollbackInput`/approval-profile 五件/split 三件/常量普查
等，逐格配语义见证）。`parseRegistrySnapshot`（146 KB 配置态快照
对象）36.2–37.7 µs——S11-D-2 装载链既裁域的同带复锚。
`rollbackActive` 薄包装在 S0d rollback op（3.0–3.3 µs）内定价。

### 2d. 10 ms 越线求解（S1c）

| 格 | 单价 | 到 10 ms 需要 | 生产调用数 |
| --- | --- | --- | --- |
| `paretoFront(n=512)`（普查最贵休眠格，暖测） | 441–450 µs | **22–23 次调用/命令** | **0**（休眠） |
| `evaluateProposalShadow(P=2000/A=1000)` | 123.4–125.9 ms | 1 次 | 0（休眠 + experiments 属地 + 幻想剂量） |
| 其余全部普查格 | ≤ 4.2 ms（E=512 幻想态）| ≥ 3 次幻想剂量调用 | 0 或 R13-D 相位账目内的有界常数 |

**普查收口**：休眠面不存在「藏着重体的导出」——最贵格到验收线
需要的调用频率（22+/命令）与实际频率（0）之间隔着无穷比；传递件
单价 ns 级且每命令次数有界（R13-D 账目）。

### 2e. 天花板裁决（频率 × 单价 × 属地三端封死）

- **频率端**：54 个休眠导出生产流量恒 0（首手 grep，二十二轮
  一致）；12 个生产件每命令调用次数被 R13-D 21 相位账目钉为有界
  常数；唯一会放大调用数的机制（auto-loop 提议）被 approval
  profile 预算与 proposal-first 门约束为每 run 至多一次。
- **单价端**：普查全部格生产现实形态 ns–µs；幻想剂量下最贵休眠格
  仍 <5 ms（除 experiments 属地的 shadow 格）；配置态 e2e 地板
  3.73–3.76 ms 由字节与 fsync 支配（历轮已裁）。
- **属地端**：每条消除思路撞死：休眠模块算法「优化」（pareto
  O(n log n)、proposeCandidates 提前 cap、reconstructPromotion 尾读）
  = 死代码抛光（零流量零收益）；边界闸/门链/验证循环弱化 =
  fail-closed 家族 fail-open 化（S12-D-1 邻域明文冻结）；装载链
  合并 = S11-D-2 重铸；台账/候选扫描 = S1-D-2/S1-D-8 族重铸；
  monitor 无界数组「修复」= 休眠合同载体行为变更 + 零性能收益。
  **三端封死，无新缝隙。**

## 3. 候选总表

**空。** 本轮真实新增候选为零：普查轴的每一处成本都钉在零流量
（54 休眠件）、ns–µs 单价（31 微格 + 八组模块操作）、既有锚内含
（门链两格）、既裁域（S11-D-2 装载、S1-D-2/8 扫描族）或切片外属地
（shadow 格）上（§2 逐格归因）；逐行重读在二十一遍裁决之外未发现
任何未点名站点（§4 换名检查）。按 PLAN「先复核再找新角度，不要
硬凑」与指令「宁可空枚举也不铸装饰性 ID」，不铸任何 S22-D-\*。

## 4. 枚举路径：识别但按更名/重现规则不铸 ID 的角度（8 项新记录）

R13-D §4 11 项、R14-D…R21-D §4 各 8 项换名检查未重铸（未以任何
形态重提）。本轮逐文件重读与普查轴中新识别、判为既有 ID 属地/
死代码抛光/明文拒绝类、**未铸 ID** 的角度：

| 本轮识别的落点 | 命中的闭合条目 | 处置 |
| --- | --- | --- |
| `paretoFront` O(n²) 支配扫描换 2 目标扫掠/分治（n=512 幻想态 441–450 µs） | 零生产调用方 = **死代码抛光**；生产 n 由 maxCandidatesPerEpoch 约束为个位数（该形态 1.9 µs） | 不铸 ID |
| `proposeCandidates` 逐证据 hashCandidateContent + 未 cap 先扫全量（E=512 幻想态 4.1–4.2 ms） | 零生产调用方；概念消费路径（auto-loop）被 profile 预算 + 每 run 一次提议钉死；E 生产形态个位数 | 不铸 ID |
| monitor `observations` 无界增长（snapshot/restore O(累计)，observe 稳态 O(8)） | 零生产调用方 + 「修复」= 休眠合同载体行为变更、零性能收益；记录为普查新钉事实（§2b） | 不铸 ID（事实归档） |
| `RollbackLog.restore` 逐项 `parseRollbackLedgerEntry` 验证循环（L=4096 幻想态 405–549 µs） | fail-closed 验证弱化 = **fail-open 家族**（明文冻结）；生产 L=2 | 死路 |
| `reconstructPromotion` 全台账正向扫描换尾向早停（L=4096 幻想态 4.8 µs） | 零生产调用方 + **S1-D-8 族** ns/µs 类 | 不铸 ID |
| `assertSingleResourceBoundary` 的 JSON.parse 探针（15.3–16.4 µs/次）换廉价预探针 | **S12-D-1 邻域**（廉价探针形态明文冻结）；且已内含在 promote op 锚内，非新增成本 | 死路 |
| `assertRoutingPolicyEvalReport` 整门重验（12.4–12.5 µs/次）折叠 | 门链完整性 = proposal-first 硬不变量属地；once-per-promote µs 类 | 死路 |
| `parseRegistrySnapshot` 装载重验（146 KB 形态 36–38 µs）与 load 合并 | **S11-D-2 重铸**（装载链融合已裁） | 不铸 ID |

逐行重读中再次识别（前轮已有属地，本轮零新增维度，仅记录复核）：
`assertNoForbiddenFields` 的 `key.toLowerCase()` = S9-D-4/S12-D-1
禁区零触碰；`saveAdaptationRegistry` 临时名 pid+UUID = S7-D-3 已裁；
每写 mkdir = S10-D-1 已裁；`preparePromotion` pendingByIntent 线性
扫描 = S1-D-8 族 ns 类。以上之外，导出 × 消除机制矩阵的剩余格全部
为空。

## 5. 更名/重现检查（指令点名项逐条）

| 点名项 | 本轮是否触碰 | 检查结论 |
| --- | --- | --- |
| S9-D-4 廉价 `toLowerCase` 跳过（U+212A fail-open） | 否 | 未重提任何折叠变体；站点首手复核仍为全量 `key.toLowerCase()`，零 diff 字节恒等（永不重开） |
| S12-D-1 边界闸 JSON.parse 预探针（廉价形态同族） | 否 | 普查只把 `assertSingleResourceBoundary` 现价记档（15.3–16.4 µs），未提任何探针变体（§4-6 判死路） |
| S12-B-2（eval-routing 传 `learned` only） | 否（未触发/未强落） | L353-354 首手复核：`assignTasks({ catalog, tasks, learned })`，无 `prior` 字段 |
| R21-D 状态增长方向面（c=0.497–0.519 ns/V²；~4.4k 版本越线；生产深度 2–3） | 否（不重铸/不重测） | 本轴**零老化驱动、零深度扫掠**：全部注册表夹具为 V=15 配置态或单 promote 孪生；S1a 剂量是休眠导出的调用次数/输入尺寸，不动状态增长方向；越线数字原样引用 |
| R20-D 多进程并发面（竞争税 6.4–12.4 ms/cmd；护航 74.8–75.6 ms；无锁读者 0/240） | 否（不重铸/不重测） | 本轴全程单进程、零锁计时；未 spawn 任何持有者/写风暴子进程 |
| R19-D 冷进程模块图（boot+import 层） | 否（不重铸） | 零子进程、零 import 计时；仿真单进程内完成 |
| R22-B 标识符几何（G×T，实测平坦） | 未移植 | 本轴无任何 id 长度自变量：全部夹具 id 为定长顺序串（`sequentialIds`）；普查以导出为格，不以 id 几何为格 |
| R21-E IDG（learning 切片标识符几何） | 未移植 | 同上；无一数字来自其方法 |
| R18-D 锁事务账目 / R17-D 拒绝路径 / R16-D 形态五轴 / R15-D 分配/GC+MDE / R14-D 规模越线 / R13-D 21 相位 | 否（不重铸/不重测） | 全部原样引用；本轴 op 锚（41.2–45.1 µs）是既有矩阵格同带复锚；R13-D 相位账目仅作频率端引用 |
| FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN | 否 | 其他切片轴名，未以任何形态重提 |
| S8-D-1 / S8-D-2 完整性重哈希 | 否 | load/配置态锚原样含重哈希；未提折叠 |
| S8-D-3 saveAdaptationRegistry drop-fsync | 否 | 全部 save 锚含 fsync；未提折叠 |
| S5-D-4 全 UNOBSERVED 前置短路 / S2-D-4 / S10-D-2 屏障 | 否 | eval 链未重提；本轴不触 eval 内部结构 |
| S1-D-7 / S7-D-1 promote 门链 | 否 | promote op 锚即门链按规格工作的价格（hash32×3 内含）；门链两微格只记现价（§2c） |
| S11-D-2 装载链融合 / S5-D-3 快照 | 否 | `parseRegistrySnapshot` 现价记档为同带复锚，未重提融合（§4-8） |
| S1-D-1 / S2-D-1 / S1-D-2 / S3-D-1 结构拷贝族 | 否 | 未重提任何可变 push/批量重建/索引/拷贝消除；R21-D 重开阈值曲线原样引用 |
| R10-D…R21-D 空枚举收口 | 否 | 未回填任何 S13-D-\* … S21-D-\* |
| X\* / S1-\* … S21-\* / S22-A/B 全表 | 否 | 逐文件重读对表复核，零重开 |

## 6. 前后对比

无。零生产改动；切片二十二遍逐字节不变（`82bef36 == 5590443` on
`src/adaptation/`）。本分支仅含本报告一个文件。

## 7. 测试与门禁

零代码改动下全量套件与门禁复核，全绿（Node v22.22.2，pnpm 10.17.1）：

```bash
pnpm test   # tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
pnpm gate   # typecheck && lint && test && build — 全绿（exit 0）
```

仿真（临时脚本 `/tmp/r22d-sim.mts`，未入库——无赢家不落死代码；完整
源码见附录，seeds `0xd22d01`–`0xd22d02` 起，三次独立运行全部
`All checks passed.`，暴力帕累托/变异后置/去重/漂移/幂等/孪生/CAS
等见证逐项通过）。诚实记录：脚本首版的 CAS 见证夹具误用了配置态
routing-policy 身份（构造器从不 promote 该候选，active==baseline，
「陈旧 CAS」分支永假），首次运行即被见证捕获 FAIL；修正为已 promote
的 prompt 身份（v2≠v1 真实换向）并加入未知键 fail-closed 分支后
三运行全绿——失败在夹具，不在生产代码，生产 CAS 语义经真实换向
三分支见证成立。

```text
run 1（摘录）:
S0c configured: load=0.57 save=1.82 | eval e2e=3.76ms
S0d: promote op=41.8us rollback op=3.1us hash32=10.1us
S1a-1 pareto: n=512 -> 1190.1us (front=104) | S1a-3 propose: E=512 -> 4062.8us (410)
S1a-6 svc=43.9us | S1a-8 restore(4096)=411.9us
S1b top: boundary=15273ns gate=12421ns | 其余<=447ns | parseSnap=37.3us
S1c: paretoFront(n=512, 441us) -> 23 calls/command | shadow P=2000 属 experiments

run 2（摘录）:
S0c: load=0.60 save=1.25 | eval=3.73ms
S0d: 45.1us / 3.3us / 9.7us
S1a-1 n=512 -> 1197.8us | S1a-3 E=512 -> 4128.7us
S1a-6 svc=42.7us | S1a-8 restore(4096)=404.7us
S1b top: boundary=16196ns gate=12412ns | 其余<=437ns | parseSnap=37.7us
S1c: paretoFront(443us) -> 23 calls/command

run 3（摘录）:
S0c: load=0.59 save=1.59 | eval=3.74ms
S0d: 41.2us / 3.0us / 9.6us
S1a-1 n=512 -> 1248.8us | S1a-3 E=512 -> 4188.1us
S1a-6 svc=44.7us | S1a-8 restore(4096)=531.8us
S1b top: boundary=16396ns gate=12467ns | 其余<=451ns | parseSnap=37.7us
S1c: paretoFront(450us) -> 22 calls/command
```

## 8. 新增排除 ID

**无。** 空枚举（与 R13-D … R21-D 同口径）：本轮识别的全部角度均为
死代码抛光（休眠件零流量）、既有 ID 属地（S11-D-2 / S1-D-2 /
S1-D-8 / S12-D-1 邻域）、fail-open 冻结家族或零收益装饰面（§4），
不铸装饰性 ID。

重开条件：导出流普查仅在三种情形下需要重测——(a) 任一休眠导出
获得生产调用方（普查单价表即时可用：以该调用频率乘 §2b/§2c 单价
对照 10 ms 线；若 `paretoFront`/`proposeCandidates` 以每命令 20+
次频率接入则首先重开本轴）；(b) monitor 被接入长驻进程（无界
`observations` 数组从零流量事实升格为内存正确性问题——按正确性
而非性能重开，见 §4-3）；(c) 导出面结构变更（新增导出、再导出
别名变化或 promotion.ts 门链重排使 12/54 分拣失锚）。成功路径的
既有重开阈值不变：E 跨量级增长时首先重开 S6-D-5（R14-D 裁定）；
P-eval ≈ 249–259 KB、O ≈ 2,646–2,733 字符、H ≈ 4,486–4,684 字符、
P-promote ≈ 2,615–2,698 KB（R16-D 裁定）；拒绝路径两端封死
（R17-D 裁定）；锁事务账目无隐藏段（R18-D 裁定）；冷进程模块图三
情形（R19-D 裁定）；并发面三情形（R20-D 裁定）；状态增长方向面三
情形（R21-D 裁定：同身份 ~500 次批准 promote / addVersion-restore
结构变更 / parseLedgerEntry 键序变更）；L/σ 轴 schema/构造封闭
不变。

## 附录：确定性仿真脚本（完整，可复现）

临时路径 `/tmp/r22d-sim.mts`，`npx tsx /tmp/r22d-sim.mts` 运行
（仓库根目录，依赖已装，Node ≥22.19）；未入库（无赢家）。seeds
`0xd22d01`–`0xd22d02` 起；三次独立运行全部 `All checks passed.`。

```typescript
/**
 * R22-D deterministic anchor + public-export flow-census simulation
 * (twenty-second pass).
 * (1) Re-anchors the configured-state x command-class matrix closed by
 *     R9-D..R21-D on THIS VM (engines-compliant Node >=22.19.0, v22.22.2 --
 *     same engine as R13-D..R21-D so all bands are directly comparable).
 * (2) NEW AXIS: the public-export flow census. Twenty-one passes priced this
 *     slice by command (R13-D phases), scale (R14-D), engine state (R15-D),
 *     payload shape (R16-D), rejection terminal (R17-D), lock transaction
 *     (R18-D), process boundary (R19-D), process coexistence (R20-D), and
 *     state-growth direction (R21-D) -- and re-verified every round that the
 *     five dormant modules (monitor/pareto/mutate/retirement/reflection)
 *     have zero production callers. But the EXPORT surface itself has never
 *     been enumerated and priced as a census: the slice ships 66 distinct
 *     value exports (+7 re-export aliases in promotion.ts), of which exactly
 *     12 bindings are production-imported (first-hand grep: cli/adapt.ts,
 *     learning/auto-loop.ts, learning/from-episode.ts,
 *     learning/learned-routing.ts; tracking/analysis.ts is type-only), the
 *     rest being slice-internal transitives or test-only contract carriers.
 *     A dormant export that hides a heavy body would be a latent cost bomb
 *     the moment any caller appears -- this axis prices every dormant/
 *     transitive export at production-realistic shape plus a dose grid so
 *     the census can close with a measured ceiling instead of a caller
 *     count. Sections:
 *       1a: dormant module operations at production shape + dose grid --
 *           paretoFront (n in {8,64,512}, brute-force front witness),
 *           mutateOnce (all three mutation kinds on a ~4.6KB prompt body,
 *           semantic postcondition witnesses), proposeCandidates
 *           (E in {8,64,512} evidence with duplicate + self-supported mix;
 *           cap/dedup/rejection witnesses), createAdaptationDriftMonitor
 *           (W in {8,64,512} observes at default windowSize=8; drift +
 *           no-drift witnesses; snapshot/restore), retirement trio on the
 *           configured registry (idempotence + active-retire refusal +
 *           assertAssignable throw witnesses), PromotionService
 *           begin/commit vs promoteWithRegistry twin (semantic-equality
 *           witness), reconstructPromotion (L in {11, 4096}; last-promoted
 *           witness), RollbackLog standalone ops (L in {2, 4096}).
 *       1b: micro-helper unit-price table (every remaining value export:
 *           identity/scope/key helpers, gate predicates, parse/validate
 *           heads, approval-profile heads, split helpers) with per-call
 *           witnesses.
 *       1c: 10 ms crossing solve -- calls-per-command needed for the
 *           priciest census cells to reach 10 ms, against production call
 *           frequency (zero for dormant exports).
 *     Frequency end: dormant exports have zero production flow by grep;
 *     transitive helpers run a bounded number of times per command (bounded
 *     by the R13-D phase account). Unit-price end: every cell must land in
 *     ns..us at production shape for the census to close. Property end:
 *     every elimination idea on these sites is already excluded
 *     (S1-D-3/6/8, S2-D-5, S3-D-5, S6-D-1, S9-D-3 families) -- the census
 *     prices the surface, it does not reopen the tickets.
 * Seeded PRNG (mulberry32) -> reproducible. Seeds: 0xd22d01 - 0xd22d02.
 * Run: npx tsx /tmp/r22d-sim.mts   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import { mkdir, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { ResourceRegistry } from "/workspace/src/adaptation/registry.js";
import {
  PromotionService,
  adaptationRegistryPath,
  loadAdaptationRegistry,
  loadAdaptationRegistryOrNew,
  parsePromotionReview,
  parseRegistrySnapshot,
  promoteWithRegistry,
  reconstructPromotion,
  saveAdaptationRegistry,
  type ChangeNote,
  type PromoteInput,
  type PromotionLedgerEntry,
  type PromotionReview
} from "/workspace/src/adaptation/promotion.js";
import {
  assertExplicitApprovalActor,
  assertRoutingPolicyEvalReport,
  intentIdFor,
  isIntentId,
  isPromotableStatus,
  validateChangeNote,
  validatePromotionReview
} from "/workspace/src/adaptation/promotion-rules.js";
import {
  evalRoutingPolicy,
  parseRoutingEvalReport,
  ROUTING_EVALUATOR_VERSION
} from "/workspace/src/adaptation/eval-routing.js";
import {
  EVIDENCE_BOUNDARIES,
  assertPromotableFromSupport,
  assertSplitSeparation,
  assignEvaluationSplit,
  evaluateProposalShadow,
  proposeCandidates,
  type OptimizerEvidence,
  type OptimizerInput
} from "/workspace/src/adaptation/reflection.js";
import {
  CANDIDATE_STATUSES,
  autoPromotableFor,
  assertAcyclicLineage,
  assertSingleResourceBoundary,
  candidateError,
  hashCandidateContent,
  isCandidate,
  validateCandidate,
  type EvaluationPlan,
  type ImprovementCandidate
} from "/workspace/src/adaptation/candidate.js";
import {
  ADAPTATION_WHEN,
  HIGH_RISK_TASK_FAMILIES,
  LOW_RISK_TASK_FAMILIES,
  MUTATABLE_KINDS,
  PARAMETERIZATION_LEVEL,
  isMutatableKind,
  mutateOnce,
  type MutationSpec
} from "/workspace/src/adaptation/mutate.js";
import { paretoFront, type CandidateMetrics } from "/workspace/src/adaptation/pareto.js";
import {
  createAdaptationDriftMonitor,
  type DriftObservation
} from "/workspace/src/adaptation/monitor.js";
import {
  assertAssignable,
  isRetired,
  retireVersion
} from "/workspace/src/adaptation/retirement.js";
import {
  ROLLBACK_LEDGER_KINDS,
  ROLLBACK_REASONS,
  RollbackLog,
  parseRollbackLedgerEntry,
  rollbackActive,
  validateRollbackInput,
  type RollbackInput,
  type RollbackLedgerEntry
} from "/workspace/src/adaptation/rollback.js";
import {
  NON_AUTO_PROMOTABLE_KINDS,
  RESOURCE_KINDS,
  identityEquals,
  isNonAutoPromotableKind,
  scopeEquals,
  type AuthorIdentity,
  type ResourceIdentity,
  type ResourceVersion
} from "/workspace/src/adaptation/resource.js";
import {
  assertExpectedActive,
  casActivePointer,
  resourceIdentityKey
} from "/workspace/src/adaptation/active-pointer.js";
import {
  canAutoPromote,
  createDefaultApprovalProfile,
  createInstalledAutoAdaptProfile,
  isAutoAdaptEnabled,
  validateApprovalProfile
} from "/workspace/src/adaptation/approval-profile.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import {
  createProjectId,
  type CandidateId,
  type IdGenerator,
  type ResourceVersionId
} from "/workspace/src/domain/ids.js";
import type { IsoTimestamp } from "/workspace/src/domain/timestamp.js";
import type { ExperimentPlan } from "/workspace/src/experiments/plan.js";
import type { ExperimentOutcome } from "/workspace/src/experiments/shadow.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

/** Recursively sort object keys so JSON compare ignores key order. */
function canonicalJson(value: unknown): string {
  return JSON.stringify(value, (_key, val: unknown) => {
    if (val !== null && typeof val === "object" && !Array.isArray(val)) {
      const sorted: Record<string, unknown> = {};
      for (const k of Object.keys(val as Record<string, unknown>).sort()) {
        sorted[k] = (val as Record<string, unknown>)[k];
      }
      return sorted;
    }
    return val;
  });
}

const NOW = "2026-08-25T12:00:00.000Z" as IsoTimestamp;
const HUMAN: AuthorIdentity = { kind: "human", identity: "operator" };
const EVAL_PLAN: EvaluationPlan = { stages: ["static", "replay"], metrics: ["utility"], planVersion: 1 };

function sequentialIds(prefix: string): IdGenerator {
  let n = 0;
  return () => {
    n += 1;
    return `${prefix}${String(n).padStart(4, "0")}`;
  };
}

const BASELINE_POLICY = JSON.stringify({ primaryModelId: "premium", avoid: [], prefer: [] });
const CANDIDATE_POLICY = JSON.stringify({
  primaryModelId: "premium",
  avoid: [{ modelId: "fast", family: "edit", reason: "deterministic FAIL on edit replay" }],
  prefer: [{ family: "test", modelId: "fast" }]
});

/* Configured-state policies (R7-I lesson; bit-identical generator to
 * R8-D..R21-D at k=1). */
function configuredPolicy(variantTag: string, k = 1): string {
  const avoid = Array.from({ length: 40 * k }, (_, i) => ({
    modelId: i % 8 === 0 ? "cheap" : `m${i % 7}`,
    family: i % 3 === 0 ? pick(() => (i % 10) / 10, ["edit", "test", "review"]) : `fam${i % 12}`,
    reason: `deterministic FAIL pattern ${i} (${variantTag}): attributable replay regression observed across paired episodes with stable family-level attribution`
  }));
  const prefer = Array.from({ length: 40 * k }, (_, i) => ({
    family: i % 4 === 0 ? "test" : `fam${i % 12}`,
    modelId: i % 4 === 0 ? "cheap" : `m${(i + 1) % 7}`
  }));
  const assignments = Array.from({ length: 7 }, (_, i) => ({
    role: ["planner", "executor", "tester", "reviewer", "scout", "debugger", "worker"][i] as string,
    model: i % 2 === 0 ? "premium" : "cheap",
    family: ["edit", "test", "review"][i % 3] as string
  }));
  if (variantTag === "candidate") {
    avoid.push({ modelId: "cheap", family: "refactor", reason: "candidate-only: deterministic FAIL on refactor replay (new attribution)" });
  }
  return JSON.stringify({ primaryModelId: "premium", avoid, prefer, assignments });
}
const CONFIGURED_BASELINE_POLICY = configuredPolicy("baseline");
const CONFIGURED_CANDIDATE_POLICY = configuredPolicy("candidate");

const ROLES = ["worker", "scout", "planner", "implementer", "reviewer", "tester", "debugger"] as const;
const OBJECTIVES = [
  "Fix the failing unit test in the adapter and rerun the suite",
  "Refactor the retry helper to remove duplicated backoff logic",
  "Review the migration PR for schema drift and unsafe defaults",
  "Investigate why the nightly benchmark regressed on large inputs",
  "Plan the rollout of the new caching layer across services"
];

function mkReview(candidateId: CandidateId, contentHash: string, tag: string): PromotionReview {
  return {
    reviewId: `rv-${tag}`,
    candidateId,
    contentHash,
    verdict: "approved",
    reviewerKind: "independent",
    reviewerId: "critic-gate",
    actorId: HUMAN.identity,
    evidenceRefs: [`review:${tag}`]
  };
}

function mkNote(scope: string, rollbackVersionId: ResourceVersionId): ChangeNote {
  return {
    scope,
    evidence: ["static"],
    guardrails: ["proposal-first"],
    rollbackVersionId
  };
}

function buildRoutingRegistry(
  tag: string,
  baselinePolicy = BASELINE_POLICY,
  candidatePolicy = CANDIDATE_POLICY
): { registry: ResourceRegistry; candidateId: CandidateId; baseline: ResourceVersion; identity: ResourceIdentity } {
  const registry = new ResourceRegistry({ now: () => NOW, generateId: sequentialIds(tag) });
  const identity: ResourceIdentity = {
    kind: "routing-policy",
    name: "learned-routing",
    scope: { kind: "project", projectId: createProjectId(() => `${tag}proj`) }
  };
  const baseline = registry.registerBaseline({ identity, content: baselinePolicy, author: HUMAN });
  const candidate = registry.createCandidate({
    identity,
    content: candidatePolicy,
    parentVersionId: baseline.versionId,
    author: HUMAN,
    evaluationPlan: EVAL_PLAN
  });
  return { registry, candidateId: candidate.candidateId, baseline, identity };
}

/* Configured-state registry (same construction as R8-D..R21-D). */
function buildConfiguredRegistry(
  tag: string,
  baselinePolicy = CONFIGURED_BASELINE_POLICY,
  candidatePolicy = CONFIGURED_CANDIDATE_POLICY
): {
  registry: ResourceRegistry;
  candidateId: CandidateId;
  baseline: ResourceVersion;
  identity: ResourceIdentity;
  prompts: { identity: ResourceIdentity; v1: ResourceVersion; candidate: ImprovementCandidate; body: string }[];
} {
  const built = buildRoutingRegistry(tag, baselinePolicy, candidatePolicy);
  const { registry } = built;
  const versions: {
    identity: ResourceIdentity;
    v1: ResourceVersion;
    v2?: ResourceVersion;
    candidate: ImprovementCandidate;
    body: string;
  }[] = [];
  for (let i = 0; i < 8; i += 1) {
    const identity: ResourceIdentity = {
      kind: "prompt",
      name: `agent-prompt-${i}`,
      scope: { kind: "project", projectId: createProjectId(() => `${tag}proj`) }
    };
    const body = `# prompt ${i}\n${`Follow the review checklist, cite evidence for every claim, and keep the diff bounded to the stated scope (${i}).\n`.repeat(30 + i * 8)}`;
    const v1 = registry.registerBaseline({ identity, content: body, author: HUMAN });
    const candidate = registry.createCandidate({
      identity,
      content: `${body}\n## amendment ${i}\nAdd an explicit check for the attributable failure.\n`,
      parentVersionId: v1.versionId,
      author: HUMAN,
      evaluationPlan: EVAL_PLAN
    });
    const entry: {
      identity: ResourceIdentity;
      v1: ResourceVersion;
      v2?: ResourceVersion;
      candidate: ImprovementCandidate;
      body: string;
    } = { identity, v1, candidate, body };
    if (i < 5) {
      const result = promoteWithRegistry(registry, {
        candidateId: candidate.candidateId,
        expectedCurrentVersionId: v1.versionId,
        content: `${body}\n## amendment ${i}\nAdd an explicit check for the attributable failure.\n`,
        approvedBy: HUMAN,
        review: mkReview(candidate.candidateId, candidate.contentHash, `${tag}p${i}`),
        changeNote: mkNote(`prompt:${tag}${i}`, v1.versionId),
        explicitApproval: true
      });
      entry.v2 = result.newVersion as ResourceVersion;
    } else if (i === 5) {
      registry.beginPromotion({
        candidateId: candidate.candidateId,
        expectedCurrentVersionId: v1.versionId,
        content: `${body}\n## amendment ${i}\nAdd an explicit check for the attributable failure.\n`,
        approvedBy: HUMAN,
        review: mkReview(candidate.candidateId, candidate.contentHash, `${tag}p${i}`),
        changeNote: mkNote(`prompt:${tag}${i}`, v1.versionId),
        explicitApproval: true
      });
    }
    versions.push(entry);
  }
  const first = versions[0] as (typeof versions)[number];
  registry.rollback({
    identity: first.identity,
    expectedCurrentVersionId: (first.v2 as ResourceVersion).versionId,
    targetVersionId: first.v1.versionId,
    reason: "degradation",
    evidence: ["ev:configured-degradation"],
    automatic: false
  });
  const second = versions[1] as (typeof versions)[number];
  registry.rollback({
    identity: second.identity,
    expectedCurrentVersionId: (second.v2 as ResourceVersion).versionId,
    targetVersionId: second.v1.versionId,
    reason: "user",
    evidence: ["ev:configured-user"],
    automatic: false
  });
  registry.retire((versions[2] as { v1: ResourceVersion }).v1.versionId);
  registry.retire((versions[3] as { v1: ResourceVersion }).v1.versionId);
  return {
    ...built,
    prompts: versions.map(({ identity, v1, candidate, body }) => ({ identity, v1, candidate, body }))
  };
}

async function writeDataset(dir: string, episodes: number, rng: () => number): Promise<void> {
  await mkdir(dir, { recursive: true });
  const eps = Array.from({ length: episodes }, (_, i) => ({
    episodeHash: `eh_${i}`,
    taskId: `tsk_e${i}`,
    role: pick(rng, ROLES),
    objective: pick(rng, OBJECTIVES),
    originalWorkspace: "/repos/alpha",
    ...(rng() < 0.7 ? { taskSuccess: rng() < 0.5 ? "PASS" : "FAIL" } : {}),
    ...(rng() < 0.5 ? { taskFamily: pick(rng, ["edit", "test", "review"]) } : {})
  }));
  const manifest = { datasetId: "ds-r22d", environmentVersion: "env-1", episodes: eps };
  await writeFile(join(dir, "manifest.json"), `${JSON.stringify(manifest, null, 2)}\n`, "utf8");
}

console.log(`node ${process.version}`);

/* ============================================================
 * Section 0: whole-slice budget re-verification, default-state anchors.
 * ============================================================ */
const workRoot = await mkdtemp(join(tmpdir(), "r22d-sim-"));
let configuredEvalReport: ReturnType<typeof parseRoutingEvalReport> | undefined;
{
  const rng = mulberry32(0xd22d01);
  const stateRoot = join(workRoot, "state");
  const { registry } = buildRoutingRegistry("b0");
  await saveAdaptationRegistry(stateRoot, registry);
  const datasetDir = join(workRoot, "dataset");
  await writeDataset(datasetDir, 200, rng);

  const loadMs = await benchAsync(async () => {
    await loadAdaptationRegistry(stateRoot);
  }, 30);
  const saveMs = await benchAsync(async () => {
    await saveAdaptationRegistry(stateRoot, registry);
  }, 30);

  const { registry: r2, candidateId } = buildRoutingRegistry("b1");
  await saveAdaptationRegistry(stateRoot, r2);
  const evalMs = await benchAsync(async () => {
    await evalRoutingPolicy({ stateRoot, candidateId, datasetDir });
  }, 20);
  console.log(
    `S0 default anchors: registry load=${loadMs.toFixed(2)}ms save(+fsync)=${saveMs.toFixed(2)}ms | adapt-eval end-to-end (E=200, toy policy)=${evalMs.toFixed(2)}ms per invocation`
  );
}

/* ============================================================
 * Section 0b: evaluateProposalShadow cross-check (experiments-slice owned).
 * ============================================================ */
{
  const mkPlan = (p: number, a: number): ExperimentPlan => ({
    planVersion: 1,
    experimentId: "exp_r22d-shadow",
    mode: "shadow",
    baselineVersionId: "rsv_r22d0001" as ResourceVersionId,
    candidateId: "cnd_r22d0002" as CandidateId,
    population: Array.from({ length: p }, (_, i) => `eh_${i}`),
    metrics: ["utility"],
    thresholds: { maxGuardrailBreaches: 1000000, maxCostUsd: 1e9 },
    budget: { maxAssignments: a + 1, maxWallClockMs: 1000000000 },
    randomization: { seed: 7 },
    stopPolicy: { onGuardrail: "halt", onBudgetExhausted: "halt" },
    missingOutcomePolicy: "exclude"
  });
  const mkOutcomes = (a: number): ExperimentOutcome[] =>
    Array.from({ length: a }, (_, i) => ({
      episodeHash: `eh_${i}`,
      utility: i % 2,
      costUsd: 0.001,
      guardrailBreached: false
    }));
  for (const [p, a, reps] of [
    [200, 100, 20],
    [2000, 1000, 3]
  ] as const) {
    const plan = mkPlan(p, a);
    const outcomes = mkOutcomes(a);
    const state = evaluateProposalShadow(plan, outcomes, 0);
    check(`S0b shadow run completes without halt (P=${p},A=${a})`, !state.halted && state.outcomes.length === a);
    const ms = bench(() => {
      evaluateProposalShadow(plan, outcomes, 0);
    }, reps);
    console.log(
      `S0b evaluateProposalShadow cross-check: P=${p} A=${a} -> ${ms.toFixed(1)}ms per call (experiments-slice owned; anchor only)`
    );
  }
}

/* ============================================================
 * Section 0c: configured-state anchors (R8-D..R21-D replica on this VM).
 * ============================================================ */
let configuredSnapshotJson = "";
{
  const rng = mulberry32(0xd22d02);
  const stateRoot = join(workRoot, "state-configured");
  const { registry, candidateId } = buildConfiguredRegistry("c0");
  await saveAdaptationRegistry(stateRoot, registry);
  const snapshot = registry.snapshot();
  const contents = snapshot.contents ?? [];
  const totalBlobBytes = contents.reduce((sum, blob) => sum + blob.content.length, 0);
  const datasetDir = join(workRoot, "dataset-configured");
  await writeDataset(datasetDir, 200, rng);

  const cfgLoadMs = await benchAsync(async () => {
    await loadAdaptationRegistry(stateRoot);
  }, 30);
  const cfgSaveMs = await benchAsync(async () => {
    await saveAdaptationRegistry(stateRoot, registry);
  }, 30);
  const cfgEvalMs = await benchAsync(async () => {
    await evalRoutingPolicy({ stateRoot, candidateId, datasetDir });
  }, 20);
  const result = await evalRoutingPolicy({ stateRoot, candidateId, datasetDir });
  configuredEvalReport = parseRoutingEvalReport(result.report);
  const raw = await readFile(adaptationRegistryPath(stateRoot), "utf8");
  configuredSnapshotJson = raw;
  console.log(
    `S0c configured anchors (registry.json=${(raw.length / 1024).toFixed(1)}KB, V=${snapshot.versions.length} C=${snapshot.candidates.length} L=${snapshot.ledger.length} B=${contents.length} blobs=${(totalBlobBytes / 1024).toFixed(1)}KB, policy=${(CONFIGURED_CANDIDATE_POLICY.length / 1024).toFixed(1)}KB): load=${cfgLoadMs.toFixed(2)}ms save(+fsync)=${cfgSaveMs.toFixed(2)}ms | adapt-eval end-to-end (E=200, configured policy)=${cfgEvalMs.toFixed(2)}ms per invocation`
  );
}

/* ============================================================
 * Section 0d: configured-state promote and rollback OP-ONLY costs.
 * ============================================================ */
{
  const report = configuredEvalReport;
  check("S0d setup: configured eval report available", report !== undefined);
  const K = 60;
  const promoteFixtures = Array.from({ length: K }, (_, i) => {
    const { registry, candidateId, baseline } = buildConfiguredRegistry(`pm${i}x`);
    const candidate = registry.getCandidate(candidateId) as ImprovementCandidate;
    const input: PromoteInput = {
      candidateId,
      expectedCurrentVersionId: baseline.versionId,
      content: CONFIGURED_CANDIDATE_POLICY,
      approvedBy: HUMAN,
      review: mkReview(candidateId, candidate.contentHash, `pm${i}`),
      changeNote: mkNote(`routing:pm${i}`, baseline.versionId),
      explicitApproval: true,
      evalReport: {
        ...(report as NonNullable<typeof report>),
        candidateId,
        contentHash: candidate.contentHash
      }
    };
    return { registry, input };
  });
  const t0 = performance.now();
  const promoted: { registry: ResourceRegistry; newVersion: ResourceVersion; identity: ResourceIdentity; baselineId: ResourceVersionId }[] = [];
  for (const fixture of promoteFixtures) {
    const result = promoteWithRegistry(fixture.registry, fixture.input);
    promoted.push({
      registry: fixture.registry,
      newVersion: result.newVersion as ResourceVersion,
      identity: (result.newVersion as ResourceVersion).identity,
      baselineId: fixture.input.expectedCurrentVersionId
    });
  }
  const promoteOpUs = ((performance.now() - t0) / K) * 1e3;
  check("S0d all configured promotes succeeded", promoted.length === K && promoted.every((p) => p.newVersion !== undefined));

  const t1 = performance.now();
  for (const p of promoted) {
    p.registry.rollback({
      identity: p.identity,
      expectedCurrentVersionId: p.newVersion.versionId,
      targetVersionId: p.baselineId,
      reason: "user",
      evidence: ["ev:r22d-op-anchor"],
      automatic: false
    });
  }
  const rollbackOpUs = ((performance.now() - t1) / K) * 1e3;
  const hashUs = bench(() => {
    hashCandidateContent(CONFIGURED_CANDIDATE_POLICY);
  }, 3000) * 1e3;
  console.log(
    `S0d configured op-only anchors (K=${K}): promote op=${promoteOpUs.toFixed(1)}us | rollback(user CAS) op=${rollbackOpUs.toFixed(1)}us | single hash32 over the ${(CONFIGURED_CANDIDATE_POLICY.length / 1024).toFixed(1)}KB policy=${hashUs.toFixed(1)}us (x3 on the promote gate chain)`
  );
}

/* ============================================================
 * Section 1 (NEW AXIS): public-export flow census.
 * Static census (first-hand grep, verified outside this script):
 *   66 distinct value exports; production-imported bindings = 12
 *   (evalRoutingPolicy, parseRoutingEvalReport, loadAdaptationRegistry,
 *    loadAdaptationRegistryOrNew, saveAdaptationRegistry,
 *    withAdaptationRegistryLock, promoteWithRegistry, parsePromotionReview,
 *    rollbackActive, ROLLBACK_REASONS, hashCandidateContent,
 *    isAutoAdaptEnabled); everything else slice-internal or test-only.
 * This section prices the dormant + transitive surface with witnesses.
 * ============================================================ */

/* --- 1a-1: paretoFront at n in {8, 64, 512} + brute-force witness. --- */
{
  const mkPoints = (n: number, seed: number): CandidateMetrics[] => {
    const rng = mulberry32(seed);
    return Array.from({ length: n }, (_, i) => ({
      candidateId: `cand-${String(i).padStart(4, "0")}`,
      quality: Math.round(rng() * 100) / 100,
      preferenceFit: Math.round(rng() * 100) / 100,
      costUsd: Math.round(rng() * 500) / 100,
      latencyMs: Math.round(rng() * 100000) / 100,
      risk: Math.round(rng() * 100) / 100
    }));
  };
  const dominatesRef = (a: CandidateMetrics, b: CandidateMetrics): boolean => {
    const geMax = a.quality >= b.quality && a.preferenceFit >= b.preferenceFit;
    const leMin = a.costUsd <= b.costUsd && a.latencyMs <= b.latencyMs && a.risk <= b.risk;
    const strict =
      a.quality > b.quality ||
      a.preferenceFit > b.preferenceFit ||
      a.costUsd < b.costUsd ||
      a.latencyMs < b.latencyMs ||
      a.risk < b.risk;
    return geMax && leMin && strict;
  };
  const parts: string[] = [];
  for (const [n, reps] of [
    [8, 4000],
    [64, 600],
    [512, 20]
  ] as const) {
    const points = mkPoints(n, 0xd22d10 + n);
    const front = paretoFront(points);
    // Brute-force witness: front = exactly the non-dominated set, sorted by id.
    const expected = points
      .filter((p, i) => !points.some((q, j) => j !== i && dominatesRef(q, p)))
      .map((p) => p.candidateId)
      .sort();
    check(
      `S1a-1 paretoFront n=${n}: front matches brute-force non-dominated set`,
      JSON.stringify(front.map((p) => p.candidateId)) === JSON.stringify(expected),
      `got ${front.length}, expected ${expected.length}`
    );
    const us = bench(() => {
      paretoFront(points);
    }, reps) * 1e3;
    parts.push(`n=${n} -> ${us.toFixed(1)}us (front=${front.length})`);
  }
  console.log(`S1a-1 paretoFront (dormant; O(n^2) dominance scan): ${parts.join(" | ")}`);
}

/* --- 1a-2: mutateOnce, all three mutation kinds, ~4.6KB prompt parent. --- */
{
  const parent = `# prompt r22d\n${"Follow the review checklist, cite evidence for every claim, and keep the diff bounded to the stated scope.\n".repeat(30)}\n## checks\nrun the full gate before landing\n\n## parameters\nmaxRetries: 3\ntimeoutSec: 45\n\n## notes\nkeep deltas bounded\n`;
  const parentVersionId = "rsv_r22dparent01" as ResourceVersionId;
  const base: Omit<MutationSpec, "instruction"> = {
    what: "prompt",
    when: ADAPTATION_WHEN,
    where: PARAMETERIZATION_LEVEL,
    parentContent: parent,
    parentVersionId
  };
  // append-instruction
  const appended = mutateOnce({ ...base, instruction: "Always cite the failing test name." });
  check(
    "S1a-2 append-instruction postcondition",
    appended.content === `${parent}\n\n[adaptation:prompt]\nAlways cite the failing test name.` &&
      appended.contentHash === hashCandidateContent(appended.content) &&
      appended.mutation === "append-instruction"
  );
  // replace-section
  const replacement = "## checks\nrun the full gate and the acceptance suite before landing";
  const replaced = mutateOnce({ ...base, instruction: replacement }, { mutation: "replace-section" });
  check(
    "S1a-2 replace-section postcondition",
    replaced.content.includes("acceptance suite") &&
      !replaced.content.includes("run the full gate before landing") &&
      replaced.content.includes("## parameters"),
    "section body not replaced in place"
  );
  // adjust-parameter
  const adjusted = mutateOnce({ ...base, instruction: "maxRetries = 5" }, { mutation: "adjust-parameter" });
  check(
    "S1a-2 adjust-parameter postcondition",
    adjusted.content.includes("maxRetries: 5") && !adjusted.content.includes("maxRetries: 3")
  );
  // fail-closed witnesses
  let ambiguous = false;
  try {
    mutateOnce({ ...base, parentContent: `${parent}\n## checks\ndup`, instruction: replacement }, { mutation: "replace-section" });
  } catch (error) {
    ambiguous = error instanceof DomainValidationError && error.message.includes("ambiguous");
  }
  check("S1a-2 replace-section ambiguity fails closed", ambiguous);
  let forbidden = false;
  try {
    mutateOnce({ ...base, what: "workflow-template" as MutationSpec["what"], instruction: "x" }, { topologySearchAllowed: true, taskFamily: "deploy" });
  } catch (error) {
    forbidden = error instanceof DomainValidationError && error.message.includes("high-risk");
  }
  check("S1a-2 topology high-risk family fails closed", forbidden);
  const appendUs = bench(() => {
    mutateOnce({ ...base, instruction: "Always cite the failing test name." });
  }, 3000) * 1e3;
  const replaceUs = bench(() => {
    mutateOnce({ ...base, instruction: replacement }, { mutation: "replace-section" });
  }, 2000) * 1e3;
  const adjustUs = bench(() => {
    mutateOnce({ ...base, instruction: "maxRetries = 5" }, { mutation: "adjust-parameter" });
  }, 2000) * 1e3;
  console.log(
    `S1a-2 mutateOnce (dormant; parent=${(parent.length / 1024).toFixed(1)}KB): append=${appendUs.toFixed(1)}us | replace-section=${replaceUs.toFixed(1)}us | adjust-parameter=${adjustUs.toFixed(1)}us per call`
  );
}

/* --- 1a-3: proposeCandidates at E in {8, 64, 512} with dup/self mix. --- */
{
  const parent = `# prompt r22d\n${"Follow the review checklist, cite evidence for every claim, and keep the diff bounded to the stated scope.\n".repeat(30)}`;
  const mkInput = (e: number, cap: number): OptimizerInput => {
    const evidence: OptimizerEvidence[] = Array.from({ length: e }, (_, i) => {
      const boundary = EVIDENCE_BOUNDARIES[i % EVIDENCE_BOUNDARIES.length] as string;
      const selfSupported = i % 5 === 4;
      const dup = i % 7 === 6;
      return {
        patternKey: dup ? `pattern-${Math.max(0, i - 7)}` : `pattern-${i}`,
        boundary,
        redacted: true as const,
        actorModelId: selfSupported ? "model-x" : undefined,
        supportingEvaluatorIds: selfSupported ? ["model-x"] : [`judge-${i % 3}`, "critic-gate"]
      };
    });
    return {
      parentVersionId: "rsv_r22dparent02" as ResourceVersionId,
      parentContent: parent,
      parentKind: "prompt",
      identity: { kind: "prompt", name: "agent-prompt-census" },
      evidence,
      budget: { maxCandidatesPerEpoch: cap, maxTopologyCandidates: 0, lowRiskTaskFamilies: [...LOW_RISK_TASK_FAMILIES] },
      taskFamily: "edit",
      epoch: 1,
      seed: 7
    };
  };
  const parts: string[] = [];
  for (const [e, reps] of [
    [8, 2000],
    [64, 400],
    [512, 40]
  ] as const) {
    const input = mkInput(e, 1_000_000);
    const result = proposeCandidates(input);
    const expectedRejected = Math.floor((e + 0) / 5); // i % 5 === 4 pattern
    const selfCount = input.evidence.filter((item) => item.actorModelId !== undefined).length;
    check(
      `S1a-3 proposeCandidates E=${e}: self-supported all rejected`,
      result.rejectedSelfSupported === selfCount,
      `rejected=${result.rejectedSelfSupported} expected=${selfCount} (${expectedRejected})`
    );
    const eligibleKeys = new Set(
      input.evidence
        .filter((item) => item.actorModelId === undefined)
        .map((item) => `${item.boundary}:${item.patternKey}`)
    );
    check(
      `S1a-3 proposeCandidates E=${e}: dedup by boundary:patternKey`,
      result.proposals.length === eligibleKeys.size,
      `proposals=${result.proposals.length} uniqueEligible=${eligibleKeys.size}`
    );
    check(
      `S1a-3 proposeCandidates E=${e}: proposals carry parent lineage + hash`,
      result.proposals.every(
        (p) => p.parentVersionId === input.parentVersionId && p.contentHash === hashCandidateContent(p.content) && !p.selfSupported
      )
    );
    const capped = proposeCandidates(mkInput(e, 3));
    check(`S1a-3 proposeCandidates E=${e}: cap enforced`, capped.proposals.length === Math.min(3, eligibleKeys.size));
    const ms = bench(() => {
      proposeCandidates(input);
    }, reps);
    parts.push(`E=${e} -> ${(ms * 1e3).toFixed(1)}us (${result.proposals.length} proposals)`);
  }
  console.log(`S1a-3 proposeCandidates (dormant; ~3.2KB parent, uncapped): ${parts.join(" | ")}`);
}

/* --- 1a-4: drift monitor at W in {8, 64, 512} + drift witnesses. --- */
{
  const mkObs = (i: number, drifted: boolean): DriftObservation => ({
    modelVersion: drifted ? `m-new-${i % 4}` : `m-${i % 2}`,
    taskFamily: drifted ? `fam-new-${i % 3}` : ["edit", "test", "review"][i % 3] as string,
    projectId: "proj-1",
    policyVersion: "pol-1",
    judgeCalibration: 0.8
  });
  const parts: string[] = [];
  for (const [w, reps] of [
    [8, 2000],
    [64, 300],
    [512, 40]
  ] as const) {
    const monitor = createAdaptationDriftMonitor();
    const t0 = performance.now();
    let lastReport = monitor.observe(mkObs(0, false));
    for (let i = 1; i < w; i += 1) {
      lastReport = monitor.observe(mkObs(i, i >= w - 8 && w > 8));
    }
    const buildMs = performance.now() - t0;
    if (w > 8) {
      check(`S1a-4 monitor W=${w}: unseen-majority window declares drift`, lastReport.drifted && lastReport.axes.modelVersion && lastReport.axes.taskMix);
    } else {
      check(`S1a-4 monitor W=${w}: stable first window does not drift`, !lastReport.drifted && lastReport.uncertainty === 0);
    }
    const steadyUs = bench(() => {
      const m2 = createAdaptationDriftMonitor();
      m2.restore(monitor.snapshot());
      m2.observe(mkObs(w, false));
    }, Math.max(20, Math.floor(reps / 4))) * 1e3;
    const observeUs = (buildMs / w) * 1e3;
    const snapshotUs = bench(() => {
      monitor.snapshot();
    }, reps) * 1e3;
    parts.push(`W=${w} -> observe=${observeUs.toFixed(1)}us snapshot=${snapshotUs.toFixed(1)}us restore+observe=${steadyUs.toFixed(1)}us`);
  }
  console.log(`S1a-4 createAdaptationDriftMonitor (dormant; windowSize=8 default): ${parts.join(" | ")}`);
}

/* --- 1a-5: retirement trio on the configured registry + witnesses. --- */
{
  const { registry, prompts } = buildConfiguredRegistry("rt0");
  const p4 = prompts[4] as (typeof prompts)[number];
  const p2 = prompts[2] as (typeof prompts)[number];
  // p2.v1 was retired by the configured fixture; retire again = idempotent.
  const again = retireVersion(registry, p2.v1.versionId);
  check("S1a-5 retireVersion idempotent on already-retired version", again.versionId === p2.v1.versionId && isRetired(registry, p2.v1.versionId));
  // Retiring the active version must refuse.
  const active4 = registry.getActiveVersion(p4.identity) as ResourceVersion;
  let refused = false;
  try {
    retireVersion(registry, active4.versionId);
  } catch (error) {
    refused = error instanceof DomainValidationError && error.message.includes("cannot retire the current active version");
  }
  check("S1a-5 retireVersion refuses the active version", refused);
  // assertAssignable throws on retired, passes on active.
  let blocked = false;
  try {
    assertAssignable(registry, p2.v1.versionId);
  } catch (error) {
    blocked = error instanceof DomainValidationError && error.message.includes("retired");
  }
  check("S1a-5 assertAssignable fails closed on retired version", blocked);
  assertAssignable(registry, active4.versionId);
  const retireUs = bench(() => {
    retireVersion(registry, p2.v1.versionId);
  }, 20000) * 1e3;
  const assignableUs = bench(() => {
    assertAssignable(registry, active4.versionId);
  }, 20000) * 1e3;
  const isRetiredUs = bench(() => {
    isRetired(registry, p2.v1.versionId);
  }, 50000) * 1e3;
  console.log(
    `S1a-5 retirement trio (dormant; configured registry V=15): retireVersion(idempotent)=${(retireUs * 1e3).toFixed(0)}ns | assertAssignable=${(assignableUs * 1e3).toFixed(0)}ns | isRetired=${(isRetiredUs * 1e3).toFixed(0)}ns per call`
  );
}

/* --- 1a-6: PromotionService begin/commit vs promoteWithRegistry twin. --- */
{
  const report = configuredEvalReport as NonNullable<typeof configuredEvalReport>;
  const mkFixture = (tag: string) => {
    const { registry, candidateId, baseline } = buildConfiguredRegistry(tag);
    const candidate = registry.getCandidate(candidateId) as ImprovementCandidate;
    const input: PromoteInput = {
      candidateId,
      expectedCurrentVersionId: baseline.versionId,
      content: CONFIGURED_CANDIDATE_POLICY,
      approvedBy: HUMAN,
      review: mkReview(candidateId, candidate.contentHash, tag),
      changeNote: mkNote(`routing:${tag}`, baseline.versionId),
      explicitApproval: true,
      evalReport: { ...report, candidateId, contentHash: candidate.contentHash }
    };
    return { registry, input };
  };
  // Twins with the same tag => same deterministic id streams.
  const a = mkFixture("svc0");
  const b = mkFixture("svc0");
  const service = new PromotionService(a.registry);
  const began = service.beginPromotion(a.input);
  check("S1a-6 PromotionService intent id matches intentIdFor", began.intentId === intentIdFor(began.pendingVersion.versionId));
  const committed = service.commitPromotion(began.intentId);
  const direct = promoteWithRegistry(b.registry, b.input);
  check(
    "S1a-6 PromotionService begin+commit == promoteWithRegistry (semantic twin)",
    canonicalJson(a.registry.snapshot()) === canonicalJson(b.registry.snapshot()) &&
      committed.newVersion?.versionId === direct.newVersion?.versionId
  );
  const K = 40;
  const fixtures = Array.from({ length: K }, (_, i) => mkFixture(`svcp${i}`));
  const t0 = performance.now();
  for (const f of fixtures) {
    const svc = new PromotionService(f.registry);
    const bg = svc.beginPromotion(f.input);
    svc.commitPromotion(bg.intentId);
  }
  const svcUs = ((performance.now() - t0) / K) * 1e3;
  console.log(
    `S1a-6 PromotionService (dormant wrapper; begin+commit two-phase): ${svcUs.toFixed(1)}us per promotion (vs S0d one-shot promote op)`
  );
}

/* --- 1a-7: reconstructPromotion at L in {11, 4096} + witness. --- */
{
  const { registry } = buildConfiguredRegistry("rc0");
  const ledger = registry.ledger();
  const rebuilt = reconstructPromotion(ledger);
  const lastPromoted = [...ledger].reverse().find((entry) => entry.kind === "promoted") as PromotionLedgerEntry;
  check(
    "S1a-7 reconstructPromotion returns the last promoted entry (configured L)",
    rebuilt.candidateId === lastPromoted.candidateId && rebuilt.toVersionId === lastPromoted.toVersionId
  );
  const bigLedger: PromotionLedgerEntry[] = Array.from({ length: 4096 }, (_, i) => ({
    kind: i % 2 === 0 ? "intent" : "promoted",
    candidateId: `cnd_big${String(i >> 1).padStart(4, "0")}` as CandidateId,
    fromVersionId: `rsv_bigfrom${i >> 1}` as ResourceVersionId,
    toVersionId: `rsv_bigto${i >> 1}` as ResourceVersionId,
    expectedCurrentVersionId: `rsv_bigfrom${i >> 1}` as ResourceVersionId,
    approvedBy: HUMAN,
    changeNote: mkNote(`big:${i}`, `rsv_bigfrom${i >> 1}` as ResourceVersionId),
    at: NOW
  }));
  const rebuiltBig = reconstructPromotion(bigLedger);
  check("S1a-7 reconstructPromotion fantasy L=4096 witness", rebuiltBig.candidateId === `cnd_big2047`);
  const smallUs = bench(() => {
    reconstructPromotion(ledger);
  }, 50000) * 1e3;
  const bigUs = bench(() => {
    reconstructPromotion(bigLedger);
  }, 1000) * 1e3;
  console.log(
    `S1a-7 reconstructPromotion (dormant; full-ledger scan): L=11 -> ${(smallUs * 1e3).toFixed(0)}ns | L=4096 -> ${bigUs.toFixed(1)}us per call`
  );
}

/* --- 1a-8: RollbackLog standalone ops at L in {2, 4096}. --- */
{
  const mkEntry = (i: number): RollbackLedgerEntry => ({
    kind: ROLLBACK_LEDGER_KINDS[i % 3] as RollbackLedgerEntry["kind"],
    fromVersionId: `rsv_rbfrom${i}` as ResourceVersionId,
    toVersionId: `rsv_rbto${i}` as ResourceVersionId,
    reason: ROLLBACK_REASONS[i % 3] as RollbackLedgerEntry["reason"],
    automatic: ROLLBACK_REASONS[i % 3] === "guardrail",
    evidence: [`ev:${i}`],
    at: NOW
  });
  const small = new RollbackLog();
  small.append(mkEntry(0));
  small.append(mkEntry(1));
  check("S1a-8 RollbackLog last() returns the newest entry", small.last()?.fromVersionId === "rsv_rbfrom1");
  const listed = small.list();
  check("S1a-8 RollbackLog list() is a defensive copy", listed !== small.list() && listed.length === 2);
  const bigEntries = Array.from({ length: 4096 }, (_, i) => mkEntry(i));
  const big = new RollbackLog();
  big.restore(bigEntries);
  check("S1a-8 RollbackLog restore(4096) holds all entries", big.list().length === 4096 && big.last()?.fromVersionId === "rsv_rbfrom4095");
  const appendNs = bench(() => {
    const log = new RollbackLog();
    log.append(mkEntry(0));
  }, 50000) * 1e6;
  const listSmallNs = bench(() => {
    small.list();
  }, 100000) * 1e6;
  const listBigUs = bench(() => {
    big.list();
  }, 2000) * 1e3;
  const restoreBigUs = bench(() => {
    const log = new RollbackLog();
    log.restore(bigEntries);
  }, 300) * 1e3;
  console.log(
    `S1a-8 RollbackLog (slice-internal engine + dormant export): append=${appendNs.toFixed(0)}ns | list(L=2)=${listSmallNs.toFixed(0)}ns | list(L=4096)=${listBigUs.toFixed(1)}us | restore(L=4096)=${restoreBigUs.toFixed(1)}us`
  );
}

/* --- 1b: micro-helper unit-price table (remaining value exports). --- */
{
  const { registry, identity, candidateId, baseline, prompts } = buildConfiguredRegistry("mh0");
  const candidate = registry.getCandidate(candidateId) as ImprovementCandidate;
  const report = configuredEvalReport as NonNullable<typeof configuredEvalReport>;
  const activeVersion = registry.getActiveVersion(identity) as ResourceVersion;
  const note = mkNote("routing:mh0", baseline.versionId);
  const review = mkReview(candidateId, candidate.contentHash, "mh0");
  const promoteInput: PromoteInput = {
    candidateId,
    expectedCurrentVersionId: baseline.versionId,
    content: CONFIGURED_CANDIDATE_POLICY,
    approvedBy: HUMAN,
    review,
    changeNote: note,
    explicitApproval: true,
    evalReport: { ...report, candidateId, contentHash: candidate.contentHash }
  };
  const rollbackInput: RollbackInput = {
    identity: (registry.getVersion((registry.rollbackLedger()[0] as RollbackLedgerEntry).fromVersionId) as ResourceVersion).identity,
    expectedCurrentVersionId: (registry.rollbackLedger()[0] as RollbackLedgerEntry).fromVersionId,
    targetVersionId: (registry.rollbackLedger()[0] as RollbackLedgerEntry).toVersionId,
    reason: "user",
    evidence: ["ev:census"],
    automatic: false
  };
  const rollbackEntryJson = JSON.parse(JSON.stringify(registry.rollbackLedger()[0])) as unknown;
  const reviewJson = JSON.parse(JSON.stringify(review)) as unknown;
  const parsedSnapshot = JSON.parse(configuredSnapshotJson) as unknown;
  const reportJson = JSON.parse(JSON.stringify(report)) as unknown;
  const profile = createDefaultApprovalProfile();
  const installedProfile = createInstalledAutoAdaptProfile();
  validateApprovalProfile(installedProfile);
  check(
    "S1b default + installed profiles never auto-promote (budget 0, empty classes)",
    !canAutoPromote(profile, "prompt", 0) && !canAutoPromote(installedProfile, "prompt", 0)
  );
  check("S1b split determinism + seal witnesses", (() => {
    const s1 = assignEvaluationSplit("cnd_census0001", 7);
    const s2 = assignEvaluationSplit("cnd_census0001", 7);
    let sealed = false;
    try {
      assertSplitSeparation("holdout", "validation");
    } catch (error) {
      sealed = error instanceof DomainValidationError && error.message === "holdout is sealed";
    }
    let same = false;
    try {
      assertSplitSeparation("validation", "validation");
    } catch {
      same = true;
    }
    assertSplitSeparation("train", "validation");
    let selfOnly = false;
    try {
      assertPromotableFromSupport({ actorModelId: "m1", supportingEvaluatorIds: ["m1"] });
    } catch {
      selfOnly = true;
    }
    assertPromotableFromSupport({ actorModelId: "m1", supportingEvaluatorIds: ["m1", "judge-2"] });
    return s1 === s2 && sealed && same && selfOnly;
  })());
  check("S1b constants census witnesses", (() => {
    return (
      CANDIDATE_STATUSES.length === 5 &&
      RESOURCE_KINDS.length === 10 &&
      NON_AUTO_PROMOTABLE_KINDS.every((kind) => isNonAutoPromotableKind(kind)) &&
      MUTATABLE_KINDS.length === 7 &&
      LOW_RISK_TASK_FAMILIES.length === 3 &&
      HIGH_RISK_TASK_FAMILIES.length === 2 &&
      EVIDENCE_BOUNDARIES.length === 8 &&
      ROLLBACK_REASONS.length === 3 &&
      ROLLBACK_LEDGER_KINDS.length === 3 &&
      ROUTING_EVALUATOR_VERSION === "routing-eval-v1" &&
      ADAPTATION_WHEN === "offline-inter-test-time" &&
      PARAMETERIZATION_LEVEL === "typed-parameters" &&
      MUTATABLE_KINDS.every((kind) => isMutatableKind(kind)) &&
      !isMutatableKind("credential")
    );
  })());
  check("S1b isAutoAdaptEnabled kill-switch semantics", (() => {
    return (
      isAutoAdaptEnabled({}) &&
      !isAutoAdaptEnabled({ SPARKLE_AUTO_ADAPT: "0" }) &&
      !isAutoAdaptEnabled({ SPARKLE_AUTO_ADAPT: "false" }) &&
      !isAutoAdaptEnabled({ SPARKLE_AUTO_ADAPT: "off" }) &&
      isAutoAdaptEnabled({ SPARKLE_AUTO_ADAPT: "1" })
    );
  })());
  check("S1b candidateError/isCandidate valid + invalid witnesses", (() => {
    return (
      candidateError(candidate) === undefined &&
      isCandidate(candidate) &&
      !isCandidate({ ...candidate, contentHash: "ZZZ" }) &&
      candidateError({ ...candidate, status: "bogus" as ImprovementCandidate["status"] }) !== undefined
    );
  })());
  check("S1b active-pointer CAS witnesses", (() => {
    /* prompts[4] was promoted in the builder and never rolled back or
     * retired, so its active version (v2) differs from v1 -- a real swap. */
    const prompt = prompts[4] as (typeof prompts)[number];
    const promptActive = registry.getActiveVersion(prompt.identity) as ResourceVersion;
    if (promptActive.versionId === prompt.v1.versionId) return false;
    const key = resourceIdentityKey(prompt.identity);
    const map = new Map<string, ResourceVersionId>([[key, promptActive.versionId]]);
    casActivePointer(map, prompt.identity, promptActive.versionId, prompt.v1.versionId);
    let stale = false;
    try {
      casActivePointer(map, prompt.identity, promptActive.versionId, prompt.v1.versionId);
    } catch (error) {
      stale = error instanceof DomainValidationError && error.message.startsWith("CAS failed");
    }
    let unknown = false;
    try {
      casActivePointer(new Map(), prompt.identity, promptActive.versionId, prompt.v1.versionId);
    } catch (error) {
      unknown = error instanceof DomainValidationError && error.message.startsWith("unknown expected version");
    }
    assertExpectedActive(promptActive, promptActive.versionId);
    let mismatch = false;
    try {
      assertExpectedActive(promptActive, prompt.v1.versionId);
    } catch {
      mismatch = true;
    }
    return map.get(key) === prompt.v1.versionId && stale && unknown && mismatch;
  })());
  check("S1b gate predicate witnesses", (() => {
    let modelRefused = false;
    try {
      assertExplicitApprovalActor({ kind: "model", identity: "m1" });
    } catch {
      modelRefused = true;
    }
    assertExplicitApprovalActor(HUMAN);
    return (
      isPromotableStatus("proposed") &&
      !isPromotableStatus("retired") &&
      isIntentId(intentIdFor(baseline.versionId)) &&
      !isIntentId("bogus") &&
      modelRefused &&
      autoPromotableFor("prompt") &&
      !autoPromotableFor("credential")
    );
  })());
  check("S1b assertSingleResourceBoundary witnesses", (() => {
    assertSingleResourceBoundary(identity, CONFIGURED_CANDIDATE_POLICY);
    assertSingleResourceBoundary(identity, "plain text, not JSON");
    let dual = false;
    try {
      assertSingleResourceBoundary(identity, JSON.stringify({ kinds: ["prompt", "skill"] }));
    } catch {
      dual = true;
    }
    return dual;
  })());
  check("S1b acyclic-lineage witnesses", (() => {
    const parents = new Map<string, string | undefined>([
      ["c", "b"],
      ["b", "a"],
      ["a", undefined]
    ]);
    assertAcyclicLineage("c", (id) => parents.get(id));
    let cyclic = false;
    try {
      assertAcyclicLineage("c", (id) => (id === "a" ? "c" : parents.get(id)));
    } catch {
      cyclic = true;
    }
    return cyclic;
  })());

  const cells: [string, number][] = [];
  const timeNs = (label: string, reps: number, fn: () => void): void => {
    cells.push([label, bench(fn, reps) * 1e6]);
  };
  timeNs("resourceIdentityKey", 200000, () => {
    resourceIdentityKey(identity);
  });
  const casMap = new Map<string, ResourceVersionId>([[resourceIdentityKey(identity), activeVersion.versionId]]);
  timeNs("casActivePointer(hit; same-id swap)", 200000, () => {
    casActivePointer(casMap, identity, activeVersion.versionId, activeVersion.versionId);
  });
  timeNs("assertExpectedActive(hit)", 200000, () => {
    assertExpectedActive(activeVersion, activeVersion.versionId);
  });
  timeNs("identityEquals", 200000, () => {
    identityEquals(identity, candidate.identity);
  });
  timeNs("scopeEquals", 200000, () => {
    scopeEquals(identity.scope, candidate.identity.scope);
  });
  timeNs("isNonAutoPromotableKind", 200000, () => {
    isNonAutoPromotableKind("prompt");
  });
  timeNs("isPromotableStatus", 200000, () => {
    isPromotableStatus("proposed");
  });
  timeNs("isIntentId", 200000, () => {
    isIntentId("int_mh00002");
  });
  timeNs("intentIdFor", 200000, () => {
    intentIdFor(baseline.versionId);
  });
  timeNs("autoPromotableFor", 200000, () => {
    autoPromotableFor("prompt");
  });
  timeNs("candidateError(valid)", 100000, () => {
    candidateError(candidate);
  });
  timeNs("isCandidate(valid)", 100000, () => {
    isCandidate(candidate);
  });
  timeNs("validateCandidate", 100000, () => {
    validateCandidate(candidate);
  });
  timeNs("assertSingleResourceBoundary(policy 9.2KB)", 5000, () => {
    assertSingleResourceBoundary(identity, CONFIGURED_CANDIDATE_POLICY);
  });
  timeNs("validateChangeNote", 100000, () => {
    validateChangeNote(note, baseline.versionId);
  });
  timeNs("validatePromotionReview", 100000, () => {
    validatePromotionReview(review);
  });
  timeNs("assertExplicitApprovalActor", 200000, () => {
    assertExplicitApprovalActor(HUMAN);
  });
  timeNs("assertRoutingPolicyEvalReport(full gate)", 3000, () => {
    assertRoutingPolicyEvalReport(candidate, promoteInput);
  });
  timeNs("parsePromotionReview", 50000, () => {
    parsePromotionReview(reviewJson);
  });
  timeNs("parseRoutingEvalReport", 100000, () => {
    parseRoutingEvalReport(reportJson);
  });
  timeNs("parseRollbackLedgerEntry", 100000, () => {
    parseRollbackLedgerEntry(rollbackEntryJson);
  });
  timeNs("validateRollbackInput", 100000, () => {
    validateRollbackInput(rollbackInput);
  });
  timeNs("canAutoPromote(default profile)", 200000, () => {
    canAutoPromote(profile, "prompt", 0);
  });
  timeNs("validateApprovalProfile", 100000, () => {
    validateApprovalProfile(profile);
  });
  timeNs("createDefaultApprovalProfile", 100000, () => {
    createDefaultApprovalProfile();
  });
  timeNs("createInstalledAutoAdaptProfile", 100000, () => {
    createInstalledAutoAdaptProfile();
  });
  timeNs("isAutoAdaptEnabled", 200000, () => {
    isAutoAdaptEnabled({});
  });
  timeNs("assignEvaluationSplit", 100000, () => {
    assignEvaluationSplit("cnd_census0001", 7);
  });
  timeNs("assertSplitSeparation", 200000, () => {
    assertSplitSeparation("train", "holdout");
  });
  timeNs("assertPromotableFromSupport", 100000, () => {
    assertPromotableFromSupport({ actorModelId: "m1", supportingEvaluatorIds: ["m1", "judge-2"] });
  });
  timeNs("adaptationRegistryPath", 200000, () => {
    adaptationRegistryPath("/tmp/state");
  });
  const parseSnapUs = bench(() => {
    parseRegistrySnapshot(parsedSnapshot);
  }, 300) * 1e3;
  const sorted = [...cells].sort((a, b) => b[1] - a[1]);
  console.log(
    `S1b micro-helper unit prices (${cells.length} cells, ns/call, top 6 by cost): ${sorted
      .slice(0, 6)
      .map(([label, ns]) => `${label}=${ns.toFixed(0)}ns`)
      .join(" | ")}`
  );
  console.log(
    `S1b remaining cells all <= ${Math.max(...sorted.slice(6).map(([, ns]) => ns)).toFixed(0)}ns; parseRegistrySnapshot(configured 146KB snapshot object)=${parseSnapUs.toFixed(1)}us per call | rollbackActive wrapper: priced in S0d (delegates to registry.rollback + one rollbackLedger copy)`
  );

  /* 1c: 10 ms crossing solve for the priciest census cells. */
  const crossing = (label: string, unitUs: number, prodCallsPerCommand: string): string =>
    `${label}: ${(10000 / unitUs).toFixed(0)} calls/command needed (production: ${prodCallsPerCommand})`;
  const paretoUs512 = bench(() => {
    paretoFront(
      Array.from({ length: 512 }, (_, i) => ({
        candidateId: `cx-${i}`,
        quality: (i % 100) / 100,
        preferenceFit: ((i * 7) % 100) / 100,
        costUsd: (i % 50) / 10,
        latencyMs: (i * 13) % 1000,
        risk: ((i * 3) % 100) / 100
      }))
    );
  }, 20) * 1e3;
  console.log(
    `S1c 10ms crossings for the priciest dormant cells: ${crossing(`paretoFront(n=512, ${paretoUs512.toFixed(0)}us)`, paretoUs512, "0 calls -- dormant")} | evaluateProposalShadow P=2000/A=1000 is the only >10ms dormant cell and its body lives in src/experiments (cross-check anchor, S0b)`
  );
}

await rm(workRoot, { recursive: true, force: true });
if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nAll checks passed.");
```

MORE_OPTIMA=no
BRANCH=cursor/r22-d-adaptation-twenty-second-pass-83a1
