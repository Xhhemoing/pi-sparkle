MODEL_SLUG=claude-fable-5-thinking-xhigh

# R22-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第二十二遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 22 / R22-A（Round 1–21 同区第二十二遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `a765f3d`（含 R21-I 空枚举收口与 Round 22 开轮、R22-A 派出）
**分支:** `cursor/r22-a-tracking-twenty-second-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S22-A-\* ID，切片零代码改动；
整片预算收口第二十次复核成立，并新增一条实测轴——「公开导出普查
（export census）+ 分析-隔离面定价」补上二十一遍搜查史的最后一类完备性
盲区：此前各轮把生产管线沿输入维（R14–R16、R21）、组成维（R17、R18）、
结局维（R19）、驻留状态维（R20）逐一定价，但**切片公开导出面本身**从未
被当作普查对象。本轮对 14 个文件的全部 **30 个可调用导出**做定价普查，
发现恰 **7 个从未有过实测价**：分析面双函数（`sanitizePacketForAnalysis` /
`proposeFromAnomaly`——规格钦定的 packet→candidate 唯一桥，S8-A-2 只记录过
81–107 ns 的塌缩差价、从未定价面本体）、隔离三函数
（`bindExecutionContext` / `executionMayNotReadSummary` /
`executionAuthority`——三线规格「Tracking 无命令权」的可执行契约载体）、
以及 config 构造臂（`applyUserThreshold` 成功臂——其 throw 终点由 R18-A
定价——与 `trackingConfig`）。本轮给出全部七个的实测价与五组契约证人
（F2a–F2e）。该面**生产流量为零由 grep 钉死**（七函数无一有切片外 src/
调用方；runTrackingTurn 在每个 hard/soft 门上构建的 wake-arm
AnomalyPacket 是生产构建但**生产死端**——applyChildThreeLine 公开返回值
恰 `{events, result}` 两键，packet 从任何生产返回值不可达，F2a 700/700
分布级钉死），但**契约存活**（proposeFromAnomaly 是规格允许的唯一
packet→candidate 桥；隔离三函数是规格行文的可执行文档，测试直调）。
轴上全部角度经换名检查判为既有排除 ID 的本体或换位点（5 项新记录，
见 §0），**只产出归档证人与数据补充，未产出可落地候选**——整面生产
零流量 + 仅幻想规模越线（操作基数 T ≈ 1.5×10⁵）按令即空收口。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R21-A 至本轮基线
（`a765f3d`）**逐字节未变**（`git diff 7acb666..a765f3d -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，二十二遍全程零
diff；清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2）并全部
实际重读）。调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..a765f3d -- src/` **完全为空**（跨 R10…R22 开轮十三段
全部为 docs 提交，`git log 89f0455..HEAD -- src/` 计数为 0），生产调用方
经本轮全库 grep 复核仍为 `supervisor.ts:483`
（applyTrackingGate/nextTrackingSeq；两个 settle 调用点 601/711 均不传
trackingAssessment ⇒ 生产 O(1) no-op，第十三次复验）/
`coordinator.ts:444` / `flowchart-run.ts:320`（applyChildThreeLine），
每子结果一次（~5 次/run），事件表几十级（41）。R21-A 的 ~66–95 µs/run
预算天花板经本轮在当前 VM（Node v22.22.2；本 VM 默认 v22.14.0 且
`/exec-daemon/node` 垫片遮蔽 nvm，按 engines >=22.19.0 经 PATH 前置
nvm v22.22.2）实测复核为 **~65–80 µs/run**（五格锚点矩阵三次独立运行
13.0–16.0 µs/gate；上沿 80 由 run3 no-contract 首格单点 16.0 拉出——
R11-A 以来「PIC/基准次序」现象的本轮形态，除该孤点外三次运行为
13.0–14.6 µs/gate ⇒ ~65–73 µs/run，与 R21-A 稳态 66–81 / R20-A 64–80
同带；单格横向比较不具意义；量级结论不变——稳态下即使切片零成本，
节省上界 ~0.07–0.08 ms/run，仍比落地线低约三个量级）。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
数据补充（5 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项 +
R15-A 5 项 + R16-A 4 项 + R17-A 4 项 + R18-A 4 项 + R19-A 2 项 +
R20-A 3 项 + R21-A 2 项换名检查全部复核维持，未追认任何 S12…S21-A-\*
ID），按令一律不铸新 ID。作为空枚举的实证支撑，本轮新落档一组数据
（§1，三次独立运行稳定；fuzz 计数三次逐位一致）：

**公开导出普查 + 分析-隔离面定价（axis 11）**：30 个可调用导出中 23 个
已由 R1-A…R21-A 在宿主路径或隔离格定价，7 个无价者全部落在**零生产
流量的契约载体**上。**sanitize 是平坦引用投影**（五形态格 36–65 ns
无规模维——bare 46–52 / +defense+identity 43–45 / +userText 1KB 63–65 /
+toolBodies 16×256B 47–48 / **T=1000 操作窗口 36–39 ns**；F2b 700/700
证 summary/contextFacts/toolSituations/evidenceRefs 按引用携带，
非拷贝）。**proposeFromAnomaly 生产形状（T=1）全程 4.0–4.5 µs，
切片内部分（sanitize + content 构建）仅 682–700 ns = 15.2–17.0%，
其余为 D 切片 registry 腿**（createCandidate 隔离 2.64–2.71 µs，
含 hash32 内容哈希 + boundary 探针 + 校验）。**操作基数斜率 T =
66.0–66.9 ns/op**（content 构建 T ∈ {1,10,100,1000} 四格线性无悬崖），
10 ms 穿越点 **T ≈ 1.50–1.51×10⁵**（生产 T=1，差 5 个量级）；
propose 全程 T=1000 现场 238–251 µs；V=100 现场 1.69–1.77 µs
（R16-A V 族斜率在零流量面上的换位点）。**隔离三函数 5.6–8.5 ns、
config 双臂 7.5–19.0 ns**（applyUserThreshold 成功臂 17.3–19.0 /
trackingConfig() 7.5–8.7 / overrides 11.5–11.9 ns）。**no-baseline
故障格 15.08–15.23 µs/fault**（once-per-fault，含 Error 构造 + 栈，
归 R18-A 拒绝面家族）。契约证人五组：F2a 生产死端引理（700 trial：
FAILED 格 352/352 建 packet、PASSED 干净格 348/348 不建、消费者返回键
恰 {events, result} 700/700——wake-arm 生产构建但下游零消费）；F2b
sanitize 契约（actorDefense/actorIdentity 700/700 剥除、userTextTrust
缺省 UNTRUSTED_TEXT 167 例 / 显式保留 177 例、toolBodies 短命透传、
引用投影 700/700）；F2c 耐久内容契约（content 键集恰
{gate,score,P,H,evidenceRefs,anomalyCodes,operations,rollbackTarget}、
operations 投影仅 {name, exitCode?}、五个哨兵串（defense/identity/
toolBody/userText/aiText）零泄漏进耐久字节、H 序列化
"UNOBSERVED"|number、rollbackTarget ≡ active baseline）；F2d
proposal-first 纯性（active 指针不动、candidate 生而 "proposed"、
autoPromotable 按 kind 分类 prompt=true/security=false、输入 packet
逐位不变、no-baseline 抛 DomainValidationError fail-closed 且 registry
零效应）；F2e 隔离真值表（bind/authority 对 task context 恒等且对
任意 directive/summary 文本不敏感；mayNotRead 拒绝别名与
prescore/openMinors 键泄漏——「分析不改 in-flight run / Tracking 无
命令权」的规格回声）。

R12-A 分解的五大组件占比（hashAssessment×3 ~43–47% / validateEvent
~21–23% / turn ~12–13% / prescore×2 ~10% / O(E) 五扫描+拷贝
~2.0–2.2%）在本 VM 逐一复现（§1 表），R18-A skip 定价与 R19-A replay
定价抽测复现同带（guard-3 skip 1926–1984 ns = 14.2–15.2% of apply；
replay find#1 3207–3245 ns = 23.7–25.0% of apply）。第二十二遍收口，
**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；未触碰在飞的
  R21-H / R21-J 分支与文件，未触碰已收口的 R21-\* 报告。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表 545 行，含
  S13-B-1；R12-A…R21-A 空枚举未铸 ID，确认）/ PROGRESS.md /
  round-22/PLAN.md / R21-A / R20-A / R19-A。EXCLUSIONS.md /
  PROGRESS.md / 各 PLAN.md / README.md 零触碰。
- 环境：本 VM 默认 Node v22.14.0（`/exec-daemon/node` 垫片遮蔽 nvm），
  按 engines（>=22.19.0）以 PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 切至 v22.22.2 后
  `pnpm install --frozen-lockfile`（pnpm 10.17.1），全部测量在
  v22.22.2 完成。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或数据补充，**一律不铸 S22-A-\* ID**。R12-A 13 项、R13-A 8 项、
  R14-A 3 项、R15-A 5 项、R16-A 4 项、R17-A 4 项、R18-A 4 项、R19-A
  2 项、R20-A 3 项、R21-A 2 项换名检查全部复核维持，不再逐条重列；
  以下 5 项为本轮**新记录**）：
  1. **propose content 构建手写序列化/单遍融合**（把 sanitize +
     operations map + JSON.stringify 改为手写拼串或单遍直构，摊平
     0.6–0.7 µs 切片内地板）= S5-A-2 本体的同型教训（hashAssessment
     手写定长序列化实测慢 11–15%）+ S1-E-3（字符串折叠改
     parts+join，现实规模负收益）族的 analysis 换位点；且整面生产
     零流量（F2a），拒列；
  2. **sanitize 条件 spread 改后置条件赋值 / 输出形状单态化**（四个
     条件键 userText/userTextTrust/aiText/toolBodies 的 spread 链改
     后置赋值或恒定形状）= S8-E-2（baseSignal 条件 spread 改后置
     赋值——收益大半为 PIC 敏感项）与 S9-D-1（输出形状单态化，
     12.8–13.9 µs/eval 仍拒列）的 analysis 换位点；本轮实测面平坦
     36–65 ns 且零流量，拒列；
  3. **proposeFromAnomaly 与 createCandidate 的 parent 双查去重**
     （analysis 侧 getActiveVersion 查得 parent 后，createCandidate
     内 versionsById.get(parentVersionId) 对同一 parent 再查）=
     S2-D-2（rollback CAS 后冗余 versionsById.get 消除——fail-closed
     兜底保留）同族；实现在 registry.ts（D 切片，越切片）+ 消除需
     拓宽公开签名（X0-4 族）；每提案一次 ~10² ns 级，拒列；
  4. **analysis 侧信任内容跳过 assertSingleResourceBoundary**
     （content 由本切片字面量构造、可静态证 boundary-safe，理论上可
     免 createCandidate 内 JSON.parse 探针）= S12-D-1（探针省略，
     once-per-command 微秒 reject 类，便宜形态 fail-open）+
     R12-A §0-13 信任调用方本体；实现在 candidate.ts（D 切片），
     拒列；
  5. **isolation/authority 恒等函数删除或调用点内联**
     （bindExecutionContext / executionAuthority 恒等返回，理论上可
     删）= 三线规格「执行只收 task context packet」的可执行契约载体
     （删除是文档/功能面变更，非保行为优化）+ S8-A-3 公开面收窄族；
     5.6–8.5 ns 零收益，拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  wake-arm packet 构建享元/条件 spread 消除 = S1-A-7/S7-B-5 可观察
  身份族 + R13-A turn 八子组件全点名（packet 构建已在 turn 剖面内
  定价）；按 packet 记忆化 propose = X1-1 隐藏缓存本体；content 复用
  hashAssessment 序列化面 = X1-2 平行实现 + 哈希契约面；no-baseline
  throw 前置探测/廉价化 = R18-A §0-3 拒绝路径廉价化整类（fail-closed
  契约）；evidenceWeight 换 Map/Set = S1-A-8 本体；`every×includes`
  Set 化 = S1-A-4/X4-7（C/R ≈ 10³ 重开条件，R15-A 轴 1）；第二次
  prescore = S1-A-2；幂等双 find/死扫描 = S8-A-1 / S11-A-2；写侧
  validateEvent = S4-G-2 族（实现在 events.ts，G 切片）；turn 恒定
  输入常量折叠 = R12-A §0-11 伞 + R16-A §0-2；skip 守卫次序/享元/
  prescore 复用 = R18-A §0-1/2/4 维持；replay 快路径/重算去重 =
  R19-A §0-1/2 维持；hashSummary 缓存/增量与 with-previous 管道融合 =
  R20-A §0-2/3 维持；human-score 单遍合并/ratio Set 削减 =
  R21-A §0-1/2 维持。
- 未重开任何 X\* / S1-\* … S21-\* 条目。**R21-A H 输入面（humanInput；
  1.65–1.66 ns/char；生产恒 `{}`）不补铸、不重测**（本轮 F2a 的
  packet.H ≡ "UNOBSERVED" 证人只是其零流量事实的引用）；**R20-A
  `gate.openMinors` M 轴与多轮驻留链不补铸**；**R19-A 重投递 /
  `wait_user` 不补铸**（replay find#1 仅做组成连续性抽测一格，见
  §1）；R18-A 两轴（skip 组成、拒绝终点普查）只抽测 guard-3 skip
  一格（no-baseline 故障格是 analysis 面的新格，归其家族但非重测）；
  R17-A…R14-A 各轴不补铸、不重测超出复核所需的部分；未再编号
  FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN；S11-A-1 /
  S11-A-2 / S10-A-1 / S9-A-1 / S8-A-1..3 / S7-A-1..4 / X0-4/X2-4 /
  X0-6 / S6-A-1 专项复核：本轮零触碰、零重提，全部维持关闭。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足——F2d/F2e 正是这些条款的**分布级回声
  验证**（active 指针不动、candidate 生而 proposed、隔离三函数恒等/
  拒读），未改任何阈值、未改任何测试；「H/score 不写路由」在本轮
  F2c 中逐位可见（durable content 只进 registry 候选面，不触任何
  路由面）。不声称 Outcome-supported；Checkpoint F-PROD 仍开放
  （ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本（按令不建
  `scripts/round22-r22a-equivalence-sim.ts`），仿真全文见本报告附录。
  不开 PR。

## 1. 预算支配论证复核 + 一条新轴（方法第 1、2 步）

R3-A §3 … R21-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..a765f3d -- <切片>` 为空，逐字节
   一致（二十二遍全程零 diff）——R1-A…R21-A 全部测量与排除按令转移，
   但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨十三段）**：`git diff 89f0455..a765f3d --
   src/` **完全为空**（R10 开轮以来全部提交均在 docs/ 下）。生产调用方
   三点位（supervisor.ts:483 / coordinator.ts:444 /
   flowchart-run.ts:320）、settle 调用点 601/711 不传 assessment
   （首行 `if (assessment === undefined) return`）、
   `parseTrackingAssessment` 唯一 src/ 调用方 events.ts:738、
   `runTrackingTurn` 唯一 src/ 调用方 from-child.ts:81（humanInput 恒
   `{}`）、`proposeFromAnomaly` / `sanitizePacketForAnalysis` /
   isolation 双函数 / `executionAuthority` **切片外零 src/ 调用方**
   （axis 11 零流量根据）、`hashSummary` 生产零流量（roller.ts:68
   唯一调用点），经本轮全库 grep 全部复验，无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               13.5      13.7      16.0  µs/gate (+1 event)
PASSED x contract(3)               13.1      13.0      13.7  µs/gate (+1 event)
FAILED x no-contract               13.9      13.9      14.4  µs/gate (+3 events)
FAILED x contract(3)               13.8      13.8      14.0  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.5      14.4      14.6  µs/gate (+3 events)
=> ~65–80 µs/run（稳态热；除 run3 首格 16.0 孤点外 ~65–73）
```

与 R21-A（13.2–19.1 µs/gate ⇒ 66–95、稳态 66–81）同带且更窄；五格三次
运行**无悬崖、无空洞**。run3 no-contract 首格 16.0 是 R11-A 以来
「PIC/基准次序」现象的本轮形态（run1/2 同格 13.5–13.7），单格横向比较
不具意义。稳态下即使把整个切片优化到零成本，节省上界 ~0.07–0.08
ms/run，仍比战役落地线（数十~数百 ms 或复杂度类下降）低**约三个
量级**。**支配论证（稳态口径）第二十次复核成立**；生产冷态口径维持
R17-A 轴 6b 裁决（[2.7, 8.5] ms/run once-per-run，不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R21-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R21-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1961–2034 ns 每次 ⇒ **5.88–6.10 µs** | 5.81–5.92 µs | ~43–47% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 326–383 ns + 第三次 hash） | **2.94–2.99 µs** | 2.91 µs | ~21–23% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.69–1.75 µs** | 1.82–2.10 µs | ~12–13% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 661–681 ns 每次 ⇒ **1.32–1.36 µs** | 1.40–1.82 µs | ~10% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 76–79 + seq 46–49 + 双幂等 find 38–43×2 + status 50–51 + `[...events]` 26–28 ns） | **276–290 ns** | 278–280 ns | **~2.0–2.2%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以当轮 contract 格（13.0–13.7 µs）计。`applyTrackingGate` 整体
隔离实测 5.93–6.01 µs，与 R21-A（5.77–5.90）同带略高。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次运行区间）：guard-3 skip（FAILED 零 evidence refs）1926–1984 ns
= 14.2–15.2% of apply（R21-A 13.0–19.5% 同带且更窄）；replay find#1
（GT idempotencyKey 命中，E=44）3207–3245 ns = 23.7–25.0% of apply
（R21-A 20.1–25.8% 同带）——组成天花板 {apply, skip, replay} 两个半边
继续成立，不重测其余格。

**本轮新轴 11：公开导出普查 + 分析-隔离面定价**（30 个可调用导出的
定价覆盖普查 + 7 个无价导出的首次定价，三次运行区间）：

```text
导出普查（14 文件 30 个可调用导出）:
  已有实测价（宿主路径或隔离格）23 个:
    applyTrackingGate / nextTrackingSeq / applyChildThreeLine /
    observationFromChild / assessChildObservation / shouldApplyThreeLine /
    prescoreInputFromObservation / runTrackingTurn / mergeOpenMinors /
    computePrescore / isSuccessClaim / combineScore / extractHumanScore /
    hasObviousHumanProblem / humanScoreValue / rollSummary / evaluateGates /
    hashAssessment / hashSummary / parseTrackingAssessment / evidenceWeight
    （S1-A-8）/ isUnobserved / isMachineScore（宿主路径内联价）
  从未定价 7 个（本轮全部补价，全部零生产流量）:
    sanitizePacketForAnalysis / proposeFromAnomaly（分析面双函数）
    bindExecutionContext / executionMayNotReadSummary / executionAuthority
    （隔离三函数）
    applyUserThreshold 成功臂（throw 终点 R18-A 已价）/ trackingConfig

sanitize 形态格（隔离，20 万次/格）——平坦，无规模维:
  production packet（bare）                      46–52 ns
  +actorDefense+actorIdentity                    43–45 ns
  +userText 1KB（trust 缺省）                    63–65 ns
  +toolBodies 16×256B（短命透传）                47–48 ns
  T=1000 操作窗口                                36–39 ns
  => 引用投影证人（F2b 700/700）：summary/contextFacts/toolSituations/
     evidenceRefs 按引用携带——sanitize 是 O(1) 字段投影，非拷贝

propose 生产形状（T=1，FAILED wake-arm packet）:
  全程                                           4007–4503 ns
  切片内（sanitize + content 构建）              682–700 ns（15.2–17.0%）
  registry createCandidate（D 切片腿，隔离）     2642–2708 ns
  content 耐久字节                               228 B
  => 生产形状下成本由 D 切片 registry 腿支配（内容 hash32 + boundary
     探针 + 校验 + 谱系走查）；切片内地板 ~0.7 µs 摊平候选撞 §0-1/2

操作基数斜率 T（切片内 content 构建）:
  T=   1   0.59–0.62 µs（221 B）
  T=  10   1.22–1.24 µs（482 B）
  T= 100   7.19–7.45 µs（3182 B）
  T=1000   66.55–67.42 µs（31082 B）
  => 66.0–66.9 ns/op 线性无悬崖；10 ms 穿越点 T ≈ 1.50–1.51×10⁵
     （生产 T=1——from-child 窗口恒单操作，差约 5 个量级）
  propose 全程 T=1000 现场                       238.28–250.51 µs
  V=100 现场（evidenceRefs）                     1.69–1.77 µs
     （vs V=2 0.59–0.62——R16-A V 族斜率在零流量面上的换位点）

隔离三函数 + config 双臂（ns 格）:
  bindExecutionContext 5.7–5.9 | executionMayNotReadSummary 6.3–6.6 |
  executionAuthority 5.6–8.5 ns
  applyUserThreshold 成功臂 17.3–19.0 | trackingConfig() 7.5–8.7 |
  trackingConfig(overrides) 11.5–11.9 ns

no-baseline 故障格（once-per-fault）:
  proposeFromAnomaly 无 active baseline throw    15.08–15.23 µs/fault
  （含 DomainValidationError 构造 + 栈；R18-A 拒绝面家族新格，
   生产 0 次/run）

生产死端引理（F2a，700 trial 三次逐位一致）: FAILED 格 352/352 在 turn
  内建 wake-arm packet（gate=codes[0]、H≡"UNOBSERVED"、summary 按引用、
  window 无 userText/toolBodies）；PASSED 干净格 348/348 不建；
  applyChildThreeLine 公开返回键恰 {events, result} 700/700 ⇒ packet
  从任何生产返回值不可达；proposeFromAnomaly 切片外零 src/ 调用方
契约存活面（F2b/F2c/F2d/F2e，三次逐位一致）: sanitize 剥除/缺省/透传/
  引用四契约；耐久内容键集精确 + operations 投影 {name, exitCode?} +
  五哨兵零泄漏 + H 序列化双态 + rollbackTarget 锚定；active 指针不动 +
  proposed 出生态 + kind 分类（prompt=true/security=false）+ 输入
  packet 逐位不变 + fail-closed throw 零效应；隔离真值表
  （恒等 × 3 directive；拒别名 + 拒 prescore/openMinors 键泄漏）
```

五个结论：

1. **导出格网闭合**：切片 30 个可调用导出自此**无一无价**——R21-A 关
   turn 输入参数格网（window / prescoreInput / gateFacts / humanInput
   四参数全价）之后，本轮把普查对象从「生产管线的维度」升到「公开面
   本身」：七个无价者全部是契约载体（分析桥、隔离载体、config 构造），
   全部零生产流量，全部 ns–µs 级。
2. **分析桥成本重心在切片外**：propose 生产形状 4.0–4.5 µs 中切片内
   仅 15.2–17.0%（0.68–0.70 µs——sanitize ~50 ns + content 构建
   ~0.6 µs），其余是 D 切片 registry 腿（2.6–2.7 µs）。「摊平切片内
   地板」的两条路（手写序列化、spread 单态化）分别撞 §0-1（S5-A-2/
   S1-E-3 族）与 §0-2（S8-E-2/S9-D-1 族）；「摊平 registry 腿」在
   切片外（§0-3/§0-4，D 区辖）。
3. **无规模乘数、无悬崖**：sanitize 对 packet 内容大小结构性平坦
   （T=1000 窗口反而最快——恒定五字段投影）；content 构建 T 斜率
   66.0–66.9 ns/op 线性，穿越 10 ms 需 T ≈ 1.5×10⁵（生产 T=1，差约
   5 个量级）；V 族斜率与 R16-A 同带。隔离/config 格无输入维。
4. **规格回声闭环**：F2d/F2e 把「分析只提案、永不晋升、永不改
   in-flight run」「执行只收 task context packet」两条三线规格行文
   钉为分布级可执行证人——active 指针在任意提案序列下不动、
   隔离三函数对任意 directive 恒等。这是二十二遍搜查史上第一次
   把规格的**权限边界条款**（而非行为条款）纳入证人集。
5. **不产生候选**：手写序列化 = §0-1；spread 单态化 = §0-2；parent
   双查去重 = §0-3（D 区 + X0-4）；boundary 探针跳过 = §0-4
   （S12-D-1 + D 区）；恒等函数删除 = §0-5；packet 记忆化 = X1-1；
   享元 = S1-A-7 族；throw 廉价化 = R18-A §0-3 整类。整面生产零流量 +
   仅幻想规模越线 ⇒ 按令空收口。

**重开条件全景更新（在 R21-A §1 表基础上新增三行）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| M（openMinors 条数） | 0 | ≈ 4.6–5.8×10⁴ | ~4+ | R20-A |
| minor-text（字节） | 0 | 无穿越点（结构性零斜率） | — | R20-A |
| K（驻留链 turn 数） | 0（恒 fresh） | ≈ 3×10³ turn | ~2 | R20-A |
| H_L / H_m / H_N（humanInput 三维） | 0 | ≈ 1.2 MB / 2.2–2.3×10⁴ / 4.65–4.74×10⁴ | ~3–5 | R21-A |
| **T（analysis content 操作基数）** | **0（面零流量；生产 packet T=1）** | **≈ 1.50–1.51×10⁵** | **~5** | **本轮** |
| **sanitize 形态维** | **0** | **无穿越点（结构性平坦：O(1) 引用投影）** | **—** | **本轮** |
| **隔离/config ns 格** | **0** | **无穿越点（无输入维）** | **—** | **本轮** |
| G（gates/run，caller 侧） | ~5 | ≈ 654–763 | ~2.1 | R16-A 锚点算术 |
| G 组成（apply/skip/replay 全混合） | 全 apply | 混入只降不升 | — | R18-A + R19-A |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–15.2 µs/fault（本轮 no-baseline 格并入上沿） | — | R18-A + 本轮 |
| 重投递面（once-per-redelivery） | 0 次/run | ~2.8–3.4 µs/次 | — | R19-A + 本轮抽测 |

C 轴（~2.5 个量级，R15-A）仍是切片内部最近的规模悬崖，第一优先重开
对象不变（S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）。本轮关闭的
是**公开面普查维**：R14–R16 关规模/载荷维、R17 关组成/温度维、R18 关
组成/故障维、R19 关结局维、R20 关驻留状态维、R21 关输入参数维之后，
切片的 30 个可调用导出自此**无一无价**——生产管线维度格网与公开导出
格网双双闭合。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 5 项 + R12-A…R21-A
48 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、数据
补充或本体，按「换名不铸 ID」纪律未列任何 S22-A-\* 候选。空枚举与
R12-A…R21-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第二十二遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `analysis.ts` | **axis 11 主宿主，二十二遍搜查史首次定价**（sanitize 形态格 36–65 ns 结构性平坦 + 引用投影证人 F2b；propose 生产形状 4.0–4.5 µs 中切片内仅 15.2–17.0%；T 斜率 66.0–66.9 ns/op 穿越 ~1.5×10⁵；no-baseline 故障格 15.1–15.2 µs once-per-fault）；耐久内容契约（F2c 键集精确 + operations 投影 + 五哨兵零泄漏 + rollbackTarget 锚定）；proposal-first 纯性（F2d active 指针不动 + kind 分类 + fail-closed）；手写序列化 = §0-1 拒列；spread 单态化 = §0-2 拒列；parent 双查/boundary 探针 = §0-3/4（D 区辖）拒列；S8-A-2 维持（sanitize choke point 零触碰） | 无新候选 |
| `isolation.ts` / `index.ts` | **隔离双函数首次定价**（bind 5.7–5.9 / mayNotRead 6.3–6.6 ns；F2e 真值表：恒等 + 拒别名 + 拒 prescore/openMinors 键泄漏——「Tracking 无命令权」分布级证人）；恒等函数删除 = §0-5 拒列（规格契约载体）；index.ts 纯再导出零逻辑；惰性 import = R12-A §0-10 维持 | 无新候选 |
| `gate-apply.ts` | **executionAuthority 首次定价**（5.6–8.5 ns 恒等，对任意 directive/summary 文本不敏感 F2e——命令权隔离在 run 侧的载体）；replay find#1 抽测 3207–3245 ns 同带复现（R19-A 8a 维持）；S11-A-2 / S8-A-1 / S2-A-4 / R15-A §0-1 维持 | 无新候选 |
| `config.ts` | **构造双臂首次定价**（applyUserThreshold 成功臂 17.3–19.0 ns——throw 终点 R18-A 已价；trackingConfig 缺省 7.5–8.7 / overrides 11.5–11.9 ns）；X0-7 阈值冻结维持（softThreshold 0.55 零触碰） | 无新候选 |
| `turn.ts` | **wake-arm packet 生产死端引理证人**（F2a：FAILED 格 352/352 建 packet 且 gate=codes[0]、summary 按引用——packet 构建价已在 R13-A turn 剖面内，本轮不重定价）；本轮整体 1.69–1.75 µs 同带；R20-A §0-3 / R21-A H 面 / R12-A §0-11 / R16-A §0-2 维持 | 无新候选 |
| `from-child.ts` | **死端引理下游宿主**（decision.turn.packet 从 applyChildThreeLine 返回值不可达——生产消费者只取 assessment；humanInput 恒 `{}` 第十三次复验）；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `child-tracking.ts` | **消费者返回键证人**（F2a 700/700 恰 {events, result}——packet 生产不可达的钉子）；guard-3 skip 抽测 1926–1984 ns 复现（R18-A 7a 维持）；skip 守卫次序/享元 = R18-A §0-1/2 维持 | 无新候选 |
| `types.ts` | AnomalyPacket/AnalysisPacketInput 类型面 = axis 11 契约存活面的载体（TrustTag 缺省语义 F2b 钉死）；写侧 parse / 哈希双旁路 / M 轴 / H 变体面全部维持（R20-A / R21-A 轴不重测）；S2-A-5 / X1-2 / S5-H-3 / S1-A-8 / X0-5 维持 | 无新候选 |
| `gates.ts` | wakeAnalysis 布尔面 = axis 11 上游开关（hard/soft 恒 true、none 恒 false——F2a 分布即其证人）；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 维持 | 无新候选 |
| `prescore.ts` | 本轮零新角度（axis 11 不触 prescore）；C/R 轴维持 R15-A 穿越点；S1-A-4 / X4-7 / S6-A-1 / S8-A-3 / R12-A §0-2..5 维持 | 无新候选 |
| `roller.ts` | packet.summary 按引用携带（F2a——roller 产物进 packet 零拷贝）；R20-A previous 面 / R17-A O(Q·D) / S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（humanInput `{}` ⇒ packet.H ≡ "UNOBSERVED"，F2a/F2c 序列化双态证人）；R21-A H 面轴不补铸；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 维持 | 无新候选 |
| `combined-score.ts` | 本轮零新角度（axis 11 不触 combineScore）；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |

**对 R21-A 声明的裁决**：输入参数维（H 面）收口在其维度上成立；R21-A
结语「输入格、结局格、状态格三张格网无一格无价」量化的是**生产管线**
维度——公开导出面本身作为普查对象不在其中，本轮补上并闭合（30/30
导出全价）。本轮全部新数据**只产出归档证人与既有拒绝记录的数据补充，
未产出可落地候选**。收口的预言力第二十二遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R21-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F2a: 700 trials, 700 applied; FAILED w/ packet=352, PASSED clean w/o=348;
     consumer keys {events,result} 700/700
F2b: 700 dirty packets; dropped=536, trust-defaulted=167, preserved=177;
     reference projection 700/700
F2c: content keys exact; ops projected; 5 sentinels absent; H dual
     serialization; rollbackTarget anchored
F2d: pointer immobile; proposed; prompt=true/security=false; packet
     unchanged; no-baseline throw fail-closed
F2e: isolation truth table exact (identity x3 directives; alias +
     prescore/openMinors rejects)
anchor: 13.5 / 13.1 / 13.9 / 13.8 / 14.5 µs/gate => ~65–72 µs/run
decomposition: hash×3=6031ns | validateEvent=2990ns | turn=1686ns
    | prescore×2=1321ns | 5 scans+copy=276ns | applyTrackingGate=5.93µs
spot: skip guard-3 1984ns (15.2%) | replay find#1 3207ns (24.5%)
axis11 sanitize: bare=50 | +def+id=43 | +userText1KB=63 | +bodies=47
    | T=1000=36 ns
axis11 propose T=1: full=4503 | in-slice=683 (15.2%) | registry=2708 ns
    | content 228 B
axis11 T: 0.62/1.24/7.45/67.42 µs => 66.9 ns/op; crossing T ≈ 149,537
axis11 propose full T=1000: 250.51µs | V=100: 1.77µs
axis11 isolation: 5.8/6.4/8.5 ns | config: 19.0/7.5/11.9 ns
axis11 fault: 15230 ns
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F2a/F2b/F2c/F2d/F2e: 计数逐位同 run 1
anchor: 13.7 / 13.0 / 13.9 / 13.8 / 14.4 µs/gate => ~65–72 µs/run
decomposition: hash×3=6102ns | validateEvent=2937ns | turn=1748ns
    | prescore×2=1361ns | 5 scans+copy=290ns | applyTrackingGate=5.96µs
spot: skip guard-3 1926ns (14.8%) | replay find#1 3245ns (25.0%)
axis11 sanitize: 52/43/65/48/39 ns
axis11 propose T=1: full=4007 | in-slice=682 (17.0%) | registry=2664 ns
axis11 T: 0.59/1.22/7.19/66.55 µs => 66.0 ns/op; crossing T ≈ 151,441
axis11 propose full T=1000: 243.11µs | V=100: 1.70µs
axis11 isolation: 5.9/6.6/5.7 ns | config: 18.1/8.7/11.5 ns
axis11 fault: 15077 ns
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F2a/F2b/F2c/F2d/F2e: 计数逐位同 run 1
anchor: 16.0 / 13.7 / 14.4 / 14.0 / 14.6 µs/gate => ~68–80 µs/run
decomposition: hash×3=5883ns | validateEvent=2994ns | turn=1745ns
    | prescore×2=1322ns | 5 scans+copy=280ns | applyTrackingGate=6.01µs
spot: skip guard-3 1942ns (14.2%) | replay find#1 3230ns (23.7%)
axis11 sanitize: 46/45/64/48/36 ns
axis11 propose T=1: full=4290 | in-slice=700 (16.3%) | registry=2642 ns
axis11 T: 0.60/1.22/7.21/66.99 µs => 66.5 ns/op; crossing T ≈ 150,469
axis11 propose full T=1000: 238.28µs | V=100: 1.69µs
axis11 isolation: 5.7/6.3/5.6 ns | config: 17.3/8.3/11.7 ns
axis11 fault: 15108 ns
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立仿真运行 F2a–F2e 计数逐位一致（700 trial 分布、700 dirty
packet 分布、耐久内容/纯性/真值表全部确定性重现）；锚点五格三次同带
无悬崖（run3 首格孤点已归因 PIC/次序）；axis 11 五格 sanitize + 三格
propose 分解 + 四格 T 斜率 + 两格现场 + 三格隔离 + 三格 config +
一格故障三次运行区间紧。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S22-A-\*）。换名检查 5 项新记录见 §0（1–5），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件本轮**新增一条收口**：

**公开导出普查（export census）收口**：切片 30 个可调用导出自此全部
有实测价——七个末批定价者是 sanitize（36–65 ns 结构性平坦引用投影）、
propose（生产形状 4.0–4.5 µs，切片内 15.2–17.0%，T 斜率 66.0–66.9
ns/op 穿越 ~1.5×10⁵）、隔离三函数（5.6–8.5 ns 恒等/真值表）、config
双臂（7.5–19.0 ns）、no-baseline 故障格（15.1–15.2 µs once-per-fault）。
重开条件：(a) 任何 src/ 生产调用方开始把 turn.packet 接线到
proposeFromAnomaly（adapt 平面接线，届时最先越线的是 D 切片 registry
腿而非切片内 0.7 µs 地板，候选先撞 §0-1..4 四重既有拒绝），或
(b) from-child 窗口操作基数 T 增长 ≥3 个量级（content 构建承载项），
或 (c) 隔离三函数获得生产调用方（当前为规格契约载体，删除/内联 =
§0-5 拒列不变）。C 轴（~2.5 个量级，R15-A）仍是切片内部最近的规模
悬崖，第一优先重开对象不变（S1-A-4/X4-7 家族 `every×includes` 站点
Set 化）。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf22a01`–`0xf22a02`。

```ts
/**
 * R22-A deterministic anchor + decomposition + NEW-axis simulation
 * (twenty-second pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R21-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R21-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation, 5-scan
 *       O(E) inventory), plus compact spot re-confirmations of the R18-A
 *       skip pricing (guard-3) and the R19-A replay pricing (find#1) for
 *       composition-ceiling continuity;
 *   (c) NEW axis 11: the EXPORT CENSUS face. Twenty-one passes priced the
 *       production pipeline along every input/outcome/state/composition
 *       dimension, but the slice's public export list was never itself
 *       audited for pricing coverage. A full census of the 30 callable
 *       exports across the 14 files finds exactly seven that have NEVER
 *       carried a measured price in R1-A..R21-A: the analysis-plane pair
 *       (sanitizePacketForAnalysis / proposeFromAnomaly -- the
 *       spec-mandated packet->candidate pipe, S8-A-2 recorded only the
 *       81-107ns collapse delta, never the face), the isolation trio
 *       (bindExecutionContext / executionMayNotReadSummary in
 *       isolation.ts, executionAuthority in gate-apply.ts -- the
 *       executable carriers of the "Tracking has no command authority"
 *       line of the spec), and the config construction arms
 *       (applyUserThreshold success arm -- its throw endpoint was priced
 *       by R18-A -- and trackingConfig). Production flow for all seven is
 *       zero BY GREP (no src/ caller outside the slice; the wake-arm
 *       AnomalyPacket that runTrackingTurn builds on every hard/soft gate
 *       is production-constructed but production-DEAD at the consumer:
 *       applyChildThreeLine returns only {events, result}, so the packet
 *       is unreachable from every production return value), but the face
 *       is contract-live: proposeFromAnomaly is the only
 *       packet->candidate bridge the spec allows, and the isolation trio
 *       is called by tests as the spec's executable documentation. This
 *       axis:
 *       (i)   proves the production dead-end lemma distribution-level
 *             (F2a): FAILED cells build a packet inside the turn, PASSED
 *             clean cells do not, and the production consumer's return
 *             value never carries it;
 *       (ii)  pins the sanitize contract (F2b): actorDefense/actorIdentity
 *             dropped, userTextTrust defaults to UNTRUSTED_TEXT, explicit
 *             trust preserved, toolBodies pass through ephemerally, and
 *             summary/contextFacts/toolSituations/toolBodies are carried
 *             BY REFERENCE (sanitize is O(1) projection, no deep copy);
 *       (iii) pins the durable-content contract (F2c): candidate content
 *             carries exactly {gate, score, P, H, evidenceRefs,
 *             anomalyCodes, operations, rollbackTarget}, operations are
 *             projected to {name, exitCode?} only, and none of the
 *             defense/identity/toolBodies/userText/aiText sentinel bytes
 *             leak into the durable string (injection-resistance echo);
 *       (iv)  pins proposal-first purity (F2d): the active pointer never
 *             moves, the candidate is born "proposed", autoPromotable
 *             follows the kind classification (prompt true / security
 *             false), the input packet is not mutated, and the
 *             no-baseline path throws DomainValidationError fail-closed;
 *       (v)   pins the isolation-face truth table (F2e): identity
 *             semantics for bindExecutionContext / executionAuthority and
 *             the three-probe reject table for executionMayNotReadSummary
 *             -- the "analysis never mutates the in-flight run / tracking
 *             has no command authority" spec echoes;
 *       (vi)  prices the face: per-call sanitize lattice (shape variants,
 *             expected flat -- reference projection), proposeFromAnomaly
 *             full vs in-slice content-build vs registry (D-slice)
 *             remainder at the production packet shape (T=1), the
 *             operations-cardinality slope T in {1,10,100,1000} with its
 *             10ms crossing, an evidenceRefs V spot cell, the isolation
 *             trio and config arms as ns cells, and the no-baseline throw
 *             as a once-per-fault cell.
 *
 * Archival-fact fuzz:
 *   F2a: production dead-end lemma. Seeded fuzz over from-child shapes:
 *        FAILED-driven applied decisions all carry turn.packet (wake-arm
 *        construction is production-live), PASSED clean cells never do,
 *        and applyChildThreeLine's public return value exposes exactly
 *        {events, result} -- no packet reachable.
 *   F2b: sanitize contract. 700 seeded dirty packets: keys, trust
 *        defaulting, reference identity, no defense/identity survivors.
 *   F2c: durable content contract. Sentinel-laden packets: exact key set,
 *        operations projection, sentinel absence, H serialization
 *        (UNOBSERVED string vs number), rollbackTarget anchoring.
 *   F2d: proposal-first purity. Active pointer immobile, status/
 *        autoPromotable classification, packet immutability, fail-closed
 *        no-baseline throw.
 *   F2e: isolation truth table. Identity + three-probe rejects.
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf22a01..0xf22a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { IsoTimestamp } from "/workspace/src/domain/timestamp.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import { ResourceRegistry } from "/workspace/src/adaptation/registry.js";
import type { ResourceIdentity } from "/workspace/src/adaptation/resource.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  proposeFromAnomaly,
  sanitizePacketForAnalysis,
  type AnalysisPacketInput
} from "/workspace/src/tracking/analysis.js";
import {
  bindExecutionContext,
  executionMayNotReadSummary
} from "/workspace/src/tracking/isolation.js";
import {
  applyUserThreshold,
  trackingConfig,
  DEFAULT_TRACKING_CONFIG
} from "/workspace/src/tracking/config.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import {
  applyTrackingGate,
  executionAuthority,
  nextTrackingSeq
} from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type AnomalyPacket,
  type RollingSummary,
  type ToolSituation,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary = opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran";
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

function gateCommandFor(
  kind: "PASSED" | "FAILED",
  table: readonly Event[],
  opts?: { taskId?: string }
): { assessment: TrackingAssessment; hash: string; seq: number } {
  const mk = mkChild(kind, { taskId: opts?.taskId });
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({
    observation: obs,
    episodeId: "ep_bench",
    runId: String(RUN_ID)
  });
  if (!decision.apply) throw new Error("fixture must apply");
  return {
    assessment: decision.assessment,
    hash: hashAssessment(decision.assessment),
    seq: nextTrackingSeq(table)
  };
}

/* ============================================================
 * Registry fixtures for the analysis face (deterministic ids).
 * ============================================================ */
let idCounter = 0;
function mkRegistry(kind: ResourceIdentity["kind"]): {
  registry: ResourceRegistry;
  identity: ResourceIdentity;
  baselineVersionId: string;
} {
  const registry = new ResourceRegistry({
    now: () => NOW_ISO as IsoTimestamp,
    generateId: () => `r22a${String(idCounter++).padStart(12, "0")}`
  });
  const identity: ResourceIdentity = {
    kind,
    name: "tracking-analysis-face",
    scope: { kind: "user-global" }
  };
  const baseline = registry.registerBaseline({
    identity,
    content: `baseline ${kind} v0`,
    author: { kind: "human", identity: "owner" }
  });
  return { registry, identity, baselineVersionId: String(baseline.versionId) };
}

function productionPacket(): AnomalyPacket {
  const mk = mkChild("FAILED", { taskId: "tsk_packet" });
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({
    observation: obs,
    episodeId: "ep_bench",
    runId: String(RUN_ID)
  });
  if (!decision.apply || decision.turn.packet === undefined) {
    throw new Error("FAILED fixture must build a wake-arm packet");
  }
  return decision.turn.packet;
}

function syntheticPacket(opts: {
  T: number;
  V?: number;
  userText?: string;
  aiText?: string;
  toolBodies?: readonly string[];
}): AnomalyPacket {
  const ops: ToolSituation[] = Array.from({ length: opts.T }, (_, i) => ({
    name: `op-${i}`,
    exitCode: i % 3 === 0 ? 1 : 0,
    wrote: i % 2 === 0,
    escaped: false,
    artifactIds: [`art_${i}`],
    evidenceIds: [`evd_${i}`],
    hashes: []
  }));
  const evidenceRefs = Array.from({ length: opts.V ?? 2 }, (_, i) => `evd_${i}`);
  const summary: RollingSummary = {
    schemaVersion: 1,
    constraints: [
      { id: "con_0", text: "constraint 0 text", kind: "constraint", mandatory: true }
    ],
    unresolvedQuestions: [],
    confirmedDecisions: [],
    operations: ops,
    prescore: 0.3,
    human: { kind: "unobserved" },
    score: 0.3,
    anomalyCodes: ["deterministic-fail"],
    evidenceRefs,
    openMinors: [],
    omissions: [],
    failClosed: false
  };
  return {
    summary,
    window: {
      contextFacts: ["role tester", "task tsk_synth"],
      toolSituations: ops,
      ...(opts.userText !== undefined ? { userText: opts.userText } : {}),
      ...(opts.aiText !== undefined ? { aiText: opts.aiText } : {}),
      ...(opts.toolBodies !== undefined ? { toolBodies: opts.toolBodies } : {})
    },
    P: 0.3,
    H: "UNOBSERVED",
    score: 0.3,
    gate: "deterministic-fail",
    evidenceRefs
  };
}

/** The in-slice portion of proposeFromAnomaly: sanitize + content build. */
function inSliceContentBuild(packet: AnalysisPacketInput, rollbackTarget: string): string {
  const safe = sanitizePacketForAnalysis(packet);
  return JSON.stringify({
    gate: safe.gate,
    score: safe.score,
    P: safe.P,
    H: safe.H,
    evidenceRefs: safe.evidenceRefs,
    anomalyCodes: safe.summary.anomalyCodes,
    operations: safe.window.toolSituations.map((tool) => ({
      name: tool.name,
      ...(tool.exitCode !== undefined ? { exitCode: tool.exitCode } : {})
    })),
    rollbackTarget
  });
}

/* ============================================================
 * F2a: production dead-end lemma. The wake-arm packet is built on
 * every production hard/soft gate but no production return value
 * carries it; proposeFromAnomaly has zero src/ callers (grep).
 * ============================================================ */
{
  const rng = mulberry32(0xf22a01);
  let applied = 0;
  let failedWithPacket = 0;
  let passedCleanNoPacket = 0;
  for (let trial = 0; trial < 700; trial += 1) {
    const kind = pick(rng, ["PASSED", "FAILED"] as const);
    const withGap = rng() < 0.25;
    const mk = mkChild(kind, { taskId: `tsk_f_${trial}`, claimSuccessWithCheckGap: withGap });
    const obs = observationFromChild(mk.child, mk.spec, rng() < 0.5 ? mk.contract : undefined);
    const decision = assessChildObservation({
      observation: obs,
      episodeId: "ep_bench",
      runId: String(RUN_ID)
    });
    if (decision.apply) {
      applied += 1;
      const packet = decision.turn.packet;
      if (kind === "FAILED") {
        // deterministic-fail hard gate => wakeAnalysis => packet built
        check(`F2a FAILED cell builds packet trial ${trial}`, packet !== undefined);
        if (packet !== undefined) {
          failedWithPacket += 1;
          check(
            `F2a packet shape trial ${trial}`,
            packet.gate === decision.turn.gate.codes[0] &&
              packet.H === "UNOBSERVED" &&
              packet.summary === decision.turn.summary &&
              packet.window.userText === undefined &&
              packet.window.toolBodies === undefined
          );
        }
      } else {
        // PASSED clean production cells: gate none => no packet
        check(`F2a PASSED clean cell has no packet trial ${trial}`, packet === undefined);
        if (packet === undefined) passedCleanNoPacket += 1;
      }
    }
    // the production consumer's public return value carries no packet
    let c = 100;
    const out = applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(rng() < 0.5 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
    const keys = Object.keys(out).sort().join(",");
    check(`F2a consumer return keys trial ${trial}`, keys === "events,result", keys);
  }
  console.log(
    `F2a: 700 fuzz trials, ${applied} applied; FAILED cells with wake-arm packet=${failedWithPacket}, ` +
      `PASSED clean cells without packet=${passedCleanNoPacket}; applyChildThreeLine exposes exactly ` +
      `{events, result} on every trial -- the packet is production-constructed but production-DEAD ` +
      `(proposeFromAnomaly has zero src/ callers; the analysis face is contract-only)`
  );
}

/* ============================================================
 * F2b: sanitize contract (drop/default/reference semantics).
 * ============================================================ */
{
  const rng = mulberry32(0xf22a02);
  let dropped = 0;
  let defaulted = 0;
  let preserved = 0;
  for (let trial = 0; trial < 700; trial += 1) {
    const withDefense = rng() < 0.5;
    const withIdentity = rng() < 0.5;
    const withUserText = rng() < 0.5;
    const withTrust = withUserText && rng() < 0.5;
    const withAiText = rng() < 0.5;
    const withBodies = rng() < 0.5;
    const base = syntheticPacket({
      T: 1 + Math.floor(rng() * 3),
      ...(withUserText ? { userText: `user says trial ${trial}` } : {}),
      ...(withAiText ? { aiText: `ai says trial ${trial}` } : {}),
      ...(withBodies ? { toolBodies: [`body ${trial}`] } : {})
    });
    const dirty: AnalysisPacketInput = {
      ...base,
      ...(withUserText && withTrust
        ? { window: { ...base.window, userTextTrust: "FACT" as const } }
        : {}),
      ...(withDefense ? { actorDefense: `defense ${trial}` } : {}),
      ...(withIdentity ? { actorIdentity: `worker-${trial}` } : {})
    };
    const safe = sanitizePacketForAnalysis(dirty);
    check(
      `F2b defense/identity dropped trial ${trial}`,
      !("actorDefense" in safe) && !("actorIdentity" in safe)
    );
    if (withDefense || withIdentity) dropped += 1;
    if (withUserText) {
      const trust = (safe.window as { userTextTrust?: string }).userTextTrust;
      if (withTrust) {
        check(`F2b explicit trust preserved trial ${trial}`, trust === "FACT");
        preserved += 1;
      } else {
        check(`F2b trust defaults UNTRUSTED_TEXT trial ${trial}`, trust === "UNTRUSTED_TEXT");
        defaulted += 1;
      }
    } else {
      check(
        `F2b absent userText leaves no keys trial ${trial}`,
        !("userText" in safe.window) && !("userTextTrust" in safe.window)
      );
    }
    check(
      `F2b toolBodies ephemeral pass-through trial ${trial}`,
      withBodies
        ? safe.window.toolBodies === dirty.window.toolBodies
        : !("toolBodies" in safe.window)
    );
    check(
      `F2b reference projection trial ${trial}`,
      safe.summary === dirty.summary &&
        safe.window.contextFacts === dirty.window.contextFacts &&
        safe.window.toolSituations === dirty.window.toolSituations &&
        safe.evidenceRefs === dirty.evidenceRefs
    );
  }
  console.log(
    `F2b: 700 dirty packets sanitized; defense/identity dropped on ${dropped} carriers, ` +
      `trust defaulted UNTRUSTED_TEXT on ${defaulted}, explicit trust preserved on ${preserved}; ` +
      `summary/contextFacts/toolSituations/evidenceRefs carried by reference on 700/700 ` +
      `-- sanitize is O(1) field projection, not a copy`
  );
}

/* ============================================================
 * F2c: durable content contract (exact keys, operations projection,
 * sentinel absence, H serialization, rollback anchoring).
 * ============================================================ */
{
  const SENTINELS = {
    defense: "SENTINEL_DEFENSE_a4b1",
    identity: "SENTINEL_IDENTITY_c7d2",
    body: "SENTINEL_TOOLBODY_e9f3",
    userText: "SENTINEL_USERTEXT_1122",
    aiText: "SENTINEL_AITEXT_3344"
  };
  const { registry, identity, baselineVersionId } = mkRegistry("prompt");
  const dirty: AnalysisPacketInput = {
    ...syntheticPacket({
      T: 3,
      userText: SENTINELS.userText,
      aiText: SENTINELS.aiText,
      toolBodies: [SENTINELS.body]
    }),
    actorDefense: SENTINELS.defense,
    actorIdentity: SENTINELS.identity
  };
  const candidate = proposeFromAnomaly({ packet: dirty, registry, identity });
  const content = registry.getContent(candidate.contentHash);
  check("F2c content stored", content !== undefined);
  if (content !== undefined) {
    const parsed = JSON.parse(content) as Record<string, unknown>;
    const keys = Object.keys(parsed).sort().join(",");
    check(
      "F2c content key set exact",
      keys === "H,P,anomalyCodes,evidenceRefs,gate,operations,rollbackTarget,score",
      keys
    );
    const ops = parsed.operations as Array<Record<string, unknown>>;
    check(
      "F2c operations projected to {name, exitCode?}",
      ops.length === 3 &&
        ops.every((op) => {
          const opKeys = Object.keys(op).sort().join(",");
          return opKeys === "exitCode,name" || opKeys === "name";
        })
    );
    for (const [label, sentinel] of Object.entries(SENTINELS)) {
      check(`F2c sentinel absent from durable content [${label}]`, !content.includes(sentinel));
    }
    check("F2c H UNOBSERVED serialization", parsed.H === "UNOBSERVED");
    check("F2c rollback anchoring", parsed.rollbackTarget === baselineVersionId);
  }
  // numeric-H variant
  const numericPacket: AnomalyPacket = { ...syntheticPacket({ T: 1 }), H: 0.3 };
  const candidate2 = proposeFromAnomaly({ packet: numericPacket, registry, identity });
  const content2 = registry.getContent(candidate2.contentHash);
  check(
    "F2c numeric H serialization",
    content2 !== undefined && (JSON.parse(content2) as { H: unknown }).H === 0.3
  );
  console.log(
    "F2c: durable candidate content carries exactly {gate,score,P,H,evidenceRefs,anomalyCodes," +
      "operations,rollbackTarget}; operations projected to {name,exitCode?}; all five sentinel " +
      "strings (defense/identity/toolBody/userText/aiText) absent from the durable bytes; " +
      "H serializes as 'UNOBSERVED' | number; rollbackTarget === active baseline"
  );
}

/* ============================================================
 * F2d: proposal-first purity + kind classification + fail-closed.
 * ============================================================ */
{
  const { registry, identity, baselineVersionId } = mkRegistry("prompt");
  const packet = productionPacket();
  const packetSnapshot = JSON.stringify(packet);
  const before = registry.getActiveVersion(identity)?.versionId;
  const candidate = proposeFromAnomaly({ packet, registry, identity });
  const after = registry.getActiveVersion(identity)?.versionId;
  check("F2d active pointer immobile", String(before) === String(after) && String(after) === baselineVersionId);
  check("F2d candidate born proposed", candidate.status === "proposed");
  check("F2d prompt kind autoPromotable classification", candidate.autoPromotable === true);
  check("F2d parent is baseline", String(candidate.parentVersionId) === baselineVersionId);
  check("F2d input packet not mutated", JSON.stringify(packet) === packetSnapshot);

  const sec = mkRegistry("security");
  const secCandidate = proposeFromAnomaly({ packet, registry: sec.registry, identity: sec.identity });
  check("F2d security kind never auto-promotable", secCandidate.autoPromotable === false);
  check("F2d security candidate still proposal-first", secCandidate.status === "proposed");

  // fail-closed: no active baseline => throw, registry untouched
  const bare = new ResourceRegistry({
    now: () => NOW_ISO as IsoTimestamp,
    generateId: () => `r22a${String(idCounter++).padStart(12, "0")}`
  });
  const bareIdentity: ResourceIdentity = {
    kind: "prompt",
    name: "no-baseline",
    scope: { kind: "user-global" }
  };
  let threw = false;
  try {
    proposeFromAnomaly({ packet, registry: bare, identity: bareIdentity });
  } catch (error) {
    threw = error instanceof DomainValidationError;
  }
  check("F2d no-baseline throws DomainValidationError", threw);
  check("F2d bare registry untouched", bare.getActiveVersion(bareIdentity) === undefined);
  console.log(
    "F2d: active pointer immobile across proposals; candidates born 'proposed'; autoPromotable " +
      "follows kind classification (prompt=true, security=false); input packet bit-unchanged; " +
      "no-baseline path throws DomainValidationError fail-closed with zero registry effect"
  );
}

/* ============================================================
 * F2e: isolation-face truth table (spec echoes, zero command authority).
 * ============================================================ */
{
  const packet = { requiredFacts: ["fact"], relevantFiles: [] } as unknown as Parameters<
    typeof bindExecutionContext
  >[0];
  const summary = productionPacket().summary;
  check("F2e bindExecutionContext identity", bindExecutionContext(packet, summary) === packet);
  check("F2e bindExecutionContext identity (no summary)", bindExecutionContext(packet) === packet);
  check("F2e mayNotRead disjoint packet", executionMayNotReadSummary({ taskId: "t" }, summary) === true);
  check(
    "F2e mayNotRead rejects summary aliasing",
    executionMayNotReadSummary(summary as unknown as object, summary) === false
  );
  check(
    "F2e mayNotRead rejects prescore leak",
    executionMayNotReadSummary({ prescore: 0.5 }, summary) === false
  );
  check(
    "F2e mayNotRead rejects openMinors leak",
    executionMayNotReadSummary({ openMinors: [] }, summary) === false
  );
  const ctx = { taskId: "tsk_1" };
  for (const directive of ["none", "queue_analysis", "wait_user"] as const) {
    check(
      `F2e executionAuthority identity under ${directive}`,
      executionAuthority({
        taskContext: ctx,
        supervisorDirective: directive,
        rollingSummaryText: "summary text"
      }) === ctx
    );
  }
  console.log(
    "F2e: isolation trio truth table exact -- bindExecutionContext/executionAuthority are " +
      "identity on the task context under every directive; executionMayNotReadSummary rejects " +
      "aliasing and prescore/openMinors key leaks -- 'tracking has no command authority' echoes"
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R21-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    anchorUs.push(one * 1e3);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition of the PASSED x contract(3) cell
 * (R12-A..R21-A methodology, 5-scan O(E) inventory) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)})`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const cmdGT = gateCommandFor("FAILED", events41, { taskId: "tsk_replay_gt" });
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: cmdGT.assessment,
      assessmentHash: cmdGT.hash,
      expectedSeq: cmdGT.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: cmdGT.assessment,
        assessmentHash: cmdGT.hash,
        expectedSeq: cmdGT.seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }
}

/* ============================================================
 * NEW axis 11: the export-census face (analysis + isolation + config).
 * ============================================================ */
{
  console.log("--- NEW axis 11: sanitizePacketForAnalysis shape lattice (expect flat) ---");
  const prodPacket = productionPacket();
  const bodies16 = Array.from({ length: 16 }, (_, i) => `tool body ${i} `.repeat(16));
  const sanitizeCells: Array<{ label: string; input: AnalysisPacketInput }> = [
    { label: "production packet (bare)", input: prodPacket },
    {
      label: "+actorDefense+actorIdentity",
      input: { ...prodPacket, actorDefense: "defense text", actorIdentity: "worker-7" }
    },
    {
      label: "+userText 1KB (trust default)",
      input: syntheticPacket({ T: 1, userText: "u".repeat(1024) })
    },
    {
      label: "+toolBodies 16x256B (ephemeral)",
      input: syntheticPacket({ T: 1, toolBodies: bodies16 })
    },
    { label: "T=1000 operations window", input: syntheticPacket({ T: 1000 }) }
  ];
  for (const cell of sanitizeCells) {
    const t = bench(() => void sanitizePacketForAnalysis(cell.input), 200000);
    console.log(`  sanitize [${cell.label}]: ${(t * 1e6).toFixed(0)}ns`);
  }

  console.log("--- NEW axis 11: proposeFromAnomaly (production shape T=1) ---");
  const { registry, identity, baselineVersionId } = mkRegistry("prompt");
  const tFull1 = bench(
    () => void proposeFromAnomaly({ packet: prodPacket, registry, identity }),
    20000
  );
  const tInSlice1 = bench(() => void inSliceContentBuild(prodPacket, baselineVersionId), 100000);
  const content1 = inSliceContentBuild(prodPacket, baselineVersionId);
  const tRegistryOnly = bench(
    () =>
      void registry.createCandidate({
        identity,
        content: content1,
        parentVersionId: registry.getActiveVersion(identity)!.versionId,
        author: { kind: "detector", identity: "tracking-analysis" },
        evaluationPlan: { stages: ["static", "replay"], metrics: ["utility", "safety"], planVersion: 1 }
      }),
    20000
  );
  console.log(
    `  propose full: ${(tFull1 * 1e6).toFixed(0)}ns | in-slice sanitize+content-build: ${(tInSlice1 * 1e6).toFixed(0)}ns (${((tInSlice1 / tFull1) * 100).toFixed(1)}%) | registry createCandidate (D-slice): ${(tRegistryOnly * 1e6).toFixed(0)}ns | content bytes: ${content1.length}`
  );

  console.log("--- NEW axis 11: operations-cardinality slope T (in-slice content build) ---");
  const tCells = [1, 10, 100, 1000] as const;
  const tUs: number[] = [];
  for (const T of tCells) {
    const packet = syntheticPacket({ T });
    const reps = T >= 1000 ? 2000 : T >= 100 ? 20000 : 100000;
    const t = bench(() => void inSliceContentBuild(packet, baselineVersionId), reps);
    tUs.push(t * 1e3);
    const bytes = inSliceContentBuild(packet, baselineVersionId).length;
    console.log(`  content-build T=${String(T).padStart(4)}: ${(t * 1e3).toFixed(2)}us (${bytes} bytes)`);
  }
  const tSlopeNs = ((tUs[3]! - tUs[0]!) * 1e3) / (1000 - 1);
  // once-per-anomaly-proposal; 10 ms budget for a single proposal call
  const crossT = ((10000 - tUs[0]!) * 1e3) / tSlopeNs;
  console.log(
    `  => slope ~${tSlopeNs.toFixed(1)} ns/op; 10ms crossing at T ~= ${Math.round(crossT).toLocaleString("en-US")} (production T=1, zero flow)`
  );
  {
    const packet1000 = syntheticPacket({ T: 1000 });
    const tFullT = bench(
      () => void proposeFromAnomaly({ packet: packet1000, registry, identity }),
      2000
    );
    console.log(`  propose full T=1000 spot: ${(tFullT * 1e3).toFixed(2)}us`);
  }
  {
    const packetV = syntheticPacket({ T: 1, V: 100 });
    const tV = bench(() => void inSliceContentBuild(packetV, baselineVersionId), 50000);
    console.log(
      `  content-build V=100 spot: ${(tV * 1e3).toFixed(2)}us (vs V=2 ${(tUs[0]!).toFixed(2)}us -- R16-A V-family slope on a zero-flow face)`
    );
  }

  console.log("--- NEW axis 11: isolation trio + config arms (ns cells) ---");
  const packetCtx = { requiredFacts: ["fact"], relevantFiles: [] } as unknown as Parameters<
    typeof bindExecutionContext
  >[0];
  const summary = prodPacket.summary;
  const tBind = bench(() => void bindExecutionContext(packetCtx, summary), 200000);
  const tMayNot = bench(() => void executionMayNotReadSummary({ taskId: "t" }, summary), 200000);
  const ctx = { taskId: "tsk_1" };
  const tAuthority = bench(
    () =>
      void executionAuthority({
        taskContext: ctx,
        supervisorDirective: "none",
        rollingSummaryText: "text"
      }),
    200000
  );
  const tThreshold = bench(() => void applyUserThreshold(DEFAULT_TRACKING_CONFIG, 0.6), 200000);
  const tConfigDefaults = bench(() => void trackingConfig(), 200000);
  const tConfigOverrides = bench(() => void trackingConfig({ softThreshold: 0.6, minorPDip: 0.05 }), 200000);
  console.log(
    `  bindExecutionContext: ${(tBind * 1e6).toFixed(1)}ns | executionMayNotReadSummary: ${(tMayNot * 1e6).toFixed(1)}ns | executionAuthority: ${(tAuthority * 1e6).toFixed(1)}ns`
  );
  console.log(
    `  applyUserThreshold (success arm): ${(tThreshold * 1e6).toFixed(1)}ns | trackingConfig(): ${(tConfigDefaults * 1e6).toFixed(1)}ns | trackingConfig(overrides): ${(tConfigOverrides * 1e6).toFixed(1)}ns`
  );

  console.log("--- NEW axis 11: no-baseline fault cell (once-per-fault) ---");
  const bare = new ResourceRegistry({
    now: () => NOW_ISO as IsoTimestamp,
    generateId: () => `r22a${String(idCounter++).padStart(12, "0")}`
  });
  const bareIdentity: ResourceIdentity = {
    kind: "prompt",
    name: "no-baseline-bench",
    scope: { kind: "user-global" }
  };
  const tFault = bench(() => {
    try {
      proposeFromAnomaly({ packet: prodPacket, registry: bare, identity: bareIdentity });
    } catch {
      /* expected */
    }
  }, 20000);
  console.log(`  propose without baseline (throw): ${(tFault * 1e6).toFixed(0)}ns/fault`);
  console.log(
    "  => the export census is closed: all seven previously unpriced exports now carry a price; " +
      "sanitize is flat reference projection, propose is dominated by the D-slice registry leg at " +
      "production shape, the T slope crosses 10ms only at fantasy cardinality, isolation/config " +
      "arms are ns-level; production flow is zero on the whole face; no candidate"
  );
}

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
