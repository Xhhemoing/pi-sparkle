MODEL_SLUG=claude-fable-5-thinking-xhigh

# R22-I：CLI / Pi adapter / config / telemetry 第二十二遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `ab275e8`（派单独占 tip，含 R22-F 收口与 R22-I 派单；R22-G / R22-H 在飞，未触碰其分支与文件）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- src/cli src/pi-adapter src/config src/telemetry` 为空（`--stat` 0 行）——**零漂移，自 `8dee7fb` 起连续第十五轮字节不变**。`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet` 通过）；切片相关测试面零漂移（`git diff --quiet 8dee7fb..HEAD -- test/` 通过）。基线增量核查：`841c2b0`（R21-I 基线）→ `ab275e8` 共 10 个提交，全部 `docs(sota-opt)` 收口/派单（`git diff --stat -- . ':!docs'` 为 0 行）；**EXCLUSIONS.md 该区间逐字节未变**（`git diff --quiet` 通过），对 I 切片无新约束行。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致（合计 4708 行，与 R19-I/R20-I/R21-I 计数逐字相同）。
- 前置阅读按令执行：README → EXCLUSIONS 全表（S1-I / S4-I-2..5 / S5-I-1..5 / S7-I-1..4 / S8-I-1..3 全部行逐条定位）→ PROGRESS → round-22/PLAN（含 R22-A…R22-F 六切片空枚举收口摘要与 I 切片行；G/H 在飞未等待）→ R21-I（十三连空 + flowchart 平面输入规模轴：可完成宽 ≤128 / 深 ≤32、解析 1.2-1.9 µs/节点、10 ms 越线 N\*≈4200-7900 不可达）→ R20-I（累积遥测 N 轴、N\*≈2300-2700）。邻区/兄弟禁移植 ID 逐一定位核实：**R22-F membership-topology**（F 区实验 restore 成员拓扑）、**SEEDX**（R22-C bootstrap 种子）、**NVG**（R22-E 数值值类面）、**KFAN**（R21-J）、**R22-D 公开导出流普查**、**R22-B 标识符几何 G×T**——本轮新轴与六者机制、站点、切片均不同（见 §2.1），未换名移植。
- 分支：`cursor/r22-i-cli-twenty-second-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（`/exec-daemon/node`，与 R9-I～R21-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。pnpm 10.17.1，`pnpm install --frozen-lockfile`（store 温装 1.6 s）+ `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**（package.json 钉死 + frozen-lockfile 内容寻址）；exports 图仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第十五轮未满足（两臂文件级 blocked 按派单原样维持；R13-I 已证文件级检查即判决性检查，未重复执行级探针）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S22-I-\* ID（诚实空枚举，与 R9-I～R21-I 同结局，十四连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第二十二组枚举而非历轮剩余项清理。R14-I 钉死配置空间维度、R15-I 钉死测量学维度、R16-I 钉死命令类覆盖与采样归因维度、R17-I 钉死错误端点定价维度、R18-I 钉死 children 负载输入规模维度、R19-I 钉死配置输入基数维度、R20-I 钉死累积遥测状态维度、R21-I 钉死 flowchart 平面输入规模维度（四个用户书写输入平面就此全部收口）；本轮补上 run 家族输入平面拼图的**镜像最后一块——run 记录回读平面（record read-back plane）**：R21-I 定价了记录**生产侧**（超线性增长逐字节取证归属 G 辖区每轮 checkpoint 重写），而记录**消费侧**——`inspect --run` / `commits preview` / `episode events` 这些以**工具自产的持久化 run 记录**（`events.jsonl` + `checkpoint.json`）为输入的 post-run 读者命令类——历轮只在默认规模上被 R17-I 按 once-per-process 类整行否决过，从未按记录规模定价。这是 I 切片第五个输入平面，也是唯一一个输入非用户书写而是工具自产的平面。该轴为测量收口、未产出候选，并带出一个与 R21-I 形成结构对照的诚实发现：

1. **零漂移五重确认（连续第十五轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变；pi-ai 0.84.1 exports 图 7 项未变；`dist/providers/` 仍恰 39 表，本 VM 直接计数）、测试面（零变动）、基线增量（`841c2b0..ab275e8` 共 10 提交全 docs，EXCLUSIONS 字节不变）、调用图前提（S5-I-1 dispatch 站点 main.ts L1421-1453 + track/loop 边 L668、S4-I 站点 L149、S1-I 目录快照复用注释与站点 L726-729/L751、S7-I-1 注释与站点 model-catalog.ts L53-58，`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` L36 失手回退 / `runtime.ts` L20 点用处 / `listed-model.ts` L1 同步面，pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方——本轮逐项 grep 直接重验）。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字，派单明令项）**：R9-I 5 格承重集 + flowchart 三格 × 2 Node 版本全部 exit 0，结构逐格同构：地板 54.0/50.6 ms，**custom−builtin 差 children +47.6 / +23.0、track +44.8 / +25.7、flowchart-flat64 +45.4 / +22.9 ms（22.14/22.22）**——与 R21-I 带（children +43.4/+25.4、track +52.2/+23.9、flat64 双态 +42.5/+25.4）及更早十三轮同号、同量级、同 Node 版本梯度（**十五轮同构**）；带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内。builtin 态目录构建比地板贵 +48.6/+53.6（children，双引擎），与 R21-I（+49.7/+51.8）紧邻。配置态 × 命令类矩阵按 R7-I 纪律复核：本轮承重面覆盖 builtin/custom × children/track/flowchart 三命令类，归属未变（`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退）。
3. **新轴（§1.3）：run 记录回读平面标定（I 切片首次；第五输入平面，首个工具自产输入）**。夹具是三个 exit-0 完成的契约规模 flowchart run 记录（flat16/64/128；R21-I 上限 128 内），落盘字节与 R21-I 取证逐字相同（27/87/167 事件、20/72/140 KB events、39/151/300 KB checkpoint）。端到端新增 9 格（双引擎全 exit 0）+ 进程内 11 段分解 + 冷进程管道探针，四个裁决性发现：
   - **回读税在契约上限处首次可达两位数 ms**：相对地板，`inspect --run`（text）@rec128 **+11.0/+11.8 ms**、`commits preview` @rec128 **+17.2/+19.8 ms**（22.14/22.22）；冷进程管道直测与之吻合（inspect 10.88/12.01、commits 14.96/16.17 ms @flat128）。与 R21-I 解析平面（越线点在上限外 33×，结构不可达）形成对照：**回读平面的 10 ms 越线点恰落在契约上限本身（N\*≈110-128，可达域内）**——但它属 once-per-invocation 一次性命令类（派单验收明文的 reject 类），不构成候选来源。
   - **I 辖区份额是 µs 级**：warm 分解（双引擎几乎逐位重合）在 rec128 处 inspect-text 管道合计 3.46-3.50 ms，其中 I 侧（main.ts L923-925 节点摘要串）仅 **0.017-0.019 ms**、`--json` 逐事件 stringify 仅 0.194-0.201 ms；其余全部为 G 辖区（readAll 0.63-0.65 + cpRead 0.85-0.86 + cpValidate 1.33-1.34 + inspectRun 二次读 0.60-0.65 + replay/epScan µs 级）与 tools 辖区（generateDecisionCommits 0.91、assemble ≈0、format 0.02-0.03）。**管道 ≥95% 在切片外。**
   - **S2-I-4 的去重上限首次被规模化定价**：inspect text 路径确实付两次全量 readAll（main.ts L901 + `inspectRun` 自带 L63）——该去重提案是 EXCLUSIONS 既有行 S2-I-4（改 run 公开签名，X0-4 族，规模无关的否决理由）。本轮在**最大可达契约规模**处量化其天花板：二次读 warm 0.60-0.65 ms、按冷因子 ~3× 折算 ≈2-3 ms/次——**在一切可达规模下低于落地线一个数量级**，否决维持且此后无需再测。
   - **episode 平面不随节点规模增长**：flowchart run 绑定的 episode 记录恒 3 事件（EPISODE_OPENED / RUN_ATTACHED / EPISODE_CLOSED），`episode events` @rec128 仅 +1.6/+2.3 ms（≈dispatch 模块惰性边）；episode 记录规模变量是 attach 的 run 数——每个 run 是真实付费 run，R20-I 相对成本裁决族。
4. **两个结构注记（散文记录，不铸 ID）**：(a) `inspect --json` 比 text **更便宜**（+3.2/+4.9 vs +11.0/+11.8 ms @rec128）——json 路径在 main.ts L906-910 早退，跳过 replay/checkpoint/inspectRun 全部三段：现行代码已是该命令类的最优形态之一，无可削；(b) 回读命令类**与配置态无关**（inspect/commits 代码路径不触 `loadProvidersConfig`/`buildLiveCatalogConfig`/router 构建——代码级核对），故 custom−builtin 结构对该平面为零耦合，矩阵无需按态叉乘。
5. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**；r7i part D 载荷探针复证 builtin 构建后 providers/all 保持冷 60.1 ms、custom 回退命中缓存 1.3 ms）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R21-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动，无 r22i 新仿真（派单：仅落地代码才须新增）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读读码，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交任何 scripts/（全部 harness 源码进附录 A，遵守 R7-F/R8-C/R9-I～R21-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README 未触碰；未触碰在飞 R22-G / R22-H 分支与文件。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S21-\* 条目；R9-I～R21-I 空枚举维持，未事后发明 S9-I-\* ～ S21-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：零漂移即字节级证明（站点逐行 grep 重验，见结论第 1 条），三个等价性仿真复跑为行为级证明。S4-I 败者为 **S4-I-2..5**，无 S4-I-1；赢家 ID 为 **S5-I-1**。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅做文件级重开条件确认（§2.2），未满足，维持淘汰；未构造替代注册表、未触碰回退路径。**两臂文件级 blocked 按派单原样维持。**
  - **未重铸 R21-I flowchart 平面输入规模轴**：本轮 flowchart 承重格维持 R21-I 的 n1-exec / flat64 双态两形态（规模格未重测未扩展）；新轴对象是**已完成 run 的持久化记录**而非 flowchart 规格/results 输入文件——文件不同（checkpoint.json/events.jsonl vs flowchart.json/results.json）、解析器不同（validateEvent/validateCheckpoint/replayRun vs flowchart-io 全家）、命令类不同（inspect/commits/episode vs run）。夹具生成复用 R21-I 的 flat 形状仅为**产出合法契约规模记录的手段**，其解析成本不在本轴计费内。
  - **未重铸 R20-I 累积遥测 N 轴**：全部回读夹具 `invocations.jsonl` 不存在（inspect/commits 代码路径也不读它——两命令不建 router，不触 `loadInvocationsFromStateRoot`，代码级正交证明）；承重格夹具全部全新拷贝 N=0。**未重铸 R19-I E×P×K**——custom 夹具维持 lineage P=1/K=2/E=2；**未重铸 R18-I children 基数轴**——children 夹具维持 C=2。
  - **未换名移植邻区/兄弟 ID**：R22-F membership-topology（F 区 restore 输入次序拓扑）/ SEEDX（C 区 bootstrap 种子恒等）/ NVG（E 区数值值类面）/ KFAN（J 区）/ R22-D 公开导出流普查（D 区导出图普查——本轴不是导出普查，是输入平面定价）/ R22-B G×T（B 区标识符几何）——六者的测量对象、切片归属、消除机制与本轴均无交集。与 R19-G **NAMESHAPE** 的区分：其对象是 `runtime/runs/` 的**目录条目扇出数**（EventStore 目录行走），本轴对象是**单个 run 记录的内容深度**（一个 events.jsonl 的行数 + 一个 checkpoint.json 的字节数），读取站点、规模变量均不同。与 S11-G-3（inspectRun 尾扫部分读，G 区已否决提案）的区分：那是 G 文件上的优化提案，本轴是 I 侧端到端命令在契约满档的测量收口，未重提其形态。
  - **未重开 S8-I-2 / S8-I-3 / S7-I-2..4 / S6-I-1..3 / S5-I-2..5 / S4-I-2..5 / S3-I-1..6 / S2-I-1..6 / S1-I-1..8**；R10-I～R21-I 全部无 ID 收口维持，其结构前提（39 表、exports 图、pi-ai 版本、调用图）本轮复核未变。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；Live=R0 等价；双 LCB / 双归因在位；提升 proposal-first（Checkpoint F-PROD 开放）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`providers/all` 失手回退 + throw-outside-catch 在位；未改任何测试。**新轴全程零代码改动、零行为差异**——所有测量都是对现行回读路径的直接观测 + 合法夹具注入（exit-0 完成的真实 run 记录），未触碰任何 reader/validator/formatter 一行；`commits apply` 未测（其主体是 git spawn 副作用，非本仓解析面）。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；未触碰在飞 R22-G / R22-H 分支与文件。

## 1. 本 VM 复测：8 格承重抽测 + 新轴 9 格（双 Node，未拷贝历轮数字）

R9-I～R21-I 抽测集承重面复刻（含 R21-I 引入的 flowchart 双态格）并**新增 9 个 run 记录回读格**（configured 态夹具、每迭代全新拷贝 state-root、2 预热 + 10 测量、显式二进制全路径，遵守 R7-I 配置态 × 命令类纪律；harness 全文附录 A.2）。

### 1.1 抽测中位数（ms，spawn-to-exit，全部 exit 0）

| 格 | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|
| `--version`（地板） | 54.0 (min 53.2, p90 54.6) | 50.6 (49.4, 52.1) |
| run-children@builtin（C=2） | 102.6 (98.8, 105.1) | 104.2 (101.0, 108.3) |
| run-track@builtin | 124.9 (116.1, 131.4) | 120.6 (117.0, 123.3) |
| run-children@custom（P=1/K=2/E=2） | **150.2** (145.1, 165.1) | **127.2** (125.3, 130.2) |
| run-track@custom | **169.7** (164.1, 179.0) | **146.3** (141.6, 148.0) |
| run-flowchart@custom-n1-exec（R16-I lineage） | 127.6 (123.0, 135.7) | 101.0 (99.4, 103.5) |
| run-flowchart@custom-flat64 | 252.6 (241.0, 256.4) | 226.9 (219.0, 231.7) |
| run-flowchart@builtin-flat64（态对照） | 207.2 (200.8, 208.5) | 204.0 (196.9, 205.4) |
| **inspect-run@rec16**（E=27，本轮新格） | 59.2 (57.8, 60.0) | 56.7 (54.9, 59.2) |
| **inspect-run@rec64**（E=87） | 61.1 (60.1, 61.7) | 59.7 (58.3, 61.5) |
| **inspect-run@rec128**（E=167，契约上限记录） | **65.0** (62.9, 66.1) | **62.4** (61.3, 63.9) |
| **inspect-run-json@rec128** | 57.2 (56.7, 58.6) | 55.5 (55.0, 56.1) |
| **commits-preview@rec16**（P=16） | 60.8 (59.7, 61.8) | 57.5 (56.3, 58.8) |
| **commits-preview@rec64**（P=64） | 65.1 (64.1, 66.0) | 62.4 (60.7, 64.2) |
| **commits-preview@rec128**（P=128） | **71.2** (70.4, 72.4) | **70.4** (68.1, 71.8) |
| **commits-preview-json@rec128** | 71.2 (70.7, 71.8) | 70.7 (68.6, 72.2) |
| **episode-events@rec128**（绑定 episode，恒 3 事件） | 55.6 (54.2, 56.3) | 52.9 (52.4, 54.9) |

- **custom−builtin 差**：children **+47.6 / +23.0**、track **+44.8 / +25.7**、flowchart-flat64 **+45.4 / +22.9** ms（22.14/22.22）。R21-I 带 +43.4/+25.4、+52.2/+23.9、+42.5/+25.4，更早十三轮见 R19-I §1.1 汇编——**十五轮同号、同量级、同 Node 版本梯度**。带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内（22.14 单窗 maxAbs 34.5 ms、22.22 8.7-10.4 ms），无新结构。归属未变。
- **builtin 态目录构建**比地板贵 +48.6/+53.6（children，双引擎）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。本 VM 地板（54.0/50.6）与 R21-I VM（54.9/51.5）几乎逐位相同。
- **flowchart 命令类交叉核**：n1-exec（127.6/101.0）与 R21-I（130.5/102.4）同带；flat64 双态差与 children/track 带同号同量级——回退成本命令类无关、规模无关（R21-I 结论第三次复现）。

### 1.2 新轴的生产流证明（先证流量，再证夹具合法性，再证数字）

- **流量**：`inspect --run <runId> [--json]`（main.ts L201/L869-970）、`commits preview|apply --run <runId>`（commits.ts，S5-I-1 惰性 dispatch 模块）、`episode events --episode <epId>`（episode.ts）都是 USAGE 文档化的 post-run 读者命令类；其输入是 run 家族每次执行**必然自产**的持久化记录 `runtime/runs/<runId>/{events.jsonl,checkpoint.json}`（+ 绑定 episode 的 `runtime/episodes/<epId>*.jsonl`）。消费机器：inspect text 路径 = EventStore.readAll（G）→ replayRun（G）→ episodeIdFromEvents（G run/episode-bind.ts）→ CheckpointStore.read + validateCheckpoint（G）→ 节点摘要串（**I**，main.ts L923-925）→ inspectRun（G run/inspection.ts，**自带第二次 readAll + replayRun**）；inspect --json 路径 = readAll → 逐事件 stringify 早退（**I**，L906-910）；commits 路径 = readAll（G）+ checkpoint 读验（G）→ assembleDecisionCommitInput / generateDecisionCommits / formatCommitMessage（**tools/decision-commit.ts，切片外**）→ join 输出（**I**，commits.ts L138）。
- **夹具合法性（与 R21-I 幻想规模的本质区别）**：三个记录夹具由真实 `run --flowchart flat{16,64,128} --results <全覆盖>` exit-0 生成（附录 A.1），宽度全部 ≤ R21-I 证明的契约上限 128；落盘字节与 R21-I 取证逐字相同（27/87/167 事件、20/72/140 KB、checkpoint 39/151/300 KB，每记录夹具另含 2 个 episode 文件、恒 3 episode 事件）。**rec128 就是本仓现行契约下能存在的最大 flowchart run 记录**——本轴全部裁决数字落在可达域内。
- **正交性**：children.json（R18-I）/ providers.json（R19-I）/ invocations.jsonl（R20-I）/ flowchart.json+results.json（R21-I）四平面都是**用户书写**输入且消费站点在 run 创建路径；本轴平面是**工具自产**记录且消费站点在 post-run 读者命令。inspect/commits 不建 router、不读 providers.json/invocations.jsonl（代码级核对：两 handler 无 `loadProvidersConfig` / `buildLiveCatalogConfig` / `loadInvocationsFromStateRoot` 调用）——五平面两两正交。

### 1.3 轴 1：run 记录回读平面标定（本轮新证据，不铸 ID）

**(a) 端到端规模格**（§1.1 下九行，相对各自地板的差，ms，22.14/22.22）：

| 记录（E 事件 / checkpoint KB） | inspect text | inspect --json | commits preview |
|---|---|---|---|
| rec16（27 / 39） | +5.2 / +6.1 | — | +6.8 / +6.9 |
| rec64（87 / 151） | +7.1 / +9.1 | — | +11.1 / +11.8 |
| **rec128（167 / 300，契约上限）** | **+11.0 / +11.8** | +3.2 / +4.9 | **+17.2 / +19.8** |

线性、无悬崖；边际 ~52/51 µs/节点（inspect text）、~93/115 µs/节点（commits）。**10 ms 越线点：inspect text N\*≈110-128、commits N\*≈64-90——均在可达域内**（对照 R21-I 解析平面 N\*≈4200-7900 的结构不可达）。`--json` 比 text 便宜 7.8/6.9 ms @rec128（早退跳过 replay/checkpoint/inspectRun 三段）；episode events 恒 +1.6/+2.3（3 事件，episode 平面规模变量是 attach 的 run 数而非节点数）。

**(b) 进程内 warm 分解**（附录 A.3；双引擎几乎逐位重合，22.22/22.14 并示；ms @rec128）：

| 段 | 辖区 | rec16 | rec64 | rec128 |
|---|---|---|---|---|
| EventStore.readAll | G | 0.21/0.19 | 0.37/0.39 | 0.65/0.63 |
| replayRun | G | 0.004 | 0.013 | 0.004 |
| CheckpointStore.read | G | 0.14 | 0.45/0.43 | 0.85/0.86 |
| validateCheckpoint | G | 0.26/0.25 | 0.82/0.79 | **1.34/1.33** |
| inspectRun（S2-I-4 二次读） | G | 0.19 | 0.36/0.38 | **0.60/0.65** |
| episodeIdFromEvents | G | 0.0008 | 0.0017 | 0.0007 |
| 节点摘要串（main L923-925） | **I** | 0.004 | 0.013 | **0.019/0.017** |
| --json 逐事件 stringify | **I** | 0.035 | 0.125 | 0.194/0.201 |
| assembleDecisionCommitInput | tools | ~0.000 | ~0.000 | ~0.000 |
| generateDecisionCommits | tools | 0.09 | 0.30/0.29 | **0.91** |
| formatCommitMessage×P + join | tools/I | 0.01 | 0.02 | 0.02/0.03 |
| **inspect-text 合计** | | 0.81/0.77 | 2.02/2.01 | **3.46/3.50** |
| **commits 合计** | | 0.71/0.67 | 1.96/1.92 | **3.77/3.76** |

per-event：readAll 3.9-7.9 µs、inspectRun 3.6-7.2 µs（E↑ 时收敛到 ~4 µs——validateEvent + JSON.parse 贴 R20-I 测定的 Node 原生解析地板族）。**I 辖区份额在契约满档只有 17-19 µs（text）/ 194-201 µs（json）——整管道的 ~0.5%/~5%；其余 ≥95% 为 G（readAll/checkpoint 读验/inspectRun/replay）与 tools（generate）。**

**(c) 冷进程管道直接归因**（附录 A.4：全新进程执行一次完整管道，10 进程取中位；ms）：

| 记录/模式 | 22.14.0 | 22.22.2 |
|---|---|---|
| flat16 / inspect | 5.59 (min 4.63) | 6.59 (6.02) |
| flat16 / commits | 5.23 (4.59) | 6.29 (6.04) |
| flat128 / inspect | **10.88** (9.75) | **12.01** (11.27) |
| flat128 / commits | **14.96** (14.43) | **16.17** (15.83) |

冷管道 ≈ 端到端差（inspect +11.0/+11.8 vs 10.88/12.01；commits +17.2/+19.8 vs 14.96/16.17 + commits.js/decision-commit.js 惰性边 ~2-3 ms）——**无隐藏二次工作**；冷/warm 因子 ~2.9-4.3×，与 R20-I 冷装载因子同族。**S2-I-4 去重上限的规模化定价**：inspectRun 二次读（自带 readAll+replayRun）warm 0.60-0.65 ms、冷折算 ≈2-3 ms/次@契约满档——在一切可达规模下低于落地线一个数量级，其否决理由（改 `inspectRun` 公开签名以接受预读事件，X0-4 族）与规模无关，**该项此后无需再测**。

## 2. 第二十二组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 输入规模定价），每形态先对 EXCLUSIONS 全表 + 二十一轮 I 报告 + 六个兄弟禁移植 ID 做同机制先例检索。

### 2.1 新轴候选性自查——测量收口，不铸 ID

- **轴 1（run 记录回读平面标定）**是既有回读路径在第五输入平面维度上的直接观测：零行为差异、零代码改动。其产出是「回读税线性 + 契约满档 11-20 ms 可达 + I 份额 µs 级 + S2-I-4 天花板 2-3 ms + episode 平面不随 N 增长」的裁决数字集，不是优化。正交性论证见 §1.2/§0；与 R21-I 的关系是**生产侧/消费侧镜像**而非同轴重测（R21-I 明文只测了记录生产，其 §1.3(c) 的字节取证恰为本轴夹具的合法性证书）。
- **派单空转卫自查**：本轴有真实生产流（三个 USAGE 文档化命令类 + 记录是 run 家族必然自产物），且核心裁决数字全部落在**可达域内**（夹具就是 exit-0 契约满档记录）——不属「零生产流 + 仅幻想尺度」空转型。发现的两位数 ms 结构（commits @rec128 +17-20 ms）不铸候选的理由是三重的：once-per-invocation 一次性命令类（派单验收明文 reject 类）、管道 ≥95% 在 G/tools 辖区（I 份额 17-201 µs）、每事件成本已贴 Node 原生解析地板族（~4 µs/事件，R20-I 同族）。

### 2.2 S8-I-1 重开条件文件级再确认——不铸 ID

- `dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 **0.84.1**（package.json + frozen-lockfile 内容寻址）。依派单「两臂文件级 blocked」纪律不重复执行级探针。
- **裁决**：重开条件连续第十五轮未满足；所需的是 pi-ai 上游导出，不是仓内改动。此项为审计证据，非机制候选——**不铸 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级即死）

| 扫描角度 | 处置 |
|---|---|
| **inspect text 双 readAll 去重**（本轴消除形态 #1） | **S2-I-4 排除行逐字**（resume/answer/inspect CLI 预读去重，需改 run 公开签名）；本轮把其收益天花板在最大可达规模处定价为 warm 0.60-0.65 / 冷 ≈2-3 ms——低于落地线一个数量级且否决理由规模无关。维持淘汰 |
| **commits 的 events+checkpoint 双读去重**（消除形态 #2） | 两读是**不同文件**（events.jsonl vs checkpoint.json），各自必要（proposals 需 checkpoint.flowchart 定义 + events 装配）；R17-I 一次性命令类行已列。无重叠可削 |
| **inspect --json 逐事件 stdout 批量合并**（消除形态 #3） | **S3-I-4 排除行逐字**（E=2000 实测批量慢 85-86 µs + CliIo 调用次数可观测 + 流式契约）；本轮 E=167 规模更小，先例直接覆盖 |
| **validateCheckpoint / validateEvent 读侧弱化或 memo**（消除形态 #4） | 切片外 G 辖区 fail-closed 读侧复验契约本体：S5-G-2（读侧复验 memo = 篡改字节静默接受）、S8-G-2（写侧 memo 生产 0 命中）、S1-G-1 族逐字覆盖。非 I 候选 |
| **inspectRun 事件走查微观 / 尾扫部分读**（消除形态 #5） | 切片外 G 文件（run/inspection.ts）；S2-G-6（两遍合并 ~1 µs）、S11-G-3（尾扫跳过早段 fail-closed 复验，0.52-0.53 ms）已否决。非 I 候选 |
| **generateDecisionCommits / assembleDecisionCommitInput 微观**（消除形态 #6） | 切片外 tools/decision-commit.ts（0.91 ms @P=128 上限）；I 侧仅 join 0.02-0.03 ms。非 I 候选 |
| **节点摘要串 / format join 微观** | I 侧 17-30 µs @契约满档；S1-I-7（parts+join 实测慢 6.4×）邻域 µs 族。非候选 |
| **episode events 读路径** | 恒 3 事件（不随 N 增长）；EpisodeEventStore 切片外；规模变量（attach run 数）属 R20-I 相对成本裁决族（每 run = 真实付费 run）。非候选 |
| **commits/episode/inspect dispatch 模块惰性化** | S5-I-1 已落地物本体（main.ts L1435/L1441 边在位）；无残留静态边。无事可做 |
| invocations.jsonl 装载税族 | R20-I 轴收口逐字引用（N\*≈2300-2700、每记录 µs 贴底）；本轮回读命令类**不读该文件**（代码级正交证明，§1.2），承重夹具 N=0 |
| flowchart 规格/results 解析平面族 | R21-I 轴收口逐字引用（契约上限 128/32、上限处全平面 0.26-0.29 ms、越线不可达）；本轮未重测其规模格，仅复刻 n1-exec/flat64 承重格 |
| 配置输入基数族 / children 基数族 | R19-I（E≈130-160）/ R18-I（C≈2400）收口；本轮维持 lineage 夹具未触两维 |
| custom id 跳回退 / 自制 union / 磁盘快照 / 失手 memo | S8-I-1 硬禁 + R10-I/R11-I 三重围栏；重开条件本轮文件级仍未满足（§2.2） |
| 拒绝路径 / 错误端点 | R17-I 六端点收口维持；本轮全部格 exit 0，未测 exit-1 端点 |
| 肥 builtin 态 / A/A 地板 / 引擎代 / 堆足迹 / 命令类覆盖与采样归因 | R14-I / R15-I / R16-I 收口；本轮带比对直接引用其 MDE 包络 |
| flowchart 双 build / providers.json 双读 / 死载荷下沉 / pause-inject 免 router / compile-cache / dist 布局 / 一次性命令类 / 凭据面 / 解析微观 / barrel 隐藏边 / root-compat 替代源 / per-provider 探针短路 / 预热重叠 | S1-I-1/2、S2-I-1..6、S3-I-1..6、S4-I-2..5、S5-I-2..5、S6-I-1..3、S7-I-2..4、S8-I-2/3、S1-I-4..8、R11-I/R12-I/R13-I 无 ID 收口逐字重现——零漂移下前提逐项成立，本轮 25 文件重读未发现任何新站点脱离既有覆盖物 |
| telemetry / config 残余面 | R20-I 已把 telemetry 真实热站点定价贴底（0.52-0.61 µs/条）；本轮回读平面不触 telemetry 文件。S1-D-8 / S3-I-5 族噪声维持 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖、pi-ai exports、测试面、基线增量五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可落地候选；本轮把 run 家族第五个（也是最后一个未定价的）输入平面——工具自产 run 记录的回读面——收口：回读税线性、10 ms 越线点落在契约上限本身（与 R21-I 解析平面的结构不可达形成完备对照）、I 辖区份额 µs 级、既有排除行 S2-I-4 的收益天花板在最大可达规模处定价为 2-3 ms/次。**I 切片自此五个输入平面（children / providers / invocations / flowchart+results / run 记录回读）与八个正交维度（配置空间、测量学、命令类、错误端点、负载规模、配置基数、累积状态、记录生产/消费双侧）全部收口。**

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx；无代码落地故按派单为自愿加测）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part C：17 组失手/custom/对抗用例 lazy===sync；part D 载荷探针：builtin 构建后 providers/all 保持冷 60.1 ms、custom 回退命中缓存 1.3 ms；part E：configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单项，Node 22.22.2）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**（与 R9-I～R21-I 计数逐字相同）、`pnpm build` 绿。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **构建**：`pnpm install --frozen-lockfile`（1.6 s 温装）+ `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动；无赢家故无新仿真脚本入库；四个 harness/探针（genfix/spot/inproc/coldprobe）均为 /tmp 临时件，全文进附录 A，未进 lint 面。
- **引擎注记**：22.14.0 的 doctor engines 预检 fail-closed 为设计行为（R9-I §3 记录在案）；nvm 22.22.2 为 gate/仿真引擎。依 R15-I 轴 1 纪律，候选验收须在 ≥22.19 引擎上做——本轮无候选，抽测数字用于十五轮带比对（双引擎同号），新轴归因不依赖包络判读（warm 分解 + 冷进程管道直测 + 端到端差三线互证）。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；新轴以测量收口关闭记录消费侧维度缺口；S8-I-1 文件级第十五轮 blocked；S2-I-4 天花板规模化定价后该项永久闭合）。
- 剩余赢家：无（custom−builtin 结构维持 S8-I-1 健全性反例封死；回读平面唯一两位数 ms 结构属 once-per-invocation reject 类且 ≥95% 在 G/tools 辖区，I 份额 17-201 µs；每事件成本贴 Node 原生解析地板族）。
- 矩阵空洞：无（R8-I 78 格全覆盖；承重格十五轮同构复现 + 本轮补入回读平面 9 格——inspect text/json × commits × episode × 三记录规模的归因完备组合；R7-I 配置态 × 命令类纪律全程遵守，且本轮证明回读平面与配置态零耦合；children 基数维持 R18-I、配置基数维持 R19-I、累积状态维持 R20-I、flowchart 输入规模维持 R21-I、错误端点维持 R17-I、命令类与归因维持 R16-I、测量学维持 R15-I、肥 builtin 态维持 R14-I 收口）。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 记录夹具生成（/tmp/r22i-bench/genfix.mjs；R21-I flat 形状复用为合法记录生成手段）

```javascript
// R22-I fixture generation: persisted run records at flowchart contract scales.
// Runs `run --flowchart flat{16,64,128} --results <full coverage>` once each
// into persistent state roots, then reports runId + record byte sizes.
// These become the READ-BACK plane inputs for inspect/commits cells.
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, readdirSync, statSync, readFileSync } from "node:fs";
import { join } from "node:path";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r22i-bench";
const FIX = join(ROOT, "fixtures");

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
for (const [name, cfg] of Object.entries(states)) {
  const dir = join(FIX, `state-${name}`);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
}
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
const CHILDREN = join(FIX, "children.json");
writeFileSync(CHILDREN, JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");

// R21-I flowchart fixture shapes (flat width-N on the "cheap" alias row).
function flatFlow(n) {
  const nodes = [];
  for (let i = 0; i < n; i++) {
    nodes.push({
      id: `n${i}`, taskId: `tsk_f${i}`, role: "actor",
      objective: `Step ${i} of the benchmark flow`,
      modelPolicy: { allowedModels: ["cheap"] },
      confidenceThreshold: 0.7, approvalRequired: false
    });
  }
  return { id: `bench-flat-${n}`, nodes, edges: [] };
}
function resultsFor(n) {
  const out = {};
  for (let i = 0; i < n; i++) {
    out[`n${i}`] = {
      outcome: "SUCCESS", confidence: 0.9,
      evidenceIds: [`evd_f${i}`],
      facts: [{ key: `fact-${i}`, value: `value ${i}`, confidence: 0.8 }]
    };
  }
  return out;
}
for (const n of [1, 16, 64, 128]) {
  writeFileSync(join(FIX, `flow-flat${n}.json`), JSON.stringify(flatFlow(n)) + "\n");
  writeFileSync(join(FIX, `results-flat${n}.json`), JSON.stringify(resultsFor(n)) + "\n");
}

const env = { ...process.env };
delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;

// Generate persisted records: one completed flowchart run per N under a
// dedicated custom-state root (record roots kept for the whole bench).
const manifest = {};
for (const n of [16, 64, 128]) {
  const sd = join(FIX, `rec-flat${n}`);
  rmSync(sd, { recursive: true, force: true });
  mkdirSync(sd, { recursive: true });
  spawnSync("cp", ["-r", join(FIX, "state-custom") + "/.", sd], { encoding: "utf8" });
  const r = spawnSync(process.execPath, [CLI, "run", "--project", PROJ, "--objective", "bench objective",
    "--state-root", sd, "--flowchart", join(FIX, `flow-flat${n}.json`), "--results", join(FIX, `results-flat${n}.json`)],
    { env, encoding: "utf8", timeout: 300000 });
  if (r.status !== 0) { console.error(`flat${n} FAILED: ${r.stderr}`); process.exit(1); }
  const runsDir = join(sd, "runtime", "runs");
  const runIds = readdirSync(runsDir);
  const runId = runIds[0];
  const evPath = join(runsDir, runId, "events.jsonl");
  const cpPath = join(runsDir, runId, "checkpoint.json");
  const evLines = readFileSync(evPath, "utf8").split("\n").filter((l) => l.trim() !== "").length;
  manifest[`flat${n}`] = { stateRoot: sd, runId, events: evLines,
    eventsKB: (statSync(evPath).size / 1024).toFixed(0), checkpointKB: (statSync(cpPath).size / 1024).toFixed(0),
    allRuns: runIds.length };
  const epDir = join(sd, "runtime", "episodes");
  try {
    manifest[`flat${n}`].episodes = readdirSync(epDir);
  } catch { manifest[`flat${n}`].episodes = []; }
  console.log(`flat${n}: runId=${runId} runs=${runIds.length} events=${evLines} (${manifest[`flat${n}`].eventsKB}KB) checkpoint=${manifest[`flat${n}`].checkpointKB}KB episodes=${manifest[`flat${n}`].episodes.join(",") || "none"}`);
}
writeFileSync(join(ROOT, "manifest.json"), JSON.stringify(manifest, null, 2) + "\n");
console.log("MANIFEST_WRITTEN");
```

原始输出（记录字节与 R21-I A.3 取证逐字相同）：

```
flat16: runId=run_56f59530-… runs=1 events=27 (20KB) checkpoint=39KB episodes=ep_864a233f-….events.jsonl,ep_864a233f-….jsonl
flat64: runId=run_556f09a6-… runs=1 events=87 (72KB) checkpoint=151KB episodes=ep_f28d5b9b-….events.jsonl,ep_f28d5b9b-….jsonl
flat128: runId=run_1932415b-… runs=1 events=167 (140KB) checkpoint=300KB episodes=ep_64c18c67-….events.jsonl,ep_64c18c67-….jsonl
MANIFEST_WRITTEN
```

### A.2 17 格抽测 harness（/tmp/r22i-bench/spot.mjs，`<node二进制全路径> spot.mjs <同一路径>` 分别以两版本执行；承重 8 格与 R21-I A.1 同构 + 新增 9 个回读格）

```javascript
// R22-I spot re-measure: floor + builtin/custom children+track+flowchart
// bearing cells (R9-I..R21-I harness lineage, R7-I configured-state x
// command-class discipline) + NEW axis: run-record READ-BACK plane — the
// persisted run record (events.jsonl + checkpoint.json) as INPUT to the
// post-run reader command classes (inspect --run, commits preview,
// episode events). Prior rounds priced the four USER-authored input planes
// (children.json R18-I, providers.json R19-I, invocations.jsonl R20-I,
// flowchart.json+results.json R21-I) and priced record CREATION (R21-I,
// superlinear growth attributed to G checkpoint rewrites). The record
// CONSUMPTION side — the only run-family input plane whose input is
// TOOL-PRODUCED — was priced only at default scale (R17-I once-per-process
// row). Fixture records are exit-0 contract-scale flowchart runs
// (flat16/64/128; R21-I cap width<=128), byte-identical to R21-I forensics
// (27/87/167 events, 39/151/300KB checkpoints).
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, cpSync, readFileSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r22i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.argv[2];
const WARMUP = 2, ITERS = 10;
const manifest = JSON.parse(readFileSync(join(ROOT, "manifest.json"), "utf8"));

const PROJ = join(FIX, "project");
const CHILDREN = join(FIX, "children.json");

const childrenArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN];
const trackArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"];
const flowResultsArgs = (name) => (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--flowchart", join(FIX, `flow-${name}.json`), "--results", join(FIX, `results-${name}.json`)];
const flowExecArgs = (name) => (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--flowchart", join(FIX, `flow-${name}.json`), "--executor", "fake"];
const inspectArgs = (rec, json) => (s) => ["inspect", "--run", manifest[rec].runId, "--state-root", s, ...(json ? ["--json"] : [])];
const commitsArgs = (rec, json) => (s) => ["commits", "preview", "--run", manifest[rec].runId, "--state-root", s, ...(json ? ["--json"] : [])];
const episodeArgs = (rec) => (s) => ["episode", "events", "--episode", manifest[rec].episodes[0].replace(/\.events\.jsonl$|\.jsonl$/, ""), "--state-root", s];

const cells = [
  // Bearing set (R9-I..R21-I lineage; builtin/custom x children/track/flowchart).
  { id: "version", args: () => ["--version"], state: undefined },
  { id: "run-children@builtin", state: "state-builtin", args: childrenArgs },
  { id: "run-track@builtin", state: "state-builtin", args: trackArgs },
  { id: "run-children@custom", state: "state-custom", args: childrenArgs },
  { id: "run-track@custom", state: "state-custom", args: trackArgs },
  { id: "run-flowchart@custom-n1-exec", state: "state-custom", args: flowExecArgs("flat1") },
  { id: "run-flowchart@custom-flat64", state: "state-custom", args: flowResultsArgs("flat64") },
  { id: "run-flowchart@builtin-flat64", state: "state-builtin", args: flowResultsArgs("flat64") },
  // NEW axis: run-record read-back plane (records are contract-scale, exit-0 reachable).
  { id: "inspect-run@rec16", state: "rec-flat16", args: inspectArgs("flat16", false) },
  { id: "inspect-run@rec64", state: "rec-flat64", args: inspectArgs("flat64", false) },
  { id: "inspect-run@rec128", state: "rec-flat128", args: inspectArgs("flat128", false) },
  { id: "inspect-run-json@rec128", state: "rec-flat128", args: inspectArgs("flat128", true) },
  { id: "commits-preview@rec16", state: "rec-flat16", args: commitsArgs("flat16", false) },
  { id: "commits-preview@rec64", state: "rec-flat64", args: commitsArgs("flat64", false) },
  { id: "commits-preview@rec128", state: "rec-flat128", args: commitsArgs("flat128", false) },
  { id: "commits-preview-json@rec128", state: "rec-flat128", args: commitsArgs("flat128", true) },
  { id: "episode-events@rec128", state: "rec-flat128", args: episodeArgs("flat128") }
];
function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, state), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 180000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-FAIL ${cell.id}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

原始输出（全部格 exit 0）：

```
== v22.14.0 ==
version: median=54.0ms min=53.2 p90=54.6 exit=0
run-children@builtin: median=102.6ms min=98.8 p90=105.1 exit=0
run-track@builtin: median=124.9ms min=116.1 p90=131.4 exit=0
run-children@custom: median=150.2ms min=145.1 p90=165.1 exit=0
run-track@custom: median=169.7ms min=164.1 p90=179.0 exit=0
run-flowchart@custom-n1-exec: median=127.6ms min=123.0 p90=135.7 exit=0
run-flowchart@custom-flat64: median=252.6ms min=241.0 p90=256.4 exit=0
run-flowchart@builtin-flat64: median=207.2ms min=200.8 p90=208.5 exit=0
inspect-run@rec16: median=59.2ms min=57.8 p90=60.0 exit=0
inspect-run@rec64: median=61.1ms min=60.1 p90=61.7 exit=0
inspect-run@rec128: median=65.0ms min=62.9 p90=66.1 exit=0
inspect-run-json@rec128: median=57.2ms min=56.7 p90=58.6 exit=0
commits-preview@rec16: median=60.8ms min=59.7 p90=61.8 exit=0
commits-preview@rec64: median=65.1ms min=64.1 p90=66.0 exit=0
commits-preview@rec128: median=71.2ms min=70.4 p90=72.4 exit=0
commits-preview-json@rec128: median=71.2ms min=70.7 p90=71.8 exit=0
episode-events@rec128: median=55.6ms min=54.2 p90=56.3 exit=0
== v22.22.2 ==
version: median=50.6ms min=49.4 p90=52.1 exit=0
run-children@builtin: median=104.2ms min=101.0 p90=108.3 exit=0
run-track@builtin: median=120.6ms min=117.0 p90=123.3 exit=0
run-children@custom: median=127.2ms min=125.3 p90=130.2 exit=0
run-track@custom: median=146.3ms min=141.6 p90=148.0 exit=0
run-flowchart@custom-n1-exec: median=101.0ms min=99.4 p90=103.5 exit=0
run-flowchart@custom-flat64: median=226.9ms min=219.0 p90=231.7 exit=0
run-flowchart@builtin-flat64: median=204.0ms min=196.9 p90=205.4 exit=0
inspect-run@rec16: median=56.7ms min=54.9 p90=59.2 exit=0
inspect-run@rec64: median=59.7ms min=58.3 p90=61.5 exit=0
inspect-run@rec128: median=62.4ms min=61.3 p90=63.9 exit=0
inspect-run-json@rec128: median=55.5ms min=55.0 p90=56.1 exit=0
commits-preview@rec16: median=57.5ms min=56.3 p90=58.8 exit=0
commits-preview@rec64: median=62.4ms min=60.7 p90=64.2 exit=0
commits-preview@rec128: median=70.4ms min=68.1 p90=71.8 exit=0
commits-preview-json@rec128: median=70.7ms min=68.6 p90=72.2 exit=0
episode-events@rec128: median=52.9ms min=52.4 p90=54.9 exit=0
```

### A.3 进程内 warm 分解（/tmp/r22i-bench/inproc.mjs，双引擎各执行一次）

```javascript
// R22-I in-process anchor: decompose the run-record READ-BACK cost at record
// scale (contract-cap flowchart fixtures rec-flat16/64/128, exit-0 reachable;
// E = 27/87/167 events, checkpoint 39/151/300 KB). Stages and ownership:
//   EventStore.readAll            (G: run/event-store.ts -> persist/jsonl +
//                                  validateEvent x E, fail-closed)
//   replayRun                     (G: run/replay.ts)
//   CheckpointStore.read +
//   validateCheckpoint            (G: run/checkpoint-store.ts + replay.ts)
//   inspectRun                    (G composite: OWN readAll + replayRun +
//                                  event walk — the S2-I-4 second read)
//   episodeIdFromEvents           (G: run/episode-bind.ts, reverse scan)
//   node-summary string           (I: main.ts L923-925 expression replica)
//   JSON.stringify x E            (I: inspect --json emit path)
//   assembleDecisionCommitInput   (tools: decision-commit.ts)
//   generateDecisionCommits       (tools)
//   formatCommitMessage x P +join (tools fn, I-side join at commits.ts L138)
// The I-owned share of the pipeline is the CLI shell: parseArgs/parseRunId,
// the summary/emit expressions, and the join — everything else is G/tools.
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";
import { EventStore } from "/workspace/dist/run/event-store.js";
import { CheckpointStore } from "/workspace/dist/run/checkpoint-store.js";
import { replayRun, validateCheckpoint } from "/workspace/dist/run/replay.js";
import { inspectRun } from "/workspace/dist/run/inspection.js";
import { episodeIdFromEvents } from "/workspace/dist/run/episode-bind.js";
import {
  assembleDecisionCommitInput,
  generateDecisionCommits,
  formatCommitMessage
} from "/workspace/dist/tools/decision-commit.js";

const ROOT = "/tmp/r22i-bench";
const manifest = JSON.parse(readFileSync(join(ROOT, "manifest.json"), "utf8"));

function median(xs) { const s = [...xs].sort((a, b) => a - b); return s[Math.floor(s.length / 2)]; }
async function timeAsync(reps, fn) {
  const ts = [];
  for (let r = 0; r < reps + 3; r++) {
    const t0 = performance.now();
    await fn();
    if (r >= 3) ts.push(performance.now() - t0);
  }
  return median(ts);
}
function timeSync(reps, fn) {
  const ts = [];
  for (let r = 0; r < reps + 3; r++) {
    const t0 = performance.now();
    fn();
    if (r >= 3) ts.push(performance.now() - t0);
  }
  return median(ts);
}

for (const rec of ["flat16", "flat64", "flat128"]) {
  const { stateRoot, runId } = manifest[rec];
  const reps = 40;
  const store = new EventStore(stateRoot, runId);
  const read = await store.readAll();
  const events = read.events;
  const E = events.length;

  const tReadAll = await timeAsync(reps, () => new EventStore(stateRoot, runId).readAll());
  const tReplay = timeSync(reps, () => replayRun(events));
  const rawCp = await new CheckpointStore(stateRoot, runId).read();
  const tCpRead = await timeAsync(reps, () => new CheckpointStore(stateRoot, runId).read());
  const tCpValidate = timeSync(reps, () => validateCheckpoint(rawCp));
  const checkpoint = validateCheckpoint(rawCp);
  const tInspectRun = await timeAsync(reps, () => inspectRun(stateRoot, runId));
  const tEpScan = timeSync(reps, () => episodeIdFromEvents(events));
  // I-side: main.ts L923-925 node summary expression on the real snapshot.
  const snapNodes = checkpoint.flowchart.snapshot.nodes;
  const tSummary = timeSync(reps, () =>
    Object.entries(snapNodes).map(([id, node]) => `${id}=${node.state}`).join(" ")
  );
  // I-side: inspect --json emit expression (stringify per event).
  const tJsonEmit = timeSync(reps, () => { for (const ev of events) JSON.stringify(ev); });
  // commits plane (tools).
  const tAssemble = timeSync(reps, () => assembleDecisionCommitInput(checkpoint, events, runId));
  const input = assembleDecisionCommitInput(checkpoint, events, runId);
  const tGenerate = timeSync(reps, () => generateDecisionCommits(input));
  const proposals = generateDecisionCommits(input);
  const tFormat = timeSync(reps, () => proposals.map(formatCommitMessage).join("\n\n"));

  const inspectTextTotal = tReadAll + tReplay + tEpScan + tCpRead + tCpValidate + tSummary + tInspectRun;
  const commitsTotal = tReadAll + tCpRead + tCpValidate + tAssemble + tGenerate + tFormat;
  console.log(
    `${rec} (E=${E}, P=${proposals.length}): readAll(G)=${tReadAll.toFixed(2)}ms replay(G)=${tReplay.toFixed(3)} ` +
    `cpRead(G)=${tCpRead.toFixed(2)} cpValidate(G)=${tCpValidate.toFixed(2)} inspectRun(G,2nd-read)=${tInspectRun.toFixed(2)} ` +
    `epScan(G)=${tEpScan.toFixed(4)} summary(I)=${tSummary.toFixed(3)} jsonEmit(I)=${tJsonEmit.toFixed(3)} ` +
    `| assemble(T)=${tAssemble.toFixed(3)} generate(T)=${tGenerate.toFixed(2)} format(T/I-join)=${tFormat.toFixed(2)} ` +
    `| inspect-text-sum=${inspectTextTotal.toFixed(2)} commits-sum=${commitsTotal.toFixed(2)} ` +
    `| per-event: readAll=${((tReadAll * 1000) / E).toFixed(1)}us inspectRun=${((tInspectRun * 1000) / E).toFixed(1)}us`
  );
}
```

原始输出：

```
== v22.22.2 ==
flat16 (E=27, P=16): readAll(G)=0.21ms replay(G)=0.004 cpRead(G)=0.14 cpValidate(G)=0.26 inspectRun(G,2nd-read)=0.19 epScan(G)=0.0008 summary(I)=0.004 jsonEmit(I)=0.035 | assemble(T)=0.000 generate(T)=0.09 format(T/I-join)=0.01 | inspect-text-sum=0.81 commits-sum=0.71 | per-event: readAll=7.9us inspectRun=7.2us
flat64 (E=87, P=64): readAll(G)=0.37ms replay(G)=0.013 cpRead(G)=0.45 cpValidate(G)=0.82 inspectRun(G,2nd-read)=0.36 epScan(G)=0.0017 summary(I)=0.013 jsonEmit(I)=0.125 | assemble(T)=0.000 generate(T)=0.30 format(T/I-join)=0.02 | inspect-text-sum=2.02 commits-sum=1.96 | per-event: readAll=4.3us inspectRun=4.1us
flat128 (E=167, P=128): readAll(G)=0.65ms replay(G)=0.004 cpRead(G)=0.85 cpValidate(G)=1.34 inspectRun(G,2nd-read)=0.60 epScan(G)=0.0007 summary(I)=0.019 jsonEmit(I)=0.194 | assemble(T)=0.000 generate(T)=0.91 format(T/I-join)=0.02 | inspect-text-sum=3.46 commits-sum=3.77 | per-event: readAll=3.9us inspectRun=3.6us
== v22.14.0 ==
flat16 (E=27, P=16): readAll(G)=0.19ms replay(G)=0.004 cpRead(G)=0.14 cpValidate(G)=0.25 inspectRun(G,2nd-read)=0.19 epScan(G)=0.0008 summary(I)=0.004 jsonEmit(I)=0.035 | assemble(T)=0.000 generate(T)=0.09 format(T/I-join)=0.01 | inspect-text-sum=0.77 commits-sum=0.67 | per-event: readAll=7.0us inspectRun=6.9us
flat64 (E=87, P=64): readAll(G)=0.39ms replay(G)=0.012 cpRead(G)=0.43 cpValidate(G)=0.79 inspectRun(G,2nd-read)=0.38 epScan(G)=0.0016 summary(I)=0.012 jsonEmit(I)=0.122 | assemble(T)=0.000 generate(T)=0.29 format(T/I-join)=0.02 | inspect-text-sum=2.01 commits-sum=1.92 | per-event: readAll=4.5us inspectRun=4.3us
flat128 (E=167, P=128): readAll(G)=0.63ms replay(G)=0.004 cpRead(G)=0.86 cpValidate(G)=1.33 inspectRun(G,2nd-read)=0.65 epScan(G)=0.0007 summary(I)=0.017 jsonEmit(I)=0.201 | assemble(T)=0.000 generate(T)=0.91 format(T/I-join)=0.03 | inspect-text-sum=3.50 commits-sum=3.76 | per-event: readAll=3.8us inspectRun=3.9us
```

### A.4 冷进程管道探针（/tmp/r22i-bench/coldprobe.mjs + coldprobe-driver.mjs）

```javascript
// coldprobe.mjs — R22-I cold-process probe: ONE full read-back pipeline in a
// FRESH process (no JIT warmup), mirroring the exact stage sequence of the
// two commands. argv: <stateRoot> <runId> <mode: inspect|commits>
// Prints: <importMs> <pipelineMs>
import { performance } from "node:perf_hooks";

const [, , stateRoot, runId, mode] = process.argv;
const t0 = performance.now();
const [{ EventStore }, { CheckpointStore }, { replayRun, validateCheckpoint }, { inspectRun }, { episodeIdFromEvents }, dc] =
  await Promise.all([
    import("/workspace/dist/run/event-store.js"),
    import("/workspace/dist/run/checkpoint-store.js"),
    import("/workspace/dist/run/replay.js"),
    import("/workspace/dist/run/inspection.js"),
    import("/workspace/dist/run/episode-bind.js"),
    import("/workspace/dist/tools/decision-commit.js")
  ]);
const tImport = performance.now() - t0;

const t1 = performance.now();
if (mode === "inspect") {
  const read = await new EventStore(stateRoot, runId).readAll();
  replayRun(read.events);
  episodeIdFromEvents(read.events);
  const rawCp = await new CheckpointStore(stateRoot, runId).read();
  const checkpoint = validateCheckpoint(rawCp);
  Object.entries(checkpoint.flowchart.snapshot.nodes).map(([id, node]) => `${id}=${node.state}`).join(" ");
  await inspectRun(stateRoot, runId);
} else {
  const read = await new EventStore(stateRoot, runId).readAll();
  const rawCp = await new CheckpointStore(stateRoot, runId).read();
  const checkpoint = validateCheckpoint(rawCp);
  const input = dc.assembleDecisionCommitInput(checkpoint, read.events, runId);
  const proposals = dc.generateDecisionCommits(input);
  proposals.map(dc.formatCommitMessage).join("\n\n");
}
const tPipeline = performance.now() - t1;
console.log(`${tImport.toFixed(2)} ${tPipeline.toFixed(2)}`);
```

```javascript
// coldprobe-driver.mjs — fresh-spawn coldprobe.mjs 10x per (record, mode).
import { spawnSync } from "node:child_process";
import { readFileSync } from "node:fs";

const manifest = JSON.parse(readFileSync("/tmp/r22i-bench/manifest.json", "utf8"));
const nodeBin = process.argv[2];
for (const rec of ["flat16", "flat128"]) {
  for (const mode of ["inspect", "commits"]) {
    const { stateRoot, runId } = manifest[rec];
    const imp = [], pipe = [];
    for (let r = 0; r < 10; r++) {
      const res = spawnSync(nodeBin, ["/tmp/r22i-bench/coldprobe.mjs", stateRoot, runId, mode], { encoding: "utf8" });
      if (res.status !== 0) { console.error(`FAIL ${rec}/${mode}: ${res.stderr}`); process.exit(1); }
      const [i, p] = res.stdout.trim().split(" ").map(Number);
      imp.push(i); pipe.push(p);
    }
    imp.sort((a, b) => a - b); pipe.sort((a, b) => a - b);
    const med = (xs) => xs[Math.floor(xs.length / 2)];
    console.log(`${rec}/${mode}: import median=${med(imp).toFixed(2)}ms pipeline median=${med(pipe).toFixed(2)}ms (min ${pipe[0].toFixed(2)})`);
  }
}
```

原始输出：

```
== v22.22.2 ==
flat16/inspect: import median=14.21ms pipeline median=6.59ms (min 6.02)
flat16/commits: import median=14.03ms pipeline median=6.29ms (min 6.04)
flat128/inspect: import median=13.99ms pipeline median=12.01ms (min 11.27)
flat128/commits: import median=14.02ms pipeline median=16.17ms (min 15.83)
== v22.14.0 ==
flat16/inspect: import median=17.06ms pipeline median=5.59ms (min 4.63)
flat16/commits: import median=16.68ms pipeline median=5.23ms (min 4.59)
flat128/inspect: import median=17.17ms pipeline median=10.88ms (min 9.75)
flat128/commits: import median=16.86ms pipeline median=14.96ms (min 14.43)
```

（import 段对 inspect 而言几乎全部已在 main.js 静态图内——main.ts L29 静态导入 run/inspection.js——故端到端差 ≈ 冷管道；对 commits 而言 commits.js + tools/decision-commit.js 是 S5-I-1 惰性边，差额 ~2-3 ms 与端到端差和冷管道之差吻合。）

MORE_OPTIMA=no
