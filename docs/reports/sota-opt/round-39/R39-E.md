MODEL_SLUG=claude-fable-5-thinking-xhigh

# R39-E：learning 切片 Round 39 三十九搜报告

**战役:** 全库持久 SOTA 优化 Round 39 / R39-E（十区之一，R1-E…R38-E 的第三十九遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `f525f27`（派单 tip，R39-B 摄取 + R39-E 派单；本分支自该点创建）。工作期间 exclusive **两次纯 docs 快进**：(一) 读序阶段（任何测量之前）fetch 检测到 `f525f27` → `445fd81`（R39-D 摄取（空枚举）+ round-39 PLAN/PROGRESS 更新 + R39-F 派单——`git diff f525f27..445fd81 --stat` 恰 3 个 docs 文件（PROGRESS / round-39 PLAN / R39-D.md，151 插入 19 删除））；(二) 门禁后、提交前二次 fetch 检测到 `445fd81` → `e9b1d30`（R39-C 摄取（空枚举）+ R39-G 派单——恰 3 个 docs 文件（PROGRESS / round-39 PLAN / R39-C.md，545 插入 18 删除））。两段增量对 `src test scripts package.json pnpm-lock.yaml` 直接复核均 **0 行**，均已按令执行**纯快进**（`git merge --ff-only`，零冲突）并各重验冻结（切片 vs `adb20d7` 与最强冻结 `89f0455..HEAD -- src/` 在三 tip 均 **0 行**，`wc -l` 仍 1770）。测量窗口（锚点三官方运行 + 聚焦套件 + `pnpm gate`）全部在 `445fd81` 工作树完成——测量窗口内零快进；FF2 后 `src/` + `test/` 逐字节相同（docs-only diff），测量结果原样携带，聚焦套件与 `pnpm gate` 在快进后 `e9b1d30` 树上**各再跑一次**同计数全绿（§4）。已先后通读新姊妹报告 **R39-D**（D 区诚实空枚举：零新 ID、零新站点级换名记录、S0c 地板 3.46–3.50 ms 落派单对照带内、两处独立重瞄归 S4-D-3 / S5-D-3 属主，全为 `src/adaptation/` D 区物理对象）与 **R39-C**（C 区诚实空枚举：零新 ID、连续第二十一遍零从未点名站点、零新换名记录、中位重锚 665.9–693.4、组合天花板 19.0–30.1 < 35、sink=7.309 十二读全同、两处独立重瞄归 X1-3 族 / X3-5 + S1-C-8 属主，全为 `src/routing/` C 区物理对象；其硬保持段独立复证 S8-E-1 恰 3 处——与本轮 §2.3 普查互为佐证不互抄）并折入非移植清单（§3）——零 E 可移植轴，不重铸。派单要求预读的两份姊妹报告 **R39-A**（空枚举，三项换名检查拒列——全为 A 区物理对象；13.6–16.0 µs/gate）与 **R39-B**（空枚举，零新站点级换名记录；S12-B-2 腿 (a) 与 S13-B-1 经其重取证均未触发）亦已按派单全文通读并折入非移植清单（§3）。R39-F/R39-G 在飞，按令未等待、未触碰姊妹分支。
**分支:** `cursor/r39-e-learning-thirty-ninth-pass-83a1`
**切片:** `src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop` 283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 / `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 / `task-success` 69 = **1770 行**，`wc -l` 与 R14-E…R38-E 逐一相同），本轮第三十九遍全量实际重读。
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R32-E…R38-E 正批及 R39-A/R39-B/R39-C/R39-D 引擎线位级相同；`/exec-daemon` PATH 暴露的系统 Node v22.14.0 已显式绕开——开工即按 R33-E…R38-E 在案陷阱 PATH 前置 `~/.nvm/versions/node/v22.22.2/bin`，`which node` / `node --version` / `process.versions.v8` 三复核 v22.22.2 / 12.4.254.21-node.39，官方三运行逐 run 打印引擎复核；v22.14.0 上**零官方批次、零弃置批次**。tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成，锁文件未改）。

## 结论

**第三十九遍诚实枚举为空：不铸任何 S39-E-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，切片生产代码零改动；且本轮枚举**零**新站点级换名记录——与 R36-E / R37-E / R38-E / R39-A / R39-B / R39-C / R39-D 同为「残余角度均系已点名站点或既档记录复命中」的干净空收口。** 对 10 个源文件逐行第三十九遍扫描，对照完整排除表（X0-\*…X4-\*、S1-E-1..8 … S9-E-1..3 全系、S13-B-1、R10-E…R38-E 全部无号关闭探针与轴，含 R30-E 悬挂点拓扑、R31-E I/O 文本编解码拓扑、R31-C **BINDX**、R31-J **MODEX**、R32-E §2.2 两项记录、R33-E §2.2 两项复命中、R34-E §2.2 L123 正则记录（派单明令不重提）、R35-E §2.2 L88 `signals.some` 记录（派单明令不重提）、R36-E §2.2 六项复命中 a–f、R37-E / R38-E §2.2 属主再验与两项独立重瞄记录（派单明令不重铸））与 round-39 PLAN 禁榜（含 R39-A/R39-B 空枚举折入 + R39-C/R39-D 空枚举快进折入）后，每一个执行站点都能指到既有 ID、无号探针、已点名裁决站点或 plan-ban 轴——**零未点名站点、零新站点级换名记录**。本轮从零重推的全部候选角度经查证均为既档复命中（§2.2）：六个主要角度与 R36-E §2.2 复命中 a–f（R37-E / R38-E 属主再验同六站点）逐站点重合（parse 路径三回调闭包 = R11-E 载入域真子集；`familyFromRole` 换表 = R24-E hit-min 臂；diagnostics 双除法代数合并 = R14-E 逐位理论失败；`kind === "human"` 被吸收析取 = R10-E 规格意图护栏；auto-loop L81 map eta 缩减 = S5-E-3 伞；跨文件相似度去重 = R12-E/R13-E test-only 伞），本轮独立重推再验其属主后引用即止；另有两处独立重瞄（diagnostics/patterns 分组循环 `get ?? []` + 冗余 `set` = **S3-E-3 本体**；`applyLearnedRouting` 三连 `includes` 回退链 = **S13-B-1 已计价格真子集**，R36-E §2.1 已点名、R37-E/R38-E §2.2 已重瞄在案）核对后归于既有 ID 本体，零新记录。生产调用方普查第三十九次确认：learning 模块切片外 `src/` **import 边恰 7 文件**（`privacy/record-classes.ts` 的 2 处 `learning/` 匹配全为数据面路径字符串（L212/L224），非 import 边，rg 直证）；`patterns` / `signatures` / `attribution` / `diagnostics` 切片外 `src/` 导入方为零（rg 空匹配 exit 1；运行时可达面仅测试，diagnostics 另有切片内 auto-loop 消费已计价）；**`loadLearnedRouting` 生产调用位点恰 3 处不变**（`cli/main.ts:708`、`run/flowchart-run.ts:712`、`track/loop.ts:88`——S8-E-1 硬不变量零去重，§2.3）。

按派单在本 VM 重锚默认预算（未拷贝历轮数字；cmp25 夹具 = R25-E 种子/NOW 跨轮可比面；装具为 R32-E 附录 A 归档件的逐行衍生——先复核归档正文指纹逐字符相同再施轮次置换 12 行，指纹与增量清单见附录 A）：

- **SLICE-CPU cmp25 总量：官方三读数 15.0 / 15.0 / 15.0 µs/run**（全部 v22.22.2）vs 默认带 14.7–15.2：**3/3 带内**（收拢在带内中段，零擦沿）；vs 派单默认对照 R38-E 读数带 15.0–15.2：恰落其低沿——本 VM 属战役窗口的中段宿主，冻结 0 行下按令不构成新发现，**默认带沿用 14.7–15.2 µs/run 不变**。即使整段清零仍 **665–668× 低于 10 ms 落地线**（装具逐 run 打印值）。
- 分段 collect 11.4–11.5（vs R25-E 带 11.3–11.9：**3/3 带内**）/ outcomes 1.3–1.4（带 1.3–1.5 **3/3 带内**）/ bandit 0.8 ×3；diagnose 1.36 ×3 vs 带 1.33–1.46：**3/3 带内**（本轮零擦沿——较 R37-E/R38-E 的低侧单格擦沿更干净）。**15/15 分段读数全部带内。**
- **夹具 VERDICT 行 3/3 逐位一致且与 R25-E…R38-E 夹具行逐字相同**（`E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3`）；live 面 `nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2` 三十九轮不变。
- 配置态锚点（3 采样）：apply(avoid=10, M=10) **266–272 ns/task**（R13-E…R30-E 带 257–312 内 **3/3**——S13-B-1 维持 loser ID 的带内直证）、空策略 **114–124 ns/task**（110–140 带内 **3/3**）、load 现实 registry **80.4–84.5 µs**（74.3–134.5 带内 **3/3**）、load fresh(ENOENT) **27.9–36.5 µs**（历史并集 25.2–36.8 内 **3/3**）。四格 **12/12 官方采样全部带内**（对照 R38-E 12/12 同为全带内收口）。

回归与门禁（§4）：聚焦切片套件全绿（unit/learning 7 文件 **46/46**：`445fd81` / `e9b1d30` 两 tip 各一次，逐字同计数；含 acceptance/adaptive-loop 的完整聚焦口径 **48/48 / 2 suites** 两 tip 各一次，与 R37-E/R38-E 记录计数逐字同）；`pnpm gate` 两跑均 **exit 0**（tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1，与 R14-E…R38-E / R39-A/R39-B/R39-C/R39-D 逐字同计数；`445fd81` / `e9b1d30` 各一跑）。

硬性不变量逐条复核（§0）：双 LCB / 双归因在位；live = R0 等价（R1 未接线）；proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；CAS / 凭证 / 数据面合同 / 公开签名零触碰；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭；**S8-E-1 零去重、S9-E-2 未触碰（两遍结构读码在位）、S13-B-1 维持 loser ID、S12-B-2 条件陆器 E 侧入口见证未触发**（`adaptation/eval-routing.ts` 全文 `prior` rg 零匹配 exit 1，仍只传 `learned`）。仓库变更仅本报告一个文件。空枚举与 R10-E…R38-E、R39-A/R39-B/R39-C/R39-D 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → EXCLUSIONS.md（完整表 545 行，S1-\* … S13-B-1 + X 继承行全部实读；快进后复核仍 545 行零增）→ PROGRESS.md → round-39/PLAN.md（E 段 + 具名/unnamed 全 ban 榜）→ R38-E.md 全文（默认对照读数带 15.0–15.2 / 默认带 14.7–15.2 与其 §2.2 属主再验 a–f + 两项独立重瞄记录）→ R39-A.md / R39-B.md 全文（两姊妹轴折入非移植清单，§3）→ R38-A…R38-J 与 R37-A…R37-I 姊妹报告（全部空枚举，折入非移植清单）→ 10 个切片源文件全量重读，之后才动手测量。第一次快进后补读 R39-D.md 全文、第二次快进后补读 R39-C.md 全文并先后折入（§3）；两版 PLAN 快进 diff 复核 E 段均仅非移植清单先后补 R39-D / R39-C，默认对照与约束无实质变更。
- 基线漂移检查：分支创建点 = 派单 tip = `f525f27`；读序阶段第一次纯 docs 快进 `f525f27` → `445fd81`（增量恰 PROGRESS / round-39 PLAN / R39-D.md 三 docs 文件，151 插入 19 删除）；测量窗口（锚点 + 聚焦套件 + 门禁）全部在 `445fd81` 完成，窗口内零快进；门禁后、提交前检测到第二次纯 docs 快进 `445fd81` → `e9b1d30`（增量恰 PROGRESS / round-39 PLAN / R39-C.md 三 docs 文件，545 插入 18 删除），两段对 `src test scripts package.json pnpm-lock.yaml` 直接 diff 均 **0 行**，均按令纯快进零冲突。冻结主张**五点核验均 0 行**（10 文件 vs `adb20d7`：分支创建时、第一次快进后、锚点测量后、第二次快进后、提交前）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均 docs，含 `445fd81` / `e9b1d30`）；切片最后生产变更维持 `9bf3e1a`（`git log -1 -- src/learning/` 直证；`adb20d7` 为其后的 docs-only 摄取提交，冻结基线语义与 R9-E…R38-E 逐轮一致）；锁文件 vs `adb20d7` **0 行**。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：async suspension-point topology / per-run suspension-manifest（R30-E——零悬挂计数、零 manifest 臂）/ **PWL**（`proposeRoutingFromOutcomes` 提案写路径读码即止）/ **RVL** / **RTL** / **CG** / **JB**（`applyCascadeRetry` spread 形态不重提）/ **NVG** / **REPRX**（切片 `JSON.parse` 站点恰 **3**：learned-routing.ts:55、bandit-store.ts:20、auto-loop.ts:239——rg 直证逐位点同 R33-E…R38-E，全部既有定价面，计数即止不重铸）/ **PAIRX** / **ZREP** / **LINKTOP** / **BINDX**（切片 `Math.*` token 恰 **1 个**：attribution.ts:45 `Math.min`，rg 直证，住在零生产流量休眠模块内，计数即止零剂量零 patch）/ **MODEX**（切片零 chmod / 零 umask / 零八进制 mode 字面量，rg 空匹配 exit 1，声明即止）/ I/O text-codec topology（R31-E——切片 3 个 fs UTF-8 位点：auto-loop.ts:236、bandit-store.ts:14/:80，读码直证原形在位，codec / transcode-byte 臂零重做）/ **R38-E 零新站点级记录**（派单明令不重铸——本轮 §2.2 为独立重推后的属主查证，非重演该声明）/ **R37-E 零新站点级记录**（同上）/ **R35-E §2.2 记录 1**（`runAutoAdaptLoop` L88 `signals.some((s) => s.modelId !== undefined)` bandit 前置门探针融合/下沉——派单明令不重提；本轮读码复核原形在位 auto-loop.ts:88，零重铸）/ **R34-E §2.2 记录 1**（`loadLearnedRouting` L123 `/no registry snapshot/` 正则字面量每调用求值——派单明令不重提；读码复核原形在位）/ R32-A 重开条件哨兵审计方法（不重铸——凡要求 src/ 变更的重开条件由 0 行冻结判空；S6-E-1 事件规模条件与 S12-B-2 条件陆器由 §2.1/§2.3 读码顺带见证未触发）/ R32-B 闭合稳定性审计（不重走，不铸「再查一遍闭合稳定性 / 重开条件 / 门扉 / 未点名站点」式新面）/ R33-A 44/44 A 账本卫生（不重演账本分类）/ **R33-A … R38-J 空枚举** / **R39-A 空枚举**（三项换名检查拒列全为 tracking A 区物理对象——turn.ts:75 `?? []` 全流量回退臂 / roller.ts 三连 `?? []` / roller.ts:84–85 条件 spread 否臂 `{}`，划界见 §3）/ **R39-B 空枚举**（零新站点级换名记录，划界见 §3）/ **R39-D 空枚举**（第一次快进折入，划界见 §3）/ **R39-C 空枚举**（第二次快进折入，划界见 §3）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX 全部未编号未移植、未写入 EXCLUSIONS 表行。X\* / S1-\* … S38-\* 全部未重开。
- 硬不变量零触碰：双 LCB（beta + normal，`src/routing/posterior.ts` 属 C 区）与双归因（logit + prob-add，`src/experiments/attribution-report.ts` 属 F 区）均保留；live = R0 等价（R1 未接线）；sparse → approved R0 baseline；promotion proposal-first（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；不改测试迁就重构；CAS / 凭证 / 数据面合同 / 公开签名不动；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭；分析不改 in-flight run、tracking 无命令权、H/score 不写路由 PASS/FAIL（零 diff 天然满足）；**S8-E-1 不去重 loadLearnedRouting**（本轮仅 §2.3 重普查其 3 个调用位点与 §1 单价带；S9-E-2 负优化未触碰——collectSignalsFromEvents 两遍结构读码在位：L139–157 pass-1 + L160–242 pass-2）；S13-B-1 维持（apply 格带内直证 + learned-routing.ts 冻结 0 行，与 R39-B §其重取证段及 R39-C 硬保持段互为佐证不互抄）。catalog 行序契约（R28-B W3）与 npm POSIX `.bin` symlink 静默 no-op（R28-I）均为切片外硬保持项，零触碰。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。不开 PR。输家脚本不入库（本轮无输家道——除锚点装具外零计时新臂；装具为既档衍生未提交 `scripts/`，增量与指纹见附录 A，临时文件核后删除）。

## 1. 冻结证明与 SLICE-CPU / 配置态重锚（cmp25，三官方运行）

```bash
git log --oneline -1 origin/cursor/sota-persistent-opt-83a1
# e9b1d30 docs(sota-opt): ingest R39-C (empty enum) and dispatch R39-G
#（测量窗口在 445fd81，窗口内零快进；两次纯 docs 快进
#  f525f27 → 445fd81 → e9b1d30，两段 src/ 均 0 行，
#  每次纯快进后冻结重验仍 0 行）
git diff adb20d7 -- src/learning/ | wc -l        # 0
git diff 89f0455..HEAD -- src/ | wc -l           # 0
wc -l src/learning/*.ts                          # 1770 total（同 R14-E..R38-E）
git log --oneline -1 -- src/learning/            # 9bf3e1a（最后生产变更不变）
```

锚点装具 `/tmp/r39e-anchor.mts`（365 行）为 **R32-E 附录 A 归档件的逐行衍生**：从 round-32/R32-E.md 附录 A 提取归档正文（复核 365 行、sha256 `494f801c2ccb621dbf7abca6d3116561b98010be09cf2aa19ad3cf366d64caff` 与 R32-E…R38-E 归档值逐字符相同），增量仅轮次置换共 **12 行**（附录 A 逐行清单）：头注 R39 标注（L2–5/7/14）、mkdtemp 前缀 `r39e-cmp25-*`（L327/332）、projectRoot `/tmp/proj-r39e`（L329/333）、detector identity `r39e-anchor-cmp25`（L336/346）。**seeds `0xe25e01/02/08` 与 NOW = R25-E `2026-08-25T17:30:00.000Z` 逐字节未动**（L84/L271–273 置换后 grep 直证；置换 diff 恰 12 对行，`r32e` 残留 0 匹配）⇒ cmp25 跨轮可比面维持，VERDICT 行必须逐字重现 R25-E…R38-E 夹具行（下表 3/3 达成）。官方批 **3 × v22.22.2** 独立进程（逐 run 打印 `node --version` + `process.versions.v8` 引擎复核；v22.14.0 零批次零弃置；一次冒烟运行验证装具启动后按纪律弃置）；每次 3/3 sanity 全过。代表性一次运行（run 1）：

```text
VERDICT fixtures: E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=11.5us outcomes=1.4us diagnose=1.36us bandit-build=0.8us | total in-slice CPU ~15.0us per run vs landing bar >=10000us (665x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=269ns/task | empty policy=116ns/task -> x10 tasks/run = 2.69us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=36.5us | realistic registry (baseline+1 candidate)=82.7us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

三官方运行原始区间（全量见附录 A）：

| 格 | 本轮三读数 | 历史带 | 判定 |
|---|---|---|---|
| SLICE-CPU 总量 | 15.0 / 15.0 / 15.0 µs | 默认带 14.7–15.2；派单对照 R38-E 15.0–15.2 | **默认带 3/3 带内**（收拢带内中段，恰落对照带低沿）——**带沿用不变** |
| collect | 11.5 / 11.4 / 11.5 µs | R25-E 11.3–11.9 | **3/3 带内** |
| outcomes | 1.4 / 1.4 / 1.3 | R25-E 1.3–1.5 | **3/3 带内** |
| diagnose | 1.36 / 1.36 / 1.36 µs | R25-E 1.33–1.46 | **3/3 带内**（本轮零擦沿） |
| bandit-build | 0.8 ×3 | 0.8 | 3/3 |
| apply(avoid=10, M=10) | 269 / 266 / 272 ns/task | 257–312 | **3/3 带内** |
| apply(空策略) | 116 / 124 / 114 ns/task | 110–140 | **3/3 带内** |
| load fresh(ENOENT) | 36.5 / 32.8 / 27.9 µs | 并集 25.2–36.8 | **3/3 带内** |
| load real registry | 82.7 / 80.4 / 84.5 µs | 74.3–134.5 | **3/3 带内** |

SLICE-CPU 重锚判定：**默认带沿用 14.7–15.2 µs/run 不变**，本轮三读数 15.0 ×3 完整落于其内且恰落派单对照 R38-E 读数带 15.0–15.2 的低沿；即使整段清零仍 665–668× 低于 ≥10 ms 落地线——派单 µs/ns 拒收类直接适用于切片全部 CPU 面。once-per-run 生产 I/O 位点（load fresh / load real）µs 域、once-per-run CLI 噪声拒收类同样适用。

**诚实注记**：(i) 本 VM 会话相对派单对照 R38-E 呈**中段刻度**（总量三读数逐一 15.0，收拢于对照带低沿），**15/15 分段读数与 12/12 配置态采样全部带内、零擦沿**——较 R37-E / R38-E 的低侧单格擦沿更干净的全带内收口。冻结 0 行 + 引擎位级同一 + 装具血统指纹逐字符相同 ⇒ 宿主刻度非切片行为变化，按派单令「host-bias windows with freeze 0 lines are not new findings」不作发现、不改带。(ii) 本轮三官方运行一次性完成（派单口径 3 正批）；一次冒烟运行（同字节装具，v22.22.2，3/3 sanity 全过、总量 15.1 带内）用于验证装具启动，按纪律弃置不计入官方口径；零校准批、零弃置批（官方口径内）、零中断存档事故。

## 2. 第三十九遍枚举（诚实结果：空；零新站点级换名记录）

### 2.1 逐文件重读与裁决（零未点名站点）

| 文件（行数） | 本轮重读检查（全部命中既有 ID / 无号探针 / 已点名站点 / plan-ban 轴） | 结论 |
|---|---|---|
| `auto-loop.ts`（283） | persistSignals evidenceIds.filter 双算（L212–214）= S1-E-4（I/O 支配）；persistSignals∥updateProjectBandit 编排重叠 = S4-E-2（双故障非确定）；ingestSubagentDirectory 顺序读 = S1-E-5；optimizedPolicy filter+map 融合（L144–146）= S2-E-6（n=10 实测更慢）；runAutoAdaptFromEvents 前置扫描 = S3-E-2 / S5-E-1；无变更跳存 = S2-E-1（X0-3 保存时机）；切片内惰性 import = S5-E-5；三段 signals spread 拼接（L86）= 分配级噪声（collect 锚整体计价）；**L88 `signals.some(modelId !== undefined)` bandit 前置门探针融合/下沉 = R35-E §2.2 记录 1 按令不重提**（原形读码复核在位）；L81 `.map((signal) => parseObservedSignal(signal))` eta 缩减 = R36-E 复命中 e 属主再验（S5-E-3 extraSignals 零生产流量伞真子集，§2.2）；avoid map 内 `toFixed(2)` reason 构串（L141）= 提案冷路径 PWL 属地；proposeAndMaybePromote 两分支各一次 `saveAdaptationRegistry` 文本合站 = 零动态增量非优化（R35-E 既点名，读码即止）；LEARN_EVALUATION_PLAN 与 from-episode 同名常量各模块求值一次 = X0-3 邻域重构（既点名）；异步悬挂点拓扑 = R30-E 面（plan-ban）；:236 utf8 decode / :239 JSON.parse = R31-E codec 面 + REPRX 计数即止 | 无新候选 |
| `learned-routing.ts`（216） | loadLearnedRouting 去重 = **S8-E-1 硬不变量**（调用位点普查 §2.3）；prefer.find 跨界去重 = S8-E-3；applyLearnedRouting 单次成本 = S13-B-1 维持 loser ID（266–272 ns 采样带内直证，§1）；其内 avoided Set 构建与**三连 `includes` 回退链（L206/209/212）**= 该格计价真子集（R36-E §2.1 已点名 + R37-E/R38-E §2.2 重瞄在案，本轮独立重瞄核对即止）；L123 错误探针正则字面量每调用求值 = **R34-E §2.2 记录 1 按令不重提**（原形读码复核在位）；parseLearnedRoutingPolicy 错误信息正则字面量提升 = R33-E 既点名（冷路径 + S3-E-5 族亚噪声）；parse 路径三个每调用映射回调闭包（avoid.map L65 / prefer.map L78 / assignments.flatMap L91）提升 = R36-E 复命中 a 属主再验（R11-E L52–109 载入期校验微观域真子集，§2.2）；stableProjectKey 记忆化 = R32-E §2.2 记录 1 拒列维持（X1-1 族 + once-per-run µs 拒收，不重铸）；identity/print/parse 面 = REPRX 划界 | 无新候选 |
| `from-episode.ts`（298） | proposeRoutingFromRoutedEvents 三遍扫描融合 = S2-E-2；提案写路径 = **PWL**（plan-ban，读码即止）；applyCascadeRetry spread / 死 behaviorDistribution 重算 = S9-E-1（**JB** 邻域不重提）；`eligibleModels.includes` 线扫（L261）= 同函数同伞；familyFromRouted 小表 includes（L288）= S7-E-3 小集合教训族；outcomeKindFromResult 死形参（L291 `outcome` 未读）= R32-E §2.2 记录 2 拒列维持（纯翻搅零收益，不重铸）；isCompleteRoute 逐字段 typeof 收窄消除 = R33-E §2.2 复命中 1 维持（谓词序 + S9-D-3 谱系防御纵深，拒列即止）；policyFromOutcomes seen Set 原形健全（个位数 arms 线性域）；routes Map + TASK_RESULT 绑定链 = outcomes 锚整体计价（1.3–1.4 µs 带内） | 无新候选 |
| `signals.ts`（439） | 两遍结构 = S1-E-1 本体 + **S9-E-2 负优化封死**（未以任何形态触碰，两遍结构读码在位：L139–157 / L160–242）；JUDGE_DECISION 双 Map.get（L218–223 role/family 各双读）= S1-E-2；verdict 双三元（L195/L204–209）= S7-E-5；extractAssistant 折叠 = S1-E-3；truncate 恒等快路径 / 流式早退 = S7-E-2 / S2-E-5；scoreUserAnswer 双正则 / trim = S6-E-2 / S7-E-1；baseSignal 条件 spread = S8-E-2（PIC 敏感）；形状单态化 = S4-E-3；ctx 提升 = S5-E-2；`/unknown agent/i` 提升（L272）= S3-E-5；request.agent 探针提升 = S7-E-4；探针复用 = S6-E-5；TASK_RESULT binding 双拷贝 = S2-E-7；五 Map 合一 = S6-E-1（重开条件未触发：事件规模未增 3 个量级且仍被 readAll 支配）；空事件快路径 = S4-E-1；familyFromRole if 链换表（L363–370）= R36-E 复命中 b 属主再验（R24-E hit-min 臂已计价站点 + 小集合教训族，§2.2） | 无新候选 |
| `bandit-store.ts`（83） | 循环内 arms.includes（L72）= 个位数 arms 小表噪声（S2-E-3 邻域，R35-E 既点名）；弃置 createBanditState（L53）= S2-E-3 本体；per-arm pulls/rewardSum 重建 = S3-E-4 邻域小表噪声；`[...arms]` spread（L52）= 锁内 I/O 支配分配级；L71 `kind === "human"` 被吸收析取消除 = R36-E 复命中 d 属主再验（R10-E 既点名冗余析取 + 规格意图护栏，§2.2）；recordReward 不可变翻搅 = X4-2 readonly 契约（本体属 C 区 `src/routing/bandit.ts`，越切片）；mkdir 每写（L79）= S6-E-3；pretty-print（L80）= S6-E-4；无变更跳写 = S2-E-4（write-always 契约）；arms/reward 双扫融合 = S3-E-4；:14/:80 utf8 / :20 JSON.parse = R31-E codec 面 + REPRX 计数即止 | 无新候选 |
| `task-success.ts`（69） | copyDefinedBinding 条件 spread = S5-E-3 族亚噪声；taskSuccessFromResult × scoreTaskResult 分派树合一 = S9-E-3（公开面扩张 / X1-2 复制）；V2 语义零触碰（RVL plan-ban） | 无新候选 |
| `attribution.ts`（92） | attributeToBoundary 排序换单遍 min = S1-E-7（test-only；其比较器内 BOUNDARY_ORDER.indexOf 逐次线扫同站点同伞）；findNegativeControlMarker 线扫 = 同一 test-only 可达面；:45 `Math.min` = BINDX 坐标计数即止（切片唯一 Math token，rg 直证）；生产调用方第三十九次为零（§2.3） | 无新候选 |
| `diagnostics.ts`（78） | 组内多遍融合单遍 = S1-E-6（真实规模亚噪声且 10× 更慢）；分组键换嵌套 Map = S5-E-4（平局序发散）；分组循环 `groups.get(key) ?? []` + 冗余 `set`（L27–29）= **S3-E-3 本体**（本轮独立重瞄核对即止，±8ns 抖动原裁决维持）；`unique()`/`mode()` Set 化 = R33-E §2.2 复命中 2 维持（FeedbackKind 基数 ≤4 线扫更快，拒列即止）；L36 `sum/samples/100` 双除法代数合并 = R36-E 复命中 c 属主再验（R14-E 逐位理论失败拒列原记录，§2.2）；L23–24 `kind === "human"` 被吸收析取 = 复命中 d 同伞（R10-E 行）；唯一生产消费方 = 切片内 auto-loop，锚点 diagnose 段 1.36 µs ×3 已计价（§1） | 无新候选 |
| `patterns.ts`（146） | detectRepeatedPatterns keys 预取 = S1-E-8（test-only）；相似度对键记忆化 = X2-6（已否决）；byKind 分组 `get ?? []` + set = S3-E-3 同机制（且住 test-only 域双重拒列）；computeFeatureSim keySet 物化 = 零生产流量域（signatures 行同判）；O(n²) 聚类 = test-only 零生产调用方（§2.3）；computeFeatureSim 与 compareSignatures 跨文件去重 = R36-E 复命中 f 属主再验（R12-E/R13-E test-only 面整体拒列伞，§2.2） | 无新候选 |
| `signatures.ts`（66） | JSON.stringify 哈希入参为内存对象（codec 不在环，R31-E C1 见证既档）；消费方 signals / attribution / patterns 均 type-only 或 test-only 可达；keySet 数组物化（L54–58 forEach push）= 同一零生产流量域；compareSignatures 去重角度 = 复命中 f 同伞 | 无新候选 |

结构位置 × 消除机制满矩阵（死码 / 常量折叠 / CSE / 记忆化 / 数据结构 / 融合 / 早退 / 免分配 / 复杂度类 / 强度削减 / 惰性 / 平行路径 / 形状单态化）逐格过筛：非空格全部落在上表既有 ID、plan-ban 轴、R32-E…R38-E §2.2 既有记录或 §2.2 本轮属主查证清单上。

### 2.2 换名拒绝检查：本轮零新站点级记录（全部既档复命中）

从零重推（假装排除表不存在重读 1770 行）枚举出的候选角度经对表与对历轮报告全文检索逐一查证，**全部为已点名站点 / 既档记录复命中或按令不重提项，零新站点级换名记录**（R33-E 恰 1 项 / R34-E 恰 1 项 / R35-E 恰 1 项 / R36-E 零项 / R37-E 零项 / R38-E 零项之后，本轮与 R38-E / R39-A / R39-B / R39-C / R39-D 同为干净零收口）：

- **属主再验 ×6（= R36-E §2.2 复命中 a–f 同站点；R37-E / R38-E 属主再验同六站点）**：本轮独立枚举再次落到同六个站点——(a) `parseLearnedRoutingPolicy` 三回调闭包提升（learned-routing.ts:65/:78/:91）→ R11-E 载入期校验微观域真子集；(b) `familyFromRole` if 链换表（signals.ts:363–370）→ R24-E hit-min 臂已计价站点 + 小集合教训族；(c) diagnostics L36 双除法代数合并 → R14-E 逐位理论失败（浮点除法非结合，S6-A-1 半格点同族）；(d) bandit-store.ts:71 / diagnostics.ts:23–24 `kind === "human"` 被吸收析取 → R10-E 规格意图护栏（human 反馈永不入 routing quality）；(e) auto-loop L81 map eta 缩减 → S5-E-3 零生产流量伞真子集；(f) `computeFeatureSim`（patterns.ts:122）↔ `compareSignatures`（signatures.ts:45）跨文件去重 → 语义不同构（kind 守卫 + hash 快路径）+ R12-E/R13-E test-only 整体拒列伞。六项属主逐一核对成立，引用即止，零新记录、零重铸。
- **独立重瞄 ×2（核对后归既有 ID 本体，非换位点；与 R37-E / R38-E §2.2 两项重瞄同站点，本轮独立重推再核）**：(i) diagnostics/patterns 分组循环 `get(key) ?? []` 后无条件 `set` 的冗余 Map.set 消除 = **S3-E-3 本体**（原行「公平副本对副本 ±8ns 抖动」逐字维持；patterns 站点另叠 test-only 域拒列）；(ii) `applyLearnedRouting` 尾部三连 `allowedModels.includes` 回退链单扫化 = **S13-B-1 已计价格真子集**（R36-E §2.1 已点名「三连 includes 回退链同属该格计价」；本轮 apply 格 266–272 ns 3/3 带内直证天花板）。
- **不重提（按令）：`runAutoAdaptLoop` L88 `signals.some` bandit 前置门探针** = R35-E §2.2 记录 1——派单 DO NOT REMINT 明列，本轮读码复核原形在位即止。
- **不重提（按令）：`loadLearnedRouting` L123 正则字面量每调用求值** = R34-E §2.2 记录 1——本轮读码复核原形在位即止。
- **不重铸：R32-E §2.2 两项拒列记录**（stableProjectKey 记忆化 = X1-1 族 + once-per-run µs 拒收双重命中；outcomeKindFromResult 死形参删除 = 纯翻搅零收益——L291 `outcome` 形参未读本轮读码直证原形在位）与 **R33-E §2.2 两项复命中**（isCompleteRoute 谓词收窄 / unique()+mode() Set 化）：裁决逐字维持，站点原形读码复核，不以任何新名重提。

### 2.3 生产调用方普查（第三十九次）

- 切片外 `src/` 对 learning 模块 **import 边恰 7 文件**：`run/flowchart-run.ts`（learned-routing）、`cli/adapt.ts`（from-episode + auto-loop）、`cli/main.ts`（learned-routing + auto-loop）、`adaptation/eval-routing.ts`（learned-routing）、`track/primary-split.ts`（L3 `import type`，type-only 读码复核）、`track/loop.ts`（learned-routing + auto-loop）、`routing/assign.ts`（applyLearnedRouting）——rg 全库直证（`from "../learning/…"` 边计数），与历轮记录逐文件一致，零新增。诚实注记：`rg "learning/" src/privacy/record-classes.ts` 命中 2 处（L212/L224），逐行核实全为数据面路径字符串（`adaptation/learning/projects/<stableProjectKey>/routing.json` / `bandit.json`），**非 import 边**，不改变 7 文件普查结论。
- `patterns` / `signatures` / `attribution` / `diagnostics` 切片外 `src/` 导入方 = **0**（rg `learning/(patterns|signatures|attribution|diagnostics)` 空匹配 exit 1）；运行时可达面仅 `test/acceptance/adaptive-loop.test.ts` 与 unit 套件（diagnostics 另有切片内 auto-loop 消费，已在锚点计价）。
- `loadLearnedRouting` 生产调用位点恰 3 处：`run/flowchart-run.ts:712`、`track/loop.ts:88`、`cli/main.ts:708`——S8-E-1 的 tracked/children 双载形态原样，零触碰。
- S12-B-2 条件陆器 E 侧入口读码直证：`adaptation/eval-routing.ts` 仍只传 `learned` 不传 `prior`（全文 `prior` rg 零匹配 exit 1，与 R39-B 重取证段及 R39-C 硬保持段一致）——未触发，属地在 B/D，声明即止。

### 2.4 枚举收口

第三十九遍逐文件重读**零未点名站点、零新站点级换名记录**；候选角度六项属主再验（§2.2 a–f）+ 两项独立重瞄归既有 ID 本体（S3-E-3 / S13-B-1 真子集），两项按令不重提项读码复核原形在位。切片全部 CPU 面住在 15.0 µs/run 总量之内（清零仍 665–668× 低于落地线），全部 I/O 面为 once-per-run µs 域既档定价（load fresh / load real 采样直证）或 R31-E codec 面既档（不重铸）。**第三十九遍枚举为空——比强铸败者 ID 更诚实。**

## 3. 姊妹面划界与非移植清单（R39-A/R39-B 派单预读 + R39-D/R39-C 快进折入）

1. **R39-A（tracking 侧空枚举）**——A 区第三十九遍从零重枚举（14 文件 1796 行）+ 三项站点级换名检查拒列不铸 ID（turn.ts:75 `?? []` 全流量回退臂 / roller.ts:30/:32/:36 三连 `?? []` / roller.ts:84–85 条件 spread 否臂 `{}`）+ 预算重锚 13.6–16.0 µs/gate ⇒ ~68–80 µs/run。E 侧划界：三站点全为 `src/tracking/` + `src/run/` A 区物理对象，与本切片零共享；其拒列站点形态（零/全流量回退臂 / 三连 `?? []` / 条件 spread 否臂）在 E 区的对应形态均已被既档承载（S5-E-3 伞 / S8-E-2 / S1-E-4 族）。无新轴可移植。
2. **R39-B（live 路由侧空枚举）**——B 区第三十九遍从零重枚举（10 文件 1359 行），零新站点级换名记录（两残余角度均为 R2-B 已点名站点复命中）；天花板重锚 ceiling M=2 8.31–9.77 / M=10 16.99–17.36 ms/eval、replay M=2 9.63–10.00；S12-B-2 腿 (a) 与 S13-B-1 重开条件经其重取证均未触发。E 侧划界：其 S13-B-1 哨兵与本轮 §1 apply 格 3/3 带内直证**互为佐证不互抄**；`routeR0` 属 B 区文件，本切片零消费。无新轴可移植。
3. **R39-D（adaptation 侧空枚举，第一次快进折入）**——D 区第三十九遍从零重枚举（14 文件 3294 行，基线 `82bef36` 四点冻结 0 行），零新 ID / 零新站点级换名记录；两处独立重瞄归既有属主（S4-D-3 / S5-D-3）；地板重锚 S0c 3.46–3.50 ms ⊂ R38-D 3.47–3.79 与 R37-D 3.40–3.80 对照带。E 侧划界：全部站点级对象住 `src/adaptation/` D 区物理对象；本切片消费的 `adaptation/promotion.js` / `adaptation/candidate.js` / `adaptation/registry.js` 公开导出双方均零改动（最强冻结 0 行覆盖）。无新轴可移植。
4. **R39-C（离线路由侧空枚举，第二次快进折入）**——C 区第三十九遍从零重枚举（9 文件 2039 行，基线 `183df9b` 五验 0 行），零新 ID / 连续第二十一遍零从未点名站点、零新换名记录；两处独立重瞄归既有属主（offline-prob-add `kappaS` 双 `means.reduce` = X1-3 族冻结数值路径本体 + S1-C-6 邻域零池；propensity `computeOverlapDiagnostics` 双 `weights.reduce` = X3-5 + S1-C-8 本体 + R27-C 零通量）；中位重锚 665.9–693.4 ms（中段宿主，官方电池一遍宿主瞬态撞 `ceiling<35` 断言按其 §1 裁定弃置）；组合天花板正典带 19.0–30.1 < 35；sink=7.309 十二读全同；其硬保持段独立复证 S8-E-1 恰 3 处与 S13-B-1 / S12-B-2 未触发（互为佐证不互抄——其 S8-E-1 rg 复核与本轮 §2.3 普查同结论）。E 侧划界：两重瞄站点均为 `src/routing/` C 区物理对象，本切片零 r1/shadow 消费（bandit-store 消费的 `src/routing/bandit.ts` 亦属 C 区，X4-2 划界维持）。无新轴可移植。
5. **R38-A…R38-J 十切片空枚举（Round 38 收口，开轮时已合入）**——全部为方法卫生 / 空枚举记录，未铸新面；R38-E 即本切片上一遍（默认对照，§1 已重锚）；R38-A 三项换名检查拒列全为 A 区物理对象；R38-B/R38-C/R38-D…R38-J 零新站点级记录。声明即止，不移植、不重做、不铸新轴。
6. **R37-A…R37-I 九切片空枚举（Round 37 收口，历轮已合入）**——全部为方法卫生 / 空枚举记录，未铸新面；其余各面物理对象与 E 零共享。声明即止，不移植、不重做、不铸新轴。
7. **R30-E 悬挂点拓扑 / R31-E codec 拓扑 / BINDX / MODEX / R32-A 哨兵方法 / R32-B 闭合稳定性方法 / R33-A…R38-J 空枚举 / R34-E L123 正则记录 / R35-E L88 探针记录 / R36-E 六项复命中记录 / R37-E、R38-E 属主再验与两项重瞄记录**——§0 逐项核对，全部零剂量零重做零重铸。

## 4. 测试与验证

```bash
# 本 VM: nvm Node v22.22.2（PATH 前置 ~/.nvm/versions/node/v22.22.2/bin
# 绕开 /exec-daemon 系统 Node v22.14.0）。
node --import tsx --test test/unit/learning/*.test.ts
# tests 46 / suites 1 / pass 46 / fail 0
#（445fd81 / e9b1d30 两 tip 各一次，逐字同计数）
npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0
#（完整聚焦口径，两 tip 各一次；与 R37-E/R38-E 记录计数逐字同）

pnpm gate
# typecheck ✓ + lint ✓ + test ✓ + build ✓ （GATE_EXIT=0，两跑）
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（两跑逐字同计数：445fd81 ×1 + e9b1d30 ×1；
#  与 R14-E..R38-E / R39-A/R39-B/R39-C/R39-D 逐一相同）
```

锚点装具官方 3 次运行全过（§1，3/3 sanity ×3；确定性 VERDICT 行 3/3 逐位一致且与 R25-E…R38-E 夹具行逐字相同）。本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交 `scripts/`，遵守 R7-F/R8-C/R10-E…R38-E 无赢家纪律）。装具与临时夹具目录（`/tmp/r39e-cmp25-*`、`/tmp/proj-r39e`、`/tmp/r39e-anchor.mts`）已在门禁前删除。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 仅 `docs/reports/sota-opt/round-39/R39-E.md`，提交前复核）。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐站点有属主；候选角度六项属主再验 + 两项独立重瞄归既有 ID 本体，零新站点级换名记录，两项按令不重提项原形在位）。
- 预算面：SLICE-CPU 默认带 14.7–15.2 µs/run 沿用不变（本轮官方读数 15.0 ×3 完整带内且零擦沿；宿主中段刻度，freeze 0 行按令不构成新发现），清零仍 665–668× 低于落地线；配置态四格 12/12 官方采样带内——切片无任何达「数十–数百 ms 真尺度非噪声」门槛的剩余面。
- 判定：**MORE_OPTIMA=no**。

## 7. 新增排除 ID：无

- 不铸 S39-E-\*：第三十九遍枚举为空；不取 ALLCAPS 绰号；不写入 EXCLUSIONS.md 任何行（含对空枚举轴外号的一切再编号）。零新站点级换名记录（§2.2 六项属主再验 + 两项本体命中各归属主，无需归档新记录）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态；S8-E-1 / S9-E-2 / S13-B-1 未重开；未重铸 R32-A 哨兵方法与 R32-B 闭合稳定性方法；未在 E 区重演 R33-A 账本分类；未重提 R32-E/R33-E §2.2 既有记录、R34-E §2.2 L123 记录与 R35-E §2.2 L88 记录；未把 R36-E/R37-E/R38-E「零新记录」声明重铸为新面；未移植 R39-A/R39-B/R39-C/R39-D 姊妹发现。

## 附录 A：锚点装具增量说明、指纹与原始输出（未入库；/tmp 留存核后删除）

- 装具：`/tmp/r39e-anchor.mts`，**365 行**，为 R32-E 附录 A 归档件的**逐行衍生**：先从 `round-32/R32-E.md` 附录 A（L387–751）提取归档正文并复核指纹（365 行，sha256 `494f801c2ccb621dbf7abca6d3116561b98010be09cf2aa19ad3cf366d64caff`，与 R32-E/R33-E/R34-E/R35-E/R36-E/R37-E/R38-E 归档值逐字符相同），再施加轮次置换共 **12 行**——头注 R39 标注（L2 轮次序数 thirty-ninth、L3 空枚举组序数、L4 谱系上界 R10-E..R38-E、L5 谱系上界 R3-E..R38-E、L7 夹具行谱系上界 R25-E..R38-E、L14 前缀/identity 注释行）、mkdtemp 前缀 `r39e-cmp25-fresh-` / `r39e-cmp25-reg-`（L327/332）、projectRoot `/tmp/proj-r39e`（L329/333）、detector identity `r39e-anchor-cmp25`（L336/346）。置换 diff 恰 12 对行（`diff` 计 24 行 `<`/`>`）；`r32e` 残留 rg 0 匹配。**seeds `0xe25e01`（collect）/ `0xe25e02`（routed）/ `0xe25e08`（signals）与 NOW = R25-E `2026-08-25T17:30:00.000Z` 逐字节未动**（L84/L271–273 置换后 grep 直证）；import 路径与语义同归档件。装具全文不重复内联（= R32-E 附录 A 正文 + 上列 12 行置换，任何人可机械重建并核指纹）。
- 本轮装具 sha256：`8386ba88e5d77c6eabc4c2c15a2bd7b24944f408863823ec9c6f51fc84a07cea`
- 运行方式（仓库根，`pnpm install --frozen-lockfile` 后，PATH 前置 nvm bin）：

```bash
node_modules/.bin/tsx /tmp/r39e-anchor.mts   # 独立进程 ×3 = 官方 runs 1–3（每 run 头部打印 node --version = v22.22.2 + process.versions.v8 复核；此前一次冒烟运行按纪律弃置）
```

三官方运行原始值：

```text
SLICE-CPU 总量:  15.0 / 15.0 / 15.0 us
collect:    11.5 / 11.4 / 11.5 us
outcomes:   1.4 / 1.4 / 1.3 us
diagnose:   1.36 / 1.36 / 1.36 us
bandit:     0.8 ×3
apply(non): 269 / 266 / 272 ns/task
apply(空):  116 / 124 / 114 ns/task
load fresh: 36.5 / 32.8 / 27.9 us
load real:  82.7 / 80.4 / 84.5 us
VERDICT 行: 3/3 逐位一致且与 R25-E..R38-E 夹具行逐字相同；
            sanity 3/3 ×3 全过
（弃置冒烟运行：总量 15.1 / collect 11.6 / outcomes 1.3 /
  diagnose 1.34 / bandit 0.8 / apply 276/125 / load 33.8/79.2，
  3/3 sanity 全过，值全部带内，按纪律不计入官方口径）
```

MORE_OPTIMA=no
BRANCH=cursor/r39-e-learning-thirty-ninth-pass-83a1
