MODEL_SLUG=claude-fable-5-thinking-xhigh

# R39-B：live 路由切片 Round 39 三十九搜报告

**战役:** 全库持久 SOTA 优化 Round 39 / R39-B（十区之一，R1-B…R38-B 的第三十九遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `fa9724e`（派单 tip，R38-J 摄取 + Round 38 收口 + Round 39 开卷 + 本波派单）。测量窗口内 exclusive **一次 docs-only 快进** `fa9724e → 08dacf8`（R39-A 摄取 + R39-D 派单）：段核对 `git diff fa9724e..08dacf8 -- src/ test/ pnpm-lock.yaml package.json` = **0 行**（触碰仅 PROGRESS.md / round-39 PLAN.md / 新增 R39-A.md 三个 docs 文件），全部测量、聚焦套件与 `pnpm gate` 在 `fa9724e` 工作树上完成后 `git merge --ff-only 08dacf8` 对齐，对齐后冻结复核仍 0 行；**R39-A 全文读毕**（A 区诚实空枚举 + 三项 A 站点级换名记录，全部折入不移植清单，B 段 PLAN 除新增「R39-A 空枚举」外零变化）。
**分支:** `cursor/r39-b-live-routing-thirty-ninth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R28-B…R38-B 引擎位级相同；系统 Node（`/exec-daemon/node`）已显式绕开——PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 后 `node --version` 复核 v22.22.2，三次官方运行首行均打印 `engine node=v22.22.2 v8=12.4.254.21-node.39`，仿真内置 L0 引擎同一性断言 ×3 全过；**零官方批次落在系统 Node 上**，无需弃批。tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成；`package.json`/`pnpm-lock.yaml` 对 `94ed3d9` 0 行 diff）。

## 结论

**诚实空枚举：无达门槛的新更优解，切片零代码改动，未铸任何 S39-B-\* ID、未铸任何新 face 名或 ALLCAPS 外号，EXCLUSIONS.md 零增行（全表仍 545 行）；且本轮枚举**零**新站点级换名记录——与 R34-B/R35-B/R36-B/R37-B/R38-B 同为「残余角度均系已点名站点复命中」的干净空收口。** 第三十九遍透镜按令**不**重走 R31-B 面格闭合审计（六扇停驻门维持）、**不**重走 R32-B 闭合稳定性审计（54/54 停驻记录）、**不**移植 R33-A…R38-J 与 R39-A 空枚举、**不**重提 R38-B / R37-B / R36-B / R35-B 零新站点级记录、**不**重铸「再查一遍重开条件 / 闭合稳定性 / 门扉 / 无名站点」式记录——改做**从零独立重枚举**：重读 10 文件全量 1359 行，重走结构位置 × 消除机制满矩阵（§2d/§4），对每个枚举出的角度执行换名拒绝检查。结果：全部角度落在既有排除 ID 本体、B 区自有封禁面、姊妹区归档面（明令不移植）或 R31-B 停驻门成员上；本轮枚举尾部仅存的两个残余微观角度（`assign.ts`/`assign-plan.ts` 每批 id 双 map / 双 Set、`decideLiveCascade` 的 `stay` 闭包每调分配）与 R34-B…R38-B §2d 完全同两站点，均为**已点名站点复命中**（分别 R2-B §4 点名 + S7-B-6 每批 ns 天花板 + R27-B batch prelude intercept face 属地、R2-B「S1-B-4 同表面」裁决并经 R5-B/R6-B/R8-B/R34-B…R38-B 八次维持），不构成新站点级记录。派单硬保持项显式重取证：**S12-B-2 重开条件腿 (a) 未触发**（`rg -c 'prior' src/adaptation/eval-routing.ts` = 0 匹配（rg exit 1），:353–354 只传 learned；`assignTasks` 生产调用方图景三处（`cli/main.ts:365`、`adaptation/eval-routing.ts:353–354`、`track/primary-split.ts:41`）与 R30-B…R38-B 逐条一致无新增）；**S13-B-1 重开条件未触发**（`learning/learned-routing.ts` 对 `94ed3d9` 逐字节 diff = 0 行，`applyLearnedRouting` 本体成本无变化，维持 loser ID）。

关键前提事实：切片 10 个文件（清单逐一核对恰为 10，`wc -l` 合计 **1359** 行，逐文件计数见 §0，与 R32-B…R38-B 逐数一致）自 R1-B 裁决基线 `94ed3d9` 以来**逐字节零变化**（`git diff 94ed3d9 -- <切片 10 文件>` = **0 行**，分支创建时、测量后、快进对齐后、提交前四点核对）。最强冻结 `git diff 89f0455..HEAD -- src/` 亦为 **0 行**（快进段本身 src/ 为空）。R1-B 的结构下界论证、R4-B 的聚合天花板论证与 S1-B-\* … S13-B-1 全部裁决对当前代码原样成立。

**测量侧诚实声明（承 R34-B…R38-B 方法注记）**：历届 B 驱动（/tmp/rXXb-sim.mts 族）从未入库，本 VM 上不存在其字节；本轮驱动为**按 R4-B…R38-B 附录约定从零重建**（种子 0xb44b01 mulberry32、N=2000/20000/30 语料、M=2/7/10 目录、×2 eval 对口径、W1/W2/W3 证人、L0 断言——细节见附录 A，含一次如实记录的簿记事项：本会话早段第一次重建批整批弃置，见附录 A 第 5 条），故与历届报告的**逐格数值带对照只具指示性**；效力载体是**结构性结论**（格间全序、learned 税方向、落地线余量、亚 µs 探针、prior 倍率）与 ×3 运行内部一致性。天花板本 VM 实测：**ceiling M=2 8.31–9.77 / M=10 16.99–17.36 ms/eval**（×3 运行，×2 对口径；M=10 三读**全部落在派单默认对照 R38-B 16.91–18.00 带内**；M=2 在 R38-B 10.91–11.98 邻接下侧（更快方向出带，R37-B 10.12–10.55 亦下侧）——回归不可能成立且更快方向出带无损（切片对 `94ed3d9` 零 diff），M=2 出带而 M=10 带内的**分向不一致**正是宿主 + 重建语料构成差的特征而非结构变化）；replay-faithful M=2 **9.63–10.00**（三读**全部落在 R38-B 9.00–10.14 带内**）；10× 压力 N=20000 **93.44–106.38 ms/eval**（与 R38-B 101.68–107.33 交叠，全部落在含 R35-B…R38-B 读数的历史并集 92.27–140.9 内）；全 18 矩阵格 ×3 保持 M=2 < M=7 < M=10 全序；learned ≥ no-learned 在 **7/9** 格对成立（M=2 +8.3%~+13.3%、M=10 +4.2%~+7.1% 六测全正，M=7 run 1 +0.5% 亦正），**诚实注记**：M=7 对 runs 2/3 反转（−1.1% / −1.8%，learned 侧反快）——与 R33-B（M=10 一例 −0.12%）、R34-B（M=7 两例 −2.3%）、R35-B（M=7 一例 −4.8%）、R36-B（M=7 三例 −1.1%~−6.0%）、R37-B（M=7 一例 −1.1%）、R38-B（M=7 三例 −1.6%~−4.9%）同一 M=7 配置格抖动族且本轮幅度带更浅，方向与 S13-B-1 机制旁注（配置目录下 avoid 的 allow-list 收缩红利可超过 learned 本体增费）一致，W1（该格双跑逐字节恒等）/ W2（分歧全部落在 avoid/prefer 命中族，inside=413 / outside=0，×3 逐数相同且三目录相等）证人全过、freeze 0 行——按令主机窗口效应不构成结构翻转、不铸坐标；`holds-below-landing-line=true` ×3（生产代表格最大 17.36 < 30，余量 ~42%）。prior 双档格：假设档 N=2000 M=7/M=10 = 38.53–43.96 / 46.72–48.98 ms/eval **六读全部越线**；M=2 = 25.83–28.81 ms/eval 三读全部线下（R37-B run 3 曾单读越线 30.68、R36-B run 1 曾 32.64，本轮未复现，同判宿主窗），三读为其 learned 孪生格（8.66–9.24）的 **2.9–3.1×**（驱动内显式复测；M=7 对矩阵孪生格 ~3.0–3.3×、M=10 ~2.9×）——「若有流量即数量级越线」的邻接配置结论不变，生产流量本轮重取证仍为零。落地线（数十~数百 ms 或复杂度类下降）在现有生产流量下依旧不可达。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- **切片恰 10 文件**（逐文件 `wc -l`）：`r0.ts` 173 / `assign.ts` 129 / `assign-plan.ts` 68 / `policy.ts` 160 / `live-cascade.ts` 118 / `live-selection.ts` 61 / `analyze-task.ts` 108 / `primary-catalog.ts` 101 / `catalog-model.ts` 83 / `supervisor/model-router.ts` 358，合计 **1359** 行，与 R32-B…R38-B 逐数一致。`catalog-observed.ts` **不在**本切片（按令未纳入、未触碰、未计时）。
- **冻结四点核对全过**：分支创建时（tip `fa9724e`）`git diff 94ed3d9 -- <切片 10 文件>` = **0 行**且 `wc -l` 合计 1359；测量后复核 0 行；ff-align 到 `08dacf8` 后复核 0 行；提交前再复核 0 行。最强冻结 `git diff 89f0455..HEAD -- src/` = **0 行**。`package.json` / `pnpm-lock.yaml` vs `94ed3d9` 亦 **0 行**。最后一次生产 live-B 变更维持 `94ed3d9`；最后一次 `src/` 生产变更维持 `183df9b`（S7-C，离线 C 区）。
- **读序（按令完成）**：README → EXCLUSIONS 全表（545 行，X0-\* … S13-B-1 全部实读；空枚举不增行）→ PROGRESS → round-39 PLAN 全文（B 段 + 具名/不具名禁令）→ R38-B 全文（默认对照带 + §2d 两残余角度边界）→ R38-A 与 R38-C…R38-J、R37-A…R37-J 全部实读（空枚举逐份折入不移植清单）→ 10 切片文件全量重读（1359 行）。快进落地后补读 **R39-A 全文**并折入（A 区三项站点级换名记录属地归 A，与本切片零交集；PLAN B 段除新增「R39-A 空枚举」入不移植清单外逐字未变）。
- **摄取状态**：测量窗口内独占分支一次 docs-only 快进（`fa9724e → 08dacf8`，段核对 src/ / test/ / lockfile 0 行）；按令 ff-align 完成，新姊妹报告 R39-A 已全文读毕折入；姊妹分支零触碰。
- **不移植 / 不重铸清单维持**（枚举时逐条遵守，本轮零触碰）：全部具名禁令 **FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX**；**R31-B 面格六停驻门**（catalog-row hidden-class / IC-shape、cascade evidence×failure-class lattice、assignTasks cold-onset segmentation、batch class-composition mix、LINKTOP-in-B、prior alias-resolution pathway——全部维持停驻，零重开）；**R32-B 闭合稳定性审计**（54/54 与 D1–D5 为停驻记录，不重走、不铸「再查一遍闭合稳定性」式新面）；**R33-A…R38-J 与 R39-A 空枚举**（各自站点级换名记录、方法卫生与轴归档均按属地维持，不移植不重铸；R33-B `ROLE_FAMILY` 死回退、R35-B / R36-B / R37-B / R38-B 零新站点级记录、R39-A 三项 A 站点级记录均不重提不重铸）；R30-B scale-cube face；R32-A F12a–d 哨兵审计；R32-J 六残余坐标（本切片零 fs 位点）。
- **B 区自有封禁面不补铸**：R29-B learned-policy container face / R28-B catalog-face row-arrangement (permutation) face / R27-B batch-cardinality bottom + prelude intercept face / R26-B privacy / provider-policy constraint-leg face / R25-B task-face optional-field presence topology / R24-B M=1 catalog collapse / R20-B objective 长度 L 轴 + K×W 双线性轴 / R19-B 比较器第三腿 + 目录基数 M 轴。**目录行预排序仍是行为变更**（R28-B W3 裁定行序为契约决策载体），本轮未尝试、未预排序。
- **硬不变量**：切片零代码改动 ⇒ Live=R0 等价（R1 未接线 live；稀疏回退 approved R0 baseline）、双 LCB（Beta+正态）、双归因（logit+prob-add）、proposal-first（不声称 Outcome-supported）、阈值、CAS/凭证/数据面契约/公共签名全部原样保持；live 面不 import R1/bandit/shadow 由 `live-isolation.test.ts` 在聚焦 198 项中复证通过。无 fsync / 完整性重哈希移除；fail-open 家族保持关闭；未改任何测试。分析不改在飞 run；tracking 无指挥权；H/score 不写 routing PASS/FAIL。S5-H-1 / S7-F-1 ≠ S6-F-5 / S8-I-1 / S8-E-1（`loadLearnedRouting` 恰 3 处：`track/loop.ts:88`、`run/flowchart-run.ts:712`、`cli/main.ts:708`，本轮 rg 重证）/ S9-D-4 / S12-D-1 全部切片外硬保持项零触碰。S9-E-2 维持负优化裁决。npm POSIX `.bin` symlink 静默 no-op 未当优化修。
- **EXCLUSIONS.md 未增行**（空枚举记录不入排除表，plan-ban 归属父代）；仓库变更恰为本报告单文件（提交前 `git status` 复核仅 `docs/reports/sota-opt/round-39/R39-B.md`）。禁止开 PR，未开。

## 1. 第三十九遍搜索方法：从零独立重枚举

R31-B 已证面格闭合、R32-B 已证闭合稳定、R33-B…R38-B 各做过一次从零重枚举（分别收获 1 / 0 / 0 / 0 / 0 / 0 项新站点级换名记录）——第三十九遍不照抄其结论，而是**独立重跑同一枚举原点**：假装排除表不存在，重读 1359 行找出每一个理论上可疑的消除/重构角度，再逐一对表执行换名拒绝检查（词根不同 ≠ 面不同；站点不同但同族同向 = 换位点记录不铸 ID；**站点已点名 = 复命中，零记录**）。方法四步：

1. **生产调用方图景重取证**——`rg 'assignTasks\(' src/` 恰 3 个调用方（`cli/main.ts:365` smartChildPlan live face N≤30、`adaptation/eval-routing.ts:353–354` 只传 learned、`track/primary-split.ts:41` C 小批），与 R30-B…R38-B 逐条一致，无新调用方、无新可选参数；`assignOne` src 内零生产调用方维持；`effectiveConfidenceThreshold` / `routeTask` / `routeFlowNode` 在 src 内仍只有 `supervisor/flowchart.ts:33–36` barrel 再导出、零运行时消费者（R12-B 零流量取证第九次转移成立）；`routeR0` 唯一调用方仍是 `r1-shadow-report.ts:88`（shadow-only）；`applyCascade` 唯一调用方仍是 `cascade-evidence.ts:102/:108`，`applyEvidenceCascade` src 内零调用方（R5-B「生产不可达」维持；`learning/from-episode.ts` 的 `applyCascadeRetry` 为异名异物，非本切片函数）；live 级联生产调用方仍是 `track/loop.ts:120`、`cli/main.ts:377`（plan）与 `run/child-coordinator.ts:468/:475`（evidence+decide），每子任务一次。
2. **结构位置 × 消除机制满矩阵**——10 文件 × 13 类消除机制（死码/常量折叠/CSE 去重/记忆化/数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/形状单态化）逐格过筛（§4 按文件汇总）。
3. **换名拒绝检查**——非空格逐一对表；本轮尾部仅存的两个残余角度均查证为已点名站点复命中（§2d），**零**新站点级记录。
4. **测量侧不铸新剂量网格**——只重锚既有格 ×3 独立运行 + W1/W2/W3 行为证人 + L0 引擎同一性断言；驱动为从零重建（诚实声明见结论段与附录 A，含一次如实记录的簿记弃批事项），冒烟运行按纪律弃置，官方口径为其后三次独立进程运行（全程留档 /tmp 日志）。

## 2. 天花板复测：R4-B…R38-B 收口第三十六次复核成立（重建 harness）

引擎 Node v22.22.2 / V8 12.4.254.21-node.39（canonical，L0 断言 ×3）。语料种子 0xb44b01（重建生成器）。×3 独立进程运行，全部 ALL CHECKS PASSED；确定性 sink ×3 逐位相同（4197240093）。单位 ms/eval（×2 对口径，eval = baseline+candidate 两次 `assignTasks`，镜像 `eval-routing.ts:353–354`）：

| 格 | run 1 | run 2 | run 3 | 历史参照（指示性） |
|---|---|---|---|---|
| ceiling N=2000 M=2（rich 语料 + limits） | 8.310 | 9.768 | 8.585 | R38-B 10.91–11.98 / R37-B 10.12–10.55 邻接下侧（更快方向出带，重建构成 + 宿主） |
| ceiling N=2000 M=10 | 16.991 | 17.358 | 17.130 | R38-B 16.91–18.00 **三读全部带内** |
| analyzeTask 份额 | 40.4% | 34.4% | 38.8% | R38-B 26.3–32.5 / R37-B 28.7–30.5 邻接上侧（重建 rich 语料构成差，指示性） |
| replay-faithful M=2（bare 语料，eval-routing 裸镜像） | 9.629 | 9.995 | 9.885 | R38-B 9.00–10.14 **三读全部带内**；R35-B 8.18–9.24 上侧邻接 |
| 10× 压力 N=20000 M=2 | 93.441 | 106.381 | 95.602 | R38-B 101.68–107.33 交叠（run 2 带内）；历史并集 92.27–140.9 三读全部带内 |
| CLI live face N=30 M=2（µs/调用） | 78.5 | 81.3 | 85.4 | R38-B 83.2–97.7 交叠（run 3 带内，runs 1/2 下侧邻接）；R37-B 88.4–92.4 下侧 |

M=2 全序、M=10 全序、analyzeTask 份额、压力格与 live face 的结构关系逐运行成立；`holds-below-landing-line=true` ×3（生产代表格最大 17.36 < 30，余量 ~42%）。**回归不可能成立**：切片对 `94ed3d9` 逐字节零 diff（四点冻结 + 快进段 src/ 为空），任何格间 Δ 只能来自主机与重建 harness 构成差；ceiling M=2 下侧出带而 M=10 带内、face M=2 下侧邻接而 face M=7+learned 带内交叠（§2b）——**分向不一致的邻接偏移**正是宿主 + 重建构成差的特征而非结构变化。W1（双跑逐字节恒等，3 目录 × 2 臂）/ W2（learned 臂仅在 avoid/prefer 命中族上分歧）/ W3（prior 拾取存在性 + 身份）三组行为证人 ×3 运行全过，语义与 R30-B…R38-B 恒等。

## 2b. 配置态 × 命令类矩阵：全六格复测（第三十次全格复核；臂交错测量）

replay N=2000 bare 语料，ms/eval（×2 对口径；六臂在同一 rep 循环内交错采样，median of 15）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| M=2 default, no learned | 7.793 | 8.091 | 8.340 |
| M=2 default, learned | 8.437 | 9.168 | 9.049 |
| M=7 configured, no learned | 12.889 | 13.352 | 13.753 |
| M=7 configured, learned | 12.958 | 13.203 | 13.505 |
| M=10 stress, no learned | 14.820 | 15.663 | 16.022 |
| M=10 stress, learned | 15.865 | 16.326 | 16.701 |

全 18 格保持 M=2 < M=7 < M=10 全序 ×3。learned ≥ no-learned 在 **7/9** 格对成立；**诚实注记**：M=7 对 runs 2/3 反转（−1.1% / −1.8%，learned 侧反快；run 1 +0.5% 为正），M=2 / M=10 对六测全正（M=2 +8.3/+13.3/+8.5%、M=10 +7.1/+4.2/+4.2%；M=2 run 2 的 +13.3% 单读高于历届 +6.5~+8.7 带，同判宿主窗）。该反转与 R33-B 的 M=10 一例 −0.12%、R34-B 的 M=7 两例 −2.3%、R35-B 的 M=7 一例 −4.8%、R36-B 的 M=7 三例 −1.1%~−6.0%、R37-B 的 M=7 一例 −1.1%、R38-B 的 M=7 三例 −1.6%~−4.9% 同为 M=7 配置格抖动族，本轮幅度带（≤1.8%）为历届最浅——机制方向与 **S13-B-1 已归档旁证**一致（配置目录下 avoid 命中使 partitionLiveCandidates 跳过被除名模型的整行 evaluateLiveCandidate，allow-list 收缩红利可超过 applyLearnedRouting 本体增费），非新信息、非结构翻转；W1（该格双跑逐字节恒等）与 W2（learned 臂仅 avoid/prefer 命中族分歧：三目录 inside=413、outside=0，×3 逐数相同）全过，freeze 0 行——按派单令「host-bias windows with freeze 0 lines are NOT findings」，不铸坐标、不加表行。CLI live face M=7 configured+learned = 108.0 / 117.8 / 119.9 µs/调用（R38-B 113.7–120.2 交叠、R37-B 103.9–110.0 邻接上侧）。级联探针（M=2 plan 基座，2 tiers）：`liveCascadePlanFromAssignment` 244–259 ns、`decideLiveCascade` 307–316 ns（R38-B 230–242 / 255–270 邻接上侧、R37-B 207–209 / 254–266；本轮 decide 探针同取 FAIL+failureClass=model 逃逸路径并断言 escalate cheap→premium 成立，两探针均亚 µs），每子任务一次的生产流量下（C≤6/run）仍为深度亚噪声。

## 2c. prior 双档格复测（第二十八次跨 VM；S12-B-2 重开腿 (a) 复核）

重建快照 `pps_r39b_realistic_v1`（4 源 21 行 + aliasMap 7 行；单拾取 10.17–10.66 µs/op——与历届快照构成不同，倍率结论按各自 rig 内部孪生格比值给出）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| 假设档 N=2000 M=2 + prior（ms/eval） | 25.831 | 25.893 | 28.809 |
| 假设档 N=2000 M=7 + prior | 38.532 | 39.659 | 43.956 |
| 假设档 N=2000 M=10 + prior | 46.724 | 47.884 | 48.982 |
| 孪生格（learned-only）N=2000 M=2 | 8.763 | 8.655 | 9.236 |
| 真实档 live face N=30 M=2+prior（µs/调用） | 227.1 | 226.3 | 232.6 |
| 真实档 N=30 M=7+prior+learned（µs/调用） | 349.3 | 340.4 | 344.4 |
| 单拾取 pickFromPublicPrior（ns，M=7 pool） | 10544 | 10171 | 10661 |

假设档 M=7/M=10 六读全部越线（>30 ms/eval）；M=2 本轮 25.83–28.81 **三读全部线下**（R37-B run 3 曾单读越线 30.68、R36-B run 1 曾 32.64，本轮未复现同型单读现象，同判宿主窗），三读为其 learned 孪生格（8.66–9.24，驱动内显式复测行）的 **2.9–3.1×**（M=7 对矩阵孪生格 ~3.0–3.3×、M=10 ~2.9×）——「若有流量即数量级越线」邻接配置结论不变；生产流量本轮重新取证仍为零：`rg -c 'prior' src/adaptation/eval-routing.ts` = 0 匹配（rg exit 1），:353–354 `assignTasks({ catalog, tasks, learned: … })` 只传 learned。**S12-B-2 腿 (a) 未触发**，条件式落地物维持在架，按令不补铸不重扫（R21-B 快照形状轴维持关闭）。W3（三种 M 下 family=edit 拾取存在：M=2 → premium 0.786、M=7/M=10 → m04 0.667——拾取**身份**与 R35-B…R38-B 完全一致；quality 数值出自重建快照构成，与 R38-B 逐位重合系配方设计使然，非跨 VM 数值可比性声明）×3 全过。真实档 face M=2+prior 226.3–232.6 µs 在 R38-B 238.5–249.2 邻接下侧；face M=7+prior+learned 340.4–349.3 µs 在 R38-B 355.9–425.7 邻接下侧（与 §2 ceiling 同向的下侧宿主窗，face 探针族一致偏快），全部亚 ms、生产流量零。

## 2d. 第三十九遍枚举：换名拒绝检查记录

从零满矩阵扫描（§4）枚举出的全部角度经对表逐一裁决。**本轮零新站点级换名记录**（与 R34-B/R35-B/R36-B/R37-B/R38-B 同为零；R33-B 为恰 1 项）。枚举尾部仅存的两个残余微观角度与 R34-B…R38-B §2d 完全同两站点，均查证为**已点名站点复命中**：

1. **`assign.ts:63` + `assign-plan.ts:29` 每批 id 双 map / 双 Set**（`catalogIds = input.catalog.models.map(id)` 与 `planAssignmentPolicy` 内 `new Set(models.map(id))` 与 `validateConfig` 内查重 `ids` Set 三处每批各枚举一遍目录 id）——R2-B §4 已点名（「`catalogIds` 重 map 与 router 内部 Set 不共享属公开接口变更」），每批一次 ns 级天花板由 S7-B-6 本体承载。复命中，零记录。
2. **`decideLiveCascade` 的 `stay` 辅助闭包每调分配**（live-cascade.ts:87）——R2-B 已点名（「`stay` 闭包每调分配为亚噪声（S1-B-4 同表面）」）并经 R5-B/R6-B/R8-B/R34-B…R38-B 八次维持；本轮探针 decide 全函数 307–316 ns/op，闭包份额为其真子集，生产流量每子任务一次（C≤6/run）⇒ 亚 µs/run。复命中，零记录。

本轮单独复核的一处边缘站点亦未产生记录：**`assignTasks` 批前奏 `limits` 条件 spread**（assign.ts:65–68，每批一次分配）——属 R27-B **batch-cardinality bottom + prelude intercept face** 属地成员（batch prelude = createModelRouter + planAssignmentPolicy + catalogIds + limits 的整体拦截面，封禁面），非新站点、不铸 ID。

其余枚举角度全部落在既有裁决**本体**上（代表性清单，与 R33-B…R38-B §2d 清单核对一致）：`validateConfig` ROLES.includes / dup-role Set = S1-D-8 类 + S10-B-3、查重 has+add 单探针 = S9-B-1；`planAssignmentPolicy` 双排序拷贝取极值 = S8-B-1；`assignPlanned` 两次防御拷贝 = S8-B-4 / S2-B-1、options 三条件 spread = S9-B-3 / S4-B-4、批内记忆化 = S3-B-1（learned 层 = S13-B-1 loser、prior 层 = S12-B-2 条件陆器）；route 输入骨架 = S5-B-4 / S3-B-6 / S4-B-5 族；`validateInput` 字面量数组 = S1-B-7、per-id trim 卫 = S9-B-2；unknown-model 预扫描下沉 = S9-B-4；per-route allowed Set = S12-B-1（双向封死）；主循环融合 = S11-B-1（镜像封死）；`evaluateLiveCandidate` 空前置失败别名 = S7-B-3、per-model request 字面量 = R1-B 契约裁决（fixedCostUsd/fixedLatencyMs 所需）、failures 合并 spread = 同站点微观；`toModelDescriptor` 每 (任务,模型) 构造 = R1-B §4.4 架构裁决；`buildDecision` 构造份额 = R32-B D1 普查（19/20 字段活）、eligible.map 与 oneHot 融合 = S6-B-1；justification 预计算 = S7-B-4；approvalPlan 享元 = S6-B-2；one-hot 载体形 = R32-B D4 三重关；`coldStartRoutingScore` 钳位 = S10-B-2；空 avoid/prefer 跳过 learned = S6-B-4；`cheapFirstTiers` 装饰-排序-还原 / localeCompare / 冗余 spread = S3-B-2 / S3-B-3 / S1-B-5；`decideLiveCascade` find+findIndex = S1-B-4；`liveRefusalMessage` 双 .some = S8-B-2；`selectLiveModel` 比较器展开 = S8-B-3；analyze-task 正则链 / HIGH_RISK_RE 双测去重 / 死值谓词 / reason join / long 惰性与换行手写计数 = S4-B-1 / S1-B-1 / S5-B-1 / S5-B-3 / S10-B-1 + S1-B-2；`familyOf` 布尔重排 = S1-B-3、deploy 门冗余合取 = S7-B-1、终端死回退 = R33-B §2d 记录 1（已点名不重提）；preferPrimary 被吸收析取 = S7-B-2（含 contractRisk=false 反例边界复核：`family === "deploy"` 非全吸收，删除仍属行为耦合）；`routeR0` 高风险 find = S1-B-6 / S2-B-3、r0 排序输出 = R1-B 契约；`applyCascade` `{...decision}` 拷贝 = 生产不可达（R5-B/R9-B 维持，本轮 `applyEvidenceCascade` 零调用方重取证）；`primary-catalog` 构造器享元 = S7-B-5 / S6-B-2 类身份可观察、throw 位点 = R17-B 拒绝路径已锚 + R32-B D2 维持；`effectiveConfidenceThreshold` candidates 数组 + `Math.max` spread 与 `routeTask`/`routeFlowNode` 包装 = R12-B 零流量不铸维持（本轮 barrel census 重证：flowchart.ts:33–36 再导出、零运行时消费者）+ Math.\* 绑定属 BINDX 坐标声明即止；`createModelRouter` `{...config, models}` 每 run 一次 CLI 噪声档、跳过二次 catalogModel = S2-B-2；`catalogModel` 条件 spread 属性存在性可观察 = S1-C-10 类。

## 3. 候选总表

**空。** 铸 ID 数：**0**。铸 face 名 / ALLCAPS 外号数：**0**。EXCLUSIONS.md 增行数：**0**。新站点级换名记录数：**0**（两个残余角度均为已点名站点复命中，§2d；limits 前奏 spread 归 R27-B 封禁面属地）。本轮产出为三点哨兵重取证（S12-B-2 腿 (a) / S13-B-1 / 调用方图景 + barrel 零流量第九次转移）与重建 harness 下的锚点复位（§2–§2c），均不构成候选。空枚举与 R8-B…R38-B 同为战役允许的诚实结局。

## 4. 结构位置 × 消除机制满矩阵扫描（第三十九遍，按文件汇总）

| 文件 | 本轮检查 | 结论 |
|---|---|---|
| `r0.ts` | `applyCascade` 生产不可达第三十九次重取证（`applyEvidenceCascade` src 零调用方；from-episode `applyCascadeRetry` 异名异物）；routeR0 唯一调用方 r1-shadow-report:88（shadow-only）；S1-B-6/S2-B-3/S3-B-4/S4-B-3 维持；排序输出即契约 | 无候选 |
| `assign.ts` | 调用方图景三处重取证一致（§1）；id 双 map = R2-B §4 已点名 + S7-B-6 本体（§2d 记录 1，复命中零记录）；批前奏 limits spread = R27-B prelude intercept face 属地（§2d 边缘站点复核）；S8-B-4/S2-B-1 双拷贝、S9-B-3/S4-B-4 options、S3-B-1 批内记忆化、X3-1 维持；S13-B-1 重开门未触发；`flowchartRoleForAgentRole` 属 graph 区（跨切片只记录） | 无候选 |
| `assign-plan.ts` | S8-B-1 维持；每批双 Set 同上复命中；`pickPreferredModel` prior 层 = S12-B-2 条件陆器在架未触发；`pickFromPublicPrior` 本体属 `public-prior.ts`（切片外） | 无候选 |
| `policy.ts` | per-model request 字面量 = R1-B 契约裁决；S7-B-3 别名护栏维持；预算/期限 toFixed 串仅失败路径 = R17-B 已锚；COMPLEXITY_RANK 常数查找无池；capability 循环已首缺早退（break 在位） | 无候选 |
| `live-cascade.ts` | `stay` 闭包 = R2-B 已点名复命中（§2d 记录 2）；S1-B-4/S1-B-5/S3-B-2/S3-B-3 维持；byId Map 每 plan 一次 M≤10 ns 档；探针 244–259 / 307–316 ns 复锚（M=2 基座，§2b，decide 断言 escalate cheap→premium 成立） | 无候选 |
| `live-selection.ts` | S8-B-3/S8-B-2 维持；`compareLiveCandidates` Number() 布尔差分已是无分支形态；总序契约（R19-B 第三腿封禁）零触碰 | 无候选 |
| `analyze-task.ts` | `ROLE_FAMILY` 死回退 = R33-B §2d 记录 1 已点名（不重提）；HIGH_RISK_RE 双测 = S1-B-1 本体；S1-B-2/S1-B-3/S4-B-1/S5-B-1/S5-B-3/S6-B-3/S7-B-1/S7-B-2/S10-B-1 维持（S7-B-2 边界含 contractRisk=false 反例复核）；份额 34.4–40.4% 复锚（重建 rich 语料构成，R38-B 26.3–32.5 邻接上侧，指示性） | 无候选 |
| `primary-catalog.ts` | 单点构造器消灭形状多态（R31-B C1 停驻维持）；构造器享元 = 身份可观察类；modelFor/inferFastId 每配置一次 ns；throw 位点归 R32-B D2 | 无候选 |
| `catalog-model.ts` | `toModelDescriptor` R1-B §4.4 架构裁决维持；oneHotDistribution = R32-B D4 维持；条件 spread 属性存在性可观察（S1-C-10 类）维持 | 无候选 |
| `supervisor/model-router.ts` | `effectiveConfidenceThreshold`/`routeTask`/`routeFlowNode` 零流量重取证（barrel 再导出 flowchart.ts:33–36、零运行时消费者，第九次转移成立）；S12-B-1/S11-B-1 镜像对封死维持；S1-B-7/S9-B-1/S9-B-2/S9-B-4/S5-B-4/S10-B-2/S10-B-3/S7-B-6/S6-B-1/S6-B-2/S6-B-4/S6-B-5/S7-B-4/S8-B-4/S2-B-2 维持；buildDecision = R32-B D1 普查维持（19/20 活）；`createModelRouter` 每 run 一次 CLI 噪声档 | 无候选 |

## 5. 关键裁决细节

- **S12-B-2 换名检查**：假设档 M=7/M=10 六读越线依旧、M=2 以 learned 孪生格 2.9–3.1× 倍率维持邻接（本轮三读全部线下，历届单读越线现象未复现，§2c），腿 (a) 重新取证未触发（eval-routing 零 prior、调用方图景三处未变、无新可选参数）。未以任何新名重扫快照形状轴。
- **S13-B-1 换名检查**：`git diff 94ed3d9 -- src/learning/learned-routing.ts` = 0 行，本体成本无变化，重开条件（applyLearnedRouting 本体成本增 ≥2 个量级）未触发。learned 容器坐标归 R29-B（封禁），本轮未触碰。本轮 M=7 runs 2/3 learned 反快（§2b）恰是 S13-B-1 已归档的「allow-list 收缩红利」机制旁证复现（幅度带为历届最浅），属既有记录复命中，不构成新面。
- **目录预排序**：R28-B W3 裁定行序为契约决策载体，预排序 = 行为变更，本轮未尝试。
- **重建 harness 与主机窗口**：本轮全部格值出自从零重建驱动（诚实声明见结论段），跨报告数值带对照只具指示性；效力护栏为 (a) 四点冻结 0 行 + 快进段 src/ 为空、(b) W1 双跑逐字节恒等 ×3 + 确定性 sink ×3 逐位（4197240093）、(c) W2/W3 语义证人 ×3（divergence 计数 inside=413 / outside=0，×3 逐数相同且三目录相等；W2 计数与 R38-B 的 379、R37-B 的 456、R36-B 的 420 差异来自重建语料 stem 字节构成，同配方不同字节，各自确定）、(d) 序结构（格间全序、份额占比、落地线余量、prior 倍率）与 R30-B…R38-B 完全同构。ceiling M=2 下侧出带而 M=10 三读带内、faithful 三读带内、face M=2 下侧邻接而 face M=7+learned 带内交叠、prior 双 face 下侧邻接而级联双探针上侧邻接——**多向不一致的邻接偏移**正是主机 + 重建构成差的特征而非结构变化；M=7 learned runs 2/3 反转（§2b 诚实注记）、M=2 learned run 2 单读 +13.3%、stress run 1 下侧 93.44（历史并集带内）均在该护栏下裁定为主机窗口 + 重建构成效应；派单已预告「host-bias windows with freeze 0 lines are NOT findings」，均不铸轴。
- **引擎纪律**：三次官方运行全部在 nvm Node v22.22.2 产出（L0 断言 + 运行前 `node --version` 复核）；系统 Node 上零官方批次，无弃批。冒烟运行按纪律弃置不计入官方口径；本会话早段第一次重建批（同引擎全绿）因逐 run 控制台记录未留档而**整批弃置**（簿记事项如实记录见附录 A 第 5 条，与 R39-A §1.1 注 (ii) 同类），官方口径为重建驱动其后三次独立进程运行（全程留档）。

## 6. 前后对比与测试

切片零代码改动，无前后对比。验证（全部在 Node v22.22.2）：

- 聚焦 live 路由单测：`npx tsx --test "test/unit/routing/*.test.ts"` —— **198/198 pass**（18 suites，含 live-isolation / cascade-evidence / failure-class / assign / assign-plan / analyze-task / r0 / live-cascade / live-selection / catalog-eligibility 等），与 R31-B…R38-B 计数逐字相同；测量前、harness 删除后各跑一次，两次同计数。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0** —— 1169 项测试 1168 pass / 0 fail / 1 skipped（既有跳过项，非本轮引入），tsc build 通过；计数与 R9-B…R38-B 逐字相同（78 suites）；测量前、harness 删除后各跑一次，两次同计数同退出码。
- 仿真 ×3 独立运行 ALL CHECKS PASSED（L0 + W1/W2/W3 逐运行全过；确定性 sink ×3 逐位相同 4197240093，消费 checksum ×3 逐位相同 12981640）。
- 提交前最终 fetch：独占分支一次 docs-only 快进 `fa9724e → 08dacf8`（段核对 src/ / test/ / lockfile 0 行），`git merge --ff-only` 对齐，对齐后冻结复核 0 行、R39-A 全文读毕折入；`git status` 仓库变更恰为本报告一个新文件。
- 无赢家故 scripts/ 零新增；harness 未入库（重建说明见附录 A，指纹见附录 B，临时文件按纪律核后删除，gate 复跑在删除之后）。

## 7. 新增排除 ID

无。未铸任何 S39-B-\* ID；EXCLUSIONS.md 零增行；零新站点级换名记录（§2d 两条均为已点名站点复命中、一条边缘站点归 R27-B 封禁面属地，无需归档新记录；plan-ban 归属父代）。

## 附录 A：本轮驱动重建说明

历届 B 驱动（/tmp/rXXb-sim.mts 族）从未入库，本 VM 无其字节可衍生；本轮驱动 `/tmp/r39b-sim.mts` 为按 R4-B…R38-B 附录约定**从零重建**：

1. 语料：mulberry32(0xb44b01) 确定性生成 20000 任务（七角色均匀、16 stem 池含 10% 高危尾（2 高危 stem）、25% long（半数 ≥3 换行）、50% 带 contextTokens/outputTokens、10% 显式 contractRisk）；标准格取前 2000，live face 取前 30；**rich**（带 token/contractRisk 选项 + limits）供 ceiling/face 格，**bare**（仅 taskId/role/objective）供 faithful/matrix/stress 格（eval-routing 裸镜像）。
2. 目录：M=2 = `catalogFromPrimary({primaryModelId:"premium"})` canonical 对；M=7 configured（cheap/m01–m05/premium，成本严格递增无并列 0.10→0.50，cheap+m01–m05+premium 均有 prior aliasMap 行）；M=10 stress（+m06–m08，premium 0.85）。
3. 口径：×2 eval 对（baseline 空 learned + candidate learned=avoid(cheap@review)+prefer(premium@plan)，镜像 `eval-routing.ts:353–354`）；ceiling/faithful/prior-假设档 median of 7、matrix 六臂同 rep 循环交错 median of 15、stress median of 3、face mean of 2000 调用（每调用 = 一个 N=30 批，镜像 `cli/main.ts:365` smartChildPlan）、级联探针 mean of 200k、单拾取 mean of 2000。
4. 证人：W1 双跑 JSON 逐字节恒等（每 M 两臂）；W2 learned 臂分歧任务全部落在 avoid/prefer 命中族（outside=0，inside 三目录均 413，×3 逐数相同）；W3 三 M 下 family=edit prior 拾取存在（M=2 → premium 0.786、M=7/M=10 → m04 0.667，身份与 R35-B…R38-B 一致）；L0 `process.version === "v22.22.2"` 断言（不计时）；确定性 sink（FNV-1a over W1 字节 + W2 计数 + W3 身份 + tiers）防 DCE 且 ×3 逐位（4197240093）；另设消费 checksum（计时循环内累加 decision/preferred 字节长）防 DCE，×3 逐位（12981640）。
5. 簿记史（如实记录）：本会话**早段第一次重建批**（驱动 485 行，sha256 `7a8a9950630e2110ae0547d1f4048b894a7b877fe8a11242c3a2acacfec4def3`，冒烟 + 三次官方运行全绿，确定性 sink ×3 逐位 1042012267）在报告撰写前因会话上下文轮转其**逐格控制台读数未留档**，按「官方读数须可逐字复核」纪律**整批弃置不采**；驱动按同一配方**第二次从零重建**（本附录第 1–4 条），冒烟一次弃置（全绿，无口径修正）后三次独立进程运行全程留档为官方口径。两批均全绿、W3 身份一致，该事项为簿记现象，无结构含义（与 R39-A §1.1 注 (ii) 同类）。

## 附录 B：驱动指纹与运行方式

- 驱动：`/tmp/r39b-sim.mts`，524 行，sha256 `cddd410274e417d5e29c3974dcb8e6b24b2af60ebf7fa920f5e7aeedeaa49eb8`（未入库；临时文件已删除，gate 复跑在删除之后执行）。
- 运行（repo 根，空闲 VM（load 0.02 复核后启动）；PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开系统 Node）：

```bash
npx tsx /tmp/r39b-sim.mts   # 冒烟 1 次（弃置）+ 独立进程 ×3 = runs 1–3（输出 tee 至 /tmp/r39b-run{1..3}.log 留档）
```

---

**MORE_OPTIMA=no** —— 第三十九遍从零重枚举空收口：零新站点级换名记录（两残余角度均为 R2-B 已点名站点复命中；limits 前奏 spread 归 R27-B 封禁面属地）；重建 harness ×3 全绿复锚天花板（ceiling M=2 8.31–9.77 / M=10 16.99–17.36 ms/eval——M=10 与 faithful 三读全部落在 R38-B 带内，M=2 更快方向下侧出带，判宿主 + 重建构成差；freeze 0 行，W1/W2/W3+L0 证人全过，确定性 sink ×3 逐位 4197240093）；全 18 矩阵格全序成立（learned 对 7/9，M=7 runs 2/3 反转 −1.1%/−1.8% 诚实记为主机窗口 + S13-B-1 机制旁证复现，幅度带历届最浅）；prior 双档格 M≥7 六读越线、M=2 以 2.9–3.1× 孪生倍率维持邻接（三读线下，历届单读越线未复现）；S12-B-2 腿 (a) 与 S13-B-1 重开门显式重取证未触发（含 barrel 零流量第九次转移）；测量窗口内一次 docs-only 快进已对齐并折入 R39-A；不铸 ID、不铸面、不增排除行；生产代码零改动，聚焦 198/198 + `pnpm gate` exit 0（1169/1168/0/1 计数逐字同）。

MORE_OPTIMA=no
BRANCH=cursor/r39-b-live-routing-thirty-ninth-pass-83a1
