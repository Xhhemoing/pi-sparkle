MODEL_SLUG=claude-fable-5-thinking-xhigh

# R24-F：`src/experiments/` 第二十四遍复查报告（S7-F-1 + S7-F-2 之上，全新第二十四组枚举）

- 基线：`cursor/sota-persistent-opt-83a1` @ `34a293a`（开工时 exclusive tip 为 `d218fdb`——R24-C 空枚举收口 + R24-F 派发；运行中 exclusive 两次快进：`a655895`（R24-D 空枚举收口 + R24-G 派发）与 `34a293a`（R24-E 空枚举收口 + R24-H 派发），均按令 rebase/快进到新 tip，本分支唯一独有文件仍只有本报告；切片最后一次改动仍是 S7-F 落地提交 `519101f`，本轮实测 `git diff 519101f..HEAD -- src/experiments/` 为空——**零漂移**，第二十四轮连续第十八轮字节不变）
- 切片：`src/experiments/` 全部 15 文件（attribution-report / canary / comparison-report / dataset / evaluation-card / gated-comparison / holdout / isolation / manifest / plan / replay / shadow-compare / shadow / simulation-holdout / threshold-calibration——与令中列名逐一对应，无缺无余，逐一全量重读，未依赖历轮记忆），工作在 S1-F / S5-F / S6-F-1 / S7-F-1 / S7-F-2 之上
- 前置阅读：README、EXCLUSIONS 全表、PROGRESS、round-24/PLAN、R21-F / R22-F / R23-F 全文、R14-F 轴 2 逐字复核（本轮新轴的最近邻标尺——证其半平面互补性，见 §0）、R17-F…R20-F 关键段、R24-A / R24-B / R24-C / R24-D 收口报告核读（禁移植清单逐条核过）、R23-G CKMIX 定义核读（「checkpoint」词面撞名的异地轴——证其异质，见 §0）、四个邻居文件（`adaptation/reflection.ts` / `adaptation/eval-routing.ts` / `adaptation/promotion-rules.ts` / `routing/r1-shadow-report.ts`，只读未改）
- 分支：`cursor/r24-f-experiments-twenty-fourth-pass-83a1`（已推送，未开 PR）
- 环境：Node 22.22.2（VM 默认 22.14.0 低于 engines ≥22.19.0，按历轮同处理以 nvm PATH 前置切换）、pnpm 10.17.1、`pnpm install --frozen-lockfile`

## 结论

**诚实的空枚举：第二十四组没有可立 ID 的新角度，不铸任何 S24-F-\* ID，生产代码零改动。** 本轮按令做全新第二十四组枚举：从零重读 15 个源文件与四个邻居、在本 VM 重建全部锚点与四次序符号（r5f/r7f 各两次独立运行 + r6f/r1f 各一次）、对照排除表穷举候选方向——每一个方向都被既有条目（或其重命名/换址形态）严格覆盖，或其量级天花板经本 VM 实测深低于数十 ms 落地线。R23-F 宣告的二十三层封条（账目直测、最大行分解、外推曲线、GC 类顶、来源格、JIT 档、MDE 地板、长度格、引擎格、剖析归属、A 越线点格、编码格、拒绝路径格、冷进程格、操作粒度格、剂量响应格、fresh-plan 格、halted 格、runner 家族格、outcome 流组成格、时间通道格、成员拓扑格、计数面联合格）经本轮复核成立。

本轮的实质产出是**一条历轮从未直测的新轴——状态序列化边界来源面完备性（state serialization-boundary provenance face：`restore(serialized)` 契约同名面——序列化状态本身真正跨过 JSON 边界、即 checkpoint-resume 配置态下的运行régime）**（§1.3）。二十三轮的**每一个**runner 操作夹具喂的都是同进程内由 runner 自己产出的状态：plan 对象指针共享、population 字符串驻留、账本在内存。R14-F 轴 2（来源格）只变了**调用方流**一侧的字符串来源（JSON 往返 outcome 哈希），并明文把状态侧钉死（「population 对象不变——生产中 plan 按引用共享」）；来源平面的**状态半边**——checkpoint-resume 消费者把整个状态从磁盘解析回来、用解析出的 plan 重建 runner 再继续，此后 S7-F-1 对齐比较、assign includes 与全部字符串站点**两侧**都是指针互异内容相等的字符串、Ω(P) validate 跑在从未哈希过的新鲜 population 上——从未被测量。r7f 仿真 partA/B 的 JSON 往返 restores 只验**正确性等价**（verdict/message/class/state 逐项一致），从未给该régime定价。本轮以四个格子把这半边落到直接测量并交叉闭合：

- **格 a：端到端（状态 × 流）来源象限（锚点 P=2000/A=1000，生产交错，时钟 0，头对齐前缀拓扑）**。II（内存 plan × 驻留流 = 历轮全部锚点）、ID（内存 plan × 去驻留流 = R14-F 轴 2 行、亦即当下生产régime，本 VM 重锚为共享仪器行）、DI（解析 plan 建 runner × 驻留流）、DD（解析 plan + 去驻留流 = checkpoint-resume 名义象限）。算术预言：字符串指针相等要求**两侧**共享，象限网格必须**塌缩为布尔**——实测两轮全中：{ID, DI, DD} 三象限中位互差仅 **0.56 / 0.17 ms**（深居 R15-F MDE 包络 1.04–2.64 ms 内），II 是唯一快角（−6.3 ～ −7.3 ms）。**决定性推论：DD − ID = +0.56/−0.06 ms ≈ 0**——状态跨序列化边界在流已去驻留（生产现状，R14-F 已定价）的前提下**一毫秒都不再加**；来源是布尔不是计数器。ID 行 +6.76/+6.42 与 R14-F 的 +7.19/+7.13 同带（共享仪器行跨 VM 复现）。
- **格 b：满账本（k=1000 活跃态）单 restore 微观仪，同一状态的四个来源变体**（mem / 整状态往返 / 仅账本往返 / 仅 plan 往返）。三个往返臂互差仅 **0.07 / 0.70 µs**（微观级布尔塌缩：税在「至少一侧非共享」时收取一次内容比较价，与哪侧、几侧无关），各 +2.70~+4.03 µs/restore——与 R14-F 流侧记录的 ~+2.4 µs/restore 内容比较税同带（同一仪器值从状态侧独立读出）。每比较税 2.70/3.55 ns × 交错账 Σ≈A²=10⁶ 次对齐比较 → 交叉预测 2.70/3.55 ms，为格 a DD 全宽的 37–56%——对齐站点是最大项非全项，余项为 assign includes 命中路内容比较与新鲜串分配/GC 二阶（与 R14-F 分解结构一致：其对齐分量 4.3–4.8 / 全宽 7.1–7.2）。
- **格 c：每次 resume 一次性首验税**。新鲜解析 plan 的首次 `validateExperimentPlan`（V8 串哈希未缓存）95.5/93.4 µs vs 新鲜**构造** plan 的首次 95.9/94.9 µs（互差 −0.4/−1.5 µs 不可分辨）vs 稳态 51.4/50.3 µs——**每 resume 一次性税恰是 R18-F 家族的 fresh-plan 税（+43~+45 µs），序列化边界在其之上一无所加**；µs 级、每 checkpoint 一次，双重不达落地线。
- **格 d：实验中途 resume 位置剂量**。前 h=500 对内存跑、整状态 JSON 往返一次（计时外）、解析 plan 重建 runner、只计时后半继续段：+2.54/+2.46 ms（线性 0.75×DD 预测 5.49/4.76 的 46–52%——次可加，重复内容比较在同一解析态上被引擎逐步侵蚀，S5-I-5/S9-C-2 引擎面家族注记归档）；上界仍立：即便取 DD 全宽 6.4–7.3 ms 亦深低于落地线一个量级。

**候选性判定：零池。** 消掉 resume 后内容比较régime = 状态串对 population 再规范化——R14-F assign 站点规范化方向的状态侧同型，天花板恰为来源 delta 本身（本轮 DD 全宽 6.4–7.3 ms / R14-F 流侧 ≤7.2 ms，均低于落地线约一个量级），跨调用规范串表变体 = X1-1 隐藏状态；对解析状态跳过/缓存 Ω(P) validate 或前缀重验 = fail-open 增量化（X3-3 / X4-1 + 指令明文「validateExperimentPlan 每调用全内容重读」）；每 resume 首验税 µs 级且每 checkpoint 一次。四格是夹具盲区闭合格（二十三轮全部锚点坐在 II 角、当下生产坐在 ID 行、restore 契约同名消费者坐在 DD 角——本轮证明三者同价差一个布尔），不是池，不立 ID。

至此全实验锚点（本 VM 120.1–134.3 ms 三视图带：r5f C2 121.34/120.07、r6f 126.87、r7f 133.64/134.31——partE 视图本轮偏热，超出历史最宽包络顶 R16-F 133.5 约 0.1–0.8 ms（~0.6%），而同窗四次序符号与 validate 卫全部同号为正、其余两视图与进程内第四视图（本轮格 a II 臂中位 116.43/114.39，与 R23-F 113.12/114.02 / R22-F 113.34/112.69 / R21-F 115.14–116.04 同带）全部居带内——判为 partE 整窗组成抖动非régime迁移）在 R23-F 二十三层封条之上再加一枚：**状态序列化边界来源格（象限布尔塌缩 + DD−ID≈0 直读 + 微观仪三往返臂 ≤0.7 µs 塌缩 + 首验税=fresh-plan 税闭合 + 中途 resume 剂量）**。锚点与四次序符号在本 VM 两次独立运行全部复现且同号为正（§1.1），S7-F-1+2 落地单元无回归，r1f 2668 项逐位全过，实验单元套件 75 项全绿，`pnpm gate` 全绿（1168 pass / 0 fail / 1 skipped——既有 provider-smoke 凭据跳过）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；无阈值/测试/公开签名/数据面变更。仓库变更仅本报告一个文件；无赢家故未提交任何 scripts 资产（新轴测量脚本全文进附录 A，遵守 R7-F 以来纪律）。

## 0. 范围与约束遵守 + 关闭 ID 重命名核查

- 阅读顺序按令执行：README → EXCLUSIONS 全表 → PROGRESS → round-24/PLAN → R21-F / R22-F / R23-F 全文 → R14-F 轴 2 逐字复核 → R17-F…R20-F 关键段 → R24-A/B/C/D 收口核读 → R23-G CKMIX 定义核读 → 15 个切片源文件全量重读 → 四个邻居文件只读。清单核对：15 文件与令中列名逐一对应，无缺无余。同轮在飞：R24-G（runtime/图）与 R24-H 在本轮运行中被 exclusive 先后派发——均未触碰其分支、其切片与其报告文件；R24-E（learning）在本轮运行中收口摄取（空枚举，JB 轴计划禁令——学习切片原生，与本轮状态来源轴无交集，未移植）；EXCLUSIONS/PROGRESS/PLAN 未编辑。未开 PR。未 force-push exclusive。exclusive 于本轮运行中两次快进（`d218fdb` → `a655895` → `34a293a`，R24-D/R24-E 收口摄取），均按令 rebase/快进本分支，`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 仅本报告一文件。
- 未重开任何 X0-\*..X4-\* / S1-\* ～ S23-\* 条目。R10-F…R23-F 未铸 S10..S23-F-\*，本轮未替其补铸；本轮亦未铸 S24-F-\*。按令未重铸：R23-F 计数面联合格（本轮**全部臂钉死 (A=1000, P=2000, m=1) 锚点坐标**——计数维度一格未动，变的是来源）；R22-F 成员拓扑组成（全部臂头部对齐前缀 Z 形态）；R21-F 时钟输入面（全部臂时钟恒 0）；R20-F outcome 流组成（全部臂干净 4 字段流 + 生产交错调度）；R19-F runner 家族（shadow 单宿主；canary restore 走同一 `validateExperimentPlan` 与同构对齐循环，来源结论经同一账目结构转移）；R18-F 对齐分数剂量（全程 f=1）；R17-F 及更早的 halted / fresh-plan / 编码 / 拒绝路径 / 冷进程 / 操作粒度 / 剂量响应 / A 越线 / GC / JIT / MDE / 长度格全部未动。
- **本轮新轴与最近邻的区分（重铸自查的核心）**：(a) **vs R14-F 轴 2（来源格）**：R14-F 只变**调用方流**一侧（JSON 往返 outcome 哈希）并明文钉死状态侧（「population 对象不变——生产中 plan 按引用共享」「validate 分量按构造不受来源影响」）；本轮的对象是被其明文排除的**状态半边**——解析 plan 建 runner（DI）、整状态跨边界（DD）、以及 restore 义务在解析态上的微观价（格 b 的 rtPlanOnly/rtLedgerOnly/rtState 三变体）与首验税（格 c）。ID 行按令重锚为共享仪器行（+6.76/+6.42 vs R14-F +7.19/+7.13 同带），不是重测——是两轴的对接点；新信息全部在 DI/DD 列与 DD−ID≈0 直读。R14-F 的方向收束（assign 站点规范化 ≤7.2 ms 天花板、跨调用串表=X1-1）本轮原样引用未重开。(b) **vs CKMIX（R23-G，按令禁移植）**：CKMIX 是 runtime 切片**磁盘检查点记录**的结构组成/字段在位混合与 I/O 定价；本轮零 I/O、零字段在位变化（全部状态满字段），对象是 F 切片 restore 契约在**内存来源régime**上的行为——词面同现「checkpoint」，机制、切片、见证三不同，无一数字、无一方法来自 R23-G。(c) **vs CALLB（R23-J，按令禁移植）**：调用边界调度轴变的是跨进程/调用编排；本轮调度全程钉死生产交错一种，变的是数据来源。(d) **vs r7f 仿真既有 JSON 往返**：partA/B 的 900+900 随机 restores 含 JSON 往返，但只断言**正确性等价**（verdict/message/class/state 一致——X3-3/X4-1 的守护仪器）；从未计时该régime。正确性早已闭合，本轮补的是价格表。(e) **vs R18-F fresh-plan 格**：格 c 的结论恰是「每 resume 首验税 = fresh-plan 税」——用等值闭合证明序列化边界**没有**新机制，不是重测其税值（解析 vs 构造两臂同轮直测互差 ≤1.5 µs）。(f) 禁令 ID（OCCX / CALLB / CKMIX / ORDX / SEEDX / YMIX / SCALEX / FITQ / TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / SHAPEK / KFAN / NVG）与计划禁令轴（R24-A 列维度普查 / R24-B M=1 catalog collapse / R24-D approval-authority face / R23-I `.pi/` 树 / R23-A G×E / R23-B 批排布 / R23-D 两相 P-dose / R23-E GS）一个未用未移植；字符串几何全程恒定（`ep_` + 8 位 base36、L=11、ASCII——R14-F/R15-F/R16-F 的长度与编码维度未变，变的只是对象身份）。
- **S7-F-1 ≠ S6-F-5 重命名核查（按令重申，本轮逐字复核落地代码）**：`shadow.ts` L373–386 / `canary.ts` L147–163 的对齐循环是**同下标单次相等比较**（`assignment.episodeHash !== population[index]` 即 break），失配税为有界常数，回退是后缀级 pending-Set（S6-F-1 形态；shadow `resolvePendingMembership` L309–327 / canary L73–86），下标 0 失配改道原落地循环（shadow L388–406）；S6-F-5 是 population 上的**双指针前向搜索**，失配白付 O(P) 扫描、符号随次序翻转。四种次序在本 VM 两次独立运行全部同号为正（§1.1）。本轮未提出任何双指针/子序列形态；本轮新轴不改任何实现，只测既有落地形态在来源平面状态半边上的行为——且格 a/b 证明该形态在全部四个来源象限下产出逐字节相同账本（附录 A 决定性见证）。
- **本轮新轴的候选性自查**：见结论段「候选性判定」——régime消除 = 再规范化（R14-F 量级拒绝的状态侧同型，天花板 = delta 本身 6.4–7.3 ms，线下约一个量级；跨调用串表 = X1-1）；解析态跳验/缓存 = fail-open（X3-3/X4-1 + 指令明文 Ω(P) 重读）；首验税 µs 级每 checkpoint 一次。四格按 R10-F 以来纪律不立 ID。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：validateExperimentPlan 每调用照跑（Ω(P) 内容重读——本轮格 c 直测其首调/稳态价）；每条 assignment 逐调用全检（Ω(A)）；`[...assignments]`/`[...outcomes]` 防御拷贝原样；TypeError 消息与属性读取序不变；双 LCB 与双归因未动（X0-11）；无阈值变更；X1-5（shadow-compare 不并 r1-shadow-report）、X3-3/X4-1（不 fail-open 增量化 canary/shadow restore——本轮新轴的候选性判定主引条目）、X4-2（不对 readonly 公开追加拷贝契约做可变 push）维持；CAS/凭据/数据面契约/公开签名未触；分析不改 in-flight run、tracking 无命令权、H/score 不写路由 PASS/FAIL；live = R0 等价（R1 不接线）、稀疏 → 已批准 R0 基线；提升 proposal-first；不声称 Outcome-supported；S5-H-1 未触碰（切片外）。
- 漂移复核：`git diff 519101f..HEAD -- src/experiments/` 为空（本轮实测，`--stat` 零行、diff 零行）——R1-F…R23-F 的全部测量与排除裁决可转移，但全部锚点仍按令在本 VM 重测（§1），未拷贝任何历轮数字。

## 1. 锚点重测 + 调用面复核 + 新轴（本 VM 实测）

### 1.1 全实验锚点与四次序符号（r5f/r7f 两次独立运行，r6f/r1f 各一次；未拷贝历轮数字；Node 22.22.2）

| 档位 | 本 VM 第 1 次 | 本 VM 第 2 次 | R23-F 记载带 |
| --- | --- | --- | --- |
| r5f C2 全实验（P=2000/A=1000） | **121.34 ms** | 120.07 ms | 122.15–129.42 ms |
| r6f partD 全实验 | **126.87 ms** | — | 120.64 ms |
| r7f partE 全实验（fixture 略异） | **133.64 ms** | 134.31 ms | 127.40–129.67 ms |
| 2A-validate 组件 | 100.46 ms（82.8%） | 99.84 ms（83.1%） | 99.06–109.25 ms（81.1–84.4%） |

诚实注记：r5f C2（120.07/121.34）与 r6f（126.87）居历史带内（R23-F 120.6–129.7 / R22-F 120.5–131.8 / R15-F 120.7–132.3），r7f partE 视图本轮 133.64/134.31 超出历史最宽包络顶（R16-F 133.5）约 0.1–0.8 ms（~0.6%）——同窗四次序符号与 validate 卫全部同号为正（下表）、同窗对照组（reference 侧 165–221 ms）同步抬升，判为 partE 整窗组成抖动/本 VM 热度非régime迁移；本轮新轴的进程内锚点格 II 臂中位 116.43/114.39（min 116.13/113.94）构成独立第四视图，与 R23-F 113.12/114.02 / R22-F 113.34/112.69 / R21-F 115.14–116.04 / R17-F 轴 2 稳态 113.2–118.5 同带——进程内视图零漂移。validate 比例 82.8–83.1% 居历轮 81.1–84.5% 带内。

四种 assignment 次序（r7f partE，S7-F-1+2 落地态 vs 编辑前冻结参考，2000 restores，两次独立运行）：

| 次序 | 第 1 次 | 第 2 次 | 符号 |
| --- | --- | --- | --- |
| prefix（锚点，全对齐） | **+40.52 ms** | **+35.97 ms** | 正 |
| half-aligned | +25.66 ms | +20.92 ms | 正 |
| scattered | +0.32 ms | +3.47 ms | 正 |
| reversed | +4.43 ms | +4.11 ms | 正 |

**全次序两轮同号为正，无符号翻转**——S7-F-1 落地单元在本 VM 复现无回归。诚实注记：scattered 余量 +0.32/+3.47 低于历史带（R23-F +5.32~+5.41 / R22-F +5.59~+5.71 / R20-F +5.18~+6.27），reversed +4.43/+4.11 亦低于 R23-F 的 +7.44/+7.65——两者都是 ~220 ms 量的两数之差、对窗口抖动敞口最大；落地保证是符号稳定（2 运行 × 4 次序 = 8 格无一翻转），量值带收窄与本轮 partE 整窗偏热同源，归档不改判。r6f partD（S6-F-1+S7-F 叠加视图，vs 前 S6-F-1 时代 population-Set 参考）：prefix +129.53 / scattered +29.18 / reversed +29.67 ms，三次序全正（R23-F：+125.04/+31.65/+29.26 同带）。r7f validate 组件视图（S7-F-2 单元）：×2000 调用省 +6.96 / +4.18 ms 两轮同号（R23-F +8.39/+9.23 同号带邻）。r5f C1（S5-F 单元）：has+add 148.75/146.54 → add+size 117.67/117.92 ms（+31.07/+28.62 ms，20.9%/19.5%——R23-F 20.1%/20.2% 紧邻带）；C4 索引循环形式 −1.98/−3.62 ms 维持 S5-F-3 抖动带记载；C5 dataset 镜像 8.6/11.1 µs（U=2000）、136.2/128.2 µs（U=20000）维持 S5-F-1 记载。r1f 2668 项逐位检查全过（生产侧 64.0 ms，参考侧 636.1 ms，9.9×）。

### 1.2 调用面复核（rg 全仓，与 R23-F 逐条一致）

- `from "../experiments/"` 全仓仍仅四文件：`reflection.ts`（validateExperimentPlan + createShadowRunner——唯一 runner 生产链；本轮逐行复核 `evaluateProposalShadow` 逐字未变：`nowMs = 0` 默认、assign+recordOutcome 交错、halt 即 break、每 proposal 一个 fresh in-process plan）；`eval-routing.ts`；`promotion-rules.ts`；`r1-shadow-report.ts`。**无新 src/ 调用方**。全仓 `restore(` 检索（切片外）命中的全部是别的类型（leases/monitor/registry/rollback/flowchart snapshot）——experiment runner 的 restore 契约仍零切片外生产消费者。
- **本轴新钉事实（来源坐标定位）**：当下生产链坐在来源平面的 **ID 行**——plan 每 proposal 在进程内新建（状态侧内存对象），而调用方哈希来自上游解析（R14-F 轴 2 已判定流侧去驻留为生产régime）；**II 角是纯夹具产物**（二十三轮全部锚点坐标）；**DD 角是 restore 契约同名面**（序列化状态跨边界的 checkpoint-resume 消费者——当前由 r7f 仿真与单测行使正确性，本轮补价格）。格 a 的布尔塌缩把三个坐标钉在同一价位差一个布尔：生产今天已付满内容比较régime价，未来任何 checkpoint-resume 消费者**不会再多付**（DD−ID≈0），每 resume 只另付一次 fresh-plan 税 +43~+45 µs（格 c）。
- 八个 test-only 入口（createCanaryRunner / replayPolicy / runSimulationHoldout / validateSealedDatasetManifest / HoldoutVault / calibrateSoftThreshold / compareShadowR1 / writeAttributionPair）维持零 src/ 切片外消费者格局（R22-F 已逐符号扫过；本轮切片与调用面零字节变更下转移成立，抽查复核一致）。

### 1.3 新轴：状态序列化边界来源面四格（两次独立运行；格 a 四臂轮转计时——每臂 1 预热 + 5 遍轮转穿插，漂移均摊；全部臂先做决定性验证：终态账本尺寸 A/A、budget-exhausted halt、指派序列 = 头对齐前缀逐条相等、零时钟场、两次执行账本摘要逐字节相等、**跨来源象限账本摘要逐字节相等**（来源不得漏入任何一个账本字节）、格 b 四变体 restore 输出摘要相等、格 d 两继续臂终态摘要 = 锚点摘要——两轮全过，验证行 `verification: all provenance-quadrant invariants hold (cross-arm ledger identity)` 两轮打印）

**格 a：端到端（状态 × 流）来源象限（P=2000/A=1000 生产交错；中位 ms，delta vs II）**

| 象限 | 第 1 次 | 第 2 次 | 备注 |
| --- | --- | --- | --- |
| II（内存 plan × 驻留流） | **116.43**（基线） | **114.39**（基线） | 二十三轮全部锚点坐标；= §1.1 第四视图 |
| ID（内存 plan × 去驻留流） | 123.18（+6.76） | 120.81（+6.42） | R14-F 轴 2 行 = 当下生产régime（共享仪器行，R14-F +7.19/+7.13 同带） |
| DI（解析 plan × 驻留流） | 123.57（+7.15） | 120.64（+6.25） | 状态半边首测 |
| DD（解析 plan × 去驻留流） | 123.74（+7.31） | 120.75（+6.35） | checkpoint-resume 名义象限首测 |

结论：(a) **布尔塌缩两轮全中**：{ID, DI, DD} 三象限互差 0.56 / 0.17 ms，深居 R15-F MDE 包络 1.04–2.64 ms 内且两轮无稳定排序——指针快路径要求两侧共享，任何一侧跨边界即整体落入内容比较régime，与哪侧、几侧无关；II 是唯一快角（−6.3 ～ −7.3 ms）。(b) **DD − ID = +0.56/−0.06 ms ≈ 0（本轴决定性直读）**：在流已去驻留（生产现状）的前提下，状态再跨序列化边界稳态**零增量**——checkpoint-resume régime价 = 生产今天已付的价。(c) 全部四臂账本摘要逐字节相等（两轮）——来源不漏入账本一个字节，决定性见证全过。

**格 b：满账本单 restore 微观仪（k=1000 活跃态（maxAssignments=2A 不 halt），200 restores/遍 × 5 遍轮转中位；µs/restore，delta vs mem）**

| 变体 | 第 1 次 | 第 2 次 | 备注 |
| --- | --- | --- | --- |
| mem（全内存） | 54.44（基线） | 52.92（基线） | 历轮微观仪坐标 |
| rtState（整状态往返） | +2.70 | +3.55 | DD 微观对应 |
| rtLedgerOnly（原 plan，解析账本） | +2.77 | +4.03 | ID 侧微观对应 |
| rtPlanOnly（解析 plan，原账本） | +2.73 | +3.33 | DI 侧微观对应 |

结论：(a) 三往返臂互差 0.07 / 0.70 µs——**微观级布尔塌缩**：对齐比较税在「至少一侧非共享」时收取一次 O(len=11) 内容比较价，两侧都新鲜不加倍（同一次字节比较）；(b) 每 restore 税 +2.70~+4.03 µs 与 R14-F 流侧记录 ~+2.4 µs/restore 同带——同一仪器值从状态侧独立读出；(c) 每比较税 2.70/3.55 ns × 交错账 Σ_{k}(2k+1)≈A²=10⁶ 次对齐比较 → 对齐份额交叉预测 2.70/3.55 ms = 格 a DD 全宽（7.31/6.35）的 37–56%——对齐站点最大项非全项，余项为 assign includes 命中路每次一整比较（共 A 次）与新鲜串分配/GC 二阶，与 R14-F 分解（对齐分量 4.3–4.8 / 全宽 7.1–7.2，67%）同结构；(d) 四变体 restore 输出摘要逐字节相等（两轮）。

**格 c：每次 resume 一次性首验税（µs，5 次新鲜对象中位）**

| 臂 | 第 1 次 | 第 2 次 | 备注 |
| --- | --- | --- | --- |
| 解析 plan 首验 | 95.5 | 93.4 | V8 串哈希未缓存 + 序列化边界 |
| 构造 plan 首验 | 95.9 | 94.9 | 同为未缓存哈希，无边界 |
| 稳态每调用 | 51.4 | 50.3 | ×50 调用均摊 |

结论：解析 vs 构造互差 −0.4/−1.5 µs 不可分辨——**每 resume 一次性税恰是 fresh-plan 税（+43~+45 µs 首调溢价），序列化边界在既有 R18-F 家族格之上一无所加**；µs 级、每 checkpoint 一次（CLI/一次性类），双重不达落地线。

**格 d：实验中途 resume 位置剂量（h=500 检查点，只计时后半继续段 500..999；中位 ms）**

| 臂 | 第 1 次 | 第 2 次 |
| --- | --- | --- |
| CONT-mem（内存继续） | 61.33 | 60.57 |
| CONT-resume（解析态+去驻留尾流继续） | 63.88 | 63.03 |
| delta | **+2.54** | **+2.46** |
| 线性预测 0.75×DD 全宽 | 5.49 | 4.76 |

结论：中途 resume 的真实继续段税 +2.54/+2.46 ms，为线性对齐账预测（继续段 Σ≈A²−h²=75% 比较量）的 46–52%——**次可加**且两轮稳定：重复内容比较跑在同一解析状态上被引擎逐步侵蚀（V8 就地驻留/ThinString 家族效应，S5-I-5/S9-C-2 引擎面注记归档，不可提案）；保守上界（DD 全宽 6.4–7.3 ms）与实测下界一致落在落地线下约一个量级。两继续臂终态摘要 = 锚点摘要（两轮）——resume 继续与全程内存产出逐字节相同账本。

## 2. 第二十四组枚举（诚实为空）：方向 → 覆盖判定表

候选生成程序：按「站点 ×（跳过 / 替换证明 / 换数据结构 / 换布局 / 复用 / 增量化 / 早退 / 重排 / 并行化 / 批量 / 惰性 / 规范化）」矩阵穷举，再对照排除表做重命名判定。结果：**每格非空即被覆盖**。

| 方向 | 量级判断（本 VM） | 覆盖条目 / 重命名判定 |
| --- | --- | --- |
| **resume 后régime消除**（restore/assign 站点把解析状态串对 population 再规范化、恢复指针快路径） | 格 a 首测：天花板 = DD 全宽 6.4–7.3 ms（落地线下约一个量级）；且 DD−ID≈0——生产已在内容régime，消它先要消 R14-F 流侧同款 | R14-F assign 站点规范化量级拒绝（≤7.2 ms，~15× 线下）的状态侧同型；跨调用规范串表 = X1-1；驻留夹具格恒零收益——不铸 ID |
| **解析态跳验/缓存 validate、前缀重验增量化**（按 plan 身份 memoize、resume 后信任前缀） | 格 c：首验税仅 +43~+45 µs；格 b：每 restore 全验 54–57 µs | **X3-3 / X4-1 fail-open 禁令本体** + 指令明文 Ω(P) 每调用重读 + X1-1 隐藏状态——不可提案域 |
| **首验哈希预热**（解析时预填 V8 串哈希、后台预验） | 格 c 首测：一次性 +43~+45 µs，每 checkpoint 一次 | µs 级 + 一次性类双重拒绝；预热干预 = S7-H-3 家族（预热净负） |
| **对齐比较再叠层**（哈希先行比较、长度分桶、内容比较特化） | 格 b：内容税 2.70/3.55 ns/比较已近引擎地板；对齐站点占 DD 全宽 37–56% | S7-F-1 唯一赢家纪律（S7-F-3..4）+ S6-F-5 禁令；引擎字符串相等语义 = S9-C-2 族 |
| **格 d 次可加性的利用**（依赖引擎就地驻留把 resume 税「摊没」） | 格 d：+2.46~+2.54 ms 次可加但载体是 V8 内部行为 | 引擎面 = S5-I-5 / S9-C-2 族——观察性事实归档，不可提案 |
| **计数面任何再测**（(A,P) 曲面、比例 1 角、m 剂量） | R23-F 三格已闭（本轮全部臂钉死锚点坐标） | R23-F 封条维持——不补铸 |
| **成员拓扑 / 时钟 / outcome 流组成 / runner 家族 / 对齐分数 / halted / fresh-plan / 拒绝路径 / 冷进程 / 操作粒度任何再测** | R17-F…R22-F 已双端关死（本轮未重测；全部臂钉死对应维度） | 各轮封条维持——不补铸 |
| **S7-F-1 任何再叠层**（双指针、相位拆分、外提、次序特化） | §1.1 四次序两轮全正（含本轮偏热窗下无翻转） | S6-F-5（禁令）/ S6-F-2 / S7-F-3..4 维持 |
| **shadow assign rng 重放消除 / recordOutcome 三扫融合 / 追加拷贝可变化 / 成本增量化** | R19-F 格 b + R9–R11-F 池账维持（未重测） | S2-F-1 + X1-1/X0-4；S9-F-2 / S1-F-8 / X4-2 / S1-F-3——机制封条原样 |
| **assign 站点调用方哈希规范化（含 L/编码变体）** | R14-F 天花板 ≤7.2 ms + R15-F L 系数 + R16-F 编码格（本轮 L=11 恒定）；本轮格 b 从状态侧复证同带税值 | R14-F 已收束；跨调用规范串表 = X1-1 |
| **A 越线点任何重解** | R16-F 轴 2 越线数维持（本轮不解阈值；格 b 满账本 k=1000 为微观仪坐标非越线证据） | S2-F-1 / S9-F-2 / S1-F-8 / S8-F-1 / S9-F-1 机制封条原样 |
| **任何贴带（≤~3 ms）候选的重测/重开** | R15-F MDE 地板 1.04–2.64 ms（本轮格 a 三象限互差 0.17–0.56 ms 居其内——包络判别力再获复现） | 「band-overlap is not a landing」数字化封条维持 |
| **`ExperimentRunnerOptions.nowMs` 死字段删除 / 公开面收窄** | R21-F 档案维持（运行时成本恒零） | 公开类型收窄 = S8-A-3 / S1-F-6 / S11-A-2 族——双重不可提案 |
| **test-only 链路一切残余** | 各 µs–亚 ms，R2–R23 已逐项量化（C5 本轮复测 8.6–11.1 µs 同带） | S1-F-1..8 / S2-F-1..6 / S3-F-1..5 / S4-F-1..3 / S5-F-1 / S6-D-5 / X1-5 / X3-2 / S3-D-3 维持；无新生产调用方（§1.2） |
| **切片内 import 边惰性化 / 并行化 / WASM / 引擎面干预** | R19-D 模块边际集 + R15-F 轴 3 维持（未重测） | S5-H-2 / S6-I-1 / S11-G-1 / S7-H-3 / S9-C-1/2 / S5-I-5 / S6-I-2 族 |

**结论：可立 ID 的新角度数为零。** 与 R23-F 的差别在于：R23-F 把计数域联合维度（(A,P) 曲面 + 比例 1 双角 + m 余量）三格首次直测；本轮把**来源平面的状态半边**（R14-F 明文排除的那半边——解析 plan、解析账本、整状态跨边界、中途 resume）四格首次直测，并以象限布尔塌缩 + DD−ID≈0 + 微观仪 ≤0.7 µs 塌缩 + 首验税等值闭合四重交叉。第二十四遍确认：在当前排除表与逐位契约下，本切片不存在不经表所有者层级契约变更即可达落地线的候选。**后续轮次对本切片的复核只需重跑 §1.1 锚点 + §1.2 调用面 grep；二十三层封条之上加第二十四枚：状态序列化边界来源格。**

## 3. 逐文件收口（R1–R23 收口之上的本轮检查点）

| 文件 | 本轮检查点 | 结论 |
| --- | --- | --- |
| `shadow.ts` | **本轮新轴主宿主**：restore 契约同名面四格直测（象限布尔塌缩、DD−ID≈0、微观仪三往返臂塌缩、中途 resume 次可加）；S7-F-1 落地形态零漂移逐行复核（§0，L373–386 对齐循环 + L388–406 下标 0 改道 + L309–327 pending-Set 回退）；四来源象限账本摘要逐字节相等；四次序符号两轮全正 | 无候选 |
| `plan.ts` | validate 组件 99.84/100.46 ms 复现（82.8–83.1%）；**格 c 首验税直测**：解析/构造首调 93.4–95.9 µs vs 稳态 50.3–51.4 µs——fresh-plan 税等值闭合，边界零新增；S5-F / S7-F-2 落地形态零漂移；r5f C1 +31.07/+28.62 ms、r7f validate 卫 +6.96/+4.18 ms 两轮同号 | 无候选 |
| `canary.ts` | R19-F 直接定价维持（未重测）；restore 走同一 validateExperimentPlan 与同构对齐循环（L147–163 逐字复核）——来源平面状态半边结论经同一账目结构转移 | 无候选 |
| `manifest.ts` | stableStringify 字节契约维持；本轮全部账本摘要断言（含跨来源象限逐字节相等——本轴的决定性见证仪器）以其为仪器 | 无候选 |
| `dataset.ts` | S5-F-1/S4-F-1 维持（C5 本轮复测 8.6/11.1 µs 同带；零 src/ 消费者） | 无候选 |
| `simulation-holdout.ts` | S2-F-5/S3-F-5/S5-F-1 维持；test-only | 无候选 |
| `comparison-report.ts` | X3-2/S4-F-3/S1-F-5 维持 | 无候选 |
| `gated-comparison.ts` | S1-F-1/S2-F-6 维持 | 无候选 |
| `attribution-report.ts` | 无循环无新调用方、零抛出点 | 无候选 |
| `replay.ts` | S2-F-1/2/3、S4-F-2 维持；replayCacheKey 外仍无生产调用方 | 无候选 |
| `isolation.ts` | S3-F-4 / S3-D-3 维持；生产 runner 不传 options 故 bindExperimentIsolation 早退 | 无候选 |
| `holdout.ts` | S1-F-3/X4-2 维持 | 无候选 |
| `threshold-calibration.ts` | S1-F-4 维持；零抛出点 | 无候选 |
| `evaluation-card.ts` | S1-B-7 域维持 | 无候选 |
| `shadow-compare.ts` | 薄封装维持 X1-5；零抛出点 | 无候选 |

## 4. 测试

- `npx tsx scripts/round05-r5f-equivalence-sim.ts` ✓ ×2 — 各 224 项 0 失败（C2 端到端 121.34 / 120.07 ms 即 §1.1 锚点）
- `npx tsx scripts/round07-r7f-equivalence-sim.ts` ✓ ×2 — 各 169 项 0 失败（partE 四次序两轮全部为正 + validate +6.96/+4.18 + 全实验 133.64/134.31 ms——偏热注记见 §1.1）
- `npx tsx scripts/round06-r6f-equivalence-sim.ts` ✓ — 27 项 0 失败（partD 三次序全正 + 全实验 126.87 ms）
- `npx tsx scripts/round01-r1f-equivalence-sim.ts` ✓ — 2668 项逐位检查 0 失败（生产侧 64.0 ms / 参考侧 636.1 ms，9.9×）
- `npx tsx --test test/unit/experiments/*.test.ts` ✓ — 75 项 0 失败（10 个 suite）
- `npx tsx tmp-r24f-axes.ts` ✓ ×2（干净独立运行）——四格两轮同带 + 全部决定性断言全过（四象限臂终态验证：账本尺寸 A/A、budget-exhausted halt、指派序列 = 头对齐前缀逐条相等、零时钟场、两次执行账本摘要逐字节相等、**跨来源象限账本摘要逐字节相等**；格 b 四变体 restore 输出摘要相等；格 d 两继续臂终态摘要 = 锚点摘要；验证行 `verification: all provenance-quadrant invariants hold (cross-arm ledger identity)` 两轮打印；附录 A）
- `pnpm gate`（typecheck + lint + test + build）✓ — 1168 pass / 0 fail / 1 skipped（既有 provider-smoke 凭据跳过）。Node ≥22.19.0（engines），本 VM 以 nvm 22.22.2 运行
- 未触碰任何版本化阈值、权限、数据面契约、公开签名；未改任何测试断言；未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README.md
- 按纪律，新轴测量脚本未入库（全文见附录 A）；`scripts/` 零新增；无赢家故未提交 `scripts/round24-r24f-equivalence-sim.ts`

## 5. 新增排除 ID

**无。** 本轮枚举为空，未铸任何 S24-F-\* ID。本轮状态序列化边界来源轴的四格均为收尾测量而非候选：格 a 是来源平面状态半边的首次象限测量（布尔塌缩 {ID,DI,DD} 互差 0.56/0.17 ms « MDE；II 唯一快角 −6.3~−7.3 ms；**DD−ID = +0.56/−0.06 ms ≈ 0**——checkpoint-resume régime价 = 生产现价；ID 行与 R14-F +7.19/+7.13 同带对接）；格 b 是微观仪（三往返臂互差 0.07/0.70 µs 塌缩、每 restore 税 +2.70~+4.03 µs 与 R14-F 流侧 ~+2.4 µs 同带、对齐份额交叉预测 37–56% 与 R14-F 分解同结构）；格 c 是首验税等值闭合（解析 vs 构造互差 ≤1.5 µs——每 resume 一次性税 = fresh-plan 税 +43~+45 µs，边界零新增机制）；格 d 是中途 resume 剂量（+2.54/+2.46 ms 次可加，引擎面侵蚀注记归档）。四格数字录档供后续轮次引用；均不构成任何既有条目的重开证据——特别地，régime的载体（S7-F-1 对齐内容比较 × 钦定 fail-closed Ω(P) 重读 × 每 op restore 义务）均为明文钦定语义，其消除方向（再规范化 / 跨调用串表 / 解析态跳验）分别被 R14-F 量级拒绝、X1-1、X3-3/X4-1 覆盖，无重开条件被触发。

MORE_OPTIMA=no
BRANCH=cursor/r24-f-experiments-twenty-fourth-pass-83a1

## 附录 A：新轴测量脚本（完整，可复现；无赢家按纪律不入库）

保存为 `tmp-r24f-axes.ts` 后 `npx tsx tmp-r24f-axes.ts` 运行（仓库根目录，依赖已装，Node ≥22.19）。这不是候选裁决仿真（本轮无候选进入测量门），是 §1.3 状态序列化边界来源轴的收尾测量。

```typescript
/**
 * R24-F new-axis measurements (temporary; NOT candidate adjudications and NOT
 * committed — no winner this round). One axis no earlier F round measured
 * directly: the STATE SERIALIZATION-BOUNDARY FACE of the public restore
 * contract — what happens when the SERIALIZED STATE itself (the thing
 * `restore(serialized)` is named for) actually crosses a JSON serialization
 * boundary, i.e. the checkpoint-resume configured state.
 *
 * Twenty-three rounds fed every runner op a state produced BY the runner in
 * the same process: the plan object pointer-shared, the population strings
 * interned, the ledgers in-memory. R14-F axis 2 (the provenance cell) varied
 * the CALLER-STREAM string provenance (JSON round-tripped outcome hashes)
 * while explicitly holding the plan object fixed ("population object
 * unchanged — production shares the plan by reference") and the state
 * threading in-memory. The complementary half of the provenance plane — the
 * STATE side — was never measured: a checkpoint-resume consumer parses the
 * whole state off disk, recreates the runner from the PARSED plan, and
 * continues; then the aligned-prefix compare (S7-F-1), the assign includes,
 * and every other string site run on pointer-distinct content-equal strings
 * on BOTH sides, and the Ω(P) validate runs on a fresh (never-hashed)
 * population.
 *
 * Cells:
 *
 *   cell a  end-to-end (state × stream) provenance quadrants at the anchor
 *           (P=2000, A=1000, production interleave, clock 0, head-aligned
 *           prefix topology):
 *             II  in-memory plan, interned stream        (= the anchor)
 *             ID  in-memory plan, de-interned stream     (= R14-F axis 2,
 *                 re-anchored on this VM as the shared instrument row)
 *             DI  parsed plan (runner created from it), interned stream
 *             DD  parsed plan + de-interned stream       (the realistic
 *                 checkpoint-resume quadrant)
 *           Arithmetic prediction: string pointer equality requires BOTH
 *           sides shared, so the quadrant grid must COLLAPSE TO A BOOLEAN —
 *           {ID, DI, DD} equal within the MDE envelope, II the only fast
 *           corner. The aligned-compare account: interleave op at ledger
 *           size k pays k aligned compares (assign) / k+1 (record), so the
 *           experiment pays Σ ≈ A² = 10⁶ compares; the delta is
 *           10⁶ × (content-compare − pointer-compare) plus the smaller
 *           non-alignment provenance remainder R14-F recorded.
 *
 *   cell b  per-restore micro at a full non-halted k=1000 ledger
 *           (maxAssignments=2A), four provenance variants of the SAME state:
 *             mem            all in-memory (baseline)
 *             rtState        whole state JSON round-tripped
 *             rtLedgerOnly   original plan, parsed assignments (ID-like)
 *             rtPlanOnly     parsed plan, original assignments (DI-like)
 *           Prediction: the three rt arms equal within noise (the Boolean
 *           collapse read at the micro level); slope = delta / 1000 compares
 *           is the per-compare content tax, which must cross-predict the
 *           alignment share of cell a (tax × 10⁶).
 *
 *   cell c  once-per-resume first-validate tax: the FIRST
 *           validateExperimentPlan call on a freshly parsed plan (V8 string
 *           hashes uncached) vs the first call on a freshly CONSTRUCTED plan
 *           (same uncached-hash condition, no serialization boundary) vs the
 *           steady per-call price. Prediction: parsed-first ≈ constructed-
 *           first, i.e. the once-per-resume tax is exactly the R18-F
 *           fresh-plan tax and serialization adds nothing beyond it.
 *
 *   cell d  mid-experiment resume position dose: run the first h=500 pairs
 *           in-memory, JSON round-trip the mid-state once (outside timing),
 *           recreate the runner from the parsed plan, and time ONLY the
 *           second-half continuation (pairs 500..999, de-interned stream)
 *           against the in-memory continuation. Aligned compares in the
 *           continuation: Σ_{k=500..999} ≈ (A² − h²) = 75% of the full
 *           experiment's, so the delta must be ≈ 0.75 × (DD − II).
 *
 * Every arm is verified deterministically before timing: final ledger sizes
 * A/A, budget-exhausted halt, assignment sequence content-identical to the
 * head-aligned prefix, zero clock fields, and — the decisive provenance
 * witness — the time-projected ledger digests of ALL arms byte-identical to
 * the anchor's (provenance must not leak into a single ledger byte). Then
 * all arms are timed round-robin (1 warmup + 5 passes) so process drift
 * spreads evenly.
 *
 * No candidate can come out of this axis: restoring the pointer fast path
 * would need state-string re-canonicalization against the population — the
 * R14-F assign-site canonicalization direction, already magnitude-rejected
 * (ceiling = the provenance delta itself, «10× under the landing line), and
 * any cross-call canonical string table is X1-1 hidden state; skipping the
 * Ω(P) validate on a parsed plan is a fail-closed violation (S3-F-2 / X1-1);
 * the once-per-resume tax is once-per-checkpoint µs class. These are
 * fixture-blind-spot closure cells, not pools.
 *
 * Uses the production modules directly. Run: npx tsx tmp-r24f-axes.ts
 */

import { createCandidateId, createResourceVersionId } from "./src/domain/ids.js";
import {
  validateExperimentPlan,
  type ExperimentPlan,
} from "./src/experiments/plan.js";
import {
  createShadowRunner,
  type ExperimentOutcome,
  type ShadowState,
} from "./src/experiments/shadow.js";
import { stableStringify } from "./src/experiments/manifest.js";

function onceMs(fn: () => void): number {
  const t0 = process.hrtime.bigint();
  fn();
  const t1 = process.hrtime.bigint();
  return Number(t1 - t0) / 1e6;
}

function median(xs: readonly number[]): number {
  const s = [...xs].sort((a, b) => a - b);
  return s[Math.floor(s.length / 2)] as number;
}

/** Historical fixture shape: `ep_` + zero-padded base36 index, L=11. */
function makePopulation(size: number): string[] {
  const population: string[] = [];
  for (let i = 0; i < size; i++) {
    population.push(`ep_${i.toString(36).padStart(8, "0")}`);
  }
  return population;
}

function makeShadowPlan(
  population: readonly string[],
  maxAssignments: number,
  id: string
): ExperimentPlan {
  return {
    planVersion: 1,
    experimentId: id,
    mode: "shadow",
    baselineVersionId: createResourceVersionId(() => "r24fbase"),
    candidateId: createCandidateId(() => "r24fcand"),
    population,
    metrics: ["utility"],
    thresholds: { maxGuardrailBreaches: 1_000_000, maxCostUsd: 1e12 },
    budget: { maxAssignments, maxWallClockMs: 1e12 },
    randomization: { seed: 42 },
    stopPolicy: { onGuardrail: "halt", onBudgetExhausted: "halt" },
    missingOutcomePolicy: "exclude",
    canary: undefined,
  };
}

const P = 2000;
const A = 1000;
const H = 500; // resume position for cell d
const population = makePopulation(P);
const internedHashes = population.slice(0, A);
/** De-interned caller hashes: content-equal, pointer-distinct. */
const deinternedHashes = JSON.parse(JSON.stringify(internedHashes)) as string[];
for (let i = 0; i < A; i++) {
  if (deinternedHashes[i] !== internedHashes[i]) throw new Error("round-trip mismatch");
}

function cleanStreamFor(hashes: readonly string[]): ExperimentOutcome[] {
  return hashes.map((h) => ({
    episodeHash: h,
    utility: 0.5,
    costUsd: 0.01,
    guardrailBreached: false,
  }));
}

/** Production interleave shape (evaluateProposalShadow), clock pinned to 0. */
function runInterleaved(plan: ExperimentPlan, stream: readonly ExperimentOutcome[]): ShadowState {
  const runner = createShadowRunner(plan);
  let state = runner.start(0);
  for (const outcome of stream) {
    if (state.halted) {
      break;
    }
    state = runner.assign(state, outcome.episodeHash, 0);
    state = runner.recordOutcome(state, outcome, 0);
  }
  return state;
}

function ledgerDigest(state: ShadowState): string {
  return stableStringify({
    assignments: state.assignments,
    outcomes: state.outcomes,
    guardrailBreaches: state.guardrailBreaches,
    halted: state.halted,
    haltReason: state.haltReason,
  });
}

interface Arm {
  readonly name: string;
  readonly exec: () => ShadowState;
}

/* ------------------------------ cell a arms ------------------------------- */
// One plan JSON per parsed arm so the parsed plan object is stable across
// passes (the once-per-resume hash fill is priced separately in cell c; the
// steady-state medians here price the post-boundary REGIME).
const planII = makeShadowPlan(population, A, "exp_r24f_axis");
const planID = makeShadowPlan(population, A, "exp_r24f_axis");
const planJson = JSON.stringify(makeShadowPlan(population, A, "exp_r24f_axis"));
const planDI = JSON.parse(planJson) as ExperimentPlan;
const planDD = JSON.parse(planJson) as ExperimentPlan;
for (const plan of [planII, planID, planDI, planDD]) {
  validateExperimentPlan(plan);
}
if ((planDI.population[0] as string) !== (population[0] as string)) {
  throw new Error("parsed population diverged");
}

const streamII = cleanStreamFor(internedHashes);
const streamID = cleanStreamFor(deinternedHashes);
const streamDI = cleanStreamFor(internedHashes);
const streamDD = cleanStreamFor(deinternedHashes);

const cellA: Arm[] = [
  { name: "II-anchor", exec: () => runInterleaved(planII, streamII) },
  { name: "ID-stream(R14F)", exec: () => runInterleaved(planID, streamID) },
  { name: "DI-state", exec: () => runInterleaved(planDI, streamDI) },
  { name: "DD-both(resume)", exec: () => runInterleaved(planDD, streamDD) },
];

/* ------------------- verification pass (untimed, twice) ------------------- */
let anchorDigest = "";
for (const arm of cellA) {
  const digests: string[] = [];
  for (let round = 0; round < 2; round++) {
    const state = arm.exec();
    if (!state.halted || state.haltReason !== "budget-exhausted") {
      throw new Error(`${arm.name}: expected budget-exhausted final state`);
    }
    if (state.assignments.length !== A || state.outcomes.length !== A) {
      throw new Error(`${arm.name}: unexpected ledger sizes`);
    }
    for (let i = 0; i < A; i++) {
      const assignment = state.assignments[i] as { episodeHash: string };
      if (assignment.episodeHash !== (population[i] as string)) {
        throw new Error(`${arm.name}: assignment ${i} not the head-aligned prefix`);
      }
    }
    if (state.guardrailBreaches !== 0 || state.startedAtMs !== 0 || state.elapsedMs !== 0) {
      throw new Error(`${arm.name}: unexpected counters/clock fields`);
    }
    digests.push(ledgerDigest(state));
  }
  if (digests[0] !== digests[1]) {
    throw new Error(`${arm.name}: nondeterministic ledger`);
  }
  if (arm.name === "II-anchor") {
    anchorDigest = digests[0] as string;
  } else if (digests[0] !== anchorDigest) {
    throw new Error(`${arm.name}: ledger diverged from anchor across provenance`);
  }
}
process.stdout.write(
  "verification: all provenance-quadrant invariants hold (cross-arm ledger identity)\n"
);

/* --------------------- cell a: round-robin end-to-end --------------------- */
for (const arm of cellA) {
  arm.exec(); // one warmup pass per arm
}
const PASSES = 5;
const samples = new Map<string, number[]>(cellA.map((arm) => [arm.name, []]));
for (let pass = 0; pass < PASSES; pass++) {
  for (const arm of cellA) {
    (samples.get(arm.name) as number[]).push(onceMs(() => {
      arm.exec();
    }));
  }
}
const baseII = median(samples.get("II-anchor") as number[]);
process.stdout.write(
  `cell a (P=${P}/A=${A}, production interleave, round-robin ${PASSES} passes, ` +
    `min/median ms; delta vs II-anchor):\n`
);
for (const arm of cellA) {
  const xs = samples.get(arm.name) as number[];
  const med = median(xs);
  const label = arm.name === "II-anchor" ? "(baseline)" : `delta=${(med - baseII).toFixed(2)}`;
  process.stdout.write(
    `${arm.name.padEnd(18)} min=${Math.min(...xs).toFixed(2)} median=${med.toFixed(2)} ${label}\n`
  );
}
const deltaDD = (median(samples.get("DD-both(resume)") as number[]) as number) - baseII;

/* -------------- cell b: per-restore micro at a full 2A ledger ------------- */
const planB = makeShadowPlan(population, 2 * A, "exp_r24f_axis");
const runnerB = createShadowRunner(planB);
let stateFull: ShadowState = runnerB.start(0);
for (const outcome of streamII) {
  stateFull = runnerB.assign(stateFull, outcome.episodeHash, 0);
  stateFull = runnerB.recordOutcome(stateFull, outcome, 0);
}
if (stateFull.halted || stateFull.assignments.length !== A) {
  throw new Error("cell b: micro state must stay active with a full ledger");
}
const stateRt = JSON.parse(JSON.stringify(stateFull)) as ShadowState;
const stateLedgerOnly: ShadowState = {
  ...stateFull,
  assignments: JSON.parse(JSON.stringify(stateFull.assignments)) as ShadowState["assignments"],
};
const statePlanOnly: ShadowState = {
  ...stateFull,
  plan: JSON.parse(JSON.stringify(stateFull.plan)) as ExperimentPlan,
};
const microArms: (readonly [string, ShadowState])[] = [
  ["mem", stateFull],
  ["rtState", stateRt],
  ["rtLedgerOnly", stateLedgerOnly],
  ["rtPlanOnly", statePlanOnly],
];
const fullDigest = ledgerDigest(runnerB.restore(stateFull));
for (const [name, state] of microArms) {
  if (ledgerDigest(runnerB.restore(state)) !== fullDigest) {
    throw new Error(`cell b ${name}: restore output diverged across provenance`);
  }
}
for (const [, state] of microArms) {
  for (let r = 0; r < 20; r++) runnerB.restore(state); // warmup (fills hash caches)
}
const RESTORES = 200;
const microSamples = new Map<string, number[]>(microArms.map(([name]) => [name, []]));
let sink = 0;
for (let pass = 0; pass < PASSES; pass++) {
  for (const [name, state] of microArms) {
    (microSamples.get(name) as number[]).push(onceMs(() => {
      for (let r = 0; r < RESTORES; r++) {
        sink += runnerB.restore(state).assignments.length;
      }
    }));
  }
}
const microBase = median(microSamples.get("mem") as number[]);
process.stdout.write(
  `cell b (full ${A}-entry active ledger, ${RESTORES} restores/pass, µs/restore medians; ` +
    `delta vs mem):\n`
);
let rtStateDeltaUs = 0;
for (const [name] of microArms) {
  const med = median(microSamples.get(name) as number[]);
  const perOp = (med * 1000) / RESTORES;
  const delta = ((med - microBase) * 1000) / RESTORES;
  if (name === "rtState") rtStateDeltaUs = delta;
  const label = name === "mem" ? "(baseline)" : `delta=${delta.toFixed(2)}µs`;
  process.stdout.write(`${name.padEnd(14)} ${perOp.toFixed(2)} µs/restore ${label}\n`);
}
const taxNsPerCompare = (rtStateDeltaUs * 1000) / A;
process.stdout.write(
  `cell b per-compare content tax ≈ ${taxNsPerCompare.toFixed(2)} ns/compare; ` +
    `alignment-share cross-prediction for cell a: ${((taxNsPerCompare * A * A) / 1e6).toFixed(2)} ms ` +
    `vs measured DD delta ${deltaDD.toFixed(2)} ms\n`
);
if (!Number.isFinite(sink)) throw new Error("sink overflow");

/* ------------- cell c: once-per-resume first-validate tax ----------------- */
const FRESH = 5;
const STEADY_CALLS = 50;
const firstParsed: number[] = [];
const firstConstructed: number[] = [];
const steady: number[] = [];
for (let i = 0; i < FRESH; i++) {
  const parsed = JSON.parse(planJson) as ExperimentPlan;
  firstParsed.push(onceMs(() => {
    validateExperimentPlan(parsed);
  }));
  const constructed = makeShadowPlan(makePopulation(P), A, "exp_r24f_axis");
  firstConstructed.push(onceMs(() => {
    validateExperimentPlan(constructed);
  }));
  const steadyMs = onceMs(() => {
    for (let c = 0; c < STEADY_CALLS; c++) {
      validateExperimentPlan(parsed);
    }
  });
  steady.push(steadyMs / STEADY_CALLS);
}
process.stdout.write(
  `cell c (first validate on fresh object, µs; median of ${FRESH}): ` +
    `parsed=${(median(firstParsed) * 1000).toFixed(1)} ` +
    `constructed=${(median(firstConstructed) * 1000).toFixed(1)} ` +
    `steady=${(median(steady) * 1000).toFixed(1)}\n`
);

/* -------------- cell d: mid-experiment resume continuation ---------------- */
const planCont = makeShadowPlan(population, A, "exp_r24f_axis");
const runnerCont = createShadowRunner(planCont);
let stateMid: ShadowState = runnerCont.start(0);
for (let i = 0; i < H; i++) {
  const outcome = streamII[i] as ExperimentOutcome;
  stateMid = runnerCont.assign(stateMid, outcome.episodeHash, 0);
  stateMid = runnerCont.recordOutcome(stateMid, outcome, 0);
}
if (stateMid.halted || stateMid.assignments.length !== H) {
  throw new Error("cell d: mid state must be active at H");
}
// The checkpoint: one JSON round trip of the whole mid-state (outside timing;
// the once-per-resume costs are cell c's), runner recreated from the parsed
// plan — the realistic resume quadrant.
const parsedMid = JSON.parse(JSON.stringify(stateMid)) as ShadowState;
const runnerRes = createShadowRunner(parsedMid.plan);
const tailII = streamII.slice(H);
const tailDD = cleanStreamFor(deinternedHashes.slice(H));

function continueFrom(
  runner: ReturnType<typeof createShadowRunner>,
  start: ShadowState,
  stream: readonly ExperimentOutcome[]
): ShadowState {
  let state = start;
  for (const outcome of stream) {
    if (state.halted) {
      break;
    }
    state = runner.assign(state, outcome.episodeHash, 0);
    state = runner.recordOutcome(state, outcome, 0);
  }
  return state;
}

const contArms: Arm[] = [
  { name: "CONT-mem", exec: () => continueFrom(runnerCont, stateMid, tailII) },
  { name: "CONT-resume", exec: () => continueFrom(runnerRes, parsedMid, tailDD) },
];
for (const arm of contArms) {
  const final = arm.exec();
  if (!final.halted || final.haltReason !== "budget-exhausted" || final.assignments.length !== A) {
    throw new Error(`${arm.name}: continuation did not reach the anchor final state`);
  }
  if (ledgerDigest(final) !== anchorDigest) {
    throw new Error(`${arm.name}: continuation ledger diverged from anchor`);
  }
  arm.exec(); // warmup
}
const contSamples = new Map<string, number[]>(contArms.map((arm) => [arm.name, []]));
for (let pass = 0; pass < PASSES; pass++) {
  for (const arm of contArms) {
    (contSamples.get(arm.name) as number[]).push(onceMs(() => {
      arm.exec();
    }));
  }
}
const contMem = median(contSamples.get("CONT-mem") as number[]);
const contRes = median(contSamples.get("CONT-resume") as number[]);
process.stdout.write(
  `cell d (resume at h=${H}, second-half continuation only, median ms): ` +
    `CONT-mem=${contMem.toFixed(2)} CONT-resume=${contRes.toFixed(2)} ` +
    `delta=${(contRes - contMem).toFixed(2)} ` +
    `(prediction ≈ 0.75 × DD delta = ${(0.75 * deltaDD).toFixed(2)})\n`
);
```

两次独立运行原始输出（完整）：

```text
run 1:
verification: all provenance-quadrant invariants hold (cross-arm ledger identity)
cell a (P=2000/A=1000, production interleave, round-robin 5 passes, min/median ms; delta vs II-anchor):
II-anchor          min=116.13 median=116.43 (baseline)
ID-stream(R14F)    min=122.80 median=123.18 delta=6.76
DI-state           min=123.12 median=123.57 delta=7.15
DD-both(resume)    min=123.34 median=123.74 delta=7.31
cell b (full 1000-entry active ledger, 200 restores/pass, µs/restore medians; delta vs mem):
mem            54.44 µs/restore (baseline)
rtState        57.14 µs/restore delta=2.70µs
rtLedgerOnly   57.21 µs/restore delta=2.77µs
rtPlanOnly     57.17 µs/restore delta=2.73µs
cell b per-compare content tax ≈ 2.70 ns/compare; alignment-share cross-prediction for cell a: 2.70 ms vs measured DD delta 7.31 ms
cell c (first validate on fresh object, µs; median of 5): parsed=95.5 constructed=95.9 steady=51.4
cell d (resume at h=500, second-half continuation only, median ms): CONT-mem=61.33 CONT-resume=63.88 delta=2.54 (prediction ≈ 0.75 × DD delta = 5.49)

run 2:
verification: all provenance-quadrant invariants hold (cross-arm ledger identity)
cell a (P=2000/A=1000, production interleave, round-robin 5 passes, min/median ms; delta vs II-anchor):
II-anchor          min=113.94 median=114.39 (baseline)
ID-stream(R14F)    min=120.60 median=120.81 delta=6.42
DI-state           min=120.28 median=120.64 delta=6.25
DD-both(resume)    min=120.16 median=120.75 delta=6.35
cell b (full 1000-entry active ledger, 200 restores/pass, µs/restore medians; delta vs mem):
mem            52.92 µs/restore (baseline)
rtState        56.47 µs/restore delta=3.55µs
rtLedgerOnly   56.95 µs/restore delta=4.03µs
rtPlanOnly     56.25 µs/restore delta=3.33µs
cell b per-compare content tax ≈ 3.55 ns/compare; alignment-share cross-prediction for cell a: 3.55 ms vs measured DD delta 6.35 ms
cell c (first validate on fresh object, µs; median of 5): parsed=93.4 constructed=94.9 steady=50.3
cell d (resume at h=500, second-half continuation only, median ms): CONT-mem=60.57 CONT-resume=63.03 delta=2.46 (prediction ≈ 0.75 × DD delta = 4.76)
```
