MODEL_SLUG=claude-fable-5-thinking-xhigh

# R24-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第二十四遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 24 / R24-A（Round 1–23 同区第二十四遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `350dd2d`（含 R23-G 空枚举收口、Round 24 PLAN 派出与 CKMIX 移植禁令；R23-I / R23-J 在飞未触碰）
**分支:** `cursor/r24-a-tracking-twenty-fourth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S24-A-\* ID，切片零代码改动；
整片预算收口第二十二次复核成立，并新增一条实测轴——「残余输入面列表维度
普查」（axis 13）补上二十三遍搜查史里输入面定价的最后一块**字段粒度**
盲区：R15-A 定价了 prescoreInput 的 C/R 参数轴（含 Cc=R、Rt=C 两条
对角线），R16-A 定价了载荷字节轴 L/V/S（V 在 N_op=1 下按每工具 ref
列表剂量），R20-A 定价了 window 的 M（openMinors）与 K（previous
驻留链），R21-A 定价了 humanInput 三维，R22-A 定价了零流量分析面 T，
R14/R17/R23-A 定价了事件表 E、组成混合与 G×E 自耦合——但组合面
（runTrackingTurn / computePrescore 两个公开输入结构）的**列表型字段
计数**从未被逐字段剂量：toolSituations 计数 N_op（R16-A V 是 N_op=1
下的 ref 剂量；R22-A T 只经零流量分析面剂量过 operations 计数，从未
经生产 turn 管线）、claims 计数 N_cl（R16-A S 是 N_cl=1 下的字节
剂量）、`writePaths × ownedPaths.includes` 二次位点 W×O
（prescore.ts:115–117；机制被 S1-A-4/X4-7 家族换名裁决过但从未剂量
定价）、roller 的 `unresolved × confirmedDecisions.includes` 二次
位点 Q×D（roller.ts:31–34；同上）、contextFacts 平坦引用 CF、以及
R15-A 对角线之外的 Cc/Rt 补充格。本轮建立确定性剂量驱动器给出六组
定价（三次独立运行区间）：**13a** N_op 线性斜率 5.89–6.03 ns/op
（clean turn 级；ref-loaded 腿 ~55.8–58.7 ns/op 是 N_op×V 乘积腿——
并集对总 ref 数线性、无独立耦合系数，与 R23-A 的 G×E 真二次耦合
不同型），10 ms/run 穿越 N_op ≈ 3.32–3.39×10⁵（生产 N_op=1，
~5.5 个量级）；**13b** N_cl 每扫 29.80–31.38 ns/claim（全不匹配最坏
情形 × 3 scans/turn = prescore×2 + turn derived-gap），穿越
N_cl ≈ 2.12–2.24×10⁴（生产 N_cl=1，~4.3 个量级）；**13c** W×O
~5.09–8.13 ns/comparison，穿越 W=O ≈ 496–627——但生产 W=O=0
（零流量面，悬崖只在面先获得流量时存在）；**13d** Q×D
~1.78–1.80 ns/comparison，穿越 Q=D ≈ 1054–1060（生产 Q=D=0，同为
零流量面）；**13e** CF 结构性平坦（4096 条 vs 2 条 delta −10~−31 ns，
切片只搬数组引用）；**13f** 对角线外 Cc+256 delta 191–201 ns /
Rt+256 delta 547–629 ns（R15-A 已价 includes 扫描家族的对角线外
纹理，生产恰在对角线原点）。五组确定性证人（F4a–F4e：700 形状填充
不变性、并集精确性 54/122、Q/D 合并精确性、W/O 语义 40/40 位置
完备、生产残余普查）三次运行逐字节一致。该轴上全部可想摊平路经换名
检查逐一判为既有排除 ID 的本体（5 项新记录，见 §0——四处 includes
站点 Set 化恰是 **S1-A-4/X4-7 家族本体**），**只产出归档证人与输入面
字段粒度格网的闭合，未产出可落地候选**——两个二次位点生产流量恰 0、
两个线性计数距生产 4–5.5 个量级，按令即空收口。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R23-A 至本轮基线
（`350dd2d`）**逐字节未变**（`git diff 7acb666..350dd2d -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，二十四遍全程零
diff；清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2）并全部
实际重读）。调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..350dd2d -- src/` **完全为空**（跨 R10…R24 开轮十五段
全部为 docs 提交，`git log 89f0455..HEAD -- src/` 计数为 0），生产调用方
经本轮全库 grep 复核仍为 `supervisor.ts:483`
（applyTrackingGate/nextTrackingSeq；两个 settle 调用点 601/711 均不传
trackingAssessment ⇒ 生产 O(1) no-op，第十五次复验，本轮源码级重读
`settleSupervisedOutcome` 首行 `if (assessment === undefined) return`）/
`coordinator.ts:444` / `flowchart-run.ts:320`（applyChildThreeLine），
每子结果一次（~5 次/run），事件表几十级（41）。R23-A 的预算天花板
（报告文本 ~70–82 µs/run；派单锚 66–78）经本轮在当前 VM
（Node v22.22.2；本 VM 默认 v22.14.0 且 `/exec-daemon/node` 垫片遮蔽
nvm，按 engines >=22.19.0 经 PATH 前置 nvm v22.22.2）实测复核为
**~67–84 µs/run**（五格锚点矩阵三次独立运行 13.4–16.9 µs/gate；上沿
84 由 run1 no-contract 首格 16.9 拉出——R11-A 以来「PIC/基准次序」
现象的本轮形态，run2/3 同格 15.9–16.0；下沿 67 由 run2
FAILED×contract 13.4 拉出；与 R23-A 70–82 / R22-A 65–80 重叠同带；
单格横向比较不具意义；量级结论不变——稳态下即使切片零成本，节省上界
~0.07–0.08 ms/run，仍比落地线低约三个量级）。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
数据补充（5 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项 +
R15-A 5 项 + R16-A 4 项 + R17-A 4 项 + R18-A 4 项 + R19-A 2 项 +
R20-A 3 项 + R21-A 2 项 + R22-A 5 项 + R23-A 5 项换名检查全部复核
维持，未追认任何 S12…S23-A-\* ID），按令一律不铸新 ID。

R12-A 分解的五大组件占比（hashAssessment×3 ~38–41% / validateEvent
~18–19% / turn ~11–12% / prescore×2 ~8.6–9.0% / O(E) 五扫描+拷贝
~1.8–1.9%）在本 VM 逐一复现（§1 表），R18-A skip 定价与 R19-A replay
定价抽测复现同带（guard-3 skip 1961–2137 ns = 12.7–14.3% of apply；
replay find#1 3166–3210 ns = 20.6–21.5% of apply）。第二十四遍收口，
**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；未触碰在飞的
  R23-I / R23-J 分支与文件，未触碰已收口的 R23-\* 报告。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表 545 行，含
  S13-B-1；R12-A…R23-A 空枚举未铸 ID，确认）/ PROGRESS.md /
  round-24/PLAN.md / R23-A / R22-A / R21-A；另通读 R23-B…R23-H 七份
  兄弟报告确认未移植 CKMIX / ORDX / GS / P-dose / batch-arrangement /
  membership-mix / plan count-field 任一轴。EXCLUSIONS.md /
  PROGRESS.md / 各 PLAN.md / README.md 零触碰。
- 环境：本 VM 默认 Node v22.14.0（`/exec-daemon/node` 垫片遮蔽 nvm），
  按 engines（>=22.19.0）以 PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 切至 v22.22.2 后
  `pnpm install --frozen-lockfile`，全部测量在 v22.22.2 完成。
- **派单提示类核对**：提示类 (i) isolation/combined-score 非 H 输入
  耦合——combined-score 输入是标量对（prescore P + human score，
  R13-A §0-1 舍入恒等已锁），isolation 双函数零 src/ 调用方
  （R22-A 已价），无未扫列表维度；(ii) turn/prescore 非 G×E 组合面
  ——**本轮 axis 13 即落在此类**（组合面两个公开输入结构的字段粒度
  列表普查）；(iii) 非 R22-A 普查的分析导出消费者面——R22-A 后导出
  消费者面仍零 src/ 流量，无新面。按令取真实缺口 (ii)。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或数据补充，**一律不铸 S24-A-\* ID**。R12-A 13 项、R13-A 8 项、
  R14-A 3 项、R15-A 5 项、R16-A 4 项、R17-A 4 项、R18-A 4 项、R19-A
  2 项、R20-A 3 项、R21-A 2 项、R22-A 5 项、R23-A 5 项换名检查全部
  复核维持，不再逐条重列；以下 5 项为本轮**新记录**，全部由 axis 13
  的字段粒度视角引出）：
  1. **四处 `includes` 站点 Set 化**（prescore.ts:116 ownedPaths /
     :123 completedChecks / :129 retainedConstraintIds /
     roller.ts:34 confirmedDecisions——本轮两处二次位点 W×O、Q×D 的
     直接摊平路）= **S1-A-4 / X4-7 家族本体**（R15-A 轴 1 重开条件
     C/R ≈ 10³ 未触发；且 W×O、Q×D 生产流量恰 0——重开条件比 C 轴
     更远：面须先获得流量），拒列；
  2. **三道 claims 扫描融合**（prescore.ts:101 evidenceOutcome 的
     `claims.some(isSuccessClaim)` × 2 次 prescore + turn.ts:180
     derived-gap 第三扫——N_cl 斜率 3× 乘数的直接摊平路）=
     S1-A-2（第二次 prescore 已拒）+ S3-A-3（claims.some 重复计算
     去重已拒）本体，拒列；
  3. **N_op 四遍历共享/单遍融合**（turn.ts:101 ownershipEscape
     some + prescore×2 内 escaped/exitCode 扫描 + turn.ts:183
     collectEvidence——每 turn 对 toolSituations 四次独立遍历）=
     S1-A-2 + S5-A-1 / S7-A-2 单遍融合家族本体（融合破坏
     turn/prescore 模块边界，家族已拒），拒列；
  4. **collectEvidence 并集按输入身份记忆化/驻留**（WeakMap 以
     toolSituations 数组身份为键）= X1-1 隐藏缓存本体（生产每 turn
     fresh window ⇒ 命中率恰 0），拒列；
  5. **isSuccessClaim 正则按 claim 记忆化 / 公开面截断残余列表**
     （per-claim regex memo = X1-1 本体且生产 N_cl=1 命中 0；截断
     claims/toolSituations/writePaths = S2-A-1 截断家族 + **功能性
     变更**——丢弃合法输入会改判决向量，非保行为优化），拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  幂等双 find/死扫描 = S8-A-1 / S11-A-2；episode/seq/status/copy 五
  扫描逐条 = X0-4/X2-4/S1-A-1/S1-A-9/R1-A 清单（R12-A 起锁定）；写侧
  validateEvent = S4-G-2 族（实现在 events.ts，G 切片）；对角线
  `every×includes` Set 化 = S1-A-4/X4-7（C/R ≈ 10³ 重开条件，R15-A
  轴 1）；第二次 prescore = S1-A-2；turn 恒定输入常量折叠 = R12-A
  §0-11 伞 + R16-A §0-2；skip 守卫次序/享元/prescore 复用 = R18-A
  §0-1/2/4 维持；replay 快路径/重算去重 = R19-A §0-1/2 维持；
  hashSummary 缓存/增量与 with-previous 管道融合 = R20-A §0-2/3
  维持；H 面 = R21-A 维持；分析-隔离面 = R22-A §0-1..5 维持；
  G×E 积分账本五路 = R23-A §0-1..5 维持。
- 未重开任何 X\* / S1-\* … S23-\* 条目。**R23-A G×E 自耦合积分账本
  （b=13.96–14.20 ns/event、二次系数 0.0484–0.0496 µs、修正穿越点
  N≈341–344）不补铸、不重测**（axis 13 是输入面字段维度，与事件表
  积分正交——13a–13f 全部剂量在固定 E=41 的公开组合面上）；**R22-A
  公开导出普查 + 分析-隔离面不补铸**；**R21-A H 输入面（humanInput；
  生产恒 `{}`）不补铸**；**R20-A `gate.openMinors` M 轴与多轮驻留链
  K 不补铸**（13d 的 Q/D 是 window 当轮字段，非 previous 驻留链；
  13e 的 CF 平坦证人与 K 链正交）；**R19-A 重投递 / wait_user
  不补铸**；R18-A 两轴只抽测 guard-3 skip 一格；R17-A 组成轴不重测；
  未把 CKMIX / ORDX / GS / P-dose / batch-arrangement /
  membership-mix / plan count-field / SHAPEK / KFAN / NVG / IDG /
  SEEDX / YMIX / SCALEX / FITQ / NAMESHAPE / XPROC / SCHEDWIN /
  TERMCLASS 移植为本切片新 A ID；S11-A-1 / S11-A-2 / S10-A-1 /
  S9-A-1 / S8-A-1..3 / S7-A-1..4 / X0-4/X2-4 / X0-6 / S6-A-1 专项
  复核：本轮零触碰、零重提，全部维持关闭。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点五格
  （gate-outcome × contract × max-codes）覆盖；配置态逐面复核——
  (a) settle 带/不带 assessment：生产两调用点 601/711 均不带
  （O(1) no-op 源码级复验），带 assessment 臂即 applyTrackingGate
  本体（已价）；(b) trackingConfig 覆写/applyUserThreshold：生产
  from-child 恒走 DEFAULT（R22-A config 双臂已价，X0-7 阈值冻结
  维持）；(c) maxItems 截断态 = S2-A-1 已拒面（axis 13 的 F4a 填充
  剂量全程走 maxItems-undefined 生产臂）；(d) policyVersion 为透传
  字符串无分支。axis 13 剂量补充覆盖了输入面字段维度的全部非零格
  （见 §1），无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足——axis 13 只经公开
  runTrackingTurn/computePrescore/applyChildThreeLine 剂量，F4a–F4e
  是对既有填充不变性/并集/合并/scope 契约的**分布级回声验证**，未改
  任何阈值、未改任何测试。不声称 Outcome-supported；Checkpoint
  F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本
  （按令不建 `scripts/round24-r24a-equivalence-sim.ts`），仿真全文见
  本报告附录。不开 PR。

## 1. 预算支配论证复核 + 一条新轴（方法第 1、2 步）

R3-A §3 … R23-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..350dd2d -- <切片>` 为空，逐字节
   一致（二十四遍全程零 diff）——R1-A…R23-A 全部测量与排除按令转移，
   但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨十五段）**：`git diff 89f0455..350dd2d --
   src/` **完全为空**（R10 开轮以来全部提交均在 docs/ 下）。生产调用方
   三点位（supervisor.ts:483 / coordinator.ts:444 /
   flowchart-run.ts:320）、settle 调用点 601/711 不传 assessment
   （首行 `if (assessment === undefined) return`）、
   `parseTrackingAssessment` 唯一 src/ 调用方 events.ts:738、
   `runTrackingTurn` 唯一 src/ 调用方 from-child.ts:81（humanInput 恒
   `{}`）、`computePrescore` 切片外零 src/ 调用方、`proposeFromAnomaly`
   / `sanitizePacketForAnalysis` / isolation 双函数 /
   `executionAuthority` 切片外零 src/ 调用方、`extractHumanScore` /
   `hashSummary` 切片外零 src/ 生产调用方，经本轮全库 grep 全部复验，
   无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               16.9      15.9      16.0  µs/gate (+1 event)
PASSED x contract(3)               15.5      14.9      15.0  µs/gate (+1 event)
FAILED x no-contract               15.3      14.9      15.2  µs/gate (+3 events)
FAILED x contract(3)               13.8      13.4      13.9  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.5      14.0      14.6  µs/gate (+3 events)
=> ~67–84 µs/run（稳态热；除 run1 首格 16.9 上沿外 ~67–80）
```

与 R23-A（14.0–16.4 µs/gate ⇒ 70–82 µs/run）/ R22-A（65–80）重叠
同带；五格三次运行**无悬崖、无空洞**。本轮仿真在锚点前多驱动了
axis 13 的剂量形状（宽 toolSituations / 长 claims / W×O / Q×D 多
形态），PIC 污染路径与 R21-A / R23-A 同型（首格上沿），已按 R11-A
「PIC/基准次序」家族记录。稳态下即使把整个切片优化到零成本，节省
上界 ~0.07–0.08 ms/run，仍比战役落地线（数十~数百 ms 或复杂度类
下降）低**约三个量级**。**支配论证（稳态口径）第二十二次复核成立**；
生产冷态口径维持 R17-A 轴 6b 裁决（[2.7, 8.5] ms/run once-per-run，
不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R23-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R23-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1975–2014 ns 每次 ⇒ **5.93–6.04 µs** | 5.77–5.81 µs | ~38–41% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 326–360 ns + 第三次 hash） | **2.77–2.81 µs** | 2.99–3.06 µs | ~18–19% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.69–1.72 µs** | 1.70–1.73 µs | ~11–12% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 666–672 ns 每次 ⇒ **1.33–1.34 µs** | 1.31–1.33 µs | ~8.6–9.0% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 75–79 + seq 46–51 + 双幂等 find 39–40×2 + status 50–52 + `[...events]` 26–27 ns） | **278–284 ns**（⇒ 6.77–6.92 ns/event @E=41） | 295–298 ns | **~1.8–1.9%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以当轮 contract 格（14.9–15.5 µs）计。`applyTrackingGate` 整体
隔离实测 5.84–5.95 µs，与 R22-A（5.93–6.01）同带（R23-A 6.41–6.44
略高为其 VM 世代抖动，组件占比不变）。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次运行区间）：guard-3 skip（FAILED 零 evidence refs）1961–2137 ns
= 12.7–14.3% of apply（R23-A 13.1–14.7% 同带）；replay find#1
（GT idempotencyKey 命中，E=44）3166–3210 ns = 20.6–21.5% of apply
（R23-A 20.2–24.0% 同带）——组成天花板 {apply, skip, replay} 两个半边
继续成立，不重测其余格。

**本轮新轴 13：残余输入面列表维度普查**（确定性剂量经公开
runTrackingTurn / computePrescore 面驱动，固定 E=41 与 R23-A 事件表
积分正交；三次运行区间）：

```text
证人（F4a–F4e 钉死，三次运行证人行逐字节一致）:
  F4a: 700 个种子化填充形状（claims≤64, tools≤64, W=O≤31,
    Cc/Rt pad≤31, Q/D≤15）——维度向量 / P / quality / coverage /
    cappedByHardFail 全不变 700/700；turn gate 恒 none 且 score === P
  F4b: 41-tool 重叠夹具 summary.evidenceRefs = 精确首插并集
    （54/122 原始 ref，重复折叠，顺序确定）
  F4c: roller Q/D 合并精确——去重 + confirmed 过滤 + 首现序
    （重叠夹具 ['q_a','q_c'] / ['q_b','q_d','dec_x']；全不相交夹具
    整体存活）
  F4d: W/O scope 语义精确——全 owned PASS；任一种子化位置单个未
    owned 路径翻 FAIL（40/40）；多余 owned 条目永不翻
  F4e: 生产残余普查——from-child 路径 N_cl=1、N_op=1、W=O=0、
    Cc/Rt 恰在 R15-A 对角线、window Q=D=0、CF=2：axis 13 全部维度
    在生产路径为零或单位值（以下剂量是公开 API 面定价，非生产
    复定价）

13a  N_op（toolSituations 计数，公开 runTrackingTurn 面）:
  clean      N=1: 2017–2071  N=8: 1725–1739  N=64: 2032–2042
             N=512: 4699–4776 ns
  ref-loaded N=512: 30191–31693 ns（N=8..512 斜率 ~55.8–58.7 ns/op
    = N_op×V 乘积腿：每 tool 2 refs ⇒ ~28–29 ns/ref，与 R16-A V 轴
    每 ref 并集价同带——并集对总 ref 数线性、无独立耦合系数，
    与 R23-A G×E 真二次耦合不同型，不构成新耦合项）
  消费者分解 @N_op=512 clean: escaped 单扫 698–808 ns |
    collectEvidence 形并集 1915–2028 ns | computePrescore 整体
    1335–1644 ns（×2/turn）
  => 斜率 5.89–6.03 ns/op（clean，turn 级，N=8..512 段；N=1 格带
     残余 PIC 纹理已预热处理并如实记录）；生产 N_op=1；
     10 ms/run 穿越（5 turn/run）N_op ≈ 3.32–3.39×10⁵（~5.5 个量级）

13b  N_cl（claims 计数，公开 computePrescore 面）:
  {1,8,64,512}: 666–790 / 892–988 / 2503–2637 / 16020–16709 ns
  => 斜率 29.80–31.38 ns/claim/scan（全不匹配最坏情形——每 claim 走
     完整 SUCCESS_CLAIM 正则不早退；3 scans/turn = prescore×2 +
     turn.ts:180 derived-gap 第三扫）；生产 N_cl=1；
     穿越 N_cl ≈ 2.12–2.24×10⁴（~4.3 个量级）

13c  W×O 二次位点（prescore.ts:115–117 writePaths × ownedPaths.includes）:
  {4,16,64,256}: 712–740 / 909–1079 / 5656–8169 / 168167–268140 ns
  => ~5.09–8.13 ns/comparison（all-owned 自索引纹理，×2/turn；区间
     偏宽系引擎字符串比较/内存布局纹理——run1 @256 168 µs vs run2/3
     265/268 µs，二次分类三次运行一致）；生产 W=O=0（零流量面）；
     穿越 W=O ≈ 496–627

13d  Q×D 二次位点（roller.ts:31–34 unresolved × confirmedDecisions.includes）:
  {4,16,64,256} 全不相交: 2408–2579 / 3638–3702 / 15475–15526 /
    118589–119943 ns
  => ~1.78–1.80 ns/comparison（全 miss 最坏情形，roller 每 turn
     一次 + fresh window 免 hashSummary）；生产 Q=D=0（零流量面）；
     穿越 Q=D ≈ 1054–1060

13e  CF 平坦引用证人（contextFacts）:
  turn @CF=2: 1671–1698 ns vs @CF=4096: 1654–1673 ns（2048× 条目
    delta −10~−31 ns）——结构性平坦：切片只搬数组引用（packet 携带
    是 wake-arm-only 且同为引用）

13f  对角线外 Cc / Rt 补充格（R15-A 对角线延拓）:
  computePrescore base 663–677 ns | Cc+256 前缀填充 859–878 ns
    （delta 191–201 = R=1 × 256-miss + turn derived-gap 扫）|
    Rt+256 前缀填充 1222–1306 ns（delta 547–629 = C=3 × 256-miss）
  ——对角线外纹理即 R15-A 在对角线上已价的同一 includes 扫描家族；
    生产坐标恰在对角线原点
```

五个结论：

1. **输入面字段粒度格网闭合**：二十三遍以参数粒度定价了轴族
   {E, C, R, V, S, L, M, K, H_L, H_m, H_N, T, G, G×E}——本轮把组合面
   两个公开输入结构的**全部列表型字段**逐一补价或补平坦证人：
   PrescoreInput 8 个列表字段（constraints=C、requiredChecks=R、
   completedChecks 对角线 R15-A + 对角线外 13f、retainedConstraintIds
   对角线 R15-A + 对角线外 13f、claims 字节 R16-A-S + 计数 13b、
   toolSituations 每工具 ref R16-A-V + 计数 13a、writePaths ×
   ownedPaths 13c）与 TrackingWindow 6 个列表字段（contextFacts 13e
   平坦、toolSituations 13a 同址、constraints=C、unresolvedDecisions
   × confirmedDecisions 13d、openMinors=M R20-A、previous 链=K
   R20-A）自此**每个字段都有价或有平坦证人**。
2. **两处二次位点首次剂量定价（非重开）**：W×O 与 Q×D 的机制此前被
   S1-A-4/X4-7 家族换名裁决过但从未剂量——本轮给出 ~5.09–8.13 与
   ~1.78–1.80 ns/comparison 及穿越点 496–627 / 1054–1060。数值上
   它们是切片穿越点表里最近的两格，但**生产流量恰 0**（W=O=0、
   Q=D=0，F4e 普查钉死）——量级距离在零流量面上无定义，悬崖只在面
   先获得流量时存在，故不改变可操作重开优先级。
3. **两条线性计数距生产 4–5.5 个量级**：N_op 穿越 3.32–3.39×10⁵、
   N_cl 穿越 2.12–2.24×10⁴；ref-loaded 腿证实 N_op×V 是乘积腿而非
   独立耦合项（并集对总 ref 数线性）——与 R23-A G×E（真二次积分项）
   对照，输入面内部**无遗漏的轴间耦合**。
4. **契约回声闭环**：F4a–F4e 把填充不变性（700 形状全维度不变）、
   并集精确性、合并精确性、scope 翻转的位置完备性（40/40）钉为
   分布级证人——这是这些字段语义在**任意计数态**下的首次分布级验证
   （此前测试只覆盖生产小计数）。
5. **不产生候选**：四处 includes Set 化 = S1-A-4/X4-7 本体（§0-1）；
   三扫融合 = S1-A-2 + S3-A-3（§0-2）；四遍历融合 = S5-A-1/S7-A-2
   （§0-3）；并集 memo = X1-1（§0-4，命中率恰 0）；正则 memo/公开面
   截断 = X1-1 + S2-A-1 + 功能性变更（§0-5）。两个二次位点零流量、
   两条线性轴距生产 4–5.5 个量级 ⇒ 按令空收口。

**重开条件全景更新（在 R23-A §1 表基础上新增六行）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| M（openMinors 条数） | 0 | ≈ 4.6–5.8×10⁴ | ~4+ | R20-A |
| K（驻留链 turn 数） | 0（恒 fresh） | ≈ 3×10³ turn | ~2 | R20-A |
| H_L / H_m / H_N（humanInput 三维） | 0 | ≈ 1.2 MB / 2.2–2.3×10⁴ / 4.65–4.74×10⁴ | ~3–5 | R21-A |
| T（analysis content 操作基数） | 0（面零流量） | ≈ 1.50–1.51×10⁵ | ~5 | R22-A |
| G（gates/run，固定 E 线性算术） | ~5 | ≈ 645–714 | ~2.1 | R16-A 系 |
| G（gates/run，含 G×E 自耦合二次项） | ~5 | ≈ 341–344 | ~1.8 | R23-A |
| **N_op（toolSituations 计数）** | **1** | **≈ 3.32–3.39×10⁵** | **~5.5** | **本轮** |
| **N_cl（claims 计数）** | **1** | **≈ 2.12–2.24×10⁴** | **~4.3** | **本轮** |
| **W×O（scope 二次位点）** | **0（零流量面）** | **≈ 496–627** | **—（面须先获流量）** | **本轮** |
| **Q×D（roller 二次位点）** | **0（零流量面）** | **≈ 1054–1060** | **—（面须先获流量）** | **本轮** |
| **CF（contextFacts） ** | **2** | **无穿越点（平坦引用）** | **—** | **本轮** |
| **Cc/Rt 对角线外** | **对角线原点** | **同 R15-A includes 家族** | **—** | **本轮** |
| G 组成（apply/skip/replay 全混合） | 全 apply | 混入只降不升 | — | R18-A + R19-A |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–15.2 µs/fault | — | R18-A + R22-A |
| 重投递面（once-per-redelivery） | 0 次/run | ~2.8–3.4 µs/次 | — | R19-A + R23-A F3d 形态 |

**可操作重开优先级不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7
家族 `every×includes` 站点 Set 化）仍是第一优先重开对象——本轮新价的
W×O / Q×D 穿越点数值上更近（496–1060）但生产流量恰 0，其摊平路又
恰是同一 S1-A-4/X4-7 家族（届时与 C 轴一并按家族程序重审）。本轮
关闭的是**字段粒度维**：R14–R16 关规模/载荷维、R17 关组成/温度维、
R18 关组成/故障维、R19 关结局维、R20 关驻留状态维、R21 关输入参数
维、R22 关公开导出维、R23 关轴间耦合维之后，组合面输入结构的列表
字段格网自此全部有价。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 5 项 + R12-A…R23-A
58 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、数据
补充或本体，按「换名不铸 ID」纪律未列任何 S24-A-\* 候选。空枚举与
R12-A…R23-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第二十四遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `prescore.ts` | **axis 13 主宿主**（13b 三扫 N_cl 斜率 29.80–31.38 ns/claim；13c W×O 二次位点 :115–117 首次剂量 ~5.09–8.13 ns/cmp 穿越 496–627；13f 对角线外 Cc :123 / Rt :129 delta 191–201 / 547–629 ns）；四处 includes Set 化 = §0-1（S1-A-4/X4-7 本体）拒列；三扫融合 = §0-2（S1-A-2+S3-A-3）拒列；正则 memo = §0-5（X1-1）拒列；C/R 轴维持 R15-A 穿越点（可操作重开第一优先不变）；S6-A-1 / S8-A-3 / R12-A §0-2..5 维持 | 无新候选 |
| `turn.ts` | **13a N_op 消费者宿主**（ownershipEscape 单扫 :101 698–808 ns@512 + collectEvidence 并集 :183 1915–2028 ns@512 + derived-gap :177–180 第三扫）+ 13e CF 平坦证人（delta −10~−31 ns@4096）；四遍历融合 = §0-3（S5-A-1/S7-A-2）拒列；并集 memo = §0-4（X1-1，fresh window 命中 0）拒列；F4b 并集精确性证人；R13-A 八子组件 / R12-A §0-11 / R16-A §0-2 / R20-A K 链正交性维持 | 无新候选 |
| `roller.ts` | **13d Q×D 二次位点宿主**（:31–34 首次剂量 ~1.78–1.80 ns/cmp 穿越 1054–1060；生产 Q=D=0）；F4c 合并精确性证人（去重+过滤+首现序）；confirmedDecisions Set 化 = §0-1 拒列；R17-A O(Q·D) 机制换名史闭环；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 / R20-A previous 面维持 | 无新候选 |
| `from-child.ts` | **F4e 生产残余普查源**（prescoreInputFromObservation 恒产 N_cl=1 / N_op=1 / W=O=0 / Cc、Rt 对角线；window Q=D=0、CF=2 源码级复验）；公开面截断 = §0-5 拒列（功能性变更）；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `gate-apply.ts` | 本轮零新角度（axis 13 是输入面字段维度，事件表侧五扫描+拷贝 278–284 ns 与 R23-A 积分账本维持）；O(E) 清单 / S11-A-2 / S8-A-1 / S2-A-4 / R15-A §0-1 / R23-A §0-1..5 维持 | 无新候选 |
| `child-tracking.ts` | 锚点/分解宿主复测同带；guard-3 skip 抽测 1961–2137 ns 复现（R18-A 7a 维持）；skip 守卫次序/享元 = R18-A §0-1/2 维持；observationFromChild 恒产单位形状（F4e 上游） | 无新候选 |
| `types.ts` | hashAssessment×3 地板复现 5.93–6.04 µs（axis 13 剂量不触 hash——哈希对输入面列表计数不敏感，敏感组件恰是 prescore/turn 消费者）；写侧 parse 326–360 ns；X1-1/S8-G-2 memo 禁维持；S2-A-5 / X1-2 / S5-H-3 / S1-A-8 / X0-5 维持 | 无新候选 |
| `gates.ts` | 本轮零新角度（gateFacts 是布尔面无列表维度；F4a 填充下 gate 恒 none 证人）；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（axis 13 全程 humanInput ≡ `{}`）；R21-A H 面轴不补铸；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 维持 | 无新候选 |
| `combined-score.ts` | 派单提示类 (i) 核对宿主：输入是标量对，无未扫列表维度（§0 提示类核对）；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |
| `config.ts` | 配置态矩阵复核宿主（§0 配置态 (b)：生产恒 DEFAULT；R22-A 双臂价维持）；X0-7 阈值冻结维持 | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方 grep 复验；派单提示类 (iii) 核对：导出消费者面无新流量）；S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R22-A 隔离三函数价维持（零 src/ 调用方复验）；恒等函数删除 = R22-A §0-5 拒列维持；index.ts 纯再导出零逻辑；惰性 import = R12-A §0-10 维持 | 无新候选 |

**对 R23-A 声明的裁决**：「切片规模轴族的最后一个耦合项（E×G）自此
有价」在其维度上成立；R23-A 结语量化的是**参数粒度的轴与轴间耦合**
——同一批输入面内部的字段粒度残余（列表字段计数从未独立剂量、二次
位点从未剂量定价、对角线外补充格）不在其中，本轮补上并闭合
（13a–13f 六组定价 + F4a–F4e 五组证人）。本轮全部新数据**只产出
归档证人与输入面字段格网的闭合，未产出可落地候选**。收口的预言力
第二十四遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R23-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F4a: 700/700 填充形状全维度不变；turn gate 恒 none、score === P
F4b: 54/122 精确首插并集 | F4c: 合并精确 | F4d: 40/40 位置翻转
F4e: 生产残余普查 N_cl=1 N_op=1 W=O=0 Cc/Rt 对角线 Q=D=0 CF=2
anchor: 16.9 / 15.5 / 15.3 / 13.8 / 14.5 µs/gate => ~69–84 µs/run
decomposition: hash×3=5925ns | validateEvent=2767ns | turn=1720ns
    | prescore×2=1344ns | 5 scans+copy=282ns (6.87 ns/event)
    | applyTrackingGate=5.95µs
spot: skip guard-3 1961ns (12.7%) | replay find#1 3188ns (20.6%)
13a N_op clean: 2071/1739/2042/4776ns => 6.03 ns/op; cross 331604
    ref-loaded@512 31693ns; consumers: escaped 749 | union 1926
    | prescore 1335ns
13b N_cl: 674/908/2637/16709ns => 31.38 ns/claim/scan; cross 21246
13c W×O: 727/1079/5656/168167ns => 5.09 ns/cmp; cross 627
13d Q×D: 2408/3638/15475/119943ns => 1.80 ns/cmp; cross 1054
13e CF: 1684 vs 1673ns (delta −10 @ 2048×)
13f: base 675 | Cc+256 866 (+191) | Rt+256 1222 (+547)
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F4a–F4e: 证人行逐字节同 run 1
anchor: 15.9 / 14.9 / 14.9 / 13.4 / 14.0 µs/gate => ~67–80 µs/run
decomposition: hash×3=6008ns | validateEvent=2806ns | turn=1688ns
    | prescore×2=1341ns | 5 scans+copy=284ns (6.92 ns/event)
    | applyTrackingGate=5.84µs
spot: skip guard-3 2137ns (14.3%) | replay find#1 3210ns (21.5%)
13a N_op clean: 2017/1731/2032/4699ns => 5.89 ns/op; cross 339311
    ref-loaded@512 30191ns; consumers: escaped 698 | union 1915
    | prescore 1375ns
13b N_cl: 666/892/2503/16218ns => 30.43 ns/claim/scan; cross 21905
13c W×O: 712/909/8169/265178ns => 8.04 ns/cmp; cross 499
13d Q×D: 2511/3702/15526/118589ns => 1.78 ns/cmp; cross 1060
13e CF: 1671 vs 1654ns (delta −18)
13f: base 663 | Cc+256 859 (+196) | Rt+256 1224 (+562)
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F4a–F4e: 证人行逐字节同 run 1
anchor: 16.0 / 15.0 / 15.2 / 13.9 / 14.6 µs/gate => ~70–80 µs/run
decomposition: hash×3=6042ns | validateEvent=2767ns | turn=1689ns
    | prescore×2=1332ns | 5 scans+copy=278ns (6.77 ns/event)
    | applyTrackingGate=5.92µs
spot: skip guard-3 2021ns (13.5%) | replay find#1 3166ns (21.1%)
13a N_op clean: 2032/1725/2042/4703ns => 5.91 ns/op; cross 338147
    ref-loaded@512 31194ns; consumers: escaped 808 | union 2028
    | prescore 1644ns
13b N_cl: 790/988/2537/16020ns => 29.80 ns/claim/scan; cross 22368
13c W×O: 740/947/6538/268140ns => 8.13 ns/cmp; cross 496
13d Q×D: 2579/3675/15509/119587ns => 1.79 ns/cmp; cross 1055
13e CF: 1698 vs 1667ns (delta −31)
13f: base 677 | Cc+256 878 (+201) | Rt+256 1306 (+629)
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立仿真运行 F4a–F4e 证人行逐字节一致（填充不变性/并集/合并/
scope 契约确定性重现）；锚点五格三次同带无悬崖；axis 13 六组剂量
三次运行区间紧（N_op 斜率区间宽 0.14 ns/op、N_cl 斜率区间宽
1.58 ns/claim、Q×D 系数区间宽 0.02 ns/cmp、穿越点区间见 §1 表；
13c W×O 系数区间 5.09–8.13 偏宽已如实记录，二次分类与穿越点量级
三次一致）。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S24-A-\*）。换名检查 5 项新记录见 §0（1–5），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件本轮**新增一条收口**：

**残余输入面列表维度（字段粒度普查）收口**：组合面两个公开输入结构
（PrescoreInput 8 列表字段 + TrackingWindow 6 列表字段）的每个列表
字段自此有价或有平坦证人——N_op 5.89–6.03 ns/op（穿越 ≈3.3×10⁵）、
N_cl 29.80–31.38 ns/claim/scan × 3 scans/turn（穿越 ≈2.1–2.2×10⁴）、
W×O ~5.09–8.13 ns/cmp（穿越 496–627，生产流量 0）、Q×D
~1.78–1.80 ns/cmp（穿越 1054–1060，生产流量 0）、CF 平坦引用、
对角线外 Cc/Rt 为 R15-A includes 家族纹理。重开条件：(a) 生产
from-child 路径给 writePaths/ownedPaths 或 unresolvedDecisions/
confirmedDecisions 供上非零流量且规模逼近 10²–10³（届时 W×O / Q×D
悬崖实体化，与 C 轴一并按 S1-A-4/X4-7 家族程序重审 Set 化）；
(b) claims / toolSituations 生产计数增长 ≥2 个量级（届时 N_cl 三扫
乘数先撞 S1-A-2/S3-A-3、N_op 四遍历先撞 S5-A-1/S7-A-2 融合家族）；
(c) contextFacts 或 packet 面出现按条目消费者（CF 平坦证人失效，
需重定价）。**可操作重开第一优先不变**：C 轴（~2.5 个量级，R15-A；
S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）——本轮两处二次位点
穿越点数值更近但生产流量恰 0，仅作输入面格网闭合记录。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf24a01`–`0xf24a02`。

```ts
/**
 * R24-A deterministic anchor + decomposition + NEW-axis simulation
 * (twenty-fourth pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R23-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R23-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation, 5-scan
 *       O(E) inventory), plus compact spot re-confirmations of the R18-A
 *       skip pricing (guard-3) and the R19-A replay pricing (find#1);
 *   (c) NEW axis 13: the RESIDUAL INPUT-FACE LIST DIMENSIONS census.
 *       Twenty-three passes dosed the slice's public input faces at
 *       parameter granularity: prescoreInput C/R (R15-A), payload L/V/S
 *       (R16-A), window openMinors M + previous chain K (R20-A),
 *       humanInput H_L/H_m/H_N (R21-A), analysis T (R22-A), table E
 *       (R14-A/R17-A), gates G + GxE (R16-A/R23-A). But at FIELD
 *       granularity the list-typed input fields were never all dosed:
 *         - N_op: window/prescore toolSituations COUNT (production <= 1;
 *           R16-A V dosed the per-tool ref lists at N_op = 1; R22-A T
 *           dosed operations count ONLY through the zero-flow analysis
 *           face, never through the production turn pipeline where the
 *           count feeds 2x3 prescore predicates, the turn ownershipEscape
 *           scan and the collectEvidence union);
 *         - N_cl: prescore claims COUNT (production <= 1 by construction,
 *           claims = [summary]; R16-A S dosed claim BYTES at N_cl = 1);
 *         - W x O: scopeOutcome writePaths x ownedPaths.includes quadratic
 *           (production W = O = 0; the site's Set-ization is the
 *           S1-A-4/X4-7 family, mechanism judged, never dose-priced);
 *         - Q x D: roller unresolvedQuestions x confirmedDecisions.includes
 *           quadratic (production Q = D = 0; rename-checked by R17-A
 *           section 0-2 as the family's fourth relocation point, never
 *           dose-priced);
 *         - CF: window contextFacts (production = 2; structurally flat:
 *           the slice only carries the array reference);
 *         - off-diagonal Cc / Rt: completedChecks beyond requiredChecks
 *           and retainedConstraintIds beyond constraints (R15-A dosed the
 *           diagonals Cc = R, Rt = C only).
 *       This axis doses each residue deterministically, decomposes the
 *       N_op slope by consumer site, fits the two quadratic sites, prices
 *       the off-diagonal supplement cells, proves the flat-reference
 *       claim for CF, and derives 10 ms/run crossings for the table.
 *
 * Archival-fact fuzz (deterministic, seeded):
 *   F4a: padding invariance. Padding claims with non-matching strings,
 *        toolSituations with clean zero-ref tools, ownedPaths with extra
 *        owned entries, completedChecks with extra completed ids and
 *        confirmedDecisions with disjoint decisions never flips any
 *        prescore dimension outcome, P, score or gate kind.
 *   F4b: collectEvidence union exactness. Overlapping evidence/artifact/
 *        hash ids across N_op tools dedupe to the exact first-insertion
 *        union.
 *   F4c: roller Q/D merge exactness. Disjoint Q survive in first-
 *        occurrence order; confirmed questions are filtered; duplicates
 *        collapse; confirmedDecisions dedupe.
 *   F4d: W/O scope semantics. All-owned PASS; one unowned path flips FAIL
 *        at every position; extra owned entries never flip.
 *   F4e: production-shape invariance. The residual fields are all zero or
 *        unit in the from-child production path, and the anchor cells'
 *        e2e outputs are byte-identical with and without the axis-13
 *        machinery loaded.
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf24a01..0xf24a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore, type PrescoreInput } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import type { ToolSituation } from "/workspace/src/tracking/types.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary = opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran";
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

/* ============================================================
 * Axis-13 fixture builders (deterministic; no RNG in the doses).
 * ============================================================ */
function cleanTool(index: number, refs: number): ToolSituation {
  const evidenceIds: string[] = [];
  for (let r = 0; r < refs; r += 1) evidenceIds.push(`evd_pad_${index}_${r}`);
  return {
    name: `tool_${index}`,
    exitCode: 0,
    wrote: false,
    escaped: false,
    artifactIds: [],
    evidenceIds,
    hashes: []
  };
}

/** Production-texture prescore input (mirrors prescoreInputFromObservation
 *  for a PASSED tester child with contract(3)) that the doses pad. */
function basePrescoreInput(): PrescoreInput {
  const constraints = [
    { id: "con_0", text: "constraint 0 text", kind: "constraint" as const, mandatory: true as const },
    { id: "con_1", text: "constraint 1 text", kind: "constraint" as const, mandatory: true as const },
    { id: "con_2", text: "constraint 2 text", kind: "constraint" as const, mandatory: true as const }
  ];
  return {
    claims: ["tests ran"],
    toolSituations: [
      {
        name: "task-result",
        exitCode: 0,
        wrote: true,
        escaped: false,
        artifactIds: ["art_1"],
        evidenceIds: ["evd_1"],
        hashes: []
      }
    ],
    writePaths: [],
    ownedPaths: [],
    requiredChecks: ["chk_0"],
    completedChecks: ["chk_0"],
    constraints,
    retainedConstraintIds: ["con_0", "con_1", "con_2"],
    progressed: true,
    stalledTurns: 0,
    independentEvidence: true
  };
}

function turnInputFor(prescoreInput: PrescoreInput, window?: {
  readonly unresolvedDecisions?: readonly string[];
  readonly confirmedDecisions?: readonly string[];
  readonly contextFacts?: readonly string[];
}): Parameters<typeof runTrackingTurn>[0] {
  return {
    window: {
      contextFacts: window?.contextFacts ?? ["role tester", "task tsk_bench"],
      toolSituations: prescoreInput.toolSituations,
      constraints: prescoreInput.constraints,
      unresolvedDecisions: window?.unresolvedDecisions ?? [],
      confirmedDecisions: window?.confirmedDecisions ?? [],
      openMinors: []
    },
    prescoreInput,
    humanInput: {},
    gateFacts: { deterministicFail: false }
  };
}

function verdictVector(result: ReturnType<typeof computePrescore>): string {
  return result.dimensions.map((d) => `${d.id}:${d.outcome}`).join("|");
}

/* ============================================================
 * F4a: padding invariance (seeded shapes; counts deterministic).
 * ============================================================ */
{
  const rng = mulberry32(0xf24a01);
  let trials = 0;
  let invariant = 0;
  for (let trial = 0; trial < 700; trial += 1) {
    const base = basePrescoreInput();
    const baseResult = computePrescore(base);
    const nCl = 1 + Math.floor(rng() * 64);
    const nOp = 1 + Math.floor(rng() * 64);
    const wo = Math.floor(rng() * 32);
    const ccPad = Math.floor(rng() * 32);
    const rtPad = Math.floor(rng() * 32);
    const claims = [...base.claims];
    for (let i = 1; i < nCl; i += 1) claims.push(`padding claim ${i} with no anchor word`);
    const tools = [...base.toolSituations];
    for (let i = 1; i < nOp; i += 1) tools.push(cleanTool(i, 0));
    const owned: string[] = [];
    for (let i = 0; i < wo; i += 1) owned.push(`src/owned_${i}.ts`);
    const writes = [...owned];
    const completed = [...base.completedChecks];
    for (let i = 0; i < ccPad; i += 1) completed.push(`chk_extra_${i}`);
    const retained = [...base.retainedConstraintIds];
    for (let i = 0; i < rtPad; i += 1) retained.push(`con_extra_${i}`);
    const padded: PrescoreInput = {
      ...base,
      claims,
      toolSituations: tools,
      writePaths: writes,
      ownedPaths: owned,
      completedChecks: completed,
      retainedConstraintIds: retained
    };
    const paddedResult = computePrescore(padded);
    const ok =
      verdictVector(paddedResult) === verdictVector(baseResult) &&
      paddedResult.P === baseResult.P &&
      paddedResult.quality === baseResult.quality &&
      paddedResult.coverage === baseResult.coverage &&
      paddedResult.cappedByHardFail === baseResult.cappedByHardFail;
    if (ok) invariant += 1;
    // turn-level: gate kind must stay none and score must stay P
    const turn = runTrackingTurn(
      turnInputFor(padded, {
        unresolvedDecisions: Array.from({ length: Math.floor(rng() * 16) }, (_, i) => `q_${i}`),
        confirmedDecisions: Array.from({ length: Math.floor(rng() * 16) }, (_, i) => `d_${i}`)
      })
    );
    check(
      `F4a turn gate stays none under padding [trial ${trial}]`,
      turn.gate.kind === "none" && turn.score === baseResult.P
    );
    trials += 1;
  }
  check("F4a all padded prescores invariant", invariant === trials, `${invariant}/${trials}`);
  console.log(
    `F4a: ${trials} seeded padded shapes (claims<=64, tools<=64, W=O<=31, Cc/Rt pad<=31, ` +
      `Q/D<=15): dimension vector, P, quality, coverage, cappedByHardFail all invariant ` +
      `${invariant}/${trials}; turn gate stays none with score === P in every trial`
  );
}

/* ============================================================
 * F4b: collectEvidence union exactness at N_op (via public turn output).
 * ============================================================ */
{
  const base = basePrescoreInput();
  const tools = [...base.toolSituations];
  // overlapping refs: tool i carries evd_dup_(i % 7) plus its own unique id
  for (let i = 1; i <= 40; i += 1) {
    tools.push({
      name: `tool_${i}`,
      exitCode: 0,
      wrote: false,
      escaped: false,
      artifactIds: [`art_dup_${i % 5}`],
      evidenceIds: [`evd_dup_${i % 7}`, `evd_uniq_${i}`],
      hashes: []
    });
  }
  const turn = runTrackingTurn(turnInputFor({ ...base, toolSituations: tools }));
  const expected: string[] = [];
  const seen = new Set<string>();
  for (const tool of tools) {
    for (const id of tool.evidenceIds) if (!seen.has(id)) { seen.add(id); expected.push(id); }
    for (const id of tool.artifactIds) if (!seen.has(id)) { seen.add(id); expected.push(id); }
    for (const hash of tool.hashes) if (!seen.has(hash)) { seen.add(hash); expected.push(hash); }
  }
  check(
    "F4b collectEvidence = exact first-insertion union",
    JSON.stringify(turn.summary.evidenceRefs) === JSON.stringify(expected),
    `${turn.summary.evidenceRefs.length} vs ${expected.length}`
  );
  console.log(
    `F4b: 41-tool overlapping fixture: summary.evidenceRefs is the exact first-insertion ` +
      `union (${expected.length} of ${41 * 3 - 1} raw refs; duplicates collapsed) -- the ` +
      `N_op consumer is a faithful set union, order deterministic`
  );
}

/* ============================================================
 * F4c: roller Q/D merge exactness.
 * ============================================================ */
{
  const base = basePrescoreInput();
  const questions = ["q_a", "q_b", "q_a", "q_c", "q_d"];
  const confirmed = ["q_b", "q_d", "q_b", "dec_x"];
  const turn = runTrackingTurn(
    turnInputFor(base, { unresolvedDecisions: questions, confirmedDecisions: confirmed })
  );
  check(
    "F4c unresolved = dedup minus confirmed, first-occurrence order",
    JSON.stringify(turn.summary.unresolvedQuestions) === JSON.stringify(["q_a", "q_c"])
  );
  check(
    "F4c confirmedDecisions dedupe preserving order",
    JSON.stringify(turn.summary.confirmedDecisions) === JSON.stringify(["q_b", "q_d", "dec_x"])
  );
  // disjoint worst case: nothing filtered
  const disjoint = runTrackingTurn(
    turnInputFor(base, {
      unresolvedDecisions: ["q_1", "q_2", "q_3"],
      confirmedDecisions: ["d_1", "d_2", "d_3"]
    })
  );
  check(
    "F4c disjoint Q survives whole",
    JSON.stringify(disjoint.summary.unresolvedQuestions) === JSON.stringify(["q_1", "q_2", "q_3"])
  );
  console.log(
    "F4c: roller Q/D merge exact -- dedup + confirmed-filter + first-occurrence order " +
      "(overlap fixture ['q_a','q_c'] / ['q_b','q_d','dec_x']; disjoint fixture survives whole)"
  );
}

/* ============================================================
 * F4d: W/O scope semantics.
 * ============================================================ */
{
  const rng = mulberry32(0xf24a02);
  const base = basePrescoreInput();
  const owned = Array.from({ length: 32 }, (_, i) => `src/owned_${i}.ts`);
  const allOwned = computePrescore({ ...base, writePaths: [...owned], ownedPaths: owned });
  check(
    "F4d all-owned scope PASS",
    allOwned.dimensions.some((d) => d.id === "scope-safety" && d.outcome === "PASS")
  );
  for (let trial = 0; trial < 40; trial += 1) {
    const pos = Math.floor(rng() * 33);
    const writes = [...owned];
    writes.splice(pos, 0, "src/UNOWNED.ts");
    const result = computePrescore({ ...base, writePaths: writes, ownedPaths: owned });
    check(
      `F4d unowned at position ${pos} flips FAIL [trial ${trial}]`,
      result.dimensions.some((d) => d.id === "scope-safety" && d.outcome === "FAIL")
    );
  }
  const extraOwned = computePrescore({
    ...base,
    writePaths: [...owned],
    ownedPaths: [...owned, "src/extra_0.ts", "src/extra_1.ts"]
  });
  check(
    "F4d extra owned entries never flip",
    extraOwned.dimensions.some((d) => d.id === "scope-safety" && d.outcome === "PASS")
  );
  console.log(
    "F4d: W/O scope semantics exact -- all-owned PASS; one unowned path flips FAIL at " +
      "every seeded position (40/40); extra owned entries never flip"
  );
}

/* ============================================================
 * F4e: production-shape residue census (from-child path).
 * ============================================================ */
{
  const mk = mkChild("PASSED");
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const pi = prescoreInputFromObservation(obs);
  check("F4e production N_cl <= 1", pi.claims.length === 1);
  check("F4e production N_op <= 1", pi.toolSituations.length === 1);
  check("F4e production W = 0", pi.writePaths.length === 0);
  check("F4e production O = 0", pi.ownedPaths.length === 0);
  check("F4e production Cc diagonal", pi.completedChecks.length === pi.requiredChecks.length);
  check(
    "F4e production Rt diagonal",
    pi.retainedConstraintIds.length === pi.constraints.length
  );
  // from-child window: Q = D = 0, CF = 2 (source-level constants, re-checked live)
  const decision = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: String(RUN_ID) });
  check("F4e production fixture applies", decision.apply === true);
  console.log(
    "F4e: production residue census -- N_cl = 1, N_op = 1, W = O = 0, Cc/Rt on the " +
      "R15-A diagonals, window Q = D = 0, CF = 2: every axis-13 dimension is zero or " +
      "unit in the production from-child path (the doses below are public-API-face " +
      "pricing, not production re-pricing)"
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R23-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    anchorUs.push(one * 1e3);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition (R12-A..R23-A methodology) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)}) => ~${((scans * 1e6) / 41).toFixed(2)} ns/event at E=41`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const mkGT = mkChild("FAILED", { taskId: "tsk_replay_gt" });
    const obsGT = observationFromChild(mkGT.child, mkGT.spec, mkGT.contract);
    const decGT = assessChildObservation({
      observation: obsGT,
      episodeId: "ep_bench",
      runId: String(RUN_ID)
    });
    if (!decGT.apply) throw new Error("replay fixture must apply");
    const hashGT = hashAssessment(decGT.assessment);
    const seqGT = nextTrackingSeq(events41);
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: decGT.assessment,
      assessmentHash: hashGT,
      expectedSeq: seqGT,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: decGT.assessment,
        assessmentHash: hashGT,
        expectedSeq: seqGT,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }
}

/* ============================================================
 * NEW axis 13: residual input-face list dimensions.
 * ============================================================ */
{
  console.log("--- NEW axis 13a: N_op dose (toolSituations count through public runTrackingTurn) ---");
  const base = basePrescoreInput();
  const doses = [1, 8, 64, 512] as const;
  const turnAtNop: Array<{ n: number; us: number }> = [];
  // pre-warm every dose shape before timing any cell (R11-A PIC/order family)
  for (const variant of ["clean", "ref-loaded"] as const) {
    for (const n of doses) {
      const tools = [...base.toolSituations];
      for (let i = 1; i < n; i += 1) tools.push(cleanTool(i, variant === "ref-loaded" ? 2 : 0));
      const input = turnInputFor({ ...base, toolSituations: tools });
      for (let w = 0; w < 300; w += 1) void runTrackingTurn(input);
    }
  }
  for (const variant of ["clean", "ref-loaded"] as const) {
    const row: string[] = [];
    for (const n of doses) {
      const tools = [...base.toolSituations];
      for (let i = 1; i < n; i += 1) tools.push(cleanTool(i, variant === "ref-loaded" ? 2 : 0));
      const input = turnInputFor({ ...base, toolSituations: tools });
      const reps = n > 100 ? 5000 : 20000;
      const one = bench(() => void runTrackingTurn(input), reps);
      row.push(`N=${String(n).padStart(3)}: ${(one * 1e6).toFixed(0)}ns`);
      if (variant === "clean") turnAtNop.push({ n, us: one * 1e3 });
    }
    console.log(`  ${variant.padEnd(10)} ${row.join("  ")}`);
  }
  // slope from the N=8..512 legs (N=1 cell carries residual PIC texture)
  const first = turnAtNop[1]!;
  const last = turnAtNop[turnAtNop.length - 1]!;
  const nopSlope = ((last.us - first.us) * 1e3) / (last.n - first.n);
  // consumer decomposition at N=512 clean (isolated single scans)
  {
    const tools = [...base.toolSituations];
    for (let i = 1; i < 512; i += 1) tools.push(cleanTool(i, 0));
    const frozen: readonly ToolSituation[] = tools;
    const tSome = bench(() => void frozen.some((tool) => tool.escaped), 20000);
    const tEvidence = bench(() => {
      const refs = new Set<string>();
      for (const tool of frozen) {
        for (const id of tool.evidenceIds) refs.add(id);
        for (const id of tool.artifactIds) refs.add(id);
        for (const hash of tool.hashes) refs.add(hash);
      }
      void [...refs];
    }, 20000);
    const tPrescoreDose = bench(
      () => void computePrescore({ ...base, toolSituations: frozen }),
      20000
    );
    console.log(
      `  consumer decomposition at N_op=512 clean: one escaped-scan ${(tSome * 1e6).toFixed(0)}ns | ` +
        `collectEvidence-shape union ${(tEvidence * 1e6).toFixed(0)}ns | ` +
        `computePrescore whole ${(tPrescoreDose * 1e6).toFixed(0)}ns (x2/turn)`
    );
  }
  const crossNop = (10000 / 5 - first.us) / (nopSlope / 1e3);
  console.log(
    `  => N_op slope ~= ${nopSlope.toFixed(2)} ns/op (clean, turn-level); production N_op = 1; ` +
      `10 ms/run crossing (5 turns/run) N_op ~= ${Math.round(crossNop)}`
  );

  console.log("--- NEW axis 13b: N_cl dose (claims count through public computePrescore) ---");
  const clAt: Array<{ n: number; us: number }> = [];
  for (const n of doses) {
    const claims = [...base.claims];
    for (let i = 1; i < n; i += 1) claims.push(`padding claim ${i} with no anchor word`);
    const input: PrescoreInput = { ...base, claims };
    const reps = n > 100 ? 10000 : 50000;
    const one = bench(() => void computePrescore(input), reps);
    clAt.push({ n, us: one * 1e3 });
  }
  const clSlope =
    ((clAt[clAt.length - 1]!.us - clAt[0]!.us) * 1e3) / (clAt[clAt.length - 1]!.n - clAt[0]!.n);
  // consumers: evidenceOutcome some x2 prescores + turn derived-gap some = 3 scans/turn
  const clCross = Math.round(1e7 / (5 * 3 * clSlope));
  console.log(
    `  computePrescore at N_cl {1,8,64,512}: ${clAt.map((p) => `${(p.us * 1e3).toFixed(0)}ns`).join(" / ")} ` +
      `=> ~${clSlope.toFixed(2)} ns/claim per scan (non-matching worst case; 3 scans/turn = ` +
      `2 prescores + derived-gap); production N_cl = 1; 10 ms/run crossing N_cl ~= ${clCross}`
  );

  console.log("--- NEW axis 13c: W x O quadratic (scopeOutcome writePaths x ownedPaths.includes) ---");
  const woDoses = [4, 16, 64, 256] as const;
  const woAt: Array<{ n: number; us: number }> = [];
  for (const n of woDoses) {
    const owned = Array.from({ length: n }, (_, i) => `src/owned_${i}.ts`);
    const input: PrescoreInput = { ...base, writePaths: [...owned], ownedPaths: owned };
    const reps = n > 100 ? 5000 : 50000;
    const one = bench(() => void computePrescore(input), reps);
    woAt.push({ n, us: one * 1e3 });
  }
  // fit c in T(n) = T0 + c * n^2 / 2 (each hit found at its own index => n(n+1)/2 comparisons)
  const woBase = clAt[0]!.us;
  const woC =
    ((woAt[woAt.length - 1]!.us - woBase) * 1e3) /
    ((woAt[woAt.length - 1]!.n * (woAt[woAt.length - 1]!.n + 1)) / 2);
  const woCross = Math.sqrt((2 * (10000 / 10 - woBase) * 1e3) / woC);
  console.log(
    `  computePrescore at W=O {4,16,64,256}: ${woAt.map((p) => `${(p.us * 1e3).toFixed(0)}ns`).join(" / ")} ` +
      `=> ~${woC.toFixed(2)} ns/comparison (all-owned self-index texture, x2/turn); production W=O=0; ` +
      `10 ms/run crossing W=O ~= ${Math.round(woCross)}`
  );

  console.log("--- NEW axis 13d: Q x D quadratic (roller unresolved x confirmed.includes) ---");
  const qdDoses = [4, 16, 64, 256] as const;
  const qdAt: Array<{ n: number; us: number }> = [];
  for (const n of qdDoses) {
    const questions = Array.from({ length: n }, (_, i) => `q_${i}`);
    const confirmed = Array.from({ length: n }, (_, i) => `d_${i}`); // disjoint => full-miss scans
    const input = turnInputFor(base, { unresolvedDecisions: questions, confirmedDecisions: confirmed });
    const reps = n > 100 ? 2000 : 20000;
    const one = bench(() => void runTrackingTurn(input), reps);
    qdAt.push({ n, us: one * 1e3 });
  }
  const qdBase = turnAtNop[0]!.us;
  const qdC =
    ((qdAt[qdAt.length - 1]!.us - qdBase) * 1e3) /
    (qdAt[qdAt.length - 1]!.n * qdAt[qdAt.length - 1]!.n);
  const qdCross = Math.sqrt(((10000 / 5 - qdBase) * 1e3) / qdC);
  console.log(
    `  runTrackingTurn at Q=D {4,16,64,256} disjoint: ${qdAt.map((p) => `${(p.us * 1e3).toFixed(0)}ns`).join(" / ")} ` +
      `=> ~${qdC.toFixed(2)} ns/comparison (full-miss worst case, roller once/turn + hashSummary-free ` +
      `fresh window); production Q=D=0; 10 ms/run crossing Q=D ~= ${Math.round(qdCross)}`
  );

  console.log("--- NEW axis 13e: CF flat-reference witness (contextFacts) ---");
  {
    const small = turnInputFor(base, { contextFacts: ["role tester", "task tsk_bench"] });
    const bigFacts = Array.from({ length: 4096 }, (_, i) => `fact ${i}`);
    const big = turnInputFor(base, { contextFacts: bigFacts });
    const oneSmall = bench(() => void runTrackingTurn(small), 20000);
    const oneBig = bench(() => void runTrackingTurn(big), 20000);
    const smallOut = runTrackingTurn(small);
    const bigOut = runTrackingTurn(big);
    check(
      "F4e CF carried by reference (no per-item consumer)",
      bigOut.summary.operations === base.toolSituations && smallOut.gate.kind === bigOut.gate.kind
    );
    console.log(
      `  turn at CF=2: ${(oneSmall * 1e6).toFixed(0)}ns vs CF=4096: ${(oneBig * 1e6).toFixed(0)}ns ` +
        `(delta ${((oneBig - oneSmall) * 1e6).toFixed(0)}ns for 2048x the items) -- structurally flat: ` +
        `the slice only moves the array reference (packet carry is wake-arm-only and also by reference)`
    );
  }

  console.log("--- NEW axis 13f: off-diagonal Cc / Rt supplement cells (R15-A diagonal extension) ---");
  {
    // R15-A dosed R with Cc = R and C with Rt = C. Here: fixed R=1 / C=3 production
    // counts, pad the *partner* lists so every includes hit scans past the padding.
    const ccPad = Array.from({ length: 256 }, (_, i) => `chk_extra_${i}`);
    const inputCc: PrescoreInput = { ...base, completedChecks: [...ccPad, ...base.completedChecks] };
    const rtPad = Array.from({ length: 256 }, (_, i) => `con_extra_${i}`);
    const inputRt: PrescoreInput = {
      ...base,
      retainedConstraintIds: [...rtPad, ...base.retainedConstraintIds]
    };
    const oneBase = bench(() => void computePrescore(base), 50000);
    const oneCc = bench(() => void computePrescore(inputCc), 50000);
    const oneRt = bench(() => void computePrescore(inputRt), 50000);
    const ccOut = computePrescore(inputCc);
    const rtOut = computePrescore(inputRt);
    check("13f Cc padding keeps coverage PASS", verdictVector(ccOut) === verdictVector(computePrescore(base)));
    check("13f Rt padding keeps constraint PASS", verdictVector(rtOut) === verdictVector(computePrescore(base)));
    console.log(
      `  computePrescore base ${(oneBase * 1e6).toFixed(0)}ns | Cc+256 prefix pad ${(oneCc * 1e6).toFixed(0)}ns ` +
        `(delta ${((oneCc - oneBase) * 1e6).toFixed(0)}ns = R=1 x 256-miss + turn's derived gap scan) | ` +
        `Rt+256 prefix pad ${(oneRt * 1e6).toFixed(0)}ns (delta ${((oneRt - oneBase) * 1e6).toFixed(0)}ns = C=3 x 256-miss) ` +
        `-- off-diagonal texture is the same includes-scan family R15-A dosed on the diagonal; ` +
        `production sits at the diagonal origin`
    );
  }

  console.log(
    "  => axis 13 verdict: every residual list dimension is zero-or-unit in production, " +
      "linear or quadratic with crossings 3-5 orders beyond production scale, and every " +
      "amortization path (Set-ize the four includes sites, fuse the prescore scans, dedupe " +
      "the ownershipEscape scan, intern the union) lands on S1-A-4 / X4-7 / S5-A-1 / " +
      "S7-A-2 / S1-A-2 -- no candidate"
  );
}

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
