MODEL_SLUG=claude-fable-5-thinking-xhigh

# R24-J：数据面切片（cluster / privacy / preferences / episode / persist / track / context / feedback）第二十四遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 24 / R24-J
**基线:** `cursor/sota-persistent-opt-83a1` @ `74a0f93`（独占 tip，含 R24-A..R24-I 九区摄取——全部空枚举，G 收 RUNLIM、H 收 operand-residency 普查、I 收 adaptation-state coordinate——及 Round 25 开卷 / R25-A/B 派发与 R24-I 轴对 D/E/J 的移植禁令注记；本报告全部测量在派发 tip `5c0608f` 上完成，H/I 摄取皆为 docs-only 提交、`src/` 其间零改动，按指令快进重定至新 tip 后测量与结论逐字有效）
**分支:** `cursor/r24-j-persist-twenty-fourth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第二十四遍枚举为空：无达门槛候选、零新 ID、零生产代码改动、切片
维持关闭（MORE_OPTIMA=no）。**
切片 29 个文件（3655 行，本轮 `wc -l` 复核与 R9-J..R23-J 一致；
3+3+7+5+2+4+2+3 清单逐目录确认恰 29 文件）自 J1 落地（`fb41417`）
以来**逐字节未变**（本轮核对 `git diff fb41417..HEAD -- <29 文件>`
输出为 0 行——J1 之后经 R2-J..R23-J 与本轮共二十三遍复查累计零后续
代码改动）。R23-J 基线 tip（`350dd2d`）之后 `git log 350dd2d..HEAD
-- src/` **零提交**（其间 15 个提交全部为 `docs/reports/sota-opt/`
文档——R23-I/R23-J 摄取、Round 23 收口、R24-A..I 各波摄取/派发、
Round 25 开卷、CALLB 与 R24 各轴移植禁令；`--name-only` 全集仅
PROGRESS / README / PLAN / R23-\* / R24-\* 报告与计划文件）
⇒ 生产调用图对本切片
**可证不变（最强形式，第十六连）**。
按指令在本 VM 重锚配置态 I/O 地板矩阵（三遍 × 各 3 次运行，§1.3）：
preferences `saveToDisk` 非空策略 **130.0–225.0µs/写**（50 观察档，
R23-J 128.0–220.6µs 带重叠）～ **421.7–547.8µs/写**（500 观察档，
R23-J 410.9–520.2µs 带重叠，高缘 +2.9µs 超 R22-J 高缘为亚噪声、
深居 R20-J 420.2–571.8µs 带内）；jsonl 追加 fsync=false
**59.4–67.4µs**（R22-J 59.9–67.5µs 带重叠，下缘 −0.5µs 亚噪声）/
fsync=true **201.2–310.1µs**（R22-J 204.0–288.2µs 带重叠，下缘
−2.8µs 亚噪声、高缘落 R21-J 251.5–381.5µs 带内）；配置态删除级联
N=200、20 命中 match=**620.9–761.0µs**（R23-J 622.2–787.1µs 带
重叠，下缘 −1.3µs 亚噪声）/ no-match=**259.5–316.8µs**（R23-J
263.7–308.2µs 带重叠，两缘均落 R20-J 267.0–371.5 / R21-J
263.2–301.3µs 邻带噪声内）；`buildProjectContextIndex`
**40.0–41.5µs/构建**（R23-J 40.2–40.9 / R22-J 40.4–41.3µs 带
重叠）；track plan CPU 计数器 **1.02–1.11µs/plan**（R23-J
1.09–1.12µs 带重叠）/ 默认 randomUUID **2.49–2.68µs/plan**（R23-J
2.60–2.66µs 带重叠）——CPU 三格与 I/O 五格全部与历史带重叠，
**I/O 支配判据第二十四次成立**。
J1 落地代码本轮 `scripts/r1j-equivalence-sim.ts` 重跑全绿（**2468 项
逐位检查，reference 10118.8ms → current 3.5ms = 2886.8×**）。
第二十四遍在完整排除表（54 个已闭 S\*-J-\* ID + J1 + R10-J..R23-J
十四轮空枚举收口）之上开一条历轮从未拉伸的新完备性轴：**偏好值
联合类型类组成面（VALCLASS）**——切片唯一的多态载荷字段
`PreferenceObservation.value: string | number | boolean` 的**类**在
二十三轮里从未成为受控变量：store 锚（R12-J 附录 A 谱系）只在
**不计时种子背景**里交替 string/int 而计时写尾恒全 string、R20-J
C2 只在 loop-eval 侧带过一个**固定**混合背景（其报告 L718–724 为
直接证据——是背景不是剂量）、而**非整双精度类**（JSON.stringify
的 shortest-round-trip dtoa / JSON.parse 的浮点解析 / J1 valueKey
的 String(double)）在任何 J fixture 中从未出现过。该类选择
`rebuildViews` 的真实分支族（explicit 锚定 / 数值同值权重累加 /
inferred-vs-inferred 数值加权均值合并 / 非数值 keep-existing）与
真实引擎值路径（dtoa、浮点解析、typeof 分派）。生产类通量 grep
再证：explicit 数值/布尔观察经一条 CLI 命令可达（`pref correct`
的 `parsePreferenceValue` 把 "true"/"false" → boolean、有限数值 →
number）；inferred 观察仅由 `learning/from-episode.ts` 写入且值恒
为 **string**（primary-model id）⇒ inferred 数值累加/合并分支族
**今日零生产通量**；boolean 类被 `track/clarify.ts`
`habitsFromPreferences` 生产**消费**（require-tests / prefer-review /
ask-before-write）。本轮固定 N、记录次序与 createdAt 序列
（R19-J）、explicit 配比 30%（R20-J）、主体/键基数（R21-J KFAN）、
每剂量单字面量构造器单键序（R22-J SHAPEK；剂量间字段表示的变化
即被测变量本身，剂量内恒单态 K=1）、调用排程（R23-J CALLB）、
每宽度层内序列化字节总量（R16-J），只变 value 的类（七剂量：4 字符
层 STR4/INT4/DBL4 逐位等字节、17 字符层 STR17/DBL17 逐位等字节、
BOOL/MIX 申报字节；每主体值模式 [v0,v0,v1] 循环使数值剂量三分支
全部点火，内联见证断言），对切片四类站点各测一胞（两次独立全网格
运行 + 逐胞确定性/账户/分支点火见证全过）：(P1) **store 写路径**
（种子 500 + 计时 200 写，MEM 腿 filter+rebuild vs DISK 腿
+stringify+写盘，另加裸序列化分解）——MEM 腿类摆幅 ≤7.4µs/写
（分支族价 1–4µs 亚噪声）；DISK 腿 DBL17 两轮皆最慢
（−STR17 = +11.9 | +35.0µs/写，方向稳定、幅度居 S0a 噪声带内）；
裸分解干净显影 **dtoa 税 ≈55–58ns/值、浮点解析税 ≈44–51ns/值**
（700 值 store 上 stringify DBL17−STR17 = +38.5 | +40.9µs、parse
+31.0 | +35.5µs，**字节逐位相等**下两轮同向）；宽度钉死见证全过
（w4 三剂量文件 145244 B 逐位相等、w17 两剂量 154344 B 逐位
相等）；(P2) 水合（configurePreferencePersistence = readFileSync +
JSON.parse + rebuild，700 观察）——DBL17−STR17 = +36.0 | +39.3µs/
水合，两轮同向，每进程恰一次；(P3) J1 站点 `evaluatePreferenceLoop`
类剂量（N=6000）——DBL17−STR4 = **+0.341 | +0.310ms/eval（~24%）**
= valueKey `${typeof v}:${String(v)}` 的 String(double) dtoa 显影，
BOOL +0.08–0.11ms、INT4 +0.01–0.02ms；四剂量输出 report **逐位
相同**（loop-eval 只做同一性比较 ⇒ 类同构，两轮成立）；(P4)
boolean-habit 配置态消费（生产 shape：project 对 3 键 + user
default 1 键 explicit boolean）——配置腿 **526.9–568.6ns/call** vs
空店 75.7–101.9ns，每 tracked run 恰一次。四胞全部得出「无候选」
（P1/P2 响应真实但**输入域决定**——值的类是用户数据，store 侧任何
「改类避 dtoa」= 改落盘字节的数据面发散（S4-G-6/S6-E-4 族）或
X1-1 缓存，且最坏腿 +40µs/写落在 320–430µs I/O 地板上的
once-per-command 路径，距落地线 3–4 个数量级；inferred 数值分支族
零生产通量 + MEM 腿亚噪声；P3 响应最大但站点零生产调用方（R10-J
面）且满剂量是 100% double N=6000 的双重幻想输入；P4 配置腿
0.53–0.57µs = once-per-run 亚 µs 拒绝类），按纪律不铸任何
S24-J-\* ID。指令点名的换名重提检查（J1 / S5-J-3 / S6-J-1 /
S8-J-2 钉死项读码逐条在位；CALLB / SHAPEK / KFAN / R20-J 输入类
组成未重铸——排程/形状多态/键扇出/记录角色类全部钉死为常数并入
账户断言，VALCLASS 与四者的区分为 §2.1 显式论证负担；RUNLIM /
CKMIX / OCCX / JB / R24-A 列维普查 / R24-B M=1 / R24-D
approval-authority / R24-F serialization-boundary provenance /
R23-I `.pi/` 树 / R23-A G×E / R23-B batch arrangement / ORDX /
SEEDX / YMIX / SCALEX / FITQ / NVG / GS 一个未移植；file-lock
retryMs 未补铸——`withExclusiveFileLock` 生产调用方 grep 再证全在
四个他区文件，J 内零位点）逐条复核全部原样在位（§0）。`pnpm gate`
全绿（1169 测试 / 78 套件 / 1168 通过 / 1 跳过 / 0 失败）。诚实
结论：枚举为空，不硬凑 ID，与 R10-J..R23-J 及 R24-A..G 空枚举收口
同型。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/cluster/`（3：mailbox / spawn / host）、`src/privacy/`
  （3：deletion / state-layout / record-classes）、`src/preferences/`
  （7：loop-eval / export / precedence / materialize / service / store /
  types）、`src/episode/`（5：manager / store / closure / events /
  replay）、`src/persist/`（2：file-lock / jsonl）、`src/track/`
  （4：primary-split / plan / clarify / loop）、`src/context/`
  （2：index / packet）、`src/feedback/`（3：redaction / store / types）
  共 **29 文件 3655 行**全量第二十四遍实际读码，未依赖前二十三轮记忆。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表，含 J1 与
  S5-J-3 / S6-J-1 / S8-J-2 行）/ PROGRESS.md / round-24/PLAN.md
  （派发版含 A–G 七区空枚举收口注记与 H/I/J 派单注记及全部移植
  禁令）/ R23-J.md（空枚举；CALLB 调用边界排程面四胞）/ R22-J.md
  （空枚举；SHAPEK 隐藏类形状面四胞）/ R21-J.md（空枚举；KFAN
  聚合键扇出面四胞）；R24-A / R24-B / R24-C / R24-D / R24-E /
  R24-F / R24-G 结论段（经 PLAN 收口注记）确认不移植其轴
  （list-dimension 普查 / M=1 catalog collapse / OCCX /
  approval-authority face / JB / serialization-boundary provenance
  face / RUNLIM）。J1 未重做（本轮重跑其等价仿真全绿 2886.8×）；
  `scripts/r1j-equivalence-sim.ts` 未改动。未开 PR；未编辑
  EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未触碰
  R24-H / R24-I 分支与文件（两者在本轮运行中途以空枚举摄取入独占
  tip——H 收 operand-residency 普查、I 收 adaptation-state
  coordinate，均为外切片轴不移植，后者经 `74a0f93` 对 D/E/J 明令
  禁移植；本分支按指令快进至新 tip，唯一独有文件仍为本报告）。
  零生产编辑 ⇒ 按指令**未提交任何新仿真
  脚本**（新轴 harness 源码与 sha256 入附录 B，锚点 harness 出处入
  附录 A，工作区临时件已在 gate 前移出工作树）。
- **指令点名的换名重提检查**（零 diff ⇒ J1 以来逐字节一致，全部
  落地形态数学上原样；本轮读码逐条复核，未列为新候选）：
  - **J1**：`loop-eval.ts` 每主体增量 fold + O(1) tombstone undo
    落地形态逐字节未变；等价仿真 2468 项逐位全绿（2886.8×）。P3
    在其**站点**测值类剂量但不动其形态——四剂量输出逐位相同
    直证 fold 对值类语义盲（只做 === 与 valueKey 串键），响应全部
    是 String() 的引擎值路径价；其零生产调用方面 grep 再证维持
    （src 内仅定义点，R10-J 面）。
  - **S5-J-3（fail-closed 钉死项）**：`cascadeFeedbackTombstones`
    L108–111 的两笔 fail-closed 读仍在匹配循环**之前**无条件执行，
    零形态改动。本轮四胞无一触级联路径（S0c 锚三遍重锚除外）。
  - **S6-J-1（partial-delete 钉死项）**：L121–124 的
    `writeFeedbackRecords` → tombstone 落盘仍为**串行两写**，零
    并行化/攒批/延迟变形。
  - **S8-J-2**：`persist/jsonl.ts` L17–22 fsync 路径仍为 appendFile
    后独立 `open("a")`+sync+close 双句柄形态，一字未动。本轮四胞
    无一触 fsync 路径（P1 DISK 走 store.ts writeFileSync，P3/P4 纯
    内存，P2 纯读）。**未去任何 fsync**。
  - **R23-J CALLB（指令明令勿补铸）**：未重测未补铸。本轮全部
    fixture 调用排程钉死：P1 每剂量恒 500 种子 + 200 计时写单相位
    （逐剂量调用数相同）、P2 恒 1 次水合调用、P3 恒单调用纯函数、
    P4 恒单调用——边界结构全程不动，动的是**载荷值的类**。
  - **R22-J SHAPEK（指令明令勿补铸）**：未重测未补铸，且区分是
    本报告显式论证负担（§2.1 第 2 层）：SHAPEK 在**固定语义**下
    剂量**形状多态度 K**（键序/可选字段在位性，全 string 值），
    测 IC 多态税；VALCLASS 每剂量恒单字面量构造器单键序全字段
    在位（剂量内 K=1 单态），剂量间动的是 value 字段的**类**——
    它激活语义上不同的分支族（合并算术 vs keep-existing）与引擎
    值路径（dtoa），不是 IC 多态剂量。字段表示（Smi/Double/
    Tagged）随类改变是被测变量的定义性后果，非 SHAPEK 的 K>1
    通道（本轮无任何双形状输入流）。
  - **R21-J KFAN（指令明令勿补铸）**：未重测未补铸。P1/P2 恒
    S=20 主体对 2 键、P3 恒 S=60 主体 1 键、P4 恒 2 视图对——
    聚合键基数钉死；值池基数恒 2（MIX 恒 4）跨剂量不变，动的是
    池成员的**类**非基数。
  - **R20-J 输入类别组成剂量面（指令明令勿重铸）**：未重测未
    补铸，且区分是本报告显式论证负担（§2.1 第 1 层）：R20-J 的
    「类」是**记录角色类**（explicit/inferred 配比、级联命中/
    背景、tombstone 密度、事件类型组成），其 C2 fixture 的 value
    多类是**固定背景**（R20-J.md L718–724 原文构造：
    `valuePick < 2 ? … : i % 7 : … true : "v9"`，逐剂量恒定）；
    VALCLASS 钉死全部记录角色配比（explicit 恒 30%、零
    tombstone、无级联/事件变量），把**载荷字段的值类**首次变成
    受控剂量，且补上 R20-J 背景里不存在的非整双精度类。
  - **R19-J 重投递剂量 + 输入次序置换（指令明令勿重铸）**：未重
    测未补铸。记录次序与 createdAt 序列跨剂量逐位相同（同一
    `makeObs` 构造仅值域替换），每记录投递恰一次。
  - **R16-J 字节轴（指令明令勿重铸）**：未重测未补铸。宽度层内
    字节账户**逐位断言相等**（w4 三剂量 145244 B、w17 两剂量
    154344 B——比 R20-J 的补偿构造更强：文件尺寸 stat 直等）；
    BOOL（145464 B）/ MIX 申报。17 字符层的存在本身即把「字节
    变化」与「类变化」解耦：STR17 vs DBL17 等字节异类，DBL4 vs
    DBL17 同类异字节，响应全部归因到类通道。
  - **R24 兄弟七轴与被禁移植轴（指令 + PLAN 明令勿移植）**：
    **RUNLIM**（R24-G 运行限额配置面）未移植——RUNLIM 扫的是
    G 辖 run-limits **配置选项**从未被扫的腿；VALCLASS 动的是
    J 辖持久化**数据载荷**的类型联合，非任何配置旋钮。**R24-F
    state serialization-boundary provenance face** 未移植——F 轴
    问 restore(serialized) 的状态半边出处；VALCLASS 全程单一出处
    （harness 构造 → 生产入口写入），动的是载荷类非序列化边界
    出处。**OCCX**（格胞占据/集中度剂量）未移植——本轮无任何
    占据/集中度变量（主体分布恒均匀）。**JB**（join/绑定命运面）
    / **R24-B M=1 catalog collapse** / **R24-D approval-authority
    face** / **R24-A residual list-dimension 普查**（N_op/N_cl 列
    维）未移植——无目录规模/审批模式/join 命运/列维变量。
    **CKMIX**（R23-G）与 R23-G 其余轴（TERMCLASS / SCHEDWIN /
    XPROC / NAMESHAPE / BYTESHAPE / SYSCENSUS）、**R23-I `.pi/`
    树**、**R23-A G×E**、**R23-B batch arrangement**、ORDX /
    SEEDX / YMIX / SCALEX / FITQ / NVG / GS 一个未移植。
  - **file-lock retryMs 阶梯（指令明令勿补铸）**：未补铸。
    `withExclusiveFileLock` 生产调用方 grep 再证全在
    `pi-adapter/file-credential-store.ts` / `learning/bandit-store.ts` /
    `adaptation/promotion.ts` / `cli/episode.ts` 四个他区文件，J 切片
    内零调用位点——D/E 已自调用方定价，无 J 拥有的未定价锁面。
  - **S1-J-1 / S1-J-2 / S3-J-5（rebuildViews 族，已闭）**：P1 测
    rebuild 的**类响应**（分支族单价），不提任何增量化/计数器化/
    时间戳提升变体——已闭 ID 是「改 rebuild 结构」，本轮是「量
    rebuild 输入类」，响应测量 ≠ 候选重提（R21-J K2 / R23-J P4
    同型先例）。S2-J-6（filter→单遍计数）未重开：applyObservation
    的 O(i) filter 类盲（只读 explicit 与三键），本轮直证其被
    saveToDisk 支配维持。
  - **S2-J-9 / S3-J-1 / S9-J-1（redaction 族）与 S7-J-6 / S9-J-2
    / S6-J-2**：全部原样在位、未补铸、本轮未触（redaction 无值
    联合字段——FeedbackRecord.score 恒 number、body 恒 string，
    无类剂量可问）。
  - R10-J 零调用方导出收口维持：`evaluatePreferenceLoop`（本轮
    grep 再证：src 内仅定义点）/ `exportForDataset` /
    `materializeView` / `findConflicts` / `durableRecordClassById` /
    `precedence.ts` 整模块等零生产调用方面维持（零 src 提交 ⇒
    数学上不变，§1.2）。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价（R1 未接线）、
  双 LCB 与双归因保留（X0-11）、晋升 proposal-first、无
  Outcome-supported 声明（Checkpoint F-PROD 仍开放，ADR-005 / X0-1）、
  阈值/测试/CAS/凭据/数据面契约/公开签名不变、**fsync 未去、完整性
  再哈希未去**、删除级联 fail-closed 两读顺序与串行两写未动、
  fail-open 家族全部保持关闭、S5-H-1 未触（切片外），天然成立。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0（本 VM
  `/exec-daemon/node` 遮蔽 PATH 前段，与 R17-J..R23-J 同注记），
  显式前置 `$HOME/.nvm/versions/node/v22.22.2/bin` 后
  `node --version` = v22.22.2 为规范引擎；pnpm 10.17.1，
  `pnpm install --frozen-lockfile`；仿真与门禁全绿（§6）。

## 1. 基线不变性、调用图复核与 I/O 地板重锚

1. **切片逐字节未变**：`git diff fb41417..HEAD -- src/cluster
   src/privacy src/preferences src/episode src/persist src/track
   src/context src/feedback` 输出为空（29 文件 3655 行，二十四遍
   全程 J1 之外零 diff）。R1-J…R23-J 全部逐函数收口与 54 个
   S\*-J-\* 已闭 ID + R10-J..R23-J 十四轮无 ID 收口继承有效。
2. **调用图可证不变（最强形式，第十六连）**：`git log 350dd2d..HEAD
   -- src/` **零提交**——R23-J tip `350dd2d` 以来 15 个提交
   （b1fbf82 / 2c2ffcb / 0eaae6a / e946fa8 / 02a300a / 7e3140a /
   bbe7979 / d218fdb / a655895 / 34a293a / c9a5789 / 5c0608f /
   aec794e / 61c3981 / 74a0f93——末三个为运行中途落地的 R24-H/I
   摄取、Round 25 开卷与 R24-I 轴移植禁令，快进后复核仍零 src
   提交）全部为 `docs/reports/sota-opt/` 文档 ⇒ 生产调用方地图
   数学上不变。
   本轮抽样复核：`evaluatePreferenceLoop` src 内仅定义点、
   `withExclusiveFileLock` 生产调用方全在他区四文件（J 内零位点）、
   `cascadeFeedbackTombstones` src 内仅 deletion.ts（定义 +
   `deleteEpisodeRecords` 调用），`deleteEpisodeRecords` /
   `deleteRunRecords` 唯一外部调用方 `cli/main.ts` 逐 episode/run
   删除命令、preferences 写入方仅 `cli/main.ts` L1298
   `correctPreference(…, parsePreferenceValue(value), …)`（L1105–
   1111：`"true"/"false"` → boolean、有限数值 → number、其余
   string）与 `learning/from-episode.ts` L146
   `recordInferredPreference("project", projectId, "primary-model",
   primaryModelId, …)`（值恒 string）、boolean 类消费方仅
   `track/clarify.ts` L20–29 `habitsFromPreferences`（三布尔键）与
   `cli/main.ts` L1260 检视命令、`createClusterHost` 生产 P ≤
   `defaultRunLimits().maxTasks` = 16（`domain/limits.ts` L16）
   全部原样。
3. **配置态 I/O 地板矩阵重锚**（三遍 × 各 3 次运行，本 VM，规范引擎
   Node 22.22.2；harness 与 R12-J 附录 A 逐字节同源仅换 `r24j-`
   前缀，sha256 见附录 A）：

```text
S0a  saveToDisk 非空策略 50 观察档:  218.5/202.5/138.6 | 222.6/186.6/130.0 | 225.0/184.8/153.5 us/write → 带 130.0–225.0µs（R23-J 128.0–220.6 / R22-J 142.4–208.8µs 带重叠）
S0a' 同上 500 观察档:  547.8/468.9/462.9 | 508.0/422.8/421.7 | 501.3/428.0/428.6 us/write → 带 421.7–547.8µs（R23-J 410.9–520.2µs 带重叠；高缘 +2.9µs 超 R22-J 544.9 为亚噪声、深居 R20-J 420.2–571.8µs 带内——地板随存量伸缩，I/O + 全量 stringify 支配判据第二十四次成立）
S0b  jsonl append fsync=false (2000 次):  65.0/67.4/62.2 | 64.4/66.6/60.1 | 64.7/65.5/59.4 us → 带 59.4–67.4µs（R22-J 59.9–67.5µs 带重叠，下缘 −0.5µs 亚噪声）
S0b  jsonl append fsync=true  (300 次):   288.3/251.9/201.2 | 260.4/244.7/221.2 | 310.1/231.4/241.1 us → 带 201.2–310.1µs（R22-J 204.0–288.2µs 带重叠，下缘 −2.8µs 亚噪声；高缘落 R21-J 251.5–381.5µs 带内）
S0c  删除级联 N=200、20 命中 MATCH:    761.0/708.3/714.1 | 688.5/651.3/626.4 | 688.9/635.9/620.9 us → 带 620.9–761.0µs（R23-J 622.2–787.1µs 带重叠，下缘 −1.3µs 亚噪声）
S0c  删除级联 N=200 NO-MATCH:          262.7/310.9/259.5 | 282.0/276.7/316.8 | 273.7/299.8/267.3 us → 带 259.5–316.8µs（R23-J 263.7–308.2µs 带重叠；下缘 −4.2µs 居 R21-J 263.2 邻域、高缘落 R20-J 267.0–371.5µs 带内）
S0d  buildProjectContextIndex: 40.5/41.0/41.5 | 40.6/40.5/40.4 | 40.1/40.0/40.0 us → 带 40.0–41.5µs（R23-J 40.2–40.9 / R22-J 40.4–41.3µs 带重叠）
S0e  track plan CPU: 计数器 gen 1.07/1.06/1.02 | 1.10/1.10/1.11 | 1.04/1.05/1.03 us/plan → 带 1.02–1.11µs（R23-J 1.09–1.12 / R22-J 1.05–1.14µs 带重叠）
     默认 randomUUID gen: 2.55/2.54/2.49 | 2.63/2.68/2.64 | 2.60/2.61/2.56 us/plan → 带 2.49–2.68µs（R23-J 2.60–2.66 / R21-J 2.48–2.68µs 带重叠）
```

   判读：CPU 三格（S0d/S0e）与 I/O 五格逐一与 R21-J/R22-J/R23-J 带
   重叠（各微小越沿均为亚噪声级或落更早轮带内）——**「同路径
   µs/ns CPU 为 I/O 地板支配即拒」判据在本 VM 第二十四次成立**。
   未复制任何前轮数字，全部本 VM 重测。
4. **J1 等价仿真重跑**（生产脚本，未改动）：
   `pnpm exec tsx scripts/r1j-equivalence-sim.ts` → 场景 1（16 例
   确定性边界）64 项 + 场景 2（600 例随机化）2400 项 + 性能夹具
   （N=6000 多主体 ~25% tombstone）reference 10118.8ms → current
   3.5ms（**2886.8×**；reference 逐元素访问 92,590,419 次/评估）。
   `ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)`。

## 2. 第二十四遍枚举：偏好值联合类型类组成面（VALCLASS，四胞 + 逐胞账户见证）→ 全部「无候选」→ 诚实空结果

### 2.1 收口角度

前二十三轮已按二十三个互不重复的收口角度穷尽（逐函数下界 R1-J、
调用图 R2-J、语义融合 R3-J、并行竞态 R4-J、fail-open R5-J、崩溃序
R6-J、配置态热路径 R7-J、fsync 句柄 R8-J、原型键/死分支 R9-J、
零调用方 R10-J、结构×机制矩阵 R11-J、跨函数重复计算 R12-J、fs
动词形态 R13-J、规模越线 R14-J、A/A 引擎代 R15-J、syscall/字节形
R16-J、拒绝路径/冷进程 R17-J、skip/整命令对账 R18-J、重投递/次序
R19-J、类别组成 R20-J、聚合扇出 KFAN R21-J、隐藏类形状 SHAPEK
R22-J、调用边界排程 CALLB R23-J）。

本轮新轴 **VALCLASS（偏好值联合类型类组成面）**：切片唯一的多态
载荷字段是 `PreferenceObservation.value: string | number | boolean`
（views 的 `aggregates` 与 loop-eval 的 `EffectiveValue` 继承同一
联合）。固定 N、次序、explicit 配比、主体/键基数、构造器形状、
调用排程与（层内）字节总量，只变**该字段的值类组成**。其新颖性
有三层直接证据：

1. **不是 R20-J 输入类别组成的重铸**：R20-J 的类是**记录角色类**
   （explicit/inferred 配比、级联命中/背景、tombstone 密度、
   episode 事件类型）；其唯一带多类值的 fixture（C2，loop-eval
   侧）里 value 类是**固定背景**（R20-J.md L718–724 原文），从未
   成为剂量。二十三轮全部 **store** fixture 的**计时段**恒全
   string 值（R12-J 谱系锚的计时写尾 `value-w${i}`；R23-J P2 值池
   `v${i%3}`）；int 只出现在锚的**不计时种子背景**；**非整
   double 从未在任何 J fixture 出现**（R20-J C2 的数值是 `i%7`
   整数；J1 等价仿真的 2.5/−0 属正确性域非定价域）。VALCLASS 把
   类变成受控变量并首次定价 dtoa/浮点解析腿。
2. **不是 R22-J SHAPEK 的重铸**：SHAPEK 固定语义动**形状多态度
   K**（IC 多态税，全 string 值）；VALCLASS 剂量内恒 K=1 单态、
   单键序全字段在位，剂量间动**值的类**——激活语义上不同的分支
   族（`rebuildViews` L112–141 的 explicit 锚定 / 数值同值权重
   累加 / 数值加权均值合并 / keep-existing 四族；loop-eval 的
   typeof 分派 + String()）与引擎值路径（dtoa/浮点解析）。字段
   表示随类改变是被测变量的定义性后果而非多形状输入流。
3. **与其余全部已闭轴正交**：次序/重数（R19-J）逐剂量逐位相同；
   键基数（KFAN）与值池基数（恒 2）钉死；调用边界（CALLB）逐
   剂量同数同相位；字节（R16-J）层内 stat 直等（145244 B ×3 /
   154344 B ×2）且 17 字符层把字节通道与类通道解耦；无任何
   tombstone/命中/事件组成变量（R20-J 域）。

生产类通量（grep，§1.2）：explicit 数值/布尔可达（CLI
`pref correct` 解析器）；inferred 恒 string（from-episode 唯一写入
方）⇒ **inferred 数值累加/合并分支族零生产通量**；boolean 被
clarify 生产消费。问题双向：（i）类响应若达门槛且有合法收割形态
（如「规范化类避 dtoa」）即成候选；（ii）配置态 boolean-habit 腿
（R7-I 透镜：默认空店 fixture 会遮蔽它）需单独定价。四胞覆盖
写路径（P1，含裸序列化分解）、水合读路径（P2）、J1 纯 CPU 站点
（P3）、生产消费腿（P4）。两次独立全网格运行，逐胞见证（确定性
重放逐位 / sourceCount 类盲账户跨剂量相等 / 宽度层内文件字节直等 /
数值剂量合并分支点火断言 / 非数值剂量 keep-existing 断言 / P3
report 跨剂量逐位相同）全过（附录 B）。

### 2.2 P1：store 写路径类剂量（七剂量 × MEM/DISK 两腿 + 裸序列化分解）——旗舰胞

方法：同一多重集几何（S=20 主体对、explicit 30% 于键 "ke"、
inferred 于键 "ki"、每主体值模式 [v0,v0,v1] 循环 ⇒ 数值剂量依次
点火 set/权重累加/均值合并三分支、createdAt 严格单调定宽、id 定
宽），值池按剂量替换：STR4 ["aa","ab"] / INT4 [1001,1002] / DBL4
[10.5,20.5]（序列化宽 4，断言）；STR17 [15 字符 ×2] / DBL17
[3.141592653589793, 2.718281828459045]（序列化宽 17，断言）；BOOL
[true,false] / MIX [aa,1001,10.5,true]（申报）。种子 500 不计时 +
计时 200 写（镜像 S0a' 几何）；MEM 腿（persistence undefined：
filter O(i) + rebuild O(i)）与 DISK 腿（+ stringify O(i) + 写盘）；
全剂量×腿预热后测量，3 运行/格 × 两轮。见证：每格确定性重放逐位
（观察 JSON + 视图投影）、sourceCount 序列跨全剂量相等（耐久性
过滤器类盲的直接证明）、宽度层内文件字节直等、分支点火断言。

```text
            MEM min（µs/写, run1 | run2）   DISK min（µs/写, run1 | run2）   fileBytes
STR4 :      63.4 | 60.7                     341.0 | 336.5                    145244
INT4 :      66.3 | 64.6                     359.4 | 320.7                    145244
DBL4 :      64.5 | 62.4                     379.5 | 339.8                    145244
STR17:      63.9 | 60.0                     400.8 | 336.4                    154344
DBL17:      65.7 | 63.4                     412.7 | 371.4                    154344
BOOL :      63.9 | 66.7                     372.6 | 346.9                    145464
MIX  :      70.8 | 63.2                     383.6 | 356.1                    145244
裸序列化分解（700 观察 payload，min µs，run1 | run2）:
  stringify: STR4 208.5|196.7  INT4 210.2|194.0  DBL4 235.3|214.4  STR17 213.4|199.5  DBL17 251.9|240.4  BOOL 201.6|194.3  MIX 210.8|201.5
  parse:     STR4 308.4|304.8  INT4 295.7|293.4  DBL4 315.5|308.9  STR17 305.2|299.4  DBL17 336.2|334.9  BOOL 291.1|291.2  MIX 304.0|302.6
```

**裁决（无候选）**：
1. **MEM 腿（rebuild 分支族单价）类盲到亚噪声**：七剂量 min 摆幅
   ≤7.4µs/写且极值剂量两轮不一致（run1 MIX 最慢、run2 BOOL 最
   慢）——数值合并算术、权重累加与 keep-existing 的单价差在
   500 存量 rebuild 上 1–4µs 级，深居写路径噪声。**零生产通量**
   （inferred 恒 string）再叠加：该分支族无问题可问。
2. **类响应的干净载体是序列化器且方向稳定**：字节逐位相等下
   stringify DBL17−STR17 = **+38.5 | +40.9µs**、parse = **+31.0 |
   +35.5µs**（两轮同向）⇒ **dtoa 税 ≈55–58ns/值、浮点解析税
   ≈44–51ns/值**；DBL4−STR4 stringify +26.8 | +17.7µs（短 dtoa
   便宜一半）；INT4≈STR4（±2µs，整数快路径）。DISK 腿 DBL17 两轮
   皆最慢（+11.9 | +35.0µs vs STR17）与分解自洽，其余剂量序被
   S0a 级 I/O 噪声（±30µs）洗牌。
3. **候选空间闭**：值的类是**用户数据**（CLI 解析器按用户输入
   决定类；from-episode 按模型 id 决定 string）——store 侧任何
   「规范化类避 dtoa」（数值转 string 存储、double 截断）都改
   落盘字节与 `typeof` 分派语义 = 数据面字节发散禁区（S4-G-6 /
   S6-E-4 族）+ 行为改变；缓存序列化 = X1-1。且最坏腿全额
   +40µs/写落在 320–430µs I/O 地板的 once-per-command 写路径上，
   距落地线 3–4 个数量级。**该胞不产生候选。**

### 2.3 P2：水合类剂量（700 观察，readFileSync + JSON.parse + rebuild）

```text
       min µs/水合（run1 | run2）
STR4 : 445.1 | 435.1    INT4 : 431.2 | 428.0    DBL4 : 449.8 | 445.0
STR17: 440.4 | 438.6    DBL17: 476.4 | 477.9    BOOL : 425.1 | 428.4    MIX: 436.7 | 441.2
```

**裁决（无候选）**：DBL17−STR17 = **+36.0 | +39.3µs**（两轮
同向）= P1 分解 parse 腿（+31.0 | +35.5µs）的直接复现（余量为
rebuild 数值分支 µs 级），与 R23-J P2 HYDRATE 锚（0.30–0.31ms @
500 观察）自洽伸缩。水合每进程恰一次（生产重启路径），类由磁盘
既有数据决定，无调用方旋钮；+39µs/进程是 once-per-process 亚
噪声。**该胞不产生候选。**

### 2.4 P3：J1 站点类剂量（N=6000，evaluatePreferenceLoop）

```text
       min ms/eval（run1 | run2）      report（四剂量逐位相同，两轮）
STR4 : 1.408 | 1.457
INT4 : 1.415 | 1.474                   {"fit":0.333…,"correctionCost":1188,
DBL17: 1.749 | 1.767                    "forgettingEvents":0,"reversalEvents":3816}
BOOL : 1.488 | 1.564
```

**裁决（无候选）**：
1. **响应真实且是本轮最大**：DBL17−STR4 = **+0.341 | +0.310ms/
   eval（~24%）**，载体是 `valueKey` 的 `${typeof v}:${String(v)}`
   ——String(double) 走 dtoa（17 字符）而 String("aa") 恒等、
   String(SMI) 快路径（INT4 +0.007 | +0.017ms）、String(boolean)
   查表（BOOL +0.080 | +0.107ms，兼 Map 串键长度效应）。四剂量
   report 逐位相同 = fold 对值类**语义盲**（只做 === 同一性与
   串键计数）的直接证明——响应纯属引擎值路径，J1 落地形态在类
   维度同样最优（无每类特化可省 dtoa：键必须区分
   `number:1001` 与 `string:1001`，串键即语义）。
2. **双重幻想输入**：满剂量是 100% 非整 double 的 N=6000 观察
   史，而该面**零生产调用方**（R10-J 收口，本轮 grep 再证）+
   生产 inferred 值恒 string。候选形态（记忆化 String(value)、
   重构 valueKey 避串接）分别落 X1-1 与「改变 Map 键语义」——
   且在零流量面上无收益可计。**该胞不产生候选。**

### 2.5 P4：boolean-habit 配置态消费腿（生产 shape，R7-I 透镜）

方法：按 `track/clarify.ts` 实际读取 shape 配置 store（project 对
"projX" 3 布尔键 + user "default" 1 布尔键，explicit）；断言
`habitsFromPreferences("projX")` 返回
`{requireTests:true, preferReview:false, askBeforeWrite:true}`
逐位；对照空店腿。50k 次 × 3 运行 × 两轮。

```text
EMPTY      : 90.7/100.7/75.7 | 96.0/101.9/78.2 ns/call
CONFIGURED : 559.5/557.3/527.7 | 568.6/562.1/526.9 ns/call
```

**裁决（无候选）**：配置态价 **526.9–568.6ns/call**（空店
75.7–101.9ns；差额 = 两次 getView 命中后的 Object.entries 投影 ×2
+ 三键 typeof 守卫 + spread 合并）。生产调用点恰一次/tracked run
（`clarifyObjective` L36）——once-per-run 亚 µs 拒绝类（判据同
S1-H-2 / S2-H-1 族），且 R7-I 透镜下该配置态主路径**不再是测量
盲区**（历轮空店 fixture 首次补上配置腿定价）。getMaterializedView
的投影拷贝形态是 S2-J-5（防御拷贝省略已闭 ~45ns）邻域，未重开。
**该胞不产生候选。**

### 2.6 其余重扫类对账

零 src 提交下数学上不可能出现新类联合面（§1.2）；本轮全量读码以
值类透镜做第二十四遍普查，浮出物全部落已闭域或无类剂量可问：
- **feedback 面无值联合**：`FeedbackRecord.score` 恒 number（0–100
  整数域）、`body/summary` 恒 string——redaction/append/级联无类
  剂量可问（S2-J-9 / S9-J-1 族维持）。
- **views 聚合值联合（`aggregates: Record<string, PrefValue>`）**：
  其类由观察类决定（P1 已定价其生成侧）；消费侧仅 clarify 三键
  typeof 守卫（P4 已定价）与 CLI 检视命令（once-per-command）。
- **episode/context/track/cluster 面**：EpisodeEvent / ContextFact /
  PlannedChild / ClusterMail 全部单类字段（string/常量枚举），
  唯一数值面（packet 的 token 估算、context 的 rank）恒整数 SMI
  域——无联合无剂量。mailbox 数据面强调区按域纪律未测
  （R1-J / S4-J-5 / S5-J-4 三裁决同域）。
- `JSON.stringify` 的 dtoa 位点普查：J 切片内数值落盘位点仅
  preferences（value/weight/recurrenceCount——weight 生产恒
  1.0/0.5 短 dtoa、recurrenceCount 恒 SMI）与 feedback score
  （恒 SMI）——P1 分解已覆盖唯一可变类位点。

### 2.7 收口陈述

二十四遍合计，本切片在当前调用图下**结构上不存在**达门槛候选：
（i）三条 I/O 路径地板 130.0–761.0µs，其上任何等价 CPU 重写不可
寻址（S0a–S0c 二十四轮复现）；（ii）纯 CPU 面每 run 合计个位
µs~41µs，距落地线 3–5 个数量级（S0d–S0e）；（iii）R17-J 拒绝面、
R18-J 无操作面、R19-J 次序面、R20-J 记录角色组成面、R21-J 键基数
面、R22-J 形状多态面、R23-J 排程面均已证无可寻址预算；（iv）本轮
值类组成面证明：**类维度上不存在可寻址预算**——rebuild 分支族
单价亚噪声且最贵分支零生产通量；序列化器的类税（dtoa ≈55–58ns/
值、解析 ≈44–51ns/值）真实、两轮同向、字节等位归因干净，但其
收割形态只有改数据面字节（禁区）或缓存（X1-1），且全额 +40µs/写
沉没于 once-per-command I/O 地板；J1 站点 +0.31–0.34ms/eval 的
dtoa 显影落在零调用方 + 幻想输入的双重否决上；配置态 boolean 腿
0.53–0.57µs/run 是 once-per-run 拒绝类。（v）语义面（fail-closed
两读、串行两写、fsync、恒拷贝 redaction、幂等重申写、recurrence
证据、锁协议、崩溃序、注册即 claim 投递契约、typeof 分派的合并
语义）全部是钉死反例/明文契约的实施点，无合法变形。重开本切片的
唯一前提维持 R9-J..R23-J 收口原文：调用图变更（test-only 面接线
生产热路径、批量删除/批量写入的真实调用方出现、或 inferred 写入
方开始产生数值观察——彼时 P1 的合并分支单价与序列化类税已预先
定价在案），或 preferences/feedback 存量真实增长到 R14-J N\* /
R16-J V\*/B\* 量级——彼时候选是持久化形态变更，走数据面契约评审
而非微观优化。

## 3. 逐文件收口（第二十四遍新视角，其余与 R1-J…R23-J 一致）

| 文件 | 本轮值类组成普查核查 | 结论 |
| --- | --- | --- |
| `cluster/mailbox.ts` | ClusterMail 全 string 字段，无值联合无类剂量；数据面强调区维持（R1-J/S4-J-5/S5-J-4） | 无新候选 |
| `cluster/spawn.ts` | SpawnRequest 数值字段（depth/quota）恒 SMI 域常量比较；无联合 | 无 |
| `cluster/host.ts` | send/register 载荷全 string；P≤16 钉死复核（limits.ts L16） | 无新候选 |
| `privacy/deletion.ts` | 级联对 record 值类盲（只读 episodeId/body/id）；S0c 双锚重锚 620.9–761.0µs；S5-J-3/S6-J-1 在位 | 无新候选 |
| `privacy/state-layout.ts` | 纯路径构造；无类面 | 无 |
| `privacy/record-classes.ts` | 静态表单；零生产调用方维持 | 无新候选 |
| `preferences/loop-eval.ts` | **J1 在位，仿真 2468 项全绿（2886.8×）**；P3 类剂量：DBL17 +0.31–0.34ms/eval 的 String(double) dtoa 显影，四剂量 report 逐位相同 = fold 类语义盲；零生产调用方 + 幻想输入双重否决；J1 形态在类维同样最优 | 无新候选 |
| `preferences/export.ts` | 零调用方维持；value 直通 stringify（P1 分解已定价该类税的唯一形态） | 无新候选 |
| `preferences/precedence.ts` | 整模块零生产调用方维持 | 整面关闭 |
| `preferences/materialize.ts` | P4 载体（getMaterializedView 投影 ×2）；配置态 0.53–0.57µs/run once-per-run 拒绝类；S2-J-5 邻域未重开 | 无新候选 |
| `preferences/service.ts` | 纯委托层；recordInferredPreference 生产值恒 string（合并分支零通量前提） | 无 |
| `preferences/store.ts` | P1/P2 主体：rebuild 分支族（L112–141 四族）单价 1–4µs 亚噪声 + 最贵分支零通量；序列化类税 dtoa ≈55–58ns/值、解析 ≈44–51ns/值（字节等位两轮同向）收割形态全禁区（数据面字节/X1-1）；宽度钉死见证 145244/154344 B 直等；sourceCount 类盲账户跨七剂量相等；S0a 双档重锚 130.0–547.8µs；S1-J-1/S1-J-2/S3-J-5/S7-J-6 维持 | 无新候选 |
| `preferences/types.ts` | 值联合的定义点；类型定义 | 无 |
| `episode/manager.ts` | 事件载荷无值联合；reduce 分支由事件类型决定（R20-J C4 已收口） | 无新候选 |
| `episode/store.ts` | append 载荷 stringify 无可变类位点（事件字段单类）；S5-G-5/S1-G-1 已闭域维持 | 无新候选 |
| `episode/closure.ts` | 数值面仅 score 阈值恒 SMI；S2-J-8/S7-J-3/S9-J-4 维持 | 无新候选 |
| `episode/events.ts` | 类型定义 | 无 |
| `episode/replay.ts` | JSON.parse 事件流单类字段；两导出零生产调用方维持（R10-J 面） | 无新候选 |
| `persist/file-lock.ts` | 零 J 内调用位点（grep 再证四他区调用方文件）；retryMs 按令未重铸 | 无新候选 |
| `persist/jsonl.ts` | 行序列化由调用方完成（类位点在上游，P1 分解覆盖）；S8-J-2 双句柄零改动；S0b 双档重锚 59.4–310.1µs | 无新候选 |
| `track/primary-split.ts` | 组装层无值联合 | 无 |
| `track/plan.ts` | S0e 重锚 1.02–1.11µs/plan；答案/目标全 string | 无新候选 |
| `track/clarify.ts` | P4 主体：habitsFromPreferences 配置态 526.9–568.6ns/call（空店 75.7–101.9ns），每 tracked run 恰一次 = once-per-run 亚 µs 拒绝类；boolean 类生产消费面首次定价（R7-I 盲区补上） | 无新候选 |
| `track/loop.ts` | startTrackedRun 唯一类接触点是 clarify 转发（P4 已定价）；S4-J-2/S6-J-4/R18-J 收口维持 | 无新候选 |
| `context/index.ts` | ContextFact.value 恒 string；数值面（rank/token 估算）恒 SMI；S0d 重锚 40.0–41.5µs | 无新候选 |
| `context/packet.ts` | 同上（estimateTokens 整数域）；S1-J-7/R12-J 位点未补铸 | 无新候选 |
| `feedback/redaction.ts` | body/summary 恒 string、score 恒 number——无联合无类剂量；S2-J-9/S3-J-1/S9-J-1 维持 | 无新候选 |
| `feedback/store.ts` | score 恒 SMI 域 ⇒ stringify 无 dtoa 位点；isFeedbackRecord typeof 守卫类固定 | 无新候选 |
| `feedback/types.ts` | 类型定义 | 无 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。切片自 R1-J 落地 J1
（`fb41417`）起经 R2-J..R23-J 与本轮 R24-J 二十三遍复查累计零后续
代码改动，逐字节一致。

## 5. 新增排除 ID

**无。** 第二十四遍枚举为空——值类组成面四胞均为测量性裁决而非
生产代码形态：P1 是类响应的首次受控定价（rebuild 分支族亚噪声 +
序列化器 dtoa/解析税 55–58/44–51ns/值的字节等位归因）、P2 是水合
读侧的同税复现、P3 是 J1 站点类语义盲 + 引擎值路径价的证认
（report 四剂量逐位相同）、P4 是配置态 boolean 消费腿的
once-per-run 定价。数字（P1 MEM 60.0–70.8 / DISK 320.7–412.7µs/写
七剂量、分解 stringify 194.0–251.9 / parse 291.1–336.2µs；P2
425.1–477.9µs/水合；P3 1.408–1.767ms/eval；P4 配置 526.9–568.6ns/
call）录档供后续轮次引用；均不构成任何既有条目的重开证据。按指令
空枚举不为 VALCLASS 添加 EXCLUSIONS 表行（plan-ban 昵称仅限
计划注记）。与 R10-J..R23-J 及 R24-A..G 的空枚举收口同型。

## 6. 测试

零代码改动下相关基线复核，全绿（规范引擎 Node v22.22.2 via nvm，
满足 engines >=22.19.0；系统 Node 22.14.0 过低 + 本 VM
`/exec-daemon/node` PATH 遮蔽的环境注记见 §0）：

```bash
pnpm exec tsx scripts/r1j-equivalence-sim.ts
# ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)
# perf fixture: reference 10118.8 ms -> current 3.5 ms (2886.8x)
pnpm gate   # typecheck + lint + test + build 全绿
# tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1（全套件，
# 含 cluster/privacy/preferences/episode/persist/track/context/feedback；
# skipped 1 为既有 provider-smoke 凭据跳过）
```

锚点重锚 harness 与值类轴 harness（临时脚本，无赢家、无新 ID，按
round01–23 仅赢家 sim 入库的仓库惯例未入库；出处与 sha256 见附录
A/B，工作区临时件已在 gate 前移出工作树——dev lint 的 no-console
规则对树内临时脚本报错，移出后 gate 全绿，生产文件零 lint 问题）。
锚点三遍、值类轴两轮全网格，方向与量级一致（数字见 §1.3 /
§2.2–2.5）。

## 附录 A：I/O 地板矩阵 harness 出处（可复现）

脚本与 R12-J 附录 A 归档件**逐字节同源**：自
`docs/reports/sota-opt/round-12/R12-J.md` 附录 A（```ts 围栏内全文）
原样提取为仓库根 `tmp-r24j-anchor.ts`，将其中 `r12j-` 临时目录前缀
替换为 `r24j-`，sha256 =
`942b9dd91a6ccca99364fe724ad47214d0383d9df0481955a94891269d799724`
（同一提取替换为 `r23j-` 前缀时 sha256 =
`9c66373486016fa573ad3f719ab2254d76c5f7dcbb1ba32f30df4306534d02fc`，
与 R23-J 附录 A 记载值逐字节吻合——同源性五向验证）。无赢家按
纪律不入库，临时文件已移出工作树。运行方式：
`pnpm exec tsx tmp-r24j-anchor.ts`。三遍输出已列入 §1.3。

## 附录 B：值类组成面 harness（完整，可复现）与两轮输出

`tmp-r24j-valclass.ts`（sha256
`bfc88c15e6b186c8ec2725ff09aecf9434dd6f5e88ea34106d36af091147cd2b`；
无赢家按纪律不入库，工作区临时件已移出）。运行
`pnpm exec tsx tmp-r24j-valclass.ts`。逐胞见证为内联断言（宽度
钉死证明 / 每格确定性重放逐位 / sourceCount 类盲账户跨剂量相等 /
宽度层内文件字节直等 / 数值剂量合并分支点火 / 非数值剂量
keep-existing / P2 水合计数 / P3 确定性 / P4 配置读逐位），任一
失败即抛 `FIDELITY FAIL` 中止。

```ts
/**
 * R24-J new-axis harness: VALCLASS — preference value-union class-composition
 * face.
 *
 * The slice's only polymorphic payload field is
 * `PreferenceObservation.value: string | number | boolean`. Twenty-three
 * rounds of J fixtures never made its CLASS a controlled variable: the store
 * anchors (R12-J appendix A lineage) alternate string/int values as a FIXED
 * untimed seed background and measure an all-string write tail; R20-J C2
 * carried a fixed mixed background on the loop-eval side only; and the
 * non-integral double class (shortest-round-trip dtoa pricing in
 * JSON.stringify / Number parsing in JSON.parse / String() in J1's valueKey)
 * never appeared in any J fixture at all. The class selects real branch
 * families in `rebuildViews` (explicit anchor / numeric same-value weight
 * accumulation / inferred-vs-inferred numeric weighted-mean merge /
 * non-numeric keep-existing) and real engine value paths (dtoa, parse-float,
 * typeof dispatch) in the write, hydrate, and J1 stations.
 *
 * Production class flux (grep-verified):
 *  - explicit numeric/boolean observations are reachable via one CLI command
 *    (`pref correct` -> parsePreferenceValue maps "true"/"false" -> boolean,
 *    finite numerics -> number);
 *  - inferred observations are written only by learning/from-episode.ts and
 *    always carry a STRING value (primary-model id) => the inferred-numeric
 *    accumulate/merge branch family has zero production flux today;
 *  - the boolean class is production-CONSUMED by track/clarify.ts
 *    habitsFromPreferences (require-tests / prefer-review / ask-before-write).
 *
 * Doses (2-value pools, serialized width pinned inside each tier):
 *   4-char tier : STR4 ["aa","ab"] | INT4 [1001,1002] | DBL4 [10.5,20.5]
 *   17-char tier: STR17 [15-char strings] | DBL17 [pi, e] (17-char dtoa)
 *   declared    : BOOL [true,false] | MIX [4-class rotation]
 * Per-pair value pattern [v0,v0,v1] cycles so numeric doses exercise all
 * three numeric branches (set / same-value weight accumulation / mean merge).
 *
 * Pinned across doses (prior-axis dimensions held constant): N, record order
 * and createdAt sequence (R19-J), explicit ratio 30% (R20-J), subject/key
 * cardinality (R21-J KFAN), one literal constructor with one key order per
 * fixture (R22-J SHAPEK; the per-dose field representation change is the
 * dosed variable itself, monomorphic K=1 within every dose), call schedule
 * (R23-J CALLB), serialized byte totals inside each width tier (R16-J).
 *
 * Cells:
 *  P1 store write path at configured state (seed 500 + 200 timed writes),
 *     MEM leg (filter+rebuild) vs DISK leg (+ stringify + write), plus a
 *     serializer decomposition (raw JSON.stringify / JSON.parse of the final
 *     700-observation store payload);
 *  P2 hydration (configurePreferencePersistence -> readFileSync + JSON.parse
 *     + rebuildViews) per class at 700 observations;
 *  P3 J1 site evaluatePreferenceLoop (N=6000) per class (valueKey
 *     `${typeof v}:${String(v)}` + typeof dispatch; zero production callers);
 *  P4 boolean-habit consumption: habitsFromPreferences at a configured store
 *     with the three production habit keys vs an empty store (per tracked
 *     run this runs exactly once).
 *
 * Fidelity gates (inline asserts):
 *  - per-dose determinism: every P1 session is run twice; final observation
 *    JSON and view projections must be bitwise equal;
 *  - class-independent accounts: observation count and per-view sourceCount
 *    sequences bitwise equal ACROSS all doses (the durability filter never
 *    reads the value class);
 *  - byte account: final store file bytes exactly equal inside the 4-char
 *    tier {STR4,INT4,DBL4} and inside the 17-char tier {STR17,DBL17};
 *    BOOL/MIX byte totals declared;
 *  - branch-activation witnesses: numeric doses must produce a merged
 *    non-pool aggregate (proves the weighted-mean branch fired); STR/BOOL
 *    doses must keep aggregates inside the pool (keep-existing branch).
 */
import { mkdtempSync, rmSync, statSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import {
  configurePreferencePersistence,
  recordObservation,
  listObservations,
  getView,
  resetPreferenceStore
} from "./src/preferences/store.js";
import { evaluatePreferenceLoop } from "./src/preferences/loop-eval.js";
import { habitsFromPreferences } from "./src/track/clarify.js";
import type { PreferenceObservation } from "./src/preferences/types.js";
import type { EpisodeId } from "./src/domain/ids.js";
import type { IsoTimestamp } from "./src/domain/timestamp.js";

const hr = (): bigint => process.hrtime.bigint();
const us = (a: bigint, b: bigint, n: number): number => Number(b - a) / 1000 / n;
const fmt = (x: number): string => x.toFixed(x < 10 ? 2 : 1);
const pad = (n: number, w: number): string => n.toString().padStart(w, "0");

type PrefValue = string | number | boolean;
interface Dose {
  readonly name: string;
  readonly pool: readonly PrefValue[];
  readonly widthTier: "w4" | "w17" | "declared";
}

const DOSES: readonly Dose[] = [
  { name: "STR4", pool: ["aa", "ab"], widthTier: "w4" },
  { name: "INT4", pool: [1001, 1002], widthTier: "w4" },
  { name: "DBL4", pool: [10.5, 20.5], widthTier: "w4" },
  { name: "STR17", pool: ["vvvvvvvvvvvvvv0", "vvvvvvvvvvvvvv1"], widthTier: "w17" },
  { name: "DBL17", pool: [3.141592653589793, 2.718281828459045], widthTier: "w17" },
  { name: "BOOL", pool: [true, false], widthTier: "declared" },
  { name: "MIX", pool: ["aa", 1001, 10.5, true], widthTier: "declared" }
];

// Serialized-width pinning proof (asserted, not assumed).
for (const dose of DOSES) {
  for (const v of dose.pool) {
    const w = JSON.stringify(v).length;
    if (dose.widthTier === "w4" && w !== 4) throw new Error(`width pin fail ${dose.name}: ${v} -> ${w}`);
    if (dose.widthTier === "w17" && w !== 17) throw new Error(`width pin fail ${dose.name}: ${v} -> ${w}`);
  }
}

function assertEqual(label: string, a: unknown, b: unknown): void {
  const sa = JSON.stringify(a);
  const sb = JSON.stringify(b);
  if (sa !== sb) throw new Error(`FIDELITY FAIL ${label}: ${sa?.slice(0, 160)} !== ${sb?.slice(0, 160)}`);
}

console.log(`node ${process.version}`);

/* ============ shared multiset construction (values are the only dose) ===== */
const S = 20; // subject pairs sk00..sk19 (R21-J key-cardinality pinned)
function makeObs(i: number, dose: Dose): PreferenceObservation {
  const explicit = i % 10 < 3; // 30% explicit (R20-J ratio pinned)
  const cyclePos = Math.floor(i / S) % 3;
  const valueIdx = dose.name === "MIX" ? Math.floor(i / S) % dose.pool.length : cyclePos === 2 ? 1 : 0;
  return {
    id: `evd_${pad(i, 8)}`,
    scope: "project",
    scopeKey: `sk${pad(i % S, 2)}`,
    key: explicit ? "ke" : "ki", // explicit anchors and inferred merges on disjoint keys
    value: dose.pool[valueIdx]!,
    evidenceEpisodeId: "ep_fixed0000000" as EpisodeId,
    weight: 1,
    createdAt: `2026-08-25T00:00:${pad(Math.floor(i / 100), 2)}.${pad(i % 100, 3)}Z` as IsoTimestamp,
    explicit,
    recurrenceCount: 1 // recomputed by applyObservation; carried value inert
  };
}

function viewProjection(): string {
  const rows: unknown[] = [];
  for (let p = 0; p < S; p++) {
    const view = getView("project", `sk${pad(p, 2)}`);
    rows.push(view === undefined ? null : { aggregates: view.aggregates, confidence: view.confidence, sourceCount: view.sourceCount });
  }
  return JSON.stringify(rows);
}

function sourceCounts(): string {
  const counts: number[] = [];
  for (let p = 0; p < S; p++) counts.push(getView("project", `sk${pad(p, 2)}`)?.sourceCount ?? -1);
  return counts.join(",");
}

/* ================= P1: store write-path value-class dose ================= */
{
  const SEED = 500;
  const WRITES = 200;
  const RUNS = 3;

  interface SessionOut {
    readonly perWriteUs: number;
    readonly obsJson: string;
    readonly views: string;
    readonly srcCounts: string;
    readonly fileBytes: number;
  }

  function runSession(dose: Dose, leg: "MEM" | "DISK", timed: boolean): SessionOut {
    const dir = leg === "DISK" ? mkdtempSync(join(tmpdir(), "r24j-vc-p1-")) : undefined;
    resetPreferenceStore();
    configurePreferencePersistence(undefined);
    if (dir !== undefined) configurePreferencePersistence(join(dir, "preferences.json"));
    for (let i = 0; i < SEED; i++) recordObservation(makeObs(i, dose));
    const t0 = hr();
    for (let i = SEED; i < SEED + WRITES; i++) recordObservation(makeObs(i, dose));
    const t1 = hr();
    const out: SessionOut = {
      perWriteUs: us(t0, t1, WRITES),
      obsJson: JSON.stringify(listObservations()),
      views: viewProjection(),
      srcCounts: sourceCounts(),
      fileBytes: dir !== undefined ? statSync(join(dir, "preferences.json")).size : 0
    };
    resetPreferenceStore();
    configurePreferencePersistence(undefined);
    if (dir !== undefined) rmSync(dir, { recursive: true, force: true });
    void timed;
    return out;
  }

  // Untimed warm-up over every dose x leg so every branch family reaches a
  // common JIT/IC state before any dose is measured (R23-J discipline).
  for (const dose of DOSES) {
    runSession(dose, "MEM", false);
    runSession(dose, "DISK", false);
  }

  const bytesByDose = new Map<string, number>();
  let srcCountsRef: string | undefined;
  for (const leg of ["MEM", "DISK"] as const) {
    for (const dose of DOSES) {
      let best = Infinity;
      let sum = 0;
      let last: SessionOut | undefined;
      for (let r = 0; r < RUNS; r++) {
        const out = runSession(dose, leg, true);
        best = Math.min(best, out.perWriteUs);
        sum += out.perWriteUs;
        last = out;
      }
      // Per-dose determinism: replay the identical session untimed and
      // compare final state bitwise.
      const replay = runSession(dose, leg, false);
      assertEqual(`P1 ${dose.name}/${leg} determinism obs`, replay.obsJson === last!.obsJson, true);
      assertEqual(`P1 ${dose.name}/${leg} determinism views`, replay.views === last!.views, true);
      // Class-independent account: durability filter never reads the class.
      if (srcCountsRef === undefined) srcCountsRef = last!.srcCounts;
      assertEqual(`P1 ${dose.name}/${leg} sourceCount account`, last!.srcCounts, srcCountsRef);
      assertEqual(`P1 ${dose.name}/${leg} obs count`, JSON.parse(last!.obsJson).length, SEED + WRITES);
      // Branch-activation witnesses on the inferred key "ki".
      const views = JSON.parse(last!.views) as ({ aggregates: Record<string, PrefValue> } | null)[];
      const kiValues = views.map((view) => view?.aggregates["ki"]).filter((v) => v !== undefined);
      const numericDose = dose.name === "INT4" || dose.name === "DBL4" || dose.name === "DBL17";
      if (numericDose) {
        const merged = kiValues.some((v) => typeof v === "number" && !dose.pool.includes(v));
        assertEqual(`P1 ${dose.name}/${leg} merge branch fired`, merged, true);
      }
      if (dose.name === "STR4" || dose.name === "STR17" || dose.name === "BOOL") {
        const kept = kiValues.every((v) => dose.pool.includes(v as PrefValue));
        assertEqual(`P1 ${dose.name}/${leg} keep-existing branch`, kept, true);
      }
      if (leg === "DISK") bytesByDose.set(dose.name, last!.fileBytes);
      console.log(
        `P1 store write ${leg.padEnd(4)} ${dose.name.padEnd(5)} (seed=${SEED}, writes=${WRITES}): ` +
          `min=${fmt(best)} mean=${fmt(sum / RUNS)} us/write${leg === "DISK" ? `  fileBytes=${last!.fileBytes}` : ""}`
      );
    }
  }
  // Byte account inside each width tier: exact equality.
  assertEqual("P1 bytes w4 STR4=INT4", bytesByDose.get("STR4"), bytesByDose.get("INT4"));
  assertEqual("P1 bytes w4 STR4=DBL4", bytesByDose.get("STR4"), bytesByDose.get("DBL4"));
  assertEqual("P1 bytes w17 STR17=DBL17", bytesByDose.get("STR17"), bytesByDose.get("DBL17"));
  console.log(
    `P1 fidelity: determinism + sourceCount account + branch witnesses passed; ` +
      `bytes w4=${bytesByDose.get("STR4")} w17=${bytesByDose.get("STR17")} BOOL=${bytesByDose.get("BOOL")} MIX=${bytesByDose.get("MIX")} (declared)`
  );

  /* -------- serializer decomposition: raw stringify/parse of the store --- */
  const N_OBS = SEED + WRITES;
  for (const dose of DOSES) {
    const payload = { observations: Array.from({ length: N_OBS }, (_, i) => makeObs(i, dose)), tombstones: [] as string[] };
    const text = JSON.stringify(payload);
    let sink = 0;
    const ITER = 200;
    for (let i = 0; i < 40; i++) sink += JSON.stringify(payload).length + (JSON.parse(text) as { observations: unknown[] }).observations.length;
    let bestS = Infinity;
    let bestP = Infinity;
    for (let r = 0; r < 3; r++) {
      const t0 = hr();
      for (let i = 0; i < ITER; i++) sink += JSON.stringify(payload).length;
      const t1 = hr();
      for (let i = 0; i < ITER; i++) sink += (JSON.parse(text) as { observations: unknown[] }).observations.length;
      const t2 = hr();
      bestS = Math.min(bestS, us(t0, t1, ITER));
      bestP = Math.min(bestP, us(t1, t2, ITER));
    }
    console.log(
      `P1s serializer ${dose.name.padEnd(5)} (${N_OBS} obs, ${text.length} B): stringify min=${fmt(bestS)} us  parse min=${fmt(bestP)} us  [sink=${sink % 10}]`
    );
  }
}

/* ================= P2: hydration value-class dose ======================== */
{
  const N_OBS = 700;
  const ITER = 30;
  for (const dose of DOSES) {
    const dir = mkdtempSync(join(tmpdir(), "r24j-vc-p2-"));
    const file = join(dir, "preferences.json");
    writeFileSync(
      file,
      JSON.stringify({ observations: Array.from({ length: N_OBS }, (_, i) => makeObs(i, dose)), tombstones: [] }),
      "utf8"
    );
    // warm
    for (let i = 0; i < 10; i++) {
      resetPreferenceStore();
      configurePreferencePersistence(file);
    }
    let best = Infinity;
    let sum = 0;
    for (let r = 0; r < 3; r++) {
      const t0 = hr();
      for (let i = 0; i < ITER; i++) {
        resetPreferenceStore();
        configurePreferencePersistence(file);
      }
      const t1 = hr();
      const dt = us(t0, t1, ITER);
      best = Math.min(best, dt);
      sum += dt;
    }
    assertEqual(`P2 ${dose.name} hydrated count`, listObservations().length, N_OBS);
    resetPreferenceStore();
    configurePreferencePersistence(undefined);
    rmSync(dir, { recursive: true, force: true });
    console.log(`P2 hydrate ${dose.name.padEnd(5)} (${N_OBS} obs): min=${fmt(best)} mean=${fmt(sum / 3)} us/hydrate`);
  }
}

/* ================= P3: J1 site value-class dose (N=6000) ================= */
{
  const N = 6000;
  const SUBJ = 60;
  const ITER = 30;
  function makeLoopObs(i: number, dose: Dose): PreferenceObservation {
    const explicit = i % 10 < 3;
    const cyclePos = Math.floor(i / SUBJ) % 3;
    const valueIdx = dose.name === "MIX" ? Math.floor(i / SUBJ) % dose.pool.length : cyclePos === 2 ? 1 : 0;
    return {
      id: `evd_${pad(i, 8)}`,
      scope: "project",
      scopeKey: `sk${pad(i % SUBJ, 3)}`,
      key: "k0",
      value: dose.pool[valueIdx]!,
      evidenceEpisodeId: "ep_fixed0000000" as EpisodeId,
      weight: 1,
      createdAt: `2026-08-25T00:${pad(Math.floor(i / 6000), 2)}:${pad(Math.floor(i / 100) % 60, 2)}.${pad(i % 100, 3)}Z` as IsoTimestamp,
      explicit,
      recurrenceCount: 1
    };
  }
  const EMPTY = new Set<string>();
  const doses = DOSES.filter((d) => ["STR4", "INT4", "DBL17", "BOOL"].includes(d.name));
  const fixtures = doses.map((dose) => ({
    dose,
    obs: Array.from({ length: N }, (_, i) => makeLoopObs(i, dose))
  }));
  // warm all doses
  for (const fixture of fixtures) for (let i = 0; i < 5; i++) evaluatePreferenceLoop(fixture.obs, EMPTY);
  for (const fixture of fixtures) {
    const r1 = evaluatePreferenceLoop(fixture.obs, EMPTY);
    const r2 = evaluatePreferenceLoop(fixture.obs, EMPTY);
    assertEqual(`P3 ${fixture.dose.name} determinism`, r1, r2);
    let best = Infinity;
    let sum = 0;
    for (let r = 0; r < 3; r++) {
      const t0 = hr();
      for (let i = 0; i < ITER; i++) evaluatePreferenceLoop(fixture.obs, EMPTY);
      const t1 = hr();
      const dt = us(t0, t1, ITER) / 1000;
      best = Math.min(best, dt);
      sum += dt;
    }
    console.log(
      `P3 loop-eval ${fixture.dose.name.padEnd(5)} (N=${N}): min=${best.toFixed(3)} mean=${(sum / 3).toFixed(3)} ms/eval  report=${JSON.stringify(r1)}`
    );
  }
}

/* ====== P4: boolean-habit configured consumption (production leg) ======== */
{
  const ITER = 50000;
  function measure(label: string): void {
    let sink = 0;
    for (let i = 0; i < 10000; i++) {
      const habits = habitsFromPreferences("projX");
      sink += (habits.requireTests === true ? 1 : 0) + (habits.preferReview === false ? 2 : 0) + (habits.askBeforeWrite === true ? 4 : 0);
    }
    const results: number[] = [];
    for (let r = 0; r < 3; r++) {
      const t0 = hr();
      for (let i = 0; i < ITER; i++) {
        const habits = habitsFromPreferences("projX");
        sink += (habits.requireTests !== undefined ? 1 : 0) + (habits.preferReview !== undefined ? 1 : 0) + (habits.askBeforeWrite !== undefined ? 1 : 0);
      }
      const t1 = hr();
      results.push((us(t0, t1, ITER)) * 1000);
    }
    console.log(`P4 habitsFromPreferences ${label}: ${results.map((x) => x.toFixed(1)).join(" / ")} ns/call  [sink=${sink % 10}]`);
  }

  resetPreferenceStore();
  configurePreferencePersistence(undefined);
  measure("EMPTY store          ");

  // Configured: the three production habit keys as explicit booleans on the
  // project pair plus the user-default pair (the exact shape clarify reads).
  let n = 0;
  const put = (scope: "project" | "user", scopeKey: string, key: string, value: boolean): void => {
    recordObservation({
      id: `evd_hb${pad(n++, 4)}`,
      scope,
      scopeKey,
      key,
      value,
      evidenceEpisodeId: "ep_fixed0000000" as EpisodeId,
      weight: 1,
      createdAt: `2026-08-25T01:00:00.${pad(n, 3)}Z` as IsoTimestamp,
      explicit: true,
      recurrenceCount: 1
    });
  };
  put("project", "projX", "require-tests", true);
  put("project", "projX", "prefer-review", false);
  put("project", "projX", "ask-before-write", true);
  put("user", "default", "require-tests", true);
  const habits = habitsFromPreferences("projX");
  assertEqual("P4 configured habits read", habits, { requireTests: true, preferReview: false, askBeforeWrite: true });
  measure("CONFIGURED (3+1 keys)");
  resetPreferenceStore();
}

console.log("VALCLASS ALL FIDELITY GATES PASSED");
```

两轮完整输出（`pnpm exec tsx tmp-r24j-valclass.ts` 两次独立进程）：

```text
run1:
node v22.22.2
P1 store write MEM  STR4  (seed=500, writes=200): min=63.4 mean=64.1 us/write
P1 store write MEM  INT4  (seed=500, writes=200): min=66.3 mean=67.1 us/write
P1 store write MEM  DBL4  (seed=500, writes=200): min=64.5 mean=65.1 us/write
P1 store write MEM  STR17 (seed=500, writes=200): min=63.9 mean=64.5 us/write
P1 store write MEM  DBL17 (seed=500, writes=200): min=65.7 mean=70.7 us/write
P1 store write MEM  BOOL  (seed=500, writes=200): min=63.9 mean=67.1 us/write
P1 store write MEM  MIX   (seed=500, writes=200): min=70.8 mean=71.8 us/write
P1 store write DISK STR4  (seed=500, writes=200): min=341.0 mean=357.7 us/write  fileBytes=145244
P1 store write DISK INT4  (seed=500, writes=200): min=359.4 mean=369.6 us/write  fileBytes=145244
P1 store write DISK DBL4  (seed=500, writes=200): min=379.5 mean=388.7 us/write  fileBytes=145244
P1 store write DISK STR17 (seed=500, writes=200): min=400.8 mean=405.7 us/write  fileBytes=154344
P1 store write DISK DBL17 (seed=500, writes=200): min=412.7 mean=431.3 us/write  fileBytes=154344
P1 store write DISK BOOL  (seed=500, writes=200): min=372.6 mean=376.0 us/write  fileBytes=145464
P1 store write DISK MIX   (seed=500, writes=200): min=383.6 mean=388.3 us/write  fileBytes=145244
P1 fidelity: determinism + sourceCount account + branch witnesses passed; bytes w4=145244 w17=154344 BOOL=145464 MIX=145244 (declared)
P1s serializer STR4  (700 obs, 144724 B): stringify min=208.5 us  parse min=308.4 us  [sink=0]
P1s serializer INT4  (700 obs, 144724 B): stringify min=210.2 us  parse min=295.7 us  [sink=0]
P1s serializer DBL4  (700 obs, 144724 B): stringify min=235.3 us  parse min=315.5 us  [sink=0]
P1s serializer STR17 (700 obs, 153824 B): stringify min=213.4 us  parse min=305.2 us  [sink=0]
P1s serializer DBL17 (700 obs, 153824 B): stringify min=251.9 us  parse min=336.2 us  [sink=0]
P1s serializer BOOL  (700 obs, 144944 B): stringify min=201.6 us  parse min=291.1 us  [sink=0]
P1s serializer MIX   (700 obs, 144724 B): stringify min=210.8 us  parse min=304.0 us  [sink=0]
P2 hydrate STR4  (700 obs): min=445.1 mean=456.7 us/hydrate
P2 hydrate INT4  (700 obs): min=431.2 mean=441.5 us/hydrate
P2 hydrate DBL4  (700 obs): min=449.8 mean=473.3 us/hydrate
P2 hydrate STR17 (700 obs): min=440.4 mean=453.7 us/hydrate
P2 hydrate DBL17 (700 obs): min=476.4 mean=498.1 us/hydrate
P2 hydrate BOOL  (700 obs): min=425.1 mean=439.9 us/hydrate
P2 hydrate MIX   (700 obs): min=436.7 mean=464.7 us/hydrate
P3 loop-eval STR4  (N=6000): min=1.408 mean=1.411 ms/eval  report={"fit":0.3333333333333333,"correctionCost":1188,"forgettingEvents":0,"reversalEvents":3816}
P3 loop-eval INT4  (N=6000): min=1.415 mean=1.418 ms/eval  report={"fit":0.3333333333333333,"correctionCost":1188,"forgettingEvents":0,"reversalEvents":3816}
P3 loop-eval DBL17 (N=6000): min=1.749 mean=1.762 ms/eval  report={"fit":0.3333333333333333,"correctionCost":1188,"forgettingEvents":0,"reversalEvents":3816}
P3 loop-eval BOOL  (N=6000): min=1.488 mean=1.489 ms/eval  report={"fit":0.3333333333333333,"correctionCost":1188,"forgettingEvents":0,"reversalEvents":3816}
P4 habitsFromPreferences EMPTY store          : 90.7 / 100.7 / 75.7 ns/call  [sink=0]
P4 habitsFromPreferences CONFIGURED (3+1 keys): 559.5 / 557.3 / 527.7 ns/call  [sink=0]
VALCLASS ALL FIDELITY GATES PASSED

run2:
node v22.22.2
P1 store write MEM  STR4  (seed=500, writes=200): min=60.7 mean=61.1 us/write
P1 store write MEM  INT4  (seed=500, writes=200): min=64.6 mean=65.2 us/write
P1 store write MEM  DBL4  (seed=500, writes=200): min=62.4 mean=62.8 us/write
P1 store write MEM  STR17 (seed=500, writes=200): min=60.0 mean=61.8 us/write
P1 store write MEM  DBL17 (seed=500, writes=200): min=63.4 mean=64.4 us/write
P1 store write MEM  BOOL  (seed=500, writes=200): min=66.7 mean=67.4 us/write
P1 store write MEM  MIX   (seed=500, writes=200): min=63.2 mean=65.0 us/write
P1 store write DISK STR4  (seed=500, writes=200): min=336.5 mean=355.4 us/write  fileBytes=145244
P1 store write DISK INT4  (seed=500, writes=200): min=320.7 mean=324.4 us/write  fileBytes=145244
P1 store write DISK DBL4  (seed=500, writes=200): min=339.8 mean=345.6 us/write  fileBytes=145244
P1 store write DISK STR17 (seed=500, writes=200): min=336.4 mean=340.5 us/write  fileBytes=154344
P1 store write DISK DBL17 (seed=500, writes=200): min=371.4 mean=391.2 us/write  fileBytes=154344
P1 store write DISK BOOL  (seed=500, writes=200): min=346.9 mean=351.0 us/write  fileBytes=145464
P1 store write DISK MIX   (seed=500, writes=200): min=356.1 mean=360.8 us/write  fileBytes=145244
P1 fidelity: determinism + sourceCount account + branch witnesses passed; bytes w4=145244 w17=154344 BOOL=145464 MIX=145244 (declared)
P1s serializer STR4  (700 obs, 144724 B): stringify min=196.7 us  parse min=304.8 us  [sink=0]
P1s serializer INT4  (700 obs, 144724 B): stringify min=194.0 us  parse min=293.4 us  [sink=0]
P1s serializer DBL4  (700 obs, 144724 B): stringify min=214.4 us  parse min=308.9 us  [sink=0]
P1s serializer STR17 (700 obs, 153824 B): stringify min=199.5 us  parse min=299.4 us  [sink=0]
P1s serializer DBL17 (700 obs, 153824 B): stringify min=240.4 us  parse min=334.9 us  [sink=0]
P1s serializer BOOL  (700 obs, 144944 B): stringify min=194.3 us  parse min=291.2 us  [sink=0]
P1s serializer MIX   (700 obs, 144724 B): stringify min=201.5 us  parse min=302.6 us  [sink=0]
P2 hydrate STR4  (700 obs): min=435.1 mean=448.8 us/hydrate
P2 hydrate INT4  (700 obs): min=428.0 mean=440.1 us/hydrate
P2 hydrate DBL4  (700 obs): min=445.0 mean=465.9 us/hydrate
P2 hydrate STR17 (700 obs): min=438.6 mean=453.7 us/hydrate
P2 hydrate DBL17 (700 obs): min=477.9 mean=538.7 us/hydrate
P2 hydrate BOOL  (700 obs): min=428.4 mean=446.0 us/hydrate
P2 hydrate MIX   (700 obs): min=441.2 mean=471.1 us/hydrate
P3 loop-eval STR4  (N=6000): min=1.457 mean=1.460 ms/eval  report={"fit":0.3333333333333333,"correctionCost":1188,"forgettingEvents":0,"reversalEvents":3816}
P3 loop-eval INT4  (N=6000): min=1.474 mean=1.479 ms/eval  report={"fit":0.3333333333333333,"correctionCost":1188,"forgettingEvents":0,"reversalEvents":3816}
P3 loop-eval DBL17 (N=6000): min=1.767 mean=1.769 ms/eval  report={"fit":0.3333333333333333,"correctionCost":1188,"forgettingEvents":0,"reversalEvents":3816}
P3 loop-eval BOOL  (N=6000): min=1.564 mean=1.616 ms/eval  report={"fit":0.3333333333333333,"correctionCost":1188,"forgettingEvents":0,"reversalEvents":3816}
P4 habitsFromPreferences EMPTY store          : 96.0 / 101.9 / 78.2 ns/call  [sink=0]
P4 habitsFromPreferences CONFIGURED (3+1 keys): 568.6 / 562.1 / 526.9 ns/call  [sink=0]
VALCLASS ALL FIDELITY GATES PASSED
```

MORE_OPTIMA=no
BRANCH=cursor/r24-j-persist-twenty-fourth-pass-83a1
