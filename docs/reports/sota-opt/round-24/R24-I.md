MODEL_SLUG=claude-fable-5-thinking-xhigh

# R24-I：CLI / Pi adapter / config / telemetry 第二十四遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `c9a5789`（派单独占 tip，含 R24-F 收口与 R24-I 派单）；运行中独占分支快进至 `aec794e`（R24-G / R24-H 收口 + Round 25 开卷），本分支已按派单令**同步快进到 `aec794e`**——增量 `c9a5789..aec794e` 全部为 docs（`git diff --stat -- . ':!docs'` 0 行），EXCLUSIONS.md 该区间逐字节未变，切片与 dist 不受影响（全部测量在 `c9a5789` 构建的 dist 上完成，二者切片字节相同）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- src/cli src/pi-adapter src/config src/telemetry` 为空（`--stat` 0 行）——**零漂移，自 `8dee7fb` 起连续第十七轮字节不变**。`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet` 通过）；切片相关测试面零漂移（`git diff --quiet 8dee7fb..HEAD -- test/` 通过）。基线增量核查：`a7d3c4b`（R23-I 基线）→ `aec794e` 共 **16 个提交，全部 `docs(sota-opt)` 收口/派单**（分段核验：`a7d3c4b..c9a5789` 14 提交 + 快进 2 提交，`git diff --stat -- . ':!docs'` 两段均 0 行）；**EXCLUSIONS.md 全区间逐字节未变**（`git diff --quiet` 通过），对 I 切片无新约束行。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致（合计 4708 行，与 R19-I～R23-I 计数逐字相同）。
- 前置阅读按令执行：README → EXCLUSIONS 全表（S1-I / S4-I-2..5 / S5-I-1..5 / S7-I-1..4 / S8-I-1..3 全部行逐条定位）→ PROGRESS → round-24/PLAN（含 R24-A…R24-F 六切片收口摘要与 I 切片行；快进后补读 G/H 行与 round-25/PLAN 开卷段）→ R23-I（十六连空 + 项目侧 `.pi/` 可观测树平面）→ R22-I（run 记录回读平面）→ R21-I（flowchart 平面 128/32）→ R20-I（累积遥测 N 轴）全文。兄弟轴禁移植逐一核读：**R24-A residual input-face list-dimension 普查** / **R24-B M=1 catalog collapse** / **OCCX** / **R24-D approval-authority face** / **JB** / **R24-F state serialization-boundary provenance face** / **RUNLIM**（快进后新增）/ **R24-H operand-residency census**（快进后新增）/ CALLB / CKMIX / ORDX / SEEDX / YMIX / SCALEX / FITQ / TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / SHAPEK / KFAN / NVG / GS / R23-A G×E / R23-B batch arrangement——本轮新轴与其机制、站点、切片均不同（§0 与 §2.1 逐项区分，含与 R21-D「状态增长方向面」和 R24-D 的显式划界）。
- 分支：`cursor/r24-i-cli-twenty-fourth-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（`/exec-daemon/node`，与 R9-I～R23-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。pnpm 10.17.1，`pnpm install --frozen-lockfile` + `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**（package.json 钉死 + frozen-lockfile 内容寻址）；exports 图仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`，本 VM 直接枚举）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第十七轮未满足（两臂文件级 blocked 按派单原样维持；R13-I 已证文件级检查即判决性检查，未重复执行级探针）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S24-I-\* ID（诚实空枚举，与 R9-I～R23-I 同结局，十七连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第二十四组枚举而非历轮剩余项清理。R14-I…R23-I 依次钉死配置空间、测量学、命令类覆盖、错误端点、children 负载规模、配置基数、累积遥测 N、flowchart 平面、run 记录回读、项目侧 `.pi/` 可观测树十个正交维度；R23-I 收官时宣告「两个配置态坐标系（state-root providers 态 × 项目态）全部收口」——本轮发现并收口**第三个配置态坐标系：适应态（adaptation-state）坐标——`adaptation/registry.json` 的存在与字节规模**。历轮全部承重夹具的 state-root 都没有 registry.json，而承重 run 类的**每次执行都无条件在头部做整文件注册表装载**（`loadLearnedRouting`：main.ts L708 children 分支前置 / track/loop.ts L88 / flowchart-run.ts L747 经 `flowchartForSupervisor` L712），ENOENT 早退（learned-routing.ts L123-126）是历轮矩阵唯一走过的腿——这正是派单点名的 R7-I 教训在第三配置坐标系上的重演。且该文件由工具自产、单调增长：auto-loop 每遇新颖 failing 剖面即铸候选（auto-loop.ts L173-188 contentHash 去重 + L139-147 reason 串含 `meanScore.toFixed(2)`/`samples`——批剖面任一位变化即新候选）、`adapt learn` 显式铸造，全仓**无任何轮转/退休工具触碰它**（privacy 两把删除刀只删 state-root `runtime/runs|episodes`；retirement.ts 无 CLI 调用方）。该轴为测量收口、未产出 I 切片候选，四个裁决性发现：

1. **零漂移五重确认（连续第十七轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变；pi-ai 0.84.1 exports 图 7 项未变；`dist/providers/` 仍恰 39 表，本 VM 直接计数）、测试面（零变动）、基线增量（`a7d3c4b..aec794e` 共 16 提交全 docs，EXCLUSIONS 字节不变）、调用图前提（S5-I-1 dispatch 站点 13 条 `await import` 直接计数在位，S7-I-1 站点 model-catalog.ts L53-58 注释与站点原样，`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` L36 失手回退 / `runtime.ts` L20 点用处 / `listed-model.ts` L1 同步面，pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方【grep 计数 0】——本轮逐项直接重验）。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字，派单明令项）**：R9-I 承重集 + flowchart 三格 × 2 Node 版本全部 exit 0，结构逐格同构：地板 54.3/51.6 ms，**custom−builtin 差 children +44.2 / +26.5、track +48.0 / +25.9、flowchart-flat64 +48.7 / +16.3 ms（22.14/22.22）**——与 R23-I 带（+42.9/+20.7、+53.5/+21.8、+39.2/+18.5）同号、同量级、同 Node 版本梯度（**十七轮同构**）；带缘偏移（flat64@22.22 −2.2、track@22.14 −5.5 等）全部落在 R15-I 轴 1 的 A/A 噪声包络内。builtin 态目录构建比地板贵 +56.5/+49.8（children，双引擎），与 R23-I（+55.0/+54.6）紧邻。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
3. **新轴（§1.3）：适应态坐标标定（I 切片首次；第三配置态坐标系，第三个累积态平面【前两个：R20-I invocations N、R23-I `.pi/` 树 F】，且是首个「每承重 run 无条件双计费」的累积平面）**。端到端新增 10 格（双引擎全 exit 0）+ 进程内 8 段分解 + 冷/温双装载探针，裁决数字：
   - **承重 children/track 每 run 付两次整注册表装载**（main.ts L708 / track/loop.ts L88 计一次 + startFlowchartRun L747 supervisor 重装一次；`run --flowchart`/resume/answer 只付一次【L747/L880/L1021】——本轮 flowchart 单装载格实测边际约为 children 的一半，佐证接线计数）。字节线性、无悬崖：children 边际 @144 KB **+2.3/+11.1**、@790 KB **+10.0/+22.0**、@1496 KB **+19.3/+25.0**、@5987 KB **+82.6/+81.8** ms（22.14/22.22）；冷/温双装载探针给出干净的每 run 税 **4.5-5.9 / 12.9-13.7 / 20.0-21.1 / 67.5-68.1** ms（同序四剂量，双引擎并示）。**10 ms/run 越线点 B\* ≈ 0.49-0.57 MB ≈ C\* ≈ 410-480 个累积候选提案**（1.20 KB/候选）——低于 R21-D 单装载越线（~1.5 MB）**2.6-3.1×**，这是只有从 I 侧接线才能看见的事实。命令类无关（track@L +17.5/+24.6 ≈ children +19.3/+25.0）、provider 态无关（builtin@L +16.7/+22.6）、**身份命中/失手无关**（regL-miss +20.5/+27.5 ≈ regL——外来项目铸出的注册表照样全额计税，消费段全部 µs 级）。
   - **税体 100% 在 fs/engine/D 辖区，I 份额为零行代码**：进程内分解（双引擎几乎逐位重合）@5987 KB：readFile 4.4-5.1 + `JSON.parse` **10.9-11.2**（引擎解析地板，R20-I 族）+ `parseRegistrySnapshot`（**D**）3.0-3.1 + `ResourceRegistry.fromSnapshot`（**D**）4.1-4.3 = `loadAdaptationRegistry` e2e 24.6-26.3 ms（温）；消费段 `getActiveContent`（D）0.7-1.4 µs、`hashCandidateContent` 校验（D）0.3-0.4 µs、`parseLearnedRoutingPolicy`（E）0.7-1.7 µs。温装载 **4.3-5.1 ns/字节**、冷装载 6.2-8.8 ns/字节——包住 R21-D 的 ~6-7 ns/字节（其混合制度）；children 端到端边际 @XL 的一半（40.9-41.3）逐位落进 R21-D load e2e @6.29 MB（40.5-43.9）——**跨切片曲线互锚闭合**。main.ts L708 / track/loop.ts L88 只是调用点，装载与消费机器全部在 adaptation（D）/ learning（E）文件内。
   - **三线互证闭合**：e2e children 边际 ≈ 冷+温探针和（@L：19.3-25.0 vs 20.0-21.1 逐位级；@XL：81.8-82.6 vs 67.5-68.1，闭合 ~82%，残差 ≈ 6 MB 串与对象图在忙进程内的分配/GC——engine 辖区）；flowchart 单装载边际（+14.5/+5.5 @L）落在温装载（7.7-7.9）与冷装载（12.3-13.2）之间（进程中段半温制度，符合 supervisor 装载位置）；adapt-status 单冷装载 @XL 边际 +71.3/+65.1 = 冷装载 37-38 + `registry.snapshot()` 防御性克隆 + **5000 行/631 KB stdout**（statusCommand L112-117 逐 proposed 候选打印一行——实测 5009 行）——与 R21-D「status 体」38-47 ms @6-7.5 MB 同族吻合。
   - **行为等价见证（Live=R0 不变量）**：注册表缺席 / 基线命中 / 外来失手三态的 children run 输出**规范化后逐字相同**（路由決策、任务分派、事件计数全同；仅 run/episode id 不同）——空 avoid/prefer 基线策略经 `applyLearnedRouting` 为恒等，R0 等价不变量在配置态下直接见证。
4. **诚实更新派单前提 + 数据面移交注记（散文记录，不铸 ID）**：R23-I 宣告的「两个配置态坐标系全部收口」不完备——本轮补上第三坐标系（适应态）并收口之。该平面的增长率上界是 ~1 候选/run（contentHash 去重下需批剖面新颖），C\*≈410-480 意味着数百次带 actionable failing 剖面的真实付费 run（R8-I/R20-I 相对成本裁决族）；但与 R20-I（N\*≈2300-2700 条）不同，本平面**每次提案还全量重写整文件**（saveAdaptationRegistry：整快照 pretty stringify + `wx` + fsync + rename，promotion.ts L219-241——R21-D save 腿已定价 28-38 ms @6-7.5 MB），累积重写字节 O(C²)。注册表无轮转不是本轮新发现的孤立事实（R21-D 死路行已裁「版本剪枝/链紧缩/台账分段=审计契约变更」），但「承重 run 类每次付双倍装载、且 10 ms/run 越线点因此比 D 侧单装载低 2.6-3.1×」是新钉的接线事实——移交 D/E 契约所有者（若未来立项候选退休策略属功能/数据面决策），移交不代行。
5. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**；r7i part D 载荷探针复证 builtin 构建后 providers/all 保持冷 58.2 ms、custom 回退命中缓存 2.0 ms）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R23-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动，无 r24i 新仿真（派单：仅落地代码才须新增）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读读码，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交任何 scripts/（全部 harness 源码进附录 A，遵守 R7-F/R8-C/R9-I～R23-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README 未触碰；未触碰在飞 R24-J 分支与文件（快进吸收的 R24-G/H 为父代理已收口分支）。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S23-\* 条目；R9-I～R23-I 空枚举维持，未事后发明 S9-I-\* ～ S23-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：零漂移即字节级证明（站点逐项重验，见结论第 1 条），三个等价性仿真复跑为行为级证明。S4-I 败者为 **S4-I-2..5**，无 S4-I-1；赢家 ID 为 **S5-I-1**。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅做文件级重开条件确认（§2.2），未满足，维持淘汰；未构造替代注册表、未触碰回退路径。**两臂文件级 blocked 按派单原样维持，未重复执行级探针。**
  - **与 R21-D「状态增长方向面」的显式划界（本轮最关键划界）**：R21-D 在 **D 辖区进程内**对注册表机器本体做了 V/C/L 剂量与方向敏感性标定（promote/save/load/status/rollback 逐操作、深度链 vs 广度行、每字节 ~6-7 ns、最早越线文件 ~1.5 MB、剪枝/分文件死路行）。本轴测的是 **I 矩阵的配置态坐标与接线**：registry.json 存在与否在承重 CLI 命令类 spawn-to-exit 面上的剂量（历轮矩阵从未有此坐标）、**每 run 双装载乘数**（main L708 + startFlowchartRun L747——R21-D 的进程内制度看不见接线次数）、冷/温不对称、以及承重类 10 ms/run 越线点被乘数压低 2.6-3.1× 的推论。两个共享标定点（1.5 MB / 6 MB）用作跨切片曲线互锚而非重测：本轮温装载 4.3-5.1 ns/字节、边际半值 40.9-41.3 @6 MB 均落回 R21-D 曲线。未重测 promote/rollback/方向敏感性任何一格，未动 D 机器一行。
  - **与 R24-D「approval-authority face」的显式划界**：其剂量对象是 promote 路径的自动提升模式 × 预算标量（其报告明记 registry.json 尺寸三档恒等 1844.7 KB 以反证无状态增长混入）；本轴剂量对象恰是其刻意钉死的那个变量（文件字节），站点在 run 头部装载而非 promote 门链。零交集。
  - **未重铸 R23-I `.pi/` 树平面**：全部夹具项目无 `.pi/` 树（F=0）；**未重铸 R22-I run 记录回读平面**：无 inspect/commits/episode 规模格；**未重铸 R21-I flowchart 平面**：flat64 规格维持双态承重形态未扩展；**未重铸 R20-I 累积遥测 N 轴**：全部夹具 `invocations.jsonl` 不存在（N=0）——其对象是 telemetry 装载税（`loadInvocationsFromStateRoot`，切片内 telemetry 文件参与），本轴对象是 adaptation 注册表装载（切片外 D/E 机器），文件、读者、辖区、消费者（成本标定 vs 学习路由策略）四不同；**未重铸 R19-I E×P×K**——custom 夹具维持 lineage P=1/K=2/E=2；**未重铸 R18-I children 基数轴**——C=2。
  - **未换名移植兄弟轴**（派单 + 快进后 PLAN 增补逐项）：R24-A list-dimension 普查（A 区输入面维度普查——本轴非维度普查，是配置态坐标）/ R24-B M=1 catalog collapse（B 区 M 轴地板）/ **OCCX**（C 区占据剂量）/ R24-D（上文已划界）/ **JB**（E 区 join/绑定命运）/ R24-F（F 区 restore 出处面）/ **RUNLIM**（G 区 run 限额配置面——本轴不触 limits 任何字段）/ R24-H operand-residency census（H 区操作数驻留）/ CALLB / CKMIX / ORDX / SEEDX / YMIX / SCALEX / FITQ / TERMCLASS / SCHEDWIN / XPROC / SHAPEK / KFAN / NVG / GS / R23-A G×E / R23-B batch arrangement——定位核过，与本轴对象机制无交集。**与 R19-G NAMESHAPE 划界**：其对象是 `runtime/runs/` 目录条目扇出的几何，本轴对象是单个 JSON 文件的内容字节——无目录行走成分。**与 R13-B「learned-size 格」划界**：其对象是 B 辖区 eval 面上已装载策略的规模效应（进程内），本轴对象是装载本身在 CLI spawn 面的字节税——B 格假设策略已在手，本轴定价把它拿到手的过程。
  - **未重开 S8-I-2 / S8-I-3 / S7-I-2..4 / S6-I-1..3 / S5-I-2..5 / S4-I-2..5 / S3-I-1..6 / S2-I-1..6 / S1-I-1..8**；R10-I～R23-I 全部无 ID 收口维持，其结构前提（39 表、exports 图、pi-ai 版本、调用图）本轮复核未变。**S2-I-1（普通 run 死载荷 loadLearnedRouting 下沉）未重提**——本轴对其站点只做剂量观测，未提议移动/下沉任何装载。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；**Live=R0 等价——本轮以三态输出规范化全同直接见证**；双 LCB / 双归因在位；提升 proposal-first（Checkpoint F-PROD 开放；夹具候选全部 proposed 态，无任何提升）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`providers/all` 失手回退 + throw-outside-catch 在位；无 fsync / 完整性再哈希削除（saveAdaptationRegistry 的 fsync 与 hashCandidateContent 校验原样，仅被测量）；fail-open 族保持关闭；未改任何测试。**新轴全程零代码改动、零行为差异**——所有测量都是对现行装载路径的直接观测 + 合法夹具注入（经生产机器 `ResourceRegistry`/`ensureRoutingBaseline`/`createCandidate`/`saveAdaptationRegistry` 生成的合法快照，形态即 auto-loop 生产增长形态），未触碰任何 loader/validator/registry 一行。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；未 force-push；未触碰在飞 R24-J 分支与文件。

## 1. 本 VM 复测：8 格承重抽测 + 新轴 10 格（双 Node，未拷贝历轮数字）

R9-I～R23-I 抽测集承重面复刻（含 flowchart 双态与 n1-exec lineage 格）并**新增 10 个适应态格**（configured 态夹具、每迭代全新拷贝 state-root【注册表含在拷贝内，拷贝在计时外】、2 预热 + 10 测量、显式二进制全路径；harness 全文附录 A.2）。

### 1.1 抽测中位数（ms，spawn-to-exit，全部 exit 0）

| 格 | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|
| `--version`（地板） | 54.3 (min 52.6, p90 55.5) | 51.6 (49.2, 52.9) |
| run-children@builtin（C=2） | 110.8 (106.5, 120.2) | 101.4 (95.6, 104.2) |
| run-track@builtin | 125.7 (121.7, 132.4) | 120.5 (115.8, 122.0) |
| run-children@custom（P=1/K=2/E=2） | **155.0** (147.3, 161.4) | **127.9** (125.7, 129.2) |
| run-track@custom | **173.7** (171.0, 186.0) | **146.4** (139.5, 150.4) |
| run-flowchart@custom-n1-exec | 127.0 (121.8, 133.6) | 104.6 (102.8, 107.3) |
| run-flowchart@custom-flat64 | 259.1 (247.7, 266.0) | 232.2 (223.0, 237.5) |
| run-flowchart@builtin-flat64（态对照） | 210.4 (206.7, 213.8) | 215.9 (209.6, 220.0) |
| **run-children@custom-regS**（144.2 KB，本轮新格） | 157.3 (153.9, 168.1) | 139.0 (136.9, 142.3) |
| **run-children@custom-regM**（790.2 KB） | 165.0 (160.6, 167.7) | 149.9 (144.2, 154.1) |
| **run-children@custom-regL**（1496.4 KB，R21-D 越线锚点） | **174.3** (165.9, 246.1) | **152.9** (150.1, 156.0) |
| **run-children@custom-regXL**（5987.1 KB，R21-D 顶锚点） | **237.6** (218.7, 252.3) | **209.7** (197.5, 232.7) |
| **run-children@custom-regL-miss**（同字节，外来项目身份） | 175.5 (167.9, 188.5) | 155.4 (147.3, 172.7) |
| **run-track@custom-regL**（命令类证人） | 191.2 (187.8, 198.4) | 171.0 (165.3, 176.7) |
| **run-children@builtin-regL**（态证人，1508.6 KB） | 127.5 (123.9, 133.2) | 124.0 (120.4, 127.8) |
| **run-flowchart@custom-flat64-regL**（单装载判别格） | 273.6 (260.8, 284.9) | 237.7 (229.4, 243.8) |
| **adapt-status@custom-plain**（一次性读者基线） | 59.5 (58.8, 61.5) | 53.8 (52.7, 55.3) |
| **adapt-status@custom-regXL** | 130.8 (122.2, 136.1) | 118.9 (113.9, 124.8) |

- **custom−builtin 差**：children **+44.2 / +26.5**、track **+48.0 / +25.9**、flowchart-flat64 **+48.7 / +16.3** ms（22.14/22.22）。R23-I 带 +42.9/+20.7、+53.5/+21.8、+39.2/+18.5，更早十五轮见 R19-I §1.1 汇编——**十七轮同号、同量级、同 Node 版本梯度**。带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内（22.14 单窗 maxAbs 34.5 ms、22.22 8.7-10.4 ms），无新结构。归属未变。
- **builtin 态目录构建**比地板贵 +56.5/+49.8（children，双引擎）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。本 VM 地板（54.3/51.6）与 R22-I VM（54.0/50.6）几乎逐位。
- **flowchart 交叉核**：n1-exec（127.0/104.6）与 R23-I（125.5/101.3）同带；flat64 双态差与 children/track 带同号同量级（第五次复现）。

### 1.2 新轴的生产流证明（先证流量与增长，再证遮蔽，再证数字）

- **流量（每承重 run 无条件、双计费）**：`loadLearnedRouting(stateRoot, projectRoot)`（learning/learned-routing.ts L115-134）在每次 run 头部执行：`run --children`/plain 于 main.ts **L708**（children 分支进入前，S2-I-1 在案的无条件站点）、`run --track` 于 track/loop.ts **L88**；随后 children/track 汇入 `startFlowchartRun`（track/loop.ts L152 / main.ts children 分支），其 **L747** `flowchartForSupervisor`（flowchart-run.ts L707-714）**再次**调用 `loadLearnedRouting`——**children/track 每 run 两次整装载**；`run --flowchart` 只经 L747（CLI 分支不预装，R21-I 在案）、resume/answer 只经 L880/L1021——**一次装载**。装载体：`loadAdaptationRegistry`（adaptation/promotion.ts L176-198）= readFile `adaptation/registry.json`（L172-174）+ `JSON.parse` + `parseRegistrySnapshot`（L250+，逐 version/candidate/ledger/blob 全量校验）+ `ResourceRegistry.fromSnapshot`（registry.ts L82-86 restore）；命中时再走 `getActiveContent`（registry.ts L169）→ `hashCandidateContent` 完整性校验（learned-routing.ts L130-132）→ `parseLearnedRoutingPolicy`（L133）。ENOENT 时 L123-126 早退返回 undefined——**历轮全部夹具走的就是这条腿**。另有条件性第三次装载：auto-loop 提案腿 `loadAdaptationRegistryOrNew`（auto-loop.ts L166，仅 actionable failing 剖面触发）+ 保存（L190/193）；一次性命令类 adapt status/learn/promote/rollback 各一次（adapt.ts L111 等）。
- **写者与增长可达性**：注册表由本工具自产——auto-loop `proposeAndMaybePromote`（L150-201）对新颖内容铸候选：`candidatesFor(identity).find(contentHash)` 去重（L173-175），但 `optimizedPolicy`（L139-147）的 avoid reason 串含 `meanScore.toFixed(2)` 与 `samples`——**诊断批剖面任一位变化即新 contentHash 即新候选**（增长率上界 ~1 候选/run）；`adapt learn --run` 显式铸造。每次提案 `saveAdaptationRegistry` 全量重写（pretty stringify + `wx` + fsync + rename，promotion.ts L219-241）。**全仓无轮转**：privacy/deletion.ts 两把删除刀只删 `runtime/runs|episodes`（不触 `adaptation/`）；adaptation/retirement.ts 在 src/cli 与 src/learning 无任何调用方（grep 直接确认）。夹具经生产机器生成（附录 A.1）：基线 = `ensureRoutingBaseline` 的 R0 等价空策略；候选 = optimizedPolicy 生产形态（1.20 KB/候选）；四剂量 144.2 KB / 790.2 KB / 1496.4 KB / 5987.1 KB（后两点对齐 R21-D 曲线锚点）。
- **遮蔽证明（R7-I 教训第三坐标系）**：R8-I 78 格矩阵与 R9-I…R23-I 全部抽测格的 state-root 夹具都只有 `runtime/providers.json`——`adaptation/registry.json` 不存在 ⇒ 每格的两次 `loadLearnedRouting` 都走 ENOENT 早退。历轮「配置态」叉乘过 providers 态（R14-I）与项目态（R23-I），**适应态（注册表有无 × 字节规模 × 身份命中/失手）从未进入矩阵**。
- **正交性**：与六个既有输入平面的文件、读者、辖区两两不同（§0 逐项）；剂量注入只改 `adaptation/registry.json` 一个文件；项目无 `.pi/` 树、invocations N=0、children C=2、flowchart 规格未扩展。

### 1.3 轴 1：适应态坐标标定（本轮新证据，不铸 ID）

**(a) 端到端剂量（相对各自注册表缺席基线格的差，ms，22.14/22.22）**：

| registry.json | children@custom | track@custom | children@builtin | flowchart-flat64@custom（单装载） | adapt-status（一次性） |
|---|---|---|---|---|---|
| 144.2 KB（C=120） | +2.3 / +11.1 | — | — | — | — |
| 790.2 KB（C=660） | **+10.0 / +22.0** | — | — | — | — |
| 1496.4 KB（C=1250） | **+19.3 / +25.0** | +17.5 / +24.6 | +16.7 / +22.6 | +14.5 / +5.5 | — |
| 1496.4 KB（外来身份） | +20.5 / +27.5 | — | — | — | — |
| 5987.1 KB（C=5000） | **+82.6 / +81.8** | — | — | — | **+71.3 / +65.1** |

字节线性、无悬崖；S/M 点的双引擎散差（+2.3 vs +11.1）在 A/A 包络内，干净数字由 (b) 探针给出。命令类无关、态无关、**身份命中/失手无关**（miss ≈ hit：消费段 µs 级，字节装载支配）⇒ 站点唯一（`loadLearnedRouting` 装载体）。flowchart 边际落在温/冷单装载带内 ⇒ 单装载腿证实。

**(b) 冷/温双装载探针**（附录 A.4/A.5；子进程复刻承重 run 的精确装载序列——load1 = main L708 冷引擎、load2 = L747 同进程温引擎；9 进程取中位；ms，22.14/22.22）：

| registry.json | load1（冷） | load2（温） | 每 run 税（和） |
|---|---|---|---|
| 144.2 KB | 3.16 / 4.55 | 1.31 / 1.35 | **4.47 / 5.90** |
| 790.2 KB | 7.73 / 8.62 | 5.14 / 5.04 | **12.87 / 13.66** |
| 1496.4 KB | 12.31 / 13.21 | 7.69 / 7.90 | **20.00 / 21.11** |
| 5987.1 KB | 38.20 / 37.40 | 29.33 / 30.69 | **67.53 / 68.09** |

**10 ms/run 越线点：B\* ≈ 0.49-0.57 MB ≈ C\* ≈ 410-480 个累积候选**（双引擎线性内插）；e2e 边际在 M 点（790 KB）双引擎均已 ≥10 ms 与之相容。冷/温比 1.2-3.4（validation 循环 JIT 温化），装载价：温 **4.3-5.1 ns/字节**、冷 6.2-8.8 ns/字节。

**(c) 进程内温分解**（附录 A.3；双引擎几乎逐位重合，22.14/22.22 并示；ms）：

| 段 | 辖区 | 144.2 KB | 790.2 KB | 1496.4 KB | 5987.1 KB |
|---|---|---|---|---|---|
| readFile | fs | 0.19/0.24 | 0.68/0.81 | 0.99/1.20 | 4.35/5.13 |
| JSON.parse | engine | 0.23/0.23 | 1.42/1.99 | 2.65/2.62 | **11.24/10.93** |
| parseRegistrySnapshot | **D** | 0.21/0.22 | 0.38/0.41 | 0.82/0.81 | 2.99/3.10 |
| ResourceRegistry.fromSnapshot | **D** | 0.09/0.09 | 0.48/0.50 | 1.13/1.18 | 4.14/4.31 |
| **loadAdaptationRegistry e2e** | **D** | 0.80/0.87 | 4.11/4.62 | 6.83/6.81 | **24.64/26.31** |
| getActiveContent | **D** | 1.2-1.4 µs | 0.7 µs | 0.7 µs | 0.7-0.8 µs |
| hashCandidateContent 校验 | **D** | 0.3-0.4 µs | 0.3 µs | 0.3 µs | 0.3 µs |
| parseLearnedRoutingPolicy | **E** | 1.6-1.7 µs | 0.7 µs | 0.7 µs | 0.7 µs |
| **loadLearnedRouting e2e（hit）** | **E 入口** | 0.64/0.72 | 3.62/3.61 | 6.52/6.48 | **25.19/25.13** |
| loadLearnedRouting e2e（miss） | | 0.61/0.96 | 3.80/3.59 | 6.58/6.48 | 25.37/25.52 |

`JSON.parse`（引擎解析地板，R20-I 族）支配 ~42-45%；D 辖区校验/重建 ~28-30%；fs ~18-20%；**消费段（含唯一 E 行为面）全部 µs 级且与规模无关**（活动内容是恒定的基线小串）。I 切片份额：**零行**（调用点 main.ts L708 一行 await 不构成成本体）。

**(d) 三线互证闭合**：children e2e 边际 ≈ 探针和（@L 逐位级、@XL 闭合 ~82%，残差 ≈ 大对象图分配/GC——engine）；children 边际半值 @XL（40.9-41.3）≈ R21-D load e2e @6.29 MB（40.5-43.9，其 §2a）——**跨切片互锚**；adapt-status 边际 @XL（+65.1/+71.3）= 冷装载（37.4-38.2）+ snapshot() 克隆 + O(C) stdout（实测 5009 行/631 KB）——与 R21-D「status 体」同族。**行为等价**：三态（缺席/命中/外来失手）children run 输出规范化后逐字相同（附录 A.6）。

## 2. 第二十四组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 输入规模定价 / 配置态坐标系补全），每形态先对 EXCLUSIONS 全表 + 二十三轮 I 报告 + 八个兄弟禁移植轴做同机制先例检索。

### 2.1 新轴候选性自查——测量收口，不铸 ID

- **轴 1（适应态坐标）**是既有装载路径在第三配置态坐标系上的直接观测：零行为差异、零代码改动。产出是「承重类每 run 双装载 × 字节线性税 + B\*≈0.49-0.57 MB / C\*≈410-480 最新可达越线点 + 税体 100% fs/engine/D + 消费段 µs + R0 等价三态见证」的裁决数字集，不是优化。
- **派单空转卫自查**：本轴有真实生产流（每次 track/children run 无条件双装载 + 工具自产单调增长文件 + USAGE 文档化 adapt 命令类），核心越线点在**可达域内**（C\*≈410-480 = 数百次带 failing 剖面的付费 run；对照 R20-I N\*≈2300-2700 更近一个量级）——不属「零生产流 + 仅幻想尺度」空转型。不铸候选的理由是三重的：税体 100% 在 fs/engine/D 辖区（I 份额零行代码；唯一 E 消费段 µs 级规模无关）、全部消除形态映射既有围栏（§2.3）、相对成本裁决（每候选 = 一次真实付费 run 的新颖 failing 剖面，R8-I/R20-I 族）。

### 2.2 S8-I-1 重开条件文件级再确认——不铸 ID

- `dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 **0.84.1**。依派单「两臂文件级 blocked」纪律不重复执行级探针。
- **裁决**：重开条件连续第十七轮未满足；所需的是 pi-ai 上游导出，不是仓内改动。审计证据，非机制候选——**不铸 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级/辖区即死）

| 扫描角度 | 处置 |
|---|---|
| **双装载去重**（把 main L708 / track L88 的 learned 下传 `startFlowchartRun`，消除 L747 重装） | **S2-I-4 族逐字**（需改 run 公开签名——`FlowchartRunInput`/`startFlowchartRun` 是 G 契约公开面；S1-I-2 双读族同型）。且天花板 = 省一次温装载：生产尺度（≤146 KB 带）0.6-0.7 ms/run——低于落地线一个量级；ms 级只在 C\* 以上的累积态出现，而该态可达性即相对成本裁决。维持关闭 |
| **跨进程注册表缓存 / mtime 键控快照** | R10-I 跨进程磁盘快照围栏 + X1-1 隐藏持久状态族逐字。非候选 |
| **普通 run 死载荷装载下沉** | **S2-I-1 排除行逐字**（损坏 registry 时错误路径发散）。未重提 |
| **部分/惰性快照解析**（只解析 active 指针及其内容，跳过 candidates/ledger 校验） | R21-D 死路行逐字（「快照 schema 公开面——架构级不可提案域」）；全量 fail-closed 校验是审计契约本体（损坏候选必须在装载时报错，不是在 promote 时）。语义降级，非候选 |
| **hashCandidateContent 校验跳过** | 完整性再哈希削除——硬不变量明令禁止；且实测 0.3-0.4 µs，无收益 |
| **注册表轮转/候选退休工具** | 功能/数据面决策（retirement.ts 存在但无 CLI 接线是产品决策）；R21-D 死路行 +本轮增长率注记一并移交 D/E 契约所有者（结论第 4 条），不铸 ID |
| **JSON.parse 支配段** | 引擎原生解析地板（R20-I 已测定族）；流式/增量 JSON 解析器 = 依赖面 + 平台面变更（S9-C-2 同族）。非候选 |
| **adapt-status O(C) stdout / snapshot() 克隆** | once-per-invocation 诊断命令类——派单验收明文 reject 类（S2-I-5 / R17-I / R22-I 同口径）；克隆是防御性拷贝语义（S4-C-3 族维持存在性） |
| **提案腿 save 全量重写攒批/增量** | R21-D save 腿已定价 + fsync 削除禁令 + append/覆写语义是 D 数据面契约（tmp+fsync+rename 是崩溃安全本体）。D 辖区，非 I 候选 |
| ingest/回读/flowchart 规模/配置基数/children 基数族 | R23-I / R22-I / R21-I / R19-I / R18-I 收口维持，本轮未触其规模格 |
| custom id 跳回退 / 自制 union / 磁盘快照 / 失手 memo | S8-I-1 硬禁 + R10-I/R11-I 三重围栏；重开条件文件级仍未满足（§2.2） |
| 拒绝路径 / 错误端点 / 肥 builtin 态 / A/A 地板 / 引擎代 / 堆足迹 / 采样归因 / 测量学 | R17-I / R14-I / R15-I / R16-I 收口维持；本轮带比对直接引用其 MDE 包络（S/M 点双引擎散差即其包络内案例） |
| flowchart 双 build / providers.json 双读 / pause-inject 免 router / compile-cache / dist 布局 / 一次性命令类 / 凭据面 / 解析微观 / barrel 隐藏边 / root-compat 替代源 / per-provider 探针短路 / 预热重叠 | S1-I-1/2、S2-I-1..6、S3-I-1..6、S4-I-2..5、S5-I-2..5、S6-I-1..3、S7-I-2..4、S8-I-2/3、R11-I/R12-I/R13-I 无 ID 收口逐字重现——零漂移下前提逐项成立，本轮 25 文件重读未发现任何新站点脱离既有覆盖物 |
| telemetry / config 残余面 | R20-I 已把 telemetry 热站点定价贴底；本轮新坐标不触 telemetry 文件；S1-D-8 / S3-I-5 族噪声维持 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖、pi-ai exports、测试面、基线增量五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可落地候选；本轮把 I 切片**第三个配置态坐标系（适应态）**收口：承重 run 类每 run 双装载的字节线性税（B\*≈0.49-0.57 MB / C\*≈410-480 为本切片继 R23-I F\*≈62-68 之后又一可达域内越线点，但税体 100% 在 fs/engine/D 辖区、I 份额为零行）、身份命中/失手同价、消费段 µs 定价、R0 等价三态见证、与 R21-D 曲线的跨切片互锚。**I 切片自此六个输入平面 + 三个配置态坐标系（providers 态 × 项目态 × 适应态）全部收口。**

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx；无代码落地故按派单为自愿加测）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part B：1220 builtin 行 × 39 providers lazy===sync；part C：17 组失手/custom/对抗用例；part D 载荷探针：builtin 构建后 providers/all 保持冷 58.2 ms、custom 回退命中缓存 2.0 ms；part E：configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单项，Node 22.22.2）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**（与 R9-I～R23-I 计数逐字相同）、`pnpm build` 绿。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动；无赢家故无新仿真脚本入库；五个 harness/探针（genfix/spot/inproc/coldload/coldharness）均为 /tmp 临时件，全文进附录 A，未进 lint 面。
- **引擎注记**：22.14.0 双引擎并测用于十七轮带比对；nvm 22.22.2 为 gate/仿真引擎。依 R15-I 轴 1 纪律，候选验收须在 ≥22.19 引擎上做——本轮无候选，新轴归因不依赖包络判读（冷/温探针 + 进程内分解 + 三态行为见证 + R21-D 互锚四线互证）。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；新轴以测量收口关闭第三配置态坐标系；S8-I-1 文件级第十七轮 blocked）。
- 剩余赢家：无（custom−builtin 结构维持 S8-I-1 健全性反例封死；适应态平面的数十 ms 结构税体 100% 在 fs/engine/D 辖区——I 份额为零行代码，唯一 E 消费段 µs 级规模无关；全部消除形态映射 S2-I-4 / S2-I-1 / R10-I / X1-1 / R21-D 死路行 / fsync·完整性禁令既有围栏）。
- 矩阵空洞：无（R8-I 78 格全覆盖；承重格十七轮同构复现 + 本轮补入适应态坐标 10 格——B×{children,track,builtin,flowchart,adapt-status} × 身份{hit,miss} 的归因完备组合；R7-I 纪律在第三配置坐标系上重演并收口；六输入平面维持 R18-I…R23-I 收口）。
- 跨辖区移交注记一条已在结论第 4 条记录（D/E 契约所有者辖区：双装载接线事实 + 无轮转增长率 + O(C²) 累积重写），非 I 候选。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 夹具生成（/tmp/r24i-bench/genfix.mjs）

```javascript
// R24-I fixture generation: adaptation-state configured coordinate (registry.json
// presence + scale) on the bearing run classes. Every prior I-round fixture had
// NO adaptation/registry.json => loadLearnedRouting's ENOENT early-return was the
// only leg the I matrix ever exercised (R7-I lesson, third configuration
// coordinate after state-root providers state and project .pi state).
// Registries are generated through the production machinery (ResourceRegistry +
// ensureRoutingBaseline + createCandidate + saveAdaptationRegistry) so snapshots
// are valid by construction. Growth form mirrors production: auto-loop /
// adapt-learn candidate proposals (breadth), content = optimizedPolicy shape.
import { mkdirSync, rmSync, writeFileSync, statSync } from "node:fs";
import { join } from "node:path";
import { ResourceRegistry } from "/workspace/dist/adaptation/registry.js";
import { saveAdaptationRegistry } from "/workspace/dist/adaptation/promotion.js";
import {
  ensureRoutingBaseline,
  routingPolicyContent,
  routingPolicyIdentity
} from "/workspace/dist/learning/learned-routing.js";

const ROOT = "/tmp/r24i-bench";
const FIX = join(ROOT, "fixtures");

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
function writeState(name, cfg) {
  const dir = join(FIX, name);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
  return dir;
}
writeState("state-builtin", states.builtin);
writeState("state-custom", states.custom);

// Children spec (R9-I..R23-I lineage, C=2).
writeFileSync(join(FIX, "children.json"), JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");

// Flowchart fixtures (R21-I flat shapes; bearing cells only).
function flatFlow(n) {
  const nodes = [];
  for (let i = 0; i < n; i++) {
    nodes.push({
      id: `n${i}`, taskId: `tsk_f${i}`, role: "actor",
      objective: `Step ${i} of the benchmark flow`,
      modelPolicy: { allowedModels: ["cheap"] },
      confidenceThreshold: 0.7, approvalRequired: false
    });
  }
  return { id: `bench-flat-${n}`, nodes, edges: [] };
}
function resultsFor(n) {
  const out = {};
  for (let i = 0; i < n; i++) {
    out[`n${i}`] = {
      outcome: "SUCCESS", confidence: 0.9,
      evidenceIds: [`evd_f${i}`],
      facts: [{ key: `fact-${i}`, value: `value ${i}`, confidence: 0.8 }]
    };
  }
  return out;
}
for (const n of [1, 64]) {
  writeFileSync(join(FIX, `flow-flat${n}.json`), JSON.stringify(flatFlow(n)) + "\n");
  writeFileSync(join(FIX, `results-flat${n}.json`), JSON.stringify(resultsFor(n)) + "\n");
}

// Project scaffold (R9-I..R23-I shape; no .pi tree — R23-I plane stays closed).
function writeProjectBase(dir) {
  mkdirSync(join(dir, "src"), { recursive: true });
  writeFileSync(join(dir, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
  writeFileSync(join(dir, "src", "index.js"), "export const x = 1;\n");
}
const PROJ = join(FIX, "project");
writeProjectBase(PROJ);
const OTHER_PROJ = join(FIX, "other-project"); // identity-miss leg
writeProjectBase(OTHER_PROJ);

// --- NEW axis: adaptation registry fixtures ---------------------------------
// Baseline is the R0-equivalent empty policy (avoid=[], prefer=[]) registered
// exactly as ensureRoutingBaseline does in production; behaviour of the run is
// unchanged (applyLearnedRouting with empty avoid/prefer is identity).
// Candidates carry optimizedPolicy-shaped contents (auto-loop production form,
// distinct content hashes => distinct proposals, the real growth mechanism).
const PRIMARY = { custom: "customco/custom-big", builtin: "anthropic/claude-fable-5" };
const FAMS = ["gpt", "claude", "gemini", "grok", "qwen", "deepseek"];
function candidatePolicy(i, primaryModelId) {
  const fam = FAMS[i % FAMS.length];
  const avoid = [{
    modelId: `${fam}-legacy-${i % 97}`,
    family: fam,
    reason: `meanScore ${(0.1 + (i % 40) / 100).toFixed(2)} over ${3 + (i % 9)} samples`
  }];
  if (i % 3 === 0) {
    avoid.push({
      modelId: `${fam}-mini-${i % 53}`,
      family: fam,
      reason: `meanScore ${(0.05 + (i % 31) / 100).toFixed(2)} over ${3 + (i % 7)} samples`
    });
  }
  const prefer = [{ family: fam, modelId: primaryModelId }];
  // salt guarantees distinct content hashes across i (distinct proposals)
  return { primaryModelId, avoid: avoid.map((a, k) => ({ ...a, reason: `${a.reason} [w${i}.${k}]` })), prefer };
}
async function makeRegistryState(name, baseCfg, primaryModelId, identityProjectRoot, candidates) {
  const dir = writeState(name, baseCfg);
  const registry = new ResourceRegistry({});
  const identity = routingPolicyIdentity(identityProjectRoot);
  const baseline = ensureRoutingBaseline(registry, identity, primaryModelId, "pi-sparkle-auto-loop");
  for (let i = 0; i < candidates; i++) {
    registry.createCandidate({
      identity,
      content: routingPolicyContent(candidatePolicy(i, primaryModelId)),
      parentVersionId: baseline.versionId,
      author: { kind: "detector", identity: "pi-sparkle-auto-loop" },
      evaluationPlan: { stages: ["static", "replay"], metrics: ["task-success", "cost"], planVersion: 1 }
    });
  }
  await saveAdaptationRegistry(dir, registry);
  const bytes = statSync(join(dir, "adaptation", "registry.json")).size;
  console.log(`${name}: C=${candidates} registry.json=${(bytes / 1024).toFixed(1)} KB`);
  return bytes;
}

// Doses (bytes calibrated by print): S ~ R21-D production witness (~146 KB),
// M ~ the 2x-wiring 10 ms/run neighbourhood, L ~ R21-D single-load 10 ms
// crossing (~1.5 MB), XL ~ R21-D top anchor (~6 MB, pure cross-check point).
await makeRegistryState("state-custom-regS", states.custom, PRIMARY.custom, PROJ, 120);
await makeRegistryState("state-custom-regM", states.custom, PRIMARY.custom, PROJ, 660);
await makeRegistryState("state-custom-regL", states.custom, PRIMARY.custom, PROJ, 1250);
await makeRegistryState("state-custom-regXL", states.custom, PRIMARY.custom, PROJ, 5000);
// identity-miss leg: same bytes, registry keyed to a different project
await makeRegistryState("state-custom-regL-miss", states.custom, PRIMARY.custom, OTHER_PROJ, 1250);
// provider-state witness
await makeRegistryState("state-builtin-regL", states.builtin, PRIMARY.builtin, PROJ, 1250);
console.log("FIXTURES_WRITTEN");
```

取证输出：

```
state-custom-regS: C=120 registry.json=144.2 KB
state-custom-regM: C=660 registry.json=790.2 KB
state-custom-regL: C=1250 registry.json=1496.4 KB
state-custom-regXL: C=5000 registry.json=5987.1 KB
state-custom-regL-miss: C=1250 registry.json=1496.4 KB
state-builtin-regL: C=1250 registry.json=1508.6 KB
FIXTURES_WRITTEN
```

（1.197 KB/候选；S 对齐 R21-D 生产见证字节【组成为候选宽度型——本轴的生产增长形态】，L/XL 对齐其越线/顶锚点。）

### A.2 18 格抽测 harness（/tmp/r24i-bench/spot.mjs，分别以两版本二进制执行）

```javascript
// R24-I spot measure: R9-I..R23-I bearing set (floor + builtin/custom x
// children/track/flowchart) + NEW axis: adaptation-state configured coordinate
// (adaptation/registry.json presence + byte scale) on the bearing run classes.
// Every prior I-round fixture had no registry.json => loadLearnedRouting's
// ENOENT early-return was the only leg ever exercised (R7-I lesson, third
// configuration coordinate). Doses anchored to R21-D's state-growth curve:
// S ~ its production witness (144 KB), M ~ the 2x-wiring 10 ms/run
// neighbourhood (790 KB), L ~ its single-load 10 ms crossing (1496 KB),
// XL ~ its top anchor (5987 KB). children/track pay the load twice per run
// (main.ts L708 / track-loop L88 + startFlowchartRun L747 supervisor load);
// run --flowchart pays once (supervisor only) — within-axis discriminator.
// usage: <node-binary> spot.mjs
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r24i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.execPath;
const WARMUP = 2, ITERS = 10;

const PROJ = join(FIX, "project");
const CHILDREN = join(FIX, "children.json");

const childrenArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN];
const trackArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"];
const flowResultsArgs = (name) => (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--flowchart", join(FIX, `flow-${name}.json`), "--results", join(FIX, `results-${name}.json`)];
const flowExecArgs = (name) => (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--flowchart", join(FIX, `flow-${name}.json`), "--executor", "fake"];
const adaptStatusArgs = (s) => ["adapt", "status", "--state-root", s];

const cells = [
  // Bearing set (R9-I..R23-I lineage).
  { id: "version", args: () => ["--version"], state: undefined },
  { id: "run-children@builtin", state: "state-builtin", args: childrenArgs },
  { id: "run-track@builtin", state: "state-builtin", args: trackArgs },
  { id: "run-children@custom", state: "state-custom", args: childrenArgs },
  { id: "run-track@custom", state: "state-custom", args: trackArgs },
  { id: "run-flowchart@custom-n1-exec", state: "state-custom", args: flowExecArgs("flat1") },
  { id: "run-flowchart@custom-flat64", state: "state-custom", args: flowResultsArgs("flat64") },
  { id: "run-flowchart@builtin-flat64", state: "state-builtin", args: flowResultsArgs("flat64") },
  // NEW axis: registry byte dose on the bearing children class (2 loads/run).
  { id: "run-children@custom-regS", state: "state-custom-regS", args: childrenArgs },
  { id: "run-children@custom-regM", state: "state-custom-regM", args: childrenArgs },
  { id: "run-children@custom-regL", state: "state-custom-regL", args: childrenArgs },
  { id: "run-children@custom-regXL", state: "state-custom-regXL", args: childrenArgs },
  // identity-miss leg (registry keyed to a foreign project; same bytes as regL).
  { id: "run-children@custom-regL-miss", state: "state-custom-regL-miss", args: childrenArgs },
  // command-class and provider-state witnesses at the L anchor.
  { id: "run-track@custom-regL", state: "state-custom-regL", args: trackArgs },
  { id: "run-children@builtin-regL", state: "state-builtin-regL", args: childrenArgs },
  // single-load discriminator: run --flowchart pays only the supervisor load.
  { id: "run-flowchart@custom-flat64-regL", state: "state-custom-regL", args: flowResultsArgs("flat64") },
  // one-shot in-slice reader witness (adapt.ts L111 loadAdaptationRegistry).
  { id: "adapt-status@custom-plain", state: "state-custom", args: adaptStatusArgs },
  { id: "adapt-status@custom-regXL", state: "state-custom-regXL", args: adaptStatusArgs }
];

function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, state), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 180000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-FAIL ${cell.id}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

原始输出：

```
== v22.14.0 ==
version: median=54.3ms min=52.6 p90=55.5 exit=0
run-children@builtin: median=110.8ms min=106.5 p90=120.2 exit=0
run-track@builtin: median=125.7ms min=121.7 p90=132.4 exit=0
run-children@custom: median=155.0ms min=147.3 p90=161.4 exit=0
run-track@custom: median=173.7ms min=171.0 p90=186.0 exit=0
run-flowchart@custom-n1-exec: median=127.0ms min=121.8 p90=133.6 exit=0
run-flowchart@custom-flat64: median=259.1ms min=247.7 p90=266.0 exit=0
run-flowchart@builtin-flat64: median=210.4ms min=206.7 p90=213.8 exit=0
run-children@custom-regS: median=157.3ms min=153.9 p90=168.1 exit=0
run-children@custom-regM: median=165.0ms min=160.6 p90=167.7 exit=0
run-children@custom-regL: median=174.3ms min=165.9 p90=246.1 exit=0
run-children@custom-regXL: median=237.6ms min=218.7 p90=252.3 exit=0
run-children@custom-regL-miss: median=175.5ms min=167.9 p90=188.5 exit=0
run-track@custom-regL: median=191.2ms min=187.8 p90=198.4 exit=0
run-children@builtin-regL: median=127.5ms min=123.9 p90=133.2 exit=0
run-flowchart@custom-flat64-regL: median=273.6ms min=260.8 p90=284.9 exit=0
adapt-status@custom-plain: median=59.5ms min=58.8 p90=61.5 exit=0
adapt-status@custom-regXL: median=130.8ms min=122.2 p90=136.1 exit=0
== v22.22.2 ==
version: median=51.6ms min=49.2 p90=52.9 exit=0
run-children@builtin: median=101.4ms min=95.6 p90=104.2 exit=0
run-track@builtin: median=120.5ms min=115.8 p90=122.0 exit=0
run-children@custom: median=127.9ms min=125.7 p90=129.2 exit=0
run-track@custom: median=146.4ms min=139.5 p90=150.4 exit=0
run-flowchart@custom-n1-exec: median=104.6ms min=102.8 p90=107.3 exit=0
run-flowchart@custom-flat64: median=232.2ms min=223.0 p90=237.5 exit=0
run-flowchart@builtin-flat64: median=215.9ms min=209.6 p90=220.0 exit=0
run-children@custom-regS: median=139.0ms min=136.9 p90=142.3 exit=0
run-children@custom-regM: median=149.9ms min=144.2 p90=154.1 exit=0
run-children@custom-regL: median=152.9ms min=150.1 p90=156.0 exit=0
run-children@custom-regXL: median=209.7ms min=197.5 p90=232.7 exit=0
run-children@custom-regL-miss: median=155.4ms min=147.3 p90=172.7 exit=0
run-track@custom-regL: median=171.0ms min=165.3 p90=176.7 exit=0
run-children@builtin-regL: median=124.0ms min=120.4 p90=127.8 exit=0
run-flowchart@custom-flat64-regL: median=237.7ms min=229.4 p90=243.8 exit=0
adapt-status@custom-plain: median=53.8ms min=52.7 p90=55.3 exit=0
adapt-status@custom-regXL: median=118.9ms min=113.9 p90=124.8 exit=0
```

### A.3 进程内分解（/tmp/r24i-bench/inproc.mjs，双引擎各执行一次）

```javascript
// R24-I in-process decomposition of the adaptation-state plane.
// Stage sequence replicates loadLearnedRouting exactly (learned-routing.ts
// L115-134): loadAdaptationRegistry (D: readFile + JSON.parse +
// parseRegistrySnapshot + ResourceRegistry.fromSnapshot) -> getActiveContent
// (D) -> hashCandidateContent verify (D) -> parseLearnedRoutingPolicy (E).
// The I slice contributes zero lines to any stage (call sites only:
// main.ts L708, track/loop.ts L88; startFlowchartRun L747 is G).
import { readFile } from "node:fs/promises";
import { join } from "node:path";
import { performance } from "node:perf_hooks";
import { loadAdaptationRegistry, parseRegistrySnapshot } from "/workspace/dist/adaptation/promotion.js";
import { ResourceRegistry } from "/workspace/dist/adaptation/registry.js";
import { hashCandidateContent } from "/workspace/dist/adaptation/candidate.js";
import { loadLearnedRouting, parseLearnedRoutingPolicy, routingPolicyIdentity } from "/workspace/dist/learning/learned-routing.js";

const FIX = "/tmp/r24i-bench/fixtures";
const PROJ = join(FIX, "project");
const OTHER = join(FIX, "other-project");

function median(xs) { const s = [...xs].sort((a, b) => a - b); return s[Math.floor(s.length / 2)]; }
async function timeAsync(reps, warm, fn) {
  const ts = [];
  for (let r = 0; r < reps + warm; r++) {
    const t0 = performance.now();
    await fn(r);
    if (r >= warm) ts.push(performance.now() - t0);
  }
  return median(ts);
}

for (const dose of ["regS", "regM", "regL", "regXL"]) {
  const dir = join(FIX, `state-custom-${dose}`);
  const path = join(dir, "adaptation", "registry.json");
  const raw = await readFile(path, "utf8");
  const kb = (Buffer.byteLength(raw) / 1024).toFixed(1);
  const tRead = await timeAsync(9, 2, () => readFile(path, "utf8"));
  const tParse = await timeAsync(9, 2, () => JSON.parse(raw));
  const parsed = JSON.parse(raw);
  const tSnap = await timeAsync(9, 2, () => parseRegistrySnapshot(parsed));
  const snap = parseRegistrySnapshot(parsed);
  const tFrom = await timeAsync(9, 2, () => ResourceRegistry.fromSnapshot(snap, {}));
  const tLoad = await timeAsync(9, 2, () => loadAdaptationRegistry(dir));
  const registry = await loadAdaptationRegistry(dir);
  const identity = routingPolicyIdentity(PROJ);
  const tActive = await timeAsync(15, 3, () => registry.getActiveContent(identity));
  const active = registry.getActiveContent(identity);
  const tHash = await timeAsync(15, 3, () => hashCandidateContent(active.content));
  const tPolicy = await timeAsync(15, 3, () => parseLearnedRoutingPolicy(active.content));
  const tHit = await timeAsync(9, 2, () => loadLearnedRouting(dir, PROJ));
  const tMiss = await timeAsync(9, 2, () => loadLearnedRouting(dir, OTHER));
  console.log(
    `${dose} (${kb} KB): read=${tRead.toFixed(2)}ms jsonParse=${tParse.toFixed(2)} ` +
    `parseSnapshot(D)=${tSnap.toFixed(2)} fromSnapshot(D)=${tFrom.toFixed(2)} loadRegistry(D)=${tLoad.toFixed(2)} ` +
    `getActive(D)=${(tActive * 1000).toFixed(1)}us hashVerify(D)=${(tHash * 1000).toFixed(1)}us ` +
    `parsePolicy(E)=${(tPolicy * 1000).toFixed(1)}us | loadLearnedRouting hit=${tHit.toFixed(2)}ms miss=${tMiss.toFixed(2)}ms ` +
    `| per-MB=${(tHit / (Buffer.byteLength(raw) / 1048576)).toFixed(2)}ms`
  );
}
```

原始输出：

```
== v22.22.2 ==
regS (144.2 KB): read=0.24ms jsonParse=0.23 parseSnapshot(D)=0.22 fromSnapshot(D)=0.09 loadRegistry(D)=0.87 getActive(D)=1.4us hashVerify(D)=0.4us parsePolicy(E)=1.6us | loadLearnedRouting hit=0.72ms miss=0.96ms | per-MB=5.09ms
regM (790.2 KB): read=0.81ms jsonParse=1.99 parseSnapshot(D)=0.41 fromSnapshot(D)=0.50 loadRegistry(D)=4.62 getActive(D)=0.7us hashVerify(D)=0.3us parsePolicy(E)=0.7us | loadLearnedRouting hit=3.61ms miss=3.59ms | per-MB=4.68ms
regL (1496.4 KB): read=1.20ms jsonParse=2.62 parseSnapshot(D)=0.81 fromSnapshot(D)=1.18 loadRegistry(D)=6.81 getActive(D)=0.7us hashVerify(D)=0.3us parsePolicy(E)=0.7us | loadLearnedRouting hit=6.48ms miss=6.48ms | per-MB=4.44ms
regXL (5987.1 KB): read=5.13ms jsonParse=10.93 parseSnapshot(D)=3.10 fromSnapshot(D)=4.31 loadRegistry(D)=26.31 getActive(D)=0.8us hashVerify(D)=0.3us parsePolicy(E)=0.7us | loadLearnedRouting hit=25.13ms miss=25.52ms | per-MB=4.30ms
== v22.14.0 ==
regS (144.2 KB): read=0.19ms jsonParse=0.23 parseSnapshot(D)=0.21 fromSnapshot(D)=0.09 loadRegistry(D)=0.80 getActive(D)=1.2us hashVerify(D)=0.3us parsePolicy(E)=1.7us | loadLearnedRouting hit=0.64ms miss=0.61ms | per-MB=4.52ms
regM (790.2 KB): read=0.68ms jsonParse=1.42 parseSnapshot(D)=0.38 fromSnapshot(D)=0.48 loadRegistry(D)=4.11 getActive(D)=0.7us hashVerify(D)=0.3us parsePolicy(E)=0.7us | loadLearnedRouting hit=3.62ms miss=3.80ms | per-MB=4.69ms
regL (1496.4 KB): read=0.99ms jsonParse=2.65 parseSnapshot(D)=0.82 fromSnapshot(D)=1.13 loadRegistry(D)=6.83 getActive(D)=0.7us hashVerify(D)=0.3us parsePolicy(E)=0.7us | loadLearnedRouting hit=6.52ms miss=6.58ms | per-MB=4.46ms
regXL (5987.1 KB): read=4.35ms jsonParse=11.24 parseSnapshot(D)=2.99 fromSnapshot(D)=4.14 loadRegistry(D)=24.64 getActive(D)=0.7us hashVerify(D)=0.3us parsePolicy(E)=0.7us | loadLearnedRouting hit=25.19ms miss=25.37ms | per-MB=4.31ms
```

### A.4 冷/温双装载探针子进程（/tmp/r24i-bench/coldload.mjs）

```javascript
// R24-I cold/warm two-load probe (child): replicates the bearing run's exact
// load sequence — load1 = main.ts L708 (cold engine), load2 = startFlowchartRun
// L747 supervisor reload (same process, warmed). Prints both times.
import { performance } from "node:perf_hooks";
import { loadLearnedRouting } from "/workspace/dist/learning/learned-routing.js";
const [dir, proj] = process.argv.slice(2);
const t0 = performance.now();
await loadLearnedRouting(dir, proj);
const t1 = performance.now();
await loadLearnedRouting(dir, proj);
const t2 = performance.now();
console.log(`${(t1 - t0).toFixed(2)} ${(t2 - t1).toFixed(2)}`);
```

### A.5 冷/温 harness（/tmp/r24i-bench/coldharness.mjs，双引擎各执行一次）

```javascript
// R24-I cold/warm harness: spawns coldload.mjs 9x per dose, reports medians of
// load1 (cold, = main.ts L708) and load2 (warm, = startFlowchartRun L747).
import { spawnSync } from "node:child_process";
import { join } from "node:path";
const FIX = "/tmp/r24i-bench/fixtures";
const PROJ = join(FIX, "project");
function median(xs) { const s = [...xs].sort((a, b) => a - b); return s[Math.floor(s.length / 2)]; }
for (const dose of ["regS", "regM", "regL", "regXL"]) {
  const dir = join(FIX, `state-custom-${dose}`);
  const l1 = [], l2 = [];
  for (let i = 0; i < 9; i++) {
    const r = spawnSync(process.execPath, ["/tmp/r24i-bench/coldload.mjs", dir, PROJ], { encoding: "utf8" });
    const [a, b] = r.stdout.trim().split(" ").map(Number);
    l1.push(a); l2.push(b);
  }
  console.log(`${dose}: load1-cold=${median(l1).toFixed(2)}ms load2-warm=${median(l2).toFixed(2)}ms sum=${(median(l1) + median(l2)).toFixed(2)}ms`);
}
```

原始输出：

```
== v22.22.2 ==
regS: load1-cold=4.55ms load2-warm=1.35ms sum=5.90ms
regM: load1-cold=8.62ms load2-warm=5.04ms sum=13.66ms
regL: load1-cold=13.21ms load2-warm=7.90ms sum=21.11ms
regXL: load1-cold=37.40ms load2-warm=30.69ms sum=68.09ms
== v22.14.0 ==
regS: load1-cold=3.16ms load2-warm=1.31ms sum=4.47ms
regM: load1-cold=7.73ms load2-warm=5.14ms sum=12.87ms
regL: load1-cold=12.31ms load2-warm=7.69ms sum=20.00ms
regXL: load1-cold=38.20ms load2-warm=29.33ms sum=67.53ms
```

### A.6 行为等价与组成取证

- **三态输出等价**：注册表缺席 / 基线命中（regS）/ 外来失手（regL-miss）三个 children run（22.22.2，exit 0×3）输出经 id 规范化（`run_/ep_/evd_/tsk_/agt_/cnd_` 与 state-root 路径掩码）后 `diff` **逐字相同**——路由行 `tsk_a (implementer, MEDIUM) -> customco/custom-small`、`tsk_b (reviewer, MEDIUM) -> customco/custom-small` 三态全同（Live=R0 等价直接见证；空 avoid/prefer 基线经 `applyLearnedRouting` 恒等）。
- **adapt-status 组成取证**：`adapt status --state-root <regXL>` 输出 **5009 行 / 631 KB**（`proposed candidates: 5000` + 逐候选一行）——其 e2e 边际（+71.3/+65.1）= 冷装载（37-38）+ `registry.snapshot()` 克隆 + O(C) stdout，与 R21-D「status 体」定价同族。
- **迭代隔离**：每迭代全新拷贝 state-root（注册表在拷贝内、拷贝在计时外）；run 尾部 auto-loop 在全部承重格 `collected>0` 但无 actionable failing 剖面 ⇒ 提案腿不触发、注册表不被 run 改写（迭代间无态泄漏；夹具注册表字节恒定）。

MORE_OPTIMA=no
