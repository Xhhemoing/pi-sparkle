MODEL_SLUG=claude-fable-5-thinking-xhigh

# R24-E：`src/learning/` 第二十四遍复查报告（Round 24）

**战役:** 全库持久 SOTA 优化 Round 24 / R24-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `a655895`（开工基线 `bbe7979`（R24-B 摄取 + 本派单）；运行中独占分支两次快进 `bbe7979` → `d218fdb`（R24-C 空枚举摄取 + R24-F 派出）→ `a655895`（R24-D 空枚举摄取 + R24-G 派出），本分支已逐次 fast-forward/rebase 跟进，唯一新增文件仍仅本报告；R24-F / R24-G 在飞未触碰、未等待）
**分支:** `cursor/r24-e-learning-twenty-fourth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第二十四组新角度枚举为空
（有理由的空枚举，未铸造任何 S24-E-\* ID，与 R10-E..R23-E 同口径），
切片维持关闭（MORE_OPTIMA=no）。本轮补齐一条 R1-E..R23-E 从未拉伸过
的完整性轴——「铸造级连接/绑定命运面（JB）：跨事件 join 关系的命运
价表」。R17-E 的逐事件类价表在其重构核算里逐字留下过一条未定价残差
（"taskId hit-rate and summary-shape differences explain the
residual"）——summary 形态已被 R16-E 载荷形态轴覆盖，但 **taskId
命中率**（TASK_RESULT / JUDGE_DECISION 消费时第一遍 route map 命中
与否）从未被剂量控制过；R23-E GS 定价的是信号流的**消费级**守卫命运
（diagnose/bandit），其对偶——**铸造级**的 join 命运（同一载荷的
结果事件，绑定与不绑定路由信息各值多少）——正是本轴对象。本轮以
确定性显式构造（无 RNG，载荷字节跨臂钉死：各臂复用同一批 body 事件
对象，唯一变量就是跨事件 join 关系；命运构成全部以镜像生产守卫链的
断言核对）双段双趟关死（12 次套件运行 = 8 次全剂量（4 × v22.22.2 +
4 × v22.14.0）+ 4 次单段隔离进程（A/B 段 × 双版本），全剂量 18/18、
隔离 8/8 与 10/10 sanity 全绿，确定性 VERDICT 构成行跨 12 次运行
跨版本逐位一致）：**

1. **A 段（`collectSignalsFromEvents` 第一遍 → 第二遍 join 命运价表，
   S=48 纯流、扣除各自前奏基线后的边际价，ns/事件）**：
   **res-hit-full（完整路由命中：model+role+family+mv+fv 全绑定）
   1148–1297 / res-hit-min（最小路由命中：family 走 `familyFromRole`
   回退、mv/fv 缺席）976–1075 / res-miss（无路由，铸造不绑定信号）
   744–811 / judge-hit 343–387 / judge-miss 214–246**；路由事件
   存储侧（第一遍五张 per-task Map 的写入价）完整载荷 123.3–138.9 /
   最小载荷 54.7–66.7 ns/事件。**join 命运差价（hit-full − miss）=
   356–505 ns/事件（20 趟中 18 趟落在 395–444）**——机制上就是
   绑定字段数的钱：每个绑定字段付两次 `Map.get`（`signals.ts:218-222`
   的 `get()!==undefined ? {field:get()}` 双取型）+ 铸造对象多一个
   属性；hit-min 恰好居中（少 mv/fv 两字段 ≈ −200 ns）——价格随
   字段数近似线性，价表内部自洽。**可加性重构**（混合命中率流：
   24 命中 + 24 不命中）重构比 **0.99–1.10（20 趟）**，与 R17-E
   采集级 1.01–1.05 同带——join 命运可加，构成效应机械化。
2. **B 段（`outcomesFromRoutedRun` 路由 join 命运表，S=48 纯流边际价，
   ns/结果）**：**die-noroute（`routes.get` 未命中即死）4.0–7.3 /
   die-incomplete（路由载荷不完整，`isCompleteRoute` 拦截致从未入
   map，join 必失败）4.2–5.4 / die-fambad（join 成功后
   `familyFromRouted` 拦死）20.2–23.5 / die-rolebad（`isAgentRole`
   拦死）25.3–29.9 / die-unobserved（`outcomeKindFromResult` 判
   UNOBSERVED 拦死）26.6–30.8 / mint-pass（铸造 PASS outcome）
   573–606 / mint-fail+classify（铸造 FAIL + `classifyTaskFailure`）
   754–852**——join 失败近乎免费（4–7 ns），join 成功后被守卫拦死
   也只有 20–31 ns，全部成本压在铸造上（classify 税 ~170–250 ns/FAIL
   结果）；路由事件侧 stored-complete 41–46 / die-incomplete 7–8
   ns/事件。**可加性重构**（混流 16 routed+PASS / 16 routed+UNOBSERVED
   / 16 无路由）**1.02–1.04（18/20 趟；1 次全剂量 v22 运行 1.12/1.17
   属该趟整体抬升的位置噪声，同趟 die-noroute=7.3、mint-fail=838/852
   同向偏高，其余 3 次同版本运行全部带内）**。
3. **域分析（生产可达构成）**：锚点夹具（R1-A 组成 E=41）铸造率
   14–18 信号/run（本轮主运行 collected=14、cmp23=16、cmp22=18），
   其中结果类事件更少；取宽容上界 26 个结果事件/run（≈ 实测铸造率
   的 1.9 倍）做全翻转敏感度——**全 miss ↔ 全 hit-full 的极端构成
   翻转也只挪动 26 × (356–505 ns) = 9.3–13.1 µs/run**，距落地线
   3 个数量级。频率端：collect 每 run 一次、outcomes 每 adapt eval
   一次（§1 矩阵复核），join 命运构成不改变调用频次。
4. **机制裁决（为何该轴没有可削形态）**：join 是语义而非开销——
   绑定信号是学习环的目的本身（diagnose 行 25 消费 modelId、
   分组消费 family、bandit 门控 `auto-loop:88` 消费 modelId 在场性、
   `persistSignals` 全字段落盘、from-episode 消费 mv/fv），**不绑定
   侧本来就是便宜格**（744–811 vs 1148–1297），付钱的格没有一个
   字段无消费者；双 LCB / 双归因硬不变量又钉死绑定信息不可缩减。
   枚举出的全部改写形态（第一遍条件跳过、懒绑定、五 Map 合一、
   路由对象引用共享、classify 短路等）逐一落进换名重提（S9-E-2 /
   S1-E-6 / S4-E-3 / S9-D-3 谱系）或 µs 级清零区（§2.1 表）。
   **该轴无缝隙，收口，无新 ID。R17-E 残差自此机械化：其重构
   ±1–5% 波动中 taskId 命中率承载的份额有了显式价签。**

**天花板裁决（频率 × 单价 × 属地三端封死，R20-E..R23-E 同式）**：
频率端——collect / outcomes 每 run（每 eval）各至多一次；单价端——
join 差价 ~0.4 µs/事件、极端构成全翻转 ~9–13 µs/run，全谱即使按
最坏格清零也距落地线 3 个数量级；属地端——绑定字段全部有下游消费者
（数据面契约 + 双归因硬不变量），join 结构改写 = S9-E-2 / S1-E-6 /
S4-E-3 换名族。**不产生候选。**

其余与前二十三轮同构的复核全数成立：切片二十四遍零 diff
（`git diff adb20d7..HEAD -- src/learning` 为 **0 字节 0 提交**，
10 文件 1770 行 `wc -l` 与 R14-E..R23-E 相同）；SLICE-CPU 本夹具
（seeds `0xe24e01/02/03`，构成 collected=14 / outcomes=2 /
diagnoseGroups=1）**16.1–17.5 µs/run**（12 次运行 = 6 × v22.22.2 +
6 × v22.14.0，版本差异不可分辨，571–620× 低于门槛）；**R23-E 种子
对调（cmp23，6 次）复现 collected=16 / outcomes=1 / diagnoseGroups=2
（VERDICT 行与 R23-E 夹具行逐字相同），总量 18.3–21.5 µs——1/6 点
落在派单点名的 R23-E 带 18.1–18.6 内，其余 +0.5–1.3 µs（另 1 点
+2.9 系该趟 collect=17.6 位置噪声）**；**R22-E 种子对调（cmp22，
6 次）复现 collected=18 / outcomes=2 / diagnoseGroups=1，总量
19.3–19.9 µs 与 R22-E 带 18.6–19.7 相邻重叠（4/6 带内，2 点高出
上缘 ≤0.2 µs；collect/outcomes/diagnose/bandit 四段全部带内）**——
本 VM 对 collect 段读数整体偏高 ~3–8%（构成序单调：collected=
14/16/18 → collect 12.9–14.0 / 14.5–17.6 / 15.3–15.9，逐发射价
同向一致），带差 = 构成差 + VM 偏置，非切片漂移（零 diff 下夹具
逐字节相同）；配置态 live 面与载入面锚点全数在历史带内
（apply(avoid=10,M=10) 265–286 ns/task、空策略 112–124、load fresh
25.5–36.8 µs、load 现实 registry 76.2–87.6 µs）；`pnpm gate` 在
engines 合规 Node v22.22.2 全绿（1169 tests / 78 suites / 1168 pass /
0 fail / 1 skipped，计数与 R14-E..R23-E 逐一相同）；切片套件 48/48
双版本过。S8-E-1 / S9-E-2 / S13-B-1 未以任何形态触碰。R23-E GS、
R22-E NVG、R21-E IDG、R20-E bandit 并发按令未复铸；R24-A
list-dimension 普查、R24-B M=1 catalog collapse、**OCCX**（R24-C）
与 approval-authority face（R24-D）——两者均为运行中快进带入的
新禁项——CALLB、CKMIX、R23-I `.pi/` 树等外区轴一律未移植。不声称 Outcome-supported；Checkpoint F-PROD 仍开放
（ADR-005）。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 /
  `auto-loop` 283 / `bandit-store` 83 / `diagnostics` 78 /
  `from-episode` 298 / `learned-routing` 216 / `patterns` 146 /
  `signals` 439 / `signatures` 66 / `task-success` 69，合计 1770 行，
  `wc -l` 与 R14-E..R23-E 逐字节相同）本轮第二十四遍全量实际读码。
  上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、
  `privacy/`、`domain/roles`、`persist/file-lock` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表，含全部
  S\*-E-\* 行与 S8-E-1 / S9-E-2 / S13-B-1）→ PROGRESS.md →
  round-24/PLAN.md → round-23/R23-E.md（GS 轴；按令勿复铸）→
  round-22/R22-E.md（NVG 轴；按令勿复铸）→ round-21/R21-E.md
  （IDG 轴；按令勿复铸）→ round-17/R17-E.md（残差原文定位）→
  round-16/R16-E.md（载荷形态轴边界核对：R16-E 变文本长度、钉死
  绑定关系——与本轴正交的第一手确认）→ R10-E 附录 A（锚点脚本
  重建源）→ 10 个源文件 → 刚合入的 R24-A / R24-B / R24-C 报告
  （辖区与轴均与本轮无交，按令未移植）。R24-D 在其他分支在跑，
  未等待、未触碰其报告与分支。
- **基线漂移二十四重确认**：`git diff adb20d7..HEAD -- src/learning`
  为 **0 字节**、`git log adb20d7..HEAD -- src/learning` 为
  **0 提交**——二十四轮全程无人触及该目录。运行中独占分支两次快进
  `bbe7979` → `d218fdb` → `a655895`（R24-C / R24-D 摄取 + R24-F /
  R24-G 派出，纯 docs 提交），本分支逐次 `--ff-only` / rebase 跟进
  后重验零 diff 与 PLAN 增量（新增禁项仅 **OCCX**——离线路由格胞
  占据轴——与 approval-authority face——adaptation 辖区自动提升
  模式 × 预算态轴——均与本轮 JB 轴无交、未移植）。R1-E..R23-E
  的全部规模测量、调用面图景与切片排除裁决（R10-E..R23-E 未铸 ID，
  其空枚举收口一并继承，含 R16-E 载荷形态、R17-E 异常路径/事件类
  构成、R19-E 输入表示/事件排序、R21-E 标识符几何、R22-E 数值值类、
  R23-E 消费级结构共十六维）原样成立、原样继承。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S23 时代条目，未
  重提 FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN /
  NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX /
  approval-authority face。特别地：
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护：本轮仅复核其行号与
    频次（§1，与 R23-E 记录逐一相同），**未做任何去重分析**；JB 轴
    不触碰载入面（load 锚点带内复测即全部涉入）。
  - **S9-E-2**（collect pass-2 消费者预过滤负优化）未触碰、未重开：
    JB 轴**测量**两遍结构的 join 价格，**不提出任何两遍结构改写**
    （枚举表中「第一遍条件跳过 / 单遍化」形态按 S9-E-2 + S1-E-6
    换名判拒，见 §2.1）。
  - **S13-B-1**（B 切片辖区）未以任何变体重提：`applyLearnedRouting`
    本体**零改动**（切片零 diff 下必然）；重开条件（本体成本 ≥2 个
    量级 ≈ ≥27 µs/task；|avoid| ≈ 965–1,063 生产写入方不可达）未
    触发——本轮 apply 锚点 265–286 ns/task 在 R13-E..R23-E
    257–312 带内，JB 轴不触及 apply（apply 不消费 join 关系）。
  - **R23-E GS 未复铸**：GS 定价信号流在 diagnose/bandit **消费级**
    守卫链上的命运；本轴定价事件流在 collect/outcomes **铸造级**
    join 关系上的命运——输入对象不同（事件 vs 信号）、位点不同
    （signals/from-episode vs diagnostics/bandit-store）、变量不同
    （跨事件绑定关系 vs 单对象守卫命运）。本轴 A 段所有臂产出信号
    数恒为 48（A1–A7 断言），消费级构成不变，两轴正交。
  - **R22-E NVG / R21-E IDG 未复铸**：本轴 id 几何与数值值类全程
    钉死（taskId 恒 12ch、score 由 outcome/verification 枚举决定、
    载荷对象跨臂复用同一实例），变的只有 join 关系。
  - **R20-E bandit 并发轴未复铸**（本轮全部单进程）；**R24-A
    list-dimension / R24-B M=1 catalog collapse / OCCX / CALLB /
    CKMIX / R23-I `.pi/` 树 / R23-A G×E / R23-B batch arrangement
    未移植**（派单明令；本轴无列表维度臂、无 catalog 臂、无格胞
    占据臂、无调用边界调度臂、无字段在场混合臂——R23-G CKMIX 的
    「字段在场」变的是 checkpoint 载荷字段，本轴 hit-full/hit-min
    变的是**跨事件 join 图的边**，且载荷对象本身逐臂复用同一实例，
    字节恒等）。
  - R10-E..R23-E 未铸 S10..S23-E-\*，本轮亦未事后发明任何此类编号。
    空枚举不向 EXCLUSIONS 表新增任何行（轴昵称 JB 仅报告内用语，
    不入表）。
- **换名重提检查**：本轮新定位形态的换名检查全部并入 §2.1 表格逐行
  给出，并逐一对照 EXCLUSIONS 全表 + 二十三轮 E 报告 + 十四轮空
  枚举面。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞
  run；tracking 无指挥权；H/score 不写 PASS/FAIL；双 LCB（beta+
  normal）/ 双归因（logit+prob-add）在位未动；晋升 proposal-first；
  无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；fail-open
  家族关死；隐藏模块缓存（X1-1）未引入；两遍
  `collectSignalsFromEvents` 语义未触碰；X0-3 维持；S5-H-1（切片外）
  未触碰；S12-B-2 未声称触发（`eval-routing.ts` 复核仍只传
  `learned`）；未改任何测试；未 test-to-fit。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开
  PR；未 force-push 任何分支；仅提交并推送本分支上的本报告一个文件。
- 环境：本 VM 系统默认 node v22.14.0（`/exec-daemon/node`，PATH 前置
  遮蔽 nvm——与 R12-E..R23-E 记录同一环境事实），nvm 另装 v22.22.2
  （engines `>=22.19.0` 仅后者合规）。锚点、双 cmp 对调与 JB 轴在
  两个版本各测（§1/§2）；`pnpm gate` 以 v22.22.2 为准（PATH 前置
  nvm bin 后 `node --version` 复核为 v22.22.2）。依赖经
  `pnpm install --frozen-lockfile` 安装（pnpm 10.17.1）。双版本批次
  以显式二进制路径调度（`~/.nvm/.../v22.22.2/bin/node` vs
  `/exec-daemon/node` 直呼 `node_modules/tsx/dist/cli.mjs`——
  `.bin/tsx` 是 shell wrapper 不可被 node 直呼，本轮实际踩过），
  沿用 R20-E..R23-E 防串版本纪律。
- 新轴测量注意事项（本轮实际踩过、供后轮复用）：(a) join 命运臂的
  **载荷必须跨臂钉死为同一批对象实例**（本轮 A8 断言显式核对
  `bodyRes` 共享），否则测出的是载荷差不是 join 差——R17-E 残差
  之所以二十三轮无人定价，正因事件类价表天然把 hit/miss 混在
  TASK_RESULT 单价里。(b) 每个 join 臂需要**各自的前奏基线**
  （disc-only / +48 完整路由 / +48 最小路由 / +24 路由），边际价 =
  (全流 − 前奏)/S；混合命中率重构时组内 fate 单价用同趟读数。
  (c) B 段的 die-incomplete 与 die-noroute 语义不同但价格同带
  （4–5 ns）——`isCompleteRoute` 把不完整路由拦在 map 之外，join
  必 miss；将其与 fambad/rolebad（join 成功后死，20–31 ns）分开
  定价才能看到「join 成功本身值 ~16–25 ns」。(d) 个别运行会整体
  抬升（本轮 jb-v22-2 全臂同向偏高连带重构比 1.12/1.17），轴结论
  必须建立在多数运行带 + 全剂量/隔离双口径一致上。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

src 自 R23-E 基线起一行未动（src/learning 自 `adb20d7` 起零提交），
故 R8-E..R23-E §1 矩阵表结构原样成立；本轮逐条 rg 复核全部切片进入
点行号，**与 R23-E 记录逐一相同、无新增调用方、无新增命令类**
（`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
`run/flowchart-run:681/712`、`track/loop:88/172`；resume/replay 经
`flowchartForSupervisor`（定义 `:707`）@ `flowchart-run:747/880/1021`
各单次载入）。切片外消费者与 R23-E 逐一相同（`adaptation/
eval-routing.ts:223` 仅 `parseLearnedRoutingPolicy`、
`track/primary-split.ts:3` 类型导入、`privacy/record-classes.ts:210`
文档字符串）。R20-E C0 三事实复核不变：`updateProjectBandit` 生产
调用方恰一处（`auto-loop.ts:89`，门控在 `:88`）；`loadProjectBandit`
在 src/ 与 test/ 均零调用方（死导出面）；切片内唯一锁调用点
`bandit-store.ts:44`（`withExclusiveFileLock`）不传 options。
**JB 域钉复核（本轮矩阵列）**：`runAutoAdaptLoop` 全部三个生产调用点
（`cli/adapt:205`、`cli/main:783`、`track/loop:172`）**均不传
extraSignals**（rg 零命中，S5-E-3 / R22-E / R23-E 裁决第一手再证）
——铸造级 join 是绑定信号的唯一生产来源，本轮 A 段的构成上界
（26 结果/run）以锚点铸造率 14–18 为实测锚。

SLICE-CPU 锚点本 VM 重测（**12 次运行**：6 × v22.22.2 + 6 ×
v22.14.0；seeds `0xe24e01`（collect）/ `0xe24e02`（routed）/
`0xe24e03`（signals）——R12-E..R23-E 拒绝采样同协议，三个首选种子
全部一次通过（collected=14>3、outcomes=2>0、diagnoseGroups=1≥1）；
探针记录 seed→(collected,outcomes,diagnoseGroups)：01→(14,0,0)、
02→(12,2,0)、03→(17,1,1)、04→(15,4,0)、05→(16,1,0)、06→(17,3,0)；
夹具镜像 R8-E..R23-E 生成器，R1-A 组成 E=41）：

```text
collect=12.9-14.0us  outcomes=1.4-1.6us  diagnose=1.04-1.17us  bandit-build=0.7us
total in-slice CPU ~16.1-17.5us per full auto-adapt run （两 Node 版本重叠带：
  v22.22.2 16.2-16.6 / v22.14.0 16.1-17.5（1 次 17.5 系该趟 collect 14.0），
  版本差异不可分辨）
vs landing bar >=10000us  ->  571-620x below EVEN IF ZEROED
带间比对（本轮双向种子对调，R15-E 协议）：本轮构成 collected=14 /
  outcomes=2 / diagnoseGroups=1 与 R23-E（16/1/2）、R22-E（18/2/1）均不同。
  cmp23（R23-E 种子 0xe23e01/02/03 + R23-E NOW，6 次）：复现
  collected=16 / outcomes=1 / diagnoseGroups=2（VERDICT 行与 R23-E 夹具行
  逐字相同），总量 18.3-21.5us vs 派单点名的 R23-E 带 18.1-18.6：1/6 带内，
  4 点 +0.5-1.3us，1 点 21.5（该趟 collect=17.6 位置噪声，同批其余
  collect 14.5-16.0）。
  cmp22（R22-E 种子 0xe22e01/02/03 + R22-E NOW，6 次）：复现
  collected=18 / outcomes=2 / diagnoseGroups=1，总量 19.3-19.9us 与 R22-E 带
  18.6-19.7 相邻重叠（4/6 带内，2 点高出上缘 <=0.2us；collect 15.3-15.9 /
  outcomes 1.9-2.1 / diagnose 1.05-1.14 / bandit 0.8 与 R22-E 15.0-16.0 /
  1.9-2.0 / 1.00-1.15 / 0.7-0.9 四段全部带内）。
  读数解释：本 VM collect 段整体偏高 ~3-8%（cmp23 偏离大于 cmp22 系
  R23-E 带本身较窄放大偏置观感）；构成序严格单调（collected=14/16/18 ->
  collect 12.9-14.0 / 14.5-17.6 / 15.3-15.9），逐发射价同向一致
  （本夹具 collected=14 的 collect 带与 R23-E cmp21 同构成读数 12.4-13.0
  相邻重叠）——带差 = 构成差 + VM 偏置，非切片漂移（零 diff 下夹具
  逐字节相同）。
configured-state: applyLearnedRouting(avoid=10, M=10) = 265-286ns/task （R13-E 258-312 /
                                                        R21-E 261-300 / R22-E 266-293 /
                                                        R23-E 261-293 同带）
                  applyLearnedRouting(空策略)        = 112-124ns/task （R15-E..R23-E
                                                        110-140 同带）
                  loadLearnedRouting fresh(ENOENT)   = 25.5-36.8us/次
                  loadLearnedRouting 现实 registry    = 76.2-87.6us/次 （R8-E 80-85 /
                                                        R15-E..R23-E 74.3-134.5 同带，
                                                        I/O 抖动，亚 ms 一次性，S8-E-1 带）
```

全部确定性 VERDICT 行 12 次运行**逐位一致**（`E=41 collected=14
routedE=41 outcomes=2 S=12 diagnoseGroups=1 prevArms=3`），3/3 sanity
×12 全过；cmp23 6 次与 cmp22 6 次 VERDICT 行各自逐位一致且分别与
R23-E / R22-E 夹具行逐字相同。**锚点复核成立：该切片不存在不推翻
既有排除就能达门槛的候选。**

## 2. 本轮实扫新轴 + 第二十四组新角度枚举：有理由的空枚举

### 2.0 新轴 JB：铸造级连接/绑定命运面（本切片首次；join 命运构成 × 绑定完整度）

R1-E..R23-E 的十六个维度没有任何一维定价过「结果类事件在铸造级
**跨事件 join 关系**上的命运」。E 切片存在两个 join 位点：
(a) `collectSignalsFromEvents` 第一遍以五张 per-task Map
（`signals.ts:131-135`）存储 MODEL_ROUTED 载荷字段，第二遍消费
TASK_RESULT / JUDGE_DECISION 时逐字段 `Map.get` 绑定
（`:196/:218-222`，双取型）；(b) `outcomesFromRoutedRun` 以单张
routes Map（`from-episode.ts:195`）join，join 后过
`isCompleteRoute` → `familyFromRouted` → `isAgentRole` →
`outcomeKindFromResult` 四道守卫。R17-E 价表把 hit/miss 混在
TASK_RESULT 单价里并在重构核算里留下 taskId 命中率残差；R16-E 载荷
形态轴钉死绑定变文本；R23-E GS 只管消费级。本轮双段 + 双基线组
+ 混合命中率重构（12 次运行 = 8 全剂量 + 4 隔离，18/18 与 8/8、
10/10 sanity 全绿；数字见结论 1–4，此处不重抄），关键机制事实与
裁决：

- **join 差价是字段搬运费不是查找费**：miss 臂照样铸造信号
  （taskSuccess criterion 不依赖路由），hit 臂多付的 356–505 ns
  = 至多 10 次 `Map.get`（五字段双取）+ 铸造对象 5 个额外属性；
  hit-min（3 字段绑定 + `familyFromRole` 回退）恰居中。价格随
  绑定字段数近似线性——不存在超线性异常，join 图规模（本轮
  48 路由 × 48 结果）不引入额外项（路由存储侧独立计价
  55–139 ns/事件，线性）。
- **join 失败在两个位点都近乎免费**：collect miss 铸造不绑定信号
  （744–811，是三命运里最便宜的**铸造**格）；outcomes miss 直接
  4–7 ns 死（`routes.get` 一次未命中）。**「提高命中率省钱」与
  「降低命中率省钱」两个方向都不存在**：命中率由生产事件流的真实
  路由拓扑决定（MODEL_ROUTED 是否先于结果发射），不是实现可调
  参数；且两侧价格都已在地板上。
- **可加性成立**：A 段混合命中率流（24/24）重构比 0.99–1.10、
  B 段三命运混流（16/16/16）1.02–1.04（18/20 趟）——铸造级成本
  = Σ join 命运计数 × 命运单价，R17-E 采集级价表自此在 join 维度
  上因子化完成：事件类单价（R17-E）× join 命运分裂（本轮）。
- **守卫链再证不是成本项**（GS 结论的铸造级对偶）：B 段 join 成功
  后被 family/role/verification 守卫拦死只值 20–31 ns/结果，比
  铸造便宜 20–30 倍；classify 税 ~170–250 ns 只对 FAIL 结果发生
  且实现体大头在 `task-failure.ts`（切片外只读）。
- **绑定字段全消费（无懒绑定收益池）**：modelId → diagnose `:25`
  守卫 + bandit 门控 `auto-loop:88` + persistSignals 落盘；family →
  diagnose 分组统计 `mode`；role/mv/fv → persistSignals 落盘 +
  from-episode 观察构造。五字段皆有生产消费者，「按需绑定」没有
  可省字段，且信号记录是数据面契约（fbk record）。

**裁决**：该轴在频率（每 run / 每 eval 各一次）× 单价（join 差价
~0.4 µs/事件、极端全翻转 9.3–13.1 µs/run，距落地线 3 个数量级）×
属地（字段消费横跨 diagnose/bandit/persist/episode 四面 + 数据面
契约 + 双归因不变量）三端封死，**不产生候选**。「构成」维度自此
在铸造级内部也二级因子化：R17-E 事件类价表 × 本轮 join 命运分裂，
与消费级（R23-E GS）、载荷形态（R16-E）、键几何（R21-E）、值类
（R22-E）俱正交。

### 2.1 第二十四组枚举表

方法：本轮代码侧透镜「**谁支付 join**」（新轴的对偶）：对第二十四
遍全量重读的 10 个源文件逐函数问 (a) 哪些跨事件绑定/查找位点的成本
随 join 命运构成变化，变化处有无可削形态；(b) 削减形态是否等价且
不越线；同时按惯例对本轮逐字新点名的位置做换名检查（对照 EXCLUSIONS
全表 + 二十三轮 E 报告 + 十四轮空枚举面）。扫描结果：**零个可立项
形态**，逐类裁决如下（均未铸造新 ID）：

| 形态（本轮第二十四遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **第一遍条件跳过**（流内无结果类事件时不建五张 Map / 单遍化融合） | 路由存储侧 55–139ns/事件 | 换名重提拒列：两遍结构语义硬钉（§0 不变量）；单遍化 = S1-E-6 逐字同形（真实规模亚噪声、10× 更慢）；预扫一遍判「有无结果」= S9-E-2 机制类（多付 O(E) 换省略） | S1-E-6 / S9-E-2 |
| **五 Map 合一**（per-task struct 单 Map，省 4 次 `Map.get`） | ~150–250ns/绑定结果 | 换名重提拒列：S1-E-6 融合族；且五 Map 写入侧已实测仅 123–139ns/路由事件，合一后读侧省的 get 被 struct 分配抵扣（R15-E 分配轴谱系）；µs 级 | S1-E-6 + R15-E |
| **双取消除**（`:218-222` 的 `get()!==undefined?{f:get()}` 改单取暂存） | ~5 次 get ≈ 50–80ns/绑定结果 | 拒列：ns 级 × 26 结果/run ≈ 2µs，距门槛 3 个量级；S3-E-3（`?? []` 冗余读消除 ±8ns）同谱系裁决维持 | S3-E-3 谱系 |
| **懒绑定 / 按消费面裁剪字段**（只绑 diagnose/bandit 用的 modelId） | ~200–300ns/绑定结果 | 契约拒列：五字段皆有生产消费者（§2.0 域钉）；persistSignals 全字段落盘是 fbk 数据面契约；双归因不变量钉死路由信息完整性 | 硬不变量 + R2-E/R4-E I/O 谱系 |
| **路由对象引用共享**（信号持 MODEL_ROUTED payload 引用免字段拷贝） | ~300ns/绑定结果 | 拒列：S4-E-3（baseSignal 隐藏类/形状契约）同族；且分析不得持有可变 run 载荷引用（分析不变更在飞 run 不变量的镜像面） | S4-E-3 + 硬不变量 |
| **outcomes routes Map 预 size / 消费即删** | ns 级 | 换名重提拒列：R15-E 分配/GC 轴清零区；die-noroute 已 4–7ns 无可省 | R15-E |
| **`isCompleteRoute` 重复载荷短路**（同 taskId 重复 MODEL_ROUTED 免重验） | 7–8ns/路由事件 | 拒列：防御纵深删除 = S9-D-3 谱系；重复路由生产流量 ~0（TASK_RETRY 走 applyCascadeRetry 换 taskId） | S9-D-3 + S9-E-1 |
| **`classifyTaskFailure` 短路 / 缓存**（FAIL 铸造的 ~170–250ns 税） | ≤26 FAIL/run ≈ ≤6.5µs | 属地拒列：实现体 `run/task-failure.ts`（切片外）；调用点语义 = 失败归类进 OutcomeObservation（学习环输入契约）；µs 级 | 属地 + R17-E 异常面 |
| **judge join 裁剪**（JUDGE_DECISION 不绑定 role/family） | ~130ns/judge 事件 | 拒列：绑定字段有消费者（diagnose 分组按 family）；judge 流量 ≤ 数个/run；ns×个位数 | §2.0 域钉 |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（二十四轮）、调用位点行号全数复核与 R23-E 一致（§1）、extraSignals 零写入方再钉、快进增量纯 docs | — |

**提议排除 ID：无（本轮无「新增排除 ID」表——空枚举未铸任何
S24-E-\*，亦未向 EXCLUSIONS 新增轴昵称行）。** 切片在现行排除表与
硬不变量下不存在可达数十 ms 落地线的候选。本切片「无落地余量」证明
自此覆盖 **CPU + 规模 + 分配 + 协议 + 累积状态 + 载荷形态 + 异常面 +
构成面 + 执行层级 + 字符宽度 + 输入表示 + 输入次序 + 进程共存 +
标识符几何 + 数值值类 + 消费级结构 + 铸造级连接命运（本轮）十七个
维度**。

## 3. 逐文件收口（前二十三轮收口之上的本轮新检查点）

| 文件 | 本轮第二十四遍新视角检查 | 结论 |
| --- | --- | --- |
| `signals.ts` | 铸造级 join 首次全谱定价：五命运价表（214–1297ns/事件）+ 路由存储侧（55–139ns/事件）+ 混合命中率可加重构 0.99–1.10；双取型绑定（`:218-222`）定价并判 S3-E-3 谱系；两遍语义未触碰；载荷跨臂钉死证明差价纯系 join | 无候选 |
| `from-episode.ts` | outcomes 路由 join 命运表（die 4–31ns / mint 573–852ns）+ 三命运混流重构 1.02–1.04；`isCompleteRoute` 拦截使不完整路由 join 必 miss（4–5ns 档）首次显式定价；classify 税 ~170–250ns 判切片外属地；TASK_RETRY 面经 S9-E-1 + R19-E 维持 | 无候选 |
| `diagnostics.ts` | R23-E GS 按令未复铸；本轮只作为绑定字段消费者取证（`:25` modelId 守卫、分组 family 统计）——懒绑定收益池不存在的证人；A 段各臂信号数恒 48 使消费级构成钉死，两轴正交实测确认 | 无候选 |
| `bandit-store.ts` | 门控 `auto-loop:88` 以 modelId 在场性消费 join 结果——绑定份额决定 bandit 触发与否（频率面语义，非成本面）；R20-E 并发轴按令未复铸；锁调用点 `:44` 复核不变 | 无候选 |
| `auto-loop.ts` | 三生产调用点 extraSignals 零传入 rg 再钉（JB 域钉前提）；门控 `:88` / 唯一调用点 `:89` 复核不变；`[...fromEvents,...fromExtra,...fromPi]` 拼接按 R12-E 同址裁决维持 | 无候选 |
| `learned-routing.ts` | apply/load 复测在带（265–286ns / 25.5–36.8 / 76.2–87.6µs）；apply 不消费 join 关系，JB 轴零触碰；S8-E-1 未做去重分析；S13-B-1 边界零触碰 | 无候选 |
| `task-success.ts` | `taskSuccessFromResult` 是 A 段 miss 臂仍能铸造的原因（criterion 不依赖路由）——「join 失败仍铸造」语义的源头取证；纯函数面无新收益位 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | test-only 面复核仍零生产调用方，自动拒列；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核（依赖 `pnpm install
--frozen-lockfile`，pnpm 10.17.1）：

```bash
# Node v22.22.2（nvm bin PATH 前置；node --version 复核 v22.22.2）
pnpm gate        # typecheck ✓ + lint ✓ + test ✓ + build ✓
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（计数与 R14-E..R23-E 逐一相同）

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.14.0 与 v22.22.2 均过）
```

锚点脚本（临时置于 `/tmp/r24e-anchor.mts`，未入库以遵守无赢家纪律；
构造见附录 A）共 **12 次独立运行**（6 × v22.22.2 + 6 × v22.14.0），
每次 3/3 sanity 全过，确定性 VERDICT 行跨 12 次运行**逐位一致**；另
**6 次 R23-E 种子比对运行**（cmp23）与 **6 次 R22-E 种子比对运行**
（cmp22）VERDICT 行各自 6/6 逐位一致且分别与 R23-E / R22-E 夹具行
逐字相同。代表性一次运行（v22.14.0）：

```text
VERDICT fixtures: E=41 collected=14 routedE=41 outcomes=2 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=12.9us outcomes=1.5us diagnose=1.04us bandit-build=0.7us | total in-slice CPU ~16.1us per run vs landing bar >=10000us (620x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=284ns/task | empty policy=114ns/task -> x10 tasks/run = 2.84us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=28.0us | realistic registry (baseline+1 candidate)=82.1us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

JB 轴套件（`/tmp/r24e-joinfate.mts`，未入库；构造见附录 A）共
**8 次全剂量运行**（4 × v22.22.2 + 4 × v22.14.0）+ **4 次单段隔离
进程运行**（A/B 段 × 双版本），全剂量每次 18/18、隔离 A 8/8、隔离
B 10/10 sanity 全过（A 段命运构成断言 ×7 + 载荷同一性断言 ×1；B 段
域钉断言 ×1 + 命运构成断言 ×9）。代表性一次全剂量运行（v22.14.0）：

```text
VERDICT JB-A compositions: hitFull=48 hitMin=48 miss=48 judgeHit=48 judgeMiss=48 mixed=48(bound=24)
VERDICT JB-B compositions: miss=0 incomplete=0 fambad=0 rolebad=0 unobserved=0 pass=48 fail=48(fc=model:48) mixed=16
BENCH JB-A fwd (ns/event): res-hit-full=1198 res-hit-min=1014 res-miss=775 judge-hit=343 judge-miss=224 | route-complete=136.7 route-minimal=57.4 | disc-baseline=539ns
BENCH JB-A fwd reconstruction(h24): measured=52.19us predicted=50.91us ratio=1.03 | join-fate delta(hit-full - miss)=423ns/event -> production ceiling 26 results x delta = 11.01us/run (R17-E residual mechanized)
BENCH JB-B fwd (ns/result): die-noroute=4.2 die-incomplete=4.4 die-fambad=21.4 die-rolebad=27.6 die-unobserved=28.7 mint-pass=575 mint-fail+classify=754 | route-stored=43 route-die-incomplete=8
BENCH JB-B fwd reconstruction(mixed 16/16/16): measured=11.39us predicted=11.02us ratio=1.03
BENCH JB-A rev (ns/event): res-hit-full=1178 res-hit-min=982 res-miss=771 judge-hit=348 judge-miss=221 | route-complete=126.8 route-minimal=56.3 | disc-baseline=524ns
BENCH JB-A rev reconstruction(h24): measured=52.93us predicted=50.24us ratio=1.05 | join-fate delta(hit-full - miss)=407ns/event -> production ceiling 26 results x delta = 10.58us/run (R17-E residual mechanized)
BENCH JB-B rev (ns/result): die-noroute=4.3 die-incomplete=4.4 die-fambad=20.8 die-rolebad=27.4 die-unobserved=29.2 mint-pass=578 mint-fail+classify=757 | route-stored=43 route-die-incomplete=8
BENCH JB-B rev reconstruction(mixed 16/16/16): measured=11.47us predicted=11.07us ratio=1.04
VERDICT total sanity checks: 18, failures: 0
ALL JB SANITY CHECKS PASSED (deterministic)
```

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round24-r24e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R23-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件
（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 仅
`docs/reports/sota-opt/round-24/R24-E.md`）。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2.1 空枚举，逐形态有理由；第二十四遍以「谁
  支付 join」透镜普查新点名的全部维度落进实测清零区（join 失败
  两位点 4–811ns 地板 + join 差价可加 + 守卫死 20–31ns）、契约区
  （五绑定字段全消费 + fbk 数据面 + 双归因不变量）或既有排除确认
  （S1-E-6 / S9-E-2 / S3-E-3 / S4-E-3 / S9-D-3 / R15-E / S9-E-1）；
  新轴本身以「无候选」收口，未发现第十八个机制类）。
- 剩余赢家：无（全部 µs 级余量在排除表点名保护 + 硬不变量明令封死
  的数据面契约、I/O 支配面或切片外辖区上；CPU 温态面即使清零也距
  门槛 571–620×）。
- 矩阵空洞：无（调用位点行号与 R8-E..R23-E 逐一相同；配置态 live
  面与载入面锚点全数复测在带内；双 Node 版本交叉不可分辨；本轮带
  16.1–17.5 由**双向**种子对调实证归因为构成效应 + VM 偏置——
  R23-E 种子在本 VM 复现其构成行逐字相同、R22-E 种子复现 19.3–19.9
  与 R22-E 带 18.6–19.7 相邻重叠且四段全部带内，构成序单调、逐
  发射价同向一致）。
- 铸造级连接假说（「taskId 命中率使 R17-E 价表低估/高估」或「join
  结构藏着未测成本」）：以双段价表关闭（join 差价 356–505 ns/事件
  = 绑定字段搬运费，随字段数线性；join 失败两位点均在地板；混合
  命中率可加重构 0.99–1.10 / 1.02–1.04 使命中率构成机械化；极端
  全翻转 9.3–13.1 µs/run）。「构成」维度自此铸造级内部二级因子化：
  事件类价表（R17-E）× join 命运分裂（本轮），与消费级（R23-E）、
  载荷形态（R16-E）、键几何（R21-E）、值类（R22-E）俱正交。
- S13-B-1 边界：`applyLearnedRouting` 本体零改动（apply 不消费
  join 关系，本轮轴零触碰）；重开条件（本体成本 ≥2 量级 ≈
  ≥27 µs/task；|avoid| ≈ 965–1,063 生产不可达）未触发——本轮
  apply 锚点 265–286 ns/task 与 R13-E..R23-E 同带。S8-E-1 未做
  任何去重分析。S9-E-2 未触碰（其换名形态在 §2.1 判拒未重开）。
  S12-B-2 未触发（`eval-routing.ts:223` 复核仍仅 parse 面）。
- 跨职权观察（非候选、非 ID，仅记录）：(a) `isCompleteRoute` 把
  不完整 MODEL_ROUTED 拦在 routes map 之外，使「路由发了但缺字段」
  与「路由根本没发」在 outcomes 侧价格同带（4–5ns）且**同样静默**
  ——若未来需要诊断「为何学习环没长进」，两者不可区分是可观测性
  缺口（属 I/J 辖区遥测语义，成本面零影响）；仅记录。(b) A 段
  hit-min 臂显示 `familyFromRole` 回退在路由载荷缺 family 时把
  tester 映射为 family="test"——回退语义正确但意味着 family 绑定
  质量依赖上游载荷完整度，属数据质量面非成本面；仅记录。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R23-E §6（含 R14-E 规模判据、R15-E
分配/GC/噪声地板判据、R16-E 累积状态与载荷形态判据、R17-E 异常面与
构成面判据、R18-E 执行层级与字符宽度判据、R19-E 表示面与次序面判据、
R20-E 并发面判据、R21-E 标识符几何判据、R22-E 数值值类判据、R23-E
消费级结构判据），并新增一条可标定判据：**(g) 铸造级连接判据**——
若单 run 结果类事件数增长 ≥2 个数量级（≥~2,600 结果/run，按本轮
差价 ~0.4 µs/事件外推才进入 ms 级）、或信号新增绑定字段使 join 差价
超线性、或 collect/outcomes 出现每 turn 热环调用方、或
`isCompleteRoute`/守卫链语义变更使 join 失败获得非平凡成本，优先
复跑 A/B 段；或任一被引裁决（S1-E-6 / S9-E-2 / S3-E-3 / S4-E-3 /
S9-D-3 / X1-1）被正式推翻，或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

主锚点脚本全文（`/tmp/r24e-anchor.mts`，`npx tsx /tmp/r24e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R24-E；
`NOW = "2026-08-25T15:30:00.000Z"`；SECTION 1 的 seeds
`0xe24e01`（collect）/ `0xe24e02`（routed）/ `0xe24e03`（signals）——
拒绝采样：三个首选种子一次通过（collected=14>3、outcomes=2>0、
diagnoseGroups=1≥1），无顺延；探针记录
seed→(collected,outcomes,diagnoseGroups)：01→(14,0,0)、02→(12,2,0)、
03→(17,1,1)、04→(15,4,0)、05→(16,1,0)、06→(17,3,0)；SECTION 2 的
`mkdtemp` 前缀 `r24e-fresh-`/`r24e-reg-` 与 detector identity
`r24e-anchor`。另加 CLI 开关 `probe`（打印候选种子构成三元组）、
`cmp23`（置换为 R23-E 的 NOW（`13:30`）与 seeds `0xe23e01/02/03`、
前缀 `r24e-cmp23-`、identity `r24e-anchor-cmp23`）与 `cmp22`
（置换为 R22-E 的 NOW（`11:30`）与 seeds `0xe22e01/02/03`、前缀
`r24e-cmp22-`、identity `r24e-anchor-cmp22`）。其余逐字同 R10-E
附录 A。

JB 轴套件（`/tmp/r24e-joinfate.mts`，单进程；`argv[2]` 可选段过滤
A|B 用于隔离进程单段定价）：**确定性显式构造，无 RNG**——事件
构造器直接产出与生产载荷同形对象（taskId 恒 12ch `tsk_NN000000`，
S=48，NOW 单值），**body 事件对象逐臂复用同一实例**（A8 断言），
命运构成以镜像生产 join + 守卫链的断言核对（`isAgentRole` 域钉
B0 断言 tester∈AgentRole、intern∉）。构造要点：

- **A 段**：4 个前奏基线（disc-only / +48 完整路由 / +48 最小路由
  / +24 完整路由）× 3 个 body（48 TASK_RESULT / 48 JUDGE_DECISION
  / 同一 48 TASK_RESULT 接 24 路由前奏 = 混合命中率臂）。命运单价
  = (全流 − 对应前奏)/48；路由存储侧单价 = (前奏 − disc)/48；混合
  重构 predicted = 前奏(mix24) + 24×hit-full + 24×miss。断言：
  hit-full 全 48 绑定五字段、hit-min 全 48 走 familyFromRole 回退
  且 mv/fv=undefined、miss 全 48 不绑定仍铸造、judge 两臂 kind=
  judge、各前奏零铸造、mixed 恰 24 绑定 + 24 不绑定。
- **B 段**：6 个前奏（disc / +48 complete / +48 incomplete（缺
  featureVersion）/ +48 fambad（family="mystery"）/ +48 rolebad
  （agentRole="intern"）/ +32 complete）× 4 个 body（48 PASS /
  48 UNOBSERVED / 48 FAIL / 混合 16 PASS + 16 UNOBSERVED +
  16 无路由 PASS）。断言：五个 die 臂 outcomes=0、PASS 臂 48 全
  PASS 无 failureClass、FAIL 臂 48 全 failureClass=model、mixed
  恰 16 且 taskId 集合 = 前 16 个 routed+PASS。
- **协议**：先全胞预热（collect 胞 1500 遍 / outcomes 胞 2500 遍）
  再正反双趟测量（每胞 8000–60000 reps 按流长配平），R21-E §0 (b)
  / R22-E §0 (a) / R23-E JIT 次序纪律。

12 次主锚点运行原始区间（6 × v22.22.2 + 6 × v22.14.0）：

```text
SLICE-CPU 总量:
  v22.22.2: 16.6 / 16.5 / 16.3 / 16.2 / 16.3 / 16.4 us
  v22.14.0: 16.1 / 16.2 / 16.8 / 16.2 / 17.5 / 16.2 us
collect:    12.9-14.0 us（17.5 那趟 collect=14.0）  outcomes: 1.4-1.6 us（outcomes=2 构成）
diagnose:   1.04-1.17 us（diagnoseGroups=1 构成）    bandit:   0.7 us
apply(non): 265-286 ns/task    apply(空): 112-124 ns/task
load fresh: 25.5-36.8 us       load real: 76.2-87.6 us
VERDICT 行: 12/12 逐位一致；sanity 3/3 x12 全过
```

6 次 R23-E 种子比对运行（cmp23）原始值（3 × v22.14.0 + 3 × v22.22.2）：

```text
总量: 21.5 / 19.1 / 19.9 | 18.3 / 19.3 / 19.6 us —— vs R23-E 带 18.1-18.6：
  1/6 带内，4 点 +0.5-1.3us，1 点 21.5（该趟 collect=17.6 位置噪声）
collect 14.5-17.6 / outcomes 1.6-1.8 / diagnose 1.13-1.31 / bandit 1.0-1.1
  —— vs R23-E 14.4-14.8 / 1.6-1.7 / 1.09-1.16 / 1.0：collect 段本 VM 偏高
VERDICT 行: E=41 collected=16 routedE=41 outcomes=1 S=12 diagnoseGroups=2
  prevArms=3 —— 6/6 逐位一致，与 R23-E 夹具行逐字相同
```

6 次 R22-E 种子比对运行（cmp22）原始值（3 × v22.14.0 + 3 × v22.22.2）：

```text
总量: 19.8 / 19.7 / 19.4 | 19.4 / 19.9 / 19.3 us —— 与 R22-E 带 18.6-19.7
  相邻重叠（4/6 带内，2 点高出上缘 <=0.2us）
collect 15.3-15.9 / outcomes 1.9-2.1 / diagnose 1.05-1.14 / bandit 0.8
  —— 与 R22-E 逐项同带（15.0-16.0 / 1.9-2.0 / 1.00-1.15 / 0.7-0.9）
VERDICT 行: E=41 collected=18 routedE=41 outcomes=2 S=12 diagnoseGroups=1
  prevArms=3 —— 6/6 逐位一致，与 R22-E 夹具行逐字相同
```

12 次 JB 轴套件运行原始区间（8 全剂量 = 4 × v22.22.2 + 4 × v22.14.0；
4 隔离 = A/B 段 × 双版本；正反双趟，每段各在场 10 次运行 = 20 趟）：

```text
A join 命运价表 (边际 ns/事件，全 20 趟 = A 段在场的 10 次运行 x 正反):
  res-hit-full 1148-1297 | res-hit-min 976-1075 | res-miss 744-811
  judge-hit 343-387 | judge-miss 214-246
  路由存储侧: complete 123.3-138.9 | minimal 54.7-66.7 ns/事件
  disc 前奏基线 522-599 ns/调用
A join 差价 (hit-full - miss): 356-505 ns/事件（18/20 趟落在 395-444；
  505 与 356 各出现一次，分别在 jb-v14-3 fwd（同趟 res-hit-full=1297
  同向抬升）与 iso-A-v22 rev）
A 天花板: 26 结果 x 差价 = 9.25-13.14 us/run（R17-E 残差机械化）
A 重构比 (24 命中 + 24 不命中): 0.99-1.10
B 路由 join 命运表 (边际 ns/结果，全 20 趟 = B 段在场的 10 次运行 x 正反):
  die-noroute 4.0-7.3 | die-incomplete 4.2-5.4 | die-fambad 20.2-23.5
  die-rolebad 25.3-29.9 | die-unobserved 26.6-30.8
  mint-pass 573-606 | mint-fail+classify 754-852（classify 税 ~170-250）
  路由事件侧: stored-complete 41-46 | die-incomplete 7-8 ns/事件
B 重构比 (16/16/16 混流): 1.02-1.04（18/20 趟；jb-v22-2 全剂量一趟
  1.12/1.17，同趟全臂同向抬升 = 位置噪声，其余 3 次同版本运行全带内）
VERDICT 行: JB-A/JB-B 构成行跨 12 次运行跨版本逐位一致；
  sanity 18/18 x8（全剂量）+ 8/8 x2（隔离 A）+ 10/10 x2（隔离 B）全过
```
