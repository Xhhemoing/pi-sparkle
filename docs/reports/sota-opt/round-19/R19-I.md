MODEL_SLUG=claude-fable-5-thinking-xhigh

# R19-I：CLI / Pi adapter / config / telemetry 第十九遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `6e3e9c6`（派单 tip，含 R19-H 收口与 R19-I 派单；F/G 在飞，未触碰）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- src/cli src/pi-adapter src/config src/telemetry` 为空（`--stat` 0 行）——**零漂移，连续第十二轮字节不变**。`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet` 通过）；切片相关测试面零漂移（`git diff --quiet 8dee7fb..HEAD -- test/` 通过）。基线增量核查：`1d42a17`（R18-I 基线）→ `6e3e9c6` 共 9 个提交，全部 `docs(sota-opt)` 收口/派单（`git diff --stat -- . ':!docs'` 为空，非 docs 零文件）；**EXCLUSIONS.md 该区间逐字节未变**（`git diff --quiet` 通过），对 I 切片无新约束行（最后 I 相关行仍是 S8-I-3）。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致（合计 4708 行）。
- 前置阅读按令执行：README → EXCLUSIONS 全表（含全部 S\*-I-\* 行与 S13-B-1 尾行）→ PROGRESS → round-19/PLAN（含 R19-A/B/C/D/E/H 六切片空枚举收口摘要与 I 切片行；F/G 在飞未等待）→ R18-I（十连空 + children-spec 基数轴 + S1-I-3 越线点 C≈2400）→ R17-I（拒绝路径定价六端点）→ R16-I / R15-I / R14-I / R13-I / R12-I / R11-I / R10-I / R9-I 收口段落逐份重读 → **25 个切片源文件全量实际重读**（含凭据面两文件 `file-credential-store.ts` / `auth-session.ts` 的只读读码，未测量未立项）。
- 分支：`cursor/r19-i-cli-nineteenth-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（唯一入口 `/exec-daemon/node` shim，与 R9-I～R18-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。R15-I 轴 2 已收口引擎代格（24 无增量红利），本轮不重装 24、不补铸。pnpm 10.17.1，`pnpm install --frozen-lockfile`（本 VM node_modules 初始缺失，store 温装 1.7 s）+ `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**（package.json 钉死 + frozen-lockfile 内容寻址，lockfile 双站点直接确认）；exports 图逐项复核仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第十二轮未满足，两臂本轮第七次直接测量再确认（§2.2）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S19-I-\* ID（诚实空枚举，与 R9-I～R18-I 同结局，十一连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第十九组枚举而非历轮剩余项清理。R14-I 钉死配置空间维度（肥 builtin 态）、R15-I 钉死测量学维度、R16-I 钉死命令类覆盖与采样归因维度、R17-I 钉死错误端点定价维度、R18-I 钉死 children 负载输入规模维度；本轮补最后一块与 R18-I 正交的输入砖——**配置输入基数维度（providers.json custom 表：E enabled × P providers × K models/provider）**：十八轮以来所有 custom 承重格的配置夹具恒为 P=1 / K=2 / E=2（R14-I 的肥态阶梯只放大 *builtin* distinct-provider 数、机制是模块装载；R18-I 只放大 *children 工作负载文件*）；CLI 的第二个用户输入文件——providers.json 自身的基数——从未被任何轮次定价，S1-I-8（custom 路径先构建再 find）的否决依据「每命令个位数次」与 R10-I 失手记忆化否决的 E 维重开条件从未有过直接越线点标定。仓内测试面甚至从未构造过任何 customProviders（`grep -rl customProviders test/` 为空——custom 态的现实基数上界就是十一轮 harness 夹具本身）。该轴为测量收口、未产出候选：

1. **零漂移五重确认（连续第十二轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变——frozen-lockfile 内容寻址使包内容漂移数学上不可能；pi-ai 0.84.1 exports 图 7 项未变；`dist/providers/` 仍恰 39 表，本 VM 直接计数）、测试面（切片相关测试目录自 `8dee7fb` 零变动）、生产调用图（本轮再 grep：`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` 失手回退 L36 / `runtime.ts` 点用处 L20 / `listed-model.ts` 同步面 L1，`model-catalog.ts` 命中为 S7-I-1 注释非导入；pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方）、基线增量（`1d42a17..6e3e9c6` 共 9 提交全 docs，EXCLUSIONS 字节不变）。R8-I 关闭矩阵、R9-I～R18-I 抽测所依据的结构前提在本基线逐项成立。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字，派单明令项）**：R9-I 5 格承重集 × 2 Node 版本全部 exit 0，结构逐格同构：地板 53.9/51.2 ms，**custom−builtin 差 children +47.6 / +26.4、track +48.4 / +23.9 ms（22.14/22.22）**——与 R18-I 带（+49.5/+26.4、+55.2/+25.0）、R17-I 带（+47.3/+24.7、+46.5/+21.8）及更早九轮同号、同量级、同 Node 版本梯度（**十二轮同构**）；带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内。builtin 态目录构建比地板贵 +54.6/+54.6（children，双引擎），与 R18-I（+54.0/+55.6）紧邻。进程内冷载锚点：`providers/all` 51.8-68.4 / 31.2-32.9 ms（22.14/22.22；warm 0.064-0.113 ms）；per-provider 表 anthropic 3.5/2.5、openai 2.3/0.8 ms（优势 ~9-25×）；custom id 失手解析首次 1.861/2.045 ms、重复失手中位 **0.097/0.096 ms**（十度复证 R10-I 记忆化否决）。S7-I-1 per-provider 优势与 custom 态回退归属完好（§1）。
3. **第十九组新角度枚举为空（§2）**，其中一轴为本轮新测量、不铸 ID：
   - **轴 1（§1.3）：providers.json 配置输入基数标定（I 切片首次；配置输入规模维度）**。端到端新增 3 个基数格（children 命令类、双引擎全 exit 0）：custom-e10（P=5×K=2 全启用）、custom-e50（P=10×K=5 全启用）、custom-k500（P=1×K=500 表仅启用 2）相对 lineage custom 格（P=1/K=2/E=2）分别 **+6.8/+3.3、+9.9/+8.6、+6.6/+3.3 ms**（22.14/22.22）——配置放大 25×（E=2→50）仅 +9-10 ms、表放大 250×（K=2→500）仅 +3-7 ms，无悬崖，全部贴 R15-I A/A 包络。进程内九点阶梯（(P,K,E) 至 (50,20,1000)，附录 A.2 `cfgscale` 模式）把切片内三个随配置基数增长的站点直接定价：**(a) `parseProvidersConfig` 全网格亚 ms**（P·K=10⁴ 也仅 json-parse 0.71 + validate 0.43 ms）；**(b) 每 enabled-custom-id 的强制 builtin-first 权威查询税 ~0.063-0.078 ms/查（线性）**，是 fat-E 态成本的 ≥99.5% 主体（E=1000 时 resolve 65.8-75.2 ms vs 被 S8-I-1 封死的 skip 形态 0.15-0.16 ms），**10 ms 越线点在 E ≈ 130-160**——比生产 enabled 基数（个位数，2-5）高约两个数量级；**(c) S1-I-8 站点（custom find + O(K) 重建/查）的消除天花板即便在 E=1000 或 E×K=5×10⁴ 压力角也仅 ~1-3 ms**（Map 反事实差分，小基数处符号翻转），10 ms 越线需 E×K ≳ 2×10⁵。附带分解：R10-I 失手锚点的「首次 ~1.9 ms」是**每进程**首次（package-reader 冷）而非每 specifier——100 个全新 distinct provider 的顺序首失手合计仅 6.6-7.5 ms（中位 0.063-0.103 ms/个）。端到端边际的切片内占比：custom-e50 格 +9.9/+8.6 中切片内三站点边际 ~3.7 ms（~40%，其中 ≥99.5% 是强制权威查询），其余是切片外 O(M) 目录机器（M=50 目录行进 calibrate/assignTasks/flowchart 编译，B 切片契约，R19-B 已证 O(M)）。**无候选、不铸 ID**；S1-I-8 与 R10-I 记忆化的否决依据自此有直接越线点数字，S8-I-1 围栏拿到 E 维定价证人。
   - 其余全部角度逐条映射到排除行或历轮无 ID 收口（§2.3），无一越过落地线。R18-I children 基数轴、R17-I 拒绝路径定价六端点、R16-I flowchart 格与采样归因、R15-I 三轴、R14-I 肥态证明、R13-I 两处无名微观（`import.meta.resolve` 预探针本轮重读 `listed-model-lazy.ts` L21-38 原样、alias-row spread `model-catalog.ts` L72/L79 原样）均未重铸、未重测（其前提——39 表计数、exports 图、pi-ai 版本——本轮复核未变）。
4. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**；r7i part B 1220 builtin 行 × 39 provider lazy===sync；part D 载荷探针复证 builtin 构建后 providers/all 保持冷 62.7 ms、custom 回退命中缓存 0.3 ms）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R18-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读证据，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交 `scripts/round19-r19i-equivalence-sim.ts`（全部 harness/探针源码进附录 A，遵守 R7-F/R8-C/R9-I～R18-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md 未触碰；未触碰在飞 R19-F / R19-G 分支与文件。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S18-\* 条目（含 S13-B-1）；R9-I～R18-I 空枚举维持，未事后发明 S9-I-\* ～ S18-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：`--children` 复用 smartChildPlan 已校准目录（main.ts L726-729 原样）、Pi runtime 点用处 `await import`（main.ts L149 / auth-session 函数体内；S4-I 败者为 **S4-I-2..5**，无 S4-I-1）、12 条分支独占分派模块惰性导入（赢家 ID **S5-I-1**；main.ts L1417-1455 dispatch 站点 + L668 track/loop 边逐字在位）、per-provider 惰性模型表（`listed-model-lazy.ts` + `model-catalog.ts` L58）——零漂移即字节级证明，三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅对其**重开条件**做第七轮直接测量再确认（§2.2）——四 specifier 全 blocked（双 Node 逐字）+ `all.models.js` 文件级不存在 + 39 表计数不变 + exports 图 7 项不变，未满足，维持淘汰。硬不变量「`providers/all` 失手回退 + throw-outside-catch」逐字在位（`listed-model-lazy.ts` L34-37 未动）。本轮新轴是既有 custom 解析路径在**配置基数维度**上的直接观测（零行为差异、零代码改动）；阶梯中的 `skip-cf` 反事实是对 S8-I-1 **被封死形态**的天花板定价（明确标注 FORBIDDEN，仅用于校准重开条件的收益上界），未构造替代注册表、未触碰回退路径——与自制 union / 磁盘快照 / memo 三重围栏（R10-I / R11-I）中的任何 rename 无涉。「先查 customProviders 再决定是否 import builtin 表」这一重排形态即 S8-I-1 逐字（custom 遮蔽 builtin、惰性面与同步面分歧），本轮未实现、仅在 §1.3 裁决中点名。
  - **未重开 R18-I children 基数轴 / R17-I 拒绝路径收口**：本轮抽测集回归 exit-0 承重格 + 新配置基数格；children 夹具维持 C=2 lineage（C 维已收口）；未测任何 exit-1 端点（错误端点定价已收口）。
  - **未重开 S8-I-2**（预热无重叠窗口）、**S8-I-3**（enabled 循环 Promise.all——本轮 resolve 循环阶梯直接展示其收益上界：循环体的 await 串行成本即 ~0.07 ms/查的失手税本体，重叠无从谈起）、**S7-I-2..4**、**S6-I-1..3**、**S5-I-2..5**、**S4-I-2..5**、**S3-I-1..6**、**S2-I-1..6**、**S1-I-1..8**。本轮 25 文件重读逐字再确认（未立项）：flowchart 双 `buildLiveCatalogConfig`（S1-I-1，main L569+L583）、providers.json 双读（S1-I-2，main L609 + createExecutor kind==="pi"）、loadLearnedRouting/loadProvidersConfig 死载荷下沉（S2-I-1/S3-I-3，main L729/L609）、`smartChildPlan` assignments.find（S1-I-3，main L372；R18-I 越线点 C≈2400 维持）、双 createAgentProfileRegistry（S3-I-2）、stdout 批量（S3-I-4）、冲突检查提升（S4-I-4）、describeSparkleModel try/catch 预探测（S4-I-5）、setDefaultModels 双 parseModelRef（S3-I-5，providers-config L100-104）、parseCliErrorJson reverse（S1-I-6）、buildInvocation +=（S1-I-7）、**resolveListedModel 自定义路径先构建再 find（S1-I-8，listed-model-lazy L54-56——本轮轴 1 对其否决依据做 E×K 越线点标定，见 §1.3）**。
  - **R10-I ～ R18-I 无 ID 收口维持**：跨进程版本键控磁盘快照、进程内失手记忆化（本轮重复失手中位 0.096-0.097 ms 十度复证 µs 族，且新增「每进程首失手 vs 每 specifier」分解——distinct 首失手也仅 ~0.07-0.10 ms/个）、进程内自制 39 表 union（三重围栏）、回退后 per-provider 探针短路（≤3.5 ms/provider，仅 mixed 态可达）、root `.` / `./compat` 作替代源（R12-I）、`all.models` 文件级探针（R13-I）、肥配置态交叉不存在证明（R14-I——本轮轴与其正交：R14-I 放大 builtin distinct-provider 数、问「何时预取 providers/all」；本轮放大 custom 表与 enabled 列表、问「切片内 O(E)/O(P)/O(K) 站点何时越线」）、A/A 噪声地板 / 引擎代格 / 堆足迹格（R15-I）、flowchart 抽测格 + 181 模块/1003 KiB 采样归因（R16-I）、fail-closed 拒绝路径六端点定价（R17-I）、**children-spec 基数轴 + S1-I-3 越线点 C≈2400（R18-I，不补铸不重测）**。**R13-I 两处无名微观维持不铸**：`import.meta.resolve` 同步预探测（S4-I-5 族镜像 + 失手锚点十度定界 <0.3 ms/构建）、`buildLiveCatalogConfig` 默认别名行 spread（ns-µs 级 S1-I-8 邻域）。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项（S1-I-5 / S7-I-4 维持关闭）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；稀疏→已批准 R0 基线；双 LCB / 双归因在位（X0-11）；提升 proposal-first（Checkpoint F-PROD 开放，ADR-005/X0-1）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`listed-model.ts` 同步公开面与惰性面经 `listed-model-common.ts` 字节等价关系未动；`providers/all` 失手回退 + throw-outside-catch 在位；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；未触碰在飞 R19-F / R19-G 分支与文件。

## 1. 本 VM 复测：5 格承重抽测 + 进程内锚点 + 新轴（双 Node，未拷贝历轮数字）

R9-I～R18-I 抽测集原样复刻并**新增 3 个配置基数格**（configured 态夹具、每迭代全新拷贝 state-root、2 预热 + 10 测量、显式二进制全路径，遵守 R7-I 配置态 × 命令类纪律；harness 全文附录 A.1）。

### 1.1 抽测中位数（ms，spawn-to-exit，全部 exit 0）

| 格 | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|
| `--version`（地板） | 53.9 (min 53.3, p90 55.8) | 51.2 (50.1, 52.1) |
| run-children@builtin（C=2） | 108.5 (104.7, 113.8) | 105.8 (103.2, 111.4) |
| run-track@builtin | 125.1 (118.7, 127.3) | 126.9 (122.9, 128.1) |
| run-children@custom（P=1/K=2/E=2） | **156.1** (143.6, 166.6) | **132.2** (127.8, 138.2) |
| run-track@custom | **173.5** (165.8, 183.4) | **150.8** (144.3, 159.2) |
| run-children@custom-e10（本轮新格，§1.3） | 162.9 (155.6, 165.7) | 135.5 (128.4, 138.9) |
| run-children@custom-e50（本轮新格，§1.3） | **166.0** (162.4, 183.7) | **140.8** (137.5, 142.8) |
| run-children@custom-k500（本轮新格，§1.3） | 162.7 (151.3, 167.1) | 135.5 (132.8, 137.6) |

- **custom−builtin 差**：children **+47.6 / +26.4**、track **+48.4 / +23.9** ms（22.14/22.22）。R18-I 带 +49.5/+26.4 与 +55.2/+25.0，R17-I 带 +47.3/+24.7 与 +46.5/+21.8，R16-I 带 +49.7/+26.3 与 +51.7/+22.5，R15-I 带 +56.3/+39.7 与 +65.9/+23.2，R14-I 带 +52.2/+26.7 与 +55.1/+23.7，R13-I 带 +45.5/+23.7 与 +51.7/+21.5，R12-I 带 +51.3/+29.1 与 +58.7/+24.1，R11-I 带 +45.9/+22.8 与 +48.0/+23.4，R10-I 带 +47.1/+36.6 与 +56.0/+26.4，R9-I 带 +53.9/+26.9 与 +49.4/+28.2——**十二轮同号、同量级、同 Node 版本梯度**。带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内（22.14 单窗 maxAbs 34.5 ms、22.22 8.7-10.4 ms），无新结构。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
- **builtin 态目录构建**比地板贵 +54.6/+54.6（children，双引擎）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。本 VM 地板（53.9/51.2）与 R18-I VM（54.3/51.4）紧邻，格间结构不变。

### 1.2 进程内冷载锚点（附录 A.2 脚本，工作区根执行，测毕删除）

| 锚点 | 22.14.0 | 22.22.2 |
|---|---|---|
| `providers/all` cold import（3 次） | 51.8 / 61.8 / 68.4 ms | 32.9 / 31.7 / 31.2 ms |
| `providers/all` warm（第二次 await） | 0.064-0.070 ms | 0.099-0.113 ms |
| per-provider `anthropic.models` cold | 3.5 ms | 2.5 ms |
| per-provider `openai.models` cold | 2.3 ms | 0.8 ms |
| `providers/all`（双表已载后） | 43.7 ms | 29.4 ms |
| custom id 失手解析首次 | 1.861 ms | 2.045 ms |
| 同上，重复失手中位（N=49） | **0.097 ms** | **0.096 ms** |

- per-provider 优势 ~9-25×，双引擎成立，S7-I-1 归属完好；custom 格增量与 `providers/all` cold 同量级，回退归属自洽。
- 重复失手 ~0.096-0.097 ms 十度复证 R10-I：Node package-reader 已缓存失败解析，记忆化物理无收益（<0.3 ms/构建）。本轮新增分解见 §1.3(b)：「首次 ~1.9-2.0 ms」是**每进程**首失手（reader 冷）而非每 specifier——后续全新 distinct specifier 的首失手也只 ~0.07-0.10 ms/个。

### 1.3 轴 1：providers.json 配置输入基数标定（本轮新证据，不铸 ID）

十八轮以来所有端到端 custom 夹具恒为 **P=1 / K=2 / E=2**（R8-I 78 格关闭矩阵与 R9-I～R18-I 每轮抽测集皆然）；S1-I-8（custom 路径先构建再 find，`listed-model-lazy.ts` L54-56）的否决依据「每命令个位数次」、R10-I 失手记忆化否决的「custom 态每次构建重复失手 ≤2 次」都建立在该基数上。配置输入规模维度与 R18-I 的 children 负载输入规模维度正交（另一个用户输入文件），在 I 从未直接测量；R14-I 的肥态阶梯放大的是 *builtin* distinct-provider 数（机制=per-provider 表模块装载 vs providers/all 整载的交叉点），从未放大 custom 表或 enabled 列表长度（机制=切片内 CPU：解析校验、每查失手税、每查 O(K) 重建）。现实基数锚：仓内 `test/` **从未构造任何 customProviders**（grep 直接确认），README 与全部历轮夹具的 custom 态即 P=1/K=2/E=2——生产 custom 基数为个位数。本轮两步补齐（spawn 格见 §1.1 下三行；进程内阶梯脚本与原始输出见附录 A.2 `cfgscale` 模式）：

**(a) 端到端基数格**：相对 lineage custom 格（156.1/132.2 ms），custom-e10（P=5×K=2，E=10）**+6.8/+3.3**、custom-e50（P=10×K=5，E=50）**+9.9/+8.6**、custom-k500（P=1×K=500，E=2）**+6.6/+3.3** ms（22.14/22.22）。配置放大 25×（enabled 2→50）仅 +9-10 ms、custom 表放大 250×（模型行 2→500，providers.json ~75 KiB）仅 +3-7 ms——**无悬崖、无新结构**；e10/k500 增量整体落在 R15-I A/A 包络内，e50 增量贴 22.22 包络缘（8.65-10.35 ms）但可由 (b) 的进程内直测归属，非噪声伪影。

**(b) 进程内切片站点阶梯**（九个 (P,K,E) 网格点，逐站点 best-of-5，warm 制度——providers/all 一次性冷载已由 §1.2 锚点与十二轮端到端格定价；双引擎数字几乎逐位重合）：

| P | K | E | json-parse | validate（parseProvidersConfig） | resolve 循环（生产形态） | map-cf（S1-I-8 反事实，保留强制查询） | skip-cf（S8-I-1 被禁形态，仅定价） | 单次 O(K) 重建 | 单次 O(P) find |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 2 | 2 | 0.002/0.002 | 0.009/0.009 | 0.278/0.298 | 0.208/0.170 | 0.001/0.001 | 0.001/0.001 | 0.000/0.000 |
| 5 | 2 | 10 | 0.007/0.007 | 0.014/0.011 | 1.065/0.797 | 0.843/0.941 | 0.002/0.002 | 0.001/0.001 | 0.000/0.000 |
| 10 | 5 | 50 | 0.029/0.028 | 0.043/0.025 | 3.989/3.909 | 3.667/3.597 | 0.007/0.008 | 0.001/0.002 | 0.000/0.000 |
| 1 | 500 | 2 | 0.169/0.172 | 0.138/0.137 | 0.290/0.337 | 0.143/0.139 | 0.001/0.001 | 0.018/0.018 | 0.000/0.000 |
| 50 | 1 | 50 | 0.037/0.036 | 0.028/0.026 | 3.664/3.445 | 3.671/3.549 | 0.008/0.007 | 0.000/0.000 | 0.001/0.001 |
| 200 | 1 | 200 | 0.146/0.142 | 0.103/0.109 | 15.652/13.176 | 14.653/12.988 | 0.030/0.030 | 0.000/0.000 | 0.002/0.002 |
| 1000 | 1 | 1000 | 0.708/0.674 | 0.408/0.433 | **75.158/65.777** | 73.952/63.164 | **0.153/0.153** | 0.000/0.000 | 0.013/0.012 |
| 1 | 1000 | 50 | 0.346/0.347 | 0.090/0.088 | 5.569/5.375 | 3.548/3.018 | 0.009/0.009 | 0.033/0.033 | 0.000/0.000 |
| 50 | 20 | 1000 | 0.386/0.384 | 0.137/0.136 | **73.904/63.028** | 72.430/62.575 | 0.163/0.157 | 0.001/0.001 | 0.001/0.001 |

（单位 ms，22.14/22.22。resolve 循环 = `buildLiveCatalogConfig` L59-68 逐 enabled-id 调真实 `resolveListedModelLazy` 的逐字形状；map-cf = S1-I-8 被排除候选的天花板形态——保留每查强制 builtin-first 权威查询、仅把 custom find+重建换预建 Map；skip-cf = S8-I-1 被封死的跳过形态——纯 Map 查、不做权威查询，**仅用于定价被禁形态的收益上界，非候选**。附带 distinct-provider 首失手剖面：全新 specifier ×10 合计 1.00/0.80 ms、×100 合计 7.47/6.56 ms——中位 0.063-0.103 ms/个。）

- **切片内三站点全部线性、无一超线性**：(a) `parseProvidersConfig`（含 JSON.parse）全网格亚 ms——P·K=10⁴ 行也只 1.1 ms 合计，10 ms 越线需 ~10⁵ 模型行，µs 族即死；(b) **每 enabled-custom-id 的强制权威查询税 ~0.063-0.078 ms/查**（E=200 → 13.2-15.7、E=1000 → 63-75 ms，斜率恒定，P/K 组成无关——(1000,1,1000) 与 (50,20,1000) 几乎逐位同价），**10 ms 越线点 E ≈ 130-160**，比生产 enabled 基数（2-5）高约两个数量级；(c) S1-I-8 的 find+O(K) 重建：resolve−map-cf 差在 E×K=5×10⁴ 压力角（1,1000,50）也仅 **2.0-2.4 ms**、在 (1000,1,1000) 仅 1.2-2.6 ms、小基数处符号翻转（(5,2,10) 22.22 为 −0.14）——**10 ms 越线需 E×K ≳ 2×10⁵**（单次 K=1000 重建实测 0.033 ms）。
- **fat-E 成本的 ≥99.5% 是语义强制项**：E=1000 时 skip-cf 仅 0.15-0.16 ms vs resolve 63-75 ms——差额全部是「per-provider 表失手 import + warm providers/all `getBuiltinModel` 查询」，即 S8-I-1 健全性反例所保护的 builtin-first 权威查询本体（custom id 必须先证明不是 builtin id，否则 custom 遮蔽 builtin、惰性面与同步面分歧）。其消除形态穷尽为三：跳过查询（S8-I-1 逐字，封死）、失手记忆化（R10-I 收口 + X1-1 隐藏状态——且本轮 E 阶梯首次给出其重开条件的直接数字：需 E ≳ 130-160 的真实配置流量才达 10 ms 收益，生产 E 个位数下收益 ≤0.4 ms）、重排 custom-first（S8-I-1 同一分歧，本轮点名不实现）。
- **端到端归属自洽**：custom-e50 端到端 +9.9/+8.6 ms 中，切片内三站点边际 ~3.7 ms（(10,5,50) 网格合计 4.0 减 E=2 基线 0.3；~40%），其中 ≥99.5% 为强制查询税；其余 ~5-6 ms 是切片外 O(M) 目录机器（M=50 行进 `calibrateCatalogFromState` / `assignTasks` / flowchart 编译 per-node allowedModels 50 id，B 切片契约面，R19-B 本轮已证 O(M) 无超线性）。custom-k500 端到端 +6.6/+3.3 中切片内合计仅 ~0.6 ms（parse 0.34 + resolve 0.3），其余为 75 KiB 文件读 + 噪声——K 维在切片内被 `enabled` 过滤天然截断（未启用的模型行只过 parse，不进目录）。
- **候选性裁决（不铸 ID）**：(a) S1-I-8 换 Map 在生产基数下维持排除（E=2 时 resolve 0.28-0.30 vs map-cf 0.17-0.21 ms，差 ~0.1 ms 且被强制查询税支配），其重开条件自此有直接数字——需 E×K ≳ 2×10⁵ 的真实配置（越线点比现实高约四个数量级），等价性形态已在本节备档；(b) 失手记忆化维持 R10-I 排除，E 维重开条件 = 真实 enabled 基数增长两个数量级（E≈130-160）；(c) 强制查询跳过 / custom-first 重排 = S8-I-1 逐字，非候选；(d) `parseProvidersConfig` 微观（per-model 8 项条件 spread 等）全网格亚 ms，S3-I-5 / S1-I-8 邻域 µs 族；(e) 「按基数分派双形态」为零流量假设格引入分支复杂度，S12-B-2/S13-B-1 条件式落地物同型先例。**配置输入规模维度就此收口**；后续轮次引用本节即可把「fat providers.json 瘦身」类候选直接判为越线点外推（E、E×K 均在现实流量数个数量级之外）或 S8-I-1 围栏重现。

## 2. 第十九组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 输入规模定价），每形态先对 EXCLUSIONS 全表 + 十八轮 I 报告做同机制先例检索。R14-I 收口配置空间（肥 builtin 态）、R15-I 收口测量学、R16-I 收口命令类覆盖与采样归因、R17-I 收口错误端点定价、R18-I 收口 children 负载输入规模；本轮收口**配置输入规模维度**（轴 1）。

### 2.1 新轴候选性自查——测量收口，不铸 ID

- **轴 1（配置基数标定）**是既有 custom 解析路径在配置输入规模维度上的直接观测：零行为差异、零代码改动。其产出是「切片内三站点全线性 + 强制查询税越线点 E≈130-160 + S1-I-8 天花板 E×K≳2×10⁵ + fat-E 成本 ≥99.5% 语义强制」的裁决数字，不是优化；两个反事实（map-cf / skip-cf）分别为被排除候选与被封死形态的天花板定价，均未落地、未改变任何加载决策。与 R14-I 肥态轴正交性已在 §0 与 §1.3 说明（builtin 装载交叉点 vs custom 表 CPU 站点）；与 R18-I 基数轴正交性即两个输入文件（children.json vs providers.json）。

### 2.2 S8-I-1 重开条件两臂第七轮直接测量再确认——不铸 ID

- 文件级：`dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 0.84.1（package.json + frozen-lockfile 内容寻址，lockfile 两处 `@earendil-works/pi-ai@0.84.1` 站点直接确认）。
- 执行级：四 specifier 探针在 22.14 / 22.22 双引擎逐字同结局（附录 A.2 原始输出：`providers/../models.generated` → ERR_INVALID_MODULE_SPECIFIER；`dist/models.generated.js` / `models.generated` → ERR_PACKAGE_PATH_NOT_EXPORTED；`providers/all.models` → ERR_MODULE_NOT_FOUND）——0.84.1 内无任何导出形态的轻量权威注册表，无任何旁路。
- **裁决**：重开条件连续第十二轮未满足；所需的是 pi-ai 上游导出，不是仓内改动。此项为审计证据，非机制候选——未构造替代注册表、未触碰回退路径——**不铸 ID**。本轮轴 1 的 skip-cf 定价（E=1000 天花板 63-75 ms）同时是该围栏的 E 维收益上界证人：即便重开条件满足，收益主体也只在零流量假设格。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级即死）

| 扫描角度 | 处置 |
|---|---|
| custom id 跳回退 / 自制 union / 磁盘快照 / 进程内失手 memo | S8-I-1 硬禁 + R11-I/R10-I 三重围栏无 ID 收口；重开条件本轮七测仍未满足；本轮轴 1 首次给出 memo 的 E 维越线点（E≈130-160）与 skip 的 E 维天花板（63-75 ms@E=1000，零流量格）；派单明令不得铸 rename ID |
| resolveListedModelLazy custom-first 重排（先查 customProviders 再决定是否 import builtin 表） | S8-I-1 逐字同型（custom 遮蔽 builtin、惰性面与同步面分歧）；本轮 §1.3 点名未实现 |
| S1-I-8 custom 路径先构建再 find 换预建 Map（本轮新标定的镜像形态） | S1-I-8 维持排除：生产基数 E=2 差 ~0.1 ms；天花板 E×K≳2×10⁵ 为零生产流量假设格（§1.3；S12-B-2/S13-B-1 条件式落地物同型） |
| 配置基数分派双形态 / 按 E、K 阈值切换数据结构 | 零流量假设格引入分支复杂度；等价证据已备档但落地条件即重开条件，未触发 |
| `parseProvidersConfig` 微观（per-model 条件 spread 塌缩 / 单遍化 / enabled 双 parseModelRef） | 全网格亚 ms（P·K=10⁴ 仅 1.1 ms）；S3-I-5 / S2-I-6 / S1-I-8 邻域 µs 族；双遍融合即 S2-I-6（错误选择发散） |
| enabled 循环 Promise.all / 批量重叠 | S8-I-3 逐字（µs + 未知 id 错误选择分歧）；本轮阶梯显示循环体成本即失手税本体，无重叠窗口（S8-I-2 同一机制） |
| `smartChildPlan` assignments.find 换 Map / children 基数族 | S1-I-3 + R18-I 越线点 C≈2400 收口；本轮未触 C 维 |
| unknown-model 拒绝 / 错误端点瘦身 | R17-I 六端点定价收口：五端点贴地板、unknown-model 消费 = S8-I-1 健全性反例逐字同型；不补铸 |
| `run --flowchart` 命令类 / 采样归因 / 模块图普查 | R16-I 轴 1/轴 2 收口，前提未变，不重测不补铸 |
| 多 provider 阈值预取 providers/all / 肥 builtin 态 | R14-I 全域支配证明（交叉点不存在），前提复核未变；本轮肥 custom 态为正交新轴（§1.3），其结论同向（无交叉、无悬崖） |
| A/A 地板 / 引擎代 / 堆足迹再测 | R15-I 三轴收口；本轮带比对直接引用其 MDE 包络 |
| `import.meta.resolve` 同步预探测（R13-I 无名微观 #1） | 维持换名拒绝：S4-I-5 族镜像 + 失手锚点十度定界 <0.3 ms/构建 |
| `buildLiveCatalogConfig` 默认别名行 spread（R13-I 无名微观 #2） | 维持换名拒绝：每构建 ≤2 次 ~11 字段 spread（L72/L79），ns-µs 级 S1-I-8 邻域 |
| root / compat 作替代注册表源 | R12-I 直接测量封死（无表 + 严格更重） |
| providers/all 预热重叠 / enabled 循环并行 | S8-I-2 / S8-I-3 逐字（R16-I CPU 剖析为 S8-I-2 第四证人） |
| 回退后 per-provider 探针短路（mixed 态） | R11-I 无 ID 收口（≤3.5 ms/provider；跨调用穿线撞 X1-1） |
| pi-adapter barrel（`index.ts`）隐藏静态边 | 本轮再 grep 为无：全 src/ 无生产导入方 |
| flowchart 双 `buildLiveCatalogConfig` / providers.json 双读 | S1-I-1 / S1-I-2 逐字（warm 锚点 0.064-0.113 ms 旁证） |
| loadLearnedRouting / loadProvidersConfig 死载荷下沉 | S2-I-1 / S3-I-3 逐字（损坏时错误路径发散） |
| pause/inject 免 router / 免 learned；resume/answer 预读去重 / PAUSED 探测降级 | S2-I-2 / S9-G-1 / S2-I-4 / S4-I-2 逐字 |
| compile-cache / 分派拆分 / 剩余独占边 | S6-I-1..3 逐字（once-per-process 亚两位数 ms） |
| dist 布局 / 引擎钉版 | S5-I-2..5 族；R15-I 轴 2 已实测 22.22 取尽红利、24 无增量 |
| 一次性命令类（models/auth/doctor/inspect/episode/commits/adapt/pref/delete） | S1-I-4 / S7-I-4 / S2-I-5 / S3-I-4 类；once-per-process 噪声按派单直接否决 |
| 凭据面（auth status --all、credential store 微观） | S1-I-5 / S7-I-4 围栏，只读证据 |
| buildInvocation / invocationError / parseModelRef / parseProvidersConfig / parseCliErrorJson / setDefaultModels 微观 | S1-I-7 / S2-I-3 / S3-I-1 / S3-I-5 / S2-I-6 / S1-I-6 逐字（实测更慢或 ns 级） |
| pi-executor / cluster-tools 执行面微观 | 网络 I/O 支配的 live 面；µs 级，无复杂度类角度 |
| telemetry INVOCATION_CALL_OUTCOMES includes→Set / compareRunToRun 微观 | 表长 4，S1-D-8 族噪声 / 诊断聚合一次性 |
| writeAtomicJson 去 fsync / doctor-overlay jsonl 扫描 | S9-G-3 族（不得去 fsync）/ S3-J-4 族 + 一次性诊断类 |
| `builtinModelLazy` 动态 specifier / `_MODELS` 键过滤微观 | 命中路径被表解析成本（0.8-3.5 ms）支配、失手路径 ~0.10 ms；µs 族 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖、pi-ai exports、测试面、调用图五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选；唯一数十 ms 结构（custom/mixed 态 providers/all 失手回退）的全部消除形态收敛到同一个上游前提——pi-ai ship 轻量权威子路径——该前提的两种成立形态维持文件级 blocked（本轮第七次直接确认）；本轮新轴进一步证明配置输入规模维度亦无切片内候选：三个 O(E)/O(P)/O(K) 站点全线性、越线点分别在现实流量两个（E≈130-160）与四个（E×K≳2×10⁵）数量级之外，且 fat-E 成本的 ≥99.5% 是 S8-I-1 围栏所保护的语义强制查询本体。

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part B：1220 builtin 行 × 39 provider lazy===sync；part C：17 组失手/custom/对抗用例；part D 载荷探针：builtin 构建后 providers/all 保持冷 62.7 ms、custom 回退命中缓存 0.3 ms；part E：configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单项）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2（与 R9-I～R18-I 计数逐字相同）、`pnpm build` 绿。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **引擎注记**：22.14.0 的 doctor engines 预检 fail-closed 为设计行为（R9-I §3 记录在案）；nvm 22.22.2 为 gate/仿真引擎。依 R15-I 轴 1 纪律，候选验收须在 ≥22.19 引擎 + 交错窗上做——本轮无候选，抽测数字仅用于十二轮带比对（多窗中位数，双引擎同号）。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动；无赢家故未提交 `scripts/round19-r19i-equivalence-sim.ts`；临时探针（`r19i-probe.mjs`）测毕即删（gate 前删除，未进 lint 面）。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；本轮新轴以测量收口关闭配置输入规模缺口；S8-I-1 两臂七测维持 blocked；两个历轮无名微观维持换名拒绝）。
- 剩余赢家：无（唯一数十 ms 结构被 S8-I-1 健全性反例 + 硬不变量封死；配置输入维度的三个站点全线性、越线点在现实流量 2-4 个数量级之外，且 fat-E 主体是语义强制查询；重开条件所需的是 pi-ai 上游导出、enabled 基数增长两个数量级或 E×K 增长四个数量级，均非仓内改动）。
- 矩阵空洞：无（R8-I 78 格全覆盖；承重格十二轮同构复现 + 抽测集本轮补入配置基数三格；R7-I 配置态 × 命令类纪律全程遵守——configured 态夹具、builtin/custom 双态 + e10/e50/k500 三个配置基数态、children/track 双命令类；肥 builtin 态维持 R14-I 收口、测量学维持 R15-I 收口、命令类与归因维持 R16-I 收口、错误端点维持 R17-I 收口、children 基数维持 R18-I 收口）。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 8 格抽测 harness（/tmp/r19i-bench/spot.mjs，`<node二进制全路径> spot.mjs <同一路径>` 分别以两版本执行；承重 5 格与 R18-I A.1 逐字同构 + 新增 e10/e50/k500 配置基数格）

```javascript
// R19-I spot re-measure: floor + builtin/custom children+track cells
// (R9-I..R18-I harness lineage) + NEW axis: providers.json configuration-input
// cardinality cells (custom-table E enabled x P providers x K models/provider).
// Every prior round's custom fixture was P=1/K=2/E=2; the configuration-input
// scale dimension was never end-to-end priced in I (R14-I scaled *builtin*
// distinct-provider count; R18-I scaled the *children workload* file).
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r19i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.argv[2];
const WARMUP = 2, ITERS = 10;

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
// NEW axis fixture generator: P custom providers x K models each, first E
// catalog ids enabled (round-robin across providers so distinct-provider
// count in `enabled` is min(P, E)).
function fatCustomState(P, K, E) {
  const providers = [];
  for (let p = 0; p < P; p++) {
    const models = [];
    for (let k = 0; k < K; k++) {
      models.push({
        id: `m${String(k).padStart(4, "0")}`,
        name: `Model ${p}/${k}`,
        contextWindow: 100000, maxTokens: 4096,
        inputCostPerMTok: 0.3 + (k % 7) * 0.1, outputCostPerMTok: 1.5 + (k % 5) * 0.2
      });
    }
    providers.push({
      id: `custp${String(p).padStart(3, "0")}`, name: `Custom P${p}`,
      baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY", models
    });
  }
  const enabled = [];
  for (let i = 0; i < E; i++) {
    const p = i % P, k = Math.floor(i / P) % K;
    enabled.push(`custp${String(p).padStart(3, "0")}/m${String(k).padStart(4, "0")}`);
  }
  return {
    version: 1, enabled,
    primary: enabled[0], fast: enabled[enabled.length - 1],
    customProviders: providers
  };
}
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] },
  // NEW axis states: configuration-input cardinality.
  "custom-e10": fatCustomState(5, 2, 10),    // 5 providers x 2 models, all 10 enabled
  "custom-e50": fatCustomState(10, 5, 50),   // 10 providers x 5 models, all 50 enabled
  "custom-k500": fatCustomState(1, 500, 2)   // 1 provider x 500-model table, 2 enabled
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
for (const [name, cfg] of Object.entries(states)) {
  const dir = join(FIX, `state-${name}`);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
}
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
const CHILDREN = join(FIX, "children.json");
writeFileSync(CHILDREN, JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");

const cells = [
  // Bearing set (R9-I..R18-I lineage).
  { id: "version", args: () => ["--version"], state: undefined },
  ...["builtin", "custom"].flatMap((state) => [
    { id: `run-children@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] },
    { id: `run-track@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"] }
  ]),
  // NEW axis: configuration-input cardinality (same command class).
  ...["custom-e10", "custom-e50", "custom-k500"].map((state) => (
    { id: `run-children@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] }
  ))
];
function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, `state-${state}`), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 180000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-FAIL ${cell.id}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

原始输出（全部格 exit 0）：

```
== v22.14.0 ==
version: median=53.9ms min=53.3 p90=55.8 exit=0
run-children@builtin: median=108.5ms min=104.7 p90=113.8 exit=0
run-track@builtin: median=125.1ms min=118.7 p90=127.3 exit=0
run-children@custom: median=156.1ms min=143.6 p90=166.6 exit=0
run-track@custom: median=173.5ms min=165.8 p90=183.4 exit=0
run-children@custom-e10: median=162.9ms min=155.6 p90=165.7 exit=0
run-children@custom-e50: median=166.0ms min=162.4 p90=183.7 exit=0
run-children@custom-k500: median=162.7ms min=151.3 p90=167.1 exit=0
== v22.22.2 ==
version: median=51.2ms min=50.1 p90=52.1 exit=0
run-children@builtin: median=105.8ms min=103.2 p90=111.4 exit=0
run-track@builtin: median=126.9ms min=122.9 p90=128.1 exit=0
run-children@custom: median=132.2ms min=127.8 p90=138.2 exit=0
run-track@custom: median=150.8ms min=144.3 p90=159.2 exit=0
run-children@custom-e10: median=135.5ms min=128.4 p90=138.9 exit=0
run-children@custom-e50: median=140.8ms min=137.5 p90=142.8 exit=0
run-children@custom-k500: median=135.5ms min=132.8 p90=137.6 exit=0
```

### A.2 进程内锚点 + 可达性探针 + 配置基数阶梯（§1.2 / §1.3(b) / §2.2 证据；临时置于 /workspace 根以获得 node_modules 解析，gate 前删除；all/tables/miss/traversal 四模式与 R18-I A.2 逐字同构，`cfgscale` 模式为本轮新增）

```javascript
// R19-I in-process anchors + reachability probes + NEW axis: providers.json
// configuration-input cardinality ladder (temporary; deleted after measurement).
import { performance } from "node:perf_hooks";
const label = process.version;
const which = process.argv[2];
if (which === "all") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const dt = performance.now() - t0;
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  console.log(`${label} providers/all: cold=${dt.toFixed(1)}ms warm=${(performance.now() - t1).toFixed(3)}ms`);
} else if (which === "tables") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/anthropic.models");
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/openai.models");
  const t2 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const t3 = performance.now();
  console.log(`${label} anthropic.models=${(t1 - t0).toFixed(1)}ms openai.models=${(t2 - t1).toFixed(1)}ms providers/all-after-tables=${(t3 - t2).toFixed(1)}ms`);
} else if (which === "miss") {
  const N = 50; const times = [];
  for (let i = 0; i < N; i++) {
    const t0 = performance.now();
    try { await import("@earendil-works/pi-ai/providers/customco.models"); } catch { /* expected miss */ }
    times.push(performance.now() - t0);
  }
  const first = times[0];
  const rest = times.slice(1).sort((a, b) => a - b);
  console.log(`${label} miss-resolution: first=${first.toFixed(3)}ms repeat-median=${rest[Math.floor(rest.length / 2)].toFixed(3)}ms min=${rest[0].toFixed(3)}ms`);
} else if (which === "traversal") {
  for (const spec of [
    "@earendil-works/pi-ai/providers/../models.generated",
    "@earendil-works/pi-ai/dist/models.generated.js",
    "@earendil-works/pi-ai/models.generated",
    "@earendil-works/pi-ai/providers/all.models"
  ]) {
    try {
      await import(spec);
      console.log(`${label} REACHABLE: ${spec}`);
    } catch (e) {
      console.log(`${label} blocked: ${spec} -> ${e.code ?? e.message.slice(0, 60)}`);
    }
  }
} else if (which === "cfgscale") {
  // NEW axis: configuration-input cardinality ladder. Prices the three
  // in-slice sites that scale with the providers.json custom table:
  //   (1) parseProvidersConfig (providers-config.ts L118-144 + L146-187):
  //       O(E) enabled validation + O(P*K) custom-table validation spreads.
  //   (2) per-enabled-custom-id repeat-miss tax inside builtinModelLazy
  //       (listed-model-lazy.ts L21-38): per-provider import miss + warm
  //       providers/all getBuiltinModel consult, paid once per lookup.
  //   (3) the S1-I-8 site (listed-model-lazy.ts L54-56): customProviders.find
  //       O(P) + listedModelsFromCustom(custom) O(K) rebuild per lookup.
  // Counterfactuals price the excluded candidates' ceilings at each grid
  // point: map-cf keeps the mandatory builtin-first miss (S8-I-1 fence) and
  // only replaces the custom find/rebuild with a prebuilt Map; skip-cf is the
  // FORBIDDEN S8-I-1 shape (no builtin consult) measured only to calibrate
  // the reopen-condition ceiling, never a candidate.
  const { parseProvidersConfig } = await import("./dist/config/providers-config.js");
  const { resolveListedModelLazy } = await import("./dist/pi-adapter/listed-model-lazy.js");
  const { listedModelsFromCustom } = await import("./dist/pi-adapter/listed-model-common.js");

  function fatConfig(P, K, E) {
    const providers = [];
    for (let p = 0; p < P; p++) {
      const models = [];
      for (let k = 0; k < K; k++) {
        models.push({
          id: `m${String(k).padStart(4, "0")}`,
          name: `Model ${p}/${k}`,
          contextWindow: 100000, maxTokens: 4096,
          inputCostPerMTok: 0.3 + (k % 7) * 0.1, outputCostPerMTok: 1.5 + (k % 5) * 0.2
        });
      }
      providers.push({
        id: `custp${String(p).padStart(3, "0")}`, name: `Custom P${p}`,
        baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY", models
      });
    }
    const enabled = [];
    for (let i = 0; i < E; i++) {
      const p = i % P, k = Math.floor(i / P) % K;
      enabled.push(`custp${String(p).padStart(3, "0")}/m${String(k).padStart(4, "0")}`);
    }
    return { version: 1, enabled, primary: enabled[0], fast: enabled[enabled.length - 1], customProviders: providers };
  }
  const bestOf = (fn, n = 5) => {
    let best = Infinity;
    for (let i = 0; i < n; i++) {
      const t0 = performance.now();
      fn();
      const dt = performance.now() - t0;
      if (dt < best) best = dt;
    }
    return best;
  };
  const bestOfAsync = async (fn, n = 5) => {
    let best = Infinity;
    for (let i = 0; i < n; i++) {
      const t0 = performance.now();
      await fn();
      const dt = performance.now() - t0;
      if (dt < best) best = dt;
    }
    return best;
  };

  // Warm the authoritative fallback once so all ladder points measure the
  // steady (warm) regime; the one-time providers/all cold load is priced by
  // the `all` anchor and twelve rounds of end-to-end custom cells.
  await resolveListedModelLazy("warmup-prov", "warmup-model", []);

  console.log(`${label} cfgscale ladder (best-of-5 per site, ms):`);
  console.log("P\tK\tE\tjson-parse\tvalidate\tresolve-loop\tmap-cf\tskip-cf\trebuild1\tfind1");
  const grid = [
    [1, 2, 2],       // lineage custom state
    [5, 2, 10],      // spot custom-e10
    [10, 5, 50],     // spot custom-e50
    [1, 500, 2],     // spot custom-k500
    [50, 1, 50],     // E-ladder: distinct-provider misses
    [200, 1, 200],
    [1000, 1, 1000],
    [1, 1000, 50],   // K x E rebuild stress
    [50, 20, 1000]   // full-fat corner
  ];
  for (const [P, K, E] of grid) {
    const cfg = fatConfig(P, K, E);
    const raw = JSON.stringify(cfg, null, 2);
    const jsonParseBest = bestOf(() => JSON.parse(raw));
    const parsedInput = JSON.parse(raw);
    const validateBest = bestOf(() => parseProvidersConfig(parsedInput));
    const config = parseProvidersConfig(parsedInput);
    const refs = config.enabled.map((id) => {
      const slash = id.indexOf("/");
      return { providerId: id.slice(0, slash), modelId: id.slice(slash + 1) };
    });
    // (2)+(3): production resolution loop (buildLiveCatalogConfig L59-68 shape).
    const resolveBest = await bestOfAsync(async () => {
      for (const ref of refs) {
        await resolveListedModelLazy(ref.providerId, ref.modelId, config.customProviders);
      }
    });
    // map-cf: keep the mandatory builtin-first consult, replace only the
    // custom find+rebuild with a prebuilt Map (S1-I-8 candidate ceiling).
    const customById = new Map();
    for (const provider of config.customProviders) {
      for (const listed of listedModelsFromCustom(provider)) {
        customById.set(`${listed.providerId}/${listed.modelId}`, listed);
      }
    }
    const mapCfBest = await bestOfAsync(async () => {
      for (const ref of refs) {
        const builtin = await resolveListedModelLazy(ref.providerId, ref.modelId, []);
        if (builtin === undefined) customById.get(`${ref.providerId}/${ref.modelId}`);
      }
    });
    // skip-cf: FORBIDDEN S8-I-1 shape (skips the authoritative consult) —
    // reopen-ceiling pricing only.
    const skipCfBest = bestOf(() => {
      for (const ref of refs) {
        customById.get(`${ref.providerId}/${ref.modelId}`);
      }
    });
    // Isolated micro-sites at this grid point (single provider row).
    const firstProvider = config.customProviders[0];
    const rebuild1 = bestOf(() => { listedModelsFromCustom(firstProvider); });
    const lastId = config.customProviders[config.customProviders.length - 1].id;
    const find1 = bestOf(() => { config.customProviders.find((item) => item.id === lastId); });
    console.log(`${P}\t${K}\t${E}\t${jsonParseBest.toFixed(3)}\t${validateBest.toFixed(3)}\t${resolveBest.toFixed(3)}\t${mapCfBest.toFixed(3)}\t${skipCfBest.toFixed(3)}\t${rebuild1.toFixed(3)}\t${find1.toFixed(3)}`);
  }
  // Distinct-provider first-pass miss profile (cold specifiers, warm reader):
  // sequential first-time misses across F fresh provider ids.
  for (const F of [10, 100]) {
    const times = [];
    for (let i = 0; i < F; i++) {
      const t0 = performance.now();
      try { await import(`@earendil-works/pi-ai/providers/freshp${label}-${F}-${String(i).padStart(4, "0")}.models`); } catch { /* miss */ }
      times.push(performance.now() - t0);
    }
    const sum = times.reduce((a, b) => a + b, 0);
    const sorted = [...times].sort((a, b) => a - b);
    console.log(`${label} distinct-provider first-miss xF=${F}: sum=${sum.toFixed(2)}ms median=${sorted[Math.floor(F / 2)].toFixed(3)}ms max=${sorted[F - 1].toFixed(3)}ms`);
  }
}
```

原始输出（anchors：每模式全新进程，`all` 每引擎 3 次；traversal 双引擎逐字一致；cfgscale 双引擎各一次）：

```
v22.14.0 providers/all: cold=51.8 / 61.8 / 68.4 ms  warm=0.064-0.070 ms
v22.22.2 providers/all: cold=32.9 / 31.7 / 31.2 ms  warm=0.099-0.113 ms
v22.14.0 anthropic.models=3.5ms openai.models=2.3ms providers/all-after-tables=43.7ms
v22.22.2 anthropic.models=2.5ms openai.models=0.8ms providers/all-after-tables=29.4ms
v22.14.0 miss-resolution: first=1.861ms repeat-median=0.097ms min=0.066ms
v22.22.2 miss-resolution: first=2.045ms repeat-median=0.096ms min=0.074ms
v22.14.0 blocked: @earendil-works/pi-ai/providers/../models.generated -> ERR_INVALID_MODULE_SPECIFIER
v22.14.0 blocked: @earendil-works/pi-ai/dist/models.generated.js -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/models.generated -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/providers/all.models -> ERR_MODULE_NOT_FOUND
v22.22.2 blocked: (同上四行，双 Node 逐字一致)
v22.14.0 cfgscale ladder (best-of-5 per site, ms):
P	K	E	json-parse	validate	resolve-loop	map-cf	skip-cf	rebuild1	find1
1	2	2	0.002	0.009	0.278	0.208	0.001	0.001	0.000
5	2	10	0.007	0.014	1.065	0.843	0.002	0.001	0.000
10	5	50	0.029	0.043	3.989	3.667	0.007	0.001	0.000
1	500	2	0.169	0.138	0.290	0.143	0.001	0.018	0.000
50	1	50	0.037	0.028	3.664	3.671	0.008	0.000	0.001
200	1	200	0.146	0.103	15.652	14.653	0.030	0.000	0.002
1000	1	1000	0.708	0.408	75.158	73.952	0.153	0.000	0.013
1	1000	50	0.346	0.090	5.569	3.548	0.009	0.033	0.000
50	20	1000	0.386	0.137	73.904	72.430	0.163	0.001	0.001
v22.14.0 distinct-provider first-miss xF=10: sum=1.00ms median=0.103ms max=0.146ms
v22.14.0 distinct-provider first-miss xF=100: sum=7.47ms median=0.072ms max=0.113ms
v22.22.2 cfgscale ladder (best-of-5 per site, ms):
P	K	E	json-parse	validate	resolve-loop	map-cf	skip-cf	rebuild1	find1
1	2	2	0.002	0.009	0.298	0.170	0.001	0.001	0.000
5	2	10	0.007	0.011	0.797	0.941	0.002	0.001	0.000
10	5	50	0.028	0.025	3.909	3.597	0.008	0.002	0.000
1	500	2	0.172	0.137	0.337	0.139	0.001	0.018	0.000
50	1	50	0.036	0.026	3.445	3.549	0.007	0.000	0.001
200	1	200	0.142	0.109	13.176	12.988	0.030	0.000	0.002
1000	1	1000	0.674	0.433	65.777	63.164	0.153	0.000	0.012
1	1000	50	0.347	0.088	5.375	3.018	0.009	0.033	0.000
50	20	1000	0.384	0.136	63.028	62.575	0.157	0.001	0.001
v22.22.2 distinct-provider first-miss xF=10: sum=0.80ms median=0.066ms max=0.154ms
v22.22.2 distinct-provider first-miss xF=100: sum=6.56ms median=0.063ms max=0.099ms
```

MORE_OPTIMA=no
