MODEL_SLUG=claude-fable-5-thinking-xhigh

# R19-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第十九遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 19 / R19-A（Round 1–18 同区第十九遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `6e2bdf6`（含 R18-I 空枚举收口与 Round 19 开轮）
**分支:** `cursor/r19-a-tracking-nineteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S19-A-\* ID，切片零代码改动；
整片预算收口第十七次复核成立，并新增两条实测轴——「at-least-once 重投递
面（幂等 replay 端点普查+定价）」补上 `applyTrackingGate` 公开结局格中
十八遍搜查史上从未定价的最后两个非抛出结局类（find#1 GATE_TRANSITION
idempotencyKey 命中与 find#2 TRACKING_ASSESSMENT seq+hash 命中——二者
仓内不可达但契约存活），「directive 类收口（wait_user 格）」补上锚点
矩阵一直缺失的第三个 fresh-apply 追加形态（TA+GT+RUN_WAITING_FOR_USER，
生产零流量但 parse 合法）。两轴合并后，切片公开 run 面两个函数的
**结局格自此全部有实测价**：fresh-apply ×3 directive 类（本轮补
wait_user）+ replay ×2 端点（本轮）+ reject（R18-A 7b）+ skip ×5 类
（R18-A 7a）——且每个 replay 仅为 apply 格的 ~22%，任何合法
apply/skip/replay 混合的每 run 成本都不高于全 apply 锚点，R18-A 组成
天花板从 {apply, skip} 扩张到 {apply, skip, replay} 后依然成立。两轴均
只产出归档证人与既有拒绝记录的数据补充，未产出可落地候选。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R18-A 至本轮基线
（`6e2bdf6`）**逐字节未变**（`git diff 7acb666..6e2bdf6 -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，十九遍全程零 diff；
清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2）并全部实际重读）。
调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..6e2bdf6 -- src/` **完全为空**（跨 R10…R19 开轮十段全部
为 docs 提交），生产调用方经全库 grep 复核仍为 `supervisor.ts:483`
（applyTrackingGate/nextTrackingSeq；两个 settle 调用点 601/711 均不传
trackingAssessment ⇒ 生产 O(1) no-op，第十次复验）/ `coordinator.ts:444` /
`flowchart-run.ts:320`（applyChildThreeLine），每子结果一次（~5 次/run），
事件表几十级（41）。R18-A 的 ~64–74 µs/run 预算天花板经本轮在当前 VM
（Node v22.22.2；本 VM 默认 v22.14.0，按 engines >=22.19.0 经 nvm 切换）
实测复核为 **~66–75 µs/run**（五格锚点矩阵三次独立运行 13.2–15.1
µs/gate，与 R18-A 12.7–14.8 / R17-A 13.0–14.8 同带；no-contract 格
14.4–15.1 复现 R11-A 以来的「PIC/基准次序」现象，单格横向比较不具意义；
量级结论不变——稳态下即使切片零成本，节省上界 ~0.07 ms/run，仍比落地线
低约三个量级）。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
数据补充（2 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项 +
R15-A 5 项 + R16-A 4 项 + R17-A 4 项 + R18-A 4 项换名检查全部复核维持，
未追认任何 S12…S18-A-\* ID），按令一律不铸新 ID。作为空枚举的实证支撑，
本轮新落档两组数据（§1，三次独立运行稳定；fuzz 计数三次逐位一致）：

1. **at-least-once 重投递面：幂等 replay 端点普查+定价（axis 8a）**：
   `applyTrackingGate` 持有两个非抛出、零追加的 replay 端点——find#1
   （GATE_TRANSITION 携带同 `${hash}:${seq}` idempotencyKey ⇒ 原样返回
   既有 transition 的 directive/transitionId/to）与 find#2
   （TRACKING_ASSESSMENT 携带同 seq+hash ⇒ 重算 directive + 一遍
   currentGateStatus 折叠）。**仓内不可达引理（seq 单调性）**：全部三个
   生产调用方（child-tracking:45 / supervisor:487 默认臂）都以
   `nextTrackingSeq = max(seq)+1` 派生 expectedSeq，严格大于表内任何
   GT/TA 携带的 seq ⇒ fresh 路径两个 find 恒 miss（F1 900/900 分布级
   证人，三次逐位一致）；契约存活面是公开 API 直调与
   `settleSupervisedOutcome` 显式传 `opts.expectedSeq` 的重投递调用方
   （切片外一跳，本轮 grep 复核仓内两个 settle 调用点均不传）。定价
   （E=42/44 更新表，三次运行区间）：**find#1 命中 2926–2959 ns、
   find#2 命中 2834–2882 ns——各为 apply 格的 21.3–22.5%，~67–70% 由
   CAS hash 复验支配**，once-per-redelivery、无规模乘数（find 斜率已被
   R17-A mix 轴覆盖）。F1 同时钉死 replay 面三条契约事实：**纯性**
   （900/900 零追加且事件表按引用原样返回）、**数据一致性**（directive/
   transitionId/runStatus 与原 apply 逐位一致或与独立折叠一致）、
   **CAS 先于幂等**（50/50 损坏 hash 重投递全部 throw，永不静默
   replay——幂等 find 前置的「replay 快路径」候选因此 fail-open，
   §0-1 拒列）。结论：**重投递混入只会让每 run 成本更低**（每 replay
   仅 ~22% apply 格），R18-A 组成天花板扩张到
   {apply, skip, replay} 全混合后成立。
2. **directive 类收口：wait_user 格（axis 8b）**：锚点矩阵十八遍只
   驱动过 directive none（+1 事件）与 queue_analysis（+3：TA+GT+
   RUN_BLOCKED）；第三类 wait_user（+3：TA+GT+RUN_WAITING_FOR_USER，
   内含 createMessageId）从未定价。生产不可达性复核：from-child 路径
   humanInput 恒 `{}` 且不传 maxItems ⇒ askUser 与 user-reject-stop
   两个触发臂均为零流量（R16-A §0-3 维持）；但两个变体均 **parse 合法**
   （F1 双变体 JSON 往返 parseTrackingAssessment 接受、e2e 追加形态
   +3 逐事件核对、messageId ≡ `msg_<evt 后缀>`、GT reasonCode 分别为
   codes[0] 与 user-reject-stop），故属公开契约面。定价
   （applyTrackingGate 隔离，E=41，三次运行区间）：**none 6.23–6.46 /
   queue_analysis 7.70–8.50 / wait_user(askUser) 7.92–8.57 /
   wait_user(user-reject-stop) 7.85–8.07 µs——wait_user 落在
   queue_analysis 同带，无悬崖、无空洞**；directive 维度就此闭合。
   F1 附带钉死 mapGateDirective 的 FAIL_CLOSED 兜底对 parse 合法 gate
   不可达（静态：kind ∈ {hard,soft,none} 全被前三臂覆盖；分布级：
   463 个 GT + 双 wait_user 变体 reasonCode 无一为 FAIL_CLOSED）。

R12-A 分解的五大组件占比（hashAssessment×3 ~44–46% / validateEvent
~21–23% / turn ~13–14% / prescore×2 ~10–12% / O(E) 五扫描+拷贝 ~2.4%）
在本 VM 逐一复现（§1 表），R18-A skip 组成定价四类抽测复现同带
（3.0–14.6% of apply）。第十九遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；未触碰在飞的
  R18-H / R18-J 分支与文件。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表 545 行，含
  S13-B-1；R12-A…R18-A 空枚举未铸 ID，确认）/ PROGRESS.md /
  round-19/PLAN.md / round-18/R18-A.md / round-17/R17-A.md /
  R14-A…R16-A 收口按需。EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md
  零触碰。
- 环境：本 VM 默认 Node v22.14.0，按 engines（>=22.19.0）经 nvm 切至
  v22.22.2 后 `pnpm install --frozen-lockfile`，全部测量在 v22.22.2
  完成。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或数据补充，**一律不铸 S19-A-\* ID**。R12-A 13 项、R13-A 8 项、
  R14-A 3 项、R15-A 5 项、R16-A 4 项、R17-A 4 项、R18-A 4 项换名检查
  全部复核维持，不再逐条重列；以下 2 项为本轮**新记录**）：
  1. **幂等 find 前置于 CAS 的 replay 快路径**（把两个幂等 find 提到
     hashAssessment 复验之前，使重投递命中免付 ~1.95 µs hash 复验）——
     **不等价，fail-open 反例**：idempotencyKey 内嵌调用方自报的
     assessmentHash，未经 CAS 验证即按键命中会让「损坏 hash 恰好拼出
     既有键」的重投递**静默 replay 而非 throw**（F1 以 50/50 损坏 hash
     重投递全部 throw 钉死现行「CAS 先于幂等」次序为契约行为）；即使
     加防御也只作用于仓内零流量、once-per-redelivery 的面，节省上界
     ~2.0 µs/次 = R1-A CAS fail-closed 契约 + S8-A-1 幂等扫描结构族 +
     验收标准 once-per-fault/redelivery 排除条款，拒列；
  2. **find#2 replay 端点的 directive 重算/状态折叠去重**（find#2 命中
     后 mapGateDirective + 一遍 O(E) currentGateStatus 折叠，理论上可
     缓存或复用 find#1 扫描的中间态）——缓存 = X1-1 隐藏状态本体；
     结构改写 = S11-A-2（none 路径死扫描收窄需动公开
     GateApplyResult）+ X0-4 签名族的 replay 换位点；且该端点仓内零
     流量、全程 ~2.9 µs 已由 hash 复验支配（折叠仅 ~76 ns），拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  replay 端点删除/fresh 路径 find 跳过 = S8-A-1 本体（变体 A 不等价：
  幂等 no-op 变重复追加；变体 B 61–65 ns）；CAS hash 复验信任调用方 =
  R12-A §0-13 三 hash 去重本体（R1-A fail-closed）；wait_user 路径
  createMessageId 闭包 = R16-A §0-3 本体（生产零流量，本轮补定价
  7.9–8.6 µs 格）；wait_user/queue_analysis 事件构造享元 =
  S7-A-3/S1-A-7 可观察身份族；第二次 prescore = S1-A-2；evidenceRefsOf
  双算 = S2-A-2；六维双 `.some` 融合 = S2-A-6；死滚轮尾 = S9-A-1；
  幂等双 find/死扫描 = S8-A-1 / S11-A-2；写侧 validateEvent = S4-G-2 族
  （~21–23% 合同地板，实现在 events.ts，G 切片）；哈希面 =
  X1-2/S5-H-3/S5-A-2 锁死；human-score 正则面 = X0-6 + S7-A-1 +
  R14-A §0-2；`every×includes` Set 化 = S1-A-4/X4-7（C/R ≈ 10³ 重开
  条件，R15-A 轴 1；roller 第四站点 R17-A §0-2）；turn 恒定输入常量
  折叠 = R12-A §0-11 伞 + R16-A §0-2；skip 守卫次序/享元/prescore
  复用 = R18-A §0-1/2/4 维持；拒绝路径廉价化整类 = R18-A §0-3 维持
  （X0-5 + X0-4 + S4-G-2）。
- 未重开任何 X\* / S1-\* … S18-\* 条目。R18-A 两轴（skip 组成、拒绝
  终点普查）**只做复核所需的抽测**（skip 四类同带复现，见 §1；28 个
  throw 终点普查不重测）；R17-A 两轴（事件表组成 mix、冷进程预算）、
  R16-A 三轴（L/V/S）、R15-A 三轴（C/R、GC、JIT）、R14-A 两轴（E、
  max-codes）**不补铸、不重测超出复核所需的部分**；S11-A-1 / S11-A-2 /
  S10-A-1 / S9-A-1 / S8-A-1..3 / S7-A-1..4 / X0-4/X2-4 / X0-6 /
  S6-A-1 专项复核：本轮零触碰、零重提，全部维持关闭。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足。不声称 Outcome-supported；Checkpoint
  F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本
  （按令不建 `scripts/round19-r19a-equivalence-sim.ts`），仿真全文见
  本报告附录。不开 PR。

## 1. 预算支配论证复核 + 两条新轴（方法第 1、2 步）

R3-A §3 … R18-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..6e2bdf6 -- <切片>` 为空，逐字节
   一致（十九遍全程零 diff）——R1-A…R18-A 全部测量与排除按令转移，但
   数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨十段）**：`git diff 89f0455..6e2bdf6 -- src/`
   **完全为空**（R10 开轮以来全部提交均在 docs/ 下）。生产调用方三点位
   （supervisor.ts:483 / coordinator.ts:444 / flowchart-run.ts:320）、
   settle 调用点 601/711 不传 assessment（首行
   `if (assessment === undefined) return`）、切片对外导入面
   （events.ts:36 写侧屏障 / supervisor:28,30 / gate-apply:4 /
   child-tracking:3-8）、`src/track/` 对切片零导入、
   `parseTrackingAssessment` 唯一 src/ 调用方 events.ts:738、
   `hashSummary` 生产零流量（roller.ts:68 唯一调用点）、
   isolation.ts 双函数与 `executionAuthority` 零 src/ 生产调用方，
   经本轮全库 grep 全部复验，无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               14.4      14.8      15.1  µs/gate (+1 event)
PASSED x contract(3)               13.2      13.2      13.3  µs/gate (+1 event)
FAILED x no-contract               14.6      14.3      14.6  µs/gate (+3 events)
FAILED x contract(3)               14.7      14.1      14.4  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.8      14.9      14.8  µs/gate (+3 events)
=> ~66–75 µs/run（稳态热）
```

与 R18-A（12.7–14.8 µs/gate ⇒ 64–74 µs/run）同带；五格三次运行
**无悬崖、无空洞**。no-contract 首格 14.4–15.1 复现 R11-A 以来的
「PIC/基准次序」现象（no-contract 格略贵于 contract 格，单格横向比较
不具意义）。稳态下即使把整个切片优化到零成本，节省上界 ~0.07 ms/run，
仍比战役落地线（数十~数百 ms 或复杂度类下降）低**约三个量级**。
**支配论证（稳态口径）第十七次复核成立**；生产冷态口径维持 R17-A 轴
6b 裁决（[2.7, 8.5] ms/run once-per-run，不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R18-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R18-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1951–2008 ns 每次 ⇒ **5.85–6.02 µs** | 5.78–5.79 µs | ~44–46% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 332–366 ns + 第三次 hash） | **2.80–3.03 µs** | 2.96–2.99 µs | ~21–23% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.71–1.85 µs** | 1.68–1.73 µs | ~13–14% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 666–783 ns 每次 ⇒ **1.33–1.57 µs** | 1.33–1.34 µs | ~10–12% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 78 + seq 56–63 + 双幂等 find 38–40×2 + status 75–76 + `[...events]` 26–27 ns） | **314–323 ns** | 278–283 ns | **~2.4%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以 13.2–13.3 µs 锚点格计；`applyTrackingGate` 整体隔离实测
6.11–6.24 µs，与 R18-A（5.98–6.02）同带略高。

**R18-A skip 组成定价抽测复现**（四类，20k 次/格，三次运行区间；与
R18-A 272–1712 ns = 2.1–13.5% 同带）：terminal undefined 393–438 ns
（3.0–3.3%）/ 无 episode 绑定 418–459 ns（3.1–3.5%）/ verification
UNOBSERVED 518–578 ns（3.9–4.4%）/ FAILED 零 evidence refs
1795–1919 ns（13.6–14.6%）——每个合法 skip 类仍严格便宜于 apply 路径，
组成天花板半边维持。

**本轮新轴 8a：at-least-once 重投递面（幂等 replay 端点普查+定价）**
（重投递 = 同一 (assessment, hash, expectedSeq) 命令对已追加表再投递；
20k 次/格，三次运行区间）：

```text
端点普查（applyTrackingGate 非抛出结局全格）:
  fresh-apply（find#1 miss + find#2 miss + 追加）   锚点矩阵（上表）
  replay find#1（GT idempotencyKey 命中，E=44）     2926–2959 ns/次
    = apply 格的 22.0–22.5%；hash 复验占 ~67–68%
  replay find#2（TA seq+hash 命中，E=42）           2834–2882 ns/次
    = apply 格的 21.3–21.9%；hash 复验占 ~68–70%
  （CAS throw 已由 R18-A 7b 定价 12.7–13.1 µs once-per-fault）

仓内不可达引理（seq 单调性，F1）:
  三个生产调用方均以 nextTrackingSeq = max(seq)+1 派生 expectedSeq
  > 表内任何 GT/TA 携带 seq ⇒ fresh 路径两 find 恒 miss
  （900/900 分布级证人，三次运行逐位一致）
契约存活面: 公开 applyTrackingGate 直调 + settleSupervisedOutcome
  显式 opts.expectedSeq（切片外一跳；仓内 601/711 不传）
```

四个结论：

1. **组成天花板扩张成立**：每个 replay 端点只为 apply 格的 ~22%（且
   严格便宜于任何 fresh-apply 格）⇒ **任何合法 apply/skip/replay
   混合的每 run 成本都不高于全 apply 锚点**，R18-A 7a 的组成半边从
   {apply, skip} 扩张到 {apply, skip, replay} 后关死。
2. **replay 面三条契约事实钉死（F1）**：纯性（900/900 零追加、事件表
   按引用原样返回、replay 期间 generateEventId 永不被调）；数据一致性
   （find#1 臂 directive/transitionId/runStatus 与原 apply 逐位一致
   463/463；find#2 臂 directive 重算一致且 runStatus ≡ 独立
   currentGateStatus 折叠 437/437）；**CAS 先于幂等**（50/50 损坏
   hash 重投递全部 throw——重投递不豁免 fail-closed）。
3. **once-per-redelivery、无规模乘数、仓内零流量**：单次 replay
   ~2.8–3.0 µs 由 CAS hash 复验支配（~1.95 µs），find 命中扫描面斜率
   已被 R17-A mix 轴（GT-sat 38.3–42.9 ns/event）覆盖；重投递本身是
   调用方故障恢复态而非生产稳态。验收标准明文排除该类。
4. **不产生候选**：幂等 find 前置（免 hash 复验）= §0-1 fail-open
   拒列；find#2 重算去重 = §0-2 拒列；find 删除 = S8-A-1 本体；hash
   复验信任调用方 = R12-A §0-13 本体。

**本轮新轴 8b：directive 类收口（wait_user 格）**（applyTrackingGate
隔离，E=41，20k 次/格，三次运行区间）：

```text
directive none（+1 TA）                             6.23–6.46 µs
directive queue_analysis（+3: TA+GT+RUN_BLOCKED）   7.70–8.50 µs
directive wait_user via askUser（+3: TA+GT+
  RUN_WAITING_FOR_USER，内含 createMessageId）      7.92–8.57 µs
directive wait_user via user-reject-stop（+3）      7.85–8.07 µs
```

三个结论：

1. **directive 维度闭合，无悬崖**：wait_user 两变体全部落在
   queue_analysis 同带（+3 事件形态的差异只在第三事件构造：
   RUN_WAITING_FOR_USER 的 createMessageId + slice vs RUN_BLOCKED 的
   requiredEvidence 拷贝）；锚点矩阵的 directive 半边（none /
   queue_analysis）外推到第三类成立。
2. **生产零流量、parse 合法（F1）**：from-child 路径 humanInput 恒
   `{}` 且不传 maxItems ⇒ askUser / user-reject-stop 两臂在
   observationFromChild 形态上不可达（R16-A §0-3 维持）；但双变体
   JSON 往返 parseTrackingAssessment 接受、e2e 追加形态逐事件核对
   （TA+GT+WAIT、messageId ≡ `msg_<evt 后缀>`、reasonCode 分别为
   codes[0] 与 user-reject-stop）、重投递 replay 保 transitionId——
   该格属公开契约面而非死代码。
3. **FAIL_CLOSED 兜底不可达性双证**：静态（parse 后 kind ∈
   {hard,soft,none} 被 mapGateDirective 前三臂完全覆盖）+ 分布级
   （F1 全部 GT reasonCode 无一 FAIL_CLOSED）；该兜底是零成本防御纵深
   （S2-A-4 族已录保留），不铸 ID。

**重开条件全景更新（在 R18-A §1 表基础上）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| G（gates/run，caller 侧） | ~5 | ≈ 654–763 | ~2.1 | R16-A 锚点算术 |
| G 组成（apply/skip 混合） | 全 apply | skip 混入只降不升 | — | R18-A |
| **G 组成（apply/skip/replay 全混合）** | 全 apply | **replay 混入只降不升（每 replay ~22% apply 格）——无独立穿越点** | — | **本轮** |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run，无重复乘数 | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–13.3 µs/fault，无重复乘数 | — | R18-A |
| **重投递面（once-per-redelivery）** | 0 次/run | **~2.8–3.0 µs/次，无重复乘数** | — | **本轮** |

C 轴（~2.5 个量级，R15-A）仍是切片内部最近的规模悬崖，第一优先重开
对象不变（S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）。本轮两轴
关闭的是**结局维**：每 gate 成本的输入规模维（R14–R16）、组成维
（R17 事件混合 + R18 apply/skip 混合 + 本轮 replay 混合）、温度维
（R17 冷进程）、故障维（R18 拒绝面 + 本轮重投递面）、结局维（本轮
directive 类 + replay 端点补全后，公开 run 面结局格 {apply×3,
skip×5, replay×2, reject} 全部有实测价）至此全部有实测封闭。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 2 项 + R12-A…R18-A
41 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、数据
补充或本体，按「换名不铸 ID」纪律未列任何 S19-A-\* 候选。空枚举与
R12-A…R18-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第十九遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `gate-apply.ts` | **replay 两端点普查+定价**（8a：find#1 2926–2959 / find#2 2834–2882 ns once-per-redelivery，hash 复验支配；仓内不可达引理 900/900）；**CAS 先于幂等次序钉死为契约**（50/50 损坏 hash 重投递全 throw ⇒ find 前置快路径 fail-open，§0-1 拒列）；**wait_user 格定价**（8b：7.85–8.57 µs 同带无悬崖；createMessageId 闭包 = R16-A §0-3 维持）；FAIL_CLOSED 兜底双证不可达（零成本防御纵深保留）；find#2 重算去重 §0-2 拒列；S11-A-2 / S8-A-1 / S2-A-4 / R15-A §0-1 replay 死字段维持 | 无新候选 |
| `child-tracking.ts` | seq 单调性引理（expectedSeq = max+1 恒新鲜 ⇒ 本文件永不触发 replay 端点——replay 面完全在公开 API 侧）；R18-A skip 四类抽测同带复现（393–1919 ns）；skip 守卫次序/享元 = R18-A §0-1/2 维持；runId 死守卫 = R18-A 7a 结论 3 维持 | 无新候选 |
| `from-child.ts` | 8b 生产不可达性复核（humanInput `{}` + 不传 maxItems ⇒ askUser/user-reject-stop 两臂零流量）；guard-3 skip 1795–1919 ns 同带；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `types.ts` | replay 定价把 hashAssessment 面的「不可省性」再证一次（重投递也付全额 CAS 复验——信任调用方 = R12-A §0-13 本体拒列）；wait_user 双变体 parse 合法性证人（parse 面对 askUser/user-reject-stop 无特殊臂）；R12-A F1 哈希天花板 + R13-A §0-2 + R17-A §0-1 冷维度维持；S2-A-5 / X1-2 / S5-H-3 / S1-A-8 / X0-5 维持 | 无新候选 |
| `turn.ts` | replay/skip 流量均不进 turn（fresh apply 独占）；R13-A 八子组件剖面维持（本轮整体 1.71–1.85 µs 同带）；R16-A §0-2/§0-4 维持 | 无新候选 |
| `prescore.ts` | replay 流量不进 prescore（hash 复验前已由调用方付过）；C/R 轴维持 R15-A 穿越点；S1-A-4 / X4-7 / S6-A-1 / S8-A-3 / R12-A §0-2..5 维持 | 无新候选 |
| `roller.ts` | replay/skip 流量不进 roller；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 / R17-A §0-2（O(Q·D) 第四站点）维持 | 无新候选 |
| `gates.ts` | 8b 两变体不经 evaluateGates（直接以 parse 合法 assessment 驱动公开 API）——gates 面生产可达 codes 引理（R14-A F1）不受影响；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 享元自败维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（humanInput {} ⇒ extractHumanScore({}) 短路）；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 维持 | 无新候选 |
| `combined-score.ts` | TypeError 终点归类维持（R18-A 7b：对 computePrescore 产物不可达 = 防御纵深保留第五次记录）；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |
| `config.ts` | applyUserThreshold throw 终点归类维持（R18-A 7b：用户改阈值冷面）；X0-7 阈值冻结维持 | 无新候选 |
| `analysis.ts` | S8-A-2 维持（零 src/ 生产调用方 + sanitize choke point，本轮 grep 复验） | 无新候选 |
| `isolation.ts` / `index.ts` | O(1) 恒等函数 / 纯再导出；`executionAuthority`（gate-apply 内）与 isolation 双函数零 src/ 生产调用方复验（零成本，无可省项）；惰性 import = R12-A §0-10 维持 | 无新候选 |

**对 R18-A 声明的裁决**：组成维（apply/skip 混合）与故障维（拒绝面）
收口在其维度上成立；本轮把搜查转向**结局维（replay 端点 + directive
类补全）**，两处新数据**全部只产出归档证人与既有拒绝记录的数据补充，
未产出可落地候选**。收口的预言力第十九遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R18-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F1: 900 fuzz trials; 437 find#2 (TA) + 463 find#1 (GT) replays, all pure
    (zero append, table by reference, data consistent); 900 seq-monotonicity
    witnesses (fresh path can never hit either find in-repo); 50/50
    corrupted-hash redeliveries all threw (CAS precedes idempotency);
    reasonCode never FAIL_CLOSED; wait_user x2 variants parse-legal e2e;
    zero violations
anchor: 14.4 / 13.2 / 14.6 / 14.7 / 14.8 µs/gate => ~66–74 µs/run
decomposition: hash×3=6023ns | validateEvent=3032ns | turn=1708ns
    | prescore×2=1339ns | 5 scans+copy=314ns | applyTrackingGate whole=6.24µs
skip re-confirm: 423 / 459 / 578 / 1919 ns = 3.2/3.5/4.4/14.6% of apply
8a: replay GT 2959ns (22.5%, hash ~68%) | replay TA 2882ns (21.9%, hash ~70%)
8b: none 6.46 | queue 8.50 | wait_user(ask) 8.57 | wait_user(urs) 8.07 µs
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F1: 900 trials / 437+463 replays / 900 witnesses / 50/50（逐位同 run 1）
anchor: 14.8 / 13.2 / 14.3 / 14.1 / 14.9 µs/gate => ~66–75 µs/run
decomposition: hash×3=5852ns | validateEvent=2952ns | turn=1845ns
    | prescore×2=1566ns | 5 scans+copy=323ns | whole=6.19µs
skip re-confirm: 438 / 428 / 518 / 1795 ns = 3.3/3.2/3.9/13.6% of apply
8a: replay GT 2926ns (22.2%, hash ~67%) | replay TA 2849ns (21.6%, hash ~68%)
8b: none 6.23 | queue 7.84 | wait_user(ask) 8.12 | wait_user(urs) 7.92 µs
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F1: 900 trials / 437+463 replays / 900 witnesses / 50/50（逐位同 run 1）
anchor: 15.1 / 13.3 / 14.6 / 14.4 / 14.8 µs/gate => ~67–75 µs/run
decomposition: hash×3=5854ns | validateEvent=2801ns | turn=1724ns
    | prescore×2=1331ns | 5 scans+copy=315ns | whole=6.11µs
skip re-confirm: 393 / 418 / 561 / 1916 ns = 3.0/3.1/4.2/14.4% of apply
8a: replay GT 2933ns (22.0%, hash ~67%) | replay TA 2834ns (21.3%, hash ~69%)
8b: none 6.25 | queue 7.70 | wait_user(ask) 7.92 | wait_user(urs) 7.85 µs
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立仿真运行 F1 计数逐位一致（900 trial、437/463 find#2/find#1
分割、900 seq 引理证人、50/50 CAS 次序 throw、双 wait_user 变体形态
核对确定性重现）；锚点五格三次同带无悬崖；8a 两端点与 8b 四格三次运行
区间紧。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S19-A-\*）。换名检查 2 项新记录见 §0（1–2），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件本轮**新增两条收口**：

1. **结局维（公开 run 面结局格）收口**：`applyTrackingGate` 的全部
   结局类至此有实测价——fresh-apply ×3 directive 类（none 6.23–6.46 /
   queue_analysis 7.70–8.50 / wait_user 7.85–8.57 µs）、replay ×2
   端点（2.83–2.96 µs once-per-redelivery）、reject（R18-A 7b
   12.7–13.1 µs once-per-fault）；`applyChildThreeLine` 侧为 apply
   锚点 ×5 格 + skip ×5 类（R18-A 7a，本轮四类抽测同带）。组成推论：
   **全 apply 锚点是任意合法 apply/skip/replay 混合下的每 run
   天花板**。该轴随幂等键格式 / find 谓词 / mapGateDirective 分派
   结构改变而需重测；在此之前「按结局类特化」候选（replay 快路径、
   wait_user 特化、directive 分派重排）无收益池。
2. **重投递面（once-per-redelivery）收口**：两个 replay 端点
   ~2.8–3.0 µs/次，由 CAS hash 复验支配、无规模乘数、仓内零流量
   （seq 单调性引理 900/900）。重开条件是**契约性**的：仅当 (a)
   「CAS 先于幂等」次序不再是契约（届时 §0-1 快路径需重审——但其
   fail-open 反例独立于性能存在），或 (b) 某调用方开始在热路径上
   高频重投递（届时应修调用方而非切片），或 (c) settleSupervisedOutcome
   的 expectedSeq 契约面改变，才需重审。C 轴（~2.5 个量级，R15-A）
   仍是切片内部最近的规模悬崖，第一优先重开对象不变（S1-A-4/X4-7
   家族 `every×includes` 站点 Set 化，现含 roller 第四站点）。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf19a01`–`0xf19a02`。

```ts
/**
 * R19-A deterministic anchor + decomposition + NEW-axis simulation
 * (nineteenth pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R18-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R18-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation,
 *       5-scan O(E) inventory), plus a compact re-confirmation of the
 *       R18-A skip-path pricing (composition ceiling);
 *   (c) NEW axis 8a: AT-LEAST-ONCE REDELIVERY face (idempotent-replay
 *       endpoint census + pricing). applyTrackingGate has two
 *       non-throwing, non-appending replay endpoints that no prior pass
 *       ever priced: find#1 (GATE_TRANSITION idempotencyKey hit) and
 *       find#2 (TRACKING_ASSESSMENT seq+hash hit). Both are in-repo
 *       unreachable (every caller derives expectedSeq = nextTrackingSeq
 *       = max(seq)+1, strictly above any carried seq, so neither find
 *       can hit on a fresh path) but contract-live through the public
 *       faces (applyTrackingGate directly; settleSupervisedOutcome with
 *       a pinned opts.expectedSeq one hop above the slice). This axis
 *       prices both endpoints as once-per-redelivery costs and proves
 *       the replay face is PURE (zero append, table returned by
 *       reference, deterministic data equal to the original apply).
 *   (d) NEW axis 8b: DIRECTIVE-CLASS completion (wait_user cell).
 *       The anchor matrix has only ever exercised directive none
 *       (+1 event) and queue_analysis (+3 events). The third directive
 *       class wait_user (+3 events: TA + GT + RUN_WAITING_FOR_USER with
 *       createMessageId) is production zero-flow from the child path
 *       but parse-legal and contract-live (gate.askUser=true, or
 *       kind hard + user-reject-stop code). This axis prices the
 *       isolated applyTrackingGate directive triple {none,
 *       queue_analysis, wait_user x2 variants} side by side, closing
 *       the directive dimension of the gate-outcome lattice.
 *
 * Archival-fact fuzz:
 *   F1: redelivery determinism + purity + ordering.
 *       Seeded fuzz over apply shapes: fresh apply, then REDELIVER the
 *       identical command (assessment, hash, expectedSeq) against the
 *       updated table:
 *       - replay never appends and returns the input table BY REFERENCE;
 *       - replay directive/transitionId/runStatus are bitwise-consistent
 *         with the original apply (find#1) or with an independently
 *         folded currentGateStatus (find#2);
 *       - CAS precedes idempotency: a corrupted hash on redelivery
 *         ALWAYS throws (never silently replays, never appends);
 *       - seq-monotonicity lemma witness: for every fresh command the
 *         derived expectedSeq strictly exceeds every seq carried by any
 *         GT/TA event in the table, so neither find can hit in-repo;
 *       - GT reasonCode is never "FAIL_CLOSED" on parse-legal gates
 *         (mapGateDirective fallback unreachable for legal input);
 *       - wait_user variants are parse-legal end to end and their
 *         3-event append shape (incl. msg_<evt-suffix> messageId) is
 *         independently checked.
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf19a01..0xf19a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    outcome?: "SUCCESS" | "FAILURE" | "CANCELLED" | "TIMEOUT" | "PARTIAL";
    noEvidence?: boolean;
    noTerminal?: boolean;
    verificationKind?: "PASSED" | "FAILED" | "UNOBSERVED" | "none";
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary = opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran";
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  const vKind = opts?.verificationKind ?? kind;
  const terminal =
    opts?.noTerminal === true
      ? undefined
      : vKind === "none"
        ? {}
        : { verification: { kind: vKind, evidenceIds: evidence } };
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: opts?.outcome ?? (kind === "PASSED" ? "SUCCESS" : "FAILURE"),
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      ...(terminal !== undefined ? { terminalResult: terminal } : {})
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);
const events41NoAttach = buildTable(40, false);

function foldGateStatus(events: readonly Event[]): string {
  let status = "RUNNING";
  for (const event of events) {
    if (event.type === "GATE_TRANSITION")
      status = (event as { payload: { to: string } }).payload.to;
    else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
    else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
    else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
  }
  return status;
}

function maxCarriedSeq(events: readonly Event[]): number {
  let max = -1;
  for (const event of events) {
    if (event.type === "TRACKING_ASSESSMENT" || event.type === "GATE_TRANSITION") {
      max = Math.max(max, (event as { payload: { seq: number } }).payload.seq);
    }
  }
  return max;
}

function gateCommandFor(
  kind: "PASSED" | "FAILED",
  table: readonly Event[],
  opts?: { taskId?: string; claimSuccessWithCheckGap?: boolean }
): { assessment: TrackingAssessment; hash: string; seq: number } {
  const mk = mkChild(kind, opts);
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({
    observation: obs,
    episodeId: "ep_bench",
    runId: String(RUN_ID)
  });
  if (!decision.apply) throw new Error("fixture must apply");
  return {
    assessment: decision.assessment,
    hash: hashAssessment(decision.assessment),
    seq: nextTrackingSeq(table)
  };
}

/* ============================================================
 * F1: redelivery determinism + purity + ordering + wait_user
 * legality (seeded, bitwise stable).
 * ============================================================ */
{
  const rng = mulberry32(0xf19a01);
  let trials = 0;
  let replaysViaGT = 0;
  let replaysViaTA = 0;
  let seqLemmaWitnesses = 0;
  let casOnRedelivery = 0;
  for (let trial = 0; trial < 900; trial += 1) {
    const kind = pick(rng, ["PASSED", "FAILED"] as const);
    const gap = rng() < 0.25;
    const cmd = gateCommandFor(kind, events41, {
      taskId: `tsk_f_${trial}`,
      claimSuccessWithCheckGap: gap
    });
    let c = 900000;
    const genId = (): EventId => `evt_${String(c++).padStart(8, "0")}` as EventId;
    const fresh = applyTrackingGate({
      events: events41,
      assessment: cmd.assessment,
      assessmentHash: cmd.hash,
      expectedSeq: cmd.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: genId
    });
    trials += 1;
    check(`F1 fresh apply trial ${trial}`, fresh.result.applied === true);
    const appended = fresh.events.length - events41.length;
    const expectAppend = fresh.result.directive === "none" ? 1 : 3;
    check(
      `F1 fresh append shape trial ${trial}`,
      appended === expectAppend,
      `directive=${fresh.result.directive} appended=${appended}`
    );
    // GT reasonCode never FAIL_CLOSED on parse-legal gates
    for (const event of fresh.events.slice(events41.length)) {
      if (event.type === "GATE_TRANSITION") {
        check(
          `F1 reasonCode never FAIL_CLOSED trial ${trial}`,
          (event as { payload: { reasonCode: string } }).payload.reasonCode !== "FAIL_CLOSED"
        );
      }
    }
    // REDELIVERY: identical command against the UPDATED table.
    const replay = applyTrackingGate({
      events: fresh.events,
      assessment: cmd.assessment,
      assessmentHash: cmd.hash,
      expectedSeq: cmd.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: genId
    });
    check(`F1 replay is a no-op trial ${trial}`, replay.result.applied === false);
    check(
      `F1 replay PURE (table by reference, zero append) trial ${trial}`,
      replay.events === fresh.events
    );
    check(
      `F1 replay directive consistent trial ${trial}`,
      replay.result.directive === fresh.result.directive
    );
    if (fresh.result.directive !== "none") {
      // GT exists -> find#1 endpoint
      replaysViaGT += 1;
      check(
        `F1 replay transitionId preserved trial ${trial}`,
        replay.result.transitionId === fresh.result.transitionId
      );
      check(
        `F1 replay runStatus = original 'to' trial ${trial}`,
        replay.result.runStatus === fresh.result.runStatus
      );
    } else {
      // only TA appended -> find#2 endpoint
      replaysViaTA += 1;
      check(`F1 replay via TA has no transitionId trial ${trial}`, replay.result.transitionId === undefined);
      check(
        `F1 replay runStatus = independent fold trial ${trial}`,
        replay.result.runStatus === foldGateStatus(fresh.events)
      );
    }
    // seq-monotonicity lemma: a FRESH command derived from the updated
    // table strictly exceeds every carried seq -> neither find can hit.
    const nextSeq = nextTrackingSeq(fresh.events);
    if (nextSeq > maxCarriedSeq(fresh.events)) {
      seqLemmaWitnesses += 1;
    } else {
      check(`F1 seq lemma trial ${trial}`, false, `${nextSeq} <= ${maxCarriedSeq(fresh.events)}`);
    }
    // CAS precedes idempotency on a sample of redeliveries.
    if (trial % 18 === 0) {
      const badHash = `${cmd.hash.slice(0, -1)}${cmd.hash.endsWith("0") ? "1" : "0"}`;
      let threw = false;
      try {
        applyTrackingGate({
          events: fresh.events,
          assessment: cmd.assessment,
          assessmentHash: badHash,
          expectedSeq: cmd.seq,
          policyVersion: "track-v1",
          nowIso: NOW_ISO,
          generateEventId: genId
        });
      } catch {
        threw = true;
      }
      check(`F1 CAS precedes idempotency trial ${trial}`, threw);
      if (threw) casOnRedelivery += 1;
    }
  }
  check("F1 both replay endpoints witnessed", replaysViaGT > 0 && replaysViaTA > 0);
  console.log(
    `F1: ${trials} fuzz trials; ${replaysViaTA} find#2 (TA) + ${replaysViaGT} find#1 (GT) replays, all pure ` +
      `(zero append, table by reference, data consistent); ${seqLemmaWitnesses} seq-monotonicity witnesses ` +
      `(fresh path can never hit either find in-repo); ${casOnRedelivery}/50 corrupted-hash redeliveries all threw ` +
      `(CAS precedes idempotency); reasonCode never FAIL_CLOSED; zero violations`
  );

  // wait_user variants: parse-legal end to end, 3-event append shape.
  {
    const base = gateCommandFor("FAILED", events41, { taskId: "tsk_wu" });
    const variants: Array<{ label: string; assessment: TrackingAssessment; reason: string }> = [
      {
        label: "askUser=true",
        assessment: {
          ...base.assessment,
          gate: { ...base.assessment.gate, askUser: true }
        },
        reason: base.assessment.gate.codes[0] ?? "ASK_USER"
      },
      {
        label: "hard + user-reject-stop",
        assessment: {
          ...base.assessment,
          gate: {
            kind: "hard",
            codes: ["user-reject-stop"],
            wakeAnalysis: true,
            expandDetail: true,
            askUser: false,
            openMinors: []
          }
        },
        reason: "user-reject-stop"
      }
    ];
    for (const variant of variants) {
      const pojo = JSON.parse(JSON.stringify(variant.assessment)) as unknown;
      let parsed: TrackingAssessment | undefined;
      try {
        parsed = parseTrackingAssessment(pojo);
      } catch {
        parsed = undefined;
      }
      check(`F1 wait_user [${variant.label}] parse-legal`, parsed !== undefined);
      const hash = hashAssessment(variant.assessment);
      const seq = nextTrackingSeq(events41);
      let c = 950000;
      const genId = (): EventId => `evt_${String(c++).padStart(8, "0")}` as EventId;
      const out = applyTrackingGate({
        events: events41,
        assessment: variant.assessment,
        assessmentHash: hash,
        expectedSeq: seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: genId
      });
      check(`F1 wait_user [${variant.label}] applies`, out.result.applied === true);
      check(
        `F1 wait_user [${variant.label}] directive`,
        out.result.directive === "wait_user" && out.result.runStatus === "WAITING_FOR_USER"
      );
      const appended = out.events.slice(events41.length);
      check(
        `F1 wait_user [${variant.label}] +3 shape`,
        appended.length === 3 &&
          appended[0]!.type === "TRACKING_ASSESSMENT" &&
          appended[1]!.type === "GATE_TRANSITION" &&
          appended[2]!.type === "RUN_WAITING_FOR_USER"
      );
      const gt = appended[1] as unknown as {
        payload: { reasonCode: string; to: string };
        id: string;
      };
      check(
        `F1 wait_user [${variant.label}] reasonCode`,
        gt.payload.reasonCode === variant.reason,
        gt.payload.reasonCode
      );
      const waiting = appended[2] as unknown as { id: string; payload: { messageId: string } };
      check(
        `F1 wait_user [${variant.label}] messageId = msg_<evt-suffix>`,
        waiting.payload.messageId === `msg_${waiting.id.slice("evt_".length)}`
      );
      // redelivery of the wait_user command replays via find#1
      const replay = applyTrackingGate({
        events: out.events,
        assessment: variant.assessment,
        assessmentHash: hash,
        expectedSeq: seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: genId
      });
      check(
        `F1 wait_user [${variant.label}] replay pure + preserved`,
        replay.result.applied === false &&
          replay.events === out.events &&
          replay.result.transitionId === out.result.transitionId &&
          replay.result.runStatus === "WAITING_FOR_USER"
      );
    }
  }
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R18-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    anchorUs.push(one * 1e3);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition of the PASSED x contract(3) cell
 * (R12-A..R18-A methodology, 5-scan O(E) inventory).
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
let hashNs = Number.NaN;
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  hashNs = tHash * 1e6;
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => void foldGateStatus(events41), 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)})`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);
}

/* ============================================================
 * R18-A skip-path re-confirmation (compact; composition ceiling).
 * ============================================================ */
{
  console.log("--- R18-A skip-path re-confirmation (E=41 production table) ---");
  const applyRef = anchorUs[1] ?? Number.NaN;
  const skipCells: Array<{ label: string; run: () => boolean }> = [
    {
      label: "SKIP terminal undefined",
      run: (() => {
        const mk = mkChild("PASSED", { noTerminal: true });
        return () => {
          let c = 100;
          return applyChildThreeLine({
            events: events41,
            child: mk.child,
            spec: mk.spec,
            contract: mk.contract,
            nowIso: NOW_ISO,
            generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
          }).result.applied;
        };
      })()
    },
    {
      label: "SKIP no episode binding",
      run: (() => {
        const mk = mkChild("PASSED");
        return () => {
          let c = 100;
          return applyChildThreeLine({
            events: events41NoAttach,
            child: mk.child,
            spec: mk.spec,
            contract: mk.contract,
            nowIso: NOW_ISO,
            generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
          }).result.applied;
        };
      })()
    },
    {
      label: "SKIP verification UNOBSERVED (guard 1)",
      run: (() => {
        const mk = mkChild("PASSED", { verificationKind: "UNOBSERVED" });
        return () => {
          let c = 100;
          return applyChildThreeLine({
            events: events41,
            child: mk.child,
            spec: mk.spec,
            contract: mk.contract,
            nowIso: NOW_ISO,
            generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
          }).result.applied;
        };
      })()
    },
    {
      label: "SKIP FAILED w/o evidence refs (guard 3)",
      run: (() => {
        const mk = mkChild("FAILED", { noEvidence: true });
        return () => {
          let c = 100;
          return applyChildThreeLine({
            events: events41,
            child: mk.child,
            spec: mk.spec,
            contract: mk.contract,
            nowIso: NOW_ISO,
            generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
          }).result.applied;
        };
      })()
    }
  ];
  for (const cell of skipCells) {
    let applied = true;
    const one = bench(() => {
      applied = cell.run();
    }, 20000);
    check(`skip cell is a skip [${cell.label}]`, applied === false);
    console.log(
      `  ${cell.label}: ${(one * 1e6).toFixed(0)}ns/call = ${(((one * 1e3) / applyRef) * 100).toFixed(1)}% of apply-path cell`
    );
  }
}

/* ============================================================
 * NEW axis 8a: idempotent-replay endpoint pricing
 * (at-least-once redelivery face; once-per-redelivery).
 * ============================================================ */
{
  console.log("--- NEW axis 8a: idempotent-replay endpoint pricing (once-per-redelivery) ---");
  const applyRef = anchorUs[1] ?? Number.NaN;

  // find#1 endpoint: FAILED apply appends GT -> redelivery hits the
  // idempotencyKey scan and returns the original transition data.
  const cmdGT = gateCommandFor("FAILED", events41, { taskId: "tsk_replay_gt" });
  let c1 = 700000;
  const freshGT = applyTrackingGate({
    events: events41,
    assessment: cmdGT.assessment,
    assessmentHash: cmdGT.hash,
    expectedSeq: cmdGT.seq,
    policyVersion: "track-v1",
    nowIso: NOW_ISO,
    generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
  });
  check("8a GT fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
  const tReplayGT = bench(() => {
    void applyTrackingGate({
      events: freshGT.events,
      assessment: cmdGT.assessment,
      assessmentHash: cmdGT.hash,
      expectedSeq: cmdGT.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => {
        throw new Error("replay must not generate ids");
      }
    });
  }, 20000);

  // find#2 endpoint: PASSED apply appends only TA -> redelivery misses
  // find#1, hits find#2, recomputes directive + currentGateStatus.
  const cmdTA = gateCommandFor("PASSED", events41, { taskId: "tsk_replay_ta" });
  let c2 = 800000;
  const freshTA = applyTrackingGate({
    events: events41,
    assessment: cmdTA.assessment,
    assessmentHash: cmdTA.hash,
    expectedSeq: cmdTA.seq,
    policyVersion: "track-v1",
    nowIso: NOW_ISO,
    generateEventId: () => `evt_${String(c2++).padStart(8, "0")}` as EventId
  });
  check("8a TA fixture applied w/ directive none", freshTA.result.applied && freshTA.result.directive === "none");
  const tReplayTA = bench(() => {
    void applyTrackingGate({
      events: freshTA.events,
      assessment: cmdTA.assessment,
      assessmentHash: cmdTA.hash,
      expectedSeq: cmdTA.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => {
        throw new Error("replay must not generate ids");
      }
    });
  }, 20000);

  const gtNs = tReplayGT * 1e6;
  const taNs = tReplayTA * 1e6;
  console.log(
    `  replay via find#1 (GT idempotencyKey hit, E=${freshGT.events.length}): ${gtNs.toFixed(0)}ns = ${((gtNs / 1000 / applyRef) * 100).toFixed(1)}% of apply cell (hash share ~${((hashNs / gtNs) * 100).toFixed(0)}%)`
  );
  console.log(
    `  replay via find#2 (TA seq+hash hit, E=${freshTA.events.length}): ${taNs.toFixed(0)}ns = ${((taNs / 1000 / applyRef) * 100).toFixed(1)}% of apply cell (hash share ~${((hashNs / taNs) * 100).toFixed(0)}%)`
  );
  console.log(
    "  => both replay endpoints are once-per-redelivery, dominated by the CAS hash recheck; in-repo unreachable (seq lemma); no candidate"
  );
}

/* ============================================================
 * NEW axis 8b: directive-class triple at the applyTrackingGate level
 * (none / queue_analysis / wait_user x2 variants).
 * ============================================================ */
{
  console.log("--- NEW axis 8b: directive-class pricing (applyTrackingGate isolated, E=41) ---");
  const cmdNone = gateCommandFor("PASSED", events41, { taskId: "tsk_dir_none" });
  const cmdQueue = gateCommandFor("FAILED", events41, { taskId: "tsk_dir_queue" });
  const baseWU = gateCommandFor("FAILED", events41, { taskId: "tsk_dir_wu" });
  const askUserAssessment: TrackingAssessment = {
    ...baseWU.assessment,
    gate: { ...baseWU.assessment.gate, askUser: true }
  };
  const rejectStopAssessment: TrackingAssessment = {
    ...baseWU.assessment,
    gate: {
      kind: "hard",
      codes: ["user-reject-stop"],
      wakeAnalysis: true,
      expandDetail: true,
      askUser: false,
      openMinors: []
    }
  };
  const cells: Array<{
    label: string;
    assessment: TrackingAssessment;
    expectDirective: string;
    expectAppend: number;
  }> = [
    { label: "directive none (+1 TA)", assessment: cmdNone.assessment, expectDirective: "none", expectAppend: 1 },
    {
      label: "directive queue_analysis (+3: TA+GT+RUN_BLOCKED)",
      assessment: cmdQueue.assessment,
      expectDirective: "queue_analysis",
      expectAppend: 3
    },
    {
      label: "directive wait_user via askUser (+3: TA+GT+RUN_WAITING_FOR_USER)",
      assessment: askUserAssessment,
      expectDirective: "wait_user",
      expectAppend: 3
    },
    {
      label: "directive wait_user via user-reject-stop (+3)",
      assessment: rejectStopAssessment,
      expectDirective: "wait_user",
      expectAppend: 3
    }
  ];
  for (const cell of cells) {
    const hash = hashAssessment(cell.assessment);
    const seq = nextTrackingSeq(events41);
    let directive = "";
    let appended = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyTrackingGate({
        events: events41,
        assessment: cell.assessment,
        assessmentHash: hash,
        expectedSeq: seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      directive = out.result.directive;
      appended = out.events.length - events41.length;
    }, 20000);
    check(
      `8b cell [${cell.label}]`,
      directive === cell.expectDirective && appended === cell.expectAppend,
      `directive=${directive} appended=${appended}`
    );
    console.log(`  ${cell.label}: ${(one * 1e3).toFixed(2)}us/gate-apply`);
  }
  console.log(
    "  => the directive dimension of the gate-outcome lattice is closed: wait_user sits in the queue_analysis band (+3 events); production zero-flow from the child path; no candidate"
  );
}

void mulberry32(0xf19a02);

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
