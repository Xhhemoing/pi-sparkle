MODEL_SLUG=claude-fable-5-thinking-xhigh

# R19-F：`src/experiments/` 第十九遍复查报告（S7-F-1 + S7-F-2 之上，全新第十九组枚举）

- 基线：`cursor/sota-persistent-opt-83a1` @ `0f7495d`（含 R19-D 空枚举收口与 R19-F 派发；切片最后一次改动仍是 S7-F 落地提交 `519101f`，本轮实测 `git diff 519101f..HEAD -- src/experiments/` 为空——**零漂移**，第十九轮连续第十三轮字节不变；`git log -- src/experiments/` tip 即 `519101f`）
- 切片：`src/experiments/` 全部 15 文件（attribution-report / canary / comparison-report / dataset / evaluation-card / gated-comparison / holdout / isolation / manifest / plan / replay / shadow-compare / shadow / simulation-holdout / threshold-calibration——`ls | wc -l` = **15**，逐一全量重读，未依赖历轮记忆），工作在 S1-F / S5-F / S6-F-1 / S7-F-1 / S7-F-2 之上
- 前置阅读：README、EXCLUSIONS 全表（546 行全读，含 S13-B-1 与 S1-F/S5-F/S6-F/S7-F/S8-F/S9-F 全系）、PROGRESS、round-19/PLAN（含 R19-A/B/D 空枚举收口摘要与 F 切片专令；C/E 仍在途，其分支与报告文件未触碰）、R18-F、R17-F、R16-F / R15-F / R14-F 收口略读、R19-D 结论略读（其冷进程模块图轴未移植——该轴的 F 属地部分即 `cli/adapt.ts` 静态边携带的 experiments=5 模块已在 R19-D 逐边归因中定价，无未具名 F 站点，见 §0）、四个邻居文件（`adaptation/reflection.ts` / `adaptation/eval-routing.ts` / `adaptation/promotion-rules.ts` / `routing/r1-shadow-report.ts`，只读未改）
- 分支：`cursor/r19-f-experiments-nineteenth-pass-83a1`（已推送，未开 PR）
- 环境：Node 22.22.2（VM 默认 22.14.0 低于 engines ≥22.19.0，按历轮同处理以 nvm PATH 前置切换）、pnpm 10.17.1、`pnpm install --frozen-lockfile`

## 结论

**诚实的空枚举：第十九组没有可立 ID 的新角度，不铸任何 S19-F-\* ID，生产代码零改动。** 本轮按令做全新第十九组枚举：从零重读 15 个源文件与四个邻居、在本 VM 重建全部锚点与四次序符号（r5f/r7f 各两次独立运行）、按「站点 ×（跳过 / 替换证明 / 换数据结构 / 换布局 / 复用 / 增量化 / 早退 / 重排 / 并行化 / 批量 / 惰性 / 规范化）」矩阵穷举候选方向后逐一对照排除表——**每一个方向都被既有条目（或其重命名/换址形态）严格覆盖，或其量级天花板经本 VM 实测深低于数十 ms 落地线**。R18-F 宣告的十八层封条（账目直测、最大行分解、外推曲线、GC 类顶、来源格、JIT 档、MDE 地板、长度格、引擎格、剖析归属、A 越线点格、编码格、拒绝路径格、冷进程格、操作粒度格、剂量响应格、fresh-plan 格、halted 格）经本轮复核成立。

本轮的实质产出是**一条历轮从未直测的新轴——runner 家族完备性（第二 runner 维度）**（§1.3），与 Round 19 同轮兄弟对齐（R19-A 新增重投递面 + wait_user 指令类、R19-B 新增比较器第三腿普查 + 目录基数 M 轴、R19-D 新增冷进程模块图组成）：十九轮把 shadow 链定价到操作粒度、剂量曲线与拒绝端点，但切片的**第二个 runner——canary runner，S6-F-1/S7-F-1 的对称落地宿主——从未收到过任何一个直接价格**：历轮全部 canary 裁决都是「test-only 加倍否决 + 从 shadow 数字对称转移」，入库仿真（r6f part B/C、r7f part B/C）只查 canary **等价性**、从不计时。本轮以四个格子把该维度落到直接测量并双端关死：

- **格 a：canary 全实验锚点（任何 F 轮的第一个 canary 直接价格）**。P=2000/A=1000，uncapped（maxExposure=10⁶，全 candidate）与 capped（maxExposure=500）两配置，与同进程 shadow 锚点对照（两次独立运行）：**canary-uncapped 113.61/116.25 ms vs shadow 115.64/118.07 ms（delta −2.04/−1.83 ms）——canary 反而更便宜**，机制即格 b 的 rng 差（canary assign 无 shadowDecisionAt 的 O(k) rng 重放，全实验平均省 ~3 ms，被 scope/exposure 常数部分回填）；capped−uncapped −3.40/−2.58 ms 为测量次序漂移混淆 + 噪声包络边缘（曝露封顶不改变任何 O(k)/O(P) 项）。**第二 runner 不藏任何昂贵路径，且零生产流量（`createCanaryRunner` 全仓零 src/ 消费者，本轮逐符号 rg 复核）——无池、无候选。**
- **格 b：canary−shadow 每操作增量分解（ACTIVE k=1000 态，budget 放宽到 2A 不触发 halt）**。restore：shadow 52.9/54.6 vs canary 52.3/53.8 µs（增量 −0.52/−0.82 µs——canary 的 per-entry action/exposureCount 整数检查 + derivedExposure 相等证明与 shadow 的 liveAction/shadowDecision 检查**同价**，噪声内为零）；assign：shadow 65.1/66.8 vs canary 58.5/60.5 µs（**增量 −6.54/−6.25 µs = shadowDecisionAt 在 k=1000 处的 O(k) rng 重放**——与 R16-F rng 池系数 3.02–3.04 ns/A² 的点预测 2·c·k ≈ 6.0–6.1 µs 交叉一致，rng 池账目首次从「另一侧」（无 rng 的孪生 runner）获得独立证认）；recordOutcome：61.0/62.5 vs 60.8/62.4 µs（−0.26/−0.12 µs——共享泛型助手，delta 只剩两个 restore 体之差＝零）。
- **格 c：S7-F-1 四次序符号在其第二宿主（canary restore）上的首次直测**。历轮每一张四次序表（R7-F…R18-F）都只计时 shadow restore。本轮以内联 S6-F-1 时代 canary 参考（朴素循环 + pending-Set，调用**生产** validateExperimentPlan，delta 严格隔离 S7-F-1 对齐机制本身；每次序先断言双侧 restore 输出逐字节相等）实测 2000 restores × 四次序 × 两次独立运行：**prefix +70.65/+77.89、half +18.78/+19.84 ms 两轮稳定为正；scattered +0.10/+0.28、reversed −0.60/−0.33 ms——全失配端为有界常数零均值抖动**（量级深居 R15-F MDE 包络 1.04–2.64 ms 之内，与 R18-F 轴 1 在 shadow 宿主上 f ≤ 1/16 格的 ±1.8/±3.3 ms 同构），而非 S6-F-5 型 O(P) 负税（那是 −18~−20 ms 量级）。落地复合单元的余量由 S7-F-2 validate 卫提供（本 VM r7f 实测 +7.39/+9.22 ms/2000 调用，双宿主共享同一 validate）——**S7-F-1 在第二宿主上结构与 shadow 宿主完全同构，对称转移假设首次有直接数字。**
- **格 d：跨 runner 共享助手形状污染格**。共享泛型助手（`recordExperimentOutcome` / `applyExperimentClock` / `haltOnAssignmentBudget` / `requireUniqueAssignment` 等）在生产中只见 ShadowState（8 字段）单形，但任何同时跑双 runner 的进程（测试套件即是）会喂进 CanaryState（9 字段）第二形状——历轮从未验证 shadow 锚点数字是否被这种 IC 多态静默degradé。本轮双臂全新子进程（canary 臂：12 shadow → 12 canary → 12 shadow；control 臂：12 → 12 → 12 全 shadow）实测：canary 臂 steady-delta −2.93/−3.39 ms vs control 臂 −0.83/−1.06 ms——**净效应 −2.10/−2.33 ms、方向为「更快」、量级贴 R15-F MDE 包络顶（2.64 ms）＝与零不可区分**。两形状多态 IC 在锚点尺度无可测代价：生产单形假设安全，且测试进程的双 runner 交错不会污染跨轮测量可比性。**无污染悬崖、无候选**（给共享助手做 per-runner 单形复制 = 生产收益恒零 + X1-2 平行实现族）。

至此全实验锚点（本 VM 120.4–130.2 ms 三视图带，与 R18-F 120.8–129.1 / R17-F 120.5–129.6 重叠；r7f 第 1 次 130.19 高出 R18-F 带顶 1.1 ms，在 R15-F MDE 包络一个 max-abs 内且低于 R16-F 带顶 133.5）在 R18-F 十八层封条之上再加一枚：**runner 家族格（第二 runner 端到端定价 + 每操作增量分解 + S7-F-1 第二宿主符号 + 形状污染四格闭合）**。锚点与四次序符号在本 VM 两次独立运行全部复现且同号为正（§1.1），S7-F-1+2 落地单元无回归，r1f 2668 项逐位全过，实验单元套件 75 项全绿，`pnpm gate` 全绿（1168 pass / 0 fail / 1 skipped——既有 provider-smoke 凭据跳过）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；无阈值/测试/公开签名/数据面变更。仓库变更仅本报告一个文件；无赢家故未提交任何 scripts 资产（新轴测量脚本全文进附录 A，遵守 R7-F 以来纪律）。

## 0. 范围与约束遵守 + 关闭 ID 重命名核查

- 阅读顺序按令执行：README → EXCLUSIONS 全表 → PROGRESS → round-19/PLAN → R18-F → R17-F → R16-F / R15-F / R14-F 收口略读 → 15 个切片源文件全量重读 → 四个邻居文件只读。清单核对：15 文件与令中列名逐一对应，无缺无余。Round 19 的 C/E 仍在途：未触碰其分支、其切片文件（`src/routing/` 九文件、`src/learning/`）与其报告文件。
- 未重开任何 X0-\*..X4-\* / S1-\* ～ S18-\* 条目。R10-F…R18-F 未铸 S10..S18-F-\*，本轮未替其补铸；本轮亦未铸 S19-F-\*。按令未重铸 R18-F 的对齐分数剂量响应（f ≥ 2/16 单调正、f\* ≈ 6–12%——格 c 只引用其 f=0 端行为作同构判据，不重测不重立其曲线）、fresh-plan 暖进程税（validate 49.6–51.5 µs / 实验 1.76 ms）与 post-halt 定价（~51 µs 地板、零流量）；未重铸 R17-F 的拒绝路径普查（142 抛出点；最贵 0.31–0.47 ms）、冷进程预算（+16.8–22.0 ms once-per-process）与操作粒度尾部（p50 ~56–58 µs）；未重铸 R16-F 的剖析归属 / A 越线点 / 编码格；未重铸 R15-F 的 A/A MDE（其数字被格 c/格 d 直接引用为噪声判据）/ L 长度格 / 引擎代格；未重铸 R14-F 的 GC 类顶、来源格、deopt 审计。**R19-D 冷进程模块图轴未移植**：其逐边归因已把 F 属地的 5 个 experiments 模块（`cli/adapt.ts` 静态边经 eval-routing 携带）定价在 16 模块 3.1–3.3 ms/非 eval 命令的边际集内，无任何未具名 F 切片站点，故按令不移植不补测。R19-A/B/D 空枚举属其他切片，本轮全部工作限于 `src/experiments/`。
- **S7-F-1 ≠ S6-F-5 重命名核查（按令重申，本轮逐字复核落地代码）**：`shadow.ts` L373–386 / `canary.ts` L147–163 的对齐循环是**同下标单次相等比较**（`assignment.episodeHash !== population[index]` 即 break），失配税为有界常数，回退是后缀级 pending-Set（S6-F-1 形态），下标 0 失配改道原落地循环（shadow L388–406 / canary L166–185 逐字保留）；S6-F-5 是 population 上的**双指针前向搜索**，失配白付 O(P) 扫描、符号随次序翻转。四种次序在本 VM 两次独立运行全部同号为正（§1.1）。本轮未提出任何双指针/子序列形态，也未提出任何 O(P) two-pointer 快路径。格 c 的第二宿主直测进一步确认：canary 侧 S7-F-1 机制的失配端也是**有界常数抖动**（scattered/reversed ±0.6 ms 内符号随运行翻转），与 S6-F-5 的 O(P) 白付负税结构可分——两宿主行为同构。
- **本轮新轴（四格）的候选性自查**：(a) 格 a 是零生产流量面的**首次定价普查**（canary 八入口之一 `createCanaryRunner` 零 src/ 消费者本轮逐符号复核），且实测第二 runner 比生产 runner 更便宜——「让 canary 更快」既无池也无动机，非候选；(b) 格 b 是账目的**跨 runner 交叉验证**（rng 池系数从无 rng 的孪生侧独立证认；restore/record 增量为零），产出为否定性结论，不提出任何变更；(c) 格 c 是落地单元在其第二宿主上的**符号验证**（每轮 shadow 侧四次序表的对称补全），S7-F-1 机制无候选叠层（S6-F-5 禁令 / S7-F-3..4 维持）；(d) 格 d 是**测量方法学完备性**（形状污染不存在＝历轮单进程混测数字可信），唯一可想象的「变更」（共享助手 per-runner 复制单形化）生产收益恒零（生产本就单形）且属 X1-2 平行实现族——四格按 R10-F 以来纪律均不立 ID。
- **其余易混形态自查**：未重提 S8-F-1 / S9-F-1（assign 站点三机制收口完成；生产 A « 1000 未触发任何重开）；未重提 S9-F-2（末位命中见证；缺席证明仍属 S1-F-8）；未重提 S9-F-3 / S6-F-2..4 / S5-F-1..3 / S7-F-3..4 / S8-F-1..3 及 S1..S4 的 F 系全部条目；`outcomes.some` 账目引用一律用 R11-F 勘误值 1.63–1.73 ms（机制锁 S1-F-8）。格 c 与 R18-F 轴 1 的区分：轴 1 是 shadow 宿主上对齐分数**连续体九格**（曲线内部），格 c 是 canary 宿主上**四个正典次序**的首次直测（宿主维度，非剂量维度）——物理对象不同（第二宿主的循环体：canary 对齐比较领先 per-entry 体、shadow 结构检查领先），且格 c 不重测 shadow 侧任何格。格 b 与 S2-F-4 的区分：S2-F-4 是 assign 站点 population Set 复用**提案**（已淘汰），格 b 是 canary/shadow 对照**定价**，不提出复用。格 d 与 R14-F deopt 审计的区分：deopt 审计是 shadow 单形稳态的 JIT 健康档，格 d 是双形混跑的 IC 多态效应——先前无任何轮测过第二形状在场的情形。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：validateExperimentPlan 每调用照跑（Ω(P) 内容重读）；每条 assignment 逐调用全检（Ω(A)）；`[...assignments]`/`[...outcomes]` 防御拷贝原样；TypeError 消息与属性读取序不变（shadow：liveAction → changedLiveAction → shadowDecision → episodeHash；canary：episodeHash 先行）；双 LCB 与双归因未动（X0-11）；无阈值变更；X1-5（shadow-compare 不并 r1-shadow-report）、X3-3/X4-1（不 fail-open 增量化 canary/shadow restore）、X4-2（不对 readonly 公开追加拷贝契约做可变 push）维持；CAS/凭据/数据面契约/公开签名未触；分析不改 in-flight run、tracking 无命令权、H/score 不写路由 PASS/FAIL；live = R0 等价（R1 不接线）、稀疏 → 已批准 R0 基线；提升 proposal-first；不声称 Outcome-supported。
- 漂移复核：`git diff 519101f..HEAD -- src/experiments/` 为空（本轮实测，`--stat` 零行）——R1-F…R18-F 的全部测量与排除裁决可转移，但全部锚点仍按令在本 VM 重测（§1），未拷贝任何历轮数字。

## 1. 锚点重测 + 配置态 × 命令类矩阵 + 新轴（本 VM 实测）

### 1.1 全实验锚点与四次序符号（r5f/r7f 两次独立运行，未拷贝历轮数字；Node 22.22.2）

| 档位 | 本 VM 第 1 次 | 本 VM 第 2 次 | R18-F 记载带 |
| --- | --- | --- | --- |
| r5f C2 全实验（P=2000/A=1000） | **120.72 ms** | 121.50 ms | 120.81–123.21 ms |
| r6f partD 全实验 | **120.37 ms** | — | 122.97 ms |
| r7f partE 全实验（fixture 略异） | 130.19 ms | **128.62 ms** | 128.92–129.09 ms |
| 2A-validate 组件 | 99.30 ms（**82.3%**） | 100.03 ms（82.3%） | 100.28–102.15 ms（82.9–83.0%） |

锚点带 120.4–130.2 ms 与 R18-F 的 120.8–129.1 / R17-F 的 120.5–129.6 重叠（r7f 第 1 次 130.19 高出 R18-F 带顶 1.1 ms，在 R15-F MDE 包络一个 max-abs 内；R16-F 带顶 133.5 覆盖之）。分解算术在本 VM 数字上自洽（validate 组件 99.3–100.0 + 历轮逐项池 ≈ 114–117，与三视图带紧邻）；格 a 的同进程锚点视图（shadow 5 runs median 115.6/118.1 ms、格 d 双臂 pre-steady 117.0–118.6 ms）构成独立第四视图，与 R17-F 轴 2 稳态中位 113.2–118.5 / R18-F second-run 视图 114.6–117.3 同带。

四种 assignment 次序（r7f partE，S7-F-1+2 落地态 vs 编辑前冻结参考，2000 restores，两次独立运行）：

| 次序 | 第 1 次 | 第 2 次 | 符号 |
| --- | --- | --- | --- |
| prefix（锚点，全对齐） | **+45.95 ms** | **+45.29 ms** | 正 |
| half-aligned | +28.25 ms | +26.64 ms | 正 |
| scattered | +4.91 ms | +4.52 ms | 正 |
| reversed | +6.55 ms | +3.20 ms | 正 |

**全次序两轮同号为正，无符号翻转**——S7-F-1 落地单元在本 VM 复现无回归（scattered 带 +4.52~+4.91 与 R18-F 的 +2.87~+4.98 / R17-F 的 +4.16~+5.28 同带）。r6f partD（S6-F-1+S7-F 叠加视图）：prefix +124.75 / scattered +25.06 / reversed +28.91 ms，三次序全正。r7f validate 组件视图（S7-F-2 单元）：×2000 调用省 +9.22 / +7.39 ms 两轮同号。r5f C1（S5-F 单元）：has+add 147.64/151.53 → add+size 117.67/118.22 ms（+29.97/+33.31 ms，20.3%/22.0%）；C4 索引循环形式 −1.88/−4.70 ms 维持 S5-F-3 抖动带记载；C5 dataset 镜像 11.5/18.9 µs（U=2000）、135.7/210.6 µs（U=20000）维持 S5-F-1 记载。r1f 2668 项逐位检查全过（生产侧 63.9 ms，参考侧 639.8 ms，10.0×）。

### 1.2 配置态 × 命令类矩阵（R7-I 教训，逐项复核）

- **生产消费链**（rg 全仓复核，与 R18-F 逐条一致，**无新 src/ 调用方**；`from "../experiments/"` 全仓仍仅四文件）：`reflection.ts` L209–225（validateExperimentPlan + createShadowRunner，唯一 runner 生产链——本轮逐行复核 `evaluateProposalShadow` L216–223 逐字未变：按调用方 outcomes 次序 assign+recordOutcome，**无对齐契约**，每 outcome 紧随其 assign 记录，**halt 即 break**；生产 createShadowRunner 不传 options，bindExperimentIsolation 早退；每 proposal 一个 fresh plan）；`eval-routing.ts`（gatedComparisonReport / createIsolationGuard / stableStringify / replayCacheKey）；`promotion-rules.ts`（validateComparisonReport）；`r1-shadow-report.ts`（gatedComparisonReport + stripImprovementClaims + comparison-report 类型）。`createCanaryRunner`、`replayPolicy`、`runSimulationHoldout`、sealed-manifest 全链路（`validateSealedDatasetManifest`）、`HoldoutVault`、`calibrateSoftThreshold`、`compareShadowR1`、`writeAttributionPair` 仓内仍仅测试可达（本轮逐符号 rg 实测，8 个入口全部零 src/ 切片外消费者——格 a 的零流量前提）。
- **命令类覆盖**：start（常数构造）、assign（站点三机制收口完成 S2-F-4/S8-F-1/S9-F-1；本轮格 b active-assign 65.1–66.8 µs 与 R18-F 轴 3 的 57.5–58.4 同构——差为本轮 per-op 夹具 maxAssignments=2A 的 budget 谓词分支与批间漂移，皆在噪声阶）、recordOutcome（三扫融合上界即三池之和 ~3.7 ms；本轮 record 61.0–62.5 µs 同带）、restore（S7-F-1 落地形态零漂移，四次序符号 §1.1 + 第二宿主符号 §1.3 格 c）、cancel（restore + clock + 一次 spread；halted 幂等路径 R18-F 已直测）。
- **runner 维度（本轮新增，格 a–d）**：历轮矩阵的行全部在 shadow runner 上定价，canary runner 只有等价性证据；本轮四格把第二 runner 的端到端、每操作、落地单元符号与跨 runner 交互全部落到直接数字（§1.3）——runner 家族维度闭合。
- **配置态维度**：canary 块（test-only；**本轮首次直接定价而非仅对称转移**）、missingOutcomePolicy 三值（同成本路径）、halted 态（R18-F 轴 3 已直测）、options.outputRoot 隔离守卫（每 runner 一次且生产不传）、maxExposure（本轮格 a：capped−uncapped 在漂移/噪声内为零——曝露封顶不改变任何 O(k)/O(P) 项）——矩阵无空洞。

### 1.3 新轴：runner 家族完备性四格（各两次独立运行）

**格 a：端到端锚点（P=2000/A=1000，各 5 runs，min/median ms；同进程 shadow 先测）**

| 配置 | 第 1 次 min/median | 第 2 次 min/median | 对照 |
| --- | --- | --- | --- |
| shadow 锚点 | 114.77 / 115.64 | 116.83 / 118.07 | §1.1 三视图带同源 |
| canary-uncapped（全 candidate） | 110.44 / **113.61** | 113.13 / **116.25** | delta vs shadow **−2.04 / −1.83 ms** |
| canary-capped（maxExposure=500） | 110.04 / 110.21 | 113.38 / 113.66 | delta vs uncapped −3.40 / −2.58 ms |

结论：(a) **canary 全实验比 shadow 便宜 ~2 ms**——机制即格 b 的 assign rng 差（全实验平均 ~3 ms，减去 scope/exposure 常数与漂移）；(b) capped−uncapped 为负但测量次序（capped 最后测、进程持续变暖，格 d 双臂显示进程生命期内有 −1~−3 ms 单调漂移）混淆 + 贴 R15-F MDE 包络（max-abs 2.64 ms）——曝露封顶机制上不改变任何随 k/P 增长的项（只改 per-entry 分支走向与 candidate 计数），裁定为漂移+噪声；(c) 决定性断言全过（uncapped 终态 exposureCount=A、全 candidate、budget-exhausted halt 与 shadow 锚点同构；capped 终态 exposureCount=500、前 500 candidate 后 500 baseline 逐条验证）。**第二 runner 端到端无隐藏昂贵路径，零生产流量——无池。**

**格 b：canary−shadow 每操作增量（ACTIVE k=1000 态，200 ops best-of-5，µs/op）**

| 操作 | shadow 第 1/2 次 | canary 第 1/2 次 | 增量 |
| --- | --- | --- | --- |
| restore | 52.9 / 54.6 | 52.3 / 53.8 | **−0.52 / −0.82 µs＝噪声内为零** |
| assign（k=1000） | 65.1 / 66.8 | 58.5 / 60.5 | **−6.54 / −6.25 µs＝O(k) rng 重放** |
| recordOutcome | 61.0 / 62.5 | 60.8 / 62.4 | −0.26 / −0.12 µs＝零 |

结论：(a) canary restore 的 per-entry action/exposureCount 整数检查 + derivedExposure 相等证明与 shadow 的 liveAction/shadowDecision 结构检查**同价**——第二 restore 体无额外池；(b) assign 增量 −6.25~−6.54 µs 与 R16-F rng 池系数（3.02–3.04 ns/A²）的 k=1000 点预测 2·c·k ≈ 6.0–6.1 µs **交叉一致（~5% 内）**——S2-F-1 关闭的 rng 池账目首次从无 rng 的孪生 runner 侧独立证认；(c) recordOutcome 是共享泛型助手，delta 只剩两 restore 体之差＝零，与 (a) 自洽；(d) restore 往返恒等断言（stableStringify）双 runner 全过。

**格 c：S7-F-1 四次序符号（canary 宿主，2000 restores/次序；参考 = S6-F-1 时代 canary 循环 + 生产 validate，delta 隔离对齐机制本身；每次序先断言双侧输出逐字节相等）**

| 次序 | 第 1 次 delta | 第 2 次 delta | 符号 | shadow 宿主对照（R18-F 轴 1 / 本轮 r7f） |
| --- | --- | --- | --- | --- |
| prefix（全对齐） | **+70.65 ms** | **+77.89 ms** | 正 | 轴 1 f=16/16：+75.33/+74.05 |
| half-aligned | +18.78 ms | +19.84 ms | 正 | 轴 1 f=8/16：+19.81/+18.52 |
| scattered | +0.10 ms | +0.28 ms | 贴零 | 轴 1 f=0~1/16：±0.7–3.3 抖动带 |
| reversed | −0.60 ms | −0.33 ms | 贴零抖动 | 同上 |

结论：(a) **canary 宿主与 shadow 宿主逐带同构**——对齐端强正（且与 R18-F 轴 1 同 f 格的数值同带），全失配端为有界常数零均值抖动（±0.6 ms « MDE 包络），非 S6-F-5 型 O(P) 负税（−18~−20 ms 量级）；(b) 落地复合单元的全次序正余量由 S7-F-2 validate 卫提供（本 VM +7.39/+9.22 ms/2000 调用，双宿主共享 `validateExperimentPlan`）——|±0.6| « 7.4，canary 宿主复合单元在全部次序为正；(c) canary 对齐循环领先体首（哈希比较在 action 检查之前）与 shadow（结构检查在前）的次序差异不改变符号结构——对称转移假设自此有直接数字；(d) scattered 用 (3i+1) mod 1000 排列（无不动点、下标 0 失配触发朴素改道）、reversed 无不动点，两者都走了 index-0 改道路径——三条路径（全对齐 / 后缀回退 / 朴素改道）全部被四次序覆盖。

**格 d：跨 runner 共享助手形状污染（双臂全新子进程，各 12+12+12 全实验；steady=中位 runs 3..12）**

| 臂 | pre-steady | post-first2 | post-steady | steady-delta |
| --- | --- | --- | --- | --- |
| canary 臂第 1 次（shadow→canary→shadow） | 117.96 ms | 114.74 | 115.03 | **−2.93 ms** |
| control 臂第 1 次（全 shadow） | 116.92 ms | 116.18 | 116.09 | −0.83 ms |
| canary 臂第 2 次 | 118.45 ms | 115.44 | 115.06 | **−3.39 ms** |
| control 臂第 2 次 | 118.59 ms | 117.26 | 117.53 | −1.06 ms |

结论：(a) 净形状污染效应 = canary 臂 − control 臂 = **−2.10 / −2.33 ms——方向为「更快」、量级贴 MDE 包络顶（2.64 ms）＝与零不可区分**；(b) post-first2 与 post-steady 同带——无「切换瞬态」尖峰（第二形状进场后 shadow 首两跑不付重优化税）；(c) 共享泛型助手的两形状多态 IC 在锚点尺度零可测代价：V8 两态 polymorphic dispatch 深低于 validate 地板；(d) 每臂每相全部终态 stableStringify 相等断言全过（三相同工作量保证）。**方法学推论：历轮在同一进程混跑双 runner 等价性检查与 shadow 计时的数字不需修正；生产（shadow 单形）更无池。**

## 2. 第十九组枚举（诚实为空）：方向 → 覆盖判定全表

候选生成程序：按「站点 ×（跳过 / 替换证明 / 换数据结构 / 换布局 / 复用 / 增量化 / 早退 / 重排 / 并行化 / 批量 / 惰性 / 规范化）」矩阵穷举，再对照排除表做重命名判定。结果：**每格非空即被覆盖**。

| 方向 | 量级判断（本 VM） | 覆盖条目 / 重命名判定 |
| --- | --- | --- |
| **canary 站点任何加速**（第二 restore 体瘦身、exposure 检查融合、scope Set 化） | **格 a/b 首测：端到端已比 shadow 便宜 ~2 ms；restore/record 增量为零；零生产流量** | test-only 加倍否决（历轮）+ 本轮直接定价——零池不可提案域；S1-F-7（reversibleScopes Set 化，scope 个位数）维持 |
| **shadow assign rng 重放消除**（闭式跳转 / 承载态——格 b 从孪生侧再证认其价） | 格 b：k=1000 处 6.25–6.54 µs/op；池 ~3 ms @A=1000（R16-F 同带） | S2-F-1（闭式 O(1) 跳转仅占全实验 ~1%）+ X1-1/X0-4——机制封条原样 |
| **共享助手 per-runner 单形复制**（防形状污染的 monomorphize） | 格 d 首测：两形状多态净效应 −2.1~−2.3 ms 方向为快＝零污染 | 生产收益恒零（生产本就单形）+ X1-2 平行实现族——不可提案域 |
| **S7-F-1 在 canary 宿主的再叠层**（双指针、相位拆分、外提、次序特化） | 格 c 首测：canary 宿主与 shadow 逐带同构，失配端有界常数抖动 | S6-F-5（禁令）/ S6-F-2 / S7-F-3..4 对称转移 + 本轮直接数字维持 |
| **对齐分数内部谷假说** | R18-F 轴 1：f ≥ 2/16 单调正、内部无谷（本轮未重测，格 c 引用其 f=0 端行为） | 否定性封条维持 |
| **fresh-plan 首验税收割 / 冷进程预热干预** | R18-F 轴 2 per-plan ~50 µs/~2 ms + R17-F 轴 2 once-per-process（未重测） | once-per-plan/process 噪声类 + S7-H-3（预热净负）+ X1-1——不立 ID |
| **halted 态操作任何加速** | R18-F 轴 3：全部贴 validate 地板 ~51 µs、零流量（未重测） | 零池 + fail-closed 硬不变量——不可提案域 |
| **拒绝路径任何加速** | R17-F 轴 1：最贵拒绝 0.31–0.47 ms 亚 ms；稳态流量为零（未重测） | 零池 + fail-closed 防御纵深——不可提案域 |
| **操作粒度悬崖假说** | R17-F 轴 3：p50 55.5–57.8 µs 平地板（本轮格 b 52.3–66.8 µs 双 runner 同带交叉复现） | 否定性封条维持 |
| **「组件计时账目有隐藏站点」假说** | R16-F 轴 1 剖析归属：无未具名站点 self ≥1.5%（未重测；格 b 增量分解交叉自洽） | 账目完备性双仪器封条维持 |
| **二次方池在更大 A 下的重开** | R16-F 轴 2：各池 A\*(10ms)≈1.8k–4.9k；生产 A « 1000 未触发；格 b 的 rng 点值即三池账目的又一独立对照 | S2-F-1 / S9-F-2 / S1-F-8 / S8-F-1 / S9-F-1 机制封条原样 |
| **任何贴带（≤~3 ms）候选的重测/重开** | R15-F 轴 1 MDE 地板 max-abs 1.04–2.64 ms（本轮格 a capped 格、格 c 失配格、格 d 污染格三处独立复现其判别力） | 「band-overlap is not a landing」数字化封条维持——S5-F-3 / S6-F-2..3 / S7-F-3..4 等贴带条目不重开 |
| **assign 站点调用方哈希规范化（含任何 L/编码变体）** | R14-F 天花板 ≤7.2 ms + R15-F L 系数 + R16-F 编码格（未重测） | R14-F 已收束；跨调用规范串表 = X1-1 |
| validate 唯一性证明的哈希工作消除/替代（任何结构） | R13-F 88.6–90.4 ms/实验；本轮 validate 组件 99.3–100.0 ms 同带 | S6-F-4 + S9-F-3 双证人；X1-1/S3-F-2；S6-F-3；S5-F-2 排序毁 first-fault——数字封条双仪器维持 |
| validate 内容重读义务侧再收割 / 常数部跳过 / 布局重构 | 地板 ~9.3–9.5 ms（R13-F）；常数部 0.94 ms（R11-F） | S5-F-3 / S6-F-3 / S6-F-4 / S7-F-2 落地形态 + S3-F-2 / X1-1 族——不可提案域 |
| restore 对齐扫描布局重构 / 检查序重排 / 防御拷贝省略 | 扫描池 ~4.2–4.9 ms（R11-F/R16-F 同带）；属性读取序是硬不变量 | 量级 + 硬不变量——不可提案域 |
| recordOutcome 正向见证 / 查重去 O(A) / 三扫融合 / 追加拷贝可变化 / 成本增量化 | 三池之和 ~3.7 ms（R9–R11-F；本轮 record 61.0–62.5 µs/op 同构） | S9-F-2 / S1-F-8 / X4-2 / S1-F-3 + 量级 |
| 分配消除类一切形态 / 时钟/预算/返回对象 spread / 恒等快路径 | GC 类顶 2.35–3.08 ms（R14-F + R16-F 双仪器） | 逐项既有封条 + 类级数字封条维持 |
| JIT/PIC/预热干预 | R14-F 稳态 deopt=0 + R17-F 冷差 once-per-process + 格 d 两形状多态零代价 | S7-H-3 + S8-A-3/S8-E-2 先例——不立 ID |
| 引擎面干预（升级 Node、V8 旗标、钉版） | R15-F 轴 3：Node 24 混合信号无 régime 迁移（未重测） | S5-I-5（切片外）+ S9-C-2（构建面）+ S6-I-2 家族——不立 ID |
| A 线性站点首字符卫镜像 / 单元素快路径 / outcome 校验微调 | 6.8 µs / ~190 µs / ~10 µs（R8-F 记载维持） | S8-F-2 / S8-F-3 + R2-F 池 |
| treat-as-failure 替换 outcome 构造 / halted trim / 隔离守卫提升 | O(字段)/缺失条目；每 restore ≤1 trim（R18-F 直测增量为零）；每 runner 一次且生产不传 options | 配置态矩阵行（§1.2）+ S3-F-4 / S3-D-3 |
| test-only 链路一切残余（replay/dataset/simulation-holdout/holdout/isolation/manifest/threshold-calibration/evaluation-card/shadow-compare/attribution-report/gated-comparison/comparison-report） | 各 µs–亚 ms，R2–R18 已逐项量化 | S1-F-1..8 / S2-F-1..6 / S3-F-1..5 / S4-F-1..3 / S5-F-1 / S6-D-5 / X1-5 / X3-2 / S3-D-3 维持；无新生产调用方（§1.2）故一切先前规模否决转移 |
| 切片内 import 边惰性化 / 模块级预热 / 并行化 / WASM | R19-D 已把 F 属地 5 模块定价在 16 模块 3.1–3.3 ms 边际集内（I 属地静态边，非 F 切片可改） | S5-H-2 / S6-I-1 / S11-G-1（生产收益恒 0）+ S7-H-3（预热净负）+ S9-C-1/2（X0-4 + 单线程逐位审计基底） |

**结论：可立 ID 的新角度数为零。** 与 R18-F 的差别在于：R18-F 把落地单元的剂量维度、plan 新鲜度维度与 halted 配置态落到直测；本轮把 **runner 家族维度**（第二 runner 端到端定价 + 每操作增量分解 + 落地单元第二宿主符号 + 跨 runner 形状污染）四格首次直测。第十九遍确认：在当前排除表与逐位契约下，本切片不存在不经表所有者层级契约变更（S3-F-2 深冻结值类型、S6-F-5 次序契约化、S8-F-1/S9-F-1 A 增长阈值等既有重开条件）即可达落地线的候选。**后续轮次对本切片的复核只需重跑 §1.1 锚点 + §1.2 调用面 grep；账目直测、最大行分解、外推曲线、GC 类顶、来源格、JIT 档、MDE 地板、长度格、引擎格、剖析归属、A 越线点格、编码格、拒绝路径格、冷进程格、操作粒度格、剂量响应格、fresh-plan 格、halted 格、runner 家族格十九层封条已闭合。**

## 3. 逐文件收口（R1–R18 收口之上的本轮检查点）

| 文件 | 本轮检查点 | 结论 |
| --- | --- | --- |
| `shadow.ts` | S7-F-1 落地形态零漂移逐行复核（§0）；**格 a/b/d 的 shadow 侧宿主**（锚点 115.6–118.1 ms、restore 52.9–54.6 / assign 65.1–66.8 / record 61.0–62.5 µs、双形混跑零污染）；共享泛型助手（recordExperimentOutcome / applyExperimentClock / haltOnAssignmentBudget）两形状多态零代价 | 无候选 |
| `canary.ts` | **本轮新轴主宿主——任何 F 轮的第一批直接价格**：端到端 110.2–116.3 ms（比 shadow 便宜 ~2 ms）、restore/record 增量为零、assign 免 rng −6.3~−6.5 µs、S7-F-1 四次序符号同构（格 c）；八入口零 src/ 消费者复核（test-only 维持） | 无候选 |
| `plan.ts` | validate 组件 99.3–100.0 ms 同带复现（82.3%）；S5-F / S7-F-2 落地形态零漂移；r7f validate 卫 +7.39/+9.22 ms/2000 为格 c 复合余量来源 | 无候选 |
| `dataset.ts` | S5-F-1/S4-F-1 维持（sealed 链 test-only；C5 本轮复测 11.5–18.9 µs 同带；零 src/ 消费者） | 无候选 |
| `simulation-holdout.ts` | S2-F-5/S3-F-5/S5-F-1 维持；requireHash 站点属 S8-F-2 同判 | 无候选 |
| `comparison-report.ts` | X3-2/S4-F-3/S1-F-5 维持；真实 N 亚 ms 预算收口维持（R4-F 量化） | 无候选 |
| `gated-comparison.ts` | S1-F-1/S2-F-6 维持；IMPROVEMENT_CLAIM 双字面量维持 R9-F「重构非优化」裁定 | 无候选 |
| `attribution-report.ts` | 无循环无新调用方、零抛出点 | 无候选 |
| `replay.ts` | 仍无生产调用方（replayCacheKey 除外——小输入每 eval 一次）；S2-F-1/2/3、S4-F-2 维持；格 b 从孪生侧再证认 rng 池账目 | 无候选 |
| `isolation.ts` | S3-F-4 维持；唯一生产路径整体维持 S3-D-3 否决；生产 runner 不传 options 故 bindExperimentIsolation 早退 | 无候选 |
| `manifest.ts` | stableStringify 字节契约维持 R2-F 不提案；S6-D-5 维持；本轮全部状态相等断言以其为仪器 | 无候选 |
| `holdout.ts` | S1-F-3/X4-2 维持（追加拷贝池裁决同族互证） | 无候选 |
| `threshold-calibration.ts` | S1-F-4 维持；零抛出点 | 无候选 |
| `evaluation-card.ts` | S1-B-7 域维持 | 无候选 |
| `shadow-compare.ts` | 薄封装维持 X1-5；零抛出点 | 无候选 |

## 4. 测试

- `npx tsx scripts/round01-r1f-equivalence-sim.ts` ✓ ×2 — 2668 项逐位检查 0 失败（生产侧 63.9 ms / 参考侧 639.8 ms，10.0×；信息行结构描述指 S1-F 时代，R6-F 已披露）
- `npx tsx scripts/round05-r5f-equivalence-sim.ts` ✓ ×2 — 各 224 项 0 失败（C2 端到端 120.72 / 121.50 ms 即 §1.1 锚点）
- `npx tsx scripts/round06-r6f-equivalence-sim.ts` ✓ — 27 项 0 失败（partD 三次序全正 + 全实验 120.37 ms）
- `npx tsx scripts/round07-r7f-equivalence-sim.ts` ✓ ×2 — 各 169 项 0 失败（partE 四次序两轮全部为正 + validate +9.22/+7.39 + 全实验 130.19/128.62 ms）
- `npx tsx --test test/unit/experiments/*.test.ts` ✓ — 75 项 0 失败（10 个实验套件文件）
- `npx tsx tmp-r19f-axes.ts` ✓ ×2（干净独立运行）——四格两轮同带 + 全部决定性断言全过（uncapped/capped 终态逐条动作与曝露验证、双 runner restore 往返恒等、格 c 每次序双侧输出逐字节相等、格 d 三相终态相等；附录 A）
- `pnpm gate`（typecheck + lint + test + build）✓ — 1168 pass / 0 fail / 1 skipped（既有 provider-smoke 凭据跳过）。Node ≥22.19.0（engines），本 VM 以 nvm 22.22.2 运行
- 未触碰任何版本化阈值、权限、数据面契约、公开签名；未改任何测试断言；未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md
- 按纪律，新轴测量脚本未入库（全文见附录 A）；`scripts/` 零新增；未弱化任何既有仿真；无赢家故未提交 `scripts/round19-r19f-equivalence-sim.ts`

## 5. 新增排除 ID

**无。** 本轮枚举为空，未铸任何 S19-F-\* ID。本轮 runner 家族轴的四格均为收尾测量而非候选（格 a 是零流量面的首次定价、格 b 是账目的跨 runner 交叉验证、格 c 是落地单元第二宿主的符号验证、格 d 是测量方法学的形状污染排除），按 R10-F..R18-F 同一纪律不立 ID。格 a 数字（canary 端到端 110.2–116.3 ms，比 shadow 便宜 ~2 ms）、格 b 数字（restore/record 增量为零；assign rng 差 6.25–6.54 µs @k=1000）、格 c 数字（canary 宿主四次序与 shadow 逐带同构）与格 d 数字（两形状多态净效应 −2.1~−2.3 ms＝与零不可区分）录档供后续轮次引用；四者均不构成任何既有条目的重开证据。

MORE_OPTIMA=no
BRANCH=cursor/r19-f-experiments-nineteenth-pass-83a1

## 附录 A：新轴测量脚本（完整，可复现；无赢家按纪律不入库）

保存为 `tmp-r19f-axes.ts` 后 `npx tsx tmp-r19f-axes.ts` 运行（格 d 由脚本自 spawn `--arm=canary` / `--arm=control` 两个全新进程实现）。这不是候选裁决仿真（本轮无候选进入测量门），是 §1.3 runner 家族轴的收尾测量。

```ts
/**
 * R19-F new-axis measurements (temporary; NOT candidate adjudications and NOT
 * committed — no winner this round). One axis no earlier F round measured
 * directly: the RUNNER-FAMILY completeness dimension. Nineteen rounds priced
 * the shadow chain exhaustively (anchor, per-op tails, dose-response,
 * fresh-plan tax, post-halt, reject paths, cold process, GC, provenance,
 * length, encoding, profile attribution, A-crossing) but the slice's SECOND
 * runner — the canary runner, the symmetric S7-F-1/S6-F-1 landing host —
 * never received a single direct price: every canary adjudication was
 * "test-only double-veto + symmetric transfer" from shadow numbers, and the
 * committed sims (r6f part B/C, r7f part B/C) check canary EQUIVALENCE only,
 * never time it. Four cells close the dimension:
 *
 *   cell a  canary end-to-end anchor (P=2000/A=1000), uncapped
 *           (all-candidate) and exposure-capped (maxExposure=A/2) configs,
 *           against the shadow anchor in the same process. First direct
 *           price of a full canary experiment in any F round.
 *
 *   cell b  canary−shadow per-op increment decomposition at anchor scale on
 *           ACTIVE states (maxAssignments=2A so nothing halts): restore
 *           (canary adds the per-entry action/exposureCount integer checks +
 *           the derivedExposure equality proof), assign at k=A (canary adds
 *           scope trim + reversibleScopes.includes + exposure arithmetic but
 *           PAYS NO rng replay — shadowDecisionAt is O(k), so the increment
 *           cross-checks the R16-F rng-pool coefficient from the other
 *           side), and recordOutcome (shared helper; delta isolates the two
 *           restore bodies).
 *
 *   cell c  S7-F-1 four-order sign check on its SECOND host: every prior
 *           four-order table (R7-F..R18-F) timed the SHADOW restore only.
 *           This times the landed canary restore against an inline
 *           S6-F-1-era canary reference (plain loop + pending-Set, calling
 *           the PRODUCTION validateExperimentPlan so the delta isolates the
 *           S7-F-1 alignment machinery alone) on prefix / half-aligned /
 *           scattered / reversed orders, with per-order state-equality
 *           assertions.
 *
 *   cell d  cross-runner shared-helper shape-pollution cell. The shared
 *           generic helpers (recordExperimentOutcome, applyExperimentClock,
 *           haltOnAssignmentBudget, requireUniqueAssignment, ...) are called
 *           with ShadowState (8 fields) in production but with BOTH
 *           ShadowState and CanaryState (9 fields) shapes in any process
 *           that runs both runners (the test suite does). Production is
 *           shadow-only, hence monomorphic — but no round ever verified
 *           that the shadow anchor numbers are not silently degraded (or
 *           improved) when canary traffic shares the process. Two fresh
 *           child processes: the CANARY arm runs 12 shadow anchors, then 12
 *           canary anchors, then 12 shadow anchors; the CONTROL arm runs
 *           12 + 12 + 12 shadow anchors. delta(post − pre) compared across
 *           arms isolates the shape-pollution effect from drift.
 *
 * Zero production traffic through every canary path (createCanaryRunner has
 * no src/ consumer — re-verified this round), so these are informational
 * floors and completeness closures, not pools.
 *
 * Uses the production modules directly. Run: npx tsx tmp-r19f-axes.ts
 * (cell d spawns itself with --arm=canary and --arm=control).
 */

import { execFileSync } from "node:child_process";
import { DomainValidationError } from "./src/domain/errors.js";
import { createCandidateId, createResourceVersionId } from "./src/domain/ids.js";
import { validateExperimentPlan, type ExperimentPlan } from "./src/experiments/plan.js";
import {
  createShadowRunner,
  canonicalHaltReason,
  requirePopulationMember,
  type ExperimentOutcome,
  type ShadowState,
} from "./src/experiments/shadow.js";
import {
  createCanaryRunner,
  type CanaryAssignment,
  type CanaryState,
} from "./src/experiments/canary.js";
import { stableStringify } from "./src/experiments/manifest.js";

function timeMs(fn: () => void, rounds = 3): number {
  let best = Infinity;
  for (let r = 0; r < rounds; r++) {
    const t0 = process.hrtime.bigint();
    fn();
    const t1 = process.hrtime.bigint();
    best = Math.min(best, Number(t1 - t0) / 1e6);
  }
  return best;
}

function onceMs(fn: () => void): number {
  const t0 = process.hrtime.bigint();
  fn();
  const t1 = process.hrtime.bigint();
  return Number(t1 - t0) / 1e6;
}

function median(xs: readonly number[]): number {
  const s = [...xs].sort((a, b) => a - b);
  return s[Math.floor(s.length / 2)] as number;
}

/** Historical fixture shape: `ep_` + zero-padded base36 index, L=11. */
function makePopulation(size: number, tag = ""): string[] {
  const population: string[] = [];
  for (let i = 0; i < size; i++) {
    population.push(`ep_${tag}${i.toString(36).padStart(8 - tag.length, "0")}`);
  }
  return population;
}

function makeShadowPlan(
  population: readonly string[],
  maxAssignments: number,
  id = "exp_r19f_shadow"
): ExperimentPlan {
  return {
    planVersion: 1,
    experimentId: id,
    mode: "shadow",
    baselineVersionId: createResourceVersionId(() => "r19fbase"),
    candidateId: createCandidateId(() => "r19fcand"),
    population,
    metrics: ["utility"],
    thresholds: { maxGuardrailBreaches: 1_000_000, maxCostUsd: 1e12 },
    budget: { maxAssignments, maxWallClockMs: 1e12 },
    randomization: { seed: 42 },
    stopPolicy: { onGuardrail: "halt", onBudgetExhausted: "halt" },
    missingOutcomePolicy: "exclude",
    canary: undefined,
  };
}

function makeCanaryPlan(
  population: readonly string[],
  maxAssignments: number,
  maxExposure: number,
  id = "exp_r19f_canary"
): ExperimentPlan {
  return {
    planVersion: 1,
    experimentId: id,
    mode: "canary",
    baselineVersionId: createResourceVersionId(() => "r19fbase"),
    candidateId: createCandidateId(() => "r19fcand"),
    population,
    metrics: ["utility"],
    thresholds: { maxGuardrailBreaches: 1_000_000, maxCostUsd: 1e12 },
    budget: { maxAssignments, maxWallClockMs: 1e12 },
    randomization: { seed: 42 },
    stopPolicy: { onGuardrail: "halt", onBudgetExhausted: "halt" },
    missingOutcomePolicy: "exclude",
    canary: { maxExposure, reversibleScopes: ["scope-a"] },
  };
}

const P = 2000;
const A = 1000;
const SCOPE = "scope-a";

function outcomeFor(h: string): ExperimentOutcome {
  return { episodeHash: h, utility: 0.5, costUsd: 0.01, guardrailBreached: false };
}

function fullShadowExperiment(plan: ExperimentPlan, hashes: readonly string[]): ShadowState {
  const runner = createShadowRunner(plan);
  let state = runner.start(0);
  for (let k = 0; k < A; k++) {
    const h = hashes[k] as string;
    state = runner.assign(state, h, 0);
    state = runner.recordOutcome(state, outcomeFor(h), 0);
  }
  if (state.assignments.length !== A) throw new Error("unexpected shadow assignment count");
  return state;
}

function fullCanaryExperiment(plan: ExperimentPlan, hashes: readonly string[]): CanaryState {
  const runner = createCanaryRunner(plan);
  let state = runner.start(0);
  for (let k = 0; k < A; k++) {
    const h = hashes[k] as string;
    state = runner.assign(state, h, SCOPE, 0);
    state = runner.recordOutcome(state, outcomeFor(h), 0);
  }
  if (state.assignments.length !== A) throw new Error("unexpected canary assignment count");
  return state;
}

/* ------------------------------------------------------------------ */
/* cell d arms (--arm=canary | --arm=control): fresh-process phases    */
/* ------------------------------------------------------------------ */
const armFlag = process.argv.find((a) => a.startsWith("--arm="));
if (armFlag !== undefined) {
  const arm = armFlag.slice("--arm=".length);
  const population = makePopulation(P);
  const shadowPlan = makeShadowPlan(population, A);
  const canaryPlan = makeCanaryPlan(population, A, 1_000_000);
  validateExperimentPlan(shadowPlan);
  validateExperimentPlan(canaryPlan);
  const hashes = population.slice(0, A);
  const RUNS = 12;

  const pre: number[] = [];
  let preDigest = "";
  for (let r = 0; r < RUNS; r++) {
    let state: ShadowState | undefined;
    pre.push(onceMs(() => {
      state = fullShadowExperiment(shadowPlan, hashes);
    }));
    const digest = stableStringify(state);
    if (r === 0) preDigest = digest;
    else if (digest !== preDigest) throw new Error("pre-phase state divergence");
  }

  if (arm === "canary") {
    let canaryDigest = "";
    for (let r = 0; r < RUNS; r++) {
      const state = fullCanaryExperiment(canaryPlan, hashes);
      const digest = stableStringify(state);
      if (r === 0) canaryDigest = digest;
      else if (digest !== canaryDigest) throw new Error("canary-phase state divergence");
    }
  } else {
    for (let r = 0; r < RUNS; r++) {
      const state = fullShadowExperiment(shadowPlan, hashes);
      if (stableStringify(state) !== preDigest) throw new Error("filler-phase state divergence");
    }
  }

  const post: number[] = [];
  for (let r = 0; r < RUNS; r++) {
    let state: ShadowState | undefined;
    post.push(onceMs(() => {
      state = fullShadowExperiment(shadowPlan, hashes);
    }));
    if (stableStringify(state) !== preDigest) throw new Error("post-phase state divergence");
  }

  const preSteady = median(pre.slice(2));
  const postFirst2 = ((post[0] as number) + (post[1] as number)) / 2;
  const postSteady = median(post.slice(2));
  process.stdout.write(
    `arm=${arm}: shadow pre-steady=${preSteady.toFixed(2)}ms | post-first2=${postFirst2.toFixed(2)}ms ` +
      `post-steady=${postSteady.toFixed(2)}ms | steady-delta=${(postSteady - preSteady).toFixed(2)}ms ` +
      `first2-delta=${(postFirst2 - preSteady).toFixed(2)}ms\n`
  );
  process.exit(0);
}

/* ------------------------------------------------------------------ */
/* main process: cells a, b, c, then spawn the two cell-d arms         */
/* ------------------------------------------------------------------ */
const population = makePopulation(P);
const shadowAnchorPlan = makeShadowPlan(population, A);
validateExperimentPlan(shadowAnchorPlan);
const anchorHashes = population.slice(0, A);

// Shadow anchor first (canary code not yet run in this process — the
// mixed-process interaction is cell d's job, in fresh child processes).
fullShadowExperiment(shadowAnchorPlan, anchorHashes);
fullShadowExperiment(shadowAnchorPlan, anchorHashes);
const shadowRuns: number[] = [];
for (let r = 0; r < 5; r++) {
  shadowRuns.push(onceMs(() => {
    fullShadowExperiment(shadowAnchorPlan, anchorHashes);
  }));
}

/* ---------------------- cell a: canary anchors --------------------- */
const canaryUncappedPlan = makeCanaryPlan(population, A, 1_000_000, "exp_r19f_can_u");
validateExperimentPlan(canaryUncappedPlan);
const uncappedFinal = fullCanaryExperiment(canaryUncappedPlan, anchorHashes);
if (uncappedFinal.exposureCount !== A) throw new Error("uncapped exposure mismatch");
if (!uncappedFinal.halted || uncappedFinal.haltReason !== "budget-exhausted") {
  throw new Error("uncapped canary anchor must end budget-exhausted like the shadow anchor");
}
for (let i = 0; i < A; i++) {
  const a = uncappedFinal.assignments[i] as CanaryAssignment;
  if (a.action !== "candidate" || a.exposureCount !== i + 1) throw new Error(`uncapped action mismatch at ${i}`);
}
fullCanaryExperiment(canaryUncappedPlan, anchorHashes);
const canaryUncappedRuns: number[] = [];
for (let r = 0; r < 5; r++) {
  canaryUncappedRuns.push(onceMs(() => {
    fullCanaryExperiment(canaryUncappedPlan, anchorHashes);
  }));
}

const CAP = A / 2;
const canaryCappedPlan = makeCanaryPlan(population, A, CAP, "exp_r19f_can_c");
validateExperimentPlan(canaryCappedPlan);
const cappedFinal = fullCanaryExperiment(canaryCappedPlan, anchorHashes);
if (cappedFinal.exposureCount !== CAP) throw new Error("capped exposure mismatch");
for (let i = 0; i < A; i++) {
  const a = cappedFinal.assignments[i] as CanaryAssignment;
  const expected = i < CAP ? "candidate" : "baseline";
  if (a.action !== expected) throw new Error(`capped action mismatch at ${i}`);
  if (a.exposureCount !== Math.min(i + 1, CAP)) throw new Error(`capped exposureCount mismatch at ${i}`);
}
const canaryCappedRuns: number[] = [];
for (let r = 0; r < 5; r++) {
  canaryCappedRuns.push(onceMs(() => {
    fullCanaryExperiment(canaryCappedPlan, anchorHashes);
  }));
}

process.stdout.write(
  `cell a (end-to-end anchors, P=${P}/A=${A}, 5 runs each, min/median ms):\n` +
    `cell a shadow          min=${Math.min(...shadowRuns).toFixed(2)} median=${median(shadowRuns).toFixed(2)}\n` +
    `cell a canary-uncapped min=${Math.min(...canaryUncappedRuns).toFixed(2)} median=${median(canaryUncappedRuns).toFixed(2)} ` +
    `(delta-vs-shadow median=${(median(canaryUncappedRuns) - median(shadowRuns)).toFixed(2)})\n` +
    `cell a canary-capped   min=${Math.min(...canaryCappedRuns).toFixed(2)} median=${median(canaryCappedRuns).toFixed(2)} ` +
    `(cap=${CAP}; delta-vs-uncapped median=${(median(canaryCappedRuns) - median(canaryUncappedRuns)).toFixed(2)})\n`
);

/* ------------- cell b: per-op increments on ACTIVE states ----------- */
// maxAssignments = 2A so the k=A states stay active (no budget halt).
const shadowPerOpPlan = makeShadowPlan(population, 2 * A, "exp_r19f_pos");
const canaryPerOpPlan = makeCanaryPlan(population, 2 * A, 1_000_000, "exp_r19f_poc");
validateExperimentPlan(shadowPerOpPlan);
validateExperimentPlan(canaryPerOpPlan);
const shadowRunner = createShadowRunner(shadowPerOpPlan);
const canaryRunner = createCanaryRunner(canaryPerOpPlan);

let shadowActive: ShadowState = shadowRunner.start(0);
let canaryActive: CanaryState = canaryRunner.start(0);
let shadowRecordBase: ShadowState = shadowRunner.start(0);
let canaryRecordBase: CanaryState = canaryRunner.start(0);
for (let k = 0; k < A; k++) {
  const h = anchorHashes[k] as string;
  shadowActive = shadowRunner.assign(shadowActive, h, 0);
  shadowActive = shadowRunner.recordOutcome(shadowActive, outcomeFor(h), 0);
  canaryActive = canaryRunner.assign(canaryActive, h, SCOPE, 0);
  canaryActive = canaryRunner.recordOutcome(canaryActive, outcomeFor(h), 0);
  shadowRecordBase = shadowRunner.assign(shadowRecordBase, h, 0);
  canaryRecordBase = canaryRunner.assign(canaryRecordBase, h, SCOPE, 0);
  if (k < A - 1) {
    shadowRecordBase = shadowRunner.recordOutcome(shadowRecordBase, outcomeFor(h), 0);
    canaryRecordBase = canaryRunner.recordOutcome(canaryRecordBase, outcomeFor(h), 0);
  }
}
if (shadowActive.halted || canaryActive.halted) throw new Error("per-op states must stay active");
// restore round-trip identity (structural)
if (stableStringify(canaryRunner.restore(canaryActive)) !== stableStringify(canaryActive)) {
  throw new Error("canary restore round-trip divergence");
}
if (stableStringify(shadowRunner.restore(shadowActive)) !== stableStringify(shadowActive)) {
  throw new Error("shadow restore round-trip divergence");
}

const OPS = 200;
let sinkB = 0;
const tShadowRestore = timeMs(() => {
  for (let r = 0; r < OPS; r++) sinkB += shadowRunner.restore(shadowActive).assignments.length;
}, 5);
const tCanaryRestore = timeMs(() => {
  for (let r = 0; r < OPS; r++) sinkB += canaryRunner.restore(canaryActive).assignments.length;
}, 5);
const nextHash = population[A] as string;
const tShadowAssign = timeMs(() => {
  for (let r = 0; r < OPS; r++) sinkB += shadowRunner.assign(shadowActive, nextHash, 0).assignments.length;
}, 5);
const tCanaryAssign = timeMs(() => {
  for (let r = 0; r < OPS; r++) sinkB += canaryRunner.assign(canaryActive, nextHash, SCOPE, 0).assignments.length;
}, 5);
const lastHash = anchorHashes[A - 1] as string;
const tShadowRecord = timeMs(() => {
  for (let r = 0; r < OPS; r++) sinkB += shadowRunner.recordOutcome(shadowRecordBase, outcomeFor(lastHash), 0).outcomes.length;
}, 5);
const tCanaryRecord = timeMs(() => {
  for (let r = 0; r < OPS; r++) sinkB += canaryRunner.recordOutcome(canaryRecordBase, outcomeFor(lastHash), 0).outcomes.length;
}, 5);
void sinkB;

const perOp = (t: number): string => ((t / OPS) * 1e3).toFixed(1);
process.stdout.write(
  `cell b (per-op µs on ACTIVE k=${A} states, ${OPS} ops best-of-5):\n` +
    `cell b restore: shadow=${perOp(tShadowRestore)} canary=${perOp(tCanaryRestore)} ` +
    `increment=${(((tCanaryRestore - tShadowRestore) / OPS) * 1e3).toFixed(2)}µs (action/exposure checks + derivedExposure proof)\n` +
    `cell b assign:  shadow=${perOp(tShadowAssign)} canary=${perOp(tCanaryAssign)} ` +
    `increment=${(((tCanaryAssign - tShadowAssign) / OPS) * 1e3).toFixed(2)}µs (negative ≈ rng replay O(k) the canary never pays, cross-check of the R16-F rng pool)\n` +
    `cell b record:  shadow=${perOp(tShadowRecord)} canary=${perOp(tCanaryRecord)} ` +
    `increment=${(((tCanaryRecord - tShadowRecord) / OPS) * 1e3).toFixed(2)}µs (shared helper; delta isolates the two restore bodies)\n`
);

/* ------ cell c: S7-F-1 four-order signs on the CANARY host ---------- */
/* Frozen S6-F-1-era canary reference restore (verbatim loop structure  */
/* from the pre-S7-F-1 code, calling PRODUCTION validateExperimentPlan  */
/* so the delta isolates the S7-F-1 alignment machinery alone).         */
function refRestoreCanaryS6F1(serialized: CanaryState, expected: ExperimentPlan): CanaryState {
  if (typeof serialized !== "object" || serialized === null) {
    throw new DomainValidationError("canary state is required");
  }
  validateExperimentPlan(serialized.plan);
  if (serialized.plan.experimentId !== expected.experimentId) {
    throw new DomainValidationError("restored plan does not match runner");
  }
  if (serialized.plan.mode !== "canary") {
    throw new DomainValidationError("canary restore requires mode \"canary\"");
  }
  if (typeof serialized.halted !== "boolean") {
    throw new DomainValidationError("halted must be a boolean");
  }
  if (!Array.isArray(serialized.assignments) || !Array.isArray(serialized.outcomes)) {
    throw new DomainValidationError("assignments and outcomes must be arrays");
  }
  if (!Number.isInteger(serialized.guardrailBreaches) || serialized.guardrailBreaches < 0) {
    throw new DomainValidationError("guardrailBreaches must be an integer >= 0");
  }
  if (!Number.isInteger(serialized.exposureCount) || serialized.exposureCount < 0) {
    throw new DomainValidationError("exposureCount must be an integer >= 0");
  }
  if (typeof serialized.startedAtMs !== "number" || !Number.isFinite(serialized.startedAtMs)) {
    throw new DomainValidationError("startedAtMs must be a finite number");
  }
  if (typeof serialized.elapsedMs !== "number" || !Number.isFinite(serialized.elapsedMs) || serialized.elapsedMs < 0) {
    throw new DomainValidationError("elapsedMs must be a finite number >= 0");
  }
  let derivedExposure = 0;
  let structuralFault: DomainValidationError | undefined;
  const pending = new Set<string>();
  for (const assignment of serialized.assignments) {
    if (typeof assignment.episodeHash !== "string" || assignment.episodeHash.trim() === "") {
      structuralFault = new DomainValidationError("episodeHash is required");
      break;
    }
    pending.add(assignment.episodeHash);
    if (assignment.action !== "baseline" && assignment.action !== "candidate") {
      structuralFault = new DomainValidationError("invalid canary action");
      break;
    }
    if (assignment.action === "candidate") {
      derivedExposure += 1;
    }
    if (!Number.isInteger(assignment.exposureCount) || assignment.exposureCount < 0) {
      structuralFault = new DomainValidationError("assignment exposureCount must be an integer >= 0");
      break;
    }
  }
  const target = pending.size;
  if (target > 0) {
    let found = 0;
    let resolved = false;
    for (const hash of serialized.plan.population) {
      if (pending.has(hash)) {
        found += 1;
        if (found === target) {
          resolved = true;
          break;
        }
      }
    }
    if (!resolved) {
      const populationSet = new Set(serialized.plan.population);
      for (const assignment of serialized.assignments) {
        requirePopulationMember(populationSet, assignment.episodeHash);
      }
    }
  }
  if (structuralFault !== undefined) {
    throw structuralFault;
  }
  if (derivedExposure !== serialized.exposureCount) {
    throw new DomainValidationError("exposureCount does not match candidate assignments");
  }
  const haltReason = canonicalHaltReason(serialized.haltReason, serialized.halted);
  return {
    plan: serialized.plan,
    halted: serialized.halted,
    haltReason,
    assignments: [...serialized.assignments],
    outcomes: [...serialized.outcomes],
    guardrailBreaches: serialized.guardrailBreaches,
    exposureCount: serialized.exposureCount,
    startedAtMs: serialized.startedAtMs,
    elapsedMs: serialized.elapsedMs,
  };
}

const canaryOrderPlan = makeCanaryPlan(population, A, 1_000_000, "exp_r19f_can_o");
validateExperimentPlan(canaryOrderPlan);
const canaryOrderRunner = createCanaryRunner(canaryOrderPlan);

function makeCanaryOrderState(kind: "prefix" | "half" | "scattered" | "reversed"): CanaryState {
  const assignments: CanaryAssignment[] = [];
  for (let i = 0; i < A; i++) {
    let hash: string;
    if (kind === "prefix") hash = population[i] as string;
    else if (kind === "half") hash = i < A / 2 ? (population[i] as string) : (population[A + (i - A / 2)] as string);
    else if (kind === "scattered") hash = population[(3 * i + 1) % A] as string;
    else hash = population[A - 1 - i] as string;
    assignments.push({ episodeHash: hash, action: "candidate", exposureCount: i + 1 });
  }
  return {
    plan: canaryOrderPlan,
    halted: false,
    haltReason: undefined,
    assignments,
    outcomes: [],
    guardrailBreaches: 0,
    exposureCount: A,
    startedAtMs: 0,
    elapsedMs: 0,
  };
}

// alignment sanity: scattered has no fixed point ((3i+1)≡i mod 1000 has no
// solution), reversed has none (A−1−i=i has no integer solution), so both
// misalign at index 0 and exercise the plain-loop rechannel.
const RESTORES = 2000;
process.stdout.write(`cell c (S7-F-1 four-order signs on the CANARY host, ${RESTORES} restores/order):\n`);
for (const kind of ["prefix", "half", "scattered", "reversed"] as const) {
  const state = makeCanaryOrderState(kind);
  const prodOut = canaryOrderRunner.restore(state);
  const refOut = refRestoreCanaryS6F1(state, canaryOrderPlan);
  if (stableStringify(prodOut) !== stableStringify(refOut)) {
    throw new Error(`canary state divergence for order ${kind}`);
  }
  let sinkC = 0;
  const tRef = timeMs(() => {
    for (let r = 0; r < RESTORES; r++) sinkC += refRestoreCanaryS6F1(state, canaryOrderPlan).assignments.length;
  }, 3);
  const tProd = timeMs(() => {
    for (let r = 0; r < RESTORES; r++) sinkC += canaryOrderRunner.restore(state).assignments.length;
  }, 3);
  void sinkC;
  process.stdout.write(
    `cell c order=${kind.padEnd(9)}: reference=${tRef.toFixed(2)}ms production=${tProd.toFixed(2)}ms ` +
      `delta=${(tRef - tProd).toFixed(2)}ms sign=${tRef - tProd > 0 ? "+" : "-"}\n`
  );
}

/* -------- cell d: spawn the two arms in fresh processes ------------- */
for (const arm of ["canary", "control"] as const) {
  const out = execFileSync("npx", ["tsx", "tmp-r19f-axes.ts", `--arm=${arm}`], {
    cwd: process.cwd(),
    encoding: "utf8",
  });
  process.stdout.write(`cell d ${out}`);
}
```

两次干净独立运行（Node 22.22.2）原始输出：

```
cell a (end-to-end anchors, P=2000/A=1000, 5 runs each, min/median ms):
cell a shadow          min=114.77 median=115.64
cell a canary-uncapped min=110.44 median=113.61 (delta-vs-shadow median=-2.04)
cell a canary-capped   min=110.04 median=110.21 (cap=500; delta-vs-uncapped median=-3.40)
cell b (per-op µs on ACTIVE k=1000 states, 200 ops best-of-5):
cell b restore: shadow=52.9 canary=52.3 increment=-0.52µs (action/exposure checks + derivedExposure proof)
cell b assign:  shadow=65.1 canary=58.5 increment=-6.54µs (negative ≈ rng replay O(k) the canary never pays, cross-check of the R16-F rng pool)
cell b record:  shadow=61.0 canary=60.8 increment=-0.26µs (shared helper; delta isolates the two restore bodies)
cell c (S7-F-1 four-order signs on the CANARY host, 2000 restores/order):
cell c order=prefix   : reference=175.65ms production=105.00ms delta=70.65ms sign=+
cell c order=half     : reference=195.98ms production=177.20ms delta=18.78ms sign=+
cell c order=scattered: reference=177.95ms production=177.85ms delta=0.10ms sign=+
cell c order=reversed : reference=176.12ms production=176.73ms delta=-0.60ms sign=-
cell d arm=canary: shadow pre-steady=117.96ms | post-first2=114.74ms post-steady=115.03ms | steady-delta=-2.93ms first2-delta=-3.22ms
cell d arm=control: shadow pre-steady=116.92ms | post-first2=116.18ms post-steady=116.09ms | steady-delta=-0.83ms first2-delta=-0.74ms
---（第 2 次独立运行）---
cell a (end-to-end anchors, P=2000/A=1000, 5 runs each, min/median ms):
cell a shadow          min=116.83 median=118.07
cell a canary-uncapped min=113.13 median=116.25 (delta-vs-shadow median=-1.83)
cell a canary-capped   min=113.38 median=113.66 (cap=500; delta-vs-uncapped median=-2.58)
cell b (per-op µs on ACTIVE k=1000 states, 200 ops best-of-5):
cell b restore: shadow=54.6 canary=53.8 increment=-0.82µs (action/exposure checks + derivedExposure proof)
cell b assign:  shadow=66.8 canary=60.5 increment=-6.25µs (negative ≈ rng replay O(k) the canary never pays, cross-check of the R16-F rng pool)
cell b record:  shadow=62.5 canary=62.4 increment=-0.12µs (shared helper; delta isolates the two restore bodies)
cell c (S7-F-1 four-order signs on the CANARY host, 2000 restores/order):
cell c order=prefix   : reference=184.54ms production=106.65ms delta=77.89ms sign=+
cell c order=half     : reference=199.63ms production=179.80ms delta=19.84ms sign=+
cell c order=scattered: reference=184.57ms production=184.29ms delta=0.28ms sign=+
cell c order=reversed : reference=181.47ms production=181.80ms delta=-0.33ms sign=-
cell d arm=canary: shadow pre-steady=118.45ms | post-first2=115.44ms post-steady=115.06ms | steady-delta=-3.39ms first2-delta=-3.01ms
cell d arm=control: shadow pre-steady=118.59ms | post-first2=117.26ms post-steady=117.53ms | steady-delta=-1.06ms first2-delta=-1.33ms
```
