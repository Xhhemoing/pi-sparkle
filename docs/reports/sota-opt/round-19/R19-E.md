MODEL_SLUG=claude-fable-5-thinking-xhigh

# R19-E：`src/learning/` 第十九遍复查报告（Round 19）

**战役:** 全库持久 SOTA 优化 Round 19 / R19-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `d95564d`（含 R19-B 空枚举摄取与本派单）
**分支:** `cursor/r19-e-learning-nineteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第十九组新角度枚举为空（有理由的
空枚举，未铸造任何 S19-E-\* ID，与 R10-E..R18-E 同口径），切片维持关闭
（MORE_OPTIMA=no）。本轮首次补齐两条从未测过的完整性轴——(a) 输入表示/
来源保真轴：十八轮全部锚点都在生成器直造夹具上测量（字符串为引擎驻留
字面量、对象为生成器侧隐藏类），而生产输入经 `EventStore.readAll()` 逐行
`JSON.parse` 抵达（扁平非驻留字符串、解析器侧对象图）——同值夹具三臂
（raw / JSON 往返 / rope 去驻留）实测：温态三臂同带（rt/raw 0.986–0.993
collect、0.926–0.944 outcomes——生产表示反而持平或更便宜），首触面
生产表示 collect 贵 17–25%（绝对 +4.9–7.8µs/夹具，每进程一次），大文本
rope 摊平税 |斜率| ≤4.9ns/char 且跨尺寸符号翻转（16K 档 6/6 为负）——
「十八轮锚点测错了字符串/对象表示」的残余怀疑就此以实价关闭，方向还
反了（生成器夹具不便宜、部分档位反而更贵）。(b) 事件次序/排列不变性轴：
R17-E 价格表固定次序只变构成，十八轮从未变动同一多重集的排列——保输出
排列（两遍结构证明 + 6/6 逐位相等断言）价差仅 0.40–0.61µs/run（collect
交错税，6/6 同号但 ≤4.3%）、0.31–0.53µs（outcomes），对抗排列
（result 先于 route）实证 route-before-result 绑定契约（outcomes 3→0）
——「构成 × 次序」输入平面就此完全因子化，次序维度不产生候选。**

1. **零漂移九重确认**：切片 10 个文件（`wc -l` 确认恰 10 文件、合计 1770
   行，与 R14-E..R18-E 逐字节相同）自 R1-E 裁决基线（`adb20d7`）至本轮
   基线（`d95564d`）**逐字节未变**——`git diff adb20d7..HEAD --
   src/learning/` 为 0 行、`git log adb20d7..HEAD -- src/learning/` 为
   **0 提交**，十九轮全程无人触及该目录。且 R18-E 基线（`b1534b0`）之后
   的全部提交（R18-E..J 摄取、Round 18 收口、Round 19 开轮、R19-A/B 摄取
   与 R19-C/D/E 派单）**仅触及 `docs/reports/sota-opt/`**——
   `git log b1534b0..HEAD --name-only` 非 docs 路径为零、
   `git diff --stat b1534b0..HEAD -- src/ test/` 为空。R1-E..R18-E 的
   全部规模测量、调用面图景与 S1-E-1..8 / S2-E-1..7 / S3-E-1..5 /
   S4-E-1..3 / S5-E-1..5 / S6-E-1..5 / S7-E-1..5 / S8-E-1..3 / S9-E-1..3
   共 44 项切片排除裁决（R10-E..R18-E 未铸 ID，其空枚举收口一并继承，含
   R13-E 三处无名微观、R14-E 规模/JIT 双轴、R15-E GC/A-A 双轴、R16-E
   累积状态/载荷形态双轴、R17-E 异常路径/事件类构成双轴、R18-E 冷层/
   字符宽度双轴）原样成立、原样继承。
2. **本 VM 锚点/矩阵重锚（未拷贝 R18-E 数字，遵派单令；同 VM 双 Node
   版本交叉，延续 R13-E..R18-E 协议）**：SLICE-CPU 总量上界锚点 **16 次
   运行**（8 × Node v22.14.0 + 8 × Node v22.22.2；engines `>=22.19.0`
   仅后者合规）**19.0–20.6µs/run**（collect 14.0–14.8µs、outcomes
   2.7–2.9µs、diagnose 1.27–1.78µs、bandit-build 1.0–1.1µs；两版本
   重叠带 v22.14.0 19.0–20.6 / v22.22.2 19.2–19.8，版本差异不可分辨）
   ——距落地线（≥10ms）**486–525×**，即使把切片 CPU 清零也远不达门槛。
   本轮种子构成 collected=14 / outcomes=3 / diagnoseGroups=2 与 R17-E
   （15/4/1）不同，按派单令补跑 **6 次 R17-E 种子比对运行**
   （`0xe17e01/02/03` + R17-E NOW）：复现 collected=15 / outcomes=4，
   总量 **18.5–19.7µs——逐点落在 R17-E 带 18.5–20.2 之内**（collect
   12.7–13.8 / outcomes 4.0–4.2 与 R17-E 12.7–14.1 / 4.0–4.3 逐项
   同带）——带差即构成差，且本轮新轴 B 的类普查（本轮 collect 夹具含
   12 条 TASK_RESULT vs R17-E 夹具 9 条，最贵类 0.97–1.18µs/事件；
   judge 1 vs 5）给出同向的价签算术。配置态 live 面：
   `applyLearnedRouting` 非空策略 **256–312ns/task**（R13-E 258–312 /
   R14-E 256–303 / R15-E 258–298 / R16-E 269–294 / R17-E 257–290 /
   R18-E 268–314 同带）、空策略 **113–136ns/task**（R15-E..R18-E
   110–140 同带）；`loadLearnedRouting` 全新 root 25.8–35.0µs、现实
   registry **77.1–109.5µs**（R8-E 80–85 / R15-E..R18-E 74.3–134.5
   同带，I/O 抖动，亚 ms 一次性）。全部确定性 VERDICT 行 16 次运行
   **逐位一致**（`E=41 collected=14 routedE=41 outcomes=3 S=12
   diagnoseGroups=2 prevArms=3`），3/3 sanity 检查全过（§1、附录 A）。
3. **配置态 × 命令类矩阵复核无洞**：全部切片进入点行号逐条 rg 复核，
   **与 R18-E 记录逐一相同、无新增调用方、无新增命令类**
   （`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
   `run/flowchart-run:681/712`、`track/loop:88/172`；resume/replay 经
   `flowchartForSupervisor` @ `flowchart-run:880/1021` 各单次载入；
   `run --flowchart` @ `cli/main:580` 在 `:708` 载入前返回，无双载入）。
   S8-E-1 双载入按令保护，未做任何去重分析。切片外消费者复核与 R18-E
   逐一相同（`adaptation/eval-routing.ts:223` 仅
   `parseLearnedRoutingPolicy`、`track/primary-split.ts:3` 类型导入、
   `privacy/record-classes.ts:212/224` 文档字符串、
   `adaptation/approval-profile.ts:44` docstring 假阳性）。测试专用面
   （`patterns`/`attribution`/`signatures` + 五个测试专用导出）本轮
   交叉检索复核仍零生产调用方。
4. **本轮两条实扫新轴（§2 前置）**：(a) **输入表示/来源保真轴**（本切片
   首次；R3-E..R18-E 全部锚点夹具由生成器直造——字符串是引擎驻留的
   源码字面量、对象是生成器侧字面量隐藏类；生产输入却经逐行 JSON.parse
   抵达，字符串扁平非驻留、对象为解析器构建。锚点是否测对了表示从未
   被检验）：同值三臂（raw 生成器原样 / rt 逐事件 JSON 往返 = 生产面 /
   rope 全字符串运行期拼接去驻留），三臂输出逐位相等断言 6/6 全过。
   温态：collect rt/raw **0.986–0.993**、outcomes rt/raw **0.926–0.944**
   （生产表示更便宜——JSON 往返丢弃 TASK_RETRY 上 present-but-undefined
   的 `taskId` 属性使形态更瘦）；首触面（每夹具只走一遍，F=300 新鲜
   夹具/臂）：collect rt/raw **1.170–1.250**（+4.9–7.8µs/夹具，V8 解析器
   侧对象图的 map/IC 冷启动，每进程一次级别）、outcomes rt/raw
   0.918–0.995；大文本摊平税站（rope vs flat，1K/16K/128K）：|斜率|
   ≤**4.9ns/char** 且跨尺寸符号翻转（1K +2.1~+4.9 一次 −0.04、16K 6/6
   为负 −0.51~−1.65、128K 6/6 为正 +1.06~+1.42），唯一 6/6 稳定正带
   （128K）折算 T\*(10ms) ≈ **7.0–9.4M 字符**——低于 R18-E 扫描斜率
   （9.1–13.6ns/char）一个量级、距生产文本 4 个量级。**「锚点测错
   表示」假说以实价反向关闭：温态同带，个别档位生成器夹具反而更贵。**
   (b) **事件次序/排列不变性轴**（本切片首次；R17-E 固定次序变构成，
   排列维度从未测过）：保输出排列（collect 四排列——两遍结构下发射类
   保持相对次序、路由/发现/噪声类自由移动；outcomes 三排列——被消费类
   保持相对次序、噪声类自由移动；逐位相等断言 + 多重集守恒断言 6/6
   全过）：collect 价差 **0.40–0.61µs/run（2.8–4.3%）且 6/6 同号**
   （原始交错序恒为最慢臂 = 分派分支预测的交错税）、outcomes 价差
   0.31–0.53µs；对抗排列（全部 TASK_RESULT 前置于 MODEL_ROUTED）：
   outcomes **3→0**（route-before-result 绑定契约的语义事实，非缺陷）、
   耗时跌至 0.32–0.34µs（绑定工作即成本主体）。**「构成 × 次序」输入
   平面因子化完成：成本 = f(构成)，次序仅贡献亚 µs 交错税。**
5. **第十九组新角度枚举为空（§2）**：本轮新透镜为「**输入表示 × 输入
   次序**」（两条测量轴的代码侧对偶：逐函数问 (a) 哪些位点对值表示
   敏感、任何表示特化是否等价且可能越线；(b) 哪些位点可利用次序不变性
   或次序依赖、任何重排/预排序是否等价且可能越线）。对第十九遍全量
   重读的 10 个源文件逐函数普查：全部命中形态落在排除表点名保护
   （X1-1 驻留/预摊平缓存族、S1-E-3 extractAssistant parts+join、
   S1-E-1 两遍合一、S3-E-1 分派重排、S9-E-2 预过滤负优化、S9-D-1
   形态单态化邻域、X0-2 行为变更）、切片外辖区（事件解析器在
   `run/events`，G 切片；V8 map/IC 冷启动是引擎层）或落地线算术封死区
   （首触 +4.9–7.8µs、交错税 ≤0.61µs、摊平税 T\* ≥7.0M 字符）。**未
   铸造任何 S19-E-\* 新 ID，无新增排除提议；S13-B-1 未以任何形态重提
   （`applyLearnedRouting` 内部零改动，重开条件 |avoid|≈965–1,063 维持
   未触发）；S8-E-1 未做去重分析；S9-E-2 未触碰；R13-E 三处无名微观
   未补铸；R14-E..R18-E 十轴未复铸。**
6. **验证面**：`pnpm gate` 在 engines 合规的 Node v22.22.2 上**全绿**
   （typecheck ✓ / lint ✓ / test **1168 pass / 0 fail / 1 skipped**（共
   1169 / 78 suites，与 R14-E..R18-E 计数逐一相同）/ build ✓）。切片
   相关套件 **48 tests / 2 suites / 48 pass / 0 fail**（v22.14.0 与
   v22.22.2 均过）。系统默认 node 仍为 v22.14.0（`/exec-daemon/node`
   PATH 前置遮蔽 nvm，违反 engines `>=22.19.0`，R12-E §0 起连续记录的
   同一环境事实）；gate 结论以 v22.22.2 为准。零代码改动下全部硬不变量
   天然满足。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与
双归因未动；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故
未提交任何 sim/锚点脚本（差异面与新轴脚本测量段进附录 A，遵守
R7-F/R8-C/R9-I/R10-E..R18-E 无赢家纪律）。本切片在其输出契约与数据面语义
下维持 SOTA——第十九遍复查确认：**剩余的全部 ms 级余量仍全部在被排除表
点名保护的 I/O 契约面、once-per-run CLI 面或切片外辖区上**，切片级收口
条件（R3-E §7 … R18-E §6）依然成立。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行）本轮第十九遍全量实际读码，未依赖前
  十八轮记忆。上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、
  `protocol/`、`privacy/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表 546 行，含全部
  S\*-E-\* 行与 S8-E-1 / S9-E-2 / S13-B-1）→ PROGRESS.md →
  round-19/PLAN.md → round-18/R18-E.md（空枚举；冷层 + 字符宽度）→
  round-17/R17-E.md（空枚举；异常路径 + 事件类构成）→ R16-E / R15-E /
  R14-E 收口段（按需，透镜与先例核对）→ 10 个源文件。R19-C / R19-D 在
  其他分支在跑，未等待、未触碰其报告与分支。
- 基线漂移检查（§结论 1）：切片零提交零 diff；`b1534b0..d95564d` 全部
  提交仅触及 `docs/reports/sota-opt/`——前十八轮全部测量与裁决原样成立。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S18 时代条目。特别地：
  - **S13-B-1**（B 切片辖区）未以任何变体重提：本切片辖区只有被调方
    `applyLearnedRouting` 本体，本轮对其**零改动**（切片零 diff 下必然）；
    其重开条件（本体成本 ≥2 个量级，R16-E 量化为 |avoid| ≈ 965–1,063）
    未触发、生产写入方（≤M×8）不可触发；本轮两轴均不触及 apply 内部。
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护，本轮仅复核其行号与频次
    （§1），未做任何去重分析。并发 promote 下的新鲜性仍是其保留理由。
  - **S9-E-2**（pass-2 预过滤负优化）未触碰：本轮次序轴的「按次序先扫
    再跳」形态在 §2.2 行 6 以 S9-E-2/S1-E-1 换名拒列，未做实验重开。
  - **R13-E 三处无名微观**（bandit `includes` 旁路已建 arms Set @
    `bandit-store:72`、apply avoided-Set filter+map 双中间数组 @
    `learned-routing:198-202`、`routingPolicyIdentity` 字面量再校验 @
    `learned-routing:43`）：位点本轮逐字复核未变（零 diff 下必然），
    维持 R13-E 裁决，**未补铸 ID、未重开**。
  - **R14-E 规模/JIT、R15-E GC/A-A、R16-E 累积状态/载荷形态、R17-E
    异常路径/事件类构成、R18-E 冷层/字符宽度十轴未复铸**：在 src/test
    零漂移下原样继承（派单明令勿复铸）；本轮两条新轴（输入表示、输入
    次序）为不同测量维度，无交叠——R18-E 冷层轴测的是**代码的执行
    层级**（解释层 vs JIT 温态），本轮 A 轴测的是**数据的表示形态**
    （驻留字面量 vs 解析器扁平串 vs rope）；R17-E B 轴测的是**类构成
    边际价**（次序固定），本轮 B 轴测的是**排列价**（构成固定）；
    R16-E B2/R18-E B 测的是文本的字节量与字宽类别，本轮 A3 测的是
    同值文本的**存储表示**（摊平税）。
  - R10-E..R18-E 未铸 S10..S18-E-\*，本轮亦未事后发明任何此类编号；
    R19-A / R19-B 空枚举（各自辖区）与本切片无交（R19-B 的比较器第三腿
    与目录基数轴均在 B 辖区）。
- **换名重提检查**：本轮新定位形态的换名检查全部并入 §2.2 表格逐行给出，
  并逐一对照 EXCLUSIONS 全表 + 十八轮 E 报告 + 九轮空枚举面（其换名裁决
  全部原样维持，不再复抄）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  稀疏 → 已批准 R0 基线；双 LCB / 双归因在位；晋升 proposal-first；无
  阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；fail-open / 隐藏
  模块缓存（X1-1）未引入；bandit/learned registry I/O 的 fsync 与完整性
  重哈希未触碰；corrupt registry fail-closed 未触碰；两遍
  `collectSignalsFromEvents` 语义未触碰；X0-3 维持；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；
  未 force-push；仅提交并推送本分支上的本报告一个文件。
- 环境：本 VM 系统默认 node v22.14.0（`/exec-daemon/node`，PATH 前置
  遮蔽 nvm），nvm 另装 v22.22.2（engines `>=22.19.0` 仅后者合规）。锚点
  与两条新轴在两个版本各测（§1/§2，同带或标注）；`pnpm gate` 以
  v22.22.2 为准（§4）。依赖经 `pnpm install --frozen-lockfile` 安装
  （pnpm 10.17.1）。
- 新轴测量注意事项（本轮实际踩过、供后轮复用）：(a) rope 臂的字符串
  **不得在计时窗前被触碰**——`JSON.stringify`、`===` 内容比较、尾部
  `.slice` 都会就地摊平 ConsString：初版 A3 用 `flatten(r)` 从 rope 派生
  flat 臂，等于把 rope 臂预先摊平成第二个 flat 臂（已修正为同种子独立
  流各造一次）；值相等断言用计时数组之外的一次性探针对完成。(b) 大字符
  串站必须**在本尺寸上先做双臂 JIT 预热**，否则先计时的臂吸收 tier-up
  成本呈假负差（初版据此修正一次）。(c) 保输出排列断言要剥离 `createdAt`
  （`nowIso()` 壁钟字段跨调合法不同，R18-E 注意事项 (d) 同源）。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

R7-I 教训（默认态夹具可能掩盖配置态主路径）按令复查。src 自 `b1534b0` 起
一行未动（且自 `d148d12` 起 src/test 累计零 diff 的 R15-E..R18-E 论证链
原样有效），故 R8-E..R18-E §1 矩阵表结构原样成立；本轮逐条 rg 复核全部
切片进入点行号，**与 R18-E 记录逐一相同、无新增调用方**：

| 命令类 × 配置态 | 切片进入点（本轮行号复核） | 频次与规模 |
| --- | --- | --- |
| `adapt auto --run` | `runAutoAdaptFromEvents` @ `cli/adapt:188` → `runAutoAdaptLoop` | 每进程一次，E≈41 |
| `adapt auto --project` | `runAutoAdaptLoop` @ `cli/adapt:205`（空事件表，S4-E-1 面） | 每进程一次 |
| `adapt learn --run` | `proposeRoutingFromRoutedEvents` @ `cli/adapt:168`（S2-E-2 面） | 每进程一次 |
| 普通 `run` | `loadLearnedRouting` @ `cli/main:708`（S2-I-1 已裁决） | 每进程一次 |
| `run --flowchart` | `startFlowchartRun` @ `cli/main:580` → `flowchartForSupervisor` @ `flowchart-run:747→712` 单次载入（在 `cli/main:708` 之前返回，无双载入） | 每进程一次 |
| `run --children` | `cli/main:708` 载入 → `applyLearnedRouting` @ `routing/assign:102` 每任务 → `startFlowchartRun` @ `cli/main:748` → `flowchart-run:747→712` 第二次载入（S8-E-1，勿去重）→ `applyLearnedRouting` @ `flowchart-run:681` 每节点 → `runAutoAdaptLoop` @ `cli/main:783` 一次 | 双载入各一次；任务/节点 ≤10/几十 |
| `track`（startTrackedRun） | `track/loop:88` 载入 → 每任务 `applyLearnedRouting` → `startFlowchartRun` @ `track/loop:152` → `flowchart-run:712` 第二次载入（同 S8-E-1）→ `runAutoAdaptLoop` @ `track/loop:172` 一次 | 同上 |
| flowchart resume / replay | `flowchartForSupervisor` @ `flowchart-run:880` / `:1021` 各单次载入 | 每恢复一次 |
| SPARKLE_AUTO_ADAPT=0 | 仍收集（persistSignals + updateProjectBandit 照付），仅跳过 propose；R5-E 裁决维持 | 不变 |
| 测试专用面 | `patterns` / `attribution` / `signatures` / `compareSignatures` / `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `parseObservedSignal`(直调) / `proposeRoutingFromAssignments`(deprecated) / `saveLearnedRouting`(deprecated, throw-only) / `learnedRoutingPath`(死导出) 交叉检索复核仍零生产调用方 | 不进热路径 |

切片外其余消费者以 `learning/` 模块路径精确检索复核：
`adaptation/eval-routing.ts:223` 仅调用 `parseLearnedRoutingPolicy`（离线
eval 面，D 切片）；`track/primary-split.ts:3` 仅导入类型；
`privacy/record-classes.ts:212/224` 仅文档字符串提及路径布局；
`adaptation/approval-profile.ts:44` 维持 R14-E 裁决的 docstring 假阳性
（「auto-loop never CAS-promotes」，非调用边）。**矩阵结论：无新洞，
消费者集合与 R18-E 逐一相同**。

SLICE-CPU 锚点本 VM 重测（**16 次运行**：8 × Node v22.14.0 + 8 ×
Node v22.22.2；seeds `0xe19e01`（collect）/ `0xe19e02`（routed）/
`0xe19e04`（signals）——R12-E..R18-E 拒绝采样同协议，signals 首选种子
`0xe19e03` 被拒（diagnoseGroups=0 < 1），顺延一位通过；探针记录
seed→(collected,outcomes,diagnoseGroups)：01→(14,2,1)、02→(17,3,1)、
03→(23,3,0 拒)、04→(13,3,2)；夹具镜像 R8-E..R18-E 生成器，R1-A 组成
E=41）：

```text
collect=14.0-14.8us  outcomes=2.7-2.9us  diagnose=1.27-1.78us  bandit-build=1.0-1.1us
total in-slice CPU ~19.0-20.6us per full auto-adapt run （两 Node 版本重叠带：
  v22.14.0 19.0-20.6 / v22.22.2 19.2-19.8，版本差异不可分辨）
vs landing bar >=10000us  ->  486-525x below EVEN IF ZEROED
带间比对：本轮构成 collected=14 / outcomes=3 / diagnoseGroups=2 与 R17-E
  （15/4/1）不同，按派单令补跑 6 次 R17-E 种子比对运行（0xe17e01/02/03 +
  R17-E NOW）：复现 collected=15 / outcomes=4 / diagnoseGroups=1，总量
  18.5-19.7us 逐点落在 R17-E 带 18.5-20.2 之内（collect 12.7-13.8 /
  outcomes 4.0-4.2 逐项同带；VERDICT 行 6/6 与 R17-E 夹具行逐字相同）。
  本轮带位置由类普查机械解释：本轮 collect 夹具 TASK_RESULT=12（R17-E 9）
  x 最贵类 0.97-1.18us、judge=1（R17-E 5）x 0.31-0.35us——同向价签算术
  （R17-E 价格表继续有效，无需重derive）
configured-state: applyLearnedRouting(avoid=10, M=10) = 256-312ns/task （R13-E 258-312 /
                                                        R14-E 256-303 / R15-E 258-298 /
                                                        R16-E 269-294 / R17-E 257-290 /
                                                        R18-E 268-314 同带）
                  applyLearnedRouting(空策略)        = 113-136ns/task （R15-E..R18-E
                                                        110-140 同带）
                  loadLearnedRouting fresh(ENOENT)   = 25.8-35.0us/次
                  loadLearnedRouting 现实 registry    = 77.1-109.5us/次 （R8-E 80-85 /
                                                        R15-E..R18-E 74.3-134.5 同带，
                                                        I/O 抖动，亚 ms 一次性，S8-E-1 带）
```

注：本轮夹具组成为 collected=14 / outcomes=3 / diagnoseGroups=2；diagnose
分组路径经 diagnoseGroups=2 全数走到。全部确定性 VERDICT 行 16 次运行
**逐位一致**，3/3 sanity 检查全过。**锚点复核成立：该切片不存在不推翻
既有排除就能达门槛的候选。**

## 2. 本轮两条实扫新轴 + 第十九组新角度枚举：有理由的空枚举

### 2.0 新轴 A：输入表示/来源保真（本切片首次）

R3-E..R18-E 十六轮锚点谱系的夹具全部由生成器直造：字符串是源码字面量
（引擎驻留、扁平、跨事件共享同一对象），对象是生成器侧对象字面量（稳定
隐藏类）。生产输入却经 `EventStore.readAll()` 逐行 `JSON.parse` 抵达：
字符串扁平但**非驻留、逐对象独立**，对象图由解析器泛型构建。「十八轮
锚点测的是不是错误的表示」是锚点谱系上最后一个未检验的保真假设。本轮
同值三臂定价（raw = 生成器原样 / rt = 逐事件 `JSON.parse(JSON.stringify)`
= 生产表示面 / rope = 全字符串运行期拼接重建 = 去驻留 + ConsString），
三臂 `JSON.stringify` 字节相等 + 输出逐位相等断言全过，6 次运行（两
Node 版本各 3 次）：

```text
A1 温态（同一夹具反复走，reps 20000）:
  collect: raw=14.90-15.97us  rt=14.70-15.86us  rope=14.80-16.03us
           rt/raw=0.986-0.993 —— 生产表示与锚点表示同带（差 <1.4%）
  outcomes: raw=2.89-2.98us  rt=2.68-2.82us  rope=2.95-3.08us
           rt/raw=0.926-0.944 —— 生产表示稳定便宜 6-7%：JSON 往返丢弃
           TASK_RETRY 上 present-but-undefined 的 taskId 属性，对象形态
           更瘦更单态（`=== undefined` 语义不变，输出逐位相等）
A2 首触面（每夹具只走一遍，F=300 新鲜夹具/臂，生成先于全部计时窗）:
  collect: raw=28.0-29.0us  rt=33.5-36.2us  rope=28.7-30.8us
           rt/raw=1.170-1.250 —— 生产表示首触贵 17-25%（+4.9-7.8us/夹具）：
           解析器侧对象图的 V8 map 过渡/IC 冷启动；每进程一次级别
  outcomes: raw=8.00-8.60us  rt=7.35-8.39us（rt/raw 0.918-0.995）
           rope=10.97-12.27us（+33-44%，去驻留串的哈希缓存冷启动 +
           Map 键比较走全内容比对）
A3 大文本摊平税站（TASK_RESULT summary，rope vs flat，每串只走一遍）:
  len~1K:   delta +2.1~+5.0us 一次 -0.04（+2.08~+4.91ns/char，符号 5/6 正）
  len~16K:  delta -8.1~-26.4us（-0.51~-1.65ns/char，6/6 负）
  len~128K: delta +135.5~+182.3us（+1.06~+1.42ns/char，6/6 正）
  唯一 6/6 稳定正带（128K）折算 T*(10ms) ~ 7.0-9.4M chars —— 距生产文本
  4 个量级；|斜率| 全表 <=4.9ns/char，低于 R18-E 扫描斜率 9.1-13.6ns/char
sanity: 10/10 x6 全过（三臂夹具字节相等 + collect/outcomes 输出逐位相等
  含 createdAt 剥离 + 摊平站值相等一次性探针）
```

**裁决**：(a) **保真假设成立且方向反了**：温态三臂同带（rt/raw ≤1.4%
差），生产表示在 outcomes 上还便宜 6–7%——十八轮锚点**没有**低估生产
表示的温态成本，个别位置反而高估；锚点谱系全部裁决在表示维度上安全。
(b) **首触溢价不产生候选**：+4.9–7.8µs/夹具是每进程一次级别（R18-E 冷层
轴的相邻现象、数据侧对偶），绝对量距落地线 3 个量级，且减法（预热解析器
对象图、形态归一化）分别是 S9-E-2 负优化换名与解析器辖区（`run/events`，
G 切片）/引擎辖区。(c) **摊平税不产生候选**：符号跨尺寸翻转、|斜率| 低于
已定价扫描斜率一个量级；切片内唯一 rope 工厂 `extractAssistant` 的
`text + "\n" + part.text` 拼接（S1-E-3 已裁面）其首次 `PEER_NEGATIVE.test`
/truncate 触碰付一次摊平，被本站上界覆盖；「入库前预摊平/驻留化」= X1-1
隐藏缓存族 + 对已扁平生产串是纯负优化。**该轴把「锚点表示保真」从假设
变成三臂价格表并关闭。**

### 2.1 新轴 B：事件次序/排列不变性（本切片首次）

R17-E 价格表证明了成本对**构成**的可加性（固定次序、变类计数、重构比
1.01–1.05）；十八轮从未变动**同一多重集的排列**。本轮在锚点夹具
（seeds `0xe19e01/02`）上做保输出排列定价——collect 是两遍结构：pass-2
发射类（CHILD_MESSAGE/USER_ANSWER/JUDGE_DECISION/RUN_FAILED）保持相对
次序，PROJECT_DISCOVERED/MODEL_ROUTED/噪声类自由移动，输出必逐位相等；
outcomes 的被消费类（MODEL_ROUTED/TASK_RETRY/CHILD_MESSAGE）保持相对
次序，噪声类自由移动。另做一个**对抗排列**（全部 TASK_RESULT 前置于
全部 MODEL_ROUTED）标定次序的语义载荷。6 次运行（两 Node 版本各 3 次），
逐位相等断言 + 多重集守恒断言全过：

```text
B0 锚点夹具类普查（构成归因输入，VERDICT 行 6/6 逐位一致）:
  collect(0xe19e01): CHILD_MESSAGE=12 JUDGE_DECISION=1 USER_ANSWER=4(1 hit)
    MODEL_ROUTED=12 PROJECT_DISCOVERED=1 噪声=11 —— E=41
  routed(0xe19e02): CHILD_MESSAGE=12 MODEL_ROUTED=9 噪声=19 PROJECT_DISCOVERED=1
B1 collect 保输出排列（original / routes-first / routes-last / noise-first）:
  original=14.07-14.33us 恒为最慢臂；块化臂 13.46-13.98us
  spread=0.40-0.61us（2.8-4.3%），6/6 同号 —— 交错序的分派分支预测税
B2 outcomes 保输出排列（original / noise-first / noise-last）:
  original=2.72-2.93us  noise-first=2.41-2.47us  noise-last=2.40-2.80us
  preserve-spread=0.31-0.53us
B2-adv 对抗排列（results-before-routes）: outcomes 3 -> 0（6/6），耗时
  0.32-0.34us —— 绑定即成本主体；route-before-result 是绑定契约本体，
  掉绑定是 correct-by-contract 的语义事实，非缺陷
sanity: 7/7 x6 全过（保输出排列逐位相等含 createdAt 剥离 + 排列多重集
  守恒 + 对抗排列只降不增断言）
```

**裁决**：(a) **交错税不产生候选**：0.40–0.61µs/run 6/6 同号是真实的
分支预测效应，但收割它需要调用方先对事件做类块化预排序——一遍 O(E)
分区 + 拷贝的成本与税额同阶（净收益 ≈0），预排序属调用方输入面变更
（X0-2 邻域），且事件次序是持久化数据面的契约次序（TASK_RETRY 语义
依赖它，B2-adv 实证）。代码侧等价形态（分派重排）= S3-E-1 逐字维持
（其八次测量异号的裁决与本轮「输入侧才有稳定符号」的发现相容：税在
输入交错，不在分派代码序）。(b) **次序依赖不可优化**：outcomes 的
route-before-result 是绑定契约本体，任何「先扫 routes 再扫 results」
的两遍化 = S1-E-1（迟到 PROJECT_DISCOVERED/重路由绑错模型）已裁面；
按次序探测跳过 = S9-E-2 换名。(c) **因子化完成**：R17-E 证明
成本 = g(类构成)（可加、重构比 1.01–1.05），本轮证明同构成下排列仅
贡献 ≤0.61µs——「构成 × 次序」输入平面全覆盖，未来带间漂移的归因
次序：先查构成（R17-E 价签），再查排列（本轮上界），最后疑代码。

### 2.2 第十九组枚举表

方法：本轮代码侧透镜「**输入表示 × 输入次序**」（两条测量轴的对偶）：
对第十九遍全量重读的 10 个源文件逐函数问 (a) 哪些位点对值表示敏感
（字符串驻留性、rope/flat、对象形态来源）、任何表示特化是否等价且可能
越线；(b) 哪些位点可利用次序不变性或次序依赖、任何重排/预排序是否等价
且可能越线；同时按惯例对本轮逐字新点名的位置做换名检查（对照 EXCLUSIONS
全表 + 十八轮 E 报告 + 九轮空枚举面）。扫描结果：**零个可立项形态**，
逐类裁决如下（均未铸造新 ID）：

| 形态（本轮第十九遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **入库侧字符串驻留化/预摊平/哈希预热**（对 JSON.parse 产物做 intern 池或预触碰，加速 Map 键比较——A 轴视角首次整体定价） | rope 臂 outcomes +33–44% 的镜像 | **实测反向 + 排除行封死拒列（不铸 ID）**：生产串经 JSON.parse 已扁平且首触后哈希即缓存（A2 rt/raw outcomes 0.918–0.995 = 无税可省）；intern 池 = X1-1 隐藏模块缓存族 + 对已扁平串纯增一遍 O(n) | X1-1 + 本轮新轴 A2 |
| **extractAssistant 拼接改 parts+join / 预摊平**（切片内唯一 rope 工厂 @ `signals:387`） | 首次 regex 触碰的一次摊平 | 换名重提拒列：S1-E-3 逐字维持（现实 6 段 ~230ns）；本轮 A3 为其补摊平税上界（\|斜率\| ≤4.9ns/char，低于扫描斜率一个量级、符号还不稳） | S1-E-3 + 本轮新轴 A3 |
| **TASK_RETRY 载荷形态归一化**（消灭 present-but-undefined `taskId` 使消费侧更单态——A1 outcomes rt<raw 的机械来源） | ~0.2µs/run（温态 6–7% × 2.9µs） | **辖区 + 已裁面双封死拒列**：事件对象由 `run/events`/EventStore 解析产出（G 切片辖区），生产面本就是瘦形态——肥形态只存在于测量夹具；切片内消费侧改动无对象可改；形态单态化定价先例 S9-D-1（D 切片）同向 | S9-D-1 族 + 本轮新轴 A1 |
| **解析器对象图首触预热**（进程起点假解析一批事件换 map/IC 温启动） | 首触溢价 +4.9–7.8µs/进程 | 净负优化拒列：预热价照付 + 真数据 map 未必命中同过渡链；S7-H-3（模块加载期 JIT 预热三次净负）与 R18-E「CLI 启动预热调用」同式 | S7-H-3 / R18-E §2.2 行 2 + 本轮新轴 A2 |
| **调用方事件预排序/类块化**（收割 B1 交错税 0.40–0.61µs） | ≤0.61µs/run | **算术 + 契约双封死拒列**：O(E) 分区+拷贝成本与税额同阶（净 ≈0）；事件次序是持久化契约次序（TASK_RETRY/绑定语义依赖，B2-adv 实证 3→0）；调用方输入面变更 = X0-2 邻域 | X0-2 + 本轮新轴 B1/B2-adv |
| **collect/outcomes 分派重排或按次序短路**（利用「routes 常在前」跳过已完成 pass-1 工作） | 亚 µs | 换名重提拒列：S3-E-1（分派重排八次异号）/ S1-E-1（两遍合一，迟到 PROJECT_DISCOVERED 发散）/ S9-E-2（预过滤负优化）逐字维持；本轮发现税在输入交错而非代码分派序，代码侧无收益位 | S3-E-1 / S1-E-1 / S9-E-2 |
| **outcomes 绑定 Map 换按次序数组游标**（假设 route 单调先行） | 亚 µs | **不等价拒列**：TASK_RETRY 就地更新路由绑定（`routes.set` 覆写）、乱序 result 掉绑定是契约行为——游标化在重试/乱序上发散；B2-adv 实证次序携带语义 | 硬不变量 + 本轮新轴 B2-adv |
| **JUDGE_DECISION 双 Map.get / verdict 双分派 / 条件 spread 形状 / baseSignal 形状多态 / binding 五重 spread** | ns 级 | 换名重提拒列：S1-E-2 / S9-E-3 族 / S8-E-2 / S4-E-3 逐字维持；R14-E JIT 轴与 R15-E GC 天花板继续覆盖；本轮 A1 温态三臂同带再证形状面无未计价余量 | S1-E-2 / S9-E-3 / S8-E-2 / S4-E-3 |
| `from-episode` 字面量再校验 / Date.parse / familyFromRouted includes / isCompleteRoute 谓词序 / applyCascadeRetry / seen-Set 单探针 / outcomeKindFromResult 死形参 / 环内 try/catch | ns 级或实测 ~0 | 全部维持 R10-E..R18-E 拒列（含 R17-E A4 零成本 try 证实），位点未变 | R10-E..R18-E §2 |
| `auto-loop` 三路拼接 / persistSignals filter 双求值 / 顺序读 / optimizedPolicy filter+map / candidatesFor().find | ns~µs 级或 I/O 支配 | 换名重提拒列：R12-E 行 13 / S1-E-4 / S1-E-5 / S2-E-6 / S1-D-2 / X0-3 逐字维持 | S1-E-4/5 / S2-E-6 / S1-D-2 / X0-3 |
| `bandit-store` / `readBanditFile` corrupt JSON catch；`parseLearnedRoutingPolicy` / `parseObservedSignal` 拒绝门族 | 每 run 至多一次，µs 级 | 数据面 fail-soft/fail-closed 契约本体；R17-E 异常轴价格表 + S6-E-3/4 / S2-E-4 / X0-5 全维持 | R17-E 新轴 A + S6-E-3/4 / X0-5 |
| I/O 契约面全体：fsync 丢弃、完整性重哈希跳过、no-change 跳写、双载入去重、corrupt registry fail-open、readdir 并行化、惰性载入、mkdir 提升、紧凑序列化 | 切片相关全部 ms 级余量所在 | 排除行逐字重现 + 硬不变量明令封死；`routingPolicyContent` pretty-print 参与 contentHash ⇒ 序列化变更破坏 CAS 契约 | X0-3 / S2-E-1/4 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1 / S2-I-1 / S9-G-1 族 + CAS |
| 测试专用面任何优化（`patterns` / `attribution` / `signatures` + 五个测试专用导出） | 不进任何生产路径 | 本轮交叉检索复核仍零生产调用方，自动拒列 | S1-E-7/8 / X2-6 族 |
| 死导出清理 / `scoreTaskResult` 尾部 `return 70` | 0ns 运行时 | 不立项：无收益路径即无候选资格；公开签名保留 | 硬不变量（公开签名） |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（十九轮）、src/test 自 R18-E 基线零漂移、调用位点行号全数复核一致（§1） | — |

**提议排除 ID：无（本轮无「新增排除 ID」表——空枚举未铸任何 S19-E-\*）。**
切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选。前十八轮
已逐类收口的十四个角度类之外，第十九遍以「输入表示 × 输入次序」双轴
普查新点名的全部维度（3 表示臂 × 2 视角 × 2 函数 + 3 尺寸摊平站 +
4+3 排列臂 + 1 对抗排列）全部给出实测价格与辖区/契约/算术封死；本切片
「无落地余量」证明自此覆盖 **CPU（R3-E..R14-E）+ 规模（R14-E）+ 分配
（R15-E）+ 协议（R15-E）+ 累积状态（R16-E）+ 载荷形态（R16-E）+ 异常面
（R17-E）+ 构成面（R17-E）+ 执行层级（R18-E）+ 字符宽度（R18-E）+
输入表示（本轮）+ 输入次序（本轮）十二个维度**。

## 3. 逐文件收口（前十八轮收口之上的本轮新检查点）

| 文件 | 本轮第十九遍新视角检查 | 结论 |
| --- | --- | --- |
| `signals.ts` | **表示三臂首次定价**（collect 温态 rt/raw 0.986–0.993 同带、首触 1.170–1.250；`extractAssistant` rope 工厂的摊平税上界 \|≤4.9\|ns/char 符号不稳）；**排列首次定价**（交错税 0.40–0.61µs 6/6 同号，收割形态全封死）；两遍结构维持硬不变量；S1-E-1..3 / S2-E-5/7 / S4-E-1/3 / S6-E-1/2/5 / S7-E-1/2/4/5 / S8-E-2 / S9-E-2/3 维持 | 无候选 |
| `from-episode.ts` | outcomes 表示三臂定价（温态 rt 便宜 6–7% = TASK_RETRY 瘦形态效应，肥形态仅存在于夹具）；对抗排列实证 route-before-result 绑定契约（3→0）；绑定 Map 游标化判不等价；既有拒列全维持 | 无候选 |
| `learned-routing.ts` | apply/load 复测在带（256–312ns / 25.8–35.0 / 77.1–109.5µs）；apply 内部零改动（S13-B-1 边界 + R13-E #2/#3 不补铸）；S8-E-1 维持；本轮两轴不触及本文件结构 | 无候选 |
| `task-success.ts` | binding 五重 spread 在表示轴温态三臂同带下再证无未计价形状余量；S8-E-2/S9-E-3 已裁面维持 | 无候选 |
| `diagnostics.ts` | 本轮夹具 diagnoseGroups=2 全数走到分组路径（1.27–1.78µs 在带偏上 = 构成效应）；R14-E 双除法/filter().length 拒列维持 | 无候选 |
| `bandit-store.ts` | in-lock 构建复制体 1.0–1.1µs 在带；corrupt JSON fail-soft 维持契约本体裁决；fsync/重哈希/mkdir/序列化按令未触碰 | 无候选 |
| `auto-loop.ts` | 进入点编排面；表示轴证明其 ingest 面（JSON.parse 产物直进 collect）已是便宜侧；三路拼接/filter 双求值/顺序读维持既有拒列 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | test-only 面复核仍零生产调用方，自动拒列；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核（依赖 `pnpm install
--frozen-lockfile`，pnpm 10.17.1）：

```bash
# Node v22.22.2（nvm；engines >=22.19.0 合规）
pnpm gate        # typecheck ✓ + lint ✓ + test ✓ + build ✓
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.14.0 与 v22.22.2 均过）
```

锚点脚本（临时置于 `/tmp/r19e-anchor.mts`，未入库以遵守无赢家纪律；差异
面见附录 A）共 **16 次独立运行**（8 × v22.14.0 + 8 × v22.22.2），每次
3/3 sanity 检查全过，全部确定性 VERDICT 行跨 16 次运行**逐位一致**；另
**6 次 R17-E 种子比对运行**（cmp17 模式）构成 VERDICT 行 6/6 逐位一致且
与 R17-E 夹具行逐字相同。代表性一次运行（v22.14.0）：

```text
VERDICT fixtures: E=41 collected=14 routedE=41 outcomes=3 S=12 diagnoseGroups=2 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=14.1us outcomes=2.8us diagnose=1.28us bandit-build=1.0us | total in-slice CPU ~19.2us per run vs landing bar >=10000us (520x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=272ns/task | empty policy=113ns/task -> x10 tasks/run = 2.72us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=33.4us | realistic registry (baseline+1 candidate)=78.7us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

新轴脚本（`/tmp/r19e-repr.mts` 6 次运行、`/tmp/r19e-order.mts` 6 次运行，
两 Node 版本各半；sanity 10/10 与 7/7 全过、确定性 VERDICT 行逐位一致）
代表性输出：

```text
VERDICT repr fixtures: E=41 collected=14 routedE=41 outcomes=3 (all three arms output-equal)
BENCH A1 warm repr: collect raw=14.90us rt=14.72us rope=14.87us | outcomes raw=2.96us rt=2.75us rope=3.01us | rt/raw=0.986/0.928
BENCH A2 first-touch repr (F=300 fresh fixtures/arm): collect raw=28.58us rt=33.56us rope=30.77us | outcomes raw=8.43us rt=8.39us rope=11.75us | rt/raw=1.174/0.995
BENCH A3 flatten-tax len~128021: flat=992.9us rope=1150.5us | delta=157.6us (1.231ns/char)
VERDICT census collect(0xe19e01): CHILD_MESSAGE=12 JUDGE_DECISION=1 LEDGER_UPDATED=3 MODEL_ROUTED=12 PROJECT_DISCOVERED=1 RUN_STARTED=4 TASK_STATUS_CHANGED=4 USER_ANSWER=4
BENCH B1 collect orderings: original=14.20us routes-first=13.82us routes-last=13.80us noise-first=13.97us | spread=0.40us (2.8% of original)
VERDICT B2 adversarial ordering (results-before-routes): outcomes 3 -> 0 (route-before-result binding contract; order carries semantics, drop is correct-by-contract)
BENCH B2 outcomes orderings: original=2.73us noise-first=2.47us noise-last=2.40us | adversarial=0.34us (0 outcomes) | preserve-spread=0.33us
```

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round19-r19e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R18-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐形态有理由；第十九遍以「输入表示 ×
  输入次序」双轴普查新点名的全部维度落进实测定价封死区（温态三臂同带、
  首触 +4.9–7.8µs、交错税 ≤0.61µs、摊平税符号不稳且 T\* ≥7.0M 字符）、
  辖区外（事件解析器 G 切片 / V8 map-IC 引擎层）或既有排除确认；两条
  新轴本身均以「无候选」收口，未发现第十五个机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令封死
  的 I/O 契约面、once-per-run CLI 面或切片外辖区上；CPU 温态面即使
  清零也距门槛 486–525×）。
- 矩阵空洞：无（调用位点行号与 R8-E..R18-E 逐一相同；消费者集合与
  R18-E 逐一相同；配置态 live 面与载入面锚点全数复测在带内；双 Node
  版本交叉不可分辨；本轮带 19.0–20.6 由 6 次种子对调运行 + 类普查
  实证归因为构成效应——R17-E 种子在本 VM 复现 18.5–19.7，逐点落在
  R17-E 带内）。
- 表示假说（「十八轮锚点测错了字符串/对象表示」）：以三臂价格表反向
  关闭（温态 rt/raw 0.986–0.993 / 0.926–0.944——生产表示同带或更便宜；
  首触溢价 +17–25% 属每进程一次级别且距落地线 3 个量级；摊平税低于
  扫描斜率一个量级且符号跨尺寸翻转）。
- 次序假说（「排列维度藏着未测余量」）：以排列价关闭（保输出排列价差
  ≤0.61µs/run；对抗排列实证次序携带语义 = 不可优化面；「构成 × 次序」
  因子化完成，R17-E 价格表在排列维度上获得不变性证明）。
- S13-B-1 边界：`applyLearnedRouting` 本体零改动；重开条件
  （|avoid| ≈ 965–1,063，R16-E 量化）未触发、生产侧不可触发；R13-E
  三处无名微观未补铸未重开。
- 跨职权观察（非候选、非 ID）：(a) TASK_RETRY 事件在磁盘序列化时天然
  丢弃 present-but-undefined 的 `taskId`（JSON 语义），故生产面永远是
  瘦形态——测量夹具若手写肥形态会把 outcomes 高估 6–7%（本轮 A1 实测），
  后轮夹具生成建议过一次 JSON 往返以贴生产表示；仅记录。(b) 事件类
  块化的交错税（0.4–0.6µs）属输入统计特征，若未来 EventStore 支持按类
  投影读取（数据面变更，X0-2/G 辖区），collect 可免税——距落地线
  4 个量级，仅记录。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R18-E §6（含 R14-E 规模判据、R15-E
分配/GC/噪声地板判据、R16-E 累积状态与载荷形态判据、R17-E 异常面与
构成面判据、R18-E 执行层级与字符宽度判据），并新增两条可标定判据：
**(a) 表示面判据**——若切片开始在进程内自造大规模 rope（新增拼接热环）
且单事件文本逼近 ~7.0M 字符（按本轮 128K 档最坏摊平斜率折算 10ms），
或事件解析面改变使对象形态与本轮 rt 臂实测失配 ≥10×；**(b) 次序面
判据**——若生产事件流的类交错度使交错税较本轮基准（0.40–0.61µs）
恶化 ≥2 个量级（需 E 同步增长 ~10³ 级，届时 R14-E E\* 判据先触发）；
或任一 I/O 契约排除（X0-3 / S2-E-1/4 / S1-G-1 / S1-E-4/5 / S4-E-2 /
S5-E-5 / S6-E-3/4 / S8-E-1）被正式推翻，或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

主锚点脚本全文（`/tmp/r19e-anchor.mts`，`npx tsx /tmp/r19e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下四处替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R19-E；
`NOW = "2026-08-25T05:30:00.000Z"`；SECTION 1 的 seeds
`0xe19e01`（collect）/ `0xe19e02`（routed）/ `0xe19e04`（signals）——
拒绝采样：signals 首选 `0xe19e03` 被拒（diagnoseGroups=0），顺延
`0xe19e04` 通过（collected=14>3、outcomes=3>0、diagnoseGroups=2≥1）；
探针记录 seed→(collected,outcomes,diagnoseGroups)：01→(14,2,1)、
02→(17,3,1)、03→(23,3,0 拒)、04→(13,3,2)、05→(17,0,0)、06→(13,5,2)；
SECTION 2 的 `mkdtemp` 前缀 `r19e-fresh-`/`r19e-reg-` 与 detector
identity `r19e-anchor`。另加 CLI 开关 `probe`（打印候选种子构成三元组）
与 `cmp17`（置换为 R17-E 的 NOW（`02:30`）与 seeds `0xe17e01/02/03`、
前缀 `r19e-cmp17-`、identity `r19e-anchor-cmp17`，用于 R15-E 协议的
种子对调构成归因运行）。其余逐字同 R10-E 附录 A。

新轴 A 脚本（`/tmp/r19e-repr.mts`，`npx tsx /tmp/r19e-repr.mts`）：复用
同一 `mulberry32`/生成器（逐字镜像，seeds `0xe19e01/02`）。三臂构造：
rt = `events.map(e => JSON.parse(JSON.stringify(e)))`（逐事件往返镜像
EventStore 逐行 parse）；rope = 深遍历重建，字符串经
`s.slice(0,mid)+s.slice(mid)`（mid 由 seed 流 `0xe19ea1/a2` 决定）或
`(" "+s).slice(1)`（len<2）去驻留，对象按原键序重建。sanity：三臂
`JSON.stringify` 字节相等（rt-routed 例外——JSON 往返合法丢弃
present-but-undefined 的 `taskId`，改断言输出逐位相等）+ collect/
outcomes 输出逐位相等（createdAt 剥离）。A1 温态 reps 20000；A2 首触
F=300 新鲜夹具/臂（种子基 `0x19e100/0x19e200` 逐夹具递增，rope 种子流
`0xe19ea3/a4`），全部批次先于任何计时窗构建；A3 摊平站：UNIT=62 字符
lorem（避开词表关键词），`buildRope` 以随机块拼接**不封尾**（尾部
`.slice`、`JSON.stringify`、`===` 都会就地摊平——值相等断言用计时数组
外的一次性探针对；flat 臂由**同种子独立流**再造一次后 JSON 往返摊平，
不得从 rope 臂派生，初版两处伪影均已修正并记录于 §0）；每尺寸先做
6 对双臂同尺寸 JIT 预热再开计时窗；reps 200/100/50 @ 1K/16K/128K。
sanity 10 项。

新轴 B 脚本（`/tmp/r19e-order.mts`，`npx tsx /tmp/r19e-order.mts`）：
同一生成器；`stablePartition` 构造保相对次序的类块化排列。collect 四臂
（original / routes-first：{PROJECT_DISCOVERED,MODEL_ROUTED,噪声} 前置 /
routes-last：发射类前置 / noise-first：噪声前置），发射类
（CHILD_MESSAGE/USER_ANSWER/JUDGE_DECISION/RUN_FAILED）相对次序恒保持
——两遍结构下输出必逐位相等（断言 + 排列多重集守恒断言）；outcomes
三臂（original / noise-first / noise-last，被消费类
{MODEL_ROUTED,TASK_RETRY,CHILD_MESSAGE} 相对次序恒保持）+ 对抗臂
（CHILD_MESSAGE 全前置，断言 outcomes 只降不增并打印语义事实行）。
类普查 VERDICT 行输出两夹具的逐类计数（构成归因输入）。reps 20000。
sanity 7 项。

16 次主锚点运行原始区间（8 × v22.14.0 + 8 × v22.22.2）：

```text
SLICE-CPU 总量:
  v22.14.0: 19.9 / 19.3 / 20.6 / 19.2 / 19.2 / 19.0 / 19.2 / 19.6 us
  v22.22.2: 19.7 / 19.5 / 19.8 / 19.6 / 19.3 / 19.3 / 19.2 / 19.8 us
collect:    14.0-14.8 us（v22.14.0 14.0-14.8 / v22.22.2 14.2-14.6）
outcomes:   2.7-2.9 us
diagnose:   1.27-1.78 us（diagnoseGroups=2 构成，带偏上）
bandit:     1.0-1.1 us
apply(non): 256-312 ns/task（v22.14.0 256-312 / v22.22.2 260-277）
apply(空):  113-136 ns/task
load fresh: 25.8-35.0 us
load real:  77.1-109.5 us
VERDICT 行: 16/16 逐位一致；sanity 3/3 x16 全过
```

6 次 R17-E 种子比对运行（cmp17）原始值（3 × v22.14.0 + 3 × v22.22.2）：

```text
总量: 18.8 / 18.5 / 19.3 | 19.0 / 19.7 / 18.9 us —— 逐点落在 R17-E 带
  18.5-20.2 之内
collect 12.7-13.8 / outcomes 4.0-4.2 / diagnose 1.01-1.07 / bandit 0.7-0.8
  —— 与 R17-E 逐项同带（12.7-14.1 / 4.0-4.3 / 0.98-1.11 / 0.7-0.8）
VERDICT 行: E=41 collected=15 routedE=41 outcomes=4 S=12 diagnoseGroups=1
  prevArms=3 —— 6/6 逐位一致，与 R17-E 夹具行逐字相同
```

6 次表示轴运行原始区间（3 × v22.14.0 + 3 × v22.22.2）：

```text
A1 collect:  raw 14.90-15.97 / rt 14.70-15.86 / rope 14.80-16.03 us
             rt/raw 0.986-0.993
A1 outcomes: raw 2.89-2.98 / rt 2.68-2.82 / rope 2.95-3.08 us
             rt/raw 0.926-0.944（6/6 rt 更便宜）
A2 collect:  raw 28.0-29.0 / rt 33.5-36.2 / rope 28.7-30.8 us
             rt/raw 1.170-1.250（6/6 rt 更贵）
A2 outcomes: raw 8.00-8.60 / rt 7.35-8.39（rt/raw 0.918-0.995）/
             rope 10.97-12.27 us
A3 斜率（ns/char）: 1K +4.913/+3.143/+2.082/+4.138/-0.042/+3.043（5/6 正）
                    16K -0.552/-0.639/-0.507/-0.740/-1.647/-0.747（6/6 负）
                    128K +1.231/+1.058/+1.175/+1.184/+1.424/+1.362（6/6 正）
sanity 10/10 x6 全过；VERDICT 行逐位一致；两版本同带
```

6 次次序轴运行原始区间（3 × v22.14.0 + 3 × v22.22.2）：

```text
B1 original:     14.20 / 14.24 / 14.33 | 14.26 / 14.07 / 14.13 us（恒最慢臂）
B1 块化臂:       13.64-13.98 us（routes-first / routes-last / noise-first）
B1 spread:       0.40 / 0.60 / 0.57 | 0.59 / 0.61 / 0.52 us（2.8-4.3%，6/6 同号）
B2 original:     2.72-2.93 us | noise-first 2.41-2.47 / noise-last 2.40-2.80 us
B2 preserve-spread: 0.31-0.53 us
B2-adv:          0.32-0.34 us，outcomes 3->0（6/6）
census VERDICT 行: 6/6 逐位一致
sanity 7/7 x6 全过；两版本同带
```
