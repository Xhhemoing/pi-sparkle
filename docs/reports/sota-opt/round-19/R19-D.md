MODEL_SLUG=claude-fable-5-thinking-xhigh

# R19-D：`src/adaptation/` 第十九遍搜查报告（Round 1–18 同区第十九遍）

**战役:** 全库持久 SOTA 优化 Round 19 / R19-D（Round 1–18 同区第十九遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `3c4e7c5`（R18-J 收口、Round 18 关轮、R19-D 分派 tip；R19-A 已收口合入，R19-B/R19-C 在飞未触碰）
**分支:** `cursor/r19-d-adaptation-nineteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S19-D-\* ID，切片零代码
改动；整片预算收口第十九次复核成立，并关掉一条十八遍从未定价的
完备性轴——「冷进程模块图组成」：R13-D…R18-D 的全部测量都在
进程内进行（R13-D 把 eval 整命令拆 21 相位、R18-D 把变更命令定价成
整锁事务 2.67–2.94 / 2.38–2.60 ms、R17-D 把拒绝终端定价成族），但
每一条真实 `pi-sparkle adapt <sub>` 命令都是一个**全新 Node 进程**：
操作者感知的墙钟 = 引擎 boot + main.js 常驻图实例化 +（惰性）
adapt.js 子树实例化 + 进程内命令体，其中 boot+import 层在全部
十八份 D 报告中从未出现过价格。本轮实测：该层占 status 整命令墙钟
的 **~99%**（61.4–61.6 ms 墙钟 vs 0.61–0.80 ms 暖态命令体）、占
eval 的 **~94%**（72.9–74.2 ms vs 3.85–4.22 ms）——十八遍定价的是
操作者等待时间的 1–6%。本轮以确定性静态 import 闭包普查（dist/
产物、tsc 已擦除 type-only 边、动态 import 边构造性排除）+ 新进程
冷计时把该层逐边归因并双端关死：**

1. **逐边归因（普查确定性一致，三运行）**：main.js 常驻图 108 模块
   （其中 adaptation=8 常驻于**每一条** pi-sparkle 命令）；adapt.js
   闭包 72 模块中 **63 个已在常驻图内**，真实 dispatch 的边际集只有
   9 模块（实测 1.4–3.7 ms）；`cli/adapt.ts` 第 5 行的 I 属地静态边
   把 eval-routing 的 16 模块子树（routing=8 + experiments=5 +
   supervisor/graph/自身）压给 status / promote / rollback 这些**从不
   调用 eval 的命令**，边际税实测 **3.1–3.3 ms/非 eval 命令**；把 D
   子树钉进常驻图的钉边者恰两个 E 切片模块（`learning/auto-loop.js`
   + `learning/learned-routing.js`），drop-out 10 模块（adaptation=8 +
   promotion-rules 同步边携带的 comparison-report / self-review），
   边际常驻税实测 **0.3 ms/进程**——S11-G-1 的「可剪位在 D/I」指针
   本轮定价关死。
2. **整命令冷墙钟组成闭合**：引擎地板 17.2–18.6 ms（平台）+ 常驻
   import 层 34.2–35.5 ms（I 属地：S5-I-1 已落地、S6-I-1/S6-I-3 已裁
   亚感知）+ adapt.js 边际 1.4–3.7 ms（I 属地惰性 dispatch）+ 冷命令
   体（status 5.1–6.1 ms、eval 17.3–18.4 ms；对暖锚差 = JIT 冷税
   4.3–5.3 / 13.4–14.5 ms，引擎属地）。fidelity 断言同步通过（真实
   dist/cli/main.js dispatch：usage 文案、status 候选清单、eval 报告
   落盘逐项验证；S5-I-1 惰性 dispatch 在 dist 产物上复核成立——常驻
   闭包不含 cli/adapt.js）。
3. **天花板裁决（频率 × 单价 × 属地三端封死）**：D 可归因分量全档
   0.3 ms（常驻子树）+ 3.1–3.3 ms（eval-routing 边际子树，且站点在
   I 切片 adapt.ts）+ 若干 9 模块边际份额——**全额清零 ≤ ~4 ms/命令**，
   距落地线（数十–数百 ms）一个数量级；频率端 = 每操作者 CLI 命令
   恰一次 / auto-loop 每 run 进程恰一次 = 验收线明文拒绝的
   once-per-run CLI 类（跨区判例族：R17-A 2.7–8.5 / R17-F 16.8–22.0 /
   R17-J 4.0–4.8 / R18-E 2.23–2.50 ms once-per-process 全部不铸）；
   层内非 D 分量全部落在 I 属地（S5-I-1/S6-I-1/S6-I-3）、构建/平台面
   （S6-I-2 编译缓存、引擎 boot）或引擎属地（JIT 冷税，S7-H-3 预热
   净负判例）。**该轴收口，无新 ID。**

配置态 eval 地板同 VM 复锚（指令要求，未抄旧数）：**3.85–4.22 ms**
——与 R18-D 3.75–4.14 在 3.85–4.14 重叠、与 R17-D 3.57–4.00 在
3.85–4.00 重叠；历史带 3.1–4.2 内（上沿 4.22 与带顶齐平），零 diff
下带间位移是 VM/窗口方差而非代码效应。同夹具 op 锚全部与 R18-D
重叠：promote op 43.1–46.4 µs（R18-D 44.2–45.2）、rollback op
3.4–3.5 µs（R18-D 3.1–3.4）、hash32 9.7–16.1 µs（min 端与 R18-D
9.6–9.9 同带；run-1 上沿 16.1 为忙窗 µs 方差，同窗全组 µs 指标同向
上移）。清零切片内计算仍差落地线一个数量级——预算结论第十九次成立。

切片 14 个文件自 R1-D 基线（`82bef36`）经 R2-D … R18-D 至本轮基线
（`3c4e7c5`）**逐字节未变**（`git diff 82bef36..HEAD --
src/adaptation` 为空，十九遍全程零 diff；`ls` 复核恰 14 文件）。
调用面维持最强冻结并再延一轮：R18-D 基线（`3cd83f4`）以来
`git diff 3cd83f4..HEAD -- src/` **完全为空**（期间 10 个提交全部为
docs(sota-opt)），生产调用方经全库 grep 首手复核仍为
`cli/adapt.ts` / `learning/auto-loop.ts` / `learning/from-episode.ts` /
`learning/learned-routing.ts` 四处（`tracking/analysis.ts` 三个导入
均为 `import type`，零运行时边——本轮并经 dist 普查从产物侧交叉
验证：type-only 边已被 tsc 擦除，不进任何运行时闭包）；
`createAdaptationDriftMonitor` / `paretoFront` / `proposeCandidates` /
`mutateOnce` / `retireVersion` / `evaluateProposalShadow` 在 `src/`
生产面依旧零切片外调用方（monitor 格维持 N/A）。R1-D…R18-D 全部
测量与排除**全额转移**。全部测量在 engines 合规 Node **v22.22.2**
完成（与 R13-D…R18-D 同引擎，带可比），三次独立运行全绿
（`All checks passed.`）。

按令对排除表做换名拒绝审计：本轮逐文件重读与模块图轴中识别的全部
角度经逐条检查判定为既有 ID 的换名/同方案异站、切片外属地、平台/
引擎面或明文拒绝类（8 项新记录，§4；指令点名项逐条核过，§5），按
「宁可空枚举也不铸装饰性 ID」的裁决纪律**一律不铸新 ID**。R18-D
锁事务组成账目（promote 整事务 2.67–2.94 / rollback 2.38–2.60 /
锁 glue 199.6–239.1 µs；无隐藏段）、R17-D 拒绝路径普查（28 终端，
最贵 2.20–2.28 ms、稳态零流量）、R16-D payload 形态五轴
（L/H/O/σ/P）、R15-D 分配/GC（~4.4–4.5 MB/eval；GC 硬顶 37–43 µs）
与 A/A MDE（窗口中位 |263–435| µs）、R14-D 规模越线标定
（10 ms @ 17–18×）**未重铸、未重测、原样引用**——本轮轴测的是
**进程边界外**的层（boot+import vs 进程内命令体），与上述进程内轴
全部正交；R18-J 的 skip 路径普查与整命令事务组成账目**未移植**
（本轴既非 no-op 面清点也非进程内相位对账，定价的是那些进程内仪器
构造上看不见的模块实例化层，且点名了从未有价的 D 相关站点：
16 模块 eval 子树税、10 模块常驻 D 子树税、9 模块 dispatch 边际集）。
S12-D-1/2、S11-D-1/2、S10-D-1/2、S9-D-4、S8-D-1..3 族未以任何形态
重提。硬不变量全程满足（零 diff 天然保持）：CAS/凭据/数据面契约、
双 LCB 与双归因、proposal-first、阈值、公开签名、fsync 与完整性
重哈希全部原样；不声称 Outcome-supported，Checkpoint F-PROD 仍开放
（ADR-005）。第十九遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/adaptation/` 全目录，**恰 14 文件**（`ls` 复核）：
  eval-routing / promotion-rules / promotion / registry / mutate /
  pareto / candidate / rollback / reflection / approval-profile /
  resource / monitor / retirement / active-pointer，**全部实际重读**
  （不靠记忆，逐行对全排除表）。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表，含 S9-D-4、
  S12-D-1..2、S8-D-1..3、S5-I-1/S6-I-1..3、S11-G-1、S5-E-5/S5-H-2/
  S7-H-3 once-per-process 判例族）/ PROGRESS.md / round-19/PLAN.md /
  round-18/R18-D.md（空枚举；锁事务组成账目）/ round-17/R17-D.md
  （空枚举；拒绝路径普查）；R16-D / R15-D / R14-D 收口略读确认
  不重铸；R18-J（skip 路径 + 事务组成）通读确认本轴非其移植。
- 基线验证：从最新 `cursor/sota-persistent-opt-83a1`
  （tip = `3c4e7c5`，含 R18-J ingest 与 Round 18 关轮、R19-A ingest）
  切出工作分支；`git diff 82bef36..HEAD -- src/adaptation` **为空**
  （第十九轮字节恒等）；`git diff 3cd83f4..HEAD -- src/` **为空**
  （自 R18-D 基线以来 `src/` 生产面零字节变更，期间 10 个提交全部为
  docs(sota-opt)）。EXCLUSIONS.md / PROGRESS.md / PLAN.md 未触碰。
  未开 PR。未 force-push。R19-B / R19-C 在飞，未触碰其分支与报告文件。
- 邻居只读：`src/cli/main.ts`（惰性 dispatch 事实）、`src/cli/adapt.ts`
  （静态边事实：第 5 行 eval-routing 运行时导入）、`src/learning/`
  （learned-routing / auto-loop / from-episode 运行时边）、
  `src/tracking/analysis.ts`（type-only）、
  `src/privacy/record-classes.ts`（仅路径字符串）、`src/routing/`、
  `src/experiments/`、`src/review/self-review.ts`、
  `src/persist/file-lock.ts`。零编辑。dist/ 产物由 `pnpm gate` 的
  build 步生成（tsc 产物 = 生产忠实 JS），普查与冷计时只**读**
  dist/，零写入。
- 环境：本 VM 默认 Node v22.14.0（且 `/exec-daemon/node` 遮蔽 PATH，
  显式前置 nvm bin 目录后生效），按 engines（>=22.19.0）切至
  **v22.22.2**（与 R13-D…R18-D 同引擎）后
  `pnpm install --frozen-lockfile`（pnpm 10.17.1）；全部测量与门禁
  在 v22.22.2 完成。
- 硬不变量全程满足（本轮零 diff，天然满足）：分析不改 in-flight run、
  Tracking 无命令权、H/score 不写路由 PASS/FAIL、live = R0 等价
  （R1 不接线）、稀疏→已批 R0 基线、双 LCB（beta+normal）与双归因
  （logit+prob-add）保留（X0-11）、提升 proposal-first、无
  Outcome-supported 声称（Checkpoint F-PROD 开放，ADR-005）、阈值
  不动、CAS/凭据/数据面契约与公开签名不动、fail-closed 不 fail-open、
  fsync 不删（S8-D-3）、restore/getActiveContent 完整性重哈希不删
  （S8-D-1..2）、fail-open 编码禁区不碰（S9-D-4 / S12-D-1 廉价形态
  永不重开；`eval-routing.ts` 的 `key.toLowerCase()` 站点本轮首手
  复核仍无 ASCII 短路变体）、测试未改。
- 无赢家 ⇒ 零生产改动，普查/冷计时仿真只留在本报告附录，**不入库**
  （无 `scripts/round19-r19d-equivalence-sim.ts`）。

## 1. 配置态 × 命令类矩阵同 VM 复锚（指令要求；eval 地板重锚，未抄旧数）

三次独立运行（同脚本、seeds `0xd19d01`–`0xd19d02` 起），构造与
R8-D…R18-D 逐字段同构（配置态：9.2 KB 路由策略 40 avoid + 40 prefer +
7 assignments；8 个 prompt 身份、5 promote、1 pending、2 rollback、
2 retire；registry.json 146.2 KB、V=15 C=9 L=11 B=18、blobs 119.5 KB）。

| 矩阵格 | 默认态 | 配置态 | 与 R18-D（同引擎 v22.22.2）比对 |
| --- | --- | --- | --- |
| load | 0.10–0.13 ms | 0.61–0.80 ms | R18-D 0.57–0.74，重叠 |
| save(+fsync) | 0.36–0.77 ms | 1.39–1.59 ms | R18-D 1.47–1.82 于 1.47–1.59 重叠；fsync 方差支配 |
| eval 端到端（E=200） | 4.39–4.63 ms | **3.85–4.22 ms** | R18-D 3.75–4.14 于 3.85–4.14 重叠；R17-D 3.57–4.00 于 3.85–4.00 重叠——历史带 3.1–4.2 内（上沿齐平）。零 diff 下带间位移是 VM/窗口方差而非代码效应 |
| promote（op-only，K=60） | — | 43.1–46.4 µs | R18-D 44.2–45.2 重叠（hash32 单次 9.7–16.1 µs：min 端与 R18-D 9.6–9.9 同带，run-1 上沿 16.1 为忙窗方差——同窗 promote/rollback/hash 三 µs 指标同向上移） |
| rollback（user CAS，op-only） | — | 3.4–3.5 µs | R18-D 3.1–3.4，重叠 |
| monitor | N/A | N/A | 仍无生产调用方（grep 首手复核） |

**预算结论复核成立**：eval 地板 ~3.9–4.2 ms、save/fsync 支配持久化
路径的图景第十九次成立。R14-D 的实测越线标定、R15-D 的分配/GC/MDE、
R16-D 的形态五轴、R17-D 的拒绝路径普查、R18-D 的锁事务账目不重测、
原样引用（§5 / §8）。

### 1b. `evaluateProposalShadow` 交叉核对（experiments 切片属地）

P=200/A=100 → 1.3–1.4 ms；P=2000/A=1000 → 120.4–126.7 ms。与 R18-D
（1.3 / 120.6–128.6 ms）同带；`reflection.ts` 只是薄委托，实现在
`src/experiments/`（切片外），维持锚点性质，不据此铸 ID。

### 1c. 调用图第十九次复核（只读邻居 grep 事实 + dist 产物交叉验证）

- `loadAdaptationRegistry(OrNew)` / `saveAdaptationRegistry` /
  `evalRoutingPolicy` / `promoteWithRegistry` /
  `withAdaptationRegistryLock`：生产调用方仍恰为 `cli/adapt.ts`、
  `learning/auto-loop.ts`、`learning/from-episode.ts`、
  `learning/learned-routing.ts` 四处——单命令单事务模式第十七次
  复验成立。
- `tracking/analysis.ts` 对本切片的三个导入均为 `import type`——
  本轮新增**产物侧交叉验证**：dist 普查显示 tracking 的 9 个常驻
  模块无一持有进入 adaptation 的运行时边（type-only 擦除被 tsc emit
  直接见证）。
- `createAdaptationDriftMonitor` / `paretoFront` / `proposeCandidates` /
  `mutateOnce` / `retireVersion` / `evaluateProposalShadow`：`src/`
  生产面零切片外调用方——monitor/pareto/mutate/retirement/reflection
  五模块维持「无生产调用方」格局（R4-D 事实第十六次复验）。与之
  一致：五模块**均不出现在任何运行时闭包**（promotion 17 模块 /
  eval-routing 38 模块 / adapt 72 模块 / main 108 模块普查均不含
  monitor.js / pareto.js / mutate.js / retirement.js；reflection.js
  仅被 adapt.js 直边携带）。
- 常驻链事实（本轴新点名）：`cli/main.js →
  learning/learned-routing.js → adaptation/promotion.js`——D 子树
  常驻于每条 pi-sparkle 命令的钉边者恰两个 E 切片模块
  （learned-routing 运行时导入 `hashCandidateContent` +
  `loadAdaptationRegistry`；auto-loop 同理），见 §2c。

## 2. 新轴：冷进程模块图组成（boot+import 层首次定价 + 逐边归因）

方法：十八遍全部在进程内测量——R13-D 拆 eval 21 相位、R18-D 把
promote/rollback 定价成整锁事务、R17-D 普查拒绝终端——但真实
`pi-sparkle adapt <sub>` 是**全新 Node 进程**，操作者感知墙钟 =
引擎 boot + 常驻 main.js 图 +（惰性）adapt.js 子树 + 进程内命令体，
boot+import 层在 D 报告史上零出现（grep 首手复核：D 系报告无
cold import / 模块图定价记录；跨区冷进程判例族 R17-A/F/J、R18-E/I
均为各自切片属地数字，无一覆盖本切片的模块边）。本轮四段仪器：
(2a) dist/ 静态 import 闭包普查（确定性 BFS，tsc 已擦 type-only 边，
动态 `import()` 构造性排除——S5-I-1 惰性 dispatch 因此在产物侧
可见）；(2b) 各根冷 import 计时（每根 12 个新进程）；(2c) 两条边际
税隔离（eval-routing 子树税 + 常驻 D 子树税）；(2d) 整命令冷墙钟
与组成闭合。判读标尺沿用 R15-D 实测 MDE（窗口中位 0.26–0.44 ms）
与验收线（数十–数百 ms 稳态；once-per-run CLI 类明文拒绝）。

### 2a. 静态闭包普查（确定性，三运行逐字节一致）

| 根 | 模块数 | 切片构成（top） | 结构事实 |
| --- | --- | --- | --- |
| `adaptation/promotion.js`（status/promote/rollback 依赖根） | 17 | adaptation=8 domain=5 experiments=1 review=1 persist=1 privacy=1 | experiments/comparison-report + review/self-review 由 promotion-rules **同步验证器边**携带（X0-4 钉死，§4-3）；不含 eval-routing.js / routing/assign.js（type-only 边已擦除） |
| `adaptation/eval-routing.js`（eval 根） | 38 | routing=10 adaptation=9 domain=7 experiments=6 … | eval 侧重子树本体（routing/assign + experiments replay 族） |
| `cli/adapt.js`（I 切片命令模块） | 72 | domain=14 routing=13 adaptation=9 learning=7 … | **63/72 已在常驻图内**——dispatch 真实边际集只有 9 模块（experiments=5 cli=1 learning=1 routing=1 adaptation=1） |
| `cli/main.js`（常驻图） | 108 | run=17 routing=17 domain=14 tracking=9 **adaptation=8** … | 不含 cli/adapt.js（**S5-I-1 惰性 dispatch 在 dist 产物上复核成立**）；D 子树常驻于每条 pi-sparkle 命令 |
| eval-routing-边独占集（`cli/adapt.ts` L5 静态边的代价） | 16 | routing=8 experiments=5 adaptation=1 supervisor=1 graph=1 | status / promote / rollback 为一个**从不调用**的子树付的实例化税；16 模块中 15 个在切片外 |

### 2b. 各根冷 import（12 新进程/根，min/med，三运行带）

| 根 | min | med |
| --- | --- | --- |
| promotion.js | 9.5–9.9 ms | 9.8–10.9 ms |
| eval-routing.js | 14.0–14.3 ms | 14.3–14.6 ms |
| cli/adapt.js | 23.0–23.5 ms | 23.5–23.9 ms |
| cli/main.js | 32.4–33.1 ms | 33.2–33.7 ms |

（孤立冷 import 是上界口径：promotion 的 9.5–10.9 ms 里 domain/
persist 等共享依赖任何命令都要付；诚实的 D 边际见 2c。）

### 2c. 两条边际税隔离（本轴核心新信息，三运行带）

| 边际税 | 单价 | 频率 | 钉名（消除路径逐条撞死） |
| --- | --- | --- | --- |
| eval-routing 子树税（先预热 adapt.js 其余全部静态依赖根，再计时 eval-routing 边际 import = 2a 的 16 模块独占集实例化） | **3.1–3.3 ms** | 恰一次/非 eval adapt 命令 | 边站点 = `cli/adapt.ts` 第 5 行静态导入（I 切片属地；S5-I-1 落地族、S6-I-1 残余边惰性化已裁 -2.2~-4.6 ms 亚感知、S6-I-3 全拆分已裁）；D 自有替代（evalRoutingPolicy 体内动态 import）= S5-E-5 同判例（切片内惰性 import 独占增量 2.8–3.0 ms 不铸）+ once-per-command 明文拒绝类 + fail-late 形变（加载期失败面移成首调用失败面） |
| 常驻 D 子树税（普查全部跨界入边：钉边者恰 `learning/auto-loop.js` + `learning/learned-routing.js` 两个 E 模块；skip 全部入边后 drop-out = 10 模块 [adaptation=8 experiments=1 review=1]；预热常驻图其余 95 模块后计时 drop-out 实例化） | **0.3 ms** | 恰一次/**每条** pi-sparkle 命令进程 | **S11-G-1「可剪位在 D/I」指针本轮定价关死**：剪 D 侧省 0.3 ms once-per-process；钉边者属 E 切片（R18-E 冷层 2.23–2.50 ms 已档 learning 侧一并覆盖其边）；once-per-process 明文拒绝类（S5-H-2 2.2–2.4 ms 同类判例） |

### 2d. 整命令冷墙钟与组成闭合（每格新进程，min/med，三运行带）

| 命令 | min | med | 组成 |
| --- | --- | --- | --- |
| 引擎地板（node -e ''） | 15.5–16.3 ms | 17.2–18.6 ms | 平台面（Node 二进制 boot），不可提案 |
| 裸 dispatch（main.js usage） | 49.7–50.9 ms | 52.6–54.1 ms | 常驻 import 层 = med 差 **34.2–35.5 ms**（I 属地；S5-I-1 已落地形态的余量，S6-I-1/S6-I-3 已裁亚感知） |
| adapt usage（dispatch + adapt.js import，无命令体） | 53.7–54.2 ms | 55.5–56.3 ms | adapt.js 真实边际 import = **1.4–3.7 ms**（9 模块；I 属地惰性 dispatch 的每命令价格） |
| adapt status（配置态） | 58.9–60.6 ms | **61.4–61.6 ms** | 冷命令体 5.1–6.1 ms（暖锚 0.61–0.80 → **JIT 冷税 4.3–5.3 ms**，引擎属地）；**boot+import+JIT 层 ≈ 墙钟的 99%** |
| adapt eval（E=200，配置态） | 69.5–70.4 ms | **72.9–74.2 ms** | 冷命令体 17.3–18.4 ms（暖锚 3.85–4.22 → **JIT 冷税 13.4–14.5 ms**，引擎属地）；**层 ≈ 墙钟的 94%** |

fidelity：整命令走真实 `dist/cli/main.js` dispatch，首样本逐项断言
（usage 文案含 `pi-sparkle adapt`、status 输出候选清单、eval 报告
`.json` 落盘且 exit 0）；普查侧断言 promotion 闭包不含
eval-routing.js / routing/assign.js、含 comparison-report、eval 独占
集含 routing/assign.js、常驻图不含 cli/adapt.js——三运行全过。

### 2e. 天花板裁决（频率 × 单价 × 属地三端封死）

- **频率端**：adapt 无稳态热环（操作者显式 CLI；auto-loop 每 run
  进程恰付一次层价）⇒ 全轴恰为验收线明文拒绝的 once-per-run /
  once-per-process CLI 类；跨区判例族已五次同判（R17-A 2.7–8.5、
  R17-F 16.8–22.0、R17-J 4.0–4.8、R18-E 2.23–2.50 ms、R18-I
  providers 冷 import 31.0–31.7 ms——无一铸 ID）。
- **单价端（D 可归因）**：常驻 D 子树 0.3 ms + eval-routing 子树
  3.1–3.3 ms（站点还在 I）+ 9 模块 dispatch 边际的 D 份额（≤1/9 ×
  1.4–3.7 ms）——**全额清零 ≤ ~4 ms/命令**，距落地线一个数量级。
- **属地端（非 D 分量）**：引擎地板 17.2–18.6 ms = 平台；常驻层
  34.2–35.5 ms = I 属地（S5-I-1 已落地 + S6-I-1/S6-I-3 已裁）；编译
  缓存路径 = S6-I-2 已裁（上界 -8 ms，构建面冻结）；JIT 冷税
  4.3–14.5 ms = 引擎属地 + S7-H-3 预热净负判例。**该轴频率、单价、
  属地三端封死，无新缝隙。**

## 3. 候选总表

**空。** 本轮真实新增候选为零：模块图轴的每一 ms 都钉在切片外属地
（I/E/平台/引擎）、既有判例（S5-I-1/S6-I-1..3/S11-G-1/S5-E-5/
S5-H-2/S7-H-3）或明文拒绝类（once-per-run CLI）上（§2 逐段归因）；
逐行重读在十八遍裁决之外未发现任何未点名站点（§4 换名检查）。按
PLAN「先复核再找新角度，不要硬凑」与指令「宁可空枚举也不铸装饰性
ID」，不铸任何 S19-D-\*。

## 4. 枚举路径：识别但按更名/重现规则不铸 ID 的角度（8 项新记录）

R13-D §4 11 项、R14-D §4 8 项、R15-D §4 8 项、R16-D §4 8 项、
R17-D §4 8 项、R18-D §4 8 项换名检查未重铸（未以任何形态重提）。
本轮逐文件重读与模块图轴中新识别、判为切片外属地/既有判例/明文
拒绝类、**未铸 ID** 的角度：

| 本轮识别的落点 | 命中的闭合条目 | 处置 |
| --- | --- | --- |
| 惰性化 `cli/adapt.ts` L5 → eval-routing.js 静态边（非 eval 子命令免付 16 模块子树 3.1–3.3 ms） | 站点 = I 切片属地（S5-I-1 落地族；S6-I-1 残余边惰性化已裁 -2.2~-4.6 ms；S6-I-3 全拆已裁亚感知一次性类）+ once-per-command 明文拒绝类 | 不铸 ID（I 轮属地，且量级不达其重开阈值） |
| `evalRoutingPolicy` 体内动态 import 重子树（D 自有站点，async 签名容纳） | **S5-E-5 同判例**（切片内惰性 import 独占增量 2.8–3.0 ms 不铸）+ once-per-command 类 + fail-late 形变（模块加载失败从进程启动移至首调用——fail-closed 时序弱化邻域） | 不铸 ID |
| 惰性化 promotion-rules → self-review / comparison-report 同步边 | `assertCanPromoteFromReview` / `validateComparisonReport` 是 promote 门链**同步验证器**——惰性化需公开签名 async 化 = **X0-4**；且 §2c 实测整个 drop-out（含这两模块）0.3 ms | 死路 |
| 削减常驻 D 足迹（10 模块 0.3 ms once-per-process） | **S11-G-1「可剪位在 D/I」指针本轮定价关死**；钉边者 = E 切片 auto-loop/learned-routing（R18-E 冷层已档 learning 侧）；S5-H-2 once-per-process 类 | 不铸 ID |
| 常驻 I 层再压缩（34.2–35.5 ms） | I 切片属地：S5-I-1 已落地（本轮 dist 复核）、S6-I-1（残余 7 边 -2.2~-4.6 ms）与 S6-I-3（全拆）已裁 | 切片外，不铸 ID |
| 编译缓存 / 快照（杀常驻层与 JIT 税） | **S6-I-2 本体**（enableCompileCache 上界 -8 ms；ESM 整图先编译后求值）+ 构建/平台面冻结 | 不铸 ID |
| JIT 冷税预热干预（status 4.3–5.3 / eval 13.4–14.5 ms） | 引擎属地 + **S7-H-3**（模块加载期预热三次净负）+ once-per-process 拒绝类 | 不铸 ID |
| 常驻进程 / daemon 化摊销 boot（CLI 变 client-server） | 架构/公开面变更（新契约面 + 锁与单命令单事务模型重写 = X0-4 族 + 数据面契约）；非切片内优化 | 不可提案域 |

逐行重读中再次识别（前轮已有属地，本轮零新增维度，仅记录复核）：
`assertNoForbiddenFields` 的 `key.toLowerCase()` = S9-D-4/S12-D-1
禁区零触碰；`preparePromotion` pendingByIntent 线性扫描 = S1-D-8 /
S1-D-2 族 ns 类；eval 侧 outputRoot 每命令 mkdir = R12-D 已裁。以上
之外，结构位 × 消除机制矩阵的剩余格全部为空。

## 5. 更名/重现检查（指令点名项逐条）

| 点名项 | 本轮是否触碰 | 检查结论 |
| --- | --- | --- |
| S9-D-4 廉价 `toLowerCase` 跳过（U+212A fail-open） | 否 | 未重提任何折叠变体；站点首手复核仍为全量 `key.toLowerCase()`，零 diff 字节恒等（永不重开） |
| S12-D-1 边界闸 JSON.parse 预探针（廉价形态同族） | 否 | createCandidate 站点未重提 |
| R18-D 锁事务组成账目（promote 2.67–2.94 / rollback 2.38–2.60 ms / 锁 glue 199.6–239.1 µs；无隐藏段） | 否（不重铸/不重测） | 本轮轴定价**进程边界外**的层；K=30 整事务矩阵未重跑；进程内锚仅按令做 S0c/S0d 复锚（§1），未复算账目闭合 |
| R17-D fail-closed 拒绝路径普查（28 终端；最贵 2.20–2.28 ms；稳态零流量） | 否（不重铸/不重测） | 本轮只测成功路径整命令墙钟；boot+import 层先于任何拒绝终端发生，正交；28 终端裁决原样引用 |
| R16-D payload 形态轴（L/H/O/σ/P） | 否（不重铸/不重测） | 本轴 fixture 固定生产形态（E=200、9.2 KB 策略、配置态注册表）；穿越点数字原样引用 |
| R15-D 分配/GC（4.4–4.5 MB/eval；GC 硬顶 37–43 µs）与 A/A MDE（263–435 µs） | 否（不重铸/不重测） | MDE 原样引用为判读标尺 |
| R14-D 规模越线标定（10 ms @ 17–18×；S6-D-5 50 ms @ 101–108×） | 否（不重铸/不重测） | §8 原样引用；本轴固定 E=200 |
| R18-J skip 路径 / 命令组成轴（指令：勿移植除非点名从未有价的 D 站点） | 未移植 | 本轴非 no-op 面清点亦非进程内相位对账；其进程内仪器构造上看不见模块实例化层；本轴点名并首次定价三个 D 相关站点（16 模块 eval 子树税 / 10 模块常驻 D 子树税 / 9 模块 dispatch 边际集） |
| S8-D-1 / S8-D-2 完整性重哈希 | 否 | load 锚原样含重哈希（0.61–0.80 ms 内含）；未提任何折叠 |
| S8-D-3 saveAdaptationRegistry drop-fsync | 否 | save 锚原样含 fsync（1.39–1.59 ms）；未提任何折叠 |
| S5-D-4 全 UNOBSERVED 前置短路 / S2-D-4 / S10-D-2 屏障 | 否 | eval 链未重提；本轴不触 eval 内部结构 |
| S1-D-7 / S7-D-1 promote 门链 | 否 | op 锚 43.1–46.4 µs 即门链按规格工作的价格（hash32×3 内含）；未提任何折叠 |
| S11-D-2 装载链融合 / S3-D-1 ledger 拷贝 / S5-D-3 快照 | 否 | 分别在 load 锚 / op 锚已裁域引用，未重提 |
| R10-D…R18-D 空枚举收口 | 否 | 未回填任何 S13-D-\* … S18-D-\* |
| X\* / S1-\* … S18-\* 全表（含 S13-B-1） | 否 | 逐文件重读对表复核，零重开 |

## 6. 前后对比

无。零生产改动；切片十九遍逐字节不变（`82bef36 == 3c4e7c5` on
`src/adaptation/`）。本分支仅含本报告一个文件。

## 7. 测试与门禁

零代码改动下相关套件与全量门禁复核，全绿（Node v22.22.2，pnpm 10.17.1）：

```bash
npx tsx --test "test/unit/adaptation/**/*.test.ts" "test/acceptance/adaptive-loop.test.ts"
# tests 101 / suites 11 / pass 101 / fail 0
pnpm gate   # typecheck && lint && test && build — 全绿（exit 0；tests 1169 / pass 1168 / skipped 1 / fail 0）
```

仿真（临时脚本 `/tmp/r19d-sim.mts`，未入库——无赢家不落死代码；完整
源码见附录，seeds `0xd19d01`–`0xd19d02` 起，三次独立运行全部
`All checks passed.`，普查与整命令 fidelity 断言逐项通过）：

```text
run 1（摘录）:
S0c configured: load=0.75 save=1.39 | eval e2e=3.94ms
S0d: promote op=43.1us rollback op=3.4us hash32=16.1us
S1b cold: promotion 9.5/10.0 eval-routing 14.3/14.5 adapt 23.2/23.5 main 32.6/33.2
S1c eval-routing subtree tax=3.1/3.3ms | S1c2 resident D-subtree=0.3ms (10 modules)
S1d walls: engine 16.3/18.6 bare 50.2/54.1 adapt-usage 53.7/55.5 status 60.6/61.6 eval 70.4/73.9
S1d comp: resident=35.5 adapt-marginal=1.4 status-cold-body=6.1 (JIT 5.3) eval-cold-body=18.4 (JIT 14.5)

run 2（摘录）:
S0c: load=0.80 save=1.59 | eval=4.22ms
S0d: 46.4us / 3.5us / 10.7us
S1b: 9.9/10.9 14.3/14.6 23.5/23.9 33.1/33.7
S1c=3.1/3.2ms | S1c2=0.3ms
S1d walls: 15.5/17.2 50.9/52.6 54.2/56.3 59.2/61.4 69.7/74.2
S1d comp: resident=35.4 adapt-marginal=3.7 status-cold-body=5.1 (JIT 4.3) eval-cold-body=17.9 (JIT 13.7)

run 3（摘录）:
S0c: load=0.61 save=1.59 | eval=3.85ms
S0d: 44.9us / 3.4us / 9.7us
S1b: 9.5/9.8 14.0/14.3 23.0/23.5 32.4/33.2
S1c=3.1/3.2ms | S1c2=0.3ms
S1d walls: 15.6/18.3 49.7/52.6 53.8/55.6 58.9/61.6 69.5/72.9
S1d comp: resident=34.2 adapt-marginal=3.1 status-cold-body=5.9 (JIT 5.3) eval-cold-body=17.3 (JIT 13.4)
```

## 8. 新增排除 ID

**无。** 空枚举（与 R13-D / R14-D / R15-D / R16-D / R17-D / R18-D
同口径）：本轮识别的全部角度均为切片外属地/既有判例/明文拒绝类
（§4），不铸装饰性 ID。

重开条件：模块图轴仅在三种情形下需要重测——(a) adapt 命令获得稳态
热环调用方（结构上当前不存在：操作者显式 CLI + auto-loop 每 run
进程一次）；(b) `cli/adapt.ts` / `cli/main.ts` 的静态边结构变更或
S5-I-1/S6-I-1..3 任何一条按其自身条款重开（I 切片属地，届时先由 I
轮裁决——本轮的 16 模块子树税 3.1–3.3 ms 与常驻层 34.2–35.5 ms 数字
可直接作 I 轮输入）；(c) 切片新增静态重依赖（例如 promotion-rules
引入新的切片外同步验证器边），使 §2c 的 0.3 ms 常驻税或 2a 闭包
构成显著变化。成功路径的既有重开阈值不变：E 跨量级增长时首先重开
S6-D-5（R14-D 裁定）；P-eval ≈ 249–259 KB、O ≈ 2,646–2,733 字符、
H ≈ 4,486–4,684 字符、P-promote ≈ 2,615–2,698 KB（R16-D 裁定）；
拒绝路径两端封死（R17-D 裁定）；锁事务账目无隐藏段（R18-D 裁定）；
L/σ 轴 schema/构造封闭不变。

## 附录：确定性仿真脚本（完整，可复现）

临时路径 `/tmp/r19d-sim.mts`，`npx tsx /tmp/r19d-sim.mts` 运行
（仓库根目录，依赖已装、`pnpm build` 产物在 dist/，Node ≥22.19）；
未入库（无赢家）。seeds `0xd19d01`–`0xd19d02` 起；三次独立运行全部
`All checks passed.`。

```typescript
/**
 * R19-D deterministic anchor + cold-process module-graph composition
 * simulation (nineteenth pass).
 * (1) Re-anchors the configured-state x command-class matrix closed by
 *     R9-D..R18-D on THIS VM (engines-compliant Node >=22.19.0, v22.22.2 --
 *     same engine as R13-D..R18-D so all bands are directly comparable).
 * (2) NEW AXIS: cold-process module-graph composition. Eighteen passes priced
 *     this slice IN-PROCESS: R13-D phase-decomposed the eval command, R18-D
 *     priced the mutation command classes as whole locked transactions
 *     (2.67-2.94 ms promote / 2.38-2.60 ms rollback), R17-D priced every
 *     rejection terminal. But every real `pi-sparkle adapt <sub>` command is
 *     a FRESH Node process: operator-perceived wall time =
 *       engine boot + resident main.js graph + (lazy) adapt.js subtree
 *       instantiation + in-process command body,
 *     and the boot/import layer has NEVER appeared in any D report. This
 *     axis prices it and attributes every module edge:
 *       2a: static import-closure census over dist/ (deterministic BFS on
 *           tsc-emitted static import/export-from edges; dynamic import()
 *           edges excluded by construction). Roots: adaptation/promotion.js
 *           (status/promote/rollback dependency root), adaptation/
 *           eval-routing.js (eval root), cli/adapt.js (I-slice command
 *           module), cli/main.js (resident graph). Also computes the exact
 *           module set reachable ONLY via the I-owned static edge
 *           cli/adapt.ts -> adaptation/eval-routing.js (what promote /
 *           rollback / status pay for the eval subtree they never call).
 *       2b: cold import timing per root in fresh processes (K spawns each).
 *       2c: marginal eval-routing subtree tax: fresh process first imports
 *           every OTHER static dependency of cli/adapt.js (promotion,
 *           rollback, from-episode, auto-loop, primary-catalog, event-store,
 *           discovery, domain), then times the marginal import of
 *           eval-routing.js -- the exact instantiation cost the I-owned
 *           static edge adds to non-eval adapt commands.
 *       2d: whole-command cold wall: engine floor (node -e ''), bare
 *           dist/cli/main.js dispatch, `adapt status` and `adapt eval` on a
 *           configured-state fixture; composition closure against the
 *           in-process anchors from Section 0.
 *     Frequency end: once per operator CLI command (adapt has no steady
 *     loop; auto-loop pays the import once per run process). Unit-price end:
 *     every elimination path lands on I-slice property (cli/adapt.ts
 *     per-subcommand lazy import; S5-I-1 / S6-I-1 / S6-I-3 territory),
 *     sync-signature contract (X0-4: promotion-rules -> comparison-report /
 *     self-review edges feed sync validators), build/platform plane
 *     (S6-I-2 compile cache; engine boot floor), or the once-per-process
 *     rejection family (S5-E-5 / S5-H-2 / S7-H-3).
 * Seeded PRNG (mulberry32) -> reproducible. Seeds: 0xd19d01 - 0xd19d02.
 * Run: npx tsx /tmp/r19d-sim.mts   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import { execFile } from "node:child_process";
import { mkdir, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { readFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { dirname, join, resolve } from "node:path";
import { promisify } from "node:util";
import { ResourceRegistry } from "/workspace/src/adaptation/registry.js";
import {
  adaptationRegistryPath,
  loadAdaptationRegistry,
  promoteWithRegistry,
  saveAdaptationRegistry,
  type ChangeNote,
  type PromoteInput,
  type PromotionReview
} from "/workspace/src/adaptation/promotion.js";
import {
  evalRoutingPolicy,
  parseRoutingEvalReport
} from "/workspace/src/adaptation/eval-routing.js";
import { evaluateProposalShadow } from "/workspace/src/adaptation/reflection.js";
import {
  hashCandidateContent,
  type EvaluationPlan,
  type ImprovementCandidate
} from "/workspace/src/adaptation/candidate.js";
import type { AuthorIdentity, ResourceIdentity, ResourceVersion } from "/workspace/src/adaptation/resource.js";
import {
  createProjectId,
  type CandidateId,
  type IdGenerator,
  type ResourceVersionId
} from "/workspace/src/domain/ids.js";
import type { IsoTimestamp } from "/workspace/src/domain/timestamp.js";
import type { ExperimentPlan } from "/workspace/src/experiments/plan.js";
import type { ExperimentOutcome } from "/workspace/src/experiments/shadow.js";

const execFileAsync = promisify(execFile);

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

const NOW = "2026-08-25T12:00:00.000Z" as IsoTimestamp;
const HUMAN: AuthorIdentity = { kind: "human", identity: "operator" };
const EVAL_PLAN: EvaluationPlan = { stages: ["static", "replay"], metrics: ["utility"], planVersion: 1 };

function sequentialIds(prefix: string): IdGenerator {
  let n = 0;
  return () => {
    n += 1;
    return `${prefix}${String(n).padStart(4, "0")}`;
  };
}

const BASELINE_POLICY = JSON.stringify({ primaryModelId: "premium", avoid: [], prefer: [] });
const CANDIDATE_POLICY = JSON.stringify({
  primaryModelId: "premium",
  avoid: [{ modelId: "fast", family: "edit", reason: "deterministic FAIL on edit replay" }],
  prefer: [{ family: "test", modelId: "fast" }]
});

/* Configured-state policies (R7-I lesson; bit-identical generator to
 * R8-D..R18-D at k=1). */
function configuredPolicy(variantTag: string, k = 1): string {
  const avoid = Array.from({ length: 40 * k }, (_, i) => ({
    modelId: i % 8 === 0 ? "cheap" : `m${i % 7}`,
    family: i % 3 === 0 ? pick(() => (i % 10) / 10, ["edit", "test", "review"]) : `fam${i % 12}`,
    reason: `deterministic FAIL pattern ${i} (${variantTag}): attributable replay regression observed across paired episodes with stable family-level attribution`
  }));
  const prefer = Array.from({ length: 40 * k }, (_, i) => ({
    family: i % 4 === 0 ? "test" : `fam${i % 12}`,
    modelId: i % 4 === 0 ? "cheap" : `m${(i + 1) % 7}`
  }));
  const assignments = Array.from({ length: 7 }, (_, i) => ({
    role: ["planner", "executor", "tester", "reviewer", "scout", "debugger", "worker"][i] as string,
    model: i % 2 === 0 ? "premium" : "cheap",
    family: ["edit", "test", "review"][i % 3] as string
  }));
  if (variantTag === "candidate") {
    avoid.push({ modelId: "cheap", family: "refactor", reason: "candidate-only: deterministic FAIL on refactor replay (new attribution)" });
  }
  return JSON.stringify({ primaryModelId: "premium", avoid, prefer, assignments });
}
const CONFIGURED_BASELINE_POLICY = configuredPolicy("baseline");
const CONFIGURED_CANDIDATE_POLICY = configuredPolicy("candidate");

const ROLES = ["worker", "scout", "planner", "implementer", "reviewer", "tester", "debugger"] as const;
const OBJECTIVES = [
  "Fix the failing unit test in the adapter and rerun the suite",
  "Refactor the retry helper to remove duplicated backoff logic",
  "Review the migration PR for schema drift and unsafe defaults",
  "Investigate why the nightly benchmark regressed on large inputs",
  "Plan the rollout of the new caching layer across services"
];

function mkReview(candidateId: CandidateId, contentHash: string, tag: string): PromotionReview {
  return {
    reviewId: `rv-${tag}`,
    candidateId,
    contentHash,
    verdict: "approved",
    reviewerKind: "independent",
    reviewerId: "critic-gate",
    actorId: HUMAN.identity,
    evidenceRefs: [`review:${tag}`]
  };
}

function mkNote(scope: string, rollbackVersionId: ResourceVersionId): ChangeNote {
  return {
    scope,
    evidence: ["static"],
    guardrails: ["proposal-first"],
    rollbackVersionId
  };
}

function buildRoutingRegistry(
  tag: string,
  baselinePolicy = BASELINE_POLICY,
  candidatePolicy = CANDIDATE_POLICY
): { registry: ResourceRegistry; candidateId: CandidateId; baseline: ResourceVersion; identity: ResourceIdentity } {
  const registry = new ResourceRegistry({ now: () => NOW, generateId: sequentialIds(tag) });
  const identity: ResourceIdentity = {
    kind: "routing-policy",
    name: "learned-routing",
    scope: { kind: "project", projectId: createProjectId(() => `${tag}proj`) }
  };
  const baseline = registry.registerBaseline({ identity, content: baselinePolicy, author: HUMAN });
  const candidate = registry.createCandidate({
    identity,
    content: candidatePolicy,
    parentVersionId: baseline.versionId,
    author: HUMAN,
    evaluationPlan: EVAL_PLAN
  });
  return { registry, candidateId: candidate.candidateId, baseline, identity };
}

/* Configured-state registry (same construction as R8-D..R18-D). */
function buildConfiguredRegistry(
  tag: string,
  baselinePolicy = CONFIGURED_BASELINE_POLICY,
  candidatePolicy = CONFIGURED_CANDIDATE_POLICY
): {
  registry: ResourceRegistry;
  candidateId: CandidateId;
  baseline: ResourceVersion;
  identity: ResourceIdentity;
} {
  const built = buildRoutingRegistry(tag, baselinePolicy, candidatePolicy);
  const { registry } = built;
  const versions: { identity: ResourceIdentity; v1: ResourceVersion; v2?: ResourceVersion }[] = [];
  for (let i = 0; i < 8; i += 1) {
    const identity: ResourceIdentity = {
      kind: "prompt",
      name: `agent-prompt-${i}`,
      scope: { kind: "project", projectId: createProjectId(() => `${tag}proj`) }
    };
    const body = `# prompt ${i}\n${`Follow the review checklist, cite evidence for every claim, and keep the diff bounded to the stated scope (${i}).\n`.repeat(30 + i * 8)}`;
    const v1 = registry.registerBaseline({ identity, content: body, author: HUMAN });
    const candidate = registry.createCandidate({
      identity,
      content: `${body}\n## amendment ${i}\nAdd an explicit check for the attributable failure.\n`,
      parentVersionId: v1.versionId,
      author: HUMAN,
      evaluationPlan: EVAL_PLAN
    });
    const entry: { identity: ResourceIdentity; v1: ResourceVersion; v2?: ResourceVersion } = { identity, v1 };
    if (i < 5) {
      const result = promoteWithRegistry(registry, {
        candidateId: candidate.candidateId,
        expectedCurrentVersionId: v1.versionId,
        content: `${body}\n## amendment ${i}\nAdd an explicit check for the attributable failure.\n`,
        approvedBy: HUMAN,
        review: mkReview(candidate.candidateId, candidate.contentHash, `${tag}p${i}`),
        changeNote: mkNote(`prompt:${tag}${i}`, v1.versionId),
        explicitApproval: true
      });
      entry.v2 = result.newVersion as ResourceVersion;
    } else if (i === 5) {
      registry.beginPromotion({
        candidateId: candidate.candidateId,
        expectedCurrentVersionId: v1.versionId,
        content: `${body}\n## amendment ${i}\nAdd an explicit check for the attributable failure.\n`,
        approvedBy: HUMAN,
        review: mkReview(candidate.candidateId, candidate.contentHash, `${tag}p${i}`),
        changeNote: mkNote(`prompt:${tag}${i}`, v1.versionId),
        explicitApproval: true
      });
    }
    versions.push(entry);
  }
  const first = versions[0] as { identity: ResourceIdentity; v1: ResourceVersion; v2?: ResourceVersion };
  registry.rollback({
    identity: first.identity,
    expectedCurrentVersionId: (first.v2 as ResourceVersion).versionId,
    targetVersionId: first.v1.versionId,
    reason: "degradation",
    evidence: ["ev:configured-degradation"],
    automatic: false
  });
  const second = versions[1] as { identity: ResourceIdentity; v1: ResourceVersion; v2?: ResourceVersion };
  registry.rollback({
    identity: second.identity,
    expectedCurrentVersionId: (second.v2 as ResourceVersion).versionId,
    targetVersionId: second.v1.versionId,
    reason: "user",
    evidence: ["ev:configured-user"],
    automatic: false
  });
  registry.retire((versions[2] as { v1: ResourceVersion }).v1.versionId);
  registry.retire((versions[3] as { v1: ResourceVersion }).v1.versionId);
  return built;
}

async function writeDataset(dir: string, episodes: number, rng: () => number): Promise<void> {
  await mkdir(dir, { recursive: true });
  const eps = Array.from({ length: episodes }, (_, i) => ({
    episodeHash: `eh_${i}`,
    taskId: `tsk_e${i}`,
    role: pick(rng, ROLES),
    objective: pick(rng, OBJECTIVES),
    originalWorkspace: "/repos/alpha",
    ...(rng() < 0.7 ? { taskSuccess: rng() < 0.5 ? "PASS" : "FAIL" } : {}),
    ...(rng() < 0.5 ? { taskFamily: pick(rng, ["edit", "test", "review"]) } : {})
  }));
  const manifest = { datasetId: "ds-r19d", environmentVersion: "env-1", episodes: eps };
  await writeFile(join(dir, "manifest.json"), `${JSON.stringify(manifest, null, 2)}\n`, "utf8");
}

console.log(`node ${process.version}`);

/* ============================================================
 * Section 0: whole-slice budget re-verification, default-state anchors.
 * ============================================================ */
const workRoot = await mkdtemp(join(tmpdir(), "r19d-sim-"));
let configuredEvalReport: ReturnType<typeof parseRoutingEvalReport> | undefined;
{
  const rng = mulberry32(0xd19d01);
  const stateRoot = join(workRoot, "state");
  const { registry } = buildRoutingRegistry("b0");
  await saveAdaptationRegistry(stateRoot, registry);
  const datasetDir = join(workRoot, "dataset");
  await writeDataset(datasetDir, 200, rng);

  const loadMs = await benchAsync(async () => {
    await loadAdaptationRegistry(stateRoot);
  }, 30);
  const saveMs = await benchAsync(async () => {
    await saveAdaptationRegistry(stateRoot, registry);
  }, 30);

  const { registry: r2, candidateId } = buildRoutingRegistry("b1");
  await saveAdaptationRegistry(stateRoot, r2);
  const evalMs = await benchAsync(async () => {
    await evalRoutingPolicy({ stateRoot, candidateId, datasetDir });
  }, 20);
  console.log(
    `S0 default anchors: registry load=${loadMs.toFixed(2)}ms save(+fsync)=${saveMs.toFixed(2)}ms | adapt-eval end-to-end (E=200, toy policy)=${evalMs.toFixed(2)}ms per invocation`
  );
}

/* ============================================================
 * Section 0b: evaluateProposalShadow cross-check (experiments-slice owned).
 * ============================================================ */
{
  const mkPlan = (p: number, a: number): ExperimentPlan => ({
    planVersion: 1,
    experimentId: "exp_r19d-shadow",
    mode: "shadow",
    baselineVersionId: "rsv_r19d0001" as ResourceVersionId,
    candidateId: "cnd_r19d0002" as CandidateId,
    population: Array.from({ length: p }, (_, i) => `eh_${i}`),
    metrics: ["utility"],
    thresholds: { maxGuardrailBreaches: 1000000, maxCostUsd: 1e9 },
    budget: { maxAssignments: a + 1, maxWallClockMs: 1000000000 },
    randomization: { seed: 7 },
    stopPolicy: { onGuardrail: "halt", onBudgetExhausted: "halt" },
    missingOutcomePolicy: "exclude"
  });
  const mkOutcomes = (a: number): ExperimentOutcome[] =>
    Array.from({ length: a }, (_, i) => ({
      episodeHash: `eh_${i}`,
      utility: i % 2,
      costUsd: 0.001,
      guardrailBreached: false
    }));
  for (const [p, a, reps] of [
    [200, 100, 20],
    [2000, 1000, 3]
  ] as const) {
    const plan = mkPlan(p, a);
    const outcomes = mkOutcomes(a);
    const state = evaluateProposalShadow(plan, outcomes, 0);
    check(`S0b shadow run completes without halt (P=${p},A=${a})`, !state.halted && state.outcomes.length === a);
    const ms = bench(() => {
      evaluateProposalShadow(plan, outcomes, 0);
    }, reps);
    console.log(
      `S0b evaluateProposalShadow cross-check: P=${p} A=${a} -> ${ms.toFixed(1)}ms per call (experiments-slice owned; anchor only)`
    );
  }
}

/* ============================================================
 * Section 0c: configured-state anchors (R8-D..R18-D replica on this VM).
 * ============================================================ */
let cfgLoadMs = 0;
let cfgSaveMs = 0;
let cfgEvalMs = 0;
let cfgStateRoot = "";
let cfgCandidateId: CandidateId | undefined;
let cfgDatasetDir = "";
{
  const rng = mulberry32(0xd19d02);
  const stateRoot = join(workRoot, "state-configured");
  const { registry, candidateId } = buildConfiguredRegistry("c0");
  await saveAdaptationRegistry(stateRoot, registry);
  const snapshot = registry.snapshot();
  const contents = snapshot.contents ?? [];
  const totalBlobBytes = contents.reduce((sum, blob) => sum + blob.content.length, 0);
  const datasetDir = join(workRoot, "dataset-configured");
  await writeDataset(datasetDir, 200, rng);
  cfgStateRoot = stateRoot;
  cfgCandidateId = candidateId;
  cfgDatasetDir = datasetDir;

  cfgLoadMs = await benchAsync(async () => {
    await loadAdaptationRegistry(stateRoot);
  }, 30);
  cfgSaveMs = await benchAsync(async () => {
    await saveAdaptationRegistry(stateRoot, registry);
  }, 30);
  cfgEvalMs = await benchAsync(async () => {
    await evalRoutingPolicy({ stateRoot, candidateId, datasetDir });
  }, 20);
  const result = await evalRoutingPolicy({ stateRoot, candidateId, datasetDir });
  configuredEvalReport = parseRoutingEvalReport(result.report);
  const raw = await readFile(adaptationRegistryPath(stateRoot), "utf8");
  console.log(
    `S0c configured anchors (registry.json=${(raw.length / 1024).toFixed(1)}KB, V=${snapshot.versions.length} C=${snapshot.candidates.length} L=${snapshot.ledger.length} B=${contents.length} blobs=${(totalBlobBytes / 1024).toFixed(1)}KB, policy=${(CONFIGURED_CANDIDATE_POLICY.length / 1024).toFixed(1)}KB): load=${cfgLoadMs.toFixed(2)}ms save(+fsync)=${cfgSaveMs.toFixed(2)}ms | adapt-eval end-to-end (E=200, configured policy)=${cfgEvalMs.toFixed(2)}ms per invocation`
  );
}

/* ============================================================
 * Section 0d: configured-state promote and rollback OP-ONLY costs.
 * ============================================================ */
{
  const report = configuredEvalReport;
  check("S0d setup: configured eval report available", report !== undefined);
  const K = 60;
  const promoteFixtures = Array.from({ length: K }, (_, i) => {
    const { registry, candidateId, baseline } = buildConfiguredRegistry(`pm${i}x`);
    const candidate = registry.getCandidate(candidateId) as ImprovementCandidate;
    const input: PromoteInput = {
      candidateId,
      expectedCurrentVersionId: baseline.versionId,
      content: CONFIGURED_CANDIDATE_POLICY,
      approvedBy: HUMAN,
      review: mkReview(candidateId, candidate.contentHash, `pm${i}`),
      changeNote: mkNote(`routing:pm${i}`, baseline.versionId),
      explicitApproval: true,
      evalReport: {
        ...(report as NonNullable<typeof report>),
        candidateId,
        contentHash: candidate.contentHash
      }
    };
    return { registry, input };
  });
  const t0 = performance.now();
  const promoted: { registry: ResourceRegistry; newVersion: ResourceVersion; identity: ResourceIdentity; baselineId: ResourceVersionId }[] = [];
  for (const fixture of promoteFixtures) {
    const result = promoteWithRegistry(fixture.registry, fixture.input);
    promoted.push({
      registry: fixture.registry,
      newVersion: result.newVersion as ResourceVersion,
      identity: (result.newVersion as ResourceVersion).identity,
      baselineId: fixture.input.expectedCurrentVersionId
    });
  }
  const promoteOpUs = ((performance.now() - t0) / K) * 1e3;
  check("S0d all configured promotes succeeded", promoted.length === K && promoted.every((p) => p.newVersion !== undefined));

  const t1 = performance.now();
  for (const p of promoted) {
    p.registry.rollback({
      identity: p.identity,
      expectedCurrentVersionId: p.newVersion.versionId,
      targetVersionId: p.baselineId,
      reason: "user",
      evidence: ["ev:r19d-op-anchor"],
      automatic: false
    });
  }
  const rollbackOpUs = ((performance.now() - t1) / K) * 1e3;
  const hashUs = bench(() => {
    hashCandidateContent(CONFIGURED_CANDIDATE_POLICY);
  }, 3000) * 1e3;
  console.log(
    `S0d configured op-only anchors (K=${K}): promote op=${promoteOpUs.toFixed(1)}us | rollback(user CAS) op=${rollbackOpUs.toFixed(1)}us | single hash32 over the ${(CONFIGURED_CANDIDATE_POLICY.length / 1024).toFixed(1)}KB policy=${hashUs.toFixed(1)}us (x3 on the promote gate chain)`
  );
}

/* ============================================================
 * Section 1 (NEW AXIS): cold-process module-graph composition.
 * ============================================================ */
const DIST = resolve("/workspace/dist");

/* 1a: deterministic static import-closure census over dist/.
 * tsc emit keeps static `import ... from "spec"` / `export ... from "spec"`
 * statements and erases type-only imports, so the census IS the runtime
 * instantiation graph; dynamic `await import(...)` is not matched, so lazy
 * edges (e.g. main.ts -> cli/adapt.js, S5-I-1) are excluded by construction. */
const STATIC_EDGE_RE = /^(?:import|export)\s[^;]*?from\s*["']([^"']+)["']|^import\s*["']([^"']+)["']/gm;

function staticClosure(rootRel: string, skipEdges?: ReadonlySet<string>): {
  modules: Set<string>;
  externals: Set<string>;
} {
  const modules = new Set<string>();
  const externals = new Set<string>();
  const queue: string[] = [resolve(DIST, rootRel)];
  while (queue.length > 0) {
    const file = queue.pop() as string;
    if (modules.has(file)) continue;
    modules.add(file);
    const source = readFileSync(file, "utf8");
    for (const match of source.matchAll(STATIC_EDGE_RE)) {
      const spec = (match[1] ?? match[2]) as string;
      if (!spec.startsWith("./") && !spec.startsWith("../")) {
        externals.add(spec);
        continue;
      }
      const target = resolve(dirname(file), spec);
      if (skipEdges?.has(`${file}=>${target}`)) continue;
      if (!modules.has(target)) queue.push(target);
    }
  }
  return { modules, externals };
}

function bySlice(modules: ReadonlySet<string>): Map<string, number> {
  const counts = new Map<string, number>();
  for (const file of modules) {
    const rel = file.slice(DIST.length + 1);
    const top = rel.split("/")[0] as string;
    counts.set(top, (counts.get(top) ?? 0) + 1);
  }
  return counts;
}

function fmtSlices(counts: Map<string, number>): string {
  return Array.from(counts.entries())
    .sort((a, b) => b[1] - a[1])
    .map(([slice, count]) => `${slice}=${count}`)
    .join(" ");
}

/** BFS with parent tracking; returns one shortest static-import chain
 *  root -> ... -> target, or undefined when target is unreachable. */
function importChain(rootRel: string, targetRel: string): string[] | undefined {
  const root = resolve(DIST, rootRel);
  const target = resolve(DIST, targetRel);
  const parent = new Map<string, string | undefined>([[root, undefined]]);
  const queue: string[] = [root];
  let head = 0;
  while (head < queue.length) {
    const file = queue[head] as string;
    head += 1;
    if (file === target) {
      const chain: string[] = [];
      let cursor: string | undefined = file;
      while (cursor !== undefined) {
        chain.unshift(cursor.slice(DIST.length + 1));
        cursor = parent.get(cursor);
      }
      return chain;
    }
    const source = readFileSync(file, "utf8");
    for (const match of source.matchAll(STATIC_EDGE_RE)) {
      const spec = (match[1] ?? match[2]) as string;
      if (!spec.startsWith("./") && !spec.startsWith("../")) continue;
      const next = resolve(dirname(file), spec);
      if (!parent.has(next)) {
        parent.set(next, file);
        queue.push(next);
      }
    }
  }
  return undefined;
}

const promotionClosure = staticClosure("adaptation/promotion.js");
const evalRoutingClosure = staticClosure("adaptation/eval-routing.js");
const adaptClosure = staticClosure("cli/adapt.js");
const mainClosure = staticClosure("cli/main.js");

// Exact module set that non-eval adapt commands pay ONLY because of the
// I-owned static edge cli/adapt.ts -> adaptation/eval-routing.js.
const adaptWithoutEvalEdge = staticClosure(
  "cli/adapt.js",
  new Set([`${resolve(DIST, "cli/adapt.js")}=>${resolve(DIST, "adaptation/eval-routing.js")}`])
);
const evalOnlyModules = new Set(
  Array.from(adaptClosure.modules).filter((file) => !adaptWithoutEvalEdge.modules.has(file))
);

check("S1a promotion closure excludes eval-routing (type-only edge erased)",
  !promotionClosure.modules.has(resolve(DIST, "adaptation/eval-routing.js")));
check("S1a promotion closure excludes routing/assign",
  !promotionClosure.modules.has(resolve(DIST, "routing/assign.js")));
check("S1a promotion closure includes experiments/comparison-report (promotion-rules sync edge)",
  promotionClosure.modules.has(resolve(DIST, "experiments/comparison-report.js")));
check("S1a eval-only set contains routing/assign",
  evalOnlyModules.has(resolve(DIST, "routing/assign.js")));
check("S1a main resident graph excludes cli/adapt.js (S5-I-1 lazy dispatch)",
  !mainClosure.modules.has(resolve(DIST, "cli/adapt.js")));

// Marginal set the (lazy) adapt dispatch instantiates beyond the resident
// graph, and one shortest resident chain into the D slice.
const adaptMarginalModules = new Set(
  Array.from(adaptClosure.modules).filter((file) => !mainClosure.modules.has(file))
);
const residentAdaptationChain = importChain("cli/main.js", "adaptation/promotion.js");

console.log(`S1a static closure census (dist/, runtime edges only):`);
console.log(`S1a   adaptation/promotion.js  -> ${promotionClosure.modules.size} modules [${fmtSlices(bySlice(promotionClosure.modules))}]`);
console.log(`S1a   adaptation/eval-routing.js -> ${evalRoutingClosure.modules.size} modules [${fmtSlices(bySlice(evalRoutingClosure.modules))}]`);
console.log(`S1a   cli/adapt.js              -> ${adaptClosure.modules.size} modules [${fmtSlices(bySlice(adaptClosure.modules))}]`);
console.log(`S1a   cli/main.js (resident)    -> ${mainClosure.modules.size} modules [${fmtSlices(bySlice(mainClosure.modules))}]`);
console.log(`S1a   eval-routing-edge-only set (paid by status/promote/rollback for a subtree they never call; I-owned edge cli/adapt.ts): ${evalOnlyModules.size} modules [${fmtSlices(evalOnlyModules.size > 0 ? bySlice(evalOnlyModules) : new Map())}]`);
console.log(`S1a   adapt-dispatch marginal set (adapt closure minus resident graph): ${adaptMarginalModules.size} modules [${fmtSlices(bySlice(adaptMarginalModules))}]`);
console.log(`S1a   resident chain into the D slice: ${residentAdaptationChain === undefined ? "(none)" : residentAdaptationChain.join(" -> ")}`);

/* 1b: cold import timing per root (K fresh processes each; min + median). */
async function coldImport(rootRel: string, reps: number): Promise<{ min: number; med: number }> {
  const script = `const t0=performance.now();await import(${JSON.stringify(join(DIST, rootRel))});console.log((performance.now()-t0).toFixed(3));`;
  const samples: number[] = [];
  for (let i = 0; i < reps; i += 1) {
    const { stdout } = await execFileAsync(process.execPath, ["--input-type=module", "-e", script]);
    samples.push(Number(stdout.trim()));
  }
  samples.sort((a, b) => a - b);
  return { min: samples[0] as number, med: samples[Math.floor(samples.length / 2)] as number };
}

const promotionCold = await coldImport("adaptation/promotion.js", 12);
const evalRoutingCold = await coldImport("adaptation/eval-routing.js", 12);
const adaptCold = await coldImport("cli/adapt.js", 12);
const mainCold = await coldImport("cli/main.js", 12);
console.log(`S1b cold import per root (12 fresh processes each, min/median ms):`);
console.log(`S1b   promotion.js  min=${promotionCold.min.toFixed(1)} med=${promotionCold.med.toFixed(1)}`);
console.log(`S1b   eval-routing.js min=${evalRoutingCold.min.toFixed(1)} med=${evalRoutingCold.med.toFixed(1)}`);
console.log(`S1b   cli/adapt.js  min=${adaptCold.min.toFixed(1)} med=${adaptCold.med.toFixed(1)}`);
console.log(`S1b   cli/main.js   min=${mainCold.min.toFixed(1)} med=${mainCold.med.toFixed(1)}`);

/* 1c: marginal eval-routing subtree tax on non-eval adapt commands.
 * Fresh process: import every OTHER static dependency root of cli/adapt.ts
 * first (warm shared deps), then time the marginal import of
 * eval-routing.js = exactly the eval-only module set from 1a. */
{
  const others = [
    "adaptation/promotion.js",
    "adaptation/rollback.js",
    "learning/from-episode.js",
    "learning/auto-loop.js",
    "routing/primary-catalog.js",
    "run/event-store.js",
    "project/discovery.js",
    "domain/errors.js",
    "domain/ids.js"
  ].map((rel) => join(DIST, rel));
  const script = `
for (const other of ${JSON.stringify(others)}) { await import(other); }
const t0 = performance.now();
await import(${JSON.stringify(join(DIST, "adaptation/eval-routing.js"))});
console.log((performance.now() - t0).toFixed(3));
`;
  const samples: number[] = [];
  for (let i = 0; i < 12; i += 1) {
    const { stdout } = await execFileAsync(process.execPath, ["--input-type=module", "-e", script]);
    samples.push(Number(stdout.trim()));
  }
  samples.sort((a, b) => a - b);
  console.log(
    `S1c marginal eval-routing subtree tax (deps of the other five subcommands pre-warmed; ${evalOnlyModules.size} modules instantiate): min=${(samples[0] as number).toFixed(1)}ms med=${(samples[Math.floor(samples.length / 2)] as number).toFixed(1)}ms per non-eval adapt command`
  );
}

/* 1c2: marginal resident tax of the D subtree inside the REAL main.js graph.
 * S11-G-1 closed slice-internal edge lazification with "prunable site is in
 * D/I"; this prices that pointer. Static: closure of main.js with the E-owned
 * edge learning/learned-routing.js -> adaptation/promotion.js skipped; the
 * drop-out set is what the D subtree exclusively adds to EVERY pi-sparkle
 * process. Dynamic: fresh process pre-warms the skip-closure (minus main.js
 * itself -- top-level dispatch side effects -- and minus any module with a
 * static edge into the drop-out set, so nothing pulls it in early), then
 * times the marginal import of promotion.js. */
{
  const mainJs = resolve(DIST, "cli/main.js");
  const promotionJs = resolve(DIST, "adaptation/promotion.js");
  const adaptationDir = `${resolve(DIST, "adaptation")}/`;
  // All static edges inside main's resident graph that cross FROM a
  // non-adaptation module INTO the adaptation slice (each is an edge-holder
  // that pins the D subtree resident).
  const crossEdges = new Set<string>();
  const edgeHolders = new Set<string>();
  for (const file of mainClosure.modules) {
    if (file.startsWith(adaptationDir)) continue;
    const source = readFileSync(file, "utf8");
    for (const match of source.matchAll(STATIC_EDGE_RE)) {
      const spec = (match[1] ?? match[2]) as string;
      if (!spec.startsWith("./") && !spec.startsWith("../")) continue;
      const target = resolve(dirname(file), spec);
      if (target.startsWith(adaptationDir)) {
        crossEdges.add(`${file}=>${target}`);
        edgeHolders.add(file);
      }
    }
  }
  const mainWithoutDSubtree = staticClosure("cli/main.js", crossEdges);
  const residentDOnly = new Set(
    Array.from(mainClosure.modules).filter((file) => !mainWithoutDSubtree.modules.has(file))
  );
  check("S1c2 drop-out set non-empty and contains promotion.js", residentDOnly.has(promotionJs));
  const prewarm = Array.from(mainWithoutDSubtree.modules).filter(
    (file) => file !== mainJs && !edgeHolders.has(file)
  );
  const dropoutList = Array.from(residentDOnly);
  const script = `
for (const m of ${JSON.stringify(prewarm)}) { await import(m); }
const t0 = performance.now();
for (const m of ${JSON.stringify(dropoutList)}) { await import(m); }
console.log((performance.now() - t0).toFixed(3));
`;
  const samples: number[] = [];
  for (let i = 0; i < 12; i += 1) {
    const { stdout } = await execFileAsync(process.execPath, ["--input-type=module", "-e", script]);
    samples.push(Number(stdout.trim()));
  }
  samples.sort((a, b) => a - b);
  const holderNames = Array.from(edgeHolders)
    .map((file) => file.slice(DIST.length + 1))
    .sort()
    .join(", ");
  console.log(
    `S1c2 marginal resident D-subtree tax (S11-G-1 "prunable site in D/I" pointer priced; edge-holders pinning the D subtree resident: ${holderNames}; drop-out=${residentDOnly.size} modules [${fmtSlices(bySlice(residentDOnly))}], ${prewarm.length} resident modules pre-warmed): min=${(samples[0] as number).toFixed(1)}ms med=${(samples[Math.floor(samples.length / 2)] as number).toFixed(1)}ms per pi-sparkle process`
  );
}

/* 1d: whole-command cold wall (engine floor / bare dispatch / adapt status /
 * adapt eval) + composition closure against in-process anchors. */
async function coldCommand(
  args: readonly string[],
  reps: number,
  expect: (stdout: string, stderr: string, code: number) => boolean
): Promise<{ min: number; med: number }> {
  const samples: number[] = [];
  for (let i = 0; i < reps; i += 1) {
    const t0 = performance.now();
    let stdout = "";
    let stderr = "";
    let code = 0;
    try {
      const result = await execFileAsync(process.execPath, args as string[]);
      stdout = result.stdout;
      stderr = result.stderr;
    } catch (error) {
      const failed = error as { stdout?: string; stderr?: string; code?: number };
      stdout = failed.stdout ?? "";
      stderr = failed.stderr ?? "";
      code = failed.code ?? 1;
    }
    samples.push(performance.now() - t0);
    if (i === 0) {
      check(`S1d fidelity ${args.slice(1).join(" ") || "(engine floor)"}`, expect(stdout, stderr, code), `code=${code} stderr=${stderr.slice(0, 120)}`);
    }
  }
  samples.sort((a, b) => a - b);
  return { min: samples[0] as number, med: samples[Math.floor(samples.length / 2)] as number };
}

{
  const engine = await coldCommand(["-e", ""], 12, (_o, _e, code) => code === 0);
  const bare = await coldCommand([join(DIST, "cli/main.js")], 8, (_o, _e, code) => code !== 0 || true);
  const adaptUsage = await coldCommand(
    [join(DIST, "cli/main.js"), "adapt"],
    8,
    (stdout) => stdout.includes("pi-sparkle adapt")
  );
  const status = await coldCommand(
    [join(DIST, "cli/main.js"), "adapt", "status", "--state-root", cfgStateRoot],
    8,
    (stdout, _e, code) => code === 0 && stdout.includes("proposed candidates:")
  );
  const evalCmd = await coldCommand(
    [
      join(DIST, "cli/main.js"),
      "adapt",
      "eval",
      "--candidate",
      String(cfgCandidateId),
      "--dataset",
      cfgDatasetDir,
      "--state-root",
      cfgStateRoot
    ],
    8,
    (stdout, _e, code) => code === 0 && stdout.includes(".json")
  );
  console.log(`S1d whole-command cold wall (fresh process each, min/median ms):`);
  console.log(`S1d   engine floor (node -e '')        min=${engine.min.toFixed(1)} med=${engine.med.toFixed(1)}`);
  console.log(`S1d   bare dispatch (main.js, usage)   min=${bare.min.toFixed(1)} med=${bare.med.toFixed(1)}`);
  console.log(`S1d   adapt usage (dispatch + adapt.js import, no body) min=${adaptUsage.min.toFixed(1)} med=${adaptUsage.med.toFixed(1)}`);
  console.log(`S1d   adapt status (configured state)  min=${status.min.toFixed(1)} med=${status.med.toFixed(1)}`);
  console.log(`S1d   adapt eval   (E=200, configured) min=${evalCmd.min.toFixed(1)} med=${evalCmd.med.toFixed(1)}`);
  const residentLayer = bare.med - engine.med;
  const adaptMarginalImport = adaptUsage.med - bare.med;
  const statusColdBody = status.med - adaptUsage.med;
  const evalColdBody = evalCmd.med - adaptUsage.med;
  console.log(
    `S1d   composition: resident import layer = med(bare) - med(engine) = ${residentLayer.toFixed(1)}ms | adapt.js marginal import (via real dispatch) = med(adapt usage) - med(bare) = ${adaptMarginalImport.toFixed(1)}ms | status cold body = ${statusColdBody.toFixed(1)}ms (warm anchor ${cfgLoadMs.toFixed(2)}ms -> JIT-cold tax ${(statusColdBody - cfgLoadMs).toFixed(1)}ms) | eval cold body = ${evalColdBody.toFixed(1)}ms (warm anchor ${cfgEvalMs.toFixed(2)}ms -> JIT-cold tax ${(evalColdBody - cfgEvalMs).toFixed(1)}ms) -- the boot+import layer dominates every adapt command wall and was never priced in eighteen passes`
  );
}

await rm(workRoot, { recursive: true, force: true });
if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nAll checks passed.");
```

MORE_OPTIMA=no
BRANCH=cursor/r19-d-adaptation-nineteenth-pass-83a1
