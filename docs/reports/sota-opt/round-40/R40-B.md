MODEL_SLUG=claude-fable-5-thinking-xhigh

# R40-B：live 路由切片 Round 40 四十搜报告

**战役:** 全库持久 SOTA 优化 Round 40 / R40-B（十区之一，R1-B…R39-B 的第四十遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `ecbf3ab`（派单 tip，R39-J 摄取 + Round 39 收口 + Round 40 开卷 + 本波派单）。测量窗口内 exclusive **一次 docs-only 快进** `ecbf3ab → 46686fc`（R40-A 摄取 + R40-D 派单）：段核对 `git diff ecbf3ab..46686fc -- src/ test/ pnpm-lock.yaml package.json` = **0 行**（触碰仅 PROGRESS.md / round-40 PLAN.md / 新增 R40-A.md 三个 docs 文件），全部测量、聚焦套件与 `pnpm gate` 在 `ecbf3ab` 工作树上完成后 `git merge --ff-only 46686fc` 对齐，对齐后冻结复核仍 0 行、聚焦套件 + `pnpm gate` 在对齐树上**复跑同计数同退出码**；**R40-A 全文读毕**（A 区诚实空枚举 + 三项 A 站点级换名记录（types.ts parseOpenMinor / parseGateDecision 属性双读 CSE、child-tracking `?? []` 双回退臂），全部属地归 A、与本切片零交集，折入不移植清单）。
**分支:** `cursor/r40-b-live-routing-fortieth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R28-B…R39-B 引擎位级相同；系统 Node 已显式绕开——PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 后 `node --version` 复核 v22.22.2，三次官方运行前各自 `node --version` 复核 + 仿真内置 L0 引擎同一性断言 ×3 全过（首行打印 `L0 OK: engine v22.22.2`）；**零官方批次落在系统 Node 上**，无需弃批。tsx 4.23.12、pnpm 10.17.1；诚实注记：本 VM 初始未装依赖（首次 `pnpm gate` 报 `tsc: not found`），`pnpm install --frozen-lockfile` 干净完成后门禁全绿（与 R38-A / R40-A 同注）；`package.json`/`pnpm-lock.yaml` 对 `94ed3d9` 0 行 diff）。

## 结论

**诚实空枚举：无达门槛的新更优解，切片零代码改动，未铸任何 S40-B-\* ID、未铸任何新 face 名或 ALLCAPS 外号，EXCLUSIONS.md 零增行（全表仍 545 行）；且本轮枚举**零**新站点级换名记录——与 R34-B…R39-B 同为「残余角度均系已点名站点复命中」的干净空收口。** 第四十遍透镜按令**不**重走 R31-B 面格闭合审计（六扇停驻门维持）、**不**重走 R32-B 闭合稳定性审计（54/54 停驻记录）、**不**移植 R33-A…R39-J 与 R40-A 空枚举、**不**重提 R39-B / R38-B / R37-B 零新站点级记录、**不**重铸「再查一遍重开条件 / 闭合稳定性 / 门扉 / 无名站点」式记录——改做**从零独立重枚举**：重读 10 文件全量 1359 行，重走结构位置 × 消除机制满矩阵（§2d/§4），对每个枚举出的角度执行换名拒绝检查。结果：全部角度落在既有排除 ID 本体、B 区自有封禁面、姊妹区归档面（明令不移植）或 R31-B 停驻门成员上；本轮枚举尾部仅存的两个残余微观角度（`assign.ts`/`assign-plan.ts` 每批 id 双 map / 双 Set、`decideLiveCascade` 的 `stay` 闭包每调分配）与 R34-B…R39-B §2d 完全同两站点，均为**已点名站点复命中**（分别 R2-B §4 点名 + S7-B-6 每批 ns 天花板 + R27-B batch prelude intercept face 属地、R2-B「S1-B-4 同表面」裁决并经 R5-B/R6-B/R8-B/R34-B…R39-B 九次维持），不构成新站点级记录。派单硬保持项显式重取证：**S12-B-2 重开条件腿 (a) 未触发**（`rg -c 'prior' src/adaptation/eval-routing.ts` = 0 匹配（rg exit 1），:353–354 只传 learned；`assignTasks` 生产调用方图景三处（`cli/main.ts:365`、`adaptation/eval-routing.ts:353–354`、`track/primary-split.ts:41`）与 R30-B…R39-B 逐条一致无新增）；**S13-B-1 重开条件未触发**（`git diff 94ed3d9 -- src/learning/learned-routing.ts` = 0 行，`applyLearnedRouting` 本体成本无变化，维持 loser ID）；**目录行序契约（R28-B W3）零触碰**（未尝试、未预排序）。

关键前提事实：切片 10 个文件（清单逐一核对恰为 10，`wc -l` 合计 **1359** 行，逐文件计数见 §0，与 R32-B…R39-B 逐数一致）自 R1-B 裁决基线 `94ed3d9` 以来**逐字节零变化**（`git diff 94ed3d9 -- <切片 10 文件>` = **0 行**，分支创建时、测量后、快进对齐后、提交前四点核对）。最强冻结 `git diff 89f0455..HEAD -- src/` 亦为 **0 行**（快进段本身 src/ 为空）。R1-B 的结构下界论证、R4-B 的聚合天花板论证与 S1-B-\* … S13-B-1 全部裁决对当前代码原样成立。

**测量侧诚实声明（承 R34-B…R39-B 方法注记）**：历届 B 驱动（/tmp/rXXb-sim.mts 族）从未入库，本 VM 上不存在其字节；本轮驱动为**按 R4-B…R39-B 附录约定从零重建**（种子 0xb44b01 mulberry32、N=2000/20000/30 语料、M=2/7/10 目录、×2 eval 对口径、W1/W2/W3 证人、L0 断言——细节见附录 A），故与历届报告的**逐格数值带对照只具指示性**；效力载体是**结构性结论**（格间全序、learned 税方向、落地线余量、亚 µs 探针、prior 倍率）与 ×3 运行内部一致性。天花板本 VM 实测：**ceiling M=2 8.91–9.98 / M=10 17.13–18.30 ms/eval**（×3 运行，×2 对口径；M=2 runs 2/3 落在派单默认对照 R39-B 8.31–9.77 带内、run 1 的 9.98 上侧邻接；M=10 run 2 的 17.13 落在 R39-B 16.99–17.36 带内、runs 1/3 的 18.30/17.94 上侧邻接且 17.94 落在 R38-B 16.91–18.00 带内——回归不可能成立（切片对 `94ed3d9` 零 diff），ceiling 上侧邻接而 faithful 下侧邻接的**分向不一致**正是宿主 + 重建语料构成差的特征而非结构变化）；replay-faithful M=2 **8.96–9.60**（R39-B 9.63–10.00 邻接下侧、更快方向；runs 1/3 的 9.01/9.60 落在 R38-B 9.00–10.14 带内）；10× 压力 N=20000 M=10 learned 单臂 **71.89–74.73 ms/eval**（为其 N=2000 矩阵孪生臂 7.34–7.65 的 **9.4–9.8×**，线性放大成立；臂构成与 R39-B 压力格（M=2 ×2 对）不同，跨报告只具指示性）；全 18 矩阵格 ×3 保持 M=2 < M=7 < M=10 全序；learned ≥ no-learned 在 **3/9** 格对成立（M=2 三读全正 +2.5%~+3.9%），**诚实注记**：M=7 三读反转（−4.2%~−5.6%）与 M=10 三读反转（−0.4%~−2.1%）——M=7 反转与 R33-B…R39-B 同一配置格抖动族（R36-B 三例 −1.1%~−6.0%、R38-B 三例 −1.6%~−4.9% 同型），M=10 三读反转为本轮重建语料构成效应（本轮 W2 divergence inside=463，高于 R39-B 的 413——avoid/prefer 命中族更大 ⇒ avoid 命中使 partitionLiveCandidates 跳过被除名模型整行 evaluateLiveCandidate 的 allow-list 收缩红利更大），方向与 **S13-B-1 已归档机制旁注**一致，非新信息；W1（全格双跑逐字节恒等）/ W2（分歧全部落在 avoid/prefer 命中族，inside=463 / outside=0，×3 逐数相同且三目录相等）证人全过、freeze 0 行——按令主机窗口 + 重建构成效应不构成结构翻转、不铸坐标；`holds-below-landing-line=true` ×3（生产代表格最大 18.30 < 30，余量 ~39%）。prior 双档格：假设档 N=2000 M=2/M=7/M=10 = 13.51–13.69 / 19.54–19.77 / 22.98–23.12 ms/eval，为各自 learned 孪生格的 **3.1–3.3× / 3.4–3.5× / 3.0–3.2×**（驱动内显式复测孪生行）——倍率结论与 R35-B…R39-B（2.8–3.4×）同构成立；**诚实注记**：本轮九读全部线下（<30 ms），与 R39-B「M≥7 六读越线（38.53–48.98）」的差异来自重建快照构成（本轮 3 源 21 行 + aliasMap 7 行，单拾取摊算 ~4.6–7.7 µs/pick；R39-B 快照 4 源、单拾取锚 10.2–10.7 µs/op——pickFromPublicPrior 本体属 `public-prior.ts` 切片外，其成本随快照构成缩放），倍率与零流量结论不受影响；「若有流量即数量级增费」的结构载体是倍率而非绝对值，且 S12-B-2 重开条件是调用方传 prior（N≥10³）而非基准值，本轮腿 (a) 重取证未触发，生产流量仍为零。真实档 face M=2+prior 243.3–248.6 µs/调用（R38-B 238.5–249.2 **带内**、R39-B 226.3–232.6 上侧邻接）；face M=7+prior+learned 375.8–379.4 µs（R38-B 355.9–425.7 **带内**、R39-B 340.4–349.3 上侧邻接——与 ceiling 同向的上侧宿主窗）。级联探针 332–344 ns/op（FAIL+failureClass=model 逃逸路径）、单拾取 selectLiveModel M=10 探针 473–563 ns/op，均亚 µs 拒收档。落地线（数十~数百 ms 或复杂度类下降）在现有生产流量下依旧不可达。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- **切片恰 10 文件**（逐文件 `wc -l`）：`r0.ts` 173 / `assign.ts` 129 / `assign-plan.ts` 68 / `policy.ts` 160 / `live-cascade.ts` 118 / `live-selection.ts` 61 / `analyze-task.ts` 108 / `primary-catalog.ts` 101 / `catalog-model.ts` 83 / `supervisor/model-router.ts` 358，合计 **1359** 行，与 R32-B…R39-B 逐数一致。`catalog-observed.ts` **不在**本切片（按令未纳入、未触碰、未计时）。
- **冻结四点核对全过**：分支创建时（tip `ecbf3ab`）`git diff 94ed3d9 -- <切片 10 文件>` = **0 行**且 `wc -l` 合计 1359；测量后复核 0 行；ff-align 到 `46686fc` 后复核 0 行；提交前再复核 0 行。最强冻结 `git diff 89f0455..HEAD -- src/` = **0 行**。`package.json` / `pnpm-lock.yaml` vs `94ed3d9` 亦 **0 行**。最后一次生产 live-B 变更维持 `94ed3d9`。
- **读序（按令完成）**：README → EXCLUSIONS 全表（545 行，X0-\* … S13-B-1 全部实读；空枚举不增行）→ PROGRESS → round-40 PLAN 全文（B 段 + 具名/不具名禁令）→ round-39 PLAN 全文 → R39-B 全文（默认对照带 + §2d 两残余角度边界）→ R39-A 与 R39-C…R39-J 全部实读（空枚举逐份折入不移植清单）→ R38-B / R37-B / R36-B 空枚举结论 + S12-B-2 / S13-B-1 排除记录（表行 541/545）+ R28-B W3 行序契约记录实读 → 10 切片文件全量重读（1359 行）。快进落地后补读 **R40-A 全文**并折入（A 区三项站点级换名记录属地归 A，与本切片零交集）。
- **摄取状态**：测量窗口内独占分支一次 docs-only 快进（`ecbf3ab → 46686fc`，段核对 src/ / test/ / lockfile 0 行）；按令 ff-align 完成，新姊妹报告 R40-A 已全文读毕折入；聚焦套件 + `pnpm gate` 在对齐树复跑同计数；姊妹分支零触碰。
- **不移植 / 不重铸清单维持**（枚举时逐条遵守，本轮零触碰）：全部具名禁令 **FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX**；**R31-B 面格六停驻门**（全部维持停驻，零重开）；**R32-B 闭合稳定性审计**（54/54 与 D1–D5 为停驻记录，不重走）；**R33-A…R39-J 与 R40-A 空枚举**（各自站点级换名记录、方法卫生与轴归档均按属地维持，不移植不重铸；R33-B `ROLE_FAMILY` 死回退、R35-B…R39-B 零新站点级记录、R39-A 三项与 R40-A 三项站点级记录均不重提不重铸）；R30-B scale-cube face；R32-A F12a–d 哨兵审计；R32-J 六残余坐标（本切片零 fs 位点）。
- **B 区自有封禁面不补铸**：R29-B learned-policy container face / R28-B catalog-face row-arrangement (permutation) face / R27-B batch-cardinality bottom + prelude intercept face / R26-B privacy / provider-policy constraint-leg face / R25-B task-face optional-field presence topology / R24-B M=1 catalog collapse / R20-B objective 长度 L 轴 + K×W 双线性轴 / R19-B 比较器第三腿 + 目录基数 M 轴。**目录行预排序仍是行为变更**（R28-B W3 裁定行序为契约决策载体——tie-flip 实验证明别名行前置使 primaryPreferredId / cheapestAssignableId 翻案），本轮未尝试、未预排序。
- **硬不变量**：切片零代码改动 ⇒ Live=R0 等价（R1 未接线 live；稀疏回退 approved R0 baseline）、双 LCB（Beta+正态）、双归因（logit+prob-add）、proposal-first（不声称 Outcome-supported；Checkpoint F-PROD 仍开放，ADR-005）、阈值、CAS/凭证/数据面契约/公共签名全部原样保持；live 面不 import R1/bandit/shadow 由 `live-isolation.test.ts` 在聚焦 198 项中复证通过。未改任何测试。S8-E-1（`loadLearnedRouting` 恰 3 处：`track/loop.ts:88`、`run/flowchart-run.ts:712`、`cli/main.ts:708`，本轮 rg 重证）等切片外硬保持项零触碰。
- **EXCLUSIONS.md 未增行**（空枚举记录不入排除表，plan-ban 归属父代）；仓库变更恰为本报告单文件（提交前 `git status` 复核仅 `docs/reports/sota-opt/round-40/R40-B.md`）。禁止开 PR，未开。

## 1. 第四十遍搜索方法：从零独立重枚举

R31-B 已证面格闭合、R32-B 已证闭合稳定、R33-B…R39-B 各做过一次从零重枚举（分别收获 1 / 0 / 0 / 0 / 0 / 0 / 0 项新站点级换名记录）——第四十遍不照抄其结论，而是**独立重跑同一枚举原点**：假装排除表不存在，重读 1359 行找出每一个理论上可疑的消除/重构角度，再逐一对表执行换名拒绝检查（词根不同 ≠ 面不同；站点不同但同族同向 = 换位点记录不铸 ID；**站点已点名 = 复命中，零记录**）。方法四步：

1. **生产调用方图景重取证**——`rg 'assignTasks\(' src/` 恰 3 个调用方（`cli/main.ts:365` smartChildPlan live face N≤30、`adaptation/eval-routing.ts:353–354` 只传 learned、`track/primary-split.ts:41` C 小批），与 R30-B…R39-B 逐条一致，无新调用方、无新可选参数；`assignOne` src 内零生产调用方维持（rg exit 1）；`effectiveConfidenceThreshold` / `routeTask` / `routeFlowNode` 在 src 内仍只有 `supervisor/flowchart.ts:34` barrel 再导出、零运行时消费者（R12-B 零流量取证**第十次转移**成立）；`routeR0` src 内唯一调用方仍是 `r1-shadow-report.ts:88`（shadow-only；`scripts/iter1-equivalence-sim.ts` 为 scripts 非生产）；`applyCascade` 唯一调用方仍是 `cascade-evidence.ts:102/:108`（R5-B「生产不可达」维持）。
2. **结构位置 × 消除机制满矩阵**——10 文件 × 13 类消除机制（死码/常量折叠/CSE 去重/记忆化/数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/形状单态化）逐格过筛（§4 按文件汇总）。
3. **换名拒绝检查**——非空格逐一对表；本轮尾部仅存的两个残余角度均查证为已点名站点复命中（§2d），**零**新站点级记录。
4. **测量侧不铸新剂量网格**——只重锚既有格 ×3 独立运行 + W1/W2/W3 行为证人 + L0 引擎同一性断言；驱动为从零重建（诚实声明见结论段与附录 A），冒烟运行按纪律弃置（全绿、值带内，无口径修正），官方口径为其后三次独立进程运行（全程 tee 留档 /tmp 日志，撰写后与驱动一并删除，gate 复跑在删除之后）。

## 2. 天花板复测：R4-B…R39-B 收口第三十七次复核成立（重建 harness）

引擎 Node v22.22.2 / V8 12.4.254.21-node.39（canonical，L0 断言 ×3）。语料种子 0xb44b01（重建生成器）。×3 独立进程运行（每次运行前 `cat /proc/loadavg` 复核空闲 0.00），全部 ALL CHECKS PASSED；确定性 sink ×3 逐位相同（**1387715043**）、消费 checksum ×3 逐位相同（**50529**）。单位 ms/eval（×2 对口径，eval = baseline+candidate 两次 `assignTasks`，镜像 `eval-routing.ts:353–354`）：

| 格 | run 1 | run 2 | run 3 | 历史参照（指示性） |
|---|---|---|---|---|
| ceiling N=2000 M=2（rich 语料 + limits） | 9.98 | 9.21 | 8.91 | R39-B 8.31–9.77（runs 2/3 带内，run 1 上侧邻接）/ R38-B 10.91–11.98 下侧 |
| ceiling N=2000 M=10 | 18.30 | 17.13 | 17.94 | R39-B 16.99–17.36（run 2 带内）/ R38-B 16.91–18.00（run 3 亦带内，run 1 上侧邻接） |
| replay-faithful M=2（bare 语料，eval-routing 裸镜像） | 9.01 | 8.96 | 9.60 | R39-B 9.63–10.00 下侧邻接（更快方向）；R38-B 9.00–10.14（runs 1/3 带内） |
| 10× 压力 N=20000 M=10 learned 单臂 | 74.73 | 71.89 | 73.07 | 本 rig 内孪生臂（矩阵 M=10 learned 7.34–7.65）的 9.4–9.8×，线性放大成立；臂构成与 R39-B 压力格（M=2 ×2 对 93.44–106.38）不同，跨报告不可直较 |

M=2 全序、M=10 全序、压力格线性放大的结构关系逐运行成立；`holds-below-landing-line=true` ×3（生产代表格最大 18.30 < 30，余量 ~39%）。**回归不可能成立**：切片对 `94ed3d9` 逐字节零 diff（四点冻结 + 快进段 src/ 为空），任何格间 Δ 只能来自主机与重建 harness 构成差；ceiling 上侧邻接而 faithful 下侧邻接、face 双格 R38-B 带内而 R39-B 上侧邻接——**分向不一致的邻接偏移**正是宿主 + 重建构成差的特征而非结构变化。W1（双跑逐字节恒等，3 目录 × 2 臂：M=2 2604472/2599758、M=7 2977172/2972432、M=10 3210204/3206272 字节，×3 恒等）/ W2（learned 臂仅在 avoid/prefer 命中族上分歧）/ W3（prior 拾取存在性 + 身份）三组行为证人 ×3 运行全过，语义与 R30-B…R39-B 恒等。

## 2b. 配置态 × 命令类矩阵：全六格复测（第三十一次全格复核；臂交错测量）

replay N=2000 bare 语料，ms/batch（单臂口径；六臂在同一 rep 循环内交错采样，median of 15）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| M=2 default, no learned | 4.18 | 4.05 | 4.01 |
| M=2 default, learned | 4.31 | 4.15 | 4.17 |
| M=7 configured, no learned | 6.09 | 5.95 | 5.94 |
| M=7 configured, learned | 5.83 | 5.62 | 5.61 |
| M=10 stress, no learned | 7.80 | 7.51 | 7.50 |
| M=10 stress, learned | 7.65 | 7.48 | 7.34 |

全 18 格保持 M=2 < M=7 < M=10 全序 ×3（两臂均成立）。learned ≥ no-learned 在 **3/9** 格对成立（M=2 三读全正 +3.1/+2.5/+3.9%）；**诚实注记**：M=7 三读反转（−4.2/−5.5/−5.6%）、M=10 三读反转（−1.9/−0.4/−2.1%）。M=7 反转与 R33-B（M=10 一例 −0.12%）、R34-B（M=7 两例 −2.3%）、R35-B（一例 −4.8%）、R36-B（三例 −1.1%~−6.0%）、R37-B（一例 −1.1%）、R38-B（三例 −1.6%~−4.9%）、R39-B（两例 −1.1%/−1.8%）同为 M=7 配置格抖动族，本轮幅度带（4.2–5.6%）落在 R36-B 历史带内；M=10 三读反转为本轮新出现的方向样本，机制与 M=7 族同源——**S13-B-1 已归档旁证**（配置目录下 avoid 命中使 partitionLiveCandidates 跳过被除名模型的整行 evaluateLiveCandidate，allow-list 收缩红利可超过 applyLearnedRouting 本体增费）在本轮重建语料下更强：W2 divergence inside=463（R39-B 413 / R38-B 379 / R37-B 456 / R36-B 420——重建语料 stem 字节构成差，各自确定 ×3 逐数相同），avoid/prefer 命中族更大 ⇒ 红利格更多；非新信息、非结构翻转；W1（全格双跑逐字节恒等）与 W2（三目录 inside=463、outside=0，×3 逐数相同）全过，freeze 0 行——按派单令「host-bias windows with freeze 0 lines are NOT findings」，不铸坐标、不加表行。级联探针（M=10 tiers 基座，FAIL+failureClass=model 逃逸路径）：`decideLiveCascade` 336/332/344 ns/op（R39-B 307–316 上侧邻接、R38-B 255–270），亚 µs；每子任务一次的生产流量下（C≤6/run）仍为深度亚噪声。单拾取 `selectLiveModel` M=10 探针 487/473/563 ns/op（偏好臂交替），亚 µs 拒收档。

## 2c. prior 双档格复测（第二十九次跨 VM；S12-B-2 重开腿 (a) 复核）

重建快照 `r40b-frozen-snapshot`（3 源 21 行 + aliasMap 7 行，qualityBar 0.6；与历届快照构成不同，倍率结论按各自 rig 内部孪生格比值给出）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| 假设档 N=2000 M=2 + prior + learned（ms/eval） | 13.69 | 13.51 | 13.63 |
| 假设档 N=2000 M=7 + prior + learned | 19.77 | 19.54 | 19.61 |
| 假设档 N=2000 M=10 + prior + learned | 22.99 | 22.98 | 23.12 |
| 孪生格（learned-only，驱动内显式复测行）M=2 / M=7 / M=10 | 4.36/5.87/7.67 | 4.20/5.71/7.41 | 4.13/5.57/7.33 |
| 真实档 live face N=30 M=2+prior（µs/调用） | 248.6 | 243.8 | 243.3 |
| 真实档 N=30 M=7+prior+learned（µs/调用） | 379.4 | 375.8 | 376.4 |

假设档倍率：M=2 = 孪生格 **3.1–3.3×**、M=7 = **3.4–3.5×**、M=10 = **3.0–3.2×**——与 R35-B…R39-B 的 2.8–3.5× 倍率带同构成立，「prior 层若有流量即数量级增费」的邻接配置结论不变。**诚实注记**：本轮九读全部线下（最大 23.12 < 30），与 R39-B「M≥7 六读越线（38.53–48.98）」的绝对值差异来自**重建快照构成**——本轮 3 源 21 行快照的拾取摊算 ~4.6–7.7 µs/pick，R39-B 4 源快照单拾取锚 10.2–10.7 µs/op；`pickFromPublicPrior` 本体属 `public-prior.ts`（**切片外**），其成本随快照构成缩放，绝对值跨报告只具指示性，结构载体是倍率与零流量。生产流量本轮重新取证仍为零：`rg -c 'prior' src/adaptation/eval-routing.ts` = 0 匹配（rg exit 1），:353–354 `assignTasks({ catalog, tasks, learned: … })` 只传 learned。**S12-B-2 腿 (a) 未触发**（重开条件是调用方传 prior（N≥10³）或榜单行数增量级——均为调用方侧事实，与基准绝对值无关），条件式落地物维持在架，按令不补铸不重扫（R21-B 快照形状轴维持关闭）。W3（三种 M 下 family=edit 拾取存在：M=2 → premium 0.786、M=7/M=10 → m04 0.667——拾取**身份**与 R35-B…R39-B 完全一致；quality 数值出自重建快照构成，与 R39-B 逐位重合系配方设计使然，非跨 VM 数值可比性声明）×3 全过。真实档 face M=2+prior 243.3–248.6 µs 落在 R38-B 238.5–249.2 **带内**（R39-B 226.3–232.6 上侧邻接）；face M=7+prior+learned 375.8–379.4 µs 落在 R38-B 355.9–425.7 **带内**（R39-B 340.4–349.3 上侧邻接，与 §2 ceiling 同向上侧宿主窗），全部亚 ms、生产流量零。

## 2d. 第四十遍枚举：换名拒绝检查记录

从零满矩阵扫描（§4）枚举出的全部角度经对表逐一裁决。**本轮零新站点级换名记录**（与 R34-B…R39-B 同为零；R33-B 为恰 1 项）。枚举尾部仅存的两个残余微观角度与 R34-B…R39-B §2d 完全同两站点，均查证为**已点名站点复命中**：

1. **`assign.ts:63` + `assign-plan.ts:29` 每批 id 双 map / 双 Set**（`catalogIds = input.catalog.models.map(id)` 与 `planAssignmentPolicy` 内 `new Set(models.map(id))` 与 `validateConfig` 内查重 `ids` Set 三处每批各枚举一遍目录 id）——R2-B §4 已点名（「`catalogIds` 重 map 与 router 内部 Set 不共享属公开接口变更」），每批一次 ns 级天花板由 S7-B-6 本体承载。复命中，零记录。
2. **`decideLiveCascade` 的 `stay` 辅助闭包每调分配**（live-cascade.ts:87）——R2-B 已点名（「`stay` 闭包每调分配为亚噪声（S1-B-4 同表面）」）并经 R5-B/R6-B/R8-B/R34-B…R39-B 九次维持；本轮探针 decide 全函数 332–344 ns/op，闭包份额为其真子集，生产流量每子任务一次（C≤6/run）⇒ 亚 µs/run。复命中，零记录。

本轮单独复核的一处边缘站点亦未产生记录：**`assignTasks` 批前奏 `limits` 条件 spread**（assign.ts:65–68，每批一次分配）——属 R27-B **batch-cardinality bottom + prelude intercept face** 属地成员（batch prelude = createModelRouter + planAssignmentPolicy + catalogIds + limits 的整体拦截面，封禁面），非新站点、不铸 ID。

其余枚举角度全部落在既有裁决**本体**上（代表性清单，与 R33-B…R39-B §2d 清单核对一致）：`validateConfig` ROLES.includes / dup-role Set = S1-D-8 类 + S10-B-3、查重 has+add 单探针 = S9-B-1；`planAssignmentPolicy` 双排序拷贝取极值 = S8-B-1；`assignPlanned` 两次防御拷贝 = S8-B-4 / S2-B-1、options 三条件 spread = S9-B-3 / S4-B-4、批内记忆化 = S3-B-1（learned 层 = S13-B-1 loser、prior 层 = S12-B-2 条件陆器）；route 输入骨架 = S5-B-4 / S3-B-6 / S4-B-5 族；`validateInput` 字面量数组 = S1-B-7、per-id trim 卫 = S9-B-2；unknown-model 预扫描下沉 = S9-B-4；per-route allowed Set = S12-B-1（双向封死）；主循环融合 = S11-B-1（镜像封死）；`evaluateLiveCandidate` 空前置失败别名 = S7-B-3、per-model request 字面量 = R1-B 契约裁决（fixedCostUsd/fixedLatencyMs 所需）、failures 合并 spread = 同站点微观；`toModelDescriptor` 每 (任务,模型) 构造 = R1-B §4.4 架构裁决；`buildDecision` 构造份额 = R32-B D1 普查（19/20 字段活）、eligible.map 与 oneHot 融合 = S6-B-1；justification 预计算 = S7-B-4；approvalPlan 享元 = S6-B-2；one-hot 载体形 = R32-B D4 三重关；`coldStartRoutingScore` 钳位 = S10-B-2；空 avoid/prefer 跳过 learned = S6-B-4；`cheapFirstTiers` 装饰-排序-还原 / localeCompare / 冗余 spread = S3-B-2 / S3-B-3 / S1-B-5；`decideLiveCascade` find+findIndex = S1-B-4；`liveRefusalMessage` 双 .some = S8-B-2；`selectLiveModel` 比较器展开 = S8-B-3；analyze-task 正则链 / HIGH_RISK_RE 双测去重 / reason join / long 惰性与换行手写计数 = S4-B-1 / S1-B-1 / S5-B-3 / S10-B-1 + S1-B-2；`familyOf` 布尔重排 = S1-B-3、deploy 门冗余合取 = S7-B-1、终端死回退 = R33-B §2d 记录 1（已点名不重提）；preferPrimary 被吸收析取 = S7-B-2（含 contractRisk=false 反例边界复核：`family === "deploy"` 非全吸收，删除仍属行为耦合）；`routeR0` 高风险 find = S1-B-6 / S2-B-3、r0 排序输出 = R1-B 契约；`applyCascade` `{...decision}` 拷贝 = 生产不可达（R5-B/R9-B 维持，本轮调用方重取证 cascade-evidence.ts:102/:108）；`primary-catalog` 构造器享元 = S7-B-5 / S6-B-2 类身份可观察、throw 位点 = R17-B 拒绝路径已锚 + R32-B D2 维持；`effectiveConfidenceThreshold` candidates 数组 + `Math.max` spread 与 `routeTask`/`routeFlowNode` 包装 = R12-B 零流量不铸维持（本轮 barrel census 重证：flowchart.ts:34 再导出、零运行时消费者，第十次转移）+ Math.\* 绑定属 BINDX 坐标声明即止；`createModelRouter` `{...config, models}` 每 run 一次 CLI 噪声档、跳过二次 catalogModel = S2-B-2；`catalogModel` 条件 spread 属性存在性可观察 = S1-C-10 类。

## 3. 候选总表

**空。** 铸 ID 数：**0**。铸 face 名 / ALLCAPS 外号数：**0**。EXCLUSIONS.md 增行数：**0**。新站点级换名记录数：**0**（两个残余角度均为已点名站点复命中，§2d；limits 前奏 spread 归 R27-B 封禁面属地）。本轮产出为三点哨兵重取证（S12-B-2 腿 (a) / S13-B-1 / 调用方图景 + barrel 零流量第十次转移）与重建 harness 下的锚点复位（§2–§2c），均不构成候选。空枚举与 R8-B…R39-B 同为战役允许的诚实结局。

## 4. 结构位置 × 消除机制满矩阵扫描（第四十遍，按文件汇总）

| 文件 | 本轮检查 | 结论 |
|---|---|---|
| `r0.ts` | `applyCascade` 生产不可达第四十次重取证（调用方恰 cascade-evidence.ts:102/:108）；routeR0 src 内唯一调用方 r1-shadow-report:88（shadow-only）；S1-B-6/S2-B-3/S3-B-4/S4-B-3 维持；排序输出即契约 | 无候选 |
| `assign.ts` | 调用方图景三处重取证一致（§1）；id 双 map = R2-B §4 已点名 + S7-B-6 本体（§2d 记录 1，复命中零记录）；批前奏 limits spread = R27-B prelude intercept face 属地（§2d 边缘站点复核）；S8-B-4/S2-B-1 双拷贝、S9-B-3/S4-B-4 options、S3-B-1 批内记忆化、X3-1 维持；S13-B-1 重开门未触发；`flowchartRoleForAgentRole` 属 graph 区（跨切片只记录） | 无候选 |
| `assign-plan.ts` | S8-B-1 维持；每批双 Set 同上复命中；`pickPreferredModel` prior 层 = S12-B-2 条件陆器在架未触发；`pickFromPublicPrior` 本体属 `public-prior.ts`（切片外，本轮快照构成差异注记见 §2c） | 无候选 |
| `policy.ts` | per-model request 字面量 = R1-B 契约裁决；S7-B-3 别名护栏维持；预算/期限 toFixed 串仅失败路径 = R17-B 已锚；COMPLEXITY_RANK 常数查找无池；capability 循环已首缺早退（break 在位） | 无候选 |
| `live-cascade.ts` | `stay` 闭包 = R2-B 已点名复命中（§2d 记录 2）；S1-B-4/S1-B-5/S3-B-2/S3-B-3 维持；byId Map 每 plan 一次 M≤10 ns 档；decide 探针 332–344 ns 复锚（M=10 tiers 基座，FAIL+failureClass=model 逃逸路径断言全过） | 无候选 |
| `live-selection.ts` | S8-B-3/S8-B-2 维持；`compareLiveCandidates` Number() 布尔差分已是无分支形态；总序契约（R19-B 第三腿封禁）零触碰；单拾取探针 473–563 ns（§2b） | 无候选 |
| `analyze-task.ts` | `ROLE_FAMILY` 死回退 = R33-B §2d 记录 1 已点名（不重提）；HIGH_RISK_RE 双测 = S1-B-1 本体；S1-B-2/S1-B-3/S4-B-1/S5-B-1/S5-B-3/S6-B-3/S7-B-1/S7-B-2/S10-B-1 维持（S7-B-2 边界含 contractRisk=false 反例复核） | 无候选 |
| `primary-catalog.ts` | 单点构造器消灭形状多态（R31-B C1 停驻维持）；构造器享元 = 身份可观察类；modelFor/inferFastId 每配置一次 ns；throw 位点归 R32-B D2 | 无候选 |
| `catalog-model.ts` | `toModelDescriptor` R1-B §4.4 架构裁决维持；oneHotDistribution = R32-B D4 维持；条件 spread 属性存在性可观察（S1-C-10 类）维持 | 无候选 |
| `supervisor/model-router.ts` | `effectiveConfidenceThreshold`/`routeTask`/`routeFlowNode` 零流量重取证（barrel 再导出 flowchart.ts:34、零运行时消费者，第十次转移成立）；S12-B-1/S11-B-1 镜像对封死维持；S1-B-7/S9-B-1/S9-B-2/S9-B-4/S5-B-4/S10-B-2/S10-B-3/S7-B-6/S6-B-1/S6-B-2/S6-B-4/S6-B-5/S7-B-4/S8-B-4/S2-B-2 维持；buildDecision = R32-B D1 普查维持（19/20 活）；`createModelRouter` 每 run 一次 CLI 噪声档 | 无候选 |

## 5. 关键裁决细节

- **S12-B-2 换名检查**：假设档倍率 3.0–3.5× 维持（本轮九读线下系重建快照构成，§2c 诚实注记；重开条件是调用方侧流量事实，与基准绝对值无关），腿 (a) 重新取证未触发（eval-routing 零 prior、调用方图景三处未变、无新可选参数）。未以任何新名重扫快照形状轴。
- **S13-B-1 换名检查**：`git diff 94ed3d9 -- src/learning/learned-routing.ts` = 0 行，本体成本无变化，重开条件（applyLearnedRouting 本体成本增 ≥2 个量级）未触发。learned 容器坐标归 R29-B（封禁），本轮未触碰。本轮 M=7 三读 + M=10 三读 learned 反快（§2b）恰是 S13-B-1 已归档的「allow-list 收缩红利」机制旁证在更大命中族语料（inside=463）下的复现，属既有记录复命中，不构成新面。
- **目录预排序**：R28-B W3 裁定行序为契约决策载体（tie-flip 实验：别名行前置使 primaryPreferredId acme/frontier→premium、cheapestAssignableId orion/lite→cheap 当场翻案），预排序 = 行为变更，本轮未尝试。
- **重建 harness 与主机窗口**：本轮全部格值出自从零重建驱动（诚实声明见结论段），跨报告数值带对照只具指示性；效力护栏为 (a) 四点冻结 0 行 + 快进段 src/ 为空、(b) W1 双跑逐字节恒等 ×3 + 确定性 sink ×3 逐位（1387715043）+ 消费 checksum ×3 逐位（50529）、(c) W2/W3 语义证人 ×3（divergence 计数 inside=463 / outside=0，×3 逐数相同且三目录相等；W2 计数与 R39-B 的 413、R38-B 的 379、R37-B 的 456、R36-B 的 420 差异来自重建语料 stem 字节构成，同配方不同字节，各自确定）、(d) 序结构（格间全序、落地线余量、prior 倍率、压力格线性放大）与 R30-B…R39-B 完全同构。ceiling 上侧邻接而 faithful 下侧邻接、face 双格 R38-B 带内而 R39-B 上侧、级联/单拾取探针上侧邻接——**多向不一致的邻接偏移**正是主机 + 重建构成差的特征而非结构变化；M=7/M=10 learned 六读反转（§2b 诚实注记）与 prior 假设档九读线下（§2c 诚实注记）均在该护栏下裁定为重建构成 + 主机窗口效应；派单已预告「host-bias windows with freeze 0 lines are NOT findings」，均不铸轴。
- **引擎纪律**：三次官方运行全部在 nvm Node v22.22.2 产出（L0 断言 + 每次运行前 `node --version` 复核 + 空闲复核 loadavg 0.00）；系统 Node 上零官方批次，无弃批。一次冒烟运行（同字节驱动，v22.22.2，全绿、值带内）用于验证 harness 启动，按纪律弃置不计入官方口径；官方口径为其后三次独立进程运行（tee 留档 /tmp/r40b-run{1..3}.log，撰写后按纪律删除）。

## 6. 前后对比与测试

切片零代码改动，无前后对比。验证（全部在 Node v22.22.2）：

- 聚焦 live 路由单测：`npx tsx --test "test/unit/routing/*.test.ts"` —— **198/198 pass**（18 suites，含 live-isolation / cascade-evidence / failure-class / assign / assign-plan / analyze-task / r0 / live-cascade / live-selection / catalog-eligibility 等），与 R31-B…R39-B 计数逐字相同；测量后、harness 删除后、ff-align 后各跑一次，三次同计数。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0** —— 1169 项测试 1168 pass / 0 fail / 1 skipped（既有跳过项，非本轮引入），78 suites，tsc build 通过；计数与 R9-B…R39-B 逐字相同；harness 删除后在 `ecbf3ab` 树、ff-align 后在 `46686fc` 树各跑一次，两次同计数同退出码（本 VM 初装依赖 `pnpm install --frozen-lockfile` 干净完成后执行，lockfile 对 `94ed3d9` 0 行）。
- 仿真 ×3 独立运行 ALL CHECKS PASSED（L0 + W1/W2/W3 逐运行全过；确定性 sink ×3 逐位相同 1387715043，消费 checksum ×3 逐位相同 50529）。
- 提交前最终 fetch：独占分支一次 docs-only 快进 `ecbf3ab → 46686fc`（段核对 src/ / test/ / lockfile 0 行），`git merge --ff-only` 对齐，对齐后冻结复核 0 行、R40-A 全文读毕折入；`git status` 仓库变更恰为本报告一个新文件。
- 无赢家故 scripts/ 零新增；harness 未入库（重建说明见附录 A，指纹见附录 B，临时文件按纪律核后删除，gate 复跑在删除之后）。

## 7. 新增排除 ID

无。未铸任何 S40-B-\* ID；EXCLUSIONS.md 零增行；零新站点级换名记录（§2d 两条均为已点名站点复命中、一条边缘站点归 R27-B 封禁面属地，无需归档新记录；plan-ban 归属父代）。

## 附录 A：本轮驱动重建说明

历届 B 驱动（/tmp/rXXb-sim.mts 族）从未入库，本 VM 无其字节可衍生；本轮驱动 `/tmp/r40b-sim.mts` 为按 R4-B…R39-B 附录约定**从零重建**：

1. 语料：mulberry32(0xb44b01) 确定性生成 20000 任务（七角色均匀、16 stem 池含 10% 高危尾（2 高危 stem）、25% long（半数 ≥3 换行）、50% 带 contextTokens/outputTokens、10% 显式 contractRisk）；标准格取前 2000，live face 取前 30；**rich**（带 token/contractRisk 选项 + limits）供 ceiling/face 格，**bare**（仅 taskId/role/objective）供 faithful/matrix/stress 格（eval-routing 裸镜像）。
2. 目录：M=2 = `catalogFromPrimary({primaryModelId:"premium"})` canonical 对；M=7 configured（cheap/m01–m05/premium，成本严格递增无并列 0.10→0.50，cheap+m01–m05+premium 均有 prior aliasMap 行）；M=10 stress（+m06–m08 于 0.55–0.65，premium 0.85）。
3. 口径：×2 eval 对（baseline 空 learned + candidate learned=avoid(cheap@review)+prefer(premium@plan)，镜像 `eval-routing.ts:353–354`）；ceiling/faithful/prior-假设档 median of 7、matrix 六臂同 rep 循环交错 median of 15、stress median of 3、face mean of 2000 调用（每调用 = 一个 N=30 批，镜像 `cli/main.ts:365` smartChildPlan）、级联探针 mean of 200k、单拾取 mean of 2000。
4. 证人：W1 双跑 JSON 逐字节恒等（每 M 两臂）；W2 learned 臂分歧任务全部落在 avoid/prefer 命中族（outside=0，inside 三目录均 463，×3 逐数相同）；W3 三 M 下 family=edit prior 拾取存在（M=2 → premium 0.786、M=7/M=10 → m04 0.667，身份与 R35-B…R39-B 一致——快照配方按 per-source min-max 设计使 m04 归一化恰 2/3、premium M=2 加权原始分恰 0.786）；L0 `process.version === "v22.22.2"` 断言（不计时）；确定性 sink（FNV-1a over W1 字节 + W2 计数 + W3 身份 + tiers）防 DCE 且 ×3 逐位（1387715043）；另设消费 checksum（计时循环内累加 decision/preferred 字节长）防 DCE，×3 逐位（50529）。
5. 校准史（如实记录）：一次冒烟运行验证 harness 启动与解析（绝对路径 .ts 导入 + tsx 解析路径），同字节驱动全绿、值带内、W3 身份一致，无需修正；按纪律弃置，官方口径为其后三次独立进程运行（全程 tee 留档）。

## 附录 B：驱动指纹与运行方式

- 驱动：`/tmp/r40b-sim.mts`，459 行，sha256 `20da67c3d1f0f2f44a377b99658b2c354f6f310fafef77aa043af0216abd2ffa`（未入库；临时文件已删除，gate 复跑在删除之后执行）。
- 运行（repo 根，空闲 VM（loadavg 0.00 复核后启动）；PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开系统 Node）：

```bash
npx tsx /tmp/r40b-sim.mts   # 冒烟 1 次（弃置）+ 独立进程 ×3 = runs 1–3（输出 tee 至 /tmp/r40b-run{1..3}.log 留档）
```

---

**MORE_OPTIMA=no** —— 第四十遍从零重枚举空收口：零新站点级换名记录（两残余角度均为 R2-B 已点名站点复命中；limits 前奏 spread 归 R27-B 封禁面属地）；重建 harness ×3 全绿复锚天花板（ceiling M=2 8.91–9.98 / M=10 17.13–18.30 ms/eval——M=2 runs 2/3 与 M=10 run 2 落在 R39-B 带内，余读上侧邻接且 17.94 在 R38-B 带内，faithful 8.96–9.60 下侧邻接，分向不一致判宿主 + 重建构成差；freeze 0 行，W1/W2/W3+L0 证人全过，确定性 sink ×3 逐位 1387715043）；全 18 矩阵格全序成立（learned 对 3/9，M=7/M=10 六读反转诚实记为重建语料命中族更大（inside=463）下的 S13-B-1 机制旁证复现 + 主机窗口，不铸坐标）；prior 假设档九读线下（诚实记为重建快照构成，切片外 pickFromPublicPrior 成本缩放）而 3.0–3.5× 孪生倍率维持同构；S12-B-2 腿 (a) 与 S13-B-1 重开门显式重取证未触发（含 barrel 零流量第十次转移）；测量窗口内一次 docs-only 快进已对齐并折入 R40-A；不铸 ID、不铸面、不增排除行；生产代码零改动，聚焦 198/198 ×3 + `pnpm gate` exit 0 ×2（1169/1168/0/1 计数逐字同）。

MORE_OPTIMA=no
BRANCH=cursor/r40-b-live-routing-fortieth-pass-83a1
