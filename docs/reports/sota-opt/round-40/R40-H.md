MODEL_SLUG=claude-fable-5-thinking-xhigh

# R40-H：`src/evaluation` + `src/requirement` + `src/review` + `src/rubric` 切片第四十遍复查报告（Round 40）

- **战役:** 全库持久 SOTA 优化 Round 40 / R40-H（R1-H…R39-H 同切片三十九遍之后的第四十遍）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `1ae917b`（派单 tip，R40-F 摄取——空枚举——+ 本 R40-H 派单，`docs(sota-opt): ingest R40-F (empty enum) and dispatch R40-H`）。全部锚点、census 与首轮聚焦套件 / `pnpm gate` 在 `1ae917b` 工作树完成；测量与首轮门禁后 fetch 先后检测到独占分支**两次纯 docs 快进** `1ae917b` → `f777e1c` → `898e11f`（`f777e1c`：R40-G 摄取（空枚举）+ R40-J 派单，恰 3 个 docs 文件 PROGRESS / PLAN / R40-G.md，228 插入 16 删除；`898e11f`：R40-I 摄取（空枚举），恰 3 个 docs 文件 PROGRESS / PLAN / R40-I.md，218 插入 11 删除），两段增量对 `src/ test/ package.json pnpm-lock.yaml` 各直接复核 **0 行**，按令逐次 `git merge --ff-only` 零冲突对齐并各重验冻结（0 行）；各 tip 间 `src/ test/ package.json pnpm-lock.yaml` 逐字节相同 ⇒ 测量结果原样携带，无需重测；聚焦套件与 `pnpm gate` 在 `f777e1c` 与 `898e11f` 两树**各重跑一次**（计数逐字相同）。**R40-G / R40-I 已全文实读并折入非移植清单（§0.3）**；PLAN 两段快进对 H 段仅状态行（本波派出 → 运行中）+ 非移植清单补 R40-G / R40-I，约束无实质变更。R40-J 在飞，按令未等待、未触碰姊妹分支。派单要求预读的 **R40-A…R40-F** 六份姊妹报告（六份均诚实空枚举）已全文通读并折入非移植清单（§0.3）；R39-A…R39-J 十份空枚举按令折入非移植（§0.3）。
- **分支:** `cursor/r40-h-eval-fortieth-pass-83a1`
- **切片:** 21 文件（evaluation 8 + requirement 7 + review 4 + rubric 2），`wc -l` 合计 **1750** = 655 + 712 + 309 + 74（逐目录实测，与 R32-H…R39-H §0.1 逐一相同）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R24-H…R39-H 同引擎位级相同，带可比；本轮 `which node` + `node --version` + `node -p process.versions.v8` 计时前三重直证）；tsx 4.23.12（仓内 node_modules）、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成、`package.json` / `pnpm-lock.yaml` 对 `fd437a9` 复核 **0 行**。本 VM 系统默认 node 为 v22.14.0（`/exec-daemon` PATH 前缀遮蔽 nvm——R33-F…R39-H 在案陷阱）——按令**开工即**显式钉 PATH 到 `~/.nvm/versions/node/v22.22.2/bin`，每个官方批次前在同 shell 复核 v22.22.2（anchor 记录轮 ×3 / 特征化 ×3 / census ×3 全部带内自报），**零批次落在 v22.14.0 上**；harness 重建后一次 anchor 冒烟运行（全绿 12/12、默认链 9171 带内）按纪律弃置，无其他弃置批。

## 结论

**诚实的空枚举：第四十组没有可立 ID 的新角度，不铸任何 S40-H-\* ID，不铸新 face 名，不取 ALLCAPS 绰号，不写入 EXCLUSIONS 表行（维持 545 行），生产代码零改动（R12-H…R39-H 二十八连空之后的第二十九连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮按派单纪律收口 H 区：从零重读 21 个切片文件与全部 9 个生产消费文件（§0.2）、在本 VM 重建全部锚点（R30-H 附录 A anchor 驱动逐行复刻 ×3 记录轮 + 3 次特征化运行，R31-H 附录 A census 驱动逐行复刻 ×3，§1）、对全部执行站点做逐格属主归属（§2.1）。哨兵检查仅作卫生应用（静态划界哨兵 + 在案重开条件逐条核对，§2.4），**不**铸「再查一遍重开条件」式新面；R32-A 哨兵方法 / R32-B 闭合稳定性纪律同法条引用不重铸。结果是**零未点名站点、零新站点级换名记录**——从零重推出的候选角度与 R35-H §2.2 八项重赛角度**逐一重合**（R36-H / R37-H / R38-H / R39-H 同款结果；按令不重铸，§2.2：每项均复核为既有排除行本体 / plan-ban 属地 / 无号关闭探针 / 硬不变量面的复命中），姊妹轮 R40-A…R40-G、R40-I 八份与 R39-A…R39-J 十份空枚举的记录家族在 H 侧的镜像面经本轮 rg 直证全部落在既有行上或结构性空（§0.3 / §2.2 划界段）；无一站点达到「从未在任何 H 报告点名」的新站点门槛。锚点全数落在历史带内或 ns 级两侧擦边（本 VM 为 R29-H/R30-H 邻域**中间窗**主机第四连轮，默认链六采样 5+1 分布；特征化 run6 为全腿同步偏暖的单轮暖窗采样——冻结 0 行按令不构成发现，§1 判读）。空枚举与 R12-H…R39-H、R40-A…R40-G、R40-I 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 21 文件自 `fd437a9` 逐字节零变化（派单 tip `1ae917b` 建支时、测量与 census 完成后、两次快进对齐 `f777e1c` / `898e11f` 后各一、提交前核对均 **0 行**，五点覆盖全程；`git diff --find-renames --name-status fd437a9..HEAD -- <切片>` **空输出**——零改名零增删；`git log -1 -- <切片>` 显示最后一次触碰切片文件的提交仍为战役前特性提交 `80a5780`（M3-T6，`fd437a9` 的祖先）、`git log fd437a9..HEAD -- <切片>` = 零提交）；最强冻结 `git diff 89f0455..HEAD -- src/` = **0 行**（R10 以来 docs-only 不变；快进段 `1ae917b..898e11f` 亦纯 docs）。锚点复测（记录轮 ×3 + 特征化 ×3，§1）：默认态链 **9134 / 9027 / 8907 ns**（与特征化 8894 / 9098 共五采样落 R29-H 带 8889–9147 内；特征化 9197 落 R30-H 带 9178–9647 内——六采样 **5（R29-H）+1（R30-H）** 分布，本 VM 为 R29-H/R30-H 邻域**中间窗**主机第四连轮（R37-H 3+2+1 / R38-H 2+3+1 / R39-H 2+4 同型），且全部六采样带内零擦边、全距 8894–9197 比 R38-H（8866–9398）更收敛；派单默认对照 R39-H 9201/9008/9247 中位同域略冷）；配置 A **8020 / 8627 / 8718 ns**（特征化 7913 / 8080 / 8422——**六采样全落** R25-H 带 7.55–9.26 µs 内，A 腿全带为 R31-H/R33-H…R38-H 七次后的**第八次**，R39-H 暖窗孤点本轮未再现）；配置 B **7202 / 5908 / 5810 ns**（特征化 6798 / 7054 / 8914——7202/6798/7054 三采样落 R28-H 带 6393–8312 内；5908/5810 低于 R28-H 带底 7.6%/9.1% 但**完全落 R25-H 带 4.11–6.70 µs 内**——B 腿横跨两历史带正是 R30-H 三度既判「B 腿宽窗口抖动非结构迁移」家族本体（R30-H 6078–8433 同型跨带）；8914 高出 R28-H 带顶 +7.2%、高出 R30-H 历史最高观测 8433 +5.7%，为暖窗 run6 采样（同 run promotion 25.1 / critique 627 / pairwise 2218 / reconcile 82 全腿同步抬升 = R39-H 特征化 9005 同款同 run 宿主争用签名）——同批带内、带下、带上并存 = 窗口采样特征，冻结 0 行不作发现）；promotion **18.2 / 22.3 / 17.9 ns**（特征化 19.5 / 21.6 / 25.1——19.5/21.6 两采样落 R30-H 观测域 19.0–22.2 内、22.3 高出域顶 +0.1 ns；17.9/18.2 与 R32-H 17.7、R35-H 17.8/17.9、R37-H 17.8–18.8、R39-H 17.7/17.8 低侧先例同类；25.1 为单读高侧擦边（+2.9 ns，浅于 R38-H run1 27.6 的 +5.4 ns 同位先例）——ns 级两侧窗口项第十次注记）；四个零生产调用方命令类 ≤2.22 µs 域内或 ns 级擦边（pairwise 2045–2218，五采样 ≤2.11 µs 域内、2218 高出 +5.1%（暖窗 run6；R38-H 2211 +4.8% 越沿先例同判，测试-only 面）；evaluate 1107–1170（1107/1124/1152/1152 落 R36-H 观测域 1085–1157 内、1165/1170 高出 +0.7%/+1.1%（绝对 ≤ +13 ns，浅于 R37-H +1.7% / R38-H +3.6% / R39-H +6.1% 同向先例））；reconcile 59–82（59/62/63/64/65 五采样落历域 59–71 内、59 与 R34-H/R37-H 低沿逐位同值；82 为暖窗 run6 单读（R30-H run2 87 单轮离群先例同判，ns 级抖动））；critique 类 577–627 ns（577/586/586/589 落 R33-H 域 569–596 内、597 高出 +1 ns 且与 R38-H 597 / R39-H 特征化 597 **逐位同值**；627 为暖窗 run6 单读 +31 ns（+5.2%，浅于 R39-H 特征化 638 的 +42 ns 同位先例）——ns 级单侧高沿与域内采样同批并存，不改域）；冷首调 dist 中位 **1677–1717 µs**（六采样全落 R6-H…R39-H 1.6–1.8 ms 带内；3-module import 3.2–3.3 ms——once-per-process 否决类归属不变）。census 驱动 **25/25 ×3 全过**（D3 天花板价 mint 38.8–41.8 ns / normalize 208–215 ns，§1——40.2 落 R31-H 带 39.7–40.3 内、41.8 落 R36-H 高侧观测域 41.4–43.9 内、38.8 低于 R31-H 带底 2.3%（绝对 −0.9 ns，R35-H 低侧 39.4/39.6 先例同向再低 0.6 ns 的 ns 级低沿）；normalize 208/215/215 三读全落 R32-H 观测域 202–228 内——清零上界 ~0.00026 ms/run，距 10 ms 落地线 4.6 个数量级，D3 门维持关闭）。时钟普查恰一命中 `src/rubric/types.ts:35`（R29-E 属地零触碰）。生产调用面 rg 复核与 R3-H…R39-H 恒定（9 文件 7 值级 + 2 纯类型，§0.2）。切片零 fs/codec/进程边界位点；切片 `Math.*` 恰 1 处（critic.ts:43 `Math.max` = BINDX 同坐标非新轴）。S5-H-1 前置守卫逐字在位、三元组两驱动九轮全过。anchor 驱动 12/12 ×6、census 25/25 ×3 全绿。聚焦切片套件 **76/76**；`pnpm gate` **全绿**（tests 1169 / pass 1168 / fail 0 / skipped 1，exit 0，三 tip 各一次）。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。仓库变更仅本报告一个文件。不开 PR。

## 0. 冻结、快进处置、生产调用面与划界

### 0.1 冻结与快进

- 分支按令创建于派单 tip `1ae917b`。冻结五点核对：派单 tip 建支时 / 测量与 census 完成后 / 两次快进对齐 `f777e1c` / `898e11f` 后各一 / 提交前，`git diff fd437a9 -- src/evaluation src/requirement src/review src/rubric` 均 **0 行**；最强冻结 `git diff 89f0455..HEAD -- src/` 全程 **0 行**；`--find-renames --name-status` 同区间**空输出**（零改名零增删，无改名裁决需做）。
- 测量与首轮门禁后 fetch 先后检测到**两次纯 docs 快进** `1ae917b` → `f777e1c` → `898e11f`（`f777e1c`：R40-G 摄取 + R40-J 派单，恰 3 个 docs 文件 PROGRESS / PLAN / R40-G.md，228 插入 16 删除；`898e11f`：R40-I 摄取，恰 3 个 docs 文件 PROGRESS / PLAN / R40-I.md，218 插入 11 删除；两段 `-- src/ test/ package.json pnpm-lock.yaml` 各 **0 行**直证），按令逐次 `git merge --ff-only` 零冲突对齐并各重验冻结。各 tip 间 `src/ test/ package.json pnpm-lock.yaml` 逐字节相同 ⇒ 锚点与 census 结果原样携带；聚焦套件与 `pnpm gate` 在 `f777e1c` 与 `898e11f` 两树各重跑（§3）。提交前重取 origin 复核无更多快进。
- `wc -l` 逐目录实测：evaluation **655**（evaluator 121 / delivery-adapter 97 / diff-adapter 84 / ownership 47 / precedence 28 / check-adapter 147 / adapters 68 / types 63）+ requirement **712**（normalizer 115 / provenance 42 / precedence 59 / extractor 132 / heuristic 220 / coverage 100 / critic 44）+ review **309**（pairwise 141 / self-review 54 / reconcile 47 / critic 67）+ rubric **74**（registry 37 / types 37）= **1750**，与 R32-H…R39-H 同数；文件普查恰 21（8+7+4+2）。
- `package.json` / `pnpm-lock.yaml` 对 `fd437a9` **0 行**；`git log -1 -- <切片>` = `80a5780`（战役前 M3-T6 特性提交，`fd437a9` 祖先）；`git log fd437a9..HEAD -- <切片>` = 零提交。EXCLUSIONS.md 逐点核对 **545 行**（快进对齐后复核仍 545）。

### 0.2 生产调用面复核（rg import 搜索，本 tip 实测，第四十次）

9 文件 7 值级 + 2 纯类型，与 R3-H…R39-H 恒定，import 行逐条核对：值级——`track/clarify.ts`（`extractHeuristicContract`，:4，另 :3/:5 类型）、`track/plan.ts`（`shouldScout`，:5）、`track/loop.ts`（`applyPrecedence`，:38）、`run/supervisor.ts` / `run/flowchart-run.ts` / `run/coordinator.ts`（`assertCoverageAllowsStart`，:27/:21/:31，调用位点 :500/:759/:226）、`adaptation/promotion-rules.ts`（`assertCanPromoteFromReview`，:4）；纯类型——`track/primary-split.ts`（`HeuristicHabits`，:6）、`adaptation/promotion.ts`（`ReviewerKind`，:10）。13/21 切片文件零生产调用方（evaluation 全部 8 文件 + review/{pairwise,reconcile,critic} + rubric 全部 2 文件）第四十次成立。9 个消费文件本轮全部从零复核调用位点，调用形态逐字不变：clarify :37 恒以 `{objective, habits}` 调 extract（S=1 sources 钉死）、loop.ts:80 恒传 `"user-first"`、run 侧三处 gate 调用条件透传 `resolvedQuestionIds` 形态逐字在位（supervisor :506–507 / flowchart-run :765 / coordinator :232–233，§2.4 D2）。

### 0.3 姊妹面划界与非移植清单（R40-A…R40-F 六份派单折入 + 快进带入 R40-G / R40-I + R39-A…R39-J 十份折入）

- **R40-A**（tracking 空枚举）：三项新站点级换名检查经查全部归 tracking 物理对象既有属主拒列，与 H 零共享物理对象；预算锚 13.8–17.2 µs/gate 为 A 属地读数。其记录家族的 H 侧镜像面维持 R38-H/R39-H 镜像划界结论（属性双读 CSE 家族 H 侧 = R32-H §2.2-1 evidence 双下标本体；单元素/字面量数组每调用新铸家族 H 侧 = heuristic questions/deliverables 数组构造 once-per-run µs 拒收类 + R31-H 归一化属地；投影 map 家族 H 侧 = coverage/extractor/reconcile 的 `new Set(x.map(...))` 站点既有行）——复命中即止，零新记录。
- **R40-B**（live 路由空枚举）：**零**新站点级换名记录；ceiling M=2 8.91–9.98 / M=10 17.13–18.30 为 B 属地读数；S12-B-2 仍未触发、S13-B-1 仍 loser ID（属地哨兵，H 不代跑）。
- **R40-C**（离线路由空枚举）：**零**新站点级换名记录（两处独立重瞄归既有属主）；中位 663.3–672.6 / 组合天花板 24.3–31.8 / sink=7.309 均 C 属地读数。
- **R40-D**（adaptation 空枚举）：**零**新站点级换名记录（两处独立重瞄归既有属主）；S0c 3.84–4.03 ms 为 D 属地读数；S9-D-4 / S12-D-1 维持关闭（D 属地哨兵）。R36-D 四家族的 H 侧镜像面维持 R36-H…R39-H §0.3 划界结论（家族 1 parse/load 闭包 H 侧结构性空——本轮合并 rg 直证 `JSON.parse` + fs/codec 位点 **0**；家族 2 `new Set(x.map(...))` H 侧四站点全落既有行：provenance.ts:30 = R30-H plan-ban 属地、extractor.ts:83 = D4 门机制内部、coverage.ts:52 = S1-H-1 属地、reconcile.ts:39 = 零生产调用方 + S1-H-6 邻域；家族 3 同谓词双遍融合 = S5-H-1 本体与 R30-H plan-ban 既有行；家族 4 `.filter().length` 计数恰 reconcile.ts:25–26 = S1-H-6 本体、`Object.values` 恰 registry.ts:32 非计数站点）——复命中即止，零新记录。
- **R40-E**（learning 空枚举）：**零**新站点级换名记录（六项属主再验 + 两项独立重瞄归 S3-E-3 本体与 S13-B-1 真子集）；SLICE-CPU 15.0/14.9/14.9 为 E 属地读数；S8-E-1 恰 3 生产位点直证为 E 属地，H 不代跑。
- **R40-F**（experiments 空枚举）：**零**新站点级换名记录（§0.3 十项重推角度全数复命中既有 F 行）；r5f 128.72/120.68/119.46、r6f 120.83/116.95/121.01、r7f 130.84/128.61/128.83 均 F 属地读数；S7-F-1 ≠ S6-F-5 维持（F 属地）。与 H 共享点仅消费者文件 `adaptation/promotion-rules.ts` 自身（双方零改动，交点为消费者文件而非切片物理对象）。
- **R40-G**（快进折入，全文实读）：runtime/graph/domain 空枚举——**零**新站点级换名记录（其 §3.0 边际角度经 rg 先例检索逐一命中先前轮次已点名站点本体，零项需新作家族真子集判定；compute 0.289–0.294 vs I/O 93.5–95.4、digest `06cbcf92c098c8f0` 第三十一次相同均 G 属地读数，按令不铸 digest 轴号）。与 H **零共享物理对象**（其切片含 H 的三个 run 侧 gate 消费文件 supervisor / flowchart-run / coordinator——双方零改动，交点为消费者文件自身而非切片物理对象）。已并入非移植清单，不重编号、不移植、不重提。
- **R40-I**（快进折入，全文实读）：cli + pi-adapter + config + telemetry 空枚举——**零**新站点级换名记录、零残余候选（六类 custom−builtin 带与 R39-I/R38-I 世系带族全部相交；r4i/r5i/r7i 等价性仿真 68/119/80 checks 全 0 失败；其 22.14 对照腿后窗宿主刻度下移判读为 I 属地注记）。与 H **零共享物理对象**（cli/pi-adapter/config/telemetry 四目录零 H 切片导入；S8-I-1 两侧 file-level 封死为 I 属地哨兵，H 不代跑）。已并入非移植清单，不重编号、不移植、不重提。
- **R39-A…R39-J 十份空枚举**（按令折入非移植）：各属地零新站点级换名记录或全拒收，均未铸新面——R39-H 即本切片上一遍（默认对照，§1：默认链 9201/9008/9247、其暖窗孤点（A 腿 10279/12000、B 腿特征化 9005、critique 特征化 638、evaluate 特征化 1227）均已按 R32-H 既判家族价签记录不改带，本轮不重铸其「零新站点级记录」为新面）；R39-G 与 H 共享点仅三个 run 侧 gate 消费文件自身（双方零改动）；其余八份与 H 零共享物理对象。均折入非移植清单，声明即止。
- **非移植清单全额维持并逐项核对**（派单 DO NOT REMINT 全清单 + round-40/PLAN H 段快进前后两版）：S5-H-1 keep-list（§2.4 逐字直证在位）；R35-H §2.2 八项重赛角度不重铸（§2.2）；R39-H / R38-H / R37-H / R36-H / R35-H 零新站点级记录（本轮不重铸其「零」为新面）；R32-H 三项换名记录（evidence 双下标 CSE / `Object.hasOwn` / `DEFAULT_REGISTRY` 惰性拷贝）不重铸；provenance-resolution pathway（R30-H）plan-ban；R31-H 四扇残门 D1–D4 停驻（§2.4 重开条件全未触发）；R32-A 哨兵方法 / R32-B 闭合稳定性审计 / R33-A 44/44 A-账本卫生仅法条引用不铸面；I/O text-codec topology / per-run transcode-byte manifest（R31-E——切片 fs/codec 位点 0，结构性空）；REPRX（`JSON.parse` 位点 0）；MODEX（chmod/umask 位点 0，零盘上工件）；FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX 全部未编号未移植；R33-A…R39-J 空枚举 + R40-A…R40-G / R40-I 空枚举（上列已划界，声明即止）；宿主偏置窗（冻结 0 行）不作新发现；「不铸『我又查了一遍重开条件 / 闭合稳定性 / 残门 / 无名站点』式新面」遵守——本节全部为既有行复命中裁决，无一铸新。
- 未重开任何 X\* / S1-\* … S39-\* 条目。**单变量纪律**：本轮全部计时臂来自两个历史附录逐行复刻驱动（附录 A 指纹机验），零新计时臂；§2 全部为静态 rg / 读码断言（不计时）。S8-E-1 / S8-I-1 / S9-D-4 / S12-D-1 / S12-B-2 / S13-B-1 / S7-F-1 ≠ S6-F-5 等姊妹区维持项零触碰（属地哨兵由 R40-B/R40-C/R40-D/R40-E/R40-F/R40-G/R40-I 各自复核未触发）。catalog 行序契约（R28-B W3）与 R28-I npm POSIX `.bin` symlink 静默 no-op 零触碰。

## 1. 锚点全数重测（历史仪器，记录轮 ×3 + 特征化 ×3）

仪器：`/tmp/r40h-anchor.mts`（242 行，R30-H 附录 A 驱动的逐行复刻，仅头注释更新为 R40-H 标注）与 `/tmp/r40h-census.mts`（233 行，R31-H 附录 A census 驱动的逐行复刻，仅头注释更新为 R40-H 卫生应用标注）；两驱动与归档原文逐行 diff 的全部差异行均在头注释块内（anchor 恰第 2/11 行、census 恰第 2/16 行，非注释差异行过滤 = **0 行**，零代码行差异）；重抽取附录原文 sha256 与 R30-H/R31-H 归档指纹逐字符相符（`b0251e6f…` 242 行 / `0f69d3ec…` 233 行），本轮复刻指纹见附录 A。`pnpm build` 后 `node --expose-gc --import tsx <driver>`，nvm Node v22.22.2（`which node` + `node --version` + `process.versions.v8` 计时前三重复核；PATH 钉死绕开 `/exec-daemon` 系统 v22.14.0）；烟测一批（12/12 全绿、默认链 9171 带内）按令弃置后开官方批。

| 锚点 | run1 | run2 | run3 | 历史带对照 |
|---|---|---|---|---|
| 默认态链（extractHeuristicContract + run-start gate(Q=0) + applyPrecedence，once-per-run） | 9134 ns | 9027 ns | 8907 ns | 三记录轮与特征化 8894 / 9098 共**五采样**落 R29-H 带 8889–9147 内；特征化 9197 落 R30-H 带 9178–9647 内。六采样 **5+1** 分布带内零擦边、全距 8894–9197 比 R38-H（8866–9398）更收敛——本 VM 为 R29-H/R30-H 邻域**中间窗**主机第四连轮（R37-H 3+2+1 / R38-H 2+3+1 / R39-H 2+4 同型）；派单默认对照 R39-H 9201/9008/9247 中位同域略冷 |
| 配置 A（habits set，Q=1 gated） | 8020 ns | 8627 ns | 8718 ns | R25-H 带 7.55–9.26 µs：**三轮全带内**；特征化 7913 / 8080 / 8422 亦全带内——**六采样全带**，A 腿全带为 R31-H/R33-H/R34-H/R35-H/R36-H/R37-H/R38-H 七次后的**第八次**（R39-H 暖窗孤点 10279/12000 本轮未再现） |
| 配置 B（vague，Q=3 gated） | 7202 ns | 5908 ns | 5810 ns | R28-H 带 6393–8312：7202 与特征化 6798 / 7054 三采样带内；5908 / 5810 低于带底 7.6%/9.1% 但**完全落 R25-H 带 4.11–6.70 µs 内**——B 腿横跨两历史带 = R30-H（6078–8433 同型）三度既判「宽窗口抖动非结构迁移」家族本体，R34-H/R36-H/R37-H/R38-H/R39-H 带下先例同向；特征化 8914 高出 R28-H 带顶 **+7.2%**（高出 R30-H 历史最高观测 8433 +5.7%）为暖窗 run6 采样（同 run 全腿同步抬升 = R39-H 特征化 9005 同款宿主争用签名，§1 判读）——同批带内、带下、带上并存 = 窗口采样特征，冻结 0 行不作发现 |
| promotion 类 `assertCanPromoteFromReview`（once per promotion） | 18.2 ns | 22.3 ns | 17.9 ns | R30-H 观测域 19.0–22.2：特征化 19.5 / 21.6 两采样域内；22.3 高出域顶 +0.1 ns；17.9 / 18.2 与 R32-H 17.7、R35-H 17.8/17.9、R37-H 17.8–18.8、R39-H 17.7/17.8 低侧先例同类；特征化 25.1 为单读高侧擦边（+2.9 ns，浅于 R38-H run1 27.6 的 +5.4 ns 同位先例）——ns 级两侧窗口项第十次注记 |
| evaluate 类 `createEvaluationRecord`(3 criteria)（测试-only 面） | 1152 ns | 1107 ns | 1124 ns | 历轮 ≤2.11 µs 命令类域内；对照 R36-H 观测域 1085–1157：1107/1124/1152 与特征化 1152 四采样域内；特征化 1165 / 1170 高出 +0.7%/+1.1%（绝对 ≤ +13 ns，浅于 R37-H +1.7% / R38-H +3.6% / R39-H +6.1% 同向先例）——ns 级高侧擦边；零生产调用方归属不变 |
| critique 类 `critiqueContract`（run-start 链上） | 577 ns | 589 ns | 586 ns | R33-H 观测域 569–596：577/586/589 与特征化 586 域内；特征化 597 高出 +1 ns 且与 R38-H 597 / R39-H 特征化 597 **逐位同值**；特征化 627（暖窗 run6）高出 +31 ns（+5.2%，浅于 R39-H 特征化 638 的 +42 ns 同位先例）——ns 级单侧高沿与域内采样同批并存，不改域 |
| pairwise 类 `runBlindPairwisePair`（测试-only 面） | 2104 ns | 2045 ns | 2090 ns | ≤2.11 µs 命令类域：2045–2104 与特征化 2072 / 2074 五采样域内；特征化 2218（暖窗 run6）高出 +5.1%（R38-H 2211 +4.8% 越沿先例同判，测试-only 面） |
| reconcile 类 `reconcileReviews`(n=2)（测试-only 面） | 62 ns | 64 ns | 63 ns | 历域 59–71（R32-H 62–64 / R34-H 59–63 / R35-H 61–71 / R36-H 59–71 / R37-H 59–70 / R38-H 60–66 / R39-H 62–66）：62/63/64 与特征化 59 / 65 五采样历域内（59 与 R34-H/R37-H 低沿逐位同值）；特征化 82（暖窗 run6）单读越沿（R30-H run2 87 单轮离群先例同判，ns 级抖动） |
| 冷首调 dist（5 fresh children 中位） | 1717 µs | 1693 µs | 1690 µs | R6-H…R39-H 1.6–1.8 ms 带：**六采样全带内**（特征化 1694/1702/1677）；3-module import 3.2–3.3 ms——once-per-process 否决类归属不变 |
| anchor 驱动断言 | 12/12 | 12/12 | 12/12 | 特征化 run4/5/6 亦 12/12（×6 全过） |

组件行（once-per-run 链内份额，全部 ns 级）：extract 腿 7908–8157（默认）/ 6920–7691（A）/ 5117–8087（B）；gate 腿 495–822（三态）；applyPrecedence 腿 114–260（三态）。

census 驱动（语义见证 + 生产组成静态普查 + D3 天花板价，R31-H 附录 A 逐行复刻的卫生应用）：**25/25 ×3 全过**。D3 信任标记机制天花板价：`createTrustedSource`(prod shape) **41.8 / 40.2 / 38.8 ns/铸** × `normalizeSources`([marked] 1 源，含 symbol 读 + 3 信号正则 + slice）**215 / 208 / 215 ns/调用**；生产流量每 run 恰 1 铸 + 1 读不变。对照 R31-H 带 39.7–40.3 / 199–204：mint 40.2 带内、41.8 落 R36-H 高侧观测域 41.4–43.9 内（R37-H/R38-H 毗邻同域先例）、38.8 低于带底 2.3%（绝对 −0.9 ns——R35-H 低侧 39.4/39.6 先例同向再低 0.6 ns 的 ns 级低沿，跨轮两侧采样并存）；normalize 208/215/215 三读全落 R32-H 观测域 202–228 内。即使整机制清零，收益上界仍 ~0.00026 ms/run，距 ≥10 ms 落地线 **4.6 个数量级**，D3 门维持关闭，不改带、不重铸。S5-H-1 三元组两驱动九轮全过。

判读：本 VM 时间窗为 R29-H/R30-H 邻域**中间窗**主机第四连轮（默认链六采样 5+1 分布、带内零擦边，与 R37-H/R38-H/R39-H 同型且更收敛），叠加 **B 腿宽窗抖动**（记录轮 5810–7202 跨 R25-H/R28-H 两带，R30-H 三度既判家族）与**特征化 run6 全腿同步偏暖的单轮暖窗**（B 8914 / promotion 25.1 / critique 627 / pairwise 2218 / reconcile 82 同 run 同向 = 宿主争用签名，R39-H 特征化 run4/run5 同款先例）——同批三腿异向 + 单 run 全腿同向正是宿主窗口采样特征而非结构迁移；冻结差分 0 行 + 引擎位级同一 + 仪器逐行复刻（指纹机验）为最强证据，按派单令「host-window samples under a 0-line freeze are NOT findings」不作发现、不改带。残余越域读数全部为 ns 级两侧擦边或暖窗单读（promotion +0.1/+2.9 ns、evaluate +8/+13 ns、critique +1/+31 ns、pairwise +108 ns、reconcile +11 ns、census mint −0.9 ns）——两侧并存同判。**没有任何锚点越过或接近 10 ms 落地线**——整条默认链清零也只有 ~0.0089–0.0092 ms/run。

## 2. 第四十遍枚举（诚实结果：空；零新站点级换名记录）

### 2.1 逐文件重读与裁决（零未点名站点）

21 文件全文重读（§0.1 行数），每个执行站点归属既有 ID / 无号探针 / 停驻门 / plan-ban 轴：

| 文件（行数） | 站点归属（全数指到既有裁决） | 新候选 |
|---|---|---|
| `evaluation/evaluator.ts`（121） | 零生产调用方命令类（第四十次确认）；criterion 循环 + `input.evidence[criterion.id]` 双下标 = R32-H §2.2-1 换名记录拒列维持（R24-H 操作数驻留类 + 测试-only 面 + µs 拒收）；overall 三遍 some/every 归约 = 既往轮命令类裁决（R35-H §2.2-5 复命中原文维持）；validateEvaluatorScope 线性扫描 = 同类；EvaluationTarget/IndependenceClass 校验臂 = 行为承载边界校验 | 无 |
| `evaluation/delivery-adapter.ts`（97） | 零生产调用方；公开 `evaluate` 签名 = S7-H-1/X0-4 禁区；isDeliveryEvidence 四合取结构卫 = 行为承载边界校验（ABSTAIN/UNOBSERVED 语义面，注释在案）；证据映射构造 = 命令类 | 无 |
| `evaluation/diff-adapter.ts`（84） | 同上类；isDiffScope 结构卫 + 三态分派 = 命令类；classifyDiffScope 委派 = ownership 属地 | 无 |
| `evaluation/ownership.ts`（47） | 规则表线性扫描 Set 化已既往裁决拒收（零生产调用方 + 规模恒小）；`rule.pattern.test` 为已提升的预编译模式（复命中对照组）；`context.changeSet.includes` 线扫 = 同类；R31-D 死码先例同法条 | 无 |
| `evaluation/precedence.ts`（28） | 小表查找三函数零生产调用方；`EVIDENCE_PRECEDENCE.find` 线性 3 元素 = 表驻留 R24-H 类 | 无 |
| `evaluation/check-adapter.ts`（147） | `changeSetsEqual` = S1-H-9 属地（:119–120 `new Set(a)`/`new Set(b)` 为其本体、非 map 中间数组家族成员——§0.3 划界）；`hashArtifact` 增量化已既往拒收；:101 `Array.isArray`+`every` 结构卫 = 命令类边界校验（行为承载），CKMIX 邻域；attributionMetadata 全字段装配 = 归因契约本体（不可省字段） | 无 |
| `evaluation/adapters.ts`（68） | 接口/类型 + 工厂；无热执行站点 | 无 |
| `evaluation/types.ts`（63） | 纯类型 | 无 |
| `requirement/normalizer.ts`（115） | `extractSignals` 3 信号正则 = R31-H 归一化属地（census 天花板价 208–215 ns/整调用显式计入）——行内字面量提升候选 = R35-H §2.2-3 复命中，按令不重铸；`createTrustedSource` × symbol 读 = **D3 停驻门**（§1 天花板价复测 38.8–41.8 / 208–215 ns）；`Object.freeze` + spread = D3 机制内部，同门；`buildContractFromSources` 零生产调用方 = R35-H §2.2-8 复命中，按令不重铸 | 无 |
| `requirement/provenance.ts`（42） | `findUnsourcedItems` 全站点 = **R30-H provenance-resolution pathway** plan-ban 属地，零触碰；三遍 filter+map 融合候选 = R35-H §2.2-2 复命中，按令不重铸；:30 assumptionIds `new Set(map)` = 同 plan-ban 属地（§0.3 家族 2 划界） | 无 |
| `requirement/precedence.ts`（59） | **S5-H-1 前置守卫 :24 在位必须保留**（本轮 rg 逐字直证 `if (fast.length === 0 \|\| slow.length === 0) return [];`）；:22–23 fast/slow 双 filter = S5-H-1 排除行本体（融合候选 = R35-H §2.2-4 复命中，按令不重铸）；:41 头/尾二值退化 = **D1 停驻门**（切片外规则字面量普查恰 1 处 `"user-first"` @ track/loop.ts:80，§2.4）；:40 `conflict.ids.includes` = 冲突臂生产死平面（D1 见证 covered），守卫之后 = S5-H-1 本体 | 无 |
| `requirement/extractor.ts`（132） | `minimumConfidence` 默认 0.8（:78）+ heuristic 传 0.8（:189）逐字冻结（无阈值改动不变量，§2.4）；:83 sourceRefs `new Set(map)` = D4 机制内部（§0.3 划界）；`assertAuthorityGrounding` 循环（含 :124 `grounding.find` 每 authority 线扫）+ inference 标注扫描 + 注入 extractor/critic 可调用对 = **D4 停驻门**（生产恒空/零流量 + R31-A injected-callable 停驻地），不重开 | 无 |
| `requirement/heuristic.ts`（220） | `createTrustedSource` 铸点 :177 = D3 门（每 run 恰 1 铸）；isVague / namedTargets / shouldScout / wantsTests 行内正则与 questions 数组构造 = R31-H 归一化属地 + TERMCLASS 正则族 + once-per-run µs 拒收（字面量提升候选 = R35-H §2.2-3 复命中，按令不重铸）；`q-scope`/`q-write` 恒假 `questions.some` 守卫 = R35-H §2.2-7 复命中（R12-H…R24-H 无号关闭探针），按令不重铸；PATH_RE 模块级提升先例（对照组）；回溯担忧不适用（输入受限、锚点未见退化——本轮 extract 腿 5117–8157 ns 全 µs 域） | 无 |
| `requirement/coverage.ts`（100） | `checkCoverageGate` O(C×K) = **S1-H-1 属地** + R29-H incidence topology plan-ban；:52 contractIds `new Set(map)` = 同属地（§0.3 划界）；Set 提升在生产规模已拒收；:27 own-key 合取化简 = R35-H §2.2-6 复命中（**S4-H-1 fail-open 家族** + R32-H §2.2-2 记录维持），按令不重铸；:82 `new Set(resolvedQuestionIds)` = D2 门消费端（§2.4）；:83–89 gated contract spread + questions map = D2 机制内部 once-per-run（gate 腿 495–822 ns 整体计价）；失败路径消息装配 = 失败臂-only；`assertCoverageAllowsStart` 3 生产调用方不变（§0.2） | 无 |
| `requirement/critic.ts`（44） | `critiqueContract` 中间数组融合已既往拒收（run-start 链 577–627 ns 全量，§1）；:29–30 双 some 探针 = S5-H-1 语义面同族（既往裁决）；:20 checks 小写映射 = 同函数同计价；:43 `Math.max(0, score)` = **BINDX 同坐标**非新轴（切片唯一 `Math.*`，§2.4） | 无 |
| `review/pairwise.ts`（141） | 零生产调用方（测试-only 面，第四十次确认）；blind 比较与盲名映射（presentInput/remapToOriginalIds 对象对）= 既往命令类；2.05–2.22 µs 天花板复测五采样域内 + 一读暖窗 +108 ns 擦边（§1） | 无 |
| `review/self-review.ts`（54） | `applyRoutingScoreUpdate` 中间对象（:37 `RoutingScoreUpdate`）= V8 标量替换已既往裁决；`assertCanPromoteFromReview` 自评拒绝分支 = 行为承载（fail-closed）不可动；spread 转发（:51）= 显式字面量既往拒收（R35-A 享元族 H 镜像面属地）；promotion 类 17.9–25.1 ns 复测（§1 两侧窗口项注记） | 无 |
| `review/reconcile.ts`（47） | 零生产调用方；n=2 归约 59–82 ns；:25–26 双 filter 计数 = **S1-H-6 排除行本体**（融合候选 = R35-H §2.2-1 复命中，按令不重铸；R36-D 家族 4 的 H 侧镜像站点，§0.3 划界）；:35 dissent filter = 同函数同裁决；:39 causalDefects `new Set(map)` = 同函数测试-only 面（§0.3 划界） | 无 |
| `review/critic.ts`（67） | 零生产调用方；`createCriticObservation` 构造 = 命令类；actorDefense 拒绝臂 = 行为承载（critic 隔离契约）；hasPass some = 同类 | 无 |
| `rubric/registry.ts`（37） | **S1-H-8**（就地写已拒）+ **S2-H-6**（外层 spread 移除已拒）双属地维持；`DEFAULT_REGISTRY` 惰性拷贝 = R32-H §2.2-3 换名记录拒列维持（once-per-process ns）；:32 `Object.values` = listRubrics 零生产调用方命令类（R36-D 家族 4 划界非计数站点，§0.3） | 无 |
| `rubric/types.ts`（37） | `createRubric` 的 `new Date().toISOString()`(:35) = **R29-E 时钟属地**（切片唯一时钟位点，普查恰 1 命中）零触碰 | 无 |

### 2.2 换名拒绝检查：本轮零新站点级记录

从零重推（假装排除表不存在重读 1750 行）枚举出的候选角度与 **R35-H §2.2 的八项重赛角度逐一重合**（R36-H / R37-H / R38-H / R39-H 同款结果）——派单令明文禁止重铸该八项（`reconcileReviews` 双 filter = S1-H-6 本体；`findUnsourcedItems` 三遍 = R30-H plan-ban；行内正则字面量提升族 = R31-H 归一化属地 + TERMCLASS + census 整调用计价；`detectConflicts` fast/slow 双 filter = S5-H-1 keep-list 本体；`createEvaluationRecord` some/every = 命令类 + µs 拒收；`checkCoverageGate` own-key = S4-H-1 fail-open 家族 + R32-H §2.2-2；q-scope/q-write `questions.some` = R12-H…R24-H 无号关闭探针；`buildContractFromSources` = R10-H 零调用方 + X0-4）。本轮处置：每项在 §2.1 对应文件行内以属主行复核收口（站点原形在位读码直证 + 本轮天花板价引用），**不重列为编号清单项、不铸任何新记录**。R32-H 三项既有换名记录（evidence 双下标 CSE / `Object.hasOwn` / `DEFAULT_REGISTRY` 惰性拷贝）裁决逐字维持，不以任何新名重提。姊妹记录家族的 H 侧镜像面划界（R36-D 四家族 + R40-A 记录家族 + R40-B…R40-G / R40-I / R39-A…R39-J 各站点）全部落在既有行或结构性空（§0.3 rg 直证）——**零新站点级换名记录**。H 区经三十九遍逐格归属后已无「从未在任何 H 报告点名」的站点，零是更强的收口（R33-H…R39-H 同款判读）。

### 2.3 生产调用方普查（第四十次）

§0.2 全文即普查结果：9 文件 7 值级 + 2 纯类型，与 R3-H…R39-H 恒定；13/21 文件零生产调用方。`assertCoverageAllowsStart`×3 / `extractHeuristicContract`×1 / `applyPrecedence`×1 / `shouldScout`×1 / `assertCanPromoteFromReview`×1 逐位点核对无新增无减少。

### 2.4 静态划界哨兵与在案重开条件核对（卫生应用，不铸面）

- **BINDX**：切片 `Math.*` 恰 **1** 处（critic.ts:43 `Math.max(0, score)`）——与 R31-H…R39-H 计数相同，重开条件（新增内建绑定位点）未触发。
- **R31-E codec / REPRX / MODEX / 进程边界**：`node:fs|readFile|writeFile|Buffer|TextEncoder|TextDecoder|JSON.parse|chmod|umask|spawnSync|process.exit|stdin` 合并一次 rg 于 21 文件 **0 命中**——四类均无物理对象，维持「无对应物」划界。
- **时钟**：`new Date|Date.now|Math.random|randomUUID|hrtime|performance.now` 恰 **1** 处（rubric/types.ts:35，R29-E 属地）。
- **阈值**：`minimumConfidence` 赋值位点恰 2 处（extractor.ts:78 默认 0.8 / heuristic.ts:189 传 0.8），逐字冻结。
- **D1 重开条件**（规则臂获得生产流量 / 三值枚举去退化）：切片外 `src/` 规则字面量普查（排除 precedence.ts 类型联合本体）恰 **1** 处 `"user-first"`（track/loop.ts:80 `applyPrecedence(clarify.candidate.contract, "user-first")`，本轮 rg 直证）——`spec-first`/`latest-first` 生产流量仍为零，precedence.ts:41 头/尾二值退化逐字不变（census D1 见证项全过）。未触发，门维持停驻。
- **D2 重开条件**（resolved-set 组成变化）：track/loop.ts:101/:168 组成位点与 run 侧三消费位点（supervisor.ts:506–507、flowchart-run.ts:765、coordinator.ts:232–233）及门消费端 coverage.ts:82 本轮 rg 直证坐标逐一在位，条件透传形态逐字不变（最强冻结 0 行携带）——盈余外来 id 生产流量仍为零，剂量空间仍归 R29-H R2 坐标。未触发。
- **D3 重开条件**（机制替换收益规模化）：天花板价复测 ~0.00026 ms/run（§1），距落地线 4.6 个数量级。未触发。
- **D4 重开条件**（死平面获得生产流量）：`authority`/`inferences` 生产恒空组成不变（census 25/25 含该见证 ×3）；注入可调用对维持 R31-A 停驻。未触发。
- **S5-H-1**：precedence.ts:24 前置守卫本轮 rg 逐字直证在位，两驱动九轮断言全过。

### 2.5 枚举收口

零未点名站点；零新站点级换名记录（从零重推与 R35-H §2.2 八角度逐一重合、按令不重铸，姊妹家族 H 侧镜像面全落既有行或结构性空，R32-H 三项既有记录维持）；四扇停驻门重开条件全未触发；13 文件零生产调用方第四十次成立；全部锚点在历史带内或 ns 级两侧擦边（§1 判读——本 VM R29-H/R30-H 中间窗第四连轮 + B 腿宽窗跨带 + run6 单轮暖窗，冻结 0 行不作发现）。**没有可立 ID 的新角度。不铸 S40-H-\*，不铸新 face 名，不取绰号，不写 EXCLUSIONS 行。**

## 3. 测试与验证

- 聚焦切片套件（`test/unit/{evaluation,requirement,review}` 13 文件，rubric 套件位于 evaluation/rubric-registry.test.ts；引擎 `tsx --test`）：**tests 76 / suites 12 / pass 76 / fail 0**——在 `1ae917b` 树与两次快进对齐后 `f777e1c` / `898e11f` 树各跑一次（共三跑），计数逐字相同（nvm v22.22.2，与 R32-H…R39-H 记录逐字同计数）。诚实注记：开工时一次误用 vitest 运行器的调用（本仓测试引擎为 `tsx --test`）产出伪失败输出，当场弃置改正，非仓库问题、非弃置官方批。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0**（`1ae917b` / `f777e1c` / `898e11f` 三树各一次，计数逐字相同）；**tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1**（既有 provider-smoke 凭据跳过），与 R33-A…R39-J / R40-A…R40-G / R40-I 历史计数逐一相同。临时驱动文件已在首次 gate 前按纪律删除（附录 A；gate 前 `git status --porcelain` 空输出直证零临时件入树）。
- 硬性不变量逐条复核：双 LCB / 双归因在位；live = R0 等价（R1 未接线）；promotion proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值改动（minimumConfidence 0.8 冻结核对 §2.4）；无测试改动；CAS / 凭证 / 数据面合同 / 公开签名零触碰；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭（S4-H-1 / coverage-gate own-key 家族，§2.1 coverage 行拒列理由维持）；catalog 行序契约（R28-B W3）与 R28-I npm `.bin` symlink 静默 no-op 未触碰；分析不改在途 run；tracking 无指挥权；H/score 不写路由 PASS/FAIL；**S5-H-1 保持**；S8-E-1 / S8-I-1 / S9-D-4 / S12-D-1 / S7-F-1 ≠ S6-F-5 / S13-B-1 / S12-B-2 未重开未代跑（属地在姊妹区，H 侧零触碰）。
- 仓库变更仅本报告一个文件：`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` = 本文件新增。

## 4. 前后对比

无生产代码变更，无前后对比对象。默认预算带维持 R31-H…R39-H 记录（默认链本 VM 复测 8907–9134 ns 记录轮，R29-H/R30-H 中间窗；跨 VM 包络 3.9–10.4 µs 不变）。

## 5. MORE_OPTIMA 判定

- 21 文件零未点名站点；从零重推候选与 R35-H §2.2 八角度逐一重合（按令不重铸、R36-H…R39-H 同款结果）、零新站点级换名记录；停驻门 4/4 重开条件未触发；锚点全数带内或 ns 级两侧擦边（本 VM 中间窗第四连轮 + B 腿宽窗跨带 + run6 单轮暖窗并存，冻结 0 行）；整默认链清零上界 ~0.0092 ms/run，距 10 ms 落地线仍在其下，且其组成已逐段归属既有 ID/门。
- 判定：**MORE_OPTIMA=no**。

## 6. 新增排除 ID：无

不铸 S40-H-\*；不新增 EXCLUSIONS 表行（维持 545 行）；空枚举轴不取绰号；不重铸 R32-A 哨兵方法 / R32-B 闭合稳定性方法 / R31-H 残门审计 / R35-H §2.2 八项重赛角度 / 姊妹空枚举方法。

## 附录 A：仪器指纹与原始输出（未入库；运行后 /tmp 留存核后删除）

两驱动均为历史附录逐行复刻、仅头注释块更新为 R40-H 标注（纪律：不提交仓库；gate 前已删除）。复刻谱系机验：从 R30-H/R31-H 附录原文重抽取的驱动全文 sha256 与两报告归档指纹逐字符相符（`b0251e6f27e76c9ee705e0118c5f54753745d1f2067c9530d26b41ac30dacf0f` 242 行 / `0f69d3ecabacd4ec8b52251aa571544ffeff75203edb8bf8252a39400259bda6` 233 行）；本轮复刻与原文逐行 diff 的全部差异行均在头注释块内（anchor 恰第 2/11 行、census 恰第 2/16 行，非注释差异行过滤 = **0 行**），零代码行差异：

- `/tmp/r40h-anchor.mts` — 242 行，R30-H 附录 A anchor 驱动复刻；sha256 `26c145878ff06d2ffac84d5caa377a44dc1b416cc42798a301db931dd9a437f8`
- `/tmp/r40h-census.mts` — 233 行，R31-H 附录 A census 驱动复刻（卫生应用标注，与原文同行数）；sha256 `e97e132ac307cab7e331a8d9b3807ff4a6351ce53976e3db380b1448a49d2c79`

anchor 记录轮原始输出（默认链关键行，×3）：

```text
run1: extractHeuristicContract=8157ns + gate(Q=0)=722ns + applyPrecedence=256ns = 9134ns | 12/12
run2: extractHeuristicContract=7994ns + gate(Q=0)=782ns + applyPrecedence=251ns = 9027ns | 12/12
run3: extractHeuristicContract=7908ns + gate(Q=0)=753ns + applyPrecedence=246ns = 8907ns | 12/12
```

特征化 ×3（默认链）：8894 / 9098 / 9197 ns（组件 7954+695+245 / 8119+729+250 / 8149+788+260），12/12 ×3。烟测（弃置）：9171 ns（8228+689+254），12/12。

census ×3 关键行：

```text
run1: createTrustedSource=41.8ns/mint + normalizeSources=215ns/call | 25/25
run2: createTrustedSource=40.2ns/mint + normalizeSources=208ns/call | 25/25
run3: createTrustedSource=38.8ns/mint + normalizeSources=215ns/call | 25/25
```

MORE_OPTIMA=no
BRANCH=cursor/r40-h-eval-fortieth-pass-83a1
