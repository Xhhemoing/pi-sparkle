MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 40 / R40-C：离线路由切片第四十遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 40 / R40-C（Round 1–39 同区三十九遍
  之后的第四十遍，叠在已落地的 S7-C 之上）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `ecbf3ab`
  （R39-J 空枚举摄取 + Round 39 收口 + Round 40 开轮，round-40/PLAN.md
  C 段在案）；锚点测量与门禁完成后 fetch 复核 exclusive 仍为
  `ecbf3ab`；提交前 fetch 发现独占分支 docs-only 快进**一段两提交**：
  `ecbf3ab` → `46686fc`（R40-A 空枚举摄取 + R40-D 派单）→
  `550ea62`（R40-B 空枚举摄取 + R40-E 派单），段增量仅触
  docs/reports/sota-opt/ 下 4 文件（PROGRESS / round-40 PLAN /
  新增 R40-A、R40-B 两报告，合计 377 插入 41 删除）。段对 `src/
  test/ scripts/ package.json pnpm-lock.yaml` 逐提交直接复核
  **0 行**；按令 `git merge --ff-only` 逐段快进报告分支（纯快进
  零冲突）并各自重验冻结。冻结主张六次核验均 **0 行**（切片 9
  文件 vs `183df9b`：分支态复核时、锚点测量后、门禁后、两次快进
  后各一、提交前）；最强冻结 `git diff 89f0455..HEAD -- src/`
  **完全为空**（R10 以来全部提交均为 docs）。`git log -1 --
  src/routing/` 显示切片最后一次生产改动**仍为 `183df9b`
  「perf(routing): land S7-C support-only eta」**。新收口姊妹项
  **R40-A / R40-B 两份空枚举**已按快进后 PLAN 并入非移植清单并
  完成划界（§4）。全部锚点测量在 `ecbf3ab` 工作树完成——快进段
  增量 `src/` 逐字节零变化 ⇒ 测量形态与提交形态位级同一，全部
  有效；聚焦套件 + `pnpm gate` 在 `ecbf3ab` / `46686fc` /
  `550ea62` 三 tip 均绿（计数逐字同）。
  **环境诚实注记：** 测量阶段 VM 为全新开机窗口（官方电池起跑时
  uptime 6–7 分钟、负载 0.01–0.38 已沉降），`node_modules` 不在位，
  按锁文件 `pnpm install --frozen-lockfile` 恢复依赖（porcelain 保持
  空、锁文件零触碰、tsx 恢复为血统同版 4.23.12）；工作树字节与 tip
  `ecbf3ab` 逐字节同一（`git status` 干净 + 冻结复核 0 行），全部
  官方数字出自当前开机窗口，无任何跨窗口引用。
- **分支:** `cursor/r40-c-offline-routing-fortieth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，`wc -l` 逐文件 170/168/311/585/254/156/88/130/177 = 合计 2,039 行，与 R29-C..R39-C 普查同构；S7-C 支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开、S2-C 规范键 eta/mu 去重、S1-C bootstrap 工件复用全部现场核对在位）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**
  （canonical，与 R28-C..R39-C 同引擎；PATH 开工即前置
  `~/.nvm/versions/node/v22.22.2/bin` 绕开系统 Node v22.14.0——
  每个官方批次头部打印 `node --version` + `process.versions.v8`
  复核 v22.22.2 / 12.4.254.21-node.39，**零批次**落在 v22.14.0 上，
  零弃置批次（协议预演遍按令弃置，非引擎违规）；tsx 4.23.12、
  pnpm 10.17.1、`pnpm install --frozen-lockfile` 工作树在位）。

## 结论

**第四十组诚实枚举为空：不铸任何 S40-C-\* ID、不铸新 face 名、
不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，本轮生产代码零改动。**
对 9 个源文件逐行第四十遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R39-C 的全部无号关闭
探针与轴，含 R31-C 的 **BINDX**、R30-C 的 REPRX、R29-C 的 PAIRX、
R28-C 的基片实例化、R27-C 的 PROBX、R19-C 的 FITQ、R31-J 的
**MODEX**，及 R32-C/R33-C/R34-C/R35-C/R36-C 的七项换名记录——
updatePosterior pow 站点、values.sort 数值特化、lnGamma 反射分支
死码、退化结局双 `every` 融合、propensity `isFabricatedPositiveSupport`
`notOneHot` 被吸收析取、posterior `estimateForKey` 双
`weightedSampleSize` CSE、r1 `routeR1` cheapest-`reduce` decorate-carry，
按派单**均不重提**；R37-C / R38-C / R39-C 零新站点级记录亦按派单
不重铸）后，本轮**从未点名站点数为零（连续第二十二遍）、新增换名
检查记录为零**——每一个执行站点都能指到既有 ID、无号探针、零池
备忘或池算术覆盖。本轮独立重瞄两处均在核对后归于既有属主，引用
即止不立新记录：(i) offline-logit.ts `averagePredictiveComparison`
L388 对 on 向量的逐 (行,列) `.slice()` off 向量物化——归 **S4-C-4
属主本体**（APC off 向量每 fit scratch 缓冲复用：分配级，APC 全
站点低于噪声带）+ APC 道全系家族 S1-C-1/S2-C-2/S8-C-2/S10-C-2
（本轮组合天花板正典 24.3–31.8 < 35 维持关闭，出路不存在）；
(ii) offline-prob-add.ts `betaInterval` L64–L65 双 `betaQuantileLcb`
（lcb/ucb 两尾各一次 80 二分 + 各一次 `lnBetaFunction`）——上分位
第二次 80 二分即 **X1-3 冻结数值路径本体**，跨分位双
`lnBetaFunction` 为 **R17-C/R18-C 已记属主**，按 (n,mean) 记忆化
即 **S1-C-7**（亚 ms + 缓存状态）；属主引用即止。R39-C 两处在案
重瞄（`kappaS` L47–L49 双 `means.reduce`、propensity
`computeOverlapDiagnostics` L73–L74 双 `weights.reduce`）与 R38-C
两处在案重瞄（shadow.ts decisions 追加拷贝 = X4-2 本体、r1
cheapest-`reduce` `costOf` 重算 = R36-C 换名记录属主）本轮重读后
原属主裁决全部维持，不重提。R26-C/R27-C 关掉输入数值坐标、R28-C
关掉执行基片坐标、R29-C 关掉工作负载组成坐标、R30-C 关掉输入表示
坐标、R31-C 关掉名字解析坐标、R32-C 独立重推元坐标系无余项、
R33-C..R39-C 复推后各归属主之后，本轮再次独立重推，**无任何
「从未受控 + 独立物理对象 + 真尺度」三条同时成立的剩余坐标**：
C 切片是进程内纯同步数值内核，零 fs 数据路径（MODEX / R31-E
codec 面结构性空，§4 计数直证）、零 `JSON.parse` 输入位点
（REPRX 划界维持）、零 OS 边界（R31-I 审计平面正交）。

按派单在本 VM 重锚默认预算（未拷贝历轮数字，装具为 R31-C 附录
A.1 逐字节血统，仅头注更新，§附录）：

- **生产中位带（本轮正典）：663.3–672.6 ms/报告**（官方电池
  1 预演 + 3 遍：正典三遍 672.6 / 663.3 / 667.2，预演 668.0 按
  历轮惯例弃置不入带）——与默认对照 R39-C 带 665.9–693.4 交叠
  665.9–672.6，全带落在默认对照 **±35 ms 离线 logit 接受带内**
  （665.9−35 = 630.9 < 663.3，693.4+35 = 728.4 > 672.6）；R38-C
  中段带 683.6–688.2 亦全落在本带 ±35 窗内（628.3–707.6）。本 VM
  属战役窗口的**中快段主机**（低沿 663.3 高出 R37-C 偏快带顶
  660.2 约 3.1 ms，带顶 672.6 低于 R39-C 中段带顶 693.4），按派单
  「host-window samples under a 0-line freeze are not findings」
  判读为宿主刻度项，非切片变化，**不铸主机偏置新发现**。**同 VM
  独立旁证钉死结构结论**：r7c 回归资产锚点两次通过 **production
  660.2 / 663.2 ms = 1.12× / 1.12× vs 冻结 S6-C ctl 741.7 /
  745.5**——比率与 R28-C..R39-C（1.11–1.13×）同层，ctl 绝对值
  741.7–745.5 与 R39-C 宿主 742.0–745.8 几乎重合、恰落 R37-C 宿主
  734.8 与 R38-C 宿主 782.7 之间（绝对刻度归宿主）；切片对
  `183df9b` 零 diff，装具协议逐字节同 R31-C..R39-C。按 R14-C 处置
  规则随读重锚为本轮带。
- **small 带 30.1–31.4 ms**（R39-C 30.1–30.4 全含于本带、R38-C
  30.1–30.6 全含、预演 31.4 同层）；整条 small 报告低于 ±35 ms
  噪声带（R7-I 矩阵义务：该档只作确定性与保真见证，不作候选
  判据）。
- **APCX / FLOORX / 组合天花板**：APCX 无计时器独立道正典
  66.3 / 62.4 / 69.0 ms（0.330 / 0.310 / 0.343 ms/fit × 201 fits；
  R38-C 带 60.2–70.2 交叠 62.4–69.0、R34-C 带 60.1–68.6 交叠
  62.4–68.6；R39-C 正典带 59.5–61.8 偏低但其预演 69.8 / 弃置遍
  67.6 与本带同层——APCX 绝对刻度历轮即随宿主漂移，判据只看
  组合天花板）；FLOORX 正典 36.8 / 38.1 / 37.2 ms（R39-C 带
  31.7–41.2 全段落内、R36-C 带 31.5–44.3 全段落内、R38-C 带
  34.4–40.5 全段落内）⇒ **组合天花板 = 29.5 / 24.3 / 31.8 ms <
  35**——正典带 24.3–31.8 与 R39-C 正典带 19.0–30.1 交叠
  （24.3–30.1）、与 R36-C 正典带 20.9–34.1 交叠（24.3–31.8）、与
  R38-C 正典带 23.3–29.7 交叠（24.3–29.7）；预演天花板 27.5 按
  历轮惯例不入正典带；正典三读数全部 < 35：组合出路维持关闭。
- **sink = floorSink = 7.309 八读数全同**（预演 + 3 遍 × 2 道，
  每运行内 `Object.is(apcSink, floorSink)` 断言全绿）——与
  R14-C/R16-C..R39-C 打印值逐位同值，跨轮确定性旁证成立：主机
  差异动不了任何输出位。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等；每遍
  2,768 项位级检查全绿，四遍（预演 + 正典三）合计 **11,072 项**
  （正典三遍 8,304 项）。

回归资产与门禁（§5）：r1c–r7c 七脚本全绿，检查数逐一匹配锚定值
**8028 / 14420 / 14730 / 24888 / 28555 / 25483 / 6193**（r7c 两次
通过，两次同为 6,193）；聚焦离线路由套件 59/10/59/0（`ecbf3ab` /
`46686fc` / `550ea62` 三 tip 各一次，逐字同计数，与 R31-C..R39-C
同计数）；`pnpm gate` 在三 tip 各跑一次均 exit 0（tests 1169 /
suites 78 / pass 1168 / fail 0 / skipped 1，三次逐字同计数，与
R32-C..R39-C 同计数；tsc build 零诊断）。

硬性不变量逐条复核（§0）：双 LCB 在位（posterior.ts L22
`LcbKind = "normal" | "beta-quantile"`，本轮全文重读直证）；双归因
在位（attribution-report.ts L18–19 `logit: fitLogitAdditive(rows),
probAdd: fitProbabilityAdditive(rows)`，现场核对）；live = R0
等价（`src/supervisor/` 对 9 切片文件导入为零，本轮 rg 复核，
R1 未接线）；S8-E-1 `loadLearnedRouting` 调用点恰 3 处（track/loop
L88 + flowchart-run L712 + cli/main L708，本轮 rg 复核）；推广
proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，
ADR-005）；无阈值改动；无测试改动；CAS/凭证/数据面合同/公开签名
零触碰。仓库变更仅本报告一个文件；无生产赢家故未提交任何临时装具
（血统锚定装具指纹见附录，临时文件已在 `pnpm gate` 前删除）。
空枚举与 R8-C..R39-C 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → EXCLUSIONS.md（完整表 545 行，
  含全部 S\*-C-\* 行与 S12-C-1、S13-B-1；本轮测量后复核仍 545 行
  零增）→ PROGRESS.md → round-40/PLAN.md（C 段 + 具名/unnamed 全
  ban 榜）→ round-39/PLAN.md → R39-C.md 全文（默认对照，空枚举
  确认，无 S39-C-\*，零新站点级记录；两处独立重瞄——`kappaS` 双
  `means.reduce`、propensity 双 `weights.reduce`——属主记录在案，
  本轮不重提）→ R39-A / R39-B 与 R39-D..R39-J 九份姊妹报告全文
  （全部空枚举，逐份折入非移植清单，§4）→ R38-C / R36-C / R35-C
  结论（换名记录属主，按派单不重提不重铸）→ 9 个切片源文件全量
  重读 → `src/experiments/attribution-report.ts` 消费图现场核对 →
  R31-C.md 附录 A.1（装具血统提取源）→ 快进后补读 **R40-A.md /
  R40-B.md 两份全文**（A 区第四十遍空枚举——三项站点级换名记录
  拒列不铸 ID，全在 tracking 面物理对象；B 区第四十遍空枚举——
  零新站点级换名记录，两残余角度均为 R2-B 已点名站点复命中，
  S12-B-2 腿 (a) / S13-B-1 经其重取证均未触发；两份均与 C 零共享
  物理对象）与更新后的 round-40/PLAN.md（C 段默认对照不变仍为
  R39-C，非移植清单增列 R40-A / R40-B 空枚举）。
- 基线漂移检查：本轮测量期起点 = 分支创建点 = `ecbf3ab`（tip 已含
  Round 40 PLAN）；锚点测量与门禁完成后 fetch 复核 exclusive 仍为
  `ecbf3ab`；提交前 fetch 发现 docs-only 快进一段两提交
  （`46686fc` / `550ea62`，见基线注记）——以 `git diff --stat` +
  对 `src test scripts package.json pnpm-lock.yaml` 的逐提交直接
  diff = **0 行**核对后按派单规程 `git merge --ff-only` 逐段快进
  报告分支（纯快进零冲突）并各自重验冻结。冻结主张六次核验均
  **0 行**（切片 9 文件 vs `183df9b`：分支态复核时、锚点测量后、
  门禁后、两次快进后各一、提交前）；最强冻结 `89f0455..HEAD --
  src/` 为空。VM 全新开机窗口（见基线注记）不动工作树：
  `git status` 干净、切片逐字节同一——官方电池在字节相同的切片
  上全量完成，测量全部有效。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：
  input object-representation / **REPRX**（切片 `JSON.parse` 输入
  位点 0，§4 计数）/ **BINDX**（R31-C；realm 级绑定态——本轮零
  patch、零剂量、零计时新臂；C1 capture-hoist 不重提；划界计数见
  §4）/ **MODEX**（R31-J——inode 权限模式字拓扑，J 区持久工件面；
  本切片数据路径零 fs 位点，面结构性空，§4 计数后声明即止）/
  **PAIRX**（工作负载组成——本轮全部测量单内核恒定，姊妹估计器
  零计时）/ KFAN / COTARG / PROBX / DELINV / PRICEX / VALCLASS /
  **R39-C 零新站点级记录**（按派单不重铸——本轮从零重枚举独立
  得出同构结论，声明即止）/ **R38-C / R37-C 零新站点级记录**
  （同上）/ **R36-C 换名记录**（r1 `routeR1` cheapest-`reduce`
  折叠成本 decorate-carry，按派单不重提、不重铸——本轮独立重读
  r1.ts L119–L121 后属主引用即止，§2.1 r1 行）/ **R35-C 换名记录**
  （posterior `estimateForKey` 双 `weightedSampleSize` CSE，本轮
  独立重读 L300–L301 后属主引用即止，不重提）/ **R34-C 换名记录**
  （propensity `isFabricatedPositiveSupport` `notOneHot` 被吸收
  析取，不重提）/ **R33-C 两项换名记录**（lnGamma 反射分支死码
  消除与 fitLogitAdditive 头部退化双 `every` 融合，不重提）/
  **R32-C 两项换名记录**（updatePosterior pow 站点、values.sort
  数值特化，不重提）/ **R39-A..R39-J 十切片空枚举**（开工读序
  折入——A 区 14 文件 1796 行 + 三项站点级换名记录拒列全在
  tracking 面 + 预算重锚 13.6–16.0 µs/gate；B 区 10 文件 1359 行
  零新记录，ceiling M=2 8.31–9.77 / M=10 16.99–17.36 ms/eval、
  replay M=2 9.63–10.00，S12-B-2 腿 (a) / S13-B-1 未触发；D 区
  14 文件 3294 行零新记录，S0c 地板 3.46–3.50 ms；E 区 10 文件
  1770 行零新记录，cmp25 锚 15.0 µs/run ×3；F 区 15 文件 2550 行
  零新记录；G 区 42 文件 8960 行零新记录，I/O 支配比 ~286–337×、
  矩阵摘要 06cbcf92c098c8f0 第三十次复现；H 区 21 文件 1750 行
  零新记录，默认链锚 9201/9008/9247 ns；I 区 25 文件 4708 行零新
  记录零残余，S8-I-1 file-level 复证；J 区 29 文件 3655 行零新
  记录，J1 等价 sim 2468 项位级全绿（2929.4×）。十份均与 C 零
  共享物理对象——tracking / live 路由 / adaptation / learning /
  experiments / runtime / eval / CLI / persist 面；`routeR0` 是
  B 区文件，本切片 r1-shadow-report 仅消费其公开导出，双方均零
  改动。声明即止，不移植、不重做、不铸新轴）/
  **R40-A / R40-B 空枚举**（快进带入，全文读毕折入——A 区第四十
  遍 14 文件 1796 行三验 0 行 + 三项站点级换名记录拒列不铸 ID +
  预算重锚 13.8–17.2 µs/gate；B 区第四十遍 10 文件 1359 行四点
  冻结 0 行 + 零新站点级换名记录 + ceiling M=2 8.91–9.98 / M=10
  17.13–18.30 ms/eval、replay M=2 8.96–9.60，S12-B-2 腿 (a) /
  S13-B-1 经其重取证均未触发。两份均与 C 零共享物理对象，声明
  即止）/
  **R33-A..R38-J 空枚举**（历轮已合入，方法卫生记录，与 C 的携带
  由 0 行冻结 + 锚点保真电池 + 本轮逐文件重读覆盖，不重演账本
  分类、不重走闭合稳定性审计、不重铸「再查一遍重开条件 / 闭合
  稳定性 / 门扉」式新面）/ **R32-A 重开条件哨兵审计**（方法不
  重铸）/ **R32-B 闭合稳定性审计**（不重走）/ R31-I CLI 进程边界
  门扉闭合审计（本切片零 argv/env/stdin/stdout/子进程/信号/退出
  腿，审计平面正交零交集）/ digest / sink=7.309 位等不另铸轴
  （本轮 sink 八读数只作既有跨轮见证复现）。FITQ / NAMESHAPE /
  SCALEX / XPROC / YMIX / SCHEDWIN / NVG / SEEDX / TERMCLASS /
  SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / CG / AGEX /
  RTL / RVL / SIDEC / PWL / ZREP / CSPELL / LINKTOP 全部未编号未
  移植。R28-C 基片实例化未移植（全部测量锁死单一基片 tsx+src）；
  R30-I 时间段拓扑、R30-J LINKTOP、R31-A/B/D/F/G/H 停驻门与属地格
  未开。X\* / S1-\* … S33-\* 全部未重开；S12-C-1 重开条件未触发
  （重开条件：无）；S6-C-3/S6-C-4 重开条件（支撑分布大改 / 因子数
  增长使支撑上界 ≥ ~8）经锚点夹具形状见证未触发（支撑上界仍 5）；
  S13-B-1 仍为 loser ID（B 区属地，未触）；S12-B-2 仍未触发
  （B 区属地）；S7-F-1 ≠ S6-F-5、S5-H-1 保持、S8-I-1 file-level
  封死、S9-D-4 / S12-D-1 永不再开、S8-E-1 禁止去重（均他区属地，
  未触；S8-E-1 恰 3 处本轮 rg 直证）；未起 S7-C 平行实现（X2-1
  引理前提复核成立：0/1 设计、+0.0 起点、有限 beta——FLOORX 道
  逐名值对 APCX 道 `Object.is` 断言全绿即为可执行见证）。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
  不开 PR。输家脚本不入库（本轮无输家道——除血统锚定装具外零
  计时新臂）。

## 1. 生产中位与锚点复核（本 VM 实测，未拷贝历轮数字）

测量装具与 R31-C 附录 A.1 逐字节血统（replica 保真电池 34 例 +
自决定性 + 中位重锚 + APCX/FLOORX；仅头注更新为 R40-C，diff 即
6 行注释，指纹见附录）；Node v22.22.2，tsx + src 直连（R28-C S
臂 = 历史正典基片）。官方电池 = 1 次预演 + 3 次测量独立进程，
全部在 `ecbf3ab` 工作树、当前开机窗口（uptime 6–7 分钟、负载
沉降 0.01–0.38）完成，逐运行日志留档：

| 运行 | perf 中位 (ms) | small 中位 (ms) | APCX (ms) | FLOORX (ms) | 组合天花板 (ms) | sink / floorSink | 检查 |
|---|---|---|---|---|---|---|---|
| 预演 | 668.0 | 31.4 | 63.5 | 36.0 | 27.5 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 1 | 672.6 | 30.3 | 66.3 | 36.8 | 29.5 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 2 | 663.3 | 30.1 | 62.4 | 38.1 | 24.3 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 3 | 667.2 | 31.4 | 69.0 | 37.2 | 31.8 | 7.309 / 7.309 | 2,768 ✓ |

perf 逐 rep（7 rep/运行）：预演 667.6 673.2 670.0 668.0 665.0
665.0 694.2；正典 1 674.4 672.1 672.6 667.6 675.0 670.2 675.8；
正典 2 665.6 663.3 661.5 662.8 665.5 662.5 671.0；正典 3 667.2
675.1 717.3 693.4 663.5 664.6 664.4。

- **本轮全绿零弃置**（预演按历轮惯例不入正典带）：四运行保真
  电池 + 自决定性 + sink 双道全部通过，无任何一遍撞 `ceiling<35`
  断言（正典最高 31.8 距断言线 3.2 ms；正典 3 的 717.3 单 rep
  尖峰被中位数吸收，与 R39-C 弃置遍同型瞬态但未及断言）。
- **生产中位带（正典三遍）：663.3–672.6 ms**。与默认对照 R39-C
  带 665.9–693.4 交叠 665.9–672.6；全带在默认对照 ±35 ms 接受带
  内（630.9–728.4）——本 VM 属战役窗口的中快段主机（低沿 663.3
  距 R37-C 偏快带顶 660.2 约 +3.1 ms），是 0 行冻结下的宿主刻度
  项，非切片变化，亦**不铸主机偏置新发现**（派单明令）。三重独立
  旁证钉死「非切片变化」：(i) 切片对 `183df9b` 逐字节零 diff；
  (ii) r7c 回归资产（§5）同 VM 两次通过打印 production 660.2 /
  663.2 ms = **1.12× / 1.12×** vs 冻结 S6-C ctl 741.7 / 745.5——
  与 R28-C..R39-C 的 1.11–1.13× 同层，ctl 绝对值与 R39-C 宿主
  742.0–745.8 几乎重合，坐实「刻度归宿主、结构比率不动」；
  (iii) sink 八读数逐位同历史值（输出位不动）。按 R14-C 处置规则
  **随读重锚为本轮带 663.3–672.6**，结构结论（层级、比率、天花板、
  确定性）全部与历轮同构。
- **small 带 30.1–31.4 ms**（R39-C 30.1–30.4 与 R38-C 30.1–30.6
  均全含于本带；预演 31.4 同层）；整条 small 报告低于 ±35 ms
  噪声带，只作确定性与保真见证。
- **APCX 正典带 62.4–69.0**（0.330 / 0.310 / 0.343 ms/fit × 201
  fits；R38-C 60.2–70.2 交叠 62.4–69.0、R34-C 60.1–68.6 交叠
  62.4–68.6；R39-C 正典 59.5–61.8 偏低而其预演 69.8 / 弃置遍
  67.6 与本带同层——APCX 绝对刻度随宿主漂移为历轮已知形态）；
  **FLOORX 正典带 36.8–38.1**（R39-C 31.7–41.2、R36-C 31.5–44.3、
  R38-C 34.4–40.5 三带全部包含本带）；**组合天花板正典带
  24.3–31.8 < 35**（与 R39-C 正典带 19.0–30.1 交叠 24.3–30.1、与
  R36-C 正典带 20.9–34.1 交叠 24.3–31.8、与 R38-C 正典带
  23.3–29.7 交叠——APCX 与 FLOORX 同 VM 同窗对减，正典三读数
  差值全数 < 35，组合出路维持关闭）。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等（含
  replica 保真电池 34 例 + perf + small 对生产 import 的全字段
  断言），每遍 2,768 项检查全绿，四遍合计 11,072 项。
- **sink = floorSink = 7.309 逐位**（8 读数 = 4 运行 × 2 道，
  每运行内 `Object.is(apcSink, floorSink)`）——与 R14-C/
  R16-C..R39-C 打印值同值；X2-1 引理前提（0/1 设计、+0.0 起点、
  有限 beta）由 FLOORX 道逐名 `Object.is` 断言随运行复核成立。

## 2. 第四十遍枚举（诚实结果：空；零新站点 + 零新换名记录）

### 2.1 逐文件重读与裁决（零未点名站点，连续第二十二遍）

| 文件 | 本轮重读检查（全部命中既有 ID / 无号探针 / 零池） | 结论 |
|---|---|---|
| `r1.ts`（170 行） | 每调用 `modelsById` Map 构造、config spread、estimates spread、`tierIds` spread、`lcb.toFixed(4)` 返回路径格式化 = S1-C-10 本体；sampled/aboveFloor 双 filter = S1-C-5；costOf/tierIndex/`aboveFloor.find(previousModelId)` 线性 find = X1-4 本体（M≤10 噪声级）；cheapest `reduce` 折叠累加器 `costOf` 重算的 decorate-carry 消除 = **R36-C 换名记录属主**（本轮独立重读 L119–L121 后属主引用即止，不重提不重铸）；prepared 观测索引 = 已落地 A2/B1；跨 episode 记忆化 = X1-6 | 无新候选 |
| `r1-shadow-report.ts`（168 行） | 每 episode request spread + routeR0 调用（B 区文件不触碰）；merge 路径 = S1-C-2（跨索引播种）+ S1-C-3（公开 ReadonlyMap 形状）；selectedCost find = X1-4 族 + PRICEX 封顶；与 shadow-compare 合并 = X1-5；sanitizeClaims once-per-report；空 episodes / 零 records 故障腿 = R18-C once-per-fault 拒绝类；L105 `exploratory !== false` 双卫 = R37-C 独立重瞄在案属主（fail-closed 跨函数不变量见证家族，不重提） | 无新候选 |
| `posterior.ts`（311 行） | 80 次二分 / Newton / 闭式 = X1-3；lnBeta 每 quantile 一次 = 已落地 C1；estimateFingerprint = S1-C-4；groupObservationsByKey = 已落地；betacf 守卫 Math.abs = BINDX 坐标（计数即止）；updatePosterior pow 站点 = R32-C 换名记录 1 属主（不重提）；`lnGamma` 反射分支死码 = R33-C 换名记录 1 属主（不重提）；`oneSidedTail(_z)` 死形参 = R17-C OSTZ 零池备忘属主；posteriorMean/Variance `total>0` 卫 = S8-C-4 fail-closed 数值卫家族；regularizedIncompleteBeta 前缀重算与分支阈 `(a+1)/(a+b+2)` 逐迭代重算 = X1-3 冻结数值路径本体；`estimateForKey` 缺键 `?? []` 分配 = 落地 memo 体内 once-per-(key,fingerprint) 分配级零池；`samples`/`wellSampled` 双 `weightedSampleSize` = **R35-C 换名记录属主**（本轮独立重读 L300–L301 后属主引用即止，不重提）；betaQuantileLcb 头部 p/参数卫 = fail-closed 卫家族 once-per-quantile ns 级 | 无新候选 |
| `offline-logit.ts`（585 行） | S1-C（bootstrap 工件复用）/S2-C（规范键 eta/mu 去重）/S3-C（单位乘法消除）/S6-C（switch 直线化）/S7-C（支撑升序直加）落地形态逐站点在位；头部退化结局双 `every` 融合 = R33-C 换名记录 2 属主（不重提）；draw 侧塌缩检查融合 = S2-C-5 本体；`beta.every(Number.isFinite)` = S7-C-2/S8-C-3；delta map+reduce = S2-C-3；per-key w / per-(key,y) z 去重 = S3-C-2；`xtwx fill(0)` = S5-C-7；dot 展开 = S5-C-5（串行依赖）；onProbabilitiesFor = S7-C-4/S8-C-1；**APC off 向量逐 (行,列) `.slice()` 物化 = 本轮独立重瞄 (i)**——归 S4-C-4 属主本体（off 向量每 fit scratch 缓冲复用：分配级，APC 全站点低于噪声带）+ APC 道全系 S1-C-1/S2-C-2/S8-C-2/S10-C-2（本轮组合天花板正典 24.3–31.8 < 35 维持关闭），属主引用即止；percentile 换 quickselect = X4-3 族；draws 循环 `.entries()` = S7-C-1；draws 预分配/pushValue = S1-C/S3-C-3 分配级；`values.sort` 数值特化 = R32-C 换名记录 2 属主（不重提）；buildDesign/computeSupports/canonicalRowKeys/columnIndex 元组物化 once-per-report 构建 = S1-C 落地后残余亚带池（R12-C 亚带分类）；诊断尾 `interactionsFor` 嵌套 filter、`e.name.slice` 闭包内重算、`ZERO_EPS` 常量每报告重乘、`successful < MIN_SUCCESSFUL_DRAWS` 循环不变量逐效应重判、mean reduce、双 Set 基数 = 尾部 once-per-report 亚噪声（R33-C..R39-C 表同款）；IRLS 收敛范数 sqrt = R17-C NSQRT 关闭探针；采样环 `Math.floor(random()*n)` = 冻结 RNG 契约（mulberry32 逐位再现义务）；worker/WASM = S9-C-1/S9-C-2；嵌套序交换 = S12-C-1（重开条件：无）；per-fit distinct-key 代表表 = S10-C-1；stamp Int32Array 换窄宽度 = S7-C-3 零效应家族 | 无新候选 |
| `offline-prob-add.ts`（254 行） | X3-4 + R17-C 零池；cell 双算合一 = S1-C-6；站点规范键去重 = S2-C-1；betaInterval 记忆化 = S1-C-7；**betaInterval L64–L65 lcb/ucb 双 `betaQuantileLcb`（两尾各一次 80 二分 + 各一次 `lnBetaFunction`）= 本轮独立重瞄 (ii)**——上分位第二次 80 二分即 X1-3 冻结数值路径本体，跨分位双 `lnBetaFunction` = R17-C/R18-C 已记属主，(n,mean) 记忆化 = S1-C-7 亚 ms 池，属主引用即止；`kappaS` L47–L49 双 `means.reduce` = R39-C 在案重瞄 (i) 属主（X1-3 族 + S1-C-6 邻域零池，不重提）；kappaS 输入 `[...values(),...values()].map(cell)` 物化 = S1-C-6 邻域 once-per-report 零池；死 `pM`/`pMp` shrink（`void` 保留）= R17-C PMV 零池备忘属主（冻结公式文档，行为一位不动）；diagnose 内 `[...entries()]` 每效应重物化 + `lastSegment` split 逐 key 重算 = once-per-report 零池覆盖；`lastSegment` 死首参 = R33-C 表已记纯文本项（零池，不立记录） | 无新候选 |
| `propensity.ts`（156 行） | 三遍改单遍 = X3-5 + S1-C-8 本体；`computeOverlapDiagnostics` L73–L74 双 `weights.reduce` = R39-C 在案重瞄 (ii) 属主（X3-5 + S1-C-8 本体 + R27-C 通量普查生产调用 0 次/报告，零通量 µs 拒收，不重提）；`isFabricatedPositiveSupport` `notOneHot` 被吸收析取 = R34-C 换名记录属主（按派单不重提）；behaviorMu/targetPi 访问器 = R27-C PROBX 冻结坐标（JIT 平凡内联）；validate 故障腿 = once-per-fault REJX 类（R18-C）；R21-C 零池维持 + R27-C 通量普查维持 | 无新候选 |
| `lin-alg.ts`（88 行） | S4-C（行引用提升）/S5-C（按四展开）落地形态在位；变体空间 = S4-C-1..3 + S5-C-1..7 + S10-C-3（对称 Schur：部分主元破坏对称）+ S12-C-1 封闭；`stop=n-3` 提升 / `a.some` 手写 = S6-C-7；回代 eps 卫 = S8-C-4（fail-closed 数值卫）；回代串行链 = S5-C-5；尾部 `Number.isFinite` 终检 L85 = 既有两站点普查之一（§4 计数） | 无新候选 |
| `bandit.ts`（130 行） | recordReward/recordExploration 拷贝追加 = X4-2 readonly 契约；`arms.includes` / meanReward 线性 = S1-C-9（arms 个位数）；selectArm 贪心循环 arm-0 重评 = S1-C-9 本体 ns 级；探索臂 `% arms.length` 冗余模 + `?? state.arms[0]` 恒不触发回退 = S8-C-4 fail-closed 卫家族（index 恒在界内，卫零成本保留）；createBanditState `[...arms]` 防御拷贝 = 契约本体；validateTaskFeatures includes = S1-C-9 族 | 无新候选 |
| `shadow.ts`（177 行） | decisions 追加拷贝 `[...state.decisions, decision]` = X4-2 本体（R38-C 独立重瞄在案属主——ShadowState 不可变契约决策承载、调用方持有历史状态引用，换可变 push 即改语义，本轮属主引用即止不重提）；halted 分支整 decision 字面量重建 / breached once-per-fault 路径 = R16-C/R18-C 拒绝类；每 step config 字面量 = S1-C-9 本体；roundUsd/预算扣减 µs 域 = S1-C-9 族 | 无新候选 |

结构位置 × 消除机制满矩阵（死码/常量折叠/CSE/记忆化/数据结构/
融合/早退/免分配/复杂度类/强度削减/惰性/平行路径/形状单态化）
逐格过筛：非空格全部落在上表既有 ID 或既有换名记录属主上，本轮
**无任何一格需要新记录**。

### 2.2 本轮新换名检查记录：无

两处独立重瞄（§2.1 表内标注）均在核对后归于既有属主——
offline-logit `averagePredictiveComparison` L388 off 向量逐
(行,列) `.slice()` 物化归 **S4-C-4 属主本体**（分配级；APC 全
站点在噪声带下，本轮组合天花板正典 24.3–31.8 < 35 直证出路
关闭）+ APC 道全系家族；offline-prob-add `betaInterval` L64–L65
双 `betaQuantileLcb` 归 **X1-3 冻结数值路径本体 + R17-C/R18-C
已记属主（跨分位双 lnBetaFunction）+ S1-C-7 亚 ms 池**。两处均
属主引用即止，**不立新换名记录、不铸 ID**。R39-C 两处在案重瞄
（`kappaS` 双 `means.reduce`、propensity 双 `weights.reduce`）与
R38-C 两处在案重瞄（shadow.ts decisions 追加拷贝、r1
cheapest-`reduce`）本轮重读后原属主裁决维持，不重提。

### 2.3 枚举收口

第四十遍逐文件重读**零未点名站点（连续第二十二遍）**、零新换名
记录。R12-C 四分类（重命名 / 亚带池 / 逐位违例 / 结构交换负优化）
+ R18-C once-per-fault 拒绝类第四十遍无余项。**第四十组枚举为空
——比强铸败者 ID 更诚实。**

## 3. 三大池地板与组合出路复核

- **APCX 池**（59 非截距名 × 400 行 APC 扫掠 × 201 fit）：无
  计时器独立道 0.330 / 0.310 / 0.343 ms/fit ⇒ 66.3 / 62.4 /
  69.0 ms/报告——正典带 62.4–69.0，与 R38-C 带 60.2–70.2 交叠
  62.4–69.0、R34-C 带 60.1–68.6 交叠 62.4–68.6（本 VM 中快段
  窗口判读见 §1；预演 63.5 带内同层）。
- **FLOORX 池**（S8-C-2 支撑升序 off-sum 形态，从未落地——X2-1
  引理前提本轮复核成立；逐名值对 APCX 道 `Object.is` 断言全绿）：
  0.183 / 0.189 / 0.185 ms/fit ⇒ 36.8 / 38.1 / 37.2 ms/报告地板。
- **组合天花板 = APCX − FLOORX = 29.5 / 24.3 / 31.8 ms < 35**
  ——正典带 24.3–31.8 与 R39-C 正典带 19.0–30.1 交叠 24.3–30.1、
  与 R36-C 正典带 20.9–34.1 交叠 24.3–31.8、与 R38-C 正典带
  23.3–29.7 交叠 24.3–29.7；预演 27.5 按历轮惯例不入正典带；
  正典三读数全部 < 35：组合出路维持关闭。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is`）——与 R14-C/R16-C..R39-C 打印值同值，派单强制的
  跨轮确定性旁证成立。
- 零池备忘维持：posterior/propensity/bandit/shadow 四文件全部
  站点在既有 ID、无号探针（含 R17-C PMV/OSTZ/NSQRT 与
  betaInterval 双 lnBetaFunction 记录）覆盖下，本轮重读零新记录、
  无余项。

## 4. 姊妹面划界（计数即止，零剂量零计时）

1. **R39-A..R39-J 十切片空枚举（Round 39 收口，开轮时已合入；
   开工读序全文读毕折入非移植清单）**——A 区第三十九遍从零重枚举
   （14 文件 1796 行，基线 `7acb666` 三验 0 行）+ 三项站点级换名
   记录拒列不铸 ID（turn.ts:75 `?? []` 全流量回退臂 /
   roller.ts:30/:32/:36 三连 `?? []` / roller.ts:84–85 条件 spread
   否臂 `{}`）+ 预算重锚 13.6–16.0 µs/gate ⇒ ~68–80 µs/run；B 区
   （10 文件 1359 行，基线 `94ed3d9` 四点冻结 0 行）零新站点级
   换名记录 + ceiling M=2 8.31–9.77 / M=10 16.99–17.36 ms/eval、
   replay M=2 9.63–10.00，S12-B-2 腿 (a) 与 S13-B-1 未触发；C 区
   即本切片上一遍（默认对照，§1 已重锚）；D 区（14 文件 3294 行，
   基线 `82bef36`）零新记录 + S0c 地板 3.46–3.50 ms（⊂ R37-D
   3.40–3.80）；E 区（10 文件 1770 行，基线 `adb20d7`）零新记录 +
   cmp25 锚 15.0 µs/run ×3（带 14.7–15.2 内）+ S8-E-1 恰 3 处
   直证；F 区（15 文件 2550 行，基线 `519101f`）零新记录 +
   S7-F-1 ≠ S6-F-5 复证 + BINDX 计数 10 token / 8 行；G 区
   （42 文件 8960 行，基线 `4efee23`）零新记录 + 计算天花板
   0.283–0.302 ms/run vs I/O 地板 80.8–99.0 ms/run（支配比
   ~286–337×）+ 矩阵摘要 06cbcf92c098c8f0 第三十次复现不铸新 ID；
   H 区（21 文件 1750 行，基线 `fd437a9`）零新记录 + 默认链锚
   9201/9008/9247 ns 全带内；I 区（25 文件 4708 行，基线
   `8dee7fb`）零新记录零残余 + custom-builtin 三命令 delta 与
   历史带相交 + S8-I-1 file-level 复证；J 区（29 文件 3655 行，
   基线 `fb41417`）零新记录 + 三处重命中归原属主 + I/O 地板矩阵
   重锚全带内 + J1 等价 sim 2468 项位级全绿（2929.4×，历史区间
   内）。十份均与 C 零共享物理对象（`routeR0` 是 B 区文件，本
   切片 r1-shadow-report 仅消费其公开导出，双方均零改动）。声明
   即止，不移植、不重做、不铸新轴。
2. **R40-A / R40-B 空枚举（本轮快进带入，全文读毕折入）**——A 区
   第四十遍从零重枚举（14 文件 1796 行，基线 `7acb666` 三验 0
   行）+ 三项站点级换名记录拒列不铸 ID（types.ts `parseOpenMinor`
   / `parseGateDecision` 属性双读 CSE 两站点归 S5-J-5 家族、
   child-tracking.ts:58/:65 `?? []` 双回退臂归 S1-A-7 享元家族 +
   R39-A 记录 1/2 镜像）+ 预算重锚 13.8–17.2 µs/gate ⇒ ~69–86
   µs/run（带与 R39-A 13.6–16.0 / R38-A 14.0–19.0 交叠）+ 哨兵
   卫生复跑无一重开条件触发；B 区第四十遍从零重枚举（10 文件
   1359 行，基线 `94ed3d9` 四点冻结 0 行）+ 零新站点级换名记录
   （两残余角度均为 R2-B 已点名站点复命中）+ 天花板重锚 ceiling
   M=2 8.91–9.98 / M=10 17.13–18.30 ms/eval、replay M=2
   8.96–9.60，S12-B-2 腿 (a) 与 S13-B-1 经其重取证均未触发。两份
   均与 C 零共享物理对象（tracking / live 路由面；`routeR0` 是
   B 区文件，本切片 r1-shadow-report 仅消费其公开导出，双方均零
   改动）。声明即止，不移植、不重做、不铸新轴。
3. **R33-A..R38-J 空枚举（六轮收口，历轮已合入）**——全部为方法
   卫生 / 空枚举记录，未铸新面；R36-C 一项换名检查（r1 `routeR1`
   cheapest-`reduce` decorate-carry）与 R35-C 一项（`estimateForKey`
   双 `weightedSampleSize` CSE）本轮按属主引用（§2.1 r1 /
   posterior 行），不重提、不重做、不铸新轴。声明即止。
4. **BINDX（R31-C）**——切片环境内建 token 静态普查（划界计数，
   非剂量）：`Math.*` 恰 **42 token**，其中函数内建 12 个
   （abs 9 / max 7 / log 6 / min 3 / floor 3 / exp 3 / sqrt 2 /
   imul 2 / sin 1 / round 1 / pow 1 / ceil 1）+ 值常量
   `Math.PI` ×3；`Number.isFinite` 恰 **2 站点**（offline-logit
   L339 `beta.every` + lin-alg L85 终检）——与 R31-C..R39-C
   十二内建 + 两站点普查逐名同构，realm 坐标未动、零 patch、
   零计时。C1 capture-hoist 不重提。
5. **REPRX（R30-C）**——切片 `JSON.parse` 输入位点 **0**（全部
   输入经内存对象抵达）——划界维持，声明即止。
6. **MODEX（R31-J）**——9 文件对 `node:fs / readFile / writeFile /
   chmod / umask / mkdir / appendFile` 命中 **0**——切片数据路径
   零持久工件、零铸盘动词，面在 C **结构性空**（R31-E codec 面
   同一计数同一结论）。声明即止。
7. **PAIRX（R29-C）**——本轮全部测量单内核恒定（perf/small 两
   夹具即历轮锚定形状），姊妹估计器零计时，工作负载组成坐标
   未动。声明即止。
8. **R31-I CLI 进程边界门扉闭合审计**——本切片零 OS 进程边界
   腿（无 argv/env/stdin/stdout/spawn/signal/exit），审计平面
   正交零交集，不移植。

## 5. 测试与验证

```
# 聚焦离线路由套件（tsx --test，7 文件：r1 / r1-shadow-report /
# offline-logit / offline-prob-add / propensity / shadow /
# offline-types）在 ecbf3ab / 46686fc / 550ea62 三 tip 各跑一次：
#   tests 59 / suites 10 / pass 59 / fail 0  （三次逐字同计数，
#   与 R31-C..R39-C 同计数）

# 全门禁 pnpm gate = typecheck + lint + test + build
# 在 ecbf3ab / 46686fc / 550ea62 各跑一次，均 exit 0：
#   tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#   （三次逐字同计数，与 R32-C..R39-C 同计数；tsc build 零诊断）

# 既有回归资产全量复跑（本 VM 当前开机窗口锚点，落地零改动仍作
# 回归门；ecbf3ab 工作树，nvm Node v22.22.2）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项（design.build 358,006/拟合）
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项（eta dots 3,586,400 → 1,249,652 去重）
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点（两次通过，检查数两次同为 6,193）：
#   production 660.2 ms = 1.12× vs ctl 741.7 /
#   production 663.2 ms = 1.12× vs ctl 745.5（VAR-SOD 1.10× / 1.09×）。
#   与 §1 生产正典中位带互证：比率与 R28-C..R39-C（1.11–1.13×）
#   同层，ctl 741.7–745.5 与 R39-C 宿主 742.0–745.8 几乎重合、恰落
#   R37-C 宿主 734.8 与 R38-C 宿主 782.7 之间——绝对刻度归宿主
#   （本 VM 中快段主机）。

# 本轮测量装具（临时脚本，未入库——无生产赢家；来源与指纹见附录）
# 官方电池 1 预演 + 3 遍：四遍 11,072 项位级检查全绿（2,768 × 4，
#          正典三遍 8,304），零弃置（预演按惯例不入正典带）；
#          sink/floorSink 8/8 = 7.309（进程内 Object.is）
```

冻结六验（§0；快进段 src/ 逐字节零变化，测量形态与提交形态位级
同一）；仓库变更 = 本报告一个文件（提交前
`git diff origin/cursor/sota-persistent-opt-83a1...HEAD --stat`
复核仅 `docs/reports/sota-opt/round-40/R40-C.md`）。无生产落地
（派单条件句未触发），未铸新仿真脚本，临时装具已在 `pnpm gate`
前从 /tmp 删除。

## 6. 新增排除 ID：无

- 不铸 S40-C-\*：第四十组枚举为空；两处独立重瞄均归既有属主
  （S4-C-4 + APC 道全系；X1-3 + R17-C/R18-C 已记属主 + S1-C-7），
  不越 ±35 ms 带，无任何站点达标。
- 不写入 EXCLUSIONS.md 任何行；不铸新 face 名；不取 ALLCAPS
  绰号（含对 MODEX / BINDX / REPRX / PAIRX 的一切再编号）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态，未重开 S12-C-1
  （重开条件：无），未起 S7-C 平行实现，未重铸 R32-A 哨兵审计
  方法，未重演 R33-A 账本分类、未重走 R32-B 闭合稳定性审计，
  未重提 R32-C/R33-C/R34-C/R35-C/R36-C 七项换名记录与 R37-C /
  R38-C / R39-C 六处在案重瞄。

## 附录：测量装具来源与指纹（未入库；运行后已删除）

- 装具：`/tmp/r40c/r40c-anchor.mts`，**814 行**，由本仓
  `docs/reports/sota-opt/round-31/R31-C.md` 附录 A.1 代码块
  （L555–L1368）逐字节提取，唯一改动为头部块注释 6 行更新为
  R40-C 说明（协议体、夹具、seed、断言零改动——R31-C A.1 本身
  即 R30-C/R14-C 血统逐字节协议；提取后对原文 diff 复核恰 6 行
  注释：L2–L5 轮次说明 + L29–L30 零新计时臂声明）。
- sha256（归档时点）：
  `270c79f7667bab2c2f616abd9332ef6384a5cd559151aa583e7fecfe4393fd44`
  （纯提取未改注基准逐字节复核与历轮同值：
  `ce62802ef484ed3e5332ebde18b990e3e90a666a3e749c71a38f80bae5fdd851`）
- 运行方式（仓库根，nvm Node v22.22.2 + tsx 4.23.12，PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 绕开系统 Node v22.14.0；
  每官方批次头部打印 `node --version` + `process.versions.v8`）：

```bash
node_modules/.bin/tsx /tmp/r40c/r40c-anchor.mts   # ×4（1 预演 + 3 遍独立进程；正典 = 三遍）
```

- 输出摘要（四运行）：§1 表格与逐 rep 行为全量记录；每遍尾行
  `ALL EQUIVALENCE CHECKS PASSED (2768 bitwise checks)`；本轮
  零断言中止、零弃置遍。
- 本轮**零新增计时臂**：无绑定/表示/组成/基片/模式剂量，无决斗
  道，无输家脚本——附录无新增源码块（装具全文已在 R31-C 附录
  A.1 归档，本轮仅头注差异，不重复占档）。

MORE_OPTIMA=no
BRANCH=cursor/r40-c-offline-routing-fortieth-pass-83a1
