MODEL_SLUG=claude-fable-5-thinking-xhigh

# R34-F：`src/experiments/` 第三十四遍复查报告（S7-F-1 + S7-F-2 之上，全新第三十四组枚举）

- **战役:** 全库持久 SOTA 优化 Round 34 / R34-F（R1-F…R33-F 同区三十三遍之后的第三十四遍）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `4aa7f2b`（R34-B/C/D 空枚举摄取 + 本 R34-E/F/G 波派单）。运行全程独占分支**零快进**：分支创建时与全部测量、门禁完成后 `git fetch origin cursor/sota-persistent-opt-83a1` 复核 tip 仍为 `4aa7f2b`，无需 rebase，无新姊妹轴需折入非移植清单。R34-E / R34-G 在飞，按令未等待、未触碰姊妹分支。
- **分支:** `cursor/r34-f-experiments-thirty-fourth-pass-83a1`
- **切片:** 15 文件 `src/experiments/`（全量重读确认，`wc -l` 合计 **2550** 行，与 R29-F…R33-F 普查同数，无缺无余：attribution-report 21 / canary 292 / comparison-report 321 / dataset 206 / evaluation-card 65 / gated-comparison 93 / holdout 140 / isolation 68 / manifest 145 / plan 165 / replay 149 / shadow-compare 44 / shadow 505 / simulation-holdout 272 / threshold-calibration 64）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R24-F…R33-F 同引擎位级相同，带可比；本轮 `node -p process.versions.v8` 直证）；tsx 4.x（仓内 node_modules）、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成（`package.json` / `pnpm-lock.yaml` 对 `519101f` 复核 **0 行**）。本 VM 系统默认 node 为 v22.14.0（`/exec-daemon` PATH 前缀遮蔽 nvm）——按 R32-D §1(iii) / R33-F 在案陷阱**开工即**显式钉 PATH 到 `~/.nvm/versions/node/v22.22.2/bin`，每个官方批次前在同 shell `node --version` 复核 v22.22.2（十个官方批次全部带内自报），**零弃置批次、零批次落在 v22.14.0 上**。

## 结论

**诚实的空枚举：第三十四组没有可立 ID 的新角度，不铸任何 S34-F-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不写 EXCLUSIONS 行，生产代码零改动（R12-F…R33-F 二十二连空之后的第二十三连空）。** R31-F 已把 F 系边界 I/O 面格逐格归属完毕并把唯一残格（边界故障通道处置）按分解并入 R17-F + R30-F + R29-F + 引擎面；R32-F / R33-F 两次独立重推无一格松动（R33-F 附 BINDX 划界行计数订正 9→10，坐标未动）；R34-A/R34-B/R34-C/R34-D 收口后姊妹侧亦无新增未审计平面泄入 F（四份报告全文实读：R34-A 三项站点级换名记录全部住在 tracking/gate-apply 物理对象内；R34-B 零新站点级记录且其两残余角度均为 B 区已点名站点；R34-C 一项换名检查为 propensity C 区属地；R34-D 三项换名记录全部住在 adaptation 物理对象内——与 F 切片**零共享物理对象**）。本轮第三十四遍从零重读 15 个源文件与全部四个生产消费文件、在本 VM 重建全部锚点（r5f/r6f/r7f 各**三次**独立运行 + r1f 一次，§1）、对 R31-F §2 面格属主表做**第四次**独立重推（无一格松动，含 R33-F 订正后 BINDX 计数 **10 token / 8 行** 的首次直接复证，§2）、并按 R32-A 首创且 plan-ban 重铸的哨兵方法对 **F 自档案**在案重开条件做**不计时静态卫生复核**（§3——与 R33-F 同款，仅作卫生性应用、不当边际产出申报、不铸「我又查了一遍重开条件」式新面），全部哨兵**未触发**。逐文件枚举的全部消除角度经换名检查裁决为既有排除 ID 本体、姊妹归档面（禁移植）或停驻门成员——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（本轮新站点级换名检查 **3 项**见 §0；R12-F…R33-F 全部既有换名记录复核维持）。空枚举与 R12-F…R33-F、R34-A…R34-D 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 15 文件自 `519101f` 逐字节零变化（分支创建时 / 测量与门禁完成后 / 提交前**三验** `git diff 519101f..HEAD -- src/experiments/` 均 **0 行**；`git log -1 -- src/experiments/` 显示切片最后一次生产改动仍为 `519101f`「perf(experiments): S7-F-1 aligned-prefix restore fast path + S7-F-2 ASCII-head trim guard」）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均为 docs）。锚点复测（各 ×3，§1 诚实注记）：**r5f C2 119.21 / 119.51 / 128.96 ms**；**r6f partD 121.63 / 123.10 / 121.90 ms**；**r7f partE 129.72 / 130.28 / 128.18 ms**；r6f 三次序 + r7f 四次序 restore 省额**三轮全正无符号翻转**（S7-F-1 ≠ S6-F-5 复核成立：同下标哈希相等 + 后缀回退形态在位，非 O(P) 双指针）；r1f 2668 项逐位检查全过（参考侧成员比较 **167,167,000 次**与历轮锚定值逐位相同）。聚焦实验测试 **75/75 绿**；`pnpm gate`（typecheck + lint + test + build）**全绿**——test 1169 / pass 1168 / fail 0 / skipped 1（与 R33-F / R34-A…R34-D 逐字同计数，exit 0）。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动（X0-11——PAIRX 编排位点 `attribution-report.ts` 本轮零字节未触、本轮零计时装具且无任何脚本 import 两估计器）；无阈值/测试/公开签名/数据面变更；无 fsync/完整性重哈希移除；fail-open 家族保持关闭。仓库变更仅本报告一个文件。不开 PR。

## 0. 冻结、摄取处置与非移植清单（方法第 1、4 步）

- 分支按令创建于派单 tip `4aa7f2b`；运行全程独占分支零快进（两次 fetch 复核同 tip），冻结三点核对：分支创建时 / 测量与门禁完成后 / 提交前，`git diff 519101f..HEAD -- src/experiments/` 均 **0 行**。
- 派单 tip 携带的四份姊妹报告全文实读并划界：**R34-A**（A 区空枚举——三项站点级换名记录 `skipped` 单例 / `completedChecks` 防御拷贝 / `readersInvoked` 享元，全部 tracking/gate-apply 站点，零共享物理对象）；**R34-B**（live 路由空枚举——零新站点级记录，S12-B-2 腿 (a) / S13-B-1 属主复核未触发，B 区属地）；**R34-C**（离线路由空枚举——一项换名检查 `isFabricatedPositiveSupport` `notOneHot` 吸收析取属 propensity C 区物理对象；其 9 文件全在 `src/routing/`，唯一 F 邻接顶点 `attribution-report.ts` 系消费图节点，PAIRX 既禁）；**R34-D**（adaptation 空枚举——三项换名记录全部 adaptation 站点；其对 `promotion-rules.ts:125–129` production-labeled 拒收的读码见证系硬不变量证人非新面）。四者均为空枚举轴外号，**已并入非移植清单，不重编号、不移植、不重提**。
- **非移植清单全额维持并逐项核对**（派单 DO NOT REMINT 全清单 + round-34/PLAN F 段）：R30-F 边界输出消费/快照读回拓扑面、R29-F 输出保留/被替换快照存活面、R28-F 边界调用协议面、R27-F 输入访问路径机制面、R26-F 输入存储状态面、**R31-F 故障通道处置残格**（propagate/swallow/read-message/materialize-stack/retain/wrap-rethrow 已分解归 R17-F + R30-F + R29-F + 引擎面——本轮不重铸、不新停驻、零计时臂）、**PAIRX**（R29-C——编排位点共驻不当新 F ID）、**BINDX**（R31-C——切片 ambient `Math.*` token 恰 **10 个于 8 行**：comparison-report.ts:120 ×2（行内双 `Math.sqrt`）/ 185 / 190 / 195 / 200、gated-comparison.ts:92 ×2、replay.ts:69/70，即 sqrt 4 + abs 4 + imul 2——R33-F 计数订正（9→10）本轮 `rg -o` 逐 token 直证**首次独立复证成立**；行集合逐行同一、坐标未动；同 BINDX 坐标划界计数即止，零 patch 零剂量）、**MODEX**（R31-J——切片 grep **零** chmod / 零 umask / 零八进制 mode 字面量、零 fs 写入位点，权限模式字坐标在 F 结构性空，划界声明即止）、I/O text-codec topology / per-run transcode-byte manifest（R31-E——`rg "node:fs|readFile|writeFile|Buffer" src/experiments/` 本轮首手复核**零匹配**，公开边界全同步纯内存，面结构性空）、REPRX（R30-C——切片 `JSON.parse` 输入位点 **0**）、R32-A 重开条件哨兵审计（**方法不重铸为面**——§3 按令仅作卫生性应用于 F 自档案）、R32-B 闭合稳定性审计（**方法不重铸为面**——本轮未重走腿分类，F 侧裁决有效性由 §3 哨兵 + 0 行冻结携带）、**R33-A … R33-J 空枚举**（各站点级换名记录与轴归档均为属地记录，不移植不重提——含 R33-F 自身四项换名记录之复核维持见下）、**R34-A / R34-B / R34-C / R34-D 空枚举**（上条已划界，声明即止）、R31-A injected-callable 停放门（HoldoutVault 注入缝见 R32-F §0 换名记录 2，本轮维持不重提）、R31-B 六停驻门、R31-D 的 R22-D 属地格、R31-G 18 门审计、R31-H 四扇边界参数残门、R31-I CLI 进程边界门扉闭合审计（F 是进程内库，零 argv/stdin/信号/spawn/exit 腿，平面正交零交集——切片仅 `node:path`（isolation.ts:1）与 `node:crypto` randomUUID（holdout.ts:1）两个 node: 引入，本轮 rg 直证恰 2）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / CSPELL / LINKTOP 全部未编号未移植。S7-F-1 ≠ S6-F-5 维持（§1 次序符号复核）。S5-F 已落地不重做（plan.ts 单探针现场在位，§4）。
- **换名重提检查**（本轮枚举全部新角度的逐项裁决，一律不铸 S34-F-\* ID；以下 **3 项为本轮新站点级记录**——均为在手报告未逐字点名过的站点，裁决同族同向；其余全部命中既有 ID 本体见 §4；R33-F 四项换名记录——replayPolicy spread 复制 / shadowDecisionAt 闭式跳步 / 改进声明孪生正则 / requireCanaryBlock 重派生缓存——与 R32-F 三项既有记录本轮全部复核维持不重提）：
  1. **`runSimulationHoldout` 条件 spread 装配链改后置条件赋值**（simulation-holdout.ts:90–93 对 `buildR1ShadowReport` 输入的四连 `...(x !== undefined ? {…} : {})` + L108–115 结果装配的两层条件 spread——改命令式条件赋值可免中间空对象与 spread 迭代）= **S8-E-2 条件 spread 后置化跨区镜像**（R34-A §0 记录 3 同链）+ R15-F 轴 3 引擎面（spread 分配域）+ 形状单态化属 S9-D-1 族；且该链**零生产消费者**（§2 六链普查 rg 精确交替式 exit 1 本轮直证）+ once-per-run µs 域。零流量 + µs + 既有面三重拒绝，不铸；
  2. **`mean`/`sampleStandardError` 孪生辅助去重**（gated-comparison.ts:79–93 与 comparison-report.ts `pairedDeltaSummary` 内联 mean/方差算术同源各自实现；同族孪生第二对：shadow.ts `resolvePendingMembership` 与 canary.ts `resolveCanaryPendingMembership` 同构环）= 去重**不消除任何运行时工作**（同一算术两处都必须执行，收益恰为零——比 R33-F 记录 3 的孪生正则更弱：那里至少省一次 once-per-process 编译），只改跨文件 import 图/模块公开面；孪生去重家族（R33-F 换名记录 3 同族）+ X1-5 薄封装维持（canary/shadow 契约不同不并）。零收益 + 模块面变更双重拒绝，不铸；
  3. **`validateComparisonReport` 家族查重探针单探针化**（comparison-report.ts:292 `new Set(report.familyBreakdown.map(…)).size !== length` 改 S5-F 型 add+size 单探针可免中间 map 数组）= 等价成立（SameValueZero 同语义、布尔结局同一）——但该站点 once-per-report 且基数 = 去重后 taskFamily 数（生产个位数，病理上界 ≤ 记录数）⇒ 真尺度亚 µs；**S9-B-1 同族拒绝**（S5-F 镜像在 once-per-X 低基数站点不达线；S8-F-3 / S8-F-2 镜像同链）。亚 µs 拒收类，不铸。
- 未重开任何 X\* / S1-\* … S33-\* 条目。**单变量纪律**：本轮**无任何计时新臂**——三个锚点脚本均为 `scripts/round0{5,6,7}-*.ts` 入库血统资产逐字节原样重跑（零改动零参数），r1f 同；§2/§3 全部为静态 rg / 读码断言（不计时）。S9-D-4 / S12-D-1 / S8-E-1 / S8-I-1 / S13-B-1 / S12-B-2 等姊妹区维持项零触碰（其哨兵属地在 A/B/C/D/E/I，本轮不代跑——R34-B/R34-C/R34-D 已各自复核未触发）。

## 1. 锚点重测（r5f/r6f/r7f 各三次独立运行，r1f 一次；本 VM 实测，未拷贝历轮数字；nvm Node v22.22.2）

| 档位 | 第 1 次 | 第 2 次 | 第 3 次 | R33-F 记载带 |
| --- | --- | --- | --- | --- |
| r5f C2 全实验（P=2000/A=1000） | 119.21 ms | 119.51 ms | 128.96 ms | 124.47 / 118.96 / 129.73 ms |
| 2A-validate 组件 | 98.49 ms（82.6%） | 99.41 ms（83.2%） | 98.08 ms（76.1%） | 98.05–109.46 ms（82.0–84.4%） |
| r5f C1 has+add → add+size 省额 | +29.12 ms（20.0%） | +30.60 ms（20.8%） | +32.18 ms（21.5%） | +30.50~+32.04 ms（20.6–21.3%） |
| r6f partD 全实验 | 121.63 ms | 123.10 ms | 121.90 ms | 122.96 / 122.14 / 121.70 ms |
| r7f partE 全实验 | 129.72 ms | 130.28 ms | 128.18 ms | 127.78 / 127.36 / 127.07 ms |
| r7f validate 卫（×2000） | +8.87 ms | +8.56 ms | +8.68 ms | +8.00 / +9.24 / +9.37 ms |

**次序符号（committed 回归脚本原生视图，全部为「落地生产形态 vs 冻结参考」省额）**：r7f partE 四次序三轮全正——prefix(aligned) +45.24/+43.82/+44.50、half-aligned +28.36/+25.85/+26.45、scattered +4.76/+4.26/+5.09、reversed +5.14/+5.04/+4.79 ms，无符号翻转；r6f partD 三次序三轮全正——prefix +126.63/+128.93/+128.12、scattered +32.03/+32.08/+31.05、reversed +29.60/+30.01/+30.55 ms。**S7-F-1 ≠ S6-F-5 复核成立**：落地代码零漂移（切片零 diff），同下标哈希相等 + 后缀回退形态（非 O(P) 双指针）在全部次序下三轮同号为正。r5f C4 索引环形式 −2.70/−2.66/−4.75 ms 维持 S5-F-3 抖动带记载（R33-F −2.29/−3.61/−1.14）；C5 dataset 镜像 12.2–15.2 µs（U=2000）/ 131.3–138.0 µs（U=20000）维持 S5-F-1 记载（R33-F 9.6–14.6 / 134.7–143.8，带交叠）。r1f 2668 项逐位检查全过（生产侧 63.3 ms / 参考侧 631.6 ms，10.0×；参考侧成员比较 **167,167,000 次**——与历轮锚定值逐位相同；参考侧壁钟非锚点义务，钉死量是比较计数与逐位等价）。

**诚实注记**：(i) r5f runs 1–2（119.21/119.51）落 R33-F 带内偏低缘，run 3（128.96）居历史读数并集（117.5–133.5）内高侧——三读数带 119.21–128.96 与 R33-F 带（118.96–129.73）几乎重合。(ii) r5f run 3 validate 组件占比 **76.1%** 低于历轮 81.1–84.5% 带——但 validate 组件**绝对值**三轮紧致（98.08–99.41 ms，带宽 1.4%），run 3 的增量全部落在分母的非 validate 残部（assign 路径 + recordOutcome + GC 窗口方差）⇒ 判分母效应的 VM 窗口个体，非 validate 结构漂移（切片零 diff，r1f 计数逐位同历轮，C1 省额比例 20.0–21.5% 居 R26-F…R33-F 紧邻带 19.9–23.5% 内）。(iii) r6f 三读数 121.63–123.10 与 R33-F 带（121.70–122.96）交叠，完全落 R24-F…R29-F 带（117.9–126.9）内。(iv) r7f 三读数 128.18–130.28 高于 R33-F 带顶（127.78）0.40–2.50 ms，但全落 R28-F…R30-F 包络（126.63–132.69）内且与 R31-F 带（127.81–130.55）交叠——四次序符号三轮稳定为正、S7-F-1 省额方向不变，量级个体属同款 VM 窗口注记，不构成任何重开触发（§7）。(v) C5 为 µs 域 informational 镜像（链 test-only），带交叠成立，不构成触发。

## 2. 调用面复核 + 面格属主表独立重推（元闭合，方法第 3 步前置）

生产消费图 rg 全仓复核（与 R30-F §1.2 / R31-F §2 / R32-F §2 / R33-F §2 逐条一致）：切片外 `src/` importers 恰四个文件，import 行逐条核对——`adaptation/reflection.ts`（validateExperimentPlan + createShadowRunner + 类型：唯一热交错消费者）、`adaptation/eval-routing.ts`（comparison-report 类型与函数 + gatedComparisonReport + createIsolationGuard + stableStringify + **replayCacheKey**——对 replay.js 的 import 恰此一项，S2-F-2 前提本轮直证成立）、`adaptation/promotion-rules.ts`（validateComparisonReport 直呼）、`routing/r1-shadow-report.ts`（comparison-report + gated-comparison）。**故障通道处置普查复核**：四消费文件 catch 位点合计 4 处、全部在 eval-routing.ts（L219/L249/L259 包 `JSON.parse`/`readFile`，非切片物），**恰一处**包切片调用——L336 `assertReplayIsolated` 包 `createIsolationGuard`（MESSAGE + WRAP-RETHROW 坐标，once-per-eval 冷通道，本轮 L325–340 读码直证）；`reflection.ts` / `promotion-rules.ts` / `r1-shadow-report.ts` 零 catch ⇒ 热通道生产坐标仍 = PROPAGATE。切片故障源普查：**142 处** `throw new DomainValidationError`（逐文件计数与 R31-F/R32-F/R33-F 全同：shadow 27 / plan 22 / dataset 19 / canary 14 / manifest 14 / holdout 11 / simulation-holdout 10 / comparison-report 8 / evaluation-card 7 / isolation 6 / replay 3 / gated-comparison 1 / 其余三文件 0），切片零 `throw new TypeError/Error` 直抛点——故障通道单类维持。

R31-F §2 面格属主表本轮从零**第四次**独立重推（(o) = F 区属主轴；(s) = 姊妹区收口面，明令禁移植；每格属主裁决**无一松动**）：输入存储状态 (o) R26-F / 访问路径 (o) R27-F / 对象表示 (s) REPRX / 引用身份·别名 (s) R29-A / 流组成 (o) R20-F / 计划标头身份几何·计数·成员拓扑 (o) R25-F/R23-F/R22-F / 规模与二阶联角 (o) R14-F 域 + (s) R30-B / 调用协议 (o) R28-F / 排程·实参物化·调用上下文 (s) CALLB/R28-H/R22-H / 调用流次序·分派历史 (s) R30-A / 时钟来源 (s) R29-E / 执行基片·名字解析 (s) R27-H 族 / BINDX / 引擎态 (o) R15-F 轴 3 + R19-F 域 / 输出成功通道保留 (o) R29-F / 输出成功通道消费·读回 (o) R30-F / 输出故障通道制造·单价 (o) R17-F / **输出故障通道调用方处置 = R31-F 分解归属（R17-F + R30-F + R29-F + 引擎面），本轮零计时零重铸维持** / 双估计器共驻 (s) PAIRX（X0-11 禁区）。结构性空直证两行维持：**I/O 编解码拓扑 (s) R31-E——切片 fs/codec 位点 0**；**持久工件权限模式字 (s) R31-J MODEX——切片 chmod/umask/mode 位点 0、零盘上工件**。**BINDX 划界行 R33-F 计数订正首次独立复证**：`rg -o "Math\.\w+"` 逐 token 直数恰 **10 个于 8 行**（120 行双 `Math.sqrt` 在列），R33-F 订正成立、行集合坐标属主裁决全部不动。第三十四遍枚举在该坐标系内**无剩余未审计平面**。

## 3. F 档案在案重开条件哨兵复核（全部不计时；R32-A 方法卫生性应用，不铸面、不当边际产出）

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| R31-F §7 (a) | 任何 `src/` 生产调用方在热交错内 catch 边界故障并做非平凡处置 | §2 catch 普查：热消费者 reflection.ts 零 catch；唯一切片 catch 仍为 eval-routing L336 once-per-eval ⇒ **未触发** |
| R31-F §7 (b) | 故障对象携带非字符串 own 载荷 | `src/domain/errors.ts` 在最强冻结内（`git diff 89f0455..HEAD -- src/` 0 行）⇒ **未触发** |
| R31-F §7 (c) | 故障通道契约（消息文本/单类不变量）解冻 | 独占分支零快进且无解冻声明；切片零 diff ⇒ **未触发** |
| S6-F-2 重开 | delete+size 形态实测稳定反超 >5% | 无任何反超证据；本轮落地形态三/四次序省额三轮全正（§1）——按纪律**不重计时被拒形态**（重计时即再剂量）⇒ **未触发** |
| S6-F-5 重开 | assignment 次序被契约化为 population 子序列 | 冻结 0 行 + round-34 PLAN 重申 S7-F-1 ≠ S6-F-5 ⇒ **未触发** |
| S5-F-1 / S4-F-1 前提 | dataset/simulation-holdout 链获得生产消费者 | `rg 'experiments/(dataset\|simulation-holdout\|holdout\|shadow-compare\|threshold-calibration\|attribution-report)' src/`（切片外）本轮**零命中**（exit 1，精确六名交替式，沿 R33-F 更净口径）⇒ 六链零生产消费者 ⇒ **未触发**（§0 换名记录 1 的零流量腿即此） |
| S2-F-2 / S4-F-2 前提 | `replayPolicy` 获得生产调用方 | eval-routing 对 replay.ts 的 import 恰 `replayCacheKey` 一项（import 行直证，§2）⇒ **成立维持** |
| S2-F-1 前提 | shadowDecisionAt 占比脱离 ~1% 域 | 切片零 diff + 锚点带内 ⇒ **成立维持**（R33-F 换名记录 2 的真尺度拒绝依此维持） |
| X0-11 / PAIRX | 双路估计器拆分/重排/省略 | attribution-report.ts 零字节未触；本轮零装具 import 估计器 ⇒ **冻结在位** |
| 签名/调用图类（S1-F-6、S1-F-8、X3-3、S3-F-2 等） | 要求 `src/` 变更 | 最强冻结 0 行 ⇒ **全体判空** |

全部哨兵未触发。凡姊妹属地哨兵（S12-B-2 / S13-B-1 / S8-E-1 / S8-I-1 / S9-D-4 / S12-D-1）本轮不代跑——其属主轮 R34-B / R34-C / R34-D（及 R33-E / R33-I）已各自复核未触发。

## 4. 逐文件收口（R1-F…R33-F 收口之上的本轮检查点）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| `shadow.ts` | 27 抛点单类维持；S7-F-1 落地形态零漂移、四次序省额三轮全正（§1）；shadowDecisionAt 闭式跳步 = R33-F 换名记录 2 维持（S2-F-1 已定价亚带 + SEEDX 邻域）；shadowDecisionAt O(index) 重放 = S2-F-1；restore→assign Set 复用 = S2-F-4/S9-F-1；recordOutcome 末元素见证 = S9-F-2、查重/成本增量 = S1-F-8；accumulatedCostUsd 每 record 全扫 = S1-F-8 本体（成本累加增量化已拒）；空 assignments 跳 Set = S3-F-3；计划引用同一跳验 = S3-F-2；每操作全量 fail-closed restore = X3-3 本体；返回值拷贝省略 = R32-F 换名记录 3 维持（R29-A/X4-2/X3-3 族）；applyExperimentClock/haltOnAssignmentBudget 状态 spread 对 = R15-F 轴 3 引擎面；`resolvePendingMembership` 孪生 = **§0 换名记录 2 同族注记** | 无候选 |
| `plan.ts` | 22 抛点；S5-F 单探针 + S7-F-2 首字符卫现场在位零漂移（不重做）；批量 Set = S5-F-2、null-proto = S6-F-4、批快路径 = S6-F-3、长度 1 快路径 = S8-F-3、scratch 共享 = S9-F-3、返回 Set = S1-F-6；EXPERIMENT_ID_PATTERN 模块级已提升；r5f C1/C2、r7f validate 卫三轮带内 | 无候选 |
| `canary.ts` | 14 抛点；同构对齐环 + S6-F-1 回退零漂移；derivedExposure 重算是 fail-closed 交叉验证本体（消除即 fail-open，非候选）；requireCanaryBlock 重派生缓存 = R33-F 换名记录 4 维持（零生产消费者 + µs + X1-1）；reversibleScopes includes = S1-F-7；exposure 派生为契约本体；`resolveCanaryPendingMembership` 孪生 = **§0 换名记录 2 同族注记**（X1-5 契约不同不并） | 无候选 |
| `gated-comparison.ts` | 1 抛点；IMPROVEMENT_CLAIM 孪生正则 = R33-F 换名记录 3 维持；**mean/sampleStandardError 孪生辅助去重 = §0 换名记录 2**（零运行时收益 + 模块面）；strip-retry 复用 = S1-F-1；pairedEvaluationCard 多遍融合 = S2-F-6；mean/SE 双遍 = 同族 | 无候选 |
| `replay.ts` | 3 抛点；orderedHashes spread 复制消除 = R33-F 换名记录 1 维持（零生产调用方 + µs + R15-F 轴 3）；双 manifestHash = S2-F-2（零生产调用方本轮直证）；selected propensityFor 二次调用 = S1-F-2；空 exclusions 免建 = S4-F-2；exclusions Set 化已落地（H1）；rerunHash 换更快同字节序列化 = S6-D-5 已拒（且 replayPolicy 零流量）；replayCacheKey 唯一生产腿 = once-per-eval µs（S7-D-2 邻域） | 无候选 |
| `comparison-report.ts` | 8 抛点；IMPROVEMENT_PATTERN 孪生编译 = R33-F 换名记录 3 同项；六遍改单遍 = X3-2；校验遍+构建遍双遍 = X3-2 族；四均值 reduce = X3-2 族；`Array.from(families.entries())` = S1-F-5；delta CSE = S4-F-3；**familyBreakdown 查重单探针化 = §0 换名记录 3**（S9-B-1 族亚 µs）；validateComparisonReport 生产消费者（promotion-rules）零 catch = PROPAGATE 维持 | 无候选 |
| `threshold-calibration.ts` | 零抛点；三遍并单遍 = S1-F-4（一次性 informational） | 无候选 |
| `shadow-compare.ts` | 零抛点；薄封装维持 X1-5 | 无候选 |
| `isolation.ts` | 6 抛点；resolve(outputRoot) 提升 = S3-F-4；per-root 双 isInside 的 resolve 重复 = 同族 + once-per-eval µs；**全生产普查唯一被 catch 的切片故障源**（eval-routing L336，once-per-eval 冷通道，§2） | 无候选 |
| `simulation-holdout.ts` | 10 抛点；**结果装配条件 spread 链 = §0 换名记录 1**（S8-E-2 镜像 + 零流量 + µs）；探针去重镜像 = S5-F-1（链零生产消费者本轮直证）；尾部双验 = S2-F-5（fail-closed 纵深）；observationsFromTrain 构建+observationsForR1 二遍 = X3-2 族 + 零流量；toFrozenEpisode 投影 = S3-F-5；auditHoldoutAccess try/register = once-per-run | 无候选 |
| `holdout.ts` | 11 抛点；审计追加拷贝 = S1-F-3/X4-2；注入缝默认臂 nowIso/randomUUID = R32-F 换名记录 2 维持（R31-A 停放 + R26-D 同类 + 零生产流量）；Map 重 set 不可变态 = X4-2 族 | 无候选 |
| `manifest.ts` | 14 抛点；stableStringify 形态改造 = R32-F 换名记录 1 维持（字节契约仪器 + 零热流量 + S1-I-7/X1-1 族）；validateManifest 每建 Set = once-per-creation；`?? []` 空数组默认分配 = R15-F 轴 3 引擎面 ns 域（mark/assert 腿 test-only 零流量） | 无候选 |
| `evaluation-card.ts` | 7 抛点；validate 数组字面量 = S1-B-7 域 | 无候选 |
| `dataset.ts` | 19 抛点；seen Set 复用 universe = S4-F-1；has+add 原形镜像去重 = S5-F-1（test-only 链维持，C5 本轮 12.2–15.2 µs 带注记见 §1(v)）；membership Map 双结构 = 同域；rotateHoldout 双验 = S2-F-5 族 once-per-rotation | 无候选 |
| `attribution-report.ts` | 零抛点；**PAIRX 编排位点**——零字节未触、X0-11 双路对未拆分/未重排/未省略 | 无候选（共驻剂量归 R29-C） |

## 5. 候选总表

**空。** 本轮零候选：面格属主表第四次独立重推（§2）无一格松动且无新残格（BINDX 订正后计数 10/8 首次独立复证成立，坐标不动）；哨兵复核（§3）按定义不产出候选（不计时、不剂量）；十五文件枚举（§4）全部命中既有 ID 本体、姊妹归档面、停驻门成员或本轮 §0 换名记录。本轮无任何计时新臂 ⇒ 无可定价候选面。

## 6. CHECKS

- 分支恰 `cursor/r34-f-experiments-thirty-fourth-pass-83a1`，基线 `4aa7f2b`；运行全程独占分支零快进（两次 fetch 复核同 tip）。
- 冻结三点核对全 **0 行**（§0）；15 文件普查 2550 行与 R29-F…R33-F 同数；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**；`package.json` / `pnpm-lock.yaml` 对 `519101f` **0 行**。
- 锚点 r5f/r6f/r7f 各三次独立运行 + r1f 一次全部 0 失败（224 / 27 / 169 / 2668 项，§1）；四次序 + 三次序省额三轮全正，无符号翻转；全部在 nvm Node v22.22.2 上执行（每个官方批次前 `node --version` 在同 shell 复核，零弃置批次，零批次落在系统 v22.14.0 上）。
- 生产消费普查：4 个 importer 文件（import 行逐条核对）；catch 位点恰 1 处包切片调用（冷通道 once-per-eval，L325–340 读码直证）；热消费者零 catch；142 抛点逐文件计数与 R31-F/R32-F/R33-F 全同；切片零直抛 Error/TypeError。
- 姊妹面划界计数：BINDX `Math.*` **10** token 于 8 行（R33-F 订正首次独立复证；行集合同一）；R31-E codec 位点 0；REPRX `JSON.parse` 位点 0；MODEX chmod/umask/mode 位点 0；node: 引入恰 2（isolation.ts node:path、holdout.ts node:crypto）；六链切片外消费者 0（精确交替式零命中 exit 1）。
- 聚焦测试：`npx tsx --test test/unit/experiments/*.test.ts` = **75/75 pass**（10 suites）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿**（exit 0）——test 1169 / pass 1168 / fail 0 / skipped 1（既有 provider-smoke 凭据跳过，与 R33-F / R34-A…R34-D 记录同计数）；Node v22.22.2。
- 仓库改动恰本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；live = R0 等价（R1 未接线）；promotion proposal-first；无 Outcome-supported 主张（Checkpoint F-PROD 开放，ADR-005）；无阈值/测试/权限/数据面/公开签名改动；CAS/凭据契约原样；无 fsync/完整性重哈希移除；fail-open 家族保持关闭；分析未变更任何 in-flight run；EXCLUSIONS / PROGRESS / PLAN / README 未编辑；不开 PR。

## 7. 重开条件

本轮无新定价轴，不加行。R31-F 故障通道处置分解的三条显式重开触发（(a) 热交错内非平凡 catch 处置；(b) 非字符串 own 载荷；(c) 通道契约解冻）经 §3 哨兵直证全部未触发，原文全额维持；R30-F（输出消费）、R29-F（输出保留）、R17-F（拒绝终端）、S6-F-2 / S6-F-5 / S5-F-1 / S2-F-1 / S2-F-2 既有重开条件同样全额维持。锚点个体注记（r5f run 3 validate 占比 76.1% 的分母效应、r7f 三读数高出 R33-F 带顶 0.40–2.50 ms 的 VM 窗口个体，§1 注记 (ii)/(iv)）不构成重开触发——validate 组件绝对值三轮紧致带宽 1.4%、带并集交叠、省额符号三轮稳定、r1f 计数逐位同历轮。本轮三项换名记录（§0）均为拒列归档，不设新重开条件。

## 8. 新增排除 ID

**无。** 空枚举轮不铸 S34-F-\* ID、不铸 face 名、不取绰号、不加 EXCLUSIONS 行。换名检查 3 项新记录见 §0（runSimulationHoldout 条件 spread 装配 / mean-SE 孪生辅助去重（含 resolvePending 孪生对同族注记）/ validateComparisonReport 家族查重单探针化），归档备后续轮次引用，无需并入 EXCLUSIONS.md；R32-F 三项 + R33-F 四项既有换名记录复核全部维持。

## 附录：仿真出处与运行摘录

本轮**未新建**任何仿真/见证脚本：全部计时证据来自入库血统资产的逐字节原样重跑（`scripts/round01-r1f-equivalence-sim.ts`、`scripts/round05-r5f-equivalence-sim.ts`、`scripts/round06-r6f-equivalence-sim.ts`、`scripts/round07-r7f-equivalence-sim.ts`，零改动零参数）；§2/§3 哨兵为只读 rg / 读码断言（不计时、无临时文件）。运行：`npx tsx scripts/round0X-*.ts`（nvm Node v22.22.2，PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开 `/exec-daemon` 系统 Node v22.14.0）。

三次官方运行关键行摘录（r7f；r5f/r6f 对应行见 §1 表）：

```text
run 1: part E prefix saving 45.24ms | half 28.36 | scattered 4.76 | reversed 5.14
       validate x2000 saving 8.87ms | full experiment 129.72ms | 169 checks, 0 failures
run 2: part E prefix saving 43.82ms | half 25.85 | scattered 4.26 | reversed 5.04
       validate x2000 saving 8.56ms | full experiment 130.28ms | 169 checks, 0 failures
run 3: part E prefix saving 44.50ms | half 26.45 | scattered 5.09 | reversed 4.79
       validate x2000 saving 8.68ms | full experiment 128.18ms | 169 checks, 0 failures
```

MORE_OPTIMA=no
BRANCH=cursor/r34-f-experiments-thirty-fourth-pass-83a1
