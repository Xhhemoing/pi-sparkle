MODEL_SLUG=claude-fable-5-thinking-xhigh

# R34-G：runtime/graph/domain 切片 Round 34 三十四搜报告

**战役:** 全库持久 SOTA 优化 Round 34 / R34-G（十区之一，R1-G…R33-G 的第三十四遍）
**基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `4aa7f2b`（派单 tip，R34-B/C/D 摄取 + 本 R34-E/F/G 波派单）；工作期间独占分支 docs-only 快进**两笔**：`4aa7f2b` → `b92b1fc`（R34-E 空枚举摄取 + R34-H 派单，门禁后 fetch 所见）→ `7f77080`（R34-F 空枚举摄取 + R34-I 派单，推送时 fetch 所见）。增量核对：两笔各自 `git diff --stat` 均仅触 `docs/reports/sota-opt/` 下 3 文件（PROGRESS / round-34 PLAN / R34-E.md、R34-F.md），对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接复核各 **0 行**；按令两次 rebase 报告分支（均纯快进零冲突），42 文件冻结在每个新 tip 复验均 **0 行**；R34-E / R34-F 结论已读并折入非移植清单（§0）。全部锚点与测量在 `4aa7f2b` 工作树完成——两笔快进增量 `src/` 逐字节零变化 ⇒ 测量形态与提交形态位级同一，全部有效；聚焦套件 + `pnpm gate` 在 `b92b1fc` 与最终 tip `7f77080` 各复跑一次全绿同计数。R34-H / R34-I 在飞，按令未等待、未触碰。
**分支:** `cursor/r34-g-runtime-thirty-fourth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（R13-G…R33-G 同引擎线，带可比；tsx 4.23.12 经仓内 node_modules，pnpm 10.17.1，`pnpm install --frozen-lockfile` 干净完成；本轮 PATH 在任何计时前钉死 v22.22.2（`which node` / `node --version` / `process.versions.v8` 三复核），**零** v22.14.0 误跑批次、零弃置批次）。

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S34-G-\* ID，不铸新 face 名、不取绰号、不加 EXCLUSIONS 行，切片零代码改动。** 第三十四遍从零重读 42 文件（`wc -l` 合计 **8960** 行与 R32-G / R33-G 记录一致，清单核对恰 42 文件：run 17 + supervisor 4 + graph 4 + domain 17），逐文件枚举的全部消除角度经换名检查裁决为既有排除 ID 本体、姊妹归档面（禁移植）、plan-ban 轴名或全局计算顶之内的亚噪声——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（§3）。本轮恰 **1 项**候选角度构成站点级换名记录（`domain/flowchart.ts` `validateJoin` 逐 required-ref `edges.some` 线扫的 O(refs·E) 内形——三重拒列：计算顶探针本体整体计价 + 一次性 µs 拒收 + 校验平面家族，§3.0-1），其余候选角度全部为 R33-G 已点名站点复命中（leaseReadyNodes 逐节点 computeStatus 重算、advanceLedgerRound 数组 spread 记账、planRound 死形参、S2-G-2 共享 visiting 本体第三十四遍复认——复命中即止，不重演 R33-G 的独立复推导叙事）。R31-G 的 18 扇聚合门闭合审计按令不重铸。本轮边际动作仅两类，均不构成新面：(a) 缺省预算重锚（计算顶 **0.296–0.399 ms/run**，其中 B3 高点 0.399 为宿主干扰窗单点、两次同引擎表征运行 0.292/0.293 未复现，§1 诚实注记；I/O 地板 **86.8–94.4 ms/run**，支配比原始 **~237–314×**、以表征顶校正 B3 后 **~288–322×**，两个数量级支配关系不变）；(b) G 档案在案零剂量停放与重开条件的**不计时静态哨兵复核**（§2——方法属 R32-A 首创并已 plan-ban 重铸，此处按令仅作卫生性应用，不申报为新轴），全部哨兵**未触发**。14 格矩阵摘要 `06cbcf92c098c8f0…` 官方引擎三批 × 进程内 3 迭代 = **9 次逐位一致——第二十五轮同值，按令不铸摘要恒等 ID**（两次表征运行再加 6 次同值，不计官方）。空枚举与 R12-G…R33-G、R34-A/B/C/D/E 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 42 文件自生产基线 `4efee23` 逐字节零变化（派单 tip / 两次快进 rebase 后 / 提交前四点核对 `git diff 4efee23 -- <42 文件>` 均 **0 行**；`git diff --find-renames --name-status` 同区间**空输出**——零改名零增删，无改名裁决需做）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均 docs，快进两笔亦纯 docs）。聚焦套件 run/supervisor/graph/domain + integration：**tests 386 / pass 385 / fail 0 / skipped 1** 全绿（`4aa7f2b` / `b92b1fc` / 最终 tip `7f77080` 树各一次，逐字同计数，v22.22.2，与 R31-G / R32-G / R33-G 记录一致）；`pnpm gate`（typecheck + lint + test + build）**全绿三次**（三 tip 各一次）——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A / R32-G / R33-G / R34-A…R34-F 记录逐字同计数），退出码 0。不开 PR。

## 0. 范围与约束遵守（含不移植清单）

- 切片：`src/run/`（除 A 属地 `child-tracking.ts` / `gate-apply.ts`）17 文件 + `src/supervisor/`（除 B 属地 `model-router.ts`）4 文件 + `src/graph/` 4 文件 + `src/domain/` 17 文件，恰 **42**，全部实际重读全文，未依赖前三十三轮记忆。
- 读序遵令：README → PROGRESS → EXCLUSIONS 全表 → round-34/PLAN（G 段 + 全 ban 榜）→ R33-G 全文 → R34-A → R34-B → R34-C → R34-D，之后才读 42 切片文件并动手；两次快进后各补读 R34-E.md / R34-F.md 全文（仅为划界与非移植清单，不据其改动任何测量或结论）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README.md 零触碰。
- **本轮六姊妹空枚举划界（零新轴可移植）**：R34-A（tracking 侧三项站点级换名记录已拒——全为 `child-tracking.ts` / `gate-apply.ts` / tracking A 属地物理对象，二文件按令排除于本切片，G 侧零共享）；R34-B（live 路由侧零新记录，S12-B-2 / S13-B-1 哨兵重取证未触发——B 属地；G↔B 唯一共享坐标为 `flowchart-run.ts` 的 learned-routing 消费腿，Live=R0 不变量 + S8-E-1 不去重 + 属地边界，零剂量，本清单原有）；R34-C（`routing/propensity.ts` 被吸收析取换名检查已拒——C 属地物理对象，G 切片零 propensity 消费）；R34-D（adaptation 侧三项站点级换名记录已拒——全为 `src/adaptation/` 物理对象，G 侧零交集）；R34-E（第一笔快进带入，learning 侧一项新站点级换名记录已拒：`loadLearnedRouting` L123 正则探针，E 属地 `src/learning/learned-routing.ts` 物理对象、R34-D 记录 3 的 E 侧孪生——G 切片对 learning 的唯一共享坐标仍为 `flowchart-run.ts:712` learned-routing 消费腿，零剂量，本清单原有）；R34-F（第二笔快进带入，experiments 侧三项站点级换名记录已拒：simulation-holdout 条件 spread 后置化 / mean 孪生辅助去重 / validateComparisonReport 单探针化——全为 `src/experiments/` 物理对象，G 切片零 experiments 导入，平面正交零交集）。六报告均未铸新面，非移植清单无需加行。
- **不移植禁令逐项核对**：M1 亲代 run 子调度格（R30-G）、M2 监督引擎裁决格（R29-G）、decision-gate resolution（R28-G）、join-policy（R27-G）、**RUNLIM / CKMIX / TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / ZREP**——本轮零剂量零重提；**BINDX**（切片 `Math.*` token 恰 **6 个**：hash.ts `Math.abs` ×1、inspection.ts `Math.max` ×3、flowchart-supervisor.ts `Math.max` ×2，与 R32-G / R33-G 计数逐一同址，rg 直证计数即止零 patch）；**MODEX**（切片 rg **零** chmod / 零 umask，exit 1 声明即止）；I/O text-codec topology / per-run transcode-byte manifest（R31-E——切片显式 fs UTF-8 位点恰 **4 个**：checkpoint-store.ts:22/:31、pause-controller.ts:31/:92，与 R32-G / R33-G 记录逐一同址；event-store / episode-store 经切片外 `persist/jsonl.ts` 走同一坐标——E 属地，本轮零编解码剂量）；R31-G 18 门闭合审计（**不重铸「我又审了一遍门」**；摘要恒等第二十五轮不铸 ID）；R32-A 重开条件哨兵审计与 R32-B 闭合稳定性审计（**方法不重铸为面**——§2 按令仅应用于 G 自档案在案条目，不申报为新轴）；**R33-A…R33-J 空枚举 / R34-A…R34-D 空枚举**（方法卫生记录，不重演账本分类、不重提「我又查了一遍重开条件 / 门扉 / 未点名站点」）；R31-B 六停驻门（cascade 升级臂证据 × 失败类格维持 routing 属地停放）；R31-E/F/H/I 各属地格全额维持。FITQ / SCALEX / YMIX / KFAN / NVG / SEEDX / SHAPEK / ORDX / CALLB / OCCX / JB / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / PAIRX / CSPELL / REPRX / LINKTOP 全部未编号未移植。
- **站位零剂量停放不重铸**：`planTaskTopology` X0-2（读码复核仍无 run loop 调用方，§2 哨兵第三十四次）；`settleSupervisedOutcome` A 属地 no-op（内部调用方 supervisor.ts:601/:711 均不带 assessment，无 assessment 即无门原形在位，§2 哨兵）。
- 未重开任何 X\* / S1-\* … S33-\* 条目。**单变量纪律**：本轮无任何计时新臂——重锚脚本与 R12-G 附录归档件**逐字节同一**（§1、附录），哨兵全部为静态 rg / 读码断言（不计时）。硬不变量零触碰：双 LCB + 双归因在位（切片侧证人：events.ts `isBehaviorDistribution` + flowchart-snapshot.ts `isOneHotDistribution` one-hot 校验原文在位）；Live = R0-equivalent（flowchart-run 的 learned 路由应用面未触碰，B 属地 model-router 未读改）；promotion proposal-first / 无 Outcome-supported 声称（Checkpoint F-PROD 仍开放，ADR-005）；无阈值变更；不改测试迁就重构；CAS / 凭据 / 数据面契约 / 公开签名不变（零 diff 天然满足）；fsync / 完整性再哈希在位（event-store 终态 fsync、checkpoint-store `handle.sync()`、pause-controller writeAtomic、episode-store 终态 fsync 全部原文在位）；fail-open 家族保持关闭（events.ts `validateEvent` 追加/重放双侧 fail-closed 原形）；S8-E-1 不去重 loadLearnedRouting；分析不变更在飞 run；tracking 无指挥权；H/score 不写 routing PASS/FAIL。无赢家 ⇒ 零生产改动、零新仿真文件入库。不开 PR。

## 1. CPU vs I/O 重锚（三批次官方运行 + 两次表征运行，nvm Node v22.22.2，seed 仅标注批次）

归档基准从 `docs/reports/sota-opt/round-12/R12-G.md` 原样提取（```ts 围栏内 536 行 → `tmp-r34g-bench.ts`，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`，与 R13-G…R33-G 附录 A 记录值**逐字节一致**；无赢家按纪律不入库，临时文件核后已删除）。官方三批 seed = 0xa34091 / 0xa34092 / 0xa34093；表征两次 seed = 0xa34094 / 0xa34095（仅用于 B3 高点归因，不计官方）。全部在 `4aa7f2b` 工作树完成（两笔快进增量纯 docs，各 tip `src/` 逐字节相同 ⇒ 测量形态与提交形态位级同一，全部有效）。

| 探针 | B1 | B2 | B3 |
| --- | --- | --- | --- |
| validateFlowchart 16n join4 | 9.417 µs | 9.254 µs | 12.469 µs |
| → ×32 轮计算顶 | **0.301 ms/run** | **0.296 ms/run** | **0.399 ms/run** |
| DeterministicJudge.decide 8×8 | 0.144 µs | 0.149 µs | 0.165 µs |
| expandTaskTransition | 0.532 µs | 0.513 µs | 0.544 µs |
| compileChildrenToFlowchart 16 子 | 8.838 µs | 8.621 µs | 9.059 µs |
| validateEvent AGENT_EVENT | 0.566 µs | 0.558 µs | 0.573 µs |
| replayRun 300 events | 0.659 µs | 0.680 µs | 0.664 µs |
| validateCheckpoint N=48 含 restore | 255.9 µs | 252.1 µs | 259.6 µs |
| CheckpointStore.write 持久（N=48） | 503.8 µs | 601.5 µs | 610.8 µs |
| → ×66 写/run | 33.3 ms | 39.7 ms | 40.3 ms |
| EventStore.append（无 fsync） | 67.9 µs | 68.1 µs | 68.5 µs |
| → ×300 事件/run | 20.4 ms | 20.4 ms | 20.6 ms |
| EventStore.readAll ×96/run 中期均幅 | 33.1 ms | 32.8 ms | 33.5 ms |
| EpisodeStore.append COMPLETED（fsync） | 283.8 µs | 303.0 µs | 308.4 µs |
| settle-close 终态命令算术 | 0.470 ms | 0.492 ms | 0.499 ms |

计算顶 = **0.301 / 0.296 / 0.399 ms/run**；I/O 地板合成 = **86.8 / 92.9 / 94.4 ms/run**；支配比原始 **~237–314×**（B3 顶含宿主高点；以两次表征顶 0.292/0.293 校正 B3 则 **~288–322×**；R33-G ~299–338× / R32-G ~304–324× 同为两个数量级，支配结论不变）。矩阵摘要 `06cbcf92c098c8f0…` 官方 3 批 × 3 迭代 = **9 次逐位一致（第二十五轮同值；14 格配置态 × 命令类矩阵逐格 I/O 计数零变化，不铸摘要轴/ID）**；两次表征运行再加 **6 次同值**（不计官方）；start→COMPLETED 首跑冷墙钟 21.0 / 19.4 / 20.3 ms，事件尾 [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED] 不变。

**诚实注记**：(i) B3 validateFlowchart 单价 **12.469 µs** 为孤立宿主干扰窗单点（同批 8n join2 5.008 µs 仅轻度偏高，其余探针全部带内；两次同引擎同装具表征运行 9.127 / 9.164 µs → 顶 0.292 / 0.293 ms 未复现该高点）——判宿主代际方差，不作发现；计算顶按最保守实测 0.399 ms 执行亚噪声拒收门，距 ≥10 ms 落地线仍 25×，按表征顶 ~0.29 ms 则两个数量级，结论完全同向。(ii) B1/B2 计算顶 0.296–0.301 轻度低于 R33-G 对照带下沿 0.302（validateFlowchart 单价 9.25–9.42 vs 9.43–9.66），I/O 地板 86.8–94.4 亦整体低于 R33-G 带 90.4–104.4 的上半（检查点持久写 503.8–610.8 µs 处历史带低中段）——与 R34-B / R34-E 各自记录的「本波 VM 偏快」同向宿主刻度项（R34-D 记录了反向高侧偏置——本波各 VM 刻度各异，结构结论均不依赖绝对刻度）；冻结 0 行 + 摘要第二十五轮恒等 ⇒ 仪器/宿主现象非切片行为变化，不作发现、对照带不改。(iii) 引擎纪律：PATH 在任何计时前钉死 nvm v22.22.2，全部批次官方引擎完成，零 v22.14.0 误跑、零弃置批次。仪器事故一则：两次表征运行的首次存档经 `tee | head` 显示管道被截断（显示管道副作用，非引擎/装具问题）——按纪律以纯重定向**重跑重archive**（seed 不变 0xa34094/95，全量存档），截断存档作废不引用；官方三批存档自始完整未受影响。(iv) 门禁事故一则：首次 `pnpm gate` 的 lint 段因仓库根尚未删除的临时基准文件 `tmp-r34g-bench.ts`（未跟踪仪器文件）报 23 错误——删除该文件后 gate 全绿；该次失败为仪器残留非仓库问题，不计入两次官方 gate。

## 2. G 档案在案零剂量停放 / 重开条件哨兵复核（全部不计时；方法应用，不铸面）

R32-A 首创的哨兵审计方法（plan-ban 重铸为面）此处按令仅作卫生性应用于 **G 自档案**的在案条目。全部哨兵为静态 rg / 读码断言，零计时臂：

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| X0-2（`planTaskTopology`） | run loop 或任何生产文件获得调用方 | `rg "planTaskTopology" src/` 除 supervisor.ts:64 定义自身外**空匹配**（第三十四次复核）⇒ **未触发** |
| `settleSupervisedOutcome` A 属地 no-op | 生产侧带 assessment 调用出现 | 调用方仅 supervisor.ts:601/:711（均不带 assessment）；函数原形 assessment 缺席即返回 ⇒ **未触发** |
| R30-G M1 格站位前提 | `startParentRun` 获得 CLI 调用方或 resume 门 | `rg "startParentRun" src/` 除 coordinator.ts:224 定义外**空匹配** ⇒ **未触发** |
| R29-G M2 格站位前提 | `startSupervisedRun` 获得 CLI 调用方 | CLI 仅动态 import `resumeSupervisedRun`（main.ts:1021，与历轮记录一致）；`startSupervisedRun` 仍无 CLI 调用方 ⇒ **未触发** |
| 摘要恒等（不铸 ID） | 14 格矩阵任一格 I/O 计数或摘要变化 | 官方三批 9 次逐位同值 `06cbcf92c098c8f0…`（第二十五轮；表征 +6 次同值）⇒ **未触发** |
| R31-B cascade 停放门（G 侧宿主） | child-coordinator cascade 臂获得新证据面 | 冻结 0 行 + `maybeCascadeRetry` 原形（:382 调用 / :458 定义，与 R33-G 记录同址）读码复核 ⇒ **未触发**（routing 属地维持） |
| R31-E 编解码坐标（G 侧四位点） | G 存储腿新增编解码剂量 | 显式 utf8 位点恰 4 个与 R32-G / R33-G 记录逐一同址，零剂量 ⇒ **未触发** |
| BINDX / MODEX 计数哨兵 | 切片 `Math.*` token 数或 chmod/umask 位点变化 | `Math.*` 恰 6 token 逐一同址；chmod/umask **0** 匹配（rg exit 1）⇒ **未触发** |
| 签名/调用图类重开条件全体 | 要求 `src/` 变更 | 最强冻结 `89f0455..HEAD -- src/` **0 行**直接判空 ⇒ **全部未触发** |

## 3. 本轮换名检查与四十二文件检查表（第三十四遍枚举残余）

### 3.0 换名检查（一项新站点级记录拒列；其余全部为已点名站点复命中，零候选）

从零重推（假装排除表不存在重读 8960 行）枚举出的候选角度经对表逐一查证：

1. **新记录（拒列）：`domain/flowchart.ts` `validateJoin` 逐 required-ref `edges.some` 线扫**（flowchart.ts:334 `edges.some((edge) => edge.from === required && edge.to === nodeId)`——每个 join 引用对全边表线扫，validateFlowchart 内形为 O(refs·E)；理论上可预构邻接索引消除）：等价成立，但三重拒绝——(i) **计算顶探针本体整体计价**：该扫描住在 validateFlowchart 内，而 validateFlowchart 恰是 G 计算顶探针本体（16n/join4 全函数 9.13–9.66 µs 含此扫描，×32 轮 = 0.29–0.31 ms/run 顶）——任何内形重构收益被顶本身封死，距 ≥10 ms 落地线两个数量级；(ii) **一次性 µs 拒收类**：真实流程每 flowchart 构造/校验一次，16 节点量级 E ≤ 数十；(iii) **校验平面家族**：VALCLASS 类既有裁决 + S3-G-5 谱系（校验平面 ns–µs 级重构历轮已拒）。µs/ns 派单拒收类直接命中，拒列，不铸 ID。
2. **复命中维持：`flowchart-supervisor.ts` `leaseReadyNodes` 逐节点 `computeStatus()` O(N) 重算**（每迭代全节点扫描，最坏 O(N²)）= R28-G 决策门格 + R33-G §3.2 已点名站点（计算顶内亚噪声，真实 N ≤ 16）。复命中即止。
3. **复命中维持：`supervisor/ledger.ts` `advanceLedgerRound` progress 数组 spread 全量记账**（O(rounds²) 累积形）= R33-G §3.2 已点名站点（计算顶内亚噪声；maxRounds=32 封顶 + facts 个位数规模）。复命中即止。
4. **本体复认（一行即止）：`failurePathCompletedGraph` 共享 `visiting` true-路径保留** = **S2-G-2 本体**（R2-G 链式恢复 b→a→C 反例在案；R33-G §3.0-1 已录第三十三遍独立复推导直证）——第三十四遍新鲜眼再次注意到同一性质，复认即止，不重演复推导叙事、不作新记录。
5. **复命中维持：`scheduler.ts` `planRound` 死形参 `_leaseDurationMs`** = R29-G 落选候选清单本体（公开导出签名硬禁 + ns 级）。不重提。

改名裁决（rename-check against baseline）：`git diff --find-renames --name-status 4efee23..HEAD -- src/run src/supervisor src/graph src/domain` **空输出**——切片自生产基线零改名、零增删文件，无改名归属需裁决。

### 3.1 `src/run/`（17 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| coordinator.ts | M0 缺省路径 300-append I/O 地板本体（排除表 G 族行）；M1 `startParentRun` = R30-G 格（站位哨兵未触发，§2）；`toModelRoutedPayload` 路由载荷映射 = 双归因见证不可动 | 无新候选 |
| supervisor.ts | M2 裁决格全维度 = R29-G；`planTaskTopology` X0-2 停放（§2）；`settleSupervisedOutcome` A 属地 no-op（§2）；`reconstructSupervisorState` 重放腿 = RUNLIM/CKMIX 既有属主 | 无新候选 |
| flowchart-run.ts | 每轮全量 readAll 放大 = S1-G-1/S2-J-11/S3-I-6/S6-G-1/S6-G-7 排除行；检查点节奏 = CKMIX；learned 路由应用 = Live=R0 不变量 + E/B 属地边界（S8-E-1 不去重，:712 消费腿零剂量）；pauseIfRequested = pause token 已锚 | 无新候选 |
| child-coordinator.ts | ConcurrencyGate FIFO 微结构 = 计算顶内亚噪声（R31-G 门）；重试/超时梯 = M1/M2 格已定价；cascade 升级臂 = R31-B 停放（§2 哨兵） | 无新候选 |
| event-store.ts | 终态 fsync = 硬不变量禁去；追加/读双侧 `validateEvent` = fail-closed 读校验契约行 | 无新候选 |
| checkpoint-store.ts | 临时文件 + rename + sync 原子写 = 持久契约；记录组成 = CKMIX；utf8 位点 = R31-E 坐标（§2） | 无新候选 |
| events.ts | EVENT_TYPES 闭表 + validateEvent = 校验平面既有属主；`routingContextFields` / `isBehaviorDistribution` = 双归因 one-hot 见证 | 无新候选 |
| replay.ts | replayRun 0.66–0.68 µs/300 事件（亚噪声）；materializeCheckpoint / validateCheckpoint = CKMIX + S8-G-2 写侧校验 | 无新候选 |
| child-grounding.ts | 包预算/`uniqueArtifacts` 去重一次性 µs 级；context packet 编译 = context 姊妹切片属地 | 无新候选 |
| child-prompt.ts | formatChildPrompt 串接已定价（R30-G [A]）；串形 = NAMESHAPE 禁 | 无新候选 |
| episode-bind.ts | settle-close 终态算术 0.470–0.499 ms 本轮带内已锚（含 `events.some` 三次 O(n) 终态扫描——聚合已定价，融合收益亚噪声）；closure 裁决 = episode 姊妹属地；`hash32` 集中化 = 既有落地 | 无新候选 |
| episode-store.ts | 队列化追加 + 终态 fsync = 硬不变量；readAll 校验 = jsonl 读契约（E 属地坐标） | 无新候选 |
| flowchart-executor.ts | 终态结果投影 = TERMCLASS 禁；薄执行器臂 = R31-G 门（真实支配成本是 executor 秒–分级真跑） | 无新候选 |
| injection.ts | 注入指令种类/校验 = R25-G 面全维度；`unexpectedKeys` 每调 Set 构造 + `parseFactValue` try/JSON.parse = 校验平面亚噪声（once-per-CLI-inject；REPRX 计数即止） | 无新候选 |
| inspection.ts | once-per-run CLI 分析腿 = 拒收类（一次性 CLI 噪声）；findChild 回退线扫（含 Array.from 物化）亚噪声 | 无新候选 |
| pause-controller.ts | writeAtomic fsync = 硬不变量；pretty-print 序列化形态 = pause.json 数据面契约；token 20.6–23.3 / 55.2–57.3 µs 带内已锚 | 无新候选 |
| scheduler.ts | LeaseRegistry / planRound / applyTaskOutcome = R29-G 格 + SCHEDWIN 禁；`_leaseDurationMs` 死形参 = R29-G 落选候选本体（§3.0-5）；`expired()` 每 lease Date.parse = 个位数 lease 规模亚噪声；expandTaskTransition 0.513–0.544 µs 亚噪声 | 无新候选 |

### 3.2 `src/supervisor/`（4 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| flowchart-supervisor.ts | `propagate()` 不动点 + edgeStatus/joinStatus = join-policy 面（R27-G）；leaseReadyNodes 逐节点 computeStatus 重算 = R28-G 格复命中（§3.0-2）；`failurePathCompletedGraph` 共享 visiting = **S2-G-2 本体**（§3.0-4 复认）；structuredClone 防御拷贝面 = R31-G 门；waiter 不变式 = 契约 | 无新候选 |
| flowchart-snapshot.ts | fail-closed 快照结构校验 = CKMIX + S8-G-2 属主（本轮 validateCheckpoint 252–260 µs 含其全量）；`isOneHotDistribution` 每决策 Set 构造 = 双归因见证内亚噪声；`snapshotValidationRouter` throw-only 桩零剂量 | 无新候选 |
| flowchart.ts | 纯再导出垫片，零运行时；模块形 = NAMESHAPE 禁 | 无新候选 |
| ledger.ts | advanceLedgerRound 数组 spread 记账 + 双段 isDuplicateFact 线扫 = 计算顶内亚噪声复命中（§3.0-3；facts 个位数规模） | 无新候选 |

### 3.3 `src/graph/`（4 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| validate.ts | Kahn 拓扑一次性构造（16 节点 µs 级，queue.shift 微形态含于内）；确定序 = ORDX 禁 | 无新候选 |
| readiness.ts | computeReadyTasks 线扫 = R29-G 格；与 planRound 的近重复形 = 调度格属地（去重是重构非优化，零真尺度收益） | 无新候选 |
| judge.ts | DeterministicJudge 0.144–0.165 µs（亚噪声地板级）；`evidenceIds.filter(includes)` 小集合二次形 = 个位数证据规模噪声 | 无新候选 |
| compile-children.ts | compileChildrenToFlowchart 8.6–9.1 µs 一次性已锚；`--children` 编译面历轮已裁 | 无新候选 |

### 3.4 `src/domain/`（17 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| flowchart.ts | validateFlowchart 9.13–12.47 µs ×32 = 计算顶本体（B3 高点宿主归因 §1(i)）；`validateJoin` edges.some 内形 = §3.0-1 本轮换名记录（拒列）；条件/join 校验数组字面量 includes = 校验平面既有属主；approval plan/reply 三段校验 = 权威面契约 | 无新候选 |
| state.ts | expandTaskTransition BFS 已定价亚噪声（全表预计算变体 = TERMCLASS 禁 + 亚噪声双拒）；转移表 = TERMCLASS 禁 | 无新候选 |
| ids.ts | 前缀 + 正则双查 + undefined 防御支 = 校验平面（VALCLASS 类 + S3-G-5 + R26-D fail-closed 族，R33-G §3.0-3 既裁）；branded 类型零运行时 | 无新候选 |
| timestamp.ts | ISO_PATTERN + Date.parse 双查 = 校验平面既有属主 | 无新候选 |
| hash.ts | hash32 集中化 = 既有落地本体（BINDX 计数即止，§0） | 无新候选 |
| task.ts / run.ts / limits.ts / project.ts / evidence.ts / episode.ts / contract.ts | 校验器族 = 校验平面既有排除行；错误串构造仅失败路径 | 无新候选 |
| status.ts / roles.ts / record.ts / errors.ts / index.ts | 闭枚举表 / type-only / 再导出 = 零剂量或亚噪声 | 无新候选 |

## 4. 候选总表

**空。** 本轮零候选：哨兵复核（§2）按定义不产出候选（不计时、不剂量）；换名检查一项新记录三重拒列、其余全部命中既有属主（§3.0）——无一是可立项新站点；四十二文件枚举（§3.1–3.4）全部命中既有 ID 本体、姊妹归档面、plan-ban 轴名或计算顶内亚噪声。本轮无任何计时新臂 ⇒ 无可定价候选面。全局支配结构不变：计算面全体封顶 0.296–0.399 ms/run（表征顶 ~0.29 ms），距 ≥10 ms 落地线两个数量级（最保守 25×）；I/O 面三腿 20.4–40.3 ms 全部契约或排除表保护。

## 5. CHECKS

- 分支恰 `cursor/r34-g-runtime-thirty-fourth-pass-83a1`，基线 `4aa7f2b` → 快进 rebase `b92b1fc` → `7f77080`（两笔纯 docs，各核对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 0 行）。
- 冻结核对：`git diff 4efee23 -- <42 文件>` 派单 tip / 两次快进 rebase 后 / 提交前均 **0 行**；`--find-renames --name-status` 同区间空输出（零改名）；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**。
- 切片清单：恰 42 文件（17+4+4+17），`wc -l` 合计 8960 行（与 R32-G / R33-G 记录一致），全部实读。
- 重锚脚本与 R12-G 附录归档件 sha256 逐字符同一（`9d46a829…b9943`，536 行）；官方三批 + 表征两批全部 nvm v22.22.2 / V8 12.4.254.21-node.39；零 v22.14.0 误跑零弃置（PATH 计时前钉死）。
- 摘要恒等：`06cbcf92c098c8f0…` 官方 9 次逐位一致（第二十五轮同值，不铸 ID；表征 +6 次同值不计官方）；事件尾不变。
- 哨兵普查（第三十四次）：X0-2 / A 属地 no-op / M1 / M2 站位、cascade 停放、编解码坐标 4 位点、MODEX 零位点、BINDX 6 token 逐一同址——全部未触发（§2）。
- 换名检查：一项新站点级记录三重拒列（validateJoin edges.some 内形——计算顶本体 + 一次性 µs + 校验平面族，§3.0-1）；复命中四项维持既有属主（R28-G 格 / R33-G 已点名 ×2 / S2-G-2 本体 / R29-G 落选清单）——零新 ID、零重开。
- 姊妹划界：R34-A/B/C/D（派单已录）+ R34-E / R34-F（两笔快进带入）六轴全为各属地物理对象，G 侧共享坐标仅 flowchart-run.ts learned-routing 消费腿（S8-E-1 + Live=R0，本清单原有，零剂量）——零新轴可移植（§0）。
- 聚焦测试：run/supervisor/graph/domain + integration = **386 tests / 385 pass / 0 fail / 1 skipped**（`4aa7f2b` / `b92b1fc` / `7f77080` 树各一次，v22.22.2，与 R31-G / R32-G / R33-G 逐字同计数）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿三次**（三 tip 各一次）——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A / R32-G / R33-G / R34-A…R34-F 逐字同计数）；退出码 0。（另一次 lint 失败为未删仪器临时文件所致，删后即绿，不计官方，§1 注记 iv。）
- 仓库改动恰本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；Live=R0-equivalent 在位；proposal-first 在位；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不变；fsync / 完整性族在位；fail-open 家族保持关闭；S8-E-1 不去重维持；分析未变更任何在飞 run；不开 PR。

## 6. 重开条件

本轮无新定价轴，不加行。X0-2、A 属地 no-op、R30-G/R29-G 站位前提、R31-B cascade 停放、R31-E 编解码坐标、CKMIX/S8-G-2、S2-G-2 等既有重开条件全额维持原文；本轮 §2 哨兵直证全部未触发。B3 计算顶高点 0.399（§1 注记 i）不构成重开触发——表征两运行未复现、冻结 0 行、摘要第二十五轮恒等 ⇒ 宿主干扰窗单点。

## 7. 新增排除 ID

**无。** 空枚举轮不铸 S34-G-\* ID；不取绰号、不作轴名、不加 EXCLUSIONS 行。换名检查一项新站点级记录见 §3.0-1（拒列），归档备后续轮次引用，无需并入 EXCLUSIONS.md。

## 附录 A：重锚基准出处与关键输出

脚本与 R12-G 附录 A 归档件**逐字节相同**：自 `docs/reports/sota-opt/round-12/R12-G.md`（```ts 围栏内 536 行）原样提取为仓库根 `tmp-r34g-bench.ts` 运行，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`（与 R13-G…R33-G 附录 A 记录值一致，逐字节可复现；无赢家按纪律不入库，临时文件核后已删除——删除时机见 §1 注记 iv）。运行方式：`node node_modules/tsx/dist/cli.mjs tmp-r34g-bench.ts <seed>`（nvm Node v22.22.2），官方三批 seed = 0xa34091/92/93，表征两批 seed = 0xa34094/95（seed 仅标注批次，全部数据确定性构造）。关键输出：

```
--- Node v22.22.2 / V8 12.4.254.21-node.39 (canonical) ---
validateFlowchart 16n join4: 9.417 / 9.254 / 12.469 us -> x32 = 0.296~0.399 ms/run
  (表征 0xa34094/95: 9.127 / 9.164 us -> 0.292 / 0.293 ms/run；B3 高点未复现)
validateFlowchart 8n join2: 4.761 / 4.637 / 5.008 us
DeterministicJudge.decide 8x8: 0.144 / 0.149 / 0.165 us
expandTaskTransition PENDING->FAILED: 0.532 / 0.513 / 0.544 us
compileChildrenToFlowchart 16 children: 8.838 / 8.621 / 9.059 us
validateEvent AGENT_EVENT: 0.566 / 0.558 / 0.573 us
replayRun 300 events: 0.659 / 0.680 / 0.664 us
validateCheckpoint full (N=48, incl. restore): 255.9 / 252.1 / 259.6 us
CheckpointStore.write durable (N=48): 503.8 / 601.5 / 610.8 us -> x66 = 33.3 / 39.7 / 40.3 ms/run
EventStore.append AGENT_EVENT (no fsync): 67.9 / 68.1 / 68.5 us -> x300 = 20.4 / 20.4 / 20.6 ms/run
EventStore.readAll 300/100 events: 496.5~503.4 / 187.6~195.2 us -> x96 = 33.1 / 32.8 / 33.5 ms/run
pause token() ENOENT / present: 20.6~23.3 / 55.2~57.3 us
loadLearnedRouting (no snapshot): 23.1 / 22.9 / 23.3 us
EpisodeStore.readAll (2 snapshots): 56.4 / 57.0 / 59.3 us
EpisodeStore.append COMPLETED (fsync): 283.8 / 303.0 / 308.4 us
EpisodeEventStore.append (no fsync): 61.7 / 64.0 / 63.1 us
settle-close arithmetic: 0.470 / 0.492 / 0.499 ms per terminal command
matrix digest (3 proc x 3 iter): 06cbcf92c098c8f0... 九次逐位一致（14 格；与 R10-G..R33-G 同值，第二十五轮；表征 +6 次同值）
start->COMPLETED wall-clock: 21.0 / 19.4 / 20.3 ms; returned events tail = [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED]
```

（引擎与存档纪律注记：PATH 在任何计时前钉死 nvm v22.22.2，零 v22.14.0 弃置批次；两次表征运行首次存档经 tee|head 显示管道截断——重跑重archive 后全量在案，截断件作废，官方三批存档自始完整。）

---

**MORE_OPTIMA=no** —— 第三十四遍复查空枚举收口：计算顶 vs I/O 地板支配比原始 ~237–314×（表征校正 ~288–322×）两个数量级不变；14 格矩阵摘要第二十五轮同值；G 档案在案停放/重开哨兵全部未触发；换名检查一项新记录三重拒列、其余全部命中既有属主；42 文件逐条消除角度全部命中既有属主。无达门槛新更优解。

MORE_OPTIMA=no
BRANCH=cursor/r34-g-runtime-thirty-fourth-pass-83a1
