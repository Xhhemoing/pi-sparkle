MODEL_SLUG=claude-fable-5-thinking-xhigh

# R34-E：learning 切片 Round 34 三十四搜报告

**战役:** 全库持久 SOTA 优化 Round 34 / R34-E（十区之一，R1-E…R33-E 的第三十四遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `4aa7f2b`（派单 tip，R34-B/C/D 摄取——三者均诚实空枚举——+ 本 R34-E/F/G 波派单）。运行全程独占分支**零快进**（分支创建时、锚点测量与门禁完成后两次 `git fetch origin cursor/sota-persistent-opt-83a1` 均确认 tip 仍为 `4aa7f2b`），全部测量、聚焦套件与 `pnpm gate` 在 `4aa7f2b` 工作树完成，无需 rebase。R34-F / R34-G 在飞，按令未等待、未触碰姊妹分支。
**分支:** `cursor/r34-e-learning-thirty-fourth-pass-83a1`
**切片:** `src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop` 283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 / `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 / `task-success` 69 = **1770 行**，`wc -l` 与 R14-E…R33-E 逐一相同），本轮第三十四遍全量实际重读。
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R32-E/R33-E 正批及 R34-A/B/C/D 引擎线位级相同；`/exec-daemon` PATH 暴露的系统 Node v22.14.0 已显式绕开——开工即按 R33-E 在案陷阱 PATH 前置 `~/.nvm/versions/node/v22.22.2/bin`，`which node` / `node --version` / `process.versions.v8` 三复核 v22.22.2 / 12.4.254.21-node.39，官方六运行逐 run 复核引擎；v22.14.0 上**零官方批次、零弃置批次**。tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成，锁文件未改）。

## 结论

**第三十四遍诚实枚举为空：不铸任何 S34-E-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，切片生产代码零改动。** 对 10 个源文件逐行第三十四遍扫描，对照完整排除表（X0-\*…X4-\*、S1-E-1..8 … S9-E-1..3 全系、S13-B-1、R10-E…R33-E 全部无号关闭探针与轴，含 R30-E 悬挂点拓扑、R31-E I/O 文本编解码拓扑、R31-C **BINDX**、R31-J **MODEX**）与 round-34 PLAN 禁榜（含 R34-A/B/C/D 空枚举折入）后，每一个执行站点都能指到既有 ID、无号探针、已点名裁决站点或 plan-ban 轴——**零未点名站点**；本轮从零重推的候选角度中恰 **1 项**构成新站点级换名记录（`loadLearnedRouting` L123 错误探针正则字面量每调用求值——三重拒列，恰为 R34-D §0 记录 3 的 E 侧物理孪生，§2.2），其余全部为已点名站点复命中（`isCompleteRoute` 逐字段 typeof 收窄 = R33-E §2.2 复命中 1 维持；`unique()`/`mode()` Set 化 = R33-E §2.2 复命中 2 维持；stableProjectKey 记忆化 / outcomeKindFromResult 死形参 = R32-E §2.2 两项拒列逐字维持）。生产调用方普查第三十四次确认：`patterns` / `signatures` / `attribution` 切片外 `src/` 导入方为零（运行时可达面仅测试）；`diagnostics` 唯一生产消费方为切片内 `auto-loop`（锚点 diagnose 段已计价）；**`loadLearnedRouting` 生产调用位点恰 3 处不变**（`cli/main.ts:708`、`run/flowchart-run.ts:712`、`track/loop.ts:88`——S8-E-1 硬不变量零去重，§2.3）。

按派单在本 VM 重锚默认预算（未拷贝历轮数字；cmp25 夹具 = R25-E 种子/NOW 跨轮可比面；装具为 R32-E 附录 A 归档件的逐行衍生——先复核归档正文指纹逐字符相同再施轮次置换 12 行，指纹与增量清单见附录 A）：

- **SLICE-CPU cmp25 总量：官方六读数 14.8–15.1 µs/run**（15.0 / 15.0 / 15.1 / 14.8 / 14.8 / 14.8，全部 v22.22.2）vs 默认带 14.7–15.2：**6/6 带内、零越沿**（比 R33-E 的两采样 15.3 擦沿更干净）；vs 派单默认对照 R33-E 读数带 15.1–15.3：仅 run 3 的 15.1 触带，其余五读数低于其下缘 0.1–0.3 µs——本 VM 会话轻度偏快（与 R34-B「主机偏快」同向宿主刻度项，冻结 0 行下非切片行为变化），**默认带沿用 14.7–15.2 µs/run 不变**。即使整段清零仍 **660–676× 低于 10 ms 落地线**。
- 分段 collect 11.3–11.6 / outcomes 1.3–1.4 / bandit 0.8 vs R25-E 带 11.3–11.9 / 1.3–1.5 / 0.8 全数在带；diagnose 1.29–1.38 vs 带 1.33–1.46：3/6 带内、三采样（1.31/1.32/1.29）低侧擦沿 0.01–0.04（与主锚偏快同向，判宿主快侧个体，价签不改带——R33-E 高侧 1.52 同位对称先例）。
- **夹具 VERDICT 行 6/6 逐位一致且与 R25-E…R33-E 夹具行逐字相同**（`E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3`）；live 面 `nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2` 三十四轮不变。
- 配置态锚点（6 采样）：apply(avoid=10, M=10) **265–289 ns/task**（R13-E…R30-E 带 257–312 内 **6/6**——S13-B-1 维持 loser ID 的带内直证）、空策略 **112–122 ns/task**（110–140 带内 **6/6**）、load 现实 registry **79.7–83.5 µs**（74.3–134.5 带内 **6/6**）、load fresh(ENOENT) **26.7–33.3 µs**（历史并集 25.2–36.8 内 **6/6**）。四格 24/24 官方采样全带内、零越沿（严于 R33-E 的 22/24）。

回归与门禁（§4）：聚焦切片套件（unit/learning 7 文件 + acceptance/adaptive-loop）**48/48 全绿**（v22.22.2，`4aa7f2b` 树两次同计数）；`pnpm gate` 两跑均 **exit 0**（tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1，与 R14-E…R33-E / R34-A/B/C/D 逐字同计数）。

硬性不变量逐条复核（§0）：双 LCB / 双归因在位；live = R0 等价（R1 未接线）；proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；CAS / 凭证 / 数据面合同 / 公开签名零触碰；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭；**S8-E-1 零去重、S9-E-2 未触碰（两遍结构读码在位）、S13-B-1 维持 loser ID、S12-B-2 条件陆器 E 侧入口见证未触发**（`eval-routing.ts:353–354` 仍只传 `learned`；全文 `prior` rg 零匹配 exit 1）。仓库变更仅本报告一个文件。空枚举与 R10-E…R33-E、R34-A/B/C/D 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → PROGRESS.md → EXCLUSIONS.md（完整表 545 行，S1-\* … S13-B-1 + X 继承行全部实读）→ round-34/PLAN.md（E 段 + 具名/unnamed 全 ban 榜）→ R33-E.md 全文（默认对照带 15.1–15.3 与零新站点级换名记录先例）→ R34-A.md / R34-B.md / R34-C.md / R34-D.md 全文（四姊妹轴折入非移植清单，§3）→ 10 个切片源文件全量重读，之后才动手测量。
- 基线漂移检查：分支创建点 = 派单 tip = `4aa7f2b`；运行全程零快进（两次 fetch 复核）。冻结主张三点核验均 **0 行**（10 文件 vs `adb20d7`：分支创建时、锚点测量与门禁完成后、提交前）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均 docs）；切片最后生产变更维持 `9bf3e1a`（`git log -1 -- src/learning/` 直证；`adb20d7` 为其后的 docs-only 摄取提交，冻结基线语义与 R9-E…R33-E 逐轮一致）。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：async suspension-point topology / per-run suspension-manifest（R30-E——本轮零悬挂计数、零 manifest 臂）/ **PWL**（`proposeRoutingFromOutcomes` 提案写路径读码即止）/ **RVL** / **RTL** / **CG** / **JB**（`applyCascadeRetry` spread 形态不重提）/ **NVG** / **REPRX**（切片 `JSON.parse` 站点恰 **3**：learned-routing.ts:55、bandit-store.ts:20、auto-loop.ts:239——rg 直证逐位点同 R33-E，全部既有定价面，计数即止不重铸）/ **PAIRX** / **ZREP** / **LINKTOP** / **BINDX**（切片 `Math.*` token 恰 **1 个**：attribution.ts:45 `Math.min`，rg 直证，住在零生产流量休眠模块内，计数即止零剂量零 patch）/ **MODEX**（切片零 chmod / 零 umask / 零八进制 mode 字面量，rg 空匹配 exit 1 声明即止）/ I/O text-codec topology（R31-E——切片 3 个 fs UTF-8 位点：auto-loop.ts:236、bandit-store.ts:14/:80，rg 直证原形在位，codec / transcode-byte 臂零重做）/ R32-A 重开条件哨兵审计方法（不重铸——凡要求 src/ 变更的重开条件由 0 行冻结判空；S6-E-1 事件规模条件与 S12-B-2 条件陆器由 §2.1/§2.3 读码顺带见证未触发）/ R32-B 闭合稳定性审计（不重走）/ **R33-A … R33-J 空枚举**（十切片方法卫生记录，不重演账本分类、不重提「我又查了一遍重开条件 / 门扉 / 未点名站点」）/ **R34-A 空枚举**（三项 A 站点级换名记录全为 tracking/gate-apply 物理对象，划界见 §3）/ **R34-B 空枚举**（零新记录；其 S13-B-1 哨兵与本轮 §1 apply 格带内直证互为佐证不互抄，划界见 §3）/ **R34-C 空枚举**（一项换名检查为 `src/routing/propensity.ts` C 区物理对象，零共享，划界见 §3）/ **R34-D 空枚举**（三项 D 站点级换名记录全为 `src/adaptation/` 物理对象；其记录 3 与本轮 §2.2 记录 1 为跨切片孪生站点——同族同向裁决、各自物理对象，划界见 §3）/ R31-A/B/D/F/G/H/I 停驻与审计记录（平面正交）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / RUNLIM / VALCLASS / AGEX / COTARG / PRICEX / DELINV / PROBX / SIDEC / CSPELL 全部未编号未移植。X\* / S1-\* … S33-\* 全部未重开。
- 硬不变量零触碰：双 LCB（beta + normal，`src/routing/posterior.ts` 属 C 区）与双归因（logit + prob-add，`src/experiments/attribution-report.ts` 属 F 区）均保留；live = R0 等价（R1 未接线）；sparse → approved R0 baseline；promotion proposal-first（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；不改测试迁就重构；CAS / 凭证 / 数据面合同 / 公开签名不动；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭；分析不改 in-flight run、tracking 无命令权、H/score 不写路由 PASS/FAIL（零 diff 天然满足）；**S8-E-1 不去重 loadLearnedRouting**（本轮仅 §2.3 重普查其 3 个调用位点与 §1 单价带）；**S9-E-2 负优化未触碰**（collectSignalsFromEvents 两遍结构读码在位：L139–157 pass-1 + L160–242 pass-2）；S13-B-1 维持（apply 格带内直证 + learned-routing.ts 冻结 0 行，与 R34-B §5 哨兵重取证互为佐证不互抄）。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。不开 PR。输家脚本不入库（本轮无输家道——除锚点装具外零计时新臂；装具为既档衍生未提交 `scripts/`，增量与指纹见附录 A）。

## 1. 冻结证明与 SLICE-CPU / 配置态重锚（cmp25，六官方运行）

```bash
git log --oneline -1 origin/cursor/sota-persistent-opt-83a1
# 4aa7f2b docs(sota-opt): ingest R34-B/C/D (empty enums) and dispatch R34-E/F/G
#（运行全程零快进，两次 fetch 复核 tip 不变）
git diff adb20d7 -- src/learning/ | wc -l        # 0
git diff 89f0455..HEAD -- src/ | wc -l           # 0
wc -l src/learning/*.ts                          # 1770 total（同 R14-E..R33-E）
git log --oneline -1 -- src/learning/            # 9bf3e1a（最后生产变更不变）
```

锚点装具 `/tmp/r34e-anchor.mts`（365 行）为 **R32-E 附录 A 归档件的逐行衍生**：从 round-32/R32-E.md 附录 A 提取归档正文（复核 365 行、sha256 `494f801c2ccb621dbf7abca6d3116561b98010be09cf2aa19ad3cf366d64caff` 与 R32-E/R33-E 归档值逐字符相同），增量仅轮次置换共 **12 行**（附录 A 逐行清单）：头注 R34 标注（L2–5/7/14）、mkdtemp 前缀 `r34e-cmp25-*`（L327/332）、projectRoot `/tmp/proj-r34e`（L329/333）、detector identity `r34e-anchor-cmp25`（L336/346）。**seeds `0xe25e01/02/08` 与 NOW = R25-E `2026-08-25T17:30:00.000Z` 逐字节未动**（L84/L271–273 提取后 grep 直证）⇒ cmp25 跨轮可比面维持，VERDICT 行必须逐字重现 R25-E…R33-E 夹具行（下表 6/6 达成）。官方批 **6 × v22.22.2** 独立进程（逐 run 复核引擎；v22.14.0 零批次零弃置）；每次 3/3 sanity 全过。代表性一次运行（run 1）：

```text
VERDICT fixtures: E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=11.5us outcomes=1.4us diagnose=1.37us bandit-build=0.8us | total in-slice CPU ~15.0us per run vs landing bar >=10000us (666x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=265ns/task | empty policy=114ns/task -> x10 tasks/run = 2.65us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=27.1us | realistic registry (baseline+1 candidate)=83.1us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

六官方运行原始区间（全量见附录 A）：

| 格 | 本轮六读数 | 历史带 | 判定 |
|---|---|---|---|
| SLICE-CPU 总量 | 15.0 / 15.0 / 15.1 / 14.8 / 14.8 / 14.8 µs | 默认带 14.7–15.2；派单对照 R33-E 15.1–15.3 | 默认带 **6/6 零越沿**；对照带仅 run 3 触带、五读数低 0.1–0.3（本 VM 偏快，注记 (i)）——**带沿用不变** |
| collect | 11.3–11.6 µs | R25-E 11.3–11.9 | **6/6 带内** |
| outcomes | 1.3–1.4 µs | R25-E 1.3–1.5 | **6/6 带内** |
| diagnose | 1.37 / 1.31 / 1.38 / 1.32 / 1.34 / 1.29 µs | R25-E 1.33–1.46 | 3/6 带内；三采样低侧擦沿 0.01–0.04（注记 (i) 同向）——价签，不改带 |
| bandit-build | 0.8 ×6 | 0.8 | 6/6 |
| apply(avoid=10, M=10) | 265 / 280 / 276 / 272 / 289 / 267 ns/task | 257–312 | **6/6 带内** |
| apply(空策略) | 114 / 113 / 112 / 115 / 122 / 118 ns/task | 110–140 | **6/6 带内** |
| load fresh(ENOENT) | 27.1 / 26.7 / 29.2 / 30.4 / 32.0 / 33.3 µs | 并集 25.2–36.8 | **6/6 带内** |
| load real registry | 83.1 / 79.7 / 83.5 / 80.6 / 81.2 / 80.6 µs | 74.3–134.5 | **6/6 带内** |

SLICE-CPU 重锚判定：**默认带沿用 14.7–15.2 µs/run 不变**，本轮读数 14.8–15.1 全数带内零越沿；即使整段清零仍 660–676× 低于 ≥10 ms 落地线——派单 µs/ns 拒收类直接适用于切片全部 CPU 面。once-per-run 生产 I/O 位点（load fresh / load real）µs 域、once-per-run CLI 噪声拒收类同样适用。

**诚实注记**：(i) 本 VM 会话呈轻度快侧宿主刻度：主锚五读数低于 R33-E 对照带下缘 0.1–0.3 µs、diagnose 三采样低侧擦沿 0.01–0.04 µs，两者同向；与 R34-B「主机偏快，freeze 0 行」同型宿主现象（R34-D 记录了反向的高侧偏置——本波三 VM 刻度各异，结构结论均不依赖绝对刻度）。冻结 0 行 + 引擎位级同一 + 装具血统指纹逐字符相同 ⇒ 仪器/宿主现象非切片行为变化，不作发现、不改带。(ii) 本轮六官方运行一次性完成，零校准批、零弃置批、零中断存档事故（R33-E 注记 (iv) 的存档纪律本轮无触发场景）；无补充表征批。

## 2. 第三十四遍枚举（诚实结果：空；一项新站点级换名记录，拒列）

### 2.1 逐文件重读与裁决（零未点名站点）

| 文件（行数） | 本轮重读检查（全部命中既有 ID / 无号探针 / 已点名站点 / plan-ban 轴） | 结论 |
|---|---|---|
| `auto-loop.ts`（283） | persistSignals evidenceIds.filter 双算 = S1-E-4（I/O 支配）；persistSignals∥updateProjectBandit 编排重叠 = S4-E-2（双故障非确定）；ingestSubagentDirectory 顺序读 = S1-E-5；optimizedPolicy filter+map 融合 = S2-E-6（n=10 实测更慢）；runAutoAdaptFromEvents 前置扫描 = S3-E-2 / S5-E-1；无变更跳存 = S2-E-1（X0-3 保存时机）；切片内惰性 import = S5-E-5；三段 signals spread 拼接 = 分配级噪声（collect 锚整体计价）；异步悬挂点拓扑 = R30-E 面（plan-ban）；:236 utf8 decode / :239 JSON.parse = R31-E codec 面 + REPRX 计数即止 | 无新候选 |
| `learned-routing.ts`（216） | loadLearnedRouting 去重 = **S8-E-1 硬不变量**（调用位点普查 §2.3）；prefer.find 跨界去重 = S8-E-3；applyLearnedRouting 单次成本 = S13-B-1 维持 loser ID（265–289 ns 采样带内直证，§1）；L123 错误探针正则字面量每调用求值 = **本轮换名记录 1**（§2.2，拒列）；parseLearnedRoutingPolicy 错误信息正则字面量提升 = R33-E 既点名（冷路径 + S3-E-5 族亚噪声）；stableProjectKey 记忆化 = R32-E §2.2 记录 1 拒列维持（X1-1 族 + once-per-run µs 拒收，不重铸）；identity/print/parse 面 = REPRX 划界 | 无新候选 |
| `from-episode.ts`（298） | proposeRoutingFromRoutedEvents 三遍扫描融合 = S2-E-2；提案写路径 = **PWL**（plan-ban，读码即止）；applyCascadeRetry spread / 死 behaviorDistribution 重算 = S9-E-1（**JB** 邻域不重提）；familyFromRouted 小表 includes = S7-E-3 小集合教训族；outcomeKindFromResult 死形参 = R32-E §2.2 记录 2 拒列维持（纯翻搅零收益，不重铸）；**isCompleteRoute 逐字段 typeof 收窄消除 = R33-E §2.2 复命中 1 维持**（谓词序 R10-E…R16-E 行 + R24-E 拦截定价 + S9-D-3 谱系防御纵深，拒列即止）；policyFromOutcomes seen Set 原形健全（个位数 arms 线性域） | 无新候选 |
| `signals.ts`（439） | 两遍结构 = S1-E-1 本体 + **S9-E-2 负优化封死**（未以任何形态触碰，两遍结构读码在位）；JUDGE_DECISION 双 Map.get = S1-E-2；verdict 双三元 = S7-E-5；extractAssistant 折叠 = S1-E-3；truncate 恒等快路径 / 流式早退 = S7-E-2 / S2-E-5；scoreUserAnswer 双正则 / trim = S6-E-2 / S7-E-1；baseSignal 条件 spread = S8-E-2（PIC 敏感）；形状单态化 = S4-E-3；ctx 提升 = S5-E-2；`/unknown agent/i` 提升 = S3-E-5；request.agent 探针提升 = S7-E-4；探针复用 = S6-E-5；TASK_RESULT binding 双拷贝 = S2-E-7；五 Map 合一 = S6-E-1（重开条件未触发：事件规模未增 3 个量级且仍被 readAll 支配）；空事件快路径 = S4-E-1 | 无新候选 |
| `bandit-store.ts`（83） | 循环内 arms.includes = 个位数 arms 小表噪声（S2-E-3 邻域）；弃置 createBanditState = S2-E-3 本体；per-arm pulls/rewardSum 重建 = S3-E-4 邻域小表噪声；recordReward 不可变翻搅 = X4-2 readonly 契约（本体属 C 区 `src/routing/bandit.ts`，越切片）；mkdir 每写 = S6-E-3；pretty-print = S6-E-4；无变更跳写 = S2-E-4（write-always 契约）；arms/reward 双扫融合 = S3-E-4；:14/:80 utf8 / :20 JSON.parse = R31-E codec 面 + REPRX 计数即止 | 无新候选 |
| `task-success.ts`（69） | copyDefinedBinding 条件 spread = S5-E-3 族亚噪声；taskSuccessFromResult × scoreTaskResult 分派树合一 = S9-E-3（公开面扩张 / X1-2 复制）；V2 语义零触碰（RVL plan-ban） | 无新候选 |
| `attribution.ts`（92） | attributeToBoundary 排序换单遍 min = S1-E-7（test-only）；findNegativeControlMarker 线扫 = 同一 test-only 可达面；:45 `Math.min` = BINDX 坐标计数即止（切片唯一 Math token，rg 直证）；生产调用方第三十四次为零（§2.3） | 无新候选 |
| `diagnostics.ts`（78） | 组内多遍融合单遍 = S1-E-6（真实规模亚噪声且 10× 更慢）；分组键换嵌套 Map = S5-E-4（平局序发散）；`unique()`/`mode()` Set 化 = **R33-E §2.2 复命中 2 维持**（R12-E 行 + R23-E 机械价格：FeedbackKind 基数 ≤4 线扫更快，拒列即止）；唯一生产消费方 = 切片内 auto-loop，锚点 diagnose 段 1.29–1.38 µs 已计价（§1） | 无新候选 |
| `patterns.ts`（146） | detectRepeatedPatterns keys 预取 = S1-E-8（test-only）；相似度对键记忆化 = X2-6（已否决）；computeFeatureSim keySet 物化 = 零生产流量域（signatures 行同判）；O(n²) 聚类 = test-only 零生产调用方（§2.3） | 无新候选 |
| `signatures.ts`（66） | JSON.stringify 哈希入参为内存对象（codec 不在环，R31-E C1 见证既档）；消费方 signals / attribution / patterns 均 type-only 或 test-only 可达；keySet 数组物化 = 同一零生产流量域 | 无新候选 |

结构位置 × 消除机制满矩阵（死码 / 常量折叠 / CSE / 记忆化 / 数据结构 / 融合 / 早退 / 免分配 / 复杂度类 / 强度削减 / 惰性 / 平行路径 / 形状单态化）逐格过筛：非空格全部落在上表既有 ID、plan-ban 轴、R32-E/R33-E §2.2 既有记录或 §2.2 本轮一项新记录上。

### 2.2 换名拒绝检查：本轮恰一项新站点级记录（拒列，不铸 ID）

从零重推（假装排除表不存在重读 1770 行）枚举出的候选角度经对表逐一查证，恰 **1 项**未在任何在手 E 报告逐字点名过、构成新站点级换名记录（若与更早轮次某条未在手记录重合，则为复命中，结论不变）；其余全部为已点名站点复命中：

1. **新记录（拒列）：`loadLearnedRouting` L123 错误探针正则字面量每调用求值提升**（learned-routing.ts:123 `/no registry snapshot/.test(error.message)`——正则字面量在 catch 臂每次求值新铸 RegExp 对象；语义上无 /g 无 lastIndex 状态风险，可提升为模块常量）：等价成立，但三重拒绝——(i) **家族已裁**：X0-6（缓存 human-score 正则）邻域 + 错误消息契约面（X0-5 族——该探针匹配 `loadAdaptationRegistry` 的公开错误消息文本，耦合面在契约上）；(ii) **既有锚整体计价**：该 catch 臂仅在 fresh-root/无 registry 时执行，整腿已由 load fresh(ENOENT) 锚计价（26.7–33.3 µs once-per-run，§1 6/6 带内）——RegExp 字面量铸造 ~ns 级，为其万分位；(iii) **跨切片孪生已同向裁决**：R34-D §0 记录 3 对同一正则的 adaptation 侧物理孪生（promotion.ts:212 `loadAdaptationRegistryOrNew` 的同名探针）已以 X0-6 邻域 + 错误消息契约家族 + ns 级 once-per-command 拒列——本站点为其 E 侧独立物理对象，同族同向。µs/ns 派单拒收类 + once-per-run CLI 噪声拒收类双重命中，拒列，不铸 ID。
2. **复命中维持：`isCompleteRoute` 逐字段 typeof 收窄消除**（from-episode.ts:275）= R33-E §2.2 复命中 1 逐字维持（R10-E…R16-E 谓词序行 + R24-E 拦截定价 + fail-open 家族冻结 + S9-D-3 零成本防御纵深）。复命中即止，不新增记录。
3. **复命中维持：`unique()` / `mode()` Set 化 / 单 distinct 早退**（diagnostics.ts:56）= R33-E §2.2 复命中 2 逐字维持（R12-E 小集合行 + R23-E 机械价格 ~12 ns/信号全额、FeedbackKind 基数 ≤4 线扫免 Set 分配更快）。复命中即止。
4. **不重铸：R32-E §2.2 两项拒列记录**（stableProjectKey 记忆化 = X1-1 族 + once-per-run µs 拒收双重命中；outcomeKindFromResult 死形参删除 = 纯翻搅零收益）：裁决逐字维持，站点原形读码复核，不以任何新名重提。

### 2.3 生产调用方普查（第三十四次）

- 切片外 `src/` 对 learning 模块导入方恰 7 文件：`run/flowchart-run.ts`（learned-routing）、`cli/adapt.ts`（from-episode + auto-loop）、`cli/main.ts`（learned-routing + auto-loop）、`adaptation/eval-routing.ts`（learned-routing）、`track/primary-split.ts`（type-only，读码复核）、`track/loop.ts`（learned-routing + auto-loop）、`routing/assign.ts`（applyLearnedRouting）——rg 全库直证，与历轮记录逐文件一致，零新增。
- `patterns` / `signatures` / `attribution` / `diagnostics` 切片外 `src/` 导入方 = **0**（rg `learning/(patterns|signatures|attribution|diagnostics)` 空匹配）；运行时可达面仅 `test/acceptance/adaptive-loop.test.ts` 与 unit 套件（diagnostics 另有切片内 auto-loop 消费，已在锚点计价）。
- `loadLearnedRouting` 生产调用位点恰 3 处：`run/flowchart-run.ts:712`、`track/loop.ts:88`、`cli/main.ts:708`——S8-E-1 的 tracked/children 双载形态原样，零触碰。
- S12-B-2 条件陆器 E 侧入口读码直证：`adaptation/eval-routing.ts:353–354` 仍只传 `learned` 不传 `prior`（全文 `prior` rg 零匹配 exit 1，与 R34-B §2c/§5 重取证及 R34-D §3 哨兵一致）——未触发，属地在 B/D，声明即止。

### 2.4 枚举收口

第三十四遍逐文件重读**零未点名站点**；候选角度中一项新站点级换名记录三重拒列（§2.2 记录 1），其余全部为复命中。切片全部 CPU 面住在 14.8–15.1 µs/run 总量之内（清零仍 660–676× 低于落地线），全部 I/O 面为 once-per-run µs 域既档定价（load fresh / load real 6/6 带内直证）或 R31-E codec 面既档（不重铸）。**第三十四遍枚举为空——比强铸败者 ID 更诚实。**

## 3. 姊妹面划界与非移植清单（R34-A/B/C/D 四轴折入）

1. **R34-A（tracking 侧空枚举）**——三项站点级换名记录（child-tracking `skipped` GateApplyResult 每调用新铸享元 / from-child `completedChecks` PASSED 臂防御拷贝 / turn `readersInvoked` 常量初值享元，全部 S1-A-7 对象身份 / S7-B-5 享元 / S8-B-4 防御拷贝族拒列）+ 预算重锚 12.8–15.2 µs/gate ⇒ ~64–76 µs/run + 层序测量仪器诚实注记。E 侧划界：三站点全为 `src/tracking/` + `src/run/` A 区物理对象，与本切片零共享；其享元/身份家族裁决与本轮 §2.1 baseSignal/copyDefinedBinding 行的既有裁决谱系一致。无新轴可移植。
2. **R34-B（live 路由侧空枚举）**——零新站点级换名记录（两残余角度均为 R2-B 已点名站点复命中）；重建 harness 诚实声明 + 天花板复锚（M=2 10.54–11.12 / M=10 16.10–16.27 ms/eval，主机偏快）；S12-B-2 腿 (a) 与 S13-B-1 重开门显式重取证未触发。E 侧划界：其 S13-B-1 哨兵（learned-routing.ts vs `94ed3d9` 冻结 0 行）与本轮 §1 apply 格 6/6 带内直证**互为佐证不互抄**；其「主机偏快」宿主注记与本轮注记 (i) 同型同向。无新轴可移植。
3. **R34-C（离线路由侧空枚举）**——一项换名检查（propensity `isFabricatedPositiveSupport` `notOneHot` 被吸收析取消除，S1-C-8 同站点族严格弱化 + R27-C 零生产通量 + 意图护栏三重拒列）；中位复核 682.1–682.5；组合天花板 28.7–31.6 < 35；sink=7.309 八读全同。E 侧划界：其换名站点为 `src/routing/propensity.ts` C 区物理对象，本切片零 propensity 消费；其「被吸收析取 + 意图护栏」裁决谱系与本轮 §2.2 复命中 1 的 S9-D-3 族一致。无新轴可移植。
4. **R34-D（adaptation 侧空枚举）**——三项站点级换名记录（registry rollback `knownTargets.some` 死检查 / promotion `parsePromotionReview` 双层校验 / promotion `loadAdaptationRegistryOrNew` L212 正则探针提升，全部拒列）+ 元闭合 35 行独立重推无一松动 + 高侧主机偏置诚实注记。E 侧划界：三站点全为 `src/adaptation/` 物理对象；**其记录 3 与本轮 §2.2 记录 1 为跨切片孪生**（同一 `/no registry snapshot/` 探针的 adaptation 侧 vs learning 侧两个独立物理对象）——本轮独立枚举出 E 侧站点并以同族同向三重拒列收口，不互抄裁决文本；其 S12-B-2 / S13-B-1 哨兵与本轮 §2.3 见证同判未触发。无新轴可移植。
5. **R30-E 悬挂点拓扑 / R31-E codec 拓扑 / BINDX / MODEX / R32-A 哨兵方法 / R32-B 闭合稳定性方法 / R33-A…R33-J 空枚举 / R31-A/B/D/F/G/H/I 停驻与审计记录**——§0 逐项核对，全部零剂量零重做零重铸。

## 4. 测试与验证

```bash
# 本 VM: nvm Node v22.22.2（PATH 前置 ~/.nvm/versions/node/v22.22.2/bin
# 绕开 /exec-daemon 系统 Node v22.14.0）。
npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0
#（4aa7f2b 树两次同计数）

pnpm gate
# typecheck ✓ + lint ✓ + test ✓ + build ✓ （GATE_EXIT=0，两跑）
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（两跑逐字同计数；与 R14-E..R33-E / R34-A/B/C/D 逐一相同）
```

锚点装具官方 6 次运行全过（§1，3/3 sanity ×6；确定性 VERDICT 行 6/6 逐位一致且与 R25-E…R33-E 夹具行逐字相同）。本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交 `scripts/`，遵守 R7-F/R8-C/R10-E…R33-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 仅 `docs/reports/sota-opt/round-34/R34-E.md`，提交前复核）。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐站点有属主；一项新站点级换名记录三重拒列，其余候选角度全部为已点名站点复命中）。
- 预算面：SLICE-CPU 默认带 14.7–15.2 µs/run 沿用不变（本轮官方读数 14.8–15.1 6/6 带内零越沿；本 VM 轻度偏快为宿主刻度项），清零仍 660–676× 低于落地线；配置态四格 24/24 官方采样全带内零越沿——切片无任何达「数十–数百 ms 真尺度非噪声」门槛的剩余面。
- 判定：**MORE_OPTIMA=no**。

## 7. 新增排除 ID：无

- 不铸 S34-E-\*：第三十四遍枚举为空；不取 ALLCAPS 绰号；不写入 EXCLUSIONS.md 任何行（含对空枚举轴外号的一切再编号）。换名检查 1 项新站点级记录见 §2.2（记录 1），归档备后续轮次引用，无需并入 EXCLUSIONS.md。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态；S8-E-1 / S9-E-2 / S13-B-1 未重开；未重铸 R32-A 哨兵方法与 R32-B 闭合稳定性方法；未在 E 区重演 R33-A 账本分类；未重提 R32-E/R33-E §2.2 既有记录、R33-C/R34-C/R34-A/R34-D 各换名记录。

## 附录 A：锚点装具增量说明、指纹与原始输出（未入库；/tmp 留存核后删除）

- 装具：`/tmp/r34e-anchor.mts`，**365 行**，为 R32-E 附录 A 归档件的**逐行衍生**：先从 `round-32/R32-E.md` 附录 A 提取归档正文并复核指纹（365 行，sha256 `494f801c2ccb621dbf7abca6d3116561b98010be09cf2aa19ad3cf366d64caff`，与 R32-E/R33-E 归档值逐字符相同），再施加轮次置换共 **12 行**——头注 R34 标注（L2–5/7/14 注释行：轮次序数、谱系上界 R33-E、夹具行谱系上界）、mkdtemp 前缀 `r34e-cmp25-fresh-` / `r34e-cmp25-reg-`（L327/332）、projectRoot `/tmp/proj-r34e`（L329/333）、detector identity `r34e-anchor-cmp25`（L336/346）。**seeds `0xe25e01`（collect）/ `0xe25e02`（routed）/ `0xe25e08`（signals）与 NOW = R25-E `2026-08-25T17:30:00.000Z` 逐字节未动**（L84/L271–273 grep 直证）；import 路径与语义同归档件。装具全文不重复内联（= R32-E 附录 A 正文 + 上列 12 行置换，任何人可机械重建并核指纹）。
- 本轮装具 sha256：`1d114829efcc4067d8598a76b91e7ddbab970df7034ee49ef4998a8636b80885`
- 运行方式（仓库根，`pnpm install --frozen-lockfile` 后，PATH 前置 nvm bin）：

```bash
node --import tsx /tmp/r34e-anchor.mts   # 独立进程 ×6 = 官方 runs 1–6（每 run 前复核 node --version = v22.22.2）
```

六官方运行原始值：

```text
SLICE-CPU 总量:  15.0 / 15.0 / 15.1 / 14.8 / 14.8 / 14.8 us
collect:    11.5 / 11.6 / 11.6 / 11.4 / 11.3 / 11.4 us
outcomes:   1.4 / 1.4 / 1.4 / 1.3 / 1.4 / 1.3 us
diagnose:   1.37 / 1.31 / 1.38 / 1.32 / 1.34 / 1.29 us
bandit:     0.8 ×6
apply(non): 265 / 280 / 276 / 272 / 289 / 267 ns/task
apply(空):  114 / 113 / 112 / 115 / 122 / 118 ns/task
load fresh: 27.1 / 26.7 / 29.2 / 30.4 / 32.0 / 33.3 us
load real:  83.1 / 79.7 / 83.5 / 80.6 / 81.2 / 80.6 us
VERDICT 行: 6/6 逐位一致且与 R25-E..R33-E 夹具行逐字相同；
            sanity 3/3 ×6 全过
```

MORE_OPTIMA=no
BRANCH=cursor/r34-e-learning-thirty-fourth-pass-83a1
