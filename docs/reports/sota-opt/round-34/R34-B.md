MODEL_SLUG=claude-fable-5-thinking-xhigh

# R34-B：live 路由切片 Round 34 三十四搜报告

**战役:** 全库持久 SOTA 优化 Round 34 / R34-B（十区之一，R1-B…R33-B 的第三十四遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `44819a4`（派单 tip，R33-J 摄取 + Round 33 收口 + Round 34 开轮 + 本 A/B/C 波派单）→ 提交前 rebase 至 **`8634e7d`**（R34-A 摄取 + R34-D 派单——**纯 docs 快进**，`git diff 44819a4..8634e7d -- src/ package.json pnpm-lock.yaml` = **0 行**）。测量窗口内 exclusive 零快进（分支创建时、测量后两次 `git fetch origin cursor/sota-persistent-opt-83a1` 均确认 tip 仍为 `44819a4`），全部测量、聚焦套件与 `pnpm gate` 在 `44819a4` 的工作树上完成；提交前第三次 fetch 发现快进 `44819a4 → 8634e7d`，按令 rebase 至新 tip、第四点复核 10 文件冻结（0 行、合计 1359）、通读 R34-A 并将其轴并入不移植清单（§0）——src 树两 tip 逐字节同一，测量效力不受影响。
**分支:** `cursor/r34-b-live-routing-thirty-fourth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R28-B…R33-B 引擎位级相同；系统 Node v22.14.0（`/exec-daemon/node`）已显式绕开——PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 后 `node --version` 复核 v22.22.2，三次官方运行首行均打印 `engine node=v22.22.2`，仿真内置 L0 引擎同一性断言 ×3 全过；**零官方批次落在 v22.14.0 上**，无需弃批。tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**诚实空枚举：无达门槛的新更优解，切片零代码改动，未铸任何 S34-B-\* ID、未铸任何新 face 名或 ALLCAPS 外号，EXCLUSIONS.md 零增行；且本轮枚举**零**新站点级换名记录——比 R33-B（恰 1 项：`ROLE_FAMILY` 死回退）更干净的一轮空收口。** 第三十四遍透镜按令**不**重走 R31-B 面格闭合审计（六扇停驻门维持）、**不**重走 R32-B 闭合稳定性审计、**不**移植 R33-A A 账本 44/44 方法卫生与 R33-C…R33-J 空枚举——改做**从零独立重枚举**：重读 10 文件全量 1359 行，重走结构位置 × 消除机制满矩阵（§2d/§4），对每个枚举出的角度执行换名拒绝检查。结果：全部角度落在既有排除 ID 本体、B 区自有封禁面、姊妹区归档面（明令不移植）或 R31-B 停驻门成员上；本轮枚举尾部仅存的两个残余微观角度（`assign.ts`/`assign-plan.ts` 每批 id 双 map、`decideLiveCascade` 的 `stay` 闭包每调分配）经查证均为**已点名站点复命中**（分别 R2-B §4 点名 + S7-B-6 每批 ns 天花板、R2-B「S1-B-4 同表面」裁决并经 R5-B/R6-B/R8-B 三次维持），不构成新站点级记录。R33-B §2d 记录 1 的 `ROLE_FAMILY ?? "unknown"` 死回退本轮复核为已点名站点复命中，按令不重提不铸 ID。派单硬保持项显式重取证：**S12-B-2 重开条件腿 (a) 未触发**（`eval-routing.ts` 全文 `prior` 计数 = 0（rg exit 1），:353–354 只传 learned，`assignTasks` 生产调用方图景三处与 R30-B…R33-B 逐条一致无新增）；**S13-B-1 重开条件未触发**（`learning/learned-routing.ts` 对 `94ed3d9` 逐字节 diff = 0 行，`applyLearnedRouting` 本体成本无变化，维持 loser ID）。

关键前提事实：切片 10 个文件（清单逐一核对恰为 10，`wc -l` 合计 **1359** 行，逐文件计数见 §0，与 R33-B 逐数一致）自 R1-B 裁决基线 `94ed3d9` 以来**逐字节零变化**（`git diff 94ed3d9..HEAD -- <切片 10 文件>` = **0 行**，分支创建时、测量后、提交前与 rebase 至 `8634e7d` 后四点核对；测量窗口内 exclusive 零快进，提交前快进为纯 docs，四点覆盖全程）。R1-B 的结构下界论证、R4-B 的聚合天花板论证与 S1-B-\* … S13-B-1 全部裁决对当前代码原样成立。

**测量侧诚实声明（本轮重要方法注记）**：历届 B 驱动（/tmp/rXXb-sim.mts 族）从未入库，本 VM 上不存在其字节；本轮驱动为**按 R4-B…R33-B 附录约定从零重建**（种子 0xb44b01 mulberry32、N=2000/20000/30 语料、M=2/7/10 目录、×2 eval 对口径、W1/W2/W3 证人、L0 断言——细节见附录 A），故与历届报告的**逐格数值带对照只具指示性**；本轮锚点的效力载体是**结构性结论**（格间全序、learned 税方向、落地线余量、亚 µs 探针、prior 倍率）与 ×3 运行内部一致性。天花板本 VM 实测：**ceiling M=2 10.54–11.12 / M=10 16.10–16.27 ms/eval**（×3 运行，×2 对口径；M=2 与 R33-B 10.75–11.73 交叠，M=10 低于 R33-B 主机偏置窗 21.48–23.74 与 R32-B canonical 17.84–18.43——差额由「本 VM 更快 + 重建 harness 的 M=10 目录/语料构成差」共同解释，回归不可能成立：切片对 `94ed3d9` 零 diff）；replay-faithful M=2 **8.94–9.59**（R33-B 7.64–8.21 邻接上侧）；10× 压力 N=20000 **106.1–107.6 ms/eval**（历史并集 94.8–140.9 **带内**）；全 18 矩阵格 ×3 保持 M=2 < M=7 < M=10 全序；learned ≥ no-learned 在 **7/9** 格对成立，两例反转均在 M=7（runs 1/3，−2.3%，±5% 抖动带内，run 2 同对 +9.9% 正常成立，W1/W2 证人全过——与 R33-B 的 17/18 同类，诚实记为主机抖动非结构翻转）；`holds-below-landing-line=true` ×3（最大 16.27 < 30）。prior 双档格：假设档 N=2000 M=7/M=10 = 36.4–36.6 / 42.0–43.8 ms/eval **仍越线**；M=2 = 23.7–23.8 ms/eval 在重建轻量快照（单拾取 9.77–9.97 µs vs 历史 19.5–21.8 µs）下低于绝对线但仍为其无 prior 孪生格的 **2.4–2.9×**——「若有流量即数量级越线」的邻接配置结论不变，生产流量本轮重取证仍为零。落地线（数十~数百 ms 或复杂度类下降）在现有生产流量下依旧不可达。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- **切片恰 10 文件**（逐文件 `wc -l`）：`r0.ts` 173 / `assign.ts` 129 / `assign-plan.ts` 68 / `policy.ts` 160 / `live-cascade.ts` 118 / `live-selection.ts` 61 / `analyze-task.ts` 108 / `primary-catalog.ts` 101 / `catalog-model.ts` 83 / `supervisor/model-router.ts` 358，合计 **1359** 行，与 R32-B/R33-B 逐数一致。`catalog-observed.ts` **不在**本切片（按令未纳入、未触碰、未计时）。
- **冻结四点核对全过**：分支创建时（tip `44819a4`）`git diff 94ed3d9..HEAD -- <切片 10 文件>` = **0 行**且 `wc -l` 合计 1359；测量后复核 0 行；提交前第三验 0 行；rebase 至 `8634e7d` 后第四验 0 行（`wc -l` 复核合计仍 1359）。`package.json` / `pnpm-lock.yaml` vs `94ed3d9` 亦 **0 行**。最后一次生产 live-B 变更维持 `94ed3d9`；最后一次 `src/routing/` 生产变更维持 `183df9b`（S7-C，离线 C 区）。
- **读序（按令完成）**：README → PROGRESS → EXCLUSIONS 全表（545 行，X0-\* … S13-B-1 全部实读）→ round-34 PLAN 全文（B 段 + 具名/不具名禁令）→ round-33 PLAN → R33-B 全文（默认对照带 + §2d 记录 1 边界）→ 10 切片文件全量实读（1359 行）。
- **摄取状态**：测量窗口内独占分支零快进（分支创建时与测量后两次 fetch 均 `44819a4`）；提交前第三次 fetch 发现纯 docs 快进 `44819a4 → 8634e7d`（R34-A 摄取 + R34-D 派单），按令 rebase、第四验冻结、通读 R34-A 全文并将其轴并入下条不移植清单；按令未等待 C——其自行返回并由父代摄取，姊妹分支零触碰。
- **不移植 / 不重铸清单维持**（枚举时逐条遵守，本轮零触碰）：全部具名禁令 **FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX**；**R31-B 面格六停驻门**（catalog-row hidden-class / IC-shape、cascade evidence×failure-class lattice、assignTasks cold-onset segmentation、batch class-composition mix、LINKTOP-in-B、prior alias-resolution pathway——全部维持停驻，零重开）；**R32-B 闭合稳定性审计**（54/54 与 D1–D5 为停驻记录，不重走、不铸「再查一遍闭合稳定性」式新面）；**R33-A 空枚举**（A 账本 44/44 方法卫生）；**R33-B 空枚举**（`ROLE_FAMILY` 死回退已拒列为已点名站点，本轮不重提）；**R33-C…R33-J 空枚举**（各自站点级换名记录与轴归档均为切片外属地，不移植不重铸）；**R34-A 空枚举**（提交前 rebase 后通读并入：A 区预算重锚 12.8–15.2 µs/gate + 层序测量仪器诚实注记 + 三项站点级换名记录——child-tracking `skipped` 单例 / from-child `completedChecks` 防御拷贝 / turn `readersInvoked` 字面量——全部为 `src/tracking/` + `src/run/` A 区站点，与本切片零共享物理对象，不移植不重铸）；R30-B scale-cube face；R32-A F12a–d 哨兵审计；R32-J 六残余坐标（本切片零 fs 位点）。
- **B 区自有封禁面不补铸**：R29-B learned-policy container face / R28-B catalog-face row-arrangement (permutation) face / R27-B batch-cardinality bottom + prelude intercept face / R26-B privacy / provider-policy constraint-leg face / R25-B task-face optional-field presence topology / R24-B M=1 catalog collapse / R20-B objective 长度 L 轴 + K×W 双线性轴 / R19-B 比较器第三腿 + 目录基数 M 轴。**目录行预排序仍是行为变更**（R28-B W3 裁定行序为契约决策载体），本轮未尝试、未预排序。
- **硬不变量**：切片零代码改动 ⇒ Live=R0 等价（R1 未接线 live；稀疏回退 approved R0 baseline）、双 LCB（Beta+正态）、双归因（logit+prob-add）、proposal-first（Checkpoint F-PROD 开放，ADR-005，不声称 Outcome-supported）、阈值、CAS/凭证/数据面契约/公共签名全部原样保持；live 面不 import R1/bandit/shadow 由 `live-isolation.test.ts` 在聚焦 198 项中复证通过。无 fsync / 完整性重哈希移除；fail-open 家族保持关闭；未改任何测试。S5-H-1 / S7-F-1 ≠ S6-F-5 / S8-I-1 / S8-E-1 / S9-D-4 / S12-D-1 全部切片外硬保持项零触碰。npm POSIX `.bin` symlink 静默 no-op 未当优化修。
- **EXCLUSIONS.md 未增行**（空枚举记录不入排除表，plan-ban 归属父代）；仓库变更恰为本报告单文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核仅 `docs/reports/sota-opt/round-34/R34-B.md`）。禁止开 PR，未开。

## 1. 第三十四遍搜索方法：从零独立重枚举

R31-B 已证面格闭合、R32-B 已证闭合稳定、R33-B 已做过一次从零重枚举（收获恰 1 项新站点级换名记录）——第三十四遍不照抄其结论，而是**独立重跑同一枚举原点**：假装排除表不存在，重读 1359 行找出每一个理论上可疑的消除/重构角度，再逐一对表执行换名拒绝检查（词根不同 ≠ 面不同；站点不同但同族同向 = 换位点记录不铸 ID；**站点已点名 = 复命中，零记录**）。方法四步：

1. **生产调用方图景重取证**——`rg 'assignTasks\(' src/` 恰 3 个调用方（`cli/main.ts:365` smartChildPlan live face N≤30、`adaptation/eval-routing.ts:353–354` 只传 learned、`track/primary-split.ts:41` C 小批），与 R30-B…R33-B 逐条一致，无新调用方、无新可选参数；`assignOne` src 内零生产调用方维持；`effectiveConfidenceThreshold` / `routeTask` / `routeFlowNode` 在 src 内仍只有 `supervisor/flowchart.ts:34–36` barrel 再导出、零消费者（R12-B 零流量取证第四次转移成立）；`routeR0` 唯一调用方仍是 `r1-shadow-report.ts:88`（shadow-only）；`applyCascade` 唯一调用方仍是 `cascade-evidence.ts:102/:108`，`applyEvidenceCascade` src 内零调用方（R5-B「生产不可达」维持；`learning/from-episode.ts` 的 `applyCascadeRetry` 为学习区同名异物，非本切片调用方）；live 级联生产调用方仍是 `track/loop.ts:120`、`cli/main.ts:377`（plan）与 `run/child-coordinator.ts:468/:475`（evidence+decide），每子任务一次。
2. **结构位置 × 消除机制满矩阵**——10 文件 × 13 类消除机制（死码/常量折叠/CSE 去重/记忆化/数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/形状单态化）逐格过筛（§4 按文件汇总）。
3. **换名拒绝检查**——非空格逐一对表；本轮尾部仅存的两个残余角度均查证为已点名站点复命中（§2d），**零**新站点级记录。
4. **测量侧不铸新剂量网格**——只重锚既有格 ×3 独立运行 + W1/W2/W3 行为证人 + L0 引擎同一性断言；驱动为从零重建（诚实声明见结论段与附录 A），两次校准运行（matrix 反复位次噪声 → 臂交错 + reps 7→15）按纪律弃置，官方三运行全绿。

## 2. 天花板复测：R4-B…R33-B 收口第三十一次复核成立（重建 harness）

引擎 Node v22.22.2 / V8 12.4.254.21-node.39（canonical，L0 断言 ×3）。语料种子 0xb44b01（重建生成器）。×3 独立进程运行，全部 ALL CHECKS PASSED；确定性 sink ×3 逐位相同（3189265）。单位 ms/eval（×2 对口径，eval = baseline+candidate 两次 `assignTasks`，镜像 `eval-routing.ts:353–354`）：

| 格 | run 1 | run 2 | run 3 | 历史参照（指示性） |
|---|---|---|---|---|
| ceiling N=2000 M=2（rich 语料 + limits） | 10.545 | 11.122 | 10.698 | R33-B 10.75–11.73 交叠 / R32-B 9.04–9.62 上侧 |
| ceiling N=2000 M=10 | 16.103 | 16.270 | 16.174 | 低于 R32-B 17.84–18.43 与 R33-B 21.48–23.74（重建构成差 + 本 VM 更快） |
| analyzeTask 份额 | 32.2% | 30.4% | 31.9% | 历史 ~20–25% 邻域上侧（rich 语料 token 选项占比更高） |
| replay-faithful M=2（bare 语料，eval-routing 裸镜像） | 9.496 | 9.594 | 8.935 | R33-B 7.64–8.21 邻接上侧 |
| 10× 压力 N=20000 M=2 | 106.127 | 106.638 | 107.586 | 历史并集 94.8–140.9 **带内** |
| CLI live face N=30 M=2（µs/调用） | 69.1 | 68.8 | 72.9 | R14-B 47.1–50.4 上侧（重建 face 语料含 long/高危尾） |

M=2 全序、M=10 全序、analyzeTask 份额、压力格与 live face 的结构关系逐运行成立；`holds-below-landing-line=true` ×3（M=10 最大 16.27 < 30，余量 ~46%）。**回归不可能成立**：切片对 `94ed3d9` 逐字节零 diff（三点冻结），任何格间 Δ 只能来自主机与重建 harness 构成差；W1（双跑逐字节恒等）/ W2（learned 臂仅在 avoid/prefer 命中族上分歧）/ W3（prior 拾取存在性）三组行为证人 ×3 运行全过，语义与 R30-B…R33-B 恒等。

## 2b. 配置态 × 命令类矩阵：全六格复测（第二十五次全格复核；臂交错测量）

replay N=2000 bare 语料，ms/eval（×2 对口径；两臂在同一 rep 循环内交错采样，median of 15）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| M=2 default, no learned | 8.196 | 8.386 | 8.406 |
| M=2 default, learned | 8.905 | 8.936 | 9.785 |
| M=7 configured, no learned | 12.660 | 11.983 | 12.774 |
| M=7 configured, learned | 12.370 | 13.169 | 12.474 |
| M=10 stress, no learned | 15.103 | 14.800 | 14.924 |
| M=10 stress, learned | 15.132 | 15.554 | 15.440 |

全 18 格保持 M=2 < M=7 < M=10 全序 ×3。learned ≥ no-learned 在 **7/9** 格对成立；**诚实注记**：M=7 对在 runs 1/3 反转（12.370 vs 12.660 / 12.474 vs 12.774，learned 侧各反快 −2.3%），深居 ±5% 抖动带内，同对在 run 2 以 +1.19 ms/eval（+9.9%）正常成立，且 M=2 / M=10 对九测七正——两例反转与 R33-B 的 run 3 M=10 反转（−0.12%）同类，W1（该格双跑逐字节恒等）与 W2（learned 臂仅 avoid/prefer 命中族分歧：M=2 inside=319 / M=7、M=10 inside=814，outside=0，×3 逐数相同）全过，记为主机抖动，不构成结构翻转、不铸坐标。CLI live face M=7 configured+learned = 104.1 / 100.8 / 104.6 µs/调用。级联探针（本轮以 M=2 plan 为基座，2 tiers——与历史 M=7 基座探针不可直比，如实注明）：`liveCascadePlanFromAssignment` 207–218 ns、`decideLiveCascade` 244–251 ns，均亚 µs，每子任务一次的生产流量下仍为深度亚噪声。

## 2c. prior 双档格复测（第二十三次跨 VM；S12-B-2 重开腿 (a) 复核）

重建快照 `pps_r34b_realistic_v1`（4 源 18 行 + aliasMap 7 行；单拾取 9.77–9.97 µs/op——轻于历史快照的 19.5–21.8 µs，倍率结论按各自 rig 内部孪生格比值给出）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| 假设档 N=2000 M=2 + prior（ms/eval） | 23.685 | 23.813 | 23.756 |
| 假设档 N=2000 M=7 + prior | 36.625 | 36.560 | 36.376 |
| 假设档 N=2000 M=10 + prior | 41.973 | 43.815 | 42.348 |
| 真实档 live face N=30 M=2+prior（µs/调用） | 170.3 | 171.2 | 172.7 |
| 真实档 N=30 M=7+prior+learned（µs/调用） | 270.4 | 271.2 | 267.2 |
| 单拾取 pickFromPublicPrior（ns） | 9973 | 9769 | 9901 |

假设档 M=7/M=10 六读全部越线（>30 ms/eval）；M=2 在本轮轻量重建快照下 23.7–23.8 低于绝对线，但为其无 prior 孪生格（8.20–8.41）的 **2.8–2.9×**（M=7 为 2.8–3.1×、M=10 为 2.7–3.0×）——「若有流量即数量级越线」邻接配置结论不变；生产流量本轮重新取证仍为零：`rg -c 'prior' src/adaptation/eval-routing.ts` = 0 匹配（rg exit 1），:353–354 `assignTasks({ catalog, tasks, learned: … })` 只传 learned。**S12-B-2 腿 (a) 未触发**，条件式落地物维持在架，按令不补铸不重扫（R21-B 快照形状轴维持关闭）。W3（三种 M 下 family=edit 拾取存在）×3 全过。

## 2d. 第三十四遍枚举：换名拒绝检查记录

从零满矩阵扫描（§4）枚举出的全部角度经对表逐一裁决。**本轮零新站点级换名记录**（R33-B 为恰 1 项；本轮更干净）。枚举尾部仅存的两个残余微观角度均查证为**已点名站点复命中**：

1. **`assign.ts:63` + `assign-plan.ts:29` 每批 id 双 map / 双 Set**（`catalogIds = input.catalog.models.map(id)` 与 `planAssignmentPolicy` 内 `new Set(models.map(id))` 与 `validateConfig` 内查重 `ids` Set 三处每批各枚举一遍目录 id）——R2-B §4 已点名（「`catalogIds` 重 map 与 router 内部 Set 不共享属公开接口变更」），每批一次 ns 级天花板由 S7-B-6 本体承载（M=2 −11~131ns / M=10 252–355ns，一次符号翻转）。复命中，零记录。
2. **`decideLiveCascade` 的 `stay` 辅助闭包每调分配**（live-cascade.ts:87）——R2-B 已点名（「`stay` 闭包每调分配为亚噪声（S1-B-4 同表面）」）并经 R5-B/R6-B/R8-B 三次维持；本轮探针 decide 全函数 244–251 ns/op，闭包份额为其真子集，生产流量每子任务一次（C≤6/run）⇒ 亚 µs/run。复命中，零记录。

其余枚举角度全部落在既有裁决**本体**上（代表性清单，与 R33-B §2d 清单核对一致）：`validateConfig` ROLES.includes / dup-role Set = S1-D-8 类 + S10-B-3；`planAssignmentPolicy` 双排序拷贝取极值 = S8-B-1；`assignPlanned` 两次防御拷贝 = S8-B-4 / S2-B-1、options 三条件 spread = S9-B-3 / S4-B-4、批内记忆化 = S3-B-1（learned 层 = S13-B-1 loser）；route 输入骨架 = S5-B-4 / S3-B-6 / S4-B-5 族；`validateInput` 字面量数组 = S1-B-7、per-id trim 卫 = S9-B-2；unknown-model 预扫描下沉 = S9-B-4；per-route allowed Set = S12-B-1（双向封死）；主循环融合 = S11-B-1（镜像封死）；`evaluateLiveCandidate` 空前置失败别名 = S7-B-3、per-model request 字面量 = R1-B §5 契约裁决（fixedCostUsd/fixedLatencyMs 所需）、failures 合并 spread = 同站点微观；`toModelDescriptor` 每 (任务,模型) 构造 = R1-B §4.4 架构裁决；`buildDecision` 构造份额 = R32-B D1 普查（19/20 字段活）、eligible.map 与 oneHot 融合 = S6-B-1；justification 预计算 = S7-B-4；approvalPlan 享元 = S6-B-2；one-hot 载体形 = R32-B D4 三重关；`coldStartRoutingScore` 钳位 = S10-B-2；空 avoid/prefer 跳过 learned = S6-B-4；`cheapFirstTiers` 装饰-排序-还原 / localeCompare / spread = S3-B-2 / S3-B-3 / S1-B-5；`decideLiveCascade` find+findIndex = S1-B-4；`liveRefusalMessage` 双 .some = S8-B-2；`selectLiveModel` 比较器展开 = S8-B-3；analyze-task 正则链 / HIGH_RISK_RE 双测去重 / 死值谓词 / reason join / long 惰性与换行手写计数 = S4-B-1 / S1-B-1 / S5-B-1 / S5-B-3 / S10-B-1 + S1-B-2；`familyOf` 布尔重排 = S1-B-3、终端死回退 = R33-B §2d 记录 1（已点名）；`routeR0` 高风险 find = S1-B-6、r0 排序输出 = R1-B 契约；`applyCascade` `{...decision}` 拷贝 = 生产不可达（R5-B/R9-B 维持，本轮 `applyEvidenceCascade` 零调用方重取证）；`primary-catalog` 构造器享元 = S7-B-5 / S6-B-2 类身份可观察、throw 位点 = R17-B 拒绝路径已锚 + R32-B D2 维持；`effectiveConfidenceThreshold` candidates 数组 + `Math.max` spread 与 `routeTask`/`routeFlowNode` 包装 = R12-B 零流量不铸维持（本轮 barrel census 重证）+ Math.\* 绑定属 BINDX 坐标声明即止；`createModelRouter` `{...config, models}` 每 run 一次 CLI 噪声档；`catalogModel` 条件 spread 属性存在性可观察 = S1-C-10 类。

## 3. 候选总表

**空。** 铸 ID 数：**0**。铸 face 名 / ALLCAPS 外号数：**0**。EXCLUSIONS.md 增行数：**0**。新站点级换名记录数：**0**（两个残余角度均为已点名站点复命中，§2d）。本轮产出为三点哨兵重取证（S12-B-2 腿 (a) / S13-B-1 / 调用方图景）与重建 harness 下的锚点复位（§2–§2c），均不构成候选。空枚举与 R8-B…R33-B 同为战役允许的诚实结局。

## 4. 结构位置 × 消除机制满矩阵扫描（第三十四遍，按文件汇总）

| 文件 | 本轮检查 | 结论 |
|---|---|---|
| `r0.ts` | `applyCascade` 生产不可达第三十四次重取证（`applyEvidenceCascade` src 零调用方；from-episode 的 `applyCascadeRetry` 为同名异物）；routeR0 唯一调用方 r1-shadow-report:88（shadow-only）；S1-B-6/S2-B-3/S3-B-4/S4-B-3 维持；排序输出即契约 | 无候选 |
| `assign.ts` | 调用方图景三处重取证一致（§1）；id 双 map = R2-B §4 已点名 + S7-B-6 本体（§2d 记录 1，复命中零记录）；S8-B-4/S2-B-1 双拷贝、S9-B-3/S4-B-4 options、S3-B-1 批内记忆化、X3-1 维持；S13-B-1 重开门未触发；`flowchartRoleForAgentRole` 属 graph 区（跨切片只记录） | 无候选 |
| `assign-plan.ts` | S8-B-1 维持；每批双 Set 同上复命中；`pickPreferredModel` prior 层 = S12-B-2 条件陆器在架未触发；`pickFromPublicPrior` 本体属 `public-prior.ts`（切片外） | 无候选 |
| `policy.ts` | per-model request 字面量 = R1-B 契约裁决；S7-B-3 别名护栏维持；预算/期限 toFixed 串仅失败路径 = R17-B 已锚；COMPLEXITY_RANK 常数查找无池；capability 循环已首缺早退 | 无候选 |
| `live-cascade.ts` | `stay` 闭包 = R2-B 已点名复命中（§2d 记录 2）；S1-B-4/S1-B-5/S3-B-2/S3-B-3 维持；byId Map 每 plan 一次 M≤10 ns 档；探针 207–218 / 244–251 ns 复锚（M=2 基座，§2b） | 无候选 |
| `live-selection.ts` | S8-B-3/S8-B-2 维持；`compareLiveCandidates` Number() 布尔差分已是无分支形态；总序契约（R19-B 第三腿封禁）零触碰 | 无候选 |
| `analyze-task.ts` | `ROLE_FAMILY` 死回退 = R33-B §2d 记录 1 已点名（不重提）；HIGH_RISK_RE 双测 = S1-B-1 本体；S1-B-2/S1-B-3/S4-B-1/S5-B-1/S5-B-3/S6-B-3/S7-B-1/S10-B-1 维持；份额 30.4–32.2% 复锚（rich 语料构成差如实注明） | 无候选 |
| `primary-catalog.ts` | 单点构造器消灭形状多态（R31-B C1 停驻维持）；构造器享元 = 身份可观察类；modelFor/inferFastId 每配置一次 ns；throw 位点归 R32-B D2 | 无候选 |
| `catalog-model.ts` | `toModelDescriptor` R1-B §4.4 架构裁决维持；oneHotDistribution = R32-B D4 维持；条件 spread 属性存在性可观察（S1-C-10 类）维持 | 无候选 |
| `supervisor/model-router.ts` | `effectiveConfidenceThreshold`/`routeTask`/`routeFlowNode` 零流量重取证（barrel 三行再导出、零消费者）；S12-B-1/S11-B-1 镜像对封死维持；S1-B-7/S9-B-2/S9-B-4/S5-B-4/S10-B-2/S10-B-3/S7-B-6/S6-B-1/S6-B-2/S6-B-4/S6-B-5/S7-B-4/S8-B-4 维持；buildDecision = R32-B D1 普查维持（19/20 活）；`createModelRouter` 每 run 一次 CLI 噪声档 | 无候选 |

## 5. 关键裁决细节

- **S12-B-2 换名检查**：假设档 M=7/M=10 六读越线依旧、M=2 以孪生格 2.8–2.9× 倍率维持邻接（§2c），腿 (a) 重新取证未触发（eval-routing 零 prior、调用方图景三处未变、无新可选参数）。未以任何新名重扫快照形状轴。
- **S13-B-1 换名检查**：`git diff 94ed3d9..HEAD -- src/learning/learned-routing.ts` = 0 行，本体成本无变化，重开条件（applyLearnedRouting 本体成本增 ≥2 个量级）未触发。learned 容器坐标归 R29-B（封禁），本轮未触碰。
- **目录预排序**：R28-B W3 裁定行序为契约决策载体，预排序 = 行为变更，本轮未尝试。
- **重建 harness 与主机窗口**：本轮全部格值出自从零重建驱动（诚实声明见结论段），跨报告数值带对照只具指示性；效力护栏为 (a) 三点冻结 0 行、(b) W1 双跑逐字节恒等 ×3 + 确定性 sink ×3 逐位（3189265）、(c) W2/W3 语义证人 ×3（divergence 计数 ×3 逐数相同）、(d) 序结构（格间全序、份额占比、落地线余量、prior 倍率）与 R30-B…R33-B 完全同构。M=7 对 runs 1/3 的 −2.3% 反转（§2b 诚实注记）在该护栏下裁定为主机抖动。
- **引擎纪律**：三次官方运行全部在 nvm Node v22.22.2 产出（L0 断言 + 每次运行前 `node --version` 复核）；系统 Node v22.14.0 上零官方批次，无弃批。两次校准运行（亦在 v22.22.2）因 harness 位次噪声修正（臂交错 + matrix reps 7→15）按纪律弃置，不计入官方口径。

## 6. 前后对比与测试

切片零代码改动，无前后对比。验证（全部在 Node v22.22.2）：

- 聚焦 live 路由单测：`npx tsx --test "test/unit/routing/*.test.ts"` —— **198/198 pass**（18 suites，含 live-isolation / cascade-evidence / failure-class / assign / assign-plan / analyze-task / r0 / live-cascade / live-selection / catalog-eligibility 等），与 R31-B / R32-B / R33-B 计数逐字相同。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0** —— 1169 项测试 1168 pass / 0 fail / 1 skipped（既有跳过项，非本轮引入），tsc build 通过；计数与 R9-B…R33-B 逐字相同。
- 仿真 ×3 独立运行 ALL CHECKS PASSED（L0 + W1/W2/W3 逐运行全过；确定性 sink ×3 逐位相同）。
- 提交前最终 fetch：独占分支快进 `44819a4 → 8634e7d`（R34-A 摄取 + R34-D 派单，纯 docs——`git diff 44819a4..8634e7d -- src/ package.json pnpm-lock.yaml` = 0 行），按令 rebase 至 `8634e7d`；冻结第四验 0 行；`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 恰为本报告一个新文件。
- 无赢家故 scripts/ 零新增；harness 未入库（重建说明见附录 A，指纹见附录 B，临时文件按纪律核后删除）。

## 7. 新增排除 ID

无。未铸任何 S34-B-\* ID；EXCLUSIONS.md 零增行；零新站点级换名记录（§2d 两条均为已点名站点复命中，无需归档新记录；plan-ban 归属父代）。

## 附录 A：本轮驱动重建说明

历届 B 驱动（/tmp/rXXb-sim.mts 族）从未入库，本 VM 无其字节可衍生；本轮驱动 `/tmp/r34b-sim.mts` 为按 R4-B…R33-B 附录约定**从零重建**：

1. 语料：mulberry32(0xb44b01) 确定性生成 20000 任务（七角色均匀、16 stem 池含 10% 高危尾、25% long（半数 ≥3 换行）、50% 带 contextTokens/outputTokens、10% 显式 contractRisk）；标准格取前 2000，live face 取前 30；**rich**（带 token/contractRisk 选项 + limits）供 ceiling 格，**bare**（仅 taskId/role/objective）供 faithful/matrix/stress 格（eval-routing 裸镜像）。
2. 目录：M=2 = `catalogFromPrimary({primaryModelId:"premium"})` canonical 对；M=7 configured（cheap/m01–m05/premium，成本严格递增无并列，m01–m05 均有 prior aliasMap 行）；M=10 stress（+m06–m08，premium 0.85）。
3. 口径：×2 eval 对（baseline 空 learned + candidate learned=avoid(review)+prefer(plan)，镜像 `eval-routing.ts:353–354`）；ceiling/faithful median of 7、matrix 臂交错 median of 15、stress median of 3、face mean of 2000 调用、探针 mean of 200k、单拾取 mean of 2000。
4. 证人：W1 双跑 JSON 逐字节恒等（每 M 两臂）；W2 learned 臂分歧任务全部落在 avoid/prefer 命中族（outside=0，inside M=2 319 / M=7、M=10 814，×3 逐数相同）；W3 三 M 下 family=edit prior 拾取存在；L0 `process.version === "v22.22.2"` 断言（不计时）；确定性 sink 防 DCE 且 ×3 逐位。
5. 校准史（如实记录）：初版 matrix 单臂顺序测量 + reps 7 在本 VM 突发负载下出现位次噪声（no-learned 首格吸收冷启 + 单格内 12–27 ms 离散），修正为两臂同 rep 循环交错 + reps 15 后 ×3 全绿；两次校准运行弃置，官方口径为其后三次独立进程运行。

## 附录 B：驱动指纹与运行方式

- 驱动：`/tmp/r34b-sim.mts`，465 行，sha256 `6052f0fe506581bcb8c53e25b795cbabaa44ffd5fbd75558d3741f8336e2d969`（未入库；临时文件已删除）。
- 运行（repo 根，空闲 VM；PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开 `/exec-daemon` 的系统 Node v22.14.0）：

```bash
npx tsx /tmp/r34b-sim.mts   # 独立进程 ×3 = runs 1–3
```

---

**MORE_OPTIMA=no** —— 第三十四遍从零重枚举空收口：零新站点级换名记录（两残余角度均为 R2-B 已点名站点复命中）；重建 harness ×3 全绿复锚天花板（ceiling M=2 10.54–11.12 / M=10 16.10–16.27 ms/eval，freeze 0 行，W1/W2/W3+L0 证人全过，确定性 sink ×3 逐位）；全 18 矩阵格全序成立（learned 对 7/9，M=7 两例 −2.3% 反转诚实记为抖动）；prior 双档格 M≥7 越线、M=2 以 2.8–2.9× 孪生倍率维持邻接；S12-B-2 腿 (a) 与 S13-B-1 重开门显式重取证未触发；不铸 ID、不铸面、不增排除行；生产代码零改动，聚焦 198/198 + `pnpm gate` exit 0（1169/1168/0/1 计数逐字同）。

MORE_OPTIMA=no
BRANCH=cursor/r34-b-live-routing-thirty-fourth-pass-83a1
