MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 30 / R30-C：离线路由切片第三十遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 30 / R30-C（Round 1–29 同区二十九遍
  之后的第三十遍，叠在已落地的 S7-C 之上）
- **基线:** 分支创建与全部测量在 `cursor/sota-persistent-opt-83a1` @
  `1d1f783`（R29-J 空枚举合入 + Round 29 关轮 + R30-C 派单后的独占分支
  tip）；提交前独占分支快进两次至 `1f41e30`（`a93ab14` R30-A 空枚举
  合入 + R30-D 派单；`1f41e30` R30-B + R30-D 空枚举合入 + R30-F 派单，
  均 docs-only：仅触 docs/reports/sota-opt/ 下文件，`src/` 与 `test/`
  零行逐次核对），报告分支已逐次 rebase 通过并在每个新 tip 上重验
  冻结。`git log -1 -- src/routing/` 显示切片最后一次生产改动**仍为
  `183df9b`「perf(routing): land S7-C support-only eta」**；
  `git diff 183df9b..HEAD --` 对 9 个切片文件为**空**（0 行，
  `1d1f783` / `a93ab14` / `1f41e30` 三个 tip 上各验一次），两次快进均
  未触及 `src/`，测量形态与提交形态位级同一。R30-A 轴（call-stream
  ordering / dispatch-history face）、R30-B 轴（scale-cube
  second-order interaction face）与 R30-D 轴（approval-profile
  constraint-list geometry face）已核对并入非移植清单（§4），未
  等待、未触碰其余兄弟分支。
- **分支:** `cursor/r30-c-offline-routing-thirtieth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，与 R29-C 普查同构 2,039 行；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第三十组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第三十遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R29-C 的全部无号关闭
探针与轴，含 R29-C 的 PAIRX、R28-C 的基片实例化、R27-C 的 PROBX、
R26-C 的 PRICEX、R25-C 的 AGEX、R24-C 的 OCCX、R23-C 的 ORDX、R22-C 的
SEEDX、R21-C 的 YMIX、R20-C 的 SCALEX、R19-C 的 FITQ）后，本轮**从未
点名站点数为零**（连续第十二遍）——每一个执行站点都能指到既有 ID、
无号探针、零池备忘或池算术覆盖。R26-C/R27-C 把切片公开输入面的**全部
数值坐标**定价完毕，R28-C 关掉**执行基片坐标**，R29-C 关掉**进程工作
负载组成坐标**（同进程姊妹内核的有无与次序）；本轮新增的完备性轴落在
输入面与执行面的交界上——**同一抽象输入值在引擎层以什么表示进入
切片**：

- **input object-representation face（输入对象表示面）的 C 区实例化
  （新轴一次性收口，非候选；face 名归 R30-C 本轮；观察绰号 REPRX——
  plan-ban 用，不铸 ID、不写入排除表）**——R1-C..R29-C 的**每一个生产
  读数**都把行喂成同一种引擎表示：单隐藏类字面量对象 + packed 数组。
  而调用方拿到的行对象在引擎层还有一整族逐位等值的表示（属性插入序 /
  字典模式 / class 实例 / 冻结映射 / holey 数组），该坐标从未剂量化。
  结构前提（本轮定理）：`fitLogitAdditive` 在入口把工作集重物化
  （offline-logit.ts L420–425 `baseRows = rows.map(...)`，其后原始
  `rows` 参数**零读取**——本轮逐行核实），所以 657 ms 热池被一次
  O(n) 边界 map **结构性防火墙**隔离于输入表示之外；表示敏感区 =
  一次 400 行 × 4 具名读的边界 map + 3 次 options 读。本轮三臂剂量
  （CANON = 历史孤表示形态；REP = 六种对抗表示的 small 形喂饲交错在
  计时 rep 之间，计时器外，把生产边界 map 的 IC 打成 megamorphic；
  PREREP = 首个计时调用前 5 遍 × 6 变体先行喂饲），**runner 逐字节
  同一**（臂仅由 R30C_ARM 选择），六变体全部钉死抽象值（字符串引用
  与标量位直接取自同一冻结夹具；行对象全新分配零别名；键集恒为
  五键），交错 3 轮 × 3 臂 = 9 个独立进程：
  **位面**——9/9 进程的 logit 六报告 IEEE-754 位型折叠 SHA-256
  **逐字节相同且与 R28-C/R29-C 打印摘要 `63e74db4…a893` 逐字节
  同值**（跨轮 + 跨基片 + 跨组成 + 跨表示四重见证）；六种对抗表示的
  perf 形报告摘要 6/6 进程同值 `55e6f81a…fb99` 且每个都与本进程
  规范表示报告逐位相等；全部喂饲调用（5×6 前置 + 7×6 交错，每进程）
  逐个摘要断言等于规范 small 报告。**对抗表示动不了任何输出位。**
  **热层**——perf 形 7-rep 中位：CANON 663.5 / 664.3 / 661.3，REP
  653.6 / 663.6 / 665.3，PREREP 662.6 / 663.8 / 662.8 ms；同轮差
  REP−CANON = −9.83 / −0.72 / +4.04，PREREP−CANON = −0.83 / −0.52 /
  +1.52——**符号翻转**（负→负→正）且与臂内散布同量级，比率
  0.985–1.006，**离 ±35 ms 带 ≥3.5×**；小形状差同样翻转 = 纯抖动。
  **边界价**——表示敏感区（生产入口 map 的逐语句复刻，runner 本地）
  在 megamorphic 稳态、七种输入表示轮转的最坏剂量下 **4.6–8.2
  µs/报告**（2000 调用 × 2 遍 × 9 进程，mapSink 9/9 同值
  11,200,000），离 ±35 ms 带 **~4,300×**——防火墙定理的实测确证。
  **收口判定**：位面全等 + 热层符号翻转 + 边界区结构封顶 µs 域 ⇒
  表示坐标对本切片三个响应面全部无越线点；且该轴**无切片内可落地
  物**——防火墙（入口重物化）本身就是已落地的生产形态，「优化」
  已存在，反方向（撤掉拷贝、暴露热池给输入表示）是负优化兼类型
  收窄违例。轴一次性收口，非候选。
- **APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠）**
  ——本轮四次读数（1 预演 + 3 正典）：APCX 无计时器独立道 **60.2 /
  58.4 / 59.1 / 59.3 ms**（与 R29-C 带 58.4–60.6 交叠），FLOORX
  最廉价逐位合法形态地板 **31.7 / 39.5 / 34.0 / 32.7 ms**（与 R29-C
  带 32.9–41.3 交叠）⇒ **组合天花板 = 28.5 / 18.8 / 25.1 / 26.6 ms
  < 35**——正典三读数带 18.8–26.6 与 R29-C 带 18.3–27.2 全交叠；
  预演读数 28.5 为十五轮最高单读（此前最高 27.3），源于该次 FLOORX
  恰为最快地板（31.7），距 35 仍差 **6.5 ms**，正典带完全落回历史
  域，组合关闭维持。**sink=7.309 与 floorSink=7.309 八读数全同，且
  与 R14-C/R16-C..R29-C 打印值逐位同值**；每次运行内
  `Object.is(apcSink, floorSink)` 断言全绿（两道 65 遍扫掠累加双精度
  逐位相等）——派单强制的跨轮确定性旁证成立。

除上述轴与天花板重测外，第三十遍逐行重读**零新发现**：其余每一个
执行站点都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 /
结构交换负优化）+ R18-C 补充的 once-per-fault 拒绝类。生产中位与
地板复核见 §1/§3：本 VM 上生产中位 **658.7–663.0 ms/报告（四次运行
658.7 / 663.0 / 659.0 / 659.9；三次正典 659.0–663.0）**——与 R29-C
带 657.1–659.6 交叠（run 0/B 落带内，run C 高 0.3，run A 663.0 落
派单点名的 R28-C 历史带 660.9–667.5 内——派单明言该交叠属预期），
同 VM 独立旁证：r7c 回归资产锚点 **production 658.6 ms = 1.13× vs
冻结 S6-C ctl 742.1**（R29-C 读数 660.1/657.1 vs 740.2/739.1 =
1.12×，R28-C 同为 1.12×——比率 1.127 vs 1.12 逐轮基本不动），按
R14-C 处置规则随读重锚为本轮带 658.7–663.0；非切片回归或加速
（切片对 `183df9b` 零 diff）。小形状档整报告中位 **30.1–30.2 ms**
（R29-C：29.9–30.1——带缘相接），整条报告低于噪声带（R7-I 矩阵
义务履行）。表示轴 CANON 臂中位（661.3–664.3）与主锚带 658.7–663.0
交叠——双装具互证带与 R29-C（L 臂 652.7–655.7 vs 主锚 657.1–659.6）
同型，本身即装具项 ms 级的又一读数。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11，`writeAttributionPair` 两路调用本轮 rg 复核在位），live 面
零文件改动（`src/supervisor/` 对 9 文件导入为零，本轮 rg 复核）。仓库
变更仅本报告一个文件；无生产赢家故未提交 `/tmp/r30c/` 两个临时脚本
（全文进附录 A，sha256 归档后已删除）。

## 0. 范围与约束遵守

- 读序（强制）已履行：round-30/PLAN.md（C 段 + 全部 plan-ban 轴；
  独占 tip `1d1f783` 已含 R29-J CSPELL 与 Round 29 关轮）→
  EXCLUSIONS.md（完整表，含全部 S\*-C-\* 行与 S12-C-1、S13-B-1、
  X1-3/X3-4/X3-5/X4-2 posterior/propensity/bandit 行）→ R29-C.md
  全文（空枚举确认，无 S29-C-\*；PAIRX 轴定义 + 「输入值轴域穷尽 +
  执行基片坐标关闭 + 工作负载组成坐标关闭」裁决域勘定——本轮轴
  因此落在输入表示坐标上，即「同一抽象值以何种引擎表示进入切片」，
  历轮从未剂量）→ 9 个切片源文件全量重读 →
  `src/experiments/attribution-report.ts` 消费图现场核对 → 首次
  rebase 后补读 R30-A.md（call-stream ordering / dispatch-history
  face 轴 19 定义段 L329–373）与更新版 PLAN.md → 二次 rebase 后补读
  更新版 PLAN.md 的 R30-B / R30-D 轴名（scale-cube second-order
  interaction face / approval-profile constraint-list geometry
  face——三轴均已列入 plan-ban 段与 C 段禁移植名单）。
- 基线漂移检查：本轮测量期起点 = 分支创建点 = `1d1f783`；测量全程
  独占分支未动，提交前复核发现两次 docs-only 快进（`a93ab14` R30-A
  合入 + R30-D 派单；`1f41e30` R30-B + R30-D 合入 + R30-F 派单），按
  派单规程逐次 rebase 报告分支通过至 `1f41e30` 并重验冻结。冻结主张
  三次核验：`git diff 183df9b..<tip> --` 对 9 个切片文件为**0 行**
  ——分支创建时（`1d1f783`）、首次 rebase 后（`a93ab14`）、二次
  rebase 后（`1f41e30`）各一次；两次快进仅触 docs/reports/sota-opt/
  下文件（`git diff --stat` 逐次核对 `src/`、`test/` 零行），测量在
  字节相同的切片上完成，全部有效。R30-A / R30-B / R30-D 轴已入 §4
  非移植清单。
- 调用图确认（派单强制项，本轮 rg 复核）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 的 `writeAttributionPair`
  消费（双归因两路在位）；`src/supervisor/` 对 9 个切片文件的导入为
  **零**——live = R0 等价维持，R1 未接线，
  `src/supervisor/model-router.ts` 零字节未触。
- 表示轴对象勘定（本轮新轴的对象域）：`fitLogitAdditive` 对原始
  `rows` 参数的读取**仅有一处**——入口 `rows.map((r) => ({...}))`
  （四具名属性读 × n 行；本轮逐行核实 L420–425 之后 `rows` 零出现，
  退化检查 L428 读的已是 baseRows）；options 参数三读（L416–418，
  FITQ 域）。故输入表示敏感区被结构性封顶为一次 O(n) 具名读 map。
  六个表示变体全部保值：REVKEY（属性插入序整体倒转）、ROTKEY（插入
  序旋转）、DICT（先插 tmp 键后 delete 强制字典模式；delete 发生在
  进入生产函数之前，调用时可观察键集恒为规范五键）、CLASSI（class
  实例，构造器自有字段，独立 map 谱系）、FROZ（Object.freeze 规范序
  对象，冻结 map）、HOLEY（new Array(n) 逆序填满，HOLEY_ELEMENTS
  容器，长度与内容与规范数组逐项同一）。字符串引用与 y/occurredAtMs
  标量位从同一冻结夹具对象直接拷出（字符串身份坐标 IDG 与标量
  值类坐标零位移）；每变体行对象全新分配（引用/别名拓扑零位移）；
  内核对输入行**零枚举**（无 spread / Object.keys，仅具名读——
  枚举序不可观察，本轮 rg 复核），故六变体对内核语义逐位透明，
  位面断言是其见证。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S29-\*（含 S12-C-1，重开条件：无）
  未重提、未立道、未重测；R13-C..R29-C 二十九个无名关闭（PIVCSE/
  FINTAIL/REFZ/TSORT/DEG/SWORD/XF64/IRLSDP/CMPH/STORD/RIDGE/ICOL/
  SFILL/NSQRT/PMV/OSTZ/TAILG/REJX/FITQ/SCALEX/YMIX/SEEDX/ORDX/OCCX/
  AGEX/PRICEX/PROBX/基片实例化/**PAIRX**）未重探未补铸未换址重提
  ——**SCALEX 未重测、YMIX 未重测、SEEDX 未重测、ORDX 未重测、
  OCCX 未重测、AGEX 未重测、PRICEX 未重测、PROBX 未重测、基片轴
  未重测、PAIRX 未重测**（表示轴全部剂量点都在冻结 fixture、单一
  基片、单一内核之上：同一 0xbeef/0x51a1/0x5eed 构造字节、同一发射
  次序、同一 options、全臂 tsx+src、同一 Node 22.22.2、姊妹估计器
  永不调用——logit 摘要与 R28-C/R29-C 逐字节同值本身就是八输入维 +
  基片维 + 组成维恒等的位级见证；三臂只换边界操作数的表示族与喂饲
  时机，fixture 生成代码在 runner 内逐字节同一）；FITQ 重开条件未
  触发（options 全程默认值/文档化 PERF 值）；ITERX/COLDX/AAFLR/GCAX
  四轴未补铸（热层读数取 7-rep 中位，冷 JIT 尾巴被中位吸收；三臂
  协议同构，冷尾对差分自消）；嵌套序 × 布局组合空间（S4-C-2 +
  S5-C-1..4 + S12-C-1）未重开；CMB 组合未重堆叠（本轮 APCX/FLOORX
  重测系派单强制天花板复测，FLOORX 逐位合法性 59 名额外断言全绿）；
  PLSEG/PMV/TAILG 备忘未重提；config 坐标（halfLife/minSamples/
  priors/lcbKind + bandit/shadow config）维持 R27-C 诚实划界，零
  剂量。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动；X2-3 数值路径未触；单线程逐位审计基底不动；fail-open 族
  维持关闭；无 fsync / 完整性再哈希移除（本切片不涉盘，天然成立）。
- 环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0——回到
  R1-C..R29-C 的处理；VM 默认 22.14.0 位于 `/exec-daemon/node` 遮蔽
  PATH，已显式前置 nvm bin 修正，未用于任何读数）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`（`pnpm gate` 全绿，见 §5）。九个
  表示轴进程共享同一 Node 22.22.2 与同一 tsx+src 基片（引擎代与
  基片坐标双冻结——表示轴动的是边界操作数的引擎表示，不是引擎或
  管线）。

## 1. 生产中位与锚点复核（本 VM 实测，未拷贝历轮数字）

本轮测量装具为**复刻道保真电池 + 未仪表化生产导入直测**：主锚脚本
携带落地管线的逐语句复刻道（buildDesign / computeSupports /
canonicalRowKeys / irls / onProbabilitiesFor /
averagePredictiveComparison / percentile 全文照抄，与 R29-C 附录
逐语句同一，仅头注更新），其忠实性先经 **34 例形状电池 + perf +
small 全报告 vs 生产 `fitLogitAdditive` 导入逐位全绿**（Object.is 含
符号零/NaN，每运行 2,768 项检查）再采信；生产中位数与全部表示轴
读数均由**未仪表化生产导入**直接测得。**范围注记（诚实划界，继承
R28-C/R29-C）**：本轮未重建 R27-C 型逐池仪表化复刻管线（派单强制项
为生产中位 ×3 + APC 天花板重锚，均已履行）；池结构证据链为（a）切片
对 `183df9b` 零 diff ⇒ 池组成不可能移动，（b）站点计数在本 VM 本轮
由回归资产独立重印——r2c 打印 **3,586,400 行访问 / 8,966 次迭代 →
1,249,652 去重 dot**，r3c 打印 **累加消乘 177,843,236 → 0**，r1c
打印 **design.build 每拟合 358,006 次**——与 R10-C..R29-C 锚定值
逐位相同（跨轮确定性旁证）。

perf 夹具与 R10-C..R29-C 完全一致（fixtureRng(0xbeef)，4 场景 ×
6 模型 × 8 项目，rows=400, passRate=0.6，bootstrap=200,
seed=20260818 → p=60，201 拟合）；小形状档同一 0x51a1 夹具（3×3×3 /
120 行，bootstrap=200）。

本轮共四次全量运行：run 0 为**预演运行**（脚本与正典运行**逐字节
相同**——预演一次通过、预演后零改动，四次运行同一脚本，sha256 见
附录 A；本轮无任何废弃试跑），run A / run B / run C 为正典运行。
四次运行全部计入处置依据，不作剔除；每次运行 2,768 项逐位检查全绿。

生产整报告中位（每次运行内 7 测量取中位）：**658.7 / 663.0 /
659.0 / 659.6→659.9 ms**（正典 659.0–663.0）——run 0/B 落在 R29-C
带 657.1–659.6 内，run C（659.9）高出带顶 0.3 ms，run A（663.0）落
在派单点名的历史交叠带 660.9–667.5 内（派单明言「Historical overlap
with R28-C median 660.9–667.5 is expected」）；rep 级读数（单 rep 至
677.2）覆盖两带。同 VM 独立旁证 r7c 打印 **production 658.6 ms =
1.13× vs 冻结 S6-C ctl 742.1**（R29-C：660.1/657.1 vs 740.2/739.1 =
1.12×；R28-C：662.3/661.3 vs 737.9 = 1.12×——比率 1.127 vs 1.12
逐轮基本不动）⇒ 判为带内往返 + VM 时段抖动，按 R14-C 处置规则
**随读重锚为本轮带 658.7–663.0（正典 659.0–663.0）**；非切片回归或
加速（切片对 `183df9b` 零 diff）。

各运行 7-rep 明细：

```text
run 0: median 658.7  (reps 662.5 667.8 656.0 656.7 665.4 658.7 657.8; small 30.1)
run A: median 663.0  (reps 657.7 677.2 660.5 663.0 668.6 663.7 658.9; small 30.1)
run B: median 659.0  (reps 658.9 669.1 658.6 657.7 660.2 659.0 660.9; small 30.1)
run C: median 659.9  (reps 660.0 660.6 656.8 659.4 664.7 659.9 656.1; small 30.2)
```

小形状档整报告中位 **30.1–30.2 ms**（R29-C：29.9–30.1——带缘
相接）——小配置态整条报告低于噪声带，调用矩阵小态一侧整体收口
维持（R7-I 矩阵义务履行）。生产自确定性复核：3 seed × 各 2 次运行全
字段 Object.is 全绿（每运行含于 2,768 项）。表示轴 CANON 臂（同基片
异装具：单导入 runner 无复刻电池前置）中位 661.3–664.3 与主锚带
658.7–663.0 交叠——双装具互证带与 R29-C（L 臂 652.7–655.7 vs 主锚
657.1–659.6，间距 1.4–4.4）同型同阶，本身即「单机位 ms 级装具项」的
又一读数。

## 2. 第三十组枚举（诚实结果：空；零新站点 + 一条新轴 + 一次按规天花板重测）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过，且本轮从未点名站点数为零**（连续第十二遍）：

| 站点/轴 | 关闭方式 | 证据 |
| --- | --- | --- |
| 从未点名站点普查：**零**（连续第十二遍） | 逐行三问全覆盖 | 30 遍扫描把 9 文件每一个执行站点指到既有覆盖物：offline-logit 编排头（DEG/BASE）、irls 全站点（S2-C/S3-C/S6-C/S7-C 落地体 + S10-C-1/S9-C-3/S9-C-4/S6-C-5/6/S5-C-7/S4-C-5/6/MUCSE/SWORD/STORD/RIDGE/ICOL/IRLSDP + 收敛尾 S2-C-3/S8-C-3/NSQRT + ITERX/FITQ）、solve 全站点（COPY/S4-C-3/XF64 + PIVCSE/S5-C-6 + PIVSWAP + FZERO + S4-C/S5-C 落地体 + S12-C-1 + S6-C-7 + SFILL/FINTAIL + S8-C-4）、APC（S1-C-1/S2-C-2/S2-C-4/S4-C-4/S8-C-2/S10-C-2 + FLOORX 天花板）、on-prob（S7-C-4/S8-C-1）、采样环（SAMP/S3-C-3/S2-C-5）、抽取效应环（S7-C-1/DMAP）、尾部效应环（TSORT/CMPH/REFZ/TAILG）、posterior（X1-3/X3-4/S1-C-7/OSTZ/S1-C-4 + 落地 memo + AGEX 封顶）、prob-add（S1-C-6/PMV/PLSEG + PAIRX 共驻封顶）、propensity（S1-C-8/X3-5 + PROBX 四面封顶）、r1（X1-4/S1-C-10/S1-C-2/3 + PRICEX 封顶）、bandit/shadow（S1-C-9/X4-2 + R27-C 划界读数 ns 域）、r1-shadow-report（X1-5 + REJX + PRICEX selectedCost 封顶）——R18-C「TAILG 是尾部段最后一个从未点名位置」的判定经 R19-C..R29-C 与本遍连续十二轮核实成立；输入值轴域（R26-C/R27-C）+ 执行基片坐标（R28-C）+ 工作负载组成坐标（R29-C）关死后，本轮把定价域推进到**输入对象的引擎表示坐标**，**站点层零位移** |
| input object-representation face 的 C 区实例化（新轴，非候选；face 名归 R30-C，观察绰号 **REPRX**——plan-ban 用，不铸 ID、不写入排除表） | 位面 9/9 logit 摘要全等且与 R28-C/R29-C 摘要逐字节同值 + 六变体 perf 报告摘要 6/6 与规范逐位相等 + 热层三臂交错直测符号翻转深亚带 + 边界区结构封顶实测 µs 域 + 无切片内可落地物（防火墙即已落地形态） | **对象**：历轮全部 C 锚点把输入行喂成单一引擎表示（单隐藏类字面量 + packed 数组）；同一抽象值的表示族（插入序/字典模式/class 实例/冻结/holey 容器）从未剂量化。**结构定理**：`fitLogitAdditive` 入口重物化 baseRows（L420–425）后原始 `rows` 零读取（本轮逐行核实）⇒ 表示敏感区 = 一次 O(n) 具名读边界 map + 3 次 options 读，657 ms 热池被结构性防火墙隔离。**剂量设计**：三臂 {CANON=历史孤表示形态（对抗表示永不进入生产函数）, REP=生产边界 IC 对抗喂饲（每计时 rep 后 6 变体 small 形各一调用，计时器外，边界 map 打成 megamorphic）, PREREP=对抗先行（首个计时调用前 5 遍 × 6 变体）}，runner 逐字节同一（臂仅由 R30C_ARM 选择）、全臂单一基片 tsx+src、六变体保值钉死（字符串引用与标量位取自同一冻结夹具对象；行对象全新分配零别名；键集恒五键；内核对行零枚举——枚举序不可观察），交错 3 轮 × 3 臂 = 9 进程。**位面**：logit 六报告位型折叠 SHA-256 —— 9/9 摘要 `63e74db4…a893` 全同且**与 R28-C/R29-C 打印摘要逐字节同值**（跨轮 + 跨基片 + 跨组成 + 跨表示四重见证）；六变体 perf 报告摘要 6/6 同值 `55e6f81a…fb99` 且逐个与本进程规范 perf 报告相等；全部喂饲调用（每进程 30 前置或 42 交错）逐个摘要断言等于规范 small 报告——**表示剂量移不动任何输出位**。**热层**：CANON 663.5/664.3/661.3，REP 653.6/663.6/665.3，PREREP 662.6/663.8/662.8 ms；同轮差 REP−CANON −9.83/−0.72/+4.04、PREREP−CANON −0.83/−0.52/+1.52、PREREP−REP +9.00/+0.20/−2.52——**三对差全部符号翻转**，比率 0.985–1.006，离 ±35 ms 带 ≥3.5×；小形状差同样翻转（−0.36/−0.13/+0.85 与 +0.47/+0.23/+0.50 ms）= 纯抖动。**边界价**：生产入口 map 的逐语句复刻在 megamorphic 稳态（七种表示 × 2000 调用 × 2 遍轮转后第二遍读数）下 canon 7.39–8.14 / revkey 4.64–5.05 / rotkey 4.65–5.08 / dict 6.59–8.02 / classi 4.68–5.01 / froz 4.64–5.03 / holey 4.66–5.42 µs/调用（400 行；mapSink 9/9 = 11,200,000 确定性旁证；探针 runner 本地，生产 IC 零触碰，且列间序差属探针内部轮转伪影不作解读）⇒ **整个表示敏感区最坏 ≤ ~8.2 µs/报告，离带 ~4,300×**——防火墙定理实测确证。**喂饲价**：REP 交错腿 179.8–181.9 ms/rep（6 个 small 全报告，计时器外，属 small 形自有已定价池 30 ms/报告）；PREREP 前置 953.4–956.9 ms（30 调用含冷层）。**收口算术**：热层最大同轮差 9.83 ms 且为**负号**（对抗臂更快）——经典进程级抖动签名；越带需响应再放大 3.5× 且符号稳定，而三轮三对差全部翻转；即便未来响应越带也无切片内可落地物——入口重物化就是防火墙本身（已落地生产形态），撤除是反向负优化兼暴露热池，「加固」他文件边界属新功能非保行为优化。**判定**：历轮全部单表示 C 锚点对任意输入表示在噪声内原样有效；轴一次性收口。重开仅当（a）入口重物化被移除/重构使 `rows` 获得第二读点（切片契约事件，后果是重测而非候选）或（b）调用方开始以本轮六族之外的、能穿透具名读的奇异表示喂行（Proxy/getter 属输入契约违例，fail 域非性能域） |
| APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠） | 天花板仍亚带 + sink 逐位同值 + FLOORX 逐位合法性 59 名断言全绿 | APCX **60.2 / 58.4 / 59.1 / 59.3**（与 R29-C 带 58.4–60.6 交叠）；FLOORX **31.7 / 39.5 / 34.0 / 32.7**（与 R29-C 带 32.9–41.3 交叠）⇒ 天花板 **28.5 / 18.8 / 25.1 / 26.6 < 35**——正典带 18.8–26.6 与 R29-C 带 18.3–27.2 全交叠；预演读数 28.5 为十五轮最高单读（此前最高 R28-C 27.3），系该次 FLOORX 恰最快（31.7）所致，距 35 仍差 6.5 ms，按 R14-C 规则随读重锚为本轮带 18.8–28.5，组合关闭维持；**sink=7.309 与 floorSink=7.309 八读数全同，且与 R14-C/R16-C..R29-C 打印值逐位同值**，每运行内 `Object.is(apcSink, floorSink)`（65 遍扫掠累加双精度）全绿——派单强制的 APC 关闭维持条件成立；S8-C-2/S10-C-2/S2-C-4/S4-C-4 与 CMB 禁令全部维持原状 |

以上无一构成候选：从未点名普查结果为零（无新站点可立道）；表示轴
是执行坐标定价不是变换（三臂全部执行同一落地形态，零变体决斗——
其唯一潜在「候选族」= 动入口重物化，而重物化正是防火墙本体 + 类型
收窄（五字段行→四字段 Row）的已落地生产形态，撤除属负优化兼契约
违例）；APCX/FLOORX 是按规天花板测量不是变换。**故第三十组候选集
为空，不铸 S30-C-1 及后续编号**，遵循 R11-C..R29-C 无号关闭先例。

**枚举收口：** 除上表外，第三十遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖）。R27-C
「切片公开输入面上不存在从未定价的数值坐标」、R28-C「执行基片坐标
无越线点」与 R29-C「工作负载组成坐标无越线点」的裁决本轮零位移地
维持；本轮把定价域第一次扩到**输入对象的引擎表示坐标**（同一抽象
值的隐藏类/对象模式/容器种类），并证明该坐标对本切片三个响应面
（位/热/边界价）全部无越线点。仍不剂量化、不铸名的坐标（诚实划界，
继承 R27-C/R28-C/R29-C）：options 坐标（maxIter/bootstrap——契约
面）、config 坐标（halfLife/minSamples/priors/lcbKind + bandit/
shadow 的 seed/epsilon/explorationBudget/comparisonBudgetUsd/
comparisonCostUsd——config 面，R24-I 邻域禁移植域）、
`TaskFeatures.contextTokens/outputTokens`（探索面 ns 域划界封顶）、
字符串身份坐标（IDG 冻结，本轮变体共享夹具字符串引用恰是其恒等
见证）、引擎版本坐标（本轮九进程共享 Node 22.22.2）、执行基片坐标
（R28-C 关闭，本轮全臂冻结在 tsx+src 单点）、工作负载组成坐标
（R29-C 关闭，本轮姊妹估计器全臂永不调用）、**穿透性输入表示
（Proxy / accessor 属性 / 原型链投影行对象）**——属输入契约违例域
（具名数据读假设是切片输入契约的一部分），诚实记名不剂量。本轮
核实换名再提交拒列：零。

## 3. 三大池地板与组合出路复核

- **solve 池（本轮未逐池重仪表；结构证据 = 零 diff + 站点计数跨轮
  逐位）**：消元主体 ≈ 6.45 亿次冻结顺序浮点乘减/报告，8,966 次调用
  计数由 r2c 在本 VM 本轮重印逐位相同（r3c 另重印累加消乘
  177,843,236 → 0）。R6-C §2.5 七类分解 + S10-C-3 + S12-C-1（嵌套
  序，重开条件：无）+ R11-C COPY + R12-C FZERO/PIVSWAP + S5-C-6 +
  R13-C PIVCSE/FINTAIL + R14-C XF64 + R16-C SFILL 之后本轮零新位置。
  **表示维度对该池零触达**（位面见证：solve 输出经由报告位型 9/9
  摘要全等；热层同轮差 −9.8~+4.0 ms 若全记在 solve 池头上也不足带宽
  1/3.5，且符号翻转）。触碰地板本体的机制只剩 SIMD/线程（S9-C-1/2 +
  不变量禁止）与数值路径改道（X2-3 冻结）。
- **irls 自身池**：可省项全部已关闭（S2-C/S6-C/S7-C 落地本体之上：
  S10-C-1 决斗带内、S9-C-3 符号翻转、S6-C-6 上界 2–4、S5-C-7 无正
  决斗、S9-C-4、S4-C-5/6、S2-C-3、S8-C-3、R11-C RID/CNT/EXP/ALLOC、
  R12-C MUCSE、R14-C SWORD/IRLSDP、R15-C STORD/RIDGE、R16-C ICOL、
  R17-C NSQRT）。行访问/去重 dot 计数（3,586,400 / 1,249,652）本轮
  重印逐位。零新位置。
- **APC 池**：派单强制天花板重测照常执行（§2 表）：APCX 无计时器
  独立道 **58.4–60.2 ms**（**sink=7.309 与 R14-C..R29-C 逐位同值** =
  跨轮确定性旁证成立）；FLOORX 最廉价逐位合法形态地板 **31.7–39.5
  ms** ⇒ **组合天花板 = 18.8–28.5 ms（四次读数，正典 18.8–26.6）
  < 35**。十五轮天花板读数 12.1–23.8 / 13.8–21.6 / 12.1–20.9 /
  13.9–24.0 / 17.4–21.6 / 12.5–19.8 / 17.1–21.8 / 14.6–16.0 /
  15.9–22.7 / 20.8–25.6 / 19.5–22.9 / 15.7–21.0 / 17.3–27.3 /
  18.3–27.2 / **18.8–28.5** 稳定亚带（四读数全部 < 35 且正典带与
  R29-C 带交叠），组合关闭维持。
- **引擎侧三轴（R15-C/R16-C 收口，本轮未重测未补铸）**：COLDX 预热
  份额、GCAX 收集器份额、ALLOC 分配封顶——三轴均无切片内可受理
  变换；本轮全部热层读数为 7-rep 中位稳态口径（冷尾被中位吸收，
  三臂协议同构使冷尾对同轮差分自消）。**表示轴与 COLDX 正交**：
  COLDX 是同内核进程内预热份额；本轮剂量是边界操作数的表示族与
  喂饲时机，7-rep 中位协议全臂恒等。
- **拒绝面（R18-C REJX 轴收口，本轮未重测未补铸）**：31 终点
  once-per-fault 定价原样引用；四分类 + once-per-fault 拒绝类继续
  穷尽（本轮喂饲价读数按 small 形自有池 + once-per-feed 拒绝类
  记名）。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测带级摆幅带内），
  维持关闭。本轮探针无一是稳定越带正项（普查为零、表示轴是执行
  坐标定价、APCX/FLOORX 是天花板测量），无新成员可入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建
面开放 / X2-3 数值路径解冻 / 单线程审计基底变更 / maxIter=50、
tol=1e-8、bootstrap/seed 契约、rows/OutcomeObservation 输入契约、
X0-11 双归因契约变更——均不预期）；（b）现实规模位移触达 SCALEX
标定的越线点（今日生产流量在 perf 夹具当量及以下，未触达；八个
输入维度经 SCALEX/YMIX/SEEDX/ORDX/OCCX/AGEX/PRICEX/PROBX 证明均无
越线点，执行基片维度经 R28-C 证明热层比率 ~1.00，工作负载组成维度
经 R29-C 证明同轮差深亚带，**输入表示维度经本轮证明位面全等 + 热层
符号翻转 + 边界区结构封顶 µs 域**）；（c）入口重物化被重构使原始
rows 获得第二读点，或调用方以穿透具名读的奇异表示喂行（分别属切片
契约事件与输入契约违例，后果是重测/报错而非候选）；或（d）指出
「重命名 / 亚带 / 逐位违例 / 结构交换负优化 / once-per-fault 拒绝」
之外的第六类成本——三十遍枚举未再发现。

## 4. 重命名检查（派单强制项）

- **本轮表示轴 vs R30-A call-stream ordering / dispatch-history face
  （mid-run 合入，PLAN 点名禁移植——最近邻，首位核对）**：R30-A 的
  剂量坐标是**固定异质调用多重集的到达排列**（同一批调用换序：
  block / round-robin / shuffle——组成钉死、只动次序），响应是交织
  溢价。本轮的剂量坐标是**边界操作数表示族的组成**（规范独占 vs
  +6 外来表示曾否流经该站点）——被计时调用在所有臂是同一规范
  调用，**次序从未作为独立变量定价**：REP vs PREREP 的时机差是剂量
  时点对照（继承 R29-C PAIR/PRE 协议形状），其差值（PREREP−REP
  +9.00/+0.20/−2.52）符号翻转，本报告**不主张任何次序价**——纯
  次序定价留在 R30-A face 的 A 区本籍。机制层两轴同经引擎分派历史
  作用，坐标层正交（排列 vs 表示族组成）。非移植、非重铸。
- **表示轴 vs R30-B scale-cube second-order interaction face
  （mid-run 合入，PLAN 点名禁移植）**：R30-B 的剂量坐标是 B 区规模
  维立方的**二阶交互**（规模维两两联动的响应面几何）；本轮全部
  规模维钉死在冻结夹具点（perf 400 行 / small 120 行，历轮同点），
  零规模剂量、零交互剂量——C 区自有规模轴 SCALEX 未重测。非移植。
- **表示轴 vs R30-D approval-profile constraint-list geometry face
  （mid-run 合入，PLAN 点名禁移植）**：R30-D 的剂量对象是 D 区
  approval-profile 约束列表的几何；本切片 9 文件内不存在
  approval-profile 容器，零重叠。非移植。
- **表示轴 vs R29-A input reference-identity / aliasing-topology
  face（PLAN 点名禁移植）**：R29-A 的剂量坐标是**输入对象的引用
  同一性/别名拓扑本身**（共享 vs 等值异身份）；本轮全部变体行对象
  **全新分配**、别名拓扑在臂内臂间恒等且从未作为剂量变量——剂量
  的是同一抽象值的**引擎表示**（隐藏类/对象模式/容器种类），身份
  结构钉死。非移植、非重铸。
- **表示轴 vs R28-A input key-set geometry face（PLAN 点名禁移植）
  **：R28-A 剂量**键集几何**（哪些键存在）；本轮六变体调用时可
  观察键集恒为规范五键（DICT 的 tmp 键在进入生产函数前已 delete），
  零键增删——动的是同一键集的插入序/存储模式，且内核对行零枚举使
  枚举序语义不可观察（位面断言见证）。非移植。
- **表示轴 vs R27-A scalar numeric value-class face（PLAN 点名禁
  重铸）**：该面剂量标量值类；本轮 y/occurredAtMs 标量位从冻结夹具
  逐位拷出（SMI 域不动），零标量类剂量。非移植。
- **表示轴 vs R25-H consumer-stage string-representation census /
  IDG 冻结**：本轮全部变体**共享夹具字符串引用**——字符串身份与
  内部表示双双钉死（恰是 IDG 恒等的构造性见证），剂量对象是包裹
  字符串的**对象容器**。非移植、IDG 未重测。
- **表示轴 vs R29-C PAIRX（process workload-composition face 的 C
  区实例化，PLAN 点名禁补铸）**：PAIRX 剂量**姊妹内核**（prob-add）
  在进程内的执行有无与次序（异质内核共驻）；本轮九进程姊妹估计器
  **永不调用**（模块图冻结但 fitProbabilityAdditive 零调用），全部
  执行同一内核 fitLogitAdditive——剂量的是该内核边界站点的操作数
  表示族。喂饲腿是同内核 small 形调用（其全报告价 30 ms 属已定价
  small 池，计时器外）；REP/PREREP 臂的（外来表示 × 附加同内核
  调用）联合剂量以联合上界收口（符号翻转深亚带），不拆分主张。
  非重铸。
- **表示轴 vs R28-C 基片实例化 / R27-H execution-substrate face
  （PLAN 点名禁移植/禁另起绰号）**：基片轴剂量解释器 + 模块管线
  （tsx/node × src/dist）；本轮九进程钉死单一基片（tsx+src），
  logitDigest 与 R28-C 打印摘要逐字节同值恰是基片维恒等的证词。
  非移植。
- **表示轴 vs S4-E-3（baseSignal 隐藏类单态化，E 区已拒变换）/
  S9-D-1（parseEpisode 输出形状单态化，D 区已拒变换）/ S8-E-2 PIC
  观察项**：三者是**他区生产者站点的变换候选**（改代码换收益）；
  本轮是 C 区输入边界的**定价轴**（零变换、零变体决斗），且结论
  恰为「无可落地物——防火墙已是落地形态」。非移植、非重开。
- **表示轴 vs FITQ（options 坐标）**：options 三读属 FITQ 已划界
  契约面，本轮 options 全程 PERF 文档值零剂量。非重测。
- **APCX/FLOORX 重测 vs S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 / CMB
  禁令**：本轮重测系**派单强制天花板复测**，产出仅用于天花板差值
  算术与 sink 逐位核对；FLOORX 道的 59 名逐位合法性断言是对既有
  S8-C-2 引理的复核不是新道；不重跑任何已关 ID 决斗、不构建堆叠
  候选、不产出可落地形态。
- **换名再提交拒列（本轮核实）**：零。R13-C..R29-C 二十九个无名
  关闭未重探、未补铸、未换址重提；S10-C-1..3 / S12-C-1 / RID / CNT
  零重提；X2-1 重写维持排除（S7-C 的 0/1 形态即落地形态）；COTARG /
  VALCLASS / CALLB / SHAPEK / KFAN / JB / RUNLIM / CKMIX / TERMCLASS /
  SCHEDWIN / XPROC / NAMESHAPE / CG / RTL / DELINV / RVL / SIDEC /
  PWL / ZREP / PAIRX / CSPELL 与全部他区无名轴（M=1 catalog
  collapse、approval-authority、state serialization-boundary
  provenance、operand-residency、adaptation-state、
  identity-coordinate、plan-header identity-geometry、injection
  command-kind、consumer-stage string-representation /
  operand-realm、injection-seam default-arm、input storage-state、
  edge condition-kind、argv surface-form、process-environment、
  collation-locale、value-domain / string content-class、scalar
  numeric value-class、batch-cardinality bottom + prelude
  intercept、policy-content tree-geometry、input access-path
  mechanics、join-policy、process output-channel disposition、
  catalog-face row-arrangement (permutation)、input key-set
  geometry、contents-store population、boundary call-protocol、
  proposal write-path termination-leg、decision-gate resolution、
  boundary argument-slot materialization、invocation-identity /
  path-referent spelling × cwd、durable-store zero-state
  provenance、learned-policy container、input reference-identity /
  aliasing-topology、process workload-composition、wall-clock
  sampling topology / timestamp-provenance、registry-record
  string-list payload、boundary output-retention / superseded-
  snapshot liveness、M2 supervised-engine adjudication lattice、
  coverage-relation incidence topology、input-file JSON
  serialization-form、durable-artifact content-spelling、
  call-stream ordering / dispatch-history、scale-cube second-order
  interaction、approval-profile constraint-list geometry）零移植。

## 5. 测试与验证

环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0）、pnpm 10.17.1、
`pnpm install --frozen-lockfile`。生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录 A）
npx tsx /tmp/r30c/r30c-anchor.mts        # ✓ 2,768 项逐位 × 4 次运行全绿
                                         #   （34 例复刻道保真电池 + perf + small
                                         #    + 3 seed × 2 自确定性
                                         #    + FLOORX 59 名逐位合法性
                                         #    + sink===floorSink 双精度断言
                                         #    + APCX/FLOORX 天花板重测内嵌）
# 表示轴（runner 逐字节同一，臂由 R30C_ARM 选择；全臂 tsx+src；交错 3 轮）
R30C_ARM=CANON  npx tsx /tmp/r30c/r30c-repr-runner.mjs
R30C_ARM=REP    npx tsx /tmp/r30c/r30c-repr-runner.mjs
R30C_ARM=PREREP npx tsx /tmp/r30c/r30c-repr-runner.mjs
# ✓ logitDigest 9/9 = 63e74db4ac45eed0f2a219e159b5f2d65b9ea8d962408e66858c7b459dea3893
#   （与 R28-C/R29-C 打印摘要逐字节同值）
# ✓ reprDigest 6/6  = 55e6f81ad8232f83cc22ce8de7e69f180fa8cea3e72d6c6d038ca030f1abfb99
#   （六变体 perf 报告逐个与规范 perf 报告摘要相等；全部喂饲调用
#    逐个摘要断言等于规范 small 报告——每进程内断言，违例即退出）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项（design.build 358,006/拟合）
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项（3,586,400 → 1,249,652 @ 8,966 迭代）
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项（累加消乘 177,843,236 → 0）
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 658.6 ms（1.13× vs 冻结 S6-C ctl 742.1）——与
# §1 生产正典中位带（659.0–663.0）互证，比率与 R28-C/R29-C（1.12×）
# 逐轮基本不动（1.127 vs 1.12）。
```

代表性一次测量彩排全量输出（正典 run B；run 0/A/C 数字见 §1/§2）：

```text
scenario 1 (replica-lane fidelity battery, 34 cases + perf + small, bitwise vs production import): OK
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is): OK
  shape small (3 models x 3 projects, 120 rows): production median 30.1 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 659.0 ms/report (7 reps)
  perf reps: 658.9 669.1 658.6 657.7 660.2 659.0 660.9
probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) 0.294 ms/fit -> x201 fits = 59.1 ms/report (base-shape approximation; sink=7.309)
probe FLOORX: cheapest bitwise-legal sweep 0.169 ms/fit -> x201 fits = 34.0 ms/report floor; APC combination ceiling on THIS VM = 25.1 ms (floorSink=7.309)

ALL EQUIVALENCE CHECKS PASSED (2768 bitwise checks)
```

表示轴九进程原始读数（交错序：每轮 CANON→REP→PREREP；logitDigest
九行全同、reprDigest 六行全同故各列一次；mapSink 9/9 = 11,200,000）：

```text
logitDigest (all 9): 63e74db4ac45eed0f2a219e159b5f2d65b9ea8d962408e66858c7b459dea3893  (== R28-C/R29-C)
reprDigest (6 co-resident): 55e6f81ad8232f83cc22ce8de7e69f180fa8cea3e72d6c6d038ca030f1abfb99
round 1: CANON  perf 663.46 (reps 667.9 657.9 656.4 683.3 663.1 663.7 663.5; small 29.91)
         REP    perf 653.63 (reps 668.3 658.5 651.9 652.8 652.2 653.6 655.1; small 29.55; feed/rep 179.77)
         PREREP perf 662.63 (reps 659.6 662.2 662.6 664.1 665.8 685.0 660.3; small 30.38; preFeed 956.7)
round 2: CANON 664.27 (small 30.02) / REP 663.55 (small 29.90; feed 181.86) / PREREP 663.75 (small 30.26; preFeed 956.9)
round 3: CANON 661.26 (small 29.83) / REP 665.30 (small 30.67; feed 180.52) / PREREP 662.78 (small 30.33; preFeed 953.4)
同轮差: REP−CANON −9.83/−0.72/+4.04 ms; PREREP−CANON −0.83/−0.52/+1.52; PREREP−REP +9.00/+0.20/−2.52
比率:   REP/CANON 0.9852/0.9989/1.0061; PREREP/CANON 0.9988/0.9992/1.0023 — 全部符号翻转深亚带（≥3.5× 低于 ±35 ms）
边界价 (megamorphic 稳态, µs/调用@400行): canon 7.39–8.14; revkey 4.64–5.05; rotkey 4.65–5.08;
        dict 6.59–8.02; classi 4.68–5.01; froz 4.64–5.03; holey 4.66–5.42 — 表示敏感区 ≤ ~8.2 µs/报告
```

## 6. 新增排除 ID：无

第三十组候选集为空。从未点名站点普查结果为零（连续第十二遍；
R18-C「最后一个从未点名位置」判定经 R19-C..R29-C 与本轮十二遍
核实）。input object-representation face 的 C 区实例化为执行坐标
定价轴（重开条件：入口重物化被重构使原始 rows 获得第二读点——
切片契约事件，后果是重测而非候选；或调用方以穿透具名读的奇异表示
喂行——输入契约违例，属 fail 域。表示维度本身**无越线点**：位面
9/9 摘要全等且与 R28-C/R29-C 逐字节同值、六变体报告 6/6 逐位等于
规范、热层同轮差 −9.8~+4.0 ms 三轮符号翻转且离带 ≥3.5×、边界区
结构封顶实测 ≤ ~8.2 µs/报告离带 ~4,300×；不铸 ID、不写入排除表，
face 名归 R30-C，观察绰号 REPRX 仅 plan-ban 用）。APCX/FLOORX 为
按规天花板重测（天花板 18.8–28.5 < 35 稳定亚带，正典带 18.8–26.6
与 R29-C 全交叠，sink=floorSink=7.309 八读数逐位同值）。生产中位
随读重锚 658.7–663.0 ms（正典 659.0–663.0），与 R29-C 带 657.1–
659.6 及派单点名历史带 660.9–667.5 双向交叠，r7c 比率 1.13× 与
历轮 1.12× 基本不动。

## 附录 A：测量彩排脚本（完整，可复现；归档后临时文件已删除）

- `/tmp/r30c/r30c-anchor.mts` — sha256
  `030e371329be3405e40549a3662a051b298eb967e8a33f7283239c5135e4281c`
  （与 R29-C 主锚逐语句同协议，仅头注更新为 R30-C；四次运行——
  1 预演 + 3 正典——逐字节同一脚本，预演一次通过零改动，本轮无
  任何废弃试跑）
- `/tmp/r30c/r30c-repr-runner.mjs` — sha256
  `44262e401cde5a2fcbb02ff5fa723bc93ad103ac8065345526e0bb0611b3561d`
  （九进程逐字节同一；位型折叠函数与 logit 报告清单与 R28-C 基片
  runner / R29-C 共驻 runner 逐语句同一，使 logitDigest 与两轮打印
  摘要可直接比对）
- 无其他临时文件；原始读数已全文誊入 §5。

### 主锚 `/tmp/r30c/r30c-anchor.mts`

```ts
/**
 * Round 30 / R30-C measurement re-anchor + thirtieth-group closure probes
 * (offline routing slice, thirtieth pass). NOT committed — the thirtieth-
 * group enumeration carries no candidate lanes that reach the acceptance
 * bar; full source goes into the R30-C report appendix.
 *
 * Protocol is verbatim R29-C (which is verbatim R14-C lineage):
 *  - replica-lane fidelity battery: a verbatim copy of the landed
 *    fitLogitAdditive pipeline (buildDesign / computeSupports /
 *    canonicalRowKeys / irls / onProbabilitiesFor /
 *    averagePredictiveComparison / percentile) compared bitwise
 *    (Object.is, includes signed zero / NaN) against the production import
 *    on a 34-case shape battery + the perf fixture + the small fixture.
 *  - production median re-anchor: perf shape (4 scenarios x 6 models x
 *    8 projects, rows=400, passRate=0.6, bootstrap=200, seed=20260818)
 *    7 reps -> median; small shape (3 x 3 x 3, 120 rows) 3 reps -> median.
 *  - production self-determinism: 3 seeds x 2 runs, all report fields
 *    Object.is.
 *  - APCX probe (dispatch-mandated ceiling re-measure): standalone
 *    timer-free APC sweep (59 non-intercept names x 400 rows, base-fit
 *    coefficients) per-fit cost x 201 fits; sink = sum of all APC values
 *    (cross-round determinism witness, expected print 7.309).
 *  - FLOORX probe: cheapest bitwise-legal sweep (S8-C-2 support-ascending
 *    off-sum form, never landed — X2-1 lemma premises hold: 0/1 design,
 *    +0.0 start, finite beta), per-name values asserted Object.is against
 *    the APCX sweep, then per-fit cost x 201 fits; combination ceiling =
 *    APCX - FLOORX must stay < 35 ms with floorSink === sink bitwise.
 *
 * The input object-representation axis arms run out of the byte-identical
 * runner r30c-repr-runner.mjs (see report appendix), not out of this file.
 */
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.ts";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.ts";
import { solveSymmetric } from "/workspace/src/routing/lin-alg.ts";
import type { AttributionEffect, AttributionLabel, AttributionReport } from "/workspace/src/routing/offline-types.ts";

let checks = 0;
function assertSame(label: string, a: unknown, b: unknown): void {
  checks += 1;
  if (!Object.is(a, b)) {
    throw new Error(`MISMATCH ${label}: ${String(a)} !== ${String(b)}`);
  }
}
function assertTrue(label: string, v: boolean): void {
  checks += 1;
  if (!v) throw new Error(`ASSERT ${label}`);
}

/* ------------- replica lane: verbatim copy of the landed pipeline ------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}
interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}
function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}
function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);
  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);
  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);
  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });
  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}
interface FitResult {
  readonly coefficients: readonly number[] | null;
}
function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}
function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}
function irls(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
): FitResult {
  const p = design.names.length;
  const n = rows.length;
  const eta = new Array<number>(n).fill(0);
  const mu = new Array<number>(n).fill(0);
  const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
  const xtwz: number[] = new Array<number>(p).fill(0);
  const stamp = new Int32Array(keySpace);
  const etaByKey = new Float64Array(keySpace);
  const muByKey = new Float64Array(keySpace);
  let beta = new Array<number>(p).fill(0);
  for (let iter = 0; iter < maxIter; iter++) {
    const mark = iter + 1;
    for (let i = 0; i < n; i++) {
      const key = keys[i]!;
      if (stamp[key] !== mark) {
        stamp[key] = mark;
        let value = 0;
        const active = supports[i]!;
        for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
        etaByKey[key] = value;
        muByKey[key] = sigmoid(value);
      }
      eta[i] = etaByKey[key]!;
      mu[i] = muByKey[key]!;
    }
    for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
    xtwz.fill(0);
    for (let i = 0; i < n; i++) {
      const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
      const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
      const wz = w * z;
      const active = supports[i]!;
      switch (active.length) {
        case 5: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          const a4 = active[4]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
          xtwz[a4] = xtwz[a4]! + wz;
          const r4 = xtwx[a4]!;
          r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
          break;
        }
        case 4: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
          break;
        }
        case 3: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
          break;
        }
        case 2: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
          break;
        }
        default: {
          for (let ai = 0; ai < active.length; ai++) {
            const a = active[ai]!;
            xtwz[a] = xtwz[a]! + wz;
            const rowA = xtwx[a]!;
            for (let bi = 0; bi < active.length; bi++) {
              const b = active[bi]!;
              rowA[b] = rowA[b]! + w;
            }
          }
        }
      }
    }
    for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
    const next = solveSymmetric(xtwx, xtwz);
    if (next === null) return { coefficients: null };
    const delta = next.map((value, index) => value - beta[index]!);
    const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
    beta = next;
    if (!beta.every(Number.isFinite)) return { coefficients: null };
    if (l2 < TOL) break;
  }
  return { coefficients: beta };
}
function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}
function onProbabilitiesFor(
  vectors: readonly number[][],
  coefficients: readonly number[]
): number[] {
  return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
}
function averagePredictiveComparison(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  coefficients: readonly number[],
  onProbabilities: readonly number[],
  column: string
): number {
  let sum = 0;
  const columnIdx = design.columnIndex.get(column);
  if (columnIdx !== undefined && columnIdx !== 0) {
    for (let i = 0; i < rows.length; i++) {
      if (vectors[i]![columnIdx] === 0) continue;
      const on = onProbabilities[i]!;
      const offVector = vectors[i]!.slice();
      offVector[columnIdx] = 0;
      const off = sigmoid(dot(coefficients, offVector));
      sum += on - off;
    }
  }
  return rows.length === 0 ? 0 : sum / rows.length;
}
function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}
type OfflineRowInput = {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
  readonly occurredAtMs: number;
};
function fitLogitAdditiveReplica(
  rows: readonly OfflineRowInput[],
  options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
): AttributionReport {
  const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
  const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
  const seed = options?.seed ?? SEED_DEFAULT;
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const effects: AttributionEffect[] = [];
  if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
  }
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irls(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
  if (fit.coefficients === null) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
  }
  const onProbabilities = onProbabilitiesFor(vectors, fit.coefficients);
  const pointEffects = new Map<string, number>();
  for (const name of design.names) {
    if (name === "intercept") continue;
    pointEffects.set(
      name,
      averagePredictiveComparison(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
    );
  }
  for (const ref of design.referenceLevels) {
    if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
      pointEffects.set(`${ref.factor}:${ref.name}`, 0);
    }
  }
  const random = rng(seed);
  const draws = new Map<string, number[]>();
  let successful = 0;
  for (let draw = 0; draw < bootstrapDraws; draw++) {
    const sample: Row[] = [];
    const sampleVectors: number[][] = [];
    const sampleSupports: number[][] = [];
    const sampleKeys: number[] = [];
    for (let i = 0; i < baseRows.length; i++) {
      const index = Math.floor(random() * baseRows.length);
      sample.push(baseRows[index]!);
      sampleVectors.push(vectors[index]!);
      sampleSupports.push(supports[index]!);
      sampleKeys.push(keys[index]!);
    }
    if (sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1)) continue;
    const bootFit = irls(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
    if (bootFit.coefficients === null) continue;
    successful += 1;
    const sampleOnProbabilities = onProbabilitiesFor(sampleVectors, bootFit.coefficients);
    for (const [name] of pointEffects.entries()) {
      const value = averagePredictiveComparison(
        design,
        sample,
        sampleVectors,
        bootFit.coefficients,
        sampleOnProbabilities,
        name
      );
      pushValue(draws, name, value);
    }
  }
  for (const [name, point] of pointEffects.entries()) {
    const values = draws.get(name) ?? [];
    if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
    }
    values.sort((a, b) => a - b);
    effects.push({
      name,
      point,
      lcb: percentile(values, 0.025),
      ucb: percentile(values, 0.975)
    });
  }
  const n = baseRows.length;
  const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
  const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
  const muLcb = betaQuantileLcb(muPosterior, 0.05);
  const models = new Set(baseRows.map((r) => r.modelVersion)).size;
  const projects = new Set(baseRows.map((r) => r.projectId)).size;
  const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
  const containsZero = (e: AttributionEffect): boolean =>
    e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
  const interactionsFor = (prefix: string): AttributionEffect[] =>
    effects.filter((e) => e.name.startsWith(`w:${prefix}`));
  let diagnosis: AttributionLabel = "uncertain";
  const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
  if (
    effects.some(
      (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
    )
  ) {
    diagnosis = "model-problem";
  } else if (
    effects.some(
      (e) =>
        e.name.startsWith("v:") &&
        e.lcb < -ATTRIBUTION_EFFECT &&
        effects
          .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
          .every(containsZero)
    )
  ) {
    diagnosis = "project-problem";
  } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
    diagnosis = "interaction-only";
  } else if (scenarioHard) {
    diagnosis = "scenario-hard";
  }
  return {
    estimator: "logit-additive",
    rowsUsed: baseRows.length,
    effects,
    diagnosis,
    reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
    writesActivePointer: false
  };
}
function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}
function pushValue(map: Map<string, number[]>, key: string, value: number): void {
  const list = map.get(key);
  if (list === undefined) map.set(key, [value]);
  else list.push(value);
}
function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}
function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRowInput[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRowInput[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
function perfRows(): OfflineRowInput[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
}
// Same small-shape fixture as R10-C..R29-C (seed 0x51a1, 3 x 3 x 3 / 120).
function smallRows(): OfflineRowInput[] {
  const r = fixtureRng(0x51a1);
  return randomRows(r, { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
}
const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

/* ------------------------------ comparisons ------------------------------ */

function compareReports(label: string, a: AttributionReport, b: AttributionReport): void {
  assertSame(`${label}.estimator`, a.estimator, b.estimator);
  assertSame(`${label}.rowsUsed`, a.rowsUsed, b.rowsUsed);
  assertSame(`${label}.diagnosis`, a.diagnosis, b.diagnosis);
  assertSame(`${label}.reason`, a.reason, b.reason);
  assertSame(`${label}.writesActivePointer`, a.writesActivePointer, b.writesActivePointer);
  assertSame(`${label}.effects.length`, a.effects.length, b.effects.length);
  for (let i = 0; i < a.effects.length; i++) {
    const ea = a.effects[i]!;
    const eb = b.effects[i]!;
    assertSame(`${label}.effects[${i}].name`, ea.name, eb.name);
    assertSame(`${label}.effects[${i}].point`, ea.point, eb.point);
    assertSame(`${label}.effects[${i}].lcb`, ea.lcb, eb.lcb);
    assertSame(`${label}.effects[${i}].ucb`, ea.ucb, eb.ucb);
  }
}

function batteryCases(): Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];
  for (let i = 0; i < 24; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }) });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 60, passRate: 0.55, pipeInModel: true }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  return cases;
}

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------------------------------- main --------------------------------- */

function main(): void {
  // 1. Replica fidelity battery (replica lane vs production import, bitwise).
  const cases = batteryCases();
  for (let i = 0; i < cases.length; i++) {
    const c = cases[i]!;
    compareReports(`battery[${i}]`, fitLogitAdditive(c.rows, c.options), fitLogitAdditiveReplica(c.rows, c.options));
  }
  compareReports("perf-fixture", fitLogitAdditive(perfRows(), PERF_OPTIONS), fitLogitAdditiveReplica(perfRows(), PERF_OPTIONS));
  compareReports("small-fixture", fitLogitAdditive(smallRows(), PERF_OPTIONS), fitLogitAdditiveReplica(smallRows(), PERF_OPTIONS));
  console.log(`scenario 1 (replica-lane fidelity battery, ${cases.length} cases + perf + small, bitwise vs production import): OK`);

  // 2. Production self-determinism: 3 seeds x 2 runs.
  for (const seed of [SEED_DEFAULT, 7_777, 424_242]) {
    const rows = perfRows();
    compareReports(`determinism.seed=${seed}`, fitLogitAdditive(rows, { bootstrap: 200, seed }), fitLogitAdditive(rows, { bootstrap: 200, seed }));
  }
  console.log("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is): OK");

  // 3. Production median re-anchor (perf 7 reps, small 3 reps).
  const prodPerf: number[] = [];
  {
    const rows = perfRows();
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      prodPerf.push(performance.now() - t0);
    }
  }
  const prodSmall: number[] = [];
  {
    const rows = smallRows();
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      prodSmall.push(performance.now() - t0);
    }
  }
  console.log(`  shape small (3 models x 3 projects, 120 rows): production median ${median(prodSmall).toFixed(1)} ms/report (3 reps)`);
  console.log(`  shape perf (6 models x 8 projects, 400 rows): production median ${median(prodPerf).toFixed(1)} ms/report (7 reps)`);
  console.log(`  perf reps: ${prodPerf.map((v) => v.toFixed(1)).join(" ")}`);

  // 4. APCX / FLOORX ceiling re-measure (dispatch-mandated).
  const rows = perfRows();
  const baseRows: Row[] = rows.map((r) => ({ scenarioId: r.scenarioId, modelVersion: r.modelVersion, projectId: r.projectId, y: r.y }));
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irls(design, baseRows, vectors, supports, keys, baseRows.length, MAX_ITER_DEFAULT);
  assertTrue("apcx.base-fit-converged", fit.coefficients !== null);
  const coefficients = fit.coefficients!;
  const onProbabilities = onProbabilitiesFor(vectors, coefficients);
  const sweepNames = design.names.filter((name) => name !== "intercept");

  // FLOORX lane bitwise-legality proof, per name (S8-C-2 form; X2-1 lemma
  // premises hold: 0/1 entries, +0.0 start, finite beta; ascending support
  // order = the full dot's nonzero visit order). Outside the sink accumulator.
  for (const name of sweepNames) {
    let sum = 0;
    const columnIdx = design.columnIndex.get(name);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < baseRows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        const active = supports[i]!;
        let value = 0;
        for (let ai = 0; ai < active.length; ai++) {
          const j = active[ai]!;
          if (j === columnIdx) continue;
          value += coefficients[j]!;
        }
        sum += onProbabilities[i]! - sigmoid(value);
      }
    }
    const floorValue = baseRows.length === 0 ? 0 : sum / baseRows.length;
    assertSame(
      `floorx.bitwise[${name}]`,
      averagePredictiveComparison(design, baseRows, vectors, coefficients, onProbabilities, name),
      floorValue
    );
  }
  // Verbatim R14-C-lineage protocol: 5 warmup sweeps + 60 timed sweeps per
  // lane, sink accumulated across all 65 (expected cross-round print 7.309).
  const names = sweepNames;
  let apcSink = 0;
  const sweepReps = 60;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tSweep0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perSweepMs = (performance.now() - tSweep0) / sweepReps;
  console.log(
    `probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) ` +
      `${perSweepMs.toFixed(3)} ms/fit -> x201 fits = ${(perSweepMs * 201).toFixed(1)} ms/report ` +
      `(base-shape approximation; sink=${apcSink.toFixed(3)})`
  );

  let floorSink = 0;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tFloor0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perFloorMs = (performance.now() - tFloor0) / sweepReps;
  console.log(
    `probe FLOORX: cheapest bitwise-legal sweep ${perFloorMs.toFixed(3)} ms/fit -> x201 fits = ` +
      `${(perFloorMs * 201).toFixed(1)} ms/report floor; APC combination ceiling on THIS VM = ` +
      `${(perSweepMs * 201 - perFloorMs * 201).toFixed(1)} ms (floorSink=${floorSink.toFixed(3)})`
  );
  assertSame("apcx.sink===floorSink", apcSink, floorSink);
  assertTrue("ceiling<35", perSweepMs * 201 - perFloorMs * 201 < 35);

  console.log("");
  console.log(`ALL EQUIVALENCE CHECKS PASSED (${checks} bitwise checks)`);
}

main();
```

### 表示轴 runner `/tmp/r30c/r30c-repr-runner.mjs`

```js
/**
 * R30-C input object-representation axis runner (input object-representation
 * face, C-slice instantiation). Plain-JS on purpose: THIS FILE IS
 * BYTE-IDENTICAL ACROSS ALL ARMS. The arm is selected entirely by R30C_ARM;
 * every arm runs on the single canonical measurement substrate
 * (tsx + /workspace/src, the R28-C S arm — the substrate coordinate stays
 * frozen). NOT committed; full source goes into the R30-C report appendix.
 *
 * Coordinate being dosed: the ENGINE-LEVEL REPRESENTATION of the input row
 * objects and their container — hidden-class insertion order, dictionary
 * mode, class-instance maps, frozen maps, holey vs packed array kind. Every
 * abstract input VALUE is pinned bit-for-bit: variant rows copy field values
 * from the one canonical fixture (string references shared -> the string
 * identity coordinate IDG stays frozen; y / occurredAtMs scalar bits copied
 * -> the R27-A scalar value-class coordinate stays frozen in its A-slice
 * home), every variant allocates FRESH row objects (the R29-A
 * reference-identity / aliasing-topology face is never dosed), and the key
 * SET {scenarioId, modelVersion, projectId, y, occurredAtMs} never changes
 * (the R28-A input key-set geometry face is never dosed). Only the
 * representation of the same abstract value moves.
 *
 * Production relevance: fitLogitAdditive re-materializes its working set at
 * entry (baseRows = rows.map(...) — offline-logit.ts lines 420-425) and
 * never reads the raw `rows` again, so the 657 ms hot pool is structurally
 * firewalled behind one O(n) boundary map. The axis doses exactly that
 * firewall: (a) do adversarial representations move any output bit?
 * (b) does boundary-site megamorphism move the production median?
 * (c) what does the entire representation-sensitive region cost under the
 * worst dose?
 *
 * Arms (dose points of the representation coordinate):
 *   CANON  : canonical representation only — the historical measurement form
 *            (single hidden class, packed literal array; no variant object
 *            ever enters the production function).
 *   REP    : representation-adversarial interleaving — between timed logit
 *            reps, the SMALL fixture in all six adversarial representations
 *            is fed through fitLogitAdditive (timer outside), driving the
 *            production boundary-map ICs megamorphic while the timed object
 *            stays the canonical frozen perf fixture. Also: one full
 *            perf-shape call per variant, digest-asserted bit-equal to the
 *            canonical perf report.
 *   PREREP : adversarial-first — 5 sweeps x 6 variants of the small shape
 *            run BEFORE the first logit call (boundary ICs tier up
 *            megamorphic first), then the CANON protocol.
 *
 * Emits one JSON line: hot-tier medians (perf 7 reps, small 3 reps), all
 * reps, feed prices, the boundary-map worst-case price per variant, and two
 * SHA-256 digests over IEEE-754 bit patterns + strings: logitDigest
 * (identical payload and fold as the R28-C substrate runner / R29-C
 * co-residency runner, so equality across arms AND against R28-C/R29-C's
 * printed digest is a single string comparison) and reprDigest (canonical
 * perf report digest that every adversarial-representation perf report must
 * equal bit-for-bit; null in arm CANON, which never executes a variant).
 */
import { createHash } from "node:crypto";
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.ts";

const arm = process.env.R30C_ARM ?? "?";
if (arm !== "CANON" && arm !== "REP" && arm !== "PREREP") {
  console.error(`R30C_ARM must be CANON | REP | PREREP, got ${arm}`);
  process.exit(1);
}

function fixtureRng(seed) {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick(r, items) {
  return items[Math.floor(r() * items.length)];
}
function randomRows(r, options) {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) => `model-${i}`);
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
const perfRows = () => randomRows(fixtureRng(0xbeef), { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
const smallRows = () => randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
const PERF_OPTIONS = { bootstrap: 200, seed: 20260818 };

/* ---------------- representation variants (equal abstract value) ----------------
 * Each variant derives FRESH row objects from one canonical fixture, sharing
 * its string references and scalar bits. Only the engine-level representation
 * of object and container moves. */

// V1 REVKEY: reversed property insertion order (distinct hidden-class chain).
function revkeyRow(r) {
  return { occurredAtMs: r.occurredAtMs, y: r.y, projectId: r.projectId, modelVersion: r.modelVersion, scenarioId: r.scenarioId };
}
// V2 ROTKEY: rotated insertion order (a third hidden-class chain).
function rotkeyRow(r) {
  return { modelVersion: r.modelVersion, projectId: r.projectId, y: r.y, occurredAtMs: r.occurredAtMs, scenarioId: r.scenarioId };
}
// V3 DICT: dictionary-mode object (delete of a non-last property).
function dictRow(r) {
  const o = { tmp: 1, scenarioId: r.scenarioId, modelVersion: r.modelVersion, projectId: r.projectId, y: r.y, occurredAtMs: r.occurredAtMs };
  delete o.tmp;
  return o;
}
// V4 CLASSI: class instance (constructor-created own fields, distinct map lineage).
class RowInstance {
  constructor(r) {
    this.scenarioId = r.scenarioId;
    this.modelVersion = r.modelVersion;
    this.projectId = r.projectId;
    this.y = r.y;
    this.occurredAtMs = r.occurredAtMs;
  }
}
function classiRow(r) {
  return new RowInstance(r);
}
// V5 FROZ: frozen canonical-order object (frozen map).
function frozRow(r) {
  return Object.freeze({ scenarioId: r.scenarioId, modelVersion: r.modelVersion, projectId: r.projectId, y: r.y, occurredAtMs: r.occurredAtMs });
}
// V6 HOLEY: canonical-order objects inside a HOLEY_ELEMENTS array (new Array(n)
// assigned back-to-front; fully populated, same length and contents, holey kind).
function canonRow(r) {
  return { scenarioId: r.scenarioId, modelVersion: r.modelVersion, projectId: r.projectId, y: r.y, occurredAtMs: r.occurredAtMs };
}
function packedVariant(rows, mk) {
  return rows.map(mk);
}
function holeyVariant(rows) {
  const out = new Array(rows.length);
  for (let i = rows.length - 1; i >= 0; i--) out[i] = canonRow(rows[i]);
  return out;
}
const VARIANT_NAMES = ["revkey", "rotkey", "dict", "classi", "froz", "holey"];
function buildVariants(rows) {
  return {
    revkey: packedVariant(rows, revkeyRow),
    rotkey: packedVariant(rows, rotkeyRow),
    dict: packedVariant(rows, dictRow),
    classi: packedVariant(rows, classiRow),
    froz: packedVariant(rows, frozRow),
    holey: holeyVariant(rows),
  };
}

/** Canonical walk: numbers as IEEE-754 bit-pattern hex, strings/bools/null tagged. */
function foldValue(hash, value) {
  if (typeof value === "number") {
    const buf = new ArrayBuffer(8);
    new Float64Array(buf)[0] = value;
    hash.update("n:");
    hash.update(Buffer.from(buf));
  } else if (typeof value === "string") {
    hash.update(`s:${value.length}:${value};`);
  } else if (typeof value === "boolean") {
    hash.update(`b:${value};`);
  } else if (value === null) {
    hash.update("null;");
  } else if (value === undefined) {
    hash.update("undef;");
  } else if (Array.isArray(value)) {
    hash.update(`a:${value.length}[`);
    for (const item of value) foldValue(hash, item);
    hash.update("]");
  } else {
    const keys = Object.keys(value);
    hash.update(`o:${keys.length}{`);
    for (const key of keys) {
      hash.update(`k:${key}=`);
      foldValue(hash, value[key]);
    }
    hash.update("}");
  }
}
function digestReports(reports) {
  const hash = createHash("sha256");
  for (const report of reports) foldValue(hash, report);
  return hash.digest("hex");
}
function median(values) {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)];
}

// One canonical fixture instance per shape; variants share its strings/scalars.
const perfCanon = perfRows();
const smallCanon = smallRows();
const smallVariants = buildVariants(smallCanon);
const perfVariants = buildVariants(perfCanon);

// Canonical small-report digest (used to bit-check every co-resident feed).
// Computed in every arm — the call below uses the canonical representation,
// so it is part of the historical form in CANON too (it is simply one more
// canonical-representation call before the timed protocol).
const smallCanonDigest = digestReports([fitLogitAdditive(smallCanon, PERF_OPTIONS)]);

// PREREP arm only: adversarial-first — the production boundary-map site
// receives all six foreign representations before the first timed call.
let preFeedMs = null;
if (arm === "PREREP") {
  const t0 = performance.now();
  for (let sweep = 0; sweep < 5; sweep++) {
    for (const name of VARIANT_NAMES) {
      const report = fitLogitAdditive(smallVariants[name], PERF_OPTIONS);
      if (digestReports([report]) !== smallCanonDigest) {
        console.error(`REPR BIT MISMATCH (prefeed ${name})`);
        process.exit(1);
      }
    }
  }
  preFeedMs = performance.now() - t0;
}

// Hot-tier timing: perf shape 7 reps, small shape 3 reps — identical rep
// protocol in every arm; REP interleaves the six adversarial small-shape
// feeds BETWEEN timed logit reps (outside the logit timer).
const perfReps = [];
const feedTotals = [];
for (let rep = 0; rep < 7; rep++) {
  const t0 = performance.now();
  fitLogitAdditive(perfCanon, PERF_OPTIONS);
  perfReps.push(performance.now() - t0);
  if (arm === "REP") {
    const f0 = performance.now();
    for (const name of VARIANT_NAMES) {
      const report = fitLogitAdditive(smallVariants[name], PERF_OPTIONS);
      if (digestReports([report]) !== smallCanonDigest) {
        console.error(`REPR BIT MISMATCH (interleave ${name})`);
        process.exit(1);
      }
    }
    feedTotals.push(performance.now() - f0);
  }
}
const smallReps = [];
for (let rep = 0; rep < 3; rep++) {
  const t0 = performance.now();
  fitLogitAdditive(smallCanon, PERF_OPTIONS);
  smallReps.push(performance.now() - t0);
}

// Bit-face payload, byte-identical fold and report list as the R28-C
// substrate runner / R29-C co-residency runner: equality across the 9
// processes (and against the printed digest of those rounds) certifies the
// representation dose moves no logit output bit.
const r = fixtureRng(0x5eed);
const batteryShapes = [
  { rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 60, passRate: 0.5 }), options: { bootstrap: 40, seed: 11 } },
  { rows: randomRows(r, { scenarios: 3, models: 4, projects: 4, rows: 150, passRate: 0.55 }), options: { bootstrap: 60, seed: 77 } },
  { rows: randomRows(r, { scenarios: 1, models: 2, projects: 3, rows: 45, passRate: 0.65 }), options: { bootstrap: 30, seed: 5 } },
];
const reports = [
  fitLogitAdditive(perfRows(), PERF_OPTIONS),
  fitLogitAdditive(smallRows(), PERF_OPTIONS),
  ...batteryShapes.map((c) => fitLogitAdditive(c.rows, c.options)),
  fitLogitAdditive(perfRows(), PERF_OPTIONS),
];
const logitDigest = digestReports(reports);

// Perf-shape representation bit face (co-resident arms only — arm CANON must
// never execute a variant): each adversarial-representation perf report must
// equal the canonical perf report bit-for-bit.
let reprDigest = null;
if (arm !== "CANON") {
  const canonPerfDigest = digestReports([fitLogitAdditive(perfCanon, PERF_OPTIONS)]);
  for (const name of VARIANT_NAMES) {
    const variantDigest = digestReports([fitLogitAdditive(perfVariants[name], PERF_OPTIONS)]);
    if (variantDigest !== canonPerfDigest) {
      console.error(`REPR BIT MISMATCH (perf ${name}): ${variantDigest} !== ${canonPerfDigest}`);
      process.exit(1);
    }
  }
  reprDigest = canonPerfDigest;
}

// Boundary-region worst-case price: a runner-local replica of the production
// entry map (offline-logit.ts lines 420-425 — the ONLY reads of the raw
// input) is swept over the canonical and all variant perf-shape inputs. Two
// full passes drive the replica's ICs megamorphic; the second pass is the
// honest worst-case steady state. Runner-local code — production ICs are
// never touched by this probe, and it runs after all timed work.
function entryMapReplica(rows) {
  return rows.map((row) => ({
    scenarioId: row.scenarioId,
    modelVersion: row.modelVersion,
    projectId: row.projectId,
    y: row.y,
  }));
}
const entryInputs = { canon: perfCanon, ...perfVariants };
let mapSink = 0;
const entryMapPriceUs = {};
for (let pass = 0; pass < 2; pass++) {
  for (const name of ["canon", ...VARIANT_NAMES]) {
    const input = entryInputs[name];
    const t0 = performance.now();
    for (let i = 0; i < 2000; i++) {
      mapSink += entryMapReplica(input).length;
    }
    entryMapPriceUs[name] = Number((((performance.now() - t0) / 2000) * 1000).toFixed(2));
  }
}

console.log(JSON.stringify({
  arm,
  node: process.version,
  perfMedianMs: Number(median(perfReps).toFixed(3)),
  smallMedianMs: Number(median(smallReps).toFixed(3)),
  perfReps: perfReps.map((v) => Number(v.toFixed(1))),
  smallReps: smallReps.map((v) => Number(v.toFixed(1))),
  preFeedMs: preFeedMs === null ? null : Number(preFeedMs.toFixed(1)),
  feedPerRepMs: feedTotals.length === 0 ? null : Number(median(feedTotals).toFixed(3)),
  entryMapPriceUs,
  mapSink,
  logitDigest,
  reprDigest,
}));
```
