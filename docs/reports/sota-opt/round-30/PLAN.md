# Round 30 分区（10 子代理，3-VM 分波）

模型一律 `claude-fable-5-thinking-xhigh`。基线：最新 `cursor/sota-persistent-opt-83a1`。

Round 1–29 已对各区做过二十九遍穷尽裁决（Round 29 已关轮，全部空枚举），并落地 S1-F / J1 / S1-C / S1-I / S2-C / S3-C / S4-C / S4-I / S5-C / S5-F / S5-I-1 / S6-C / S6-F-1 / S7-C / S7-F-1 / S7-F-2 / S7-I-1。本轮只接受排除表未覆盖、且理论+仿真达门槛的新更优解。禁止重开任何 X* / S1-* … S29-* 条目（含已合入的 S13-B-1 与 R12–R29 全部空枚举收口，含刚合入的 R28-A … R28-J / R29-A … R29-J / R30-A / R30-B / R30-C / R30-D / R30-E）。S7-C 已落地，不得另起平行实现。S7-F-1 不是 S6-F-5。S5-H-1 必须保留。禁止再编号 **FITQ** / **NAMESHAPE** / **SCALEX** / **XPROC** / **YMIX** / **SCHEDWIN** / **KFAN** / **NVG** / **SEEDX** / **TERMCLASS** / **SHAPEK** / **ORDX** / **CKMIX** / **CALLB** / **OCCX** / **JB** / **RUNLIM** / **VALCLASS** / **CG** / **AGEX** / **COTARG** / **PRICEX** / **RTL** / **DELINV** / **PROBX** / **RVL** / **SIDEC** / **PWL** / **ZREP** / **PAIRX** / **CSPELL** / **REPRX**。空枚举轴「M=1 catalog collapse」「approval-authority face」「state serialization-boundary provenance face」「operand-residency census」「adaptation-state coordinate」「value-domain / string content-class census」「task-face optional-field presence topology」「identity-coordinate face」「plan-header identity-geometry face」「injection command-kind face」「consumer-stage string-representation census」「process-environment coordinate」「collation-locale face」「privacy / provider-policy constraint-leg face」「injection-seam default-arm face」「input storage-state face」「edge condition-kind face」「argv surface-form face」「consumer-stage operand-realm face」「scalar numeric value-class face」「batch-cardinality bottom + prelude intercept face」「policy-content tree-geometry face」「input access-path mechanics face」「join-policy face」「process output-channel disposition face」「execution-substrate face」「catalog-face row-arrangement (permutation) face」「input key-set geometry face」「C-slice numeric-kernel execution-substrate instantiation」「contents-store population face」「boundary call-protocol face」「proposal write-path termination-leg face」「decision-gate resolution face」「boundary argument-slot materialization face」「invocation-identity / path-referent spelling × cwd face」「durable-store zero-state provenance」「learned-policy container face」「input reference-identity / aliasing-topology face」「process workload-composition face」「wall-clock sampling topology / timestamp-provenance face」「registry-record string-list payload face」「boundary output-retention / superseded-snapshot liveness face」「M2 supervised-engine adjudication lattice」「coverage-relation incidence topology face」「input-file JSON serialization-form face」「durable-artifact content-spelling face」「call-stream ordering / dispatch-history face」「scale-cube second-order interaction face」「approval-profile constraint-list geometry face」「input object-representation face」「async suspension-point topology / per-run suspension-manifest face」不铸 ID、不写入排除表，但禁止再编号或跨区移植。face 名「execution-substrate face」归 R27-H；R28-C 只是该面在 C 区数值内核上的一格剂量，勿另起绰号。**PAIRX** 是 process workload-composition face 的 C 区双估计器共驻实例化观察绰号，不写入排除表。**PWL** 是 proposal write-path termination-leg face 的观察绰号，不写入排除表。**ZREP** 是 durable-store zero-state provenance 的观察绰号，不写入排除表。**CSPELL** 是 durable-artifact content-spelling face 的观察绰号，不写入排除表；与 R29-I input-file JSON serialization-form face 缝不同，勿因撞「拼写」词根而补铸。**REPRX** 是 input object-representation face 的 C 区实例化观察绰号，不写入排除表；与 R30-A call-stream / R29-A aliasing / R28-A key-set geometry 不同面。

R7-I 的教训：默认态夹具会遮蔽配置态主路径。本轮须按「配置态 × 命令类」矩阵复核测量盲区。多个切片已有整片预算收口——先复核再找新角度，不要硬凑。若独占分支因兄弟切片合入而快进，rebase docs-only 并把新轴加入不移植清单。

分区与 Round 1 相同（R30-A … R30-J），报告写入 `docs/reports/sota-opt/round-30/`。

状态：第 1–8 波 A–E 已合入、F/G 运行中、H 本波派出。Round 29 已关轮。

A 切片已合入：空枚举，未铸 S30-A-*。切片 `git diff 7acb666..HEAD` 为空。预算复核 12.8–15.3 µs/gate ⇒ ~64–77 µs/run（与 R25–R29 带重叠）。本轮新增 call-stream ordering / dispatch-history face（异质门调用多重集到达共享生产路径的次序；整门交错溢价 +0.20–0.26 µs/gate，µs 拒收类）。R29-A input reference-identity / aliasing-topology face 与 R28-A input key-set geometry face / R27-A scalar numeric value-class face / R26-A collation-locale face 不补铸。R29-I input-file JSON serialization-form face / R29-J **CSPELL** 不要移植。基线 `7acb666` 空 diff 再确认。

B 切片已合入：空枚举，未铸 S30-B-*。切片 `git diff 94ed3d9..HEAD` 为空。天花板复核 M=2 8.87–9.13 / M=10 17.34–18.23 ms/eval（×4，与 R29-B 带重叠）。S12-B-2 / S13-B-1 重开条件未触发。本轮新增 scale-cube second-order interaction face（N×M / N×L / M×L 联角；残差属 GC/内存 regime，非切片代码路径）。R29-B learned-policy container face 与 R28-B catalog-face row-arrangement (permutation) face / R27-B batch-cardinality bottom + prelude intercept face / R24-B M=1 catalog collapse 不补铸。目录行预排序是行为变更（R28-B W3）。基线 `94ed3d9` 空 diff 再确认。

C 切片已合入：空枚举，未铸 S30-C-*。切片 `git diff 183df9b..HEAD` 为空。生产中位复核 658.7–663.0 ms（×4，与 R29-C 带重叠）。APC ceiling 18.8–26.6 < 35（预演含 28.5 仍低于 35）；sink=7.309 逐位。r1c–r7c 8028 / 14420 / 14730 / 24888 / 28555 / 25483 / 6193 全绿。本轮新增 input object-representation face / **REPRX**（同值六种引擎表示：插入序反转/旋转、字典模式、class 实例、frozen、holey；热层符号翻转 −9.8…+4.0 ms，入口拷贝防火墙 ≤~8.2 µs/report）。**PROBX** / **PAIRX** 不补铸。R29-C process workload-composition face / R28-C C-slice numeric-kernel execution-substrate instantiation 不要移植。基线 `183df9b` 空 diff 再确认。

D 切片已合入：空枚举，未铸 S30-D-*。切片 `git diff 82bef36..HEAD` 为空。eval 地板复核 S0c 3.29–3.66 / 同进程 3.13–3.27 ms（与 R26–R29 带重叠）。S9-D-4 / S12-D-1 未重开。本轮新增 approval-profile constraint-list geometry face（autoPromoteClasses n × neverAutoPromote m 的多重集几何；双线性交叉 0.63–0.85 ns/cell，合法枚举封顶 <0.3 µs）。R29-D registry-record string-list payload face 与 R28-D contents-store population face / R27-D policy-content tree-geometry face / R24-D approval-authority face 不补铸（几何面 ≠ 权威面）。S9-D-4 / S12-D-1 永不再开。基线 `82bef36` 空 diff 再确认。

E 切片已合入：空枚举，未铸 S30-E-*。切片 `git diff adb20d7..HEAD` 为空。SLICE-CPU 复核 14.7–15.2 µs/run（cmp25 回放，带内）。S8-E-1 / S9-E-2 / S13-B-1 未重开。本轮新增 async suspension-point topology / per-run suspension-manifest face（19 个 await 全在 4 个 I/O 文件；CPU 热导出全同步；~29–45 ns/traversal ⇒ ~0.7 µs/run）。**PWL** / **RVL** / **RTL** / **CG** / **JB** / **NVG** 不补铸。R29-E wall-clock sampling topology / timestamp-provenance face / R30-C **REPRX** 不要移植。基线 `adb20d7` 空 diff 再确认。

F 切片 = 15 文件（`src/experiments/`）。锚点默认 R29-F：r5f 120.28/121.96、r6f 121.05、r7f 131.29/126.76。S7-F-1 ≠ S6-F-5。R29-F boundary output-retention / superseded-snapshot liveness face 与 R28-F boundary call-protocol face / R27-F input access-path mechanics face / R26-F input storage-state face 不补铸。R29-C **PAIRX**（编排位点 `attribution-report.ts`）/ R30-D approval-profile constraint-list geometry face / R30-E async suspension-point topology / per-run suspension-manifest face / R30-C **REPRX** / R30-B scale-cube second-order interaction face / R30-A call-stream ordering / dispatch-history face 不要移植。基线 `519101f` 预期空 diff。运行中。若落地：重跑 r1f/r5f/r6f/r7f（2668 / 224 / 27 / 169）+ r30f。

G 切片 = 42 文件（`src/run/` 除 A 两文件；`src/supervisor/` 除 model-router；`src/graph/`；`src/domain/`）。计算顶默认 R29-G：0.290–0.298 vs I/O 93.7–97.5 ms（~323–327×）。digest `06cbcf92c098c8f0` 第二十次逐位相同，勿补铸 digest。R29-G M2 supervised-engine adjudication lattice 与 R28-G decision-gate resolution face / R27-G join-policy face / **RUNLIM** / **CKMIX** / **TERMCLASS** / **SCHEDWIN** / **XPROC** / **NAMESHAPE** 不补铸。R29-E wall-clock sampling topology / timestamp-provenance face / R29-F boundary output-retention / superseded-snapshot liveness face / R30-C **REPRX** / R30-D approval-profile constraint-list geometry face / R30-E async suspension-point topology / per-run suspension-manifest face / R30-A call-stream ordering / dispatch-history face / R28-J **ZREP** 不要移植。基线 `4efee23` 预期空 diff。运行中。

H 切片 = 21 文件（evaluation **8** + requirement 7 + review 4 + rubric 2）。S5-H-1 必须保留。R29-H coverage-relation incidence topology face 与 R28-H boundary argument-slot materialization face / R27-H execution-substrate face / R26-H consumer-stage operand-realm face / R25-H consumer-stage string-representation census / R24-H operand-residency census 不补铸。热层默认 R29-H：8889–9147 ns。PATH_RE 仅当目标为程序化/对抗或真实载荷 ≥~3.2K 无斜杠 `[\w.-]` 才重开。R30-E async suspension-point topology / per-run suspension-manifest face / R30-C **REPRX** / R30-D approval-profile constraint-list geometry face / R29-G M2 supervised-engine adjudication lattice 不要移植。基线 `fd437a9` 预期空 diff。本波派出。

I 切片 = 25 文件（cli 13 + pi-adapter 9 + config 2 + telemetry 1）。S8-I-1 两臂文件级 blocked。R29-I input-file JSON serialization-form face 与 R28-I invocation-identity / path-referent spelling × cwd face / R27-I process output-channel disposition face / R26-I argv surface-form face / R25-I process-environment coordinate / **NAMESHAPE** 不补铸。R29-J **CSPELL** 不要移植（缝不同）。npm POSIX `.bin` 符号链接 silent no-op 已归档，不在本战役改。custom−builtin 默认 R29-I：children +44.0~+50.5 / +22.6~+25.7、track +44.0~+53.2 / +22.5~+25.6、flowchart-flat64 +41.9~+42.8 / +18.5~+21.7 ms。基线 `8dee7fb` 预期空 diff。等待派出。若落地：重跑 r4i/r5i/r7i（68 / 119 / 80）+ r30i。

J 切片 = 29 文件。J1 / S5-J-3 / S6-J-1 / S8-J-2 钉死。禁止去 fsync。R29-J **CSPELL** 与 R28-J **ZREP** / R27-J **SIDEC** / R26-J **DELINV** / R25-J **COTARG** / R24-J **VALCLASS** / R23-J **CALLB** / R22-J **SHAPEK** / R21-J **KFAN** 不补铸。R29-I input-file JSON serialization-form face 不要移植（缝不同）。file-lock retryMs 已由 D/E 定价，勿作为 J ID 补铸。基线 `fb41417` 预期空 diff。等待派出。若落地：重跑 J1（2468）+ r30j。I/O 地板 R29-J：preferences 146.5–234.7 / 431.5–542.6 µs；jsonl 59.3–68.1 / 212.8–319.8 µs。
