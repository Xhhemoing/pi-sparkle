MODEL_SLUG=claude-fable-5-thinking-xhigh

# R30-E：`src/learning/` 第三十遍复查报告（Round 30）

**战役:** 全库持久 SOTA 优化 Round 30 / R30-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `3902706`（R30-B 空枚举摄取 + 本派单；全部测量与写稿在该 tip 完成——测量期零快进，收稿 fetch 确认）。提交前独占分支一次 fetch 内快进两笔至 `37fbb28`（`1f41e30` R30-D 空枚举摄取 + R30-F 派单；`37fbb28` R30-C 空枚举摄取 + R30-G 派单），逐笔核对均 docs-only：仅触 `docs/reports/sota-opt/` 下 4 个文件（PROGRESS / round-30 PLAN / R30-C.md / R30-D.md），`git diff 3902706..37fbb28 -- ':!docs/reports/sota-opt'` = **0 行**——两 tip 的 `src/` 与 `test/` 逐字节相同，全部测量维持有效。报告分支已 rebase 至 `37fbb28`（快进式零冲突），10 文件冻结在新 tip 复验仍 **0 行**；R30-C 轴（input object-representation face / REPRX）与 R30-D 轴（approval-profile constraint-list geometry face）已读结论与轴段、补入非移植清单并完成划界（§2.2 / §5）。
**分支:** `cursor/r30-e-learning-thirtieth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第三十组新角度枚举为空
（有理由的空枚举，未铸造任何 S30-E-\* ID，与 R10-E..R29-E 同口径），
切片维持关闭（MORE_OPTIMA=no）。本轮补齐一条 R1-E..R29-E 从未拉伸过
的完整性轴——「异步悬挂点拓扑 / 每 run 悬挂清单面 (async
suspension-point topology / per-run suspension-manifest face)：切片内
`await` 位点住在哪里（19 个位点全部在 4 个 I/O 文件；6 个文件完全同
步）、一次收集运行逐腿穿越多少个悬挂点（作为 F_json 与 S_bound 的仿
射清单）、哪些导出面是同步域哪些是异步域（域普查）、以及剥离其所包
裹 I/O 之后纯异步机器（promise 分配 + 微任务节拍）的价格，作为受控
变量」。它以「谁悬挂、悬挂几次、机器费多少」为受控变量——这是 30 轮
里唯一从未受控过的坐标：S1-E-5 / S4-E-2 关闭的是两个**并行化形态**
（Promise.all 形）；S5-G-5 / S6-J-4 关闭的是攒批形态；S7-H-1 在 H 区
关闭的是假异步链的同步化形态；R20-E 控的是**跨进程**竞争面；R21-H
（H 区）控的是同步体假异步链的调度窗计数与外来队列内容剂量——没有
任何一轮 E 报告数过悬挂位点、钉过真实 I/O 收集运行的逐腿悬挂清单、
或给机器份额定过价。杠杆非变量声明：事件构成钉死（R17-E）；时钟采样
拓扑钉死（R29-E——本轴顺带钉死一条跨轴不对称：悬挂清单 1 + F_json
按 .json **文件名**计费 ≥ 时钟清单 1 + F_parsed 按**解析成功**计费）；
容器形状钉死（R29-B 不移植）；store 零态钉死（R28-J ZREP 不移植）；
跨调用到达序钉死（R30-A call-stream ordering face 不移植——彼为
调用间序，本为调用内结构）；提交前快进合入的 R30-C **REPRX**
（引擎级输入表示形态，routing 属地）与 R30-D approval-profile
constraint-list geometry face（adaptation 属地）异属地异坐标，
均不移植（§2.2 划界）。5 次套件运行 = 3 × v22.22.2 + 2 ×
v22.14.0，27/27 sanity ×5 全绿，全部确定性 VERDICT 行**跨版本跨运行
逐位一致（含全部 PromiseHook 计数常数——两引擎计数逐一相同）**：**

1. **域普查（C1）**：全部 20 个 CPU 热面导出（collect 双通道、parse、
   score ×2、outcomes、diagnose、apply、task-success ×2、policy
   parse/print/identity/key/from-assignments、patterns、attribution
   ×2、signatures ×2）**同步且零 promise 分配**（PromiseHook 可见
   init 全零）；异步域恰 9 个导出（runAutoAdaptLoop /
   runAutoAdaptFromEvents / proposeRoutingFromOutcomes /
   proposeRoutingFromAssignments / proposeRoutingFromRoutedEvents /
   loadLearnedRouting / saveLearnedRouting（deprecated 拒绝腿见证）/
   loadProjectBandit（死导出）/ updateProjectBandit；前 8 个以调用
   见证 Promise 实例、第 9 个 proposeRoutingFromRoutedEvents 以源级
   `async` 声明见证 :164）——**公开异步域与排除表已保护的 I/O 契约
   域逐一重合，不存在非 I/O 的异步面**。
2. **ingest 悬挂清单（C2，确定性）**：collect-only 腿 promise inits
   = 14 + 30 × F_json；对内容盲（{2 valid, 1 malformed, 1 empty,
   1 non-record} 与 5 valid 逐位同价）、对非 .json 名盲（±.txt 零
   变化）；`auto-loop.ts:236` 的 readFile 悬挂按 .json **文件名**
   每名恰付一次——与 R29-E 时钟清单（1 + F_parsed，按解析成功计费）
   构成蓄意不对称见证。
3. **persist 悬挂清单（C3，确定性）**：`auto-loop.ts:221` 的
   appendFeedback 悬挂按 episode **绑定**信号每行恰付一次（机器斜率
   39 inits/绑定行，顺序执行属设计——攒批/并行形态 = S5-G-5 /
   S6-J-4 / S4-E-2 已关闭属地）；未绑定信号零悬挂（循环同步
   continue）；extra 通道的绑定跟随信号自带的调用方供给 episodeId
   而非运行上下文（R29-E C4 调用方全权的推论）。
4. **免悬挂异步腿（C4）**：proposeRoutingFromOutcomes(outcomes=[])
   与 runAutoAdaptFromEvents(无 PROJECT_DISCOVERED) 在任何 await 前
   返回——机器与空异步函数逐位相同（4 inits）；其函数体（策略推导 /
   事件预扫）全同步。
5. **全 W1 failing run（C5）**：总 promise inits = **415/run**（2/2
   全新根重复逐位相同，且**跨两个 Node 版本逐位相同**）；切片内
   await 位点穿越清单 = **16**（:82 + :229 + :236×5 + :87 + :89 +
   :45 + :14 + :79 + :80 + :108 + :166 + :193；:221×0 无绑定信号）——
   每个穿越都挂在一个 ~10¹–10²µs 级真实 I/O 操作上，全部已被排除表
   定价/保护。
6. **尾链形态与条件腿（C6/C7）**：deprecated 别名
   proposeRoutingFromAssignments 付常数 +2 inits 链税（test-only
   面）；runAutoAdaptFromEvents 包装增量 +4 inits（once-per-run）；
   :89 bandit 悬挂仅在存在 modelId 时穿越（+118 inits 整事务含锁
   机器）——悬挂布点全部决策门控，每个门都是既有行为契约
   （S2-E-1 / S2-E-4 保护的 write-always 语义）。
7. **逐项定价（5 采样 = 3 × v22.22.2 + 2 × v22.14.0，PromiseHook
   关闭）**：已决 promise 单 await 28.5–29.3 ns；异步调用（await
   inner()）43.3–44.9 ns/call；`return inner()` 链税 48.3–50.4 ns、
   return-await 差 1.2–3.7 ns/call；**整轴预算 = 现实 W1 16 个穿越
   × ~45ns ≈ 0.69–0.72 µs/run 纯机器（即使清零也距 ≥10 ms 门槛
   13924–14447×）；荒谬档 F_json=100 + S_bound=100 = 211 穿越 =
   9.1–9.5 µs（1056–1095×）**；且每个穿越包裹的真实 I/O 以 ~10³×
   支配机器份额（机器 ~0.1%）——I/O 面先越线三个数量级。
   **不产生候选。**

**天花板裁决（频率 × 单价 × 属地三端封死，R20-E..R29-E 同式）**：
频率端——全部悬挂挂在 once-per-run I/O 腿（域普查 + 清单定理），
逐项循环以 F_json / S_bound 现实个位数为界，无每任务/每事件悬挂
放大器（CPU 热面零悬挂由 C1 断言）；单价端——单穿越 ~29–45 ns，
现实全轴 0.69–0.72 µs/run，荒谬档亦 >10³× 低于门槛；属地端——机器
单价的实现体是 V8 await/promise 机器（执行基底属地，R27-H face
邻域拒列），全部消除形态撞已关闭条目（S1-E-5 / S4-E-2 / S5-G-5 /
S6-J-4 / S7-H-1 / S6-E-3 / S2-E-1 / S2-E-4 / X0-3）或公开 Promise
签名契约（X0-4）或崩溃前缀顺序契约（S5-G-4 族）。**不产生候选。**

其余与前二十九轮同构的复核全数成立：切片三十遍零 diff
（`git diff adb20d7..HEAD -- src/learning` 为 **0 字节 0 提交**，
开工、测量后、写报告前各验一次；10 文件 1770 行 `wc -l` 与
R14-E..R29-E 相同）；SLICE-CPU 本夹具（seeds `0xe30e01/02/04`，构成
collected=7 / outcomes=3 / diagnoseGroups=1）**12.9–13.2 µs/run**
（6 次运行 = 3 × v22.22.2 + 3 × v22.14.0，版本差异不可分辨，
756–774× 低于门槛；构成比 cmp25 轻——collected=7 使 collect 段
8.3–8.5 vs 11.4–11.7 即主差源，随构成单调，无新机制）；**R25-E 种子
对调（cmp25，6 次 = 3 × v22.22.2 + 3 × v22.14.0）复现 collected=14 /
outcomes=1 / diagnoseGroups=2（VERDICT 行与 R25-E..R29-E 夹具行逐字
相同），总量 14.8–15.2 µs，6/6 落在默认带 14.7–15.2 内（派单指令的
×3 复测以双版本各 ×3 双倍满足），分段 collect 11.4–11.7 / outcomes
1.3–1.4 / bandit 0.8 全在 R25-E 带内，diagnose 1.32–1.37 有 2/6 采样
落 1.32 = 低于带下缘恰 0.01 µs（表显粒度级单侧擦边，R29-E 同例只读
作价签不改带）**——默认带沿用不变；配置态 live 面锚点：
apply(avoid=10,M=10) 260–297 ns/task（257–312 带内 **12/12**）、
空策略 112–150 ns/task（110–140 带内 10/12——两个高位 144/150 均在
主夹具批、每引擎各一，cmp25 批 6/6 在 112–116 低位区；R28-E
load-fresh 位置噪声同例只读价签）、load 现实 registry 78.4–90.7 µs
+ 一次 145.1（74.3–134.5 带内 11/12——145.1 为 v22.14.0 首进程首批，
冷进程位置噪声）、load fresh(ENOENT) 19.9–48.7 µs（历史并集
25.2–36.8 带内 10/12——48.7 高位与 19.9 低位均在 v22.14.0 主夹具批，
快慢双侧个体，R28-E/R30-B 同类判读；cmp25 批 6/6 带内 25.2–34.0）；
历史 nonempty 格 VERDICT `allowed=5 preferred=m5` 三十轮不变）；
`pnpm gate` 在 engines 合规 Node v22.22.2 全绿（1169 tests / 78
suites / 1168 pass / 0 fail / 1 skipped，计数与 R14-E..R29-E 逐一
相同，GATE_EXIT=0）；切片套件 48/48 双版本过。S8-E-1 / S9-E-2 /
S13-B-1 未以任何形态触碰。R29-E 壁钟采样拓扑面、R29-B learned-policy
container face、R30-A call-stream ordering / dispatch-history face、
R30-B scale-cube second-order interaction face、R30-C input
object-representation face（**REPRX**，提交前快进合入）、R30-D
approval-profile constraint-list geometry face（提交前快进合入）、
R29-C **PAIRX**、R28-J **ZREP**、R29-J **CSPELL**、R28-E **PWL**、
R29-I input-file JSON serialization-form face 均未移植；**PWL / RVL / RTL / CG / JB /
NVG** 未复铸（本轮不触 propose 腿身份、不动裁决格、不触 apply 终止
腿——applyLearnedRouting 经 C1 复核零悬挂全同步、无 TASK_RETRY
链态、无数值类剂量）。不声称 Outcome-supported；Checkpoint F-PROD
仍开放（ADR-005）。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 /
  `auto-loop` 283 / `bandit-store` 83 / `diagnostics` 78 /
  `from-episode` 298 / `learned-routing` 216 / `patterns` 146 /
  `signals` 439 / `signatures` 66 / `task-success` 69 = 1770 行，
  `wc -l` 与 R14-E..R29-E 逐一相同），本轮第三十遍全量实际重读。
  上下游 `feedback/store.ts`、`persist/jsonl.ts`、`adaptation/`、
  `routing/`、`run/` 只读取证，一行未改。
- 生产最后落地 `adb20d7`；`git diff adb20d7..HEAD -- src/learning`
  为 0 字节、`git log adb20d7..HEAD -- src/learning` 为 0 提交
  （开工、测量后、写报告前各验一次）。
- 独占分支：测量期零快进（开工 fetch 与收稿 fetch 均为 `3902706`，
  全部测量与 VERDICT 在该 tip 产出）；提交前 fetch 见两笔 docs-only
  快进 `3902706` → `1f41e30` → `37fbb28`（R30-D、R30-C 空枚举摄取 +
  R30-F/R30-G 派单），`git diff 3902706..37fbb28` 仅触
  `docs/reports/sota-opt/` 4 文件、`src/`+`test/` **0 行**逐笔核对；
  按令 rebase 至 `37fbb28`（零冲突）、冻结复验 0 行、两条新兄弟轴
  补入非移植清单（§2.2 / §5），未等待其余在飞兄弟。
- 硬不变量全数未触碰：分析不变更在飞 run；tracking 无指挥权；
  H/score 不写路由 PASS/FAIL；双 LCB / 双归因均在；提案先行
  （Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；
  CAS / 凭据 / 数据面契约 / 公开签名不动；S8-E-1 零去重分析
  （本轮仅在 §1 复核其调用位点与单价带）；S9-E-2 未触碰（两遍结构
  零变换分析）；S13-B-1 重开条件未触发（apply 本体零触碰，且本轮
  C1 复核其全同步零悬挂）。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / PLAN.md；未开 PR；未触兄弟
  分支；pnpm-lock.yaml 未改（本机仅 `pnpm install --frozen-lockfile`
  补装依赖）。
- 阅读序合规：round-30/PLAN（E 段）→ EXCLUSIONS 全表 → R29-E 全文
  → R30-A / R30-B 结论与轴段（划界）→ R20-E / R25-E / R10-E 复核段
  （方法学与先例核对）→ 10 个源文件全量重读；提交前快进后补读
  R30-C / R30-D 结论与轴段（仅为划界，不据其改动任何测量或结论）。

## 1. 冻结证明与 SLICE-CPU 重锚

```bash
git log --oneline -1 origin/cursor/sota-persistent-opt-83a1
# 3902706 docs(sota-opt): ingest R30-B (empty enum) and dispatch R30-E
#（测量时 tip；提交前 docs-only 快进至 37fbb28 并已 rebase——见基线行，
#  src/ 逐字节相同，冻结在新 tip 复验仍 0 行）
git diff adb20d7..HEAD --stat -- src/learning/   # （空输出，0 文件 0 字节）
wc -l src/learning/*.ts                          # 1770 total（同 R14-E..R29-E）
```

锚点脚本 `/tmp/r30e-anchor.mts`（构造 = R10-E 附录 A 逐字同构 +
R30-E 置换，见附录 A）共 **6 次主运行**（3 × v22.22.2 + 3 ×
v22.14.0）+ **6 次 cmp25 对调运行**（R25-E 种子 `0xe25e01/02/08` +
R25-E NOW `2026-08-25T17:30`，3 + 3），每次 3/3 sanity 全过，确定性
VERDICT 行各自跨运行跨版本逐位一致。代表性一次主运行（v22.22.2）：

```text
VERDICT fixtures: E=41 collected=7 routedE=41 outcomes=3 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=8.3us outcomes=2.8us diagnose=1.05us bandit-build=0.8us | total in-slice CPU ~13.0us per run vs landing bar >=10000us (771x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=272ns/task | empty policy=130ns/task -> x10 tasks/run = 2.72us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=31.2us | realistic registry (baseline+1 candidate)=90.7us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

拒绝采样审计（`probe` 开关，k=1..12 三元组全表见附录 A）：collect 角
首选 `0xe30e01` 一次通过（collected=7>3）；routed 角首选 `0xe30e02`
一次通过（outcomes=3>0）；signals 角 `0xe30e03`（diagnoseGroups=0）
拒绝，`0xe30e04`（diagnoseGroups=1）通过——一次拒绝。

cmp25 对调 6 次 VERDICT 行 6/6 逐位一致且与 R25-E..R29-E 夹具行逐字
相同（`E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2
prevArms=3`）；总量 15.1 / 15.0 / 15.0 | 14.8 / 15.2 / 14.9 µs vs
默认带 14.7–15.2：**6/6 带内（派单的 ×3 复测以双版本各 ×3 双倍满
足）**，分段 collect 11.4–11.7 / outcomes 1.3–1.4 / bandit 0.8 vs
R25-E 带 11.3–11.9 / 1.3–1.5 / 0.8 全数在带；diagnose 1.32–1.37 vs
带 1.33–1.46：**2/6 采样落 1.32，低于带下缘恰 0.01 µs**——表显粒度
级单侧擦边（R29-E 2/9 同位擦边先例；同批其余全带内、零 diff 下夹具
逐字节相同、VERDICT 逐位一致）——按 R28-E/R29-E 位置噪声同例只读作
价签记录，不改带、不立项。SLICE-CPU 重锚判定：**默认带沿用
14.7–15.2 µs/run 不变**，本轮主夹具 12.9–13.2 µs 为新构成点
（collected=7 使 collect 段 8.3–8.5 vs cmp25 的 11.4–11.7、
outcomes=3 使 outcomes 段 2.7–3.0 vs 1.3–1.4——两段皆随构成单调，
无新机制；跨轮可比面唯 cmp25 且 6/6 带内）。

配置态锚点（12 采样 = 主夹具 6 + cmp25 6）：apply(avoid=10, M=10)
260–297 ns/task（R13-E..R29-E 带 257–312 内 **12/12**）、空策略
112–150 ns/task（110–140 带内 10/12：**两个高位采样 144（v22.22.2
主夹具 run2）与 150（v22.14.0 主夹具 run3）越上缘 3–7%**，cmp25 批
6/6 在 112–116 低位区、主夹具其余 112–130 带内——同批 VERDICT 逐位
一致且零 diff，判读为瞬时位置噪声价签）、load 现实 registry
78.4–90.7 µs + 一次 145.1（R15-E..R29-E 带 74.3–134.5 内 11/12：
145.1 为 **v22.14.0 首进程首批**冷进程噪声）、load fresh(ENOENT)
19.9–48.7 µs（历史并集 25.2–36.8 内 10/12：48.7 高位与 19.9 低位
均在 v22.14.0 主夹具批——快慢双侧个体，R28-E 会话高位噪声与 R30-B
「快侧个体」同类判读；cmp25 批 6/6 带内 25.2–34.0）。三个越带采样
全部集中在主夹具 v22.14.0/v22.22.2 批、cmp25 十二格零越带——
零 diff + VERDICT 逐位一致下均按价签记录，不改带。

## 2. 新轴：异步悬挂点拓扑 / 每 run 悬挂清单面

### 2.1 定义与盲区

受控变量：**切片内 await 位点的数量与位置、一次收集运行逐腿穿越的
悬挂点数（仿射清单）、导出面的同步/异步域归属、以及剥离所包裹 I/O
后纯异步机器的价格**。切片内 await 位点普查（`rg "await "
src/learning/` 钉死）：恰 **19 处**，全部在 4 个 I/O 文件——

- `auto-loop.ts`（10 处）：:82 ingest / :87 persist / :89 bandit
  （条件腿）/ :108 propose（条件腿）/ :166 registry load / :190 与
  :193 save（恰一对）/ :221 appendFeedback（**每绑定信号一次**）/
  :229 readdir / :236 readFile（**每 .json 名一次**）；
- `learned-routing.ts`（1 处）：:121 registry load；
- `bandit-store.ts`（4 处）：:14 readFile / :45 readBanditFile /
  :79 mkdir / :80 writeFile；
- `from-episode.ts`（4 处）：:104 lock / :105 registry load / :137
  save（create 腿）/ :170 readAll。

`signals.ts` / `diagnostics.ts` / `task-success.ts` / `patterns.ts` /
`attribution.ts` / `signatures.ts` 六文件**零 await**——CPU 热面
整体处于同步域（C1 以零 promise 分配断言）。

盲区证明（为什么 29 轮没人控过这条轴）：S1-E-5（ingest Promise.all
并行读）与 S4-E-2（persist∥bandit 编排重叠）关闭的是两个**并行化
形态**——形态裁决 ≠ 悬挂拓扑轴；S5-G-5 / S6-J-4 关闭的是攒批形态
（durability + read-your-writes 屏障）；S7-H-1 在 H 区关闭的是
**假异步链**（同步体 + 异步签名）的同步化，其收益构成即异步机器
本体（167–175ns/run），但 E 切片的异步腿包裹**真实 I/O**，机器份额
从未被单独剥离定价；R20-E 并发面控的是跨进程锁竞争（单飞 vs 竞争）
不是单飞内部的悬挂结构；R21-H（H 区）控的是调度窗计数 + 外来队列
内容剂量（Y/QM/QT 腿），其对象是 H 链且工具是微任务泵——本轴对象
是 E 切片真实 I/O 腿、工具是 PromiseHook init 计数，坐标（位点拓扑
+ 逐腿清单 + 域普查）从未在 E 出现过。没有任何一轮数过 19 个位点、
证过 1 + F_json 与 S_bound 仿射清单、把「哪个导出面属于哪个域」当
可断言拓扑钉死过。

### 2.2 与近邻轴的区分（换名自查核心）

| 近邻 | 其受控变量 | 本轴钉死之 | 区分 |
| --- | --- | --- | --- |
| S1-E-5 / S4-E-2（已关闭形态） | Promise.all 并行化两形态 | 引为本轴消除形态的已关闭先例 | 形态 ≠ 轴；本轮不重提任何并行化 |
| S5-G-5 / S6-J-4（已关闭形态） | 写攒批形态 | :221 逐行顺序性引为设计契约 | 攒批不重提 |
| S7-H-1（H 区已关闭） | 假异步链同步化 | E 腿为真实 I/O，同步化物理不可行 + X0-4 | 假异步 vs 真 I/O |
| R20-E 并发面 | 跨进程锁竞争/可串行化/撕裂读 | 单飞进程内悬挂结构 | 进程间 vs 调用内 |
| R21-H 异步调度面（H 区） | 调度窗计数 + 外来微任务剂量（Y/QM/QT） | E 区位点拓扑 + 逐腿仿射清单 + 域普查；工具为 PromiseHook 计数非微任务泵 | 方法学近亲、坐标与属地不同；未在 H 属地重测 |
| R30-A call-stream ordering face（兄弟，plan-ban） | 跨调用到达序（多重集次序） | 单调用内悬挂布点 | 调用间 vs 调用内；未移植 |
| R30-B scale-cube face（兄弟，plan-ban） | N×M/N×L/M×L 联角交互残差 | F/S 剂量仅证本切片自身清单仿射性（等差断言），无跨规模联角 | 未移植 |
| R30-C input object-representation face / **REPRX**（兄弟，提交前快进合入，plan-ban） | 同一抽象输入值在引擎层的表示形态（隐藏类/属性插入序/字典模式/class 实例/冻结/holey），属地 src/routing/ | 本轴受控变量是调用内悬挂位点数与布点，计数断言全为 PromiseHook init 整数相等——对对象表示定义盲（promise init 数不随输入对象隐藏类变动）；夹具全部单一字面量构造形态，未剂量表示 | 异属地（routing vs learning）+ 异坐标（表示 vs 悬挂拓扑）；未移植 |
| R30-D approval-profile constraint-list geometry face（兄弟，提交前快进合入，plan-ban） | `ApprovalProfile` 两约束列表（autoPromoteClasses n / neverAutoPromote m）的多重集几何（基数 + 重复重数），属地 src/adaptation/ | E 切片对 approval-profile 的唯一触点是 auto-loop.ts:93 `isAutoAdaptEnabled()`——纯同步 env 标量读（C1 域普查覆盖：零悬挂零 promise init），不构造、不传入、不读取任何 `ApprovalProfile` 列表 | 异属地 + 异对象（列表几何 vs 悬挂拓扑）；未移植 |
| R29-E 时钟采样拓扑面 | 采样次数/位置/共享 | 仅引 1+F_parsed 作跨轴不对称对照（1+F_json ≥ 1+F_parsed） | 时钟不重剂量 |
| R29-A 引用同一性面（兄弟轮） | 输入别名拓扑 | 计数断言全为整数相等，无引用剂量 | 未移植 |
| R15-E 分配/GC 面 | 分配体积与 GC 压力 | promise init 数用作**计数器**而非分配剂量；价以 ns/穿越计 | 字节 vs 节拍 |
| R17-E 异常面 | 拒绝路径逐类定价 | malformed/empty 文件仅作内容盲见证的杠杆，错误腿不重价 | 杠杆非变量 |
| R27-H execution-substrate face | 基底实例化 | 机器单价实现体 = V8 await/promise（属地拒列引用），受控变量是源级位点拓扑 | 基底不剂量 |
| **PWL**（R28-E） | propose 终止腿 × 写激活集 | W1/W2 腿仅作穿越清单的杠杆，腿身份不重价 | 未复铸 |
| **RTL**（R26-E） | apply 终止腿 | applyLearnedRouting 属同步域零悬挂（C1 复核） | 无交集 |
| R28-J **ZREP** / R29-J **CSPELL** / R29-C **PAIRX**（兄弟轮） | store 零态 / 工件拼写 / 双估计器共驻 | store 态全新临时根钉死；无字节拼写剂量；单进程单工作负载 | 均未移植 |

### 2.3 悬挂命运普查（确定性，5/5 运行跨版本逐位一致）

套件 `/tmp/r30e-async.mts`（全文见附录 A）SEGMENT C 共 27 检查：
计数器 = `async_hooks.createHook` PROMISE init 计数（PromiseHook
可见 promise 分配；全部断言为相等/等差式，harness 常数自消；定价段
钩子关闭）：

```text
VERDICT ASYNC C1 realm census: the entire CPU-hot surface (... 20 exported faces) is synchronous and allocates zero promises; every suspension point in the slice lives on the I/O legs (19 await sites in auto-loop/learned-routing/bandit-store/from-episode; signals/diagnostics/task-success/patterns/attribution/signatures have none)
VERDICT ASYNC C1b async realm: 9 async exports (...) — all Promise-returning; the public async realm is exactly the I/O-contract realm the exclusion table already protects
VERDICT ASYNC C2 ingest manifest: suspension/machinery cost is affine in F_json — promise inits = 14 + 30 x F_json (collect-only leg); the readFile await at auto-loop.ts:236 is paid once per .json NAME (malformed/empty/non-record .json all pay it; .txt pays nothing) — deliberate asymmetry witness vs the R29-E clock manifest (clock samples = 1 + F_parsed)
VERDICT ASYNC C3 persist manifest: the appendFeedback await at auto-loop.ts:221 is paid once per episode-BOUND signal (machinery slope 39 promise inits per bound row, sequential by design ...); unbound signals traverse zero suspensions in persistSignals ..., and binding on the extra channel follows the signal's own caller-supplied episodeId (R29-E C4 caller-control corollary), not the run context
VERDICT ASYNC C4 suspension-free async legs: proposeRoutingFromOutcomes(outcomes=[]) and runAutoAdaptFromEvents(no PROJECT_DISCOVERED) return before any await — PromiseHook-visible machinery identical to an empty async function (4 inits)
VERDICT ASYNC C5 full W1 failing run: total PromiseHook-visible promise inits = 415 per run (2/2 fresh-root repeats identical); structural in-slice await-site traversal ledger = 16 (...)
VERDICT ASYNC C6 tail-chaining shapes: proposeRoutingFromAssignments (deprecated, test-only) pays a constant +2-init chaining tax ...; runAutoAdaptFromEvents wrapper delta = +4 inits over the direct loop call — both once-per-run ns-level shapes on dead/CLI faces
VERDICT ASYNC C7 conditional suspension legs: the auto-loop.ts:89 bandit await is traversed only when some signal carries a modelId (+118 inits ...); ... suspension placement is decision-gated, and every gate is an existing behaviour contract
```

跨职权观察（非候选、非 ID，仅记录；均为行为可见的语义/属地面）：

- **(a) 悬挂-时钟不对称**：每个 .json **文件名**付一次读悬挂
  （:236），但只有**解析成功**的 record 付一次时钟采样（R29-E）——
  悬挂清单逐点支配时钟清单（1 + F_json ≥ 1 + F_parsed）；统一二者 =
  行为面变更（跳读坏名文件 = 输入访问契约变更，R27-F 属地邻域），
  仅记录。
- **(b) extra 通道绑定的调用方全权推论**：extra 信号是否付 :221
  悬挂由**信号自带**的 episodeId 决定（parseObservedSignal 直通），
  运行上下文的 episodeId 不覆盖——R29-E C4 调用方全权在悬挂面上的
  对应事实，仅记录。
- **(c) 域重合定理**：9 个异步导出与排除表 I/O 契约域逐一重合——
  切片不存在「异步但非 I/O」的面，也不存在「I/O 但同步」的面
  （loadProjectBandit 死导出与 saveLearnedRouting 拒绝腿包含在
  异步域内）；因此本轴的任何消除通道必然穿过已保护的 I/O 契约。
- **(d) 尾链税的死面栖息**：+2 inits 链税住在 deprecated test-only
  别名上；+4 inits 包装增量住在 CLI once-per-run 包装上；
  loadProjectBandit 的 `return readBanditFile(...)` 链形住在零调用
  方死导出上（src/ 与 test/ 双侧 grep 复核仍零）——全部自动拒列
  （S1-E-7/8 族）。

### 2.4 逐项定价（5 采样 = 3 × v22.22.2 + 2 × v22.14.0）

```text
已决 promise 单 await:            28.5–29.3 ns/await
异步调用 await inner():           43.3–44.9 ns/call
'return inner()' 链税:            48.3–50.4 ns/call（return-await 差 1.2–3.7 ns）
整轴预算（现实 W1，16 穿越）:      0.69–0.72 µs/run —— 13924–14447× 低于门槛
荒谬档（F_json=100 + S_bound=100，211 穿越）: 9.1–9.5 µs —— 1056–1095×
机器份额:                         每穿越包裹 ~10¹–10²µs 真实 I/O，机器 ~0.1%
```

结构判读：(i) 全部悬挂挂在 once-per-run I/O 腿（§2.1 位点普查 +
C2/C3/C5 清单定理）；(ii) 每个穿越必然伴随一次已被历轮定价的 I/O
（appendFeedback 59–68µs（R29-J 带）、loadLearnedRouting ~80µs、
bandit 事务 0.65–1.07ms（R20-E 带））——机器份额 ~0.1%，即便悬挂数
增两个数量级，先越线的是 I/O 不是机器；(iii) 切片内可控份额全谱
0.69–9.5 µs/run（现实到荒谬），距 ≥10 ms 门槛 3–4 个数量级；(iv)
全部消除/重排形态撞已关闭条目或契约面（§2.6）。两 Node 版本交叉
不可分辨（计数逐位相同、定价带重叠）。

### 2.5 三重收口

- **频率端**：await 位点仅 19 处（普查钉死），全部挂 once-per-run
  收集/载入/提案管线；逐项循环以 F_json（现实个位）与 S_bound
  （现实个位）为界；CPU 热面（每任务/每事件处理）零悬挂（C1）。
- **单价端**：单穿越 ~29–45 ns；现实全轴 0.69–0.72 µs/run；荒谬档
  211 穿越也仅 9.1–9.5 µs = >10³× 低于门槛。
- **属地端**：机器单价实现体 = V8 await/promise 机器（执行基底，
  R27-H face 属地拒列）；同步化 = X0-4 公开 Promise 签名 + 真实
  I/O 物理不可行；并行化/攒批/重排 = S1-E-5 / S4-E-2 / S5-G-5 /
  S6-J-4 / S5-G-4 已关闭条目或崩溃前缀契约；链税栖息死面
  （S1-E-7/8 族）。**不产生候选。**

### 2.6 第三十组枚举表

方法：本轮代码侧透镜「**谁悬挂、悬挂几次、机器费多少**」：对第三十
遍全量重读的 10 个源文件逐函数问 (a) 哪些位置的成本/行为随悬挂数与
位置变动，(b) 变动处有无等价可削形态；同时按惯例对逐字新点名位置
做换名检查（对照 EXCLUSIONS 全表 + 二十九轮 E 报告 + 十九轮空枚举
面 + R30-A/R30-B 兄弟轴；提交前快进合入的 R30-C/R30-D 轴事后复对
一遍，枚举表零行受影响——异属地异坐标，见 §2.2）。扫描结果：
**零个可立项形态**（均未铸新 ID）：

| 形态（本轮第三十遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| ingest per-file readFile 并行化（:236 → Promise.all） | 重叠 F 次 I/O 延迟 | 换名重提拒列：S1-E-5 逐字（fd 风险 + 非复杂度下降） | S1-E-5 |
| persistSignals∥updateProjectBandit / 跨腿编排重叠（:87/:89/:108） | 重叠两腿 I/O | 换名重提拒列：S4-E-2 逐字（317–443µs + 双故障错误非确定）+ Promise.all 家族 | S4-E-2 / S2-J-10 / S4-J-2 / S7-G-5 / S7-I-3 |
| appendFeedback 逐行 await 攒批（:221） | 省 S_bound−1 次悬挂 + 合并写 | S5-G-5 / S6-J-4 族：逐消息持久性 + read-your-writes 屏障；机器份额 39 inits × ~30ns = µs 级 | S5-G-5 / S6-J-4 |
| 公开异步面同步化（9 导出去 async） | 每面 ~40–100ns 机器 | X0-4 公开 Promise 签名变更 + 真实 I/O 腿物理不可同步化；S7-H-1 在假异步链上已裁同类且 ns 级 | X0-4 / S7-H-1 |
| 尾链 tick 消除（runAutoAdaptFromEvents:283 / proposeRoutingFromAssignments:161 / loadProjectBandit:30 的 `return asyncFn()` 改 return-await 或去 async） | +2~+4 inits ≈ 100–200ns once-per-run | 死面/CLI 面栖息（(d) 观察）：deprecated 别名 test-only、loadProjectBandit 零调用方、包装每进程一次；return-await 差实测 1.2–3.7ns | S1-E-7/8 族 |
| await 腿重排/提升（:87→:89→:108 顺序变换） | 无复杂度下降 | 行为可见发散：持久化顺序是崩溃前缀契约（signals 先于 bandit 先于 propose）；S5-G-4 族 | S5-G-4 / S3-G-4 邻域 |
| readFile/readdir `.catch` 链剥离（:229/:236） | 每文件一个 promise 分配 | fail-soft 跳过契约（坏文件/缺目录静默跳过）实施点；R17-E 异常面已价错误腿 | R17-E / S1-E-4 邻域 |
| bandit mkdir 每写提升（:79） | 每事务一次 mkdir | 换名重提拒列：S6-E-3 逐字（22.7–23.7µs；自愈语义） | S6-E-3 |
| :190/:193 双 save 位点合并 / 无变更跳写 | 每提案一次 save | 换名重提拒列：S2-E-1（无变更跳写侵蚀保存时机）/ S2-E-4 / X0-3 保存时机语义 | S2-E-1 / S2-E-4 / X0-3 |
| V8 await 机器本体加速（悬挂单价 ~29ns 的实现体） | 全轴 0.7µs | 属地拒列：执行基底（R27-H face 邻域）；引擎版本面 S6-I-2 族 | R27-H / S6-I-2 |
| 漂移带来的新悬挂位点 / 新放大器 | — | 不存在：位点普查 19 处钉死；切片三十遍零漂移；独占分支测量期零快进、提交前两笔快进均 docs-only（src 0 行，冻结复验） | — |

**提议排除 ID：无（空枚举未铸任何 S30-E-\*，亦未向 EXCLUSIONS 新增
轴昵称行——异步悬挂点拓扑 / 每 run 悬挂清单面为 observation-only
轴，按令由父代 plan-ban）。**

## 3. 逐文件复核

| 文件 | 第三十遍悬挂透镜下的复核 | 结论 |
| --- | --- | --- |
| `auto-loop.ts` | 19 处位点中的 10 处宿主；C2/C3 仿射清单定理宿主（14 + 30·F_json；39/绑定行）；C5 全 W1 穿越清单 16 的 12 处贡献者；:89/:108 条件腿门控见证（C7）；S2-E-1 / X0-3 / S5-E-5 / S1-E-4/5 未触 | 无候选 |
| `signals.ts` | 同步域（零 await）；collect 双通道 + parse 全部零 promise 分配（C1）；S9-E-2 / S1-E-1 未触 | 无候选 |
| `from-episode.ts` | 4 处位点宿主（:104/:105/:137/:170）；W2-N 零结局腿免悬挂见证（C4 = 空异步函数逐位同价）；deprecated 别名 +2 链税（C6，test-only）；PWL 腿身份未触 | 无候选 |
| `learned-routing.ts` | 1 处位点（:121）；applyLearnedRouting 同步域零悬挂（C1，RTL 关闭维持）；saveLearnedRouting 拒绝腿 = 异步域死写面见证；S8-E-1 / S13-B-1 零触碰 | 无候选 |
| `bandit-store.ts` | 4 处位点宿主；:89 条件穿越 +118 inits 整事务定价（C7）；loadProjectBandit 死导出（零调用方 grep 复核）链形自动拒列；S2-E-3/4 / S6-E-3/4 维持 | 无候选 |
| `diagnostics.ts` | 同步域零悬挂零 promise（C1）；GS 关闭不重价 | 无候选 |
| `task-success.ts` | 同步域（C1 双面见证）；V2 语义未触（RVL 关闭） | 无候选 |
| `signatures.ts` / `patterns.ts` / `attribution.ts` | 同步域零悬挂；test-only 面复核仍零生产调用方；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核：

```bash
# 本 VM: shell 默认 node v22.14.0（/exec-daemon 前置于 PATH）；
# engines >=22.19.0 合规版本 v22.22.2 经 ~/.nvm 提供（gate 与
# v22.22.2 批次以该 PATH 前置调度）；v22.14.0 对照批次以
# /exec-daemon/node node_modules/tsx/dist/cli.mjs 调度。
# 本环境无预装依赖：pnpm install --frozen-lockfile（锁文件未改）。
pnpm gate
# typecheck ✓ + lint ✓ + test ✓ + build ✓ （GATE_EXIT=0）
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（计数与 R14-E..R29-E 逐一相同）

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.22.2 与 v22.14.0 均过）
```

锚点脚本 6 次主运行 + 6 次 cmp25 对调全过（§1）；悬挂轴套件
（`/tmp/r30e-async.mts`，全文见附录 A）共 **5 次运行**（3 ×
v22.22.2 + 2 × v22.14.0），每次 **27/27 sanity 全过**（域断言 ×22 +
仿射清单等差断言 ×2 + 全新根确定性断言 ×9 组内含 + 免悬挂等价断言
×2 + 条件腿断言），全部 VERDICT 行跨 5 次运行**且跨两个 Node 版本**
逐位一致（PromiseHook 计数常数 14/30/39/4/415/+2/+4/+118 两引擎
逐一相同）；定价段 5 采样区间见 §2.4 与附录 A。

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round30-r30e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R29-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件
（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 仅
`docs/reports/sota-opt/round-30/R30-E.md`）。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2.6 空枚举，逐形态有理由；第三十遍以「谁悬挂、
  悬挂几次、机器费多少」透镜普查新点名的全部维度落进换名重提区
  （S1-E-5 / S4-E-2 / S5-G-5 / S6-J-4 / S6-E-3 / S2-E-1/4 / X0-3 /
  S5-G-4）、属地区（V8 await 机器 = 执行基底 / R27-H 邻域）、契约面
  （X0-4 公开 Promise 签名 / 崩溃前缀顺序 / fail-soft 跳过）或死面
  区（链税全部栖息 deprecated/零调用方/once-per-run 面）；新轴本身
  以「无候选」收口，未发现第二十四个机制类）。
- 剩余赢家：无（整轴现实档 0.69–0.72 µs/run，荒谬档 211 穿越也仅
  9.1–9.5 µs = >10³× 低于门槛；CPU 温态面即使清零也距门槛 756–774×
  （本构成）/ 659–674×（cmp25 构成）；每穿越包裹的 I/O 以 ~10³×
  支配机器份额）。
- 矩阵空洞：无（调用位点行号与 R8-E..R29-E 逐一相同；配置态 live
  面与载入面锚点 12 采样中 apply 12/12 带内，空策略/load 两格共
  4 个越带采样全部集中于主夹具批且 cmp25 十二格零越带——零 diff +
  VERDICT 逐位一致下按 R28-E/R29-E 位置噪声同例读价签；双 Node
  版本交叉不可分辨；cmp25 种子对调 6/6 VERDICT 逐字相同、总量 6/6
  带内；diagnose 段 2/6 采样 1.32 = 带下缘外 0.01 µs 表显粒度擦边，
  只读价签）。
- 悬挂假说的双侧完成（「异步机器里藏着未测的成本洞或语义洞」）：
  成本洞以清单定理 + 逐项定价关闭（19 位点、16 穿越/现实 W1、
  ~29–45ns/穿越，全谱 µs 级 once-per-run）；语义洞以域普查关闭且
  **有实洞**——悬挂-时钟不对称（.json 名 vs 解析成功）、extra 通道
  绑定的调用方全权推论、异步域与 I/O 契约域的重合定理、链税的死面
  栖息——全部为行为可见的契约/属地面（记录为 §2.3 (a)–(d)，非
  候选）。「执行机器」维度自此在 E 切片收口：悬挂拓扑（本轮）×
  时钟来源（R29-E）× 并发面（R20-E）× 执行层级（R18-E）。
- S13-B-1 边界：apply 本体零触碰（且本轮 C1 证其同步域零悬挂），
  重开条件未触发。S8-E-1 零去重分析。S9-E-2 未触碰。PWL/RVL/RTL/
  CG/JB/NVG/GS 未复铸（§2.2 逐轴区分）。
- 不移植清单：R30-A call-stream ordering / dispatch-history face
  （跨调用到达序——本轴为调用内结构，已划界）、R30-B scale-cube
  second-order interaction face（N×M/N×L/M×L 联角——本轮 F/S 剂量
  仅证切片自身清单仿射性，无跨规模联角）、R30-C input
  object-representation face / **REPRX**（提交前快进合入——引擎级
  输入表示形态，routing 属地；本轴 PromiseHook 计数对隐藏类/表示
  定义盲，夹具单一构造形态未剂量表示，已划界 §2.2）、R30-D
  approval-profile constraint-list geometry face（提交前快进
  合入——`ApprovalProfile` 双列表多重集几何，adaptation 属地；E 切片
  唯一触点 auto-loop.ts:93 `isAutoAdaptEnabled()` 为纯同步 env 标量
  读，零列表读写，已划界 §2.2）、R29-E wall-clock sampling
  topology / timestamp-provenance face（仅引作不对称对照）、R29-A
  input reference-identity / aliasing-topology face、R29-B
  learned-policy container face、R29-C process workload-composition
  face / **PAIRX**、R29-I input-file JSON serialization-form face、
  R29-J durable-artifact content-spelling face / **CSPELL**、R28-J
  **ZREP**、R28-E **PWL**、R28-A..R28-H 与更早各无名面、以及全部
  已名轴（FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN /
  KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB /
  OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX /
  RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX /
  CSPELL）——逐一定位核过，与本轴对象机制无交集。R30-F / R30-G
  在飞（提交前快进的派单），未读、未等待、未移植。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R29-E §6（含 R14-E 规模判据、R15-E
分配/GC/噪声地板判据、R16-E 累积状态与载荷形态判据、R17-E 异常面与
构成面判据、R18-E 执行层级与字符宽度判据、R19-E 表示面与次序面判据、
R20-E 并发面判据、R21-E 标识符几何判据、R22-E 数值值类判据、R23-E
消费级结构判据、R24-E 铸造级连接判据、R25-E 同键链态判据、R26-E
终止腿判据、R27-E 裁决格判据、R28-E 提案腿判据、R29-E 采样拓扑
判据），并新增一条可标定判据：**(m) 悬挂拓扑判据**——若切片获得
每任务/每事件级 await 位点（脱离 once-per-run I/O 腿，位点普查
§2.1 的 19 处需重证）、或 F_json / S_bound 生产规模增至使穿越机器
越过两位数 ms（需 ~10⁵ 穿越——届时 I/O 面先越线 ~10³× 须一并
重证）、或任一公开异步面被改签同步（X0-4 撤销）、或持久化顺序契约
（:87→:89→:108）被正式放宽（S4-E-2 / S5-G-4 被引裁决推翻）、或
引擎 await 机器单价增长 ≥2 个数量级（执行基底重证）、或 S1-E-5 /
S5-G-5 / S6-E-3 任一被引裁决被正式推翻，或切片/调用面出现实际代码
漂移，优先复跑悬挂轴双版本套件。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

临时脚本 sha256（测量完成后已从 /tmp 删除，正文与本附录为其存档）：

```text
7beb2f2f9885b97503e0328e65e133bd11a78127aadae82fb3fcce78df84b4fc  /tmp/r30e-anchor.mts
dee381f0db252022e2fea246767488948dfd540063a361cb27d2ad6b22bcaa6c  /tmp/r30e-async.mts
```

主锚点脚本全文（`/tmp/r30e-anchor.mts`，`npx tsx /tmp/r30e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R30-E；
`NOW = "2026-08-26T03:45:00.000Z"`；SECTION 1 的 seeds
`0xe30e01`（collect）/ `0xe30e02`（routed）/ `0xe30e04`（signals）——
拒绝采样：collect/routed 角首选一次通过（collected=7>3、
outcomes=3>0），signals 角 `0xe30e03`（diagnoseGroups=0）拒绝、
`0xe30e04` 通过（diagnoseGroups=1）；探针三元组 k=1..12 由 `probe`
开关打印（01: (7,2,0) / 02: (12,3,0) / 03: (17,5,0) / 04: (14,1,1) /
05: (17,2,1) / 06: (11,1,0) / 07: (9,1,0) / 08: (13,5,0) / 09:
(10,1,1) / 0a: (13,4,1) / 0b: (18,6,1) / 0c: (16,0,0)）；SECTION 2
的 `mkdtemp` 前缀 `r30e-fresh-`/`r30e-reg-` 与 detector identity
`r30e-anchor`。另加 CLI 开关 `probe` 与 `cmp25`（置换为 R25-E 的
NOW（`2026-08-25T17:30:00.000Z`）与 seeds `0xe25e01/02/08`、前缀
`r30e-cmp25-`、identity `r30e-anchor-cmp25`——种子/NOW/前缀经模式
常量 `CMP25` 参数化，语义与 R28-E/R29-E cmp25 开关逐一相同）。其余
逐字同 R10-E 附录 A。

6 次主锚点运行原始区间（3 × v22.22.2 + 3 × v22.14.0）：

```text
SLICE-CPU 总量:
  v22.22.2: 13.0 / 13.2 / 13.1 us
  v22.14.0: 12.9 / 13.2 / 12.9 us
collect:    8.3-8.5 us（collected=7 构成）   outcomes: 2.7-3.0 us（outcomes=3 构成）
diagnose:   1.02-1.16 us（diagnoseGroups=1 构成） bandit:  0.7-0.8 us
apply(non): 260-297 ns/task    apply(空): 112-150 ns/task（144/150 两高位见 §1 价签）
load fresh: 19.9-48.7 us（10/12 带内；48.7/19.9 双侧个体均在 v22.14.0 批）
load real:  78.4-90.7 us + 一次 145.1（11/12 带内；145.1 为 v22.14.0 首进程）
VERDICT 行: 6/6 逐位一致；sanity 3/3 x6 全过
```

6 次 R25-E 种子比对运行（cmp25）原始值（3 × v22.22.2 + 3 × v22.14.0）：

```text
总量: 15.1 / 15.0 / 15.0 | 14.8 / 15.2 / 14.9 us
  —— vs 默认带 14.7-15.2：6/6 带内
collect 11.4-11.7 / outcomes 1.3-1.4 / diagnose 1.32-1.37 / bandit 0.8
  —— vs R25-E 11.3-11.9 / 1.3-1.5 / 1.33-1.46 / 0.8：collect 与
  outcomes 全带内；diagnose 2/6 采样 1.32 低于带下缘 0.01 us
  （表显粒度级单侧擦边，§1 价签记录）
apply(non) 264-285 / apply(空) 112-116 / load fresh 25.2-34.0 /
load real 80.9-86.2 —— cmp25 批全部格 6/6 带内
VERDICT 行: 6/6 逐位一致且与 R25-E / R26-E / R27-E / R28-E / R29-E
夹具行逐字相同
```

悬挂轴套件 5 次运行原始区间（3 × v22.22.2 + 2 × v22.14.0；
v22.14.0 批次以 `/exec-daemon/node node_modules/tsx/dist/cli.mjs`
调度）：

```text
p1 已决 await:   29.3 / 29.1 / 28.5 | 28.5 / 28.7 ns/await
p2 异步调用:     44.7 / 44.9 / 43.3 | 44.4 / 44.5 ns/call
p3 链税:         49.2 / 48.3 / 50.4 | 49.2 / 48.7 ns（return-await 差 2.5/3.6/2.1|1.2/3.3）
p4 现实预算:     0.72 / 0.72 / 0.69 | 0.71 / 0.71 us/run（13924-14447x）
p4 荒谬档:       9.4 / 9.5 / 9.1 | 9.4 / 9.4 us（1056-1095x）
计数常数:        14 + 30xF_json；39/绑定行；noop=4；W1=415；
                 alias +2；wrapper +4；bandit +118 —— 5/5 运行且
                 双引擎逐位一致
VERDICT 行:      5/5 逐位一致（跨版本亦逐位一致）；sanity 27/27 x5 全过
```

悬挂轴套件全文（`/tmp/r30e-async.mts`，`npx tsx /tmp/r30e-async.mts`）：

```ts
/**
 * R30-E new completeness axis: async suspension-point topology / per-run
 * suspension-manifest face — WHERE the in-slice `await` sites live (19 sites
 * in 4 files; 6 files are entirely synchronous), HOW MANY suspension points
 * one collection run traverses per leg (as an affine manifest in the number
 * of .json names F_json and episode-bound signals S_bound), WHICH exported
 * faces are sync vs async (realm census), and what the pure async machinery
 * (promise allocations + microtask ticks) costs with the I/O it wraps
 * excluded, as the controlled variable.
 *
 * What prior passes did NOT control: S1-E-5 / S4-E-2 closed two
 * PARALLELIZATION forms (Promise.all shapes); S5-G-5 / S6-J-4 closed write
 * batching; S7-H-1 closed async-machine SYNC-IFICATION for the H slice's
 * fake-async chain; R20-E controlled CROSS-PROCESS contention; R21-H (H area)
 * controlled scheduling-window counts + external queue-content dosing on a
 * chain whose bodies are synchronous. No E pass ever counted the suspension
 * sites, pinned the per-leg suspension manifest of a real-I/O collection run,
 * or priced the machinery share. Levers, not variables: event composition
 * pinned (R17-E), clock sampling pinned (R29-E — note the deliberate
 * asymmetry witness: suspensions = 1 + F_json, clock samples = 1 + F_parsed),
 * container shapes pinned (R29-B), store zero-state pinned (R28-J ZREP),
 * arrival order of separate calls pinned (R30-A face, not ported).
 *
 * Instrumentation: async_hooks PromiseHook init counter (deterministic
 * PromiseHook-visible promise-init counts per fixed code path per Node
 * version; all assertions are equality / equality-of-differences so harness
 * constants cancel). Hook is disabled during pricing. Segments (argv[2]
 * optional filter): C (census, deterministic VERDICT lines) | P (pricing,
 * BENCH/OBS lines).
 */
import { createHook } from "node:async_hooks";
import { mkdtemp, writeFile, readFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { performance } from "node:perf_hooks";
import {
  collectSignalsFromEvents,
  collectSignalsFromSubagentRun,
  parseObservedSignal,
  scoreTaskResult,
  scoreUserAnswer,
  type ObservedSignal
} from "/workspace/src/learning/signals.js";
import { runAutoAdaptLoop, runAutoAdaptFromEvents } from "/workspace/src/learning/auto-loop.js";
import {
  proposeRoutingFromOutcomes,
  proposeRoutingFromAssignments,
  outcomesFromRoutedRun
} from "/workspace/src/learning/from-episode.js";
import {
  applyLearnedRouting,
  loadLearnedRouting,
  saveLearnedRouting,
  parseLearnedRoutingPolicy,
  routingPolicyContent,
  routingPolicyIdentity,
  stableProjectKey,
  policyFromAssignments,
  type LearnedRoutingPolicy
} from "/workspace/src/learning/learned-routing.js";
import { loadProjectBandit, updateProjectBandit } from "/workspace/src/learning/bandit-store.js";
import { diagnoseModelProjectIssues } from "/workspace/src/learning/diagnostics.js";
import { taskSuccessFromResult, taskSuccessFromExitCode } from "/workspace/src/learning/task-success.js";
import { detectRepeatedPatterns } from "/workspace/src/learning/patterns.js";
import { attributeToBoundary, findNegativeControlMarker } from "/workspace/src/learning/attribution.js";
import { createSignature, compareSignatures } from "/workspace/src/learning/signatures.js";
import { feedbackLogPath } from "/workspace/src/feedback/store.js";
import { oneHotDistribution } from "/workspace/src/routing/catalog-model.js";
import type { Event } from "/workspace/src/run/events.js";
import type { EpisodeId, ProjectId } from "/workspace/src/domain/ids.js";

const SEG = process.argv[2] ?? "CP";
const PROJECT_ID = "prj_asy00001" as ProjectId;
const EPISODE_ID = "epi_asy00001" as EpisodeId;

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}`);
  }
}

/* ---------- PromiseHook-visible promise-init counter ---------- */
let promiseInits = 0;
const hook = createHook({
  init(_asyncId, type) {
    if (type === "PROMISE") promiseInits += 1;
  }
});
function syncDelta(fn: () => unknown): { value: unknown; delta: number } {
  hook.enable();
  const before = promiseInits;
  const value = fn();
  const delta = promiseInits - before;
  hook.disable();
  return { value, delta };
}
async function asyncDelta(fn: () => Promise<unknown>): Promise<number> {
  hook.enable();
  const before = promiseInits;
  await fn().catch(() => undefined);
  const delta = promiseInits - before;
  hook.disable();
  return delta;
}

/* ---------- fixtures ---------- */
function collectEvents(withRouted: boolean): Event[] {
  const out: unknown[] = [
    { type: "PROJECT_DISCOVERED", payload: { project: { id: PROJECT_ID, rootPath: "/tmp/proj-asy" } } },
    { type: "USER_ANSWER", runId: "run_asy00001", payload: { answer: "no, revert this" } }
  ];
  if (withRouted) {
    for (let i = 1; i <= 5; i += 1) {
      out.push({
        type: "MODEL_ROUTED",
        runId: "run_asy00001",
        payload: {
          taskId: `tsk_0000000${i}`,
          model: "m-bad",
          role: "actor",
          eligibleModels: ["m-bad", "m-good"],
          behaviorDistribution: oneHotDistribution(["m-bad", "m-good"], "m-bad"),
          family: "edit",
          modelVersion: "v1",
          featureVersion: "fv1",
          agentRole: "worker"
        }
      });
      out.push({
        type: "CHILD_MESSAGE",
        runId: "run_asy00001",
        payload: {
          message: {
            type: "TASK_RESULT",
            taskId: `tsk_0000000${i}`,
            runId: "run_asy00001",
            outcome: "FAILURE",
            verification: { kind: "FAILED" },
            summary: "plain verdict probe body",
            evidenceIds: ["evd_00000001"]
          }
        }
      });
    }
  }
  return out as Event[];
}
const PI_VALID_TWO_RESULTS = JSON.stringify({
  status: "completed",
  request: { agent: "tester" },
  results: [{ agent: "tester", messages: [] }, { messages: [] }]
});
const PI_MALFORMED = "{not json";
const PI_NONRECORD = "[1,2,3]";

/** piDir with a parameterized number of .json names + optional extra .txt files. */
async function makePiDir(jsonSpecs: readonly ("valid" | "malformed" | "empty" | "nonrecord")[], txtCount: number): Promise<string> {
  const dir = await mkdtemp(join(tmpdir(), "r30e-asy-pi-"));
  for (let i = 0; i < jsonSpecs.length; i += 1) {
    const spec = jsonSpecs[i]!;
    const body =
      spec === "valid" ? PI_VALID_TWO_RESULTS : spec === "malformed" ? PI_MALFORMED : spec === "empty" ? "" : PI_NONRECORD;
    await writeFile(join(dir, `f${String(i).padStart(2, "0")}-${spec}.json`), body, "utf8");
  }
  for (let t = 0; t < txtCount; t += 1) {
    await writeFile(join(dir, `t${t}-skipped.txt`), PI_VALID_TWO_RESULTS, "utf8");
  }
  return dir;
}

function extraSignal(n: number, bound = true): ObservedSignal {
  return {
    source: "subagent",
    kind: "peer",
    projectId: PROJECT_ID,
    score: 55,
    boundary: "review",
    summary: `extra ${bound ? "bound" : "unbound"} signal ${n}`,
    createdAt: "2026-08-26T03:45:00.000Z",
    ...(bound ? { episodeId: EPISODE_ID } : {}),
    evidenceIds: []
  } as unknown as ObservedSignal;
}

/** Kill-switch collect-only leg with F_json dosing (no episodeId, no modelId anywhere). */
async function collectLeg(jsonSpecs: readonly ("valid" | "malformed" | "empty" | "nonrecord")[], txtCount: number): Promise<number> {
  const stateRoot = await mkdtemp(join(tmpdir(), "r30e-asy-root-"));
  const piDir = await makePiDir(jsonSpecs, txtCount);
  process.env.SPARKLE_AUTO_ADAPT = "0";
  const delta = await asyncDelta(() =>
    runAutoAdaptLoop({
      stateRoot,
      projectRoot: "/tmp/proj-asy",
      projectId: PROJECT_ID,
      primaryModelId: "m-good",
      events: collectEvents(false),
      subagentRunsDir: piDir
    })
  );
  delete process.env.SPARKLE_AUTO_ADAPT;
  return delta;
}

/** Kill-switch leg with S_bound dosing via episodeId + extraSignals (empty pi dir).
 * withEpisode=true: run context AND extras carry episodeId (extras bound).
 * withEpisode=false: neither does — extras are genuinely UNBOUND (the extra
 * channel binds from the signal's own caller-supplied episodeId, R29-E C4). */
async function persistLeg(extraCount: number, withEpisode: boolean): Promise<number> {
  const stateRoot = await mkdtemp(join(tmpdir(), "r30e-asy-root-"));
  const piDir = await makePiDir([], 0);
  process.env.SPARKLE_AUTO_ADAPT = "0";
  const extras = Array.from({ length: extraCount }, (_, n) => extraSignal(n, withEpisode));
  const delta = await asyncDelta(() =>
    runAutoAdaptLoop({
      stateRoot,
      projectRoot: "/tmp/proj-asy",
      projectId: PROJECT_ID,
      primaryModelId: "m-good",
      ...(withEpisode ? { episodeId: EPISODE_ID } : {}),
      events: collectEvents(false),
      extraSignals: extras,
      subagentRunsDir: piDir
    })
  );
  delete process.env.SPARKLE_AUTO_ADAPT;
  return delta;
}

/* warm-up: one full W1 run + one of each op family on throwaway roots */
{
  const warmRoot = await mkdtemp(join(tmpdir(), "r30e-asy-warm-"));
  const warmPi = await makePiDir(["valid", "malformed"], 1);
  await runAutoAdaptLoop({
    stateRoot: warmRoot,
    projectRoot: "/tmp/proj-asy",
    projectId: PROJECT_ID,
    primaryModelId: "m-good",
    episodeId: EPISODE_ID,
    events: collectEvents(true),
    extraSignals: [extraSignal(0)],
    subagentRunsDir: warmPi
  });
  await loadLearnedRouting(warmRoot, "/tmp/proj-asy");
  await loadProjectBandit(warmRoot, "/tmp/proj-asy");
  await proposeRoutingFromOutcomes({
    stateRoot: warmRoot,
    projectRoot: "/tmp/proj-asy",
    projectId: PROJECT_ID,
    primaryModelId: "m-good",
    outcomes: []
  });
}

/* ================================================================
 * SEGMENT C: realm census + suspension manifests + machinery deltas.
 * ================================================================ */
if (SEG.includes("C")) {
  /* ---- C1: realm census — the CPU-hot faces are synchronous AND allocate zero promises. ---- */
  {
    const events = collectEvents(true);
    const routed = outcomesFromRoutedRun(events);
    const policy: LearnedRoutingPolicy = {
      primaryModelId: "m0",
      avoid: [{ modelId: "m1", family: "edit", reason: "r" }],
      prefer: [{ family: "edit", modelId: "m2" }]
    };
    const sig = createSignature(EPISODE_ID, "execution", { operation: "read" });
    const sigB = createSignature(EPISODE_ID, "execution", { operation: "read", extra: true });
    const syncFaces: readonly [string, () => unknown][] = [
      ["collectSignalsFromEvents", () => collectSignalsFromEvents(events, { projectId: PROJECT_ID })],
      ["collectSignalsFromSubagentRun", () => collectSignalsFromSubagentRun(JSON.parse(PI_VALID_TWO_RESULTS), { projectId: PROJECT_ID })],
      ["parseObservedSignal", () => parseObservedSignal(extraSignal(0))],
      ["scoreTaskResult", () => scoreTaskResult("FAILURE", "FAILED")],
      ["scoreUserAnswer", () => scoreUserAnswer("no, revert this")],
      ["outcomesFromRoutedRun", () => outcomesFromRoutedRun(events)],
      ["diagnoseModelProjectIssues", () => diagnoseModelProjectIssues(collectSignalsFromEvents(events, { projectId: PROJECT_ID }))],
      ["applyLearnedRouting", () => applyLearnedRouting("edit", ["m0", "m1", "m2"], "m1", policy)],
      ["taskSuccessFromResult", () => taskSuccessFromResult("FAILURE", "FAILED", { modelId: "m1" })],
      ["taskSuccessFromExitCode", () => taskSuccessFromExitCode(1, { modelId: "m1" })],
      ["parseLearnedRoutingPolicy", () => parseLearnedRoutingPolicy(routingPolicyContent(policy))],
      ["routingPolicyContent", () => routingPolicyContent(policy)],
      ["routingPolicyIdentity", () => routingPolicyIdentity("/tmp/proj-asy")],
      ["stableProjectKey", () => stableProjectKey("/tmp/proj-asy")],
      ["policyFromAssignments", () => policyFromAssignments("m0", [])],
      ["detectRepeatedPatterns", () => detectRepeatedPatterns([sig, sigB])],
      ["attributeToBoundary", () => attributeToBoundary([{ kind: "execution", count: 2 }])],
      ["findNegativeControlMarker", () => findNegativeControlMarker([sig])],
      ["createSignature", () => createSignature(EPISODE_ID, "execution", { operation: "read" })],
      ["compareSignatures", () => compareSignatures(sig, sigB)]
    ];
    let allSync = true;
    let allZero = true;
    for (const [name, fn] of syncFaces) {
      fn(); // warm
      const { value, delta } = syncDelta(fn);
      if (value instanceof Promise) allSync = false;
      if (delta !== 0) {
        allZero = false;
        console.error(`C1 ${name}: unexpected promise inits ${delta}`);
      }
    }
    check("C1 all 20 CPU-hot faces return non-Promise values", allSync);
    check("C1 all 20 CPU-hot faces allocate ZERO promises (PromiseHook-visible)", allZero);
    void routed;
    console.log(
      "VERDICT ASYNC C1 realm census: the entire CPU-hot surface (collect x2 channels, parse, score x2, outcomes, diagnose, apply, task-success x2, policy parse/print/identity/key/from-assignments, patterns, attribution x2, signatures x2 = 20 exported faces) is synchronous and allocates zero promises; every suspension point in the slice lives on the I/O legs (19 await sites in auto-loop/learned-routing/bandit-store/from-episode; signals/diagnostics/task-success/patterns/attribution/signatures have none)"
    );

    // async realm: the 9 async exports return Promise instances.
    const freshRoot = await mkdtemp(join(tmpdir(), "r30e-asy-realm-"));
    const piDir = await makePiDir([], 0);
    process.env.SPARKLE_AUTO_ADAPT = "0";
    const p1 = runAutoAdaptLoop({
      stateRoot: freshRoot, projectRoot: "/tmp/proj-asy", projectId: PROJECT_ID,
      primaryModelId: "m-good", events: collectEvents(false), subagentRunsDir: piDir
    });
    const p2 = runAutoAdaptFromEvents({ stateRoot: freshRoot, events: [], primaryModelId: "m-good" });
    const p3 = proposeRoutingFromOutcomes({
      stateRoot: freshRoot, projectRoot: "/tmp/proj-asy", projectId: PROJECT_ID, primaryModelId: "m-good", outcomes: []
    });
    const p4 = proposeRoutingFromAssignments({
      stateRoot: freshRoot, projectRoot: "/tmp/proj-asy", projectId: PROJECT_ID, primaryModelId: "m-good", outcomes: []
    });
    const p5 = loadLearnedRouting(freshRoot, "/tmp/proj-asy");
    const p6 = saveLearnedRouting(freshRoot, "/tmp/proj-asy", { primaryModelId: "m0", avoid: [], prefer: [] });
    const p7 = loadProjectBandit(freshRoot, "/tmp/proj-asy");
    const p8 = updateProjectBandit(freshRoot, "/tmp/proj-asy", []);
    const all = [p1, p2, p3, p4, p5, p6, p7, p8];
    check("C1 async exports return Promise instances", all.every((p) => p instanceof Promise));
    let saveRefused = false;
    await p6.catch((error: Error) => {
      saveRefused = /routing\.json is not a live policy store/.test(error.message);
    });
    await Promise.all([p1, p2, p3, p4, p5, p7, p8].map((p) => (p as Promise<unknown>).catch(() => undefined)));
    delete process.env.SPARKLE_AUTO_ADAPT;
    check("C1 saveLearnedRouting rejects with the deprecation refusal (dead write face)", saveRefused);
    console.log(
      "VERDICT ASYNC C1b async realm: 9 async exports (runAutoAdaptLoop, runAutoAdaptFromEvents, proposeRoutingFromOutcomes, proposeRoutingFromAssignments, proposeRoutingFromRoutedEvents, loadLearnedRouting, saveLearnedRouting [deprecated: rejects], loadProjectBandit [dead export], updateProjectBandit) — all Promise-returning; the public async realm is exactly the I/O-contract realm the exclusion table already protects"
    );
  }

  /* ---- C2: ingest suspension manifest — affine in F_json, blind to content and to non-.json names. ---- */
  {
    const mix5 = ["valid", "valid", "malformed", "empty", "nonrecord"] as const;
    const val5 = ["valid", "valid", "valid", "valid", "valid"] as const;
    const val3 = ["valid", "valid", "valid"] as const;
    const val1 = ["valid"] as const;
    const rep = async (specs: readonly ("valid" | "malformed" | "empty" | "nonrecord")[], txt: number): Promise<number> => {
      const a = await collectLeg(specs, txt);
      const b = await collectLeg(specs, txt);
      check(`C2 determinism for F_json=${specs.length},txt=${txt} (fresh-root repeats equal)`, a === b);
      return a;
    };
    const pMix5 = await rep(mix5, 1);
    const pVal5 = await rep(val5, 1);
    const pVal5noTxt = await rep(val5, 0);
    const pVal3 = await rep(val3, 1);
    const pVal1 = await rep(val1, 1);
    const pVal0 = await rep([], 1);
    check("C2 content-blindness: {2 valid,1 malformed,1 empty,1 non-record} costs exactly what 5 valid files cost", pMix5 === pVal5);
    check("C2 non-.json blindness: adding a .txt file changes nothing", pVal5 === pVal5noTxt);
    const k53 = (pVal5 - pVal3) / 2;
    const k31 = (pVal3 - pVal1) / 2;
    check("C2 affine manifest: per-.json-file machinery slope constant (5->3 == 3->1)", k53 === k31 && Number.isInteger(k53) && k53 > 0);
    console.log(
      `VERDICT ASYNC C2 ingest manifest: suspension/machinery cost is affine in F_json — promise inits = ${pVal0} + ${k53} x F_json (collect-only leg); the readFile await at auto-loop.ts:236 is paid once per .json NAME (malformed/empty/non-record .json all pay it; .txt pays nothing) — deliberate asymmetry witness vs the R29-E clock manifest (clock samples = 1 + F_parsed: only successfully PARSED records pay a sample, but every .json NAME pays a read suspension)`
    );
  }

  /* ---- C3: persistSignals suspension manifest — affine in S_bound; unbound signals suspend nothing. ---- */
  {
    const rep = async (extras: number, withEpisode: boolean): Promise<number> => {
      const a = await persistLeg(extras, withEpisode);
      const b = await persistLeg(extras, withEpisode);
      check(`C3 determinism extras=${extras} episode=${withEpisode}`, a === b);
      return a;
    };
    const q5 = await rep(4, true); // S_bound = 1 user + 4 extra = 5
    const q3 = await rep(2, true); // S_bound = 3
    const q1 = await rep(0, true); // S_bound = 1
    const r3 = await rep(3, false); // S_bound = 0 (no episodeId anywhere)
    const r0 = await rep(0, false); // S_bound = 0
    const m53 = (q5 - q3) / 2;
    const m31 = (q3 - q1) / 2;
    check("C3 affine manifest: per-bound-signal machinery slope constant (5->3 == 3->1)", m53 === m31 && Number.isInteger(m53) && m53 > 0);
    check("C3 unbound signals are suspension-free: unbound extras change nothing", r3 === r0);
    console.log(
      `VERDICT ASYNC C3 persist manifest: the appendFeedback await at auto-loop.ts:221 is paid once per episode-BOUND signal (machinery slope ${m53} promise inits per bound row, sequential by design — the batching/parallel forms are S5-G-5/S6-J-4/S4-E-2 closed territory); unbound signals traverse zero suspensions in persistSignals (the loop continues synchronously), and binding on the extra channel follows the signal's own caller-supplied episodeId (R29-E C4 caller-control corollary), not the run context`
    );
  }

  /* ---- C4: suspension-free async legs — early returns equal an empty async function. ---- */
  {
    const noop = async (): Promise<void> => {};
    await noop();
    const dNoop = await asyncDelta(() => noop());
    const stateRoot = await mkdtemp(join(tmpdir(), "r30e-asy-w2n-"));
    const dW2N = await asyncDelta(() =>
      proposeRoutingFromOutcomes({
        stateRoot, projectRoot: "/tmp/proj-asy", projectId: PROJECT_ID, primaryModelId: "m-good", outcomes: []
      })
    );
    const dNoProject = await asyncDelta(() =>
      runAutoAdaptFromEvents({ stateRoot, events: [], primaryModelId: "m-good" })
    );
    check("C4 W2-N zero-outcome leg machinery == empty async function", dW2N === dNoop);
    check("C4 runAutoAdaptFromEvents no-project leg machinery == empty async function", dNoProject === dNoop);
    console.log(
      `VERDICT ASYNC C4 suspension-free async legs: proposeRoutingFromOutcomes(outcomes=[]) and runAutoAdaptFromEvents(no PROJECT_DISCOVERED) return before any await — PromiseHook-visible machinery identical to an empty async function (${dNoop} inits); their bodies (policy derivation / event prescan) are fully synchronous`
    );
  }

  /* ---- C5: full W1 failing run — total machinery deterministic; structural in-slice traversal ledger. ---- */
  {
    const runOnce = async (): Promise<{ delta: number; created: boolean; fed: boolean }> => {
      const stateRoot = await mkdtemp(join(tmpdir(), "r30e-asy-w1-"));
      const piDir = await makePiDir(["valid", "valid", "malformed", "empty", "nonrecord"], 1);
      let created = false;
      const delta = await asyncDelta(async () => {
        const res = await runAutoAdaptLoop({
          stateRoot,
          projectRoot: "/tmp/proj-asy",
          projectId: PROJECT_ID,
          primaryModelId: "m-good",
          events: collectEvents(true),
          subagentRunsDir: piDir
        });
        created = res.created;
      });
      const fed = await readFile(feedbackLogPath(stateRoot), "utf8").then(() => true).catch(() => false);
      return { delta, created, fed };
    };
    const a = await runOnce();
    const b = await runOnce();
    check("C5 W1 proposes on both fresh roots", a.created && b.created);
    check("C5 W1 no feedback rows (no episodeId anywhere)", !a.fed && !b.fed);
    check("C5 W1 total machinery deterministic across fresh-root repeats", a.delta === b.delta);
    console.log(
      `VERDICT ASYNC C5 full W1 failing run: total PromiseHook-visible promise inits = ${a.delta} per run (2/2 fresh-root repeats identical); structural in-slice await-site traversal ledger = 16 (:82 ingest + :229 readdir + :236 x5 json reads + :87 persist + :89 bandit + :45 read-bandit + :14 readFile + :79 mkdir + :80 writeFile + :108 propose + :166 registry load + :193 registry save; :221 x0 — no bound signals); every traversal is attached to a real I/O operation of ~10^1-10^2 us already priced/protected by the exclusion table`
    );
  }

  /* ---- C6: tail-chaining shapes (deprecated alias + from-events wrapper) — constant machinery tax. ---- */
  {
    const stateRoot = await mkdtemp(join(tmpdir(), "r30e-asy-alias-"));
    const direct = await asyncDelta(() =>
      proposeRoutingFromOutcomes({
        stateRoot, projectRoot: "/tmp/proj-asy", projectId: PROJECT_ID, primaryModelId: "m-good", outcomes: []
      })
    );
    const alias = await asyncDelta(() =>
      proposeRoutingFromAssignments({
        stateRoot, projectRoot: "/tmp/proj-asy", projectId: PROJECT_ID, primaryModelId: "m-good", outcomes: []
      })
    );
    const aliasTax = alias - direct;
    check("C6 deprecated alias pays a constant positive chaining tax", aliasTax > 0);

    const legRoot1 = await mkdtemp(join(tmpdir(), "r30e-asy-fe1-"));
    const legRoot2 = await mkdtemp(join(tmpdir(), "r30e-asy-fe2-"));
    const piA = await makePiDir([], 0);
    process.env.SPARKLE_AUTO_ADAPT = "0";
    const dLoop = await asyncDelta(() =>
      runAutoAdaptLoop({
        stateRoot: legRoot1, projectRoot: "/tmp/proj-asy", projectId: PROJECT_ID,
        primaryModelId: "m-good", events: collectEvents(false), subagentRunsDir: piA
      })
    );
    // runAutoAdaptFromEvents derives projectRoot from PROJECT_DISCOVERED then tail-calls the loop.
    // Its default subagentRunsDir differs (projectRoot/.pi/subagents/runs, ENOENT) — keep the pi dir
    // ENOENT-equivalent by pointing the loop at an empty dir vs the wrapper at a nonexistent one:
    // both traverse readdir exactly once (ENOENT catch leg vs empty list), machinery equal by C2 base.
    const dWrapper = await asyncDelta(() =>
      runAutoAdaptFromEvents({
        stateRoot: legRoot2, events: collectEvents(false), primaryModelId: "m-good"
      })
    );
    delete process.env.SPARKLE_AUTO_ADAPT;
    const wrapperTax = dWrapper - dLoop;
    console.log(
      `VERDICT ASYNC C6 tail-chaining shapes: proposeRoutingFromAssignments (deprecated, test-only) pays a constant +${aliasTax}-init chaining tax over the direct call ('return innerAsync()' from an async body); runAutoAdaptFromEvents wrapper delta = ${wrapperTax >= 0 ? "+" : ""}${wrapperTax} inits over the direct loop call (prescan is sync; tail 'return runAutoAdaptLoop(...)' chains one extra resolve) — both once-per-run ns-level shapes on dead/CLI faces`
    );
  }

  /* ---- C7: bandit leg gating — the :89 await is traversed only when a modelId is present. ---- */
  {
    const runLeg = async (withModel: boolean): Promise<number> => {
      const stateRoot = await mkdtemp(join(tmpdir(), "r30e-asy-band-"));
      const piDir = await makePiDir([], 0);
      process.env.SPARKLE_AUTO_ADAPT = "0";
      const events: unknown[] = [
        { type: "PROJECT_DISCOVERED", payload: { project: { id: PROJECT_ID, rootPath: "/tmp/proj-asy" } } },
        { type: "USER_ANSWER", runId: "run_asy00001", payload: { answer: "no, revert this" } }
      ];
      if (withModel) {
        events.push({
          type: "MODEL_ROUTED",
          runId: "run_asy00001",
          payload: { taskId: "tsk_00000001", model: "m-bad", role: "actor" }
        });
        events.push({
          type: "CHILD_MESSAGE",
          runId: "run_asy00001",
          payload: {
            message: {
              type: "TASK_RESULT", taskId: "tsk_00000001", runId: "run_asy00001",
              outcome: "FAILURE", verification: { kind: "FAILED" }, summary: "x", evidenceIds: []
            }
          }
        });
      }
      const delta = await asyncDelta(() =>
        runAutoAdaptLoop({
          stateRoot, projectRoot: "/tmp/proj-asy", projectId: PROJECT_ID,
          primaryModelId: "m-good", events: events as Event[], subagentRunsDir: piDir
        })
      );
      delete process.env.SPARKLE_AUTO_ADAPT;
      return delta;
    };
    const with0 = await runLeg(false);
    const with1 = await runLeg(true);
    check("C7 bandit leg gates on modelId presence (machinery strictly larger when traversed)", with1 > with0);
    console.log(
      `VERDICT ASYNC C7 conditional suspension legs: the auto-loop.ts:89 bandit await is traversed only when some signal carries a modelId (+${with1 - with0} inits for the full bandit transaction incl. lock machinery); the :108 propose await only on the failing leg; the :190/:193 saves are an exactly-one pair — suspension placement is decision-gated, and every gate is an existing behaviour contract (S2-E-1/S2-E-4 protected write-always semantics)`
    );
  }
}

/* ================================================================
 * SEGMENT P: pricing (hook disabled; landing-bar arithmetic).
 * ================================================================ */
if (SEG.includes("P")) {
  console.log("--- BENCH ASYNC pricing (PromiseHook disabled) ---");
  const RESOLVED = Promise.resolve();
  async function spinAwait(n: number): Promise<void> {
    for (let i = 0; i < n; i += 1) await RESOLVED;
  }
  async function inner(): Promise<number> {
    return 1;
  }
  async function outerChain(): Promise<number> {
    return inner();
  }
  async function outerAwait(): Promise<number> {
    return await inner();
  }
  function benchAsyncNs(fn: () => Promise<unknown>, reps: number): Promise<number> {
    return (async () => {
      await fn();
      const t0 = performance.now();
      for (let i = 0; i < reps; i += 1) await fn();
      return ((performance.now() - t0) / reps) * 1e6;
    })();
  }
  // p1: per-await price on an already-resolved promise (amortized inside one async frame).
  {
    const N = 1_000_000;
    await spinAwait(1000);
    const t0 = performance.now();
    await spinAwait(N);
    const perAwaitNs = ((performance.now() - t0) / N) * 1e6;
    console.log(`BENCH ASYNC p1 resolved-await price: ${perAwaitNs.toFixed(1)}ns/await (amortized, ${N} awaits)`);
    // p2/p3: async invocation + chaining shapes.
    const innerNs = await benchAsyncNs(() => inner(), 500000);
    const chainNs = await benchAsyncNs(() => outerChain(), 500000);
    const awaitNs = await benchAsyncNs(() => outerAwait(), 500000);
    console.log(
      `BENCH ASYNC p2 async invocation (await inner()): ${innerNs.toFixed(1)}ns/call | p3 chain shapes: 'return inner()' ${chainNs.toFixed(1)}ns vs 'return await inner()' ${awaitNs.toFixed(1)}ns (chaining tax ${(chainNs - innerNs).toFixed(1)}ns, return-await delta ${(awaitNs - chainNs).toFixed(1)}ns/call)`
    );
    // p4: axis budget arithmetic.
    const traversalsRealistic = 16; // C5 ledger (F_json=5, S_bound=0)
    const perTraversal = Math.max(perAwaitNs, innerNs);
    const budgetRealistic = (traversalsRealistic * perTraversal) / 1000;
    const traversalsAbsurd = 11 + 100 + 100; // orchestration+bandit+propose ~11, F_json=100, S_bound=100
    const budgetAbsurd = (traversalsAbsurd * perTraversal) / 1000;
    console.log(
      `BENCH ASYNC p4 axis budget: realistic W1 run = ${traversalsRealistic} in-slice await traversals x ~${perTraversal.toFixed(0)}ns = ${budgetRealistic.toFixed(2)}us/run pure machinery (${Math.round(10000 / budgetRealistic)}x below the >=10000us landing bar even if zeroed); absurd F_json=100 + S_bound=100 = ${traversalsAbsurd} traversals = ${budgetAbsurd.toFixed(1)}us (${Math.round(10000 / budgetAbsurd)}x below) — and each traversed suspension wraps a real I/O op of ~10^1-10^2us (machinery share ~0.1%), so the I/O face crosses the bar orders of magnitude before the machinery does`
    );
  }
}

console.log(`VERDICT total sanity checks: ${checks}, failures: ${failures}`);
if (failures > 0) {
  console.error(`${failures} SANITY CHECK(S) FAILED`);
  process.exit(1);
}
console.log("ALL ASYNC SANITY CHECKS PASSED (deterministic census, no RNG)");
```

MORE_OPTIMA=no
BRANCH=cursor/r30-e-learning-thirtieth-pass-83a1
