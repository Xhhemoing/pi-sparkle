MODEL_SLUG=claude-fable-5-thinking-xhigh

# R30-I：CLI / Pi adapter / config / telemetry 第三十遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `be9449f`（派单独占 tip，含 R30-F 收口与本 R30-I 派单；全部测量在该 tip 构建的 dist 上完成）；运行中独占分支**两次** docs-only 快进：`be9449f` → `274b552`（R30-G「M1 parent-run child-scheduling lattice」空枚举收口 + R30-J 派单）→ `3f80ab6`（R30-H「provenance-resolution pathway face」空枚举收口 + Round 31 开轮 + R31-A 派单），本分支已按派单令**逐次 docs-only rebase 到新 tip**——增量 `be9449f..274b552` 仅 1 个提交全 docs（`git diff --stat` 仅 PROGRESS / round-30/PLAN / R30-G.md 三文件，1196 插入），增量 `274b552..3f80ab6` 仅 1 个提交全 docs（`git diff --stat` 仅 PROGRESS / README / round-30/PLAN / R30-H.md / round-31/PLAN 五文件，1029 插入），两增量对 `src/`、`test/`、`scripts/`、`package.json`、`pnpm-lock.yaml` 均**零行**（`git diff <base>..<tip> -- src test scripts package.json pnpm-lock.yaml | wc -l` = 0 逐次直接复核）——切片与 dist 不受影响（每次 rebase 后 `git diff 8dee7fb..HEAD -- <25 文件>` 重验均为 **0 行**）。快进吸收的 R30-G / R30-H 均为父代理已收口分支（空枚举：R30-G 归档轴「M1 parent-run child-scheduling lattice」属 G 辖区 supervised 引擎的 parent-run 子任务调度格；R30-H 归档轴「provenance-resolution pathway face（溯源解析通道面）」属 H 辖区 critique 链的每合同项溯源举证通道组成——两轴与本轮新轴对象机制均无交集，已列入非移植清单 §0）。docs-only 快进未按生产变更对待：全部测量在 `be9449f` 工作树的字节相同切片上完成，两增量 `src/` 零变化 ⇒ 测量全部有效。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- <25 文件>` 为空（0 行，派单 tip、两次 rebase 后、提交前共四验）——**零漂移，自 `8dee7fb` 起连续第二十三轮字节不变**。`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet 8dee7fb..HEAD -- package.json pnpm-lock.yaml` 通过）。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），逐目录 `ls` 计数与派单清单逐字一致，全部 25 文件本轮逐一重读。
- 前置阅读按令执行：round-30/PLAN 全文（A–F 六切片收口摘要 + G/H 在飞【G、H 均于运行中收口并被两次快进逐次吸收】+ I 派单行；每次 rebase 后重读收口行）→ EXCLUSIONS 全表（546 行；S1-I / S4-I-2..5 / S5-I-1..5 / S7-I-1..4 / S8-I-1..3 / S2-I-* / S3-I-* 逐条定位）→ R29-I 全文（input-file JSON serialization-form face——最近邻 I 轴 + 锚带来源 + 报告样式基准）→ R30-A…R30-F 收口行（六个兄弟空枚举面逐一划界，§0）→ R30-G / R30-H 结论段（快进吸收后补读，划界 §0）。
- 分支：`cursor/r30-i-cli-thirtieth-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（`/exec-daemon/node`，与 R9-I～R29-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。pnpm 10.17.1，`pnpm install --frozen-lockfile` + `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit 与流事件分段。`@earendil-works/pi-ai` **0.84.1**（钉死 + frozen-lockfile）；exports 图仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`，本 VM 直接枚举）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第二十三轮未满足（两臂文件级 blocked 按派单原样维持，未重复执行级探针）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S30-I-\* ID（诚实空枚举，与 R9-I～R29-I 同结局，二十三连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第三十组枚举而非历轮剩余项清理。历轮把 I 切片的输入面（R25-I 环境块、R26-I argv 句法、R28-I 路径拼写×cwd、R29-I 文件内容拼写）、输出面（R27-I 通道配置）、状态面（R22/R23/R24-I）、规模面（R18/R19/R20/R21-I）逐一收口——但二十九轮里每个承重命令始终被测成**一个数**（spawnSync 的 spawn-to-exit 整体），进程生命期内部的**时间段结构**从未被分解过。本轮把它作为**进程生命期时间段拓扑面（process-lifetime temporal-segment topology face：lead = spawn→首字节 stdout / span = 首→末字节 / tail = 末字节→exit 的收尾排水段 / close = exit→close）**收口：**零剂量纯观测**（全部输入坐标钉死在历轮规范值；异步 spawn 观测协议对 spawnSync 锚带做 A/A 验证通过），**tail 全格均匀 2.1–3.0 ms** 且归因判决为**引擎退出地板（1.7–1.9 ms）+ 亚 ms 堆/图规模项**——切片可触碰的唯一杠杆（`process.exitCode` 软退出改 `process.exit()` 硬退出）被**三重否决**：省 0.3–0.7 ms once-per-command（REJECT 类双料）+ 管道 stdout 截断 fail-open（双引擎见证：1 MB 输出被截到 219/146 KB）+ `--executor pi` 下 fire-and-forget invocations.jsonl 追加截断 fail-open——**tail 是契约地板**（S6-G-6 同型）。分段结构同时给出 S8-I-1 归因的段分辨佐证：custom−builtin 差**全部落在 lead 段**（children lead +41.2~+44.1 ms，span 差 ≈0 噪声）。五个裁决性发现：

1. **零漂移五重确认（连续第二十三轮）**：切片源码（`git diff 8dee7fb..HEAD` 空，派单 tip / 两次 rebase 后 / 提交前四验）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变；pi-ai 0.84.1 exports 图 7 项未变；39 表直接计数；`all.models.js` 不存在）、测试面（gate 计数逐字同历轮：1169 用例 1168 pass / 0 fail / 1 skip）、基线增量（`be9449f..274b552` 与 `274b552..3f80ab6` 各仅 1 提交全 docs【三文件 / 五文件】，src/test/scripts 均零行）、调用图前提（S5-I-1 dispatch 站点 **13 条 `await import`** 直接计数在位【`rg -c "await import" src/cli/main.ts` = 13】；`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` L36 失手回退 / `runtime.ts` L20 点用处 / `listed-model.ts` L1 同步面【grep 直接重验，注释行除外恰 3 边】；pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方【grep 复核——`src/context/packet.ts` 的 `./index.js` 命中是其自身目录的 context/index，非本切片 barrel，已逐行排除】；**parseArgs 调用站点恰 23 处分布于 9 个切片文件**——main.ts 9 / models.ts 4 / auth.ts 3 / commits.ts 2 / adapt.ts、doctor.ts、episode.ts、inject.ts、pause.ts 各 1【直接计数，与 R27-I/R28-I/R29-I 逐字同】；全切片 isTTY 读取 **0**、`process.cwd()` 读取 **0**【rg 全 src/ 计数 0】；fire-and-forget 站点普查：全 src/ 恰 **1 处** `void appendFile`——main.ts L620 invocation 遥测钩子）。
2. **本 VM 锚点复测（三次运行 × 双引擎，spawnSync 历史协议原样，未拷贝历轮数字，派单明令项）**：R9-I 承重集 + flowchart 双态格全部 exit 0，**custom−builtin 差 children +38.2~+49.4 / +22.7~+26.4、track +46.0~+52.9 / +22.4~+26.1、flowchart-flat64 +42.5~+51.0 / +15.2~+20.3 ms（22.14/22.22，三次运行带）**——与 R29-I 派单锚带（children +44.0~+50.5 / +22.6~+25.7、track +44.0~+53.2 / +22.5~+25.6、flat64 +41.9~+42.8 / +18.5~+21.7）**六类全部直接相交**（track@22.14 本带完全落于 R29-I 带内；children/track@22.22 交于 [22.7,25.7]/[22.5,25.6]；flowchart 双引擎交于 [42.5,42.8]/[18.5,20.3]）——**二十三轮同构，无新结构、无新空洞**。归属第三线互证：r7i part D 本轮复跑（builtin 构建后 providers/all 保持冷 **59.3 ms**、custom 回退命中缓存 **0.3 ms**）。
3. **新轴（§1.2-§1.3）：进程生命期时间段拓扑面标定（I 切片首次；二十九轮单数积分史上的首次段分解——零剂量纯观测收口，性能面为地板）**。结构判决先于测量并被测量证实：(a) **tail（末字节 stdout→exit）全 7 格 × 双引擎 × 三运行的中位带 2.1–3.0 ms**，跨命令类近乎常量、仅随堆规模微升（version 2.4 < children 2.2-2.5 < track 2.6-2.7 < flowchart 2.7-3.0）；(b) **归因探针**（裸 `node -e` 五格）：单写+自然排水的引擎退出地板 **1.71–1.85 ms**，切片的 `process.exitCode` 惯用形与裸落空**同价**（1.84/2.17——惯用形零溢价），1 MB 写只增 +0.3 ms（tail 不随输出字节缩放），`--version` 实测 2.1–2.4 = 地板 + ~0.3–0.5 ms 模块图/堆规模项；(c) **硬退出对照**：`process.exit(0)` 省 **0.3–0.7 ms**，但**双引擎截断见证**——1 MB 管道 stdout 经硬退出只送达 219,264 / 146,176 字节（自然排水 1,048,577 字节完整）——正是 main.ts L1489-1495 EPIPE 守卫与 L620 fire-and-forget 遥测追加所依赖的排水语义；(d) **lead/span 结构**：children 类 span ≈ 37.6–40.7 ms（routing 表先打印、run+inspect+adapt 随后——输出与工作交错），track/flowchart 类 span ≈ 0.1 ms（全部输出在工作完成后一次涌出——outcome 形输出契约）；**custom−builtin 差全部落在 lead 段**（children lead 差 +41.2~+44.1 / +24.2~+24.4 ms，span 差 −1.0~+2.6 ms 交替噪声；track lead 差 +47.5~+50.3 / +22.0~+25.5）——S7-I-1/S8-I-1 归因结构的段分辨第四线互证；(e) close 段（exit→close 父侧流关闭）全格 **0.00 ms**——观测协议无父侧残留。
4. **A/A 协议卫（R28-F 划界项）**：异步 spawn 观测协议的每格 spawn→close 总时长与 spawnSync 锚带全部重叠（children@custom 148.9–151.7 vs 149.7–156.6、flowchart@custom 256.7–258.5 vs 251.9–267.2、track@custom 172.5–174.5 vs 171.2–183.9 @22.14；@22.22 同验全过）——观测协议本身不扭曲被测量；调用协议是**仪器**而非受剂量变量，承重锚本身仍在历史 spawnSync 协议上完成。
5. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**，22.22.2 + tsx）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R29-I 计数逐字相同 + build；第二次快进 rebase 到 `3f80ab6` 后复跑一次仍 exit 0、计数逐字同）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。生产代码零改动，无 r30i 新仿真入库（派单：仅落地代码才须新增；本轮 segment/floor/truncation 三探针为新轴观测件，归附录不入库）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读读码）；无阈值/测试/公开签名变更；未触碰 npm POSIX `.bin` 符号链接 silent no-op（main.ts L1489——按派单维持归档，不在本战役改）。仓库变更仅本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 复核）；无赢家故未提交任何 scripts/（全部 harness 源码进附录 A 并附 sha256，临时文件已删除，遵守 R7-F/R8-C/R9-I～R29-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README 未触碰；未触碰在飞 R30-J / R31-A 与已收口 R30-G / R30-H 分支与文件。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S29-\* 条目；R9-I～R29-I 空枚举维持，未事后发明 S9-I-\* ～ S29-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：零漂移即字节级证明（站点逐项重验，结论第 1 条），三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅做文件级重开条件确认（§2.2），未满足，维持淘汰。**两臂文件级 blocked 按派单原样维持。**
  - **与 R29-E「wall-clock sampling topology / timestamp-provenance face」的划界（派单明令禁移植）**：其对象是 E 辖区**运行内时钟被采样的拓扑与时间戳来源**（受剂量变量是子进程内的时钟消费结构）；本轴零时钟剂量——观测点全部在**父进程侧**的流事件（stdout 'data' / 'exit' / 'close' 到达时刻），子进程内任何时钟/时间戳消费均未做受控变量、未注入、未替换。对象辖区（父侧观测 vs 子内采样）与机制（生命期段分解 vs 时戳来源）双正交。
  - **与 R30-E「async suspension-point topology / per-run suspension-manifest face」的划界（派单明令禁移植）**：其对象是 E 辖区热导出内 **`await` 悬挂点的代码拓扑**（每 run 悬挂清单）；本轴不枚举、不剂量任何悬挂点——对象是整进程生命期的**外部可观测时间段**（含 main 返回后的事件环排水段，代码拓扑零变量）。I 切片的 await 布局本轮未动未测。
  - **与 R27-I「process output-channel disposition face」的划界（plan-ban 不补铸）**：其对象是 stdout/stderr **通道对象是什么**（pipe/file/null…）；本轮通道钉死在其收口的规范 pipe 配置，剂量为零——观测的是固定通道上的**事件时刻**，非通道处置。
  - **与 R28-F「boundary call-protocol face」的划界（plan-ban 不补铸）**：其把调用方协议（spawn 族选型）作为**受剂量面变量**；本轮协议是**观测仪器**——承重锚在历史 spawnSync 协议原样完成（§1.1），异步 spawn 仅用于取流事件时刻，且以 A/A 总时长重叠验证仪器无扭曲（结论 4）。协议未进入任何面坐标。
  - **与 R30-F「boundary output-consumption / snapshot-readback topology face」的划界（派单明令禁移植）**：其剂量 F 辖区调用方**读回返回快照的哪些字段**；本轮 harness 对每格消费完全相同（收流+断言 exit），零读回剂量；且对象平面不同（内存返回值读回 vs 进程流时间段）。
  - **与 R30-G「M1 parent-run child-scheduling lattice」的划界（快进增补，按令）**：其对象是 G 辖区 supervised 引擎 parent-run 的**子任务调度格**；本轮 children 格全部走标准 C=2 spec 的既有调度（零调度剂量），命令类与结局逐臂恒等。
  - **与 R30-H「provenance-resolution pathway face（溯源解析通道面）」的划界（第二次快进增补，按令）**：其对象是 H 辖区 critique 链（`critiqueContract` → `findUnsourcedItems` → `isSourced`）所消费的**每合同项溯源举证通道组成**（引用通道短路 vs 假设通道合取匹配的析取谓词，内存内每 run 恰一次）；本轮零合同、零溯源谓词、零 critique 消费——I 切片不含任何合同溯源机制，本轴对象是**进程外部可观测的生命期时间段**（父侧流事件时刻）。对象平面（H 内存谓词通道 vs I 进程流分段）与机制（举证通道组成 vs 时间段分解）双正交，零移植。
  - **与 R30-A「call-stream ordering / dispatch-history face」的划界（派单明令禁移植）**：其对象是异质调用多重集到达共享生产路径的**次序**；CLI 是 once-per-process 单命令流，`main()` 无仓内多次调用方——零流量面，本轮未铸其 I 区镜像，亦未剂量任何调用次序。
  - **与 R30-B scale-cube / R30-C REPRX / R30-D approval-profile geometry 的划界（派单明令禁移植）**：规模联角（B）、引擎对象表示（C）、约束列表几何（D）三面对象均为内存值/规模坐标；本轮全部输入值、规模、表示钉死在承重规范形，零剂量。
  - **与 R29-I「input-file JSON serialization-form face」/ R29-J CSPELL / R28-I / R26-I / R25-I / NAMESHAPE 的划界（plan-ban 不补铸）**：文件内容拼写、耐久工件拼写、路径拼写×cwd、argv 句法、环境块、名字几何六坐标全部钉死在各自收口的规范值（providers pretty-2 / spec 紧凑单行 / 绝对规范路径 / R26 规范旗标 / 6 键全清除 / 固定文件名）——本轮无一被重剂量。
  - **与 S3-I-4（inspect --json 每事件 stdout 批量合并，排除行）的划界**：本轮不提任何写粒度/攒批变更；span 结构是观测事实，非写侧候选。
  - **与 R15-I 测量学 / R16-I 引擎代的划界**：MDE 包络直接引用其在案结论；双引擎并测沿用其纪律，未重推导。
  - **未换名移植其余兄弟轴与具名禁令**（FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX；计划禁令轴全名录逐一定位核过）——与本轴对象机制无交集。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码——未测量、未剂量、未立项。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 不接线）；稀疏 → 已批准 R0 基线；双 LCB / 双归因在位；提升 proposal-first（无任何提升）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`providers/all` 失手回退 + throw-outside-catch 在位；无 fsync / 完整性再哈希削除；fail-open 族保持关闭；未改任何测试。**新轴全程零代码改动、零行为注入**——所有测量都是对既有进程生命期的外部观测（父进程流事件时刻），子进程的输入坐标全部保持承重规范值。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；未 force-push 独占分支；未触碰在飞 R30-J / R31-A 与已收口 R30-G / R30-H 分支与文件。

## 1. 本 VM 复测：7 格承重再锚（spawnSync ×3）+ 新轴分段（异步 spawn ×3）+ 归因五格 + 截断见证（双 Node）

每迭代全新拷贝 state-root、基底 env 显式清除 6 键（R25-I 全缺席角）、旗标句法钉 R26-I 规范形、stdio 钉 R27-I 规范 pipe 配置、cwd 与路径拼写钉 R28-I 规范坐标、文件内容拼写钉 R29-I canon 形、2 预热 + 10 测量、显式二进制全路径。harness 全文附录 A，三次运行原始输出附录 A.5/A.6。

### 1.1 承重集三运行中位数（ms，spawn-to-exit，spawnSync 历史协议；22.14.0 / 22.22.2）

| 格 | run1 | run2 | run3 |
|---|---|---|---|
| `--version`（地板） | 54.6 / 51.4 | 57.6 / 50.2 | 56.1 / 50.9 |
| run-children@builtin | 107.2 / 106.4 | 111.5 / 103.3 | 107.0 / 106.0 |
| run-track@builtin | 131.0 / 125.4 | 125.2 / 120.0 | 122.9 / 122.9 |
| run-children@custom | 156.6 / 129.4 | 149.7 / 129.7 | 151.6 / 128.7 |
| run-track@custom | 183.9 / 150.0 | 171.2 / 146.1 | 175.6 / 145.3 |
| run-flowchart@custom-flat64 | 267.2 / 227.8 | 253.4 / 228.4 | 251.9 / 228.3 |
| run-flowchart@builtin-flat64 | 216.2 / 212.6 | 210.9 / 208.8 | 209.4 / 208.0 |

- **custom−builtin 差（三运行带）**：children **+38.2~+49.4 / +22.7~+26.4**、track **+46.0~+52.9 / +22.4~+26.1**、flowchart-flat64 **+42.5~+51.0 / +15.2~+20.3** ms（22.14/22.22）。对 R29-I 带逐类核对（派单明令「anchors 重叠 = 无新发现」项）：children@22.14 交于 [+44.0,+49.4]；children@22.22 交于 [+22.7,+25.7]；track@22.14 本带 [+46.0,+52.9] 完全落于 R29-I 带 [+44.0,+53.2] 内；track@22.22 交于 [+22.5,+25.6]；flowchart@22.14 交于 [+42.5,+42.8]；flowchart@22.22 交于 [+18.5,+20.3]——**六类全交，二十三轮同构，无新结构、无新空洞**。
- 全部 exit 0，三运行 × 双引擎无一失败。归属第三线互证：r7i part D 本轮复跑（§3）——builtin 构建后 providers/all 保持冷 **59.3 ms**、custom 回退命中缓存 **0.3 ms**。

### 1.2 新轴的结构证明（先证段的存在与归属机制，再证数字）

- **段定义与门**：任何 CLI 命令的生命期由引擎与切片共同产生四个外部可观测段——**lead**（spawn→首字节 stdout：引擎 boot + ESM 图实例化 + 首个用户可见字节前的全部工作）、**span**（首→末字节：输出期）、**tail**（末字节→exit：输出后残余工作 + main 返回后的事件环排水——泄漏的定时器/句柄或未决的 fire-and-forget 写会在此显形）、**close**（exit→close：父侧流关闭）。二十九轮的 spawnSync 协议把四段积分成一个数；段分解从未做过。
- **切片的退出路径清单（源结构直证）**：全命令**唯一**软退出 `process.exitCode = code` + 自然排水（main.ts L1497）；唯一硬退出是 EPIPE 守卫 `process.exit(0)`（L1493，仅下游管道关闭时）；全 src/ 唯一 fire-and-forget 站点 `void appendFile`（L620，invocation 遥测——仅 `--executor pi` 有真实调用流，fake 执行器零流量）——自然排水语义是该追加完成性的**唯一**保证。消费图定时器普查：`rg setInterval|setTimeout|fs.watch` 全 src/ 仅 child-coordinator.ts L150（per-child 超时，运行内清除）与 file-lock.ts L36（重试睡眠，锁内）——**无任何可越过 main 返回的持久句柄**；pause controller 纯文件式（无 watcher/interval，逐行读码确认）。
- **正交性**：观测零剂量——七格的 argv、env、cwd、stdio、文件字节、状态根与 §1.1 承重格逐字节相同；唯一差异是父侧观测方式（异步 spawn 收流事件 vs spawnSync 阻塞收割），已由 A/A 总时长重叠验证为无扭曲仪器（结论 4；children@custom 148.9–151.7 vs 149.7–156.6、flowchart@custom 256.7–258.5 vs 251.9–267.2、track@custom 172.5–174.5 vs 171.2–183.9 @22.14）。

### 1.3 轴 1：生命期分段标定（本轮新证据，不铸 ID）

**(a) 分段三运行中位带（ms；22.14.0 / 22.22.2；tail 保留两位小数）**：

| 格 | lead | span | tail | close |
|---|---|---|---|---|
| version | 51.0~52.0 / 48.1~49.3 | 0.0 / 0.0 | 2.39~2.59 / 2.36~2.40 | 0.00 / 0.00 |
| run-children@builtin | 65.8~66.8 / 62.4~63.2 | 37.9~38.4 / 37.1~39.7 | 2.12~2.19 / 2.17~2.40 | 0.00 / 0.00 |
| run-track@builtin | 121.5~122.4 / 118.3~119.6 | 0.1 / 0.1 | 2.28~2.33 / 2.41~2.56 | 0.00 / 0.00 |
| run-children@custom | 107.5~110.9 / 86.8~87.6 | 37.6~40.7 / 38.7~39.7 | 2.41~2.48 / 2.35~2.53 | 0.00 / 0.00 |
| run-track@custom | 169.7~171.8 / 141.6~143.8 | 0.0~0.1 / 0.1 | 2.68~2.74 / 2.56~2.64 | 0.00 / 0.00 |
| run-flowchart@custom-flat64 | 253.7~255.5 / 226.4~226.9 | 0.1 / 0.1 | 2.82~3.04 / 2.73~2.98 | 0.00 / 0.00 |
| run-flowchart@builtin-flat64 | 204.5~213.9 / 207.6~209.0 | 0.1 / 0.1 | 2.78~2.86 / 2.78~2.99 | 0.00 / 0.00 |

判读：**tail 全格 2.1–3.0 ms 近常量**，仅随堆/工作量微升（0.5–0.8 ms 级差，version→flowchart 单调）；**span 二态**——children 类 ~38–41 ms（routing 表在 startFlowchartRun 之前打印：输出与 run+inspect+adapt 交错），track/flowchart 类 ~0.1 ms（outcome 形输出契约：全部输出在工作完成后涌出）；**custom−builtin 差全部落在 lead**——children lead 差 +41.2~+44.1 / +24.2~+24.4，span 差 −1.0~+2.6（交替噪声）；track lead 差 +47.5~+50.3 / +22.0~+25.5；flowchart lead 差 +41.6~+49.2（22.14）——providers/all 回退的载荷在首字节之前发生（buildLiveCatalogConfig 在任何输出之前），与 S7-I-1/S8-I-1 载荷归因逐段吻合（第四线互证：r7i part D 直测 / R9-I 差带 / R29-I 差带 / 本轮段分辨）。

**(b) tail 归因五格探针（裸 `node -e` 隔离引擎地板；中位 + 带，ms）**：

| 格 | 22.14.0 | 22.22.2 | 语义 |
|---|---|---|---|
| bare（零输出） | total 14.1 | total 16.5 | 引擎全生命期地板（无首字节，tail 无定义计 total） |
| write1（单写+落空） | tail 1.85 (1.43..2.29) | 1.71 (1.35..2.30) | **单写自然排水的引擎退出地板** |
| writebig（1 MB 写） | tail 2.11 (1.56..2.53) | 2.16 (1.68..2.87) | tail 不随输出字节缩放（1 MB 仅 +0.3） |
| exitcode（写+`process.exitCode=0`） | tail 1.84 (1.48..2.06) | 2.17 (1.42..2.68) | **切片惯用形与裸落空同价——惯用形零溢价** |
| hardexit（写+`process.exit(0)`） | tail 1.50 (1.12..1.84) | 1.42 (1.07..1.57) | 硬退出仅省 0.3–0.7 ms |
| version（真实 CLI） | tail 2.38 (1.96..2.94) | 2.08 (1.87..2.58) | 地板 + ~0.3–0.5 ms 模块图/堆规模项 |

**归因判决**：CLI 的 tail ≈ 引擎自然排水退出地板（1.7–1.9）+ 亚 ms 图/堆撤销增量（承重格最重 flowchart 也只到 3.0）——切片代码对 tail 的贡献上界即「硬退出可省项」**0.3–0.7 ms once-per-command**。

**(c) 硬退出截断见证（双引擎，附录 A.7）**：1 MB 管道 stdout 后 `process.exitCode=0`（自然排水）送达 **1,048,577 字节完整**；同字节后 `process.exit(0)` 只送达 **219,264 / 146,176 字节（22.14/22.22，TRUNCATED）**——硬退出是输出契约 fail-open（`inspect --json | jq` 类管道消费方直接丢数据），且同机制截断 L620 的未决遥测追加。**tail 的自然排水语义是契约地板**（S6-G-6「契约地板」同型判决）。

### 1.4 诚实注记（只读观察，不铸 ID）

- **(a) 唯一杠杆三重否决**：`process.exitCode` → `process.exit()` 转换：①量级 0.3–0.7 ms once-per-command——验收线明文 REJECT 类双料（µs/once-per-run CLI 噪声拒收；且远低于「数十–数百 ms/run」达标带）；②管道 stdout 截断 fail-open（(c) 双引擎见证）；③ `--executor pi` 下 fire-and-forget invocations.jsonl 追加截断 fail-open。非候选，永不重开（除非 Node 引入「flush-then-exit」原语——届时也属引擎辖区）。
- **(b) span 二态非候选**：track/flowchart 的 outcome 形输出契约（全部输出后置）意味着「更早打印部分结果」可缩短感知首字节——但这是**输出契约变更**（stdout 字节次序可观察）且总时长零缩短（工作量不变），与 S3-I-4 的 CliIo 调用次数可观察同族。观测归档，非候选。
- **(c) tail 的堆规模微升（version 2.4 → flowchart 3.0）**：随命令工作集单调、双引擎同构——V8/libuv 撤销随堆规模的固有缩放，发生在 main 返回之后、切片零行代码参与。即便全额消除（不可能）也仅 ≤3 ms once-per-command。引擎辖区，非候选。

## 2. 第三十组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 输入规模定价 / 生命期段分解），每形态先对 EXCLUSIONS 全表 + 二十九轮 I 报告 + 兄弟禁移植轴做同机制先例检索。

### 2.1 新轴候选性自查——测量收口，不铸 ID

- **轴 1（生命期段拓扑面）**是对既有进程生命期的零剂量外部观测。产出是「四段定义 + 全格段带（tail 2.1–3.0 近常量 / span 二态 / lead 承载全部配置态差）+ 五格归因探针（引擎地板 1.7–1.9 / 惯用形零溢价 / 硬退出省 0.3–0.7）+ 截断见证（1 MB → 219/146 KB）+ A/A 协议卫 + 退出路径与句柄普查」的裁决数字集与一条契约地板钉死，不是优化。
- **派单空转卫自查**：本轴有真实生产流（每次 CLI 调用必然经历全部四段；tail 的排水语义是 L620 遥测通道与管道消费方的既有依赖）且填补的是二十九轮单数积分制造的测量盲区（历轮 harness 的 spawnSync 单数可逐字指认）——不属「零生产流 + 仅幻想尺度」空转型。不铸候选的理由是三重的：性能面唯一杠杆 0.3–0.7 ms once-per-command（REJECT 类双料）、行为面双 fail-open（输出截断 + 遥测截断——改法非法）、tail 主体在引擎辖区（切片已持有该面最省的合法形态：惯用形零溢价被 (b) 格直接证明）。

### 2.2 S8-I-1 重开条件文件级再确认——不铸 ID

- `dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 **0.84.1**。依派单「两臂文件级 blocked」纪律不重复执行级探针。
- **裁决**：重开条件连续第二十三轮未满足；r7i part D 复跑（59.3 / 0.3 ms）再度坐实回退结构的健全性价格。审计证据，非机制候选——**不铸 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级/辖区即死）

| 扫描角度 | 处置 |
|---|---|
| **退出惯用形转换 / tail 消除** | 本轮轴 1 三重否决（§1.4(a)）：REJECT 量级 + 双 fail-open + 引擎辖区。非候选 |
| **track/flowchart 输出前置 / 流式化** | 输出契约变更 + 总时长零缩短（§1.4(b)）；S3-I-4 同族。非候选 |
| **lead 段再压缩（dispatch 惰性化残余 / compile-cache / dist 布局 / 引擎参数）** | S5-I-1 全集在位（13 边直接计数）+ S6-I-1/2/3 与 S5-I-5 收口维持——本轮 25 文件重读未发现任何新分支独占边或新命令类 |
| **providers/all 回退结构（custom 差的本体）** | S8-I-1 文件级第二十三轮 blocked（§2.2）+ S8-I-2 预热无窗口 + S8-I-3 错误选择发散——前提零漂移，维持 |
| **providers.json 双读 / parse 复用 / 写形 / 拼写** | S1-I-2 / S8-E-1 / S4-G-6-S6-E-4 族 / R29-I 拼写面收口维持（本轮全钉 canon 未再剂量） |
| **parseArgs / USAGE / packageVersion / defaultIo / EPIPE** | R26-I / R27-I §2.3 收口维持（23 站点计数逐字同）；X1-1 / X1-2 / S1-B-7 族 |
| **环境门 / cwd / 路径拼写 / 入口门 / `.bin` no-op** | R25-I / R28-I 收口维持（本轮坐标钉死未再剂量）；`.bin` no-op 按派单归档不触 |
| **状态面（`.pi/` 树 / 适应态 / run 回读 / 累积遥测）** | R23-I / R24-I / R22-I / R20-I 收口维持，零角未动 |
| **规模面（children 基数 / flowchart 形态 / E×P×K / 配置基数）** | R18-I / R21-I / R19-I / R14-I 收口维持，本轮承重形态未扩展 |
| **拒绝路径 / 肥 builtin / A/A 地板 / 引擎代 / 测量学** | R17-I / R14-I / R15-I / R16-I 收口维持；本轮带比对直接引用其 MDE 包络 |
| **一次性命令类 / 凭据面 / pause-inject 免 router / doctor 并行 / 解析微观 / barrel 隐藏边** | S2-I-1..6 / S3-I-1..6 / S4-I-2..5 / S5-I-2..5 / S7-I-2..4 / S1-I-* 无 ID 收口逐字重现——零漂移下前提逐项成立 |
| **telemetry / config 残余面** | R20-I 热站点定价贴底维持；S1-D-8 / S3-I-5 族噪声维持 |
| **漂移带来的新导入边 / 新命令类 / 新句柄** | 不存在：切片、依赖、pi-ai exports、测试面、基线增量五重零漂移 + 本轮句柄/定时器普查（§1.2）双证 |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可落地候选；本轮把 I 切片**生命期时间维**（输入面六平面 + 配置态四坐标系 + spawn 面五通道 + 文件内容拼写平面之后的最后一个未分解观测维）收口：lead 承载全部配置态结构、span 忠实反映输出契约二态、tail 是引擎排水地板 + 契约地板、close 恒零。**I 切片自此输入、输出、状态、规模、拼写、时间六个观测维全部收口。**

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx；无代码落地故按派单为自愿加测）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part B 1220 builtin 行 × 39 provider lazy===sync 全过；part D 载荷探针复证 builtin 构建后 providers/all 保持冷 59.3 ms、custom 回退命中缓存 0.3 ms；part E configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单项，Node 22.22.2）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**（与 R9-I～R29-I 计数逐字相同）、`pnpm build` 绿。第二次快进 rebase 到 `3f80ab6` 后复跑一次：exit 0，计数逐字同。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动；无赢家故无新仿真脚本入库；四个 harness/探针（genfix/spot/segment/floor）与截断见证均为 /tmp 临时件，全文进附录 A 并附 sha256，测量后已删除，未进 lint 面。
- **引擎注记**：22.14.0（`/exec-daemon/node`）双引擎并测用于二十三轮带比对；nvm 22.22.2 为 gate/仿真引擎（engines ≥22.19 纪律，R15-I/S5-I-5 在案）。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；新轴以测量收口关闭生命期时间维；S8-I-1 文件级第二十三轮 blocked）。
- 剩余赢家：无（custom−builtin 结构维持 S8-I-1 健全性反例封死且本轮段分辨证明其全部位于 lead；tail 的唯一杠杆三重否决——REJECT 量级 × 输出截断 fail-open × 遥测截断 fail-open；span 二态是输出契约本体；全部消除形态映射 S1-I-2 / X1-1 / X1-2 / S3-I-4 / S4-G-6 / S6-G-6 / S9-C-2 / S6-I-2 既有围栏）。
- 矩阵空洞：无（承重格二十三轮同构复现 + 本轮补入分段观测——{builtin,custom} × {children,track,flowchart} × {lead,span,tail,close} 的完备段分解 + 五格归因探针 + 截断见证 + A/A 协议卫；单数积分史的测量盲区在时间维上收口，性能结论是地板）。
- 跨辖区移交注记：**零新增缺陷级**——本轮契约地板发现（tail 自然排水 = 输出完整性 + 遥测完整性的承载段）行为完全正确，以契约钉死归档（§1.4(a)），不构成移交件。R28-I 入口门 symlink 缺陷、R25-I executor 优先级不对称、R26-I stage 标签三分不对称三条既有移交注记维持原状，未推进未重提。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律；sha256 见 A.8，临时文件已删除）

### A.1 夹具生成（/tmp/r30i-bench/genfix.mjs）

```javascript
// R30-I fixture generation: bearing-set re-anchor states only (R9-I..R29-I
// lineage, value- and byte-identical in shape to the R29-I appendix A.1 canon
// arm). This round doses NOTHING on any input coordinate: every closed plane
// stays at its canonical bearing value (providers.json pretty-2 write form,
// children/flow/results compact single-line, R25-I all-unset env corner,
// R26-I canonical flag syntax, R27-I canonical pipe disposition, R28-I
// canonical cwd + absolute path spellings, R29-I canonical content spelling).
// The new axis is purely observational: the temporal SEGMENTS of the child
// process lifetime (spawn -> first stdout byte -> last stdout byte -> exit ->
// close), which twenty-nine prior I rounds always integrated into a single
// spawn-to-exit number.
import { mkdirSync, rmSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const ROOT = "/tmp/r30i-bench";
const FIX = join(ROOT, "fixtures");

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
function writeState(name, cfg) {
  const dir = join(FIX, name);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  if (cfg !== undefined) {
    writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
  }
  return dir;
}
writeState("state-builtin", states.builtin);
writeState("state-custom", states.custom);

// Children spec (R9-I..R29-I lineage, C=2).
const childrenValue = { tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] };
writeFileSync(join(FIX, "children.json"), JSON.stringify(childrenValue) + "\n");

// Flowchart fixtures (R21-I flat shapes; bearing cells only).
function flatFlow(n) {
  const nodes = [];
  for (let i = 0; i < n; i++) {
    nodes.push({
      id: `n${i}`, taskId: `tsk_f${i}`, role: "actor",
      objective: `Step ${i} of the benchmark flow`,
      modelPolicy: { allowedModels: ["cheap"] },
      confidenceThreshold: 0.7, approvalRequired: false
    });
  }
  return { id: `bench-flat-${n}`, nodes, edges: [] };
}
function resultsFor(n) {
  const out = {};
  for (let i = 0; i < n; i++) {
    out[`n${i}`] = {
      outcome: "SUCCESS", confidence: 0.9,
      evidenceIds: [`evd_f${i}`],
      facts: [{ key: `fact-${i}`, value: `value ${i}`, confidence: 0.8 }]
    };
  }
  return out;
}
writeFileSync(join(FIX, "flow-flat64.json"), JSON.stringify(flatFlow(64)) + "\n");
writeFileSync(join(FIX, "results-flat64.json"), JSON.stringify(resultsFor(64)) + "\n");

// Project scaffold (no .pi tree, no adaptation registry — R23-I / R24-I
// planes stay closed at their zero corner).
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
console.log("FIXTURES_WRITTEN");
```

### A.2 承重抽测 harness（/tmp/r30i-bench/spot.mjs，分别以两版本二进制执行 × 3 运行）

```javascript
// R30-I bearing spot measure: the mandated budget-anchor re-measurement on
// the exact historical protocol (spawnSync, R9-I..R29-I lineage — floor +
// builtin/custom x children/track/flowchart). Nothing is dosed; all input
// coordinates stay at their canonical bearing values. This run exists to
// satisfy the "re-measure x3 before hunting new axes" dispatch order and to
// serve as the A/A total-duration reference for the NEW observational axis
// (segment.mjs measures the same cells over async spawn with stream-event
// timestamps; its spawn->close totals must land inside these bands or the
// observation protocol itself is distorting — R28-F demarcation).
// usage: <node-binary> spot.mjs
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r30i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.execPath;
const WARMUP = 2, ITERS = 10;

const PROJ = join(FIX, "project");
const childrenArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", join(FIX, "children.json")];
const trackArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"];
const flowArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--flowchart", join(FIX, "flow-flat64.json"), "--results", join(FIX, "results-flat64.json")];

const cells = [
  { id: "version", args: () => ["--version"], state: undefined },
  { id: "run-children@builtin", state: "state-builtin", args: childrenArgs },
  { id: "run-track@builtin", state: "state-builtin", args: trackArgs },
  { id: "run-children@custom", state: "state-custom", args: childrenArgs },
  { id: "run-track@custom", state: "state-custom", args: trackArgs },
  { id: "run-flowchart@custom-flat64", state: "state-custom", args: flowArgs },
  { id: "run-flowchart@builtin-flat64", state: "state-builtin", args: flowArgs }
];

function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, state), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = cell.args(sd);
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    delete env.PI_FAST_MODEL; delete env.SPARKLE_AUTO_ADAPT; delete env.PI_THINKING_LEVEL;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, cwd: ROOT, encoding: "utf8", timeout: 180000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-UNEXPECTED ${cell.id}: exit=${r.status} ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

### A.3 分段观测 harness（/tmp/r30i-bench/segment.mjs，分别以两版本二进制执行 × 3 运行）

```javascript
// R30-I NEW axis: process-lifetime temporal-segment topology face.
// Twenty-nine prior I rounds measured every bearing command as ONE number
// (spawn-to-exit over spawnSync). This round decomposes that lifetime into
// its structural segments, dosing nothing:
//   lead  = spawn call -> first stdout data event   (engine boot + module
//           graph + command work up to the first user-visible byte)
//   span  = first -> last stdout data event         (the output phase)
//   tail  = last stdout data event -> 'exit'        (post-output work + the
//           post-main event-loop drain: pending handles, microtasks, stdio
//           teardown — the segment where a leaked timer/handle or a pending
//           fire-and-forget write would show up)
//   close = 'exit' -> 'close'                       (parent-side stdio drain)
// The only structural way the slice can stretch `tail` is the single
// fire-and-forget telemetry write (main.ts L620 `void appendFile`) — zero
// flow under fake executors — plus whatever the engine holds. Every input
// coordinate stays at the canonical bearing value; the async-spawn
// observation protocol is validated A/A: per-cell spawn->close totals must
// land inside the spawnSync bands from spot.mjs (protocol-distortion guard,
// R28-F demarcation — the call protocol here is an instrument, not a dosed
// variable).
// usage: <node-binary> segment.mjs
import { spawn } from "node:child_process";
import { mkdirSync, rmSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r30i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "segwork");
const nodeBin = process.execPath;
const WARMUP = 2, ITERS = 10;

const PROJ = join(FIX, "project");
const childrenArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", join(FIX, "children.json")];
const trackArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"];
const flowArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--flowchart", join(FIX, "flow-flat64.json"), "--results", join(FIX, "results-flat64.json")];

const cells = [
  { id: "version", args: () => ["--version"], state: undefined },
  { id: "run-children@builtin", state: "state-builtin", args: childrenArgs },
  { id: "run-track@builtin", state: "state-builtin", args: trackArgs },
  { id: "run-children@custom", state: "state-custom", args: childrenArgs },
  { id: "run-track@custom", state: "state-custom", args: trackArgs },
  { id: "run-flowchart@custom-flat64", state: "state-custom", args: flowArgs },
  { id: "run-flowchart@builtin-flat64", state: "state-builtin", args: flowArgs }
];

function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, state), dir, { recursive: true });
  return dir;
}
function median(values) {
  const s = [...values].sort((a, b) => a - b);
  const mid = (s.length - 1) / 2;
  return (s[Math.floor(mid)] + s[Math.ceil(mid)]) / 2;
}
function band(values) {
  const s = [...values].sort((a, b) => a - b);
  return `${s[0].toFixed(1)}..${s[s.length - 1].toFixed(1)}`;
}

function runOnce(args, cwd) {
  return new Promise((resolve, reject) => {
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    delete env.PI_FAST_MODEL; delete env.SPARKLE_AUTO_ADAPT; delete env.PI_THINKING_LEVEL;
    const t0 = performance.now();
    const child = spawn(nodeBin, [CLI, ...args], { env, cwd, stdio: ["pipe", "pipe", "pipe"] });
    let firstOut, lastOut, exitAt, exitCode;
    child.stdout.on("data", () => {
      const now = performance.now();
      if (firstOut === undefined) firstOut = now;
      lastOut = now;
    });
    child.stderr.resume();
    child.on("error", reject);
    child.on("exit", (code) => { exitAt = performance.now(); exitCode = code; });
    child.on("close", () => {
      const closeAt = performance.now();
      resolve({
        exitCode,
        total: closeAt - t0,
        lead: (firstOut ?? exitAt) - t0,
        span: firstOut === undefined ? 0 : lastOut - firstOut,
        tail: firstOut === undefined ? 0 : exitAt - lastOut,
        close: closeAt - exitAt
      });
    });
    setTimeout(() => child.kill("SIGKILL"), 180000).unref();
  });
}

rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const rows = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const r = await runOnce(cell.args(sd), ROOT);
    exit = r.exitCode;
    if (i === 0 && r.exitCode !== 0) console.error(`  FIRST-UNEXPECTED ${cell.id}: exit=${r.exitCode}`);
    if (i >= WARMUP) rows.push(r);
  }
  const pick = (k) => rows.map((r) => r[k]);
  console.log(
    `${cell.id}: total=${median(pick("total")).toFixed(1)}ms ` +
    `lead=${median(pick("lead")).toFixed(1)} span=${median(pick("span")).toFixed(1)} ` +
    `tail=${median(pick("tail")).toFixed(2)} (band ${band(pick("tail"))}) ` +
    `close=${median(pick("close")).toFixed(2)} exit=${exit}`
  );
}
```

### A.4 tail 归因探针（/tmp/r30i-bench/floor.mjs，双引擎各执行一次）

```javascript
// R30-I tail attribution probe: how much of the observed ~2.2-3.0ms
// last-output->exit tail is the ENGINE's own exit floor (V8/libuv teardown +
// pipe flush) versus anything the slice contributes. Cells:
//   bare        node -e "" (no output at all; lead==exit, tail undefined -> 0)
//   write1      node -e one small stdout write then fall off the loop
//   writebig    node -e one ~1MB stdout write (does tail scale with bytes?)
//   exitcode    node -e write + process.exitCode=0 (the slice's exact exit
//               idiom: soft exit code + natural drain)
//   hardexit    node -e write + process.exit(0) (the idiom the slice
//               deliberately does NOT use; measures what a hard exit would
//               save and whether piped output survives)
//   version     the real CLI floor command for direct comparison
// Same observation protocol as segment.mjs. Nothing in the repo is touched.
// usage: <node-binary> floor.mjs
import { spawn } from "node:child_process";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const nodeBin = process.execPath;
const WARMUP = 2, ITERS = 10;

const cells = [
  { id: "bare", args: ["-e", ""] },
  { id: "write1", args: ["-e", "process.stdout.write('x\\n')"] },
  { id: "writebig", args: ["-e", `process.stdout.write('${"y".repeat(64)}'.repeat(16384) + '\\n')`] },
  { id: "exitcode", args: ["-e", "process.stdout.write('x\\n'); process.exitCode = 0;"] },
  { id: "hardexit", args: ["-e", "process.stdout.write('x\\n'); process.exit(0);"] },
  { id: "version", args: [CLI, "--version"] }
];

function median(values) {
  const s = [...values].sort((a, b) => a - b);
  const mid = (s.length - 1) / 2;
  return (s[Math.floor(mid)] + s[Math.ceil(mid)]) / 2;
}
function band(values) {
  const s = [...values].sort((a, b) => a - b);
  return `${s[0].toFixed(2)}..${s[s.length - 1].toFixed(2)}`;
}
function runOnce(args) {
  return new Promise((resolve, reject) => {
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    delete env.PI_FAST_MODEL; delete env.SPARKLE_AUTO_ADAPT; delete env.PI_THINKING_LEVEL;
    const t0 = performance.now();
    const child = spawn(nodeBin, args, { env, cwd: "/tmp/r30i-bench", stdio: ["pipe", "pipe", "pipe"] });
    let firstOut, lastOut, exitAt, exitCode, bytes = 0;
    child.stdout.on("data", (chunk) => {
      const now = performance.now();
      if (firstOut === undefined) firstOut = now;
      lastOut = now;
      bytes += chunk.length;
    });
    child.stderr.resume();
    child.on("error", reject);
    child.on("exit", (code) => { exitAt = performance.now(); exitCode = code; });
    child.on("close", () => {
      resolve({
        exitCode, bytes,
        total: performance.now() - t0,
        tail: firstOut === undefined ? exitAt - t0 : exitAt - lastOut
      });
    });
  });
}
for (const cell of cells) {
  const rows = []; let exit, bytes;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const r = await runOnce(cell.args);
    exit = r.exitCode; bytes = r.bytes;
    if (i >= WARMUP) rows.push(r);
  }
  const pick = (k) => rows.map((r) => r[k]);
  console.log(
    `${cell.id}: total=${median(pick("total")).toFixed(1)}ms ` +
    `tail=${median(pick("tail")).toFixed(2)} (band ${band(pick("tail"))}) bytes=${bytes} exit=${exit}`
  );
}
```

### A.5 承重三次运行原始输出（spot-run1/2/3.txt）

```
### run1
== v22.14.0 ==
version: median=54.6ms min=53.3 p90=55.6 exit=0
run-children@builtin: median=107.2ms min=103.1 p90=109.4 exit=0
run-track@builtin: median=131.0ms min=121.3 p90=136.4 exit=0
run-children@custom: median=156.6ms min=150.1 p90=170.1 exit=0
run-track@custom: median=183.9ms min=176.7 p90=201.8 exit=0
run-flowchart@custom-flat64: median=267.2ms min=247.0 p90=282.3 exit=0
run-flowchart@builtin-flat64: median=216.2ms min=209.8 p90=223.8 exit=0
== v22.22.2 ==
version: median=51.4ms min=48.5 p90=53.4 exit=0
run-children@builtin: median=106.4ms min=100.4 p90=107.6 exit=0
run-track@builtin: median=125.4ms min=120.7 p90=132.4 exit=0
run-children@custom: median=129.4ms min=126.5 p90=132.2 exit=0
run-track@custom: median=150.0ms min=141.6 p90=160.9 exit=0
run-flowchart@custom-flat64: median=227.8ms min=222.3 p90=238.8 exit=0
run-flowchart@builtin-flat64: median=212.6ms min=194.8 p90=220.2 exit=0
### run2
== v22.14.0 ==
version: median=57.6ms min=52.8 p90=93.8 exit=0
run-children@builtin: median=111.5ms min=103.8 p90=126.2 exit=0
run-track@builtin: median=125.2ms min=123.2 p90=130.6 exit=0
run-children@custom: median=149.7ms min=147.7 p90=159.0 exit=0
run-track@custom: median=171.2ms min=167.9 p90=179.1 exit=0
run-flowchart@custom-flat64: median=253.4ms min=246.4 p90=260.1 exit=0
run-flowchart@builtin-flat64: median=210.9ms min=202.3 p90=212.9 exit=0
== v22.22.2 ==
version: median=50.2ms min=49.1 p90=51.7 exit=0
run-children@builtin: median=103.3ms min=100.2 p90=104.7 exit=0
run-track@builtin: median=120.0ms min=115.2 p90=120.6 exit=0
run-children@custom: median=129.7ms min=125.9 p90=131.7 exit=0
run-track@custom: median=146.1ms min=141.1 p90=149.6 exit=0
run-flowchart@custom-flat64: median=228.4ms min=222.6 p90=232.4 exit=0
run-flowchart@builtin-flat64: median=208.8ms min=204.4 p90=214.4 exit=0
### run3
== v22.14.0 ==
version: median=56.1ms min=52.7 p90=68.7 exit=0
run-children@builtin: median=107.0ms min=105.5 p90=108.3 exit=0
run-track@builtin: median=122.9ms min=120.9 p90=123.7 exit=0
run-children@custom: median=151.6ms min=144.0 p90=166.1 exit=0
run-track@custom: median=175.6ms min=167.3 p90=184.2 exit=0
run-flowchart@custom-flat64: median=251.9ms min=247.4 p90=254.2 exit=0
run-flowchart@builtin-flat64: median=209.4ms min=206.3 p90=216.3 exit=0
== v22.22.2 ==
version: median=50.9ms min=49.4 p90=52.9 exit=0
run-children@builtin: median=106.0ms min=102.5 p90=109.3 exit=0
run-track@builtin: median=122.9ms min=120.6 p90=124.1 exit=0
run-children@custom: median=128.7ms min=126.3 p90=130.4 exit=0
run-track@custom: median=145.3ms min=141.9 p90=148.8 exit=0
run-flowchart@custom-flat64: median=228.3ms min=225.1 p90=231.0 exit=0
run-flowchart@builtin-flat64: median=208.0ms min=203.9 p90=212.3 exit=0
```

### A.6 分段三次运行原始输出（seg-run1/2/3.txt）

```
### seg-run1
== v22.14.0 ==
version: total=54.5ms lead=52.0 span=0.0 tail=2.49 (band 1.8..2.9) close=0.00 exit=0
run-children@builtin: total=106.5ms lead=65.8 span=38.4 tail=2.18 (band 2.0..2.4) close=0.00 exit=0
run-track@builtin: total=125.0ms lead=122.4 span=0.1 tail=2.28 (band 2.1..2.8) close=0.00 exit=0
run-children@custom: total=150.5ms lead=108.1 span=38.7 tail=2.41 (band 2.3..2.7) close=0.00 exit=0
run-track@custom: total=174.1ms lead=171.2 span=0.0 tail=2.74 (band 2.5..3.3) close=0.00 exit=0
run-flowchart@custom-flat64: total=257.0ms lead=254.0 span=0.1 tail=2.89 (band 2.5..3.5) close=0.00 exit=0
run-flowchart@builtin-flat64: total=209.8ms lead=206.8 span=0.1 tail=2.82 (band 2.5..3.3) close=0.00 exit=0
== v22.22.2 ==
version: total=50.5ms lead=48.1 span=0.0 tail=2.37 (band 1.9..2.7) close=0.00 exit=0
run-children@builtin: total=105.2ms lead=62.8 span=39.7 tail=2.33 (band 1.9..2.5) close=0.00 exit=0
run-track@builtin: total=121.0ms lead=118.3 span=0.1 tail=2.41 (band 2.2..2.9) close=0.00 exit=0
run-children@custom: total=128.2ms lead=87.0 span=38.7 tail=2.46 (band 2.2..3.0) close=0.00 exit=0
run-track@custom: total=146.8ms lead=143.8 span=0.1 tail=2.64 (band 2.4..3.2) close=0.00 exit=0
run-flowchart@custom-flat64: total=229.6ms lead=226.6 span=0.1 tail=2.98 (band 2.6..3.3) close=0.00 exit=0
run-flowchart@builtin-flat64: total=210.4ms lead=207.6 span=0.1 tail=2.78 (band 2.6..3.1) close=0.00 exit=0
### seg-run2
== v22.14.0 ==
version: total=54.3ms lead=51.6 span=0.0 tail=2.59 (band 2.1..3.1) close=0.00 exit=0
run-children@builtin: total=106.3ms lead=66.3 span=38.1 tail=2.12 (band 2.0..2.6) close=0.00 exit=0
run-track@builtin: total=124.7ms lead=122.2 span=0.1 tail=2.30 (band 2.1..2.8) close=0.00 exit=0
run-children@custom: total=148.9ms lead=107.5 span=40.7 tail=2.47 (band 2.4..2.7) close=0.00 exit=0
run-track@custom: total=172.5ms lead=169.7 span=0.1 tail=2.68 (band 2.5..3.0) close=0.00 exit=0
run-flowchart@custom-flat64: total=256.7ms lead=253.7 span=0.1 tail=2.82 (band 2.7..3.4) close=0.00 exit=0
run-flowchart@builtin-flat64: total=207.1ms lead=204.5 span=0.1 tail=2.78 (band 2.5..3.2) close=0.00 exit=0
== v22.22.2 ==
version: total=50.7ms lead=48.4 span=0.0 tail=2.36 (band 1.9..3.2) close=0.00 exit=0
run-children@builtin: total=102.2ms lead=62.4 span=37.1 tail=2.17 (band 1.9..2.5) close=0.00 exit=0
run-track@builtin: total=121.6ms lead=119.1 span=0.1 tail=2.54 (band 2.4..2.9) close=0.00 exit=0
run-children@custom: total=128.1ms lead=86.8 span=39.2 tail=2.35 (band 2.1..2.9) close=0.00 exit=0
run-track@custom: total=146.4ms lead=143.3 span=0.1 tail=2.60 (band 2.4..7.5) close=0.00 exit=0
run-flowchart@custom-flat64: total=229.2ms lead=226.4 span=0.1 tail=2.73 (band 2.6..3.2) close=0.00 exit=0
run-flowchart@builtin-flat64: total=210.6ms lead=207.9 span=0.1 tail=2.82 (band 2.5..3.1) close=0.00 exit=0
### seg-run3
== v22.14.0 ==
version: total=53.2ms lead=51.0 span=0.0 tail=2.39 (band 1.9..2.9) close=0.00 exit=0
run-children@builtin: total=107.1ms lead=66.8 span=37.9 tail=2.19 (band 2.0..2.7) close=0.00 exit=0
run-track@builtin: total=124.0ms lead=121.5 span=0.1 tail=2.33 (band 2.2..2.6) close=0.00 exit=0
run-children@custom: total=151.7ms lead=110.9 span=37.6 tail=2.48 (band 2.2..6.2) close=0.00 exit=0
run-track@custom: total=174.5ms lead=171.8 span=0.1 tail=2.71 (band 2.5..2.8) close=0.00 exit=0
run-flowchart@custom-flat64: total=258.5ms lead=255.5 span=0.1 tail=3.04 (band 2.8..3.4) close=0.00 exit=0
run-flowchart@builtin-flat64: total=217.1ms lead=213.9 span=0.1 tail=2.86 (band 2.6..4.0) close=0.00 exit=0
== v22.22.2 ==
version: total=51.7ms lead=49.3 span=0.0 tail=2.40 (band 1.9..3.5) close=0.00 exit=0
run-children@builtin: total=104.3ms lead=63.2 span=38.3 tail=2.40 (band 2.1..2.5) close=0.00 exit=0
run-track@builtin: total=121.9ms lead=119.6 span=0.1 tail=2.56 (band 2.2..3.2) close=0.00 exit=0
run-children@custom: total=130.3ms lead=87.6 span=39.7 tail=2.53 (band 2.2..3.3) close=0.00 exit=0
run-track@custom: total=144.1ms lead=141.6 span=0.1 tail=2.56 (band 2.4..3.0) close=0.00 exit=0
run-flowchart@custom-flat64: total=229.9ms lead=226.9 span=0.1 tail=2.78 (band 2.6..3.2) close=0.00 exit=0
run-flowchart@builtin-flat64: total=212.4ms lead=209.0 span=0.1 tail=2.99 (band 2.5..3.4) close=0.00 exit=0
```

### A.7 归因探针与截断见证原始输出（floor-out.txt / truncation-witness.txt）

```
== floor v22.14.0 ==
bare: total=14.1ms tail=14.13 (band 13.42..15.28) bytes=0 exit=0
write1: total=15.8ms tail=1.85 (band 1.43..2.29) bytes=2 exit=0
writebig: total=17.6ms tail=2.11 (band 1.56..2.53) bytes=1048577 exit=0
exitcode: total=15.9ms tail=1.84 (band 1.48..2.06) bytes=2 exit=0
hardexit: total=15.6ms tail=1.50 (band 1.12..1.84) bytes=2 exit=0
version: total=53.6ms tail=2.38 (band 1.96..2.94) bytes=6 exit=0
== floor v22.22.2 ==
bare: total=16.5ms tail=16.53 (band 15.02..17.71) bytes=0 exit=0
write1: total=18.6ms tail=1.71 (band 1.35..2.30) bytes=2 exit=0
writebig: total=21.1ms tail=2.16 (band 1.68..2.87) bytes=1048577 exit=0
exitcode: total=19.8ms tail=2.17 (band 1.42..2.68) bytes=2 exit=0
hardexit: total=18.4ms tail=1.42 (band 1.07..1.57) bytes=2 exit=0
version: total=50.7ms tail=2.08 (band 1.87..2.58) bytes=6 exit=0
```

截断见证（内联单行命令：子进程写 `"z".repeat(1<<20)+"\n"` 后分别软/硬退出，父进程 spawnSync 收满 stdout 计数）：

```
== v22.14.0 ==
natural-drain bytes: 1048577 exit: 0
hard-exit bytes: 219264 exit: 0 (TRUNCATED)
== v22.22.2 ==
natural-drain bytes: 1048577 exit: 0
hard-exit bytes: 146176 exit: 0 (TRUNCATED)
```

（bare 格注记：无任何输出故 `firstOut === undefined`，探针把 total 计入 tail 列——该格的用途是给出引擎全生命期地板 14.1/16.5 ms，非 tail 语义；writebig/exitcode/hardexit/version 四格的 bytes 列同时充当输出完整性核对。）

### A.8 资产 sha256 与清理

```
f106fa109810cde830cddf6ff5d31797db1a6cde7c5019db78cd0a111f9dd98e  genfix.mjs
4b744d7753a18ccb4553c5f802ebe9f54a649ddbcb377fe464158d2ec7a58163  spot.mjs
26242f0d7d560d8668c3d9f58c5a5edf391b11868220bf8828904bf4128023ef  segment.mjs
767be220fe2582e3311412a0595081a7cc1845b7c2a6c385fee3531617613d80  floor.mjs
79a36f731c062fe79972d04e6a07d8b75995f376c2fe84714192a9a252c389b5  spot-run1.txt
dc451292023400c228065cb272ba54e8749c274de2f06f0faa597df61319d10c  spot-run2.txt
06ac299493752871fd9076496bf7cc9f6ca31a423a83505322127aeed98e587c  spot-run3.txt
14a1ea3617d1626048d111d570823277979d0fe7e1ed03d2259f2b66cdfe047e  seg-run1.txt
c4458912f0d69fa445283701394a4082915e658abece027acc69c7018632a272  seg-run2.txt
740b699107d7c4e479a074435becd32d817605430336a7f798f7fb37158b984f  seg-run3.txt
b7785932ef2da1c8e9ec343fc876e46acbede4911caac5412d05ac28cfbcc6fc  floor-out.txt
d3687449662913976dfb9eb8c5506ece743f83d5ac25a8b4671de2232c473957  truncation-witness.txt
```

`/tmp/r30i-bench/`（fixtures / work / segwork 含内）全部在归档后删除；本轮未在 dist/ 或仓库内创建任何临时文件。

MORE_OPTIMA=no
BRANCH=cursor/r30-i-cli-thirtieth-pass-83a1
