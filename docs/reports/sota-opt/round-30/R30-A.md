MODEL_SLUG=claude-fable-5-thinking-xhigh

# R30-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第三十遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 30 / R30-A（Round 1–29 同区第三十遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `52bd98b`（R29-H 空枚举收口 + Round 30 开卷 + 本 R30-A 派发）。运行中 exclusive **一笔 docs-only 快进**：`52bd98b` → `ead66ec`（R29-I 空枚举收口——新归档命名轴 **input-file JSON serialization-form face（输入文件 JSON 序列化拼写面）**，I 切片输入文件内容的字节拼写形（空白形 × 转义拼写 × 对象键序）——+ R30-B 派发）。增量 `git diff --stat 52bd98b..ead66ec` 核对**仅触及 docs/reports/sota-opt/ 下 4 个文件（PROGRESS / round-29/PLAN / R29-I.md / round-30/PLAN），`src/` 与 `test/` 零行**；按令 rebase 本分支到 `ead66ec` 通过（当时仅本报告一个未提交文件，快进式跟进），14 文件冻结 `git diff 7acb666..HEAD -- src/tracking src/run/child-tracking.ts src/run/gate-apply.ts` 在派发 tip 与新 tip 复验均为 **0 行**，新收口兄弟轴已补入 §0 不移植栏并完成划界（见 §0）。全部测量（三次官方仿真、聚焦套件、`pnpm gate`）在 `52bd98b` 的工作树完成——快进增量 `src/` 零变化 ⇒ 测量在字节相同的切片上完成，全部有效。R29-J 在本轮收尾时仍在飞，按令未等待。
**分支:** `cursor/r30-a-tracking-thirtieth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S30-A-\* ID，切片零代码
改动；整片预算收口第二十八次复核成立，并新增一条实测轴——「调用流
时序 / 分派历史面 (call-stream ordering / dispatch-history face)」
（axis 19）补上二十九遍测量方法学一直隐含的最后一个未受控坐标：
**同一多重集的门调用以什么次序到达共享生产调用路径**。二十九遍 A 轮
的每一个计时循环都是**同质块**——锚点五格各自背靠背重复、R12-A 分解
逐组件隔离、R23-A 账本驱动器虽然跑过异质流但其次序是耦合轴的**固定
夹具**而非受控变量；而生产流按构造是**异质且次序任意**的（F10a 普查：
coordinator.ts 对 `Promise.race` 胜者逐到达门控、flowchart-run.ts 按
遍历序逐节点门控、supervisor.ts 逐 settle——三站点无一按形状排序或
攒批，到达序由数据/时序决定）。该坐标在本切片三重承重：(a) R18-A 的
组成天花板（「全 apply 锚点界住任意 apply/skip 混合」）比较的是同质
块测量，其论证**对次序视盲**——隐含假设交织不加价；(b) 战役从 R11-A
起十九遍观察到「no-contract 格 PIC/基准次序」现象并以卫生纪律
（全臂 sweep 预热，R28-A/R29-A）回应，但从未把分派历史当生产流坐标
剂量过；(c) 语义次序不变性是结构性的（F10b：同多重集三种次序逐位同
结果——门是对冻结表的纯函数，每调用自带 id 计数器），唯一可能的价
是引擎级（多态 IC 分派、形状分支预测器训练、代码/数据缓存驻留）。
本轮补上并闭合：F10a 生产流次序普查、F10b 次序不变性证人、五组定价
（19a–19e，三次官方运行）。定价裁决：**面有真实但 µs 级的价**——
19a 整门交织溢价 +0.20~+0.26 µs/gate（~1.6–2.0%，12 组对比 10 组
同号正，run3 前向 BLOCK 16.38 µs 为单 sweep 离群、其 re-echo 13.10
回带并复现 +0.22/+0.23 溢价）⇒ 最坏 ~1.0–1.3 µs/run；19b 哈希站点
形状交织溢价 +57~+133 ns/hash（两元素隐藏类：FAIL 维度带
evidenceRefs 键）⇒ ×15 腿/run 最坏 ~1.0–2.0 µs/run；19c turn 站点
+15~+29 ns 亚噪声；19d 命令类（apply/skip/replay）次序**符号翻转**
（前向/回声跨三 run 双向，−0.36~+0.51 µs/call）⇒ R18-A 天花板的
次序修正上界 ≤ ~2.4 µs/run 且无稳定次序税——**天花板在 µs 精度内
维持**；19e 形状纯净进程角（全 PASSED run / 全 FAILED run 生产态）
经三趟轨迹分离成熟度后，P_C 纯净稳态 11.62–11.86 µs/gate **快于**
混合历史进程 12.76–13.33（单形折扣 0.93–1.71 µs/gate 三次同号），
F_C 纯净 13.19–13.47 vs 混合 13.34–13.81 平价带（−0.44~+0.05）——
混合坐标（生产一般态）即锚点所测坐标，纯净角更便宜 ⇒ **锚点在全部
次序坐标上保持天花板性**。全部 µs/ns 拒收类。全部可想利用路径经
换名检查逐一判为既有排除 ID 本体或冻结契约功能性变更（7 项新记录，
见 §0），**只产出归档证人与时序平面的闭合，未产出可落地候选**，
按令即空收口。

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R29-A 至本轮两 tip
（`52bd98b` / `ead66ec`）**逐字节未变**（`git diff 7acb666..HEAD --
<切片>` 两点均为空，三十遍全程零 diff；清单核对恰 14 文件（12 + 2）
并全部实际重读，`wc -l` 合计 1796 行与 R29-A / R28-A 一致）。调用面
维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来 `git diff
89f0455..ead66ec -- src/` **完全为空**（跨 R10…R30 全部提交均为
docs，含本轮快进带入的 R29-I 收口）。生产调用方经
本轮全库 grep 复核仍为
`src/run/supervisor.ts:483`（applyTrackingGate；:487 nextTrackingSeq）
/ `src/run/coordinator.ts:444` / `src/run/flowchart-run.ts:320`
（applyChildThreeLine），每子结果一次（~5 次/run），事件表几十级
（41）。R29-A 的预算天花板（12.2–15.7 µs/gate ⇒ ~61–80 µs/run）经
本轮在当前 VM（Node v22.22.2，PATH 前置 nvm）实测复核为
**12.8–15.3 µs/gate ⇒ ~64–77 µs/run**（五格锚点矩阵三次官方运行，
与 R29-A 61–80 / R28-A 61–76 / R27-A 68–84 / R26-A 69–84 完全重叠
同带；无孤点、无悬崖；分解组件绝对值三次基本全落 R29-A 带内，逐项
注记见 §1 表）。量级结论不变——稳态下即使切片零成本，节省上界
~0.06–0.08 ms/run，仍比落地线（数十~数百 ms）低约**三个量级**。

**方法学归档两条（本轮实测确认）**：(1) **锚点同质块方法学是保守
口径**——19a 的 anchor-methodology 检查显示五格同质锚点均值
（13.84–14.23 µs/gate）系统性高于同组成 BLOCK 流（12.84–13.20），
差 +0.6~+1.1 µs/gate 来自锚点测量位于进程生命更早段（tier/反馈
成熟度），方向保守（高估而非低估预算）——二十九遍锚点血统的天花板
性由此获得次序维一侧的独立确证；(2) **纯净进程首趟高读数是成熟度
非纯度**——19e 首趟 15.06–18.20 µs/gate 的抬升经三趟轨迹
（→12.08–14.36→11.62–13.47）证伪为 tier/反馈成熟度（R17-A 冷预算
属地），轴价只取末趟稳态；单趟子进程测量若不做轨迹分离会把冷尾
误记为形状纯度税（+2~+5 µs/gate 的假增量）。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
功能性变更（7 项新记录，见 §0；R12-A…R29-A 共 90 项换名检查全部复核
维持，未追认任何 S12…S29-A-\* ID），按令一律不铸新 ID。

R12-A 分解的五大组件占比（hashAssessment×3 ~43–47% / validateEvent
~21–22% / turn ~12–14% / prescore×2 ~10–12% / O(E) 五扫描+拷贝
~2.1–2.3%，以当轮 contract 格 12.8–13.3 µs 计；绝对值逐项落
R29-A/R28-A 带内或贴带，注记见 §1）在本 VM 逐一复现，R18-A skip
定价与 R19-A replay 定价抽测复现同带（guard-3 skip 1975–1992 ns =
14.8–15.5% of apply；replay find#1 3153–3190 ns = 23.6–25.0% of
apply）。第三十遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；F10a 普查对
  coordinator.ts / flowchart-run.ts / supervisor.ts 三个调用点做了
  **只读**静态见证（调用点上下文实读，零编辑）。未触碰在飞的
  R29-J 与已收口的 R29-I 分支/文件（R29-I.md 仅只读实读用于划界）。
- 先读并遵守（顺序强制）：EXCLUSIONS.md（全表 545 行，含 S13-B-1；
  R12-A…R29-A 空枚举未铸 ID，确认）/ round-30/PLAN.md（A 段 + 全部
  plan-ban 轴）/ R29-A 全文（axis 18 定义 + 全臂 sweep 预热与冷分支
  训练偏置两条方法学——本轮 19a–19d 预热纪律直接沿用）/ R28-A 结论
  与 IC 污染方法学段 / R17-A 组成+温度轴段与 R18-A 组成天花板段
  （本轮新轴的两块最近邻标尺）/ R23-A G×E 耦合轴段（其账本驱动器的
  固定次序夹具属性）/ R11-A「PIC/基准次序」现象首记录段 / R29-H /
  R29-G / R29-F 结论段（最新收口兄弟轴划界）/ R29-I 结论与划界段
  （快进带入后按令补读补划界）。EXCLUSIONS.md /
  PROGRESS.md / 各 PLAN.md / README.md 零触碰。
- 环境：本 VM PATH 前置 `~/.nvm/versions/node/v22.22.2/bin`
  （Node v22.22.2，满足 engines >=22.19.0；VM 默认 22.14.0 不合规
  未使用），tsx 4.23.12、TypeScript 5.9.3、pnpm 10.17.1，
  `pnpm install --frozen-lockfile` 干净完成，全部测量在 v22.22.2
  完成。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：
  R29-A input reference-identity / aliasing-topology face（A 前轮，
  不补铸——axis 19 全部臂的输入是**同一批夹具对象跨臂复用**，引用
  结构钉死为常量，零别名剂量）/ R28-A input key-set geometry face
  （键集恒 schema-精确声明序）/ R27-A scalar numeric value-class
  face（数值恒生产值类）/ R26-A collation-locale face（locale 恒
  en 族）/ R25-A value-domain / string content-class census（字符串
  恒生产纹理 ASCII 且跨臂共享引用）/ **R29-H coverage-relation
  incidence topology face**（H 收口：AC 列表 × 合同准则集的二部
  匹配几何——本轮 requiredChecks/acceptanceCriteria 恒定单准则
  生产形，关联结构零剂量；且其属地在 H 切片门 `coverageMatrixFrom
  Tasks`，与本切片 prescore 的 `coverageOutcome` 每查每档恒同输入）
  / R29-G M2 supervised-engine adjudication lattice（G 收口：judge
  裁决源 × child 结局 × 预算竞态——本轮零监督循环、零 judge、零真
  child 进程语义，19e 子进程仅为测量容器非被测对象）/ R29-F
  boundary output-retention / superseded-snapshot liveness face
  （F 收口：被替换快照的可达存活期——本轮全部臂输出每调用即弃且
  逐臂恒等，零保留剂量）/ **R29-I input-file JSON
  serialization-form face**（I 收口，运行中快进带入，全文实读，
  专项划界见下第 12 条）/ R28-J **ZREP** durable-store zero-state
  provenance（磁盘零态表征——本切片数据路径零磁盘 I/O）/ **R29-C
  PAIRX process workload-composition face**——**最近邻，专项划界
  见下**/ R29-B learned-policy container face（本切片无 learned
  容器）/ R29-E wall-clock sampling topology / timestamp-provenance
  face（本切片门路径零时钟铸造，nowIso 是输入字符串）/ R29-D
  registry-record string-list payload face / R28-I
  invocation-identity / path-referent spelling × cwd face（19e 子
  进程 argv/cwd 恒定单形，非剂量变量）/ R28-H boundary argument-slot
  materialization face（本轮全部调用按声明签名满槽）/ R28-G / R28-F
  / R28-E **PWL** / R28-B / R28-C / R28-D / R27-I / R27-J **SIDEC**
  / R26-J **DELINV** / R26-H / R27-H execution-substrate face（单
  基质 tsx/src；19e 子进程同基质同入口，基质非变量）/ R25-I /
  R26-I 均未移植为 A ID。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX /
  SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX /
  CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG /
  PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX
  未编号；本轮新轴**不取 ALLCAPS 绰号**。
- **与十二个名义近邻的专项划界**（axis 19 逐字复核后判定**非移植非
  补铸**）：
  1. **vs R17-A「事件表组成 (mix) + 温度维」**：R17-A 剂量表内事件
     类型混合与冷/热进程态——axis 19 把表钉死在血统 E=41 夹具
     （每调用同一冻结表，零增长零混合变化），温度钉死稳态热（19e
     子进程 warm 3000 + 三趟轨迹显式剥离成熟度），动的只是**调用流
     的到达次序**。
  2. **vs R18-A「apply/skip 组成定价」**：R18-A 剂量组成**分数**并证
     「skip 混入只降不升」——其比较对象全部是同质块测量，天花板论证
     对次序视盲。axis 19d 恰好检验该视盲假设：命令类次序税跨三 run
     符号翻转（−0.36~+0.51 µs/call），最坏合法修正 ≤ ~2.4 µs/run
     ⇒ 天花板在 µs 精度内维持，R18-A 裁决获次序维补强而非推翻。
  3. **vs R19-A「结局面 / replay 定价」**：R19-A 剂量单调用的结局
     值；axis 19 钉死每调用的输入与结局，只动序列。
  4. **vs R23-A「G×E 自耦合账本」**：R23-A 剂量**表增长**耦合项，
     其账本驱动器的异质流是固定次序夹具（次序从未受控）；axis 19
     关掉增长（每调用同一冻结表——与锚点同口径），单独动次序。
  5. **vs R29-C「process workload-composition face（PAIRX）」**
     （禁移植，最近邻）：PAIRX 剂量**哪些不同工作负载**（C 区双
     估计器）共驻一个进程——工作负载集合是变量；axis 19 把工作
     负载集合钉死（单一 tracking-gate 引擎、同一调用多重集），只动
     **到达次序**。19e 的「纯净 vs 混合」是同一引擎的输入形状历史
     纯度（全 PASSED run 是真实生产进程态），不是异类负载共驻。
     不移植、不补铸、不抢先编号。
  6. **vs R28-A「IC 污染方法学」**：R28-A 把跨臂 IC 污染当**测量
     卫生**问题处理（全臂 sweep 预热纪律，防止别的轴的臂间串扰）；
     axis 19 把分派历史本身当**被测坐标**钉上生产流（预热纪律照常
     沿用——每个次序模式在计时前各跑一遍全 sweep，测的是模式间
     稳态残差而非首训偏置）。
  7. **vs R11-A…R29-A「PIC/基准次序」现象家族**：十九遍的重复
     观察均为锚点格间次序伪影的**注记**（卫生处理后不入价）；本轮
     首次以受控臂（同多重集 × 三种次序 × 前向/回声）把它定价成轴。
  8. **vs R17-A 冷进程预算**：19e 首趟抬升（15.06–18.20）是 tier/
     反馈成熟度，属 R17-A 冷预算属地（[2.7, 8.5] ms/run
     once-per-run，不重测）；axis 19 只取末趟稳态入价。
  9. **vs R27-H execution-substrate face**：19e 子进程与父进程同
     基质（node --import tsx、同源文件、同 cwd），基质非变量；
     子进程仅提供「无先验分派历史」的干净容器。
  10. **vs R22-J SHAPEK「输入对象隐藏类形状面」**（禁移植）：axis
      19 不制造新形状——PASSED/FAILED 两形（FAIL 维度带
      evidenceRefs 键）是**生产铸造机自己发射的**两个既有形状
      （from-child 原样铸造），剂量的是它们在调用流里的排列次序，
      不是形状基数/混合比本身（19b 组成恒 50/50）。
  11. **vs R25-H consumer-stage string-representation census**
      （禁移植）：全部字符串跨臂共享引用、同构造位点，表示类钉死。
  12. **vs R29-I「input-file JSON serialization-form face」**（快进
      带入的最新收口兄弟轴，全文实读后划界）：R29-I 剂量**输入文件
      内容的字节拼写形**（同一 JSON 值的空白形 × 转义拼写 × 对象
      键序）对 I 切片 `JSON.parse` 站点的定价——变量在磁盘字节与
      parse 门。本切片 14 文件数据路径**零文件读取、零
      `JSON.parse`**（`parseTrackingAssessment` 收 `unknown`
      内存对象非字节流，types.ts 实证），axis 19 全部臂输入为内存
      夹具对象跨臂共享引用——不存在可剂量的拼写坐标。切片内唯一
      序列化位点是 hashAssessment / hashSummary 的**发射侧**
      `JSON.stringify`（types.ts:501/:521），其输出字节形是内存
      对象的确定性纯函数（键序即插入序，由 from-child 铸造位点
      钉死，R28-A 键集几何复核在案），属被测函数本体而非可变输入
      拼写维。剂量平面正交（磁盘输入字节 vs 内存调用流次序），
      零交集，不移植、不补铸。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或功能性变更，**一律不铸 S30-A-\* ID**。R12-A…R29-A 共 90 项
  换名检查全部复核维持，不再逐条重列；以下 7 项为本轮**新记录**，
  全部由 axis 19 的时序视角引出）：
  1. **调用方按形状排序/攒批门调用**（把生产流重排成 BLOCK 序吃
     19a 的 0.2 µs/gate 或 19e 的单形折扣）= 切片外调用方变更 +
     事件交错次序是可观察契约（R23-A §0-2 批量化拒绝本体：TA/GT 与
     ambient 事件的交错次序改变）+ 到达序由 `Promise.race` 胜出
     时序决定，重排即改变追加字节序，不等价，拒列；
  2. **门/哈希路径按形状特化双实现**（PASSED 专用 + FAILED 专用以
     恢复单态 IC）= X1-2 平行实现 + S7-A-1/S9-A-1 平行路径拒绝
     结构本体；收益上界即 19e 单形折扣 ≤ ~1.7 µs/gate，拒列；
  3. **AssessmentDimension 形状单态化**（恒物化 evidenceRefs 自有
     键让两 verdict 类共享隐藏类）= 公开输出形状变更（own-key 经
     `in`/Object.keys 可观察；S8-A-3 公开类型家族 + S9-D-1 的 D 区
     镜像已按 µs 拒收）；收益上界即 19b ≤ ~133 ns/hash，拒列；
  4. **hashAssessment 按形状记忆化/缓存** = X1-1 隐藏缓存本体，
     拒列；
  5. **模块加载期对两形状做 JIT 预热调用** = S7-H-3 预热家族
     （三次净负，once-per-process 否决类），拒列；
  6. **evaluateGates / prescore 分支按常见形状重排** = S1-A-6 /
     S7-A-3 家族（ns 级），拒列；
  7. **gate codes / hardCodes 数组形状池化享元** = S1-A-7 可观察
     对象身份改变家族，拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  幂等双 find/死扫描 = S8-A-1 / S11-A-2；五扫描逐条 = X0-4/X2-4/
  S1-A-1/S1-A-9/R1-A 清单；写侧 validateEvent = S4-G-2 族；第二次
  prescore = S1-A-2；turn 恒定输入常量折叠 = R12-A §0-11 伞 +
  R16-A §0-2；skip/replay/hashSummary/H 面/分析-隔离面/G×E/输入面/
  内容类/collation/值类/键集几何/别名拓扑 = R18-A…R29-A 各 §0 维持。
- 未重开任何 X\* / S1-\* … S29-\* 条目。**单变量纪律逐轴钉死**：
  组成恒定（19a/19d 三臂同多重集，前置断言）；表恒血统 E=41 冻结
  夹具（增长关闭）；每调用输入跨臂**同一对象**（键集/别名/值类/
  内容类/表示类全部按对象身份钉死）；温度稳态热（19e 轨迹剥离）；
  单 realm / 单基质；humanInput ≡ `{}`（R21-A）；M=0/K=0（R20-A）。
  S11-A-1 / S11-A-2 / S10-A-1 / S9-A-1 / S8-A-1..3 / S7-A-1..4 /
  X0-4/X2-4 / X0-6 / S6-A-1 / S5-H-1（切片外，未提议移除）专项
  复核：本轮零触碰、零重提，全部维持关闭。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点五格
  （gate-outcome × contract × max-codes）覆盖；配置态逐面复核——
  (a) settle 带/不带 assessment：生产两调用点不带（O(1) no-op 由
  src/ 最强冻结覆盖，第二十一次复验）；(b) trackingConfig 覆写/
  applyUserThreshold：生产恒 DEFAULT（R22-A 双臂已价，X0-7 维持）；
  (c) maxItems 截断态 = S2-A-1 已拒面；(d) **本轮新增：调用流次序
  本身成为受控配置坐标**（19a 3 次序 × 前向/回声 + 19b/19c 2 形状
  × 3 次序 + 19d 3 命令类 × 3 次序 × 前向/回声 + 19e 2 格 × 纯净/
  混合 × 3 趟轨迹，每臂前置逐位等价断言）——无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变
  ——本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking
  无命令权、H/score 不写路由、双 LCB 双归因保留、提升
  proposal-first、无 fsync/完整性重哈希移除、fail-open 家族保持
  关闭）同样天然满足——axis 19 只经公开 applyChildThreeLine /
  applyTrackingGate / nextTrackingSeq / hashAssessment /
  parseTrackingAssessment / validateEvent / computePrescore /
  runTrackingTurn / assessChildObservation / observationFromChild /
  prescoreInputFromObservation 驱动，未改任何阈值、未改任何测试。
  不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。
  无赢家 ⇒ 零生产改动，未提交任何仿真脚本（按令不建 repo scripts），
  仿真全文见本报告附录。不开 PR。

## 1. 预算支配论证复核 + 一条新轴（方法第 1、2 步）

R3-A §3 … R29-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..HEAD -- <切片>` 在派发 tip
   `52bd98b` 为空，逐字节一致（三十遍全程零 diff）——R1-A…R29-A
   全部测量与排除按令转移，但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨二十一段）**：`git diff 89f0455..52bd98b
   -- src/` **完全为空**。生产调用方三点位（supervisor.ts:483 /
   coordinator.ts:444 / flowchart-run.ts:320）经本轮全库 grep 复验，
   无新增调用方。

**锚点矩阵（本 VM 重锚，三次官方运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes；全部在任何次序臂之前测得）：**

```text
                                   run1      run2      run3
PASSED x no-contract               15.0      14.9      15.3  µs/gate (+1 event)
PASSED x contract(3)               12.8      13.3      13.3  µs/gate (+1 event)
FAILED x no-contract               14.1      14.4      14.3  µs/gate (+3 events)
FAILED x contract(3)               13.3      13.4      13.8  µs/gate (+3 events)
FAILED x contract(3) x 2-code      13.9      13.8      14.3  µs/gate (+3 events)
=> ~64–77 µs/run（稳态热；无孤点、无悬崖；五格全部落 R29-A 五格带
   12.2–16.0 内）
```

与 R29-A（12.2–15.7 µs/gate ⇒ ~61–80 µs/run）/ R28-A（61–76）/
R27-A（68–84）/ R26-A（69–84）重叠同带；五格三次运行**无悬崖、无
空洞**。稳态下即使把整个切片优化到零成本，节省上界 ~0.06–0.08
ms/run，仍比战役落地线（数十~数百 ms 或复杂度类下降）低**约三个
量级**。**支配论证（稳态口径）第二十八次复核成立**；生产冷态口径
维持 R17-A 轴 6b 裁决（[2.7, 8.5] ms/run once-per-run，不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
官方运行区间；与 R29-A 括号内区间逐项对照）：

| 组件 | 本轮实测/gate | R29-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1917–2027 ns 每次 ⇒ **5.75–6.08 µs** | 5.93–6.10 µs | ~43–47% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁（run3 1917 低于 R29-A 单次带底 1977 约 3% = VM 窗口性微快，run1/2 带内） |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 328–340 ns + 第三次 hash） | **2.76–2.79 µs** | 2.78–2.79 µs | ~21–22% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G）；parse 单测 328–340 低于 R29-A 356–413 约 5–8%，validateEvent 整体带内 ⇒ 归 VM 窗口 |
| `runTrackingTurn` 完整 | **1.63–1.83 µs** | 1.65–1.67 µs | ~12–14% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构（run3 1829 与其 prescore 同窗抬升 ~10%，run1/2 带内） |
| `computePrescore` × 2 | 663–780 ns 每次 ⇒ **1.33–1.56 µs** | 1.33–1.34（R28-A 1.32–1.62） | ~10–12% | S1-A-2（run3 780 落 R28-A 带内） |
| O(E) 五扫描 + 拷贝（episode 74–79 + seq 46–66 + 双幂等 find 39–41×2 + status 50 + `[...events]` 27 ns） | **274–301 ns**（⇒ 6.69–7.35 ns/event @E=41） | 277–280 ns | **~2.1–2.3%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界（run2 seq 66 单项抖动，合计仍 <0.35 µs） |

\* 以当轮 contract 格（12.8–13.3 µs）计。`applyTrackingGate` 整体
隔离实测 5.69–5.88 µs，与 R29-A（5.71–5.75）贴带互叠。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次官方运行区间）：guard-3 skip（FAILED 零 evidence refs）
1975–1992 ns = 14.8–15.5% of apply（R29-A 12.8–14.8% 贴顶邻接，
绝对值高其带顶 1.7–2.5%）；replay find#1（GT idempotencyKey 命中）
3153–3190 ns = 23.6–25.0% of apply（R29-A 3152–3180 交叠）——组成
天花板 {apply, skip, replay} 两个半边继续成立，且本轮 19d 补上其
**次序半边**（见下）。

**本轮新轴 19：调用流时序 / 分派历史面 (call-stream ordering /
dispatch-history face)**（确定性剂量经公开 applyChildThreeLine /
applyTrackingGate / hashAssessment / runTrackingTurn 全链驱动；
单变量纪律见 §0——组成/表/输入对象/温度/基质全部钉死；锚点+分解
先测（血统可比），每个次序模式在计时前各跑一遍全 sweep（R28-A/
R29-A 预热纪律），每臂前置逐位等价断言；三次官方运行区间）：

```text
证人（F10a–F10b；F10a 静态普查三 run 逐字节一致；F10b 三 run 全过）:
  F10a: 生产流次序普查（三调用点静态扫描 + 上下文实读）——
    coordinator.ts 恰 1 个 applyChildThreeLine 调用点，位于
    Promise.race 胜者处理块内（逐到达门控）；flowchart-run.ts 恰
    1 个，位于 await handle.done 之后（遍历序逐节点门控）；
    supervisor.ts 恰 1 个 applyTrackingGate（逐 settle）；三站点
    无任何按形状排序/攒批逻辑 ⇒ 生产流次序由数据/时序决定，异质
    且任意——生产占本轴的「任意次序」坐标，而二十九遍锚点全部
    测量同质块坐标
  F10b: 次序不变性证人——同一五格多重集（各 20 调用）以 block /
    round-robin / seeded-shuffle 三种次序执行，每格 result +
    追加字节逐位等于预计算期望（门是对冻结表的纯函数，每调用
    自带 id 计数器）⇒ 语义次序不变性是结构性的，唯一可能的价
    是引擎级分派/分支/缓存历史

19a  整门流次序（5 格 × 1000 调用 = 5000 调用/sweep，三臂同多重集
     前置断言；前向 BLOCK→RR→SHUF 后回声 SHUF→RR→BLOCK）:
  run1: 前向 12.88 | +0.21 | +0.20   回声 12.84 | +0.23 | +0.25
  run2: 前向 13.20 | +0.26 | +0.23   回声 13.20 | +0.24 | +0.21
  run3: 前向 16.38(离群) | −3.00 | −3.02
        回声 13.10 | +0.23 | +0.22
  ⇒ 交织溢价 +0.20~+0.26 µs/gate（~1.6–2.0%），12 组对比 10 组
    同号正；run3 前向 BLOCK 单 sweep 离群（16.38 vs 自身回声
    13.10，GC/系统级一次性事件），其回声复现标准溢价 ⇒ 最坏
    ~1.0–1.3 µs/run
  anchor-methodology 检查: 同质锚点均值 13.84/13.96/14.23 vs
    BLOCK 流 12.84–13.20 ⇒ 锚点保守 +0.6~+1.1 µs/gate（进程
    生命早段成熟度），方向为高估 ⇒ 天花板性获独立确证
19b  哈希站点形状交织（PASSED 形 vs FAILED 形——FAIL 维度带
     evidenceRefs 键 = 两元素隐藏类；50/50，200k 调用/臂）:
  block 均值 1958–2008 ns
  alternate 溢价 +68/+133/+68 ns | shuffle 溢价 +63/+57/+72 ns
  ⇒ 三 run 全同号正但 ≤133 ns/hash；×3 腿 ×5 gate ⇒ 最坏
    ~1.0–2.0 µs/run
19c  turn 站点形状交织（deterministicFail 翻转硬门分支；100k/臂）:
  alternate 溢价 +18/+22/+15 ns | shuffle 溢价 +29/+26/+19 ns
  ⇒ ≤29 ns/turn ⇒ ≤ ~0.15 µs/run，亚噪声
19d  命令类次序（apply / skip guard-3 / replay find#1 各 2000，
     R18/R19 组成类；前向+回声）:
  run1: 前向 −0.35/−0.36   回声 +0.08/+0.08
  run2: 前向 +0.13/+0.10   回声 −0.22/−0.25
  run3: 前向 +0.47/+0.04   回声 +0.17/+0.17
  ⇒ 跨 run 跨趟符号翻转 = 抖动主导，无稳定命令类次序税；最坏
    合法修正界 0.39/0.64/2.37 µs/run ⇒ R18-A 组成天花板在 µs
    精度内维持（次序视盲假设获实测补强）
19e  形状纯净进程角（子进程只跑一格：全 PASSED run / 全 FAILED
     run 生产态；warm 3000 + 三趟 ×5000 轨迹；vs 父进程混合
     历史锚点值）:
  P_C: 15.06→12.08→11.83 | 15.59→12.11→11.62 | 17.12→12.10→11.86
       vs 混合 12.76/13.33/13.34
       ⇒ 末趟纯净折扣 −0.93/−1.71/−1.48 µs/gate 三次同号（单形
         IC/分支历史红利 ~7–13%）
  F_C: 13.19/13.47/13.37 vs 混合 13.34/13.42/13.81
       ⇒ −0.15/+0.05/−0.44 平价带
  ⇒ 首趟抬升是成熟度（R17-A 属地，轨迹剥离后弃）；纯净角比混合
    坐标便宜 ⇒ 锚点（混合坐标）在全部次序坐标上保持天花板性
```

五个结论：

1. **测量方法学平面自此补上次序半边**：R14–R16 关规模/载荷维、
   R17/R18 关组成/温度维、R19 关结局维、R20 关驻留维、R21 关 H 面、
   R22 关导出面、R23 关耦合维、R24 关声明粒度维、R25/R27 关值平面、
   R26 关环境维、R28 关键集几何维、R29 关引用结构维之后，**调用流
   的到达次序**（同多重集的排列 × 进程形状历史纯度）是锚点方法学
   最后一个未受控坐标——本轮补上：生产流普查（F10a：三调用点逐
   到达门控、零排序零攒批）、次序不变性证人（F10b）、五组定价
   （19a–19e）与两条方法学归档（锚点同质块保守性；纯净进程轨迹
   分离纪律）。
2. **生产占「任意次序」坐标但语义面免疫**：门是对冻结表的纯函数
   （F10b 逐位证人），次序只在引擎级定价——整门交织溢价 +0.20~
   +0.26 µs/gate 三 run 同号（真实、可复现、µs 级），单形纯净折扣
   ≤ ~1.7 µs/gate（只在全同结局 run 的进程态可达）。
3. **两条既有裁决获次序维补强**：R18-A 组成天花板的次序视盲假设
   经 19d 实测——命令类次序税符号翻转，天花板修正 ≤ ~2.4 µs/run；
   二十九遍同质块锚点经 19a anchor-methodology 检查证为**保守**
   口径（高估 +0.6~+1.1 µs/gate）——天花板性两侧闭合。
4. **价格结构一句话**：分派历史面有真实但 µs 级的价——最坏合法
   对抗面（交织溢价 + 哈希站点溢价上界）≤ ~3.3 µs/run ≈ 预算 ~5%；
   单形折扣不可主动制造（结局由 child 决定，重排/特化/单态化全部
   撞既有排除家族，§0 换名检查 1–3）。µs/ns 拒收类。
5. **不产生候选**：调用方重排 = 事件交错契约（§0-1）；按形状特化
   = X1-2 平行实现（§0-2）；输出形状单态化 = 公开面变更（§0-3）；
   形状 memo = X1-1（§0-4）；加载期预热 = S7-H-3（§0-5）；分支
   重排/享元 = ns 家族（§0-6/7）。

**重开条件全景更新（在 R29-A §1 表基础上新增一行）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| M（openMinors 条数） | 0 | ≈ 4.6–5.8×10⁴ | ~4+ | R20-A |
| K（驻留链 turn 数） | 0（恒 fresh） | ≈ 3×10³ turn | ~2 | R20-A |
| H_L / H_m / H_N（humanInput 三维） | 0 | ≈ 1.2 MB / 2.2–2.3×10⁴ / 4.65–4.74×10⁴ | ~3–5 | R21-A |
| T（analysis content 操作基数） | 0（面零流量） | ≈ 1.50–1.51×10⁵ | ~5 | R22-A |
| G（gates/run，固定 E 线性算术） | ~5 | ≈ 645–714 | ~2.1 | R16-A 系 |
| G（gates/run，含 G×E 自耦合二次项） | ~5 | ≈ 341–344 | ~1.8 | R23-A |
| N_op / N_cl / W×O / Q×D / CF / Cc/Rt 对角线外 | 1 / 1 / 0 / 0 / 2 / 原点 | 见 R24-A 表 | ~4.3–5.5 / 面须先获流量 | R24-A |
| claim 内容类 / turnId 转义编码类 / includes 分歧纹理 | 最便宜格 | 见 R25-A 表 | ~4–4.7 / — | R25-A |
| 环境坐标（collation locale） | en/C 族（最便宜格） | 非字节量轴：部署态坐标 | — | R26-A |
| 数值值类 / seq 量级格 | toFixed(4) 格网 / 恒小 SMI | 最坏合法对抗面 +7.2~+8.4 µs/run | ~4 / — | R27-A |
| 键集几何 W（单对象盈余键，spread 面） | 0（原点） | ≈ 4.3–4.4×10⁴ 盈余键/对象 | ~3.6 | R28-A |
| 表几何（每行盈余键，O(E) 命名读面） | 0（行恒 schema-精确） | 最坏合法 ROWS64 +1.0–1.3 µs/gate | ~3.2 | R28-A |
| 引用结构（对象图别名拓扑，全消费面） | 非平凡混合坐标——语义面免疫 | 面平坦，最坏合法对抗面 ≤ ~3.5 µs/run | ~3.5 | R29-A |
| **调用流次序（分派历史面，全消费面）** | **任意次序（F10a：逐到达门控、零排序）——语义面免疫（F10b）** | **非计数穿越：交织溢价 +0.20~+0.26 µs/gate 三 run 同号 ⇒ ≤ ~1.3 µs/run；哈希站点 ≤ ~2.0 µs/run；单形折扣 ≤ ~1.7 µs/gate 不可主动制造；R18-A 天花板次序修正 ≤ ~2.4 µs/run 符号不稳** | **~3.5–4** | **本轮** |
| G 组成（apply/skip/replay 全混合） | 全 apply | 混入只降不升（次序半边本轮补强） | — | R18-A + R19-A + 本轮 19d |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–15.2 µs/fault | — | R18-A + R22-A |
| 重投递面（once-per-redelivery） | 0 次/run | ~2.8–3.4 µs/次 | — | R19-A + R23-A F3d 形态 |

**可操作重开优先级不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7
家族 `every×includes` 站点 Set 化）仍是第一优先重开对象。本轮关闭
的是**测量方法学的次序维**：输入平面（R14–R16/R24–R29）、组成/
温度/结局/驻留/耦合平面（R17–R20/R23）、H/导出/环境平面（R21/R22/
R26）之后，**调用流以什么次序抵达**自此有价——且额外产出两条
方法学归档（同质块锚点保守性 = 天花板性独立确证；纯净进程三趟
轨迹分离纪律）与 R18-A 天花板的次序维补强供后续轮次引用。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 7 项 + R12-A…R29-A
90 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、功能性
变更或数据补充，按「换名不铸 ID」纪律未列任何 S30-A-\* 候选。空枚举
与 R12-A…R29-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第三十遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `types.ts` | **axis 19b 宿主**（hashAssessment 的 dimensions.map 回调是切片唯一双元素隐藏类站点——FAIL 维度带 evidenceRefs 键；形状交织溢价 +57~+133 ns/hash 三 run 同号但 µs 拒收；形状 memo = §0-4 拒列；输出形状单态化 = §0-3 拒列）；hash×3 地板复现 5.75–6.08 µs；X1-1/S8-G-2 memo 禁维持 | 无新候选 |
| `prescore.ts` | 纯值消费者（PASSED/FAILED 两形经同一维度构造器——19c 溢价 ≤29 ns 的机制下游）；S1-A-3 / S1-A-4/X4-7 / S6-A-1 / S8-A-3 维持；C 轴可操作重开第一优先不变 | 无新候选 |
| `combined-score.ts` | 纯值消费者；S6-A-1 / S7-A-4 / R13-A §0-1 维持 | 无新候选 |
| `gate-apply.ts` | **19a/19d 宿主**（整门交织溢价 +0.20~+0.26 µs/gate；命令类次序符号翻转 ⇒ R18-A 天花板维持；replay find#1 3153–3190 ns 复现）；O(E) 清单 / S11-A-2 / S8-A-1 / S2-A-4 维持；分支重排 = §0-6 拒列 | 无新候选 |
| `from-child.ts` | **19b/19e 形状发射源**（PASSED/FAILED 两形是生产铸造机自己发射的既有形状——SHAPEK 划界 §0-10；按形状特化 = §0-2 拒列）；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `child-tracking.ts` | 锚点/分解宿主复测同带（五格 12.8–15.3 µs/gate）；guard-3 skip 抽测 1975–1992 ns 复现（R18-A 7a 维持，贴 R29-A 带顶 +1.7–2.5%）；19d apply/skip 类边界宿主 | 无新候选 |
| `turn.ts` | **19c 宿主**（deterministicFail 分支交织 ≤29 ns 亚噪声）；R13-A 八子组件 / R12-A §0-11 / R16-A §0-2 维持；runTrackingTurn 复现 1.63–1.83 µs | 无新候选 |
| `roller.ts` | 次序不可见（每调用独立 rollSummary，无跨调用状态）；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `gates.ts` | evaluateGates 硬门分支是 19c 翻转位点（交织价亚噪声）；分支按常见形状重排 = §0-6 拒列；X0-7 阈值冻结维持；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（humanInput ≡ `{}`）；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 / R21-A 维持 | 无新候选 |
| `config.ts` | 生产恒 DEFAULT（配置态矩阵复核宿主）；X0-7 维持 | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方）；S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R29-A F9c(iv) 契约角维持（`packet !== summary` fail-closed 冗余腿保留）；index.ts 纯再导出零逻辑 | 无新候选 |

**对 R29-A 声明的裁决**：「输入对象图的边共享结构补上输入平面最后
一个未受控坐标」在输入平面上成立；但输入平面 + 组成/温度/结局平面
合起来约束的是**喂什么**——**以什么次序喂**（同多重集的排列 ×
进程形状历史）不属于其中任何一维：二十九遍每个计时循环都是同质块，
生产流却按构造次序任意（F10a）。本轮补上并闭合（F10a–F10b 两组
证人 + 19a–19e 五组定价 + 两条方法学归档）。本轮全部新数据**只
产出归档证人、时序平面闭合与两条既有裁决的补强，未产出可落地
候选**。收口的预言力第三十遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件（`git diff
origin/cursor/sota-persistent-opt-83a1...HEAD` 复核）。仿真为临时
脚本未入库（无赢家不落仿真文件，遵守方法纪律；完整源码见附录），
未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R29-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）在派发
tip `52bd98b` 全绿（Node v22.22.2；test 计数 1169 / pass 1168 /
fail 0 / skipped 1，与主线基线一致；退出码 0）。

仿真（临时脚本；seed 参数化可复现，三次官方运行摘要）：

```text
run 1 (seed 0x30a001):
F10a/F10b: 全过（调用点普查 3/3；次序不变性 3 臂逐位）
anchor: 15.0 / 12.8 / 14.1 / 13.3 / 13.9 µs/gate => ~64–75 µs/run
decomposition: hash×3=6076ns | validateEvent=2787ns | turn=1635ns
    | prescore×2=1328ns | 5 scans+copy=274ns (6.69 ns/event)
    | applyTrackingGate=5.69µs
spot: skip guard-3 1976ns (15.5%) | replay find#1 3186ns (25.0%)
19a: 前向 BLOCK 12.88 RR +0.21 SHUF +0.20 | 回声 BLOCK 12.84
     RR +0.23 SHUF +0.25 | 锚点均值 13.84 vs BLOCK 流 12.88/12.84
19b: block 均值 1997ns | alt +69ns | shuf +63ns
19c: block 均值 1672ns | alt +18ns | shuf +29ns
19d: 前向 −0.35/−0.36 | 回声 +0.08/+0.08 => 界 0.39µs/run
19e: P_C 15.06→12.08→11.83 vs 12.76 (末趟 −0.93) |
     F_C 17.20→13.91→13.19 vs 13.34 (末趟 −0.15)
ALL CHECKS PASSED (deterministic, seeded)

run 2 (seed 0x30a002):
anchor: 14.9 / 13.3 / 14.4 / 13.4 / 13.8 µs/gate => ~67–74 µs/run
decomposition: hash×3=6082ns | validateEvent=2759ns | turn=1629ns
    | prescore×2=1326ns | 5 scans+copy=301ns (7.35 ns/event)
    | applyTrackingGate=5.72µs
spot: skip guard-3 1975ns (14.8%) | replay find#1 3190ns (23.9%)
19a: 前向 BLOCK 13.20 RR +0.26 SHUF +0.23 | 回声 BLOCK 13.20
     RR +0.24 SHUF +0.21 | 锚点均值 13.96 vs BLOCK 流 13.20/13.20
19b: block 均值 1958ns | alt +133ns | shuf +57ns
19c: block 均值 1675ns | alt +22ns | shuf +26ns
19d: 前向 +0.13/+0.10 | 回声 −0.22/−0.25 => 界 0.64µs/run
19e: P_C 15.59→12.11→11.62 vs 13.33 (末趟 −1.71) |
     F_C 18.20→14.36→13.47 vs 13.42 (末趟 +0.05)
ALL CHECKS PASSED (deterministic, seeded)

run 3 (seed 0x30a003):
anchor: 15.3 / 13.3 / 14.3 / 13.8 / 14.3 µs/gate => ~67–77 µs/run
decomposition: hash×3=5751ns | validateEvent=2778ns | turn=1829ns
    | prescore×2=1559ns | 5 scans+copy=279ns (6.81 ns/event)
    | applyTrackingGate=5.88µs
spot: skip guard-3 1992ns (14.9%) | replay find#1 3153ns (23.6%)
19a: 前向 BLOCK 16.38(离群) RR −3.00 SHUF −3.02 | 回声 BLOCK 13.10
     RR +0.23 SHUF +0.22 | 锚点均值 14.23 vs BLOCK 回声流 13.10
19b: block 均值 2008ns | alt +68ns | shuf +72ns
19c: block 均值 1813ns | alt +15ns | shuf +19ns
19d: 前向 +0.47/+0.04 | 回声 +0.17/+0.17 => 界 2.37µs/run
19e: P_C 17.12→12.10→11.86 vs 13.34 (末趟 −1.48) |
     F_C 17.43→13.87→13.37 vs 13.81 (末趟 −0.44)
ALL CHECKS PASSED (deterministic, seeded)
```

三次官方仿真运行 F10a 证人行逐字节一致，F10b 三臂三 run 全过；锚点
五格三次同带无悬崖、无孤点；19a 交织溢价 12 组对比 10 组同号正
（run3 前向对被 BLOCK 单 sweep 离群污染，其回声复现标准带）；19b
三 run 同号正 ≤133 ns；19c 全正但亚噪声；19d 符号翻转 = 抖动主导；
19e P_C 末趟折扣三次同号、F_C 平价。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S30-A-\*）。换名检查 7 项新记录见 §0（1–7），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md；本轴轴名
（调用流时序 / 分派历史面 / call-stream ordering / dispatch-history
face）按令**不加 EXCLUSIONS 行**，由 parent 决定 plan-ban。

重开条件本轮**新增一条收口**：

**调用流时序收口**：门调用流的到达次序（同多重集排列 × 进程形状
历史纯度）自此有价——生产流普查（F10a：三调用点逐到达门控、零
排序零攒批 ⇒ 生产占任意次序坐标）、次序不变性证人（F10b：三种
次序逐位同结果——纯函数结构性免疫）、五组定价（19a 整门交织溢价
+0.20~+0.26 µs/gate 三 run 同号 ⇒ ≤~1.3 µs/run / 19b 哈希站点
+57~+133 ns/hash ⇒ ≤~2.0 µs/run / 19c 亚噪声 / 19d 命令类符号
翻转 ⇒ R18-A 天花板次序修正 ≤~2.4 µs/run / 19e 单形折扣
0.93–1.71 µs/gate 三次同号但不可主动制造）+ 两条方法学归档
（同质块锚点保守 +0.6~+1.1 µs/gate = 天花板性独立确证；纯净进程
三趟轨迹分离纪律）。重开条件：(a) 生产调用方引入按形状排序/攒批
（届时事件交错契约须重裁且 19a 溢价可被兑现——但属调用方切片，
非 A）；(b) 切片新增形状多态热站点使 19b 类溢价超过 µs/gate 量级
（现距 ~1 个量级）；(c) E 或 G 增两个量级使交织溢价绝对值越过
数十 µs/run（现距 ~1.5–2 个量级，且须先触发 R14-A/R23-A 的重开
条件）；(d) 引擎变更使 IC/分支历史敏感度量级跃迁（无现实路径，
挂名封口）。**可操作重开第一优先不变**：C 轴（~2.5 个量级，
R15-A；S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file> --seed <hex>`（仓库
根目录，依赖已装，Node ≥22.19；官方三 run seeds：`30a001` /
`30a002` / `30a003`；另有两次草稿烟测 seed `30a000`，用于修正 19e
单趟测量的成熟度混淆——即结论段方法学归档 (2) 的现场——其测量
不作引用）。脚本 sha256：
`6d6ce4432512f4303d312d613977c6c53e1d212b8209f9b1326b829bc1def796`
（921 行）。三次官方运行原始输出已核对入 §5，日志 sha256：
run 1 = `0aaa12e59d716868b1a81732d5dfbae2e4a275ba46c7d2c52817ab31da950020`、
run 2 = `5e8dd3d7a9a37fe7da3f707edd5e7a90e649f1b2fd31c02ff360b858585e5920`、
run 3 = `f829c21635a575cd2a0d03d7c8c39e6b48cd9eff2cc4dc3497ed37dab37ba0d5`；
`pnpm gate` 完整日志 sha256 = `fdcf935a0ec0c6feb5ab21a58f96be86e62a6a4b4530cefdceab22e99295a7b1`。
临时文件（/tmp/r30a-sim.mts、/tmp/r30a-run{1,2,3}.log、
/tmp/r30a-gate.log）归档 sha256 后已删除。

```ts
/**
 * R30-A deterministic anchor + decomposition + NEW-axis simulation
 * (thirtieth pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R29-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R29-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell plus compact spot
 *       re-confirmations of the R18-A skip pricing (guard-3) and the
 *       R19-A replay pricing (find#1);
 *   (c) NEW axis 19: the CALL-STREAM ORDERING / DISPATCH-HISTORY face.
 *       Twenty-nine passes dosed WHAT the slice is fed -- scale (R14/R15),
 *       payload (R16), event-table mix + temperature (R17), apply/skip
 *       composition FRACTIONS (R18), outcome (R19), residency (R20),
 *       human face (R21), export face (R22), G x E growth coupling (R23),
 *       declared-field count (R24), string content class (R25), collation
 *       (R26), numeric value class (R27), key-set geometry (R28),
 *       reference/aliasing topology (R29) -- but every timing loop in
 *       every pass was HOMOGENEOUS: one fixed cell repeated back-to-back
 *       (the anchor's five cells are measured as five separate blocks;
 *       the R23-A ledger driver ran a heterogeneous stream but with one
 *       FIXED order, as a fixture for the growth-coupling axis, never as
 *       a controlled variable). The temporal ORDER in which gate calls of
 *       different shapes arrive through the shared production call path
 *       -- at fixed composition, fixed table, fixed per-call inputs --
 *       has never been a controlled variable anywhere in the campaign.
 *       It is load-bearing here for three reasons:
 *         - PRODUCTION streams are heterogeneous and order-arbitrary by
 *           construction: coordinator.ts gates each child outcome as it
 *           wins a Promise.race (completion order is timing/data
 *           dependent); flowchart-run.ts gates per node in traversal
 *           order; a run's ~5 gates mix PASSED/FAILED, contract and
 *           no-contract shapes in whatever order children finish. The
 *           anchor's homogeneous blocks sit at a coordinate production
 *           only occupies in the all-same-outcome corner;
 *         - the R18-A composition ceiling ("the all-apply anchor bounds
 *           every apply/skip mixture") compares HOMOGENEOUS block
 *           measurements; the ceiling is order-blind -- it silently
 *           assumes per-call cost does not increase when call classes
 *           interleave. If dispatch history (polymorphic inline-cache
 *           dispatch, branch-predictor training on shape-dependent
 *           branches) taxed interleaved streams, the anchor and the
 *           ceiling would understate production. Axis 19 prices exactly
 *           that assumption;
 *         - the campaign has SEEN order effects as a measurement
 *           artifact for 19 passes (the "no-contract cell PIC /
 *           benchmark-order" family, R11-A onward) and answered with
 *           hygiene (full-sweep pre-warm discipline, R28-A / R29-A),
 *           never with a controlled dose on the production-stream
 *           coordinate itself.
 *
 * Archival-fact witnesses (deterministic):
 *   F10a: production stream-order census. The three production call
 *         sites gate PER CHILD inside sequential per-arrival control
 *         flow in ONE process (coordinator.ts Promise.race winner ->
 *         applyChildThreeLine; flowchart-run.ts per-node await ->
 *         applyChildThreeLine; supervisor.ts settle -> applyTrackingGate)
 *         -- witnessed by static scan; each site appears exactly once.
 *         The stream order is data/timing-dependent: no code sorts or
 *         batches gate calls by shape (no such sort exists in the call
 *         sites), so production occupies the ARBITRARY-order coordinate.
 *   F10b: result order-invariance. The gate is a pure function of its
 *         per-call inputs against a fixed table: the same five-cell
 *         multiset executed in block / round-robin / seeded-shuffle
 *         order produces bitwise identical per-cell results and appended
 *         bytes. Semantic order-invariance is structural (each call gets
 *         its own fresh id generator and the same frozen table), so the
 *         ONLY possible price of order is engine-level (IC dispatch
 *         history, branch-predictor state, code/data cache residency).
 *   Doses (19a-19e): the engine-level price of arrival order at fixed
 *         composition:
 *     19a  whole-gate face: the five anchor cells x 1000 calls each
 *          (5000 calls, identical multiset) in BLOCK vs ROUND-ROBIN vs
 *          seeded-SHUFFLE order, forward + reverse-order re-echo;
 *     19b  hash face (the ~41-50% component): PASSED-shape vs
 *          FAILED-shape assessments (FAIL dimensions carry an extra
 *          evidenceRefs key => two element hidden classes inside
 *          hashAssessment's dimension map) in block vs alternating vs
 *          shuffled order at equal counts;
 *     19c  turn face: PASSED-shaped vs FAILED-shaped turn inputs
 *          (deterministicFail flips the hard-gate branch) same arms;
 *     19d  command-class face: the R18/R19 composition classes (apply /
 *          skip guard-3 / replay find#1) at equal counts in block vs
 *          round-robin vs shuffled order -- the direct test of the
 *          R18-A ceiling's order-blindness;
 *     19e  shape-pure process corner: a fresh child process runs ONLY
 *          one cell (all-PASSED-run / all-FAILED-run production states)
 *          -- warmed in-child, then timed -- vs the same cell measured
 *          in this mixed-history process. Prices the monomorphic-purity
 *          delta production actually reaches when a run's children all
 *          share one outcome shape.
 *
 * Single-variable discipline: composition is FIXED inside every 19a/19d
 * comparison (identical multiset, only order moves); the table is the
 * frozen E=41 lineage fixture in EVERY call (growth off -- R23-A's
 * coupling axis stays pinned); per-call inputs are the SAME fixture
 * objects across arms (key-set geometry R28-A, aliasing R29-A, value
 * classes R27-A, string content R25-A all pinned by object identity);
 * steady-hot tier (cold priced by R17-A; the 19e children warm 3000
 * reps in-process before timing so 19e doses shape-history purity, not
 * temperature); single realm / substrate tsx/src (R26-H / R27-H pinned);
 * humanInput === {} (R21-A); M=0/K=0 (R20-A).
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0x30a001..0x30a003.
 * Run: npx tsx /tmp/r30a-sim.mts --seed <hex>   (repo root, Node >=22.19)
 */
import { readFileSync } from "node:fs";
import { spawnSync } from "node:child_process";
import { fileURLToPath } from "node:url";
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn, type TrackingTurnInput } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import { applyChildThreeLine, observationFromChild } from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-26T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];
const SCRIPT_PATH = fileURLToPath(import.meta.url);

const seedIdx = process.argv.indexOf("--seed");
const SEED = seedIdx === -1 ? 0x30a001 : Number.parseInt(process.argv[seedIdx + 1] ?? "", 16);
const pureIdx = process.argv.indexOf("--pure-cell");
const PURE_CELL = pureIdx === -1 ? undefined : process.argv[pureIdx + 1];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    summary?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary =
    opts?.summary ?? (opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran");
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

/* Anchor cell definitions (R3-A..R29-A lineage incl. R14-A max-codes cell). */
interface AnchorCell {
  readonly label: string;
  readonly shortLabel: string;
  readonly kind: "PASSED" | "FAILED";
  readonly withContract: boolean;
  readonly maxCodes?: boolean;
}
const ANCHOR_CELLS: readonly AnchorCell[] = [
  {
    label: "PASSED x no-contract (lineage anchor)",
    shortLabel: "P_NC",
    kind: "PASSED",
    withContract: false
  },
  { label: "PASSED x contract(3)", shortLabel: "P_C", kind: "PASSED", withContract: true },
  { label: "FAILED x no-contract", shortLabel: "F_NC", kind: "FAILED", withContract: false },
  {
    label: "FAILED x contract(3) (hard gate, 3 events appended)",
    shortLabel: "F_C",
    kind: "FAILED",
    withContract: true
  },
  {
    label: "FAILED x contract(3) x max-codes(2 hard)",
    shortLabel: "F_C2",
    kind: "FAILED",
    withContract: true,
    maxCodes: true
  }
];

function makeCellFn(cell: AnchorCell): {
  fn: () => void;
  outcome: () => { result: unknown; appended: unknown };
} {
  const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
  const run = (): { result: unknown; appended: unknown } => {
    let c = 100;
    const out = applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(cell.withContract ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
    return { result: out.result, appended: out.events.slice(events41.length) };
  };
  return { fn: () => void run(), outcome: run };
}

/* ============================================================
 * Child mode (19e shape-pure process corner): warm 3000 reps of ONE
 * cell in this fresh process, then time 5000 reps, print, exit.
 * ============================================================ */
if (PURE_CELL !== undefined) {
  const cell = ANCHOR_CELLS.find((c) => c.shortLabel === PURE_CELL);
  if (cell === undefined) {
    console.error(`unknown --pure-cell ${PURE_CELL}`);
    process.exit(2);
  }
  const { fn } = makeCellFn(cell);
  for (let i = 0; i < 3000; i += 1) fn();
  // three timed passes: the pass trajectory separates tier/feedback
  // maturity (falls across passes) from genuine shape-purity price
  // (persists in the final pass)
  const p1 = bench(fn, 5000);
  const p2 = bench(fn, 5000);
  const p3 = bench(fn, 5000);
  console.log(
    `PURE ${cell.shortLabel} ${(p1 * 1e3).toFixed(2)} ${(p2 * 1e3).toFixed(2)} ${(p3 * 1e3).toFixed(2)}`
  );
  process.exit(0);
}

console.log(`seed: 0x${SEED.toString(16)}`);

/* ============================================================
 * F10a: production stream-order census (static, call-site scan).
 * ============================================================ */
{
  const sites: Array<{ file: string; needle: RegExp; expect: number }> = [
    { file: "/workspace/src/run/coordinator.ts", needle: /applyChildThreeLine\(/g, expect: 1 },
    { file: "/workspace/src/run/flowchart-run.ts", needle: /applyChildThreeLine\(/g, expect: 1 },
    { file: "/workspace/src/run/supervisor.ts", needle: /applyTrackingGate\(/g, expect: 1 }
  ];
  for (const site of sites) {
    const text = readFileSync(site.file, "utf8");
    const hits = [...text.matchAll(site.needle)].length;
    check(
      `F10a ${site.file.split("/").pop()} has exactly ${site.expect} gate call site`,
      hits === site.expect,
      String(hits)
    );
  }
  const coordinatorText = readFileSync("/workspace/src/run/coordinator.ts", "utf8");
  check(
    "F10a coordinator gates the Promise.race winner (per-arrival order, no sort/batch)",
    coordinatorText.includes("Promise.race") &&
      coordinatorText.indexOf("Promise.race") < coordinatorText.indexOf("applyChildThreeLine(") &&
      !/sort[^\n]*outcome|batch[^\n]*gate/i.test(coordinatorText)
  );
  const flowText = readFileSync("/workspace/src/run/flowchart-run.ts", "utf8");
  check(
    "F10a flowchart-run gates per node after awaiting that child (traversal order, no sort/batch)",
    flowText.includes("await handle.done") &&
      flowText.indexOf("await handle.done") < flowText.indexOf("applyChildThreeLine(") &&
      !/sort[^\n]*outcome|batch[^\n]*gate/i.test(flowText)
  );
  console.log(
    "F10a: production stream-order census -- the three production call sites gate per child inside " +
      "sequential per-arrival control flow in one process (coordinator: Promise.race winner; " +
      "flowchart-run: per-node await; supervisor: per-settle). No call site sorts or batches gate " +
      "calls by shape, so the production stream order is data/timing-dependent and heterogeneous by " +
      "construction -- production occupies the ARBITRARY-order coordinate of this axis, while all " +
      "twenty-nine prior anchor passes measured homogeneous per-cell blocks."
  );
}

/* ============================================================
 * Fixtures + F10b: result order-invariance witness.
 * ============================================================ */
const cellRunners = ANCHOR_CELLS.map((cell) => ({ cell, ...makeCellFn(cell) }));

{
  // expected per-cell outputs computed once
  const expected = cellRunners.map(({ outcome }) => JSON.stringify(outcome()));
  const orders: Array<{ label: string; seq: number[] }> = [];
  const block: number[] = [];
  for (let c = 0; c < 5; c += 1) for (let i = 0; i < 20; i += 1) block.push(c);
  orders.push({ label: "block", seq: block });
  const rr: number[] = [];
  for (let i = 0; i < 20; i += 1) for (let c = 0; c < 5; c += 1) rr.push(c);
  orders.push({ label: "round-robin", seq: rr });
  const shuf = [...rr];
  const rng = mulberry32(SEED ^ 0x51ab);
  for (let i = shuf.length - 1; i > 0; i -= 1) {
    const j = Math.floor(rng() * (i + 1));
    const a = shuf[i] as number;
    shuf[i] = shuf[j] as number;
    shuf[j] = a;
  }
  orders.push({ label: "seeded-shuffle", seq: shuf });
  for (const { label, seq } of orders) {
    let allOk = true;
    for (const idx of seq) {
      const runner = cellRunners[idx];
      if (runner === undefined) throw new Error("bad order index");
      if (JSON.stringify(runner.outcome()) !== expected[idx]) allOk = false;
    }
    check(`F10b per-cell outputs bitwise identical in ${label} order (100 calls)`, allOk);
  }
  console.log(
    "F10b: result order-invariance -- the same five-cell multiset executed in block / round-robin / " +
      "seeded-shuffle order produces bitwise identical per-cell results and appended bytes (pure " +
      "function of per-call inputs against the frozen table; each call carries its own fresh id " +
      "counter). Semantic order-invariance is structural; the only possible order price is " +
      "engine-level dispatch/branch/cache history -- which is what 19a-19e dose."
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes
 * (lineage discipline, R29-A identical: 3000 mixed rounds).
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R29-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
for (const { cell, fn, outcome } of cellRunners) {
  const first = outcome() as { result: { applied: boolean }; appended: unknown[] };
  check(`anchor cell applies the gate [${cell.label}]`, first.result.applied);
  const one = bench(fn, 5000);
  anchorUs.push(one * 1e3);
  console.log(
    `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${first.appended.length} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
  );
}

/* ============================================================
 * Contract-floor decomposition (R12-A..R29-A methodology) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
let turnInputP: TrackingTurnInput | undefined;
let assessmentP: TrackingAssessment | undefined;
if (decisionP.apply) {
  assessmentP = decisionP.assessment;
  const assessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  turnInputP = {
    window: {
      contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
      toolSituations: prescoreInputP.toolSituations,
      constraints: observationP.constraints,
      unresolvedDecisions: [],
      confirmedDecisions: [],
      openMinors: []
    },
    prescoreInput: prescoreInputP,
    humanInput: {},
    gateFacts: { deterministicFail: false }
  };
  const localTurnInputP = turnInputP;
  const tTurn = bench(() => void runTrackingTurn(localTurnInputP), 100000);
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)}) => ~${((scans * 1e6) / 41).toFixed(2)} ns/event at E=41`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const mkGT = mkChild("FAILED", { taskId: "tsk_replay_gt" });
    const obsGT = observationFromChild(mkGT.child, mkGT.spec, mkGT.contract);
    const decGT = assessChildObservation({
      observation: obsGT,
      episodeId: "ep_bench",
      runId: String(RUN_ID)
    });
    if (!decGT.apply) throw new Error("replay fixture must apply");
    const hashGT = hashAssessment(decGT.assessment);
    const seqGT = nextTrackingSeq(events41);
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: decGT.assessment,
      assessmentHash: hashGT,
      expectedSeq: seqGT,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: decGT.assessment,
        assessmentHash: hashGT,
        expectedSeq: seqGT,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }
}

/* ============================================================
 * NEW axis 19a: whole-gate face, stream-order arms.
 * Fixed multiset: 5 cells x 1000 calls = 5000 calls per sweep.
 * Arms: BLOCK / ROUND-ROBIN / seeded-SHUFFLE; forward then
 * reverse-order re-echo (drift + order-artifact control).
 * ============================================================ */
{
  console.log("--- NEW axis 19a: whole-gate face, stream order at fixed composition (5000 calls) ---");
  const fns = cellRunners.map(({ fn }) => fn);
  const block: number[] = [];
  for (let c = 0; c < 5; c += 1) for (let i = 0; i < 1000; i += 1) block.push(c);
  const rr: number[] = [];
  for (let i = 0; i < 1000; i += 1) for (let c = 0; c < 5; c += 1) rr.push(c);
  const shuf = [...rr];
  const rng = mulberry32(SEED ^ 0x19a0);
  for (let i = shuf.length - 1; i > 0; i -= 1) {
    const j = Math.floor(rng() * (i + 1));
    const a = shuf[i] as number;
    shuf[i] = shuf[j] as number;
    shuf[j] = a;
  }
  const sameMultiset = (seq: number[]): boolean => {
    const counts = [0, 0, 0, 0, 0];
    for (const idx of seq) counts[idx] = (counts[idx] ?? 0) + 1;
    return counts.every((n) => n === 1000);
  };
  check("19a all three arms share the identical multiset (1000 x 5 cells)", sameMultiset(block) && sameMultiset(rr) && sameMultiset(shuf));

  const timeStream = (seq: readonly number[]): number => {
    const t0 = performance.now();
    for (let i = 0; i < seq.length; i += 1) (fns[seq[i] as number] as () => void)();
    return ((performance.now() - t0) / seq.length) * 1e3; // us/gate
  };
  // full-sweep pre-warm across ALL arm patterns before any timing
  // (R28-A/R29-A discipline: every order pattern trains before measuring)
  timeStream(block);
  timeStream(rr);
  timeStream(shuf);
  const fBlock = timeStream(block);
  const fRr = timeStream(rr);
  const fShuf = timeStream(shuf);
  // reverse-order re-echo
  const rShuf = timeStream(shuf);
  const rRr = timeStream(rr);
  const rBlock = timeStream(block);
  const anchorMean = anchorUs.reduce((s, v) => s + v, 0) / anchorUs.length;
  console.log(
    `  forward: BLOCK ${fBlock.toFixed(2)}us | RR ${fRr.toFixed(2)}us (delta ${(fRr - fBlock).toFixed(2)}) | SHUF ${fShuf.toFixed(2)}us (delta ${(fShuf - fBlock).toFixed(2)})`
  );
  console.log(
    `  re-echo: SHUF ${rShuf.toFixed(2)}us | RR ${rRr.toFixed(2)}us | BLOCK ${rBlock.toFixed(2)}us (deltas vs re-echo BLOCK: RR ${(rRr - rBlock).toFixed(2)}, SHUF ${(rShuf - rBlock).toFixed(2)})`
  );
  console.log(
    `  anchor-methodology check: mean of 5 homogeneous anchor cells ${anchorMean.toFixed(2)}us/gate vs BLOCK stream ${fBlock.toFixed(2)}/${rBlock.toFixed(2)}us/gate (same composition, block order)`
  );
  console.log(
    `  => worst interleave premium this run: ${Math.max(fRr - fBlock, fShuf - fBlock, rRr - rBlock, rShuf - rBlock).toFixed(2)}us/gate x ~5 gates/run = ${(Math.max(fRr - fBlock, fShuf - fBlock, rRr - rBlock, rShuf - rBlock) * 5).toFixed(2)}us/run`
  );
}

/* ============================================================
 * NEW axis 19b: hash face (the ~41-50% component) shape interleave.
 * PASSED-shape vs FAILED-shape assessments: FAIL dimensions carry an
 * extra evidenceRefs key => two element hidden classes inside
 * hashAssessment's dimension map; gate codes differ. Equal counts.
 * ============================================================ */
{
  console.log("--- NEW axis 19b: hash face, shape order at fixed composition ---");
  const mkF = mkChild("FAILED", { taskId: "tsk_hash_f" });
  const obsF = observationFromChild(mkF.child, mkF.spec, mkF.contract);
  const decF = assessChildObservation({ observation: obsF, episodeId: "ep_bench", runId: String(RUN_ID) });
  if (!decF.apply || assessmentP === undefined) throw new Error("19b fixtures must apply");
  const aP = assessmentP;
  const aF = decF.assessment;
  check(
    "19b the two shapes differ exactly on the dosed coordinate (FAIL dims carry evidenceRefs)",
    aP.dimensions.every((d) => d.evidenceRefs === undefined) &&
      aF.dimensions.some((d) => d.verdict === "FAIL" && d.evidenceRefs !== undefined)
  );
  const hP = hashAssessment(aP);
  const hF = hashAssessment(aF);
  const N = 200000;
  const shufIdx = new Uint8Array(N);
  const rng = mulberry32(SEED ^ 0x19b0);
  for (let i = 0; i < N; i += 1) shufIdx[i] = rng() < 0.5 ? 0 : 1;
  // pre-warm both shapes + both interleave patterns
  for (let w = 0; w < 20000; w += 1) {
    void hashAssessment(aP);
    void hashAssessment(aF);
  }
  const tBlockP = bench(() => void hashAssessment(aP), N / 2);
  const tBlockF = bench(() => void hashAssessment(aF), N / 2);
  const tAlt = (() => {
    const t0 = performance.now();
    for (let i = 0; i < N; i += 1) void hashAssessment(i % 2 === 0 ? aP : aF);
    return (performance.now() - t0) / N;
  })();
  const tShuf = (() => {
    const t0 = performance.now();
    for (let i = 0; i < N; i += 1) void hashAssessment(shufIdx[i] === 0 ? aP : aF);
    return (performance.now() - t0) / N;
  })();
  check("19b arm outputs value-identical (hash strings stable across arms)", hashAssessment(aP) === hP && hashAssessment(aF) === hF);
  const blockMean = (tBlockP + tBlockF) / 2;
  console.log(
    `  block P ${(tBlockP * 1e6).toFixed(0)}ns | block F ${(tBlockF * 1e6).toFixed(0)}ns | block mean ${(blockMean * 1e6).toFixed(0)}ns`
  );
  console.log(
    `  alternate ${(tAlt * 1e6).toFixed(0)}ns (premium ${((tAlt - blockMean) * 1e6).toFixed(0)}ns) | seeded-shuffle ${(tShuf * 1e6).toFixed(0)}ns (premium ${((tShuf - blockMean) * 1e6).toFixed(0)}ns)`
  );
  console.log(
    `  => hash-site interleave premium x 3 legs x 5 gates = ${(Math.max(tAlt - blockMean, tShuf - blockMean) * 15 * 1e6).toFixed(0)}ns/run worst`
  );
}

/* ============================================================
 * NEW axis 19c: turn face shape interleave. PASSED-shaped vs
 * FAILED-shaped turn inputs (deterministicFail flips the hard branch).
 * ============================================================ */
{
  console.log("--- NEW axis 19c: turn face, shape order at fixed composition ---");
  if (turnInputP === undefined) throw new Error("19c needs the decomposition turn input");
  const tiP = turnInputP;
  const mkF = mkChild("FAILED", { taskId: "tsk_turn_f" });
  const obsF = observationFromChild(mkF.child, mkF.spec, mkF.contract);
  const piF = prescoreInputFromObservation(obsF);
  const tiF: TrackingTurnInput = {
    window: {
      contextFacts: [`role ${obsF.role}`, `task ${obsF.taskId}`],
      toolSituations: piF.toolSituations,
      constraints: obsF.constraints,
      unresolvedDecisions: [],
      confirmedDecisions: [],
      openMinors: []
    },
    prescoreInput: piF,
    humanInput: {},
    gateFacts: { deterministicFail: true }
  };
  const expP = JSON.stringify(runTrackingTurn(tiP));
  const expF = JSON.stringify(runTrackingTurn(tiF));
  const N = 100000;
  const shufIdx = new Uint8Array(N);
  const rng = mulberry32(SEED ^ 0x19c0);
  for (let i = 0; i < N; i += 1) shufIdx[i] = rng() < 0.5 ? 0 : 1;
  for (let w = 0; w < 10000; w += 1) {
    void runTrackingTurn(tiP);
    void runTrackingTurn(tiF);
  }
  const tBlockP = bench(() => void runTrackingTurn(tiP), N / 2);
  const tBlockF = bench(() => void runTrackingTurn(tiF), N / 2);
  const tAlt = (() => {
    const t0 = performance.now();
    for (let i = 0; i < N; i += 1) void runTrackingTurn(i % 2 === 0 ? tiP : tiF);
    return (performance.now() - t0) / N;
  })();
  const tShuf = (() => {
    const t0 = performance.now();
    for (let i = 0; i < N; i += 1) void runTrackingTurn(shufIdx[i] === 0 ? tiP : tiF);
    return (performance.now() - t0) / N;
  })();
  check(
    "19c arm outputs bitwise identical after arms",
    JSON.stringify(runTrackingTurn(tiP)) === expP && JSON.stringify(runTrackingTurn(tiF)) === expF
  );
  const blockMean = (tBlockP + tBlockF) / 2;
  console.log(
    `  block P ${(tBlockP * 1e6).toFixed(0)}ns | block F ${(tBlockF * 1e6).toFixed(0)}ns | block mean ${(blockMean * 1e6).toFixed(0)}ns`
  );
  console.log(
    `  alternate ${(tAlt * 1e6).toFixed(0)}ns (premium ${((tAlt - blockMean) * 1e6).toFixed(0)}ns) | seeded-shuffle ${(tShuf * 1e6).toFixed(0)}ns (premium ${((tShuf - blockMean) * 1e6).toFixed(0)}ns)`
  );
}

/* ============================================================
 * NEW axis 19d: command-class face (R18/R19 composition classes) in
 * block vs round-robin vs shuffled order at equal counts -- the direct
 * test of the R18-A ceiling's order-blindness.
 * ============================================================ */
{
  console.log("--- NEW axis 19d: command-class order (apply / skip guard-3 / replay find#1) ---");
  // apply class
  const applyRunner = cellRunners[1];
  if (applyRunner === undefined) throw new Error("19d needs the P_C runner");
  const fnApply = applyRunner.fn;
  // skip class (guard-3: FAIL dims with zero evidence refs)
  const mkSkip = mkChild("FAILED", { noEvidence: true, taskId: "tsk_19d_skip" });
  let skipApplied = false;
  const fnSkip = (): void => {
    let c = 100;
    skipApplied = applyChildThreeLine({
      events: events41,
      child: mkSkip.child,
      spec: mkSkip.spec,
      contract: mkSkip.contract,
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    }).result.applied;
  };
  fnSkip();
  check("19d skip class is a skip", skipApplied === false);
  // replay class (find#1 GT idempotency hit)
  const mkGT = mkChild("FAILED", { taskId: "tsk_19d_replay" });
  const obsGT = observationFromChild(mkGT.child, mkGT.spec, mkGT.contract);
  const decGT = assessChildObservation({ observation: obsGT, episodeId: "ep_bench", runId: String(RUN_ID) });
  if (!decGT.apply) throw new Error("19d replay fixture must apply");
  const hashGT = hashAssessment(decGT.assessment);
  const seqGT = nextTrackingSeq(events41);
  let cGT = 730000;
  const tableGT = applyTrackingGate({
    events: events41,
    assessment: decGT.assessment,
    assessmentHash: hashGT,
    expectedSeq: seqGT,
    policyVersion: "track-v1",
    nowIso: NOW_ISO,
    generateEventId: () => `evt_${String(cGT++).padStart(8, "0")}` as EventId
  }).events;
  let replayNoop = false;
  const fnReplay = (): void => {
    replayNoop =
      applyTrackingGate({
        events: tableGT,
        assessment: decGT.assessment,
        assessmentHash: hashGT,
        expectedSeq: seqGT,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      }).result.applied === false;
  };
  fnReplay();
  check("19d replay class is a value-keyed no-op", replayNoop);

  const classFns = [fnApply, fnSkip, fnReplay];
  const M = 2000;
  const block: number[] = [];
  for (let c = 0; c < 3; c += 1) for (let i = 0; i < M; i += 1) block.push(c);
  const rr: number[] = [];
  for (let i = 0; i < M; i += 1) for (let c = 0; c < 3; c += 1) rr.push(c);
  const shuf = [...rr];
  const rng = mulberry32(SEED ^ 0x19d0);
  for (let i = shuf.length - 1; i > 0; i -= 1) {
    const j = Math.floor(rng() * (i + 1));
    const a = shuf[i] as number;
    shuf[i] = shuf[j] as number;
    shuf[j] = a;
  }
  const timeStream = (seq: readonly number[]): number => {
    const t0 = performance.now();
    for (let i = 0; i < seq.length; i += 1) (classFns[seq[i] as number] as () => void)();
    return ((performance.now() - t0) / seq.length) * 1e3; // us/call
  };
  timeStream(block);
  timeStream(rr);
  timeStream(shuf);
  const fBlock = timeStream(block);
  const fRr = timeStream(rr);
  const fShuf = timeStream(shuf);
  const rShuf = timeStream(shuf);
  const rRr = timeStream(rr);
  const rBlock = timeStream(block);
  console.log(
    `  forward: BLOCK ${fBlock.toFixed(2)}us | RR ${fRr.toFixed(2)}us (delta ${(fRr - fBlock).toFixed(2)}) | SHUF ${fShuf.toFixed(2)}us (delta ${(fShuf - fBlock).toFixed(2)})`
  );
  console.log(
    `  re-echo: SHUF ${rShuf.toFixed(2)}us | RR ${rRr.toFixed(2)}us | BLOCK ${rBlock.toFixed(2)}us (deltas vs re-echo BLOCK: RR ${(rRr - rBlock).toFixed(2)}, SHUF ${(rShuf - rBlock).toFixed(2)})`
  );
  console.log(
    `  => R18-A ceiling order-correction bound this run: ${Math.max(0, fRr - fBlock, fShuf - fBlock, rRr - rBlock, rShuf - rBlock).toFixed(3)}us/call x ~5 calls/run = ${(Math.max(0, fRr - fBlock, fShuf - fBlock, rRr - rBlock, rShuf - rBlock) * 5).toFixed(2)}us/run`
  );
}

/* ============================================================
 * NEW axis 19e: shape-pure process corner. A fresh child process runs
 * ONLY one cell (the all-PASSED-run / all-FAILED-run production process
 * states), warmed in-child, then timed -- vs the same cell in this
 * mixed-history process (anchor values above).
 * ============================================================ */
{
  console.log("--- NEW axis 19e: shape-pure fresh process vs mixed-history process ---");
  for (const shortLabel of ["P_C", "F_C"]) {
    const idx = ANCHOR_CELLS.findIndex((c) => c.shortLabel === shortLabel);
    const out = spawnSync(
      process.execPath,
      ["--import", "tsx", SCRIPT_PATH, "--pure-cell", shortLabel, "--seed", SEED.toString(16)],
      { cwd: "/workspace", encoding: "utf8", timeout: 120000 }
    );
    const line = (out.stdout ?? "").split("\n").find((l) => l.startsWith("PURE "));
    check(`19e pure-process child for ${shortLabel} ran`, out.status === 0 && line !== undefined, out.stderr ?? "");
    if (line !== undefined) {
      const parts = line.split(" ");
      const p1 = Number.parseFloat(parts[2] ?? "NaN");
      const p2 = Number.parseFloat(parts[3] ?? "NaN");
      const p3 = Number.parseFloat(parts[4] ?? "NaN");
      const mixedUs = anchorUs[idx] ?? Number.NaN;
      console.log(
        `  ${shortLabel}: pure fresh process pass trajectory ${p1.toFixed(2)} -> ${p2.toFixed(2)} -> ${p3.toFixed(2)}us/gate vs mixed-history process ${mixedUs.toFixed(2)}us/gate (final-pass purity delta ${(p3 - mixedUs).toFixed(2)}us/gate => ${((p3 - mixedUs) * 5).toFixed(2)}us/run if all 5 gates share one shape)`
      );
    }
  }
}

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
