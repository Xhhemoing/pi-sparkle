MODEL_SLUG=claude-fable-5-thinking-xhigh

# R14-E：`src/learning/` 第十四遍复查报告（Round 14）

**战役:** 全库持久 SOTA 优化 Round 14 / R14-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `85b04ba`（含 R14-A / R14-B 空枚举摄取与本派单）
**分支:** `cursor/r14-e-learning-fourteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第十四组新角度枚举为空（有理由的
空枚举，未铸造任何 S14-E-\* ID，与 R10-E / R11-E / R12-E / R13-E 同口径），
切片维持关闭（MORE_OPTIMA=no）。**

1. **零漂移四重确认**：切片 10 个文件（`wc -l` 确认恰 10 文件、合计 1770 行，
   无多无少）自 R1-E 裁决基线（`adb20d7`）至本轮基线（`85b04ba`）**逐字节
   未变**——`git diff adb20d7..HEAD -- src/learning/` 为 0 行、
   `git log adb20d7..HEAD -- src/learning/` 为 **0 提交**，十四轮全程无人触及
   该目录。且 R13-E 基线（`49841da`）之后的全部提交（R13-E..J 摄取、Round 13
   收口、Round 14 开轮、R14-A/R14-B 摄取与 R14-C/D/E 派单）**仅触及
   `docs/reports/sota-opt/`**——`git log 49841da..HEAD --name-only` 全部路径
   均在该目录下，`git diff --stat 49841da..HEAD -- src/ test/` 为空，src 与
   test 一行未动。R1-E..R13-E 的全部规模测量、调用面图景与 S1-E-1..8 /
   S2-E-1..7 / S3-E-1..5 / S4-E-1..3 / S5-E-1..5 / S6-E-1..5 / S7-E-1..5 /
   S8-E-1..3 / S9-E-1..3 共 44 项切片排除裁决（R10-E..R13-E 未铸 ID，其空
   枚举收口一并继承，含 R13-E 三处无名微观）原样成立、原样继承。
2. **本 VM 锚点/矩阵重锚（未拷贝 R13-E 数字，遵派单令；同 VM 双 Node 版本
   交叉，延续 R13-E 协议）**：SLICE-CPU 总量上界锚点 **16 次运行**（8 ×
   Node v22.14.0 + 8 × Node v22.22.2，engines `>=22.19.0` 仅后者合规）
   **17.2–19.2µs/run**（collect 13.1–15.0µs、outcomes 2.2–2.3µs、diagnose
   1.02–1.13µs、bandit-build 0.7–0.8µs；两版本同带）——距落地线（≥10ms）
   **521–583×**，即使把切片 CPU 清零也远不达门槛，在历史带（~11–24µs/run）
   之内（派单点名的 R13-E 带 13.5–15.9µs 与本轮带重叠于同一历史区间，差异
   为 VM/夹具态非代码差异——切片逐字节零漂移）。按历史最保守合成口径
   （R7-E outcomes 带 7.0–7.3µs + R12-E diagnose 分组上界 3.83–3.92µs 同时
   替换本轮两项）总量 ~24.6–27.0µs，仍 **≥370×** 低于门槛。三种口径结论
   一致。配置态 live 面：`applyLearnedRouting` 非空策略 **256–303ns/task**
   （R12-E 258–295 / R13-E 258–312 同带）、空策略 **110–158ns/task**
   （R13-E 111–132 同带偏宽）；`loadLearnedRouting` 全新 root 25.2–35.1µs、
   现实 registry **78.5–104.5µs**（v22.22.2 单看 78.5–86.0；104.5 为
   v22.14.0 单次离群；R8-E 80–85 / R13-E 76.1–88.6 同带，亚 ms 一次性）。
   全部确定性 VERDICT 行 16 次运行**逐位一致**，3/3 sanity 检查全过
   （§1、附录 A）。
3. **配置态 × 命令类矩阵复核无洞**：全部切片进入点行号逐条 grep 复核，
   **与 R8-E..R13-E 记录逐一相同、无新增调用方、无新增命令类**
   （`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
   `run/flowchart-run:681/712`、`track/loop:88/172`；resume/replay 经
   `flowchartForSupervisor` @ `flowchart-run:880/1021` 各单次载入；
   `run --flowchart` @ `cli/main:580` 在 `:708` 载入前返回，无双载入）。
   S8-E-1 双载入按令保护，未做任何去重分析。切片外消费者复核唯一新匹配
   `adaptation/approval-profile.ts:44` 经查仅为文档字符串提及 auto-loop，
   非调用边；其余仍为 `adaptation/eval-routing.ts:223` 的
   `parseLearnedRoutingPolicy`（离线 eval 面，D 切片辖区）、
   `track/primary-split.ts:3` 类型导入、`privacy/record-classes.ts` 文档
   字符串。
4. **本轮两条实扫新轴（§2 前置）**：(a) **规模越线标定**（R14-A E-scale 轴
   在本切片的首次落地——前十三轮均只在生产规模 E≈41/S≈12 测量）：E 阶梯
   {40,400,4000} × S 阶梯 {12,120,1200} 六次运行（两 Node 版本各三次）得
   **E\* ≈ 18,184–18,833 events/run** 才能使切片 CPU 越过 10ms 落地线——
   生产规模 E≈41 距越线 **~444–459×**；阶梯顶 per-event 502–520ns、
   per-signal 78–85ns；collect 百倍规模比 0.72–0.77（亚线性）、outcomes
   1.59–1.70（有界温和超线性，源自路由命中密度构成效应）——**无任何隐藏
   二次方行为**。(b) **JIT 层级审计**（R14-B JIT 档轴在本切片的首次落地）：
   现实预热后 6/6 热导出函数（collect / outcomes / diagnose / apply /
   scoreUserAnswer / taskSuccessFromResult）在两个 Node 版本均为
   `optimized+turbofanned` 且无 `markedForDeoptimization`；`--trace-deopt`
   全锚点运行进程级仅 8 条 deopt（tsx 装载器等），**`src/learning` 帧 0 条**
   ——「修复 deopt 环」这一前十三轮纯读码无法排除的机制类被测量封死。
5. **第十四组新角度枚举为空（§2）**：本轮新透镜为「规模退化 × JIT 健康
   普查」（两条测量轴的代码侧对偶：逐函数问 (a) 是否存在会在超生产规模
   显形的超线性位点，(b) 是否存在会诱发 deopt 环的形状多态位点）。对第十四
   遍全量重读的 10 个源文件逐函数普查，非 O(1)-per-item 位点全数有界
   （`unique` O(n·k) k≤4、`applyLearnedRouting` 三连 includes 级联 M≤10、
   bandit 奖励环 includes A≤10、`clusterSignatures` O(n²) test-only），
   两条新轴实测均无异常。本轮逐字新点名的四个结构位置（diagnostics 双除法、
   diagnostics `filter().length` 计数、apply 三连 includes 级联、超大
   subagent 文本的字符串管道规模面）全部落进理论失败、已关闭机制类或落地线
   算术封死区。**未铸造任何 S14-E-\* 新 ID，无新增排除提议；S13-B-1 未以
   任何形态在本切片重提；R13-E 三处无名微观未补铸、未重开；
   `applyLearnedRouting` 内部零改动。**
6. **验证面**：`pnpm gate` 在 engines 合规的 Node v22.22.2 上**全绿**
   （typecheck ✓ / lint ✓ / test **1168 pass / 0 fail / 1 skipped**（共
   1169）/ build ✓）。切片相关套件 **48 tests / 2 suites / 48 pass /
   0 fail**（两个 Node 版本均过）。系统默认 node 仍为 v22.14.0（违反
   engines `>=22.19.0`，R12-E §0 / R13-E §0 已记录的同一环境事实）；gate
   结论以 v22.22.2 为准。零代码改动下全部硬不变量天然满足。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与
双归因未动；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故
未提交任何 sim/锚点脚本（差异面与新轴脚本测量段进附录 A，遵守
R7-F/R8-C/R9-I/R10-E..R13-E 无赢家纪律）。本切片在其输出契约与数据面语义
下维持 SOTA——第十四遍复查确认：**剩余的全部 ms 级余量仍全部在被排除表点名
保护的 I/O 契约面上**，切片级收口条件（R3-E §7 … R13-E §6）依然成立。
**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行）本轮第十四遍全量实际读码，未依赖前十三轮
  记忆。上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、
  `feedback/`、`persist/`、`privacy/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表，含 S8-E-1 /
  S9-E-2 / S13-B-1）→ round-14/PLAN.md → round-13/PLAN.md +
  round-13/R13-E.md（空枚举）→ R12-E / R11-E / R10-E（空枚举）→ 10 个
  源文件。R14-C / R14-D 在其他分支在跑，未等待、未触碰其报告与分支。
- 基线漂移检查（§结论 1）：切片零提交零 diff；`49841da..85b04ba` 全部提交
  仅触及 `docs/reports/sota-opt/`——前十三轮全部测量与裁决原样成立。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S13-\* 条目。特别地：
  - **S13-B-1**（B 切片辖区：assignPlanned learned 层批内记忆化）未以任何
    变体在本切片重提：本切片辖区只有被调方 `applyLearnedRouting` 本体，
    本轮对其**零改动**（切片零 diff 下必然）；其单次成本锚点 256–303ns/task
    与 R13-B「本体从不是成本主体」的机制性结论一致；重开条件（本体成本
    增长 ≥2 个量级）既未触碰亦未实现。
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护，本轮仅复核其行号与频次
    （§1），未做任何去重分析。并发 promote 下的新鲜性仍是其保留理由。
  - **S9-E-2 pass-2 预过滤是负优化（E=400 五次全负）**——本轮未再尝试
    任何两遍结构的工作集缩减变体；两遍语义按硬不变量维持。规模越线新轴
    （§2 前置）显示即使在 E=4000（百倍生产）两遍 collect 也仅 ~1.4ms，
    该轴不改变 S9-E-2 裁决。
  - **R13-E 三处无名微观**（bandit `includes` 旁路已建 arms Set、
    `applyLearnedRouting` filter+map 双中间数组、`routingPolicyIdentity`
    构造字面量再校验）：位点本轮逐字复核未变（零 diff 下必然），维持
    R13-E 裁决，**未补铸 ID、未重开**（§2 表逐行确认）。
  - R10-E / R11-E / R12-E / R13-E 未铸 S10/S11/S12/S13-E-\*，本轮亦未事后
    发明任何此类编号；R14-A / R14-B 空枚举（tracking / live-routing 辖区）
    与本切片无交，未在本切片追认。
- **换名重提检查**：本轮新定位形态的换名检查全部并入 §2 表格逐行给出，并
  逐一对照 S1-E-\* … S9-E-\* 全表、S13-B-1、R10-E..R13-E 四轮空枚举面
  （R10-E §0 七条、R11-E §2 四条、R12-E §2 五条、R13-E §2 三条换名裁决
  全部原样维持，不再复抄）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  稀疏 → 已批准 R0 基线；双 LCB（beta+normal）/ 双归因（logit+prob-add）
  在位；晋升 proposal-first；无阈值变更；CAS / 凭据 / 数据面契约 / 公开
  签名不动；fail-open / 隐藏模块缓存（X1-1）未引入；bandit/learned
  registry I/O 的 fsync 与完整性重哈希未触碰；corrupt registry
  fail-closed（S2-I-1 / S9-G-1 族）未触碰；两遍
  `collectSignalsFromEvents` 语义未触碰；X0-3（auto-loop 与 from-episode
  不合并）维持；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未 force-push；
  仅提交并推送本分支上的本报告一个文件。
- 环境：本 VM 系统默认 node v22.14.0（`/exec-daemon/node`，PATH 前置遮蔽
  nvm），nvm 另装 v22.22.2（engines `>=22.19.0` 仅后者合规）。锚点与两条
  新轴在两个版本各测（§1/§2，同带）；`pnpm gate` 以 v22.22.2 为准（§4）。
  依赖经 `pnpm install --frozen-lockfile` 安装（pnpm 10.17.1）。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

R7-I 教训（默认态夹具可能掩盖配置态主路径）按令复查。src 自 `49841da` 起
一行未动（且自 `8d598f5` 起 src/test 累计零 diff 的 R13-E 论证链原样有效），
故 R8-E..R13-E §1 矩阵表结构原样成立；本轮逐条 grep 复核全部切片进入点
行号，**与 R13-E 记录逐一相同、无新增调用方**：

| 命令类 × 配置态 | 切片进入点（本轮行号复核） | 频次与规模 |
| --- | --- | --- |
| `adapt auto --run` | `runAutoAdaptFromEvents` @ `cli/adapt:188` → `runAutoAdaptLoop` | 每进程一次，E≈41 |
| `adapt auto --project` | `runAutoAdaptLoop` @ `cli/adapt:205`（空事件表，S4-E-1 面） | 每进程一次 |
| `adapt learn --run` | `proposeRoutingFromRoutedEvents` @ `cli/adapt:168`（S2-E-2 面） | 每进程一次 |
| 普通 `run` | `loadLearnedRouting` @ `cli/main:708`（S2-I-1 已裁决） | 每进程一次 |
| `run --flowchart` | `startFlowchartRun` @ `cli/main:580` → `flowchartForSupervisor` @ `flowchart-run:747→712` 单次载入（在 `cli/main:708` 之前返回，无双载入） | 每进程一次 |
| `run --children` | `cli/main:708` 载入 → `applyLearnedRouting` @ `routing/assign:102` 每任务 → `startFlowchartRun` 内 `flowchartForSupervisor` @ `flowchart-run:747→712` 第二次载入（S8-E-1，勿去重）→ `applyLearnedRouting` @ `flowchart-run:681` 每节点 → `runAutoAdaptLoop` @ `cli/main:783` 一次 | 双载入各一次；任务/节点 ≤10/几十 |
| `track`（startTrackedRun） | `track/loop:88` 载入 → 每任务 `applyLearnedRouting` → `startFlowchartRun` → `flowchart-run:712` 第二次载入（同 S8-E-1）→ `runAutoAdaptLoop` @ `track/loop:172` 一次 | 同上 |
| flowchart resume / replay | `flowchartForSupervisor` @ `flowchart-run:880` / `:1021` 各单次载入（无双载入，R8-E..R13-E 复核维持） | 每恢复一次 |
| SPARKLE_AUTO_ADAPT=0 | 仍收集（persistSignals + updateProjectBandit 照付），仅跳过 propose；R5-E 裁决维持 | 不变 |
| 测试专用面 | `patterns` / `attribution` / `signatures` / `compareSignatures` / `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `parseObservedSignal`(直调) / `proposeRoutingFromAssignments`(deprecated) / `saveLearnedRouting`(deprecated, throw-only) / `learnedRoutingPath`(死导出) 交叉检索复核仍零生产调用方 | 不进热路径 |

切片外其余消费者复核：`adaptation/eval-routing.ts:223` 仅调用
`parseLearnedRoutingPolicy`（离线 eval 面，D 切片）；`track/primary-split.ts:3`
仅导入类型；`privacy/record-classes.ts:212/224` 仅文档字符串提及路径布局；
本轮交叉检索的唯一新匹配 `adaptation/approval-profile.ts:44` 经查仅为
docstring 提及「auto-loop never CAS-promotes」，非调用边。**矩阵结论：
无新洞**。

SLICE-CPU 锚点本 VM 重测（**16 次运行**：8 × Node v22.14.0 + 8 ×
Node v22.22.2；seeds `0xe14e01` / `0xe14e04` / `0xe14e03`——`0xe14e02`
被拒绝采样出局：其 routed 夹具产出 outcomes=0 不过非平凡 sanity 检查，
属夹具非代码，与 R12-E diagnose 补测拒绝采样同协议；夹具镜像 R8-E..R13-E
生成器，R1-A 组成 E=41）：

```text
collect=13.1-15.0us  outcomes=2.2-2.3us  diagnose=1.02-1.13us  bandit-build=0.7-0.8us
total in-slice CPU ~17.2-19.2us per full auto-adapt run （两 Node 版本同带：
  v22.14.0 17.2-19.2 / v22.22.2 17.3-17.9，版本差异不可分辨）
vs landing bar >=10000us  ->  521-583x below EVEN IF ZEROED
最保守合成口径（R7-E outcomes 7.0-7.3us + R12-E diagnose 分组上界 3.83-3.92us 同替）
  -> ~24.6-27.0us，仍 >=370x 低于门槛
configured-state: applyLearnedRouting(avoid=10, M=10) = 256-303ns/task （R12-E 258-295 / R13-E 258-312 同带）
                  applyLearnedRouting(空策略)        = 110-158ns/task （R13-E 111-132 同带偏宽）
                  loadLearnedRouting fresh(ENOENT)   = 25.2-35.1us/次
                  loadLearnedRouting 现实 registry    = 78.5-104.5us/次 （v22.22.2 单看 78.5-86.0；
                                                        104.5 为 v22.14.0 单次离群；R8-E 80-85 /
                                                        R13-E 76.1-88.6 同带，亚 ms 一次性，S8-E-1 带）
```

注：本轮夹具组成为 collected=15 / outcomes=2 / diagnoseGroups=1（与
R11-E 15/2/1 相同、与 R13-E 14/2/1 接近，夹具差异非代码差异）；diagnose
分组路径经 diagnoseGroups=1 全数走到，保守合成口径另引 R12-E 分组上界
3.83–3.92µs 替换。全部确定性 VERDICT 行 16 次运行**逐位一致**，3/3
sanity 检查全过。**锚点复核成立：该切片不存在不推翻既有排除就能达门槛的
候选。**

## 2. 本轮两条实扫新轴 + 第十四组新角度枚举：有理由的空枚举

### 2.0 新轴 A：规模越线标定（本切片首次）

R14-A 对 tracking 切片做过 E-scale 越线标定（10ms/run 需 E≈1.4–1.7×10⁵）；
本切片前十三轮全部锚点只在生产规模（E≈41、S≈12）测量，「规模要长到多大
该切片才可能有落地级余量」从未被量化。本轮 E 阶梯 {40,400,4000} × S 阶梯
{12,120,1200}，两 Node 版本各 3 次运行（seeds `0xe14e11`–`0xe14e13`，
确定性阶梯 VERDICT 六次逐位一致）：

```text
E-ladder（collect+outcomes）: E=40: 41 events -> collected=14 outcomes=2
                              E=400: collected=158 outcomes=66
                              E=4000: collected=1494 outcomes=721
  per-event 阶梯顶 502-520ns；collect 百倍规模比 0.72-0.77（亚线性——Map
  摊还 + 分支预热），outcomes 1.59-1.70（有界温和超线性——路由命中密度
  构成效应：E 越大 TASK_RESULT 绑定成功率越高，每事件做更多
  parseOutcomeObservation 实工作，非算法超线性）
S-ladder（diagnose+bandit-build）: S=12 groups=0 / S=120 groups=3 /
                              S=1200 groups=6；per-signal 阶梯顶 78-85ns
E*（切片 CPU 越过 10ms/run 的事件规模，S~0.35E 生产耦合口径）
  = 18,184-18,833 events/run（6 次运行区间；两版本同带）
  -> 生产规模 E≈41 距越线 ~444-459x
```

**裁决**：无隐藏二次方行为——名义非 O(1)-per-item 位点全数有界
（`unique` O(n·k) k≤4；`applyLearnedRouting` includes 级联 M≤10；bandit
奖励环 `includes` A≤10 有目录上界；`clusterSignatures`/`averageSimilarity`
O(n²) 为 test-only 零生产调用方）。即使在越线规模 E\*，成本主体也是两遍
collect 的 O(E) 扫描本体——其结构变换被 S1-E-1 / S9-E-2 / S4-E-1 + 硬
不变量（两遍语义）四向封死，故**该轴不产生任何新候选，反而把「切片在
任何现实规模下无落地余量」从单点测量升级为标定曲线**。

### 2.1 新轴 B：JIT 层级审计（本切片首次）

R14-B 对 live-routing 切片做过 JIT 档审计（13 热函数 turbofan、0/17
deopt）；本切片镜像执行：现实预热（锚点夹具 × 锚点 reps）后经
`%GetOptimizationStatus` 读 6 个生产热导出函数——

```text
collectSignalsFromEvents / outcomesFromRoutedRun / diagnoseModelProjectIssues /
applyLearnedRouting / scoreUserAnswer / taskSuccessFromResult
  -> 全部 status=0b1010001 [isFunction+optimized+turbofanned]，无
     markedForDeoptimization（v22.14.0 与 v22.22.2 逐一相同，6/6 HEALTHY）
--trace-deopt 全锚点运行：进程级 8 条 deopt（tsx 装载器/一次性初始化），
  src/learning 帧 0 条
```

**裁决**：不存在 deopt 环或形状病理可修——「JIT 病理修复」是纯读码无法
证伪的最后一个机制类假说（S8-A-3 / S9-D-1 / S8-H-1 的 PIC 观察线在邻
切片曾两度左右测量符号），本轮以直接观测封死：切片热面全部驻留 TurboFan
稳态，条件 spread 的形状多态（S4-E-3 / S8-E-2 已裁决面）未诱发任何再
优化循环。**该轴不产生任何新候选。**

### 2.2 第十四组枚举表

方法：本轮代码侧透镜「**规模退化 × JIT 健康普查**」（两条测量轴的对偶）：
对第十四遍全量重读的 10 个源文件逐函数问 (a) 有无会在超生产规模显形的
超线性位点；(b) 有无会诱发 deopt 环/形状病理的位点；同时按惯例对本轮逐字
新点名的结构位置做换名检查（对照 EXCLUSIONS 全表 + 十三轮 E 报告 + 四轮
空枚举面）。扫描结果：**零个可立项形态**，逐类裁决如下（均未铸造新 ID）：

| 形态（本轮第十四遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **`diagnoseModelProjectIssues` 双除法**（`diagnostics:36`：`sum/samples/100` 可代数合并为 `sum/(samples*100)`）——从未逐字点名 | 每组一次除法，ns 级 | **理论失败拒列（不铸 ID）**：浮点除法非结合，合并后舍入位可观察发散（逐位等价性不成立，S6-A-1 半格点发散同族）；且落地线算术封死（diagnose 全额 1.02–1.13µs 即天花板） | S6-A-1 族 + 逐位契约 |
| **`diagnoseModelProjectIssues` failures 计数的 `filter().length` 中间数组**（`diagnostics:37`）——从未单列 | 每组一个中间数组分配，ns 级 | 已关闭类真子集拒列：diagnose 组内多遍融合 = S1-E-6（真实规模亚噪声、10× 更慢实测）；filter→单遍计数 = S2-J-6 同机制邻切片先例（被 I/O 支配）；S 阶梯顶 diagnose 全额 42–45µs 仍低于落地线 200× | S1-E-6 / S2-J-6 族 |
| **`applyLearnedRouting` 三连 `includes` 级联**（`learned-routing:206-215`：prefer/preferredModel/primaryModelId 至多三次 O(M) 线性扫描）——毗邻但不同于 R13-E 无名微观 #2（双中间数组） | 非空策略全额 256–303ns/task 即天花板；M≤10 | 已关闭类机制变体拒列：小集合线性扫描族（R6-E/R7-E 收口、S1-D-8、R10-E `familyFromRouted` includes 同裁决）；Set 化在 M≤10 且至多三探针下构造费 > 扫描省费（S1-B-6 M=10 实测更慢同型）；**S13-B-1 辖区纪律显式声明：`applyLearnedRouting` 内部零改动，R13-E 无名微观 #2 未重开未补铸** | S1-B-6 / S1-D-8 / R10-E §2 行 4 + S13-B-1 边界 |
| **超大 subagent 文本的字符串管道规模面**（`signals:372-397`：extractAssistant 折叠 + PEER_NEGATIVE.test + truncate 全量 `\s+/g` replace 对 MB 级 Pi run 文件为 O(len)）——规模轴视角首次整体点名 | 假设 MB 级文件才可达 ms | 三向封死拒列：truncate 流式早退 = S2-E-5（实测更慢）；折叠 = S1-E-3；整管道被切片外 `JSON.parse` + `readFile` I/O 支配（S1-E-5 面，JSON.parse 常数 ≥ 正则扫描数倍）；生产 Pi run 文件为 KB 级 | S2-E-5 / S1-E-3 / S1-E-5 |
| **R13-E 三处无名微观复核**（bandit `includes` 旁路已建 arms Set @ `bandit-store:72`、apply avoided-Set filter+map 双中间数组 @ `learned-routing:198-202`、`routingPolicyIdentity` 字面量再校验 @ `learned-routing:43`） | 各 ns~亚 µs | **位点逐字未变（零 diff 下必然），R13-E 裁决逐字维持，不补铸 ID、不重开**；规模轴旁证：bandit 奖励环在 S=1200 全额 52–57µs 仍低于落地线两个量级 | R13-E §2 行 1–3 |
| **被调侧记忆化整族**（stableProjectKey / truncate / apply 结果缓存） | 各 ≤4 / ≤S / ≤10+几十次每 run | 双重辖区封死维持：X1-1（模块缓存）+ S13-B-1 辖区（调用点批内记忆化，B 切片已裁决无收益池）；R13-E §2 行 4 逐字维持 | X1-1 / S13-B-1 + R13-B §2d |
| 两遍 `collectSignalsFromEvents` 的任何结构变换（合遍/预过滤/空快路径/惰性构建/工作集缓存） | pass 1+2 合计 ~13.1–15.0µs/run 全部；E\* 标定显示即使 E=4000 也仅 ~1.4ms | 三向封死 + 硬不变量明令：S1-E-1（合遍）/ S9-E-2（预过滤负优化）/ S4-E-1（空快路径生产不可达）/ R12-E 行 4（惰性构建）；规模轴不改变任何一项裁决 | S1-E-1 / S9-E-2 / S4-E-1 + 硬不变量 |
| pass-1 五 Map / JUDGE_DECISION 双 Map.get / verdict 双分派链 / 条件 spread 形状 / baseSignal 形状多态 | ns 级；JIT 轴实测无形状病理（6/6 turbofanned、0 deopt） | 换名重提拒列 + JIT 轴关闭「形状病理修复」假说：S1-E-2 / R10-E 行 2–3 / S8-E-2 / S4-E-3 逐字维持 | S1-E-2 / S4-E-3 / S8-E-2 + 本轮新轴 B |
| `from-episode` 内部字面量再校验 / Date.parse / familyFromRouted includes / isCompleteRoute 谓词序 / applyCascadeRetry / seen-Set 单探针 / outcomeKindFromResult 死形参 | ns 级 | 全部维持 R10-E..R13-E 拒列，位点未变 | R10-E..R13-E §2 |
| `auto-loop` 三路拼接 / persistSignals filter 双求值 / some-探针跨函数去重 / 顺序读 / optimizedPolicy filter+map / candidatesFor().find() | ns~µs 级或 I/O 支配 | 换名重提拒列：R12-E 行 13 / S1-E-4 / R11-E 行 4 / S1-E-5 / S2-E-6 / S1-D-2 / X0-3 逐字维持 | S1-E-4/5 / S2-E-6 / S1-D-2 / X0-3 |
| `bandit-store` 不可变链批量化 / 吸收析取 / mkdir / 紧凑序列化 / no-change 跳写 / fsync / 重哈希 | 每 run ≤µs 级或 I/O 契约面 | 换名重提拒列 + 硬不变量：R12-E 行 3 / S6-E-3/4 / S2-E-4 / X1-2 维持 | R12-E §2 行 3 / S6-E-3/4 / S2-E-4 |
| I/O 契约面全体：fsync 丢弃、完整性重哈希跳过、no-change 跳写、双载入去重、corrupt registry fail-open、readdir 并行化、惰性载入、mkdir 提升、紧凑序列化 | 切片相关全部 ms 级余量所在（本轮实测 registry 载入 78.5–104.5µs/次、写侧更贵） | 排除行逐字重现 + 硬不变量明令封死；`routingPolicyContent` pretty-print 参与 contentHash ⇒ 序列化变更破坏 CAS 契约，比 S6-E-4 更硬 | X0-3 / S2-E-1/4 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1 / S1-E-4/5 / S2-I-1 / S9-G-1 族 + CAS |
| 测试专用面任何优化（`patterns` / `attribution` / `signatures` 三文件 + 五个测试专用导出；含 clusterSignatures O(n²) 与 computeFeatureSim keySet 双拷贝） | 不进任何生产路径 | 本轮交叉检索复核仍零生产调用方，自动拒列（规模普查中唯一名义 O(n²) 生产外位点） | S1-E-7/8 / X2-6 族 |
| 死导出清理 / `scoreTaskResult` 尾部 `return 70` | 0ns 运行时 | 不立项：无收益路径即无候选资格；公开签名保留（硬不变量）；R11-E 行 5 / R12-E 行 5 同式 | 硬不变量（公开签名） |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（十四轮）、src/test 自 R13-E 基线零漂移、调用位点行号全数复核一致（§1，含 approval-profile 假阳性排除） | — |

**提议排除 ID：无（本轮无「新增排除 ID」表——空枚举未铸任何 S14-E-\*）。**
切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选。前十三轮已
逐类收口的九个角度类 + R13-E 被调侧结构复用透镜之外，第十四遍以「规模退化
× JIT 健康」双轴普查新点名的四个结构位置全部落进理论失败、已关闭机制类或
落地线算术封死区；两条新轴的实测结论（E\* ≈ 1.8×10⁴ 为生产 444–459×；
6/6 turbofanned、0 学习帧 deopt）把本切片「无落地余量」从单点测量升级为
标定曲线 + JIT 稳态双重证明。

## 3. 逐文件收口（前十三轮收口之上的本轮新检查点）

| 文件 | 本轮第十四遍新视角检查 | 结论 |
| --- | --- | --- |
| `diagnostics.ts` | **双除法（§2 行 1）判逐位理论失败拒列**；**`filter().length` 中间数组（§2 行 2）判 S1-E-6/S2-J-6 族拒列**；S 阶梯 {12,120,1200} 首次标定（0.21→45µs，groups 0→6，无超线性）；冗余 Map.set = S3-E-3 维持；`unique`/`mode` 小集合族维持 | 无候选 |
| `learned-routing.ts` | **三连 includes 级联（§2 行 3）判小集合族 + S1-B-6 同型拒列（内部零改动，S13-B-1 边界显式遵守）**；R13-E 无名微观 #2/#3 位点复核未变不补铸；JIT 轴 applyLearnedRouting turbofanned；载入面全体 = 派单 reject 真子集维持；S8-E-1（勿去重）、S8-E-3、CAS 契约维持 | 无候选 |
| `signals.ts` | **字符串管道规模面（§2 行 4）判 S2-E-5/S1-E-3/S1-E-5 三向封死**；E 阶梯 collect 亚线性（0.72–0.77）确认无隐藏二次方；JIT 轴 4 函数 turbofanned、条件 spread 无形状病理；两遍结构维持硬不变量；S1-E-1..3 / S2-E-5/7 / S3-E-1/5 / S4-E-1/3 / S5-E-2/3 / S6-E-1/2/5 / S7-E-1/2/5 / S8-E-2 / S9-E-2/3 维持 | 无候选 |
| `from-episode.ts` | E 阶梯 outcomes 温和超线性定性为路由命中密度构成效应（非算法超线性，E=4000 全额 0.61ms 仍低于落地线 16×）；内部字面量再校验 / Date.parse / includes / 谓词序 / seen-Set / 死形参维持 R10-E..R13-E 拒列；JIT 轴 outcomesFromRoutedRun turbofanned | 无候选 |
| `bandit-store.ts` | R13-E 无名微观 #1（includes 旁路）位点复核未变不补铸；S=1200 奖励环全额 52–57µs 规模旁证；不可变链批量化 = R12-E 行 3 维持；fsync/重哈希/mkdir/序列化按令未触碰 | 无候选 |
| `auto-loop.ts` | 函数级普查无超线性位点（三路拼接 O(S)、persistSignals O(S)×I/O）；既有拒列全维持（§2 行 10）；LEARN_EVALUATION_PLAN 双定义维持 R9-E 纯重构拒列 | 无候选 |
| `task-success.ts` | JIT 轴 taskSuccessFromResult turbofanned（copyDefinedBinding 条件 spread 无形状病理）；S9-E-3、S2-E-7、S8-E-2 覆盖维持 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | 规模普查中唯一名义 O(n²) 位点（clusterSignatures / averageSimilarity / computeFeatureSim 双拷贝）全部 test-only，交叉检索复核仍零生产调用方，自动拒列；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核（依赖 `pnpm install
--frozen-lockfile`，pnpm 10.17.1）：

```bash
# Node v22.22.2（nvm；engines >=22.19.0 合规）
pnpm gate        # typecheck ✓ + lint ✓ + test ✓ + build ✓
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.14.0 与 v22.22.2 均过）
```

锚点脚本（临时置于 `/tmp/r14e-anchor.mts`，未入库以遵守无赢家纪律；差异面
见附录 A）共 **16 次独立运行**（8 × v22.14.0 + 8 × v22.22.2），每次 3/3
sanity 检查全过，全部确定性 VERDICT 行跨 16 次运行**逐位一致**。代表性
一次运行（v22.22.2）：

```text
VERDICT fixtures: E=41 collected=15 routedE=41 outcomes=2 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=13.3us outcomes=2.2us diagnose=1.03us bandit-build=0.8us | total in-slice CPU ~17.4us per run vs landing bar >=10000us (575x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=272ns/task | empty policy=113ns/task -> x10 tasks/run = 2.72us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=30.4us | realistic registry (baseline+1 candidate)=80.2us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

新轴脚本（`/tmp/r14e-scale.mts` 6 次运行、`/tmp/r14e-jit.mts` 两版本各
1 次 + `--trace-deopt` 全锚点 1 次）代表性输出：

```text
VERDICT-BENCH crossing: per-event(E-ladder top)=507ns per-signal(S-ladder top)=79ns | E* (slice CPU crosses 10ms/run) ~= 18,705 events/run | linearity collect x100-scale ratio=0.74 outcomes=1.61 (1.0 = perfectly linear)
JIT-AUDIT VERDICT: 6/6 hot exported functions on an optimized tier after realistic warm-up; unhealthy=0
--trace-deopt: 8 process-wide deopt lines, 0 in src/learning frames
```

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round14-r14e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R13-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐形态有理由；第十四遍以「规模退化 × JIT
  健康」双轴普查新点名的四个结构位置全部落进理论失败、已关闭机制类或落地
  线算术封死区；两条新轴本身均以「无异常」收口，未发现第十个机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令封死的
  I/O 契约面上；CPU 面即使清零也距门槛 521–583×，最保守合成口径 ≥370×；
  越线标定 E\* ≈ 18,184–18,833 为生产规模 444–459×）。
- 矩阵空洞：无（调用位点行号与 R8-E..R13-E 逐一相同；唯一新 grep 匹配
  approval-profile 为 docstring 假阳性；配置态 live 面与载入面锚点全数
  复测在带内；双 Node 版本交叉不可分辨）。
- JIT 病理假说：以直接观测关闭（6/6 turbofanned、0 学习帧 deopt，两版本
  一致）。
- S13-B-1 边界：`applyLearnedRouting` 本体零改动，重开条件（本体成本增长
  ≥2 个量级）未触碰亦未实现；R13-E 三处无名微观未补铸未重开。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R13-E §6，并新增一条可标定判据：SLICE-CPU
锚点失效（全切片 CPU 增长 ≥2 个量级，本轮复核值 17.2–19.2µs、最保守合成
口径 ~24.6–27.0µs）或任一 I/O 契约排除（X0-3 / S2-E-1/4 / S1-G-1 /
S1-E-4/5 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1）被正式推翻，或切片/调用面
出现实际代码漂移，**或生产事件规模逼近本轮标定的越线曲线（E 持续
≥10⁴/run，本轮 E\* = 18,184–18,833）**。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

主锚点脚本全文（`/tmp/r14e-anchor.mts`，`npx tsx /tmp/r14e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下四处替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R14-E；
`NOW = "2026-08-24T23:00:00.000Z"`；SECTION 1 的 seeds
`0xe14e01`/`0xe14e04`/`0xe14e03`（routed 生成器用 `0xe14e04`——顺位
`0xe14e02` 的 routed 夹具产出 outcomes=0 不过非平凡 sanity 检查，按
R12-E 拒绝采样协议出局，探针记录 0xe14e02→0、0xe14e04→2、0xe14e05→5、
0xe14e06→3、0xe14e07→4、0xe14e08→0、0xe14e09→3）；SECTION 2 的
`mkdtemp` 前缀 `r14e-fresh-`/`r14e-reg-` 与 detector identity
`r14e-anchor`。其余逐字同 R10-E 附录 A。

新轴 A 脚本（`/tmp/r14e-scale.mts`）：复用同一 `mulberry32` /
`genCollectEvents` / `genRoutedEvents` / `genSignal` / `banditBuild`
生成器（逐字镜像），seeds `0xe14e11`–`0xe14e13`。测量段：E 阶梯
{40,400,4000} 对 `collectSignalsFromEvents` / `outcomesFromRoutedRun`
计时（reps = max(200, 80000/E)），S 阶梯 {12,120,1200} 对
`diagnoseModelProjectIssues` / banditBuild 计时（reps = max(400,
160000/S)），以阶梯顶 per-event/per-signal 成本 + 生产耦合口径 S≈0.35E
计算 `E* = 10ms / (perEvent + 0.35·perSignal)`，并输出百倍规模线性比。

新轴 B 脚本（`/tmp/r14e-jit.mts`）：`node --allow-natives-syntax
--import tsx` 运行；锚点夹具 × 锚点 reps 现实预热后经
`new Function("fn","return %GetOptimizationStatus(fn);")` 读 6 个热导出
函数状态位（isFunction/optimized/maglevved/turbofanned/
markedForDeoptimization 等）；另以 `node --trace-deopt --import tsx`
全量运行主锚点并统计 `src/learning` 帧 deopt 行数。注：
`--allow-natives-syntax` 不可经 NODE_OPTIONS 传入（Node 拒绝，exit 9），
须直传 node argv。

16 次主锚点运行原始区间（8 × v22.14.0 + 8 × v22.22.2）：

```text
SLICE-CPU 总量:
  v22.14.0: 18.6 / 17.4 / 19.2 / 18.9 / 17.5 / 17.2 / 17.2 / 18.9 us
  v22.22.2: 17.4 / 17.7 / 17.5 / 17.9 / 17.3 / 17.4 / 17.4 / 17.8 us
collect:    13.1-15.0 us（v22.22.2 13.2-13.7）
outcomes:   2.2-2.3 us
diagnose:   1.02-1.13 us
bandit:     0.7-0.8 us
apply(non): 256-303 ns/task
apply(空):  110-158 ns/task
load fresh: 25.2-35.1 us
load real:  78.5-104.5 us（v22.22.2 单看 78.5-86.0；104.5 为 v22.14.0
            批一 run 4 单次离群，同次 load fresh 33.4 亦为该批最高）
VERDICT 行: 16/16 逐位一致；sanity 3/3 x16 全过
```

6 次规模标定运行原始值（3 × v22.22.2 + 3 × v22.14.0）：

```text
E*:            18,705 / 18,833 / 18,226 | 18,595 / 18,683 / 18,184
per-event 顶:  507 / 502 / 519 | 509 / 508 / 520 ns
per-signal 顶: 79 / 82 / 85 | 84 / 78 / 85 ns
线性比 collect: 0.74 / 0.72 / 0.77 | 0.74 / 0.74 / 0.76
线性比 outcomes: 1.61 / 1.66 / 1.70 | 1.59 / 1.60 / 1.65
阶梯 VERDICT 行: 6/6 逐位一致
```

JIT 审计（两版本各 1 次 + trace-deopt 1 次）：

```text
6/6 函数 status=0b1010001 [isFunction+optimized+turbofanned]（两版本逐一相同）
JIT-AUDIT VERDICT: 6/6 ... unhealthy=0
--trace-deopt 全锚点: 进程级 8 条 deopt，src/learning 帧 0 条
```
