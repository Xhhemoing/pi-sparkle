MODEL_SLUG=claude-fable-5-thinking-xhigh

# R14-H：`src/evaluation/` + `src/requirement/` + `src/review/` + `src/rubric/` 第十四遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 14 / R14-H
**基线:** `cursor/sota-persistent-opt-83a1` @ `ba97de1`（独占 tip，含 R14-A/B/C/D/E 空枚举收口与本次派单；R14-F/G 在飞，未等待、未触碰）
**分支:** `cursor/r14-h-eval-fourteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十四遍枚举为空：无达门槛候选、零新 ID、切片零代码改动、切片维持关闭
（MORE_OPTIMA=no）。**
切片 21 个文件（1750 行 = 655+712+309+74，本轮 `wc -l` 逐目录复核一致；
8+7+4+2 清单逐目录确认恰 21 文件——派单文本中 `src/evaluation/` 标注
"(9)" 为计数笔误，其自身逐名列出的恰是 8 个文件且与目录实际一致，总数
21 与指令要求吻合）自 R1-H 基线（`fd437a9`）以来**逐字节未变**
（`git diff fd437a9..ba97de1 --
src/{evaluation,requirement,review,rubric}/` 为 0 行、同范围 `git log`
为 **0 提交**，十四遍全程零 diff——指令要求的空 diff 检查通过），且
R13-H 基线（`525a052`）之后 `src/` **零提交**（`git log
525a052..ba97de1 -- src/` 为空——其间全部 10 个提交均为 R13-F/G/H/I/J
摄取、Round 13 收口与 Round 14 全部派单/摄取文档；R14-A/B/C/D/E 全数
空枚举无代码落地）⇒ 生产调用方地图**可证不变**（最强形式），本轮
全库 import 交叉检索再次独立确认（9 个切片外导入位点与 R3-H…R13-H
完全一致：7 个值级 = `assertCoverageAllowsStart`×3 /
`extractHeuristicContract` / `applyPrecedence` / `shouldScout` /
`assertCanPromoteFromReview`，2 个纯类型 = promotion.ts ReviewerKind /
primary-split.ts HeuristicHabits；`src/evaluation/` 全部 8 文件、
`review/{pairwise,reconcile,critic}.ts`、`src/rubric/` 仍零生产调用方）。
按指令在本 VM 重锚满格矩阵（§1.3–§1.5，三次独立运行，Node v22.22.2）：
热层默认态 **9.27–9.38µs**/run（与派单点名的 R13-H 热默认锚
9.3–10.4µs 同带下沿、居十四轮历史跨 VM 带 3.9–10.4µs 上段；三次
9379/9271/9354ns 离散度 <1.2%）、配置态 A **7.5–9.1µs**（三次全低于
同轮默认态）、配置态 B **4.0–4.7µs**（三次全低于默认态）——**无配置
态悬崖**、默认锚点仍是保守口径；晋升类 17.7–18.8ns；四个零生产调用方
命令类（evaluate 1.10–1.13µs / critique 0.60–0.61µs / pairwise
2.05–2.09µs / reconcile 63–68ns）**全部 ≤2.1µs**；冷层首调默认
1699–1748µs、配置 1625–1679µs once-per-process（与 R6-H…R13-H 同带，
否决类归属不变）。第十四遍在完整排除表（S1-H-1..9 … S9-H-1..2 及
R10-H/R11-H/R12-H/R13-H 四轮空枚举收口共 60+ 处不立 ID 记录）之上以
一个从未用过的完备性轴收口：**引擎态基底透镜（engine-substrate
lens）**（§2.1，镜像 R14-B §2d/§2e 与 R14-E 的 JIT 档审计在本切片的
对应物）——前十三轮的十四个完备性面全部测的是源码层成本，从未直接
测过承载这段代码的两个 V8 子系统本身：(a) **GC/分配预算格**：热链
每 run 分配实测 **默认 11,458 / 配置 A 11,484–11,485 / 配置 B
9,888–9,941 字节**（三次运行中位数逐位复现，分配形态确定性）+ 门/
优先级腿 3,012 字节；30,000 rep 持续负载下 GC 暂停份额
**8.1–9.0%**、每 run 摊销 **265–278ns**（scavenge=75、major=1 三次
逐位相同）；流量交叉核（75 次 scavenge × 半空间 4MiB ≈ 10,486
字节/run）与直接探针 9% 内吻合 ⇒ **整个「分配消除」机制类获得联合
预算收口**：把切片热链的每一个分配（防御拷贝 S1-H-2/S6-H-3/S4-H-3/
R12-H P1、中间数组 R12-H P2、spread、每调用角色对 S6-H-4、问题/合同
字面量）全部消为零，可回收上界 ≈ GC 摊销 + bump-pointer 亚 µs 级，
低于数十 ms 落地线 **4+ 个数量级**——历史上逐个裁决的全部分配向
拒列就此获得一次性共同天花板；(b) **JIT 档审计格**：dist 新进程
--allow-natives-syntax 下生产规模预热后审计 27 个切片函数（含
extractor/critic 私有闭包与切片外只读 validateRequirementContract），
三次运行 **deopt 标记 0/27 ×3**——切片不存在任何隐形 deopt 循环成本
通道；瞬时层级 21–24/27 turbofanned，链内真热闭包
`extractor.extract` / `critic.critique` 三次全部稳定 turbofanned，
少数包装器/门函数在 baseline↔turbofanned 间随读取协议摆动（V8 代码
老化/分层时点伪影，无一停滞 interpreted、无一带 deopt 位——
读数波动本身佐证 R12-H「锚点是保守口径」）。结构位置 × 消除机制扫描
本轮识别出一处**从未点名**的位点并在候选化之前拒列（门 throw 路径
错误消息装配，§0——fail-closed 错误路径频次类 + 消息格式被单元与
集成测试双重断言 + 融合家族），其余全部微观位点逐一对表均为换名
重提或已关家族站点换位（§0 十六项审计）。诚实结论：枚举为空，不硬
凑 ID。未重开任何 X* / S1-* … S13-* 条目；S5-H-1 守卫经本轮 diff +
读码双复核**原样保留、逐字节未变**；S8-H-3 全决议快路径**未做任何
形态**；S8-H-1 PairwiseInput 未收窄。`pnpm gate` 全绿。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/evaluation/`（8 文件：evaluator / delivery-adapter /
  diff-adapter / ownership / precedence / check-adapter / adapters /
  types）、`src/requirement/`（7 文件：normalizer / provenance /
  precedence / extractor / heuristic / coverage / critic）、
  `src/review/`（4 文件：pairwise / self-review / reconcile / critic）、
  `src/rubric/`（2 文件：registry / types）计 21 文件全量第十四遍实际
  读码，未依赖前十三轮记忆。上下游 `domain/contract.ts`、
  `track/{clarify,loop,plan,primary-split}.ts`、
  `run/{supervisor,coordinator,flowchart-run}.ts`、
  `adaptation/{promotion,promotion-rules}.ts` 只读取证，一行未改。
  切片外其余树零触碰。在飞 R14-F / R14-G 分支与文件未触碰、未等待。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表 545 行，含
  S13-B-1 与 S5-H-1、S8-H-1 全条目）/ round-14/PLAN.md /
  round-13/PLAN.md + R13-H.md / R12-H.md + R11-H.md + R10-H.md（全部
  空枚举）/ 更早 H 报告按需（R10-H §2.1 导出面对账、R11-H §2.1 私有
  面对账、R12-H §2.1 剖面归账、R13-H §2.1 规模标定为本轮换名检查
  基底）。EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md 未编辑。
- **换名重提检查**（第十四遍读码发现的全部微观位点逐条对表；除一处
  从未点名者外全部为换名/站点换位，均在候选化之前拒列，零探针、零
  仿真、零触碰）：
  - **S5-H-1（必须保留项）**：`requirement/precedence.ts` L24 的
    `if (fast.length === 0 || slow.length === 0) return [];` 分配前
    守卫经本轮读码 + `git diff fd437a9..HEAD --
    src/requirement/precedence.ts`（0 行）双复核**原样在位、逐字节
    未变**。本轮对该函数零候选、零「优化掉」提案；§2.2 仿真含三项
    确定性语义 attestation（fast-only → [] / slow-only → [] /
    fast+slow → 单冲突全 id），三次运行全过。
  - **S8-H-3（永不重开项）**：`assertCoverageAllowsStart` 未做任何
    全决议快路径（含 resolved-superset 预判、gated 分支条件跳过、
    Q=0 免拷贝、blocking 预扫描短路全部变形）。`coverage.ts` 零
    diff。§2.1 引擎测量只跑生产函数原样执行，未引入任何变体。
  - **S8-H-1（永不重开项）**：`PairwiseInput` 全形状（含 comment
    双字段）未收窄；PIC 二态教训（S8-A-3 / S8-E-2 / S9-D-1 同族）
    未重演。
  - **本轮唯一从未点名位点（候选化之前拒列，不铸 ID）**：
    `coverage.ts` L92–98 门 throw 路径的错误消息装配（三个条件三元 +
    `filter` 类型收窄 + `join`）。拒列三重：(1) 频次类——仅在门
    **阻止** run 启动时执行（fail-closed 抛错路径，每次被阻启动至多
    一次，随后启动中止；生产成功流零次）；(2) 消息格式载荷——
    `test/unit/requirement/coverage-gate.test.ts` L67 与
    `test/integration/m3/coverage-gate.test.ts` L50 均以
    `/coverage gate blocked start/i` 断言该消息（X0-5 错误消息被
    测试断言同族）；(3) 机制归属——中间数组融合 = S1-C-5 / S5-J-6 /
    R12-H P2 已判家族。错误路径微优化无收益类，无需探针。
  - `heuristic.ts`：L48/L64 双正则子集短路 = S3-H-3 原条目；vague 路
    径死 `wantsReview` = R12-H P3；`objective.trim()` 与 isVague 内
    重复 trim = R6-H no-ID（S1-H-3 家族）；L85 namedTargets 与
    shouldAskScope 内二次调用 = S1-H-3；SMALLEST_CHANGE 每调用
    spread = R2-H/R8-H 拒列；vague 分支问题字面量提升 = S6-H-4 /
    S7-B-5 可观察身份家族；isVague split+filter 词计数 = R2-H/R5-H/
    R7-H/R8-H no-ID；q-scope/q-write 恒假 `questions.some` 守卫 =
    R4-H §2 注 no-ID；正则字面量提升 = S1-B-1 / S3-E-5 家族 +
    R1-H extractSignals no-ID。
  - `coverage.ts`（throw 路径外）：L22 orphans 防御 spread = R12-H
    P1；L27 `Object.keys(...).includes` 每准则 = S1-H-1 / S4-H-1
    辖区（Face C 二次方已由 R13-H 实测标定）；「消 includes 只留
    ?.length」= S4-H-1 fail-open 家族（"constructor" 原型链反例，
    R13-H 已钉）；L52 Set-from-map = R12-H P2；gated 合同拷贝 =
    S1-H-2 / S6-H-3。
  - `extractor.ts`：L68 roleId 双 trim = R10-H no-ID；L75
    `[...input.sources]` = R2-H no-ID；L83 sourceRefs Set = S2-H-1；
    L122 空 authority 下 sourcesByRef Map 死构造 = S2-H-2 本体；
    L80 生产链双 validate = S3-H-1；三扩展点异步机器 = S7-H-1 +
    X0-4。
  - `critic.ts` / `provenance.ts`：四遍装配 = S2-H-3 / S3-H-2；
    findUnsourcedItems L30 Set-from-map = R12-H P2 镜像站点；
    isSourced `Array.isArray` 数据面守卫 = R4-H no-ID。
  - `normalizer.ts`：excerpt slice(0,200) = R5-H no-ID + R10-H
    SlicedString 取证；extractSignals 三正则 = R1-H no-ID；signals
    生产组合死输出 = S7-H-2（对任意 extractor 是活契约）；默认
    origin 守卫 = S2-H-7；buildContractFromSources filter+map 融合 =
    R11-H L3（零生产调用方维持）。
  - `requirement/precedence.ts`（守卫外）：双 filter 序贯早退 =
    S6-H-1；`conflict.ids.includes` 与双 filter Set 化 = S1-H-5；
    `[...fast, ...slow]` 中间拼接 = 冲突侧零生产流量 + S1-H-5 家族
    （生产合同无 contradictory-latency，S5-H-1 早退使冲突侧不可达）。
  - `evaluation/evaluator.ts`：`input.evidence[criterion.id]` 双读 =
    R5-H no-ID；聚合三遍 = S1-H-7；INDEPENDENCE_CLASSES includes =
    R1-H 表族（表长 3）；`String()` 分配 = R10-H no-ID。
  - `evaluation/check-adapter.ts`：changeSetsEqual 三面 = S1-H-9 /
    S2-H-4 / S3-H-4；hashArtifact 拼接 = S5-H-3；attributionMetadata
    活性（全部非 ABSTAIN 路径消费）本轮读码再确认。
  - `evaluation/delivery-adapter.ts` / `diff-adapter.ts`：输入校验
    合取序 = R10-H no-ID；FAIL 路径死 `hasEpisodeOwned` = R11-H L1。
  - `evaluation/ownership.ts`：`context.changeSet.includes` 线性扫 =
    X4-9；控制流去糖 = R10-H no-ID；DEFAULT_RULES 已模块级。
  - `evaluation/precedence.ts`：reduce 内 comparePrecedence 双调用 =
    R2-H no-ID + S2-H-5；三元素表 find = R1-H 表族。
  - `review/pairwise.ts`：remap "ab" 分支冗余展开 = R1-H no-ID；
    presentInput "ba" 对象构造 = 命令类天花板辖区（≤2.1µs）。
  - `review/reconcile.ts`：三遍 filter = S1-H-6（n=2 亚噪声）；L39
    Set-from-map = R12-H P2 家族第四站点（R13-H 已按站点换位拒列，
    本轮维持）。
  - `review/critic.ts`：hasPass 融合 = R2-H no-ID；score 条件键单态
    化 = R9-H 拒列。`review/self-review.ts`：S9-H-1 / S9-H-2 维持。
  - `rubric/registry.ts`：copy-on-write = S1-H-8 反例 + S2-H-6；
    `{ ...DEFAULT_REGISTRY }` 浅拷贝别名安全性第六次读码复核成立
    （全部写路径整体替换 registry）。`rubric/types.ts`：createRubric
    时间戳 = R10-H 取证维持。
  - **S13-B-1 / S12-* / S11-*（其他切片）**：均不在本切片，未触碰、
    未换名重提（本切片无 learned 层、无批内重复可收拢——每 run 恰
    一次调用链）。
  - R10-H / R11-H / R12-H / R13-H 未铸 S10-H-* / S11-H-* / S12-H-* /
    S13-H-*，本轮亦未事后发明任何此类编号，不为空枚举轮补铸 ID。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价（R1 未接线）、
  双 LCB（beta+normal）与双归因（logit+prob-add）保留（X0-11）、
  晋升 proposal-first、阈值/CAS/凭据/数据面契约/公开签名不变、
  DEFAULT_REGISTRY 未就地变异、fail-open 零引入、测试未改，天然
  成立。不声称 Outcome-supported，Checkpoint F-PROD 仍开放
  （ADR-005 / X0-1）。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，且本 VM
  `/exec-daemon/node`（v22.14.0）在 PATH 前段遮蔽 nvm bin（与
  R11-H…R13-H §0 注记同型）；测量与测试前已显式把 nvm v22.22.2 bin
  置于 PATH 首并复核 `node --version` = v22.22.2。
  `pnpm install --frozen-lockfile`（pnpm 10.17.1）后 `pnpm build` 绿。
- 未开 PR；未 force-push；未推送专属分支；仅提交并推送工作分支
  `cursor/r14-h-eval-fourteenth-pass-83a1`。无赢家 ⇒ 未提交任何
  仿真脚本（按指令），锚点/引擎测量脚本全文在附录。

## 1. 基线不变性、调用图复核与满格矩阵重锚

1. **切片逐字节未变**：`git diff fd437a9..ba97de1 --
   src/{evaluation,requirement,review,rubric}/` 输出为 0 行（`--stat`
   同空），同范围 `git log` 为 0 提交（十四遍全程零 diff——指令要求
   的空 diff 检查通过）。21 文件 1750 行逐目录计数（655/712/309/74）
   与 R1-H 清单一致。R1-H 逐函数下界表 … R12-H 剖面归账、R13-H
   规模-越线标定与全部 S*-H-* 排除继承有效。
2. **调用图可证不变（最强形式）**：`git log 525a052..ba97de1 -- src/`
   **为空**——R13-H 基线以来全部 10 个提交均为 `docs/reports/` 文档
   （R13-F/G/H/I/J 摄取、Round 13 收口、Round 14 全部派单/摄取；
   R14-A/B/C/D/E 空枚举无代码落地）⇒ 生产调用方地图数学上不变。
   本轮全库 import 交叉检索独立双确认：切片外 `src/` 引用切片的文件
   恰 9 个，与 R3-H…R13-H 完全一致——值级 7 处：
   `assertCoverageAllowsStart` ← `run/supervisor.ts` /
   `run/coordinator.ts` / `run/flowchart-run.ts`（每 run 启动一次）；
   `extractHeuristicContract` ← `track/clarify.ts`（每 run 一次）；
   `applyPrecedence` ← `track/loop.ts`（每 run 一次，`"user-first"`）；
   `shouldScout` ← `track/plan.ts`（每 run 一次）；
   `assertCanPromoteFromReview` ← `adaptation/promotion-rules.ts`
   （每晋升一次）；纯类型 2 处（`adaptation/promotion.ts`
   ReviewerKind、`track/primary-split.ts` HeuristicHabits）。
   `src/evaluation/` 全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、
   `src/rubric/` 及 `applyRoutingScoreUpdate`、
   `buildContractFromSources` 的切片外 `src/` 值级引用**为零**——
   未把假想接线当今日流量。
3. **热层三态锚点重锚**（三次独立运行，本 VM，Node 22.22.2；测量
   条件与 R10-H…R13-H 严格同构——benchAsync 单次预热 + 3000 计时，
   引擎测量块放在锚点之后以免额外预热污染可比性）：

```text
CEILING default  (9-pass anchor shape, Q=0):        9379 / 9271 / 9354 ns/run
CEILING configured A (habits 三键全设, Q=1 gated):   8420 / 7453 / 9060 ns/run
CEILING configured B (vague objective, Q=3 gated):   4026 / 4715 / 3991 ns/run
CEILING promotion class: assertCanPromoteFromReview = 17.7 / 17.9 / 18.8 ns/promotion
```

   默认态 9.27–9.38µs 与派单点名的 R13-H 热默认锚 **9.3–10.4µs**
   同带下沿（三次离散度 <1.2%，比 R13-H 本带更窄），居十四轮历史跨
   VM 带（3.9–10.4µs）上段。两档配置态三次**全部低于**同轮默认态 ⇒
   R8-H…R13-H「无配置态悬崖、默认锚点是保守上界」在本 VM 复现。
   量级类结论不变：整个热面 µs 级，低于落地线 **3–4 个数量级**。
   gated 重映射真实执行（production assume-defaults 流恒传覆盖全部
   问题的 `resolvedQuestionIds`）。
4. **命令类矩阵剩余格重锚**（R9-H 首锚的四个零生产调用方类，本轮
   三次复测）：

```text
CEILING evaluate class  (TEST-ONLY): createEvaluationRecord(3 criteria) = 1131 / 1103 / 1127 ns/call
CEILING critique class  (chain 内):  critiqueContract(real contract)    =  608 /  598 /  604 ns/call
CEILING pairwise class  (TEST-ONLY): runBlindPairwisePair               = 2054 / 2067 / 2093 ns/call
CEILING reconcile class (TEST-ONLY): reconcileReviews(n=2)              =   68 /   67 /   63 ns/call
```

   与 R9-H…R13-H 锚点带一致（critique 三次全落 ~600ns 峰 = R11-H/
   R12-H/R13-H 双峰之高峰，JIT 分层状态，量级类属不变）。判据成立：
   **调用图未变下即便把这四个面全部接线，当前规模每类 ≤2.1µs，距
   落地线仍差 4+ 个数量级**。矩阵在本 VM 无未测格。
5. **冷层锚点复测（默认 + 配置双态）**：dist 产物、新进程、模块加载
   预扣除后，三生产入口首调合计中位数三次：默认态
   **1699 / 1715 / 1748 µs**（区间 [1662, 1877]µs）、配置态
   **1625 / 1634 / 1679 µs**（区间 [1608, 1719]µs）——与 R6-H
   1.65–1.71ms … R13-H 1.6–1.8ms 同带；once-per-process 否决类归属
   不变（S7-H-3 已证该层无净正对策）。

## 2. 第十四遍枚举：引擎态基底透镜（新轴）→ 诚实空结果

### 2.1 GC/分配预算格 + JIT 档审计（engine-substrate lens，本轮新轴）

前十三轮的十四个完备性面（单函数 / 函数间 / 跨模块 / 调用图 /
模块图 / 执行层级 / 调用机器 / 配置态 / 命令类 / 编译器地板 /
导出面 / 私有面 / 成本剖面归账 / 规模-越线标定）全部测的是**源码层
成本**；同一段字节码的真实成本由两个从未直接测过的 V8 子系统共同
决定——内存子系统（分配 + GC）与分层编译器（tiering + deopt）。
第十四遍换到**代码之下**（镜像 R14-B §2d/§2e 与 R14-E 在各自切片的
引擎审计）：若任一子系统存在病态（分配风暴、deopt 循环），它就是
十三轮微基准平均掉的隐形成本通道；若两者健康，其实测预算反过来给
一整类候选一次性收口。三次独立运行代表值：

```text
ALLOC default chain:        11458 / 11458 / 11458 bytes/run（25 窗 × 64 run 中位数，三次逐位复现）
ALLOC configured A chain:   11485 / 11484 / 11484 bytes/run
ALLOC configured B (vague): 9921  / 9941  / 9888  bytes/run
ALLOC default gate+precedence legs: 3012 / 3012 / 3012 bytes/run
GC sustained 30000 reps: pauses = 8.0/8.3/7.9 ms（份额 8.8/9.0/8.1%）、minor=75、major=1（三次逐位相同）、摊销 265/278/265 ns/run
GC flow cross-check: 75 scavenges × 4096KiB 半空间 / 30000 reps ≈ 10486 bytes/run（与直接探针 9% 内吻合）
JIT SUMMARY: 0/27 functions carry deopt marks（三次运行全部 0/27）
```

四个收口事实：

1. **分配形态确定性**：三态热链每 run 分配中位数三次运行**逐位
   相同**（默认恰 11,458 字节），scavenge/major 计数三次逐位相同
   （75/1）——切片无分配抖动源、无隐藏分配风暴；~11.5KB/run 与
   R12-H 剖面归账的组件构成（extractor 字面量构造 + 胶水 + 校验
   拷贝）完全对得上账。
2. **「分配消除」机制类联合预算收口**：GC 摊销 265–278ns/run + 直接
   bump-pointer 成本（11.5KB @ ~纳秒级/KB）合计**亚 µs 级**。即把
   切片热链的每一个分配全部消为零——防御拷贝（S1-H-2 / S6-H-3 /
   S4-H-3 / R12-H P1）、中间数组（R12-H P2 三站点 + reconcile 第四
   站点）、spread（S8-H-2 / S2-H-6）、每调用角色对（S6-H-4）、问题/
   合同字面量（R2-H/R8-H）——可回收上界也比数十 ms 落地线低 **4+
   个数量级**。历史上逐个裁决的全部分配向拒列就此获得一次性共同
   天花板；未来任何分配向候选须先推翻本格数字。
3. **无隐形 deopt 通道**：27 个审计对象（含链内私有闭包
   `extractor.extract` / `critic.critique`、切片外只读
   `validateRequirementContract`）三次运行 **deopt 标记 0/27**——
   `kMaybeDeopted` / `kMarkedForDeoptimization` 全零。链内真热闭包
   三次全部稳定 turbofanned（per-call 实例经共享 FeedbackCell 分层
   有效，与 R14-B route 闭包结论同型）；瞬时层级 21–24/27
   turbofanned，少数包装器/门函数（extractHeuristicContract 包装、
   checkCoverageGate / assertCoverageAllowsStart / applyPrecedence /
   runBlindPairwisePair / buildContractCandidate 等）在
   baseline↔turbofanned↔tiering-in-progress 间随读取时点摆动——V8
   代码老化与并发分层的时点伪影（无一停滞 interpreted、无一带
   deopt 位）。「修 deopt」候选类在本切片**收益池为零**，在候选化
   之前关闭（不铸 ID，探针关闭类）。
4. **锚点保守性的引擎侧佐证**：瞬时层级摆动意味着 3000-rep 锚点
   口径混有 baseline 执行片段——真实长驻进程的每 run 成本只会更低
   （R12-H 完全分层稳态 3.3–3.6µs 的引擎侧解释），距落地线的量级差
   只会更大。

引擎轴由此闭合：**代码之下没有隐形成本通道**；本切片「结构上不存在
达门槛候选」的十三轮结论现在有了 V8 子系统层的实测支撑。

### 2.2 结构位置 × 消除机制扫描：一处从未点名位点 → 候选化之前拒列

第十四遍全格对账（结构位置：入口守卫 / 循环体 / 返回构造 / 跨调用
重复 / 数据字面量 / 正则求值 / 防御校验 / 拷贝语义 / 异步机器 /
模块加载 / 错误路径 × 消除机制：死码 / CSE / 融合 / 快路径 / 记忆化 /
表示变换 / 去糖 / 惰性 / 可变化 / 复杂度类）。与 R10-H（6 处）、
R11-H（3 处）、R12-H（3 处）、R13-H（0 处）的「从未点名位点」收敛
序列一致：**本轮 1 处**——`coverage.ts` L92–98 门 throw 路径错误
消息装配，在候选化之前以频次类（fail-closed 抛错路径，成功流零次）+
消息格式测试断言（单元 + 集成双重 `/coverage gate blocked start/i`，
X0-5 同族）+ 已判融合家族三重拒列（§0），零探针。其余全部微观位点
均对到既有 ID / no-ID 收口（§0 十六项审计）。1750 行 × 14 遍后，
切片可优化面（导出 + 私有 + 剖面 + 规模 + 引擎）五向闭合。**不立
任何 S14-H-* ID**（与 R10-H…R13-H 及 R14-A/B/C/D/E 空枚举收口同型）。

### 2.3 收口陈述

十四遍合计：单函数、函数间、跨模块、调用图、模块图、执行层级、调用
机器、配置态、命令类、编译器地板、导出面完备性、私有面完备性、成本
剖面归账、规模-越线标定、**引擎态基底（GC/分配预算 + JIT 档）**十五
个面全部有实测锚点或对账记录。本切片在当前调用图下**结构上不存在**
达门槛候选。总门槛按本 VM 数字刷新：任何未来候选须先推翻满格矩阵
实测上界——热层默认 **9.27–9.38µs/run**（3000-rep 保守口径）、分配
**11.5KB/run（GC 摊销 ≤278ns/run）**、四个零调用方命令类 ≤2.1µs、
冷层 **1.6–1.8ms once-per-process**（否决类）、deopt 通道 0/27。
重开该切片的唯一前提维持 R4-H…R13-H 收口原文：调用图变更
（evaluation/review/rubric 面接入每 turn 热路径，或合同规模增长
≥2 个量级——R13-H 已实测标定该阈值先于 10ms 落地地板触发）。

## 3. 逐文件收口（第十四遍新视角，其余与 R1-H…R13-H 一致）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `requirement/coverage.ts` | **throw 路径消息装配首次点名并拒列（§0/§2.2，频次类 + 测试断言格式 + 融合家族）**；ALLOC 门腿 3,012B/run 归账；JIT 无 deopt 位；S8-H-3 零形态、S1-H-1/2、S4-H-1、S6-H-2/3、R12-H P1/P2 维持 | 无新候选 |
| `requirement/heuristic.ts` | ALLOC 链主体（三态 9.9–11.5KB/run 逐位复现）；`extractor.extract` 闭包三次稳定 turbofanned；isVague/namedTargets/shouldScout 全 turbofanned 零 deopt；S1-H-3、S3-H-3、S6-H-4、S7-H-1、S8-H-2、R12-H P3 维持 | 无新候选 |
| `requirement/precedence.ts` | **S5-H-1 守卫在位复核（L24 早退，零 diff + 三项语义 attestation ×3 运行）**；detectConflicts turbofanned 零 deopt；`[...fast, ...slow]` 中间拼接识别为冲突侧零流量 + S1-H-5 家族（§0）；S1-H-5、S4-H-2、S6-H-1 维持 | 无新候选 |
| `requirement/extractor.ts` | buildContractCandidate 零 deopt 位（瞬时层级摆动 = 代码老化伪影）；S2-H-1/2、S3-H-1、S6-H-4、S7-H-1、R10-H roleId trim 维持 | 无新候选 |
| `requirement/critic.ts` / `provenance.ts` | critiqueContract / findUnsourcedItems 零 deopt；四遍装配 = S2-H-3 / S3-H-2 维持；findUnsourcedItems L30 = R12-H P2 站点维持 | 无新候选 |
| `requirement/normalizer.ts` | normalizeSources / createTrustedSource / buildContractFromSources 全 turbofanned 零 deopt；S2-H-7、S7-H-2、R5-H slice、R10-H SlicedString、R11-H L3 维持 | 无新候选 |
| `evaluation/evaluator.ts` | createEvaluationRecord turbofanned 零 deopt；evaluate 类锚点复测同带；S1-H-7、R5-H evidence 双读、R1-H 表族、R10-H String() 维持 | 无新候选 |
| `evaluation/check-adapter.ts` / `delivery-adapter.ts` / `diff-adapter.ts` | changeSetsEqual 三面、S5-H-3、attributionMetadata 活性、R10-H 合取序、R11-H L1 维持 | 无新候选 |
| `evaluation/ownership.ts` / `precedence.ts` / `adapters.ts` / `types.ts` | classifyDiffScope / selectHighestPrecedence turbofanned 零 deopt；X4-9、R2-H/S2-H-5、纯类型面维持 | 无新候选 |
| `review/pairwise.ts` / `reconcile.ts` / `critic.ts` / `self-review.ts` | runBlindPairwisePair / reconcileReviews / createCriticObservation / 晋升双函数零 deopt；reconcile L39 = R12-H P2 第四站点维持（R13-H 裁决沿用）；S8-H-1、S1-H-6、S9-H-1/2 维持；pairwise/reconcile 锚点复测同带 | 无新候选 |
| `rubric/registry.ts` / `types.ts` | S1-H-8 反例 + S2-H-6 + R10-H createRubric 时间戳取证维持；DEFAULT_REGISTRY 别名安全第六次复核成立 | 无新候选 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。无仿真脚本入库（无赢家，遵循
round01–13 仅赢家 sim 入库惯例 + 本轮指令明文；完整脚本见附录）。

## 5. 测试与门

`pnpm gate` 全绿（Node 22.22.2 via nvm，满足 engines >=22.19.0；
系统 Node 22.14.0 过低 + `/exec-daemon/node` PATH 遮蔽的环境注记见
§0；`pnpm install --frozen-lockfile` 后）：

```bash
pnpm gate   # typecheck ✓ + lint ✓ + test: tests 1169 / pass 1168 / fail 0 / skipped 1 ✓ + build ✓
```

切片相关套件基线复核，全绿：

```bash
npx tsx --test "test/unit/requirement/*.test.ts" "test/unit/evaluation/*.test.ts" \
  "test/unit/review/*.test.ts" "test/integration/m3/checkpoint-d.test.ts" \
  "test/integration/m3/coverage-gate.test.ts" \
  "test/integration/m3/requirement-extraction.test.ts" \
  "test/integration/m4/delivery-evidence.test.ts"
# tests 93 / suites 13 / pass 93 / fail 0（与 R1-H…R13-H 同套件同计数）
```

锚点 + 引擎测量（临时脚本 `/tmp/r14h-sim.mts` + JIT 子进程
`/tmp/r14h-jit-audit.mjs`，无赢家故未入库；完整源码见附录；主脚本以
`NODE_OPTIONS="--expose-gc" npx tsx` 运行，JIT 子进程以 dist 产物 +
`--allow-natives-syntax` 运行）三次独立运行，28 项确定性 fail-closed/
sanity 检查全部通过、裁决结论逐位一致（默认锚点三次 9379/9271/9354ns、
配置 A 8420/7453/9060ns、配置 B 4026/4715/3991ns；晋升类
17.7/17.9/18.8ns；evaluate 1131/1103/1127ns、critique 608/598/604ns、
pairwise 2054/2067/2093ns、reconcile 68/67/63ns；ALLOC 默认三次逐位
11458B/run、GC 摊销 265/278/265ns/run、minor=75 major=1 三次逐位；
JIT deopt 0/27 ×3；冷层默认 1699/1715/1748µs、配置 1625/1634/1679µs）。

## 6. 新增排除 ID

**无。** 第十四遍枚举为空——引擎态基底透镜证明代码之下无隐形成本
通道且分配向候选类被联合预算收口（§2.1），结构位置 × 消除机制扫描的
唯一从未点名位点（throw 路径消息装配）在候选化之前三重拒列（§0/
§2.2）。不硬凑 ID，不为站点换位铸 ID。与 R10-H…R13-H 及
R14-A/B/C/D/E 的空枚举收口同型。

## 附录 A：确定性锚点 + 引擎态基底测量脚本（完整，可复现）

运行方式：`pnpm build` 后保存为任意 `.mts` 并
`NODE_OPTIONS="--expose-gc" npx tsx <file>`（仓库根目录，依赖已装；
冷层/JIT 探针需要 dist 产物存在；JIT 审计体见附录 B，保存为
`/tmp/r14h-jit-audit.mjs`）。无赢家 ⇒ 无随机 fuzz（无候选可裁决），
28 项 sanity/fail-closed 检查全为固定输入确定性断言，三次运行为精确
重执行。按惯例不入库。

```ts
/**
 * R14-H deterministic anchor re-verification + engine-substrate lens
 * (fourteenth pass): GC/allocation budget grid + JIT tier audit.
 *
 * No fresh candidate survived enumeration (every micro-site found by the
 * fourteenth read maps to an exclusion-table ID or a prior no-ID closure —
 * see report §0/§2), so this script (a) re-establishes, on THIS VM, the full
 * measurement matrix that R8-H..R13-H closed — hot tier three states
 * (default Q=0 / configured A habits+Q=1 gated / configured B vague+Q=3
 * gated; the production assume-defaults track flow always passes
 * resolvedQuestionIds covering ALL questions so the gated re-map materially
 * executes), promotion class, the four zero-production-caller command
 * classes (evaluate / critique / pairwise / reconcile), and cold-tier
 * once-per-process first-call totals on dist — and (b) runs the fourteenth
 * pass's NEW completeness axis: the ENGINE-SUBSTRATE LENS (mirroring
 * R14-B §2d/§2e and R14-E's JIT audit in their slices). Thirteen passes
 * measured the slice's source-level cost; none ever directly measured the
 * two V8 subsystems that carry it:
 *
 *   1. GC/allocation budget grid: per-run allocation bytes of the hot
 *      production chain (three states), GC pause share under sustained
 *      chain execution, and the resulting HARD CEILING for the entire
 *      "allocation-elimination" mechanism class (defensive copies S1-H-2/
 *      S6-H-3/S4-H-3/R12-H P1, intermediate arrays R12-H P2, spreads,
 *      per-call role pairs S6-H-4, question/contract literals — all
 *      previously rejected individually; this jointly budget-caps them).
 *      Direct probe (--expose-gc heapUsed deltas, median-of-samples) is
 *      cross-checked against a GC-flow estimate (scavenge count ×
 *      semi-space capacity).
 *   2. JIT tier audit (child process, --allow-natives-syntax, dist):
 *      production-scale warm-up then %GetOptimizationStatus for 27 slice
 *      functions — closes the "hidden deopt loop" channel never audited
 *      for H.
 *
 * Reference = production imports everywhere; no candidate is proposed, so
 * there is no randomized fuzz — every sanity/fail-closed check below is
 * fully deterministic (fixed inputs), and three runs are exact
 * re-executions (timing varies, adjudication class must not).
 *
 * Run: pnpm build, then NODE_OPTIONS="--expose-gc" npx tsx /tmp/r14h-sim.mts
 * (JIT child needs dist + /tmp/r14h-jit-audit.mjs).
 */
import { performance, PerformanceObserver, constants as perfConstants } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import { existsSync } from "node:fs";
import v8 from "node:v8";
import {
  extractHeuristicContract,
  type HeuristicHabits
} from "/workspace/src/requirement/heuristic.js";
import {
  assertCoverageAllowsStart,
  checkCoverageGate,
  coverageMatrixFromTasks,
  type CoverageTaskRef,
  type CoverageStartOptions
} from "/workspace/src/requirement/coverage.js";
import { applyPrecedence, detectConflicts } from "/workspace/src/requirement/precedence.js";
import { critiqueContract } from "/workspace/src/requirement/critic.js";
import { validateRequirementContract, type RequirementContract } from "/workspace/src/domain/contract.js";
import { assertCanPromoteFromReview } from "/workspace/src/review/self-review.js";
import { runBlindPairwisePair, type PairwiseInput } from "/workspace/src/review/pairwise.js";
import { reconcileReviews } from "/workspace/src/review/reconcile.js";
import { createEvaluationRecord } from "/workspace/src/evaluation/evaluator.js";
import { createRubric } from "/workspace/src/rubric/types.js";
import type { EpisodeId, TaskId } from "/workspace/src/domain/ids.js";

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean, detail?: string): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function median(xs: number[]): number {
  const sorted = [...xs].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)] ?? Number.NaN;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

/* ================================================================
 * Hot tier, three states (R8-H..R13-H anchor shapes). Measured FIRST so
 * anchor conditions stay comparable (benchAsync single warm call + 3000
 * timed reps, no extra chain executions before).
 * ================================================================ */
async function measureChain(label: string, objective: string, habits: HeuristicHabits | undefined): Promise<void> {
  const args = { objective, ...(habits !== undefined ? { habits } : {}) };
  const chain = await benchAsync(async () => {
    await extractHeuristicContract(args);
  }, 3000);
  const prod = await extractHeuristicContract(args);
  const real = prod.contract;
  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
  const options: CoverageStartOptions | undefined =
    real.questions.length > 0
      ? { resolvedQuestionIds: real.questions.map((question) => question.id) }
      : undefined;
  const gate = bench(() => assertCoverageAllowsStart(real, tasks, options), 50000);
  const precedence = bench(() => void applyPrecedence(real, "user-first"), 100000);
  const total = chain + gate + precedence;
  log(
    `CEILING ${label}: extractHeuristicContract=${(chain * 1e6).toFixed(0)}ns + run-start gate(Q=${real.questions.length}${options !== undefined ? ", all resolved" : ", no options"})=${(gate * 1e6).toFixed(0)}ns + applyPrecedence=${(precedence * 1e6).toFixed(0)}ns = ${(total * 1e6).toFixed(0)}ns once-per-run production total`
  );
}
{
  await measureChain(
    "default (9-pass anchor shape, Q=0)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    undefined
  );
  await measureChain(
    "configured A (habits set, Q=1 gated)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    { requireTests: true, preferReview: false, askBeforeWrite: true }
  );
  await measureChain("configured B (vague, Q=3 gated)", "fix bug please", { askBeforeWrite: true });
}

/* ================================================================
 * Command-class anchors beyond run-start.
 * ================================================================ */
{
  const promo = bench(
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    200000
  );
  log(`CEILING promotion class: assertCanPromoteFromReview=${(promo * 1e6).toFixed(1)}ns once per promotion (production)`);

  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const evaluate = bench(() => void createEvaluationRecord(evalInput), 50000);
  log(`CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=${(evaluate * 1e6).toFixed(0)}ns/call`);

  const prod = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const critique = bench(() => void critiqueContract(prod.contract), 50000);
  log(`CEILING critique class (inside run-start chain): critiqueContract(real contract)=${(critique * 1e6).toFixed(0)}ns/call`);

  const pairInput: PairwiseInput = {
    episodeId: "ep_00000001" as EpisodeId,
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pairwise = bench(() => void runBlindPairwisePair(pairInput, () => 0.7), 20000);
  const pair = runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  const reconcile = bench(() => void reconcileReviews(two), 100000);
  log(`CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=${(pairwise * 1e6).toFixed(0)}ns/call`);
  log(`CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=${(reconcile * 1e6).toFixed(0)}ns/call`);
  // Deterministic behavior sanity for the two TEST-ONLY faces measured above.
  check("pairwise identity-stable scores agree across swap", pair.reconciliation.consensus === "a");
  const biased = runBlindPairwisePair(pairInput, () => 0.7, { bindScoresToSlots: true });
  check("pairwise slot-bound scores reconcile to uncertain", biased.reconciliation.consensus === "uncertain");
  const record = createEvaluationRecord(evalInput);
  check("evaluate deterministic FAIL on missing evidence", record.overall === "FAIL");
  check(
    "evaluate per-criterion outcomes",
    record.scores.map((s) => s.outcome).join(",") === "PASS,PASS,FAIL"
  );
}

/* ================================================================
 * NEW AXIS part 1: GC/allocation budget grid (engine-substrate lens).
 * Direct probe: median heapUsed delta across gc()-isolated sample windows.
 * Cross-check: scavenge count × semi-space capacity over a sustained loop.
 * Ceiling: amortized GC pause per run + direct allocation share — the joint
 * budget cap for the ENTIRE allocation-elimination candidate class.
 * ================================================================ */
{
  const hasGc = typeof globalThis.gc === "function";
  check("alloc probe prerequisite: --expose-gc active", hasGc);
  if (hasGc) {
    const gcNow = globalThis.gc as () => void;
    const chains: [string, () => Promise<unknown>][] = [
      [
        "default",
        () => extractHeuristicContract({ objective: "fix the login retry bug in src/auth/session.ts and keep tests green" })
      ],
      [
        "configured A",
        () =>
          extractHeuristicContract({
            objective: "fix the login retry bug in src/auth/session.ts and keep tests green",
            habits: { requireTests: true, preferReview: false, askBeforeWrite: true }
          })
      ],
      ["configured B (vague)", () => extractHeuristicContract({ objective: "fix bug please", habits: { askBeforeWrite: true } })]
    ];
    // Include the gate + precedence legs on the default state to cover the
    // whole once-per-run production surface.
    const prod = await extractHeuristicContract({
      objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
    });
    const real = prod.contract;
    const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
      id: `tsk_0000000${i}` as TaskId,
      acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
    }));

    for (const [label, run] of chains) {
      // Warm before measuring so allocation reflects steady state, not lazy init.
      for (let i = 0; i < 200; i += 1) await run();
      const samples: number[] = [];
      for (let s = 0; s < 25; s += 1) {
        gcNow();
        const before = process.memoryUsage().heapUsed;
        for (let i = 0; i < 64; i += 1) await run();
        const after = process.memoryUsage().heapUsed;
        samples.push((after - before) / 64);
      }
      const perRun = median(samples);
      log(`ALLOC ${label} chain: ~${perRun.toFixed(0)} bytes/run (median of 25 gc-isolated windows × 64 runs)`);
      check(`alloc probe produced a sane positive number (${label})`, Number.isFinite(perRun) && perRun > 0 && perRun < 10e6);
    }
    {
      for (let i = 0; i < 200; i += 1) {
        assertCoverageAllowsStart(real, tasks);
        applyPrecedence(real, "user-first");
      }
      const samples: number[] = [];
      for (let s = 0; s < 25; s += 1) {
        gcNow();
        const before = process.memoryUsage().heapUsed;
        for (let i = 0; i < 256; i += 1) {
          assertCoverageAllowsStart(real, tasks);
          applyPrecedence(real, "user-first");
        }
        const after = process.memoryUsage().heapUsed;
        samples.push((after - before) / 256);
      }
      log(`ALLOC default gate+precedence legs: ~${median(samples).toFixed(0)} bytes/run (median of 25 gc-isolated windows × 256 runs)`);
    }

    // GC pause share + flow cross-check under sustained default-chain load.
    const gcRecords: { kind: number; duration: number }[] = [];
    const obs = new PerformanceObserver((list) => {
      for (const entry of list.getEntries()) {
        const detail = (entry as unknown as { detail?: { kind?: number } }).detail;
        gcRecords.push({ kind: detail?.kind ?? 0, duration: entry.duration });
      }
    });
    obs.observe({ entryTypes: ["gc"] });
    gcNow();
    gcRecords.length = 0;
    const reps = 30000;
    const t0 = performance.now();
    for (let i = 0; i < reps; i += 1) {
      await extractHeuristicContract({ objective: "fix the login retry bug in src/auth/session.ts and keep tests green" });
    }
    const wallMs = performance.now() - t0;
    await new Promise((resolve) => setTimeout(resolve, 50)); // flush observer
    obs.disconnect();
    const minor = gcRecords.filter((r) => r.kind === perfConstants.NODE_PERFORMANCE_GC_MINOR);
    const major = gcRecords.filter((r) => r.kind === perfConstants.NODE_PERFORMANCE_GC_MAJOR);
    const totalGcMs = gcRecords.reduce((acc, r) => acc + r.duration, 0);
    const newSpace = v8.getHeapSpaceStatistics().find((s) => s.space_name === "new_space");
    const semiCapacity = (newSpace?.space_size ?? 0) / 2;
    const flowEstimate = minor.length > 0 && semiCapacity > 0 ? (minor.length * semiCapacity) / reps : Number.NaN;
    const gcPerRunNs = (totalGcMs / reps) * 1e6;
    log(
      `GC default chain sustained ${reps} reps: wall=${wallMs.toFixed(0)}ms, gc pauses=${totalGcMs.toFixed(1)}ms (${((totalGcMs / wallMs) * 100).toFixed(1)}% share), minor=${minor.length}, major=${major.length}, amortized gc=${gcPerRunNs.toFixed(0)}ns/run`
    );
    log(
      `GC flow cross-check: ${minor.length} scavenges × semi-space ${(semiCapacity / 1024).toFixed(0)}KiB / ${reps} reps ≈ ${Number.isFinite(flowEstimate) ? flowEstimate.toFixed(0) : "n/a"} bytes/run (order-of-magnitude check vs direct probe)`
    );
    log(
      `ALLOC-CLASS CEILING: zeroing EVERY allocation in the slice hot chain can recover at most ~${gcPerRunNs.toFixed(0)}ns/run of GC amortization plus bump-pointer time — the entire allocation-elimination mechanism class (defensive copies, intermediate arrays, spreads, per-call role pairs, literals) is jointly capped 4+ orders below the tens-of-ms landing line`
    );
    check("gc observer captured a plausible pause share (<30%)", totalGcMs / wallMs < 0.3);
  }
}

/* ================================================================
 * Deterministic fail-closed / behavior sanity (fixed inputs; mirrors the
 * R13-H check families so the fourteenth pass re-attests every guard).
 * ================================================================ */
{
  const prod = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const real = prod.contract;
  check("default contract has C=2 criteria (ac-objective + ac-tests)", real.acceptanceCriteria.length === 2);
  check("default contract Q=0", real.questions.length === 0);
  check("default confidence 0.86", prod.confidence === 0.86);
  const vague = await extractHeuristicContract({ objective: "fix bug please" });
  check("vague confidence 0.55", vague.confidence === 0.55);
  check("vague emits questions", vague.contract.questions.length > 0);

  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
  let threw = false;
  try {
    assertCoverageAllowsStart(real, tasks);
  } catch {
    threw = true;
  }
  check("gate passes when fully covered", !threw);
  const uncovered = tasks.map((task) => ({ ...task, acceptanceCriteria: task.acceptanceCriteria.slice(0, 1) }));
  threw = false;
  try {
    assertCoverageAllowsStart(real, uncovered);
  } catch {
    threw = true;
  }
  check("gate fail-closed when one criterion uncovered", threw);
  const matrix = coverageMatrixFromTasks(real, tasks);
  const gateResult = checkCoverageGate(real, matrix);
  check("raw gate result clean", gateResult.ok && gateResult.uncoveredCriteria.length === 0);

  // S5-H-1 semantics attestation: pre-allocation guard early-returns [] when
  // either side is empty; conflict side still detected.
  const mk = (checksArr: string[]): RequirementContract =>
    validateRequirementContract({
      schemaVersion: 1,
      objective: "conflict guard attestation objective",
      deliverables: [{ id: "d-1", description: "d", artifactKind: "diff", sourceRefs: [{ kind: "message", ref: "s" }] }],
      constraints: [],
      nonGoals: [],
      acceptanceCriteria: checksArr.map((c, i) => ({
        id: `ac-${i + 1}`,
        description: `criterion ${i + 1}`,
        observableCheck: c,
        sourceRefs: [{ kind: "message", ref: "s" }]
      })),
      assumptions: [],
      questions: [],
      authority: [],
      sourceRefs: [{ kind: "message", ref: "s" }]
    });
  check("S5-H-1: fast-only contract → no conflicts", detectConflicts(mk(["fast check", "other"])).length === 0);
  check("S5-H-1: slow-only contract → no conflicts", detectConflicts(mk(["slow check", "other"])).length === 0);
  const conflicted = mk(["must be fast", "may be slow"]);
  const conflicts = detectConflicts(conflicted);
  check("S5-H-1: fast+slow contract → one conflict", conflicts.length === 1 && conflicts[0]?.ids.length === 2);
  const resolved = applyPrecedence(conflicted, "user-first");
  check(
    "applyPrecedence user-first keeps first criterion and drops second",
    resolved.acceptanceCriteria.length === 1 &&
      resolved.acceptanceCriteria[0]?.id === "ac-1" &&
      resolved.assumptions.some((a) => a.id === "a-superseded-ac-2")
  );
  check("production contract has no conflicts", detectConflicts(real).length === 0);
  check("critique of production contract is clean", critiqueContract(real).score === 100);
  threw = false;
  try {
    assertCanPromoteFromReview({ reviewerKind: "self", reviewerId: "rev-1", actorId: "act-1" });
  } catch {
    threw = true;
  }
  check("self-review promotion rejected fail-closed", threw);
  const empty = reconcileReviews([]);
  check("reconcile empty input → tie", empty.consensus === "tie" && empty.dissentCount === 0);
}

/* ================================================================
 * Cold tier on dist, default + configured (5 fresh processes each).
 * ================================================================ */
const mkColdScript = (configured: boolean): string => `
  const { performance } = await import("node:perf_hooks");
  const { extractHeuristicContract } = await import("/workspace/dist/requirement/heuristic.js");
  const { assertCoverageAllowsStart } = await import("/workspace/dist/requirement/coverage.js");
  const { applyPrecedence } = await import("/workspace/dist/requirement/precedence.js");
  const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
  const args = ${"{"} objective${"}"};
  ${configured ? 'const cfgArgs = { objective, habits: { requireTests: true, preferReview: false, askBeforeWrite: true } };' : ""}
  const t0 = performance.now();
  const prod = await extractHeuristicContract(${configured ? "cfgArgs" : "args"});
  const tasks = Array.from({ length: 5 }, (_, i) => ({
    id: "tsk_0000000" + i,
    acceptanceCriteria: prod.contract.acceptanceCriteria.map((c) => ({ id: c.id }))
  }));
  ${configured
    ? "assertCoverageAllowsStart(prod.contract, tasks, { resolvedQuestionIds: prod.contract.questions.map((q) => q.id) });"
    : "assertCoverageAllowsStart(prod.contract, tasks);"}
  applyPrecedence(prod.contract, "user-first");
  const t1 = performance.now();
  process.stdout.write((t1 - t0).toFixed(3));
`;
{
  for (const [label, configured] of [
    ["default", false],
    ["configured (habits + Q=1 gated)", true]
  ] as const) {
    const samples: number[] = [];
    for (let i = 0; i < 5; i += 1) {
      const proc = spawnSync(process.execPath, ["--input-type=module", "-e", mkColdScript(configured)], {
        encoding: "utf8"
      });
      if (proc.status !== 0) {
        check(`cold probe runs (${label})`, false, proc.stderr);
        break;
      }
      samples.push(Number(proc.stdout.trim()));
    }
    log(
      `COLD first-call production total (dist, fresh process, ${label}): median=${(median(samples) * 1e3).toFixed(0)}µs range=[${(Math.min(...samples) * 1e3).toFixed(0)}, ${(Math.max(...samples) * 1e3).toFixed(0)}]µs — once per PROCESS (veto class)`
    );
    check(`cold probes produced numbers (${label})`, samples.length === 5 && samples.every(Number.isFinite));
  }
}

/* ================================================================
 * NEW AXIS part 2: JIT tier audit (child process on dist with
 * --allow-natives-syntax; see /tmp/r14h-jit-audit.mjs for the audit body).
 * ================================================================ */
{
  if (existsSync("/tmp/r14h-jit-audit.mjs")) {
    const proc = spawnSync(process.execPath, ["--allow-natives-syntax", "/tmp/r14h-jit-audit.mjs"], {
      encoding: "utf8",
      timeout: 300000
    });
    if (proc.status !== 0) {
      check("JIT audit child runs", false, proc.stderr.slice(0, 2000));
    } else {
      process.stdout.write(proc.stdout);
      check("JIT audit reports zero deopt marks", /JIT SUMMARY: 0\//.test(proc.stdout), proc.stdout.split("\n").at(-2));
    }
  } else {
    check("JIT audit script present", false);
  }
}

if (failures > 0) {
  process.stderr.write(`\n${failures} of ${checks} check(s) FAILED\n`);
  process.exit(1);
}
log(`\nALL ${checks} SANITY/FAIL-CLOSED CHECKS PASSED (deterministic)`);
```

## 附录 B：JIT 档审计子进程（完整，可复现）

保存为 `/tmp/r14h-jit-audit.mjs`，由附录 A 主脚本以
`node --allow-natives-syntax` 拉起（需 dist 产物）。层级读数采用
touch-settle-read 协议（重触碰 → 500ms 并发分层沉淀 → 轻触碰再物化 →
读取）；瞬时层级随 V8 代码老化时点在 baseline↔turbofanned 间摆动属
预期（报告主指标为 deopt 位计数，三次运行 0/27 稳定）。

```js
/**
 * R14-H JIT tier audit (fourteenth pass) — engine-substrate lens, part 2.
 * Run with: node --allow-natives-syntax /tmp/r14h-jit-audit.mjs
 *
 * Warms every audited slice function at production scale on the dist build,
 * then reads %GetOptimizationStatus for each and decodes the V8 12.x bits.
 * Purpose: prove (or refute) that the H slice hides no deopt-loop cost
 * channel and that the hot-chain functions tier up cleanly — the one V8
 * subsystem never directly measured for this slice in thirteen prior
 * passes (mirrors R14-B §2e / R14-E JIT audits).
 */
import { extractHeuristicContract, heuristicExtractor, heuristicCritic, isVague, namedTargets, shouldScout } from "/workspace/dist/requirement/heuristic.js";
import { assertCoverageAllowsStart, checkCoverageGate, coverageMatrixFromTasks } from "/workspace/dist/requirement/coverage.js";
import { applyPrecedence, detectConflicts } from "/workspace/dist/requirement/precedence.js";
import { critiqueContract } from "/workspace/dist/requirement/critic.js";
import { findUnsourcedItems } from "/workspace/dist/requirement/provenance.js";
import { normalizeSources, createTrustedSource, buildContractFromSources } from "/workspace/dist/requirement/normalizer.js";
import { buildContractCandidate } from "/workspace/dist/requirement/extractor.js";
import { validateRequirementContract } from "/workspace/dist/domain/contract.js";
import { assertCanPromoteFromReview, applyRoutingScoreUpdate } from "/workspace/dist/review/self-review.js";
import { runBlindPairwisePair } from "/workspace/dist/review/pairwise.js";
import { reconcileReviews } from "/workspace/dist/review/reconcile.js";
import { createCriticObservation } from "/workspace/dist/review/critic.js";
import { createEvaluationRecord } from "/workspace/dist/evaluation/evaluator.js";
import { classifyDiffScope } from "/workspace/dist/evaluation/ownership.js";
import { selectHighestPrecedence, getPrecedenceWeight } from "/workspace/dist/evaluation/precedence.js";
import { createRubric } from "/workspace/dist/rubric/types.js";

// V8 12.x (Node 22) %GetOptimizationStatus bits.
const BITS = {
  isFunction: 1 << 0,
  maybeDeopted: 1 << 3,
  optimized: 1 << 4,
  maglevved: 1 << 5,
  turbofanned: 1 << 6,
  interpreted: 1 << 7,
  markedForOptimization: 1 << 8,
  markedForConcurrentOptimization: 1 << 9,
  optimizingConcurrently: 1 << 10,
  markedForDeoptimization: 1 << 14,
  baseline: 1 << 15,
  isLazy: 1 << 18
};
function decode(status) {
  if ((status & BITS.isFunction) === 0) return "NOT-A-FUNCTION";
  const tags = [];
  if (status & BITS.turbofanned) tags.push("turbofanned");
  else if (status & BITS.maglevved) tags.push("maglevved");
  else if (status & BITS.optimized) tags.push("optimized");
  else if (status & BITS.baseline) tags.push("baseline");
  else if (status & BITS.interpreted) tags.push("interpreted");
  else if (status & BITS.isLazy) tags.push("lazy(never-compiled)");
  if (status & (BITS.markedForOptimization | BITS.markedForConcurrentOptimization | BITS.optimizingConcurrently)) {
    tags.push("tiering-in-progress");
  }
  if (status & BITS.maybeDeopted) tags.push("MAYBE-DEOPTED");
  if (status & BITS.markedForDeoptimization) tags.push("MARKED-FOR-DEOPT");
  return tags.join("+") || `raw:${status}`;
}

const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
const vagueObjective = "fix bug please";

/* Production-scale warm-up: the real chain shapes drive every function the
 * chain reaches (incl. private closures sharing FeedbackCells across
 * per-call extractor/critic instances). */
for (let i = 0; i < 4000; i += 1) {
  const prod = await extractHeuristicContract({ objective });
  const tasks = Array.from({ length: 5 }, (_, k) => ({
    id: "tsk_0000000" + k,
    acceptanceCriteria: prod.contract.acceptanceCriteria.map((c) => ({ id: c.id }))
  }));
  assertCoverageAllowsStart(prod.contract, tasks);
  applyPrecedence(prod.contract, "user-first");
}
for (let i = 0; i < 1000; i += 1) {
  await extractHeuristicContract({ objective, habits: { requireTests: true, preferReview: false, askBeforeWrite: true } });
  await extractHeuristicContract({ objective: vagueObjective, habits: { askBeforeWrite: true } });
}
const prod = await extractHeuristicContract({ objective });
const contract = prod.contract;
const tasks5 = Array.from({ length: 5 }, (_, k) => ({
  id: "tsk_0000000" + k,
  acceptanceCriteria: contract.acceptanceCriteria.map((c) => ({ id: c.id }))
}));
const matrix = coverageMatrixFromTasks(contract, tasks5);
const rubric = createRubric("rub-1", "task", [
  { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
  { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
  { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
]);
const evalInput = {
  episodeId: "ep_00000001",
  evaluator: { kind: "deterministic", version: "v1", rubricVersion: "1" },
  rubric,
  evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
};
const pairInput = {
  episodeId: "ep_00000001",
  aId: "cand-a", bId: "cand-b",
  aScore: 0.8, bScore: 0.7,
  aComment: "solid", bComment: "weaker"
};
const rawSource = createTrustedSource({ kind: "message", ref: "cli-objective", origin: "user-turn", content: objective });
const sources = normalizeSources([rawSource]);
const extractorInstance = heuristicExtractor({});
const criticInstance = heuristicCritic();
const diffCtx = { episodeId: "ep_00000001", workingDirectory: "/w", revision: "r1", changeSet: ["src/a.ts", "src/b.ts"] };
const changed = ["src/a.ts", "dist/x.js", "node_modules/y/z.js", "other.ts", "src/gen.generated.ts"];

// Direct warm-up for faces the chain does not reach (zero-caller command classes).
for (let i = 0; i < 20000; i += 1) {
  checkCoverageGate(contract, matrix);
  coverageMatrixFromTasks(contract, tasks5);
  detectConflicts(contract);
  critiqueContract(contract);
  findUnsourcedItems(contract);
  isVague(objective);
  namedTargets(objective);
  shouldScout(objective);
  normalizeSources([rawSource]);
  createTrustedSource({ kind: "message", ref: "cli-objective", origin: "user-turn", content: objective });
  validateRequirementContract(contract);
  assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" });
  applyRoutingScoreUpdate({ reviewerId: "rev-1", actorId: "act-1", reviewerKind: "peer", currentScore: 1, delta: 0.5 });
  createEvaluationRecord(evalInput);
  runBlindPairwisePair(pairInput, () => 0.7);
  createCriticObservation({ episodeId: "ep_00000001", rubric, evidence: { "crit-1": "e" } });
  classifyDiffScope(diffCtx, changed);
  selectHighestPrecedence(["inferential", "deterministic", "human"]);
  getPrecedenceWeight("human");
  buildContractFromSources(objective, [rawSource]);
}
const pair = runBlindPairwisePair(pairInput, () => 0.7);
for (let i = 0; i < 20000; i += 1) reconcileReviews([pair.first, pair.swapped]);
for (let i = 0; i < 5000; i += 1) {
  await extractorInstance.extract({ objective, sources });
  await criticInstance.critique({ contract, sources });
  await buildContractCandidate({ objective, sources: [rawSource], extractor: extractorInstance, critic: criticInstance, minimumConfidence: 0.8 });
}

const audit = [
  ["extractHeuristicContract", extractHeuristicContract],
  ["heuristicExtractor(factory)", heuristicExtractor],
  ["extractor.extract(closure)", extractorInstance.extract],
  ["critic.critique(closure)", criticInstance.critique],
  ["buildContractCandidate", buildContractCandidate],
  ["isVague", isVague],
  ["namedTargets", namedTargets],
  ["shouldScout", shouldScout],
  ["critiqueContract", critiqueContract],
  ["findUnsourcedItems", findUnsourcedItems],
  ["normalizeSources", normalizeSources],
  ["createTrustedSource", createTrustedSource],
  ["buildContractFromSources", buildContractFromSources],
  ["validateRequirementContract(out-of-slice)", validateRequirementContract],
  ["checkCoverageGate", checkCoverageGate],
  ["coverageMatrixFromTasks", coverageMatrixFromTasks],
  ["assertCoverageAllowsStart", assertCoverageAllowsStart],
  ["applyPrecedence", applyPrecedence],
  ["detectConflicts", detectConflicts],
  ["assertCanPromoteFromReview", assertCanPromoteFromReview],
  ["applyRoutingScoreUpdate", applyRoutingScoreUpdate],
  ["runBlindPairwisePair", runBlindPairwisePair],
  ["reconcileReviews", reconcileReviews],
  ["createEvaluationRecord", createEvaluationRecord],
  ["createCriticObservation", createCriticObservation],
  ["classifyDiffScope", classifyDiffScope],
  ["selectHighestPrecedence", selectHighestPrecedence]
];
/* Touch-settle-read protocol: without a recent execution V8 may flush the
 * bytecode of idle functions (status collapses to bare isFunction), so
 * (1) re-touch every audited callable, (2) let concurrent tier-up jobs
 * settle, (3) lightly re-materialize, then read. */
async function touchAll(reps) {
  for (let i = 0; i < reps; i += 1) {
    await extractHeuristicContract({ objective });
    await extractorInstance.extract({ objective, sources });
    await criticInstance.critique({ contract, sources });
    await buildContractCandidate({ objective, sources: [rawSource], extractor: extractorInstance, critic: criticInstance, minimumConfidence: 0.8 });
    heuristicExtractor({});
    isVague(objective);
    namedTargets(objective);
    shouldScout(objective);
    critiqueContract(contract);
    findUnsourcedItems(contract);
    normalizeSources([rawSource]);
    createTrustedSource({ kind: "message", ref: "cli-objective", origin: "user-turn", content: objective });
    buildContractFromSources(objective, [rawSource]);
    validateRequirementContract(contract);
    checkCoverageGate(contract, matrix);
    coverageMatrixFromTasks(contract, tasks5);
    assertCoverageAllowsStart(contract, tasks5);
    applyPrecedence(contract, "user-first");
    detectConflicts(contract);
    assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" });
    applyRoutingScoreUpdate({ reviewerId: "rev-1", actorId: "act-1", reviewerKind: "peer", currentScore: 1, delta: 0.5 });
    runBlindPairwisePair(pairInput, () => 0.7);
    reconcileReviews([pair.first, pair.swapped]);
    createEvaluationRecord(evalInput);
    createCriticObservation({ episodeId: "ep_00000001", rubric, evidence: { "crit-1": "e" } });
    classifyDiffScope(diffCtx, changed);
    selectHighestPrecedence(["inferential", "deterministic", "human"]);
  }
}
await touchAll(500);
await new Promise((resolve) => setTimeout(resolve, 500));
await touchAll(100);

let deopts = 0;
for (const [name, fn] of audit) {
  const status = %GetOptimizationStatus(fn);
  const decoded = decode(status);
  if (decoded.includes("DEOPT")) deopts += 1;
  process.stdout.write(`JIT ${name}: ${decoded} (raw=${status})\n`);
}
process.stdout.write(`JIT SUMMARY: ${deopts}/${audit.length} functions carry deopt marks\n`);
```

MORE_OPTIMA=no
BRANCH=cursor/r14-h-eval-fourteenth-pass-83a1
