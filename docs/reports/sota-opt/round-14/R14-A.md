MODEL_SLUG=claude-fable-5-thinking-xhigh

# R14-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第十四遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 14 / R14-A（Round 1–13 同区第十四遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `a9402f4`（含 R13-H 空枚举收口与 Round 14 开轮）
**分支:** `cursor/r14-a-tracking-fourteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S14-A-\* ID，切片零代码改动；
整片预算收口第十二次复核成立，并新增两条实测轴——「E 规模越线标定」
（R13-H 方法学移植到 A 区，把重开条件从定性量级改为实测穿越点）与
「max-codes 生产格」（补齐锚点矩阵的 codes 长度维度）。**
切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R13-A 至本轮基线
（`a9402f4`）**逐字节未变**（`git diff 7acb666..a9402f4 -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，十四遍全程零 diff；
清单核对恰 14 文件并全部实际重读）。调用面维持最强冻结并再延一轮：
R10-A 基线（`89f0455`）以来 `git diff 89f0455..a9402f4 -- src/`
**完全为空**（跨 R10…R14 开轮五段全部为 docs 提交），生产调用方经全库
grep 复核仍为 `supervisor.ts:483`（applyTrackingGate/nextTrackingSeq；
两个 settle 调用点 601/711 均不传 trackingAssessment ⇒ 生产 O(1) no-op，
R9-A 事实第六次复验）/ `coordinator.ts:444` / `flowchart-run.ts:320`
（applyChildThreeLine），每子结果一次（~5 次/run），事件表几十级（41）。
R13-A 的 ~70–81 µs/run 预算天花板经本轮在当前 VM（Node v22.22.2）实测
复核为 **~69–81 µs/run**（2×2 锚点矩阵 + 新增 max-codes 格，三次独立
运行，五格 13.9–16.3 µs/gate，与 R13-A 同带；量级结论不变——即使切片
零成本，节省上界 ~0.08 ms/run，仍比落地线低约三个量级）。

按令对 R13-A「turn 内部无一子组件未点名」的声明做了**结构位置 ×
消除机制满矩阵**第十四遍扫描：**未找到 R13-A 漏掉的站点**。本轮枚举出
的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点（3 项新记录，
见 §0；R12-A 13 项 + R13-A 8 项换名检查全部复核维持，未追认任何
S12-A-\*/S13-A-\* ID），按令一律不铸新 ID。作为空枚举的实证支撑，本轮
新落档两组数据（§1，三次独立运行稳定）：

1. **E 规模越线标定**：全生产门在 E∈{41, 410, 4100} 实测拟合出
   「固定地板 ~11.2–11.8 µs/gate + ~11.6–13.9 ns/event」（四次运行，
   含附录脚本复验）；据此整片预算穿越 10 ms/run 线需要 **E ≈
   1.4–1.7×10⁵ 事件（比生产 E=41 高 ~3.5–3.6 个量级）**，穿越
   50 ms/run 线需要 **E ≈ 7.2–8.6×10⁵
   （~4.2–4.3 个量级）**——既有「≥2–3 个量级」重开条件被证明是偏
   乐观的下界；另获机制修正：O(E) 扫描面在 **E ≈ ~650** 即反超三次
   hashAssessment 地板（E=4100 时已占整门 38%），故若规模真解冻，
   X0-4/X2-4 扫描面候选确为第一优先（收紧 R12-A「届时哈希地板仍占
   主导」的预期表述——该表述仅在 E ≲ 650 成立）。
2. **max-codes 生产格 + 可达 codes 集合引理（F1）**：8000 组 seeded
   fuzz 证实 from-child 生产路径 gate.codes 恒 ≤2 且 hard 侧
   ⊆ {deterministic-fail, claimed-verification-without-checks}（其余
   四个 hard code 被构造常量钉死为 false：escaped≡false、
   stalledTurns≡0、human≡unobserved），soft 侧在 7707 个 apply 路径上
   **零出现**（S10-A-1 引理 C 二元化的分布级复现）；两码最重生产形态
   （FAILED + 成功声明 + 检查缺口）e2e 实测 **14.6–15.0 µs/gate**，与
   2×2 矩阵同带**无悬崖**——codes 长度维度闭合。

R12-A 分解的五大组件占比（hashAssessment×3 ~41–42% / validateEvent
~20–21% / turn ~13% / prescore×2 ~10% / O(E) 扫描 ~1.6%）在本 VM 逐一
复现，且本轮把第五个事件表走查（currentGateStatus，51–65 ns）**首次
列为显式分解行**：五扫描 + 拷贝合计 275–294 ns（~1.9–2.0%；扣除第五
扫描后 224–229 ns 与历轮 ~1.6% 口径逐位对齐——先前口径未把 status 扫描
单列，O(E) 库存至此补全）。第十四遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：roller / analysis / gates / config /
  combined-score / from-child / index / turn / isolation / human-score /
  prescore / types）+ `src/run/child-tracking.ts` + `src/run/gate-apply.ts`，
  **清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2），全部实际
  重读**。未触碰其余 `src/run/`（G 切片）及任何其他切片。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表，含 S13-B-1；
  R12-A / R13-A 空枚举未铸 ID，确认）/ round-14/PLAN.md /
  round-13/PLAN.md + R13-A.md / round-12/R12-A.md / round-11/R11-A.md /
  round-10/R10-A.md / round-09/R9-A.md。R13-I / R13-J 在其他分支在跑，
  未等待、未触碰其分支与报告。EXCLUSIONS.md / PROGRESS.md / PLAN.md
  零触碰。
- 环境：本 VM 默认 Node v22.14.0，按 engines（>=22.19.0）经 nvm 切至
  v22.22.2 后 `pnpm install --frozen-lockfile`，全部测量在 v22.22.2
  完成。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点，**一律不铸 S14-A-\* ID**。R12-A 的 13 项与 R13-A 的 8 项换名
  检查全部复核维持，不再逐条重列；以下 3 项为本轮**新记录**的换名
  检查）：
  1. gate-apply **幂等双 find 谓词闭包 + idempotencyKey 模板字符串的
     每 gate 分配提升/预构造**（两个 find 回调是捕获
     `idempotencyKey`/`input` 的新鲜闭包，模板串是每 gate 一次的小字符
     串分配）= 模块级提升需可变捕获态 ⇒ X1-1 隐藏状态本体；逐调用携带
     = S3-E-5 字面量提升族（1–6 ns）+ S1-B-5 分配噪声族。本轮分解实测
     两个 find **含全扫描**各仅 39–41 ns，分配份额为其中个位 ns，
     拒列；
  2. `extractTenPoint` **每调用 `new RegExp(source, "gi")` 构造的
     缓存/预构造**（TEN_POINT_SLASH/FEN 每次调用重建全局正则）=
     X0-6 本体（其否决理由——/g lastIndex 状态风险——正是该结构存在
     的原因）+ S7-A-1（from-child 生产路径在 `text === ""` 处短路，
     该构造**生产零流量**，R13-A 剖面 extractHumanScore({}) 12–14 ns
     即全部成本），拒列；
  3. `hashAssessment` **gate.codes 长度 ≤1 时跳过 `[...codes].sort()`
     快路径**（F1 引理：生产 codes ≤2，none 路径恒 []）= S8-F-3
     长度-1 快路径族 + S2-A-5 spread 拷贝族换位点；收益上界内含于
     R12-A F1 哈希排序机器天花板（272–283 ns/hash）的真子集且哈希
     契约整体锁死（X1-2/S5-H-3 禁复制），拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  六维双 `.some` 谓词融合（from-child line 57/70）= S2-A-6 本体；
  caller 侧 `nextTrackingSeq` 与 gate 内 `currentGateStatus` 跨公开
  边界单遍共走 = X2-4/X0-4 本体（expectedSeq 是公开输入）；
  `observationFromChild` 条件 spread = R12-A §0-5；evidenceRefsOf
  双算 = S2-A-2；`combineScore` isMachineScore 守卫消除 = 防御纵深
  维持（R12-A/R13-A 已录保留）；streaming hash 免中间 payload 对象
  = S5-A-2（实测慢 11–15%）。
- 未重开任何 X\* / S1-\* … S13-\* 条目。S11-A-1 / S11-A-2 / S10-A-1 /
  S9-A-1 / S8-A-1..3 / S7-A-1..4（平行 turn 路径/公开旗标结构）、
  X0-4/X2-4（签名/单遍合并）、X0-6（human-score 正则缓存）、S6-A-1
  （toFixed 舍入）专项复核：本轮零触碰、零重提，全部维持关闭。
  R12-A / R13-A 空枚举未铸 ID，本轮未追认、未补铸。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足。不声称 Outcome-supported；Checkpoint
  F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本
  （`scripts/round14-r14a-equivalence-sim.ts` 按令不建），仿真全文见
  本报告附录。不开 PR。

## 1. 预算支配论证复核 + 两条新轴（方法第 1、2 步）

R3-A §3 … R13-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..a9402f4 -- <切片>` 为空，逐字节
   一致（十四遍全程零 diff）——R1-A…R13-A 全部测量与排除按令转移，但
   数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨五段）**：`git diff 89f0455..a9402f4 -- src/`
   **完全为空**。生产调用方三点位、supervisor settle no-op（601/711
   不传 assessment，`settleSupervisedOutcome` 首行
   `if (assessment === undefined) return`）、hashSummary 生产零流量
   （roller.ts:68 唯一调用点且 from-child 恒无 previous）、
   `parseTrackingAssessment` 唯一 `src/` 调用方为 events.ts:738（写侧
   屏障）、`GateApplyResult.runStatus/applied/transitionId` 零 `src/`
   读者（coordinator 455/460 只读 directive；flowchart-run 只切
   `gated.events`）、analysis/isolation 面零 `src/` 生产调用方、
   `src/track/` 对切片零导入七项事实经本轮全库 grep 全部复验。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run；本轮新增第
五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               16.1      15.8      16.3  µs/gate (+1 event)
PASSED x contract(3)               14.4      13.9      14.0  µs/gate (+1 event)
FAILED x no-contract               16.1      15.3      15.7  µs/gate (+3 events)
FAILED x contract(3)               14.3      13.9      14.5  µs/gate (+3 events)
NEW FAILED x contract(3) x 2-code  14.9      14.6      15.0  µs/gate (+3 events)
=> ~69–81 µs/run
```

与 R13-A（14.1–16.1 µs/gate ⇒ 70–81 µs/run）同带；R11-A 以来的
「no-contract 格略贵于 contract 格」PIC/基准次序现象复现，单格横向比较
不具意义；五格三次运行**无悬崖、无空洞**。max-codes 格（两个 hard
code：deterministic-fail + claimed-verification-without-checks，哈希内
codes sort 2 元素）与 1-code FAILED 格差 +0.3–0.7 µs，在格间噪声带内
⇒ codes 长度维度对预算无结构性影响，闭合。即使把整个切片优化到零成本，
节省上界 ~0.08 ms/run，仍比战役落地线（数十~数百 ms 或复杂度类下降）低
**约三个量级**。**支配论证第十二次复核成立。**

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R13-A 括号内区间逐项同带；本轮把第五扫描 currentGateStatus
首次单列）：

| 组件 | 本轮实测/gate | R13-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1954–2004 ns 每次 ⇒ **5.86–6.01 µs** | 5.86–5.93 µs | ~41–42% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 306–352 ns + 第三次 hash） | **2.90–3.10 µs** | 2.92–2.95 µs | ~20–21% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 已下探至八子组件全点名） | **1.77–1.81 µs** | 1.78–1.95 µs | ~13% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 695–706 ns 每次 ⇒ **1.39–1.41 µs** | 1.39–1.60 µs | ~10% | S1-A-2 |
| O(E) 扫描合计（episodeId 65–68 + seq 53–54 + 双幂等 find 39–41×2 + **status 51–65（本轮新单列）** + `[...events]` 拷贝 27–28 ns） | **275–294 ns**（扣 status 后 224–229，与历轮 ~1.6% 口径对齐） | 226–228 ns（4 扫描口径） | **~1.9–2.0%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |
| `observationFromChild` 等杂项 | 56–57 ns | 56–59 ns | <1% | S1-B-5 分配噪声族 |

\* 以 13.9–14.5 µs 锚点格计；`applyTrackingGate` 整体隔离实测
6.15–6.34 µs，与 R13-A（6.23–6.27）同带。

**本轮新轴 1：E 规模越线标定**（全生产门 + 五扫描组件在三个规模档
实测，三次运行区间）：

```text
E=41   : full gate 12.0–12.8 µs | 5 scans+copy 0.52–0.53 µs ( ~4.1–4.3% of gate)
E=410  : full gate 17.2–17.4 µs | 5 scans+copy 2.68–2.72 µs (~15.5–15.7%)
E=4100 : full gate 67.1–68.5 µs | 5 scans+copy 25.97–26.14 µs (~38.2–38.7%)
fit    : fixed floor ~11.6–11.8 µs/gate + ~13.53–13.86 ns/event（取 E=410..4100 斜率）
crossing (10 ms/run) : E ≈ 143,454–146,980（~3.5–3.6 个量级于生产 E=41 之上）
crossing (50 ms/run) : E ≈ 720,626–738,356（~4.2–4.3 个量级）
sanity  : perRun(41) ≈ 0.061–0.062 ms；perRun(4100) ≈ 0.336–0.342 ms
（附录脚本第四次复验：斜率 11.59 ns/event、穿越 171,604 / 861,878——
斜率跨运行有 ~±10–20% 抖动，穿越带取四次并集 1.4–1.7×10⁵ /
7.2–8.6×10⁵；量级结论 3.5–3.6 / 4.2–4.3 个量级四次全部稳定）
```

（方法学记录：标定段的 E=41 全门 12.0–12.8 µs 略低于锚点段同格
13.9–14.4 µs——测量位置/PIC 状态不同所致，与 R11-A/R12-A「单格横向
比较不具意义」现象同型；标定采用**同段内**三档相对斜率，不受影响。）

三个结论：

1. **重开条件量化收紧**：历轮「run 事件表增长 ≥2–3 个量级」的定性重开
   条件，实测为**穿越 10 ms/run 线需 ~3.5–3.6 个量级、穿越 50 ms/run
   线需 ~4.2–4.3 个量级**——即使事件表膨胀 100×（E=4100），整片也仅
   ~0.34 ms/run，仍低于落地线 1.5–2 个量级。支配论证对规模扰动的鲁棒
   余量首次有了实测曲线而非外推。
2. **扫描面反超点实测**：O(E) 面（五扫描 + 拷贝，13.5–13.9 ns/event）
   在 **E ≈ ~650** 反超三次 hashAssessment 地板（5.9–6.0 µs），
   E=4100 时已占整门 38%。R12-A §6「届时哈希地板仍占主导」的预期仅在
   E ≲ 650 成立；若规模真解冻，第一优先重开对象确为 X0-4/X2-4 扫描面
   索引化（与既有重开优先级一致，表述获机制修正）。
3. **曲线无悬崖**：三档全部线性外插吻合（sanity 行），无隐藏超线性
   组件——`[...events]` 拷贝与四次 find/走查同斜率族，validateEvent/
   hash/turn 全部与 E 无关。

**本轮新轴 2 / F1/F2 归档事实**（8000 组 seeded fuzz + e2e 构造，三次
运行逐位一致）：

- **F1（可达 codes 集合引理）**：7707 个 apply 路径上 gate.askUser
  恒 false；hard 侧 codes ⊆ {deterministic-fail,
  claimed-verification-without-checks}（构造常量钉死其余四个 hard
  code：`escaped: false` 字面量、`stalledTurns: 0`、`humanInput: {}` ⇒
  human 恒 unobserved ⇒ userRejectStop/safetyRejected 恒 false）；
  codes 长度恒 ≤2（两码形态 915 次可达）；soft 侧**零出现**——
  S10-A-1 引理 C（PASSED 侧 P 下界 0.6667 > 0.55）的分布级复现。
  任何「按 codes 形态特化哈希/门控」提案可直接引用本事实 + §0-3
  拒列裁决。
- **F2（max-codes e2e 构造证人）**：FAILED + 成功声明 summary +
  tester 检查缺口经真实构造链恰好产出两码
  `["claimed-verification-without-checks","deterministic-fail"]`，
  即生产最重门控形态；其 e2e 成本（14.6–15.0 µs/gate）已入锚点矩阵
  第五格。
- **五扫描库存补全**：fresh 路径事件表走查恰五次（episodeId / seq /
  幂等 find×2 / status）+ 一次不可变拷贝；status 扫描 51–65 ns 单列后
  O(E) 口径自洽（~1.9–2.0% vs 历轮 4 扫描口径 ~1.6%），S1-A-1 的
  ~60 ns 量级判断获直接复核。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 3 项 + R12-A 13 项 +
R13-A 8 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点或
本体，按「换名不铸 ID」纪律未列任何 S14-A-\* 候选。空枚举与 R12-A /
R13-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第十四遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `gate-apply.ts` | find 谓词闭包/模板串分配提升（§0-1，实测 find 含扫描仅 39–41 ns）；第五扫描 status 单列（51–65 ns，S1-A-1/X2-4 库存补全）；E 规模标定（新轴 1：13.5–13.9 ns/event 斜率全部落在 X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 拷贝下界守护的既关面上）；S11-A-2 / S2-A-4 / S8-A-1 / FAIL_CLOSED 兜底维持；executionAuthority 零调用方零计算维持 | 无新候选 |
| `types.ts` | codes ≤1 sort 快路径（§0-3，F1 引理支撑，哈希契约锁死）；R12-A F1 常量置换天花板 + R13-A F2 比较器域一致性维持；S1-A-8/X0-5 parse 面、S2-A-5/S5-A-2/S3-B-3 哈希面维持；hashSummary 生产零流量维持 | 无新候选 |
| `human-score.ts` | extractTenPoint 每调用 RegExp 构造缓存（§0-2，X0-6 本体 + 生产零流量）；X0-6 / S1-A-3 / R5-A 频次重排 / S7-A-1 整管道死维持 | 无新候选 |
| `from-child.ts` | 六维双 .some 融合 = S2-A-6 本体再确认；F1/F2 可达 codes 引理归档（gateFacts 四常量钉死）；S1-A-2 / S2-A-2/6 / S3-A-3 / S4-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十一层收口维持 | 无新候选 |
| `child-tracking.ts` | caller 侧 seq 与 gate 内 status 跨边界共走 = X2-4/X0-4 本体（expectedSeq 公开输入）；skipped 死分配 = S7-A-3 族维持；批量门控 API = X0-4+X1-2 维持 | 无新候选 |
| `turn.ts` | R13-A 八子组件剖面维持（本轮整体 1.77–1.81 µs 同带）；恒定输入常量折叠伞（R12-A §0-11）维持；S1-A-7 / S2-A-3 / S3-A-2/4 / S4-A-1 / S5-A-1 / S7-A-1 / S9-A-1 / S10-A-1 收口维持 | 无新候选 |
| `prescore.ts` | R12-A §0-2..5 / R13-A §0-7..8 维持；S1-A-4 / S3-A-3 / S6-A-1 / S8-A-3 收口维持 | 无新候选 |
| `gates.ts` | F1 分布级复现 S10-A-1 引理 C（soft 零出现、askUser 恒 false）；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 享元自败维持 | 无新候选 |
| `roller.ts` | S9-A-1 / S1-A-5 / S2-A-1 / S3-A-1 / S7-A-2 维持（R13-A 剖面量化档案沿用，本轮 turn 整体同带） | 无新候选 |
| `combined-score.ts` | isMachineScore 边界防御保留维持；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |
| `analysis.ts` | S8-A-2 维持（test-only + sanitize choke point，本轮 grep 复验零 src/ 生产调用方） | 无新候选 |
| `isolation.ts` / `config.ts` / `index.ts` | O(1) 谓词 / 常量 / 纯再导出；零 src/ 生产调用方复验；惰性 import = R12-A §0-10 维持 | 无新候选 |

**对 R13-A 声明的裁决**：「turn 内部无一子组件未点名」在其粒度上成立；
本轮把搜查转向**规模维与形态维**（E 曲线、codes 长度、可达 codes
集合），三处新数据**全部只产出归档证人而非可落地候选**，且把唯一的
口径缺口（第五扫描未单列）补齐后 O(E) 库存自洽。收口的预言力第十四遍
成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R13-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F1: 8000 fuzz trials, 7707 apply paths; kinds=[["hard",3772],["none",3935]]; max codes=2; two-code gates seen=915
anchor: 16.1 / 14.4 / 16.1 / 14.3 / 14.9(max-codes) µs/gate => ~72–81 µs/run
decomposition: hash×3=5916ns | validateEvent=2917ns | turn=1810ns | prescore×2=1405ns | 5 scans+copy=278ns | applyTrackingGate whole=6.34µs
E-scaling: 41→12.8µs (4.1%) | 410→17.2µs (15.7%) | 4100→67.1µs (38.7%); fit 11.7µs + 13.53ns/event; crossing 10ms/run ≈ 146,980 (3.6 orders); 50ms/run ≈ 738,356 (4.3 orders)
ALL CHECKS PASSED (deterministic, seeded)

run 2:
anchor: 15.8 / 13.9 / 15.3 / 13.9 / 14.6 µs/gate => ~69–79 µs/run
decomposition: hash×3=6012ns | validateEvent=3103ns | turn=1768ns | prescore×2=1411ns | 5 scans+copy=275ns | whole=6.19µs
E-scaling: fit 11.8µs + 13.61ns/event; crossing 146,088 / 733,909
ALL CHECKS PASSED (deterministic, seeded)

run 3:
anchor: 16.3 / 14.0 / 15.7 / 14.5 / 15.0 µs/gate => ~70–81 µs/run
decomposition: hash×3=5861ns | validateEvent=2896ns | turn=1792ns | prescore×2=1390ns | 5 scans+copy=294ns | whole=6.15µs
E-scaling: fit 11.6µs + 13.86ns/event; crossing 143,454 / 720,626
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立运行 F1/F2 结论逐位一致（7707 apply、915 两码门、soft 零出现
确定性重现）；锚点五格三次同带无悬崖；E 曲线三档线性吻合、穿越点区间
紧（10 ms 线 143–147k、50 ms 线 721–738k）。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S14-A-\*）。换名检查 3 项新记录见 §0（1–3），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件本轮**量化收紧**：整片预算支配论证的重开条件由「run 事件表或
每 turn 集合规模增长 ≥2–3 个量级」修正为**实测穿越点 E ≈ 1.4–1.7×10⁵
（10 ms/run 线，~3.5–3.6 个量级）/ E ≈ 7.2–8.6×10⁵（50 ms/run 线，
~4.2–4.3 个量级）**；且届时第一优先重开对象是 X0-4/X2-4 扫描面索引化
（扫描面在 E ≈ ~650 反超哈希地板，E=4100 已占整门 38%——修正 R12-A
「届时哈希地板仍占主导」的预期，该表述仅在 E ≲ 650 成立）。F1 可达
codes 集合引理随 from-child gateFacts 构造常量（escaped/stalledTurns/
humanInput）或 evaluateGates 门控结构改变而自动失效；E 曲线拟合随
事件表元素形状（payload 大小）系统性变化而需重测。

## 附录：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.ts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xe14a01`–`0xe14a02`。

```ts
/**
 * R14-A deterministic anchor + decomposition + NEW-axis simulation
 * (fourteenth pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R13-A lineage, gate-outcome x contract 2x2);
 *   (b) re-verifies the R12-A/R13-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation),
 *       this round listing currentGateStatus as an explicit O(E) line
 *       (the fifth event-table scan, S1-A-1/X2-4);
 *   (c) NEW axis 1: E-scaling crossing calibration. Measures the full
 *       production gate at E in {41, 410, 4100}, fits the marginal
 *       per-event cost of the five O(E) scans + immutable copy, and
 *       computes the event-table size at which the whole-slice per-run
 *       budget would cross 10 ms and 50 ms lines -- turning the
 *       qualitative ">=2-3 orders of magnitude" reopen condition into
 *       a measured crossing point (R13-H scale-calibration methodology
 *       transplanted to A).
 *   (d) NEW axis 2: max-codes production cell. The from-child
 *       production path can emit at most TWO hard codes
 *       (deterministic-fail + claimed-verification-without-checks;
 *       ownership-escape/repeated-no-progress/user-reject-stop/
 *       permission-security-reject are constant-false by construction).
 *       F1 verifies the reachable code-set lemma by fuzz; the cell
 *       measures the 2-hard-code gate e2e to close the codes-length
 *       fixture axis (prior anchors used 1 hard code max).
 *
 * Archival-fact fuzz:
 *   F1: production gate.codes reachability lemma. Over seeded fuzz of
 *       from-child observations: every apply-path gate has codes as a
 *       subset of {deterministic-fail, claimed-verification-without-checks}
 *       on the hard side, a single soft code on the soft side (zero soft
 *       gates observed, reconfirming S10-A-1 Lemma C binarization),
 *       [] on none; askUser === false always; length <= 2 always.
 *   F2: max-codes production shape is exactly 2 hard codes via the real
 *       e2e constructor (FAILED + success-claim summary + tester check
 *       gap).
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xe14a01..0xe14a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation,
  type ChildObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-24T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

/* ============================================================
 * Shared: realistic event tables (R3-A..R13-A harness lineage).
 * ============================================================ */
function buildEventTable(opts: { attached: boolean; filler: number }): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (opts.attached) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < opts.filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: { claimSuccessWithCheckGap?: boolean }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  // claimSuccessWithCheckGap: summary claims success while the tester's
  // required check is NOT completed (verification FAILED => completedChecks
  // empty), which flips claimed-verification-without-checks true -- the
  // maximum-hard-codes production shape (2 codes when kind === FAILED).
  const summary = opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran";
  return {
    child: {
      taskId: "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: ["evd_1"],
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: ["evd_1"] } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "tests pass" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "keep scope" },
        { id: "con_1", description: "no new deps" },
        { id: "con_2", description: "tests stay green" }
      ]
    } as unknown as RequirementContract
  };
}

function genObservation(rng: () => number, trial: number): ChildObservation {
  const kind = pick(rng, ["PASSED", "FAILED"] as const);
  return {
    taskId: `tsk_${trial % 7}`,
    role: pick(rng, ["implementer", "tester", "scout", "worker"]),
    outcome: pick(rng, ["SUCCESS", "PARTIAL", "FAILURE", "CANCELLED", "TIMEOUT"] as const),
    summary: pick(rng, [
      "tests passed",
      "child completed the task",
      "",
      "wip",
      "verified output",
      "all checks succeed"
    ]),
    evidenceIds: rng() < 0.75 ? [`evd_${trial}`, "evd_shared"] : [],
    artifactIds: rng() < 0.5 ? [`art_${trial}`] : [],
    verification: { kind, evidenceIds: rng() < 0.8 ? [`evd_v_${trial}`] : [] },
    requiredChecks: rng() < 0.5 ? (rng() < 0.5 ? ["test"] : ["chk_0", "chk_1"]) : [],
    constraints: Array.from({ length: Math.floor(rng() * 5) }, (_, i) => ({
      id: `c_${i}`,
      text: `constraint ${i}`,
      kind: "constraint" as const,
      mandatory: true as const
    }))
  };
}

/* ============================================================
 * F1: production gate.codes reachability lemma over the REAL code.
 * From-child pins ownershipEscape (escaped:false constant),
 * repeatedNoProgress (stalledTurns:0), userRejectStop / safetyRejected
 * (humanInput {} => human unobserved) all false. Reachable hard codes
 * are only deterministic-fail (verification FAILED) and
 * claimed-verification-without-checks (success claim + check gap).
 * ============================================================ */
{
  const rng = mulberry32(0xe14a01);
  const HARD_ALLOWED = new Set(["deterministic-fail", "claimed-verification-without-checks"]);
  let applies = 0;
  let maxCodes = 0;
  let twoCodeSeen = 0;
  const kindCount = new Map<string, number>();
  for (let trial = 0; trial < 8000; trial += 1) {
    const observation = genObservation(rng, trial);
    const decision = assessChildObservation({ observation, episodeId: "ep_x", runId: "run_x" });
    if (!decision.apply) continue;
    applies += 1;
    const gate = decision.turn.gate;
    kindCount.set(gate.kind, (kindCount.get(gate.kind) ?? 0) + 1);
    maxCodes = Math.max(maxCodes, gate.codes.length);
    if (gate.codes.length === 2) twoCodeSeen += 1;
    check("F1 (askUser always false on from-child path)", gate.askUser === false, `trial ${trial}`);
    if (gate.kind === "hard") {
      check(
        "F1 (hard codes subset of {deterministic-fail, claimed-verification-without-checks})",
        gate.codes.every((code) => HARD_ALLOWED.has(code)),
        `trial ${trial}: ${JSON.stringify(gate.codes)}`
      );
    } else if (gate.kind === "soft") {
      check(
        "F1 (soft codes exactly [soft-threshold] or [minor-escalated])",
        gate.codes.length === 1 &&
          (gate.codes[0] === "soft-threshold" || gate.codes[0] === "minor-escalated"),
        `trial ${trial}: ${JSON.stringify(gate.codes)}`
      );
    } else {
      check("F1 (none => empty codes)", gate.codes.length === 0, `trial ${trial}`);
    }
    check("F1 (codes length <= 2)", gate.codes.length <= 2, `trial ${trial}`);
  }
  console.log(
    `F1: 8000 fuzz trials, ${applies} apply paths; kinds=${JSON.stringify([...kindCount.entries()])}; max codes=${maxCodes}; two-code gates seen=${twoCodeSeen}`
  );
  check("F1 (two-code hard gate is reachable in fuzz)", twoCodeSeen > 0);
}

/* ============================================================
 * F2: max-codes production shape is exactly 2 hard codes via the real
 * e2e constructor (FAILED + success-claim summary + tester check gap).
 * ============================================================ */
const mkMax = mkChild("FAILED", { claimSuccessWithCheckGap: true });
{
  const obs = observationFromChild(mkMax.child, mkMax.spec, mkMax.contract);
  const decision = assessChildObservation({ observation: obs, episodeId: "ep_x", runId: "run_x" });
  check("F2 (max-codes fixture applies)", decision.apply === true);
  if (decision.apply) {
    const codes = [...decision.turn.gate.codes].sort();
    check(
      "F2 (max-codes fixture emits exactly the 2 reachable hard codes)",
      codes.length === 2 &&
        codes[0] === "claimed-verification-without-checks" &&
        codes[1] === "deterministic-fail",
      JSON.stringify(codes)
    );
  }
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
const events41 = buildEventTable({ attached: true, filler: 39 });
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R13-A lineage) + NEW max-codes cell.
 * ============================================================ */
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "NEW FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition of the PASSED x contract(3) cell
 * (R12-A/R13-A methodology, re-anchored; currentGateStatus listed
 * explicitly this round as the fifth O(E) scan).
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  // Fifth O(E) scan: currentGateStatus replica (private in gate-apply.ts).
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tObs = bench(() => void observationFromChild(mkP.child, mkP.spec, mkP.contract), 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate: caller+CAS+write-side): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side, in validateEvent): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole (parse + 3rd hash inside): ${ns(tVev)}`);
  console.log(`  episodeIdFromEvents scan (E=41): ${ns(tEpisode)}`);
  console.log(`  nextTrackingSeq scan (E=41): ${ns(tSeq)}`);
  console.log(`  idempotency find GATE_TRANSITION miss (E=41): ${ns(tFindGT)}`);
  console.log(`  idempotency find TRACKING_ASSESSMENT miss (E=41): ${ns(tFindTA)}`);
  console.log(`  currentGateStatus scan replica (E=41, 5th scan): ${ns(tStatus)}`);
  console.log(`  [...events] immutable append copy (E=41): ${ns(tCopy)}`);
  console.log(`  observationFromChild: ${ns(tObs)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full (contains S9-A-1 dead tail): ${ns(tTurn)}`);
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(`  SUM O(E) scans (5 scans + copy): ${ns(scans)}`);

  /* ============================================================
   * NEW axis 1: E-scaling crossing calibration.
   * ============================================================ */
  console.log("--- NEW: E-scaling crossing calibration ---");
  const sizes = [41, 410, 4100];
  const gateCosts: number[] = [];
  const scanCosts: number[] = [];
  for (const size of sizes) {
    const table = buildEventTable({ attached: true, filler: size - 2 });
    const mk = mkChild("PASSED");
    for (let i = 0; i < 200; i += 1) {
      let c = 100000;
      void applyChildThreeLine({
        events: table,
        child: mk.child,
        spec: mk.spec,
        contract: mk.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    }
    const reps = size <= 41 ? 5000 : size <= 410 ? 2000 : 400;
    const one = bench(() => {
      let c = 100000;
      void applyChildThreeLine({
        events: table,
        child: mk.child,
        spec: mk.spec,
        contract: mk.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    }, reps);
    gateCosts.push(one);
    const seqS = nextTrackingSeq(table);
    const hashS = hash;
    const oneScans =
      bench(() => void episodeIdFromEvents(table), 20000) +
      bench(() => void nextTrackingSeq(table), 20000) +
      bench(
        () =>
          void table.find(
            (event) =>
              event.type === "GATE_TRANSITION" &&
              (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
                `${hashS}:${seqS}`
          ),
        20000
      ) +
      bench(
        () =>
          void table.find(
            (event) =>
              event.type === "TRACKING_ASSESSMENT" &&
              (event as { payload: { seq?: number } }).payload.seq === seqS
          ),
        20000
      ) +
      bench(() => {
        let status = "RUNNING";
        for (const event of table) {
          if (event.type === "GATE_TRANSITION")
            status = (event as { payload: { to: string } }).payload.to;
          else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
          else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
          else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED")
            status = "RUNNING";
        }
        void status;
      }, 20000) +
      bench(() => void [...table], 20000);
    scanCosts.push(oneScans);
    console.log(
      `  E=${size}: full gate=${(one * 1e3).toFixed(1)}us | 5 scans+copy=${(oneScans * 1e3).toFixed(2)}us (${((oneScans / one) * 100).toFixed(1)}% of gate)`
    );
  }
  const slopeNsPerEvent =
    ((gateCosts[2]! - gateCosts[1]!) / (sizes[2]! - sizes[1]!)) * 1e6;
  const fixedFloorUs = (gateCosts[1]! - (slopeNsPerEvent / 1e6) * sizes[1]!) * 1e3;
  const perRunMs = (E: number): number =>
    (5 * (fixedFloorUs + (slopeNsPerEvent * E) / 1e3)) / 1e3;
  const crossing = (lineMs: number): number =>
    ((lineMs * 1e3) / 5 - fixedFloorUs) / (slopeNsPerEvent / 1e3);
  console.log(
    `  fit: fixed floor ~${fixedFloorUs.toFixed(1)}us/gate + ~${slopeNsPerEvent.toFixed(2)}ns/event (from E=410..4100)`
  );
  console.log(
    `  crossing E for 10ms/run: ~${Math.round(crossing(10)).toLocaleString()} events (~${(Math.log10(crossing(10) / 41)).toFixed(1)} orders above production E=41)`
  );
  console.log(
    `  crossing E for 50ms/run: ~${Math.round(crossing(50)).toLocaleString()} events (~${(Math.log10(crossing(50) / 41)).toFixed(1)} orders above production E=41)`
  );
  console.log(
    `  sanity: perRun(41)=${perRunMs(41).toFixed(3)}ms perRun(4100)=${perRunMs(4100).toFixed(3)}ms`
  );
  void scanCosts;
}

void mulberry32(0xe14a02);

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
