MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 14 / R14-C：离线路由切片第十四遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 14 / R14-C（Round 1–13 同区第十四遍，叠在
  已落地的 S7-C 之上）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `76e31da`（R13-J 空枚举合入 +
  Round 13 收口 + R14-C 派单后的独占分支最新 tip，与派单预期一致；
  `git log -1 -- src/routing/` 显示切片最后一次生产改动**仍为 `183df9b`
  「perf(routing): land S7-C support-only eta」**，其后基线区间仅文档更新。
  `git diff 183df9b..HEAD --` 对 9 个切片文件为**空**（stat 输出为空、exit 0），
  本轮全部测量在该形态之上，未回退未重写。R14-A / R14-B 在飞，未等待、
  未触碰其分支与文件）
- **分支:** `cursor/r14-c-offline-routing-fourteenth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十四组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第十四遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C/R12-C/R13-C 的全部无号
关闭探针）后，本轮找到**四个从未点名的微观站点**与**一处需要重锚定的
池算术**，全部以关闭探针（不铸号）收口——没有任何一个达到接受门槛：

- **SWORD（S6-C 累加 switch 的 case 标签次序置换，关闭探针，不铸
  号）**——从未有 ID 点名过落地 switch 内 case 标签的**排列次序**这一
  位置（S6-C 是分派本体、S6-C-1/2 是子集特化剂量响应、S6-C-3 是 ai 滚
  动混合、S6-C-4 是 bi 循环展开——均非标签次序）。case 体逐字节相同、
  按支撑长度精确分派，故任意排列结构性逐位等价（仍过 53 例三道电池全
  绿）。算术上界：3,586,400 次分派/报告 × ±1 ns = **±3.6 ms**，亚带
  ~10×；2..5 稠密范围编译为 V8 跳转表，标签次序不被读取。实测有一个
  诚实的方法论插曲：**未做位置交替的交错决斗中六次读数同号
  （+2.3~+6.4 ms，置换侧「更快」）**——把每对 rep 内先跑哪一路改为
  逐 rep 交替后，三次运行 delta 变为 **+3.8 / −0.4 / −0.7 ms 符号翻
  转**，证明此前的同号是**位置偏置**（先跑的一路吃到更冷的分配器/GC
  状态），不是机制。双重关闭（跳转表零机制 + 交替后纯抖动 + 上界亚带）。
- **XF64（xtwx/xtwz 累加缓冲改 Float64Array，关闭探针，不铸号）**——
  S7-C-3 只点名过 irls **私有 eta/mu 缓冲**（不跨函数边界）；S4-C-2 是
  solve **内部**扁平表示。xtwx/xtwz 会**跨越 solveSymmetric 调用边界**
  ——这一站点从未点名。裁决为稳定灾难性负优化：solve 防御拷贝
  `a.map(row => [...row])` 对 PACKED_DOUBLE 普通数组走 spread 快路径
  **2.2–2.8 µs/次（60×60）**，对 Float64Array 行走迭代器协议
  **68.6–69.7 µs/次**，×8,966 解 = **19.4–24.7 vs 615.0–625.3 ms/
  报告（慢 ~25–30×，九次读数全同向）**；累加站点自身继承 S7-C-3 的
  PACKED_DOUBLE 平价（零效应）；另有导出符号 `solveSymmetric` 的
  `readonly number[][]` 签名摩擦。关闭。
- **IRLSDP（irls 死参数 `vectors`，关闭探针，不铸号）**——S7-C 落地
  把 eta 改为支撑直加后，`irls()` 的 `vectors` 形参在函数体内**零读
  取**（eta 走 supports、累加走 supports、w/z 走 rows[i].y）——从未
  有 ID 或探针点名过。池为零：201 次 irls 调用/报告只各付一次死实参
  传递（亚 ns 级）；vectors 数组本身无论如何都要为 on-prob/APC 构建，
  无下游可省。删参是零收益纯重构，非性能候选（本战役只落性能赢家），
  不立道不铸号，记录给表所有者作代码卫生备忘。
- **CMPH（draws 排序比较器闭包提升，关闭探针，不铸号）**——
  `values.sort((a, b) => a - b)` 的比较器在 59–61 次排序/报告处逐调用
  分配；整个排序/分位尾池本轮实测 **1.5–1.6 ms/报告**（TSORT 独立上界
  1.26–1.28 同证），任何比较器分配节省被池封顶，亚带 ~22×。算术关闭。
- **APC 池漂移重锚定（APCX/TIMER/FLOORX 探针组，不铸号、不重开任何
  ID、不做 CMB 重堆叠）**——本 VM 仪表化 APC 池读数 **53.0–55.6 ms**，
  稳定高于 R12-C/R13-C 的 44.8–47.0 带 ~15–20%（solve/irls/总量均在
  历史带内，漂移集中于 slice+dot 密集的 APC 循环，记为本 VM 微架构差
  异）。因池总量**漂出历史带**，按 R11-C 原始方法在本 VM 重锚定地板
  算术（这不是重跑已关 ID 的决斗，也不构建任何堆叠候选道）：
  TIMER 探针证明仪表开销仅 **1.1 ms**（performance.now() 45.0–45.8
  ns/call × 12,259 对）非漂移来源；APCX 无计时器独立道同证池量
  **51.6–57.0 ms**；FLOORX 以最廉价逐位合法形态（S8-C-2 型支撑升序
  off 和——仅作地板测量，S8-C-2/S10-C-2/S2-C-4/S4-C-4 全部维持关闭）
  实测本 VM 地板 **31.2–39.8 ms** ⇒ **组合天花板 = 池 − 地板 =
  13.6–21.9 ms（六次读数）< 35 ms**。结论：本 VM 上池的增量坐在
  **不可省的扫描+sigmoid 地板**里，可省份额反而比 R11-C 算术
  （30.8–32.0）更小——组合关闭在本 VM 上**更强而非更弱**，且与
  S8-C-2 历史决斗实测（22.2–23.6）同带互证。

除上述站点外，第十四遍逐行重读**零新发现**：其余每一个执行站点都落入
R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 / 结构交换负优化）。
三大池地板与组合天花板复核维持（§3）：solve 452.8–460.7 ms 为冻结
浮点序列本体（全部已点名机制关闭，本轮 SWORD/XF64 补上分派次序与
跨边界表示两个从未点名位置）；irls 自身 154.2–167.1 ms 可省项全闭
（本轮 IRLSDP 坐实死参零池）；APC 53.0–55.6 ms 漂移已重锚定收口。
小形状档整报告中位 **32.0–32.9 ms**，整条报告低于噪声带（R7-I 矩阵
义务履行）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11），live 面零文件改动。仓库变更仅本报告一个文件；无生产赢家故
未提交 `scripts/round14-r14c-equivalence-sim.ts`（败者/锚点仿真全文进
附录，遵守 R7-C..R13-C 先例）。

## 0. 范围与约束遵守

- 读序（强制）已履行：README → EXCLUSIONS.md（完整表，含 S13-B-1 与
  S12-C-1）→ round-14/PLAN.md → round-13/PLAN.md + R13-C.md（空枚举
  确认，无 S13-C-\*；五个无号关闭探针 PIVCSE/FINTAIL/REFZ/TSORT/DEG
  记名）→ R12-C.md（S12-C-1 淘汰 + FZERO/PIVSWAP/BASE/SAMP/MUCSE）→
  R11-C.md（空枚举确认，无 S11-C-\*；RID/CNT/AUGF/EXP/ALLOC/DMAP/
  COPY/SCAN）→ R10-C / R8-C / R7-C 按需 → 9 个切片源文件全量重读。
- 基线漂移检查：基线 `76e31da` 相对 R13-C 基线 `6f7bc60` 仅文档合入
  （R13 各区报告收口、Round 13 关闭、Round 14 PLAN 与派单）；
  `git log -1 -- src/routing/` 仍为 `183df9b`（S7-C 落地本身），
  `git diff 183df9b..HEAD --` 对 9 文件为空（stat 空输出、exit 0），
  落地形态原样在位。既有回归资产七连全绿且检查数逐一匹配锚定值（§5）。
- 调用图确认（派单强制项）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 消费（双归因两路在位）；
  `solveSymmetric` 仅被 `offline-logit.ts` 导入；`src/supervisor/`
  对 9 个切片文件的导入为**零**——live = R0 等价维持，R1 未接线，
  `src/supervisor/model-router.ts` 零字节未触。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S10-\*、S12-C-1 全系未重提、未立道、
  未重测；R13-C 五个无名微观（PIVCSE/FINTAIL/REFZ/TSORT/DEG）未重
  探未补铸；RID/CNT 归约重排反例原样引用；嵌套序 × 布局组合空间
  （S4-C-2 + S5-C-1..4 + S12-C-1）未重开（XF64 是**跨边界缓冲表示**
  站点，非 solve 内部表示，见 §4）；CMB 组合未重堆叠（FLOORX 是地板
  测量非堆叠候选，见 §3）；APC 组合成员 S8-C-2/S10-C-2/S2-C-4/
  S4-C-4 维持关闭。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动；X2-3 数值路径未触；单线程逐位审计基底不动。
- 环境：Node 22.22.2（VM 默认 22.14.0 低于 engines ≥22.19.0，nvm
  切换并前置 PATH，与 R1-C..R13-C 同处理）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`。

## 1. 重剖析：S7-C 后成本模型（本 VM 实测，未拷贝历轮数字）

测量器为**仪表化逐字复刻管线**（与 183df9b 生产 irls/solve/on-prob/
APC 逐语句相同，仅在调用边界加 performance.now() 与计数器，浮点操作/
操作数/顺序零改动），其忠实性先经 **53 例全形状电池 ×{仪表道,
纯净落地道, 纯净置换道} vs 生产 `fitLogitAdditive` 导入逐位全绿**
（Object.is 含符号零）再采信读数；生产中位数由**未仪表化生产导入**
直接测得。perf 夹具与 R10-C..R13-C 完全一致（fixtureRng(0xbeef)，
4 场景 × 6 模型 × 8 项目，rows=400，passRate=0.6，bootstrap=200,
seed=20260818 → p=60，201 拟合，8,966 次 IRLS 迭代）。

生产整报告中位（每次独立进程 7 测量取中位，三次正典运行）：**666.2 /
689.3 / 668.0 ms**（三次前置彩排 679.3 / 665.5 / 666.4 与三次中间
彩排 661.7 / 665.4 / 660.3 同带；同 VM 独立旁证：r7c 回归资产锚点
**657.6 ms = 1.13× vs 冻结 S6-C ctl**）——与派单已知带 663.4–674.6
重叠（本 VM 读数 657.6–689.3，带内 ~1–2% 漂移，预期之内）。

仪表化池表（perf 形状，每次运行内 3 次仪表化取中位；三次正典运行
读数并列）：

| 池 | run A | run B | run C | 占比 | 站点计数（精确） |
| --- | --- | --- | --- | --- | --- |
| 总计 | 686.4 | 677.0 | 674.5 | 100% | fits=201 |
| `solveSymmetric` | **460.7** | **454.5** | **452.8** | **67.1%** | 8,966 次调用 |
| `irls` 自身（扣 solve） | **155.2** | **154.3** | **155.1** | **22.6–23.0%** | 8,966 迭代 / 3,586,400 行访问 / 1,249,652 sigmoid |
| APC 全站点 | 54.6 | 54.7 | 53.1 | 7.9–8.1% | 277,606 活跃对 / 12,259 次调用 |
| `onProbabilitiesFor` | 7.1 | 7.0 | 7.1 | 1.0–1.1% | 80,400 sigmoid+dot |
| bootstrap 采样 | 1.8 | 1.7 | 1.6 | 0.2–0.3% | —（SAMP 池 R12-C 已封顶） |
| 排序/分位尾 | 1.5 | 1.5 | 1.5 | 0.2% | 12,200 draws-map ops |
| 基行投影 | 0.009 | 0.006 | 0.005 | ~0% | 每报告 1 次（R12-C BASE 已封顶） |
| 编排余量（build/supports/keys） | 5.5 | 3.3 | 3.2 | 0.5–0.8% | — |

除 solve / irls / APC 外每池整池清零也低于 ±35 ms 带（R9-C..R13-C
同结论，逐池复测未漂移）；solve 与 irls 为落地线唯二宿主。**APC 池
本 VM 读数 53.1–54.7（另三次中间彩排 52.8–55.6）高于 R12-C/R13-C 的
44.8–47.0 历史带 ~15–20%**，而 solve/irls/总量均在历史带内——漂移
集中于 slice 分配 + 全长 dot 密集的 APC 循环，三重探针定位后在 §3
重锚定收口（仪表开销仅 1.1 ms 非因；独立无计时器道同证；地板同比
放大更多，可省份额反而缩小）。小形状档（3 模型 × 3 项目 / 120 行，
bootstrap=200）整报告中位 **32.0 / 32.9 / 32.9 ms**——小配置态整条
报告低于噪声带，调用矩阵小态一侧整体收口（R7-I 矩阵义务履行）。

生产自确定性复核：3 seed × 各 2 次运行全字段 Object.is 全绿（含于
§5 的 8,747 项检查 × 3 次正典运行）。

## 2. 第十四组枚举（诚实结果：空；四个无号关闭探针 + 一处池算术重锚定）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过。** 从未点名的站点全部在下表关闭（无一铸号；探针数字
为本 VM 实测，出处为附录测量彩排三次正典运行 + 三次前置彩排）：

| 站点（从未点名） | 关闭方式 | 证据 |
| --- | --- | --- |
| SWORD：S6-C 累加 switch case 标签次序置换（5,4,3,2 ↔ 2,3,4,5） | 零机制（跳转表）+ 位置交替后纯抖动 + 算术上界亚带 | 结构性逐位等价（53 例 × 置换道电池全绿）；**未交替时六次同号 +2.3~+6.4 ms 被证为位置偏置**——逐 rep 交替先跑方后三次运行 delta **+3.8 / −0.4 / −0.7 ms 符号翻转**；上界 3,586,400 分派 × ±1 ns = ±3.6 ms 亚带 ~10× |
| XF64：xtwx/xtwz 累加缓冲改 Float64Array（跨 solve 边界） | 稳定灾难性负优化 + S7-C-3 机制平价 + 签名摩擦 | solve 防御拷贝 60×60：普通数组 spread **2.2–2.8 µs** vs Float64Array 行迭代器路径 **68.6–69.7 µs** → ×8,966 = **19.4–24.7 vs 615.0–625.3 ms/报告（慢 ~25–30×，九次全同向）**；累加站点继承 S7-C-3 PACKED_DOUBLE 平价（零效应）；`solveSymmetric` 公开 `readonly number[][]` 签名摩擦 |
| IRLSDP：`irls()` 死参数 `vectors` 删除 | 零池算术 | S7-C 后函数体零读取（eta 走 supports / 累加走 supports / w/z 走 rows[i].y）；201 次调用/报告 × 一次死实参传递 ≈ 亚 µs；vectors 无论如何要为 on-prob/APC 构建，无下游可省；纯重构非性能候选 |
| CMPH：draws 排序比较器闭包提升 | 池上界亚带 | 59–61 次排序/报告；整尾池本轮 **1.5–1.6 ms/报告**（R13-C TSORT 独立 1.26–1.28 同证），亚带 ~22× |
| APCX/TIMER/FLOORX：APC 池漂出历史带的重锚定（非候选、非重开、非堆叠） | 地板重锚定后组合天花板仍亚带 | 仪表池 53.0–55.6（历史带 44.8–47.0）；TIMER：now() 45.0–45.8 ns × 12,259 对 = **1.1 ms** 非因；APCX 无计时器道 **51.6–57.0** 同证；FLOORX 最廉价逐位合法形态地板 **31.2–39.8** ⇒ **天花板 13.6–21.9 ms（六次）< 35**；与 S8-C-2 历史决斗 22.2–23.6 同带互证 |

以上无一构成候选：SWORD 是零机制 + 亚带（其未交替同号读数是测量
方法论产物，位置偏置的具体演示留档以防将来误判）；XF64 是稳定负
优化（S12-C-1/S4-C-2 之后表示类的第三个负项证人，跨边界站点收口）；
IRLSDP 池为零；CMPH 池封顶；APC 重锚定证明漂移坐在地板里。**故第
十四组候选集为空，不铸 S14-C-1 及后续编号**，遵循 R11-C（RID/CNT/
AUGF/EXP/ALLOC/DMAP/COPY/SCAN）、R12-C（FZERO/PIVSWAP/BASE/SAMP/
MUCSE）与 R13-C（PIVCSE/FINTAIL/REFZ/TSORT/DEG）无号关闭先例。

**枚举收口：** 除上表外，第十四遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖；本轮
重读零新发现——posterior 的 80 次二分 × betacf 每报告一次为亚 ms
且 X1-3 冻结替换；prob-add 的 diagnose 双层 entries 过滤在整报告
亚 ms 池内且 S1-C-6/7 在位；estimateFingerprint 是 S1-C-4 本体）。
`offline-logit.ts` 与 `lin-alg.ts` 的全部剩余站点经本轮四个新位置
关闭后，四分类（重命名 / 亚带 / 逐位违例 / 结构交换负优化）继续
穷尽。本轮顺带核实的换名再提交拒列（不铸号，见 §4 细分）：按支撑
长度预分组行序（= CNT 跨键归约重排反例本体换址）；删除消元行循环
`if (factor === 0) continue`（−0 符号位反例：factor×负值 = −0，
`x - (−0)` 在 x=−0 处翻 +0；且节省份额在 R12-C FZERO 已封顶的行
循环控制类内）；per-key w 去重（S3-C-2）；eta/mu 行缓冲消除
（S9-C-3）；bootstrap 缓冲跨 draw 复用（S3-C-3）。

## 3. 三大池地板与组合出路复核

- **solve 池 452.8–460.7 ms**：消元主体 ≈ 6.45 亿次冻结顺序浮点
  乘减/报告。R6-C §2.5 七类分解 + S10-C-3（对称镜像）+ S12-C-1
  （嵌套序交换，稳定 ~3.6× 负优化）+ R11-C COPY（防御拷贝契约
  封闭）+ R12-C FZERO（稀疏跳跃无池）/PIVSWAP（交换恰 0 但搜索
  冻结）+ S5-C-6（搜索 ~10 上界）+ R13-C PIVCSE（跨环 CSE 等量
  交换）/FINTAIL（尾检查 1.07–1.10 封顶）之后，本轮 XF64 关闭
  「跨边界输入表示」这一最后一个从未点名的表示类位置（solve 内部
  表示是 S4-C-2，输入表示从未点名——实测负 ~30×）。触碰地板本体
  的机制只剩 SIMD/线程（S9-C-1/2 + 不变量禁止）与数值路径改道
  （X2-3 冻结）。
- **irls 自身池 154.2–167.1 ms**：可省项全部已关闭（S2-C/S6-C/S7-C
  落地本体之上：S10-C-1 决斗带内、S9-C-3 符号翻转、S6-C-6 上界
  2–4、S5-C-7 无正决斗、S9-C-4、S4-C-5/6、S2-C-3、S8-C-3、R11-C
  RID/CNT 归约重排反例、EXP 16–21 亚带、ALLOC 2–3.5 封顶、R12-C
  MUCSE 纯抖动）。本轮 SWORD 关闭分派标签次序（跳转表零机制 +
  交替后抖动）、IRLSDP 坐实死参零池——irls 侧从未点名位置清零。
- **APC 池 53.0–55.6 ms（本 VM，漂出 44.8–47.0 历史带 ~15–20%）**：
  本轮完成漂移归因与重锚定三部曲——（i）TIMER：12,259 个计时器对
  仅 1.1 ms，仪表非因；（ii）APCX：无计时器独立道 51.6–57.0 同证
  池量为真；（iii）FLOORX：按 R11-C 原始地板方法在本 VM 直接实测
  最廉价逐位合法形态（S8-C-2 型支撑升序 off 和，**仅作地板测量**，
  不构成候选道、不重开 S8-C-2/S10-C-2/S2-C-4/S4-C-4、不做派单明令
  禁止的 CMB 重堆叠）得地板 31.2–39.8 ms ⇒ **组合天花板 = 池 −
  地板 = 13.6–21.9 ms（六次读数全部 < 35）**。漂移坐在扫描+
  sigmoid+支撑和的不可省地板内；可省份额（slice 分配 + 全长 dot
  的差额）与 S8-C-2 历史决斗实测 22.2–23.6 同带——组合出路在本 VM
  以直接测量关闭，比 R11-C 的分项算术（池 − 地板 ≈ 30.8–32.0）
  更紧。R13-C REFZ 的参考水平份额（5.0–5.5 µs）不受漂移影响。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测 +8.8~+30.5 带级
  摆幅带内），维持关闭。本轮五组探针无一是正项（SWORD 抖动、XF64
  负项、IRLSDP 零、CMPH 封顶、FLOORX 是地板不是候选），无新成员
  可入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建面
开放 / X2-3 数值路径解冻 / 单线程审计基底变更——均不预期）；
（b）现实规模位移 ≥1 个数量级使亚带池放大越带；或（c）指出
「重命名 / 亚带 / 逐位违例 / 结构交换负优化」四分类之外的第五类
成本——十四遍枚举未再发现。APC 池若在后续 VM 上继续单调漂高，
建议表所有者要求 C 区子代理按本轮 FLOORX 方法随池重锚地板（天花板
是池与地板之差，池单独漂高不构成重开条件）。

## 4. 重命名检查（派单强制项）

- **SWORD vs S6-C / S6-C-1..4**：S6-C 是「按支撑大小直线化分派」的
  存在本身；S6-C-1/2 是**子集特化**（仅 s=4..5 / 仅 s=5 的剂量
  响应）；S6-C-3 是 ai 滚动 + bi 直线化混合形态；S6-C-4 是 bi 循环
  **展开因子**。SWORD 只问「落地 switch 内 case 标签的排列次序」——
  分派结构、case 体、分派键全部不动。结构位置（标签序）此前无任何
  ID 或探针覆盖，非重命名；以跳转表机制 + 交替决斗抖动 + 3.6 ms
  算术上界关闭故不铸号。
- **XF64 vs S7-C-3 / S4-C-2 / S12-C-1**：S7-C-3 是 irls **私有**
  eta/mu 缓冲的类型替换（不跨函数边界，零效应裁决）；S4-C-2 是
  solveSymmetric **内部工作副本**的扁平表示；S12-C-1 是消元嵌套序。
  XF64 是**跨 solve 调用边界的输入缓冲**表示——防御拷贝的 spread
  语义在边界上被表示选择改变（快路径 vs 迭代器协议），此站点从未
  点名，非重命名；以 ~30× 稳定负优化关闭故不铸号。表示类维度
  （私有缓冲 S7-C-3 / 内部副本 S4-C-2 / 跨边界输入 XF64）至此三面
  收口。
- **IRLSDP vs S8-C-3 / S9-A-1 族**：S8-C-3 是死**代码**（恒真有限性
  检查）的消除与其 0.40 ms 实测；IRLSDP 是死**形参**——从未有条目
  点名。池为零（非亚带——是恰好为零），按「零收益纯重构不立道」
  纪律关闭，不铸号。
- **CMPH vs TSORT / X4-3 / S1-C 尾部族**：TSORT（R13-C 无号）是排序
  **实现替换**（typed-array sort，−0 反例）；X4-3 是异切片
  quickselect。CMPH 是比较器**闭包分配位置**——从未点名；以整尾池
  1.5–1.6 ms 封顶关闭，不铸号。
- **APCX/TIMER/FLOORX vs S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 / CMB
  禁令**：三探针**不重跑任何已关 ID 的决斗、不构建堆叠候选、不产出
  可落地形态**——FLOORX 的 S8-C-2 型循环只为测出「任何完美堆叠都
  无法低于的地板」，其输出仅用于天花板差值算术（R11-C 建立该方法，
  R12-C/R13-C 在池未漂移时按规免测，本轮池漂出带故按规重测）。
  四个成员 ID 与 CMB 禁令全部维持原状。
- **换名再提交拒列（本轮核实，均不铸号）**：按支撑长度预分组/重排
  行序 = CNT（跨键归约重排 5,087/10,000 反例）换址；删除
  `factor === 0` 既有分支 = −0 符号位逐位违例（0 × 负值 = −0，
  减 −0 翻符号零）+ FZERO 已封顶控制类；per-key w / per-(key,y) z
  去重 = S3-C-2；eta/mu 行缓冲消除 = S9-C-3；采样缓冲跨 draw 复用 =
  S3-C-3；irls 收敛检查重排 = S8-C-3 辖区。
- **R13-C 五个无名微观零重提**：PIVCSE / FINTAIL / REFZ / TSORT /
  DEG 未重探、未补铸、未换址重提（CMPH 与 TSORT 的区分见上）。
  S10-C-1..3 / S12-C-1 / RID / CNT 零重提；irls/APC 侧组合分析仅
  引用历轮已有实测数字加本轮 FLOORX 直测。

## 5. 测试与验证

环境：Node 22.22.2（nvm + PATH 前置；VM 默认低于 engines）、
pnpm 10.17.1。生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录）
npx tsx /tmp/r14c-anchor.mts                # ✓ 8,747 项逐位 × 3 次正典运行全绿
                                            #   （53 例 ×{仪表道,纯净落地道,纯净置换道} 忠实性电池
                                            #    + 3 seed × 2 自确定性 + perf 夹具逐位
                                            #    + XF64 spread 内容同一性检查）
                                            #   另有 3 次前置彩排（同脚本减 APCX/TIMER/FLOORX、
                                            #   SWORD 未交替版）全绿，其 SWORD 读数作位置偏置证据留档

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 657.6 ms（1.13× vs 冻结 S6-C ctl），与 §1
# 生产中位（666.2–689.3）同带。
```

代表性一次测量彩排全量输出（正典 run A；run B/C 数字见 §1 池表与 §2）：

```text
scenario 1 (three-lane bitwise fidelity, 53 cases x {instrumented, plain-landed, plain-permuted-switch} vs production fitLogitAdditive import)
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)
  shape small (3 models x 3 projects, 120 rows): production median 32.0 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 666.2 ms/report (7 reps)
instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):
  total                 686.4 ms
  solveSymmetric        460.7 ms  (67.1%)  [8966 calls]
  irls minus solve      155.2 ms  (22.6%)  [8966 iterations, 3586400 row visits, 1249652 irls sigmoids]
  onProbabilitiesFor    7.1 ms  (1.0%)  [80400 sigmoid+dot]
  APC (all sweeps)      54.6 ms  (8.0%)  [277606 active pairs, 12259 calls]
  bootstrap sampling    1.8 ms  (0.3%)
  sort/percentile tail  1.5 ms  (0.2%)  [12200 draws-map ops]
  base-row projection   0.009 ms  (once/report)
  outside pools (build/supports/keys/base copies) 5.5 ms  (0.8%)
  fits 201  [IRLSDP: irls calls/report carrying the dead vectors argument]
probe SWORD: landed-order copy 692.5 ms vs permuted-order copy 688.7 ms (delta +3.8 ms landed-minus-permuted; 6 position-alternated interleaved reps each after warmup)
  -> arithmetic bound: 3,586,400 switch dispatches/report; even +-1 ns/dispatch = +-3.6 ms, sub-band ~10x; dense case range 2..5 compiles to a V8 jump table where label order is unread
  landed reps:   678.0 667.6 696.5 707.5 681.0 692.5
  permuted reps: 667.3 666.7 671.8 703.1 702.3 688.7
probe XF64: solve defensive copy (60x60, map+spread) plain=2.8 us vs Float64Array-rows=69.1 us per copy -> x8,966 solves = 24.7 vs 620.0 ms/report (sink=7100.430)
  -> accumulation site inherits S7-C-3 PACKED_DOUBLE parity (zero effect); the typed-array boundary cost above is pure downside, plus the exported solveSymmetric readonly number[][] signature friction
probe IRLSDP: irls dead `vectors` parameter — 201 argument passes/report (fits count), arrays exist regardless for on-prob/APC -> zero pool; refactor-only, not a lane
probe CMPH: sort comparator closure hoist bounded by the whole sort/percentile tail pool (~1.6 ms/report, sub-band ~22x); arithmetic closure, no lane
probe TIMER: performance.now() 45.3 ns/call -> 12,259 APC timer pairs = 1.1 ms/report of instrumentation overhead inside the APC pool reading (sink=1.148)
probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) 0.265 ms/fit -> x201 fits = 53.3 ms/report (base-shape approximation; sink=7.309)
probe FLOORX: cheapest bitwise-legal sweep 0.184 ms/fit -> x201 fits = 36.9 ms/report floor; APC combination ceiling on THIS VM = 53.3 - 36.9 = 16.3 ms (floorSink=7.309)

ALL EQUIVALENCE CHECKS PASSED (8747 bitwise checks)
```

其余两次正典运行的探针行（关键数字）：

```text
run B: prod median 689.3; SWORD -0.4 (landed 676.2 vs perm 676.6); XF64 20.6 vs 625.3;
       TIMER 45.4 ns -> 1.1 ms; APCX 53.7; FLOORX 36.6 -> ceiling 17.1
run C: prod median 668.0; SWORD -0.7 (landed 668.4 vs perm 669.0); XF64 22.7 vs 622.1;
       TIMER 45.8 ns -> 1.1 ms; APCX 53.4; FLOORX 36.6 -> ceiling 20.4
（前置彩排三次（SWORD 未交替版，作位置偏置证据）：SWORD +3.1/+4.0/+2.3（先跑置换方，
 六次同号含中间版 +6.4/+3.6/+4.6）；APCX 51.6/53.6/53.4；FLOORX 31.2/31.7/39.8 ->
 ceiling 20.4/21.9/13.6；XF64 全部 ~30× 同向。）
```

## 6. 新增排除 ID：无

第十四组候选集为空。SWORD / XF64 / IRLSDP / CMPH 为关闭探针，
APCX/TIMER/FLOORX 为池算术重锚定，遵循 R11-C / R12-C / R13-C 先例
不铸号；重开条件均为现实规模/设计结构位移 ≥1 个数量级（不预期），
SWORD 与 IRLSDP 的重开条件为无（跳转表语义与死参零池是结构恒等式）。
APC 池漂移的处置建议已交表所有者（§3 末）。

## 附录：测量彩排脚本（完整，可复现）

```ts
/**
 * Round 14 / R14-C measurement re-anchor + fourteenth-group closure probes
 * (offline routing slice, fourteenth pass). NOT committed — the fourteenth-
 * group enumeration carries no candidate lanes that reach the acceptance bar;
 * full source goes into the R14-C report appendix (R7-C..R13-C precedent).
 *
 * Discipline (same as R11-C..R13-C):
 *   1. re-anchor the post-S7-C profile on THIS VM: production median
 *      ms/report on the perf shape (400 rows, 6 models x 8 projects,
 *      bootstrap=200) and the small shape, x-checked by an INSTRUMENTED
 *      verbatim pipeline copy (timers/counters only; every float op, value,
 *      and order identical — validated bitwise against the production
 *      import over the full 53-case battery before its numbers are used);
 *   2. derive the per-pool cost table (solve / irls-other / on-prob / APC /
 *      sampling / tail / base-copy) plus site counts;
 *   3. close the fourteenth-group never-named sites (no lanes reach the bar;
 *      all arithmetic/mechanism closures, no IDs minted):
 *        SWORD  landed S6-C accumulation switch case-order permutation
 *               (cases 5,4,3,2 vs 2,3,4,5) — copy-vs-copy duel expected to
 *               show jump-table parity; arithmetic bound 3,586,400
 *               dispatches/report x ~1ns = ~3.6 ms sub-band either way
 *        XF64   xtwx/xtwz accumulation buffers as Float64Array — S7-C-3
 *               PACKED_DOUBLE parity generalizes to the accumulation site;
 *               NEW negative mechanism measured here: solveSymmetric's
 *               defensive copy `[...row]` takes the iterator path on typed
 *               arrays (x8,966 solves/report), plus the exported
 *               solveSymmetric signature friction
 *        IRLSDP `vectors` parameter of irls() is dead since S7-C landed
 *               (eta comes from supports) — zero pool: 201 irls calls per
 *               report pay one dead argument pass; the arrays must exist
 *               anyway for onProbabilitiesFor/APC; removal is a pure
 *               refactor with no perf pool, not a candidate
 *        CMPH   sort comparator closure per values.sort((a,b)=>a-b) call —
 *               bounded by the whole sort/percentile tail pool (~1.6 ms),
 *               sub-band ~22x
 *   4. re-check production self-determinism across 3 seeds x 2 runs.
 *
 * Run: npx tsx /tmp/r14c-anchor.mts
 */

import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import type { AttributionReport, OfflineRow } from "/workspace/src/routing/offline-types.js";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.js";

/* --------------------------- shared constants --------------------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}

interface FitResult {
  readonly coefficients: readonly number[] | null;
}

type IrlsImpl = (
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
) => FitResult;

function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}

function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/* ------------- verbatim production pipeline building blocks -------------- *
 * Transcribed from src/routing/offline-logit.ts and src/routing/lin-alg.ts
 * at the 183df9b-landed state. The instrumented lane's ONLY additions are
 * STATS counter increments and performance.now() reads at call boundaries;
 * no float operation, operand, or ordering differs. The plain lanes (landed
 * switch order vs permuted switch order for the SWORD duel) carry no timers
 * at all. Every lane is validated bitwise against the production import
 * over the full 53-case battery before any number is reported. */

const STATS = {
  fits: 0,
  irlsMs: 0,
  irlsIterations: 0,
  solveCalls: 0,
  solveMs: 0,
  sigmoidIrls: 0,
  rowVisits: 0,
  onProbMs: 0,
  onProbSigmoids: 0,
  apcMs: 0,
  apcActivePairs: 0,
  apcCalls: 0,
  samplingMs: 0,
  drawsMapOps: 0,
  tailMs: 0,
  baseCopyMs: 0,
  totalMs: 0,
  reset(): void {
    this.fits = 0; this.irlsMs = 0; this.irlsIterations = 0; this.solveCalls = 0;
    this.solveMs = 0; this.sigmoidIrls = 0; this.rowVisits = 0; this.onProbMs = 0;
    this.onProbSigmoids = 0; this.apcMs = 0; this.apcActivePairs = 0;
    this.apcCalls = 0; this.samplingMs = 0; this.drawsMapOps = 0;
    this.tailMs = 0; this.baseCopyMs = 0; this.totalMs = 0;
  }
};

function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);

  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);

  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);

  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });

  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}

function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}

function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}

/* verbatim landed solveSymmetric (lin-alg.ts) — plain, no timers */
function solvePlain(a: readonly (readonly number[])[], b: readonly number[]): number[] | null {
  const n = b.length;
  if (a.length !== n || a.some((row) => row.length !== n)) {
    throw new DomainValidationError("solveSymmetric requires a square matrix matching b");
  }
  const m: number[][] = a.map((row) => [...row]);
  const x: number[] = [...b];
  const eps = 1e-12;

  for (let col = 0; col < n; col++) {
    let pivotRow = col;
    let pivotAbs = Math.abs(m[col]![col]!);
    for (let row = col + 1; row < n; row++) {
      const abs = Math.abs(m[row]![col]!);
      if (abs > pivotAbs) {
        pivotAbs = abs;
        pivotRow = row;
      }
    }
    if (pivotAbs < eps) return null;
    if (pivotRow !== col) {
      const tmp = m[col]!;
      m[col] = m[pivotRow]!;
      m[pivotRow] = tmp;
      const tb = x[col]!;
      x[col] = x[pivotRow]!;
      x[pivotRow] = tb;
    }
    const colArr = m[col]!;
    const pivot = colArr[col]!;
    const xCol = x[col]!;
    for (let row = col + 1; row < n; row++) {
      const rowArr = m[row]!;
      const factor = rowArr[col]! / pivot;
      if (factor === 0) continue;
      let k = col;
      const stop = n - 3;
      for (; k < stop; k += 4) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
        rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
        rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
      }
      for (; k < n; k++) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
      }
      x[row] = x[row]! - factor * xCol;
    }
  }

  const solution = new Array<number>(n).fill(0);
  for (let row = n - 1; row >= 0; row--) {
    const rowArr = m[row]!;
    let sum = x[row]!;
    for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
    const diag = rowArr[row]!;
    if (Math.abs(diag) < eps) return null;
    solution[row] = sum / diag;
  }
  for (const value of solution) {
    if (!Number.isFinite(value)) return null;
  }
  return solution;
}

/* Shared accumulation preamble/epilogue for the three irls lanes. The lanes
 * differ ONLY in (a) instrumentation and (b) the ORDER OF CASE LABELS in the
 * accumulation switch (SWORD probe). Case bodies are byte-identical; the
 * switch dispatches on the exact support length, so label order cannot
 * change which body runs — bitwise equality is structural, and is still
 * checked against the production import over the whole battery. */

function makeIrls(caseOrder: "landed" | "permuted", instrumented: boolean): IrlsImpl {
  return (design, rows, _vectors, supports, keys, keySpace, maxIter) => {
    const tIrls0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.fits += 1;
    const p = design.names.length;
    const n = rows.length;
    const eta = new Array<number>(n).fill(0);
    const mu = new Array<number>(n).fill(0);
    const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
    const xtwz: number[] = new Array<number>(p).fill(0);
    const stamp = new Int32Array(keySpace);
    const etaByKey = new Float64Array(keySpace);
    const muByKey = new Float64Array(keySpace);
    let beta = new Array<number>(p).fill(0);
    for (let iter = 0; iter < maxIter; iter++) {
      if (instrumented) {
        STATS.irlsIterations += 1;
        STATS.rowVisits += n;
      }
      const mark = iter + 1;
      for (let i = 0; i < n; i++) {
        const key = keys[i]!;
        if (stamp[key] !== mark) {
          stamp[key] = mark;
          if (instrumented) STATS.sigmoidIrls += 1;
          let value = 0;
          const active = supports[i]!;
          for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
          etaByKey[key] = value;
          muByKey[key] = sigmoid(value);
        }
        eta[i] = etaByKey[key]!;
        mu[i] = muByKey[key]!;
      }
      for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
      xtwz.fill(0);
      if (caseOrder === "landed") {
        for (let i = 0; i < n; i++) {
          const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
          const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
          const wz = w * z;
          const active = supports[i]!;
          switch (active.length) {
            case 5: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              const a4 = active[4]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
              xtwz[a4] = xtwz[a4]! + wz;
              const r4 = xtwx[a4]!;
              r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
              break;
            }
            case 4: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
              break;
            }
            case 3: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
              break;
            }
            case 2: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
              break;
            }
            default: {
              for (let ai = 0; ai < active.length; ai++) {
                const a = active[ai]!;
                xtwz[a] = xtwz[a]! + wz;
                const rowA = xtwx[a]!;
                for (let bi = 0; bi < active.length; bi++) {
                  const b = active[bi]!;
                  rowA[b] = rowA[b]! + w;
                }
              }
            }
          }
        }
      } else {
        for (let i = 0; i < n; i++) {
          const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
          const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
          const wz = w * z;
          const active = supports[i]!;
          switch (active.length) {
            case 2: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
              break;
            }
            case 3: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
              break;
            }
            case 4: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
              break;
            }
            case 5: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              const a4 = active[4]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
              xtwz[a4] = xtwz[a4]! + wz;
              const r4 = xtwx[a4]!;
              r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
              break;
            }
            default: {
              for (let ai = 0; ai < active.length; ai++) {
                const a = active[ai]!;
                xtwz[a] = xtwz[a]! + wz;
                const rowA = xtwx[a]!;
                for (let bi = 0; bi < active.length; bi++) {
                  const b = active[bi]!;
                  rowA[b] = rowA[b]! + w;
                }
              }
            }
          }
        }
      }
      for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
      let next: number[] | null;
      if (instrumented) {
        const tSolve0 = performance.now();
        next = solvePlain(xtwx, xtwz);
        STATS.solveMs += performance.now() - tSolve0;
        STATS.solveCalls += 1;
      } else {
        next = solvePlain(xtwx, xtwz);
      }
      if (next === null) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      const delta = next.map((value, index) => value - beta[index]!);
      const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
      beta = next;
      if (!beta.every(Number.isFinite)) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      if (l2 < TOL) break;
    }
    if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
    return { coefficients: beta };
  };
}

const irlsInstrumented = makeIrls("landed", true);
const irlsLanded = makeIrls("landed", false);
const irlsPermuted = makeIrls("permuted", false);

function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}

function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}

function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}

function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

/**
 * Full pipeline parameterized by irls lane. instrumented=true additionally
 * times onProb/APC/sampling/tail/baseCopy pools (timers only).
 */
function makeFit(irlsImpl: IrlsImpl, instrumented: boolean) {
  const onProb = (vectors: readonly number[][], coefficients: readonly number[]): number[] => {
    if (!instrumented) return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    const t0 = performance.now();
    const result = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    STATS.onProbMs += performance.now() - t0;
    STATS.onProbSigmoids += vectors.length;
    return result;
  };

  const apc = (
    design: Design,
    rows: readonly Row[],
    vectors: readonly number[][],
    coefficients: readonly number[],
    onProbabilities: readonly number[],
    column: string
  ): number => {
    const t0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.apcCalls += 1;
    let sum = 0;
    const columnIdx = design.columnIndex.get(column);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < rows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        if (instrumented) STATS.apcActivePairs += 1;
        const on = onProbabilities[i]!;
        const offVector = vectors[i]!.slice();
        offVector[columnIdx] = 0;
        const off = sigmoid(dot(coefficients, offVector));
        sum += on - off;
      }
    }
    const result = rows.length === 0 ? 0 : sum / rows.length;
    if (instrumented) STATS.apcMs += performance.now() - t0;
    return result;
  };

  const pushValue = (map: Map<string, number[]>, key: string, value: number): void => {
    if (instrumented) STATS.drawsMapOps += 1;
    const list = map.get(key);
    if (list === undefined) map.set(key, [value]);
    else list.push(value);
  };

  return function fit(
    rows: readonly OfflineRow[],
    options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
  ): AttributionReport {
    const tTotal0 = instrumented ? performance.now() : 0;
    const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
    const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
    const seed = options?.seed ?? SEED_DEFAULT;

    const tBase0 = instrumented ? performance.now() : 0;
    const baseRows: Row[] = rows.map((r) => ({
      scenarioId: r.scenarioId,
      modelVersion: r.modelVersion,
      projectId: r.projectId,
      y: r.y
    }));
    if (instrumented) STATS.baseCopyMs += performance.now() - tBase0;
    const effects: Array<{ name: string; point: number; lcb: number; ucb: number }> = [];

    if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
    }

    const design = buildDesign(baseRows);
    const vectors = baseRows.map((r) => design.build(r));
    const supports = computeSupports(vectors);
    const keys = canonicalRowKeys(baseRows);
    const fit = irlsImpl(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
    if (fit.coefficients === null) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
    }

    const onProbabilities = onProb(vectors, fit.coefficients);
    const pointEffects = new Map<string, number>();
    for (const name of design.names) {
      if (name === "intercept") continue;
      pointEffects.set(
        name,
        apc(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
      );
    }
    for (const ref of design.referenceLevels) {
      if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
        pointEffects.set(`${ref.factor}:${ref.name}`, 0);
      }
    }

    const random = rng(seed);
    const draws = new Map<string, number[]>();
    let successful = 0;
    for (let draw = 0; draw < bootstrapDraws; draw++) {
      const tSample0 = instrumented ? performance.now() : 0;
      const sample: Row[] = [];
      const sampleVectors: number[][] = [];
      const sampleSupports: number[][] = [];
      const sampleKeys: number[] = [];
      for (let i = 0; i < baseRows.length; i++) {
        const index = Math.floor(random() * baseRows.length);
        sample.push(baseRows[index]!);
        sampleVectors.push(vectors[index]!);
        sampleSupports.push(supports[index]!);
        sampleKeys.push(keys[index]!);
      }
      const collapsed = sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1);
      if (instrumented) STATS.samplingMs += performance.now() - tSample0;
      if (collapsed) continue;
      const bootFit = irlsImpl(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
      if (bootFit.coefficients === null) continue;
      successful += 1;
      const sampleOnProbabilities = onProb(sampleVectors, bootFit.coefficients);
      for (const [name] of pointEffects.entries()) {
        const value = apc(
          design,
          sample,
          sampleVectors,
          bootFit.coefficients,
          sampleOnProbabilities,
          name
        );
        pushValue(draws, name, value);
      }
    }

    const tTail0 = instrumented ? performance.now() : 0;
    for (const [name, point] of pointEffects.entries()) {
      const values = draws.get(name) ?? [];
      if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
        return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
      }
      values.sort((a, b) => a - b);
      effects.push({
        name,
        point,
        lcb: percentile(values, 0.025),
        ucb: percentile(values, 0.975)
      });
    }

    const n = baseRows.length;
    const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
    const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
    const muLcb = betaQuantileLcb(muPosterior, 0.05);
    const models = new Set(baseRows.map((r) => r.modelVersion)).size;
    const projects = new Set(baseRows.map((r) => r.projectId)).size;
    const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
    const containsZero = (e: { lcb: number; ucb: number }): boolean =>
      e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
    const interactionsFor = (prefix: string): Array<{ name: string; lcb: number; ucb: number; point: number }> =>
      effects.filter((e) => e.name.startsWith(`w:${prefix}`));

    let diagnosis: AttributionReport["diagnosis"] = "uncertain";
    const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
    if (
      effects.some(
        (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
      )
    ) {
      diagnosis = "model-problem";
    } else if (
      effects.some(
        (e) =>
          e.name.startsWith("v:") &&
          e.lcb < -ATTRIBUTION_EFFECT &&
          effects
            .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
            .every(containsZero)
      )
    ) {
      diagnosis = "project-problem";
    } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
      diagnosis = "interaction-only";
    } else if (scenarioHard) {
      diagnosis = "scenario-hard";
    }

    const report: AttributionReport = {
      estimator: "logit-additive",
      rowsUsed: baseRows.length,
      effects,
      diagnosis,
      reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
      writesActivePointer: false
    };
    if (instrumented) {
      STATS.tailMs += performance.now() - tTail0;
      STATS.totalMs += performance.now() - tTotal0;
    }
    return report;
  };
}

const fitInst = makeFit(irlsInstrumented, true);
const fitPlain = makeFit(irlsLanded, false);
const fitPerm = makeFit(irlsPermuted, false);

/* ------------------------------ harness ------------------------------ */

let checksPassed = 0;
let failures = 0;

function check(label: string, ok: boolean): void {
  if (ok) checksPassed++;
  else {
    failures++;
    process.stderr.write(`FAIL ${label}\n`);
  }
}

function compareReports(label: string, expected: AttributionReport, actual: AttributionReport): void {
  check(`${label}.estimator`, expected.estimator === actual.estimator);
  check(`${label}.rowsUsed`, expected.rowsUsed === actual.rowsUsed);
  check(`${label}.diagnosis`, expected.diagnosis === actual.diagnosis);
  check(`${label}.reason`, expected.reason === actual.reason);
  check(`${label}.writesActivePointer`, expected.writesActivePointer === actual.writesActivePointer);
  check(`${label}.effects.length`, expected.effects.length === actual.effects.length);
  if (expected.effects.length === actual.effects.length) {
    for (let i = 0; i < expected.effects.length; i++) {
      const e = expected.effects[i]!;
      const g = actual.effects[i]!;
      check(`${label}.effects[${i}].name`, e.name === g.name);
      check(`${label}.effects[${i}].point`, Object.is(e.point, g.point));
      check(`${label}.effects[${i}].lcb`, Object.is(e.lcb, g.lcb));
      check(`${label}.effects[${i}].ucb`, Object.is(e.ucb, g.ucb));
    }
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}

function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRow[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function allUniqueRows(r: () => number, count: number): OfflineRow[] {
  const rows: OfflineRow[] = [];
  for (let i = 0; i < count; i++) {
    rows.push({
      scenarioId: `s${i % 2}`,
      modelVersion: `m${i}`,
      projectId: `p${i}`,
      y: r() < 0.6 ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function batteryCases(): Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];

  for (let i = 0; i < 40; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, {
      scenarios: 2,
      models: 3,
      projects: 3,
      rows: 60,
      passRate: 0.55,
      pipeInModel: true,
    }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  cases.push({
    rows: allUniqueRows(r, 36),
    options: { bootstrap: 40, seed: 23 },
  });
  {
    const oneKey: OfflineRow[] = [];
    for (let i = 0; i < 30; i++) {
      oneKey.push({
        scenarioId: "s0",
        modelVersion: "m0",
        projectId: "p0",
        y: i % 3 === 0 ? 0 : 1,
        occurredAtMs: 1_000 + i,
      });
    }
    cases.push({ rows: oneKey, options: { bootstrap: 40, seed: 31 } });
  }
  cases.push({
    rows: [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1, occurredAtMs: 1_000 },
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 0, occurredAtMs: 1_001 },
    ],
    options: { bootstrap: 30, seed: 5 },
  });
  return cases;
}

const out = (line: string): void => {
  process.stdout.write(`${line}\n`);
};

function perfRows(): OfflineRow[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, {
    scenarios: 4,
    models: 6,
    projects: 8,
    rows: 400,
    passRate: 0.6,
  });
}

const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------- scenario 1: three-lane bitwise fidelity battery --------- */

function scenarioEquivalence(): void {
  const cases = batteryCases();
  for (const [index, testCase] of cases.entries()) {
    const expected = fitLogitAdditive(testCase.rows, testCase.options);
    compareReports(`R14C-inst[${index}]`, expected, fitInst(testCase.rows, testCase.options));
    compareReports(`R14C-plain[${index}]`, expected, fitPlain(testCase.rows, testCase.options));
    compareReports(`R14C-perm[${index}]`, expected, fitPerm(testCase.rows, testCase.options));
  }
  out(
    `scenario 1 (three-lane bitwise fidelity, ${cases.length} cases x {instrumented, plain-landed, plain-permuted-switch} ` +
      `vs production fitLogitAdditive import)`
  );
}

/* -------- scenario 2: production self-determinism across 3 seeds -------- */

function scenarioDeterminism(): void {
  const r = fixtureRng(0xd14c);
  const rows = randomRows(r, { scenarios: 3, models: 4, projects: 5, rows: 150, passRate: 0.6 });
  for (const seed of [101, 202, 303]) {
    const first = fitLogitAdditive(rows, { bootstrap: 60, seed });
    const second = fitLogitAdditive(rows, { bootstrap: 60, seed });
    compareReports(`R14C-det[seed=${seed}]`, first, second);
  }
  out("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)");
}

/* ------------- scenario 3: profile re-anchor + pool table ------------- */

function scenarioProfile(): void {
  const shapes: Array<[string, OfflineRow[]]> = [
    ["small (3 models x 3 projects, 120 rows)",
      randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 })],
    ["perf (6 models x 8 projects, 400 rows)", perfRows()],
  ];
  for (const [label, rows] of shapes) {
    const times: number[] = [];
    for (let rep = 0; rep < (label.startsWith("perf") ? 7 : 3); rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape ${label}: production median ${median(times).toFixed(1)} ms/report (${times.length} reps)`);
  }

  // Instrumented pool table on the perf shape (3 runs; medians reported).
  const rows = perfRows();
  const expected = fitLogitAdditive(rows, PERF_OPTIONS);
  const runs: Array<Record<string, number>> = [];
  for (let rep = 0; rep < 3; rep++) {
    STATS.reset();
    const actual = fitInst(rows, PERF_OPTIONS);
    if (rep === 0) compareReports("R14C-perf-fixture", expected, actual);
    runs.push({
      totalMs: STATS.totalMs, irlsMs: STATS.irlsMs, solveMs: STATS.solveMs,
      onProbMs: STATS.onProbMs, apcMs: STATS.apcMs, samplingMs: STATS.samplingMs,
      tailMs: STATS.tailMs, baseCopyMs: STATS.baseCopyMs,
      fits: STATS.fits, irlsIterations: STATS.irlsIterations, solveCalls: STATS.solveCalls,
      sigmoidIrls: STATS.sigmoidIrls, rowVisits: STATS.rowVisits,
      onProbSigmoids: STATS.onProbSigmoids, apcActivePairs: STATS.apcActivePairs,
      apcCalls: STATS.apcCalls, drawsMapOps: STATS.drawsMapOps,
    });
  }
  const med = (key: string): number => median(runs.map((r) => r[key]!));
  const total = med("totalMs");
  const solveMs = med("solveMs");
  const irlsOther = med("irlsMs") - solveMs;
  const rest = total - med("irlsMs") - med("onProbMs") - med("apcMs") - med("samplingMs") - med("tailMs");
  out(`instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):`);
  out(`  total                 ${total.toFixed(1)} ms`);
  out(`  solveSymmetric        ${solveMs.toFixed(1)} ms  (${(100 * solveMs / total).toFixed(1)}%)  [${med("solveCalls")} calls]`);
  out(`  irls minus solve      ${irlsOther.toFixed(1)} ms  (${(100 * irlsOther / total).toFixed(1)}%)  [${med("irlsIterations")} iterations, ${med("rowVisits")} row visits, ${med("sigmoidIrls")} irls sigmoids]`);
  out(`  onProbabilitiesFor    ${med("onProbMs").toFixed(1)} ms  (${(100 * med("onProbMs") / total).toFixed(1)}%)  [${med("onProbSigmoids")} sigmoid+dot]`);
  out(`  APC (all sweeps)      ${med("apcMs").toFixed(1)} ms  (${(100 * med("apcMs") / total).toFixed(1)}%)  [${med("apcActivePairs")} active pairs, ${med("apcCalls")} calls]`);
  out(`  bootstrap sampling    ${med("samplingMs").toFixed(1)} ms  (${(100 * med("samplingMs") / total).toFixed(1)}%)`);
  out(`  sort/percentile tail  ${med("tailMs").toFixed(1)} ms  (${(100 * med("tailMs") / total).toFixed(1)}%)  [${med("drawsMapOps")} draws-map ops]`);
  out(`  base-row projection   ${med("baseCopyMs").toFixed(3)} ms  (once/report)`);
  out(`  outside pools (build/supports/keys/base copies) ${rest.toFixed(1)} ms  (${(100 * rest / total).toFixed(1)}%)`);
  out(`  fits ${med("fits")}  [IRLSDP: irls calls/report carrying the dead vectors argument]`);
}

/* --------- scenario 4: fourteenth-group closure probes (no lanes) --------- */

function scenarioProbes(): void {
  // SWORD: landed switch case order (5,4,3,2,default) vs permuted (2,3,4,5,
  // default). Copy-vs-copy duel (S3-E-3 fairness discipline), interleaved
  // with position alternation after one warmup each; whole-report timings
  // on the perf shape.
  {
    const rows = perfRows();
    fitPlain(rows, PERF_OPTIONS); // warmup landed copy
    fitPerm(rows, PERF_OPTIONS); // warmup permuted copy
    const landedTimes: number[] = [];
    const permTimes: number[] = [];
    // Positional bias control: the lane that runs first ALTERNATES per rep.
    for (let rep = 0; rep < 6; rep++) {
      if (rep % 2 === 0) {
        let t0 = performance.now();
        fitPerm(rows, PERF_OPTIONS);
        permTimes.push(performance.now() - t0);
        t0 = performance.now();
        fitPlain(rows, PERF_OPTIONS);
        landedTimes.push(performance.now() - t0);
      } else {
        let t0 = performance.now();
        fitPlain(rows, PERF_OPTIONS);
        landedTimes.push(performance.now() - t0);
        t0 = performance.now();
        fitPerm(rows, PERF_OPTIONS);
        permTimes.push(performance.now() - t0);
      }
    }
    const landedMed = median(landedTimes);
    const permMed = median(permTimes);
    out(
      `probe SWORD: landed-order copy ${landedMed.toFixed(1)} ms vs permuted-order copy ${permMed.toFixed(1)} ms ` +
        `(delta ${(landedMed - permMed >= 0 ? "+" : "") + (landedMed - permMed).toFixed(1)} ms landed-minus-permuted; ` +
        `6 position-alternated interleaved reps each after warmup)`
    );
    out(
      `  -> arithmetic bound: 3,586,400 switch dispatches/report; even +-1 ns/dispatch = +-3.6 ms, ` +
        `sub-band ~10x; dense case range 2..5 compiles to a V8 jump table where label order is unread`
    );
    out(`  landed reps:   ${landedTimes.map((t) => t.toFixed(1)).join(" ")}`);
    out(`  permuted reps: ${permTimes.map((t) => t.toFixed(1)).join(" ")}`);
  }

  // XF64: xtwx rows as Float64Array. The accumulation site inherits S7-C-3's
  // PACKED_DOUBLE parity (number[] of doubles is already unboxed). The NEW
  // cost lives at the solve boundary: solveSymmetric's defensive copy
  // `a.map(row => [...row])` spreads each row 8,966 times per report; on a
  // plain PACKED_DOUBLE array the spread takes the fast path, on a typed
  // array it walks the iterator protocol.
  {
    const p = 60;
    const r = fixtureRng(0xf64a);
    const srcPlain: number[][] = [];
    for (let i = 0; i < p; i++) {
      const row: number[] = [];
      for (let j = 0; j < p; j++) row.push(r() * 8 - 4);
      srcPlain.push(row);
    }
    const srcTyped: Float64Array[] = srcPlain.map((row) => Float64Array.from(row));
    // Contents identity of the spread result (semantic sanity, not a lane):
    {
      const viaTyped = [...srcTyped[0]!];
      let same = viaTyped.length === srcPlain[0]!.length;
      for (let j = 0; same && j < viaTyped.length; j++) {
        same = Object.is(viaTyped[j], srcPlain[0]![j]);
      }
      check("xf64.spread-contents-identical", same);
    }
    const reps = 3000;
    let sink = 0;
    let t0 = performance.now();
    for (let rep = 0; rep < reps; rep++) {
      const m = srcPlain.map((row) => [...row]);
      sink += m[59]![59]!;
    }
    const plainMs = (performance.now() - t0) / reps;
    t0 = performance.now();
    for (let rep = 0; rep < reps; rep++) {
      const m = srcTyped.map((row) => [...row]);
      sink += m[59]![59]!;
    }
    const typedMs = (performance.now() - t0) / reps;
    out(
      `probe XF64: solve defensive copy (60x60, map+spread) plain=${(plainMs * 1e3).toFixed(1)} us vs ` +
        `Float64Array-rows=${(typedMs * 1e3).toFixed(1)} us per copy -> x8,966 solves = ` +
        `${(plainMs * 8966).toFixed(1)} vs ${(typedMs * 8966).toFixed(1)} ms/report (sink=${sink.toFixed(3)})`
    );
    out(
      `  -> accumulation site inherits S7-C-3 PACKED_DOUBLE parity (zero effect); the typed-array boundary ` +
        `cost above is pure downside, plus the exported solveSymmetric readonly number[][] signature friction`
    );
  }

  // IRLSDP: irls()'s `vectors` parameter is dead since S7-C landed (eta is a
  // support-only sum; the accumulation reads supports; w/z reads rows[i].y).
  // Pool: one extra argument pass per irls call — fits=201 per report (see
  // pool table), and the vectors arrays must exist regardless for
  // onProbabilitiesFor/APC. Removing a dead parameter is a refactor with a
  // zero perf pool, not a candidate. Nothing to time; the count is anchored
  // by the instrumented run.
  out(
    `probe IRLSDP: irls dead \`vectors\` parameter — 201 argument passes/report (fits count), ` +
      `arrays exist regardless for on-prob/APC -> zero pool; refactor-only, not a lane`
  );

  // CMPH: hoisting the `(a, b) => a - b` comparator out of the per-name sort
  // calls. 59-61 sorts/report; the WHOLE sort/percentile tail pool measures
  // ~1.6 ms (pool table above; R13-C TSORT standalone 1.26-1.28 ms), so any
  // comparator-allocation saving is bounded sub-band ~22x. Arithmetic
  // closure; no lane.
  out(
    `probe CMPH: sort comparator closure hoist bounded by the whole sort/percentile tail pool ` +
      `(~1.6 ms/report, sub-band ~22x); arithmetic closure, no lane`
  );

  // APCX + TIMER: decompose this VM's APC pool reading (53-56 ms vs the
  // R12-C/R13-C 44.8-47.0 band). (a) the instrumented lane pays 12,259
  // performance.now() PAIRS inside the APC pool — measure the pair cost;
  // (b) measure the APC sweep standalone with a timer-free inner loop
  // (verbatim body, base-fit coefficients; x201 fits extrapolates the pool
  // under the base-shape approximation the prior rounds' SCAN probe used).
  {
    // TIMER: cost of one performance.now() call on this VM.
    let sink = 0;
    const reps = 2_000_000;
    const t0 = performance.now();
    for (let i = 0; i < reps; i++) sink += performance.now();
    const perCallNs = ((performance.now() - t0) / reps) * 1e6;
    const perReportMs = (perCallNs * 2 * 12_259) / 1e6;
    out(
      `probe TIMER: performance.now() ${perCallNs.toFixed(1)} ns/call -> 12,259 APC timer pairs = ` +
        `${perReportMs.toFixed(1)} ms/report of instrumentation overhead inside the APC pool reading (sink=${(sink % 10).toFixed(3)})`
    );

    // APCX: standalone timer-free APC sweep at the base-fit shape.
    const rows = perfRows();
    const baseRows: Row[] = rows.map((r) => ({
      scenarioId: r.scenarioId,
      modelVersion: r.modelVersion,
      projectId: r.projectId,
      y: r.y
    }));
    const design = buildDesign(baseRows);
    const vectors = baseRows.map((r) => design.build(r));
    const supports = computeSupports(vectors);
    const keys = canonicalRowKeys(baseRows);
    const fit = irlsLanded(design, baseRows, vectors, supports, keys, baseRows.length, MAX_ITER_DEFAULT);
    const coefficients = fit.coefficients!;
    const onProbabilities = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    const names = design.names.filter((name) => name !== "intercept");
    let apcSink = 0;
    const sweepReps = 60;
    // warmup
    for (let rep = 0; rep < 5; rep++) {
      for (const name of names) {
        let sum = 0;
        const columnIdx = design.columnIndex.get(name);
        if (columnIdx !== undefined && columnIdx !== 0) {
          for (let i = 0; i < baseRows.length; i++) {
            if (vectors[i]![columnIdx] === 0) continue;
            const on = onProbabilities[i]!;
            const offVector = vectors[i]!.slice();
            offVector[columnIdx] = 0;
            const off = sigmoid(dot(coefficients, offVector));
            sum += on - off;
          }
        }
        apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
      }
    }
    const tSweep0 = performance.now();
    for (let rep = 0; rep < sweepReps; rep++) {
      for (const name of names) {
        let sum = 0;
        const columnIdx = design.columnIndex.get(name);
        if (columnIdx !== undefined && columnIdx !== 0) {
          for (let i = 0; i < baseRows.length; i++) {
            if (vectors[i]![columnIdx] === 0) continue;
            const on = onProbabilities[i]!;
            const offVector = vectors[i]!.slice();
            offVector[columnIdx] = 0;
            const off = sigmoid(dot(coefficients, offVector));
            sum += on - off;
          }
        }
        apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
      }
    }
    const perSweepMs = (performance.now() - tSweep0) / sweepReps;
    out(
      `probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) ` +
        `${perSweepMs.toFixed(3)} ms/fit -> x201 fits = ${(perSweepMs * 201).toFixed(1)} ms/report ` +
        `(base-shape approximation; sink=${apcSink.toFixed(3)})`
    );

    // FLOORX: this-VM re-anchor of the R11-C APC floor arithmetic. The
    // cheapest bitwise-legal sweep shape (the S8-C-2 support-ascending
    // off-sum skipping the contrast column — kept CLOSED; this is a floor
    // measurement, not a lane, and no stacked candidate is built): per
    // active pair pay the scan hit + a <=5-add support sum + sigmoid +
    // (on - off) accumulate; inactive rows pay the scan only. The gap
    // APCX - FLOORX is the maximum any perfect stack of the closed APC IDs
    // could ever save on this VM.
    let floorSink = 0;
    for (let rep = 0; rep < 5; rep++) {
      for (const name of names) {
        let sum = 0;
        const columnIdx = design.columnIndex.get(name);
        if (columnIdx !== undefined && columnIdx !== 0) {
          for (let i = 0; i < baseRows.length; i++) {
            if (vectors[i]![columnIdx] === 0) continue;
            const active = supports[i]!;
            let value = 0;
            for (let ai = 0; ai < active.length; ai++) {
              const j = active[ai]!;
              if (j === columnIdx) continue;
              value += coefficients[j]!;
            }
            sum += onProbabilities[i]! - sigmoid(value);
          }
        }
        floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
      }
    }
    const tFloor0 = performance.now();
    for (let rep = 0; rep < sweepReps; rep++) {
      for (const name of names) {
        let sum = 0;
        const columnIdx = design.columnIndex.get(name);
        if (columnIdx !== undefined && columnIdx !== 0) {
          for (let i = 0; i < baseRows.length; i++) {
            if (vectors[i]![columnIdx] === 0) continue;
            const active = supports[i]!;
            let value = 0;
            for (let ai = 0; ai < active.length; ai++) {
              const j = active[ai]!;
              if (j === columnIdx) continue;
              value += coefficients[j]!;
            }
            sum += onProbabilities[i]! - sigmoid(value);
          }
        }
        floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
      }
    }
    const perFloorMs = (performance.now() - tFloor0) / sweepReps;
    out(
      `probe FLOORX: cheapest bitwise-legal sweep ${perFloorMs.toFixed(3)} ms/fit -> x201 fits = ` +
        `${(perFloorMs * 201).toFixed(1)} ms/report floor; APC combination ceiling on THIS VM = ` +
        `${(perSweepMs * 201).toFixed(1)} - ${(perFloorMs * 201).toFixed(1)} = ` +
        `${((perSweepMs - perFloorMs) * 201).toFixed(1)} ms (floorSink=${floorSink.toFixed(3)})`
    );
  }
}

/* --------------------------------- main --------------------------------- */

scenarioEquivalence();
scenarioDeterminism();
scenarioProfile();
scenarioProbes();

if (failures > 0) {
  process.stderr.write(`${failures} CHECK(S) FAILED\n`);
  process.exitCode = 1;
} else {
  out(`\nALL EQUIVALENCE CHECKS PASSED (${checksPassed} bitwise checks)`);
}
```

MORE_OPTIMA=no
BRANCH=cursor/r14-c-offline-routing-fourteenth-pass-83a1
