MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 31 / R31-C：离线路由切片第三十一遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 31 / R31-C（Round 1–30 同区三十遍
  之后的第三十一遍，叠在已落地的 S7-C 之上）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `dabf551`
  （R30-J 空枚举合入 + Round 30 关轮 + 本 R31-C 派单）；工作期间独占
  分支 docs-only 快进**三次**：`dabf551` → `2fdbefb`（R31-A 空枚举
  合入 + R31-D 派单，增量仅触 docs/reports/sota-opt/ 下 3 文件
  PROGRESS / round-31 PLAN / R31-A.md，1,137 插入 10 删除）→
  `def155b`（R31-B 空枚举合入 + R31-E 派单，同目录 3 文件，786 插入
  10 删除）→ `3592410`（R31-D 空枚举合入 + R31-F 派单，同目录 3
  文件，688 插入 9 删除）。三笔增量对 `src/ test/ scripts/
  package.json pnpm-lock.yaml` 逐次直接复核均**零行**；按令逐次
  rebase 报告分支（快进式，零冲突）并在每个新 tip 上重验冻结：
  `git diff 183df9b..<tip> --` 对 9 个切片文件在 `dabf551` /
  `2fdbefb` / `def155b` / `3592410` 及提交前**五验均为 0 行**。
  `git log -1 -- src/routing/` 显示切片最后一次生产改动**仍为
  `183df9b`「perf(routing): land S7-C support-only eta」**。三条新
  收口姊妹轴（R31-A injected-callable plane 停放门 / R31-B 面格闭合
  审计六扇停放门 / R31-D R22-D 属地格）已核对并入非移植清单（§4），
  未等待、未触碰其余兄弟分支。全部锚点/轴/决斗测量在 `2fdbefb`
  工作树完成，r1c–r7c 回归资产在 `def155b` 工作树完成，聚焦套件 +
  `pnpm gate` 在 `2fdbefb` / `def155b` / `3592410` 三 tip 各跑一次
  ——快进增量 `src/` 逐字节零变化 ⇒ 测量形态与提交形态位级同一，
  全部有效。
- **分支:** `cursor/r31-c-offline-routing-thirty-first-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，与 R29-C/R30-C 普查同构 2,039 行；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第三十一组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第三十一遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R30-C 的全部无号关闭
探针与轴，含 R30-C 的 REPRX、R29-C 的 PAIRX、R28-C 的基片实例化、
R27-C 的 PROBX、R26-C 的 PRICEX、R25-C 的 AGEX、R24-C 的 OCCX、
R23-C 的 ORDX、R22-C 的 SEEDX、R21-C 的 YMIX、R20-C 的 SCALEX、
R19-C 的 FITQ）后，本轮**从未点名站点数为零**（连续第十三遍）——
每一个执行站点都能指到既有 ID、无号探针、零池备忘或池算术覆盖。
R26-C/R27-C 把公开输入面的全部数值坐标定价完毕，R28-C 关掉执行基片
坐标，R29-C 关掉进程工作负载组成坐标，R30-C 关掉输入对象表示坐标；
本轮新增的完备性轴落在切片代码与其原语操作之间的**名字解析层**上
——**切片的自由环境引用在执行时解析到哪些函数对象**：

- **ambient intrinsic-binding / intrinsic-resolution face（环境内建
  绑定/内建解析面）的一次性收口（新轴，非候选；face 名归 R31-C
  本轮；观察绰号 BINDX——plan-ban 用，不铸 ID、不写入排除表）**——
  切片九文件恰好消费十二个 Math 内建（exp/abs/max/min/sqrt/floor/
  ceil/log/pow/sin/imul/round）、`Number.isFinite` 与内建迭代协议
  （for-of / spread / Map 构造背后的 array/map/set 迭代器 `next`）。
  R1-C..R30-C 的**每一个生产读数**都在原生未打补丁的 V8 内建绑定下
  运行：这层可变的全局解析状态（含 V8 押在其原生性上的迭代器
  protector cell、Math.\* 全局载入的 map-stability 常量折叠、
  TurboFan 的 builtin call reduction）从未被剂量化。**精确普查**
  （计数透明包装器在 import 前安装，摘要门在剂量下仍逐位复现跨轮
  摘要）：一次 perf 形生产报告 = **23,472,380 次**环境内建调用
  （abs 16,952,940 / max 3,586,403 / exp 1,607,738 / isFinite
  1,075,920 / imul 160,000 / floor 80,122 / sqrt 8,966 / log 169 /
  ceil 122），small 形 = 1,447,578 次——热池对该层的通量是每报告
  两千三百万次量级，轴的物理规模成立。本轮四臂剂量（PRIST = 历史
  原生形态；WRAPM = 十二 Math 内建 + isFinite 在 import 前换成
  逐位透明的精确元数直通包装器；LATE = 原生 import + 3 个计时 rep
  后**进程中途**安装同一包装组（常量追踪失效/去优化悬崖剂量点）；
  PROTO = import 前包装 %ArrayIteratorPrototype%.next /
  %MapIteratorPrototype%.next / %SetIteratorPrototype%.next，
  使迭代器 protector cell 失效），**runner 逐字节同一**（臂仅由
  R31C_ARM 选择；全臂 tsx+src，基片坐标冻结），交错 3 轮 × 4 臂 =
  12 个独立进程：
  **位面**——12/12 进程 logit 六报告 IEEE-754 位型折叠 SHA-256
  **逐字节相同且与 R28-C/R29-C/R30-C 打印摘要
  `63e74db4…3893` 逐字节同值**（跨轮 + 跨基片 + 跨组成 + 跨表示 +
  跨绑定五重见证）；CAP 探针 sink 12/12 同值 1,951,200.6101761512
  且每进程内 `Object.is` 断言。**任何绑定剂量动不了任何输出位。**
  **热层**——perf 形 7-rep 中位：PRIST 658.373 / 657.993 /
  657.787（与主锚带 658.1–660.1 交叠——历史读数全部坐在 PRIST 格
  的直接确证）；WRAPM 680.835 / 677.844 / 681.814（同轮差 +22.46 /
  +19.85 / +24.03 ms）；LATE 补丁前段 658.376 / 656.088 / 653.372
  （= 原生带），补丁后段 843.432 / 846.627 / 849.668（**后−前 =
  +185.06 / +190.54 / +196.30 ms**，四个补丁后 rep 持续高位——
  常量追踪失效是持久性坍塌，非一次性 deopt 尖峰）；PROTO
  3,128.787 / 3,123.010 / 3,135.066（**+2,465–2,477 ms ≈
  4.75×**；small 形 74.2–74.9 vs 30.2–30.8 ≈ 2.4×）。轴响应
  **非平坦且巨大**（几十到几千 ms，真实规模非噪声）——但每一个
  非原生剂量点都是**代价**，方向全为正。
  **生产占位证明**——rg 全库普查：`src/` 内 Math.\* 赋值 **0 处**、
  内建原型 `next`/`Symbol.iterator` 补丁 **0 处**、`freeze(Math)`/
  `globalThis.Math` 触碰 **0 处** ⇒ 生产只可能铸 PRIST 臂，
  **该轴的最优点已被落地形态占据**，轴上不存在正向候选。
- **候选道 C1（capture-hoisting，切片内唯一可表达的反向手）定价并
  拒绝**——模块局部捕获 `const E = Math.exp` 等以跳过每调用环境
  查找。CAP 微探针（runner 本地 sigmoid 逐语句复刻，2M 调用 × 2
  遍 × 12 进程）：captured 10.40–10.72 vs live 13.12–13.59
  ns/call ⇒ 捕获红利 **2.7–2.9 ns/call**；× 精确普查 23.47M
  调用/报告 ⇒ **朴素天花板 63.4–68.1 ms/报告 > 35 ms 带**——
  微探针红利若在真实管线成立，该道就是赢家，故按规升级为真实
  规模决斗（诚实升级，不许天花板算术单方面杀道也不许微探针
  单方面立道）。**VERB/CAPT 决斗**：replica-verb =
  落地管线逐语句复刻（含 lin-alg `solveSymmetric` 逐语句复刻——
  普查中 72% 的环境调用住在该函数的主元扫描里），replica-capt =
  仅经机械替换生成的孪生（捕获头 + 引用形态 `Math.exp(` →
  `M_EXP(` 等，录得 diff 存档，无其他 token 变化）；runner
  逐字节同一（道由 R31C_LANE 选择），每进程先过 **1,956 项
  位级保真门**（34 例形状电池 + perf + small，全字段 `Object.is`
  对生产 import）再计时，跨轮摘要 8/8 复现。1 次烟测 + 交错 3
  轮：VERB 673.238 / 671.803 / 674.688，CAPT 671.826 / 675.173 /
  674.230 ms；**同轮差（VERB−CAPT）= +1.412 / −3.370 / +0.458
  ms——符号翻转，|Δ| ≤ 3.4 ms，离 ±35 ms 带 ≥10×**；small 形差
  −0.18/−0.53/−0.33 ms 同为噪声域。**物理解释**：TurboFan 在
  优化帧里把环境 `Math.abs`/`Math.exp` 载入折叠为常量（map-
  stability 依赖）并把调用还原为内建算子；捕获常量形经模块胞
  常量追踪还原为**同一机器操作**。微探针的 2.7–2.9 ns/call 是
  低层执行/探针形态工件，在全帧已优化的 657 ms 热池里**不
  存在**——真实价 = 0 ± 3.4 ms。**第二独立拒绝**：捕获形把
  绑定语义收窄到模块求值时刻（落地的活绑定形按 JS 语义追踪
  运行时补丁，LATE 臂展示的正是该追踪的代价面），在污染态下
  两形态对非透明补丁可观察发散——为 0 ± 3.4 ms 的原生态红利
  换语义收窄 + 改两个冻结文件，双重违例。**C1 拒绝，道关闭。**
  防护向反手（`Object.freeze(Math)`/protector 加固）住在 9 文件
  之外且是行为变更，超出 C 区落地范围，不构成切片内物。
- **APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠）**
  ——本轮四次读数（1 预演 + 3 正典）：APCX 无计时器独立道
  **58.9 / 60.7 / 61.8 / 60.4 ms**（正典 60.4–61.8，与 R30-C 带
  58.4–60.2 带缘相接），FLOORX 最廉价逐位合法形态地板 **33.0 /
  35.6 / 41.1 / 35.3 ms**（与 R30-C 带 31.7–39.5 交叠）⇒
  **组合天花板 = 25.9 / 25.0 / 20.7 / 25.1 ms < 35**——正典三
  读数带 20.7–25.1 完全落入 R30-C 正典带 18.8–26.6，组合关闭
  维持。**sink=7.309 与 floorSink=7.309 八读数全同，且与
  R14-C/R16-C..R30-C 打印值逐位同值**；每次运行内
  `Object.is(apcSink, floorSink)` 断言全绿。

除上述轴与天花板重测外，第三十一遍逐行重读**零新发现**：其余每一个
执行站点都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 /
结构交换负优化）+ R18-C 补充的 once-per-fault 拒绝类。生产中位与
地板复核见 §1/§3：本 VM 上生产中位 **658.1–660.1 ms/报告（四次运行
659.9 / 660.1 / 659.0 / 658.1；三次正典 658.1–660.1）**——与 R30-C
带 658.7–663.0 交叠（预演与 run 1/2 落带内，run 3 低 0.6 ms），按
R14-C 处置规则随读重锚为本轮带 658.1–660.1；同 VM 独立旁证：r7c
回归资产锚点 **production 661.1 ms = 1.12× vs 冻结 S6-C ctl
743.8**（R30-C 读数 658.6/742.1 = 1.13×，R29-C/R28-C 均 1.12×——
比率逐轮基本不动）；非切片回归或加速（切片对 `183df9b` 零 diff）。
小形状档整报告中位 **29.9–30.2 ms**（R30-C：30.1–30.2——交叠），
整条报告低于噪声带（R7-I 矩阵义务履行）。绑定轴 PRIST 臂中位
（657.787–658.373）与主锚带 658.1–660.1 交叠——双装具互证带与
R29-C/R30-C 同型。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态：posterior.ts `LcbKind = "normal" | "beta-quantile"`
两路本轮 rg 复核在位）与双归因（offline-logit vs offline-prob-add：
`writeAttributionPair` 两路调用本轮现场核对在位）全部保留（X0-11），
live 面零文件改动（`src/supervisor/` 对 9 文件导入为零，本轮 rg
复核）。仓库变更仅本报告一个文件；无生产赢家故未提交 `/tmp/r31c/`
六个临时文件（全文进附录 A，sha256 归档后已删除）。

## 0. 范围与约束遵守

- 读序（强制）已履行：round-31/PLAN.md（C 段 + 全部 plan-ban 轴；
  独占 tip `dabf551` 已含 R30-J LINKTOP 与 Round 30 关轮）→
  EXCLUSIONS.md（完整表，含全部 S\*-C-\* 行与 S12-C-1、S13-B-1、
  X1-3/X3-4/X3-5/X4-2 posterior/propensity/bandit 行）→
  PROGRESS.md → R30-C.md 全文（空枚举确认，无 S30-C-\*；REPRX 轴
  定义 + 「输入值轴域穷尽 + 执行基片坐标关闭 + 工作负载组成坐标
  关闭 + 输入表示坐标关闭」裁决域勘定——本轮轴因此落在名字解析
  坐标上，即「自由环境引用解析到哪些函数对象」，历轮从未剂量）→
  R29-C.md（PAIRX）→ R30-J.md 结论（LINKTOP，不重铸不移植）→
  9 个切片源文件全量重读 → `src/experiments/attribution-report.ts`
  消费图现场核对 → 首次 rebase 后补读 R31-A.md（injected-callable
  plane 停放门：独立物理对象证明失败，计时会重开 R28-F/R29-A/
  R30-A——本轮轴与其划界见 §4 第 2 条）与更新版 PLAN.md → 二次
  rebase 后补读更新版 PLAN.md 的 R31-B 六扇停放门名录 → 三次
  rebase 后补读更新版 PLAN.md 的 R31-D 属地归档段。
- 基线漂移检查：本轮测量期起点 = 分支创建点 = `dabf551`；提交前
  复核发现三次 docs-only 快进（`2fdbefb` R31-A 合入 + R31-D 派单；
  `def155b` R31-B 合入 + R31-E 派单；`3592410` R31-D 合入 + R31-F
  派单），按派单规程逐次 rebase 报告分支通过至 `3592410` 并重验
  冻结。冻结主张五次核验：`git diff 183df9b..<tip> --` 对 9 个
  切片文件为 **0 行**——分支创建时（`dabf551`）、每次 rebase 后
  （`2fdbefb` / `def155b` / `3592410`）、提交前各一次；三次快进
  仅触 docs/reports/sota-opt/ 下文件（`git diff --stat` +
  `git diff <a>..<b> -- src test scripts package.json
  pnpm-lock.yaml | wc -l` = 0 逐次核对），测量在字节相同的切片上
  完成，全部有效。R31-A / R31-B / R31-D 三条姊妹轴已入 §4 非移植
  清单并完成划界。
- 硬性不变量逐条复核：双 LCB 在位（posterior.ts L22 `LcbKind =
  "normal" | "beta-quantile"`，L69–72 双路选择）；双归因在位
  （attribution-report.ts L18–19 `logit: fitLogitAdditive(rows),
  probAdd: fitProbabilityAdditive(rows)`）；live = R0 等价
  （`src/supervisor/` 对 9 切片文件 rg 导入为零，R1 未接线）；
  推广 proposal-first、无 Outcome-supported 主张（Checkpoint
  F-PROD 开放，ADR-005）；无阈值改动；无测试改动；CAS/凭证/数据面
  合同/公开签名零触碰；分析未变更任何 in-flight run（全部测量在
  /tmp 临时脚本与只读 import 上完成）；S7-C 已落地、未起平行实现
  （本轮全部复刻仅存于 /tmp 决斗装具，见 §2，未提交）。
- 禁止重铸/移植清单遵守：REPRX / PAIRX / PROBX / PRICEX / AGEX /
  FITQ / SCALEX / YMIX / SEEDX / ORDX / OCCX / LINKTOP 零触碰；
  R28-C 基片实例化与 R27-H execution-substrate face 未移植（本轮
  全臂锁死单一基片 tsx+src，基片坐标恒定不剂量）；R30-I 时间段
  拓扑面未移植（无 spawn-to-exit 分段测量）；R30-J LINKTOP 未
  移植（无文件系统指称绑定剂量）；R31-A/R31-B/R31-D 停放门与
  属地格未开（划界见 §4）。X\* / S1-\* … S30-\* 全部未重开，
  S12-C-1 重开条件未触发。
- EXCLUSIONS.md 未添加任何行（空枚举轴绰号 BINDX 按令不入表，
  parent 拥有 plan-ban）。

## 1. 生产中位与锚点复核（本 VM 实测，未拷贝历轮数字）

测量装具与 R30-C 逐字节同协议（replica 保真电池 + 自决定性 + 中位
重锚 + APCX/FLOORX，全文见附录 A.1）；Node v22.22.2，tsx + src
直连（R28-C S 臂 = 历史正典基片）。1 次预演 + 3 次正典独立进程，
全部在 `2fdbefb` 工作树完成：

| 运行 | perf 中位 (ms) | small 中位 (ms) | APCX (ms) | FLOORX (ms) | 组合天花板 (ms) | sink / floorSink | 检查 |
|---|---|---|---|---|---|---|---|
| 预演 | 659.9 | 30.2 | 58.9 | 33.0 | 25.9 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 1 | 660.1 | 30.2 | 60.7 | 35.6 | 25.0 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 2 | 659.0 | 29.9 | 61.8 | 41.1 | 20.7 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 3 | 658.1 | 29.9 | 60.4 | 35.3 | 25.1 | 7.309 / 7.309 | 2,768 ✓ |

perf 逐 rep（7 rep/运行）：预演 659.8 664.8 659.9 666.6 659.5
656.5 662.0；正典 1 661.4 665.5 659.2 659.0 657.3 660.1 660.4；
正典 2 659.0 658.5 656.2 658.0 668.4 662.0 660.9；正典 3 658.1
658.9 658.4 656.1 655.4 657.3 663.3。

- **生产中位带（本轮）：658.1–660.1 ms**（四读数 658.1–660.1，
  正典同带）。与 R30-C 带 658.7–663.0 交叠 658.7–660.1；正典 3
  的 658.1 低于 R30-C 带缘 0.6 ms（同量级于臂内散布），按 R14-C
  处置规则**随读重锚为本轮带 658.1–660.1**。同 VM 独立旁证：r7c
  回归资产（§5）打印 production 661.1 ms = 1.12× vs 冻结 S6-C
  ctl 743.8——比率与 R28-C/R29-C（1.12×）/R30-C（1.13×）逐轮
  基本不动。非切片回归或加速：切片对 `183df9b` 零 diff，装具与
  协议逐字节同 R30-C。
- **small 带 29.9–30.2 ms**（R30-C 30.1–30.2、R29-C 29.9–30.1，
  三轮带互相交叠）；整条 small 报告低于 ±35 ms 噪声带（R7-I
  矩阵义务：该档只作确定性与保真见证，不作候选判据）。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等（含
  replica 保真电池 34 例 + perf + small 对生产 import 的全字段
  断言），每运行 2,768 项检查全绿，四运行合计 11,072 项。
- 绑定轴 PRIST 臂（§2，独立装具、独立进程）中位 657.787–658.373
  与主锚带 658.1–660.1 带缘相接——双装具互证与 R29-C（L 臂 vs
  主锚 −1.4~−4.4 ms）/R30-C（CANON 臂 vs 主锚 +0.6~+2.6 ms）
  同型，装具项本身 ms 级。

## 2. 第三十一组枚举（诚实结果：空；零新站点 + 一条新轴一次性收口 + 候选道 C1 定价拒绝 + 一次按规天花板重测）

### 2.0 轴的物理对象与历史空白

九文件的全部原语操作经由三类**自由环境引用**抵达引擎：十二个
Math 内建（切片内 rg 普查逐点名：exp——offline-logit sigmoid 双
分支 + posterior 逆正则化 Beta 前缀；abs——lin-alg 主元扫描 ×2 +
回代守卫 + posterior betacf 六守卫；max——buildDesign dropLast +
irls 权重钳制 + offline-prob-add / propensity / posterior 钳制；
min——offline-prob-add / propensity；sqrt——irls 收敛范数 +
posterior sd；floor——bootstrap 重采样索引 + percentile + bandit；
ceil——percentile；log/pow/sin——posterior；imul——rng 双点；
round——shadow）、`Number.isFinite`（irls `beta.every` + lin-alg
终检——切片内恰两站点）、以及
内建迭代协议（for-of / spread / `new Map(...)` 背后的
%ArrayIteratorPrototype%.next 等）。这些引用不是参数、不是注入
缝、不是输入值——它们是**领域（realm）级的可变名字解析状态**，
V8 在其原生性上押了三层机器：全局载入的 map-stability 常量折叠、
builtin call reduction（把 `Math.abs(x)` 还原为浮点算子而非真实
调用）、迭代器 protector cell（原生迭代快速路径的全局开关）。
R1-C..R30-C 三十遍的每一个读数都在该状态的同一格（原生）上完成
——坐标存在、从未剂量，且有独立于历轮所有面的物理对象（protector
cell 与常量追踪失效没有任何「调用协议/别名/派发历史/表示」坐标
能表达，见 §4 划界）。

**通量普查（轴规模的精确证词，附录 A.3）**：计数直通包装器在
import 前安装（WRAPM 剂量点 + 计数器；摘要门在剂量下逐位复现
`63e74db4…3893` ⇒ 计数器透明），模块专属增量（夹具构造的
runner 本地消费已快照扣除）：

| 内建 | perf 形/报告 | small 形/报告 |
|---|---|---|
| Math.abs | 16,952,940 | 589,381 |
| Math.max | 3,586,403 | 461,163 |
| Math.exp | 1,607,738 | 197,978 |
| Number.isFinite | 1,075,920 | 122,976 |
| Math.imul | 160,000 | 48,000 |
| Math.floor | 80,122 | 24,034 |
| Math.sqrt | 8,966 | 3,843 |
| Math.log | 169 | 169 |
| Math.ceil | 122 | 34 |
| **合计** | **23,472,380** | **1,447,578** |

abs 占 72%（lin-alg 主元扫描：p≈60 时每 solve ~1,860 次 × ~9,100
solve/报告），max/exp/isFinite 为第二梯队。每报告两千三百万次
环境解析——若解析层有任何每调用价，规模上必然越过 ±35 ms 带；
轴不是 µs 域摆设，必须实测收口。

### 2.1 四臂剂量与三个响应面（附录 A.2）

runner 逐字节同一（臂由 `R31C_ARM` 选择），交错 3 轮 × 4 臂 =
12 个独立进程，全臂 tsx + /workspace/src（基片坐标冻结在 R28-C
S 臂）；夹具字节（0xbeef / 0x51a1 / 0x5eed）、发射序、options、
规范输入表示（REPRX 坐标不剂量）、工作负载组成（姊妹估计器零
调用，PAIRX 坐标不剂量）全部冻结，输入值零移动。分支创建 tip
（`dabf551`）上的先行预演给出同序臂排布与同值摘要后，正典 12
进程在 `2fdbefb` 完成并存档：

| 臂 | perf 中位 ×3 (ms) | 同轮差 vs PRIST (ms) | small 中位 ×3 (ms) |
|---|---|---|---|
| PRIST | 658.373 / 657.993 / 657.787 | — | 30.801 / 30.248 / 30.278 |
| WRAPM | 680.835 / 677.844 / 681.814 | +22.46 / +19.85 / +24.03 | 30.930 / 30.858 / 31.324 |
| LATE（全程） | 813.231 / 817.769 / 819.584 | +154.9 / +159.8 / +161.8 | 45.938 / 45.520 / 44.450 |
| PROTO | 3,128.787 / 3,123.010 / 3,135.066 | +2,470.4 / +2,465.0 / +2,477.3 | 74.879 / 74.239 / 74.235 |

LATE 臂分段（补丁装在 rep 2 与 rep 3 之间、计时器外）：前段中位
658.376 / 656.088 / 653.372（= 原生带——补丁前与 PRIST 无差，
进程内自对照），后段中位 843.432 / 846.627 / 849.668（**后−前
+185.06 / +190.54 / +196.30 ms**；四个后段 rep 全程高位不回落
——中途失效比编译期可见的外来绑定（WRAPM +20~24）贵 8 倍且
持久，V8 不为已失效的常量追踪假设重建同级代码）。

**位面（收口的先决条件）**：12/12 进程 logitDigest =
`63e74db4ac45eed0f2a219e159b5f2d65b9ea8d962408e66858c7b459dea3893`
——与 R28-C/R29-C/R30-C 打印摘要逐字节同值（同一折叠、同一载荷：
perf + small + 三个 0x5eed 形 + perf 复读）。包装器逐位透明 +
迭代器包装转发原语 ⇒ 绑定剂量动不了任何输出位；CAP 探针 sink
12/12 = 1,951,200.6101761512 且每进程内 `Object.is` 断言。

**判读**：三个非原生剂量点全部是代价（+20 到 +2,477 ms，真实
规模、方向一致、轮间稳定），原生格是轴上唯一最优点；rg 全库
普查（`src/` 内 `Math.[a-z]+ =` 赋值 0 处、`.next =` /
`Symbol.iterator] =` / `defineProperty(..Math|prototype)` 0 处、
`freeze(Math|globalThis.Math` 0 处）证明生产不可能离开原生格
——**最优点已被落地形态占据，轴上无正向候选**。轴的产出是
防御性刻度：若宿主嵌入污染领域，C 切片的账单是 +20（编译期
可见包装）/ +190（中途补丁）/ +2,470 ms（迭代协议污染），
且全部不动输出位。

### 2.2 候选道 C1：capture-hoisting 定价与双重拒绝（附录 A.4–A.6）

切片内唯一可表达的反向手：把环境引用捕获为模块局部
`const`，跳过（假设中的）每调用解析价。**CAP 微探针**（runner
本地 sigmoid 逐语句复刻，live vs captured 两形态交错 2M 调用 ×
2 遍，第二遍为稳态读数，12 进程）：captured 10.40–10.72 vs
live 13.12–13.59 ns/call ⇒ 红利 **2.7–2.9 ns/call**。× 普查
23,472,380 调用/报告 ⇒ **朴素天花板 63.4–68.1 ms > 35 ms 带**。
微探针与普查的乘积**不许单方面立道**（探针形态低层执行的工件
可能不在优化帧里成立），也**不许**反向用「优化帧会折叠」的
理论单方面杀道——按规升级为真实规模决斗：

- **replica-verb.mts**：落地管线逐语句复刻（sigmoid / rng /
  buildDesign / computeSupports / canonicalRowKeys / irls / dot /
  onProbabilitiesFor / averagePredictiveComparison / percentile /
  fitLogitAdditiveReplica **+ solveSymmetric 逐语句复刻**——72%
  的环境调用住在这里，必须入剂量域；`DomainValidationError` 以
  普通 `Error` 拼写于永不触发的方阵守卫；`betaQuantileLcb` 两道
  同为生产 import，每报告 ~340 次环境调用不剂量、两道同项）。
- **replica-capt.mts**：仅经机械替换从 verb 生成——捕获头 8 行
  （M_EXP/M_ABS/M_MAX/M_SQRT/M_FLOOR/M_CEIL/M_IMUL/N_ISFIN）+
  引用形态替换（`Math.exp(` → `M_EXP(` 等 13 处 +
  `Number.isFinite` → `N_ISFIN` 2 处，含 `beta.every(N_ISFIN)`
  的按引用传递位点——同一函数对象，位为同一），**录得 diff
  81 行存档（附录 A.5），无其他 token 变化**。
- **决斗 runner 逐字节同一**（道由 `R31C_LANE` 选择动态 import
  哪个 replica）；每进程协议：(1) **保真门**——34 例形状电池
  （0x3c01，与主锚同构）+ perf + small，全字段 `Object.is` 对
  **生产 import**，1,956 项/进程；(2) 热层 7-rep perf + 3-rep
  small；(3) 摘要见证——跨轮载荷折叠，期望 `63e74db4…3893`。

1 次烟测双道 + 交错 3 正典轮（8 进程全部保真门绿 = 15,648 项，
摘要 8/8 同值）：

| 轮 | VERB perf 中位 | CAPT perf 中位 | Δ = VERB−CAPT |
|---|---|---|---|
| 烟测 | 672.852 | 671.082 | +1.770 |
| 正典 1 | 673.238 | 671.826 | **+1.412** |
| 正典 2 | 671.803 | 675.173 | **−3.370** |
| 正典 3 | 674.688 | 674.230 | **+0.458** |

**符号翻转（正→负→正）且 |Δ| ≤ 3.4 ms，离 ±35 ms 带 ≥10×**；
small 形差 −0.18 / −0.53 / −0.33 ms 同为噪声域。（决斗 replica
中位 671.8–675.2 比生产 import 主锚高 ~13–16 ms 是装具项：每
决斗进程先跑 36 形状保真电池、两条管线 JIT 反馈共驻——两道
协议逐字节同一，装具项在道间抵消；与 R29-C/R30-C 双装具 ms 级
偏移同型。）**物理结论**：优化帧里环境载入被折叠、调用被还原
——每调用解析价在 657 ms 热池中**不存在**，CAP 微红利是低层
工件，真实价 0 ± 3.4 ms = 噪声拒绝。**第二独立拒绝（语义）**：
捕获形把绑定冻结在模块求值时刻，落地活绑定形按 JS 语义追踪
运行时领域状态（LATE 臂 +190 ms 展示的正是追踪的代价面）——
污染态下两形态对非透明补丁**可观察发散**；C1 落地 = 为 0 ±
3.4 ms 换语义收窄 + 触碰两个冻结文件（offline-logit / lin-alg）。
**双重违例，道关闭，不铸 ID。**

### 2.3 枚举收口

除 §2.1–2.2 外，第三十一遍逐文件重读**零未点名站点**：r1.ts
（选择器 + 滞后，R12-C 四分类全覆盖）、r1-shadow-report.ts
（报告组装，R13-C/R15-C 零池）、posterior.ts（lnGamma/betacf/
逆正则化不完全 Beta——X1-3 + R11-C 关闭）、offline-logit.ts
（S1-C..S7-C 全系落地形态逐站点在位）、offline-prob-add.ts
（X3-4 + R17-C）、propensity.ts（X3-5 + R21-C）、lin-alg.ts
（S4-C/S5-C 落地形态在位）、bandit.ts（X4-2）、shadow.ts
（R16-C/R18-C once-per-fault）。**第三十一组枚举为空——比强铸
败者 ID 更诚实。**

## 3. 三大池地板与组合出路复核

- **APCX 池**（59 非截距名 × 400 行 APC 扫掠 × 201 fit）：无
  计时器独立道 0.293 / 0.302 / 0.307 / 0.301 ms/fit ⇒ 58.9 /
  60.7 / 61.8 / 60.4 ms/报告——正典带 60.4–61.8 与 R30-C 带
  58.4–60.2 带缘相接（本 VM 本轮 APCX 道整体略高 ~1 ms，与
  FLOORX 同向漂移，组合差稳定）。
- **FLOORX 池**（S8-C-2 支撑升序 off-sum 形态，从未落地——X2-1
  引理前提本轮复核成立：0/1 设计、+0.0 起点、有限 beta；逐名值
  对 APCX 道 `Object.is` 断言全绿）：0.164 / 0.177 / 0.204 /
  0.176 ms/fit ⇒ 33.0 / 35.6 / 41.1 / 35.3 ms/报告地板。
- **组合天花板 = APCX − FLOORX = 25.9 / 25.0 / 20.7 / 25.1 ms
  < 35**——正典带 20.7–25.1 完全落入 R30-C 正典带 18.8–26.6，
  组合出路维持关闭；距 35 ms 带下缘最近读数（预演 25.9）仍差
  9.1 ms。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is(apcSink, floorSink)`，两道 65 遍扫掠累加双精度逐位
  相等）——与 R14-C/R16-C..R30-C 打印值同值，派单强制的跨轮
  确定性旁证成立。
- 零池备忘维持：posterior/propensity/bandit/shadow 四文件全部
  站点在既有 ID 或无号探针覆盖下，本轮重读无新增。

## 4. 重命名检查（派单强制项）

本轮轴「ambient intrinsic-binding / intrinsic-resolution face」
（BINDX）与全部近邻的划界，逐条核对不是重命名：

1. **vs R28-F「boundary call-protocol face」（禁重铸）**——R28-F
   剂量**切片公开边界如何被调用**（八适配器平面：调用侧的
   BIND/WRAP 机制）；本轴剂量**切片内部自由引用解析到什么**
   ——调用点、实参、公开边界在全臂逐字节同一，剂量经由领域
   变异（import 前/中途改写全局绑定）而非任何调用形态。响应
   机器也不同：protector cell 失效（PROTO +2,470 ms）与常量
   追踪失效（LATE +190 ms）没有调用协议坐标能表达。
2. **vs R31-A「injected-callable plane」（停放门，首次快进
   带入）**——A 门是**经显式注入缝（参数/DI）到达的可调用物**，
   其独立物理对象证明失败正因计时会重开 R28-F/R29-A/R30-A；
   本轴**无注入缝**：Math.\* 与迭代协议是环境全局，不经任何
   参数抵达，剂量是领域状态而非传参内容。独立对象证明成立的
   判据：PROTO 臂在**零调用点、零实参、零注入值变化**下移动
   +2,470 ms——该响应仅能归因于领域级 protector 状态，任何
   既有面（协议/别名/派发史/表示）都没有能表达它的坐标。
3. **vs R27-H「execution-substrate face」/ R28-C 基片实例化
   （禁移植）**——基片 = 哪个引擎/加载器执行字节；本轴全臂
   锁死单一基片（tsx+src，R28-C S 臂），剂量的是**同一基片内**
   的领域绑定状态。基片坐标全程恒定。
4. **vs R30-C「input object-representation face」（REPRX，禁
   重铸）**——REPRX 剂量输入行的引擎表示；本轴输入表示恒为
   规范形（同一冻结夹具字节），剂量的是代码到原语的名字解析
   层。输入面零移动。
5. **vs R29-C「process workload-composition face」（PAIRX，禁
   重铸）**——PAIRX 剂量同进程姊妹内核的有无与次序；本轴姊妹
   估计器零调用，进程工作负载恒定单内核。
6. **vs R30-A「call-stream ordering / dispatch-history face」
   （A 区，禁移植）**——派发历史剂量公开边界的调用流次序；
   本轴调用流恒定（同 rep 协议每臂），剂量在解析层。
7. **vs R25-H「consumer-stage string-representation census」/
   R22-H caller-context geometry（H 区）**——字符串表示与调用方
   上下文几何均不含领域绑定坐标；且本轴无字符串剂量。
8. **vs R31-B 六扇停放门（二次快进带入，B 区）**——其第一扇
   「catalog-row hidden-class/IC-shape」剂量**每对象** map/IC
   形态（B 区目录行对象）；本轴剂量**领域级**绑定与 protector
   状态，无对象形态移动（12/12 摘要同值 + 输入表示冻结即证）。
   其余五扇（cascade lattice / cold-onset / class-composition /
   LINKTOP-in-B / prior alias-resolution）与本轴无共享物理对象。
9. **vs R31-D R22-D 属地格（三次快进带入，D 区）**——drift-
   monitor analysis-window 配置平面，D 区零调用方格，与 C 区
   解析层无交集。
10. **vs R30-I 时间段拓扑 / R30-J LINKTOP（禁移植）**——进程
    生命期分段与文件系统链接拓扑均不触及领域内建绑定；本轴
    反向亦未测量 spawn-to-exit 分段或任何文件系统坐标。

BINDX 绰号在 R1-C..R30-C 全部报告与 EXCLUSIONS/PROGRESS/PLAN 中
rg 零命中（本轮铸名前普查：intrinsic / builtin / prototype /
protector / BINDX 词根在 C 区历史轴名录零碰撞）；plan-ban 禁再
编号绰号全列（FITQ … PAIRX / CSPELL / REPRX / LINKTOP 共 33 个）
零触碰。

## 5. 测试与验证

```
# 聚焦离线路由套件（tsx --test，7 文件）
# 在 2fdbefb / def155b / 3592410 三 tip 各跑一次：
#   tests 59 / suites 10 / pass 59 / fail 0  （三次逐字同计数）

# 全门禁 pnpm gate = typecheck + lint + test + build
# 在 2fdbefb / def155b / 3592410 三 tip 各跑一次，均 exit 0：
#   tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#   （三次逐字同计数；tsc build 零诊断）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门；
# def155b 工作树）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项（design.build 358,006/拟合）
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项（eta dots 3,586,400 → 1,249,652 去重）
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 661.1 ms（1.12× vs 冻结 S6-C ctl 743.8）——与
# §1 生产正典中位带（658.1–660.1）互证，比率与 R28-C（1.12×）/
# R29-C（1.12×）/R30-C（1.13×）逐轮基本不动。

# 本轮测量装具（临时脚本，未入库——无生产赢家，全文见附录 A）
# 主锚 ×4：       11,072 项位级检查全绿（2,768 × 4）
# 绑定轴 ×12：    logitDigest 12/12 = 63e74db4…3893（== R28-C/R29-C/R30-C）
#                 capSink 12/12 = 1,951,200.6101761512（进程内 Object.is）
# 普查 ×1：       摘要门 = 63e74db4…3893（计数剂量下逐位复现）
# 决斗 ×8：       保真门 15,648 项全绿（1,956 × 8），摘要 8/8 同值
```

冻结五验（§0）；仓库变更 = 本报告一个文件（提交前
`git diff origin/cursor/sota-persistent-opt-83a1...HEAD --stat`
复核仅 `docs/reports/sota-opt/round-31/R31-C.md`）。r31c 新仿真
未铸：无生产落地（派单条件句未触发），临时装具全文归档于附录 A
后已从 /tmp 删除。

## 6. 新增排除 ID：无

- 不铸 S31-C-\*：第三十一组枚举为空，候选道 C1 双重拒绝（0 ±
  3.4 ms 噪声 + 语义收窄），无任何站点越过 ±35 ms 带。
- 不写入 EXCLUSIONS.md 任何行（BINDX 为空枚举轴观察绰号，按令
  归 parent plan-ban）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态，未重开 S12-C-1
  （重开条件：无），未起 S7-C 平行实现。

## 附录 A：测量装具全文（完整，可复现；归档后临时文件已删除）

六个文件，sha256（归档时点）：

```
ce62802ef484ed3e5332ebde18b990e3e90a666a3e749c71a38f80bae5fdd851  r31c-anchor.mts
8c0304d04d5c88a0a82950affdd550e01649cccb5e74be757a496fd246ed6d07  r31c-bind-runner.mjs
e9761583c107677a77ac81c27bcd64a0add94c07124b928cb19b7119866d9f03  r31c-count-census.mjs
924eaadb9bc38dc78c43504fe99d2e3e323db6fc52e59876f6e37f5d8cce5510  replica-verb.mts
ea2d44526a303b7ded2c9e515b40bbdfb2f1d361c9a76369b1338aafbbbac724  replica-capt.mts
579e75d3c019df00211aab9580501c74905ed28b4be6fbd5459070d87b0bd14e  r31c-capt-duel.mts
```

运行方式（Node v22.22.2，仓库根，pnpm install --frozen-lockfile 后）：

```
node_modules/.bin/tsx /tmp/r31c/r31c-anchor.mts                       # 主锚（×4）
R31C_ARM={PRIST|WRAPM|LATE|PROTO} node_modules/.bin/tsx /tmp/r31c/r31c-bind-runner.mjs   # 轴臂（交错 3 轮）
node_modules/.bin/tsx /tmp/r31c/r31c-count-census.mjs                 # 通量普查
R31C_LANE={VERB|CAPT} node_modules/.bin/tsx /tmp/r31c/r31c-capt-duel.mts                 # C1 决斗（烟测 + 交错 3 轮）
```

replica-capt.mts 由 replica-verb.mts 经机械替换生成（生成器全文
见 A.5，录得 diff 81 行附后）。

### A.1 主锚 `/tmp/r31c/r31c-anchor.mts`（协议逐字节同 R30-C，仅头注更新）

```ts
/**
 * Round 31 / R31-C measurement re-anchor + thirty-first-group closure probes
 * (offline routing slice, thirty-first pass). NOT committed — the thirty-
 * first-group enumeration carries no candidate lanes that reach the
 * acceptance bar; full source goes into the R31-C report appendix.
 *
 * Protocol is verbatim R30-C (which is verbatim R14-C lineage):
 *  - replica-lane fidelity battery: a verbatim copy of the landed
 *    fitLogitAdditive pipeline (buildDesign / computeSupports /
 *    canonicalRowKeys / irls / onProbabilitiesFor /
 *    averagePredictiveComparison / percentile) compared bitwise
 *    (Object.is, includes signed zero / NaN) against the production import
 *    on a 34-case shape battery + the perf fixture + the small fixture.
 *  - production median re-anchor: perf shape (4 scenarios x 6 models x
 *    8 projects, rows=400, passRate=0.6, bootstrap=200, seed=20260818)
 *    7 reps -> median; small shape (3 x 3 x 3, 120 rows) 3 reps -> median.
 *  - production self-determinism: 3 seeds x 2 runs, all report fields
 *    Object.is.
 *  - APCX probe (dispatch-mandated ceiling re-measure): standalone
 *    timer-free APC sweep (59 non-intercept names x 400 rows, base-fit
 *    coefficients) per-fit cost x 201 fits; sink = sum of all APC values
 *    (cross-round determinism witness, expected print 7.309).
 *  - FLOORX probe: cheapest bitwise-legal sweep (S8-C-2 support-ascending
 *    off-sum form, never landed — X2-1 lemma premises hold: 0/1 design,
 *    +0.0 start, finite beta), per-name values asserted Object.is against
 *    the APCX sweep, then per-fit cost x 201 fits; combination ceiling =
 *    APCX - FLOORX must stay < 35 ms with floorSink === sink bitwise.
 *
 * The ambient intrinsic-binding axis arms run out of the byte-identical
 * runner r31c-bind-runner.mjs (see report appendix), not out of this file.
 */
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.ts";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.ts";
import { solveSymmetric } from "/workspace/src/routing/lin-alg.ts";
import type { AttributionEffect, AttributionLabel, AttributionReport } from "/workspace/src/routing/offline-types.ts";

let checks = 0;
function assertSame(label: string, a: unknown, b: unknown): void {
  checks += 1;
  if (!Object.is(a, b)) {
    throw new Error(`MISMATCH ${label}: ${String(a)} !== ${String(b)}`);
  }
}
function assertTrue(label: string, v: boolean): void {
  checks += 1;
  if (!v) throw new Error(`ASSERT ${label}`);
}

/* ------------- replica lane: verbatim copy of the landed pipeline ------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}
interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}
function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}
function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);
  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);
  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);
  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });
  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}
interface FitResult {
  readonly coefficients: readonly number[] | null;
}
function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}
function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}
function irls(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
): FitResult {
  const p = design.names.length;
  const n = rows.length;
  const eta = new Array<number>(n).fill(0);
  const mu = new Array<number>(n).fill(0);
  const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
  const xtwz: number[] = new Array<number>(p).fill(0);
  const stamp = new Int32Array(keySpace);
  const etaByKey = new Float64Array(keySpace);
  const muByKey = new Float64Array(keySpace);
  let beta = new Array<number>(p).fill(0);
  for (let iter = 0; iter < maxIter; iter++) {
    const mark = iter + 1;
    for (let i = 0; i < n; i++) {
      const key = keys[i]!;
      if (stamp[key] !== mark) {
        stamp[key] = mark;
        let value = 0;
        const active = supports[i]!;
        for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
        etaByKey[key] = value;
        muByKey[key] = sigmoid(value);
      }
      eta[i] = etaByKey[key]!;
      mu[i] = muByKey[key]!;
    }
    for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
    xtwz.fill(0);
    for (let i = 0; i < n; i++) {
      const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
      const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
      const wz = w * z;
      const active = supports[i]!;
      switch (active.length) {
        case 5: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          const a4 = active[4]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
          xtwz[a4] = xtwz[a4]! + wz;
          const r4 = xtwx[a4]!;
          r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
          break;
        }
        case 4: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
          break;
        }
        case 3: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
          break;
        }
        case 2: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
          break;
        }
        default: {
          for (let ai = 0; ai < active.length; ai++) {
            const a = active[ai]!;
            xtwz[a] = xtwz[a]! + wz;
            const rowA = xtwx[a]!;
            for (let bi = 0; bi < active.length; bi++) {
              const b = active[bi]!;
              rowA[b] = rowA[b]! + w;
            }
          }
        }
      }
    }
    for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
    const next = solveSymmetric(xtwx, xtwz);
    if (next === null) return { coefficients: null };
    const delta = next.map((value, index) => value - beta[index]!);
    const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
    beta = next;
    if (!beta.every(Number.isFinite)) return { coefficients: null };
    if (l2 < TOL) break;
  }
  return { coefficients: beta };
}
function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}
function onProbabilitiesFor(
  vectors: readonly number[][],
  coefficients: readonly number[]
): number[] {
  return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
}
function averagePredictiveComparison(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  coefficients: readonly number[],
  onProbabilities: readonly number[],
  column: string
): number {
  let sum = 0;
  const columnIdx = design.columnIndex.get(column);
  if (columnIdx !== undefined && columnIdx !== 0) {
    for (let i = 0; i < rows.length; i++) {
      if (vectors[i]![columnIdx] === 0) continue;
      const on = onProbabilities[i]!;
      const offVector = vectors[i]!.slice();
      offVector[columnIdx] = 0;
      const off = sigmoid(dot(coefficients, offVector));
      sum += on - off;
    }
  }
  return rows.length === 0 ? 0 : sum / rows.length;
}
function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}
type OfflineRowInput = {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
  readonly occurredAtMs: number;
};
function fitLogitAdditiveReplica(
  rows: readonly OfflineRowInput[],
  options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
): AttributionReport {
  const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
  const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
  const seed = options?.seed ?? SEED_DEFAULT;
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const effects: AttributionEffect[] = [];
  if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
  }
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irls(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
  if (fit.coefficients === null) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
  }
  const onProbabilities = onProbabilitiesFor(vectors, fit.coefficients);
  const pointEffects = new Map<string, number>();
  for (const name of design.names) {
    if (name === "intercept") continue;
    pointEffects.set(
      name,
      averagePredictiveComparison(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
    );
  }
  for (const ref of design.referenceLevels) {
    if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
      pointEffects.set(`${ref.factor}:${ref.name}`, 0);
    }
  }
  const random = rng(seed);
  const draws = new Map<string, number[]>();
  let successful = 0;
  for (let draw = 0; draw < bootstrapDraws; draw++) {
    const sample: Row[] = [];
    const sampleVectors: number[][] = [];
    const sampleSupports: number[][] = [];
    const sampleKeys: number[] = [];
    for (let i = 0; i < baseRows.length; i++) {
      const index = Math.floor(random() * baseRows.length);
      sample.push(baseRows[index]!);
      sampleVectors.push(vectors[index]!);
      sampleSupports.push(supports[index]!);
      sampleKeys.push(keys[index]!);
    }
    if (sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1)) continue;
    const bootFit = irls(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
    if (bootFit.coefficients === null) continue;
    successful += 1;
    const sampleOnProbabilities = onProbabilitiesFor(sampleVectors, bootFit.coefficients);
    for (const [name] of pointEffects.entries()) {
      const value = averagePredictiveComparison(
        design,
        sample,
        sampleVectors,
        bootFit.coefficients,
        sampleOnProbabilities,
        name
      );
      pushValue(draws, name, value);
    }
  }
  for (const [name, point] of pointEffects.entries()) {
    const values = draws.get(name) ?? [];
    if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
    }
    values.sort((a, b) => a - b);
    effects.push({
      name,
      point,
      lcb: percentile(values, 0.025),
      ucb: percentile(values, 0.975)
    });
  }
  const n = baseRows.length;
  const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
  const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
  const muLcb = betaQuantileLcb(muPosterior, 0.05);
  const models = new Set(baseRows.map((r) => r.modelVersion)).size;
  const projects = new Set(baseRows.map((r) => r.projectId)).size;
  const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
  const containsZero = (e: AttributionEffect): boolean =>
    e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
  const interactionsFor = (prefix: string): AttributionEffect[] =>
    effects.filter((e) => e.name.startsWith(`w:${prefix}`));
  let diagnosis: AttributionLabel = "uncertain";
  const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
  if (
    effects.some(
      (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
    )
  ) {
    diagnosis = "model-problem";
  } else if (
    effects.some(
      (e) =>
        e.name.startsWith("v:") &&
        e.lcb < -ATTRIBUTION_EFFECT &&
        effects
          .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
          .every(containsZero)
    )
  ) {
    diagnosis = "project-problem";
  } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
    diagnosis = "interaction-only";
  } else if (scenarioHard) {
    diagnosis = "scenario-hard";
  }
  return {
    estimator: "logit-additive",
    rowsUsed: baseRows.length,
    effects,
    diagnosis,
    reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
    writesActivePointer: false
  };
}
function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}
function pushValue(map: Map<string, number[]>, key: string, value: number): void {
  const list = map.get(key);
  if (list === undefined) map.set(key, [value]);
  else list.push(value);
}
function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}
function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRowInput[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRowInput[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
function perfRows(): OfflineRowInput[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
}
// Same small-shape fixture as R10-C..R30-C (seed 0x51a1, 3 x 3 x 3 / 120).
function smallRows(): OfflineRowInput[] {
  const r = fixtureRng(0x51a1);
  return randomRows(r, { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
}
const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

/* ------------------------------ comparisons ------------------------------ */

function compareReports(label: string, a: AttributionReport, b: AttributionReport): void {
  assertSame(`${label}.estimator`, a.estimator, b.estimator);
  assertSame(`${label}.rowsUsed`, a.rowsUsed, b.rowsUsed);
  assertSame(`${label}.diagnosis`, a.diagnosis, b.diagnosis);
  assertSame(`${label}.reason`, a.reason, b.reason);
  assertSame(`${label}.writesActivePointer`, a.writesActivePointer, b.writesActivePointer);
  assertSame(`${label}.effects.length`, a.effects.length, b.effects.length);
  for (let i = 0; i < a.effects.length; i++) {
    const ea = a.effects[i]!;
    const eb = b.effects[i]!;
    assertSame(`${label}.effects[${i}].name`, ea.name, eb.name);
    assertSame(`${label}.effects[${i}].point`, ea.point, eb.point);
    assertSame(`${label}.effects[${i}].lcb`, ea.lcb, eb.lcb);
    assertSame(`${label}.effects[${i}].ucb`, ea.ucb, eb.ucb);
  }
}

function batteryCases(): Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];
  for (let i = 0; i < 24; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }) });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 60, passRate: 0.55, pipeInModel: true }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  return cases;
}

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------------------------------- main --------------------------------- */

function main(): void {
  // 1. Replica fidelity battery (replica lane vs production import, bitwise).
  const cases = batteryCases();
  for (let i = 0; i < cases.length; i++) {
    const c = cases[i]!;
    compareReports(`battery[${i}]`, fitLogitAdditive(c.rows, c.options), fitLogitAdditiveReplica(c.rows, c.options));
  }
  compareReports("perf-fixture", fitLogitAdditive(perfRows(), PERF_OPTIONS), fitLogitAdditiveReplica(perfRows(), PERF_OPTIONS));
  compareReports("small-fixture", fitLogitAdditive(smallRows(), PERF_OPTIONS), fitLogitAdditiveReplica(smallRows(), PERF_OPTIONS));
  console.log(`scenario 1 (replica-lane fidelity battery, ${cases.length} cases + perf + small, bitwise vs production import): OK`);

  // 2. Production self-determinism: 3 seeds x 2 runs.
  for (const seed of [SEED_DEFAULT, 7_777, 424_242]) {
    const rows = perfRows();
    compareReports(`determinism.seed=${seed}`, fitLogitAdditive(rows, { bootstrap: 200, seed }), fitLogitAdditive(rows, { bootstrap: 200, seed }));
  }
  console.log("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is): OK");

  // 3. Production median re-anchor (perf 7 reps, small 3 reps).
  const prodPerf: number[] = [];
  {
    const rows = perfRows();
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      prodPerf.push(performance.now() - t0);
    }
  }
  const prodSmall: number[] = [];
  {
    const rows = smallRows();
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      prodSmall.push(performance.now() - t0);
    }
  }
  console.log(`  shape small (3 models x 3 projects, 120 rows): production median ${median(prodSmall).toFixed(1)} ms/report (3 reps)`);
  console.log(`  shape perf (6 models x 8 projects, 400 rows): production median ${median(prodPerf).toFixed(1)} ms/report (7 reps)`);
  console.log(`  perf reps: ${prodPerf.map((v) => v.toFixed(1)).join(" ")}`);

  // 4. APCX / FLOORX ceiling re-measure (dispatch-mandated).
  const rows = perfRows();
  const baseRows: Row[] = rows.map((r) => ({ scenarioId: r.scenarioId, modelVersion: r.modelVersion, projectId: r.projectId, y: r.y }));
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irls(design, baseRows, vectors, supports, keys, baseRows.length, MAX_ITER_DEFAULT);
  assertTrue("apcx.base-fit-converged", fit.coefficients !== null);
  const coefficients = fit.coefficients!;
  const onProbabilities = onProbabilitiesFor(vectors, coefficients);
  const sweepNames = design.names.filter((name) => name !== "intercept");

  // FLOORX lane bitwise-legality proof, per name (S8-C-2 form; X2-1 lemma
  // premises hold: 0/1 entries, +0.0 start, finite beta; ascending support
  // order = the full dot's nonzero visit order). Outside the sink accumulator.
  for (const name of sweepNames) {
    let sum = 0;
    const columnIdx = design.columnIndex.get(name);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < baseRows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        const active = supports[i]!;
        let value = 0;
        for (let ai = 0; ai < active.length; ai++) {
          const j = active[ai]!;
          if (j === columnIdx) continue;
          value += coefficients[j]!;
        }
        sum += onProbabilities[i]! - sigmoid(value);
      }
    }
    const floorValue = baseRows.length === 0 ? 0 : sum / baseRows.length;
    assertSame(
      `floorx.bitwise[${name}]`,
      averagePredictiveComparison(design, baseRows, vectors, coefficients, onProbabilities, name),
      floorValue
    );
  }
  // Verbatim R14-C-lineage protocol: 5 warmup sweeps + 60 timed sweeps per
  // lane, sink accumulated across all 65 (expected cross-round print 7.309).
  const names = sweepNames;
  let apcSink = 0;
  const sweepReps = 60;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tSweep0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perSweepMs = (performance.now() - tSweep0) / sweepReps;
  console.log(
    `probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) ` +
      `${perSweepMs.toFixed(3)} ms/fit -> x201 fits = ${(perSweepMs * 201).toFixed(1)} ms/report ` +
      `(base-shape approximation; sink=${apcSink.toFixed(3)})`
  );

  let floorSink = 0;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tFloor0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perFloorMs = (performance.now() - tFloor0) / sweepReps;
  console.log(
    `probe FLOORX: cheapest bitwise-legal sweep ${perFloorMs.toFixed(3)} ms/fit -> x201 fits = ` +
      `${(perFloorMs * 201).toFixed(1)} ms/report floor; APC combination ceiling on THIS VM = ` +
      `${(perSweepMs * 201 - perFloorMs * 201).toFixed(1)} ms (floorSink=${floorSink.toFixed(3)})`
  );
  assertSame("apcx.sink===floorSink", apcSink, floorSink);
  assertTrue("ceiling<35", perSweepMs * 201 - perFloorMs * 201 < 35);

  console.log("");
  console.log(`ALL EQUIVALENCE CHECKS PASSED (${checks} bitwise checks)`);
}

main();
```

### A.2 绑定轴 runner `/tmp/r31c/r31c-bind-runner.mjs`（逐字节同一，臂由 R31C_ARM 选择）

```js
/**
 * R31-C ambient intrinsic-binding axis runner (ambient callee-binding /
 * intrinsic-resolution face, C-slice minting round). Plain-JS on purpose:
 * THIS FILE IS BYTE-IDENTICAL ACROSS ALL ARMS. The arm is selected entirely
 * by R31C_ARM; every arm runs on the single canonical measurement substrate
 * (tsx + /workspace/src — the R28-C S arm; the substrate coordinate stays
 * frozen). NOT committed; full source goes into the R31-C report appendix.
 *
 * Coordinate being dosed: WHICH FUNCTION OBJECTS the slice's free ambient
 * references resolve to at execution time — the mutable name-resolution
 * layer between the slice's code and the primitive operations it performs.
 * The slice's nine files consume exactly twelve Math intrinsics
 * (exp/abs/max/min/sqrt/floor/ceil/log/pow/sin/imul/round), Number.isFinite,
 * and the built-in iteration protocol (array/map/set iterator `next` behind
 * for-of, spread, and the Map constructor). Every prior C reading
 * (R1-C..R30-C) ran with pristine V8 intrinsics: the binding coordinate was
 * never a controlled variable. Everything else stays frozen: same fixture
 * bytes (0xbeef / 0x51a1 / 0x5eed), same emission order, same options, same
 * canonical input representation (the R30-C REPRX coordinate is never
 * dosed), same substrate, same workload composition (the sibling estimator
 * is never called — R29-C PAIRX stays frozen), zero input-value movement.
 *
 * Production relevance: the repo never patches an intrinsic or a built-in
 * prototype (rg census in the report), so production can only ever mint the
 * pristine arm — the binding optimum is already occupied by the landed
 * form. The axis prices what the coordinate would cost if a host embedding
 * contaminated it, and whether any slice-internal counter-move exists
 * (it does not: capture-hoisting is priced ~0 in the pristine state by the
 * CAP probe below and narrows late-patch semantics; guarding/freezing
 * intrinsics lives outside the nine files and outside campaign rules).
 *
 * Arms (dose points of the binding coordinate):
 *   PRIST : pristine bindings — the historical measurement form.
 *   WRAPM : the twelve Math intrinsics + Number.isFinite are replaced with
 *           exact-arity transparent pass-through wrappers BEFORE the
 *           production module is imported (compile-time-visible foreign
 *           bindings; every wrapper calls the captured original, so every
 *           output bit is unchanged).
 *   LATE  : pristine import + 3 timed perf reps, THEN the same wrapper set
 *           is installed mid-process (constant-tracking invalidation /
 *           deopt-cliff dose point), then 4 more timed perf reps.
 *   PROTO : the built-in iteration protocol is wrapped BEFORE import —
 *           %ArrayIteratorPrototype%.next, %MapIteratorPrototype%.next,
 *           %SetIteratorPrototype%.next become transparent pass-throughs
 *           (invalidates the V8 iterator protector cells consumed by the
 *           slice's for-of / spread / Map-constructor sites).
 *
 * Emits one JSON line: hot-tier medians (perf 7 reps, small 3 reps), all
 * reps, LATE pre/post segment medians, the CAP probe (module-local capture
 * vs live Math.exp lookup — candidate lane C1's pristine-state price), and
 * logitDigest over IEEE-754 bit patterns + strings (identical payload and
 * fold as the R28-C substrate runner / R29-C co-residency runner / R30-C
 * representation runner, so equality across all 12 processes AND against
 * the printed digest of those rounds is a single string comparison — the
 * bit face: no binding dose may move any output bit).
 */
import { createHash } from "node:crypto";

const arm = process.env.R31C_ARM ?? "?";
if (arm !== "PRIST" && arm !== "WRAPM" && arm !== "LATE" && arm !== "PROTO") {
  console.error(`R31C_ARM must be PRIST | WRAPM | LATE | PROTO, got ${arm}`);
  process.exit(1);
}

/* ---------------- binding doses (transparent by construction) ---------------- */

// Exact-arity pass-throughs over the originals. Every Math call site in the
// nine slice files is unary or binary (read census in the report §0); the
// wrappers forward those shapes bit-transparently. Number.isFinite receives
// (value, index, array) from Array.prototype.every — only the first argument
// is read by the original, so the unary forward is transparent there too.
function installMathWrappers() {
  const E = Math.exp, A = Math.abs, X = Math.max, N = Math.min, Q = Math.sqrt,
    F = Math.floor, C = Math.ceil, L = Math.log, P = Math.pow, S = Math.sin,
    I = Math.imul, R = Math.round;
  Math.exp = (x) => E(x);
  Math.abs = (x) => A(x);
  Math.max = (a, b) => X(a, b);
  Math.min = (a, b) => N(a, b);
  Math.sqrt = (x) => Q(x);
  Math.floor = (x) => F(x);
  Math.ceil = (x) => C(x);
  Math.log = (x) => L(x);
  Math.pow = (a, b) => P(a, b);
  Math.sin = (x) => S(x);
  Math.imul = (a, b) => I(a, b);
  Math.round = (x) => R(x);
  const isF = Number.isFinite;
  Number.isFinite = (x) => isF(x);
}

function installIterWrappers() {
  const wrapNext = (proto) => {
    const orig = proto.next;
    proto.next = function next() {
      return orig.call(this);
    };
  };
  wrapNext(Object.getPrototypeOf([][Symbol.iterator]()));
  wrapNext(Object.getPrototypeOf(new Map()[Symbol.iterator]()));
  wrapNext(Object.getPrototypeOf(new Set()[Symbol.iterator]()));
}

if (arm === "WRAPM") installMathWrappers();
if (arm === "PROTO") installIterWrappers();

// Import AFTER the pre-import dose point so compile-time-visible foreign
// bindings are what the production module's code objects grow up with.
const { fitLogitAdditive } = await import("/workspace/src/routing/offline-logit.ts");

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed) {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick(r, items) {
  return items[Math.floor(r() * items.length)];
}
function randomRows(r, options) {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) => `model-${i}`);
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
const perfRows = () => randomRows(fixtureRng(0xbeef), { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
const smallRows = () => randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
const PERF_OPTIONS = { bootstrap: 200, seed: 20260818 };

/** Canonical walk: numbers as IEEE-754 bit-pattern hex, strings/bools/null tagged. */
function foldValue(hash, value) {
  if (typeof value === "number") {
    const buf = new ArrayBuffer(8);
    new Float64Array(buf)[0] = value;
    hash.update("n:");
    hash.update(Buffer.from(buf));
  } else if (typeof value === "string") {
    hash.update(`s:${value.length}:${value};`);
  } else if (typeof value === "boolean") {
    hash.update(`b:${value};`);
  } else if (value === null) {
    hash.update("null;");
  } else if (value === undefined) {
    hash.update("undef;");
  } else if (Array.isArray(value)) {
    hash.update(`a:${value.length}[`);
    for (const item of value) foldValue(hash, item);
    hash.update("]");
  } else {
    const keys = Object.keys(value);
    hash.update(`o:${keys.length}{`);
    for (const key of keys) {
      hash.update(`k:${key}=`);
      foldValue(hash, value[key]);
    }
    hash.update("}");
  }
}
function digestReports(reports) {
  const hash = createHash("sha256");
  for (const report of reports) foldValue(hash, report);
  return hash.digest("hex");
}
function median(values) {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)];
}

// One canonical fixture instance per shape (canonical representation only —
// the R30-C REPRX coordinate is never dosed by this axis).
const perfCanon = perfRows();
const smallCanon = smallRows();

// Hot-tier timing: perf shape 7 reps, small shape 3 reps — identical rep
// protocol in every arm; LATE installs the wrapper set between rep 2 and
// rep 3 (outside the timer).
const perfReps = [];
let latePatchedAtRep = null;
for (let rep = 0; rep < 7; rep++) {
  if (arm === "LATE" && rep === 3 && latePatchedAtRep === null) {
    installMathWrappers();
    latePatchedAtRep = rep;
  }
  const t0 = performance.now();
  fitLogitAdditive(perfCanon, PERF_OPTIONS);
  perfReps.push(performance.now() - t0);
}
const smallReps = [];
for (let rep = 0; rep < 3; rep++) {
  const t0 = performance.now();
  fitLogitAdditive(smallCanon, PERF_OPTIONS);
  smallReps.push(performance.now() - t0);
}

// Bit-face payload, byte-identical fold and report list as the R28-C
// substrate runner / R29-C co-residency runner / R30-C representation
// runner: equality across the 12 processes (and against the printed digest
// of those rounds) certifies the binding dose moves no logit output bit.
const r = fixtureRng(0x5eed);
const batteryShapes = [
  { rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 60, passRate: 0.5 }), options: { bootstrap: 40, seed: 11 } },
  { rows: randomRows(r, { scenarios: 3, models: 4, projects: 4, rows: 150, passRate: 0.55 }), options: { bootstrap: 60, seed: 77 } },
  { rows: randomRows(r, { scenarios: 1, models: 2, projects: 3, rows: 45, passRate: 0.65 }), options: { bootstrap: 30, seed: 5 } },
];
const reports = [
  fitLogitAdditive(perfRows(), PERF_OPTIONS),
  fitLogitAdditive(smallRows(), PERF_OPTIONS),
  ...batteryShapes.map((c) => fitLogitAdditive(c.rows, c.options)),
  fitLogitAdditive(perfRows(), PERF_OPTIONS),
];
const logitDigest = digestReports(reports);

// CAP probe (candidate lane C1's pristine-state price): a module-local
// capture of Math.exp versus the live ambient lookup, on the exact sigmoid
// body the hot pool runs. Runner-local code; the production module is never
// touched. Two interleaved passes; the second pass is the steady state.
// Only the PRIST reading prices lane C1 (capture-hoisting inside the slice);
// the patched arms' readings document how much of a foreign-binding tax the
// capture form would dodge — the semantic-divergence surface that rejects
// the lane even if the pristine delta were nonzero.
const CAPTURED_EXP = Math.exp;
function sigmoidLive(z) {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}
function sigmoidCaptured(z) {
  if (z >= 0) return 1 / (1 + CAPTURED_EXP(-z));
  const e = CAPTURED_EXP(z);
  return e / (1 + e);
}
const CAP_N = 2_000_000;
let capSinkLive = 0;
let capSinkCaptured = 0;
let capLiveMs = 0;
let capCapturedMs = 0;
for (let pass = 0; pass < 2; pass++) {
  let t0 = performance.now();
  for (let i = 0; i < CAP_N; i++) capSinkLive += sigmoidLive((i % 41) - 20.5);
  capLiveMs = performance.now() - t0;
  t0 = performance.now();
  for (let i = 0; i < CAP_N; i++) capSinkCaptured += sigmoidCaptured((i % 41) - 20.5);
  capCapturedMs = performance.now() - t0;
}
if (!Object.is(capSinkLive, capSinkCaptured)) {
  console.error(`CAP BIT MISMATCH: ${capSinkLive} !== ${capSinkCaptured}`);
  process.exit(1);
}

console.log(JSON.stringify({
  arm,
  node: process.version,
  perfMedianMs: Number(median(perfReps).toFixed(3)),
  smallMedianMs: Number(median(smallReps).toFixed(3)),
  perfReps: perfReps.map((v) => Number(v.toFixed(1))),
  smallReps: smallReps.map((v) => Number(v.toFixed(1))),
  latePreMedianMs: arm === "LATE" ? Number(median(perfReps.slice(0, 3)).toFixed(3)) : null,
  latePostMedianMs: arm === "LATE" ? Number(median(perfReps.slice(3)).toFixed(3)) : null,
  capLiveNsPerCall: Number(((capLiveMs / CAP_N) * 1e6).toFixed(2)),
  capCapturedNsPerCall: Number(((capCapturedMs / CAP_N) * 1e6).toFixed(2)),
  capSink: capSinkLive,
  logitDigest,
}));
```

### A.3 通量普查 `/tmp/r31c/r31c-count-census.mjs`

```js
/**
 * R31-C lane-C1 pricing census: exact ambient-intrinsic call counts for ONE
 * production perf report (0xbeef shape, bootstrap=200, seed=20260818) and ONE
 * small report (0x51a1). Counting pass-through wrappers are installed BEFORE
 * the production module is imported (the WRAPM dose point with counters), so
 * every ambient Math/Number.isFinite call the landed pipeline makes is
 * enumerated exactly. Digest gate: the counting dose must reproduce the
 * cross-round logitDigest bitwise (same payload/fold as the axis runner).
 * NOT committed; full source goes into the R31-C report appendix.
 *
 * Lane C1 ceiling = (total ambient intrinsic calls per perf report) x
 * (CAP-probe capture dividend per call, 2.7-2.9 ns upper bound). The census
 * runs counters on BOTH timing shapes, but never times anything itself —
 * counting wrappers are not a timing substrate.
 */
import { createHash } from "node:crypto";

const counts = Object.create(null);
function counting(name, fn, arity) {
  counts[name] = 0;
  if (arity === 2) {
    return (a, b) => {
      counts[name] += 1;
      return fn(a, b);
    };
  }
  return (x) => {
    counts[name] += 1;
    return fn(x);
  };
}
Math.exp = counting("Math.exp", Math.exp, 1);
Math.abs = counting("Math.abs", Math.abs, 1);
Math.max = counting("Math.max", Math.max, 2);
Math.min = counting("Math.min", Math.min, 2);
Math.sqrt = counting("Math.sqrt", Math.sqrt, 1);
Math.floor = counting("Math.floor", Math.floor, 1);
Math.ceil = counting("Math.ceil", Math.ceil, 1);
Math.log = counting("Math.log", Math.log, 1);
Math.pow = counting("Math.pow", Math.pow, 2);
Math.sin = counting("Math.sin", Math.sin, 1);
Math.imul = counting("Math.imul", Math.imul, 2);
Math.round = counting("Math.round", Math.round, 1);
Number.isFinite = counting("Number.isFinite", Number.isFinite, 1);

const { fitLogitAdditive } = await import("/workspace/src/routing/offline-logit.ts");

function fixtureRng(seed) {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick(r, items) {
  return items[Math.floor(r() * items.length)];
}
function randomRows(r, options) {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) => `model-${i}`);
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
function foldValue(hash, value) {
  if (typeof value === "number") {
    const buf = new ArrayBuffer(8);
    new Float64Array(buf)[0] = value;
    hash.update("n:");
    hash.update(Buffer.from(buf));
  } else if (typeof value === "string") {
    hash.update(`s:${value.length}:${value};`);
  } else if (typeof value === "boolean") {
    hash.update(`b:${value};`);
  } else if (value === null) {
    hash.update("null;");
  } else if (value === undefined) {
    hash.update("undef;");
  } else if (Array.isArray(value)) {
    hash.update(`a:${value.length}[`);
    for (const item of value) foldValue(hash, item);
    hash.update("]");
  } else {
    const keys = Object.keys(value);
    hash.update(`o:${keys.length}{`);
    for (const key of keys) {
      hash.update(`k:${key}=`);
      foldValue(hash, value[key]);
    }
    hash.update("}");
  }
}
function digestReports(reports) {
  const hash = createHash("sha256");
  for (const report of reports) foldValue(hash, report);
  return hash.digest("hex");
}

const PERF_OPTIONS = { bootstrap: 200, seed: 20260818 };
const perfCanon = randomRows(fixtureRng(0xbeef), { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
const smallCanon = randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });

// Fixture construction itself consumes counted intrinsics (imul/floor in the
// runner-local rng). Snapshot and subtract so the census is module-only.
function snapshot() {
  return Object.fromEntries(Object.entries(counts));
}
function delta(before, after) {
  const out = {};
  let total = 0;
  for (const key of Object.keys(after)) {
    const d = after[key] - (before[key] ?? 0);
    if (d !== 0) out[key] = d;
    total += d;
  }
  out.TOTAL = total;
  return out;
}

const beforePerf = snapshot();
const perfReport = fitLogitAdditive(perfCanon, PERF_OPTIONS);
const perfCensus = delta(beforePerf, snapshot());

const beforeSmall = snapshot();
const smallReport = fitLogitAdditive(smallCanon, PERF_OPTIONS);
const smallCensus = delta(beforeSmall, snapshot());

// Digest gate: identical payload/fold as the axis runner and the
// R28-C/R29-C/R30-C runners (perf, small, 3 battery shapes, perf again).
const r = fixtureRng(0x5eed);
const batteryShapes = [
  { rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 60, passRate: 0.5 }), options: { bootstrap: 40, seed: 11 } },
  { rows: randomRows(r, { scenarios: 3, models: 4, projects: 4, rows: 150, passRate: 0.55 }), options: { bootstrap: 60, seed: 77 } },
  { rows: randomRows(r, { scenarios: 1, models: 2, projects: 3, rows: 45, passRate: 0.65 }), options: { bootstrap: 30, seed: 5 } },
];
const reports = [
  fitLogitAdditive(randomRows(fixtureRng(0xbeef), { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 }), PERF_OPTIONS),
  fitLogitAdditive(randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 }), PERF_OPTIONS),
  ...batteryShapes.map((c) => fitLogitAdditive(c.rows, c.options)),
  fitLogitAdditive(randomRows(fixtureRng(0xbeef), { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 }), PERF_OPTIONS),
];
const logitDigest = digestReports(reports);

const CAP_DELTA_NS_LO = 2.7;
const CAP_DELTA_NS_HI = 2.9;
console.log(JSON.stringify({
  node: process.version,
  perfCensus,
  smallCensus,
  laneC1CeilingMsLo: Number(((perfCensus.TOTAL * CAP_DELTA_NS_LO) / 1e6).toFixed(3)),
  laneC1CeilingMsHi: Number(((perfCensus.TOTAL * CAP_DELTA_NS_HI) / 1e6).toFixed(3)),
  logitDigest,
}, null, 2));
```

### A.4 决斗 VERB 道 `/tmp/r31c/replica-verb.mts`（落地管线逐语句复刻）

```ts
/**
 * R31-C lane-C1 duel, VERB lane: verbatim replica of the landed
 * fitLogitAdditive pipeline (offline-logit.ts @ 183df9b) plus a verbatim
 * replica of solveSymmetric (lin-alg.ts @ 183df9b, DomainValidationError
 * spelled as a plain Error on the never-taken guard). All ambient intrinsic
 * references keep the landed live form (Math.exp / Math.abs / Math.max /
 * Math.sqrt / Math.floor / Math.ceil / Math.imul / Number.isFinite).
 * betaQuantileLcb stays a production import in BOTH lanes (one call per
 * report, ~340 ambient calls — undosed, identical). replica-capt.mts is
 * generated from this file by mechanical substitution only (see the diff in
 * the report appendix). NOT committed.
 */
import { betaQuantileLcb } from "/workspace/src/routing/posterior.ts";
import type { AttributionEffect, AttributionLabel, AttributionReport } from "/workspace/src/routing/offline-types.ts";

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}
interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}
function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}
function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function solveSymmetricReplica(a: readonly (readonly number[])[], b: readonly number[]): number[] | null {
  const n = b.length;
  if (a.length !== n || a.some((row) => row.length !== n)) {
    throw new Error("solveSymmetric requires a square matrix matching b");
  }
  const m: number[][] = a.map((row) => [...row]);
  const x: number[] = [...b];
  const eps = 1e-12;

  for (let col = 0; col < n; col++) {
    let pivotRow = col;
    let pivotAbs = Math.abs(m[col]![col]!);
    for (let row = col + 1; row < n; row++) {
      const abs = Math.abs(m[row]![col]!);
      if (abs > pivotAbs) {
        pivotAbs = abs;
        pivotRow = row;
      }
    }
    if (pivotAbs < eps) return null;
    if (pivotRow !== col) {
      const tmp = m[col]!;
      m[col] = m[pivotRow]!;
      m[pivotRow] = tmp;
      const tb = x[col]!;
      x[col] = x[pivotRow]!;
      x[pivotRow] = tb;
    }
    const colArr = m[col]!;
    const pivot = colArr[col]!;
    const xCol = x[col]!;
    for (let row = col + 1; row < n; row++) {
      const rowArr = m[row]!;
      const factor = rowArr[col]! / pivot;
      if (factor === 0) continue;
      let k = col;
      const stop = n - 3;
      for (; k < stop; k += 4) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
        rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
        rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
      }
      for (; k < n; k++) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
      }
      x[row] = x[row]! - factor * xCol;
    }
  }

  const solution = new Array<number>(n).fill(0);
  for (let row = n - 1; row >= 0; row--) {
    const rowArr = m[row]!;
    let sum = x[row]!;
    for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
    const diag = rowArr[row]!;
    if (Math.abs(diag) < eps) return null;
    solution[row] = sum / diag;
  }
  for (const value of solution) {
    if (!Number.isFinite(value)) return null;
  }
  return solution;
}
function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);
  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);
  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);
  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });
  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}
interface FitResult {
  readonly coefficients: readonly number[] | null;
}
function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}
function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}
function irls(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
): FitResult {
  const p = design.names.length;
  const n = rows.length;
  const eta = new Array<number>(n).fill(0);
  const mu = new Array<number>(n).fill(0);
  const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
  const xtwz: number[] = new Array<number>(p).fill(0);
  const stamp = new Int32Array(keySpace);
  const etaByKey = new Float64Array(keySpace);
  const muByKey = new Float64Array(keySpace);
  let beta = new Array<number>(p).fill(0);
  for (let iter = 0; iter < maxIter; iter++) {
    const mark = iter + 1;
    for (let i = 0; i < n; i++) {
      const key = keys[i]!;
      if (stamp[key] !== mark) {
        stamp[key] = mark;
        let value = 0;
        const active = supports[i]!;
        for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
        etaByKey[key] = value;
        muByKey[key] = sigmoid(value);
      }
      eta[i] = etaByKey[key]!;
      mu[i] = muByKey[key]!;
    }
    for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
    xtwz.fill(0);
    for (let i = 0; i < n; i++) {
      const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
      const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
      const wz = w * z;
      const active = supports[i]!;
      switch (active.length) {
        case 5: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          const a4 = active[4]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
          xtwz[a4] = xtwz[a4]! + wz;
          const r4 = xtwx[a4]!;
          r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
          break;
        }
        case 4: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
          break;
        }
        case 3: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
          break;
        }
        case 2: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
          break;
        }
        default: {
          for (let ai = 0; ai < active.length; ai++) {
            const a = active[ai]!;
            xtwz[a] = xtwz[a]! + wz;
            const rowA = xtwx[a]!;
            for (let bi = 0; bi < active.length; bi++) {
              const b = active[bi]!;
              rowA[b] = rowA[b]! + w;
            }
          }
        }
      }
    }
    for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
    const next = solveSymmetricReplica(xtwx, xtwz);
    if (next === null) return { coefficients: null };
    const delta = next.map((value, index) => value - beta[index]!);
    const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
    beta = next;
    if (!beta.every(Number.isFinite)) return { coefficients: null };
    if (l2 < TOL) break;
  }
  return { coefficients: beta };
}
function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}
function onProbabilitiesFor(
  vectors: readonly number[][],
  coefficients: readonly number[]
): number[] {
  return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
}
function averagePredictiveComparison(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  coefficients: readonly number[],
  onProbabilities: readonly number[],
  column: string
): number {
  let sum = 0;
  const columnIdx = design.columnIndex.get(column);
  if (columnIdx !== undefined && columnIdx !== 0) {
    for (let i = 0; i < rows.length; i++) {
      if (vectors[i]![columnIdx] === 0) continue;
      const on = onProbabilities[i]!;
      const offVector = vectors[i]!.slice();
      offVector[columnIdx] = 0;
      const off = sigmoid(dot(coefficients, offVector));
      sum += on - off;
    }
  }
  return rows.length === 0 ? 0 : sum / rows.length;
}
function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}
type OfflineRowInput = {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
  readonly occurredAtMs: number;
};
export function fitLogitAdditiveReplica(
  rows: readonly OfflineRowInput[],
  options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
): AttributionReport {
  const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
  const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
  const seed = options?.seed ?? SEED_DEFAULT;
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const effects: AttributionEffect[] = [];
  if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
  }
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irls(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
  if (fit.coefficients === null) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
  }
  const onProbabilities = onProbabilitiesFor(vectors, fit.coefficients);
  const pointEffects = new Map<string, number>();
  for (const name of design.names) {
    if (name === "intercept") continue;
    pointEffects.set(
      name,
      averagePredictiveComparison(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
    );
  }
  for (const ref of design.referenceLevels) {
    if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
      pointEffects.set(`${ref.factor}:${ref.name}`, 0);
    }
  }
  const random = rng(seed);
  const draws = new Map<string, number[]>();
  let successful = 0;
  for (let draw = 0; draw < bootstrapDraws; draw++) {
    const sample: Row[] = [];
    const sampleVectors: number[][] = [];
    const sampleSupports: number[][] = [];
    const sampleKeys: number[] = [];
    for (let i = 0; i < baseRows.length; i++) {
      const index = Math.floor(random() * baseRows.length);
      sample.push(baseRows[index]!);
      sampleVectors.push(vectors[index]!);
      sampleSupports.push(supports[index]!);
      sampleKeys.push(keys[index]!);
    }
    if (sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1)) continue;
    const bootFit = irls(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
    if (bootFit.coefficients === null) continue;
    successful += 1;
    const sampleOnProbabilities = onProbabilitiesFor(sampleVectors, bootFit.coefficients);
    for (const [name] of pointEffects.entries()) {
      const value = averagePredictiveComparison(
        design,
        sample,
        sampleVectors,
        bootFit.coefficients,
        sampleOnProbabilities,
        name
      );
      pushValue(draws, name, value);
    }
  }
  for (const [name, point] of pointEffects.entries()) {
    const values = draws.get(name) ?? [];
    if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
    }
    values.sort((a, b) => a - b);
    effects.push({
      name,
      point,
      lcb: percentile(values, 0.025),
      ucb: percentile(values, 0.975)
    });
  }
  const n = baseRows.length;
  const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
  const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
  const muLcb = betaQuantileLcb(muPosterior, 0.05);
  const models = new Set(baseRows.map((r) => r.modelVersion)).size;
  const projects = new Set(baseRows.map((r) => r.projectId)).size;
  const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
  const containsZero = (e: AttributionEffect): boolean =>
    e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
  const interactionsFor = (prefix: string): AttributionEffect[] =>
    effects.filter((e) => e.name.startsWith(`w:${prefix}`));
  let diagnosis: AttributionLabel = "uncertain";
  const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
  if (
    effects.some(
      (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
    )
  ) {
    diagnosis = "model-problem";
  } else if (
    effects.some(
      (e) =>
        e.name.startsWith("v:") &&
        e.lcb < -ATTRIBUTION_EFFECT &&
        effects
          .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
          .every(containsZero)
    )
  ) {
    diagnosis = "project-problem";
  } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
    diagnosis = "interaction-only";
  } else if (scenarioHard) {
    diagnosis = "scenario-hard";
  }
  return {
    estimator: "logit-additive",
    rowsUsed: baseRows.length,
    effects,
    diagnosis,
    reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
    writesActivePointer: false
  };
}
function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}
function pushValue(map: Map<string, number[]>, key: string, value: number): void {
  const list = map.get(key);
  if (list === undefined) map.set(key, [value]);
  else list.push(value);
}
function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}
```

### A.5 CAPT 道生成器（机械替换，node -e 内联执行）与录得 diff

```js
const fs = require('fs');
let src = fs.readFileSync('/tmp/r31c/replica-verb.mts','utf8');
const lines = src.split('\n');
// Replace the VERB doc header (first comment block) with the CAPT one.
const endOfHeader = lines.findIndex(l => l.trim() === '*/');
const captHeader = [
'/**',
' * R31-C lane-C1 duel, CAPT lane: byte-derived from replica-verb.mts by',
' * mechanical substitution ONLY — a module-scope capture header plus the',
' * intrinsic reference form (Math.exp( -> M_EXP( etc., Number.isFinite ->',
' * N_ISFIN). No other token changes; see the recorded diff in the report',
' * appendix. NOT committed.',
' */'];
let body = lines.slice(endOfHeader+1).join('\n');
body = body
  .replaceAll('Math.exp(','M_EXP(')
  .replaceAll('Math.abs(','M_ABS(')
  .replaceAll('Math.max(','M_MAX(')
  .replaceAll('Math.sqrt(','M_SQRT(')
  .replaceAll('Math.floor(','M_FLOOR(')
  .replaceAll('Math.ceil(','M_CEIL(')
  .replaceAll('Math.imul(','M_IMUL(')
  .replaceAll('Number.isFinite','N_ISFIN');
// Insert capture consts after the import lines.
const bodyLines = body.split('\n');
const lastImport = bodyLines.map((l,i)=>l.startsWith('import')?i:-1).filter(i=>i>=0).pop();
const captures = [
'',
'const M_EXP = Math.exp;',
'const M_ABS = Math.abs;',
'const M_MAX = Math.max;',
'const M_SQRT = Math.sqrt;',
'const M_FLOOR = Math.floor;',
'const M_CEIL = Math.ceil;',
'const M_IMUL = Math.imul;',
'const N_ISFIN = Number.isFinite;'];
bodyLines.splice(lastImport+1, 0, ...captures);
fs.writeFileSync('/tmp/r31c/replica-capt.mts', captHeader.join('\n')+'\n'+bodyLines.join('\n'));
console.log('written');
```

录得 `diff replica-verb.mts replica-capt.mts`（81 行，完整）：

```diff
2,11c2,6
<  * R31-C lane-C1 duel, VERB lane: verbatim replica of the landed
<  * fitLogitAdditive pipeline (offline-logit.ts @ 183df9b) plus a verbatim
<  * replica of solveSymmetric (lin-alg.ts @ 183df9b, DomainValidationError
<  * spelled as a plain Error on the never-taken guard). All ambient intrinsic
<  * references keep the landed live form (Math.exp / Math.abs / Math.max /
<  * Math.sqrt / Math.floor / Math.ceil / Math.imul / Number.isFinite).
<  * betaQuantileLcb stays a production import in BOTH lanes (one call per
<  * report, ~340 ambient calls — undosed, identical). replica-capt.mts is
<  * generated from this file by mechanical substitution only (see the diff in
<  * the report appendix). NOT committed.
---
>  * R31-C lane-C1 duel, CAPT lane: byte-derived from replica-verb.mts by
>  * mechanical substitution ONLY — a module-scope capture header plus the
>  * intrinsic reference form (Math.exp( -> M_EXP( etc., Number.isFinite ->
>  * N_ISFIN). No other token changes; see the recorded diff in the report
>  * appendix. NOT committed.
15a11,19
> const M_EXP = Math.exp;
> const M_ABS = Math.abs;
> const M_MAX = Math.max;
> const M_SQRT = Math.sqrt;
> const M_FLOOR = Math.floor;
> const M_CEIL = Math.ceil;
> const M_IMUL = Math.imul;
> const N_ISFIN = Number.isFinite;
> 
38,39c42,43
<   if (z >= 0) return 1 / (1 + Math.exp(-z));
<   const e = Math.exp(z);
---
>   if (z >= 0) return 1 / (1 + M_EXP(-z));
>   const e = M_EXP(z);
47,48c51,52
<     let t = Math.imul(a ^ (a >>> 15), 1 | a);
<     t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
---
>     let t = M_IMUL(a ^ (a >>> 15), 1 | a);
>     t = (t + M_IMUL(t ^ (t >>> 7), 61 | t)) ^ t;
63c67
<     let pivotAbs = Math.abs(m[col]![col]!);
---
>     let pivotAbs = M_ABS(m[col]![col]!);
65c69
<       const abs = Math.abs(m[row]![col]!);
---
>       const abs = M_ABS(m[row]![col]!);
108c112
<     if (Math.abs(diag) < eps) return null;
---
>     if (M_ABS(diag) < eps) return null;
112c116
<     if (!Number.isFinite(value)) return null;
---
>     if (!N_ISFIN(value)) return null;
121c125
<     levels.slice(0, Math.max(0, levels.length - 1));
---
>     levels.slice(0, M_MAX(0, levels.length - 1));
241c245
<       const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
---
>       const w = M_MAX(mu[i]! * (1 - mu[i]!)!, 1e-10);
331c335
<     const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
---
>     const l2 = M_SQRT(delta.reduce((acc, d) => acc + d * d, 0));
333c337
<     if (!beta.every(Number.isFinite)) return { coefficients: null };
---
>     if (!beta.every(N_ISFIN)) return { coefficients: null };
374,375c378,379
<   const lo = Math.floor(index);
<   const hi = Math.ceil(index);
---
>   const lo = M_FLOOR(index);
>   const hi = M_CEIL(index);
434c438
<       const index = Math.floor(random() * baseRows.length);
---
>       const index = M_FLOOR(random() * baseRows.length);
```

### A.6 决斗 runner `/tmp/r31c/r31c-capt-duel.mts`（逐字节同一，道由 R31C_LANE 选择）

```ts
/**
 * R31-C lane-C1 duel runner (candidate lane C1: module-local capture-hoist
 * of the slice's ambient intrinsics). THIS FILE IS BYTE-IDENTICAL ACROSS
 * BOTH LANES; R31C_LANE selects which replica module is imported:
 *   VERB : replica-verb.mts — verbatim landed reference form (live
 *          Math.* / Number.isFinite ambient references).
 *   CAPT : replica-capt.mts — byte-derived twin, all dosed intrinsic
 *          references routed through module-scope consts captured at
 *          module-evaluation time (the maximal capture-hoist form).
 * Protocol per process: (1) fidelity — 34-case shape battery (0x3c01) +
 * perf (0xbeef) + small (0x51a1), every report field Object.is against the
 * PRODUCTION import; (2) hot-tier timing — 7 perf reps -> median, 3 small
 * reps -> median, on the replica; (3) digest witness — the cross-round
 * payload (perf, small, 3x 0x5eed shapes, perf) folded exactly as the
 * R28-C/R29-C/R30-C runners, expected 63e74db4...a3893. The pristine-state
 * price of lane C1 is medianVERB - medianCAPT across interleaved processes.
 * NOT committed; full source goes into the R31-C report appendix.
 */
import { createHash } from "node:crypto";
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.ts";
import type { AttributionReport } from "/workspace/src/routing/offline-types.ts";

const lane = process.env.R31C_LANE ?? "?";
if (lane !== "VERB" && lane !== "CAPT") {
  console.error(`R31C_LANE must be VERB | CAPT, got ${lane}`);
  process.exit(1);
}
const replicaPath = lane === "VERB" ? "/tmp/r31c/replica-verb.mts" : "/tmp/r31c/replica-capt.mts";
const { fitLogitAdditiveReplica } = await import(replicaPath) as {
  fitLogitAdditiveReplica: typeof fitLogitAdditive;
};

let checks = 0;
function assertSame(label: string, a: unknown, b: unknown): void {
  checks += 1;
  if (!Object.is(a, b)) {
    throw new Error(`MISMATCH ${label}: ${String(a)} !== ${String(b)}`);
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
type OfflineRowInput = {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
  readonly occurredAtMs: number;
};
function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}
function randomRows(
  r: () => number,
  options: { scenarios: number; models: number; projects: number; rows: number; passRate: number; pipeInModel?: boolean }
): OfflineRowInput[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRowInput[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
function perfRows(): OfflineRowInput[] {
  return randomRows(fixtureRng(0xbeef), { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
}
function smallRows(): OfflineRowInput[] {
  return randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
}
const PERF_OPTIONS = { bootstrap: 200, seed: 20260818 };

function batteryCases(): Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];
  for (let i = 0; i < 24; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }) });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 60, passRate: 0.55, pipeInModel: true }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  return cases;
}

function compareReports(label: string, a: AttributionReport, b: AttributionReport): void {
  assertSame(`${label}.estimator`, a.estimator, b.estimator);
  assertSame(`${label}.rowsUsed`, a.rowsUsed, b.rowsUsed);
  assertSame(`${label}.diagnosis`, a.diagnosis, b.diagnosis);
  assertSame(`${label}.reason`, a.reason, b.reason);
  assertSame(`${label}.writesActivePointer`, a.writesActivePointer, b.writesActivePointer);
  assertSame(`${label}.effects.length`, a.effects.length, b.effects.length);
  for (let i = 0; i < a.effects.length; i++) {
    const ea = a.effects[i]!;
    const eb = b.effects[i]!;
    assertSame(`${label}.effects[${i}].name`, ea.name, eb.name);
    assertSame(`${label}.effects[${i}].point`, ea.point, eb.point);
    assertSame(`${label}.effects[${i}].lcb`, ea.lcb, eb.lcb);
    assertSame(`${label}.effects[${i}].ucb`, ea.ucb, eb.ucb);
  }
}

/* --------------------- 1. fidelity vs production import --------------------- */

const cases = batteryCases();
for (let i = 0; i < cases.length; i++) {
  const c = cases[i]!;
  compareReports(`battery[${i}]`, fitLogitAdditive(c.rows, c.options), fitLogitAdditiveReplica(c.rows, c.options));
}
compareReports("perf", fitLogitAdditive(perfRows(), PERF_OPTIONS), fitLogitAdditiveReplica(perfRows(), PERF_OPTIONS));
compareReports("small", fitLogitAdditive(smallRows(), PERF_OPTIONS), fitLogitAdditiveReplica(smallRows(), PERF_OPTIONS));

/* --------------------------- 2. hot-tier timing --------------------------- */

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}
const perfCanon = perfRows();
const smallCanon = smallRows();
const perfReps: number[] = [];
for (let rep = 0; rep < 7; rep++) {
  const t0 = performance.now();
  fitLogitAdditiveReplica(perfCanon, PERF_OPTIONS);
  perfReps.push(performance.now() - t0);
}
const smallReps: number[] = [];
for (let rep = 0; rep < 3; rep++) {
  const t0 = performance.now();
  fitLogitAdditiveReplica(smallCanon, PERF_OPTIONS);
  smallReps.push(performance.now() - t0);
}

/* --------------------------- 3. digest witness --------------------------- */

function foldValue(hash: ReturnType<typeof createHash>, value: unknown): void {
  if (typeof value === "number") {
    const buf = new ArrayBuffer(8);
    new Float64Array(buf)[0] = value;
    hash.update("n:");
    hash.update(Buffer.from(buf));
  } else if (typeof value === "string") {
    hash.update(`s:${value.length}:${value};`);
  } else if (typeof value === "boolean") {
    hash.update(`b:${value};`);
  } else if (value === null) {
    hash.update("null;");
  } else if (value === undefined) {
    hash.update("undef;");
  } else if (Array.isArray(value)) {
    hash.update(`a:${value.length}[`);
    for (const item of value) foldValue(hash, item);
    hash.update("]");
  } else {
    const keys = Object.keys(value as Record<string, unknown>);
    hash.update(`o:${keys.length}{`);
    for (const key of keys) {
      hash.update(`k:${key}=`);
      foldValue(hash, (value as Record<string, unknown>)[key]);
    }
    hash.update("}");
  }
}
const r = fixtureRng(0x5eed);
const digestShapes = [
  { rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 60, passRate: 0.5 }), options: { bootstrap: 40, seed: 11 } },
  { rows: randomRows(r, { scenarios: 3, models: 4, projects: 4, rows: 150, passRate: 0.55 }), options: { bootstrap: 60, seed: 77 } },
  { rows: randomRows(r, { scenarios: 1, models: 2, projects: 3, rows: 45, passRate: 0.65 }), options: { bootstrap: 30, seed: 5 } },
];
const digestReports = [
  fitLogitAdditiveReplica(perfRows(), PERF_OPTIONS),
  fitLogitAdditiveReplica(smallRows(), PERF_OPTIONS),
  ...digestShapes.map((c) => fitLogitAdditiveReplica(c.rows, c.options)),
  fitLogitAdditiveReplica(perfRows(), PERF_OPTIONS),
];
const hash = createHash("sha256");
for (const report of digestReports) foldValue(hash, report);
const logitDigest = hash.digest("hex");

console.log(JSON.stringify({
  lane,
  node: process.version,
  checks,
  perfMedianMs: Number(median(perfReps).toFixed(3)),
  smallMedianMs: Number(median(smallReps).toFixed(3)),
  perfReps: perfReps.map((v) => Number(v.toFixed(1))),
  smallReps: smallReps.map((v) => Number(v.toFixed(1))),
  logitDigest,
}));
```
