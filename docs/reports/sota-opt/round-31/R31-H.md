MODEL_SLUG=claude-fable-5-thinking-xhigh

# R31-H：`src/evaluation` + `src/requirement` + `src/review` + `src/rubric` 切片第三十一遍复查报告（Round 31）

**战役:** 全库持久 SOTA 优化 Round 31 / R31-H
**基线:** `cursor/sota-persistent-opt-83a1` @ `b585235`（R31-E 空枚举摄取 + 本派单）。运行期间独占分支 docs-only 快进两笔：`b585235` → `90c4365`（R31-F 空枚举摄取 + R31-I 派单）→ `a8de3d1`（R31-G 空枚举摄取 + R31-J 派单）。逐笔核对均 docs-only：两笔合计仅触 `docs/reports/sota-opt/` 4 个文件（PROGRESS / round-31 PLAN / R31-F.md / R31-G.md），`git diff b585235..a8de3d1 -- ':!docs/reports/sota-opt'` = **0 行**——三个 tip 的 `src/` 与 `test/` 逐字节相同。按令 rebase 至 `a8de3d1`（快进式零冲突），21 文件冻结在新 tip 复验 **0 行**；R31-F 结论（空枚举，边界故障通道输出处置门按分解并入 R17-F/R30-F/R29-F/引擎面属地）与 R31-G 结论（空枚举，全切片门扉闭合审计穷尽归属，未铸新 face 名）已读，补入非移植清单并完成划界（§0.3 / §4）。锚点批与普查批在 `b585235` 产出——三 tip src 逐字节相同下全部测量维持有效。
**分支:** `cursor/r31-h-eval-thirty-first-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2**（R28-H…R30-H 同引擎约定，带可比；VM 默认亦解析至 v22.22.2）。

## 结论

**诚实的空枚举：第三十一组没有可立 ID 的新角度，不铸任何 S31-H-\* ID，不铸新 face 名，生产代码零改动（R12-H…R30-H 十九连空之后的第二十连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮按 R31-A/B/D 已确立的元闭合纪律 + R31-F 的「故意不计时结构见证」纪律收口 H 区：从零重读 21 个切片文件与全部 9 个生产消费文件、在本 VM 重建全部锚点（历史仪器逐行复刻，三次独立运行，§1）、对切片生产调用面上**每一个残余参数/选项/枚举平面**做逐格属主审计（§2）。审计对象是三十轮从未单独盘点过的一类物理对象——**边界参数平面**：生产边界函数签名上的枚举参数、可选 options、注入回调与惰性字段各自「取过哪些生产值、剂量空间归谁」。结果是**穷尽归属**——恰四扇残门（D1 precedence 规则臂平面、D2 resolved-set × 问题默认格组成、D3 符号键信任标记机制、D4 死平面流量格），每扇按 R31-A 三步铸轴前置证明逐一裁决，**全部失败于第 1 步（无独立物理对象/零生产流量）或第 2 步（已是既有属主的受控坐标）**，没有一扇到达第 3 步的规模检验而值得铸面。四扇门全部以**故意不计时**的语义见证（25 项断言 ×3 轮全过）+ 生产组成静态普查 + 仅 D3 一组「整调用天花板价」（非剂量、上界任意未来机制替换）关闭：D1 规则臂在生产上**未被读取**（真实合同无冲突早退，`applyPrecedence(real, rule) === real` 引用同一性对全部三规则成立；`src/` 规则字面量普查恰 1 处 `"user-first"`），冲突臂零生产流量按 R31-D 死码先例不铸只停放，且三值枚举实为**二值退化**（`spec-first` ≡ `latest-first` 模规则名标签逐字节同一——`winner = rule === "user-first" ? ordered[0] : ordered.at(-1)` 只认头/尾）；D2 组成剂量空间是 **R29-H R2 覆盖层坐标 (v) 的既有属地**（盈余外来 id ~21.1–21.6 ns/id 已定价、生产盈余流量为零——本轮仅补两条未曾书面化的**划界语义见证**：解析已带默认的问题是 no-op 坐标、投影律不计时重见证），拒绝路 R17-H、故障对象处置 R31-F 门，均不重铸；D3 是切片唯一符号键坐标（`TRUSTED_SOURCE`，铸点 heuristic.ts:177 × 读点 normalizer.ts:44，生产流量每 run 恰 1 铸 + 1 读），机制替换（symbol prop / brand / WeakSet / closure）全部骑在对象形状/IC 机器上 = SHAPEK/R29-A/R31-B hidden-class 停放门同类非独立对象，整调用天花板 **39.7–40.3 ns/铸 + 199–204 ns/normalize**（三轮）⇒ 任何机制变更收益上界 ~0.00024 ms/run，距 ≥10 ms 落地线 **4.6 个数量级**；D4 三格（`assertAuthorityGrounding` 循环、inference 标注 map、注入 extractor/critic 可调用对）生产流量普查全零或既停放（`authority=[]`/`inferences=[]` 生产恒空——可执行见证；注入可调用 = R31-A 停放的 injected-callable 平面 + R28-F BIND/WRAP 停放近旁，不重开）。故本轮**不铸、不计时剂量、不新停放任何带名 face**——四扇门按分解并入 R29-H + R17-H + R31-D 先例 + SHAPEK/R31-B 门类 + R31-A/R28-F 停放地（§2）。空枚举与 R12-H…R30-H 同为战役允许的诚实结局。

关键前提事实：切片 21 文件自 `fd437a9` 逐字节零变化（派单 tip、rebase 后、提交前核对均 **0 行**；`wc -l` 合计 **1750** = 655+712+309+74，evaluation 8 文件确认）。锚点复测（×3，§1）：默认态链 **8889 / 9350 / 8996 ns**（run2 落派单点名的 R30-H 带 9178–9647 内；run1 恰等于 R29-H 带底 8889、run3 落 R29-H 带 8889–9147 内——本 VM 窗口较 R30-H「偏暖窗」整体回落至 R29-H 邻域，三轮全落三十轮历史跨 VM 带 3.9–10.4 µs 内，切片零 diff 为窗口性非结构迁移的最强证据）；配置 A **7908–8858 ns**（三轮全落 R25-H 带 7.55–9.26 µs 内）；配置 B **6557–7099 ns**（三轮全落 R28-H 带 6393–8312 内——B 腿本轮罕见地三轮同带）；promotion **20.8–21.0 ns**（落 R30-H 观测域 19.0–22.2 内、高出 R26-H 带顶 4.5–5.5%——R30-H 已注记的 ns 级窗口项第二次复现）；四个零生产调用方命令类 ≤2.11 µs；critique 类 577–591 ns（R28-H/R29-H 带内或低于带底 ≤1.9%）；冷首调 dist 中位 **1662–1680 µs**（R6-H…R30-H 1.6–1.8 ms 同带，once-per-process 否决类归属不变）。时钟普查恰一命中 `src/rubric/types.ts:35`（R29-E 属地零触碰）。生产调用面 rg 复核与 R3-H…R30-H 恒定（9 文件 7 值级 + 2 纯类型，§0.2）。切片**零 fs 编解码位点**（R31-E 面划界：按令声明后移步，§0.3）；切片 `Math.*` 恰 1 处（critic.ts:43 `Math.max` = BINDX 同坐标非新轴，§0.3）。S5-H-1 三元组两驱动六轮全过。anchor 驱动 12/12 ×3、census 驱动 25/25 ×3 全绿。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；无阈值/测试/公开签名/数据面变更。仓库变更仅本报告一个文件；见证脚本按纪律不入库（census 驱动全文附录 A、anchor 驱动为 R30-H 附录 A 逐行复刻仅头注释更新，附录 B 存 sha256；临时文件已删除）。不开 PR。

## 0. 冻结、快进处置、生产调用面与划界（方法第 1 步）

### 0.1 冻结与快进

- `git diff fd437a9 -- src/evaluation src/requirement src/review src/rubric` = **0 行**（派单 tip `b585235`、rebase 后 `a8de3d1`、提交前三点核对一致）。
- `wc -l`：evaluation 655（8 文件：evaluator 129 / adapters 190 / types 78 / delivery-adapter 61 / precedence 54 / check-adapter 51 / ownership 48 / diff-adapter 44）+ requirement 712 + review 309（pairwise 121 / critic 87 / self-review 54 / reconcile 47）+ rubric 74（registry 37 / types 37）= **1750**。
- 快进处置：`b585235..a8de3d1` 两笔（`90c4365` R31-F 摄取、`a8de3d1` R31-G 摄取）`--stat` 仅 4 个 docs 文件、`':!docs/reports/sota-opt'` 差分 0 行；rebase 快进零冲突；两笔均不触 src ⇒ 在 `b585235` 产出的锚点/普查批全额有效。

### 0.2 生产调用面复核（rg import 搜索，本 tip 实测）

切片外导入位点与 R3-H…R30-H 恒定——**9 文件：7 个值级** = `assertCoverageAllowsStart`×3（run/supervisor L27、run/coordinator L31、run/flowchart-run L21）/ `extractHeuristicContract`（track/clarify L4）/ `applyPrecedence`（track/loop L38）/ `shouldScout`（track/plan L5）/ `assertCanPromoteFromReview`（adaptation/promotion-rules L4），**2 个纯类型** = promotion.ts `ReviewerKind` / primary-split.ts `HeuristicHabits`（clarify.ts 另有 `HeuristicHabits`/`ContractCandidate` 纯类型位点，同文件已计入值级行）。`src/evaluation/` 全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、`src/rubric/` 两文件维持**零生产调用方**。

### 0.3 姊妹面划界（按令声明）

- **R31-E I/O 文本编解码拓扑面**：`rg "readFile|writeFile|from \"node:fs\""` 于 21 文件 = **0 命中**——H 切片没有任何切片内 fs 编解码位点，该面在 H 无对应物理对象，按令声明后移步，不作为 H 轴。
- **BINDX（R31-C）**：切片 `Math.*` 普查恰 1 处——`src/requirement/critic.ts:43` `Math.max(0, score)`（critiqueContract 内、run-start 链上）。这是 realm 级内建绑定态的**同一 BINDX 坐标**，非新轴，零触碰零剂量。
- **R31-F 边界故障通道输出处置门**：H 切片边界的 `DomainValidationError` 抛出（coverage 门 / self-review 断言 / extractor 校验）之后调用方对故障对象的处置，属 R31-F 已分解的同一门类（制造与单价 = R17-H 拒绝路属地）。本轮 D2 见证仅以 `throws()` 布尔探针触碰拒绝路（不计时、不读字段、不保持可达），零重铸。
- **R31-G**：未铸新 face 名，无需增补带名非移植项；其第 4 类裁决（零生产调用方平面不铸面只停放，R31-D 先例引用）与本轮 D1/D4 裁决同法条。

## 1. 锚点全数重测（历史仪器，三轮）

仪器：`/tmp/r31h-anchor.mts`（243 行，R30-H 附录 A 驱动的逐行复刻，仅头注释更新为 R31-H 标注；sha256 见附录 B）。`pnpm build` 后 `node --expose-gc --import tsx /tmp/r31h-anchor.mts`。

| 锚点 | run1 | run2 | run3 | 历史带对照 |
|---|---|---|---|---|
| 默认态链（extract+gate+precedence，Q=0） | 8889 ns | 9350 ns | 8996 ns | R30-H 9178–9647（run2 带内）；R29-H 8889–9147（run1 恰等带底、run3 带内）；R28-H 9006–9483（run2 带内）；历史跨 VM 3.9–10.4 µs 全带内 |
| —— extract 成分 | 7930 | 8393 | 7970 | R29-H 7899–8140（run1/3 带内）；R28-H 8011–8493（run2 带内） |
| —— 满门成分 | 715 | 703 | 773 | R26-H 676–771（run1/2 带内；773 高出顶 0.3%） |
| —— precedence 成分 | 243 | 255 | 254 | R26-H 246–253 / R29-H 248–254（254 带内；255 高 0.4%；243 低于 R26-H 带底 1.2%） |
| 配置 A（habits，Q=1 gated） | 7908 | 8497 | 8858 | R25-H 7.55–9.26 µs 三轮全带内（R30-H 观测域 8102–9642，run1 低于其底 2.4%） |
| 配置 B（vague，Q=3 gated） | 6557 | 7099 | 6732 | R28-H 6393–8312 **三轮全带内**（B 腿宽窗口抖动裁决第四次复现，本轮罕见同带） |
| promotion 类（生产） | 21.0 ns | 20.9 ns | 20.8 ns | R26-H 17.8–19.9（高出顶 4.5–5.5%）；R30-H 观测域 19.0–22.2 全带内——ns 级窗口项二次复现 |
| evaluate 类（零生产调用方） | 1134 ns | 1142 ns | 1100 ns | R28-H 1105–1285（run1/2 带内；1100 低于带底 0.5%，R30-H run3 同款） |
| critique 类（run-start 链内） | 591 ns | 577 ns | 587 ns | R28-H 588–594（591 带内）/ R29-H 582–590（587 带内；577 低于底 0.9%）——较 R30-H 599–605 回落带内 |
| pairwise 类（零生产调用方） | 2040 ns | 2108 ns | 2030 ns | R25-H 2.059–2.361（2108 带内；2040/2030 低于带底 0.9–1.4%） |
| reconcile 类（零生产调用方） | 64 ns | 63 ns | 62 ns | R28-H 63–65（run1/2 带内；62 低 1.6%，ns 级） |
| 冷首调 dist 默认（5 进程中位） | 1680 µs | 1669 µs | 1662 µs | R6-H…R30-H 1.6–1.8 ms 同带（once-per-process 拒收类） |

三轮 CHECKS 12/12 全过 ×3（含 S5-H-1 三元组、pairwise 双向语义、evaluate 确定性 FAIL 语义）。**诚实注记三条**：(a) 本 VM 窗口较 R30-H「偏暖窗」整体回落：默认锚三轮回到 R29-H 邻域（run1 恰等 R29-H 带底），涨落照例集中在 extract 成分（7930–8393，横跨 R29-H/R28-H 两带）——正则/短串路径的既知窗口敏感项（R28-H §1 注记 (b)、R29-H 注记 (a)、R30-H 注记 (a) 同型）；门/precedence 成分至多偏离历史带 ~0.3–1.2%；切片零 diff 为「窗口回落非结构迁移」的最强证据。(b) 同轮次序 default > B 三轮全成立；A−default = **−981/−853/−138 ns**（A 低 1.5–11.0%，三轮同号）——与 R29-H 符号翻转、R30-H A≈default 均属同一 extract 成分窗口涨落族（A 腿三轮全落 R25-H 历史带内），无配置态悬崖；本轮同号只是窗口相位，不改「A≈default 无稳定次序」的跨轮判读。(c) promotion 20.8–21.0 与 R30-H run2/3（21.7/22.2）同为高出 R26-H 顶的 ns 级窗口项，且本轮三轮离散 0.2 ns 内——结构稳定，无迁移。四个零生产调用方命令类 ≤2.11 µs——即便被生产接线也不构成候选池（R30-H 同判）。

## 2. 残余边界参数平面普查（本轮主体：元闭合审计，恰四扇门，全部不铸）

**方法。** 生产调用面 7 个值级边界（§0.2）的**签名参数与选项**逐个盘点：每个参数取过哪些生产值（静态普查 + 可执行见证）、其剂量空间（若当受控变量拉伸）归属哪个既有 face/先例。凡剂量空间无既有属主**且**有独立物理对象**且**有真实规模潜力者才进入铸轴三步证明（R31-A）。盘点结果：`extractHeuristicContract` 的 `objective/sources/habits`、`shouldScout` 的 `objective`、门的 `contract/tasks` 已分别是历轮受控变量（objective 词法类 = TERMCLASS/正则族属地；sources 注入 = D4；habits 配置态 = R25-H 配置态锚；tasks×contract 关联 = R29-H R1）；残余**恰四扇**从未书面化的门，逐扇裁决如下。见证仪器：`/tmp/r31h-census.mts`（233 行，附录 A 全文），**25/25 断言 ×3 轮全过**；除 D3 两条整调用天花板外全部**故意不计时**（R31-F 纪律）。

### 2.1 D1：precedence 规则臂平面（`applyPrecedence(contract, rule)` 的 `rule ∈ {user-first, spec-first, latest-first}`）——第 1 步失败，不铸，停放

**物理对象**：`src/requirement/precedence.ts` L33–58 的规则臂——L41 `winner = rule === "user-first" ? ordered[0] : ordered.at(-1)` 与 L48 假设语句里的 `${rule}` 标签。31 轮从未有任何 H 夹具变动过 `rule` 值（历轮锚点恒 `"user-first"`）。

**生产流量普查**：(i) 静态——`rg '"user-first"|"spec-first"|"latest-first"' src/` 除定义文件外**恰 1 命中** `src/track/loop.ts:80`（字面量 `"user-first"`）；(ii) 语义——真实生产合同（锚形 Q=0 与 vague 形）`detectConflicts(real).length === 0`（heuristic 产出的 observableCheck 词面不含 fast/slow/< 10ms/> 1000ms），故 L35 早退命中，**规则参数在生产流上从未被读取**——见证：`applyPrecedence(real, rule) === real` **引用同一性**对全部三个规则成立（×3 轮）。

**冲突臂语义见证**（不计时，仅立此存照）：合成冲突合同上 `"user-first"` 保留冲突集**头**（ac-fast 存活）、`"spec-first"`/`"latest-first"` 均保留**尾**（ac-slow 存活）且两者输出**模规则名标签逐字节同一**（JSON 替换标签后全等）——三值枚举实为二值退化，枚举平面的真实基数是 2 而非 3。

**裁决**：冲突臂零生产流量 ⇒ R31-D「零生产流量平面不铸面只停放」先例直接适用（R31-G 第 4 类同法条）；有流量的部分（早退路径）就是锚点 precedence 腿 116–255 ns/run 的既有成分（R26-H/R29-H 带内，§1）；常量字面量的实参材料化 = R28-H argument-slot 属地；分支结构 = R18-H 属地。**无独立物理对象 + 无规模**（即便冲突臂被生产接线，单次 filter/Set/spread 是 ns 级）。不铸，停放。S5-H-1（L24 早退）零触碰，三元组两驱动全过。

### 2.2 D2：resolved-set × 问题默认格组成（`assertCoverageAllowsStart` 的 `options.resolvedQuestionIds`）——第 2 步失败（R29-H 属地），仅补划界见证

**属主判定（首要事实）**：R29-H 覆盖关系关联拓扑面**明文包含** R2 关系「resolvedQuestionIds × questions 匹配覆盖层」及坐标 (v)「固定问题集下 resolved 集的盈余外来 id 组成」，并已定价盈余 id ~21.1–21.6 ns/id、越线需 ~46–47 万个盈余 id、生产盈余流量为零（`track/loop.ts` L101–104 恒传精确问题 id 集 `contract.questions.map(q => q.id)` 或已答键集 `Object.keys(applied.resolved)`）。**组成剂量空间是 R29-H 既有受控坐标——在 H 重剂量即重铸计划禁令面，明拒。**

**生产组成普查（本 tip 复核）**：producer 恰 1 处（loop.ts L101–104，二值组成：全 id 集或已答键集——两者都不含重复、不含外来 id）；三个 run 位点（supervisor L506–507 / flowchart-run L765 / coordinator L232–233）均为 `input.resolvedQuestionIds !== undefined ? {resolvedQuestionIds: ...} : undefined` 直通投影，不改组成。

**本轮唯一增量——两条未曾书面化的划界语义见证**（不计时，×3 轮全过）：(i) **已带默认的问题的解析是 no-op 坐标**：双问题合同（q-defaulted 有 default、q-open 无）上，`[q-defaulted]` 仍被拒（q-open 阻塞）、`[q-open]` 即过——L86 首析取使 default 已存在的问题对 resolved 集不敏感（生产 loop.ts 全 id 组成中已答问题 id 的存在因此是语义冗余但无害的字节）；(ii) 投影律重见证：外来 id 单独出现被静默忽略（仍拒）。拒绝路仅以 `throws()` 布尔探针触碰（R17-H 属地 + R31-F 门划界，§0.3）。两条见证不改变属主，仅把 R29-H 面的语义边界书面化。不铸。

### 2.3 D3：符号键信任标记机制（`createTrustedSource` / `TRUSTED_SOURCE`）——第 1/3 步失败，天花板封顶后停放

**物理对象**：切片**唯一**符号键坐标——`normalizer.ts` L17 `Symbol("pi-sparkle.trusted-source")`，铸点 `createTrustedSource`（L27–29：spread + 计算符号属性 + `Object.freeze`），读点 `normalizeSources` L44（`source[TRUSTED_SOURCE] === true`）。31 轮从未有夹具变动过标记机制。**生产流量**：每 run 恰 1 铸（heuristic.ts:177 包 CLI objective）+ 1 读（extractor.ts:75 → normalizeSources，单 source）。

**语义见证**（不计时，×3 轮全过）：标记源 → authority `user` + canGrantAuthority；同字段未标记 → `untrusted-data`；字符串键冒名 `"TRUSTED_SOURCE"` → 仍 untrusted；**同描述外来符号** `Symbol("pi-sparkle.trusted-source")` → 仍 untrusted（符号**同一性**而非描述在承载信任）；铸出对象 `Object.isFrozen` = true。安全语义闭合，任何机制替换须保全这五条。

**天花板价**（整调用、生产形、非剂量——不变动任何机制变体，故 SHAPEK/R29-A 属地零触碰；上界**任意**未来机制替换的收益）：`createTrustedSource(生产形)` = **39.7 / 40.3 / 39.7 ns/铸**；`normalizeSources([marked])`（含符号读 + 3 条 signal 正则 + slice(0,200)）= **201 / 199 / 204 ns/调用**（三轮）。合计 ≤ **~245 ns/run** ⇒ 即便整个机制免费化，收益 0.000245 ms/run，距 ≥10 ms 落地线 **4.6 个数量级**。

**裁决**：机制替换的性能表现全部经由对象形状/hidden-class/IC 机器 = SHAPEK 属地 + R31-B 停放的 hidden-class/IC-shape 门同类，**非独立物理对象**；且 once-per-run ns 级**无规模**。不铸，天花板封顶后停放。

### 2.4 D4：死平面流量格（authority 接地循环 / inference 标注 map / 注入可调用对 / 置信阈值）——第 1 步失败，零剂量裁决

可执行流量见证（×3 轮）：生产合同（锚形与 vague 形）`authority.length === 0` ⇒ `assertAuthorityGrounding`（extractor.ts L117–132）循环**每 run 0 次迭代**；`inferences.length === 0` ⇒ 标注 map（L84–93）**每 run 0 次迭代**——heuristic 提取器 L140/L148 恒发空集，两格零生产流量，R31-D 先例适用，不铸。`buildContractCandidate` 的注入 `extractor`/`critic` 可调用对 = R31-A 停放的 injected-callable 平面 + R28-F BIND/WRAP 停放近旁——**不重开**（无新独立物理对象：生产恒 heuristicExtractor/heuristicCritic 单形注入）。`minimumConfidence`（生产恒 0.8）= 阈值硬保持禁区 + 单生产值配置平面（R31-D 同类），零剂量。role-id 守卫两条（L68–73）once-per-run ns 级字符串比较，无规模。

**穷尽性说明**：至此生产调用面 7 个值级边界的全部签名参数（objective/sources/habits/contract/tasks/options/rule/reviewer 三元组）与内部注入点均有属主或裁决书面化。`assertCanPromoteFromReview` 的 reviewer 三元组平面（kind 枚举 × id 等值）由 R26-H promotion 锚承载且 17.8–22.2 ns 全域封顶（§1），不足门。零生产调用方的 13 个文件（§0.2）按 R30-H「即便接线也非候选池」判定维持在池外。**没有余项。**

## 3. 本轮不做什么（否决摘要）

- 不重铸/不重剂量：R30-H 证据溯源路径面、R29-H 关联拓扑面（含 R2 覆盖层——D2 明拒）、R28-H argument-slot、R27-H 执行基质、R18-H 分支、R17-H 拒绝路、R26-H promotion 锚、R25-H 配置态、R13-H 规模轴。
- 不移植：BINDX（Math.max 恰 1 位点声明即止）、R31-E codec 面（0 位点声明即止）、R31-A injected-callable（D4 引用即止）、R31-B 六停放门（hidden-class 门在 D3 作属主引用即止）、R31-D 配置平面先例（作为 D1/D4 法条引用）、R31-F 故障处置门（D2 划界即止）、R31-G（无新名）。
- 不修「正确性观察」：`createEvaluationRecord` 的 `input.evidence[criterion.id]` 原型链回退观察（zero-caller 文件、且属正确性/fail-open 家族非性能面——家族保持关闭）；npm POSIX `.bin` 符号链接静默 no-op 不修（R28-I）。
- 不动时钟位点 rubric/types.ts:35（R29-E 属地）；不触 PATH_RE（重开条件未触发：`src/` 零提交 ⇒ 无程序化/对抗 objective、无 ≥3.2K 无斜杠载荷）。

## 4. 非移植清单（增补后全表）

命名面：FITQ、NAMESHAPE、SCALEX、XPROC、YMIX、SCHEDWIN、KFAN、NVG、SEEDX、TERMCLASS、SHAPEK、ORDX、CKMIX、CALLB、OCCX、JB、RUNLIM、VALCLASS、CG、AGEX、COTARG、PRICEX、RTL、DELINV、PROBX、RVL、SIDEC、PWL、ZREP、PAIRX、CSPELL、REPRX、LINKTOP、**BINDX**（R31-C）。
H 本地已关面：证据溯源路径拓扑 / provenance-resolution pathway（R30-H）、coverage-relation incidence topology（R29-H）、argument-slot（R28-H）、execution-substrate（R27-H）。
姊妹无名面/停放门：R31-E I/O 文本编解码拓扑 / 每 run 转码字节清单面（H 零位点，§0.3）；R31-A injected-callable 平面；R31-B 六停放门（catalog-row hidden-class/IC-shape、cascade evidence×failure-class 格、assignTasks 冷启动分段、batch 类组成混合、LINKTOP-in-B、prior 别名解析路径）；R31-D 残余监视器配置平面（R22-D 属地）；**R31-F 边界故障通道输出处置门（本轮新增：已分解并入 R17-F/R30-F/R29-F/引擎面——H 侧对应物按 §0.3 划界）**；R31-G 无新名（其穷尽归属五类裁决法条已在 §2 引用）。
本轮四扇门（D1–D4）**未铸名**：按属主分解/停放记录于 §2，供后轮直接引用，无需计划禁令新条目。

## 5. 硬保持核对清单（生产零 diff 下逐条成立）

S5-H-1 保留（precedence.ts L24 逐字在位 + 三元组两驱动 ×6 全过）；PATH_RE 未触；分析不改 in-flight run / tracking 无命令权 / H-score 不写路由 PASS/FAIL；双 LCB（beta+normal）与双归因（logit+prob-add）都在；提升 proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值变更（minimumConfidence 0.8 零触碰）、不改测试适配重构；CAS/凭据/数据面契约/公开签名未触；无 fsync/完整性再哈希触碰（本切片无该面）；fail-open 家族保持关闭（D2 拒绝路仅布尔探针）；S7-F-1 ≠ S6-F-5、S8-I-1 文件级封锁、S9-D-4/S12-D-1 toLowerCase 跳过、S8-E-1 不去重 loadLearnedRouting、S12-B-2 条件着陆未触发、S13-B-1 loser ID、J1 锁定——均未涉足；catalog 行序契约（R28-B W3）未涉足；npm `.bin` no-op 不修。测量全程 nvm Node v22.22.2。生产代码零改动，无需跑聚焦套件/gate 之外的新增仿真族（本轮 `pnpm build` 全绿以产出 dist 冷探针）。

## 6. MORE_OPTIMA 判定

R12-H…R30-H 十九连空后，本轮把三十轮从未盘点的「边界参数平面」类物理对象逐格审计穷尽：恰四扇残门，全部在铸轴三步证明的第 1 或第 2 步失败——零生产流量（D1 冲突臂、D4 全部）、既有属主（D2 = R29-H R2、D3 = SHAPEK/hidden-class 门类）、once-per-run ns 天花板（D3 ≤245 ns/run，距落地线 4.6 个数量级）。锚点三轮全落历史带、冻结 0 行、生产调用面恒定。**没有可落地的更优解；切片维持关闭。**

## 附录 A：普查驱动 `/tmp/r31h-census.mts`（233 行，sha256 `0f69d3ecabacd4ec8b52251aa571544ffeff75203edb8bf8252a39400259bda6`）

运行：`node --expose-gc --import tsx /tmp/r31h-census.mts`（cwd = /workspace）。三轮输出：`CEILING D3 trust-marker mechanism: createTrustedSource(prod shape)={39.7|40.3|39.7}ns/mint + normalizeSources([marked], ...)={201|199|204}ns/call` + `CHECKS 25/25 passed`。

```ts
/**
 * R31-H RESIDUAL BOUNDARY PARAMETER-PLANE CENSUS driver (thirty-first pass).
 *
 * Not a new-axis dose. This is the executable half of a closure audit in the
 * R31-A/R31-B/R31-D style: every remaining parameter/option/enum plane on the
 * slice's production call surface is adjudicated (owned / production-dead /
 * once-per-run ns ceiling) with untimed semantic witnesses plus whole-call
 * ceiling prices on production shapes only. No plane is varied as a
 * controlled variable at fixed everything-else (that would remint R29-H R2 /
 * SHAPEK / R18-H); ceilings bound ANY future change of the plane from above.
 *
 * Ordering discipline (R27-H appendix-C): all timed ceilings run FIRST on
 * production shapes; exotic witness shapes (conflicted contracts, impostor
 * markers) are created only AFTER timing.
 *
 * Run: node --expose-gc --import tsx /tmp/r31h-census.mts   (from /workspace)
 */
import { performance } from "node:perf_hooks";
import { extractHeuristicContract } from "/workspace/src/requirement/heuristic.ts";
import { assertCoverageAllowsStart, type CoverageTaskRef } from "/workspace/src/requirement/coverage.ts";
import { applyPrecedence, detectConflicts, type PrecedenceRule } from "/workspace/src/requirement/precedence.ts";
import {
  createTrustedSource,
  normalizeSources,
  type RawSource
} from "/workspace/src/requirement/normalizer.ts";
import type { RequirementContract } from "/workspace/src/domain/contract.ts";

const OBJ = "fix the login retry bug in src/auth/session.ts and keep tests green";
const forceGc = (globalThis as { gc?: () => void }).gc;
function gcBarrier(): void {
  if (forceGc !== undefined) {
    forceGc();
    forceGc();
  }
}
let checks = 0;
let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function bench(fn: () => void, reps: number): number {
  fn();
  gcBarrier();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
function throws(fn: () => void): boolean {
  try {
    fn();
    return false;
  } catch {
    return true;
  }
}

/* ================= PHASE 1: timed ceilings, production shapes only ====== */

/* D3 ceiling — trust-marker mechanism, whole-call price on the exact
 * production composition (heuristic.ts L177 mint shape; 1 mint + 1
 * normalize-read per run). A whole-call price is an upper bound on ANY
 * marker-mechanism substitution (symbol prop / brand / WeakSet / closure):
 * no mechanism variant is dosed, so SHAPEK / R29-A stay untouched. */
const PROD_RAW = {
  kind: "message" as const,
  ref: "cli-objective",
  origin: "user-turn" as const,
  content: OBJ
};
const mintNs = bench(() => void createTrustedSource(PROD_RAW), 200000) * 1e6;
const markedProd = createTrustedSource(PROD_RAW);
const normalizeNs = bench(() => void normalizeSources([markedProd]), 100000) * 1e6;
log(
  `CEILING D3 trust-marker mechanism: createTrustedSource(prod shape)=${mintNs.toFixed(1)}ns/mint + normalizeSources([marked], 1 source incl. symbol read + 3 signal regexes + slice)=${normalizeNs.toFixed(0)}ns/call; production flow = 1 mint + 1 normalize per run`
);

/* D1 flowed-portion price is the anchor driver's precedence leg (conflict-
 * free early return) — cited from /tmp/r31h-anchor.mts output, not re-dosed
 * here. D2 composition prices are R29-H property (R2 overlay, ~21ns/surplus
 * id) — not re-dosed. */

/* ================= PHASE 2: untimed semantic witnesses ================== */

/* ---- D4: dead-plane flow census on the real production contract ---- */
const cand = await extractHeuristicContract({ objective: OBJ });
const real = cand.contract;
check("D4 production contract authority=[] (assertAuthorityGrounding loop 0 iterations/run)", real.authority.length === 0);
check("D4 production inferences=[] (extractor labeling map 0 iterations/run)", cand.inferences.length === 0);
const vague = await extractHeuristicContract({ objective: "fix bug please" });
check("D4 vague-shape contract authority also []", vague.contract.authority.length === 0);

/* ---- D1: precedence rule-arm plane ---- */
const RULES: readonly PrecedenceRule[] = ["user-first", "spec-first", "latest-first"];
check("D1 production contract is conflict-free", detectConflicts(real).length === 0);
for (const rule of RULES) {
  check(
    `D1 applyPrecedence(real, "${rule}") returns the SAME REFERENCE (rule coordinate unread in production flow)`,
    applyPrecedence(real, rule) === real
  );
}
/* exotic conflicted shape (created after all timing) */
const mkConflicted = (): RequirementContract =>
  ({
    schemaVersion: 1,
    objective: "t",
    deliverables: [],
    constraints: [],
    nonGoals: [],
    acceptanceCriteria: [
      { id: "ac-fast", description: "latency", observableCheck: "response is fast", sourceRefs: [] },
      { id: "ac-mid", description: "neutral", observableCheck: "output matches spec", sourceRefs: [] },
      { id: "ac-slow", description: "batch", observableCheck: "batch job is slow", sourceRefs: [] }
    ],
    assumptions: [],
    questions: [],
    authority: [],
    sourceRefs: []
  }) as unknown as RequirementContract;
const conflicted = mkConflicted();
const byRule = Object.fromEntries(RULES.map((rule) => [rule, applyPrecedence(conflicted, rule)])) as Record<
  PrecedenceRule,
  RequirementContract
>;
const keptIds = (c: RequirementContract): string => c.acceptanceCriteria.map((a) => a.id).join(",");
check(
  'D1 conflict arm: "user-first" keeps HEAD of conflict set (ac-fast survives, ac-slow dropped)',
  keptIds(byRule["user-first"]) === "ac-fast,ac-mid"
);
check(
  'D1 conflict arm: "spec-first" keeps TAIL (ac-slow survives)',
  keptIds(byRule["spec-first"]) === "ac-mid,ac-slow"
);
check(
  'D1 conflict arm: "latest-first" keeps TAIL (ac-slow survives)',
  keptIds(byRule["latest-first"]) === "ac-mid,ac-slow"
);
check(
  "D1 rule-arm DEGENERACY: spec-first ≡ latest-first byte-identical modulo the rule-name label inside assumption statements",
  JSON.stringify(byRule["spec-first"]).replaceAll("spec-first", "R") ===
    JSON.stringify(byRule["latest-first"]).replaceAll("latest-first", "R")
);
check(
  "D1 rule-arm binary: user-first differs from the tail pair in kept-criteria set",
  keptIds(byRule["user-first"]) !== keptIds(byRule["spec-first"])
);

/* ---- D2: resolved-set × question-default law (demarcation witnesses only;
 * composition axis is R29-H R2 property — nothing here is timed) ---- */
const mkGateContract = (): RequirementContract =>
  ({
    schemaVersion: 1,
    objective: "t",
    deliverables: [],
    constraints: [],
    nonGoals: [],
    acceptanceCriteria: [{ id: "ac-1", description: "d", observableCheck: "manual", sourceRefs: [] }],
    assumptions: [],
    questions: [
      { id: "q-defaulted", question: "a?", options: ["x", "y"], default: "x" },
      { id: "q-open", question: "b?", options: ["u", "v"] }
    ],
    authority: [],
    sourceRefs: []
  }) as unknown as RequirementContract;
const gateContract = mkGateContract();
const gateTasks = [
  { id: "tsk_00000001", acceptanceCriteria: [{ id: "ac-1" }] }
] as unknown as CoverageTaskRef[];
const gate = (ids: readonly string[] | undefined): boolean =>
  !throws(() =>
    assertCoverageAllowsStart(gateContract, gateTasks, ids === undefined ? undefined : { resolvedQuestionIds: ids })
  );
check("D2 no options -> blocked (q-open has no default)", gate(undefined) === false);
check("D2 resolve only q-open -> passes (defaulted question needs no resolution)", gate(["q-open"]) === true);
check("D2 resolve only q-defaulted -> still blocked (resolving a defaulted question is a NO-OP coordinate)", gate(["q-defaulted"]) === false);
check("D2 resolve both -> passes", gate(["q-defaulted", "q-open"]) === true);
check("D2 foreign id alone -> blocked (projective law, R29-H-owned coordinate re-witnessed untimed)", gate(["q-nonexistent"]) === false);

/* ---- D3 semantic: marker is symbol-IDENTITY keyed, frozen, unforgeable by
 * string key or same-description foreign symbol ---- */
check("D3 marked production source -> authority user + canGrantAuthority", (() => {
  const n = normalizeSources([markedProd])[0];
  return n !== undefined && n.authority === "user" && n.canGrantAuthority;
})());
check("D3 unmarked identical fields -> untrusted-data", (() => {
  const n = normalizeSources([{ ...PROD_RAW }])[0];
  return n !== undefined && n.authority === "untrusted-data" && !n.canGrantAuthority;
})());
check("D3 impostor STRING key 'TRUSTED_SOURCE' -> still untrusted", (() => {
  const impostor = { ...PROD_RAW, TRUSTED_SOURCE: true } as unknown as RawSource;
  const n = normalizeSources([impostor])[0];
  return n !== undefined && n.authority === "untrusted-data";
})());
check("D3 foreign symbol with SAME description -> still untrusted (identity, not description)", (() => {
  const impostor = { ...PROD_RAW, [Symbol("pi-sparkle.trusted-source")]: true } as unknown as RawSource;
  const n = normalizeSources([impostor])[0];
  return n !== undefined && n.authority === "untrusted-data";
})());
check("D3 minted source is frozen", Object.isFrozen(markedProd));

/* ---- S5-H-1 attestation triple (unchanged semantics) ---- */
const mkLat = (checks_: string[]): RequirementContract =>
  ({
    schemaVersion: 1,
    objective: "t",
    deliverables: [],
    constraints: [],
    nonGoals: [],
    acceptanceCriteria: checks_.map((oc, i) => ({
      id: `ac_0000000${i}`,
      description: "d",
      observableCheck: oc,
      sourceRefs: []
    })),
    assumptions: [],
    questions: [],
    authority: [],
    sourceRefs: []
  }) as unknown as RequirementContract;
check("S5-H-1 fast-only -> []", detectConflicts(mkLat(["response is fast", "check completes < 10ms"])).length === 0);
check("S5-H-1 slow-only -> []", detectConflicts(mkLat(["batch job is slow", "runs > 1000ms"])).length === 0);
check("S5-H-1 fast+slow -> exactly 1", detectConflicts(mkLat(["response is fast", "batch job is slow"])).length === 1);

log(`CHECKS ${checks - failures}/${checks} passed`);
if (failures > 0) process.exit(1);
```

## 附录 B：仪器与原始输出台账（临时文件，按纪律不入库、已删除）

| 文件 | 行数 | sha256 | 处置 |
|---|---|---|---|
| `/tmp/r31h-anchor.mts` | 243 | `eecdcdce8f07f9aab9fffdf3fdd4dfcdb4b836cf1b5979544dd6b241a2c2a8c5` | R30-H 附录 A 驱动（sha256 `b0251e6f…`）的逐行复刻，测量段逐字未动，仅头注释块更新为 R31-H 标注（sha 差异全部来自头注释）；全文见 R30-H 附录 A，不在此重复 |
| `/tmp/r31h-census.mts` | 233 | `0f69d3ecabacd4ec8b52251aa571544ffeff75203edb8bf8252a39400259bda6` | 全文存档于附录 A 后删除 |
| `/tmp/r31h-anchor-run{1,2,3}.log` / `/tmp/r31h-census-run{1,2,3}.log` | — | —（六轮原始输出，数字已全额转录 §1/§2） | 删除 |

MORE_OPTIMA=no
BRANCH=cursor/r31-h-eval-thirty-first-pass-83a1
