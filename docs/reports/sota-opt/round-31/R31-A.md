MODEL_SLUG=claude-fable-5-thinking-xhigh

# R31-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第三十一遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 31 / R31-A（Round 1–30 同区第三十一遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `3f80ab6`（R30-H 空枚举收口 + Round 31 开轮 + 本 R31-A 派发）。运行中 exclusive docs-only 快进**两次**：`3f80ab6` → `810b141`（R30-I 空枚举收口——新归档命名轴 **process-lifetime temporal-segment topology face（进程生命期时间段拓扑面）**，I 切片 CLI 进程 spawn-to-exit 的 lead/span/tail/close 分段，tail 为引擎退出地板——+ R31-B 派发）→ `dabf551`（R30-J 空枚举收口——新归档命名轴 **durable-artifact referent-binding / filesystem link-topology face**（观察绰号 **LINKTOP**，不编号），J 切片持久工件的文件系统指称绑定/链接拓扑——+ **Round 30 关轮** + R31-C 派发）。两笔增量逐次核对：`git diff --stat 3f80ab6..810b141` **仅触及 docs/reports/sota-opt/ 下 4 个文件（PROGRESS / round-30/PLAN / R30-I.md / round-31/PLAN，708 插入 15 删除）**，`git diff --stat 810b141..dabf551` **仅触及同目录 4 个文件（PROGRESS / round-30/PLAN / R30-J.md / round-31/PLAN，1302 插入 13 删除）**，对 `src/`、`test/`、`scripts/`、`package.json`、`pnpm-lock.yaml` 两笔均**零行**（`git diff <base>..<tip> -- src test scripts package.json pnpm-lock.yaml | wc -l` = 0 逐次直接复核）；按令逐次快进/rebase 本分支到新 tip，14 文件冻结 `git diff 7acb666..HEAD -- src/tracking src/run/child-tracking.ts src/run/gate-apply.ts` 在派发 tip、两个新 tip、提交前**四验均为 0 行**，两条新收口兄弟轴已补入 §0 不移植栏并完成划界。三次官方仿真与聚焦套件在 `3f80ab6` 的工作树完成——快进增量 `src/` 零变化 ⇒ 测量在字节相同的切片上完成，全部有效；`pnpm gate` 在三 tip 各跑一次**均 exit 0（计数逐字同）**，聚焦套件在两个新 tip 各复跑一次同计数。Round 30 已关轮；R31-B / R31-C 在本轮收尾时仍在飞，按令未等待。
**分支:** `cursor/r31-a-tracking-thirty-first-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S31-A-\* ID，切片零代码
改动；整片预算收口第二十九次复核成立。本轮不铸新轴——第三十一遍
枚举以一次「元闭合审计 + 一扇停靠门」收口。** R30-A 关掉调用流
时序半边之后，「在一个 Node 进程内调用一个纯同步函数」这件测量行为
的元坐标系已被三十遍 A 轮 + 兄弟切片归档轴满覆盖（喂什么 / 喂多少
什么混合 / 什么结局 / 什么驻留与 H 面 / 什么值与环境 / 什么键集
几何与引用结构 / 以什么次序与什么进程史 / 经什么协议抵达边界 /
输出往哪去 / 时间从哪来 / 进程生命期怎么分段——逐面属主见 §1.3
审计表）。本轮逐面审计后剩下**唯一一块从未单独受控的平面：门经什么
可调用物注出效应——injected-callable 平面**（`generateEventId` 是
门链全部三个公开入口唯一的注入效应通道；`readers.readToolBodies`
是第二个注入可调用物但生产零流量）。按令对它做了铸轴前置证明，
**证明失败于「独立物理对象」一步，故停靠不铸**，三重理由全部实测
归档：

1. **生产简并（F11a 普查，静态只读，三 run 逐字节一致）**：生产在
   该平面只占**恰好一个坐标**——每调用新铸单分配位点箭头闭包。
   coordinator.ts:444 / flowchart-run.ts:320 在 options 字面量内
   新铸箭头；child-tracking.ts 对调用方参数纯透传；supervisor.ts
   两个生产 settle 位点各自新铸箭头**且均不带 trackingAssessment**
   ⇒ 生产 supervisor 门腿是 O(1) no-op 早退（第二十二次复验）。
   全平面零 `.bind`、零包装工厂、零共享长寿生成器；锚点血统
   （每调用新铸箭头）恰好坐在生产坐标上 ⇒ 三十一遍预算测量对
   该平面的生产坐标**按构造就是天花板口径**。readers 可调用物经
   turn.ts:114 的 `gate.expandDetail && input.readers?.readToolBodies
   !== undefined` 双守卫且 from-child 生产路径不传 readers ⇒
   生产零流量（实测断言）。
2. **语义不可见（F11b 证人，三 run 全过，故意不计时）**：同一 id
   流下，新铸箭头 / 跨调用共享闭包 / 预绑定函数三种可调用物身份
   驱动同一门格，result + 追加字节 **逐位一致**（3 臂 × 100 调用
   = 300 调用）。门把可调用物当不透明 id 源消费；身份与种类在
   语义面结构性不可见。
3. **价不独立（停靠的裁决理由）**：给三臂计时得到的任何 µs 都只能
   归因到**别处已有属主的轴**上——(a) 可调用物「种类」（直呼闭包
   vs bound trampoline vs 包装转发）正是 R28-F **boundary
   call-protocol face** 的调用适配器平面在被调侧的镜像（R28-F 的
   BIND/WRAP 格与本平面字面重叠；R29-H 已把「扩展点函数种类」方向
   **停放**于 R28-F 邻域，R30-H 复审后**维持停放**——A 侧铸轴
   等于未经授权解除停放）；(b) 「共享闭包跨调用复用」= 输入图上
   可调用物边的引用共享，属 R29-A **input reference-identity /
   aliasing-topology face**（禁移植）；(c) 门内部 `generate()`
   调用位点因可调用物种类不同而受的 IC/分支历史训练差，属 R30-A
   **call-stream ordering / dispatch-history face**（上一轮 A 轴，
   禁重铸）。三重再剂量 ⇒ 无独立物理对象 ⇒ 按派单令**停靠此门并
   写明原因**，不铸 ID、不起绰号、不加 EXCLUSIONS 行。

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R30-A 至本轮三 tip
（`3f80ab6` / `810b141` / `dabf551`）**逐字节未变**（`git diff
7acb666..HEAD -- <切片>` 四验均空，三十一遍全程零 diff；清单核对恰
14 文件（12 + 2）并全部实际重读，`wc -l` 合计 1796 行与 R30-A /
R29-A 一致）。调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）
以来 `git diff 89f0455..dabf551 -- src/` **完全为空**（跨 R10…R31
共二十二段全部提交均为 docs，含本轮快进带入的 R30-I / R30-J 收口）。生产调用方经本轮全库
grep 复核仍为 `src/run/supervisor.ts:483`（applyTrackingGate；:487
nextTrackingSeq）/ `src/run/coordinator.ts:444` /
`src/run/flowchart-run.ts:320`（applyChildThreeLine），每子结果一次
（~5 次/run），事件表几十级（41）。R30-A 的预算天花板（12.8–15.3
µs/gate ⇒ ~64–77 µs/run）经本轮在当前 VM（Node v22.22.2，PATH 前置
nvm）实测复核为 **13.6–16.9 µs/gate ⇒ ~68–84 µs/run**（五格锚点
矩阵三次官方运行；区间与 R27-A（68–84）/ R26-A（69–84）历史带完全
重合，与 R30-A（64–77）/ R29-A（61–80）/ R28-A（61–76）交叠同带；
五格三 run 保序（P_C < F_C < F_C2 < F_NC < P_NC，与 R30-A 逐格
同构），相对 R30-A 窗口整体上移 ~4–11%（同 VM 不同会话，五格同向
⇒ 会话窗口非结构变化），无孤点、无悬崖；分解组件绝对值三次全落
R30-A 带内或贴带 ±10% 以内，逐项注记见 §1 表）。量级结论不变——
稳态下即使切片零成本，节省上界 ~0.07–0.08 ms/run，仍比落地线
（数十~数百 ms）低约**三个量级**。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点
或功能性变更（5 项新记录，见 §0；R12-A…R30-A 共 97 项换名检查全部
复核维持，未追认任何 S12…S30-A-\* ID），按令一律不铸新 ID。R12-A
分解的五大组件占比（hashAssessment×3 ~41–42% / validateEvent
~21–22% / turn ~12% / prescore×2 ~9–10% / O(E) 五扫描+拷贝 ~2.0%，
以当轮 contract 格 13.6–14.2 µs 计）在本 VM 逐一复现，R18-A skip
定价与 R19-A replay 定价抽测复现同带（guard-3 skip 1983–2044 ns =
14.0–15.0% of apply；replay find#1 3104–3148 ns = 22.2–22.9% of
apply）。第三十一遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**
  （`wc -l` 合计 1796 行）。未触碰其余 `src/run/`（G 切片）及任何
  其他切片；F11a 普查对 coordinator.ts / flowchart-run.ts /
  supervisor.ts 三个调用文件做了**只读**静态见证（调用点上下文
  实读，零编辑）。未触碰在飞的 R31-B / R31-C 分支（R30-I.md /
  R30-J.md 仅只读实读用于快进后划界）。
- 先读并遵守（顺序强制）：README.md / EXCLUSIONS.md（全表 545 行，
  含 S13-B-1；R12-A…R30-A 空枚举未铸 ID，确认）/ PROGRESS.md /
  round-31/PLAN.md（A 段 + 全部 plan-ban 轴，快进前后两版）/
  R30-A 全文（上一轮 A 轴定义 + 新轴方法学 + 预算锚点血统——本轮
  锚点矩阵与分解仪器逐行沿用其血统）/ R29-A 全文（aliasing-
  topology face——本轮停靠门 (b) 腿的属主标尺）/ R30-H 结论 +
  新轴划界（provenance-resolution pathway face——不重铸；其 §3
  「维持停放 R29-H 扩展点函数种类方向」段是本轮停靠裁决的直接
  上位依据）/ R29-H 相应停放段 + R28-F 关键段（boundary
  call-protocol face 八适配器 + BIND/WRAP 机制归因——停靠门 (a)
  腿的属主标尺）/ R30-I 结论与划界段（第一次快进带入后按令补读补
  划界）/ R30-J 结论与划界段（第二次快进带入后按令补读补划界）。
  EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
- 环境：本 VM PATH 前置 `~/.nvm/versions/node/v22.22.2/bin`
  （Node v22.22.2，满足 engines >=22.19.0），tsx 4.23.12、
  TypeScript 5.9.3、pnpm 10.17.1，`pnpm install --frozen-lockfile`
  干净完成，全部测量在 v22.22.2 完成。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：
  R30-A call-stream ordering / dispatch-history face（A 上一轮，
  不重铸——本轮零次序臂、零计时新臂；F11b 故意不计时的第一重
  理由即避免在门内部调用位点再剂量分派历史）/ R29-A input
  reference-identity / aliasing-topology face（不重铸——本轮全部
  锚点/分解夹具沿用血统对象跨臂共享引用、引用结构钉死；F11b 的
  共享闭包臂是**语义证人**非剂量——不计时故不定价可调用物边的
  别名结构）/ R28-A input key-set geometry face（键集恒
  schema-精确声明序）/ R27-A scalar numeric value-class face
  （数值恒生产值类）/ R26-A collation-locale face（locale 恒 en
  族）/ R30-H provenance-resolution pathway face（H 收口：合同项
  溯源举证通道——本切片门链不解析溯源，prescore 的
  coverageOutcome 恒同输入，零剂量）/ R29-H coverage-relation
  incidence topology face（AC×准则集关联结构恒定单准则生产形）/
  R30-G M1 parent-run child-scheduling lattice（零监督循环、零
  child 调度语义——本轮连测量容器子进程都没有）/ R29-G M2
  supervised-engine adjudication lattice（零 judge、零裁决格）/
  R29-F boundary output-retention / superseded-snapshot liveness
  face（全部输出每调用即弃且逐臂恒等，零保留剂量）/ R30-F
  boundary output-consumption / snapshot-readback topology face
  （F 收口：输出消费/快照读回拓扑——本轮输出仅做 F11b 的
  `JSON.stringify` 逐位相等断言，消费投影逐臂恒等且为证人仪器
  非剂量变量）/ **R30-I process-lifetime temporal-segment
  topology face**（I 收口，第一次快进带入，全文实读，专项划界
  见下第 4 条）/ **R30-J durable-artifact referent-binding /
  filesystem link-topology face（LINKTOP，不编号）**（J 收口，
  第二次快进带入，结论与划界段实读——J 切片持久工件的文件系统
  指称绑定/链接拓扑：变量在磁盘链接结构与指称解析；本切片数据
  路径**零磁盘 I/O、零文件指称**（与 R28-J ZREP 划界同据，
  types.ts 哈希为内存对象纯函数），不存在可剂量的链接拓扑坐标
  ——剂量平面正交，不移植、不补铸、绰号不编号）/ R28-J
  **ZREP**（磁盘零态表征——本切片数据路径零磁盘 I/O）/ R29-C
  **PAIRX**（单一工作负载组成）/ R29-J
  **CSPELL** / R30-C **REPRX**（切片零 `JSON.parse` 输入面，
  `parseTrackingAssessment` 收内存对象非字节流）/ R28-F boundary
  call-protocol face——**最近邻，专项划界见下第 1 条**。FITQ /
  NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG /
  SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB /
  RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV /
  PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX /
  LINKTOP 未编号；本轮停靠门**不取 ALLCAPS 绰号、不作轴名**。
- **与四个最近邻的专项划界**（injected-callable 平面停靠裁决的
  三条属主腿 + 快进新轴）：
  1. **vs R28-F「boundary call-protocol face」**（禁移植，最近
     邻）：R28-F 定价的是**控制流经什么 ECMAScript 调用适配器
     抵达切片公开边界函数**（DIRECT/LOCAL/CALL/APPLY/REFLECT/
     BIND/SPREAD/WRAP 八格，F 区裁决面平坦）；本轮停靠门问的是
     **边界函数经什么可调用物注出效应**——同一调用协议平面的
     被调侧镜像，BIND（预绑定）与 WRAP（包装转发）格字面重叠。
     R29-H 已把「扩展点函数种类」方向停放于 R28-F 邻域、R30-H
     复审维持停放；A 侧若对三种可调用物身份计时铸轴 = 未经授权
     解除该停放 + 跨区移植。**维持停放，扩展其覆盖到 A 切片的
     注入实例，不铸、不移植、不计时。**
  2. **vs R29-A「input reference-identity / aliasing-topology
     face」**（A 前轮，不重铸）：R29-A 剂量输入对象图的边共享
     结构并裁决语义面免疫、面平坦；「同一个闭包对象跨 100 次门
     调用复用」正是可调用物边上的引用共享坐标。F11b 的共享闭包
     臂只做逐位等价断言（语义证人），**不计时** ⇒ 零别名剂量。
  3. **vs R30-A「call-stream ordering / dispatch-history face」**
     （A 上一轮，不重铸）：R30-A 已把进程内 IC/分支/缓存历史当
     生产流坐标定价（µs 级拒收）。可调用物种类对 gate-apply 内部
     `generate()` 调用位点的 IC 训练差是同一分派历史机制的又一
     实例——计时即重铸。F11b 不计时 ⇒ 零剂量。
  4. **vs R30-I「process-lifetime temporal-segment topology
     face」**（快进带入的最新收口兄弟轴，全文实读后划界）：
     R30-I 剂量 **CLI 子进程 spawn-to-exit 的生命期分段**（lead
     / span / tail / close；tail 为引擎退出地板，双引擎截断见证
     否决硬退出）——变量在进程边界的时间结构。本切片门链是
     **进程内纯同步函数**，本轮零子进程（连 R30-A 19e 那样的
     测量容器子进程都没有），不存在可剂量的生命期分段坐标；
     切片数据路径也不含任何 `process.exit` / 退出腿。剂量平面
     正交（进程生命期时间段 vs 进程内调用微秒），零交集，不
     移植、不补铸。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案
  换名/换位点或功能性变更，**一律不铸 S31-A-\* ID**。R12-A…R30-A
  共 97 项换名检查全部复核维持，不再逐条重列；以下 5 项为本轮
  **新记录**，全部由 injected-callable 平面审计引出）：
  1. **调用点闭包提升/共享**（把 coordinator.ts:444 /
     flowchart-run.ts:320 每调用新铸的箭头提升为模块级共享生成
     器，省一次闭包分配）= 切片外调用方变更（两站点属 G 切片）+
     收益为每 gate 一次闭包分配（10¹ ns 量级）⇒ µs/ns 拒收类
     双料；且共享化恰好把生产从单坐标挪进 R29-A 别名面的非平凡
     坐标（方向存疑），拒列；
  2. **generateEventId 预绑定/方法化**（`.bind` 或对象方法替代
     箭头）= R28-F BIND 格在被调侧的镜像（禁移植 + R29-H/R30-H
     停放维持）+ ns 级 + F11b 已证语义零差，拒列；
  3. **id 预生成/池化**（预铸 id 数组消除 per-append 回调分派）
     = X1-1 隐藏缓存本体 + replay 契约功能性风险——重放臂**不得
     铸 id**（本轮 replay 抽测用 throwing generator 实证重放
     路径零调用），预生成使 id 铸造脱离追加事实，拒列；
  4. **readers 死参裁剪**（`runTrackingTurn` 的 readers? /
     readersInvoked 腿生产零流量，删参数省守卫）= 公开签名变更
     （硬不变量：公开签名不动）+ 生产路径本就被 turn.ts:114
     双守卫短路（单次 undefined 检查 ~ns），拒列；
  5. **expandDetail 守卫重排/常量折叠**（生产恒 false 前提下
     预折叠 readers 腿）= R12-A §0-11 turn 恒定输入常量折叠伞 +
     S1-A-6 / S7-A-3 分支重排 ns 家族本体，拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新
  记录）：幂等双 find/死扫描 = S8-A-1 / S11-A-2；五扫描逐条 =
  X0-4/X2-4/S1-A-1/S1-A-9/R1-A 清单；写侧 validateEvent =
  S4-G-2 族；第二次 prescore = S1-A-2；turn 恒定输入常量折叠 =
  R12-A §0-11 伞 + R16-A §0-2；skip/replay/hashSummary/H 面/
  分析-隔离面/G×E/输入面/内容类/collation/值类/键集几何/别名
  拓扑/调用流时序 = R18-A…R30-A 各 §0 维持。
- 未重开任何 X\* / S1-\* … S30-\* 条目。**单变量纪律**：本轮
  无计时新臂——锚点矩阵与分解全部沿用血统 E=41 冻结夹具（组成/
  增长/键集/别名/值类/内容类/locale/温度稳态热/单 realm 单基质/
  humanInput ≡ `{}`/M=0/K=0 逐轴钉死）；F11a 纯静态只读；F11b
  纯语义断言（不计时）。S11-A-1 / S11-A-2 / S10-A-1 / S9-A-1 /
  S8-A-1..3 / S7-A-1..4 / X0-4/X2-4 / X0-6 / S6-A-1 / S5-H-1
  （切片外，未提议移除）专项复核：本轮零触碰、零重提，全部维持
  关闭。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点五格
  （gate-outcome × contract × max-codes）覆盖；配置态逐面复核
  ——(a) settle 带/不带 assessment：生产两调用点不带（O(1)
  no-op 由 src/ 最强冻结覆盖，第二十二次复验，F11a 断言）；(b)
  trackingConfig 覆写/applyUserThreshold：生产恒 DEFAULT
  （R22-A 双臂已价，X0-7 维持）；(c) maxItems 截断态 = S2-A-1
  已拒面；(d) **本轮新增：注入可调用物坐标本身作为配置坐标被
  审计**（F11a：生产单坐标普查 11 项断言；F11b：三种可调用物
  身份语义证人 3 项断言）——无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部
  不变——本轮零 diff，天然满足。三线规格（分析不改 in-flight、
  Tracking 无命令权、H/score 不写路由、Live=R0 等价、稀疏 →
  已批准 R0 基线、双 LCB 双归因保留、提升 proposal-first、无
  阈值变更、无 fsync/完整性重哈希移除、fail-open 家族保持关闭、
  未改任何测试）同样天然满足——本轮只经公开 applyChildThreeLine
  / applyTrackingGate / nextTrackingSeq / hashAssessment /
  parseTrackingAssessment / validateEvent / computePrescore /
  runTrackingTurn / assessChildObservation / observationFromChild
  / prescoreInputFromObservation 驱动。不声称 Outcome-supported；
  Checkpoint F-PROD 仍开放（ADR-005）。目录行序契约（R28-B W3）
  与 npm POSIX .bin 符号链接 silent no-op（R28-I）零触碰。无赢家
  ⇒ 零生产改动，未提交任何仿真脚本（按令不建 repo scripts），
  仿真全文见本报告附录。不开 PR。

## 1. 预算支配论证复核 + 一扇停靠门 + 元闭合审计（方法第 1、2、3 步）

R3-A §3 … R30-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..HEAD -- <切片>` 在派发 tip
   `3f80ab6`、两个快进 tip `810b141` / `dabf551`、提交前四验均为
   空，逐字节一致（三十一遍全程零 diff）——R1-A…R30-A 全部测量与
   排除按令转移，但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨二十二段）**：`git diff
   89f0455..dabf551 -- src/` **完全为空**。生产调用方三点位
   （supervisor.ts:483 / coordinator.ts:444 / flowchart-run.ts:320）
   经本轮全库 grep 复验，无新增调用方。

### 1.1 锚点矩阵（本 VM 重锚，三次官方运行，E=41，~5 gate/run，含 R14-A 第五格 max-codes）

```text
                                   run1      run2      run3
PASSED x no-contract               15.8      16.9      15.8  µs/gate (+1 event)
PASSED x contract(3)               13.7      14.2      13.6  µs/gate (+1 event)
FAILED x no-contract               15.6      15.7      15.5  µs/gate (+3 events)
FAILED x contract(3)               13.8      14.1      13.9  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.5      14.9      14.6  µs/gate (+3 events)
=> ~68–84 µs/run（稳态热；无孤点、无悬崖；五格三 run 保序
   P_C < F_C < F_C2 < F_NC < P_NC，与 R30-A 逐格同构）
```

与 R27-A（68–84 µs/run）/ R26-A（69–84）历史带**完全重合**，与
R30-A（64–77）/ R29-A（61–80）/ R28-A（61–76）交叠同带；相对
R30-A 窗口整体上移 ~4–11%（同 VM 不同会话，五格同向平移、保序
⇒ 会话窗口效应非结构变化；R30-A 19a anchor-methodology 归档已证
锚点口径本身偏保守 +0.6~+1.1 µs/gate）。P_NC 格贴带顶 16.9 属
R11-A 起二十遍在案的「no-contract 格 PIC/基准次序」现象族（卫生
注记不入价，R30-A 已以受控臂定价过该族为 µs 拒收类）。稳态下即使
把整个切片优化到零成本，节省上界 ~0.07–0.08 ms/run，仍比战役落地
线（数十~数百 ms 或复杂度类下降）低**约三个量级**。**支配论证
（稳态口径）第二十九次复核成立**；生产冷态口径维持 R17-A 轴 6b
裁决（[2.7, 8.5] ms/run once-per-run，不重测）。

### 1.2 合同地板分解复核（R12-A 方法学，PASSED×contract(3) 生产格，三次官方运行区间；与 R30-A 括号内区间逐项对照）

| 组件 | 本轮实测/gate | R30-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1895–1934 ns 每次 ⇒ **5.69–5.80 µs** | 5.75–6.08 µs | ~41–42% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁（贴 R30-A 带底 −1%，带内） |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 336–361 ns + 第三次 hash） | **3.01–3.06 µs** | 2.76–2.79 µs | ~21–22% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G）；高 R30-A 带 ~+8–10%，与锚点五格 +4–11% 会话窗口同向同幅 ⇒ 归窗口非结构 |
| `runTrackingTurn` 完整 | **1.64–1.74 µs** | 1.63–1.83 µs | ~12% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构（带内） |
| `computePrescore` × 2 | 661–669 ns 每次 ⇒ **1.32–1.34 µs** | 1.33–1.56 µs | ~9–10% | S1-A-2（贴带底） |
| O(E) 五扫描 + 拷贝（episode 77–78 + seq 46–47 + 双幂等 find 39×2 + status 49–50 + `[...events]` 25–28 ns） | **276–280 ns**（⇒ 6.74–6.82 ns/event @E=41） | 274–301 ns | **~2.0%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界（带内） |

\* 以当轮 contract 格（13.6–14.2 µs）计。`applyTrackingGate` 整体
隔离实测 5.87–5.96 µs，与 R30-A（5.69–5.88）贴带上沿互叠。
`parseTrackingAssessment` 单测 336–361 ns（R30-A 328–340，贴带）。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次官方运行区间）：guard-3 skip（FAILED 零 evidence refs）
1983–2044 ns = 14.0–15.0% of apply（R30-A 1975–1992 = 14.8–15.5%，
绝对值交叠、占比因 apply 分母上移而微降）；replay find#1（GT
idempotencyKey 命中）3104–3148 ns = 22.2–22.9% of apply（R30-A
3153–3190 = 23.6–25.0%，绝对值低其带底 ~0.2–1.6% = 会话窗口内）
——组成天花板 {apply, skip, replay} 继续成立（次序半边由 R30-A
19d 收口，本轮不重测）。

### 1.3 停靠门与元闭合审计（方法第 3 步——铸轴前置证明失败于「独立物理对象」，按令停靠）

**门的定义**：injected-callable 平面 = 门链经注入参数消费的全部
可调用物坐标——`generateEventId`（applyChildThreeLine /
applyTrackingGate 的唯一注入效应通道：铸 id）与
`readers.readToolBodies`（runTrackingTurn 的可选细节读取通道）。
三十遍 A 轮从未把「注入的是**什么样的**可调用物」当受控变量：
每个夹具都用每调用新铸箭头（血统纪律），从未扫过共享闭包/预绑定/
包装等其它坐标。

**F11a 生产普查（静态只读，11 项断言 × 3 run 全过，普查行逐字节
一致）**：

```text
coordinator.ts   恰 1 个 applyChildThreeLine 调用点（:444），
                 options 字面量内新铸 () => createEventId(generateId)
flowchart-run.ts 恰 1 个（:320），同型新铸
                 () => createEventId(ctx.generateId)
child-tracking.ts 纯透传 generateEventId: input.generateEventId
supervisor.ts    门腿纯透传 opts.generateEventId；恰 2 个生产
                 settleSupervisedOutcome 调用点，各自新铸箭头
                 且均不带 trackingAssessment ⇒ 生产 supervisor
                 门腿 = O(1) no-op 早退（第二十二次复验）
全平面           零 .bind、零包装工厂、零共享长寿生成器
readers          from-child 生产路径不传 readers（humanInput ≡ {}
                 同断言）⇒ readers 可调用物生产零流量，且
                 turn.ts:114 expandDetail 双守卫短路
⇒ 生产在 injected-callable 平面恰占一个坐标（每调用新铸单分配
  位点箭头），锚点血统恰坐其上 ⇒ 预算测量对生产坐标按构造即
  天花板口径
```

**F11b 语义证人（3 项断言 × 3 run 全过，故意不计时）**：同一门格
（PASSED × contract(3)）、同一 id 流，(i) 每调用新铸箭头 (ii) 跨
调用共享闭包 (iii) 预绑定函数三种身份各驱动 100 次，result + 追加
字节 `JSON.stringify` **逐位一致**（300 调用零分歧）⇒ 可调用物
身份/种类语义不可见（门当不透明 id 源消费）。

**停靠裁决**：该平面唯一可能的价是引擎级（调用适配器分派 + 闭包
边引用结构 + 内部调用位点 IC 历史），而这三条腿分别是 R28-F
boundary call-protocol face（+ R29-H/R30-H 停放）、R29-A aliasing
face、R30-A dispatch-history face 的**本体**——计时即三重再剂量，
测得的任何 µs 无法归因到一个独立物理对象上。生产简并（单坐标）+
语义不可见 + 价不独立 ⇒ **停靠不铸**。停靠状态：证人归档（F11a/
F11b 全文入附录），不取绰号、不作轴名、不加 EXCLUSIONS 行，
plan-ban 归 parent。

**元闭合审计（第三十一遍的枚举面收口论证）**：「在一个 Node 进程
内调用一个纯同步函数」的元坐标逐面属主：

| 元坐标 | 属主 / 状态 |
| --- | --- |
| 喂什么——规模 / 声明粒度 / 内容类 / 值类 / 键集几何 / 引用结构 | R14–R16-A / R24-A / R25-A / R27-A / R28-A / R29-A（全部已闭） |
| 喂多少、什么混合——组成 / 增长耦合 / 驻留 / H 面 / 导出面 | R17/R18-A / R23-A / R20-A / R21-A / R22-A（全部已闭） |
| 什么结局 | R19-A（已闭） |
| 什么环境 / 什么基质 / 什么 realm | R26-A collation / R27-H execution-substrate（禁移植）/ 单 realm 血统钉死 |
| 以什么次序、什么进程史、什么温度 | R30-A（上一轮闭）+ R17-A 温度 |
| 经什么协议抵达边界 | R28-F（禁移植，F 区已闭平坦） |
| 边界经什么可调用物注出效应 | **本轮停靠门**（生产单坐标 + 语义不可见 + 价不独立 ⇒ 停靠） |
| 输出往哪去——消费 / 读回 / 保留 / 存活 | R30-F / R29-F（禁移植；本切片输出即弃） |
| 时间从哪来 | R29-E 时钟拓扑（禁移植；本切片 nowIso 为输入字符串，门路径零时钟铸造） |
| 进程生命期怎么分段 | R30-I（禁移植；本切片进程内纯函数，零子进程零退出腿） |
| 持久工件怎么落盘、怎么指称/链接 | R28-J ZREP / R30-J LINKTOP（禁移植；本切片数据路径零磁盘 I/O 零文件指称） |
| 溯源怎么解析 / 覆盖关系什么形 | R30-H / R29-H（禁移植；本切片不解析溯源） |
| 调度格 / 裁决格 | R30-G M1 / R29-G M2（禁移植；零监督语义） |

每一面要么已被 A 轴闭合、要么属兄弟切片归档轴且禁移植、要么对本
切片结构性空（零流量/零对象）。第三十一遍枚举在该坐标系内**无
剩余未审计平面**——本轮产出即最后一块平面的停靠归档。

**重开条件全景**：R30-A §1 表二十六行全部维持（本轮无新定价轴，
不加行）。停靠门自带三条重开条件：(a) 生产在可调用物平面获得第二
坐标（某调用方开始传 `.bind`/包装器/共享长寿生成器——届时种类价
须实测，但铸轴权属 call-protocol 平面的停放裁决方，非 A 单方）；
(b) readers 可调用物获得生产流量（expandDetail 接线上线——届时
readers 腿须定价，turn.ts:114 守卫成为热分支）；(c) gate-apply
内部调用位点观察到 >2 种可调用物种类共存（megamorphic 化——届时
按 R30-A 分派历史面重开条件走，属其轴属地）。**可操作重开第一
优先不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7 家族
`every×includes` 站点 Set 化）。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 5 项 + R12-A…R30-A
97 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、功能
性变更或数据补充，停靠门按令不作候选（无独立物理对象即无可定价的
候选面）。空枚举与 R12-A…R30-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第三十一遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/
记忆化/数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行
路径特化/形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部
命中既有 ID 或本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `gate-apply.ts` | **停靠门宿主**（`generateEventId` 消费点——内部 `generate()` 调用位点是 F11b 三种身份的分派汇点，计时即 R30-A 再剂量故不计时；id 预生成 = §0-3 拒列）；replay find#1 3104–3148 ns 复现（重放臂 throwing generator 实证零 id 铸造）；O(E) 清单 / S11-A-2 / S8-A-1 / S2-A-4 维持 | 无新候选 |
| `child-tracking.ts` | 锚点/分解宿主复测同带（五格 13.6–16.9 µs/gate，与 R27-A 带重合）；**F11a 透传断言宿主**（generateEventId: input.generateEventId 纯透传）；guard-3 skip 抽测 1983–2044 ns 复现 | 无新候选 |
| `turn.ts` | **readers 可调用物宿主**（:37 声明 / :114 expandDetail 双守卫——生产零流量实证；死参裁剪 = §0-4 拒列、守卫折叠 = §0-5 拒列）；runTrackingTurn 复现 1.64–1.74 µs 带内；R13-A 八子组件 / R12-A §0-11 / R16-A §0-2 维持 | 无新候选 |
| `types.ts` | hash×3 地板复现 5.69–5.80 µs（贴 R30-A 带底）；parseTrackingAssessment 336–361 ns；X1-1/S8-G-2 memo 禁维持 | 无新候选 |
| `from-child.ts` | **F11a readers 零流量断言宿主**（生产 turn 调用不传 readers、humanInput ≡ {}）；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `prescore.ts` | 纯值消费者；S1-A-3 / S1-A-4/X4-7 / S6-A-1 / S8-A-3 维持；C 轴可操作重开第一优先不变 | 无新候选 |
| `combined-score.ts` | 纯值消费者；S6-A-1 / S7-A-4 / R13-A §0-1 维持 | 无新候选 |
| `gates.ts` | evaluateGates 分支面维持（重排 = §0-5 族拒列）；X0-7 阈值冻结维持；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 维持 | 无新候选 |
| `roller.ts` | 每调用独立 rollSummary 无跨调用状态；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（humanInput ≡ `{}`）；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 / R21-A 维持 | 无新候选 |
| `config.ts` | 生产恒 DEFAULT（配置态矩阵复核宿主）；X0-7 维持 | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方）；S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R29-A F9c(iv) 契约角维持（`packet !== summary` fail-closed 冗余腿保留）；index.ts 纯再导出零逻辑 | 无新候选 |

**对 R30-A 声明的裁决**：「调用流的到达次序是锚点方法学最后一个
未受控坐标」在**计时坐标系**内成立；但测量行为还有一个**非计时**
平面——效应经什么可调用物注入。本轮审计该平面并停靠（生产简并 +
语义不可见 + 价不独立），至此元坐标系逐面有主（§1.3 审计表）。
本轮全部新数据**只产出停靠归档与生产普查证人，未产出可落地候选**。
收口的预言力第三十一遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件（`git diff
origin/cursor/sota-persistent-opt-83a1...HEAD` 复核）。仿真为临时
脚本未入库（无赢家不落仿真文件，遵守方法纪律；完整源码见附录），
未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R30-A 同套件同计数；
两次快进后在新 tip `810b141` / `dabf551` 各复跑一次同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）在派发
tip `3f80ab6` 与两个快进 tip `810b141` / `dabf551` 各跑一次**均
全绿**（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 /
skipped 1，三次逐字相同，与主线基线一致；退出码 0）。

仿真（临时脚本；seed 参数化可复现，三次官方运行摘要；每 run 22 项
check——F11a 11 + F11b 3 + 锚点 5 + 分解/抽测夹具 3——三 run 共
66 项全过）：

```text
run 1 (seed 0x31a001):
F11a: 生产普查 11/11 全过（普查行逐字节一致）
F11b: 身份证人 3/3 全过（3 臂 × 100 调用逐位一致，不计时）
anchor: 15.8 / 13.7 / 15.6 / 13.8 / 14.5 µs/gate => ~68–79 µs/run
decomposition: hash×3=5780ns | parse=361ns | validateEvent=3013ns
    | prescore×2=1337ns | turn=1643ns | 5 scans+copy=280ns
    (6.82 ns/event) | applyTrackingGate=5.87µs
spot: skip guard-3 2029ns (14.8%) | replay find#1 3104ns (22.7%)
ALL CHECKS PASSED (deterministic, seeded)

run 2 (seed 0x31a002):
F11a: 11/11 | F11b: 3/3
anchor: 16.9 / 14.2 / 15.7 / 14.1 / 14.9 µs/gate => ~70–84 µs/run
decomposition: hash×3=5803ns | parse=336ns | validateEvent=3055ns
    | prescore×2=1329ns | turn=1737ns | 5 scans+copy=277ns
    (6.75 ns/event) | applyTrackingGate=5.89µs
spot: skip guard-3 1983ns (14.0%) | replay find#1 3148ns (22.2%)
ALL CHECKS PASSED (deterministic, seeded)

run 3 (seed 0x31a003):
F11a: 11/11 | F11b: 3/3
anchor: 15.8 / 13.6 / 15.5 / 13.9 / 14.6 µs/gate => ~68–79 µs/run
decomposition: hash×3=5685ns | parse=339ns | validateEvent=3044ns
    | prescore×2=1323ns | turn=1670ns | 5 scans+copy=276ns
    (6.74 ns/event) | applyTrackingGate=5.96µs
spot: skip guard-3 2044ns (15.0%) | replay find#1 3127ns (22.9%)
ALL CHECKS PASSED (deterministic, seeded)
```

三次官方仿真运行 F11a 普查行与 F11b 证人行逐字节一致；锚点五格
三次同带无悬崖、无孤点、保序同构。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S31-A-\*）。换名检查 5 项新记录见 §0（1–5），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md；停靠门按令
**不取绰号、不作轴名、不加 EXCLUSIONS 行**，plan-ban 归 parent。

停靠门归档一条：

**injected-callable 平面停靠**：门链注入可调用物（`generateEventId`
生产通道 + `readers.readToolBodies` 零流量通道）的身份/种类平面
自此有归档——生产普查（F11a：生产恰占一个坐标 = 每调用新铸单分配
位点箭头，锚点血统恰坐其上；readers 生产零流量；supervisor 门腿
O(1) no-op 第二十二次复验）、语义证人（F11b：新铸箭头/共享闭包/
预绑定三种身份逐位同结果——门当不透明 id 源消费）、停靠裁决（价
不独立：计时即 R28-F call-protocol（含 R29-H/R30-H 停放）+ R29-A
aliasing + R30-A dispatch-history 三重再剂量）。重开条件：(a)
生产获得第二可调用物坐标（调用方引入 .bind/包装器/共享生成器）；
(b) readers 获得生产流量（expandDetail 接线）；(c) 内部调用位点
megamorphic 化（>2 种类共存，归 R30-A 轴属地）。**可操作重开第一
优先不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7 家族
`every×includes` 站点 Set 化）。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式（repo 根，Node v22.22.2 + tsx）：

```bash
npx tsx /tmp/r31a-sim.mts --seed 31a001   # run 1
npx tsx /tmp/r31a-sim.mts --seed 31a002   # run 2
npx tsx /tmp/r31a-sim.mts --seed 31a003   # run 3
```

```typescript
/**
 * R31-A deterministic anchor + decomposition + closure-audit simulation
 * (thirty-first pass). No fresh candidate cleared the rename check this
 * round and NO new axis is minted: the thirty-first enumeration ends in a
 * meta-closure audit (every remaining meta-coordinate of "invoking a pure
 * synchronous function in one Node process" is either already closed by an
 * A axis, owned by a sibling slice's archived face with cross-slice porting
 * banned by plan, or structurally void for this slice), plus ONE parked
 * door (the injected-callable plane of `generateEventId` / `readers`),
 * parked with a production census and semantic witnesses instead of a dose
 * -- pricing that plane would re-dose faces owned elsewhere (R28-F boundary
 * call-protocol attribution via the R29-H/R30-H park; R29-A input-graph
 * aliasing on the callable edge; R30-A dispatch history at the internal
 * call sites), so per instructions the door is parked, not minted.
 *
 * The script therefore has NO candidate machinery and NO new-axis timing
 * arms. It:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R30-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R30-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell plus compact spot
 *       re-confirmations of the R18-A skip pricing (guard-3) and the
 *       R19-A replay pricing (find#1);
 *   (c) F11a: injected-callable production census (static, read-only) --
 *       every production gate call site mints a FRESH single-allocation-
 *       site arrow closure per call (coordinator / flowchart-run) or
 *       passes through a parameter that its own production callers bind to
 *       a fresh arrow per settle (supervisor; and those settle sites pass
 *       no trackingAssessment, so the production supervisor gate leg is
 *       the O(1) no-op early return). No .bind, no wrapper, no shared
 *       long-lived generator anywhere on the plane => production occupies
 *       exactly ONE coordinate, and the anchor lineage (fresh arrow per
 *       call, same as R30-A's makeCellFn) sits exactly on it;
 *   (d) F11b: injected-callable identity semantic witness -- the same
 *       cell driven with (i) a fresh arrow per call, (ii) one shared
 *       closure reused across calls, (iii) a pre-bound function, all fed
 *       the same id stream, produces bitwise identical results and
 *       appended bytes (the gate treats the callable as an opaque id
 *       source; identity/kind of the callable is semantically invisible).
 *       WITNESS ONLY -- deliberately untimed: timing those arms would be
 *       the R28-F/R29-A/R30-A re-dose this round refuses to make.
 *
 * Single-variable discipline: all anchor/decomposition fixtures are the
 * frozen E=41 lineage fixtures (R14-A..R30-A pins: composition, growth,
 * key-set geometry, aliasing, value classes, content classes, locale,
 * temperature steady-hot, single realm/substrate, humanInput === {},
 * M=0/K=0). Seeded (mulberry32) mixed warm-up order -> reproducible.
 * Seeds: 0x31a001..0x31a003.
 * Run: npx tsx /tmp/r31a-sim.mts --seed <hex>   (repo root, Node >=22.19)
 */
import { readFileSync } from "node:fs";
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn, type TrackingTurnInput } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import { applyChildThreeLine, observationFromChild } from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-26T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

const seedIdx = process.argv.indexOf("--seed");
const SEED = seedIdx === -1 ? 0x31a001 : Number.parseInt(process.argv[seedIdx + 1] ?? "", 16);
console.log(`seed: 0x${SEED.toString(16)}`);

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    summary?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary =
    opts?.summary ?? (opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran");
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

/* Anchor cell definitions (R3-A..R30-A lineage incl. R14-A max-codes cell). */
interface AnchorCell {
  readonly label: string;
  readonly shortLabel: string;
  readonly kind: "PASSED" | "FAILED";
  readonly withContract: boolean;
  readonly maxCodes?: boolean;
}
const ANCHOR_CELLS: readonly AnchorCell[] = [
  {
    label: "PASSED x no-contract (lineage anchor)",
    shortLabel: "P_NC",
    kind: "PASSED",
    withContract: false
  },
  { label: "PASSED x contract(3)", shortLabel: "P_C", kind: "PASSED", withContract: true },
  { label: "FAILED x no-contract", shortLabel: "F_NC", kind: "FAILED", withContract: false },
  {
    label: "FAILED x contract(3) (hard gate, 3 events appended)",
    shortLabel: "F_C",
    kind: "FAILED",
    withContract: true
  },
  {
    label: "FAILED x contract(3) x max-codes(2 hard)",
    shortLabel: "F_C2",
    kind: "FAILED",
    withContract: true,
    maxCodes: true
  }
];

function makeCellFn(cell: AnchorCell): {
  fn: () => void;
  outcome: () => { result: unknown; appended: unknown };
} {
  const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
  const run = (): { result: unknown; appended: unknown } => {
    let c = 100;
    const out = applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(cell.withContract ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
    return { result: out.result, appended: out.events.slice(events41.length) };
  };
  return { fn: () => void run(), outcome: run };
}

const cellRunners = ANCHOR_CELLS.map((cell) => ({ cell, ...makeCellFn(cell) }));

/* ============================================================
 * F11a: injected-callable production census (static, read-only).
 * The gate's ONLY injected callables are `generateEventId` (production)
 * and `readers.readToolBodies` (zero production flow on the from-child
 * path). Census of every production binding site:
 *   coordinator.ts / flowchart-run.ts: fresh arrow per gate call, minted
 *     inside the options literal;
 *   child-tracking.ts / supervisor.ts settleSupervisedOutcome: pure
 *     pass-through of the caller's parameter;
 *   supervisor.ts production settle call sites: fresh arrow per settle,
 *     and NO trackingAssessment key => production supervisor leg is the
 *     O(1) no-op early return (R30-A twenty-first re-verification lineage);
 *   nowhere on the plane: .bind( / wrapper factories / shared long-lived
 *     generator objects.
 * ============================================================ */
{
  const coordinatorText = readFileSync("/workspace/src/run/coordinator.ts", "utf8");
  const flowText = readFileSync("/workspace/src/run/flowchart-run.ts", "utf8");
  const supervisorText = readFileSync("/workspace/src/run/supervisor.ts", "utf8");
  const childTrackingText = readFileSync("/workspace/src/run/child-tracking.ts", "utf8");

  check(
    "F11a coordinator has exactly 1 gate call site",
    [...coordinatorText.matchAll(/applyChildThreeLine\(/g)].length === 1
  );
  check(
    "F11a coordinator mints a fresh arrow per gate call",
    /generateEventId:\s*\(\)\s*=>\s*createEventId\(generateId\)/.test(coordinatorText)
  );
  check(
    "F11a flowchart-run has exactly 1 gate call site",
    [...flowText.matchAll(/applyChildThreeLine\(/g)].length === 1
  );
  check(
    "F11a flowchart-run mints a fresh arrow per gate call",
    /generateEventId:\s*\(\)\s*=>\s*createEventId\(ctx\.generateId\)/.test(flowText)
  );
  check(
    "F11a child-tracking passes the caller's callable through unchanged",
    /generateEventId:\s*input\.generateEventId/.test(childTrackingText)
  );
  check(
    "F11a supervisor gate leg passes the settle caller's callable through unchanged",
    /generateEventId:\s*opts\.generateEventId/.test(supervisorText)
  );
  const settleCalls = [...supervisorText.matchAll(/settleSupervisedOutcome\(\{/g)].length;
  check("F11a supervisor has exactly 2 production settle call sites", settleCalls === 2, String(settleCalls));
  const settleBlocks = supervisorText
    .split(/settleSupervisedOutcome\(\{/)
    .slice(1)
    .map((chunk) => chunk.slice(0, 220));
  check(
    "F11a both settle call sites mint a fresh arrow per settle",
    settleBlocks.every((block) => /generateEventId:\s*\(\)\s*=>\s*createEventId\(generateId\)/.test(block))
  );
  check(
    "F11a neither production settle call site passes a trackingAssessment (O(1) no-op leg)",
    settleBlocks.every((block) => !block.includes("trackingAssessment"))
  );
  const noBindNoWrap = (text: string): boolean =>
    !/generateEventId:\s*[^,\n]*\.bind\(/.test(text) && !/generateEventId:\s*wrap/i.test(text);
  check(
    "F11a no .bind / wrapper factory anywhere on the injected-callable plane",
    noBindNoWrap(coordinatorText) && noBindNoWrap(flowText) && noBindNoWrap(supervisorText) && noBindNoWrap(childTrackingText)
  );
  check(
    "F11a from-child production turn invocation passes no readers (zero production flow on the readers callable)",
    /humanInput:\s*\{\}/.test(readFileSync("/workspace/src/tracking/from-child.ts", "utf8")) &&
      !/readers/.test(readFileSync("/workspace/src/tracking/from-child.ts", "utf8"))
  );
  console.log(
    "F11a: injected-callable census -- every production gate call site mints a fresh single-" +
      "allocation-site arrow closure per call (coordinator, flowchart-run) or passes through a " +
      "parameter that production callers bind to a fresh arrow per settle (supervisor; those settle " +
      "sites pass no assessment => O(1) no-op leg). No .bind, no wrappers, no shared long-lived " +
      "generators. Production occupies exactly ONE coordinate on this plane, and the anchor lineage " +
      "(fresh arrow per call) sits exactly on it. The readers callable has zero production flow on " +
      "the from-child path."
  );
}

/* ============================================================
 * F11b: injected-callable identity semantic witness (UNTIMED).
 * Same cell, same id stream, three callable identities:
 *   (i) fresh arrow per call (production + anchor coordinate);
 *   (ii) one shared closure reused across calls;
 *   (iii) a pre-bound function.
 * Bitwise identical results + appended bytes => callable identity/kind is
 * semantically invisible to the gate (opaque id source).
 * ============================================================ */
{
  const mk = mkChild("PASSED");
  const runWith = (generate: () => EventId): string => {
    const out = applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      contract: mk.contract,
      nowIso: NOW_ISO,
      generateEventId: generate
    });
    return JSON.stringify({ result: out.result, appended: out.events.slice(events41.length) });
  };
  const REPS = 100;
  // (i) fresh arrow per call
  const freshOutputs: string[] = [];
  for (let i = 0; i < REPS; i += 1) {
    let c = 100;
    freshOutputs.push(runWith(() => `evt_${String(c++).padStart(8, "0")}` as EventId));
  }
  // (ii) one shared closure reused across calls (counter reset per call)
  const counterBox = { c: 100 };
  const shared = (): EventId => `evt_${String(counterBox.c++).padStart(8, "0")}` as EventId;
  const sharedOutputs: string[] = [];
  for (let i = 0; i < REPS; i += 1) {
    counterBox.c = 100;
    sharedOutputs.push(runWith(shared));
  }
  // (iii) a pre-bound function
  function generateFrom(this: void, box: { c: number }): EventId {
    return `evt_${String(box.c++).padStart(8, "0")}` as EventId;
  }
  const boundBox = { c: 100 };
  const bound = generateFrom.bind(undefined, boundBox) as () => EventId;
  const boundOutputs: string[] = [];
  for (let i = 0; i < REPS; i += 1) {
    boundBox.c = 100;
    boundOutputs.push(runWith(bound));
  }
  const expected = freshOutputs[0];
  check(
    "F11b fresh-arrow outputs self-consistent across 100 calls",
    freshOutputs.every((s) => s === expected)
  );
  check(
    "F11b shared-closure outputs bitwise identical to fresh-arrow outputs",
    sharedOutputs.every((s) => s === expected)
  );
  check(
    "F11b pre-bound outputs bitwise identical to fresh-arrow outputs",
    boundOutputs.every((s) => s === expected)
  );
  console.log(
    "F11b: injected-callable identity witness -- fresh-arrow / shared-closure / pre-bound callable " +
      "identities produce bitwise identical results and appended bytes on the same id stream (300 " +
      "calls). The gate consumes the callable as an opaque id source; identity and kind are " +
      "semantically invisible. UNTIMED by design: pricing these arms would re-dose the R28-F " +
      "call-protocol attribution (parked per R29-H/R30-H), the R29-A aliasing face on the callable " +
      "edge, and the R30-A dispatch-history face at the internal call sites."
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes
 * (lineage discipline, R30-A identical: 3000 mixed rounds; seeded order).
 * ============================================================ */
{
  const rng = mulberry32(SEED ^ 0x77a1);
  for (let round = 0; round < 3000; round += 1) {
    const kind = rng() < 0.5 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R30-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
for (const { cell, fn, outcome } of cellRunners) {
  const first = outcome() as { result: { applied: boolean }; appended: unknown[] };
  check(`anchor cell applies the gate [${cell.label}]`, first.result.applied);
  const one = bench(fn, 5000);
  anchorUs.push(one * 1e3);
  console.log(
    `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${first.appended.length} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
  );
}

/* ============================================================
 * Contract-floor decomposition (R12-A..R30-A methodology) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const turnInputP: TrackingTurnInput = {
    window: {
      contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
      toolSituations: prescoreInputP.toolSituations,
      constraints: observationP.constraints,
      unresolvedDecisions: [],
      confirmedDecisions: [],
      openMinors: []
    },
    prescoreInput: prescoreInputP,
    humanInput: {},
    gateFacts: { deterministicFail: false }
  };
  const tTurn = bench(() => void runTrackingTurn(turnInputP), 100000);
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)}) => ~${((scans * 1e6) / 41).toFixed(2)} ns/event at E=41`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const mkGT = mkChild("FAILED", { taskId: "tsk_replay_gt" });
    const obsGT = observationFromChild(mkGT.child, mkGT.spec, mkGT.contract);
    const decGT = assessChildObservation({
      observation: obsGT,
      episodeId: "ep_bench",
      runId: String(RUN_ID)
    });
    if (!decGT.apply) throw new Error("replay fixture must apply");
    const hashGT = hashAssessment(decGT.assessment);
    const seqGT = nextTrackingSeq(events41);
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: decGT.assessment,
      assessmentHash: hashGT,
      expectedSeq: seqGT,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: decGT.assessment,
        assessmentHash: hashGT,
        expectedSeq: seqGT,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }
}

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
