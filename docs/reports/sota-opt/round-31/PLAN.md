# Round 31 作战计划

**目标**：在 R30 十面（call-stream ordering / dispatch-history；scale-cube 二阶交互；input object-representation / **REPRX**；approval-profile constraint-list geometry；async suspension-point topology / per-run suspension-manifest；boundary output-consumption / snapshot-readback topology；M1 parent-run child-scheduling lattice；provenance-resolution pathway；process-lifetime temporal-segment topology；durable-artifact referent-binding / filesystem link-topology / **LINKTOP**）于 R30-A–J 诚实空枚举关闭、且 **FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX** 具名 ban 与 unnamed R21–R30 面（R21-A 跨工具调用序 / 跨工具时序拓扑、R21-B 多任务并发规模立方、R21-C 输入表示形态、R21-D 审批画像形状、R21-E 异步边界拓扑、R21-F 边界输出消费拓扑、R21-G 监督调度拓扑、R21-H 证据溯源路径拓扑、R21-I 进程寿命拓扑、R21-J 外部存储拓扑、R22-A 跨工具调用序 / 跨工具时序拓扑、R22-B 多任务并发规模立方、R22-C 输入表示形态、R22-D 审批画像形状、R22-E 异步边界拓扑、R22-F 边界输出消费拓扑、R22-G 监督调度拓扑、R22-H 证据溯源路径拓扑、R22-I 进程寿命拓扑、R22-J 外部存储拓扑、R23-A 跨工具调用序 / 跨工具时序拓扑、R23-B 多任务并发规模立方、R23-C 输入表示形态、R23-D 审批画像形状、R23-E 异步边界拓扑、R23-F 边界输出消费拓扑、R23-G 监督调度拓扑、R23-H 证据溯源路径拓扑、R23-I 进程寿命拓扑、R23-J 外部存储拓扑、R24-A 跨工具调用序 / 跨工具时序拓扑、R24-B 多任务并发规模立方、R24-C 输入表示形态、R24-D 审批画像形状、R24-E 异步边界拓扑、R24-F 边界输出消费拓扑、R24-G 监督调度拓扑、R24-H 证据溯源路径拓扑、R24-I 进程寿命拓扑、R24-J 外部存储拓扑、R25-A 跨工具调用序 / 跨工具时序拓扑、R25-B 多任务并发规模立方、R25-C 输入表示形态、R25-D 审批画像形状、R25-E 异步边界拓扑、R25-F 边界输出消费拓扑、R25-G 监督调度拓扑、R25-H 证据溯源路径拓扑、R25-I 进程寿命拓扑、R25-J 外部存储拓扑、R26-A 跨工具调用序 / 跨工具时序拓扑、R26-B 多任务并发规模立方、R26-C 输入表示形态、R26-D 审批画像形状、R26-E 异步边界拓扑、R26-F 边界输出消费拓扑、R26-G 监督调度拓扑、R26-H 证据溯源路径拓扑、R26-I 进程寿命拓扑、R26-J 外部存储拓扑、R27-A 跨工具调用序 / 跨工具时序拓扑、R27-B 多任务并发规模立方、R27-C 输入表示形态、R27-D 审批画像形状、R27-E 异步边界拓扑、R27-F 边界输出消费拓扑、R27-G 监督调度拓扑、R27-H 证据溯源路径拓扑、R27-I 进程寿命拓扑、R27-J 外部存储拓扑、R28-A 跨工具调用序 / 跨工具时序拓扑、R28-B 多任务并发规模立方、R28-C 输入表示形态、R28-D 审批画像形状、R28-E 异步边界拓扑、R28-F 边界输出消费拓扑、R28-G 监督调度拓扑、R28-H 证据溯源路径拓扑、R28-I 进程寿命拓扑、R28-J 外部存储拓扑、R29-A 跨工具调用序 / 跨工具时序拓扑、R29-B 多任务并发规模立方、R29-C 输入表示形态、R29-D 审批画像形状、R29-E 异步边界拓扑、R29-F 边界输出消费拓扑、R29-G 监督调度拓扑、R29-H 证据溯源路径拓扑、R29-I 进程寿命拓扑、R29-J 外部存储拓扑、R30-A call-stream ordering / dispatch-history、R30-B scale-cube 二阶交互、R30-C input object-representation / **REPRX**、R30-D approval-profile constraint-list geometry、R30-E async suspension-point topology / per-run suspension-manifest、R30-F boundary output-consumption / snapshot-readback topology、R30-G M1 parent-run child-scheduling lattice、R30-H provenance-resolution pathway、R30-I process-lifetime temporal-segment topology、R30-J durable-artifact referent-binding / filesystem link-topology / **LINKTOP**、R31-C ambient intrinsic-binding / intrinsic-resolution / **BINDX**、R31-E I/O text-codec topology / per-run transcode-byte manifest）plan-ban 的前提下，对十切片做**第三十一遍**独立枚举。R31-A 合入时**未铸造新面**：injected-callable 平面停驻（`generateEventId` / `readers.readToolBodies` 独立物理对象证明失败；对其计时会重剂量 R28-F / R29-A / R30-A），**禁止**再编号为新 A 轴、**禁止**写入 EXCLUSIONS 表行。R31-B 合入时**未铸造新面**：面格闭包审计关闭六个停驻门（catalog-row hidden-class / IC-shape、cascade evidence×failure-class lattice、assignTasks cold-onset segmentation、batch class-composition mix、LINKTOP-in-B、prior alias-resolution pathway），**禁止**再编号为新 B 轴、**禁止**写入 EXCLUSIONS 表行。R31-C 合入时铸造 **BINDX**（ambient intrinsic-binding / intrinsic-resolution face；realm-level 绑定态剂量；C1 capture-hoist 实尺度决斗符号翻转拒收），**禁止**再编号、**禁止**写入 EXCLUSIONS 表行；与 R31-A injected-callable 停驻门划界（无注入缝、realm-state 剂量、protector-cell 响应无法用既有面坐标表达）。R31-D 合入时**未铸造新面**：元闭包审计将残余 monitor 配置平面（`windowSize × calibrationDelta`）归入 **R22-D 所有权**（零生产调用方），**禁止**再编号为新 D 轴、**禁止**写入 EXCLUSIONS 表行。R31-E 合入时铸造 I/O text-codec topology / per-run transcode-byte manifest face（切片内恰 3 个 fs UTF-8 编解码位点；全 W1 = 7 解码 / 736 字节 / 4 编码；现实预算 0.08–0.81 µs/run），**禁止**再编号、**禁止**写入 EXCLUSIONS 表行；未铸 ALLCAPS 外号。R31-F 合入时**未铸造新面**：边界故障通道输出处置残格（propagate/swallow/read message/materialize stack/retain/wrap-rethrow）在铸轴前置证明第 1 步失败，按分解归入 R17-F + R30-F + R29-F + 引擎面（热通道零流量；不新停驻），**禁止**再编号为新 F 轴、**禁止**写入 EXCLUSIONS 表行。已合入报告：`R31-A.md` / `R31-B.md` / `R31-C.md` / `R31-D.md` / `R31-E.md` / `R31-F.md`。本轮状态：**A–F 已合入；G/H 运行中；I 本波派出**。R30 已于 R30-J 合入后 10/10 关闭。

**约束（相对 R30 收口）**：

- R30 十切片全部诚实空枚举关闭，无新 S30-* ID；R30-A–J 报告已合入 exclusive。R31-A / R31-B / R31-C / R31-D / R31-E / R31-F 亦诚实空枚举关闭（无 S31-A-* / S31-B-* / S31-C-* / S31-D-* / S31-E-* / S31-F-*）。
- 生产基线未变：最后 `src/` 提交仍为 `183df9b`（S7-C）；R31-A / R31-B / R31-C / R31-D / R31-E / R31-F 子仓相对 exclusive 的 diff 仅新报告。
- 禁止再编号：FITQ、NAMESHAPE、SCALEX、XPROC、YMIX、SCHEDWIN、KFAN、NVG、SEEDX、TERMCLASS、SHAPEK、ORDX、CKMIX、CALLB、OCCX、JB、RUNLIM、VALCLASS、CG、AGEX、COTARG、PRICEX、RTL、DELINV、PROBX、RVL、SIDEC、PWL、ZREP、PAIRX、CSPELL、REPRX、LINKTOP、BINDX（空枚举轴外号，**不是** EXCLUSIONS 表行）。
- 上述 R21–R30 具名面与 R30 十面 + R31-C **BINDX** + R31-E I/O text-codec topology / per-run transcode-byte manifest 亦 plan-ban，同样**不**写入 EXCLUSIONS 表行。
- R31-A injected-callable 平面、R31-B 面格六个停驻门、R31-D 归入 R22-D 的 monitor 配置平面、R31-F 边界故障通道输出处置残格（分解归入 R17-F + R30-F + R29-F + 引擎面）按停驻 / 所有权 / 分解记录，**不**铸造新面、**不**写入 EXCLUSIONS 表行。
- 落地条件、硬不变量、sim 家族与 [PROGRESS.md](../PROGRESS.md) / [EXCLUSIONS.md](../EXCLUSIONS.md) / [round-30/PLAN.md](../round-30/PLAN.md) 一致。

## 切片（与 R1–R30 相同）

| 切片 | 范围 | 状态 |
| --- | --- | --- |
| A | `src/tracking/` + `src/run/child-tracking.ts` + `src/run/gate-apply.ts`（14） | 已合入（空枚举；未铸造新面；injected-callable 平面停驻；预算 13.6–16.9 µs/gate ⇒ ~68–84 µs/run） |
| B | 在线路由 9 + `src/supervisor/model-router.ts`（10） | 已合入（空枚举；未铸造新面；面格闭包审计 / 六门停驻；M=2 9.25–11.46 / M=10 21.02–23.17；VM +10–27% host offset vs R30-B；freeze 0 行） |
| C | 离线路由 9 | 已合入（空枚举；铸造 **BINDX** / ambient intrinsic-binding face；中位 658.1–660.1；APC 20.7–25.1 < 35；sink=7.309；C1 capture-hoist 实尺度决斗符号翻转拒收） |
| D | `src/adaptation/`（14） | 已合入（空枚举；未铸造新面；元闭包审计；残余 monitor 配置平面归入 R22-D 所有权；S0c 3.38–3.64 ms） |
| E | `src/learning/`（10） | 已合入（空枚举；铸造 I/O text-codec topology / per-run transcode-byte manifest face；全 W1 = 7 解码 / 736 字节 / 4 编码；现实预算 0.08–0.81 µs/run；SLICE-CPU cmp25 14.9–15.3 vs 14.7–15.2） |
| F | `src/experiments/`（15） | 已合入（空枚举；未铸造新面；故障通道处置残格按分解归属；r5f 119.97/121.51/122.37、r6f 119.70/119.52/117.11、r7f 127.81/129.00/130.55） |
| G | `src/run/`（除 A 两文件）+ `src/supervisor/`（除 model-router）+ `src/graph/` + `src/domain/`（42） | 运行中 |
| H | evaluation 8 + requirement 7 + review 4 + rubric 2（21） | 运行中 |
| I | cli 13 + pi-adapter 9 + config 2 + telemetry 1（25） | 本波派出 |
| J | cluster 3 + privacy 3 + preferences 7 + episode 5 + persist 2 + track 4 + context 2 + feedback 3（29） | 待派出 |

A 默认对照 R30-A（13.6–15.3 µs/gate 量级；~64–77 µs/run）。B 默认对照 R30-B（M=2 8.87–9.13 / M=10 17.34–18.23；R31-B 本 VM 天井 M=2 9.25–11.46 / M=10 21.02–23.17，+10–27% host offset，freeze 0 行）。C 默认对照 R30-C（中位 658.7–663.0；APC 18.8–26.6 < 35；sink=7.309；R31-C 本 VM 中位 658.1–660.1 / APC 20.7–25.1）。D 默认对照 R30-D（S0c 3.29–3.66 ms / 同进程 3.13–3.27；R31-D 本 VM S0c 3.38–3.64）。E 默认对照 R30-E（SLICE-CPU 14.7–15.2 µs/run；R31-E cmp25 14.9–15.3）。F 默认对照 R30-F（r5f 119.31/120.55/121.20、r6f 120.71/121.34/121.90、r7f 128.31/130.42/132.69；R31-F 本 VM r5f 119.97/121.51/122.37、r6f 119.70/119.52/117.11、r7f 127.81/129.00/130.55）。G 默认对照 R30-G（compute 0.292–0.296 vs I/O 93.9–97.1；digest `06cbcf92c098c8f0` 第二十一次相同，**禁止**再铸 digest 轴号）。H 默认对照 R30-H（热路径 9178–9647 ns）。I 默认对照 R30-I（custom−builtin：children +38.2~+49.4 / +22.7~+26.4、track +46.0~+52.9 / +22.4~+26.1、flowchart-flat64 +42.5~+51.0 / +15.2~+20.3）。J 默认对照 R30-J（preferences 134.2–238.7 / 415.9–525.4 µs；jsonl 稳态 61.6–66.9 / 220.1–305.4 µs）。S12-B-2 条件陆器仍未触发。S8-I-1 两侧 file-level 封死。S13-B-1 仍为 loser ID。S7-F-1 ≠ S6-F-5。S5-H-1 保持。catalog 行序契约决策承载（R28-B W3）。npm POSIX `.bin` symlink 静默 no-op（R28-I）**禁止**当优化修。

## 第三十一遍焦点（每切片一个新问题，禁止复述 R1–R30 已否决坐标）

**A — 已合入（空枚举；未铸造新面；injected-callable 平面停驻）**：[R31-A.md](./R31-A.md)。14 文件；基线 `7acb666`。R31-A 以独立物理对象测试检验 `generateEventId` / `readers.readToolBodies` 的 injected-callable 平面：两缝皆非独立物理对象（与 R28-F / R29-A / R30-A 调用流同一剂量通道），对其计时会重剂量既有面。预算 13.6–16.9 µs/gate ⇒ ~68–84 µs/run，相对 ~64–77 µs/run 对照同量级。**禁止**再编号为新 A 轴；**禁止**写入 EXCLUSIONS 表行。非再移植：call-stream ordering / dispatch-history、跨工具调用序 / 跨工具时序拓扑、FITQ、CALLB、OCCX、SEEDX、KFAN。

**B — 已合入（空枚举；未铸造新面；面格闭包审计 / 六门停驻）**：[R31-B.md](./R31-B.md)。10 文件；基线 `94ed3d9`。R31-B 以面格闭包审计关闭六个候选门并全部停驻：catalog-row hidden-class / IC-shape、cascade evidence×failure-class lattice、assignTasks cold-onset segmentation、batch class-composition mix、LINKTOP-in-B、prior alias-resolution pathway。天井 M=2 9.25–11.46 / M=10 21.02–23.17（本 VM 相对 R30-B +10–27% host offset；freeze 0 行）。S12-B-2 条件陆器仍未触发。**禁止**再编号为新 B 轴；**禁止**写入 EXCLUSIONS 表行。非再移植：scale-cube 二阶交互、多任务并发规模立方、SCALEX、ORDX、AGEX、YMIX、SHAPEK、REPRX、LINKTOP。

**C — 已合入（空枚举；铸造 BINDX / ambient intrinsic-binding face）**：[R31-C.md](./R31-C.md)。9 文件；基线 `183df9b`。切片 primitive 经 23,472,380 次 ambient intrinsic 调用/次报告到达（Math.abs 16.95M、max 3.59M、exp 1.61M、isFinite 1.08M）；realm-level 绑定态此前从未当控制变量。四剂量臂（byte-identical runner、3 交错轮、12 进程）呈巨大非平坦响应且每位输出不变（12/12 digest = R28-C/R29-C/R30-C 跨轮 digest）：预导入透明 Math wrapper +19.9–24.0 ms、进程中 patch +185–196 ms、iterator-protector 失效 +2,465–2,477 ms（≈4.75×）。rg 普查证明生产从不 patch intrinsic / prototype — **轴最优已被落地形态占据**，无正候选。唯一切片内反击 C1（`const E = Math.exp` 等 capture-hoist）经 CAP 微探针 2.7–2.9 ns/call × 普查得天真天花板 63–68 ms，被迫做实尺度决斗（落地管线 verbatim replica vs 机械派生 captured-intrinsics 孪生；每进程 1,956 位级保真检查后计时）：同轮 Δ +1.4 / −3.4 / +0.5 ms，符号翻转，≥10× 落入 ±35 ms 带 — TurboFan 在优化帧折叠 ambient load。按噪声 + 语义收窄拒收。本 VM 锚：中位 658.1–660.1；APC 20.7–25.1 < 35；sink=floorSink=7.309（8 次读数位等）。**BINDX** 为 C 切片观察外号，**不是** EXCLUSIONS 表行。与 R31-A injected-callable 停驻门划界：无注入缝、realm-state 剂量、protector-cell 响应无法用既有面坐标表达。非再移植：input object-representation / **REPRX**、输入表示形态、KFAN、COTARG、PROBX、DELINV、PRICEX、VALCLASS。

**D — 已合入（空枚举；未铸造新面；元闭包审计）**：[R31-D.md](./R31-D.md)。14 文件；基线 `82bef36`。R31-D 做元闭包审计：残余 drift-monitor 分析窗配置平面（`monitor.ts` L48–59 的 `windowSize × calibrationDelta`）因零生产调用方归入 **R22-D 所有权**，不是新停驻门、不是新面。S0c 3.38–3.64 ms。**禁止**再编号为新 D 轴；**禁止**写入 EXCLUSIONS 表行。非再移植：approval-profile constraint-list geometry、审批画像形状、SIDEC、PAIRX、CSPELL、ZREP。

**E — 已合入（空枚举；铸造 I/O text-codec topology / per-run transcode-byte manifest face）**：[R31-E.md](./R31-E.md)。10 文件；基线 `adb20d7`。切片内 fs UTF-8 编解码位点恰 3 个（`auto-loop.ts:236` 解码、`bandit-store.ts:14` 解码、`bandit-store.ts:80` 编码）；其余 I/O 腿委托姊妹属地。双仿射清单：调用数 = 每 `.json` 名 1 次，字节 = 文件大小精确和（空 `.json` 付一次零字节解码）。20 个 CPU 热面全部后编解码（零调用）。全 W1 = 7 解码 / 736 字节 / 4 编码（8 跑 × 2 引擎 × 跨 tip 位等）。纯编解码现实预算 0.08–0.81 µs/run（低于 10 ms 栏 12,342–126,414×）；host I/O 主导编解码份额 3–34×。十个消除形态均撞关闭项 / 行为契约 / 姊妹属地。SLICE-CPU 复锚 cmp25 14.9–15.3 µs vs 14.7–15.2（5/6 带内）。Buffer patch 是计量器，不是剂量变量（与 **BINDX** 划界）。**禁止**再编号；**禁止**写入 EXCLUSIONS 表行；未铸 ALLCAPS 外号。非再移植：async suspension-point topology / per-run suspension-manifest、异步边界拓扑、PWL、RVL、RTL、CG、JB、NVG、REPRX、PAIRX、ZREP、LINKTOP、**BINDX**。

**F — 已合入（空枚举；未铸造新面；故障通道处置残格按分解归属）**：[R31-F.md](./R31-F.md)。15 文件；基线 `519101f`。S7-F-1 ≠ S6-F-5 三轮次序省额全正复核。边界 I/O 面格审计后唯一残格（调用方对抛出故障对象的处置）在铸轴前置证明第 1 步失败：分解进 R17-F（制造/拒绝单价）+ R30-F（字段载入）+ R29-F（保留）+ 引擎面（惰性栈物化）；热消费者 `evaluateProposalShadow` 无 catch，干净锚点流 2001 次边界调用零故障；唯一生产 catch（`assertReplayIsolated`）为 once-per-eval。不铸、不计时、不新停驻。本 VM 锚 ×3：r5f C2 119.97/121.51/122.37、r6f partD 119.70/119.52/117.11、r7f partE 127.81/129.00/130.55；r1f 2668 位等、参考比较 167,167,000。**禁止**再编号为新 F 轴；**禁止**写入 EXCLUSIONS 表行。非再移植：boundary output-consumption / snapshot-readback topology、边界输出消费拓扑、输出保留拓扑、调用协议、访问路径、存储状态、PAIRX、**BINDX**、I/O text-codec topology / per-run transcode-byte manifest。

**G — 运行中**：42 文件：`src/run/` 除 A 的 `child-tracking.ts` / `gate-apply.ts`；`src/supervisor/` 除 `model-router.ts`；`src/graph/`；`src/domain/`。基线 `4efee23`。默认对照 R30-G（compute 0.292–0.296 vs I/O 93.9–97.1；digest `06cbcf92c098c8f0` 第二十一次相同，**禁止**再铸 digest 轴号）。非再移植：M1 parent-run child-scheduling lattice、M2 supervised-engine adjudication lattice、decision-gate resolution、join-policy、**RUNLIM** / **CKMIX** / **TERMCLASS** / **SCHEDWIN** / **XPROC** / **NAMESHAPE**、async suspension-point、boundary output-consumption、**ZREP**、**BINDX**、I/O text-codec topology / per-run transcode-byte manifest、injected-callable 平面、R31-B 六个停驻门、R31-D 的 R22-D 单元格、R31-F 故障通道处置残格。若落地：gate + 子代理已有 G harness，不另开 sim 家族。报告 `docs/reports/sota-opt/round-31/R31-G.md`。禁止开 PR。

**H — 运行中**：evaluation 8 + requirement 7 + review 4 + rubric 2（21）。基线 `fd437a9`。S5-H-1 保持（`src/requirement/precedence.ts` L24 `if (fast.length === 0 || slow.length === 0) return [];`）。PATH_RE 仅当 programmatic/adversarial 或真实载荷 ≥~3.2K 无斜杠 `[\w.-]`。默认对照 R30-H（热路径 9178–9647 ns）。非再移植：provenance-resolution pathway、coverage-relation incidence topology、argument-slot、execution-substrate、证据溯源路径拓扑、**BINDX**、I/O text-codec topology / per-run transcode-byte manifest、R31-F 故障通道处置残格。若落地：gate + 既有 H harness；不另开 sim 家族。报告 `docs/reports/sota-opt/round-31/R31-H.md`。禁止开 PR。

**I — 本波派出**：cli 13 + pi-adapter 9 + config 2 + telemetry 1（25）。基线 `8dee7fb`。S8-I-1 两侧 file-level 封死（pi-ai 0.84.1；`all.models.js` 不存在）。默认对照 R30-I（custom−builtin：children +38.2~+49.4 / +22.7~+26.4、track +46.0~+52.9 / +22.4~+26.1、flowchart-flat64 +42.5~+51.0 / +15.2~+20.3）。非再移植：process-lifetime temporal-segment topology、进程寿命拓扑、JSON serialization-form、invocation-identity、**NAMESHAPE**、**BINDX**、I/O text-codec topology / per-run transcode-byte manifest、R31-F 故障通道处置残格。**禁止**把 R28-I 记录的 npm POSIX `.bin` symlink 静默 no-op（`main.ts` L1489 `import.meta.url` vs `pathToFileURL(argv[1])`）当优化修。若落地：复跑 r4i/r5i/r7i（68 / 119 / 80）+ gate。报告 `docs/reports/sota-opt/round-31/R31-I.md`。禁止开 PR。

**J — 其余（第三十一遍）**：29 文件。基线 `fb41417`。J1 锁定。默认对照 R30-J（preferences 134.2–238.7 / 415.9–525.4 µs；jsonl 稳态 61.6–66.9 / 220.1–305.4 µs）。非再移植：durable-artifact referent-binding / filesystem link-topology / **LINKTOP**、外部存储拓扑、**CSPELL**、**ZREP**、**SIDEC**、**BINDX**、I/O text-codec topology / per-run transcode-byte manifest、R31-F 故障通道处置残格。报告 `R31-J.md`。禁止开 PR。

A–J 均要求返回首行 `MODEL_SLUG=claude-fable-5-thinking-xhigh`，末两行 `MORE_OPTIMA=yes|no` 与 `BRANCH=...`。模型必须是 **claude-fable-5-thinking-xhigh**，禁止静默降级。

## 期望

- 默认预期：诚实空枚举（R8–R30 连续空枚举 + R31-A/B/C/D/E/F 已空）。空枚举轴外号只进本 PLAN「禁止再编号」+ 首段 unnamed plan-ban，**不**进 EXCLUSIONS 表。
- 若有候选：理论 + 确定性 sim + 真尺度非噪声（offline-logit ±35 ms；µs/ns 与 once-per-run CLI 噪声拒收）。输家脚本只进报告附录，不提交仓库。
- 平台硬顶 3 并发云 VM；本波保持 G + H + I。
- Round 31 在 J 返回并合入后 10/10 收口，再开 Round 32。
