# Round 31 分区（10 子代理，3-VM 分波）

模型一律 `claude-fable-5-thinking-xhigh`。基线：最新 `cursor/sota-persistent-opt-83a1`。

Round 1–30 已对各区做过三十遍穷尽裁决（Round 30 已关轮，全部空枚举），并落地 S1-F / J1 / S1-C / S1-I / S2-C / S3-C / S4-C / S4-I / S5-C / S5-F / S5-I-1 / S6-C / S6-F-1 / S7-C / S7-F-1 / S7-F-2 / S7-I-1。本轮只接受排除表未覆盖、且理论+仿真达门槛的新更优解。禁止重开任何 X* / S1-* … S31-* 条目（含已合入的 S13-B-1 与 R12–R30 全部空枚举收口，含刚合入的 R28-A … R28-J / R29-A … R29-J / R30-A … R30-J / R31-A）。S7-C 已落地，不得另起平行实现。S7-F-1 不是 S6-F-5。S5-H-1 必须保留。禁止再编号 **FITQ** / **NAMESHAPE** / **SCALEX** / **XPROC** / **YMIX** / **SCHEDWIN** / **KFAN** / **NVG** / **SEEDX** / **TERMCLASS** / **SHAPEK** / **ORDX** / **CKMIX** / **CALLB** / **OCCX** / **JB** / **RUNLIM** / **VALCLASS** / **CG** / **AGEX** / **COTARG** / **PRICEX** / **RTL** / **DELINV** / **PROBX** / **RVL** / **SIDEC** / **PWL** / **ZREP** / **PAIRX** / **CSPELL** / **REPRX** / **LINKTOP**。空枚举轴「M=1 catalog collapse」「approval-authority face」「state serialization-boundary provenance face」「operand-residency census」「adaptation-state coordinate」「value-domain / string content-class census」「task-face optional-field presence topology」「identity-coordinate face」「plan-header identity-geometry face」「injection command-kind face」「consumer-stage string-representation census」「process-environment coordinate」「collation-locale face」「privacy / provider-policy constraint-leg face」「injection-seam default-arm face」「input storage-state face」「edge condition-kind face」「argv surface-form face」「consumer-stage operand-realm face」「scalar numeric value-class face」「batch-cardinality bottom + prelude intercept face」「policy-content tree-geometry face」「input access-path mechanics face」「join-policy face」「process output-channel disposition face」「execution-substrate face」「catalog-face row-arrangement (permutation) face」「input key-set geometry face」「C-slice numeric-kernel execution-substrate instantiation」「contents-store population face」「boundary call-protocol face」「proposal write-path termination-leg face」「decision-gate resolution face」「boundary argument-slot materialization face」「invocation-identity / path-referent spelling × cwd face」「durable-store zero-state provenance」「learned-policy container face」「input reference-identity / aliasing-topology face」「process workload-composition face」「wall-clock sampling topology / timestamp-provenance face」「registry-record string-list payload face」「boundary output-retention / superseded-snapshot liveness face」「M2 supervised-engine adjudication lattice」「coverage-relation incidence topology face」「input-file JSON serialization-form face」「durable-artifact content-spelling face」「call-stream ordering / dispatch-history face」「scale-cube second-order interaction face」「approval-profile constraint-list geometry face」「input object-representation face」「async suspension-point topology / per-run suspension-manifest face」「boundary output-consumption / snapshot-readback topology face」「M1 parent-run child-scheduling lattice」「provenance-resolution pathway face」「process-lifetime temporal-segment topology face」「durable-artifact referent-binding / filesystem link-topology face」不铸 ID、不写入排除表，但禁止再编号或跨区移植。face 名「execution-substrate face」归 R27-H；R28-C 只是该面在 C 区数值内核上的一格剂量，勿另起绰号。**PAIRX** 是 process workload-composition face 的 C 区双估计器共驻实例化观察绰号，不写入排除表。**PWL** 是 proposal write-path termination-leg face 的观察绰号，不写入排除表。**ZREP** 是 durable-store zero-state provenance 的观察绰号，不写入排除表。**CSPELL** 是 durable-artifact content-spelling face 的观察绰号，不写入排除表；与 R29-I input-file JSON serialization-form face 缝不同，勿因撞「拼写」词根而补铸。**REPRX** 是 input object-representation face 的 C 区实例化观察绰号，不写入排除表；与 R30-A call-stream / R29-A aliasing / R28-A key-set geometry 不同面。provenance-resolution pathway face 归 R30-H：每合同项的溯源举证通道（直接 `sourceRefs` vs 合取假设集匹配 vs unsourced）；一条外来假设边翻转整项，与 R29-H coverage-relation incidence topology face 的失败律相反，勿因撞「匹配/覆盖」词根而补铸。process-lifetime temporal-segment topology face 归 R30-I：CLI 进程 spawn-to-exit 的 lead/span/tail/close 分段；tail 是引擎退出地板，勿移植到 live 路由热路径或当作新 B ID。**LINKTOP** 是 durable-artifact referent-binding / filesystem link-topology face 的观察绰号，不写入排除表；与 R28-I 路径拼写 / **ZREP** 零态 / **CSPELL** 内容字节缝不同，勿移植到离线路由热路径。R31-A 停放「injected-callable plane」门（未铸面、不写入排除表）：独立物理对象证明失败，计时会重开 R28-F boundary call-protocol / R29-A aliasing / R30-A dispatch-history；禁止再开或跨区移植。

R7-I 的教训：默认态夹具会遮蔽配置态主路径。本轮须按「配置态 × 命令类」矩阵复核测量盲区。多个切片已有整片预算收口——先复核再找新角度，不要硬凑。若独占分支因兄弟切片合入而快进，rebase docs-only 并把新轴加入不移植清单。

分区与 Round 1 相同（R31-A … R31-J），报告写入 `docs/reports/sota-opt/round-31/`。

状态：第 1–4 波 A 已合入、B/C 运行中、D 本波派出。Round 30 已关轮。

A 切片已合入：空枚举，未铸 S31-A-*。切片 `git diff 7acb666..HEAD` 为空。预算复核 13.6–16.9 µs/gate ⇒ ~68–84 µs/run（与 R26-A/R27-A 带重合，重叠 R30-A/R29-A）。本轮不铸新轴；停放 injected-callable plane 门（`generateEventId` / `readers.readToolBodies` 注入可调用物；生产只占每调用新铸箭头一格；三臂语义逐位同；计时会重开 R28-F / R29-A / R30-A）。R30-A call-stream ordering / dispatch-history face 与 R29-A input reference-identity / aliasing-topology face / R28-A input key-set geometry face 不补铸。基线 `7acb666` 空 diff 再确认。

B 切片 = 10 文件（live：r0 / assign / assign-plan / policy / live-cascade / live-selection / analyze-task / primary-catalog / catalog-model + `src/supervisor/model-router.ts`）。天花板默认 R30-B：M=2 8.87–9.13 / M=10 17.34–18.23 ms/eval。S12-B-2 / S13-B-1 重开条件未触发。R30-B scale-cube second-order interaction face 与 R29-B learned-policy container face / R28-B catalog-face row-arrangement (permutation) face / R27-B batch-cardinality bottom + prelude intercept face / R24-B M=1 catalog collapse 不补铸。目录行预排序是行为变更（R28-B W3）。R30-A call-stream ordering / dispatch-history face / R31-A injected-callable plane（停放门） / R30-H provenance-resolution pathway face / R30-I process-lifetime temporal-segment topology face / R30-J **LINKTOP** / R28-J **ZREP** 不要移植。基线 `94ed3d9` 预期空 diff。运行中。若落地：重跑 live 路由仿真 + r31b。

C 切片 = 9 文件（offline：r1 / r1-shadow-report / posterior / offline-logit / offline-prob-add / propensity / lin-alg / bandit / shadow）。生产中位默认 R30-C：658.7–663.0 ms。APC ceiling 18.8–26.6 < 35；sink=7.309。**REPRX** / **PROBX** / **PRICEX** / **AGEX** / **FITQ** / **SCALEX** / **YMIX** / **SEEDX** / **ORDX** / **OCCX** / **PAIRX** / **LINKTOP** 不补铸。R30-C input object-representation face / R29-C process workload-composition face（C 区双估计器共驻实例化）/ R28-C C-slice numeric-kernel execution-substrate instantiation（face 名归 R27-H）/ R27-H execution-substrate face 不要移植。R30-J **LINKTOP** / R30-I process-lifetime temporal-segment topology face / R31-A injected-callable plane（停放门） 不要移植。基线 `183df9b` 预期空 diff。运行中。若落地：重跑 r1c–r7c（8028 / 14420 / 14730 / 24888 / 28555 / 25483 / 6193）+ r31c。

D 切片 = 14 文件（`src/adaptation/`）。eval 地板默认 R30-D：S0c 3.29–3.66 / 同进程 3.13–3.27 ms。S9-D-4 / S12-D-1 永不再开。R30-D approval-profile constraint-list geometry face 与 R29-D registry-record string-list payload face / R28-D contents-store population face / R27-D policy-content tree-geometry face / R26-D injection-seam default-arm face / R25-D identity-coordinate face / R24-D approval-authority face 不补铸（几何面 ≠ 权威面）。R30-H provenance-resolution pathway face / R29-E wall-clock sampling topology / timestamp-provenance face / R29-C **PAIRX** / R28-J **ZREP** / R28-E **PWL** / R31-A injected-callable plane（停放门） / R30-J **LINKTOP** 不要移植。基线 `82bef36` 预期空 diff。本波派出。若落地：重跑 adaptation eval 仿真 + r31d。

E 切片 = 10 文件（`src/learning/`）。SLICE-CPU 默认 R30-E：14.7–15.2 µs/run（cmp25 回放）。S8-E-1 / S9-E-2 / S13-B-1 未重开。**PWL** / **RVL** / **RTL** / **CG** / **JB** / **NVG** 不补铸。R30-E async suspension-point topology / per-run suspension-manifest face / R29-E wall-clock sampling topology / timestamp-provenance face / R30-C **REPRX** / R29-C **PAIRX** / R28-J **ZREP** 不要移植。基线 `adb20d7` 预期空 diff。等待派出。

F 切片 = 15 文件（`src/experiments/`）。锚点默认 R30-F：r5f 119.31/120.55/121.20、r6f 120.71/121.34/121.90、r7f 128.31/130.42/132.69。S7-F-1 ≠ S6-F-5。R30-F boundary output-consumption / snapshot-readback topology face 与 R29-F boundary output-retention / superseded-snapshot liveness face / R28-F boundary call-protocol face / R27-F input access-path mechanics face / R26-F input storage-state face 不补铸。R29-C **PAIRX**（编排位点 `attribution-report.ts`）不要移植。基线 `519101f` 预期空 diff。等待派出。若落地：重跑 r1f/r5f/r6f/r7f（2668 / 224 / 27 / 169）+ r31f。

G 切片 = 42 文件（`src/run/` 除 A 两文件；`src/supervisor/` 除 model-router；`src/graph/`；`src/domain/`）。计算顶默认 R30-G：0.292–0.296 vs I/O 93.9–97.1 ms（~309–337×）。digest `06cbcf92c098c8f0` 第二十一次逐位相同，勿补铸 digest。R30-G M1 parent-run child-scheduling lattice 与 R29-G M2 supervised-engine adjudication lattice / R28-G decision-gate resolution face / R27-G join-policy face / **RUNLIM** / **CKMIX** / **TERMCLASS** / **SCHEDWIN** / **XPROC** / **NAMESHAPE** 不补铸（M1 ≠ M2）。R30-E async suspension-point topology / per-run suspension-manifest face / R30-F boundary output-consumption / snapshot-readback topology face / R28-J **ZREP** 不要移植。基线 `4efee23` 预期空 diff。等待派出。

H 切片 = 21 文件（evaluation **8** + requirement 7 + review 4 + rubric 2）。S5-H-1 必须保留。R30-H provenance-resolution pathway face 与 R29-H coverage-relation incidence topology face / R28-H boundary argument-slot materialization face / R27-H execution-substrate face / R26-H consumer-stage operand-realm face / R25-H consumer-stage string-representation census / R24-H operand-residency census 不补铸。热层默认 R30-H：9178–9647 ns。PATH_RE 仅当目标为程序化/对抗或真实载荷 ≥~3.2K 无斜杠 `[\w.-]` 才重开。R30-E async suspension-point topology / per-run suspension-manifest face / R30-F boundary output-consumption / snapshot-readback topology face / R30-G M1 parent-run child-scheduling lattice / R30-C **REPRX** / R30-D approval-profile constraint-list geometry face / R29-G M2 supervised-engine adjudication lattice 不要移植。基线 `fd437a9` 预期空 diff。等待派出。

I 切片 = 25 文件（cli 13 + pi-adapter 9 + config 2 + telemetry 1）。S8-I-1 两臂文件级 blocked。R30-I process-lifetime temporal-segment topology face 与 R29-I input-file JSON serialization-form face / R28-I invocation-identity / path-referent spelling × cwd face / R27-I process output-channel disposition face / R26-I argv surface-form face / R25-I process-environment coordinate / **NAMESHAPE** 不补铸。R29-J **CSPELL** / R30-H provenance-resolution pathway face / R30-G M1 parent-run child-scheduling lattice / R30-C **REPRX** 不要移植（缝不同）。npm POSIX `.bin` 符号链接 silent no-op 已归档，不在本战役改。custom−builtin 默认 R30-I：children +38.2~+49.4 / +22.7~+26.4、track +46.0~+52.9 / +22.4~+26.1、flowchart-flat64 +42.5~+51.0 / +15.2~+20.3 ms。基线 `8dee7fb` 预期空 diff。等待派出。若落地：重跑 r4i/r5i/r7i（68 / 119 / 80）+ r31i。

J 切片 = 29 文件。J1 / S5-J-3 / S6-J-1 / S8-J-2 钉死。禁止去 fsync。R30-J **LINKTOP** 与 R29-J **CSPELL** / R28-J **ZREP** / R27-J **SIDEC** / R26-J **DELINV** / R25-J **COTARG** / R24-J **VALCLASS** / R23-J **CALLB** / R22-J **SHAPEK** / R21-J **KFAN** 不补铸。R29-I input-file JSON serialization-form face / R30-I process-lifetime temporal-segment topology face / R30-H provenance-resolution pathway face / R30-G M1 parent-run child-scheduling lattice 不要移植（缝不同）。file-lock retryMs 已由 D/E 定价，勿作为 J ID 补铸。基线 `fb41417` 预期空 diff。等待派出。若落地：重跑 J1（2468）+ r31j。I/O 地板 R30-J：preferences 134.2–238.7 / 415.9–525.4 µs；jsonl 稳态 61.6–66.9 / 220.1–305.4 µs。
