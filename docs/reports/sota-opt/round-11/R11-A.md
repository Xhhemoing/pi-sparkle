MODEL_SLUG=claude-fable-5-thinking-xhigh

# R11-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第十一遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 11 / R11-A（Round 1–10 同区第十一遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `9c45fa1`（含 R10-G/I 收口与 Round 11 开轮）
**分支:** `cursor/r11-a-tracking-eleventh-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无达门槛的新更优解，切片零代码改动；整片预算收口第九次复核成立。**
切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R10-A 至本轮基线
（`9c45fa1`）**逐字节未变**（`git diff 7acb666..9c45fa1 -- src/tracking/
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，十一遍全程零 diff）。
调用面再次达到最强冻结：R10-A 基线（`89f0455`）以来 `git diff
89f0455..9c45fa1 -- src/` **完全为空**（期间提交全部为 docs），生产调用方
经全库 grep 复核仍为 `supervisor.ts:483`（applyTrackingGate/nextTrackingSeq，
两个 settle 调用点 601/711 均不传 trackingAssessment ⇒ 生产 O(1) no-op，
R9-A 事实第三次复验）/ `coordinator.ts:444` / `flowchart-run.ts:320`
（applyChildThreeLine），每子结果一次（~5 次/run），事件表几十级（41）。
R10-A 的 ~59–70 µs/run 预算天花板经本轮在当前 VM（Node v22.22.2）实测复核为
**~60–74 µs/run**（2×2 锚点矩阵三次独立运行全部同带），量级结论不变。

本轮在完整排除表之上做**第十一组新角度枚举**，把搜查粒度下探到
R10-A「无剩余未点名结构」声明之下的**结果字段读者面**与**守卫常量面**，
得到 2 个此前确实未点名的新候选（S11-A-1 守卫常量折叠 / S11-A-2 门控结果
死字段扫描），全部经理论（引理 D + 调用矩阵字段级 grep 事实）+ 确定性仿真
（seeded mulberry32，6000 组引理 fuzz + 4000 组端到端 fuzz 逐位等价，三次
独立运行结论逐位一致）裁决后淘汰：两者孤立折叠量各 ~14 ns/子结果与
~66 ns/gate（合计 ~0.4 µs/run，比落地线低约五个量级），e2e delta 三次运行
**双双符号翻转**＝噪声；S11-A-2 落地还需收窄公开 `GateApplyResult`
（S1-F-6/S8-A-3 族）。未重开任何 X* / S1-* … S10-* 条目；S10-A-1 /
S9-A-1 / S7-A-1 换名检查通过（见 §3）。本轮的两个发现**收紧而非动摇**
整片预算收口——第十一遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：roller / analysis / gates / config /
  combined-score / from-child / index / turn / isolation / human-score /
  prescore / types）+ `src/run/child-tracking.ts` + `src/run/gate-apply.ts`，
  **14 文件全部实际读码**。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表，含 S10-A-1 与
  S10-G-1；R10-I 未铸 ID，确认）/ round-11/PLAN.md / round-10/PLAN.md +
  R10-A.md / round-09/R9-A.md … round-01/R1-A.md。候选枚举刻意绕开全部
  既有排除（X0-4、X0-5、X0-6、X1-1、X1-2、X2-4、S1-A-1..9、S2-A-1..6、
  S3-A-1..4、S4-A-1..3、S5-A-1..3、S6-A-1..3、S7-A-1..4、S8-A-1..3、
  S9-A-1、S10-A-1、S6-G-7 及全部无 ID 收口裁决），只探索**未被点名的
  第十一组新角度**：apply 守卫常量折叠（S11-A-1）与门控结果死字段
  读者面（S11-A-2）。
- 环境：本 VM 默认 Node v22.14.0，按 engines（>=22.19.0）经 nvm 切至
  v22.22.2 后 `pnpm install --frozen-lockfile`，全部测量在 v22.22.2 完成。
- 换名重提检查（识别为既有方案换名/换位点，**未列为新候选**）：
  - `PrescoreInput.independentEvidence` / `actorSelfScore` 死输入字段收窄
    （computePrescore 显式 `void` 两字段；prescoreInputFromObservation 每
    子结果算一次 independentEvidence 布尔）= **S8-A-3 死输入字段本体换位点**
    ——`void` 语句是刻意的规格意图文档（保留位），删字段属公开类型变更，
    计算本体两次比较 ns 级，拒列；
  - `observationFromChild` tester 无验收时 `["test"]` 字面量与
    requiredChecks/constraints 逐 gate map 分配的享元/单例化 =
    S7-B-5/S1-A-7 可观察身份族 + S1-B-5 分配噪声族（R9-A/R10-A 各拒过
    一次），维持；
  - gate-apply 幂等 TRACKING_ASSESSMENT 路径上
    `mapGateDirective(input.assessment).directive` 构造整 mapped 对象只取
    一字段 = S1-B-5 微观常数族，且该路径生产死（S8-A-1 fresh-seq 事实），
    拒列；
  - RUN_BLOCKED `requiredEvidence: [...evidenceRefs]` 拷贝省略 = S1-A-7
    可观察身份族，拒列；
  - caller/CAS 双 `hashAssessment` 的任何去重（含 WeakMap 身份 memo）=
    R1-A CAS fail-closed 契约裁决 + X1-1/S8-G-2（R10-A §0 已拒），维持；
  - 5 次事件表扫描（episodeId/seq/幂等×2/status）单遍合并或索引化 =
    X2-4/X0-4，批量门控 API = X0-4+X1-2（R9-A/R10-A 已拒），维持；
  - `Number(x.toFixed(4))` 舍入任何改写/快路径 = S6-A-1 反例域，维持。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无命令权、
  H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升 proposal-first）
  同样天然满足。不声称 Outcome-supported；Checkpoint F-PROD 仍开放
  （ADR-005）。无赢家 ⇒ 零生产改动，loser 仿真只留在本报告附录，不入库。

## 1. 预算支配论证复核（方法第 1 步，不假设直接实测）

R3-A §3 … R10-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..9c45fa1 -- <切片>` 为空，逐字节
   一致（十一遍全程零 diff）。
2. **调用面未变（最强形式再现）**：`git diff 89f0455..9c45fa1 -- src/`
   **完全为空**——R10-A 以来仓库仅有 docs 提交。生产调用方与 R9-A/R10-A
   落档的调用矩阵事实（supervisor settle no-op / hashSummary 零流量 /
   `src/track/` 零导入）经本轮全库 grep 全部复验不变。

本轮在当前 VM 重测 R10-A closed 的**门控结局 × 合同 2×2 满矩阵**
（三次独立运行）：

```text
anchor [PASSED x no-contract（历代谱系锚点）]: 12.1–12.9 µs/gate (+1 event) => ~60–65 µs/run
anchor [PASSED x contract(3)]:                 12.0–12.9 µs/gate (+1 event) => ~60–65 µs/run
anchor [FAILED x no-contract]:                 13.9–14.6 µs/gate (+3 events) => ~69–73 µs/run
anchor [FAILED x contract(3)]:                 14.0–14.9 µs/gate (+3 events) => ~70–74 µs/run
```

与 R10-A（11.8–11.9 / 13.7–14.0）与 R4-A/R9-A 谱系同带；四格三次运行
无悬崖、无空洞。**本轮新落档的字段级调用矩阵事实**（S11-A-2 的证据基底，
也是对 R9-A「coordinator/flowchart-run 只读 {events, result}」的粒度收紧）：

- `coordinator.ts` 对 `gated.result` **只读 `directive`**（455/460 两处
  比较）；
- `flowchart-run.ts:320` **完全不读 `gated.result`**（只切 `gated.events`
  追加）；
- `settleSupervisedOutcome` 只读 `applied.events`（且生产 no-op）；
- 全库 grep `\.runStatus|result\.applied|\.transitionId`：除 gate-apply.ts
  自身与 events.ts 写侧校验外 `src/` **零读者**。
  ⇒ `GateApplyResult.runStatus` / `.applied` / `.transitionId` 生产零读者，
  门控结果的生产消费面**只有 `directive` 一个字段**。

即使把整个切片优化到零成本，节省上界 ~0.07 ms/run，仍比战役落地线
（数十~数百 ms 或复杂度类下降）低**约三个量级**。复杂度类下降的仅存位点
维持既有排除（X0-4/X2-4 事件表索引化、S1-A-1/S1-A-9 反向早退、X1-1
hashSummary 跨调用缓存、S6-G-7 读侧内存镜像、R1-A 不可变累计快照构造
下界、S8-A-1 fresh-seq 死扫描、S9-A-1 死输出尾、S10-A-1 门控二元化）。
**支配论证第九次复核成立。**

## 2. 候选总表（全部淘汰，无赢家落地）

| ID | 候选 | 理论 | 仿真等价 | 真实规模基准 | 裁决 |
| --- | --- | --- | --- | --- | --- |
| S11-A-1 | `assessChildObservation` **apply 守卫常量折叠**：入口守卫收窄 kind ∈ {PASSED, FAILED} 后，`prescoreInputFromObservation` 恒推入一个带 exitCode(0\|1) 的 task-result 工具情形。**引理 D**：(i) evidence-consistency 恒 observed（PASSED ⇒ PASS：failedTool=false ∧ observedPass=true；FAILED ⇒ FAIL：failedTool=true）且 hardRelated；(ii) scope-safety 恒 observed PASS（escaped 硬编码 false、writePaths 恒空、toolSituations 非空）；(iii) ⇒ `hasHardPassOrFail ≡ true` 且 observedWeight ≥ 2 ⇒ coverage ≥ 0.4 > 0（紧下界 0.5，fuzz 恰打到）——`shouldApplyThreeLine` 内部调用点**恒真**，some 扫描 + 该调用是对**全体输入**（非仅生产形态）可证的死工作。候选＝删除 from-child.ts:57–68 守卫块（`shouldApplyThreeLine` 公开导出本体不动）。**与 S5-A-3/S6-A-3/S7-A-1/S9-A-1/S10-A-1 关键不同：不需要平行路径也不需要公开旗标——本家族十一遍来首个可作内部编辑落地的折叠** | 免每子结果 1 次 ≤6 元素 some 扫描 + 1 次 shouldApplyThreeLine 调用 | ✅ 引理 D：6000 组 fuzz 全体通过入口守卫的观察上守卫恒真，min coverage **恰 0.5**（解析下界 0.4，紧下界 0.5＝FAILED×有 checks×无合同×CANCELLED 格）；✅ e2e：4000 组 fuzz（PASSED/FAILED/UNOBSERVED/缺失 verification × 角色 × 验收 × 合同约束 0–4 × 证据形态 × 对抗摘要 × 附着/未附着/空事件表）经 applyChildThreeLine 的 {events, result} **逐位一致** + skip 引用身份一致；三次独立运行逐位一致 | 孤立折叠量 **13.9–15.0 ns/子结果**（×~5/run ⇒ ~0.07 µs/run）；whole-assess delta 80–190 ns（含 PIC 语境差）；**e2e delta 三次运行符号翻转**：PASSED +329/−0/+161、FAILED +110/+88/**−65** ns/gate ＝ 噪声带 | 淘汰：量级比落地线低约**六个量级**（本切片历代候选最小之一）；e2e 已在噪声内不可测；守卫是三线规格「coverage>0 ∧ 硬 PASS/FAIL 才计分」的**意图文档与防御纵深锚点**（S4-A-3 内联否决方向 + S9-D-3「零成本保留」先例——若未来 prescoreInputFromObservation 改变构造不变量，守卫是唯一规格屏障）；被 §1 第九次复核的整片预算收口覆盖 |
| S11-A-2 | gate-apply **none 路径死 `from` 扫描 / 门控结果死字段**：fresh apply 且 directive="none"（生产 PASSED 常态）时 `currentGateStatus(events)` 的 O(E) 折叠只喂 `result.runStatus`，而 §1 字段级 grep 证明 runStatus/applied/transitionId 生产零读者（coordinator 只读 directive，flowchart-run 整个 result 不读，supervisor settle 丢弃 result 且生产 no-op）——该扫描在生产 none 路径是**死输出工作**（S9-A-1 死输出读者机制在 gate-apply 结果面的对偶，非其重开：turn 路径未触碰）。候选＝none 路径跳过扫描并收窄结果（去 runStatus）；非 none 路径与两条幂等路径原样（from 进 GATE_TRANSITION payload 可观察 / 生产死） | none 路径免 1 次 O(E) 状态折叠 | ✅ 3 次独立运行：apply 路径 fuzz 中 events **逐位一致** + 消费投影（directive/applied/transitionId）逐位一致 + 非 none 路径全 result 逐位一致；none 路径结果**恰好只少 runStatus**（公开面代价被精确断言） | 死扫描实测 **65.9–67.1 ns/gate**（E=41；×~5 ⇒ 上界 ~0.33 µs/run）；**e2e delta 三次运行符号翻转**：−130/+3/−346 ns/gate ＝ 噪声带 | 淘汰：落地需收窄公开 `GateApplyResult`（4 字段形状与 `'runStatus' in result` 可观察，S1-F-6/S8-A-3 公开类型族）或 X1-2 平行入口；孤立量 ~0.3 µs/run 比落地线低约五个量级且 e2e 不可测；被 §1 收口覆盖 |

## 3. 关键裁决细节

### 第十一遍还剩什么（两个候选的定位）

R10-A 声明「无剩余未点名结构」在其命名粒度上成立；本轮把粒度再下探两层
仍找到两处：**守卫常量面**（S11-A-1——lemma C 证了门控结局的值域坍缩，
但没人指出 apply 守卫本身对全体输入恒真、可作内部编辑删除）与**结果字段
读者面**（S11-A-2——S9-A-1 证了 `ChildTrackingDecision.turn/.prescore`
零读者，但没人把 `GateApplyResult` 拆到字段级：生产消费面只剩 directive
一个字段）。两处的绝对量（14 ns / 66 ns）恰好印证收口：粒度每下探一层，
可折叠量再掉一个量级——第十一遍的「新结构」已进入单次函数调用与单次
O(41) 扫描的粒度，离落地线五到六个量级。

### S10-A-1 / S9-A-1 / S7-A-1 换名检查（按令专项）

- **S11-A-1 不是 S10-A-1 换名**：S10-A-1 折叠的是 `evaluateGates` +
  `mapGateDirective` 的**门控结局**（需平行 turn 路径/公开旗标，四度
  拒绝的结构，本轮未触碰）；S11-A-1 折叠的是 from-child 的 **apply
  守卫**（`shouldApplyThreeLine` 调用点），利用面是内部编辑、与该结构
  正交。引理 D 与引理 C 共享 from-child 构造不变量供体（该供体也是
  S5-A-3/S6-A-3 的供体），但结论对象（apply 决策 vs 门控结局）与落地
  结构（内部删除 vs 平行分派）均不同。S10-A-1 维持关闭。
- **S11-A-2 不是 S9-A-1 换名**：S9-A-1 跳过 runTrackingTurn 的死输出尾
  （rollSummary/packet，需平行 turn 路径）；S11-A-2 在 gate-apply 的
  结果面用同一「生产读者消失」证明方法，但位点、被折叠机器（O(E) 状态
  折叠 vs O(累计量) 滚轮）与公开面代价（GateApplyResult 收窄 vs
  ChildTrackingDecision 收窄）都不同。turn 路径零触碰，S9-A-1 维持关闭。
- **S7-A-1 未被触碰**：human 管道常量折叠本轮无任何形式重提；两个候选
  均不涉 extractHumanScore/combineScore 面。维持关闭。
- 反向检查：两个新候选也不是 S1-A-1（把扫描变快 vs 证明扫描死）、
  S8-A-1（幂等 find vs 状态折叠）、S4-A-3（改公开函数/内联残余谓词 vs
  证明残余谓词恒真后整体删除）的换名——机制、证明义务与落地面各不同；
  裁决结局（量级 + 噪声 + 公开面/纵深）与它们同构，这正是预算收口的
  预言力而非重复提案。

### 引理 D 的紧下界（记录备查）

coverage 的解析下界由 evidence+scope 双恒 observed 给出（≥ 2/5 = 0.4）；
紧下界 **0.5** 在 FAILED × requiredChecks 非空 × 无合同 × outcome=CANCELLED
格达到（applicable=4：evidence FAIL、scope PASS、check-coverage
UNOBSERVED、progress UNOBSERVED ⇒ observed=2）。fuzz min coverage 恰打
0.5，证明引理是紧的。PASSED 侧因 completedChecks=required 拷贝
（S5-A-3 引理）coverage ≥ 0.75。该下界与 lemma C 的 P ≥ 2/3 同列
from-child 构造不变量档案；未来任何以「coverage 可为 0 / 无硬维度」为
前提的 from-child 提案可直接引用本引理否决。

### 逐文件收口（第十一遍新角度复查）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `from-child.ts` | apply 守卫常量折叠（引理 D——本文件十一遍来最后一处未点名可内部编辑结构）；S1-A-2 / S2-A-2/6 / S4-A-3 / S5-A-3 / S6-A-2/3 / S9-A-1 / S10-A-1 九层收口维持 | S11-A-1 淘汰 |
| `gate-apply.ts` | none 路径死 from 扫描 + GateApplyResult 字段级死读者（本文件十一遍来最后一处未点名死输出结构）；幂等路径 mapped 整对象构造 = S1-B-5 + S8-A-1 拒列（见 §0）；双 find / currentGateStatus / nextTrackingSeq 维持 X2-4/X0-4/S1-A-1/S1-A-9/S8-A-1；FAIL_CLOSED 兜底维持保留 | S11-A-2 淘汰 |
| `prescore.ts` | 引理 D 的 evidence/scope 恒 observed 供体；`void` 死字段 = S8-A-3 换位点拒列（见 §0）；S1-A-4 / S3-A-3 / S6-A-1 收口维持 | S11-A-1 覆盖 |
| `turn.ts` | 零触碰复查：S7-A-1 / S9-A-1 / S10-A-1 收口维持；S1-A-7 / S2-A-3 / S3-A-2/3/4 / S4-A-1 / S5-A-1 / S8-A-3 维持 | 无新候选 |
| `gates.ts` | S10-A-1 / S7-A-3 / S1-A-6 / S8-A-3 收口维持 | 无新候选 |
| `roller.ts` | S9-A-1 死输出尾收口维持；S1-A-5 / S2-A-1 / S3-A-1 / S7-A-2 维持 | 无新候选 |
| `child-tracking.ts` | 字段级结果读者事实的传导面（结果直传 applyChildThreeLine）；`["test"]` 字面量享元拒列（见 §0）；5 次扫描维持 X2-4/X0-4；S4-A-2 / S6-G-7 维持 | S11-A-2 覆盖 |
| `human-score.ts` | X0-6 / S1-A-3 / R3-A 对偶面 / R5-A 频次重排收口维持；from-child 路径整管道死 = S7-A-1 已裁决 | 无新候选 |
| `combined-score.ts` | S6-A-1 / S7-A-4 反例收口维持；isMachineScore 边界防御保留 | 无新候选 |
| `types.ts` | parse 面 = S1-A-8/X0-5；哈希面 = S2-A-5/S5-A-2/S3-B-3；hashSummary 生产零流量维持 | 无新候选 |
| `analysis.ts` | S8-A-2 收口维持（test-only + choke point） | 无新候选 |
| `isolation.ts` / `config.ts` / `index.ts` | O(1) 谓词 / 常量 / 纯再导出 | 无新候选 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家不落
仿真文件；loser 仿真完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R10-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm typecheck` / `pnpm lint` / `pnpm build` 全绿
（Node v22.22.2）。

仿真（临时脚本；seed 固定可复现，三次独立运行）：

```text
run 1:
S11-A-1 Lemma D: 6000 fuzz trials, 6000 past entry guard, apply-guard constant-true in all; min coverage=0.5 (floor 0.4, > 0)
S11-A-1 e2e: 4000 fuzz trials bit-equal (events, result, skip identity) | S11-A-2: events + consumed projection bit-equal; none path drops only runStatus
S11-A-1 bench folded work: some-scan + shouldApplyThreeLine = 13.9ns/child result
S11-A-1 bench whole assess: PASSED delta=190ns | FAILED delta=127ns
S11-A-1 bench e2e (E=41, contract 3): PASSED delta=329ns/gate | FAILED delta=110ns/gate
S11-A-2 bench dead scan: currentGateStatus over E=41 = 67.1ns/gate | e2e delta=-130ns/gate (SIGN FLIP)
anchor: 12.9 / 12.9 / 14.6 / 14.9 us => ~65-74 us/run
ALL EQUIVALENCE CHECKS PASSED (deterministic, seeded)

run 2:
S11-A-1 folded=15.0ns | whole assess PASSED=175ns FAILED=80ns | e2e PASSED=-0ns FAILED=+88ns (SIGN FLIP)
S11-A-2 dead scan=65.9ns | e2e delta=+3ns
anchor: 12.1 / 12.2 / 14.2 / 14.3 us => ~60-71 us/run
ALL EQUIVALENCE CHECKS PASSED (deterministic, seeded)

run 3:
S11-A-1 folded=14.3ns | whole assess PASSED=99ns FAILED=88ns | e2e PASSED=+161ns FAILED=-65ns (SIGN FLIP)
S11-A-2 dead scan=65.9ns | e2e delta=-346ns (SIGN FLIP)
anchor: 12.2 / 12.0 / 13.9 / 14.0 us => ~60-70 us/run
ALL EQUIVALENCE CHECKS PASSED (deterministic, seeded)
```

三次独立运行等价/引理结论逐位一致（6000 组引理 fuzz、4000 组 e2e fuzz
全绿确定性重现，min coverage 恰 0.5 三次一致）；两候选孤立折叠量方向
稳定（13.9–15.0 ns / 65.9–67.1 ns）；两候选 e2e delta 均现符号翻转＝
噪声裁决本体；锚点矩阵四格三次同带。裁决方向不变。

## 6. 新增排除 ID（请并入全局 EXCLUSIONS.md「本战役新增」）

| ID | 方案 | 原因 |
| --- | --- | --- |
| S11-A-1 | from-child apply 守卫常量折叠（引理 D：入口守卫收窄后 evidence/scope 恒 observed ⇒ hasHardPassOrFail ≡ true ∧ coverage ≥ 0.5 > 0 ⇒ 内部 shouldApplyThreeLine 调用恒真，some 扫描 + 调用可整体删除；对全体输入等价，无需平行路径/公开旗标） | 等价（6000 引理 + 4000 e2e fuzz 逐位，三次一致）但折叠量 13.9–15.0ns/子结果 ⇒ ~0.07µs/run，低于落地线约六个量级；e2e 符号翻转（+329/−0/+161ns）＝噪声；守卫是规格意图文档/防御纵深锚点（S4-A-3 方向 + S9-D-3 零成本保留先例） |
| S11-A-2 | gate-apply none 路径死 from 扫描跳过 + GateApplyResult 死字段收窄（runStatus/applied/transitionId 生产零读者，消费面仅 directive；none 路径 currentGateStatus 为死输出工作） | 消费投影等价（3 次 fuzz：events 逐位 + directive/applied/transitionId 逐位 + 非 none 全 result 逐位）但落地需收窄公开 GateApplyResult（S1-F-6/S8-A-3 族）或 X1-2 平行入口；死扫描 65.9–67.1ns/gate ⇒ ~0.33µs/run；e2e 符号翻转（−130/+3/−346ns）＝噪声 |

重开条件：S11-A-1 需先破坏引理 D 的前提——`prescoreInputFromObservation`
不再无条件推入带 exitCode 的 task-result 工具情形、或 escaped/writePaths
构造不变量被推翻（届时守卫恢复活性，本候选自动失效而非重开）；或
子结果频次增长 ≥3 个量级。S11-A-2 需 `GateApplyResult` 公开面解冻（字段
被正式声明 test-only 并移除）或出现读 runStatus 的高频生产调用方（届时
该字段变活、候选自动失效）；事件表规模增 ≥2–3 个量级时与 X0-4/X2-4/
S1-A-1 一并重开。整片预算支配论证（§1）的重开条件不变：run 事件表或每
turn 集合规模增长 ≥2–3 个量级，届时 S1-A-1、S2-A-1、S2-A-3、S4-A-1、
S7-A-2、S8-A-1(B)、S9-A-1、S10-A-1、S11-A-1、S11-A-2 可凭既有等价性
证据优先重开。

## 附录：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.ts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xb11a01`–`0xb11a04`。

```ts
/**
 * R11-A deterministic equivalence + benchmark simulation (eleventh pass).
 * Adjudicates the fresh candidates S11-A-1 and S11-A-2 against the current
 * implementations in src/tracking + src/run/{child-tracking,gate-apply},
 * and re-verifies the whole-slice budget anchor matrix (R3-A..R10-A lineage).
 *
 * S11-A-1 (NEW angle): apply-guard constancy fold. Lemma D: for EVERY
 * ChildObservation passing assessChildObservation's entry guard
 * (verification.kind in {PASSED, FAILED}), prescoreInputFromObservation
 * always emits exactly one task-result tool situation with a defined
 * exitCode (0|1), so the evidence-consistency dimension is always observed
 * (PASS on PASSED, FAIL on FAILED; hardRelated) and scope-safety is always
 * observed PASS => hasHardPassOrFail === true and coverage >= 0.4 > 0 =>
 * shouldApplyThreeLine(...) === true at the internal call site. The
 * hasHardPassOrFail some-scan + the shouldApplyThreeLine call are provably
 * dead work for ALL inputs (not just production shapes) -- exploitable as an
 * internal edit with NO parallel path / public flag (unlike S4-A-3 / S10-A-1).
 *
 * S11-A-2 (NEW angle): gate-apply none-path dead `from` scan. Call-matrix
 * facts (grep, this round): GateApplyResult.runStatus / .applied /
 * .transitionId have ZERO src/ readers outside gate-apply.ts itself --
 * coordinator.ts:455/460 reads only result.directive, flowchart-run.ts:320
 * ignores gated.result entirely, supervisor settle discards the result (and
 * is a production no-op per R9-A). On the fresh apply path with directive
 * "none" (the production PASSED case), currentGateStatus(events) -- a full
 * O(E) fold -- feeds ONLY result.runStatus, i.e. dead output work. A
 * narrowed-result candidate (drops runStatus on the none path, skipping the
 * scan) is bit-equal on everything production consumes.
 *
 * Seeded PRNG (mulberry32) -> fully reproducible.
 * Seeds: 0xb11a01 .. 0xb11a04.
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation,
  shouldApplyThreeLine,
  type ChildObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import {
  applyTrackingGate,
  nextTrackingSeq,
  type GateApplyResult
} from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event, type GateDirective, type GateRunStatus } from "/workspace/src/run/events.js";
import type { TrackingAssessment } from "/workspace/src/tracking/types.js";
import { hashAssessment } from "/workspace/src/tracking/types.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import { createMessageId, type RunId } from "/workspace/src/domain/ids.js";
import type { IsoTimestamp } from "/workspace/src/domain/timestamp.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-24T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

/* ============================================================
 * Shared: realistic event tables (R3-A..R10-A harness lineage).
 * ============================================================ */
function buildEventTable(opts: { attached: boolean; filler: number }): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (opts.attached) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < opts.filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function genObservation(rng: () => number, trial: number): ChildObservation {
  const kind = pick(rng, ["PASSED", "FAILED"] as const);
  return {
    taskId: `tsk_${trial % 7}`,
    role: pick(rng, ["implementer", "tester", "scout", "worker"]),
    outcome: pick(rng, ["SUCCESS", "PARTIAL", "FAILURE", "CANCELLED", "TIMEOUT"] as const),
    summary: pick(rng, ["tests passed", "child completed the task", "", "wip", "verified output", "all checks succeed"]),
    evidenceIds: rng() < 0.75 ? [`evd_${trial}`, "evd_shared"] : [],
    artifactIds: rng() < 0.5 ? [`art_${trial}`] : [],
    verification: { kind, evidenceIds: rng() < 0.8 ? [`evd_v_${trial}`] : [] },
    requiredChecks: rng() < 0.5 ? (rng() < 0.5 ? ["test"] : ["chk_0", "chk_1"]) : [],
    constraints: Array.from({ length: Math.floor(rng() * 5) }, (_, i) => ({
      id: `c_${i}`,
      text: `constraint ${i}`,
      kind: "constraint" as const,
      mandatory: true as const
    }))
  };
}

/* ============================================================
 * S11-A-1 Lemma D fuzz over the REAL code: for every observation
 * passing the entry guard (kind in {PASSED, FAILED}),
 *   - dimensions[0] is evidence-consistency, observed (PASS|FAIL),
 *     hardRelated (PASS on PASSED, FAIL on FAILED),
 *   - scope-safety is observed PASS,
 *   - hasHardPassOrFail === true,
 *   - prescore.coverage >= 0.4 > 0,
 *   - shouldApplyThreeLine(...) === true at the internal call site.
 * ============================================================ */
{
  const rng = mulberry32(0xb11a01);
  let seen = 0;
  let minCoverage = Infinity;
  for (let trial = 0; trial < 6000; trial += 1) {
    const observation = genObservation(rng, trial);
    const kind = observation.verification?.kind;
    if (kind !== "PASSED" && kind !== "FAILED") continue;
    seen += 1;
    const prescoreInput = prescoreInputFromObservation(observation);
    const prescore = computePrescore(prescoreInput);
    const evidence = prescore.dimensions[0];
    check(
      "S11-A-1 Lemma D (evidence dim always observed, sign = kind)",
      evidence !== undefined &&
        evidence.id === "evidence-consistency" &&
        evidence.hardRelated &&
        (kind === "PASSED" ? evidence.outcome === "PASS" : evidence.outcome === "FAIL"),
      `trial ${trial}: ${JSON.stringify(evidence)}`
    );
    const scope = prescore.dimensions[1];
    check(
      "S11-A-1 Lemma D (scope dim always observed PASS)",
      scope !== undefined && scope.id === "scope-safety" && scope.outcome === "PASS",
      `trial ${trial}: ${JSON.stringify(scope)}`
    );
    const hasHardPassOrFail = prescore.dimensions.some(
      (dimension) => dimension.hardRelated && (dimension.outcome === "PASS" || dimension.outcome === "FAIL")
    );
    check("S11-A-1 Lemma D (hasHardPassOrFail === true)", hasHardPassOrFail, `trial ${trial}`);
    if (prescore.coverage < minCoverage) minCoverage = prescore.coverage;
    check("S11-A-1 Lemma D (coverage >= 0.4 > 0)", prescore.coverage >= 0.4, `trial ${trial}: ${prescore.coverage}`);
    check(
      "S11-A-1 Lemma D (shouldApplyThreeLine === true)",
      shouldApplyThreeLine({ verificationKind: kind, coverage: prescore.coverage, hasHardPassOrFail }),
      `trial ${trial}`
    );
  }
  console.log(
    `S11-A-1 Lemma D: 6000 fuzz trials, ${seen} past entry guard, apply-guard constant-true in all; min coverage=${minCoverage} (floor 0.4, > 0)`
  );
}

/* ============================================================
 * S11-A-1 candidate machinery: verbatim replica of
 * assessChildObservation MINUS the hasHardPassOrFail some-scan and the
 * shouldApplyThreeLine call (dead by Lemma D). Everything else --
 * duplicate computePrescore (S1-A-2 stays closed), FAIL-no-refs guard,
 * window, the real runTrackingTurn, assessment build -- stays verbatim
 * so the measured delta isolates ONLY the S11-A-1 fold.
 * ============================================================ */
function evidenceRefsOfReplica(observation: ChildObservation): string[] {
  const refs = new Set<string>();
  for (const id of observation.evidenceIds) refs.add(id);
  for (const id of observation.verification?.evidenceIds ?? []) refs.add(id);
  return [...refs];
}

function candidateAssess(input: {
  readonly observation: ChildObservation;
  readonly episodeId: string;
  readonly runId: string;
}): { readonly apply: false } | { readonly apply: true; readonly assessment: TrackingAssessment } {
  const verification = input.observation.verification;
  if (verification === undefined || (verification.kind !== "PASSED" && verification.kind !== "FAILED")) {
    return { apply: false };
  }
  const prescoreInput = prescoreInputFromObservation(input.observation);
  const prescore = computePrescore(prescoreInput);
  // S11-A-1 FOLD: hasHardPassOrFail scan + shouldApplyThreeLine call removed
  // (constant-true by Lemma D for every input reaching this point).
  const failRefs = evidenceRefsOfReplica(input.observation);
  if (prescore.dimensions.some((dimension) => dimension.outcome === "FAIL") && failRefs.length === 0) {
    return { apply: false };
  }
  const window = {
    contextFacts: [`role ${input.observation.role}`, `task ${input.observation.taskId}`],
    toolSituations: prescoreInput.toolSituations,
    constraints: input.observation.constraints,
    unresolvedDecisions: [],
    confirmedDecisions: [],
    openMinors: []
  };
  const turn = runTrackingTurn({
    window,
    prescoreInput,
    humanInput: {},
    gateFacts: { deterministicFail: verification.kind === "FAILED" }
  });
  return {
    apply: true,
    assessment: {
      schemaVersion: 1,
      episodeId: input.episodeId,
      runId: input.runId,
      turnId: input.observation.taskId,
      prescore: prescore.displayPrescore,
      quality: prescore.quality,
      coverage: prescore.coverage,
      human: turn.human,
      score: turn.score,
      dimensions: prescore.dimensions.map((dimension) => {
        const verdict = dimension.outcome === "ABSTAIN" ? "UNOBSERVED" : dimension.outcome;
        if (verdict === "FAIL") {
          return { id: dimension.id, verdict, evidenceRefs: failRefs };
        }
        return { id: dimension.id, verdict };
      }),
      gate: turn.gate,
      evidenceRefs: failRefs
    }
  };
}

function candidateApplyChildThreeLine(input: Parameters<typeof applyChildThreeLine>[0]): {
  readonly events: readonly Event[];
  readonly result: GateApplyResult;
} {
  const skipped: GateApplyResult = { applied: false, directive: "none", runStatus: "RUNNING" };
  const terminal = input.child.terminalResult;
  if (terminal === undefined) {
    return { events: input.events, result: skipped };
  }
  const episodeId = episodeIdFromEvents(input.events);
  if (episodeId === undefined) {
    return { events: input.events, result: skipped };
  }
  const runId = input.events[0]?.runId;
  if (runId === undefined) {
    return { events: input.events, result: skipped };
  }
  const observation = observationFromChild(input.child, input.spec, input.contract);
  const assessed = candidateAssess({ observation, episodeId, runId });
  if (!assessed.apply) {
    return { events: input.events, result: skipped };
  }
  return applyTrackingGate({
    events: input.events,
    assessment: assessed.assessment,
    assessmentHash: hashAssessment(assessed.assessment),
    expectedSeq: nextTrackingSeq(input.events),
    policyVersion: "track-v1",
    nowIso: input.nowIso,
    generateEventId: input.generateEventId
  });
}

/* ============================================================
 * S11-A-2 candidate machinery: verbatim replica of applyTrackingGate
 * except the fresh none-directive path skips currentGateStatus and
 * returns a NARROWED result without runStatus (the field's only
 * consumer plane). Non-none paths and both idempotent-hit paths stay
 * verbatim (from is event-payload-observable there / production-dead
 * by S8-A-1 fresh-seq fact respectively).
 * ============================================================ */
interface NarrowedResult {
  readonly applied: boolean;
  readonly directive: GateDirective;
  readonly transitionId?: string;
  readonly runStatus?: GateRunStatus;
}

function currentGateStatusReplica(events: readonly Event[]): GateRunStatus {
  let status: GateRunStatus = "RUNNING";
  for (const event of events) {
    if (event.type === "GATE_TRANSITION") status = event.payload.to;
    else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
    else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
    else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
  }
  return status;
}

function mapGateDirectiveReplica(assessment: TrackingAssessment): {
  directive: GateDirective;
  runStatus: GateRunStatus;
  reasonCode: string;
} {
  const gate = assessment.gate;
  if (gate.askUser) {
    return { directive: "wait_user", runStatus: "WAITING_FOR_USER", reasonCode: gate.codes[0] ?? "ASK_USER" };
  }
  if (gate.kind === "none") {
    return { directive: "none", runStatus: "RUNNING", reasonCode: "NONE" };
  }
  if (gate.kind === "hard" && gate.codes.includes("user-reject-stop")) {
    return { directive: "wait_user", runStatus: "WAITING_FOR_USER", reasonCode: "user-reject-stop" };
  }
  if (gate.kind === "soft" || gate.kind === "hard" || gate.wakeAnalysis) {
    return {
      directive: "queue_analysis",
      runStatus: "BLOCKED",
      reasonCode: gate.codes[0] ?? "ANALYSIS_QUEUED"
    };
  }
  return { directive: "wait_user", runStatus: "WAITING_FOR_USER", reasonCode: "FAIL_CLOSED" };
}

function narrowedApplyTrackingGate(input: {
  readonly events: readonly Event[];
  readonly assessment: TrackingAssessment;
  readonly assessmentHash: string;
  readonly expectedSeq: number;
  readonly policyVersion: string;
  readonly nowIso: string;
  readonly generateEventId: () => EventId;
}): { readonly events: readonly Event[]; readonly result: NarrowedResult } {
  if (input.assessmentHash !== hashAssessment(input.assessment)) {
    throw new DomainValidationError("assessmentHash mismatch: does not match hashAssessment(assessment)");
  }
  const idempotencyKey = `${input.assessmentHash}:${input.expectedSeq}`;
  const existing = input.events.find(
    (event): event is Extract<Event, { type: "GATE_TRANSITION" }> =>
      event.type === "GATE_TRANSITION" && event.payload.idempotencyKey === idempotencyKey
  );
  if (existing !== undefined) {
    return {
      events: input.events,
      result: {
        applied: false,
        directive: existing.payload.directive,
        transitionId: existing.payload.transitionId,
        runStatus: existing.payload.to
      }
    };
  }
  const existingAssessment = input.events.find(
    (event): event is Extract<Event, { type: "TRACKING_ASSESSMENT" }> =>
      event.type === "TRACKING_ASSESSMENT" &&
      event.payload.seq === input.expectedSeq &&
      event.payload.assessmentHash === input.assessmentHash
  );
  if (existingAssessment !== undefined) {
    return {
      events: input.events,
      result: {
        applied: false,
        directive: mapGateDirectiveReplica(input.assessment).directive,
        runStatus: currentGateStatusReplica(input.events)
      }
    };
  }

  const mapped = mapGateDirectiveReplica(input.assessment);
  const runId = input.assessment.runId as RunId;
  const occurredAt = input.nowIso as IsoTimestamp;
  const next = [...input.events];

  next.push(
    validateEvent({
      id: input.generateEventId(),
      schemaVersion: 1,
      occurredAt,
      runId,
      type: "TRACKING_ASSESSMENT",
      actor: "supervisor",
      payload: {
        assessment: input.assessment,
        assessmentHash: input.assessmentHash,
        seq: input.expectedSeq
      }
    })
  );

  if (mapped.directive === "none") {
    // S11-A-2 FOLD: skip the currentGateStatus O(E) fold; runStatus has
    // zero production readers on this path (dead output field).
    return {
      events: next,
      result: { applied: true, directive: "none" }
    };
  }

  const from = currentGateStatusReplica(input.events);
  const transitionId = input.generateEventId();
  next.push(
    validateEvent({
      id: transitionId,
      schemaVersion: 1,
      occurredAt,
      runId,
      type: "GATE_TRANSITION",
      actor: "supervisor",
      payload: {
        transitionId,
        episodeId: input.assessment.episodeId,
        turnId: input.assessment.turnId,
        seq: input.expectedSeq,
        from,
        to: mapped.runStatus,
        reasonCode: mapped.reasonCode,
        assessmentHash: input.assessmentHash,
        evidenceRefs: input.assessment.evidenceRefs,
        policyVersion: input.policyVersion,
        idempotencyKey,
        directive: mapped.directive
      }
    })
  );

  if (mapped.directive === "queue_analysis") {
    next.push(
      validateEvent({
        id: input.generateEventId(),
        schemaVersion: 1,
        occurredAt,
        runId,
        type: "RUN_BLOCKED",
        actor: "supervisor",
        payload: {
          reason: "ANALYSIS_QUEUED",
          requiredEvidence: [...input.assessment.evidenceRefs]
        }
      })
    );
  }

  if (mapped.directive === "wait_user") {
    const waitingId = input.generateEventId();
    next.push(
      validateEvent({
        id: waitingId,
        schemaVersion: 1,
        occurredAt,
        runId,
        type: "RUN_WAITING_FOR_USER",
        actor: "supervisor",
        payload: {
          messageId: createMessageId(() => waitingId.slice("evt_".length))
        }
      })
    );
  }

  return {
    events: next,
    result: {
      applied: true,
      directive: mapped.directive,
      transitionId,
      runStatus: mapped.runStatus
    }
  };
}

/* ============================================================
 * Joint end-to-end equivalence fuzz through applyChildThreeLine:
 * S11-A-1 candidate must be bit-identical on {events, result} + skip
 * reference identity. S11-A-2 candidate must be bit-identical on
 * events + everything production consumes (directive; full result on
 * non-none paths), while the none-path result drops ONLY runStatus
 * (the documented public-face cost, asserted exactly).
 * ============================================================ */
{
  const rng = mulberry32(0xb11a02);
  for (let trial = 0; trial < 4000; trial += 1) {
    const vroll = rng();
    const verification =
      vroll < 0.85
        ? {
            kind: vroll < 0.45 ? ("PASSED" as const) : vroll < 0.75 ? ("FAILED" as const) : ("UNOBSERVED" as const),
            evidenceIds: rng() < 0.8 ? [`evd_v_${trial}`, "evd_shared"] : []
          }
        : undefined;
    const child = {
      taskId: `tsk_${trial % 7}`,
      outcome: pick(rng, ["SUCCESS", "PARTIAL", "FAILURE", "CANCELLED", "TIMEOUT"] as const),
      summary: pick(rng, ["tests passed", "child completed the task", "", "wip", "回滚全部 rollback", "verified output"]),
      evidenceIds: rng() < 0.75 ? [`evd_${trial}`, "evd_shared"] : [],
      artifactIds: rng() < 0.5 ? [`art_${trial}`] : [],
      ...(rng() < 0.9 ? { terminalResult: { ...(verification !== undefined ? { verification } : {}) } } : {})
    } as unknown as ChildShape;
    const spec =
      rng() < 0.8
        ? ({
            role: pick(rng, ["tester", "implementer", "scout"]),
            acceptanceCriteria: Array.from({ length: Math.floor(rng() * 4) }, (_, i) => ({
              id: `chk_${i}`,
              description: `criterion ${i}`
            }))
          } as unknown as SpecShape)
        : undefined;
    const contract =
      rng() < 0.5
        ? ({
            constraints: Array.from({ length: Math.floor(rng() * 5) }, (_, i) => ({
              id: `con_${i}`,
              description: `constraint ${i}`
            }))
          } as unknown as RequirementContract)
        : undefined;
    const shape = rng();
    const events =
      shape < 0.8
        ? buildEventTable({ attached: true, filler: Math.floor(rng() * 45) })
        : shape < 0.9
          ? buildEventTable({ attached: false, filler: Math.floor(rng() * 10) })
          : [];
    let c1 = 100;
    let c2 = 100;
    const base = {
      events,
      child,
      nowIso: NOW_ISO,
      ...(spec !== undefined ? { spec } : {}),
      ...(contract !== undefined ? { contract } : {})
    };
    const expected = applyChildThreeLine({
      ...base,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    const a1 = candidateApplyChildThreeLine({
      ...base,
      generateEventId: () => `evt_${String(c2++).padStart(8, "0")}` as EventId
    });
    check("S11-A-1 e2e equivalence (result)", JSON.stringify(expected.result) === JSON.stringify(a1.result), `trial ${trial}`);
    check("S11-A-1 e2e equivalence (events)", JSON.stringify(expected.events) === JSON.stringify(a1.events), `trial ${trial}`);
    check(
      "S11-A-1 skip-path reference identity matches",
      (expected.events === events) === (a1.events === events),
      `trial ${trial}`
    );

    // S11-A-2: only exercised on real apply paths (fresh gate application)
    if (expected.result.applied) {
      const observation = observationFromChild(child, spec, contract);
      const episodeId = episodeIdFromEvents(events);
      const runId = events[0]?.runId;
      const assessed = assessChildObservation({
        observation,
        episodeId: episodeId as string,
        runId: runId as string
      });
      check("S11-A-2 apply agreement", assessed.apply === true, `trial ${trial}`);
      if (assessed.apply) {
        let c3 = 100;
        const a2 = narrowedApplyTrackingGate({
          events,
          assessment: assessed.assessment,
          assessmentHash: hashAssessment(assessed.assessment),
          expectedSeq: nextTrackingSeq(events),
          policyVersion: "track-v1",
          nowIso: NOW_ISO,
          generateEventId: () => `evt_${String(c3++).padStart(8, "0")}` as EventId
        });
        check("S11-A-2 events bit-equal", JSON.stringify(expected.events) === JSON.stringify(a2.events), `trial ${trial}`);
        check(
          "S11-A-2 consumed projection equal (directive/applied/transitionId)",
          expected.result.directive === a2.result.directive &&
            expected.result.applied === a2.result.applied &&
            expected.result.transitionId === a2.result.transitionId,
          `trial ${trial}`
        );
        if (expected.result.directive === "none") {
          check(
            "S11-A-2 none path drops EXACTLY runStatus (documented public-face cost)",
            a2.result.runStatus === undefined && expected.result.runStatus !== undefined,
            `trial ${trial}`
          );
        } else {
          check(
            "S11-A-2 non-none paths full result bit-equal",
            JSON.stringify(expected.result) === JSON.stringify(a2.result),
            `trial ${trial}`
          );
        }
      }
    }
  }
  console.log(
    "S11-A-1 e2e: 4000 fuzz trials bit-equal (events, result, skip identity) | S11-A-2: events + consumed projection bit-equal; none path drops only runStatus"
  );
}

/* ============================================================
 * Benches: (a) S11-A-1 folded work in isolation; (b) whole
 * assessChildObservation current vs candidate; (c) e2e deltas at E=41;
 * (d) S11-A-2 dead currentGateStatus scan cost + e2e delta on the
 * PASSED/none production path.
 * ============================================================ */
function mkChild(kind: "PASSED" | "FAILED"): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  return {
    child: {
      taskId: "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary: "tests ran",
      evidenceIds: ["evd_1"],
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: ["evd_1"] } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "tests pass" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "keep scope" },
        { id: "con_1", description: "no new deps" },
        { id: "con_2", description: "tests stay green" }
      ]
    } as unknown as RequirementContract
  };
}

{
  // (a) S11-A-1 folded work in isolation
  const { child, spec, contract } = mkChild("PASSED");
  const observation = observationFromChild(child, spec, contract);
  const prescoreInput = prescoreInputFromObservation(observation);
  const prescore = computePrescore(prescoreInput);
  const folded = bench(() => {
    const hasHardPassOrFail = prescore.dimensions.some(
      (dimension) => dimension.hardRelated && (dimension.outcome === "PASS" || dimension.outcome === "FAIL")
    );
    void shouldApplyThreeLine({
      verificationKind: "PASSED",
      coverage: prescore.coverage,
      hasHardPassOrFail
    });
  }, 500000);
  console.log(`S11-A-1 bench folded work: some-scan + shouldApplyThreeLine = ${(folded * 1e6).toFixed(1)}ns/child result (x~5/run)`);

  // (b) whole assessChildObservation current vs candidate
  for (const kind of ["PASSED", "FAILED"] as const) {
    const mk = mkChild(kind);
    const obs = observationFromChild(mk.child, mk.spec, mk.contract);
    const cur = bench(() => void assessChildObservation({ observation: obs, episodeId: "ep_x", runId: "run_x" }), 50000);
    const cand = bench(() => void candidateAssess({ observation: obs, episodeId: "ep_x", runId: "run_x" }), 50000);
    console.log(
      `S11-A-1 bench whole assess (${kind}): current=${(cur * 1e6).toFixed(0)}ns cand=${(cand * 1e6).toFixed(0)}ns delta=${((cur - cand) * 1e6).toFixed(0)}ns/child result`
    );
  }

  // (c) S11-A-1 e2e deltas at E=41
  for (const kind of ["PASSED", "FAILED"] as const) {
    const mk = mkChild(kind);
    const events = buildEventTable({ attached: true, filler: 39 });
    const cur = bench(() => {
      let c = 100;
      void applyChildThreeLine({
        events, child: mk.child, spec: mk.spec, contract: mk.contract, nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    }, 10000);
    const cand = bench(() => {
      let c = 100;
      void candidateApplyChildThreeLine({
        events, child: mk.child, spec: mk.spec, contract: mk.contract, nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    }, 10000);
    console.log(
      `S11-A-1 bench e2e (${kind}, E=41, contract 3): current=${(cur * 1e3).toFixed(2)}us cand=${(cand * 1e3).toFixed(2)}us delta=${((cur - cand) * 1e6).toFixed(0)}ns/gate (x~5 gates/run)`
    );
  }

  // (d) S11-A-2: dead currentGateStatus scan + e2e delta on PASSED/none path
  const events41 = buildEventTable({ attached: true, filler: 39 });
  const scan = bench(() => void currentGateStatusReplica(events41), 200000);
  console.log(`S11-A-2 bench dead scan: currentGateStatus over E=41 = ${(scan * 1e6).toFixed(1)}ns/gate (none path only, x~5 gates/run)`);
  {
    const mk = mkChild("PASSED");
    const obs = observationFromChild(mk.child, mk.spec, mk.contract);
    const assessed = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: String(RUN_ID) });
    if (assessed.apply) {
      const hash = hashAssessment(assessed.assessment);
      const seq = nextTrackingSeq(events41);
      const cur = bench(() => {
        let c = 100;
        void applyTrackingGate({
          events: events41, assessment: assessed.assessment, assessmentHash: hash, expectedSeq: seq,
          policyVersion: "track-v1", nowIso: NOW_ISO,
          generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
        });
      }, 10000);
      const cand = bench(() => {
        let c = 100;
        void narrowedApplyTrackingGate({
          events: events41, assessment: assessed.assessment, assessmentHash: hash, expectedSeq: seq,
          policyVersion: "track-v1", nowIso: NOW_ISO,
          generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
        });
      }, 10000);
      console.log(
        `S11-A-2 bench e2e applyTrackingGate (PASSED/none, E=41): current=${(cur * 1e3).toFixed(2)}us cand=${(cand * 1e3).toFixed(2)}us delta=${((cur - cand) * 1e6).toFixed(0)}ns/gate`
      );
    }
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R10-A lineage): applyChildThreeLine
 * end-to-end at real scale (41-event table) across gate-outcome x
 * contract cells (2x2 full matrix, closed by R10-A).
 * ============================================================ */
{
  const cells: Array<{ label: string; kind: "PASSED" | "FAILED"; withContract: boolean }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true }
  ];
  const events = buildEventTable({ attached: true, filler: 39 });
  for (const cell of cells) {
    const mk = mkChild(cell.kind);
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

void mulberry32(0xb11a03);
void mulberry32(0xb11a04);

if (failures > 0) {
  console.error(`\n${failures} equivalence check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL EQUIVALENCE CHECKS PASSED (deterministic, seeded)");
```
