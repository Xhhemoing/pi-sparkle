MODEL_SLUG=claude-fable-5-thinking-xhigh

# R11-E：`src/learning/` 第十一遍复查报告（Round 11）

**战役:** 全库持久 SOTA 优化 Round 11 / R11-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `b3f0cb8`（含 S11-A-1..2、S11-B-1 排除已入表）
**分支:** `cursor/r11-e-learning-eleventh-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第十一组新角度枚举为空（有理由的
空枚举，未铸造任何 S11-E-\* ID，与 R10-E 同口径），切片维持关闭
（MORE_OPTIMA=no）。**

1. **零漂移三重确认**：切片 10 个文件（`wc -l` 确认恰 10 文件、合计 1770 行，
   无多无少）自 R1-E 裁决基线（`adb20d7`）至本轮基线（`b3f0cb8`）**逐字节
   未变**——`git diff adb20d7..HEAD -- src/learning/` 为 0 行、
   `git log adb20d7..HEAD -- src/learning/` 为 **0 提交**，十一轮全程无人触及
   该目录。且 R10-E 基线（`8d598f5`）之后的全部提交（R10-E/F/G/H/I/J 报告
   摄取、Round 10 收口、R11-A/B 报告与排除表摄取、R11-C/D/E 派单）**仅触及
   `docs/reports/sota-opt/`**——`git diff --stat 8d598f5..HEAD -- src/ test/`
   为空，src 与 test 一行未动。R1-E..R10-E 的全部规模测量、调用面图景与
   S1-E-1..8 / S2-E-1..7 / S3-E-1..5 / S4-E-1..3 / S5-E-1..5 / S6-E-1..5 /
   S7-E-1..5 / S8-E-1..3 / S9-E-1..3 共 44 项切片排除裁决（R10-E 未铸 ID，
   其空枚举收口一并继承）原样成立、原样继承。
2. **本 VM 锚点/矩阵重锚（未拷贝 R10-E 数字，遵派单令）**：SLICE-CPU 总量
   上界锚点八次运行 **17.2–17.6µs/run**（collect 13.0–13.3µs、outcomes
   2.4µs、diagnose 1.04–1.08µs、bandit-build 0.7–0.8µs）——距落地线
   （≥10ms）**570–583×**，即使把切片 CPU 清零也远不达门槛。本 VM 慢于
   R10-E 的 VM（collect 13.0–13.3 vs 7.4–7.5µs）而与 R8-E/R9-E 的 VM 同带
   （切片逐字节未变，纯环境差异）；按历史最保守口径（R7-E outcomes 带
   7.0–7.3µs 替换本轮 outcomes 项）总量 ~22.1–22.5µs，仍 **≥444×** 低于
   门槛，两种口径结论一致。配置态 live 面：`applyLearnedRouting` 非空策略
   **254–281ns/task**（R9-E 258–280 / R10-E 264–289 同带）、空策略
   **113–125ns/task**（R9-E 120–126 / R10-E 112–120 同带）；
   `loadLearnedRouting` 全新 root 25.6–34.1µs、现实 registry
   **76.0–84.5µs**（R8-E 80–85 / R10-E 75.5–83.4 同级，亚 ms 一次性）。
   全部确定性 VERDICT 行八次运行**逐位一致**，3/3 sanity 检查全过
   （§1、附录 A）。
3. **配置态 × 命令类矩阵复核无洞**：全部切片进入点行号逐条 grep 复核，
   **与 R8-E/R9-E/R10-E 记录逐一相同、无新增调用方、无新增命令类**
   （`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
   `run/flowchart-run:681/712`、`track/loop:88/172`；resume/replay 经
   `flowchartForSupervisor` @ `flowchart-run:880/1021` 各单次载入）。
   S8-E-1 双载入（`cli/main:708`+`flowchart-run:712` 与
   `track/loop:88`+`flowchart-run:712`）按令保护，未做任何去重分析。
   切片外唯一其余消费者仍为 `adaptation/eval-routing.ts:223` 的
   `parseLearnedRoutingPolicy`（离线 eval 面，D 切片辖区）。
4. **第十一组新角度枚举为空（§2）**：按「结构位置 × 消除机制」对第十一遍
   全量重读的 10 个源文件做双向扫描，本轮定位的全部形态（含四个此前从未
   逐字点名的结构位置：`parseLearnedRoutingPolicy` 载入期校验微观、
   `applyLearnedRouting` 空 avoid 函数内快路径、`runAutoAdaptLoop`
   some-探针 × `updateProjectBandit` arms 重扫的跨函数去重、
   `scoreTaskResult` 尾部不可达 `return 70`）要么是既有排除 ID/已关闭机制
   类的换名或换位点重现，要么被落地线算术直接封死（整个 76–84.5µs 载入面
   都被派单明文列为 reject，其内部微观更不具候选资格），要么是零运行时
   收益的纯重构。**未铸造任何 S11-E-\* 新 ID，无新增排除提议。**
5. **验证面**：切片相关套件全绿（**48 tests / 2 suites / 48 pass / 0 fail**，
   Node v22.14.0，与 R8-E..R10-E 同口径）。零代码改动下全部硬不变量天然
   满足。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与
双归因未动；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故
未提交 `scripts/round11-r11e-equivalence-sim.ts`（锚点脚本全文进附录 A，
遵守 R7-F/R8-C/R9-I/R10-E 无赢家纪律）。本切片在其输出契约与数据面语义下
维持 SOTA——第十一遍复查确认：**剩余的全部 ms 级余量仍全部在被排除表点名
保护的 I/O 契约面上**，切片级收口条件（R3-E §7 … R10-E §6）依然成立。
**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行）本轮第十一遍全量实际读码，未依赖前十轮
  记忆。上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、
  `feedback/`、`persist/`、`privacy/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表，含 S11-A-1..2、
  S11-B-1）→ PROGRESS.md → round-11/PLAN.md → round-10/PLAN.md +
  round-10/R10-E.md（空枚举，未铸 S10-E-\*）→ R9-E / R8-E → 10 个源文件。
- 基线漂移检查（§结论 1）：切片零提交零 diff；`8d598f5..b3f0cb8` 仅
  `docs/reports/sota-opt/` 文档——前十轮全部测量与裁决原样成立。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S10-\*、S11-A-1..2、
  S11-B-1 条目。特别地：
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护，本轮仅复核其行号与频次
    （§1），未做任何去重分析。并发 promote 下的新鲜性仍是其保留理由。
  - **S9-E-2 pass-2 预过滤是负优化（E=400 五次全负）**——本轮未再尝试
    任何两遍结构的工作集缩减变体；两遍语义按硬不变量维持。
  - **S9-E-1 / S9-E-3 / X2-6 / X0-3** 及派单点名的全部「勿重述」条目
    未以任何新 ID 重述（§2 逐条换名检查）。
  - R10-E 未铸 S10-E-\*，本轮亦未事后发明任何 S10-E-\* 编号。
- **换名重提检查**（识别为既有方案换名/换位点，**未列为新候选**；R10-E §0
  的七条换名裁决全部原样维持，此处不再复抄）：本轮新定位形态的换名检查
  全部并入 §2 表格逐行给出。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  稀疏 → 已批准 R0 基线；双 LCB（beta+normal）/ 双归因（logit+prob-add）
  在位；晋升 proposal-first；无阈值变更；CAS / 凭据 / 数据面契约 / 公开
  签名不动；fail-open / 隐藏模块缓存（X1-1）未引入；bandit/learned
  registry I/O 的 fsync 与完整性重哈希未触碰；corrupt registry
  fail-closed（S2-I-1 / S9-G-1 族）未触碰；两遍
  `collectSignalsFromEvents` 语义未触碰；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未 force-push。
- 环境：Node v22.14.0（`nvm`/系统默认；与 R8-E..R10-E 测量环境同版本）；
  依赖经 `pnpm install --frozen-lockfile` 全新安装。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

R7-I 教训（默认态夹具可能掩盖配置态主路径）按令复查。src 自 `8d598f5` 起
一行未动，故 R8-E/R9-E/R10-E §1 矩阵表结构原样成立；本轮逐条 grep 复核
全部切片进入点行号，**与 R10-E 记录逐一相同、无新增调用方**：

| 命令类 × 配置态 | 切片进入点（本轮行号复核） | 频次与规模 |
| --- | --- | --- |
| `adapt auto --run` | `runAutoAdaptFromEvents` @ `cli/adapt:188` → `runAutoAdaptLoop` | 每进程一次，E≈41 |
| `adapt auto --project` | `runAutoAdaptLoop` @ `cli/adapt:205`（空事件表，S4-E-1 面） | 每进程一次 |
| `adapt learn --run` | `proposeRoutingFromRoutedEvents` @ `cli/adapt:168`（S2-E-2 面） | 每进程一次 |
| 普通 `run` | `loadLearnedRouting` @ `cli/main:708`（S2-I-1 已裁决） | 每进程一次 |
| `run --children` | `cli/main:708` 载入 → `applyLearnedRouting` @ `routing/assign:102` 每任务 → `startFlowchartRun`（@ `cli/main:748`）内 `flowchartForSupervisor` @ `flowchart-run:747→712` 第二次载入（S8-E-1，勿去重）→ `applyLearnedRouting` @ `flowchart-run:681` 每节点 → `runAutoAdaptLoop` @ `cli/main:783` 一次 | 双载入各一次；任务/节点 ≤10/几十 |
| `track`（startTrackedRun） | `track/loop:88` 载入 → 每任务 `applyLearnedRouting` → `startFlowchartRun` @ `track/loop:152` → `flowchart-run:712` 第二次载入（同 S8-E-1）→ `runAutoAdaptLoop` @ `track/loop:172` 一次 | 同上 |
| flowchart resume / replay | `flowchartForSupervisor` @ `flowchart-run:880` / `:1021` 各单次载入（无双载入，R8-E..R10-E 复核维持） | 每恢复一次 |
| SPARKLE_AUTO_ADAPT=0 | 仍收集（persistSignals + updateProjectBandit 照付），仅跳过 propose；R5-E 裁决维持 | 不变 |
| 测试专用面 | `patterns` / `attribution` / `signatures` / `compareSignatures` / `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `parseObservedSignal`(直调) / `proposeRoutingFromAssignments`(deprecated) / `saveLearnedRouting`(deprecated, throw-only) 交叉检索复核仍零生产调用方 | 不进热路径 |

切片外其余消费者复核：`adaptation/eval-routing.ts:20,223` 仅导入并调用
`parseLearnedRoutingPolicy`（离线 eval 面，D 切片）；`track/primary-split.ts:3`
仅导入类型；`privacy/record-classes.ts:212,224` 仅文档字符串提及路径布局。
**矩阵结论：无新洞**。配置态给本切片的全部新增工作仍是每任务/每节点一次
`applyLearnedRouting` + 每 run 至多两次 `loadLearnedRouting`（S8-E-1 已
点名保护），均在本轮实测锚点覆盖内。

SLICE-CPU 锚点本 VM 重测（八次运行区间；Node v22.14.0；seeds
`0xe11e01`–`0xe11e05`；夹具镜像 R8-E..R10-E 生成器，R1-A 组成 E=41）：

```text
collect=13.0-13.3us  outcomes=2.4us  diagnose=1.04-1.08us  bandit-build=0.7-0.8us
total in-slice CPU ~17.2-17.6us per full auto-adapt run
vs landing bar >=10000us  ->  570-583x below EVEN IF ZEROED
configured-state: applyLearnedRouting(avoid=10, M=10) = 254-281ns/task （R9-E 258-280 / R10-E 264-289 同带）
                  applyLearnedRouting(空策略)        = 113-125ns/task （R9-E 120-126 / R10-E 112-120 同带）
                  loadLearnedRouting fresh(ENOENT)   = 25.6-34.1us/次
                  loadLearnedRouting 现实 registry    = 76.0-84.5us/次 （亚 ms 一次性，S8-E-1 带）
```

注：本 VM 慢于 R10-E 的 VM（collect 13.0–13.3µs vs 7.4–7.5µs）而与
R8-E（14.6–14.9µs）/ R9-E（13.6–13.8µs）的 VM 同带；切片逐字节未变，为
环境差异非代码差异。本轮夹具（seed 组 `0xe11e01`–`0xe11e03`）与 R10-E
夹具组成不同（collected=15 / outcomes=2 / diagnoseGroups=1 vs R10-E 的
7/1/2，夹具差异非代码差异）。按历史最保守口径（R7-E outcomes 带 7.0–7.3µs
替换本轮 2.4µs）总量 ~22.1–22.5µs，仍 **≥444×** 低于门槛。两种口径下
结论一致：**锚点复核成立，该切片不存在不推翻既有排除就能达门槛的候选。**
全部确定性 VERDICT 行（夹具规模、live 面 sanity、registry 夹具产出激活
策略）八次运行逐位一致，3/3 sanity 检查全过。

## 2. 第十一组新角度枚举：有理由的空枚举

方法：对第十一遍全量重读的 10 个源文件按「结构位置 × 消除机制」双向扫描
（死代码/不可达代码 / 冗余求值 / 快路径 / 分派整形 / 数据结构替换 /
分配避免 / 跨函数去重 / 载入期解析微观 / I/O 契约面），每个形态先对
EXCLUSIONS 全表 + 十轮 E 报告做同机制先例检索（换名检查），只有「排除表
未覆盖且理论收益可达数十 ms 或复杂度类下降」者才立项。本轮扫描结果：
**零个可立项形态**，逐类裁决如下（均未铸造新 ID——按 R9-E/R10-E 方法论，
排除行换名/换位点重现与已关闭类的机制变体不重复立项；落地线算术直接封死
的形态无候选资格）：

| 形态（本轮第十一遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **`parseLearnedRoutingPolicy` 载入期校验微观**（`learned-routing:52-109`：avoid/prefer 逐条 isRecord+typeof 校验、assignments flatMap）——此前十轮从未逐字点名 | 每 `loadLearnedRouting` 一次，O(A+P) 条目级 ns 微观 | 落地线算术封死，不立项：该函数只在载入面执行，而派单明文把**整个** 76.0–84.5µs 的 once-per-run 载入列为 reject——其内部 ns 级微观是 reject 的真子集，无候选资格。校验语义本身是 fail-closed 契约（错误消息带下标，X0-5 邻域） | 派单落地线条款 + X0-5 邻域 |
| `loadLearnedRouting` 错误路径 `/no registry snapshot/.test(error.message)` regex 探针（`learned-routing:123`） | 仅 ENOENT/损坏错误路径一次 | 换位点重现拒列：一次性错误路径微整理（R2-E 错误路径裁决、R9-E `readBanditFile` ENOENT catch 同式）；且改动毗邻 corrupt-registry fail-closed 契约（S2-I-1 / S9-G-1 族） | R2-E/R9-E 无 ID 收口 |
| **`applyLearnedRouting` 空 avoid 函数内快路径**（`learned-routing:198-204`：avoid=[] 时仍建空 Set + 全量 filter 拷贝）——S6-B-4 是调用方跳过（语义发散被否决），函数内忠实形态从未单列 | 空策略面实测 113–125ns/task 全额即天花板；忠实免 Set 形态省 <100ns/task，×10 任务 <1µs/run | 已关闭类机制变体拒列：快路径家族（S4-E-1 空事件快路径、S7-E-2 truncate 恒等快路径均 ns~µs 级淘汰）；别名形态（直接返回 catalogIds）改变返回数组身份 = S1-A-7 / S7-B-5 可观察身份族，且 `applyLearnedToNode` 会把该数组写进新 node 的 modelPolicy；空策略面锚点本轮已实测，全额清零也不达线 | S4-E-1 / S7-E-2 快路径族 + S1-A-7 身份族 + S6-B-4 |
| **`runAutoAdaptLoop` 的 `signals.some(modelId!==undefined)` 探针（`auto-loop:88`）与 `updateProjectBandit` 内 arms 重扫（`bandit-store:47-51`）跨函数去重**（把探针结果/预建 arms 传入避免二次扫描） | 每 run 一次 O(S) 早退探针 + 一次 O(S) arms 扫描，S≈12 ⇒ ns 级 | 已关闭类机制变体拒列：跨函数冗余求值角度已在三个层级闭合（S1-E-2 表达式内、S8-E-3 跨界、S9-E-3 跨分派树）；落地需拓宽 `updateProjectBandit` 公开签名（X0-4 族）或引入模块缓存（X1-1）；且 bandit 面被锁内 I/O 支配（S3-E-4 实测结论） | S8-E-3 / S9-E-3 / X0-4 / X1-1 |
| **`scoreTaskResult` 尾部 `return 70` 不可达代码**（`signals:54`：verification ∈ {PASSED, FAILED, UNOBSERVED} 全被前分支覆盖，合法枚举输入下末行永不执行） | 0ns——不可达代码从不执行，消除属纯重构无任何运行时收益（且该行是非法输入下的防御回退） | 不立项：无收益路径即无候选资格，与 R10-E `outcomeKindFromResult` 死形参、S9-D-3 零成本防御纵深同式；删除反而收窄对 schema 外输入的防御回退 | R10-E §2 行 1 / S9-D-3 同式 |
| `policyFromOutcomes` 的 seen-Set `has`+`add` 双探针换 `add`+size 单探针（`from-episode:74-79`） | 每 `adapt learn --run` 一次，FAIL 行数个位级 ⇒ ns 级 | 已关闭类机制变体拒列：S5-F 单探针机制在每批一次 / N≤10 站点已被 S9-B-1（33–69ns 淘汰）裁决；本站点频次更低（一次性 CLI 命令类，被 readAll I/O 支配） | S5-F 族 / S9-B-1 |
| `collectSignalsFromSubagentRun` failed 析取顺序（`signals:259-263`：status 双比较 → exitCode → regex 尾置） | 每 result 条目 ns 级 | 换名重提拒列：谓词短路重排族（S6-A-2 / S7-E-4 / R7-E 无 ID 收口）；regex 已在最短路位 | S7-E-4 族 |
| R10-E §2 已点名的七条换名形态（JUDGE_DECISION 双分派链、条件 spread 双 Map.get、`familyFromRouted` includes、`isCompleteRoute` 谓词序、Date.parse、空单例身份族、bandit 吸收析取） | ns 级 | 全部维持 R10-E 拒列，本轮读码确认位点未变（切片零 diff 下必然） | S9-E-3 族 / S1-E-2 / S6-E-1 域 / S7-E-4 族 / X1-1 / S1-A-7 族 |
| 两遍 `collectSignalsFromEvents` 的任何结构变换（合遍/预过滤/空快路径/工作集缓存） | pass 1+2 合计 ~13µs/run 全部 | 三向封死 + 硬不变量明令：合遍改变 PROJECT_DISCOVERED 后置发现语义（S1-E-1）；预过滤为理论+实测双重**负**优化（S9-E-2，E=400 五次全负）；空事件快路径生产不可达（S4-E-1）。派单明令两遍语义不动 | S1-E-1 / S9-E-2 / S4-E-1 + 硬不变量 |
| I/O 契约面全体：fsync 丢弃、完整性重哈希跳过、no-change 跳写、双载入去重、corrupt registry fail-open、readdir 并行化、惰性载入、mkdir 提升、紧凑序列化 | 切片相关全部 ms 级余量所在（本轮实测 registry 载入 76.0–84.5µs/次、写侧更贵） | 排除行逐字重现 + 硬不变量明令封死：全部数十 µs~ms 级形态均在排除表点名保护的 I/O 契约面上。特别地 `routingPolicyContent` 的 pretty-print 格式参与 contentHash ⇒ 任何序列化变更破坏 CAS 契约，比 S6-E-4 更硬 | X0-3 / S2-E-1 / S2-E-4 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1 / S1-E-4/5 / S2-I-1 / S9-G-1 族 + CAS |
| 死导出清理（`saveLearnedRouting` throw-only deprecated、`proposeRoutingFromAssignments` deprecated 转发） | 0ns 运行时 | 不立项：公开签名必须保留（硬不变量）；无运行时收益 | 硬不变量（公开签名） |
| 测试专用面任何优化（`patterns` / `attribution` / `signatures` 三文件 + `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `compareSignatures`） | 不进任何生产路径 | 本轮交叉检索复核仍零生产调用方，自动拒列 | S1-E-7/8 / X2-6 族 |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（十一轮）、src/test 自 R10-E 基线零漂移、调用位点行号全数复核一致（§1） | — |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可达数十 ms
落地线的候选。前十轮已逐类收口的九个角度类（单遍化、分配避免、正则整形、
数据结构替换、I/O 重叠、形状单态化、死存储、工作集缩减、分派树合一）之外，
第十一遍读码新探的四个结构位置（载入期解析微观、空 avoid 函数内快路径、
some-探针跨函数去重、不可达尾 return）全部落进已关闭机制类或落地线算术
封死区：切片总 CPU 17.2–17.6µs（本 VM）意味着**即使发明一个能清零全切片
CPU 的新机制也距门槛 570 倍以上**，唯一物理上可达 ms 的位置全部是被硬
不变量与排除表双重保护的 I/O 契约面。

## 3. 逐文件收口（前十轮收口之上的本轮新检查点）

| 文件 | 本轮第十一遍新视角检查 | 结论 |
| --- | --- | --- |
| `signals.ts` | `scoreTaskResult` 尾部不可达 `return 70`（§2 行 5）判零收益纯重构不立项；`collectSignalsFromSubagentRun` failed 析取序（§2 行 7）换名拒列；两遍结构维持硬不变量；S1-E-1/2/3、S2-E-5/7、S3-E-1/5、S4-E-1/3、S5-E-2/3、S6-E-1/2/5、S7-E-1/2/5、S8-E-2、S9-E-2/3 维持 | 无候选 |
| `from-episode.ts` | `policyFromOutcomes` seen-Set 单探针（§2 行 6）判 S5-F/S9-B-1 族拒列；`outcomeKindFromResult` 死形参、`familyFromRouted` includes、`isCompleteRoute` 谓词序、Date.parse 维持 R10-E 拒列；`applyCascadeRetry` 载荷形状按令未触碰；S2-E-2、S7-E-3、S9-E-1 维持 | 无候选 |
| `task-success.ts` | S9-E-3 涉及面维持；`copyDefinedBinding`+`present()` 空白字段契约实施点不动；S2-E-7、S8-E-2 覆盖维持 | 无候选 |
| `auto-loop.ts` | `signals.some(modelId)` 探针 × bandit arms 重扫跨函数去重（§2 行 4）判 S8-E-3/S9-E-3 族 + X0-4/X1-1 拒列；`events/extraSignals ?? []` 空单例拒列维持；I/O 契约边全点名维持（S1-E-4/5、S2-E-1、S3-E-2、S4-E-2、S5-E-1/5） | 无候选 |
| `bandit-store.ts` | arms 重扫去重的被调侧（§2 行 4）同裁决；吸收析取、ENOENT catch 微整理拒列维持；fsync/重哈希/mkdir/序列化按令未触碰；S2-E-3/4、S3-E-4、S6-E-3/4、X1-2 维持 | 无候选 |
| `diagnostics.ts` | 本轮 diagnose 锚点 1.04–1.08µs（夹具差异非代码差异）；`unique`/`mode` 小集合族维持；S1-E-6、S3-E-3、S4-E-3、S5-E-4 维持 | 无候选 |
| `learned-routing.ts` | `parseLearnedRoutingPolicy` 载入期校验微观（§2 行 1）判落地线真子集不立项；错误路径 regex 探针（§2 行 2）换位点拒列；空 avoid 函数内快路径（§2 行 3）判快路径族+身份族拒列；`routingPolicyContent` pretty-print 参与 contentHash 判 CAS 契约（比 S6-E-4 更硬）；读路径完整性重哈希拒列维持（S8-D-2 同式）；S8-E-1（双载入，勿去重）、S8-E-3、S2-E-6、X1-1 维持 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | 本轮交叉检索复核仍零生产调用方（仅测试使用）；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下切片相关套件基线复核，全绿（Node v22.14.0）：

```bash
npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0
```

锚点脚本（临时置于 `/tmp/r11e-anchor.mts`，未入库以遵守无赢家纪律；
完整源码见附录 A，seeds `0xe11e01`–`0xe11e05`）共 **8 次独立运行**，每次
3/3 sanity 检查全过，全部确定性 VERDICT 行跨 8 次运行**逐位一致**。
代表性一次运行：

```text
VERDICT fixtures: E=41 collected=15 routedE=41 outcomes=2 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=13.1us outcomes=2.4us diagnose=1.05us bandit-build=0.7us | total in-slice CPU ~17.3us per run vs landing bar >=10000us (579x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=266ns/task | empty policy=115ns/task -> x10 tasks/run = 2.66us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=27.3us | realistic registry (baseline+1 candidate)=76.0us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

计时跨 8 次运行汇总：SLICE-CPU 总量 17.2–17.6µs 稳定（570–583×）；
`applyLearnedRouting` 非空 254–281ns/task、空策略 113–125ns/task 稳定；
`loadLearnedRouting` fresh 25.6–34.1µs、现实 registry 76.0–84.5µs 稳定。
本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐形态有理由；第十一遍读码新探的四个结构
  位置全部落进已关闭机制类或落地线算术封死区，未发现第十个机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令封死的
  I/O 契约面上；CPU 面即使清零也距门槛 570–583×）。
- 矩阵空洞：无（调用位点行号与 R8-E/R9-E/R10-E 逐一相同；配置态 live 面
  与载入面锚点全数复测在带内）。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R10-E §6：SLICE-CPU 锚点失效（全切片 CPU
增长 ≥2 个量级，本轮复核值 17.2–17.6µs、历史保守口径 ~22.1–22.5µs）或
任一 I/O 契约排除（X0-3 / S2-E-1/4 / S1-G-1 / S1-E-4/5 / S4-E-2 / S5-E-5 /
S6-E-3/4 / S8-E-1）被正式推翻，或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

锚点脚本全文（`/tmp/r11e-anchor.mts`，`npx tsx /tmp/r11e-anchor.mts`）。
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅 seeds 换为
`0xe11e01`–`0xe11e05`、NOW/标签/临时目录前缀换为本轮值——完整差异面
即以下四处：`NOW = "2026-08-24T18:30:00.000Z"`；SECTION 1 的 seeds
`0xe11e01`/`0xe11e02`/`0xe11e03`；SECTION 2 的 `mkdtemp` 前缀
`r11e-fresh-`/`r11e-reg-` 与 detector identity `r11e-anchor`；头注释轮次。
其余逐字同 R10-E 附录 A（含 `genCollectEvents` / `genRoutedEvents` /
`genSignal` / `banditBuild` 四个生成器与全部 BENCH/VERDICT 逻辑），
不在此重复 360 行全文；需复现时以 R10-E 附录 A 为模板做上述四处替换即可
逐字重建，或直接比对本报告 §1/§4 的输出行格式（完全一致）。

8 次运行原始区间：

```text
SLICE-CPU:  17.5 / 17.2 / 17.3 / 17.2 / 17.6 / 17.3 / 17.3 / 17.3 us
collect:    13.2 / 13.0 / 13.1 / 13.0 / 13.3 / 13.0 / 13.1 / 13.1 us
outcomes:   2.4 x8 us
diagnose:   1.07 / 1.05 / 1.08 / 1.06 / 1.06 / 1.05 / 1.05 / 1.04 us
bandit:     0.8 / 0.7 / 0.7 / 0.8 / 0.7 / 0.7 / 0.8 / 0.7 us
apply(non): 277 / 254 / 281 / 269 / 262 / 273 / 277 / 266 ns/task
apply(空):  125 / 115 / 117 / 123 / 113 / 114 / 123 / 115 ns/task
load fresh: 30.8 / 25.6 / 26.3 / 34.1 / 26.4 / 26.1 / 26.6 / 27.3 us
load real:  80.7 / 83.1 / 84.4 / 77.9 / 81.2 / 84.5 / 77.9 / 76.0 us
VERDICT 行: 8/8 逐位一致；sanity 3/3 x8 全过
```
