MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 11 / R11-C：离线路由切片第十一遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 11 / R11-C（Round 1–10 同区第十一遍，叠在
  已落地的 S7-C 之上）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `ee57e97`（Round 10 收口、R10-J
  空枚举合入后的独占分支最新 tip；`git log -1 -- src/routing/` 显示切片最后一次
  生产改动**仍为 `183df9b`「perf(routing): land S7-C support-only eta」**——与派单
  预期一致，其后基线区间仅文档更新，本轮全部测量在该形态之上，未回退未重写）
- **分支:** `cursor/r11-c-offline-routing-eleventh-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十一组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行重读 + 对照完整排除表（X0-\*..X4-\*、S1-C-1..10 …
S10-C-1..3 全系）后，切片内每一个剩余站点都落入三类之一：
（i）已有排除条目本体或其重命名/重选址；（ii）纯算术池上界即低于
±35 ms 噪声带（整池清零也不越带）；（iii）逐位契约违例且本轮以数值
反例探针坐实。没有任何一个站点需要立仿真道决斗——立道本身就构成对
已关闭 ID 的重铸，或对亚带池的重复测量。因此本轮无 S11-C-\* 编号，
遵循 R10-J「从未点名微站点均低于门槛→零铸号」先例。

本轮新完成的关闭性工作（全部为**关闭证据**，非候选）：

- **RID 探针（岭项预置）**：把 `xtwx[d][d] += 1e-6` 从累加后移到累加前
  （对角以 1e-6 起始）是归约重排——**10,000 组随机 w 序列中 3,792 组
  逐位不等**（首例 ws=[0.102448, 0.0822254, 0.127424] → post=
  0.312098406538669 vs pre=0.31209840653866905）。X2-3 数值路径冻结
  同族，理论直接淘汰，永不成为候选。
- **CNT 探针（同键分组累加）**：把行序累加改为按键分组（每键子和再
  合并）是跨键归约重排——在生产真实量纲下（mu 按 logit 尺度抽取，
  w=mu(1−mu) 跨多个数量级直至 1e-10 钳制）**10,000 组随机交错序列中
  5,087 组逐位不等**。两个诚实副发现一并留档：（a）**连续**同值重复
  加法 w+…+w（k≤8）与 k·w 在 10,000/10,000 组中逐位相同；（b）同量纲
  均匀 (0,0.25] 抽样下交错重排也 10,000/10,000 相同——破坏逐位的是
  **混合量纲**，而生产 w 在接近分离时按构造即混合量纲。X2-3 同族，
  理论淘汰。
- **AUGF（增广列折叠）**：把 RHS `x` 折入行数组、让消元 k 循环覆盖
  增广列——**不删除任何一次浮点或访存**（x 更新本就是每 (col,row)
  一条直线语句，无循环开销可摊销），纯布局改动，S4-C-2（扁平内部
  表示，实测更劣）同族。零收益机制，不立道。
- **EXP 池上界（irls sigmoid）**：本 VM 实测 12.9–17.0 ns/sigmoid ×
  **1,249,652 次/报告**（仪表化精确计数）≈ **16–21 ms 整池** <
  ±35 ms 带；且任何 exp 替换（查表/多项式/降精度）本体即非逐位
  （X1-3/X2-3 同族）。双重关闭。
- **ALLOC 上界（irls 缓冲跨拟合提升）**：整套 per-fit 缓冲
  （eta/mu/xtwx/xtwz/stamp/etaByKey/muByKey/beta，n=400, p=60）实测
  分配成本 **9.96–17.20 µs/拟合 × 201 拟合 = 2.0–3.5 ms/报告封顶**，
  深度亚带（S1-C 落地形态已把分配摊到 per-fit；再往上提是 X1-1 模块
  态或私有签名改动换 ~2 ms，不成立）。
- **DMAP 上界（bootstrap draws 字符串键 Map）**：200 draw × 59 名 =
  12,200 次 get+push 实测 **0.110–0.121 ms/报告**，亚带 ~300×。
- **COPY 上界（solve 防御拷贝）**：`m = a.map(row => [...row])` + 
  `x = [...b]` 实测 **2.09–2.18 µs/次 × 8,966 解 = 18.8–19.5 ms/报告
  封顶**——即使整项清零也在带内；且拷贝存在性是公开 readonly 契约
  （S4-C-3 结论「拷贝存在性维持」），机制层已关闭。
- **SCAN 上界（APC 逐列活跃行扫描）**：59 名 × 400 行扫描集实测
  **0.059–0.061 ms × 201 拟合 = 11.9–12.2 ms/报告**——扫描反转/预索引
  是 S2-C-4 已排除家族，且整项亚带。

**三大池地板分解（§3）显示不存在未点名的 ≥35 ms 机制**：solve 池
（451.7–453.7 ms）扣除全部已关闭可省项后余量即冻结浮点序列本体；
irls 自身池（149.0–152.3 ms）同理；APC 池总量 45.0–46.2 ms 虽越带，
但实测地板（扫描 ~12 + 独立对 support-sum+sigmoid ~2–3 + 逐对累加
~0.5 + 调用开销 ~0.1 ≈ **~15 ms**）之上的全部可省空间 ≈ **30–31 ms
< 35 ms**——**即使把 S8-C-2、S10-C-2、S2-C-4、S4-C-4 全部完美叠加，
APC 组合天花板也无法越带**，组合出路在本 VM 以算术关闭。irls 侧与
solve 侧的组合出路同样只由已关闭 ID 构成（DKL 实测 +11–17、S9-C-3
三次符号翻转 ≈ 0、S5-C-7 仅上界无正决斗、COPY 契约封闭、S5-C-6 ~10
上界），实测和不具备符号稳定越带能力——R10-C 已用 CMB 探针实测最大
两项叠加 +8.8/+28.5/+30.5 ms 带级摆幅带内，派单明令不得重铸该组合，
本轮不再立道。与 R7-C SOD 档案先例（单站点 +71–77 ms 干净越带才值得
上呈表所有者）对照，本轮无任何组合的实测中位与摆幅支持上呈，**不建
档案**。

重剖析（§1）确认 S7-C 后成本模型在本 VM 复现且与 R9-C/R10-C 同带：
生产整报告中位 **657.9–663.2 ms**（另有 r7c 回归资产独立锚点
658.3 ms，1.12× vs 冻结 S6-C 参照）；`solveSymmetric` **67.8–68.1%
（451.7–453.7 ms）**、`irls` 自身 **22.5–22.8%（149.0–152.3 ms）**、
其余各池（APC 45.0–46.2、on-prob 7.1–7.6、采样 4.9–5.3、排序/分位尾
1.5、编排余量 1.3–3.9 ms）——除 APC 外每池整池清零也低于噪声带，
APC 池由 §3 地板分解关闭。小形状矩阵复核（R7-I 教训）：3 模型 ×
3 项目 / 120 行档整报告中位 **33.7–34.2 ms**——小配置态一侧**整条
报告**都低于 ±35 ms 带，该侧不存在任何可越带候选。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11），live 面零文件改动。仓库变更仅本报告一个文件；无生产赢家故
未提交 `scripts/round11-r11c-equivalence-sim.ts`（测量彩排全文进附录，
遵守 R7-C/R8-C/R9-C/R10-C 先例）。

## 0. 范围与约束遵守

- 读序（强制）已履行：README → EXCLUSIONS.md（完整表，含
  S10-A-1 / S10-B-1..3 / S10-C-1..3 / S10-D-1..2 / S10-G-1）→
  round-11/PLAN.md → round-10/PLAN.md + R10-C.md → R9-C … R1-C 与
  R7-C-LAND.md → 9 个切片源文件全量重读。
- 基线漂移检查：基线 `ee57e97` 相对 R10-C 基线 `253d589` 仅文档合入
  （R10-C..J 报告、Round 10 收口、R11 PLAN 与 S10-\* 排除行）；
  `git log -1 -- src/routing/` 仍为 `183df9b`（S7-C 落地本身），落地
  形态原样在位。既有回归资产七连全绿且检查数逐一匹配锚定值（§5）。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径；
  X0-\*..X4-\*、S1-\*..S10-\* 全系未重提、未立道、未重测。本轮全部
  新探针（RID/CNT/AUGF/EXP/ALLOC/DMAP/COPY/SCAN）均为关闭证据而非
  候选道；S10-C-1..3 重命名检查见 §4。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价；
  双 LCB 与双归因两路一行未删；promotion proposal-first；阈值 / 测试 /
  公开签名 / 数据面契约不动；单线程逐位审计基底不动（无 worker_threads
  / WASM / SIMD 尝试）。
- 环境：Node 22.22.2（VM 默认 22.14.0 低于 engines ≥22.19.0，nvm
  切换并前置 PATH，与 R1-C..R10-C 同处理）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`。

## 1. 重剖析：S7-C 后成本模型（本 VM 实测，未拷贝历轮数字）

测量器为**仪表化逐字复刻管线**（fitInst：与 183df9b 生产
irls/solve/on-prob/APC 逐语句相同，仅在调用边界加 performance.now()
与计数器，浮点操作/操作数/顺序零改动），其忠实性先经 **53 例全形状
电池 vs 生产 `fitLogitAdditive` 导入逐位全绿**（Object.is 含符号零）
再采信读数；生产中位数则由**未仪表化生产导入**直接测得。perf 夹具与
R10-C 完全一致（fixtureRng(0xbeef)，4 场景 × 6 模型 × 8 项目，
rows=400，passRate=0.6，bootstrap=200, seed=20260818 → p=60，
201 拟合，8,966 次 IRLS 迭代）。

生产整报告中位（每次独立进程 7 测量取中位，三次运行）：**663.2 /
662.1 / 661.6 ms**（另两次彩排 661.2 / 657.9；r7c 回归资产同 VM 独立
锚点 658.3 ms, 1.12× vs 冻结 S6-C）——与派单已知带 657–670 逐位重合。

仪表化池表（perf 形状，3 次仪表化运行中位；三次全量运行读数并列）：

| 池 | run A | run B | run C | 占比 | 站点计数（精确） |
| --- | --- | --- | --- | --- | --- |
| 总计 | 663.3 | 668.7 | 663.5 | 100% | fits=201 |
| `solveSymmetric` | **451.7** | **453.7** | **452.1** | **67.8–68.1%** | 8,966 次调用 |
| `irls` 自身（扣 solve） | **149.0** | **151.1** | **151.2** | **22.5–22.8%** | 8,966 迭代 / 3,586,400 行访问 / 1,249,652 sigmoid |
| APC 全站点 | 45.7 | 46.2 | 45.0 | 6.8–6.9% | 277,606 活跃对 |
| `onProbabilitiesFor` | 7.4 | 7.6 | 7.1 | 1.1% | 80,400 sigmoid+dot |
| bootstrap 采样 | 5.0 | 5.1 | 5.3 | 0.8% | — |
| 排序/分位尾 | 1.5 | 1.5 | 1.5 | 0.2% | 12,200 draws-map ops |
| 编排余量（build/supports/keys/拷贝） | 2.9 | 3.5 | 1.3 | 0.2–0.6% | — |

除 solve / irls / APC 外每池整池清零也低于 ±35 ms 带（R9-C/R10-C 同
结论，逐池复测未漂移）；solve 与 irls 为落地线唯二宿主、APC 池总量
越带但地板分解关闭（§3）。小形状档（3 模型 × 3 项目 / 120 行，
bootstrap=200）整报告中位 **33.7 / 34.2 / 34.2 ms**——小配置态整条
报告低于噪声带，调用矩阵小态一侧整体收口（R7-I 矩阵义务履行）。

生产自确定性复核：3 seed × 各 2 次运行全字段 Object.is 全绿（含于
§5 的 3,332 项检查）。

## 2. 第十一组枚举（诚实结果：空）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位。**没有任何站点三问全过。**
从未点名的站点全部在下表关闭（无一铸号；探针数字为本 VM 实测，
出处为附录测量彩排三次独立运行）：

| 站点（从未点名） | 关闭方式 | 证据 |
| --- | --- | --- |
| RID：岭项预置进 fill 循环（对角起始 1e-6） | 理论淘汰：归约重排（X2-3 同族） | 3,792/10,000 随机 w 序列逐位不等；首例见结论 |
| CNT：同键分组累加（每键子和/计数乘再合并） | 理论淘汰：跨键归约重排（X2-3 同族） | 真实量纲下 5,087/10,000 交错序列逐位不等；两个副发现（连续同值 k≤8 加法 = k·w；同量纲均匀抽样重排 0 反例）留档以防将来误判 |
| AUGF：RHS 增广列折叠进消元 k 循环 | 零收益机制：不删任何浮点/访存，纯布局（S4-C-2 家族） | x 更新本为每 (col,row) 单条直线语句 |
| EXP：irls sigmoid 整池 | 算术上界亚带 + 替换非逐位（X1-3/X2-3） | 1,249,652 次/报告 × 12.9–17.0 ns ≈ 16–21 ms < 35 |
| ALLOC：irls 缓冲跨拟合提升 | 算术上界深度亚带 | 9.96–17.20 µs/拟合 × 201 = 2.0–3.5 ms 封顶 |
| DMAP：draws 字符串键 Map 换列索引 | 算术上界亚带 ~300× | 12,200 ops 实测 0.110–0.121 ms/报告 |
| COPY：solve 防御拷贝消除 | 上界带内 + 公开 readonly 契约（S4-C-3「拷贝存在性维持」） | 2.09–2.18 µs × 8,966 = 18.8–19.5 ms 封顶 |
| SCAN：APC 逐列活跃行扫描预索引 | 上界亚带 + S2-C-4 家族 | 11.9–12.2 ms/报告实测 |

以上无一构成候选：RID/CNT 是逐位违例（永久关闭，无重开条件）；
AUGF 无机制；EXP/ALLOC/DMAP/SCAN 整池亚带（重开条件同历轮：现实
规模位移 ≥1 个数量级）；COPY 受契约与带内上界双重封闭。**故第十一组
候选集为空，不铸 S11-C-1 及后续编号。**

## 3. 三大池地板分解与组合出路复核

**solve 池 451.7–453.7 ms**：消元主体 ≈ n³/3 = 72,000 mul-sub/解 ×
8,966 解 ≈ **6.45 亿次浮点乘减/报告**——顺序冻结的操作序列本体，
在单线程逐位契约下即地板。可省项全部已关闭：防御拷贝 18.8–19.5
（COPY，契约封闭）、主元搜索 ~10 上界（S5-C-6）、回代展开（S5-C-5
串行依赖链）、循环开销（S5-C 落地 U4；U8 仅 +9–15 = S5-C-2；分块
S5-C-3/4 更劣）、死存储跳过（S4-C-1 双向抖动）、扁平表示（S4-C-2
实测更劣）、残留微观（S6-C-7）、对角卫 DCE（S8-C-4 更慢）、对称
镜像（S10-C-3 理论否决）。触碰地板本体的机制只剩 SIMD/线程
（S9-C-1/2 + 不变量禁止）与数值路径改道（X2-3 冻结）。

**irls 自身池 149.0–152.3 ms**：地板为 S6-C 直线累加本体
（3,586,400 行访问 × 平均支撑 ~4.46 → ~9,300 万次直加+读）与每行
w/z 计算（~2,150 万 flop）。可省项全部已关闭或本轮理论淘汰：
eta/mu 键机制（S2-C 落地本体之上的 S10-C-1 实测决斗 +11–17 带内、
S9-C-3 三次符号翻转、S6-C-6 上界 2–4）、整行 fill(0)（S5-C-7 上界
15–25 仅上界无正决斗——触碰单元追踪自身有成本）、w 钳制（S9-C-4
0.7–2.3）、ys 预提取（S4-C-5）、首迭代短路（S4-C-6）、delta 融合
（S2-C-3）、有限性检查 DCE（S8-C-3）、岭项预置（本轮 RID 反例
淘汰）、分组累加（本轮 CNT 反例淘汰）、sigmoid 整池（本轮 EXP
上界 16–21 亚带）。

**APC 池 45.0–46.2 ms（唯一总量越带的非宿主池）**：实测地板 ≈
扫描 11.9–12.2（SCAN）+ 独立对不可省工作 ~2–3（573/1,383 独立比 ×
277,606 对 ≈ 11.5 万独立对 × (support-sum + sigmoid ~13–17 ns)）+
逐对 on−off 累加 ~0.3–0.6 + 11,859 次调用开销 ~0.1 ≈ **~15 ms**。
池减地板 = **30–31 ms < 35 ms**：把 S8-C-2（支撑求和免 slice+dot，
实测天花板 22.2–23.6、决斗 +14–18）、S10-C-2（键去重，天花板真子集、
决斗 +11–13）、S2-C-4（扫描反转）、S4-C-4（scratch 复用）全部完美
叠加也无法越带。**APC 组合出路在本 VM 以算术关闭，无需立道。**

**跨池大组合**：唯一能在纸面凑出 ≥35 的聚合全部由已关闭 ID 构成
（irls 侧 DKL+S9-C-3+S5-C-7 + APC 侧 S8-C-2+S10-C-2 + solve 侧
COPY/S5-C-6）。实测证据链：R10-C CMB（最大两项叠加）三次干净两路
+30.5/+28.5/+8.8 ms——中位带内且跨进程摆幅 21.7 ms 本身即带级；
本轮各池地板显示其余可加项的实测正贡献（S9-C-3 ≈ 0 符号翻转、
S5-C-7 无正决斗、COPY 契约封闭）不足以把该组合推到**符号稳定越带**。
派单明令「Do not remint that composition」，且组合的每一元素均为
关闭 ID——按验收第 1 条（不在排除表且非重命名）组合不可能成为赢家，
按 R7-C 档案先例（单站点 +71–77 干净越带才上呈）也不够档案门槛。
**组合出路维持 R10-C 裁决：关闭。**

推翻本轮空枚举声明需要：（a）表所有者层级契约变更（公开异步签名 /
构建面开放 / X2-3 数值路径解冻 / 单线程审计基底变更——均不预期）；
（b）现实规模位移 ≥1 个数量级使 EXP/SCAN/COPY/ALLOC 级池放大越带；
或（c）指出本报告 §2 表格与 §3 三池分解之外的第八类成本——十一遍
枚举未再发现。

## 4. S10-C-1..3 重命名检查（派单强制项）

- **S10-C-1（DKL 独立键代表表）**：本轮零重提。全部新探针不触碰
  stamp 扫描结构——RID/CNT 关注累加顺序、EXP 关注 sigmoid 成本、
  ALLOC 关注分配；无任何「代表行列表 / 首现序键收集 / 独立键循环」
  形态出现在任何探针或分析中。irls 侧组合分析仅引用 R10-C 已有实测
  数字（+11–17），未重测未立道。
- **S10-C-2（AKD APC off 键去重）**：本轮零重提。APC 侧只做了池总量
  与地板的**边界测量**（SCAN 探针测扫描本体成本，不含任何 off 值
  缓存/去重形态）；§3 组合天花板仅引用 R10-C/R8-C 已有实测数字
  （+11–13 / +14–18），未重测未立道。
- **S10-C-3（SYM 对称 Schur 镜像）**：本轮零重提。无任何对称性利用
  出现：CNT/RID 反例针对 irls 累加序，与消元更新 fl(fl(x/p)·y) 无关；
  solve 侧分析未触碰消元数学，仅测防御拷贝与引用既有上界。X2-3
  冻结与 3,458/10,000 反例裁决原样维持。

## 5. 测试与验证

环境：Node 22.22.2（nvm；VM 默认低于 engines）、pnpm 10.17.1。
生产代码零改动，测试文件零改动。

```bash
pnpm typecheck && pnpm lint && pnpm build    # ✓ 全绿（零 diff 基线自证）
pnpm test                                    # ✓ 全套通过（pass 1168 / fail 0 / skipped 1）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录）
npx tsx /tmp/r11c-anchor.mts                # ✓ 3,332 项逐位 × 3 次独立运行全绿
                                            #   （53 例仪表化忠实性电池 + 3 seed × 2 自确定性 + perf 夹具逐位）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 658.3 ms（1.12× vs 冻结 S6-C），与 §1 生产中位
# （657.9–663.2）同带。
```

代表性一次测量彩排全量输出（run A；run B/C 数字见 §1 池表与 §2）：

```text
scenario 1 (instrumented-lane bitwise fidelity, 53 cases x {instrumented} vs production fitLogitAdditive import)
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)
  shape small (3 models x 3 projects, 120 rows): production median 34.2 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 663.2 ms/report (7 reps)
instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):
  total                 663.3 ms
  solveSymmetric        451.7 ms  (68.1%)  [8966 calls]
  irls minus solve      149.0 ms  (22.5%)  [8966 iterations, 3586400 row visits, 1249652 irls sigmoids]
  onProbabilitiesFor    7.4 ms  (1.1%)  [80400 sigmoid+dot]
  APC (all sweeps)      45.7 ms  (6.9%)  [277606 active pairs]
  bootstrap sampling    5.0 ms  (0.8%)
  sort/percentile tail  1.5 ms  (0.2%)  [12200 draws-map ops]
  outside pools (build/supports/keys/base copies) 2.9 ms  (0.4%)
  fits 201
probe RID (ridge pre-seed): 3792/10000 random w-sequences give a different double; first: ws=[0.102448,0.0822254,0.127424] post=0.312098406538669 pre=0.31209840653866905
probe CNT (grouped same-key accumulation, interleaved keys): 5087/10000 random interleavings give row-order != grouped; first: ws=[0.000438563,0.00000570099,3.96669e-9] seq=011002011 serial=0.0017770588476092668 grouped=0.001777058847609267
probe EXP: 12.9 ns/sigmoid (sink=814964.253)
probe ALLOC: 9.96 us per irls buffer set -> x201 fits = 2.00 ms/report ceiling (sink=4360000)
probe COPY: 2.18 us per defensive copy -> x8966 solves = 19.5 ms/report ceiling (sink=2400000)
probe SCAN: 0.059 ms per 59-name x 400-row sweep set -> x201 fits = 11.9 ms/report (sink=624400)
probe DMAP: 0.112 ms/report for 200x59 map get+push (sink=17700)

ALL EQUIVALENCE CHECKS PASSED (3332 bitwise checks)
```

## 附录：测量彩排脚本（完整，可复现）

```ts
/**
 * Round 11 / R11-C measurement re-anchor + micro-site closure probes
 * (offline routing slice, eleventh pass). NOT committed — no candidate
 * lanes exist this round; full source goes into the R11-C report appendix.
 *
 * This harness deliberately carries ZERO variant lanes. The eleventh-group
 * enumeration is empty: every remaining site is (i) an existing exclusion,
 * (ii) bounded sub-band by pool arithmetic, or (iii) a bitwise violation
 * with a concrete counterexample. The harness therefore only:
 *
 *   1. re-anchors the post-S7-C profile on THIS VM: production median
 *      ms/report on the R10-C perf shape (400 rows, 6 models x 8 projects,
 *      bootstrap=200) and the small shape, x-checked by an INSTRUMENTED
 *      verbatim pipeline copy (timers/counters only; every float op, value,
 *      and order identical — validated bitwise against the production
 *      import over the full 53-case battery before its numbers are used);
 *   2. derives the per-pool cost table (solve / irls-other / on-prob / APC /
 *      sampling / tail) plus site counts (irls iterations, sigmoid calls,
 *      row visits, APC active pairs, draws-map ops);
 *   3. closes the never-named micro-sites with counterexample probes
 *      (RID ridge pre-seed, CNT grouped same-key accumulation — both
 *      reduction-order / X2-3-family) and arithmetic pool bounds
 *      (EXP sigmoid pool, ALLOC cross-fit buffer hoist, DMAP draws-map
 *      keying);
 *   4. re-checks production self-determinism across 3 seeds x 2 runs.
 *
 * Run: npx tsx /tmp/r11c-anchor.mts
 */

import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import type { AttributionReport, OfflineRow } from "/workspace/src/routing/offline-types.js";

/* --------------------------- shared constants --------------------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}

interface FitResult {
  readonly coefficients: readonly number[] | null;
}

function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}

function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/* ------------- verbatim production pipeline (instrumented) -------------- *
 * Transcribed from src/routing/offline-logit.ts and src/routing/lin-alg.ts
 * at 183df9b-landed state. The ONLY additions are STATS counter increments
 * and performance.now() reads at call boundaries; no float operation,
 * operand, or ordering differs. Bitwise fidelity is checked against the
 * production import over the full battery before any number is reported. */

const STATS = {
  fits: 0,
  irlsMs: 0,
  irlsIterations: 0,
  solveCalls: 0,
  solveMs: 0,
  sigmoidIrls: 0,
  rowVisits: 0,
  onProbMs: 0,
  onProbSigmoids: 0,
  apcMs: 0,
  apcActivePairs: 0,
  samplingMs: 0,
  drawsMapOps: 0,
  tailMs: 0,
  totalMs: 0,
  reset(): void {
    this.fits = 0; this.irlsMs = 0; this.irlsIterations = 0; this.solveCalls = 0;
    this.solveMs = 0; this.sigmoidIrls = 0; this.rowVisits = 0; this.onProbMs = 0;
    this.onProbSigmoids = 0; this.apcMs = 0; this.apcActivePairs = 0;
    this.samplingMs = 0; this.drawsMapOps = 0; this.tailMs = 0; this.totalMs = 0;
  }
};

function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);

  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);

  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);

  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });

  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}

function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}

function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}

/* verbatim landed solveSymmetric (lin-alg.ts) */
function solveInst(a: readonly (readonly number[])[], b: readonly number[]): number[] | null {
  const n = b.length;
  if (a.length !== n || a.some((row) => row.length !== n)) {
    throw new DomainValidationError("solveSymmetric requires a square matrix matching b");
  }
  const m: number[][] = a.map((row) => [...row]);
  const x: number[] = [...b];
  const eps = 1e-12;

  for (let col = 0; col < n; col++) {
    let pivotRow = col;
    let pivotAbs = Math.abs(m[col]![col]!);
    for (let row = col + 1; row < n; row++) {
      const abs = Math.abs(m[row]![col]!);
      if (abs > pivotAbs) {
        pivotAbs = abs;
        pivotRow = row;
      }
    }
    if (pivotAbs < eps) return null;
    if (pivotRow !== col) {
      const tmp = m[col]!;
      m[col] = m[pivotRow]!;
      m[pivotRow] = tmp;
      const tb = x[col]!;
      x[col] = x[pivotRow]!;
      x[pivotRow] = tb;
    }
    const colArr = m[col]!;
    const pivot = colArr[col]!;
    const xCol = x[col]!;
    for (let row = col + 1; row < n; row++) {
      const rowArr = m[row]!;
      const factor = rowArr[col]! / pivot;
      if (factor === 0) continue;
      let k = col;
      const stop = n - 3;
      for (; k < stop; k += 4) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
        rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
        rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
      }
      for (; k < n; k++) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
      }
      x[row] = x[row]! - factor * xCol;
    }
  }

  const solution = new Array<number>(n).fill(0);
  for (let row = n - 1; row >= 0; row--) {
    const rowArr = m[row]!;
    let sum = x[row]!;
    for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
    const diag = rowArr[row]!;
    if (Math.abs(diag) < eps) return null;
    solution[row] = sum / diag;
  }
  for (const value of solution) {
    if (!Number.isFinite(value)) return null;
  }
  return solution;
}

/* verbatim landed irls (offline-logit.ts), timers/counters only added */
function irlsInst(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
): FitResult {
  const tIrls0 = performance.now();
  STATS.fits += 1;
  const p = design.names.length;
  const n = rows.length;
  const eta = new Array<number>(n).fill(0);
  const mu = new Array<number>(n).fill(0);
  const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
  const xtwz: number[] = new Array<number>(p).fill(0);
  const stamp = new Int32Array(keySpace);
  const etaByKey = new Float64Array(keySpace);
  const muByKey = new Float64Array(keySpace);
  let beta = new Array<number>(p).fill(0);
  let result: FitResult | null = null;
  for (let iter = 0; iter < maxIter; iter++) {
    STATS.irlsIterations += 1;
    STATS.rowVisits += n;
    const mark = iter + 1;
    for (let i = 0; i < n; i++) {
      const key = keys[i]!;
      if (stamp[key] !== mark) {
        stamp[key] = mark;
        STATS.sigmoidIrls += 1;
        let value = 0;
        const active = supports[i]!;
        for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
        etaByKey[key] = value;
        muByKey[key] = sigmoid(value);
      }
      eta[i] = etaByKey[key]!;
      mu[i] = muByKey[key]!;
    }
    for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
    xtwz.fill(0);
    for (let i = 0; i < n; i++) {
      const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
      const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
      const wz = w * z;
      const active = supports[i]!;
      switch (active.length) {
        case 5: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          const a4 = active[4]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
          xtwz[a4] = xtwz[a4]! + wz;
          const r4 = xtwx[a4]!;
          r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
          break;
        }
        case 4: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
          break;
        }
        case 3: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
          break;
        }
        case 2: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
          break;
        }
        default: {
          for (let ai = 0; ai < active.length; ai++) {
            const a = active[ai]!;
            xtwz[a] = xtwz[a]! + wz;
            const rowA = xtwx[a]!;
            for (let bi = 0; bi < active.length; bi++) {
              const b = active[bi]!;
              rowA[b] = rowA[b]! + w;
            }
          }
        }
      }
    }
    for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
    const tSolve0 = performance.now();
    const next = solveInst(xtwx, xtwz);
    STATS.solveMs += performance.now() - tSolve0;
    STATS.solveCalls += 1;
    if (next === null) { result = { coefficients: null }; break; }
    const delta = next.map((value, index) => value - beta[index]!);
    const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
    beta = next;
    if (!beta.every(Number.isFinite)) { result = { coefficients: null }; break; }
    if (l2 < TOL) break;
  }
  STATS.irlsMs += performance.now() - tIrls0;
  return result ?? { coefficients: beta };
}

function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}

function onProbInst(vectors: readonly number[][], coefficients: readonly number[]): number[] {
  const t0 = performance.now();
  const result = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
  STATS.onProbMs += performance.now() - t0;
  STATS.onProbSigmoids += vectors.length;
  return result;
}

function apcInst(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  coefficients: readonly number[],
  onProbabilities: readonly number[],
  column: string
): number {
  const t0 = performance.now();
  let sum = 0;
  const columnIdx = design.columnIndex.get(column);
  if (columnIdx !== undefined && columnIdx !== 0) {
    for (let i = 0; i < rows.length; i++) {
      if (vectors[i]![columnIdx] === 0) continue;
      STATS.apcActivePairs += 1;
      const on = onProbabilities[i]!;
      const offVector = vectors[i]!.slice();
      offVector[columnIdx] = 0;
      const off = sigmoid(dot(coefficients, offVector));
      sum += on - off;
    }
  }
  const result = rows.length === 0 ? 0 : sum / rows.length;
  STATS.apcMs += performance.now() - t0;
  return result;
}

function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}

function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}

function pushValue(map: Map<string, number[]>, key: string, value: number): void {
  STATS.drawsMapOps += 1;
  const list = map.get(key);
  if (list === undefined) map.set(key, [value]);
  else list.push(value);
}

function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

function fitInst(
  rows: readonly OfflineRow[],
  options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
): AttributionReport {
  const tTotal0 = performance.now();
  const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
  const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
  const seed = options?.seed ?? SEED_DEFAULT;

  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const effects: Array<{ name: string; point: number; lcb: number; ucb: number }> = [];

  if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
  }

  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irlsInst(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
  if (fit.coefficients === null) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
  }

  const onProbabilities = onProbInst(vectors, fit.coefficients);
  const pointEffects = new Map<string, number>();
  for (const name of design.names) {
    if (name === "intercept") continue;
    pointEffects.set(
      name,
      apcInst(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
    );
  }
  for (const ref of design.referenceLevels) {
    if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
      pointEffects.set(`${ref.factor}:${ref.name}`, 0);
    }
  }

  const random = rng(seed);
  const draws = new Map<string, number[]>();
  let successful = 0;
  for (let draw = 0; draw < bootstrapDraws; draw++) {
    const tSample0 = performance.now();
    const sample: Row[] = [];
    const sampleVectors: number[][] = [];
    const sampleSupports: number[][] = [];
    const sampleKeys: number[] = [];
    for (let i = 0; i < baseRows.length; i++) {
      const index = Math.floor(random() * baseRows.length);
      sample.push(baseRows[index]!);
      sampleVectors.push(vectors[index]!);
      sampleSupports.push(supports[index]!);
      sampleKeys.push(keys[index]!);
    }
    const collapsed = sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1);
    STATS.samplingMs += performance.now() - tSample0;
    if (collapsed) continue;
    const bootFit = irlsInst(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
    if (bootFit.coefficients === null) continue;
    successful += 1;
    const sampleOnProbabilities = onProbInst(sampleVectors, bootFit.coefficients);
    for (const [name] of pointEffects.entries()) {
      const value = apcInst(
        design,
        sample,
        sampleVectors,
        bootFit.coefficients,
        sampleOnProbabilities,
        name
      );
      pushValue(draws, name, value);
    }
  }

  const tTail0 = performance.now();
  for (const [name, point] of pointEffects.entries()) {
    const values = draws.get(name) ?? [];
    if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
    }
    values.sort((a, b) => a - b);
    effects.push({
      name,
      point,
      lcb: percentile(values, 0.025),
      ucb: percentile(values, 0.975)
    });
  }

  const n = baseRows.length;
  const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
  const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
  const muLcb = betaQuantileLcbLocal(muPosterior, 0.05);
  const models = new Set(baseRows.map((r) => r.modelVersion)).size;
  const projects = new Set(baseRows.map((r) => r.projectId)).size;
  const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
  const containsZero = (e: { lcb: number; ucb: number }): boolean =>
    e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
  const interactionsFor = (prefix: string): Array<{ name: string; lcb: number; ucb: number; point: number }> =>
    effects.filter((e) => e.name.startsWith(`w:${prefix}`));

  let diagnosis: AttributionReport["diagnosis"] = "uncertain";
  const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
  if (
    effects.some(
      (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
    )
  ) {
    diagnosis = "model-problem";
  } else if (
    effects.some(
      (e) =>
        e.name.startsWith("v:") &&
        e.lcb < -ATTRIBUTION_EFFECT &&
        effects
          .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
          .every(containsZero)
    )
  ) {
    diagnosis = "project-problem";
  } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
    diagnosis = "interaction-only";
  } else if (scenarioHard) {
    diagnosis = "scenario-hard";
  }

  const report: AttributionReport = {
    estimator: "logit-additive",
    rowsUsed: baseRows.length,
    effects,
    diagnosis,
    reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
    writesActivePointer: false
  };
  STATS.tailMs += performance.now() - tTail0;
  STATS.totalMs += performance.now() - tTotal0;
  return report;
}

/* betaQuantileLcb is imported by production from posterior.ts; the tail is
 * timing-irrelevant (sub-ms) but must stay bitwise for the equivalence
 * battery, so import the production function directly. */
import { betaQuantileLcb } from "/workspace/src/routing/posterior.js";
function betaQuantileLcbLocal(posterior: { alpha: number; beta: number }, quantile: number): number {
  return betaQuantileLcb(posterior, quantile);
}

/* ------------------------------ harness ------------------------------ */

let checksPassed = 0;
let failures = 0;

function check(label: string, ok: boolean): void {
  if (ok) checksPassed++;
  else {
    failures++;
    process.stderr.write(`FAIL ${label}\n`);
  }
}

function compareReports(label: string, expected: AttributionReport, actual: AttributionReport): void {
  check(`${label}.estimator`, expected.estimator === actual.estimator);
  check(`${label}.rowsUsed`, expected.rowsUsed === actual.rowsUsed);
  check(`${label}.diagnosis`, expected.diagnosis === actual.diagnosis);
  check(`${label}.reason`, expected.reason === actual.reason);
  check(`${label}.writesActivePointer`, expected.writesActivePointer === actual.writesActivePointer);
  check(`${label}.effects.length`, expected.effects.length === actual.effects.length);
  if (expected.effects.length === actual.effects.length) {
    for (let i = 0; i < expected.effects.length; i++) {
      const e = expected.effects[i]!;
      const g = actual.effects[i]!;
      check(`${label}.effects[${i}].name`, e.name === g.name);
      check(`${label}.effects[${i}].point`, Object.is(e.point, g.point));
      check(`${label}.effects[${i}].lcb`, Object.is(e.lcb, g.lcb));
      check(`${label}.effects[${i}].ucb`, Object.is(e.ucb, g.ucb));
    }
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}

function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRow[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function allUniqueRows(r: () => number, count: number): OfflineRow[] {
  const rows: OfflineRow[] = [];
  for (let i = 0; i < count; i++) {
    rows.push({
      scenarioId: `s${i % 2}`,
      modelVersion: `m${i}`,
      projectId: `p${i}`,
      y: r() < 0.6 ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function batteryCases(): Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];

  for (let i = 0; i < 40; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, {
      scenarios: 2,
      models: 3,
      projects: 3,
      rows: 60,
      passRate: 0.55,
      pipeInModel: true,
    }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  cases.push({
    rows: allUniqueRows(r, 36),
    options: { bootstrap: 40, seed: 23 },
  });
  {
    const oneKey: OfflineRow[] = [];
    for (let i = 0; i < 30; i++) {
      oneKey.push({
        scenarioId: "s0",
        modelVersion: "m0",
        projectId: "p0",
        y: i % 3 === 0 ? 0 : 1,
        occurredAtMs: 1_000 + i,
      });
    }
    cases.push({ rows: oneKey, options: { bootstrap: 40, seed: 31 } });
  }
  cases.push({
    rows: [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1, occurredAtMs: 1_000 },
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 0, occurredAtMs: 1_001 },
    ],
    options: { bootstrap: 30, seed: 5 },
  });
  return cases;
}

const out = (line: string): void => {
  process.stdout.write(`${line}\n`);
};

function perfRows(): OfflineRow[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, {
    scenarios: 4,
    models: 6,
    projects: 8,
    rows: 400,
    passRate: 0.6,
  });
}

const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------- scenario 1: instrumented-lane bitwise fidelity battery --------- */

function scenarioEquivalence(): void {
  const cases = batteryCases();
  for (const [index, testCase] of cases.entries()) {
    const expected = fitLogitAdditive(testCase.rows, testCase.options);
    compareReports(`R11C-inst[${index}]`, expected, fitInst(testCase.rows, testCase.options));
  }
  out(
    `scenario 1 (instrumented-lane bitwise fidelity, ${cases.length} cases x {instrumented} ` +
      `vs production fitLogitAdditive import)`
  );
}

/* -------- scenario 2: production self-determinism across 3 seeds -------- */

function scenarioDeterminism(): void {
  const r = fixtureRng(0xd11c);
  const rows = randomRows(r, { scenarios: 3, models: 4, projects: 5, rows: 150, passRate: 0.6 });
  for (const seed of [101, 202, 303]) {
    const first = fitLogitAdditive(rows, { bootstrap: 60, seed });
    const second = fitLogitAdditive(rows, { bootstrap: 60, seed });
    compareReports(`R11C-det[seed=${seed}]`, first, second);
  }
  out("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)");
}

/* ------------- scenario 3: profile re-anchor + pool table ------------- */

function scenarioProfile(): void {
  const shapes: Array<[string, OfflineRow[]]> = [
    ["small (3 models x 3 projects, 120 rows)",
      randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 })],
    ["perf (6 models x 8 projects, 400 rows)", perfRows()],
  ];
  for (const [label, rows] of shapes) {
    const times: number[] = [];
    for (let rep = 0; rep < (label.startsWith("perf") ? 7 : 3); rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape ${label}: production median ${median(times).toFixed(1)} ms/report (${times.length} reps)`);
  }

  // Instrumented pool table on the perf shape (3 runs; medians reported).
  const rows = perfRows();
  const expected = fitLogitAdditive(rows, PERF_OPTIONS);
  const runs: Array<Record<string, number>> = [];
  for (let rep = 0; rep < 3; rep++) {
    STATS.reset();
    const actual = fitInst(rows, PERF_OPTIONS);
    if (rep === 0) compareReports("R11C-perf-fixture", expected, actual);
    runs.push({
      totalMs: STATS.totalMs, irlsMs: STATS.irlsMs, solveMs: STATS.solveMs,
      onProbMs: STATS.onProbMs, apcMs: STATS.apcMs, samplingMs: STATS.samplingMs,
      tailMs: STATS.tailMs,
      fits: STATS.fits, irlsIterations: STATS.irlsIterations, solveCalls: STATS.solveCalls,
      sigmoidIrls: STATS.sigmoidIrls, rowVisits: STATS.rowVisits,
      onProbSigmoids: STATS.onProbSigmoids, apcActivePairs: STATS.apcActivePairs,
      drawsMapOps: STATS.drawsMapOps,
    });
  }
  const med = (key: string): number => median(runs.map((r) => r[key]!));
  const total = med("totalMs");
  const solveMs = med("solveMs");
  const irlsOther = med("irlsMs") - solveMs;
  const rest = total - med("irlsMs") - med("onProbMs") - med("apcMs") - med("samplingMs") - med("tailMs");
  out(`instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):`);
  out(`  total                 ${total.toFixed(1)} ms`);
  out(`  solveSymmetric        ${solveMs.toFixed(1)} ms  (${(100 * solveMs / total).toFixed(1)}%)  [${med("solveCalls")} calls]`);
  out(`  irls minus solve      ${irlsOther.toFixed(1)} ms  (${(100 * irlsOther / total).toFixed(1)}%)  [${med("irlsIterations")} iterations, ${med("rowVisits")} row visits, ${med("sigmoidIrls")} irls sigmoids]`);
  out(`  onProbabilitiesFor    ${med("onProbMs").toFixed(1)} ms  (${(100 * med("onProbMs") / total).toFixed(1)}%)  [${med("onProbSigmoids")} sigmoid+dot]`);
  out(`  APC (all sweeps)      ${med("apcMs").toFixed(1)} ms  (${(100 * med("apcMs") / total).toFixed(1)}%)  [${med("apcActivePairs")} active pairs]`);
  out(`  bootstrap sampling    ${med("samplingMs").toFixed(1)} ms  (${(100 * med("samplingMs") / total).toFixed(1)}%)`);
  out(`  sort/percentile tail  ${med("tailMs").toFixed(1)} ms  (${(100 * med("tailMs") / total).toFixed(1)}%)  [${med("drawsMapOps")} draws-map ops]`);
  out(`  outside pools (build/supports/keys/base copies) ${rest.toFixed(1)} ms  (${(100 * rest / total).toFixed(1)}%)`);
  out(`  fits ${med("fits")}`);
}

/* ---------- scenario 4: micro-site closure probes (no lanes) ---------- */

function scenarioProbes(): void {
  // RID: pre-seeding the ridge (diagonal starts at 1e-6 before accumulation)
  // vs the landed post-accumulation add. Reduction-order counterexamples:
  // ((1e-6 + w1) + ... + wk) vs ((w1 + ... + wk) + 1e-6).
  {
    const r = fixtureRng(0x21d0);
    let mismatches = 0;
    let first: string | null = null;
    const probes = 10000;
    for (let t = 0; t < probes; t++) {
      const k = 2 + Math.floor(r() * 6);
      const ws: number[] = [];
      for (let i = 0; i < k; i++) ws.push(Math.max(r() * 0.25, 1e-10));
      let post = 0;
      for (const w of ws) post += w;
      post += 1e-6;
      let pre = 1e-6;
      for (const w of ws) pre += w;
      if (!Object.is(post, pre)) {
        mismatches++;
        if (first === null) first = `ws=[${ws.map((w) => w.toPrecision(6)).join(",")}] post=${post} pre=${pre}`;
      }
    }
    out(`probe RID (ridge pre-seed): ${mismatches}/${probes} random w-sequences give a different double; first: ${first}`);
    check("rid.counterexample-found", mismatches > 0);
  }

  // CNT: grouped same-key accumulation. Rows sharing a key are interleaved
  // with other keys in row order, so grouping (per-key subtotal, then
  // combine) reorders the cross-key summation. Counterexamples: interleaved
  // row-order serial sum vs per-key-grouped sum over 3 distinct w values,
  // with w drawn production-realistically (mu on the logit scale, so
  // w = mu(1-mu) spans decades down to the 1e-10 clamp). Two side findings
  // from probe design, kept for honesty: (a) CONSECUTIVE same-value
  // repeated addition w+...+w (k<=8) matched k*w in 10000/10000 trials;
  // (b) interleaved reorder with SAME-magnitude uniform (0,0.25] draws also
  // matched in 10000/10000 — mixed magnitudes are what break bitwise, and
  // production w is mixed-magnitude by construction near separation.
  {
    const r = fixtureRng(0xc417);
    let mismatches = 0;
    let first: string | null = null;
    const probes = 10000;
    for (let t = 0; t < probes; t++) {
      const ws = [0, 1, 2].map(() => {
        const etaDraw = r() * 40 - 20;
        const muDraw = 1 / (1 + Math.exp(-etaDraw));
        return Math.max(muDraw * (1 - muDraw), 1e-10);
      });
      const seq: number[] = [];
      const len = 6 + Math.floor(r() * 10);
      for (let i = 0; i < len; i++) seq.push(Math.floor(r() * 3));
      let serial = 0;
      for (const idx of seq) serial += ws[idx]!;
      const counts = [0, 0, 0];
      for (const idx of seq) counts[idx]!++;
      // First-seen key order, per-key subtotal by repeated addition (the
      // friendliest grouped form), then combined.
      let grouped = 0;
      const seen: number[] = [];
      for (const idx of seq) if (!seen.includes(idx)) seen.push(idx);
      for (const idx of seen) {
        let sub = 0;
        for (let c = 0; c < counts[idx]!; c++) sub += ws[idx]!;
        grouped += sub;
      }
      if (!Object.is(serial, grouped)) {
        mismatches++;
        if (first === null) {
          first = `ws=[${ws.map((w) => w.toPrecision(6)).join(",")}] seq=${seq.join("")} serial=${serial} grouped=${grouped}`;
        }
      }
    }
    out(`probe CNT (grouped same-key accumulation, interleaved keys): ${mismatches}/${probes} random interleavings give row-order != grouped; first: ${first}`);
    check("cnt.counterexample-found", mismatches > 0);
  }

  // EXP pool bound: measured ns/sigmoid x irls sigmoid count from the pool
  // table run (recomputed here on the perf shape counts).
  {
    const r = fixtureRng(0xe4b0);
    const etas: number[] = [];
    for (let i = 0; i < 4096; i++) etas.push(r() * 12 - 6);
    let sink = 0;
    const reps = 400;
    const t0 = performance.now();
    for (let rep = 0; rep < reps; rep++) {
      for (let i = 0; i < etas.length; i++) sink += sigmoid(etas[i]!);
    }
    const nsPerSigmoid = ((performance.now() - t0) / (reps * etas.length)) * 1e6;
    out(`probe EXP: ${nsPerSigmoid.toFixed(1)} ns/sigmoid (sink=${sink.toFixed(3)})`);
  }

  // ALLOC pool bound: per-fit irls buffer set (n=400, p=60, keySpace=400),
  // ns/fit x 201 fits.
  {
    const n = 400;
    const p = 60;
    let sink = 0;
    const reps = 2000;
    const t0 = performance.now();
    for (let rep = 0; rep < reps; rep++) {
      const eta = new Array<number>(n).fill(0);
      const mu = new Array<number>(n).fill(0);
      const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
      const xtwz: number[] = new Array<number>(p).fill(0);
      const stamp = new Int32Array(n);
      const etaByKey = new Float64Array(n);
      const muByKey = new Float64Array(n);
      const beta = new Array<number>(p).fill(0);
      sink += eta.length + mu.length + xtwx.length + xtwz.length + stamp.length + etaByKey.length + muByKey.length + beta.length;
    }
    const usPerFit = ((performance.now() - t0) / reps) * 1e3;
    out(`probe ALLOC: ${usPerFit.toFixed(2)} us per irls buffer set -> x201 fits = ${(usPerFit * 201 / 1e3).toFixed(2)} ms/report ceiling (sink=${sink})`);
  }

  // COPY pool bound: the solveSymmetric defensive input copy
  // (m = a.map(row => [...row]) + x = [...b]) at p=60, x8966 solves/report.
  {
    const p = 60;
    const a: number[][] = Array.from({ length: p }, () => Array.from({ length: p }, (_, j) => j * 0.5 + 1));
    const b: number[] = Array.from({ length: p }, (_, j) => j * 0.25);
    let sink = 0;
    const reps = 20000;
    const t0 = performance.now();
    for (let rep = 0; rep < reps; rep++) {
      const m: number[][] = a.map((row) => [...row]);
      const x: number[] = [...b];
      sink += m.length + x.length;
    }
    const usPerSolve = ((performance.now() - t0) / reps) * 1e3;
    out(`probe COPY: ${usPerSolve.toFixed(2)} us per defensive copy -> x8966 solves = ${(usPerSolve * 8966 / 1e3).toFixed(1)} ms/report ceiling (sink=${sink})`);
  }

  // SCAN pool bound: the APC per-column active-row scan
  // (vectors[i][columnIdx] === 0 test over n rows), n=400 x 59 names x 201 fits.
  {
    const n = 400;
    const p = 60;
    const r = fixtureRng(0x5ca9);
    const vectors: number[][] = Array.from({ length: n }, () => {
      const v = new Array<number>(p).fill(0);
      v[0] = 1;
      for (let k = 0; k < 4; k++) v[1 + Math.floor(r() * (p - 1))] = 1;
      return v;
    });
    let sink = 0;
    const reps = 400;
    const t0 = performance.now();
    for (let rep = 0; rep < reps; rep++) {
      for (let name = 1; name < p; name++) {
        for (let i = 0; i < n; i++) {
          if (vectors[i]![name] === 0) continue;
          sink++;
        }
      }
    }
    const msPerSweepSet = (performance.now() - t0) / reps;
    out(`probe SCAN: ${msPerSweepSet.toFixed(3)} ms per 59-name x 400-row sweep set -> x201 fits = ${(msPerSweepSet * 201).toFixed(1)} ms/report (sink=${sink})`);
  }

  // DMAP pool bound: draws-map get+push per (draw, name).
  {
    const names = Array.from({ length: 59 }, (_, i) => `name-${i}`);
    let sink = 0;
    const reps = 300;
    const t0 = performance.now();
    for (let rep = 0; rep < reps; rep++) {
      const map = new Map<string, number[]>();
      for (let draw = 0; draw < 200; draw++) {
        for (const name of names) pushValueBare(map, name, draw * 0.001);
      }
      sink += map.size;
    }
    const msPerReport = (performance.now() - t0) / reps;
    out(`probe DMAP: ${msPerReport.toFixed(3)} ms/report for 200x59 map get+push (sink=${sink})`);
  }
}

function pushValueBare(map: Map<string, number[]>, key: string, value: number): void {
  const list = map.get(key);
  if (list === undefined) map.set(key, [value]);
  else list.push(value);
}

/* --------------------------------- main --------------------------------- */

scenarioEquivalence();
scenarioDeterminism();
scenarioProfile();
scenarioProbes();

if (failures > 0) {
  process.stderr.write(`${failures} CHECK(S) FAILED\n`);
  process.exitCode = 1;
} else {
  out(`\nALL EQUIVALENCE CHECKS PASSED (${checksPassed} bitwise checks)`);
}
```

MORE_OPTIMA=no
BRANCH=cursor/r11-c-offline-routing-eleventh-pass-83a1
