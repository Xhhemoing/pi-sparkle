MODEL_SLUG=claude-fable-5-thinking-xhigh

# R11-H：`src/evaluation/` + `src/requirement/` + `src/review/` + `src/rubric/` 第十一遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 11 / R11-H
**基线:** `cursor/sota-persistent-opt-83a1` @ `3855ab1`（独占 tip，含 R11-A/B/C/D/E 报告并入；S11-A-1..2 / S11-B-1 / S11-D-1..2 已入排除表；R11-C / R11-E 空枚举未铸 ID）
**分支:** `cursor/r11-h-eval-eleventh-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十一遍枚举为空：无达门槛候选、零新 ID、切片零代码改动、切片维持关闭。**
切片 21 个文件（1750 行 = 655+712+309+74，本轮 `wc -l` 逐目录复核一致；
8+7+4+2 清单逐目录确认）自 R1-H 基线（`fd437a9`）以来**逐字节未变**
（`git diff fd437a9..3855ab1 -- src/{evaluation,requirement,review,rubric}/`
为空，十一遍全程零 diff），且 R10-H 基线（`004aa20`）之后 `src/`
**零提交**（`git log 004aa20..3855ab1 -- src/` 为空——其间 18 个提交
全部为 R10-G/H/I/J 与 R11 各波报告及排除表文档；R11-A/B/C/D/E 全数
无落地）⇒ 生产调用方地图**可证不变**（最强形式），本轮全库 import
交叉检索再次独立确认（值级导入位点与 R3-H…R10-H 完全一致；
`src/evaluation/` 全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、
`src/rubric/` 及 `applyRoutingScoreUpdate`、`buildContractFromSources`
仍零生产调用方——本轮定向 rg 证实切片外 `src/` 对这些导出的值级引用
为零）。按指令在本 VM 重锚「配置态（3 档）× 命令类（6 类）× 执行层
（热/冷）」满格矩阵（§1.3–§1.5，三次独立运行）：热层默认态
**8.5–9.1µs**/run、配置态 A **7.1–8.3µs**、配置态 B **4.1–4.2µs**
（两档配置态三次全部 ≤ 默认态 ⇒ R8-H…R10-H「无配置态悬崖、默认锚点
是保守上界」在本 VM 复现；绝对值居十一轮历史跨 VM 带 3.9–10µs 中上
段）；晋升类 18.1–18.8ns；四个零生产调用方命令类（evaluate
1.09–1.15µs / critique 0.25–0.61µs / pairwise 2.0–2.1µs / reconcile
64–65ns）**全部 ≤2.2µs**——调用图未变下即便未来接线也不达落地线
（数十~数百 ms）；冷层首调默认 1670–1677µs、配置 1629–1644µs
once-per-process（与 R6-H…R10-H 同带，否决类归属不变）。第十一遍在
完整排除表（S1-H-1..9、S2-H-1..7、S3-H-1..4、S4-H-1..3、S5-H-1..3、
S6-H-1..4、S7-H-1..3、S8-H-1..3、S9-H-1..2 及十轮 45+ 处不立 ID 收口）
之上以两个从未用过的完备性轴收口：(a) **私有面完备性审计**——R10-H
对账的是 30+ 个值级导出，本轮对账其对偶面：21 文件全部私有函数/私有
方法/模块级常量逐一核对裁决记录，**零遗漏**（§2.1）；(b) R10-H 导出面
完备性声明的**独立再推导**——本轮从源码重新枚举导出清单与 R10-H §2.1
对表，**未发现其遗漏的任何位点**（§2.2）。结构位置 × 消除机制扫描
找到三处**从未点名**的微观位点（L1 diff-adapter FAIL 路径死
`hasEpisodeOwned`、L2 `coverageMatrixFromTasks` 冗余重存、
L3 `buildContractFromSources` filter+map 融合），全部经理论 + 确定性
仿真（seeded mulberry32，50,000 项等价检查 + 真实规模基准，三次独立
运行）裁决后以既有家族 + 量级淘汰，**无一达到立 ID 门槛**（§2.3）；
另有六处换名重提在候选化之前即被识别拒列（§0）。诚实结论：枚举为空，
不硬凑 ID，与 R10-H / R11-C / R11-E 空枚举收口同型。未重开任何 X* /
S1-* … S10-* / S11-A/B/D-* 条目；S5-H-1 守卫经本轮 diff + 读码双复核
**原样保留**；S8-H-3 全决议快路径**未做任何形态**（含 Q=0 条件跳过
形态——识别为 gated-branch 变体后未候选化）。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/evaluation/`（8 文件：evaluator / delivery-adapter /
  diff-adapter / ownership / precedence / check-adapter / adapters /
  types）、`src/requirement/`（7 文件：normalizer / provenance /
  precedence / extractor / heuristic / coverage / critic）、
  `src/review/`（4 文件：pairwise / self-review / reconcile / critic）、
  `src/rubric/`（2 文件：registry / types）计 21 文件全量第十一遍实际
  读码，未依赖前十轮记忆。上下游
  `track/{clarify,loop,plan,primary-split}.ts`、
  `run/{supervisor,coordinator,flowchart-run}.ts`、
  `adaptation/{promotion,promotion-rules}.ts` 只读取证（§1.2 逐调用点
  引用），一行未改。切片外其余树零触碰。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表 536 行，含
  S11-A-1..2 / S11-B-1 / S11-D-1..2）/ PROGRESS.md / round-11/PLAN.md /
  round-10/PLAN.md + R10-H.md / R9-H … R1-H（R4-H §2 注与 §3.1 本轮
  重点复核，因其 no-ID 收口清单与本轮扫描直接相关）。
- **换名重提检查**（逐条核对，全部未列为新候选、未触碰）：
  - **S5-H-1（必须保留项）**：`requirement/precedence.ts` L24 的
    `if (fast.length === 0 || slow.length === 0) return [];` 分配前
    守卫经本轮读码 + `git diff fd437a9..HEAD --
    src/requirement/precedence.ts`（空）双复核**原样在位、逐字节
    未变**。本轮对该函数零候选、零「优化掉」提案。
  - **S8-H-3（永不重开项）**：`assertCoverageAllowsStart` 未做任何
    全决议快路径（含 resolved-superset 预判、gated 分支条件跳过、
    blocking 预扫描短路全部变形）。本轮扫描曾识别「Q=0 时条件跳过
    gated 拷贝构造」形态——虽然 Q=0 侧无 `default:""` 反例，但它是
    指令明文点名的 **gated-branch 变体**，且其收益上界即 S6-H-3
    已裁决的 21–45ns/run，双重拒列，**未候选化、未仿真、未触碰**。
    `coverage.ts` 本轮零 diff。
  - `heuristic.ts` L48/L64 双正则（`tests?|coverage|qa` ⊃
    `tests?|coverage`）子集关系短路 = **S3-H-3 原条目本体**
    （q-tests 补问子集正则短路，34–45ns 已裁决）+ S7-B-1 子集合取
    删除同族教训。识别为换名后拒列。
  - `checkCoverageGate` 的 `Object.keys` 纯数组提升（不 Set 化）=
    S1-H-1 提升机制真子变体（Set 形态真实 C=2 实测更慢；数组形态仅省
    C−1=1 次 K≤2 键数组物化，ns 级）；hasOwn 换写维持 S4-H-1 枚举性
    发散钉死。拒列。
  - 三个 adapter `evaluate` 的 async→sync 机器消除 = **S7-H-1 机制
    在 adapter 面**（公开 Promise 扩展点签名 X0-4 + 零生产调用方 +
    R9-H 命令类锚点 ≤2.2µs）。拒列。
  - q-scope/q-write 恒假 `questions.some` 守卫跳过与 `isSourced` 的
    `Array.isArray` 守卫删除 = R4-H §2 注 no-ID 收口原条目（对未来
    分支扩展的防御 / 数据面防御），维持不重提。
  - S8-H-1（PairwiseInput 全形状含 comment 双字段未收窄）、
    S9-H-1/S9-H-2（self-review 三函数裁决记录完备）、S1-H-9 /
    S2-H-4 / S3-H-4（changeSetsEqual 三面）、S1-H-8 / S2-H-6
    （registerRubric copy-on-write，别名安全性本轮读码再确认：全部
    写路径整体替换 registry）、S6-H-1..4、S7-H-1..3、S2-H-1/2、
    S3-H-1/2、S4-H-2/3、X4-9 / X0-5 / X0-6：全部维持，零候选。
  - S11-A-1..2 / S11-B-1 / S11-D-1..2：均不在本切片，未触碰、未换名
    重提（S11-D-1 的「键编码单射性守卫消除」与本切片
    `assertNotSelfReview` 恒等守卫无对应关系——后者是活门，非可证死）。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价、双 LCB 与双归因
  保留、阈值/CAS/凭据/数据面契约/公开签名不变、fail-open 零引入、
  测试未改，天然成立。不声称 Outcome-supported，Checkpoint F-PROD
  仍开放（ADR-005）。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，经 nvm 切换
  v22.22.2（与 R1-J §3 / R9-H / R10-H 既知注记一致）；本 VM 额外存在
  `/exec-daemon/node`（v22.14.0）在 PATH 前段遮蔽 nvm bin，测量与
  测试前已显式把 nvm bin 置于 PATH 首并复核 `node --version` =
  v22.22.2。`pnpm install --frozen-lockfile` 后 `pnpm build` /
  `pnpm typecheck` / `pnpm lint` 全绿。

## 1. 基线不变性、调用图复核与满格矩阵重锚

1. **切片逐字节未变**：`git diff fd437a9..3855ab1 --
   src/{evaluation,requirement,review,rubric}/` 输出为空（`--stat` 行数
   0）；十一遍全程零 diff。21 文件 1750 行逐目录计数
   （655/712/309/74）与 R1-H 清单一致。R1-H 逐函数下界表、R2-H 调用图
   上界、R3-H 重复工作枚举、R4-H 三类角度收口、R5-H 三层级收口、
   R6-H 执行层级收口、R7-H 机器成本三面收口、R8-H 配置态矩阵收口、
   R9-H 命令类矩阵满格 + 逃逸分析地板、R10-H 导出面完备性审计与全部
   S*-H-* 排除继承有效。
2. **调用图可证不变（最强形式）**：`git log 004aa20..3855ab1 -- src/`
   **为空**——R10-H 基线以来 18 个提交全部是 `docs/reports/` 文档
   （R10-G/H/I/J 报告、R11 全部已回收波次报告与排除表并入；
   R11-A/B/C/D/E 全数无代码落地）⇒ 生产调用方地图数学上不变。本轮
   全库 import 检索双确认（逐文件读码取证）：
   `assertCoverageAllowsStart` ←
   `run/supervisor.ts` L498-500 / `run/coordinator.ts` L224-226 /
   `run/flowchart-run.ts` L757-759（每 run 启动一次，且仅当
   `input.contract !== undefined`）；`extractHeuristicContract` ←
   `track/clarify.ts` L37（每 run 一次）；`applyPrecedence` ←
   `track/loop.ts` L80（每 run 一次，`"user-first"`）；`shouldScout` ←
   `track/plan.ts` L42（每 run 一次）；`assertCanPromoteFromReview` ←
   `adaptation/promotion-rules.ts` L81（每晋升一次）；其余全部为类型
   导入（`adaptation/promotion.ts` 的 ReviewerKind、
   `track/primary-split.ts` 与 `track/clarify.ts` 的 HeuristicHabits /
   ContractCandidate）。切片外 `src/` 对
   `runBlindPairwisePair` / `createEvaluationRecord` /
   `reconcileReviews` / `createCriticObservation` /
   `applyRoutingScoreUpdate` / `buildContractFromSources` /
   `registerRubric` / `classifyDiffScope` / `selectHighestPrecedence`
   等其余值级导出的引用为零（定向 rg，切片自身与 `test/` 除外）。
3. **热层三态锚点重锚**（三次运行，本 VM，Node 22.22.2）：

```text
CEILING default  (9-pass anchor shape, Q=0):        9085 / 8801 / 8518 ns/run
CEILING configured A (habits 三键全设, Q=1 gated):   8277 / 7958 / 7073 ns/run
CEILING configured B (vague objective, Q=3 gated):   4164 / 4108 / 4067 ns/run
CEILING promotion class: assertCanPromoteFromReview = 18.1 / 18.8 / 18.3 ns/promotion
```

   两档配置态三次全部 ≤ 默认态（gated 重映射真实执行：production
   assume-defaults 流恒传覆盖全部问题的 `resolvedQuestionIds`）——
   R8-H…R10-H「无配置态悬崖、默认锚点是保守上界」在本 VM 复现。绝对值
   居十一轮历史跨 VM 带（3.9–10µs）中上段，属测量环境差异而非调用图
   变更（§1.2 已证零变更）；量级结论不变：整个热面在 µs 级，低于落地
   线 **3–4 个数量级**。
4. **命令类矩阵剩余格重锚**（R9-H 首锚的四个零生产调用方类，本轮三次
   复测）：

```text
CEILING evaluate class  (TEST-ONLY): createEvaluationRecord(3 criteria) = 1109 / 1145 / 1089 ns/call
CEILING critique class  (chain 内):  critiqueContract(real contract)    =  251 /  249 /  614 ns/call
CEILING pairwise class  (TEST-ONLY): runBlindPairwisePair               = 2130 / 2054 / 2021 ns/call
CEILING reconcile class (TEST-ONLY): reconcileReviews(n=2)              =   64 /   65 /   64 ns/call
```

   与 R9-H/R10-H 锚点带一致或更低（critique 三次中两次 ~250ns、一次
   614ns 与历史带 611–637ns 重合——JIT 分层状态抖动，量级类属不变）。
   指令要求的判据成立：**调用图未变下即便把这四个面全部接线，当前
   规模每类 ≤2.2µs，距落地线仍差 4+ 个数量级**。「配置态（3 档）×
   命令类（6 类）× 执行层（热/冷）」矩阵在本 VM 无未测格。
5. **冷层锚点复测（默认 + 配置双态）**：dist 产物、新进程、模块加载
   预扣除后，三生产入口首调合计中位数三次：默认态
   **1677 / 1671 / 1670 µs**（区间 [1653, 1962]µs）、配置态
   **1632 / 1644 / 1629 µs**（区间 [1607, 1687]µs）——与 R6-H
   1.65–1.71ms / R7-H 1.72–1.76ms / R8-H 1.6–1.7ms / R9-H 1.6–1.8ms /
   R10-H 1.6–1.7ms 同带；once-per-process 否决类归属不变（S7-H-3
   已证该层无净正对策）。

## 2. 第十一遍枚举：私有面完备性 + 导出面独立复审 + 三处新位点裁决 → 诚实空结果

### 2.1 私有面完备性审计（R10-H 导出面对账的对偶，本轮新轴）

R10-H 按**导出面**对账 30+ 个值级导出；第十一遍补上其对偶：21 文件
全部**非导出**函数/私有方法/模块级常量逐一核对裁决记录。结果：
**零遗漏**——切片的可优化面（导出 + 私有）至此双向闭合。

| 私有面 | 裁决记录 |
| --- | --- |
| `normalizer.ts` assertOriginMatchesKind / defaultOrigin / authorityFor | S2-H-7（默认 origin 守卫维持防御纵深）+ X1-4/S1-A-8 表族（if 链换表拒列） |
| `normalizer.ts` extractSignals / TRUSTED_SOURCE | R1-H 三正则 no-ID + S7-H-2 活契约（生产组合死输出但对任意 extractor 是活契约）；symbol 为模块常量零每调用成本 |
| `heuristic.ts` shouldAskScope / SMALLEST_CHANGE / DEFAULT_NON_GOALS / PATH_RE | S1-H-3（namedTargets/shouldScout 去重）+ S3-H-3（子集正则短路）；模块常量；PATH_RE lastIndex 安全性 R10-H §2.2-2 取证 |
| `extractor.ts` validateConfidence / assertAuthorityGrounding | O(1)（R1-H 下界表）；S1-H-4（find→Map 重复 authorityIndex 发散）+ S2-H-2（空 authority 早退 44ns 拒列） |
| `requirement/precedence.ts` isFastCheck / isSlowCheck | R6-H toLowerCase CSE no-ID + S5-H-1（保留守卫）/ S6-H-1（序贯守卫拒列）两面 |
| `provenance.ts` isSourced | R4-H no-ID：Array.isArray 运行时守卫为数据面防御 |
| `check-adapter.ts` isCommandResult / hashArtifact / changeSetsEqual / attributionMetadata / CHECK_DECLARATION | R10-H §2.2-5 合取序；S5-H-3；S1-H-9+S2-H-4+S3-H-4 三面钉死；R9-H/R10-H 全分支活性复核（本轮读码再确认：metadata 在全部到达路径被消费）；模块常量 |
| `delivery-adapter.ts` / `diff-adapter.ts` isDeliveryEvidence / isDiffScope / 声明常量 | R10-H §2.2-5 合取序 + R4-H 分支序即归因契约；本轮新检视 L1（§2.3） |
| `ownership.ts` DEFAULT_RULES | R4-H 顺序即分类语义 + X4-9 |
| `pairwise.ts` comparePresented / presentInput / remapToOriginalIds | R1-H remap "ab" 冗余展开 no-ID + S8-H-1 三重钉死（输入形状未收窄） |
| `self-review.ts` assertNotSelfReview | S9-H-1（逃逸分析地板）/ S9-H-2（晋升频次墙）+ S8-A-3 形状二态伪影类 |
| `rubric/registry.ts` DEFAULT_REGISTRY / 模块级 registry | S1-H-8 反例 + R9-H 别名安全复核（copy-on-write 纪律下 `{...DEFAULT_REGISTRY}` 浅拷贝安全，本轮读码再确认成立） |

### 2.2 R10-H 导出面完备性声明的独立复审

按指令「找到 R10-H 漏掉的位点，或写诚实空枚举」：本轮不信任 R10-H
表格，从 21 文件源码**重新枚举**全部值级导出（coverage 4 + heuristic
8 + extractor 1 + normalizer 3 + requirement/critic 1 +
requirement/precedence 2 + provenance 1 + evaluator 3 + check-adapter
2 + delivery-adapter 2 + diff-adapter 2 + ownership 1 +
evaluation/precedence 4 + evaluation/types 1 + pairwise 2 + reconcile
1 + review/critic 1 + self-review 2 + registry 4 + rubric/types 1），
逐一与 R10-H §2.1 对表：**每一项都在其表内且裁决记录仍然成立，未
发现遗漏位点**。R10-H 的完备性声明经独立再推导后维持。

### 2.3 结构位置 × 消除机制扫描：三处从未点名位点，全部不立 ID

第十一遍的主动搜索轴：结构位置（入口守卫 / 循环体 / 返回构造 /
跨调用重复 / 数据字面量 / 正则求值 / 防御校验 / 拷贝语义 / 异步机器 /
模块加载）× 消除机制（死码 / CSE / 融合 / 快路径 / 记忆化 / 表示
变换 / 去糖 / 惰性 / 可变化 / 复杂度类）全格对账。绝大多数格由既有
ID / no-ID 收口直接覆盖（§0 换名重提检查 + §2.1 私有面表）；剩余
三格是**从未被任何轮次点名**的位点，本轮全部按门槛走完
理论 + 仿真 + 基准（附录脚本，seeded，50,000 项等价检查三次运行全
通过），逐一淘汰：

| # | 位点 | 理论 | 仿真等价 | 真实规模基准（三次） | 裁决 |
| --- | --- | --- | --- | --- | --- |
| L1 | `diff-adapter.ts` `hasEpisodeOwned` 在 FAIL 早退之前计算——FAIL 路径上可证死（仅 `.length > 0` 布尔化，零分配） | 死计算下沉过 FAIL return | ✅ 20,000 组 fuzz（四数组空/非空全格）载荷逐字节一致；replica-vs-production sanity 闸先行零失配 | **−0.6 / −0.1 / +0.5ns**——符号翻转纯抖动 | 淘汰三重：S3-D-5 死分配下沉家族（本例连分配都没有，只有一次属性读+比较）；面零生产调用方（R9-H 矩阵 ≤2.2µs 天花板）；V8 布尔化即寄存器值，源码删除零可测收益（S9-H-1 逃逸分析地板同象） |
| L2 | `coverage.ts` `coverageMatrixFromTasks` 内循环 `requirementToTasks[criterion.id] = owners` 无条件重存——`owners` 从记录读回时是同一引用，重存冗余 | 首插才存（`existing === undefined` 分支化） | ✅ 20,000 组 fuzz（1–6 任务 × 准则随机子集 × 幽灵准则）JSON 逐字节一致；replica sanity 闸零失配 | **53.9 / 52.5 / 42.0ns**/call（方向稳定）；生产整函数 411–413ns | 淘汰：S3-E-3（分组循环冗余 Map.set 消除，±8ns 抖动拒列）/ S7-J-4（rebuildViews 冗余 Map.set 条件化拒列）同机制家族在 Record 位点；频次每 run 一次（run-start 门内），42–54ns 占默认态链 ~0.5%，低于落地线 **6 个数量级**；且该门是 S8-H-3 毗邻区，无收益不动 |
| L3 | `normalizer.ts` `buildContractFromSources` 的 `filter().map()` 两遍 → 单遍融合 | 免一次中间数组 + 一遍迭代 | ✅ 10,000 组 fuzz（1–4 源 × 信号词组合）合同 JSON 逐字节一致（`acc-${n}` 序号在融合下保持一致性已证） | **12.8 / 16.2 / 0.1ns**/call——第三次归零，抖动 | 淘汰：S1-C-5 / S5-J-6 融合家族（小数组融合无稳定收益的第 N 例）；面自 R1-H 起十一轮零生产调用方（本轮 rg 复核）；R9-H 矩阵前瞻上界已覆盖 |

三处合计即便全部落地且全部接入生产热路径，上界 <70ns/run——比落地线
低 6 个数量级。**不立任何 S11-H-* ID**（与 R10-H / R11-C / R11-E 空
枚举收口同型：no-ID 收口记录在本报告即可，防止后续轮次重新发现）。

### 2.4 收口陈述

十一遍合计：单函数、函数间、跨模块、调用图、模块图、执行层级、调用
机器、配置态、命令类、编译器地板、导出面完备性、**私有面完备性**
十二个面全部有实测锚点或对账记录；导出+私有双向闭合后，本切片在
当前调用图下**结构上不存在**达门槛候选。R10-H 总门槛维持并按本 VM
数字刷新：任何未来候选须先推翻满格矩阵实测上界——热层默认
**8.5–9.1µs/run**（本 VM；配置态更低 ⇒ 默认锚点是保守上界）、四个零
调用方命令类 ≤2.2µs、冷层 **1.6–1.7ms once-per-process**（否决类）。
重开该切片的唯一前提维持 R4-H…R10-H 收口原文：调用图变更
（evaluation/review/rubric 面接入每 turn 热路径，或合同规模增长
≥2 个量级）。

## 3. 逐文件收口（第十一遍新视角，其余与 R1-H…R10-H 一致）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `requirement/coverage.ts` | **L2 冗余重存裁决（§2.3，42–54ns 淘汰）**；Q=0 gated 拷贝条件跳过识别为 S6-H-3/S8-H-3 gated-branch 变体未候选化（§0）；**S8-H-3 零形态复核**；S1-H-1/2、S4-H-1、S6-H-2/3 维持 | 无新候选 |
| `requirement/precedence.ts` | **S5-H-1 守卫在位复核（L24 早退，零 diff）**；S1-H-5、S4-H-2、S6-H-1 维持 | 无新候选 |
| `requirement/heuristic.ts` | L48/L64 子集正则短路 = S3-H-3 换名识别（§0）；q-scope/q-write 恒假守卫 R4-H no-ID 维持；S3-H-3、S6-H-4、S7-H-1、S8-H-2 维持 | 无新候选 |
| `requirement/normalizer.ts` | **L3 融合裁决（§2.3，抖动级淘汰）**；S2-H-7、S7-H-2、R5-H slice no-ID、R10-H SlicedString 取证维持 | 无新候选 |
| `requirement/extractor.ts` / `critic.ts` / `provenance.ts` | 私有面对账（validateConfidence / assertAuthorityGrounding / isSourced，§2.1）；S2-H-1/2、S3-H-1/2、S4-H-2 维持 | 无新候选 |
| `evaluation/diff-adapter.ts` | **L1 死布尔化裁决（§2.3，符号翻转淘汰）**；R10-H §2.2-5 合取序维持 | 无新候选 |
| `evaluation/check-adapter.ts` / `delivery-adapter.ts` | 私有面对账（§2.1）；changeSetsEqual 三面 + S5-H-3 + attributionMetadata 活性再确认维持 | 无新候选 |
| `evaluation/evaluator.ts` / `ownership.ts` / `precedence.ts` / `adapters.ts` / `types.ts` | S1-H-7、R5-H evidence 双读 no-ID、X4-9、S2-H-5、纯类型面维持；adapter async 面 = S7-H-1 换名识别（§0） | 无新候选 |
| `review/pairwise.ts` / `reconcile.ts` / `critic.ts` / `self-review.ts` | 私有面对账（comparePresented / presentInput / remapToOriginalIds / assertNotSelfReview，§2.1）；S8-H-1、S1-H-6、S9-H-1/2 维持 | 无新候选 |
| `rubric/registry.ts` / `types.ts` | DEFAULT_REGISTRY 别名安全第三次复核成立；S1-H-8 反例 + S2-H-6 + R10-H createRubric 时间戳取证维持 | 无新候选 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。无仿真脚本入库（无赢家，遵循
round01–10 仅赢家 sim 入库惯例；完整脚本见附录）。

## 5. 测试

零代码改动下切片相关套件基线复核，全绿（Node 22.22.2 via nvm，满足
engines >=22.19.0；系统 Node 22.14.0 过低 + `/exec-daemon/node` PATH
遮蔽的环境注记见 §0；`pnpm install --frozen-lockfile` 后 `pnpm build` /
`pnpm typecheck` / `pnpm lint` 全绿）：

```bash
npx tsx --test "test/unit/requirement/*.test.ts" "test/unit/evaluation/*.test.ts" \
  "test/unit/review/*.test.ts" "test/integration/m3/checkpoint-d.test.ts" \
  "test/integration/m3/coverage-gate.test.ts" \
  "test/integration/m3/requirement-extraction.test.ts" \
  "test/integration/m4/delivery-evidence.test.ts"
# tests 93 / suites 13 / pass 93 / fail 0（与 R1-H…R10-H 同套件同计数）
```

仿真 + 锚点重锚（临时脚本 `/tmp/r11h-sim.mts`，无赢家故未入库；完整
源码见附录，seed 固定可复现；冷层探针以 `pnpm build` 后 dist 产物为
准）代表性一次运行：

```text
L1 bench diff-adapter dead hasEpisodeOwned on FAIL path: replica=8.0ns candidate=8.7ns delta=-0.6ns/call (face has ZERO production callers)
L2 bench coverageMatrixFromTasks redundant re-store: replica=402ns candidate=348ns delta=53.9ns/call production=413ns (once per run)
L3 bench buildContractFromSources fusion: two-pass=566ns fused=553ns delta=12.8ns/call (face has ZERO production callers)
CEILING default (9-pass anchor shape, Q=0): extractHeuristicContract=8189ns + run-start gate(Q=0, no options)=650ns + applyPrecedence=246ns = 9085ns once-per-run production total
CEILING configured A (habits set, Q=1 gated): extractHeuristicContract=7401ns + run-start gate(Q=1, all resolved)=660ns + applyPrecedence=217ns = 8277ns once-per-run production total
CEILING configured B (vague, Q=3 gated): extractHeuristicContract=3569ns + run-start gate(Q=3, all resolved)=480ns + applyPrecedence=115ns = 4164ns once-per-run production total
CEILING promotion class: assertCanPromoteFromReview=18.1ns once per promotion (production)
CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=1109ns/call
CEILING critique class (inside run-start chain): critiqueContract(real contract)=251ns/call
CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=2130ns/call
CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=64ns/call
COLD first-call production total (dist, fresh process, default): median=1677µs range=[1675, 1962]µs — once per PROCESS (veto class)
COLD first-call production total (dist, fresh process, configured (habits + Q=1 gated)): median=1632µs range=[1607, 1684]µs — once per PROCESS (veto class)

ALL EQUIVALENCE CHECKS PASSED (deterministic, seeded)
```

三次独立运行 50,000 项等价检查全部通过、裁决结论逐位一致；计时抖动内
方向一致或识别为符号翻转（L1 三次 −0.6/−0.1/+0.5ns 符号翻转＝零收益；
L2 三次 53.9/52.5/42.0ns 方向稳定但量级淘汰；L3 三次 12.8/16.2/0.1ns
第三次归零＝抖动；默认锚点三次 9085/8801/8518ns、配置 A 三次
8277/7958/7073ns、配置 B 三次 4164/4108/4067ns；晋升类三次
18.1/18.8/18.3ns；evaluate 三次 1109/1145/1089ns、critique
251/249/614ns、pairwise 2130/2054/2021ns、reconcile 64/65/64ns；冷层
默认三次 1677/1671/1670µs、配置三次 1632/1644/1629µs）。

## 6. 新增排除 ID

**无。** 第十一遍枚举为空——私有面完备性审计零遗漏（§2.1）、R10-H
导出面完备性独立复审零遗漏（§2.2）、三处从未点名微观位点全部以既有
家族 + 量级 + 仿真数字淘汰（§2.3），不硬凑 ID。与 R10-H / R11-C /
R11-E 的空枚举收口同型。

## 附录：确定性仿真 + 锚点脚本（完整，可复现）

运行方式：`pnpm build` 后保存为任意 `.mts` 并 `npx tsx <file>`（仓库
根目录，依赖已装；冷层探针需要 dist 产物存在）。seeds：
`0xB11801`–`0xB11803`。无赢家，脚本仅做三处 loser 裁决 + 满格矩阵
重锚，按惯例不入库。

```ts
/**
 * R11-H deterministic anchor re-verification + loser-probe adjudication
 * (eleventh pass).
 *
 * No fresh candidate survived enumeration (empty eleventh group), so this
 * script (a) re-establishes, on THIS VM, the full measurement matrix that
 * R8-H/R9-H/R10-H closed — hot tier three states (default Q=0 / configured A
 * habits+Q=1 gated / configured B vague+Q=3 gated; the production
 * assume-defaults track flow always passes resolvedQuestionIds covering ALL
 * questions so the gated re-map materially executes), promotion class, the
 * four zero-production-caller command classes (evaluate / critique /
 * pairwise / reconcile), and cold-tier once-per-process first-call totals on
 * dist (default AND configured, 5 fresh processes each) — and (b) puts
 * honest seeded-fuzz + bench numbers on the three never-point-named
 * micro-sites found by the eleventh structure-position × elimination-
 * mechanism scan, all three of which are REJECTED (family + magnitude):
 *
 *   L1: diff-adapter.ts `hasEpisodeOwned` is computed before the
 *       FAIL branch where it is dead (dead-computation sink family,
 *       S3-D-5; face has zero production callers).
 *   L2: coverage.ts `coverageMatrixFromTasks` re-stores
 *       `requirementToTasks[criterion.id] = owners` even when the array was
 *       already present (redundant-store family, S3-E-3 / S7-J-4).
 *   L3: normalizer.ts `buildContractFromSources` filter().map() two-pass →
 *       single-pass fusion (fusion family, S1-C-5 / S5-J-6; zero production
 *       callers).
 *
 * Reference = production imports wherever the function is exported; private
 * helpers are replicated verbatim and each candidate differs from its
 * replica ONLY by the candidate edit. Replica-vs-production sanity gates
 * precede every adjudication. Benchmarks keep reference and candidate on
 * separate function identities and interleave three rounds to blunt PIC
 * shape-pollution artifacts (S8-A-3 / S8-E-2 / S8-H-1 lesson).
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xB11801..0xB11803.
 */
import { performance } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import {
  extractHeuristicContract,
  type HeuristicHabits
} from "/workspace/src/requirement/heuristic.js";
import {
  assertCoverageAllowsStart,
  coverageMatrixFromTasks,
  type CoverageTaskRef,
  type CoverageStartOptions
} from "/workspace/src/requirement/coverage.js";
import { applyPrecedence } from "/workspace/src/requirement/precedence.js";
import { critiqueContract } from "/workspace/src/requirement/critic.js";
import { assertCanPromoteFromReview } from "/workspace/src/review/self-review.js";
import { runBlindPairwisePair, type PairwiseInput } from "/workspace/src/review/pairwise.js";
import { reconcileReviews } from "/workspace/src/review/reconcile.js";
import { createEvaluationRecord } from "/workspace/src/evaluation/evaluator.js";
import { createRubric } from "/workspace/src/rubric/types.js";
import { DiffAdapter } from "/workspace/src/evaluation/diff-adapter.js";
import { buildContractFromSources, type RawSource } from "/workspace/src/requirement/normalizer.js";
import type { RequirementContract, CoverageMatrix } from "/workspace/src/domain/contract.js";
import type { AdapterContext } from "/workspace/src/evaluation/adapters.js";
import type { EpisodeId, TaskId } from "/workspace/src/domain/ids.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

/* ================================================================
 * L1: diff-adapter dead-on-FAIL-path `hasEpisodeOwned`.
 * Replica of DiffAdapter.evaluate's classification body (verbatim) vs a
 * candidate that defers `hasEpisodeOwned` past the FAIL return. Face has
 * zero production callers (R9-H §1.2 grep, re-verified this round).
 * ================================================================ */
interface DiffScopeLike {
  readonly episodeOwned: readonly string[];
  readonly unrelatedUser: readonly string[];
  readonly generated: readonly string[];
  readonly unknown: readonly string[];
}
type Verdict = { outcome: string; reason?: string; meta?: Record<string, number> };
function replicaDiffBody(scope: DiffScopeLike, revision: string): Verdict {
  const hasUnrelated = scope.unrelatedUser.length > 0;
  const hasUnknown = scope.unknown.length > 0;
  const hasEpisodeOwned = scope.episodeOwned.length > 0; // computed before FAIL return
  if (hasUnrelated || hasUnknown) {
    return {
      outcome: "FAIL",
      reason: hasUnrelated ? "unrelated user changes detected" : "unknown ownership files detected",
      meta: {
        episodeOwned: scope.episodeOwned.length,
        unrelated: scope.unrelatedUser.length,
        generated: scope.generated.length,
        unknown: scope.unknown.length
      }
    };
  }
  if (!hasEpisodeOwned) {
    return { outcome: "UNOBSERVED", reason: "no episode-owned changes" };
  }
  return {
    outcome: "PASS",
    reason: `diff:${revision}`,
    meta: { episodeOwned: scope.episodeOwned.length, generated: scope.generated.length }
  };
}
function candidateDiffBody(scope: DiffScopeLike, revision: string): Verdict {
  const hasUnrelated = scope.unrelatedUser.length > 0;
  const hasUnknown = scope.unknown.length > 0;
  if (hasUnrelated || hasUnknown) {
    return {
      outcome: "FAIL",
      reason: hasUnrelated ? "unrelated user changes detected" : "unknown ownership files detected",
      meta: {
        episodeOwned: scope.episodeOwned.length,
        unrelated: scope.unrelatedUser.length,
        generated: scope.generated.length,
        unknown: scope.unknown.length
      }
    };
  }
  const hasEpisodeOwned = scope.episodeOwned.length > 0; // sunk past the FAIL return
  if (!hasEpisodeOwned) {
    return { outcome: "UNOBSERVED", reason: "no episode-owned changes" };
  }
  return {
    outcome: "PASS",
    reason: `diff:${revision}`,
    meta: { episodeOwned: scope.episodeOwned.length, generated: scope.generated.length }
  };
}
{
  const rng = mulberry32(0xb11801);
  const adapter = new DiffAdapter();
  const context: AdapterContext = {
    episodeId: "ep_00000001" as EpisodeId,
    workingDirectory: "/w",
    revision: "rev-1",
    changeSet: ["a.ts"]
  };
  const files = ["a.ts", "b.ts", "c.md", "d.json"];
  for (let trial = 0; trial < 20000; trial += 1) {
    const mk = (): string[] => (rng() < 0.5 ? [] : [pick(rng, files)]);
    const scope: DiffScopeLike = {
      episodeOwned: mk(),
      unrelatedUser: mk(),
      generated: mk(),
      unknown: mk()
    };
    const prod = await adapter.evaluate(context, scope);
    const rep = replicaDiffBody(scope, context.revision);
    const cand = candidateDiffBody(scope, context.revision);
    check(
      "L1 sanity gate (replica outcome parity vs production)",
      prod.outcome === rep.outcome,
      `trial ${trial}: prod=${prod.outcome} rep=${rep.outcome}`
    );
    check(
      "L1 equivalence (deferred hasEpisodeOwned)",
      JSON.stringify(rep) === JSON.stringify(cand),
      `trial ${trial}`
    );
  }
  // Bench the FAIL shape (only shape where the candidate differs).
  const failScope: DiffScopeLike = {
    episodeOwned: ["a.ts"],
    unrelatedUser: ["b.ts"],
    generated: [],
    unknown: []
  };
  let cur = 0;
  let cand = 0;
  for (let round = 0; round < 3; round += 1) {
    cur += bench(() => void replicaDiffBody(failScope, "rev-1"), 200000);
    cand += bench(() => void candidateDiffBody(failScope, "rev-1"), 200000);
  }
  cur /= 3;
  cand /= 3;
  log(
    `L1 bench diff-adapter dead hasEpisodeOwned on FAIL path: replica=${(cur * 1e6).toFixed(1)}ns candidate=${(cand * 1e6).toFixed(1)}ns delta=${((cur - cand) * 1e6).toFixed(1)}ns/call (face has ZERO production callers)`
  );
}

/* ================================================================
 * L2: coverageMatrixFromTasks redundant re-store. The inner loop does
 * `requirementToTasks[criterion.id] = owners` even when `owners` was read
 * back from the record (same reference). Candidate stores only on first
 * insertion. Production frequency: once per run inside the run-start gate.
 * ================================================================ */
function replicaMatrixFromTasks(contract: RequirementContract, tasks: readonly CoverageTaskRef[]): CoverageMatrix {
  const contractIds = new Set(contract.acceptanceCriteria.map((criterion) => criterion.id));
  const requirementToTasks: Record<string, TaskId[]> = {};
  const taskToChecks: Record<string, string[]> = {};
  for (const task of tasks) {
    const mapped: string[] = [];
    for (const criterion of task.acceptanceCriteria) {
      if (!contractIds.has(criterion.id)) continue;
      mapped.push(criterion.id);
      const owners = requirementToTasks[criterion.id] ?? [];
      owners.push(task.id);
      requirementToTasks[criterion.id] = owners; // unconditional re-store
    }
    taskToChecks[task.id] = mapped;
  }
  return {
    contractVersion: contract.schemaVersion,
    requirementToTasks,
    taskToChecks,
    orphanRequirements: []
  };
}
function candidateMatrixFromTasks(contract: RequirementContract, tasks: readonly CoverageTaskRef[]): CoverageMatrix {
  const contractIds = new Set(contract.acceptanceCriteria.map((criterion) => criterion.id));
  const requirementToTasks: Record<string, TaskId[]> = {};
  const taskToChecks: Record<string, string[]> = {};
  for (const task of tasks) {
    const mapped: string[] = [];
    for (const criterion of task.acceptanceCriteria) {
      if (!contractIds.has(criterion.id)) continue;
      mapped.push(criterion.id);
      const existing = requirementToTasks[criterion.id];
      if (existing === undefined) {
        requirementToTasks[criterion.id] = [task.id]; // store only on first insertion
      } else {
        existing.push(task.id);
      }
    }
    taskToChecks[task.id] = mapped;
  }
  return {
    contractVersion: contract.schemaVersion,
    requirementToTasks,
    taskToChecks,
    orphanRequirements: []
  };
}
{
  const rng = mulberry32(0xb11802);
  const prodCandidate = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const contract = prodCandidate.contract;
  const criterionIds = contract.acceptanceCriteria.map((criterion) => criterion.id);
  for (let trial = 0; trial < 20000; trial += 1) {
    const taskCount = 1 + Math.floor(rng() * 6);
    const tasks: CoverageTaskRef[] = Array.from({ length: taskCount }, (_, i) => ({
      id: `tsk_0000000${i}` as TaskId,
      acceptanceCriteria: criterionIds
        .filter(() => rng() < 0.7)
        .concat(rng() < 0.3 ? ["ac-ghost"] : [])
        .map((id) => ({ id }))
    }));
    const prod = coverageMatrixFromTasks(contract, tasks);
    const rep = replicaMatrixFromTasks(contract, tasks);
    const cand = candidateMatrixFromTasks(contract, tasks);
    check(
      "L2 sanity gate (verbatim replica parity vs production)",
      JSON.stringify(prod) === JSON.stringify(rep),
      `trial ${trial}`
    );
    check(
      "L2 equivalence (first-insertion-only store)",
      JSON.stringify(rep) === JSON.stringify(cand),
      `trial ${trial}`
    );
  }
  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: criterionIds.map((id) => ({ id }))
  }));
  let cur = 0;
  let cand = 0;
  for (let round = 0; round < 3; round += 1) {
    cur += bench(() => void replicaMatrixFromTasks(contract, tasks), 100000);
    cand += bench(() => void candidateMatrixFromTasks(contract, tasks), 100000);
  }
  cur /= 3;
  cand /= 3;
  const prodT = bench(() => void coverageMatrixFromTasks(contract, tasks), 100000);
  log(
    `L2 bench coverageMatrixFromTasks redundant re-store: replica=${(cur * 1e6).toFixed(0)}ns candidate=${(cand * 1e6).toFixed(0)}ns delta=${((cur - cand) * 1e6).toFixed(1)}ns/call production=${(prodT * 1e6).toFixed(0)}ns (once per run)`
  );
}

/* ================================================================
 * L3: buildContractFromSources filter().map() → single-pass fusion.
 * Face has zero production callers (grep re-verified this round).
 * ================================================================ */
function candidateBuildFused(objective: string, sources: RawSource[]): RequirementContract {
  // Mirrors buildContractFromSources but fuses the acceptance filter+map.
  // normalizeSources itself is the production export, reused verbatim.
  return buildFusedInner(objective, sources);
}
import { normalizeSources } from "/workspace/src/requirement/normalizer.js";
import { validateRequirementContract } from "/workspace/src/domain/contract.js";
function buildFusedInner(objective: string, sources: RawSource[]): RequirementContract {
  const normalized = normalizeSources(sources);
  const acceptanceCriteria: { id: string; description: string; observableCheck: string }[] = [];
  for (const n of normalized) {
    if (!n.signals.includes("acceptance")) continue;
    acceptanceCriteria.push({
      id: `acc-${acceptanceCriteria.length + 1}`,
      description: n.text.slice(0, 120),
      observableCheck: "manual-or-test"
    });
  }
  const contract = {
    schemaVersion: 1 as const,
    objective,
    deliverables: [],
    constraints: [],
    nonGoals: [],
    acceptanceCriteria,
    assumptions: [],
    questions: [],
    authority: [],
    sourceRefs: normalized.map((n) => n.ref)
  };
  return validateRequirementContract(contract);
}
{
  const rng = mulberry32(0xb11803);
  const texts = [
    "the change must pass all tests",
    "avoid touching unrelated files",
    "verify the fix works end to end",
    "some plain descriptive text",
    "never break the public api and always test"
  ];
  for (let trial = 0; trial < 10000; trial += 1) {
    const n = 1 + Math.floor(rng() * 4);
    const sources: RawSource[] = Array.from({ length: n }, (_, i) => ({
      kind: "message" as const,
      ref: `src-${i}`,
      content: pick(rng, texts)
    }));
    const ref = buildContractFromSources("obj", sources.map((s) => ({ ...s })));
    const cand = candidateBuildFused("obj", sources.map((s) => ({ ...s })));
    check(
      "L3 equivalence (fused acceptance build)",
      JSON.stringify(ref) === JSON.stringify(cand),
      `trial ${trial}`
    );
  }
  const sources: RawSource[] = Array.from({ length: 4 }, (_, i) => ({
    kind: "message" as const,
    ref: `src-${i}`,
    content: texts[i % texts.length] as string
  }));
  let cur = 0;
  let cand = 0;
  for (let round = 0; round < 3; round += 1) {
    cur += bench(() => void buildContractFromSources("obj", sources), 20000);
    cand += bench(() => void candidateBuildFused("obj", sources), 20000);
  }
  cur /= 3;
  cand /= 3;
  log(
    `L3 bench buildContractFromSources fusion: two-pass=${(cur * 1e6).toFixed(0)}ns fused=${(cand * 1e6).toFixed(0)}ns delta=${((cur - cand) * 1e6).toFixed(1)}ns/call (face has ZERO production callers)`
  );
}

/* ================================================================
 * Hot tier, three states (R8-H/R9-H/R10-H anchor shapes).
 * ================================================================ */
async function measureChain(label: string, objective: string, habits: HeuristicHabits | undefined): Promise<void> {
  const args = { objective, ...(habits !== undefined ? { habits } : {}) };
  const chain = await benchAsync(async () => {
    await extractHeuristicContract(args);
  }, 3000);
  const prod = await extractHeuristicContract(args);
  const real = prod.contract;
  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
  const options: CoverageStartOptions | undefined =
    real.questions.length > 0
      ? { resolvedQuestionIds: real.questions.map((question) => question.id) }
      : undefined;
  const gate = bench(() => assertCoverageAllowsStart(real, tasks, options), 50000);
  const precedence = bench(() => void applyPrecedence(real, "user-first"), 100000);
  const total = chain + gate + precedence;
  log(
    `CEILING ${label}: extractHeuristicContract=${(chain * 1e6).toFixed(0)}ns + run-start gate(Q=${real.questions.length}${options !== undefined ? ", all resolved" : ", no options"})=${(gate * 1e6).toFixed(0)}ns + applyPrecedence=${(precedence * 1e6).toFixed(0)}ns = ${(total * 1e6).toFixed(0)}ns once-per-run production total`
  );
}
{
  await measureChain(
    "default (9-pass anchor shape, Q=0)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    undefined
  );
  await measureChain(
    "configured A (habits set, Q=1 gated)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    { requireTests: true, preferReview: false, askBeforeWrite: true }
  );
  await measureChain("configured B (vague, Q=3 gated)", "fix bug please", { askBeforeWrite: true });
}

/* ================================================================
 * Command-class anchors beyond run-start.
 * ================================================================ */
{
  const promo = bench(
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    200000
  );
  log(`CEILING promotion class: assertCanPromoteFromReview=${(promo * 1e6).toFixed(1)}ns once per promotion (production)`);

  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const evaluate = bench(() => void createEvaluationRecord(evalInput), 50000);
  log(`CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=${(evaluate * 1e6).toFixed(0)}ns/call`);

  const prod = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const critique = bench(() => void critiqueContract(prod.contract), 50000);
  log(`CEILING critique class (inside run-start chain): critiqueContract(real contract)=${(critique * 1e6).toFixed(0)}ns/call`);

  const pairInput: PairwiseInput = {
    episodeId: "ep_00000001" as EpisodeId,
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pairwise = bench(() => void runBlindPairwisePair(pairInput, () => 0.7), 20000);
  const pair = runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  const reconcile = bench(() => void reconcileReviews(two), 100000);
  log(`CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=${(pairwise * 1e6).toFixed(0)}ns/call`);
  log(`CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=${(reconcile * 1e6).toFixed(0)}ns/call`);
}

/* ================================================================
 * Cold tier on dist, default + configured (5 fresh processes each).
 * ================================================================ */
{
  const mkScript = (configured: boolean): string => `
    const { performance } = await import("node:perf_hooks");
    const { extractHeuristicContract } = await import("/workspace/dist/requirement/heuristic.js");
    const { assertCoverageAllowsStart } = await import("/workspace/dist/requirement/coverage.js");
    const { applyPrecedence } = await import("/workspace/dist/requirement/precedence.js");
    const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
    const args = ${"{"} objective${"}"};
    ${configured ? 'const cfgArgs = { objective, habits: { requireTests: true, preferReview: false, askBeforeWrite: true } };' : ""}
    const t0 = performance.now();
    const prod = await extractHeuristicContract(${configured ? "cfgArgs" : "args"});
    const tasks = Array.from({ length: 5 }, (_, i) => ({
      id: "tsk_0000000" + i,
      acceptanceCriteria: prod.contract.acceptanceCriteria.map((c) => ({ id: c.id }))
    }));
    ${configured
      ? "assertCoverageAllowsStart(prod.contract, tasks, { resolvedQuestionIds: prod.contract.questions.map((q) => q.id) });"
      : "assertCoverageAllowsStart(prod.contract, tasks);"}
    applyPrecedence(prod.contract, "user-first");
    const t1 = performance.now();
    process.stdout.write((t1 - t0).toFixed(3));
  `;
  const median = (xs: number[]): number => {
    const sorted = [...xs].sort((a, b) => a - b);
    return sorted[Math.floor(sorted.length / 2)] ?? Number.NaN;
  };
  for (const [label, configured] of [
    ["default", false],
    ["configured (habits + Q=1 gated)", true]
  ] as const) {
    const samples: number[] = [];
    for (let i = 0; i < 5; i += 1) {
      const proc = spawnSync(process.execPath, ["--input-type=module", "-e", mkScript(configured)], {
        encoding: "utf8"
      });
      if (proc.status !== 0) {
        check(`cold probe runs (${label})`, false, proc.stderr);
        break;
      }
      samples.push(Number(proc.stdout.trim()));
    }
    log(
      `COLD first-call production total (dist, fresh process, ${label}): median=${(median(samples) * 1e3).toFixed(0)}µs range=[${(Math.min(...samples) * 1e3).toFixed(0)}, ${(Math.max(...samples) * 1e3).toFixed(0)}]µs — once per PROCESS (veto class)`
    );
    check(`cold probes produced numbers (${label})`, samples.length === 5 && samples.every(Number.isFinite));
  }
}

if (failures > 0) {
  process.stderr.write(`\n${failures} check(s) FAILED\n`);
  process.exit(1);
}
log("\nALL EQUIVALENCE CHECKS PASSED (deterministic, seeded)");
```

MORE_OPTIMA=no
BRANCH=cursor/r11-h-eval-eleventh-pass-83a1
