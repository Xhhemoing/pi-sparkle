MODEL_SLUG=claude-fable-5-thinking-xhigh

# R38-G：runtime/graph/domain 切片 Round 38 三十八搜报告

- **战役:** 全库持久 SOTA 优化 Round 38 / R38-G（R1-G…R37-G 同切片三十七遍之后的第三十八遍）
- **基线:** 分支创建于独占分支 `cursor/sota-persistent-opt-83a1` @ 派单 tip `7b8abab`（R38-D 摄取——空枚举——+ R38-G 派单）。工作期间独占分支**两次纯 docs 快进**：(一) 测量后 fetch 检测到 `7b8abab` → `d9ffba4`（R38-E 摄取（空枚举）+ round-38 PLAN/PROGRESS 更新 + R38-H 派单——`git diff --stat` 恰 3 个 docs 文件（PROGRESS / round-38 PLAN / R38-E.md，212 插入 17 删除））；(二) 提交前二次 fetch 又检测到 `d9ffba4` → `bf05c26`（R38-F 摄取（空枚举）+ R38-I 派单——恰 3 个 docs 文件（PROGRESS / round-38 PLAN / R38-F.md，176 插入 16 删除））。两段增量对 `src test scripts package.json pnpm-lock.yaml` 直接复核均 **0 行**，均已按令执行**纯快进**（`git merge --ff-only`，零冲突）并各重验冻结（0 行，`wc -l` 8960 不变）。官方三批测量在 `7b8abab` 工作树完成；两段快进增量均 docs-only ⇒ 三 tip 间 `src/` + `test/` 逐字节相同，测量结果原样携带位级有效；聚焦套件与 `pnpm gate` 在快进后 `d9ffba4` / `bf05c26` 树**各一次**同计数全绿（§5）。新姊妹报告 **R38-E**（第一次快进）与 **R38-F**（第二次快进）已先后全文通读并折入非移植清单（§0）。R38-H / R38-I 在飞，按令未等待、未触碰姊妹分支。
- **分支:** `cursor/r38-g-runtime-thirty-eighth-pass-83a1`
- **切片:** 恰 **42** 文件——`src/run/` 17（除 A 属地 `child-tracking.ts` / `gate-apply.ts`）+ `src/supervisor/` 4（除 B 属地 `model-router.ts`）+ `src/graph/` 4 + `src/domain/` 17；`wc -l` 合计 **8960** 行（与 R32-G…R37-G 记录一致），全部从零实读全文，未依赖前三十七轮记忆。
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R24-G…R37-G 同引擎位级相同，带可比；计时前 `which node` / `node --version` / `process.versions.v8` 三项直证）。本 VM 系统默认 node 为 v22.14.0——按在案陷阱**开工即**显式钉 PATH 到 `~/.nvm/versions/node/v22.22.2/bin`，官方三批全部落在 v22.22.2 上，**零** v22.14.0 误跑、零弃置官方批次（烟测一批按令弃置）。pnpm 10.17.1 / tsx 4.23.12；`pnpm install --frozen-lockfile` 干净完成，锁文件未改。

## 结论

**诚实的空枚举：第三十八遍没有可立 ID 的新更优解，不铸任何 S38-G-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不加 EXCLUSIONS 行，切片零代码改动。** 从零重读 8960 行（42 文件清单核对恰 42：run 17 + supervisor 4 + graph 4 + domain 17），逐文件枚举的全部消除角度经换名检查裁决为既有排除 ID 本体、姊妹归档面（禁移植）、plan-ban 轴名、按令不重提的历史换名记录本体或全局计算顶之内的亚噪声——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（§3）。本轮**零项新站点级换名记录**（与 R37-G / R38-A/B/C/D/E/F 同型）：候选站点名对 R1-G…R37-G 全部 G 报告 + EXCLUSIONS 全表 rg 先例检索，全部命中既有行。四项按令不重提项读码复核原形在位即止：R36-G `contract.ts` `validateRequirementContract` 溯源域中间数组物化（:97–101 原文在位）；R35-G 两项（`flowchart-run.ts` 孪生 Map 物化 :168–176 / `familyForFlowNode` :671–675 原文在位）；R34-G `validateJoin` `edges.some`（flowchart.ts:334 原文在位）。其余候选角度全部为已点名站点复命中（`unexpectedKeys` 每调 Set、`findChild` 回退 Array.from 线扫、Kahn `queue.shift`、`expandTaskTransition` BFS、leaseReadyNodes 逐节点 computeStatus 重算、advanceLedgerRound spread 记账、`isDuplicateFact` O(F) 线扫、planRound 死形参、scheduler `expired()` 每 lease Date.parse、S2-G-2 共享 visiting 本体第三十八遍复认——复命中即止，不重演独立复推导叙事）；本轮新鲜眼唯一边际角度——协调器子任务完成路径的事件日志全量重读节奏——查证后归 **readAll ×96/run 既有定价腿本体**（R6-G 计数谱系 = I/O 地板面本身；事件溯源重放契约保护），复命中即止不铸记录（§3.0-14）。R31-G 的 18 扇聚合门闭合审计按令不重铸。本轮边际动作仅两类，均不构成新面：(a) 缺省预算重锚（计算顶 **0.291–0.296 ms/run**，I/O 地板 **87.0–106.1 ms/run**，支配比 **~294–358×**，两个数量级支配关系不变；持久写 fsync 腿宽散布刻度项见 §1 诚实注记，冻结 0 行 ⇒ 非发现）；(b) G 档案在案零剂量停放与重开条件的**不计时静态哨兵复核**（§2——方法属 R32-A 首创并已 plan-ban 重铸，此处按令仅作卫生性应用，不申报为新轴），全部哨兵**未触发**。14 格矩阵摘要 `06cbcf92c098c8f0…` 官方引擎三批 × 进程内 3 迭代 = **9 次逐位一致——第二十九轮同值，按令不铸摘要恒等 ID / 轴绰号**。空枚举与 R12-G…R37-G、R38-A/B/C/D/E/F 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 42 文件自生产基线 `4efee23` 逐字节零变化（派单 tip 建支时 / 测量后 / 两次快进后各一 / 提交前**五点**核对 `git diff 4efee23 -- <42 文件>` 均 **0 行**；`git diff --find-renames --name-status` 同区间**空输出**——零改名零增删，无改名裁决需做）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均 docs，含本轮两段快进增量 `d9ffba4` / `bf05c26`）。锁文件 `git diff 4efee23 -- pnpm-lock.yaml` **0 行**。聚焦套件 run/supervisor/graph/domain + integration：**tests 386 / pass 385 / fail 0 / skipped 1** 全绿（`d9ffba4` / `bf05c26` 两 tip 各一次，逐字同计数，v22.22.2，与 R31-G…R37-G 记录逐字同计数）；`pnpm gate`（typecheck + lint + test + build）**全绿**——test 1169 / suites 78 / pass 1168 / fail 0 / skipped 1（`d9ffba4` / `bf05c26` 各一跑，逐字同计数；与 R31-D / R32-A…R37-J / R38-A/B/C/D/E/F 记录逐字同计数），退出码 0。临时基准文件在任何 gate 运行前删除，零门禁事故。不开 PR。

## 0. 范围与约束遵守（含不移植清单）

- 切片：`src/run/`（除 A 属地 `child-tracking.ts` / `gate-apply.ts`）17 文件 + `src/supervisor/`（除 B 属地 `model-router.ts`）4 文件 + `src/graph/` 4 文件 + `src/domain/` 17 文件，恰 **42**，全部实际重读全文。
- 读序遵令：README → EXCLUSIONS 全表（545 行）→ PROGRESS → round-38/PLAN（G 段 + 全 ban 榜）→ R37-G 全文（默认对照）→ R38-A → R38-B → R38-C → R38-D 全文 + R37-A…R37-J 姊妹报告全部，之后才读 42 切片文件并动手。第一次快进后补读 R38-E 全文、第二次快进后补读 R38-F 全文并先后折入（本节）；两版 PLAN 快进 diff 复核 G 段均仅状态翻转（本波派出 → 运行中）+ 非移植清单先后补 R38-E / R38-F，默认对照与约束无实质变更。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README.md 零触碰。
- **本轮六姊妹空枚举划界（零新轴可移植）**：R38-A（tracking 侧三项换名检查已拒——全为 `src/tracking/` + A 属地两 run 文件物理对象；G 侧共享精确划界：`src/graph/` `src/domain/` 零 tracking 导入（rg 直证）；run 侧恰 **2 处类型/哈希边界导入**（events.ts:36、supervisor.ts:30，均自 `tracking/types.js` 取 `hashAssessment` / `parseTrackingAssessment` / 类型——assessment 契约面，tracking 无指挥权不变量证人，零剂量零触碰））；R38-B（live 路由侧零新站点级换名记录；S12-B-2 腿 (a) / S13-B-1 经其重取证均未触发——B 属地零触碰；G↔B 唯一共享坐标为 `flowchart-run.ts` 的 learned-routing 消费腿，Live=R0 + S8-E-1 不去重 + 属地边界，零剂量，本清单原有）；R38-C（离线路由侧零新站点级换名记录，两处独立重瞄归 X4-2 / R36-C 既有属主——全为 `src/routing/` C 属地物理对象；G 侧零 posterior/r1/shadow 消费，`flowchart-snapshot.ts:2` 对 `routing/catalog-model.js` 的唯一引用为 throw-only 校验桩 `snapshotValidationRouter`（:488）零剂量）；R38-D（adaptation 侧零新站点级换名记录，35 行元闭合坐标表无一松动——全为 `src/adaptation/` D 属地物理对象，G 四目录零 adaptation 导入）；**R38-E（第一次快进折入：learning 侧零新站点级换名记录；六项属主再验 + 两项独立重瞄归 S3-E-3 / S13-B-1 既有本体——全为 `src/learning/` E 属地物理对象；G 侧共享坐标仍仅 `flowchart-run.ts:712` loadLearnedRouting 消费腿（S8-E-1 + Live=R0，本清单原有，零剂量）；其 §2.3 生产位点普查恰 3 处与本轮 §2 哨兵同结论——互为佐证不互抄）**；**R38-F（第二次快进折入：experiments 侧零新站点级换名记录；十项重推角度全数复命中即止、R32-F…R34-F 十项换名记录 + R37-F 两项重瞄全部复核维持、BINDX 10 token / 8 行第五次复证、S7-F-1 ≠ S6-F-5 读码直证维持——全为 `src/experiments/` F 属地物理对象；G 侧零共享，42 文件 rg 直证零 experiments 导入）**。六报告均未铸新面，非移植清单无需加行。
- **不移植禁令逐项核对**：M1 亲代 run 子调度格（R30-G）、M2 监督引擎裁决格（R29-G）、decision-gate resolution（R28-G）、join-policy（R27-G）、**RUNLIM / CKMIX / TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / ZREP**——本轮零剂量零重提；**BINDX**（切片 `Math.*` token 恰 **6 个**：hash.ts:16 `Math.abs` ×1、flowchart-supervisor.ts:606/:608 `Math.max` ×2、inspection.ts:111/:118/:140 `Math.max` ×3，与 R32-G…R37-G 计数逐一同址，rg 直证计数即止零 patch）；**MODEX**（切片 rg **零** chmod / 零 umask，exit 1 直证，声明即止）；I/O text-codec topology / per-run transcode-byte manifest（R31-E——切片显式 fs UTF-8 位点恰 **4 个**：checkpoint-store.ts:22/:31、pause-controller.ts:31/:92，与 R32-G…R37-G 记录逐一同址；event-store / episode-store 经切片外 `persist/jsonl.ts` 走同一坐标——J 属地，本轮零编解码剂量）；R31-G 18 门闭合审计（**不重铸「我又审了一遍门」**；摘要恒等第二十九轮不铸 ID）；R32-A 重开条件哨兵审计与 R32-B 闭合稳定性审计（**方法不重铸为面**——§2 按令仅应用于 G 自档案在案条目，不申报为新轴）；R33-A 44/44 A-账本方法（不重铸「我又查了一遍闭合稳定性」）；**R33-A…R37-J 空枚举 / R38-A/B/C/D/E/F 空枚举**（方法卫生记录，不重演账本分类、不重提「我又查了一遍重开条件 / 门扉 / 未点名站点」）；**R36-G 一项换名记录**（`contract.ts` `validateRequirementContract` 溯源域中间数组物化——派单明令不重提，本轮读码复核原形在位即止）；**R35-G 两项换名记录**（`flowchart-run.ts` 孪生 Map 物化 :168–176 / `familyForFlowNode` :671–675——派单明令不重提，本轮读码复核原形在位即止）；**R34-G §3.0-1 换名记录**（`validateJoin` 逐 required-ref `edges.some` 线扫——派单明令不重提，本轮读码复核原形在位即止，flowchart.ts:334）；R31-B 六停驻门（cascade 升级臂证据 × 失败类格维持 routing 属地停放）；R31-E/F/H/I 各属地格全额维持；R28-I npm POSIX `.bin` symlink 静默 no-op 不「修复」。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX 全部未编号未移植。S5-H-1、S7-F-1 ≠ S6-F-5、S8-I-1、S9-D-4 / S12-D-1、S12-B-2、S13-B-1 属姊妹切片，未从 G 侧重开。宿主偏置窗（冻结 0 行）按令不作新发现。
- **站位零剂量停放不重铸**：`planTaskTopology` X0-2（读码复核仍无 run loop 调用方，§2 哨兵第三十八次）；`settleSupervisedOutcome` A 属地 no-op（内部调用方 supervisor.ts:601/:711 均不带 assessment，无 assessment 即无门原形在位，§2 哨兵）。
- 未重开任何 X\* / S1-\* … S37-\* 条目。**单变量纪律**：本轮无任何计时新臂——重锚脚本与 R12-G 附录归档件**逐字节同一**（§1、附录），哨兵全部为静态 rg / 读码断言（不计时）。硬不变量零触碰：双 LCB + 双归因在位（切片侧证人：events.ts `isBehaviorDistribution`（:352 定义 / :666 追加侧调用）+ flowchart-snapshot.ts `isOneHotDistribution`（:207 定义 / :189 调用）one-hot 校验原文在位）；Live = R0-equivalent（flowchart-run 的 learned 路由应用面未触碰，B 属地 model-router 未读改）；promotion proposal-first / 无 Outcome-supported 声称（Checkpoint F-PROD 仍开放，ADR-005）；无阈值变更；不改测试迁就重构；CAS / 凭据 / 数据面契约 / 公开签名不变（零 diff 天然满足）；fsync / 完整性再哈希在位（event-store 终态 fsync、checkpoint-store `handle.sync()`、pause-controller writeAtomic、episode-store 终态 fsync 全部原文在位）；fail-open 家族保持关闭（events.ts `validateEvent` 追加/重放双侧 fail-closed 原形）；S8-E-1 不去重 loadLearnedRouting（生产位点恰 3 个：track/loop.ts:88、flowchart-run.ts:712、cli/main.ts:708——rg 直证，与 R38-D §3 哨兵表 / R38-E §2.3 普查互为佐证不互抄，G 侧消费腿零触碰）；catalog 行序契约（R28-B W3）零预排序；分析不变更在飞 run；tracking 无指挥权；H/score 不写 routing PASS/FAIL。无赢家 ⇒ 零生产改动、零新仿真文件入库。不开 PR。

## 1. CPU vs I/O 重锚（三批次官方运行，nvm Node v22.22.2，seed 仅标注批次）

归档基准从 `docs/reports/sota-opt/round-12/R12-G.md` 原样提取（```ts 围栏内 536 行 → `tmp-r38g-bench.ts`，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`，与 R13-G…R37-G 附录 A 记录值**逐字节一致**；无赢家按纪律不入库，临时文件核后即删——早于任何 gate 运行，零门禁事故）。烟测一批（seed 0xa38090）按令弃置；官方三批 seed = 0xa38091 / 0xa38092 / 0xa38093；本轮无表征运行需求（B1 持久写腿高读数经三批对照归 fsync 腿在案宽散布刻度，见诚实注记 ii）。官方三批全部在派单 tip `7b8abab` 工作树完成（两段快进增量均 docs-only ⇒ 三 tip 间 `src/` 逐字节同一，测量形态与提交形态位级同一，全部有效）。

| 探针 | B1 | B2 | B3 |
| --- | --- | --- | --- |
| validateFlowchart 16n join4 | 9.246 µs | 9.242 µs | 9.086 µs |
| → ×32 轮计算顶 | **0.296 ms/run** | **0.296 ms/run** | **0.291 ms/run** |
| validateFlowchart 8n join2 | 4.748 µs | 4.749 µs | 4.580 µs |
| DeterministicJudge.decide 8×8 | 0.142 µs | 0.145 µs | 0.147 µs |
| expandTaskTransition | 0.473 µs | 0.503 µs | 0.477 µs |
| compileChildrenToFlowchart 16 子 | 8.649 µs | 8.744 µs | 8.834 µs |
| validateEvent AGENT_EVENT | 0.562 µs | 0.559 µs | 0.552 µs |
| replayRun 300 events | 0.659 µs | 0.657 µs | 0.684 µs |
| validateCheckpoint N=48 含 restore | 255.4 µs | 263.5 µs | 251.2 µs |
| CheckpointStore.write 持久（N=48） | 768.5 µs | 477.4 µs | 520.1 µs |
| → ×66 写/run | 50.7 ms | 31.5 ms | 34.3 ms |
| EventStore.append（无 fsync） | 74.4 µs | 72.9 µs | 69.5 µs |
| → ×300 事件/run | 22.3 ms | 21.9 ms | 20.9 ms |
| EventStore.readAll ×96/run 中期均幅 | 33.1 ms | 33.6 ms | 34.5 ms |
| EpisodeStore.append COMPLETED（fsync） | 274.2 µs | 216.4 µs | 259.1 µs |
| settle-close 终态命令算术 | 0.465 ms | 0.409 ms | 0.445 ms |

计算顶 = **0.296 / 0.296 / 0.291 ms/run**（3/3 完整落派单默认对照带 R37-G 0.286–0.298 之内）；I/O 地板合成 = **106.1 / 87.0 / 89.7 ms/run**；支配比 **~294–358×**（R37-G ~318–325× / R36-G ~317–332× 同为两个数量级，支配结论不变）。矩阵摘要 `06cbcf92c098c8f0…` 官方 3 批 × 3 迭代 = **9 次逐位一致（第二十九轮同值；14 格配置态 × 命令类矩阵逐格 I/O 计数三批零变化，按令不铸摘要轴/ID）**；start→COMPLETED 首跑冷墙钟 18.0 / 17.5 / 21.4 ms，事件尾 [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED] 不变。

**诚实注记**：(i) 本轮计算顶带 0.291–0.296 完整落派单默认对照带 0.286–0.298 之内（无越沿）；分段探针 validateFlowchart 16n 9.086–9.246 µs 相对 R37-G 8.935–9.327 同带内。(ii) **持久写 fsync 腿宽散布刻度项**：CheckpointStore.write 持久单价 477.4–768.5 µs 三批散布宽（B1 768.5 高于 R37-G 带 587.9–614.6、B2 477.4 低于其下沿——双向跨带），致 I/O 地板合成带 87.0–106.1 相对 R37-G 92.8–95.1 双向加宽，B1 106.1 高沿越出 R37-G 注记引用的历史包络上沿 100.0 约 6%——驱动项恰为 fsync 腿单批抖动（EpisodeStore.append COMPLETED 216.4–274.2 µs 同为 fsync 腿三批宽散布，R37-G 注记 (ii) 231.7–358.8 µs 同型在案先例；settle-close 0.409–0.465 ms 同理）。冻结 0 行 + 摘要第二十九轮恒等 + 逐格 I/O 计数三批零变化 + 计算顶 3/3 对照带内 ⇒ 宿主 I/O 刻度项非切片行为变化，按派单令「host-bias window + freeze 0 lines is NOT a new finding」不作发现、对照带不改。(iii) 计算顶按最保守实测 0.296 ms 执行亚噪声拒收门，距 ≥10 ms 落地线仍 **~34×**，两个数量级，结论完全同向。(iv) 引擎纪律：PATH 在任何计时前钉死 nvm v22.22.2（`which node` / `node --version` / `process.versions.v8` 三项直证在案），官方三批全部官方引擎完成，零 v22.14.0 误跑、零弃置官方批次（烟测 0xa38090 按令弃置不计）、零存档事故、零门禁事故（临时基准文件在任何 gate 前删除）。

## 2. G 档案在案零剂量停放 / 重开条件哨兵复核（全部不计时；方法应用，不铸面）

R32-A 首创的哨兵审计方法（plan-ban 重铸为面）此处按令仅作卫生性应用于 **G 自档案**的在案条目。全部哨兵为静态 rg / 读码断言，零计时臂：

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| X0-2（`planTaskTopology`） | run loop 或任何生产文件获得调用方 | `rg "planTaskTopology" src/` 除 supervisor.ts:64 定义自身外**空匹配**（第三十八次复核）⇒ **未触发** |
| `settleSupervisedOutcome` A 属地 no-op | 生产侧带 assessment 调用出现 | 调用方仅 supervisor.ts:601/:711（均不带 assessment）；函数原形（:472）assessment 缺席即返回 ⇒ **未触发** |
| R30-G M1 格站位前提 | `startParentRun` 获得 CLI 调用方或 resume 门 | `rg "startParentRun" src/` 除 coordinator.ts:224 定义外**空匹配** ⇒ **未触发** |
| R29-G M2 格站位前提 | `startSupervisedRun` 获得 CLI 调用方 | CLI 仅动态 import `resumeSupervisedRun`（main.ts:1021，与历轮记录一致）⇒ **未触发** |
| 摘要恒等（不铸 ID） | 14 格矩阵任一格 I/O 计数或摘要变化 | 官方三批 9 次逐位同值 `06cbcf92c098c8f0…`（第二十九轮）⇒ **未触发** |
| R31-B cascade 停放门（G 侧宿主） | child-coordinator cascade 臂获得新证据面 | 冻结 0 行 + `maybeCascadeRetry` 原形（:382 调用 / :458 定义，与 R33-G…R37-G 记录同址）读码复核 ⇒ **未触发**（routing 属地维持） |
| R31-E 编解码坐标（G 侧四位点） | G 存储腿新增编解码剂量 | 显式 utf8 位点恰 4 个与 R32-G…R37-G 记录逐一同址，零剂量 ⇒ **未触发** |
| BINDX / MODEX 计数哨兵 | 切片 `Math.*` token 数或 chmod/umask 位点变化 | `Math.*` 恰 6 token 逐一同址；chmod/umask **0** 匹配（rg exit 1）⇒ **未触发** |
| S8-E-1 消费腿哨兵 | `loadLearnedRouting` 生产位点被去重/移动 | 生产位点恰 3 个（track/loop.ts:88、flowchart-run.ts:712、cli/main.ts:708）rg 直证（与 R38-D/R38-E 独立普查同结论，互为佐证不互抄）⇒ **未触发** |
| R36-G 换名记录原形 | `validateRequirementContract` 溯源域内形变化 | contract.ts:97–101 `provenanceFields` 中间数组原文在位（冻结 0 行直证）⇒ **原形维持，按令不重提** |
| R35-G 两项换名记录原形 | 孪生 Map 物化 / `familyForFlowNode` 内形变化 | flowchart-run.ts:168–176 / :671–675 原文在位（冻结 0 行直证）⇒ **原形维持，按令不重提** |
| R34-G §3.0-1 换名记录原形 | `validateJoin` 内形变化 | flowchart.ts:334 `edges.some` 原文在位（冻结 0 行直证）⇒ **原形维持，按令不重提** |
| 签名/调用图类重开条件全体 | 要求 `src/` 变更 | 最强冻结 `89f0455..HEAD -- src/` **0 行**直接判空 ⇒ **全部未触发** |

## 3. 本轮换名检查与四十二文件检查表（第三十八遍枚举残余）

### 3.0 换名检查（零项新站点级记录；四项按令不重提，其余全部为已点名站点复命中）

从零重推（假装排除表不存在重读 8960 行）枚举出的候选角度经对表逐一查证（候选站点名对 R1-G…R37-G 全部 G 报告 + EXCLUSIONS 全表 rg 先例检索），**零项构成新站点级换名记录**：

1. **按令不重提：`domain/contract.ts` `validateRequirementContract` 溯源域中间数组物化**（:97–101 `provenanceFields` spread 合并）= R36-G §3.0-1 换名记录本体——派单 DO NOT REMINT 明列，本轮读码复核原形在位即止。
2. **按令不重提：`flowchart-run.ts` 命令入口孪生 Map 物化 `childResultsMap` / `childTaskMap`**（:168–176）= R35-G §3.0-1 换名记录本体——派单明令不重提，原形在位即止。
3. **按令不重提：`flowchart-run.ts` `familyForFlowNode` 内形辅助**（:671–675）= R35-G §3.0-2 换名记录本体——派单明令不重提，原形在位即止。
4. **按令不重提：`domain/flowchart.ts` `validateJoin` 逐 required-ref `edges.some` 线扫**（:334）= R34-G §3.0-1 换名记录本体——派单明令不重提，原形在位即止。
5. **复命中维持：`injection.ts` `unexpectedKeys` 每调 `new Set(allowed)` 构造**（:45）= R35-G §3.1 已点名站点（校验平面亚噪声，once-per-CLI-inject）。复命中即止。
6. **复命中维持：`inspection.ts` `findChild` 回退线扫含 `Array.from(children.values())` 物化**（:161，:136 同族）= R35-G §3.1 已点名站点（once-per-run CLI 分析腿亚噪声）。复命中即止。
7. **复命中维持：`graph/validate.ts` Kahn `queue.shift()` 微形态**（:54）= R35-G §3.3 已点名（16 节点 µs 级一次性构造内含）。复命中即止。
8. **复命中维持：`domain/state.ts` `expandTaskTransition` BFS**（0.473–0.503 µs 本轮实测）= 已定价亚噪声（全表预计算变体 = TERMCLASS 禁 + 亚噪声双拒）。复命中即止。
9. **复命中维持：`flowchart-supervisor.ts` `leaseReadyNodes` 逐节点 `computeStatus()` O(N) 重算** = R28-G 决策门格 + R33-G §3.2 已点名站点（计算顶内亚噪声，真实 N ≤ 16）。复命中即止。
10. **复命中维持：`supervisor/ledger.ts` `advanceLedgerRound` progress 数组 spread 全量记账 + `isDuplicateFact` O(F) 线扫** = R33-G §3.2 已点名站点（maxRounds=32 封顶 + facts 个位数规模）。复命中即止。
11. **本体复认（一行即止）：`failurePathCompletedGraph` 共享 `visiting` true-路径保留**（flowchart-supervisor.ts:467–477 原文在位）= **S2-G-2 本体**——第三十八遍新鲜眼再次注意到同一性质，复认即止，不重演复推导叙事、不作新记录。
12. **复命中维持：`scheduler.ts` `planRound` 死形参 `_leaseDurationMs`（:87）+ `expired()` 每 lease `Date.parse`（:65）** = R29-G 落选候选清单本体 + R36-G §3.1 已点名（公开导出签名硬禁 + 个位数 lease 规模 ns 级）。不重提。
13. **家族真子集判定（不铸记录）**：本轮新鲜眼注意到的校验器内部微形态——`validateFlowchart` 逐节点 role 数组字面量 `includes`（flowchart.ts:367）、`flowchart-snapshot.ts` `isOneHotDistribution` 每调 `new Set(eligible…)`（:211）、`validateEpisode` 解构重建（episode.ts）——全部为**校验平面既有排除行家族真子集**（R35-G §3.4「校验器族 = 校验平面既有排除行」同一属主），且全部住在已定价计算顶 0.291–0.296 ms/run 之内（validateFlowchart 探针本体 9.086–9.246 µs 已含 role 校验路径；快照校验 once-per-restore）。残余微角度全为已点名面复命中，按空枚举规则声明即止，不铸 ID。
14. **本轮新鲜眼唯一边际角度（查证后归既有定价腿本体，不铸记录）**：协调器子任务完成路径每次全量重读事件日志 JSONL 的节奏（coordinator / child-coordinator 完成腿）——对表查证即 **EventStore.readAll ×96/run 既有定价腿本体**（R6-G 计数谱系；本轮实测 33.1–34.5 ms/run，为 I/O 地板三腿之一的面本身，非未受控坐标）；事件溯源重放契约（追加/重放双侧 fail-closed + 分析不变更在飞 run）保护该形态，改造属缓存/增量重放架构面 = 既档 I/O 拓扑与契约属主。复命中即止。

改名裁决（rename-check against baseline）：`git diff --find-renames --name-status 4efee23..HEAD -- src/run src/supervisor src/graph src/domain` **空输出**——切片自生产基线零改名、零增删文件，无改名归属需裁决。

### 3.1 `src/run/`（17 文件）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| coordinator.ts | `startParentRun` 仍无 CLI 调用方（§2 M1 哨兵第三十八次）；M1 亲代 run 子调度格全维度 = R30-G 停放；子完成路径事件日志全量重读节奏 = readAll ×96/run 定价腿本体（§3.0-14）复命中即止 | 无新候选 |
| supervisor.ts | M2 裁决格全维度 = R29-G；`planTaskTopology` X0-2 停放（§2）；`settleSupervisedOutcome` A 属地 no-op（§2）；重放腿 = RUNLIM/CKMIX 既有属主；tracking/types 类型边界导入（:30）= A 契约面零剂量（§0 划界） | 无新候选 |
| flowchart-run.ts | 孪生 Map 物化 + `familyForFlowNode` = R35-G 两项记录按令不重提（§3.0-2/-3，原形在位）；`loadLearnedRouting` 消费腿 :712 = S8-E-1 + Live=R0 零触碰；命令面 14 格矩阵摘要第二十九轮恒等 | 无新候选 |
| child-coordinator.ts | `ConcurrencyGate` / 重试 / cascade 臂 = R31-B 停放门宿主（`maybeCascadeRetry` :382/:458 同址，§2）；执行事件处理线扫 = 已点名域 | 无新候选 |
| injection.ts | 注入指令种类/校验 = R25-G 面全维度；`unexpectedKeys` 每调 Set = 复命中（§3.0-5）；`parseFactValue` try/JSON.parse = 校验平面亚噪声 | 无新候选 |
| flowchart-executor.ts | 每子执行一次的 for-await 事件折叠 = 执行器 I/O 支配（子代理进程级延迟 ≫ 折叠 ns）；`childNodeResultFromExecution` 分支树 = 契约面 | 无新候选 |
| checkpoint-store.ts | 临时文件 + rename + sync 原子写 = 持久契约（fsync 腿宽散布刻度项见 §1 注记 ii，非发现）；记录组成 = CKMIX；utf8 位点 = R31-E 坐标（§2）；`JSON.stringify(…, null, 2)` 美化 = 既有记录组成/编解码属地 | 无新候选 |
| scheduler.ts | LeaseRegistry / planRound / applyTaskOutcome = R29-G 格 + SCHEDWIN 禁；`_leaseDurationMs` 死形参 + `expired()` Date.parse = 复命中（§3.0-12）；expandTaskTransition 0.473–0.503 µs 亚噪声 | 无新候选 |
| pause-controller.ts | writeAtomic fsync + EPERM/EEXIST/EACCES 回退 = 持久契约；utf8 位点 = R31-E 坐标（§2）；parsePauseToken fail-closed = 校验平面 | 无新候选 |
| episode-bind.ts | settle-close 终态算术 0.409–0.465 ms 本轮带内已锚（含 `events.some` 终态扫描——聚合已定价，融合收益亚噪声）；顺序 append 链 = 事件次序契约面；closure 裁决 = episode 姊妹属地；`hash32` 集中化 = 既有落地 | 无新候选 |
| replay.ts | 单遍 O(E) 状态机（0.657–0.684 µs/事件本轮实测）；`eventsLookLikeFlowchartRun` 单遍 some = R1-G 已点名；validateFlowchartCheckpointState 三段 try + restore 试跑 = fail-closed 契约本体 | 无新候选 |
| events.ts | `payloadError` 闭型 switch = 校验平面既有属主；`isBehaviorDistribution` one-hot 校验（:352/:666）= 双 LCB 硬保持项证人（§0）；追加/重放双侧 validateEvent = fail-closed 家族关闭维持；tracking/types 边界导入（:36）= A 契约面零剂量（§0 划界） | 无新候选 |
| child-prompt.ts | lines 数组 + join 一次性构造 = 每子一次 µs 域（prompt 面 I/O 支配） | 无新候选 |
| inspection.ts | once-per-run CLI 分析腿 = 拒收类（一次性 CLI 噪声）；findChild 回退线扫（含 Array.from 物化）= 复命中（§3.0-6）；双遍事件扫（先答案后累积）= 已点名域 | 无新候选 |
| child-grounding.ts | `uniqueArtifacts` Set 去重 + packet 编译一次性 = 每子一次 µs 域；EMPTY_CONTRACT 模块级已提升 | 无新候选 |
| event-store.ts | 队列化 append + 终态 fsync = 持久契约（fsync 家族保持，§0）；readAll 逐行 validateEvent = fail-closed 双侧维持；readAll 单价 191.5–192.5 µs（100 事件）本轮实测（相对 R37-G 186.1–188.7 轻高侧宿主刻度，冻结 0 行非发现） | 无新候选 |
| episode-store.ts | 同 event-store 同构（终态 fsync + 逐行 validateEpisode）= 持久契约 + 校验平面；append COMPLETED fsync 腿 216.4–274.2 µs 刻度项（§1 注记 ii） | 无新候选 |

### 3.2 `src/supervisor/`（4 文件）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| flowchart.ts | 纯再导出（46 行 type/值转发）= 零剂量 | 无新候选 |
| ledger.ts | `advanceLedgerRound` spread 记账 + `isDuplicateFact` O(F) = 复命中（§3.0-10）；createLedger 字面量 = 零剂量 | 无新候选 |
| flowchart-supervisor.ts | `propagate()` 不动点 + edgeStatus/joinStatus = join-policy 面（R27-G）；leaseReadyNodes 逐节点 computeStatus 重算 = R28-G 格复命中（§3.0-9）；`failurePathCompletedGraph` 共享 visiting（:467–477）= **S2-G-2 本体**（§3.0-11 复认）；structuredClone 防御拷贝面 = R31-G 门；waiter 不变式 = 契约 | 无新候选 |
| flowchart-snapshot.ts | 快照校验器族 = 校验平面既有属主（once-per-restore；`isOneHotDistribution` 每调 Set :211 = 家族真子集 §3.0-13）；`snapshotValidationRouter` throw-only 桩（:488）对 catalog-model（:2）的引用 = C 属地边界零剂量 | 无新候选 |

### 3.3 `src/graph/`（4 文件）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| judge.ts | DeterministicJudge 0.142–0.147 µs 本轮实测亚噪声；`evidenceIds.filter(includes)` 二次方微形态 = 证据个位数规模已点名域 | 无新候选 |
| compile-children.ts | 一次性编译 8.649–8.834 µs 本轮实测；per-child Set 查重 = 一次性 µs 域 | 无新候选 |
| validate.ts | Kahn 拓扑一次性构造（16 节点 µs 级，queue.shift :54 微形态含于内 = 复命中 §3.0-7）；确定序 = ORDX 禁 | 无新候选 |
| readiness.ts | `computeReadyTasks` / `allDependenciesSatisfied` 线扫 = R29-G 格既有域（N ≤ 16 亚噪声） | 无新候选 |

### 3.4 `src/domain/`（17 文件）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| flowchart.ts | validateFlowchart 9.086–9.246 µs 本轮实测 = 计算顶探针本体；`validateJoin` edges.some = R34-G 记录按令不重提（§3.0-4，原形在位 :334）；逐节点 role 数组字面量 includes（:367）= 校验器族真子集（§3.0-13）；DFS 环检测 + edgePairs `\u0000` 键 = 已点名域 | 无新候选 |
| contract.ts | 溯源域中间数组物化 = R36-G 换名记录按令不重提（§3.0-1，原形在位 :97–101）；validateRequirementContract 校验器族 = 校验平面既有排除行 | 无新候选 |
| ids.ts | 前缀表 + ID_SUFFIX_PATTERN 单例正则 = 已提升；startsWith+slice 校验 = ns 域 | 无新候选 |
| state.ts | expandTaskTransition BFS = 复命中（§3.0-8）；转移表 = TERMCLASS 禁 | 无新候选 |
| hash.ts | hash32 FNV 风格集中化 = 既有落地；`Math.abs` :16 = BINDX 计数位点（§2） | 无新候选 |
| timestamp.ts | ISO_PATTERN + Date.parse 双查 = 校验平面既有属主 | 无新候选 |
| episode.ts | validateEpisode 解构重建 = 校验平面既有属主（once-per-append；家族真子集 §3.0-13） | 无新候选 |
| task.ts / run.ts / limits.ts / project.ts / evidence.ts | 校验器族 = 校验平面既有排除行；错误串构造仅失败路径 | 无新候选 |
| status.ts / roles.ts / record.ts / errors.ts / index.ts | 闭枚举表 / type-only / 再导出 = 零剂量或亚噪声 | 无新候选 |

## 4. 候选总表

**空。** 本轮零候选：哨兵复核（§2）按定义不产出候选（不计时、不剂量）；换名检查零项新记录、四项按令不重提、其余全部命中既有属主（§3.0——含本轮新鲜眼唯一边际角度归 readAll 定价腿本体）——无一是可立项新站点；四十二文件枚举（§3.1–3.4）全部命中既有 ID 本体、姊妹归档面、plan-ban 轴名或计算顶内亚噪声。本轮无任何计时新臂 ⇒ 无可定价候选面。全局支配结构不变：计算面全体封顶 0.291–0.296 ms/run，距 ≥10 ms 落地线 ~34×（两个数量级）；I/O 面三腿 20.9–50.7 ms 全部契约或排除表保护。

## 5. CHECKS

- 分支恰 `cursor/r38-g-runtime-thirty-eighth-pass-83a1`，基线派单 tip `7b8abab`；工作期间独占分支两次纯 docs 快进 `7b8abab` → `d9ffba4` → `bf05c26`（每段恰 3 个 docs 文件；两段对 `src test scripts package.json pnpm-lock.yaml` 直接复核均 0 行），均 `git merge --ff-only` 零冲突，每次快进后冻结重验 0 行；R38-E / R38-F 全文先后补读折入（§0）。
- 冻结核对：`git diff 4efee23 -- <42 文件>` 派单 tip 建支时 / 测量后 / 两次快进后各一 / 提交前均 **0 行**（五点）；`--find-renames --name-status` 同区间空输出（零改名）；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**（含两次快进后 tip）；锁文件 diff **0 行**。
- 切片清单：恰 42 文件（17+4+4+17），`wc -l` 合计 8960 行（与 R32-G…R37-G 记录一致），全部实读。
- 重锚脚本与 R12-G 附录归档件 sha256 逐字符同一（`9d46a829…b9943`，536 行）；官方三批全部 nvm v22.22.2 / V8 12.4.254.21-node.39；零 v22.14.0 误跑零弃置（PATH 计时前钉死）；烟测一批（0xa38090）按令弃置；临时基准文件门禁前删除，零门禁事故。
- 摘要恒等：`06cbcf92c098c8f0…` 官方 9 次逐位一致（**第二十九轮同值**，按令不铸 ID / 轴绰号）；事件尾不变。
- 哨兵普查（第三十八次）：X0-2 / A 属地 no-op / M1 / M2 站位、cascade 停放、编解码坐标 4 位点、MODEX 零位点、BINDX 6 token 逐一同址、S8-E-1 生产位点恰 3、R34-G / R35-G / R36-G 换名记录原形——全部未触发（§2）。
- 换名检查：**零项新站点级记录**（候选全部命中既有行——rg 全 G 报告 + EXCLUSIONS 先例检索）；四项按令不重提（R36-G contract.ts 溯源数组 + R35-G 孪生 Map / familyForFlowNode + R34-G validateJoin edges.some，原形均在位）；复命中八项 + S2-G-2 本体复认 + 家族真子集判定一组 + 本轮边际角度归 readAll 定价腿本体（§3.0-14）——零新 ID、零重开。
- 姊妹划界：R38-A/B/C/D/E/F 六轴全为各属地物理对象，G 侧共享坐标仅 flowchart-run.ts:712 learned-routing 消费腿（S8-E-1 + Live=R0，本清单原有，零剂量）、flowchart-snapshot.ts throw-only catalog 校验桩（C 属地边界零剂量）与 run 侧 2 处 tracking/types 类型边界导入（events.ts:36 / supervisor.ts:30，A 契约面零剂量）；graph/domain 零 tracking 导入、42 文件零 experiments 导入（rg 直证）——零新轴可移植（§0）。R38-H / R38-I 在飞未触碰。
- 聚焦测试：run/supervisor/graph/domain + integration = **386 tests / 385 pass / 0 fail / 1 skipped**（`d9ffba4` / `bf05c26` 两 tip 各一次，逐字同计数，v22.22.2，与 R31-G…R37-G 逐字同计数）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿**（`d9ffba4` / `bf05c26` 树各一跑，逐字同计数）——test 1169 / suites 78 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A…R37-J / R38-A/B/C/D/E/F 逐字同计数）；退出码 0。
- 仓库改动恰本报告一个文件（`git status` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；Live=R0-equivalent 在位；proposal-first 在位；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不变；fsync / 完整性族在位；fail-open 家族保持关闭；S8-E-1 不去重维持；分析未变更任何在飞 run；不开 PR。

## 6. 重开条件

本轮无新定价轴，不加行。X0-2、A 属地 no-op、R30-G/R29-G 站位前提、R31-B cascade 停放、R31-E 编解码坐标、CKMIX/S8-G-2、S2-G-2 等既有重开条件全额维持原文；本轮 §2 哨兵直证全部未触发。宿主 I/O 刻度项（持久写 fsync 腿 477.4–768.5 µs 宽散布 → I/O 地板合成带 87.0–106.1，B1 高沿越历史包络上沿 ~6%，§1 注记 ii）不构成重开触发——冻结 0 行、摘要第二十九轮恒等、逐格 I/O 计数三批零变化、计算顶 3/3 对照带内 ⇒ 宿主刻度项非切片行为变化。

## 7. 新增排除 ID

**无。** 空枚举轮不铸 S38-G-\* ID；不取绰号、不作轴名、不加 EXCLUSIONS 行。本轮换名检查零项新站点级记录（§3.0），无归档新增。

## 附录 A：重锚基准出处与关键输出

脚本与 R12-G 附录 A 归档件**逐字节相同**：自 `docs/reports/sota-opt/round-12/R12-G.md`（```ts 围栏内 536 行）原样提取为仓库根 `tmp-r38g-bench.ts` 运行，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`（与 R13-G…R37-G 附录 A 记录值一致，逐字节可复现；无赢家按纪律不入库，临时文件核后即删——早于任何 gate 运行）。运行方式：`npx tsx tmp-r38g-bench.ts <seed>`（nvm Node v22.22.2，PATH 前置 `~/.nvm/versions/node/v22.22.2/bin`），烟测 seed = 0xa38090（弃置），官方三批 seed = 0xa38091/92/93（seed 仅标注批次，全部数据确定性构造）。关键输出：

```
--- Node v22.22.2 / V8 12.4.254.21-node.39 (canonical) ---
validateFlowchart 16n join4: 9.246 / 9.242 / 9.086 us -> x32 = 0.296 / 0.296 / 0.291 ms/run
validateFlowchart 8n join2: 4.748 / 4.749 / 4.580 us
DeterministicJudge.decide 8x8: 0.142 / 0.145 / 0.147 us
expandTaskTransition PENDING->FAILED: 0.473 / 0.503 / 0.477 us
compileChildrenToFlowchart 16 children: 8.649 / 8.744 / 8.834 us
validateEvent AGENT_EVENT: 0.562 / 0.559 / 0.552 us
replayRun 300 events: 0.659 / 0.657 / 0.684 us
validateCheckpoint full (N=48, incl. restore): 255.4 / 263.5 / 251.2 us
CheckpointStore.write durable (N=48): 768.5 / 477.4 / 520.1 us -> x66 = 50.7 / 31.5 / 34.3 ms/run
EventStore.append AGENT_EVENT (no fsync): 74.4 / 72.9 / 69.5 us -> x300 = 22.3 / 21.9 / 20.9 ms/run
EventStore.readAll 300/100 events: 497.6~526.9 / 191.5~192.5 us -> x96 = 33.1 / 33.6 / 34.5 ms/run
pause token() ENOENT / present: 20.5~21.8 / 55.0~55.5 us
loadLearnedRouting (no snapshot): 23.1 / 23.0 / 23.2 us
EpisodeStore.readAll (2 snapshots): 54.5 / 55.5 / 54.6 us
EpisodeStore.append OPEN (no fsync): 69.3 / 65.3 / 71.0 us
EpisodeStore.append COMPLETED (fsync): 274.2 / 216.4 / 259.1 us
EpisodeEventStore.append (no fsync): 61.8 / 63.8 / 61.5 us
settle-close arithmetic: 0.465 / 0.409 / 0.445 ms per terminal command
matrix digest (3 proc x 3 iter): 06cbcf92c098c8f0... 九次逐位一致（14 格；与 R10-G..R37-G 同值，第二十九轮）
start->COMPLETED wall-clock: 18.0 / 17.5 / 21.4 ms; returned events tail = [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED]
```

（引擎与存档纪律注记：PATH 在任何计时前钉死 nvm v22.22.2，零 v22.14.0 弃置批次；官方三批存档自始完整，零截断零重跑；临时基准文件在任何 gate 运行前删除，聚焦套件与官方 gate 均一次通过。）

---

**MORE_OPTIMA=no** —— 第三十八遍复查空枚举收口：计算顶 vs I/O 地板支配比 ~294–358× 两个数量级不变（fsync 腿宿主刻度项非发现）；14 格矩阵摘要第二十九轮同值；G 档案在案停放/重开哨兵全部未触发；换名检查零项新记录、四项按令不重提、其余全部命中既有属主（含本轮边际角度归 readAll 定价腿本体）；42 文件逐条消除角度全部命中既有属主。无达门槛新更优解。

MORE_OPTIMA=no
BRANCH=cursor/r38-g-runtime-thirty-eighth-pass-83a1
