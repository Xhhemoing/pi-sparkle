MODEL_SLUG=claude-fable-5-thinking-xhigh

# R38-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第三十八遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 38 / R38-A（Round 1–37 同区第三十八遍）
**基线:** 派发 tip `cursor/sota-persistent-opt-83a1` @ `2e4de37`（R37-J 摄取 + Round 37 关轮 + Round 38 开卷 + 本 R38-A/B/C 派发）；提交前 fetch 复核 exclusive 仍为 `2e4de37`——测量窗口内**零快进**，无需 rebase；姊妹 R38-B/C 在飞，无新姊妹报告可读，按令不等待不触碰其分支。R37-B…R37-J 九份空枚举报告已全文读毕并折入不移植清单。
**分支:** `cursor/r38-a-tracking-thirty-eighth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R28-A…R37-A 同引擎位级相同；PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 显式绕开系统 Node v22.14.0——`node --version` / `process.versions.v8` 双复核在三次官方运行头部逐次打印（L0 见证 ×3 全过），全部官方读数在该引擎产出，零批次落在 v22.14.0 上；tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成——诚实注记：本 VM 初始未装依赖（`tsc: not found`），frozen-lockfile 安装后门禁全绿）。

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S38-A-\* ID，不铸新 face 名、不取 ALLCAPS 绰号，切片零代码改动，EXCLUSIONS.md 零增行（全表仍 545 行）；整片预算收口第三十六次复核成立（本 VM 重锚 14.0–19.0 µs/gate ⇒ ~70–95 µs/run，四格 2×2 满矩阵三次官方运行；带与 R37-A 主带 13.1–16.3、R36-A 13.8–17.6、R35-A 12.6–19.0、R34-A 12.8–15.2 交叠；本轮驱动按令为 R12-A 附录 A 同源正典重建——采样口径与格数差异见 §1.1 诚实注记 (i)，为仪器现象非切片变化；宿主偏置窗 + 冻结 0 行按令不构成新发现）。** R31-A 已把元坐标系逐面归属完毕并停靠最后一扇 injected-callable 门；R32-A 完成重开条件哨兵审计（F12a–d）；R33-A 把 R32-B 三腿方法应用到 A 区 44 行账本（44/44 仍立）；R34-A / R35-A / R36-A / R37-A 共十二项换名检查已拒列。按令，上述均为方法 / 停驻记录——本轮**不重走** R32-B 闭合稳定性审计、**不再铸**「再查一遍闭合稳定性 / 重开条件 / 门扉」式新面，仅以卫生身份复跑哨兵（L0 可执行 ×3 + 驱动内建确定性断言每 run 逾 9800 项全过 ×3 + F12c/F12d/F11a-lite 静态见证 + 全库 grep 普查，无一重开条件触发）。本轮第三十八遍独立枚举从零重读 14 文件（`wc -l` 逐文件 79/29/38/155/76/107/22/14/137/103/195/522/83/236 = 合计 **1796** 行，与 R35-A…R37-A 逐数一致）、重走 18 结构位置 × 13 消除机制满矩阵（§4），枚举出的全部角度经换名检查逐一裁决为既有排除 ID 本体、姊妹归档面（禁移植）或 R31-A 停靠门成员——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（3 项新站点级换名记录见 §0；R12-A…R37-A 共 120 项换名检查全部复核维持）。

切片 14 文件自 R1-A 基线（`7acb666`）经 R2-A … R37-A 至本轮 tip `2e4de37` **逐字节未变**（`git diff 7acb666 -- <切片>` 在派发 tip、测量后、提交前**三验均 0 行**，三十八遍全程零 diff；清单核对恰 14 文件（12 + 2）并全部实际重读）。调用面维持最强冻结再延一轮：`git diff 89f0455..2e4de37 -- src/` **完全为空**（R10-A 基线以来跨 R10…R38 共二十九段全部提交均为 docs）。生产调用方经本轮全库 grep 普查复核仍为 `src/run/supervisor.ts:483/:487`（applyTrackingGate / nextTrackingSeq）、`src/run/coordinator.ts:444`、`src/run/flowchart-run.ts:320`（applyChildThreeLine），每子结果一次（~5 次/run），事件表几十级（E=41）。稳态下即使把整个切片优化到零成本，节省上界 ~0.070–0.095 ms/run，仍比落地线（数十~数百 ms）低约**三个量级**——第三十六次支配论证成立。聚焦套件 72/72 绿；`pnpm gate`（typecheck + lint + test + build）全绿（test 1169 / pass 1168 / fail 0 / skipped 1，与 R31-A…R37-A 逐字同计数；exit 0）。空枚举与 R12-A…R37-J 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config / from-child / gates / human-score / index / isolation / prescore / roller / turn / types）+ `src/run/child-tracking.ts` + `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。静态普查对 coordinator.ts / flowchart-run.ts / supervisor.ts 及全 `src/` 树做**只读**见证（零编辑）。未触碰其余切片与任何兄弟分支（R38-B/C 在飞零触碰；exclusive 测量窗口内零快进，无姊妹报告需折入）。
- 先读并遵守（顺序强制）：README.md / EXCLUSIONS.md（全表 545 行）/ PROGRESS.md / round-38/PLAN.md（A 段 + 具名/unnamed 全 ban 榜）/ R37-A.md 全文（默认对照 + 三项换名记录——按令不再重提）/ R37-B…R37-J 九份报告全文（全部空枚举，折入不移植）。之后才从零重读 14 切片文件并动手。EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md / 任何 `src/` 零触碰。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：R37-A 三项换名检查（types.ts parseHumanSignal ratio 臂双读 CSE / from-child `claims` 单元素数组 / child-tracking `acceptance.map` 投影——本轮复核均维持拒列，不再单列、不重铸）/ R36-A 三项换名检查（types.ts parse 路径回调闭包提升 / from-child `artifactIds` 防御拷贝 / roller `keptIds` 中间映射数组——复核维持）/ R35-A 三项换名检查（`{kind:"unobserved"}` 五站点享元 / `asString` 首字符卫 / prescore dip 乘后判——复核维持）/ R33-A 44/44 A 账本闭合稳定性方法（不重走、不移植结论、**禁止**再铸「再查一遍闭合稳定性」——本轮遵守：§2 只做哨兵卫生复跑，不做账本重分类）/ R32-A 重开条件哨兵方法（F12a–d 仅卫生复跑，不铸面）/ R33-A…R37-J 空枚举（不重提「我又查了一遍重开条件 / 门扉 / 未点名站点」）/ R31-A injected-callable 停靠门（`generateEventId` / `readers.readToolBodies` 非独立物理对象——仅以不计时见证复核重开条件，见 §2）/ **BINDX**（切片 `Math.*` token 恰 6 个于 5 行，同 BINDX 坐标，划界计数即止，零剂量）/ I/O text-codec 面（切片 fs 编解码位点 0，结构性空）/ REPRX（切片 `JSON.parse` 输入位点 0）/ R32-J 六残余坐标（本切片数据路径零磁盘 I/O，未过三合取，零重铸）。**FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX** 全部未编号未移植、未写入 EXCLUSIONS 表行。S12-B-2 / S13-B-1 / S8-I-1 / S8-E-1（`loadLearnedRouting` 恰 3 处生产调用维持：track/loop.ts:88、flowchart-run.ts:712、cli/main.ts:708）/ S9-D-4 / S12-D-1 / S5-H-1 / S7-F-1≠S6-F-5 全部切片外硬保持项零触碰。catalog 行序契约（R28-B W3）零触碰。npm POSIX `.bin` symlink 静默 no-op（R28-I）不当优化修。
- **换名重提检查**（本轮枚举全部新角度的逐项裁决，一律不铸 S38-A-\* ID。R12-A…R37-A 共 120 项换名检查全部复核维持；以下 **3 项为本轮新站点级记录**——均为在手报告未逐字点名过的站点，裁决同族同向，不新增任何 ID；若与更早轮次某条未在手记录重合，则为复命中，结论不变）：
  1. **from-child.ts:153 `verification?.evidenceIds ?? []` 空数组回退臂每命中新铸**（evidenceRefsOf 第二循环头；受理路径上 verification 经 :52 守卫恒已定义且 `evidenceIds` 为必填字段 ⇒ `?? []` 臂生产**零流量**、空臂不取则不分配）= **S1-A-7 享元家族 + R33-A 记录 1（`["test"]` 回退享元）镜像形**——生产流量下节省字面为 0，拒列；
  2. **from-child.ts:143 `observation.constraints.map((constraint) => constraint.id)` retainedConstraintIds 投影数组每调用新铸**（prescoreInputFromObservation 内；与 R36-A 记录 3（roller keptIds）/ R37-A 记录 3（child-tracking acceptance.map）同族**第四站点**，非彼两站点之重提）= **S1-A-7/S7-B-5 投影身份家族**——享元不可行（ids 随 constraints 变化），每 gate 至多一次 ns 级，拒列；
  3. **turn.ts:76–78 `openMinors.filter((item) => item.status === "verified-true").length` 计数即弃中间数组**（lightMinorCount 回退臂；生产 from-child 路径 window.openMinors ≡ `[]` 且 previous ≡ undefined ⇒ 过滤对象为空数组）= **S1-A-6 单遍融合家族姊妹站点 +（准）零流量伞**——融合计数每 gate 至多省一次空数组分配，ns 级，拒列。
- 其余枚举角度全部落在既有 ID **本体**上（非换位点，无需新记录；清单与 R37-A §0 末条逐项同集，本轮逐条复核维持，代表项）：hashAssessment dimensions / gate.codes 冗余 spread = S2-A-5；`localeCompare` 排序 = R26-A collation 本体 + X1-1/S8-G-2 memo 禁；JSON.stringify 换手写序列化 = S5-A-2；extractTenPoint 每调用 `new RegExp` 与 `[...matchAll]` 双 spread = X0-6 本体 + 零流量伞（humanInput ≡ `{}`）；evidenceRefsOf 双算 = S2-A-2；第二次 prescore = S1-A-2 / S3-A-2；`every×includes` 两族站点 = S1-A-4/X4-7（C 轴可操作重开第一优先，重开条件未触发）；幂等双 find/死扫描 = S8-A-1/S11-A-2；五扫描逐条 = X0-4/X2-4/S1-A-1/S1-A-9/R1-A 清单；`[...input.events]` 拷贝 = R1-A 不可变拷贝下界本体；写侧 validateEvent = S4-G-2 族；roller 全站点 = S2-A-1/S7-A-2/S1-A-5/S3-A-1/R20-A 维持；turn.ts rollSummary 调用处 `{...window, openMinors}` 死覆盖 = S4-A-1 本体；HumanScoreInput 重建 = S7-A-1 伞本体；mergeOpenMinors previousById Map = S3-A-4 同函数裁决；GateInput 死字段 / `void` 双死读 = S8-A-3；hardCodes 恒分配 = S7-A-3；shouldEscalateMinors 单遍融合 = S1-A-6；combineScore 代数重排 = S7-A-4；toFixed 换 Math.round = S6-A-1；S4-A-2/S4-A-3/S5-A-3/S6-A-2/S6-A-3/S2-A-6 与 S7-A-1/S9-A-1/S10-A-1/S11-A-1 十二层收口维持；window contextFacts 模板字面量 + idempotencyKey 拼接 = R21-A 面成员；mapGateDirective / currentGateStatus 双调用位点 = 互斥路径非双算（R34-A 裁决维持）；executionAuthority / bindExecutionContext / evidenceWeight 死导出 = R22-A 普查本体；analysis.ts = S8-A-2 + R22-A axis 11（零 src/ 生产调用方）；isolation/index 零逻辑面 = R29-A F9c(iv) 维持；extractRatio agreedIds Set = 零流量伞 + S1-A-3 族。
- 未重开任何 X\* / S1-\* … S37-\* 条目。**单变量纪律**：本轮无计时新臂——按令驱动为 R12-A 附录 A 同源正典（E=41 冻结夹具、humanInput ≡ `{}`、M=0/K=0 同轴钉死）；F12c/F12d/F11a-lite 以静态见证复核（不计时），驱动内建确定性断言（F1/F2 fuzz + 锚点 apply + 夹具 apply）可执行 ×3；对可调用物 / 内建绑定 / 分派历史零计时臂（R31-F 纪律）。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点四格（gate-outcome × contract 2×2，R12-A 正典口径）覆盖；R14-A 第五格（max-codes 2-code）不在正典驱动内——该格 R14-A…R37-A 在案实测恒为五格**最低**（R37-A 13.1–13.3 µs），略去只会收紧而非放宽支配上界，不构成测量盲区（见 §1.1 注 (i)）。配置态逐面复核——(a) settle 带/不带 assessment：生产两调用点（supervisor.ts:601/:711）经静态普查复验均不带 trackingAssessment（O(1) no-op 第二十九次复验，函数头 `if (assessment === undefined) return` @ :482）；(b) trackingConfig 覆写/applyUserThreshold：生产恒 DEFAULT（X0-7 维持，F12d 四元组静态见证）；(c) maxItems 截断态 = S2-A-1 已拒面；(d) 注入可调用物坐标 = R31-A 停放（重开条件 (a)/(b)/(c) 复核未触发，见 §2）。无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 / minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无命令权、H/score 不写路由 PASS/FAIL、Live = R0 等价、双 LCB 双归因保留、提升 proposal-first、无阈值变更、未改任何测试、无 fsync/完整性重哈希移除、fail-open 家族保持关闭）同样天然满足——本轮只经公开 applyChildThreeLine / applyTrackingGate / nextTrackingSeq / hashAssessment / parseTrackingAssessment / validateEvent / computePrescore / runTrackingTurn / assessChildObservation / observationFromChild / prescoreInputFromObservation 驱动。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本（按令不建 repo scripts；驱动指纹见附录 B，临时文件已删除）。不开 PR。

## 1. 预算支配论证复核（方法第 1、2 步）

R3-A §3 … R37-A §1 的整片预算论证两前提本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666 -- <切片>` 在派发 tip `2e4de37`、测量后、提交前三验均为空，逐字节一致（三十八遍全程零 diff）——R1-A…R37-A 全部测量与排除按令转移，但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨二十九段）**：`git diff 89f0455..2e4de37 -- src/` **完全为空**。生产调用方三点位经全库 grep 普查复验（`applyChildThreeLine` 恰 {child-tracking(def), coordinator:444, flowchart-run:320}；`applyTrackingGate`/`nextTrackingSeq` 恰 {gate-apply(def), child-tracking, supervisor:483/:487}；`runTrackingTurn` 恰 {turn(def), from-child, tracking/index(re-export)}——与 R31-A…R37-A 期集合逐文件一致），无新增调用方；切片与三调用文件 `.bind(` 计数 0，coordinator/flowchart-run 各恰 1 站点每调用新铸箭头，supervisor 两 settle 站点均新铸箭头。

### 1.1 锚点矩阵（本 VM 重锚，三次官方运行 + 一次弃置烟测，E=41，~5 gate/run；R12-A 附录 A 正典四格；引擎 Node v22.22.2 L0 见证在案）

```text
                                   run1      run2      run3
PASSED x no-contract               19.0      15.3      17.8  µs/gate (+1 event)
PASSED x contract(3)               14.0      14.2      16.4  µs/gate (+1 event)
FAILED x no-contract               17.9      16.1      17.4  µs/gate (+3 events)
FAILED x contract(3)               14.2      14.1      14.5  µs/gate (+3 events)
=> 14.0–19.0 µs/gate ⇒ ~70–95 µs/run（稳态热；烟测弃置值 15.4/13.9/15.8/14.2 同带）
```

与 R37-A（主带 13.1–16.3 ⇒ 65–82；含窗全带 13.1–23.5）/ R36-A（13.8–17.6 ⇒ 69–88）/ R35-A（12.6–19.0 ⇒ 63–95）/ R34-A（12.8–15.2 ⇒ 64–76）带交叠；低沿 14.0 落在历史包络内、高沿 19.0 与 R35-A 上沿重合（按派单令「宿主偏置窗 + freeze 0 行不构成新发现」）。**诚实注记（仪器）**：(i) 本轮驱动**按派单令为 R12-A 附录 A 同源正典逐字重建**（附录 A/B；血统正典源码完整存档于 round-12/R12-A.md 附录，本轮重建经确定性计数 + 正典抽取件见证运行验证同源），与 R34-A…R37-A 的 median-of-blocks 重建版口径不同：四格 2×2（无 R14-A 第五格 max-codes——该格历轮恒为最低，略去不放宽支配上界）、单块均值采样（每格 5000 次均值，对瞬态较敏感 ⇒ 带略宽）、固定 seeds `0xc12a01`–`0xc12a02`（正典不带 seed 参数）。口径差异为仪器现象、非切片行为变化。(ii) PASSED×no-contract 三 run 均为四格最高（19.0/15.3/17.8）——与 R11-A 起在案、R30-A 已定价为 µs 拒收类的 **no-contract 格 PIC/基准次序现象族**一致（warm-up 混序使调用位点 IC 从头多态），不改变任何结构结论，不铸新轴。(iii) FAILED×contract(3) 三 run 位邻最紧（14.2/14.1/14.5），无 R37-A run3 型宿主干扰窗复现；三 run 无全格同向平移，无结构悬崖。稳态下即使切片零成本，节省上界 ~0.070–0.095 ms/run，仍比战役落地线低**约三个量级**。**支配论证（稳态口径）第三十六次复核成立**；生产冷态口径维持 R17-A 轴 6b 裁决（[2.7, 8.5] ms/run once-per-run，不重测）。

### 1.2 合同地板分解复核（R12-A 方法学正典口径，PASSED×contract(3) 生产格，三次官方运行区间；括号内 R37-A / R36-A 区间对照）

| 组件 | 本轮实测/gate | R37-A | R36-A | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1885–1967 ns 每次 ⇒ **5.65–5.90 µs** | 5.02–5.15 | 5.80–6.76 | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体 | **2.90–2.92 µs** | 2.63–2.69 | 3.30–3.92 | S4-G-2 族 schema 屏障；实现在 events.ts（G 切片） |
| `runTrackingTurn` 完整 | **1.72–1.76 µs** | 1.64–1.90 | 1.72–1.91 | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构（带交叠） |
| `computePrescore` × 2 | 683–689 ns 每次 ⇒ **1.37–1.38 µs** | 1.37–1.38 | 2.69–3.27 | S1-A-2；与 R37-A 带**逐位重合** |
| `parseTrackingAssessment` 单测 | **318–346 ns** | 428–503 | 520–653 | 带毗邻偏快，宿主同向（正典 pojo 夹具） |
| O(E) 五扫描 + 拷贝（正典逐件口径：episode 64–65 / seq 52–53 / findGT 40–41 / findTA 39–41 / copy 26–28 ns） | 合计 **221–228 ns**（⇒ 5.4–5.6 ns/event @E=41） | 589–601（复刻块口径，不可直较） | — | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界；与 R35-A 正典式口径 203–222 带毗邻 |
| `observationFromChild` 单测 | **55–57 ns** | —（未单列） | — | S1-A-7/S7-B-5 家族宿主 |
| `applyTrackingGate` 整体隔离 | **6.12–6.30 µs** | 4.96–5.23 | 6.92–7.56 | 介于 R37-A/R36-A 带间；结构同构 |
| 合同锁定合计（CAS 2×hash + validateEvent + 扫描 + 拷贝） | **6.90–7.08 µs** | — | — | 正典报表行；与命名残差（caller hash + obs + 2×prescore + turn）5.07–5.15 µs 之比 ~1.36：合同占大头，结构未变 |

**诚实注记**：hash / validateEvent / gate 整体相对 R37-A 同向偏慢 ~10–20%（宿主窗 + 正典单块采样），parse 偏快，prescore 带逐位重合，turn 带交叠——无结构悬崖。组件占比次序 **hash×3 > validateEvent > turn > prescore×2 > parse > scans+copy** 三 run 严格成立，与 R34-A / R35-A / R37-A 次序一致。**F1/F2 归档事实复核**（正典 fuzz，5000 trial/run）：受理路径 4819 条全部固定 6 维构造序（F1，X1-2/S5-H-3 家族前提维持）；cappedByHardFail 3508 次全部 min-fold 一致（F2，S8-A-3 族零边际成本前提维持）——计数在烟测、三次官方、正典抽取件见证运行间**逐位相同**（确定性 + 同源双证）。**F1 天花板复测**：hashAssessment 排序+映射机件 269–278 ns/hash ⇒ ×3 hash/gate × ~5 gate/run ≈ **4.0–4.2 µs/run 天花板**（X1-2/S5-H-3 家族，维持拒列）。R18-A skip / R19-A replay 抽测不在正典驱动内，本轮不重测（R30-A 19d 收口维持，组成天花板 {apply, skip, replay} 裁决携带）。

## 2. 哨兵卫生复跑（全部不计时；按令仅卫生，不铸面、不重走审计）

按 round-38 PLAN 禁令，本轮**不**重走 R32-B 闭合稳定性审计、**不**重做 R33-A 的 44 行账本三腿分类（该记录仍立，本轮三条携带前提均在案：K 腿由三验 0 diff 与最强冻结携带，M 腿由 §1.1 重锚携带——整片上界 ~70–95 µs/run，无一收益池变大，E 腿由 L0 见证携带）。仅以卫生身份复跑在案重开条件哨兵：

| 重开条件 | 哨兵 | 裁决 |
| --- | --- | --- |
| R31-A 停靠门 (a) 生产获得第二可调用物坐标 | 静态普查：coordinator:444 / flowchart-run:320 各恰 1 站点每调用新铸箭头；supervisor 恰 2 个 settle 站点（:601/:711）均新铸箭头且不带 trackingAssessment（O(1) no-op 第二十九次复验，守卫 @ :482）；切片 + 三调用文件 `.bind(` 计数 0，零包装工厂 | **未触发** |
| R31-A 停靠门 (b) readers 获得生产流量 | 静态见证：from-child.ts:84 生产 turn 调用 humanInput ≡ `{}`、零 readers 传参（源文逐行复读）；正典 fuzz 4819 条受理路径可执行驱动同一生产形 ×3 | **未触发** |
| R31-A 停靠门 (c) 调用位点 megamorphic 化 | 全 src/ 引用文件集普查：三公开入口引用集合与 R31-A…R37-A 期集合逐文件一致（§1 前提 2） | **未触发** |
| C 轴第一优先重开（R15-A ~2.5 量级） | 静态见证：生产 requiredChecks 形未变（child-tracking.ts:59–64 tester + 1 acceptance ⇒ `["chk_0"]` 长度 1；`["test"]` 回退臂仍零流量；contract(3) ⇒ 3 条 mandatory "constraint"）+ 正典夹具 apply 断言可执行 ×3 | **未触发**；可操作重开第一优先地位不变 |
| X0-7 阈值冻结 | 静态见证：config.ts 四元组 (0.55/0.3/0.54/0.03, version 1) 源文复读 | **冻结在位** |
| 签名/调用图类条件（S8-A-1/S11-A-2 族、S6-H-2 型热环、S2-A-1 截断态） | `git diff 89f0455..HEAD -- src/` = 0 行 | **全体判空** |
| 引擎代际（R33-A E 腿显式留白） | L0 ×3：Node v22.22.2 / V8 12.4.254.21-node.39 与 canonical 位级相同（三次官方运行头部逐次打印） | **未触发**（E 腿裁决继续携带） |

### 2.1 姊妹面划界计数（声明即止，零剂量）

- **BINDX（R31-C）**：切片环境内建 `Math.*` token 恰 **6 个于 5 行**（combined-score.ts:24 ×2、prescore.ts:66/:72、turn.ts:64、gate-apply.ts:232）——与 R34-A…R37-A 逐点一致，同 BINDX 坐标，零触碰零 patch。
- **R31-E I/O text-codec 面**：切片 `node:fs|readFile|writeFile` 命中 **0**——面在 A 结构性空。
- **REPRX（R30-C）**：切片 `JSON.parse` 输入位点 **0**（parseTrackingAssessment 收内存对象非字节流）——面空。
- **R32-J 六残余坐标**：本切片数据路径零磁盘 I/O、零持久工件——六坐标在 A 无物理对象，声明即止，不移植不重铸。
- **补充卫生 grep**：切片 `Math.random` 命中 0；`0.5` 命中 5 处全部为不变量文档注释（"never 0.5"）或阈值子串（0.55/0.54），零填充违规；S8-E-1 `loadLearnedRouting` 恰 3 处生产调用（track/loop.ts:88、flowchart-run.ts:712、cli/main.ts:708）+ 1 定义 + 3 import，与在案不变量逐点一致。

## 3. 候选总表

**空。** 本轮枚举的全部角度（§0 新站点级换名记录 3 项 + R12-A…R37-A 120 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、姊妹归档面或停靠门成员；哨兵卫生按定义不产出候选（不计时、不剂量）。空枚举与 R12-A…R37-J 同为战役允许的诚实结局。

## 4. 结构位置 × 消除机制满矩阵扫描（第三十八遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或本轮 §0 换名检查；按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `gate-apply.ts` | mapGateDirective / currentGateStatus 双调用位点 = 互斥路径非双算（R34-A 裁决维持）；`[...input.events]` 拷贝 = R1-A 下界本体（正典单测 26–28 ns）；RUN_BLOCKED 防御拷贝不对称 = R33-A 记录 2 维持；wait_user 腿外向回调（含 `"evt_".length`）= R32-A 记录 1 维持；nextTrackingSeq `Math.max` = BINDX 坐标；幂等双 find 正典单测 39–41 ns 各（E=41 miss）；O(E) 清单 / S11-A-2 / S8-A-1 / S2-A-4 维持 | 无新候选 |
| `child-tracking.ts` | `acceptance.map` 投影 = R37-A 记录 3 维持（不重提）；`skipped` 每调用新铸 = R34-A 记录 1 维持；`["test"]` 回退享元 = R33-A 记录 1 维持；条件 spread 后置化 = R33-A 记录 3 维持；constraints map 逐 gate 新铸 = S1-A-7/S7-B-5 本体；S4-A-2 caller 侧预检维持；observationFromChild 正典单测 55–57 ns（全函数 ns 级，任何内部重排上界即此）；锚点带 14.0–19.0 µs/gate | 无新候选 |
| `turn.ts` | **lightMinorCount 计数即弃 filter 中间数组 = §0 换名记录 3 拒列（S1-A-6 融合家族姊妹站点 + 准零流量伞）**；`readersInvoked` 常量初值享元 = R34-A 记录 3 维持；HumanScoreInput 重建（:83–87，生产恒 `{}`）= S7-A-1 伞本体；rollSummary 调用处 `{...window, openMinors}` 死覆盖 = S4-A-1 本体（roller 不读 window.openMinors，源文复读确认）；anomalyCodes 拷贝 = S1-A-7 本体；uniqueCodes 中间拼接 = S7-A-2 族；mergeOpenMinors `Math.max` = BINDX、previousById Map = S3-A-4 同函数裁决；prescoreInput spread 克隆 = S3-A-2；failClosed 分支 gate 重铸 = S2-A-1 零流量同伞；claims.some 双算 = S3-A-3；runTrackingTurn 正典 1.72–1.76 µs | 无新候选 |
| `types.ts` | parseHumanSignal ratio 臂双读 CSE = R37-A 记录 1 维持（不重提）；parse 路径回调闭包提升 = R36-A 记录 1 维持；`{ kind: "unobserved" }` 五站点享元 = R35-A 记录 1 维持；`asString` 首字符卫 = R35-A 记录 2 维持；hash×3 地板 5.65–5.90 µs；parse 318–346 ns；`localeCompare` = R26-A 本体；X1-1/S8-G-2 memo 禁维持；X0-5 asRecord/asArray 合并禁维持；枚举表 includes / evidenceWeight find = S1-A-8；hashAssessment 冗余 spread = S2-A-5 本体；hashSummary 多排序 = R20-A 零流量伞；F1 天花板 269–278 ns/hash 复测（X1-2/S5-H-3 维持拒列） | 无新候选 |
| `from-child.ts` | **`?? []` 空数组回退臂 = §0 换名记录 1 拒列（零流量臂 + S1-A-7 享元家族）**；**retainedConstraintIds 投影 = §0 换名记录 2 拒列（S1-A-7/S7-B-5 第四站点）**；`claims` 单元素数组 = R37-A 记录 2 维持（不重提）；artifactIds 防御拷贝 = R36-A 记录 2 维持；`completedChecks` PASSED 臂防御拷贝 = R34-A 记录 2 维持；FAIL 守卫合取重排 = S6-A-2 本体；evidenceRefsOf 双算 = S2-A-2 本体；第二次 prescore = S1-A-2；window contextFacts 模板字面量 = R21-A 面成员；S3-A-3 / S5-A-3 / S6-A-3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `prescore.ts` | dip 乘后判 = R35-A 记录 3 维持（不重提）；C 轴哨兵对象（`every×includes` 两站点；重开未触发，第一优先地位不变）；dimension() 条件 spread = R28-A key-set geometry 面；`void actorSelfScore/independentEvidence` 死读 = S8-A-3 族规格意图文档；正典单测 683–689 ns（R37-A 带逐位重合）；S1-A-3 / S1-A-4/X4-7 / S6-A-1 / S8-A-3 维持 | 无新候选 |
| `combined-score.ts` | `Math.min/max` = BINDX 坐标；S6-A-1 / S7-A-4 / R13-A §0-1 维持 | 无新候选 |
| `gates.ts` | 分支面维持（重排 = R31-A §0-5 族）；hardCodes 数组恒分配 = S7-A-3 实测负优化本体；X0-7 哨兵宿主；S1-A-6 / S8-A-3 维持 | 无新候选 |
| `roller.ts` | keptIds Set 中间映射数组 = R36-A 记录 3 维持（不重提）；keptIds 恒等过滤 / 无截断态 = S2-A-1 本体；uniqueStrings 中间拼接 = S7-A-2；mergeConstraints `[...byId.values()]` 为结果构造非冗余拷贝（非候选）；confirmedDecisions includes 过滤 = S1-A-5；mandatory 死 text 字段 = S3-A-1；prevSummaryHash 链深 = R20-A 本体 + from-child 零流量（previous ≡ undefined）；S9-A-1 维持 | 无新候选 |
| `human-score.ts` | 四条 unobserved 早退臂字面量 = R35-A 记录 1 维持（不重提）；extractTenPoint 每调用 RegExp 铸造 + `[...matchAll]` 双 spread = X0-6 本体 + 生产零流量（humanInput ≡ `{}` 静态见证 + 正典 fuzz 驱动同形）；`userText?.trim()` 分配 = 零流量同伞；extractRatio agreedIds Set = 零流量同伞 + S1-A-3 族；S1-A-3 / S7-A-1 / R14-A §0-2 / R21-A 维持 | 无新候选 |
| `config.ts` | 生产恒 DEFAULT（X0-7 静态见证宿主）；trackingConfig ?? 链 = 一次性配置亚噪声 | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方）；sanitize-then-project = S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R29-A F9c(iv) 契约角维持；index.ts 纯再导出零逻辑 | 无新候选 |

**对 R33-A 声明的裁决**：「44/44 行至少一腿成立，无一裁决重开」经本轮三条携带前提独立复核继续成立（0 行冻结三验、L0 引擎同一、天花板不增）；本轮不做重复审计，产出全部为卫生归档，无可落地候选。收口的预言力第三十八遍成立。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核 = 恰本文件）。仿真为临时脚本未入库（无赢家不落仿真文件；本轮驱动**按派单令为 R12-A 附录 A 同源正典逐字重建**——血统源码完整存档于 round-12/R12-A.md 附录，可逐字抽取复现；重建同源性经双证：确定性计数逐位相同 + 正典抽取件见证运行 ALL CHECKS PASSED；驱动 sha256 见附录 B，临时文件已在门禁前删除），未触碰任何测试。

## 6. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R37-A 同套件同计数；Node v22.22.2）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）在派发 tip `2e4de37` 工作树全绿（test 计数 1169 / pass 1168 / fail 0 / skipped 1，与 R31-A…R37-A 逐字相同；退出码 0；本 VM 初装依赖 `pnpm install --frozen-lockfile` 干净完成后执行）。

仿真（临时脚本；R12-A 正典固定 seed 可复现，三次官方运行摘要；每 run 内建确定性断言 = F2 5000 项 + F1 4819 项 + 锚点 apply 4 项 + 夹具 apply 1 项，三 run 全过，另烟测一次弃置、正典抽取件见证运行一次弃置）：

```text
run 1: anchor 19.0 / 14.0 / 17.9 / 14.2 µs/gate => ~70-95 µs/run
       hash×3=5886ns (1962ns each) | parse=346ns | validateEvent=2917ns
       | prescore×2=1366ns | turn=1715ns | 5 scans+copy=222ns | obs=57ns
       | applyTrackingGate=6.12µs | locked=7.07µs residue=5.10µs | F1-ceiling 278ns
run 2: anchor 15.3 / 14.2 / 16.1 / 14.1 µs/gate => ~71-81 µs/run
       hash×3=5654ns (1885ns each) | parse=331ns | validateEvent=2904ns
       | prescore×2=1372ns | turn=1759ns | 5 scans+copy=221ns | obs=57ns
       | applyTrackingGate=6.30µs | locked=6.90µs residue=5.07µs | F1-ceiling 269ns
run 3: anchor 17.8 / 16.4 / 17.4 / 14.5 µs/gate => ~73-89 µs/run
       hash×3=5901ns (1967ns each) | parse=318ns | validateEvent=2923ns
       | prescore×2=1378ns | turn=1745ns | 5 scans+copy=225ns | obs=56ns
       | applyTrackingGate=6.23µs | locked=7.08µs residue=5.15µs | F1-ceiling 275ns
F1/F2: 5000 fuzz trials, 4819 apply paths fixed 6-dim order,
       cappedByHardFail 3508x min-fold-consistent —— 三 run + 烟测 + 抽取件逐位相同
ALL CHECKS PASSED (deterministic, seeded) ×3
```

三次官方仿真确定性计数行逐位一致；锚点四格无干扰窗复现（FAILED×contract(3) 位邻 14.1–14.5 最紧）。空枚举裁决成立。

## 7. 新增排除 ID

**无**（空枚举，不铸 S38-A-\*、不铸 face 名、不取绰号、EXCLUSIONS.md 零增行）。换名检查 3 项新站点级记录见 §0（1–3），归档备后续轮次引用，无需并入 EXCLUSIONS.md。

**可操作重开第一优先不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）——本轮静态见证重开条件仍未触发。

## 附录 A：本轮驱动说明（R12-A 正典逐字重建）

按派单令，本轮驱动为 **R12-A 附录 A 同源血统**的逐字重建（非 R34-A…R37-A median-of-blocks 重建版）：血统源码完整存档于 `docs/reports/sota-opt/round-12/R12-A.md` 附录代码块（449 行），重建件 `/tmp/r38a-harness.ts` 按该代码块逐字誊写。结构六段：(1) mulberry32 seeded fuzz（seeds `0xc12a01`–`0xc12a02` 固定）F1/F2 归档事实断言 5000 trial；(2) E=41 事件表夹具 + 3000 次四形混序 warm-up；(3) 四格锚点（gate-outcome × contract 2×2，每格 5000 次单块均值 + apply 断言）；(4) 合同地板分解（hash / parse / validateEvent / 五扫描逐件 / copy / obs / prescore / turn / applyTrackingGate 整体）；(5) F1 天花板块；(6) 报表 + 失败计数出口。与 R34-A…R37-A 重建版的口径差异（四格无第五格、单块均值、固定 seed）已在 §1.1 注 (i) 诚实声明，均为仪器现象。

**同源性双证**：(a) 三次官方运行 + 烟测的确定性计数（4819 / 3508）逐位相同；(b) 从 R12-A.md 附录围栏逐字抽取的正典件（sha256 见附录 B）单独运行一次，确定性计数与 ALL CHECKS PASSED 逐位复现（该次计时弃置，仅作同源见证），抽取件即删。

## 附录 B：驱动指纹与运行方式

- 正典源：`docs/reports/sota-opt/round-12/R12-A.md` 附录 ```ts 围栏代码块，449 行，抽取件 sha256 `3e624bdb0d866036e10a911eed7c1668e7bf95b76f5b002ae46e45c68c3a9c4a`（awk 围栏抽取；抽取件与重建件均为临时文件，已在门禁前删除，未入库）。
- 运行（repo 根，nvm Node v22.22.2 + tsx 4.23.12；PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开系统 Node v22.14.0）：

```bash
npx tsx /tmp/r38a-harness.ts   # 烟测 1 次（弃置）+ 官方 3 次（正典固定 seed，无参数）
```

MORE_OPTIMA=no
BRANCH=cursor/r38-a-tracking-thirty-eighth-pass-83a1
