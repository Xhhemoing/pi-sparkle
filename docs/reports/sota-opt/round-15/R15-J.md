MODEL_SLUG=claude-fable-5-thinking-xhigh

# R15-J：数据面切片（cluster / privacy / preferences / episode / persist / track / context / feedback）第十五遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 15 / R15-J
**基线:** `cursor/sota-persistent-opt-83a1` @ `2932c41`（独占 tip，含 R15-A/B/C/D/E/F/G/H 全部摄取——八区全部空枚举；R15-I 在飞未触碰）
**分支:** `cursor/r15-j-persist-fifteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十五遍枚举为空：无达门槛候选、零新 ID、零生产代码改动、切片维持
关闭（MORE_OPTIMA=no）。**
切片 29 个文件（3655 行，本轮 `wc -l` 复核与 R9-J..R14-J 一致；
3+3+7+5+2+4+2+3 清单逐目录确认恰 29 文件）自 J1 落地（`fb41417`）以来
**逐字节未变**（本轮核对 `git diff fb41417..HEAD -- <29 文件>` 输出为
0 行——J1 之后经 R2-J..R14-J 与本轮共十四遍复查累计零后续代码改动）。
R14-J 基线（`2b83fc8`）之后 `git log 2b83fc8..HEAD -- src/` **零提交**
（其间 7 个提交全部为 `docs/reports/` 文档——R14-G/R14-H 摄取、
Round 14 收口与 R15 开轮及 A–H 各波摄取/派发；R15 前八区全部空枚举，
无一代码落地）⇒ 生产调用图对本切片**可证不变（最强形式，第七连）**。
按指令在本 VM 重锚配置态 I/O 地板矩阵（两遍 × 各 3 次运行，§1.3）：
preferences `saveToDisk` 非空策略 **149.3–224.0µs/写**（50 观察档，
R14-J 142.5–280.0µs 带重叠）～**415.6–520.8µs/写**（500 观察档，
R14-J 454.0–520.6 / R13-J 407.6–514.9µs 带重叠——地板随存量伸缩，
I/O + 全量 stringify 支配判据第十五次成立）；jsonl 追加 fsync=false
**64.6–93.4µs**（R14-J 61.9–98.5µs 带内）/ fsync=true
**213.9–426.6µs**（R14-J 310.9–405.4 / R13-J 242.0–396.0µs 带重叠，
第二遍上缘 426.6 高出历史 max ~21µs——本轮新轴 A/A 直测 fsync 协议
噪声 ±45–175µs/append 完全覆盖，§2.2）；配置态删除级联 N=200、
20 命中：match=**724.4–949.1µs** / no-match=**267.3–342.1µs**（R14-J
666.1–786.8 / 260.8–319.1µs 带重叠/毗邻，match 上缘高出 ~162µs——
A/A 级联噪声 max-abs ±169–233µs/cascade 完全覆盖，§2.2）；
`buildProjectContextIndex` **40.3–41.4µs/构建**（R10-J 40.1–41.3µs
带重叠）；track plan CPU 计数器 IdGenerator **1.09–1.15µs/plan** /
默认 randomUUID **2.63–2.69µs/plan**（R14-J 1.08–1.11 / 2.62–2.69µs
带重叠）。J1 落地代码本轮 `scripts/r1j-equivalence-sim.ts` 重跑全绿
且**首次跨引擎**：Node 22.22.2（V8 12.4）**2468 项逐位检查通过，
reference 10304.2ms → current 3.9ms = 2612.5×**；Node 24.19.0
（V8 13.6）**同 2468 项逐位通过，2903.3×**——J1 行为面对引擎代际
逐位不变的首个直接见证。第十五遍在完整排除表（54 个已闭 S*-J-* ID +
J1 + R10-J..R14-J 五轮空枚举收口）之上以本轮真实新增轴收口：R15-B
首创、R15-D/E/F/G/H 已各自镜像的「**测量学基底**」透镜在 J 切片
**首次落地**（§2）——(A) **A/A 噪声地板标定**：用与十五轮锚点/决斗
完全相同的协议测两臂同函数同夹具（真实效应恰为零），6 胞 × 8 窗 ×
（3 次 N22 + 1 次 N24）= **24 胞 192 窗，0/24 出现 8/8 同号伪稳**；
I/O 胞噪声地板 saveToDisk ±33.5–64.1µs/写、级联 ±168.7–232.8µs/次、
fsync ±45.4–174.8µs/追加——**首次定量证明历史轮间 I/O 地板带漂移
（R11-J..本轮五组带）是协议+VM 噪声而非代码漂移**（与零 diff 互证），
历史全部 µs/ns 级 CPU 拒绝从测量端获得 a fortiori 证书（CPU 胞稳态
地板 index ±0.6–2.0µs、plan ±0.006–0.058µs，被拒差额全部在地板之内
且落地线高于地板 2–5 个数量级——两端封死，无翻案通道也无伪稳假赢家
通道）；(B) **引擎代际敏感性**：全套九格锚点在 Node 22.22.2 与
Node 24.19.0 双引擎复跑（十四轮全部 J 测量都在单一 22.x 引擎上做出）
——I/O 六格**引擎不变**（全部同带，syscall 束缚），CPU 三格**均匀
加速**（index −7%、plan counter −17~−20%、plan uuid −22%；与 R15-B
的 B 切片均匀红利同型、与 R15-G 的 G 切片混号漂移不同），绝对量
−0.2~−3.1µs/call 深度亚 ms ⇒ **I/O 支配性拒绝对引擎代际双向鲁棒**：
更快的引擎只会把计算面压得更低于 I/O 地板。两轴全部得出「无候选」的
测量性裁决，按纪律不铸任何 S15-J-* ID。指令点名的换名重提检查（J1 /
S5-J-3 / S6-J-1 / S8-J-2 / R11-J 两处无名微观 / R12-J 三处无名微观 /
R13-J 两处理论失败探针 / R14-J 三条规模越线数字）逐条读码复核全部
原样在位、未补铸（§0）。诚实结论：枚举为空，不硬凑 ID，与
R10-J..R14-J 及 R15-A..H 空枚举收口同型。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/cluster/`（3：mailbox / spawn / host）、`src/privacy/`
  （3：deletion / state-layout / record-classes）、`src/preferences/`
  （7：loop-eval / export / precedence / materialize / service / store /
  types）、`src/episode/`（5：manager / store / closure / events /
  replay）、`src/persist/`（2：file-lock / jsonl）、`src/track/`
  （4：primary-split / plan / clarify / loop）、`src/context/`
  （2：index / packet）、`src/feedback/`（3：redaction / store / types）
  共 **29 文件 3655 行**全量第十五遍实际读码，未依赖前十四轮记忆。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表，含 J1 /
  S5-J-3 / S6-J-1 / S8-J-2）/ round-15/PLAN.md（含 A–H 八区收口注记与
  J 区派单注记）/ R14-J.md（空枚举；规模越线标定 + 相位分解 + deopt
  审计）/ R13-J.md（空枚举；fs 动词 × Node API 形态普查）及前轮 J
  报告按需。J1 未重做（本轮重跑其等价仿真双引擎全绿）；
  `scripts/r1j-equivalence-sim.ts` 未改动。未开 PR；未编辑
  EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未触碰在飞 R15-I 分支与
  文件。零生产编辑 ⇒ 按指令**未提交任何新仿真脚本**（锚点/新轴
  harness 源码与 sha256 入附录 A/B，工作区临时件已在 gate 前删除）。
- **指令点名的换名重提检查**（逐条读码复核，全部原样在位、未列为
  新候选）：
  - **J1**：`loop-eval.ts` 每主体增量 fold + O(1) tombstone undo 落地
    形态逐字节未变（SubjectState/pushObservation/undoPush 结构
    L35–91 原样）；等价仿真 2468 项逐位全绿——且本轮首次双引擎
    （N22 2612.5× / N24 2903.3×）。
  - **S5-J-3（fail-closed 钉死项）**：`cascadeFeedbackTombstones`
    L111 的 `readFeedbackTombstoneIds` 仍在匹配循环**之前**无条件执行
    ——两读 fail-closed 顺序零形态改动。本轮 S0c 锚点与 A/A 级联胞
    均经生产函数原样测量。
  - **S6-J-1（partial-delete 钉死项）**：L121–124 的
    `writeFeedbackRecords` → tombstone 落盘仍为**串行两写**，零并行化/
    攒批/延迟变形。
  - **S8-J-2**：`persist/jsonl.ts` L17–22 fsync 路径仍为 appendFile 后
    独立 `open("a")`+sync+close 双句柄形态，一字未动。本轮 A/A 直测
    该路径协议噪声 ±45.4–174.8µs/append——S8-J-2 原判据「符号不稳
    （fsync 抖动）」首次获得定量地板。
  - **S9-J-1..4**：`stripForbidden` 顺序 split/join 无 includes 卫
    （redaction.ts L88–94）；`rebuildViews` 内部 `weights` 仍为 Record
    （store.ts L97）；index 构建 architecture/risks 仍为双遍
    filter+sort+map（index.ts L140–147）；`decideClosure` 仍无
    structured-pass 短路、legacyMatch 探针原样（closure.ts L32）。
  - **R11-J 两处无名微观（指令明令勿补铸）**：`context/index.ts`
    L170/L191 对 `snapshot.commands` 的 `.some`+`.find` 同谓词双扫
    **原样在位、未铸 ID**；`cluster/host.ts` `send()` L126–128 役播
    queued 路径 `role === undefined` 死复核**原样保留、未铸 ID**
    （S4-J-1 防御纵深判据维持）。
  - **R12-J 三处无名微观（指令明令勿补铸）**：spawn 链跨函数双
    `objective.trim()`（spawn.ts L41 + host.ts L181）、
    `mailbox.claimRole` skip 分支逐 own-mail `byRole` 重取重存
    （mailbox.ts L70–72）、`packet.compileContextPacket` 对
    `codeMapSelection.omissions` 同数组两遍循环（packet.ts L130–140）
    **全部原样在位、未补铸**。
  - **R13-J 两处理论失败存在性探针（指令明令勿重铸）**：
    `privacy/deletion.ts` `statExists` catch-all 探针形态（L41–48，
    两调用点 L61/L85）**原样在位**——探针消除/access 换形均未重提
    （ENOTDIR fail-open→fail-closed 反例维持）；`preferences/store.ts`
    `loadFromDisk` 的 existsSync+readFileSync 双探针（L20–21）
    **原样在位**——单读合并未重提。两处均**未铸 ID**。
  - **R14-J 规模越线数字（指令明令勿重铸）**：saveToDisk
    N\*≈1.1×10⁴、级联 N\*≈4×10³、jsonl L\*≈1.4×10⁴ 三条标定**未重测、
    未补铸**——本轮引用为既得收口（越线规模上成本相位 1:1 落在已闭
    ID 或数据面契约评审域的裁决原文继承）；生产配置锚（50–500 观察 /
    N=200 记录）本轮复核仍距 N\* 1.5–2.3 个数量级。
  - R10-J 无 ID 收口维持：`preferences/precedence.ts` 整模块、
    `durableRecordClassById`、`episode/replay.ts` 两导出、
    `exportForDataset`、`evaluatePreferenceLoop`、`materializeView`、
    `findConflicts` 等零生产调用方本轮 re-grep 全量复证（§1.2）；
    S7-J-6 saveToDisk 每写 `mkdirSync` 自愈保留（store.ts L34）；
    S2-J-1 loop-eval 两次 `tombstones.has` 原样（L153/L159）；
    S1-J-7 packet.ts L122 首个 `omissions.sort` 原样在位。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价（R1 未接线）、
  双 LCB 与双归因保留（X0-11）、晋升 proposal-first、无
  Outcome-supported 声明（Checkpoint F-PROD 仍开放，ADR-005 / X0-1）、
  阈值/测试/CAS/凭据/数据面契约/公开签名不变、**fsync 未去**、
  **完整性再哈希未去**、**增量读镜像未加**（EventStore/EpisodeStore
  readAll 原样，S1-G-1/S2-J-11 族）、删除级联 fail-closed 两读顺序与
  串行两写未动，天然成立。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，经 nvm 切换
  v22.22.2 为规范引擎（`/exec-daemon/node` 遮蔽 PATH，需显式前置
  nvm bin）；引擎代际轴另经 nvm 安装 v24.19.0（与 R15-B/G 同版本）；
  `pnpm install --frozen-lockfile` 后仿真与门禁全绿（§6）。

## 1. 基线不变性、调用图复核与 I/O 地板重锚（含配置态矩阵）

1. **切片逐字节未变**：`git diff fb41417..HEAD -- src/cluster
   src/privacy src/preferences src/episode src/persist src/track
   src/context src/feedback` 输出为空（29 文件 3655 行，十五遍全程 J1
   之外零 diff）。R1-J…R14-J 全部逐函数收口与 54 个 S*-J-* 已闭 ID +
   R10-J..R14-J 五轮无 ID 收口继承有效。
2. **调用图可证不变（最强形式，第七连）**：`git log 2b83fc8..HEAD --
   src/` **零提交**——R14-J 基线以来 7 个提交全部为 `docs/reports/`
   文档（R14-G/R14-H 摄取、Round 14 收口、R15 开轮与 A–H 各波摄取/
   派发；R15 前八区全部空枚举，无一代码落地）⇒ 生产调用方地图数学上
   不变。本轮 re-grep 全量复证关键面：
   - `appendJsonlLine` 四调用方原样（fsync 非 false 仅
     `run/event-store.ts:39` 与 `run/episode-store.ts:36`；
     `feedback/store.ts:40` 与 `episode/store.ts:27` 均 false）——
     **无新调用方、无新 I/O 动词**；
   - `withExclusiveFileLock` 生产调用方四处原样（bandit-store:44 /
     promotion:247 / file-credential-store:43,54 / cli/episode:90）；
   - 生产入口行号逐点一致：`buildProjectContextIndex`
     （coordinator:253 / flowchart-run:255）、`compileContextPacket`
     （child-grounding:48）、`decideClosure`（episode-bind:181 /
     cli/episode:105）、`getMaterializedView`（clarify:21–22 /
     main:1260）、`exportAuthorizedPreferences`（main:1317–1318 惰性
     import）、`deleteRunRecords`/`deleteEpisodeRecords`
     （main:1388–1392 惰性 import）；
   - 零生产调用方面全量复证：`evaluatePreferenceLoop` /
     `exportForDataset` / `materializeView` / `findConflicts` /
     `durableRecordClassById` / `replayEpisodeEvents` / `replayFromLog` /
     `queryPacketGrounding` / `refreshProjectContextIndex` /
     `compilePacket` / `applyRedaction` / `getObservationsByKey` /
     `recordObservation` / `tombstoneIds` /
     `materializeWithoutTombstones` / `createEmptyContext` 在 `src/`
     内全部仍仅定义处（或注释引用）。
3. **配置态 I/O 地板矩阵重锚**（两遍 × 各 3 次运行，本 VM，规范引擎
   Node 22.22.2，harness 与 R12-J 附录 A 逐字节同源仅换 `r15j-` 前缀，
   sha256 见附录 A）：

```text
S0a  saveToDisk 非空策略（recordPreference 端到端，50 观察种子 + 200 写）:
       224.0/177.6/156.4 | 217.7/149.3/151.3 us/write → 带 149.3–224.0µs（R14-J 142.5–280.0 / R13-J 131.7–211.2µs 带重叠）
S0a' 同上 500 观察种子档:  520.8/421.3/415.6 | 509.0/442.7/429.9 us/write → 带 415.6–520.8µs（R14-J 454.0–520.6 / R13-J 407.6–514.9µs 带重叠）
S0b  jsonl append fsync=false (2000 次):  64.6/65.6/80.2 | 83.4/84.8/93.4 us → 带 64.6–93.4µs（R14-J 61.9–98.5µs 带内）
S0b  jsonl append fsync=true  (300 次):   303.4/285.7/213.9 | 420.1/426.6/424.1 us → 带 213.9–426.6µs（R14-J 310.9–405.4 / R13-J 242.0–396.0µs 带重叠；
       第二遍 420.1–426.6 高出历史 max 405.4 约 21µs——§2.2 A/A 直测 fsync 协议噪声 ±45.4–174.8µs/append 完全覆盖，属协议噪声非漂移）
S0c  删除级联 N=200、20 命中 MATCH:    817.3/801.9/724.4 | 949.1/861.8/816.8 us → 带 724.4–949.1µs（R14-J 666.1–786.8 / R12-J 672.9–797.7µs 带重叠/毗邻；
       上缘高出 R14-J max ~162µs——§2.2 A/A 级联噪声 max-abs ±168.7–232.8µs/cascade 完全覆盖）
S0c  删除级联 N=200 NO-MATCH:          315.0/342.1/304.9 | 293.0/270.6/267.3 us → 带 267.3–342.1µs（R14-J 260.8–319.1µs 带重叠）
S0d  buildProjectContextIndex (F=40 + 30 codeMap + 双哈希表): 40.4/40.3/40.4 | 41.4/40.3/40.3 us → 带 40.3–41.4µs（R10-J 40.1–41.3µs 带重叠）
S0e  track plan CPU: 计数器 gen 1.09/1.09/1.10 | 1.15/1.11/1.10 us/plan → 带 1.09–1.15µs（R14-J 1.08–1.11µs 带重叠）
     默认 randomUUID gen: 2.69/2.68/2.67 | 2.64/2.64/2.63 us/plan → 带 2.63–2.69µs（R14-J 2.62–2.69µs 带内）
```

   判读：全部九格与 R13-J/R14-J 带重叠或毗邻——**「同路径 µs/ns CPU
   为 I/O 地板支配即拒」判据在本 VM 第十五次成立**（级联/追加/落盘
   三条路径数百 µs 起步；纯切片内命令类 CPU 地板 1.09µs/plan，距
   落地线 4–5 个数量级）。两处带缘超出（fsync 上缘 +21µs、级联 match
   上缘 +162µs）由本轮新轴 A/A 直测的协议噪声地板**定量覆盖**
   （§2.2）——历轮「带内漂移不作 landing 处理」纪律首次在 J 获得
   测量端根据。未复制任何前轮数字，全部本 VM 重测。
4. **J1 等价仿真重跑（生产脚本，未改动，首次双引擎）**：
   `pnpm exec tsx scripts/r1j-equivalence-sim.ts` → 场景 1（16 例
   确定性边界）64 项 + 场景 2（600 例随机化）2400 项 + 性能夹具
   （N=6000 多主体 ~25% tombstone）：Node 22.22.2 reference
   10304.2ms → current 3.9ms（**2612.5×**）；Node 24.19.0 reference
   11098.3ms → current 3.8ms（**2903.3×**）。两引擎均
   `ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)`——J1 行为面
   跨引擎代际逐位不变的首个直接见证。

## 2. 第十五遍枚举：测量学基底透镜（A/A 噪声地板 + 引擎代际）→ 两轴全部「无候选」→ 诚实空结果

### 2.1 收口角度

前十四轮已按十四个互不重复的收口角度穷尽：逐函数下界（R1-J）、
调用图（R2-J）、语义融合反例（R3-J）、并行化竞态（R4-J）、fail-open
反例（R5-J）、崩溃序/攒批反例（R6-J）、配置态热路径（R7-J）、fsync
句柄面（R8-J）、原型键等价 + 短路死分支（R9-J）、零调用方导出审计 +
微观位点定向核查（R10-J）、结构位点 × 消除机制矩阵猎漏（R11-J）、
跨函数重复纯计算普查（R12-J）、fs 动词 × Node API 形态普查（R13-J）、
规模越线标定 + 相位分解（R14-J）。本轮取 R15-B 首创、R15-D/E/F/G/H
已各自镜像的「**测量学基底**」透镜在 J 切片首次落地——不再问「代码
还有什么可改」，而问「十五轮裁决所依赖的测量协议本身噪声几何、对
引擎代际是否鲁棒」。J 的特殊性与 G 同型但更极端：**切片三条生产 I/O
路径全部数百 µs 起步且每命令次数为个位**，协议噪声主体在文件系统
syscall 方差；且 J 有一个 G 没有的悬案——历史轮间 I/O 地板带端点
漂移达 ±20–35%（如 50-obs 档五轮 min 131.7~149.7µs、max
211.2~280.0µs），从未有轮次直接量化「同代码同 VM 下该协议自身能
漂多少」。两条子轴把该悬案与引擎假设全部变成直测事实。

### 2.2 新轴 A：A/A 噪声地板标定（J 决斗/锚点协议本体噪声的首次直测）

方法：两臂为**同生产函数 + 同确定性夹具**（真实效应恰为零），逐窗
`delta_w = arm(A) − arm(B)`；每臂协议与附录 A 锚点各格逐字相同
（同夹具、同预热、同迭代数）。6 胞 = saveToDisk（50-obs 种子 +
200 计时写/臂）、级联 MATCH（30 次计时级联/臂）、jsonl fsync=false
（2000 追加/臂）、jsonl fsync=true（300 追加/臂）、contextIndex
（2000 预热 + 20000 计时构建/臂）、plan CPU 计数器（20000 预热 +
200000 计时/臂）。8 窗/胞 ×（3 次 N22 + 1 次 N24）= **24 胞
192 窗**。脚本 `tmp-r15j-aa.ts`（sha256 `d340ec07…`，全文见附录 B）。

```text
                       max-abs/run（µs）           符号分布（8 窗）
                    run1    run2    run3   n24    run1   run2   run3   n24
saveToDisk /写      54.1    64.1    33.5   43.5   4+/4-  6+/2-  3+/5-  5+/3-
级联 MATCH /次     214.3   168.7   232.8  231.6   5+/3-  5+/3-  6+/2-  5+/3-
jsonl false /追加    3.8     6.2     6.4    1.3   5+/3-  3+/5-  3+/5-  4+/4-
jsonl true  /追加   45.4    92.2   174.8   93.5   2+/6-  5+/3-  6+/2-  6+/2-
contextIndex /构建   0.60    2.03    0.96   0.61  3+/5-  4+/4-  6+/2-  3+/5-
plan 计数器 /plan    0.017   0.058   0.025  0.006 2+/6-  4+/4-  2+/6-  2+/6-
伪稳集：24 胞 192 窗 0/24 出现 8/8 同号（最偏 6+/2-）；逐窗全量见附录 B
```

**裁决**（三条结构性结论，均为「无候选」）：

1. **历史轮间 I/O 地板带漂移定量归因**：saveToDisk 单窗纯协议噪声
   ±33.5–64.1µs/写，完全覆盖 50-obs 档五轮带端漂移（R11-J
   132.7–215.3 / R12-J 149.7–271.6 / R13-J 131.7–211.2 / R14-J
   142.5–280.0 / 本轮 149.3–224.0µs——min 端散布 ~18µs、max 端散布
   ~69µs）；级联 ±168.7–232.8µs/次恰覆盖本轮 match 上缘对 R14-J 的
   +162µs；fsync ±45.4–174.8µs/追加覆盖 fsync 历史带（206.5–426.6µs
   跨 R11-J..本轮）的全部散布——**轮间带差是协议+VM 噪声而非代码或
   环境漂移**（与 §1.1 逐字节零 diff 互证），「带内漂移不作 landing
   处理」的历轮纪律获得定量根据。S8-J-2 的原否决判据「符号不稳
   （fsync 抖动）」由 jsonl-true 胞首次给出数值地板。
2. **历史计算面拒绝获得测量端证书**：J 切片全部 ns–µs 级 CPU 拒绝
   （S1-J-3..7、S2-J-3/5/8、S3-J-2/3/6、S4-J-4/5/6、S5-J-4/5、
   S6-J-3/6、S7-J-1..4、S8-J-1、S9-J-1..4 等）的名义差额全部坐落在
   稳态窗地板（plan ±0.006–0.058µs、index ±0.6–2.0µs）之内或与之
   同阶——该协议物理上无法为它们出具稳定符号，历史「噪声级/亚噪声」
   裁决 a fortiori 成立（与 R14-J 从成因端给出的 deopt/JIT 审计互为
   补证）。
3. **判据自洽性（两端封死）**：凡 I/O 面差额低于 ~50–230µs/操作、
   CPU 面差额低于 ~2µs/call 的候选，本协议无法稳定分辨——而落地线
   （数十–数百 ms）高于地板 2–3 个数量级，真赢家若存在必可测。
   J 的裁决协议不存在「漏掉真赢家」的盲区，也不存在「伪稳定假赢家」
   的通道（0/24 直测，192 窗无一 8/8 同号）。**该轴不产生任何
   候选。**

### 2.3 新轴 B：引擎代际敏感性（V8 12.4 → 13.6 双引擎全锚复跑）

方法：同一 VM、同一字节的九格锚点 harness（附录 A）在 Node 22.22.2
（V8 12.4，规范引擎）与 Node 24.19.0（V8 13.6，R15-B/G 同版本）各跑
两遍（每遍 3 rep），逐格对照。十四轮全部 J 测量都在单一 22.x 引擎上
做出，引擎代际是从未直测的维度。

```text
I/O 六格（N22 带 → N24 带）：
  saveToDisk 50-obs    149.3–224.0 → 141.2–202.3 µs/写   （同带）
  saveToDisk 500-obs   415.6–520.8 → 449.8–572.3 µs/写   （同带）
  jsonl fsync=false     64.6–93.4  →  62.7–67.7  µs      （同带略窄）
  jsonl fsync=true     213.9–426.6 → 219.7–304.1 µs      （同带）
  级联 MATCH           724.4–949.1 → 666.3–806.8 µs      （同带）
  级联 NO-MATCH        267.3–342.1 → 244.8–277.8 µs      （同带毗邻）
CPU 三格（N22 带 → N24 带，方向注记）：
  contextIndex          40.3–41.4  →  37.4–38.2  µs/构建 （−7%，绝对 −3µs）
  plan 计数器            1.09–1.15  →  0.91–0.92  µs/plan （−17~−20%，绝对 −0.2µs）
  plan randomUUID        2.63–2.69  →  2.04–2.12  µs/plan （−22%，绝对 −0.6µs）
J1 sim：2468 项逐位双引擎全绿（N22 2612.5× / N24 2903.3×，§1.4）
A/A 交叉运行（n24-run1）：6 胞全 mixed，与 N22 三次同构（§2.2 表）
```

**裁决**：I/O 支配性拒绝对引擎代际**双向鲁棒**——(a) I/O 六格
syscall 束缚、引擎不变（全部同带），故引擎升级不改变本切片的地板
结构；(b) CPU 三格呈**均匀加速**（−7~−22%，与 R15-B 在 B 切片测得的
均匀 ~15–30% 红利同型；与 R15-G 在 G 切片的混号漂移不同——J 的 CPU
面是纯数据整形循环，无 G 的重放/校验型多态面，与均匀红利一致），
但绝对量 −0.2~−3.1µs/call 深度亚 ms，且更快的引擎只会把计算面压得
**更低于** I/O 地板（计算/落地线距离从 4–5 个数量级变 4–5 个数量级
略增）——没有任何格在任一引擎、任一方向越过或接近落地线，也没有
「为 V8 13.6 调形」的候选（引擎版本特化 + 绝对量亚 µs + I/O 支配
三重不合格）。引擎升级本身（CPU 面净 −7~−22%）是环境/运维事项
（R15-B §2e 先例），不构成切片生产代码优化、不铸 ID。测量卫生学
记档：后续轮次若在 Node 24 上重锚，index/plan 读数偏低 7–22% 属引擎
代际效应，勿误读为代码漂移；I/O 六格读数引擎不敏感可直接对带。
**该轴不产生任何候选。**

### 2.4 其余重扫形状对账

零 src 提交下数学上不可能出现新形状（§1.2）；本轮全量读码以两条新轴
为透镜对 29 文件做第十五遍普查——逐函数问 (a) 有无「历史拒绝其实被
噪声掩盖了真收益」的翻案空间（A 轴直接否定：稳态地板与被拒差额同阶
或更大且 0/24 伪稳，两端封死）；(b) 有无引擎代际暴露的新收益通道
（B 轴直接否定：均匀红利无口袋、I/O 地板引擎不变）。抽样复核与
R14-J §2.3 对账表逐条一致：`context/index.ts` L130–133 filter 后冗余
slice（S4-J-6 原文在位）、`mailbox.enqueue` 冗余 `byRole.set`
（S5-J-4 在位）、`export.ts` scopes includes（S3-J-6）、
`packet.collectCandidates` 的 `Object.keys(manifests).sort`
（S4-J-4/S7-J-2 家族换位点）、`closure.decideClosure` legacyMatch
模板字面量（S7-J-3）、`reduceEpisodeEvents` 不可变 reducer 拷贝
（S6-J-5 身份反例域）、file-lock acquire/release 协议（R9-J/R10-J
锁协议数据面）。

### 2.5 收口陈述

十五遍合计，本切片在当前调用图下**结构上不存在**达门槛候选：
（i）三条 I/O 路径地板 149.3–949.1µs，其上任何等价 CPU 重写不可
寻址（S0a–S0c 十五轮复现）；（ii）纯 CPU 面每 run 合计个位 µs~41µs，
距落地线 3–5 个数量级（S0d–S0e），且引擎代际只会拉大该距离（§2.3）；
（iii）本轮 A/A 标定证明：十五轮全部裁决所依赖的协议本身地板为
I/O ±34–233µs/操作、CPU ±0.006–2.0µs/call，被拒差额全部在地板内
**且**落地线远在地板上——历史裁决无一依赖亚噪声读数成立，也无一
真赢家可能被噪声掩埋；（iv）历史轮间 I/O 带漂移定量归因为协议+VM
噪声（与逐字节零 diff 互证）；（v）语义面（fail-closed 两读、串行
两写、fsync、catch-all 探针错误面、白名单拷贝、锁协议、崩溃序、
own-property 形状）全部是钉死反例/明文契约的实施点，无合法变形。
重开本切片的唯一前提维持 R9-J..R14-J 收口原文：调用图变更
（test-only 面接线生产热路径），或 preferences/feedback 存量真实
增长到 R14-J 标定的 N\* 量级（saveToDisk N\*≈1.1×10⁴ / 级联
N\*≈4×10³ / readAll L\*≈1.4×10⁴）——彼时候选是持久化形态变更
（原子改名写、增量落盘），须走数据面契约评审而非微观优化。

## 3. 逐文件收口（第十五遍新视角，其余与 R1-J…R14-J 一致）

| 文件 | 本轮测量学基底轴核查 | 结论 |
| --- | --- | --- |
| `cluster/mailbox.ts` | 零 fs；R12-J 位点 B 未补铸复核；五 ID + 拷贝契约维持 | 无新候选 |
| `cluster/spawn.ts` | 零 fs；R12-J 位点 A 双 trim 未补铸复核 | 无新候选 |
| `cluster/host.ts` | 零 fs；R11-J 位点 B 死复核原样保留 | 无新候选 |
| `privacy/deletion.ts` | **A/A 级联胞：协议噪声 ±168.7–232.8µs/次直测，覆盖本轮 match 带上缘超出；引擎轴：级联两格 N24 同带**；S5-J-3 两读顺序 + S6-J-1 串行两写在位复核（零 diff）；R13-J statExists 探针原样未重铸 | 无新候选 |
| `privacy/state-layout.ts` | 纯路径构造，零 fs | 无 |
| `privacy/record-classes.ts` | 零 fs；`durableRecordClassById` 零生产调用方维持 | 无新候选 |
| `preferences/loop-eval.ts` | **J1 在位，仿真 2468 项双引擎全绿（2612.5× / 2903.3×）——首个跨引擎逐位见证**；R14-J Cell D 零调用方裁决继承 | 无新候选 |
| `preferences/export.ts` | 零 fs；`exportForDataset` 零调用方维持 | 无新候选 |
| `preferences/precedence.ts` | 零 fs；整模块零生产调用方维持 | 整面关闭 |
| `preferences/materialize.ts` | 零 fs；`materializeView` 零调用方维持 | 无新候选 |
| `preferences/service.ts` | 零 fs；纯委托层 | 无 |
| `preferences/store.ts` | **A/A saveToDisk 胞：±33.5–64.1µs/写协议噪声直测，历史五轮带漂移定量归因；引擎轴双档 N24 同带**；R13-J loadFromDisk 双探针原样未重铸；S0a 双档重锚 149.3–520.8µs | 无新候选 |
| `preferences/types.ts` | 类型定义 | 无 |
| `episode/manager.ts` | 零 fs；不可变 reducer 拷贝面维持 | 无新候选 |
| `episode/store.ts` | 间接触盘（仅经 persist/jsonl）；jsonl 两胞 A/A + 引擎轴覆盖其 append/readAll 形态 | 无新候选 |
| `episode/closure.ts` | 零 fs；S2-J-8、S7-J-3、S9-J-4 维持 | 无新候选 |
| `episode/events.ts` | 类型定义 | 无 |
| `episode/replay.ts` | 零 fs；两导出零调用方维持 | 无新候选 |
| `persist/file-lock.ts` | 锁协议数据面第十五次零碰；R9-J/R10-J 拒列维持 | 无新候选 |
| `persist/jsonl.ts` | **A/A jsonl 双胞：fsync=false ±1.3–6.4µs / fsync=true ±45.4–174.8µs——S8-J-2「符号不稳」判据首次数值化；引擎轴双格 N24 同带**；S8-J-2 双句柄形态零改动；S0b 重锚 64.6–426.6µs | 无新候选 |
| `track/primary-split.ts` | 零 fs；纯组装层 | 无 |
| `track/plan.ts` | **A/A plan 胞：±0.006–0.058µs 稳态地板——历史 ns 级拒绝测量端证书；引擎轴 −17~−22% 均匀红利、绝对 −0.2~−0.6µs 亚 ms**；S0e 重锚 1.09–2.69µs/plan | 无新候选 |
| `track/clarify.ts` | 零 fs；habitsFromPreferences spread ns 级维持 | 无新候选 |
| `track/loop.ts` | S6-J-4 位点在位；S1-J-5、S2-J-11、S4-J-2 维持 | 无新候选 |
| `context/index.ts` | **A/A index 胞：±0.6–2.0µs 地板；引擎轴 −7%、绝对 −3µs 亚 ms**；R11-J 位点 A 未补铸；S0d 重锚 40.3–41.4µs | 无新候选 |
| `context/packet.ts` | 零 fs；R12-J 位点 C 未补铸；S1-J-7 原位在位 | 无新候选 |
| `feedback/redaction.ts` | 零 fs；S2-J-9、S3-J-1、S7-J-5、S9-J-1 维持 | 无新候选 |
| `feedback/store.ts` | A/A 级联胞 read/rewrite 两相实施点（S5-J-3 读端 / S6-J-1 链成员）；四 jsonl 调用方 fsync 旗标复核原样 | 无新候选 |
| `feedback/types.ts` | 类型定义 | 无 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。切片自 R1-J 落地 J1
（`fb41417`）起经 R2-J..R14-J 与本轮 R15-J 十四遍复查累计零后续代码
改动，逐字节一致。

## 5. 新增排除 ID

**无。** 第十五遍枚举为空——测量学基底透镜（§2.2/§2.3）证明：
(a) 协议噪声地板与历史被拒差额同阶且 0/24 伪稳 ⇒ 无历史拒绝可翻案、
无伪稳假赢家通道；(b) I/O 地板引擎不变、CPU 面均匀红利绝对量亚 ms ⇒
无引擎代际暴露的新候选；(c) 历史轮间 I/O 带漂移为协议+VM 噪声，非
代码漂移信号。两轴均为测量性裁决、非生产代码形态，按「换名/已闭域/
测量协议不铸 ID」纪律不铸。R13-J 两处理论失败探针与 R14-J 三条规模
越线数字按指令未重铸。与 R10-J..R14-J 及 R15-A..H 的空枚举收口同型。

## 6. 测试

零代码改动下相关基线复核，全绿（规范引擎 Node v22.22.2 via nvm，
满足 engines >=22.19.0；系统 Node 22.14.0 过低的既知环境注记同
R1-J；引擎轴另装 Node v24.19.0；`pnpm install --frozen-lockfile`
后）：

```bash
pnpm exec tsx scripts/r1j-equivalence-sim.ts   # Node 22.22.2
# ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)
# perf fixture: reference 10304.2 ms -> current 3.9 ms (2612.5x)
pnpm exec tsx scripts/r1j-equivalence-sim.ts   # Node 24.19.0（引擎轴附加）
# ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)
# perf fixture: reference 11098.3 ms -> current 3.8 ms (2903.3x)
pnpm gate   # typecheck + lint + test + build 全绿（Node 22.22.2）
# tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1（全套件，
# 含 cluster/privacy/preferences/episode/persist/track/context/feedback）
```

锚点重锚 harness 与 A/A 新轴 harness（临时脚本，无赢家、无新 ID，按
round01–14 仅赢家 sim 入库的仓库惯例未入库；出处与 sha256 见附录
A/B，工作区临时件已在 gate 前删除）。锚点两遍 × 双引擎、A/A 四次
运行方向与量级一致（数字见 §1.3 / §2.2 / §2.3）。

## 附录 A：I/O 地板矩阵 harness 出处与双引擎输出（可复现）

脚本与 R12-J 附录 A 归档件**逐字节同源**：自
`docs/reports/sota-opt/round-12/R12-J.md` 附录 A（```ts 围栏内全文）
原样提取为仓库根 `tmp-r15j-anchor.ts`，将其中 `r12j-` 临时目录前缀
替换为 `r15j-`，sha256 =
`b63cf4a323f5389a05d698746d2d589561c8d6a0e8fd06b8b284297145a4fd56`
（无赢家按纪律不入库，临时文件已删除）。运行方式：
`pnpm exec tsx tmp-r15j-anchor.ts`。本轮四遍输出（N22 两遍 + N24
两遍）：

```text
--- Node 22.22.2 (V8 12.4, 规范引擎) ---
run1  saveToDisk (50-obs): 224.0 / 177.6 / 156.4 us/write
      saveToDisk (500-obs): 520.8 / 421.3 / 415.6 us/write
      jsonl fsync=false: 64.6 / 65.6 / 80.2 us   fsync=true: 303.4 / 285.7 / 213.9 us
      cascade MATCH: 817.3 / 801.9 / 724.4 us   NO-MATCH: 315.0 / 342.1 / 304.9 us
      index: 40.4 / 40.3 / 40.4 us   plan counter: 1.09 / 1.09 / 1.10 us   plan uuid: 2.69 / 2.68 / 2.67 us
run2  saveToDisk (50-obs): 217.7 / 149.3 / 151.3 us/write
      saveToDisk (500-obs): 509.0 / 442.7 / 429.9 us/write
      jsonl fsync=false: 83.4 / 84.8 / 93.4 us   fsync=true: 420.1 / 426.6 / 424.1 us
      cascade MATCH: 949.1 / 861.8 / 816.8 us   NO-MATCH: 293.0 / 270.6 / 267.3 us
      index: 41.4 / 40.3 / 40.3 us   plan counter: 1.15 / 1.11 / 1.10 us   plan uuid: 2.64 / 2.64 / 2.63 us
--- Node 24.19.0 (V8 13.6, 引擎代际轴) ---
run1  saveToDisk (50-obs): 178.3 / 197.5 / 141.2 us/write
      saveToDisk (500-obs): 572.3 / 466.7 / 478.3 us/write
      jsonl fsync=false: 67.7 / 63.4 / 63.2 us   fsync=true: 284.9 / 247.9 / 219.7 us
      cascade MATCH: 728.6 / 733.7 / 666.3 us   NO-MATCH: 264.2 / 248.8 / 244.8 us
      index: 38.0 / 37.4 / 37.4 us   plan counter: 0.92 / 0.92 / 0.91 us   plan uuid: 2.12 / 2.06 / 2.06 us
run2  saveToDisk (50-obs): 202.3 / 182.7 / 157.0 us/write
      saveToDisk (500-obs): 532.9 / 450.5 / 449.8 us/write
      jsonl fsync=false: 64.8 / 62.8 / 62.7 us   fsync=true: 274.8 / 277.3 / 304.1 us
      cascade MATCH: 806.8 / 702.7 / 679.7 us   NO-MATCH: 270.3 / 277.8 / 260.1 us
      index: 38.2 / 37.6 / 37.5 us   plan counter: 0.91 / 0.92 / 0.91 us   plan uuid: 2.12 / 2.04 / 2.04 us
```

## 附录 B：A/A 噪声地板 harness（完整，可复现）与逐窗输出

`tmp-r15j-aa.ts`（sha256
`d340ec0745c373dac7868d8f12d0aee344d72118549fb46ee7d72fa82003e438`；
无赢家按纪律不入库，工作区临时件已删除）。夹具（preferences 种子 /
feedback 种子 / snapshot+codeMap / plan contract）与附录 A 锚点
harness **逐字相同**；每臂协议与对应锚点格逐字相同（同预热、同迭代
数）；每胞 8 窗、每窗 `delta = arm(A) − arm(B)`，A/B 为同一生产函数
与夹具。运行 `pnpm exec tsx tmp-r15j-aa.ts <label>`。

```ts
/**
 * R15-J new-axis harness: A/A noise-floor calibration for the J-slice
 * anchor/duel protocol (J-slice counterpart of the R15-B/G/D/E/F/H
 * measurement-science lens). Both arms run the SAME production function on
 * the SAME deterministic fixture (true effect exactly zero); per window,
 * delta = arm(A) - arm(B) under the exact per-cell protocol the fifteen
 * rounds of anchors used (same fixtures, same warm counts, same iteration
 * counts as the appendix-A harness). 6 cells x 8 windows per run.
 * A false-stable cell = 8/8 windows with the same sign.
 * Temporary script: not committed (no production winner -> report only).
 */
import { mkdtempSync, rmSync, mkdirSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join, dirname } from "node:path";

import {
  configurePreferencePersistence,
  recordPreference,
  resetPreferenceStore
} from "./src/preferences/store.js";
import { appendJsonlLine } from "./src/persist/jsonl.js";
import { cascadeFeedbackTombstones } from "./src/privacy/deletion.js";
import { feedbackLogPath, feedbackTombstonesPath } from "./src/feedback/store.js";
import { buildProjectContextIndex, type CodeMapSymbol } from "./src/context/index.js";
import { applyAnswers } from "./src/track/clarify.js";
import { planFromContract, acceptanceForRole } from "./src/track/plan.js";
import type { ProjectSnapshot } from "./src/domain/project.js";
import type { RequirementContract, DecisionQuestion } from "./src/domain/contract.js";
import type { EpisodeId } from "./src/domain/ids.js";

const hr = (): bigint => process.hrtime.bigint();
const us = (a: bigint, b: bigint, n: number): number => Number(b - a) / 1000 / n;

let idCounter = 0;
const gen = (): string => `bench${(idCounter++).toString(36)}`;

const WINDOWS = 8;

function report(name: string, deltas: number[]): void {
  const pos = deltas.filter((d) => d > 0).length;
  const neg = deltas.filter((d) => d < 0).length;
  const maxAbs = Math.max(...deltas.map((d) => Math.abs(d)));
  const stable = pos === WINDOWS || neg === WINDOWS ? "FALSE-STABLE" : "mixed";
  console.log(
    `${name}: [${deltas.map((d) => d.toFixed(3)).join(", ")}]  sign ${pos}+/${neg}-  max-abs ${maxAbs.toFixed(3)}  ${stable}`
  );
}

// ---------------------------------------------------------------- cell 1
// saveToDisk write path A/A: each arm = fresh store dir, 50-obs seed,
// 200 timed recordPreference writes (identical to anchor cell 1 rep).
function saveArm(): number {
  const dir = mkdtempSync(join(tmpdir(), "r15j-aa-pref-"));
  resetPreferenceStore();
  configurePreferencePersistence(join(dir, "prefs.json"));
  for (let i = 0; i < 50; i++) {
    recordPreference(
      (["user", "project", "role"] as const)[i % 3]!,
      `key-${i % 7}`,
      `pref-${i % 11}`,
      i % 2 === 0 ? `value-${i}` : i,
      `eps_seed${i.toString(36)}` as EpisodeId,
      1.0,
      i % 4 === 0
    );
  }
  const N = 200;
  const t0 = hr();
  for (let i = 0; i < N; i++) {
    recordPreference(
      "project",
      `key-${i % 7}`,
      `pref-${i % 11}`,
      `value-w${i}`,
      `eps_w${i.toString(36)}` as EpisodeId,
      1.0,
      false
    );
  }
  const t1 = hr();
  resetPreferenceStore();
  configurePreferencePersistence(undefined);
  rmSync(dir, { recursive: true, force: true });
  return us(t0, t1, N);
}

// ---------------------------------------------------------------- cell 2
function seedFeedback(stateRoot: string): void {
  const log = feedbackLogPath(stateRoot);
  mkdirSync(dirname(log), { recursive: true });
  const lines: string[] = [];
  for (let i = 0; i < 200; i++) {
    const hit = i % 10 === 0;
    lines.push(
      JSON.stringify({
        id: `fb-${i}`,
        episodeId: hit ? "eps_hit" : `eps_other${i % 7}`,
        kind: "human",
        rubricVersion: "r1",
        score: 50 + (i % 40),
        evidenceRefs: [],
        redacted: false,
        createdAt: "2026-08-24T00:00:00.000Z",
        body: `free text feedback payload number ${i} `.repeat(3),
        summary: `summary ${i}`
      })
    );
  }
  writeFileSync(log, lines.join("\n") + "\n", "utf8");
  writeFileSync(
    feedbackTombstonesPath(stateRoot),
    JSON.stringify(["fb-pre-a", "fb-pre-b", "fb-pre-c", "fb-pre-d", "fb-pre-e"], null, 2) + "\n",
    "utf8"
  );
}

// cascade MATCH A/A: each arm = 30 timed cascades over re-seeded state
// (identical to anchor cell 3 match rep).
async function cascadeArm(): Promise<number> {
  const stateRoot = mkdtempSync(join(tmpdir(), "r15j-aa-casc-"));
  seedFeedback(stateRoot);
  await cascadeFeedbackTombstones(stateRoot, "eps_warm" as EpisodeId);
  const iters = 30;
  let total = 0n;
  for (let i = 0; i < iters; i++) {
    seedFeedback(stateRoot);
    const t0 = hr();
    const out = await cascadeFeedbackTombstones(stateRoot, "eps_hit" as EpisodeId);
    const t1 = hr();
    if (out.length !== 20) throw new Error(`expected 20 cascaded, got ${out.length}`);
    total += t1 - t0;
  }
  rmSync(stateRoot, { recursive: true, force: true });
  return Number(total) / 1000 / iters;
}

// ---------------------------------------------------------------- cells 3+4
// jsonl append A/A (fsync=false 2000 iters / fsync=true 300 iters),
// identical to anchor cell 2 rep.
async function jsonlArm(fsync: boolean, iters: number): Promise<number> {
  const dir = mkdtempSync(join(tmpdir(), "r15j-aa-jsonl-"));
  const file = join(dir, "log.jsonl");
  const line = JSON.stringify({ id: "evt_x", type: "BENCH", payload: { n: 42, text: "x".repeat(120) } });
  for (let i = 0; i < 50; i++) await appendJsonlLine(file, line, false);
  const t0 = hr();
  for (let i = 0; i < iters; i++) await appendJsonlLine(file, line, fsync);
  const t1 = hr();
  rmSync(dir, { recursive: true, force: true });
  return us(t0, t1, iters);
}

// ---------------------------------------------------------------- cell 5
function makeSnapshot(): { snapshot: ProjectSnapshot; codeMap: CodeMapSymbol[] } {
  const facts: ProjectSnapshot["facts"] = [];
  for (let i = 0; i < 12; i++) facts.push({ key: `architecture.layer${i}`, value: `layer ${i} does thing ${i}`, confidence: "HIGH" });
  for (let i = 0; i < 12; i++) facts.push({ key: `risk.area${i}`, value: `risk in area ${i}`, confidence: "MEDIUM" });
  for (let i = 0; i < 16; i++) facts.push({ key: `misc.fact${i}`, value: `misc value ${i}`, confidence: "LOW" });
  const codeMap: CodeMapSymbol[] = [];
  for (let i = 0; i < 30; i++) {
    codeMap.push({
      path: `src/mod${i % 8}/file${i}.ts`,
      symbol: `symbol${i}`,
      kind: (["class", "function", "interface", "type", "method", "module"] as const)[i % 6]!,
      public: i % 3 === 0,
      calls: [`callee${i % 5}`, `callee${(i + 2) % 5}`]
    });
  }
  const snapshot = {
    id: "prj_bench0001",
    rootPath: "/repo",
    discoveredAt: "2026-08-24T00:00:00.000Z",
    instructionFiles: [{ path: "/repo/AGENTS.md" }, { path: "/repo/docs/AGENTS.md" }, { path: "/repo/src/deep/AGENTS.md" }],
    manifests: [{ path: "/repo/package.json" }, { path: "/repo/pnpm-lock.yaml" }, { path: "/repo/tsconfig.json" }, { path: "/repo/sub/package.json" }],
    commands: [
      { name: "test", command: "pnpm test" },
      { name: "lint", command: "pnpm lint" },
      { name: "build", command: "pnpm build" }
    ],
    facts
  } as unknown as ProjectSnapshot;
  return { snapshot, codeMap };
}

const indexFixture = makeSnapshot();
const indexOptions = {
  dirtyPaths: ["/repo/src/a.ts", "/repo/src/b.ts", "/repo/gen/out.js", "/repo/src/c.ts", "/repo/docs/x.md", "/repo/src/d.ts"],
  generatedPaths: ["/repo/gen", "/repo/dist", "/repo/.cache"],
  sourceHashes: { "/repo/package.json": "h1", "/repo/AGENTS.md": "h2", "architecture.layer0": "h3" },
  priorHashes: { "/repo/package.json": "h1", "/repo/AGENTS.md": "hOLD" },
  codeMap: indexFixture.codeMap
};

let indexSink = 0;
// index A/A: each arm = 2000 warm + 20000 timed builds
// (identical to anchor cell 4 rep).
function indexArm(): number {
  for (let i = 0; i < 2000; i++) indexSink += buildProjectContextIndex(indexFixture.snapshot, indexOptions).facts.length;
  const N = 20000;
  const t0 = hr();
  for (let i = 0; i < N; i++) indexSink += buildProjectContextIndex(indexFixture.snapshot, indexOptions).facts.length;
  const t1 = hr();
  return us(t0, t1, N);
}

// ---------------------------------------------------------------- cell 6
const planQuestions: DecisionQuestion[] = [
  { id: "q-tests", question: "Include tests?", options: ["yes", "no"], default: "yes" },
  { id: "q-done", question: "What does done look like?", options: [], default: "shipped" },
  { id: "q-write", question: "Write code or investigate?", options: ["write", "investigate"], default: "write" }
];
const planAnswers = { "q-tests": "yes", "q-done": "feature shipped and verified", "q-write": "write the code" } as const;
const planContract: RequirementContract = {
  schemaVersion: 1,
  objective: "Implement the new export module and add tests covering redaction",
  deliverables: [],
  constraints: [{ id: "c-smallest", description: "smallest change", enforceable: false } as never],
  nonGoals: [],
  acceptanceCriteria: [
    { id: "ac-1", description: "Export module produces valid JSON", observableCheck: "run export" },
    { id: "ac-tests", description: "Unit tests pass", observableCheck: "pnpm test" },
    { id: "ac-2", description: "Redaction verified on fixtures", observableCheck: "fixture diff" }
  ],
  assumptions: [],
  questions: planQuestions,
  authority: [],
  sourceRefs: []
};
const planHabits = { preferReview: true } as const;

let planSink = 0;
// plan CPU A/A (counter gen): each arm = 20000 warm + 200000 timed plans
// (identical to anchor cell 5 counter rep).
function planArm(): number {
  for (let i = 0; i < 20000; i++) {
    const applied = applyAnswers(planQuestions, planAnswers);
    const children = planFromContract({ contract: planContract, habits: planHabits, answers: applied.resolved, generateId: gen });
    for (const child of children) planSink += acceptanceForRole(child.role, planContract).length;
  }
  const N = 200000;
  const t0 = hr();
  for (let i = 0; i < N; i++) {
    const applied = applyAnswers(planQuestions, planAnswers);
    const children = planFromContract({ contract: planContract, habits: planHabits, answers: applied.resolved, generateId: gen });
    for (const child of children) planSink += acceptanceForRole(child.role, planContract).length;
  }
  const t1 = hr();
  return us(t0, t1, N);
}

// ---------------------------------------------------------------- main
async function main(): Promise<void> {
  console.log(`node ${process.version} label=${process.argv[2] ?? "?"}`);

  const save: number[] = [];
  for (let w = 0; w < WINDOWS; w++) save.push(saveArm() - saveArm());
  report("saveToDisk(us/write)   ", save);

  const casc: number[] = [];
  for (let w = 0; w < WINDOWS; w++) casc.push((await cascadeArm()) - (await cascadeArm()));
  report("cascadeMATCH(us/casc)  ", casc);

  const jf: number[] = [];
  for (let w = 0; w < WINDOWS; w++) jf.push((await jsonlArm(false, 2000)) - (await jsonlArm(false, 2000)));
  report("jsonl fsync=false(us)  ", jf);

  const jt: number[] = [];
  for (let w = 0; w < WINDOWS; w++) jt.push((await jsonlArm(true, 300)) - (await jsonlArm(true, 300)));
  report("jsonl fsync=true(us)   ", jt);

  const idx: number[] = [];
  for (let w = 0; w < WINDOWS; w++) idx.push(indexArm() - indexArm());
  report("contextIndex(us/build) ", idx);

  const plan: number[] = [];
  for (let w = 0; w < WINDOWS; w++) plan.push(planArm() - planArm());
  report("planCPU counter(us)    ", plan);

  console.log(`[sinks index=${indexSink} plan=${planSink}]`);
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
```

逐窗实测输出（3 次 N22 + 1 次 N24）：

```text
--- run1 @ v22.22.2 ---
saveToDisk(us/write)   : [54.078, 12.290, -5.651, -5.160, 2.916, -3.075, -10.911, 15.786]  sign 4+/4-  max-abs 54.078  mixed
cascadeMATCH(us/casc)  : [46.344, -60.064, 32.253, -4.850, -13.106, 4.077, 214.295, 14.157]  sign 5+/3-  max-abs 214.295  mixed
jsonl fsync=false(us)  : [3.846, -2.467, 0.068, 0.059, -1.855, 0.231, 1.428, -1.745]  sign 5+/3-  max-abs 3.846  mixed
jsonl fsync=true(us)   : [25.429, -32.139, -45.435, -16.141, -4.546, -43.862, 40.561, -23.708]  sign 2+/6-  max-abs 45.435  mixed
contextIndex(us/build) : [0.275, -0.016, -0.065, 0.600, -0.092, 0.005, -0.039, -0.008]  sign 3+/5-  max-abs 0.600  mixed
planCPU counter(us)    : [-0.002, -0.001, -0.001, -0.001, -0.001, 0.003, -0.017, 0.004]  sign 2+/6-  max-abs 0.017  mixed
--- run2 @ v22.22.2 ---
saveToDisk(us/write)   : [64.116, 12.831, 8.463, 30.677, -7.018, -0.195, 15.128, 19.303]  sign 6+/2-  max-abs 64.116  mixed
cascadeMATCH(us/casc)  : [150.811, -18.807, 61.822, -95.208, 37.350, 43.744, 168.682, -22.334]  sign 5+/3-  max-abs 168.682  mixed
jsonl fsync=false(us)  : [6.243, -0.439, -0.392, -0.415, -0.150, -0.621, 0.977, 0.167]  sign 3+/5-  max-abs 6.243  mixed
jsonl fsync=true(us)   : [7.049, 16.670, 19.105, -21.068, -13.028, -60.665, 92.163, 39.793]  sign 5+/3-  max-abs 92.163  mixed
contextIndex(us/build) : [-0.129, -1.118, 2.027, 0.330, -0.544, -0.345, 0.170, 0.474]  sign 4+/4-  max-abs 2.027  mixed
planCPU counter(us)    : [0.009, -0.058, -0.027, 0.019, 0.009, -0.020, -0.003, 0.011]  sign 4+/4-  max-abs 0.058  mixed
--- run3 @ v22.22.2 ---
saveToDisk(us/write)   : [24.742, 30.280, -12.130, -3.300, -5.478, -33.530, 0.616, -12.015]  sign 3+/5-  max-abs 33.530  mixed
cascadeMATCH(us/casc)  : [113.966, -206.415, 23.682, -17.829, 23.624, 40.916, 232.793, 4.529]  sign 6+/2-  max-abs 232.793  mixed
jsonl fsync=false(us)  : [6.409, 0.244, -4.176, 0.432, -1.213, -2.745, -0.027, -1.071]  sign 3+/5-  max-abs 6.409  mixed
jsonl fsync=true(us)   : [30.560, 174.846, 119.616, 6.405, -93.338, 100.473, 70.780, -1.873]  sign 6+/2-  max-abs 174.846  mixed
contextIndex(us/build) : [0.745, 0.152, 0.955, 0.676, -0.065, -0.245, 0.941, 0.140]  sign 6+/2-  max-abs 0.955  mixed
planCPU counter(us)    : [-0.008, 0.003, -0.008, -0.000, 0.005, -0.005, -0.004, -0.025]  sign 2+/6-  max-abs 0.025  mixed
--- n24-run1 @ v24.19.0 ---
saveToDisk(us/write)   : [39.502, -43.497, 3.100, -31.469, 15.852, 43.317, -9.423, 9.073]  sign 5+/3-  max-abs 43.497  mixed
cascadeMATCH(us/casc)  : [17.336, -58.715, 16.302, 17.618, -5.646, -231.572, 35.802, 14.931]  sign 5+/3-  max-abs 231.572  mixed
jsonl fsync=false(us)  : [-0.507, -0.632, 0.870, 1.169, -0.933, -1.279, 0.916, 0.941]  sign 4+/4-  max-abs 1.279  mixed
jsonl fsync=true(us)   : [73.887, 73.558, 93.456, 47.461, -44.038, 46.151, -12.346, 14.116]  sign 6+/2-  max-abs 93.456  mixed
contextIndex(us/build) : [0.608, -0.018, -0.003, -0.308, 0.176, -0.176, -0.071, 0.410]  sign 3+/5-  max-abs 0.608  mixed
planCPU counter(us)    : [-0.006, -0.002, -0.001, 0.006, -0.005, -0.003, -0.002, 0.003]  sign 2+/6-  max-abs 0.006  mixed
falseStable: 0/24 胞（全 192 窗无 8/8 同号集）
```

MORE_OPTIMA=no
BRANCH=cursor/r15-j-persist-fifteenth-pass-83a1
