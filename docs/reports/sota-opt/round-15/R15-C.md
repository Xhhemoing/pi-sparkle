MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 15 / R15-C：离线路由切片第十五遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 15 / R15-C（Round 1–14 同区第十五遍，叠在
  已落地的 S7-C 之上）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `d148d12`（R14-J/R15-A/R15-B
  空枚举合入 + Round 14 收口 + R15-C 派单后的独占分支最新 tip，与派单预期一致；
  `git log -1 -- src/routing/` 显示切片最后一次生产改动**仍为 `183df9b`
  「perf(routing): land S7-C support-only eta」**，其后基线区间仅文档更新。
  `git diff 183df9b..HEAD --` 对 9 个切片文件为**空**（stat 输出为空、exit 0），
  本轮全部测量在该形态之上，未回退未重写。R15-D / R15-E 在飞，未等待、
  未触碰其分支与文件）
- **分支:** `cursor/r15-c-offline-routing-fifteenth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十五组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第十五遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C/R12-C/R13-C/R14-C 的全部
无号关闭探针）后，本轮找到**两个从未点名的微观站点**并新增**两条测量
轴**（R15-A/B 先例），全部以关闭探针（不铸号）收口——没有任何一个达到
接受门槛：

- **STORD（S6-C 累加 switch case 体内语句次序置换，关闭探针，不铸
  号）**——从未有 ID 点名过落地 case 体**内部**的语句交错次序这一位置
  （SWORD 是 case **标签**次序、S6-C-3 是 ai 滚动混合结构、S6-C-4 是
  bi 循环展开因子、S6-C 是分派本体——均非体内语句调度）。把每个
  case 体内「xtwz 存储与其 xtwx 行块交错」改为「全部 xtwz 存储前提、
  行块后置」是结构性逐位等价：xtwz 与各 xtwx 行是互不相交的新分配
  目标、每行每单元恰收到一个加数、单元级跨行次序不变（仍过 53 例
  四道电池全绿 × 3 次运行）。位置交替决斗（R14-C 方法论）三次运行
  delta **+9.9 / +7.8 / +2.0 ms（landed 减 stord，stord 侧「更快」，
  同号但幅度 5 倍摆动）**——即使按面值全收，最大读数 9.9 ms 仍低于
  ±35 ms 带 ~3.5×，且先验池算术封顶：累加 switch 是 irls 自身池
  （156.6–164.2 ms）的真子集，存储调度类变换的理论上界远够不到越带。
  贴带不落地线之下一个档位，关闭。
- **RIDGE（岭项对角加法循环整池，关闭探针，不铸号）**——落地的
  每迭代 `xtwx[d][d] += 1e-6` 循环从未被单独测过池（R11-C 的 RID 只
  关闭了唯一的结构变换——预置进 fill 循环 = 归约重排，3,792/10,000
  逐位反例）。精确计数 60 加法 × 8,966 迭代 = **537,960 次对角加法/
  报告**，独立计时 **3.08–3.85 ms/报告封顶**，亚带 ~9–11×；除 RID
  外的另一条结构出路（把岭语义融进 `solveSymmetric`）会把估计器私有
  语义推进公开通用求解器契约（X1-2/X0-4 族）。算术关闭。
- **AAFLR（A/A 决斗地板，新轴，非候选）**——两个**字节相同**的
  plain 落地道（独立闭包身份）按位置交替交错决斗，三次运行 A/A
  中位 delta **+0.2 / −1.1 / +1.4 ms**——本 VM 上「中位数级」决斗
  方法论的分辨率地板约 ±1.5 ms；而单 rep 漂移可达 +90 ms（run C 两
  道各出现一个 761 ms 环境漂移 rep，交替设计使其对称落在两侧），
  坐实「中位数 + 位置交替」纪律的必要性，并给 STORD 的同号读数提供
  校准参照（其 2.0–9.9 ms 幅度不稳，位于 A/A 地板与噪声带之间的
  深亚带区间）。
- **GCAX（GC 停顿份额，新轴，非候选）**——`PerformanceObserver('gc')`
  跨 7 次生产报告实测 **210 次 GC 事件、41.2–41.5 ms 总停顿 =
  5.89–5.93 ms/报告（占中位 0.88–0.89%）**。即使把全部收集器停顿
  清零也只省 ~6 ms，亚带 ~6×——ALLOC（R11-C）封顶了分配侧缓冲成本
  （2.0–3.5 ms），本轴补上收集器侧停顿，分配‑回收两侧合计仍深度
  亚带。方法论脚注：GC 条目在 Node 22.22.2 上经计时器队列派发而非
  setImmediate（本轮实测坐实），run A 的 GCAX 行因此读到 0 属仪表
  冲刷缺陷，修正后 run B/C 两次读数一致；run A 其余读数不受影响。

除上述站点与轴外，第十五遍逐行重读**零新发现**：其余每一个执行站点
都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 / 结构交换负
优化）。三大池地板与组合天花板复核维持（§3）：solve 452.8–453.8 ms
为冻结浮点序列本体；irls 自身 156.6–164.2 ms 可省项全闭（本轮 STORD
关闭语句调度位置、RIDGE 坐实岭循环 3–4 ms 池）；APC 54.1–54.2 ms 在
R14-C 重锚定带（53.0–55.6）内故按规免于重测地板（组合天花板关闭
原样维持）。小形状档整报告中位 **30.8–32.7 ms**，整条报告低于噪声带
（R7-I 矩阵义务履行）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11），live 面零文件改动。仓库变更仅本报告一个文件；无生产赢家故
未提交 `scripts/round15-r15c-equivalence-sim.ts`（败者/锚点仿真全文进
附录，遵守 R7-C..R14-C 先例）。

## 0. 范围与约束遵守

- 读序（强制）已履行：README → EXCLUSIONS.md（完整表，含 S12-C-1 与
  S13-B-1）→ round-15/PLAN.md → R14-C.md（空枚举确认，无 S14-C-\*；
  四个无号关闭探针 SWORD/XF64/IRLSDP/CMPH 与 APCX/TIMER/FLOORX 重锚定
  记名）→ R13-C.md（空枚举；PIVCSE/FINTAIL/REFZ/TSORT/DEG）→
  R12-C.md（S12-C-1 淘汰 + FZERO/PIVSWAP/BASE/SAMP/MUCSE）→
  R11-C.md（空枚举；RID/CNT/AUGF/EXP/ALLOC/DMAP/COPY/SCAN）→
  9 个切片源文件全量重读。
- 基线漂移检查：基线 `d148d12` 相对 R14-C 基线 `76e31da` 仅文档合入
  （R14-G/H/J 与 R15-A/B 空枚举收口、Round 14 关闭、Round 15 PLAN 与
  派单）；`git log -1 -- src/routing/` 仍为 `183df9b`（S7-C 落地
  本身），`git diff 183df9b..HEAD --` 对 9 文件为空（stat 空输出、
  exit 0），落地形态原样在位。既有回归资产七连全绿且检查数逐一匹配
  锚定值（§5）。
- 调用图确认（派单强制项，本轮 rg 复核）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 消费（双归因两路在位）；
  `solveSymmetric` 仅被 `offline-logit.ts` 导入；`src/supervisor/`
  对 9 个切片文件的导入为**零**——live = R0 等价维持，R1 未接线，
  `src/supervisor/model-router.ts` 零字节未触。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S10-\*、S12-C-1 全系未重提、未立道、
  未重测；R13-C 五个无名微观（PIVCSE/FINTAIL/REFZ/TSORT/DEG）与
  R14-C 四个无名微观（SWORD/XF64/IRLSDP/CMPH）未重探未补铸未换址
  重提；RID/CNT 归约重排反例原样引用（RIDGE 与 RID 的区分见 §4）；
  嵌套序 × 布局组合空间（S4-C-2 + S5-C-1..4 + S12-C-1）未重开
  （STORD 是 irls 侧 case 体内语句调度，非 solve 侧任何形态）；
  CMB 组合未重堆叠；APC 组合天花板仅引用 R14-C 重锚定结果
  （13.6–21.9 < 35）与 R11-C 算术（30.8–32.0 < 35），未重测未立道
  ——本轮池读数 54.1–54.2 落在 R14-C 重锚定带 53.0–55.6 内，按
  R14-C §3 末处置规则（池漂出带才随池重锚地板）免于重测。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动；X2-3 数值路径未触；单线程逐位审计基底不动。
- 环境：Node 22.22.2（VM 默认 22.14.0 低于 engines ≥22.19.0，nvm
  切换并前置 PATH，与 R1-C..R14-C 同处理）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`。

## 1. 重剖析：S7-C 后成本模型（本 VM 实测，未拷贝历轮数字）

测量器为**仪表化逐字复刻管线**（与 183df9b 生产 irls/solve/on-prob/
APC 逐语句相同，仅在调用边界加 performance.now() 与计数器，浮点操作/
操作数/顺序零改动），其忠实性先经 **53 例全形状电池 ×{仪表道,
纯净落地道 A, 纯净落地道 B, 纯净语句置换道} vs 生产 `fitLogitAdditive`
导入逐位全绿**（Object.is 含符号零）再采信读数；生产中位数由**未仪表
化生产导入**直接测得。perf 夹具与 R10-C..R14-C 完全一致
（fixtureRng(0xbeef)，4 场景 × 6 模型 × 8 项目，rows=400，
passRate=0.6，bootstrap=200, seed=20260818 → p=60，201 拟合，
8,966 次 IRLS 迭代）。

生产整报告中位（每次独立进程 7 测量取中位，三次正典运行）：**660.6 /
665.2 / 667.1 ms**（同 VM 独立旁证：r7c 回归资产锚点 **677.5 ms =
1.10× vs 冻结 S6-C ctl**、r3c 资产内生产道 663.3 ms）——与派单已知带
657.6–689.3（R14-C 本带）逐位重叠，带内 ~1% 漂移，预期之内。

仪表化池表（perf 形状，每次运行内 3 次仪表化取中位；三次正典运行
读数并列）：

| 池 | run A | run B | run C | 占比 | 站点计数（精确） |
| --- | --- | --- | --- | --- | --- |
| 总计 | 683.3 | 676.8 | 680.4 | 100% | fits=201 |
| `solveSymmetric` | **453.2** | **452.8** | **453.8** | **66.3–66.9%** | 8,966 次调用 |
| `irls` 自身（扣 solve） | **164.2** | **156.6** | **158.8** | **23.1–24.0%** | 8,966 迭代 / 3,586,400 行访问 / 1,249,652 sigmoid / **岭对角加法 537,960** |
| APC 全站点 | 54.2 | 54.1 | 54.2 | 7.9–8.0% | 277,606 活跃对 / 12,259 次调用 |
| `onProbabilitiesFor` | 7.0 | 7.0 | 7.3 | 1.0–1.1% | 80,400 sigmoid+dot |
| bootstrap 采样 | 1.6 | 1.6 | 1.7 | 0.2% | —（SAMP 池 R12-C 已封顶） |
| 排序/分位尾 | 1.7 | 1.6 | 1.6 | 0.2% | 12,200 draws-map ops |
| 基行投影 | 0.006 | 0.009 | 0.005 | ~0% | 每报告 1 次（R12-C BASE 已封顶） |
| 编排余量（build/supports/keys） | 1.6 | 3.2 | 2.9 | 0.2–0.5% | — |

除 solve / irls / APC 外每池整池清零也低于 ±35 ms 带（R9-C..R14-C
同结论，逐池复测未漂移）；solve 与 irls 为落地线唯二宿主。**APC 池
本 VM 三次读数 54.1–54.2 ms（互差 0.1）落在 R14-C 重锚定带
53.0–55.6 内**——R14-C 的三重探针归因（漂移坐在不可省扫描+sigmoid
地板内、组合天花板 13.6–21.9 < 35）在本 VM 上按规免于重测，组合
关闭原样维持。本轮新增仪表计数：岭对角加法 537,960 次/报告（RIDGE
坐实用）。GC 轴（GCAX）读数 5.89–5.93 ms/报告（0.88–0.89%），
收集器侧停顿非隐藏池。小形状档（3 模型 × 3 项目 / 120 行，
bootstrap=200）整报告中位 **30.8 / 32.1 / 32.7 ms**——小配置态整条
报告低于噪声带，调用矩阵小态一侧整体收口（R7-I 矩阵义务履行）。

生产自确定性复核：3 seed × 各 2 次运行全字段 Object.is 全绿（含于
§5 的 11,436 项检查 × 3 次正典运行）。

## 2. 第十五组枚举（诚实结果：空；两个无号关闭探针 + 两条新轴）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过。** 从未点名的站点全部在下表关闭（无一铸号；探针数字
为本 VM 实测，出处为附录测量彩排三次正典运行）：

| 站点/轴（从未点名） | 关闭方式 | 证据 |
| --- | --- | --- |
| STORD：S6-C 累加 switch case 体内语句次序置换（xtwz 存储全部前提于 xtwx 行块） | 先验池算术封顶 + 同号但幅度不稳且深亚带 | 结构性逐位等价（xtwz 与 xtwx 行互不相交、每单元每行恰一个加数、单元级跨行次序不变；53 例 × 置换道电池全绿 × 3 运行）；位置交替决斗 delta **+9.9 / +7.8 / +2.0 ms（landed 减 stord）**——同号但幅度 5 倍摆动，最大读数亚带 ~3.5×；存储调度类上界为累加 switch 池真子集（< irls 自身池 156.6–164.2），结构上够不到越带 |
| RIDGE：岭项对角加法循环整池（`xtwx[d][d] += 1e-6` × 60 × 8,966） | 算术上界深度亚带 + 结构出路撞已关 ID/公开契约 | 精确计数 **537,960 次对角加法/报告**；独立计时 **3.08–3.85 ms/报告封顶**，亚带 ~9–11×；结构变换仅两条：预置进 fill 循环 = RID 归约重排逐位违例（R11-C，3,792/10,000 反例）；融进 `solveSymmetric` = 把估计器私有岭语义推进公开通用求解器契约（X1-2/X0-4 族） |
| AAFLR：A/A 决斗地板（新轴，非候选） | 方法论校准，无候选形态 | 字节相同双 plain 道位置交替决斗，A/A 中位 delta **+0.2 / −1.1 / +1.4 ms**——中位级分辨率地板 ~±1.5 ms；单 rep 环境漂移可达 +90 ms（run C 双道各一个 761 ms rep，对称落两侧）——坐实中位数+交替纪律，校准 STORD 读数的解释 |
| GCAX：GC 停顿份额（新轴，非候选） | 整轴清零也亚带 | 7 次生产报告实测 **210 次 GC / 41.2–41.5 ms = 5.89–5.93 ms/报告（0.88–0.89%）**；全部停顿清零省 ~6 ms，亚带 ~6×；与 ALLOC（分配侧 2.0–3.5 封顶）合计分配‑回收两侧全亚带 |

以上无一构成候选：STORD 是零浮点机制的存储调度置换，先验被池算术
封顶，实测最大读数也只有带宽 ~29%（「贴带不落地」线之下一个档位；
其同号性按 AAFLR 地板校准后不排除为真实 2–10 ms 级微效应，但幅度
不稳且结构上永远够不到越带）；RIDGE 池为 3–4 ms 级且两条结构出路
分别撞 RID 反例与公开契约；AAFLR/GCAX 是测量轴不是变换。**故第十五
组候选集为空，不铸 S15-C-1 及后续编号**，遵循 R11-C（RID/CNT/AUGF/
EXP/ALLOC/DMAP/COPY/SCAN）、R12-C（FZERO/PIVSWAP/BASE/SAMP/MUCSE）、
R13-C（PIVCSE/FINTAIL/REFZ/TSORT/DEG）与 R14-C（SWORD/XF64/IRLSDP/
CMPH）无号关闭先例。

**枚举收口：** 除上表外，第十五遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖；本轮
重读零新发现——posterior 的 80 次二分 × betacf 每报告一次为亚 ms
且 X1-3 冻结替换；prob-add 的 diagnose 双层 entries 展开在整报告
亚 ms 池内且 S1-C-6/7 在位；estimateFingerprint 是 S1-C-4 本体）。
`offline-logit.ts` 与 `lin-alg.ts` 的剩余站点经本轮两个新位置关闭
后，四分类（重命名 / 亚带 / 逐位违例 / 结构交换负优化）继续穷尽。
两处顺带坐实的零池站点（不铸号，非探针）：solve 回代输出数组
`solution` 的分配是返回值本体，按构造无可省池；`fitLogitAdditive`
尾部诊断段（一次 betaQuantileLcb + 两个 Set + reduce）位于已测
排序/分位尾池（1.6–1.7 ms）内，被池算术覆盖。本轮核实换名再提交
拒列：零——没有任何已关 ID 的换址/换名形态在本轮出现。

## 3. 三大池地板与组合出路复核

- **solve 池 452.8–453.8 ms**：消元主体 ≈ 6.45 亿次冻结顺序浮点
  乘减/报告。R6-C §2.5 七类分解 + S10-C-3（对称镜像）+ S12-C-1
  （嵌套序交换，稳定 ~3.6× 负优化）+ R11-C COPY（防御拷贝契约
  封闭）+ R12-C FZERO/PIVSWAP + S5-C-6 + R13-C PIVCSE/FINTAIL +
  R14-C XF64（跨边界输入表示）之后，本轮**零新位置**（STORD 是
  irls 侧站点）。触碰地板本体的机制只剩 SIMD/线程（S9-C-1/2 +
  不变量禁止）与数值路径改道（X2-3 冻结）。
- **irls 自身池 156.6–164.2 ms**：可省项全部已关闭（S2-C/S6-C/S7-C
  落地本体之上：S10-C-1 决斗带内、S9-C-3 符号翻转、S6-C-6 上界
  2–4、S5-C-7 无正决斗、S9-C-4、S4-C-5/6、S2-C-3、S8-C-3、R11-C
  RID/CNT 归约重排反例、EXP 16–21 亚带、ALLOC 2–3.5 封顶、R12-C
  MUCSE 纯抖动、R14-C SWORD 标签序/IRLSDP 死参）。本轮 STORD 关闭
  case 体内语句调度（最后一个从未点名的调度类位置：标签序 SWORD、
  体内语句序 STORD 至此双面收口）、RIDGE 坐实岭循环池 3.08–3.85 ms
  ——irls 侧从未点名位置再次清零。
- **APC 池 54.1–54.2 ms**：落在 R14-C 重锚定带 53.0–55.6 内（三次
  读数互差仅 0.1 ms），按 R14-C §3 末处置规则免于重测地板——
  组合天花板关闭原样维持（R14-C 本 VM 类直测 13.6–21.9 < 35；
  R11-C 分项算术 30.8–32.0 < 35 双重覆盖；S8-C-2/S10-C-2/S2-C-4/
  S4-C-4 全部维持关闭，CMB 未重堆叠）。R13-C REFZ 参考水平份额
  不受影响。
- **分配‑回收两侧（本轮新轴收口）**：分配侧 ALLOC 2.0–3.5 ms 封顶
  （R11-C）+ 收集器侧 GCAX 5.89–5.93 ms（0.88–0.89%）——两侧合计
  <10 ms，即使全部清零也亚带 ~4×；GC 非隐藏池，池表读数未被收集器
  停顿污染出带。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测 +8.8~+30.5 带级
  摆幅带内），维持关闭。本轮两个探针无一是稳定越带正项（STORD
  2.0–9.9 幅度不稳且亚带、RIDGE 3–4 封顶），无新成员可入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建面
开放 / X2-3 数值路径解冻 / 单线程审计基底变更——均不预期）；
（b）现实规模位移 ≥1 个数量级使亚带池放大越带（STORD/RIDGE 的重开
条件同此：设计因子数或迭代量级增长使累加/岭池放大一个数量级）；
或（c）指出「重命名 / 亚带 / 逐位违例 / 结构交换负优化」四分类之外
的第五类成本——十五遍枚举未再发现。

## 4. 重命名检查（派单强制项）

- **STORD vs SWORD / S6-C / S6-C-1..4 / S5-C-5**：SWORD（R14-C 无号）
  是 case **标签排列**次序（分派结构外壳）；S6-C 是按支撑大小直线化
  分派的存在本身；S6-C-1/2 是子集特化剂量响应；S6-C-3 是 ai 滚动 +
  bi 直线化混合**结构**；S6-C-4 是 bi 循环**展开因子**；S5-C-5 是
  串行依赖链上的**浮点运算序**重排（逐位违例域）。STORD 只置换
  case 体内**对互不相交目标的存储语句**交错次序——不动任何浮点
  运算的操作数与顺序，不动分派，不动标签。该结构位置（体内语句
  调度）此前无任何 ID 或探针覆盖，非重命名；以先验池算术 + 三次
  同号但幅度不稳的深亚带决斗关闭，不铸号。
- **RIDGE vs RID / S6-C-6 / ALLOC / S8-C-3**：RID（R11-C 无号）是
  岭项**预置进 fill 循环**这一归约重排变换（逐位违例淘汰）；
  S6-C-6 是戳拷贝循环微观；ALLOC 是缓冲分配封顶；S8-C-3 是收敛侧
  有限性检查 DCE。RIDGE 是落地岭循环**自身的池测量**——份额从未
  单独坐实；以 3.08–3.85 ms 封顶关闭，不铸号。融合进 solve 的
  出路不是新候选：它改变公开 `solveSymmetric` 的通用求解器语义
  （X1-2 平行实现 / X0-4 公开签名族）。
- **AAFLR vs SWORD 位置偏置插曲**：R14-C 在 SWORD 上发现「先跑方
  吃冷分配器」的位置偏置并引入逐 rep 交替；AAFLR 把该纪律推进到
  **字节相同副本对副本**的完整 A/A 校准（中位地板 ±1.5 ms、单 rep
  尾部 +90 ms）——是方法论轴，无任何生产变换形态，非重命名亦非
  候选，不铸号。
- **GCAX vs ALLOC / S3-G-1 族**：ALLOC 封顶的是 irls 缓冲**分配**
  成本；S3-G-1 是异切片 structuredClone 池。GCAX 测的是**收集器
  停顿**在整报告内的份额（0.88–0.89%）——观测轴，非变换，不铸号。
- **换名再提交拒列（本轮核实）**：零。R13-C 五个与 R14-C 四个无名
  微观（PIVCSE/FINTAIL/REFZ/TSORT/DEG/SWORD/XF64/IRLSDP/CMPH）未
  重探、未补铸、未换址重提；S10-C-1..3 / S12-C-1 / RID / CNT 零
  重提；APC 侧组合分析仅引用 R14-C/R11-C 已有实测数字；嵌套序 ×
  布局组合空间未触碰。

## 5. 测试与验证

环境：Node 22.22.2（nvm + PATH 前置；VM 默认低于 engines）、
pnpm 10.17.1。生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录）
npx tsx /tmp/r15c-anchor.mts                # ✓ 11,436 项逐位 × 3 次正典运行全绿
                                            #   （53 例 ×{仪表道,落地道A,落地道B,语句置换道} 忠实性电池
                                            #    + 3 seed × 2 自确定性 + perf 夹具逐位）
                                            #   run A 的 GCAX 行因观察器冲刷时机读 0（GC 条目走计时器
                                            #   队列而非 setImmediate，本轮实测坐实后修正），其余读数
                                            #   有效；run B/C 为修正后全量运行

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 677.5 ms（1.10× vs 冻结 S6-C ctl），r3c 内生产道
# 663.3 ms（1.47× vs S2-C 参照），与 §1 生产中位（660.6–667.1）同带。
```

代表性一次测量彩排全量输出（正典 run B，GCAX 修正后；run A/C 数字
见 §1 池表与 §2）：

```text
scenario 1 (four-lane bitwise fidelity, 53 cases x {instrumented, plain-landed-A, plain-landed-B, plain-stord} vs production fitLogitAdditive import)
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)
  shape small (3 models x 3 projects, 120 rows): production median 32.1 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 665.2 ms/report (7 reps)
probe GCAX: 210 GC events, 41.2 ms total pause across 7 production reports = 5.89 ms/report (0.88% of the median)
instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):
  total                 676.8 ms
  solveSymmetric        452.8 ms  (66.9%)  [8966 calls]
  irls minus solve      156.6 ms  (23.1%)  [8966 iterations, 3586400 row visits, 1249652 irls sigmoids]
  onProbabilitiesFor    7.0 ms  (1.0%)  [80400 sigmoid+dot]
  APC (all sweeps)      54.1 ms  (8.0%)  [277606 active pairs, 12259 calls]
  bootstrap sampling    1.6 ms  (0.2%)
  sort/percentile tail  1.6 ms  (0.2%)  [12200 draws-map ops]
  base-row projection   0.009 ms  (once/report)
  outside pools (build/supports/keys/base copies) 3.2 ms  (0.5%)
  fits 201  [ridge adds/report = 60 x 8966 iterations = 537960]
probe STORD: landed-order copy 672.9 ms vs stord-order copy 665.0 ms (delta +7.8 ms landed-minus-stord; 6 position-alternated interleaved reps each after warmup)
  -> structural bitwise: xtwz and xtwx rows are disjoint fresh targets, each cell gets exactly one addend per row, per-cell cross-row order unchanged; whole irls-other pool bounds any effect either way
  landed reps: 672.9 675.6 669.2 671.2 671.2 676.2
  stord reps:  663.5 666.9 664.6 665.0 664.8 666.2
probe AAFLR: byte-identical copy A 675.2 ms vs copy B 676.2 ms (A/A delta -1.1 ms; 6 position-alternated interleaved reps each after warmup) — the whole-report duel noise floor on this VM
  A reps: 668.7 680.9 671.7 675.2 675.0 694.9
  B reps: 669.0 706.9 676.5 671.2 669.9 676.2
probe RIDGE: landed ridge diagonal-add loop, 60 adds x 8,966 iterations = 537,960 adds/report, standalone 3.08 ms/report pool ceiling (median of 6 passes; sink=4.323)
  -> the only structural moves are RID (pre-placement reduction reorder, bitwise violation, R11-C) and fusing ridge into the public solveSymmetric contract; anything else is bounded by this sub-band pool

ALL EQUIVALENCE CHECKS PASSED (11436 bitwise checks)
```

其余两次正典运行的探针行（关键数字）：

```text
run A: prod median 660.6 (small 30.8); STORD +9.9 (landed 674.6 vs stord 664.7;
       landed reps 679.6 675.5 667.7 666.4 671.6 674.6 / stord reps 720.5 664.7 662.8 664.8 663.5 664.7);
       AAFLR +0.2 (A 667.9 vs B 667.7); RIDGE 3.85; GCAX 行无效（冲刷缺陷，见 §5 脚注）
run C: prod median 667.1 (small 32.7); STORD +2.0 (landed 669.4 vs stord 667.4;
       landed reps 672.7 669.4 668.2 668.3 679.2 668.5 / stord reps 674.5 663.4 665.8 662.7 667.4 682.8);
       AAFLR +1.4 (A 675.3 vs B 673.9; 双道各一个 761 ms 环境漂移 rep 对称落两侧);
       RIDGE 3.47; GCAX 210 events / 41.5 ms = 5.93 ms/report (0.89%)
```

## 6. 新增排除 ID：无

第十五组候选集为空。STORD / RIDGE 为关闭探针，AAFLR / GCAX 为测量
轴，遵循 R11-C / R12-C / R13-C / R14-C 先例不铸号；重开条件均为
现实规模/设计结构位移 ≥1 个数量级（不预期），AAFLR/GCAX 作为轴无
重开语义。STORD 的同号读数已按 A/A 地板校准留档：若后续轮次在同类
调度位置观察到符号稳定且越带的读数，应先复核 AAFLR 地板再立道。

## 附录：测量彩排脚本（完整，可复现）

```ts
/**
 * Round 15 / R15-C measurement re-anchor + fifteenth-group closure probes
 * (offline routing slice, fifteenth pass). NOT committed — the fifteenth-
 * group enumeration carries no candidate lanes that reach the acceptance bar;
 * full source goes into the R15-C report appendix (R7-C..R14-C precedent).
 *
 * Discipline (same as R11-C..R14-C):
 *   1. re-anchor the post-S7-C profile on THIS VM: production median
 *      ms/report on the perf shape (400 rows, 6 models x 8 projects,
 *      bootstrap=200) and the small shape, x-checked by an INSTRUMENTED
 *      verbatim pipeline copy (timers/counters only; every float op, value,
 *      and order identical — validated bitwise against the production
 *      import over the full 53-case battery before its numbers are used);
 *   2. derive the per-pool cost table (solve / irls-other / on-prob / APC /
 *      sampling / tail / base-copy) plus site counts;
 *   3. close the fifteenth-group never-named sites (no lanes reach the bar;
 *      all arithmetic/mechanism closures, no IDs minted):
 *        STORD  statement-order permutation INSIDE the landed S6-C case
 *               bodies (all xtwz stores hoisted before the xtwx row blocks;
 *               case labels, bodies' addends, and per-cell addend order
 *               unchanged) — xtwz and xtwx rows are disjoint targets and
 *               each cell receives exactly one addend per row, so the
 *               permutation is structurally bitwise; expected duel result
 *               is pure jitter under the R14-C position-alternation
 *               methodology, bounded by the irls-other pool either way
 *        RIDGE  the landed per-iteration ridge diagonal-add loop
 *               (for d: xtwx[d][d] += 1e-6) as a standalone pool — RID
 *               (R11-C) closed the only structural transformation
 *               (pre-placement = reduction reorder, 3,792/10,000 bitwise
 *               counterexamples); this probe measures the pool itself:
 *               60 adds x 8,966 iterations = 537,960 diagonal adds/report,
 *               expected low single-digit ms, deeply sub-band
 *   4. NEW AXES this round (R15-A/B precedent of adding observability
 *      angles):
 *        AAFLR  copy-vs-copy A/A duel floor — two byte-identical plain
 *               landed lanes (separate function identities) dueled with
 *               position alternation; the observed median |delta| is this
 *               VM's whole-report duel noise floor, calibrating how large a
 *               pseudo-signal the +-35 ms band methodology could produce
 *        GCAX   GC share of a production report — PerformanceObserver('gc')
 *               pause total and event count across the 7 perf-shape
 *               production reps
 *   5. re-check production self-determinism across 3 seeds x 2 runs.
 *
 * Run: npx tsx /tmp/r15c-anchor.mts
 */

import { PerformanceObserver } from "node:perf_hooks";
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";
import type { AttributionReport, OfflineRow } from "/workspace/src/routing/offline-types.js";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.js";

/* --------------------------- shared constants --------------------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}

interface FitResult {
  readonly coefficients: readonly number[] | null;
}

type IrlsImpl = (
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
) => FitResult;

function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}

function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/* ------------- verbatim production pipeline building blocks -------------- *
 * Transcribed from src/routing/offline-logit.ts and src/routing/lin-alg.ts
 * at the 183df9b-landed state. The instrumented lane's ONLY additions are
 * STATS counter increments and performance.now() reads at call boundaries;
 * no float operation, operand, or ordering differs. The plain lanes (landed
 * statement order A, byte-identical landed copy B for the A/A floor, and
 * the STORD statement-order permutation) carry no timers at all. Every lane
 * is validated bitwise against the production import over the full 53-case
 * battery before any number is reported. */

const STATS = {
  fits: 0,
  irlsMs: 0,
  irlsIterations: 0,
  solveCalls: 0,
  solveMs: 0,
  sigmoidIrls: 0,
  rowVisits: 0,
  onProbMs: 0,
  onProbSigmoids: 0,
  apcMs: 0,
  apcActivePairs: 0,
  apcCalls: 0,
  samplingMs: 0,
  drawsMapOps: 0,
  tailMs: 0,
  baseCopyMs: 0,
  totalMs: 0,
  reset(): void {
    this.fits = 0; this.irlsMs = 0; this.irlsIterations = 0; this.solveCalls = 0;
    this.solveMs = 0; this.sigmoidIrls = 0; this.rowVisits = 0; this.onProbMs = 0;
    this.onProbSigmoids = 0; this.apcMs = 0; this.apcActivePairs = 0;
    this.apcCalls = 0; this.samplingMs = 0; this.drawsMapOps = 0;
    this.tailMs = 0; this.baseCopyMs = 0; this.totalMs = 0;
  }
};

function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);

  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);

  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);

  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });

  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}

function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}

function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}

/* verbatim landed solveSymmetric (lin-alg.ts) — plain, no timers */
function solvePlain(a: readonly (readonly number[])[], b: readonly number[]): number[] | null {
  const n = b.length;
  if (a.length !== n || a.some((row) => row.length !== n)) {
    throw new DomainValidationError("solveSymmetric requires a square matrix matching b");
  }
  const m: number[][] = a.map((row) => [...row]);
  const x: number[] = [...b];
  const eps = 1e-12;

  for (let col = 0; col < n; col++) {
    let pivotRow = col;
    let pivotAbs = Math.abs(m[col]![col]!);
    for (let row = col + 1; row < n; row++) {
      const abs = Math.abs(m[row]![col]!);
      if (abs > pivotAbs) {
        pivotAbs = abs;
        pivotRow = row;
      }
    }
    if (pivotAbs < eps) return null;
    if (pivotRow !== col) {
      const tmp = m[col]!;
      m[col] = m[pivotRow]!;
      m[pivotRow] = tmp;
      const tb = x[col]!;
      x[col] = x[pivotRow]!;
      x[pivotRow] = tb;
    }
    const colArr = m[col]!;
    const pivot = colArr[col]!;
    const xCol = x[col]!;
    for (let row = col + 1; row < n; row++) {
      const rowArr = m[row]!;
      const factor = rowArr[col]! / pivot;
      if (factor === 0) continue;
      let k = col;
      const stop = n - 3;
      for (; k < stop; k += 4) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
        rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
        rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
      }
      for (; k < n; k++) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
      }
      x[row] = x[row]! - factor * xCol;
    }
  }

  const solution = new Array<number>(n).fill(0);
  for (let row = n - 1; row >= 0; row--) {
    const rowArr = m[row]!;
    let sum = x[row]!;
    for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
    const diag = rowArr[row]!;
    if (Math.abs(diag) < eps) return null;
    solution[row] = sum / diag;
  }
  for (const value of solution) {
    if (!Number.isFinite(value)) return null;
  }
  return solution;
}

/* Shared preamble/epilogue for the irls lanes. The lanes differ ONLY in
 * (a) instrumentation and (b) the STATEMENT ORDER inside the accumulation
 * switch's case bodies (STORD probe): the "landed" order interleaves each
 * xtwz store with its xtwx row block exactly as production does; the
 * "stord" order hoists ALL xtwz stores of a case body before ALL xtwx row
 * blocks. Case labels, addends, and per-cell addend order are unchanged —
 * xtwz and the xtwx rows are disjoint freshly-allocated targets, support
 * indices within a row are strictly ascending and distinct, and each cell
 * receives exactly one addend per row, so the permutation is structurally
 * bitwise. It is still checked against the production import over the
 * whole battery. The default case (support sizes outside 2..5, i.e. the
 * intercept-only rows) is verbatim in both orders: its rolled loop is the
 * production body and permuting it would be a structure change, not a
 * statement reorder. */

function makeIrls(bodyOrder: "landed" | "stord", instrumented: boolean): IrlsImpl {
  return (design, rows, _vectors, supports, keys, keySpace, maxIter) => {
    const tIrls0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.fits += 1;
    const p = design.names.length;
    const n = rows.length;
    const eta = new Array<number>(n).fill(0);
    const mu = new Array<number>(n).fill(0);
    const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
    const xtwz: number[] = new Array<number>(p).fill(0);
    const stamp = new Int32Array(keySpace);
    const etaByKey = new Float64Array(keySpace);
    const muByKey = new Float64Array(keySpace);
    let beta = new Array<number>(p).fill(0);
    for (let iter = 0; iter < maxIter; iter++) {
      if (instrumented) {
        STATS.irlsIterations += 1;
        STATS.rowVisits += n;
      }
      const mark = iter + 1;
      for (let i = 0; i < n; i++) {
        const key = keys[i]!;
        if (stamp[key] !== mark) {
          stamp[key] = mark;
          if (instrumented) STATS.sigmoidIrls += 1;
          let value = 0;
          const active = supports[i]!;
          for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
          etaByKey[key] = value;
          muByKey[key] = sigmoid(value);
        }
        eta[i] = etaByKey[key]!;
        mu[i] = muByKey[key]!;
      }
      for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
      xtwz.fill(0);
      if (bodyOrder === "landed") {
        for (let i = 0; i < n; i++) {
          const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
          const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
          const wz = w * z;
          const active = supports[i]!;
          switch (active.length) {
            case 5: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              const a4 = active[4]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
              xtwz[a4] = xtwz[a4]! + wz;
              const r4 = xtwx[a4]!;
              r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
              break;
            }
            case 4: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
              xtwz[a3] = xtwz[a3]! + wz;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
              break;
            }
            case 3: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
              xtwz[a2] = xtwz[a2]! + wz;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
              break;
            }
            case 2: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              xtwz[a0] = xtwz[a0]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
              xtwz[a1] = xtwz[a1]! + wz;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
              break;
            }
            default: {
              for (let ai = 0; ai < active.length; ai++) {
                const a = active[ai]!;
                xtwz[a] = xtwz[a]! + wz;
                const rowA = xtwx[a]!;
                for (let bi = 0; bi < active.length; bi++) {
                  const b = active[bi]!;
                  rowA[b] = rowA[b]! + w;
                }
              }
            }
          }
        }
      } else {
        for (let i = 0; i < n; i++) {
          const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
          const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
          const wz = w * z;
          const active = supports[i]!;
          switch (active.length) {
            case 5: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              const a4 = active[4]!;
              xtwz[a0] = xtwz[a0]! + wz;
              xtwz[a1] = xtwz[a1]! + wz;
              xtwz[a2] = xtwz[a2]! + wz;
              xtwz[a3] = xtwz[a3]! + wz;
              xtwz[a4] = xtwz[a4]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
              const r4 = xtwx[a4]!;
              r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
              break;
            }
            case 4: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              const a3 = active[3]!;
              xtwz[a0] = xtwz[a0]! + wz;
              xtwz[a1] = xtwz[a1]! + wz;
              xtwz[a2] = xtwz[a2]! + wz;
              xtwz[a3] = xtwz[a3]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
              const r3 = xtwx[a3]!;
              r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
              break;
            }
            case 3: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              const a2 = active[2]!;
              xtwz[a0] = xtwz[a0]! + wz;
              xtwz[a1] = xtwz[a1]! + wz;
              xtwz[a2] = xtwz[a2]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
              const r2 = xtwx[a2]!;
              r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
              break;
            }
            case 2: {
              const a0 = active[0]!;
              const a1 = active[1]!;
              xtwz[a0] = xtwz[a0]! + wz;
              xtwz[a1] = xtwz[a1]! + wz;
              const r0 = xtwx[a0]!;
              r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
              const r1 = xtwx[a1]!;
              r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
              break;
            }
            default: {
              for (let ai = 0; ai < active.length; ai++) {
                const a = active[ai]!;
                xtwz[a] = xtwz[a]! + wz;
                const rowA = xtwx[a]!;
                for (let bi = 0; bi < active.length; bi++) {
                  const b = active[bi]!;
                  rowA[b] = rowA[b]! + w;
                }
              }
            }
          }
        }
      }
      for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
      let next: number[] | null;
      if (instrumented) {
        const tSolve0 = performance.now();
        next = solvePlain(xtwx, xtwz);
        STATS.solveMs += performance.now() - tSolve0;
        STATS.solveCalls += 1;
      } else {
        next = solvePlain(xtwx, xtwz);
      }
      if (next === null) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      const delta = next.map((value, index) => value - beta[index]!);
      const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
      beta = next;
      if (!beta.every(Number.isFinite)) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      if (l2 < TOL) break;
    }
    if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
    return { coefficients: beta };
  };
}

const irlsInstrumented = makeIrls("landed", true);
const irlsLandedA = makeIrls("landed", false);
const irlsLandedB = makeIrls("landed", false); // byte-identical second identity for the A/A floor
const irlsStord = makeIrls("stord", false);

function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}

function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}

function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}

function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

/**
 * Full pipeline parameterized by irls lane. instrumented=true additionally
 * times onProb/APC/sampling/tail/baseCopy pools (timers only).
 */
function makeFit(irlsImpl: IrlsImpl, instrumented: boolean) {
  const onProb = (vectors: readonly number[][], coefficients: readonly number[]): number[] => {
    if (!instrumented) return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    const t0 = performance.now();
    const result = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    STATS.onProbMs += performance.now() - t0;
    STATS.onProbSigmoids += vectors.length;
    return result;
  };

  const apc = (
    design: Design,
    rows: readonly Row[],
    vectors: readonly number[][],
    coefficients: readonly number[],
    onProbabilities: readonly number[],
    column: string
  ): number => {
    const t0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.apcCalls += 1;
    let sum = 0;
    const columnIdx = design.columnIndex.get(column);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < rows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        if (instrumented) STATS.apcActivePairs += 1;
        const on = onProbabilities[i]!;
        const offVector = vectors[i]!.slice();
        offVector[columnIdx] = 0;
        const off = sigmoid(dot(coefficients, offVector));
        sum += on - off;
      }
    }
    const result = rows.length === 0 ? 0 : sum / rows.length;
    if (instrumented) STATS.apcMs += performance.now() - t0;
    return result;
  };

  const pushValue = (map: Map<string, number[]>, key: string, value: number): void => {
    if (instrumented) STATS.drawsMapOps += 1;
    const list = map.get(key);
    if (list === undefined) map.set(key, [value]);
    else list.push(value);
  };

  return function fit(
    rows: readonly OfflineRow[],
    options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
  ): AttributionReport {
    const tTotal0 = instrumented ? performance.now() : 0;
    const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
    const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
    const seed = options?.seed ?? SEED_DEFAULT;

    const tBase0 = instrumented ? performance.now() : 0;
    const baseRows: Row[] = rows.map((r) => ({
      scenarioId: r.scenarioId,
      modelVersion: r.modelVersion,
      projectId: r.projectId,
      y: r.y
    }));
    if (instrumented) STATS.baseCopyMs += performance.now() - tBase0;
    const effects: Array<{ name: string; point: number; lcb: number; ucb: number }> = [];

    if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
    }

    const design = buildDesign(baseRows);
    const vectors = baseRows.map((r) => design.build(r));
    const supports = computeSupports(vectors);
    const keys = canonicalRowKeys(baseRows);
    const fit = irlsImpl(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
    if (fit.coefficients === null) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
    }

    const onProbabilities = onProb(vectors, fit.coefficients);
    const pointEffects = new Map<string, number>();
    for (const name of design.names) {
      if (name === "intercept") continue;
      pointEffects.set(
        name,
        apc(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
      );
    }
    for (const ref of design.referenceLevels) {
      if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
        pointEffects.set(`${ref.factor}:${ref.name}`, 0);
      }
    }

    const random = rng(seed);
    const draws = new Map<string, number[]>();
    let successful = 0;
    for (let draw = 0; draw < bootstrapDraws; draw++) {
      const tSample0 = instrumented ? performance.now() : 0;
      const sample: Row[] = [];
      const sampleVectors: number[][] = [];
      const sampleSupports: number[][] = [];
      const sampleKeys: number[] = [];
      for (let i = 0; i < baseRows.length; i++) {
        const index = Math.floor(random() * baseRows.length);
        sample.push(baseRows[index]!);
        sampleVectors.push(vectors[index]!);
        sampleSupports.push(supports[index]!);
        sampleKeys.push(keys[index]!);
      }
      const collapsed = sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1);
      if (instrumented) STATS.samplingMs += performance.now() - tSample0;
      if (collapsed) continue;
      const bootFit = irlsImpl(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
      if (bootFit.coefficients === null) continue;
      successful += 1;
      const sampleOnProbabilities = onProb(sampleVectors, bootFit.coefficients);
      for (const [name] of pointEffects.entries()) {
        const value = apc(
          design,
          sample,
          sampleVectors,
          bootFit.coefficients,
          sampleOnProbabilities,
          name
        );
        pushValue(draws, name, value);
      }
    }

    const tTail0 = instrumented ? performance.now() : 0;
    for (const [name, point] of pointEffects.entries()) {
      const values = draws.get(name) ?? [];
      if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
        return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
      }
      values.sort((a, b) => a - b);
      effects.push({
        name,
        point,
        lcb: percentile(values, 0.025),
        ucb: percentile(values, 0.975)
      });
    }

    const n = baseRows.length;
    const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
    const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
    const muLcb = betaQuantileLcb(muPosterior, 0.05);
    const models = new Set(baseRows.map((r) => r.modelVersion)).size;
    const projects = new Set(baseRows.map((r) => r.projectId)).size;
    const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
    const containsZero = (e: { lcb: number; ucb: number }): boolean =>
      e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
    const interactionsFor = (prefix: string): Array<{ name: string; lcb: number; ucb: number; point: number }> =>
      effects.filter((e) => e.name.startsWith(`w:${prefix}`));

    let diagnosis: AttributionReport["diagnosis"] = "uncertain";
    const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
    if (
      effects.some(
        (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
      )
    ) {
      diagnosis = "model-problem";
    } else if (
      effects.some(
        (e) =>
          e.name.startsWith("v:") &&
          e.lcb < -ATTRIBUTION_EFFECT &&
          effects
            .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
            .every(containsZero)
      )
    ) {
      diagnosis = "project-problem";
    } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
      diagnosis = "interaction-only";
    } else if (scenarioHard) {
      diagnosis = "scenario-hard";
    }

    const report: AttributionReport = {
      estimator: "logit-additive",
      rowsUsed: baseRows.length,
      effects,
      diagnosis,
      reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
      writesActivePointer: false
    };
    if (instrumented) {
      STATS.tailMs += performance.now() - tTail0;
      STATS.totalMs += performance.now() - tTotal0;
    }
    return report;
  };
}

const fitInst = makeFit(irlsInstrumented, true);
const fitPlainA = makeFit(irlsLandedA, false);
const fitPlainB = makeFit(irlsLandedB, false);
const fitStord = makeFit(irlsStord, false);

/* ------------------------------ harness ------------------------------ */

let checksPassed = 0;
let failures = 0;

function check(label: string, ok: boolean): void {
  if (ok) checksPassed++;
  else {
    failures++;
    process.stderr.write(`FAIL ${label}\n`);
  }
}

function compareReports(label: string, expected: AttributionReport, actual: AttributionReport): void {
  check(`${label}.estimator`, expected.estimator === actual.estimator);
  check(`${label}.rowsUsed`, expected.rowsUsed === actual.rowsUsed);
  check(`${label}.diagnosis`, expected.diagnosis === actual.diagnosis);
  check(`${label}.reason`, expected.reason === actual.reason);
  check(`${label}.writesActivePointer`, expected.writesActivePointer === actual.writesActivePointer);
  check(`${label}.effects.length`, expected.effects.length === actual.effects.length);
  if (expected.effects.length === actual.effects.length) {
    for (let i = 0; i < expected.effects.length; i++) {
      const e = expected.effects[i]!;
      const g = actual.effects[i]!;
      check(`${label}.effects[${i}].name`, e.name === g.name);
      check(`${label}.effects[${i}].point`, Object.is(e.point, g.point));
      check(`${label}.effects[${i}].lcb`, Object.is(e.lcb, g.lcb));
      check(`${label}.effects[${i}].ucb`, Object.is(e.ucb, g.ucb));
    }
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}

function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRow[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function allUniqueRows(r: () => number, count: number): OfflineRow[] {
  const rows: OfflineRow[] = [];
  for (let i = 0; i < count; i++) {
    rows.push({
      scenarioId: `s${i % 2}`,
      modelVersion: `m${i}`,
      projectId: `p${i}`,
      y: r() < 0.6 ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function batteryCases(): Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];

  for (let i = 0; i < 40; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, {
      scenarios: 2,
      models: 3,
      projects: 3,
      rows: 60,
      passRate: 0.55,
      pipeInModel: true,
    }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  cases.push({
    rows: allUniqueRows(r, 36),
    options: { bootstrap: 40, seed: 23 },
  });
  {
    const oneKey: OfflineRow[] = [];
    for (let i = 0; i < 30; i++) {
      oneKey.push({
        scenarioId: "s0",
        modelVersion: "m0",
        projectId: "p0",
        y: i % 3 === 0 ? 0 : 1,
        occurredAtMs: 1_000 + i,
      });
    }
    cases.push({ rows: oneKey, options: { bootstrap: 40, seed: 31 } });
  }
  cases.push({
    rows: [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1, occurredAtMs: 1_000 },
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 0, occurredAtMs: 1_001 },
    ],
    options: { bootstrap: 30, seed: 5 },
  });
  return cases;
}

const out = (line: string): void => {
  process.stdout.write(`${line}\n`);
};

function perfRows(): OfflineRow[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, {
    scenarios: 4,
    models: 6,
    projects: 8,
    rows: 400,
    passRate: 0.6,
  });
}

const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------- scenario 1: four-lane bitwise fidelity battery --------- */

function scenarioEquivalence(): void {
  const cases = batteryCases();
  for (const [index, testCase] of cases.entries()) {
    const expected = fitLogitAdditive(testCase.rows, testCase.options);
    compareReports(`R15C-inst[${index}]`, expected, fitInst(testCase.rows, testCase.options));
    compareReports(`R15C-plainA[${index}]`, expected, fitPlainA(testCase.rows, testCase.options));
    compareReports(`R15C-plainB[${index}]`, expected, fitPlainB(testCase.rows, testCase.options));
    compareReports(`R15C-stord[${index}]`, expected, fitStord(testCase.rows, testCase.options));
  }
  out(
    `scenario 1 (four-lane bitwise fidelity, ${cases.length} cases x {instrumented, plain-landed-A, plain-landed-B, plain-stord} ` +
      `vs production fitLogitAdditive import)`
  );
}

/* -------- scenario 2: production self-determinism across 3 seeds -------- */

function scenarioDeterminism(): void {
  const r = fixtureRng(0xd15c);
  const rows = randomRows(r, { scenarios: 3, models: 4, projects: 5, rows: 150, passRate: 0.6 });
  for (const seed of [101, 202, 303]) {
    const first = fitLogitAdditive(rows, { bootstrap: 60, seed });
    const second = fitLogitAdditive(rows, { bootstrap: 60, seed });
    compareReports(`R15C-det[seed=${seed}]`, first, second);
  }
  out("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)");
}

/* ------------- scenario 3: profile re-anchor + pool table + GCAX ------------- */

function scenarioProfile(): void {
  const smallRows = randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
  {
    const times: number[] = [];
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(smallRows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape small (3 models x 3 projects, 120 rows): production median ${median(times).toFixed(1)} ms/report (3 reps)`);
  }

  // Perf shape with the GCAX axis observing GC pauses across the 7 reps.
  {
    const rows = perfRows();
    let gcCount = 0;
    let gcMs = 0;
    const obs = new PerformanceObserver((list) => {
      for (const entry of list.getEntries()) {
        gcCount += 1;
        gcMs += entry.duration;
      }
    });
    obs.observe({ entryTypes: ["gc"] });
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    // Flush pending observer callbacks before reading the totals. GC entries
    // are dispatched on the timer queue, not by setImmediate (verified on
    // this VM), so the read waits one timer turn.
    const med = median(times);
    setTimeout(() => {
      obs.disconnect();
      out(`  shape perf (6 models x 8 projects, 400 rows): production median ${med.toFixed(1)} ms/report (7 reps)`);
      out(
        `probe GCAX: ${gcCount} GC events, ${gcMs.toFixed(1)} ms total pause across 7 production reports = ` +
          `${(gcMs / 7).toFixed(2)} ms/report (${((gcMs / 7 / med) * 100).toFixed(2)}% of the median)`
      );
      afterProfile();
    }, 100);
  }
}

function afterProfile(): void {
  // Instrumented pool table on the perf shape (3 runs; medians reported).
  const rows = perfRows();
  const expected = fitLogitAdditive(rows, PERF_OPTIONS);
  const runs: Array<Record<string, number>> = [];
  for (let rep = 0; rep < 3; rep++) {
    STATS.reset();
    const actual = fitInst(rows, PERF_OPTIONS);
    if (rep === 0) compareReports("R15C-perf-fixture", expected, actual);
    runs.push({
      totalMs: STATS.totalMs, irlsMs: STATS.irlsMs, solveMs: STATS.solveMs,
      onProbMs: STATS.onProbMs, apcMs: STATS.apcMs, samplingMs: STATS.samplingMs,
      tailMs: STATS.tailMs, baseCopyMs: STATS.baseCopyMs,
      fits: STATS.fits, irlsIterations: STATS.irlsIterations, solveCalls: STATS.solveCalls,
      sigmoidIrls: STATS.sigmoidIrls, rowVisits: STATS.rowVisits,
      onProbSigmoids: STATS.onProbSigmoids, apcActivePairs: STATS.apcActivePairs,
      apcCalls: STATS.apcCalls, drawsMapOps: STATS.drawsMapOps,
    });
  }
  const med = (key: string): number => median(runs.map((r) => r[key]!));
  const total = med("totalMs");
  const solveMs = med("solveMs");
  const irlsOther = med("irlsMs") - solveMs;
  const rest = total - med("irlsMs") - med("onProbMs") - med("apcMs") - med("samplingMs") - med("tailMs");
  out(`instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):`);
  out(`  total                 ${total.toFixed(1)} ms`);
  out(`  solveSymmetric        ${solveMs.toFixed(1)} ms  (${(100 * solveMs / total).toFixed(1)}%)  [${med("solveCalls")} calls]`);
  out(`  irls minus solve      ${irlsOther.toFixed(1)} ms  (${(100 * irlsOther / total).toFixed(1)}%)  [${med("irlsIterations")} iterations, ${med("rowVisits")} row visits, ${med("sigmoidIrls")} irls sigmoids]`);
  out(`  onProbabilitiesFor    ${med("onProbMs").toFixed(1)} ms  (${(100 * med("onProbMs") / total).toFixed(1)}%)  [${med("onProbSigmoids")} sigmoid+dot]`);
  out(`  APC (all sweeps)      ${med("apcMs").toFixed(1)} ms  (${(100 * med("apcMs") / total).toFixed(1)}%)  [${med("apcActivePairs")} active pairs, ${med("apcCalls")} calls]`);
  out(`  bootstrap sampling    ${med("samplingMs").toFixed(1)} ms  (${(100 * med("samplingMs") / total).toFixed(1)}%)`);
  out(`  sort/percentile tail  ${med("tailMs").toFixed(1)} ms  (${(100 * med("tailMs") / total).toFixed(1)}%)  [${med("drawsMapOps")} draws-map ops]`);
  out(`  base-row projection   ${med("baseCopyMs").toFixed(3)} ms  (once/report)`);
  out(`  outside pools (build/supports/keys/base copies) ${rest.toFixed(1)} ms  (${(100 * rest / total).toFixed(1)}%)`);
  out(`  fits ${med("fits")}  [ridge adds/report = 60 x ${med("irlsIterations")} iterations = ${60 * med("irlsIterations")}]`);

  scenarioProbes();

  if (failures > 0) {
    process.stderr.write(`${failures} CHECK(S) FAILED\n`);
    process.exitCode = 1;
  } else {
    out(`\nALL EQUIVALENCE CHECKS PASSED (${checksPassed} bitwise checks)`);
  }
}

/* --------- scenario 4: fifteenth-group closure probes (no lanes) --------- */

function scenarioProbes(): void {
  const rows = perfRows();

  // STORD: landed statement order (each xtwz store interleaved with its xtwx
  // row block) vs permuted order (all xtwz stores hoisted before the row
  // blocks). Copy-vs-copy duel with the R14-C position-alternation
  // methodology: the lane that runs first alternates per rep after one
  // warmup each.
  {
    fitPlainA(rows, PERF_OPTIONS); // warmup landed copy
    fitStord(rows, PERF_OPTIONS); // warmup stord copy
    const landedTimes: number[] = [];
    const stordTimes: number[] = [];
    for (let rep = 0; rep < 6; rep++) {
      if (rep % 2 === 0) {
        let t0 = performance.now();
        fitStord(rows, PERF_OPTIONS);
        stordTimes.push(performance.now() - t0);
        t0 = performance.now();
        fitPlainA(rows, PERF_OPTIONS);
        landedTimes.push(performance.now() - t0);
      } else {
        let t0 = performance.now();
        fitPlainA(rows, PERF_OPTIONS);
        landedTimes.push(performance.now() - t0);
        t0 = performance.now();
        fitStord(rows, PERF_OPTIONS);
        stordTimes.push(performance.now() - t0);
      }
    }
    const landedMed = median(landedTimes);
    const stordMed = median(stordTimes);
    out(
      `probe STORD: landed-order copy ${landedMed.toFixed(1)} ms vs stord-order copy ${stordMed.toFixed(1)} ms ` +
        `(delta ${(landedMed - stordMed >= 0 ? "+" : "") + (landedMed - stordMed).toFixed(1)} ms landed-minus-stord; ` +
        `6 position-alternated interleaved reps each after warmup)`
    );
    out(
      `  -> structural bitwise: xtwz and xtwx rows are disjoint fresh targets, each cell gets exactly one addend ` +
        `per row, per-cell cross-row order unchanged; whole irls-other pool bounds any effect either way`
    );
    out(`  landed reps: ${landedTimes.map((t) => t.toFixed(1)).join(" ")}`);
    out(`  stord reps:  ${stordTimes.map((t) => t.toFixed(1)).join(" ")}`);
  }

  // AAFLR: A/A duel floor — two BYTE-IDENTICAL plain landed lanes (separate
  // closure identities), same alternation protocol. The observed |delta| is
  // the copy-vs-copy noise floor of this duel methodology on this VM.
  {
    fitPlainA(rows, PERF_OPTIONS); // warmup A
    fitPlainB(rows, PERF_OPTIONS); // warmup B
    const aTimes: number[] = [];
    const bTimes: number[] = [];
    for (let rep = 0; rep < 6; rep++) {
      if (rep % 2 === 0) {
        let t0 = performance.now();
        fitPlainB(rows, PERF_OPTIONS);
        bTimes.push(performance.now() - t0);
        t0 = performance.now();
        fitPlainA(rows, PERF_OPTIONS);
        aTimes.push(performance.now() - t0);
      } else {
        let t0 = performance.now();
        fitPlainA(rows, PERF_OPTIONS);
        aTimes.push(performance.now() - t0);
        t0 = performance.now();
        fitPlainB(rows, PERF_OPTIONS);
        bTimes.push(performance.now() - t0);
      }
    }
    const aMed = median(aTimes);
    const bMed = median(bTimes);
    out(
      `probe AAFLR: byte-identical copy A ${aMed.toFixed(1)} ms vs copy B ${bMed.toFixed(1)} ms ` +
        `(A/A delta ${(aMed - bMed >= 0 ? "+" : "") + (aMed - bMed).toFixed(1)} ms; 6 position-alternated ` +
        `interleaved reps each after warmup) — the whole-report duel noise floor on this VM`
    );
    out(`  A reps: ${aTimes.map((t) => t.toFixed(1)).join(" ")}`);
    out(`  B reps: ${bTimes.map((t) => t.toFixed(1)).join(" ")}`);
  }

  // RIDGE: the landed per-iteration ridge diagonal-add loop as a standalone
  // pool. 60 diagonal adds x 8,966 iterations = 537,960 adds/report. The
  // only structural transformation (pre-placement into the fill loop) is
  // RID's reduction-reorder bitwise violation (R11-C, 3,792/10,000
  // counterexamples); fusing the add into solveSymmetric would push ridge
  // semantics into a public general-purpose solver signature. This probe
  // measures the pool ceiling of anything else.
  {
    const p = 60;
    const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0.5));
    let sink = 0;
    // warmup
    for (let rep = 0; rep < 2; rep++) {
      for (let it = 0; it < 8966; it++) {
        for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
      }
      sink += xtwx[59]![59]!;
    }
    const passMs: number[] = [];
    for (let rep = 0; rep < 6; rep++) {
      const t0 = performance.now();
      for (let it = 0; it < 8966; it++) {
        for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
      }
      passMs.push(performance.now() - t0);
      sink += xtwx[0]![0]!;
    }
    const perReport = median(passMs);
    out(
      `probe RIDGE: landed ridge diagonal-add loop, 60 adds x 8,966 iterations = 537,960 adds/report, ` +
        `standalone ${perReport.toFixed(2)} ms/report pool ceiling (median of 6 passes; sink=${sink.toFixed(3)})`
    );
    out(
      `  -> the only structural moves are RID (pre-placement reduction reorder, bitwise violation, R11-C) and ` +
        `fusing ridge into the public solveSymmetric contract; anything else is bounded by this sub-band pool`
    );
  }
}

/* --------------------------------- main --------------------------------- */

scenarioEquivalence();
scenarioDeterminism();
scenarioProfile();
```

MORE_OPTIMA=no
BRANCH=cursor/r15-c-offline-routing-fifteenth-pass-83a1
