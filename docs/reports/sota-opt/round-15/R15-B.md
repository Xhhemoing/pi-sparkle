# R15-B：live 路由切片 Round 15 十五搜报告

MODEL_SLUG=claude-fable-5-thinking-xhigh

**战役:** 全库持久 SOTA 优化 Round 15 / R15-B（十区之一，R1-B…R14-B 的第十五遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `0958c1d`（含 R14-H 空枚举并入与 R15-B 派发；R14-J / R15-A 在飞，未等待、未触碰）
**分支:** `cursor/r15-b-live-routing-fifteenth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**空枚举：无达门槛的新更优解，切片零代码改动，未铸任何 S15-B-\* ID；
R4-B…R14-B 的整片成本天花板经第十二次复测成立，配置态 × 命令类矩阵全六格 +
prior 双档格全部复测仍带内，并新增两个「测量学基底」测量格——A/A 噪声地板
标定格与引擎版本敏感性格（V8 12.4 vs 13.6 双引擎全格复跑）——补上十四轮
从未直接测过的决斗协议本体噪声与引擎代际两个维度，切片按指令收口。**
关键前提事实：切片 10 个文件自 R1-B 的裁决基线 `94ed3d9` 以来**逐字节零
变化**（`git diff 94ed3d9..0958c1d` 对 10 个切片文件为空），且 `git diff
0dc433d..0958c1d -- src/` **整棵 src 树为空**、`git log 78f3b64..0958c1d
-- src/` 零提交（R10-B 基线之后仅 docs 合入）——R9-B…R14-B 逐条复核过的
调用方图景本轮已重新 grep 全 src 取证，逐条原样成立，无新增调用方（§0）。
R1-B 的结构下界论证、R4-B 的聚合天花板论证与 S1-B-\* … S13-B-1 全部裁决
对当前代码原样成立。

天花板本轮实测复核为 **9.5–10.9 ms/eval（M=2）/ 18.5–25.4 ms/eval
（M=10）**（canonical 引擎 Node 22.22.2），replay-faithful token-free 口径
**7.3–9.9 ms/eval**，10× 压力 109.7–140.9 ms/eval——M=2 与 R14-B
（9.3–10.2）同带略宽；M=10 三次中一次（18.53）落在 R12-B…R14-B 历史带
（17.6–19.7）内、两次（23.9–25.4）偏高，判为本 VM 个体偏慢加噪
（§2d 的 A/A 地板实测 ±2.3 ms/窗直接解释单元读数的数 ms 摆动；字节同一性
已证；replay-faithful、live face、占比结论全部与历史带重叠）。配置态矩阵
全六格实测：M=7 configured+learned **15.1–18.5 ms/eval**、M=10
stress+learned **18.6–24.3 ms/eval**。prior 双档格逐格复现：假设 N=2000 档
M=2/7/10 = 37.6–42.1 / 53.6–59.1 / 80.0–88.2 ms/eval（仍为首个若有流量即
越线的邻接配置，今日流量仍为零——eval-routing 不传 prior 本轮重新取证，
`eval-routing.ts:353–354` 只传 learned）；真实档 live face
M=7+prior+learned 300.5–302.6 µs/批。**S12-B-2 重开条件（任何 N≥10³
调用方传 prior）本轮重新取证未触发，条件式落地物维持在架。** 落地线
（数十~数百 ms 或复杂度类下降）在现有生产流量下依旧不可达：把整个切片
成本消为零也在落地线下沿之下。

本轮第十五组透镜扫的是**测量学基底**——R14-B 把代码之下的引擎态
（GC/分配 + JIT 层级）收口后，还剩两个从未直接测过的层：**裁决协议本身
的噪声地板**与**引擎代际**（十四轮全部测量都在单一引擎 Node 22.x /
V8 12.4 上做出）：

1. **A/A 噪声地板标定格（§2d，新测量格）**：用 R1-B…R14-B 决斗的**精确
   协议**（`delta = bench(armA,8) − bench(armB,8)`，每运行 5 轮）测两臂
   **完全相同**的生产 assignTasks——真实效应恰为零，故 delta 分布即协议
   噪声地板本体。实测 N=2000 批档地板 **max-abs 762–2346 µs/窗（V8 12.4）
   / 245–1182 µs/窗（V8 13.6）**，且真零效应下单窗符号计数跑满 **0/5 …
   5/5 全谱**——含一个 **5/5 全正的假稳定符号窗**（M=10 run2，
   +228~+1785 µs）。两条结构性结论：(a) 凡真实差额低于 ~1–2 ms/批的
   候选，该协议物理上无法为其出具稳定符号——S12-B-1（−2748~+2859 µs）、
   S13-B-1（−1056~+639 µs）、S9-B-3、R14-B 分层税（−577~+612 µs）的
   15 轮符号翻转全部坐落在实测地板之内，历史拒绝从**测量端**获得直接
   证书（与 R14-B §2d 从**成因端**给出的 GC scavenge 量子解释互为补
   证）；(b) 单窗符号稳定**可以说谎**（真零效应出过 5/5），故「符号
   稳定」判据必须同时满足量级 ≥ 地板 + 跨运行复现——这与落地线（数十
   ms）自洽，也回溯确认了 S13-B-1 fat×M=10 格（+91~+1549 µs，15/15 正）
   按量级淘汰的正确性。live face 档地板 5.7–78.2 µs——S13-B-1 的
   ±15 µs live-face 差额同样在地板之内。
2. **引擎版本敏感性格（§2e，新测量格）**：全套天花板 + 矩阵 + prior +
   A/A 格在 **Node 22.22.2（V8 12.4）与 Node 24.19.0（V8 13.6）双引擎
   逐格复跑**（十四轮从未变过引擎维度）。稳态结论：V8 13.6 把 token-free
   replay 各格一致加速 **~15–30%**（M=10+learned 18.6–24.3 →
   15.3–18.6 ms/eval），prior 单拾取成本引擎不变（19.6–21.1 vs
   19.6–19.9 µs），成本结构占比稳定，A/A 地板同量级——**没有任何格在
   任一方向越过落地线**；一次 ~1.3× 的均匀漂移不可能把任何已关 µs 级
   候选抬到数十 ms，排除表整体对引擎代际鲁棒。测量卫生学新发现：**首测
   格分层税对引擎敏感**（cold−steady = +2.1–2.6 ms/eval @12.4 →
   +3.9–4.5 ms/eval @13.6，经专用次序探针三次复现分离；N24 run1 的
   22.3 ms/eval 首格读数为该方差的一次极端窗口），已记档防止后续轮次把
   首格读数误读为代码漂移——R4-B…R15-B 的天花板 M=2 格历来是进程首测
   位置，同位置口径跨轮可比性不受影响。

排除表换名重提审计全过（§4）：枚举中冒出的每个念头均落在已关 ID、已落地
赢家、架构裁决或契约地板上。未重开任何 X\* / S1-\* … S14-\*（R14 为空
枚举无 ID）条目；S13-B-1 / S12-B-1..2 / S11-B-1 的换名检查逐条通过
（§4.2）。引擎升级本身（免费 ~20% 提速）是环境/运维事项，不构成切片
生产代码优化、不铸 ID（§2e 第 5 条）。按指令不硬凑赢家：现状仍为该数据
面契约下的 SOTA，切片关闭。

## 0. 范围与约束遵守

- 切片（10 文件口径，与 R8-B…R14-B 一致）：`src/routing/{r0,assign,assign-plan,policy,live-cascade,live-selection,analyze-task,primary-catalog,catalog-model}.ts`、
  `src/supervisor/model-router.ts` 全量重读；清单确认恰为 10 个文件。上下游
  `offline-logit.ts`、`capability-registry.ts`、`cascade-evidence.ts`、
  `public-prior.ts`、`learning/learned-routing.ts` 只读，一行未改。S7-C 落地
  在 `offline-logit.ts`（切片外）未触碰，未另起任何平行 live R1 实现。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表，含 S13-B-1、
  S12-B-1..2）/ round-15/PLAN.md / round-14/PLAN.md + R14-B.md /
  round-13/R13-B.md / 更早 B 报告按需。
- 基线漂移检查（指令项）：`git diff 94ed3d9..0958c1d -- <切片 10 文件>`
  为空；`git diff 0dc433d..0958c1d -- src/` 整棵 src 为空；`git log
  78f3b64..0958c1d -- src/` 零提交——调用方图景与只读上下游必然零变化；
  按指令仍在本 VM 重新锚定全部数字并重新 grep 调用方，未拷贝 R14-B 数值。
- 调用方重取证（指令项，本轮重新 grep 全 src）：
  - `assignTasks` 生产调用方四处不变——`cli/main.ts:365 smartChildPlan`
    （传 learned+prior，N≤30）、`track/primary-split.ts:41`（传 prior，
    C≤~6）与 `track/loop.ts:95–96`（经 primary-split，传 learned+prior）、
    `adaptation/eval-routing.ts:353–354`（**不传 prior、传 learned**，
    N=episodes 级、每 eval 恰 2 次调用——「×2/eval」乘数本轮重验）。
  - `assignOne` src 内零生产调用方；`routeR0` 唯一调用方
    r1-shadow-report.ts:88；`applyCascade` 唯一调用方 cascade-evidence.ts
    （库 R0 路径）。
- 换名重提检查：S13-B-1 / S12-B-1..2 / S11-B-1 / S10-B-1..3 未以任何变体
  重开（§4.2）；本轮枚举中识别出并**未列为新候选**的既有方案换名见 §4.3。
  S12-B-2 的重开条件（任何 N≥10³ 调用方传 prior）本轮重新取证**未满足**，
  故未落地、未重提。
- R1/posterior/offline-\* 未碰；live 保持 R0 等价，R1 未接线：
  `live-isolation` 3/3 绿（§6）。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、双 LCB 双归因保留、提升
  proposal-first、Checkpoint F-PROD 开放 ADR-005、CAS/凭据/数据面契约/公开
  签名不动、无阈值改动、不改测试）零 diff 天然满足。不声称
  Outcome-supported。
- 零 diff，公开 API / 决策对象 schema / refusal 消息优先级 / tie-break
  语义天然不变。无赢家故仿真脚本**未入库**（完整源码见附录），未提交
  `scripts/round15-r15b-*.ts`。EXCLUSIONS.md / PROGRESS.md / PLAN.md 未
  触碰；仓库变更仅本报告一个文件。

## 1. 第十五遍搜索方法：测量学基底透镜（A/A 噪声地板 + 引擎代际）

R1-B「输出契约渐近下界」…R13-B「跨界调用点记忆化键域 + learned 规模
盲区」、R14-B「引擎态基底（GC/分配预算 + JIT 层级）」。源码层面
（R12-B 12×19 叉积）、输入/目录/prior/learned 形状与规模维度
（R9-B/R11-B/R12-B/R13-B）、引擎运行态（R14-B）全部清点后，还剩两个
从未直接测过的层，且都在「测量」而非「代码」一侧：

1. **裁决协议自身的噪声地板**（§2d）：十五轮里每一次「符号翻转 → 拒绝」
   都隐式引用了一个从未直接标定过的量——决斗协议对真零效应的 delta
   分布。R14-B 从成因端（单次 scavenge 0.7–1.2 ms）解释了翻转，但协议
   端到端地板（含调度、分层、计时器粒度、缓存态漂移的总和）从未直测。
   本格用两臂同码的 A/A 决斗直测之；若地板 « 历史候选差额，历史拒绝
   需要重审——实测相反，地板把历史全部被拒差额带完整覆盖（§2d）。
2. **引擎代际敏感性**（§2e）：十四轮的每个数字都出自单一引擎
   （Node 22.x / V8 12.4）。若 V8 13.x 系统性改变切片成本结构（例如某
   已关候选的负优化在新分层管线下翻正），排除表将出现引擎依赖的暗伤。
   本格在 Node 24.19.0（V8 13.6，本 VM 经 nvm 现装）上逐格复跑全套
   harness 并与 canonical Node 22.22.2 并排（含 A/A 地板与专用首格次序
   探针）。
3. **换名识别透镜**：枚举中冒出的其余想法逐一对表（§4.3），全部识别为
   既有条目换名/换址或零流量位，不立新 ID。

## 2. 天花板复测：R4-B…R14-B 收口第十二次复核成立

实测（本 VM，canonical 引擎 Node 22.22.2，三次完整运行区间；语料生成器
与 R4-B…R14-B 逐字节相同、种子 `0xb44b01` 复用以保证可比；完整脚本见
附录）：

```text
ceiling eval-replay N=2000: assignTasks M=2 4763.8–5466.6us | M=10 9267.4–12705.9us | analyzeTask share 1136.8–1206.6us (21–24%)
ceiling per eval run (x2 calls): M=2 total=9.53–10.93ms | M=10 total=18.53–25.41ms | analyzeTask total=2.27–2.41ms
ceiling replay-faithful (token-free) N=2000: assignTasks M=2 3637.0–4930.6us per call (7.27–9.86ms per eval x2)
ceiling 10x stress N=20000: assignTasks M=2 54.9–70.5ms per call (109.7–140.9ms per eval x2)
ceiling CLI live face N=30: assignTasks M=2 49.1–52.2us per call
```

推论链（R4-B §2 …R14-B §2 的各条在复测数字上原样成立）：

1. M=2 天花板 9.5–10.9 ms/eval，与 R14-B（9.3–10.2）同带略宽。M=10
   18.5–25.4 ms/eval：三次中一次在历史带（17.6–19.7）内、两次偏高——
   live face（49.1–52.2 vs R14-B 47.1–50.4 µs）与占比结论全部同带，判为
   本 VM 个体偏慢加噪而非代码漂移（字节同一性已证；§2d 实测本 VM 单窗
   噪声 ±2.3 ms，可直接解释 M=10 单元读数的摆动幅度）。
2. R5-B 收紧口径复测成立：token-free 真实回放 7.3–9.9 ms/eval。
3. 复杂度类通道维持关闭：R1-B §2 逐函数下界在逐字节未变的代码上原样成立。
4. 结构性重开条件不变：10× 压力（N=20000）下切片全量 ~109.7–140.9
   ms/eval（本 VM 偏慢端；历史 98.6–110.8）。

## 2b. 配置态 × 命令类矩阵：全六格复测（第六次全格复核）

replay-faithful token-free 语料（seed `0xb44b01`，N=2000），目录与 learned
策略与 R8-B…R14-B 逐字节相同（Node 22.22.2）：

```text
matrix replay N=2000 [M=2 default, no learned]: 7.25–9.63ms per eval x2
matrix replay N=2000 [M=2 default, learned]: 7.65–9.44ms per eval x2
matrix replay N=2000 [M=7 configured, no learned]: 14.22–18.31ms per eval x2
matrix replay N=2000 [M=7 configured, learned]: 15.09–18.52ms per eval x2
matrix replay N=2000 [M=10 stress, no learned]: 17.29–22.47ms per eval x2
matrix replay N=2000 [M=10 stress, learned]: 18.58–24.33ms per eval x2
matrix CLI live face N=30: M=7 configured+learned 105.8–115.9us per call
matrix per-child cascade M=7: plan=537–557ns decide=333–338ns
```

全格无翻案：「learned 非空只加个位数百分比」的 R8-B…R14-B 结论继续成立
（M=2 格 run1 出现 learned 更快的倒挂噪声，与 R11-B/R13-B 同现象——
§2d 的地板量化了该倒挂的物理必然性）。live 面与级联探针仍低于落地线三个
量级。

## 2c. prior 双档格复测（第四次跨 VM 复现；S12-B-2 重开条件复核）

流量取证先行（§0）：传 prior 的生产调用方仍仅 CLI live face（N≤30）与
track loop/primary-split（C≤~6）；唯一 N=2000 级调用方 eval-routing
**不传 prior**（本轮重新 grep 取证，`eval-routing.ts:353–354`）。两档实测
（Node 22.22.2）：

```text
matrix prior HYPOTHETICAL replay N=2000 [M=2 default, prior]: 37.63–42.06ms per eval x2
matrix prior HYPOTHETICAL replay N=2000 [M=7 configured, prior]: 53.64–59.11ms per eval x2
matrix prior HYPOTHETICAL replay N=2000 [M=10 stress, prior]: 80.00–88.20ms per eval x2
matrix prior REAL-SCALE live face N=30: M=2+prior 192.0–196.2us | M=7+prior+learned 300.5–302.6us per call
prior unit cost: pickFromPublicPrior(edit, M=7 pool, 56-row snapshot) = 19617–21087ns per call
```

R12-B…R14-B 三条结论逐格复现：假设规模下 prior 仍是首个越线邻接配置
（若有流量）；真实规模下 prior 面仍是 µs 级；**S12-B-2 的重开条件（任何
N≥10³ 调用方传 prior）未触发**，条件式落地物维持在架、未落地、未重提。

## 2d. 新测量格 A：A/A 噪声地板标定（决斗协议本体的直接标定）

方法：两臂**同为未改动的生产 `assignTasks`**、同语料（seed `0xb44b01`
replay-faithful N=2000 + learned = 生产 eval 形状；live face N=30 +
learned + prior），协议与历史候选决斗逐字节相同
（`delta = bench(armA,8) − bench(armB,8)`，5 轮/运行；live face 用
reps=400，与 S13-B-1 live-face 格同口径）。真实效应恰为零。三次完整运行
（Node 22.22.2）：

```text
aa-noise-floor N=2000 [M=2 default, learned]: 窗口 max-abs = 843.8 / 1064.1 / 762.5 us | 符号 4/5, 3/5, 3/5 positive
aa-noise-floor N=2000 [M=7 configured, learned]: 窗口 max-abs = 866.0 / 1745.5 / 1707.7 us | 符号 3/5, 3/5, 2/5 positive
aa-noise-floor N=2000 [M=10 stress, learned]: 窗口 max-abs = 783.8 / 1785.3 / 2345.5 us | 符号 2/5, 5/5, 3/5 positive
aa-noise-floor live-face N=30 [M=7+learned+prior]: 窗口 max-abs = 16.85 / 10.25 / 36.38 us
```

（另有两次同 harness 补充运行，其 M=10 窗符号为 1/5 与 0/5、max-abs
2557.1 / 1493.6 µs——与上带同阶；连同主三次，真零效应的单窗符号计数在
本 VM 实测跑满 **0/5、1/5、2/5、3/5、4/5、5/5 全谱**。）

三条结论：

1. **协议地板把历史全部被拒差额带完整覆盖**：N=2000 批档地板 max-abs
   762–2346 µs/窗。对照：S12-B-1 真实档 −2748~+2859 µs、S13-B-1 真实档
   −1056~+639 µs、R14-B route 分层税 −577~+612 µs、S4-B-4/S5-B-3 等
   百 µs 噪声带族——全部量级 ≤ 地板。这些候选的 15 轮符号翻转不是运气
   差，是**测量物理**：该协议无法为低于地板的效应出具稳定符号。历史
   拒绝就此获得测量端直接证书，与 R14-B §2d 的成因端解释（单次
   scavenge 0.7–1.2 ms 量子）互为补证、数量级吻合。
2. **单窗符号稳定可以说谎**：真零效应出过一个 5/5 全正窗（M=10 run2，
   +228~+1785 µs）与 0/5 全负窗。故「符号稳定」不可单独作裁决判据，
   必须同时满足**量级显著超过地板**（≥数 ms/批）+ 跨运行复现——这正是
   验收线第 3 条与落地线的物理基础，也回溯确认 S13-B-1 fat×M=10
   （15/15 正但 +91~+1549 µs）按量级淘汰而非按符号采信的正确性。
3. live face 档地板 5.7–78.2 µs（双引擎合并带）：S13-B-1 live-face
   ±15 µs 差额、S12-B-2 真实档若干 µs 级读数全在地板内——live 面的
   任何 µs 级候选同样物理不可证书。**未来任何 B 切片微观候选可先对照
   本格地板预筛：预期差额 < ~2 ms/批（N=2000）或 < ~80 µs/批（N=30）
   者无需实现即知不可证书。**

## 2e. 新测量格 B：引擎版本敏感性（V8 12.4 → 13.6 双引擎全格复跑）

方法：同一 harness 逐字节不变，分别在 canonical Node 22.22.2
（V8 12.4.254.21）与本轮经 nvm 现装的 Node 24.19.0（V8 13.6.233.17，
满足 engines `>=22.19.0`）上各三次完整运行；另有专用**首格次序探针**
（cold-position vs steady-position 测同一 with-token M=2 格，两引擎各
三次）分离测量位置效应与语料形状效应。Node 24.19.0 关键格：

```text
[V8 13.6] ceiling per eval x2: M=2 11.34–22.33ms(首格,见下) | M=10 17.68–26.02ms | replay-faithful 6.32–9.48ms | 10x stress 102.5–124.5ms
[V8 13.6] matrix replay N=2000: M=2 no/learned 6.33–6.72/6.80–6.88 | M=7 no/learned 11.53–15.02/12.12–15.01 | M=10 no/learned 14.14–17.73/15.25–18.55 ms per eval x2
[V8 13.6] matrix CLI live face N=30: M=7 configured+learned 89.0–102.0us | cascade plan=493–679ns decide=283–315ns
[V8 13.6] prior HYPOTHETICAL N=2000: M=2 35.20–37.45 | M=7 49.30–53.95 | M=10 79.89–83.14 ms per eval x2
[V8 13.6] prior REAL live face: M=2+prior 191.3–206.2us | M=7+prior+learned 274.9–295.8us | per-pick 19557–19944ns
[V8 13.6] aa-noise-floor N=2000 max-abs: M=2 535.1/572.5/727.5 | M=7 580.6/1128.7/719.2 | M=10 933.7/1182.0/245.0 us | live-face 7.08/78.18/5.70us
order-probe with-token M=2 [V8 13.6]: COLD 10.88–11.37 | STEADY 6.86–7.03 ms per eval x2（三次）
order-probe with-token M=2 [V8 12.4]: COLD 10.20–10.86 | STEADY 7.99–8.47 ms per eval x2（三次）
```

五条结论：

1. **稳态方向一致为快**：V8 13.6 把 token-free replay 各格一致加速
   ~15–30%（M=2+learned 7.65–9.44 → 6.80–6.88、M=7+learned 15.09–18.52
   → 12.12–15.01、M=10+learned 18.58–24.33 → 15.25–18.55 ms/eval）；
   次序探针稳态下 with-token M=2 同样更快（8.0–8.5 → 6.9–7.0 ms/eval）。
   **无任何格出现语料形状回退**；prior 单拾取成本引擎不变
   （19.6–21.1 vs 19.6–19.9 µs），analyzeTask 占比同带——切片成本结构
   对引擎代际稳定。
2. **排除表整体引擎鲁棒**：没有格在任一方向越过落地线（全部
   still-below-landing-line=true ×双引擎）；~1.3× 均匀漂移不可能把任何
   已关 µs/百 µs 级候选抬两个量级到数十 ms；量级类裁决（X 系、S 系全部
   「低于落地线」条目）在 V8 13.6 上原样成立。
3. **A/A 地板引擎同量级**（12.4: 762–2346 µs；13.6: 245–1182 µs/窗）：
   符号翻转类拒绝（S12-B-1/S13-B-1/S9-B-3/分层税）在新引擎上同样物理
   必然，无需逐条复测。
4. **测量卫生学：首测格分层税对引擎敏感**。次序探针三次复现：同一
   with-token M=2 格 cold−steady = +2.1–2.6 ms/eval（V8 12.4）→
   +3.9–4.5 ms/eval（V8 13.6，约 2×）；N24 run1 主 harness 首格读数
   22.33 ms/eval 为该方差的一次极端窗口（探针 cold 典型值 10.9–11.4）。
   已记档：R4-B…R15-B 的天花板 M=2 格历来是进程首测位置、跨轮同位置
   可比；但**跨引擎或跨脚本比较首格读数会把分层税误读为代码漂移**——
   后续轮次引擎升级后复锚时必须用稳态位置口径。
5. **引擎升级本身不铸 ID**：Node 24 的免费 ~20% 提速是环境/运维事项
   （engines 字段与部署面），不是切片生产代码优化，不在本战役落地定义
   内（与 S5-I-5「引擎参数追打」同界线；该条的重开条件「引擎升级到
   ≥22.19 后复测」本轮顺带部分覆盖——V8 13.6 上 I 区病理不属本切片，
   未越区测量）。记录为观察项供运维参考。

## 3. 候选总表

**空。** 本轮无任何候选存活过换名审计（§4.3）；两个新测量格均为「协议/
环境标定」而非代码候选，按「宁可空枚举不铸装饰性 ID」纪律不铸。无赢家
落地，无等价仿真需求（无新候选可证等价），仿真为纯测量口径（附录）。

## 4. 关键裁决细节

### 4.1 为什么第十五遍聚焦测量学基底而不再扫源码/引擎态枚举空间

R12-B 的 12 结构位置 × 19 消除机制叉积、R9-B/R11-B/R13-B 的形状与规模
维度、R14-B 的 GC/JIT 引擎态在逐字节未变的代码上原样成立且无未清点格。
源码层面每个生产路径操作（§5 逐文件表）均落在已关 ID、已落地赢家、架构
裁决（toModelDescriptor 预计算，R1-B §4.4）或契约地板上。剩下的未测层
只在测量本体：裁决协议的噪声地板从未直测（十五轮的「符号翻转」判据全部
隐式依赖它），引擎维度从未变过（全部历史数字出自 V8 12.4 单引擎）。
§2d/§2e 证明：地板把历史被拒差额带完整覆盖（拒绝获得测量端证书），引擎
代际不改变任何裁决类别（排除表引擎鲁棒）。两格合并把「测量方法论」通道
一次性收口。

### 4.2 S13-B-1 / S12-B-1..2 / S11-B-1 与 S1-B-\*…S10-B-\* 换名检查（指令项）

- **S13-B-1**（learned 层批内记忆化）：未以任何变体重开。本轮 §2d 反向
  巩固其判决：其真实档差额带（−1056~+639 µs）完整坐落在 A/A 地板
  （max-abs 866–1746 µs @M=7）之内，符号翻转是测量物理必然。重开条件
  （applyLearnedRouting 本体成本增 ≥2 个量级）未触发——src 零变化。
- **S12-B-2**（prior 层批内记忆化，条件式落地物）：未重开。重开条件
  （任何 N≥10³ 调用方传 prior）本轮重新取证未满足（§0/§2c）；假设档
  53.6–59.1 ms/eval 与真实档 300.5–302.6 µs 双锚点逐格复现，维持在架。
- **S12-B-1 / S11-B-1**（route() 主循环结构维度的反索引/融合镜像对）：
  未以任何形态重提。§2d 的地板直测反向证明其 15 轮符号翻转（S12-B-1
  −2748~+2859 µs ⊂ 地板 ±2346 µs 同阶带）为协议物理；「主循环结构维度
  双向封死」维持。
- **S10-B-1..3 / S9-B-1..4 / S8-B-1..4 / S7-B-1..6 / S6-B-1..5 /
  S5-B-1..4 / S4-B-1..5 / S3-B-1..6 / S2-B-1..4 / S1-B-1..8**：站点字节
  同一，未重提、未变体；其中全部「百 ns~百 µs + 符号翻转」类条目本轮
  经 §2d 获得统一的测量端拒绝证书。
- **S2-D-4 / S10-D-2**（eval-routing 双 assignTasks，D 区已关）：
  「×2/eval」乘数本轮重验（`eval-routing.ts:353–354`），未越区重提。
- **R14-B 两个通道级收口**（分配消除类 ≤4.6 ms/eval 预算；JIT 局部性
  收益池为零）：未重开；本轮未重跑 GC/natives 格（其结论为通道收口，
  非逐轮锚点），§2d/§2e 的新格与其正交且结论相容。

### 4.3 未立候选的换名/零流量/理论即负识别（负例记录）

- **analyzeTask 正则链合并/提升/重排**：S4-B-1（单遍多模式）+ S1-B-1
  （提升去重）+ S6-B-3（交替项重排）+ S7-B-1（deploy 门合取）覆盖。
  不铸 ID。
- **assignPlanned options 三条件 spread 单态化**：S9-B-3 原文（九次符号
  翻转=抖动；本轮 §2d 给出其物理解释）。不铸 ID。
- **partitionLiveCandidates allowed Set→includes / 与 selection 融合**：
  S12-B-1 / S11-B-1 镜像对。不铸 ID。
- **learned/prior 层批内记忆化**：S13-B-1 / S12-B-2 原文。不铸 ID。
- **toModelDescriptor 预计算/WeakMap**：R1-B §4.4 架构裁决 + X1-1。
  维持，不铸 ID。
- **justification 模板字面量/常量尾段**：S5-B-3 + S7-B-4。不铸 ID。
- **oneHotDistribution / makeApprovalPlan / 冻结单例享元**：S4-B-3 /
  S6-B-2 / S7-B-5 身份可观察族。不铸 ID。
- **validateConfig/validateInput 微观（Set/直扫/首字符卫）**：S10-B-3 /
  S9-B-1..2 / S3-B-5 / S1-B-7。不铸 ID。
- **cheapFirstTiers 装饰排序/码点比较**：S3-B-2..3 冻结面。不铸 ID。
- **「换更快引擎跑同代码」作为候选**：非代码优化，落地面在 engines
  字段与部署环境（运维/父代理面），且 §2e 已证其不改变任何裁决类别。
  按界线不铸 ID（S5-I-5 同族界线）。
- **`effectiveConfidenceThreshold` / `routeTask` / `routeFlowNode` 站位
  微观**：R12-B 零流量取证在 src 零变化下转移成立。不铸 ID。

## 5. 逐文件收口（第十五遍透镜下的残余检查）

| 文件 | 检查项 | 结论 |
| --- | --- | --- |
| `assign.ts` | S9-B-3 条件 spread、S13-B-1/S12-B-2 宿主端、两次防御拷贝护栏（S2-B-1/S8-B-4/S1-B-8）维持；§2d 地板覆盖其全部历史微观差额带 | 无候选 |
| `assign-plan.ts` | S12-B-2 未重开（prior 流量条件未触发 §2c）；S8-B-1 维持；per-pick 单位成本双引擎不变（§2e） | 无候选 |
| `supervisor/model-router.ts` | 主循环结构维度（S11-B-1/S12-B-1）维持封死且获测量端证书（§2d/§4.2）；S10-B-2/3、S9-B-1/2/4、S8-B-3、S7-B-4/6、S6-B-1/2/5、S5-B-4、S3-B-5、S2-B-2、S1-B-7 维持 | 无候选 |
| `analyze-task.ts` | 正则链四重覆盖确认（§4.3）；S10-B-1、S1-B-1/2/3、S4-B-1、S5-B-1/3、S6-B-3、S7-B-1/2/5 维持；share 占比双引擎稳定 | 无候选 |
| `policy.ts` | descriptor 预计算架构裁决维持（R1-B §4.4）；S7-B-3、S5-B-2、S4-B-2/3 维持；全约束独立评估为契约下界 | 无候选 |
| `live-selection.ts` | 无新面；S8-B-2/3、S3-B-3 冻结面维持 | 无候选 |
| `live-cascade.ts` | 级联探针双引擎复核无漂移（493–679/283–338 ns）；S1-B-4/5、S3-B-2/3 维持 | 无候选 |
| `r0.ts` | 无新面；routeR0 唯一调用方 r1-shadow-report 重验；`applyCascade` 生产不可达维持；S1-B-6/S2-B-3/S3-B-4/S4-B-3 维持 | 无候选 |
| `primary-catalog.ts` / `catalog-model.ts` | 无新面；享元/身份族维持（S7-B-5/S1-C-10）；纯构造 Θ(字段) | 无候选 |
| （跨切片，只记录不改） | `applyLearnedRouting`（learning 区）、`pickFromPublicPrior`（public-prior.ts，S12-B-2 重开时复测）、`flowchart-supervisor.ts:684` 第二 route 形状（graph/supervisor 区）均只读 | 不属本切片 |

## 6. 前后对比与测试

无代码 diff。仓库变更仅本报告一个文件。零改动下门禁与相关套件全绿
（Node 22.22.2）：

```bash
pnpm gate        # typecheck ✓ lint ✓ test ✓（tests 1169 / pass 1168 / fail 0 / skipped 1）build ✓
npx tsx --test test/unit/routing/*.test.ts test/unit/supervisor/*.test.ts
# tests 260 / suites 18 / pass 260 / fail 0
npx tsx --test test/unit/routing/live-isolation.test.ts
# tests 3 / pass 3 / fail 0   （live 面不 import R1/bandit/shadow 继续成立）
```

环境披露：本 VM 预装 Node 22.14.0 低于 engines `>=22.19.0`，经 nvm 切换
Node 22.22.2（canonical，与 R7-B…R14-B 同处置）后运行全部 canonical
测量与门禁；§2e 引擎格另经 `nvm install 24.19.0` 现装 Node 24.19.0
（V8 13.6.233.17，满足 engines）。node_modules 经
`pnpm install --frozen-lockfile` 补装。与本切片与本报告零关联。

仿真（临时脚本未入库——无赢家不落地死代码；完整源码见附录；天花板语料
复用 R4-B 的 `0xb44b01` 以保证可比；纯测量口径，无等价 fuzz——本轮无新
候选可证）canonical 引擎最终一次运行：

```text
engine node=v22.22.2 v8=12.4.254.21-node.39
ceiling eval-replay N=2000: assignTasks M=2 4763.8us | M=10 9267.4us | analyzeTask share 1136.8us (24%)
ceiling per eval run (x2 calls): M=2 total=9.53ms | M=10 total=18.53ms | analyzeTask total=2.27ms
ceiling replay-faithful (token-free) N=2000: assignTasks M=2 3637.0us per call (7.27ms per eval x2)
ceiling 10x stress N=20000: assignTasks M=2 54.9ms per call (109.7ms per eval x2)
ceiling CLI live face N=30: assignTasks M=2 49.1us per call
matrix replay N=2000 [M=2 default, no learned]: 3622.9us per call (7.25ms per eval x2)
matrix replay N=2000 [M=2 default, learned]: 3823.4us per call (7.65ms per eval x2)
matrix replay N=2000 [M=7 configured, no learned]: 7110.8us per call (14.22ms per eval x2)
matrix replay N=2000 [M=7 configured, learned]: 7546.6us per call (15.09ms per eval x2)
matrix replay N=2000 [M=10 stress, no learned]: 8646.3us per call (17.29ms per eval x2)
matrix replay N=2000 [M=10 stress, learned]: 9292.4us per call (18.58ms per eval x2)
matrix CLI live face N=30: M=7 configured+learned 105.8us per call
matrix per-child cascade M=7: liveCascadePlanFromAssignment=537ns decideLiveCascade=336ns
matrix prior HYPOTHETICAL replay N=2000 [M=2 default, prior]: 18815.3us per call (37.63ms per eval x2)
matrix prior HYPOTHETICAL replay N=2000 [M=7 configured, prior]: 26820.5us per call (53.64ms per eval x2)
matrix prior HYPOTHETICAL replay N=2000 [M=10 stress, prior]: 39997.9us per call (80.00ms per eval x2)
matrix prior REAL-SCALE live face N=30: M=2+prior 192.9us | M=7+prior+learned 301.8us per call
prior unit cost: pickFromPublicPrior(edit, M=7 pool, 56 rows) = 21087ns per call
aa-noise-floor N=2000 [M=2 default, learned]: delta(A - A) us = [762.5, 414.2, -561.9, -85.9, 277.6] | 3/5 positive | max-abs=762.5us
aa-noise-floor N=2000 [M=7 configured, learned]: delta(A - A) us = [-742.2, -1.9, 1707.7, 826.8, -1349.3] | 2/5 positive | max-abs=1707.7us
aa-noise-floor N=2000 [M=10 stress, learned]: delta(A - A) us = [2345.5, 721.0, -2051.6, 114.5, -24.1] | 3/5 positive | max-abs=2345.5us
aa-noise-floor live-face N=30 [M=7+learned+prior]: delta(A - A) us = [11.57, 16.22, -0.31, -2.81, -36.38] | max-abs=36.38us

CONCLUSIONS: ceiling M=2 per-eval=9.5ms M=10 per-eval=18.5ms replay-faithful=7.3ms (holds-below-landing-line=true) | configured-state M=7+learned replay per-eval=15.1ms (still-below-landing-line=true) | M=10+learned replay per-eval=18.6ms (still-below-landing-line=true) | configured-state live-face=105.8us | prior-cells real-scale-live-face M=7+prior+learned=301.8us hypothetical-N2000 M=7+prior per-eval=53.6ms per-pick=21087ns | aa-noise-floor M=10 N=2000 max-abs-us=2345.5 signs=3/5-positive | aa-noise-floor live-face max-abs-us=36.38
ALL CHECKS PASSED (measurement-only harness; corpora seeded + deterministic)
```

三次完整独立运行（×2 引擎 = 六次）全部检查零失败；A/A 与引擎格结论字段
六次一致（全部 still-below-landing-line=true）。另有两次 Node 22 补充
运行（早期 tail 截断只留 A/A 与 CONCLUSIONS 字段）数据同带，其 M=10 A/A
窗（1/5、0/5，max-abs 2557.1/1493.6 µs）计入 §2d 的符号全谱陈述、未计入
主三次区间。引擎格三次运行与首格次序探针（两引擎各三次）数据见 §2e。

## 7. 新增排除 ID

**无。** 本轮为诚实空枚举——无候选达到铸 ID 门槛（两个新格均为测量
标定，无代码候选走到实现，按纪律不铸装饰性 ID）。

**结构性重开条件（对整个切片，与 R4-B…R14-B 一致并经本轮第十二次复测
确认）**：eval 数据集规模增长 ≥1 个量级（N≥20000 时切片全量
~109.7–140.9 ms/eval），或 analyzeTask/route 进入每 turn 热路径，或出现
新的高频调用方，或配置目录 M 增长到使 M=10 压力口径失效；S12-B-2 专属
条件维持：任何 N≥10³ 调用方开始传 prior（本轮重新取证未触发）；S13-B-1
专属条件维持：applyLearnedRouting 本体单次成本增长 ≥2 个量级。R14-B 的
两条通道级收口（分配消除 ≤4.6 ms/eval；JIT 局部性收益池为零）维持。
本轮新增两条测量学收口：**微观候选证书通道**——任何预期差额
< ~2 ms/批（N=2000）或 < ~80 µs/批（N=30）的候选被 §2d 地板预先判定
不可证书，重开需协议换代（如硬件计数器/中位数-of-N 协议将地板压低
≥1 个量级）；**引擎代际通道**——排除表经 V8 12.4→13.6 双引擎复跑证明
鲁棒，重开需下一次 V8 大版本使任一格漂移 ≥1 个量级或翻越落地线（届时
先用稳态位置口径复锚，避免 §2e 第 4 条的首格分层税伪信号）。输入/目录
形状通道（R9-B/R11-B）、融合与反索引通道（S11-B-1/S12-B-1）、learned
规模通道（R13-B §2d）、GC/JIT 通道（R14-B）维持关闭。

MORE_OPTIMA=no

## 附录 A：测量仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后在仓库根目录 `node --import tsx <file>`
（依赖已装；两引擎各跑三次即复现 §2–§2e 主格）。天花板语料复用 R4-B 的
`0xb44b01`；配置态目录与 R8-B…R14-B 逐字节相同；prior 快照与 R12-B…
R14-B 逐字节相同（仅 snapshotId 改为 r15b 标记）。

```ts
/**
 * R15-B deterministic measurement simulation (fifteenth pass).
 * No fresh candidate cleared the rename-reject audit this round, so this is a
 * measurement-only harness (no equivalence fuzz is needed when there is
 * nothing new to prove equivalent):
 * 1) Re-anchors the R4-B..R14-B aggregate slice ceiling (corpus seed 0xb44b01
 *    reused verbatim) plus replay-faithful token-free and the FULL
 *    configured-state x command-class matrix ({M=2 default, M=7 configured,
 *    M=10 stress} x {no learned, learned}) plus CLI live face, per-child
 *    cascade probes, and the R12-B prior cells (real N=30 + hypothetical
 *    N=2000 + per-pick unit cost; S12-B-2 reopen-condition re-anchor).
 * 2) NEW measurement cell A: A/A noise-floor calibration. Fifteen passes of
 *    candidate duels rejected micro-candidates on "sign flips" (S12-B-1,
 *    S13-B-1, S9-B-3, R14-B tiering tax) without ever measuring the duel
 *    protocol's intrinsic noise floor directly. This cell runs the EXACT duel
 *    protocol (bench(arm,8) vs bench(arm,8), 5 rounds per run) with BOTH arms
 *    being the same unmodified production assignTasks — the true effect is
 *    exactly zero, so the observed delta distribution IS the noise floor any
 *    real candidate delta must clear. Cells: N=2000 replay x {M=2, M=7, M=10}
 *    + learned, and CLI live face N=30 (bench(arm,400)).
 * 3) NEW measurement cell B: engine-version sensitivity. All fourteen prior
 *    passes measured on one engine (Node 22.x / V8 12.4). This harness is run
 *    on BOTH Node 22.22.2 (V8 12.4) and Node 24.19.0 (V8 13.6) to test
 *    whether the ceiling bands, cost-structure shares, and the noise floor —
 *    i.e. every magnitude-based rejection in the exclusion table — are stable
 *    across a V8 major-generation change. The engine dimension lives in HOW
 *    this file is run (two node binaries), not in extra code.
 * Seeded PRNG (mulberry32) -> corpora fully reproducible.
 * Run: node --import tsx /tmp/r15b-sim.mts   (from the repo root, both nodes)
 */
import { performance } from "node:perf_hooks";
import { analyzeTask } from "/workspace/src/routing/analyze-task.js";
import { assignTasks, type AssignableTask } from "/workspace/src/routing/assign.js";
import { applyLearnedRouting, type LearnedRoutingPolicy } from "/workspace/src/learning/learned-routing.js";
import { catalogFromPrimary } from "/workspace/src/routing/primary-catalog.js";
import { type CatalogModelInput } from "/workspace/src/routing/catalog-model.js";
import {
  liveCascadePlanFromAssignment,
  decideLiveCascade
} from "/workspace/src/routing/live-cascade.js";
import { planAssignmentPolicy } from "/workspace/src/routing/assign-plan.js";
import { pickFromPublicPrior, type PublicPriorSnapshot } from "/workspace/src/routing/public-prior.js";
import { createModelRouter, type ModelRouterConfig } from "/workspace/src/supervisor/model-router.js";
import type { AgentRole } from "/workspace/src/domain/roles.js";
import type { TaskComplexity } from "/workspace/src/domain/flowchart.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}
function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const ROLES: readonly AgentRole[] = [
  "worker", "scout", "planner", "implementer", "reviewer", "tester", "debugger"
];
const OBJECTIVE_TEMPLATES = [
  "Survey the payment module",
  "Plan the checkout migration",
  "Run the unit tests",
  "Deploy payment credentials to production",
  "Implement retry logic for the ledger sync",
  "Review the audit log formatting nits",
  "Refactor and rename the tracking roller",
  "Fix the flaky spec for gate transitions",
  "Research and compare vector store options",
  "Investigate why the drop table migration failed in prod",
  "验证路由细节并补充测试",
  ""
];
const FRAGMENTS = [
  "deploy", "production", "credentials", "review", "tests", "coverage", "plan",
  "design", "survey", "research", "refactor", "implement", "add ", "fix ",
  "migrate", "verify", "\n", "the module", "碎片", " "
];
function genObjective(rng: () => number): string {
  if (rng() < 0.5) return pick(rng, OBJECTIVE_TEMPLATES);
  let text = "";
  const parts = 1 + Math.floor(rng() * 8);
  for (let i = 0; i < parts; i += 1) text += pick(rng, FRAGMENTS) + " ";
  return text;
}
function genTasks(rng: () => number, n: number): AssignableTask[] {
  return Array.from({ length: n }, (_, i) => ({
    taskId: `tsk_${i.toString().padStart(6, "0")}` as AssignableTask["taskId"],
    role: pick(rng, ROLES),
    objective: genObjective(rng),
    ...(rng() < 0.1 ? { contractRisk: rng() < 0.5 } : {}),
    ...(rng() < 0.3 ? { contextTokens: Math.floor(rng() * 100000) } : {}),
    ...(rng() < 0.3 ? { outputTokens: Math.floor(rng() * 4000) } : {})
  }));
}
/** Replay-faithful shape: adaptation/eval-routing.ts maps only taskId/role/objective. */
function genReplayTasks(rng: () => number, n: number): AssignableTask[] {
  return Array.from({ length: n }, (_, i) => ({
    taskId: `tsk_${i.toString().padStart(6, "0")}` as AssignableTask["taskId"],
    role: pick(rng, ROLES),
    objective: genObjective(rng)
  }));
}
function tenModelCatalog(): ModelRouterConfig {
  const models: CatalogModelInput[] = Array.from({ length: 10 }, (_, i) => ({
    id: `m${i}`,
    version: `m${i}-v1`,
    roles: ["actor", "critic"] as const,
    maxComplexity: (i >= 7 ? "HIGH" : "MEDIUM") as TaskComplexity,
    estimatedCostUsd: 0.05 * (i + 1),
    estimatedDurationMs: 500 * (i + 1),
    approvedForHighRisk: i >= 8
  }));
  return { policyVersion: "sim-r15b", models };
}
/** Configured-state catalog, byte-identical to R8-B..R14-B's (M=7, alias rows). */
function configuredCatalog(): ModelRouterConfig {
  const enabled: CatalogModelInput[] = [
    {
      id: "acme/fast-mini", version: "fast-mini-2026-01", providerId: "acme",
      roles: ["actor", "critic"] as const, maxComplexity: "MEDIUM" as TaskComplexity,
      estimatedCostUsd: 0.0009, estimatedDurationMs: 1_500,
      inputCostPerMTok: 0.4, outputCostPerMTok: 1.0,
      contextWindow: 200_000, maxOutputTokens: 16_000,
      capabilities: ["tool-use", "vision"], approvedForHighRisk: false
    },
    {
      id: "acme/std", version: "std-2026-02", providerId: "acme",
      roles: ["actor", "critic"] as const, maxComplexity: "MEDIUM" as TaskComplexity,
      estimatedCostUsd: 0.004, estimatedDurationMs: 1_500,
      inputCostPerMTok: 2.0, outputCostPerMTok: 4.0,
      contextWindow: 200_000, maxOutputTokens: 32_000,
      capabilities: ["tool-use"], approvedForHighRisk: false
    },
    {
      id: "orion/coder", version: "coder-9", providerId: "orion",
      roles: ["actor", "critic"] as const, maxComplexity: "MEDIUM" as TaskComplexity,
      estimatedCostUsd: 0.0035, estimatedDurationMs: 1_500,
      inputCostPerMTok: 1.5, outputCostPerMTok: 4.0,
      contextWindow: 128_000, maxOutputTokens: 8_000,
      capabilities: ["tool-use"], approvedForHighRisk: false
    },
    {
      id: "orion/lite", version: "lite-3", providerId: "orion",
      roles: ["actor", "critic"] as const, maxComplexity: "MEDIUM" as TaskComplexity,
      estimatedCostUsd: 0.0006, estimatedDurationMs: 1_500,
      inputCostPerMTok: 0.25, outputCostPerMTok: 0.7,
      contextWindow: 64_000, maxOutputTokens: 8_000,
      capabilities: ["tool-use"], approvedForHighRisk: false
    },
    {
      id: "acme/frontier", version: "frontier-2026-03", providerId: "acme",
      roles: ["actor", "critic", "judge", "router"] as const, maxComplexity: "HIGH" as TaskComplexity,
      estimatedCostUsd: 0.0125, estimatedDurationMs: 4_000,
      inputCostPerMTok: 5.0, outputCostPerMTok: 15.0,
      contextWindow: 400_000, maxOutputTokens: 64_000,
      capabilities: ["tool-use", "vision"], approvedForHighRisk: true
    }
  ];
  const fastRow = enabled[3]!;
  const primaryRow = enabled[4]!;
  return {
    policyVersion: "router-v1-live",
    models: [...enabled, { ...fastRow, id: "cheap" }, { ...primaryRow, id: "premium" }]
  };
}
function configuredLearned(): LearnedRoutingPolicy {
  return {
    primaryModelId: "acme/frontier",
    avoid: [{ modelId: "orion/coder", reason: "sim: weak review outcomes", family: "review" }],
    prefer: [{ family: "plan", modelId: "acme/frontier" }]
  };
}
function tenModelLearned(): LearnedRoutingPolicy {
  return {
    primaryModelId: "m9",
    avoid: [{ modelId: "m2", reason: "sim", family: "review" }],
    prefer: [{ family: "plan", modelId: "m9" }]
  };
}
function learnedM2(): LearnedRoutingPolicy {
  return {
    primaryModelId: "premium",
    avoid: [{ modelId: "cheap", reason: "sim", family: "review" }],
    prefer: [{ family: "plan", modelId: "premium" }]
  };
}

/* Realistic public-prior snapshot, byte-identical to R12-B..R14-B's (id retagged). */
const TS = "2026-08-19T00:00:00.000Z";
function realisticPrior(): PublicPriorSnapshot {
  const scores: {
    sourceId: string; modelAliases: string[]; raw: number; unit: string;
    fetchedAt: string; sourceUrl: string;
  }[] = [];
  const push = (sourceId: string, alias: string, raw: number, unit = "pass_rate", url = "https://example.com/board"): void => {
    scores.push({ sourceId, modelAliases: [alias, `${alias} (latest)`], raw, unit, fetchedAt: TS, sourceUrl: url });
  };
  push("aider-polyglot", "cheap", 0.41);
  push("aider-polyglot", "premium", 0.88);
  push("swe-bench-verified-mini", "cheap", 0.32);
  push("swe-bench-verified-mini", "premium", 0.74);
  push("terminal-bench-2.1-fixed-harness", "cheap", 0.5);
  push("terminal-bench-2.1-fixed-harness", "premium", 0.8);
  push("arena-coding", "cheap", 1180, "elo");
  push("arena-coding", "premium", 1367, "elo");
  push("aider-polyglot", "Acme Fast Mini 2026-01", 0.44);
  push("aider-polyglot", "acme/std", 0.61);
  push("aider-polyglot", "Orion Coder 9", 0.58);
  push("aider-polyglot", "orion/lite", 0.35);
  push("aider-polyglot", "Acme Frontier (2026-03)", 0.86);
  push("swe-bench-verified-mini", "Acme Fast Mini 2026-01", 0.38);
  push("swe-bench-verified-mini", "acme/std", 0.55);
  push("swe-bench-verified-mini", "Orion Coder 9", 0.51);
  push("swe-bench-verified-mini", "orion/lite", 0.28);
  push("swe-bench-verified-mini", "Acme Frontier (2026-03)", 0.79);
  push("terminal-bench-2.1-fixed-harness", "Acme Fast Mini 2026-01", 0.42);
  push("terminal-bench-2.1-fixed-harness", "acme/std", 0.57);
  push("terminal-bench-2.1-fixed-harness", "Acme Frontier (2026-03)", 0.82);
  push("arena-coding", "acme/std", 1240, "elo");
  push("arena-coding", "Orion Coder 9", 1215, "elo");
  push("arena-coding", "Acme Frontier (2026-03)", 1360, "elo");
  for (let i = 0; i < 10; i += 1) push("aider-polyglot", `m${i}`, 0.3 + i * 0.055);
  for (let i = 0; i < 10; i += 1) push("swe-bench-verified-mini", `m${i}`, 0.25 + i * 0.05);
  const rivals = ["rival/alpha", "rival/beta", "rival/gamma", "zephyr-long-name/model-x", "labs/quill", "labs/scribe"];
  for (const [i, id] of rivals.entries()) {
    push("aider-polyglot", id, 0.45 + i * 0.05);
    push("arena-coding", id, 1200 + i * 21, "elo");
  }
  return {
    schemaVersion: 1,
    snapshotId: "pps_r15b_realistic_v1",
    createdAt: TS,
    qualityBar: 0.55,
    scores,
    aliasMap: [
      { canonicalId: "acme/fast-mini", alias: "Acme Fast Mini 2026-01" },
      { canonicalId: "orion/coder", alias: "Orion Coder 9" },
      { canonicalId: "acme/frontier", alias: "Acme Frontier (2026-03)" }
    ]
  } as unknown as PublicPriorSnapshot;
}

const conclusions: string[] = [];
console.log(`engine node=${process.version} v8=${process.versions.v8}`);

/* ============================================================
 * §0 Ceiling re-anchor (R4-B..R14-B methodology, corpus seed 0xb44b01
 * reused verbatim) + replay-faithful token-free corpus (R5-B).
 * ============================================================ */
{
  const catalog2 = catalogFromPrimary({ primaryModelId: "premium" });
  const catalog10 = tenModelCatalog();
  const tasks = genTasks(mulberry32(0xb44b01), 2000);
  const totalM2 = bench(() => assignTasks({ catalog: catalog2, tasks }), 30);
  const totalM10 = bench(() => assignTasks({ catalog: catalog10, tasks }), 30);
  const analyzeOnly = bench(() => {
    for (const task of tasks) analyzeTask(task.objective, task.role);
  }, 30);
  console.log(
    `ceiling eval-replay N=2000: assignTasks M=2 ${(totalM2 * 1e3).toFixed(1)}us | M=10 ${(totalM10 * 1e3).toFixed(1)}us | analyzeTask share ${(analyzeOnly * 1e3).toFixed(1)}us (${((analyzeOnly / totalM2) * 100).toFixed(0)}%)`
  );
  console.log(
    `ceiling per eval run (x2 calls): M=2 total=${(totalM2 * 2).toFixed(2)}ms | M=10 total=${(totalM10 * 2).toFixed(2)}ms | analyzeTask total=${(analyzeOnly * 2).toFixed(2)}ms`
  );
  const replayTasks = genReplayTasks(mulberry32(0xb44b01), 2000);
  const replayM2 = bench(() => assignTasks({ catalog: catalog2, tasks: replayTasks }), 30);
  console.log(
    `ceiling replay-faithful (token-free) N=2000: assignTasks M=2 ${(replayM2 * 1e3).toFixed(1)}us per call (${(replayM2 * 2).toFixed(2)}ms per eval x2)`
  );
  const stress = genTasks(mulberry32(0xb44b01), 20000);
  const stressM2 = bench(() => assignTasks({ catalog: catalog2, tasks: stress }), 5);
  console.log(
    `ceiling 10x stress N=20000: assignTasks M=2 ${stressM2.toFixed(1)}ms per call (${(stressM2 * 2).toFixed(1)}ms per eval x2)`
  );
  const cli = genTasks(mulberry32(0xb44b01), 30);
  const cliM2 = bench(() => assignTasks({ catalog: catalog2, tasks: cli }), 2000);
  console.log(`ceiling CLI live face N=30: assignTasks M=2 ${(cliM2 * 1e3).toFixed(1)}us per call`);
  conclusions.push(
    `ceiling M=2 per-eval=${(totalM2 * 2).toFixed(1)}ms M=10 per-eval=${(totalM10 * 2).toFixed(1)}ms replay-faithful=${(replayM2 * 2).toFixed(1)}ms (holds-below-landing-line=${totalM10 * 2 < 30})`
  );
}

/* ============================================================
 * §0b Configured-state x command-class matrix (R7-I lesson; R8-B..R14-B
 * anchors). Full grid + CLI live face + per-child cascade probes.
 * ============================================================ */
{
  const catalog2 = catalogFromPrimary({ primaryModelId: "premium" });
  const catalogCfg = configuredCatalog();
  const catalog10 = tenModelCatalog();
  const replayTasks = genReplayTasks(mulberry32(0xb44b01), 2000);
  const cells: readonly [string, ModelRouterConfig, LearnedRoutingPolicy | undefined][] = [
    ["M=2 default, no learned", catalog2, undefined],
    ["M=2 default, learned", catalog2, learnedM2()],
    ["M=7 configured, no learned", catalogCfg, undefined],
    ["M=7 configured, learned", catalogCfg, configuredLearned()],
    ["M=10 stress, no learned", catalog10, undefined],
    ["M=10 stress, learned", catalog10, tenModelLearned()]
  ];
  for (const [label, catalog, learned] of cells) {
    const cost = bench(
      () => assignTasks({ catalog, tasks: replayTasks, ...(learned !== undefined ? { learned } : {}) }),
      30
    );
    console.log(
      `matrix replay N=2000 [${label}]: ${(cost * 1e3).toFixed(1)}us per call (${(cost * 2).toFixed(2)}ms per eval x2)`
    );
    if (label === "M=7 configured, learned") {
      conclusions.push(
        `configured-state M=7+learned replay per-eval=${(cost * 2).toFixed(1)}ms (still-below-landing-line=${cost * 2 < 30})`
      );
    }
    if (label === "M=10 stress, learned") {
      conclusions.push(
        `M=10+learned replay per-eval=${(cost * 2).toFixed(1)}ms (still-below-landing-line=${cost * 2 < 30})`
      );
    }
  }
  const cli = genTasks(mulberry32(0xb44b01), 30);
  const learned = configuredLearned();
  const cfgCli = bench(() => assignTasks({ catalog: catalogCfg, tasks: cli, learned }), 2000);
  console.log(`matrix CLI live face N=30: M=7 configured+learned ${(cfgCli * 1e3).toFixed(1)}us per call`);
  conclusions.push(`configured-state live-face=${(cfgCli * 1e3).toFixed(1)}us`);
  const assignments = assignTasks({ catalog: catalogCfg, tasks: cli, learned });
  const first = assignments[0]!;
  const planCost = bench(() => liveCascadePlanFromAssignment(first, { models: catalogCfg.models as never }), 40000);
  const plan = liveCascadePlanFromAssignment(first, { models: catalogCfg.models as never });
  const decideCost = bench(
    () =>
      decideLiveCascade({
        plan,
        previousModelId: first.decision.model,
        evidence: { source: "deterministic-check", kind: "FAIL" },
        failureClass: "model"
      }),
    40000
  );
  console.log(
    `matrix per-child cascade M=7: liveCascadePlanFromAssignment=${(planCost * 1e6).toFixed(0)}ns decideLiveCascade=${(decideCost * 1e6).toFixed(0)}ns`
  );
}

/* ============================================================
 * §0c Prior cells (R12-B §2c / R13-B §2c / R14-B §2c re-anchor). Production
 * callers passing prior: cli/main smartChildPlan (live face N<=30) and track
 * loop / primary-split (C small). adaptation/eval-routing does NOT pass prior
 * (re-verified by grep this round: eval-routing.ts:353-354 passes learned
 * only), so N=2000 stays a HYPOTHETICAL-scale documentation cell and the
 * S12-B-2 reopen condition stays untriggered.
 * ============================================================ */
{
  const catalog2 = catalogFromPrimary({ primaryModelId: "premium" });
  const catalogCfg = configuredCatalog();
  const catalog10 = tenModelCatalog();
  const prior = realisticPrior();
  const replayTasks = genReplayTasks(mulberry32(0xb44b01), 2000);
  const cli = genTasks(mulberry32(0xb44b01), 30);
  for (const [label, catalog] of [["M=2", catalog2], ["M=7", catalogCfg], ["M=10", catalog10]] as const) {
    const models = createModelRouter(catalog).config.models;
    const planNow = planAssignmentPolicy(models, catalog.models.map((m) => m.id));
    const picked = pickFromPublicPrior(prior, "edit", planNow.assignableModels);
    check(`prior cell live [${label}]: pickFromPublicPrior returns a pick for family=edit`, picked !== undefined, label);
  }
  const p2 = bench(() => assignTasks({ catalog: catalog2, tasks: replayTasks, prior }), 10);
  const p7 = bench(() => assignTasks({ catalog: catalogCfg, tasks: replayTasks, prior }), 10);
  const p10 = bench(() => assignTasks({ catalog: catalog10, tasks: replayTasks, prior }), 10);
  console.log(
    `matrix prior HYPOTHETICAL replay N=2000 [M=2 default, prior]: ${(p2 * 1e3).toFixed(1)}us per call (${(p2 * 2).toFixed(2)}ms per eval x2)`
  );
  console.log(
    `matrix prior HYPOTHETICAL replay N=2000 [M=7 configured, prior]: ${(p7 * 1e3).toFixed(1)}us per call (${(p7 * 2).toFixed(2)}ms per eval x2)`
  );
  console.log(
    `matrix prior HYPOTHETICAL replay N=2000 [M=10 stress, prior]: ${(p10 * 1e3).toFixed(1)}us per call (${(p10 * 2).toFixed(2)}ms per eval x2)`
  );
  const cli2 = bench(() => assignTasks({ catalog: catalog2, tasks: cli, prior }), 2000);
  const cli7 = bench(() => assignTasks({ catalog: catalogCfg, tasks: cli, prior, learned: configuredLearned() }), 2000);
  console.log(
    `matrix prior REAL-SCALE live face N=30: M=2+prior ${(cli2 * 1e3).toFixed(1)}us | M=7+prior+learned ${(cli7 * 1e3).toFixed(1)}us per call`
  );
  const models7 = createModelRouter(catalogCfg).config.models;
  const plan7 = planAssignmentPolicy(models7, catalogCfg.models.map((m) => m.id));
  const perPick = bench(() => {
    pickFromPublicPrior(prior, "edit", plan7.assignableModels);
  }, 20000);
  console.log(`prior unit cost: pickFromPublicPrior(edit, M=7 pool, ${prior.scores.length} rows) = ${(perPick * 1e6).toFixed(0)}ns per call`);
  conclusions.push(
    `prior-cells real-scale-live-face M=7+prior+learned=${(cli7 * 1e3).toFixed(1)}us hypothetical-N2000 M=7+prior per-eval=${(p7 * 2).toFixed(1)}ms per-pick=${(perPick * 1e6).toFixed(0)}ns`
  );
}

/* ============================================================
 * §0d NEW measurement cell A: A/A noise-floor calibration. Both duel arms are
 * the SAME unmodified production assignTasks on the SAME corpus, measured
 * with the exact protocol every candidate duel in R1-B..R14-B used
 * (delta = bench(armA, 8) - bench(armB, 8), 5 rounds per run; live face uses
 * reps=400 like the S13-B-1 live-face cells). True effect is exactly zero, so
 * the delta distribution directly measures the noise floor that a real
 * candidate's per-batch delta must clear to show a stable sign.
 * ============================================================ */
{
  const replayTasks = genReplayTasks(mulberry32(0xb44b01), 2000);
  const cliTasks = genTasks(mulberry32(0xb44b01), 30);
  const cells: readonly [string, ModelRouterConfig, LearnedRoutingPolicy][] = [
    ["M=2 default", catalogFromPrimary({ primaryModelId: "premium" }), learnedM2()],
    ["M=7 configured", configuredCatalog(), configuredLearned()],
    ["M=10 stress", tenModelCatalog(), tenModelLearned()]
  ];
  for (const [label, catalog, learned] of cells) {
    const run = (): void => {
      assignTasks({ catalog, tasks: replayTasks, learned });
    };
    const deltas: number[] = [];
    for (let round = 0; round < 5; round += 1) {
      const armA = bench(run, 8);
      const armB = bench(run, 8);
      deltas.push((armA - armB) * 1e3);
    }
    const positives = deltas.filter((d) => d > 0).length;
    console.log(
      `aa-noise-floor N=2000 [${label}, learned]: delta(A - A) us = [${deltas.map((d) => d.toFixed(1)).join(", ")}] | ${positives}/5 positive | max-abs=${Math.max(...deltas.map(Math.abs)).toFixed(1)}us`
    );
    if (label === "M=10 stress") {
      conclusions.push(
        `aa-noise-floor M=10 N=2000 max-abs-us=${Math.max(...deltas.map(Math.abs)).toFixed(1)} signs=${positives}/5-positive`
      );
    }
  }
  // Live-face variant: catalog/prior built once outside the timed arm, exactly
  // like the S13-B-1 live-face duel cells.
  const catalogCfg = configuredCatalog();
  const learnedCfg = configuredLearned();
  const priorCfg = realisticPrior();
  const runCliFixed = (): void => {
    assignTasks({ catalog: catalogCfg, tasks: cliTasks, learned: learnedCfg, prior: priorCfg });
  };
  const cliDeltas: number[] = [];
  for (let round = 0; round < 5; round += 1) {
    const armA = bench(runCliFixed, 400);
    const armB = bench(runCliFixed, 400);
    cliDeltas.push((armA - armB) * 1e3);
  }
  console.log(
    `aa-noise-floor live-face N=30 [M=7+learned+prior]: delta(A - A) us = [${cliDeltas.map((d) => d.toFixed(2)).join(", ")}] | max-abs=${Math.max(...cliDeltas.map(Math.abs)).toFixed(2)}us`
  );
  conclusions.push(
    `aa-noise-floor live-face max-abs-us=${Math.max(...cliDeltas.map(Math.abs)).toFixed(2)}`
  );
}

console.log(`\nCONCLUSIONS: ${conclusions.join(" | ")}`);
if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("ALL CHECKS PASSED (measurement-only harness; corpora seeded + deterministic)");
```

## 附录 B：首格次序探针（§2e 第 4 条，完整）

```ts
/**
 * R15-B order-disambiguation probe: is the Node 24 first-cell reading
 * (with-token M=2 ceiling) a corpus-shape effect or a measurement-position
 * (tier-up) artifact? Measure the SAME with-token M=2 cell twice: once as the
 * first benchmark of the process (cold position, like the main harness) and
 * once after a full token-free warm-up (steady position).
 */
import { performance } from "node:perf_hooks";
import { assignTasks, type AssignableTask } from "/workspace/src/routing/assign.js";
import { catalogFromPrimary } from "/workspace/src/routing/primary-catalog.js";
import type { AgentRole } from "/workspace/src/domain/roles.js";

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
const ROLES: readonly AgentRole[] = [
  "worker", "scout", "planner", "implementer", "reviewer", "tester", "debugger"
];
const OBJECTIVE_TEMPLATES = [
  "Survey the payment module", "Plan the checkout migration", "Run the unit tests",
  "Deploy payment credentials to production", "Implement retry logic for the ledger sync",
  "Review the audit log formatting nits", "Refactor and rename the tracking roller",
  "Fix the flaky spec for gate transitions", "Research and compare vector store options",
  "Investigate why the drop table migration failed in prod", "验证路由细节并补充测试", ""
];
const FRAGMENTS = [
  "deploy", "production", "credentials", "review", "tests", "coverage", "plan",
  "design", "survey", "research", "refactor", "implement", "add ", "fix ",
  "migrate", "verify", "\n", "the module", "碎片", " "
];
function genObjective(rng: () => number): string {
  if (rng() < 0.5) return pick(rng, OBJECTIVE_TEMPLATES);
  let text = "";
  const parts = 1 + Math.floor(rng() * 8);
  for (let i = 0; i < parts; i += 1) text += pick(rng, FRAGMENTS) + " ";
  return text;
}
function genTasks(rng: () => number, n: number): AssignableTask[] {
  return Array.from({ length: n }, (_, i) => ({
    taskId: `tsk_${i.toString().padStart(6, "0")}` as AssignableTask["taskId"],
    role: pick(rng, ROLES),
    objective: genObjective(rng),
    ...(rng() < 0.1 ? { contractRisk: rng() < 0.5 } : {}),
    ...(rng() < 0.3 ? { contextTokens: Math.floor(rng() * 100000) } : {}),
    ...(rng() < 0.3 ? { outputTokens: Math.floor(rng() * 4000) } : {})
  }));
}
function genReplayTasks(rng: () => number, n: number): AssignableTask[] {
  return Array.from({ length: n }, (_, i) => ({
    taskId: `tsk_${i.toString().padStart(6, "0")}` as AssignableTask["taskId"],
    role: pick(rng, ROLES),
    objective: genObjective(rng)
  }));
}

console.log(`engine node=${process.version} v8=${process.versions.v8}`);
const catalog2 = catalogFromPrimary({ primaryModelId: "premium" });
const tasks = genTasks(mulberry32(0xb44b01), 2000);
const replayTasks = genReplayTasks(mulberry32(0xb44b01), 2000);
// Cold position: with-token M=2 is the first benchmark of the process,
// exactly like the main harness §0.
const cold = bench(() => assignTasks({ catalog: catalog2, tasks }), 30);
console.log(`order-probe with-token M=2 COLD-POSITION: ${(cold * 1e3).toFixed(1)}us per call (${(cold * 2).toFixed(2)}ms per eval x2)`);
// Steady position: full token-free warm-up first, then the SAME cell again.
for (let i = 0; i < 10; i += 1) assignTasks({ catalog: catalog2, tasks: replayTasks });
const steady = bench(() => assignTasks({ catalog: catalog2, tasks }), 30);
console.log(`order-probe with-token M=2 STEADY-POSITION: ${(steady * 1e3).toFixed(1)}us per call (${(steady * 2).toFixed(2)}ms per eval x2)`);
const tokenFree = bench(() => assignTasks({ catalog: catalog2, tasks: replayTasks }), 30);
console.log(`order-probe token-free M=2 STEADY-POSITION: ${(tokenFree * 1e3).toFixed(1)}us per call (${(tokenFree * 2).toFixed(2)}ms per eval x2)`);
```
