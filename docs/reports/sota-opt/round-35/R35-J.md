MODEL_SLUG=claude-fable-5-thinking-xhigh

# R35-J：persist / 数据面切片第三十五遍复查报告（Round 35）

**战役:** 全库持久 SOTA 优化 Round 35 / R35-J（R1-J…R34-J 同区三十四遍之后的第三十五遍）
**基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `9439c77`（派单 tip，R35-G 空枚举摄取 + 本 R35-J 派单）。测量窗口（重锚三遍、J1 sim、聚焦套件、`pnpm gate`）全部在 `9439c77` 工作树完成；提交阶段 fetch 先后检测到 exclusive **两次纯 docs 快进**：`9439c77` → `47f759b`（R35-H 摄取（空枚举）——`--stat` 恰 3 个 docs 文件（PROGRESS.md / round-35 PLAN.md / R35-H.md，190 插入 11 删除），提交前 fetch 所见）→ `01b2918`（R35-I 摄取（空枚举）——`--stat` 同样恰 3 个 docs 文件（PROGRESS / PLAN / R35-I.md，226 插入 11 删除），提交后推送前 fetch 所见），两笔对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接 diff 各 **0 行**；按令逐次执行**纯快进 rebase**（均零冲突）并各重验冻结（29 文件 vs `fb41417` 与最强冻结 `89f0455..HEAD -- src/` 在两个新 tip 均 **0 行**）——三批测量结果原样携带（三 tip `src/`+`test/` 位级同一）。已通读快进带入的 **R35-H** 与 **R35-I** 并折入非移植清单（§0.3）；派单要求预读的七份姊妹报告 **R35-A…R35-G**（七份均诚实空枚举）已全文通读并折入。十切片 round-35 至此 J 为最后收口者；工作期间按令未等待、未触碰任何姊妹分支。
**分支:** `cursor/r35-j-persist-thirty-fifth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（R28-J…R34-J 同引擎约定，带可比；本 VM 系统 PATH 暴露 Node v22.14.0——开工即 PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 并以 `node --version` + `node -p process.versions.v8` 双复核，正批全程 v22.22.2，**v22.14.0 上零批次**；`pnpm install --frozen-lockfile` 干净完成，锁文件未改）。

## 结论

**诚实的空枚举：第三十五组没有可立 ID 的新角度，不铸任何 S35-J-\* ID，不铸新 face 名，不取 ALLCAPS 绰号，不写入 EXCLUSIONS 表行，生产代码零改动，切片维持关闭（MORE_OPTIMA=no）。** 本轮按派单纪律收口 J 区：从零重读 29 个切片文件（3655 行，§0.1）、在本 VM 以 R12-J 附录 A 同源 harness 重锚配置态 I/O 地板矩阵九格 ×3 遍（§1）、重跑 J1 等价 sim 全绿（**2468 项逐位检查**，§1.2）、对每个「看起来新」的残余位点做换名拒绝检查（§2.2）。结果是**零新站点级换名记录**——本轮从零重推出的全部候选角度经查证均为既有 ID / 既有记录 / 姊妹属地的复命中（§2.2 十二项归属：R34-J 四项记录按令不重铸原形在位；`track/loop.ts` `assignments.find` = **S1-J-5 排除行本体**（本轮逐字复核 L106 原样）；`context/packet.ts` L122 首个 `omissions.sort` = **S1-J-7 排除行本体**（R1-J S17 原文比较器全字段覆盖论证维持）；waitForClarification 双 `readAll` = **S2-J-11**；`pickCanonical` 单值组 spread+sort = **S7-J-1**；`stripForbidden` 顺序 split/join 与 needles 每调用重排序 = **S3-J-1 / S9-J-1..4**（R3-J 已把 needle 排序显式计价为 I/O 支配）；context 跨模块 token 估算孪生 = **R3-J / X1-1 公开形状禁区**（S5-J-2 注）；episode append 队列 promise 链 = **R7-J 顺序契约裁决 + R10-J §2.3-4 / R11-J S0b 地板支配**；`reduceEpisodeEvents` fold/`markFailed` 粘性拒绝面 = **R17-J ln 域 + R18-J 空 fold 定价**（233.8–234.9 ns）；jsonl fsync open/sync/close = **fsync 不变量本体 + 地板格**；`queryPacketGrounding` = test-only 面（本轮 re-grep 再证 `src/` 仅 L498 定义位点、零生产调用方）；file-lock `retryMs ?? 10` = D/E 已计价 + 本轮 rg 直证 `withExclusiveFileLock` 调用方全在 pi-adapter/cli/learning/adaptation 四个切片外文件、J 属地零生产调用方），无一达到「从未在任何 J 报告点名」的新站点门槛——零新记录与 R35-B / R35-F / R35-H / R35-I 同款（R33-E「零是更强收口」判读同向适用）。静态哨兵仅作卫生应用（chmod/umask/readdir/xattr/link/symlink/chown 八目录普查 **0 命中**，§2.4），不铸门审计 face。空枚举与 R33-A…R34-J 及 R35-A…R35-I 同为战役允许的诚实结局。

关键前提事实：切片 29 文件自 J1 落地（`fb41417`）**逐字节零变化**——派发 tip `9439c77`、两次快进 rebase 后 tip `47f759b` / `01b2918` 与提交前核对 `git diff fb41417 -- <29 文件>` 均 **0 行**；`git log fb41417..HEAD -- <切片八目录>` **零提交**；最强冻结 `git diff 89f0455..HEAD -- src/` = **0 行**（R10 以来 docs-only）⇒ 生产调用图对本切片**可证不变（最强形式，第二十七连）**。`wc -l` 合计 **3655**（29 文件逐一实测，§0.1）。重锚九格与历史带的关系（§1.1）：saveToDisk 50/500 观察档、cascade NO-MATCH、plan 计数器/randomUUID 五格带重叠或全含；jsonl fsync=false 慢侧贴沿（快缘 69.7 对 R34-J 慢缘 69.4）、context index 快侧 −6%、cascade MATCH 慢侧交叠——**九格全部为冻结 0 行下的宿主偏移窗口**（按派单原文：host-bias windows with freeze 0 lines are NOT new findings）；其中 jsonl fsync=true 格 pass-2 带 290.0–326.3 与 R34-J 带 227.1–338.2 重叠、pass-1/pass-3 出现 399.5–801.5 的 fsync 设备侧慢窗（共享 VM flush 竞争特征，同批同格 pass-2 即回带为窗口性最直接证据）。**I/O 支配判据第三十五次成立**（persist 地板数百 µs vs CPU 格 1–3 µs）。J1 落地代码本轮 `scripts/r1j-equivalence-sim.ts` 重跑全绿（**2468 项逐位检查，reference 10400.5 ms → current 3.6 ms 邻域实测 3.5 ms = 2948.5×**——比率高侧系 O(N²) reference 在本宿主跑慢（10400.5 对 R34-J 9912.2 ms）而 current 稳定 3.5–3.6 ms，与派单记载 2760.9×、R32-J 2713.8×、R31-J 2836.9× 同为宿主偏移带内；J1 锁不重铸，fold 形状锁定原样）。聚焦切片套件 **91/91**（8 目录 4 suites，`9439c77` 树）；`pnpm gate` **exit 0**（tests **1169** / suites 78 / pass **1168** / fail **0** / skipped **1**，与 R14-E…R35-I 历史计数逐字相同）。硬性不变量逐条零触碰（§3）。仓库变更仅本报告一个文件；重锚 harness 按纪律不入库（与 R12-J 附录 A 归档件同源，除头注释与临时目录前缀 `r12j-`→`r35j-` 外逐字同源；运行后移出仓库树，附录 A 存原始输出）。不开 PR。

## 0. 冻结、快进处置与划界

### 0.1 冻结与快进

- `git diff fb41417 -- <29 文件>` = **0 行**（派单 tip `9439c77`、两次快进 rebase 后 `47f759b` / `01b2918`、提交前多次核对一致）。
- 最强冻结：`git diff 89f0455..HEAD -- src/` = **0 行**（R10 以来 `src/` docs-only 不变；两笔快进段 `9439c77..47f759b` 与 `47f759b..01b2918` 对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接 diff 各 **0 行**）。
- `git log fb41417..HEAD -- src/cluster src/privacy src/preferences src/episode src/persist src/track src/context src/feedback` = **零提交**。
- `wc -l`（本轮逐文件实测）：cluster **345**（host 200 / mailbox 91 / spawn 54）+ privacy **416**（deletion 126 / record-classes 261 / state-layout 29）+ preferences **707**（export 65 / loop-eval 180 / materialize 36 / precedence 40 / service 65 / store 288 / types 33）+ episode **396**（closure 49 / events 44 / manager 207 / replay 49 / store 47）+ persist **102**（file-lock 49 / jsonl 53）+ track **518**（clarify 65 / loop 279 / plan 122 / primary-split 52）+ context **941**（index 418 / packet 523）+ feedback **230**（redaction 121 / store 85 / types 24）= **3655**，文件数恰 **29**（3+3+7+5+2+4+2+3 逐目录确认）。
- 快进处置：测量批全部在 `9439c77` 树完成；提交前 fetch 见一笔 `9439c77..47f759b`（R35-H 摄取，`--stat` 仅 PROGRESS.md / round-35 PLAN.md / R35-H.md 三 docs 文件，190 插入 11 删除），提交后推送前 fetch 又见一笔 `47f759b..01b2918`（R35-I 摄取，`--stat` 同样恰三 docs 文件，226 插入 11 删除）——两笔各对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接 diff **0 行**，逐次纯快进 rebase 零冲突，冻结各重验 0 行 ⇒ 测量与门禁结果原样携带（三 tip `src/`+`test/` 位级同一），无需重测；R35-H / R35-I 实读折入 §0.3。

### 0.2 生产调用面复核（本 tip rg 实测）

- `withExclusiveFileLock` 调用方恰 4 个切片外文件：`pi-adapter/file-credential-store.ts`（:43/:54，I 属地）、`cli/episode.ts`（:90，I 属地）、`learning/bandit-store.ts`（:44，E 属地）、`adaptation/promotion.ts`（:247，D 属地）——J 属地仅定义位点，**零 J 侧生产调用方**（派单不变量原文维持：`retryMs ?? 10` 已由 D/E 计价，J 无可铸对象）。
- `queryPacketGrounding` = `src/` 内仅 `context/packet.ts:498` 定义位点，零生产调用方（test-only 面第三十五次确认）。
- `loadLearnedRouting` 生产调用位点恰 3 处不变（cli/main.ts:708、run/flowchart-run.ts:712、track/loop.ts:88）——**S8-E-1 零去重**维持（E 属地，J 侧 loop.ts:88 为调用方侧坐标，零触碰）。
- `track/loop.ts:80` `applyPrecedence(contract, "user-first")` = R31-H **D1 停驻门**的切片外规则字面量普查坐标（H 属地，R35-H §2.4 本轮直证过）——J 侧零触碰、不代跑。

### 0.3 姊妹面划界与非移植清单（R35-A…R35-G 派单折入 + 快进带入 R35-H / R35-I）

派单非移植项全数持有：命名轴 FITQ NAMESHAPE SCALEX XPROC YMIX SCHEDWIN KFAN NVG SEEDX TERMCLASS SHAPEK ORDX CKMIX CALLB OCCX JB RUNLIM VALCLASS CG AGEX COTARG PRICEX RTL DELINV PROBX RVL SIDEC PWL ZREP PAIRX CSPELL REPRX **LINKTOP BINDX MODEX**；R32-J 六残余坐标（inode 时间戳 / extent 布局 / owner 字 / ACL-xattr / dirent 序 / 线程池宽度——已三方败）；R30-J 链接拓扑（LINKTOP）与 R31-J inode 模式拓扑（MODEX）耐久工件面；R32-A 哨兵方法 / R32-B 闭合稳定性方法 / R33-A A-ledger 卫生仅作卫生应用不铸「我又查了一遍」；R33-A…R34-J 二十切片空枚举。round-35 姊妹八份逐项划界：

- **R35-A**（tracking 空枚举）：三项站点级换名记录全部住在 tracking 物理对象内（字面量享元族 / asString 首字符卫镜像 / prescore dip 乘后判）——J 零共享物理对象，无可移植轴。
- **R35-B**（live 路由空枚举）：**零新站点级换名记录**；S12-B-2 / S13-B-1 停驻维持——零共享，无可移植轴；其「零是更强收口」判读本轮 J 侧同款适用。
- **R35-C**（离线路由空枚举）：一项换名检查（posterior `estimateForKey` 双 `weightedSampleSize` CSE）为 C 区物理对象——零共享；「同参纯函数双求值 CSE」族 J 侧无新对应站点（本轮重读未浮出同型未点名位点）。
- **R35-D**（adaptation 空枚举）：四项记录全 `src/adaptation/` 物理对象；eval 地板重锚 3.54–3.93 ms 为 D 属地价签——零共享，无可移植轴（file-lock 调用方 promotion.ts:247 属 D 侧计价面，§0.2 互证）。
- **R35-E**（learning 空枚举）：一项记录（`runAutoAdaptLoop` L88 bandit 前置门探针）为 E 属地；S8-E-1 三站点普查含 J 调用方侧文件但均 E 属地站点——零共享 J 物理对象，无可移植轴（bandit-store.ts:44 file-lock 调用方同理 E 侧）。
- **R35-F**（experiments 空枚举）：**零新站点级换名记录**——15 文件全在 `src/experiments/`，零共享，无可移植轴。
- **R35-G**（run/supervisor/graph/domain 空枚举）：两项新记录均 G 属地三重拒列（flowchart-run 命令入口孪生 Map 物化前奏 / `familyForFlowNode` 内形辅助）；I/O 地板 87.0–100.0 ms/run 支配关系、14 格矩阵摘要第二十六轮同值、digest `06cbcf92c098c8f0` 第二十六次同一均为 **G-slice 对象——J 没有 digest 可铸**；G 侧 run 消费面（flowchart-run.ts 等）是 J 的调用方侧非 J 属地——零共享本体，无可移植轴。
- **快进带入 R35-H**（evaluation/requirement/review/rubric 空枚举）：**零新站点级换名记录**（八候选全为 H 既有 ID / plan-ban 轴 / 停驻门复命中）；三腿锚点 6/6 全带；其 §0.2 生产调用面普查含 J 侧三个调用方文件（clarify.ts:4 `extractHeuristicContract` / loop.ts:38 `applyPrecedence` / plan.ts:5 `shouldScout`）均为 **H 导出的 J 调用方侧坐标**——H 属地本体、J 零可铸对象；其 D1 门坐标 track/loop.ts:80 划界见 §0.2。与 J **零共享物理对象**，无可移植轴。
- **快进带入 R35-I**（cli/pi-adapter/config/telemetry 空枚举）：**零新站点级换名记录**（素读残余候选恰一项 `assertKnownCatalogId` 惰性化 = **S7-I-4 排除行本体复命中**即止）；custom−builtin 六类带与世系带族全部相交（宿主窗动裁定回带）；r4i/r5i/r7i 仿真 68/119/80 checks 全 0 失败；S8-I-1 两臂文件级封锁复验维持、npm `.bin` symlink no-op 按令未「修复」——25 文件全在 cli/pi-adapter/config/telemetry，与 J **零共享物理对象**（其 S8-E-1 哨兵普查含 track/loop.ts:88 = J 文件内的 E 属地调用方侧坐标，与 §0.2 互证；file-lock 调用方 file-credential-store.ts / cli/episode.ts 为 I 侧计价面，§0.2 互证），无可移植轴。

## 1. 重锚（历史仪器，I/O 地板矩阵 ×3 遍 + J1 sim）

### 1.1 I/O 地板矩阵（R12-J 附录 A 同源 harness，三遍 × 各 3 次运行，nvm v22.22.2）

| 格 | 本轮三遍并集 | R34-J 记载带 | 裁决 |
|---|---|---|---|
| saveToDisk 50 obs | 146.3–397.2 µs/写（稳态 146.3–253.7；319.2/397.2 系 pass-1/pass-3 首块暖窗采样） | 141.9–234.4（稳态；暖窗并集至 252.7） | 带重叠（快缘 146.3 带内；稳态慢缘 253.7 对暖窗并集 252.7 逐位邻域） |
| saveToDisk 500 obs | 438.7–509.9 µs/写 | 429.1–533.4 | **带内全含** |
| jsonl append fsync=false | 69.7–76.9 µs | 61.2–69.4 | 慢侧贴沿宿主窗（快缘 69.7 对历史慢缘 69.4 触沿；九采样齐移 +10% 为宿主刻度特征，冻结 0 行 ⇒ 非发现） |
| jsonl append fsync=true | pass-2 290.0–326.3；pass-1/3 399.5–801.5 | 227.1–338.2 | pass-2 **带重叠**；pass-1/3 系 fsync 设备侧慢窗（共享 VM flush 竞争；同批同格 pass-2 回带直证窗口性，非结构迁移） |
| 级联 MATCH（N=200，20 hits） | 739.9–897.6 µs | 636.0–857.7（并集） | 交叠（739.9–857.7 共域；慢缘 +4.7% 单侧宿主采样） |
| 级联 NO-MATCH | 285.5–344.0 µs | 260.3–335.6 | 带重叠 |
| context index（F=40 + 30 syms） | 37.7–38.4 µs | 40.4–41.9 | 快侧 −6% 宿主窗（冻结 0 行，方向与 fsync 慢窗互补，同 VM 双向偏移并存 = 窗口采样特征） |
| track plan 计数器 | 0.97–1.08 µs | 1.01–1.11 | 带重叠 |
| track plan randomUUID | 2.52–2.74 µs | 2.52–2.70 | 带重叠（快缘逐位同值） |

判读：九格无一为结构迁移——同批冻结差分 0 行 + 双向（fsync 慢侧 / context 快侧）偏移并存 + fsync 格 pass-2 回带，为「窗口性、非结构」的三重证据；按派单原文不作新发现申报。**I/O 支配判据第三十五次成立**：persist 地板（saveToDisk 146–534 µs、jsonl 61–338 µs 历史带、级联 260–898 µs）比切片 CPU 格（plan 1.0–2.7 µs、context 38–42 µs）高 1–2 个数量级，任何纯 CPU 换名的收益上界被同路径 I/O 地板吞没。

### 1.2 J1 等价 sim（`scripts/r1j-equivalence-sim.ts`，不重铸）

场景 1 确定性边角 **64 checks / 16 cases** + 场景 2 随机化 **2400 checks / 600 cases** + perf fixture（N=6000 多主体 ~25% tombstoned）= **ALL EQUIVALENCE CHECKS PASSED（2468 bitwise checks）**。reference **10400.5 ms** → current **3.5 ms** = **2948.5×**（reference 逐元素访问 92,590,419 次不变）；对照派单 2760.9×、R32-J 2713.8×、R31-J 2836.9×——比率高侧系 reference 腿在本宿主跑慢（current 腿 3.5–3.6 ms 跨轮稳定），宿主偏移带内。J1 fold 形状锁定原样，不重铸。

## 2. 第三十五遍枚举（诚实结果：空；零新站点级换名记录）

### 2.1 逐文件重读与裁决（29 文件从零重读，零未点名站点）

| 文件（行数） | 站点归属（全数指到既有裁决） | 新候选 |
|---|---|---|
| `cluster/host.ts`（200） | 广播扇出 `body.trim()` + `targets.slice(1)` 提升 = **R34-J 记录 3** 原形在位不重铸（纯内存 ns、零持久坐标） | 无 |
| `cluster/mailbox.ts`（91） | mailbox n 三连 = **S5-J-4**（6.2–6.6 ns 原价签）；冗余 `byRole.set` = R15-J 拒列原文 | 无 |
| `cluster/spawn.ts`（54） | 组装命令类；公开签名 X0-4 禁区 | 无 |
| `privacy/deletion.ts`（126） | 级联读序 `.catch(() => [])` 错误面 = R5-J §3 契约；`[...tombstones].sort()`/`cascaded.sort()` = R10-J µs 拒列；双文件 stat/rm 串行 = **S4-J-3**（并行化竞态拒）；O(记录数) 整读改写 = 删除正确性契约本体（**DELINV** 域，§2.3） | 无 |
| `privacy/state-layout.ts`（29） | 路径构造命令类 | 无 |
| `privacy/record-classes.ts`（261） | `durableRecordClassById` 零生产调用方（R12-J §1.2 起逐轮复证）；表驻留命令类 | 无 |
| `preferences/loop-eval.ts`（180） | 两次 `tombstones.has` = **S2-J-1** 原样（L153/L159） | 无 |
| `preferences/store.ts`（288） | `rebuildViews` 条件 set = **S7-J-4** / `nowIso` 提升 = **S3-J-5**；`applyObservation` 全量 filter = **S1-J-2 / S2-J-6**（saveToDisk 支配）；每写 `mkdirSync` 自愈 = **S7-J-6 保留**；地板本轮 146–510 µs 直测（§1.1） | 无 |
| `preferences/export.ts`（65） | scopes includes / listObservations 拷贝 = **S3-J-6 / S5-J-1**；`exportForDataset` 零生产调用方 | 无 |
| `preferences/precedence.ts`（40）/ `materialize.ts`（36）/ `service.ts`（65）/ `types.ts`（33） | 既往轮命令类裁决原样；types 纯类型 | 无 |
| `episode/manager.ts`（207） | 事件构造恒写可选键 / `runIds.includes` = R11-J §2.3 own-property 面 + **S1-J-3**；reduce fold + `markFailed` 粘性拒绝面 = **R17-J ln 域 + R18-J 空 fold 定价**（233.8–234.9 ns）——本轮候选复命中 9（§2.2） | 无 |
| `episode/replay.ts`（49） | 两导出零生产调用方（R12-J §1.2 起复证）；单遍 parse 支配 = S3-J-4 域 | 无 |
| `episode/events.ts`（44） | 纯类型 | 无 |
| `episode/store.ts`（47） | append 队列 promise 链 = **R7-J 顺序契约** + R10-J §2.3-4 / R11-J **S0b 地板支配**——本轮候选复命中 8（§2.2） | 无 |
| `episode/closure.ts`（49) | L32 legacy-match 模板串 = **R34-J 记录 4** 原形在位不重铸（once-per-close + legacy 兼容契约面）；acceptanceEvidence 结构扫描命令类 | 无 |
| `persist/file-lock.ts`（49） | `retryMs ?? 10` = D/E 已计价；J 零生产调用方本轮 rg 直证（§0.2）——整面等价重写生产收益恒 0 | 无 |
| `persist/jsonl.ts`（53） | fsync 腿 open/sync/close = **fsync 不变量本体**（不可去）+ 地板格直测（§1.1）；`readJsonlObjects` split 单遍 = S2-J 读地板族 | 无 |
| `track/primary-split.ts`（52） | 纯组装转发；公开形状 X0-4 | 无 |
| `track/plan.ts`（122） | L41 `/\b(tests?\|coverage)\b/i` + L112 `/\btests?\b/i` 行内正则 = **R34-J 记录 1** 原形在位不重铸（once-per-plan 1.0–2.7 µs 整调用直测内的 ns 份额） | 无 |
| `track/clarify.ts`（65） | H 消费面（`extractHeuristicContract` 调用方侧，H 属地）；spread 合并命令类 | 无 |
| `track/loop.ts`（279） | `assignments.find` in `planned.map`（L106）= **S1-J-5 排除行本体**（本轮逐字复核，C ≤ ~6）；waitForClarification 双 `readAll`（L261/L266）= **S2-J-11**；catalog∥learned = **S4-J-2**；`resolvedQuestionIds`/`catalogIds.includes` = X3-1 域；L80 `"user-first"` = R31-H D1 门坐标（H 属地零触碰，§0.2）；L88 = S8-E-1 站点（E 属地零触碰） | 无 |
| `context/index.ts`（418） | `estimateCodeMapTokens` 跨模块孪生 = **R3-J / X1-1 公开形状禁区**（S5-J-2 注原文）；`isUnderGenerated` O(d×g) 已计价（context 地板 37.7–38.4 µs 直测整含）；排序/去重命令类 | 无 |
| `context/packet.ts`（523） | L122 首个 `omissions.sort` = **S1-J-7 排除行本体**（本轮逐字复核在位；R1-J S17 比较器全字段覆盖 ⇒ 双排结构等价论证维持）；`pickCanonical` 单值组 spread+sort = **S7-J-1**；`estimateCodeMapEntry` 孪生 = 同 R3-J/X1-1；`queryPacketGrounding` = test-only 面（L498 唯一位点，§0.2） | 无 |
| `feedback/types.ts`（24） | 纯类型 | 无 |
| `feedback/store.ts`（85） | `readFeedback` 串行两读 = **R34-J 记录 2** 原形在位不重铸（**S5-J-3 / S6-J-1** 删除面串行形态族）；`isFeedbackRecord` 结构卫命令类 | 无 |
| `feedback/redaction.ts`（121） | `stripForbidden` 顺序 split/join = **S3-J-1 / S9-J-1..4**；needles 每调用长度降序重排序 = R3-J 已显式计价（n=4 小常数，I/O 支配）；`copyFeedback` 显式字段拷贝 = 既往裁决 | 无 |

### 2.2 换名拒绝检查：本轮零新站点级记录

从零重推（假装排除表不存在重读 3655 行）枚举出的候选角度经对表逐一查证，**全部为已裁决站点 / 已点名属地的复命中，无一达到「从未在任何 J 报告点名」的新站点门槛**——本轮不新增站点级换名记录（对照 R34-J 四项均为其轮首次点名站点；J 区经三十四遍逐格归属后已无此类站点，零是更强的收口——R35-B / R35-F / R35-H / R35-I 同款判读）：

1. **不重铸：R34-J 四项记录**（`track/plan.ts` 行内正则；`readFeedback` 串行双读；`host.ts` 广播 trim/slice；`closure.ts` legacy-match 模板）——裁决逐字维持，站点原形在位读码复核（§2.1 对应行），按派单令不以任何新名重提。
2. **复命中：`track/loop.ts` `assignments.find` 换 Map**（L106，planned.map 内 O(C²)）= **S1-J-5 排除行本体**（R1-J S14 原文：C ≤ ~6 + live 面 X3-1 同理）。本轮逐字复核原样在位。复命中即止。
3. **复命中：`context/packet.ts` L122 首个 `omissions.sort` 冗余移除** = **S1-J-7 排除行本体**（R1-J S17 原文：结构等价但 k 小、常数噪声 X3-2 同理；R2-A 曾引为同类先例）。复命中即止。
4. **复命中：waitForClarification 双 `eventStore.readAll()`**（L261/L266）= **S2-J-11**（R5-J 归属原文）。once-per-clarify 且 jsonl 读地板支配。复命中即止。
5. **复命中：`pickCanonical` 单值组 spread+sort** = **S7-J-1**。复命中即止。
6. **复命中：`stripForbidden` 顺序 split/join + needles 每调用重排序** = **S3-J-1 / S9-J-1..4**（R3-J 附录已把 needle 排序与顺序剥离显式计价：n=4 小常数、appendFeedback 路径 I/O 支配）。复命中即止。
7. **复命中：context 跨模块 token 估算同式去重**（index.ts:265–268 `estimateCodeMapTokens` / packet.ts:198–201 `estimateCodeMapEntry` 孪生）= **R3-J / X1-1 公开形状禁区**（S5-J-2 注原文：合并触公开导出面）。复命中即止。
8. **复命中：`episode/store.ts` append 队列 promise 链每次 `then`/`catch` 分配**——队列本体 = **R7-J「append 队列为顺序契约」裁决**；分配份额 ns 级，被 S0b jsonl 追加地板（本轮 61–338 µs 历史带直测）吞没两个数量级（R10-J §2.3-4 / R11-J 同格）。顺序契约 + 地板支配双重命中即止。
9. **复命中：`reduceEpisodeEvents` 每调用 `markFailed` 闭包分配与逐事件 spread**——粘性拒绝面 = **R17-J ln 域**（重复/悬垂事件拒绝终点集）；fold 本体 = **R18-J 空 fold 定价**（233.8–234.9 ns）+ S1-J-3/R11-J own-property 面。episode 日志事件数个位数规模，once-per-replay ns 级。复命中即止。
10. **复命中：`persist/jsonl.ts` fsync 腿 open("a")+sync+close 每追加重开句柄**——句柄常驻化触 **fsync 不变量**（硬不变量：No fsync removal；句柄生命周期改变 = fsync 语义面结构触碰）且 fsync=true 地板 227–338 µs 历史带为该格本体价签。不变量面 + 地板格双重命中即止。
11. **复命中：`queryPacketGrounding` 评分环** = test-only 面（本轮 re-grep 再证零生产调用方，§0.2）。复命中即止。
12. **复命中：file-lock 整面**（`retryMs ?? 10`、owner-token 回读）= D/E 已计价 + J 属地零生产调用方（§0.2 rg 直证四调用方全在切片外）。生产收益恒 0，复命中即止。

### 2.3 放大因子收口（引用 R33-J §2.2 方法作卫生核对，不铸面）

切片每个持久 I/O 位点的每命令倍率均为 O(1) 或 O(记录数)：preferences 每观察 1 次全量 stringify+write（地板 146–534 µs）、episode/feedback 每事件 1 次 jsonl 追加（61–338 µs 历史带）、删除级联每次 2 读 + 命中时 1 改写（260–898 µs）。与实测地板相乘，真实规模下每命令持久成本 **< 1 ms**——距 tens-of-ms 落地线仍 **1.5–2 个数量级**；唯一 O(记录数) 整读改写（删除级联）是删除正确性契约本体（**DELINV** 域，不可折叠）。核对非发现，不铸面。

### 2.4 静态哨兵与在案条件核对（卫生应用，不铸门审计 face）

- **静态划界哨兵**：`chmod|umask|readdir|xattr|linkSync|\.link\(|symlink|lchown|chown` 对切片八目录一次 rg = **0 命中**（空匹配直证）——**MODEX / LINKTOP 在 J 切片代码内无物理对象**，耐久工件面维持 R30-J/R31-J plan-ban 划界，不铸「我又查了门」。
- **R32-J 六残余坐标**（inode 时间戳 / extent 布局 / owner 字 / ACL-xattr / dirent 序 / 线程池宽度）：已三方败，本轮零重提零代跑。
- **S8-E-1 / D1 门坐标**：J 调用方侧位点在位核对（§0.2），属地在 E/H，零触碰。
- **R32-A 哨兵方法 / R32-B 闭合稳定性方法 / R33-A ledger 卫生**：作为方法应用于上两条核对，不铸「我检查过」新面。

### 2.5 枚举收口

零未点名站点（§2.1 29/29 文件全归属）；零新站点级换名记录（§2.2 十二项全复命中，R34-J 四项记录维持不重铸）；放大因子收口 < 1 ms/命令（§2.3）；静态哨兵 0 命中（§2.4）；重锚九格全为冻结 0 行下的宿主窗（§1.1）；J1 sim 2468 全绿（§1.2）。**没有可立 ID 的新角度。不铸 S35-J-\*，不铸新 face 名，不取绰号，不写 EXCLUSIONS 行。**

## 3. 测试与验证

- 聚焦切片套件（`test/unit/{cluster,privacy,preferences,episode,persist,track,context,feedback}`，node:test via tsx）：**tests 91 / suites 4 / pass 91 / fail 0**（`9439c77` 树，nvm v22.22.2——两笔快进段 `src/`+`test/` 逐字节相同，结果原样携带至 `01b2918`）。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0**；**tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1**，与 R14-E…R35-I 历史计数逐字相同。
- J1 sim：**2468/2468 逐位通过**（§1.2）。
- 硬性不变量逐条复核：分析不改在途 run；tracking 无指挥权；H/score 不写路由 PASS/FAIL；live = R0 等价（R1 未接线）；双 LCB / 双归因在位；promotion proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；CAS / 凭证 / 数据面合同 / 公开签名零触碰；**无 fsync / 完整性再哈希去除**（§2.2 复命中 10 拒列理由维持）；fail-open 家族保持关闭；S5-H-1 / S7-F-1≠S6-F-5 / S8-I-1 / S9-D-4 / S12-D-1 / S8-E-1 / S12-B-2 / S13-B-1 未重开未代跑（属地在姊妹区）；catalog 行序契约（R28-B W3）与 R28-I npm `.bin` symlink 静默 no-op 均未触碰；file-lock `retryMs ?? 10` 原样；**J1 fold 形状锁定原样**。
- 仓库变更仅本报告一个文件：`git diff cursor/sota-persistent-opt-83a1...HEAD` = 本文件新增；重锚 harness 未入库（附录 A）。

## 4. 前后对比

无生产代码变更，无前后对比对象。切片 I/O 地板带维持 R34-J 记载（本轮九格复测见 §1.1）。

## 5. MORE_OPTIMA 判定

- 29 文件零未点名站点；候选复命中 12/12 拒列、零新站点级换名记录；R34-J 四项记录 + 既有 S\*-J-\* 全系原形在位；重锚九格全为宿主窗（冻结 0 行三重证据）；J1 sim 2468 全绿；每命令持久成本上界 < 1 ms，距落地线 1.5–2 个数量级，且其组成已逐段归属既有 ID / 契约面。
- 判定：**MORE_OPTIMA=no**。

## 6. 新增排除 ID：无

不铸 S35-J-\*；不新增 EXCLUSIONS 表行；空枚举轴不取绰号；不重铸 J1 / R34-J 四项记录 / R32-J 六坐标 / LINKTOP / MODEX / 姊妹空枚举方法。

## 附录 A：重锚原始输出（harness 未入库；与 R12-J 附录 A 除头注释与 tmpdir 前缀外逐字同源）

```text
# pass 1                                          # node v22.22.2
saveToDisk 50-obs seed: 319.2 / 217.6 / 146.7 us/write
saveToDisk 500-obs seed: 479.6 / 489.6 / 438.7 us/write
jsonl fsync=false: 76.9 / 73.0 / 69.7   fsync=true: 606.0 / 399.5 / 583.4 us/append
cascade MATCH: 833.1 / 808.1 / 775.0    NO-MATCH: 311.8 / 320.7 / 344.0 us/cascade
buildProjectContextIndex: 38.1 / 37.9 / 38.0 us/build
plan counter: 1.06 / 1.06 / 1.07        plan randomUUID: 2.67 / 2.65 / 2.66 us/plan

# pass 2                                          # node v22.22.2
saveToDisk 50: 253.7 / 219.9 / 150.7    saveToDisk 500: 509.9 / 466.9 / 446.2
jsonl false: 76.3 / 74.9 / 70.8         jsonl true: 326.3 / 299.4 / 290.0
cascade MATCH: 865.5 / 772.0 / 840.0    NO-MATCH: 290.3 / 285.5 / 326.9
context: 37.8 / 37.8 / 37.7             plan: 0.97 / 0.99 / 0.98   uuid: 2.56 / 2.58 / 2.52

# pass 3                                          # node v22.22.2
saveToDisk 50: 397.2 / 146.3 / 148.5    saveToDisk 500: 490.8 / 446.5 / 454.0
jsonl false: 75.1 / 76.5 / 69.8         jsonl true: 801.5 / 698.4 / 742.7
cascade MATCH: 897.6 / 739.9 / 746.8    NO-MATCH: 296.5 / 324.2 / 298.9
context: 38.4 / 37.9 / 38.0             plan: 1.06 / 1.06 / 1.08   uuid: 2.74 / 2.69 / 2.69

# J1 sim (scripts/r1j-equivalence-sim.ts)
scenario 1 (deterministic edge cases): 64 checks over 16 cases
scenario 2 (randomized): 2400 checks over 600 cases
perf fixture (N=6000, multi-subject, ~25% tombstoned):
  reference 10400.5 ms -> current 3.5 ms (2948.5x)
  reference element visits per evaluation: 92,590,419
ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)
```

---

MORE_OPTIMA=no
BRANCH=cursor/r35-j-persist-thirty-fifth-pass-83a1
