MODEL_SLUG=claude-fable-5-thinking-xhigh

# R35-F：`src/experiments/` 第三十五遍复查报告（S7-F-1 + S7-F-2 之上，全新第三十五组枚举）

- **战役:** 全库持久 SOTA 优化 Round 35 / R35-F（R1-F…R34-F 同区三十四遍之后的第三十五遍）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `a47c9b0`（R35-C 摄取——空枚举——+ 本 R35-F 派单）。运行中独占分支**两次纯 docs 快进**：`a47c9b0` → `32c63a9`（R35-D 摄取 + R35-G 派单）→ `17febc2`（R35-E 摄取 + R35-H 派单）——两次增量均恰 3 个 docs 文件（PROGRESS / round-35 PLAN / 新姊妹报告），`-- src/` 直接复核各 **0 行**；按令两次**纯快进 rebase**（零冲突）并各自重验冻结（切片 vs `519101f` 与最强冻结 `89f0455..HEAD -- src/` 均 **0 行**）。新姊妹报告 **R35-D**（adaptation 空枚举，四项换名记录拒列）与 **R35-E**（learning 空枚举，一项换名记录拒列）已全文实读并折入非移植清单（§0）——零 F 可移植轴，不重铸。提交前 fetch 复核 tip 仍为 `17febc2`。派单要求预读的 **R35-A / R35-B / R35-C** 三份姊妹报告亦已全文通读并折入非移植清单（§0）。R35-G / R35-H 在飞，按令未等待、未触碰姊妹分支。
- **分支:** `cursor/r35-f-experiments-thirty-fifth-pass-83a1`
- **切片:** 15 文件 `src/experiments/`（全量重读确认，`wc -l` 合计 **2550** 行，与 R29-F…R34-F 普查同数，无缺无余：attribution-report 21 / canary 292 / comparison-report 321 / dataset 206 / evaluation-card 65 / gated-comparison 93 / holdout 140 / isolation 68 / manifest 145 / plan 165 / replay 149 / shadow-compare 44 / shadow 505 / simulation-holdout 272 / threshold-calibration 64）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R24-F…R34-F 同引擎位级相同，带可比；本轮 `node -p process.versions.v8` 直证）；tsx（仓内 node_modules）、pnpm 10.17.1、`package.json` / `pnpm-lock.yaml` 对 `519101f` 复核 **0 行**。本 VM 系统默认 node 为 v22.14.0（`/exec-daemon` PATH 前缀遮蔽 nvm）——按 R32-D §1(iii) / R33-F / R34-F 在案陷阱**开工即**显式钉 PATH 到 `~/.nvm/versions/node/v22.22.2/bin`，每个官方批次前在同 shell `node --version` 复核 v22.22.2（十个官方批次全部带内自报），**零批次落在 v22.14.0 上**（会话重置弃置批说明见 §1 诚实注记 (iii)）。

## 结论

**诚实的空枚举：第三十五组没有可立 ID 的新角度，不铸任何 S35-F-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不写 EXCLUSIONS 行，生产代码零改动（R12-F…R34-F 二十三连空之后的第二十四连空）。且本轮**零新站点级换名记录**：从零重推的全部候选角度经对表逐一查证，每一个都命中既有排除 ID 本体、R4-F 在案防御纵深保留域、R32-F/R33-F/R34-F 既有换名记录（十项全部复核维持）、姊妹归档面（禁移植）或停驻门成员——没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标（§0 裁决清单）。本轮第三十五遍从零重读 15 个源文件与全部四个生产消费文件、在本 VM 重建全部锚点（r5f/r6f/r7f 各**三次**独立运行 + r1f 一次，§1）、对 R31-F §2 面格属主表做**第五次**独立重推（无一格松动，BINDX 计数 **10 token / 8 行** 第二次独立复证成立、坐标未动，§2）、并按 R32-A 首创且 plan-ban 重铸的哨兵方法对 **F 自档案**在案重开条件做**不计时静态卫生复核**（§3——与 R33-F/R34-F 同款，仅作卫生性应用、不当边际产出申报、不铸「我又查了一遍重开条件」式新面），全部哨兵**未触发**。空枚举与 R12-F…R34-F、R35-A…R35-E 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 15 文件自 `519101f` 逐字节零变化（分支创建时 / 两次快进 rebase 后 / 测量与门禁完成后 / 提交前**四验** `git diff 519101f..HEAD -- src/experiments/` 均 **0 行**；`git log -1 -- src/experiments/` 显示切片最后一次生产改动仍为 `519101f`「perf(experiments): S7-F-1 aligned-prefix restore fast path + S7-F-2 ASCII-head trim guard」）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均为 docs，含两次快进增量）。锚点复测（各 ×3，§1 诚实注记）：**r5f C2 117.53 / 126.01 / 117.46 ms**；**r6f partD 119.12 / 117.99 / 117.61 ms**；**r7f partE 124.19 / 127.43 / 125.82 ms**（本 VM 呈同向轻度偏快，与 R35-B/R35-C/R35-E「host-fast」同波注记；冻结 0 行按令不构成新发现）；r6f 三次序 + r7f 四次序 restore 省额**三轮全正无符号翻转**（S7-F-1 ≠ S6-F-5 复核成立：同下标哈希相等 + 后缀回退形态在位——shadow.ts L355–430 读码直证 L383 `episodeHash !== population[index]` 同位比较、首个错位回退 S6-F-1 pending-Set、index 0 错位重跑原环，非 O(P) 双指针）；r1f 2668 项逐位检查全过（参考侧成员比较 **167,167,000 次**与历轮锚定值逐位相同）。聚焦实验测试 **75/75 绿**；`pnpm gate`（typecheck + lint + test + build）**全绿**——test 1169 / pass 1168 / fail 0 / skipped 1（与 R33-F/R34-F/R35-A/R35-B/R35-E 逐字同计数，exit 0）。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动（X0-11——PAIRX 编排位点 `attribution-report.ts` 本轮零字节未触、本轮零计时装具且无任何脚本 import 两估计器）；无阈值/测试/公开签名/数据面变更；无 fsync/完整性重哈希移除；fail-open 家族保持关闭。仓库变更仅本报告一个文件。不开 PR。

## 0. 冻结、摄取处置与非移植清单（方法第 1、4 步）

- 分支按令创建于派单 tip `a47c9b0`；两次纯 docs 快进各自按令纯快进 rebase（零冲突），冻结四点核对：分支创建时 / 每次 rebase 后 / 提交前，`git diff 519101f..HEAD -- src/experiments/` 均 **0 行**。
- 派单预读 + 快进携带的五份姊妹报告全文实读并划界：**R35-A**（tracking 空枚举——三项站点级换名记录 unobserved 单例享元 / asString trim 首字符卫 / prescore dip 乘序，全部 tracking/human-score 站点）；**R35-B**（live 路由空枚举——零新站点级记录；天花板复锚主机偏快）；**R35-C**（离线路由空枚举——一项换名检查为 `src/routing/posterior.ts` 双 `weightedSampleSize` 去重，C 区物理对象）；**R35-D**（adaptation 空枚举——四项换名记录全部 adaptation 站点）；**R35-E**（learning 空枚举——一项换名记录 `runAutoAdaptLoop` L88 bandit 前置门探针融合/下沉，`src/learning/auto-loop.ts` E 区物理对象）。五者与 F 切片**零共享物理对象**，均为空枚举轴外号，**已并入非移植清单，不重编号、不移植、不重提**。
- **非移植清单全额维持并逐项核对**（派单 DO NOT REMINT 全清单 + round-35/PLAN F 段）：R30-F 边界输出消费/快照读回拓扑面、R29-F 输出保留/被替换快照存活面、R28-F 边界调用协议面、R27-F 输入访问路径机制面、R26-F 输入存储状态面、**R31-F 故障通道处置残格**（分解归 R17-F + R30-F + R29-F + 引擎面——本轮不重铸、不新停驻、零计时臂）、**PAIRX**（R29-C——编排位点共驻不当新 F ID）、**BINDX**（R31-C——切片 ambient `Math.*` token 恰 **10 个于 8 行**：comparison-report `Math.abs` ×4 + `Math.sqrt` ×2、gated-comparison `Math.sqrt` ×2、replay `Math.imul` ×2，本轮 `rg -o` 逐 token 直数与 R33-F 订正值/R34-F 首复证**第二次独立复证成立**；行集合同一、坐标未动；计数即止零 patch 零剂量）、**MODEX**（R31-J——切片 chmod/umask/八进制 mode 字面量 **0**（宽式 `mode:` 仅命中 plan.ts L21/L97 的 `ExperimentMode` 领域字段，非权限位）、零盘上工件，划界声明即止）、I/O text-codec topology（R31-E——`rg "node:fs|readFile|writeFile|Buffer"` 本轮首手复核**零匹配**，公开边界全同步纯内存，面结构性空）、REPRX（R30-C——切片 `JSON.parse` 位点 **0**）、R32-A 重开条件哨兵审计（**方法不重铸为面**——§3 按令仅作卫生性应用于 F 自档案）、R32-B 闭合稳定性审计（**方法不重铸为面**）、**R33-A … R34-J 空枚举**与 **R35-A…R35-E 空枚举**（上条已划界，声明即止）、R31-A injected-callable 停放门、R31-B 六停驻门、R31-D 的 R22-D 属地格、R31-G 18 门审计、R31-H 四扇边界参数残门、R31-I CLI 进程边界门扉闭合审计（F 是进程内库——切片 node: 引入恰 2：isolation.ts:1 `node:path`、holdout.ts:1 `node:crypto` randomUUID，本轮 rg 直证）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / CSPELL / REPRX / LINKTOP / MODEX 全部未编号未移植。S7-F-1 ≠ S6-F-5 维持（§1 次序符号复核 + 结论段读码直证）。S5-F 已落地不重做（plan.ts 单探针现场在位，§4）。
- **换名重提检查**（本轮枚举全部残余角度的逐项裁决，一律不铸 S35-F-\* ID；**零新站点级记录**——每一项均能指到在手报告逐字点名过的行）：
  1. `pairedEvaluationCard` 内 `sampleStandardError` 对 `mean` 的内部重算 = **S2-F-6 本体亚案** + R34-F 换名记录 2（mean/SE 孪生辅助）同族——复命中即止；
  2. `pairedDeltaSummary` `count===0` 死抛分支与 threshold-calibration `best?.f1 ?? -1` 死回退 = **R4-F 在案「pairedDeltaSummary 型死分支（rows[0] 恒存在）」防御纵深保留域**（R4-F §逐文件表 threshold-calibration 行逐字点名）——原型站点复命中即止；
  3. `IMPROVEMENT_CLAIM` / `IMPROVEMENT_PATTERN` 孪生正则 = R33-F 换名记录 3 维持；`validateComparisonReport`+`computeComparisonReport` strip-retry 双调 = S1-F-1 本体；utility delta 双算 = S4-F-3 本体；
  4. replay.ts orderedHashes spread 复制 = R33-F 换名记录 1 维持（零生产调用方 + µs + R15-F 轴 3）；
  5. simulation-holdout 条件 spread 装配链 = R34-F 换名记录 1 维持；familyBreakdown 查重单探针化 = R34-F 换名记录 3 维持；mean/SE 孪生去重 = R34-F 换名记录 2 维持（含 resolvePending 孪生对同族注记）；
  6. manifest stableStringify 形态 = R32-F 换名记录 1 维持；HoldoutVault 注入缝默认臂 = R32-F 换名记录 2 维持；shadow 返回值拷贝省略 = R32-F 换名记录 3 维持；shadowDecisionAt 闭式跳步 = R33-F 换名记录 2 维持；requireCanaryBlock 重派生缓存 = R33-F 换名记录 4 维持。
  R32-F 三项 + R33-F 四项 + R34-F 三项既有换名记录本轮**全部复核维持不重提、不新增**。
- 未重开任何 X\* / S1-\* … S34-\* 条目。**单变量纪律**：本轮**无任何计时新臂**——四个锚点脚本均为 `scripts/round0{1,5,6,7}-*.ts` 入库血统资产逐字节原样重跑（零改动零参数）；§2/§3 全部为静态 rg / 读码断言（不计时）。S9-D-4 / S12-D-1 / S8-E-1 / S8-I-1 / S13-B-1 / S12-B-2 等姊妹区维持项零触碰（其哨兵属地在 A/B/C/D/E/I，本轮不代跑——R35-B/R35-C/R35-D/R35-E 已各自复核未触发）。

## 1. 锚点重测（r5f/r6f/r7f 各三次独立运行，r1f 一次；本 VM 实测，未拷贝历轮数字；nvm Node v22.22.2；全部在最终 tip `17febc2` 工作树完成）

| 档位 | 第 1 次 | 第 2 次 | 第 3 次 | R34-F 记载带（派单对照） |
| --- | --- | --- | --- | --- |
| r5f C2 全实验（P=2000/A=1000） | 117.53 ms | 126.01 ms | 117.46 ms | 119.21 / 119.51 / 128.96 ms |
| 2A-validate 组件 | 96.39 ms（82.0%） | 105.09 ms（83.4%） | 96.81 ms（82.4%） | 98.08–99.41 ms（76.1–83.2%） |
| r5f C1 has+add → add+size 省额 | +36.12 ms（23.9%） | +35.02 ms（23.0%） | +36.41 ms（23.9%） | +29.12~+32.18 ms（20.0–21.5%） |
| r6f partD 全实验 | 119.12 ms | 117.99 ms | 117.61 ms | 121.63 / 123.10 / 121.90 ms |
| r7f partE 全实验 | 124.19 ms | 127.43 ms | 125.82 ms | 129.72 / 130.28 / 128.18 ms |
| r7f validate 卫（×2000） | +10.86 ms | +10.83 ms | +10.06 ms | +8.87 / +8.56 / +8.68 ms |

**次序符号（committed 回归脚本原生视图，全部为「落地生产形态 vs 冻结参考」省额）**：r7f partE 四次序三轮全正——prefix(aligned) +48.06/+48.04/+46.42、half-aligned +29.76/+28.14/+28.42、scattered +4.48/+4.49/+3.13、reversed +5.85/+5.45/+4.50 ms，无符号翻转；r6f partD 三次序三轮全正——prefix +128.30/+126.49/+126.60、scattered +29.73/+24.08/+30.72、reversed +25.22/+23.86/+26.82 ms。**S7-F-1 ≠ S6-F-5 复核成立**：落地代码零漂移（切片零 diff），同下标哈希相等 + 后缀回退形态（非 O(P) 双指针）在全部次序下三轮同号为正。r5f C4 索引环形式 −2.77/−4.71/−3.72 ms 维持 S5-F-3 抖动带记载（R34-F −2.70/−2.66/−4.75）；C5 dataset 镜像 17.5–17.7 µs（U=2000）/ 136.1–144.7 µs（U=20000）维持 S5-F-1 记载（R34-F 12.2–15.2 / 131.3–138.0、R33-F 9.6–14.6 / 134.7–143.8——U=20000 与 R33-F 带交叠；U=2000 高侧个体见注记 (ii)）。r1f 2668 项逐位检查全过（生产侧 60.3 ms / 参考侧 564.6 ms，9.4×；参考侧成员比较 **167,167,000 次**——与历轮锚定值逐位相同；参考侧壁钟非锚点义务，钉死量是比较计数与逐位等价）。

**诚实注记**：(i) 本 VM 会话呈**同向轻度偏快**宿主刻度：r5f C2 两读数 117.53/117.46 擦沿历史读数并集下缘 117.5（run 3 低 0.04）；r6f partD run 3 117.61 低于 R24-F…R29-F 带下缘 117.9 约 0.29；r7f partE runs 1/3（124.19/125.82）低于 R28-F…R30-F 包络下缘 126.63 约 0.81–2.44 ms（run 2 127.43 与 R33-F 带交叠）。三档同向，与 R35-B「host-fast」/ R35-C / R35-E「主机偏快」同波 VM 现象；冻结 0 行 + 引擎位级同一 + 脚本逐字节原样 ⇒ 宿主刻度项非切片行为变化，按派单令「host-bias windows with freeze 0 lines are not new findings」不作发现、不改带。(ii) 少数格高侧个体同判宿主刻度：C1 省额比例 23.0–23.9% 高出 R26-F…R33-F 紧邻带顶 23.5 约 0.4pp（绝对省额三轮紧致 +35.02~+36.41 ms、符号稳定）；r7f validate 卫 +10.06~+10.86 高出 R33-F/R34-F 带（+8.00~+9.37）约 0.7–1.5 ms（同号）；C5 U=2000 17.5–17.7 µs 高出历带 ~2.5 µs（informational test-only µs 域镜像，U=20000 带交叠成立）——省额方向全部不变，不构成任何重开触发（§7）。(iii) 本轮运行中一次**会话重置**导致早前锚点批次的终端输出未留存——该批亦在同 shell 自报 v22.22.2，但按官方口径纪律**整批弃置**，于最终 tip `17febc2` 工作树全新重测全部四脚本官方批（本节全部数字均出自该单树单引擎批次）；零批次落在系统 v22.14.0 上。(iv) 两次纯 docs 快进（基线段）均在官方批次**之前**完成，官方批与门禁全部在最终 tip 完成，无跨树携带。

## 2. 调用面复核 + 面格属主表独立重推（元闭合，方法第 3 步前置；卫生性应用不当边际产出）

生产消费图 rg 全仓复核（与 R30-F §1.2 / R31-F §2 / R32-F §2 / R33-F §2 / R34-F §2 逐条一致）：切片外 `src/` importers 恰四个文件，import 行逐条核对——`adaptation/reflection.ts`（validateExperimentPlan + createShadowRunner + 类型：唯一热交错消费者）、`adaptation/eval-routing.ts`（comparison-report 类型与函数 + gatedComparisonReport + createIsolationGuard + stableStringify + **replayCacheKey**——对 replay.js 的 import 恰此一项，L18 直证，S2-F-2 前提本轮成立）、`adaptation/promotion-rules.ts`（validateComparisonReport 直呼）、`routing/r1-shadow-report.ts`（comparison-report + gated-comparison）。**故障通道处置普查复核**：四消费文件 catch 位点合计 4 处、全部在 eval-routing.ts（L219/L249/L259 包 `JSON.parse`/`readFile`，非切片物），**恰一处**包切片调用——L336 `assertReplayIsolated` 包 `createIsolationGuard`（MESSAGE + WRAP-RETHROW 坐标，once-per-eval 冷通道）；`reflection.ts` / `promotion-rules.ts` / `r1-shadow-report.ts` 零 catch ⇒ 热通道生产坐标仍 = PROPAGATE。切片故障源普查：**142 处** `throw new DomainValidationError`（逐文件计数与 R31-F…R34-F 全同：shadow 27 / plan 22 / dataset 19 / canary 14 / manifest 14 / holdout 11 / simulation-holdout 10 / comparison-report 8 / evaluation-card 7 / isolation 6 / replay 3 / gated-comparison 1 / 其余三文件 0），切片零 `throw new TypeError/Error` 直抛点——故障通道单类维持。

R31-F §2 面格属主表本轮从零**第五次**独立重推（(o) = F 区属主轴；(s) = 姊妹区收口面，明令禁移植；每格属主裁决**无一松动**）：输入存储状态 (o) R26-F / 访问路径 (o) R27-F / 对象表示 (s) REPRX / 引用身份·别名 (s) R29-A / 流组成 (o) R20-F / 计划标头身份几何·计数·成员拓扑 (o) R25-F/R23-F/R22-F / 规模与二阶联角 (o) R14-F 域 + (s) R30-B / 调用协议 (o) R28-F / 排程·实参物化·调用上下文 (s) CALLB/R28-H/R22-H / 调用流次序·分派历史 (s) R30-A / 时钟来源 (s) R29-E / 执行基片·名字解析 (s) R27-H 族 / BINDX / 引擎态 (o) R15-F 轴 3 + R19-F 域 / 输出成功通道保留 (o) R29-F / 输出成功通道消费·读回 (o) R30-F / 输出故障通道制造·单价 (o) R17-F / **输出故障通道调用方处置 = R31-F 分解归属（R17-F + R30-F + R29-F + 引擎面），本轮零计时零重铸维持** / 双估计器共驻 (s) PAIRX（X0-11 禁区）。结构性空直证两行维持：**I/O 编解码拓扑 (s) R31-E——切片 fs/codec 位点 0**；**持久工件权限模式字 (s) R31-J MODEX——切片 chmod/umask/八进制 mode 位点 0、零盘上工件**。**BINDX 划界行第二次独立复证**：`rg -o "Math\.\w+"` 逐 token 直数恰 **10 个于 8 行**（abs 4 + sqrt 4 + imul 2；120 行双 `Math.sqrt` 在列），R33-F 订正持续成立、行集合坐标属主裁决全部不动。第三十五遍枚举在该坐标系内**无剩余未审计平面**。

## 3. F 档案在案重开条件哨兵复核（全部不计时；R32-A 方法卫生性应用，不铸面、不当边际产出）

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| R31-F §7 (a) | 任何 `src/` 生产调用方在热交错内 catch 边界故障并做非平凡处置 | §2 catch 普查：热消费者 reflection.ts 零 catch；唯一切片 catch 仍为 eval-routing L336 once-per-eval ⇒ **未触发** |
| R31-F §7 (b) | 故障对象携带非字符串 own 载荷 | `src/domain/errors.ts` 在最强冻结内（`git diff 89f0455..HEAD -- src/` 0 行）⇒ **未触发** |
| R31-F §7 (c) | 故障通道契约（消息文本/单类不变量）解冻 | 两次快进均 docs-only 且无解冻声明；切片零 diff ⇒ **未触发** |
| S6-F-2 重开 | delete+size 形态实测稳定反超 >5% | 无任何反超证据；本轮落地形态三/四次序省额三轮全正（§1）——按纪律**不重计时被拒形态** ⇒ **未触发** |
| S6-F-5 重开 | assignment 次序被契约化为 population 子序列 | 冻结 0 行 + round-35 PLAN 重申 S7-F-1 ≠ S6-F-5 ⇒ **未触发** |
| S5-F-1 / S4-F-1 前提 | dataset/simulation-holdout 链获得生产消费者 | `rg 'experiments/(dataset\|simulation-holdout\|holdout\|shadow-compare\|threshold-calibration\|attribution-report)' src/`（切片外）本轮**零命中**（精确六名交替式）⇒ 六链零生产消费者 ⇒ **未触发** |
| S2-F-2 / S4-F-2 前提 | `replayPolicy` 获得生产调用方 | eval-routing 对 replay.ts 的 import 恰 `replayCacheKey` 一项（L18 直证，§2）⇒ **成立维持** |
| S2-F-1 前提 | shadowDecisionAt 占比脱离 ~1% 域 | 切片零 diff + 锚点带内/宿主快侧同向 ⇒ **成立维持**（R33-F 换名记录 2 的真尺度拒绝依此维持） |
| X0-11 / PAIRX | 双路估计器拆分/重排/省略 | attribution-report.ts 零字节未触；本轮零装具 import 估计器 ⇒ **冻结在位** |
| 签名/调用图类（S1-F-6、S1-F-8、X3-3、S3-F-2 等） | 要求 `src/` 变更 | 最强冻结 0 行 ⇒ **全体判空** |

全部哨兵未触发。凡姊妹属地哨兵（S12-B-2 / S13-B-1 / S8-E-1 / S8-I-1 / S9-D-4 / S12-D-1）本轮不代跑——其属主轮 R35-B / R35-C / R35-D / R35-E（及 R33-I / R34-H）已各自复核未触发。

## 4. 逐文件收口（R1-F…R34-F 收口之上的本轮检查点）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| `shadow.ts` | 27 抛点单类维持；S7-F-1 落地形态零漂移（L355–430 读码直证：L383 同位串等 + 首错位 S6-F-1 后缀回退 + index 0 原环重跑）、四次序省额三轮全正（§1）；shadowDecisionAt 闭式跳步 = R33-F 换名记录 2 维持；shadowDecisionAt O(index) 重放 = S2-F-1；restore→assign Set 复用 = S2-F-4/S9-F-1；recordOutcome 末元素见证 = S9-F-2、查重/成本增量 = S1-F-8；accumulatedCostUsd 每 record 全扫 = S1-F-8 本体；空 assignments 跳 Set = S3-F-3；计划引用同一跳验 = S3-F-2；每操作全量 fail-closed restore = X3-3 本体；返回值拷贝省略 = R32-F 换名记录 3 维持；状态 spread 对 = R15-F 轴 3 引擎面；`resolvePendingMembership` 孪生 = R34-F 换名记录 2 同族注记维持 | 无候选 |
| `plan.ts` | 22 抛点；S5-F 单探针 + S7-F-2 首字符卫现场在位零漂移（不重做）；批量 Set = S5-F-2、null-proto = S6-F-4、批快路径 = S6-F-3、长度 1 快路径 = S8-F-3、scratch 共享 = S9-F-3、返回 Set = S1-F-6；EXPERIMENT_ID_PATTERN 模块级已提升；`mode` 字段 = 领域枚举非权限位（MODEX 划界注记，§0）；r5f C1/C2、r7f validate 卫三轮全正 | 无候选 |
| `canary.ts` | 14 抛点；同构对齐环 + S6-F-1 回退零漂移；derivedExposure 重算是 fail-closed 交叉验证本体（消除即 fail-open，非候选）；requireCanaryBlock 重派生缓存 = R33-F 换名记录 4 维持；reversibleScopes includes = S1-F-7；exposure 派生为契约本体；`resolveCanaryPendingMembership` 孪生 = R34-F 换名记录 2 同族注记（X1-5 契约不同不并） | 无候选 |
| `gated-comparison.ts` | 1 抛点；IMPROVEMENT_CLAIM 孪生正则 = R33-F 换名记录 3 维持；mean/sampleStandardError 孪生辅助去重 = R34-F 换名记录 2 维持；**pairedEvaluationCard 内 SE 对 mean 的内部重算 = S2-F-6 亚案**（§0 裁决 1，复命中即止）；strip-retry 复用 = S1-F-1；多遍融合 = S2-F-6 本体 | 无候选 |
| `replay.ts` | 3 抛点；orderedHashes spread 复制消除 = R33-F 换名记录 1 维持（零生产调用方 + µs + R15-F 轴 3）；双 manifestHash = S2-F-2（零生产调用方本轮直证）；selected propensityFor 二次调用 = S1-F-2；空 exclusions 免建 = S4-F-2；exclusions Set 化已落地（H1）；rerunHash 换更快同字节序列化 = S6-D-5 已拒（且 replayPolicy 零流量）；replayCacheKey 唯一生产腿 = once-per-eval µs（S7-D-2 邻域） | 无候选 |
| `comparison-report.ts` | 8 抛点；IMPROVEMENT_PATTERN 孪生编译 = R33-F 换名记录 3 同项；六遍改单遍 = X3-2；校验遍+构建遍双遍 = X3-2 族；四均值 reduce = X3-2 族；`Array.from(families.entries())` = S1-F-5；delta CSE = S4-F-3 本体（本轮 utility delta 双算复命中即止）；familyBreakdown 查重单探针化 = R34-F 换名记录 3 维持；**pairedDeltaSummary `count===0` 死抛 = R4-F「pairedDeltaSummary 型死分支」防御纵深保留域原型站点**（§0 裁决 2，复命中即止）；validateComparisonReport 生产消费者（promotion-rules）零 catch = PROPAGATE 维持 | 无候选 |
| `threshold-calibration.ts` | 零抛点；三遍并单遍 = S1-F-4（一次性 informational）；**`best?.f1 ?? -1` 死回退 = R4-F 同一防御纵深保留域**（R4-F 逐文件表本行逐字点名，§0 裁决 2） | 无候选 |
| `shadow-compare.ts` | 零抛点；薄封装维持 X1-5 | 无候选 |
| `isolation.ts` | 6 抛点；resolve(outputRoot) 提升 = S3-F-4；per-root 双 isInside 的 resolve 重复 = 同族 + once-per-eval µs；**全生产普查唯一被 catch 的切片故障源**（eval-routing L336，once-per-eval 冷通道，§2） | 无候选 |
| `simulation-holdout.ts` | 10 抛点；结果装配条件 spread 链 = R34-F 换名记录 1 维持（S8-E-2 镜像 + 零流量 + µs）；探针去重镜像 = S5-F-1（链零生产消费者本轮直证）；尾部双验 = S2-F-5（fail-closed 纵深）；二遍构建 = X3-2 族 + 零流量；toFrozenEpisode 投影 = S3-F-5；auditHoldoutAccess try/register = once-per-run | 无候选 |
| `holdout.ts` | 11 抛点；审计追加拷贝 = S1-F-3/X4-2；注入缝默认臂 nowIso/randomUUID = R32-F 换名记录 2 维持（R31-A 停放 + 零生产流量）；Map 重 set 不可变态 = X4-2 族；replace 的 reason 前置检查次序 = 契约文本面（消息文本冻结，R31-F (c) 邻域，非候选） | 无候选 |
| `manifest.ts` | 14 抛点；stableStringify 形态改造 = R32-F 换名记录 1 维持（字节契约仪器 + 零热流量 + S1-I-7/X1-1 族）；validateManifest 每建 Set = once-per-creation；`?? []` 空数组默认分配 = R15-F 轴 3 引擎面 ns 域（mark/assert 腿 test-only 零流量） | 无候选 |
| `evaluation-card.ts` | 7 抛点；validate 数组字面量 = S1-B-7 域 | 无候选 |
| `dataset.ts` | 19 抛点；seen Set 复用 universe = S4-F-1；has+add 原形镜像去重 = S5-F-1（test-only 链维持，C5 本轮 17.5–17.7 µs 高侧个体注记见 §1(ii)）；membership Map 双结构 = 同域；rotateHoldout 双验 = S2-F-5 族 once-per-rotation | 无候选 |
| `attribution-report.ts` | 零抛点；**PAIRX 编排位点**——零字节未触、X0-11 双路对未拆分/未重排/未省略 | 无候选（共驻剂量归 R29-C） |

## 5. 候选总表

**空。** 本轮零候选：面格属主表第五次独立重推（§2）无一格松动且无新残格（BINDX 计数 10/8 第二次独立复证成立，坐标不动）；哨兵复核（§3）按定义不产出候选（不计时、不剂量）；十五文件枚举（§4）全部命中既有 ID 本体、R4-F 防御纵深保留域、R32-F/R33-F/R34-F 既有换名记录、姊妹归档面或停驻门成员——**零新站点级换名记录**。本轮无任何计时新臂 ⇒ 无可定价候选面。

## 6. CHECKS

- 分支恰 `cursor/r35-f-experiments-thirty-fifth-pass-83a1`，基线 `a47c9b0`；两次纯 docs 快进（`a47c9b0`→`32c63a9`→`17febc2`）各自纯快进 rebase 零冲突、增量 `-- src/` 各 0 行；提交前 fetch 复核 tip 仍 `17febc2`。
- 冻结四点核对全 **0 行**（§0）；15 文件普查 2550 行与 R29-F…R34-F 同数；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**；`package.json` / `pnpm-lock.yaml` 对 `519101f` **0 行**；`git log -1 -- src/experiments/` 仍 = `519101f`。
- 锚点 r5f/r6f/r7f 各三次独立运行 + r1f 一次全部 0 失败（224 / 27 / 169 / 2668 项，§1）；四次序 + 三次序省额三轮全正，无符号翻转；全部在 nvm Node v22.22.2 上执行（每个官方批次前 `node --version` 在同 shell 复核；会话重置弃置批说明见 §1(iii)，零批次落在系统 v22.14.0 上）；官方批全部在最终 tip `17febc2` 单树完成。
- 生产消费普查：4 个 importer 文件（import 行逐条核对）；catch 位点恰 1 处包切片调用（冷通道 once-per-eval，L336）；热消费者零 catch；142 抛点逐文件计数与 R31-F…R34-F 全同；切片零直抛 Error/TypeError。
- 姊妹面划界计数：BINDX `Math.*` **10** token 于 8 行（第二次独立复证；abs 4 + sqrt 4 + imul 2，行集合同一）；R31-E codec 位点 0；REPRX `JSON.parse` 位点 0；MODEX chmod/umask/八进制 mode 位点 0（宽式 `mode:` 命中仅 plan.ts 领域枚举字段）；node: 引入恰 2（isolation.ts node:path、holdout.ts node:crypto）；六链切片外消费者 0（精确交替式零命中）。
- 聚焦测试：`npx tsx --test test/unit/experiments/*.test.ts` = **75/75 pass**（10 suites，最终 tip）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿**（exit 0，最终 tip）——test 1169 / suites 78 / pass 1168 / fail 0 / skipped 1（既有 provider-smoke 凭据跳过，与 R33-F/R34-F/R35-A/R35-B/R35-E 记录同计数）；Node v22.22.2。
- 仓库改动恰本报告一个文件（`git diff cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；live = R0 等价（R1 未接线）；promotion proposal-first；无 Outcome-supported 主张（Checkpoint F-PROD 开放，ADR-005）；无阈值/测试/权限/数据面/公开签名改动；CAS/凭据契约原样；无 fsync/完整性重哈希移除；fail-open 家族保持关闭；分析未变更任何 in-flight run；tracking 无命令权；H/score 未写路由 PASS/FAIL；catalog 行序契约（R28-B W3）与 npm POSIX `.bin` symlink 静默 no-op（R28-I）零触碰；EXCLUSIONS / PROGRESS / PLAN / README 未编辑；不开 PR。

## 7. 重开条件

本轮无新定价轴，不加行。R31-F 故障通道处置分解的三条显式重开触发（(a) 热交错内非平凡 catch 处置；(b) 非字符串 own 载荷；(c) 通道契约解冻）经 §3 哨兵直证全部未触发，原文全额维持；R30-F（输出消费）、R29-F（输出保留）、R17-F（拒绝终端）、S6-F-2 / S6-F-5 / S5-F-1 / S2-F-1 / S2-F-2 既有重开条件同样全额维持。锚点宿主刻度注记（三档同向偏快 + C1 比例/validate 卫/C5 U=2000 少数格高侧个体，§1 注记 (i)/(ii)）不构成重开触发——冻结 0 行、省额符号三轮稳定、r1f 计数逐位同历轮、带并集近重合。本轮零新换名记录，不设新重开条件。

## 8. 新增排除 ID

**无。** 空枚举轮不铸 S35-F-\* ID、不铸 face 名、不取绰号、不加 EXCLUSIONS 行。本轮**零新站点级换名记录**（§0 裁决清单全部为既有行复命中）；R32-F 三项 + R33-F 四项 + R34-F 三项既有换名记录复核全部维持。

## 附录：仿真出处与运行摘录

本轮**未新建**任何仿真/见证脚本：全部计时证据来自入库血统资产的逐字节原样重跑（`scripts/round01-r1f-equivalence-sim.ts`、`scripts/round05-r5f-equivalence-sim.ts`、`scripts/round06-r6f-equivalence-sim.ts`、`scripts/round07-r7f-equivalence-sim.ts`，零改动零参数）；§2/§3 哨兵为只读 rg / 读码断言（不计时、无临时文件）。运行：`npx tsx scripts/round0X-*.ts`（nvm Node v22.22.2，PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开 `/exec-daemon` 系统 Node v22.14.0）。

三次官方运行关键行摘录（r7f；r5f/r6f 对应行见 §1 表）：

```text
run 1: part E prefix saving 48.06ms | half 29.76 | scattered 4.48 | reversed 5.85
       validate x2000 saving 10.86ms | full experiment 124.19ms | 169 checks, 0 failures
run 2: part E prefix saving 48.04ms | half 28.14 | scattered 4.49 | reversed 5.45
       validate x2000 saving 10.83ms | full experiment 127.43ms | 169 checks, 0 failures
run 3: part E prefix saving 46.42ms | half 28.42 | scattered 3.13 | reversed 4.50
       validate x2000 saving 10.06ms | full experiment 125.82ms | 169 checks, 0 failures
```

r1f 摘录：`reference 564.6 ms -> current 60.3 ms (9.4x)`；`reference membership comparisons for the full run: 167,167,000`；`ALL EQUIVALENCE CHECKS PASSED (2668 bitwise checks)`。

MORE_OPTIMA=no
BRANCH=cursor/r35-f-experiments-thirty-fifth-pass-83a1
