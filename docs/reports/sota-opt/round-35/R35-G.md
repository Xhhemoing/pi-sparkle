MODEL_SLUG=claude-fable-5-thinking-xhigh

# R35-G：runtime/graph/domain 切片 Round 35 三十五搜报告

**战役:** 全库持久 SOTA 优化 Round 35 / R35-G（十区之一，R1-G…R34-G 的第三十五遍）
**基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `32c63a9`（派单 tip，R35-D 摄取 + 本 R35-G 派单）；工作期间独占分支 docs-only 快进**两笔**：`32c63a9` → `17febc2`（R35-E 空枚举摄取 + R35-H 派单，测试与门禁完成后 fetch 所见）→ `598b0f9`（R35-F 空枚举摄取 + R35-I 派单，提交前 fetch 所见）。增量核对：两笔各仅触 `docs/reports/sota-opt/` 下 3 文件（PROGRESS / round-35 PLAN / 新姊妹报告），对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接复核各 **0 行**；按令各执行纯快进 rebase（零冲突），42 文件冻结在每个新 tip 复验 **0 行**；R35-E / R35-F 结论均已全文通读并折入非移植清单（§0）。全部锚点与测量在 `32c63a9` 工作树完成——快进增量纯 docs、三 tip `src/` 逐字节相同 ⇒ 测量形态与提交形态位级同一，全部有效；聚焦套件 + `pnpm gate` 在 `17febc2` 与最终 tip `598b0f9` 各复跑一次全绿同计数。R35-H / R35-I 在飞，按令未等待、未触碰。
**分支:** `cursor/r35-g-runtime-thirty-fifth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（R13-G…R34-G 同引擎线，带可比；tsx 4.23.12 经仓内 node_modules，pnpm 10.17.1，`pnpm install --frozen-lockfile` 干净完成；本轮 PATH 在任何计时前钉死 v22.22.2（`which node` / `node --version` / `process.versions.v8` 三复核），**零** v22.14.0 误跑批次、零弃置批次、零仪器事故——临时基准文件按 R34-G §1 注记 iv 在案教训于门禁前删除，三次官方 gate 均一次通过）。

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S35-G-\* ID，不铸新 face 名、不取绰号、不加 EXCLUSIONS 行，切片零代码改动。** 第三十五遍从零重读 42 文件（`wc -l` 合计 **8960** 行与 R32-G / R33-G / R34-G 记录一致，清单核对恰 42 文件：run 17 + supervisor 4 + graph 4 + domain 17），逐文件枚举的全部消除角度经换名检查裁决为既有排除 ID 本体、姊妹归档面（禁移植）、plan-ban 轴名或全局计算顶之内的亚噪声——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（§3）。本轮恰 **2 项新站点级换名记录（覆盖 3 站点）**，全部三重拒列：(1) `flowchart-run.ts` 命令入口 `childResultsMap` / `childTaskMap` 孪生 Map 物化前奏（:168–176，全部 G 报告 rg 零先例命中——once-per-command ns–µs 拒收 + 计算顶内亚噪声 + 融合属零动态增量改写，§3.0-1）；(2) `flowchart-run.ts` `familyForFlowNode` 内形辅助（:671–675，零先例命中——所居外围站点 `applyLearnedToNode` 即 **S8-E-3** 排除行本体 + once-per-run µs 拒收 + Live=R0 不变量毗邻，§3.0-2）。其余候选角度全部为已点名站点复命中（R34-G §3.0-1 `validateJoin` `edges.some` 换名记录按令不重提、原形读码复核在位；leaseReadyNodes 逐节点 computeStatus 重算、advanceLedgerRound 数组 spread 记账、planRound 死形参、S2-G-2 共享 visiting 本体第三十五遍复认——复命中即止，不重演独立复推导叙事）。R31-G 的 18 扇聚合门闭合审计按令不重铸。本轮边际动作仅两类，均不构成新面：(a) 缺省预算重锚（计算顶 **0.293–0.303 ms/run**，I/O 地板 **87.0–100.0 ms/run**，支配比 **~295–330×**，两个数量级支配关系不变；B3 双面轻度高侧均落 R33-G 历史带内，宿主刻度项非发现，§1 诚实注记）；(b) G 档案在案零剂量停放与重开条件的**不计时静态哨兵复核**（§2——方法属 R32-A 首创并已 plan-ban 重铸，此处按令仅作卫生性应用，不申报为新轴），全部哨兵**未触发**。14 格矩阵摘要 `06cbcf92c098c8f0…` 官方引擎三批 × 进程内 3 迭代 = **9 次逐位一致——第二十六轮同值，按令不铸摘要恒等 ID**。空枚举与 R12-G…R34-G、R35-A/B/C/D/E/F 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 42 文件自生产基线 `4efee23` 逐字节零变化（派单 tip / 两次快进 rebase 后 / 提交前**四点**核对 `git diff 4efee23 -- <42 文件>` 均 **0 行**；`git diff --find-renames --name-status` 同区间**空输出**——零改名零增删，无改名裁决需做）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均 docs，两笔快进亦纯 docs）。聚焦套件 run/supervisor/graph/domain + integration：**tests 386 / pass 385 / fail 0 / skipped 1** 全绿（`32c63a9` / `17febc2` / 最终 tip `598b0f9` 树各一次，逐字同计数，v22.22.2，与 R31-G / R32-G / R33-G / R34-G 记录一致）；`pnpm gate`（typecheck + lint + test + build）**全绿三次**（三 tip 各一次）——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A / R32-G / R33-G / R34-A…R34-J / R35-A/B/C/D/E/F 记录逐字同计数），退出码 0。不开 PR。

## 0. 范围与约束遵守（含不移植清单）

- 切片：`src/run/`（除 A 属地 `child-tracking.ts` / `gate-apply.ts`）17 文件 + `src/supervisor/`（除 B 属地 `model-router.ts`）4 文件 + `src/graph/` 4 文件 + `src/domain/` 17 文件，恰 **42**，全部实际重读全文，未依赖前三十四轮记忆。
- 读序遵令：README → EXCLUSIONS 全表 → PROGRESS → round-35/PLAN（G 段 + 全 ban 榜）→ R34-G 全文 → R35-A → R35-B → R35-C → R35-D，之后才读 42 切片文件并动手；两次快进后各补读新姊妹报告全文（R35-E.md / R35-F.md，仅为划界与非移植清单，不据其改动任何测量或结论）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README.md 零触碰。
- **本轮六姊妹空枚举划界（零新轴可移植）**：R35-A（tracking 侧三项站点级换名记录已拒——human-score/types `{kind:"unobserved"}` 五站点字面量享元 / types `asString` 首字符卫镜像 / prescore dip 乘后判，全为 `src/tracking/` A 属地物理对象；本切片按令排除 `child-tracking.ts` / `gate-apply.ts` 二文件，G 侧零共享）；R35-B（live 路由侧**零**新站点级换名记录，两残余角度均 R2-B 已点名站点复命中；S12-B-2 腿 (a) / S13-B-1 重取证未触发——B 属地；G↔B 唯一共享坐标为 `flowchart-run.ts` 的 learned-routing 消费腿，Live=R0 不变量 + S8-E-1 不去重 + 属地边界，零剂量，本清单原有）；R35-C（离线路由侧一项换名记录已拒——posterior `estimateForKey` 双 `weightedSampleSize` CSE，`src/routing/posterior.ts` C 属地物理对象；G 切片零 posterior 消费，`flowchart-snapshot.ts` 对 `routing/catalog-model.js` 的唯一引用为 throw-only 校验桩 `snapshotValidationRouter` 零剂量）；R35-D（adaptation 侧四项站点级换名记录已拒——全为 `src/adaptation/` 物理对象，G 侧零 adaptation 导入零交集）；R35-E（第一笔快进带入，learning 侧一项新站点级换名记录已拒：`auto-loop.ts:88` bandit 前置门探针融合/下沉，E 属地物理对象——G 切片对 learning 的唯一共享坐标仍为 `flowchart-run.ts:712` learned-routing 消费腿，零剂量，本清单原有）；R35-F（第二笔快进带入，experiments 侧空枚举且**零**新站点级换名记录——全部候选命中既有 F 属地行/防御纵深保留域/停驻门；物理对象全在 `src/experiments/`，其 `src/` 消费者恰四文件均属 routing/adaptation 属地，本轮 rg 直证 G 四目录**零** experiments 导入，零共享坐标）。六报告均未铸新面，非移植清单无需加行。
- **不移植禁令逐项核对**：M1 亲代 run 子调度格（R30-G）、M2 监督引擎裁决格（R29-G）、decision-gate resolution（R28-G）、join-policy（R27-G）、**RUNLIM / CKMIX / TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / ZREP**——本轮零剂量零重提；**BINDX**（切片 `Math.*` token 恰 **6 个**：hash.ts:16 `Math.abs` ×1、flowchart-supervisor.ts:606/:608 `Math.max` ×2、inspection.ts:111/:118/:140 `Math.max` ×3，与 R32-G / R33-G / R34-G 计数逐一同址，rg 直证计数即止零 patch）；**MODEX**（切片 rg **零** chmod / 零 umask，exit 1 声明即止）；I/O text-codec topology / per-run transcode-byte manifest（R31-E——切片显式 fs UTF-8 位点恰 **4 个**：checkpoint-store.ts:22/:31、pause-controller.ts:31/:92，与 R32-G…R34-G 记录逐一同址；event-store / episode-store 经切片外 `persist/jsonl.ts` 走同一坐标——E 属地，本轮零编解码剂量）；R31-G 18 门闭合审计（**不重铸「我又审了一遍门」**；摘要恒等第二十六轮不铸 ID）；R32-A 重开条件哨兵审计与 R32-B 闭合稳定性审计（**方法不重铸为面**——§2 按令仅应用于 G 自档案在案条目，不申报为新轴）；**R33-A…R34-J 空枚举 / R35-A…R35-F 空枚举**（方法卫生记录，不重演账本分类、不重提「我又查了一遍重开条件 / 门扉 / 未点名站点」）；**R34-G §3.0-1 换名记录**（`validateJoin` 逐 required-ref `edges.some` 线扫——派单明令不重提，本轮读码复核原形在位即止，flowchart.ts:334）；R31-B 六停驻门（cascade 升级臂证据 × 失败类格维持 routing 属地停放）；R31-E/F/H/I 各属地格全额维持。FITQ / SCALEX / YMIX / KFAN / NVG / SEEDX / SHAPEK / ORDX / CALLB / OCCX / JB / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / PAIRX / CSPELL / REPRX / LINKTOP / MODEX 全部未编号未移植。
- **站位零剂量停放不重铸**：`planTaskTopology` X0-2（读码复核仍无 run loop 调用方，§2 哨兵第三十五次）；`settleSupervisedOutcome` A 属地 no-op（内部调用方 supervisor.ts:601/:711 均不带 assessment，无 assessment 即无门原形在位，§2 哨兵）。
- 未重开任何 X\* / S1-\* … S34-\* 条目。**单变量纪律**：本轮无任何计时新臂——重锚脚本与 R12-G 附录归档件**逐字节同一**（§1、附录），哨兵全部为静态 rg / 读码断言（不计时）。硬不变量零触碰：双 LCB + 双归因在位（切片侧证人：events.ts `isBehaviorDistribution` + flowchart-snapshot.ts `isOneHotDistribution` one-hot 校验原文在位）；Live = R0-equivalent（flowchart-run 的 learned 路由应用面未触碰，B 属地 model-router 未读改）；promotion proposal-first / 无 Outcome-supported 声称（Checkpoint F-PROD 仍开放，ADR-005）；无阈值变更；不改测试迁就重构；CAS / 凭据 / 数据面契约 / 公开签名不变（零 diff 天然满足）；fsync / 完整性再哈希在位（event-store 终态 fsync、checkpoint-store `handle.sync()`、pause-controller writeAtomic、episode-store 终态 fsync 全部原文在位）；fail-open 家族保持关闭（events.ts `validateEvent` 追加/重放双侧 fail-closed 原形）；S8-E-1 不去重 loadLearnedRouting（G 侧消费腿 flowchart-run.ts:712 零触碰）；catalog 行序契约（R28-B W3）与 npm POSIX `.bin` symlink 静默 no-op（R28-I）零触碰；分析不变更在飞 run；tracking 无指挥权；H/score 不写 routing PASS/FAIL。无赢家 ⇒ 零生产改动、零新仿真文件入库。不开 PR。

## 1. CPU vs I/O 重锚（三批次官方运行，nvm Node v22.22.2，seed 仅标注批次）

归档基准从 `docs/reports/sota-opt/round-12/R12-G.md` 原样提取（```ts 围栏内 536 行 → `tmp-r35g-bench.ts`，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`，与 R13-G…R34-G 附录 A 记录值**逐字节一致**；无赢家按纪律不入库，临时文件核后即删——早于任何 gate 运行，零门禁事故）。官方三批 seed = 0xa35091 / 0xa35092 / 0xa35093；本轮无表征运行需求（B3 双面轻度高侧均落 R33-G 历史带内，无孤立高点需归因）。全部在 `32c63a9` 工作树完成（两笔快进增量纯 docs，三 tip `src/` 逐字节相同 ⇒ 测量形态与提交形态位级同一，全部有效）。

| 探针 | B1 | B2 | B3 |
| --- | --- | --- | --- |
| validateFlowchart 16n join4 | 9.160 µs | 9.213 µs | 9.480 µs |
| → ×32 轮计算顶 | **0.293 ms/run** | **0.295 ms/run** | **0.303 ms/run** |
| validateFlowchart 8n join2 | 4.644 µs | 4.693 µs | 4.771 µs |
| DeterministicJudge.decide 8×8 | 0.144 µs | 0.147 µs | 0.155 µs |
| expandTaskTransition | 0.496 µs | 0.499 µs | 0.507 µs |
| compileChildrenToFlowchart 16 子 | 9.063 µs | 8.913 µs | 8.939 µs |
| validateEvent AGENT_EVENT | 0.553 µs | 0.564 µs | 0.557 µs |
| replayRun 300 events | 0.657 µs | 0.721 µs | 0.703 µs |
| validateCheckpoint N=48 含 restore | 252.3 µs | 254.4 µs | 287.9 µs |
| CheckpointStore.write 持久（N=48） | 582.6 µs | 490.6 µs | 611.4 µs |
| → ×66 写/run | 38.5 ms | 32.4 ms | 40.4 ms |
| EventStore.append（无 fsync） | 75.1 µs | 67.1 µs | 76.0 µs |
| → ×300 事件/run | 22.5 ms | 20.1 ms | 22.8 ms |
| EventStore.readAll ×96/run 中期均幅 | 33.1 ms | 34.5 ms | 36.8 ms |
| EpisodeStore.append COMPLETED（fsync） | 279.7 µs | 293.2 µs | 276.9 µs |
| settle-close 终态命令算术 | 0.474 ms | 0.479 ms | 0.474 ms |

计算顶 = **0.293 / 0.295 / 0.303 ms/run**；I/O 地板合成 = **94.1 / 87.0 / 100.0 ms/run**；支配比 **~295–330×**（R34-G ~237–322× / R33-G ~299–338× 同为两个数量级，支配结论不变）。矩阵摘要 `06cbcf92c098c8f0…` 官方 3 批 × 3 迭代 = **9 次逐位一致（第二十六轮同值；14 格配置态 × 命令类矩阵逐格 I/O 计数零变化，不铸摘要轴/ID）**；start→COMPLETED 首跑冷墙钟 24.9 / 19.6 / 24.7 ms，事件尾 [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED] 不变。

**诚实注记**：(i) 本轮计算顶 0.293–0.303 与派单默认对照带 0.292–0.301（R34-G 表征顶谱系）几乎重合，仅 B3 高沿 0.303 越出 0.002（validateFlowchart 单价 9.480 µs 落 R33-G 带 9.43–9.66 之内）；I/O 地板 87.0–100.0 vs 派单默认带 86.8–94.4：B1/B2 带内、B3 100.0 越出上沿 5.6（检查点持久写 611.4 µs 与 readAll 558.6 µs 同批同向高侧，均落 R33-G 带 90.4–104.4 之内）——判本 VM 轻度慢侧宿主刻度项（与 R35-A 单格 19.0 孤点 / R34-D 高侧偏置窗同型；R35-B/R35-E/R35-F 记录反向偏快——本波各 VM 刻度各异，结构结论均不依赖绝对刻度）；冻结 0 行 + 摘要第二十六轮恒等 ⇒ 仪器/宿主现象非切片行为变化，按派单令「host-bias windows with freeze 0 lines are not new findings」不作发现、对照带不改。(ii) 计算顶按最保守实测 0.303 ms 执行亚噪声拒收门，距 ≥10 ms 落地线仍 **33×**，两个数量级，结论完全同向。(iii) 引擎纪律：PATH 在任何计时前钉死 nvm v22.22.2，全部批次官方引擎完成，零 v22.14.0 误跑、零弃置批次、零存档事故、零门禁事故（临时基准文件在任何 gate 前删除——R34-G §1 注记 iv 教训直接吸收）。

## 2. G 档案在案零剂量停放 / 重开条件哨兵复核（全部不计时；方法应用，不铸面）

R32-A 首创的哨兵审计方法（plan-ban 重铸为面）此处按令仅作卫生性应用于 **G 自档案**的在案条目。全部哨兵为静态 rg / 读码断言，零计时臂：

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| X0-2（`planTaskTopology`） | run loop 或任何生产文件获得调用方 | `rg "planTaskTopology" src/` 除 supervisor.ts:64 定义自身外**空匹配**（第三十五次复核）⇒ **未触发** |
| `settleSupervisedOutcome` A 属地 no-op | 生产侧带 assessment 调用出现 | 调用方仅 supervisor.ts:601/:711（均不带 assessment）；函数原形 assessment 缺席即返回 ⇒ **未触发** |
| R30-G M1 格站位前提 | `startParentRun` 获得 CLI 调用方或 resume 门 | `rg "startParentRun" src/` 除 coordinator.ts:224 定义外**空匹配** ⇒ **未触发** |
| R29-G M2 格站位前提 | `startSupervisedRun` 获得 CLI 调用方 | CLI 仅动态 import `resumeSupervisedRun`（main.ts:1021，与历轮记录一致）；`startSupervisedRun` 仍无 CLI 调用方 ⇒ **未触发** |
| 摘要恒等（不铸 ID） | 14 格矩阵任一格 I/O 计数或摘要变化 | 官方三批 9 次逐位同值 `06cbcf92c098c8f0…`（第二十六轮）⇒ **未触发** |
| R31-B cascade 停放门（G 侧宿主） | child-coordinator cascade 臂获得新证据面 | 冻结 0 行 + `maybeCascadeRetry` 原形（:382 调用 / :458 定义，与 R33-G / R34-G 记录同址）读码复核 ⇒ **未触发**（routing 属地维持） |
| R31-E 编解码坐标（G 侧四位点） | G 存储腿新增编解码剂量 | 显式 utf8 位点恰 4 个与 R32-G…R34-G 记录逐一同址，零剂量 ⇒ **未触发** |
| BINDX / MODEX 计数哨兵 | 切片 `Math.*` token 数或 chmod/umask 位点变化 | `Math.*` 恰 6 token 逐一同址；chmod/umask **0** 匹配（rg exit 1）⇒ **未触发** |
| R34-G §3.0-1 换名记录原形 | `validateJoin` 内形变化 | flowchart.ts:334 `edges.some` 原文在位（冻结 0 行直证）⇒ **原形维持，按令不重提** |
| 签名/调用图类重开条件全体 | 要求 `src/` 变更 | 最强冻结 `89f0455..HEAD -- src/` **0 行**直接判空 ⇒ **全部未触发** |

## 3. 本轮换名检查与四十二文件检查表（第三十五遍枚举残余）

### 3.0 换名检查（两项新站点级记录拒列，覆盖 3 站点；其余全部为已点名站点复命中或按令不重提项）

从零重推（假装排除表不存在重读 8960 行）枚举出的候选角度经对表逐一查证（候选站点名对 R1-G…R34-G 全部 G 报告 + EXCLUSIONS 全表 rg 先例检索）：

1. **新记录（拒列）：`flowchart-run.ts` 命令入口孪生 Map 物化前奏 `childResultsMap` / `childTaskMap`**（:168–170 `new Map(Object.entries(results ?? {}))`、:172–176 逐 task `map.set`——start/resume/restore 每命令各求值一次，理论上可惰性化或与 ctx 构造融合；两站点同文件同族同形，计一条记录）：rg 全部 G 报告零先例命中，构成新站点级记录。三重拒绝——(i) **once-per-command ns–µs 拒收类**：每命令恰一次，条目数 ≤ 节点/子任务数（真实 ≤ 16），Object.entries + Map 构造合计 ns–µs 域；(ii) **计算顶内亚噪声**：命令入口前奏住在 0.293–0.303 ms/run 计算顶之内，顶本身距 ≥10 ms 落地线 33×；(iii) **零动态增量改写**：每命令恰一次求值，无重复求值可消除——惰性化/融合属纯翻搅（X0-3 邻域重构），零收益。拒列，不铸 ID。
2. **新记录（拒列）：`flowchart-run.ts` `familyForFlowNode` 内形辅助**（:671–675——`applyLearnedToNode` 链内逐节点 `analyzeTask(node.objective, mapped).family`，仅当 learned 策略在场的 start/resume 各求值一次/节点；理论上可跨节点缓存 family）：rg 零先例命中，构成新站点级记录。三重拒绝——(i) **所居外围站点即既有排除行本体**：`applyLearnedToNode` × `applyLearnedRouting` 的跨界形态 = **S8-E-3**（15–16 ns/节点已拒），内形辅助的任何重构收益被同一行封死；(ii) **once-per-run µs 拒收类**：≤16 节点 × analyzeTask µs 级 = 每命令一次的百 µs 上界，且仅 learned 策略在场才触发；(iii) **Live=R0 不变量毗邻**：learned 路由应用面为硬保持项（S8-E-1 不去重 + 属地边界），零触碰纪律优先。拒列，不铸 ID。
3. **按令不重提：`domain/flowchart.ts` `validateJoin` 逐 required-ref `edges.some` 线扫**（flowchart.ts:334）= R34-G §3.0-1 换名记录本体（计算顶探针本体整体计价 + 一次性 µs + 校验平面家族三重拒列）——派单 DO NOT REMINT 明列，本轮读码复核原形在位即止。
4. **复命中维持：`flowchart-supervisor.ts` `leaseReadyNodes` 逐节点 `computeStatus()` O(N) 重算**（每迭代全节点扫描，最坏 O(N²)）= R28-G 决策门格 + R33-G §3.2 已点名站点（计算顶内亚噪声，真实 N ≤ 16）。复命中即止。
5. **复命中维持：`supervisor/ledger.ts` `advanceLedgerRound` progress 数组 spread 全量记账**（O(rounds²) 累积形）= R33-G §3.2 已点名站点（计算顶内亚噪声；maxRounds=32 封顶 + facts 个位数规模）。复命中即止。
6. **本体复认（一行即止）：`failurePathCompletedGraph` 共享 `visiting` true-路径保留** = **S2-G-2 本体**（R2-G 链式恢复 b→a→C 反例在案；R33-G §3.0-1 独立复推导直证在档）——第三十五遍新鲜眼再次注意到同一性质，复认即止，不重演复推导叙事、不作新记录。
7. **复命中维持：`scheduler.ts` `planRound` 死形参 `_leaseDurationMs`** = R29-G 落选候选清单本体（公开导出签名硬禁 + ns 级）。不重提。

改名裁决（rename-check against baseline）：`git diff --find-renames --name-status 4efee23..HEAD -- src/run src/supervisor src/graph src/domain` **空输出**——切片自生产基线零改名、零增删文件，无改名归属需裁决。

### 3.1 `src/run/`（17 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| coordinator.ts | M0 缺省路径 300-append I/O 地板本体（排除表 G 族行）；M1 `startParentRun` = R30-G 格（站位哨兵未触发，§2）；`startReady` 的 `remaining.splice(indexOf)` 二次形 = R30-G 格内已点名调度腿（≤16 子任务 ns 域）；`toModelRoutedPayload` 路由载荷映射 = 双归因见证不可动 | 无新候选 |
| supervisor.ts | M2 裁决格全维度 = R29-G；`planTaskTopology` X0-2 停放（§2）；`settleSupervisedOutcome` A 属地 no-op（§2）；`reconstructSupervisorState` 重放腿 = RUNLIM/CKMIX 既有属主；`recordStatus` 经 `expandTaskTransition` 展开逐步 append = 事件契约面（每步一事件为审计语义，非可融合冗余） | 无新候选 |
| flowchart-run.ts | 每轮全量 readAll 放大 = S1-G-1/S2-J-11/S3-I-6/S6-G-1/S6-G-7 排除行；检查点节奏 = CKMIX；learned 路由应用 = Live=R0 不变量 + E/B 属地边界（S8-E-1 不去重，:712 消费腿零剂量；`familyForFlowNode` 内形 = §3.0-2 本轮记录拒列）；命令入口 Map 物化 = §3.0-1 本轮记录拒列；`nodeTaskId` `nodes.find` 线扫 = R31-G 门内亚噪声（once-per-approval）；pauseIfRequested = pause token 已锚 | 无新候选 |
| child-coordinator.ts | ConcurrencyGate FIFO 微结构 = 计算顶内亚噪声（R31-G 门）；重试/超时梯 = M1/M2 格已定价；cascade 升级臂 = R31-B 停放（§2 哨兵）；`pendingQuestions` getter 防御拷贝 = 契约面 | 无新候选 |
| event-store.ts | 终态 fsync = 硬不变量禁去；追加/读双侧 `validateEvent` = fail-closed 读校验契约行 | 无新候选 |
| checkpoint-store.ts | 临时文件 + rename + sync 原子写 = 持久契约；记录组成 = CKMIX；utf8 位点 = R31-E 坐标（§2） | 无新候选 |
| events.ts | EVENT_TYPES 闭表 + validateEvent = 校验平面既有属主；`routingContextFields` / `isBehaviorDistribution` = 双归因 one-hot 见证 | 无新候选 |
| replay.ts | replayRun 0.657–0.721 µs/300 事件（亚噪声）；materializeCheckpoint / validateCheckpoint = CKMIX + S8-G-2 写侧校验 | 无新候选 |
| child-grounding.ts | 包预算/`uniqueArtifacts` 去重一次性 µs 级；context packet 编译 = context 姊妹切片属地 | 无新候选 |
| child-prompt.ts | formatChildPrompt 串接已定价（R30-G [A]）；串形 = NAMESHAPE 禁 | 无新候选 |
| episode-bind.ts | settle-close 终态算术 0.474–0.479 ms 本轮带内已锚（含 `events.some` 终态扫描——聚合已定价，融合收益亚噪声）；closure 裁决 = episode 姊妹属地；`hash32` 集中化 = 既有落地 | 无新候选 |
| episode-store.ts | 队列化追加 + 终态 fsync = 硬不变量；readAll 校验 = jsonl 读契约（E 属地坐标） | 无新候选 |
| flowchart-executor.ts | 终态结果投影 = TERMCLASS 禁；薄执行器臂 = R31-G 门（真实支配成本是 executor 秒–分级真跑） | 无新候选 |
| injection.ts | 注入指令种类/校验 = R25-G 面全维度；`unexpectedKeys` 每调 Set 构造 + `parseFactValue` try/JSON.parse = 校验平面亚噪声（once-per-CLI-inject；REPRX 计数即止） | 无新候选 |
| inspection.ts | once-per-run CLI 分析腿 = 拒收类（一次性 CLI 噪声）；findChild 回退线扫（含 Array.from 物化）亚噪声 | 无新候选 |
| pause-controller.ts | writeAtomic fsync = 硬不变量；pretty-print 序列化形态 = pause.json 数据面契约；token 21.0–21.4 / 55.1–59.5 µs 带内已锚 | 无新候选 |
| scheduler.ts | LeaseRegistry / planRound / applyTaskOutcome = R29-G 格 + SCHEDWIN 禁；`_leaseDurationMs` 死形参 = R29-G 落选候选本体（§3.0-7）；`expired()` 每 lease Date.parse = 个位数 lease 规模亚噪声；expandTaskTransition 0.496–0.507 µs 亚噪声 | 无新候选 |

### 3.2 `src/supervisor/`（4 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| flowchart-supervisor.ts | `propagate()` 不动点 + edgeStatus/joinStatus = join-policy 面（R27-G）；leaseReadyNodes 逐节点 computeStatus 重算 = R28-G 格复命中（§3.0-4）；`failurePathCompletedGraph` 共享 visiting = **S2-G-2 本体**（§3.0-6 复认）；structuredClone 防御拷贝面 = R31-G 门；waiter 不变式 = 契约 | 无新候选 |
| flowchart-snapshot.ts | fail-closed 快照结构校验 = CKMIX + S8-G-2 属主（本轮 validateCheckpoint 252–288 µs 含其全量）；`isOneHotDistribution` 每决策 Set 构造 = 双归因见证内亚噪声；`snapshotValidationRouter` throw-only 桩零剂量（对 `catalog-model` 的引用 = C 属地边界零剂量，§0 R35-C 划界） | 无新候选 |
| flowchart.ts | 纯再导出垫片，零运行时；模块形 = NAMESHAPE 禁 | 无新候选 |
| ledger.ts | advanceLedgerRound 数组 spread 记账 + 双段 isDuplicateFact 线扫 = 计算顶内亚噪声复命中（§3.0-5；facts 个位数规模） | 无新候选 |

### 3.3 `src/graph/`（4 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| validate.ts | Kahn 拓扑一次性构造（16 节点 µs 级，queue.shift 微形态含于内）；确定序 = ORDX 禁 | 无新候选 |
| readiness.ts | computeReadyTasks 线扫 = R29-G 格；与 planRound 的近重复形 = 调度格属地（去重是重构非优化，零真尺度收益） | 无新候选 |
| judge.ts | DeterministicJudge 0.144–0.155 µs（亚噪声地板级）；`evidenceIds.filter(includes)` 小集合二次形 = 个位数证据规模噪声 | 无新候选 |
| compile-children.ts | compileChildrenToFlowchart 8.9–9.1 µs 一次性已锚；`--children` 编译面历轮已裁 | 无新候选 |

### 3.4 `src/domain/`（17 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| flowchart.ts | validateFlowchart 9.16–9.48 µs ×32 = 计算顶本体；`validateJoin` edges.some 内形 = R34-G §3.0-1 换名记录按令不重提（§3.0-3）；条件/join 校验数组字面量 includes = 校验平面既有属主；approval plan/reply 三段校验 = 权威面契约 | 无新候选 |
| state.ts | expandTaskTransition BFS 已定价亚噪声（全表预计算变体 = TERMCLASS 禁 + 亚噪声双拒）；转移表 = TERMCLASS 禁 | 无新候选 |
| ids.ts | 前缀 + 正则双查 + undefined 防御支 = 校验平面（VALCLASS 类 + S3-G-5 + R26-D fail-closed 族，R33-G §3.0-3 既裁）；branded 类型零运行时 | 无新候选 |
| timestamp.ts | ISO_PATTERN + Date.parse 双查 = 校验平面既有属主 | 无新候选 |
| hash.ts | hash32 集中化 = 既有落地本体（BINDX 计数即止，§0） | 无新候选 |
| task.ts / run.ts / limits.ts / project.ts / evidence.ts / episode.ts / contract.ts | 校验器族 = 校验平面既有排除行；错误串构造仅失败路径 | 无新候选 |
| status.ts / roles.ts / record.ts / errors.ts / index.ts | 闭枚举表 / type-only / 再导出 = 零剂量或亚噪声 | 无新候选 |

## 4. 候选总表

**空。** 本轮零候选：哨兵复核（§2）按定义不产出候选（不计时、不剂量）；换名检查两项新记录三重拒列、其余全部命中既有属主（§3.0）——无一是可立项新站点；四十二文件枚举（§3.1–3.4）全部命中既有 ID 本体、姊妹归档面、plan-ban 轴名或计算顶内亚噪声。本轮无任何计时新臂 ⇒ 无可定价候选面。全局支配结构不变：计算面全体封顶 0.293–0.303 ms/run，距 ≥10 ms 落地线 33×（两个数量级）；I/O 面三腿 20.1–40.4 ms 全部契约或排除表保护。

## 5. CHECKS

- 分支恰 `cursor/r35-g-runtime-thirty-fifth-pass-83a1`，基线 `32c63a9` → 快进 rebase `17febc2` → 快进 rebase `598b0f9`（两笔均纯 docs，各核对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 0 行）。
- 冻结核对：`git diff 4efee23 -- <42 文件>` 派单 tip / 两次快进 rebase 后 / 提交前均 **0 行**；`--find-renames --name-status` 同区间空输出（零改名）；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**。
- 切片清单：恰 42 文件（17+4+4+17），`wc -l` 合计 8960 行（与 R32-G / R33-G / R34-G 记录一致），全部实读。
- 重锚脚本与 R12-G 附录归档件 sha256 逐字符同一（`9d46a829…b9943`，536 行）；官方三批全部 nvm v22.22.2 / V8 12.4.254.21-node.39；零 v22.14.0 误跑零弃置（PATH 计时前钉死）；临时基准文件门禁前删除，零门禁事故。
- 摘要恒等：`06cbcf92c098c8f0…` 官方 9 次逐位一致（第二十六轮同值，不铸 ID）；事件尾不变。
- 哨兵普查（第三十五次）：X0-2 / A 属地 no-op / M1 / M2 站位、cascade 停放、编解码坐标 4 位点、MODEX 零位点、BINDX 6 token 逐一同址、R34-G §3.0-1 原形在位——全部未触发（§2）。
- 换名检查：两项新站点级记录三重拒列（命令入口孪生 Map 物化 §3.0-1 / familyForFlowNode 内形 §3.0-2——均 rg 全 G 报告零先例命中）；一项按令不重提（validateJoin edges.some = R34-G 记录本体）；复命中四项维持既有属主（R28-G 格 / R33-G 已点名 ×2 / S2-G-2 本体 / R29-G 落选清单）——零新 ID、零重开。
- 姊妹划界：R35-A/B/C/D（派单已录）+ R35-E / R35-F（两笔快进带入）六轴全为各属地物理对象，G 侧共享坐标仅 flowchart-run.ts:712 learned-routing 消费腿（S8-E-1 + Live=R0，本清单原有，零剂量）与 flowchart-snapshot.ts 的 throw-only catalog 校验桩（C 属地边界零剂量）；experiments 侧零共享（G 四目录零 experiments 导入，rg 直证）——零新轴可移植（§0）。
- 聚焦测试：run/supervisor/graph/domain + integration = **386 tests / 385 pass / 0 fail / 1 skipped**（`32c63a9` / `17febc2` / `598b0f9` 树各一次，v22.22.2，与 R31-G / R32-G / R33-G / R34-G 逐字同计数）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿三次**（三 tip 各一次）——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A / R32-G / R33-G / R34-A…R34-J / R35-A/B/C/D/E/F 逐字同计数）；退出码 0。
- 仓库改动恰本报告一个文件（`git diff cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；Live=R0-equivalent 在位；proposal-first 在位；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不变；fsync / 完整性族在位；fail-open 家族保持关闭；S8-E-1 不去重维持；分析未变更任何在飞 run；不开 PR。

## 6. 重开条件

本轮无新定价轴，不加行。X0-2、A 属地 no-op、R30-G/R29-G 站位前提、R31-B cascade 停放、R31-E 编解码坐标、CKMIX/S8-G-2、S2-G-2 等既有重开条件全额维持原文；本轮 §2 哨兵直证全部未触发。B3 双面轻度高侧（计算顶 0.303 / I/O 100.0，§1 注记 i）不构成重开触发——均落 R33-G 历史带内、冻结 0 行、摘要第二十六轮恒等 ⇒ 宿主刻度项。

## 7. 新增排除 ID

**无。** 空枚举轮不铸 S35-G-\* ID；不取绰号、不作轴名、不加 EXCLUSIONS 行。换名检查两项新站点级记录见 §3.0-1/-2（拒列），归档备后续轮次引用，无需并入 EXCLUSIONS.md。

## 附录 A：重锚基准出处与关键输出

脚本与 R12-G 附录 A 归档件**逐字节相同**：自 `docs/reports/sota-opt/round-12/R12-G.md`（```ts 围栏内 536 行）原样提取为仓库根 `tmp-r35g-bench.ts` 运行，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`（与 R13-G…R34-G 附录 A 记录值一致，逐字节可复现；无赢家按纪律不入库，临时文件核后即删——早于任何 gate 运行）。运行方式：`node node_modules/tsx/dist/cli.mjs tmp-r35g-bench.ts <seed>`（nvm Node v22.22.2），官方三批 seed = 0xa35091/92/93（seed 仅标注批次，全部数据确定性构造）。关键输出：

```
--- Node v22.22.2 / V8 12.4.254.21-node.39 (canonical) ---
validateFlowchart 16n join4: 9.160 / 9.213 / 9.480 us -> x32 = 0.293~0.303 ms/run
validateFlowchart 8n join2: 4.644 / 4.693 / 4.771 us
DeterministicJudge.decide 8x8: 0.144 / 0.147 / 0.155 us
expandTaskTransition PENDING->FAILED: 0.496 / 0.499 / 0.507 us
compileChildrenToFlowchart 16 children: 9.063 / 8.913 / 8.939 us
validateEvent AGENT_EVENT: 0.553 / 0.564 / 0.557 us
replayRun 300 events: 0.657 / 0.721 / 0.703 us
validateCheckpoint full (N=48, incl. restore): 252.3 / 254.4 / 287.9 us
CheckpointStore.write durable (N=48): 582.6 / 490.6 / 611.4 us -> x66 = 38.5 / 32.4 / 40.4 ms/run
EventStore.append AGENT_EVENT (no fsync): 75.1 / 67.1 / 76.0 us -> x300 = 22.5 / 20.1 / 22.8 ms/run
EventStore.readAll 300/100 events: 495.4~558.6 / 191.2~208.3 us -> x96 = 33.1 / 34.5 / 36.8 ms/run
pause token() ENOENT / present: 21.0~21.4 / 55.1~59.5 us
loadLearnedRouting (no snapshot): 23.4 / 23.7 / 24.5 us
EpisodeStore.readAll (2 snapshots): 55.9 / 56.6 / 58.9 us
EpisodeStore.append COMPLETED (fsync): 279.7 / 293.2 / 276.9 us
EpisodeEventStore.append (no fsync): 63.4 / 62.0 / 62.1 us
settle-close arithmetic: 0.474 / 0.479 / 0.474 ms per terminal command
matrix digest (3 proc x 3 iter): 06cbcf92c098c8f0... 九次逐位一致（14 格；与 R10-G..R34-G 同值，第二十六轮）
start->COMPLETED wall-clock: 24.9 / 19.6 / 24.7 ms; returned events tail = [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED]
```

（引擎与存档纪律注记：PATH 在任何计时前钉死 nvm v22.22.2，零 v22.14.0 弃置批次；官方三批存档自始完整，零截断零重跑；临时基准文件在任何 gate 运行前删除，三次官方 gate 均一次通过。）

---

**MORE_OPTIMA=no** —— 第三十五遍复查空枚举收口：计算顶 vs I/O 地板支配比 ~295–330× 两个数量级不变；14 格矩阵摘要第二十六轮同值；G 档案在案停放/重开哨兵全部未触发；换名检查两项新记录三重拒列、一项按令不重提、其余全部命中既有属主；42 文件逐条消除角度全部命中既有属主。无达门槛新更优解。

MORE_OPTIMA=no
BRANCH=cursor/r35-g-runtime-thirty-fifth-pass-83a1
