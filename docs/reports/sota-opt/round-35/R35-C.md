MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 35 / R35-C：离线路由切片第三十五遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 35 / R35-C（Round 1–34 同区三十四遍
  之后的第三十五遍，叠在已落地的 S7-C 之上）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `f337d21`
  （R34-J 空枚举摄取 + Round 34 收口 + Round 35 开轮 + 本 R35-A/B/C
  波派单）；工作期间独占分支 docs-only 快进**两次**：`f337d21` →
  `00a0390`（R35-B 空枚举摄取 + R35-D 派单，增量仅触
  docs/reports/sota-opt/ 下 3 文件 PROGRESS / round-35 PLAN /
  R35-B.md，185 插入 21 删除）→ `b20061b`（R35-A 空枚举摄取 +
  R35-E 派单，同样仅触 3 个 docs 文件，216 插入 20 删除）。两段
  增量对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接
  复核均**零行**；按令两次 rebase 报告分支（均纯快进零冲突）并
  各重验冻结：`git diff 183df9b -- <9 切片文件>` 在分支创建点
  （`f337d21`）、锚点测量与门禁完成后（`f337d21`）、第一次快进后
  （`00a0390`）、第二次快进后（`b20061b`）、提交前**五验均 0 行**。
  最强冻结再延一轮：`git diff 89f0455..HEAD -- src/` **完全为空**
  （R10 以来全部提交均为 docs）。`git log -1 -- src/routing/` 显示
  切片最后一次生产改动**仍为 `183df9b`「perf(routing): land S7-C
  support-only eta」**。新收口姊妹项 **R35-B 空枚举**（B 区第三十五
  遍从零重枚举，零新站点级换名记录，预算重锚 ceiling M=2 9.78–10.25
  / M=10 15.54–15.85，主机偏快）与 **R35-A 空枚举**（A 区第三十五
  遍从零重枚举 + 三项站点级换名记录拒列，预算重锚 12.6–19.0
  µs/gate，宿主抖动孤点如实注记）已按快进后 PLAN 并入非移植清单并
  完成划界（§4）。全部锚点测量在 `f337d21` 工作树完成——两段快进
  增量 `src/` 逐字节零变化 ⇒ 测量形态与提交形态位级同一，全部
  有效；聚焦套件 + `pnpm gate` 在三个 tip 各跑一次均绿（计数
  逐字同）。
- **分支:** `cursor/r35-c-offline-routing-thirty-fifth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，`wc -l` 逐文件 170/168/311/585/254/156/88/130/177 = 合计 2,039 行，与 R29-C..R34-C 普查同构；S7-C 支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开、S2-C 规范键 eta/mu 去重、S1-C bootstrap 工件复用全部现场核对在位）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，
  与 R28-C..R34-C 同引擎位级相同；`/exec-daemon` PATH 暴露的系统 Node
  v22.14.0 已显式绕开——PATH 前置 nvm bin 后 `node --version` 在每个
  官方批次前复核 v22.22.2，**零批次**落在 v22.14.0 上，无弃置批次；
  tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**第三十五组诚实枚举为空：不铸任何 S35-C-\* ID、不铸新 face 名、
不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，本轮生产代码零改动。**
对 9 个源文件逐行第三十五遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R34-C 的全部无号关闭
探针与轴，含 R31-C 的 **BINDX**、R30-C 的 REPRX、R29-C 的 PAIRX、
R28-C 的基片实例化、R27-C 的 PROBX、R19-C 的 FITQ、R31-J 的
**MODEX**，及 R32-C/R33-C/R34-C 的五项换名记录——updatePosterior
pow 站点、values.sort 数值特化、lnGamma 反射分支死码、退化结局双
`every` 融合、propensity `isFabricatedPositiveSupport` `notOneHot`
被吸收析取，按派单**均不重提**）后，本轮**从未点名站点数为零
（连续第十七遍）**——每一个执行站点都能指到既有 ID、无号探针、
零池备忘或池算术覆盖。R26-C/R27-C 关掉输入数值坐标、R28-C 关掉
执行基片坐标、R29-C 关掉工作负载组成坐标、R30-C 关掉输入表示
坐标、R31-C 关掉名字解析坐标、R32-C 独立重推元坐标系无余项、
R33-C/R34-C 复推后各加换名记录之后，本轮再次独立重推，**无任何
「从未受控 + 独立物理对象 + 真尺度」三条同时成立的剩余坐标**：
C 切片是进程内纯同步数值内核，零 fs 数据路径（MODEX / R31-E
codec 面结构性空，§4 计数直证）、零 `JSON.parse` 输入位点（REPRX
划界维持）、零 OS 边界（R31-I 审计平面正交）。本轮新增换名检查
**1 项**（§2.2：posterior `estimateForKey` 的 `samples` /
`wellSampled` 双 `weightedSampleSize` CSE——同 (config, posterior)
上同一纯函数被求值两次，可提为局部复用），裁决为已落地 B1 memo
体内一次性 ns 级站点 + 管道零通量 + 落地形态需复制阈值契约逻辑，
三重拒绝，不铸 ID。

按派单在本 VM 重锚默认预算（未拷贝历轮数字，装具为 R31-C 附录
A.1 逐字节血统，仅头注更新，§附录）：

- **生产中位带（本轮正典）：657.9–662.3 ms/报告**（四次运行
  659.0 / 657.9 / 662.3 / 662.0；正典三读数 657.9–662.3）——与
  R32-C 带 659.0–662.0 几乎重合（657.9 低出带缘 1.1 ms），显著低
  于 R34-C 对照带 682.1–682.5 与 R33-C 主机干扰窗带 670.2–708.7：
  本 VM 属战役窗口的**偏快主机**（与 R32-C 宿主同层），按派单
  「host-bias windows with freeze 0 lines are not new findings」
  判读为宿主刻度项，非切片变化。**同 VM 独立旁证钉死结构结论**：
  r7c 回归资产锚点两次通过 **production 663.3 / 657.8 ms =
  1.12× / 1.12× vs 冻结 S6-C ctl 737.0**——比率与 R28-C..R34-C
  （1.12×）逐轮不动；切片对 `183df9b` 零 diff，装具协议逐字节同
  R31-C..R34-C。按 R14-C 处置规则随读重锚为本轮带。
- **small 带 30.0–30.3 ms**（R34-C 30.2–30.5 交叠 30.2–30.3、
  R32-C 30.0–30.2 交叠 30.0–30.2）；整条 small 报告低于 ±35 ms
  噪声带（R7-I 矩阵义务：该档只作确定性与保真见证，不作候选
  判据）。
- **APCX / FLOORX / 组合天花板**：APCX 无计时器独立道正典
  59.7 / 62.9 / 58.7 ms（R33-C 正典带 63.3–68.9 带缘相接、R32-C
  带 60.2–61.7 交叠 60.2–61.7）；FLOORX 正典 32.0 / 40.6 /
  31.6 ms（R34-C 带 31.3–37.0 交叠 31.6–37.0）⇒ **组合天花板 =
  27.7 / 22.3 / 27.0 ms < 35**——正典带 22.3–27.7 与 R32-C 正典带
  20.2–26.6 交叠（22.3–26.6）、与 R34-C 正典带 28.7–31.6 带缘
  相接；预演天花板 19.2 为本轮最低读数，按历轮惯例预演不入正典
  带；四读数全部 < 35：组合出路维持关闭。
- **sink = floorSink = 7.309 八读数全同**（4 运行 × 2 道，每运行
  内 `Object.is(apcSink, floorSink)` 断言全绿）——与
  R14-C/R16-C..R34-C 打印值逐位同值，跨轮确定性旁证成立：主机
  差异动不了任何输出位。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等；每运行
  2,768 项位级检查全绿，四运行合计 **11,072 项**。

回归资产与门禁（§5）：r1c–r7c 七脚本全绿，检查数逐一匹配锚定值
**8028 / 14420 / 14730 / 24888 / 28555 / 25483 / 6193**；聚焦
离线路由套件 59/10/59/0；`pnpm gate` 在 `f337d21`、`00a0390`、
`b20061b` 三 tip 各跑一次均 exit 0（tests 1169 / suites 78 /
pass 1168 / fail 0 / skipped 1，三次逐字同计数，与
R32-C/R33-A..R34-J/R35-A/R35-B 同计数）。

硬性不变量逐条复核（§0）：双 LCB 在位（posterior.ts L22
`LcbKind = "normal" | "beta-quantile"`，本轮 rg 复核）；双归因
在位（attribution-report.ts L18–19 `logit: fitLogitAdditive(rows),
probAdd: fitProbabilityAdditive(rows)`，现场核对）；live = R0
等价（`src/supervisor/` 对 9 切片文件导入为零，本轮 rg 复核，
R1 未接线）；推广 proposal-first、不声称 Outcome-supported
（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；
CAS/凭证/数据面合同/公开签名零触碰。仓库变更仅本报告一个文件；
无生产赢家故未提交任何临时装具（血统锚定装具指纹见附录，临时
文件归档后已删除）。空枚举与 R8-C..R34-C 同为战役允许的诚实
结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → EXCLUSIONS.md（完整表 546 行，
  含全部 S\*-C-\* 行与 S12-C-1、S13-B-1）→ PROGRESS.md →
  round-35/PLAN.md（C 段 + 具名/unnamed 全 ban 榜）→ R34-C.md
  全文（空枚举确认，无 S34-C-\*；一项换名检查——propensity
  `isFabricatedPositiveSupport` `notOneHot` 被吸收析取——属主
  记录在案，本轮不重提）→ R31-C.md 附录 A.1（装具血统提取源）
  与 R17-C §4（PMV / OSTZ / NSQRT 零池备忘属主核对，及
  「betaInterval 双 lnBetaFunction 在 S1-C-7 亚 ms 池」属主记录
  ——本轮 §2.1 表内引用）→ 9 个切片源文件全量重读 →
  `src/experiments/attribution-report.ts` 消费图现场核对 → 快进后
  补读 R35-B.md 与 R35-A.md 结论（A/B 区空枚举，与 C 零共享物理
  对象）与两版更新 round-35/PLAN.md（C 段默认对照不变，非移植
  清单先后增列 R35-B、R35-A 空枚举）。
- 基线漂移检查：本轮测量期起点 = 分支创建点 = `f337d21`；锚点
  测量与门禁完成后 fetch 发现第一次 docs-only 快进（`00a0390`
  R35-B 合入 + R35-D 派单），提交前 fetch 发现第二次 docs-only
  快进（`b20061b` R35-A 合入 + R35-E 派单）——每次均以
  `git diff --stat` + 对 `src test scripts package.json
  pnpm-lock.yaml` 的直接 diff = **0 行**逐项核对后按派单规程
  rebase 报告分支（两次均纯快进零冲突）并重验冻结。冻结主张五次
  核验均 **0 行**（切片 9 文件 vs `183df9b`：分支创建时、锚点
  测量与门禁完成后、两次快进后、提交前）；最强冻结
  `89f0455..HEAD -- src/` 为空。测量在字节相同的切片上完成，
  全部有效。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：
  input object-representation / **REPRX**（切片 `JSON.parse` 输入
  位点 0，§4 计数）/ **BINDX**（R31-C；realm 级绑定态——本轮零
  patch、零剂量、零计时新臂；C1 capture-hoist 不重提；划界计数见
  §4）/ **MODEX**（R31-J——inode 权限模式字拓扑，J 区持久工件面；
  本切片数据路径零 fs 位点，面结构性空，§4 计数后声明即止）/
  **PAIRX**（工作负载组成——本轮全部测量单内核恒定，姊妹估计器
  零计时）/ KFAN / COTARG / PROBX / DELINV / PRICEX / VALCLASS /
  **R34-C 换名记录**（propensity `isFabricatedPositiveSupport`
  `notOneHot` 被吸收析取，按派单不重提、不重铸——本轮逐文件表
  仅按属主引用）/ **R33-C 两项换名记录**（lnGamma 反射分支死码
  消除与 fitLogitAdditive 头部退化双 `every` 融合，不重提）/
  **R32-C 两项换名记录**（updatePosterior pow 站点、values.sort
  数值特化，不重提）/ **R35-B 空枚举**（快进带入——B 区两残余
  角度均为 B 文件已点名站点复命中，与 C 零共享物理对象，声明
  即止）/ **R35-A 空枚举**（快进带入——A 区三项换名检查全在
  tracking 面享元 / ASCII 卫镜像 / 算术微观族，与 C 零共享物理
  对象，声明即止）/ **R33-A..R33-J、R34-A..R34-J 空枚举**
  （二十切片方法卫生记录，与 C 的携带由 0 行冻结 + 锚点保真
  电池 + 本轮逐文件重读覆盖，不重演账本分类、不重走闭合稳定性
  审计、不重铸「再查一遍重开条件 / 闭合稳定性 / 门扉」式新面）/
  **R32-A 重开条件哨兵审计**（方法不重铸——C 档案在案重开条件均
  要求 src/ 变更或规模增长，由 0 行冻结与锚点保真电池自然覆盖）/
  **R32-B 闭合稳定性审计**（不重走）/ R31-I CLI 进程边界门扉闭合
  审计（本切片零 argv/env/stdin/stdout/子进程/信号/退出腿，审计
  平面正交零交集）/ digest / sink=7.309 位等不另铸轴（本轮 sink
  八读数只作既有跨轮见证复现）。FITQ / NAMESHAPE / SCALEX /
  XPROC / YMIX / SCHEDWIN / NVG / SEEDX / TERMCLASS / SHAPEK /
  ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / CG / AGEX / RTL /
  RVL / SIDEC / PWL / ZREP / CSPELL / LINKTOP 全部未编号未移植。
  R28-C 基片实例化未移植（全部测量锁死单一基片 tsx+src）；R30-I
  时间段拓扑、R30-J LINKTOP、R31-A/B/D/F/G/H 停驻门与属地格未开。
  X\* / S1-\* … S33-\* 全部未重开；S12-C-1 重开条件未触发（重开
  条件：无）；S6-C-3/S6-C-4 重开条件（支撑分布大改 / 因子数增长
  使支撑上界 ≥ ~8）经锚点夹具形状见证未触发（支撑上界仍 5）；
  S13-B-1 仍为 loser ID（B 区属地，未触）；S7-F-1 ≠ S6-F-5、
  S5-H-1 保持、S8-I-1 file-level 封死、S9-D-4 / S12-D-1 永不再开、
  S8-E-1 禁止去重（均他区属地，未触）；catalog 行序契约决策承载
  （R28-B W3，B 区属地，未触）；未起 S7-C 平行实现（X2-1 引理
  前提复核成立：0/1 设计、+0.0 起点、有限 beta——FLOORX 道逐名值
  对 APCX 道 `Object.is` 断言全绿即为可执行见证）。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
  不开 PR。输家脚本不入库（本轮无输家道——除血统锚定装具外零
  计时新臂）。

## 1. 生产中位与锚点复核（本 VM 实测，未拷贝历轮数字）

测量装具与 R31-C 附录 A.1 逐字节血统（replica 保真电池 34 例 +
自决定性 + 中位重锚 + APCX/FLOORX；仅头注更新为 R35-C，diff 即
6 行注释，指纹见附录）；Node v22.22.2，tsx + src 直连（R28-C S
臂 = 历史正典基片）。1 次预演 + 3 次正典独立进程，全部在
`f337d21` 工作树完成：

| 运行 | perf 中位 (ms) | small 中位 (ms) | APCX (ms) | FLOORX (ms) | 组合天花板 (ms) | sink / floorSink | 检查 |
|---|---|---|---|---|---|---|---|
| 预演 | 659.0 | 30.0 | 59.4 | 40.2 | 19.2 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 1 | 657.9 | 30.0 | 59.7 | 32.0 | 27.7 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 2 | 662.3 | 30.3 | 62.9 | 40.6 | 22.3 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 3 | 662.0 | 30.0 | 58.7 | 31.6 | 27.0 | 7.309 / 7.309 | 2,768 ✓ |

perf 逐 rep（7 rep/运行）：预演 662.0 663.3 659.0 657.9 660.2
657.5 658.8；正典 1 657.9 660.2 657.3 656.5 658.6 657.0 658.5；
正典 2 664.6 662.6 662.7 661.9 658.1 660.5 662.3；正典 3 662.0
671.2 660.5 661.0 674.2 663.1 660.3。

- **生产中位带（本轮正典）：657.9–662.3 ms**。与 R32-C 带
  659.0–662.0 几乎重合，低于 R34-C 对照带 682.1–682.5 约 20 ms、
  低于 R33-C 主机干扰窗带 670.2–708.7——本 VM 属战役窗口的偏快
  主机（与 R32-C 宿主同层；正典逐 rep 散布 656.5–674.2，层内
  紧致），是 0 行冻结下的宿主刻度项，非切片变化，亦**不铸主机
  偏置新发现**（派单明令）。三重独立旁证钉死「非切片变化」：
  (i) 切片对 `183df9b` 逐字节零 diff；(ii) r7c 回归资产（§5）
  同 VM 两次通过打印 production 663.3 / 657.8 ms = **1.12× /
  1.12×** vs 冻结 S6-C ctl 737.0——与 R28-C..R34-C 逐轮同比率；
  (iii) sink 八读数逐位同历史值（输出位不动）。按 R14-C 处置
  规则**随读重锚为本轮带 657.9–662.3**，结构结论（层级、比率、
  天花板、确定性）全部与历轮同构。
- **small 带 30.0–30.3 ms**（R34-C 30.2–30.5 交叠、R32-C
  30.0–30.2 交叠）；整条 small 报告低于 ±35 ms 噪声带，只作
  确定性与保真见证。
- **APCX 正典带 58.7–62.9**（R32-C 60.2–61.7 交叠 60.2–61.7、
  R34-C 60.1–68.6 交叠 60.1–62.9）；**FLOORX 正典带 31.6–40.6**
  （R34-C 31.3–37.0 交叠 31.6–37.0、R33-C 37.2–40.4 交叠
  37.2–40.4）；**组合天花板正典带 22.3–27.7 < 35**（R32-C
  20.2–26.6 交叠 22.3–26.6、R34-C 28.7–31.6 带缘相接；预演 19.2
  为本轮最低、不入正典带——APCX 与 FLOORX 同 VM 同窗对减，四读数
  差值仍全数 < 35，组合出路维持关闭）。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等（含
  replica 保真电池 34 例 + perf + small 对生产 import 的全字段
  断言），每运行 2,768 项检查全绿，四运行合计 11,072 项。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is(apcSink, floorSink)`）——与 R14-C/R16-C..R34-C
  打印值同值；X2-1 引理前提（0/1 设计、+0.0 起点、有限 beta）
  由 FLOORX 道逐名 `Object.is` 断言随运行复核成立。

## 2. 第三十五遍枚举（诚实结果：空；零新站点 + 一项新换名检查）

### 2.1 逐文件重读与裁决（零未点名站点，连续第十七遍）

| 文件 | 本轮重读检查（全部命中既有 ID / 无号探针 / 零池） | 结论 |
|---|---|---|
| `r1.ts`（170 行） | 每调用 `modelsById` Map 构造、config spread、estimates spread、sampled/aboveFloor 双 filter、cheapest reduce = S1-C-5/S1-C-10 本体；costOf/tierIndex/`aboveFloor.find(previousModelId)` 线性 find = X1-4 本体（M≤10 噪声级）；prepared 观测索引 = 已落地 A2/B1；跨 episode 记忆化 = X1-6；`tierIds` spread 与 `lcb.toFixed(4)` 返回路径格式化 = S1-C-10 | 无新候选 |
| `r1-shadow-report.ts`（168 行） | 每 episode request spread + routeR0 调用（B 区文件不触碰）；merge 路径 = S1-C-2（跨索引播种）+ S1-C-3（公开 ReadonlyMap 形状）；selectedCost find = X1-4 族 + PRICEX 封顶；与 shadow-compare 合并 = X1-5；sanitizeClaims once-per-report；空 episodes / 零 records 故障腿 = R18-C once-per-fault 拒绝类 | 无新候选 |
| `posterior.ts`（311 行） | 80 次二分 / Newton / 闭式 = X1-3；lnBeta 每 quantile 一次 = 已落地 C1；estimateFingerprint = S1-C-4；groupObservationsByKey = 已落地；betacf 守卫 Math.abs = BINDX 坐标（计数即止）；updatePosterior pow 站点 = R32-C 换名记录 1 属主（不重提）；`lnGamma` 反射分支死码 = R33-C 换名记录 1 属主（不重提）；`oneSidedTail(_z)` 死形参 = R17-C OSTZ 零池备忘属主；posteriorMean/Variance `total>0` 卫 = S8-C-4 fail-closed 数值卫家族；regularizedIncompleteBeta 前缀重算 = X1-3 冻结数值路径本体；`estimateForKey` 缺键 `?? []` 分配 = 落地 memo 体内 once-per-(key,fingerprint) 分配级零池；`samples`/`wellSampled` 双 `weightedSampleSize` = **本轮换名记录 1**（§2.2）；nObsEff→weightedSampleSize 函数分层 = 同站点同裁决 | 无新候选 |
| `offline-logit.ts`（585 行） | S1-C（bootstrap 工件复用）/S2-C（规范键 eta/mu 去重）/S3-C（单位乘法消除）/S6-C（switch 直线化）/S7-C（支撑升序直加）落地形态逐站点在位；头部退化结局双 `every` 融合 = R33-C 换名记录 2 属主（不重提）；draw 侧塌缩检查融合 = S2-C-5 本体；`beta.every(Number.isFinite)` = S7-C-2/S8-C-3；delta map+reduce = S2-C-3；per-key w / per-(key,y) z 去重 = S3-C-2（本轮复核 `w=max(mu(1-mu),1e-10)` 仍为 per-key 纯函数，属主不动）；`xtwx fill(0)` = S5-C-7；dot 展开 = S5-C-5（串行依赖）；onProbabilitiesFor = S7-C-4/S8-C-1；APC off 道全系 = S1-C-1/S2-C-2/S4-C-4/S8-C-2/S10-C-2（本轮组合天花板 22.3–27.7 < 35 维持关闭）；percentile 换 quickselect = X4-3 族；draws 循环 `.entries()` = S7-C-1；draws 预分配/pushValue = S1-C/S3-C-3 分配级；`values.sort` 数值特化 = R32-C 换名记录 2 属主（不重提）；buildDesign/computeSupports/canonicalRowKeys/columnIndex 元组物化 once-per-report 构建 = S1-C 落地后残余亚带池（R12-C 亚带分类）；诊断尾 `interactionsFor` 嵌套 filter、`e.name.slice` 闭包内重算、mean reduce、双 Set 基数 = 尾部 once-per-report 亚噪声（R33-C/R34-C 表同款）；IRLS 收敛范数 sqrt = R17-C NSQRT 关闭探针；采样环 `Math.floor(random()*n)` = 冻结 RNG 契约（mulberry32 逐位再现义务）；worker/WASM = S9-C-1/S9-C-2；嵌套序交换 = S12-C-1（重开条件：无）；per-fit distinct-key 代表表 = S10-C-1；stamp Int32Array 换窄宽度 = S7-C-3 零效应家族 | 无新候选 |
| `offline-prob-add.ts`（254 行） | X3-4 + R17-C 零池；cell 双算合一 = S1-C-6；站点规范键去重 = S2-C-1；betaInterval 记忆化 = S1-C-7；betaInterval 上分位第二次 80 二分 = X1-3 本体 + S1-C-7 亚 ms 池；betaInterval lcb/ucb 跨分位双 `lnBetaFunction` = **R17-C/R18-C 已记属主**（「betaInterval 双 lnBetaFunction 在 S1-C-7 亚 ms 池」——本轮独立重发现后核对属主在案，不立新记录）；kappaS 输入 `[...values(),...values()].map(cell)` 物化 = S1-C-6 邻域 once-per-report 零池；死 `pM`/`pMp` shrink（`void` 保留）= R17-C PMV 零池备忘属主（冻结公式文档，行为一位不动）；diagnose 内 `[...entries()]` 每效应重物化 = once-per-report 零池覆盖；`lastSegment` 死首参 = R33-C 表已记纯文本项（零池，不立记录） | 无新候选 |
| `propensity.ts`（156 行） | 三遍改单遍 = X3-5 + S1-C-8 本体（weights 双 reduce 融合同属）；`isFabricatedPositiveSupport` `notOneHot` 被吸收析取 = R34-C 换名记录属主（按派单不重提）；behaviorMu/targetPi 访问器 = R27-C PROBX 冻结坐标（JIT 平凡内联）；validate 故障腿 = once-per-fault REJX 类（R18-C）；R21-C 零池维持 + R27-C 通量普查（生产调用 0 次/报告）维持 | 无新候选 |
| `lin-alg.ts`（88 行） | S4-C（行引用提升）/S5-C（按四展开）落地形态在位；变体空间 = S4-C-1..3 + S5-C-1..7 + S10-C-3（对称 Schur：部分主元破坏对称）+ S12-C-1 封闭；`stop=n-3` 提升 / `a.some` 手写 = S6-C-7；回代 eps 卫 = S8-C-4（fail-closed 数值卫）；回代串行链 = S5-C-5 | 无新候选 |
| `bandit.ts`（130 行） | recordReward/recordExploration 拷贝追加 = X4-2 readonly 契约；`arms.includes` / meanReward 线性 = S1-C-9（arms 个位数）；selectArm 贪心循环 arm-0 重评 = S1-C-9 本体 ns 级；探索臂 `?? state.arms[0]` 恒不触发回退 = S8-C-4 fail-closed 卫家族（index 恒在界内，卫零成本保留）；createBanditState `[...arms]` 防御拷贝 = 契约本体；validateTaskFeatures includes = S1-C-9 族 | 无新候选 |
| `shadow.ts`（177 行） | decisions 追加拷贝 = X4-2 本体；halted 分支整 decision 字面量重建 / breached once-per-fault 路径 = R16-C/R18-C 拒绝类；每 step config 字面量 = S1-C-9 本体；roundUsd/预算扣减 µs 域 = S1-C-9 族 | 无新候选 |

结构位置 × 消除机制满矩阵（死码/常量折叠/CSE/记忆化/数据结构/
融合/早退/免分配/复杂度类/强度削减/惰性/平行路径/形状单态化）
逐格过筛：非空格全部落在上表既有 ID、既有换名记录属主或 §2.2
一项新记录上。

### 2.2 本轮新换名检查记录（裁决后拒列，不铸 ID）

1. **posterior `estimateForKey` 的 `samples` / `wellSampled` 双
   `weightedSampleSize` CSE**（posterior.ts L294–L302：估计构造体
   同时写 `samples: weightedSampleSize(config, posterior)` 与
   `wellSampled: isWellSampled(config, posterior)`，而
   `isWellSampled → nObsEff → weightedSampleSize` 在同一
   (config, posterior) 上把 `alpha + beta - priorAlpha -
   priorBeta` 求值**第二次**——提为局部 `const samples = …` 后
   两字段复用一值，纯函数同参 IEEE 确定性下逐位同值，零可观察
   副作用差）：等价成立，但三重拒绝——
   (i) **站点在落地 memo 体内**：该双算仅发生在 estimateCache
   miss 上，由已落地 B1 per-key 估计备忘录限定为每
   (key, fingerprint) 恰一次；重复功仅 2 次加法 + 2 次减法 +
   1 次调用分派，个位 ns 级。
   (ii) **管道零通量**：`estimateForKey` 不被
   fitLogitAdditive / fitProbabilityAdditive 调用（±35 ms 带
   所在的 perf 管道触不到该站点）；其生产通量在 shadow 道
   routeR1（每 (tier 模型, episode) 至多一次 miss）。R25-C
   AGEX 剂量装具的相位分解显示该工作负载（160 键 × 480
   episode）由 lcb-quantile ~1.5 ms 支配，本 CSE 上界 ≤160
   miss × 个位 ns ≈ 亚 µs/工作负载，深度低于派单 µs/ns 拒绝线。
   (iii) **落地形态自损**：`wellSampled` 若改写为
   `samples >= config.minSamples` 内联，即把阈值规则从其命名
   属主 `isWellSampled`（公开导出）复制出第二份契约逻辑
   （X1-2 平行实现邻域 + S7-B-1 意图护栏同类）；若保留调用则
   收益恒零。µs/ns 派单拒绝类 + B1 属地 + 契约分层，拒列。

### 2.3 枚举收口

除 §2.2 一项外，第三十五遍逐文件重读**零未点名站点**（含一处
独立重发现——prob-add betaInterval 跨分位双 lnBetaFunction——
核对后确认 R17-C/R18-C 属主在案，按属主引用不立新记录）。R12-C
四分类（重命名 / 亚带池 / 逐位违例 / 结构交换负优化）+ R18-C
once-per-fault 拒绝类第三十五遍无余项。**第三十五组枚举为空——
比强铸败者 ID 更诚实。**

## 3. 三大池地板与组合出路复核

- **APCX 池**（59 非截距名 × 400 行 APC 扫掠 × 201 fit）：无
  计时器独立道 0.296 / 0.297 / 0.313 / 0.292 ms/fit ⇒ 59.4 /
  59.7 / 62.9 / 58.7 ms/报告——正典带 58.7–62.9，与 R32-C 带
  60.2–61.7 交叠、R34-C 带 60.1–68.6 交叠（主机窗口判读见 §1）。
- **FLOORX 池**（S8-C-2 支撑升序 off-sum 形态，从未落地——X2-1
  引理前提本轮复核成立；逐名值对 APCX 道 `Object.is` 断言全绿）：
  0.200 / 0.159 / 0.202 / 0.157 ms/fit ⇒ 40.2 / 32.0 / 40.6 /
  31.6 ms/报告地板。
- **组合天花板 = APCX − FLOORX = 19.2 / 27.7 / 22.3 / 27.0 ms
  < 35**——正典带 22.3–27.7 与 R32-C 正典带 20.2–26.6 交叠、与
  R34-C 正典带 28.7–31.6 带缘相接；预演 19.2 为本轮最低读数，
  按历轮惯例预演不入正典带；四读数全部 < 35：组合出路维持关闭。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is`）——与 R14-C/R16-C..R34-C 打印值同值，派单强制的
  跨轮确定性旁证成立。
- 零池备忘维持：posterior/propensity/bandit/shadow 四文件全部
  站点在既有 ID、无号探针（含 R17-C PMV/OSTZ/NSQRT 与
  betaInterval 双 lnBetaFunction 记录）覆盖下，本轮重读新增一项
  换名记录（§2.2）后无余项。

## 4. 姊妹面划界（计数即止，零剂量零计时）

1. **R35-B 空枚举（本轮快进带入）**——B 区第三十五遍从零重枚举
   （10 文件 1359 行，基线 `94ed3d9` 三点冻结 0 行）+ 零新站点级
   换名记录（枚举尾部两残余角度均为 B 文件已点名站点复命中）+
   预算重锚 ceiling M=2 9.78–10.25 / M=10 15.54–15.85 ms/eval
   （主机偏快）。与 C 切片零共享物理对象（其 10 文件全在 live
   路由 / model-router 面；`routeR0` 是 B 区文件，本切片
   r1-shadow-report 仅消费其公开导出，双方均零改动）。声明即止，
   不移植、不重做、不铸新轴。
2. **R35-A 空枚举（本轮第二次快进带入）**——A 区第三十五遍从零
   重枚举（14 文件 1796 行，基线 `7acb666` 三验 0 行）+ 三项
   站点级换名记录拒列不铸 ID（human-score/types `unobserved`
   字面量五站点享元化 / types `asString` 可打印 ASCII 首字符卫
   镜像 / prescore dip 乘后判改判后乘——全部落 S1-A-7 对象身份 /
   S7-F-2 镜像 / 算术微观族）+ 预算重锚 12.6–19.0 µs/gate（宿主
   抖动孤点如实注记）。与 C 切片零共享物理对象（其 14 文件全在
   tracking / gate 面，与本切片零交集）。声明即止，不移植、
   不重做、不铸新轴。
3. **R34-A..R34-J 空枚举（Round 34 十切片收口）**——全部为方法
   卫生 / 空枚举记录，未铸新面。R34-C 一项换名检查（propensity
   被吸收析取）本轮按属主引用（§2.1 propensity 行），不重提、
   不重做、不铸新轴。R33-A..R33-J 同理。声明即止。
4. **BINDX（R31-C）**——切片环境内建 token 静态普查（划界计数，
   非剂量）：`Math.*` 恰 **42 token**，其中函数内建 12 个
   （abs 9 / max 7 / log 6 / min 3 / floor 3 / exp 3 / sqrt 2 /
   imul 2 / sin 1 / round 1 / pow 1 / ceil 1）+ 值常量
   `Math.PI` ×3；`Number.isFinite` 恰 **2 站点**（offline-logit
   L339 `beta.every` + lin-alg L85 终检）——与 R31-C..R34-C
   十二内建 + 两站点普查逐名同构，realm 坐标未动、零 patch、
   零计时。C1 capture-hoist 不重提。
5. **REPRX（R30-C）**——切片 `JSON.parse` 输入位点 **0**（全部
   输入经内存对象抵达）——划界维持，声明即止。
6. **MODEX（R31-J）**——9 文件对 `node:fs / readFile / writeFile /
   chmod / umask / mkdir / appendFile` 命中 **0**——切片数据路径
   零持久工件、零铸盘动词，面在 C **结构性空**（R31-E codec 面
   同一计数同一结论）。声明即止。
7. **PAIRX（R29-C）**——本轮全部测量单内核恒定（perf/small 两
   夹具即历轮锚定形状），姊妹估计器零计时，工作负载组成坐标
   未动。声明即止。
8. **R31-I CLI 进程边界门扉闭合审计**——本切片零 OS 进程边界
   腿（无 argv/env/stdin/stdout/spawn/signal/exit），审计平面
   正交零交集，不移植。

## 5. 测试与验证

```
# 聚焦离线路由套件（tsx --test，7 文件）
# 在 f337d21 / 00a0390 / b20061b 三 tip 各跑一次：
#   tests 59 / suites 10 / pass 59 / fail 0  （三次逐字同计数，
#   与 R31-C..R34-C 同计数）

# 全门禁 pnpm gate = typecheck + lint + test + build
# 在 f337d21 / 00a0390 / b20061b 三 tip 各跑一次，均 exit 0：
#   tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#   （三次逐字同计数，与 R32-C/R33-A..R34-J/R35-A/R35-B 同计数；
#    tsc build 零诊断）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门；
# f337d21 工作树，nvm Node v22.22.2）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项（design.build 358,006/拟合）
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项（eta dots 3,586,400 → 1,249,652 去重）
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项（production 666.0 ms = 1.47× vs S2-C ref 978.3）
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点（两次通过，检查数两次同为 6,193）：
#   production 663.3 ms = 1.12× / 657.8 ms = 1.12× vs 冻结 S6-C
#   ctl 737.0（完整块）。与 §1 生产正典中位带互证：比率与
#   R28-C..R34-C（1.12×）逐轮不动，绝对刻度归宿主（本 VM 偏快，
#   ctl 737.0 亦低于 R34-C 宿主的 771.4，同向缩放）。

# 本轮测量装具（临时脚本，未入库——无生产赢家；来源与指纹见附录）
# 主锚 ×4：11,072 项位级检查全绿（2,768 × 4）；
#          sink/floorSink 8/8 = 7.309（进程内 Object.is）
```

冻结五验（§0）；仓库变更 = 本报告一个文件（提交前
`git diff origin/cursor/sota-persistent-opt-83a1...HEAD --stat`
复核仅 `docs/reports/sota-opt/round-35/R35-C.md`）。无生产落地
（派单条件句未触发），未铸新仿真脚本，临时装具已从 /tmp 删除。

## 6. 新增排除 ID：无

- 不铸 S35-C-\*：第三十五组枚举为空；§2.2 一项换名检查落 B1
  落地 memo 属地 + 管道零通量 + 契约分层三重拒绝，不越 ±35 ms
  带，无任何站点达标。
- 不写入 EXCLUSIONS.md 任何行；不铸新 face 名；不取 ALLCAPS
  绰号（含对 MODEX / BINDX / REPRX / PAIRX 的一切再编号）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态，未重开 S12-C-1
  （重开条件：无），未起 S7-C 平行实现，未重铸 R32-A 哨兵审计
  方法，未重演 R33-A 账本分类、未重走 R32-B 闭合稳定性审计，
  未重提 R32-C/R33-C/R34-C 五项换名记录。

## 附录：测量装具来源与指纹（未入库；运行后已删除）

- 装具：`/tmp/r35c/r35c-anchor.mts`，**814 行**，由本仓
  `docs/reports/sota-opt/round-31/R31-C.md` 附录 A.1 代码块
  （L555–L1368）逐字节提取，唯一改动为头部块注释 6 行更新为
  R35-C 说明（协议体、夹具、seed、断言零改动——R31-C A.1 本身
  即 R30-C/R14-C 血统逐字节协议；提取后对原文 diff 复核恰 6 行
  注释：L2–L5 轮次说明 + L29–L30 零新计时臂声明）。
- sha256（归档时点）：
  `0eb247b1d69b99f8b7bc7c5d66989ec288583a29cd075d3afba2c81839322032`
- 运行方式（仓库根，nvm Node v22.22.2 + tsx 4.23.12，
  `pnpm install --frozen-lockfile` 后，PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 绕开系统 Node v22.14.0）：

```bash
node_modules/.bin/tsx /tmp/r35c/r35c-anchor.mts   # ×4（1 预演 + 3 正典，独立进程）
```

- 输出摘要（四运行）：§1 表格与逐 rep 行为全量记录；每运行尾行
  `ALL EQUIVALENCE CHECKS PASSED (2768 bitwise checks)`。
- 本轮**零新增计时臂**：无绑定/表示/组成/基片/模式剂量，无决斗
  道，无输家脚本——附录无新增源码块（装具全文已在 R31-C 附录
  A.1 归档，本轮仅头注差异，不重复占档）。

MORE_OPTIMA=no
BRANCH=cursor/r35-c-offline-routing-thirty-fifth-pass-83a1
