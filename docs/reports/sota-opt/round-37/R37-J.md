MODEL_SLUG=claude-fable-5-thinking-xhigh

# R37-J：persist/cluster/privacy/preferences/episode/track/context/feedback 切片 Round 37 三十七搜报告

- **战役:** 全库持久 SOTA 优化 Round 37 / R37-J（R1-J…R36-J 同切片三十六遍之后的第三十七遍独立枚举）
- **基线:** 分支创建于独占分支 `cursor/sota-persistent-opt-83a1` @ 派单 tip `8e86fe2`（R37-G 摄取——空枚举——+ R37-J 派单）。测量与门禁全部完成后独占分支一次**纯 docs 快进** `8e86fe2` → `f83bce5`（R37-H/R37-I 摄取——双空枚举；`git diff 8e86fe2..f83bce5 -- src/ test/ package.json pnpm-lock.yaml` = **0 行**直证），已 `git merge --ff-only` 对齐并重验冻结全 0 行；重锚三遍、J1 sim、聚焦套件与 `pnpm gate` 全部在 `8e86fe2` 工作树完成，src/ 与 `f83bce5` **位级同一** ⇒ 全部结果位级同一有效。按令未等待在飞姊妹、未触碰姊妹分支（H/I 落地为快进摄取件，全文实读折入 §0.2）。
- **分支:** `cursor/r37-j-persist-thirty-seventh-pass-83a1`
- **切片:** 恰 **29** 文件——cluster 3（mailbox / spawn / host）+ privacy 3（deletion / state-layout / record-classes）+ preferences 7（loop-eval / export / precedence / materialize / service / store / types）+ episode 5（manager / replay / events / store / closure）+ persist 2（file-lock / jsonl）+ track 4（primary-split / plan / clarify / loop）+ context 2（index / packet）+ feedback 3（redaction / store / types）；`wc -l` 合计 **3655** 行（29 文件逐一实测，与 R36-J 记录同数，§0.1），全部从零实读全文，未依赖前三十六轮记忆。
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R24-J…R36-J 同引擎位级相同，带可比；计时前 `which node` / `node --version` / `process.versions.v8` 三项直证）。本 VM 系统默认 node 为 v22.14.0——按在案陷阱**开工即**显式钉 PATH 到 `~/.nvm/versions/node/v22.22.2/bin`，官方三遍、J1 sim、聚焦套件与 gate 全部落在 v22.22.2 上，**零** v22.14.0 误跑、零因引擎弃置批次（烟测一遍按令弃置，§1.1）。`pnpm install --frozen-lockfile` 干净完成，锁文件 0 行差分。

## 结论

**诚实的空枚举：第三十七遍没有可立 ID 的新角度，不铸任何 S37-J-\* ID，不铸新 face 名，不取 ALLCAPS 绰号，不写入 EXCLUSIONS 表行，生产代码零改动，切片维持关闭（MORE_OPTIMA=no）。** 本轮按派单纪律收口 J 区：从零重读 29 个切片文件（3655 行，§0.1）、在本 VM 以 R12-J 附录 A 同源 harness 重锚配置态 I/O 地板矩阵九格 ×3 遍（烟测弃置，§1.1）、重跑 J1 等价 sim 全绿（**2468 项逐位检查**，§1.2）、对每个「看起来新」的残余位点做换名拒绝检查（§2.2）。结果是**零新站点级换名记录**——本轮从零重推出的全部候选角度经查证均为既有 ID / 既有记录 / 姊妹属地的复命中：R36-J §2.2 两组处置按令逐一确认原裁决在位、不重铸（十二项重赛角度 + 三处「看起来新」角度全体复命中即止，§2.2 第一组）；本轮重读另核对的三处「看起来新」角度（`persist/jsonl.ts` 每 append `mkdir` 自愈、`context/packet.ts` `contractDigest`+`collapseFacts` 每编译份额、`persist/file-lock.ts` owner-token 回读三段式）全部落在既往轮裁决 / 既有家族价签之内（§2.2 第二组），无一达到「从未在任何 J 报告点名」的新站点门槛。重锚九格全部与历史带交叠（fsync=true 快缘 −5.1% 与 saveToDisk 50 慢缘 +3.9% 为对称双向窗样，非同向齐移非结构迁移，§1.1 判读；R35-J 型 399–801 极端慢窗连续第二轮未再现）。空枚举与 R32-J…R36-J、R37-A…R37-I 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 29 文件自 J1 落地（`fb41417`）**逐字节零变化**——建支时、测量后与提交前**三点**核对 `git diff fb41417 -- <29 文件>` 均 **0 行**；`git log fb41417..HEAD -- <切片八目录>` **零提交**（最后触碰切片的提交仍为 `fb41417` J1 本体）；最强冻结 `git diff 89f0455..HEAD -- src/` = **0 行**（R10 以来 docs-only；本轮快进段 `8e86fe2..f83bce5` 亦纯 docs）⇒ 生产调用图对本切片**可证不变（最强形式，第二十九连）**。`wc -l` 合计 **3655**（29 文件逐一实测，§0.1）。重锚九格与历史带关系（§1.1）：九格全部与 R34-J/R35-J/R36-J 带交叠或全含；saveToDisk 500 / jsonl false / 级联双格 / context / plan 双档七格并集几乎整段落于历史共域；saveToDisk 50 慢缘 277.5（暖窗首采样）高于 R36-J 慢缘 +3.9%、jsonl fsync=true 快缘 215.5 低于 R34-J 快缘 −5.1%——一慢一快对称双向、幅度个位数百分比 = 宿主窗样本而非位点性变化；R35-J 型 399–801 极端 flush 竞争窗连续第二轮未再现。**I/O 支配判据第三十七次成立**（persist 地板数百 µs vs CPU 格 1–2.7 µs）。J1 落地代码本轮 `scripts/r1j-equivalence-sim.ts` 重跑全绿（**2468 项逐位检查，reference 10267.1 ms → current 3.5 ms = 2921.6×**——比率落派单记载 2823.6×、R31-J 2836.9×、R35-J 2948.5× 的宿主偏移带内，reference 腿 10267.1 介于 R36-J 9997.7 与 R35-J 10400.5 之间，current 腿 3.5 ms 跨轮稳定；J1 锁不重铸，fold 形状锁定原样）。聚焦切片套件 **91/91**（8 目录 4 suites，`8e86fe2` 树，src/ 与快进后 `f83bce5` 位级同一）；`pnpm gate` **exit 0**（tests **1169** / suites 78 / pass **1168** / fail **0** / skipped **1**，与 R14-E…R37-I 历史计数逐字相同）。哨兵静态普查 0 命中（§2.4）。硬性不变量逐条零触碰（§3）。仓库变更仅本报告一个文件；重锚 harness 按纪律不入库（与 R12-J 附录 A 归档件同源，除头注释与临时目录前缀 `r12j-`→`r37j-` 外逐字同源；gate 运行前已从仓库树删除，附录 A 存原始输出）。不开 PR。

## 0. 范围与约束遵守

### 0.1 切片清单与冻结（29 文件，3655 行）

`wc -l` 逐文件实测：host 200 / mailbox 91 / spawn 54 / deletion 126 / state-layout 29 / record-classes 261 / loop-eval 180 / export 65 / precedence 40 / materialize 36 / service 65 / store 288 / types 33 / manager 207 / replay 49 / events 44 / store(episode) 47 / closure 49 / file-lock 49 / jsonl 53 / primary-split 52 / plan 122 / clarify 65 / loop 279 / index 418 / packet 523 / redaction 121 / store(feedback) 85 / types(feedback) 24 = **合计 3655**（与 R36-J 逐文件计数一致）。冻结四联核对（建支时 + 测量后 + 快进对齐 `f83bce5` 后提交前）：`git diff fb41417 -- <29 文件>` = **0 行**；`git diff 89f0455..HEAD -- src/` = **0 行**；`git diff fb41417 -- pnpm-lock.yaml` = **0 行**；`git log fb41417..HEAD -- src/{cluster,privacy,preferences,episode,persist,track,context,feedback}` = **零提交**。

### 0.2 读序遵令与属地静态直证

- 读序遵令：README → EXCLUSIONS 全表（545 行）→ PROGRESS → round-37/PLAN（J 段 + 命名/未命名 ban 榜）→ R36-J 全文（默认对照）→ R37-A → R37-B → R37-C → R37-D → R37-E → R37-F → R37-G，之后才从零重读 29 切片文件并动手；快进后补读 R37-H / R37-I 全文折入本清单。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README.md 零触碰。
- **九姊妹空枚举划界折入非移植清单（零新轴可移植）**：R37-A（3 项 tracking 侧换名检查已拒——A 属地物理对象，J 侧零共享）；R37-B / R37-C / R37-D / R37-E / R37-F（各属地**零**新站点级换名记录，均未铸新面）；R37-G（compute 0.286–0.298 vs I/O 92.8–95.1 —— G 属地读数；摘要 `06cbcf92c098c8f0` 第二十八轮同值——本轮 J 侧**未产生该摘要**、未铸摘要 ID）；R37-H（H 属地零未点名站点零新记录，R35-H 八项重赛角度复命中即止——J 侧共享坐标仅 loop.ts L80 D1 门消费腿，其 §2.4 D1 普查已直证该坐标恰 1 处且形态逐字不变，J 零触碰）；R37-I（I 属地零新站点级换名记录零残余候选，S7-I-1/S8-I-1 结构维持——其 §5 哨兵已复证 S8-E-1 恰 3 生产位点含 J 侧消费腿 loop.ts:88，与本轮 §0.2 rg 直证互证；J 侧零 cli/pi-adapter 共享物理对象）。九报告均未铸新面，非移植清单无需加行。
- `queryPacketGrounding`：第三十七次 re-grep，`src/` 内唯一位点仍为 `context/packet.ts` L498 定义本体，**零生产调用方**（test-only 面维持）。
- `durableRecordClassById`：re-grep `src/` 内零 record-classes.ts 之外调用方（R12-J §1.2 起逐轮复证维持）。
- `withExclusiveFileLock` 调用方 re-grep：`pi-adapter/file-credential-store.ts`、`learning/bandit-store.ts`、`adaptation/promotion.ts`、`cli/episode.ts` ——**全部在 J 切片之外**（D/E 已计价属地），J 切片零生产调用方维持。
- **S8-E-1**：`loadLearnedRouting(` 生产位点恰 **3 个**（track/loop.ts:88、run/flowchart-run.ts:712、cli/main.ts:708，rg 直证）——J 调用方侧位点（loop.ts:88）在位核对，属地在 E，零触碰。loop.ts L80 `"user-first"` = R31-H D1 门坐标（H 属地零触碰）。
- fsync 双腿在位直证：`persist/jsonl.ts:19` `handle.sync()` + `run/event-store.ts` 终态事件 `appendJsonlLine(…, TERMINAL_EVENT_TYPES.has(…))` fsync=true 腿——零去除。

### 0.3 不移植 / 不重铸禁令逐项核对

- 命名空枚举轴（plan-ban，非 EXCLUSIONS 行）：FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX——全部未编号未移植未重提；**LINKTOP / CSPELL / ZREP / SIDEC / BINDX / MODEX** 重点核对零重铸。
- R32-J 六残余坐标（inode 时间戳 / extent 布局 / owner 字 / ACL-xattr / dirent 序 / 线程池宽度）三方败——本轮零重提零代跑。
- R34-J 四项换名检查、R35-J / R36-J 零新站点级记录——原裁决在位（§2.2 第一组）。
- R32-A 重开条件哨兵方法 / R32-B 闭合稳定性方法 / R33-A 44/44 A-账本卫生——均仅作方法卫生应用（§2.4），不重铸「我又查了 reopen / closure / 门」。
- 宿主偏置窗（冻结 0 行）按派单原文不构成新发现（§1.1 判读遵此）。
- S5-H-1、S7-F-1 ≠ S6-F-5、S8-I-1、S9-D-4 / S12-D-1、S12-B-2（仍未触发）、S13-B-1（仍 loser ID）——全部姊妹属地，零触碰零代跑。catalog 行序契约（R28-B W3）与 R28-I npm POSIX `.bin` symlink 静默 no-op 均未触碰。

## 1. 重锚与 J1 复验（官方三遍，nvm Node v22.22.2）

### 1.1 I/O 地板矩阵（R12-J 附录 A 同源 harness，三遍 × 各 3 次运行，nvm v22.22.2）

harness 与 R12-J 附录 A 归档件除头注释与 tmpdir 前缀 `r12j-`→`r37j-` 外逐字同源；保存为仓库根 `r37j-reanchor.ts`，`pnpm exec tsx` 运行；烟测一遍按令弃置；官方三遍原始输出见附录 A；gate 运行前已从仓库树删除（`git status --porcelain` 空输出直证，零门禁事故）。

| 格 | 本轮三遍并集 | R36-J 记载带 | R34-J 记载带 | 裁决 |
|---|---|---|---|---|
| saveToDisk 50 obs | 132.8–277.5 µs/写 | 132.0–267.2 | 141.9–234.4（稳态；暖窗并集至 252.7） | 快缘 132.8 与 R36-J 快缘 132.0 逐位邻域；慢缘 277.5 为暖窗首采样（三遍首采样 277.5/256.8/267.1 均暖窗型），+3.9% 对 R36-J 慢缘 = 温和宿主慢样；稳态尾 132.8–196.8 全含于历史带 |
| saveToDisk 500 obs | 411.4–521.6 µs/写 | 412.4–534.9 | 429.1–533.4 | 与 R36-J 带几乎全含（快缘 411.4 对 412.4 −0.2% 逐位邻域，慢缘 521.6 < 534.9） |
| jsonl append fsync=false | 59.8–67.0 µs | 59.6–65.1 | 61.2–69.4 | 与两带均交叠（59.8–65.1 / 61.2–67.0 双共域）；无窗样 |
| jsonl append fsync=true | 215.5–385.6 µs | 240.5–380.2 | 227.1–338.2 | 快缘 215.5 低于 R34-J 快缘 −5.1% = host-fast 窗样（pass-3 尾采样）；慢缘 385.6 对 R36-J 慢缘 +1.4% 逐位邻域；**R35-J 型 399–801 极端 flush 竞争窗连续第二轮未再现** |
| 级联 MATCH（N=200，20 hits） | 624.6–747.8 µs | 623.0–717.0 | 636.0–857.7（并集） | 快缘 624.6 对 R36-J 快缘 623.0 逐位邻域；慢缘 747.8 全含于 R34-J 带内 |
| 级联 NO-MATCH | 275.6–308.5 µs | 254.1–302.9 | 260.3–335.6 | 带重叠（275.6–302.9 / 275.6–308.5 双共域） |
| context index（F=40 + 30 syms） | 40.4–41.1 µs | 40.3–41.0 | 40.4–41.9 | 与两带均交叠（40.4–41.0 三带共域） |
| track plan 计数器 | 1.02–1.13 µs | 1.02–1.12 | 1.01–1.11 | 带重叠（快缘逐位同值 1.02） |
| track plan randomUUID | 2.53–2.71 µs | 2.48–2.63 | 2.52–2.70 | 与 R34-J / R35-J（2.52–2.74）带交叠；慢缘 2.71 全含 R35-J 带内 |

判读：九格无一为结构迁移——同批冻结差分 0 行 + 九格全部与历史带交叠 + 仅有的两处缘外样一慢（saveToDisk 50 暖窗 +3.9%）一快（fsync=true 尾采样 −5.1%）**对称双向**（双向散布 = 宿主窗样特征而非位点性变化；对照 R36-J 六格同向齐移 host-fast 判读，本轮宿主刻度更中性）+ fsync 格极端慢窗连续第二轮消失（设备侧窗口性最直接证据：窗口可来可去而代码不变）。按派单原文「host-bias windows with freeze 0 lines are not new findings」不作新发现申报、不改带。**I/O 支配判据第三十七次成立**：persist 地板（saveToDisk 132.8–521.6 µs、jsonl 59.8–385.6 µs、级联 275.6–747.8 µs）比切片 CPU 格（plan 1.02–2.71 µs、context 40.4–41.1 µs）高 1–2 个数量级，任何纯 CPU 换名的收益上界被同路径 I/O 地板吞没。

### 1.2 J1 等价 sim（`scripts/r1j-equivalence-sim.ts`，不重铸）

场景 1 确定性边角 **64 checks / 16 cases** + 场景 2 随机化 **2400 checks / 600 cases** + perf fixture（N=6000 多主体 ~25% tombstoned）= **ALL EQUIVALENCE CHECKS PASSED（2468 bitwise checks）**。reference **10267.1 ms** → current **3.5 ms** = **2921.6×**（reference 逐元素访问 92,590,419 次不变）；对照派单 2823.6×、R31-J 2836.9×、R32-J 2713.8×、R35-J 2948.5×——本轮比率落宿主偏移带内（reference 腿 10267.1 介于 R36-J 9997.7 与 R35-J 10400.5 之间，current 腿 3.5 ms 跨轮稳定）。J1 fold 形状锁定原样，J1 锁不重铸。

## 2. 第三十七遍枚举（诚实结果：空；零新站点级换名记录）

### 2.1 逐文件重读与裁决（29 文件从零重读，零未点名站点）

| 文件（行数） | 站点归属（全数指到既有裁决） | 新候选 |
|---|---|---|
| `cluster/host.ts`（200） | 广播扇出 `body.trim()` + `targets.slice(1)` 提升 = **R34-J 记录 3** 原形在位不重铸（纯内存 ns、零持久坐标） | 无 |
| `cluster/mailbox.ts`（91） | mailbox n 三连 = **S5-J-4**；冗余 `byRole.set` = R15-J 拒列原文（R12-J 位点 B 已裁） | 无 |
| `cluster/spawn.ts`（54） | 组装命令类；双 `objective.trim()` = R12-J 位点 A 已裁；公开签名 X0-4 禁区 | 无 |
| `privacy/deletion.ts`（126） | 级联读序 `.catch(() => [])` 错误面 = R5-J §3 契约；`[...tombstones].sort()`/`cascaded.sort()` = R10-J µs 拒列；双文件串行 = **S4-J-3**（并行化竞态拒，写序姊妹 **S6-J-1**）；O(记录数) 整读改写 = 删除正确性契约本体（**DELINV** 域，§2.3） | 无 |
| `privacy/state-layout.ts`（29） | 路径构造命令类 | 无 |
| `privacy/record-classes.ts`（261） | `durableRecordClassById` 零生产调用方（§0.2 本轮 re-grep 直证）；表驻留命令类 | 无 |
| `preferences/loop-eval.ts`（180） | J1 落地本体（fold 形状锁定；`[...observations].sort` 属锁定形状）；两次 `tombstones.has` = **S2-J-1** 原样（L153/L159） | 无 |
| `preferences/store.ts`（288） | `rebuildViews` 条件 set = **S7-J-4** / `nowIso` 提升 = **S3-J-5**；`applyObservation` 全量 filter = **S1-J-2 / S2-J-6**（saveToDisk 支配）；每写 `mkdirSync` 自愈 = **S7-J-6 保留**；地板本轮 132.8–521.6 µs 直测（§1.1） | 无 |
| `preferences/export.ts`（65） | scopes includes / listObservations 拷贝 = **S3-J-6 / S5-J-1**；`exportForDataset` 零生产调用方；`JSON.stringify(…, null, 2)` 美化 = once-per-export 命令类 | 无 |
| `preferences/precedence.ts`（40）/ `materialize.ts`（36）/ `service.ts`（65）/ `types.ts`（33） | `SCOPE_PRECEDENCE.find` 线性扫描 = **R36-J §2.2 角度 b** 已拒；materialize `Object.entries` 拷贝环 = **R36-J §2.2 角度 a** 已拒（X0-4 公开形状域）；service 纯转发；types 纯类型 | 无 |
| `episode/manager.ts`（207） | 事件构造恒写可选键 / `runIds.includes` = R11-J §2.3 own-property 面 + **S1-J-3**；reduce fold + `markFailed` 粘性拒绝面 = **R17-J ln 域 + R18-J 空 fold 定价**；`TERMINAL_STATUSES` 模块级 Set 驻留命令类 | 无 |
| `episode/replay.ts`（49） | 两导出零生产调用方（R12-J §1.2 起复证）；单遍 parse 支配 = S3-J-4 域 | 无 |
| `episode/events.ts`（44） | 纯类型 | 无 |
| `episode/store.ts`（47） | append 队列 promise 链 = **R7-J 顺序契约** + R10-J §2.3-4 / R11-J **S0b 地板支配** | 无 |
| `episode/closure.ts`（49） | L32 legacy-match 模板串 = **R34-J 记录 4** 原形在位不重铸（once-per-close + legacy 兼容契约面）；acceptanceEvidence 结构扫描命令类 | 无 |
| `persist/file-lock.ts`（49） | `retryMs ?? 10` = D/E 已计价；J 零生产调用方本轮 rg 直证（§0.2）——整面等价重写生产收益恒 0；owner-token 回读三段式 = 锁释放正确性契约（§2.2 角度 c 复命中收口） | 无 |
| `persist/jsonl.ts`（53） | fsync 腿 open/sync/close = **fsync 不变量本体**（不可去）+ 地板格直测（§1.1）；`readJsonlObjects` split 单遍 = S2-J 读地板族；每 append `mkdir` 自愈 = R3-J 已裁 S5-G-1/S6-E-3 同型（§2.2 角度 a 复命中收口） | 无 |
| `track/primary-split.ts`（52） | 纯组装转发；公开形状 X0-4 | 无 |
| `track/plan.ts`（122） | L41 `/\b(tests?\|coverage)\b/i` + L112 `/\btests?\b/i` 行内正则 = **R34-J 记录 1** 原形在位不重铸（once-per-plan 1.02–2.71 µs 整调用直测内的 ns 份额） | 无 |
| `track/clarify.ts`（65） | H 消费面（`extractHeuristicContract` 调用方侧，H 属地）；spread 合并命令类 | 无 |
| `track/loop.ts`（279） | `assignments.find` in `planned.map`（L106）= **S1-J-5 排除行本体**（本轮逐字复核，C ≤ ~6）；waitForClarification 双 `readAll`（L261/L266）= **S2-J-11**；catalog∥learned = **S4-J-2**；`resolvedQuestionIds`/`catalogIds.includes` = X3-1 域；L80 `"user-first"` = R31-H D1 门坐标（H 属地零触碰，§0.2）；L88 = S8-E-1 站点（E 属地零触碰） | 无 |
| `context/index.ts`（418） | `estimateCodeMapTokens` 跨模块孪生 = **R3-J / X1-1 公开形状禁区**（S5-J-2 注原文）；`isUnderGenerated` O(d×g) 已计价（context 地板 40.4–41.1 µs 直测整含）；排序/去重命令类 | 无 |
| `context/packet.ts`（523） | L122 首个 `omissions.sort` = **S1-J-7 排除行本体**（本轮逐字复核在位；R1-J S17 比较器全字段覆盖 ⇒ 双排结构等价论证维持）；`pickCanonical` 单值组 spread+sort = **S7-J-1**；`estimateCodeMapEntry` 孪生 = 同 R3-J/X1-1；`contractDigest`+`collapseFacts` 每编译份额 = §2.2 角度 b 复命中收口；`queryPacketGrounding` = test-only 面（L498 唯一位点，§0.2） | 无 |
| `feedback/types.ts`（24） | 纯类型 | 无 |
| `feedback/store.ts`（85） | `readFeedback` 串行两读 = **R34-J 记录 2** 原形在位不重铸（**S5-J-3 / S6-J-1** 删除面串行形态族）；双 `.filter` 链 = **R36-J §2.2 角度 c** 已拒（族价签内份额）；`isFeedbackRecord` 结构卫命令类 | 无 |
| `feedback/redaction.ts`（121） | `stripForbidden` 顺序 split/join = **S3-J-1 / S9-J-1..4**；needles 每调用长度降序重排序 = R3-J 已显式计价（n=4 小常数，I/O 支配）；`copyFeedback` 显式字段拷贝 = 既往裁决 | 无 |

### 2.2 换名拒绝检查：本轮零新站点级记录

从零重推（假装排除表不存在重读 3655 行）枚举出的候选角度经对表逐一查证，**全部为已裁决站点 / 已点名属地的复命中，无一达到「从未在任何 J 报告点名」的新站点门槛**——本轮不新增站点级换名记录（J 区经三十六遍逐格归属后已无此类站点，零是更强的收口——R35-J / R36-J / R37-B…R37-G 同款判读）。处置分两组：

**第一组：R36-J §2.2 两组处置十五项按令复命中即止**（派单明令 R36-J 零新站点级记录不重铸）——R35-J 十二项重赛角度（`assignments.find` = S1-J-5 本体；L122 `omissions.sort` = S1-J-7 本体；双 `readAll` = S2-J-11；`pickCanonical` = S7-J-1；`stripForbidden` = S3-J-1 / S9-J-1..4；token 估算孪生 = R3-J / X1-1；episode append promise 链 = R7-J 顺序契约 + S0b 地板；`reduceEpisodeEvents` fold/`markFailed` = R17-J + R18-J；jsonl fsync 腿 = fsync 不变量 + 地板格；`queryPacketGrounding` = test-only 面（§0.2 第三十七次 re-grep）；file-lock 整面 = D/E 计价 + 零 J 调用方；R34-J 四项记录裁决逐字维持）+ R36-J 三处「看起来新」角度（materialize `Object.entries` 拷贝环 / precedence 5 元素表线性 `find` / readFeedback 双 `.filter` 链）——本轮从零重读中逐一再浮出，每项在 §2.1 对应文件行内以属主行复核收口（站点原形在位读码直证），不重列细节、不铸任何新记录。

**第二组：本轮重读中另核对的三处「看起来新」角度**（逐项拒收，均为既往裁决域内份额，不铸记录）：

- **a. `persist/jsonl.ts` `appendJsonlLine` 每 append `mkdir(dirname, {recursive})` 自愈**（L14）——R3-J 已裁 **S5-G-1 / S6-E-3 同型自愈语义**（R6-J §逐文件行、R14-J 同行原文在案）；与 preferences 侧 **S7-J-6**（每写 `mkdirSync` 自愈保留——自愈语义收窄拒：外部删目录后 ENOENT）同族同判据；成本已整含于 jsonl 地板格直测（fsync=false 59.8–67.0 µs，§1.1），缓存化收益 µs 负数量级且收窄崩溃自愈语义。复命中 + 语义收窄拒即止。
- **b. `context/packet.ts` `contractDigest`（hash32 一次）+ `collapseFacts` 分组 Map 物化每编译份额**（L431-434 / L371-411）——once-per-packet-compile 命令类；`collapseFacts` 的组内排序/挑选 = **S7-J-1**（`pickCanonical`）与 R1-J S17 比较器域的同函数邻座，包外无独立物理对象；packet 编译整面在 R1-J…R36-J 已逐段归属（S1-J-7 双排、S7-J-1 挑选、R3-J/X1-1 估算孪生），此两处为同面内剩余 µs 份额。复命中 + µs 拒收类即止。
- **c. `persist/file-lock.ts` finally 腿 owner-token 回读三段式**（L43-47 `close` → `readFile.catch(()=>"")` → 串匹配后 `rm`）——锁释放正确性契约本体（防误删他持有者锁文件；R36-J §2.1 原文「owner-token 回读 = 锁释放正确性契约」在案）；且 file-lock 整面 J 零生产调用方（§0.2 rg 直证）⇒ 任何重写生产收益恒 0。契约面 + 零调用方双重拒即止。

### 2.3 放大因子收口（引用 R33-J §2.2 方法作卫生核对，不铸面）

切片每个持久 I/O 位点的每命令倍率均为 O(1) 或 O(记录数)：preferences 每观察 1 次全量 stringify+write（地板 132.8–521.6 µs）、episode/feedback 每事件 1 次 jsonl 追加（59.8–385.6 µs 本轮带）、删除级联每次 2 读 + 命中时 1 改写（275.6–747.8 µs）。与实测地板相乘，真实规模下每命令持久成本 **< 1 ms**——距 tens-of-ms 落地线仍 **1.5–2 个数量级**；唯一 O(记录数) 整读改写（删除级联）是删除正确性契约本体（**DELINV** 域，不可折叠）。核对非发现，不铸面。

### 2.4 静态哨兵与在案条件核对（卫生应用，不铸门审计 face）

- **静态划界哨兵**：`chmod|umask|readdir|xattr|\blink\(|symlink|chown` 对切片八目录一次 rg = **0 命中**（空匹配直证）——**MODEX / LINKTOP 在 J 切片代码内无物理对象**，耐久工件面维持 R30-J/R31-J plan-ban 划界，不铸「我又查了门」。
- **fsync / 完整性面**：jsonl fsync 腿（jsonl.ts:19）+ run 侧终态事件 fsync=true 腿（event-store）双位点在位直证（§0.2），零去除。
- **R32-J 六残余坐标**：已三方败，本轮零重提零代跑。
- **S8-E-1 / D1 门坐标**：J 调用方侧位点在位核对（§0.2），属地在 E/H，零触碰。
- **R32-A 哨兵方法 / R32-B 闭合稳定性方法**：作为方法应用于上列核对，不铸「我检查过」新面。

### 2.5 枚举收口

零未点名站点（§2.1 29/29 文件全归属）；零新站点级换名记录（§2.2 第一组十五项按令复命中即止、第二组三角度全拒收，R34-J 四项记录维持不重铸）；放大因子收口 < 1 ms/命令（§2.3）；静态哨兵 0 命中（§2.4）；重锚九格全为冻结 0 行下的宿主窗（§1.1 对称双向窗样判读）；J1 sim 2468 全绿（§1.2）。**没有可立 ID 的新角度。不铸 S37-J-\*，不铸新 face 名，不取绰号，不写 EXCLUSIONS 行。**

## 3. 测试与验证

- 聚焦切片套件（`test/unit/{cluster,privacy,preferences,episode,persist,track,context,feedback}`，node:test via tsx）：**tests 91 / suites 4 / pass 91 / fail 0**（`8e86fe2` 树，nvm v22.22.2，与 R36-J 记录同计数；src/ 与 test/ 对快进后 `f83bce5` 位级同一 ⇒ 结果同一有效）。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0**（`8e86fe2` 树，src/ test/ package.json 对 `f83bce5` 位级同一）；**tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1**，与 R14-E…R37-I 历史计数逐字相同。临时重锚 harness 在 gate 运行前已从仓库树删除（`git status --porcelain` 空输出直证，附录 A 纪律）。
- J1 sim：**2468/2468 逐位通过**（§1.2）。
- 硬性不变量逐条复核：分析不改在途 run；tracking 无指挥权；H/score 不写路由 PASS/FAIL；live = R0 等价（R1 未接线）；双 LCB / 双归因在位；promotion proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；CAS / 凭证 / 数据面合同 / 公开签名零触碰；**无 fsync / 完整性再哈希去除**（§2.4 直证）；fail-open 家族保持关闭；S5-H-1 / S7-F-1≠S6-F-5 / S8-I-1 / S9-D-4 / S12-D-1 / S8-E-1 / S12-B-2 / S13-B-1 未重开未代跑（属地在姊妹区）；catalog 行序契约（R28-B W3）与 R28-I npm `.bin` symlink 静默 no-op 均未触碰；file-lock `retryMs ?? 10` 原样；**J1 fold 形状锁定原样**。
- 仓库变更仅本报告一个文件：`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` = 本文件新增；重锚 harness 未入库（附录 A）。不开 PR。

## 4. 前后对比

无生产代码变更，无前后对比对象。切片 I/O 地板带维持 R34-J/R35-J/R36-J 记载（本轮九格 ×3 遍复测见 §1.1，对称双向宿主窗样如实注记）。

## 5. MORE_OPTIMA 判定

- 29 文件零未点名站点；R36-J 十五项处置复命中即止 + 本轮三角度全拒收、零新站点级换名记录；R34-J 四项记录 + 既有 S\*-J-\* 全系原形在位；重锚九格全为宿主窗（冻结 0 行 + 对称双向散布 + 极端慢窗连续第二轮消失三重证据）；J1 sim 2468 全绿；每命令持久成本上界 < 1 ms，距落地线 1.5–2 个数量级，且其组成已逐段归属既有 ID / 契约面。
- 判定：**MORE_OPTIMA=no**。

## 6. 新增排除 ID：无

不铸 S37-J-\*；不新增 EXCLUSIONS 表行；空枚举轴不取绰号；不重铸 J1 / R34-J 四项记录 / R35-J 十二项重赛角度 / R36-J 三角度 / R32-J 六坐标 / LINKTOP / MODEX / digest 轴 / 姊妹空枚举方法。

## 附录 A：重锚原始输出（harness 未入库；与 R12-J 附录 A 除头注释与 tmpdir 前缀 `r12j-`→`r37j-` 外逐字同源；烟测一遍已弃置）

```text
# pass 1 (8e86fe2 树)                              # node v22.22.2
saveToDisk 50-obs seed: 277.5 / 195.5 / 132.9 us/write
saveToDisk 500-obs seed: 521.6 / 419.3 / 419.0 us/write
jsonl fsync=false: 66.1 / 66.5 / 60.7   fsync=true: 385.6 / 365.9 / 346.4 us/append
cascade MATCH: 747.8 / 640.1 / 637.5    NO-MATCH: 275.6 / 308.5 / 281.3 us/cascade
buildProjectContextIndex: 40.5 / 40.4 / 40.4 us/build
plan counter: 1.12 / 1.11 / 1.13        plan randomUUID: 2.71 / 2.65 / 2.68 us/plan

# pass 2 (8e86fe2 树)                              # node v22.22.2
saveToDisk 50: 256.8 / 194.9 / 132.8    saveToDisk 500: 508.9 / 417.4 / 411.4
jsonl false: 64.4 / 64.8 / 59.8         jsonl true: 305.5 / 272.9 / 293.4
cascade MATCH: 700.2 / 704.8 / 624.6    NO-MATCH: 278.4 / 276.7 / 299.8
context: 41.1 / 40.7 / 40.6             plan: 1.08 / 1.09 / 1.08   uuid: 2.68 / 2.69 / 2.68

# pass 3 (8e86fe2 树)                              # node v22.22.2
saveToDisk 50: 267.1 / 196.8 / 152.8    saveToDisk 500: 492.8 / 452.7 / 439.6
jsonl false: 67.0 / 65.7 / 59.9         jsonl true: 271.6 / 224.3 / 215.5
cascade MATCH: 698.4 / 683.4 / 669.2    NO-MATCH: 288.2 / 306.6 / 279.2
context: 40.7 / 40.6 / 40.5             plan: 1.05 / 1.04 / 1.02   uuid: 2.58 / 2.59 / 2.53

# J1 sim (scripts/r1j-equivalence-sim.ts, 8e86fe2 树)
scenario 1 (deterministic edge cases): 64 checks over 16 cases
scenario 2 (randomized): 2400 checks over 600 cases
perf fixture (N=6000, multi-subject, ~25% tombstoned):
  reference 10267.1 ms -> current 3.5 ms (2921.6x)
  reference element visits per evaluation: 92,590,419
ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)
```

---

MORE_OPTIMA=no
BRANCH=cursor/r37-j-persist-thirty-seventh-pass-83a1
