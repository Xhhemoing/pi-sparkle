MODEL_SLUG=claude-fable-5-thinking-xhigh

# R37-F：`src/experiments/` 第三十七遍复查报告（S7-F-1 + S7-F-2 之上，全新第三十七组枚举）

- **战役:** 全库持久 SOTA 优化 Round 37 / R37-F（R1-F…R36-F 同区三十六遍之后的第三十七遍）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `da2a01d`（R37-C 摄取——空枚举——+ 本 R37-F 派单；tip 已含 R37-A.md + R37-B.md 摄取）。**测量窗口（全部锚点测量 + 聚焦套件 + `pnpm gate`）在 `da2a01d` 工作树内完成、窗口内零快进**；提交前 `git fetch origin cursor/sota-persistent-opt-83a1` 检测到 exclusive **一次纯 docs 快进** `da2a01d` → `f209ddd`（R37-E 摄取（空枚举）+ round-37 PLAN/PROGRESS 更新 + R37-G 派单——`git diff da2a01d..f209ddd --stat` 恰 3 个 docs 文件（PROGRESS / round-37 PLAN / R37-E.md，205 插入 18 删除），`-- src/` 直接复核 **0 行**），按令执行**纯快进**（`git merge --ff-only`，零冲突）至 `f209ddd` 并重验冻结（切片 vs `519101f`、最强冻结 `89f0455..HEAD -- src/`、锁文件均 **0 行**，`wc -l` 仍 2550，`git log -1 -- src/experiments/` 仍 `519101f`）；两 tip 间 `src/` + `test/` 逐字节相同（docs-only diff），锚点测量结果原样携带；聚焦套件与 `pnpm gate` 在快进后 `f209ddd` 树上**各再跑一次**同计数全绿（§6）。已通读新姊妹报告 **R37-E**（E 区诚实空枚举：零新 S37-E-\* ID、零新站点级换名记录——两处独立重瞄均归 S3-E-3 本体与 S13-B-1 已计价格真子集；SLICE-CPU cmp25 14.7–15.3 µs/run 带内；S8-E-1 三生产位点零去重直证——全部物理对象在 `src/learning/`，零 F 可移植轴）并折入非移植清单（§0）。派单要求预读的 **R37-A / R37-B / R37-C** 三份姊妹报告已全文实读并折入非移植清单（§0，含其记录家族的 F 侧镜像面划界）。R37-D 在飞，按令未等待、零触碰姊妹分支。
- **分支:** `cursor/r37-f-experiments-thirty-seventh-pass-83a1`
- **切片:** 15 文件 `src/experiments/`（全量重读确认，`wc -l` 合计 **2550** 行，与 R29-F…R36-F 普查同数，无缺无余：attribution-report 21 / canary 292 / comparison-report 321 / dataset 206 / evaluation-card 65 / gated-comparison 93 / holdout 140 / isolation 68 / manifest 145 / plan 165 / replay 149 / shadow-compare 44 / shadow 505 / simulation-holdout 272 / threshold-calibration 64）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R24-F…R36-F 同引擎位级相同，带可比；本轮 `node -p process.versions.v8` 直证）；tsx 4.23.12（仓内 node_modules）、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成、`package.json` / `pnpm-lock.yaml` 对 `519101f` 复核 **0 行**。本 VM 系统默认 node 为 v22.14.0（`/exec-daemon` PATH 前缀遮蔽 nvm）——按 R32-D §1(iii) / R33-F…R36-F 在案陷阱**开工即**显式钉 PATH 到 `~/.nvm/versions/node/v22.22.2/bin`，harness 按令重建（`--frozen-lockfile` 全新装载）后先跑**一次 r5f 冒烟运行验证启动，按纪律弃置**；其后十个官方批次（r5f ×3、r6f ×3、r7f ×3、r1f ×1）每批前在同 shell `node --version` 复核 v22.22.2（十批全部带内自报），**零批次落在 v22.14.0 上**，除冒烟外无弃置批次。

## 结论

**诚实的空枚举：第三十七组没有可立 ID 的新角度，不铸任何 S37-F-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不写 EXCLUSIONS 行，生产代码零改动（R12-F…R36-F 二十五连空之后的第二十六连空）。且本轮**零新站点级换名记录**：从零重推的全部候选角度经对表逐一查证，每一个都命中既有排除 ID 本体、R4-F 在案防御纵深保留域、R12-F/R20-F 已直测定价轴、R32-F/R33-F/R34-F 既有换名记录（十项全部复核维持）、姊妹归档面（禁移植）或停驻门成员——没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标（§0 裁决清单）。本轮第三十七遍从零重读 15 个源文件与全部四个生产消费文件、在本 VM 重建全部锚点（r5f/r6f/r7f 各**三次**独立运行 + r1f 一次，§1）、对 R31-F §2 面格属主表做**第七次**独立重推（无一格松动，BINDX 计数 **10 token / 8 行** 第四次独立复证成立、坐标未动，§2）、并按 R32-A 首创且 plan-ban 重铸的哨兵方法对 **F 自档案**在案重开条件做**不计时静态卫生复核**（§3——与 R33-F…R36-F 同款，仅作卫生性应用、不当边际产出申报、不铸「我又查了一遍重开条件」式新面），全部哨兵**未触发**。空枚举与 R12-F…R36-F、R37-A/R37-B/R37-C/R37-E 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 15 文件自 `519101f` 逐字节零变化（**三点核验**——分支创建时 / 测量与门禁完成后 / 纯 docs 快进至 `f209ddd` 后提交前，`git diff 519101f..HEAD -- src/experiments/` 均 **0 行**；`git log -1 -- src/experiments/` 显示切片最后一次生产改动仍为 `519101f`「perf(experiments): S7-F-1 aligned-prefix restore fast path + S7-F-2 ASCII-head trim guard」）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均为 docs）。锚点复测（各 ×3，§1 诚实注记）：**r5f C2 127.53 / 120.56 / 119.95 ms**；**r6f partD 121.39 / 121.60 / 121.48 ms**；**r7f partE 130.06 / 129.45 / 128.53 ms**（本 VM 为战役窗口的**中段主机、r7f 档轻微偏高沿**——r5f 落 R34-F 带（119.21–128.96）与 R36-F 带（118.82–123.76）并集内、r6f 落 R36-F 带（118.50–122.96）内、r7f 与 R34-F 带（128.18–130.28）交叠且贴 R36-F 高沿（128.74）——三档均无同向越出历史包络；冻结 0 行按令不构成新发现）；r6f 三次序 + r7f 四次序 restore 省额**三轮全正无符号翻转**（S7-F-1 ≠ S6-F-5 复核成立：同下标哈希相等 + 后缀回退形态在位——shadow.ts L355–L438 读码直证 L383 `episodeHash !== population[index]` 同位比较、首个错位回退 S6-F-1 pending-Set（L407–L434 后缀臂）、下标 0 错位重跑原落地环（L388–L406），非 O(P) 双指针）；r1f 2668 项逐位检查全过（参考侧成员比较 **167,167,000 次**与历轮锚定值逐位相同）。聚焦实验测试 **75/75 绿**（`da2a01d` 树一次 + 快进后 `f209ddd` 树一次，两次同计数）；`pnpm gate`（typecheck + lint + test + build）**两跑全绿**——test 1169 / pass 1168 / fail 0 / skipped 1（一跑在 `da2a01d`、一跑在快进后 `f209ddd`，与 R33-F…R36-F、R37-A/R37-B/R37-C/R37-E 逐字同计数，exit 0）。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动（X0-11——PAIRX 编排位点 `attribution-report.ts` 本轮零字节未触、本轮零计时新臂且无任何脚本 import 两估计器）；无阈值/测试/公开签名/数据面变更；无 fsync/完整性重哈希移除；fail-open 家族保持关闭。仓库变更仅本报告一个文件。不开 PR。

## 0. 冻结、摄取处置与非移植清单（方法第 1、4 步）

- 分支按令创建于派单 tip `da2a01d`；测量窗口（锚点 + 聚焦套件 + gate）内独占分支零快进；提交前 fetch 检测到一次纯 docs 快进 `da2a01d` → `f209ddd`（增量恰 PROGRESS / round-37 PLAN / R37-E.md 三 docs 文件，`src/` 0 行），按令 `git merge --ff-only` 纯快进零冲突，快进后补读 R37-E.md 全文并折入本清单。冻结核对**三点**：分支创建时 / 提交前快进前 / 快进后，`git diff 519101f..HEAD -- src/experiments/` 均 **0 行**；`wc -l` 三点均 2550。
- 派单预读的三份姊妹报告全文实读并划界：**R37-A**（tracking 空枚举——三项新站点级换名检查拒列：types.ts parseHumanSignal ratio 臂属性双读/双 cast CSE（S5-J-5 家族）、from-child claims 单元素数组每调用新铸（S1-A-7 + S3-A-2 邻域）、child-tracking acceptance.map 投影数组每调用新铸（S1-A-7/S7-B-5 同族第三站点）；预算重锚 13.1–16.3 µs/gate ⇒ ~65–82 µs/run）；**R37-B**（live 路由空枚举——零新站点级换名记录，两残余角度均为 R2-B 已点名站点复命中；S12-B-2 腿 (a) 与 S13-B-1 重开条件经其重取证均未触发）；**R37-C**（离线路由空枚举——零新站点级换名记录，两处独立重瞄均归既有属主（S8-C-3/S8-C-4 fail-closed 卫家族；X1-3 冻结数值路径本体）；中位复核 658.6–660.2 主机偏快；组合天花板 28.1–28.5 < 35；sink=7.309）。三者与 F 切片**零共享物理对象**（R37-B/R37-C 的 routing 站点均在 `src/routing/`，本切片 shadow-compare.ts 仅消费 `routeR1` 公开导出，双方零改动），均为空枚举，**已并入非移植清单，不重编号、不移植、不重提**。快进折入的 **R37-E**（learning 空枚举——零新站点级换名记录，两处独立重瞄归 S3-E-3 本体与 S13-B-1 已计价格真子集；cmp25 14.7–15.3 µs/run；S8-E-1 恰 3 生产 `loadLearnedRouting` 位点直证维持）全部物理对象在 `src/learning/`，与 F 切片零共享对象、零 F 可移植轴，同并入非移植清单——其 S8-E-1 / S12-B-2 / S13-B-1 属地哨兵已由其自证未触发，F 不代跑（§3）。
- **三记录家族的 F 侧镜像面本轮划界**（R37-A 三项记录逐一镜像到 F）：记录 1 家族（属性双读 / 双 cast CSE）——F 侧对应站点即 comparison-report.ts L167/L170 与 L168/L171 的 delta 双算 = **S4-F-3 本体**（circuit 内 CSE 已拒，41µs 级）复命中维持；记录 2 家族（单元素/字面量数组每调用新铸）——F 侧承载行为 threshold-calibration `thresholds`/`rows` 数组构造 = **S1-F-4** once-per-report 属地、manifest `?? []` 空数组默认 = R15-F 轴 3 引擎面 ns 域，复命中即止；记录 3 家族（投影 map 数组每调用新铸）——F 侧恰三站点：replay `eligible.map` propensities 装配 = off-policy 账本**输出契约本体**（字段必需，非可消除），simulation-holdout `holdout.map(toFrozenEpisode)` = **S3-F-5** 投影拒列本体，gated-comparison 四条 `records.map` = **S2-F-6** 多遍融合已拒本体。三镜像划界全部命中既有行，本报告**零新站点级记录**结论不变。
- **非移植清单全额维持并逐项核对**（派单 DO NOT REMINT 全清单 + round-37/PLAN F 段）：R30-F 边界输出消费/快照读回拓扑面、R29-F 输出保留/被替换快照存活面、R28-F 边界调用协议面、R27-F 输入访问路径机制面、R26-F 输入存储状态面、**R31-F 故障通道处置残格**（分解归 R17-F + R30-F + R29-F + 引擎面——本轮不重铸、不新停驻、零计时臂）、**PAIRX**（R29-C——编排位点共驻不当新 F ID）、**BINDX**（R31-C——切片 ambient `Math.*` token 恰 **10 个于 8 行**：comparison-report `Math.abs` ×4（L185/L190/L195/L200）+ `Math.sqrt` ×2（L120）、gated-comparison `Math.sqrt` ×2（L92）、replay `Math.imul` ×2（L69/L70），本轮 `rg -o` 逐 token 直数与 R33-F 订正值/R34-F/R35-F/R36-F 复证**第四次独立复证成立**；行集合同一、坐标未动；计数即止零 patch 零剂量）、**MODEX**（R31-J——切片 chmod/umask/八进制 mode 字面量 **0**（宽式 `mode:` 仅命中 plan.ts L21/L97 的 `ExperimentMode` 领域字段，非权限位）、零盘上工件，划界声明即止）、I/O text-codec topology（R31-E——`rg "node:fs|readFile|writeFile|Buffer"` 本轮首手复核**零匹配**，公开边界全同步纯内存，面结构性空）、REPRX（R30-C——切片 `JSON.parse` 位点 **0**）、R32-A 重开条件哨兵审计（**方法不重铸为面**——§3 按令仅作卫生性应用于 F 自档案）、R32-B 闭合稳定性审计（**方法不重铸为面**）、R33-A 44/44 A 账本卫生（方法不移植）、**R33-A … R36-J 空枚举**与 **R37-A/R37-B/R37-C/R37-E 空枚举**（上两条已划界，声明即止）、R31-A injected-callable 停放门、R31-B 六停驻门、R31-D 的 R22-D 属地格、R31-G 18 门审计、R31-H 四扇边界参数残门、R31-I CLI 进程边界门扉闭合审计（F 是进程内库——切片 node: 引入恰 2：isolation.ts:1 `node:path`、holdout.ts:1 `node:crypto` randomUUID，本轮 rg 直证）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX 全部未编号未移植。S7-F-1 ≠ S6-F-5 维持（§1 次序符号复核 + 结论段读码直证）。S5-F 已落地不重做（plan.ts 单探针 + S7-F-2 首字符卫现场在位，§4）。R36-F / R35-F 零新站点级记录按令不重提、不重铸为「我又找了一遍零记录」新面。「不铸『我又查了一遍无名站点』」遵守：本节全部为既有行复命中裁决，无一铸新。
- **换名重提检查**（本轮枚举全部残余角度的逐项裁决，一律不铸 S37-F-\* ID；**零新站点级记录**——每一项均能指到在手报告逐字点名或直测定价过的行）：
  1. 本轮独立重瞄 1：`applyMissingOutcomePolicy` 策略臂与 `OutcomePolicyResult` 中间对象（shadow.ts L67–71 / L147–183，treat-as-failure 臂替换 outcome 重建）= **R20-F outcome 流组成轴属主**（格 a 三值网格直测同价 + 「干净条目上策略值是不可达读」死代码级证明 + 生产脏流量为零）+ **R12-F 6A 对象构造位直测定价**（天花板 8–204 µs/实验，V8 逃逸分析先行收割；R9-F §0 不立 ID 裁决维持）——属主引用即止；
  2. 本轮独立重瞄 2：`haltOnAssignmentBudget` 直通检查与 `{...next}` 状态 spread 对（shadow.ts L100–110）= **R15-F 轴 3 引擎面 + R12-F 同一 6A 直测**（D3 项逐字点名）——复命中即止；
  3. `pairedEvaluationCard` 内 `sampleStandardError` 对 `mean` 的内部重算 = **S2-F-6 本体亚案** + R34-F 换名记录 2（mean/SE 孪生辅助）同族——复命中即止；
  4. `pairedDeltaSummary` `count===0` 死抛分支与 threshold-calibration `best?.f1 ?? -1` 死回退 = **R4-F 在案「pairedDeltaSummary 型死分支（rows[0] 恒存在）」防御纵深保留域**——原型站点复命中即止；
  5. `IMPROVEMENT_CLAIM` / `IMPROVEMENT_PATTERN` 孪生正则 = R33-F 换名记录 3 维持；`validateComparisonReport`+`computeComparisonReport` strip-retry 双调 = S1-F-1 本体；utility delta 双算 = S4-F-3 本体；
  6. replay.ts orderedHashes spread 复制 = R33-F 换名记录 1 维持；replayPolicy 双 `manifestHash`（L124/L128）= S2-F-2（零生产调用方本轮直证——eval-routing 对 replay.ts import 恰 `replayCacheKey` 一项）；
  7. simulation-holdout 条件 spread 装配链 = R34-F 换名记录 1 维持；familyBreakdown 查重单探针化 = R34-F 换名记录 3 维持；mean/SE 孪生去重 = R34-F 换名记录 2 维持（含 resolvePendingMembership 孪生对同族注记——shadow/canary 两形 X1-5 契约不同不并）；
  8. manifest stableStringify 形态 = R32-F 换名记录 1 维持；HoldoutVault 注入缝默认臂 = R32-F 换名记录 2 维持；shadow 返回值拷贝省略 = R32-F 换名记录 3 维持；shadowDecisionAt 闭式跳步 = R33-F 换名记录 2 维持；requireCanaryBlock 重派生缓存 = R33-F 换名记录 4 维持；
  9. assign 路径 O(P) `population.includes` 与 O(A) 唯一性扫描 = S8-F-1 / S9-F-1 / S2-F-4 / S3-F-1 本体复命中；`recordExperimentOutcome` 双 some + `accumulatedCostUsd` 全扫 = S1-F-8 本体；validate 三站点 scratch 共享 = S9-F-3 本体。
  R32-F 三项 + R33-F 四项 + R34-F 三项既有换名记录本轮**全部复核维持不重提、不新增**；R35-F / R36-F 零新站点级记录不重提、不重铸。
- 未重开任何 X\* / S1-\* … S36-\* 条目。**单变量纪律**：本轮**无任何计时新臂**——四个锚点脚本均为 `scripts/round0{1,5,6,7}-*.ts` 入库血统资产逐字节原样重跑（零改动零参数）；§2/§3 全部为静态 rg / 读码断言（不计时）。S9-D-4 / S12-D-1 / S8-E-1 / S8-I-1 / S13-B-1 / S12-B-2 / S5-H-1 等姊妹区维持项零触碰（其哨兵属地在 A/B/C/D/E/H/I，本轮不代跑——R37-B/R37-C 已各自复核未触发）。catalog 行序契约（R28-B W3）与 npm POSIX `.bin` symlink 静默 no-op（R28-I）零触碰。

## 1. 锚点重测（r5f/r6f/r7f 各三次独立运行，r1f 一次；本 VM 实测，未拷贝历轮数字；nvm Node v22.22.2；全部在 tip `da2a01d` 工作树完成，窗口零快进）

| 档位 | 第 1 次 | 第 2 次 | 第 3 次 | R36-F 记载带（派单对照） |
| --- | --- | --- | --- | --- |
| r5f C2 全实验（P=2000/A=1000） | 127.53 ms | 120.56 ms | 119.95 ms | 123.76 / 119.45 / 118.82 ms |
| 2A-validate 组件 | 107.55 ms（84.3%） | 100.06 ms（83.0%） | 100.32 ms（83.6%） | 98.73–100.80 ms（81.4–83.3%） |
| r5f C1 has+add → add+size 省额 | +30.44 ms（20.7%） | +31.53 ms（21.3%） | +31.16 ms（21.2%） | +28.85~+34.88 ms（19.8–23.0%） |
| r6f partD 全实验 | 121.39 ms | 121.60 ms | 121.48 ms | 118.50–122.96 ms |
| r7f partE 全实验 | 130.06 ms | 129.45 ms | 128.53 ms | 125.52–128.74 ms |
| r7f validate 卫（×2000） | +9.45 ms | +7.32 ms | +6.91 ms | +9.50~+10.29 ms |

**次序符号（committed 回归脚本原生视图，全部为「落地生产形态 vs 冻结参考」省额）**：r7f partE 四次序三轮全正——prefix(aligned) +44.48/+45.96/+45.10、half-aligned +27.22/+27.18/+31.00、scattered +4.21/+4.48/+5.23、reversed +5.20/+4.51/+5.14 ms，无符号翻转；r6f partD 三次序三轮全正——prefix +124.86/+121.89/+123.93、scattered +30.11/+29.11/+29.00、reversed +29.86/+27.59/+29.25 ms。**S7-F-1 ≠ S6-F-5 复核成立**：落地代码零漂移（切片零 diff），同下标哈希相等 + 后缀回退形态（非 O(P) 双指针）在全部次序下三轮同号为正。r5f C4 索引环形式 −2.27/−1.54/−2.83 ms 维持 S5-F-3 抖动带记载（R36-F −2.30/−3.14/−3.13、R35-F −2.77/−4.71/−3.72）；C5 dataset 镜像 14.2–14.5 µs（U=2000）/ 121.2–130.3 µs（U=20000）维持 S5-F-1 记载（R36-F 14.7–18.0 / 132.9–146.6、R34-F 12.2–15.2 / 131.3–138.0——两档均与历带交叠或带缘毗邻）。r1f 2668 项逐位检查全过（生产侧 62.4 ms / 参考侧 714.5 ms，11.4×；参考侧成员比较 **167,167,000 次**——与历轮锚定值逐位相同；参考侧壁钟非锚点义务，钉死量是比较计数与逐位等价）。

**诚实注记**：(i) 本 VM 为战役窗口的**中段主机、个别档轻微偏高沿**：r5f C2 三读数 119.95–127.53 落在 R34-F 带（119.21–128.96）内且 runs 2/3 落 R36-F 带（118.82–123.76）内（run 1 的 127.53 高出 R36-F 高沿但深居 R34-F 带）；r6f partD 121.39–121.60 完整落 R36-F 带（118.50–122.96）内；r7f partE 128.53–130.06 与 R34-F 带（128.18–130.28）近乎重合、低沿贴 R36-F 高沿（128.74）——三档均在历史包络内，无同向越沿，冻结 0 行 + 引擎位级同一 + 脚本逐字节原样 ⇒ 宿主刻度项非切片行为变化，按派单令「host-bias windows with freeze 0 lines are not new findings」不作发现、不改带。(ii) 少数格个体注记：2A-validate 组件 run 1 的 107.55 高出 R35-F 高沿（105.09）约 2.3%（runs 2/3 回落 R35-F 带内），占比 83.0–84.3% 贴 R35-F 高沿（83.4）——与 r5f run 1 整档偏高同向，同宿主窗现象；r7f validate 卫 +6.91~+9.45 与 R33-F/R34-F 带（+8.00~+9.37）交叠且 runs 2/3 落带下侧（R36-F 带 +9.50~+10.29 为高沿轮）——**省额方向三轮全正不变**，无符号翻转，不构成任何重开触发（§7）。(iii) 十个官方批次（r5f ×3、r6f ×3、r7f ×3、r1f ×1）全部在同一 shell 会话、PATH 钉死后完成，每批前 `node --version` 自报 v22.22.2；harness 重建后的一次 r5f 冒烟运行（全绿、值带内：C2 122.57）按纪律弃置，官方口径为其后十批；无会话重置、无其他弃置批。

## 2. 调用面复核 + 面格属主表独立重推（元闭合，方法第 3 步前置；卫生性应用不当边际产出）

生产消费图 rg 全仓复核（与 R30-F §1.2 / R31-F §2 / R32-F…R36-F §2 逐条一致）：切片外 `src/` importers 恰四个文件，import 行逐条核对——`adaptation/reflection.ts`（validateExperimentPlan + createShadowRunner + 类型：唯一热交错消费者）、`adaptation/eval-routing.ts`（comparison-report 类型与函数 + gatedComparisonReport + createIsolationGuard + stableStringify + **replayCacheKey**——对 replay.js 的 import 恰此一项，L18 直证，S2-F-2 前提本轮成立）、`adaptation/promotion-rules.ts`（validateComparisonReport 直呼）、`routing/r1-shadow-report.ts`（comparison-report + gated-comparison）。**故障通道处置普查复核**：四消费文件 catch 位点合计 4 处、全部在 eval-routing.ts（L219/L249/L259 包 `JSON.parse`/`readFile`，非切片物），**恰一处**包切片调用——L336 `assertReplayIsolated` 包 `createIsolationGuard`（MESSAGE + WRAP-RETHROW 坐标，once-per-eval 冷通道）；`reflection.ts` / `promotion-rules.ts` / `r1-shadow-report.ts` 零 catch ⇒ 热通道生产坐标仍 = PROPAGATE。切片故障源普查：**142 处** `throw new DomainValidationError`（逐文件计数与 R31-F…R36-F 全同：shadow 27 / plan 22 / dataset 19 / canary 14 / manifest 14 / holdout 11 / simulation-holdout 10 / comparison-report 8 / evaluation-card 7 / isolation 6 / replay 3 / gated-comparison 1 / 其余三文件 0），切片零 `throw new TypeError/Error` 直抛点——故障通道单类维持。

R31-F §2 面格属主表本轮从零**第七次**独立重推（(o) = F 区属主轴；(s) = 姊妹区收口面，明令禁移植；每格属主裁决**无一松动**）：输入存储状态 (o) R26-F / 访问路径 (o) R27-F / 对象表示 (s) REPRX / 引用身份·别名 (s) R29-A / 流组成 (o) R20-F / 计划标头身份几何·计数·成员拓扑 (o) R25-F/R23-F/R22-F / 规模与二阶联角 (o) R14-F 域 + (s) R30-B / 调用协议 (o) R28-F / 排程·实参物化·调用上下文 (s) CALLB/R28-H/R22-H / 调用流次序·分派历史 (s) R30-A / 时钟来源 (s) R29-E / 执行基片·名字解析 (s) R27-H 族 / BINDX / 引擎态 (o) R15-F 轴 3 + R19-F 域 / 输出成功通道保留 (o) R29-F / 输出成功通道消费·读回 (o) R30-F / 输出故障通道制造·单价 (o) R17-F / **输出故障通道调用方处置 = R31-F 分解归属（R17-F + R30-F + R29-F + 引擎面），本轮零计时零重铸维持** / 双估计器共驻 (s) PAIRX（X0-11 禁区）。结构性空直证两行维持：**I/O 编解码拓扑 (s) R31-E——切片 fs/codec 位点 0**；**持久工件权限模式字 (s) R31-J MODEX——切片 chmod/umask/八进制 mode 位点 0、零盘上工件**。**BINDX 划界行第四次独立复证**：`rg -o "Math\.\w+"` 逐 token 直数恰 **10 个于 8 行**（abs 4 + sqrt 4 + imul 2；120 行与 92 行各双 `Math.sqrt` 在列），R33-F 订正持续成立、行集合坐标属主裁决全部不动。第三十七遍枚举在该坐标系内**无剩余未审计平面**。

## 3. F 档案在案重开条件哨兵复核（全部不计时；R32-A 方法卫生性应用，不铸面、不当边际产出）

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| R31-F §7 (a) | 任何 `src/` 生产调用方在热交错内 catch 边界故障并做非平凡处置 | §2 catch 普查：热消费者 reflection.ts 零 catch；唯一切片 catch 仍为 eval-routing L336 once-per-eval ⇒ **未触发** |
| R31-F §7 (b) | 故障对象携带非字符串 own 载荷 | `src/domain/errors.ts` 在最强冻结内（`git diff 89f0455..HEAD -- src/` 0 行）⇒ **未触发** |
| R31-F §7 (c) | 故障通道契约（消息文本/单类不变量）解冻 | 测量窗口零快进且无解冻声明；切片零 diff ⇒ **未触发** |
| S6-F-2 重开 | delete+size 形态实测稳定反超 >5% | 无任何反超证据；本轮落地形态三/四次序省额三轮全正（§1）——按纪律**不重计时被拒形态** ⇒ **未触发** |
| S6-F-5 重开 | assignment 次序被契约化为 population 子序列 | 冻结 0 行 + round-37 PLAN 重申 S7-F-1 ≠ S6-F-5 ⇒ **未触发** |
| S5-F-1 / S4-F-1 前提 | dataset/simulation-holdout 链获得生产消费者 | `rg 'experiments/(dataset\|simulation-holdout\|holdout\|shadow-compare\|threshold-calibration\|attribution-report)' src/`（切片外）本轮**零命中**（精确六名交替式）⇒ 六链零生产消费者 ⇒ **未触发** |
| S2-F-2 / S4-F-2 前提 | `replayPolicy` 获得生产调用方 | eval-routing 对 replay.ts 的 import 恰 `replayCacheKey` 一项（L18 直证，§2）⇒ **成立维持** |
| S2-F-1 前提 | shadowDecisionAt 占比脱离 ~1% 域 | 切片零 diff + 锚点全部落在历史包络内 ⇒ **成立维持**（R33-F 换名记录 2 的真尺度拒绝依此维持） |
| X0-11 / PAIRX | 双路估计器拆分/重排/省略 | attribution-report.ts 零字节未触；本轮零装具 import 估计器 ⇒ **冻结在位** |
| 签名/调用图类（S1-F-6、S1-F-8、X3-3、S3-F-2 等） | 要求 `src/` 变更 | 最强冻结 0 行 ⇒ **全体判空** |

全部哨兵未触发。凡姊妹属地哨兵（S12-B-2 / S13-B-1 / S8-E-1 / S8-I-1 / S9-D-4 / S12-D-1 / S5-H-1）本轮不代跑——其属主轮 R37-B / R37-C / R37-E（及 R36-D / R33-I / R36-H）已各自复核未触发。

## 4. 逐文件收口（R1-F…R36-F 收口之上的本轮检查点）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| `shadow.ts` | 27 抛点单类维持；S7-F-1 落地形态零漂移（L355–L438 读码直证：L383 同位串等 + 首错位 S6-F-1 后缀回退（L407–L434）+ index 0 原环重跑（L388–L406））、四次序省额三轮全正（§1）；**本轮独立重瞄 1/2**：applyMissingOutcomePolicy 策略臂与 OutcomePolicyResult 中间对象 = R20-F 流组成轴 + R12-F 6A 直测属主、haltOnAssignmentBudget 直通 spread 对 = R15-F 轴 3 + R12-F D3 逐字点名（§0 裁决 1/2，属主引用即止）；shadowDecisionAt 闭式跳步 = R33-F 换名记录 2 维持；shadowDecisionAt O(index) 重放 = S2-F-1；restore→assign Set 复用 = S2-F-4/S9-F-1；assign 路径 O(P) includes + O(A) 唯一性 = S8-F-1/S3-F-1 本体；recordOutcome 末元素见证 = S9-F-2、查重/成本增量 = S1-F-8；accumulatedCostUsd 每 record 全扫 = S1-F-8 本体；空 assignments 跳 Set = S3-F-3；计划引用同一跳验 = S3-F-2；每操作全量 fail-closed restore = X3-3 本体；返回值拷贝省略 = R32-F 换名记录 3 维持；`resolvePendingMembership` 孪生 = R34-F 换名记录 2 同族注记维持 | 无候选 |
| `plan.ts` | 22 抛点；S5-F 单探针 + S7-F-2 首字符卫现场在位零漂移（不重做）；批量 Set = S5-F-2、null-proto = S6-F-4、批快路径 = S6-F-3、长度 1 快路径 = S8-F-3、scratch 共享 = S9-F-3、返回 Set = S1-F-6；EXPERIMENT_ID_PATTERN 模块级已提升；`mode` 字段 = 领域枚举非权限位（MODEX 划界注记，§0）；r5f C1/C2、r7f validate 卫三轮全正 | 无候选 |
| `canary.ts` | 14 抛点；同构对齐环 + S6-F-1 回退零漂移（对齐比较先于本体，与 shadow 次序相反——落地注释在案）；derivedExposure 重算是 fail-closed 交叉验证本体（消除即 fail-open，非候选）；requireCanaryBlock 重派生缓存 = R33-F 换名记录 4 维持；reversibleScopes includes = S1-F-7；exposure 派生为契约本体；`resolveCanaryPendingMembership` 孪生 = R34-F 换名记录 2 同族注记（X1-5 契约不同不并） | 无候选 |
| `gated-comparison.ts` | 1 抛点；IMPROVEMENT_CLAIM 孪生正则 = R33-F 换名记录 3 维持；mean/sampleStandardError 孪生辅助去重 = R34-F 换名记录 2 维持；pairedEvaluationCard 内 SE 对 mean 的内部重算 = S2-F-6 亚案（§0 裁决 3，复命中即止）；四条 records.map 投影 = S2-F-6 本体遍集合成员（R37-A 记录 3 镜像划界，§0）；strip-retry 复用 = S1-F-1 | 无候选 |
| `replay.ts` | 3 抛点；orderedHashes spread 复制消除 = R33-F 换名记录 1 维持（零生产调用方 + µs + R15-F 轴 3）；双 manifestHash = S2-F-2（零生产调用方本轮直证）；selected propensityFor 二次调用 = S1-F-2；空 exclusions 免建 = S4-F-2；exclusions Set 化已落地（H1）；`eligible.map` propensities 装配 = off-policy 账本输出契约本体（R37-A 记录 3 镜像划界非候选）；rerunHash 换更快同字节序列化 = S6-D-5 已拒（且 replayPolicy 零流量）；replayCacheKey 唯一生产腿 = once-per-eval µs（S7-D-2 邻域） | 无候选 |
| `comparison-report.ts` | 8 抛点；IMPROVEMENT_PATTERN 孪生编译 = R33-F 换名记录 3 同项；六遍改单遍 = X3-2；校验遍+构建遍双遍 = X3-2 族；四均值 reduce = X3-2 族；`Array.from(families.entries())` = S1-F-5；delta CSE（L167/L170 与 L168/L171 双算）= S4-F-3 本体（R37-A 记录 1 镜像划界，§0）；familyBreakdown 查重单探针化 = R34-F 换名记录 3 维持；pairedDeltaSummary `count===0` 死抛 = R4-F 防御纵深保留域原型站点（§0 裁决 4，复命中即止）；validateComparisonReport 生产消费者（promotion-rules）零 catch = PROPAGATE 维持 | 无候选 |
| `threshold-calibration.ts` | 零抛点；三遍并单遍 = S1-F-4（一次性 informational）；`best?.f1 ?? -1` 死回退 = R4-F 同一防御纵深保留域；thresholds/rows 字面量数组每调用构造 = S1-F-4 once-per-report 属地（R37-A 记录 2 镜像划界，§0） | 无候选 |
| `shadow-compare.ts` | 零抛点；薄封装维持 X1-5；routeR1 本体属 C 区（R36-C/R37-C 已对其站点作换名拒列，F 不重提不代跑） | 无候选 |
| `isolation.ts` | 6 抛点；resolve(outputRoot) 提升 = S3-F-4；per-root 双 isInside 的 resolve 重复 = 同族 + once-per-eval µs；**全生产普查唯一被 catch 的切片故障源**（eval-routing L336，once-per-eval 冷通道，§2） | 无候选 |
| `simulation-holdout.ts` | 10 抛点；结果装配条件 spread 链 = R34-F 换名记录 1 维持（S8-E-2 镜像 + 零流量 + µs）；探针去重镜像 = S5-F-1（链零生产消费者本轮直证）；尾部双验 = S2-F-5（fail-closed 纵深）；二遍构建 = X3-2 族 + 零流量；toFrozenEpisode 投影 = S3-F-5（R37-A 记录 3 镜像划界，§0）；auditHoldoutAccess try/register = once-per-run | 无候选 |
| `holdout.ts` | 11 抛点；审计追加拷贝 = S1-F-3/X4-2；注入缝默认臂 nowIso/randomUUID = R32-F 换名记录 2 维持（R31-A 停放 + 零生产流量）；Map 重 set 不可变态 = X4-2 族；replace 的 reason 前置检查次序 = 契约文本面（消息文本冻结，R31-F (c) 邻域，非候选） | 无候选 |
| `manifest.ts` | 14 抛点；stableStringify 形态改造 = R32-F 换名记录 1 维持（字节契约仪器 + 零热流量 + S1-I-7/X1-1 族）；validateManifest 每建 Set = once-per-creation；`?? []` 空数组默认分配 = R15-F 轴 3 引擎面 ns 域（mark/assert 腿 test-only 零流量） | 无候选 |
| `evaluation-card.ts` | 7 抛点；validate 数组字面量 = S1-B-7 域 | 无候选 |
| `dataset.ts` | 19 抛点；seen Set 复用 universe = S4-F-1；has+add 原形镜像去重 = S5-F-1（test-only 链维持，C5 本轮 14.2–14.5 µs 与历带交叠，§1(ii)）；membership Map 双结构 = 同域；rotateHoldout 双验 = S2-F-5 族 once-per-rotation | 无候选 |
| `attribution-report.ts` | 零抛点；**PAIRX 编排位点**——零字节未触、X0-11 双路对未拆分/未重排/未省略 | 无候选（共驻剂量归 R29-C） |

## 5. 候选总表

**空。** 本轮零候选：面格属主表第七次独立重推（§2）无一格松动且无新残格（BINDX 计数 10/8 第四次独立复证成立，坐标不动）；哨兵复核（§3）按定义不产出候选（不计时、不剂量）；十五文件枚举（§4）全部命中既有 ID 本体、R4-F 防御纵深保留域、R12-F/R20-F 已直测定价轴、R32-F/R33-F/R34-F 既有换名记录、姊妹归档面或停驻门成员——**零新站点级换名记录**。本轮无任何计时新臂 ⇒ 无可定价候选面。

## 6. CHECKS

- 分支恰 `cursor/r37-f-experiments-thirty-seventh-pass-83a1`，基线 `da2a01d`；**测量窗口零快进**（全部锚点 + 聚焦套件 + gate 在 `da2a01d` 树完成）；提交前 fetch 检测到一次纯 docs 快进 `da2a01d` → `f209ddd`（R37-E 摄取 + R37-G 派单；3 docs 文件，`src/` 0 行），按令 `git merge --ff-only` 零冲突快进并重验冻结、补读折入 R37-E；R37-D 在飞零触碰、未等待。
- 冻结三点核对全 **0 行**（分支创建时 / 快进前 / 快进后提交前）；15 文件普查 2550 行与 R29-F…R36-F 同数；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**（含 `f209ddd`）；`package.json` / `pnpm-lock.yaml` 对 `519101f` **0 行**；`git log -1 -- src/experiments/` 仍 = `519101f`。
- 锚点 r5f/r6f/r7f 各三次独立运行 + r1f 一次全部 0 失败（224 / 27 / 169 / 2668 项，§1）；四次序 + 三次序省额三轮全正，无符号翻转；全部在 nvm Node v22.22.2 上执行（每个官方批次前 `node --version` 在同 shell 复核；零批次落在系统 v22.14.0 上）；harness 重建后一次 r5f 冒烟运行按纪律弃置（全绿、值带内），官方口径为其后十批。
- 生产消费普查：4 个 importer 文件（import 行逐条核对）；catch 位点恰 1 处包切片调用（冷通道 once-per-eval，L336）；热消费者零 catch；142 抛点逐文件计数与 R31-F…R36-F 全同；切片零直抛 Error/TypeError。
- 姊妹面划界计数：BINDX `Math.*` **10** token 于 8 行（第四次独立复证；abs 4 + sqrt 4 + imul 2，行集合同一）；R31-E codec 位点 0；REPRX `JSON.parse` 位点 0；MODEX chmod/umask/八进制 mode 位点 0（宽式 `mode:` 命中仅 plan.ts 领域枚举字段）；node: 引入恰 2（isolation.ts node:path、holdout.ts node:crypto）；六链切片外消费者 0（精确交替式零命中）。
- 聚焦测试：`npx tsx --test test/unit/experiments/*.test.ts` = **75/75 pass**（10 suites；`da2a01d` 树一次 + 快进后 `f209ddd` 树一次，两次同计数）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**两跑全绿**（exit 0；一跑在 `da2a01d`、一跑在快进后 `f209ddd`）——test 1169 / suites 78 / pass 1168 / fail 0 / skipped 1（既有 provider-smoke 凭据跳过，与 R33-F…R36-F、R37-A/R37-B/R37-C/R37-E 记录同计数）；Node v22.22.2。
- 仓库改动恰本报告一个文件（`git status` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；live = R0 等价（R1 未接线）；promotion proposal-first；无 Outcome-supported 主张（Checkpoint F-PROD 开放，ADR-005）；无阈值/测试/权限/数据面/公开签名改动；CAS/凭据契约原样；无 fsync/完整性重哈希移除；fail-open 家族保持关闭；分析未变更任何 in-flight run；tracking 无命令权；H/score 未写路由 PASS/FAIL；catalog 行序契约（R28-B W3）与 npm POSIX `.bin` symlink 静默 no-op（R28-I）零触碰；EXCLUSIONS / PROGRESS / PLAN / README 未编辑；不开 PR。

## 7. 重开条件

本轮无新定价轴，不加行。R31-F 故障通道处置分解的三条显式重开触发（(a) 热交错内非平凡 catch 处置；(b) 非字符串 own 载荷；(c) 通道契约解冻）经 §3 哨兵直证全部未触发，原文全额维持；R30-F（输出消费）、R29-F（输出保留）、R17-F（拒绝终端）、S6-F-2 / S6-F-5 / S5-F-1 / S2-F-1 / S2-F-2 既有重开条件同样全额维持。锚点宿主刻度注记（本 VM 中段主机、r7f 档贴 R34-F 带、validate 组件 run 1 单读偏高、r7f validate 卫落带下侧但三轮同号为正，§1 注记 (i)/(ii)）不构成重开触发——冻结 0 行、省额符号三轮稳定、r1f 计数逐位同历轮、带并集近重合。本轮零新换名记录，不设新重开条件。

## 8. 新增排除 ID

**无。** 空枚举轮不铸 S37-F-\* ID、不铸 face 名、不取绰号、不加 EXCLUSIONS 行。本轮**零新站点级换名记录**（§0 裁决清单全部为既有行复命中——两处独立重瞄归 R20-F/R12-F/R15-F 直测属主；R37-A 三记录家族的 F 侧镜像面划界亦全部命中既有行）；R32-F 三项 + R33-F 四项 + R34-F 三项既有换名记录复核全部维持；R35-F / R36-F 零新记录与 R37-A/R37-B/R37-C/R37-E 空枚举均按令未重提、未重铸。

## 附录：仿真出处与运行摘录

本轮**未新建**任何仿真/见证脚本：全部计时证据来自入库血统资产的逐字节原样重跑（`scripts/round01-r1f-equivalence-sim.ts`、`scripts/round05-r5f-equivalence-sim.ts`、`scripts/round06-r6f-equivalence-sim.ts`、`scripts/round07-r7f-equivalence-sim.ts`，零改动零参数）；§2/§3 哨兵为只读 rg / 读码断言（不计时、无临时文件）。运行：`npx tsx scripts/round0X-*.ts`（nvm Node v22.22.2，PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开 `/exec-daemon` 系统 Node v22.14.0；harness 以 `pnpm install --frozen-lockfile` 重建；一次 r5f 冒烟运行验证启动后弃置）。

三次官方运行关键行摘录（r7f；r5f/r6f 对应行见 §1 表）：

```text
run 1: part E prefix saving 44.48ms | half 27.22 | scattered 4.21 | reversed 5.20
       validate x2000 saving 9.45ms | full experiment 130.06ms | 169 checks, 0 failures
run 2: part E prefix saving 45.96ms | half 27.18 | scattered 4.48 | reversed 4.51
       validate x2000 saving 7.32ms | full experiment 129.45ms | 169 checks, 0 failures
run 3: part E prefix saving 45.10ms | half 31.00 | scattered 5.23 | reversed 5.14
       validate x2000 saving 6.91ms | full experiment 128.53ms | 169 checks, 0 failures
```

r1f 摘录：`reference 714.5 ms -> current 62.4 ms (11.4x)`；`reference membership comparisons for the full run: 167,167,000`；`ALL EQUIVALENCE CHECKS PASSED (2668 bitwise checks)`。

MORE_OPTIMA=no
BRANCH=cursor/r37-f-experiments-thirty-seventh-pass-83a1
