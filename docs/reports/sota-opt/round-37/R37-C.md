MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 37 / R37-C：离线路由切片第三十七遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 37 / R37-C（Round 1–36 同区三十六遍
  之后的第三十七遍，叠在已落地的 S7-C 之上）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `9e1114a`
  （R36-J 空枚举摄取 + Round 36 收口 + Round 37 开轮 + 本 R37-A/B/C
  波派单）；锚点测量与门禁完成后 fetch 复核 exclusive 仍为
  `9e1114a`；提交前 fetch 发现独占分支 docs-only 快进**一次**：
  `9e1114a` → `e5338ef`（R37-A/R37-B 空枚举摄取 + R37-D/R37-E
  派单，增量仅触 docs/reports/sota-opt/ 下 4 文件 PROGRESS /
  round-37 PLAN / R37-A.md / R37-B.md，388 插入 23 删除）。该段
  增量对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接
  复核为**零行**；按令 `git merge --ff-only` 快进报告分支（纯快进
  零冲突）并重验冻结：`git diff 183df9b -- <9 切片文件>` 在分支
  创建点（`9e1114a`）、锚点测量前（`9e1114a`）、快进后
  （`e5338ef`）、提交前**四验均 0 行**。最强冻结再延一轮：
  `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交
  均为 docs）。`git log -1 -- src/routing/` 显示切片最后一次生产
  改动**仍为 `183df9b`「perf(routing): land S7-C support-only
  eta」**。新收口姊妹项 **R37-A 空枚举**（A 区第三十七遍从零重
  枚举 + 三项站点级换名记录拒列不铸 ID，预算重锚 13.1–16.3
  µs/gate ⇒ ~65–82 µs/run，run 3 前三格宿主干扰窗如实注记）与
  **R37-B 空枚举**（B 区第三十七遍从零重枚举，零新站点级换名
  记录，ceiling M=2 10.12–10.55 / M=10 18.01–19.80，replay M=2
  8.37–9.12；S12-B-2 / S13-B-1 重开条件经其重取证均未触发）已按
  快进后 PLAN 并入非移植清单并完成划界（§4）。全部锚点测量在
  `9e1114a` 工作树完成——快进增量 `src/` 逐字节零变化 ⇒ 测量形态
  与提交形态位级同一，全部有效；聚焦套件 + `pnpm gate` 在两个 tip
  均绿（计数逐字同）。
- **分支:** `cursor/r37-c-offline-routing-thirty-seventh-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，`wc -l` 逐文件 170/168/311/585/254/156/88/130/177 = 合计 2,039 行，与 R29-C..R36-C 普查同构；S7-C 支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开、S2-C 规范键 eta/mu 去重、S1-C bootstrap 工件复用全部现场核对在位）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 12.4 系（canonical，与
  R28-C..R36-C 同引擎；`/exec-daemon` PATH 暴露的系统 Node v22.14.0
  已显式绕开——PATH 前置 nvm bin 后 `node --version` 在每个官方批次
  前复核 v22.22.2，**零批次**落在 v22.14.0 上，无弃置批次；
  tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**第三十七组诚实枚举为空：不铸任何 S37-C-\* ID、不铸新 face 名、
不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，本轮生产代码零改动。**
对 9 个源文件逐行第三十七遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R36-C 的全部无号关闭
探针与轴，含 R31-C 的 **BINDX**、R30-C 的 REPRX、R29-C 的 PAIRX、
R28-C 的基片实例化、R27-C 的 PROBX、R19-C 的 FITQ、R31-J 的
**MODEX**，及 R32-C/R33-C/R34-C/R35-C/R36-C 的七项换名记录——
updatePosterior pow 站点、values.sort 数值特化、lnGamma 反射分支
死码、退化结局双 `every` 融合、propensity `isFabricatedPositiveSupport`
`notOneHot` 被吸收析取、posterior `estimateForKey` 双
`weightedSampleSize` CSE、r1 `routeR1` cheapest-`reduce` decorate-carry，
按派单**均不重提**）后，本轮**从未点名站点数为零（连续第十九遍）、
新增换名检查记录为零**——每一个执行站点都能指到既有 ID、无号探针、
零池备忘或池算术覆盖。本轮两处独立重瞄均在核对后归于既有属主，
引用即止不立新记录：(i) r1-shadow-report L105 `exploratory !== false`
双卫（类型面恒假、运行时保真的 per-episode 防御卫）——归
S8-C-3/S8-C-4 fail-closed 跨函数不变量见证家族，ns 级零成本保留；
(ii) posterior `regularizedIncompleteBeta` 分支阈 `(a+1)/(a+b+2)`
在 80 次二分内的逐迭代重算——X1-3 冻结数值路径本体（与 R36-C 表
「前缀重算」同函数同属主）。R26-C/R27-C 关掉输入数值坐标、R28-C
关掉执行基片坐标、R29-C 关掉工作负载组成坐标、R30-C 关掉输入表示
坐标、R31-C 关掉名字解析坐标、R32-C 独立重推元坐标系无余项、
R33-C..R36-C 复推后各加换名记录之后，本轮再次独立重推，**无任何
「从未受控 + 独立物理对象 + 真尺度」三条同时成立的剩余坐标**：
C 切片是进程内纯同步数值内核，零 fs 数据路径（MODEX / R31-E codec
面结构性空，§4 计数直证）、零 `JSON.parse` 输入位点（REPRX 划界
维持）、零 OS 边界（R31-I 审计平面正交）。

按派单在本 VM 重锚默认预算（未拷贝历轮数字，装具为 R31-C 附录
A.1 逐字节血统，仅头注更新，§附录）：

- **生产中位带（本轮正典）：658.6–660.2 ms/报告**（四次运行
  657.3 / 659.4 / 658.6 / 660.2；正典三读数 658.6–660.2）——与
  R35-C 偏快主机带 657.9–662.3 交叠（658.6–660.2 全段落内）、与
  R32-C 带 659.0–662.0 交叠（659.0–660.2）、与 R29-C 带
  657.1–659.6 交叠（658.6–659.6），低于 R36-C 中段主机带
  667.9–681.0 约 9–21 ms：本 VM 属战役窗口的**偏快主机**，按派单
  「host-bias windows with freeze 0 lines are not new findings」
  判读为宿主刻度项，非切片变化。**同 VM 独立旁证钉死结构结论**：
  r7c 回归资产锚点两次通过 **production 658.0 / 659.6 ms = 1.12× /
  1.11× vs 冻结 S6-C ctl（第二次完整块打印 ctl 734.8）**——比率与
  R28-C..R36-C（1.11–1.12×）逐轮不动；切片对 `183df9b` 零 diff，
  装具协议逐字节同 R31-C..R36-C。按 R14-C 处置规则随读重锚为本轮带。
- **small 带 29.9–30.1 ms**（R35-C 30.0–30.3 交叠 30.0–30.1、
  R36-C 30.4–34.1 带缘相邻 30.1 对 30.4）；整条 small 报告低于
  ±35 ms 噪声带（R7-I 矩阵义务：该档只作确定性与保真见证，不作
  候选判据）。
- **APCX / FLOORX / 组合天花板**：APCX 无计时器独立道正典
  60.0 / 60.5 / 60.3 ms（R34-C 带 60.1–68.6 交叠 60.1–60.5；
  预演 60.8 同层）；FLOORX 正典 31.4 / 32.3 / 32.3 ms（R36-C 带
  31.5–44.3 交叠 31.5–32.3、R35-C 带 31.6–40.6 交叠 31.6–32.3）⇒
  **组合天花板 = 28.5 / 28.2 / 28.1 ms < 35**——正典带 28.1–28.5
  落在 R35-C 正典带 22.3–27.7 与 R34-C 正典带 28.7–31.6 之间
  （对 27.7 与 28.7 双侧带缘相邻），完整落于 R36-C 正典带
  20.9–34.1 之内；预演天花板 29.2 按历轮惯例不入正典带；四读数
  全部 < 35：组合出路维持关闭。
- **sink = floorSink = 7.309 八读数全同**（4 运行 × 2 道，每运行
  内 `Object.is(apcSink, floorSink)` 断言全绿）——与
  R14-C/R16-C..R36-C 打印值逐位同值，跨轮确定性旁证成立：主机
  差异动不了任何输出位。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等；每运行
  2,768 项位级检查全绿，四运行合计 **11,072 项**。

回归资产与门禁（§5）：r1c–r7c 七脚本全绿，检查数逐一匹配锚定值
**8028 / 14420 / 14730 / 24888 / 28555 / 25483 / 6193**；聚焦
离线路由套件 59/10/59/0（`9e1114a` 与 `e5338ef` 各一次，逐字同
计数）；`pnpm gate` 在 `9e1114a` 跑两次、`e5338ef` 跑一次均
exit 0（tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1，
三次逐字同计数，与 R32-C..R36-C 同计数）。

硬性不变量逐条复核（§0）：双 LCB 在位（posterior.ts L22
`LcbKind = "normal" | "beta-quantile"`，本轮 rg 复核）；双归因
在位（attribution-report.ts L18–19 `logit: fitLogitAdditive(rows),
probAdd: fitProbabilityAdditive(rows)`，现场核对）；live = R0
等价（`src/supervisor/` 对 9 切片文件导入为零，本轮 rg 复核，
R1 未接线）；S8-E-1 `loadLearnedRouting` 调用点恰 3 处（track/loop
L88 + flowchart-run L712 + cli/main L708，本轮 rg 复核）；推广
proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，
ADR-005）；无阈值改动；无测试改动；CAS/凭证/数据面合同/公开签名
零触碰。仓库变更仅本报告一个文件；无生产赢家故未提交任何临时装具
（血统锚定装具指纹见附录，临时文件归档后已删除）。空枚举与
R8-C..R36-C 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → EXCLUSIONS.md（完整表 545 行，
  含全部 S\*-C-\* 行与 S12-C-1、S13-B-1）→ PROGRESS.md →
  round-37/PLAN.md（C 段 + 具名/unnamed 全 ban 榜）→ R36-C.md
  全文（空枚举确认，无 S36-C-\*；一项换名检查——r1 `routeR1`
  cheapest-`reduce` decorate-carry——属主记录在案，本轮不重提）→
  9 个切片源文件全量重读 → `src/experiments/attribution-report.ts`
  消费图现场核对 → R31-C.md 附录 A.1（装具血统提取源）→ 快进后
  补读 R37-A.md 与 R37-B.md 结论（A/B 区空枚举，与 C 零共享物理
  对象）与更新 round-37/PLAN.md（C 段默认对照不变，非移植清单
  增列 R37-A、R37-B 空枚举）。
- 基线漂移检查：本轮测量期起点 = 分支创建点 = `9e1114a`；锚点
  测量与门禁完成后 fetch 复核 exclusive 仍为 `9e1114a`；提交前
  fetch 发现 docs-only 快进（`e5338ef` R37-A/R37-B 合入 +
  R37-D/R37-E 派单）——以 `git diff --stat` + 对 `src test
  scripts package.json pnpm-lock.yaml` 的直接 diff = **0 行**
  逐项核对后按派单规程 `git merge --ff-only` 快进报告分支（纯
  快进零冲突）并重验冻结。冻结主张四次核验均 **0 行**（切片
  9 文件 vs `183df9b`：分支创建时、锚点测量前、快进后、提交前）；
  最强冻结 `89f0455..HEAD -- src/` 为空。测量在字节相同的切片上
  完成，全部有效。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：
  input object-representation / **REPRX**（切片 `JSON.parse` 输入
  位点 0，§4 计数）/ **BINDX**（R31-C；realm 级绑定态——本轮零
  patch、零剂量、零计时新臂；C1 capture-hoist 不重提；划界计数见
  §4）/ **MODEX**（R31-J——inode 权限模式字拓扑，J 区持久工件面；
  本切片数据路径零 fs 位点，面结构性空，§4 计数后声明即止）/
  **PAIRX**（工作负载组成——本轮全部测量单内核恒定，姊妹估计器
  零计时）/ KFAN / COTARG / PROBX / DELINV / PRICEX / VALCLASS /
  **R36-C 换名记录**（r1 `routeR1` cheapest-`reduce` 折叠成本
  decorate-carry，按派单不重提、不重铸——本轮独立重读 r1.ts
  L119–L121 后属主引用即止，§2.1 r1 行）/ **R35-C 换名记录**
  （posterior `estimateForKey` 双 `weightedSampleSize` CSE，
  不重提）/ **R34-C 换名记录**（propensity
  `isFabricatedPositiveSupport` `notOneHot` 被吸收析取，不重提）/
  **R33-C 两项换名记录**（lnGamma 反射分支死码消除与
  fitLogitAdditive 头部退化双 `every` 融合，不重提）/ **R32-C
  两项换名记录**（updatePosterior pow 站点、values.sort 数值特化，
  不重提）/ **R37-A 空枚举**（快进带入——A 区第三十七遍从零重
  枚举 + 三项站点级换名记录拒列不铸 ID，全部落 tracking 面物理
  对象，与 C 零共享物理对象，声明即止）/ **R37-B 空枚举**（快进
  带入——B 区第三十七遍从零重枚举，零新站点级换名记录，两残余
  角度均为 B 文件已点名站点复命中；S12-B-2 / S13-B-1 重开条件
  经其重取证均未触发，与 C 零共享物理对象，声明即止）/
  **R36-A..R36-J 空枚举**（Round 36 十切片收口，开轮时已合入——
  全部为方法卫生 / 空枚举记录，未铸新面；R36-A 三项换名检查、
  R36-D 四项换名检查、R36-G 一项换名检查全在他区物理对象上，
  与 C 零共享物理对象，声明即止）/ **R33-A..R33-J、R34-A..R34-J、
  R35-A..R35-J 空枚举**（三十切片方法卫生记录，与 C 的携带由 0 行
  冻结 + 锚点保真电池 + 本轮逐文件重读覆盖，不重演账本分类、不重走
  闭合稳定性审计、不重铸「再查一遍重开条件 / 闭合稳定性 / 门扉」式
  新面）/ **R32-A 重开条件哨兵审计**（方法不重铸——C 档案在案重开
  条件均要求 src/ 变更或规模增长，由 0 行冻结与锚点保真电池自然
  覆盖）/ **R32-B 闭合稳定性审计**（不重走）/ R31-I CLI 进程边界
  门扉闭合审计（本切片零 argv/env/stdin/stdout/子进程/信号/退出腿，
  审计平面正交零交集）/ digest / sink=7.309 位等不另铸轴（本轮
  sink 八读数只作既有跨轮见证复现）。FITQ / NAMESHAPE / SCALEX /
  XPROC / YMIX / SCHEDWIN / NVG / SEEDX / TERMCLASS / SHAPEK /
  ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / CG / AGEX / RTL /
  RVL / SIDEC / PWL / ZREP / CSPELL / LINKTOP 全部未编号未移植。
  R28-C 基片实例化未移植（全部测量锁死单一基片 tsx+src）；R30-I
  时间段拓扑、R30-J LINKTOP、R31-A/B/D/F/G/H 停驻门与属地格未开。
  X\* / S1-\* … S33-\* 全部未重开；S12-C-1 重开条件未触发（重开
  条件：无）；S6-C-3/S6-C-4 重开条件（支撑分布大改 / 因子数增长
  使支撑上界 ≥ ~8）经锚点夹具形状见证未触发（支撑上界仍 5）；
  S13-B-1 仍为 loser ID（B 区属地，未触）；S12-B-2 仍未触发
  （B 区属地）；S7-F-1 ≠ S6-F-5、S5-H-1 保持、S8-I-1 file-level
  封死、S9-D-4 / S12-D-1 永不再开、S8-E-1 禁止去重（均他区属地，
  未触；S8-E-1 恰 3 处本轮 rg 直证）；catalog 行序契约决策承载
  （R28-B W3，B 区属地，未触）；npm POSIX `.bin` symlink 静默
  no-op 未当优化修（R28-I，I 区属地）；未起 S7-C 平行实现（X2-1
  引理前提复核成立：0/1 设计、+0.0 起点、有限 beta——FLOORX 道
  逐名值对 APCX 道 `Object.is` 断言全绿即为可执行见证）。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
  不开 PR。输家脚本不入库（本轮无输家道——除血统锚定装具外零
  计时新臂）。

## 1. 生产中位与锚点复核（本 VM 实测，未拷贝历轮数字）

测量装具与 R31-C 附录 A.1 逐字节血统（replica 保真电池 34 例 +
自决定性 + 中位重锚 + APCX/FLOORX；仅头注更新为 R37-C，diff 即
6 行注释，指纹见附录）；Node v22.22.2，tsx + src 直连（R28-C S
臂 = 历史正典基片）。1 次预演 + 3 次正典独立进程，全部在
`9e1114a` 工作树完成：

| 运行 | perf 中位 (ms) | small 中位 (ms) | APCX (ms) | FLOORX (ms) | 组合天花板 (ms) | sink / floorSink | 检查 |
|---|---|---|---|---|---|---|---|
| 预演 | 657.3 | 30.1 | 60.8 | 31.7 | 29.2 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 1 | 659.4 | 30.1 | 60.0 | 31.4 | 28.5 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 2 | 658.6 | 30.1 | 60.5 | 32.3 | 28.2 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 3 | 660.2 | 29.9 | 60.3 | 32.3 | 28.1 | 7.309 / 7.309 | 2,768 ✓ |

perf 逐 rep（7 rep/运行）：预演 660.6 659.0 657.3 657.1 662.6
655.8 655.5；正典 1 664.9 660.6 657.4 656.6 659.4 656.3 659.6；
正典 2 658.6 665.2 657.7 657.4 660.6 658.1 661.7；正典 3 662.0
663.1 659.7 658.0 659.6 660.2 664.5。

- **生产中位带（本轮正典）：658.6–660.2 ms**。与 R35-C 偏快主机
  带 657.9–662.3 全段落内、与 R32-C 带 659.0–662.0 交叠
  （659.0–660.2）、与 R29-C 带 657.1–659.6 交叠（658.6–659.6），
  低于 R36-C 中段主机带 667.9–681.0 约 9–21 ms——本 VM 属战役
  窗口的偏快主机（正典逐 rep 散布 656.3–665.2，层内极紧致），
  是 0 行冻结下的宿主刻度项，非切片变化，亦**不铸主机偏置新发现**
  （派单明令）。三重独立旁证钉死「非切片变化」：(i) 切片对
  `183df9b` 逐字节零 diff；(ii) r7c 回归资产（§5）同 VM 两次通过
  打印 production 658.0 / 659.6 ms = **1.12× / 1.11×** vs 冻结
  S6-C ctl（第二次完整块 734.8）——与 R28-C..R36-C 逐轮同比率；
  (iii) sink 八读数逐位同历史值（输出位不动）。按 R14-C 处置规则
  **随读重锚为本轮带 658.6–660.2**，结构结论（层级、比率、天花板、
  确定性）全部与历轮同构。
- **small 带 29.9–30.1 ms**（R35-C 30.0–30.3 交叠 30.0–30.1、
  R36-C 30.4–34.1 带缘相邻）；整条 small 报告低于 ±35 ms 噪声带，
  只作确定性与保真见证。
- **APCX 正典带 60.0–60.5**（R34-C 60.1–68.6 交叠 60.1–60.5；
  与主机偏快判读一致地落带底层）；**FLOORX 正典带 31.4–32.3**
  （R36-C 31.5–44.3 交叠 31.5–32.3、R35-C 31.6–40.6 交叠
  31.6–32.3）；**组合天花板正典带 28.1–28.5 < 35**（介于 R35-C
  正典带 22.3–27.7 与 R34-C 正典带 28.7–31.6 之间、完整落于
  R36-C 正典带 20.9–34.1 之内；预演 29.2 按历轮惯例不入正典带——
  APCX 与 FLOORX 同 VM 同窗对减，四读数差值仍全数 < 35，组合出路
  维持关闭）。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等（含
  replica 保真电池 34 例 + perf + small 对生产 import 的全字段
  断言），每运行 2,768 项检查全绿，四运行合计 11,072 项。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is(apcSink, floorSink)`）——与 R14-C/R16-C..R36-C
  打印值同值；X2-1 引理前提（0/1 设计、+0.0 起点、有限 beta）
  由 FLOORX 道逐名 `Object.is` 断言随运行复核成立。

## 2. 第三十七遍枚举（诚实结果：空；零新站点 + 零新换名记录）

### 2.1 逐文件重读与裁决（零未点名站点，连续第十九遍）

| 文件 | 本轮重读检查（全部命中既有 ID / 无号探针 / 零池） | 结论 |
|---|---|---|
| `r1.ts`（170 行） | 每调用 `modelsById` Map 构造、config spread、estimates spread、`tierIds` spread、`lcb.toFixed(4)` 返回路径格式化 = S1-C-10 本体；sampled/aboveFloor 双 filter = S1-C-5；costOf/tierIndex/`aboveFloor.find(previousModelId)` 线性 find = X1-4 本体（M≤10 噪声级）；cheapest `reduce` 折叠累加器 `costOf` 重算的 decorate-carry 消除 = **R36-C 换名记录属主**（本轮独立重读后属主引用即止，不重提不重铸）；prepared 观测索引 = 已落地 A2/B1；跨 episode 记忆化 = X1-6 | 无新候选 |
| `r1-shadow-report.ts`（168 行） | 每 episode request spread + routeR0 调用（B 区文件不触碰）；merge 路径 = S1-C-2（跨索引播种）+ S1-C-3（公开 ReadonlyMap 形状）；selectedCost find = X1-4 族 + PRICEX 封顶；与 shadow-compare 合并 = X1-5；sanitizeClaims once-per-report；空 episodes / 零 records 故障腿 = R18-C once-per-fault 拒绝类；L105 `r0.exploratory !== false \|\| r1.exploratory !== false` 双卫（类型面恒假、运行时保真）= **本轮独立重瞄 1**，归 S8-C-3/S8-C-4 fail-closed 跨函数不变量见证家族（per-episode ns 级零成本保留，删除侧无收益池），属主引用即止 | 无新候选 |
| `posterior.ts`（311 行） | 80 次二分 / Newton / 闭式 = X1-3；lnBeta 每 quantile 一次 = 已落地 C1；estimateFingerprint = S1-C-4；groupObservationsByKey = 已落地；betacf 守卫 Math.abs = BINDX 坐标（计数即止）；updatePosterior pow 站点 = R32-C 换名记录 1 属主（不重提）；`lnGamma` 反射分支死码 = R33-C 换名记录 1 属主（不重提）；`oneSidedTail(_z)` 死形参 = R17-C OSTZ 零池备忘属主；posteriorMean/Variance `total>0` 卫 = S8-C-4 fail-closed 数值卫家族；regularizedIncompleteBeta 前缀重算 = X1-3 冻结数值路径本体；分支阈 `(a+1)/(a+b+2)` 逐迭代重算 = **本轮独立重瞄 2**，同函数同属主 X1-3（80 次二分冻结体内的常量表达式重算，改动即触数值路径重构禁令，且 ns×80/quantile 深度亚带），属主引用即止；`estimateForKey` 缺键 `?? []` 分配 = 落地 memo 体内 once-per-(key,fingerprint) 分配级零池；`samples`/`wellSampled` 双 `weightedSampleSize` = R35-C 换名记录 1 属主（不重提）；betaQuantileLcb 头部 p/参数卫 = fail-closed 卫家族 once-per-quantile ns 级 | 无新候选 |
| `offline-logit.ts`（585 行） | S1-C（bootstrap 工件复用）/S2-C（规范键 eta/mu 去重）/S3-C（单位乘法消除）/S6-C（switch 直线化）/S7-C（支撑升序直加）落地形态逐站点在位；头部退化结局双 `every` 融合 = R33-C 换名记录 2 属主（不重提）；draw 侧塌缩检查融合 = S2-C-5 本体；`beta.every(Number.isFinite)` = S7-C-2/S8-C-3；delta map+reduce = S2-C-3；per-key w / per-(key,y) z 去重 = S3-C-2（本轮复核 `w=max(mu(1-mu),1e-10)` 仍为 per-key 纯函数，属主不动）；`xtwx fill(0)` = S5-C-7；dot 展开 = S5-C-5（串行依赖）；onProbabilitiesFor = S7-C-4/S8-C-1；APC off 道全系 = S1-C-1/S2-C-2/S4-C-4/S8-C-2/S10-C-2（本轮组合天花板 28.1–28.5 < 35 维持关闭）；percentile 换 quickselect = X4-3 族；draws 循环 `.entries()` = S7-C-1；draws 预分配/pushValue = S1-C/S3-C-3 分配级；`values.sort` 数值特化 = R32-C 换名记录 2 属主（不重提）；buildDesign/computeSupports/canonicalRowKeys/columnIndex 元组物化 once-per-report 构建（含 dropLast 闭包与 build() 逐行末元素读）= S1-C 落地后残余亚带池（R12-C 亚带分类）；诊断尾 `interactionsFor` 嵌套 filter、`e.name.slice` 闭包内重算、`ZERO_EPS` 常量每报告重乘、`successful < MIN_SUCCESSFUL_DRAWS` 循环不变量逐效应重判、mean reduce、双 Set 基数 = 尾部 once-per-report 亚噪声（R33-C..R36-C 表同款）；IRLS 收敛范数 sqrt = R17-C NSQRT 关闭探针；采样环 `Math.floor(random()*n)` = 冻结 RNG 契约（mulberry32 逐位再现义务）；worker/WASM = S9-C-1/S9-C-2；嵌套序交换 = S12-C-1（重开条件：无）；per-fit distinct-key 代表表 = S10-C-1；stamp Int32Array 换窄宽度 = S7-C-3 零效应家族 | 无新候选 |
| `offline-prob-add.ts`（254 行） | X3-4 + R17-C 零池；cell 双算合一 = S1-C-6；站点规范键去重 = S2-C-1；betaInterval 记忆化 = S1-C-7；betaInterval 上分位第二次 80 二分 = X1-3 本体 + S1-C-7 亚 ms 池；betaInterval lcb/ucb 跨分位双 `lnBetaFunction` = R17-C/R18-C 已记属主（属主在案，不立新记录）；kappaS 输入 `[...values(),...values()].map(cell)` 物化 = S1-C-6 邻域 once-per-report 零池；死 `pM`/`pMp` shrink（`void` 保留）= R17-C PMV 零池备忘属主（冻结公式文档，行为一位不动）；diagnose 内 `[...entries()]` 每效应重物化 + `lastSegment` split 逐 key 重算 = once-per-report 零池覆盖；`lastSegment` 死首参 = R33-C 表已记纯文本项（零池，不立记录） | 无新候选 |
| `propensity.ts`（156 行） | 三遍改单遍 = X3-5 + S1-C-8 本体（weights 双 reduce 融合同属）；`isFabricatedPositiveSupport` `notOneHot` 被吸收析取 = R34-C 换名记录属主（按派单不重提）；behaviorMu/targetPi 访问器 = R27-C PROBX 冻结坐标（JIT 平凡内联）；validate 故障腿 = once-per-fault REJX 类（R18-C）；R21-C 零池维持 + R27-C 通量普查（生产调用 0 次/报告）维持 | 无新候选 |
| `lin-alg.ts`（88 行） | S4-C（行引用提升）/S5-C（按四展开）落地形态在位；变体空间 = S4-C-1..3 + S5-C-1..7 + S10-C-3（对称 Schur：部分主元破坏对称）+ S12-C-1 封闭；`stop=n-3` 提升 / `a.some` 手写 = S6-C-7；回代 eps 卫 = S8-C-4（fail-closed 数值卫）；回代串行链 = S5-C-5；尾部 `Number.isFinite` 终检 = 既有两站点普查之一（§4 计数） | 无新候选 |
| `bandit.ts`（130 行） | recordReward/recordExploration 拷贝追加 = X4-2 readonly 契约；`arms.includes` / meanReward 线性 = S1-C-9（arms 个位数）；selectArm 贪心循环 arm-0 重评 = S1-C-9 本体 ns 级；探索臂 `% arms.length` 冗余模 + `?? state.arms[0]` 恒不触发回退 = S8-C-4 fail-closed 卫家族（index 恒在界内，卫零成本保留）；createBanditState `[...arms]` 防御拷贝 = 契约本体；validateTaskFeatures includes = S1-C-9 族 | 无新候选 |
| `shadow.ts`（177 行） | decisions 追加拷贝 = X4-2 本体；halted 分支整 decision 字面量重建 / breached once-per-fault 路径 = R16-C/R18-C 拒绝类；每 step config 字面量 = S1-C-9 本体；roundUsd/预算扣减 µs 域 = S1-C-9 族 | 无新候选 |

结构位置 × 消除机制满矩阵（死码/常量折叠/CSE/记忆化/数据结构/
融合/早退/免分配/复杂度类/强度削减/惰性/平行路径/形状单态化）
逐格过筛：非空格全部落在上表既有 ID 或既有换名记录属主上，本轮
**无任何一格需要新记录**。

### 2.2 本轮新换名检查记录：无

两处独立重瞄（§2.1 表内标注）均在核对后归于既有属主——
r1-shadow-report `exploratory` 双卫归 S8-C-3/S8-C-4 fail-closed
跨函数不变量见证家族（ns 级零成本保留，无收益池）；posterior
二分分支阈重算归 X1-3 冻结数值路径本体（同函数「前缀重算」既有
属主的同体延伸）。两处均属主引用即止，**不立新换名记录、不铸
ID**。R36-C 的 §2.2（decorate-carry）为该文件族最后一个独立
形态角度，本轮重读后无后继。

### 2.3 枚举收口

第三十七遍逐文件重读**零未点名站点（连续第十九遍）**、零新换名
记录。R12-C 四分类（重命名 / 亚带池 / 逐位违例 / 结构交换负优化）
+ R18-C once-per-fault 拒绝类第三十七遍无余项。**第三十七组枚举
为空——比强铸败者 ID 更诚实。**

## 3. 三大池地板与组合出路复核

- **APCX 池**（59 非截距名 × 400 行 APC 扫掠 × 201 fit）：无
  计时器独立道 0.303 / 0.298 / 0.301 / 0.300 ms/fit ⇒ 60.8 /
  60.0 / 60.5 / 60.3 ms/报告——正典带 60.0–60.5，与 R34-C 带
  60.1–68.6 交叠 60.1–60.5（主机偏快窗口判读见 §1）。
- **FLOORX 池**（S8-C-2 支撑升序 off-sum 形态，从未落地——X2-1
  引理前提本轮复核成立；逐名值对 APCX 道 `Object.is` 断言全绿）：
  0.158 / 0.156 / 0.161 / 0.160 ms/fit ⇒ 31.7 / 31.4 / 32.3 /
  32.3 ms/报告地板。
- **组合天花板 = APCX − FLOORX = 29.2 / 28.5 / 28.2 / 28.1 ms
  < 35**——正典带 28.1–28.5 介于 R35-C 正典带 22.3–27.7 与
  R34-C 正典带 28.7–31.6 之间（双侧带缘相邻），完整落于 R36-C
  正典带 20.9–34.1 之内；预演 29.2 为本轮最高读数，按历轮惯例
  预演不入正典带；四读数全部 < 35：组合出路维持关闭。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is`）——与 R14-C/R16-C..R36-C 打印值同值，派单强制的
  跨轮确定性旁证成立。
- 零池备忘维持：posterior/propensity/bandit/shadow 四文件全部
  站点在既有 ID、无号探针（含 R17-C PMV/OSTZ/NSQRT 与
  betaInterval 双 lnBetaFunction 记录）覆盖下，本轮重读零新记录、
  无余项。

## 4. 姊妹面划界（计数即止，零剂量零计时）

1. **R37-A 空枚举（本轮快进带入）**——A 区第三十七遍从零重枚举
   （14 文件 1796 行，基线 `7acb666` 三验 0 行）+ 三项站点级换名
   记录拒列不铸 ID + 预算重锚 13.1–16.3 µs/gate ⇒ ~65–82 µs/run
   （run 3 前三格宿主干扰窗经卫生复跑证为瞬态，如实注记）。与 C
   切片零共享物理对象（其 14 文件全在 tracking / gate 面，与本
   切片零交集）。声明即止，不移植、不重做、不铸新轴。
2. **R37-B 空枚举（本轮快进带入）**——B 区第三十七遍从零重枚举
   （10 文件 1359 行，基线 `94ed3d9` 两点冻结 0 行）+ 零新站点级
   换名记录（枚举尾部两残余角度均为 B 文件已点名站点复命中）+
   天花板重锚 ceiling M=2 10.12–10.55 / M=10 18.01–19.80
   ms/eval、replay M=2 8.37–9.12；S12-B-2 重开条件腿 (a) 与
   S13-B-1 重开条件经其重取证均未触发。与 C 切片零共享物理对象
   （其 10 文件全在 live 路由 / model-router 面；`routeR0` 是
   B 区文件，本切片 r1-shadow-report 仅消费其公开导出，双方均零
   改动）。声明即止，不移植、不重做、不铸新轴。
3. **R36-A..R36-J 空枚举（Round 36 十切片收口，开轮时已合入）**——
   全部为方法卫生 / 空枚举记录，未铸新面。R36-C 一项换名检查
   （r1 `routeR1` cheapest-`reduce` decorate-carry）本轮按属主
   引用（§2.1 r1 行），不重提、不重做、不铸新轴。R36-A 三项、
   R36-D 四项、R36-G 一项换名检查全在他区物理对象上，与 C 零共享
   物理对象。R33-A..R33-J、R34-A..R34-J、R35-A..R35-J 同理。
   声明即止。
4. **BINDX（R31-C）**——切片环境内建 token 静态普查（划界计数，
   非剂量）：`Math.*` 恰 **42 token**，其中函数内建 12 个
   （abs 9 / max 7 / log 6 / min 3 / floor 3 / exp 3 / sqrt 2 /
   imul 2 / sin 1 / round 1 / pow 1 / ceil 1）+ 值常量
   `Math.PI` ×3；`Number.isFinite` 恰 **2 站点**（offline-logit
   L339 `beta.every` + lin-alg L85 终检）——与 R31-C..R36-C
   十二内建 + 两站点普查逐名同构，realm 坐标未动、零 patch、
   零计时。C1 capture-hoist 不重提。
5. **REPRX（R30-C）**——切片 `JSON.parse` 输入位点 **0**（全部
   输入经内存对象抵达）——划界维持，声明即止。
6. **MODEX（R31-J）**——9 文件对 `node:fs / readFile / writeFile /
   chmod / umask / mkdir / appendFile` 命中 **0**——切片数据路径
   零持久工件、零铸盘动词，面在 C **结构性空**（R31-E codec 面
   同一计数同一结论）。声明即止。
7. **PAIRX（R29-C）**——本轮全部测量单内核恒定（perf/small 两
   夹具即历轮锚定形状），姊妹估计器零计时，工作负载组成坐标
   未动。声明即止。
8. **R31-I CLI 进程边界门扉闭合审计**——本切片零 OS 进程边界
   腿（无 argv/env/stdin/stdout/spawn/signal/exit），审计平面
   正交零交集，不移植。

## 5. 测试与验证

```
# 聚焦离线路由套件（tsx --test，7 文件）
# 在 9e1114a / e5338ef 两 tip 各跑一次：
#   tests 59 / suites 10 / pass 59 / fail 0  （两次逐字同计数，
#   与 R31-C..R36-C 同计数）

# 全门禁 pnpm gate = typecheck + lint + test + build
# 在 9e1114a 跑两次、e5338ef 跑一次，均 exit 0：
#   tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#   （三次逐字同计数，与 R32-C..R36-C 同计数；tsc build 零诊断）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门；
# 9e1114a 工作树，nvm Node v22.22.2）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项（design.build 358,006/拟合）
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项（eta dots 3,586,400 → 1,249,652 去重）
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点（两次通过，检查数两次同为 6,193）：
#   production 658.0 ms = 1.12× vs ctl / 659.6 ms = 1.11× vs
#   ctl 734.8（第二次完整块）。与 §1 生产正典中位带互证：比率与
#   R28-C..R36-C（1.11–1.12×）逐轮不动，绝对刻度归宿主（本 VM
#   偏快，ctl 734.8 与 R36-C 宿主的 735.7/736.9 同层）。

# 本轮测量装具（临时脚本，未入库——无生产赢家；来源与指纹见附录）
# 主锚 ×4：11,072 项位级检查全绿（2,768 × 4）；
#          sink/floorSink 8/8 = 7.309（进程内 Object.is）
```

冻结四验（§0）；仓库变更 = 本报告一个文件（提交前
`git diff origin/cursor/sota-persistent-opt-83a1...HEAD --stat`
复核仅 `docs/reports/sota-opt/round-37/R37-C.md`）。无生产落地
（派单条件句未触发），未铸新仿真脚本，临时装具已从 /tmp 删除。

## 6. 新增排除 ID：无

- 不铸 S37-C-\*：第三十七组枚举为空；两处独立重瞄均归既有属主
  （S8-C-3/S8-C-4 fail-closed 卫家族；X1-3 冻结数值路径本体），
  不越 ±35 ms 带，无任何站点达标。
- 不写入 EXCLUSIONS.md 任何行；不铸新 face 名；不取 ALLCAPS
  绰号（含对 MODEX / BINDX / REPRX / PAIRX 的一切再编号）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态，未重开 S12-C-1
  （重开条件：无），未起 S7-C 平行实现，未重铸 R32-A 哨兵审计
  方法，未重演 R33-A 账本分类、未重走 R32-B 闭合稳定性审计，
  未重提 R32-C/R33-C/R34-C/R35-C/R36-C 七项换名记录。

## 附录：测量装具来源与指纹（未入库；运行后已删除）

- 装具：`/tmp/r37c/r37c-anchor.mts`，**814 行**，由本仓
  `docs/reports/sota-opt/round-31/R31-C.md` 附录 A.1 代码块
  （L555–L1368）逐字节提取，唯一改动为头部块注释 6 行更新为
  R37-C 说明（协议体、夹具、seed、断言零改动——R31-C A.1 本身
  即 R30-C/R14-C 血统逐字节协议；提取后对原文 diff 复核恰 6 行
  注释：L2–L5 轮次说明 + L29–L30 零新计时臂声明）。
- sha256（归档时点）：
  `395e775ae3c4f3bcf86e1f298d213c0c42fe90c8e796070293d9fdf0f2eba1f1`
- 运行方式（仓库根，nvm Node v22.22.2 + tsx 4.23.12，
  `pnpm install --frozen-lockfile` 后，PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 绕开系统 Node v22.14.0）：

```bash
node_modules/.bin/tsx /tmp/r37c/r37c-anchor.mts   # ×4（1 预演 + 3 正典，独立进程）
```

- 输出摘要（四运行）：§1 表格与逐 rep 行为全量记录；每运行尾行
  `ALL EQUIVALENCE CHECKS PASSED (2768 bitwise checks)`。
- 本轮**零新增计时臂**：无绑定/表示/组成/基片/模式剂量，无决斗
  道，无输家脚本——附录无新增源码块（装具全文已在 R31-C 附录
  A.1 归档，本轮仅头注差异，不重复占档）。

MORE_OPTIMA=no
BRANCH=cursor/r37-c-offline-routing-thirty-seventh-pass-83a1
