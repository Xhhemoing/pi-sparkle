MODEL_SLUG=claude-fable-5-thinking-xhigh

# R12-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第十二遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 12 / R12-A（Round 1–11 同区第十二遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `30b6813`（含 R11-I 收口与 Round 12 开轮）
**分支:** `cursor/r12-a-tracking-twelfth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S12-A-\* ID，切片零代码改动；
整片预算收口第十次复核成立，并新增「合同地板分解」把收口从总量级下探到
逐组件级。** 切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R11-A 至
本轮基线（`30b6813`）**逐字节未变**（`git diff 7acb666..30b6813 --
src/tracking src/run/child-tracking.ts src/run/gate-apply.ts` 为空，十二遍
全程零 diff）。调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..30b6813 -- src/` **完全为空**（跨 R10/R11/R12 三轮全部
为 docs 提交），生产调用方经全库 grep 复核仍为 `supervisor.ts:483`
（applyTrackingGate/nextTrackingSeq；两个 settle 调用点 601/711 均不传
trackingAssessment ⇒ 生产 O(1) no-op，R9-A 事实第四次复验）/
`coordinator.ts:444` / `flowchart-run.ts:320`（applyChildThreeLine），每
子结果一次（~5 次/run），事件表几十级（41）。R11-A 的 ~60–74 µs/run 预算
天花板经本轮在当前 VM（Node v22.22.2）实测复核为 **~70–92 µs/run**
（2×2 锚点矩阵三次独立运行；本 VM 略慢且首格受基准次序/PIC 噪声影响，
量级结论不变——即使切片零成本，节省上界 ~0.09 ms/run，仍比落地线低约
三个量级）。

按令对 R11-A「结果字段读者面与守卫常量面已关」的声明做了**结构位置 ×
消除机制满矩阵**第十二遍扫描：**未找到 R11-A 漏掉的站点**。本轮枚举出的
全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点（13 项，见 §0，
其中 8 项为本轮新记录的换名检查），按令一律不铸新 ID。作为空枚举的实证
支撑，本轮新落档**合同地板分解**（§1，三次独立运行稳定）：生产锚点格
（PASSED×contract(3)，E=41）的 ~14.1–14.8 µs/gate 中约 42% 是三次
`hashAssessment`（R1-A CAS fail-closed + S4-G-2 写侧，三重契约锁定）、
约 21% 是写侧 `validateEvent` schema 屏障（G 切片实现）、约 13% 是含
S9-A-1 死尾的完整 turn、约 10% 是 S1-A-2 重复 prescore，而 X0-4/X2-4/
S1-A-1/S1-A-9/S8-A-1 所守护的全部 O(E) 扫描在当前规模**合计只占
~1.6%（~0.22 µs/gate）**。逐组件全部落在已关 ID 或契约裁决上，无一残余
未点名——第十二遍收口，**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：roller / analysis / gates / config /
  combined-score / from-child / index / turn / isolation / human-score /
  prescore / types）+ `src/run/child-tracking.ts` + `src/run/gate-apply.ts`，
  **清单核对恰 14 文件，全部实际重读**。未触碰其余 `src/run/` 文件
  （G 切片）。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表，含 S11-A-1、
  S11-A-2、S11-B-1、S11-D-1..2；R11-C/E/F/H/I 空枚举未铸 ID，确认）/
  PROGRESS.md / round-12/PLAN.md / round-11/PLAN.md + R11-A.md /
  round-10/R10-A.md … round-01/R1-A.md。R11-G / R11-J 在其他分支在跑，
  未等待、未触碰其报告。
- 环境：本 VM 默认 Node v22.14.0，按 engines（>=22.19.0）切至 v22.22.2
  后 `pnpm install --frozen-lockfile`，全部测量在 v22.22.2 完成。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点，**一律不铸 S12-A-\* ID**）：
  1. `hashAssessment` **常量排序置换特化**（本轮 F1 事实：生产路径
     assessment 恒 6 维固定构造序、gate.codes ≤ 2 且已按构造有序，两处
     sort 的置换是常量，理论上可对 from-child 生产形态折叠）= 利用面须
     X1-2 平行哈希入口或复制集中化哈希（S5-H-3 族）——哈希契约是 CAS/
     幂等键的字节基底，禁复制；天花板实测 272–283 ns/hash × 3 hash/gate
     × ~5 gate/run ⇒ **~4.1 µs/run 绝对上限**，低于落地线约四个量级。
     事实归档（见 §1 F1），候选拒列；
  2. `PrescoreResult.cappedByHardFail` **死公开字段**（本轮 grep：`src/`
     除 prescore.ts/types.ts 外零读者）= S8-A-3 死字段/公开类型收窄族
     换位点；且其边际成本恰为 **~0 ns**——hard-fail 扫描经
     `displayPrescore` 的 min-fold 分支是活的（F2 5000 fuzz 佐证），
     删字段无任何可测收益，拒列；
  3. computePrescore **P = qualitySum/applicableWeight 代数约分直算**
     （跳过 quality×coverage 两次除法一次乘法）= 浮点非逐位
     （(qs/ow)·(ow/aw) ≠ qs/aw bitwise，且在 toFixed(4) 半格点可翻位）
     ——S7-A-4 舍入发散族 + S6-A-1 反例域 + X1-3/X2-3 非逐位数值路径族，
     拒列；
  4. `DimensionScore` **形状单态化**（value 键恒在/恒缺以消 PIC 双形态）
     = S4-E-3「隐藏类单态化＝形状不等价」+ S9-D-1 机制换切片 + 公开类型
     own-property 可测（deepStrictEqual 区分 `{value: undefined}` 与
     缺键）；量级 ns（6 维一次循环的 PIC 差），拒列；
  5. prescore `dimension()` 条件 spread 改后置条件赋值 = S8-E-2 换位点
     第三例（R9-A §0 已拒 turn.ts/roller.ts 两位点），拒列；
  6. `prescoreInputFromObservation` 的 `summary.trim() === ""` 探针换
     `/\S/` 免分配 = S7-E-1 本体换位点 + S8-F-2/S9-B-2 镜像族（方向随
     输入翻转的 ±ns 级），拒列；
  7. types.ts parse 面 `asString` 的 trim 可打印 ASCII 首字符卫 =
     S7-F-2 镜像族（S8-F-2/S9-B-2 两度拒绝同型），拒列；
  8. `nextTrackingSeq` 把 `max(next, seq+1)` 强度削减为「求 max 后一次
     +1」= 算术微常数族（S3-I-1「V8 已优化」域，匹配事件 ≤ ~5 条，
     亚 ns），拒列；
  9. `currentGateStatus` if-else 链频次重排 = R5-A 频次重排不等价裁决
     邻域 + S1-B-3 短路重排族（互斥分支下等价但噪声级），维持拒列；
  10. 切片生产子树惰性 import = S5-H-2/S7-H-3 once-per-process 族；且
      `applyChildThreeLine` 是同步公开函数，ESM 下无同步惰性通道，双重
      不可行，拒列；
  11. turn.ts `lightMinorCount` 空 filter / `config ??` / `userText ??`
      等 from-child 恒定输入的常量折叠 = S7-A-1/S10-A-1/S11-A-1 三度
      拒绝的**同一平行路径/公开旗标落地结构**伞下（孤立量 ~20 ns 级），
      拒列；
  12. gate-apply 写侧 `validateEvent` 跳过 = S4-G-2 换名（R5-A 已录、
      R6-A/R8-A 维持），本轮以分解数据再证其为 ~21% 合同地板而非可折叠
      项，维持拒列；
  13. 每 gate 三次 `hashAssessment` 的任何去重（caller+CAS+写侧；含
      WeakMap 身份 memo）= R1-A CAS fail-closed 契约 + X1-1/S8-G-2 +
      S4-G-2 写侧（R9-A/R10-A 已录），本轮以分解数据量化其为 ~42% 合同
      地板，维持拒列。
- 未重开任何 X\* / S1-\* … S11-\* 条目。S11-A-1（apply 守卫常量折叠）、
  S11-A-2（none 路径死扫描）、S10-A-1（门控结局二元化）、S9-A-1（死输出
  尾）、S8-A-3、S6-G-7 专项复核：本轮零触碰、零重提，全部维持关闭。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无命令
  权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足。不声称 Outcome-supported；Checkpoint
  F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，仿真只留在本报告附录，
  不入库（方法第 6 条）。

## 1. 预算支配论证复核 + 合同地板分解（方法第 1、2 步）

R3-A §3 … R11-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..30b6813 -- <切片>` 为空，逐字节
   一致（十二遍全程零 diff）——R1-A…R11-A 全部测量与排除按令转移，但
   数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨三轮）**：`git diff 89f0455..30b6813 -- src/`
   **完全为空**。生产调用方、supervisor settle no-op（601/711 不传
   assessment）、hashSummary 生产零流量（roller.ts:68 是唯一调用点且
   from-child 恒无 previous）、`parseTrackingAssessment` 唯一 `src/`
   调用方为 events.ts:738（写侧屏障）四项事实经本轮全库 grep 全部复验。

**2×2 锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run）：**

```text
                          run1      run2      run3
PASSED x no-contract      15.3      16.0      18.5  µs/gate (+1 event)
PASSED x contract(3)      14.8      14.1      14.5  µs/gate (+1 event)
FAILED x no-contract      16.1      15.7      16.2  µs/gate (+3 events)
FAILED x contract(3)      14.3      14.2      14.3  µs/gate (+3 events)
=> ~70–92 µs/run
```

与 R11-A（12.0–14.9 µs/gate ⇒ 60–74 µs/run）同量级；本 VM 整体略慢，
且首测格（PASSED×no-contract）显示 ±2–3 µs 的基准次序/PIC 噪声（四格
混合 warm-up 后首个被测格波动最大），单格横向比较不具意义；四格三次
运行**无悬崖、无空洞**，量级结论不变。即使把整个切片优化到零成本，节省
上界 ~0.09 ms/run，仍比战役落地线低**约三个量级**。**支配论证第十次
复核成立。**

**本轮新落档：合同地板分解**（PASSED×contract(3) 生产格逐组件隔离测量，
三次运行区间）：

| 组件 | 实测/gate | 占比* | 锁定裁决 |
| --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1951–2011 ns 每次 ⇒ **5.85–6.03 µs** | ~42% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 309–325 ns + 第三次 hash） | **2.90–2.99 µs** | ~21% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（内含 S9-A-1 死尾 ~0.9–1.0 µs） | **1.73–2.07 µs** | ~13% | S9-A-1/S7-A-1/S10-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 694–848 ns 每次 ⇒ **1.39–1.70 µs** | ~10% | S1-A-2 |
| O(E) 扫描合计（episodeIdFromEvents 64–65 + nextTrackingSeq 52–53 + 双幂等 find 39–41×2 + `[...events]` 拷贝 27 ns） | **~0.22 µs** | **~1.6%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |
| `observationFromChild` 等杂项 | 56–82 ns | <1% | S1-B-5 分配噪声族 |

\* 以 14.1–14.8 µs 锚点格计；隔离和（12.2–13.0 µs）与端到端锚点一致
（隔离测量略低于 e2e 属正常）。

分解的三个方法论结论（比任何单个候选更值钱）：

1. **哈希机器是切片的真实地板**：~42%/gate 被三次 `hashAssessment`
   占据，三次调用分别被三个独立契约锁定，任何去重路线（memo/信任传递/
   写侧跳过）在 R1-A/R9-A/R10-A 已逐一封死。切片内无字节等价的更快
   实现（S5-A-2 手写序列化实测更慢；S2-A-5 spread 173ns 噪声；hash32
   本体在切片外）。
2. **复杂度类下降的仅存位点保护的只是 ~1.6%**：X0-4/X2-4 等事件表索引
   化排除即使全部解冻并做到零成本，每 gate 也只省 ~0.22 µs——「复杂度
   类下降」在当前 E=41 规模下不构成落地线通道，除非事件表增长 ≥2–3 个
   量级（与既有重开条件一致，且届时哈希地板仍占主导）。
3. **可支配残差全部有名**：S9-A-1 死尾 + S1-A-2 重复 prescore 合计
   ~2.3–2.7 µs/gate（~18%），是仅有的理论可折叠体量，其落地面（平行
   turn 路径/公开类型收窄）已被四轮裁决钉死。**第十二遍无新结构可点名。**

**F1/F2 归档事实**（5000 组 seeded fuzz，三次运行逐位一致）：

- **F1**：4819 个 apply 路径的 assessment **全部**恰 6 维且为固定构造序
  （evidence→scope→check→constraint→progress→narrative）⇒
  `hashAssessment` 内两处 sort 在生产路径上是常量置换；利用被 §0-1 拒列，
  天花板 ~4.1 µs/run 归档。未来任何「哈希排序特化/预排序」提案可直接
  引用本事实 + 本裁决否决。
- **F2**：`cappedByHardFail` 与 `displayPrescore = min(P, 0.3)` 的
  min-fold 一致性全体成立 ⇒ 该死字段边际成本 ~0，S8-A-3 族收口的字段级
  补充证人。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 换名检查 13 项）均为既有排除 ID 的
换名/换位点，按「换名不铸 ID」纪律未列任何 S12-A-\* 候选。空枚举与
R11-C/E/F/H/I 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第十二遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `from-child.ts` | trim 探针换 /\S/（§0-6）；FAIL 守卫（line 70）确认对全体输入**活**（FAILED×空证据可达 apply:false，非 S11-A-1 可折叠域）；S1-A-2 / S2-A-2/6 / S3-A-3 / S4-A-3 / S5-A-3 / S6-A-2/3 / S9-A-1 / S10-A-1 / S11-A-1 十层收口维持 | 无新候选 |
| `gate-apply.ts` | 写侧 validateEvent = S4-G-2 换名维持（§0-12），本轮量化为 ~21% 合同地板；三 hash 去重维持拒列（§0-13），量化为 ~42% 地板；currentGateStatus 频次重排（§0-9）；双 find / nextTrackingSeq 维持 X2-4/X0-4/S1-A-1/S1-A-9/S8-A-1，合计仅 ~1.6%；S11-A-2 / S2-A-4 / FAIL_CLOSED 兜底维持 | 无新候选 |
| `child-tracking.ts` | nextTrackingSeq 强度削减（§0-8）；skipped 死分配 = S7-A-3 族维持；批量门控 API = X0-4+X1-2 维持；S4-A-2 / S6-G-7 维持 | 无新候选 |
| `prescore.ts` | cappedByHardFail 死字段（§0-2，F2 佐证）；P 代数约分（§0-3）；dimension() 条件 spread（§0-5）与 DimensionScore 形状单态化（§0-4）；S1-A-4 / S3-A-3 / S6-A-1 / S8-A-3 收口维持 | 无新候选 |
| `turn.ts` | 恒定输入常量折叠伞（§0-11）；S1-A-7 / S2-A-3 / S3-A-2/3/4 / S4-A-1 / S5-A-1 / S7-A-1 / S8-A-3 / S9-A-1 / S10-A-1 收口维持 | 无新候选 |
| `types.ts` | hashAssessment 常量置换特化（§0-1，F1 佐证——本文件十二遍来最后一个未量化角度，已归档天花板）；asString trim 卫（§0-7）；parse 面 = S1-A-8/X0-5；哈希面 = S2-A-5/S5-A-2/S3-B-3；hashSummary 生产零流量维持 | 无新候选 |
| `gates.ts` | S10-A-1 / S7-A-3 / S1-A-6 / S8-A-3 收口维持；shouldEscalateMinors 空集常假 = 引理 C(ii) 已档 | 无新候选 |
| `roller.ts` | S9-A-1 死尾（本轮分解量化 ~0.9–1.0 µs）；S1-A-5 / S2-A-1 / S3-A-1 / S7-A-2 维持 | 无新候选 |
| `human-score.ts` | X0-6 / S1-A-3 / R3-A 对偶面 / R5-A 频次重排 / S7-A-1 整管道死维持 | 无新候选 |
| `combined-score.ts` | S6-A-1 / S7-A-4 反例收口维持；isMachineScore 边界防御保留 | 无新候选 |
| `analysis.ts` | S8-A-2 维持（test-only + sanitize choke point） | 无新候选 |
| `isolation.ts` / `config.ts` / `index.ts` | O(1) 谓词 / 常量 / 纯再导出；executionAuthority 零 src/ 调用方、零计算量 | 无新候选 |

**对 R11-A 声明的裁决**：结果字段读者面（S11-A-2 之后 `GateApplyResult`
消费面仅 directive；本轮补 `PrescoreResult.cappedByHardFail` 零读者但
边际成本为零、非可折叠站点）与守卫常量面（S11-A-1 之后 from-child
line 70 FAIL 守卫经本轮 fuzz 确认对全体输入活）**确实已关**——本轮把
粒度再下探到「哈希内部置换常量」（F1）与「逐组件合同占比」（分解表），
两处都只产出归档事实而非可落地候选。粒度谱系：S10-A-1 门控结局
（~20 ns）→ S11-A-1 守卫（~14 ns）→ 本轮哈希置换天花板（~272 ns/hash
但被哈希契约整体锁死）——收口的预言力持续成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家不
落仿真文件，遵守方法第 6 条；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R11-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm typecheck` / `pnpm lint` / `pnpm build` 全绿
（Node v22.22.2）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F1/F2: 5000 fuzz trials, 4819 apply paths all with fixed 6-dim order; cappedByHardFail seen 3508x, always min-fold-consistent
anchor: 15.3 / 14.8 / 16.1 / 14.3 µs/gate => ~72–76 µs/run
decomposition: hash×3=6032ns | validateEvent=2967ns | turn=1769ns | prescore×2=1393ns | scans+copy≈224ns | applyTrackingGate whole=6.23µs
F1 ceiling: 277ns/hash => ~4.1µs/run (rejected, X1-2/S5-H-3)
ALL CHECKS PASSED (deterministic, seeded)

run 2:
anchor: 16.0 / 14.1 / 15.7 / 14.2 µs/gate => ~70–80 µs/run
decomposition: hash×3=5945ns | validateEvent=2899ns | turn=1729ns | prescore×2=1388ns | scans+copy≈224ns
ALL CHECKS PASSED (deterministic, seeded)

run 3:
anchor: 18.5 / 14.5 / 16.2 / 14.3 µs/gate => ~71–92 µs/run
decomposition: hash×3=5854ns | validateEvent=2909ns | turn=1797ns | prescore×2=1393ns | scans+copy≈222ns
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立运行 F1/F2 fuzz 结论逐位一致（4819 apply、恒 6 维固定序、
min-fold 全体一致确定性重现）；分解各组件区间紧（hash 1951–2011 ns、
validateEvent 2.90–2.99 µs、扫描合计 ~0.22 µs）；锚点四格同带无悬崖。
空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S12-A-\*）。换名检查 13 项见 §0，其中 8 项为本轮
新记录（§0-1..8），归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件全部维持不变：整片预算支配论证的重开条件仍为 run 事件表或每
turn 集合规模增长 ≥2–3 个量级（届时 S1-A-1、S2-A-1、S2-A-3、S4-A-1、
S7-A-2、S8-A-1(B)、S9-A-1、S10-A-1、S11-A-1、S11-A-2 可凭既有等价性
证据优先重开——但本轮分解表明届时首先该重开的是哈希/验证地板的量级
复核，扫描面即使解冻也仅占 ~1.6%）。F1 常量置换事实随 from-child 构造
不变量（6 维固定序）失效而自动失效。

## 附录：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.ts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xc12a01`–`0xc12a02`。

```ts
/**
 * R12-A deterministic anchor + contract-floor decomposition simulation
 * (twelfth pass). No fresh candidate cleared the rename check this round,
 * so there is NO candidate machinery here: the script (a) re-verifies the
 * whole-slice budget anchor matrix on THIS VM (R3-A..R11-A lineage,
 * gate-outcome x contract 2x2), and (b) NEW this round: decomposes the
 * anchor cells into their contract-locked components, measured in
 * isolation over the exact production shapes, to quantify how much of
 * the per-gate cost is pinned by adjudicated contracts (CAS fail-closed
 * dual hash R1-A + write-side validateEvent third hash+parse S4-G-2/R9-A,
 * O(E) scans X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1, immutable append copy R1-A
 * floor) versus the already-named discretionary residue
 * (S1-A-2 duplicate prescore, S9-A-1 dead turn output tail).
 *
 * Also fuzz-verifies two archival facts used by this round's adjudication:
 *   F1: production-path assessments always carry exactly 6 dimensions in
 *       the fixed constructor order, so hashAssessment's sort permutation
 *       is constant on this path (exploiting it = X1-2/S5-H-3 family,
 *       rejected; the fact is archived to pre-empt future re-proposals).
 *   F2: PrescoreResult.cappedByHardFail has zero src/ readers (grep fact)
 *       AND is a zero-marginal-cost byproduct: the hard-fail scan output
 *       is live via displayPrescore whenever cappedByHardFail is true
 *       (min(P, cap) branch), so field removal saves ~0 ns (S8-A-3 family).
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xc12a01..0xc12a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation,
  type ChildObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-24T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

/* ============================================================
 * Shared: realistic event tables (R3-A..R11-A harness lineage).
 * ============================================================ */
function buildEventTable(opts: { attached: boolean; filler: number }): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (opts.attached) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < opts.filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(kind: "PASSED" | "FAILED"): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  return {
    child: {
      taskId: "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary: "tests ran",
      evidenceIds: ["evd_1"],
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: ["evd_1"] } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "tests pass" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "keep scope" },
        { id: "con_1", description: "no new deps" },
        { id: "con_2", description: "tests stay green" }
      ]
    } as unknown as RequirementContract
  };
}

function genObservation(rng: () => number, trial: number): ChildObservation {
  const kind = pick(rng, ["PASSED", "FAILED"] as const);
  return {
    taskId: `tsk_${trial % 7}`,
    role: pick(rng, ["implementer", "tester", "scout", "worker"]),
    outcome: pick(rng, ["SUCCESS", "PARTIAL", "FAILURE", "CANCELLED", "TIMEOUT"] as const),
    summary: pick(rng, [
      "tests passed",
      "child completed the task",
      "",
      "wip",
      "verified output",
      "all checks succeed"
    ]),
    evidenceIds: rng() < 0.75 ? [`evd_${trial}`, "evd_shared"] : [],
    artifactIds: rng() < 0.5 ? [`art_${trial}`] : [],
    verification: { kind, evidenceIds: rng() < 0.8 ? [`evd_v_${trial}`] : [] },
    requiredChecks: rng() < 0.5 ? (rng() < 0.5 ? ["test"] : ["chk_0", "chk_1"]) : [],
    constraints: Array.from({ length: Math.floor(rng() * 5) }, (_, i) => ({
      id: `c_${i}`,
      text: `constraint ${i}`,
      kind: "constraint" as const,
      mandatory: true as const
    }))
  };
}

const FIXED_DIMENSION_ORDER = [
  "evidence-consistency",
  "scope-safety",
  "check-coverage",
  "constraint-retention",
  "progress-vs-stall",
  "narrative-coherence"
] as const;

/* ============================================================
 * F1 + F2 archival-fact fuzz over the REAL code.
 * ============================================================ */
{
  const rng = mulberry32(0xc12a01);
  let applies = 0;
  let cappedSeen = 0;
  for (let trial = 0; trial < 5000; trial += 1) {
    const observation = genObservation(rng, trial);
    const decision = assessChildObservation({ observation, episodeId: "ep_x", runId: "run_x" });
    // F2: cappedByHardFail is a zero-marginal-cost byproduct of the live
    // displayPrescore computation (min(P, cap) applied exactly when true).
    const prescore = computePrescore(prescoreInputFromObservation(observation));
    if (prescore.cappedByHardFail) {
      cappedSeen += 1;
      check(
        "F2 (cappedByHardFail true => displayPrescore = min(P, 0.3))",
        prescore.displayPrescore === Number(Math.min(prescore.P, 0.3).toFixed(4)),
        `trial ${trial}: P=${prescore.P} display=${prescore.displayPrescore}`
      );
    } else {
      check(
        "F2 (cappedByHardFail false => displayPrescore === P)",
        prescore.displayPrescore === prescore.P,
        `trial ${trial}`
      );
    }
    if (!decision.apply) continue;
    applies += 1;
    // F1: fixed 6-dimension constructor order on every production assessment
    const ids = decision.assessment.dimensions.map((d) => d.id);
    check(
      "F1 (assessment dimensions in fixed constructor order, length 6)",
      ids.length === 6 && ids.every((id, i) => id === FIXED_DIMENSION_ORDER[i]),
      `trial ${trial}: ${JSON.stringify(ids)}`
    );
  }
  console.log(
    `F1/F2: 5000 fuzz trials, ${applies} apply paths all with fixed 6-dim order; cappedByHardFail seen ${cappedSeen}x, always min-fold-consistent`
  );
}

/* ============================================================
 * Warm-up: drive the full production path (applyChildThreeLine incl.
 * applyTrackingGate/validateEvent) across all four matrix shapes so the
 * anchor cells measure steady-state JIT tiers (lineage harnesses warmed
 * via their e2e fuzz phases; replicated here explicitly).
 * ============================================================ */
const events41 = buildEventTable({ attached: true, filler: 39 });
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind);
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R11-A lineage): applyChildThreeLine
 * end-to-end at real scale (41-event table) across gate-outcome x
 * contract cells (2x2 full matrix, closed by R10-A, re-anchored here).
 * ============================================================ */
{
  const cells: Array<{ label: string; kind: "PASSED" | "FAILED"; withContract: boolean }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind);
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * NEW (twelfth pass): contract-floor decomposition of the
 * PASSED x contract(3) production cell. Every component is measured
 * in isolation over the exact production shapes.
 * ============================================================ */
{
  const mk = mkChild("PASSED");
  const observation = observationFromChild(mk.child, mk.spec, mk.contract);
  const prescoreInput = prescoreInputFromObservation(observation);
  const decision = assessChildObservation({
    observation,
    episodeId: "ep_bench",
    runId: String(RUN_ID)
  });
  check("decomposition fixture applies", decision.apply === true);
  if (decision.apply) {
    const assessment: TrackingAssessment = decision.assessment;
    const hash = hashAssessment(assessment);
    const seq = nextTrackingSeq(events41);

    const tHash = bench(() => void hashAssessment(assessment), 200000);
    const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
    const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

    const trackingEvent = {
      id: "evt_00000100" as EventId,
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "TRACKING_ASSESSMENT",
      actor: "supervisor",
      payload: { assessment, assessmentHash: hash, seq }
    } as unknown as Event;
    const tVev = bench(() => void validateEvent(trackingEvent), 100000);

    const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
    const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
    const tFindGT = bench(
      () =>
        void events41.find(
          (event) =>
            event.type === "GATE_TRANSITION" &&
            (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
              `${hash}:${seq}`
        ),
      200000
    );
    const tFindTA = bench(
      () =>
        void events41.find(
          (event) =>
            event.type === "TRACKING_ASSESSMENT" &&
            (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
            (event as { payload: { seq?: number; assessmentHash?: string } }).payload
              .assessmentHash === hash
        ),
      200000
    );
    const tCopy = bench(() => void [...events41], 200000);
    const tObs = bench(() => void observationFromChild(mk.child, mk.spec, mk.contract), 200000);
    const tPrescore = bench(() => void computePrescore(prescoreInput), 200000);
    const tTurn = bench(
      () =>
        void runTrackingTurn({
          window: {
            contextFacts: [`role ${observation.role}`, `task ${observation.taskId}`],
            toolSituations: prescoreInput.toolSituations,
            constraints: observation.constraints,
            unresolvedDecisions: [],
            confirmedDecisions: [],
            openMinors: []
          },
          prescoreInput,
          humanInput: {},
          gateFacts: { deterministicFail: false }
        }),
      100000
    );
    const tGate = bench(() => {
      let c = 100;
      void applyTrackingGate({
        events: events41,
        assessment,
        assessmentHash: hash,
        expectedSeq: seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    }, 20000);

    const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
    console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
    console.log(`  hashAssessment (x3/gate: caller+CAS+write-side): ${ns(tHash)} each => ${ns(tHash * 3)}`);
    console.log(`  parseTrackingAssessment (write-side, in validateEvent): ${ns(tParse)}`);
    console.log(`  validateEvent(TRACKING_ASSESSMENT) whole (parse + 3rd hash inside): ${ns(tVev)}`);
    console.log(`  episodeIdFromEvents scan (E=41): ${ns(tEpisode)}`);
    console.log(`  nextTrackingSeq scan (E=41): ${ns(tSeq)}`);
    console.log(`  idempotency find GATE_TRANSITION miss (E=41): ${ns(tFindGT)}`);
    console.log(`  idempotency find TRACKING_ASSESSMENT miss (E=41): ${ns(tFindTA)}`);
    console.log(`  [...events] immutable append copy (E=41): ${ns(tCopy)}`);
    console.log(`  observationFromChild: ${ns(tObs)}`);
    console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
    console.log(`  runTrackingTurn full (contains S9-A-1 dead tail): ${ns(tTurn)}`);
    console.log(`  applyTrackingGate whole (CAS hash + finds + validate + copy + append): ${(tGate * 1e3).toFixed(2)}us`);
    const contractLocked =
      tHash * 2 + tVev + tEpisode + tSeq + tFindGT + tFindTA + tCopy;
    const namedResidue = tObs + tPrescore * 2 + tTurn + tHash; // caller-side hash + compute core (all named)
    console.log(
      `  SUM contract-locked (CAS 2x hash + validateEvent + scans + copy): ${(contractLocked * 1e3).toFixed(2)}us`
    );
    console.log(
      `  SUM named-residue (caller hash + obs + 2x prescore + full turn): ${(namedResidue * 1e3).toFixed(2)}us`
    );
    console.log(
      `  (anchor cell for reference: see matrix above; components measured in isolation, sum is indicative not additive-exact)`
    );
  }
}

/* ============================================================
 * F1 exploitation ceiling (recorded, NOT a candidate): the constant
 * sort permutation inside hashAssessment on the production path.
 * Measured as the cost of the two sorts + dimension map on the
 * production assessment -- the absolute ceiling any X1-2-family
 * specialization could recover (rejected: hash-contract duplication,
 * S5-H-3/X1-2; magnitude recorded for the archive).
 * ============================================================ */
{
  const mk = mkChild("PASSED");
  const observation = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({
    observation,
    episodeId: "ep_bench",
    runId: String(RUN_ID)
  });
  if (decision.apply) {
    const assessment = decision.assessment;
    const tSortPart = bench(() => {
      void [...assessment.dimensions]
        .map((dimension) => ({ id: dimension.id, verdict: dimension.verdict }))
        .sort((left, right) => left.id.localeCompare(right.id));
      void [...assessment.gate.codes].sort();
    }, 200000);
    console.log(
      `F1 ceiling: hashAssessment sort+map machinery on production assessment = ${(tSortPart * 1e6).toFixed(0)}ns/hash (x3 hashes/gate x ~5 gates/run => ~${(tSortPart * 15 * 1e6).toFixed(0)}ns/run ceiling; X1-2/S5-H-3 family, rejected)`
    );
  }
}

void mulberry32(0xc12a02);

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
