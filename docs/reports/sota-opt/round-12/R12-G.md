MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 12 / R12-G：运行时执行面第十二遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 12 / R12-G（Round 1–11 同区第十二遍）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `5d3e0dd`（含 R12-A/B/C/D 摄取；
  S12-B-1..2 / S12-C-1 / S12-D-1..2 已入 EXCLUSIONS；R12-A 未铸 ID；
  R12-E/F 在跑。R11–R12 波次至今**全部为仅报告合入**，`src/` 自 R10-G
  报告提交 `cca125e` 起零改动）
- **分支:** `cursor/r12-g-runtime-twelfth-pass-83a1`
- **切片:** 42 文件清点确认——`src/run/` 除 `child-tracking.ts`、
  `gate-apply.ts` 外 17 文件；`src/supervisor/` 除 `model-router.ts` 外
  4 文件；`src/graph/` 全部 4 文件；`src/domain/` 全部 17 文件。
  全部 42 文件逐一实读。
- **模型:** `claude-fable-5-thinking-xhigh`
- **读序:** README → EXCLUSIONS 全表（含 S12-B-1..2 / S12-C-1 /
  S12-D-1..2 与 S11-G-1..3）→ PROGRESS → round-12/PLAN →
  round-11/PLAN + R11-G → R10-G → R9-G … R1-G → 42 源文件，全部在
  任何候选之前完成。

## 结论

**无可落地的新更优解，本轮生产代码零改动，切片维持关闭
（MORE_OPTIMA=no）。** 第十二遍为本切片十二轮以来**首次完全空枚举**：
结构位置 × 消除机制全叉积扫描未产出任何位置与机制组合均无先例的
真新候选——每个格位经逐条重命名核查后要么命中已关闭 ID（46 项
S1-G-1..9 / S2-G-1..8 / S3-G-1..5 / S4-G-1..7 / S5-G-1..6 /
S6-G-1..7 / S7-G-1..5 / S8-G-1..2 / S9-G-1..3 / S10-G-1 /
S11-G-1..3 及 X4-6/7/8 等继承项），要么直接触碰硬禁（fail-closed
复验、fsync/writeAtomic 持久性、确定性 id 流与事件次序、末次
persistCheckpoint、租约后持久写），要么落在已裁决噪声带（计算面
µs 级、本轮复锚上界 0.294–0.304 ms/run）。按纪律**不铸任何
S12-G-\* ID**——空枚举即诚实结果（见 §3 勘察记录与重命名核查表）。

1. **零漂移确认（双层）**：`git diff 4efee23..HEAD -- src/run
   src/supervisor src/graph src/domain` 为空——切片自 R1-G 裁决基线起
   十二轮**逐字节未变**，全部 46 项切片裁决原样继承。更强地：
   `git diff cca125e..HEAD -- src/` 亦为空（R11 全波次与 R12-A/B/C/D
   均仅报告合入），整个仓库生产面与 R10-G 派单尖端逐字节一致 ⇒
   **无新生产调用方、无新 I/O 动词**（§0 另有正面核查）。
2. **物理收口在本 VM 三批次复测下继续成立**：计算顶
   **0.294–0.304 ms/run** vs I/O 地板 **95.5–105.6 ms/run**（比率
   **~314–359×**；R11-G 为 311–332×、R10-G 为 331–364×）。把整个
   计算面清零仍差落地线（数十 ms）两个数量级；I/O 面每一项（66 次
   checkpoint 持久写 ≈ 37.8–46.0 ms、~96 次 readAll ≈ 34.4–37.1 ms、
   ~300 次事件追加 ≈ 20.6–23.8 ms）均为已裁决契约地板（S6-G-6 /
   S3-G-4 / S1-G-1 / S6-G-1/2/7 / S5-G-4/5 / S4-G-6）。
3. **14 格动态矩阵复跑全绿**：R10-G 的 14 格真实命令面矩阵（含 2 格
   inspect 只读类）在本 VM 复跑，摘要 `06cbcf92c098c8f0…` 跨
   **3 进程 × 3 迭代 = 9 次逐位一致**（时间戳/临时路径归一后），且
   与 R10-G、R11-G 报告的摘要值相同——切片逐字节未变的行为面第三度
   直接见证。未暴露任何新 I/O 操作、逐格 I/O 计数零变化。
4. **空枚举的结构论证**：本轮枚举输入（42 文件字节、生产调用方
   集合、I/O 动词集合、14 格矩阵行为面）与 R11-G 枚举时**完全相同**；
   R11-G 在该输入上已把仅剩的三个无先例组合（G 内 import 边惰性化、
   写侧字节去重、尾扫部分读）铸为 S11-G-1..3 并淘汰。本轮全叉积
   重扫（含新机制维度「写去重」「部分读」并入后的完整机制轴）确认
   位置 × 机制矩阵已无空白格——每格均有 ID 或硬禁点名（§3 全表）。
5. **门禁全绿零改动**：node 22.22.2（VM 经 nvm 切换；engines
   >=22.19.0）+ pnpm 10.17.1 frozen-lockfile；`pnpm gate` =
   typecheck + lint + test（**1168 pass / 0 fail / 1 skip**，
   1169 tests / 78 suites）+ build 全过，与 R11-G 门禁基线逐项一致。
   无赢家 ⇒ 按纪律基准/矩阵脚本**不入库**，全文存附录 A。

## §0 范围、读序、零漂移与调用面核查

42 文件实读清单：`src/run/`（checkpoint-store、child-coordinator、
child-grounding、child-prompt、coordinator、episode-bind、episode-store、
event-store、events、flowchart-executor、flowchart-run、injection、
inspection、pause-controller、replay、scheduler、supervisor，17）；
`src/supervisor/`（flowchart-snapshot、flowchart-supervisor、flowchart、
ledger，4）；`src/graph/`（compile-children、judge、readiness、
validate，4）；`src/domain/`（contract、episode、errors、evidence、
flowchart、hash、ids、index、limits、project、record、roles、run、
state、status、task、timestamp，17）。A/B 文件（child-tracking、
gate-apply、model-router）未触碰。

- `git diff 4efee23..HEAD` 对四目录为空 ⇒ 全部先前裁决直接转移；
  本轮仍在本 VM 重锚（§1–§2），未拷贝历史数字。
- `git diff cca125e..HEAD -- src/` 为空（R11 全波次与 R12-A/B/C/D
  仅报告合入）⇒ 与 R10-G 相比**无任何生产面变化**。正面核查：
  存储类构造点全集不变——切片内 `flowchart-run.ts`（:728/:851/:995
  EventStore+CheckpointStore、:941/:1068 pause 回退构造）、
  `coordinator.ts`（:95/:254）、`supervisor.ts`（:522/:629）、
  `child-coordinator.ts`（:188/:286）、`inspection.ts`（:62）、
  `episode-bind.ts`（:69/:131 EpisodeStore + EpisodeEventStore）；
  切片外调用方仍为 `src/cli/{main,pause,inject,episode,adapt,
  commits}.ts`、`src/learning/from-episode.ts`、`src/track/loop.ts`、
  `src/experiments/shadow.ts`、`src/tools/decision-commit.ts`——与
  R9-G…R11-G 裁决时逐一相同。存储层 I/O 动词集合（append / readAll /
  write / read / token / requestPause / clearPause）无增减。

## §1 CPU vs I/O 重锚（三批次，seed 仅标注批次）

CPU 面（R8-G…R11-G 同探针组，三批）：

| 探针 | B1 (0xa12071) | B2 (0xb12072) | B3 (0xc12073) |
|---|---|---|---|
| validateFlowchart 16n join4 | 9.174 µs | 9.337 µs | 9.501 µs |
| → ×32 轮/run 计算顶 | **0.294 ms** | **0.299 ms** | **0.304 ms** |
| validateFlowchart 8n join2 | 4.964 µs | 4.616 µs | 4.742 µs |
| DeterministicJudge.decide 8×8 | 0.148 µs | 0.154 µs | 0.153 µs |
| expandTaskTransition | 0.513 µs | 0.507 µs | 0.504 µs |
| compileChildrenToFlowchart 16 | 9.064 µs | 9.253 µs | 9.353 µs |
| validateEvent AGENT_EVENT | 0.567 µs | 0.560 µs | 0.570 µs |
| replayRun 300 events | 0.661 µs | 0.729 µs | 0.665 µs |
| → readAll(300) 计算份额 | 0.171 ms | 0.169 ms | 0.172 ms |
| validateCheckpoint N=48 含 restore | 277.2 µs | 273.8 µs | 277.4 µs |

I/O 地板（同批）：

| 探针 | B1 | B2 | B3 |
|---|---|---|---|
| CheckpointStore.write 持久 (N=48) | 573.5 µs | 625.4 µs | 696.3 µs |
| → ×66 写/run | **37.8 ms** | **41.3 ms** | **46.0 ms** |
| EventStore.append（无 fsync） | 68.7 µs | 79.2 µs | 78.2 µs |
| → ×300 事件/run | **20.6 ms** | **23.8 ms** | **23.5 ms** |
| EventStore.readAll 300ev | 563.1 µs | 521.5 µs | 546.7 µs |
| EventStore.readAll 100ev | 208.9 µs | 194.4 µs | 204.6 µs |
| → ×96 readAll/run（中期均值） | **37.1 ms** | **34.4 ms** | **36.1 ms** |
| pause token() ENOENT | 21.8 µs | 26.7 µs | 24.8 µs |
| pause token() present | 72.6 µs | 62.8 µs | 69.7 µs |
| loadLearnedRouting（无 registry） | 29.9 µs | 30.0 µs | 29.4 µs |
| EpisodeStore.readAll（2 快照） | 55.8 µs | 93.0 µs | 73.0 µs |
| EpisodeStore.append COMPLETED (fsync) | 457.4 µs | 611.3 µs | 332.0 µs |
| EpisodeEventStore.append（无 fsync） | 106.0 µs | 105.4 µs | 60.6 µs |
| settle-close 算术（S10-G-1 复核） | 0.688 ms | 0.889 ms | 0.544 ms |

**I/O 地板合计 95.5–105.6 ms/run vs 计算顶 0.294–0.304 ms/run =
~314–359×。** 与 R11-G（311–332×）、R10-G（331–364×）同量级；结论
不变：清零整个计算面仍差落地线两个数量级，I/O 每项均为已裁决契约
地板。

## §2 配置态 × 命令类 14 格矩阵（复跑）

R10-G 的 14 格矩阵原样复跑（真实 startFlowchartRun / resume / pause /
inject / inspect 命令面 + 原型插桩 I/O 计数）：**摘要
`06cbcf92c098c8f0…` 跨 3 进程 × 3 迭代 = 9 次逐位一致**（时间戳/
临时路径归一后），且与 R10-G、R11-G 报告值相同——切片行为面零漂移的
第三度直接见证。start→COMPLETED 命令墙钟 23.1–24.4 ms。

| 命令类 | 状态 | 结果 | ev.app(fsync) | readAll(events) | cp.w | cp.r | pz.tok | epi.app/read/evt |
|---|---|---|---|---|---|---|---|---|
| start | fresh→COMPLETED | COMPLETED | 13(1) | 11(104) | 8 | 0 | 6 | 3/1/3 |
| start | fresh→WAITING | WAITING_FOR_USER | 10(0) | 8(62) | 5 | 0 | 3 | 3/1/3 |
| inspect | WAITING | WAITING_FOR_USER | 0 | 1(10) | 0 | 0 | 0 | 0 |
| pause | WAITING | PAUSED | 1(0) | 5(53) | 1 | 1 | 0 | 0/1/0 |
| resume(unpause) | PAUSED | WAITING_FOR_USER | 1(0) | 7(82) | 1 | 1 | 2 | 0/1/0 |
| inject(fact) | WAITING | WAITING_FOR_USER | 2(0) | 4(54) | 1 | 1 | 0 | 0/1/0 |
| resume(approval) | WAITING | COMPLETED | 6(1) | 9(155) | 4 | 1 | 3 | 1/1/1 |
| resume(noop) | COMPLETED | COMPLETED | 0 | 5(100) | 1 | 1 | 0 | 0 |
| pause | COMPLETED | THROWS | 0 | 1(20) | 0 | 1 | 0 | 0 |
| inject(fact) | COMPLETED | THROWS | 0 | 1(20) | 0 | 1 | 0 | 0 |
| start | fresh→WAITING(2) | WAITING_FOR_USER | 10(0) | 8(62) | 5 | 0 | 3 | 3/1/3 |
| cancel(x-process append) | WAITING | appended | 1(1) | 0 | 0 | 0 | 0 | 0 |
| resume(observe cancel) | CANCELLED | CANCELLED | 1(0) | 5(56) | 1 | 1 | 0 | 1/1/1 |
| inspect | CANCELLED | CANCELLED | 0 | 1(12) | 0 | 0 | 0 | 0 |

逐格 I/O 计数与 R10-G/R11-G 一致（S6-G-6 的 8 次 cp.w、S8-G-1 的
6 次 pz.tok = 3 轮 × 2 等对账吻合）。全矩阵 cp.write 合计 27 次——
S11-G-2 字节普查（0/27 零命中）的分母原样在位，该裁决逐字节转移。

## §3 第十二遍枚举：结构位置 × 消除机制全叉积与重命名核查

对 42 文件按 位置（存储层 / 命令编排 / 重放校验 / supervisor 推进 /
子协调 / episode 绑定 / 巡检 / domain 校验器 / 模块图 import 边 /
调度）× 机制（跳过延迟 / 缓存记忆 / 并行 / 批并 / 换格式 / 复杂度
下降 / 惰性加载 / 系统调用摊销 / 快路径 / 死代码 / **写去重** /
**部分读**——后两维为 R11-G 新增后的完整机制轴）全格扫描。枚举输入
与 R11-G 完全相同（§0 零漂移 + §2 行为面零漂移），扫描结果：
**无空白格**。

**重命名核查（命中已关闭 ID，不铸新 ID，不重开）：**

| 勘察方向（本轮实读中逐一核对） | 命中的已关闭 ID | 关闭理由回执 |
|---|---|---|
| `leaseReadyNodes` 循环内 computeStatus/activeCount 提升缓存 | **S2-G-1**（+X4-8 跨调用增量） | 中途重算是载荷语义；O(N²) 上界 ~5µs/调用 |
| `failurePathCompletedGraph` DFS 记忆化/每根独立 visiting | **S2-G-2** | 共享 visiting 是可观察终态语义，去重翻转 FAILED↔COMPLETED |
| `approvedActionIds`/ledger progress copy-on-append 改可变 push | **S2-G-3 / X4-7**（R1-G 收口：返回新对象是快照语义，S1-D-1 同域） | 亚噪声 + 别名泄漏风险 |
| `expandTaskTransition` BFS 预计算/索引队列 | **S2-G-4 / S7-G-4**（X4-6 同族） | 噪声 + 模块级派生缓存 |
| 每轮 `new ChildCoordinator` 提升复用 | **S2-G-5** | 构造消费确定性 id 流，提升即事件 id 平移 |
| `inspectRun` 两遍合并 / 尾扫部分读 | **S2-G-6 / S11-G-3** | 噪声；部分读跳过 fail-closed 复验（硬禁） |
| `joinStatus` 三遍融合 / `edgeStatus`→`conditionHolds` 双 getRuntime 传参 | **S2-G-7 / S5-G-6** | ns 级亚噪声 |
| `LeaseRegistry` Date.parse 缓存 | **S2-G-8** | ~35µs/run 噪声 + 隐藏派生状态 |
| `snapshot()`/`nodeRuntime()`/`restore()` structuredClone 浅展开 | **S3-G-1** | 2.8–8.6ms 上界 + 隔离性降级 |
| replay 外层/内层校验去重、审批双层校验去重 | **S3-G-2 / S6-G-3 / S4-G-3** | 错误面发散 / restore 屏障本体 / 追加前拦截角色 |
| finish 末次 persistCheckpoint 跳过 | **S3-G-4**（硬禁） | 陈旧 RUNNING 态当终态落盘 |
| 同轮 RUNNING 节点 Promise.all | **S4-G-1**（硬禁） | id 流交错 + 事件次序漂移 |
| 写侧 validateCheckpoint 跳过 | **S4-G-2**（硬禁） | 损坏字节 fsync 落盘 |
| persistLedger 窄投影 / finish snapshot 复用 | **S4-G-4 / S4-G-5** | 不达线 + 公开签名面 / 别名化 |
| checkpoint 紧凑 JSON | **S4-G-6** | 磁盘字节流是数据面契约 |
| ready 空轮 failed 过滤器下沉 | **S4-G-7** | 9µs/run 亚噪声 |
| 每写 mkdir 提升 | **S5-G-1** | 外部清理后自愈 vs ENOENT 丢写 |
| TRACKING_ASSESSMENT 读侧 parse+hash memo | **S5-G-2** | 磁盘篡改静默接受（fail-closed 防篡改屏障） |
| `ReconstructedRun` 暴露 unmatchedPause 旗标 | **S5-G-3** | 亚噪声 + 公开签名面 |
| 跨 store 相邻追加 Promise.all / 逐消息 AGENT_EVENT 攒批 / 门控事件 Promise.all | **S5-G-4 / S5-G-5 / S6-G-5** | 崩溃序契约 / 实时可见性契约 / 零收益 |
| finish/resume readAll 穿线复用、applyChildThreeLine 内存镜像、任何增量读 | **S6-G-1/2/7 + S1-G-1**（硬禁） | 读隙 cancel 竞态 / 磁盘唯一事实源 / 门控证据边界 |
| 租约后 persist 去除 | **S6-G-6**（硬禁） | 恢复窗口重路由 + id 流发散 |
| isIsoTimestamp 快路径 / isId 前缀表 / 字面量数组提升 / judge Set / validateJoin 边索引 | **S6-G-4 / S3-G-5 / S7-G-2 / S7-G-3 / S7-G-1** | V8 回卷语义不可复刻 / ns 级 / 噪声 |
| resume 三段 I/O 并行 | **S7-G-5** | 真数据依赖 + Promise.all 家族已封 |
| pause 双观测点去重 / fs.watch 替代轮询 | **S8-G-1 / S9-G-2** | 唯一观测点 / 观测-磁盘语义 |
| validateFlowchart WeakSet 恒等 memo | **S8-G-2** | 生产 0/64 零命中 |
| 会话构造跳过 loadLearnedRouting | **S9-G-1** | 损坏 registry fail-open |
| fsync 移除（pause/checkpoint） | **S9-G-3**（硬禁） | 持久性契约 |
| settle/返回后延迟任何变体 | **S10-G-1** | 方向已全局关闭（前缀分歧 + 终态 OPEN） |
| G 内跨界 import 边惰性化 | **S11-G-1** | main.ts 静态边钉死，生产收益恒 0 |
| CheckpointStore 写侧字节去重（write-if-unchanged） | **S11-G-2** | 0/27 结构性零命中（本轮矩阵分母原样在位） |
| 计算面一切微优化（domain/graph/supervisor 校验器、replay 折叠、调度、判定、prompt/grounding 字符串构建） | R7-G/R8-G 范畴性关闭 | 计算顶 0.294–0.304 ms/run 本轮复锚 |

**扫描后不铸 ID 的勘察记录（已关闭家族内部的次级观察，无独立
候选资格）：**

- `buildQuestion`（flowchart-supervisor.ts:668）对刚构造的内存态
  question 跑 `validateAgentMessage`——写侧校验是 S4-G-3 裁决过的
  「追加/暴露前拦截」契约面同款角色，且每 run 审批次数个位、µs 级。
- `snapshotValidationRouter()`（flowchart-snapshot.ts:488）每次
  checkpoint 校验构造一个 catalogModel stub——S6-G-3 已裁决 restore
  干跑整体为可恢复性屏障本体，stub 构造为其亚 µs 份额（×66 写/run
  仍亚 ms）。
- `setRuntime`（flowchart-supervisor.ts:417）每补丁 `{...old,
  ...patch}` 新对象分配——S3-G-1 同噪声带（平坦小记录、几十 ns），
  且不可变 runtime 记录是 snapshot 隔离性的实现基础。
- `advanceLedgerRound`（ledger.ts:118/139）每轮拷贝 progress/facts
  数组——R1-G 已收口（X4-7 维持 + 返回新对象是快照语义），R≤32 ×
  几十条目 = µs 级。
- `validateFlowchartSupervisorSnapshot` 的逐 decision 全字段校验
  （flowchart-snapshot.ts:141-196，含 one-hot 分布检查）——
  validateCheckpoint 273.8–277.4 µs 计算份额的主体，S4-G-2/S6-G-3
  封死的 fail-closed 读写屏障本体，不可跳过不可 memo（S8-G-2 零命中
  同构）。

以上五项均为已关闭 ID 的机制/位置邻域内再观察，按「不是换名重提」
原则不赋新 ID；无一项在理论上够到落地线（最大者亚 ms/run）。

**空枚举判定**：位置 × 机制矩阵全格核查完毕，无任何位置与机制组合
均无先例的真新候选。R11-G 用尽了当时仅剩的三个空白格（S11-G-1..3），
本轮输入面（字节/调用方/动词/行为摘要）与彼轮完全相同，枚举空间
未再生长。**不铸 S12-G-\* ID。**

## §4 既有关闭项抽查（与 R10-G/R11-G 相同的在位见证）

- **S6-G-6**：矩阵 start→COMPLETED 格 8 次 cp.w 对账吻合（含每
  lease 后持久写）。
- **S8-G-1**：每轮两个 pause 观测点原样（6 次 pz.tok = 3 轮 × 2）。
- **S9-G-1**：`flowchartForSupervisor`（flowchart-run.ts:707-714）仍
  在全部会话构造路径调用 `loadLearnedRouting`（29.4–30.0 µs 本轮
  复测）。
- **S9-G-3**：`pause-controller.ts` writeAtomic 仍 `handle.sync()`
  ——requestPause fsync 在位，**永不重开**。
- **S10-G-1**：start→COMPLETED 返回事件尾仍为
  `[LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED]`（三批全同）——
  settle 在命令关键路径内、返回值含全部 settle 事件，完成延迟方向
  维持全局关闭；settle-close 算术复核 0.544–0.889 ms/终态命令。
- **S11-G-2**：矩阵 cp.write 合计仍为 27 次（8+5+1+1+1+4+1+5+1），
  0/27 普查分母原样，裁决逐字节转移。
- **S4-G-1**：`executeRemainingRunningNodes` 仍为串行 await；同轮
  RUNNING 节点无 Promise.all——确定性 id 流硬禁在位。

## §5 门禁与纪律

- node v22.22.2（nvm 切换，`/exec-daemon/node` 22.14.0 被 PATH 前置
  覆盖；engines >=22.19.0）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`。
- `pnpm gate`（typecheck + lint + test + build）全绿：
  **1168 pass / 0 fail / 1 skip**（1169 tests / 78 suites），现有
  测试零改动，与 R11-G 门禁基线逐项一致。
- 生产代码零改动；无赢家 ⇒ 仅报告，基准/矩阵脚本**未提交**（全文
  附录 A，临时文件已删除）；EXCLUSIONS/PROGRESS/PLAN 未触碰
  （父级所有）。
- 不开 PR；分支推送由父级合并。

## §「新增排除 ID」

**本轮无新增排除 ID。** 空枚举——父级无需向 EXCLUSIONS.md 合并任何
S12-G-\* 条目。

## 附录 A：重锚基准 / 矩阵脚本（完整，可复现；无赢家按纪律不入库）

运行方式：置于仓库根目录为 `tmp-r12g-bench.ts`，
`npx tsx tmp-r12g-bench.ts 0xa12071`（种子仅标注批次，全部数据
确定性构造）。三批关键输出：

```
validateFlowchart 16n join4: 9.174 / 9.337 / 9.501 us -> x32 = 0.294~0.304 ms/run
DeterministicJudge.decide 8x8: 0.148 / 0.154 / 0.153 us
expandTaskTransition: 0.513 / 0.507 / 0.504 us
compileChildrenToFlowchart 16: 9.064 / 9.253 / 9.353 us
validateEvent AGENT_EVENT: 0.567 / 0.560 / 0.570 us
replayRun 300 events: 0.661 / 0.729 / 0.665 us
validateCheckpoint N=48 incl. restore: 277.248 / 273.790 / 277.441 us
CheckpointStore.write durable N=48: 573.5 / 625.4 / 696.3 us -> x66 = 37.8~46.0 ms/run
EventStore.append (no fsync): 68.7 / 79.2 / 78.2 us -> x300 = 20.6~23.8 ms/run
EventStore.readAll 300ev: 563.1 / 521.5 / 546.7 us; 100ev: 208.9 / 194.4 / 204.6 us -> x96 = 34.4~37.1 ms/run
pause token() ENOENT: 21.8 / 26.7 / 24.8 us; present: 72.6 / 62.8 / 69.7 us
loadLearnedRouting (no registry snapshot): 29.9 / 30.0 / 29.4 us
EpisodeStore.readAll (2 snapshots): 55.8 / 93.0 / 73.0 us
EpisodeStore.append COMPLETED (fsync): 457.4 / 611.3 / 332.0 us
EpisodeEventStore.append (no fsync): 106.0 / 105.4 / 60.6 us
settle-close arithmetic: 0.688 / 0.889 / 0.544 ms per terminal command
matrix digest (3 proc x 3 iter): 06cbcf92c098c8f0... 九次逐位一致（14 格；与 R10-G/R11-G 同值）
start->COMPLETED wall-clock: 24.4 / 24.4 / 23.1 ms; returned events tail = [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED]
gate @ node 22.22.2: 1168 pass / 0 fail / 1 skip
```

脚本全文：

```ts
/**
 * R12-G twelfth-pass re-measure bench + configured-state x command-class matrix
 * (NOT committed to scripts/; source archived in the report appendix).
 *
 * Parts:
 *   A. CPU-plane probes (identical to the R8-G..R11-G set, for drift
 *      comparability).
 *   B. I/O-floor probes (checkpoint durable write, per-event append, readAll,
 *      pause token, loadLearnedRouting ENOENT path, episode-store op costs).
 *   C. Deterministic configured-state x command-class matrix over the REAL
 *      flowchart-run command surface (R10-G's 14 cells re-run), with
 *      per-command I/O op counts via prototype instrumentation. Run 3x
 *      in-process and digest-compared (timestamps and tmp paths normalized);
 *      the whole script is run 3x cross-process with different seed labels.
 *
 * Deterministic: fixed clock, sequential id generator, fixed fixtures. The
 * seed argv only labels the batch. Run: npx tsx tmp-r12g-bench.ts 0xa12071
 */
import { createHash } from "node:crypto";
import { mkdtempSync, rmSync } from "node:fs";
import { readFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

import { validateFlowchart, validateConfidenceScore, type FlowEdge, type FlowNode, type Flowchart, type JoinPolicy } from "./src/domain/flowchart.js";
import { expandTaskTransition } from "./src/domain/state.js";
import { createTaskId, type AgentInstanceId, type EpisodeId, type EventId, type ProjectId, type RunId, type TaskId } from "./src/domain/ids.js";
import { parseIsoTimestamp, type IsoTimestamp } from "./src/domain/timestamp.js";
import type { ProjectEpisode } from "./src/domain/episode.js";
import { DeterministicJudge } from "./src/graph/judge.js";
import { compileChildrenToFlowchart, type CompilableChild } from "./src/graph/compile-children.js";
import { validateEvent, type Event } from "./src/run/events.js";
import { materializeCheckpoint, replayRun, validateCheckpoint, type RunCheckpoint } from "./src/run/replay.js";
import { CheckpointStore } from "./src/run/checkpoint-store.js";
import { EventStore } from "./src/run/event-store.js";
import { EpisodeStore } from "./src/run/episode-store.js";
import { EpisodeEventStore } from "./src/episode/store.js";
import { createFilePauseController, type PauseController } from "./src/run/pause-controller.js";
import { createFlowchartSupervisor, type ChildNodeResult, type FlowchartRunLimits } from "./src/supervisor/flowchart-supervisor.js";
import { snapshotValidationRouter } from "./src/supervisor/flowchart-snapshot.js";
import { createModelRouter, type ModelRouter } from "./src/supervisor/model-router.js";
import { startFlowchartRun, resumeFlowchartRun, pauseFlowchartRun, injectFlowchartRun } from "./src/run/flowchart-run.js";
import { inspectRun } from "./src/run/inspection.js";
import { loadLearnedRouting } from "./src/learning/learned-routing.js";

const SEED = process.argv[2] ?? "0xa12071";
console.log(`seed=${SEED}`);

const tid = (n: number): TaskId => `tsk_r12g${n.toString(36).padStart(4, "0")}` as TaskId;
const eid = (n: number): EventId => `evt_r12g${n.toString(36).padStart(8, "0")}` as EventId;
const aid = (n: number): AgentInstanceId => `agt_r12g${n.toString(36).padStart(4, "0")}` as AgentInstanceId;
const RUN_ID = "run_r12g-bench" as RunId;
const T0 = "2026-08-24T12:00:00.000Z" as IsoTimestamp;

function bench(label: string, iters: number, fn: () => void): number {
  for (let i = 0; i < Math.max(50, iters / 10); i++) fn();
  const start = performance.now();
  for (let i = 0; i < iters; i++) fn();
  const us = ((performance.now() - start) * 1000) / iters;
  console.log(`${label}: ${us.toFixed(3)} us/call`);
  return us;
}

async function benchAsync(label: string, iters: number, fn: () => Promise<void>): Promise<number> {
  for (let i = 0; i < Math.max(5, iters / 10); i++) await fn();
  const start = performance.now();
  for (let i = 0; i < iters; i++) await fn();
  const us = ((performance.now() - start) * 1000) / iters;
  console.log(`${label}: ${us.toFixed(1)} us/call`);
  return us;
}

/* ---- fixtures (R8-G set) ------------------------------------------------ */

function fixtureFlowchart(n: number, joinFanIn: number): Flowchart {
  const nodes: FlowNode[] = [];
  const edges: FlowEdge[] = [];
  for (let i = 0; i < n; i++) {
    const isJoin = i === n - 1 && joinFanIn >= 2;
    nodes.push({
      id: `n${i}`,
      taskId: tid(i),
      role: "actor",
      objective: `objective ${i}`,
      modelPolicy: { allowedModels: ["cheap", "premium"], preferredModel: "cheap" },
      confidenceThreshold: 0.7,
      approvalRequired: false,
      ...(isJoin
        ? { joinPolicy: { mode: "all" as const, requiredNodeIds: Array.from({ length: joinFanIn }, (_, k) => `n${n - 2 - k}`) } }
        : {})
    });
  }
  for (let i = 1; i < n - 1; i++) {
    edges.push({ from: `n${i - 1}`, to: `n${i}`, condition: { type: "success", expected: true } });
  }
  for (let k = 0; k < joinFanIn; k++) {
    edges.push({ from: `n${n - 2 - k}`, to: `n${n - 1}`, condition: { type: "success", expected: true } });
  }
  return { id: "bench", nodes, edges };
}

const fcSeedA = fixtureFlowchart(16, 4);
const fcSeedB = fixtureFlowchart(8, 2);

function syntheticEvents(count: number): Event[] {
  const events: Event[] = [];
  events.push(validateEvent({
    id: eid(0), schemaVersion: 1, occurredAt: T0, runId: RUN_ID,
    type: "RUN_STARTED", actor: "cli", payload: {}
  }));
  for (let i = 1; i < count; i++) {
    events.push(validateEvent({
      id: eid(i), schemaVersion: 1, occurredAt: T0, runId: RUN_ID,
      type: "AGENT_EVENT", actor: "executor",
      taskId: tid(i % 16),
      payload: { agentInstanceId: aid(i % 16), kind: "TEXT_DELTA", summary: `delta chunk ${i} of a realistically sized agent message summary line` }
    }));
  }
  return events;
}

const events300 = syntheticEvents(300);
const events100 = syntheticEvents(100);

const LIMITS: FlowchartRunLimits = { maxConcurrentNodes: 2, maxConsecutiveStalls: 3 };
function checkpointFor(fc: Flowchart): RunCheckpoint {
  const supervisor = createFlowchartSupervisor({
    flowchart: validateFlowchart(fc), router: snapshotValidationRouter(), limits: LIMITS
  });
  const replayed = replayRun(events100);
  return validateCheckpoint(materializeCheckpoint(replayed, T0, {
    definition: validateFlowchart(fc), snapshot: supervisor.snapshot(), limits: LIMITS
  }));
}

const children48: CompilableChild[] = Array.from({ length: 48 }, (_, i) => ({
  taskId: tid(1000 + i), role: "implementer" as const, objective: `child objective ${i}`,
  ...(i > 0 && i % 3 === 0 ? { dependsOn: [tid(1000 + i - 1)] } : {})
}));
const fc48 = compileChildrenToFlowchart(children48);
const checkpoint48 = checkpointFor(fc48);

/* ---- Part A: CPU plane -------------------------------------------------- */
console.log("\n[A] CPU plane (R8-G..R11-G probe set re-run)");
const vfA = bench("validateFlowchart 16n join4 (seedA)", 20000, () => validateFlowchart(fcSeedA));
bench("validateFlowchart  8n join2 (seedB)", 20000, () => validateFlowchart(fcSeedB));
console.log(`  -> x32 rounds = ${(vfA * 32 / 1000).toFixed(3)} ms/run ceiling`);

const judge = new DeterministicJudge();
const evd = Array.from({ length: 8 }, (_, i) => `evd_r12g${i}` as never);
bench("DeterministicJudge.decide 8x8", 100000, () => {
  judge.decide({ taskId: tid(0), verification: { kind: "PASSED", evidenceIds: evd, notes: "" } as never, evidenceIds: evd });
});
bench("expandTaskTransition PENDING->FAILED", 200000, () => { expandTaskTransition("PENDING", "FAILED"); });
bench("compileChildrenToFlowchart 16 children (one-shot)", 20000, () => {
  compileChildrenToFlowchart(children48.slice(0, 16));
});
const veUs = bench("validateEvent AGENT_EVENT", 100000, () => { validateEvent(events300[7] as never); });
const rrUs = bench("replayRun 300 events", 5000, () => { replayRun(events300); });
console.log(`  -> readAll(300) compute share = ${(veUs * 300 / 1000 + rrUs / 1000).toFixed(3)} ms`);
bench("validateCheckpoint full (N=48, incl. restore)", 2000, () => { validateCheckpoint(JSON.parse(JSON.stringify(checkpoint48))); });

/* ---- Part B: I/O floor --------------------------------------------------- */
console.log("\n[B] I/O floor on this VM");
const ioRoot = mkdtempSync(join(tmpdir(), "r12g-bench-"));
let epiReadUs = 0;
let epiAppendTerminalUs = 0;
let epiEvtAppendUs = 0;
let evAppendUs = 0;
try {
  const ckStore = new CheckpointStore(ioRoot, RUN_ID);
  const ckWriteUs = await benchAsync("CheckpointStore.write durable (N=48)", 200, async () => {
    await ckStore.write(checkpoint48);
  });
  console.log(`  -> x66 writes/run (R6-G count) = ${(ckWriteUs * 66 / 1000).toFixed(1)} ms/run`);

  const evStore = new EventStore(ioRoot, RUN_ID);
  let n = 0;
  evAppendUs = await benchAsync("EventStore.append AGENT_EVENT (no fsync)", 400, async () => {
    const e = { ...events300[1] as object, id: eid(100000 + n++) } as Event;
    await evStore.append(e);
  });
  console.log(`  -> x300 events/run = ${(evAppendUs * 300 / 1000).toFixed(1)} ms/run`);

  const evStoreR = new EventStore(ioRoot, "run_r12g-readbench" as RunId);
  for (const e of events300) await evStoreR.append({ ...(e as object), runId: "run_r12g-readbench" } as Event);
  const raUs = await benchAsync("EventStore.readAll 300 events", 300, async () => { await evStoreR.readAll(); });
  const evStoreS = new EventStore(ioRoot, "run_r12g-readsmall" as RunId);
  for (const e of events100) await evStoreS.append({ ...(e as object), runId: "run_r12g-readsmall" } as Event);
  const raSmallUs = await benchAsync("EventStore.readAll 100 events", 300, async () => { await evStoreS.readAll(); });
  console.log(`  -> x96 readAll/run at avg mid-run size = ${(((raUs + raSmallUs) / 2) * 96 / 1000).toFixed(1)} ms/run`);

  const pause = createFilePauseController(ioRoot, () => T0);
  await benchAsync("pause token() ENOENT (not paused)", 2000, async () => {
    await pause.token(RUN_ID);
  });
  await pause.requestPause(RUN_ID, "bench");
  await benchAsync("pause token() present (paused)", 2000, async () => { await pause.token(RUN_ID); });
  await pause.clearPause(RUN_ID);

  await benchAsync("loadLearnedRouting (no registry snapshot)", 2000, async () => {
    await loadLearnedRouting(ioRoot, "/tmp/nonexistent-project");
  });

  // Episode-store op costs (settle-cost arithmetic).
  const EPI = "ep_r12gbench" as EpisodeId;
  const epiFixture = (status: ProjectEpisode["status"]): ProjectEpisode => ({
    id: EPI,
    projectId: "prj_r12g" as ProjectId,
    objective: "bench episode objective",
    contractVersion: 1,
    runIds: [RUN_ID],
    startedAt: T0,
    status,
    acceptance: [{ id: "run-complete", description: "The run reaches a terminal status", observableCheck: "run.status is terminal" }],
    evidenceRefs: []
  });
  {
    const root = mkdtempSync(join(tmpdir(), "r12g-epi-"));
    try {
      const snapshots = new EpisodeStore(root, EPI);
      await snapshots.append(epiFixture("OPEN"));
      await snapshots.append(epiFixture("OPEN"));
      epiReadUs = await benchAsync("EpisodeStore.readAll (2 snapshots)", 1000, async () => { await snapshots.readAll(); });
      await benchAsync("EpisodeStore.append OPEN (no fsync)", 300, async () => {
        await snapshots.append(epiFixture("OPEN"));
      });
      epiAppendTerminalUs = await benchAsync("EpisodeStore.append COMPLETED (fsync)", 200, async () => {
        await snapshots.append(epiFixture("COMPLETED"));
      });
      const epiEvents = new EpisodeEventStore(root, EPI);
      let m = 0;
      epiEvtAppendUs = await benchAsync("EpisodeEventStore.append (no fsync)", 300, async () => {
        await epiEvents.append({ type: "EPISODE_CLOSED", episodeId: EPI, status: "COMPLETED", closedAt: T0, seq: m++ } as never);
      });
    } finally {
      rmSync(root, { recursive: true, force: true });
    }
  }
  console.log(`  (settle-close arithmetic: ${((epiReadUs + epiAppendTerminalUs + epiEvtAppendUs + evAppendUs) / 1000).toFixed(3)} ms per terminal command)`);
} finally {
  rmSync(ioRoot, { recursive: true, force: true });
}

/* ---- Part C: configured-state x command-class matrix --------------------- */
console.log("\n[C] configured-state x command-class matrix (real command surface; R10-G 14 cells re-run)");

interface OpCounts {
  evAppend: number; evAppendFsync: number; evReadAll: number; evEventsRead: number;
  cpWrite: number; cpRead: number;
  pauseToken: number; pauseRequest: number; pauseClear: number;
  epiAppend: number; epiReadAll: number; epiEvtAppend: number;
}
const zeroCounts = (): OpCounts => ({
  evAppend: 0, evAppendFsync: 0, evReadAll: 0, evEventsRead: 0,
  cpWrite: 0, cpRead: 0, pauseToken: 0, pauseRequest: 0, pauseClear: 0,
  epiAppend: 0, epiReadAll: 0, epiEvtAppend: 0
});
let counts = zeroCounts();
const TERMINAL_EVENT_TYPES = new Set(["RUN_COMPLETED", "RUN_FAILED", "RUN_CANCEL_REQUESTED"]);

// Prototype instrumentation (observation only; no behavior change).
const origEvAppend = EventStore.prototype.append;
EventStore.prototype.append = function (event: Event) {
  counts.evAppend += 1;
  if (TERMINAL_EVENT_TYPES.has(event.type)) counts.evAppendFsync += 1;
  return origEvAppend.call(this, event);
};
const origEvReadAll = EventStore.prototype.readAll;
EventStore.prototype.readAll = async function () {
  counts.evReadAll += 1;
  const out = await origEvReadAll.call(this);
  counts.evEventsRead += out.events.length;
  return out;
};
const origCpWrite = CheckpointStore.prototype.write;
CheckpointStore.prototype.write = function (checkpoint: unknown) {
  counts.cpWrite += 1;
  return origCpWrite.call(this, checkpoint);
};
const origCpRead = CheckpointStore.prototype.read;
CheckpointStore.prototype.read = function () {
  counts.cpRead += 1;
  return origCpRead.call(this);
};
const origEpiAppend = EpisodeStore.prototype.append;
EpisodeStore.prototype.append = function (episode: never) {
  counts.epiAppend += 1;
  return origEpiAppend.call(this, episode);
};
const origEpiReadAll = EpisodeStore.prototype.readAll;
EpisodeStore.prototype.readAll = function () {
  counts.epiReadAll += 1;
  return origEpiReadAll.call(this);
};
const origEpiEvtAppend = EpisodeEventStore.prototype.append;
EpisodeEventStore.prototype.append = function (event: never) {
  counts.epiEvtAppend += 1;
  return origEpiEvtAppend.call(this, event);
};

function countingPause(inner: PauseController): PauseController {
  return {
    requestPause(runId, reason) { counts.pauseRequest += 1; return inner.requestPause(runId, reason); },
    clearPause(runId) { counts.pauseClear += 1; return inner.clearPause(runId); },
    token(runId) { counts.pauseToken += 1; return inner.token(runId); }
  };
}

function sequenceGenerator(): () => string {
  let n = 0;
  return () => `00000000-0000-4000-8000-${String(n++).padStart(12, "0")}`;
}

const routerConfig = {
  policyVersion: "router-v1",
  models: [
    { id: "cheap", version: "cheap-v1", roles: ["actor", "critic"] as const, maxComplexity: "MEDIUM" as const, estimatedCostUsd: 0.1, estimatedDurationMs: 1_000 },
    { id: "premium", version: "premium-v1", roles: ["actor", "critic", "judge", "router"] as const, maxComplexity: "HIGH" as const, estimatedCostUsd: 0.5, estimatedDurationMs: 4_000 }
  ]
};
function router(): ModelRouter { return createModelRouter(routerConfig); }

interface MatrixNodeOpts {
  role?: FlowNode["role"]; models?: readonly string[]; preferred?: string;
  approvalRequired?: boolean; parallelGroup?: string; joinPolicy?: JoinPolicy;
}
function mnode(id: string, opts: MatrixNodeOpts = {}): FlowNode {
  return {
    id,
    taskId: createTaskId(() => id),
    role: opts.role ?? "actor",
    objective: `Do ${id}`,
    modelPolicy: {
      allowedModels: opts.models ?? ["cheap", "premium"],
      ...(opts.preferred !== undefined ? { preferredModel: opts.preferred } : {})
    },
    confidenceThreshold: validateConfidenceScore(0.7),
    approvalRequired: opts.approvalRequired ?? false,
    ...(opts.parallelGroup !== undefined ? { parallelGroup: opts.parallelGroup } : {}),
    ...(opts.joinPolicy !== undefined ? { joinPolicy: opts.joinPolicy } : {})
  };
}
const sEdge = (from: string, to: string): FlowEdge => ({ from, to, condition: { type: "success", expected: true } });

function chainFlowchart(): Flowchart {
  return {
    id: "matrix-chain",
    nodes: [mnode("a"), mnode("b"), mnode("c")],
    edges: [sEdge("a", "b"), sEdge("b", "c")]
  };
}
function approvalFlowchart(id: string): Flowchart {
  return {
    id,
    nodes: [
      mnode("a"),
      mnode("selector", { role: "router", models: ["premium"], approvalRequired: true }),
      mnode("pathA", { models: ["cheap"] }),
      mnode("pathB", { models: ["premium"], preferred: "premium" })
    ],
    edges: [sEdge("a", "selector"), sEdge("selector", "pathA"), sEdge("selector", "pathB")]
  };
}
function fakeResult(confidence: number, evidence: string): ChildNodeResult {
  return {
    outcome: "SUCCESS",
    confidence: validateConfidenceScore(confidence),
    evidenceIds: [evidence],
    facts: [{ key: "coverage", value: "green", confidence: validateConfidenceScore(confidence) }]
  };
}

interface Cell { command: string; state: string; result: string; ms: number; ops: OpCounts }

async function cell(
  cells: Cell[], command: string, state: string, fn: () => Promise<string>
): Promise<void> {
  counts = zeroCounts();
  const start = performance.now();
  let result: string;
  try {
    result = await fn();
  } catch (error) {
    result = `THROWS(${(error as Error).message.slice(0, 48)})`;
  }
  const ms = performance.now() - start;
  cells.push({ command, state, result, ms, ops: counts });
}

interface MatrixOutcome { cells: Cell[]; digest: string; run1LastEventTypes: string[]; run1Ms: number }

async function runMatrix(): Promise<MatrixOutcome> {
  const stateRoot = mkdtempSync(join(tmpdir(), "r12g-matrix-state-"));
  const projectRoot = mkdtempSync(join(tmpdir(), "r12g-matrix-proj-"));
  const cells: Cell[] = [];
  let run1LastEventTypes: string[] = [];
  let run1Ms = 0;
  try {
    const generateId = sequenceGenerator();
    const deps = {
      stateRoot,
      router: router(),
      now: () => parseIsoTimestamp("2026-08-24T09:00:00.000Z"),
      generateId,
      pause: countingPause(createFilePauseController(stateRoot, () => parseIsoTimestamp("2026-08-24T09:00:00.000Z")))
    };

    // C1: start -> COMPLETED (chain, results supplied).
    let run1 = "" as RunId;
    await cell(cells, "start", "fresh->COMPLETED", async () => {
      const out = await startFlowchartRun(deps, {
        projectRoot, flowchart: chainFlowchart(), objective: "matrix chain",
        childResults: { a: fakeResult(0.9, "evd_a"), b: fakeResult(0.9, "evd_b"), c: fakeResult(0.9, "evd_c") }
      });
      run1 = out.runId;
      run1LastEventTypes = out.events.slice(-3).map((e) => e.type);
      return out.status;
    });
    run1Ms = cells[cells.length - 1]!.ms;

    // C2: start -> WAITING_FOR_USER (approval gate).
    let run2 = "" as RunId;
    let planId = "";
    await cell(cells, "start", "fresh->WAITING", async () => {
      const out = await startFlowchartRun(deps, {
        projectRoot, flowchart: approvalFlowchart("matrix-approval"), objective: "matrix approval",
        childResults: { a: fakeResult(0.9, "evd_a2") }
      });
      run2 = out.runId;
      planId = out.pendingApproval?.plan.id ?? "";
      return out.status;
    });

    // C3: inspect @ WAITING (read-only command class).
    await cell(cells, "inspect", "WAITING", async () => (await inspectRun(stateRoot, run2)).status);

    // C4: pause @ WAITING_FOR_USER.
    await cell(cells, "pause", "WAITING", async () => (await pauseFlowchartRun(deps, run2, "matrix pause")).status);

    // C5: resume(unpause) @ PAUSED -> back to WAITING.
    await cell(cells, "resume(unpause)", "PAUSED", async () => (await resumeFlowchartRun(deps, run2, { unpause: true })).status);

    // C6: inject(fact) @ WAITING_FOR_USER.
    await cell(cells, "inject(fact)", "WAITING", async () =>
      (await injectFlowchartRun(deps, run2, { kind: "fact", actor: "user", confidence: 0.9, key: "matrix", value: "yes" })).status);

    // C7: approval @ WAITING_FOR_USER -> COMPLETED.
    await cell(cells, "resume(approval)", "WAITING", async () =>
      (await resumeFlowchartRun(deps, run2, {
        approvalReply: { approvalPlanId: planId, selectedActionIds: ["pathA"] },
        childResults: { pathA: fakeResult(0.85, "evd_pathA") }
      })).status);

    // C8: resume @ COMPLETED (terminal noop finish).
    await cell(cells, "resume(noop)", "COMPLETED", async () => (await resumeFlowchartRun(deps, run2)).status);

    // C9/C10: illegal cells.
    await cell(cells, "pause", "COMPLETED", async () => (await pauseFlowchartRun(deps, run2)).status);
    await cell(cells, "inject(fact)", "COMPLETED", async () =>
      (await injectFlowchartRun(deps, run2, { kind: "fact", actor: "user", confidence: 0.9, key: "k", value: "v" })).status);

    // C11: cross-process cancel observation.
    let run3 = "" as RunId;
    await cell(cells, "start", "fresh->WAITING(2)", async () => {
      const out = await startFlowchartRun(deps, {
        projectRoot, flowchart: approvalFlowchart("matrix-cancel"), objective: "matrix cancel",
        childResults: { a: fakeResult(0.9, "evd_a3") }
      });
      run3 = out.runId;
      return out.status;
    });
    await cell(cells, "cancel(x-process append)", "WAITING", async () => {
      const store = new EventStore(stateRoot, run3);
      await store.append({
        id: `evt_${generateId()}` as EventId, schemaVersion: 1,
        occurredAt: parseIsoTimestamp("2026-08-24T09:00:00.000Z"),
        runId: run3, type: "RUN_CANCEL_REQUESTED", actor: "cli", payload: {}
      } as Event);
      return "appended";
    });
    await cell(cells, "resume(observe cancel)", "CANCELLED", async () => (await resumeFlowchartRun(deps, run3)).status);

    // C14: inspect @ CANCELLED (read-only command class on a settled run).
    await cell(cells, "inspect", "CANCELLED", async () => (await inspectRun(stateRoot, run3)).status);

    // Digest: op counts + results + normalized persisted bytes.
    const normalize = (raw: string): string =>
      raw
        .replaceAll(stateRoot, "<STATE>")
        .replaceAll(projectRoot, "<PROJ>")
        .replace(/\d{4}-\d{2}-\d{2}T[0-9:.]+(Z|[+-]\d{2}:\d{2})/g, "<T>");
    const hash = createHash("sha256");
    hash.update(JSON.stringify(cells.map((c) => ({ command: c.command, state: c.state, result: normalize(c.result), ops: c.ops }))));
    for (const runId of [run1, run2, run3]) {
      const dir = join(stateRoot, "runtime", "runs", runId);
      hash.update(normalize(await readFile(join(dir, "events.jsonl"), "utf8")));
      hash.update(normalize(await readFile(join(dir, "checkpoint.json"), "utf8")));
    }
    return { cells, digest: hash.digest("hex"), run1LastEventTypes, run1Ms };
  } finally {
    rmSync(stateRoot, { recursive: true, force: true });
    rmSync(projectRoot, { recursive: true, force: true });
  }
}

const digests: string[] = [];
let firstCells: Cell[] = [];
let witnessTail: string[] = [];
let witnessRun1Ms = 0;
for (let iteration = 1; iteration <= 3; iteration += 1) {
  const out = await runMatrix();
  if (iteration === 1) {
    firstCells = out.cells;
    witnessTail = out.run1LastEventTypes;
    witnessRun1Ms = out.run1Ms;
  }
  digests.push(out.digest);
  console.log(`matrix iteration ${iteration}: digest=${out.digest.slice(0, 16)}...`);
}
console.log(`matrix deterministic (3 runs bit-identical after normalization): ${digests.every((d) => d === digests[0])}`);
console.log(`start->COMPLETED wall-clock: ${witnessRun1Ms.toFixed(1)} ms; returned events tail = [${witnessTail.join(", ")}]`);

console.log("\ncommand class            | state         | result                    |  ms    | ev.app(fsync) ev.readAll(evts) cp.w cp.r pz.tok pz.req pz.clr epi.app epi.read epiEvt");
for (const c of firstCells) {
  const o = c.ops;
  console.log(
    `${c.command.padEnd(24)} | ${c.state.padEnd(13)} | ${c.result.padEnd(25)} | ${c.ms.toFixed(1).padStart(6)} | ` +
    `${String(o.evAppend).padStart(6)}(${o.evAppendFsync}) ${String(o.evReadAll).padStart(6)}(${String(o.evEventsRead).padStart(4)}) ` +
    `${String(o.cpWrite).padStart(4)} ${String(o.cpRead).padStart(4)} ${String(o.pauseToken).padStart(6)} ${String(o.pauseRequest).padStart(6)} ${String(o.pauseClear).padStart(6)} ` +
    `${String(o.epiAppend).padStart(7)} ${String(o.epiReadAll).padStart(8)} ${String(o.epiEvtAppend).padStart(6)}`
  );
}

console.log("\ndone");
```

---

MORE_OPTIMA=no
BRANCH=cursor/r12-g-runtime-twelfth-pass-83a1
