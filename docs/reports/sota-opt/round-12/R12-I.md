MODEL_SLUG=claude-fable-5-thinking-xhigh

# R12-I：CLI / Pi adapter / config / telemetry 第十二遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `84aa66f`（派单 tip，含 R12-A/B/C/D/E/F 收口：S12-B-1..2 / S12-C-1 / S12-D-1..2 淘汰，R12-A / R12-E / R12-F 空枚举未铸 ID）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，`git diff 8dee7fb..HEAD -- src/cli src/pi-adapter src/config src/telemetry` 为空——**零漂移，连续第五轮字节不变**。全仓 `src/` 自 `8dee7fb` 后仅 `src/experiments/{canary,plan,shadow}.ts` 与 `src/routing/offline-logit.ts` 变动（S7-C / S7-F 已落地项），全部切片外；`package.json` / `pnpm-lock.yaml` 零漂移（`git diff --quiet` 通过）；切片相关测试面零漂移（`git diff --stat 8dee7fb..HEAD -- test/` 为空）。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致。R8-I 的「~28 / cli 16」维持 R9-I/R10-I/R11-I 判定：头部笔误计数而非文件漂移，git 为权威，全表见 §0。
- 前置阅读按令执行：README → EXCLUSIONS 全表（含 S12-B-1..2 / S12-C-1 / S12-D-1..2 新行）→ PROGRESS → round-12/PLAN → round-11/PLAN + R11-I → R10-I → R9-I ～ R1-I 按需 → 25 个切片源文件全量实际重读（含凭据面两文件的只读读码，未测量未立项）。
- 分支：`cursor/r12-i-cli-twelfth-pass-83a1`（已推送，未开 PR）。
- 环境：**双 Node 版本实测**——VM 默认 22.14.0（唯一入口 `/exec-daemon/node` shim，与 R9-I～R11-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规）。pnpm 10.17.1，`pnpm install --frozen-lockfile`（本 VM node_modules 初始缺失，冷装）+ `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**，与 R7-I～R11-I 同版本；exports 图逐项复核仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`），**无与模型表版本锁定的轻量 provider-id 子路径——S8-I-1 的唯一重开条件连续第五轮未满足**（本轮以「7 导出逐项特征化」把该审计收口到完备，见 §2.1）。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S12-I-\* ID（诚实空枚举，与 R9-I / R10-I / R11-I 同结局），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第十二组枚举而非历轮剩余项清理；空枚举的证据面比 R11-I 再进一层——R11-I 把 S8-I-1 重开物下钻到包内 `models.generated.js` 并证明三种 specifier 不可达；本轮补上此前从未被特征化的最后两个导出子路径（root `.` 与 `./compat`）的直接测量，使 pi-ai 0.84.1 的 **7 个导出全部被逐项特征化**，「重开物是否已在包内其它导出中 ship」的问题第一次得到完备否定（§2.1）：

1. **零漂移五重确认（连续第五轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变、pi-ai 0.84.1 exports 图未变）、测试面（切片相关测试目录自 `8dee7fb` 零变动）、生产调用图（本轮再 grep：`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` 失手回退 / `runtime.ts` 点用处 / `listed-model.ts` 同步面；pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方）、基线增量（`84aa66f` 相对 R11-I 基线仅 R12 系文档收口，全部切片外）。R8-I 关闭矩阵、R9-I～R11-I 抽测所依据的结构前提在本基线逐项成立。
2. **本 VM 锚点/矩阵抽测复测（未拷贝历轮数字）**：R9-I 5 格承重集 × 2 Node 版本全部 exit 0，结构逐格同构：地板 58.4/55.7 ms，**custom−builtin 差 children +51.3 / +29.1、track +58.7 / +24.1 ms（22.14/22.22）**——与 R11-I 带（+45.9/+22.8、+48.0/+23.4）、R10-I 带（+47.1/+36.6、+56.0/+26.4）、R9-I 带（+53.9/+26.9、+49.4/+28.2）同号、同量级、同 Node 版本梯度（五轮同构）。进程内冷载锚点：`providers/all` 57.6-73.1 ms@22.14 / 31.6-34.1 ms@22.22（warm 0.070-0.103 ms）；per-provider 表 anthropic 3.4/2.6、openai 1.1/0.9 ms（优势 ~9-28×）；失手解析首次 1.904/1.921 ms、重复失手中位 **0.108/0.097 ms**（复证 R10-I 记忆化否决）。S7-I-1 per-provider 优势与 custom 态回退归属完好（§1）。
3. **第十二组新角度枚举为空（§2）**，其中一项为本轮新下钻、新测量、仍不铸 ID：
   - **root `.` 与 `./compat` 导出的注册表可达性 + 成本特征化（§2.1）**：root 导入 **171.9/96.0 ms**（22.14/22.22），46 个导出中无任何模型表（modelish 命中仅 `InMemoryModelsStore`/`ModelsError`/`createImagesModels`/`createModels`/`modelsAreEqual`——全部是工厂/store 机制，不含数据表）；`./compat` 导入 **207.6/119.2 ms**，100 个导出同样无 `MODELS`。两者均**严格重于 providers/all 本身**（57.6-73.1/31.6-34.1 ms）且不含权威表——作为回退替代源在成本与可表示性上双重不成立。至此 7 个导出全部特征化：`.`（重机制）、`./compat`（更重超集）、`./providers/*`（per-provider 表 + all，S7-I-1 已用尽）、`./api/*`（API 机制，仅 --executor pi 面）、`./oauth`/`./bedrock-provider`/`./bun-oauth`（auth 机制）。**S8-I-1 重开条件的审计自此完备**：0.84.1 包内唯一轻量权威源 `models.generated.js` 未被任何导出 ship，三种 specifier 形态本 VM 双 Node 复测仍全部 blocked（ERR_INVALID_MODULE_SPECIFIER / ERR_PACKAGE_PATH_NOT_EXPORTED × 2）。此项为重开条件审计而非机制候选——不是自制 union 的 rename（未构造任何替代注册表），不铸 ID。
   - 其余全部角度逐条映射到排除行或历轮无 ID 收口（§2.2），无一越过落地线。
4. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**；r7i part D 载荷探针复证 builtin 构建后 providers/all 保持冷 62.2 ms、custom 回退命中缓存 2.1 ms）；切片相关子集 **132 用例：131 pass / 1 skip / 0 fail**（与 R11-I 逐字同计数）；全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2（与 R9-I～R11-I 逐字相同）；`pnpm typecheck` 与 `pnpm lint` 全绿。生产代码零改动。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读证据，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交 `scripts/round12-r12i-equivalence-sim.ts`（全部 harness/探针源码进附录 A，遵守 R7-F/R8-C/R9-I～R11-I 纪律）。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S11-\* / S12-B-1..2 / S12-C-1 / S12-D-1..2 条目；R9-I / R10-I / R11-I 空枚举维持，未事后发明 S9-I-\* / S10-I-\* / S11-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：`--children` 复用 smartChildPlan 已校准目录（main.ts L726-729 原样）、Pi runtime 点用处 `await import`（main.ts L149 / auth-session 函数体内；S4-I 败者为 **S4-I-2..5**，无 S4-I-1）、12 条分支独占分派模块惰性导入（赢家 ID **S5-I-1**）、per-provider 惰性模型表（`listed-model-lazy.ts` + `model-catalog.ts` L58）——零漂移即字节级证明，三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅对其**重开条件**做完备性审计（§2.1）——7 导出逐项特征化后确认 0.84.1 无轻量注册表导出，未满足，维持淘汰。硬不变量「`providers/all` 失手回退 + throw-outside-catch」逐字在位（`listed-model-lazy.ts` L34-37 未动）。root/compat 探针是重开条件证据，不是绕道重开，更不是自制 union（R11-I §2.1 三重围栏）或磁盘快照（R10-I §2.1）的 rename——本轮未构造任何替代注册表。
  - **未重开 S8-I-2**（预热无重叠窗口：ESM 实例化主线程 CPU）、**S8-I-3**（enabled 循环 Promise.all——µs + 未知 id 错误选择分歧）、**S7-I-2..4**、**S6-I-1..3**、**S5-I-2..5**、**S4-I-2..5**、**S3-I-1..6**、**S2-I-1..6**、**S1-I-1..8**。本轮读码逐字再确认（未立项）：flowchart 双 `buildLiveCatalogConfig`（S1-I-1）、providers.json 双读（S1-I-2，track 路径 main L609 + track/loop 内部）、loadLearnedRouting/loadProvidersConfig 死载荷下沉（S2-I-1/S3-I-3）、`smartChildPlan` assignments.find（S1-I-3）、双 createAgentProfileRegistry（S3-I-2）、stdout 批量（S3-I-4）、冲突检查提升（S4-I-4）。
  - **R10-I / R11-I 无 ID 收口维持**：跨进程版本键控磁盘快照、进程内失手记忆化（本轮重复失手中位 0.097-0.108 ms 三度复证 µs 族）、进程内自制 union（R11-I 三重围栏：硬不变量明令 + 键表不被 pi-ai 版本锁定 + 相等性不加载 providers/all 不可核验）、回退后 per-provider 探针短路（≤3.5 ms/provider，仅 mixed 态可达）。派单明令「不得为自制 union / 磁盘快照 / 进程内 memo 铸 rename ID」——本轮一项未铸。
  - **凭据面围栏遵守**：`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项（S1-I-5 / S7-I-4 维持关闭）。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；稀疏→已批准 R0 基线；双 LCB / 双归因在位；提升 proposal-first（Checkpoint F-PROD 开放）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`listed-model.ts` 同步公开面与惰性面经 `listed-model-common.ts` 字节等价关系未动；`providers/all` 失手回退 + throw-outside-catch 在位；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未推送专属分支。

## 1. 本 VM 复测：5 格承重抽测 + 进程内锚点（双 Node，未拷贝历轮数字）

R9-I～R11-I 抽测集原样复刻（configured 态夹具、每迭代全新拷贝 state-root、2 预热 + 10 测量、显式二进制全路径；harness 全文附录 A.1）。

### 1.1 抽测中位数（ms，spawn-to-exit，全部 exit 0）

| 格 | 22.14.0（`/exec-daemon/node`） | 22.22.2（nvm 全路径） |
|---|---|---|
| `--version`（地板） | 58.4 (min 56.6, p90 61.8) | 55.7 (50.9, 57.3) |
| run-children@builtin | 117.8 (112.7, 119.3) | 112.8 (109.1, 114.9) |
| run-track@builtin | 134.8 (130.4, 137.6) | 134.9 (126.2, 137.6) |
| run-children@custom | **169.1** (162.8, 174.1) | **141.9** (132.4, 144.7) |
| run-track@custom | **193.5** (180.2, 208.3) | **159.0** (152.4, 168.0) |

- **custom−builtin 差**：children **+51.3 / +29.1**、track **+58.7 / +24.1** ms（22.14/22.22）。R11-I 带 +45.9/+22.8 与 +48.0/+23.4，R10-I 带 +47.1/+36.6 与 +56.0/+26.4，R9-I 带 +53.9/+26.9 与 +49.4/+28.2——**五轮同号、同量级、同 Node 版本梯度**。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
- **builtin 态目录构建**比地板贵 +59.4/+57.1（children）——S7-I-1 + S1-I 在 configured 态按设计工作，无回归。本 VM 整体比 R11-I VM 慢 ~5-10%（地板 58.4 vs 53.0），属 VM 个体差，格间结构不变。
- **Node 版本项**：22.22.2 的节省集中在触发 `providers/all` 的 custom 格（−27.2 children / −34.5 track），地板与 builtin 格 ±5 ms 内——`getPackageScopeConfig` 病理属 Node 版本项的历轮判断第五轮吻合。环境项，非切片代码角度。

### 1.2 进程内冷载锚点（附录 A.2 脚本，工作区根执行）

| 锚点 | 22.14.0 | 22.22.2 |
|---|---|---|
| `providers/all` cold import（3 次） | 72.9 / 57.6 / 73.1 ms | 34.1 / 32.8 / 31.6 ms |
| `providers/all` warm（第二次 await） | 0.070-0.073 ms | 0.098-0.103 ms |
| per-provider `anthropic.models` cold | 3.4 ms | 2.6 ms |
| per-provider `openai.models` cold | 1.1 ms | 0.9 ms |
| `providers/all`（双表已载后） | 56.5 ms | 31.1 ms |
| custom id 失手解析首次 | 1.904 ms | 1.921 ms |
| 同上，重复失手中位（N=49） | **0.108 ms** | **0.097 ms** |

- per-provider 优势 ~9-28×，S7-I-1 归属完好；custom 格增量与 `providers/all` cold 同量级，回退归属自洽。
- 重复失手 ~0.10 ms 三度复证 R10-I：Node package-reader 已缓存失败解析，记忆化物理无收益（<0.3 ms/构建）。

## 2. 第十二组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类），每形态先对 EXCLUSIONS 全表 + 十一轮 I 报告做同机制先例检索。本轮把唯一数十 ms 结构（custom 回退）的重开条件审计推进到 **7 导出完备特征化**，是历轮未达到的完备度。

### 2.1 root `.` / `./compat` 注册表可达性 + 成本特征化——不铸 ID，S8-I-1 重开条件审计收口

R11-I 证明了 `models.generated.js`（包内唯一轻量权威源）经三种 specifier 均不可达，但 7 个导出中 root `.` 与 `./compat` 此前从未被直接特征化——理论上若任一者以低成本 re-export 权威表，重开条件即满足且无需上游改动。本轮直接测量（附录 A.3 脚本，双 Node）：

- **root `.` 导入**：171.9 ms@22.14 / 96.0 ms@22.22，46 个导出。以 `/model|MODEL|catalog|registry|builtin/i` 过滤仅命中 `InMemoryModelsStore` / `ModelsError` / `createImagesModels` / `createModels` / `modelsAreEqual`——全部为工厂/store/比较机制，**无任何数据表导出**。root 导入后 providers/all 仍需 30.2/25.9 ms（部分机制模块共享，但表装配未发生）——root 不是权威表的可达路径，且其自身成本约为 providers/all 的 2.4-3.0×。
- **`./compat` 导入**：207.6 ms@22.14 / 119.2 ms@22.22，100 个导出（API/auth/env 常量与流机制），无 `MODELS`、无 `getBuiltinModel`。是 root 的更重超集。
- **三种 `models.generated` specifier 形态本 VM 复测**：`providers/../models.generated` → ERR_INVALID_MODULE_SPECIFIER；`dist/models.generated.js` 与 `models.generated` → ERR_PACKAGE_PATH_NOT_EXPORTED（双 Node 一致，与 R11-I 逐字同结局）。
- **裁决**：7 个导出至此逐项特征化完毕——`./providers/*`（S7-I-1 已用尽 + all 回退）、`./api/*`（--executor pi 面机制）、`./oauth`/`./bedrock-provider`/`./bun-oauth`（auth 机制）、`.` 与 `./compat`（本轮：更重且无表）。**0.84.1 内不存在任何导出形态的轻量权威注册表**；S8-I-1 重开条件维持 R11-I 收紧后的表述（pi-ai 自身 ship `models.generated` 或等价 `./providers/all.models` 导出）且本轮确证该条件在现版本下无任何旁路。此项为审计证据，非机制候选：未构造替代注册表、未触碰回退路径，不属自制 union / 磁盘快照 / memo 三族中的任何 rename——**不铸 ID**。

### 2.2 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级即死）

| 扫描角度 | 处置 |
|---|---|
| custom id 跳回退 / 自制 union / 磁盘快照 / 进程内失手 memo | S8-I-1 硬禁 + R11-I/R10-I 同族无 ID 收口；重开条件本轮审计后仍未满足；派单明令不得铸 rename ID |
| root / compat 作替代注册表源 | **本轮新测量封死**（§2.1）：无表 + 严格更重，双重不成立 |
| providers/all 预热重叠 | S8-I-2 逐字（ESM 实例化主线程 CPU，无重叠窗口） |
| enabled 循环并行 per-provider 导入 | S8-I-3 逐字（µs + 未知 id 错误选择分歧） |
| 回退后 per-provider 探针短路（mixed 态） | R11-I §2.2 无 ID 收口（≤3.5 ms/provider，仅 mixed 态可达；跨调用穿线撞 X1-1） |
| pi-adapter barrel（`index.ts`）隐藏静态边 | 本轮再 grep 为无：全 src/ 无生产导入方；providers/all 的 src 内边仅 lazy 回退 / runtime 点用处 / 同步面 |
| flowchart 路径双 `buildLiveCatalogConfig` | S1-I-1 逐字（第二次构建全走 warm 模块缓存，~190 µs） |
| providers.json 双读（track：main L609 + loop 内部） | S1-I-2 逐字（~60 µs） |
| loadLearnedRouting / loadProvidersConfig 死载荷下沉 | S2-I-1 / S3-I-3 逐字（损坏时错误路径发散） |
| `smartChildPlan` assignments.find 换 Map / 双 registry | S1-I-3 / S3-I-2 逐字（children 个位数 / 8.4 µs） |
| pause/inject 免 router / 免 learned | S2-I-2 / S9-G-1 逐字 |
| resume/answer 预读去重 / PAUSED 探测降级 | S2-I-4 / S4-I-2 逐字 |
| compile-cache / 分派拆分 / 剩余独占边 | S6-I-1..3 逐字（once-per-process 亚两位数 ms） |
| dist 布局向 getPackageScopeConfig 开刀 / 引擎升级 | S5-I-2..5 族；Node 版本项（§1.1 梯度第五轮旁证），环境项非切片角度 |
| 一次性命令类（models/auth/doctor/inspect/episode/commits/adapt/pref） | S1-I-4 / S7-I-4 / S2-I-5 / S3-I-4 类；once-per-process 噪声按派单直接否决 |
| 凭据面（auth status --all、credential store 微观） | S1-I-5 / S7-I-4 围栏，只读证据 |
| buildInvocation / invocationError / parseModelRef / parseProvidersConfig / parseCliErrorJson 微观 | S1-I-7 / S2-I-3 / S3-I-1 / S3-I-5 / S2-I-6 / S1-I-6 逐字（实测更慢或 ns 级） |
| pi-executor / cluster-tools 执行面微观 | 网络 I/O 支配的 live 面；µs 级，无复杂度类角度 |
| telemetry INVOCATION_CALL_OUTCOMES includes→Set | 表长 4，S1-D-8 族噪声 |
| writeAtomicJson 去 fsync / doctor-overlay jsonl 扫描 | S9-G-3 族（不得去 fsync）/ S3-J-4 族 + 一次性诊断类 |
| `builtinModelLazy` 动态 specifier / `_MODELS` 键过滤微观 | 命中路径被表解析成本（1-3.5 ms）支配、失手路径 ~0.1 ms；µs 族 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖、pi-ai exports、测试面、调用图五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选；唯一数十 ms 结构（custom/mixed 态 providers/all 失手回退）的全部消除形态收敛到同一个上游前提——pi-ai ship 轻量权威子路径——该前提在 0.84.1 的 7 个导出内已被本轮完备排除旁路。

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**（part E：builtin-miss resolveListedModel 12.8 µs × M≤10）；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part C：17 组失手/custom/对抗用例 lazy===sync；part D 载荷探针：builtin 构建后 providers/all 保持冷 62.2 ms、custom 回退命中缓存 2.1 ms；part E：configured run --children 全轮 normalized-identical）。
- **测试**：切片相关子集（unit cli/config/telemetry/pi-adapter + pi-boundary + integration cli/pi-adapter/track）**132 用例：131 pass / 1 skip / 0 fail**（与 R11-I 逐字同计数）；全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2，与 R9-I/R10-I/R11-I 计数逐字相同。
- **typecheck / lint**：`pnpm typecheck` exit 0；`pnpm lint` exit 0。
- **引擎注记**：22.14.0 的 doctor engines 预检 fail-closed 为设计行为（R9-I §3 记录在案），本轮不重复展开；本 VM 22.14.0 仅经 `/exec-daemon/node` 可达。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动，无需 landed-sim 之外的额外 gate。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；本轮新特征化的 root/compat 双导出以直接测量封死其作为替代源的可能）。
- 剩余赢家：无（唯一数十 ms 结构被 S8-I-1 健全性反例 + 硬不变量封死；重开条件的包内旁路已被完备排除——所需的是 pi-ai 上游导出，不是仓内改动）。
- 矩阵空洞：无（R8-I 78 格全覆盖；本轮承重格五轮同构复现；mixed 态增量收益维持 R11-I 定界 ≤3.5 ms/provider）。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 5 格抽测 harness（/tmp/r12i-bench/spot.mjs，`<node二进制全路径> spot.mjs <同一路径>` 分别以两版本执行）

```javascript
// R12-I spot re-measure: floor + builtin/custom children+track cells (R9-I..R11-I harness style).
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, writeFileSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r12i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.argv[2];
const WARMUP = 2, ITERS = 10;

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
for (const [name, cfg] of Object.entries(states)) {
  const dir = join(FIX, `state-${name}`);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
}
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
const CHILDREN = join(FIX, "children.json");
writeFileSync(CHILDREN, JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");

const cells = [
  { id: "version", args: () => ["--version"], state: undefined },
  ...["builtin", "custom"].flatMap((state) => [
    { id: `run-children@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN] },
    { id: `run-track@${state}`, state, args: (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"] }
  ])
];
function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, `state-${state}`), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 120000 });
    const dt = performance.now() - t0;
    exit = r.status;
    if (i === 0 && r.status !== 0) console.error(`  FIRST-FAIL ${cell.id}: ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

原始输出（run 命令均为 `--executor` 缺省下的域内校验路径，全部 exit 0）：

```
== v22.14.0 ==
version: median=58.4ms min=56.6 p90=61.8 exit=0
run-children@builtin: median=117.8ms min=112.7 p90=119.3 exit=0
run-track@builtin: median=134.8ms min=130.4 p90=137.6 exit=0
run-children@custom: median=169.1ms min=162.8 p90=174.1 exit=0
run-track@custom: median=193.5ms min=180.2 p90=208.3 exit=0
== v22.22.2 ==
version: median=55.7ms min=50.9 p90=57.3 exit=0
run-children@builtin: median=112.8ms min=109.1 p90=114.9 exit=0
run-track@builtin: median=134.9ms min=126.2 p90=137.6 exit=0
run-children@custom: median=141.9ms min=132.4 p90=144.7 exit=0
run-track@custom: median=159.0ms min=152.4 p90=168.0 exit=0
```

### A.2 进程内冷载锚点脚本（临时置于 /workspace 根以获得 node_modules 解析，测毕删除）

```javascript
import { performance } from "node:perf_hooks";
const which = process.argv[2];
const label = process.version;
if (which === "tables") {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/anthropic.models");
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/openai.models");
  const t2 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const t3 = performance.now();
  console.log(`${label} anthropic.models=${(t1 - t0).toFixed(1)}ms openai.models=${(t2 - t1).toFixed(1)}ms providers/all-after-tables=${(t3 - t2).toFixed(1)}ms`);
} else if (which === "miss") {
  const N = 50; const times = [];
  for (let i = 0; i < N; i++) {
    const t0 = performance.now();
    try { await import("@earendil-works/pi-ai/providers/customco.models"); } catch { /* expected miss */ }
    times.push(performance.now() - t0);
  }
  const first = times[0];
  const rest = times.slice(1).sort((a, b) => a - b);
  console.log(`${label} miss-resolution: first=${first.toFixed(3)}ms repeat-median=${rest[Math.floor(rest.length / 2)].toFixed(3)}ms min=${rest[0].toFixed(3)}ms`);
} else {
  const t0 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  const dt = performance.now() - t0;
  const t1 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  console.log(`${label} providers/all: cold=${dt.toFixed(1)}ms warm=${(performance.now() - t1).toFixed(3)}ms`);
}
```

原始输出：

```
v22.14.0 providers/all: cold=72.9 / 57.6 / 73.1 ms  warm=0.070-0.073 ms
v22.22.2 providers/all: cold=34.1 / 32.8 / 31.6 ms  warm=0.098-0.103 ms
v22.14.0 anthropic.models=3.4ms openai.models=1.1ms providers/all-after-tables=56.5ms
v22.22.2 anthropic.models=2.6ms openai.models=0.9ms providers/all-after-tables=31.1ms
v22.14.0 miss-resolution: first=1.904ms repeat-median=0.108ms min=0.070ms
v22.22.2 miss-resolution: first=1.921ms repeat-median=0.097ms min=0.069ms
```

### A.3 root / compat / traversal 探针（§2.1 证据，同置 /workspace 根，测毕删除）

```javascript
import { performance } from "node:perf_hooks";
const label = process.version;
const which = process.argv[2];
if (which === "root") {
  const t0 = performance.now();
  const root = await import("@earendil-works/pi-ai");
  const t1 = performance.now();
  const keys = Object.keys(root);
  const modelish = keys.filter((k) => /model|MODEL|catalog|registry|builtin/i.test(k));
  console.log(`${label} root-import=${(t1 - t0).toFixed(1)}ms exports=${keys.length} modelish=[${modelish.join(",")}]`);
  const t2 = performance.now();
  await import("@earendil-works/pi-ai/providers/all");
  console.log(`${label} providers/all-after-root=${(performance.now() - t2).toFixed(1)}ms`);
} else if (which === "compat") {
  const t0 = performance.now();
  const compat = await import("@earendil-works/pi-ai/compat");
  const t1 = performance.now();
  const keys = Object.keys(compat);
  console.log(`${label} compat-import=${(t1 - t0).toFixed(1)}ms exports=${keys.length} keys=[${keys.slice(0, 20).join(",")}]`);
} else if (which === "traversal") {
  for (const spec of [
    "@earendil-works/pi-ai/providers/../models.generated",
    "@earendil-works/pi-ai/dist/models.generated.js",
    "@earendil-works/pi-ai/models.generated"
  ]) {
    try {
      await import(spec);
      console.log(`${label} REACHABLE: ${spec}`);
    } catch (e) {
      console.log(`${label} blocked: ${spec} -> ${e.code ?? e.message.slice(0, 60)}`);
    }
  }
}
```

原始输出：

```
v22.14.0 root-import=171.9ms exports=46 modelish=[InMemoryModelsStore,ModelsError,createImagesModels,createModels,modelsAreEqual]
v22.14.0 providers/all-after-root=30.2ms
v22.22.2 root-import=96.0ms exports=46 modelish=[InMemoryModelsStore,ModelsError,createImagesModels,createModels,modelsAreEqual]
v22.22.2 providers/all-after-root=25.9ms
v22.14.0 compat-import=207.6ms exports=100 keys=[ANTHROPIC_API_KEY_ENV,...]（无 MODELS/getBuiltinModel）
v22.22.2 compat-import=119.2ms exports=100 keys=（同上）
v22.14.0 blocked: @earendil-works/pi-ai/providers/../models.generated -> ERR_INVALID_MODULE_SPECIFIER
v22.14.0 blocked: @earendil-works/pi-ai/dist/models.generated.js -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.14.0 blocked: @earendil-works/pi-ai/models.generated -> ERR_PACKAGE_PATH_NOT_EXPORTED
v22.22.2 blocked: (同上三行，双 Node 一致)
```

MORE_OPTIMA=no
