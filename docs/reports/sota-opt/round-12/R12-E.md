MODEL_SLUG=claude-fable-5-thinking-xhigh

# R12-E：`src/learning/` 第十二遍复查报告（Round 12）

**战役:** 全库持久 SOTA 优化 Round 12 / R12-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `f661a87`（含 R12-B 摄取与 S12-B-1..2 排除已入表）
**分支:** `cursor/r12-e-learning-twelfth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第十二组新角度枚举为空（有理由的
空枚举，未铸造任何 S12-E-\* ID，与 R10-E / R11-E 同口径），切片维持关闭
（MORE_OPTIMA=no）。**

1. **零漂移三重确认**：切片 10 个文件（`wc -l` 确认恰 10 文件、合计 1770 行，
   无多无少）自 R1-E 裁决基线（`adb20d7`）至本轮基线（`f661a87`）**逐字节
   未变**——`git diff adb20d7..HEAD -- src/learning/` 为 0 行、
   `git log adb20d7..HEAD -- src/learning/` 为 **0 提交**，十二轮全程无人触及
   该目录。且 R10-E 基线（`8d598f5`）之后的全部提交（R10/R11 收口、R11-A..J
   报告摄取、R12-A/R12-B 摄取与 R12-C/D/E 派单）**仅触及
   `docs/reports/sota-opt/`**——`git diff --stat 8d598f5..HEAD -- src/ test/`
   为空，src 与 test 一行未动（`adb20d7..8d598f5` 间的 src 变更全部是切片外
   已落地战役赢家：cli/experiments/pi-adapter/routing，与本切片无交）。
   R1-E..R11-E 的全部规模测量、调用面图景与 S1-E-1..8 / S2-E-1..7 /
   S3-E-1..5 / S4-E-1..3 / S5-E-1..5 / S6-E-1..5 / S7-E-1..5 / S8-E-1..3 /
   S9-E-1..3 共 44 项切片排除裁决（R10-E / R11-E 未铸 ID，其空枚举收口一并
   继承）原样成立、原样继承。
2. **本 VM 锚点/矩阵重锚（未拷贝 R11-E 数字，遵派单令；注意本轮环境为
   engines 合规的 Node v22.22.2，而 R11-E 测于 v22.14.0）**：SLICE-CPU 总量
   上界锚点八次运行 **18.5–19.2µs/run**（collect 14.5–15.1µs、outcomes
   3.1–3.3µs、diagnose 0.13–0.15µs、bandit-build 0.6–0.7µs）——距落地线
   （≥10ms）**520–541×**，即使把切片 CPU 清零也远不达门槛，在历史带
   （~11–24µs/run）之内。本轮 diagnose 夹具（seed `0xe12e03`）分组数为 0，
   为此补测**分组上界夹具**（seed `0xe12e07` 拒绝采样、12 信号全数入组、
   6 组）得 diagnose **3.83–3.92µs**——以该上界替换后总量 ~22.0–23.0µs，
   仍 **434–454×** 低于门槛；按历史最保守口径（R7-E outcomes 带 7.0–7.3µs
   + 本轮分组上界 diagnose）总量 ~27.0µs，仍 **≥370×**。三种口径结论一致。
   配置态 live 面：`applyLearnedRouting` 非空策略 **258–295ns/task**
   （R10-E 264–289 / R11-E 254–281 同带）、空策略 **111–128ns/task**
   （R11-E 113–125 同带）；`loadLearnedRouting` 全新 root 25.5–33.6µs、
   现实 registry **76.1–83.2µs**（R8-E 80–85 / R11-E 76.0–84.5 同带，
   亚 ms 一次性）。全部确定性 VERDICT 行八次运行**逐位一致**，3/3 sanity
   检查全过（§1、附录 A）。
3. **配置态 × 命令类矩阵复核无洞**：全部切片进入点行号逐条 grep 复核，
   **与 R8-E/R9-E/R10-E/R11-E 记录逐一相同、无新增调用方、无新增命令类**
   （`cli/adapt:168/188/205`、`cli/main:708/783`、`routing/assign:102`、
   `run/flowchart-run:681/712`、`track/loop:88/172`；resume/replay 经
   `flowchartForSupervisor` @ `flowchart-run:880/1021` 各单次载入；
   `run --flowchart` @ `cli/main:580` 在 `:708` 载入前返回，无双载入）。
   S8-E-1 双载入（`cli/main:708`+`flowchart-run:712` 与
   `track/loop:88`+`flowchart-run:712`）按令保护，未做任何去重分析。
   切片外唯一其余消费者仍为 `adaptation/eval-routing.ts:223` 的
   `parseLearnedRoutingPolicy`（离线 eval 面，D 切片辖区）。
4. **第十二组新角度枚举为空（§2）**：按「结构位置 × 消除机制」对第十二遍
   全量重读的 10 个源文件做双向扫描，本轮定位的全部形态（含五个此前从未
   逐字点名的结构位置：`stableProjectKey` 跨函数重复求值、
   `outcomesFromRoutedRun` 内部字面量全量再校验、`updateProjectBandit`
   奖励环 `recordReward` 不可变链批量化、pass-1 五 Map 无消费者惰性构建、
   `learnedRoutingPath` 死导出）要么是既有排除 ID/已关闭机制类的换名或
   换位点重现，要么被落地线算术直接封死（整个 76–83µs 载入面被派单明文
   列为 reject，其内部微观无候选资格），要么是零运行时收益的纯重构。
   **未铸造任何 S12-E-\* 新 ID，无新增排除提议。**
5. **验证面**：切片相关套件全绿（**48 tests / 2 suites / 48 pass / 0 fail**，
   Node v22.22.2）；`pnpm typecheck` 与 `pnpm lint` 全过。零代码改动下
   全部硬不变量天然满足。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与
双归因未动；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故
未提交 `scripts/round12-r12e-equivalence-sim.ts`（锚点脚本全文进附录 A，
遵守 R7-F/R8-C/R9-I/R10-E/R11-E 无赢家纪律）。本切片在其输出契约与数据面
语义下维持 SOTA——第十二遍复查确认：**剩余的全部 ms 级余量仍全部在被排除表
点名保护的 I/O 契约面上**，切片级收口条件（R3-E §7 … R11-E §6）依然成立。
**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop`
  283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 /
  `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 /
  `task-success` 69，合计 1770 行）本轮第十二遍全量实际读码，未依赖前十一轮
  记忆。上下游 `adaptation/`、`routing/`、`run/`、`track/`、`cli/`、
  `feedback/`、`persist/`、`privacy/` 只读取证，一行未改。
- 先读并遵守（顺序强制）：README → EXCLUSIONS.md（完整表，含 S12-B-1..2）→
  PROGRESS.md → round-12/PLAN.md → round-11/PLAN.md + round-11/R11-E.md →
  round-10/R10-E.md（空枚举，未铸 S10-E-\*）→ R9-E … R1-E 按需 → 10 个
  源文件。R12-C / R12-D 在其他分支在跑，未等待、未触碰其报告。
- 基线漂移检查（§结论 1）：切片零提交零 diff；`8d598f5..f661a87` 仅
  `docs/reports/sota-opt/` 文档——前十一轮全部测量与裁决原样成立。
- 排除表遵守：未重开任何 X0-\* … X4-\*、S1-\* … S11-\*、S12-B-1..2 条目。
  特别地：
  - **S12-B-1 / S12-B-2**（B 切片，本轮新入表）未以任何变体在本切片重述：
    本切片无 per-route allowed-Set 站点（`applyLearnedRouting` 的 avoid-Set
    是另一位置且其空/非空两态锚点本轮均实测），亦无 prior 层调用方——
    「prior-memo / allowed 反索引」两族按令未在本切片追认。
  - **S8-E-1 双 `loadLearnedRouting`** 按令保护，本轮仅复核其行号与频次
    （§1），未做任何去重分析。并发 promote 下的新鲜性仍是其保留理由。
  - **S9-E-2 pass-2 预过滤是负优化（E=400 五次全负）**——本轮未再尝试
    任何两遍结构的工作集缩减变体（§2 行 4 的惰性构建变体判同族拒列）；
    两遍语义按硬不变量维持。
  - **S9-E-1 / S9-E-3 / X2-6 / X0-3 / X0-8** 及派单点名的全部「勿重述」
    条目未以任何新 ID 重述（§2 逐条换名检查）。
  - R10-E / R11-E 未铸 S10-E-\* / S11-E-\*，本轮亦未事后发明任何此类编号。
- **换名重提检查**（识别为既有方案换名/换位点，**未列为新候选**；R10-E §0
  的七条与 R11-E §2 的四条换名裁决全部原样维持，此处不再复抄）：本轮新
  定位形态的换名检查全部并入 §2 表格逐行给出，并逐一对照 S1-E-\* …
  S9-E-\* 全表与 R10-E/R11-E 两轮空枚举面。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；
  tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（R1 未接线）；
  稀疏 → 已批准 R0 基线；双 LCB（beta+normal）/ 双归因（logit+prob-add）
  在位；晋升 proposal-first；无阈值变更；CAS / 凭据 / 数据面契约 / 公开
  签名不动；fail-open / 隐藏模块缓存（X1-1）未引入；bandit/learned
  registry I/O 的 fsync 与完整性重哈希未触碰；corrupt registry
  fail-closed（S2-I-1 / S9-G-1 族）未触碰；两遍
  `collectSignalsFromEvents` 语义未触碰；未改任何测试。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未 force-push；
  未推送专属分支。
- 环境：本 VM nvm 仅装 Node v22.22.2（engines `>=22.19.0` 合规；与 R12-A
  测量环境同版本；R11-E 测于 v22.14.0，版本差属环境差异非代码差异）；
  依赖经 `pnpm install --frozen-lockfile` 全新安装（pnpm 10.17.1）。

## 1. 配置态 × 命令类矩阵复核 + SLICE-CPU 锚点

R7-I 教训（默认态夹具可能掩盖配置态主路径）按令复查。src 自 `8d598f5` 起
一行未动，故 R8-E..R11-E §1 矩阵表结构原样成立；本轮逐条 grep 复核全部
切片进入点行号，**与 R11-E 记录逐一相同、无新增调用方**：

| 命令类 × 配置态 | 切片进入点（本轮行号复核） | 频次与规模 |
| --- | --- | --- |
| `adapt auto --run` | `runAutoAdaptFromEvents` @ `cli/adapt:188` → `runAutoAdaptLoop` | 每进程一次，E≈41 |
| `adapt auto --project` | `runAutoAdaptLoop` @ `cli/adapt:205`（空事件表，S4-E-1 面） | 每进程一次 |
| `adapt learn --run` | `proposeRoutingFromRoutedEvents` @ `cli/adapt:168`（S2-E-2 面） | 每进程一次 |
| 普通 `run` | `loadLearnedRouting` @ `cli/main:708`（S2-I-1 已裁决） | 每进程一次 |
| `run --flowchart` | `startFlowchartRun` @ `cli/main:580` → `flowchartForSupervisor` @ `flowchart-run:747→712` 单次载入（在 `cli/main:708` 之前返回，无双载入） | 每进程一次 |
| `run --children` | `cli/main:708` 载入 → `applyLearnedRouting` @ `routing/assign:102` 每任务 → `startFlowchartRun`（@ `cli/main:748`）内 `flowchartForSupervisor` @ `flowchart-run:747→712` 第二次载入（S8-E-1，勿去重）→ `applyLearnedRouting` @ `flowchart-run:681` 每节点 → `runAutoAdaptLoop` @ `cli/main:783` 一次 | 双载入各一次；任务/节点 ≤10/几十 |
| `track`（startTrackedRun） | `track/loop:88` 载入 → 每任务 `applyLearnedRouting` → `startFlowchartRun` @ `track/loop:152` → `flowchart-run:712` 第二次载入（同 S8-E-1）→ `runAutoAdaptLoop` @ `track/loop:172` 一次 | 同上 |
| flowchart resume / replay | `flowchartForSupervisor` @ `flowchart-run:880` / `:1021` 各单次载入（无双载入，R8-E..R11-E 复核维持） | 每恢复一次 |
| SPARKLE_AUTO_ADAPT=0 | 仍收集（persistSignals + updateProjectBandit 照付），仅跳过 propose；R5-E 裁决维持 | 不变 |
| 测试专用面 | `patterns` / `attribution` / `signatures` / `compareSignatures` / `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `parseObservedSignal`(直调) / `proposeRoutingFromAssignments`(deprecated) / `saveLearnedRouting`(deprecated, throw-only) / `learnedRoutingPath`(死导出，见 §2) 交叉检索复核仍零生产调用方 | 不进热路径 |

切片外其余消费者复核：`adaptation/eval-routing.ts:20,223` 仅导入并调用
`parseLearnedRoutingPolicy`（离线 eval 面，D 切片）；`track/primary-split.ts:3`
仅导入类型；`privacy/record-classes.ts:212,224` 仅文档字符串提及路径布局。
**矩阵结论：无新洞**。配置态给本切片的全部新增工作仍是每任务/每节点一次
`applyLearnedRouting` + 每 run 至多两次 `loadLearnedRouting`（S8-E-1 已
点名保护），均在本轮实测锚点覆盖内。

SLICE-CPU 锚点本 VM 重测（八次运行区间；Node v22.22.2；seeds
`0xe12e01`–`0xe12e03`；夹具镜像 R8-E..R11-E 生成器，R1-A 组成 E=41）：

```text
collect=14.5-15.1us  outcomes=3.1-3.3us  diagnose=0.13-0.15us  bandit-build=0.6-0.7us
total in-slice CPU ~18.5-19.2us per full auto-adapt run
vs landing bar >=10000us  ->  520-541x below EVEN IF ZEROED
diagnose 分组上界补测（seed 0xe12e07 拒绝采样、S=12 全入组、6 组）= 3.83-3.92us
  -> 以上界替换后总量 ~22.0-23.0us，仍 434-454x 低于门槛
configured-state: applyLearnedRouting(avoid=10, M=10) = 258-295ns/task （R10-E 264-289 / R11-E 254-281 同带）
                  applyLearnedRouting(空策略)        = 111-128ns/task （R11-E 113-125 同带）
                  loadLearnedRouting fresh(ENOENT)   = 25.5-33.6us/次
                  loadLearnedRouting 现实 registry    = 76.1-83.2us/次 （亚 ms 一次性，S8-E-1 带）
```

注：本轮夹具（seed 组 `0xe12e01`–`0xe12e03`）与 R11-E 夹具组成不同
（collected=19 / outcomes=3 / diagnoseGroups=0 vs R11-E 的 15/2/1，夹具
差异非代码差异）；diagnose 主夹具分组数为 0（随机 12 信号中过 taskSuccess
四重过滤者期望 <1），故补测分组上界夹具单独锚定分组路径（`reduce` /
`filter` / `unique` / `mode` / `sort` 全数走到）。按历史最保守口径
（R7-E outcomes 带 7.0–7.3µs + 分组上界 diagnose 3.92µs 替换本轮项）总量
~27.0µs，仍 **≥370×** 低于门槛。三种口径下结论一致：**锚点复核成立，
该切片不存在不推翻既有排除就能达门槛的候选。**全部确定性 VERDICT 行
（夹具规模、live 面 sanity、registry 夹具产出激活策略）八次运行逐位一致，
3/3 sanity 检查全过。

## 2. 第十二组新角度枚举：有理由的空枚举

方法：对第十二遍全量重读的 10 个源文件按「结构位置 × 消除机制」双向扫描
（死代码/死导出 / 冗余求值 / 跨函数去重 / 快路径 / 分派整形 / 数据结构
替换 / 分配避免 / 批处理 / 惰性构建 / 校验跳过 / I/O 契约面），每个形态
先对 EXCLUSIONS 全表 + 十一轮 E 报告（含 R10-E/R11-E 两轮空枚举面）做
同机制先例检索（换名检查），只有「排除表未覆盖且理论收益可达数十 ms 或
复杂度类下降」者才立项。本轮扫描结果：**零个可立项形态**，逐类裁决如下
（均未铸造新 ID——按 R9-E..R11-E 方法论，排除行换名/换位点重现与已关闭类
的机制变体不重复立项；落地线算术直接封死的形态无候选资格）：

| 形态（本轮第十二遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| **`stableProjectKey` 跨函数重复求值**（`learned-routing:32-35`：同一 projectRoot 的 normalize 双 replace + toLowerCase + hash32，经 `routingPolicyIdentity`×2（S8-E-1 双载入）+ `banditPath`×1 + propose 路径 ×1，每 run 约 3–4 次）——此前十一轮从未逐字点名 | 每次 ~数十 ns（40 字符级路径哈希）× ≤4 次/run ⇒ 亚 µs | 落地线算术封死 + 已关闭类机制变体拒列：跨函数冗余求值已在三个层级闭合（S1-E-2 表达式内、S8-E-3 跨界、S9-E-3 跨分派树）；参数化记忆化 = X1-1 模块级隐藏缓存 | S1-E-2 / S8-E-3 / S9-E-3 / X1-1 |
| **`outcomesFromRoutedRun` 内部构造字面量全量再校验**（`from-episode:227-247`：切片内以受控字面量构造观测，仍逐行经切片外 `parseOutcomeObservation` 契约实施点全量校验 + 每行 try/catch）——从未逐字点名 | 每合规 TASK_RESULT 一次 O(字段数) 校验，once-per-process CLI 面合计 µs 级（本轮 outcomes 项全额 3.1–3.3µs 即天花板） | 防御纵深删除族拒列：与 S2-D-5（跳过已验证观测重复校验）、S3-H-1（生产链双校验去重）同型跨切片重现；绕过需平行非校验构造器 = X1-2 第二入口；且 `parseOutcomeObservation` 在切片外（routing/outcomes，只读区） | S2-D-5 / S3-H-1 族 + X1-2 |
| **`updateProjectBandit` 奖励环 `recordReward` 不可变链批量化**（`bandit-store:69-78`：每合格信号一次 O(A) spread 拷贝，改为可变累加后单次构造）——从未逐字点名 | 合格信号 ≤S=12、A≈3 ⇒ 每 run ≤µs 级拷贝费 | 已关闭类机制变体拒列：批量化须在切片内复制切片外公开 `recordReward` 的 unknown-arm 验证 + 累加语义（X1-2 复制集中化逻辑族）；bandit 面被文件锁 I/O 支配（S3-E-4 实测结论、S6-E-3 mkdir 22.7µs 同带）；环内 `arms.includes` 前置守卫是防御纵深 | X1-2 / S3-E-4 / S6-E-3 |
| **pass-1 五 Map 无消费者惰性构建**（`signals:131-157`：events 无 TASK_RESULT/JUDGE_DECISION 时 pass-1 索引全死）——两遍结构的第四变体 | 无消费者档全额 pass-1；生产 run 恒有 TASK_RESULT ⇒ 生产不可达 | 三向封死 + 硬不变量明令：合遍（S1-E-1）/ 预过滤（S9-E-2 负优化）/ 空快路径（S4-E-1 生产不可达）已封两遍结构全维度，惰性构建是其第四变体；派单明令两遍语义不动 | S1-E-1 / S9-E-2 / S4-E-1 + 硬不变量 |
| **`learnedRoutingPath` 死导出**（`learned-routing:48-50`：src 全域零调用方，仅 `privacy/record-classes` 文档字符串提及路径布局）——从未逐字点名 | 0ns——死导出从不执行，删除属纯重构无任何运行时收益 | 不立项：无收益路径即无候选资格；公开签名必须保留（硬不变量），与 R11-E 对 `saveLearnedRouting` throw-only deprecated 的裁决同式 | R11-E §2 死导出行同式 + 硬不变量 |
| `persistSignals` 的 `evidenceIds.filter(isEvidenceId)` 双重求值（`auto-loop:212-214`） | 每信号 ns 级 | 换名重提拒列：S1-E-4 逐字同位点（被 appendFeedback I/O 支配） | S1-E-4 |
| `signalFromAgentMessage` TASK_RESULT 的 binding 构造 + taskSuccess 结果再 spread 回信号（`signals:304-335`） | 每 TASK_RESULT ~260ns | 换名重提拒列：S2-E-7 逐字同位点（契约实施点） | S2-E-7 |
| `truncate` 模板常量前缀（`user: ` / `peer: ` / `judge ` 等）每次参与 `\s+/g` 全扫描（`signals:394-397`） | 每信号 ns 级 | 已关闭类机制变体拒列：truncate 族三度闭合（S1-E-3 折叠、S2-E-5 流式早退更慢、S7-E-2 恒等快路径 µs 级淘汰） | S1-E-3 / S2-E-5 / S7-E-2 族 |
| `extractAssistant` 返回对象双形状（`{text}` vs `{text, model}`）PIC 单态化（`signals:391`） | 每文件 ns 级 | 换名重提拒列：S4-E-3 形状单态化＝形状不等价；S8-E-2 姊妹已裁决 | S4-E-3 / S8-E-2 族 |
| `diagnoseModelProjectIssues` 分组循环 `groups.get(key) ?? []` + push + 冗余 `set`（`diagnostics:27-29`） | 每信号 ns 级 | 换名重提拒列：S3-E-3 逐字同位点（公平副本对副本 ±8ns 抖动） | S3-E-3 |
| `unique()` O(n²) includes / `mode()` Map 计数（`diagnostics:56-78`） | kinds ≤4、每组一次 ⇒ ns 级 | 已关闭类机制变体拒列：小集合线性扫描族（R6-E/R7-E 收口、R10-E `familyFromRouted` includes 同裁决）；本轮分组上界实测 6 组全额 3.83–3.92µs 即天花板 | S1-E-6 / R10-E §2 行 4 域 |
| `proposeAndMaybePromote` / `proposeRoutingFromOutcomes` 的 `candidatesFor(identity).find(contentHash)` 线性扫描（`auto-loop:173-175` / `from-episode:112-114`） | C 个位数 ⇒ ns 级；且在 registry 锁内被 load/save I/O 支配 | 换名重提拒列：S1-D-2（candidatesFor identity 二级索引，C=8 实测更慢）同机制换调用侧；X0-3 保护两函数主体不合并 | S1-D-2 / X0-3 |
| `runAutoAdaptLoop` 的 `[...fromEvents, ...fromExtra, ...fromPi]` 三路拼接拷贝（`auto-loop:86`） | 每 run 一次 O(S) 拷贝 ⇒ ns 级 | 已关闭类机制变体拒列：分配避免族在本切片多位点闭合（S5-E-3 两级 spread、S8-E-2 条件 spread）；三数组直接下传改变 persistSignals/diagnose 的输入身份形态 | S5-E-3 / S8-E-2 族 |
| `applyCascadeRetry` 的 `eligible.includes` + 条件 spread（`from-episode:258-272`） | 每 TASK_RETRY ns 级 | 换名重提拒列：S9-E-1 毗邻（载荷形状按令未触碰）；小集合族 | S9-E-1 / 小集合族 |
| R10-E §2 七条 + R11-E §2 四条已点名换名形态（JUDGE_DECISION 双分派链、条件 spread 双 Map.get、`familyFromRouted` includes、`isCompleteRoute` 谓词序、Date.parse、空单例身份族、bandit 吸收析取；载入期校验微观、空 avoid 快路径、some-探针跨函数去重、不可达尾 return 70） | ns 级 | 全部维持 R10-E/R11-E 拒列，本轮读码确认位点未变（切片零 diff 下必然） | R10-E §2 / R11-E §2 |
| 两遍 `collectSignalsFromEvents` 的任何结构变换（合遍/预过滤/空快路径/惰性构建/工作集缓存） | pass 1+2 合计 ~14.5–15.1µs/run 全部 | 三向封死 + 硬不变量明令（本轮惰性构建变体见上行 4） | S1-E-1 / S9-E-2 / S4-E-1 + 硬不变量 |
| I/O 契约面全体：fsync 丢弃、完整性重哈希跳过、no-change 跳写、双载入去重、corrupt registry fail-open、readdir 并行化、惰性载入、mkdir 提升、紧凑序列化 | 切片相关全部 ms 级余量所在（本轮实测 registry 载入 76.1–83.2µs/次、写侧更贵） | 排除行逐字重现 + 硬不变量明令封死：全部数十 µs~ms 级形态均在排除表点名保护的 I/O 契约面上。特别地 `routingPolicyContent` 的 pretty-print 格式参与 contentHash ⇒ 任何序列化变更破坏 CAS 契约，比 S6-E-4 更硬 | X0-3 / S2-E-1 / S2-E-4 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1 / S1-E-4/5 / S2-I-1 / S9-G-1 族 + CAS |
| 死导出清理（`saveLearnedRouting` throw-only deprecated、`proposeRoutingFromAssignments` deprecated 转发、`learnedRoutingPath`） | 0ns 运行时 | 不立项：公开签名必须保留（硬不变量）；无运行时收益 | 硬不变量（公开签名） |
| 测试专用面任何优化（`patterns` / `attribution` / `signatures` 三文件 + `loadProjectBandit` / `taskSuccessFromExitCode` / `policyFromAssignments` / `compareSignatures`） | 不进任何生产路径 | 本轮交叉检索复核仍零生产调用方，自动拒列 | S1-E-7/8 / X2-6 族 |
| 漂移带来的新调用边 / 新命令类 / 新热路径 | — | 不存在：切片逐字节零漂移（十二轮）、src/test 自 R10-E 基线零漂移、调用位点行号全数复核一致（§1） | — |

**提议排除 ID：无（本轮无「新增排除 ID」表——空枚举未铸任何 S12-E-\*）。**
切片在现行排除表与硬不变量下不存在可达数十 ms 落地线的候选。前十一轮已
逐类收口的九个角度类（单遍化、分配避免、正则整形、数据结构替换、I/O 重叠、
形状单态化、死存储、工作集缩减、分派树合一）之外，第十二遍读码新探的五个
结构位置（跨函数键重复求值、内部字面量再校验、不可变链批量化、pass-1
惰性构建、死导出）全部落进已关闭机制类或落地线算术封死区：切片总 CPU
18.5–19.2µs（本 VM）意味着**即使发明一个能清零全切片 CPU 的新机制也距
门槛 520 倍以上**，唯一物理上可达 ms 的位置全部是被硬不变量与排除表双重
保护的 I/O 契约面。

## 3. 逐文件收口（前十一轮收口之上的本轮新检查点）

| 文件 | 本轮第十二遍新视角检查 | 结论 |
| --- | --- | --- |
| `signals.ts` | pass-1 五 Map 惰性构建变体（§2 行 4）判两遍结构第四变体拒列；truncate 常量前缀（§2 行 8）判 truncate 族拒列；extractAssistant 双形状（§2 行 9）判 S4-E-3 族拒列；binding 双拷贝（§2 行 7）= S2-E-7 逐字维持；两遍结构维持硬不变量；S1-E-1/2/3、S2-E-5/7、S3-E-1/5、S4-E-1/3、S5-E-2/3、S6-E-1/2/5、S7-E-1/2/5、S8-E-2、S9-E-2/3 维持 | 无候选 |
| `from-episode.ts` | 内部字面量全量再校验（§2 行 2）判防御纵深族 + X1-2 拒列；`applyCascadeRetry` includes/spread（§2 行 14）小集合族拒列；`candidatesFor.find`（§2 行 12）判 S1-D-2 换调用侧拒列；`policyFromOutcomes` seen-Set、`outcomeKindFromResult` 死形参、`familyFromRouted` includes、`isCompleteRoute` 谓词序、Date.parse 维持 R10-E/R11-E 拒列；S2-E-2、S7-E-3、S9-E-1 维持 | 无候选 |
| `task-success.ts` | S9-E-3 涉及面维持；`copyDefinedBinding`+`present()` 空白字段契约实施点不动；S2-E-7、S8-E-2 覆盖维持 | 无候选 |
| `auto-loop.ts` | 三路拼接拷贝（§2 行 13）判分配避免族拒列；`persistSignals` filter 双求值 = S1-E-4 逐字维持；some-探针跨函数去重维持 R11-E 拒列；I/O 契约边全点名维持（S1-E-4/5、S2-E-1、S3-E-2、S4-E-2、S5-E-1/5）；LEARN_EVALUATION_PLAN 双定义维持 R9-E 纯重构拒列 | 无候选 |
| `bandit-store.ts` | `recordReward` 不可变链批量化（§2 行 3）判 X1-2 复制 + I/O 支配拒列；`banditPath` 的 stableProjectKey 站点并入 §2 行 1 裁决；吸收析取、ENOENT catch 微整理拒列维持；fsync/重哈希/mkdir/序列化按令未触碰；S2-E-3/4、S3-E-4、S6-E-3/4、X1-2 维持 | 无候选 |
| `diagnostics.ts` | 主夹具 0 组（0.13–0.15µs）+ 分组上界补测 6 组 3.83–3.92µs 双档锚定（夹具差异非代码差异）；冗余 Map.set = S3-E-3 逐字维持；`unique`/`mode` 小集合族维持；S1-E-6、S4-E-3、S5-E-4 维持 | 无候选 |
| `learned-routing.ts` | `stableProjectKey` 跨函数重复求值（§2 行 1）判三层闭合 + X1-1 拒列；`learnedRoutingPath` 死导出（§2 行 5）判纯重构 + 公开签名拒列；`routingPolicyContent` pretty-print 参与 contentHash 判 CAS 契约维持；读路径完整性重哈希拒列维持（S8-D-2 同式）；S8-E-1（双载入，勿去重）、S8-E-3、S2-E-6、X1-1 维持；R11-E 载入校验微观/空 avoid 快路径拒列维持 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | 本轮交叉检索复核仍零生产调用方（仅测试使用）；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下切片相关套件基线复核，全绿（Node v22.22.2；依赖
`pnpm install --frozen-lockfile` 全新安装）：

```bash
npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0
pnpm typecheck   # 通过（0 错误）
pnpm lint        # 通过（0 错误）
```

锚点脚本（临时置于 `/tmp/r12e-anchor.mts` 与补测 `/tmp/r12e-diag-supp.mts`，
未入库以遵守无赢家纪律；完整差异面见附录 A，seeds `0xe12e01`–`0xe12e03` +
`0xe12e07`）主脚本共 **8 次独立运行**，每次 3/3 sanity 检查全过，全部
确定性 VERDICT 行跨 8 次运行**逐位一致**；补测脚本 3 次运行 VERDICT 行
逐位一致。代表性一次运行：

```text
VERDICT fixtures: E=41 collected=19 routedE=41 outcomes=3 S=12 diagnoseGroups=0 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=15.1us outcomes=3.3us diagnose=0.14us bandit-build=0.7us | total in-slice CPU ~19.2us per run vs landing bar >=10000us (520x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=278ns/task | empty policy=115ns/task -> x10 tasks/run = 2.78us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=27.2us | realistic registry (baseline+1 candidate)=77.7us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
--- 补测（分组上界） ---
VERDICT seed=0xe12e07 rejection-sampled S=12 all-qualifying diagnoseGroups=6 samples=[1,3,4,1,2,1]
BENCH diagnose(grouped upper-bound, S=12, groups=6) = 3.92us
```

计时跨 8 次运行汇总：SLICE-CPU 总量 18.5–19.2µs 稳定（520–541×）；
`applyLearnedRouting` 非空 258–295ns/task、空策略 111–128ns/task 稳定；
`loadLearnedRouting` fresh 25.5–33.6µs、现实 registry 76.1–83.2µs 稳定；
分组上界 diagnose 3.83–3.92µs 稳定。本轮无候选立项，故无败者仿真；无
新增等价性仿真脚本入库。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐形态有理由；第十二遍读码新探的五个结构
  位置全部落进已关闭机制类或落地线算术封死区，未发现第十个机制类）。
- 剩余赢家：无（全部 µs~ms 级余量在排除表点名保护 + 硬不变量明令封死的
  I/O 契约面上；CPU 面即使清零也距门槛 520–541×，最保守口径 ≥370×）。
- 矩阵空洞：无（调用位点行号与 R8-E..R11-E 逐一相同；配置态 live 面与
  载入面锚点全数复测在带内；本轮补齐 diagnose 分组路径的独立上界锚点）。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R11-E §6：SLICE-CPU 锚点失效（全切片 CPU
增长 ≥2 个量级，本轮复核值 18.5–19.2µs、分组上界口径 ~22.0–23.0µs、历史
保守口径 ~27.0µs）或任一 I/O 契约排除（X0-3 / S2-E-1/4 / S1-G-1 /
S1-E-4/5 / S4-E-2 / S5-E-5 / S6-E-3/4 / S8-E-1）被正式推翻，或切片/调用面
出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

主锚点脚本全文（`/tmp/r12e-anchor.mts`，`npx tsx /tmp/r12e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下五处替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R12-E；
`NOW = "2026-08-24T20:30:00.000Z"`；SECTION 1 的 seeds
`0xe12e01`/`0xe12e02`/`0xe12e03`；SECTION 2 的 `mkdtemp` 前缀
`r12e-fresh-`/`r12e-reg-` 与 detector identity `r12e-anchor`。其余逐字同
R10-E 附录 A（含 `genCollectEvents` / `genRoutedEvents` / `genSignal` /
`banditBuild` 四个生成器与全部 BENCH/VERDICT 逻辑），输出行格式与本报告
§1/§4 完全一致。

补测脚本（`/tmp/r12e-diag-supp.mts`）：复用同一 `mulberry32`/`genSignal`
生成器，以 seed `0xe12e07` 拒绝采样出 12 条全数通过 diagnose 四重过滤
（criterion=taskSuccess ∧ source≠user ∧ kind=deterministic ∧ modelId 非空）
的信号（产出 6 组、samples=[1,3,4,1,2,1]），对
`diagnoseModelProjectIssues` 以 40000 reps 计时——分组路径（分组 /
reduce / filter / unique / mode / sort）全数走到的**上界**夹具（生产混合
合格与不合格信号，密度低于此）。

8 次主脚本运行原始区间：

```text
SLICE-CPU:  19.2 / 18.5 / 18.7 / 19.1 / 19.0 / 19.1 / 19.0 / 18.7 us
collect:    15.1 / 14.5 / 14.8 / 15.1 / 15.0 / 15.0 / 15.1 / 14.7 us
outcomes:   3.3 / 3.2 / 3.2 / 3.2 / 3.2 / 3.3 / 3.1 / 3.2 us
diagnose:   0.14 / 0.13 / 0.13 / 0.13 / 0.15 / 0.13 / 0.13 / 0.13 us
bandit:     0.7 / 0.6 / 0.7 / 0.7 / 0.7 / 0.7 / 0.7 / 0.6 us
apply(non): 278 / 261 / 269 / 258 / 283 / 295 / 273 / 267 ns/task
apply(空):  115 / 117 / 115 / 113 / 128 / 111 / 114 / 114 ns/task
load fresh: 27.2 / 28.2 / 25.6 / 31.9 / 27.9 / 29.5 / 25.5 / 33.6 us
load real:  77.7 / 80.5 / 77.2 / 77.2 / 83.2 / 77.4 / 76.1 / 79.8 us
VERDICT 行: 8/8 逐位一致；sanity 3/3 x8 全过
补测 diagnose(6 组上界): 3.92 / 3.87 / 3.83 us（3 次，VERDICT 逐位一致）
```
