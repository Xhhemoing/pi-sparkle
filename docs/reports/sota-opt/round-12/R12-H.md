MODEL_SLUG=claude-fable-5-thinking-xhigh

# R12-H：`src/evaluation/` + `src/requirement/` + `src/review/` + `src/rubric/` 第十二遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 12 / R12-H
**基线:** `cursor/sota-persistent-opt-83a1` @ `ea1b214`（独占 tip，含 R12-A/B/C/D/E 全部摄取；S12-B-1..2 / S12-C-1 / S12-D-1..2 已入排除表；R12-A / R12-E 空枚举未铸 ID）
**分支:** `cursor/r12-h-eval-twelfth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十二遍枚举为空：无达门槛候选、零新 ID、切片零代码改动、切片维持关闭
（MORE_OPTIMA=no）。**
切片 21 个文件（1750 行 = 655+712+309+74，本轮 `wc -l` 逐目录复核一致；
8+7+4+2 清单逐目录确认恰 21 文件）自 R1-H 基线（`fd437a9`）以来**逐字节
未变**（`git diff fd437a9..ea1b214 --
src/{evaluation,requirement,review,rubric}/` 为 0 行、同范围 `git log`
为 **0 提交**，十二遍全程零 diff），且 R11-H 基线（`3855ab1`）之后
`src/` **零提交**（`git log 3855ab1..ea1b214 -- src/` 为空——其间全部
10 个提交均为 R11-F/G/H/I/J 与 R12 各波报告及排除表文档；
R12-A/B/C/D/E 全数无代码落地）⇒ 生产调用方地图**可证不变**（最强
形式），本轮全库 import 交叉检索再次独立确认（9 个切片外导入位点与
R3-H…R11-H 完全一致；`src/evaluation/` 全部 8 文件、
`review/{pairwise,reconcile,critic}.ts`、`src/rubric/` 及
`applyRoutingScoreUpdate`、`buildContractFromSources` 仍零生产调用方——
本轮定向 rg 证实这些导出的值级引用全部落在切片自身与 `test/`，未把
假想的未来接线当作今日流量）。按指令在本 VM 重锚「配置态（3 档）×
命令类（6 类）× 执行层（热/冷）」满格矩阵（§1.3–§1.5，三次独立运行，
Node v22.22.2）：热层默认态 **8.5–8.9µs**/run（居 R11-H 本带
8.5–9.1µs 与十二轮历史跨 VM 带 3.9–10µs 中上段）、配置态 A
**7.0–8.8µs**（三次中两次低于默认态、一次高 1.2%——首见的 +1.2% 单次
重叠属抖动量级，配置态与默认态同带的类结论不变：**无配置态悬崖**，
默认锚点仍是保守口径）、配置态 B **3.9–4.8µs**（三次全低于默认态）；
晋升类 18.6–19.8ns；四个零生产调用方命令类（evaluate 1.10–1.15µs /
critique 0.27–0.63µs / pairwise 2.0–2.1µs / reconcile 63–66ns）**全部
≤2.2µs**——调用图未变下即便未来接线也不达落地线（数十~数百 ms）；
冷层首调默认 1676–1704µs、配置 1617–1643µs once-per-process（与
R6-H…R11-H 同带，否决类归属不变）。第十二遍在完整排除表
（S1-H-1..9、S2-H-1..7、S3-H-1..4、S4-H-1..3、S5-H-1..3、S6-H-1..4、
S7-H-1..3、S8-H-1..3、S9-H-1..2 及 R10-H/R11-H 两轮空枚举收口共
50+ 处不立 ID 记录）之上以一个从未用过的完备性轴收口：**热链成本
剖面分解**（§2.1，R12-A 合同地板分解在本切片的对应物）——把默认态
生产链在完全分层稳态（3267–3583ns）下逐组件归账：切片外
`validateRequirementContract`（domain 面）双跑合计仅 **4.0–4.8%**、
extractor 切片侧（正则 + 合同字面量构造）**42–47%**、critique
**7–8%**、candidate-build 胶水 **35–40%**——且胶水桶内每一项都已有
裁决记录（S6-H-4 角色对构造 / S7-H-1 异步机器 / S2-H-1 sourceRefs
Set / S2-H-2 grounding Map / S3-H-1 第二次校验 / R10-H roleId trim），
**剖面归账零未解释质量**：不存在任何「消掉即达落地线」的成分（全链
即使清零也差 3–4 个数量级）。结构位置 × 消除机制全格扫描找到三处
**从未点名**的微观位点（P1 `checkCoverageGate` orphans 防御拷贝、
P2 `coverageMatrixFromTasks` Set 构造中间数组、P3 heuristicExtractor
vague 路径死 `wantsReview`），全部经理论 + 确定性仿真（seeded
mulberry32，40,000+ 项等价检查 + 真实规模基准，三次独立运行）裁决后
以既有家族 + 量级淘汰，**无一达到立 ID 门槛**（§2.2）；另有多处换名
重提在候选化之前即被识别拒列（§0）。诚实结论：枚举为空，不硬凑 ID，
与 R10-H / R11-H / R12-A / R12-E 空枚举收口同型。未重开任何 X* /
S1-* … S11-* / S12-B/C/D-* 条目；S5-H-1 守卫经本轮 diff + 读码双复核
**原样保留**；S8-H-3 全决议快路径**未做任何形态**；S8-H-1
PairwiseInput 未收窄。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/evaluation/`（8 文件：evaluator / delivery-adapter /
  diff-adapter / ownership / precedence / check-adapter / adapters /
  types）、`src/requirement/`（7 文件：normalizer / provenance /
  precedence / extractor / heuristic / coverage / critic）、
  `src/review/`（4 文件：pairwise / self-review / reconcile / critic）、
  `src/rubric/`（2 文件：registry / types）计 21 文件全量第十二遍实际
  读码，未依赖前十一轮记忆。上下游
  `track/{clarify,loop,plan,primary-split}.ts`、
  `run/{supervisor,coordinator,flowchart-run}.ts`、
  `adaptation/{promotion,promotion-rules}.ts`、`domain/contract.ts`
  只读取证（§1.2 逐调用点引用 + §2.1 剖面分解），一行未改。切片外
  其余树零触碰。邻区（F/G 本波运行中）未等待、未触碰其报告。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表 544 行，含
  S12-B-1..2 / S12-C-1 / S12-D-1..2）/ PROGRESS.md / round-12/PLAN.md /
  round-11/PLAN.md + R11-H.md / round-10/R10-H.md / R9-H … R1-H 按需
  （R10-H §2.1 导出面对账表与 R11-H §2.1 私有面对账表为本轮换名检查
  的基底）。
- **换名重提检查**（逐条核对，全部未列为新候选、未触碰）：
  - **S5-H-1（必须保留项）**：`requirement/precedence.ts` L24 的
    `if (fast.length === 0 || slow.length === 0) return [];` 分配前
    守卫经本轮读码 + `git diff fd437a9..HEAD --
    src/requirement/precedence.ts`（空）双复核**原样在位、逐字节
    未变**。本轮对该函数零候选、零「优化掉」提案。
  - **S8-H-3（永不重开项）**：`assertCoverageAllowsStart` 未做任何
    全决议快路径（含 resolved-superset 预判、gated 分支条件跳过、
    Q=0 免 `resolved` Set / 免 gated 拷贝、blocking 预扫描短路全部
    变形——R11-H 已把「Q=0 条件跳过」识别为 gated-branch 变体拒列，
    本轮维持不候选化）。`coverage.ts` 零 diff。
  - **S8-H-1（永不重开项）**：`PairwiseInput` 全形状（含 comment
    双字段）未收窄；盲评链 PIC 二态教训（S8-A-3 / S8-E-2 / S9-D-1
    同族）未重演。
  - **S9-H-1 / S9-H-2**：`self-review.ts` 三函数裁决记录完备（死
    中间体 = 逃逸分析地板；spread 单态化 = 晋升频次墙），本轮零重提。
  - 本轮三处新位点的**非换名性逐一论证**：P1（`checkCoverageGate`
    L22 orphans 防御拷贝省略）≠ S1-H-2 / S6-H-3（后两者是
    `assertCoverageAllowsStart` 的 **gated 合同拷贝**，P1 是 gate
    结果里 orphans 数组的防御 spread——不同函数不同对象）；P2
    （`coverageMatrixFromTasks` L52 `new Set(map(...))` 中间数组融合）
    ≠ R11-H L2（L2 是同函数内循环的**冗余重存**，P2 是 Set 构造的
    **中间数组**——同函数不同语句不同机制）≠ S1-H-1（S1-H-1 是
    `checkCoverageGate` 的 Object.keys→Set 提升，另一函数）；P3
    （heuristicExtractor vague 路径死 `wantsReview` 下沉）≠ R11-H L1
    （同为死计算下沉家族但位点不同：L1 在 diff-adapter FAIL 路径，
    P3 在 heuristic vague 路径——家族裁决沿用，位点首次点名）。
  - `changeSetsEqual` 三面（S1-H-9 / S2-H-4 / S3-H-4）、
    `registerRubric` copy-on-write（S1-H-8 反例 + S2-H-6；别名安全性
    本轮读码再确认：全部写路径整体替换 registry）、`heuristicCritic`
    omissions 双拷贝（S4-H-3）、adapter async→sync（S7-H-1 +
    X0-4）、normalizeSources 默认 origin 守卫（S2-H-7 防御纵深）、
    q-tests 子集正则短路（S3-H-3）、`Object.keys` 纯数组提升
    （S1-H-1 真子变体，R11-H §0 已拒列）、角色对象单例化
    （S6-H-4）、q-scope/q-write 恒假守卫与 heuristicCritic
    `acceptance-missing-while-questions-open` 恒假分支（R4-H §2 注
    no-ID：对未来分支扩展的防御）：全部维持，零候选。
  - **S12-B-1..2 / S12-C-1 / S12-D-1..2（本轮新入表，其他切片）**：
    均不在本切片，未触碰、未换名重提（本切片无 per-route
    allowed-Set 站点、无 prior 层、无 solveSymmetric 嵌套循环、无
    JSON.parse 预探针、无四遍回放链——S12-D-2 的「遍数融合负优化」
    教训与本切片 S2-H-3 / S3-H-2 四遍融合裁决同向，互为佐证而非
    重开）。
  - R10-H / R11-H / R12-A / R12-E 未铸 S10-H-* / S11-H-* /
    S12-A-* / S12-E-*，本轮亦未事后发明任何此类编号。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价（R1 未接线）、
  稀疏 → 已批准 R0 基线、双 LCB（beta+normal）与双归因
  （logit+prob-add）保留、晋升 proposal-first、阈值/CAS/凭据/数据面
  契约/公开签名不变、DEFAULT_REGISTRY 未就地变异、fail-open 零引入
  （含 S4-H-1 非可枚举键与 S8-H-3 空串 default/options 两个点名
  反例）、测试未改，天然成立。不声称 Outcome-supported，Checkpoint
  F-PROD 仍开放（ADR-005）。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，且本 VM
  `/exec-daemon/node`（v22.14.0）在 PATH 前段遮蔽 nvm bin（与 R11-H
  §0 注记同型）；测量与测试前已显式把 nvm v22.22.2 bin 置于 PATH 首
  并复核 `node --version` = v22.22.2。`pnpm install --frozen-lockfile`
  （pnpm 10.17.1）后 `pnpm build` / `pnpm typecheck` / `pnpm lint`
  全绿。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；未开 PR；未
  force-push；未推送专属分支；仅提交并推送工作分支。

## 1. 基线不变性、调用图复核与满格矩阵重锚

1. **切片逐字节未变**：`git diff fd437a9..ea1b214 --
   src/{evaluation,requirement,review,rubric}/` 输出为 0 行，同范围
   `git log` 为 0 提交（十二遍全程零 diff——指令要求的「若非空即停」
   检查通过）。21 文件 1750 行逐目录计数（655/712/309/74）与 R1-H
   清单一致。R1-H 逐函数下界表、R2-H 调用图上界、R3-H 重复工作枚举、
   R4-H 三类角度收口、R5-H 三层级收口、R6-H 执行层级收口、R7-H 机器
   成本三面收口、R8-H 配置态矩阵收口、R9-H 命令类矩阵满格 + 逃逸分析
   地板、R10-H 导出面完备性审计、R11-H 私有面完备性审计与全部
   S*-H-* 排除继承有效。
2. **调用图可证不变（最强形式）**：`git log 3855ab1..ea1b214 -- src/`
   **为空**——R11-H 基线以来全部 10 个提交均为 `docs/reports/` 文档
   （R11-F/G/H/I/J 报告摄取与 Round 12 五个波次的摄取/派单；
   R12-A/B/C/D/E 全数无代码落地）⇒ 生产调用方地图数学上不变。本轮
   全库 import 检索双确认（9 个切片外位点逐一读码取证）：
   `assertCoverageAllowsStart` ← `run/supervisor.ts` L498-500 /
   `run/coordinator.ts` L224-226 / `run/flowchart-run.ts` L757-759
   （每 run 启动一次，且仅当 `input.contract !== undefined`）；
   `extractHeuristicContract` ← `track/clarify.ts` L37（每 run 一次，
   恒以 `{objective, habits}` 形状调用、不传 sources）；
   `applyPrecedence` ← `track/loop.ts` L80（每 run 一次，
   `"user-first"`）；`shouldScout` ← `track/plan.ts` L42（每 run
   一次）；`assertCanPromoteFromReview` ←
   `adaptation/promotion-rules.ts` L81（每晋升一次）；其余全部为类型
   导入（`adaptation/promotion.ts` 的 ReviewerKind、
   `track/primary-split.ts` 与 `track/clarify.ts` 的 HeuristicHabits /
   ContractCandidate）。指令点名的历史零调用方面定向 rg 复核：
   `src/evaluation/` 全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、
   `src/rubric/` 及 `applyRoutingScoreUpdate`、
   `buildContractFromSources`（另含 `runBlindPairwisePair` /
   `createEvaluationRecord` / `reconcileReviews` /
   `createCriticObservation` / `registerRubric` / `classifyDiffScope` /
   `selectHighestPrecedence` 等全部值级导出）的切片外 `src/` 值级
   引用**为零**——未把假想接线当今日流量。
3. **热层三态锚点重锚**（三次独立运行，本 VM，Node 22.22.2；测量
   条件与 R10-H/R11-H 严格同构——benchAsync 单次预热 + 3000 计时，
   剖面分解块放在锚点之后以免额外预热污染可比性，见附录脚本注）：

```text
CEILING default  (9-pass anchor shape, Q=0):        8657 / 8472 / 8858 ns/run
CEILING configured A (habits 三键全设, Q=1 gated):   8760 / 7028 / 8260 ns/run
CEILING configured B (vague objective, Q=3 gated):   3931 / 4774 / 4147 ns/run
CEILING promotion class: assertCanPromoteFromReview = 19.8 / 18.9 / 18.6 ns/promotion
```

   默认态 8.5–8.9µs 落在 R11-H 本带（8.5–9.1µs）之内、十二轮历史跨
   VM 带（3.9–10µs）中上段。配置态 B 三次全部低于默认态；配置态 A
   三次中两次低于默认态、一次高 1.2%（8760 vs 8657）——十二轮首见的
   单次符号重叠，量级 ~100ns 属 JIT 分层抖动（同一脚本内 critique
   命令类三次测得 269/627/615ns 的既知双峰同源）；「无配置态悬崖、
   默认锚点是保守口径」的**类结论不变**：两档配置态与默认态同处
   µs 带，整个热面低于落地线 **3–4 个数量级**。gated 重映射真实
   执行（production assume-defaults 流恒传覆盖全部问题的
   `resolvedQuestionIds`）。
4. **命令类矩阵剩余格重锚**（R9-H 首锚的四个零生产调用方类，本轮
   三次复测）：

```text
CEILING evaluate class  (TEST-ONLY): createEvaluationRecord(3 criteria) = 1146 / 1104 / 1095 ns/call
CEILING critique class  (chain 内):  critiqueContract(real contract)    =  269 /  627 /  615 ns/call
CEILING pairwise class  (TEST-ONLY): runBlindPairwisePair               = 2111 / 2042 / 2036 ns/call
CEILING reconcile class (TEST-ONLY): reconcileReviews(n=2)              =   63 /   64 /   66 ns/call
```

   与 R9-H/R10-H/R11-H 锚点带一致（critique 双峰 ~250/~620ns 与
   R11-H 251/249/614 同构——JIT 分层状态，量级类属不变）。判据成立：
   **调用图未变下即便把这四个面全部接线，当前规模每类 ≤2.2µs，距
   落地线仍差 4+ 个数量级**。「配置态（3 档）× 命令类（6 类）×
   执行层（热/冷）」矩阵在本 VM 无未测格。
5. **冷层锚点复测（默认 + 配置双态）**：dist 产物、新进程、模块加载
   预扣除后，三生产入口首调合计中位数三次：默认态
   **1704 / 1681 / 1676 µs**（区间 [1644, 1780]µs）、配置态
   **1618 / 1643 / 1617 µs**（区间 [1597, 1793]µs）——与 R6-H
   1.65–1.71ms / R7-H 1.72–1.76ms / R8-H 1.6–1.7ms / R9-H 1.6–1.8ms /
   R10-H 1.6–1.7ms / R11-H 1.6–1.7ms 同带；once-per-process 否决类
   归属不变（S7-H-3 已证该层无净正对策）。

## 2. 第十二遍枚举：热链成本剖面分解（新轴）+ 三处从未点名位点裁决 → 诚实空结果

### 2.1 热链成本剖面分解（切片内 vs 切片外归账，本轮新轴）

前十一轮的完备性轴（单函数 / 函数间 / 跨模块 / 调用图 / 模块图 /
执行层级 / 调用机器 / 配置态 / 命令类 / 编译器地板 / 导出面 /
私有面）都以「函数」为归账单位；第十二遍换用 R12-A 在 A 切片的
合同地板分解思路，把默认态生产链在**完全分层稳态**下逐组件归账，
回答一个前十一轮从未直接回答的问题：**这 ~8.5µs 里有多少根本不属于
本切片？剩余质量是否全部有裁决记录？** 三次独立运行（组件与全链
同 JIT 状态、三轮交错测量）：

```text
PROFILE default chain = 3308 / 3583 / 3267 ns（完全分层稳态）
  validateRequirementContract ×2（domain 面，切片外）= 132–173ns = 4.0–4.8%
  extractor 切片侧（正则 + 合同字面量构造，validate 已扣除）= 1518–1555ns = 42.4–47.0%
  critiqueContract（含 findUnsourcedItems）= 254–265ns = 7.1–8.0%
  normalizeSources = 167–171ns；createTrustedSource = 37–38ns
  candidate-build 胶水（buildContractCandidate 编排残差）= 1139–1431ns = 34.7–39.9%
```

三个收口事实：

1. **切片外质量仅 4–4.8%**：`validateRequirementContract`（domain/，
   切片外只读）单跑仅 66–86ns，双跑合计不到链的 5%——S3-H-1（生产链
   双 validate 去重，134–185ns，删防御纵深拒列）的量级记录在稳态下
   独立复现。即便把切片外部分整体清零也移不动量级。
2. **胶水桶零未解释质量**：编排残差 1.1–1.4µs 的构成逐项皆有裁决
   记录——S6-H-4（extractor/critic 角色对每调用新建，466–853ns，
   偏好档需要拒列）、S7-H-1（三个 async 扩展点机器，167–175ns，
   X0-4 拒列）、S2-H-1（sourceRefs Set，44ns）、S2-H-2（grounding
   Map，44ns）、R10-H roleId trim no-ID、R2-H `[...sources]` 双拷贝
   no-ID。桶内无一新面孔。
3. **锚点保守性的直接证据**：3000-rep 锚点口径（8.5–8.9µs）与完全
   分层稳态（3.3–3.6µs）之差是 JIT 分层残余——历史锚点是**保守
   上界**，真实长驻进程中的每 run 成本更低，距落地线的量级差只会
   更大（4 个数量级）。

剖面轴由此闭合：本切片热链不存在任何「消掉即达落地线」的成分——
最大单桶（extractor 切片侧 ~1.5µs）即使**整体清零**也比落地线低
4 个数量级。

### 2.2 结构位置 × 消除机制扫描：三处从未点名位点，全部不立 ID

第十二遍全格对账（结构位置：入口守卫 / 循环体 / 返回构造 / 跨调用
重复 / 数据字面量 / 正则求值 / 防御校验 / 拷贝语义 / 异步机器 /
模块加载 × 消除机制：死码 / CSE / 融合 / 快路径 / 记忆化 / 表示
变换 / 去糖 / 惰性 / 可变化 / 复杂度类）。绝大多数格由既有 ID /
no-ID 收口直接覆盖（§0 换名重提检查 + R10-H §2.1 + R11-H §2.1 两张
对账表）；剩余三格是**从未被任何轮次点名**的位点，全部按门槛走完
理论 + 仿真 + 基准（附录脚本，seeded，40,000+ 项等价检查三次运行全
通过），逐一淘汰：

| # | 位点 | 理论 | 仿真等价 | 真实规模基准（三次） | 裁决 |
| --- | --- | --- | --- | --- | --- |
| P1 | `coverage.ts` `checkCoverageGate` L22 `[...matrix.orphanRequirements]` 防御 spread——生产路径经 `coverageMatrixFromTasks` 恒喂 `[]`，拷贝的是空数组 | 直接别名代替拷贝 | ✅ 20,000 组 fuzz（含 30% 非空 orphans 手造矩阵）JSON 逐字节一致；replica-vs-production sanity 闸零失配；**身份发散已实证**（候选返回值 `orphans === matrix.orphanRequirements`，落地即公开结果对输入矩阵别名化） | **+5.8 / −1.1 / +5.3ns**——符号翻转 | 淘汰三重：S1-B-8 / S2-J-5 防御拷贝家族（身份可观察改变）；每 run 一次 ns 级符号翻转＝零收益；`CoverageGateResult.orphans` 是公开 readonly 面，别名化让调用方可经输入矩阵观察到共享（X4-2 邻域） |
| P2 | `coverage.ts` `coverageMatrixFromTasks` L52 `new Set(criteria.map(id))`——Set 构造前物化一个中间 id 数组 | 单循环 add 融合，免中间数组 | ✅ 20,000 组 fuzz（1–6 任务 × 准则随机子集 × 幽灵准则）JSON 逐字节一致；replica sanity 闸零失配 | **+25.3 / +26.5 / +24.3ns**/call（方向稳定） | 淘汰：S1-C-5 / S5-J-6 融合家族在 Set-from-map 形态的又一例；频次每 run 一次（run-start 门内），~25ns 占默认态链 ~0.3%，低于落地线 **6 个数量级**；镜像位点 `provenance.ts` findUnsourcedItems L30 与 `extractor.ts` buildContractCandidate L83（后者已被 S2-H-1 覆盖）同机制同判——三站点一并收口，不再逐点重提 |
| P3 | `heuristic.ts` heuristicExtractor L49 `wantsReview = habits.preferReview !== false` 在 vague 分支前计算——vague 路径上可证死（confidence 恒 0.55，wantsReview 仅在 !vague 侧被 L143 消费） | 死比较下沉过 vague 分支 | ✅ 穷举等价（3 档 preferReview × 2 档 vague 全格）+ 生产 extractor 双形状 sanity 闸（vague=0.55 / precise=0.86 逐位） | **+0.20 / +0.18 / +0.19ns**——单次比较，深度亚噪声 | 淘汰：R11-H L1 / S3-D-5 死计算下沉家族（本例连分配都没有，只有一次严格不等比较）；vague 仅出现于 CLI 短语目标；V8 布尔比较即寄存器操作，源码删除无可测收益（S9-H-1 逃逸分析地板同象） |

三处合计即便全部落地且全部命中，上界 <32ns/run——比落地线低 6 个
数量级。**不立任何 S12-H-* ID**（与 R10-H / R11-H / R12-A / R12-E
空枚举收口同型：no-ID 收口记录在本报告即可，防止后续轮次重新发现）。

### 2.3 收口陈述

十二遍合计：单函数、函数间、跨模块、调用图、模块图、执行层级、调用
机器、配置态、命令类、编译器地板、导出面完备性、私有面完备性、
**成本剖面归账**十三个面全部有实测锚点或对账记录；剖面归账证明
剩余质量 100% 已有裁决记录且无「消掉即达线」成分。本切片在当前
调用图下**结构上不存在**达门槛候选。R11-H 总门槛维持并按本 VM 数字
刷新：任何未来候选须先推翻满格矩阵实测上界——热层默认
**8.5–8.9µs/run**（3000-rep 保守口径；完全分层稳态 3.3–3.6µs 更低）、
四个零调用方命令类 ≤2.2µs、冷层 **1.6–1.7ms once-per-process**
（否决类）。重开该切片的唯一前提维持 R4-H…R11-H 收口原文：调用图
变更（evaluation/review/rubric 面接入每 turn 热路径，或合同规模增长
≥2 个量级）。

## 3. 逐文件收口（第十二遍新视角，其余与 R1-H…R11-H 一致）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `requirement/coverage.ts` | **P1 orphans 防御拷贝裁决（§2.2，符号翻转 + 身份淘汰）**；**P2 Set 构造中间数组裁决（§2.2，~25ns 淘汰）**；S8-H-3 零形态复核（含 Q=0 变形不候选化）；S1-H-1/2、S4-H-1、S6-H-2/3 维持 | 无新候选 |
| `requirement/heuristic.ts` | **P3 vague 路径死 wantsReview 裁决（§2.2，亚 ns 淘汰）**；S1-H-3、S3-H-3、S6-H-4、S7-H-1、S8-H-2 维持；PATH_RE 模块级形态复核不变 | 无新候选 |
| `requirement/precedence.ts` | **S5-H-1 守卫在位复核（L24 早退，零 diff）**；S1-H-5、S4-H-2、S6-H-1 维持 | 无新候选 |
| `requirement/extractor.ts` | 剖面归账：胶水桶逐项对 S2-H-1/2、S3-H-1、S6-H-4、S7-H-1、R10-H roleId trim（§2.1-2，零未解释质量）；P2 镜像位点 L83 同判 | 无新候选 |
| `requirement/normalizer.ts` | 剖面归账 normalizeSources 167–171ns；S2-H-7、S7-H-2、R5-H slice no-ID、R10-H SlicedString 取证维持；P2 镜像判据不适用（无 Set-from-map 站点） | 无新候选 |
| `requirement/critic.ts` / `provenance.ts` | 剖面归账 critique 桶 254–265ns = 7–8%；S2-H-3、S3-H-2 维持；findUnsourcedItems L30 为 P2 镜像位点同判 | 无新候选 |
| `evaluation/evaluator.ts` / `check-adapter.ts` / `delivery-adapter.ts` / `diff-adapter.ts` | S1-H-7、changeSetsEqual 三面、S5-H-3、attributionMetadata 活性、R11-H L1、R10-H 合取序维持；命令类锚点复测 ≤2.2µs | 无新候选 |
| `evaluation/ownership.ts` / `precedence.ts` / `adapters.ts` / `types.ts` | X4-9、S2-H-5、三元表族、纯类型面维持 | 无新候选 |
| `review/pairwise.ts` / `reconcile.ts` / `critic.ts` / `self-review.ts` | S8-H-1、S1-H-6、S9-H-1/2 维持；pairwise/reconcile 锚点复测同带 | 无新候选 |
| `rubric/registry.ts` / `types.ts` | S1-H-8 反例 + S2-H-6 + R10-H createRubric 时间戳取证维持；DEFAULT_REGISTRY 别名安全第四次复核成立 | 无新候选 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。无仿真脚本入库（无赢家，遵循
round01–11 仅赢家 sim 入库惯例；完整脚本见附录）。

## 5. 测试

零代码改动下切片相关套件基线复核，全绿（Node 22.22.2 via nvm，满足
engines >=22.19.0；系统 Node 22.14.0 过低 + `/exec-daemon/node` PATH
遮蔽的环境注记见 §0；`pnpm install --frozen-lockfile` 后 `pnpm build` /
`pnpm typecheck` / `pnpm lint` 全绿）：

```bash
npx tsx --test "test/unit/requirement/*.test.ts" "test/unit/evaluation/*.test.ts" \
  "test/unit/review/*.test.ts" "test/integration/m3/checkpoint-d.test.ts" \
  "test/integration/m3/coverage-gate.test.ts" \
  "test/integration/m3/requirement-extraction.test.ts" \
  "test/integration/m4/delivery-evidence.test.ts"
# tests 93 / suites 13 / pass 93 / fail 0（与 R1-H…R11-H 同套件同计数）
```

仿真 + 锚点重锚（临时脚本 `/tmp/r12h-sim.mts`，无赢家故未入库；完整
源码见附录，seed 固定可复现；冷层探针以 `pnpm build` 后 dist 产物为
准）代表性一次运行：

```text
P1 bench checkCoverageGate orphans defensive spread: replica=49.3ns candidate=43.5ns delta=5.8ns/call (once per run inside run-start gate; candidate CHANGES observable identity)
P2 bench coverageMatrixFromTasks fused Set construction: replica=389ns candidate=363ns delta=25.3ns/call (once per run; mirror sites findUnsourcedItems / buildContractCandidate share the verdict)
P3 bench heuristicExtractor dead wantsReview on vague path: replica=3.47ns candidate=3.27ns delta=0.20ns/call (single comparison; vague objectives only)
CEILING default (9-pass anchor shape, Q=0): extractHeuristicContract=7699ns + run-start gate(Q=0, no options)=708ns + applyPrecedence=251ns = 8657ns once-per-run production total
CEILING configured A (habits set, Q=1 gated): extractHeuristicContract=7865ns + run-start gate(Q=1, all resolved)=685ns + applyPrecedence=211ns = 8760ns once-per-run production total
CEILING configured B (vague, Q=3 gated): extractHeuristicContract=3342ns + run-start gate(Q=3, all resolved)=476ns + applyPrecedence=112ns = 3931ns once-per-run production total
CEILING promotion class: assertCanPromoteFromReview=19.8ns once per promotion (production)
CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=1146ns/call
CEILING critique class (inside run-start chain): critiqueContract(real contract)=269ns/call
CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=2111ns/call
CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=63ns/call
PROFILE default chain=3308ns | validateRequirementContract=66ns ×2 (domain, OUT-of-slice) = 132ns (4.0%) | extractor slice-side=1555ns (47.0%) | critique=265ns (8.0%) | normalize=171ns | trusted-source=38ns | candidate-build glue=1147ns (34.7%)
COLD first-call production total (dist, fresh process, default): median=1704µs range=[1665, 1755]µs — once per PROCESS (veto class)
COLD first-call production total (dist, fresh process, configured (habits + Q=1 gated)): median=1618µs range=[1597, 1678]µs — once per PROCESS (veto class)

ALL EQUIVALENCE CHECKS PASSED (deterministic, seeded)
```

三次独立运行 40,000+ 项等价检查全部通过、裁决结论逐位一致；计时抖动
内方向一致或识别为符号翻转（P1 三次 +5.8/−1.1/+5.3ns 符号翻转＝零
收益；P2 三次 +25.3/+26.5/+24.3ns 方向稳定但量级淘汰；P3 三次
+0.20/+0.18/+0.19ns 亚 ns；默认锚点三次 8657/8472/8858ns、配置 A
三次 8760/7028/8260ns、配置 B 三次 3931/4774/4147ns；晋升类三次
19.8/18.9/18.6ns；evaluate 三次 1146/1104/1095ns、critique
269/627/615ns、pairwise 2111/2042/2036ns、reconcile 63/64/66ns；
PROFILE 链三次 3308/3583/3267ns、切片外占比 4.0/4.8/4.1%；冷层默认
三次 1704/1681/1676µs、配置三次 1618/1643/1617µs）。

## 6. 新增排除 ID

**无。** 第十二遍枚举为空——热链成本剖面归账零未解释质量（§2.1）、
三处从未点名微观位点全部以既有家族 + 量级 + 仿真数字淘汰（§2.2），
不硬凑 ID。与 R10-H / R11-H / R12-A / R12-E 的空枚举收口同型。

## 附录：确定性仿真 + 锚点脚本（完整，可复现）

运行方式：`pnpm build` 后保存为任意 `.mts` 并 `npx tsx <file>`（仓库
根目录，依赖已装；冷层探针需要 dist 产物存在）。seeds：
`0xB12801`–`0xB12803`。无赢家，脚本仅做三处 loser 裁决 + 满格矩阵
重锚 + 剖面分解，按惯例不入库。

```ts
/**
 * R12-H deterministic anchor re-verification + loser-probe adjudication +
 * hot-chain cost-profile decomposition (twelfth pass).
 *
 * No fresh candidate survived enumeration (empty twelfth group), so this
 * script (a) re-establishes, on THIS VM, the full measurement matrix that
 * R8-H..R11-H closed — hot tier three states (default Q=0 / configured A
 * habits+Q=1 gated / configured B vague+Q=3 gated; the production
 * assume-defaults track flow always passes resolvedQuestionIds covering ALL
 * questions so the gated re-map materially executes), promotion class, the
 * four zero-production-caller command classes (evaluate / critique /
 * pairwise / reconcile), and cold-tier once-per-process first-call totals on
 * dist (default AND configured, 5 fresh processes each) — (b) decomposes the
 * default-state hot chain into in-slice vs out-of-slice (domain validation)
 * shares — the twelfth pass's NEW completeness axis, mirroring R12-A's
 * contract-floor decomposition — and (c) puts honest seeded-fuzz + bench
 * numbers on the three never-point-named micro-sites found by the twelfth
 * structure-position × elimination-mechanism scan, all three of which are
 * REJECTED (family + magnitude):
 *
 *   P1: coverage.ts `checkCoverageGate` L22 defensive spread copy of
 *       `matrix.orphanRequirements` (always [] on the production path via
 *       coverageMatrixFromTasks). Candidate aliases instead of copying —
 *       observable identity change (S1-B-8 / S2-J-5 defensive-copy family).
 *   P2: coverage.ts `coverageMatrixFromTasks` L52 `new Set(criteria.map(id))`
 *       materializes an intermediate id array before Set construction.
 *       Candidate folds the ids into the Set in a single loop (fusion
 *       family, S1-C-5 / S5-J-6; mirror sites: provenance.ts
 *       findUnsourcedItems L30, extractor.ts buildContractCandidate L83).
 *   P3: heuristic.ts `heuristicExtractor` computes
 *       `wantsReview = habits.preferReview !== false` unconditionally, but
 *       the value is dead on the vague path (confidence = 0.55 regardless).
 *       Candidate defers the comparison past the vague branch
 *       (dead-computation family, R11-H L1 / S3-D-5).
 *
 * Reference = production imports wherever the function is exported; private
 * logic is replicated verbatim and each candidate differs from its replica
 * ONLY by the candidate edit. Replica-vs-production sanity gates precede
 * every adjudication. Benchmarks keep reference and candidate on separate
 * function identities and interleave three rounds to blunt PIC
 * shape-pollution artifacts (S8-A-3 / S8-E-2 / S8-H-1 lesson).
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xB12801..0xB12803.
 */
import { performance } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import {
  extractHeuristicContract,
  heuristicExtractor,
  type HeuristicHabits
} from "/workspace/src/requirement/heuristic.js";
import {
  assertCoverageAllowsStart,
  checkCoverageGate,
  coverageMatrixFromTasks,
  type CoverageTaskRef,
  type CoverageStartOptions
} from "/workspace/src/requirement/coverage.js";
import { applyPrecedence } from "/workspace/src/requirement/precedence.js";
import { critiqueContract } from "/workspace/src/requirement/critic.js";
import { normalizeSources, createTrustedSource, type RawSource } from "/workspace/src/requirement/normalizer.js";
import { validateRequirementContract, type RequirementContract, type CoverageMatrix } from "/workspace/src/domain/contract.js";
import { assertCanPromoteFromReview } from "/workspace/src/review/self-review.js";
import { runBlindPairwisePair, type PairwiseInput } from "/workspace/src/review/pairwise.js";
import { reconcileReviews } from "/workspace/src/review/reconcile.js";
import { createEvaluationRecord } from "/workspace/src/evaluation/evaluator.js";
import { createRubric } from "/workspace/src/rubric/types.js";
import type { EpisodeId, TaskId } from "/workspace/src/domain/ids.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

/* ================================================================
 * P1: checkCoverageGate defensive spread of matrix.orphanRequirements.
 * Replica is verbatim; candidate aliases the readonly array instead of
 * spreading. Production path (coverageMatrixFromTasks) always feeds [].
 * ================================================================ */
interface GateResultLike {
  readonly ok: boolean;
  readonly orphans: readonly string[];
  readonly uncoveredCriteria: readonly string[];
  readonly blockingDecisions: readonly string[];
}
function replicaGate(contract: RequirementContract, matrix: CoverageMatrix): GateResultLike {
  const orphans = [...matrix.orphanRequirements];
  const uncoveredCriteria: string[] = [];
  const blockingDecisions: string[] = [];
  for (const criterion of contract.acceptanceCriteria) {
    const covered = Object.keys(matrix.requirementToTasks).includes(criterion.id) &&
      (matrix.requirementToTasks[criterion.id]?.length ?? 0) > 0;
    if (!covered) {
      uncoveredCriteria.push(criterion.id);
    }
  }
  for (const q of contract.questions) {
    if (!q.default) {
      blockingDecisions.push(q.id);
    }
  }
  const ok = orphans.length === 0 && uncoveredCriteria.length === 0 && blockingDecisions.length === 0;
  return { ok, orphans, uncoveredCriteria, blockingDecisions };
}
function candidateGate(contract: RequirementContract, matrix: CoverageMatrix): GateResultLike {
  const orphans = matrix.orphanRequirements; // alias instead of defensive copy
  const uncoveredCriteria: string[] = [];
  const blockingDecisions: string[] = [];
  for (const criterion of contract.acceptanceCriteria) {
    const covered = Object.keys(matrix.requirementToTasks).includes(criterion.id) &&
      (matrix.requirementToTasks[criterion.id]?.length ?? 0) > 0;
    if (!covered) {
      uncoveredCriteria.push(criterion.id);
    }
  }
  for (const q of contract.questions) {
    if (!q.default) {
      blockingDecisions.push(q.id);
    }
  }
  const ok = orphans.length === 0 && uncoveredCriteria.length === 0 && blockingDecisions.length === 0;
  return { ok, orphans, uncoveredCriteria, blockingDecisions };
}
{
  const rng = mulberry32(0xb12801);
  const prodCandidate = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const contract = prodCandidate.contract;
  const criterionIds = contract.acceptanceCriteria.map((criterion) => criterion.id);
  let identityDivergenceSeen = false;
  for (let trial = 0; trial < 20000; trial += 1) {
    // Handcrafted matrices exercise non-empty orphans too (production never
    // produces them, but checkCoverageGate is exported and must stay total).
    const requirementToTasks: Record<string, TaskId[]> = {};
    for (const id of criterionIds) {
      if (rng() < 0.8) {
        requirementToTasks[id] = rng() < 0.9 ? ([`tsk_00000001`] as TaskId[]) : [];
      }
    }
    const orphanRequirements = rng() < 0.3 ? [pick(rng, ["ac-ghost", "ac-lost"])] : [];
    const matrix: CoverageMatrix = {
      contractVersion: 1,
      requirementToTasks,
      taskToChecks: {},
      orphanRequirements
    };
    const prod = checkCoverageGate(contract, matrix);
    const rep = replicaGate(contract, matrix);
    const cand = candidateGate(contract, matrix);
    check(
      "P1 sanity gate (verbatim replica parity vs production)",
      JSON.stringify(prod) === JSON.stringify(rep),
      `trial ${trial}`
    );
    check(
      "P1 content equivalence (aliased orphans)",
      JSON.stringify(rep) === JSON.stringify(cand),
      `trial ${trial}`
    );
    if (cand.orphans === matrix.orphanRequirements && rep.orphans !== matrix.orphanRequirements) {
      identityDivergenceSeen = true;
    }
  }
  check("P1 identity divergence is real (candidate aliases, replica copies)", identityDivergenceSeen);
  // Bench the production shape: C=2, K=2, Q=0, orphans=[].
  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: criterionIds.map((id) => ({ id }))
  }));
  const prodMatrix = coverageMatrixFromTasks(contract, tasks);
  let cur = 0;
  let cand = 0;
  for (let round = 0; round < 3; round += 1) {
    cur += bench(() => void replicaGate(contract, prodMatrix), 200000);
    cand += bench(() => void candidateGate(contract, prodMatrix), 200000);
  }
  cur /= 3;
  cand /= 3;
  log(
    `P1 bench checkCoverageGate orphans defensive spread: replica=${(cur * 1e6).toFixed(1)}ns candidate=${(cand * 1e6).toFixed(1)}ns delta=${((cur - cand) * 1e6).toFixed(1)}ns/call (once per run inside run-start gate; candidate CHANGES observable identity)`
  );
}

/* ================================================================
 * P2: coverageMatrixFromTasks `new Set(criteria.map(id))` intermediate
 * array. Candidate folds ids into the Set in one loop. Mirror sites:
 * provenance.ts findUnsourcedItems L30, extractor.ts
 * buildContractCandidate L83 (same mechanism, same family verdict).
 * ================================================================ */
function replicaMatrix(contract: RequirementContract, tasks: readonly CoverageTaskRef[]): CoverageMatrix {
  const contractIds = new Set(contract.acceptanceCriteria.map((criterion) => criterion.id));
  const requirementToTasks: Record<string, TaskId[]> = {};
  const taskToChecks: Record<string, string[]> = {};
  for (const task of tasks) {
    const mapped: string[] = [];
    for (const criterion of task.acceptanceCriteria) {
      if (!contractIds.has(criterion.id)) continue;
      mapped.push(criterion.id);
      const owners = requirementToTasks[criterion.id] ?? [];
      owners.push(task.id);
      requirementToTasks[criterion.id] = owners;
    }
    taskToChecks[task.id] = mapped;
  }
  return {
    contractVersion: contract.schemaVersion,
    requirementToTasks,
    taskToChecks,
    orphanRequirements: []
  };
}
function candidateMatrix(contract: RequirementContract, tasks: readonly CoverageTaskRef[]): CoverageMatrix {
  const contractIds = new Set<string>();
  for (const criterion of contract.acceptanceCriteria) contractIds.add(criterion.id); // fused, no intermediate array
  const requirementToTasks: Record<string, TaskId[]> = {};
  const taskToChecks: Record<string, string[]> = {};
  for (const task of tasks) {
    const mapped: string[] = [];
    for (const criterion of task.acceptanceCriteria) {
      if (!contractIds.has(criterion.id)) continue;
      mapped.push(criterion.id);
      const owners = requirementToTasks[criterion.id] ?? [];
      owners.push(task.id);
      requirementToTasks[criterion.id] = owners;
    }
    taskToChecks[task.id] = mapped;
  }
  return {
    contractVersion: contract.schemaVersion,
    requirementToTasks,
    taskToChecks,
    orphanRequirements: []
  };
}
{
  const rng = mulberry32(0xb12802);
  const prodCandidate = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const contract = prodCandidate.contract;
  const criterionIds = contract.acceptanceCriteria.map((criterion) => criterion.id);
  for (let trial = 0; trial < 20000; trial += 1) {
    const taskCount = 1 + Math.floor(rng() * 6);
    const tasks: CoverageTaskRef[] = Array.from({ length: taskCount }, (_, i) => ({
      id: `tsk_0000000${i}` as TaskId,
      acceptanceCriteria: criterionIds
        .filter(() => rng() < 0.7)
        .concat(rng() < 0.3 ? ["ac-ghost"] : [])
        .map((id) => ({ id }))
    }));
    const prod = coverageMatrixFromTasks(contract, tasks);
    const rep = replicaMatrix(contract, tasks);
    const cand = candidateMatrix(contract, tasks);
    check(
      "P2 sanity gate (verbatim replica parity vs production)",
      JSON.stringify(prod) === JSON.stringify(rep),
      `trial ${trial}`
    );
    check(
      "P2 equivalence (fused Set construction)",
      JSON.stringify(rep) === JSON.stringify(cand),
      `trial ${trial}`
    );
  }
  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: criterionIds.map((id) => ({ id }))
  }));
  let cur = 0;
  let cand = 0;
  for (let round = 0; round < 3; round += 1) {
    cur += bench(() => void replicaMatrix(contract, tasks), 100000);
    cand += bench(() => void candidateMatrix(contract, tasks), 100000);
  }
  cur /= 3;
  cand /= 3;
  log(
    `P2 bench coverageMatrixFromTasks fused Set construction: replica=${(cur * 1e6).toFixed(0)}ns candidate=${(cand * 1e6).toFixed(0)}ns delta=${((cur - cand) * 1e6).toFixed(1)}ns/call (once per run; mirror sites findUnsourcedItems / buildContractCandidate share the verdict)`
  );
}

/* ================================================================
 * P3: heuristicExtractor computes wantsReview before the vague branch
 * where it is dead (confidence = 0.55 regardless). Replica models the
 * confidence tail verbatim; candidate defers the comparison.
 * ================================================================ */
function replicaConfidenceTail(habits: HeuristicHabits, vague: boolean): number {
  const wantsReview = habits.preferReview !== false; // computed before the vague branch
  return vague ? 0.55 : wantsReview ? 0.86 : 0.8;
}
function candidateConfidenceTail(habits: HeuristicHabits, vague: boolean): number {
  if (vague) return 0.55;
  return habits.preferReview !== false ? 0.86 : 0.8; // deferred past the vague branch
}
{
  // Exhaustive equivalence: 3 preferReview states × 2 vague states, plus a
  // production cross-check through the real extractor on both shapes.
  for (const preferReview of [true, false, undefined]) {
    for (const vague of [true, false]) {
      const habits: HeuristicHabits = preferReview === undefined ? {} : { preferReview };
      check(
        "P3 equivalence (deferred wantsReview)",
        replicaConfidenceTail(habits, vague) === candidateConfidenceTail(habits, vague),
        `preferReview=${String(preferReview)} vague=${vague}`
      );
    }
  }
  const vagueSources = normalizeSources([
    createTrustedSource({ kind: "message", ref: "cli-objective", origin: "user-turn", content: "fix bug please" })
  ]);
  const preciseSources = normalizeSources([
    createTrustedSource({
      kind: "message",
      ref: "cli-objective",
      origin: "user-turn",
      content: "fix the login retry bug in src/auth/session.ts and keep tests green"
    })
  ]);
  const vagueOut = await heuristicExtractor({}).extract({ objective: "fix bug please", sources: vagueSources });
  const preciseOut = await heuristicExtractor({}).extract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green",
    sources: preciseSources
  });
  check("P3 sanity gate (production vague confidence)", vagueOut.confidence === replicaConfidenceTail({}, true));
  check("P3 sanity gate (production precise confidence)", preciseOut.confidence === replicaConfidenceTail({}, false));
  // Bench the vague shape (only shape where the candidate differs).
  const habits: HeuristicHabits = {};
  let cur = 0;
  let cand = 0;
  for (let round = 0; round < 3; round += 1) {
    cur += bench(() => void replicaConfidenceTail(habits, true), 500000);
    cand += bench(() => void candidateConfidenceTail(habits, true), 500000);
  }
  cur /= 3;
  cand /= 3;
  log(
    `P3 bench heuristicExtractor dead wantsReview on vague path: replica=${(cur * 1e6).toFixed(2)}ns candidate=${(cand * 1e6).toFixed(2)}ns delta=${((cur - cand) * 1e6).toFixed(2)}ns/call (single comparison; vague objectives only)`
  );
}

/* ================================================================
 * Hot tier, three states (R8-H..R11-H anchor shapes).
 * NOTE: the PROFILE decomposition runs AFTER these anchors so the anchor
 * measurement conditions stay comparable to R10-H/R11-H (same warmup
 * budget: benchAsync's single warm call + 3000 timed reps, no extra
 * chain executions beforehand).
 * ================================================================ */
async function measureChain(label: string, objective: string, habits: HeuristicHabits | undefined): Promise<void> {
  const args = { objective, ...(habits !== undefined ? { habits } : {}) };
  const chain = await benchAsync(async () => {
    await extractHeuristicContract(args);
  }, 3000);
  const prod = await extractHeuristicContract(args);
  const real = prod.contract;
  const tasks: CoverageTaskRef[] = Array.from({ length: 5 }, (_, i) => ({
    id: `tsk_0000000${i}` as TaskId,
    acceptanceCriteria: real.acceptanceCriteria.map((criterion) => ({ id: criterion.id }))
  }));
  const options: CoverageStartOptions | undefined =
    real.questions.length > 0
      ? { resolvedQuestionIds: real.questions.map((question) => question.id) }
      : undefined;
  const gate = bench(() => assertCoverageAllowsStart(real, tasks, options), 50000);
  const precedence = bench(() => void applyPrecedence(real, "user-first"), 100000);
  const total = chain + gate + precedence;
  log(
    `CEILING ${label}: extractHeuristicContract=${(chain * 1e6).toFixed(0)}ns + run-start gate(Q=${real.questions.length}${options !== undefined ? ", all resolved" : ", no options"})=${(gate * 1e6).toFixed(0)}ns + applyPrecedence=${(precedence * 1e6).toFixed(0)}ns = ${(total * 1e6).toFixed(0)}ns once-per-run production total`
  );
}
{
  await measureChain(
    "default (9-pass anchor shape, Q=0)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    undefined
  );
  await measureChain(
    "configured A (habits set, Q=1 gated)",
    "fix the login retry bug in src/auth/session.ts and keep tests green",
    { requireTests: true, preferReview: false, askBeforeWrite: true }
  );
  await measureChain("configured B (vague, Q=3 gated)", "fix bug please", { askBeforeWrite: true });
}

/* ================================================================
 * Command-class anchors beyond run-start.
 * ================================================================ */
{
  const promo = bench(
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    200000
  );
  log(`CEILING promotion class: assertCanPromoteFromReview=${(promo * 1e6).toFixed(1)}ns once per promotion (production)`);

  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const evaluate = bench(() => void createEvaluationRecord(evalInput), 50000);
  log(`CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=${(evaluate * 1e6).toFixed(0)}ns/call`);

  const prod = await extractHeuristicContract({
    objective: "fix the login retry bug in src/auth/session.ts and keep tests green"
  });
  const critique = bench(() => void critiqueContract(prod.contract), 50000);
  log(`CEILING critique class (inside run-start chain): critiqueContract(real contract)=${(critique * 1e6).toFixed(0)}ns/call`);

  const pairInput: PairwiseInput = {
    episodeId: "ep_00000001" as EpisodeId,
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pairwise = bench(() => void runBlindPairwisePair(pairInput, () => 0.7), 20000);
  const pair = runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  const reconcile = bench(() => void reconcileReviews(two), 100000);
  log(`CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=${(pairwise * 1e6).toFixed(0)}ns/call`);
  log(`CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=${(reconcile * 1e6).toFixed(0)}ns/call`);
}

/* ================================================================
 * NEW AXIS: hot-chain cost-profile decomposition (default state).
 * Attribute the once-per-run chain to its components and split the total
 * into in-slice vs out-of-slice (domain validateRequirementContract ×2)
 * shares. Mirrors R12-A's contract-floor decomposition. All components
 * and the chain are measured in the same (fully warmed) JIT state and
 * interleaved over three rounds to blunt order bias; the residual "glue"
 * bucket carries buildContractCandidate orchestration (role-pair
 * construction per call = S6-H-4, async machinery = S7-H-1, sourceRefs
 * Set = S2-H-1, grounding map = S2-H-2, roleId trims = R10-H no-ID).
 * ================================================================ */
{
  const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
  const prod = await extractHeuristicContract({ objective });
  const contract = prod.contract;
  const rawSource: RawSource = createTrustedSource({
    kind: "message",
    ref: "cli-objective",
    origin: "user-turn",
    content: objective
  });
  const sources = normalizeSources([rawSource]);
  const extractor = heuristicExtractor({});
  let chain = 0;
  let tVal = 0;
  let tCts = 0;
  let tNorm = 0;
  let tExtract = 0;
  let tCrit = 0;
  for (let round = 0; round < 3; round += 1) {
    chain += await benchAsync(async () => {
      await extractHeuristicContract({ objective });
    }, 3000);
    tVal += bench(() => void validateRequirementContract(contract), 100000);
    tCts += bench(
      () => void createTrustedSource({ kind: "message", ref: "cli-objective", origin: "user-turn", content: objective }),
      200000
    );
    tNorm += bench(() => void normalizeSources([rawSource]), 100000);
    tExtract += await benchAsync(async () => {
      await extractor.extract({ objective, sources });
    }, 5000);
    tCrit += bench(() => void critiqueContract(contract), 50000);
  }
  chain /= 3;
  tVal /= 3;
  tCts /= 3;
  tNorm /= 3;
  tExtract /= 3;
  tCrit /= 3;
  const outOfSlice = 2 * tVal;
  const extractorSliceSide = tExtract - tVal; // extract() minus its internal domain validation
  const orchestration = chain - tCts - tNorm - tExtract - tVal - tCrit;
  log(
    `PROFILE default chain=${(chain * 1e6).toFixed(0)}ns | validateRequirementContract=${(tVal * 1e6).toFixed(0)}ns ×2 (domain, OUT-of-slice) = ${(outOfSlice * 1e6).toFixed(0)}ns (${((outOfSlice / chain) * 100).toFixed(1)}%) | extractor slice-side=${(extractorSliceSide * 1e6).toFixed(0)}ns (${((extractorSliceSide / chain) * 100).toFixed(1)}%) | critique=${(tCrit * 1e6).toFixed(0)}ns (${((tCrit / chain) * 100).toFixed(1)}%) | normalize=${(tNorm * 1e6).toFixed(0)}ns | trusted-source=${(tCts * 1e6).toFixed(0)}ns | candidate-build glue=${(orchestration * 1e6).toFixed(0)}ns (${((orchestration / chain) * 100).toFixed(1)}%)`
  );
}

/* ================================================================
 * Cold tier on dist, default + configured (5 fresh processes each).
 * ================================================================ */
{
  const mkScript = (configured: boolean): string => `
    const { performance } = await import("node:perf_hooks");
    const { extractHeuristicContract } = await import("/workspace/dist/requirement/heuristic.js");
    const { assertCoverageAllowsStart } = await import("/workspace/dist/requirement/coverage.js");
    const { applyPrecedence } = await import("/workspace/dist/requirement/precedence.js");
    const objective = "fix the login retry bug in src/auth/session.ts and keep tests green";
    const args = ${"{"} objective${"}"};
    ${configured ? 'const cfgArgs = { objective, habits: { requireTests: true, preferReview: false, askBeforeWrite: true } };' : ""}
    const t0 = performance.now();
    const prod = await extractHeuristicContract(${configured ? "cfgArgs" : "args"});
    const tasks = Array.from({ length: 5 }, (_, i) => ({
      id: "tsk_0000000" + i,
      acceptanceCriteria: prod.contract.acceptanceCriteria.map((c) => ({ id: c.id }))
    }));
    ${configured
      ? "assertCoverageAllowsStart(prod.contract, tasks, { resolvedQuestionIds: prod.contract.questions.map((q) => q.id) });"
      : "assertCoverageAllowsStart(prod.contract, tasks);"}
    applyPrecedence(prod.contract, "user-first");
    const t1 = performance.now();
    process.stdout.write((t1 - t0).toFixed(3));
  `;
  const median = (xs: number[]): number => {
    const sorted = [...xs].sort((a, b) => a - b);
    return sorted[Math.floor(sorted.length / 2)] ?? Number.NaN;
  };
  for (const [label, configured] of [
    ["default", false],
    ["configured (habits + Q=1 gated)", true]
  ] as const) {
    const samples: number[] = [];
    for (let i = 0; i < 5; i += 1) {
      const proc = spawnSync(process.execPath, ["--input-type=module", "-e", mkScript(configured)], {
        encoding: "utf8"
      });
      if (proc.status !== 0) {
        check(`cold probe runs (${label})`, false, proc.stderr);
        break;
      }
      samples.push(Number(proc.stdout.trim()));
    }
    log(
      `COLD first-call production total (dist, fresh process, ${label}): median=${(median(samples) * 1e3).toFixed(0)}µs range=[${(Math.min(...samples) * 1e3).toFixed(0)}, ${(Math.max(...samples) * 1e3).toFixed(0)}]µs — once per PROCESS (veto class)`
    );
    check(`cold probes produced numbers (${label})`, samples.length === 5 && samples.every(Number.isFinite));
  }
}

if (failures > 0) {
  process.stderr.write(`\n${failures} check(s) FAILED\n`);
  process.exit(1);
}
log("\nALL EQUIVALENCE CHECKS PASSED (deterministic, seeded)");
```

MORE_OPTIMA=no
BRANCH=cursor/r12-h-eval-twelfth-pass-83a1
