MODEL_SLUG=claude-fable-5-thinking-xhigh

# R12-J：数据面切片（cluster / privacy / preferences / episode / persist / track / context / feedback）第十二遍扫描报告

**战役:** 全库持久 SOTA 优化 Round 12 / R12-J
**基线:** `cursor/sota-persistent-opt-83a1` @ `c7e344f`（独占 tip，含 R12-A/B/C/D/E/F/H 全部摄取；S12-B-1..2 / S12-C-1 / S12-D-1..2 已入排除表；R12-A / R12-E / R12-F / R12-H 空枚举未铸 ID）
**分支:** `cursor/r12-j-persist-twelfth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第十二遍枚举为空：无达门槛候选、零新 ID、零生产代码改动、切片维持
关闭（MORE_OPTIMA=no）。**
切片 29 个文件（3655 行，本轮 `wc -l` 复核与 R9-J/R10-J/R11-J 一致；
3+3+7+5+2+4+2+3 清单逐目录确认恰 29 文件）自 J1 落地（`fb41417`）以来
**逐字节未变**（本轮核对 `git diff fb41417..HEAD -- <29 文件>` 输出为
0 行——J1 之后经 R2-J..R11-J 与本轮共十一遍复查累计零后续代码改动）。
R11-J 基线（`134f73e`）之后 `git log 134f73e..HEAD -- src/` **零提交**
（其间 11 个提交全部为 R11-G/R11-I 摄取与 R12 各波报告/排除表文档——
R12 前八区 B/C/D 三区落 ID 全为淘汰、A/E/F/H 四区空枚举，无一代码
落地）⇒ 生产调用图对本切片**可证不变（最强形式，第四连）**。按指令
在本 VM 重锚配置态 I/O 地板矩阵（两遍 × 各 3 次运行，§1.3）：
preferences `saveToDisk` 非空策略 **149.7–271.6µs/写**（50 观察档，
R11-J 132.7–215.3µs 带重叠）～ **429.5–543.7µs/写**（500 观察档，
R11-J 402.7–541.0µs 带重叠——地板随存量伸缩，I/O + 全量 stringify
支配判据第十二次成立）；jsonl 追加 fsync=false **61.1–71.2µs** /
fsync=true **209.5–321.0µs**（R11-J 59.9–65.5 / 206.5–385.3µs 带毗邻/
带内，fsync 抖动量级不变）；配置态删除级联 N=200、20 命中：
match=**672.9–797.7µs** / no-match=**275.8–288.2µs**（R11-J 639.8–745.9
/ 264.3–290.1µs 带重叠，**fail-closed 两读顺序 + 串行两写零形态
改动**）；`buildProjectContextIndex` **40.8–41.3µs/构建**（R10-J
40.1–41.3µs 带内，每 run 一次）；track plan CPU 计数器 IdGenerator
**1.10–1.14µs/plan** / 默认 randomUUID **2.63–2.73µs/plan**（R11-J
1.01–1.11 / 2.49–2.62µs 带毗邻）。J1 落地代码本轮
`scripts/r1j-equivalence-sim.ts` 重跑全绿（**2468 项逐位检查，
reference 10379.9ms → current 3.8ms = 2718.1×**）。第十二遍在完整
排除表（S1-J-1..7、S2-J-1..11、S3-J-1..6、S4-J-1..6、S5-J-1..6、
S6-J-1..6、S7-J-1..6、S8-J-1..2、S9-J-1..4 共 54 个已闭 ID + J1 +
R10-J/R11-J 两轮空枚举收口）之上以一个从未系统化的完备性轴收口：
**跨函数重复纯计算普查**（§2.1）——把同一纯计算在调用链两个函数间
重复执行的所有位点（R11-J 位点 A 的 some+find 双扫是其同函数特例）
全量列举并逐一裁决，同时对 R11-J 的矩阵完备性声明做对抗性复audit。
普查恰命中**三处十一轮从未点名的位点**：（A）spawn 链
`validateSpawn` 检查 trim 与 `host.spawn` 载荷 trim 的跨函数双 trim；
（B）`mailbox.claimRole` skip 分支内逐 own-mail 的 `byRole` 重取重存；
（C）`packet.compileContextPacket` 对 `codeMapSelection.omissions`
同数组两遍循环。三处均经理论 + 内联等价断言探针（两次独立运行 ×
3 rep，附录 B）裁决为**既有已闭家族的换位点 + 深度亚噪声**（A =
S3-J-3 双 trim 家族换链路，4.5–5.7ns/spawn × 每 parent ≤4 次；B =
S4-J-5/S5-J-4 同函数同结构家族，3.9–15.5ns/claim 且仅 own-mail 稀有
分支触发；C = S4-J-4/S7-J-2/S9-J-3 双遍融合家族换位点，n=0 生产常态
5.2–14.5ns、n=10 压力档 10.9–24.4ns/编译 × 每 run ≤16 次），按换名
纪律**全部不铸 ID**。零生产调用方导出审计本轮**再加十面精化**
（§1.2）：`findConflicts` / `materializeView` / `compilePacket` /
`closure.closeEpisode` / `getObservationsByKey` / `recordObservation` /
`tombstoneIds` / `materializeWithoutTombstones` / `createEmptyContext` /
`applyRedaction` 经定向 rg 证实在 `src/` 内全部仅定义处（值级引用只
落在 `test/`）——其中 `findConflicts` 修正 R10-J「唯一生产调用方为
CLI 检查命令」的宽松表述为**精确零 src/ 调用方**。其余全部重扫形状
逐一对账为已闭条目原文域/换位点（§2.3）。未重开任何 X* / S1-* …
S11-* / S12-B/C/D-* 条目；J1 / S5-J-3 / S6-J-1 / S8-J-2 与 R11-J 两处
无名微观（commands 双扫、役播 `role === undefined`）换名重提检查逐条
通过且**未补铸**（§0）。诚实结论：枚举为空，不硬凑 ID，与 R10-J /
R11-J / R12-A / R12-E / R12-F / R12-H 空枚举收口同型。现状即为 SOTA。

## 0. 范围与约束遵守（含换名重提检查）

- 切片：`src/cluster/`（3：mailbox / spawn / host）、`src/privacy/`
  （3：deletion / state-layout / record-classes）、`src/preferences/`
  （7：loop-eval / export / precedence / materialize / service / store /
  types）、`src/episode/`（5：manager / store / closure / events /
  replay）、`src/persist/`（2：file-lock / jsonl）、`src/track/`
  （4：primary-split / plan / clarify / loop）、`src/context/`
  （2：index / packet）、`src/feedback/`（3：redaction / store / types）
  共 **29 文件 3655 行**全量第十二遍实际读码，未依赖前十一轮记忆。
  切片外 `domain/{project,contract,ids}.ts`、`requirement/heuristic.ts`
  只读取证（夹具形状），一行未改。
- 先读并遵守（顺序按指令）：README / EXCLUSIONS.md（全表，含
  S12-B-1..2 / S12-C-1 / S12-D-1..2；R12-A/E/F/H 空枚举）/
  round-12/PLAN.md / round-11/PLAN.md + R11-J.md（空枚举，未铸
  S11-J-*）/ R10-J.md（空枚举，未铸 S10-J-*）及 R9-J…R1-J 按需。J1
  未重做（本轮重跑其等价仿真全绿）；`scripts/r1j-equivalence-sim.ts`
  未改动。未开 PR；未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md；
  未触碰其他切片报告。零生产编辑 ⇒ 按指令**未提交任何新仿真脚本**
  （锚点/探针 harness 源码入附录 A/B）。
- **指令点名的换名重提检查**（逐条读码复核，全部原样在位、未列为
  新候选）：
  - **J1**：`loop-eval.ts` 每主体增量 fold + O(1) tombstone undo 落地
    形态逐字节未变；等价仿真 2468 项逐位全绿（2718.1×）。
  - **S5-J-3（fail-closed 钉死项）**：`cascadeFeedbackTombstones`
    L111 的 `readFeedbackTombstoneIds` 仍在匹配循环**之前**无条件执行
    ——两读 fail-closed 顺序零形态改动；损坏侧车在任何匹配判定前即
    抛。本轮 S0c 锚点经生产函数原样测量。
  - **S6-J-1（partial-delete 钉死项）**：L121–124 的
    `writeFeedbackRecords` → tombstone 落盘仍为**串行两写**，零并行化/
    攒批/延迟变形。
  - **S8-J-2**：`persist/jsonl.ts` fsync 路径仍为 appendFile 后独立
    `open("a")`+sync+close 双句柄形态，一字未动（unlink 窗口面维持）。
  - **S9-J-1..4**：`stripForbidden` 顺序 split/join 无 includes 卫
    （redaction.ts L88–94，S3-J-1 顺序剥除同函数钉死）；`rebuildViews`
    内部 `weights` 仍为 Record（store.ts L97）；index 构建
    architecture/risks 仍为双遍 filter+sort+map（index.ts L140–147）；
    `decideClosure` 仍无 structured-pass 短路、legacyMatch 探针原样
    （closure.ts L32）。
  - **R11-J 两处无名微观（指令明令勿补铸）**：`context/index.ts`
    L170/L191 对 `snapshot.commands` 的 `.some`+`.find` 同谓词双扫
    **原样在位、本轮未铸 ID**（本轮普查把它归入跨函数重复计算轴的
    同函数特例，域仍属 S9-J-3/S8-J-1 收口）；`cluster/host.ts` `send()`
    L126–128 役播 queued 路径 `role === undefined` 死复核**原样保留、
    未铸 ID**（S4-J-1 防御纵深判据维持）。
  - R10-J 无 ID 收口维持：`preferences/precedence.ts` 整模块、
    `durableRecordClassById`、`episode/replay.ts` 两导出、
    `exportForDataset` 零生产调用方本轮 re-grep 复证（§1.2）；
    S7-J-6 saveToDisk 每写 `mkdirSync` 自愈保留（store.ts L34）；
    S2-J-1 loop-eval 两次 `tombstones.has` 原样（L153/L159）；
    S1-J-7 packet.ts L122 首个 `omissions.sort` 原样在位。
- 硬不变量全部满足：本轮零 diff ⇒ 分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由 PASS/FAIL、live = R0 等价（R1 未接线，
  稀疏 → 已批准 R0 基线）、双 LCB 与双归因保留、晋升 proposal-first、
  无 Outcome-supported 声明（Checkpoint F-PROD 仍开放，ADR-005）、
  阈值/测试/CAS/凭据/数据面契约/公开签名不变、**fsync 未去**、
  **完整性再哈希未去**、EventStore/EpisodeStore readAll 未加增量缓存
  （S1-G-1/S2-J-11 族）、删除级联 fail-closed 两读顺序与串行两写未动，
  天然成立。
- 环境注记：系统 Node v22.14.0 低于 engines >=22.19.0，经 nvm 切换
  v22.22.2（`/exec-daemon/node` 遮蔽 PATH，需显式前置 nvm bin）；
  `pnpm install --frozen-lockfile` 后仿真与门禁全绿（§6）。

## 1. 基线不变性、调用图复核与 I/O 地板重锚（含配置态矩阵）

1. **切片逐字节未变**：`git diff fb41417..HEAD -- src/cluster
   src/privacy src/preferences src/episode src/persist src/track
   src/context src/feedback` 输出为空（29 文件 3655 行，十二遍全程 J1
   之外零 diff）。R1-J…R11-J 全部逐函数收口与 54 个 S*-J-* 已闭 ID +
   R10-J/R11-J 无 ID 收口继承有效。
2. **调用图可证不变（最强形式，第四连）**：`git log 134f73e..HEAD --
   src/` **零提交**——R11-J 基线以来 11 个提交全部为 `docs/reports/`
   文档（R11-G/R11-I 摄取、R12 排除表与 A–H 各波报告；R12 前八区无一
   代码落地）⇒ 生产调用方地图数学上不变。本轮 re-grep 全量复证
   R10-J/R11-J 各面并**再加十面精化**：
   - `preferences/precedence.ts` 整模块（4 导出）/
     `durableRecordClassById` / `replayEpisodeEvents` / `replayFromLog` /
     `exportForDataset` / `evaluatePreferenceLoop` /
     `queryPacketGrounding` / `refreshProjectContextIndex`：`src/` 内
     全部仍仅定义处（或注释引用）——**零生产调用方原样**；
   - **本轮新精化十面**：`findConflicts` / `materializeView` /
     `compilePacket` / `closure.ts` 的 `closeEpisode` /
     `getObservationsByKey` / `recordObservation` / `tombstoneIds` /
     `materializeWithoutTombstones` / `createEmptyContext` /
     `applyRedaction` 定向 rg 证实 `src/` 内全部仅定义处，值级引用只
     落在 `test/`（redaction / isolation / child-grounding /
     child-prompt / project-index / packet / deletion-replay /
     preferences 等测试面）——其中 `findConflicts` 把 R10-J §2.3-2
     「唯一生产调用方为 CLI 检查命令（稀频）」的宽松表述修正为
     **精确零 src/ 调用方**（十面任何等价重写生产收益恒 0，整面
     关闭；接线属功能工作，X0-2 域）；
   - `appendJsonlLine` 四调用方原样（fsync 非 false 仅
     `run/event-store.ts:39` 与 `run/episode-store.ts:36`；
     `feedback/store.ts:40` 与 `episode/store.ts:27` 均 false）——
     **无新调用方、无新 I/O 动词**；
   - `withExclusiveFileLock` 生产调用方四处原样（bandit-store:44 /
     promotion:247 / file-credential-store:43,54 / cli/episode:90）；
   - `buildProjectContextIndex`（coordinator:253 / flowchart-run:255，
     每 run 一次）、`compileContextPacket`（child-grounding:48，
     ≤16/run）、`decideClosure`（episode-bind:181 / cli/episode:105）、
     `getMaterializedView`（clarify:21–22 / main:1260）、
     `exportAuthorizedPreferences`（main:1318 惰性 import，稀频 CLI）、
     `deleteRunRecords`/`deleteEpisodeRecords`（main:1388 惰性 import）
     全部与 R11-J 逐行一致；episode manager 四导出的切片外动词
     （episode-bind / cli/episode 的 open/attach/wait/close +
     append + readAll）逐行核对无新动词。
3. **配置态 I/O 地板矩阵重锚**（两遍 × 各 3 次运行，本 VM，
   Node 22.22.2，harness 与 R11-J 附录 A 同构，全文见附录 A）：

```text
S0a  saveToDisk 非空策略（recordPreference 端到端，50 观察种子 + 200 写）:
       271.6/218.6/153.1 | 219.5/195.4/149.7 us/write → 带 149.7–271.6µs（R11-J 132.7–215.3µs 带重叠；R10-J 125–237µs 基本带内）
S0a' 同上 500 观察种子档:  517.6/429.5/436.3 | 543.7/443.9/462.2 us/write → 带 429.5–543.7µs（R11-J 402.7–541.0µs 带重叠）
S0b  jsonl append fsync=false (2000 次):  67.9/67.1/61.1 | 71.2/66.6/61.5 us → 带 61.1–71.2µs（R11-J 59.9–65.5µs 带毗邻）
S0b  jsonl append fsync=true  (300 次):   258.6/281.0/321.0 | 271.0/263.8/209.5 us → 带 209.5–321.0µs（R11-J 206.5–385.3µs 带内）
S0c  删除级联 N=200、20 命中 MATCH:    797.7/725.9/672.9 | 748.3/699.6/729.4 us → 带 672.9–797.7µs（R11-J 639.8–745.9µs 带重叠）
S0c  删除级联 N=200 NO-MATCH:          287.6/283.7/277.6 | 284.7/275.8/288.2 us → 带 275.8–288.2µs（R11-J 264.3–290.1µs 带重叠）
S0d  buildProjectContextIndex (F=40 + 30 codeMap + 双哈希表): 41.2/40.8/40.8 | 41.3/41.0/40.9 us → 带 40.8–41.3µs（R10-J 40.1–41.3µs 带内）
S0e  track plan CPU: 计数器 gen 1.12/1.12/1.14 | 1.10/1.11/1.10 us/plan → 带 1.10–1.14µs（R11-J 1.01–1.11µs 带毗邻）
     默认 randomUUID gen: 2.73/2.70/2.63 | 2.64/2.68/2.63 us/plan → 带 2.63–2.73µs（R11-J 2.49–2.62µs 带毗邻）
```

   判读：全部九格与 R10-J/R11-J 带重叠或毗邻——**「同路径 µs/ns CPU
   为 I/O 地板支配即拒」判据在本 VM 第十二次成立**（级联/追加/落盘
   三条路径数百 µs 起步；纯切片内命令类 CPU 地板 1.10µs/plan，距
   落地线 4–5 个数量级）。未复制任何前轮数字，全部本 VM 重测。
4. **J1 等价仿真重跑**（生产脚本，未改动）：
   `pnpm exec tsx scripts/r1j-equivalence-sim.ts` → 场景 1（16 例
   确定性边界）64 项 + 场景 2（600 例随机化）2400 项 + 性能夹具
   （N=6000 多主体 ~25% tombstone）reference 10379.9ms → current
   3.8ms（**2718.1×**）。`ALL EQUIVALENCE CHECKS PASSED (2468 bitwise
   checks)`。

## 2. 第十二遍枚举：跨函数重复纯计算普查 → 三处从未点名位点全部换名淘汰 → 诚实空结果

### 2.1 收口角度

前十一轮已按十一个互不重复的收口角度穷尽：逐函数下界（R1-J）、
调用图（R2-J）、语义融合反例（R3-J）、并行化竞态（R4-J）、fail-open
反例（R5-J）、崩溃序/攒批反例（R6-J）、配置态热路径（R7-J）、fsync
句柄面（R8-J）、原型键等价 + 短路死分支（R9-J）、零调用方导出审计 +
微观位点定向核查（R10-J）、结构位点 × 消除机制矩阵猎漏（R11-J）。
R11-J 找到两处无名位点并换名拒绝。本轮取一个从未系统化的完备性轴做
对抗性复 audit：**跨函数重复纯计算普查**——对 29 文件的每条生产调用
链，列举「同一纯计算在链上两个（或同一）函数内对同一未变异输入重复
执行」的全部位点（R11-J 位点 A 的 some+find 双扫即该轴的同函数特例；
S2-J-3/S3-J-3 双 trim、S8-J-1 跨调用 CSE、S5-J-5 属性双读是该轴已闭
先例），凡与 54 个已闭 ID + 历轮无 ID 收口对不上账者单独裁决。同时把
结构位点 × 消除机制矩阵重扫一遍作 R11-J 完备性声明的复核。

### 2.2 普查结果：恰三处从未点名位点（均不铸 ID）

**位点 A：spawn 链跨函数双 `objective.trim()`。**
`cluster/spawn.ts` `validateSpawn` L41 以 `request.objective.trim() ===
""` 做非空检查；调用方 `cluster/host.ts` `spawn()` L181 对同一字符串
再次 `input.objective.trim()` 构造 onSpawn 载荷——同一纯计算跨函数
双执行（两点之间 objective 未变异）。候选：host 侧先 trim 一次复用
（validateSpawn 公开签名不动）。
- 理论：trim 幂等且错误消息不含 objective 内容 ⇒ 复用逐位等价；
  改 validateSpawn 返回 trimmed 串则是公开签名变更（X0-4 族），不取。
- 探针实测（附录 B，生产形状 objective，两次独立运行 × 3 rep）：
  双 trim vs 单 trim delta = **+5.53 / +4.75 / +5.73ns**（run1）、
  **+4.74 / +4.48 / +5.29ns**（run2）每 spawn——方向稳定但
  4.5–5.7ns/spawn；spawn 受 MAX_SPAWNS_PER_PARENT=4 钳位 + 深度 ≤2
  钳位，每 run 合计数十 ns，距落地线 7 个数量级。
- 裁决：**换链路不铸 ID**——双 trim 去重域已由 S2-J-3（役播 trim
  提升）与 S3-J-3（单播 send 双 trim 合并，16–19ns，mailbox 数据面
  拒）在 send 链原文覆盖；本位点是同机制在 spawn 链的换位点，量级
  更低且 cluster 数据面强调区判据同样适用。

**位点 B：`mailbox.claimRole` skip 分支逐 own-mail `byRole` 重取重存。**
`cluster/mailbox.ts` L70–72：claim 循环内遇 `mail.from === agentId`
（agent 对自身角色的役播）时执行 `const remaining = byRole.get(role)
?? []; remaining.push(mail); byRole.set(role, remaining);`——每条
own-mail 一次 Map get + set，而 L66 已将该 role 队列重置，循环内无
其他 byRole 触碰。候选：循环外提升单一 remaining 数组 + 单次 set。
- 理论：claim 为同步单线程执行，box() 只触 byAgent 不触 byRole ⇒
  提升后 delivered / 剩余队列 / 收件箱三面逐位等价（探针内联断言
  三面全过）。
- 探针实测（附录 B，4 邮件含 2 own-mail 夹具，两次独立运行 × 3 rep）：
  production vs hoisted delta = **+15.5 / +9.1 / +3.9ns**（run1）、
  **+14.2 / +13.6 / +10.4ns**（run2）每 claim——3.9–15.5ns，且仅在
  own-mail 存在的稀有形态触发（agent 向自身角色役播）；claimRole 每
  register 一次。
- 裁决：**换名不铸 ID**——同函数同机制（claimRole 循环不变量提升）
  已由 S4-J-5（claimRole box() 提升，76–92ns，mailbox 数据面拒）原文
  覆盖，冗余 byRole.set 消除已由 S5-J-4（6.2–6.6ns 拒）原文覆盖；
  本位点量级介于两者之间，双重家族收口。

**位点 C：`packet.compileContextPacket` 对 `codeMapSelection.omissions`
同数组两遍循环。**
`context/packet.ts` L130–140：先 `codeMapSelection.omissions.map(...)`
构造 packetCodeMapOmissions，再对同一数组 for-of 逐项 push 进
omissions——同数组双遍。候选：单遍融合。
- 理论：两遍无交叉依赖（map 产物与 push 目标互不读取）⇒ 融合后两个
  输出数组逐位等价（探针内联断言 n=0 与 n=10 双档全过）。
- 探针实测（附录 B，两次独立运行 × 3 rep）：n=0（生产常态——默认
  token 预算 2000 下 index omissions 通常为空）delta =
  **+5.2 / +14.5 / +5.9ns**（run1）、**+5.2 / +13.4 / +5.9ns**
  （run2）每编译；n=10 压力档 delta = **+20.3 / +10.9 / +17.3ns**
  （run1）、**+24.4 / +17.6 / +15.8ns**（run2）——每 run ≤16 次
  编译，合计亚 µs，距 S0d/S0a 地板 3–4 个数量级。
- 裁决：**换位点不铸 ID**——同函数双遍融合域已由 S4-J-4（packet
  omissions 双遍融合，9–87ns 抖动拒）、S7-J-2（requiredFacts/
  relevantFiles 双 filter+map 融合，408–443ns 拒）与 S9-J-3（index
  双遍融合，21–88ns 拒）三重原文覆盖；本位点是同函数第三个双遍站点
  （S4-J-4 同函数），量级同带。

### 2.3 其余重扫形状对账（全部为已闭条目原文域/换位点，拒列）

| 形状 | 对账 |
| --- | --- |
| `context/index.ts` `fileFact`（L161）与 manifests 记录循环（L187）对同一 manifest 路径重复 `relativeToRoot` | 跨调用 CSE 域已由 S8-J-1（sourcedFact 跨调用 CSE，8–47ns/调用、sourcedFact 分解闭合）原文覆盖；S0d 地板 40.8–41.3µs 下不可寻址 |
| `context/index.ts` `commandSourceKey` 每命令重扫 manifests find | S2-J-4 原文（~6µs/构建，已拒） |
| `context/index.ts` 排序比较器每比较重算 pathDepth/relativeToRoot | S3-J-2 decorate-sort 原文域 |
| `context/index.ts` L170/L191 commands `.some`+`.find` 双扫 | **R11-J 位点 A 原文，指令明令勿补铸**——本轮跨函数重复计算普查把它对账为同函数特例，维持不铸 |
| `context/packet.ts` L122 与 L167 `omissions.sort` 双排序 | S1-J-7 原文位点在位 |
| `cluster/host.ts` 役播每 target 重复 `input.body.trim()` + 首/余双段 stampMail 构造 | S2-J-3 原文 + R5-J 已裁（ns 级，mailbox 数据面） |
| `cluster/host.ts` `send()` L126–128 `role === undefined` 死复核 | **R11-J 位点 B 原文，指令明令勿补铸**——S4-J-1 防御纵深判据维持，原样保留 |
| `cluster/host.ts` `spawn()` L173–177 深度/配额双重复核 | S4-J-1 原文（防御纵深保留） |
| `cluster/spawn.ts` `PARENT_SPAWN_ALLOWLIST[...] ?? []` 死回退 + allowlist includes | R4-J / R10-J §2.3-3 维持 |
| `cluster/mailbox.ts` `inbox()`/`pendingForRole()` spread 拷贝 | readonly 返回公开拷贝契约（R3-J 同判族） |
| `privacy/deletion.ts` 级联 records 读 `.catch(() => [])` 错误面、`[...tombstones].sort()`/`cascaded.sort()` | R5-J §3 级联读序契约 + R10-J sort µs 级拒列原文；S2-J-7 无匹配早退已闭 |
| `privacy/deletion.ts` `deleteEpisodeRecords` 双文件 stat/rm 串行 | S4-J-3 原文（并行化竞态拒） |
| `preferences/store.ts` `rebuildViews` byPair get/push/set 与每 view `nowIso()` | S7-J-4（冗余 Map.set 条件化）/ S3-J-5（nowIso 提升语义发散）原文；saveToDisk 地板 149.7–543.7µs 吞没 |
| `preferences/store.ts` `applyObservation` 全量 filter 计数 | S1-J-2 / S2-J-6 原文（saveToDisk 支配） |
| `preferences/export.ts` scopes includes / listObservations 拷贝 | S3-J-6 / S5-J-1 原文（stringify 支配） |
| `episode/manager.ts` `closeEpisode`/事件构造恒写可选键、runIds includes | R11-J §2.3 own-property 面拒列 + S1-J-3 原文 |
| `episode/store.ts` enqueue 链节点 | R10-J §2.3-4 维持（崩溃序契约实施点 + S0b 地板支配） |
| `episode/closure.ts` 死参数 `_latestRunIds` | R11-J §2.3 公开签名（X0-4 族）拒列维持 |
| `persist/file-lock.ts` acquire 双步 / 释放 ownerToken 重读 | R9-J/R10-J 锁协议数据面原文 |
| `persist/jsonl.ts` 每 append mkdir / split 行扫描 | R3-J + S7-J-6 自愈同型 / S3-J-4 原文 |
| `track/plan.ts` 函数体内正则字面量 | V8 按源位置缓存已编译 pattern，每求值仅分配壳对象 ns 级；S0e 地板 1.10µs/plan 覆盖 |
| `track/plan.ts` `acceptanceForRole` 双 filter+map | S4-J-4/S7-J-2 家族换位点（R11-J §2.3 已账）+ S5-J-6 共享 default 身份反例 |
| `track/loop.ts` assignments.find / 双 readAll / catalog∥learned / 五次追加 | S1-J-5 / S2-J-11 / S4-J-2 / S6-J-4 原文 |
| `feedback/redaction.ts` needles 每调用排序 / copyFeedback 白名单 | S2-J-9（X1-1 邻域）/ R10-J §2.3-6 白名单即语义 |
| `feedback/store.ts` readFeedback 双 filter / 双读 Promise.all | R10-J 换位点拒列 + S2-J-10 原文 |

### 2.4 收口陈述

十二遍合计，本切片在当前调用图下**结构上不存在**达门槛候选：
（i）三条 I/O 路径（preferences 落盘 / jsonl 追加 / 删除级联）地板
149.7–797.7µs，其上任何等价 CPU 重写不可寻址（S0a–S0c 十二轮复现）；
（ii）纯 CPU 面每 run 合计个位 µs~41µs，距落地线 3–5 个数量级
（S0d–S0e）；（iii）本轮跨函数重复纯计算普查找到的仅有三处从未点名
位点均为 3.9–24.4ns 深度亚噪声 + 既有家族收口之内（§2.2），其余重扫
形状全部对账为已闭条目原文域（§2.3）；（iv）零生产调用方面本轮再精化
十面（§1.2），任何等价重写生产收益恒 0；（v）语义面（fail-closed
两读、串行两写、fsync、白名单拷贝、锁协议、崩溃序、级联 catch 错误面、
own-property 形状）全部是钉死反例/明文契约的实施点，无合法变形。重开
本切片的唯一前提维持 R9-J/R10-J/R11-J 收口原文：调用图变更（test-only
面接线生产热路径），或 preferences/feedback 存量增长 ≥2 个数量级使
全量 stringify 落盘本身成为瓶颈——彼时候选是持久化形态变更，须走
数据面契约评审而非微观优化。

## 3. 逐文件收口（第十二遍新视角，其余与 R1-J…R11-J 一致）

| 文件 | 本轮普查核查 | 结论 |
| --- | --- | --- |
| `cluster/mailbox.ts` | **位点 B 猎中并淘汰（§2.2，S4-J-5/S5-J-4 换名）**；五 ID + 拷贝契约维持 | 无新候选 |
| `cluster/spawn.ts` | **位点 A 猎中并淘汰（§2.2，S3-J-3 换链路，与 host.ts 跨文件）**；`?? []` 死回退 R4-J 维持 | 无新候选 |
| `cluster/host.ts` | 位点 A 对偶端；R11-J 位点 B 死复核原样保留未补铸；S2-J-3/S4-J-1 维持 | 无新候选 |
| `privacy/deletion.ts` | **S5-J-3 两读顺序 + S6-J-1 串行两写在位复核（零 diff）**；catch 错误面 = R5-J §3 契约 | 无新候选 |
| `privacy/state-layout.ts` | 纯路径构造 | 无 |
| `privacy/record-classes.ts` | `durableRecordClassById` 零生产调用方维持（re-grep） | 无新候选 |
| `preferences/loop-eval.ts` | **J1 在位，仿真 2468 项全绿（2718.1×）**；S2-J-1/S2-J-2/S6-J-2 维持 | 无新候选 |
| `preferences/export.ts` | `exportForDataset` 零调用方维持；S3-J-6/S5-J-1 维持 | 无新候选 |
| `preferences/precedence.ts` | 整模块零生产调用方维持（re-grep） | 整面关闭 |
| `preferences/materialize.ts` | `materializeView` 零调用方（本轮新精化）；entries→spread R10-J §2.3-1 维持 | 无新候选 |
| `preferences/service.ts` | 纯委托层；`recordObservation` 零调用方（本轮新精化） | 无 |
| `preferences/store.ts` | S0a 双档重锚（149.7–543.7µs 随存量伸缩）；`findConflicts`/`getObservationsByKey` 零 src/ 调用方（本轮精化修正 R10-J 表述）；八面已闭 ID 维持 | 无新候选 |
| `preferences/types.ts` | 类型定义 | 无 |
| `episode/manager.ts` | 恒写可选键形状面拒列维持；S1-J-3、S6-J-5 维持 | 无新候选 |
| `episode/store.ts` | enqueue 链节点 R10-J §2.3-4 维持；S0b 地板支配 | 无新候选 |
| `episode/closure.ts` | `closure.closeEpisode` 零调用方（本轮新精化，两处生产 import 均只取 decideClosure）；S2-J-8、S7-J-3、S9-J-4 维持 | 无新候选 |
| `episode/events.ts` | 类型定义 | 无 |
| `episode/replay.ts` | 两导出零调用方维持；`reduceEpisodeEvents` 生产消费仅经由本零调用方模块 ⇒ 面维持关闭 | 无新候选 |
| `persist/file-lock.ts` | acquire/release 锁协议数据面第十二次零碰；R9-J/R10-J 拒列维持 | 无新候选 |
| `persist/jsonl.ts` | **S8-J-2 双句柄形态零改动**；S0b 重锚 61.1–321.0µs | 无新候选 |
| `track/primary-split.ts` | 纯组装层，S0e 地板覆盖 | 无 |
| `track/plan.ts` | S0e 重锚 1.10–2.73µs/plan；正则字面量 V8 缓存判读（§2.3）；acceptanceForRole 双遍家族账目维持 | 无新候选 |
| `track/clarify.ts` | habitsFromPreferences spread ns 级维持（R10-J） | 无新候选 |
| `track/loop.ts` | S1-J-5、S2-J-11、S4-J-2、S5-J-6、S6-J-4、S8-A-3 族维持 | 无新候选 |
| `context/index.ts` | manifests 双 relativeToRoot 对账 S8-J-1 域（§2.3）；R11-J 位点 A 未补铸；S0d 重锚 40.8–41.3µs；八面已闭 ID 维持 | 无新候选 |
| `context/packet.ts` | **位点 C 猎中并淘汰（§2.2，S4-J-4/S7-J-2/S9-J-3 换位点）**；S1-J-7 原位在位；`compilePacket` 零调用方（本轮新精化） | 无新候选 |
| `feedback/redaction.ts` | `applyRedaction` 零调用方（本轮新精化）；needles 排序 = S2-J-9；S3-J-1、S7-J-5、S9-J-1 维持 | 无新候选 |
| `feedback/store.ts` | readFeedback 双遍融合 R10-J 拒列维持；S2-J-9/10 维持 | 无新候选 |
| `feedback/types.ts` | 类型定义 | 无 |

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。切片自 R1-J 落地 J1
（`fb41417`）起经 R2-J..R11-J 与本轮 R12-J 十一遍复查累计零后续代码
改动，逐字节一致。

## 5. 新增排除 ID

**无。** 第十二遍枚举为空——跨函数重复纯计算普查找到的三处从未点名
位点（§2.2）均为既有已闭 ID 的换位点/换链路（S3-J-3、S4-J-5/S5-J-4、
S4-J-4/S7-J-2/S9-J-3）且探针实测 3.9–24.4ns 深度亚噪声，按换名纪律
不铸 ID；R11-J 两处无名微观按指令未补铸；其余重扫形状全部对账为已闭
条目原文域（§2.3）。与 R10-J / R11-J / R12-A / R12-E / R12-F / R12-H
的空枚举收口同型。

## 6. 测试

零代码改动下相关基线复核，全绿（Node v22.22.2 via nvm，满足 engines
>=22.19.0；系统 Node 22.14.0 过低的既知环境注记同 R1-J；
`pnpm install --frozen-lockfile` 后）：

```bash
pnpm exec tsx scripts/r1j-equivalence-sim.ts
# ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)
# perf fixture: reference 10379.9 ms -> current 3.8 ms (2718.1x)
pnpm gate   # typecheck + lint + test + build 全绿
# tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1（全套件，
# 含 cluster/privacy/preferences/episode/persist/track/context/feedback）
```

锚点重锚 harness 与三位点探针（临时脚本，无赢家、无新 ID，按
round01–11 仅赢家 sim 入库的仓库惯例未入库；完整源码见附录 A/B）
两遍独立运行方向与量级一致（数字见 §1.3 / §2.2）。

## 附录 A：I/O 地板矩阵 harness（完整，可复现）

与 R11-J 附录 A 同构（九格：saveToDisk 50/500 观察双档、jsonl
fsync=false/true、级联 match/no-match、context index、plan CPU
计数器/randomUUID 双档），除临时目录前缀外逐字节同源。运行方式：
保存为仓库根目录任意 `.ts` 并 `pnpm exec tsx <file>`。

```ts
/**
 * R12-J re-anchor harness (same cells as the R11-J appendix-A harness).
 * Re-anchors the configured-state I/O-floor / CPU cells on this VM.
 */
import { mkdtempSync, rmSync, mkdirSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join, dirname } from "node:path";

import {
  configurePreferencePersistence,
  recordPreference,
  resetPreferenceStore
} from "./src/preferences/store.js";
import { appendJsonlLine } from "./src/persist/jsonl.js";
import { cascadeFeedbackTombstones } from "./src/privacy/deletion.js";
import { feedbackLogPath, feedbackTombstonesPath } from "./src/feedback/store.js";
import { buildProjectContextIndex, type CodeMapSymbol } from "./src/context/index.js";
import { applyAnswers } from "./src/track/clarify.js";
import { planFromContract, acceptanceForRole } from "./src/track/plan.js";
import type { ProjectSnapshot } from "./src/domain/project.js";
import type { RequirementContract, DecisionQuestion } from "./src/domain/contract.js";
import type { EpisodeId } from "./src/domain/ids.js";

const hr = (): bigint => process.hrtime.bigint();
const us = (a: bigint, b: bigint, n: number): number => Number(b - a) / 1000 / n;
const fmt = (x: number): string => x.toFixed(x < 10 ? 2 : 1);

let idCounter = 0;
const gen = (): string => `bench${(idCounter++).toString(36)}`;

// ---------------------------------------------------------------- cell 1
async function cellSaveToDisk(seedCount: number): Promise<void> {
  const results: number[] = [];
  for (let run = 0; run < 3; run++) {
    const dir = mkdtempSync(join(tmpdir(), "r12j-pref-"));
    resetPreferenceStore();
    configurePreferencePersistence(join(dir, "prefs.json"));
    for (let i = 0; i < seedCount; i++) {
      recordPreference(
        (["user", "project", "role"] as const)[i % 3]!,
        `key-${i % 7}`,
        `pref-${i % 11}`,
        i % 2 === 0 ? `value-${i}` : i,
        `eps_seed${i.toString(36)}` as EpisodeId,
        1.0,
        i % 4 === 0
      );
    }
    const N = 200;
    const t0 = hr();
    for (let i = 0; i < N; i++) {
      recordPreference(
        "project",
        `key-${i % 7}`,
        `pref-${i % 11}`,
        `value-w${i}`,
        `eps_w${i.toString(36)}` as EpisodeId,
        1.0,
        false
      );
    }
    const t1 = hr();
    results.push(us(t0, t1, N));
    resetPreferenceStore();
    configurePreferencePersistence(undefined);
    rmSync(dir, { recursive: true, force: true });
  }
  console.log(`saveToDisk nonempty policy (recordPreference, ${seedCount}-obs seed, 200 writes): ${results.map(fmt).join(" / ")} us/write`);
}

// ---------------------------------------------------------------- cell 2
async function cellJsonl(): Promise<void> {
  for (const [fsync, iters] of [[false, 2000], [true, 300]] as const) {
    const results: number[] = [];
    for (let run = 0; run < 3; run++) {
      const dir = mkdtempSync(join(tmpdir(), "r12j-jsonl-"));
      const file = join(dir, "log.jsonl");
      const line = JSON.stringify({ id: "evt_x", type: "BENCH", payload: { n: 42, text: "x".repeat(120) } });
      for (let i = 0; i < 50; i++) await appendJsonlLine(file, line, false); // warm
      const t0 = hr();
      for (let i = 0; i < iters; i++) await appendJsonlLine(file, line, fsync);
      const t1 = hr();
      results.push(us(t0, t1, iters));
      rmSync(dir, { recursive: true, force: true });
    }
    console.log(`jsonl append fsync=${fsync} (${iters} iters): ${results.map(fmt).join(" / ")} us/append`);
  }
}

// ---------------------------------------------------------------- cell 3
function seedFeedback(stateRoot: string, withBodies: boolean): void {
  const log = feedbackLogPath(stateRoot);
  mkdirSync(dirname(log), { recursive: true });
  const lines: string[] = [];
  for (let i = 0; i < 200; i++) {
    const hit = i % 10 === 0; // 20 hits
    lines.push(
      JSON.stringify({
        id: `fb-${i}`,
        episodeId: hit ? "eps_hit" : `eps_other${i % 7}`,
        kind: "human",
        rubricVersion: "r1",
        score: 50 + (i % 40),
        evidenceRefs: [],
        redacted: false,
        createdAt: "2026-08-24T00:00:00.000Z",
        ...(withBodies ? { body: `free text feedback payload number ${i} `.repeat(3) } : {}),
        summary: `summary ${i}`
      })
    );
  }
  writeFileSync(log, lines.join("\n") + "\n", "utf8");
  writeFileSync(
    feedbackTombstonesPath(stateRoot),
    JSON.stringify(["fb-pre-a", "fb-pre-b", "fb-pre-c", "fb-pre-d", "fb-pre-e"], null, 2) + "\n",
    "utf8"
  );
}

async function cellCascade(): Promise<void> {
  // match cell: 20 hits -> full rewrite + tombstone write
  const matchResults: number[] = [];
  for (let run = 0; run < 3; run++) {
    const stateRoot = mkdtempSync(join(tmpdir(), "r12j-casc-"));
    seedFeedback(stateRoot, true);
    await cascadeFeedbackTombstones(stateRoot, "eps_warm" as EpisodeId); // warm no-match
    const iters = 30;
    let total = 0n;
    for (let i = 0; i < iters; i++) {
      seedFeedback(stateRoot, true); // untimed re-seed
      const t0 = hr();
      const out = await cascadeFeedbackTombstones(stateRoot, "eps_hit" as EpisodeId);
      const t1 = hr();
      if (out.length !== 20) throw new Error(`expected 20 cascaded, got ${out.length}`);
      total += t1 - t0;
    }
    matchResults.push(Number(total) / 1000 / iters);
    rmSync(stateRoot, { recursive: true, force: true });
  }
  console.log(`deletion cascade N=200 20-hit MATCH: ${matchResults.map(fmt).join(" / ")} us/cascade`);

  // no-match cell: 0 hits -> two reads, no rewrite
  const nomatchResults: number[] = [];
  for (let run = 0; run < 3; run++) {
    const stateRoot = mkdtempSync(join(tmpdir(), "r12j-casc-"));
    seedFeedback(stateRoot, true);
    const iters = 200;
    for (let i = 0; i < 20; i++) await cascadeFeedbackTombstones(stateRoot, "eps_none" as EpisodeId);
    const t0 = hr();
    for (let i = 0; i < iters; i++) {
      const out = await cascadeFeedbackTombstones(stateRoot, "eps_none" as EpisodeId);
      if (out.length !== 0) throw new Error("expected no cascade");
    }
    const t1 = hr();
    nomatchResults.push(us(t0, t1, iters));
    rmSync(stateRoot, { recursive: true, force: true });
  }
  console.log(`deletion cascade N=200 NO-MATCH: ${nomatchResults.map(fmt).join(" / ")} us/cascade`);
}

// ---------------------------------------------------------------- cell 4
function makeSnapshot(): { snapshot: ProjectSnapshot; codeMap: CodeMapSymbol[] } {
  const facts: ProjectSnapshot["facts"] = [];
  for (let i = 0; i < 12; i++) facts.push({ key: `architecture.layer${i}`, value: `layer ${i} does thing ${i}`, confidence: "HIGH" });
  for (let i = 0; i < 12; i++) facts.push({ key: `risk.area${i}`, value: `risk in area ${i}`, confidence: "MEDIUM" });
  for (let i = 0; i < 16; i++) facts.push({ key: `misc.fact${i}`, value: `misc value ${i}`, confidence: "LOW" });
  const codeMap: CodeMapSymbol[] = [];
  for (let i = 0; i < 30; i++) {
    codeMap.push({
      path: `src/mod${i % 8}/file${i}.ts`,
      symbol: `symbol${i}`,
      kind: (["class", "function", "interface", "type", "method", "module"] as const)[i % 6]!,
      public: i % 3 === 0,
      calls: [`callee${i % 5}`, `callee${(i + 2) % 5}`]
    });
  }
  const snapshot = {
    id: "prj_bench0001",
    rootPath: "/repo",
    discoveredAt: "2026-08-24T00:00:00.000Z",
    instructionFiles: [{ path: "/repo/AGENTS.md" }, { path: "/repo/docs/AGENTS.md" }, { path: "/repo/src/deep/AGENTS.md" }],
    manifests: [{ path: "/repo/package.json" }, { path: "/repo/pnpm-lock.yaml" }, { path: "/repo/tsconfig.json" }, { path: "/repo/sub/package.json" }],
    commands: [
      { name: "test", command: "pnpm test" },
      { name: "lint", command: "pnpm lint" },
      { name: "build", command: "pnpm build" }
    ],
    facts
  } as unknown as ProjectSnapshot;
  return { snapshot, codeMap };
}

function cellContextIndex(): void {
  const { snapshot, codeMap } = makeSnapshot();
  const options = {
    dirtyPaths: ["/repo/src/a.ts", "/repo/src/b.ts", "/repo/gen/out.js", "/repo/src/c.ts", "/repo/docs/x.md", "/repo/src/d.ts"],
    generatedPaths: ["/repo/gen", "/repo/dist", "/repo/.cache"],
    sourceHashes: { "/repo/package.json": "h1", "/repo/AGENTS.md": "h2", "architecture.layer0": "h3" },
    priorHashes: { "/repo/package.json": "h1", "/repo/AGENTS.md": "hOLD" },
    codeMap
  };
  const results: number[] = [];
  let sink = 0;
  for (let run = 0; run < 3; run++) {
    for (let i = 0; i < 2000; i++) sink += buildProjectContextIndex(snapshot, options).facts.length;
    const N = 20000;
    const t0 = hr();
    for (let i = 0; i < N; i++) sink += buildProjectContextIndex(snapshot, options).facts.length;
    const t1 = hr();
    results.push(us(t0, t1, N));
  }
  console.log(`buildProjectContextIndex (F=40, 30 codemap syms): ${results.map(fmt).join(" / ")} us/build  [sink=${sink}]`);
}

// ---------------------------------------------------------------- cell 5
function cellPlanCpu(useCounterGen: boolean): void {
  const questions: DecisionQuestion[] = [
    { id: "q-tests", question: "Include tests?", options: ["yes", "no"], default: "yes" },
    { id: "q-done", question: "What does done look like?", options: [], default: "shipped" },
    { id: "q-write", question: "Write code or investigate?", options: ["write", "investigate"], default: "write" }
  ];
  const answers = { "q-tests": "yes", "q-done": "feature shipped and verified", "q-write": "write the code" } as const;
  const contract: RequirementContract = {
    schemaVersion: 1,
    objective: "Implement the new export module and add tests covering redaction",
    deliverables: [],
    constraints: [{ id: "c-smallest", description: "smallest change", enforceable: false } as never],
    nonGoals: [],
    acceptanceCriteria: [
      { id: "ac-1", description: "Export module produces valid JSON", observableCheck: "run export" },
      { id: "ac-tests", description: "Unit tests pass", observableCheck: "pnpm test" },
      { id: "ac-2", description: "Redaction verified on fixtures", observableCheck: "fixture diff" }
    ],
    assumptions: [],
    questions,
    authority: [],
    sourceRefs: []
  };
  const habits = { preferReview: true } as const;

  // fidelity check once: expect 5 children
  {
    const applied = applyAnswers(questions, answers);
    const children = planFromContract({
      contract, habits, answers: applied.resolved,
      ...(useCounterGen ? { generateId: gen } : {})
    });
    if (children.length !== 5) throw new Error(`expected 5 children, got ${children.length}`);
    const roles = children.map((c) => c.role).join(",");
    if (roles !== "planner,scout,implementer,reviewer,tester") throw new Error(`unexpected roles ${roles}`);
  }

  const results: number[] = [];
  let sink = 0;
  const warm = useCounterGen ? 20000 : 5000;
  const N = useCounterGen ? 200000 : 50000;
  for (let run = 0; run < 3; run++) {
    for (let i = 0; i < warm; i++) {
      const applied = applyAnswers(questions, answers);
      const children = planFromContract({
        contract, habits, answers: applied.resolved,
        ...(useCounterGen ? { generateId: gen } : {})
      });
      for (const child of children) sink += acceptanceForRole(child.role, contract).length;
    }
    const t0 = hr();
    for (let i = 0; i < N; i++) {
      const applied = applyAnswers(questions, answers);
      const children = planFromContract({
        contract, habits, answers: applied.resolved,
        ...(useCounterGen ? { generateId: gen } : {})
      });
      for (const child of children) sink += acceptanceForRole(child.role, contract).length;
    }
    const t1 = hr();
    results.push(us(t0, t1, N));
  }
  console.log(`track plan CPU (${useCounterGen ? "counter gen" : "default randomUUID gen"}): ${results.map(fmt).join(" / ")} us/plan  [sink=${sink}]`);
}

// ---------------------------------------------------------------- main
async function main(): Promise<void> {
  console.log(`node ${process.version}`);
  await cellSaveToDisk(50);
  await cellSaveToDisk(500);
  await cellJsonl();
  await cellCascade();
  cellContextIndex();
  cellPlanCpu(true);
  cellPlanCpu(false);
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
```

## 附录 B：三处从未点名位点的探针（完整，可复现）

```ts
/**
 * R12-J never-named-site upper-bound probes (loser adjudication; not committed).
 * Site A: cluster spawn path double objective.trim()
 *         (spawn.ts L41 emptiness check + host.ts L181 payload trim).
 * Site B: mailbox.claimRole skip-branch per-own-mail byRole re-fetch + re-set
 *         (mailbox.ts L70-72).
 * Site C: packet compileContextPacket same-array double loop over
 *         codeMapSelection.omissions (packet.ts L130-140).
 * Deterministic fixtures; three timed reps each; equivalence asserted inline.
 */
const hr = (): bigint => process.hrtime.bigint();
const ns = (a: bigint, b: bigint, n: number): number => Number(b - a) / n;

// ---- Site A: double trim vs single trim on the spawn path
function spawnDoubleTrim(objective: string): string {
  // mirrors validateSpawn L41 (check) + host.spawn L181 (payload)
  if (typeof objective !== "string" || objective.trim() === "") {
    throw new Error("spawn objective must be non-empty");
  }
  return objective.trim();
}
function spawnSingleTrim(objective: string): string {
  if (typeof objective !== "string") {
    throw new Error("spawn objective must be non-empty");
  }
  const trimmed = objective.trim();
  if (trimmed === "") {
    throw new Error("spawn objective must be non-empty");
  }
  return trimmed;
}
{
  const fixtures = [
    "Survey the workspace and constraints for: implement the export module",
    "  padded objective with whitespace  ",
    "x"
  ];
  for (const fixture of fixtures) {
    if (spawnDoubleTrim(fixture) !== spawnSingleTrim(fixture)) {
      throw new Error("site A equivalence violated");
    }
  }
  let threwA = false;
  let threwB = false;
  try { spawnDoubleTrim("   "); } catch { threwA = true; }
  try { spawnSingleTrim("   "); } catch { threwB = true; }
  if (!threwA || !threwB) throw new Error("site A empty-objective equivalence violated");

  const objective = fixtures[0]!;
  const N = 10_000_000;
  let sink = 0;
  for (let rep = 0; rep < 3; rep++) {
    for (let i = 0; i < N / 10; i++) sink += spawnDoubleTrim(objective).length + spawnSingleTrim(objective).length;
    let t0 = hr();
    for (let i = 0; i < N; i++) sink += spawnDoubleTrim(objective).length;
    let t1 = hr();
    const two = ns(t0, t1, N);
    t0 = hr();
    for (let i = 0; i < N; i++) sink += spawnSingleTrim(objective).length;
    t1 = hr();
    const one = ns(t0, t1, N);
    console.log(`site A rep${rep}: double-trim=${two.toFixed(2)}ns single-trim=${one.toFixed(2)}ns delta=${(two - one).toFixed(2)}ns/spawn [sink=${sink}]`);
  }
}

// ---- Site B: claimRole skip-branch re-fetch vs hoisted remaining list
interface Mail { readonly id: string; readonly from: string; to?: string; readonly body: string }

function claimProduction(
  byRole: Map<string, Mail[]>,
  byAgent: Map<string, Mail[]>,
  role: string,
  agentId: string
): Mail[] {
  // mirrors mailbox.ts claimRole L64-79
  const pending = byRole.get(role) ?? [];
  byRole.set(role, []);
  const delivered: Mail[] = [];
  const box = (id: string): Mail[] => {
    let list = byAgent.get(id);
    if (list === undefined) {
      list = [];
      byAgent.set(id, list);
    }
    return list;
  };
  for (const mail of pending) {
    if (mail.from === agentId) {
      const remaining = byRole.get(role) ?? [];
      remaining.push(mail);
      byRole.set(role, remaining);
      continue;
    }
    const copy = { ...mail, to: agentId };
    box(agentId).push(copy);
    delivered.push(copy);
  }
  return delivered;
}

function claimHoisted(
  byRole: Map<string, Mail[]>,
  byAgent: Map<string, Mail[]>,
  role: string,
  agentId: string
): Mail[] {
  const pending = byRole.get(role) ?? [];
  const remaining: Mail[] = [];
  byRole.set(role, remaining);
  const delivered: Mail[] = [];
  const box = (id: string): Mail[] => {
    let list = byAgent.get(id);
    if (list === undefined) {
      list = [];
      byAgent.set(id, list);
    }
    return list;
  };
  for (const mail of pending) {
    if (mail.from === agentId) {
      remaining.push(mail);
      continue;
    }
    const copy = { ...mail, to: agentId };
    box(agentId).push(copy);
    delivered.push(copy);
  }
  return delivered;
}

{
  const mkQueue = (): Mail[] => [
    { id: "m1", from: "agent-2", body: "ping" },
    { id: "m2", from: "agent-1", body: "self-cast" }, // own mail (skip branch)
    { id: "m3", from: "agent-3", body: "pong" },
    { id: "m4", from: "agent-1", body: "self-cast-2" } // own mail (skip branch)
  ];
  // equivalence: delivered mails, remaining role queue, agent inbox all match
  {
    const roleA = new Map([["tester", mkQueue()]]);
    const agentA = new Map<string, Mail[]>();
    const roleB = new Map([["tester", mkQueue()]]);
    const agentB = new Map<string, Mail[]>();
    const a = claimProduction(roleA, agentA, "tester", "agent-1");
    const b = claimHoisted(roleB, agentB, "tester", "agent-1");
    if (JSON.stringify(a) !== JSON.stringify(b)) throw new Error("site B delivered mismatch");
    if (JSON.stringify(roleA.get("tester")) !== JSON.stringify(roleB.get("tester"))) {
      throw new Error("site B remaining-queue mismatch");
    }
    if (JSON.stringify(agentA.get("agent-1")) !== JSON.stringify(agentB.get("agent-1"))) {
      throw new Error("site B inbox mismatch");
    }
  }
  const N = 2_000_000;
  let sink = 0;
  for (let rep = 0; rep < 3; rep++) {
    for (let i = 0; i < N / 10; i++) {
      sink += claimProduction(new Map([["t", mkQueue()]]), new Map(), "t", "agent-1").length;
      sink += claimHoisted(new Map([["t", mkQueue()]]), new Map(), "t", "agent-1").length;
    }
    let t0 = hr();
    for (let i = 0; i < N; i++) {
      sink += claimProduction(new Map([["t", mkQueue()]]), new Map(), "t", "agent-1").length;
    }
    let t1 = hr();
    const prod = ns(t0, t1, N);
    t0 = hr();
    for (let i = 0; i < N; i++) {
      sink += claimHoisted(new Map([["t", mkQueue()]]), new Map(), "t", "agent-1").length;
    }
    t1 = hr();
    const hoisted = ns(t0, t1, N);
    console.log(`site B rep${rep}: production=${prod.toFixed(1)}ns hoisted=${hoisted.toFixed(1)}ns delta=${(prod - hoisted).toFixed(1)}ns/claim [sink=${sink}]`);
  }
}

// ---- Site C: packet codeMap-omissions double loop vs fused single loop
interface CmOmission { readonly path: string; readonly symbol: string; readonly reason: "token-budget"; readonly rank: number }
interface PacketCmOmission extends CmOmission { readonly source: "index" | "packet" }
interface OmRecord { readonly key: string; readonly reason: string; readonly rank: number }
const RANK_CODE_MAP = 70;

function omissionsTwoLoops(cm: readonly CmOmission[], omissions: OmRecord[]): PacketCmOmission[] {
  // mirrors packet.ts L130-140
  const packetCodeMapOmissions: PacketCmOmission[] = cm.map((omission) => ({
    ...omission,
    source: "index"
  }));
  for (const omission of cm) {
    omissions.push({
      key: `code-map:${omission.path}:${omission.symbol}`,
      reason: "token-budget",
      rank: RANK_CODE_MAP
    });
  }
  return packetCodeMapOmissions;
}
function omissionsFused(cm: readonly CmOmission[], omissions: OmRecord[]): PacketCmOmission[] {
  const packetCodeMapOmissions: PacketCmOmission[] = [];
  for (const omission of cm) {
    packetCodeMapOmissions.push({ ...omission, source: "index" });
    omissions.push({
      key: `code-map:${omission.path}:${omission.symbol}`,
      reason: "token-budget",
      rank: RANK_CODE_MAP
    });
  }
  return packetCodeMapOmissions;
}
{
  const mkCm = (n: number): CmOmission[] => {
    const out: CmOmission[] = [];
    for (let i = 0; i < n; i++) {
      out.push({ path: `src/mod${i % 4}/file${i}.ts`, symbol: `symbol${i}`, reason: "token-budget", rank: 2 });
    }
    return out;
  };
  for (const n of [0, 10]) {
    const cm = mkCm(n);
    const omA: OmRecord[] = [];
    const omB: OmRecord[] = [];
    const a = omissionsTwoLoops(cm, omA);
    const b = omissionsFused(cm, omB);
    if (JSON.stringify(a) !== JSON.stringify(b) || JSON.stringify(omA) !== JSON.stringify(omB)) {
      throw new Error(`site C equivalence violated at n=${n}`);
    }
  }
  for (const n of [0, 10]) {
    const cm = mkCm(n);
    const N = 2_000_000;
    let sink = 0;
    for (let rep = 0; rep < 3; rep++) {
      for (let i = 0; i < N / 10; i++) {
        sink += omissionsTwoLoops(cm, []).length + omissionsFused(cm, []).length;
      }
      let t0 = hr();
      for (let i = 0; i < N; i++) sink += omissionsTwoLoops(cm, []).length;
      let t1 = hr();
      const two = ns(t0, t1, N);
      t0 = hr();
      for (let i = 0; i < N; i++) sink += omissionsFused(cm, []).length;
      t1 = hr();
      const one = ns(t0, t1, N);
      console.log(`site C n=${n} rep${rep}: two-loops=${two.toFixed(1)}ns fused=${one.toFixed(1)}ns delta=${(two - one).toFixed(1)}ns/compile [sink=${sink}]`);
    }
  }
}
```

实测输出（两次独立运行）：

```text
run1  site A: delta = +5.53 / +4.75 / +5.73 ns/spawn
      site B: delta = +15.5 / +9.1 / +3.9 ns/claim
      site C: n=0 delta = +5.2 / +14.5 / +5.9   n=10 delta = +20.3 / +10.9 / +17.3 ns/compile
run2  site A: delta = +4.74 / +4.48 / +5.29 ns/spawn
      site B: delta = +14.2 / +13.6 / +10.4 ns/claim
      site C: n=0 delta = +5.2 / +13.4 / +5.9   n=10 delta = +24.4 / +17.6 / +15.8 ns/compile
```

MORE_OPTIMA=no
BRANCH=cursor/r12-j-persist-twelfth-pass-83a1
