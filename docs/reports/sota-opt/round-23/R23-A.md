MODEL_SLUG=claude-fable-5-thinking-xhigh

# R23-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第二十三遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 23 / R23-A（Round 1–22 同区第二十三遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `7762f21`（含 R22-H 空枚举收口与 R23-A 派出；R22-I / R22-J 在飞未触碰）
**分支:** `cursor/r23-a-tracking-twenty-third-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S23-A-\* ID，切片零代码改动；
整片预算收口第二十一次复核成立，并新增一条实测轴——「G×E 自耦合 run
ledger（积分账本）」补上二十二遍搜查史里 E 轴与 G 轴之间的最后一块
耦合盲区：R14-A 在**固定 gate**下定价了事件表规模 E 的斜率，R16-A 用
**固定 E=41** 的锚点算术推出 G（gates/run）的 10 ms 穿越点（≈654–763），
但两轴的耦合从未被定价——每个 applied gate 会向事件表**追加 1–3 个
事件**（PASSED/none +1 TA；FAILED/blocked +3 = TA+GT+RUN_BLOCKED），
而下一个 gate 必须对这张变大的表重跑五次 O(E) 扫描加一次 `[...events]`
拷贝，所以 N-gate run 的真实积分成本是 N 的二次式
total(N) ≈ a·N + c·N²，R16-A 的线性穿越点隐含 c=0。本轮建立确定性
积分账本驱动器（表随 gate 自身追加 + 每 gate m 条 ambient
CHILD_MESSAGE 增长），给出五组定价：真实长表上的 gate-at-depth 单价
（E ∈ {41, 591, 1141, 2241}：12.30–13.15 → 43.41–43.85 µs/gate，
直测斜率 **b = 13.96–14.20 ns/event**——约为 R14-A 系 CHILD_MESSAGE
填充表斜率（本轮同址复测 7.20–7.26 ns/event）的 **1.9–2.0×**，因为
账本追加的 TA/GT 事件在 nextTrackingSeq 与双幂等 find 的谓词上按事件
更贵——落在 R17-A 组成包络（最坏合法混合 ~2.5–3×）之内，斜率成分
是 R17-A 的已价点，本轮新内容只在积分项）；积分账本四格
N ∈ {5, 25, 100, 400}（m=4 mixed：12.03–12.20 → 31.84–32.29
µs/gate 均价）；最小二乘拟合 **total(N) = (12.30–12.59)·N +
(0.0484–0.0496)·N² µs**，拟合 c 为模型预测 b·(g+m)/2（0.0384–0.0390）
的 1.24–1.27×（超出部分来自账本加深时 TA/GT 占比上升使有效 b 递增的
超二次尾巴，已记录）；**修正后 10 ms 穿越点 N ≈ 341–344 gates/run，
对比同夹具线性算术 794–813（锚点格口径 645–714，与 R16-A 系 654–763
同带）——自耦合把 G 轴穿越点压低 ~2.3–2.4×**；生产规模格（N=5
all-PASSED 积分 56.5–57.0 µs vs 锚点算术 69.8–77.6 µs，delta
−12.8~−21.2 µs——积分态反而更便宜，系紧环单态 PIC 效应，R11-A 以来
「PIC/基准次序」家族；生产 N≈5 下增长项本体仅 ~0.35 µs，锚点算术
仍是有效且略保守的上界）。四组确定性证人（F3a–F3d）三次运行计数
逐位一致。该轴上全部可想角度经换名检查逐一判为既有排除 ID 的本体
（5 项新记录，见 §0——caller 侧 seq/status 携带恰是 **X0-4 的字面
本体**），**只产出归档证人与 G 轴穿越点的数据修正，未产出可落地
候选**——修正后穿越点距生产规模（N≈5）仍有 **~1.8 个量级**，按令即
空收口。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R22-A 至本轮基线
（`7762f21`）**逐字节未变**（`git diff 7acb666..7762f21 -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，二十三遍全程零
diff；清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2）并全部
实际重读）。调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..7762f21 -- src/` **完全为空**（跨 R10…R23 开轮十四段
全部为 docs 提交，`git log 89f0455..HEAD -- src/` 计数为 0），生产调用方
经本轮全库 grep 复核仍为 `supervisor.ts:483`
（applyTrackingGate/nextTrackingSeq；两个 settle 调用点 601/711 均不传
trackingAssessment ⇒ 生产 O(1) no-op，第十四次复验，本轮源码级重读
`settleSupervisedOutcome` 首行 `if (assessment === undefined) return`）/
`coordinator.ts:444` / `flowchart-run.ts:320`（applyChildThreeLine），
每子结果一次（~5 次/run），事件表几十级（41）。R22-A 的 ~65–80 µs/run
预算天花板经本轮在当前 VM（Node v22.22.2；本 VM 默认 v22.14.0 且
`/exec-daemon/node` 垫片遮蔽 nvm，按 engines >=22.19.0 经 PATH 前置
nvm v22.22.2）实测复核为 **~70–82 µs/run**（五格锚点矩阵三次独立运行
14.0–16.4 µs/gate；上沿 82 由 run3 no-contract 首格 16.4 拉出——
R11-A 以来「PIC/基准次序」现象的本轮形态，run1/2 同格 15.4–16.0；
与 R22-A 65–80 / R21-A 稳态 66–81 同带略高；单格横向比较不具意义；
量级结论不变——稳态下即使切片零成本，节省上界 ~0.07–0.08 ms/run，
仍比落地线低约三个量级）。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
数据补充（5 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项 +
R15-A 5 项 + R16-A 4 项 + R17-A 4 项 + R18-A 4 项 + R19-A 2 项 +
R20-A 3 项 + R21-A 2 项 + R22-A 5 项换名检查全部复核维持，未追认任何
S12…S22-A-\* ID），按令一律不铸新 ID。

R12-A 分解的五大组件占比（hashAssessment×3 ~37–41% / validateEvent
~20–22% / turn ~11–12% / prescore×2 ~8.6–9.5% / O(E) 五扫描+拷贝
~1.9–2.1%）在本 VM 逐一复现（§1 表），R18-A skip 定价与 R19-A replay
定价抽测复现同带（guard-3 skip 2033–2094 ns = 13.1–14.7% of apply；
replay find#1 3096–3350 ns = 20.2–24.0% of apply）。第二十三遍收口，
**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；未触碰在飞的
  R22-I / R22-J 分支与文件，未触碰已收口的 R22-\* 报告。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表 545 行，含
  S13-B-1；R12-A…R22-A 空枚举未铸 ID，确认）/ PROGRESS.md /
  round-23/PLAN.md / R22-A / R21-A。EXCLUSIONS.md / PROGRESS.md /
  各 PLAN.md / README.md 零触碰。
- 环境：本 VM 默认 Node v22.14.0（`/exec-daemon/node` 垫片遮蔽 nvm），
  按 engines（>=22.19.0）以 PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 切至 v22.22.2 后
  `pnpm install --frozen-lockfile`（pnpm 10.17.1），全部测量在
  v22.22.2 完成。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或数据补充，**一律不铸 S23-A-\* ID**。R12-A 13 项、R13-A 8 项、
  R14-A 3 项、R15-A 5 项、R16-A 4 项、R17-A 4 项、R18-A 4 项、R19-A
  2 项、R20-A 3 项、R21-A 2 项、R22-A 5 项换名检查全部复核维持，不再
  逐条重列；以下 5 项为本轮**新记录**，全部由 axis 12 的积分视角
  引出）：
  1. **caller 侧 seq/status 跨 gate 携带**（coordinator/flowchart 循环
     里把上一 gate 的 expectedSeq+1 与 runStatus 线程化传递，免去每
     gate 的 nextTrackingSeq / currentGateStatus O(E) 重扫——积分账本
     里这正是二次项的直接承载者）= **X0-4 字面本体**（「改
     applyTrackingGate/nextTrackingSeq 公开签名为增量索引 | 接口
     破坏」）+ S1-A-1（currentGateStatus 反向扫描早退已拒）+ S1-A-9
     （nextTrackingSeq 反向扫描不等价：乱序 seq 发散）；supervisor
     settle 的 `expectedSeq?` 入参是既有可选面，但 status 携带须拓宽
     公开返回/入参形状且 caller 在切片外（G 切片辖），拒列；
  2. **账本批量 gate API**（把同一 run 内待结算的多个子结果在一次
     调用里共享一遍扫描+一次拷贝地结算，摊平二次项）= X1-2 平行
     入口/实现 + 时序语义功能变更（生产 gate 随子结果完成逐个结算，
     事件交错顺序是可观察契约——批量化改变 TA/GT 与 ambient 事件的
     interleaving，非保行为优化），拒列；
  3. **跨 gate 可变追加摊销**（run 内维护私有可变事件表，免去每 gate
     `[...events]` 拷贝）= R1-A 不可变拷贝下界本体 + S2-A-4
     （spread+push 换 concat 已拒）+ X1-1（跨调用隐藏可变状态），
     拒列；
  4. **按数组身份记忆化 nextTrackingSeq / currentGateStatus**
     （WeakMap 以 events 数组身份为键缓存扫描结果——积分账本里每
     gate 输入数组都是新身份，命中率恰 0；replay 场景才可能命中）=
     X1-1 隐藏缓存本体 + S8-G-2 同型（生产命中 0），拒列；
  5. **混合感知扫描特化**（事件表按类型分区/建 per-type 索引，绕过
     TA/GT 事件在 seq 扫描与幂等 find 谓词上的高价——本轮 b 的
     1.9–2.0× 混合乘数的直接摊平路）= 共享 `Event[]` 契约的数据
     结构变更（X0-4 族：事件表是跨模块事实源）+ 派生索引即隐藏
     缓存（X1-1）；且斜率成分本身在 R17-A 组成包络内已价，拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  幂等双 find/死扫描 = S8-A-1 / S11-A-2；episode/seq/status/copy 五
  扫描逐条 = X0-4/X2-4/S1-A-1/S1-A-9/R1-A 清单（R12-A 起锁定）；写侧
  validateEvent = S4-G-2 族（实现在 events.ts，G 切片）；
  `every×includes` Set 化 = S1-A-4/X4-7（C/R ≈ 10³ 重开条件，R15-A
  轴 1）；第二次 prescore = S1-A-2；turn 恒定输入常量折叠 = R12-A
  §0-11 伞 + R16-A §0-2；skip 守卫次序/享元/prescore 复用 = R18-A
  §0-1/2/4 维持；replay 快路径/重算去重 = R19-A §0-1/2 维持
  （F3d 只验形态不重定价）；hashSummary 缓存/增量与 with-previous
  管道融合 = R20-A §0-2/3 维持；H 面 = R21-A 维持；分析-隔离面 =
  R22-A §0-1..5 维持。
- 未重开任何 X\* / S1-\* … S22-\* 条目。**R22-A 公开导出普查 +
  分析-隔离面（7 个无价导出、sanitize 36–65 ns、proposeFromAnomaly
  切片内 682–700 ns、10 ms T≈1.5×10⁵）不补铸、不重测**；**R21-A H
  输入面（humanInput；生产恒 `{}`）不补铸**；**R20-A
  `gate.openMinors` M 轴与多轮驻留链 K 不补铸**（axis 12 的账本是
  事件表面的 gate 积分，不是 turn 面的 K 链——K 链是 window.previous
  驻留，二者正交）；**R19-A 重投递 / wait_user 不补铸**（F3d 是
  grown-table 态的形态证人，引用 R19-A 带，不重定价）；R18-A 两轴
  只抽测 guard-3 skip 一格；R17-A 组成轴不重测（本轮 b 的混合乘数
  是其包络内的实现点，作数据连续性引用）；未把 TERMCLASS / SEEDX /
  NVG / KFAN / R22-H 调用上下文几何 / R22-F membership-topology 移植
  为本切片新 A ID；S11-A-1 / S11-A-2 / S10-A-1 / S9-A-1 / S8-A-1..3 /
  S7-A-1..4 / X0-4/X2-4 / X0-6 / S6-A-1 专项复核：本轮零触碰、
  零重提，全部维持关闭。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点五格
  （gate-outcome × contract × max-codes）覆盖；配置态逐面复核——
  (a) settle 带/不带 assessment：生产两调用点 601/711 均不带
  （O(1) no-op 源码级复验），带 assessment 臂即 applyTrackingGate
  本体（已价）；(b) trackingConfig 覆写/applyUserThreshold：生产
  from-child 恒走 DEFAULT（R22-A config 双臂已价，X0-7 阈值冻结
  维持）；(c) maxItems 截断态 = S2-A-1 已拒面；(d) policyVersion 为
  透传字符串无分支。无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足——axis 12 的账本只经由公开
  applyChildThreeLine/applyTrackingGate 驱动，F3a–F3d 是对既有幂等/
  seq/growth 契约的**分布级回声验证**，未改任何阈值、未改任何测试。
  不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。
  无赢家 ⇒ 零生产改动，未提交任何仿真脚本（按令不建
  `scripts/round23-r23a-equivalence-sim.ts`），仿真全文见本报告附录。
  不开 PR。

## 1. 预算支配论证复核 + 一条新轴（方法第 1、2 步）

R3-A §3 … R22-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..7762f21 -- <切片>` 为空，逐字节
   一致（二十三遍全程零 diff）——R1-A…R22-A 全部测量与排除按令转移，
   但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨十四段）**：`git diff 89f0455..7762f21 --
   src/` **完全为空**（R10 开轮以来全部提交均在 docs/ 下）。生产调用方
   三点位（supervisor.ts:483 / coordinator.ts:444 /
   flowchart-run.ts:320）、settle 调用点 601/711 不传 assessment
   （首行 `if (assessment === undefined) return`）、
   `parseTrackingAssessment` 唯一 src/ 调用方 events.ts:738、
   `runTrackingTurn` 唯一 src/ 调用方 from-child.ts:81（humanInput 恒
   `{}`）、`proposeFromAnomaly` / `sanitizePacketForAnalysis` /
   isolation 双函数 / `executionAuthority` 切片外零 src/ 调用方、
   `extractHumanScore` / `hashSummary` 切片外零 src/ 生产调用方，
   经本轮全库 grep 全部复验，无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               16.0      15.4      16.4  µs/gate (+1 event)
PASSED x contract(3)               14.0      14.3      15.5  µs/gate (+1 event)
FAILED x no-contract               15.4      15.3      15.0  µs/gate (+3 events)
FAILED x contract(3)               14.5      14.6      14.4  µs/gate (+3 events)
FAILED x contract(3) x 2-code      15.2      15.2      15.1  µs/gate (+3 events)
=> ~70–82 µs/run（稳态热；除 run3 首格 16.4 上沿外 ~70–80）
```

与 R22-A（13.0–16.0 µs/gate ⇒ 65–80 µs/run）同带略高；五格三次运行
**无悬崖、无空洞**。本轮仿真在锚点前多驱动了 axis 12 的账本形状
（长表 + TA/GT 混合），PIC 污染路径与 R21-A（F1b/F1c 多形态预热后
首格 19.1）同型但更轻。稳态下即使把整个切片优化到零成本，节省上界
~0.07–0.08 ms/run，仍比战役落地线（数十~数百 ms 或复杂度类下降）低
**约三个量级**。**支配论证（稳态口径）第二十一次复核成立**；生产
冷态口径维持 R17-A 轴 6b 裁决（[2.7, 8.5] ms/run once-per-run，
不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R22-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R22-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1924–1935 ns 每次 ⇒ **5.77–5.81 µs** | 5.88–6.10 µs | ~37–41% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 312–364 ns + 第三次 hash） | **2.99–3.06 µs** | 2.94–2.99 µs | ~20–22% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.70–1.73 µs** | 1.69–1.75 µs | ~11–12% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 657–666 ns 每次 ⇒ **1.31–1.33 µs** | 1.32–1.36 µs | ~8.6–9.5% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 78–79 + seq 63–64 + 双幂等 find 38–39×2 + status 50–53 + `[...events]` 25–28 ns） | **295–298 ns**（⇒ 7.20–7.26 ns/event @E=41） | 276–290 ns | **~1.9–2.1%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以当轮 contract 格（14.0–15.5 µs）计。`applyTrackingGate` 整体
隔离实测 6.41–6.44 µs，与 R22-A（5.93–6.01）同带略高（VM 世代内
抖动，组件占比不变）。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次运行区间）：guard-3 skip（FAILED 零 evidence refs）2033–2094 ns
= 13.1–14.7% of apply（R22-A 14.2–15.2% 同带）；replay find#1
（GT idempotencyKey 命中，E=44）3096–3350 ns = 20.2–24.0% of apply
（R22-A 23.7–25.0% 同带）——组成天花板 {apply, skip, replay} 两个半边
继续成立，不重测其余格。

**本轮新轴 12：G×E 自耦合 run ledger（积分账本）**（确定性账本驱动器
经公开 applyChildThreeLine 驱动；三次运行区间）：

```text
账本驱动器（F3a/F3b/F3c 钉死，三次逐位一致）:
  基表 E0=41（含 RUN_ATTACHED）；每 gate 先追加 m 条 ambient
  CHILD_MESSAGE 再结算一个子结果；mixed = 每第 4 个 gate FAILED
  （25%），其余 PASSED×contract(3)
  N=64 mixed m=4: 终表 393 = 41 + 256(ambient) + 96(自生成)；
  TA seq 恰 0..63 顺序；16 FAILED ⇒ 16 GT + 16 RUN_BLOCKED；
  每 gate 追加恰 1（none）| 3（blocked）；两次独立构建逐字节相同；
  nextTrackingSeq(表@gate k) ≡ k 全 k 成立；终态 BLOCKED；
  all-PASSED 账本零 GT/RUN_BLOCKED
  F3d: 40 个种子化早期 gate 命令对终表（E=393）重放，全部
  applied=false、events 按引用原样返回、directive 与原值一致
  （R19-A 形态证人，不重定价）

gate-at-depth 单价（真实 grown mixed 表上一个 fresh PASSED×contract gate）:
  depth k=  0 (E=  41)     12.30–13.15 µs/gate
  depth k=100 (E= 591)     19.87–20.94 µs/gate
  depth k=200 (E=1141)     27.51–27.66 µs/gate
  depth k=400 (E=2241)     43.41–43.85 µs/gate
  => 直测斜率 b = 13.96–14.20 ns/event，为五扫描清单同址斜率
     （7.20–7.26 ns/event @E=41，CHILD_MESSAGE 填充）的 1.9–2.0×：
     账本自生成的 TA/GT 事件在 seq 扫描（payload.seq 读 + Math.max）
     与双幂等 find 的第二谓词上按事件更贵——落在 R17-A 组成包络
     （最坏合法混合 ~2.5–3×）之内，斜率成分非新事实，积分项才是

积分账本（只计 gate 调用时间，ambient 拼接为夹具管道不计时）:
  N=  5 m=4 mixed          0.060–0.061 ms（12.03–12.20 µs/gate 均价）
  N= 25 m=4 mixed          0.318–0.321 ms（12.73–12.84）
  N=100 m=4 mixed          1.724–1.752 ms（17.24–17.52）
  N=400 m=4 mixed          12.736–12.917 ms（31.84–32.29）
  N=400 m=0 mixed（纯自耦合） 7.138–7.798 ms（17.85–19.50）
  生产规模格: N=5 all-PASSED m=4 积分 56.5–57.0 µs
    vs 锚点算术 5×PASSED-contract = 69.8–77.6 µs（delta −12.8~−21.2 µs）
    ——积分态更便宜：紧环单态 PIC 红利 » 增长项本体（5 gate ×
    ~5.5 event/gate × ~14 ns ≈ 0.35 µs）；锚点算术仍是生产口径的
    有效且略保守上界

二次拟合与穿越点修正（零截距最小二乘，四格 N）:
  total(N) = (12.30–12.59)·N + (0.0484–0.0496)·N² µs
  模型预测 c = b·(g+m)/2 = 0.0384–0.0390 µs（g_avg=1.5, m=4）
  拟合/预测 = 1.24–1.27（超出部分 = 账本加深时 TA/GT 占比上升 ⇒
  有效 b 递增的超二次尾巴，随深度单调、无悬崖）
  10 ms 穿越点: 二次修正 N ≈ 341–344 gates/run
    vs 同夹具线性算术 794–813（锚点格口径 10000/(14.0–15.5) =
    645–714，与 R16-A 系 654–763 同带）
  => 自耦合把 G 轴穿越点压低 ~2.3–2.4×；距生产 N≈5 仍 ~1.8 个量级
```

五个结论：

1. **E×G 耦合格网闭合**：R14-A 定价了 E|G=const，R16-A 用算术推了
   G|E=const，R17-A 定价了固定 gate 下的混合组成——本轮把「表由 gate
   自身生成」这一反馈回路定价完毕：二次系数实测 0.0484–0.0496 µs、
   模型可解释度 ~79–81%（其余为混合占比漂移的超二次尾巴）。切片的
   规模轴族 {E, C, R, V, S, L, M, K, H_L, H_m, H_N, T, G} 中最后一个
   未定价的**轴间耦合项**自此有价。
2. **G 轴穿越点数据修正（非重开）**：R16-A 系的 G 穿越点是固定 E 的
   线性外推；计入自耦合后 10 ms 穿越点从 ~645–813 压至 **~341–344
   gates/run**。修正后 G 距生产（N≈5）~1.8 个量级，数值上取代 C 轴
   （~2.5 个量级，R15-A）成为**最近的规模悬崖**；但 G 悬崖的全部
   摊平路（§0-1..5）均被 X0-4/X1-2/R1-A/X1-1 钉死，而 C 轴的摊平路
   （S1-A-4/X4-7 家族 Set 化）是合法重开物——**可操作重开优先级仍归
   C 轴**，G 悬崖只作预算天花板的精化记录。
3. **生产口径不动**：N=5 积分账本 56.5–57.0 µs 落在锚点算术之下
   （PIC 家族解释），增长项在生产规模是 ~0.35 µs 级噪声——R16-A 起
   沿用的「锚点 × 5」算术在生产口径继续成立且略保守。
4. **契约回声闭环**：F3a–F3d 把账本态下的幂等/seq/growth 契约钉为
   分布级证人——seq 严格 0..N-1、追加恰 1|3、重放对任意深度终表
   no-op 且按引用返回、episode 绑定在深表上稳定。这是幂等键
   （hash:seq）设计在**多 gate 累积态**下的首次分布级验证（此前
   R19-A 只验单 gate 重放）。
5. **不产生候选**：seq/status 携带 = X0-4 字面本体（§0-1）；批量
   结算 = X1-2 + 时序语义（§0-2）；可变摊销 = R1-A/S2-A-4/X1-1
   （§0-3）；身份 memo = X1-1（§0-4，账本态命中率恰 0）；混合感知
   索引 = X0-4 族 + X1-1（§0-5）。修正后穿越点仍距生产 ~1.8 个
   量级 ⇒ 按令空收口。

**重开条件全景更新（在 R22-A §1 表基础上新增两行、修正一行）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| M（openMinors 条数） | 0 | ≈ 4.6–5.8×10⁴ | ~4+ | R20-A |
| K（驻留链 turn 数） | 0（恒 fresh） | ≈ 3×10³ turn | ~2 | R20-A |
| H_L / H_m / H_N（humanInput 三维） | 0 | ≈ 1.2 MB / 2.2–2.3×10⁴ / 4.65–4.74×10⁴ | ~3–5 | R21-A |
| T（analysis content 操作基数） | 0（面零流量） | ≈ 1.50–1.51×10⁵ | ~5 | R22-A |
| **G（gates/run，固定 E 线性算术）** | ~5 | ≈ 645–714（本 VM 锚点口径） | ~2.1 | R16-A 系（本轮复核） |
| **G（gates/run，含 G×E 自耦合二次项）** | **~5** | **≈ 341–344** | **~1.8** | **本轮** |
| **b 混合乘数（grown 表 vs 填充表）** | **账本自生成** | **无独立穿越点（斜率成分在 R17-A 包络内）** | **—** | **本轮** |
| G 组成（apply/skip/replay 全混合） | 全 apply | 混入只降不升 | — | R18-A + R19-A |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–15.2 µs/fault | — | R18-A + R22-A |
| 重投递面（once-per-redelivery） | 0 次/run | ~2.8–3.4 µs/次 | — | R19-A + 本轮 F3d 形态 |

**可操作重开优先级不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7
家族 `every×includes` 站点 Set 化）仍是第一优先重开对象——修正后的
G 悬崖（~1.8 个量级）虽然数值上更近，但其全部摊平路被接口契约
（X0-4）与隐藏状态禁令（X1-1）封死，不构成可操作候选。本轮关闭的
是**轴间耦合维**：R14–R16 关规模/载荷维、R17 关组成/温度维、R18 关
组成/故障维、R19 关结局维、R20 关驻留状态维、R21 关输入参数维、
R22 关公开导出维之后，切片规模轴族的最后一个耦合项（E×G）自此
有价。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 5 项 + R12-A…R22-A
53 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、数据
补充或本体，按「换名不铸 ID」纪律未列任何 S23-A-\* 候选。空枚举与
R12-A…R22-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第二十三遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `gate-apply.ts` | **axis 12 主宿主**（五 O(E) 扫描 + `[...events]` 拷贝是二次项的全部承载者：b=13.96–14.20 ns/event 直测、混合乘数 1.9–2.0× 归因于 nextTrackingSeq 的 payload.seq 读与双幂等 find 第二谓词在 TA/GT 事件上的按事件价）；caller 侧 seq/status 携带 = §0-1（X0-4 字面本体）拒列；身份 memo = §0-4（X1-1，账本态命中 0）拒列；F3d 重放深表形态证人（R19-A 定价维持）；S11-A-2 / S8-A-1 / S2-A-4 / R15-A §0-1 维持 | 无新候选 |
| `child-tracking.ts` | **账本驱动入口**（applyChildThreeLine 每子结果一次——批量结算 = §0-2（X1-2 + 时序语义）拒列）；guard-3 skip 抽测 2033–2094 ns 复现（R18-A 7a 维持）；skip 守卫次序/享元 = R18-A §0-1/2 维持；nextTrackingSeq 重算 = §0-1 拒列 | 无新候选 |
| `turn.ts` | 本轮整体 1.70–1.73 µs 同带（axis 12 不改 turn 面——账本增长全部落在事件表侧，turn 输入恒 fresh window）；R20-A K 链（window.previous 驻留）与 axis 12（事件表积分）正交性确认；R13-A 八子组件 / R12-A §0-11 / R16-A §0-2 维持 | 无新候选 |
| `from-child.ts` | 账本每 gate 经 assessChildObservation 全程走生产路径（humanInput 恒 `{}` 第十四次复验）；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `types.ts` | hashAssessment×3 地板复现 5.77–5.81 µs（账本态下每 gate 同价——hash 对表深不敏感，唯一表深敏感组件恰是五扫描）；写侧 parse 312–364 ns；X1-1/S8-G-2 memo 禁维持；S2-A-5 / X1-2 / S5-H-3 / S1-A-8 / X0-5 维持 | 无新候选 |
| `gates.ts` | 账本 FAILED 腿恒 deterministic-fail 硬门（F3a 16/16 GT 证人）；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 维持 | 无新候选 |
| `prescore.ts` | 本轮零新角度（axis 12 不触 prescore；账本每 gate prescore×2 同价）；C/R 轴维持 R15-A 穿越点（可操作重开第一优先不变）；S1-A-4 / X4-7 / S6-A-1 / S8-A-3 / R12-A §0-2..5 维持 | 无新候选 |
| `roller.ts` | 账本每 gate fresh window ⇒ rollSummary 无 previous 腿（R20-A previous 面维持不重测）；R17-A O(Q·D) / S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（账本全程 human ≡ unobserved）；R21-A H 面轴不补铸；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 维持 | 无新候选 |
| `combined-score.ts` | 本轮零新角度；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |
| `config.ts` | 配置态矩阵复核宿主（§0 配置态 (b)：生产恒 DEFAULT；R22-A 双臂价维持）；X0-7 阈值冻结维持 | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方 grep 复验；不重测）；S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R22-A 隔离三函数价维持（零 src/ 调用方复验）；恒等函数删除 = R22-A §0-5 拒列维持；index.ts 纯再导出零逻辑；惰性 import = R12-A §0-10 维持 | 无新候选 |

**对 R22-A 声明的裁决**：公开导出普查（30/30 导出全价）在其维度上
成立；R22-A 结语「生产管线维度格网与公开导出格网双双闭合」量化的是
**单轴与单面**——轴与轴之间的耦合项（表由 gate 自身生成的反馈回路）
不在其中，本轮补上并闭合（E×G 二次项 + 混合乘数 + 修正穿越点）。
本轮全部新数据**只产出归档证人与 G 轴穿越点的数据修正，未产出可落地
候选**。收口的预言力第二十三遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R22-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F3a/F3b/F3c: N=64 mixed m=4 账本逐字节可复现；TA seq 0..63；16 FAILED
    ⇒ 16 GT + 16 RUN_BLOCKED；追加恰 1|3；终表 393 = 41+256+96；
    nextTrackingSeq(表@k) ≡ k；终态 BLOCKED；all-PASSED 账本干净
F3d: 40 个种子化重放全部 no-op、按引用返回、directive 保持
anchor: 16.0 / 14.0 / 15.4 / 14.5 / 15.2 µs/gate => ~70–80 µs/run
decomposition: hash×3=5771ns | validateEvent=2994ns | turn=1731ns
    | prescore×2=1314ns | 5 scans+copy=295ns (7.20 ns/event)
    | applyTrackingGate=6.41µs
spot: skip guard-3 2043ns (14.6%) | replay find#1 3350ns (24.0%)
axis12 depth: E=41/591/1141/2241: 12.30/19.92/27.51/43.41 µs
    => b = 14.14 ns/event
axis12 ledger m=4: N=5/25/100/400: 0.061/0.321/1.724/12.736 ms
axis12 ledger m=0 N=400: 7.138 ms
axis12 production cell: 57.0µs vs arithmetic 69.8µs (delta −12.8)
axis12 fit: 12.30·N + 0.0488·N² | predicted c 0.0389
axis12 crossings: quad 344 vs linear 813
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F3a–F3d: 计数逐位同 run 1
anchor: 15.4 / 14.3 / 15.3 / 14.6 / 15.2 µs/gate => ~71–77 µs/run
decomposition: hash×3=5778ns | validateEvent=3062ns | turn=1713ns
    | prescore×2=1323ns | 5 scans+copy=298ns (7.26 ns/event)
    | applyTrackingGate=6.42µs
spot: skip guard-3 2094ns (14.7%) | replay find#1 3096ns (21.7%)
axis12 depth: 12.30/19.87/27.64/43.54 µs => b = 14.20 ns/event
axis12 ledger m=4: 0.060/0.321/1.749/12.917 ms
axis12 ledger m=0 N=400: 7.446 ms
axis12 production cell: 56.9µs vs arithmetic 71.3µs (delta −14.4)
axis12 fit: 12.47·N + 0.0496·N² | predicted c 0.0390
axis12 crossings: quad 341 vs linear 802
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F3a–F3d: 计数逐位同 run 1
anchor: 16.4 / 15.5 / 15.0 / 14.4 / 15.1 µs/gate => ~72–82 µs/run
decomposition: hash×3=5806ns | validateEvent=3003ns | turn=1695ns
    | prescore×2=1332ns | 5 scans+copy=297ns (7.24 ns/event)
    | applyTrackingGate=6.44µs
spot: skip guard-3 2033ns (13.1%) | replay find#1 3140ns (20.2%)
axis12 depth: 13.15/20.94/27.66/43.85 µs => b = 13.96 ns/event
axis12 ledger m=4: 0.060/0.318/1.752/12.782 ms
axis12 ledger m=0 N=400: 7.798 ms
axis12 production cell: 56.5µs vs arithmetic 77.6µs (delta −21.2)
axis12 fit: 12.59·N + 0.0484·N² | predicted c 0.0384
axis12 crossings: quad 343 vs linear 794
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立仿真运行 F3a–F3d 计数逐位一致（账本逐字节复现、seq/growth/
重放契约确定性重现）；锚点五格三次同带无悬崖；axis 12 四格深度单价 +
四格积分账本 + 一格 m=0 变体 + 一格生产规模 + 拟合/穿越点三次运行
区间紧（b 区间宽 0.24 ns/event、c 区间宽 0.0012 µs、修正穿越点区间
宽 3 gates）。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S23-A-\*）。换名检查 5 项新记录见 §0（1–5），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件本轮**新增一条收口**：

**G×E 自耦合（积分账本）收口**：事件表由 gate 自身生成的反馈回路
自此有价——真实 grown mixed 表上 b = 13.96–14.20 ns/event（填充表的
1.9–2.0×，R17-A 包络内）、二次系数 0.0484–0.0496 µs（模型 b·(g+m)/2
解释 ~79–81%，余为混合占比漂移的超二次尾巴）、10 ms 穿越点修正为
N ≈ 341–344 gates/run（固定 E 线性算术 645–813 的 ~2.3–2.4× 压低）、
生产规模（N=5）下增长项 ~0.35 µs 为噪声且积分实测落在锚点算术之下。
重开条件：(a) 生产每 run 子结果数增长 ≥1 个量级（届时先撞 §0-1
X0-4 字面本体与 §0-2 时序语义，二次项本体仍不可摊平），或 (b) 事件
表契约放弃不可变数组形态（R1-A 拷贝下界随之重审——非本战役事项），
或 (c) run 内出现每 gate 事件增长 ≥10² 的新事件类（届时 m 主导，
先重测 R17-A 组成包络）。**可操作重开第一优先不变**：C 轴
（~2.5 个量级，R15-A；S1-A-4/X4-7 家族 `every×includes` 站点
Set 化）——修正后 G 悬崖（~1.8 个量级）数值上更近但摊平路全部被
X0-4/X1-2/R1-A/X1-1 封死，仅作预算天花板精化记录。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf23a01`–`0xf23a02`。

```ts
/**
 * R23-A deterministic anchor + decomposition + NEW-axis simulation
 * (twenty-third pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R22-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R22-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation, 5-scan
 *       O(E) inventory), plus compact spot re-confirmations of the R18-A
 *       skip pricing (guard-3) and the R19-A replay pricing (find#1) for
 *       composition-ceiling continuity;
 *   (c) NEW axis 12: the G x E SELF-COUPLING run ledger. Twenty-two
 *       passes priced E (event-table size) at a FIXED gate (R14-A) and
 *       the G (gates/run) crossing by FIXED-E anchor arithmetic
 *       (R16-A: crossing ~= 10ms / per-gate-cost at E=41 ~= 654-763),
 *       but the coupling between them was never priced: every applied
 *       gate APPENDS 1-3 events to the very table the NEXT gate must
 *       rescan (five O(E) scans) and re-copy ([...events]), so the true
 *       integrated cost of an N-gate run is quadratic in N:
 *         total(N) ~= a*N + b*(g+m)/2 * N^2
 *       where a is the fixed-table per-gate cost, b the per-event slope
 *       of the O(E) inventory (~7 ns/event at E=41 per R12-A..R22-A),
 *       g the self-generated growth (1 event for PASSED/none, 3 for
 *       FAILED/blocked), and m the ambient per-gate growth (other run
 *       events, e.g. CHILD_MESSAGE). The R16-A G crossing implicitly
 *       assumed c=0. This axis:
 *       (i)   builds a deterministic integrated ledger driver: N
 *             sequential applyChildThreeLine gates over a table that
 *             actually grows by each gate's appended events plus m
 *             ambient messages per gate;
 *       (ii)  proves the ledger's determinism and growth accounting
 *             distribution-level (F3a/F3b): byte-identical final tables
 *             across replays, seq strictly 0..N-1, appended-per-gate
 *             exactly 1 (PASSED->none) or 3 (FAILED->blocked), final
 *             length exactly E0 + N*m + sum(appended);
 *       (iii) proves seq/status invariants at depth (F3c) and replay
 *             idempotency at depth (F3d: re-issuing an earlier gate
 *             command against the fully grown table is a no-op that
 *             returns the input array by reference -- R19-A shape
 *             witness at grown-table state, NOT a re-pricing);
 *       (iv)  prices the axis: gate-at-depth cells (one fresh gate on
 *             real grown ledger tables at four depths -> direct b),
 *             integrated ledger cells N in {5,25,100,400} at m=4
 *             (production texture ~= 41-event table has ~39 ambient
 *             messages over ~5 gates) plus an m=0 pure-self-coupling
 *             variant, a production-scale N=5 all-PASSED cell vs the
 *             anchor arithmetic (5 x fixed-E cell), a least-squares
 *             a/c fit, the predicted-vs-fitted quadratic coefficient,
 *             and the corrected 10 ms crossing vs the R16-A arithmetic
 *             crossing.
 *
 * Archival-fact fuzz:
 *   F3a: ledger determinism. Two independent builds of the same seeded
 *        ledger are byte-identical; TA count = N; GT/RUN_BLOCKED counts
 *        match the FAILED count; TA seq values are exactly 0..N-1 in
 *        order.
 *   F3b: growth accounting. Per-gate appended = 1 + 2*[directive != none];
 *        final length = E0 + N*m + sum(appended).
 *   F3c: seq/status invariants at depth. At every gate k,
 *        nextTrackingSeq(table) === k; final replayed status is BLOCKED
 *        iff the ledger contains a FAILED gate, else RUNNING.
 *   F3d: replay idempotency at depth. Earlier gate commands re-issued at
 *        the final grown table return applied=false with the original
 *        directive and the events array by reference (no copy).
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf23a01..0xf23a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary = opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran";
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

function childMessage(id: EventId, k: number): Event {
  return {
    id,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "CHILD_MESSAGE",
    actor: "child",
    payload: { taskId: `tsk_amb_${k % 5}`, content: "..." }
  } as unknown as Event;
}

function gateCommandFor(
  kind: "PASSED" | "FAILED",
  table: readonly Event[],
  opts?: { taskId?: string }
): { assessment: TrackingAssessment; hash: string; seq: number } {
  const mk = mkChild(kind, { taskId: opts?.taskId });
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const decision = assessChildObservation({
    observation: obs,
    episodeId: "ep_bench",
    runId: String(RUN_ID)
  });
  if (!decision.apply) throw new Error("fixture must apply");
  return {
    assessment: decision.assessment,
    hash: hashAssessment(decision.assessment),
    seq: nextTrackingSeq(table)
  };
}

/* ============================================================
 * NEW axis 12 ledger driver: N sequential gates over a table that
 * grows by each gate's appended events + m ambient messages/gate.
 * kindAt(k): deterministic mixed sequence (25% FAILED) or all-PASSED.
 * Returns the final table, per-gate append counts, per-gate seq
 * observations, and the summed slice time (gate calls only; the
 * ambient concat is untimed fixture plumbing when timed=true).
 * ============================================================ */
interface LedgerOutcome {
  events: readonly Event[];
  appended: number[];
  seqs: number[];
  directives: string[];
  gateUs: number;
  commands: Array<{ assessment: TrackingAssessment; hash: string; seq: number; directive: string }>;
}

function runLedger(opts: {
  N: number;
  ambient: number;
  mix: "mixed" | "all-passed";
  timed: boolean;
  keepCommands?: boolean;
}): LedgerOutcome {
  let events: readonly Event[] = events41;
  let idc = 1000000;
  const gen = (): EventId => `evt_${String(idc++).padStart(8, "0")}` as EventId;
  const appended: number[] = [];
  const seqs: number[] = [];
  const directives: string[] = [];
  const commands: LedgerOutcome["commands"] = [];
  let gateUs = 0;
  for (let k = 0; k < opts.N; k += 1) {
    if (opts.ambient > 0) {
      const amb: Event[] = [];
      for (let j = 0; j < opts.ambient; j += 1) amb.push(childMessage(gen(), k));
      events = [...events, ...amb];
    }
    seqs.push(nextTrackingSeq(events));
    const kind = opts.mix === "mixed" && k % 4 === 3 ? ("FAILED" as const) : ("PASSED" as const);
    const mk = mkChild(kind, { taskId: `tsk_ledger_${k}` });
    const before = events.length;
    let out: ReturnType<typeof applyChildThreeLine>;
    if (opts.timed) {
      const t0 = performance.now();
      out = applyChildThreeLine({
        events,
        child: mk.child,
        spec: mk.spec,
        contract: mk.contract,
        nowIso: NOW_ISO,
        generateEventId: gen
      });
      gateUs += (performance.now() - t0) * 1e3;
    } else {
      out = applyChildThreeLine({
        events,
        child: mk.child,
        spec: mk.spec,
        contract: mk.contract,
        nowIso: NOW_ISO,
        generateEventId: gen
      });
    }
    if (!out.result.applied) throw new Error(`ledger gate ${k} must apply`);
    appended.push(out.events.length - before);
    directives.push(out.result.directive);
    if (opts.keepCommands === true) {
      const ta = out.events[before] as unknown as {
        payload: { assessment: TrackingAssessment; assessmentHash: string; seq: number };
      };
      commands.push({
        assessment: ta.payload.assessment,
        hash: ta.payload.assessmentHash,
        seq: ta.payload.seq,
        directive: out.result.directive
      });
    }
    events = out.events;
  }
  return { events, appended, seqs, directives, gateUs, commands };
}

/* ============================================================
 * F3a/F3b/F3c: ledger determinism + growth accounting + seq/status
 * invariants at depth (N=64 mixed, m=4; deterministic, replayed twice).
 * ============================================================ */
{
  const a = runLedger({ N: 64, ambient: 4, mix: "mixed", timed: false, keepCommands: true });
  const b = runLedger({ N: 64, ambient: 4, mix: "mixed", timed: false, keepCommands: true });
  check(
    "F3a ledger byte-identical across replays",
    JSON.stringify(a.events) === JSON.stringify(b.events)
  );
  const taSeqs = a.events
    .filter((event) => event.type === "TRACKING_ASSESSMENT")
    .map((event) => (event as unknown as { payload: { seq: number } }).payload.seq);
  check("F3a TA count = N", taSeqs.length === 64, String(taSeqs.length));
  check(
    "F3a TA seq exactly 0..N-1 in order",
    taSeqs.every((seq, index) => seq === index)
  );
  const failedCount = a.directives.filter((d) => d === "queue_analysis").length;
  const gtCount = a.events.filter((event) => event.type === "GATE_TRANSITION").length;
  const blockedCount = a.events.filter((event) => event.type === "RUN_BLOCKED").length;
  check("F3a GT count = FAILED count", gtCount === failedCount, `${gtCount} vs ${failedCount}`);
  check("F3a RUN_BLOCKED count = FAILED count", blockedCount === failedCount);
  check("F3a mixed ledger has 16 FAILED gates", failedCount === 16, String(failedCount));

  let growthOk = true;
  for (let k = 0; k < 64; k += 1) {
    const expected = a.directives[k] === "none" ? 1 : 3;
    if (a.appended[k] !== expected) growthOk = false;
  }
  check("F3b appended per gate = 1 (none) | 3 (blocked)", growthOk);
  const sumAppended = a.appended.reduce((acc, item) => acc + item, 0);
  check(
    "F3b final length = E0 + N*m + sum(appended)",
    a.events.length === 41 + 64 * 4 + sumAppended,
    `${a.events.length} vs ${41 + 64 * 4 + sumAppended}`
  );

  check(
    "F3c nextTrackingSeq(table before gate k) === k for every k",
    a.seqs.every((seq, index) => seq === index)
  );
  const finalStatus = ((): string => {
    let status = "RUNNING";
    for (const event of a.events) {
      if (event.type === "GATE_TRANSITION")
        status = (event as unknown as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    return status;
  })();
  check("F3c final status BLOCKED (ledger contains FAILED gates)", finalStatus === "BLOCKED");
  const allPassed = runLedger({ N: 16, ambient: 4, mix: "all-passed", timed: false });
  const allPassedStatus = allPassed.events.some(
    (event) => event.type === "RUN_BLOCKED" || event.type === "GATE_TRANSITION"
  );
  check("F3c all-PASSED ledger appends no transition/blocked events", allPassedStatus === false);
  check(
    "F3c episode binding stable at depth",
    episodeIdFromEvents(a.events) === "ep_bench"
  );
  console.log(
    `F3a/F3b/F3c: N=64 mixed m=4 ledger deterministic (byte-identical replay); ` +
      `TA seq 0..63 in order; 16 FAILED gates -> 16 GT + 16 RUN_BLOCKED; per-gate append ` +
      `exactly 1|3; final length ${a.events.length} = 41 + 256 + ${sumAppended}; ` +
      `nextTrackingSeq(table@k) === k for all k; final status BLOCKED; all-PASSED ledger clean ` +
      `-- the self-coupling driver is exact`
  );

  /* F3d: replay idempotency at depth (R19-A shape witness, no re-pricing). */
  const rng = mulberry32(0xf23a01);
  let replayChecks = 0;
  for (let trial = 0; trial < 40; trial += 1) {
    const idx = Math.floor(rng() * a.commands.length);
    const cmd = a.commands[idx]!;
    const out = applyTrackingGate({
      events: a.events,
      assessment: cmd.assessment,
      assessmentHash: cmd.hash,
      expectedSeq: cmd.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => {
        throw new Error("replay must not generate ids");
      }
    });
    check(
      `F3d replay at depth is a no-op [trial ${trial}]`,
      out.result.applied === false && out.events === a.events
    );
    if (cmd.directive !== "none") {
      check(
        `F3d replay directive preserved [trial ${trial}]`,
        out.result.directive === cmd.directive
      );
    }
    replayChecks += 1;
  }
  console.log(
    `F3d: ${replayChecks} seeded replays of earlier gate commands against the fully grown ` +
      `table (E=${a.events.length}) all return applied=false with the input array by reference ` +
      `and the original directive -- idempotency holds at every depth (shape witness; ` +
      `pricing stays with R19-A)`
  );
  void mulberry32(0xf23a02);
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R22-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    anchorUs.push(one * 1e3);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition of the PASSED x contract(3) cell
 * (R12-A..R22-A methodology, 5-scan O(E) inventory) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
let fiveScanSlopeNsPerEvent = Number.NaN;
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  fiveScanSlopeNsPerEvent = (scans * 1e6) / 41;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)}) => ~${fiveScanSlopeNsPerEvent.toFixed(2)} ns/event at E=41`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const cmdGT = gateCommandFor("FAILED", events41, { taskId: "tsk_replay_gt" });
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: cmdGT.assessment,
      assessmentHash: cmdGT.hash,
      expectedSeq: cmdGT.seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: cmdGT.assessment,
        assessmentHash: cmdGT.hash,
        expectedSeq: cmdGT.seq,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }
}

/* ============================================================
 * NEW axis 12: the G x E self-coupling run ledger.
 * ============================================================ */
{
  console.log("--- NEW axis 12: gate-at-depth cells (one fresh gate on real grown tables) ---");
  // real grown mixed m=4 ledger snapshots at four depths
  const depths = [0, 100, 200, 400] as const;
  const depthTables: Array<{ k: number; table: readonly Event[] }> = [];
  for (const k of depths) {
    const ledger = runLedger({ N: k, ambient: 4, mix: "mixed", timed: false });
    depthTables.push({ k, table: ledger.events });
  }
  const depthUs: Array<{ E: number; us: number }> = [];
  for (const { k, table } of depthTables) {
    const mk = mkChild("PASSED", { taskId: `tsk_depth_${k}` });
    let applied = false;
    const reps = table.length > 1500 ? 2000 : 5000;
    const one = bench(() => {
      let c = 5000000;
      applied = applyChildThreeLine({
        events: table,
        child: mk.child,
        spec: mk.spec,
        contract: mk.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, reps);
    check(`gate-at-depth applies [k=${k}]`, applied);
    depthUs.push({ E: table.length, us: one * 1e3 });
    console.log(
      `  gate at depth k=${String(k).padStart(3)} (E=${String(table.length).padStart(4)}): ${(one * 1e3).toFixed(2)}us/gate`
    );
  }
  const first = depthUs[0]!;
  const last = depthUs[depthUs.length - 1]!;
  const bNsPerEvent = ((last.us - first.us) * 1e3) / (last.E - first.E);
  console.log(
    `  => direct per-event slope b ~= ${bNsPerEvent.toFixed(2)} ns/event on grown mixed tables ` +
      `(five-scan inventory at E=41 predicts ~${fiveScanSlopeNsPerEvent.toFixed(2)} ns/event)`
  );

  console.log("--- NEW axis 12: integrated ledger cells (timed gate calls only) ---");
  const ledgerCells = [
    { N: 5, reps: 400 },
    { N: 25, reps: 80 },
    { N: 100, reps: 20 },
    { N: 400, reps: 5 }
  ] as const;
  const totals: Array<{ N: number; us: number }> = [];
  for (const cell of ledgerCells) {
    const samples: number[] = [];
    for (let rep = 0; rep < cell.reps; rep += 1) {
      samples.push(runLedger({ N: cell.N, ambient: 4, mix: "mixed", timed: true }).gateUs);
    }
    samples.sort((left, right) => left - right);
    const median = samples[Math.floor(samples.length / 2)]!;
    totals.push({ N: cell.N, us: median });
    console.log(
      `  ledger N=${String(cell.N).padStart(3)} m=4 mixed: total gate time ${(median / 1e3).toFixed(3)}ms (median of ${cell.reps}) => ${(median / cell.N).toFixed(2)}us/gate avg`
    );
  }
  // m=0 pure self-coupling variant at N=400
  {
    const samples: number[] = [];
    for (let rep = 0; rep < 5; rep += 1) {
      samples.push(runLedger({ N: 400, ambient: 0, mix: "mixed", timed: true }).gateUs);
    }
    samples.sort((left, right) => left - right);
    const median = samples[2]!;
    console.log(
      `  ledger N=400 m=0 mixed (pure self-coupling): total ${(median / 1e3).toFixed(3)}ms => ${(median / 400).toFixed(2)}us/gate avg`
    );
  }
  // production-scale cell: N=5 all-PASSED m=4 vs anchor arithmetic
  {
    const samples: number[] = [];
    for (let rep = 0; rep < 400; rep += 1) {
      samples.push(runLedger({ N: 5, ambient: 4, mix: "all-passed", timed: true }).gateUs);
    }
    samples.sort((left, right) => left - right);
    const median = samples[200]!;
    const arithmetic = (anchorUs[1] ?? Number.NaN) * 5;
    console.log(
      `  production cell: N=5 all-PASSED m=4 integrated = ${median.toFixed(1)}us vs anchor arithmetic 5 x PASSED-contract = ${arithmetic.toFixed(1)}us (delta ${(median - arithmetic).toFixed(1)}us)`
    );
  }
  // least-squares fit total(N) = a*N + c*N^2 (zero intercept)
  {
    let s22 = 0;
    let s24 = 0;
    let sy2 = 0;
    let sy3 = 0;
    for (const point of totals) {
      const n = point.N;
      s22 += n * n;
      s24 += n * n * n * n;
      sy2 += point.us * n;
      sy3 += point.us * n * n;
    }
    let s23 = 0;
    for (const point of totals) s23 += point.N * point.N * point.N;
    const det = s22 * s24 - s23 * s23;
    const aFit = (sy2 * s24 - sy3 * s23) / det;
    const cFit = (s22 * sy3 - s23 * sy2) / det;
    const predictedC = (bNsPerEvent / 1e3) * (1.5 + 4) * 0.5; // b*(g+m)/2 in us
    // corrected 10 ms crossing: a*N + c*N^2 = 10000 us
    const crossQuad = (-aFit + Math.sqrt(aFit * aFit + 4 * cFit * 10000)) / (2 * cFit);
    const crossLinear = 10000 / aFit;
    console.log(
      `  fit: total(N) = ${aFit.toFixed(2)}*N + ${cFit.toFixed(4)}*N^2 us | predicted c = b*(g+m)/2 = ${predictedC.toFixed(4)} us (g_avg=1.5, m=4)`
    );
    console.log(
      `  => corrected 10ms crossing N ~= ${Math.round(crossQuad)} gates/run vs fixed-E arithmetic crossing N ~= ${Math.round(crossLinear)} (R16-A lineage; production N ~= 5)`
    );
    console.log(
      "  => the self-coupling is real but second-order at every non-fantasy scale: the quadratic " +
        "term is invisible at production N~=5, bends the crossing only by a modest fraction at " +
        "N~=10^2-10^3, and every path to exploiting it (caller-side seq/status carry, ledger " +
        "batching, mutable append, table-identity memo) lands on X0-4 / X1-2 / R1-A copy floor / " +
        "X1-1 -- no candidate"
    );
  }
}

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
