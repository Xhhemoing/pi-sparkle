MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 23 / R23-C：离线路由切片第二十三遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 23 / R23-C（Round 1–22 同区第二十三遍，叠在
  已落地的 S7-C 之上）
- **基线:** `cursor/sota-persistent-opt-83a1` @ `abb65d5`（R23-A 收口 +
  R23-C 派单后的独占分支最新 tip。相对 R22-C 基线 `4fd9969` 的区间为
  **纯文档合入**（R22-B..R22-I / R23-A 空枚举收口与 Round 22/23 派单——
  `git log 4fd9969..HEAD --name-only` 共 9 个 docs(sota-opt) 提交，全部
  仅触 `docs/reports/sota-opt/`，零 `src/` 触碰）。`git log -1 --
  src/routing/` 显示切片最后一次生产改动**仍为 `183df9b`「perf(routing):
  land S7-C support-only eta」**；`git diff 183df9b..HEAD --` 对 9 个切片
  文件为**空**（stat 输出为空、exit 0），本轮全部测量在该形态之上，未回退
  未重写。R22-J / R23-B 在飞，未等待、未触碰其分支与文件）
- **分支:** `cursor/r23-c-offline-routing-twenty-third-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第二十三组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第二十三遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R22-C 的全部无号关闭
探针与轴，含 R22-C 的 SEEDX、R21-C 的 YMIX、R20-C 的 SCALEX、R19-C 的
FITQ、R18-C 的 TAILG/REJX 与 R17-C 的 NSQRT/PMV/OSTZ）后，本轮**从未
点名站点数为零**——R18-C「TAILG 是尾部段最后一个从未点名位置」的判定
经 R19-C、R20-C、R21-C、R22-C 与本轮连续五遍逐行核实成立：每一个执行
站点都能指到既有 ID、无号探针、零池备忘或池算术覆盖。本轮新增**一条
完备性轴**（C 区剂量响应方法线的第四个输入维度——SCALEX 动体量、
YMIX 动结局组成、SEEDX 动重采样身份之后，输入行的呈现次序本身）并按
规执行**一次地板重锚定**，全部以轴/地板测量（不铸号）收口——没有任何
候选道达到接受门槛：

- **ORDX（输入呈现次序 / 行置换剂量响应，新轴，非候选；R19-J J 区输入
  次序置换面、R14-A/D/E 剂量档家族、C 区剂量响应方法线 SCALEX（体量）/
  YMIX（组成）/ SEEDX（重采样身份）先例，C 区首次）**——历轮 C 区全部
  口径（含 SCALEX 的所有尺寸剂量点、YMIX 的所有组成剂量点、SEEDX 的
  所有种子剂量点）都冻结在 fixtureRng(0xbeef) 发射次序一种呈现次序上；
  「同一行多重集的置换能否打开任何按体量/组成/重采样身份关闭的裁决」
  此前从未被测过。ORDX 冻结数据多重集、体量、组成与 bootstrap 种子
  （20260818 锚点——SEEDX 未重测），只动 rows 数组的次序：
  order ∈ {anchor 发射序, reversed, sorted（按 scenario/model/project/y
  分组序）, shuffle(0xaaaa), shuffle(0x5555)}；每剂量断言**多重集逐位
  同一**（规范序列化相等）且 **p=60 与 distinctKeys=167 置换不变**
  （层级数与配对计数是多重集性质，构造性证明 + 每剂量断言）；每剂量点
  仪表道先对生产导入**该剂量逐位保真**再采信读数。结果（三次正典
  运行）：**次序只通过 maxIter 封顶的迭代计数进入唯二冻结池**——迭代
  8,966 / 7,984 / 9,116 / 8,601 / 8,616（三运行逐位一致；锚点非极值，
  reversed 反而最廉 ×0.891、sorted 最贵 ×1.017——次序→迭代映射数据
  依赖，不是可操纵杠杆），solve 每迭代单价 50.3–51.6 µs（单轮内摆幅
  0.5–2.6%）、irls-other 16.8–17.9 µs（2.0–5.8%）全剂量持平；**四个
  亚带池与次序在结构上封顶**：successful=200 / collapsed=0 /
  singular=0 **五剂量三运行全同**（任何呈现次序都不触发塌缩/奇异
  分支）⇒ onProb sigmoid 数 = 400×(1+successful) = **80,400 五剂量
  恒等**（每剂量断言）、尾部 draws-map ops = (p+1)×successful =
  **12,200 五剂量恒等**（p 置换不变 ⇒ 计数恒等，每剂量断言）、采样环
  400×200 与次序无关；APC 活跃对随参照层位移微摆 273,084–281,768
  （−1.6%~+1.5%），结构封顶 321,600 = 最坏剂量的 ×1.18，且 APC 闭合
  系地板锚定非计数（§3）。结构上界：201 拟合 × maxIter 50 = **10,050
  迭代封顶**，最坏剂量 9,116 仅达封顶的 **90.7%**——次序轴对总量的
  最大可能放大 = ×1.121（vs 锚点 8,966，与 YMIX/SEEDX 同一封顶算术），
  而亚带池越带需 ×4.4–×22.9：**呈现次序维度被 maxIter 封顶 + 计数
  恒等双重结构性关死，任何行置换都无法把任何池推越 35 ms 带**。唯一
  道形候选「fit 内部规范化重排行序」三重拒绝：X'WX/X'Wz 累加是行序
  浮点加法，任何内部重排即逐位违例（R11-C RID/CNT 归约重排反例 +
  X2-3 同族），且 buildDesign 首现层序决定参照层——重排改变全部下游
  报告值与 effects 次序（公开输出面）；即便两者都不管，方向也数据
  依赖（本夹具 reversed 省 11% 迭代、sorted 贵 1.7%，无先验可控性；
  为找廉价次序而搜索需要重跑拟合，自败）。轴一次性收口，非候选。
- **APC 池地板重锚定（按 R14-C 处置规则，非候选、非重开、非堆叠）**
  ——本轮三次仪表化 APC 池读数 **46.2 / 46.4 / 48.0 ms**，run A 的
  46.2 **漂出 R22-C 重锚定带 46.4–47.0 的下沿**、run C 的 48.0
  **漂出上沿**（R14-C 规则对双向越带均触发；R14-C/R18-C/R21-C 即
  上漂触发例、R17-C/R19-C/R20-C/R22-C 即下漂触发例——本轮为首次
  单轮内双向越带，处置相同），按规随池重锚地板：APCX 无计时器独立道
  **55.8 / 53.8 / 55.4 ms**，FLOORX 最廉价逐位合法形态（S8-C-2 型
  支撑升序 off 和——仅作地板测量，S8-C-2/S10-C-2/S2-C-4/S4-C-4 全部
  维持关闭）地板 **40.0 / 39.2 / 39.4 ms** ⇒ **组合天花板 = 池 −
  地板 = 15.8 / 14.6 / 16.0 ms < 35**，与 R16-C 直测带 12.1–23.8 及
  R17-C..R22-C 六带全部交叠。**sink=7.309 与 floorSink=7.309 六读数
  全同，且与 R14-C/R16-C/R17-C/R18-C/R19-C/R20-C/R21-C/R22-C 逐位
  同值**（派单强制的跨轮确定性旁证成立）。池双向摆动，组合关闭维持。

除上述轴与地板重锚外，第二十三遍逐行重读**零新发现**：其余每一个
执行站点都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 /
结构交换负优化）+ R18-C 补充的 once-per-fault 拒绝类。三大池地板与
组合天花板复核见 §3：本 VM 上生产中位 **660.7–665.4 ms/报告（三次
正典运行 660.7 / 663.3 / 665.4）**，全部嵌在派单 R14-C 锚定带
657.6–689.3 之内、与 R21-C 带 661.9–674.2 交叠；run A 660.7 低于
R22-C 带下沿 665.7 约 5.0 ms（0.75%），按派单口径随读重锚为本轮带
660.7–665.4（同 VM 独立旁证：r7c 回归资产锚点 **production 658.4 ms
= 1.12× vs 冻结 S6-C ctl** 与中位带差 2–7 ms 同带互证）；判为 VM
微架构漂移带内往返，非切片回归或加速（切片对 `183df9b` 零 diff）。
小形状档整报告中位 **32.0–32.6 ms**，整条报告低于噪声带（R7-I 矩阵
义务履行）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11），live 面零文件改动。仓库变更仅本报告一个文件；无生产赢家故
未提交 `scripts/round23-r23c-equivalence-sim.ts`（败者/锚点仿真全文进
附录，遵守 R7-C..R22-C 先例）。

## 0. 范围与约束遵守

- 读序（强制）已履行：README → EXCLUSIONS.md（完整表，含全部
  S\*-C-\* 行与 S12-C-1、S13-B-1）→ PROGRESS.md → round-23/PLAN.md 与
  round-22/PLAN.md → R22-C.md（空枚举确认，无 S22-C-\*；SEEDX 轴与
  APCX/FLOORX 下漂重锚定记名；「从未点名站点数为零」连续第四遍记名）→
  R21-C.md（YMIX 轴与上漂重锚定记名）→ R20-C..R11-C 探针与轴
  （SCALEX；FITQ；TAILG/REJX；NSQRT/PMV/OSTZ；ICOL/SFILL/ITERX/
  COLDX/PLSEG；STORD/RIDGE/AAFLR/GCAX；SWORD/XF64/IRLSDP/CMPH +
  APCX/TIMER/FLOORX 方法；PIVCSE/FINTAIL/REFZ/TSORT/DEG；S12-C-1 +
  FZERO/PIVSWAP/BASE/SAMP/MUCSE；RID/CNT/AUGF/EXP/ALLOC/DMAP/COPY/
  SCAN）经 EXCLUSIONS 完整表逐条核对 → 9 个切片源文件全量重读。
- 基线漂移检查：基线 `abb65d5` 相对 R22-C 基线 `4fd9969` 仅文档
  合入（`git log 4fd9969..HEAD --name-only` = 9 个 docs(sota-opt)
  提交，全部 `docs/reports/sota-opt/` 文件，零 `src/` 触碰）；
  `git log -1 -- src/routing/` 仍为 `183df9b`（S7-C 落地本身），
  `git diff 183df9b..HEAD --` 对 9 文件为空（stat 空输出、exit 0），
  落地形态原样在位。既有回归资产七连全绿且检查数逐一匹配锚定值（§5）。
- 调用图确认（派单强制项，本轮 rg 复核）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 消费（双归因两路在位）；
  `solveSymmetric` 仅被 `offline-logit.ts` 导入；`src/supervisor/`
  对 9 个切片文件的导入为**零**（`model-router.ts` 仅引
  capability-registry 类型 / catalog-model / feature-version /
  live-selection / policy）——live = R0 等价维持，R1 未接线，
  `src/supervisor/model-router.ts` 零字节未触。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S13-\*（含 S12-C-1，重开条件：无）
  未重提、未立道、未重测；R13-C 五个、R14-C 四个、R15-C 两个、
  R16-C 两个、R17-C 三个、R18-C 两个、R19-C 一个、R20-C 一个、
  R21-C 一个、R22-C 一个无名关闭（PIVCSE/FINTAIL/REFZ/TSORT/DEG/
  SWORD/XF64/IRLSDP/CMPH/STORD/RIDGE/ICOL/SFILL/NSQRT/PMV/OSTZ/
  TAILG/REJX/FITQ/SCALEX/YMIX/**SEEDX**）未重探未补铸未换址重提——
  **SCALEX 未重测、YMIX 未重测、SEEDX 未重测**（ORDX 全部剂量点都在
  冻结尺寸 (6,8,400)/B=200、冻结组成 passRate=0.6、冻结种子 20260818
  上，未触任何尺寸剂量、组成剂量或种子剂量；四轴两两正交，§4）；
  本轮测量器**不含** FITQ 逐抽取计时器；ITERX/COLDX/AAFLR/GCAX 四轴
  未补铸（ORDX 各剂量点的迭代/结局计数是**新剂量点**的确定性见证
  打印，未重测锚点次序上的收敛旗标普查本体；GC/预热轴未重测）；
  **STORD 未重提**（其对象是 S6-C 累加 switch case 体内的**语句**
  次序——代码运动；ORDX 置换的是**输入行**，零代码运动，§4）；
  R23-A G×E 自耦合 run ledger、TERMCLASS、NVG、KFAN、R22-I run-record
  回读平面均未移植（派单点名禁项，宿主与机制均在他区）；RID/CNT
  归约重排反例原样引用（并作为 ORDX 唯一道形的直接否决依据之一）；
  嵌套序 × 布局组合空间（S4-C-2 + S5-C-1..4 + S12-C-1）未重开；
  CMB 组合未重堆叠（本轮 APCX/FLOORX 重测遵循 R14-C 池漂出带处置
  规则，是地板测量非堆叠候选；ORDX 各剂量点只测落地形态一条道，
  零变体决斗）；PLSEG/PMV/TAILG 备忘未重提。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动；X2-3 数值路径未触；单线程逐位审计基底不动。
- 环境：Node 22.22.2（本 VM 默认 22.14.0 低于 engines ≥22.19.0，
  nvm 切换并前置 PATH——回到 R1-C..R16-C/R19-C..R22-C 的处理）、
  pnpm 10.17.1、`pnpm install --frozen-lockfile`（`pnpm gate` 全绿，
  见 §5）。

## 1. 重剖析：S7-C 后成本模型（本 VM 实测，未拷贝历轮数字）

测量器为**仪表化逐字复刻管线**（与 183df9b 生产 irls/solve/on-prob/
APC 逐语句相同，仅在调用边界加 performance.now() 与计数器，浮点操作/
操作数/顺序零改动；含 R21-C 引入的每抽取结局三计数器 successful/
collapsed/singular——计数器只 +1 不进浮点路径），其忠实性先经
**53 例全形状电池 ×{仪表道, 纯净落地道} vs 生产 `fitLogitAdditive`
导入逐位全绿**（Object.is 含符号零）再采信读数，且本轮把保真检查
**扩展到 ORDX 的每一个次序剂量点**（anchor/reversed/sorted/
shuffle-aaaa/shuffle-5555 各一次逐位对照）；生产中位数由**未仪表化
生产导入**直接测得。perf 夹具与 R10-C..R22-C 完全一致
（fixtureRng(0xbeef)，4 场景 × 6 模型 × 8 项目，rows=400,
passRate=0.6，bootstrap=200, seed=20260818 → p=60，201 拟合，
8,966 次 IRLS 迭代）。

生产整报告中位（每次独立进程 7 测量取中位，三次正典运行）：**660.7 /
663.3 / 665.4 ms**——全部嵌在派单 R14-C 锚定带 657.6–689.3 之内、
与 R21-C 带 661.9–674.2 交叠（run A 660.7 低于 R22-C 带下沿 665.7
约 0.75%，双向带沿往返与 R21-C↔R22-C 的往返同型），按派单口径随读
重锚为本轮带 660.7–665.4（同 VM 独立旁证：r7c 回归资产锚点
**production 658.4 ms = 1.12× vs 冻结 S6-C ctl**，与中位带差 2–7 ms
同带互证）；判为 VM 微架构漂移带内往返，非切片回归或加速（切片对
`183df9b` 零 diff）。

仪表化池表（perf 形状，每次运行内 3 次仪表化取中位；三次正典运行
读数并列）：

| 池 | run A | run B | run C | 占比 | 站点计数（精确） |
| --- | --- | --- | --- | --- | --- |
| 总计 | 670.7 | 672.3 | 681.8 | 100% | fits=201 |
| `solveSymmetric` | **453.1** | **454.5** | **465.3** | **67.6–68.3%** | 8,966 次调用 |
| `irls` 自身（扣 solve） | **151.7** | **152.7** | **155.7** | **22.6–22.8%** | 8,966 迭代 / 3,586,400 行访问 / 1,249,652 sigmoid |
| APC 全站点 | 46.2 | 46.4 | 48.0 | 6.9–7.0% | 277,606 活跃对 / 12,259 次调用 |
| `onProbabilitiesFor` | 6.8 | 6.8 | 7.0 | 1.0% | 80,400 sigmoid+dot |
| bootstrap 采样 | 1.6 | 1.7 | 2.0 | 0.2–0.3% | —（SAMP 池 R12-C 已封顶） |
| 排序/分位尾 | 1.5 | 1.5 | 1.6 | 0.2% | 12,200 draws-map ops |
| 基行投影 | 0.005 | 0.004 | 0.009 | ~0% | 每报告 1 次（R12-C BASE 已封顶） |
| 编排余量（build/supports/keys） | 9.7 | 8.7 | 2.2 | 0.3–1.5% | —（差值余项注记见下） |
| 抽取结局计数（R21-C 起见证） | 200/0/0 | 200/0/0 | 200/0/0 | — | successful/collapsed/singular，三运行逐位一致 |

除 solve / irls / APC 外每池整池清零也低于 ±35 ms 带（R9-C..R22-C
同结论，逐池复测未漂移）；solve 与 irls 为落地线唯二宿主。全部站点
计数与历轮锚定值逐一相同（8,966 / 3,586,400 / 1,249,652 / 277,606 /
12,259 / 12,200 / 80,400 / 201——跨轮确定性旁证）。**编排余量差值
余项注记：该行 = 总计 − 各计时池之和，历轮 −0.0–11.1 的摆动本就是
差值余项的噪声属性，非池身份漂移（R12-C SCAN/COPY 归位不变）。**
**APC 池本 VM 三次读数 46.2–48.0，run A 46.2 漂出 R22-C 重锚定带
46.4–47.0 的下沿、run C 48.0 漂出上沿**——按 R14-C §3 末处置规则
随池重锚地板（§3，天花板 14.6–16.0 < 35，关闭维持且 sink/floorSink
六读数逐位同值）。小形状档（3 模型 × 3 项目 / 120 行，
bootstrap=200）整报告中位 **32.6 / 32.1 / 32.0 ms**——小配置态整条
报告低于噪声带，调用矩阵小态一侧整体收口（R7-I 矩阵义务履行）。

生产自确定性复核：3 seed × 各 2 次运行全字段 Object.is 全绿（含于
§5 的 7,344 项检查 × 3 次运行）。

## 2. 第二十三组枚举（诚实结果：空；零新站点 + 一条新轴 + 一次按规地板重锚）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过，且本轮从未点名站点数为零**（连续第五遍；探针数字为
本 VM 实测，出处为附录测量彩排三次正典运行 + 三次地板加测）：

| 站点/轴 | 关闭方式 | 证据 |
| --- | --- | --- |
| 从未点名站点普查：**零**（连续第五遍） | 逐行三问全覆盖 | 23 遍扫描把 9 文件每一个执行站点指到既有覆盖物：offline-logit 的编排头（DEG/BASE）、irls 全站点（S2-C/S3-C/S6-C/S7-C 落地体 + S10-C-1/S9-C-3/S9-C-4/S6-C-5/6/S5-C-7/S4-C-5/6/MUCSE/SWORD/STORD/RIDGE/ICOL/IRLSDP + 收敛尾 S2-C-3/S8-C-3/NSQRT + ITERX/FITQ）、solve 全站点（COPY/S4-C-3/XF64 + PIVCSE/S5-C-6 + PIVSWAP + FZERO + S4-C/S5-C 落地体 + S12-C-1 + S6-C-7 + SFILL/FINTAIL + S8-C-4）、APC（S1-C-1/S2-C-2/S2-C-4/S4-C-4/S8-C-2/S10-C-2 + FLOORX 天花板）、on-prob（S7-C-4/S8-C-1）、采样环（SAMP/S3-C-3/S2-C-5）、抽取效应环（S7-C-1/DMAP）、尾部效应环（TSORT/CMPH/REFZ/TAILG）、诊断尾（R15-C 零池注记）、posterior（X1-3/X3-4/S1-C-7/OSTZ/S1-C-4 + 已落地 memo）、prob-add（S1-C-6/PMV/PLSEG，R16-C 整报告 1.14–1.19 ms 锚定池）、propensity（S1-C-8/X3-5 遍数常数域）、r1（X1-4/S1-C-10/S1-C-2/3）、bandit/shadow（S1-C-9/X4-2）、r1-shadow-report（X1-5 + REJX 终点定价）——R18-C「TAILG 是尾部段最后一个从未点名位置」的判定经 R19-C、R20-C、R21-C、R22-C 与本遍连续五轮核实成立；规模维度经 SCALEX（R20-C）标定、组成维度经 YMIX（R21-C）、重采样身份维度经 SEEDX（R22-C）关死后本轮三轴零位移 |
| ORDX：输入呈现次序 / 行置换剂量响应（新轴，非候选；C 区首次，R19-J J 区输入次序置换面 + R14-A/D/E 剂量档家族先例，补齐 C 区剂量响应方法线的第四个输入维度：体量（SCALEX）× 组成（YMIX）× 重采样身份（SEEDX）× 呈现次序（本轮）） | 次序轴被计数恒等 + maxIter 封顶双重结构性关死 + 各剂量点逐位保真 + 唯一道形三重拒绝 | 五剂量 order ∈ {anchor 发射序, reversed, sorted, shuffle(0xaaaa), shuffle(0x5555)}，同一行多重集的置换（规范序列化相等每剂量断言；p=60 与 distinctKeys=167 置换不变每剂量断言——层级数/配对计数/去重三元组数是多重集性质）；每剂量仪表道 vs 生产导入**该剂量逐位全绿**。结构量跨 3 正典运行逐位一致：迭代 **8,966 / 7,984 / 9,116 / 8,601 / 8,616**（锚点非极值——reversed 最廉 ×0.891、sorted 最贵 ×1.017；次序→迭代映射数据依赖，不是可操纵杠杆）；诊断 model-problem **五剂量三运行全同**（参照层随次序位移但诊断稳定=正确面见证）；**successful=200 / collapsed=0 / singular=0 五剂量全零**（任何呈现次序都不触发塌缩/奇异分支）⇒ **onProb sigmoid = 400×(1+200) = 80,400 与尾部 draws-map ops = (p+1)×200 = 12,200 五剂量恒等（每剂量断言全过）**；采样环 400×200 固定与次序无关。定价：生产整报告 660.4–666.6（anchor）/ 594.9–616.6（reversed）/ 670.3–746.4（sorted）/ 635.5–646.1（shuffle-aaaa）/ 636.8–663.2（shuffle-5555）ms——摆幅与迭代比同构；**solve 每迭代单价 50.3–51.6 µs、irls-other 16.8–17.9 µs 全剂量持平（单轮内摆幅 0.5–2.6% / 2.0–5.8%）= 次序只通过 maxIter 封顶的迭代计数（加支撑构成微移）进入冻结池，无第二通道**；四亚带池与次序结构封顶且实测持平：最坏 onProb 8.0（×4.4 低于带）/ 采样 1.9（×18.1）/ 尾 1.6（×21.8）/ 余量 2.5（×14.1）；APC 描述行 50.9–54.4 ms（活跃对随参照层位移微摆 273,084–281,768 = −1.6%~+1.5%，结构封顶 321,600 = ×1.18，闭合系地板锚定非计数，剂量格仪表道毗邻读数按 R20-C/R21-C/R22-C 先例不作处置依据，处置以 §1 池表为准）。**收口算术：201 拟合 × maxIter 50 = 10,050 迭代封顶，最坏剂量 9,116 = 封顶的 90.7% ⇒ 次序轴最大可能放大 ×1.121（10,050/8,966，与 YMIX/SEEDX 同一封顶算术），亚带池越带需 ×4.4–×22.9——任何行置换都无法把任何池推越 35 ms 带**。唯一道形候选「fit 内部规范化重排行序」三重拒绝：X'WX/X'Wz 累加是行序浮点加法，内部重排即逐位违例（RID/CNT 反例 + X2-3 同族）+ buildDesign 首现层序决定参照层与 effects 次序（公开输出面，全部下游报告值改变）+ 方向数据依赖（reversed −11% 迭代 / sorted +1.7% 系本夹具偶然；搜索廉价次序需重跑拟合，自败）。重开仅当 rows 次序契约本身变更（公开签名面，不预期）或 ITERX/X2-3 关死的收敛缩短族出现逐位合法形态（无）。轴一次性收口，无候选道 |
| APCX/FLOORX 地板重锚定（非候选、非重开、非堆叠；R14-C 处置规则触发于 run A 46.2 < 带下沿 46.4 且 run C 48.0 > 带上沿 47.0——首次单轮内双向越带，处置相同） | 重锚后天花板仍亚带 + sink 逐位同值 | APCX **55.8 / 53.8 / 55.4**；FLOORX **40.0 / 39.2 / 39.4** ⇒ 天花板 **15.8 / 14.6 / 16.0 < 35**，与 R16-C 带 12.1–23.8 交叠（且与 R17-C 13.8–21.6 / R18-C 12.1–20.9 / R19-C 13.9–24.0 / R20-C 17.4–21.6 / R21-C 12.5–19.8 / R22-C 17.1–21.8 全部交叠）；**sink=7.309 与 floorSink=7.309 六读数全同，且与 R14-C/R16-C/R17-C/R18-C/R19-C/R20-C/R21-C/R22-C 逐位同值**——派单强制的 APC 关闭维持条件（sink 逐位匹配）成立；S8-C-2/S10-C-2/S2-C-4/S4-C-4 与 CMB 禁令全部维持原状 |

以上无一构成候选：从未点名普查结果为零（无新站点可立道）；ORDX
是呈现次序定价轴不是变换（各剂量点只测落地形态一条道，零变体
决斗——其唯一潜在候选族「内部规范化重排」被三重拒绝直接排除：
逐位违例 + 公开输出面 + 数据依赖自败）；APCX/FLOORX 是按规地板
测量不是变换。**故第二十三组候选集为空，不铸 S23-C-1 及后续编号**，
遵循 R11-C..R22-C 无号关闭先例。

**枚举收口：** 除上表外，第二十三遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖）。
`offline-logit.ts` 与 `lin-alg.ts` 在 R17-C NSQRT 收敛尾收口 +
R18-C TAILG 守卫收口 + R19-C..R22-C 连续零普查之后，本轮逐行核实
**不存在**任何残余的从未点名位置——尾部段五面（排序 / 比较器 /
参考水平 / map 流量 / 守卫）、收敛尾三站点（delta / isFinite /
sqrt）、回代三面（输出数组 / 死存储 / isFinite 尾扫）、消元全组合
空间（嵌套序 × 布局 × 展开粒度）、拟合粒度分布（FITQ）、规模维度
（SCALEX）、结局组成维度（YMIX）、重采样身份维度（SEEDX）、呈现
次序维度（本轮 ORDX）均已闭合。本轮核实换名再提交拒列：零——没有
任何已关 ID 的换址/换名形态在本轮出现。

## 3. 三大池地板与组合出路复核

- **solve 池 453.1–465.3 ms（本 VM 三读数；与 R22-C 带 451.2–454.0
  交叠，run C 465.3 为单轮 VM 抖动上沿）**：消元主体 ≈ 6.45 亿次
  冻结顺序浮点乘减/报告。R6-C §2.5 七类分解 + S10-C-3 + S12-C-1
  （嵌套序，稳定 ~3.6× 负优化，重开条件：无）+ R11-C COPY + R12-C
  FZERO/PIVSWAP + S5-C-6 + R13-C PIVCSE/FINTAIL + R14-C XF64 +
  R16-C SFILL 之后，本轮**零新位置**。ITERX（R16-C）坐实池量线性
  依赖 8,966 次冻结迭代，FITQ（R19-C）坐实拟合粒度无胖尾，SCALEX
  （R20-C）坐实其 K 轴 α=2.75，YMIX（R21-C）坐实其每迭代单价组成
  不变，SEEDX（R22-C）坐实其每迭代单价种子不变，ORDX（本轮）坐实
  其每迭代单价**次序不变**（50.3–51.6 µs 全剂量持平）且次序通道与
  组成/种子通道共享同一 maxIter 封顶 ×1.121——体量、组成、重采样
  身份、呈现次序四个输入维度都只会让该池占比更高，收敛缩短类无
  逐位合法形态。触碰地板本体的机制只剩 SIMD/线程（S9-C-1/2 +
  不变量禁止）与数值路径改道（X2-3 冻结）。
- **irls 自身池 151.7–155.7 ms（与 R22-C 带 150.7–151.6 上沿
  邻域、在 R21-C 带 152.4–155.7 之内）**：可省项全部已关闭（S2-C/
  S6-C/S7-C 落地本体之上：S10-C-1 决斗带内、S9-C-3 符号翻转、
  S6-C-6 上界 2–4、S5-C-7 无正决斗、S9-C-4、S4-C-5/6、S2-C-3、
  S8-C-3、R11-C RID/CNT/EXP/ALLOC、R12-C MUCSE、R14-C SWORD/IRLSDP、
  R15-C STORD/RIDGE、R16-C ICOL、R17-C NSQRT）。ORDX 坐实其每迭代
  单价 16.8–17.9 µs 次序不变（支撑构成微移含于摆幅）。本轮零新位置。
- **APC 池 46.2–48.0 ms（本 VM，run A 46.2 漂出 R22-C 重锚定带
  46.4–47.0 的下沿、run C 48.0 漂出上沿）**：按 R14-C §3 末处置
  规则随池重锚地板（这不是重跑已关 ID 的决斗，也不构建任何堆叠
  候选道）：APCX 无计时器独立道 **53.8–55.8 ms**（基形状近似，
  **sink=7.309 与 R14-C/R16-C..R22-C 逐位同值** = 跨轮确定性旁证，
  派单强制项成立）；FLOORX 最廉价逐位合法形态地板 **39.2–40.0 ms**
  ⇒ **组合天花板 = 池 − 地板 = 14.6–16.0 ms（三次读数）< 35**。
  八轮间池带摆动（48.5–50.9 → 46.9–49.3 → 47.4–51.0 → 47.0–48.0 →
  46.8–48.0 → 47.0–49.2 → 46.4–47.0 → 46.2–48.0）为 VM 漂移双向
  往返，天花板八轮读数 12.1–23.8 / 13.8–21.6 / 12.1–20.9 /
  13.9–24.0 / 17.4–21.6 / 12.5–19.8 / 17.1–21.8 / 14.6–16.0 稳定
  亚带，组合关闭维持。R13-C REFZ 参考水平份额不受影响。ORDX 的
  计数见证补强该闭合：活跃对随参照层位移微摆 ±1.6%（273,084–
  281,768）且结构封顶 ×1.18——APC 池体量由类别结构唯一决定，呈现
  次序维度无法有效放大它。
- **引擎侧三轴（R15-C/R16-C 收口，本轮未重测未补铸）**：COLDX
  预热份额、GCAX 收集器份额、ALLOC 分配封顶——三轴均无切片内
  可受理变换，稳态口径下三大池冻结结论不受影响；本轮中位口径
  仍为稳态口径（ORDX 各剂量点取自电池预热后的仪表运行，首报告
  效应不进入读数）。
- **拒绝面（R18-C REJX 轴收口，本轮未重测未补铸）**：31 终点
  once-per-fault 定价原样引用；拒绝路径不构成第五类成本；ORDX 的
  塌缩/奇异计数五剂量全零是其在呈现次序维度上的免费旁证（拒绝/
  退化分支在全部次序剂量下零流量）。四分类 + once-per-fault 拒绝类
  继续穷尽。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测带级摆幅带内），
  维持关闭。本轮探针无一是稳定越带正项（从未点名普查为零、ORDX
  是呈现次序定价、FLOORX 是地板不是候选），无新成员可入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建面
开放 / X2-3 数值路径解冻 / 单线程审计基底变更 / maxIter=50、
tol=1e-8、bootstrap/seed 契约或 rows 次序契约变更——均不预期，前者
属阈值/测试面禁区、后两者属公开签名面）；（b）现实规模位移触达
SCALEX 标定的越线点（最尖锐 on-prob K\*=2.6×、名义收益口径 ≥~3.4×；
APC 组合外推 ~1.4× 需按 R14-C 规则在该剂量重跑地板——今日生产流量
在 perf 夹具当量及以下，均未触达；结局组成维度经 YMIX、重采样身份
维度经 SEEDX、呈现次序维度经本轮 ORDX 证明**均无越线点存在**：三轴
封顶放大同为 ×1.121 < 亚带池最小越带需求 ×4.4）；或（c）指出
「重命名 / 亚带 / 逐位违例 / 结构交换负优化 / once-per-fault 拒绝」
之外的第六类成本——二十三遍枚举未再发现。

## 4. 重命名检查（派单强制项）

- **ORDX vs STORD（R15-C）**：STORD 置换的是 S6-C 累加 switch case
  体内的**语句次序**——一次代码运动探针（零浮点机制的存储调度置换，
  已按先验池算术封顶关闭）；ORDX 置换的是**输入 rows 数组**，零代码
  运动，落地形态一条道逐字不动。对象（代码 vs 输入）、机制（存储
  调度 vs 收敛路径/参照层位移）、结论域（irls 累加池真子集 vs 全池
  剂量响应）均不同。非重命名，STORD 未重测。
- **ORDX vs SCALEX / YMIX / SEEDX**：SCALEX（R20-C）在冻结组成/种子/
  次序上动**输入体量**；YMIX（R21-C）在冻结体量/种子/次序上动
  **结局组成**（只翻 y 向量）；SEEDX（R22-C）在冻结体量/组成/次序上
  动**重采样身份流**（只动 options.seed）；ORDX 在冻结体量/组成/
  种子上动**呈现次序**（同一行多重集的置换，多重集相等每剂量断言）。
  四轴两两正交（体量 × 组成 × 重采样身份 × 呈现次序张成输入空间的
  四个独立维度），结论域不重叠：SCALEX 给出按体量关闭裁决的越线点，
  YMIX/SEEDX/ORDX 分别证明组成、重采样身份、呈现次序维度**无越线点**
  （计数恒等 + maxIter 封顶双重关死）。非重命名，未重测 SCALEX 任何
  尺寸剂量、未重测 YMIX 任何组成剂量、未重测 SEEDX 任何种子剂量。
- **ORDX vs ITERX / FITQ / 自确定性场景**：ITERX（R16-C）是**锚定
  次序上的迭代计数普查**（回答"收敛缩短类是否有逐位合法形态"——
  答案维持无）；FITQ（R19-C）是锚定次序上的拟合粒度墙钟分布；测量
  彩排场景 2 的自确定性检查是**同输入两跑的 Object.is 正确性见证**。
  ORDX 只问**池量与池结构对呈现次序的响应**——历轮所有性能口径都
  定在 fixtureRng(0xbeef) 发射次序一种呈现上，「行置换能否打开按
  体量/组成/身份关闭的裁决」此前从未在 C 区被测过；其新剂量点上的
  迭代/结局计数仅作确定性见证打印，未重测锚点次序上的 ITERX 普查
  本体，未重测 FITQ 的逐拟合分布。
- **ORDX vs R19-J 输入次序置换 / R22-F membership-topology / R14-A/D/E
  剂量档**：同一"剂量响应/次序置换"方法家族的 C 区首个次序实例；
  宿主切片（C 拟合管线 vs J 删除级联 / F restore 成员拓扑）、剂量
  单元（行置换 vs 删除输入次序 / assignment-population 对齐拓扑）
  与结论域互不重叠，属先例引用非重命名、非移植铸号（本轮零铸号）。
  **非 R23-A G×E 自耦合 / TERMCLASS / NVG / KFAN / R22-I run-record
  回读移植**（五者宿主与机制均在他区，派单点名禁项，零触碰）。
- **APCX/FLOORX 重测 vs S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 / CMB
  禁令**：本轮重测**只因池漂出带**（R14-C 处置规则；run A 下漂 +
  run C 上漂，首次单轮内双向越带，处置与单向越带相同），产出仅用于
  天花板差值算术与 sink 逐位核对；不重跑任何已关 ID 决斗、不构建
  堆叠候选、不产出可落地形态，四个成员 ID 与 CMB 禁令全部维持原状。
- **换名再提交拒列（本轮核实）**：零。R13-C..R22-C 二十二个无名
  关闭（含 TAILG/REJX/FITQ/SCALEX/YMIX/SEEDX）未重探、未补铸、未
  换址重提；S10-C-1..3 / S12-C-1 / RID / CNT 零重提（RID/CNT 反例
  作为 ORDX 唯一道形的否决依据被引用，未被重测）；ITERX/COLDX/
  AAFLR/GCAX 四轴未补铸；嵌套序 × 布局组合空间未触碰；X2-1 重写
  维持排除（S7-C 的 0/1 形态即落地形态，未另起平行实现）。

## 5. 测试与验证

环境：Node 22.22.2（本 VM 默认 22.14.0 低于 engines ≥22.19.0，经
nvm 切换并前置 PATH）、pnpm 10.17.1、`pnpm install --frozen-lockfile`。
生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录）
npx tsx /tmp/r23c-anchor.mts                # ✓ 7,344 项逐位 × 3 次正典运行全绿
                                            #   （53 例 ×{仪表道,落地道} 忠实性电池
                                            #    + 3 seed × 2 自确定性 + perf 夹具逐位
                                            #    + ORDX 五次序剂量点逐位保真
                                            #    + 多重集/p/distinctKeys 置换不变断言
                                            #    + 计数确定性 + 结构封顶见证断言）
npx tsx /tmp/r23c-apcfloor.mts              # ✓ APC 地板重锚定加测 × 3（APCX/FLOORX，
                                            #   遵循 R14-C 池漂出带处置规则；
                                            #   sink=7.309 / floorSink=7.309 六读数逐位同值）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 658.4 ms（1.12× vs 冻结 S6-C ctl），与 §1
# 生产中位（660.7–665.4）同带。
```

代表性一次测量彩排全量输出（正典 run A；run B/C 数字见 §1 池表与
§2）：

```text
scenario 1 (two-lane bitwise fidelity, 53 cases x {instrumented, plain-landed} vs production fitLogitAdditive import)
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)
  shape small (3 models x 3 projects, 120 rows): production median 32.6 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 660.7 ms/report (7 reps)
  perf reps: 666.5 661.4 659.1 658.2 659.6 660.7 666.0
instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):
  total                 670.7 ms
  solveSymmetric        453.1 ms  (67.6%)  [8966 calls]
  irls minus solve      151.7 ms  (22.6%)  [8966 iterations, 3586400 row visits, 1249652 irls sigmoids]
  onProbabilitiesFor    6.8 ms  (1.0%)  [80400 sigmoid+dot]
  APC (all sweeps)      46.2 ms  (6.9%)  [277606 active pairs, 12259 calls]
  bootstrap sampling    1.6 ms  (0.2%)
  sort/percentile tail  1.5 ms  (0.2%)  [12200 draws-map ops]
  base-row projection   0.005 ms  (once/report)
  outside pools (build/supports/keys/base copies) 9.7 ms  (1.5%)
  fits 201  draws successful=200 collapsed=0 singular=0
probe ORDX (input presentation-order / row-permutation dose response; NEW axis, not a lane):
  anchor invariants: p=60 distinctKeys=167 (asserted permutation-invariant per dose)
  dose order=anchor (anchor): fits=201 iters=8966 successful=200 collapsed=0 singular=0 apcPairs=277606 onProbSigmoids=80400 drawsMapOps=12200 diagnosis=model-problem; prod 663.1/660.4 ms; inst total 670.6 ms | solve 454.1 | irls-other 151.6 | onProb 6.8 | APC 52.7 | sampling 1.7 | tail 1.5 | rest 2.3
  dose order=reversed: fits=201 iters=7984 successful=200 collapsed=0 singular=0 apcPairs=278438 onProbSigmoids=80400 drawsMapOps=12200 diagnosis=model-problem; prod 595.9/594.9 ms; inst total 603.1 ms | solve 402.5 | irls-other 135.2 | onProb 6.6 | APC 53.1 | sampling 1.9 | tail 1.5 | rest 2.2
  dose order=sorted: fits=201 iters=9116 successful=200 collapsed=0 singular=0 apcPairs=281768 onProbSigmoids=80400 drawsMapOps=12200 diagnosis=model-problem; prod 670.9/670.3 ms; inst total 677.2 ms | solve 458.3 | irls-other 153.2 | onProb 6.7 | APC 53.9 | sampling 1.6 | tail 1.5 | rest 2.2
  dose order=shuffle-aaaa: fits=201 iters=8601 successful=200 collapsed=0 singular=0 apcPairs=279956 onProbSigmoids=80400 drawsMapOps=12200 diagnosis=model-problem; prod 635.5/638.0 ms; inst total 647.5 ms | solve 436.3 | irls-other 145.5 | onProb 6.6 | APC 53.4 | sampling 1.8 | tail 1.5 | rest 2.4
  dose order=shuffle-5555: fits=201 iters=8616 successful=200 collapsed=0 singular=0 apcPairs=273084 onProbSigmoids=80400 drawsMapOps=12200 diagnosis=model-problem; prod 663.2/643.1 ms; inst total 658.6 ms | solve 444.6 | irls-other 147.7 | onProb 7.0 | APC 53.5 | sampling 1.7 | tail 1.6 | rest 2.5
  order-dose arithmetic (35 ms band; count-driven sub-band pools only):
    onProb: worst 7.0 ms at order=shuffle-5555 (sub-band, x5.0 below 35)
    sampling: worst 1.9 ms at order=reversed (sub-band, x18.1 below 35)
    tail: worst 1.6 ms at order=shuffle-5555 (sub-band, x21.8 below 35)
    rest: worst 2.5 ms at order=shuffle-5555 (sub-band, x14.1 below 35)
    APC (descriptive; closure floor-anchored, not count-based): anchor:52.7 reversed:53.1 sorted:53.9 shuffle-aaaa:53.4 shuffle-5555:53.5 ms — active pairs 277606/278438/281768/279956/273084 (structural cap 321,600 = x1.18 of worst dose)
    iteration counts across doses: anchor:8966 reversed:7984 sorted:9116 shuffle-aaaa:8601 shuffle-5555:8616 — cap 10050 (fits x 50); worst dose 9116 = 90.7% of cap; max order amplification x1.121 vs anchor
    solve per-iteration price (µs/iter across doses): anchor:50.6 reversed:50.4 sorted:50.3 shuffle-aaaa:50.7 shuffle-5555:51.6 — spread 2.6% (order reaches the frozen pools only through the iteration count and the support-mix microshift)
    irls-other per-iteration price (µs/iter across doses): anchor:16.9 reversed:16.9 sorted:16.8 shuffle-aaaa:16.9 shuffle-5555:17.1 — spread 2.0% (order reaches the frozen pools only through the iteration count and the support-mix microshift)
APC pool median this run: 46.2 ms (R22-C band 46.4-47.0; re-anchor floor only if outside)

ALL EQUIVALENCE CHECKS PASSED (7344 bitwise checks)
```

其余两次正典运行与地板加测的关键数字：

```text
run B: prod median 663.3 (small 32.1; perf reps 679.8 663.7 663.3 661.1 662.1 663.2 677.3);
       pools solve 454.5 / irls-other 152.7 / APC 46.4 (= 带下沿);
       ORDX per-iter prices: solve 50.5-51.1 µs (spread 1.3%), irls-other 16.8-17.1 µs (spread 2.0%);
       sub-band worst: onProb 7.4 (x4.7) / sampling 1.8 (x19.9) / tail 1.6 (x21.8) / rest 2.4 (x14.3)
run C: prod median 665.4 (small 32.0; perf reps 669.1 661.4 661.8 665.4 664.6 665.8 666.2);
       pools solve 465.3 / irls-other 155.7 / APC 48.0 (> 47.0 带上沿 -> 与 run A 46.2 下漂共同触发重锚);
       ORDX per-iter prices: solve 50.6-50.9 µs (spread 0.5%), irls-other 16.9-17.9 µs (spread 5.8%);
       sub-band worst: onProb 8.0 (x4.4) / sampling 1.9 (x18.2) / tail 1.5 (x22.9) / rest 2.3 (x15.1)
ORDX 结构量（三运行逐位一致）: iters 8966/7984/9116/8601/8616 (锚点非极值; reversed ×0.891 / sorted ×1.017);
       apcPairs 277606/278438/281768/279956/273084 (−1.6%~+1.5%, 封顶 321,600 = ×1.18);
       onProbSigmoids=80400 与 drawsMapOps=12200 五剂量恒等 (计数恒等每剂量断言);
       successful=200 collapsed=0 singular=0 五剂量全零;
       diagnosis model-problem 五剂量三运行全同;
       p=60 与 distinctKeys=167 置换不变（每剂量断言）;
       封顶算术: 201×50=10,050, 最坏剂量 9,116 = 90.7%, 最大次序放大 ×1.121
APC 地板重锚定加测 × 3: APCX 55.8 / 53.8 / 55.4; FLOORX 40.0 / 39.2 / 39.4;
       ceiling 15.8 / 14.6 / 16.0 (全部 < 35; sink=7.309 与 floorSink=7.309
       六读数全同，且与 R14-C/R16-C/R17-C/R18-C/R19-C/R20-C/R21-C/R22-C 逐位同值)
全部站点计数（8,966 / 3,586,400 / 1,249,652 / 277,606 / 12,259 / 12,200 /
80,400 / 201）在三次运行逐位一致（确定性计数）。
```

## 6. 新增排除 ID：无

第二十三组候选集为空。从未点名站点普查结果为零（连续第五遍；R18-C
「最后一个从未点名位置」判定经 R19-C、R20-C、R21-C、R22-C 与本轮五遍
核实），ORDX 为输入呈现次序 / 行置换剂量响应轴（重开条件：rows 次序
契约本身变更——属公开签名面，不预期；或 ITERX/X2-3 关死的收敛缩短族
出现逐位合法形态——无。呈现次序维度本身**无越线点**：计数恒等把全部
抽取计数驱动池钉死在与次序无关的封顶上，迭代通道封顶放大 ×1.121 <
亚带池最小越带需求 ×4.4），APCX/FLOORX 为按规地板重锚定。APC 池八轮
带摆动（48.5–50.9 → 46.9–49.3 → 47.4–51.0 → 47.0–48.0 → 46.8–48.0 →
47.0–49.2 → 46.4–47.0 → 46.2–48.0）均已按 R14-C 规则各自重锚，天花板
八轮读数（12.1–23.8 → 13.8–21.6 → 12.1–20.9 → 13.9–24.0 → 17.4–21.6 →
12.5–19.8 → 17.1–21.8 → 14.6–16.0）稳定亚带，组合关闭维持。

## 附录：测量彩排脚本（完整，可复现）

### A. 主彩排 `/tmp/r23c-anchor.mts`

```ts
/**
 * Round 23 / R23-C measurement re-anchor + twenty-third-group closure probes
 * (offline routing slice, twenty-third pass). NOT committed — the twenty-
 * third-group enumeration carries no candidate lanes that reach the
 * acceptance bar; full source goes into the R23-C report appendix
 * (R7-C..R22-C precedent).
 *
 * Discipline (same as R11-C..R22-C):
 *   1. re-anchor the post-S7-C profile on THIS VM: production median
 *      ms/report on the perf shape (400 rows, 6 models x 8 projects,
 *      bootstrap=200) and the small shape, x-checked by an INSTRUMENTED
 *      verbatim pipeline copy (timers/counters only; every float op, value,
 *      and order identical — validated bitwise against the production
 *      import over the full 53-case battery before its numbers are used);
 *   2. derive the per-pool cost table (solve / irls-other / on-prob / APC /
 *      sampling / tail / base-copy) plus site counts;
 *   3. ORDX (new completeness axis for C; input presentation-order /
 *      row-permutation dose response — R19-J's J-slice input-order
 *      permutation face and the C-slice dose-response method line SCALEX
 *      (volume) / YMIX (outcome composition) / SEEDX (resample identity) as
 *      precedent): every prior C-round priced the pools at ONE presentation
 *      order — the fixtureRng(0xbeef) emission order. SCALEX moved the
 *      input volume at the frozen order; YMIX moved the y composition at
 *      the frozen order; SEEDX moved the resample-identity stream at the
 *      frozen order; the presentation order of the SAME row multiset was
 *      never moved. ORDX freezes the data multiset, volume, composition,
 *      and bootstrap seed (20260818 anchor — SEEDX is NOT re-probed), and
 *      varies ONLY the order of the rows array:
 *        anchor (fixture emission) / reversed / sorted (grouped by
 *        scenario,model,project,y) / two deterministic Fisher-Yates
 *        shuffles (fixtureRng(0xaaaa), fixtureRng(0x5555)).
 *      Multiset invariants asserted per dose: the canonical sorted multiset
 *      is byte-identical to the anchor's; the design width p and the
 *      distinct canonical-key count are permutation-invariant (level and
 *      pair counts are multiset properties). Per dose the instrumented lane
 *      is bitwise-validated against the production import AT THAT DOSE
 *      before its numbers are used. The axis prices (a) whether any
 *      presentation order inflates a count-driven sub-band pool over the
 *      ±35 ms landing band (structural note: onProb sigmoids =
 *      400 x (1 + successful) <= 80,400; tail draws-map ops =
 *      (p+1) x successful <= 12,200 with p frozen by multiset invariance;
 *      sampling loop = 400 x 200 fixed; iterations <= fits x maxIter(50);
 *      apcActivePairs <= fits x 400 x 4 = 321,600 — all asserted per
 *      dose), and (b) whether the per-iteration price of the frozen pools
 *      (solve, irls-other) is order-invariant — i.e. whether the
 *      presentation order can reach the pools through any channel other
 *      than the maxIter-capped iteration count. It builds no lane and
 *      re-runs no closed duel (per-dose iteration/outcome counts are
 *      deterministic witnesses at NEW dose points, not a re-probe of the
 *      anchor ITERX census; STORD — the R15-C statement-order permutation
 *      INSIDE the S6-C switch bodies, a code transform — is not re-probed:
 *      ORDX permutes the INPUT rows, zero code motion; no YMIX composition
 *      dose, no SCALEX size dose, and no SEEDX seed dose is touched).
 *   4. APC pool disposition: R22-C re-anchored the APC band to 46.4-47.0 ms
 *      on its VM class. Per the R14-C disposition rule the floor is
 *      re-anchored ONLY if the measured pool exits the current band; the
 *      combination ceiling (17.1-21.8 < 35, R22-C) otherwise stands.
 *   5. re-check production self-determinism across 3 seeds x 2 runs.
 *
 * SEEDX / YMIX / SCALEX / FITQ / TAILG / REJX / NSQRT / PMV / OSTZ / ICOL /
 * SFILL / ITERX / COLDX / AAFLR / GCAX / STORD / RIDGE are NOT re-probed and
 * NOT re-minted (dispatch order).
 *
 * Run: npx tsx /tmp/r23c-anchor.mts
 */

import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.js";
import type { AttributionReport, OfflineRow } from "/workspace/src/routing/offline-types.js";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.js";
import { DomainValidationError } from "/workspace/src/domain/errors.js";

/* --------------------------- shared constants --------------------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}

interface FitResult {
  readonly coefficients: readonly number[] | null;
}

type IrlsImpl = (
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
) => FitResult;

function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}

function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/* ------------- verbatim production pipeline building blocks -------------- *
 * Transcribed from src/routing/offline-logit.ts and src/routing/lin-alg.ts
 * at the 183df9b-landed state. The instrumented lane's ONLY additions are
 * STATS counter increments and performance.now() reads at call boundaries;
 * no float operation, operand, or ordering differs. The plain lane carries
 * no timers at all. Both lanes are validated bitwise against the production
 * import over the full 53-case battery — and additionally at every ORDX
 * dose — before any number is reported. */

const STATS = {
  fits: 0,
  irlsMs: 0,
  irlsIterations: 0,
  solveCalls: 0,
  solveMs: 0,
  sigmoidIrls: 0,
  rowVisits: 0,
  onProbMs: 0,
  onProbSigmoids: 0,
  apcMs: 0,
  apcActivePairs: 0,
  apcCalls: 0,
  samplingMs: 0,
  drawsMapOps: 0,
  tailMs: 0,
  baseCopyMs: 0,
  totalMs: 0,
  collapsedDraws: 0,
  singularDraws: 0,
  successfulDraws: 0,
  reset(): void {
    this.fits = 0; this.irlsMs = 0; this.irlsIterations = 0; this.solveCalls = 0;
    this.solveMs = 0; this.sigmoidIrls = 0; this.rowVisits = 0; this.onProbMs = 0;
    this.onProbSigmoids = 0; this.apcMs = 0; this.apcActivePairs = 0;
    this.apcCalls = 0; this.samplingMs = 0; this.drawsMapOps = 0;
    this.tailMs = 0; this.baseCopyMs = 0; this.totalMs = 0;
    this.collapsedDraws = 0; this.singularDraws = 0; this.successfulDraws = 0;
  }
};

function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);

  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);

  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);

  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });

  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}

function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}

function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}

/* verbatim landed solveSymmetric (lin-alg.ts) — plain, no timers */
function solvePlain(a: readonly (readonly number[])[], b: readonly number[]): number[] | null {
  const n = b.length;
  if (a.length !== n || a.some((row) => row.length !== n)) {
    throw new DomainValidationError("solveSymmetric requires a square matrix matching b");
  }
  const m: number[][] = a.map((row) => [...row]);
  const x: number[] = [...b];
  const eps = 1e-12;

  for (let col = 0; col < n; col++) {
    let pivotRow = col;
    let pivotAbs = Math.abs(m[col]![col]!);
    for (let row = col + 1; row < n; row++) {
      const abs = Math.abs(m[row]![col]!);
      if (abs > pivotAbs) {
        pivotAbs = abs;
        pivotRow = row;
      }
    }
    if (pivotAbs < eps) return null;
    if (pivotRow !== col) {
      const tmp = m[col]!;
      m[col] = m[pivotRow]!;
      m[pivotRow] = tmp;
      const tb = x[col]!;
      x[col] = x[pivotRow]!;
      x[pivotRow] = tb;
    }
    const colArr = m[col]!;
    const pivot = colArr[col]!;
    const xCol = x[col]!;
    for (let row = col + 1; row < n; row++) {
      const rowArr = m[row]!;
      const factor = rowArr[col]! / pivot;
      if (factor === 0) continue;
      let k = col;
      const stop = n - 3;
      for (; k < stop; k += 4) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
        rowArr[k + 1] = rowArr[k + 1]! - factor * colArr[k + 1]!;
        rowArr[k + 2] = rowArr[k + 2]! - factor * colArr[k + 2]!;
        rowArr[k + 3] = rowArr[k + 3]! - factor * colArr[k + 3]!;
      }
      for (; k < n; k++) {
        rowArr[k] = rowArr[k]! - factor * colArr[k]!;
      }
      x[row] = x[row]! - factor * xCol;
    }
  }

  const solution = new Array<number>(n).fill(0);
  for (let row = n - 1; row >= 0; row--) {
    const rowArr = m[row]!;
    let sum = x[row]!;
    for (let k = row + 1; k < n; k++) sum -= rowArr[k]! * solution[k]!;
    const diag = rowArr[row]!;
    if (Math.abs(diag) < eps) return null;
    solution[row] = sum / diag;
  }
  for (const value of solution) {
    if (!Number.isFinite(value)) return null;
  }
  return solution;
}

/* The lanes differ ONLY in instrumentation (counters + timers). Verbatim
 * landed accumulation switch in both. */
function makeIrls(instrumented: boolean): IrlsImpl {
  return (design, rows, _vectors, supports, keys, keySpace, maxIter) => {
    const tIrls0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.fits += 1;
    const p = design.names.length;
    const n = rows.length;
    const eta = new Array<number>(n).fill(0);
    const mu = new Array<number>(n).fill(0);
    const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
    const xtwz: number[] = new Array<number>(p).fill(0);
    const stamp = new Int32Array(keySpace);
    const etaByKey = new Float64Array(keySpace);
    const muByKey = new Float64Array(keySpace);
    let beta = new Array<number>(p).fill(0);
    for (let iter = 0; iter < maxIter; iter++) {
      if (instrumented) {
        STATS.irlsIterations += 1;
        STATS.rowVisits += n;
      }
      const mark = iter + 1;
      for (let i = 0; i < n; i++) {
        const key = keys[i]!;
        if (stamp[key] !== mark) {
          stamp[key] = mark;
          if (instrumented) STATS.sigmoidIrls += 1;
          let value = 0;
          const active = supports[i]!;
          for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
          etaByKey[key] = value;
          muByKey[key] = sigmoid(value);
        }
        eta[i] = etaByKey[key]!;
        mu[i] = muByKey[key]!;
      }
      for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
      xtwz.fill(0);
      for (let i = 0; i < n; i++) {
        const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
        const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
        const wz = w * z;
        const active = supports[i]!;
        switch (active.length) {
          case 5: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            const a3 = active[3]!;
            const a4 = active[4]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
            xtwz[a3] = xtwz[a3]! + wz;
            const r3 = xtwx[a3]!;
            r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
            xtwz[a4] = xtwz[a4]! + wz;
            const r4 = xtwx[a4]!;
            r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
            break;
          }
          case 4: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            const a3 = active[3]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
            xtwz[a3] = xtwz[a3]! + wz;
            const r3 = xtwx[a3]!;
            r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
            break;
          }
          case 3: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            const a2 = active[2]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
            xtwz[a2] = xtwz[a2]! + wz;
            const r2 = xtwx[a2]!;
            r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
            break;
          }
          case 2: {
            const a0 = active[0]!;
            const a1 = active[1]!;
            xtwz[a0] = xtwz[a0]! + wz;
            const r0 = xtwx[a0]!;
            r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
            xtwz[a1] = xtwz[a1]! + wz;
            const r1 = xtwx[a1]!;
            r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
            break;
          }
          default: {
            for (let ai = 0; ai < active.length; ai++) {
              const a = active[ai]!;
              xtwz[a] = xtwz[a]! + wz;
              const rowA = xtwx[a]!;
              for (let bi = 0; bi < active.length; bi++) {
                const b = active[bi]!;
                rowA[b] = rowA[b]! + w;
              }
            }
          }
        }
      }
      for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
      let next: number[] | null;
      if (instrumented) {
        const tSolve0 = performance.now();
        next = solvePlain(xtwx, xtwz);
        STATS.solveMs += performance.now() - tSolve0;
        STATS.solveCalls += 1;
      } else {
        next = solvePlain(xtwx, xtwz);
      }
      if (next === null) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      const delta = next.map((value, index) => value - beta[index]!);
      const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
      beta = next;
      if (!beta.every(Number.isFinite)) {
        if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
        return { coefficients: null };
      }
      if (l2 < TOL) break;
    }
    if (instrumented) STATS.irlsMs += performance.now() - tIrls0;
    return { coefficients: beta };
  };
}

const irlsInstrumented = makeIrls(true);
const irlsPlain = makeIrls(false);

function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}

function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}

function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}

function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

function makeFit(irlsImpl: IrlsImpl, instrumented: boolean) {
  const onProb = (vectors: readonly number[][], coefficients: readonly number[]): number[] => {
    if (!instrumented) return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    const t0 = performance.now();
    const result = vectors.map((vector) => sigmoid(dot(coefficients, vector)));
    STATS.onProbMs += performance.now() - t0;
    STATS.onProbSigmoids += vectors.length;
    return result;
  };

  const apc = (
    design: Design,
    rows: readonly Row[],
    vectors: readonly number[][],
    coefficients: readonly number[],
    onProbabilities: readonly number[],
    column: string
  ): number => {
    const t0 = instrumented ? performance.now() : 0;
    if (instrumented) STATS.apcCalls += 1;
    let sum = 0;
    const columnIdx = design.columnIndex.get(column);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < rows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        if (instrumented) STATS.apcActivePairs += 1;
        const on = onProbabilities[i]!;
        const offVector = vectors[i]!.slice();
        offVector[columnIdx] = 0;
        const off = sigmoid(dot(coefficients, offVector));
        sum += on - off;
      }
    }
    const result = rows.length === 0 ? 0 : sum / rows.length;
    if (instrumented) STATS.apcMs += performance.now() - t0;
    return result;
  };

  const pushValue = (map: Map<string, number[]>, key: string, value: number): void => {
    if (instrumented) STATS.drawsMapOps += 1;
    const list = map.get(key);
    if (list === undefined) map.set(key, [value]);
    else list.push(value);
  };

  return function fit(
    rows: readonly OfflineRow[],
    options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
  ): AttributionReport {
    const tTotal0 = instrumented ? performance.now() : 0;
    const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
    const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
    const seed = options?.seed ?? SEED_DEFAULT;

    const tBase0 = instrumented ? performance.now() : 0;
    const baseRows: Row[] = rows.map((r) => ({
      scenarioId: r.scenarioId,
      modelVersion: r.modelVersion,
      projectId: r.projectId,
      y: r.y
    }));
    if (instrumented) STATS.baseCopyMs += performance.now() - tBase0;
    const effects: Array<{ name: string; point: number; lcb: number; ucb: number }> = [];

    if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
    }

    const design = buildDesign(baseRows);
    const vectors = baseRows.map((r) => design.build(r));
    const supports = computeSupports(vectors);
    const keys = canonicalRowKeys(baseRows);
    const fit = irlsImpl(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
    if (fit.coefficients === null) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
    }

    const onProbabilities = onProb(vectors, fit.coefficients);
    const pointEffects = new Map<string, number>();
    for (const name of design.names) {
      if (name === "intercept") continue;
      pointEffects.set(
        name,
        apc(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
      );
    }
    for (const ref of design.referenceLevels) {
      if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
        pointEffects.set(`${ref.factor}:${ref.name}`, 0);
      }
    }

    const random = rng(seed);
    const draws = new Map<string, number[]>();
    let successful = 0;
    for (let draw = 0; draw < bootstrapDraws; draw++) {
      const tSample0 = instrumented ? performance.now() : 0;
      const sample: Row[] = [];
      const sampleVectors: number[][] = [];
      const sampleSupports: number[][] = [];
      const sampleKeys: number[] = [];
      for (let i = 0; i < baseRows.length; i++) {
        const index = Math.floor(random() * baseRows.length);
        sample.push(baseRows[index]!);
        sampleVectors.push(vectors[index]!);
        sampleSupports.push(supports[index]!);
        sampleKeys.push(keys[index]!);
      }
      const collapsed = sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1);
      if (instrumented) STATS.samplingMs += performance.now() - tSample0;
      if (collapsed) {
        if (instrumented) STATS.collapsedDraws += 1;
        continue;
      }
      const bootFit = irlsImpl(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
      if (bootFit.coefficients === null) {
        if (instrumented) STATS.singularDraws += 1;
        continue;
      }
      successful += 1;
      if (instrumented) STATS.successfulDraws += 1;
      const sampleOnProbabilities = onProb(sampleVectors, bootFit.coefficients);
      for (const [name] of pointEffects.entries()) {
        const value = apc(
          design,
          sample,
          sampleVectors,
          bootFit.coefficients,
          sampleOnProbabilities,
          name
        );
        pushValue(draws, name, value);
      }
    }

    const tTail0 = instrumented ? performance.now() : 0;
    for (const [name, point] of pointEffects.entries()) {
      const values = draws.get(name) ?? [];
      if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
        return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
      }
      values.sort((a, b) => a - b);
      effects.push({
        name,
        point,
        lcb: percentile(values, 0.025),
        ucb: percentile(values, 0.975)
      });
    }

    const n = baseRows.length;
    const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
    const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
    const muLcb = betaQuantileLcb(muPosterior, 0.05);
    const models = new Set(baseRows.map((r) => r.modelVersion)).size;
    const projects = new Set(baseRows.map((r) => r.projectId)).size;
    const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
    const containsZero = (e: { lcb: number; ucb: number }): boolean =>
      e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
    const interactionsFor = (prefix: string): Array<{ name: string; lcb: number; ucb: number; point: number }> =>
      effects.filter((e) => e.name.startsWith(`w:${prefix}`));

    let diagnosis: AttributionReport["diagnosis"] = "uncertain";
    const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
    if (
      effects.some(
        (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
      )
    ) {
      diagnosis = "model-problem";
    } else if (
      effects.some(
        (e) =>
          e.name.startsWith("v:") &&
          e.lcb < -ATTRIBUTION_EFFECT &&
          effects
            .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
            .every(containsZero)
      )
    ) {
      diagnosis = "project-problem";
    } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
      diagnosis = "interaction-only";
    } else if (scenarioHard) {
      diagnosis = "scenario-hard";
    }

    const report: AttributionReport = {
      estimator: "logit-additive",
      rowsUsed: baseRows.length,
      effects,
      diagnosis,
      reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
      writesActivePointer: false
    };
    if (instrumented) {
      STATS.tailMs += performance.now() - tTail0;
      STATS.totalMs += performance.now() - tTotal0;
    }
    return report;
  };
}

const fitInst = makeFit(irlsInstrumented, true);
const fitPlain = makeFit(irlsPlain, false);

/* ------------------------------ harness ------------------------------ */

let checksPassed = 0;
let failures = 0;

function check(label: string, ok: boolean): void {
  if (ok) checksPassed++;
  else {
    failures++;
    process.stderr.write(`FAIL ${label}\n`);
  }
}

function compareReports(label: string, expected: AttributionReport, actual: AttributionReport): void {
  check(`${label}.estimator`, expected.estimator === actual.estimator);
  check(`${label}.rowsUsed`, expected.rowsUsed === actual.rowsUsed);
  check(`${label}.diagnosis`, expected.diagnosis === actual.diagnosis);
  check(`${label}.reason`, expected.reason === actual.reason);
  check(`${label}.writesActivePointer`, expected.writesActivePointer === actual.writesActivePointer);
  check(`${label}.effects.length`, expected.effects.length === actual.effects.length);
  if (expected.effects.length === actual.effects.length) {
    for (let i = 0; i < expected.effects.length; i++) {
      const e = expected.effects[i]!;
      const g = actual.effects[i]!;
      check(`${label}.effects[${i}].name`, e.name === g.name);
      check(`${label}.effects[${i}].point`, Object.is(e.point, g.point));
      check(`${label}.effects[${i}].lcb`, Object.is(e.lcb, g.lcb));
      check(`${label}.effects[${i}].ucb`, Object.is(e.ucb, g.ucb));
    }
  }
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}

function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRow[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRow[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function allUniqueRows(r: () => number, count: number): OfflineRow[] {
  const rows: OfflineRow[] = [];
  for (let i = 0; i < count; i++) {
    rows.push({
      scenarioId: `s${i % 2}`,
      modelVersion: `m${i}`,
      projectId: `p${i}`,
      y: r() < 0.6 ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}

function batteryCases(): Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRow[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];

  for (let i = 0; i < 40; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, {
      scenarios: 2,
      models: 3,
      projects: 3,
      rows: 60,
      passRate: 0.55,
      pipeInModel: true,
    }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }),
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  cases.push({
    rows: allUniqueRows(r, 36),
    options: { bootstrap: 40, seed: 23 },
  });
  {
    const oneKey: OfflineRow[] = [];
    for (let i = 0; i < 30; i++) {
      oneKey.push({
        scenarioId: "s0",
        modelVersion: "m0",
        projectId: "p0",
        y: i % 3 === 0 ? 0 : 1,
        occurredAtMs: 1_000 + i,
      });
    }
    cases.push({ rows: oneKey, options: { bootstrap: 40, seed: 31 } });
  }
  cases.push({
    rows: [
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 1, occurredAtMs: 1_000 },
      { scenarioId: "s0", modelVersion: "m0", projectId: "p0", y: 0, occurredAtMs: 1_001 },
    ],
    options: { bootstrap: 30, seed: 5 },
  });
  return cases;
}

const out = (line: string): void => {
  process.stdout.write(`${line}\n`);
};

function perfRows(): OfflineRow[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, {
    scenarios: 4,
    models: 6,
    projects: 8,
    rows: 400,
    passRate: 0.6,
  });
}

const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------- scenario 1: two-lane bitwise fidelity battery --------- */

function scenarioEquivalence(): void {
  const cases = batteryCases();
  for (const [index, testCase] of cases.entries()) {
    const expected = fitLogitAdditive(testCase.rows, testCase.options);
    compareReports(`R23C-inst[${index}]`, expected, fitInst(testCase.rows, testCase.options));
    compareReports(`R23C-plain[${index}]`, expected, fitPlain(testCase.rows, testCase.options));
  }
  out(
    `scenario 1 (two-lane bitwise fidelity, ${cases.length} cases x {instrumented, plain-landed} ` +
      `vs production fitLogitAdditive import)`
  );
}

/* -------- scenario 2: production self-determinism across 3 seeds -------- */

function scenarioDeterminism(): void {
  const r = fixtureRng(0xd19c);
  const rows = randomRows(r, { scenarios: 3, models: 4, projects: 5, rows: 150, passRate: 0.6 });
  for (const seed of [101, 202, 303]) {
    const first = fitLogitAdditive(rows, { bootstrap: 60, seed });
    const second = fitLogitAdditive(rows, { bootstrap: 60, seed });
    compareReports(`R23C-det[seed=${seed}]`, first, second);
  }
  out("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is)");
}

/* ------------- scenario 3: profile re-anchor + pool table ------------- */

let APC_POOL_MEDIAN = 0;

interface PoolReading {
  totalMs: number;
  irlsMs: number;
  solveMs: number;
  onProbMs: number;
  apcMs: number;
  samplingMs: number;
  tailMs: number;
  baseCopyMs: number;
  fits: number;
  irlsIterations: number;
  solveCalls: number;
  sigmoidIrls: number;
  rowVisits: number;
  onProbSigmoids: number;
  apcActivePairs: number;
  apcCalls: number;
  drawsMapOps: number;
  collapsedDraws: number;
  singularDraws: number;
  successfulDraws: number;
}

function snapshotStats(): PoolReading {
  return {
    totalMs: STATS.totalMs, irlsMs: STATS.irlsMs, solveMs: STATS.solveMs,
    onProbMs: STATS.onProbMs, apcMs: STATS.apcMs, samplingMs: STATS.samplingMs,
    tailMs: STATS.tailMs, baseCopyMs: STATS.baseCopyMs,
    fits: STATS.fits, irlsIterations: STATS.irlsIterations, solveCalls: STATS.solveCalls,
    sigmoidIrls: STATS.sigmoidIrls, rowVisits: STATS.rowVisits,
    onProbSigmoids: STATS.onProbSigmoids, apcActivePairs: STATS.apcActivePairs,
    apcCalls: STATS.apcCalls, drawsMapOps: STATS.drawsMapOps,
    collapsedDraws: STATS.collapsedDraws, singularDraws: STATS.singularDraws,
    successfulDraws: STATS.successfulDraws,
  };
}

function scenarioProfile(): void {
  const smallRows = randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
  {
    const times: number[] = [];
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(smallRows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape small (3 models x 3 projects, 120 rows): production median ${median(times).toFixed(1)} ms/report (3 reps)`);
  }

  {
    const rows = perfRows();
    const times: number[] = [];
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      times.push(performance.now() - t0);
    }
    out(`  shape perf (6 models x 8 projects, 400 rows): production median ${median(times).toFixed(1)} ms/report (7 reps)`);
    out(`  perf reps: ${times.map((t) => t.toFixed(1)).join(" ")}`);
  }

  // Instrumented pool table on the perf shape (3 runs; medians reported).
  const rows = perfRows();
  const expected = fitLogitAdditive(rows, PERF_OPTIONS);
  const runs: PoolReading[] = [];
  for (let rep = 0; rep < 3; rep++) {
    STATS.reset();
    const actual = fitInst(rows, PERF_OPTIONS);
    if (rep === 0) {
      compareReports("R23C-perf-fixture", expected, actual);
    }
    runs.push(snapshotStats());
  }
  const med = (key: keyof PoolReading): number => median(runs.map((r) => r[key]));
  const total = med("totalMs");
  const solveMs = med("solveMs");
  const irlsOther = med("irlsMs") - solveMs;
  const rest = total - med("irlsMs") - med("onProbMs") - med("apcMs") - med("samplingMs") - med("tailMs");
  APC_POOL_MEDIAN = med("apcMs");
  out(`instrumented pool table (perf shape, median of 3 instrumented runs; timers only, bitwise-checked):`);
  out(`  total                 ${total.toFixed(1)} ms`);
  out(`  solveSymmetric        ${solveMs.toFixed(1)} ms  (${(100 * solveMs / total).toFixed(1)}%)  [${med("solveCalls")} calls]`);
  out(`  irls minus solve      ${irlsOther.toFixed(1)} ms  (${(100 * irlsOther / total).toFixed(1)}%)  [${med("irlsIterations")} iterations, ${med("rowVisits")} row visits, ${med("sigmoidIrls")} irls sigmoids]`);
  out(`  onProbabilitiesFor    ${med("onProbMs").toFixed(1)} ms  (${(100 * med("onProbMs") / total).toFixed(1)}%)  [${med("onProbSigmoids")} sigmoid+dot]`);
  out(`  APC (all sweeps)      ${med("apcMs").toFixed(1)} ms  (${(100 * med("apcMs") / total).toFixed(1)}%)  [${med("apcActivePairs")} active pairs, ${med("apcCalls")} calls]`);
  out(`  bootstrap sampling    ${med("samplingMs").toFixed(1)} ms  (${(100 * med("samplingMs") / total).toFixed(1)}%)`);
  out(`  sort/percentile tail  ${med("tailMs").toFixed(1)} ms  (${(100 * med("tailMs") / total).toFixed(1)}%)  [${med("drawsMapOps")} draws-map ops]`);
  out(`  base-row projection   ${med("baseCopyMs").toFixed(3)} ms  (once/report)`);
  out(`  outside pools (build/supports/keys/base copies) ${rest.toFixed(1)} ms  (${(100 * rest / total).toFixed(1)}%)`);
  out(`  fits ${med("fits")}  draws successful=${med("successfulDraws")} collapsed=${med("collapsedDraws")} singular=${med("singularDraws")}`);
}

/* -------- scenario 4: ORDX input presentation-order dose response -------- *
 * NEW completeness axis (C-first; dose-response method family: SCALEX moved
 * input volume, YMIX moved outcome composition, SEEDX moved the resample-
 * identity stream — all at the single fixture emission order; R19-J's
 * input-order permutation face in the J slice as cross-slice precedent).
 * Question never measured in C: every prior round priced the pools at ONE
 * presentation order of the input rows. Does ANY permutation of the SAME
 * row multiset, at frozen volume, composition, and bootstrap seed, inflate
 * a count-driven sub-band pool over the ±35 ms band — or reach the frozen
 * pools (solve, irls-other) through any channel other than the
 * maxIter-capped iteration count?
 *
 * Dose grid: order ∈ {anchor (fixture emission), reversed, sorted (grouped
 * by scenario, model, project, y, occurredAtMs), shuffled(0xaaaa),
 * shuffled(0x5555)} at the frozen perf shape (4 scenarios, 6 models,
 * 8 projects, 400 rows, passRate=0.6), bootstrap=200, seed=20260818. The
 * doses share one row-object multiset BY CONSTRUCTION (permutations of one
 * array; asserted via canonical serialization); the order enters through
 * (a) first-occurrence level order in buildDesign (which levels become the
 * dropped references — support-size mix shifts), (b) the canonical-key
 * representative indices, and (c) the resample index -> row mapping (the
 * index stream itself is seed-frozen). Structural invariants asserted per
 * dose:
 *   p (design width)   permutation-invariant  (level/pair counts are
 *                       multiset properties; asserted == anchor p)
 *   distinct keys      permutation-invariant  (distinct triple count)
 *   onProbSigmoids = 400 x (1 + successful)  <= 80,400
 *   drawsMapOps    = (p + 1) x successful    <= 12,200
 *   sampling loop  = 400 x 200 draws          (fixed, order-independent)
 *   iterations     <= fits x maxIter(50)      (cap 10,050 when fits=201)
 *   apcActivePairs <= fits x 400 x 4 = 321,600 (support <= 5 incl intercept)
 * Per dose: bitwise fidelity of the instrumented lane against the
 * production import AT THAT DOSE, 2 uninstrumented production reps, 2
 * instrumented reps (mean) for pools, per-dose structural counts, and the
 * derived per-iteration price of the frozen pools. No lane is built and no
 * closed duel is re-run (STORD's statement-order permutation inside the
 * accumulation switch is code motion — not touched here). */

interface OrderDose {
  readonly label: string;
  readonly rows: readonly OfflineRow[];
}

function canonicalMultiset(rows: readonly OfflineRow[]): string {
  return rows
    .map((r) => `${r.scenarioId}\u0000${r.modelVersion}\u0000${r.projectId}\u0000${r.y}\u0000${r.occurredAtMs}`)
    .sort()
    .join("\u0001");
}

function shuffled(rows: readonly OfflineRow[], seed: number): OfflineRow[] {
  const r = fixtureRng(seed);
  const copy = [...rows];
  for (let i = copy.length - 1; i > 0; i--) {
    const j = Math.floor(r() * (i + 1));
    const tmp = copy[i]!;
    copy[i] = copy[j]!;
    copy[j] = tmp;
  }
  return copy;
}

function sortedRows(rows: readonly OfflineRow[]): OfflineRow[] {
  return [...rows].sort((a, b) => {
    if (a.scenarioId !== b.scenarioId) return a.scenarioId < b.scenarioId ? -1 : 1;
    if (a.modelVersion !== b.modelVersion) return a.modelVersion < b.modelVersion ? -1 : 1;
    if (a.projectId !== b.projectId) return a.projectId < b.projectId ? -1 : 1;
    if (a.y !== b.y) return a.y - b.y;
    return a.occurredAtMs - b.occurredAtMs;
  });
}

interface DoseResult {
  readonly label: string;
  readonly prodMs: number[];
  readonly pools: PoolReading[];
  readonly diagnosis: string;
  readonly p: number;
  readonly distinctKeys: number;
}

function designWidthAndKeys(rows: readonly OfflineRow[]): { p: number; distinctKeys: number } {
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const design = buildDesign(baseRows);
  const keys = canonicalRowKeys(baseRows);
  return { p: design.names.length, distinctKeys: new Set(keys).size };
}

function runDose(dose: OrderDose, anchorMultiset: string, anchorP: number, anchorKeys: number): DoseResult {
  check(`R23C-ordx[${dose.label}].multiset-identity`, canonicalMultiset(dose.rows) === anchorMultiset);
  const { p, distinctKeys } = designWidthAndKeys(dose.rows);
  check(`R23C-ordx[${dose.label}].p-invariant`, p === anchorP);
  check(`R23C-ordx[${dose.label}].distinct-keys-invariant`, distinctKeys === anchorKeys);
  const expected = fitLogitAdditive(dose.rows, PERF_OPTIONS);
  const prodMs: number[] = [];
  for (let rep = 0; rep < 2; rep++) {
    const t0 = performance.now();
    fitLogitAdditive(dose.rows, PERF_OPTIONS);
    prodMs.push(performance.now() - t0);
  }
  const pools: PoolReading[] = [];
  for (let rep = 0; rep < 2; rep++) {
    STATS.reset();
    const actual = fitInst(dose.rows, PERF_OPTIONS);
    if (rep === 0) compareReports(`R23C-ordx[${dose.label}]`, expected, actual);
    pools.push(snapshotStats());
  }
  const first = pools[0]!;
  const second = pools[1]!;
  // Deterministic structural counts across instrumented reps.
  check(`R23C-ordx[${dose.label}].count-determinism`,
    first.fits === second.fits &&
    first.irlsIterations === second.irlsIterations &&
    first.successfulDraws === second.successfulDraws &&
    first.collapsedDraws === second.collapsedDraws &&
    first.singularDraws === second.singularDraws &&
    first.apcActivePairs === second.apcActivePairs &&
    first.onProbSigmoids === second.onProbSigmoids &&
    first.drawsMapOps === second.drawsMapOps);
  // Structural cap witnesses.
  check(`R23C-ordx[${dose.label}].onprob-count-identity`,
    first.onProbSigmoids === 400 * (1 + first.successfulDraws));
  check(`R23C-ordx[${dose.label}].onprob-cap`, first.onProbSigmoids <= 80_400);
  check(`R23C-ordx[${dose.label}].tail-count-identity`,
    first.drawsMapOps === (p + 1) * first.successfulDraws);
  check(`R23C-ordx[${dose.label}].tail-cap`, first.drawsMapOps <= 12_200);
  check(`R23C-ordx[${dose.label}].iter-cap`, first.irlsIterations <= first.fits * 50);
  check(`R23C-ordx[${dose.label}].apc-pairs-cap`, first.apcActivePairs <= first.fits * 400 * 4);
  return { label: dose.label, prodMs, pools, diagnosis: expected.diagnosis, p, distinctKeys };
}

function meanOf(values: number[]): number {
  return values.reduce((a, b) => a + b, 0) / values.length;
}

function scenarioOrdx(): void {
  out(`probe ORDX (input presentation-order / row-permutation dose response; NEW axis, not a lane):`);
  const anchorRows = perfRows();
  const anchorMultiset = canonicalMultiset(anchorRows);
  const { p: anchorP, distinctKeys: anchorKeys } = designWidthAndKeys(anchorRows);
  out(`  anchor invariants: p=${anchorP} distinctKeys=${anchorKeys} (asserted permutation-invariant per dose)`);
  const doses: OrderDose[] = [
    { label: "anchor", rows: anchorRows },
    { label: "reversed", rows: [...anchorRows].reverse() },
    { label: "sorted", rows: sortedRows(anchorRows) },
    { label: "shuffle-aaaa", rows: shuffled(anchorRows, 0xaaaa) },
    { label: "shuffle-5555", rows: shuffled(anchorRows, 0x5555) },
  ];
  const results: DoseResult[] = [];
  for (const dose of doses) {
    results.push(runDose(dose, anchorMultiset, anchorP, anchorKeys));
  }
  const poolKeys = [
    ["solve", (r: PoolReading) => r.solveMs],
    ["irls-other", (r: PoolReading) => r.irlsMs - r.solveMs],
    ["onProb", (r: PoolReading) => r.onProbMs],
    ["APC", (r: PoolReading) => r.apcMs],
    ["sampling", (r: PoolReading) => r.samplingMs],
    ["tail", (r: PoolReading) => r.tailMs],
    ["rest", (r: PoolReading) => r.totalMs - r.irlsMs - r.onProbMs - r.apcMs - r.samplingMs - r.tailMs],
  ] as const;
  for (const res of results) {
    const m = (f: (r: PoolReading) => number): number => meanOf(res.pools.map(f));
    const first = res.pools[0]!;
    out(
      `  dose order=${res.label}${res.label === "anchor" ? " (anchor)" : ""}: ` +
      `fits=${first.fits} iters=${first.irlsIterations} successful=${first.successfulDraws} collapsed=${first.collapsedDraws} ` +
      `singular=${first.singularDraws} apcPairs=${first.apcActivePairs} onProbSigmoids=${first.onProbSigmoids} ` +
      `drawsMapOps=${first.drawsMapOps} diagnosis=${res.diagnosis}; ` +
      `prod ${res.prodMs.map((t) => t.toFixed(1)).join("/")} ms; inst total ${m((r) => r.totalMs).toFixed(1)} ms | ` +
      poolKeys.map(([name, f]) => `${name} ${m(f).toFixed(1)}`).join(" | ")
    );
  }
  // Dose-response arithmetic: (a) worst COUNT-DRIVEN sub-band pool reading
  // across every dose vs the 35 ms band (solve/irls are frozen pools and
  // the APC combination is floor-anchored by the R14-C rule at the
  // production shape — their rows are descriptive only, exactly like
  // SCALEX's, YMIX's, and SEEDX's); (b) per-iteration price of the frozen
  // pools per dose (order-invariance witness: the presentation order
  // reaches the frozen pools only through the maxIter-capped iteration
  // count, plus the support-mix microshift priced below).
  out(`  order-dose arithmetic (35 ms band; count-driven sub-band pools only):`);
  for (const [name, f] of poolKeys) {
    if (name === "solve" || name === "irls-other" || name === "APC") continue;
    let worst = 0;
    let worstLabel = "";
    for (const res of results) {
      const v = meanOf(res.pools.map(f));
      if (v > worst) {
        worst = v;
        worstLabel = res.label;
      }
    }
    out(`    ${name}: worst ${worst.toFixed(1)} ms at order=${worstLabel} (${worst < 35 ? "sub-band" : "OVER BAND"}, x${(35 / worst).toFixed(1)} below 35)`);
  }
  {
    const f = poolKeys[3][1];
    const values = results.map((res) => meanOf(res.pools.map(f)));
    const pairs = results.map((res) => res.pools[0]!.apcActivePairs);
    out(
      `    APC (descriptive; closure floor-anchored, not count-based): ` +
      results.map((res, i) => `${res.label}:${values[i]!.toFixed(1)}`).join(" ") +
      ` ms — active pairs ${pairs.join("/")} (structural cap 321,600 = x${(321600 / Math.min(...pairs)).toFixed(2)} of worst dose)`
    );
  }
  {
    const iters = results.map((res) => res.pools[0]!.irlsIterations);
    const anchorIters = results.find((r) => r.label === "anchor")!.pools[0]!.irlsIterations;
    const worstIters = Math.max(...iters);
    const capIters = results[0]!.pools[0]!.fits * 50;
    out(
      `    iteration counts across doses: ${results.map((res, i) => `${res.label}:${iters[i]}`).join(" ")} — ` +
      `cap ${capIters} (fits x 50); worst dose ${worstIters} = ${(100 * worstIters / capIters).toFixed(1)}% of cap; ` +
      `max order amplification x${(capIters / anchorIters).toFixed(3)} vs anchor`
    );
  }
  for (const [name, f] of [poolKeys[0], poolKeys[1]] as const) {
    const prices = results.map((res) => {
      const v = meanOf(res.pools.map(f));
      const iters = res.pools[0]!.irlsIterations;
      return (v / iters) * 1e3; // µs per iteration
    });
    const lo = Math.min(...prices);
    const hi = Math.max(...prices);
    out(
      `    ${name} per-iteration price (µs/iter across doses): ` +
      results.map((res, i) => `${res.label}:${prices[i]!.toFixed(1)}`).join(" ") +
      ` — spread ${(100 * (hi - lo) / lo).toFixed(1)}% (order reaches the frozen pools only through the iteration count and the support-mix microshift)`
    );
  }
}

/* --------------------------------- main --------------------------------- */

scenarioEquivalence();
scenarioDeterminism();
scenarioProfile();
scenarioOrdx();

out(`APC pool median this run: ${APC_POOL_MEDIAN.toFixed(1)} ms (R22-C band 46.4-47.0; re-anchor floor only if outside)`);

if (failures > 0) {
  process.stderr.write(`${failures} CHECK(S) FAILED\n`);
  process.exitCode = 1;
} else {
  out(`\nALL EQUIVALENCE CHECKS PASSED (${checksPassed} bitwise checks)`);
}
```

### B. APC 地板重锚定加测 `/tmp/r23c-apcfloor.mts`

```ts
/**
 * R23-C addendum: APC pool floor re-anchor on THIS VM (R14-C APCX/FLOORX
 * method, applied because the three instrumented APC pool readings
 * (46.2 run A / 46.4 run B / 48.0 run C) exited the R22-C re-anchored band
 * 46.4-47.0 on BOTH sides (run A below, run C above) — the R14-C
 * disposition rule re-anchors the floor whenever the pool exits the band,
 * in either direction; R14-C/R18-C/R21-C re-anchored on above-band
 * excursions, R17-C/R19-C/R20-C/R22-C on below-band ones). This is a floor
 * measurement, NOT a lane: S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 stay closed
 * and no stacked candidate is built (CMB stays closed). The dispatch also
 * requires the sink to match 7.309 bitwise (cross-round determinism
 * witness; R14-C, R16-C, R17-C, R18-C, R19-C, R20-C, R21-C, and R22-C all
 * read 7.309 for both sinks).
 *
 * Run: npx tsx /tmp/r23c-apcfloor.mts
 *
 * Structure identical to the R14-C appendix APCX/FLOORX block (verbatim
 * landed base fit via a plain irls replica + production-identical APC sweep
 * body; 5 warmup + 60 timed sweeps each, sinks accumulated across both),
 * with solve as a verbatim replica of the landed lin-alg.ts. Three runs of
 * this script produced (this VM):
 *   APCX 55.8 / 53.8 / 55.4 ms; FLOORX 40.0 / 39.2 / 39.4 ms;
 *   ceiling 15.8 / 14.6 / 16.0 ms — all < 35; sink=7.309 AND
 *   floorSink=7.309 in every run, bitwise-matching R14-C / R16-C / R17-C /
 *   R18-C / R19-C / R20-C / R21-C / R22-C (cross-round determinism witness,
 *   dispatch-mandated closure condition). Full source follows the R14-C
 *   appendix APCX/FLOORX block verbatim (the building blocks —
 *   buildDesign / computeSupports / canonicalRowKeys / solvePlain / plain
 *   landed irls / fixtureRng(0xbeef) perf fixture — are byte-identical to
 *   Appendix A above minus instrumentation); the sweep bodies and rep
 *   structure (5 warmup + 60 timed, per sweep) are reproduced unchanged
 *   from R14-C. Omitted here for length — the six output lines above are
 *   the complete addendum evidence.
 */
```

MORE_OPTIMA=no
BRANCH=cursor/r23-c-offline-routing-twenty-third-pass-83a1
