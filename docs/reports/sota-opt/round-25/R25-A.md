MODEL_SLUG=claude-fable-5-thinking-xhigh

# R25-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第二十五遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 25 / R25-A（Round 1–24 同区第二十五遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `74a0f93`（开工于 `aec794e`——含 R24-H 空枚举收口、Round 25 开卷与 R25-A 派出；运行中 exclusive 快进两笔 docs-only 提交：R24-I 空枚举收口 + adaptation-state coordinate 禁移植扩栏，按令 fast-forward 至新 tip，冻结主张全部在 `74a0f93` 复验；R24-J 在飞未触碰）
**分支:** `cursor/r25-a-tracking-twenty-fifth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S25-A-\* ID，切片零代码改动；
整片预算收口第二十三次复核成立，并新增一条实测轴——「值域 / 字符串
内容类普查」（axis 14）补上二十四遍搜查史里字符串输入面的最后一个
维度：R24-A 定价了列表字段的**计数**维（N_op / N_cl / W×O / Q×D /
CF），R16-A 定价了固定计数下的**字节长度**维（L / V / S），R15-A +
R24-A 13f 定价了参数规模维——但**内容类**（固定计数、固定长度下，
字节本身是什么）从未被剂量：切片有三个消费者的每字节单价是内容
敏感的——(i) `SUCCESS_CLAIM` 正则扫描（prescore.ts:28；生产每 gate
3 扫 = from-child 外层 prescore + turn 内层 prescore + derived-gap
第三扫）的**匹配位置**（早退点）、**近失配密度**（关键词前缀逼出
部分匹配尝试）与**字母表宽度**（单字节 vs 双字节）；(ii)
`hashAssessment` 的 JSON.stringify（types.ts:501，切片第一大组件
~37–38% ×3/gate）的 **JSON 转义类**（引号/反斜杠/控制符）与**编码
类**（CJK 双字节）；(iii) includes 相等扫描（prescore.ts:116/123/
129、roller.ts:34）的**分歧点纹理**（等长候选的共享前缀 memcmp）。
本轮建立确定性内容类剂量驱动器给出四组定价（三次独立运行区间）：
**14a** 正则扫描类谱 @L=4096：match@0 早退 0.001–0.003 ns/char、
match@end ≈ no-match plain 0.506–0.513 ns/char（早退在匹配点，
end/0 比 ×3.59–4.05）、near-miss 最坏类 0.785–0.847 ns/char/scan
（plain 的 ×1.40–1.49）、双字节反而 ×0.43–0.46（0.129–0.130
ns/char）；10 ms/run 穿越（5 gate × 3 scans）claim 字节 ≈
7.9–8.5×10⁵（生产 9–17 字节，~4.7 个量级）；**14b** hash 转义/编码
类谱 @turnId=1024 字节（生产铸造面直驱）：单一转义类几乎免费
（plain 3.69–3.80 / quote 3.76–3.78 / backslash 3.69–3.87 /
control 3.81–3.96 / two-byte 3.79–4.03 ns/char，×0.98–1.04），
**唯 mixed 类（双字节+转义混合）×1.42–1.44**（6.18–6.23 ns/char）；
e2e 回声：mixed-1024 turnId 的 applyTrackingGate 19.24–19.88 µs
vs 生产形状 5.92–6.21 µs（surcharge 恰乘在门内两条 hash 腿 +
写侧 parse 上，线性、schema 封顶）；10 ms/run 穿越（×3 hash 腿 ×
5 gate）id 字节 ≈ 1.07–1.08×10⁵（生产 9 字节，~4 个量级）；
**14c** 分歧点剂量 @Cc=256×256 字节 id：长度失配 861–997 /
早分歧 4115–4248 / 晚分歧 2824–3000 ns，晚-短差 1963–2003 ns ⇒
~30.1–30.7 ps/前缀字符——且**早分歧格稳定高于晚分歧格**（三次运行
同号），分歧点响应在这些形状上**非单调**（引擎相等比较的分块/表示
纹理，如实记录）；纹理是 R15-A/R24-A includes 家族价的乘数
（≤ ~id 长度倍），无独立悬崖；**14d** 生产长度 worst-class e2e 回声：
9 字节 mixed 类 summary+taskId 全管线格 12.6–13.2 µs vs plain 锚点
15.4–15.6 µs（delta −2.3~−2.8 µs，负号=尾格 PIC 热度红利）——内容类
在生产尺度零牵引。五组确定性证人（F5a–F5e：700 内容类形状判决
不变性、hash 值纯性（字面量/拼接/切片同值同哈希 + 40/40 单字符
雪崩）、isSuccessClaim 真值表 40/40（含 "compass"/"surpassed"
子串语义钉死为冻结契约）、分歧点语义 40/40×2、生产 18 字符串全
ASCII 免转义 max 17 字节普查）三次运行逐字节一致。该轴上全部可想
摊平路经换名检查逐一判为既有排除 ID 的本体（5 项新记录，见 §0），
**只产出归档证人与内容类格网的闭合，未产出可落地候选**——最坏类
穿越点距生产 4–4.7 个量级、生产恰坐在每个类维度的最便宜格
（F5e 钉死），按令即空收口。**

切片 14 个文件自 R1-A 基线（`7acb666`）经 R2-A … R24-A 至本轮基线
（`74a0f93`）**逐字节未变**（`git diff 7acb666..74a0f93 -- src/tracking
src/run/child-tracking.ts src/run/gate-apply.ts` 为空，二十五遍全程零
diff；清单核对恰 14 文件（`ls src/tracking | wc -l` = 12 + 2）并全部
实际重读）。调用面维持最强冻结并再延一轮：R10-A 基线（`89f0455`）以来
`git diff 89f0455..74a0f93 -- src/` **完全为空**（跨 R10…R25 开轮十六段
全部为 docs 提交），生产调用方经本轮全库 grep 复核仍为
`supervisor.ts:483`（applyTrackingGate/nextTrackingSeq；两个 settle
调用点 601/711 均不传 trackingAssessment ⇒ 生产 O(1) no-op，第十六次
复验，本轮源码级重读 `settleSupervisedOutcome` 首行
`if (assessment === undefined) return`）/ `coordinator.ts:444` /
`flowchart-run.ts:320`（applyChildThreeLine），每子结果一次
（~5 次/run），事件表几十级（41）。R24-A 的预算天花板（67–84 µs/run）
经本轮在当前 VM（Node v22.22.2；本 VM 默认 v22.14.0 且
`/exec-daemon/node` 垫片遮蔽 nvm，按 engines >=22.19.0 经 PATH 前置
nvm v22.22.2）实测复核为 **~69–89 µs/run**（五格锚点矩阵三次独立运行
13.9–17.9 µs/gate；上沿 89 由 run3 no-contract 首格 17.9 拉出——
R11-A 以来「PIC/基准次序」现象的本轮形态，run1/2 同格 16.2–16.5；
除该孤点外 ~69–82，与 R24-A 67–84 / R23-A 70–82 重叠同带；单格横向
比较不具意义；量级结论不变——稳态下即使切片零成本，节省上界
~0.07–0.09 ms/run，仍比落地线低约三个量级）。

本轮枚举出的全部角度经换名检查逐一判定为既有排除 ID 的换名/换位点或
数据补充（5 项新记录，见 §0；R12-A 13 项 + R13-A 8 项 + R14-A 3 项 +
R15-A 5 项 + R16-A 4 项 + R17-A 4 项 + R18-A 4 项 + R19-A 2 项 +
R20-A 3 项 + R21-A 2 项 + R22-A 5 项 + R23-A 5 项 + R24-A 5 项换名
检查全部复核维持，未追认任何 S12…S24-A-\* ID），按令一律不铸新 ID。

R12-A 分解的五大组件占比（hashAssessment×3 ~37–38% / validateEvent
~19–20% / turn ~11–12% / prescore×2 ~8.7–10.1% / O(E) 五扫描+拷贝
~1.8%）在本 VM 逐一复现（§1 表），R18-A skip 定价与 R19-A replay
定价抽测复现同带（guard-3 skip 2014–2078 ns = 13.0–13.3% of apply；
replay find#1 3087–3125 ns = 20.0–20.1% of apply）。第二十五遍收口，
**MORE_OPTIMA=no**。

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config /
  from-child / gates / human-score / index / isolation / prescore /
  roller / turn / types）+ `src/run/child-tracking.ts` +
  `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。
  未触碰其余 `src/run/`（G 切片）及任何其他切片；未触碰在飞的
  R24-J 分支与文件（R24-I 于本轮运行中由 exclusive 收口合入，其
  报告与分支同样未触碰），未触碰已收口的 R24-\* 报告。
- 先读并遵守（顺序强制）：README / EXCLUSIONS.md（全表 546 行，含
  S13-B-1；R12-A…R24-A 空枚举未铸 ID，确认）/ PROGRESS.md /
  round-25/PLAN.md / round-24/PLAN.md / R24-A / R23-A / R22-A。
  EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
- 环境：本 VM 默认 Node v22.14.0（`/exec-daemon/node` 垫片遮蔽 nvm），
  按 engines（>=22.19.0）以 PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 切至 v22.22.2 后
  `pnpm install --frozen-lockfile`（pnpm 10.17.1），全部测量在
  v22.22.2 完成。
- **不移植禁令逐项核对**：R24-B M=1 catalog collapse（B）/ **OCCX**
  （C）/ R24-D approval-authority face（D）/ **JB**（E）/ R24-F
  state serialization-boundary provenance face（F）/ **RUNLIM**（G）/
  **R24-H operand-residency census（H）**/ R24-I adaptation-state
  coordinate（I；运行中随 exclusive 快进新增的禁移植栏，axis 14 与其
  无关——本轴剂量字符串**值**在固定调用面上的每字节单价，不触碰任何
  adaptation 装载/接线状态坐标）均未移植为 A ID。与 R24-H
  的边界特别声明：**axis 14 的全部夹具字符串为预构建、平坦、复用的
  固定对象，只有值变化——操作数身份轮换 / 新鲜配对 / 堆代驻留
  （OR/OF/OT）一律未剂量**（F5b 恰好从反面钉死该边界：字面量/拼接/
  切片三种构造同值同哈希——表示与新鲜度对本切片耐久契约不可见）。
  CALLB / CKMIX / ORDX / SEEDX / YMIX / SCALEX / FITQ / TERMCLASS /
  SCHEDWIN / XPROC / NAMESHAPE / SHAPEK / KFAN / NVG / GS 未编号。
- **换名重提检查**（本轮枚举全部角度的逐项裁决；识别为既有方案换名/
  换位点或数据补充，**一律不铸 S25-A-\* ID**。R12-A…R24-A 共 63 项
  换名检查全部复核维持，不再逐条重列；以下 5 项为本轮**新记录**，
  全部由 axis 14 的内容类视角引出）：
  1. **SUCCESS_CLAIM 换手写扫描 / 首字符卫**（near-miss 类 ×1.40–1.49
     与 no-match 全扫价的直接摊平路——手写 indexOf 链或可打印首字符
     预筛）= **S1-A-3 家族**（human-score 正则早退/惰性——A 切片
     正则站点的既有拒绝）+ S7-F-2 / S9-B-2 首字符卫家族的 A 换位点；
     生产 N_cl=1、L≈9–17 单字节 ⇒ 全类差 <1 ns，拒列；
  2. **claims 匹配早退重排 / 按类分派**（match@0 早退红利 ×3.59–4.05
     的摊平路——把最可能匹配的 claim 前置或先测首词）= claims 次序是
     公开输入契约（重排是**功能性变更**——some 的短路次序可观察）+
     S1-A-2/S3-A-3 扫描融合家族本体，拒列；
  3. **hashAssessment 免转义手写序列化 / 预转义缓存**（mixed 类
     ×1.42–1.44 的摊平路）= **S5-A-2 字面本体**（手写定长序列化实测
     慢 11–15%）+ X1-1（预转义/intern 隐藏缓存；生产 id 恒 9 字节
     ASCII ⇒ 命中收益恒 0），拒列；
  4. **id 规范化 / 截断 / intern**（14c 长 id 分歧纹理与 14b 长
     turnId 的摊平路）= S2-A-1 截断家族 + **功能性变更**（episodeId/
     runId/turnId 是外部契约字节，哈希与幂等键随之改变）+ X1-1
     intern 本体，拒列；
  5. **四处 includes 站点 Set 化**（14c 的直接摊平路，与 R24-A §0-1
     同站点）= **S1-A-4 / X4-7 家族本体**（R15-A 轴 1 重开条件
     C/R ≈ 10³ 未触发；且分歧纹理只是家族价的乘数，不改变重开
     条件的规模坐标），拒列。
- 其余枚举角度全部落在既有 ID 的**本体**上（非换位点，无需新记录）：
  幂等双 find/死扫描 = S8-A-1 / S11-A-2；episode/seq/status/copy 五
  扫描逐条 = X0-4/X2-4/S1-A-1/S1-A-9/R1-A 清单（R12-A 起锁定）；写侧
  validateEvent = S4-G-2 族（实现在 events.ts，G 切片）；第二次
  prescore = S1-A-2；turn 恒定输入常量折叠 = R12-A §0-11 伞 +
  R16-A §0-2；skip 守卫次序/享元/prescore 复用 = R18-A §0-1/2/4
  维持；replay 快路径/重算去重 = R19-A §0-1/2 维持；hashSummary
  缓存/增量 = R20-A §0-2/3 维持；H 面 = R21-A 维持；分析-隔离面 =
  R22-A §0-1..5 维持；G×E 积分账本五路 = R23-A §0-1..5 维持；
  输入面字段粒度五路 = R24-A §0-1..5 维持。
- 未重开任何 X\* / S1-\* … S24-\* 条目。**R24-A residual input-face
  list-dimension 普查（N_op 5.89–6.03 ns/op；N_cl 29.80–31.38
  ns/claim×3；W×O / Q×D 生产流量 0）不补铸、不重测**——axis 14 在
  **固定计数**（N_cl=1 或 +1 条剂量 claim）下只动内容类，与计数维
  正交；**R23-A G×E 自耦合积分账本不补铸**（axis 14 全程固定
  E=41）；**R22-A 公开导出普查 + 分析-隔离面不补铸**；**R21-A H
  输入面不补铸**（axis 14 全程 humanInput ≡ `{}`）；**R20-A
  openMinors M 轴与驻留链 K 不补铸**；R19-A 重投递 / R18-A 拒绝面
  只做组成连续性抽测各一格；R17-A 组成轴不重测；R16-A L/V/S **不
  重测**（14a/14b 是其字节价的**类分解**，基线格直接对照生产形状
  而非复测其剂量曲线）；S11-A-1 / S11-A-2 / S10-A-1 / S9-A-1 /
  S8-A-1..3 / S7-A-1..4 / X0-4/X2-4 / X0-6 / S6-A-1 专项复核：本轮
  零触碰、零重提，全部维持关闭。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点五格
  （gate-outcome × contract × max-codes）覆盖；配置态逐面复核——
  (a) settle 带/不带 assessment：生产两调用点 601/711 均不带
  （O(1) no-op 源码级复验），带 assessment 臂即 applyTrackingGate
  本体（已价）；(b) trackingConfig 覆写/applyUserThreshold：生产
  from-child 恒走 DEFAULT（R22-A config 双臂已价，X0-7 阈值冻结
  维持）；(c) maxItems 截断态 = S2-A-1 已拒面；(d) policyVersion 为
  透传字符串无分支。axis 14 剂量补充覆盖了字符串输入面的**内容类**
  格网（见 §1），无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 /
  minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——
  本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无
  命令权、H/score 不写路由、live = R0 等价、双 LCB 双归因保留、提升
  proposal-first）同样天然满足——axis 14 只经公开
  computePrescore/runTrackingTurn/hashAssessment/applyTrackingGate/
  applyChildThreeLine 剂量，F5a–F5e 是对既有判决不变性/哈希值纯性/
  正则契约/相等语义的**分布级回声验证**，未改任何阈值、未改任何
  测试。不声称 Outcome-supported；Checkpoint F-PROD 仍开放
  （ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本（按令不建
  `scripts/round25-r25a-equivalence-sim.ts`），仿真全文见本报告附录。
  不开 PR。

## 1. 预算支配论证复核 + 一条新轴（方法第 1、2 步）

R3-A §3 … R24-A §1 的整片预算论证依赖两个前提，本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666..74a0f93 -- <切片>` 为空，逐字节
   一致（二十五遍全程零 diff；开工 tip `aec794e` 与快进后 tip
   `74a0f93` 两点均验空）——R1-A…R24-A 全部测量与排除按令转移，
   但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨十六段）**：`git diff 89f0455..74a0f93 --
   src/` **完全为空**（R10 开轮以来全部提交均在 docs/ 下）。生产调用方
   三点位（supervisor.ts:483 / coordinator.ts:444 /
   flowchart-run.ts:320）、settle 调用点 601/711 不传 assessment
   （首行 `if (assessment === undefined) return`）、
   `parseTrackingAssessment` 唯一 src/ 调用方 events.ts:738、
   `runTrackingTurn` 唯一 src/ 调用方 from-child.ts:81（humanInput 恒
   `{}`）、`computePrescore` 切片外零 src/ 调用方、`proposeFromAnomaly`
   / `sanitizePacketForAnalysis` / isolation 双函数 /
   `executionAuthority` 切片外零 src/ 调用方、`extractHumanScore` /
   `hashSummary` 切片外零 src/ 生产调用方，经本轮全库 grep 全部复验，
   无新增调用方。

**锚点矩阵（本 VM 重锚，三次独立运行，E=41，~5 gate/run，含 R14-A
第五格 max-codes）：**

```text
                                   run1      run2      run3
PASSED x no-contract               16.2      16.5      17.9  µs/gate (+1 event)
PASSED x contract(3)               15.4      15.5      15.6  µs/gate (+1 event)
FAILED x no-contract               15.6      15.2      15.5  µs/gate (+3 events)
FAILED x contract(3)               14.1      14.4      13.9  µs/gate (+3 events)
FAILED x contract(3) x 2-code      15.1      14.5      14.7  µs/gate (+3 events)
=> ~69–89 µs/run（稳态热；除 run3 首格 17.9 上沿外 ~69–82）
```

与 R24-A（13.4–16.9 µs/gate ⇒ 67–84 µs/run）/ R23-A（70–82）重叠
同带；五格三次运行**无悬崖、无空洞**。本轮仿真在锚点前多驱动了
axis 14 的内容类形状（长 claim 六类 / 长 turnId 六类 / 分歧三类），
PIC 污染路径与 R21-A / R23-A / R24-A 同型（首格上沿，run3 17.9），
已按 R11-A「PIC/基准次序」家族记录。稳态下即使把整个切片优化到零
成本，节省上界 ~0.07–0.09 ms/run，仍比战役落地线（数十~数百 ms 或
复杂度类下降）低**约三个量级**。**支配论证（稳态口径）第二十三次
复核成立**；生产冷态口径维持 R17-A 轴 6b 裁决（[2.7, 8.5] ms/run
once-per-run，不重测）。

**合同地板分解复核**（R12-A 方法学，PASSED×contract(3) 生产格，三次
运行区间；与 R24-A 括号内区间逐项同带）：

| 组件 | 本轮实测/gate | R24-A | 占比* | 锁定裁决 |
| --- | --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1891–1966 ns 每次 ⇒ **5.67–5.90 µs** | 5.93–6.04 µs | ~37–38% | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁 |
| `validateEvent`(TRACKING_ASSESSMENT) 整体（内含 parse 315–374 ns + 第三次 hash） | **3.01–3.14 µs** | 2.77–2.81 µs | ~19–20% | S4-G-2 族 schema 屏障；实现在 events.ts（切片外，G） |
| `runTrackingTurn` 完整（R13-A 八子组件全点名） | **1.72–1.91 µs** | 1.69–1.72 µs | ~11–12% | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构 |
| `computePrescore` × 2 | 673–790 ns 每次 ⇒ **1.35–1.58 µs** | 1.33–1.34 µs | ~8.7–10.1% | S1-A-2 |
| O(E) 五扫描 + 拷贝（episode 78–80 + seq 46 + 双幂等 find 39–41×2 + status 50 + `[...events]` 27–29 ns） | **279–284 ns**（⇒ 6.82–6.92 ns/event @E=41） | 278–284 ns | **~1.8%** | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界 |

\* 以当轮 contract 格（15.4–15.6 µs）计。`applyTrackingGate` 整体
隔离实测 5.92–6.21 µs，与 R22-A（5.93–6.01）/ R24-A（5.84–5.95）
同带。

**R18-A skip / R19-A replay 定价抽测复现**（各取代表格，20k 次/格，
三次运行区间）：guard-3 skip（FAILED 零 evidence refs）2014–2078 ns
= 13.0–13.3% of apply（R24-A 12.7–14.3% 同带）；replay find#1
（GT idempotencyKey 命中，E=44）3087–3125 ns = 20.0–20.1% of apply
（R24-A 20.6–21.5% 同带）——组成天花板 {apply, skip, replay} 两个半边
继续成立，不重测其余格。

**本轮新轴 14：值域 / 字符串内容类普查**（确定性剂量经公开
computePrescore / runTrackingTurn / 生产铸造面（mkChild →
observationFromChild → assessChildObservation）→ hashAssessment /
applyTrackingGate 驱动，固定 E=41、固定计数与 R24-A 计数维正交；
全部夹具字符串预构建平坦复用，残留不剂量（R24-H 边界）；三次运行
区间）：

```text
证人（F5a–F5e 钉死，三次运行证人行逐字节一致）:
  F5a: 700 个种子化内容类 claim 形状（关键词任意位置/大小写/字母表、
    转义密集、近失配、双字节）——维度向量 / P / quality / coverage /
    cappedByHardFail 全不变 700/700；turn gate 恒 none 且 score === P
    （匹配类 claim 在干净生产纹理上不可能翻判决——evidenceOutcome 的
    successClaim 合取需要 failedTool 或零 observedPass 才 FAIL）
  F5b: hash 值纯性——字面量/逐字符拼接/切片三种构造的同值 turnId
    哈希逐字节相同（638b913）；40 个种子化位置单字符翻转全部翻哈希
    （40/40 雪崩）；表示与新鲜度对耐久契约不可见（residency 轴
    留在 R24-H，未移植）
  F5c: isSuccessClaim 真值表——4 关键词 × 种子化位置/大小写/字母表
    全命中（40/40）；near-miss 单元（pas/verifie/succee 重复串）、
    plain、双字节填充永不命中；"compass"/"surpassed" 命中钉死子串
    语义为冻结契约
  F5d: 分歧点语义——真 needle 在任意种子化数量的 255/256 共享前缀
    诱饵之后仍满足 coverage（40/40）；纯诱饵永不满足（40/40）；
    长前缀逐字节相同的 write/owned 路径 scope 恒 PASS
  F5e: 生产内容类普查——from-child 路径喂给切片的全部 18 个字符串
    均为免转义单字节 ASCII，最长 17 字节；干净生产 claim 不匹配
    SUCCESS_CLAIM（最坏匹配纹理只在 FAILED "all checks passed"
    类，~17 字节）：生产坐在每个类维度的最便宜格（以下剂量是公开
    API 面定价，非生产复定价）

14a  SUCCESS_CLAIM 扫描内容类谱（claims=[base,dose] 之 dose 格，
     L=4096，prescore 单扫 delta；turn 格含内层 prescore +
     derived-gap 两扫，生产每 gate 3 扫 = 外层 prescore 又一扫）:
  match@0           683–803 ns   (delta    2–12 ⇒ 0.001–0.003 ns/char)
  match@mid        1741–1856 ns  (delta 1050–1070 ⇒ 0.256–0.261)
  match@end        2764–2879 ns  (delta 2072–2093 ⇒ 0.506–0.511)
  no-match plain   2774–2894 ns  (delta 2078–2103 ⇒ 0.507–0.513)
  no-match near-miss 4010–4141 ns (delta 3216–3470 ⇒ 0.785–0.847)
  no-match two-byte 1204–1328 ns (delta  527–533 ⇒ 0.129–0.130)
  => 类谱：near-miss/plain ×1.40–1.49、two-byte/plain ×0.43–0.46
     （双字节反而更便宜——引擎双字节扫描在该模式上的实测纹理，
     如实记录）、match@end/match@0 ×3.59–4.05（早退在匹配点）；
     10 ms/run 穿越（最坏 near-miss 类 × 3 scans/gate × 5 gate）
     claim 字节 ≈ 7.9–8.5×10⁵（生产 9–17，~4.7 个量级）

14b  hashAssessment JSON 转义/编码类谱（turnId=1024 字节六类，
     生产铸造面直驱；delta 对生产 9 字节格；plain-1024 为类基准）:
  plain            5772–5808 ns  (3.69–3.80 ns/char, ×1.00)
  quote-heavy      5764–5823 ns  (3.76–3.78, ×0.99–1.01)
  backslash-heavy  5688–5874 ns  (3.69–3.87, ×0.98–1.01)
  control-heavy    5853–5969 ns  (3.81–3.96, ×1.01–1.03)
  two-byte         5825–6040 ns  (3.79–4.03, ×1.01–1.04)
  mixed            8245–8304 ns  (6.18–6.23, ×1.42–1.44)
  e2e 回声: mixed-1024 turnId 的 applyTrackingGate 19.24–19.88 µs
    vs 生产形状 5.92–6.21 µs——surcharge ≈ 门内两条 hash 腿 ×
    6.3–6.4 µs + 写侧 parse（含 trim），线性、schema 封顶
  => 单一转义类几乎免费（引擎 stringify 的转义写出与单字节快路径
     同价带），唯双字节+转义混合类 ×1.42–1.44；10 ms/run 穿越
     （×3 hash 腿 × 5 gate，最坏 mixed 类）id 字节 ≈ 1.07–1.08×10⁵
     （生产 9，~4 个量级）

14c  includes 分歧点剂量（prescore.ts:123 相等扫描；R=1，Cc=256 pad，
     256 字节等长 id；×2/turn + derived-gap 第三扫）:
  长度失配类        861–997 ns
  早分歧类（char 0） 4115–4248 ns
  晚分歧类（char 255）2824–3000 ns
  => 晚-短差 1963–2003 ns / (256 诱饵 × 255 前缀字符) ≈
     30.1–30.7 ps/前缀字符；**早分歧格稳定高于晚分歧格（三次同号）
     ——分歧点响应非单调**（引擎相等比较在该形状上的分块/表示纹理，
     如实记录；不影响量级）；纹理是 R15-A/R24-A includes 家族价的
     乘数（≤ ~id 长度倍），家族穿越点坐标不变，无独立悬崖；
     生产 id ≤ 32 字节且首词内分歧

14d  生产长度 worst-class e2e 回声:
  9 字节 mixed 类 summary+taskId 全管线 PASSED×contract 格
  12.6–13.2 µs vs plain 锚点 15.4–15.6 µs（delta −2.3~−2.8 µs，
  负号=尾格 PIC 热度红利，R11-A 家族）——内容类在生产尺度零牵引
```

五个结论：

1. **字符串输入面三维格网闭合**：计数维（R24-A：N_op/N_cl/W×O/Q×D/
   CF）× 长度维（R16-A：L/V/S）× **内容类维（本轮）**自此全部有价
   ——切片三个内容敏感消费者（正则扫描 / JSON 哈希 / 相等扫描）每个
   都有类谱与最坏类穿越点。
2. **正则扫描类谱（非重开）**：R16-A S 轴是单一纹理的字节价；本轮
   类分解显示最坏类（near-miss）只把它放大 ×1.40–1.49，最好类
   （match@0 早退）把它塌到 ~0；穿越点 7.9–8.5×10⁵ 字节距生产
   ~4.7 个量级。
3. **哈希转义类谱（非重开）**：第一大组件 hashAssessment 的字节价
   对单一转义类不敏感（×0.98–1.04），唯 mixed 类 ×1.42–1.44；e2e
   回声证实 surcharge 恰乘在两条门内 hash 腿上；穿越点 1.07–1.08×10⁵
   id 字节距生产 ~4 个量级且 schema 封顶（R16-A L 轴结论加强）。
4. **分歧点纹理的诚实异常记录**：早分歧格稳定高于晚分歧格（三次
   同号）——includes 相等扫描的分歧点响应在 256 字节等长 id 上非
   单调；该纹理只是 S1-A-4/X4-7 家族价的乘数，重开条件坐标不变。
5. **不产生候选**：手写扫描/首字符卫 = S1-A-3 + S7-F-2/S9-B-2 家族
   （§0-1）；匹配早退重排 = 功能性变更 + S1-A-2/S3-A-3（§0-2）；
   免转义序列化/预转义缓存 = S5-A-2 + X1-1（§0-3）；id 规范化/
   截断/intern = S2-A-1 + 功能性变更 + X1-1（§0-4）；includes
   Set 化 = S1-A-4/X4-7（§0-5）。最坏类穿越点距生产 4–4.7 个量级 +
   生产坐在最便宜格 ⇒ 按令空收口。

**重开条件全景更新（在 R24-A §1 表基础上新增三行）：**

| 轴 | 生产规模 | 10 ms/run 穿越点 | 量级距离 | 来源 |
| --- | --- | --- | --- | --- |
| E（事件表，最廉混合） | 41 | ≈ 1.2–1.8×10⁵ | ~3.5–3.6 | R14-A |
| E（事件表，最坏合法混合） | 41 | ≈ 4.7–5.2×10⁴ | ~3.1 | R17-A |
| C（constraints） | 3 | ≈ 996–1,015 | ~2.5 | R15-A |
| R（required checks） | 1 | ≈ 1,020–1,037 | ~3.0 | R15-A |
| V（evidenceRefs） | 2 | ≈ 21,447–25,331 | ~4.0–4.1 | R16-A |
| S（summary 字节） | ~16–10² | ≈ 1.32×10⁶ | ~4.9 | R16-A |
| L（id 字节） | 后缀 ~8–36 | 无穿越点（schema 封顶） | — | R16-A |
| M（openMinors 条数） | 0 | ≈ 4.6–5.8×10⁴ | ~4+ | R20-A |
| K（驻留链 turn 数） | 0（恒 fresh） | ≈ 3×10³ turn | ~2 | R20-A |
| H_L / H_m / H_N（humanInput 三维） | 0 | ≈ 1.2 MB / 2.2–2.3×10⁴ / 4.65–4.74×10⁴ | ~3–5 | R21-A |
| T（analysis content 操作基数） | 0（面零流量） | ≈ 1.50–1.51×10⁵ | ~5 | R22-A |
| G（gates/run，固定 E 线性算术） | ~5 | ≈ 645–714 | ~2.1 | R16-A 系 |
| G（gates/run，含 G×E 自耦合二次项） | ~5 | ≈ 341–344 | ~1.8 | R23-A |
| N_op / N_cl / W×O / Q×D / CF / Cc/Rt 对角线外 | 1 / 1 / 0 / 0 / 2 / 原点 | 见 R24-A 表 | ~4.3–5.5 / 面须先获流量 | R24-A |
| **claim 内容类（最坏 near-miss，×3 扫/gate）** | **9–17 字节 ASCII（最便宜格）** | **≈ 7.9–8.5×10⁵ 字节** | **~4.7** | **本轮** |
| **turnId 转义/编码类（最坏 mixed，×3 hash 腿/gate）** | **9 字节 ASCII（最便宜格）** | **≈ 1.07–1.08×10⁵ 字节** | **~4** | **本轮** |
| **includes 分歧点纹理** | **短 id 首词内分歧** | **无独立穿越（家族乘数 ≤ ~id 长度；非单调如实记录）** | **—** | **本轮** |
| G 组成（apply/skip/replay 全混合） | 全 apply | 混入只降不升 | — | R18-A + R19-A |
| 冷进程预算（once-per-run） | 1 次/run | [2.7, 8.5] ms/run | — | R17-A |
| 拒绝面（once-per-fault） | 0 次/run | ~9.0–15.2 µs/fault | — | R18-A + R22-A |
| 重投递面（once-per-redelivery） | 0 次/run | ~2.8–3.4 µs/次 | — | R19-A + R23-A F3d 形态 |

**可操作重开优先级不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7
家族 `every×includes` 站点 Set 化）仍是第一优先重开对象——本轮
14c 的分歧纹理届时与 C 轴一并按家族程序重审（长 id 纹理会把家族
穿越点按 ≤id 长度倍压低，但生产 id 短且首词分歧，坐标未动）。本轮
关闭的是**内容类维**：R14–R16 关规模/载荷维、R17 关组成/温度维、
R18 关组成/故障维、R19 关结局维、R20 关驻留状态维、R21 关输入参数
维、R22 关公开导出维、R23 关轴间耦合维、R24 关字段粒度维之后，
字符串输入面的「计数 × 长度 × 内容类」三维格网自此全部有价。

## 2. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 5 项 + R12-A…R24-A
63 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、数据
补充或本体，按「换名不铸 ID」纪律未列任何 S25-A-\* 候选。空枚举与
R12-A…R24-A 同为战役允许的诚实结局。

## 3. 结构位置 × 消除机制满矩阵扫描（第二十五遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/
数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/
形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或
本轮 §0 换名检查；下表按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `prescore.ts` | **axis 14 主宿主之一**（14a SUCCESS_CLAIM :28 类谱六格——early-exit ×3.59–4.05 / near-miss ×1.40–1.49 / two-byte ×0.43–0.46；14c :123 分歧点三类——30.1–30.7 ps/前缀字符 + 非单调异常记录）；手写扫描/首字符卫 = §0-1（S1-A-3 家族）拒列；匹配早退重排 = §0-2（功能性变更）拒列；includes Set 化 = §0-5（S1-A-4/X4-7 本体）拒列；C/R 轴维持 R15-A 穿越点（可操作重开第一优先不变）；S6-A-1 / S8-A-3 / R12-A §0-2..5 / R24-A 13b/13c/13f 维持 | 无新候选 |
| `types.ts` | **axis 14 主宿主之二**（14b hashAssessment :484–502 转义/编码类谱六格——单类 ×0.98–1.04 免费、mixed ×1.42–1.44；F5b 值纯性 + 40/40 雪崩证人）；免转义手写序列化/预转义缓存 = §0-3（S5-A-2 + X1-1 本体）拒列；id intern = §0-4（X1-1）拒列；hash×3 地板复现 5.67–5.90 µs；写侧 parse 315–374 ns（asString.trim 的类敏感面已并入 14b e2e 回声）；X1-1/S8-G-2 memo 禁维持；S2-A-5 / X1-2 / S5-H-3 / S1-A-8 / X0-5 维持 | 无新候选 |
| `turn.ts` | 14a turn 格宿主（内层 prescore + derived-gap :180 两扫承载 3 扫乘数中的两扫；F5a turn gate 恒 none 证人 700/700）；三扫融合 = R24-A §0-2（S1-A-2+S3-A-3）维持拒列；R13-A 八子组件 / R12-A §0-11 / R16-A §0-2 / R20-A K 链 / R24-A 13a/13e 维持 | 无新候选 |
| `from-child.ts` | **F5e 生产内容类普查源**（prescoreInputFromObservation + assessment 铸造：18 字符串全免转义单字节 ASCII max 17；干净 claim 不匹配 SUCCESS_CLAIM 源码级+夹具级复验）；14b 剂量经其生产铸造面直驱（taskId→turnId）；id 规范化/截断 = §0-4 拒列；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `gate-apply.ts` | 14b e2e 回声宿主（mixed-1024 turnId 19.24–19.88 µs vs 生产 5.92–6.21——surcharge 两条 hash 腿线性；evidenceRefs/idempotencyKey 字节透传无类分支）；O(E) 清单 / S11-A-2 / S8-A-1 / S2-A-4 / R15-A §0-1 / R23-A §0-1..5 维持 | 无新候选 |
| `child-tracking.ts` | 锚点/分解宿主复测同带；14d 生产长度 worst-class e2e 格（12.6–13.2 µs 零牵引）；guard-3 skip 抽测 2014–2078 ns 复现（R18-A 7a 维持）；observationFromChild 字符串透传无类分支（F5e 上游） | 无新候选 |
| `roller.ts` | 本轮零新角度（axis 14 不触 Q/D 计数——14c 在 prescore 站点剂量，roller :34 是同 includes 家族的已价换位点，R24-A 13d 维持）；F5d 语义证人间接覆盖 uniqueStrings/filter 的相等语义；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 / R20-A previous 面维持 | 无新候选 |
| `gates.ts` | 本轮零新角度（gateFacts 是布尔面无字符串维度；F5a 下 gate 恒 none 证人）；S1-A-6 / S7-A-3 / S8-A-3 / R13-A §0-3 维持 | 无新候选 |
| `human-score.ts` | 生产零流量维持（axis 14 全程 humanInput ≡ `{}`；其正则站点的内容类是 R21-A H_L 轴的字节价所含，H 面不补铸）；§0-1 的 S1-A-3 家族本籍地；X0-6 / S1-A-3 / S7-A-1 / R14-A §0-2 维持 | 无新候选 |
| `combined-score.ts` | 本轮零新角度（输入是标量对，无字符串维度）；S6-A-1 / S7-A-4 / R13-A §0-1 舍入恒等维持 | 无新候选 |
| `config.ts` | 配置态矩阵复核宿主（§0 配置态 (b)：生产恒 DEFAULT；R22-A 双臂价维持）；X0-7 阈值冻结维持 | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方 grep 复验；其 JSON.stringify 的内容类面与 14b 同型但零流量，不另立格）；S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R22-A 隔离三函数价维持（零 src/ 调用方复验）；index.ts 纯再导出零逻辑；惰性 import = R12-A §0-10 维持 | 无新候选 |

**对 R24-A 声明的裁决**：「组合面输入结构的列表字段格网自此全部
有价」在其维度上成立；R24-A 结语量化的是**字段粒度的计数与
（承接 R16-A 的）字节长度**——同一批字符串字段在固定计数、固定
长度下的**内容类**（字节是什么）不在其中，本轮补上并闭合（14a–14d
四组定价 + F5a–F5e 五组证人）。本轮全部新数据**只产出归档证人与
内容类格网的闭合，未产出可落地候选**。收口的预言力第二十五遍成立。

## 4. 前后对比

无代码 diff。仓库变更仅本报告一个文件。仿真为临时脚本未入库（无赢家
不落仿真文件，遵守方法纪律；完整源码见附录），未触碰任何测试。

## 5. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R24-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）全绿
（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，
与主线基线一致）。

仿真（临时脚本；seed 固定可复现，三次独立运行摘要）：

```text
run 1:
F5a: 700/700 内容类形状全维度不变；turn gate 恒 none、score === P
F5b: 值纯性 638b913 | 雪崩 40/40 | F5c: 40/40 + 子串语义钉死
F5d: 40/40 × 2 | F5e: 18 字符串全 ASCII 免转义 max 17
anchor: 16.2 / 15.4 / 15.6 / 14.1 / 15.1 µs/gate => ~70–81 µs/run
decomposition: hash×3=5674ns | validateEvent=3009ns | turn=1721ns
    | prescore×2=1346ns | 5 scans+copy=279ns (6.82 ns/event)
    | applyTrackingGate=6.21µs
spot: skip guard-3 2014ns (13.0%) | replay find#1 3110ns (20.1%)
14a: 683/1741/2764/2774/4141/1204ns => near-miss ×1.49,
    two-byte ×0.43, end/0 ×4.05
14b: 5772/5823/5808/5855/5833/8304ns => mixed ×1.44 (6.199 ns/char);
    e2e gate 19.24µs
14c: short 861 | early 4115 | late 2824ns => 30.1 ps/prefix-char
14d: 12.6µs vs 15.4µs (delta −2.8)
ALL CHECKS PASSED (deterministic, seeded)

run 2:
F5a–F5e: 证人行逐字节同 run 1
anchor: 16.5 / 15.5 / 15.2 / 14.4 / 14.5 µs/gate => ~72–82 µs/run
decomposition: hash×3=5753ns | validateEvent=3011ns | turn=1874ns
    | prescore×2=1564ns | 5 scans+copy=282ns (6.87 ns/event)
    | applyTrackingGate=5.97µs
spot: skip guard-3 2032ns (13.2%) | replay find#1 3087ns (20.0%)
14a: 798/1844/2865/2872/4010/1323ns => near-miss ×1.40,
    two-byte ×0.46, end/0 ×3.59
14b: 5808/5770/5874/5969/6040/8245ns => mixed ×1.42 (6.183 ns/char);
    e2e gate 19.88µs
14c: short 972 | early 4248 | late 2963ns => 30.5 ps/prefix-char
14d: 13.2µs vs 15.5µs (delta −2.3)
ALL CHECKS PASSED (deterministic, seeded)

run 3:
F5a–F5e: 证人行逐字节同 run 1
anchor: 17.9 / 15.6 / 15.5 / 13.9 / 14.7 µs/gate => ~69–89 µs/run
decomposition: hash×3=5898ns | validateEvent=3143ns | turn=1907ns
    | prescore×2=1580ns | 5 scans+copy=284ns (6.92 ns/event)
    | applyTrackingGate=5.92µs
spot: skip guard-3 2078ns (13.3%) | replay find#1 3125ns (20.1%)
14a: 803/1856/2879/2894/4050/1328ns => near-miss ×1.40,
    two-byte ×0.46, end/0 ×3.59
14b: 5793/5764/5688/5853/5825/8287ns => mixed ×1.43 (6.226 ns/char);
    e2e gate 19.46µs
14c: short 997 | early 4240 | late 3000ns => 30.7 ps/prefix-char
14d: 13.2µs vs 15.6µs (delta −2.4)
ALL CHECKS PASSED (deterministic, seeded)
```

三次独立仿真运行 F5a–F5e 证人行逐字节一致（判决不变性/值纯性/真值
表/分歧语义/生产普查确定性重现）；锚点五格三次同带无悬崖（run3
首格 17.9 为 PIC 首格上沿孤点）；axis 14 四组剂量三次运行区间紧
（14a 六类区间宽 ≤0.062 ns/char、14b mixed 类区间宽 0.043 ns/char、
14c 系数区间宽 0.6 ps/char、14c 非单调异常三次同号、14d 三次同号
零牵引）。空枚举裁决成立。

## 6. 新增排除 ID

**无**（空枚举，不铸 S25-A-\*）。换名检查 5 项新记录见 §0（1–5），
归档于本报告备后续轮次引用，无需并入 EXCLUSIONS.md。

重开条件本轮**新增一条收口**：

**值域 / 字符串内容类收口**：切片字符串输入面的「计数 × 长度 ×
内容类」三维格网自此闭合——正则扫描类谱（早退 ~0 / plain 0.507–0.513
/ near-miss 0.785–0.847 ns/char/scan，穿越 ≈7.9–8.5×10⁵ 字节）、
哈希转义类谱（单类免费、mixed ×1.42–1.44 = 6.18–6.23 ns/char，穿越
≈1.07–1.08×10⁵ id 字节）、includes 分歧点纹理（30.1–30.7 ps/前缀
字符，非单调如实记录，家族乘数无独立悬崖）、生产坐标恒在每个类
维度的最便宜格（F5e：18 字符串全免转义 ASCII max 17 字节）。重开
条件：(a) 生产 claims/summary 出现 ≥10⁵ 字节量级的 worst-class
纹理（近失配密集或多语混排——届时先撞 S1-A-3/S7-F-2 手写扫描家族与
S2-A-1 截断家族）；(b) id schema（episodeId/runId/turnId）开始携带
需转义字节或双字节且长度增长 ≥3 个量级（届时 mixed 类 hash
surcharge 实体化，先撞 S5-A-2/X1-1）；(c) SUCCESS_CLAIM 关键词表
扩张使生产纹理落入近失配密集类（届时正则本体重定价，X0-6 邻域
谨慎）。**可操作重开第一优先不变**：C 轴（~2.5 个量级，R15-A；
S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）——14c 分歧纹理届时
与 C 轴一并按家族程序重审。

## 附录 A：确定性仿真脚本（完整，可复现）

运行方式：保存为任意 `.mts` 后 `npx tsx <file>`（仓库根目录，依赖已装，
Node ≥22.19）。seeds：`0xf25a01`–`0xf25a02`。

```ts
/**
 * R25-A deterministic anchor + decomposition + NEW-axis simulation
 * (twenty-fifth pass). No fresh candidate cleared the rename check this
 * round, so there is NO candidate machinery here. The script:
 *   (a) re-verifies the whole-slice budget anchor matrix on THIS VM
 *       (R3-A..R24-A lineage, gate-outcome x contract 2x2 + max-codes);
 *   (b) re-verifies the R12-A..R24-A contract-floor decomposition of the
 *       PASSED x contract(3) production cell (component isolation, 5-scan
 *       O(E) inventory), plus compact spot re-confirmations of the R18-A
 *       skip pricing (guard-3) and the R19-A replay pricing (find#1);
 *   (c) NEW axis 14: the VALUE-DOMAIN / STRING CONTENT-CLASS census.
 *       Twenty-four passes priced the slice's string-bearing input faces
 *       by element COUNT (R24-A: N_op/N_cl/WxO/QxD/CF), by byte LENGTH
 *       at fixed count (R16-A: L/V/S), and by parameter scale (R15-A C/R
 *       + R24-A off-diagonals) -- but never by CONTENT CLASS: what the
 *       bytes ARE at fixed count and fixed length. Three consumers in
 *       the slice have content-dependent per-byte prices:
 *         - 14a: the SUCCESS_CLAIM regex scan (prescore.ts:28, 3
 *           scans/turn): match position (early-exit point), near-miss
 *           density (keyword prefixes forcing partial-match attempts)
 *           and alphabet width (one-byte vs two-byte) were never dosed
 *           (R16-A S dosed claim BYTES in one texture; R24-A 13b dosed
 *           claim COUNT with one all-non-matching texture);
 *         - 14b: hashAssessment's JSON.stringify (types.ts:501, the
 *           slice's #1 component at ~38-41% x3/gate): JSON escape class
 *           (quote/backslash/control chars) and string encoding (CJK
 *           two-byte) change the per-char serialization price; R16-A L
 *           dosed id LENGTH in plain ASCII only;
 *         - 14c: the includes equality scans (prescore.ts:116/123/129,
 *           roller.ts:34): string equality cost depends on the
 *           DIVERGENCE POINT of equal-length candidates (shared-prefix
 *           memcmp), a content dimension orthogonal to R15-A count and
 *           R16-A length;
 *       plus 14d: an end-to-end worst-class echo at production lengths
 *       through the full applyChildThreeLine anchor cell.
 *       Residency textures (operand identity rotation / freshness /
 *       heap generation) are R24-H's axis (H slice) and are explicitly
 *       NOT dosed here: every fixture string below is pre-built, flat
 *       and reused; only its VALUE varies.
 *
 * Archival-fact fuzz (deterministic, seeded):
 *   F5a: content-class invariance. Claims of every class (matching at
 *        any position, near-miss, CJK), worst-class ids and mixed-case
 *        keywords never flip any prescore dimension outcome, P, quality,
 *        coverage or turn gate on the production-texture input.
 *   F5b: hash value-semantics. hashAssessment depends only on string
 *        VALUE: same-content strings built by literal, concat and slice
 *        hash identically (contrast with R24-H residency: representation
 *        and freshness are invisible); flipping one char at any seeded
 *        position flips the hash (40/40 avalanche witness).
 *   F5c: isSuccessClaim truth table across classes. Every keyword
 *        (pass/passed/verified/succeed) matches at every seeded position
 *        in one-byte and two-byte contexts and in every case variant
 *        (40/40); near-miss units (pas/verifie/succee) never match;
 *        substring semantics ("compass" matches) is pinned as the
 *        frozen contract.
 *   F5d: divergence-point semantics. Equality verdicts are independent
 *        of the divergence point: late-divergence decoys never satisfy
 *        coverage; the true needle satisfies it at every seeded decoy
 *        count; scope stays FAIL-free with byte-identical owned paths.
 *   F5e: production content-class census. Every string the production
 *        from-child path feeds the slice is short, one-byte, escape-free
 *        ASCII: production sits in the cheapest cell of every class
 *        dimension (fixture-driven + source-level).
 *
 * Seeded PRNG (mulberry32) -> fully reproducible. Seeds: 0xf25a01..0xf25a02.
 * Run: npx tsx <file>   (repo root, deps installed, Node >=22.19)
 */
import { performance } from "node:perf_hooks";
import type { EventId } from "/workspace/src/domain/ids.js";
import type { RequirementContract } from "/workspace/src/domain/contract.js";
import { computePrescore, isSuccessClaim, type PrescoreInput } from "/workspace/src/tracking/prescore.js";
import { runTrackingTurn } from "/workspace/src/tracking/turn.js";
import {
  assessChildObservation,
  prescoreInputFromObservation
} from "/workspace/src/tracking/from-child.js";
import {
  applyChildThreeLine,
  observationFromChild
} from "/workspace/src/run/child-tracking.js";
import { applyTrackingGate, nextTrackingSeq } from "/workspace/src/run/gate-apply.js";
import { episodeIdFromEvents } from "/workspace/src/run/episode-bind.js";
import { validateEvent, type Event } from "/workspace/src/run/events.js";
import {
  hashAssessment,
  parseTrackingAssessment,
  type TrackingAssessment
} from "/workspace/src/tracking/types.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}

function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function bench(fn: () => void, reps: number): number {
  fn(); // warm
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const NOW_ISO = "2026-08-25T00:00:00.000Z";
const RUN_ID = "run_x" as Event["runId"];

type ChildShape = Parameters<typeof applyChildThreeLine>[0]["child"];
type SpecShape = Parameters<typeof applyChildThreeLine>[0]["spec"];

function mkChild(
  kind: "PASSED" | "FAILED",
  opts?: {
    claimSuccessWithCheckGap?: boolean;
    taskId?: string;
    summary?: string;
    noEvidence?: boolean;
  }
): {
  child: ChildShape;
  spec: SpecShape;
  contract: RequirementContract;
} {
  const summary =
    opts?.summary ?? (opts?.claimSuccessWithCheckGap === true ? "all checks passed" : "tests ran");
  const evidence = opts?.noEvidence === true ? [] : ["evd_1"];
  return {
    child: {
      taskId: opts?.taskId ?? "tsk_bench",
      outcome: kind === "PASSED" ? "SUCCESS" : "FAILURE",
      summary,
      evidenceIds: evidence,
      artifactIds: ["art_1"],
      terminalResult: { verification: { kind, evidenceIds: evidence } }
    } as unknown as ChildShape,
    spec: {
      role: "tester",
      acceptanceCriteria: [{ id: "chk_0", description: "check 0 passes" }]
    } as unknown as SpecShape,
    contract: {
      constraints: [
        { id: "con_0", description: "constraint 0 text" },
        { id: "con_1", description: "constraint 1 text" },
        { id: "con_2", description: "constraint 2 text" }
      ]
    } as unknown as RequirementContract
  };
}

function buildTable(filler: number, withAttach: boolean): Event[] {
  let idCounter = 0;
  const nextId = (): EventId => `evt_${String(idCounter++).padStart(8, "0")}` as EventId;
  const events: Event[] = [];
  events.push({
    id: nextId(),
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "RUN_STARTED",
    actor: "system",
    payload: { title: "bench" }
  } as unknown as Event);
  if (withAttach) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "RUN_ATTACHED",
      actor: "supervisor",
      payload: { episodeId: "ep_bench", runId: RUN_ID, attachedAt: NOW_ISO }
    } as unknown as Event);
  }
  for (let i = 0; i < filler; i += 1) {
    events.push({
      id: nextId(),
      schemaVersion: 1,
      occurredAt: NOW_ISO as Event["occurredAt"],
      runId: RUN_ID,
      type: "CHILD_MESSAGE",
      actor: "child",
      payload: { taskId: `tsk_${i % 5}`, content: "..." }
    } as unknown as Event);
  }
  return events;
}

const events41 = buildTable(39, true);

/** Production-texture prescore input (mirrors prescoreInputFromObservation
 *  for a PASSED tester child with contract(3)) that the doses vary. */
function basePrescoreInput(): PrescoreInput {
  const constraints = [
    { id: "con_0", text: "constraint 0 text", kind: "constraint" as const, mandatory: true as const },
    { id: "con_1", text: "constraint 1 text", kind: "constraint" as const, mandatory: true as const },
    { id: "con_2", text: "constraint 2 text", kind: "constraint" as const, mandatory: true as const }
  ];
  return {
    claims: ["tests ran"],
    toolSituations: [
      {
        name: "task-result",
        exitCode: 0,
        wrote: true,
        escaped: false,
        artifactIds: ["art_1"],
        evidenceIds: ["evd_1"],
        hashes: []
      }
    ],
    writePaths: [],
    ownedPaths: [],
    requiredChecks: ["chk_0"],
    completedChecks: ["chk_0"],
    constraints,
    retainedConstraintIds: ["con_0", "con_1", "con_2"],
    progressed: true,
    stalledTurns: 0,
    independentEvidence: true
  };
}

function turnInputFor(prescoreInput: PrescoreInput): Parameters<typeof runTrackingTurn>[0] {
  return {
    window: {
      contextFacts: ["role tester", "task tsk_bench"],
      toolSituations: prescoreInput.toolSituations,
      constraints: prescoreInput.constraints,
      unresolvedDecisions: [],
      confirmedDecisions: [],
      openMinors: []
    },
    prescoreInput,
    humanInput: {},
    gateFacts: { deterministicFail: false }
  };
}

function verdictVector(result: ReturnType<typeof computePrescore>): string {
  return result.dimensions.map((d) => `${d.id}:${d.outcome}`).join("|");
}

/* ============================================================
 * Axis-14 content-class fixture builders (deterministic, pre-built,
 * flat, reused: only the VALUE varies -- residency is NOT dosed).
 * ============================================================ */
const KEYWORDS = ["pass", "passed", "verified", "succeed"] as const;
const NEAR_MISS_UNIT = "pasverifiesuccee"; // prefixes of the keywords, none complete
const CLAIM_LEN = 4096;
const claimClasses: Record<string, string> = {
  "match@0": "pass" + "x".repeat(CLAIM_LEN - 4),
  "match@mid": "x".repeat(CLAIM_LEN / 2 - 2) + "pass" + "x".repeat(CLAIM_LEN / 2 - 2),
  "match@end": "x".repeat(CLAIM_LEN - 4) + "pass",
  "no-match plain": "x".repeat(CLAIM_LEN),
  "no-match near-miss": NEAR_MISS_UNIT.repeat(CLAIM_LEN / NEAR_MISS_UNIT.length),
  "no-match two-byte": "\u5e93".repeat(CLAIM_LEN)
};

const ID_LEN = 1024;
const idClasses: Record<string, string> = {
  plain: "t".repeat(ID_LEN),
  "quote-heavy": 'a"'.repeat(ID_LEN / 2),
  "backslash-heavy": "a\\".repeat(ID_LEN / 2),
  "control-heavy": "a\n".repeat(ID_LEN / 2),
  "two-byte": "\u5e93".repeat(ID_LEN),
  mixed: 'a"\\\u5e93\n'.repeat(ID_LEN / 5) + "a".repeat(ID_LEN % 5)
};

/* ============================================================
 * F5a: content-class invariance (seeded shapes, deterministic counts).
 * ============================================================ */
{
  const rng = mulberry32(0xf25a01);
  const base = basePrescoreInput();
  const baseResult = computePrescore(base);
  let trials = 0;
  let invariant = 0;
  const classKeys = Object.keys(claimClasses);
  for (let trial = 0; trial < 700; trial += 1) {
    let claim: string;
    const pick = Math.floor(rng() * 3);
    if (pick === 0) {
      // seeded keyword at a seeded position, seeded case, seeded alphabet
      const kw = KEYWORDS[Math.floor(rng() * KEYWORDS.length)]!;
      const cased = [...kw].map((ch) => (rng() < 0.5 ? ch.toUpperCase() : ch)).join("");
      const filler = rng() < 0.5 ? "x" : "\u5e93";
      const len = 16 + Math.floor(rng() * 512);
      const pos = Math.floor(rng() * (len - kw.length));
      claim = filler.repeat(pos) + cased + filler.repeat(len - pos - kw.length);
    } else if (pick === 1) {
      claim = claimClasses[classKeys[Math.floor(rng() * classKeys.length)]!]!;
    } else {
      claim = NEAR_MISS_UNIT.repeat(1 + Math.floor(rng() * 32));
    }
    const padded: PrescoreInput = { ...base, claims: [...base.claims, claim] };
    const result = computePrescore(padded);
    const ok =
      verdictVector(result) === verdictVector(baseResult) &&
      result.P === baseResult.P &&
      result.quality === baseResult.quality &&
      result.coverage === baseResult.coverage &&
      result.cappedByHardFail === baseResult.cappedByHardFail;
    if (ok) invariant += 1;
    const turn = runTrackingTurn(turnInputFor(padded));
    check(
      `F5a turn gate stays none under content class [trial ${trial}]`,
      turn.gate.kind === "none" && turn.score === baseResult.P
    );
    trials += 1;
  }
  check("F5a all content-class claims invariant", invariant === trials, `${invariant}/${trials}`);
  console.log(
    `F5a: ${trials} seeded content-class claim shapes (keyword at any position/case/alphabet, ` +
      `escape-heavy, near-miss, two-byte): dimension vector, P, quality, coverage, ` +
      `cappedByHardFail all invariant ${invariant}/${trials}; turn gate stays none with ` +
      `score === P in every trial (matching claims cannot flip the clean production texture)`
  );
}

/* ============================================================
 * F5b: hash value-semantics (construction independence + avalanche).
 * ============================================================ */
{
  const rng = mulberry32(0xf25a02);
  const mintFor = (taskId: string): TrackingAssessment => {
    const mk = mkChild("PASSED", { taskId });
    const obs = observationFromChild(mk.child, mk.spec, mk.contract);
    const decision = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: String(RUN_ID) });
    if (!decision.apply) throw new Error("F5b fixture must apply");
    return decision.assessment;
  };
  const literal = "content_class_witness_string_0123456789".repeat(8);
  let concat = "";
  for (const ch of literal) concat += ch;
  const sliced = (literal + "!").slice(0, literal.length);
  const hLiteral = hashAssessment(mintFor(literal));
  const hConcat = hashAssessment(mintFor(concat));
  const hSliced = hashAssessment(mintFor(sliced));
  check("F5b same-content different-construction hashes equal", hLiteral === hConcat && hLiteral === hSliced);
  let avalanche = 0;
  for (let trial = 0; trial < 40; trial += 1) {
    const pos = Math.floor(rng() * literal.length);
    const flipped = literal.slice(0, pos) + (literal[pos] === "z" ? "y" : "z") + literal.slice(pos + 1);
    if (hashAssessment(mintFor(flipped)) !== hLiteral) avalanche += 1;
  }
  check("F5b one-char flip at any seeded position flips the hash", avalanche === 40, `${avalanche}/40`);
  console.log(
    `F5b: hashAssessment is value-pure -- literal/concat/slice constructions of the same ` +
      `content hash identically (${hLiteral}); one-char flips at 40 seeded positions all ` +
      `flip the hash (${avalanche}/40). Representation and freshness are invisible to the ` +
      `slice's durable contract (residency axis stays with R24-H, unported)`
  );
}

/* ============================================================
 * F5c: isSuccessClaim truth table across content classes.
 * ============================================================ */
{
  const rng = mulberry32(0xf25a01 ^ 0x5a5a);
  let hits = 0;
  for (let trial = 0; trial < 40; trial += 1) {
    const kw = KEYWORDS[trial % KEYWORDS.length]!;
    const cased = [...kw]
      .map((ch, i) => ((trial >> (i % 8)) & 1) === 1 ? ch.toUpperCase() : ch)
      .join("");
    const filler = trial % 2 === 0 ? "x" : "\u5e93";
    const len = 8 + Math.floor(rng() * 256);
    const pos = Math.floor(rng() * len);
    const text = filler.repeat(pos) + cased + filler.repeat(len);
    if (isSuccessClaim(text)) hits += 1;
  }
  check("F5c keyword matches at every seeded position/case/alphabet", hits === 40, `${hits}/40`);
  check("F5c near-miss unit never matches", !isSuccessClaim(NEAR_MISS_UNIT.repeat(64)));
  check("F5c plain filler never matches", !isSuccessClaim("x".repeat(4096)));
  check("F5c two-byte filler never matches", !isSuccessClaim("\u5e93".repeat(4096)));
  check("F5c substring semantics pinned (compass matches)", isSuccessClaim("compass"));
  check("F5c substring semantics pinned (surpassed matches)", isSuccessClaim("surpassed"));
  console.log(
    `F5c: isSuccessClaim truth table -- 4 keywords x seeded positions/cases/alphabets all ` +
      `match (${hits}/40); near-miss (pas/verifie/succee), plain and two-byte fillers never ` +
      `match; substring semantics ("compass", "surpassed" match) pinned as the frozen contract`
  );
}

/* ============================================================
 * F5d: divergence-point semantics (equality independent of prefix).
 * ============================================================ */
{
  const rng = mulberry32(0xf25a02 ^ 0x5a5a);
  const base = basePrescoreInput();
  const needle = "chk_" + "p".repeat(252); // 256 chars
  for (let trial = 0; trial < 40; trial += 1) {
    const decoyCount = 1 + Math.floor(rng() * 256);
    const late = Array.from({ length: decoyCount }, (_, i) =>
      needle.slice(0, 255) + String.fromCharCode(65 + ((i + 1) % 26))
    );
    const withNeedle = computePrescore({
      ...base,
      requiredChecks: [needle],
      completedChecks: [...late, needle]
    });
    check(
      `F5d needle found after ${decoyCount} late-divergence decoys [trial ${trial}]`,
      withNeedle.dimensions.some((d) => d.id === "check-coverage" && d.outcome === "PASS")
    );
    const withoutNeedle = computePrescore({
      ...base,
      requiredChecks: [needle],
      completedChecks: late
    });
    check(
      `F5d late-divergence decoys never satisfy coverage [trial ${trial}]`,
      withoutNeedle.dimensions.some((d) => d.id === "check-coverage" && d.outcome === "UNOBSERVED")
    );
  }
  const owned = Array.from({ length: 32 }, (_, i) => "src/" + "q".repeat(200) + `_${i}.ts`);
  const scope = computePrescore({ ...base, writePaths: [...owned], ownedPaths: owned });
  check(
    "F5d byte-identical long-prefix owned paths stay PASS",
    scope.dimensions.some((d) => d.id === "scope-safety" && d.outcome === "PASS")
  );
  console.log(
    "F5d: divergence-point semantics exact -- the true needle satisfies coverage behind any " +
      "seeded count of 255/256-shared-prefix decoys (40/40); decoys alone never satisfy it " +
      "(40/40); byte-identical long-prefix write/owned paths keep scope PASS"
  );
}

/* ============================================================
 * F5e: production content-class census (from-child path).
 * ============================================================ */
{
  const mk = mkChild("PASSED");
  const obs = observationFromChild(mk.child, mk.spec, mk.contract);
  const pi = prescoreInputFromObservation(obs);
  const decision = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: String(RUN_ID) });
  if (!decision.apply) throw new Error("F5e fixture must apply");
  const strings: string[] = [
    ...pi.claims,
    ...pi.requiredChecks,
    ...pi.completedChecks,
    ...pi.retainedConstraintIds,
    ...pi.constraints.map((c) => c.id),
    ...pi.constraints.map((c) => c.text),
    ...pi.toolSituations.flatMap((t) => [t.name, ...t.evidenceIds, ...t.artifactIds, ...t.hashes]),
    decision.assessment.episodeId,
    decision.assessment.runId,
    decision.assessment.turnId
  ];
  const needsEscape = (s: string): boolean =>
    [...s].some((ch) => {
      const code = ch.codePointAt(0)!;
      return code < 0x20 || ch === '"' || ch === "\\" || code > 127;
    });
  const dirty = strings.filter(needsEscape);
  const maxLen = Math.max(...strings.map((s) => s.length));
  check("F5e production strings all escape-free one-byte ASCII", dirty.length === 0, dirty.join(","));
  check("F5e production strings all short", maxLen <= 32, String(maxLen));
  check(
    "F5e production claim is non-matching for the clean texture",
    !isSuccessClaim(pi.claims[0] ?? "")
  );
  console.log(
    `F5e: production content-class census -- all ${strings.length} strings the from-child ` +
      `path feeds the slice are escape-free one-byte ASCII, max length ${maxLen}; the clean ` +
      `production claim does not match SUCCESS_CLAIM (worst case only on FAILED "all checks ` +
      `passed" textures, length ~17): production sits in the cheapest cell of every class ` +
      `dimension (the doses below are public-API-face pricing, not production re-pricing)`
  );
}

/* ============================================================
 * Warm-up: drive the full production path across all matrix shapes.
 * ============================================================ */
{
  for (let round = 0; round < 3000; round += 1) {
    const kind = round % 2 === 0 ? ("PASSED" as const) : ("FAILED" as const);
    const mk = mkChild(kind, { claimSuccessWithCheckGap: round % 8 === 3 });
    let c = 100;
    void applyChildThreeLine({
      events: events41,
      child: mk.child,
      spec: mk.spec,
      ...(round % 4 < 2 ? { contract: mk.contract } : {}),
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }
}

/* ============================================================
 * Budget anchor matrix (R3-A..R24-A lineage incl. max-codes cell).
 * ============================================================ */
const anchorUs: number[] = [];
{
  const cells: Array<{
    label: string;
    kind: "PASSED" | "FAILED";
    withContract: boolean;
    maxCodes?: boolean;
  }> = [
    { label: "PASSED x no-contract (lineage anchor)", kind: "PASSED", withContract: false },
    { label: "PASSED x contract(3)", kind: "PASSED", withContract: true },
    { label: "FAILED x no-contract", kind: "FAILED", withContract: false },
    { label: "FAILED x contract(3) (hard gate, 3 events appended)", kind: "FAILED", withContract: true },
    {
      label: "FAILED x contract(3) x max-codes(2 hard)",
      kind: "FAILED",
      withContract: true,
      maxCodes: true
    }
  ];
  for (const cell of cells) {
    const mk = mkChild(cell.kind, { claimSuccessWithCheckGap: cell.maxCodes === true });
    let applied = false;
    let appendedCount = 0;
    const one = bench(() => {
      let c = 100;
      const out = applyChildThreeLine({
        events: events41,
        child: mk.child,
        spec: mk.spec,
        ...(cell.withContract ? { contract: mk.contract } : {}),
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
      applied = out.result.applied;
      appendedCount = out.events.length - events41.length;
    }, 5000);
    check(`anchor cell applies the gate [${cell.label}]`, applied);
    anchorUs.push(one * 1e3);
    console.log(
      `anchor [${cell.label}]: one applyChildThreeLine over 41-event table = ${(one * 1e3).toFixed(1)}us (+${appendedCount} events) -> ~5 gates/run => ~${(one * 5 * 1e3).toFixed(0)}us/run`
    );
  }
}

/* ============================================================
 * Contract-floor decomposition (R12-A..R24-A methodology) + compact
 * skip (guard-3) and replay (find#1) spot re-confirmations.
 * ============================================================ */
const mkP = mkChild("PASSED");
const observationP = observationFromChild(mkP.child, mkP.spec, mkP.contract);
const prescoreInputP = prescoreInputFromObservation(observationP);
const decisionP = assessChildObservation({
  observation: observationP,
  episodeId: "ep_bench",
  runId: String(RUN_ID)
});
check("decomposition fixture applies", decisionP.apply === true);
if (decisionP.apply) {
  const assessment: TrackingAssessment = decisionP.assessment;
  const hash = hashAssessment(assessment);
  const seq = nextTrackingSeq(events41);

  const tHash = bench(() => void hashAssessment(assessment), 200000);
  const assessmentPojo = JSON.parse(JSON.stringify(assessment)) as unknown;
  const tParse = bench(() => void parseTrackingAssessment(assessmentPojo), 100000);

  const trackingEvent = {
    id: "evt_00000100" as EventId,
    schemaVersion: 1,
    occurredAt: NOW_ISO as Event["occurredAt"],
    runId: RUN_ID,
    type: "TRACKING_ASSESSMENT",
    actor: "supervisor",
    payload: { assessment, assessmentHash: hash, seq }
  } as unknown as Event;
  const tVev = bench(() => void validateEvent(trackingEvent), 100000);

  const tEpisode = bench(() => void episodeIdFromEvents(events41), 200000);
  const tSeq = bench(() => void nextTrackingSeq(events41), 200000);
  const tFindGT = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "GATE_TRANSITION" &&
          (event as { payload: { idempotencyKey?: string } }).payload.idempotencyKey ===
            `${hash}:${seq}`
      ),
    200000
  );
  const tFindTA = bench(
    () =>
      void events41.find(
        (event) =>
          event.type === "TRACKING_ASSESSMENT" &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload.seq === seq &&
          (event as { payload: { seq?: number; assessmentHash?: string } }).payload
            .assessmentHash === hash
      ),
    200000
  );
  const tStatus = bench(() => {
    let status = "RUNNING";
    for (const event of events41) {
      if (event.type === "GATE_TRANSITION")
        status = (event as { payload: { to: string } }).payload.to;
      else if (event.type === "RUN_BLOCKED") status = "BLOCKED";
      else if (event.type === "RUN_WAITING_FOR_USER") status = "WAITING_FOR_USER";
      else if (event.type === "USER_ANSWER" || event.type === "RUN_STARTED") status = "RUNNING";
    }
    void status;
  }, 200000);
  const tCopy = bench(() => void [...events41], 200000);
  const tPrescore = bench(() => void computePrescore(prescoreInputP), 200000);
  const tTurn = bench(
    () =>
      void runTrackingTurn({
        window: {
          contextFacts: [`role ${observationP.role}`, `task ${observationP.taskId}`],
          toolSituations: prescoreInputP.toolSituations,
          constraints: observationP.constraints,
          unresolvedDecisions: [],
          confirmedDecisions: [],
          openMinors: []
        },
        prescoreInput: prescoreInputP,
        humanInput: {},
        gateFacts: { deterministicFail: false }
      }),
    100000
  );
  const tGate = bench(() => {
    let c = 100;
    void applyTrackingGate({
      events: events41,
      assessment,
      assessmentHash: hash,
      expectedSeq: seq,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
    });
  }, 20000);

  const ns = (v: number): string => `${(v * 1e6).toFixed(0)}ns`;
  console.log("--- contract-floor decomposition (PASSED x contract(3), E=41) ---");
  console.log(`  hashAssessment (x3/gate): ${ns(tHash)} each => ${ns(tHash * 3)}`);
  console.log(`  parseTrackingAssessment (write-side): ${ns(tParse)}`);
  console.log(`  validateEvent(TRACKING_ASSESSMENT) whole: ${ns(tVev)}`);
  console.log(`  computePrescore (x2/gate, S1-A-2): ${ns(tPrescore)} each => ${ns(tPrescore * 2)}`);
  console.log(`  runTrackingTurn full: ${ns(tTurn)}`);
  const scans = tEpisode + tSeq + tFindGT + tFindTA + tStatus + tCopy;
  console.log(
    `  O(E) 5 scans + copy: ${ns(scans)} (episode=${ns(tEpisode)} seq=${ns(tSeq)} findGT=${ns(tFindGT)} findTA=${ns(tFindTA)} status=${ns(tStatus)} copy=${ns(tCopy)}) => ~${((scans * 1e6) / 41).toFixed(2)} ns/event at E=41`
  );
  console.log(`  applyTrackingGate whole: ${(tGate * 1e3).toFixed(2)}us`);

  // compact spot re-confirmations (composition-ceiling continuity)
  {
    const applyRef = anchorUs[1] ?? Number.NaN;
    const mkSkip = mkChild("FAILED", { noEvidence: true });
    let appliedSkip = true;
    const tSkip = bench(() => {
      let c = 100;
      appliedSkip = applyChildThreeLine({
        events: events41,
        child: mkSkip.child,
        spec: mkSkip.spec,
        contract: mkSkip.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 20000);
    check("skip spot cell is a skip", appliedSkip === false);
    const mkGT = mkChild("FAILED", { taskId: "tsk_replay_gt" });
    const obsGT = observationFromChild(mkGT.child, mkGT.spec, mkGT.contract);
    const decGT = assessChildObservation({
      observation: obsGT,
      episodeId: "ep_bench",
      runId: String(RUN_ID)
    });
    if (!decGT.apply) throw new Error("replay fixture must apply");
    const hashGT = hashAssessment(decGT.assessment);
    const seqGT = nextTrackingSeq(events41);
    let c1 = 700000;
    const freshGT = applyTrackingGate({
      events: events41,
      assessment: decGT.assessment,
      assessmentHash: hashGT,
      expectedSeq: seqGT,
      policyVersion: "track-v1",
      nowIso: NOW_ISO,
      generateEventId: () => `evt_${String(c1++).padStart(8, "0")}` as EventId
    });
    check("replay spot fixture applied", freshGT.result.applied && freshGT.result.directive !== "none");
    const tReplay = bench(() => {
      void applyTrackingGate({
        events: freshGT.events,
        assessment: decGT.assessment,
        assessmentHash: hashGT,
        expectedSeq: seqGT,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => {
          throw new Error("replay must not generate ids");
        }
      });
    }, 20000);
    console.log(
      `  spot re-confirm: skip guard-3 ${(tSkip * 1e6).toFixed(0)}ns (${(((tSkip * 1e3) / applyRef) * 100).toFixed(1)}% of apply) | replay find#1 ${(tReplay * 1e6).toFixed(0)}ns (${(((tReplay * 1e3) / applyRef) * 100).toFixed(1)}% of apply)`
    );
  }
}

/* ============================================================
 * NEW axis 14: value-domain / string content-class census.
 * ============================================================ */
{
  console.log("--- NEW axis 14a: SUCCESS_CLAIM scan content-class dose (public computePrescore / runTrackingTurn) ---");
  const base = basePrescoreInput();
  // pre-warm every class shape before timing any cell (R11-A PIC/order family)
  for (const claim of Object.values(claimClasses)) {
    const input: PrescoreInput = { ...base, claims: [claim] };
    for (let w = 0; w < 500; w += 1) void computePrescore(input);
    const tin = turnInputFor(input);
    for (let w = 0; w < 300; w += 1) void runTrackingTurn(tin);
  }
  const prescoreBase = bench(() => void computePrescore(base), 50000);
  const scanRows: Array<{ label: string; prescoreNs: number; turnNs: number }> = [];
  for (const [label, claim] of Object.entries(claimClasses)) {
    const input: PrescoreInput = { ...base, claims: [claim] };
    const tin = turnInputFor(input);
    const onePrescore = bench(() => void computePrescore(input), 20000);
    const oneTurn = bench(() => void runTrackingTurn(tin), 10000);
    scanRows.push({ label, prescoreNs: onePrescore * 1e6, turnNs: oneTurn * 1e6 });
  }
  for (const row of scanRows) {
    const deltaNs = row.prescoreNs - prescoreBase * 1e6;
    const perChar = deltaNs / CLAIM_LEN;
    console.log(
      `  ${row.label.padEnd(20)} prescore(1 scan) ${row.prescoreNs.toFixed(0).padStart(6)}ns ` +
        `(delta ${deltaNs.toFixed(0).padStart(6)}ns => ${perChar.toFixed(3)} ns/char) | ` +
        `turn(3 scans) ${row.turnNs.toFixed(0).padStart(6)}ns`
    );
  }
  const plainRow = scanRows.find((r) => r.label === "no-match plain")!;
  const nearRow = scanRows.find((r) => r.label === "no-match near-miss")!;
  const twoByteRow = scanRows.find((r) => r.label === "no-match two-byte")!;
  const early = scanRows.find((r) => r.label === "match@0")!;
  const lateM = scanRows.find((r) => r.label === "match@end")!;
  console.log(
    `  => class spreads at L=4096: near-miss/plain x${(nearRow.prescoreNs / plainRow.prescoreNs).toFixed(2)}, ` +
      `two-byte/plain x${(twoByteRow.prescoreNs / plainRow.prescoreNs).toFixed(2)}, ` +
      `match@end/match@0 x${(lateM.prescoreNs / early.prescoreNs).toFixed(2)} ` +
      `(early-exit at the match point); production claim length ~9-17 one-byte ASCII => ` +
      `all class deltas collapse below ~1ns at production scale`
  );

  console.log("--- NEW axis 14b: hashAssessment JSON escape/encoding class dose (production mint face) ---");
  const mintFor = (taskId: string): TrackingAssessment => {
    const mk = mkChild("PASSED", { taskId });
    const obs = observationFromChild(mk.child, mk.spec, mk.contract);
    const decision = assessChildObservation({ observation: obs, episodeId: "ep_bench", runId: String(RUN_ID) });
    if (!decision.apply) throw new Error("14b fixture must apply");
    return decision.assessment;
  };
  const prodAssessment = mintFor("tsk_bench");
  const classAssessments = new Map<string, TrackingAssessment>();
  for (const [label, id] of Object.entries(idClasses)) classAssessments.set(label, mintFor(id));
  // pre-warm all classes
  for (const a of classAssessments.values()) for (let w = 0; w < 2000; w += 1) void hashAssessment(a);
  const tHashProd = bench(() => void hashAssessment(prodAssessment), 100000);
  const hashRows: Array<{ label: string; ns: number }> = [];
  for (const [label, a] of classAssessments) {
    const one = bench(() => void hashAssessment(a), 50000);
    hashRows.push({ label, ns: one * 1e6 });
  }
  const plainHash = hashRows.find((r) => r.label === "plain")!;
  for (const row of hashRows) {
    const deltaNs = row.ns - tHashProd * 1e6;
    console.log(
      `  turnId=${row.label.padEnd(16)} hashAssessment ${row.ns.toFixed(0).padStart(6)}ns ` +
        `(delta vs production-9-char ${deltaNs.toFixed(0).padStart(6)}ns => ${(deltaNs / ID_LEN).toFixed(3)} ns/char; ` +
        `x${(row.ns / plainHash.ns).toFixed(2)} vs plain-1024)`
    );
  }
  // e2e echo: full gate with a 1024-char worst-class turnId (hash x3 + write-side parse/trim)
  {
    const worst = classAssessments.get("mixed")!;
    const hashW = hashAssessment(worst);
    const seqW = nextTrackingSeq(events41);
    const tGateWorst = bench(() => {
      let c = 100;
      void applyTrackingGate({
        events: events41,
        assessment: worst,
        assessmentHash: hashW,
        expectedSeq: seqW,
        policyVersion: "track-v1",
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      });
    }, 20000);
    console.log(
      `  e2e echo: applyTrackingGate with mixed-class 1024-char turnId = ${(tGateWorst * 1e3).toFixed(2)}us ` +
        `(vs production-shape gate in the decomposition above) -- the class surcharge rides ` +
        `only the 3 hash legs + write-side parse, linear in id bytes, schema-capped in production`
    );
  }

  console.log("--- NEW axis 14c: includes divergence-point dose (prescore.ts:123 equality scans) ---");
  const needle = "chk_" + "p".repeat(252); // 256 chars
  const DECOYS = 256;
  const lateDecoys = Array.from({ length: DECOYS }, (_, i) =>
    needle.slice(0, 255) + String.fromCharCode(65 + (i % 26))
  );
  const earlyDecoys = Array.from({ length: DECOYS }, (_, i) =>
    String.fromCharCode(65 + (i % 26)) + needle.slice(1)
  );
  const shortDecoys = Array.from({ length: DECOYS }, (_, i) => `zk_${i}`); // length-mismatch class
  const inputLate: PrescoreInput = { ...base, requiredChecks: [needle], completedChecks: [...lateDecoys, needle] };
  const inputEarly: PrescoreInput = { ...base, requiredChecks: [needle], completedChecks: [...earlyDecoys, needle] };
  const inputShort: PrescoreInput = { ...base, requiredChecks: [needle], completedChecks: [...shortDecoys, needle] };
  for (const input of [inputLate, inputEarly, inputShort]) {
    for (let w = 0; w < 500; w += 1) void computePrescore(input);
  }
  const oneLate = bench(() => void computePrescore(inputLate), 20000);
  const oneEarly = bench(() => void computePrescore(inputEarly), 20000);
  const oneShort = bench(() => void computePrescore(inputShort), 20000);
  const lateDelta = (oneLate - oneShort) * 1e6;
  const perCharCmp = (lateDelta * 1000) / (DECOYS * 255); // ps/char over the shared-prefix memcmp
  console.log(
    `  Cc=${DECOYS} pad, 256-char ids: length-mismatch ${(oneShort * 1e6).toFixed(0)}ns | ` +
      `early-divergence ${(oneEarly * 1e6).toFixed(0)}ns | late-divergence ${(oneLate * 1e6).toFixed(0)}ns ` +
      `(delta late-vs-short ${lateDelta.toFixed(0)}ns => ~${perCharCmp.toFixed(1)} ps/prefix-char; x2/turn ` +
      `+ derived-gap third scan)`
  );
  console.log(
    `  => production ids are <=32 chars with divergence inside the first word; the divergence ` +
      `texture multiplies the R15-A/R24-A includes family price by at most ~id-length, ` +
      `crossings stay 3+ orders out; the amortization path (Set-ize) is S1-A-4/X4-7 verbatim`
  );

  console.log("--- NEW axis 14d: end-to-end worst-class echo at production lengths ---");
  {
    // production-length (9-char) worst-class strings through the full anchor cell
    const worstSummary = '\u5e93"\u5e93\\\u5e93\n\u5e93\u5e93\u5e93';
    const worstTask = '\u5e93"t\\\u5e93\nqrs';
    const mkWorst = mkChild("PASSED", { taskId: worstTask, summary: worstSummary });
    let applied = false;
    const oneWorst = bench(() => {
      let c = 100;
      applied = applyChildThreeLine({
        events: events41,
        child: mkWorst.child,
        spec: mkWorst.spec,
        contract: mkWorst.contract,
        nowIso: NOW_ISO,
        generateEventId: () => `evt_${String(c++).padStart(8, "0")}` as EventId
      }).result.applied;
    }, 5000);
    check("14d worst-class production-length cell applies", applied);
    const plainCell = anchorUs[1] ?? Number.NaN;
    console.log(
      `  PASSED x contract(3) with 9-char mixed-class summary+taskId: ${(oneWorst * 1e3).toFixed(1)}us ` +
        `vs plain anchor cell ${plainCell.toFixed(1)}us (delta ${((oneWorst * 1e3) - plainCell).toFixed(1)}us) ` +
        `-- content class has no production-scale traction`
    );
  }

  console.log(
    "  => axis 14 verdict: every content-class surcharge is linear in bytes the slice " +
      "already priced (R16-A L/V/S), multiplies them by at most ~2-4x in the worst class, " +
      "and production sits in the cheapest cell of every class dimension (F5e); every " +
      "amortization path (manual scanner / first-char guard, escape-free hand serializer, " +
      "interning, Set-izing, truncation) lands on S1-A-3 / S5-A-2 / X1-1 / S1-A-4 / X4-7 / " +
      "S2-A-1 -- no candidate"
  );
}

if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("\nALL CHECKS PASSED (deterministic, seeded)");
```
