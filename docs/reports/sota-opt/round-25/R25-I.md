MODEL_SLUG=claude-fable-5-thinking-xhigh

# R25-I：CLI / Pi adapter / config / telemetry 第二十五遍复查报告（S1-I + S4-I + S5-I-1 + S7-I-1 之上）

- 基线：`cursor/sota-persistent-opt-83a1` @ `35b70f1`（派单独占 tip，含 R25-F 收口与 R25-I 派单）；运行中独占分支快进至 `3ecbd94`（R25-G 收口 + R25-J 派单），本分支已按派单令**同步快进（rebase）到 `3ecbd94`**——增量 `35b70f1..3ecbd94` 仅 1 个提交且全部为 docs（`git diff --stat -- . ':!docs'` 0 行），EXCLUSIONS.md 该区间逐字节未变，切片与 dist 不受影响（全部测量在 `35b70f1` 构建的 dist 上完成，二者切片字节相同——快进后 `git diff 8dee7fb..HEAD -- <25 文件>` 重验仍为 0 行）。切片最后一次改动仍是 S7-I-1 落地提交 `8dee7fb`，本轮实测 `git diff 8dee7fb..HEAD -- <25 文件>` 为空（`--stat` 0 行）——**零漂移，自 `8dee7fb` 起连续第十八轮字节不变**。`package.json` / `pnpm-lock.yaml` / 切片相关测试面零漂移（`git diff --quiet 8dee7fb..HEAD -- package.json pnpm-lock.yaml test/` 通过，快进后复验通过）。基线增量核查：`aec794e`（R24-I 收官 tip）→ `3ecbd94` 共 **10 个提交，全部 `docs(sota-opt)` 收口/派单**（`git diff --stat -- . ':!docs'` 0 行）；**EXCLUSIONS.md 全区间逐字节未变**（`git diff --quiet` 通过），对 I 切片无新约束行。快进吸收的 R25-G 为父代理已收口分支（其 PLAN 行禁移植轴对 I 无新增约束；R25-J 在飞，未触碰其分支与文件）。
- **切片清单核实（派单指令项）：恰好 25 文件**（cli 13 / pi-adapter 9 / config 2 / telemetry 1），`ls` 逐目录计数与派单清单逐字一致，合计 **4708 行**（与 R19-I～R24-I 计数逐字相同）。
- 前置阅读按令执行：README → EXCLUSIONS 全表（S1-I / S4-I-2..5 / S5-I-1..5 / S7-I-1..4 / S8-I-1..3 逐条定位）→ PROGRESS → round-25/PLAN（A–F 六切片收口摘要 + G/H 在飞行 + I 派单行）→ R24-I（适应态坐标）→ R23-I（`.pi/` 树）→ R22-I（run 记录回读）→ R21-I（flowchart 平面）→ R20-I（累积遥测 N）全文。兄弟轴禁移植逐一核读：R25-F plan-header identity-geometry face（刚合入）/ **AGEX** / **CG** / R25-D identity-coordinate face / R25-B task-face optional-field presence topology / R25-A value-domain content-class census / **VALCLASS** / R24-B M=1 catalog collapse / R24-D approval-authority face / R24-F provenance face / R24-H operand-residency census / R24-I adaptation-state coordinate（本区自有，不补铸）——本轮新轴与其对象、站点、机制在 §2.1 逐项划界。
- 分支：`cursor/r25-i-cli-twenty-fifth-pass-83a1`（已推送，未开 PR）。
- 环境：双 Node 版本实测——VM 默认 22.14.0（`/exec-daemon/node`，与 R9-I～R24-I 同构；基准全部显式传二进制全路径）与 nvm 22.22.2（engines ≥22.19 合规，gate/仿真主引擎）。pnpm 10.17.1，`pnpm install --frozen-lockfile` + `pnpm build` 后对 `dist/cli/main.js` 测 spawn-to-exit。`@earendil-works/pi-ai` **0.84.1**（钉死 + frozen-lockfile）；exports 图仍为 7 项（`.`、`./compat`、`./providers/*`、`./api/*`、`./oauth`、`./bedrock-provider`、`./bun-oauth`，本 VM 直接枚举）；`dist/providers/` 仍恰 **39 个** `.models.js` 表（直接计数）；`dist/models.generated.js` 存在但未导出；`dist/providers/all.models.js` **文件不存在**（`ls` 直接确认）——**S8-I-1 的唯一重开条件连续第十八轮未满足（两臂文件级 blocked 按派单原样维持，未重复执行级探针）**。

## 结论

**无可落地的新更优解，本轮生产代码零改动，未铸造任何 S25-I-\* ID（诚实空枚举，与 R9-I～R24-I 同结局，十八连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮是全新第二十五组枚举而非历轮剩余项清理。R24-I 收官时宣告「三个配置态坐标系（state-root providers 态 × 项目态 × 适应态）全部收口」——本轮发现并收口**第四个配置态坐标系：进程环境坐标（process-environment coordinate）——环境变量块对承重 run 类的配置输入面**。这是一个历轮测量学上被**主动钉死**的盲区：R8-I 78 格矩阵与 R9-I…R24-I 全部抽测 harness 每迭代执行 `delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY`，且从未设置 `PI_FAST_MODEL` / `SPARKLE_AUTO_ADAPT` / `PI_THINKING_LEVEL`——环境块被固定在全缺席角上从未进过矩阵（R7-I 教训在第四配置坐标系上的重演；本 VM 基底 env 实测 0 个 `PI_*`/`SPARKLE*` 变量，历轮测量本身不受污染，但坐标系从未被剂量）。该轴为测量收口、未产出 I 切片候选，四个裁决性发现：

1. **零漂移五重确认（连续第十八轮）**：切片源码（`git diff 8dee7fb..HEAD` 空）、依赖面（`package.json`/`pnpm-lock.yaml` 字节不变；pi-ai 0.84.1 exports 图 7 项未变；39 表直接计数）、测试面（零变动）、基线增量（`aec794e..35b70f1` 共 9 提交全 docs，EXCLUSIONS 字节不变）、调用图前提（S5-I-1 dispatch 站点 13 条 `await import` 直接计数在位；S7-I-1 站点 model-catalog.ts L53-58 注释与站点原样；`providers/all` 的 src 内边仍仅 `listed-model-lazy.ts` L36 失手回退 / `runtime.ts` L20 点用处 / `listed-model.ts` L1 同步面；pi-adapter barrel `index.ts` 仍无任何 src/ 生产导入方【grep 计数 0】——本轮逐项直接重验）。
2. **本 VM 锚点复测（三次运行 × 双引擎，未拷贝历轮数字，派单明令项）**：R9-I 承重集 + flowchart 双态格全部 exit 0，**custom−builtin 差 children +37.0~+50.7 / +20.9~+25.8、track +45.7~+53.1 / +23.6~+25.6、flowchart-flat64 +42.3~+52.5 / +15.1~+24.9 ms（22.14/22.22，三次运行带）**——与 R24-I 点值（+44.2/+26.5、+48.0/+25.9、+48.7/+16.3）同号、同量级、同 Node 版本梯度（**十八轮同构**）；带缘偏移全部落在 R15-I 轴 1 的 A/A 噪声包络内。地板 54.6-58.8 / 52.9-53.5 ms 与 R24-I（54.3/51.6）几乎同位。归属未变。
3. **新轴（§1.2-§1.4）：进程环境坐标标定（I 切片首次；第四配置态坐标系）**。环境门在承重路径上恰有三个：`main.ts` L626-637 的 `envCatalogId`（`PI_PROVIDER`+`PI_MODEL` 双设才生效；目录侧优先级 flag > providers.primary > env）与 `PI_FAST_MODEL`（flag > env > providers.fast > `"cheap"`）——解析出的 id 经 `smartChildPlan`（main.ts L357）与 `startTrackedRun`（track/loop.ts L82-85）**直达 `buildLiveCatalogConfig` 的 overrides**；`approval-profile.ts` L45-48 的 `SPARKLE_AUTO_ADAPT` 收集/提案 kill switch（每承重 run 尾部 auto-loop L93 读取）。结构判决先于测量并被测量证实：**默认 providers 态下别名 id（`"premium"`/`"cheap"`）过不了 `tryParseModelRef`，目录走零 import 的 `catalogFromPrimary` 腿（model-catalog.ts L47-48）；一个可解析的 env 来源 ref 把同一 run 翻到 per-provider 表 import 腿（S7-I-1 机器，L58-61）；未声明的 custom env ref 必走 per-provider 失手 → `providers/all` 回退 → unknown-model 拒绝（L62-63，exit 1）**。10 个新格（双引擎 × 三次运行全部按预期 exit）裁决数字：
   - **env-pair 剂量 = 单表冷 import 的个位 ms**：children@default+envPair − children@default = **+2.0~+5.9 ms**（双引擎三运行带）；进程内分解给出干净归属：openai 表冷 import **3.06-3.28 ms**、第二表（anthropic）**1.09-1.18 ms**、温态仅 50.5-64.9 µs——e2e 边际与冷表成本逐位相容。命令类无关（track+envPair 边际 −0.1~+12.3 同带）。**低于落地线（数十 ms）约一个量级。**
   - **门源平坦性（source-flatness）**：env 门与 providers.json 门对同一下游机器同价——children@builtin − children@default = +2.0~+7.9 ms（builtin 配置态的 2 张表）与 env-pair 剂量同带；**custom 身份税（+37~+53 ms）附着在 providers.json 的 enabled 列表门上，env 门在配置态被 providers.primary 遮蔽（L630-635 优先级），实测 children@custom+envPair − children@custom = −6.8~+5.8 / −1.0~+0.9 ms ≈ 0（符号翻转）**，children@builtin+envPair 同样 ≈0。**I 切片自有的门段（两次 `process.env` 属性读 + `formatModelRef` 模板串 + `??` 优先级链）实测 679-684 ns/run——深度亚噪声。**
   - **拒绝端点交叉互锚**：children@default+envCustom（未声明 custom ref）经 env 门触发 unknown-model 拒绝，e2e 比地板贵 **+47.9~+54.0 / +28.6~+30.5 ms**（exit 1）；进程内 time-to-throw 冷 **53.77 / 25.36 ms**（22.14/22.22）、温 ~250 µs——即 `providers/all` 回退 import 的全额（r7i part D 本轮复证：builtin 构建后 providers/all 保持冷 71.2 ms、custom 回退命中缓存 2.3 ms）。**custom−builtin 带经第二扇门被独立复现**——S8-I-1 健全性结构的又一交叉见证，重开条件仍未满足，维持 blocked。
   - **kill switch 平坦 + 行为见证**：SPARKLE_AUTO_ADAPT=0 在承重格 e2e 边际 −2.6~+2.7 / −1.7~+9.5 ms ≈ 0（收集与诊断照付，仅提案腿被闸——承重格提案腿本就不触发）；行为对照：掩掉设计内的 `adapt:` 理由行（disabled 态打印文档化的 `auto-adapt disabled; collected only`）后输出规范化逐字相同。**W1/W2/W3 三类行为见证 26 项检查 × 双引擎 × 3 运行全 0 失败**（遮蔽等价、kill-switch 等价、env 翻腿确定性 + 结构行断言）。
4. **诚实更新派单前提 + 契约面移交注记（散文记录，不铸 ID）**：R24-I 宣告的「三个配置态坐标系全部收口」不完备——本轮补上第四坐标系（进程环境）并收口之。顺带钉住一个**代码级优先级不对称事实**（只读观察，非候选）：executor 站点（main.ts L116 注释 + L136-137 实现）优先级为 **flag > env > providers.json**，而路由目录站点（L630-635）为 **flag > providers.json > env**——两站点对「env 对 providers.primary」次序相反。承重 fake-executor 格不可观察（executor 腿只在 `--executor pi` 读 env）；若用户同时设置 providers.primary 与 PI_PROVIDER/PI_MODEL 并用 `--executor pi`，执行通道跟 env 而路由目录跟 providers.primary。这是语义/产品契约问题（是否有意的 pin 语义），非性能项——移交契约所有者，移交不代行，不铸 ID。
5. **验证面**：三个已落地赢家等价性仿真本基线全部复跑通过（**r4i 68 / r5i 119 / r7i 80 checks，全 0 失败**）；**`pnpm gate` exit 0**（typecheck + lint + 全套件 **1169 用例：1168 pass / 0 fail / 1 skip**@22.22.2——与 R9-I～R24-I 计数逐字相同 + build）；`node dist/cli/main.js --version` smoke → `0.1.0` exit 0。测量学注记：gate 首跑误在 22.14.0 引擎上执行，恰好只挂 doctor preflight 一例（engines ≥22.19 检查按设计 FAIL）——该假阳本身即 R15-I/S5-I-5 引擎纪律的直接证人，22.22.2 重跑全绿。生产代码零改动，无 r25i 新仿真（派单：仅落地代码才须新增）。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；凭据/数据面契约未触碰（`file-credential-store.ts` / `auth-session.ts` 仅只读读码，`PI_API_KEY` 门只做站点定位未测量、未立项，S1-I-5 / S7-I-4 维持关闭）；无阈值/测试/公开签名变更。仓库变更仅本报告一个文件；无赢家故未提交任何 scripts/（全部 harness 源码进附录 A，遵守 R7-F/R8-C/R9-I～R24-I 纪律）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README 未触碰；未触碰在飞 R25-G / R25-H 分支与文件。

## 0. 范围与约束遵守

- **切片清单（git 权威，@HEAD 与 @8dee7fb 集合相同，恰 25 文件）**：
  - `src/cli/`（13）：adapt.ts、auth.ts、commits.ts、doctor-overlay.ts、doctor.ts、episode.ts、errors.ts、flowchart-io.ts、inject.ts、main.ts、model-catalog.ts、models.ts、pause.ts
  - `src/pi-adapter/`（9）：auth-session.ts、cluster-tools.ts、file-credential-store.ts、index.ts、listed-model-common.ts、listed-model-lazy.ts、listed-model.ts、pi-executor.ts、runtime.ts
  - `src/config/`（2）：model-ref.ts、providers-config.ts
  - `src/telemetry/`（1）：model-invocation.ts
- 未重开任何 X\* / S1-\*…S24-\* 条目；R9-I～R24-I 空枚举维持，未事后发明 S9-I-\* ～ S24-I-\*。逐条 rename-check：
  - **未重做已落地赢家 S1-I / S4-I / S5-I-1 / S7-I-1**：零漂移即字节级证明（站点逐项重验，结论第 1 条），三个等价性仿真复跑为行为级证明。
  - **未重开 S8-I-1**（custom id 跳回退）：本轮仅做文件级重开条件确认（§2.2），未满足，维持淘汰。拒绝端点格只是**观察**现行回退在 env 门上的既有价格，未构造替代注册表、未触碰回退路径一行。**两臂文件级 blocked 按派单原样维持。**
  - **与 R24-I「适应态坐标」的划界（本区自有轴，不补铸）**：其对象是 `adaptation/registry.json` 的存在与字节规模（磁盘累积态文件）；本轴对象是进程环境块（每次 spawn 由调用方注入的易失键值面）。本轮全部夹具无 registry.json（适应态平面维持其零角），children/track 每 run 双装载接线未触碰。
  - **与 R14-I「providers 配置空间」/ R19-I「E×P×K 配置基数」的划界**：二者剂量的是 providers.json 这扇门内的值与基数；本轴剂量的是**门本身的来源拓扑**（flag/env/providers/default 四源优先级链 × env 键存在性），并给出「门源平坦性」量化结论（env 门与 providers 门对同一表 import 机器同价、custom 税只附着 enabled 列表门）。providers.json 内部值域与基数本轮未再剂量。
  - **与 R25-B「task-face optional-field presence topology」的划界（禁移植轴）**：其对象是 B 辖区 `AssignableTask` 域对象的可选字段存在角（进程内 PIC/形状机制）；本轴对象是进程输入通道（环境块）的键存在性 × 优先级选择（spawn 面配置机制）。对象、面、机制三不同，未移植其 2³ 角格法到任何域对象。
  - **与 R24-D「approval-authority face」的划界（禁移植轴）**：其剂量对象是 promote 路径的自动提升模式 × 预算标量（D 辖区门链）；本轴的 SPARKLE_AUTO_ADAPT 格只在 run 尾部 auto-loop 的收集/提案闸（E 辖区调用点、I 矩阵 spawn 面）上取 e2e 差并做行为见证，promote 门链未触碰。`isAutoAdaptEnabled` 与 approval-profile 的自动提升预算是两个不同机制（注释明示「auto-loop never CAS-promotes」）。
  - **与 R17-I「拒绝路径定价」的划界**：其普查的是当时矩阵内的错误端点集合；env 门 unknown-model 拒绝是本轮新坐标带来的**新端点**，其价格 = providers/all 回退全额（既有已定价机器），不构成候选（once-per-command reject 类 + S8-I-1 硬禁），仅作为交叉互锚记录。
  - **与 R25-A「value-domain / string content-class census」的划界（禁移植轴）**：其对象是固定长度下的字节内容类；本轴对 env 值只用两个固定 ref（openai/gpt-4o、customco/custom-big），未做任何内容类扫描。
  - **未换名移植其余兄弟轴**（R25-F plan-header identity-geometry / **AGEX** / **CG** / R25-D identity-coordinate face / R24-B M=1 collapse / R24-F provenance face / R24-H operand-residency / **VALCLASS** / **CALLB** / **CKMIX** / **ORDX** / **SEEDX** / **YMIX** / **SCALEX** / **FITQ** / **TERMCLASS** / **SCHEDWIN** / **XPROC** / **NAMESHAPE** / **SHAPEK** / **KFAN** / **NVG** / **RUNLIM** / **JB** / **OCCX** / GS）——逐一定位核过，与本轴对象机制无交集。
  - **未重铸 R23-I `.pi/` 树平面**（全部夹具项目无 `.pi/` 树）/ **R22-I run 记录回读平面**（无 inspect/commits/episode 规模格）/ **R21-I flowchart 平面**（flat64 规格维持承重形态未扩展）/ **R20-I 累积遥测 N 轴**（全部夹具 invocations.jsonl 不存在）/ **R19-I E×P×K**（custom 夹具维持 P=1/K=2/E=2）/ **R18-I children 基数轴**（C=2）。
  - **S2-I-1（普通 run 死载荷 loadLearnedRouting 下沉）/ S3-I-3（loadProvidersConfig 下沉）未重提**——本轴对 loadProvidersConfig 只做剂量观测（38-82 µs），未提议移动任何装载。
  - **凭据面围栏遵守**：`PI_API_KEY` 门（main.ts L163）只做站点定位；`file-credential-store.ts` / `auth-session.ts` 仅只读读码，未测量、未立项。
- 硬不变量（生产零 diff 下天然成立，仍逐条核对）：分析不变更在飞 run；tracking 无指挥权；H/score 不写 PASS/FAIL；Live=R0 等价（本轮 W1/W2 规范化全同见证遮蔽与 kill-switch 格）；双 LCB / 双归因在位；提升 proposal-first（无任何提升）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不动；`providers/all` 失手回退 + throw-outside-catch 在位（拒绝格即其按设计工作的直接见证）；无 fsync / 完整性再哈希削除；fail-open 族保持关闭；未改任何测试。**新轴全程零代码改动、零行为注入**——所有测量都是对现行环境门的直接观测 + 合法 env 键注入（文档化的公开配置面：USAGE/README 记载的 PI_* 变量族）。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / 任何 PLAN.md / README；未开 PR；未 force-push 独占分支；未触碰在飞 R25-H / R25-J 分支与文件（R25-G 已由父代理收口并经快进吸收）。

## 1. 本 VM 复测：7 格承重再锚 + 新轴 11 格（双 Node × 三运行，未拷贝历轮数字）

R9-I～R24-I 抽测集承重面复刻并**新增 11 个进程环境格**（每迭代全新拷贝 state-root、基底 env 显式清除 `PI_PROVIDER`/`PI_MODEL`/`PI_API_KEY`/`PI_FAST_MODEL`/`SPARKLE_AUTO_ADAPT`/`PI_THINKING_LEVEL` 后按格注入、2 预热 + 10 测量、显式二进制全路径；harness 全文附录 A.2，三次运行原始输出附录 A.5）。

### 1.1 承重集三运行中位数带（ms，spawn-to-exit；22.14.0 / 22.22.2）

| 格 | run1 | run2 | run3 |
|---|---|---|---|
| `--version`（地板） | 58.8 / 53.3 | 54.6 / 53.5 | 54.8 / 52.9 |
| run-children@builtin | 113.9 / 107.1 | 109.6 / 108.5 | 115.0 / 108.7 |
| run-track@builtin | 130.8 / 125.2 | 129.4 / 127.2 | 128.6 / 120.2 |
| run-children@custom | 160.8 / 132.9 | 160.3 / 134.2 | 152.0 / 129.6 |
| run-track@custom | 183.9 / 150.8 | 175.1 / 152.2 | 180.7 / 143.8 |
| run-flowchart@custom-flat64 | 273.0 / 231.4 | 271.6 / 237.6 | 269.1 / 243.0 |
| run-flowchart@builtin-flat64 | 230.7 / 216.3 | 219.1 / 220.2 | 216.9 / 218.1 |

- **custom−builtin 差（三运行带）**：children **+37.0~+50.7 / +20.9~+25.8**、track **+45.7~+53.1 / +23.6~+25.6**、flowchart-flat64 **+42.3~+52.5 / +15.1~+24.9** ms（22.14/22.22）。R24-I 点值 +44.2/+26.5、+48.0/+25.9、+48.7/+16.3 全部落带或贴带缘——**十八轮同号、同量级、同 Node 版本梯度**。带缘偏移（children run3@22.14 的 +37.0 等）落在 R15-I 轴 1 的 A/A 噪声包络内（22.14 单窗 maxAbs ~34.5 ms 量级），无新结构。归属未变：`buildLiveCatalogConfig` → `resolveListedModelLazy` → per-provider 导入对 custom id 失手 → `providers/all` 回退。
- 全部 exit 按预期（拒绝格 exit 1，其余 exit 0），三运行 × 双引擎无一失败。

### 1.2 新轴的生产流证明（先证门存在与流量，再证遮蔽史，再证数字）

- **门与流量**：承重 run 路径上的环境读恰有三处（全仓 `process.env` grep 穷举后按调用图筛选）：
  1. `main.ts` **L626-629** `envCatalogId = PI_PROVIDER && PI_MODEL ? formatModelRef(...) : undefined`（每次 `run` 无条件求值）；**L630-635** primaryModelId 优先级链 `flag ?? providers.primary ?? envCatalogId ?? (pi: PI_MODEL) ?? "premium"`；**L636-637** fastModelId 链 `flag ?? PI_FAST_MODEL ?? providers.fast ?? "cheap"`。解析出的两 id 经 `smartChildPlan`（**L357**）与 `startTrackedRun`（track/loop.ts **L82-85**）传入 `buildLiveCatalogConfig(stateRoot, {primaryModelId, fastModelId})` 的 overrides——**children 与 track 两个承重类都被环境门直接支配**。
  2. `model-catalog.ts` **L38-48**：`primaryId`/`fastId` 进入 enabled 解析循环（L42 `[...config.enabled, primaryId, fastId]`）；过不了 `tryParseModelRef` 的 id 被跳过，enabled 为空即走 `catalogFromPrimary`（**零 pi-ai import**）。默认 id `"premium"`/`"cheap"`（primary-catalog.ts L5-6）不是 `provider/model` 形状 ⇒ **默认态 + 环境缺席 = 零 import 腿**；任何可解析 env ref ⇒ **L58-61 per-provider 表 import 腿（S7-I-1 机器）**；未声明 custom ref ⇒ per-provider 失手 → `providers/all` 回退 → L62-63 throw `unknown model`。
  3. `approval-profile.ts` **L45-48** `isAutoAdaptEnabled`（SPARKLE_AUTO_ADAPT kill switch），auto-loop.ts **L93** 每承重 run 尾部读取；disabled 时收集与诊断照付、仅提案腿被闸（注释与实现一致：「still collects」）。
  - 非承重面的环境门（枚举完备性）：`createExecutor("pi")` L132-133/L143/L163（PI_PROVIDER/PI_MODEL/PI_THINKING_LEVEL/PI_API_KEY——仅 `--executor pi`，凭据面围栏内只定位不测量）；`adapt.ts` L167/L183（PI_MODEL——一次性命令类）；`doctor.ts` L98（npm_config_user_agent）与 `doctor-overlay.ts` L48（PI_SKILL_ROUTE_LOG）、`dispatch-preflight.ts` L167（PI_CODING_AGENT_DIR，切片外，doctor 为唯一 CLI 读者）——全部 once-per-command 诊断类。
- **遮蔽史（R7-I 教训第四坐标系）**：R24-I 附录 A.2（及其上溯 R9-I 同构 harness）每迭代 `delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY`，且从未设置 PI_FAST_MODEL / SPARKLE_AUTO_ADAPT——**历轮矩阵的环境块被固定在全缺席角**。历轮「配置态」叉乘过 providers 态（R14-I）、项目态（R23-I）、适应态（R24-I），进程环境态（键存在性 × 来源优先级 × 门下游翻腿）从未进入矩阵。本 VM 基底 env 实测 0 个 `PI_*`/`SPARKLE*` 键（历轮数字不受追溯污染；盲区是坐标未剂量，不是数字被污染）。
- **正交性**：剂量注入只改子进程 env 键；providers.json / `.pi/` 树 / registry.json / invocations.jsonl / children C=2 / flowchart 规格全部维持既有坐标不动。

### 1.3 轴 1：进程环境坐标标定（本轮新证据，不铸 ID）

**(a) 端到端剂量（三运行中位数带，相对各自基线格的差，ms，22.14/22.22）**：

| 格（相对基线） | run1 | run2 | run3 | 判决 |
|---|---|---|---|---|
| children@default+envPair − children@default | +5.9 / +2.6 | +3.2 / +2.4 | +2.0 / +4.9 | **单表冷 import 个位 ms** |
| children@default+envPair2tab − children@default | +2.2 / +1.3 | +5.8 / −0.9 | +4.1 / +1.6 | 同带（第二表 ~1 ms 沉入噪声） |
| children@default+envFast − children@default | +1.0 / +6.1 | +2.7 / −2.0 | +1.4 / +4.5 | 同带（fast 门同机器） |
| track@default+envPair − track@default | +4.5 / +8.6 | +2.5 / −0.1 | +12.3 / +6.4 | 命令类无关 |
| children@builtin − children@default（providers 门对照） | +5.7 / +2.0 | +4.0 / +4.4 | +7.9 / +5.2 | **门源平坦：与 env 门同价** |
| children@custom+envPair − children@custom（遮蔽） | −5.3 / +0.7 | −6.8 / +0.9 | +5.8 / −1.0 | **≈0，符号翻转** |
| children@builtin+envPair − children@builtin（遮蔽） | −4.4 / +2.1 | +5.1 / +1.8 | −10.1 / −1.4 | ≈0，符号翻转 |
| children@default+killswitch − children@default | −0.7 / −0.4 | +2.7 / −1.7 | −2.6 / +0.3 | ≈0 |
| children@custom+killswitch − children@custom | −2.3 / +0.7 | +2.4 / +1.3 | +1.7 / +9.5 | ≈0/噪声 |
| children@default+envCustom(rej) − 地板（exit 1） | +53.5 / +28.6 | +47.9 / +30.5 | +54.0 / +30.2 | **providers/all 回退全额** |

新格绝对值（children@default：104.1-108.2 / 103.5-105.1；track@default：121.3-125.9 / 116.2-123.5 等）见附录 A.5 原始输出。三运行 × 双引擎 exit 全部按预期。

**(b) 进程内门段分解**（附录 A.3；双引擎并示；22.14/22.22）：

| 段 | 辖区 | 22.14.0 | 22.22.2 |
|---|---|---|---|
| loadProvidersConfig@default（ENOENT 腿） | config(I) | 38.1 µs | 42.5 µs |
| loadProvidersConfig@builtin / @custom | config(I) | 57.1 / 59.7 µs | 81.8 / 65.8 µs |
| **envCatalogId 组装（I 自有门段全额）** | **cli(I)** | **684 ns/call** | **679 ns/call** |
| buildLiveCatalogConfig 别名腿（首调 / 温） | cli(I) | 0.43 ms / 45.4 µs | 0.42 ms / 46.3 µs |
| override openai（**冷表 import**） | ESM/engine | **3.28 ms** | **3.06 ms** |
| override +anthropic（第二表冷） | ESM/engine | 1.09 ms | 1.18 ms |
| override 温 | cli(I) | 50.5 µs | 64.9 µs |
| **拒绝腿 time-to-throw（冷，providers/all 回退）** | ESM/engine + S8-I-1 面 | **53.77 ms** | **25.36 ms** |
| 拒绝腿温（重抛） | | 255.9 µs | 246.9 µs |

- e2e 边际与门段闭合：envPair 边际（+2.0~+5.9）≈ 冷表 3.1-3.3 ms（±A/A 抖动）；拒绝格边际（+47.9~+54.0 / +28.6~+30.5）≈ time-to-throw 冷（53.8 / 25.4）+ 配置装载 µs 级——**双线互证闭合**。r7i part D 本轮复跑给出第三线：builtin 构建后 providers/all 保持冷 **71.2 ms**、custom 回退命中缓存 **2.3 ms**（temperature 差异属 r7i 载荷探针的进程内制度，量级同族）。
- **I 切片份额**：环境门的 I 自有段（两次 env 属性读 + formatModelRef + 优先级链）**0.68 µs/run**；表 import 是 ESM/引擎地板（S8-I-2 已裁「ESM 实例化是主线程 CPU，无可重叠窗口」）；providers/all 回退是 S8-I-1 blocked 面。**无任何段构成 I 候选。**

**(c) 行为见证**（附录 A.4；双引擎 × 3 运行，26 检查全 0 失败）：

- **W1 遮蔽等价**：custom/builtin 配置态 ± envPair 的 children run 输出规范化后逐字相同（providers.primary 在路由站点胜出，L630-635 直接见证）。
- **W2 kill-switch 等价**：±SPARKLE_AUTO_ADAPT=0 输出在掩掉设计内 `adapt:` 理由行后逐字相同；disabled 态打印文档化理由 `auto-adapt disabled; collected only`、enabled 态打印 `no actionable model-project issue`（该行差异是 kill switch 的公开语义本体，非等价破坏——首版见证未掩行而假阳，修正为掩行 + 理由串双断言）。
- **W3 翻腿确定性**：envPair 格三次运行输出规范化逐字相同；结构行断言 `primary=openai/gpt-4o`、`tsk_a (implementer, MEDIUM) -> openai/gpt-4o`（翻腿后路由到真实 id）与默认格 `primary=premium`（别名腿）同时成立。

### 1.4 优先级不对称注记（只读观察，移交契约所有者，不铸 ID）

`createExecutor("pi")`（main.ts L116 注释「flag > env vars > providers.json」，L136-137 实现 `modelOverride ?? envProvider ?? primary`）与路由目录站点（L630-635 `flag ?? providers.primary ?? envCatalogId`）对「env vs providers.json」次序**相反**。fake-executor 承重格不可观察；`--executor pi` + 双源同设时执行通道与路由目录将各跟一源。是否有意（「providers.json 主导路由、env 主导执行通道」的 pin 语义）属产品/契约判定——**移交，不代行，非性能项**。

## 2. 第二十五组新角度枚举（诚实为空，未铸 ID）

方法：结构位置 × 消除机制双向扫描 25 文件（导入图惰性化 / 计算复用 / 缓存 / 并行重叠 / 语义降级 / 数据结构 / 复杂度类 / 输入规模定价 / 配置态坐标系补全），每形态先对 EXCLUSIONS 全表 + 二十四轮 I 报告 + 兄弟禁移植轴做同机制先例检索。

### 2.1 新轴候选性自查——测量收口，不铸 ID

- **轴 1（进程环境坐标）**是既有环境门在第四配置态坐标系上的直接观测：零行为注入、零代码改动。产出是「门源平坦性 + I 自有门段 0.68 µs + 表 import 个位 ms 天花板 + 拒绝端点 providers/all 全额互锚 + kill-switch 平坦 + 26 项行为见证」的裁决数字集，不是优化。
- **派单空转卫自查**：本轴有真实生产流（PI_* 变量族是 USAGE 文档化的公开配置面；envCatalogId 每 run 无条件求值）且填补的是历轮 harness **主动制造**的测量盲区（delete 语句可指认）——不属「零生产流 + 仅幻想尺度」空转型。不铸候选的理由是三重的：I 自有段亚 µs、表 import 段是引擎地板且属请求内工作（用户配置了该模型，import 该表就是交付）、拒绝段是 S8-I-1 blocked 面上的 once-per-command reject 类。

### 2.2 S8-I-1 重开条件文件级再确认——不铸 ID

- `dist/providers/all.models.js` 不存在（`ls` 直接确认）；`dist/models.generated.js` 存在但未导出（exports 图 7 项逐项复核无该子路径）；`dist/providers/` 恰 **39 个** `.models.js`（直接计数）；pi-ai 钉死 **0.84.1**。依派单「两臂文件级 blocked」纪律不重复执行级探针。
- **裁决**：重开条件连续第十八轮未满足；本轮拒绝格给出的 env 门交叉互锚进一步坐实回退结构的健全性价格。审计证据，非机制候选——**不铸 ID**。

### 2.3 其余机制类逐条映射（全部为排除行逐字重现、历轮无 ID 收口重现或量级/辖区即死）

| 扫描角度 | 处置 |
|---|---|
| **未声明 custom env ref 跳过 providers/all 回退** | **S8-I-1 逐字**（健全性反例封死）；且是错误路径 once-per-command reject 类。维持 blocked |
| **provider 表 / envCatalogId 跨进程缓存** | R10-I 跨进程磁盘快照围栏 + X1-1 隐藏持久状态族逐字。非候选 |
| **env 读提升/重排/惰性化** | I 自有段实测 0.68 µs/run——深度亚噪声；重排还动错误次序（S2-I-1/S3-I-3 同型风险）。非候选 |
| **env pair 全指定时跳过 loadProvidersConfig** | 损坏 providers.json 静默放行（S3-I-3 排除行逐字同型 fail-open）+ 38-82 µs 量级。非候选 |
| **两站点优先级统一** | 语义/产品契约变更（行为可观察发散），非保行为优化——§1.4 移交注记 |
| **kill-switch 闸前移（跳过收集/诊断）** | 公开语义本体（注释「still collects」+ e2e 实测 ≈0——无收益池）。非候选 |
| **别名腿快路径** | 已是现行零 import 腿（catalogFromPrimary），无可减物 |
| **defaultStateRoot()/homedir 解析** | 全部格显式传 --state-root；默认腿 = 一次 homedir+join，ns 级。非候选 |
| **PI_THINKING_LEVEL 校验表 Set 化** | pi-executor 腿 once-per-command + 表长 ~4（S1-D-8 族噪声）。非候选 |
| **表 import 段（3.1-3.3 ms/表）** | ESM 实例化引擎地板（S8-I-2 已裁无重叠窗口；S6-I-2 compile-cache 已裁构建面）。非候选 |
| 双装载去重 / 装载下沉 / 部分解析 / 完整性跳过 / 轮转工具 / JSON.parse / status stdout / save 攒批 | R24-I §2.3 十一行收口维持（本轮适应态平面零角未动，前提未变） |
| ingest/回读/flowchart 规模/配置基数/children 基数族 | R23-I / R22-I / R21-I / R19-I / R18-I 收口维持，本轮未触其规模格 |
| 拒绝路径 / 错误端点 / 肥 builtin 态 / A/A 地板 / 引擎代 / 堆足迹 / 采样归因 / 测量学 | R17-I / R14-I / R15-I / R16-I 收口维持；本轮带比对直接引用其 MDE 包络 |
| flowchart 双 build / providers.json 双读 / pause-inject 免 router / compile-cache / dist 布局 / 一次性命令类 / 凭据面 / 解析微观 / barrel 隐藏边 / root-compat 替代源 / per-provider 探针短路 / 预热重叠 | S1-I-1/2、S2-I-1..6、S3-I-1..6、S4-I-2..5、S5-I-2..5、S6-I-1..3、S7-I-2..4、S8-I-2/3、R11-I/R12-I/R13-I 无 ID 收口逐字重现——零漂移下前提逐项成立，本轮 25 文件重读未发现任何新站点脱离既有覆盖物 |
| telemetry / config 残余面 | R20-I 已把 telemetry 热站点定价贴底；本轮新坐标只经过 config 装载段（38-82 µs 已并入门段分解）；S1-D-8 / S3-I-5 族噪声维持 |
| 漂移带来的新导入边/新命令类 | 不存在：切片、依赖、pi-ai exports、测试面、基线增量五重零漂移（头部核查） |

**提议排除 ID：无。** 切片在现行排除表与硬不变量下不存在可落地候选；本轮把 I 切片**第四个配置态坐标系（进程环境）**收口：环境门的 I 自有段 0.68 µs/run、门源平坦性（env 门与 providers 门对表 import 机器同价、custom 税只附着 enabled 列表门、配置态下 env 被优先级链遮蔽 ≈0）、拒绝端点 providers/all 全额互锚（+47.9~+54.0 / +28.6~+30.5 ms 经第二扇门复现 custom−builtin 带）、kill-switch 平坦 + 全部行为见证。**I 切片自此六个输入平面 + 四个配置态坐标系（providers 态 × 项目态 × 适应态 × 进程环境态）全部收口。**

## 3. 验证面（本 VM）

- **等价性仿真复跑**（三个已落地赢家，零漂移基线上的行为级回归验证，22.22.2 + tsx；无代码落地故按派单为自愿加测）：`round04-r4i-equivalence-sim.ts` **68 checks / 0 失败**；`round05-r5i-equivalence-sim.ts` **119 / 0**；`round07-r7i-equivalence-sim.ts` **80 / 0**（part D 载荷探针复证 builtin 构建后 providers/all 保持冷 71.2 ms、custom 回退命中缓存 2.3 ms；part E configured run --children 全轮 normalized-identical）。
- **`pnpm gate`（派单项，Node 22.22.2）**：exit 0——`pnpm typecheck` 绿、`pnpm lint` 绿、全套件 **1169 用例：1168 pass / 0 fail / 1 skip**（与 R9-I～R24-I 计数逐字相同）、`pnpm build` 绿。诚实注记：gate 首跑误落 22.14.0 引擎，恰好只挂 doctor preflight 一例（engines ≥22.19 检查按设计 FAIL，`test/unit/cli/doctor.test.ts:22`）——22.22.2 重跑全绿；该假阳是 R15-I/S5-I-5 引擎纪律的直接证人，按纪律记录不掩。
- **CLI smoke**：`node dist/cli/main.js --version` → `0.1.0`，exit 0。
- **构建**：`pnpm install --frozen-lockfile` + `pnpm build` 通过（dist 产物即 §1 基准对象）。生产代码零改动；无赢家故无新仿真脚本入库；四个 harness/探针（genfix/spot/inproc/equiv）均为 /tmp 临时件，全文进附录 A，未进 lint 面。
- **引擎注记**：22.14.0 双引擎并测用于十八轮带比对；nvm 22.22.2 为 gate/仿真引擎。本轮无候选，新轴归因不依赖包络判读（e2e × 进程内门段 × r7i part D 三线互证 + 26 项行为见证）。

## 4. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举；新轴以测量收口关闭第四配置态坐标系；S8-I-1 文件级第十八轮 blocked 并获 env 门交叉互锚）。
- 剩余赢家：无（custom−builtin 结构维持 S8-I-1 健全性反例封死；环境坐标的全部成本段——I 自有 0.68 µs / 表 import 引擎地板 / 拒绝腿 blocked 面 reject 类——无一构成候选；全部消除形态映射 S8-I-1 / R10-I / X1-1 / S3-I-3 / S8-I-2 / S6-I-2 既有围栏）。
- 矩阵空洞：无（承重格十八轮同构复现 + 本轮补入环境坐标 11 格——{default,builtin,custom} × {children,track} × env{缺席,pair,pair2tab,fast,custom-rej,kill} 的归因完备组合；R7-I 纪律在第四配置坐标系上重演并收口；六输入平面维持 R18-I…R24-I 收口）。
- 跨辖区移交注记一条已在结论第 4 条 / §1.4 记录（契约所有者辖区：executor 站点与路由站点的 env-vs-providers 优先级不对称），非 I 候选。

**MORE_OPTIMA=no。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

### A.1 夹具生成（/tmp/r25i-bench/genfix.mjs）

```javascript
// R25-I fixture generation: bearing-set re-anchor states (R9-I..R24-I lineage)
// plus the NEW axis states for the process-environment configured coordinate.
// Every prior I-round spot harness executed `delete env.PI_PROVIDER; delete
// env.PI_MODEL; delete env.PI_API_KEY` per iteration and never set
// PI_FAST_MODEL / SPARKLE_AUTO_ADAPT / PI_THINKING_LEVEL — so the environment
// block was pinned to one corner (all-unset) across every historical matrix.
// The env doses themselves are set per-cell in spot.mjs; state fixtures here
// give the three provider-state coordinates the env cells cross against:
// default (no providers.json), builtin-configured, custom-configured.
import { mkdirSync, rmSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const ROOT = "/tmp/r25i-bench";
const FIX = join(ROOT, "fixtures");

const customProvider = {
  id: "customco", name: "Custom Co", baseUrl: "http://127.0.0.1:59999/v1", envVar: "CUSTOMCO_API_KEY",
  models: [
    { id: "custom-big", name: "Custom Big", contextWindow: 200000, maxTokens: 8192, inputCostPerMTok: 3, outputCostPerMTok: 15 },
    { id: "custom-small", name: "Custom Small", contextWindow: 100000, maxTokens: 4096, inputCostPerMTok: 0.3, outputCostPerMTok: 1.5 }
  ]
};
const states = {
  builtin: { version: 1, enabled: ["anthropic/claude-fable-5", "anthropic/claude-haiku-4-5", "openai/gpt-4o"], primary: "anthropic/claude-fable-5", fast: "anthropic/claude-haiku-4-5", customProviders: [] },
  custom: { version: 1, enabled: ["customco/custom-big", "customco/custom-small"], primary: "customco/custom-big", fast: "customco/custom-small", customProviders: [customProvider] }
};
rmSync(FIX, { recursive: true, force: true });
mkdirSync(FIX, { recursive: true });
function writeState(name, cfg) {
  const dir = join(FIX, name);
  mkdirSync(join(dir, "runtime"), { recursive: true });
  if (cfg !== undefined) {
    writeFileSync(join(dir, "runtime", "providers.json"), JSON.stringify(cfg, null, 2) + "\n");
  }
  return dir;
}
writeState("state-builtin", states.builtin);
writeState("state-custom", states.custom);
// NEW: default provider state — runtime/ exists but providers.json does not
// (loadProvidersConfig ENOENT leg; catalog resolves through the alias path
// unless an env door supplies a parseable provider/model ref).
writeState("state-default", undefined);

// Children spec (R9-I..R24-I lineage, C=2).
writeFileSync(join(FIX, "children.json"), JSON.stringify({ tasks: [
  { id: "tsk_a", role: "implementer", objective: "Do the work" },
  { id: "tsk_b", role: "reviewer", objective: "Review the work", dependsOn: ["tsk_a"] }
] }) + "\n");

// Flowchart fixtures (R21-I flat shapes; bearing cells only).
function flatFlow(n) {
  const nodes = [];
  for (let i = 0; i < n; i++) {
    nodes.push({
      id: `n${i}`, taskId: `tsk_f${i}`, role: "actor",
      objective: `Step ${i} of the benchmark flow`,
      modelPolicy: { allowedModels: ["cheap"] },
      confidenceThreshold: 0.7, approvalRequired: false
    });
  }
  return { id: `bench-flat-${n}`, nodes, edges: [] };
}
function resultsFor(n) {
  const out = {};
  for (let i = 0; i < n; i++) {
    out[`n${i}`] = {
      outcome: "SUCCESS", confidence: 0.9,
      evidenceIds: [`evd_f${i}`],
      facts: [{ key: `fact-${i}`, value: `value ${i}`, confidence: 0.8 }]
    };
  }
  return out;
}
writeFileSync(join(FIX, "flow-flat64.json"), JSON.stringify(flatFlow(64)) + "\n");
writeFileSync(join(FIX, "results-flat64.json"), JSON.stringify(resultsFor(64)) + "\n");

// Project scaffold (R9-I..R24-I shape; no .pi tree, no adaptation registry —
// R23-I / R24-I planes stay closed at their zero corner).
const PROJ = join(FIX, "project");
mkdirSync(join(PROJ, "src"), { recursive: true });
writeFileSync(join(PROJ, "package.json"), JSON.stringify({ name: "bench-project", version: "1.0.0" }) + "\n");
writeFileSync(join(PROJ, "src", "index.js"), "export const x = 1;\n");
console.log("FIXTURES_WRITTEN");
```

### A.2 18 格抽测 harness（/tmp/r25i-bench/spot.mjs，分别以两版本二进制执行 × 3 运行）

```javascript
// R25-I spot measure: R9-I..R24-I bearing set (floor + builtin/custom x
// children/track/flowchart re-anchor) + NEW axis: process-environment
// configured coordinate. Every prior I-round harness pinned the environment
// block to the all-unset corner (deleting PI_PROVIDER/PI_MODEL/PI_API_KEY and
// never setting PI_FAST_MODEL/SPARKLE_AUTO_ADAPT) — the fourth configuration
// coordinate system after providers state (R14-I), project state (R23-I) and
// adaptation state (R24-I). Env doors on the bearing path:
//   main.ts L626-637: envCatalogId (PI_PROVIDER+PI_MODEL, only if BOTH set;
//     precedence flag > providers.primary > env), PI_FAST_MODEL
//     (flag > env > providers.fast > "cheap");
//   the resolved ids reach buildLiveCatalogConfig overrides in both
//     smartChildPlan (main.ts L357) and startTrackedRun (track/loop.ts L82);
//   approval-profile.ts L45-48: SPARKLE_AUTO_ADAPT kill switch read at the
//     auto-loop tail of every bearing run.
// Structural prediction under test: on default provider state the alias ids
// ("premium"/"cheap") fail tryParseModelRef so the catalog takes the
// import-free catalogFromPrimary leg; a parseable env-sourced ref flips the
// same run onto the per-provider table-import leg (S7-I-1 machinery), and an
// undeclared custom env ref must walk per-provider miss -> providers/all
// fallback -> unknown-model rejection (exit 1).
// usage: <node-binary> spot.mjs
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, cpSync } from "node:fs";
import { join } from "node:path";
import { performance } from "node:perf_hooks";

const CLI = "/workspace/dist/cli/main.js";
const ROOT = "/tmp/r25i-bench";
const FIX = join(ROOT, "fixtures");
const WORK = join(ROOT, "work");
const nodeBin = process.execPath;
const WARMUP = 2, ITERS = 10;

const PROJ = join(FIX, "project");
const CHILDREN = join(FIX, "children.json");

const childrenArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--children", CHILDREN];
const trackArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--track", "--assume-defaults"];
const flowResultsArgs = (s) => ["run", "--project", PROJ, "--objective", "bench objective", "--state-root", s, "--flowchart", join(FIX, "flow-flat64.json"), "--results", join(FIX, "results-flat64.json")];

const ENV_PAIR = { PI_PROVIDER: "openai", PI_MODEL: "gpt-4o" };
const ENV_PAIR2 = { PI_PROVIDER: "openai", PI_MODEL: "gpt-4o", PI_FAST_MODEL: "anthropic/claude-haiku-4-5" };
const ENV_FAST = { PI_FAST_MODEL: "anthropic/claude-haiku-4-5" };
const ENV_CUSTOM = { PI_PROVIDER: "customco", PI_MODEL: "custom-big" };
const ENV_KILL = { SPARKLE_AUTO_ADAPT: "0" };

const cells = [
  // Bearing set re-anchor (R9-I..R24-I lineage, env corner all-unset).
  { id: "version", args: () => ["--version"], state: undefined },
  { id: "run-children@builtin", state: "state-builtin", args: childrenArgs },
  { id: "run-track@builtin", state: "state-builtin", args: trackArgs },
  { id: "run-children@custom", state: "state-custom", args: childrenArgs },
  { id: "run-track@custom", state: "state-custom", args: trackArgs },
  { id: "run-flowchart@custom-flat64", state: "state-custom", args: flowResultsArgs },
  { id: "run-flowchart@builtin-flat64", state: "state-builtin", args: flowResultsArgs },
  // NEW axis baseline: default provider state, env corner all-unset
  // (alias-catalog leg; the corner every historical cell sat in for
  // provider-default fixtures).
  { id: "run-children@default", state: "state-default", args: childrenArgs },
  { id: "run-track@default", state: "state-default", args: trackArgs },
  // Env dose cells on default state: parseable env refs flip the catalog leg.
  { id: "run-children@default+envPair", state: "state-default", args: childrenArgs, env: ENV_PAIR },
  { id: "run-children@default+envPair2tab", state: "state-default", args: childrenArgs, env: ENV_PAIR2 },
  { id: "run-children@default+envFast", state: "state-default", args: childrenArgs, env: ENV_FAST },
  { id: "run-track@default+envPair", state: "state-default", args: trackArgs, env: ENV_PAIR },
  // Rejection endpoint: undeclared custom ref through the env door
  // (per-provider miss -> providers/all fallback import -> unknown model).
  { id: "run-children@default+envCustom(rej)", state: "state-default", args: childrenArgs, env: ENV_CUSTOM, expectExit: 1 },
  // Shadowing witness: providers.primary outranks env at the routing site,
  // so env pair on configured states must price ~zero.
  { id: "run-children@custom+envPair", state: "state-custom", args: childrenArgs, env: ENV_PAIR },
  { id: "run-children@builtin+envPair", state: "state-builtin", args: childrenArgs, env: ENV_PAIR },
  // Kill-switch witness: collect+diagnose still run; only the proposal leg
  // (never triggered in bearing cells) is gated.
  { id: "run-children@default+killswitch", state: "state-default", args: childrenArgs, env: ENV_KILL },
  { id: "run-children@custom+killswitch", state: "state-custom", args: childrenArgs, env: ENV_KILL }
];

function freshState(state, cellId, iter) {
  if (state === undefined) return undefined;
  const dir = join(WORK, `${cellId.replace(/[^a-z0-9-]/gi, "_")}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, state), dir, { recursive: true });
  return dir;
}
function q(sorted, p) { const idx = (sorted.length - 1) * p, lo = Math.floor(idx), hi = Math.ceil(idx); return sorted[lo] + (sorted[hi] - sorted[lo]) * (idx - lo); }
rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
for (const cell of cells) {
  const times = []; let exit;
  for (let i = 0; i < WARMUP + ITERS; i++) {
    const sd = freshState(cell.state, cell.id, i);
    const args = typeof cell.args === "function" ? cell.args(sd) : cell.args;
    const env = { ...process.env };
    delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
    delete env.PI_FAST_MODEL; delete env.SPARKLE_AUTO_ADAPT; delete env.PI_THINKING_LEVEL;
    Object.assign(env, cell.env ?? {});
    const t0 = performance.now();
    const r = spawnSync(nodeBin, [CLI, ...args], { env, encoding: "utf8", timeout: 180000 });
    const dt = performance.now() - t0;
    exit = r.status;
    const want = cell.expectExit ?? 0;
    if (i === 0 && r.status !== want) console.error(`  FIRST-UNEXPECTED ${cell.id}: exit=${r.status} ${(r.stderr ?? "").slice(0, 200)}`);
    if (i >= WARMUP) times.push(dt);
  }
  times.sort((a, b) => a - b);
  console.log(`${cell.id}: median=${q(times, 0.5).toFixed(1)}ms min=${times[0].toFixed(1)} p90=${q(times, 0.9).toFixed(1)} exit=${exit}`);
}
```

### A.3 进程内门段分解（/tmp/r25i-bench/inproc.mjs，双引擎各执行一次）

```javascript
// R25-I in-process decomposition of the process-environment coordinate.
// Prices each door segment the env cells exercise:
//   (a) loadProvidersConfig: ENOENT default leg vs configured legs;
//   (b) envCatalogId assembly (two env reads + formatModelRef) — main.ts L626-629;
//   (c) buildLiveCatalogConfig: alias leg (default ids unparseable ->
//       catalogFromPrimary, zero imports) vs override leg (per-provider table
//       import, S7-I-1 machinery) — cold (first import in process) and warm;
//   (d) the rejection leg: undeclared custom override -> per-provider miss ->
//       providers/all fallback import -> unknown-model throw (time-to-throw,
//       cold), then warm repeat.
// The I-slice share of the env coordinate is: two process.env property reads,
// one template-literal formatModelRef, and the ?? precedence chain — priced
// here directly. Table imports are ESM/engine territory; providers/all is the
// S8-I-1 blocked plane.
import { performance } from "node:perf_hooks";
import { loadProvidersConfig } from "/workspace/dist/config/providers-config.js";
import { formatModelRef } from "/workspace/dist/config/model-ref.js";
import { buildLiveCatalogConfig } from "/workspace/dist/cli/model-catalog.js";

const FIX = "/tmp/r25i-bench/fixtures";
function median(xs) { const s = [...xs].sort((a, b) => a - b); return s[Math.floor(s.length / 2)]; }
async function timeAsync(reps, warm, fn) {
  const ts = [];
  for (let r = 0; r < reps + warm; r++) {
    const t0 = performance.now();
    await fn(r);
    if (r >= warm) ts.push(performance.now() - t0);
  }
  return median(ts);
}

// (a) providers config load by state
for (const st of ["state-default", "state-builtin", "state-custom"]) {
  const t = await timeAsync(9, 2, () => loadProvidersConfig(`${FIX}/${st}`));
  console.log(`loadProvidersConfig@${st}: ${(t * 1000).toFixed(1)}us`);
}

// (b) envCatalogId assembly (the whole I-slice-owned segment of the door)
process.env.PI_PROVIDER = "openai";
process.env.PI_MODEL = "gpt-4o";
{
  const N = 100000;
  let sink = 0;
  const t0 = performance.now();
  for (let i = 0; i < N; i++) {
    const envCatalogId =
      process.env.PI_PROVIDER !== undefined && process.env.PI_MODEL !== undefined
        ? formatModelRef(process.env.PI_PROVIDER, process.env.PI_MODEL)
        : undefined;
    sink += envCatalogId === undefined ? 0 : envCatalogId.length;
  }
  const perCall = ((performance.now() - t0) / N) * 1e6;
  console.log(`envCatalogId assembly: ${perCall.toFixed(1)}ns/call (sink=${sink})`);
}

// (c) catalog legs — cold import measured first, in dependency-clean order
const DEF = `${FIX}/state-default`;
const tAliasCold = await timeAsync(1, 0, () => buildLiveCatalogConfig(DEF));
console.log(`buildLiveCatalogConfig alias-leg first-call: ${tAliasCold.toFixed(2)}ms`);
const tAliasWarm = await timeAsync(9, 2, () => buildLiveCatalogConfig(DEF));
console.log(`buildLiveCatalogConfig alias-leg warm: ${(tAliasWarm * 1000).toFixed(1)}us`);
const tOv1Cold = await timeAsync(1, 0, () => buildLiveCatalogConfig(DEF, { primaryModelId: "openai/gpt-4o" }));
console.log(`buildLiveCatalogConfig override openai (cold table import): ${tOv1Cold.toFixed(2)}ms`);
const tOv2Cold = await timeAsync(1, 0, () => buildLiveCatalogConfig(DEF, { primaryModelId: "openai/gpt-4o", fastModelId: "anthropic/claude-haiku-4-5" }));
console.log(`buildLiveCatalogConfig override openai+anthropic (2nd table cold): ${tOv2Cold.toFixed(2)}ms`);
const tOvWarm = await timeAsync(9, 2, () => buildLiveCatalogConfig(DEF, { primaryModelId: "openai/gpt-4o", fastModelId: "anthropic/claude-haiku-4-5" }));
console.log(`buildLiveCatalogConfig override warm: ${(tOvWarm * 1000).toFixed(1)}us`);

// (d) rejection leg: time-to-throw for undeclared custom override.
// Cold call pays per-provider miss + providers/all fallback import.
async function throwLeg() {
  try {
    await buildLiveCatalogConfig(DEF, { primaryModelId: "customco/custom-big" });
    return "NO_THROW";
  } catch (err) {
    return err.constructor.name;
  }
}
{
  const t0 = performance.now();
  const kind = await throwLeg();
  console.log(`rejection leg cold (providers/all fallback + throw): ${(performance.now() - t0).toFixed(2)}ms [${kind}]`);
  const tWarm = await timeAsync(9, 2, throwLeg);
  console.log(`rejection leg warm: ${(tWarm * 1000).toFixed(1)}us`);
}
```

原始输出：

```
== v22.14.0 ==
loadProvidersConfig@state-default: 38.1us
loadProvidersConfig@state-builtin: 57.1us
loadProvidersConfig@state-custom: 59.7us
envCatalogId assembly: 684.2ns/call (sink=1300000)
buildLiveCatalogConfig alias-leg first-call: 0.43ms
buildLiveCatalogConfig alias-leg warm: 45.4us
buildLiveCatalogConfig override openai (cold table import): 3.28ms
buildLiveCatalogConfig override openai+anthropic (2nd table cold): 1.09ms
buildLiveCatalogConfig override warm: 50.5us
rejection leg cold (providers/all fallback + throw): 53.77ms [DomainValidationError]
rejection leg warm: 255.9us
== v22.22.2 ==
loadProvidersConfig@state-default: 42.5us
loadProvidersConfig@state-builtin: 81.8us
loadProvidersConfig@state-custom: 65.8us
envCatalogId assembly: 678.5ns/call (sink=1300000)
buildLiveCatalogConfig alias-leg first-call: 0.42ms
buildLiveCatalogConfig alias-leg warm: 46.3us
buildLiveCatalogConfig override openai (cold table import): 3.06ms
buildLiveCatalogConfig override openai+anthropic (2nd table cold): 1.18ms
buildLiveCatalogConfig override warm: 64.9us
rejection leg cold (providers/all fallback + throw): 25.36ms [DomainValidationError]
rejection leg warm: 246.9us
```

### A.4 行为见证（/tmp/r25i-bench/equiv.mjs，双引擎各执行一次）

```javascript
// R25-I behavioral witnesses for the process-environment coordinate.
//   W1 shadow: providers.primary outranks envCatalogId at the routing site
//      (main.ts L630-635), so children@custom with and without the env pair
//      must produce normalized-identical output.
//   W2 kill switch: SPARKLE_AUTO_ADAPT=0 still collects + diagnoses; the only
//      designed observable divergence is the `adapt:` reason line ("auto-adapt
//      disabled; collected only" vs "no actionable model-project issue").
//      Witness: masking that one line, outputs are normalized-identical, and
//      the disabled run prints exactly the documented disabled reason.
//   W3 env-pair determinism: the flipped catalog leg routes identically on
//      every repeat (same routing lines, same event count).
// Each witness is run 3x; ids and state paths are normalized before diff.
import { spawnSync } from "node:child_process";
import { mkdirSync, rmSync, cpSync } from "node:fs";
import { join } from "node:path";

const CLI = "/workspace/dist/cli/main.js";
const FIX = "/tmp/r25i-bench/fixtures";
const WORK = "/tmp/r25i-bench/equiv-work";
const PROJ = join(FIX, "project");
const CHILDREN = join(FIX, "children.json");

function normalize(text, stateDir) {
  return text
    .replaceAll(stateDir, "<STATE>")
    .replace(/run_[0-9a-f-]+/g, "run_X")
    .replace(/ep_[0-9a-f-]+/g, "ep_X")
    .replace(/evd_[0-9a-f-]+/g, "evd_X")
    .replace(/cnd_[0-9a-f-]+/g, "cnd_X")
    .replace(/agt_[0-9a-f-]+/g, "agt_X");
}
function runCell(state, extraEnv, tag, iter) {
  const dir = join(WORK, `${tag}-${iter}`);
  rmSync(dir, { recursive: true, force: true });
  cpSync(join(FIX, state), dir, { recursive: true });
  const env = { ...process.env };
  delete env.PI_PROVIDER; delete env.PI_MODEL; delete env.PI_API_KEY;
  delete env.PI_FAST_MODEL; delete env.SPARKLE_AUTO_ADAPT; delete env.PI_THINKING_LEVEL;
  Object.assign(env, extraEnv);
  const r = spawnSync(process.execPath, [CLI, "run", "--project", PROJ, "--objective", "bench objective", "--state-root", dir, "--children", CHILDREN], { env, encoding: "utf8", timeout: 180000 });
  if (r.status !== 0) throw new Error(`${tag}-${iter} exit=${r.status}: ${r.stderr}`);
  return normalize(r.stdout, dir);
}

rmSync(WORK, { recursive: true, force: true });
mkdirSync(WORK, { recursive: true });
let checks = 0, failures = 0;
function assertEq(a, b, label) {
  checks++;
  if (a !== b) { failures++; console.error(`FAIL ${label}`); }
}

for (let i = 0; i < 3; i++) {
  // W1 shadow (custom state)
  const plainCustom = runCell("state-custom", {}, "custom-plain", i);
  const envCustom = runCell("state-custom", { PI_PROVIDER: "openai", PI_MODEL: "gpt-4o" }, "custom-envpair", i);
  assertEq(plainCustom, envCustom, `W1 shadow custom run${i}`);
  // W1b shadow (builtin state)
  const plainBuiltin = runCell("state-builtin", {}, "builtin-plain", i);
  const envBuiltin = runCell("state-builtin", { PI_PROVIDER: "openai", PI_MODEL: "gpt-4o" }, "builtin-envpair", i);
  assertEq(plainBuiltin, envBuiltin, `W1b shadow builtin run${i}`);
  // W2 kill switch (default + custom states): identical modulo the designed
  // adapt-reason line, and the disabled reason is exactly the documented one.
  const maskAdapt = (s) => s.replace(/^ {2}adapt: .*$/m, "  adapt: <MASKED>");
  const defPlain = runCell("state-default", {}, "default-plain", i);
  const defKill = runCell("state-default", { SPARKLE_AUTO_ADAPT: "0" }, "default-kill", i);
  assertEq(maskAdapt(defPlain), maskAdapt(defKill), `W2 killswitch default run${i}`);
  assertEq(defKill.includes("adapt: auto-adapt disabled; collected only"), true, `W2 disabled reason default run${i}`);
  assertEq(defPlain.includes("adapt: no actionable model-project issue"), true, `W2 enabled reason default run${i}`);
  const customKill = runCell("state-custom", { SPARKLE_AUTO_ADAPT: "0" }, "custom-kill", i);
  assertEq(maskAdapt(plainCustom), maskAdapt(customKill), `W2 killswitch custom run${i}`);
  assertEq(customKill.includes("adapt: auto-adapt disabled; collected only"), true, `W2 disabled reason custom run${i}`);
  // W3 env-pair determinism across repeats
  const envDef = runCell("state-default", { PI_PROVIDER: "openai", PI_MODEL: "gpt-4o" }, "default-envpair", i);
  if (i === 0) {
    globalThis.__envDef0 = envDef;
    // structural spot-checks of the flipped leg
    assertEq(envDef.includes("primary=openai/gpt-4o"), true, "W3 env primary line");
    assertEq(/tsk_a \(implementer, MEDIUM\) -> openai\/gpt-4o/.test(envDef), true, "W3 env routing line");
    // and the unflipped default leg routes to aliases
    assertEq(defPlain.includes("primary=premium"), true, "W3 default alias line");
  } else {
    assertEq(envDef, globalThis.__envDef0, `W3 env-pair determinism run${i}`);
  }
}
console.log(`equiv checks=${checks} failures=${failures}`);
if (failures > 0) process.exit(1);
```

输出（22.22.2 与 22.14.0 各一次）：

```
equiv checks=26 failures=0
equiv checks=26 failures=0
```

（首版 W2 未掩 `adapt:` 行而 6 项假阳——差异恰为 kill switch 的文档化理由串 `auto-adapt disabled; collected only` vs `no actionable model-project issue`，即公开语义本体；修正为掩行等价 + 理由串双断言后全绿。该假阳作为「kill switch 有可观察输出语义、不可作等价消除对象」的直接证据记录在案。）

### A.5 三次运行原始输出（spot-run1/2/3.txt）

```
### run1
== v22.14.0 ==
version: median=58.8ms min=55.2 p90=62.8 exit=0
run-children@builtin: median=113.9ms min=108.7 p90=119.5 exit=0
run-track@builtin: median=130.8ms min=124.6 p90=135.0 exit=0
run-children@custom: median=160.8ms min=154.4 p90=174.5 exit=0
run-track@custom: median=183.9ms min=173.3 p90=200.4 exit=0
run-flowchart@custom-flat64: median=273.0ms min=259.9 p90=289.9 exit=0
run-flowchart@builtin-flat64: median=230.7ms min=211.5 p90=256.9 exit=0
run-children@default: median=108.2ms min=104.7 p90=112.1 exit=0
run-track@default: median=125.9ms min=119.9 p90=127.7 exit=0
run-children@default+envPair: median=114.1ms min=111.7 p90=114.7 exit=0
run-children@default+envPair2tab: median=110.4ms min=105.5 p90=114.4 exit=0
run-children@default+envFast: median=109.2ms min=106.1 p90=115.7 exit=0
run-track@default+envPair: median=130.4ms min=123.5 p90=136.0 exit=0
run-children@default+envCustom(rej): median=112.3ms min=107.5 p90=123.3 exit=1
run-children@custom+envPair: median=155.5ms min=146.0 p90=172.4 exit=0
run-children@builtin+envPair: median=109.5ms min=102.1 p90=124.8 exit=0
run-children@default+killswitch: median=107.5ms min=101.3 p90=110.8 exit=0
run-children@custom+killswitch: median=158.5ms min=151.4 p90=168.5 exit=0
== v22.22.2 ==
version: median=53.3ms min=51.1 p90=57.1 exit=0
run-children@builtin: median=107.1ms min=105.0 p90=108.6 exit=0
run-track@builtin: median=125.2ms min=116.6 p90=144.4 exit=0
run-children@custom: median=132.9ms min=125.0 p90=133.6 exit=0
run-track@custom: median=150.8ms min=140.1 p90=158.7 exit=0
run-flowchart@custom-flat64: median=231.4ms min=226.0 p90=237.2 exit=0
run-flowchart@builtin-flat64: median=216.3ms min=211.4 p90=225.9 exit=0
run-children@default: median=105.1ms min=97.6 p90=110.7 exit=0
run-track@default: median=119.0ms min=114.9 p90=124.6 exit=0
run-children@default+envPair: median=107.7ms min=101.2 p90=111.9 exit=0
run-children@default+envPair2tab: median=106.4ms min=103.6 p90=116.6 exit=0
run-children@default+envFast: median=111.2ms min=103.3 p90=114.6 exit=0
run-track@default+envPair: median=127.6ms min=121.3 p90=132.7 exit=0
run-children@default+envCustom(rej): median=81.9ms min=80.1 p90=84.3 exit=1
run-children@custom+envPair: median=133.6ms min=129.1 p90=140.6 exit=0
run-children@builtin+envPair: median=109.2ms min=106.3 p90=110.6 exit=0
run-children@default+killswitch: median=104.7ms min=101.9 p90=108.5 exit=0
run-children@custom+killswitch: median=133.6ms min=129.5 p90=137.5 exit=0
### run2
== v22.14.0 ==
version: median=54.6ms min=53.3 p90=56.8 exit=0
run-children@builtin: median=109.6ms min=103.7 p90=112.8 exit=0
run-track@builtin: median=129.4ms min=125.0 p90=130.2 exit=0
run-children@custom: median=160.3ms min=155.1 p90=172.3 exit=0
run-track@custom: median=175.1ms min=169.0 p90=186.6 exit=0
run-flowchart@custom-flat64: median=271.6ms min=259.0 p90=281.2 exit=0
run-flowchart@builtin-flat64: median=219.1ms min=204.1 p90=221.7 exit=0
run-children@default: median=105.6ms min=100.5 p90=108.6 exit=0
run-track@default: median=124.6ms min=122.5 p90=128.4 exit=0
run-children@default+envPair: median=108.8ms min=106.0 p90=113.1 exit=0
run-children@default+envPair2tab: median=111.4ms min=109.2 p90=114.7 exit=0
run-children@default+envFast: median=108.3ms min=105.5 p90=111.2 exit=0
run-track@default+envPair: median=127.1ms min=123.3 p90=129.7 exit=0
run-children@default+envCustom(rej): median=102.5ms min=98.2 p90=112.0 exit=1
run-children@custom+envPair: median=153.5ms min=144.3 p90=164.5 exit=0
run-children@builtin+envPair: median=114.7ms min=111.0 p90=116.1 exit=0
run-children@default+killswitch: median=108.3ms min=100.1 p90=110.5 exit=0
run-children@custom+killswitch: median=162.7ms min=153.7 p90=171.5 exit=0
== v22.22.2 ==
version: median=53.5ms min=50.6 p90=54.3 exit=0
run-children@builtin: median=108.5ms min=101.9 p90=114.3 exit=0
run-track@builtin: median=127.2ms min=119.3 p90=132.7 exit=0
run-children@custom: median=134.2ms min=130.8 p90=139.3 exit=0
run-track@custom: median=152.2ms min=142.9 p90=158.8 exit=0
run-flowchart@custom-flat64: median=237.6ms min=231.4 p90=239.3 exit=0
run-flowchart@builtin-flat64: median=220.2ms min=200.5 p90=231.4 exit=0
run-children@default: median=104.1ms min=101.2 p90=105.4 exit=0
run-track@default: median=123.5ms min=119.6 p90=128.1 exit=0
run-children@default+envPair: median=106.5ms min=101.0 p90=109.0 exit=0
run-children@default+envPair2tab: median=103.2ms min=99.6 p90=108.5 exit=0
run-children@default+envFast: median=102.1ms min=99.5 p90=105.6 exit=0
run-track@default+envPair: median=123.4ms min=115.0 p90=129.2 exit=0
run-children@default+envCustom(rej): median=84.0ms min=81.9 p90=86.0 exit=1
run-children@custom+envPair: median=135.1ms min=131.7 p90=137.2 exit=0
run-children@builtin+envPair: median=110.3ms min=106.4 p90=111.5 exit=0
run-children@default+killswitch: median=102.4ms min=99.9 p90=105.6 exit=0
run-children@custom+killswitch: median=135.5ms min=122.3 p90=139.0 exit=0
### run3
== v22.14.0 ==
version: median=54.8ms min=53.4 p90=56.8 exit=0
run-children@builtin: median=115.0ms min=107.1 p90=119.1 exit=0
run-track@builtin: median=128.6ms min=125.7 p90=131.8 exit=0
run-children@custom: median=152.0ms min=147.2 p90=171.3 exit=0
run-track@custom: median=180.7ms min=163.6 p90=185.9 exit=0
run-flowchart@custom-flat64: median=269.1ms min=246.5 p90=277.8 exit=0
run-flowchart@builtin-flat64: median=216.9ms min=212.8 p90=219.7 exit=0
run-children@default: median=107.1ms min=101.3 p90=114.9 exit=0
run-track@default: median=121.3ms min=117.4 p90=127.2 exit=0
run-children@default+envPair: median=109.1ms min=104.5 p90=112.0 exit=0
run-children@default+envPair2tab: median=111.2ms min=105.6 p90=113.3 exit=0
run-children@default+envFast: median=108.5ms min=102.6 p90=111.1 exit=0
run-track@default+envPair: median=133.6ms min=122.4 p90=146.6 exit=0
run-children@default+envCustom(rej): median=108.8ms min=101.2 p90=113.9 exit=1
run-children@custom+envPair: median=157.8ms min=146.9 p90=163.7 exit=0
run-children@builtin+envPair: median=104.9ms min=101.1 p90=111.3 exit=0
run-children@default+killswitch: median=104.5ms min=98.5 p90=138.7 exit=0
run-children@custom+killswitch: median=153.7ms min=145.2 p90=160.8 exit=0
== v22.22.2 ==
version: median=52.9ms min=50.8 p90=55.7 exit=0
run-children@builtin: median=108.7ms min=100.6 p90=116.3 exit=0
run-track@builtin: median=120.2ms min=118.5 p90=122.6 exit=0
run-children@custom: median=129.6ms min=127.5 p90=133.5 exit=0
run-track@custom: median=143.8ms min=142.6 p90=149.1 exit=0
run-flowchart@custom-flat64: median=243.0ms min=231.2 p90=261.7 exit=0
run-flowchart@builtin-flat64: median=218.1ms min=209.0 p90=226.2 exit=0
run-children@default: median=103.5ms min=98.8 p90=105.8 exit=0
run-track@default: median=116.2ms min=111.6 p90=121.0 exit=0
run-children@default+envPair: median=108.4ms min=104.5 p90=118.1 exit=0
run-children@default+envPair2tab: median=105.1ms min=102.2 p90=107.8 exit=0
run-children@default+envFast: median=108.0ms min=104.9 p90=113.9 exit=0
run-track@default+envPair: median=122.6ms min=121.4 p90=126.2 exit=0
run-children@default+envCustom(rej): median=83.1ms min=80.2 p90=84.5 exit=1
run-children@custom+envPair: median=128.6ms min=127.6 p90=133.3 exit=0
run-children@builtin+envPair: median=107.3ms min=104.6 p90=110.9 exit=0
run-children@default+killswitch: median=103.8ms min=101.8 p90=109.0 exit=0
run-children@custom+killswitch: median=139.1ms min=131.1 p90=152.3 exit=0
```

### A.6 拒绝格取证（smoke）

```
$ PI_PROVIDER=customco PI_MODEL=custom-big node dist/cli/main.js run --project <proj> --objective smoke --state-root <default-state> --children children.json
error: unknown model "customco/custom-big"
  command: run
  stage: validation
  next: fix the reported error, then retry; use pi-sparkle doctor for preflight
exit=1
```

（env 门未声明 custom ref → per-provider 失手 → providers/all 回退 → model-catalog.ts L62-63 throw——结构预言逐字命中；env pair 生效 smoke 见 W3 结构行断言。）

MORE_OPTIMA=no
