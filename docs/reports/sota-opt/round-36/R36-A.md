MODEL_SLUG=claude-fable-5-thinking-xhigh

# R36-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第三十六遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 36 / R36-A（Round 1–35 同区第三十六遍）
**基线:** 派发 tip `cursor/sota-persistent-opt-83a1` @ `514b3ff`（R35-J 摄取 + Round 35 关轮 + Round 36 开卷 + 本 R36-A/B/C 派发）；提交前 fetch 复核 exclusive 仍为 `514b3ff`——测量窗口内**零快进**，无需 rebase；姊妹 R36-B/C 在飞，无新姊妹报告可读，按令不等待不触碰其分支。
**分支:** `cursor/r36-a-tracking-thirty-sixth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R28-A…R35-A 同引擎位级相同；PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 显式绕开系统 Node v22.14.0——`node --version` / `process.versions.v8` 双复核，L0 见证 ×3 全过，全部官方读数在该引擎产出，零批次落在 v22.14.0 上；tsx 4.x、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S36-A-\* ID，不铸新 face 名、不取 ALLCAPS 绰号，切片零代码改动，EXCLUSIONS.md 零增行（全表仍 545 行）；整片预算收口第三十四次复核成立（本 VM 重锚 13.8–17.6 µs/gate ⇒ ~69–88 µs/run；run 3 五格整体较 run 1/2 均匀偏热 +1.5~+2.2 µs——同向全格平移证其为宿主偏置窗而非切片行为变化；带与 R35-A 剔孤点带 12.6–17.1、R34-A 12.8–15.2、R33-A 11.8–17.6 交叠；宿主偏置窗 + 冻结 0 行按令不构成新发现）。** R31-A 已把元坐标系逐面归属完毕并停靠最后一扇 injected-callable 门；R32-A 完成重开条件哨兵审计（F12a–d）；R33-A 把 R32-B 三腿方法应用到 A 区 44 行账本（44/44 仍立）；R34-A / R35-A 共六项换名检查已拒列。按令，上述均为方法 / 停驻记录——本轮**不重走** R32-B 闭合稳定性审计、**不再铸**「再查一遍闭合稳定性 / 重开条件 / 门扉」式新面，仅以卫生身份复跑哨兵断言（L0 / F12c / F12d / F11a-lite 语义证人，21 项 × 3 次官方运行共 63 项全过，无一重开条件触发）。本轮第三十六遍独立枚举从零重读 14 文件（`wc -l` 逐文件 79/29/38/155/76/107/22/14/137/103/195/522/83/236 = 合计 **1796** 行，与 R35-A 逐数一致）、重走 18 结构位置 × 13 消除机制满矩阵（§4），枚举出的全部角度经换名检查逐一裁决为既有排除 ID 本体、姊妹归档面（禁移植）或 R31-A 停靠门成员——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（3 项新站点级换名记录见 §0；R12-A…R35-A 共 114 项换名检查全部复核维持）。

切片 14 文件自 R1-A 基线（`7acb666`）经 R2-A … R35-A 至本轮 tip `514b3ff` **逐字节未变**（`git diff 7acb666 -- <切片>` 在派发 tip、测量后、提交前**三验均 0 行**，三十六遍全程零 diff；清单核对恰 14 文件（12 + 2）并全部实际重读）。调用面维持最强冻结再延一轮：`git diff 89f0455..514b3ff -- src/` **完全为空**（R10-A 基线以来跨 R10…R36 共二十七段全部提交均为 docs）。生产调用方经本轮全库 grep 普查复核仍为 `src/run/supervisor.ts:483/:487`（applyTrackingGate / nextTrackingSeq）、`src/run/coordinator.ts:444`、`src/run/flowchart-run.ts:320`（applyChildThreeLine），每子结果一次（~5 次/run），事件表几十级（E=41）。稳态下即使把整个切片优化到零成本，节省上界 ~0.069–0.088 ms/run，仍比落地线（数十~数百 ms）低约**三个量级**——第三十四次支配论证成立。聚焦套件 72/72 绿；`pnpm gate`（typecheck + lint + test + build）全绿（test 1169 / pass 1168 / fail 0 / skipped 1，与 R31-A…R35-A 逐字同计数；exit 0）。空枚举与 R12-A…R35-A 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config / from-child / gates / human-score / index / isolation / prescore / roller / turn / types）+ `src/run/child-tracking.ts` + `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**。静态普查对 coordinator.ts / flowchart-run.ts / supervisor.ts 及全 `src/` 树做**只读**见证（零编辑）。未触碰其余切片与任何兄弟分支（R36-B/C 在飞零触碰；exclusive 测量窗口内零快进，无姊妹报告需折入）。
- 先读并遵守（顺序强制）：README.md / EXCLUSIONS.md（全表 545 行）/ PROGRESS.md / round-36/PLAN.md（A 段 + 具名/unnamed 全 ban 榜）/ R35-A.md 全文（默认对照 + 三项换名记录——按令不再重提）。之后才从零重读 14 切片文件并动手。EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md / 任何 `src/` 零触碰。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：R32-A 哨兵审计 F12a–d（仅卫生复跑，不铸面）/ R32-B / R33-A 闭合稳定性方法（不重走、不移植结论、**禁止**再铸「再查一遍闭合稳定性」——本轮遵守：§2 只做哨兵卫生复跑，不做账本重分类）/ A 账本 44/44（方法卫生记录维持，不重审）/ R33-A…R35-J 空枚举（不重提「我又查了一遍重开条件 / 门扉 / 未点名站点」）/ **R35-A 三项换名检查**（human-score/types `{kind:"unobserved"}` 字面量五站点享元化 / types `asString` 首字符卫 / prescore dip 乘后判——本轮复核均维持拒列，不再单列、不重铸）/ R34-A 三项换名检查（child-tracking `skipped` 享元 / from-child `completedChecks` 防御拷贝 / turn `readersInvoked` 常量享元——复核维持）/ R31-A injected-callable 停靠门（`generateEventId` / `readers.readToolBodies` 非独立物理对象——仅以不计时语义证人复核重开条件，见 §2）/ **BINDX**（切片 `Math.*` token 恰 6 个于 5 行，同 BINDX 坐标，划界计数即止，零剂量）/ I/O text-codec 面（切片 fs 编解码位点 0，结构性空）/ REPRX（切片 `JSON.parse` 输入位点 0）/ R32-J 六残余坐标（本切片数据路径零磁盘 I/O，未过三合取，零重铸）。**FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX** 全部未编号未移植、未写入 EXCLUSIONS 表行。S12-B-2 / S13-B-1 / S8-I-1 / S8-E-1（`loadLearnedRouting` 恰 3 处生产调用维持）/ S9-D-4 / S12-D-1 / S5-H-1 / S7-F-1≠S6-F-5 全部切片外硬保持项零触碰。catalog 行序契约（R28-B W3）零触碰。npm POSIX `.bin` symlink 静默 no-op（R28-I）不当优化修。
- **换名重提检查**（本轮枚举全部新角度的逐项裁决，一律不铸 S36-A-\* ID。R12-A…R35-A 共 114 项换名检查全部复核维持；以下 **3 项为本轮新站点级记录**——均为在手报告未逐字点名过的站点，裁决同族同向，不新增任何 ID；若与更早轮次某条未在手记录重合，则为复命中，结论不变）：
  1. **types.ts:288/:405/:420 parse 路径每调用回调闭包提升**（`asStringArray` 的 `(item) => typeof item !== "string"` 谓词 + `parseGateDecision` 的 codes / openMinors 两个带 index 映射闭包各自每调用新铸）提升为模块级常量 = **S7-J-3 回调外提家族**（S3-E-5 字面量提升深度亚噪声同链）+ parse 是诊断口径组件（本轮整 parse 520–653 ns，占整门 <5%），逐回调 ns 级 + 写侧 validateEvent 每 gate 至多一次重放，拒列；
  2. **from-child.ts:124 `artifactIds: [...observation.artifactIds]` 防御拷贝省略**（prescoreInputFromObservation 构造 ToolSituation 时对 artifactIds 的 spread 拷贝）= **R34-A 记录 2 同函数同族**（completedChecks 防御拷贝，毗邻 :141）——别名省略使 ToolSituation.artifactIds 与外部 observation 共享可变引用（S1-B-8 / S2-J-5 同类身份新鲜性护栏），ns 级，拒列；
  3. **roller.ts:64 `new Set(keptMandatory.map((item) => item.key))` 中间映射数组消除**（改直接 add 循环免一次中间数组分配）= **S7-A-2 roller 中间拼接数组家族本体扩站**——同被已否决 S2-A-1（无截断态 keptMandatory ≡ mandatory 恒等过滤）支配，亚 µs，拒列。
- 其余枚举角度全部落在既有 ID **本体**上（非换位点，无需新记录）：hashAssessment dimensions 冗余 spread = S2-A-5；`localeCompare` 排序 = R26-A collation 本体 + X1-1/S8-G-2 memo 禁；JSON.stringify 换手写序列化 = S5-A-2；extractTenPoint 每调用 `new RegExp` = X0-6 本体 + humanInput ≡ `{}` 零流量；evidenceRefsOf 双算 = S2-A-2；第二次 prescore = S1-A-2；claims.some(isSuccessClaim) 双算 = S3-A-3；`every×includes` 两族站点 = S1-A-4/X4-7（C 轴可操作重开第一优先，重开条件未触发——F12c 见证 ×3）；from-child FAIL 守卫合取短路重排（failRefs.length 前置）= S6-A-2 本体；幂等双 find/死扫描 = S8-A-1/S11-A-2；五扫描逐条 = X0-4/X2-4/S1-A-1/S1-A-9/R1-A 清单；`[...input.events]` 拷贝预分配 = R1-A 不可变拷贝下界本体；spread+push 换 concat = S2-A-4；写侧 validateEvent = S4-G-2 族；roller keptIds 恒等过滤 / 无截断快路径 = S2-A-1 本体；uniqueStrings / uniqueCodes 中间拼接数组 = S7-A-2；confirmedDecisions includes 过滤 = S1-A-5；mandatory 死 text 字段 = S3-A-1；prevSummaryHash 链深 = R20-A 本体 + from-child 零流量（previous ≡ undefined）；小表 includes / evidenceWeight find = S1-A-8；X0-5 asRecord/asArray 合并禁维持；anomalyCodes 别名省略拷贝 = S1-A-7 原行本体；mergeOpenMinors 双空早退 = S3-A-4（其内部 `previous.map` 中间对数组同函数同裁决维持）；prescoreInput 克隆跳过 = S3-A-2（生产不可达）；条件 spread 后置化 = R33-A 记录 3 维持（dimension() 站点 = R28-A key-set geometry 面成员）；rollSummary 死 openMinors 覆盖 = S4-A-1；failClosed 分支 gate 重铸 = S2-A-1 已拒面零流量同伞；GateInput 死字段 = S8-A-3 本体（PrescoreInput `void actorSelfScore/independentEvidence` 双死读同族——规格意图文档 + 亚 ns）；gates 空 hardCodes 析取守卫 = S7-A-3；shouldEscalateMinors 单遍融合 = S1-A-6；combineScore 代数重排 = S7-A-4；toFixed 换 Math.round = S6-A-1；shouldApplyThreeLine 死首条件 = S4-A-3；caller 侧 verification 预检 = S4-A-2；PASSED 臂 check-coverage / constraint-retention 死校验短路 = S5-A-3 / S6-A-3；FAIL 守卫合取重排 = S6-A-2；from-child dimensions 多遍 = S2-A-6；生产路径 human 管道常量折叠 / 死 turn 输出尾 / 门控结局二元化 / apply 守卫常量折叠 = S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 四层收口维持；window contextFacts 模板字面量 + idempotencyKey 拼接 = R21-A 标识符几何面成员；RUN_BLOCKED 防御拷贝不对称 = R33-A 记录 2 维持；wait_user 腿外向回调（含 `"evt_".length` 常量）= R32-A 记录 1 维持（R31-A 平面成员 + 生产不可达）；`{ id, verdict }` 双形输出 = R28-A key-set geometry 面 + S8-A-3 PIC 注记；mapGateDirective / currentGateStatus 双调用位点 = 互斥路径非双算（R34-A §4 裁决维持）；executionAuthority / bindExecutionContext / evidenceWeight 死导出 = R22-A 公开导出普查本体；analysis.ts sanitize-then-project = S8-A-2 + 零 src/ 生产调用方（R22-A axis 11）；isolation/index 零逻辑面 = R29-A F9c(iv) 契约角维持。
- 未重开任何 X\* / S1-\* … S35-\* 条目。**单变量纪律**：本轮无计时新臂——锚点矩阵沿用血统 E=41 冻结夹具口径（组成/增长/键集几何/humanInput ≡ `{}`/M=0/K=0 逐轴钉死；本轮夹具与驱动为结构重建，见 §1.1 诚实注记）；L0/F12c/F12d/F11a-lite 纯语义断言不计时；对可调用物 / 内建绑定 / 分派历史零计时臂（R31-F 纪律；计时即 R28-F + R29-A + R30-A + BINDX 再剂量）。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点五格（gate-outcome × contract × max-codes）覆盖；配置态逐面复核——(a) settle 带/不带 assessment：生产两调用点（supervisor.ts:601/:711）经静态普查复验均不带 trackingAssessment（O(1) no-op 第二十七次复验，settle 函数头 `if (assessment === undefined) return`）；(b) trackingConfig 覆写/applyUserThreshold：生产恒 DEFAULT（X0-7 维持，F12d 四元组可执行见证 ×3）；(c) maxItems 截断态 = S2-A-1 已拒面；(d) 注入可调用物坐标 = R31-A 停放（重开条件 (a)/(b)/(c) 复核未触发，见 §2）。无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 / minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无命令权、H/score 不写路由 PASS/FAIL、Live = R0 等价、双 LCB 双归因保留、提升 proposal-first、无阈值变更、未改任何测试、无 fsync/完整性重哈希移除、fail-open 家族保持关闭）同样天然满足——本轮只经公开 applyChildThreeLine / applyTrackingGate / nextTrackingSeq / hashAssessment / parseTrackingAssessment / validateEvent / computePrescore / runTrackingTurn / assessChildObservation / observationFromChild / prescoreInputFromObservation 驱动。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本（按令不建 repo scripts；驱动指纹见附录 B，临时文件已删除）。不开 PR。

## 1. 预算支配论证复核（方法第 1、2 步）

R3-A §3 … R35-A §1 的整片预算论证两前提本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666 -- <切片>` 在派发 tip `514b3ff`、测量后、提交前三验均为空，逐字节一致（三十六遍全程零 diff）——R1-A…R35-A 全部测量与排除按令转移，但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨二十七段）**：`git diff 89f0455..514b3ff -- src/` **完全为空**。生产调用方三点位经全库 grep 普查复验（`applyChildThreeLine` 恰 {child-tracking(def), coordinator:444, flowchart-run:320}；`applyTrackingGate`/`nextTrackingSeq` 恰 {gate-apply(def), child-tracking, supervisor:483/:487}；`runTrackingTurn` 恰 {turn(def), from-child, tracking/index(re-export)}——与 R31-A…R35-A 期集合逐文件一致），无新增调用方；切片与三调用文件 `.bind(` 计数 0，coordinator/flowchart-run 各恰 1 站点每调用新铸箭头，supervisor 两 settle 站点均新铸箭头。

### 1.1 锚点矩阵（本 VM 重锚，三次官方运行，E=41，~5 gate/run，含 R14-A 第五格 max-codes；引擎 Node v22.22.2 L0 见证在案）

```text
                                   run1      run2      run3
PASSED x no-contract               15.2      15.2      17.4  µs/gate (+1 event)
PASSED x contract(3)               13.9      13.8      14.5  µs/gate (+1 event)
FAILED x no-contract               15.6      16.2      17.6  µs/gate (+3 events)
FAILED x contract(3)               14.9      15.2      17.2  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.9      15.1      16.9  µs/gate (+3 events)
=> 13.8–17.6 µs/gate ⇒ ~69–88 µs/run（稳态热；run3 五格整体较 run1/2 均匀
   偏热 +1.5~+2.2 µs，同向全格平移 = 宿主偏置窗，非切片行为变化，无悬崖）
```

与 R35-A（12.6–19.0 ⇒ 63–95；剔孤点 12.6–17.1 ⇒ 63–86）/ R34-A（12.8–15.2 ⇒ 64–76）/ R33-A（11.8–17.6 ⇒ 59–88）带交叠；上沿 17.6 恰与 R33-A 上沿重合，全带落在历史包络内——三 run 全格对照证 run3 偏热为宿主抖动窗而非切片行为变化（0 行冻结三验在案；按派单令「宿主偏置窗 + freeze 0 行不构成新发现」）。**诚实注记（仪器）**：(i) 本轮驱动为血统脚本的结构重建（附录 A），锚点与分解计时采用 **median-of-blocks 采样**（锚点每格 12 块 × 250 次取中位块；组件 7 块取中位），与 R34-A/R35-A 重建版同口径；血统脚本未存档于仓库，重建间的形式差异为测量仪器现象、非切片行为变化。(ii) R33-A 记录的三层保序在本轮同样未复现（R34-A/R35-A 已同注）：PASSED×contract(3) 仍为五格最低（13.8–14.5），PASSED×no-contract 高于 FAILED×contract(3) 两格——与 R11-A 起在案、R30-A 已定价为 µs 拒收类的 **no-contract 格 PIC/基准次序现象族**一致（warm-up 混序使调用位点 IC 从头多态），不改变任何结构结论，不铸新轴。稳态下即使切片零成本，节省上界 ~0.069–0.088 ms/run，仍比战役落地线低**约三个量级**。**支配论证（稳态口径）第三十四次复核成立**；生产冷态口径维持 R17-A 轴 6b 裁决（[2.7, 8.5] ms/run once-per-run，不重测）。

### 1.2 合同地板分解复核（R12-A 方法学，PASSED×contract(3) 生产格，三次官方运行区间；括号内 R35-A 区间对照）

| 组件 | 本轮实测/gate | R35-A | 锁定裁决 |
| --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1934–2252 ns 每次 ⇒ **5.80–6.76 µs** | 5.95–6.11 µs | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁（带交叠） |
| `validateEvent`(TRACKING_ASSESSMENT) 整体 | **3.30–3.92 µs** | 3.12–3.24 µs | S4-G-2 族 schema 屏障；实现在 events.ts（G 切片）；低沿交叠、高沿 run3 宿主同向 |
| `runTrackingTurn` 完整 | **1.72–1.91 µs** | 1.69–1.83 µs | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构（带交叠） |
| `computePrescore` × 2 | 1344–1634 ns 每次 ⇒ **2.69–3.27 µs** | 1.47–1.84 µs | S1-A-2；高带 ~1.8×——重建夹具形 + 宿主（诚实注记见下） |
| `parseTrackingAssessment` 单测 | **520–653 ns** | 519–559 ns | run1/2 与 R35-A 带逐位重合；run3 高沿宿主同向 |
| O(E) 五扫描 + 拷贝（复刻块） | **569–685 ns**（⇒ 13.9–16.7 ns/event @E=41） | 203–222 ns | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界；复刻块结构差异（诚实注记见下） |
| `applyTrackingGate` 整体隔离 | **6.92–7.56 µs** | 5.84–6.03 µs | 高带 ~15–25% 宿主同向；结构同构 |

**诚实注记**：hash×3 / parse / turn 三大件与 R35-A 带逐位或交叠；validateEvent / applyTrackingGate 高沿由 run3 宿主偏置窗抬升（run3 全组件同向 +10~+17%，与 §1.1 锚点全格平移同窗）。两处口径差需诚实声明：(a) **prescore×2 高带 ~1.8×**（1344–1634 vs 735–919 ns 每次）——本轮分解夹具为 contract(3) 观测的完整 prescoreInput（constraint-retention 走 3 约束 `every×includes` + retained-ids 表），叠加宿主窗；prescore 是诊断口径组件，占整门 <22%，整门锚点带与历史带交叠，组件占比次序改变为仪器/夹具现象。(b) **五扫描+拷贝复刻块 ~3×**（569–685 vs 203–222 ns）——本轮复刻块把 episodeIdFromEvents 反向扫描、模板键构造、双 find miss、nextTrackingSeq 与 `[...events]` 拷贝合在单闭包逐迭代全做；血统复刻块形式未存档，形式差异为复刻仪器现象（真实路径本体由 applyTrackingGate 整体隔离口径覆盖，6.92–7.56 µs 与锚点格自洽）。组件占比次序（hash×3 > validate > prescore×2 > turn > parse > scans）三 run 严格成立；prescore×2 与 turn 的次序相对 R34-A/R35-A 出现夹具性对调（本轮 prescore×2 恒大于 turn），同为仪器现象，不改结构结论。

**R18-A skip / R19-A replay 定价抽测复现**（三次官方运行区间）：guard-3 skip（FAILED 零 evidence refs）**1250–1402 ns = 8.1–8.5% of F×c3 gate**（R35-A 1161–1177 = 7.7–7.9%；带毗邻）；replay find#1（GT idempotencyKey 命中，throwing generator 实证重放臂零 id 铸造）**2604–2866 ns = 16.7–17.4% of F×c3 gate**（R35-A 2375–2409 = 15.8–16.1%；带毗邻交叠）——组成天花板 {apply, skip, replay} 继续成立（次序半边由 R30-A 19d 收口，本轮不重测）。

## 2. 哨兵卫生复跑（全部不计时；按令仅卫生，不铸面、不重走审计）

按 round-36 PLAN 禁令，本轮**不**重走 R32-B 闭合稳定性审计、**不**重做 R33-A 的 44 行账本三腿分类（该记录仍立，本轮三条携带前提均在案：K 腿由三验 0 diff 与最强冻结携带，M 腿由 §1.1 重锚携带——整片上界 ~69–88 µs/run，无一收益池变大，E 腿由 L0 见证携带）。仅以卫生身份复跑在案重开条件哨兵：

| 重开条件 | 哨兵 | 裁决 |
| --- | --- | --- |
| R31-A 停靠门 (a) 生产获得第二可调用物坐标 | 静态普查：coordinator:444 / flowchart-run:320 各恰 1 站点每调用新铸箭头（`() => createEventId(...)`）；supervisor 恰 2 个 settle 站点（:601/:711）均新铸箭头且不带 trackingAssessment（O(1) no-op 第二十七次复验）；切片 + 三调用文件 `.bind(` 计数 0，零包装工厂 | **未触发** |
| R31-A 停靠门 (b) readers 获得生产流量 | F11a-lite 语义证人 ×3：from-child 生产 turn 调用零 readers、humanInput ≡ `{}`（readersInvoked 双 false + human.kind === "unobserved" 断言全过） | **未触发** |
| R31-A 停靠门 (c) 调用位点 megamorphic 化 | 全 src/ 引用文件集普查：三公开入口引用集合与 R31-A…R35-A 期集合逐文件一致（§1 前提 2） | **未触发** |
| C 轴第一优先重开（R15-A ~2.5 量级） | F12c ×3：生产 requiredChecks 形未变（tester + 1 acceptance ⇒ `["chk_0"]` 长度 1；tester + 0 acceptance 回退臂 `["test"]` 仍零流量见证；contract(3) ⇒ 3 条 mandatory "constraint"） | **未触发**；可操作重开第一优先地位不变 |
| X0-7 阈值冻结 | F12d ×3：DEFAULT_TRACKING_CONFIG 四元组 (0.55/0.3/0.54/0.03, version 1) 可执行见证 | **冻结在位** |
| 签名/调用图类条件（S8-A-1/S11-A-2 族、S6-H-2 型热环、S2-A-1 截断态） | `git diff 89f0455..HEAD -- src/` = 0 行 | **全体判空** |
| 引擎代际（R33-A E 腿显式留白） | L0 ×3：Node v22.22.2 / V8 12.4.254.21-node.39 与 canonical 位级相同 | **未触发**（E 腿裁决继续携带） |

### 2.1 姊妹面划界计数（声明即止，零剂量）

- **BINDX（R31-C）**：切片环境内建 `Math.*` token 恰 **6 个于 5 行**（turn.ts:64、prescore.ts:66/:72、combined-score.ts:24 ×2、gate-apply.ts:232）——与 R34-A/R35-A 逐点一致，同 BINDX 坐标，零触碰零 patch。
- **R31-E I/O text-codec 面**：切片 `node:fs|readFile|writeFile` 命中 **0**——面在 A 结构性空。
- **REPRX（R30-C）**：切片 `JSON.parse` 输入位点 **0**（parseTrackingAssessment 收内存对象非字节流）——面空。
- **R32-J 六残余坐标**：本切片数据路径零磁盘 I/O、零持久工件——六坐标在 A 无物理对象，声明即止，不移植不重铸。

## 3. 候选总表

**空。** 本轮枚举的全部角度（§0 新站点级换名记录 3 项 + R12-A…R35-A 114 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、姊妹归档面或停靠门成员；哨兵卫生按定义不产出候选（不计时、不剂量）。空枚举与 R12-A…R35-A 同为战役允许的诚实结局。

## 4. 结构位置 × 消除机制满矩阵扫描（第三十六遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或本轮 §0 换名检查；按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `gate-apply.ts` | mapGateDirective / currentGateStatus 双调用位点复核为互斥路径非双算（早退臂 vs 主臂，R34-A 裁决维持）；`[...input.events]` 拷贝预分配角度 = R1-A 不可变拷贝下界本体；RUN_BLOCKED 防御拷贝不对称 = R33-A 记录 2 维持；wait_user 腿外向回调（含 `"evt_".length`）= R32-A 记录 1 维持；nextTrackingSeq `Math.max` = BINDX 坐标；replay find#1 2604–2866 ns 复现（throwing generator 实证零 id 铸造 + events 引用身份保持）；O(E) 清单 / S11-A-2 / S8-A-1 / S2-A-4 维持 | 无新候选 |
| `child-tracking.ts` | `skipped` 每调用新铸 = R34-A 记录 1 维持；`["test"]` 回退享元 = R33-A 记录 1 维持；条件 spread 后置化 = R33-A 记录 3 维持；constraints map 逐 gate 新铸对象享元化 = S1-A-7/S7-B-5 身份家族；S4-A-2 caller 侧预检维持；锚点/分解宿主复测同带（五格 13.8–17.6 µs/gate）；guard-3 skip 1250–1402 ns 复现 | 无新候选 |
| `turn.ts` | `readersInvoked` 常量初值享元 = R34-A 记录 3 维持；readers 零流量证人宿主（门 (b) 未触发）；anomalyCodes 拷贝 = S1-A-7 本体；uniqueCodes 中间拼接 = S7-A-2 族；mergeOpenMinors `Math.max` = BINDX、其 `previous.map` 中间对数组并入 S3-A-4 同函数裁决；prescoreInput spread 克隆 = S3-A-2 生产不可达维持；failClosed 分支 gate 重铸 = S2-A-1 零流量同伞；S4-A-1 死 openMinors 覆盖维持；claims.some 双算 = S3-A-3；runTrackingTurn 1.72–1.91 µs；R13-A 八子组件 / R12-A §0-11 维持 | 无新候选 |
| `types.ts` | **parse 路径每调用回调闭包提升（asStringArray 谓词 + parseGateDecision 双映射闭包）= §0 换名记录 1 拒列**；`{ kind: "unobserved" }` 五站点享元 = R35-A 记录 1 维持（不重提）；`asString` 首字符卫 = R35-A 记录 2 维持（不重提）；hash×3 地板 5.80–6.76 µs（带交叠）；parse 520–653 ns（run1/2 与 R35-A 逐位重合）；`localeCompare` 排序 = R26-A collation 本体；X1-1/S8-G-2 memo 禁维持；X0-5 asRecord/asArray 合并禁维持；枚举表 includes / evidenceWeight find = S1-A-8；hashAssessment dimensions 冗余 spread = S2-A-5 本体；hashSummary 多排序 = R20-A 零流量伞 | 无新候选 |
| `from-child.ts` | **prescoreInputFromObservation artifactIds 防御拷贝 = §0 换名记录 2 拒列（R34-A 记录 2 同函数第二站点）**；`completedChecks` PASSED 臂防御拷贝 = R34-A 记录 2 维持；FAIL 守卫合取重排（failRefs.length 前置早退）= S6-A-2 本体；F12c 基数形哨兵宿主；evidenceRefsOf 双算 = S2-A-2 本体；第二次 prescore = S1-A-2；window contextFacts 模板字面量 = R21-A 面成员；S3-A-3 / S5-A-3 / S6-A-3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `prescore.ts` | dip 乘后判 = R35-A 记录 3 维持（不重提）；C 轴哨兵对象（`every×includes` 两站点；重开未触发，第一优先地位不变）；dimension() 条件 spread = R28-A key-set geometry 面；`void actorSelfScore/independentEvidence` 死读 = S8-A-3 族规格意图文档；S1-A-3 / S1-A-4/X4-7 / S6-A-1 / S8-A-3 维持 | 无新候选 |
| `combined-score.ts` | `Math.min/max` = BINDX 坐标；S6-A-1 / S7-A-4 / R13-A §0-1 维持 | 无新候选 |
| `gates.ts` | 分支面维持（重排 = R31-A §0-5 族）；hardCodes 数组恒分配 = S7-A-3 实测负优化本体；X0-7 哨兵 F12d 见证；S1-A-6 / S8-A-3 维持 | 无新候选 |
| `roller.ts` | **keptIds Set 构造中间映射数组消除 = §0 换名记录 3 拒列（S7-A-2 家族扩站 + S2-A-1 支配）**；keptIds 恒等过滤 / 无截断态 = S2-A-1 本体；uniqueStrings 中间拼接 = S7-A-2；confirmedDecisions includes 过滤 = S1-A-5；unresolvedQuestions filter×includes = S1-A-4 族；mandatory 死 text 字段 = S3-A-1；prevSummaryHash 链深 = R20-A 本体 + from-child 零流量（previous ≡ undefined）；S9-A-1 维持 | 无新候选 |
| `human-score.ts` | 四条 unobserved 早退臂字面量 = R35-A 记录 1 维持（不重提）；extractTenPoint 每调用 RegExp 铸造 = X0-6 本体 + 生产零流量（humanInput ≡ `{}` F11a-lite 见证）；`userText?.trim()` 分配 = 零流量同伞；extractRatio Set/循环 = 单遍已收口；S1-A-3 / S7-A-1 / R14-A §0-2 / R21-A 维持 | 无新候选 |
| `config.ts` | 生产恒 DEFAULT（F12d 可执行见证宿主）；X0-7 维持；trackingConfig ?? 链 = 一次性配置亚噪声 | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方）；sanitize-then-project = S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R29-A F9c(iv) 契约角维持；index.ts 纯再导出零逻辑（runTrackingTurn 再导出行普查在案） | 无新候选 |

**对 R33-A 声明的裁决**：「44/44 行至少一腿成立，无一裁决重开」经本轮三条携带前提独立复核继续成立（0 行冻结三验、L0 引擎同一、天花板不增）；本轮不做重复审计，产出全部为卫生归档，无可落地候选。收口的预言力第三十六遍成立。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核 = 恰本文件）。仿真为临时脚本未入库（无赢家不落仿真文件；本轮驱动为血统结构的重建版，与 R31-A…R35-A 附录脚本非逐行同源——血统脚本未存档于仓库——结构七段（warm-up/夹具/五格锚点/合同地板分解/skip+replay 抽测/语义证人/报表）逐段对齐，采样器与复刻块差异及影响见 §1.1/§1.2 诚实注记；驱动 sha256 见附录 B，临时文件已删除），未触碰任何测试。

## 6. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R35-A 同套件同计数；Node v22.22.2）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）在派发 tip `514b3ff` 工作树全绿（test 计数 1169 / pass 1168 / fail 0 / skipped 1，与 R31-A…R35-A 逐字相同；退出码 0）。

仿真（临时脚本；seed 参数化可复现，三次官方运行摘要；每 run 21 项 check——L0 1 + F12d 1 + 夹具 3 + F12c 3 + F11a-lite 2 + 五格语义 5 + 2-code 身份 1 + skip 2 + replay 2 + CAS 往返 1——三 run 共 63 项全过）：

```text
run 1 (seed 0x36a001):
anchor: 15.2 / 13.9 / 15.6 / 14.9 / 14.9 µs/gate => ~70-78 µs/run
decomposition: hash×3=5803ns (1934ns each) | parse=521ns | validateEvent=3386ns
    | prescore×2=2688ns | turn=1911ns | 5 scans+copy=569ns (13.87 ns/event)
    | applyTrackingGate=6.92µs
spot: skip guard-3 1250ns (8.4%) | replay find#1 2604ns (17.4%)
ALL 21 CHECKS PASSED (deterministic, seeded)

run 2 (seed 0x36a002):
anchor: 15.2 / 13.8 / 16.2 / 15.2 / 15.1 µs/gate => ~69-81 µs/run
decomposition: hash×3=6040ns (2013ns each) | parse=520ns | validateEvent=3298ns
    | prescore×2=2924ns | turn=1718ns | 5 scans+copy=629ns (15.34 ns/event)
    | applyTrackingGate=6.93µs
spot: skip guard-3 1293ns (8.5%) | replay find#1 2608ns (17.2%)
ALL 21 CHECKS PASSED (deterministic, seeded)

run 3 (seed 0x36a003):
anchor: 17.4 / 14.5 / 17.6 / 17.2 / 16.9 µs/gate => ~73-88 µs/run
decomposition: hash×3=6755ns (2252ns each) | parse=653ns | validateEvent=3922ns
    | prescore×2=3268ns | turn=1869ns | 5 scans+copy=685ns (16.72 ns/event)
    | applyTrackingGate=7.56µs
spot: skip guard-3 1402ns (8.1%) | replay find#1 2866ns (16.7%)
ALL 21 CHECKS PASSED (deterministic, seeded)
```

三次官方仿真语义证人行逐位一致；锚点五格三次同构、无悬崖（run3 全格宿主同向偏热注记见 §1.1）。空枚举裁决成立。

## 7. 新增排除 ID

**无**（空枚举，不铸 S36-A-\*、不铸 face 名、不取绰号、EXCLUSIONS.md 零增行）。换名检查 3 项新站点级记录见 §0（1–3），归档备后续轮次引用，无需并入 EXCLUSIONS.md。

**可操作重开第一优先不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）——本轮 F12c 见证重开条件仍未触发。

## 附录 A：本轮驱动说明（重建版）

血统驱动（R31-A 附录 A + R32-A F12 块 + R33-A L0 增量 + R34-A/R35-A 重建版）为历轮临时文件，未存档于仓库；本轮驱动 `/tmp/r36a-sim.mts` 按其报告记载的结构逐段重建：

1. seeded warm-up（mulberry32，异或常量 `0x77a5`→`0x77a6`，默认 seed `0x36a001`；3000 次五格 + 两抽测混序调用 + 每格 200 次不计时稳态预跑）；
2. E=41 事件表夹具（RUN_STARTED + RUN_ATTACHED + 4 个真实先行 PASSED 门（seq 0..3）+ 35 个 AGENT_EVENT 同形填充；`nextTrackingSeq === 4` 可执行见证）；replay 表 = 38 事件基座 + 1 个真实 FAILED×contract(3) 门（+3 = 41，GT idempotencyKey `hash:4`）；
3. 五格锚点（PASSED/FAILED × contract 有无 + 2-code max-codes 格；每格语义断言先行：+1/+3 事件、directive、runStatus、codes 身份）；**采样器为 median-of-blocks**（每格 12 块 × 250 次取中位块；与血统单块口径的差异及理由见 §1.1 诚实注记）；
4. 合同地板分解七组件（hash / parse / validateEvent / prescore / turn / 五扫描+拷贝复刻块 / applyTrackingGate 整体；各 7 块取中位；复刻块结构差异注记见 §1.2）；
5. skip guard-3 与 replay find#1 抽测（双 throwing-generator 实证零 id 铸造 + events 引用身份保持断言）；
6. 不计时语义证人：L0 引擎同一性、F12d 四元组、F12c 三形、F11a-lite readers 零流量 + humanInput ≡ `{}`、CAS 哈希往返。

与血统脚本的已知口径差异及影响已在 §1.1（采样器 + 层序 + run3 宿主窗）/ §1.2（prescore 夹具形 + 复刻块结构）诚实注记，均为仪器现象，不改变结构结论。

## 附录 B：驱动指纹与运行方式

- 驱动：`/tmp/r36a-sim.mts`，403 行，sha256 `771f4200ae31d70c90df5c766bd7ea31a8ad9351a7a71ee52efcf201924c1383`（未入库；临时文件已删除）。
- 运行（repo 根，nvm Node v22.22.2 + tsx；PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开系统 Node v22.14.0）：

```bash
npx tsx /tmp/r36a-sim.mts --seed 36a001   # run 1
npx tsx /tmp/r36a-sim.mts --seed 36a002   # run 2
npx tsx /tmp/r36a-sim.mts --seed 36a003   # run 3
```

MORE_OPTIMA=no
BRANCH=cursor/r36-a-tracking-thirty-sixth-pass-83a1
