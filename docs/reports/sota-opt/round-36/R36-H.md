MODEL_SLUG=claude-fable-5-thinking-xhigh

# R36-H：`src/evaluation` + `src/requirement` + `src/review` + `src/rubric` 切片第三十六遍复查报告（Round 36）

- **战役:** 全库持久 SOTA 优化 Round 36 / R36-H（R1-H…R35-H 同切片三十五遍之后的第三十六遍）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `6891c74`（派单 tip，R36-E 空枚举摄取 + 本 R36-H 派单）。全部锚点与 census 测量在 `6891c74` 工作树完成；测量后 fetch 检测到独占分支**一次纯 docs 快进** `6891c74` → `5adb0f0`（R36-F 摄取（空枚举）+ R36-I 派单 + PLAN/PROGRESS 更新——`git diff 6891c74..5adb0f0 --stat` 恰 3 个 docs 文件（PROGRESS / PLAN / R36-F.md，154 插入 16 删除），`-- src/` **0 行**），已按令 `git merge --ff-only` 纯快进对齐并重验冻结（0 行）；两 tip 间 `src/ test/ package.json pnpm-lock.yaml` 逐字节相同 ⇒ 测量结果原样携带，无需重测；聚焦套件与 `pnpm gate` 在 `5adb0f0` 树完成。**R36-F 已全文实读并折入非移植清单（§0.3）**；R36-G / R36-I 在飞，按令未等待、未触碰姊妹分支。派单要求预读的 **R36-A / R36-B / R36-C / R36-D / R36-E** 五份姊妹报告（五份均诚实空枚举）已全文通读并折入非移植清单（§0.3）。
- **分支:** `cursor/r36-h-eval-thirty-sixth-pass-83a1`
- **切片:** 21 文件（evaluation 8 + requirement 7 + review 4 + rubric 2），`wc -l` 合计 **1750** = 655 + 712 + 309 + 74（逐目录实测，与 R32-H §0.1 勘误后数字、R33-H/R34-H/R35-H §0.1 逐一相同）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R24-H…R35-H 同引擎位级相同，带可比；本轮 `which node` + `node --version` + `node -e "process.versions.v8"` 计时前三重直证）；tsx 4.23.12（仓内 node_modules）、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成、`package.json` / `pnpm-lock.yaml` 对 `fd437a9` 复核 **0 行**。本 VM 系统默认 node 为 v22.14.0（`/exec-daemon` PATH 前缀遮蔽 nvm——R33-F…R36-F 在案陷阱）——按令**开工即**显式钉 PATH 到 `~/.nvm/versions/node/v22.22.2/bin`，每个官方批次前在同 shell 复核 v22.22.2（anchor 记录批 / 特征化批 / census 批 / 聚焦套件批全部带内自报），**零批次落在 v22.14.0 上**，无弃置批。

## 结论

**诚实的空枚举：第三十六组没有可立 ID 的新角度，不铸任何 S36-H-\* ID，不铸新 face 名，不取 ALLCAPS 绰号，不写入 EXCLUSIONS 表行，生产代码零改动（R12-H…R35-H 二十四连空之后的第二十五连空），切片维持关闭（MORE_OPTIMA=no）。** 本轮按派单纪律收口 H 区：从零重读 21 个切片文件与全部 9 个生产消费文件（§0.2）、在本 VM 重建全部锚点（R30-H 附录 A anchor 驱动逐行复刻 ×3 记录轮 + 3 次特征化运行，R31-H 附录 A census 驱动逐行复刻 ×3，§1）、对全部执行站点做逐格属主归属（§2.1）。哨兵检查仅作卫生应用（静态划界哨兵 + 在案重开条件逐条核对，§2.4），**不**铸「再查一遍重开条件」式新面；R32-A 哨兵方法 / R32-B 闭合稳定性纪律同法条引用不重铸。结果是**零未点名站点、零新站点级换名记录**——从零重推出的候选角度与 R35-H §2.2 八项重赛角度**逐一重合**（按令不重铸，§2.2：每项均复核为既有排除行本体 / plan-ban 属地 / 无号关闭探针 / 硬不变量面的复命中），姊妹轮 R36-A…R36-F 六份空枚举的记录家族在 H 侧的镜像面经本轮 rg 直证全部落在既有行上（§2.2 划界段）；无一站点达到「从未在任何 H 报告点名」的新站点门槛。锚点全数落在历史带内或 ns 级两侧擦边（本 VM 为 R30-H「偏暖窗」邻域主机 + B 腿低侧窗并存——冻结 0 行按令不构成发现，§1 判读）。空枚举与 R12-H…R35-H、R36-A…R36-F 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 21 文件自 `fd437a9` 逐字节零变化（派单 tip `6891c74`、测量后、快进对齐 `5adb0f0` 后、提交前核对均 **0 行**，四点覆盖全程；`git log -1 -- <切片>` 显示最后一次触碰切片文件的提交仍为战役前特性提交 `80a5780`（M3-T6，`fd437a9` 的祖先）——与「自 `fd437a9` 零变化」相容且为其更强形式）；最强冻结 `git diff 89f0455..HEAD -- src/` = **0 行**（R10 以来 docs-only 不变；快进段 `6891c74..5adb0f0` 亦纯 docs）。锚点复测（记录轮 ×3 + 特征化 ×3，§1）：默认态链 **9781 / 9262 / 9139 ns**（run3 落 R29-H 带 8889–9147 内；run2 与特征化 9439 / 9293 / 9252 四采样落 R30-H 带 9178–9647 内；run1 9781 高出 R30-H 带顶 +1.4%、仍落三十五轮跨 VM 包络 3.9–10.4 µs 内——本 VM 窗口较 R35-H「R29-H 邻域中位窗」整体偏暖至 R30-H 邻域，六采样 1+4+1 分布，切片零 diff 为窗口性非结构迁移的最强证据）；配置 A **8245 / 8951 / 8272 ns**（特征化 8631 / 8467 / 8141——**六采样全落** R25-H 带 7.55–9.26 µs 内，A 腿全带为 R31-H/R33-H/R34-H/R35-H 后第五次）；配置 B **7900 / 6011 / 6127 ns**（特征化 7582 / 6330 / 7270——7900/7582/7270 落 R28-H 带 6393–8312 内，6011/6127/6330 低于带底 6.0%/4.2%/1.0%，R34-H 特征化三次低于带底同向先例；同批高低两侧并存 = 窗口采样特征）；promotion **19.5 / 19.5 / 18.5 ns**（特征化 22.2 / 19.7 / 19.0——五采样落 R30-H 观测域 19.0–22.2 内（22.2 恰在域顶），18.5 低 2.6%——R35-H 17.8/17.9 更低同向先例，ns 级低侧窗口项第六次注记）；四个零生产调用方命令类 ≤2.11 µs 域内（pairwise 六采样 2003–2092 全域内；evaluate 1085–1157 与 R33-H/R34-H/R35-H 观测域两侧亚 1% 擦边；reconcile 60–71 落 R35-H 扩展域 61–71 邻接）；critique 类 573–614 ns（573/583 落 R33-H 域 569–596 内，599–614 高沿 +0.5–3.0%（绝对 ≤ +18 ns 的 ns 级单侧擦边））；冷首调 dist 中位 **1711–1900 µs**（五采样落 R6-H…R35-H 1.6–1.8 ms 带内，run3 1900 高出带顶 5.6%——once-per-process 否决类归属不变）。census 驱动 **25/25 ×3 全过**（D3 天花板价 mint 41.4–43.9 ns / normalize 211–213 ns，§1——清零上界 ~0.00026 ms/run，距 10 ms 落地线 4.6 个数量级，D3 门维持关闭）。时钟普查恰一命中 `src/rubric/types.ts:35`（R29-E 属地零触碰）。生产调用面 rg 复核与 R3-H…R35-H 恒定（9 文件 7 值级 + 2 纯类型，§0.2）。切片零 fs/codec/进程边界位点；切片 `Math.*` 恰 1 处（critic.ts:43 `Math.max` = BINDX 同坐标非新轴）。S5-H-1 前置守卫逐字在位、三元组两驱动九轮全过。anchor 驱动 12/12 ×6、census 25/25 ×3 全绿。聚焦切片套件 **76/76**；`pnpm gate` **全绿**（tests 1169 / pass 1168 / fail 0 / skipped 1）。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。仓库变更仅本报告一个文件。不开 PR。

## 0. 冻结、快进处置、生产调用面与划界

### 0.1 冻结与快进

- 分支按令创建于派单 tip `6891c74`。冻结四点核对：派单 tip / 测量与 census 完成后 / 快进对齐 `5adb0f0` 后 / 提交前，`git diff fd437a9 -- src/evaluation src/requirement src/review src/rubric` 均 **0 行**；最强冻结 `git diff 89f0455..HEAD -- src/` 全程 **0 行**。
- 测量后 fetch 检测到**一次纯 docs 快进** `6891c74` → `5adb0f0`（R36-F 摄取 + R36-I 派单：恰 3 个 docs 文件 PROGRESS / PLAN / R36-F.md，`-- src/` **0 行**），`git merge --ff-only` 零冲突对齐并重验冻结。两 tip 间 `src/ test/ package.json pnpm-lock.yaml` 逐字节相同 ⇒ 锚点与 census 结果原样携带；聚焦套件与 `pnpm gate` 在 `5adb0f0` 完成。
- `wc -l` 逐目录实测：evaluation **655**（evaluator 121 / delivery-adapter 97 / diff-adapter 84 / ownership 47 / precedence 28 / check-adapter 147 / adapters 68 / types 63）+ requirement **712**（normalizer 115 / provenance 42 / precedence 59 / extractor 132 / heuristic 220 / coverage 100 / critic 44）+ review **309**（pairwise 141 / self-review 54 / reconcile 47 / critic 67）+ rubric **74**（registry 37 / types 37）= **1750**，与 R32-H…R35-H 同数。
- `package.json` / `pnpm-lock.yaml` 对 `fd437a9` **0 行**；`git log -1 -- <切片>` = `80a5780`（战役前 M3-T6 特性提交，`fd437a9` 祖先）。

### 0.2 生产调用面复核（rg import 搜索，本 tip 实测，第三十六次）

9 文件 7 值级 + 2 纯类型，与 R3-H…R35-H 恒定，import 行逐条核对：值级——`track/clarify.ts`（`extractHeuristicContract`，:4，另 :3/:5 类型）、`track/plan.ts`（`shouldScout`，:5）、`track/loop.ts`（`applyPrecedence`，:38）、`run/supervisor.ts` / `run/flowchart-run.ts` / `run/coordinator.ts`（`assertCoverageAllowsStart`，:27/:21/:31）、`adaptation/promotion-rules.ts`（`assertCanPromoteFromReview`，:4）；纯类型——`track/primary-split.ts`（`HeuristicHabits`，:6）、`adaptation/promotion.ts`（`ReviewerKind`，:10）。13/21 切片文件零生产调用方（evaluation 全部 8 文件 + review/{pairwise,reconcile,critic} + rubric 全部 2 文件）第三十六次成立。9 个消费文件本轮全部从零重读（小文件全文 + 大文件调用位点），调用形态逐字不变：clarify 恒以 `{objective, habits}` 调 extract（S=1 sources 钉死）、loop.ts:80 恒传 `"user-first"`、run 侧三处 gate 调用条件透传 `resolvedQuestionIds` 形态逐字在位（§2.4 D2）。

### 0.3 姊妹面划界与非移植清单（R36-A…R36-E 五份派单折入 + 快进带入 R36-F）

- **R36-A**（tracking 空枚举）：三项站点级换名检查拒列（types parse 路径回调闭包提升 / from-child `artifactIds` 防御拷贝省略 / roller `keptMandatory` 中间映射数组消除），全部 tracking 站点，与 H 零共享物理对象。
- **R36-B**（live 路由空枚举）：零新站点级换名记录；两残余角度均为 R2-B 已点名站点复命中；S12-B-2 腿 (a) 与 S13-B-1 重开条件经其重取证均未触发（属地哨兵，H 不代跑）。
- **R36-C**（离线路由空枚举）：一项换名检查（`routeR1` cheapest-`reduce` 折叠成本携带）= X1-4 具名成本池属地拒列，C 区物理对象；posterior 双 `weightedSampleSize` 独立重瞄确认 R35-C 记录属主不重提。
- **R36-D**（adaptation 空枚举）：四项站点级换名检查拒列（parse/load 腿每调用回调闭包提升镜像 / monitor `freezeBaseline` 的 `new Set(window.map(...))` 中间映射数组消除 / `validateChangeNote` 双 for 循环融合 / `Object.values(axes).filter` 布尔计数物化），全部 adaptation 站点。**其记录家族的 H 侧镜像面本轮 rg 直证划界**（零新记录）：家族 1（parse/load 每调用闭包）在 H **结构性空**——切片 `JSON.parse` 位点 **0** + fs/codec 位点 **0**（§2.4 合并直证），无 parse/load 腿可承载；家族 2（`new Set(x.map(...))` 中间数组）——H 侧恰四站点，全部落既有行：provenance.ts:30（assumptionIds）= **R30-H provenance-resolution pathway plan-ban 属地**、extractor.ts:83（sourceRefs）= **D4 停驻门**机制内部（extract 链 once-per-run µs 内份额）、coverage.ts:52（contractIds）= **S1-H-1 属地** + R29-H incidence topology plan-ban（gate 腿 523–870 ns 整体计价，§1 组件行）、reconcile.ts:39（causalDefects）= 零生产调用方测试-only 面 + S1-H-6 同函数邻域（整调用 60–71 ns，§1）——复命中即止；家族 3（同谓词双遍融合）= **S5-H-1 排除行本体**（precedence.ts:22–23）与 R30-H plan-ban（provenance 三遍）既有行；家族 4（filter 布尔计数 / `Object.values`）——H 侧 `.filter().length` 计数恰 reconcile.ts:25–26 = **S1-H-6 排除行本体**，`Object.values` 恰 1 处 registry.ts:32（listRubrics，零生产调用方命令类）——非计数站点，划界即止。
- **R36-E**（learning 空枚举）：零新站点级换名记录，六项候选全部复命中（R11-E 载入域 / R24-E 已计价站点 / R14-E 逐位理论失败 / R10-E 意图护栏 / S5-E-3 伞 / R12-E/R13-E test-only 伞），全部 learning 站点；其复命中 f（孪生辅助跨文件去重家族）在 H 侧无对应新站点（切片内辅助函数单站点化，无跨文件孪生对——本轮读码直证），复命中 a（parse 腿闭包家族）H 侧结构性空同上。SLICE-CPU 默认带沿用、S8-E-1 三调用位点直证均为 E 属地，H 不代跑。
- **R36-F**（快进折入，全文实读）：experiments 空枚举——零新站点级换名记录（其 §0 裁决清单七组全部命中既有 F 行；BINDX 10 token/8 行第三次复证、R31-F 面格属主表第六次重推无松动均为 F 属地）。与 H **零共享物理对象**（其四个消费文件中 `adaptation/promotion-rules.ts` 同时是 H 的值级消费者——双方零改动，交点为消费者文件自身而非切片物理对象）。已并入非移植清单，不重编号、不移植、不重提。
- **非移植清单全额维持并逐项核对**（派单 DO NOT REMINT 全清单 + round-36/PLAN H 段）：S5-H-1 keep-list（§2.4 逐字直证在位）；R35-H §2.2 八项重赛角度不重铸（§2.2）；R32-H 三项换名记录（evidence 双下标 CSE / `Object.hasOwn` / `DEFAULT_REGISTRY` 惰性拷贝）不重铸；provenance-resolution pathway（R30-H）plan-ban；R31-H 四扇残门 D1–D4 停驻（§2.4 重开条件全未触发）；R32-A 哨兵方法 / R32-B 闭合稳定性审计仅卫生应用不铸面；I/O text-codec topology / per-run transcode-byte manifest（R31-E——切片 fs/codec 位点 0，结构性空）；REPRX（`JSON.parse` 位点 0）；MODEX（chmod/umask/八进制 mode 位点 0，零盘上工件）；FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX 全部未编号未移植；R36-A…R36-F 空枚举（上五条已划界，声明即止）；「不铸『我又查了一遍重开条件 / 闭合稳定性 / 残门 / 无名站点』式新面」遵守——本节全部为既有行复命中裁决，无一铸新。
- 未重开任何 X\* / S1-\* … S35-\* 条目。**单变量纪律**：本轮全部计时臂来自两个历史附录逐行复刻驱动（附录 A 指纹机验），零新计时臂；§2 全部为静态 rg / 读码断言（不计时）。S8-E-1 / S8-I-1 / S9-D-4 / S12-D-1 / S12-B-2 / S13-B-1 / S7-F-1 ≠ S6-F-5 等姊妹区维持项零触碰（属地哨兵由 R36-B/R36-C/R36-D/R36-E/R36-F 各自复核未触发）。

## 1. 锚点全数重测（历史仪器，记录轮 ×3 + 特征化 ×3）

仪器：`/tmp/r36h-anchor.mts`（242 行，R30-H 附录 A 驱动的逐行复刻，仅头注释更新为 R36-H 标注）与 `/tmp/r36h-census.mts`（235 行，R31-H 附录 A census 驱动的逐行复刻，头注释更新为 R36-H 卫生应用标注）；两驱动与归档原文逐行 diff 的全部差异行均在头注释块内（非注释差异行过滤 = **0 行**，零代码行差异）；重抽取附录原文 sha256 与 R30-H/R31-H 归档指纹逐字符相符（`b0251e6f…` 242 行 / `0f69d3ec…` 233 行），本轮指纹见附录 A。`pnpm build` 后 `node --expose-gc --import tsx <driver>`，nvm Node v22.22.2（`which node` + `node --version` + `process.versions.v8` 计时前三重复核；PATH 钉死绕开 `/exec-daemon` 系统 v22.14.0）。

| 锚点 | run1 | run2 | run3 | 历史带对照 |
|---|---|---|---|---|
| 默认态链（extractHeuristicContract + run-start gate(Q=0) + applyPrecedence，once-per-run） | 9781 ns | 9262 ns | 9139 ns | run3 落 R29-H 带 8889–9147 内（近顶）；run2 与特征化 9439 / 9293 / 9252 四采样落 R30-H 带 9178–9647 内；run1 9781 高出 R30-H 带顶 **+1.4%**、仍落三十五轮跨 VM 包络 3.9–10.4 µs 内。六采样 1（R29-H）+4（R30-H）+1（包络内高沿）——本 VM 为 **R30-H「偏暖窗」邻域主机**（对照 R35-H 中位窗 6/6 R29-H 带、R34-H 5/6）；派单默认对照 R35-H 8927/9022/8970 偏暖 +3–9% |
| 配置 A（habits set，Q=1 gated） | 8245 ns | 8951 ns | 8272 ns | R25-H 带 7.55–9.26 µs：**三轮全带内**；特征化 8631 / 8467 / 8141 亦全带内——**六采样全带**，A 腿全带为 R31-H/R33-H/R34-H/R35-H 后第五次 |
| 配置 B（vague，Q=3 gated） | 7900 ns | 6011 ns | 6127 ns | R28-H 带 6393–8312：run1 与特征化 7582 / 7270 三采样带内；run2/run3 与特征化 6330 低于带底 **6.0%/4.2%/1.0%**——R34-H 特征化三次低于带底同向先例；同批带内高值（7900）与带下低值并存 = 窗口采样特征，冻结 0 行不作发现 |
| promotion 类 `assertCanPromoteFromReview`（once per promotion） | 19.5 ns | 19.5 ns | 18.5 ns | R30-H 观测域 19.0–22.2：run1/2 与特征化 22.2 / 19.7 / 19.0 五采样域内（22.2 恰在域顶）；run3 18.5 低 2.6%——R35-H 17.8/17.9 更低同向先例，与 R32-H…R35-H 17.6–22.0 逐位同类 ns 级低侧窗口项（第六次注记，两侧采样并存） |
| evaluate 类 `createEvaluationRecord`(3 criteria)（测试-only 面） | 1145 ns | 1109 ns | 1153 ns | 历轮 ≤2.11 µs 命令类域内；对照 R33-H 1095–1149 / R34-H 1087–1119 / R35-H 1095–1145：特征化 1085 低 R34-H 域底 0.2%、1157 高 R33-H 域顶 0.7%（特征化 1085/1091/1157）——ns 级两侧亚 1% 擦边；零生产调用方归属不变 |
| critique 类 `critiqueContract`（run-start 链上） | 614 ns | 573 ns | 600 ns | R33-H 观测域 569–596 / R35-H 577–596：run2 与特征化 583 域内；599/600/602 高沿 +0.5–1.0%、run1 614 高 3.0%（绝对 +18 ns，特征化 583/599/602）——ns 级单侧擦边与域内采样同批并存，R35-H pairwise +1.9% / R32-H 配置 A +0.8% 越沿先例同判，不改域 |
| pairwise 类 `runBlindPairwisePair`(测试-only 面） | 2090 ns | 2003 ns | 2085 ns | ≤2.11 µs 命令类域：**六采样全域内**（特征化 2037/2078/2092；R35-H run6 2151 越沿本轮未再现） |
| reconcile 类 `reconcileReviews`(n=2)（测试-only 面） | 65 ns | 60 ns | 64 ns | 历域 59–71（R32-H 62–64 / R33-H 62–63 / R34-H 59–63 / R35-H 61–71）：六采样（含特征化 61/71/64）全落 R35-H 扩展域内；特征化 run5 71 与 R35-H run3 71 逐位同值高沿 |
| 冷首调 dist（5 fresh children 中位） | 1711 µs | 1746 µs | 1900 µs | R6-H…R35-H 1.6–1.8 ms 带：五采样带内（特征化 1771/1733/1725）；run3 1900 高出带顶 5.6% 单侧擦边（同批其余五采样带内）；3-module import 3.3–4.3 ms——once-per-process 否决类归属不变 |
| anchor 驱动断言 | 12/12 | 12/12 | 12/12 | 特征化 run4/5/6 亦 12/12（×6 全过） |

组件行（once-per-run 链内份额，全部 ns 级）：extract 腿 8134–8778（默认）/ 7028–7905（A）/ 5251–7075（B）；gate 腿 523–870（三态）；applyPrecedence 腿 143–285（三态）。

census 驱动（语义见证 + 生产组成静态普查 + D3 天花板价，R31-H 附录 A 逐行复刻的卫生应用）：**25/25 ×3 全过**。D3 信任标记机制天花板价：`createTrustedSource`(prod shape) **41.4 / 41.7 / 43.9 ns/铸** × `normalizeSources`([marked] 1 源，含 symbol 读 + 3 信号正则 + slice）**212 / 213 / 211 ns/调用**；生产流量每 run 恰 1 铸 + 1 读不变。对照 R31-H 带 39.7–40.3 / 199–204：mint 三采样高出带顶 +2.7%/+3.5%/+8.9%（绝对 +1.1–3.6 ns 的 ns 级高侧读数窗口——R35-H mint 低侧 39.4/39.6 亚 ns 先例，跨轮两侧采样并存）；normalize 高出 R31-H 带顶 +3.4–4.4%、三采样全落 R32-H 观测域 202–228 内。即使整机制清零，收益上界仍 ~0.00026 ms/run，距 ≥10 ms 落地线 **4.6 个数量级**，D3 门维持关闭，不改带、不重铸。S5-H-1 三元组两驱动九轮全过。

判读：本 VM 时间窗为 **R30-H「偏暖窗」邻域主机**（默认链六采样 1+4+1 分布）叠加 **B 腿低侧窗**（三采样带下 ≤6.0%）与 A 腿全带——同批三腿异向（默认偏暖、B 偏冷、A 居中）正是宿主窗口采样特征而非结构迁移；冻结差分 0 行 + 引擎位级同一 + 仪器逐字节复刻（指纹机验）为最强证据，按派单令「host-bias windows with freeze 0 lines are NOT findings」不作发现、不改带。残余越域读数全部为 ns 级两侧擦边（默认 run1 +1.4% 包络内、critique +18 ns、promotion −0.5 ns、evaluate ±1%、cold run3 +5.6%、census mint +1.1–3.6 ns）——两侧并存同判。**没有任何锚点越过或接近 10 ms 落地线**——整条默认链清零也只有 ~0.0092–0.0098 ms/run。

## 2. 第三十六遍枚举（诚实结果：空；零新站点级换名记录）

### 2.1 逐文件重读与裁决（零未点名站点）

21 文件全文重读（§0.1 行数），每个执行站点归属既有 ID / 无号探针 / 停驻门 / plan-ban 轴：

| 文件（行数） | 站点归属（全数指到既有裁决） | 新候选 |
|---|---|---|
| `evaluation/evaluator.ts`（121） | 零生产调用方命令类（第三十六次确认）；criterion 循环 + `input.evidence[criterion.id]` 双下标 = R32-H §2.2-1 换名记录拒列维持（R24-H 操作数驻留类 + 测试-only 面 + µs 拒收）；overall 三遍 some/every 归约 = 既往轮命令类裁决（R35-H §2.2-5 复命中原文维持）；validateEvaluatorScope 线性扫描 = 同类；EvaluationTarget/IndependenceClass 校验臂 = 行为承载边界校验 | 无 |
| `evaluation/delivery-adapter.ts`（97） | 零生产调用方；公开 `evaluate` 签名 = S7-H-1/X0-4 禁区；证据映射构造 = 命令类 | 无 |
| `evaluation/diff-adapter.ts`（84） | 同上类；isDiffScope 结构卫 + 三态分派 = 命令类；classifyDiffScope 委派 = ownership 属地 | 无 |
| `evaluation/ownership.ts`（47） | 规则表线性扫描 Set 化已既往裁决拒收（零生产调用方 + 规模恒小）；`rule.pattern.test` 为已提升的预编译模式（复命中对照组）；R31-D 死码先例同法条 | 无 |
| `evaluation/precedence.ts`（28） | 小表查找三函数零生产调用方；表驻留 = R24-H 类 | 无 |
| `evaluation/check-adapter.ts`（147） | `changeSetsEqual` = S1-H-9 属地（:119–120 `new Set(a)`/`new Set(b)` 为其本体、非 map 中间数组家族成员——§0.3 划界）；`hashArtifact` 增量化已既往拒收；:101 `Array.isArray`+`every` 结构卫 = 命令类边界校验（行为承载），CKMIX 邻域 | 无 |
| `evaluation/adapters.ts`（68） | 接口/类型 + 工厂；无热执行站点 | 无 |
| `evaluation/types.ts`（63） | 纯类型 | 无 |
| `requirement/normalizer.ts`（115） | `extractSignals` 3 信号正则 = R31-H 归一化属地（census 天花板价 211–213 ns/整调用显式计入）——行内字面量提升候选 = R35-H §2.2-3 复命中，按令不重铸；`createTrustedSource` × symbol 读 = **D3 停驻门**（§1 天花板价复测 41.4–43.9 / 211–213 ns）；`Object.freeze` + spread = D3 机制内部，同门；`buildContractFromSources` 零生产调用方 = R35-H §2.2-8 复命中，按令不重铸 | 无 |
| `requirement/provenance.ts`（42） | `findUnsourcedItems` 全站点 = **R30-H provenance-resolution pathway** plan-ban 属地，零触碰；三遍 filter+map 融合候选 = R35-H §2.2-2 复命中，按令不重铸；:30 assumptionIds `new Set(map)` = 同 plan-ban 属地（§0.3 R36-D 家族 2 划界） | 无 |
| `requirement/precedence.ts`（59） | **S5-H-1 前置守卫 :24 在位必须保留**（本轮 rg 逐字直证 `if (fast.length === 0 \|\| slow.length === 0) return [];`）；:22–23 fast/slow 双 filter = S5-H-1 排除行本体（融合候选 = R35-H §2.2-4 复命中，按令不重铸）；:41 头/尾二值退化 = **D1 停驻门**（切片外规则字面量普查恰 1 处 `"user-first"` @ track/loop.ts:80，§2.4）；:40 `conflict.ids.includes` = 冲突臂生产死平面（D1 见证 covered），守卫之后 = S5-H-1 本体 | 无 |
| `requirement/extractor.ts`（132） | `minimumConfidence` 默认 0.8（:78）+ heuristic 传 0.8（:189）逐字冻结（无阈值改动不变量，§2.4）；:83 sourceRefs `new Set(map)` = D4 机制内部（§0.3 划界）；`assertAuthorityGrounding` 循环 + inference 标注扫描 + 注入 extractor/critic 可调用对 = **D4 停驻门**（生产恒空/零流量 + R31-A injected-callable 停驻地），不重开 | 无 |
| `requirement/heuristic.ts`（220） | `createTrustedSource` 铸点 :177 = D3 门（每 run 恰 1 铸）；isVague / namedTargets / shouldScout / wantsTests 行内正则与 questions 数组构造 = R31-H 归一化属地 + TERMCLASS 正则族 + once-per-run µs 拒收（字面量提升候选 = R35-H §2.2-3 复命中，按令不重铸）；`q-scope`/`q-write` 恒假 `questions.some` 守卫 = R35-H §2.2-7 复命中（R12-H…R24-H 无号关闭探针），按令不重铸；PATH_RE 模块级提升先例（对照组）；回溯担忧不适用（输入受限、锚点未见退化——本轮 extract 腿 5251–8778 ns 全 µs 域） | 无 |
| `requirement/coverage.ts`（100） | `checkCoverageGate` O(C×K) = **S1-H-1 属地** + R29-H incidence topology plan-ban；:52 contractIds `new Set(map)` = 同属地（§0.3 划界）；Set 提升在生产规模已拒收；:27 own-key 合取化简 = R35-H §2.2-6 复命中（**S4-H-1 fail-open 家族** + R32-H §2.2-2 记录维持），按令不重铸；:82 `new Set(resolvedQuestionIds)` = D2 门消费端（§2.4）；失败路径消息装配 = 失败臂-only + gate 腿 523–870 ns once-per-run 整体计价；`assertCoverageAllowsStart` 3 生产调用方不变（§0.2） | 无 |
| `requirement/critic.ts`（44） | `critiqueContract` 中间数组融合已既往拒收（run-start 链 573–614 ns 全量，§1）；:29–30 双 some 探针 = S5-H-1 语义面同族（既往裁决）；:43 `Math.max(0, score)` = **BINDX 同坐标**非新轴（切片唯一 `Math.*`，§2.4） | 无 |
| `review/pairwise.ts`（141） | 零生产调用方（测试-only 面，第三十六次确认）；blind 比较与盲名映射 = 既往命令类；2.00–2.09 µs 天花板复测六采样全域内（§1） | 无 |
| `review/self-review.ts`（54） | `applyRoutingScoreUpdate` 中间对象 = V8 标量替换已既往裁决；`assertCanPromoteFromReview` 自评拒绝分支 = 行为承载（fail-closed）不可动；spread 返回 = 显式字面量既往拒收（R35-A 享元族 H 镜像面属地）；promotion 类 18.5–22.2 ns 复测 | 无 |
| `review/reconcile.ts`（47） | 零生产调用方；n=2 归约 60–71 ns；:25–26 双 filter 计数 = **S1-H-6 排除行本体**（融合候选 = R35-H §2.2-1 复命中，按令不重铸；R36-D 家族 4 的 H 侧镜像站点，§0.3 划界）；:35 dissent filter = 同函数同裁决；:39 causalDefects `new Set(map)` = 同函数测试-only 面（§0.3 划界） | 无 |
| `review/critic.ts`（67） | 零生产调用方；`createCriticObservation` 构造 = 命令类；hasPass some = 同类 | 无 |
| `rubric/registry.ts`（37） | **S1-H-8**（就地写已拒）+ **S2-H-6**（外层 spread 移除已拒）双属地维持；`DEFAULT_REGISTRY` 惰性拷贝 = R32-H §2.2-3 换名记录拒列维持（once-per-process ns）；:32 `Object.values` = listRubrics 零生产调用方命令类（R36-D 家族 4 划界非计数站点，§0.3） | 无 |
| `rubric/types.ts`（37） | `createRubric` 的 `new Date().toISOString()`(:35) = **R29-E 时钟属地**（切片唯一时钟位点，普查恰 1 命中）零触碰 | 无 |

### 2.2 换名拒绝检查：本轮零新站点级记录

从零重推（假装排除表不存在重读 1750 行）枚举出的候选角度与 **R35-H §2.2 的八项重赛角度逐一重合**——派单令明文禁止重铸该八项（`reconcileReviews` 双 filter = S1-H-6 本体；`findUnsourcedItems` 三遍 = R30-H plan-ban；行内正则字面量提升族 = R31-H 归一化属地 + TERMCLASS + census 整调用计价；`detectConflicts` fast/slow 双 filter = S5-H-1 keep-list 本体；`createEvaluationRecord` some/every = 命令类 + µs 拒收；`checkCoverageGate` own-key = S4-H-1 fail-open 家族 + R32-H §2.2-2；q-scope/q-write `questions.some` = R12-H…R24-H 无号关闭探针；`buildContractFromSources` = R10-H 零调用方 + X0-4）。本轮处置：每项在 §2.1 对应文件行内以属主行复核收口（站点原形在位读码直证 + 本轮天花板价引用），**不重列为编号清单项、不铸任何新记录**。R32-H 三项既有换名记录（evidence 双下标 CSE / `Object.hasOwn` / `DEFAULT_REGISTRY` 惰性拷贝）裁决逐字维持，不以任何新名重提。姊妹记录家族的 H 侧镜像面划界（R36-D 四家族 + R36-E 复命中 a/f + R36-A/R36-C 站点）全部落在既有行或结构性空（§0.3 rg 直证）——**零新站点级换名记录**。H 区经三十五遍逐格归属后已无「从未在任何 H 报告点名」的站点，零是更强的收口（R33-H/R34-H/R35-H 同款判读）。

### 2.3 生产调用方普查（第三十六次）

§0.2 全文即普查结果：9 文件 7 值级 + 2 纯类型，与 R3-H…R35-H 恒定；13/21 文件零生产调用方。`assertCoverageAllowsStart`×3 / `extractHeuristicContract`×1 / `applyPrecedence`×1 / `shouldScout`×1 / `assertCanPromoteFromReview`×1 逐位点核对无新增无减少。

### 2.4 静态划界哨兵与在案重开条件核对（卫生应用，不铸面）

- **BINDX**：切片 `Math.*` 恰 **1** 处（critic.ts:43 `Math.max(0, score)`）——与 R31-H…R35-H 计数相同，重开条件（新增内建绑定位点）未触发。
- **R31-E codec / REPRX / MODEX / 进程边界**：`node:fs|readFile|writeFile|Buffer|TextEncoder|TextDecoder|JSON.parse|chmod|umask|spawnSync|process.exit|stdin` 合并一次 rg 于 21 文件 **0 命中**——四类均无物理对象，维持「无对应物」划界。
- **时钟**：`new Date|Date.now|Math.random|randomUUID|hrtime|performance.now` 恰 **1** 处（rubric/types.ts:35，R29-E 属地）。
- **阈值**：`minimumConfidence` 赋值位点恰 2 处（extractor.ts:78 默认 0.8 / heuristic.ts:189 传 0.8），逐字冻结。
- **D1 重开条件**（规则臂获得生产流量 / 三值枚举去退化）：切片外 `src/` 规则字面量普查（排除 precedence.ts 类型联合本体）恰 **1** 处 `"user-first"`（track/loop.ts:80 `applyPrecedence(clarify.candidate.contract, "user-first")`，本轮 rg 直证）——`spec-first`/`latest-first` 生产流量仍为零，precedence.ts:41 头/尾二值退化逐字不变（census D1 见证 9 项全过）。未触发，门维持停驻。
- **D2 重开条件**（resolved-set 组成变化）：track/loop.ts:101/:168 组成位点与 run 侧三消费位点（flowchart-run.ts:765、supervisor.ts:506–507、coordinator.ts:232–233）及门消费端 coverage.ts:82 本轮 rg 直证坐标逐一在位，条件透传形态逐字不变（最强冻结 0 行携带）——盈余外来 id 生产流量仍为零，剂量空间仍归 R29-H R2 坐标。未触发。
- **D3 重开条件**（机制替换收益规模化）：天花板价复测 ~0.00026 ms/run（§1），距落地线 4.6 个数量级。未触发。
- **D4 重开条件**（死平面获得生产流量）：`authority`/`inferences` 生产恒空组成不变（census 25/25 含该见证 ×3）；注入可调用对维持 R31-A 停驻。未触发。
- **S5-H-1**：precedence.ts:24 前置守卫本轮 rg 逐字直证在位，两驱动九轮断言全过。

### 2.5 枚举收口

零未点名站点；零新站点级换名记录（从零重推与 R35-H §2.2 八角度逐一重合、按令不重铸，姊妹家族 H 侧镜像面全落既有行或结构性空，R32-H 三项既有记录维持）；四扇停驻门重开条件全未触发；13 文件零生产调用方第三十六次成立；全部锚点在历史带内或 ns 级两侧擦边（§1 判读——本 VM R30-H 偏暖窗邻域 + B 腿低侧窗，冻结 0 行不作发现）。**没有可立 ID 的新角度。不铸 S36-H-\*，不铸新 face 名，不取绰号，不写 EXCLUSIONS 行。**

## 3. 测试与验证

- 聚焦切片套件（`test/unit/{evaluation,requirement,review}` 13 文件，rubric 套件位于 evaluation/rubric-registry.test.ts）：**tests 76 / suites 12 / pass 76 / fail 0**（`5adb0f0` 树，nvm v22.22.2——快进段 `src/` + test 逐字节相同）。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0**（`5adb0f0` 树）；**tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1**（既有 provider-smoke 凭据跳过），与 R33-A…R35-J / R36-A…R36-F 历史计数逐一相同。临时驱动文件已在 gate 前按纪律删除（附录 A）。
- 硬性不变量逐条复核：双 LCB / 双归因在位；live = R0 等价（R1 未接线）；promotion proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值改动（minimumConfidence 0.8 冻结核对 §2.4）；无测试改动；CAS / 凭证 / 数据面合同 / 公开签名零触碰；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭（S4-H-1 / coverage-gate own-key 家族，§2.1 coverage 行拒列理由维持）；catalog 行序契约（R28-B W3）与 R28-I npm `.bin` symlink 静默 no-op 未触碰；分析不改在途 run；tracking 无指挥权；H/score 不写路由 PASS/FAIL；**S5-H-1 保持**；S8-E-1 / S8-I-1 / S9-D-4 / S12-D-1 / S7-F-1 ≠ S6-F-5 / S13-B-1 / S12-B-2 未重开未代跑（属地在姊妹区，H 侧零触碰）。
- 仓库变更仅本报告一个文件：`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` = 本文件新增。

## 4. 前后对比

无生产代码变更，无前后对比对象。默认预算带维持 R31-H…R35-H 记录（默认链本 VM 复测 9139–9781 ns 记录轮，R30-H 偏暖窗邻域；跨 VM 包络 3.9–10.4 µs 不变）。

## 5. MORE_OPTIMA 判定

- 21 文件零未点名站点；从零重推候选与 R35-H §2.2 八角度逐一重合（按令不重铸）、零新站点级换名记录；停驻门 4/4 重开条件未触发；锚点全数带内或 ns 级两侧擦边（本 VM 偏暖窗 + B 腿低侧窗并存，冻结 0 行）；整默认链清零上界 ~0.0098 ms/run，距 10 ms 落地线仍一个数量级以上，且其组成已逐段归属既有 ID/门。
- 判定：**MORE_OPTIMA=no**。

## 6. 新增排除 ID：无

不铸 S36-H-\*；不新增 EXCLUSIONS 表行；空枚举轴不取绰号；不重铸 R32-A 哨兵方法 / R32-B 闭合稳定性方法 / R31-H 残门审计 / R35-H §2.2 八项重赛角度 / 姊妹空枚举方法。

## 附录 A：仪器指纹与原始输出（未入库；运行后 /tmp 留存核后删除）

两驱动均为历史附录逐行复刻、仅头注释块更新为 R36-H 标注（纪律：不提交仓库；gate 前已删除）。复刻谱系机验：从 R30-H/R31-H 附录原文重抽取的驱动全文 sha256 与两报告归档指纹逐字符相符（`b0251e6f27e76c9ee705e0118c5f54753745d1f2067c9530d26b41ac30dacf0f` 242 行 / `0f69d3ecabacd4ec8b52251aa571544ffeff75203edb8bf8252a39400259bda6` 233 行）；本轮复刻与原文逐行 diff 的全部差异行均在头注释块内（`rg -v '^\s*[<>]\s*\*'` 过滤非注释差异行 = **0 行**），零代码行差异：

- `/tmp/r36h-anchor.mts` — 242 行，R30-H 附录 A anchor 驱动复刻；sha256 `2a31872b422f2a53c473e9a6c87073d879bcf33e4e10a472f00b9f9671c432c9`
- `/tmp/r36h-census.mts` — 235 行，R31-H 附录 A census 驱动复刻（卫生应用标注）；sha256 `a390180bb83f71ac4b656d588438d8f80df6fde4641dbcb6f67ea6a692779020`

anchor 记录轮原始输出（默认链关键行，×3）：

```text
run1: extractHeuristicContract=8778ns + gate(Q=0)=720ns + applyPrecedence=282ns = 9781ns | 12/12
run2: extractHeuristicContract=8216ns + gate(Q=0)=762ns + applyPrecedence=284ns = 9262ns | 12/12
run3: extractHeuristicContract=8134ns + gate(Q=0)=721ns + applyPrecedence=285ns = 9139ns | 12/12
```

特征化 ×3（默认链）：9439 / 9293 / 9252 ns（组件 8420+749+270 / 8253+756+284 / 8251+716+284），12/12 ×3。

census ×3 关键行：

```text
run1: createTrustedSource=41.4ns/mint + normalizeSources=212ns/call | 25/25
run2: createTrustedSource=41.7ns/mint + normalizeSources=213ns/call | 25/25
run3: createTrustedSource=43.9ns/mint + normalizeSources=211ns/call | 25/25
```

MORE_OPTIMA=no
BRANCH=cursor/r36-h-eval-thirty-sixth-pass-83a1
