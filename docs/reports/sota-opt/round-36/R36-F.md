MODEL_SLUG=claude-fable-5-thinking-xhigh

# R36-F：`src/experiments/` 第三十六遍复查报告（S7-F-1 + S7-F-2 之上，全新第三十六组枚举）

- **战役:** 全库持久 SOTA 优化 Round 36 / R36-F（R1-F…R35-F 同区三十五遍之后的第三十六遍）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `a0fd7ed`（R36-C 摄取——空枚举——+ 本 R36-F 派单）。锚点测量与门禁完成后的多次 `git fetch origin cursor/sota-persistent-opt-83a1` 均确认 tip 仍为 `a0fd7ed`；提交前工作期间独占分支**两次纯 docs 快进**：(一) `a0fd7ed` → `f4f804f`（R36-D 摄取——空枚举——+ R36-G 派单 + PLAN/PROGRESS 状态更新：`git diff a0fd7ed..f4f804f --stat` 恰 3 个 docs 文件（PROGRESS / PLAN / R36-D.md），`git diff a0fd7ed..f4f804f -- src/` **0 行**）；(二) `f4f804f` → `6891c74`（R36-E 摄取——空枚举——+ R36-H 派单 + PLAN/PROGRESS 状态更新：`git diff f4f804f..6891c74 --stat` 恰 3 个 docs 文件（PROGRESS / PLAN / R36-E.md），`-- src/` **0 行**）。两次均已按令执行**纯快进 rebase**（本分支无本地提交，`git merge --ff-only` 干净快进）并各重验冻结（切片 vs `519101f` 与最强冻结 `89f0455..HEAD -- src/` 在每个新 tip 均 **0 行**，`wc -l` 仍 2550）；三 tip 间 `src/ test/ scripts/ package` 逐字节相同 ⇒ 测量与门禁结果原样携带，无需重测。已先后通读新姊妹报告 **R36-D**、**R36-E** 全文并折入非移植清单（§0，含记录家族的 F 侧镜像面 rg 直证划界）。派单要求预读的 **R36-A / R36-B / R36-C** 三份姊妹报告亦已全文实读并折入。R36-G / R36-H 在飞，按令未等待、未触碰姊妹分支。全部测量、聚焦套件与 `pnpm gate` 在 `a0fd7ed` 的工作树上完成（与 `f4f804f` / `6891c74` 的 `src/` 逐字节相同）。
- **分支:** `cursor/r36-f-experiments-thirty-sixth-pass-83a1`
- **切片:** 15 文件 `src/experiments/`（全量重读确认，`wc -l` 合计 **2550** 行，与 R29-F…R35-F 普查同数，无缺无余：attribution-report 21 / canary 292 / comparison-report 321 / dataset 206 / evaluation-card 65 / gated-comparison 93 / holdout 140 / isolation 68 / manifest 145 / plan 165 / replay 149 / shadow-compare 44 / shadow 505 / simulation-holdout 272 / threshold-calibration 64）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R24-F…R35-F 同引擎位级相同，带可比；本轮 `node -p process.versions.v8` 直证）；tsx 4.23.12（仓内 node_modules）、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成、`package.json` / `pnpm-lock.yaml` 对 `519101f` 复核 **0 行**。本 VM 系统默认 node 为 v22.14.0（`/exec-daemon` PATH 前缀遮蔽 nvm）——按 R32-D §1(iii) / R33-F…R35-F 在案陷阱**开工即**显式钉 PATH 到 `~/.nvm/versions/node/v22.22.2/bin`，每个官方批次前在同 shell `node --version` 复核 v22.22.2（十个官方批次全部带内自报），**零批次落在 v22.14.0 上**，无弃置批次。

## 结论

**诚实的空枚举：第三十六组没有可立 ID 的新角度，不铸任何 S36-F-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不写 EXCLUSIONS 行，生产代码零改动（R12-F…R35-F 二十四连空之后的第二十五连空）。且本轮**零新站点级换名记录**：从零重推的全部候选角度经对表逐一查证，每一个都命中既有排除 ID 本体、R4-F 在案防御纵深保留域、R32-F/R33-F/R34-F 既有换名记录（十项全部复核维持）、姊妹归档面（禁移植）或停驻门成员——没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标（§0 裁决清单）。本轮第三十六遍从零重读 15 个源文件与全部四个生产消费文件、在本 VM 重建全部锚点（r5f/r6f/r7f 各**三次**独立运行 + r1f 一次，§1）、对 R31-F §2 面格属主表做**第六次**独立重推（无一格松动，BINDX 计数 **10 token / 8 行** 第三次独立复证成立、坐标未动，§2）、并按 R32-A 首创且 plan-ban 重铸的哨兵方法对 **F 自档案**在案重开条件做**不计时静态卫生复核**（§3——与 R33-F/R34-F/R35-F 同款，仅作卫生性应用、不当边际产出申报、不铸「我又查了一遍重开条件」式新面），全部哨兵**未触发**。空枚举与 R12-F…R35-F、R36-A…R36-E 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 15 文件自 `519101f` 逐字节零变化（分支创建时 / 测量与门禁完成后 / 两次纯 docs 快进（`a0fd7ed` → `f4f804f` → `6891c74`）后各一验 / 提交前**五验** `git diff 519101f..HEAD -- src/experiments/` 均 **0 行**，五点覆盖全程；`git log -1 -- src/experiments/` 显示切片最后一次生产改动仍为 `519101f`「perf(experiments): S7-F-1 aligned-prefix restore fast path + S7-F-2 ASCII-head trim guard」）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均为 docs）。锚点复测（各 ×3，§1 诚实注记）：**r5f C2 123.76 / 119.45 / 118.82 ms**；**r6f partD 120.24 / 122.96 / 118.50 ms**；**r7f partE 128.74 / 125.52 / 128.33 ms**（本 VM 为战役窗口的**中段主机**——三档全部落在 R34-F/R35-F 历史包络内，无同向越沿；冻结 0 行按令不构成新发现）；r6f 三次序 + r7f 四次序 restore 省额**三轮全正无符号翻转**（S7-F-1 ≠ S6-F-5 复核成立：同下标哈希相等 + 后缀回退形态在位——shadow.ts L355–L438 读码直证 L383 `episodeHash !== population[index]` 同位比较、首个错位回退 S6-F-1 pending-Set（L407–L434 后缀臂）、下标 0 错位重跑原落地环（L388–L406），非 O(P) 双指针）；r1f 2668 项逐位检查全过（参考侧成员比较 **167,167,000 次**与历轮锚定值逐位相同）。聚焦实验测试 **75/75 绿**；`pnpm gate`（typecheck + lint + test + build）**全绿**——test 1169 / pass 1168 / fail 0 / skipped 1（与 R33-F/R34-F/R35-F/R36-A…R36-E 逐字同计数，exit 0）。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动（X0-11——PAIRX 编排位点 `attribution-report.ts` 本轮零字节未触、本轮零计时装具且无任何脚本 import 两估计器）；无阈值/测试/公开签名/数据面变更；无 fsync/完整性重哈希移除；fail-open 家族保持关闭。仓库变更仅本报告一个文件。不开 PR。

## 0. 冻结、摄取处置与非移植清单（方法第 1、4 步）

- 分支按令创建于派单 tip `a0fd7ed`；提交前先后检测到两次纯 docs 快进 `a0fd7ed` → `f4f804f`（恰 3 docs 文件：PROGRESS / PLAN / R36-D.md；`-- src/` **0 行**）与 `f4f804f` → `6891c74`（恰 3 docs 文件：PROGRESS / PLAN / R36-E.md；`-- src/` **0 行**），均已 `git merge --ff-only` 纯快进对齐。冻结五点核对：分支创建时 / 测量与门禁完成后 / 两次快进后各一验 / 提交前，`git diff 519101f..HEAD -- src/experiments/` 均 **0 行**。
- 派单预读的三份姊妹报告全文实读并划界：**R36-A**（tracking 空枚举——三项站点级换名检查拒列：types parse 路径回调闭包提升 / from-child `artifactIds` 防御拷贝省略 / roller `keptMandatory` 中间映射数组消除，全部 tracking 站点）；**R36-B**（live 路由空枚举——零新站点级换名记录；两残余角度均为 R2-B 已点名站点复命中；S12-B-2 腿 (a) 与 S13-B-1 重开条件经其重取证均未触发）；**R36-C**（离线路由空枚举——一项换名检查为 `routeR1` cheapest-`reduce` 折叠成本携带，X1-4 具名成本池属地拒列，C 区物理对象；posterior `estimateForKey` 双 `weightedSampleSize` 独立重瞄确认 R35-C 换名记录属主，未重提——按派单 F 亦不重提）。三者与 F 切片**零共享物理对象**（R36-C 的 routeR1 站点在 `src/routing/r1.ts`，本切片 shadow-compare.ts 仅消费其公开导出，双方零改动），均为空枚举轴外号，**已并入非移植清单，不重编号、不移植、不重提**。
- **R36-D 快进折入**（全文实读）：adaptation 空枚举——四项站点级换名检查拒列（D 侧 parse/load 腿每调用回调闭包提升镜像 / monitor `freezeBaseline` 四处 `new Set(window.map(...))` 中间映射数组消除 / `validateChangeNote` 双 for 循环融合 / `Object.values(axes).filter` 布尔计数物化），全部 `src/adaptation/` 物理对象，与 F **零共享**；其 §3 哨兵复核（S9-D-4 / S12-D-1 / S12-B-2 腿 (a) / S13-B-1 / R22-D 三腿）均未触发，与本报告 §3 属地划界一致。**四记录家族的 F 侧镜像面本轮 rg 直证划界**：记录 1 家族（parse/load 腿每调用闭包）在 F **结构性空**——切片 `JSON.parse` 位点 0 + fs/codec 位点 0（§0 非移植清单 REPRX / R31-E 行），无 parse/load 腿可承载；记录 4 家族（filter 布尔计数）——切片 `\.filter(...).length` 与 `Object.values` 均 **0 命中**，结构性空；记录 2 家族（`new Set(x.map(...))` 中间数组）——F 侧恰两站点：comparison-report.ts:292 familyBreakdown 查重 = **R34-F 换名记录 3 逐字点名站点**复命中维持，gated-comparison.ts:33 domains 构造 = **S2-F-6 多遍融合本体**遍集合成员（`pairedEvaluationCard` L33–L37 五遍之首）的分配层真子集（once-per-eval µs 下界，复命中即止）；记录 3 家族（同谓词双遍融合）= **S1-F-4** 本 F 区属主行在位（threshold-calibration 三遍并单遍已拒）。四镜像划界全部命中既有行，本报告**零新站点级记录**结论不变；R36-D 空枚举并入非移植清单，不重编号、不移植、不重提。
- **R36-E 快进折入**（全文实读）：learning 空枚举——**零新站点级换名记录**，六项候选角度全部查证为复命中（parseLearnedRoutingPolicy 三个每调用映射回调闭包 = R11-E 载入域真子集 / familyFromRole if 链换表 = R24-E 已计价站点 / diagnostics 双除法代数合并 = R14-E 逐位理论失败原记录 / `kind === "human"` 被吸收析取 = R10-E 意图护栏原记录 / auto-loop L81 eta 缩减 = S5-E-3 伞 / computeFeatureSim–compareSignatures 跨文件去重 = R12-E/R13-E test-only 拒列伞），全部 `src/learning/` 物理对象，与 F **零共享**；其 SLICE-CPU 默认带 14.7–15.2 µs/run 沿用不变、S8-E-1 恰 3 调用位点第三十六次直证、S12-B-2 E 侧入口与 S13-B-1 均未触发——与本报告 §3 属地划界一致。F 侧镜像面划界：其复命中 a（parse 腿每调用闭包家族）在 F **结构性空**（上条 R36-D 折入已 rg 直证——切片零 parse/load 腿）；复命中 f（孪生辅助跨文件去重家族）的 F 侧对应站点 = mean/sampleStandardError 孪生辅助 = **R34-F 换名记录 2 逐字点名站点**复命中维持；复命中 b/c/d/e 各机制家族的 F 侧承载行均为既有 F 属主行（小表 = S1-F-4/X3-2 族、代数合并/CSE = S4-F-3 本体、回调 ns 级 = R15-F 轴 3 引擎面），无新面可移植。R36-E 空枚举并入非移植清单，不重编号、不移植、不重提。
- **非移植清单全额维持并逐项核对**（派单 DO NOT REMINT 全清单 + round-36/PLAN F 段）：R30-F 边界输出消费/快照读回拓扑面、R29-F 输出保留/被替换快照存活面、R28-F 边界调用协议面、R27-F 输入访问路径机制面、R26-F 输入存储状态面、**R31-F 故障通道处置残格**（分解归 R17-F + R30-F + R29-F + 引擎面——本轮不重铸、不新停驻、零计时臂）、**PAIRX**（R29-C——编排位点共驻不当新 F ID）、**BINDX**（R31-C——切片 ambient `Math.*` token 恰 **10 个于 8 行**：comparison-report `Math.abs` ×4（L185/L190/L195/L200）+ `Math.sqrt` ×2（L120）、gated-comparison `Math.sqrt` ×2（L92）、replay `Math.imul` ×2（L69/L70），本轮 `rg -o` 逐 token 直数与 R33-F 订正值/R34-F/R35-F 复证**第三次独立复证成立**；行集合同一、坐标未动；计数即止零 patch 零剂量）、**MODEX**（R31-J——切片 chmod/umask/八进制 mode 字面量 **0**（宽式 `mode:` 仅命中 plan.ts L21/L97 的 `ExperimentMode` 领域字段，非权限位）、零盘上工件，划界声明即止）、I/O text-codec topology（R31-E——`rg "node:fs|readFile|writeFile|Buffer"` 本轮首手复核**零匹配**，公开边界全同步纯内存，面结构性空）、REPRX（R30-C——切片 `JSON.parse` 位点 **0**）、R32-A 重开条件哨兵审计（**方法不重铸为面**——§3 按令仅作卫生性应用于 F 自档案）、R32-B 闭合稳定性审计（**方法不重铸为面**）、**R33-A … R35-J 空枚举**与 **R36-A/R36-B/R36-C/R36-D/R36-E 空枚举**（上三条已划界，声明即止）、R31-A injected-callable 停放门、R31-B 六停驻门、R31-D 的 R22-D 属地格、R31-G 18 门审计、R31-H 四扇边界参数残门、R31-I CLI 进程边界门扉闭合审计（F 是进程内库——切片 node: 引入恰 2：isolation.ts:1 `node:path`、holdout.ts:1 `node:crypto` randomUUID，本轮 rg 直证）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX 全部未编号未移植。S7-F-1 ≠ S6-F-5 维持（§1 次序符号复核 + 结论段读码直证）。S5-F 已落地不重做（plan.ts 单探针 + S7-F-2 首字符卫现场在位，§4）。「不铸『我又查了一遍无名站点』」遵守：本节全部为既有行复命中裁决，无一铸新。
- **换名重提检查**（本轮枚举全部残余角度的逐项裁决，一律不铸 S36-F-\* ID；**零新站点级记录**——每一项均能指到在手报告逐字点名过的行）：
  1. `pairedEvaluationCard` 内 `sampleStandardError` 对 `mean` 的内部重算 = **S2-F-6 本体亚案** + R34-F 换名记录 2（mean/SE 孪生辅助）同族——复命中即止；
  2. `pairedDeltaSummary` `count===0` 死抛分支与 threshold-calibration `best?.f1 ?? -1` 死回退 = **R4-F 在案「pairedDeltaSummary 型死分支（rows[0] 恒存在）」防御纵深保留域**（R4-F §逐文件表 threshold-calibration 行逐字点名）——原型站点复命中即止；
  3. `IMPROVEMENT_CLAIM` / `IMPROVEMENT_PATTERN` 孪生正则 = R33-F 换名记录 3 维持；`validateComparisonReport`+`computeComparisonReport` strip-retry 双调 = S1-F-1 本体；utility delta 双算 = S4-F-3 本体；
  4. replay.ts orderedHashes spread 复制 = R33-F 换名记录 1 维持（零生产调用方 + µs + R15-F 轴 3）；
  5. simulation-holdout 条件 spread 装配链 = R34-F 换名记录 1 维持；familyBreakdown 查重单探针化 = R34-F 换名记录 3 维持；mean/SE 孪生去重 = R34-F 换名记录 2 维持（含 resolvePendingMembership 孪生对同族注记——shadow/canary 两形 X1-5 契约不同不并）；
  6. manifest stableStringify 形态 = R32-F 换名记录 1 维持；HoldoutVault 注入缝默认臂 = R32-F 换名记录 2 维持；shadow 返回值拷贝省略 = R32-F 换名记录 3 维持；shadowDecisionAt 闭式跳步 = R33-F 换名记录 2 维持；requireCanaryBlock 重派生缓存 = R33-F 换名记录 4 维持；
  7. assign 路径 O(P) `population.includes` 与 O(A) 唯一性扫描 = S8-F-1 / S9-F-1 / S2-F-4 / S3-F-1 本体复命中；`recordExperimentOutcome` 双 some + `accumulatedCostUsd` 全扫 = S1-F-8 本体；validate 三站点 scratch 共享 = S9-F-3 本体。
  R32-F 三项 + R33-F 四项 + R34-F 三项既有换名记录本轮**全部复核维持不重提、不新增**；R35-F 零新站点级记录不重提、不重铸。
- 未重开任何 X\* / S1-\* … S35-\* 条目。**单变量纪律**：本轮**无任何计时新臂**——四个锚点脚本均为 `scripts/round0{1,5,6,7}-*.ts` 入库血统资产逐字节原样重跑（零改动零参数）；§2/§3 全部为静态 rg / 读码断言（不计时）。S9-D-4 / S12-D-1 / S8-E-1 / S8-I-1 / S13-B-1 / S12-B-2 等姊妹区维持项零触碰（其哨兵属地在 A/B/C/D/E/I，本轮不代跑——R36-B/R36-C/R36-D/R36-E 已各自复核未触发）。

## 1. 锚点重测（r5f/r6f/r7f 各三次独立运行，r1f 一次；本 VM 实测，未拷贝历轮数字；nvm Node v22.22.2；全部在 tip `a0fd7ed` 工作树完成——两次快进后三 tip 间 `src/ test/ scripts/ package` 逐字节相同，结果原样携带）

| 档位 | 第 1 次 | 第 2 次 | 第 3 次 | R35-F 记载带（派单对照） |
| --- | --- | --- | --- | --- |
| r5f C2 全实验（P=2000/A=1000） | 123.76 ms | 119.45 ms | 118.82 ms | 117.53 / 126.01 / 117.46 ms |
| 2A-validate 组件 | 100.80 ms（81.4%） | 99.54 ms（83.3%） | 98.73 ms（83.1%） | 96.39–105.09 ms（82.0–83.4%） |
| r5f C1 has+add → add+size 省额 | +34.88 ms（23.0%） | +28.85 ms（19.8%） | +29.27 ms（20.2%） | +35.02~+36.41 ms（23.0–23.9%） |
| r6f partD 全实验 | 120.24 ms | 122.96 ms | 118.50 ms | 119.12 / 117.99 / 117.61 ms |
| r7f partE 全实验 | 128.74 ms | 125.52 ms | 128.33 ms | 124.19 / 127.43 / 125.82 ms |
| r7f validate 卫（×2000） | +10.29 ms | +9.50 ms | +9.99 ms | +10.06~+10.86 ms |

**次序符号（committed 回归脚本原生视图，全部为「落地生产形态 vs 冻结参考」省额）**：r7f partE 四次序三轮全正——prefix(aligned) +46.35/+45.12/+45.01、half-aligned +28.76/+26.68/+27.77、scattered +6.18/+5.06/+2.94、reversed +8.02/+6.52/+6.13 ms，无符号翻转；r6f partD 三次序三轮全正——prefix +123.44/+125.25/+123.16、scattered +33.49/+32.96/+30.88、reversed +29.00/+31.22/+28.50 ms。**S7-F-1 ≠ S6-F-5 复核成立**：落地代码零漂移（切片零 diff），同下标哈希相等 + 后缀回退形态（非 O(P) 双指针）在全部次序下三轮同号为正。r5f C4 索引环形式 −2.30/−3.14/−3.13 ms 维持 S5-F-3 抖动带记载（R35-F −2.77/−4.71/−3.72、R34-F −2.70/−2.66/−4.75）；C5 dataset 镜像 14.7–18.0 µs（U=2000）/ 132.9–146.6 µs（U=20000）维持 S5-F-1 记载（R35-F 17.5–17.7 / 136.1–144.7、R34-F 12.2–15.2 / 131.3–138.0——两档均与历带交叠）。r1f 2668 项逐位检查全过（生产侧 62.1 ms / 参考侧 630.8 ms，10.2×；参考侧成员比较 **167,167,000 次**——与历轮锚定值逐位相同；参考侧壁钟非锚点义务，钉死量是比较计数与逐位等价）。

**诚实注记**：(i) 本 VM 为战役窗口的**中段主机**：r5f C2 三读数 118.82–123.76 落在 R34-F 带（119.21–128.96）与 R35-F 带（117.46–126.01）并集内；r6f partD 118.50–122.96 落在 R34-F 带（121.63–123.10）与 R35-F 带（117.61–119.12）之间的历史包络内；r7f partE 125.52–128.74 与 R35-F 高读数（127.43）及 R34-F 带（128.18–130.28）交叠——三档均无同向越沿，与 R35-F「主机偏快」相比回到中带（R36-B/R36-C 同一 VM 世代亦各自注记复位），冻结 0 行 + 引擎位级同一 + 脚本逐字节原样 ⇒ 宿主刻度项非切片行为变化，按派单令「host-bias windows with freeze 0 lines are NOT new findings」不作发现、不改带。(ii) 少数格个体注记：C1 省额比例 19.8–23.0% 回落到 R26-F…R33-F 紧邻带（≤23.5）内（R35-F 曾 23.0–23.9 高沿）；r7f validate 卫 +9.50~+10.29 与 R35-F 带（+10.06~+10.86）低沿交叠、高于 R33-F/R34-F 带（+8.00~+9.37）——省额方向全部不变，不构成任何重开触发（§7）。(iii) 十个官方批次（r5f ×3、r6f ×3、r7f ×3、r1f ×1）全部在同一 shell 会话、PATH 钉死后连续完成，每批前 `node --version` 自报 v22.22.2；无会话重置、无弃置批。

## 2. 调用面复核 + 面格属主表独立重推（元闭合，方法第 3 步前置；卫生性应用不当边际产出）

生产消费图 rg 全仓复核（与 R30-F §1.2 / R31-F §2 / R32-F…R35-F §2 逐条一致）：切片外 `src/` importers 恰四个文件，import 行逐条核对——`adaptation/reflection.ts`（validateExperimentPlan + createShadowRunner + 类型：唯一热交错消费者）、`adaptation/eval-routing.ts`（comparison-report 类型与函数 + gatedComparisonReport + createIsolationGuard + stableStringify + **replayCacheKey**——对 replay.js 的 import 恰此一项，L18 直证，S2-F-2 前提本轮成立）、`adaptation/promotion-rules.ts`（validateComparisonReport 直呼）、`routing/r1-shadow-report.ts`（comparison-report + gated-comparison）。**故障通道处置普查复核**：四消费文件 catch 位点合计 4 处、全部在 eval-routing.ts（L219/L249/L259 包 `JSON.parse`/`readFile`，非切片物），**恰一处**包切片调用——L336 `assertReplayIsolated` 包 `createIsolationGuard`（MESSAGE + WRAP-RETHROW 坐标，once-per-eval 冷通道）；`reflection.ts` / `promotion-rules.ts` / `r1-shadow-report.ts` 零 catch ⇒ 热通道生产坐标仍 = PROPAGATE。切片故障源普查：**142 处** `throw new DomainValidationError`（逐文件计数与 R31-F…R35-F 全同：shadow 27 / plan 22 / dataset 19 / canary 14 / manifest 14 / holdout 11 / simulation-holdout 10 / comparison-report 8 / evaluation-card 7 / isolation 6 / replay 3 / gated-comparison 1 / 其余三文件 0），切片零 `throw new TypeError/Error` 直抛点——故障通道单类维持。

R31-F §2 面格属主表本轮从零**第六次**独立重推（(o) = F 区属主轴；(s) = 姊妹区收口面，明令禁移植；每格属主裁决**无一松动**）：输入存储状态 (o) R26-F / 访问路径 (o) R27-F / 对象表示 (s) REPRX / 引用身份·别名 (s) R29-A / 流组成 (o) R20-F / 计划标头身份几何·计数·成员拓扑 (o) R25-F/R23-F/R22-F / 规模与二阶联角 (o) R14-F 域 + (s) R30-B / 调用协议 (o) R28-F / 排程·实参物化·调用上下文 (s) CALLB/R28-H/R22-H / 调用流次序·分派历史 (s) R30-A / 时钟来源 (s) R29-E / 执行基片·名字解析 (s) R27-H 族 / BINDX / 引擎态 (o) R15-F 轴 3 + R19-F 域 / 输出成功通道保留 (o) R29-F / 输出成功通道消费·读回 (o) R30-F / 输出故障通道制造·单价 (o) R17-F / **输出故障通道调用方处置 = R31-F 分解归属（R17-F + R30-F + R29-F + 引擎面），本轮零计时零重铸维持** / 双估计器共驻 (s) PAIRX（X0-11 禁区）。结构性空直证两行维持：**I/O 编解码拓扑 (s) R31-E——切片 fs/codec 位点 0**；**持久工件权限模式字 (s) R31-J MODEX——切片 chmod/umask/八进制 mode 位点 0、零盘上工件**。**BINDX 划界行第三次独立复证**：`rg -o "Math\.\w+"` 逐 token 直数恰 **10 个于 8 行**（abs 4 + sqrt 4 + imul 2；120 行与 92 行各双 `Math.sqrt` 在列），R33-F 订正持续成立、行集合坐标属主裁决全部不动。第三十六遍枚举在该坐标系内**无剩余未审计平面**。

## 3. F 档案在案重开条件哨兵复核（全部不计时；R32-A 方法卫生性应用，不铸面、不当边际产出）

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| R31-F §7 (a) | 任何 `src/` 生产调用方在热交错内 catch 边界故障并做非平凡处置 | §2 catch 普查：热消费者 reflection.ts 零 catch；唯一切片 catch 仍为 eval-routing L336 once-per-eval ⇒ **未触发** |
| R31-F §7 (b) | 故障对象携带非字符串 own 载荷 | `src/domain/errors.ts` 在最强冻结内（`git diff 89f0455..HEAD -- src/` 0 行）⇒ **未触发** |
| R31-F §7 (c) | 故障通道契约（消息文本/单类不变量）解冻 | 两段快进均纯 docs（`-- src/` 0 行）且无解冻声明；切片零 diff ⇒ **未触发** |
| S6-F-2 重开 | delete+size 形态实测稳定反超 >5% | 无任何反超证据；本轮落地形态三/四次序省额三轮全正（§1）——按纪律**不重计时被拒形态** ⇒ **未触发** |
| S6-F-5 重开 | assignment 次序被契约化为 population 子序列 | 冻结 0 行 + round-36 PLAN 重申 S7-F-1 ≠ S6-F-5 ⇒ **未触发** |
| S5-F-1 / S4-F-1 前提 | dataset/simulation-holdout 链获得生产消费者 | `rg 'experiments/(dataset\|simulation-holdout\|holdout\|shadow-compare\|threshold-calibration\|attribution-report)' src/`（切片外）本轮**零命中**（精确六名交替式）⇒ 六链零生产消费者 ⇒ **未触发** |
| S2-F-2 / S4-F-2 前提 | `replayPolicy` 获得生产调用方 | eval-routing 对 replay.ts 的 import 恰 `replayCacheKey` 一项（L18 直证，§2）⇒ **成立维持** |
| S2-F-1 前提 | shadowDecisionAt 占比脱离 ~1% 域 | 切片零 diff + 锚点全部落在历史包络内 ⇒ **成立维持**（R33-F 换名记录 2 的真尺度拒绝依此维持） |
| X0-11 / PAIRX | 双路估计器拆分/重排/省略 | attribution-report.ts 零字节未触；本轮零装具 import 估计器 ⇒ **冻结在位** |
| 签名/调用图类（S1-F-6、S1-F-8、X3-3、S3-F-2 等） | 要求 `src/` 变更 | 最强冻结 0 行 ⇒ **全体判空** |

全部哨兵未触发。凡姊妹属地哨兵（S12-B-2 / S13-B-1 / S8-E-1 / S8-I-1 / S9-D-4 / S12-D-1）本轮不代跑——其属主轮 R36-B / R36-C / R36-D / R36-E（及 R33-I / R34-H）已各自复核未触发。

## 4. 逐文件收口（R1-F…R35-F 收口之上的本轮检查点）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| `shadow.ts` | 27 抛点单类维持；S7-F-1 落地形态零漂移（L355–L438 读码直证：L383 同位串等 + 首错位 S6-F-1 后缀回退（L407–L434）+ index 0 原环重跑（L388–L406））、四次序省额三轮全正（§1）；shadowDecisionAt 闭式跳步 = R33-F 换名记录 2 维持；shadowDecisionAt O(index) 重放 = S2-F-1；restore→assign Set 复用 = S2-F-4/S9-F-1；assign 路径 O(P) includes + O(A) 唯一性 = S8-F-1/S3-F-1 本体；recordOutcome 末元素见证 = S9-F-2、查重/成本增量 = S1-F-8；accumulatedCostUsd 每 record 全扫 = S1-F-8 本体；空 assignments 跳 Set = S3-F-3；计划引用同一跳验 = S3-F-2；每操作全量 fail-closed restore = X3-3 本体；返回值拷贝省略 = R32-F 换名记录 3 维持；状态 spread 对 = R15-F 轴 3 引擎面；`resolvePendingMembership` 孪生 = R34-F 换名记录 2 同族注记维持 | 无候选 |
| `plan.ts` | 22 抛点；S5-F 单探针 + S7-F-2 首字符卫现场在位零漂移（不重做）；批量 Set = S5-F-2、null-proto = S6-F-4、批快路径 = S6-F-3、长度 1 快路径 = S8-F-3、scratch 共享 = S9-F-3、返回 Set = S1-F-6；EXPERIMENT_ID_PATTERN 模块级已提升；`mode` 字段 = 领域枚举非权限位（MODEX 划界注记，§0）；r5f C1/C2、r7f validate 卫三轮全正 | 无候选 |
| `canary.ts` | 14 抛点；同构对齐环 + S6-F-1 回退零漂移（对齐比较先于本体，与 shadow 次序相反——落地注释在案）；derivedExposure 重算是 fail-closed 交叉验证本体（消除即 fail-open，非候选）；requireCanaryBlock 重派生缓存 = R33-F 换名记录 4 维持；reversibleScopes includes = S1-F-7；exposure 派生为契约本体；`resolveCanaryPendingMembership` 孪生 = R34-F 换名记录 2 同族注记（X1-5 契约不同不并） | 无候选 |
| `gated-comparison.ts` | 1 抛点；IMPROVEMENT_CLAIM 孪生正则 = R33-F 换名记录 3 维持；mean/sampleStandardError 孪生辅助去重 = R34-F 换名记录 2 维持；**pairedEvaluationCard 内 SE 对 mean 的内部重算 = S2-F-6 亚案**（§0 裁决 1，复命中即止）；strip-retry 复用 = S1-F-1；多遍融合 = S2-F-6 本体 | 无候选 |
| `replay.ts` | 3 抛点；orderedHashes spread 复制消除 = R33-F 换名记录 1 维持（零生产调用方 + µs + R15-F 轴 3）；双 manifestHash = S2-F-2（零生产调用方本轮直证）；selected propensityFor 二次调用 = S1-F-2；空 exclusions 免建 = S4-F-2；exclusions Set 化已落地（H1）；rerunHash 换更快同字节序列化 = S6-D-5 已拒（且 replayPolicy 零流量）；replayCacheKey 唯一生产腿 = once-per-eval µs（S7-D-2 邻域） | 无候选 |
| `comparison-report.ts` | 8 抛点；IMPROVEMENT_PATTERN 孪生编译 = R33-F 换名记录 3 同项；六遍改单遍 = X3-2；校验遍+构建遍双遍 = X3-2 族；四均值 reduce = X3-2 族；`Array.from(families.entries())` = S1-F-5；delta CSE = S4-F-3 本体（本轮 utility delta 双算复命中即止）；familyBreakdown 查重单探针化 = R34-F 换名记录 3 维持；**pairedDeltaSummary `count===0` 死抛 = R4-F「pairedDeltaSummary 型死分支」防御纵深保留域原型站点**（§0 裁决 2，复命中即止）；validateComparisonReport 生产消费者（promotion-rules）零 catch = PROPAGATE 维持 | 无候选 |
| `threshold-calibration.ts` | 零抛点；三遍并单遍 = S1-F-4（一次性 informational）；**`best?.f1 ?? -1` 死回退 = R4-F 同一防御纵深保留域**（R4-F 逐文件表本行逐字点名，§0 裁决 2） | 无候选 |
| `shadow-compare.ts` | 零抛点；薄封装维持 X1-5；routeR1 本体属 C 区（R36-C 本轮已对其 cheapest-reduce 站点作换名拒列，F 不重提不代跑） | 无候选 |
| `isolation.ts` | 6 抛点；resolve(outputRoot) 提升 = S3-F-4；per-root 双 isInside 的 resolve 重复 = 同族 + once-per-eval µs；**全生产普查唯一被 catch 的切片故障源**（eval-routing L336，once-per-eval 冷通道，§2） | 无候选 |
| `simulation-holdout.ts` | 10 抛点；结果装配条件 spread 链 = R34-F 换名记录 1 维持（S8-E-2 镜像 + 零流量 + µs）；探针去重镜像 = S5-F-1（链零生产消费者本轮直证）；尾部双验 = S2-F-5（fail-closed 纵深）；二遍构建 = X3-2 族 + 零流量；toFrozenEpisode 投影 = S3-F-5；auditHoldoutAccess try/register = once-per-run | 无候选 |
| `holdout.ts` | 11 抛点；审计追加拷贝 = S1-F-3/X4-2；注入缝默认臂 nowIso/randomUUID = R32-F 换名记录 2 维持（R31-A 停放 + 零生产流量）；Map 重 set 不可变态 = X4-2 族；replace 的 reason 前置检查次序 = 契约文本面（消息文本冻结，R31-F (c) 邻域，非候选） | 无候选 |
| `manifest.ts` | 14 抛点；stableStringify 形态改造 = R32-F 换名记录 1 维持（字节契约仪器 + 零热流量 + S1-I-7/X1-1 族）；validateManifest 每建 Set = once-per-creation；`?? []` 空数组默认分配 = R15-F 轴 3 引擎面 ns 域（mark/assert 腿 test-only 零流量） | 无候选 |
| `evaluation-card.ts` | 7 抛点；validate 数组字面量 = S1-B-7 域 | 无候选 |
| `dataset.ts` | 19 抛点；seen Set 复用 universe = S4-F-1；has+add 原形镜像去重 = S5-F-1（test-only 链维持，C5 本轮 14.7–18.0 µs 与历带交叠，§1(ii)）；membership Map 双结构 = 同域；rotateHoldout 双验 = S2-F-5 族 once-per-rotation | 无候选 |
| `attribution-report.ts` | 零抛点；**PAIRX 编排位点**——零字节未触、X0-11 双路对未拆分/未重排/未省略 | 无候选（共驻剂量归 R29-C） |

## 5. 候选总表

**空。** 本轮零候选：面格属主表第六次独立重推（§2）无一格松动且无新残格（BINDX 计数 10/8 第三次独立复证成立，坐标不动）；哨兵复核（§3）按定义不产出候选（不计时、不剂量）；十五文件枚举（§4）全部命中既有 ID 本体、R4-F 防御纵深保留域、R32-F/R33-F/R34-F 既有换名记录、姊妹归档面或停驻门成员——**零新站点级换名记录**。本轮无任何计时新臂 ⇒ 无可定价候选面。

## 6. CHECKS

- 分支恰 `cursor/r36-f-experiments-thirty-sixth-pass-83a1`，基线 `a0fd7ed`；测量与门禁完成后多次 fetch 复核 tip 仍 `a0fd7ed`；提交前先后检测到两次纯 docs 快进 `a0fd7ed` → `f4f804f` 与 `f4f804f` → `6891c74`（各恰 3 docs 文件，`-- src/` 均 0 行），均已按令 `git merge --ff-only` 纯快进对齐并各重验冻结；R36-D / R36-E 已先后补读折入（§0），R36-G / R36-H 在飞零触碰。
- 冻结五点核对全 **0 行**（§0，含两次快进后各一验）；15 文件普查 2550 行与 R29-F…R35-F 同数（两次快进后重数一致）；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**（含两段快进）；`package.json` / `pnpm-lock.yaml` 对 `519101f` **0 行**；`git log -1 -- src/experiments/` 仍 = `519101f`。
- 锚点 r5f/r6f/r7f 各三次独立运行 + r1f 一次全部 0 失败（224 / 27 / 169 / 2668 项，§1）；四次序 + 三次序省额三轮全正，无符号翻转；全部在 nvm Node v22.22.2 上执行（每个官方批次前 `node --version` 在同 shell 复核；零批次落在系统 v22.14.0 上，无弃置批）；官方批全部在 tip `a0fd7ed` 单树完成——两次快进后三 tip 间 `src/ test/ scripts/ package` 逐字节相同 ⇒ 结果原样携带，无需重测。
- 生产消费普查：4 个 importer 文件（import 行逐条核对）；catch 位点恰 1 处包切片调用（冷通道 once-per-eval，L336）；热消费者零 catch；142 抛点逐文件计数与 R31-F…R35-F 全同；切片零直抛 Error/TypeError。
- 姊妹面划界计数：BINDX `Math.*` **10** token 于 8 行（第三次独立复证；abs 4 + sqrt 4 + imul 2，行集合同一）；R31-E codec 位点 0；REPRX `JSON.parse` 位点 0；MODEX chmod/umask/八进制 mode 位点 0（宽式 `mode:` 命中仅 plan.ts 领域枚举字段）；node: 引入恰 2（isolation.ts node:path、holdout.ts node:crypto）；六链切片外消费者 0（精确交替式零命中）。
- 聚焦测试：`npx tsx --test test/unit/experiments/*.test.ts` = **75/75 pass**（10 suites，tip `a0fd7ed`）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿**（exit 0，tip `a0fd7ed`）——test 1169 / suites 78 / pass 1168 / fail 0 / skipped 1（既有 provider-smoke 凭据跳过，与 R33-F/R34-F/R35-F/R36-A…R36-E 记录同计数）；Node v22.22.2。
- 仓库改动恰本报告一个文件（`git diff cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；live = R0 等价（R1 未接线）；promotion proposal-first；无 Outcome-supported 主张（Checkpoint F-PROD 开放，ADR-005）；无阈值/测试/权限/数据面/公开签名改动；CAS/凭据契约原样；无 fsync/完整性重哈希移除；fail-open 家族保持关闭；分析未变更任何 in-flight run；tracking 无命令权；H/score 未写路由 PASS/FAIL；catalog 行序契约（R28-B W3）与 npm POSIX `.bin` symlink 静默 no-op（R28-I）零触碰；EXCLUSIONS / PROGRESS / PLAN / README 未编辑；不开 PR。

## 7. 重开条件

本轮无新定价轴，不加行。R31-F 故障通道处置分解的三条显式重开触发（(a) 热交错内非平凡 catch 处置；(b) 非字符串 own 载荷；(c) 通道契约解冻）经 §3 哨兵直证全部未触发，原文全额维持；R30-F（输出消费）、R29-F（输出保留）、R17-F（拒绝终端）、S6-F-2 / S6-F-5 / S5-F-1 / S2-F-1 / S2-F-2 既有重开条件同样全额维持。锚点宿主刻度注记（本 VM 中段主机、三档全落历史包络、C1 比例回落带内、validate 卫与 R35-F 低沿交叠，§1 注记 (i)/(ii)）不构成重开触发——冻结 0 行、省额符号三轮稳定、r1f 计数逐位同历轮、带并集近重合。本轮零新换名记录，不设新重开条件。

## 8. 新增排除 ID

**无。** 空枚举轮不铸 S36-F-\* ID、不铸 face 名、不取绰号、不加 EXCLUSIONS 行。本轮**零新站点级换名记录**（§0 裁决清单全部为既有行复命中；R36-D 四记录家族与 R36-E 六复命中家族的 F 侧镜像面划界亦全部命中既有行）；R32-F 三项 + R33-F 四项 + R34-F 三项既有换名记录复核全部维持；R35-F 零新记录与 R36-A/R36-B/R36-C/R36-D/R36-E 空枚举均按令未重提、未重铸。

## 附录：仿真出处与运行摘录

本轮**未新建**任何仿真/见证脚本：全部计时证据来自入库血统资产的逐字节原样重跑（`scripts/round01-r1f-equivalence-sim.ts`、`scripts/round05-r5f-equivalence-sim.ts`、`scripts/round06-r6f-equivalence-sim.ts`、`scripts/round07-r7f-equivalence-sim.ts`，零改动零参数）；§2/§3 哨兵为只读 rg / 读码断言（不计时、无临时文件）。运行：`npx tsx scripts/round0X-*.ts`（nvm Node v22.22.2，PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开 `/exec-daemon` 系统 Node v22.14.0）。

三次官方运行关键行摘录（r7f；r5f/r6f 对应行见 §1 表）：

```text
run 1: part E prefix saving 46.35ms | half 28.76 | scattered 6.18 | reversed 8.02
       validate x2000 saving 10.29ms | full experiment 128.74ms | 169 checks, 0 failures
run 2: part E prefix saving 45.12ms | half 26.68 | scattered 5.06 | reversed 6.52
       validate x2000 saving 9.50ms | full experiment 125.52ms | 169 checks, 0 failures
run 3: part E prefix saving 45.01ms | half 27.77 | scattered 2.94 | reversed 6.13
       validate x2000 saving 9.99ms | full experiment 128.33ms | 169 checks, 0 failures
```

r1f 摘录：`reference 630.8 ms -> current 62.1 ms (10.2x)`；`reference membership comparisons for the full run: 167,167,000`；`ALL EQUIVALENCE CHECKS PASSED (2668 bitwise checks)`。

MORE_OPTIMA=no
BRANCH=cursor/r36-f-experiments-thirty-sixth-pass-83a1
