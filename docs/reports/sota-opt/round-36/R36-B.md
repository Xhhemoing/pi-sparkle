MODEL_SLUG=claude-fable-5-thinking-xhigh

# R36-B：live 路由切片 Round 36 三十六搜报告

**战役:** 全库持久 SOTA 优化 Round 36 / R36-B（十区之一，R1-B…R35-B 的第三十六遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `514b3ff`（派单 tip，R35-J 摄取 + Round 35 收口 + Round 36 开轮 + 本 A/B/C 波派单）。测量窗口内 exclusive **零快进**（分支创建时、测量后 + 提交前多次 `git fetch origin cursor/sota-persistent-opt-83a1` 均确认 tip 仍为 `514b3ff`），全部测量、聚焦套件与 `pnpm gate` 在 `514b3ff` 的工作树上完成，无需 rebase、无姊妹报告需折并。
**分支:** `cursor/r36-b-live-routing-thirty-sixth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R28-B…R35-B 引擎位级相同；系统 Node v22.14.0（`/exec-daemon/node`）已显式绕开——PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 后 `node --version` 复核 v22.22.2，三次官方运行首行均打印 `engine node=v22.22.2 v8=12.4.254.21-node.39`，仿真内置 L0 引擎同一性断言 ×3 全过；**零官方批次落在 v22.14.0 上**，无需弃批。tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**诚实空枚举：无达门槛的新更优解，切片零代码改动，未铸任何 S36-B-\* ID、未铸任何新 face 名或 ALLCAPS 外号，EXCLUSIONS.md 零增行；且本轮枚举**零**新站点级换名记录——与 R34-B/R35-B 同为「残余角度均系已点名站点复命中」的干净空收口。** 第三十六遍透镜按令**不**重走 R31-B 面格闭合审计（六扇停驻门维持）、**不**重走 R32-B 闭合稳定性审计（54/54 裁决停驻记录）、**不**移植 R33-A…R35-J 空枚举、**不**重提 R35-B 零新站点级记录——改做**从零独立重枚举**：重读 10 文件全量 1359 行，重走结构位置 × 消除机制满矩阵（§2d/§4），对每个枚举出的角度执行换名拒绝检查。结果：全部角度落在既有排除 ID 本体、B 区自有封禁面、姊妹区归档面（明令不移植）或 R31-B 停驻门成员上；本轮枚举尾部仅存的两个残余微观角度（`assign.ts`/`assign-plan.ts` 每批 id 双 map / 双 Set、`decideLiveCascade` 的 `stay` 闭包每调分配）与 R34-B/R35-B §2d 完全同两站点，均为**已点名站点复命中**（分别 R2-B §4 点名 + S7-B-6 每批 ns 天花板 + R27-B batch prelude intercept face 属地、R2-B「S1-B-4 同表面」裁决并经 R5-B/R6-B/R8-B/R34-B/R35-B 五次维持），不构成新站点级记录。`assignTasks` 批前奏 `limits` 条件 spread（assign.ts:65–68）本轮再次单独复核：每批一次分配，属 R27-B **batch-cardinality bottom + prelude intercept face** 属地成员（封禁面，不移植不重铸），非新站点。派单硬保持项显式重取证：**S12-B-2 重开条件腿 (a) 未触发**（`rg -c 'prior' src/adaptation/eval-routing.ts` = 0 匹配（rg exit 1），:353–354 只传 learned；`assignTasks` 生产调用方图景三处（`cli/main.ts:365`、`adaptation/eval-routing.ts:353–354`、`track/primary-split.ts:41`）与 R30-B…R35-B 逐条一致无新增）；**S13-B-1 重开条件未触发**（`learning/learned-routing.ts` 对 `94ed3d9` 逐字节 diff = 0 行，`applyLearnedRouting` 本体成本无变化，维持 loser ID）。

关键前提事实：切片 10 个文件（清单逐一核对恰为 10，`wc -l` 合计 **1359** 行，逐文件计数见 §0，与 R33-B/R34-B/R35-B 逐数一致）自 R1-B 裁决基线 `94ed3d9` 以来**逐字节零变化**（`git diff 94ed3d9..HEAD -- <切片 10 文件>` = **0 行**，分支创建时、测量后、提交前三点核对；测量窗口内 exclusive 零快进，三点覆盖全程）。最强冻结 `git diff 89f0455..HEAD -- src/` 亦为 **0 行**。R1-B 的结构下界论证、R4-B 的聚合天花板论证与 S1-B-\* … S13-B-1 全部裁决对当前代码原样成立。

**测量侧诚实声明（承 R34-B/R35-B 方法注记）**：历届 B 驱动（/tmp/rXXb-sim.mts 族）从未入库，本 VM 上不存在其字节；本轮驱动为**按 R4-B…R35-B 附录约定从零重建**（种子 0xb44b01 mulberry32、N=2000/20000/30 语料、M=2/7/10 目录、×2 eval 对口径、W1/W2/W3 证人、L0 断言——细节见附录 A），故与历届报告的**逐格数值带对照只具指示性**；效力载体是**结构性结论**（格间全序、learned 税方向、落地线余量、亚 µs 探针、prior 倍率）与 ×3 运行内部一致性。天花板本 VM 实测：**ceiling M=2 10.18–11.68 / M=10 18.10–19.18 ms/eval**（×3 运行，×2 对口径；与派单对照 R35-B 9.78–10.25 / 15.54–15.85 邻接上侧——派单已注记 R35-B 为「主机偏快」窗口，本 VM 复位到历史中带：M=10 落在 R14-B 17.6–19.6 / R17-B 19.0–23.8 历史并集内；回归不可能成立：切片对 `94ed3d9` 零 diff）；replay-faithful M=2 **8.38–8.53**（与派单默认对照 R35-B **8.18–9.24** 交叠）；10× 压力 N=20000 **94.06–98.03 ms/eval**（全部落在含 R35-B 读数的历史并集 92.27–140.9 内）；全 18 矩阵格 ×3 保持 M=2 < M=7 < M=10 全序；learned ≥ no-learned 在 **6/9** 格对成立，M=2 / M=10 两对六测全正（+1.7%~+11.7%），**诚实注记**：M=7 对本轮三测全部反转（−1.1% / −6.0% / −5.7%）——与 R33-B（M=10 一例 −0.12%）、R34-B（M=7 两例 −2.3%）、R35-B（M=7 一例 −4.8%）同一 M=7 抖动族且方向与 S13-B-1 机制旁注（配置目录下 avoid 的 allow-list 收缩红利可超过 learned 本体增费）一致，W1（该格双跑逐字节恒等）/ W2（分歧全部落在 avoid/prefer 命中族，inside=420 / outside=0，×3 逐数相同）证人全过、freeze 0 行——按令主机窗口效应不构成结构翻转、不铸坐标；`holds-below-landing-line=true` ×3（最大 19.18 < 30，余量 ~36%）。prior 双档格：假设档 N=2000 M=7/M=10 = 40.84–41.29 / 48.19–49.21 ms/eval **六读全部越线**；M=2 = 27.19 / 27.40 / 32.64 ms/eval——run 1 的 32.64 越绝对线（该格宿主窗，runs 2/3 复位线下），三读为其无 prior 孪生格（8.46–8.98）的 **3.1–3.9×**——「若有流量即数量级越线」的邻接配置结论不变且本轮更强，生产流量本轮重取证仍为零。落地线（数十~数百 ms 或复杂度类下降）在现有生产流量下依旧不可达。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- **切片恰 10 文件**（逐文件 `wc -l`）：`r0.ts` 173 / `assign.ts` 129 / `assign-plan.ts` 68 / `policy.ts` 160 / `live-cascade.ts` 118 / `live-selection.ts` 61 / `analyze-task.ts` 108 / `primary-catalog.ts` 101 / `catalog-model.ts` 83 / `supervisor/model-router.ts` 358，合计 **1359** 行，与 R32-B…R35-B 逐数一致。`catalog-observed.ts` **不在**本切片（按令未纳入、未触碰、未计时）。
- **冻结三点核对全过**：分支创建时（tip `514b3ff`）`git diff 94ed3d9 -- <切片 10 文件>` = **0 行**且 `wc -l` 合计 1359；测量后复核 0 行；提交前第三验 0 行。最强冻结 `git diff 89f0455..HEAD -- src/` = **0 行**。`package.json` / `pnpm-lock.yaml` vs `94ed3d9` 亦 **0 行**。最后一次生产 live-B 变更维持 `94ed3d9`；最后一次 `src/` 生产变更维持 `183df9b`（S7-C，离线 C 区）。
- **读序（按令完成）**：README → EXCLUSIONS 全表（546 行，X0-\* … S13-B-1 全部实读）→ PROGRESS → round-36 PLAN 全文（B 段 + 具名/不具名禁令）→ R35-B 全文（默认对照带 + §2d 两残余角度边界）→ 10 切片文件全量实读（1359 行）。
- **摄取状态**：测量窗口内独占分支零快进（分支创建时与提交前多次 fetch 均 `514b3ff`）；按令未等待 A/C——其自行返回并由父代摄取，姊妹分支零触碰。
- **不移植 / 不重铸清单维持**（枚举时逐条遵守，本轮零触碰）：全部具名禁令 **FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX**；**R31-B 面格六停驻门**（catalog-row hidden-class / IC-shape、cascade evidence×failure-class lattice、assignTasks cold-onset segmentation、batch class-composition mix、LINKTOP-in-B、prior alias-resolution pathway——全部维持停驻，零重开）；**R32-B 闭合稳定性审计**（54/54 与 D1–D5 为停驻记录，不重走、不铸「再查一遍闭合稳定性」式新面）；**R33-A…R35-J 空枚举**（各自站点级换名记录、方法卫生与轴归档均按属地维持，不移植不重铸；R33-B `ROLE_FAMILY` 死回退、R35-B 零新站点级记录均不重提不重铸）；R30-B scale-cube face；R32-A F12a–d 哨兵审计；R32-J 六残余坐标（本切片零 fs 位点）。
- **B 区自有封禁面不补铸**：R29-B learned-policy container face / R28-B catalog-face row-arrangement (permutation) face / R27-B batch-cardinality bottom + prelude intercept face / R26-B privacy / provider-policy constraint-leg face / R25-B task-face optional-field presence topology / R24-B M=1 catalog collapse / R20-B objective 长度 L 轴 + K×W 双线性轴 / R19-B 比较器第三腿 + 目录基数 M 轴。**目录行预排序仍是行为变更**（R28-B W3 裁定行序为契约决策载体），本轮未尝试、未预排序。
- **硬不变量**：切片零代码改动 ⇒ Live=R0 等价（R1 未接线 live；稀疏回退 approved R0 baseline）、双 LCB（Beta+正态）、双归因（logit+prob-add）、proposal-first（Checkpoint F-PROD 开放，ADR-005，不声称 Outcome-supported）、阈值、CAS/凭证/数据面契约/公共签名全部原样保持；live 面不 import R1/bandit/shadow 由 `live-isolation.test.ts` 在聚焦 198 项中复证通过。无 fsync / 完整性重哈希移除；fail-open 家族保持关闭；未改任何测试。分析不改在飞 run；tracking 无指挥权；H/score 不写 routing PASS/FAIL。S5-H-1 / S7-F-1 ≠ S6-F-5 / S8-I-1 / S8-E-1（`loadLearnedRouting` 恰 3 处：`track/loop.ts:88`、`run/flowchart-run.ts:712`、`cli/main.ts:708`，本轮 rg 重证）/ S9-D-4 / S12-D-1 全部切片外硬保持项零触碰。npm POSIX `.bin` symlink 静默 no-op 未当优化修。
- **EXCLUSIONS.md 未增行**（空枚举记录不入排除表，plan-ban 归属父代）；仓库变更恰为本报告单文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核仅 `docs/reports/sota-opt/round-36/R36-B.md`）。禁止开 PR，未开。

## 1. 第三十六遍搜索方法：从零独立重枚举

R31-B 已证面格闭合、R32-B 已证闭合稳定、R33-B/R34-B/R35-B 各做过一次从零重枚举（分别收获 1 / 0 / 0 项新站点级换名记录）——第三十六遍不照抄其结论，而是**独立重跑同一枚举原点**：假装排除表不存在，重读 1359 行找出每一个理论上可疑的消除/重构角度，再逐一对表执行换名拒绝检查（词根不同 ≠ 面不同；站点不同但同族同向 = 换位点记录不铸 ID；**站点已点名 = 复命中，零记录**）。方法四步：

1. **生产调用方图景重取证**——`rg 'assignTasks\(' src/` 恰 3 个调用方（`cli/main.ts:365` smartChildPlan live face N≤30、`adaptation/eval-routing.ts:353–354` 只传 learned、`track/primary-split.ts:41` C 小批），与 R30-B…R35-B 逐条一致，无新调用方、无新可选参数；`assignOne` src 内零生产调用方维持；`effectiveConfidenceThreshold` / `routeTask` / `routeFlowNode` 在 src 内仍只有 `supervisor/flowchart.ts:34–36` barrel 再导出、零消费者（R12-B 零流量取证第六次转移成立）；`routeR0` 唯一调用方仍是 `r1-shadow-report.ts:88`（shadow-only）；`applyCascade` 唯一调用方仍是 `cascade-evidence.ts:102/:108`，`applyEvidenceCascade` src 内零调用方（R5-B「生产不可达」维持；`learning/from-episode.ts:204/:252` 的 `applyCascadeRetry` 为学习区同名异物，非本切片调用方）；live 级联生产调用方仍是 `track/loop.ts:120`、`cli/main.ts:377`（plan）与 `run/child-coordinator.ts:468/:475`（evidence+decide），每子任务一次。
2. **结构位置 × 消除机制满矩阵**——10 文件 × 13 类消除机制（死码/常量折叠/CSE 去重/记忆化/数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/形状单态化）逐格过筛（§4 按文件汇总）。
3. **换名拒绝检查**——非空格逐一对表；本轮尾部仅存的两个残余角度均查证为已点名站点复命中（§2d），**零**新站点级记录。
4. **测量侧不铸新剂量网格**——只重锚既有格 ×3 独立运行 + W1/W2/W3 行为证人 + L0 引擎同一性断言；驱动为从零重建（诚实声明见结论段与附录 A），一次冒烟运行（同字节驱动，全绿、值带内）用于验证 harness 启动后按纪律弃置，官方口径为其后三次独立进程运行。

## 2. 天花板复测：R4-B…R35-B 收口第三十三次复核成立（重建 harness）

引擎 Node v22.22.2 / V8 12.4.254.21-node.39（canonical，L0 断言 ×3）。语料种子 0xb44b01（重建生成器）。×3 独立进程运行，全部 ALL CHECKS PASSED；确定性 sink ×3 逐位相同（4002128459）。单位 ms/eval（×2 对口径，eval = baseline+candidate 两次 `assignTasks`，镜像 `eval-routing.ts:353–354`）：

| 格 | run 1 | run 2 | run 3 | 历史参照（指示性） |
|---|---|---|---|---|
| ceiling N=2000 M=2（rich 语料 + limits） | 10.176 | 10.246 | 11.675 | R35-B 9.78–10.25 邻接上侧（R35-B 主机偏快，本 VM 复位） |
| ceiling N=2000 M=10 | 18.097 | 18.381 | 19.177 | R35-B 15.54–15.85 邻接上侧；落在 R14-B 17.6–19.6 / R17-B 19.0–23.8 历史并集 |
| analyzeTask 份额 | 31.1% | 31.6% | 27.6% | R34-B 30.4–32.2% / R35-B 28.3–30.1% 交叠 |
| replay-faithful M=2（bare 语料，eval-routing 裸镜像） | 8.383 | 8.437 | 8.529 | 派单对照 R35-B 8.18–9.24 交叠 |
| 10× 压力 N=20000 M=2 | 95.589 | 98.029 | 94.062 | 含 R35-B 读数的历史并集 92.27–140.9：三读全部带内 |
| CLI live face N=30 M=2（µs/调用） | 68.7 | 69.8 | 75.9 | R34-B 68.8–72.9 / R35-B 71.6–72.7 交叠 |

M=2 全序、M=10 全序、analyzeTask 份额、压力格与 live face 的结构关系逐运行成立；`holds-below-landing-line=true` ×3（M=10 非压力最大 19.18 < 30，余量 ~36%）。**回归不可能成立**：切片对 `94ed3d9` 逐字节零 diff（三点冻结），任何格间 Δ 只能来自主机与重建 harness 构成差；W1（双跑逐字节恒等，3 目录 × 2 臂）/ W2（learned 臂仅在 avoid/prefer 命中族上分歧）/ W3（prior 拾取存在性）三组行为证人 ×3 运行全过，语义与 R30-B…R35-B 恒等。

## 2b. 配置态 × 命令类矩阵：全六格复测（第二十七次全格复核；臂交错测量）

replay N=2000 bare 语料，ms/eval（×2 对口径；六臂在同一 rep 循环内交错采样，median of 15）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| M=2 default, no learned | 8.022 | 8.037 | 7.977 |
| M=2 default, learned | 8.462 | 8.979 | 8.742 |
| M=7 configured, no learned | 12.693 | 13.379 | 13.270 |
| M=7 configured, learned | 12.559 | 12.574 | 12.517 |
| M=10 stress, no learned | 15.056 | 15.215 | 14.995 |
| M=10 stress, learned | 15.700 | 15.469 | 15.888 |

全 18 格保持 M=2 < M=7 < M=10 全序 ×3。learned ≥ no-learned 在 **6/9** 格对成立；**诚实注记**：M=7 对本轮三测全部反转（−1.1% / −6.0% / −5.7%，learned 侧反快），M=2 / M=10 对六测全正（M=2 +5.5/+11.7/+9.6%、M=10 +4.3/+1.7/+6.0%）。该反转与 R33-B 的 M=10 一例 −0.12%、R34-B 的 M=7 两例 −2.3%、R35-B 的 M=7 一例 −4.8% 同为 M=7 配置格抖动族，本轮三读同向且两读略出 ±5% 带——机制方向与 **S13-B-1 已归档旁证**一致（配置目录下 avoid 命中使 partitionLiveCandidates 跳过被除名模型的整行 evaluateLiveCandidate，allow-list 收缩红利可超过 applyLearnedRouting 本体增费），非新信息、非结构翻转；W1（该格双跑逐字节恒等）与 W2（learned 臂仅 avoid/prefer 命中族分歧：三目录 inside=420、outside=0，×3 逐数相同）全过，freeze 0 行——按派单令「host-bias windows with freeze 0 lines are not new findings」，不铸坐标、不加表行。CLI live face M=7 configured+learned = 112.7 / 113.7 / 121.8 µs/调用（R35-B 84.3–107.1 邻接上侧，主机 + 重建构成差同因）。级联探针（M=2 plan 基座，2 tiers）：`liveCascadePlanFromAssignment` 230–236 ns、`decideLiveCascade` 289–291 ns（R35-B 233–242 / 303–305 交叠邻接），均亚 µs，每子任务一次的生产流量下（C≤6/run）仍为深度亚噪声。

## 2c. prior 双档格复测（第二十五次跨 VM；S12-B-2 重开腿 (a) 复核）

重建快照 `pps_r36b_realistic_v1`（4 源 21 行 + aliasMap 7 行；单拾取 11.83–12.28 µs/op——与历届快照构成不同（R35-B 为 18 行），倍率结论按各自 rig 内部孪生格比值给出）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| 假设档 N=2000 M=2 + prior（ms/eval） | 32.636 | 27.398 | 27.188 |
| 假设档 N=2000 M=7 + prior | 40.843 | 41.286 | 40.968 |
| 假设档 N=2000 M=10 + prior | 48.689 | 49.209 | 48.194 |
| 真实档 live face N=30 M=2+prior（µs/调用） | 219.1 | 219.3 | 215.9 |
| 真实档 N=30 M=7+prior+learned（µs/调用） | 329.3 | 333.0 | 331.1 |
| 单拾取 pickFromPublicPrior（ns，M=7 pool） | 12056 | 11826 | 12283 |

假设档 M=7/M=10 六读全部越线（>30 ms/eval）；M=2 本轮 27.19–32.64——run 1 的 32.64 **自身越绝对线**（该格宿主窗，runs 2/3 复位线下 27.19/27.40），三读为其无 prior 孪生格（8.46–8.98）的 **3.1–3.9×**（M=7 为 3.3×、M=10 为 3.0–3.2×）——「若有流量即数量级越线」邻接配置结论不变且本轮更强；生产流量本轮重新取证仍为零：`rg -c 'prior' src/adaptation/eval-routing.ts` = 0 匹配（rg exit 1），:353–354 `assignTasks({ catalog, tasks, learned: … })` 只传 learned。**S12-B-2 腿 (a) 未触发**，条件式落地物维持在架，按令不补铸不重扫（R21-B 快照形状轴维持关闭）。W3（三种 M 下 family=edit 拾取存在：M=2 → premium 0.777、M=7/M=10 → m04 0.823——拾取**身份**与 R35-B 完全一致，quality 数值差异来自重建快照构成，非行为变化）×3 全过。

## 2d. 第三十六遍枚举：换名拒绝检查记录

从零满矩阵扫描（§4）枚举出的全部角度经对表逐一裁决。**本轮零新站点级换名记录**（与 R34-B/R35-B 同为零；R33-B 为恰 1 项）。枚举尾部仅存的两个残余微观角度与 R34-B/R35-B §2d 完全同两站点，均查证为**已点名站点复命中**：

1. **`assign.ts:63` + `assign-plan.ts:29` 每批 id 双 map / 双 Set**（`catalogIds = input.catalog.models.map(id)` 与 `planAssignmentPolicy` 内 `new Set(models.map(id))` 与 `validateConfig` 内查重 `ids` Set 三处每批各枚举一遍目录 id）——R2-B §4 已点名（「`catalogIds` 重 map 与 router 内部 Set 不共享属公开接口变更」），每批一次 ns 级天花板由 S7-B-6 本体承载。复命中，零记录。
2. **`decideLiveCascade` 的 `stay` 辅助闭包每调分配**（live-cascade.ts:87）——R2-B 已点名（「`stay` 闭包每调分配为亚噪声（S1-B-4 同表面）」）并经 R5-B/R6-B/R8-B/R34-B/R35-B 五次维持；本轮探针 decide 全函数 289–291 ns/op，闭包份额为其真子集，生产流量每子任务一次（C≤6/run）⇒ 亚 µs/run。复命中，零记录。

本轮单独复核的一处边缘站点亦未产生记录：**`assignTasks` 批前奏 `limits` 条件 spread**（assign.ts:65–68，每批一次分配）——属 R27-B **batch-cardinality bottom + prelude intercept face** 属地成员（batch prelude = createModelRouter + planAssignmentPolicy + catalogIds + limits 的整体拦截面，封禁面），非新站点、不铸 ID。

其余枚举角度全部落在既有裁决**本体**上（代表性清单，与 R33-B/R34-B/R35-B §2d 清单核对一致）：`validateConfig` ROLES.includes / dup-role Set = S1-D-8 类 + S10-B-3、查重 has+add 单探针 = S9-B-1；`planAssignmentPolicy` 双排序拷贝取极值 = S8-B-1；`assignPlanned` 两次防御拷贝 = S8-B-4 / S2-B-1、options 三条件 spread = S9-B-3 / S4-B-4、批内记忆化 = S3-B-1（learned 层 = S13-B-1 loser、prior 层 = S12-B-2 条件陆器）；route 输入骨架 = S5-B-4 / S3-B-6 / S4-B-5 族；`validateInput` 字面量数组 = S1-B-7、per-id trim 卫 = S9-B-2；unknown-model 预扫描下沉 = S9-B-4；per-route allowed Set = S12-B-1（双向封死）；主循环融合 = S11-B-1（镜像封死）；`evaluateLiveCandidate` 空前置失败别名 = S7-B-3、per-model request 字面量 = R1-B 契约裁决（fixedCostUsd/fixedLatencyMs 所需）、failures 合并 spread = 同站点微观；`toModelDescriptor` 每 (任务,模型) 构造 = R1-B §4.4 架构裁决；`buildDecision` 构造份额 = R32-B D1 普查（19/20 字段活）、eligible.map 与 oneHot 融合 = S6-B-1；justification 预计算 = S7-B-4；approvalPlan 享元 = S6-B-2；one-hot 载体形 = R32-B D4 三重关；`coldStartRoutingScore` 钳位 = S10-B-2；空 avoid/prefer 跳过 learned = S6-B-4；`cheapFirstTiers` 装饰-排序-还原 / localeCompare / 冗余 spread = S3-B-2 / S3-B-3 / S1-B-5；`decideLiveCascade` find+findIndex = S1-B-4；`liveRefusalMessage` 双 .some = S8-B-2；`selectLiveModel` 比较器展开 = S8-B-3；analyze-task 正则链 / HIGH_RISK_RE 双测去重 / 死值谓词 / reason join / long 惰性与换行手写计数 = S4-B-1 / S1-B-1 / S5-B-1 / S5-B-3 / S10-B-1 + S1-B-2；`familyOf` 布尔重排 = S1-B-3、deploy 门冗余合取 = S7-B-1、终端死回退 = R33-B §2d 记录 1（已点名不重提）；preferPrimary 被吸收析取 = S7-B-2（含 contractRisk=false 反例边界复核：`family === "deploy"` 非全吸收，删除仍属行为耦合）；`routeR0` 高风险 find = S1-B-6 / S2-B-3、r0 排序输出 = R1-B 契约；`applyCascade` `{...decision}` 拷贝 = 生产不可达（R5-B/R9-B 维持，本轮 `applyEvidenceCascade` 零调用方重取证）；`primary-catalog` 构造器享元 = S7-B-5 / S6-B-2 类身份可观察、throw 位点 = R17-B 拒绝路径已锚 + R32-B D2 维持；`effectiveConfidenceThreshold` candidates 数组 + `Math.max` spread 与 `routeTask`/`routeFlowNode` 包装 = R12-B 零流量不铸维持（本轮 barrel census 重证）+ Math.\* 绑定属 BINDX 坐标声明即止；`createModelRouter` `{...config, models}` 每 run 一次 CLI 噪声档、跳过二次 catalogModel = S2-B-2；`catalogModel` 条件 spread 属性存在性可观察 = S1-C-10 类。

## 3. 候选总表

**空。** 铸 ID 数：**0**。铸 face 名 / ALLCAPS 外号数：**0**。EXCLUSIONS.md 增行数：**0**。新站点级换名记录数：**0**（两个残余角度均为已点名站点复命中，§2d；limits 前奏 spread 归 R27-B 封禁面属地）。本轮产出为三点哨兵重取证（S12-B-2 腿 (a) / S13-B-1 / 调用方图景）与重建 harness 下的锚点复位（§2–§2c），均不构成候选。空枚举与 R8-B…R35-B 同为战役允许的诚实结局。

## 4. 结构位置 × 消除机制满矩阵扫描（第三十六遍，按文件汇总）

| 文件 | 本轮检查 | 结论 |
|---|---|---|
| `r0.ts` | `applyCascade` 生产不可达第三十六次重取证（`applyEvidenceCascade` src 零调用方；from-episode 的 `applyCascadeRetry` 为同名异物）；routeR0 唯一调用方 r1-shadow-report:88（shadow-only）；S1-B-6/S2-B-3/S3-B-4/S4-B-3 维持；排序输出即契约 | 无候选 |
| `assign.ts` | 调用方图景三处重取证一致（§1）；id 双 map = R2-B §4 已点名 + S7-B-6 本体（§2d 记录 1，复命中零记录）；批前奏 limits spread = R27-B prelude intercept face 属地（§2d 边缘站点复核）；S8-B-4/S2-B-1 双拷贝、S9-B-3/S4-B-4 options、S3-B-1 批内记忆化、X3-1 维持；S13-B-1 重开门未触发；`flowchartRoleForAgentRole` 属 graph 区（跨切片只记录） | 无候选 |
| `assign-plan.ts` | S8-B-1 维持；每批双 Set 同上复命中；`pickPreferredModel` prior 层 = S12-B-2 条件陆器在架未触发；`pickFromPublicPrior` 本体属 `public-prior.ts`（切片外） | 无候选 |
| `policy.ts` | per-model request 字面量 = R1-B 契约裁决；S7-B-3 别名护栏维持；预算/期限 toFixed 串仅失败路径 = R17-B 已锚；COMPLEXITY_RANK 常数查找无池；capability 循环已首缺早退（break 在位） | 无候选 |
| `live-cascade.ts` | `stay` 闭包 = R2-B 已点名复命中（§2d 记录 2）；S1-B-4/S1-B-5/S3-B-2/S3-B-3 维持；byId Map 每 plan 一次 M≤10 ns 档；探针 230–236 / 289–291 ns 复锚（M=2 基座，§2b） | 无候选 |
| `live-selection.ts` | S8-B-3/S8-B-2 维持；`compareLiveCandidates` Number() 布尔差分已是无分支形态；总序契约（R19-B 第三腿封禁）零触碰 | 无候选 |
| `analyze-task.ts` | `ROLE_FAMILY` 死回退 = R33-B §2d 记录 1 已点名（不重提）；HIGH_RISK_RE 双测 = S1-B-1 本体；S1-B-2/S1-B-3/S4-B-1/S5-B-1/S5-B-3/S6-B-3/S7-B-1/S7-B-2/S10-B-1 维持（S7-B-2 边界含 contractRisk=false 反例复核）；份额 27.6–31.6% 复锚（rich 语料构成 R34-B/R35-B 邻域） | 无候选 |
| `primary-catalog.ts` | 单点构造器消灭形状多态（R31-B C1 停驻维持）；构造器享元 = 身份可观察类；modelFor/inferFastId 每配置一次 ns；throw 位点归 R32-B D2 | 无候选 |
| `catalog-model.ts` | `toModelDescriptor` R1-B §4.4 架构裁决维持；oneHotDistribution = R32-B D4 维持；条件 spread 属性存在性可观察（S1-C-10 类）维持 | 无候选 |
| `supervisor/model-router.ts` | `effectiveConfidenceThreshold`/`routeTask`/`routeFlowNode` 零流量重取证（barrel 三行再导出、零消费者）；S12-B-1/S11-B-1 镜像对封死维持；S1-B-7/S9-B-1/S9-B-2/S9-B-4/S5-B-4/S10-B-2/S10-B-3/S7-B-6/S6-B-1/S6-B-2/S6-B-4/S6-B-5/S7-B-4/S8-B-4/S2-B-2 维持；buildDecision = R32-B D1 普查维持（19/20 活）；`createModelRouter` 每 run 一次 CLI 噪声档 | 无候选 |

## 5. 关键裁决细节

- **S12-B-2 换名检查**：假设档 M=7/M=10 六读越线依旧、M=2 以孪生格 3.1–3.9× 倍率维持邻接（run 1 甚至自身越线，§2c），腿 (a) 重新取证未触发（eval-routing 零 prior、调用方图景三处未变、无新可选参数）。未以任何新名重扫快照形状轴。
- **S13-B-1 换名检查**：`git diff 94ed3d9 -- src/learning/learned-routing.ts` = 0 行，本体成本无变化，重开条件（applyLearnedRouting 本体成本增 ≥2 个量级）未触发。learned 容器坐标归 R29-B（封禁），本轮未触碰。本轮 M=7 三测 learned 反快（§2b）恰是 S13-B-1 已归档的「allow-list 收缩红利」机制旁证复现，属既有记录复命中，不构成新面。
- **目录预排序**：R28-B W3 裁定行序为契约决策载体，预排序 = 行为变更，本轮未尝试。
- **重建 harness 与主机窗口**：本轮全部格值出自从零重建驱动（诚实声明见结论段），跨报告数值带对照只具指示性；效力护栏为 (a) 三点冻结 0 行、(b) W1 双跑逐字节恒等 ×3 + 确定性 sink ×3 逐位（4002128459）、(c) W2/W3 语义证人 ×3（divergence 计数 inside=420 / outside=0，×3 逐数相同；W2 计数与 R35-B 的 448 差异来自重建语料 stem 字节构成，同配方不同字节，两者各自确定）、(d) 序结构（格间全序、份额占比、落地线余量、prior 倍率）与 R30-B…R35-B 完全同构。M=7 learned 对三测反转（§2b 诚实注记）、prior M=2 run 1 越线（§2c）与 face M=7+learned 高于 R35-B 窗均在该护栏下裁定为主机窗口 + 重建构成效应；派单已预告「host-bias windows with freeze 0 lines are not new findings」，均不铸轴。
- **引擎纪律**：三次官方运行全部在 nvm Node v22.22.2 产出（L0 断言 + 每次运行前 `node --version` 复核）；系统 Node v22.14.0 上零官方批次，无弃批。一次冒烟运行（同字节驱动，v22.22.2，全绿、值带内）用于验证 harness 启动，按纪律弃置不计入官方口径；官方口径为其后三次独立进程运行。

## 6. 前后对比与测试

切片零代码改动，无前后对比。验证（全部在 Node v22.22.2）：

- 聚焦 live 路由单测：`npx tsx --test "test/unit/routing/*.test.ts"` —— **198/198 pass**（18 suites，含 live-isolation / cascade-evidence / failure-class / assign / assign-plan / analyze-task / r0 / live-cascade / live-selection / catalog-eligibility 等），与 R31-B…R35-B 计数逐字相同。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0** —— 1169 项测试 1168 pass / 0 fail / 1 skipped（既有跳过项，非本轮引入），tsc build 通过；计数与 R9-B…R35-B 逐字相同。
- 仿真 ×3 独立运行 ALL CHECKS PASSED（L0 + W1/W2/W3 逐运行全过；确定性 sink ×3 逐位相同 4002128459）。
- 提交前最终 fetch：独占分支 tip 仍为 `514b3ff`（测量窗口内零快进，无需 rebase）；冻结第三验 0 行；`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 恰为本报告一个新文件。
- 无赢家故 scripts/ 零新增；harness 未入库（重建说明见附录 A，指纹见附录 B，临时文件按纪律核后删除）。

## 7. 新增排除 ID

无。未铸任何 S36-B-\* ID；EXCLUSIONS.md 零增行；零新站点级换名记录（§2d 两条均为已点名站点复命中、一条边缘站点归 R27-B 封禁面属地，无需归档新记录；plan-ban 归属父代）。

## 附录 A：本轮驱动重建说明

历届 B 驱动（/tmp/rXXb-sim.mts 族）从未入库，本 VM 无其字节可衍生；本轮驱动 `/tmp/r36b-sim.mts` 为按 R4-B…R35-B 附录约定**从零重建**：

1. 语料：mulberry32(0xb44b01) 确定性生成 20000 任务（七角色均匀、16 stem 池含 10% 高危尾、25% long（半数 ≥3 换行）、50% 带 contextTokens/outputTokens、10% 显式 contractRisk）；标准格取前 2000，live face 取前 30；**rich**（带 token/contractRisk 选项 + limits）供 ceiling 格，**bare**（仅 taskId/role/objective）供 faithful/matrix/stress 格（eval-routing 裸镜像）。
2. 目录：M=2 = `catalogFromPrimary({primaryModelId:"premium"})` canonical 对；M=7 configured（cheap/m01–m05/premium，成本严格递增无并列，cheap+m01–m05 均有 prior aliasMap 行）；M=10 stress（+m06–m08，premium 0.85）。
3. 口径：×2 eval 对（baseline 空 learned + candidate learned=avoid(cheap@review)+prefer(premium@plan)，镜像 `eval-routing.ts:353–354`）；ceiling/faithful/prior-假设档 median of 7、matrix 六臂同 rep 循环交错 median of 15、stress median of 3、face mean of 2000 调用、探针 mean of 200k、单拾取 mean of 2000。
4. 证人：W1 双跑 JSON 逐字节恒等（每 M 两臂）；W2 learned 臂分歧任务全部落在 avoid/prefer 命中族（outside=0，inside 三目录均 420，×3 逐数相同）；W3 三 M 下 family=edit prior 拾取存在（M=2 → premium、M=7/M=10 → m04，身份与 R35-B 一致）；L0 `process.version === "v22.22.2"` 断言（不计时）；确定性 sink 防 DCE 且 ×3 逐位（4002128459）。
5. 校准史（如实记录）：一次冒烟运行验证 harness 启动与解析（绝对路径 .ts 导入 + tsx 解析路径），同字节驱动全绿、值带内，无需修正；按纪律弃置，官方口径为其后三次独立进程运行。

## 附录 B：驱动指纹与运行方式

- 驱动：`/tmp/r36b-sim.mts`，428 行，sha256 `37c8b6a681d00c965b6c294620cfa694536c57f6de66696e1a0a58c150c3c9c6`（未入库；临时文件已删除）。
- 运行（repo 根，空闲 VM；PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开 `/exec-daemon` 的系统 Node v22.14.0）：

```bash
npx tsx /tmp/r36b-sim.mts   # 独立进程 ×3 = runs 1–3
```

---

**MORE_OPTIMA=no** —— 第三十六遍从零重枚举空收口：零新站点级换名记录（两残余角度均为 R2-B 已点名站点复命中；limits 前奏 spread 归 R27-B 封禁面属地）；重建 harness ×3 全绿复锚天花板（ceiling M=2 10.18–11.68 / M=10 18.10–19.18 ms/eval，freeze 0 行，W1/W2/W3+L0 证人全过，确定性 sink ×3 逐位 4002128459）；全 18 矩阵格全序成立（learned 对 6/9，M=7 三例反转 −1.1~−6.0% 诚实记为主机窗口 + S13-B-1 机制旁证复现）；prior 双档格 M≥7 六读越线、M=2 以 3.1–3.9× 孪生倍率维持邻接且 run 1 自身越线；S12-B-2 腿 (a) 与 S13-B-1 重开门显式重取证未触发；不铸 ID、不铸面、不增排除行；生产代码零改动，聚焦 198/198 + `pnpm gate` exit 0（1169/1168/0/1 计数逐字同）。

MORE_OPTIMA=no
BRANCH=cursor/r36-b-live-routing-thirty-sixth-pass-83a1
