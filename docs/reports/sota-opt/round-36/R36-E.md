MODEL_SLUG=claude-fable-5-thinking-xhigh

# R36-E：learning 切片 Round 36 三十六搜报告

**战役:** 全库持久 SOTA 优化 Round 36 / R36-E（十区之一，R1-E…R35-E 的第三十六遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `83c3429`（派单 tip，R36-B 摄取（`bf0a146`）后的 plan 表 C/D 在飞标注提交；本分支自该点创建）。测量窗口（锚点六官方运行 + 聚焦套件 ×2 + `pnpm gate` ×2）全部在 `83c3429` 工作树完成；提交前 fetch 检测到 exclusive **一次纯 docs 快进** `83c3429` → `a0fd7ed`（R36-C 摄取（空枚举）+ round-36 PLAN/PROGRESS 更新 + R36-F 派单——`git diff 83c3429..a0fd7ed --stat` 恰 3 个 docs 文件（PROGRESS / round-36 PLAN / R36-C.md，483 插入 19 删除），`-- src/` 直接复核 **0 行**），按令执行**纯快进**（`git merge --ff-only`，零冲突）至 `a0fd7ed` 并重验冻结（切片 vs `adb20d7` 与最强冻结 `89f0455..HEAD -- src/` 均 **0 行**，`wc -l` 仍 1770）；已通读新姊妹报告 **R36-C**（C 区诚实空枚举：零新 ID、连续第十八遍零从未点名站点、一项换名检查为 `src/routing/r1.ts` C 区物理对象）并折入非移植清单（§3）——零 E 可移植轴，不重铸。两 tip 间 `src/` + `test/` 逐字节相同（docs-only diff），测量结果原样携带；聚焦套件与 `pnpm gate` 在快进后 `a0fd7ed` 树上**各再跑一次**同计数全绿（§4）。派单要求预读的两份姊妹报告 **R36-A**（空枚举，三项换名检查拒列——全为 A 区物理对象）与 **R36-B**（空枚举，零新站点级换名记录）亦已全文通读并折入非移植清单（§3）。R36-D 在飞，按令未等待、未触碰姊妹分支。
**分支:** `cursor/r36-e-learning-thirty-sixth-pass-83a1`
**切片:** `src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop` 283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 / `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 / `task-success` 69 = **1770 行**，`wc -l` 与 R14-E…R35-E 逐一相同），本轮第三十六遍全量实际重读。
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R32-E…R35-E 正批及 R36-A/R36-B/R36-C 引擎线位级相同；`/exec-daemon` PATH 暴露的系统 Node v22.14.0 已显式绕开——开工即按 R33-E…R35-E 在案陷阱 PATH 前置 `~/.nvm/versions/node/v22.22.2/bin`，`which node` / `node --version` / `process.versions.v8` 三复核 v22.22.2 / 12.4.254.21-node.39，官方六运行逐 run 打印引擎复核；v22.14.0 上**零官方批次、零弃置批次**。tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成，锁文件未改）。

## 结论

**第三十六遍诚实枚举为空：不铸任何 S36-E-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，切片生产代码零改动；且本轮枚举**零**新站点级换名记录——与 R36-B / R34-B / R35-B 同为「残余角度均系已点名站点或既档记录复命中」的干净空收口。** 对 10 个源文件逐行第三十六遍扫描，对照完整排除表（X0-\*…X4-\*、S1-E-1..8 … S9-E-1..3 全系、S13-B-1、R10-E…R35-E 全部无号关闭探针与轴，含 R30-E 悬挂点拓扑、R31-E I/O 文本编解码拓扑、R31-C **BINDX**、R31-J **MODEX**、R32-E §2.2 两项记录、R33-E §2.2 两项复命中、R34-E §2.2 L123 正则记录（派单明令不重提）、R35-E §2.2 L88 `signals.some` 记录（派单明令不重提））与 round-36 PLAN 禁榜（含 R36-A/R36-B 空枚举折入 + R36-C 空枚举快进折入）后，每一个执行站点都能指到既有 ID、无号探针、已点名裁决站点或 plan-ban 轴——**零未点名站点、零新站点级换名记录**。本轮从零重推的全部候选角度经查证均为复命中（§2.2）：`parseLearnedRoutingPolicy` 三个每调用映射回调闭包（L65/L78/L91）落在 R11-E 已点名的 L52–109 载入期校验微观域内（once-per-run 载入 reject 真子集）；`familyFromRole` if 链换表 = R24-E hit-min 臂已计价站点 + 小集合教训族；`compareSignatures`/`computeFeatureSim` 跨文件相似度函数去重 = R12-E/R13-E test-only 面整体拒列伞（S1-E-7/8 / X2-6 族 + X0-5 邻域）；diagnostics `sum/samples/100` 双除法代数合并 = R14-E 逐位理论失败拒列原记录；bandit/diagnostics `kind === "human"` 被吸收析取 = R10-E 既点名冗余析取记录；auto-loop L81 `.map((signal) => parseObservedSignal(signal))` eta 缩减 = S5-E-3 extraSignals 零生产流量伞。生产调用方普查第三十六次确认：learning 模块切片外 `src/` **import 边恰 7 文件**（`privacy/record-classes.ts` 的 4 处 `learning` 匹配全为数据面路径字符串/文档字串，非 import 边，rg 直证）；`patterns` / `signatures` / `attribution` 切片外 `src/` 导入方为零（运行时可达面仅测试）；`diagnostics` 唯一生产消费方为切片内 `auto-loop`（锚点 diagnose 段已计价）；**`loadLearnedRouting` 生产调用位点恰 3 处不变**（`cli/main.ts:708`、`run/flowchart-run.ts:712`、`track/loop.ts:88`——S8-E-1 硬不变量零去重，§2.3）。

按派单在本 VM 重锚默认预算（未拷贝历轮数字；cmp25 夹具 = R25-E 种子/NOW 跨轮可比面；装具为 R32-E 附录 A 归档件的逐行衍生——先复核归档正文指纹逐字符相同再施轮次置换 12 行，指纹与增量清单见附录 A）：

- **SLICE-CPU cmp25 总量：官方六读数 14.9–15.4 µs/run**（15.1 / 15.0 / 14.9 / 14.9 / 15.1 / 15.4，全部 v22.22.2）vs 默认带 14.7–15.2：5/6 带内，run 6 的 15.4 高侧擦沿 0.2（R33-E 高侧 15.3 先例同向）；vs 派单默认对照 R35-E 读数带 14.6–15.2（该 VM 主机偏快）：本 VM 回到带中偏高——与 R36-A run 3 偏热窗、R36-B「R35-B 主机偏快，本 VM 复位」同为本波宿主刻度各异现象，冻结 0 行下按令不构成新发现，**默认带沿用 14.7–15.2 µs/run 不变**。即使整段清零仍 **651–672× 低于 10 ms 落地线**。
- 分段 collect 11.5–11.9（vs R25-E 带 11.3–11.9：**6/6 带内**）/ outcomes 1.3–1.4（带 1.3–1.5 **6/6 带内**）/ bandit 0.8 ×6；diagnose 1.33–1.37 vs 带 1.33–1.46：**6/6 带内**。
- **夹具 VERDICT 行 6/6 逐位一致且与 R25-E…R35-E 夹具行逐字相同**（`E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3`）；live 面 `nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2` 三十六轮不变。
- 配置态锚点（6 采样）：apply(avoid=10, M=10) **262–283 ns/task**（R13-E…R30-E 带 257–312 内 **6/6**——S13-B-1 维持 loser ID 的带内直证）、空策略 **111–116 ns/task**（110–140 带内 **6/6**）、load 现实 registry **79.7–88.7 µs**（74.3–134.5 带内 **6/6**）、load fresh(ENOENT) **29.3–37.4 µs**（历史并集 25.2–36.8 内 **5/6**——run 2 的 37.4 高侧擦沿 0.6，once-per-run I/O 格宿主抖动个体，与主锚 run 6 高侧同向；冻结 0 行 + 其余五读带内 ⇒ 判宿主刻度项不改带，诚实注记 (i)）。四格 23/24 官方采样带内、一格高侧擦沿（对照 R35-E 24/24 与 R34-E 24/24——单格 I/O 擦沿属 R35-A「单格宿主孤点」同类先例）。

回归与门禁（§4）：聚焦切片套件（unit/learning 7 文件 + acceptance/adaptive-loop）**48/48 全绿**（v22.22.2；`83c3429` 树两次 + 快进后 `a0fd7ed` 树一次，三次同计数）；`pnpm gate` 三跑均 **exit 0**（tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1，与 R14-E…R35-E / R36-A/R36-B/R36-C 逐字同计数；两跑在 `83c3429`、一跑在 `a0fd7ed`）。

硬性不变量逐条复核（§0）：双 LCB / 双归因在位；live = R0 等价（R1 未接线）；proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；CAS / 凭证 / 数据面合同 / 公开签名零触碰；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭；**S8-E-1 零去重、S9-E-2 未触碰（两遍结构读码在位）、S13-B-1 维持 loser ID、S12-B-2 条件陆器 E 侧入口见证未触发**（`eval-routing.ts:353–354` 仍只传 `learned`；全文 `prior` rg 零匹配 exit 1）。仓库变更仅本报告一个文件。空枚举与 R10-E…R35-E、R36-A/R36-B/R36-C 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → EXCLUSIONS.md（完整表 546 行，S1-\* … S13-B-1 + X 继承行全部实读）→ PROGRESS.md → round-36/PLAN.md（E 段 + 具名/unnamed 全 ban 榜）→ R35-E.md 全文（默认对照带 14.6–15.2 与 L88 记录先例）→ R36-A.md / R36-B.md 全文（两姊妹轴折入非移植清单，§3）→ 10 个切片源文件全量重读，之后才动手测量。
- 基线漂移检查：分支创建点 = 派单 tip = `83c3429`；测量窗口内零快进；提交前检测到一次纯 docs 快进 `83c3429` → `a0fd7ed`（增量恰 PROGRESS / round-36 PLAN / R36-C.md 三 docs 文件，`src/` 0 行），按令纯快进零冲突。冻结主张**三点核验均 0 行**（10 文件 vs `adb20d7`：分支创建时、锚点测量与门禁完成后、快进后提交前）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均 docs，含 `a0fd7ed`）；切片最后生产变更维持 `9bf3e1a`（`git log -1 -- src/learning/` 直证；`adb20d7` 为其后的 docs-only 摄取提交，冻结基线语义与 R9-E…R35-E 逐轮一致）。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：async suspension-point topology / per-run suspension-manifest（R30-E——本轮零悬挂计数、零 manifest 臂）/ **PWL**（`proposeRoutingFromOutcomes` 提案写路径读码即止）/ **RVL** / **RTL** / **CG** / **JB**（`applyCascadeRetry` spread 形态不重提）/ **NVG** / **REPRX**（切片 `JSON.parse` 站点恰 **3**：learned-routing.ts:55、bandit-store.ts:20、auto-loop.ts:239——rg 直证逐位点同 R33-E…R35-E，全部既有定价面，计数即止不重铸）/ **PAIRX** / **ZREP** / **LINKTOP** / **BINDX**（切片 `Math.*` token 恰 **1 个**：attribution.ts:45 `Math.min`，rg 直证，住在零生产流量休眠模块内，计数即止零剂量零 patch）/ **MODEX**（切片零 chmod / 零 umask / 零八进制 mode 字面量，rg 空匹配 exit 1 声明即止）/ I/O text-codec topology（R31-E——切片 3 个 fs UTF-8 位点：auto-loop.ts:236、bandit-store.ts:14/:80，rg 直证原形在位，codec / transcode-byte 臂零重做）/ **R35-E §2.2 记录 1**（`runAutoAdaptLoop` L88 `signals.some((s) => s.modelId !== undefined)` bandit 前置门探针融合/下沉——派单明令不重提；本轮读码复核原形在位 auto-loop.ts:88，零重铸）/ **R34-E §2.2 记录 1**（`loadLearnedRouting` L123 `/no registry snapshot/` 正则字面量每调用求值——派单转承 R35-E 明令不重提；读码复核原形在位）/ R32-A 重开条件哨兵审计方法（不重铸——凡要求 src/ 变更的重开条件由 0 行冻结判空；S6-E-1 事件规模条件与 S12-B-2 条件陆器由 §2.1/§2.3 读码顺带见证未触发）/ R32-B 闭合稳定性审计（不重走，不铸「再查一遍闭合稳定性 / 重开条件 / 门扉 / 未点名站点」式新面）/ **R33-A … R35-J 空枚举**（十切片方法卫生记录，不重演账本分类）/ **R36-A 空枚举**（三项换名检查全为 tracking A 区物理对象，划界见 §3）/ **R36-B 空枚举**（零新站点级换名记录；S12-B-2 腿 (a) 与 S13-B-1 重开门显式重取证未触发，与本轮 §2.3 E 侧见证互为佐证不互抄，划界见 §3）/ **R36-C 空枚举**（快进折入；一项换名检查为 `src/routing/r1.ts` C 区物理对象，零共享，划界见 §3）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / RUNLIM / VALCLASS / AGEX / COTARG / PRICEX / DELINV / PROBX / SIDEC / CSPELL 全部未编号未移植、未写入 EXCLUSIONS 表行。X\* / S1-\* … S35-\* 全部未重开。
- 硬不变量零触碰：双 LCB（beta + normal，`src/routing/posterior.ts` 属 C 区）与双归因（logit + prob-add，`src/experiments/attribution-report.ts` 属 F 区）均保留；live = R0 等价（R1 未接线）；sparse → approved R0 baseline；promotion proposal-first（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；不改测试迁就重构；CAS / 凭证 / 数据面合同 / 公开签名不动；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭；分析不改 in-flight run、tracking 无命令权、H/score 不写路由 PASS/FAIL（零 diff 天然满足）；**S8-E-1 不去重 loadLearnedRouting**（本轮仅 §2.3 重普查其 3 个调用位点与 §1 单价带）；**S9-E-2 负优化未触碰**（collectSignalsFromEvents 两遍结构读码在位：L139–157 pass-1 + L160–242 pass-2）；S13-B-1 维持（apply 格带内直证 + learned-routing.ts 冻结 0 行，与 R36-B §5 哨兵重取证互为佐证不互抄）。catalog 行序契约（R28-B W3）与 npm POSIX `.bin` symlink 静默 no-op（R28-I）均为切片外硬保持项，零触碰。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。不开 PR。输家脚本不入库（本轮无输家道——除锚点装具外零计时新臂；装具为既档衍生未提交 `scripts/`，增量与指纹见附录 A，临时文件核后删除）。

## 1. 冻结证明与 SLICE-CPU / 配置态重锚（cmp25，六官方运行）

```bash
git log --oneline -1 origin/cursor/sota-persistent-opt-83a1
# 83c3429 docs(sota-opt): mark R36-C/D as in flight on the plan table
#（测量窗口内零快进；提交前一次纯 docs 快进 83c3429 → a0fd7ed，
#  src/ 0 行，纯快进后冻结第三验仍 0 行）
git diff adb20d7 -- src/learning/ | wc -l        # 0
git diff 89f0455..HEAD -- src/ | wc -l           # 0
wc -l src/learning/*.ts                          # 1770 total（同 R14-E..R35-E）
git log --oneline -1 -- src/learning/            # 9bf3e1a（最后生产变更不变）
```

锚点装具 `/tmp/r36e-anchor.mts`（365 行）为 **R32-E 附录 A 归档件的逐行衍生**：从 round-32/R32-E.md 附录 A 提取归档正文（复核 365 行、sha256 `494f801c2ccb621dbf7abca6d3116561b98010be09cf2aa19ad3cf366d64caff` 与 R32-E/R33-E/R34-E/R35-E 归档值逐字符相同），增量仅轮次置换共 **12 行**（附录 A 逐行清单）：头注 R36 标注（L2–5/7/14）、mkdtemp 前缀 `r36e-cmp25-*`（L327/332）、projectRoot `/tmp/proj-r36e`（L329/333）、detector identity `r36e-anchor-cmp25`（L336/346）。**seeds `0xe25e01/02/08` 与 NOW = R25-E `2026-08-25T17:30:00.000Z` 逐字节未动**（L84/L271–273 提取后 grep 直证）⇒ cmp25 跨轮可比面维持，VERDICT 行必须逐字重现 R25-E…R35-E 夹具行（下表 6/6 达成）。官方批 **6 × v22.22.2** 独立进程（逐 run 打印引擎复核；v22.14.0 零批次零弃置；一次冒烟运行验证装具启动后按纪律弃置）；每次 3/3 sanity 全过。代表性一次运行（run 3）：

```text
VERDICT fixtures: E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=11.5us outcomes=1.3us diagnose=1.35us bandit-build=0.8us | total in-slice CPU ~14.9us per run vs landing bar >=10000us (670x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=262ns/task | empty policy=112ns/task -> x10 tasks/run = 2.62us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=29.9us | realistic registry (baseline+1 candidate)=83.4us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

六官方运行原始区间（全量见附录 A）：

| 格 | 本轮六读数 | 历史带 | 判定 |
|---|---|---|---|
| SLICE-CPU 总量 | 15.1 / 15.0 / 14.9 / 14.9 / 15.1 / 15.4 µs | 默认带 14.7–15.2；派单对照 R35-E 14.6–15.2 | 默认带 5/6 带内、run 6 的 15.4 高侧擦沿 0.2（R33-E 15.3 先例同向，注记 (i)）——**带沿用不变** |
| collect | 11.5–11.9 µs | R25-E 11.3–11.9 | **6/6 带内** |
| outcomes | 1.4 / 1.4 / 1.3 / 1.3 / 1.4 / 1.3 | R25-E 1.3–1.5 | **6/6 带内** |
| diagnose | 1.35 / 1.34 / 1.35 / 1.33 / 1.35 / 1.37 µs | R25-E 1.33–1.46 | **6/6 带内** |
| bandit-build | 0.8 ×6 | 0.8 | 6/6 |
| apply(avoid=10, M=10) | 265 / 264 / 262 / 270 / 270 / 283 ns/task | 257–312 | **6/6 带内** |
| apply(空策略) | 114 / 111 / 112 / 116 / 115 / 115 ns/task | 110–140 | **6/6 带内** |
| load fresh(ENOENT) | 30.9 / 37.4 / 29.9 / 35.7 / 33.6 / 29.3 µs | 并集 25.2–36.8 | 5/6 带内；run 2 的 37.4 高侧擦沿 0.6（once-per-run I/O 格宿主抖动个体，注记 (i)） |
| load real registry | 80.3 / 79.7 / 83.4 / 80.2 / 83.0 / 88.7 µs | 74.3–134.5 | **6/6 带内** |

SLICE-CPU 重锚判定：**默认带沿用 14.7–15.2 µs/run 不变**，本轮读数 14.9–15.4 与其几乎重合（run 6 高侧擦沿 0.2 为宿主慢侧个体）；即使整段清零仍 651–672× 低于 ≥10 ms 落地线——派单 µs/ns 拒收类直接适用于切片全部 CPU 面。once-per-run 生产 I/O 位点（load fresh / load real）µs 域、once-per-run CLI 噪声拒收类同样适用。

**诚实注记**：(i) 本 VM 会话相对派单对照 R35-E（主机偏快窗）呈带中偏高刻度：主锚 run 6 高于默认带上缘 0.2 µs、load fresh run 2 高于历史并集上缘 0.6 µs，两者同向；其余 22/24 配置态采样与全部 collect/outcomes/diagnose 分段带内。与 R36-A run 3 全格偏热窗、R36-B「R35-B 主机偏快，本 VM 复位到历史中带」同为本波跨 VM 宿主刻度各异现象。冻结 0 行 + 引擎位级同一 + 装具血统指纹逐字符相同 ⇒ 仪器/宿主现象非切片行为变化，按派单令「host-bias windows with freeze 0 lines are not new findings」不作发现、不改带。(ii) 本轮六官方运行一次性完成；一次冒烟运行（同字节装具，v22.22.2，3/3 sanity 全过、值带内）用于验证装具启动，按纪律弃置不计入官方口径；零校准批、零弃置批（官方口径内）、零中断存档事故。

## 2. 第三十六遍枚举（诚实结果：空；零新站点级换名记录）

### 2.1 逐文件重读与裁决（零未点名站点）

| 文件（行数） | 本轮重读检查（全部命中既有 ID / 无号探针 / 已点名站点 / plan-ban 轴） | 结论 |
|---|---|---|
| `auto-loop.ts`（283） | persistSignals evidenceIds.filter 双算 = S1-E-4（I/O 支配）；persistSignals∥updateProjectBandit 编排重叠 = S4-E-2（双故障非确定）；ingestSubagentDirectory 顺序读 = S1-E-5；optimizedPolicy filter+map 融合 = S2-E-6（n=10 实测更慢）；runAutoAdaptFromEvents 前置扫描 = S3-E-2 / S5-E-1；无变更跳存 = S2-E-1（X0-3 保存时机）；切片内惰性 import = S5-E-5；三段 signals spread 拼接 = 分配级噪声（collect 锚整体计价）；**L88 `signals.some(modelId !== undefined)` bandit 前置门探针融合/下沉 = R35-E §2.2 记录 1 按令不重提**（原形读码复核在位）；L81 `.map((signal) => parseObservedSignal(signal))` eta 缩减 = **本轮复命中 e**（§2.2，S5-E-3 extraSignals 零生产流量伞真子集）；proposeAndMaybePromote 两分支各一次 `saveAdaptationRegistry` 文本合站 = 零动态增量非优化（R35-E 既点名，读码即止）；LEARN_EVALUATION_PLAN 与 from-episode 同名常量各模块求值一次 = X0-3 邻域重构（既点名）；异步悬挂点拓扑 = R30-E 面（plan-ban）；:236 utf8 decode / :239 JSON.parse = R31-E codec 面 + REPRX 计数即止 | 无新候选 |
| `learned-routing.ts`（216） | loadLearnedRouting 去重 = **S8-E-1 硬不变量**（调用位点普查 §2.3）；prefer.find 跨界去重 = S8-E-3；applyLearnedRouting 单次成本 = S13-B-1 维持 loser ID（262–283 ns 采样带内直证，§1；其内 avoided Set 构建与三连 `includes` 回退链同属该格计价）；L123 错误探针正则字面量每调用求值 = **R34-E §2.2 记录 1 按令不重提**（原形读码复核在位，D 侧孪生 R34-D 记录 3 同族同向裁决维持）；parseLearnedRoutingPolicy 错误信息正则字面量提升 = R33-E 既点名（冷路径 + S3-E-5 族亚噪声）；**parse 路径三个每调用映射回调闭包（avoid.map L65 / prefer.map L78 / assignments.flatMap L91）提升 = 本轮复命中 a**（§2.2，R11-E L52–109 载入期校验微观域真子集）；stableProjectKey 记忆化 = R32-E §2.2 记录 1 拒列维持（X1-1 族 + once-per-run µs 拒收，不重铸）；identity/print/parse 面 = REPRX 划界 | 无新候选 |
| `from-episode.ts`（298） | proposeRoutingFromRoutedEvents 三遍扫描融合 = S2-E-2；提案写路径 = **PWL**（plan-ban，读码即止）；applyCascadeRetry spread / 死 behaviorDistribution 重算 = S9-E-1（**JB** 邻域不重提）；familyFromRouted 小表 includes = S7-E-3 小集合教训族；outcomeKindFromResult 死形参 = R32-E §2.2 记录 2 拒列维持（纯翻搅零收益，不重铸）；isCompleteRoute 逐字段 typeof 收窄消除 = **R33-E §2.2 复命中 1 维持**（谓词序 R10-E…R16-E 行 + R24-E 拦截定价 + S9-D-3 谱系防御纵深，拒列即止）；policyFromOutcomes seen Set 原形健全（个位数 arms 线性域）；routes Map + TASK_RESULT 绑定链 = outcomes 锚整体计价（1.3–1.4 µs 带内） | 无新候选 |
| `signals.ts`（439） | 两遍结构 = S1-E-1 本体 + **S9-E-2 负优化封死**（未以任何形态触碰，两遍结构读码在位）；JUDGE_DECISION 双 Map.get = S1-E-2；verdict 双三元 = S7-E-5；extractAssistant 折叠 = S1-E-3；truncate 恒等快路径 / 流式早退 = S7-E-2 / S2-E-5；scoreUserAnswer 双正则 / trim = S6-E-2 / S7-E-1；baseSignal 条件 spread = S8-E-2（PIC 敏感）；形状单态化 = S4-E-3；ctx 提升 = S5-E-2；`/unknown agent/i` 提升 = S3-E-5；request.agent 探针提升 = S7-E-4；探针复用 = S6-E-5；TASK_RESULT binding 双拷贝 = S2-E-7；五 Map 合一 = S6-E-1（重开条件未触发：事件规模未增 3 个量级且仍被 readAll 支配）；空事件快路径 = S4-E-1；**familyFromRole if 链换表 = 本轮复命中 b**（§2.2，R24-E hit-min 臂已计价站点 + 小集合教训族） | 无新候选 |
| `bandit-store.ts`（83） | 循环内 arms.includes = 个位数 arms 小表噪声（S2-E-3 邻域，R35-E 既点名）；弃置 createBanditState = S2-E-3 本体；per-arm pulls/rewardSum 重建 = S3-E-4 邻域小表噪声；**L71 `kind === "human"` 被吸收析取消除 = 本轮复命中 d**（§2.2，R10-E 既点名冗余析取 + 规格意图护栏）；recordReward 不可变翻搅 = X4-2 readonly 契约（本体属 C 区 `src/routing/bandit.ts`，越切片）；mkdir 每写 = S6-E-3；pretty-print = S6-E-4；无变更跳写 = S2-E-4（write-always 契约）；arms/reward 双扫融合 = S3-E-4；:14/:80 utf8 / :20 JSON.parse = R31-E codec 面 + REPRX 计数即止 | 无新候选 |
| `task-success.ts`（69） | copyDefinedBinding 条件 spread = S5-E-3 族亚噪声；taskSuccessFromResult × scoreTaskResult 分派树合一 = S9-E-3（公开面扩张 / X1-2 复制）；V2 语义零触碰（RVL plan-ban） | 无新候选 |
| `attribution.ts`（92） | attributeToBoundary 排序换单遍 min = S1-E-7（test-only；其比较器内 BOUNDARY_ORDER.indexOf 逐次线扫同站点同伞）；findNegativeControlMarker 线扫 = 同一 test-only 可达面；:45 `Math.min` = BINDX 坐标计数即止（切片唯一 Math token，rg 直证）；生产调用方第三十六次为零（§2.3） | 无新候选 |
| `diagnostics.ts`（78） | 组内多遍融合单遍 = S1-E-6（真实规模亚噪声且 10× 更慢）；分组键换嵌套 Map = S5-E-4（平局序发散）；`unique()`/`mode()` Set 化 = **R33-E §2.2 复命中 2 维持**（R12-E 行 + R23-E 机械价格：FeedbackKind 基数 ≤4 线扫更快，拒列即止）；**L36 `sum/samples/100` 双除法代数合并 = 本轮复命中 c**（§2.2，R14-E 逐位理论失败拒列原记录）；L23–24 `kind === "human"` 被吸收析取 = 复命中 d 同伞（R10-E 行）；唯一生产消费方 = 切片内 auto-loop，锚点 diagnose 段 1.33–1.37 µs 已计价（§1） | 无新候选 |
| `patterns.ts`（146） | detectRepeatedPatterns keys 预取 = S1-E-8（test-only）；相似度对键记忆化 = X2-6（已否决）；computeFeatureSim keySet 物化 = 零生产流量域（signatures 行同判）；O(n²) 聚类 = test-only 零生产调用方（§2.3）；**computeFeatureSim 与 compareSignatures 跨文件去重 = 本轮复命中 f**（§2.2，R12-E/R13-E test-only 面整体拒列伞） | 无新候选 |
| `signatures.ts`（66） | JSON.stringify 哈希入参为内存对象（codec 不在环，R31-E C1 见证既档）；消费方 signals / attribution / patterns 均 type-only 或 test-only 可达；keySet 数组物化 = 同一零生产流量域；compareSignatures 去重角度 = 复命中 f 同伞 | 无新候选 |

结构位置 × 消除机制满矩阵（死码 / 常量折叠 / CSE / 记忆化 / 数据结构 / 融合 / 早退 / 免分配 / 复杂度类 / 强度削减 / 惰性 / 平行路径 / 形状单态化）逐格过筛：非空格全部落在上表既有 ID、plan-ban 轴、R32-E/R33-E/R34-E/R35-E §2.2 既有记录或 §2.2 本轮复命中清单上。

### 2.2 换名拒绝检查：本轮零新站点级记录（全部复命中）

从零重推（假装排除表不存在重读 1770 行）枚举出的候选角度经对表与对历轮报告全文检索逐一查证，**全部为已点名站点 / 既档记录复命中或按令不重提项，零新站点级换名记录**（R33-E 恰 1 项 / R34-E 恰 1 项 / R35-E 恰 1 项之后，本轮与 R36-B 同为干净零收口）：

- **复命中 a：`parseLearnedRoutingPolicy` 三个每调用映射回调闭包提升**（learned-routing.ts:65/:78/:91——avoid.map / prefer.map / assignments.flatMap 回调各自每调用新铸；候选形态为模块级常量提升，R36-A 记录 1 在 A 区 types.ts 的同构对象）——查证为 **R11-E 已点名 L52–109 载入期校验微观域的真子集**（R11-E 原裁决：「该函数只在载入面执行，派单把整个 once-per-run 载入列为 reject——其内部 ns 级微观是 reject 的真子集，无候选资格」）+ S7-J-3 回调外提家族。复命中即止，零新记录。
- **复命中 b：`familyFromRole` if 链换查表/Map**（signals.ts:363–370，role 基数 ≤8）——查证为 **R24-E hit-min 臂已计价站点**（1148–1297 ns 全臂含 `familyFromRole` 回退，任何内部重构收益为其真子集）+ 小集合教训族（S7-E-3 第十例谱系、S1-D-8/S1-A-8 小表伞）。复命中即止。
- **复命中 c：diagnostics `sum/samples/100` 双除法代数合并**（diagnostics.ts:36）——查证为 **R14-E §2 原记录逐字维持**（浮点除法非结合，合并后舍入位可观察发散 = 逐位等价性不成立，S6-A-1 半格点同族；且 diagnose 全额 1.33–1.37 µs 即天花板）。复命中即止。
- **复命中 d：bandit reward 循环与 diagnostics 分组守卫的 `kind === "human"` 被吸收析取消除**（bandit-store.ts:71、diagnostics.ts:23–24——`kind !== "deterministic"` 已蕴含 human 排除）——查证为 **R10-E 既点名冗余析取记录逐字维持**（显式 human 排除是「human 反馈永不入 routing quality」的规格意图护栏，S7-B-1 意图护栏同族；ns 级）。复命中即止。
- **复命中 e：auto-loop L81 `.map((signal) => parseObservedSignal(signal))` eta 缩减**（直接传函数引用省一层闭包）——S5-E-3 既档「extraSignals 零生产流量」伞真子集 + 回调族 ns 级。复命中即止。
- **复命中 f：`computeFeatureSim`（patterns.ts:122）与 `compareSignatures`（signatures.ts:45）跨文件相似度实现去重**——两实现语义不同构（compareSignatures 带 kind 守卫 + hash 相等快路径），合并属重构非优化；且 **R12-E/R13-E 既档把 test-only 面（patterns/attribution/signatures 三文件 + compareSignatures）整体自动拒列**（S1-E-7/8 / X2-6 族 + X0-5 私有辅助合并禁邻域）。复命中即止。
- **不重提（按令）：`runAutoAdaptLoop` L88 `signals.some` bandit 前置门探针** = R35-E §2.2 记录 1——派单 DO NOT REMINT 明列，本轮读码复核原形在位即止。
- **不重提（按令）：`loadLearnedRouting` L123 正则字面量每调用求值** = R34-E §2.2 记录 1——本轮读码复核原形在位即止。
- **不重铸：R32-E §2.2 两项拒列记录**（stableProjectKey 记忆化 = X1-1 族 + once-per-run µs 拒收双重命中；outcomeKindFromResult 死形参删除 = 纯翻搅零收益）与 **R33-E §2.2 两项复命中**（isCompleteRoute 谓词收窄 / unique()+mode() Set 化）：裁决逐字维持，站点原形读码复核，不以任何新名重提。

### 2.3 生产调用方普查（第三十六次）

- 切片外 `src/` 对 learning 模块 **import 边恰 7 文件**：`run/flowchart-run.ts`（learned-routing）、`cli/adapt.ts`（from-episode + auto-loop）、`cli/main.ts`（learned-routing + auto-loop）、`adaptation/eval-routing.ts`（learned-routing）、`track/primary-split.ts`（type-only，读码复核）、`track/loop.ts`（learned-routing + auto-loop）、`routing/assign.ts`（applyLearnedRouting）——rg 全库直证（`from "../learning/…"` 边计数），与历轮记录逐文件一致，零新增。诚实注记：宽松模式 `rg -l "learning/"` 额外命中 `privacy/record-classes.ts`，逐行核实其 4 处匹配全为数据面路径字符串 / 脱敏文档字串（`adaptation/learning/projects/<stableProjectKey>/routing.json` 等），**非 import 边**，不改变 7 文件普查结论。
- `patterns` / `signatures` / `attribution` / `diagnostics` 切片外 `src/` 导入方 = **0**（rg `learning/(patterns|signatures|attribution|diagnostics)` 空匹配 exit 1）；运行时可达面仅 `test/acceptance/adaptive-loop.test.ts` 与 unit 套件（diagnostics 另有切片内 auto-loop 消费，已在锚点计价）。
- `loadLearnedRouting` 生产调用位点恰 3 处：`run/flowchart-run.ts:712`、`track/loop.ts:88`、`cli/main.ts:708`——S8-E-1 的 tracked/children 双载形态原样，零触碰。
- S12-B-2 条件陆器 E 侧入口读码直证：`adaptation/eval-routing.ts:353–354` 仍只传 `learned` 不传 `prior`（全文 `prior` rg 零匹配 exit 1，与 R36-B §2c/§5 重取证一致）——未触发，属地在 B/D，声明即止。

### 2.4 枚举收口

第三十六遍逐文件重读**零未点名站点、零新站点级换名记录**；候选角度六项全部查证为复命中（§2.2 a–f），两项按令不重提项读码复核原形在位。切片全部 CPU 面住在 14.9–15.4 µs/run 总量之内（清零仍 651–672× 低于落地线），全部 I/O 面为 once-per-run µs 域既档定价（load fresh / load real 采样直证）或 R31-E codec 面既档（不重铸）。**第三十六遍枚举为空——比强铸败者 ID 更诚实。**

## 3. 姊妹面划界与非移植清单（R36-A/R36-B 派单预读 + R36-C 快进折入）

1. **R36-A（tracking 侧空枚举）**——三项站点级换名检查（types.ts parse 路径每调用回调闭包提升 = S7-J-3 族；from-child artifactIds 防御拷贝省略 = R34-A 记录 2 同函数同族；roller keptIds Set 中间映射数组消除 = S7-A-2 族扩站）+ 预算重锚 13.8–17.6 µs/gate ⇒ ~69–88 µs/run（run 3 全格宿主偏热窗如实注记）+ 哨兵卫生复跑 63/63。E 侧划界：三站点全为 `src/tracking/` A 区物理对象，与本切片零共享；其记录 1（parse 回调闭包族）与本轮复命中 a 为**同构机制、不同物理对象**——E 侧对象（parseLearnedRoutingPolicy L65/L78/L91）已由 R11-E 载入域拒列承载，各自属主不互抄。无新轴可移植。
2. **R36-B（live 路由侧空枚举）**——零新站点级换名记录（两残余角度均为 R2-B 已点名站点复命中；limits 前奏 spread 归 R27-B 封禁面属地）；重建 harness 诚实声明 + 天花板复锚（ceiling M=2 10.18–11.68 / M=10 18.10–19.18；replay M=2 8.38–8.53）；S12-B-2 腿 (a) 与 S13-B-1 重开门显式重取证未触发；M=7 learned 三测反转如实记为主机窗口 + S13-B-1 机制旁证复现。E 侧划界：其 S13-B-1 哨兵（learned-routing.ts vs `94ed3d9` 冻结 0 行）与本轮 §1 apply 格 6/6 带内直证**互为佐证不互抄**；其「重建 harness 指示性对照」方法注记与本轮装具血统衍生路线（附录 A）为两种既档允许口径。无新轴可移植。
3. **R36-C（离线路由侧空枚举，提交前快进折入）**——零新 ID / 零新面 / 连续第十八遍零从未点名站点；一项新换名检查（`routeR1` cheapest-`reduce` 折叠内再算 `costOf`，拒列）；生产中位与组合天花板复锚（中位 667.9–681.0；组合 20.9–34.1 < 35 维持关闭；sink=7.309 八读全同）；S7-C…S1-C 落地形态逐站点在位复核。E 侧划界：其换名站点为 `src/routing/r1.ts` C 区物理对象，本切片零 r1 消费（bandit-store 消费的 `src/routing/bandit.ts` 亦属 C 区，X4-2 划界维持）；其「reduce 折叠内重算 CSE + once-per-report 亚带」裁决谱系与本轮既有裁决一致，各自物理对象不互抄。无新轴可移植。
4. **R30-E 悬挂点拓扑 / R31-E codec 拓扑 / BINDX / MODEX / R32-A 哨兵方法 / R32-B 闭合稳定性方法 / R33-A…R35-J 空枚举 / R34-E L123 正则记录 / R35-E L88 探针记录**——§0 逐项核对，全部零剂量零重做零重铸。

## 4. 测试与验证

```bash
# 本 VM: nvm Node v22.22.2（PATH 前置 ~/.nvm/versions/node/v22.22.2/bin
# 绕开 /exec-daemon 系统 Node v22.14.0）。
npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0
#（83c3429 树两次 + 快进后 a0fd7ed 树一次，三次同计数）

pnpm gate
# typecheck ✓ + lint ✓ + test ✓ + build ✓ （GATE_EXIT=0，三跑）
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（三跑逐字同计数：83c3429 ×2 + a0fd7ed ×1；
#  与 R14-E..R35-E / R36-A/R36-B/R36-C 逐一相同）
```

锚点装具官方 6 次运行全过（§1，3/3 sanity ×6；确定性 VERDICT 行 6/6 逐位一致且与 R25-E…R35-E 夹具行逐字相同）。本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交 `scripts/`，遵守 R7-F/R8-C/R10-E…R35-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件（`git diff cursor/sota-persistent-opt-83a1...HEAD` 仅 `docs/reports/sota-opt/round-36/R36-E.md`，提交前复核）。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐站点有属主；候选角度六项全部为复命中，零新站点级换名记录，两项按令不重提项原形在位）。
- 预算面：SLICE-CPU 默认带 14.7–15.2 µs/run 沿用不变（本轮官方读数 14.9–15.4 与其几乎重合；run 6 高侧擦沿 0.2 与 load fresh run 2 擦沿 0.6 为宿主刻度项，freeze 0 行按令不构成新发现），清零仍 651–672× 低于落地线；配置态四格 23/24 官方采样带内、一格 I/O 擦沿——切片无任何达「数十–数百 ms 真尺度非噪声」门槛的剩余面。
- 判定：**MORE_OPTIMA=no**。

## 7. 新增排除 ID：无

- 不铸 S36-E-\*：第三十六遍枚举为空；不取 ALLCAPS 绰号；不写入 EXCLUSIONS.md 任何行（含对空枚举轴外号的一切再编号）。零新站点级换名记录（§2.2 六项复命中各归属主，无需归档新记录）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态；S8-E-1 / S9-E-2 / S13-B-1 未重开；未重铸 R32-A 哨兵方法与 R32-B 闭合稳定性方法；未在 E 区重演 R33-A 账本分类；未重提 R32-E/R33-E §2.2 既有记录、R34-E §2.2 L123 记录与 R35-E §2.2 L88 记录；未移植 R36-A/R36-B/R36-C 姊妹发现。

## 附录 A：锚点装具增量说明、指纹与原始输出（未入库；/tmp 留存核后删除）

- 装具：`/tmp/r36e-anchor.mts`，**365 行**，为 R32-E 附录 A 归档件的**逐行衍生**：先从 `round-32/R32-E.md` 附录 A（L387–751）提取归档正文并复核指纹（365 行，sha256 `494f801c2ccb621dbf7abca6d3116561b98010be09cf2aa19ad3cf366d64caff`，与 R32-E/R33-E/R34-E/R35-E 归档值逐字符相同），再施加轮次置换共 **12 行**——头注 R36 标注（L2–5/7/14 注释行：轮次序数、谱系上界 R35-E、夹具行谱系上界）、mkdtemp 前缀 `r36e-cmp25-fresh-` / `r36e-cmp25-reg-`（L327/332）、projectRoot `/tmp/proj-r36e`（L329/333）、detector identity `r36e-anchor-cmp25`（L336/346）。**seeds `0xe25e01`（collect）/ `0xe25e02`（routed）/ `0xe25e08`（signals）与 NOW = R25-E `2026-08-25T17:30:00.000Z` 逐字节未动**（L84/L271–273 提取后 grep 直证）；import 路径与语义同归档件。装具全文不重复内联（= R32-E 附录 A 正文 + 上列 12 行置换，任何人可机械重建并核指纹）。
- 本轮装具 sha256：`ffa3f8a2f7c5f6e39bd1a8270d9154edc3df9e7f6b82b6d40d9811e5b927e3b2`
- 运行方式（仓库根，`pnpm install --frozen-lockfile` 后，PATH 前置 nvm bin）：

```bash
node_modules/.bin/tsx /tmp/r36e-anchor.mts   # 独立进程 ×6 = 官方 runs 1–6（每 run 打印 node --version = v22.22.2 复核）
```

六官方运行原始值：

```text
SLICE-CPU 总量:  15.1 / 15.0 / 14.9 / 14.9 / 15.1 / 15.4 us
collect:    11.6 / 11.5 / 11.5 / 11.5 / 11.6 / 11.9 us
outcomes:   1.4 / 1.4 / 1.3 / 1.3 / 1.4 / 1.3 us
diagnose:   1.35 / 1.34 / 1.35 / 1.33 / 1.35 / 1.37 us
bandit:     0.8 ×6
apply(non): 265 / 264 / 262 / 270 / 270 / 283 ns/task
apply(空):  114 / 111 / 112 / 116 / 115 / 115 ns/task
load fresh: 30.9 / 37.4 / 29.9 / 35.7 / 33.6 / 29.3 us
load real:  80.3 / 79.7 / 83.4 / 80.2 / 83.0 / 88.7 us
VERDICT 行: 6/6 逐位一致且与 R25-E..R35-E 夹具行逐字相同；
            sanity 3/3 ×6 全过
```

MORE_OPTIMA=no
BRANCH=cursor/r36-e-learning-thirty-sixth-pass-83a1
