MODEL_SLUG=claude-fable-5-thinking-xhigh

# R36-G：runtime/graph/domain 切片 Round 36 三十六搜报告

- **战役:** 全库持久 SOTA 优化 Round 36 / R36-G（R1-G…R35-G 同切片三十五遍之后的第三十六遍；首次派单 bc-896f4daf 返回 ERROR 无分支，本轮为全新重启，无前次 G 工作可摄取）
- **基线:** 分支创建于独占分支 `cursor/sota-persistent-opt-83a1` @ 派单 tip `6891c74`（R36-E 摄取——空枚举——+ R36-H 派单）。工作期间独占分支**一次纯 docs 快进** `6891c74` → `5adb0f0`（R36-F 摄取——空枚举——+ R36-I 派单：`git diff 6891c74..5adb0f0 --stat` 恰 3 个 docs 文件（PROGRESS / PLAN / R36-F.md），`-- src/` **0 行**），已按令 `git merge --ff-only` 纯快进对齐并重验冻结（切片 42 文件 vs `4efee23` 与最强冻结 `89f0455..HEAD -- src/` 在新 tip 均 **0 行**）。官方三批测量在 `6891c74` 工作树完成，聚焦套件与 `pnpm gate` 在 `5adb0f0` 工作树完成——两 tip 间 `src/ test/ scripts/ package` 逐字节相同 ⇒ 全部结果位级同一有效。R36-H / R36-I 在飞，按令未等待、未触碰姊妹分支。
- **分支:** `cursor/r36-g-runtime-thirty-sixth-pass-83a1`
- **切片:** 恰 **42** 文件——`src/run/` 17（除 A 属地 `child-tracking.ts` / `gate-apply.ts`）+ `src/supervisor/` 4（除 B 属地 `model-router.ts`）+ `src/graph/` 4 + `src/domain/` 17；`wc -l` 合计 **8960** 行（与 R32-G / R33-G / R34-G / R35-G 记录一致），全部从零实读全文，未依赖前三十五轮记忆。
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R24-G…R35-G 同引擎位级相同，带可比；计时前 `which node` / `node --version` / `node -e process.versions.v8` 三项直证）。本 VM 系统默认 node 为 v22.14.0——按在案陷阱**开工即**显式钉 PATH 到 `~/.nvm/versions/node/v22.22.2/bin`，官方三批全部落在 v22.22.2 上，**零** v22.14.0 误跑、零弃置批次。`pnpm install --frozen-lockfile` 干净完成。

## 结论

**诚实的空枚举：第三十六遍没有可立 ID 的新更优解，不铸任何 S36-G-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不加 EXCLUSIONS 行，切片零代码改动。** 从零重读 8960 行（42 文件清单核对恰 42：run 17 + supervisor 4 + graph 4 + domain 17），逐文件枚举的全部消除角度经换名检查裁决为既有排除 ID 本体、姊妹归档面（禁移植）、plan-ban 轴名或全局计算顶之内的亚噪声——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（§3）。本轮恰 **1 项新站点级换名记录**，三重拒列：`domain/contract.ts` `validateRequirementContract` 溯源域中间数组物化（:97–101 `[...deliverables, ...constraints, ...acceptanceCriteria]`——全部 G 报告 + EXCLUSIONS rg 零先例命中；校验平面既有属主 + once-per-run ns–µs 拒收 + 计算顶内亚噪声，§3.0-1）。其余候选角度全部为已点名站点复命中（R35-G 两项换名记录——`flowchart-run.ts` 孪生 Map 物化 :168–176 与 `familyForFlowNode` :671–675——按令不重提、原形读码复核在位；R34-G §3.0-1 `validateJoin` `edges.some`（flowchart.ts:334）按令不重提、原形在位；`unexpectedKeys` 每调 Set、`findChild` 回退 Array.from 线扫、Kahn `queue.shift`、`expandTaskTransition` BFS、leaseReadyNodes 逐节点 computeStatus 重算、propagate 不动点、advanceLedgerRound spread 记账、planRound 死形参、S2-G-2 共享 visiting 本体第三十六遍复认——复命中即止，不重演独立复推导叙事）。R31-G 的 18 扇聚合门闭合审计按令不重铸。本轮边际动作仅两类，均不构成新面：(a) 缺省预算重锚（计算顶 **0.292–0.295 ms/run**，I/O 地板 **93.6–96.9 ms/run**，支配比 **~317–332×**，两个数量级支配关系不变；全部读数落派单默认对照带/历史包络内或轻度刻度侧偏，宿主刻度项非发现，§1 诚实注记）；(b) G 档案在案零剂量停放与重开条件的**不计时静态哨兵复核**（§2——方法属 R32-A 首创并已 plan-ban 重铸，此处按令仅作卫生性应用，不申报为新轴），全部哨兵**未触发**。14 格矩阵摘要 `06cbcf92c098c8f0…` 官方引擎三批 × 进程内 3 迭代 = **9 次逐位一致——第二十七轮同值，按令不铸摘要恒等 ID**。空枚举与 R12-G…R35-G、R36-A…R36-F 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 42 文件自生产基线 `4efee23` 逐字节零变化（派单 tip / 测量后 / 快进对齐后 / 提交前**四点**核对 `git diff 4efee23 -- <42 文件>` 均 **0 行**；`git diff --find-renames --name-status` 同区间**空输出**——零改名零增删，无改名裁决需做）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均 docs，本轮快进亦纯 docs）。聚焦套件 run/supervisor/graph/domain + integration：**tests 386 / pass 385 / fail 0 / skipped 1** 全绿（`5adb0f0` 树，v22.22.2，与 R31-G / R32-G / R33-G / R34-G / R35-G 记录逐字同计数）；`pnpm gate`（typecheck + lint + test + build）**全绿**——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A…R35-F / R36-A…R36-F 记录逐字同计数），退出码 0。临时基准文件在任何 gate 运行前删除，零门禁事故。不开 PR。

## 0. 范围与约束遵守（含不移植清单）

- 切片：`src/run/`（除 A 属地 `child-tracking.ts` / `gate-apply.ts`）17 文件 + `src/supervisor/`（除 B 属地 `model-router.ts`）4 文件 + `src/graph/` 4 文件 + `src/domain/` 17 文件，恰 **42**，全部实际重读全文。
- 读序遵令：README → EXCLUSIONS 全表 → PROGRESS → round-36/PLAN（G 段 + 全 ban 榜）→ R35-G 全文 → R36-A → R36-B → R36-C → R36-D → R36-E，之后才读 42 切片文件并动手；快进后补读新姊妹报告全文（R36-F.md，仅为划界与非移植清单，不据其改动任何测量或结论——`src/` 逐字节相同）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README.md 零触碰。
- **本轮六姊妹空枚举划界（零新轴可移植）**：R36-A（tracking 侧三项站点级换名记录已拒——types 解析回调闭包 / from-child `artifactIds` 防御拷贝 / roller 中间映射数组，全为 `src/tracking/` A 属地物理对象；本切片按令排除 `child-tracking.ts` / `gate-apply.ts`，G 侧零共享）；R36-B（live 路由侧**零**新站点级换名记录，残余角度均 R2-B 已点名站点复命中；S12-B-2 腿 (a) / S13-B-1 重取证未触发——B 属地；G↔B 唯一共享坐标为 `flowchart-run.ts` 的 learned-routing 消费腿，Live=R0 不变量 + S8-E-1 不去重 + 属地边界，零剂量，本清单原有）；R36-C（离线路由侧一项换名记录已拒——`routeR1` cheapest-`reduce` 折叠成本携带，`src/routing/r1.ts` C 属地物理对象；G 切片零 posterior/r1 消费，`flowchart-snapshot.ts` 对 `routing/catalog-model.js` 的唯一引用为 throw-only 校验桩 `snapshotValidationRouter` 零剂量）；R36-D（adaptation 侧四项站点级换名记录已拒——D 侧 parse/load 回调闭包镜像 / `freezeBaseline` 中间映射数组 / `validateChangeNote` 双循环融合 / `Object.values().filter` 布尔计数物化，全为 `src/adaptation/` 物理对象，G 侧零 adaptation 导入零交集）；R36-E（learning 侧**零**新站点级换名记录，六项候选全部复命中既有 E 属主行；G 侧共享坐标仍仅 `flowchart-run.ts:712` learned-routing 消费腿，零剂量，本清单原有）；R36-F（快进带入，experiments 侧空枚举且**零**新站点级换名记录——物理对象全在 `src/experiments/`，本轮 rg 直证 G 四目录**零** experiments 导入，零共享坐标）。六报告均未铸新面，非移植清单无需加行。
- **不移植禁令逐项核对**：M1 亲代 run 子调度格（R30-G）、M2 监督引擎裁决格（R29-G）、decision-gate resolution（R28-G）、join-policy（R27-G）、**RUNLIM / CKMIX / TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / ZREP**——本轮零剂量零重提；**BINDX**（切片 `Math.*` token 恰 **6 个**：hash.ts:16 `Math.abs` ×1、flowchart-supervisor.ts:606/:608 `Math.max` ×2、inspection.ts:111/:118/:140 `Math.max` ×3，与 R32-G…R35-G 计数逐一同址，rg 直证计数即止零 patch）；**MODEX**（切片 rg **零** chmod / 零 umask，声明即止）；I/O text-codec topology / per-run transcode-byte manifest（R31-E——切片显式 fs UTF-8 位点恰 **4 个**：checkpoint-store.ts:22/:31、pause-controller.ts:31/:92，与 R32-G…R35-G 记录逐一同址；event-store / episode-store 经切片外 `persist/jsonl.ts` 走同一坐标——E 属地，本轮零编解码剂量）；R31-G 18 门闭合审计（**不重铸「我又审了一遍门」**；摘要恒等第二十七轮不铸 ID）；R32-A 重开条件哨兵审计与 R32-B 闭合稳定性审计（**方法不重铸为面**——§2 按令仅应用于 G 自档案在案条目，不申报为新轴）；R33-A 44/44 A-账本方法（不重铸「我又查了一遍闭合稳定性」）；**R33-A…R35-J 空枚举 / R36-A…R36-F 空枚举**（方法卫生记录，不重演账本分类、不重提「我又查了一遍重开条件 / 门扉 / 未点名站点」）；**R35-G 两项换名记录**（`flowchart-run.ts` 孪生 Map 物化 :168–176 / `familyForFlowNode` :671–675——派单明令不重提，本轮读码复核原形在位即止）；**R34-G §3.0-1 换名记录**（`validateJoin` 逐 required-ref `edges.some` 线扫——派单明令不重提，本轮读码复核原形在位即止，flowchart.ts:334）；R31-B 六停驻门（cascade 升级臂证据 × 失败类格维持 routing 属地停放）；R31-E/F/H/I 各属地格全额维持；R28-I npm POSIX `.bin` symlink 静默 no-op 不「修复」。FITQ / SCALEX / YMIX / KFAN / NVG / SEEDX / SHAPEK / ORDX / CALLB / OCCX / JB / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / PAIRX / CSPELL / REPRX / LINKTOP / BINDX / MODEX 全部未编号未移植。S5-H-1、S7-F-1 ≠ S6-F-5、S9-D-4 / S12-D-1、S12-B-2、S13-B-1 属姊妹切片，未从 G 侧重开。
- **站位零剂量停放不重铸**：`planTaskTopology` X0-2（读码复核仍无 run loop 调用方，§2 哨兵第三十六次）；`settleSupervisedOutcome` A 属地 no-op（内部调用方 supervisor.ts:601/:711 均不带 assessment，无 assessment 即无门原形在位，§2 哨兵）。
- 未重开任何 X\* / S1-\* … S35-\* 条目。**单变量纪律**：本轮无任何计时新臂——重锚脚本与 R12-G 附录归档件**逐字节同一**（§1、附录），哨兵全部为静态 rg / 读码断言（不计时）。硬不变量零触碰：双 LCB + 双归因在位（切片侧证人：events.ts `isBehaviorDistribution` + flowchart-snapshot.ts `isOneHotDistribution` one-hot 校验原文在位）；Live = R0-equivalent（flowchart-run 的 learned 路由应用面未触碰，B 属地 model-router 未读改）；promotion proposal-first / 无 Outcome-supported 声称（Checkpoint F-PROD 仍开放，ADR-005）；无阈值变更；不改测试迁就重构；CAS / 凭据 / 数据面契约 / 公开签名不变（零 diff 天然满足）；fsync / 完整性再哈希在位（event-store 终态 fsync、checkpoint-store `handle.sync()`、pause-controller writeAtomic、episode-store 终态 fsync 全部原文在位）；fail-open 家族保持关闭（events.ts `validateEvent` 追加/重放双侧 fail-closed 原形）；S8-E-1 不去重 loadLearnedRouting（G 侧消费腿 flowchart-run.ts:712 零触碰）；catalog 行序契约（R28-B W3）零预排序；分析不变更在飞 run；tracking 无指挥权；H/score 不写 routing PASS/FAIL。无赢家 ⇒ 零生产改动、零新仿真文件入库。不开 PR。

## 1. CPU vs I/O 重锚（三批次官方运行，nvm Node v22.22.2，seed 仅标注批次）

归档基准从 `docs/reports/sota-opt/round-12/R12-G.md` 原样提取（```ts 围栏内 536 行 → `tmp-r36g-bench.ts`，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`，与 R13-G…R35-G 附录 A 记录值**逐字节一致**；无赢家按纪律不入库，临时文件核后即删——早于任何 gate 运行，零门禁事故）。官方三批 seed = 0xa35091 / 0xa35092 / 0xa35093；本轮无表征运行需求（全部读数落历史包络内或轻度刻度侧偏，无孤立高点需归因）。全部在 `6891c74` 工作树完成（快进增量纯 docs，两 tip `src/` 逐字节相同 ⇒ 测量形态与提交形态位级同一，全部有效）。

| 探针 | B1 | B2 | B3 |
| --- | --- | --- | --- |
| validateFlowchart 16n join4 | 9.136 µs | 9.227 µs | 9.228 µs |
| → ×32 轮计算顶 | **0.292 ms/run** | **0.295 ms/run** | **0.295 ms/run** |
| validateFlowchart 8n join2 | 4.539 µs | 4.698 µs | 4.921 µs |
| DeterministicJudge.decide 8×8 | 0.143 µs | 0.154 µs | 0.147 µs |
| expandTaskTransition | 0.483 µs | 0.469 µs | 0.489 µs |
| compileChildrenToFlowchart 16 子 | 8.491 µs | 8.649 µs | 8.948 µs |
| validateEvent AGENT_EVENT | 0.554 µs | 0.552 µs | 0.557 µs |
| replayRun 300 events | 0.681 µs | 0.688 µs | 0.661 µs |
| validateCheckpoint N=48 含 restore | 249.1 µs | 248.4 µs | 250.7 µs |
| CheckpointStore.write 持久（N=48） | 571.9 µs | 619.7 µs | 630.0 µs |
| → ×66 写/run | 37.7 ms | 40.9 ms | 41.6 ms |
| EventStore.append（无 fsync） | 68.4 µs | 71.7 µs | 67.6 µs |
| → ×300 事件/run | 20.5 ms | 21.5 ms | 20.3 ms |
| EventStore.readAll ×96/run 中期均幅 | 35.4 ms | 34.5 ms | 33.2 ms |
| EpisodeStore.append COMPLETED（fsync） | 357.5 µs | 314.7 µs | 317.5 µs |
| settle-close 终态命令算术 | 0.544 ms | 0.506 ms | 0.504 ms |

计算顶 = **0.292 / 0.295 / 0.295 ms/run**；I/O 地板合成 = **93.6 / 96.9 / 95.1 ms/run**；支配比 **~317–332×**（R35-G ~295–330× / R34-G ~237–322× 同为两个数量级，支配结论不变）。矩阵摘要 `06cbcf92c098c8f0…` 官方 3 批 × 3 迭代 = **9 次逐位一致（第二十七轮同值；14 格配置态 × 命令类矩阵逐格 I/O 计数零变化，不铸摘要轴/ID）**；start→COMPLETED 首跑冷墙钟 20.2 / 21.2 / 20.7 ms，事件尾 [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED] 不变。

**诚实注记**：(i) 本轮计算顶 0.292–0.295 与派单默认对照带 0.293–0.303（R35-G）低沿几乎重合（B1 0.292 低出 0.001，落 R34-G 表征带 0.292–0.301 之内）；I/O 地板 93.6–96.9 全部落派单默认带 87.0–100.0 之内；支配比高沿 ~332 轻越 R35-G 记载 ~330（由 B2 地板 96.9 / B1 计算顶 0.292 组合驱动，非任何单探针越带）——两个数量级支配关系不变。个体探针注记：EpisodeStore.append COMPLETED（fsync）314.7–357.5 µs 高于 R35-G 带 276.9–293.2，settle-close 0.504–0.544 ms 高于 R35-G 带 0.474–0.479——同为 fsync 腿同向轻度慢侧，判本 VM 宿主刻度项（冻结 0 行 + 摘要第二十七轮恒等 + 逐格 I/O 计数零变化 ⇒ 仪器/宿主现象非切片行为变化，按派单令「host-bias windows with freeze 0 lines are NOT findings」不作发现、对照带不改）。(ii) 计算顶按最保守实测 0.295 ms 执行亚噪声拒收门，距 ≥10 ms 落地线仍 **34×**，两个数量级，结论完全同向。(iii) 引擎纪律：PATH 在任何计时前钉死 nvm v22.22.2（`which node` / `node --version` / `process.versions.v8` 三项直证在案），全部批次官方引擎完成，零 v22.14.0 误跑、零弃置批次、零存档事故、零门禁事故（临时基准文件在任何 gate 前删除）。

## 2. G 档案在案零剂量停放 / 重开条件哨兵复核（全部不计时；方法应用，不铸面）

R32-A 首创的哨兵审计方法（plan-ban 重铸为面）此处按令仅作卫生性应用于 **G 自档案**的在案条目。全部哨兵为静态 rg / 读码断言，零计时臂：

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| X0-2（`planTaskTopology`） | run loop 或任何生产文件获得调用方 | `rg "planTaskTopology" src/` 除 supervisor.ts:64 定义自身外**空匹配**（第三十六次复核）⇒ **未触发** |
| `settleSupervisedOutcome` A 属地 no-op | 生产侧带 assessment 调用出现 | 调用方仅 supervisor.ts:601/:711（均不带 assessment）；函数原形 assessment 缺席即返回 ⇒ **未触发** |
| R30-G M1 格站位前提 | `startParentRun` 获得 CLI 调用方或 resume 门 | `rg "startParentRun" src/` 除 coordinator.ts:224 定义外**空匹配** ⇒ **未触发** |
| R29-G M2 格站位前提 | `startSupervisedRun` 获得 CLI 调用方 | CLI 仅动态 import `resumeSupervisedRun`（main.ts:1021，与历轮记录一致）⇒ **未触发** |
| 摘要恒等（不铸 ID） | 14 格矩阵任一格 I/O 计数或摘要变化 | 官方三批 9 次逐位同值 `06cbcf92c098c8f0…`（第二十七轮）⇒ **未触发** |
| R31-B cascade 停放门（G 侧宿主） | child-coordinator cascade 臂获得新证据面 | 冻结 0 行 + `maybeCascadeRetry` 原形（:382 调用 / :458 定义，与 R33-G…R35-G 记录同址）读码复核 ⇒ **未触发**（routing 属地维持） |
| R31-E 编解码坐标（G 侧四位点） | G 存储腿新增编解码剂量 | 显式 utf8 位点恰 4 个与 R32-G…R35-G 记录逐一同址，零剂量 ⇒ **未触发** |
| BINDX / MODEX 计数哨兵 | 切片 `Math.*` token 数或 chmod/umask 位点变化 | `Math.*` 恰 6 token 逐一同址；chmod/umask **0** 匹配 ⇒ **未触发** |
| S8-E-1 消费腿哨兵 | `loadLearnedRouting` G 侧消费腿被去重/移动 | flowchart-run.ts:712 原文在位（冻结 0 行直证）⇒ **未触发** |
| R34-G §3.0-1 换名记录原形 | `validateJoin` 内形变化 | flowchart.ts:334 `edges.some` 原文在位（冻结 0 行直证）⇒ **原形维持，按令不重提** |
| R35-G 两项换名记录原形 | 孪生 Map 物化 / `familyForFlowNode` 内形变化 | flowchart-run.ts:168–176 / :671–675 原文在位（冻结 0 行直证）⇒ **原形维持，按令不重提** |
| 签名/调用图类重开条件全体 | 要求 `src/` 变更 | 最强冻结 `89f0455..HEAD -- src/` **0 行**直接判空 ⇒ **全部未触发** |

## 3. 本轮换名检查与四十二文件检查表（第三十六遍枚举残余）

### 3.0 换名检查（一项新站点级记录拒列；其余全部为已点名站点复命中或按令不重提项）

从零重推（假装排除表不存在重读 8960 行）枚举出的候选角度经对表逐一查证（候选站点名对 R1-G…R35-G 全部 G 报告 + EXCLUSIONS 全表 rg 先例检索）：

1. **新记录（拒列）：`domain/contract.ts` `validateRequirementContract` 溯源域中间数组物化**（:97–101 `const provenanceFields = [...deliverables, ...constraints, ...acceptanceCriteria]`——纯为单遍溯源字段检查而物化一个合并数组，理论上可三数组直循环免物化）：rg 全部 G 报告 + EXCLUSIONS **零先例命中**（`provenanceFields` 零匹配），构成新站点级记录。三重拒绝——(i) **校验平面既有属主**：contract.ts 校验器族 = 校验平面既有排除行家族（R35-G §3.4 同文件行逐字点名「校验器族 = 校验平面既有排除行」），本站点为其真子集；(ii) **once-per-run ns–µs 拒收类**：`validateRequirementContract` 每 run 绑定期恰一次（episode-bind `contractFromObjective` / CLI contract 载入），三数组合计个位数条目，spread 物化 ns 域；(iii) **计算顶内亚噪声**：住在 0.292–0.295 ms/run 计算顶之内，顶本身距 ≥10 ms 落地线 34×。拒列，不铸 ID。
2. **按令不重提：`flowchart-run.ts` 命令入口孪生 Map 物化 `childResultsMap` / `childTaskMap`**（:168–176）= R35-G §3.0-1 换名记录本体——派单 DO NOT REMINT 明列，本轮读码复核原形在位即止。
3. **按令不重提：`flowchart-run.ts` `familyForFlowNode` 内形辅助**（:671–675）= R35-G §3.0-2 换名记录本体（所居外围站点 = S8-E-3 排除行本体 + Live=R0 毗邻）——派单明令不重提，本轮读码复核原形在位即止。
4. **按令不重提：`domain/flowchart.ts` `validateJoin` 逐 required-ref `edges.some` 线扫**（flowchart.ts:334）= R34-G §3.0-1 换名记录本体——派单明令不重提，本轮读码复核原形在位即止。
5. **复命中维持：`injection.ts` `unexpectedKeys` 每调 `new Set(allowed)` 构造**（:45）= R35-G §3.1 injection.ts 行已点名站点（校验平面亚噪声，once-per-CLI-inject）。复命中即止。
6. **复命中维持：`inspection.ts` `findChild` 回退线扫含 `Array.from(children.values())` 物化**（:161）= R35-G §3.1 inspection.ts 行已点名站点（once-per-run CLI 分析腿亚噪声）。复命中即止。
7. **复命中维持：`graph/validate.ts` Kahn `queue.shift()` 微形态**（:54）= R35-G §3.3 validate.ts 行已点名（16 节点 µs 级一次性构造内含）。复命中即止。
8. **复命中维持：`domain/state.ts` `expandTaskTransition` BFS**（0.469–0.489 µs 本轮实测）= 已定价亚噪声（全表预计算变体 = TERMCLASS 禁 + 亚噪声双拒）。复命中即止。
9. **复命中维持：`flowchart-supervisor.ts` `leaseReadyNodes` 逐节点 `computeStatus()` O(N) 重算** = R28-G 决策门格 + R33-G §3.2 已点名站点（计算顶内亚噪声，真实 N ≤ 16）。复命中即止。
10. **复命中维持：`supervisor/ledger.ts` `advanceLedgerRound` progress 数组 spread 全量记账** = R33-G §3.2 已点名站点（maxRounds=32 封顶 + facts 个位数规模）。复命中即止。
11. **本体复认（一行即止）：`failurePathCompletedGraph` 共享 `visiting` true-路径保留** = **S2-G-2 本体**——第三十六遍新鲜眼再次注意到同一性质，复认即止，不重演复推导叙事、不作新记录。
12. **复命中维持：`scheduler.ts` `planRound` 死形参 `_leaseDurationMs`** = R29-G 落选候选清单本体（公开导出签名硬禁 + ns 级）。不重提。

改名裁决（rename-check against baseline）：`git diff --find-renames --name-status 4efee23..HEAD -- src/run src/supervisor src/graph src/domain` **空输出**——切片自生产基线零改名、零增删文件，无改名归属需裁决。

### 3.1 `src/run/`（17 文件）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| coordinator.ts | `startParentRun` 仍无 CLI 调用方（§2 M1 哨兵）；M1 亲代 run 子调度格全维度 = R30-G 停放 | 无新候选 |
| supervisor.ts | M2 裁决格全维度 = R29-G；`planTaskTopology` X0-2 停放（§2）；`settleSupervisedOutcome` A 属地 no-op（§2）；`reconstructSupervisorState` 重放腿 = RUNLIM/CKMIX 既有属主 | 无新候选 |
| flowchart-run.ts | 孪生 Map 物化 + `familyForFlowNode` = R35-G 两项记录按令不重提（§3.0-2/-3，原形在位）；`loadLearnedRouting` 消费腿 :712 = S8-E-1 + Live=R0 零触碰；命令面 14 格矩阵摘要第二十七轮恒等 | 无新候选 |
| child-coordinator.ts | `ConcurrencyGate` / 重试 / cascade 臂 = R31-B 停放门宿主（`maybeCascadeRetry` :382/:458 同址，§2）；执行事件处理线扫 = 已点名域 | 无新候选 |
| injection.ts | 注入指令种类/校验 = R25-G 面全维度；`unexpectedKeys` 每调 Set = 复命中（§3.0-5）；`parseFactValue` try/JSON.parse = 校验平面亚噪声 | 无新候选 |
| flowchart-executor.ts | 每子执行一次的 for-await 事件折叠 = 执行器 I/O 支配（子代理进程级延迟 ≫ 折叠 ns）；`childNodeResultFromExecution` 分支树 = 契约面 | 无新候选 |
| checkpoint-store.ts | 临时文件 + rename + sync 原子写 = 持久契约；记录组成 = CKMIX；utf8 位点 = R31-E 坐标（§2）；`JSON.stringify(…, null, 2)` 美化 = 既有记录组成/编解码属地 | 无新候选 |
| scheduler.ts | LeaseRegistry / planRound / applyTaskOutcome = R29-G 格 + SCHEDWIN 禁；`_leaseDurationMs` 死形参 = 复命中（§3.0-12）；`expired()` 每 lease Date.parse = 个位数 lease 规模亚噪声；expandTaskTransition 0.469–0.489 µs 亚噪声 | 无新候选 |
| pause-controller.ts | writeAtomic fsync + EPERM/EEXIST/EACCES 回退 = 持久契约；utf8 位点 = R31-E 坐标（§2）；parsePauseToken fail-closed = 校验平面 | 无新候选 |
| episode-bind.ts | settle-close 终态算术 0.504–0.544 ms 本轮带内已锚（含 `events.some` 终态扫描——聚合已定价，融合收益亚噪声）；顺序 append 链 = 事件次序契约面；closure 裁决 = episode 姊妹属地；`hash32` 集中化 = 既有落地 | 无新候选 |
| replay.ts | 单遍 O(E) 状态机（0.661–0.688 µs/事件本轮实测）；`eventsLookLikeFlowchartRun` 单遍 some = R1-G 已点名；validateFlowchartCheckpointState 三段 try + restore 试跑 = fail-closed 契约本体 | 无新候选 |
| events.ts | `payloadError` 闭型 switch = 校验平面既有属主；`isBehaviorDistribution` one-hot 校验 = 双 LCB 硬保持项证人（§0）；追加/重放双侧 validateEvent = fail-closed 家族关闭维持 | 无新候选 |
| child-prompt.ts | lines 数组 + join 一次性构造 = 每子一次 µs 域（prompt 面 I/O 支配） | 无新候选 |
| inspection.ts | once-per-run CLI 分析腿 = 拒收类（一次性 CLI 噪声）；findChild 回退线扫（含 Array.from 物化）= 复命中（§3.0-6）；双遍事件扫（先答案后累积）= 已点名域 | 无新候选 |
| child-grounding.ts | `uniqueArtifacts` Set 去重 + packet 编译一次性 = 每子一次 µs 域；EMPTY_CONTRACT 模块级已提升 | 无新候选 |
| event-store.ts | 队列化 append + 终态 fsync = 持久契约（fsync 家族保持，§0）；readAll 逐行 validateEvent = fail-closed 双侧维持；readAll 单价 187.4–190.5 µs（100 事件）本轮带内 | 无新候选 |
| episode-store.ts | 同 event-store 同构（终态 fsync + 逐行 validateEpisode）= 持久契约 + 校验平面 | 无新候选 |

### 3.2 `src/supervisor/`（4 文件）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| flowchart.ts | 纯再导出（47 行 type/值转发）= 零剂量 | 无新候选 |
| ledger.ts | `advanceLedgerRound` spread 记账 = 复命中（§3.0-10）；`isDuplicateFact` O(F) 线扫 = facts 个位数规模亚噪声（已点名域）；createLedger 字面量 = 零剂量 | 无新候选 |
| flowchart-supervisor.ts | `propagate()` 不动点 + edgeStatus/joinStatus = join-policy 面（R27-G）；leaseReadyNodes 逐节点 computeStatus 重算 = R28-G 格复命中（§3.0-9）；`failurePathCompletedGraph` 共享 visiting = **S2-G-2 本体**（§3.0-11 复认）；structuredClone 防御拷贝面 = R31-G 门；waiter 不变式 = 契约 | 无新候选 |
| flowchart-snapshot.ts | 快照校验器族 = 校验平面既有属主（once-per-restore）；`isOneHotDistribution` = 双 LCB 证人（§0）；`snapshotValidationRouter` throw-only 桩对 catalog-model 的引用 = C 属地边界零剂量 | 无新候选 |

### 3.3 `src/graph/`（4 文件）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| judge.ts | DeterministicJudge 0.143–0.154 µs 本轮实测亚噪声；`evidenceIds.filter(includes)` 二次方微形态 = 证据个位数规模已点名域 | 无新候选 |
| compile-children.ts | 一次性编译 8.491–8.948 µs 本轮实测；per-child Set 查重 = 一次性 µs 域 | 无新候选 |
| validate.ts | Kahn 拓扑一次性构造（16 节点 µs 级，queue.shift 微形态含于内 = 复命中 §3.0-7）；确定序 = ORDX 禁 | 无新候选 |
| readiness.ts | `computeReadyTasks` / `allDependenciesSatisfied` 线扫 = R29-G 格既有域（N ≤ 16 亚噪声） | 无新候选 |

### 3.4 `src/domain/`（17 文件）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| flowchart.ts | validateFlowchart 9.136–9.228 µs 本轮实测 = 计算顶探针本体；`validateJoin` edges.some = R34-G 记录按令不重提（§3.0-4，原形在位 :334）；DFS 环检测 + edgePairs `\u0000` 键 = 已点名域 | 无新候选 |
| contract.ts | **溯源域中间数组物化 = 本轮唯一新站点级换名记录，三重拒列（§3.0-1）**；validateRequirementContract 校验器族 = 校验平面既有排除行 | 无新候选（记录已拒列） |
| ids.ts | 前缀表 + ID_SUFFIX_PATTERN 单例正则 = 已提升；startsWith+slice 校验 = ns 域 | 无新候选 |
| state.ts | expandTaskTransition BFS = 复命中（§3.0-8）；转移表 = TERMCLASS 禁 | 无新候选 |
| hash.ts | hash32 FNV 风格集中化 = 既有落地；`Math.abs` :16 = BINDX 计数位点（§2） | 无新候选 |
| timestamp.ts | ISO_PATTERN + Date.parse 双查 = 校验平面既有属主 | 无新候选 |
| episode.ts | validateEpisode 解构重建 = 校验平面既有属主（once-per-append） | 无新候选 |
| task.ts / run.ts / limits.ts / project.ts / evidence.ts | 校验器族 = 校验平面既有排除行；错误串构造仅失败路径 | 无新候选 |
| status.ts / roles.ts / record.ts / errors.ts / index.ts | 闭枚举表 / type-only / 再导出 = 零剂量或亚噪声 | 无新候选 |

## 4. 候选总表

**空。** 本轮零候选：哨兵复核（§2）按定义不产出候选（不计时、不剂量）；换名检查一项新记录三重拒列、三项按令不重提、其余全部命中既有属主（§3.0）——无一是可立项新站点；四十二文件枚举（§3.1–3.4）全部命中既有 ID 本体、姊妹归档面、plan-ban 轴名或计算顶内亚噪声。本轮无任何计时新臂 ⇒ 无可定价候选面。全局支配结构不变：计算面全体封顶 0.292–0.295 ms/run，距 ≥10 ms 落地线 34×（两个数量级）；I/O 面三腿 20.3–41.6 ms 全部契约或排除表保护。

## 5. CHECKS

- 分支恰 `cursor/r36-g-runtime-thirty-sixth-pass-83a1`，基线 `6891c74` → 快进对齐 `5adb0f0`（纯 docs：恰 3 文件 PROGRESS / PLAN / R36-F.md，`-- src/` 0 行）。
- 冻结核对：`git diff 4efee23 -- <42 文件>` 派单 tip / 测量后 / 快进对齐后 / 提交前均 **0 行**；`--find-renames --name-status` 同区间空输出（零改名）；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**。
- 切片清单：恰 42 文件（17+4+4+17），`wc -l` 合计 8960 行（与 R32-G / R33-G / R34-G / R35-G 记录一致），全部实读。
- 重锚脚本与 R12-G 附录归档件 sha256 逐字符同一（`9d46a829…b9943`，536 行）；官方三批全部 nvm v22.22.2 / V8 12.4.254.21-node.39；零 v22.14.0 误跑零弃置（PATH 计时前钉死）；临时基准文件门禁前删除，零门禁事故。
- 摘要恒等：`06cbcf92c098c8f0…` 官方 9 次逐位一致（第二十七轮同值，不铸 ID）；事件尾不变。
- 哨兵普查（第三十六次）：X0-2 / A 属地 no-op / M1 / M2 站位、cascade 停放、编解码坐标 4 位点、MODEX 零位点、BINDX 6 token 逐一同址、S8-E-1 消费腿 :712、R34-G §3.0-1 原形、R35-G 两项记录原形——全部未触发（§2）。
- 换名检查：一项新站点级记录三重拒列（contract.ts 溯源域中间数组物化 §3.0-1——rg 全 G 报告 + EXCLUSIONS 零先例命中）；三项按令不重提（R35-G 孪生 Map / familyForFlowNode + R34-G validateJoin edges.some，原形均在位）；复命中八项维持既有属主——零新 ID、零重开。
- 姊妹划界：R36-A/B/C/D/E（派单已录）+ R36-F（快进带入）六轴全为各属地物理对象，G 侧共享坐标仅 flowchart-run.ts:712 learned-routing 消费腿（S8-E-1 + Live=R0，本清单原有，零剂量）与 flowchart-snapshot.ts 的 throw-only catalog 校验桩（C 属地边界零剂量）；experiments 侧零共享（G 四目录零 experiments 导入，rg 直证）——零新轴可移植（§0）。
- 聚焦测试：run/supervisor/graph/domain + integration = **386 tests / 385 pass / 0 fail / 1 skipped**（`5adb0f0` 树，v22.22.2，与 R31-G / R32-G / R33-G / R34-G / R35-G 逐字同计数）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿**（`5adb0f0` 树）——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A…R35-F / R36-A…R36-F 逐字同计数）；退出码 0。
- 仓库改动恰本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；Live=R0-equivalent 在位；proposal-first 在位；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不变；fsync / 完整性族在位；fail-open 家族保持关闭；S8-E-1 不去重维持；分析未变更任何在飞 run；不开 PR。

## 6. 重开条件

本轮无新定价轴，不加行。X0-2、A 属地 no-op、R30-G/R29-G 站位前提、R31-B cascade 停放、R31-E 编解码坐标、CKMIX/S8-G-2、S2-G-2 等既有重开条件全额维持原文；本轮 §2 哨兵直证全部未触发。fsync 腿轻度慢侧（EpisodeStore.append COMPLETED 314.7–357.5 µs / settle-close 0.504–0.544 ms，§1 注记 i）不构成重开触发——冻结 0 行、摘要第二十七轮恒等、逐格 I/O 计数零变化 ⇒ 宿主刻度项。

## 7. 新增排除 ID

**无。** 空枚举轮不铸 S36-G-\* ID；不取绰号、不作轴名、不加 EXCLUSIONS 行。换名检查一项新站点级记录见 §3.0-1（拒列），归档备后续轮次引用，无需并入 EXCLUSIONS.md。

## 附录 A：重锚基准出处与关键输出

脚本与 R12-G 附录 A 归档件**逐字节相同**：自 `docs/reports/sota-opt/round-12/R12-G.md`（```ts 围栏内 536 行）原样提取为仓库根 `tmp-r36g-bench.ts` 运行，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`（与 R13-G…R35-G 附录 A 记录值一致，逐字节可复现；无赢家按纪律不入库，临时文件核后即删——早于任何 gate 运行）。运行方式：`npx tsx tmp-r36g-bench.ts <seed>`（nvm Node v22.22.2，PATH 前置 `~/.nvm/versions/node/v22.22.2/bin`），官方三批 seed = 0xa35091/92/93（seed 仅标注批次，全部数据确定性构造）。关键输出：

```
--- Node v22.22.2 / V8 12.4.254.21-node.39 (canonical) ---
validateFlowchart 16n join4: 9.136 / 9.227 / 9.228 us -> x32 = 0.292~0.295 ms/run
validateFlowchart 8n join2: 4.539 / 4.698 / 4.921 us
DeterministicJudge.decide 8x8: 0.143 / 0.154 / 0.147 us
expandTaskTransition PENDING->FAILED: 0.483 / 0.469 / 0.489 us
compileChildrenToFlowchart 16 children: 8.491 / 8.649 / 8.948 us
validateEvent AGENT_EVENT: 0.554 / 0.552 / 0.557 us
replayRun 300 events: 0.681 / 0.688 / 0.661 us
validateCheckpoint full (N=48, incl. restore): 249.1 / 248.4 / 250.7 us
CheckpointStore.write durable (N=48): 571.9 / 619.7 / 630.0 us -> x66 = 37.7 / 40.9 / 41.6 ms/run
EventStore.append AGENT_EVENT (no fsync): 68.4 / 71.7 / 67.6 us -> x300 = 20.5 / 21.5 / 20.3 ms/run
EventStore.readAll 300/100 events: 504.2~548.9 / 187.4~190.5 us -> x96 = 35.4 / 34.5 / 33.2 ms/run
pause token() ENOENT / present: 20.4~21.2 / 53.8~54.8 us
loadLearnedRouting (no snapshot): 23.3 / 22.9 / 22.8 us
EpisodeStore.readAll (2 snapshots): 55.6 / 57.6 / 56.9 us
EpisodeStore.append COMPLETED (fsync): 357.5 / 314.7 / 317.5 us
EpisodeEventStore.append (no fsync): 62.6 / 62.3 / 61.8 us
settle-close arithmetic: 0.544 / 0.506 / 0.504 ms per terminal command
matrix digest (3 proc x 3 iter): 06cbcf92c098c8f0... 九次逐位一致（14 格；与 R10-G..R35-G 同值，第二十七轮）
start->COMPLETED wall-clock: 20.2 / 21.2 / 20.7 ms; returned events tail = [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED]
```

（引擎与存档纪律注记：PATH 在任何计时前钉死 nvm v22.22.2，零 v22.14.0 弃置批次；官方三批存档自始完整，零截断零重跑；临时基准文件在任何 gate 运行前删除，聚焦套件与官方 gate 均一次通过。）

---

**MORE_OPTIMA=no** —— 第三十六遍复查空枚举收口：计算顶 vs I/O 地板支配比 ~317–332× 两个数量级不变；14 格矩阵摘要第二十七轮同值；G 档案在案停放/重开哨兵全部未触发；换名检查一项新记录三重拒列、三项按令不重提、其余全部命中既有属主；42 文件逐条消除角度全部命中既有属主。无达门槛新更优解。

MORE_OPTIMA=no
BRANCH=cursor/r36-g-runtime-thirty-sixth-pass-83a1
