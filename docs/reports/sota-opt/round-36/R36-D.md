MODEL_SLUG=claude-fable-5-thinking-xhigh

# R36-D：adaptation 切片 Round 36 三十六搜报告

**战役:** 全库持久 SOTA 优化 Round 36 / R36-D（十区之一，R1-D…R35-D 的第三十六遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `fead867`（派单 tip，R36-A 摄取——诚实空枚举——+ 本 R36-D 派单）。测量与门禁全部在 `fead867` 工作树完成；工作期间 exclusive **两次纯 docs 快进**：(一) 提交前 fetch 检测到 `fead867` → `83c3429`（经 `bf0a146`：R36-B 摄取（空枚举）+ R36-E 派单 + PLAN 表状态更新——`git diff fead867..83c3429 --stat` 恰 3 个 docs 文件（PROGRESS / PLAN / R36-B.md），`git diff fead867..83c3429 -- src/` **0 行**）；(二) 最终提交前二次 fetch 又检测到 `83c3429` → `a0fd7ed`（R36-C 摄取（空枚举）+ R36-F 派单 + PLAN 表状态更新——`git diff 83c3429..a0fd7ed --stat` 恰 3 个 docs 文件（PROGRESS / PLAN / R36-C.md），`git diff 83c3429..a0fd7ed -- src/` **0 行**）。两次均已按令执行**纯快进 rebase**（本分支无本地提交，`git merge --ff-only` 干净快进）并各重验冻结（切片 vs `82bef36` 与最强冻结 `89f0455..HEAD -- src/` 在每个新 tip 均 **0 行**，`wc -l` 仍 3294）；三 tip 间 `src/` 逐字节相同 ⇒ 测量与门禁结果原样携带，无需重测。已通读新姊妹报告 **R36-B** 并折入非移植清单（其枚举尾部两残余角度——assign/assign-plan 每批 id 双 map/双 Set、live-cascade `stay` 闭包——均为 B 区已点名站点复命中，零新站点级记录，全部住 B 物理对象，与 D 零共享；其 S12-B-2 腿 (a) / S13-B-1 重取证结论与本报告 §3 D 侧独立哨兵完全一致）；随后通读新姊妹报告 **R36-C** 并折入非移植清单（诚实空枚举，一项新换名记录——r1 `routeR1` cheapest `reduce` 折叠成本携带（decorate-carry）——已按 X1-4 具名成本池本体 + shadow-only 通道亚 µs 上界 + 家族先例（S2-H-5 / S3-B-2）三重拒列不铸 ID；其全部站点级对象住 C 区离线路由 9 文件，与 D 零共享物理对象）。两次快进 diff 中 PLAN 的 D 段均仅状态行 + 非移植清单先后补 R36-B / R36-C，约束无实质变更。R36-E / R36-F 在飞，按令未等待、未触碰。
**分支:** `cursor/r36-d-adaptation-thirty-sixth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R13-D…R35-D 同引擎线，带直接可比；`/exec-daemon` PATH 暴露的系统 Node v22.14.0 已按 R32-D §1(iii) …R35-D 在案陷阱**开工即预防性绕开**——PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 后 `which node` / `node --version` / `process.versions.v8` 三复核，官方五运行**全部**带内自报 `node v22.22.2`，本轮**零弃置批次**；tsx 4.23.12 经仓内 node_modules，pnpm 10.17.1，`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S36-D-\* ID，不铸新 face 名、不取 ALLCAPS 绰号、不加 EXCLUSIONS 行（全表仍 545 行），切片零代码改动；eval 地板经五次独立官方运行复测成立（要求 ≥3），本 VM 主锚与派单默认对照带交叠且低沿回落（S0c 官方五运行 **3.48–3.83 ms**——vs R35-D 3.54–3.93 交叠偏快侧；runs 1/4/5（3.50/3.57/3.48）落 R29-D…R33-D 历史并集带（3.29–3.67）内，runs 2/3（3.73/3.83）高出并集上沿 0.06–0.16 ms（~2–5%）但仍低于 R35-D 高尾 3.93、远低于 R34-D 主机高偏窗 3.65–4.70。冻结 0 行 + 引擎位级同一 + 脚本逐字节同一 ⇒ 残余高尾属宿主仪器现象，按派单令「host-bias windows with freeze 0 lines are not new findings」不作发现）。** R31-D 的元闭合审计（35 行坐标表）本轮第三十六遍从零独立重推**无一行松动**（§2）；R36-A / R36-B / R36-C 姊妹收口折入后亦无新增未审计平面泄入 D（三报告全部站点级记录分别住在 tracking、live 路由与离线路由物理对象内，与 D 零共享物理对象；R36-A 记录 1 / 记录 3 在 D 侧的镜像面为本报告 §0 记录 1 / 记录 2，均显式裁决拒列；R36-C 唯一拒列记录住 C 区 r1.ts）。本轮从零重读 14 文件（`wc -l` 合计 **3294** 行，清单核对恰 14 文件，与 R33-D / R34-D / R35-D 逐数一致），逐文件枚举的全部消除角度经换名检查裁决为既有排除 ID 本体、姊妹归档面（禁移植）或 plan-ban 轴名——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（§4；本轮 **4 项新站点级换名记录**见 §0，全部拒列——其中记录 1 / 记录 2 为折入 R36-A 时显式裁决的 D 侧镜像面）。本轮边际动作仅两类，均不构成新面：(a) 地板再锚定（五官方运行 `ALL CHECKS GREEN`，脚本为 R31-D 附录归档件的逐字节重跑）；(b) D 档案在案重开条件与条件陆器的**不计时静态哨兵复核**（§3——方法属 R32-A 首创并已 plan-ban 重铸，此处按令仅作卫生性应用），全部哨兵**未触发**。空枚举与 R12-D…R35-D、R36-A、R36-B、R36-C 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 14 文件自 `82bef36` 逐字节零变化（派单 tip `fead867`、测量后、两次快进（`83c3429` / `a0fd7ed`）后、提交前**五点核对** `git diff 82bef36 -- src/adaptation/` 均 **0 行**）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均 docs；两段快进 `fead867..a0fd7ed` 亦纯 docs）。聚焦 adaptation 测试 **99/99 绿**（tests 99 / suites 10 / fail 0）；`pnpm gate`（typecheck + lint + test + build）**全绿**——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D…R35-D / R36-A / R36-B / R36-C 记录逐字同计数），退出码 0。不开 PR。

## 0. 范围与约束遵守（含不移植清单与换名检查）

- 切片：`src/adaptation/` 全树恰 14 文件（promotion-rules / registry / eval-routing / promotion / active-pointer / monitor / retirement / approval-profile / resource / mutate / reflection / rollback / candidate / pareto），全部实际重读，未依赖前三十五轮记忆。
- 读序遵令：README → EXCLUSIONS 全表（545 行，X0-1 … S13-B-1 全部实读）→ PROGRESS → round-36/PLAN（D 段 + 具名/unnamed 全 ban 榜）→ R35-D 全文（默认对照）→ R36-A 全文（已合入姊妹折入非移植）；第一次快进后补读 **R36-B 全文**、第二次快进后补读 **R36-C 全文**（新姊妹先后折入非移植）与两版 PLAN 快进 diff（D 段均仅状态行 + 非移植清单先后补 R36-B / R36-C，无实质变更）。之后才读 14 切片文件并动手。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README.md 零触碰。
- **不移植禁令逐项核对**：approval-profile constraint-list geometry（R30-D——零剂量零重提）；**SIDEC / PAIRX / CSPELL / ZREP**（J/C 区收口面，未编号未移植）；R22-D 所有权单元格（`monitor.ts` `windowSize × calibrationDelta`，零生产调用方——**不重铸为新 D 面、不新停放**，本轮仅以静态哨兵复核其属地前提第三十六次成立，§3）；R32-A 重开条件哨兵审计（**方法不重铸为面**——§3 按令仅作卫生应用）；R32-B 闭合稳定性审计与 R33-A A 账本 44/44 卫生（A/B 区方法记录，不重走、不在 D 重演账本分类、不铸「再查一遍闭合稳定性」式新面）；**R33-A … R35-J 空枚举**；**R35-D 四项换名记录**（canAutoPromote 预算/计数整数重验 / mutate 双 trim / reflection 复合串键换嵌套 Map / D 侧 parse 腿空串卫 ASCII 首字符卫镜像——本轮全部复核为**在手已点名站点复命中**，维持拒列不再单列）；R34-D 三项与 R33-D 三项换名记录（rollback `knownTargets.some` 死检查 / `parsePromotionReview` 双层校验 / `loadAdaptationRegistryOrNew` 正则探针提升 / promote 链 hash 三算 / eval mkdir 每写 / `RollbackLog.list()` 每读防御拷贝——全部原文在位复命中维持）；**R36-A 空枚举**（其 3 项换名记录——types.ts parse 路径每调用回调闭包提升 / from-child artifactIds 防御拷贝省略 / roller `new Set(map)` 中间映射数组消除——全部 A 区物理对象；记录 1 与记录 3 在 D 侧的镜像面为本节换名记录 1 / 记录 2，显式裁决拒列不重铸；记录 2 的 D 侧镜像面为防御拷贝家族既有已点名行（X4-2 / S1-F-3 族经 R33-D §0 记录 3 + rollback evidence 拷贝群），复命中即止）；**R36-B 空枚举**（快进折入；零新站点级记录，两残余角度均 B 区已点名站点复命中、一边缘站点归 R27-B 封禁面属地；全部 B 物理对象，零 D 可移植物；其 S12-B-2 腿 (a)（`rg -c 'prior' eval-routing.ts` = 0）与 S13-B-1 重取证结论与本报告 §3 D 侧独立复核逐项一致）；**R36-C 空枚举**（第二次快进折入；一项新换名记录——r1 `routeR1` cheapest `reduce` 折叠成本携带 decorate-carry——按 X1-4 具名成本池本体 + shadow-only 通道亚 µs 上界 + 家族先例（S2-H-5 / S3-B-2）三重拒列不铸 ID；其站点住 C 区 r1.ts 离线路由面，与 D 零共享物理对象，声明即止，不移植、不重做、不铸新轴）；**MODEX**（R31-J——切片 grep **零** chmod / 零 umask / 零八进制 mode 字面量，计数即止）；**BINDX**（R31-C——切片 `Math.*` token 恰 **4 个**：monitor.ts:150/:179/:223 + reflection.ts:103，全部住在零流量休眠模块内，本轮 rg 重数逐点一致，计数即止零剂量）；I/O text-codec topology（R31-E——切片 fs UTF-8 编解码位点恰 **4 个**：promotion.ts:183/:229、eval-routing.ts:203/:248，本轮 rg 重数逐点一致，已由 S0/S0c 锚点整体定价，不重铸）；**REPRX**（R30-C——切片 `JSON.parse` 输入位点恰 4 个：promotion.ts:193、eval-routing.ts:218/:258、candidate.ts:149，本轮 rg 重数逐点一致，全部住在 load/eval/边界闸腿、已由锚点与 S12-D-1 永冻裁决覆盖，计数即止）；R31-A injected-callable 停放门（`RegistryOptions.now/generateId` 注入缝默认臂在位读码复核，registry.ts:78–79 `?? nowIso` / `?? (() => createResourceVersionId())` 原文在位 = R26-D 已剂量，零计时臂）；R31-B/R31-F/R31-G/R31-H/R31-I 各属 B/F/G/H/I 停驻或审计记录（零共享物理对象）；R32-J 六残余坐标（D 侧持久工件仅 registry.json / eval 报告两类，全部经既有锚点定价，零重铸）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / PWL / LINKTOP 全部未编号未移植、未写入 EXCLUSIONS 表行。
- **S9-D-4 / S12-D-1 永不再开**：读码复核两站点原形在位——eval-routing.ts:237 逐键 `toLowerCase` 对 Set 查询（健全形态，无廉价 skip）；candidate.ts `assertSingleResourceBoundary`（L144–167）无字面量子串预探针，trim 首字符卫（`{`/`[`）后直入 JSON.parse。零触碰。
- **换名重提检查**（本轮枚举全部新角度的逐项裁决，一律不铸 S36-D-\* ID；以下 **4 项为本轮新站点级记录**——均为在手报告未逐字点名过的站点/镜像面，裁决同族同向；若与更早轮次某条未在手记录重合，则为复命中，结论不变）：
  1. **D 侧 parse/load 腿每调用回调闭包提升镜像**（折入 R36-A 记录 1 时显式裁决的 D 镜像面：promotion.ts:320 `asStringArray` 的 `(item) => typeof item !== "string"` 谓词 + `parseRegistrySnapshot` 三个带 index 映射闭包（L253 activeVersionIds / L273 retiredVersionIds / L282 contents）+ rollback.ts:182 `asStringArray` 谓词，各自每调用新铸，可提升为模块级常量）= **S7-J-3 回调外提家族 + R36-A 记录 1 镜像家族第二区**；D 侧全部站点住在 once-per-command load/parse 腿（S0c load 锚 0.57–0.62 ms 已整体定价），逐回调 ns 级，拒列；
  2. **monitor.ts:132–135 `freezeBaseline` 四处 `new Set(window.map(...))` 中间映射数组消除**（改直接 add 循环免四次中间数组分配）= **R36-A 记录 3 镜像面**（S7-A-2 中间拼接/映射数组家族）在 D 的对应站点 + **休眠模块零流量**（R22-D 属地，本轮 §3 哨兵直证零切片外调用方第三十六次成立）+ **X2-5 本体毗邻**（每 observe 基线重建已否决——本角度是其真子集的分配层）；拒列；
  3. **promotion-rules.ts:35–47 `validateChangeNote` evidence/guardrails 双 for 循环融合**（两循环谓词逐字相同，可融合或 every 化）= **S2-H-3 / S1-F-4 多遍融合真实规模噪声族 + S1-D-8 小表家族**；表长个位数字符串、once-per-promote（S0d promote 锚 40.9–45.8 µs 已整体定价），逐调用 ns 级，拒列；
  4. **monitor.ts:177 `evaluateWindow` 的 `Object.values(axes).filter((axis) => axis).length` 布尔计数物化消除**（换直接布尔求和免一次数组 + 一次 filter 分配）= **休眠模块零流量**（R22-D 属地）+ **S1-A-4 / S6-J-3 融合-计数同族**（filter→单遍计数已在 A/J 区拒）；分配级，拒列。
- 其余枚举角度全部落在既有 ID **本体**上（非换位点，无需新记录）：routing-policy promote 链 hash 三算（promotion-rules.ts:116 + registry putContent）= R33-D §0 记录 1 维持（S1-D-7 本体 + X0-4/S5-D-5 族 + S0d 锚整体定价）；eval 报告写路径 mkdir 每 eval（eval-routing.ts:201）= R33-D §0 记录 2 维持（S10-D-1 同族 + X1-1）；`RollbackLog.list()` 每读防御拷贝 = R33-D §0 记录 3 维持（X4-2/S1-F-3 族）；rollback `knownTargets.some` 死检查 = R34-D §0 记录 1 维持；`parsePromotionReview` parse 体与末行 `validatePromotionReview` 双层校验 = R34-D §0 记录 2 维持（S3-G-2/S2-D-5/S4-D-4 族）；`loadAdaptationRegistryOrNew` L212 正则字面量提升 = R34-D §0 记录 3 维持（X0-6 邻域 + 错误消息契约面）；canAutoPromote 预算/计数整数重验 = R35-D §0 记录 1 维持；mutate 双 trim = R35-D §0 记录 2 维持；reflection `partitionEvidence` 复合串键 = R35-D §0 记录 3 维持；parse 腿空串卫 ASCII 首字符卫镜像 = R35-D §0 记录 4 维持；rollback CAS 后冗余 `versionsById.get`（registry.ts:294）= S2-D-2；双 parseTaskId（eval-routing.ts:292/:349）= S2-D-3；四遍 O(E) 融合 = S12-D-2（真尺度稳定负优化维持）；catalogCost find = S1-D-4；`assertNoForbiddenFields` entries→keys = S4-D-5；roots 去重/构造融合（eval-routing.ts:333）= S3-D-3 / S9-D-2；parseEpisode 尾部条件展开 / 形状单态化 = S8-D-5 / S9-D-1；报告尾链 = S7-D-2；load 链 parse→restore 融合 = S11-D-2；parseRollbackLedgerEntry 外层再拷贝 = S3-D-4；载入链双拷贝 = S4-D-2 禁去；restore/getActiveContent 再哈希 = S8-D-1 / S8-D-2 禁去；saveAdaptationRegistry 去 fsync = S8-D-3 禁去（promotion.ts:230 `handle.sync()` 在位）；临时名 UUID = S7-D-3（promotion.ts:226）；parseLedgerEntry 尾部条件 spread = S4-D-1；美化 JSON = S4-G-6 族磁盘数据面；`reconstructPromotion` 反向早退 = S1-D-3；`candidatesFor` 线扫 + `Array.from(values())`（含 snapshot() 的 `Array.from(entries()).map` 两遍形）= S1-D-2 / S1-D-5；addVersion 拷贝追加（registry.ts:636）= S1-D-1 / R21-D；ledger() 拷贝丢弃（promote 路径 begin 返回件弃用）= S3-D-1；保存链中间 snapshot() 对象 = S5-D-3；preparePromotion 第二次 review.candidateId 检查（L581 对 L571 死重）= S9-D-3 防御纵深保留；restore() 头部 `rollbackLog.restore([])` 预清除（L492 对 L531）= S8-D-4 禁去；restore() 逐 blob 内容再哈希（L497）= S8-D-1 禁去；restore() 对解析器产物的 id 再校验 = S5-D-1 禁去；restore versionsByKey 批量重建 = S2-D-1；parseRegistrySnapshot identity intern = S6-D-4；rerunHash 换更快序列化 = S6-D-5；pendingByIntent 在飞线扫（registry.ts:600–606）/ 小表 includes（isPromotableStatus 3 元 / ROLLBACK_REASONS / RESOURCE_KINDS / CANDIDATE_STATUSES / canAutoPromote 链 / validateApprovalProfile 嵌套 includes）= S1-D-8 + S4-D-4（默认档案跳校验已拒）；paretoFront O(n²) + 尾 spread（`[...front].sort`）= S1-D-6 / S3-D-2；rollback 幂等 last() 探针前置 = S7-D-4 禁去；`identityEquals` 守卫消除 = S11-D-1；retire/assertAssignable 前置 isResourceVersionId 守卫 = S6-D-1；beginPromotion 重复意图守卫 = S6-D-2 保留；monitor `report()` 每 observe 重建基线（含 `slice(0,windowSize)`/`slice(-windowSize)` 分配）= X2-5 本体 + monitor snapshot 逐项再验/再拷贝 = S2-D-5 族（零流量休眠对象）+ report() 死 emptyAxes 分配 = S3-D-5；`escapeRegExp` 每调用铸 RegExp（mutate.ts:224/:232）= 零流量 + X0-6 邻域；`isSelfSupported` 线性去重 = 个位数评估者 + S1-D-8 族；commitPromotion/appendLedger 条件 spread = S4-D-1 族；evalRoutingPolicy registry/dataset 载入重叠 = S4-D-3；全 UNOBSERVED 前置短路 = S5-D-4；pairedRecords entries() 换索引循环 / 冗余 ?? 回退 = S5-D-2 / S7-D-5；rollback() resourceIdentityKey CSE = S5-D-5；candidate==baseline 跳过第二次 assignTasks = S10-D-2；每写 mkdir 省略 = S10-D-1；preparePromotion putContent 重存 = S7-D-1 永冻；asRecord/asArray/asStringArray 私有重复（promotion.ts / rollback.ts 两套）+ rollback.ts `validateIdentity` 与 parseIdentity 重复 = X0-5（错误消息被测试断言）。
- 未重开任何 X\* / S1-\* … S35-\* 条目。**单变量纪律**：本轮无任何计时新臂——地板脚本与 R31-D 附录归档件**逐字节同一**（§5 附录）；哨兵全部为静态 grep / 读码断言（不计时）。硬不变量零触碰：双 LCB + 双归因在位（切片侧证人：promotion-rules.ts:125–129 拒收 production-labeled comparison 原文在位）；proposal-first 在位（approval-profile `defaultMode: "proposal-first"` 字面量 ×2 + 默认/installed budget 均 0，读码复核）；无阈值变更（`REPLAY_COMPARISON_CONFIG` 冻结原文在位，eval-routing.ts:41–44）；fsync 在位（promotion.ts:230）；fail-open 家族保持关闭；CAS / 凭据 / 数据面契约 / 公开签名不变（零 diff 天然满足）；分析不改 in-flight run、Tracking 无命令权、H/score 不写路由 PASS/FAIL、Live=R0 等价（R1 未接线 live）同样天然满足。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动、零新仿真文件入库。不开 PR。

## 1. 地板复测（S0 / S0c / S0d / S-late；官方五运行区间，方法第 2 步）

脚本与 R31-D 附录归档件**逐字节同一**（从 R31-D.md 附录 L176–L673 提取，sha256 复核 `0862c6b6f96c7734d340ce931621fc6e5be227917806ff1a9bfde65145a43e41`、498 行，与 R31-D…R35-D 归档值逐字符相同）——夹具构建器、种子（0xd30d01/02/03）、bench 形态、S-late 进程相位全部与 R31-D…R35-D 严格同形，带直接可比。

| 锚 | 本轮官方五运行区间 | 对照 |
| --- | --- | --- |
| S0 默认态 load / save(+fsync) / eval-e2e(E=200,toy) | 0.10–0.13 / 0.52–0.64 / 4.15–4.28 ms | R35-D 3.99–4.45 / R33-D 4.14–4.63 / R32-D 4.03–4.22——交叠居中 |
| S0c 配置态（reg=146.2KB, policy=9.2KB）load / save / eval-e2e | 0.57–0.62 / 1.39–1.64 / **3.48–3.83 ms** | R35-D 3.54–3.93（派单默认对照，交叠偏快侧）/ R34-D 3.65–4.70（主机偏高，本轮明确回落）/ R33-D 3.42–3.67 / R32-D 3.37–3.60 / R31-D 3.38–3.64 / R30-D 3.29–3.66 / R29-D 3.45–3.67；runs 1/4/5（3.50/3.57/3.48）落历史并集带内，runs 2/3（3.73/3.83）高出并集上沿 0.06–0.16 ms（注记 (i)） |
| S0d op 锚（K=60）promote / rollback / hash32(9.2KB) | 40.9–45.8 µs / 3.0–3.3 µs / 9.2–10.6 µs | R35-D 42.3–46.3 / 2.9–3.4 / 9.3–11.1（三口径全交叠）；R33-D 40.2–45.4 / 2.9–3.2 / 9.4–10.0 |
| S-late 同进程靠后 load / eval-e2e | 0.59–0.66 / **3.20–3.50 ms** | R35-D 3.36–3.79 / R31-D 3.19–3.68 / R33-D 3.19–3.55（交叠且低沿更快）；R34-D 4.07–4.76（主机偏高，本轮回落） |

**诚实注记**：(i) 本 VM 相对 R34-D 高偏置 VM **整体回落**且相对 R35-D 轻度偏快（S0c 低沿 3.48 < R35-D 低沿 3.54，高沿 3.83 < R35-D 高沿 3.93），主锚三读回到 R29-D…R33-D 历史并集带内，残余轻度高尾（runs 2/3 高出并集上沿 ~2–5%）为宿主尾波：冻结 0 行（切片与全 src/ 两级五点核对）+ 引擎位级同一（v22.22.2 / V8 12.4.254.21-node.39 五次带内自报）+ 脚本逐字节同一（sha256 逐字符）⇒ 唯余宿主变量；同型先例 R33-B / R33-C / R34-D §1(i) / R35-D §1(i) 在案，按派单令不作发现、不铸轴。锚间关系全序五次运行全保持：**S0c < S0 toy**（5/5）；**promote > hash32 > rollback**（5/5）；无孤立悬崖，5/5 运行 `ALL CHECKS GREEN`。地板结论以带间关系为准：**配置态 S0c 仍低于默认态 toy 锚、once-per-command ms 量级、支配论证全额成立**——µs/ns 级候选相对 ms 级地板的拒收裁决方向不变，不构成重开触发。(ii) 官方五运行零弃置批次：开工即按在案陷阱预防性 PATH 前置 nvm bin，首运行前三复核引擎，5/5 带内自报 v22.22.2。(iii) 脚本内含的 R31-D W-M1–W-M4 语义见证（monitor 配置平面 fail-closed / 剂量判定 / 单轴隔离 / 快照不含配置）随脚本五次重跑**全过**——按令仅作归档见证的连续性复核，不重新裁决、不重新计时、不构成对 R22-D 属地格的重铸。

## 2. 元闭合审计独立重推（方法第 3 步前置）

R31-D §2 的 35 行坐标表（喂什么 ×6 / 喂多少 ×3 / 次序相位 / 引擎态 / 拒绝终端 / 写路径终止 / 锁事务 / 进程边界·共存·生命期·负载 / 导出面 / 权威 / 注入缝 ×2 / 时间 / 溯源 / 持久工件 ×3 / 序列化形 / 对象表示 / 引用身份 / 调用协议 / 调用流 / 输出去向 / 任务面可选字段 / 值域 / 进程环境 / 校勘 + 残格）本轮第三十六遍从零独立重推：每一维的属主裁决**无一行松动**——(o) 行读码复核原样成立（举证抽样：R25-D 身份坐标 = resourceIdentityKey/identityEquals/scopeEquals 原文在位；R24-D 权威面 = canAutoPromote 七道门原文在位；R26-D 注入缝默认臂 = registry.ts:78–79 `?? nowIso` / `?? createResourceVersionId` 原文在位；R23-D 两相 begin/commit + 在飞意图种群原文在位）；(s) 行的姊妹面经 R32-\* + R33-\* + R34-\* + R35-\* + **R36-A + R36-B + R36-C** 收口后边界不变（R36-A 三项记录全部 A 区物理对象——其 D 镜像面已在本报告 §0 记录 1 / 记录 2 显式裁决；R36-B 零新记录、两残余角度全部 B 区物理对象；R36-C 唯一拒列记录住 C 区 r1.ts——shadow-only 离线路由面，与 D 零交集；均无泄入平面）；(p) 行 plan-ban 轴名一个未补铸。唯一残格（monitor 配置平面）维持 R31-D §3 的 R22-D 属地裁决，本轮 §3 哨兵直证其裁决前提（零生产调用方）第三十六次成立。**第三十六遍枚举在该坐标系内无剩余未审计平面。**

## 3. D 档案在案重开条件 / 条件陆器哨兵复核（全部不计时；方法应用，不铸面）

R32-A 首创的哨兵审计方法（plan-ban 重铸为面）此处按令仅作卫生性应用于 **D 自档案**的在案条目。全部哨兵为静态 grep / 读码断言，零计时臂：

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| R22-D §5 (a)（含 R31-D §3 显式扩展的配置平面腿） | 任一休眠导出获得生产调用方 | `rg "adaptation/(monitor\|pareto\|mutate\|retirement\|reflection)" src/`（排除切片自身）= **空匹配**（第三十六次复核）⇒ **未触发** |
| R22-D §5 (b) | monitor 接入长驻进程 | 零切片外 importer ⇒ 不可能被接入 ⇒ **未触发** |
| R22-D §5 (c) | 导出面结构变更 | 冻结 0 行 ⇒ **未触发** |
| S12-B-2 条件陆器（D 侧入口） | `eval-routing.ts` 开始向 `assignTasks` 传 `prior` | `rg -c 'prior' src/adaptation/eval-routing.ts` = **0 匹配**（rg exit 1）；L353–354 读码直证仍只传 `learned` ⇒ **未触发**（与 R36-B §5 独立重取证同结论） |
| S13-B-1 重开条件 | `applyLearnedRouting` 单次成本增长 ≥2 个量级 | 冻结 0 行（learning 侧亦在最强冻结内）⇒ **未触发**，维持 loser ID（与 R36-B §5 同结论） |
| S2-D-3 / S12-D-2 等规模型重开 | E 规模增长数量级 | 生产 eval 入口仍为 CLI once-per-command、dataset 形未变 ⇒ **未触发** |
| adaptation importers 面 | 新增生产调用方 | 全库 import 形 grep：恰 cli/adapt.ts + learning 三文件（learned-routing / from-episode / auto-loop）+ tracking/analysis.ts（全 `import type`，L1–3 读码复核）——与 R31-D…R35-D 记录逐文件一致 ⇒ **零新增**。卫生注记：裸词宽口径 grep 另中 privacy/state-layout.ts 与 privacy/record-classes.ts，逐行核查全部为 `adaptationRoot` 注释或路径字符串字面量，非 import，普查结论不受影响 |
| X0-7 / REPLAY_COMPARISON_CONFIG | 规格解冻 | 读码直证配置字面量原样（eval-routing.ts:41–44）⇒ **冻结在位** |

全部哨兵未触发。凡要求 `src/` 变更的重开条件（签名族 / 调用图变化 / 幻想态命中）由 0 行冻结直接判空。

## 4. 十四文件新视角检查表（方法第 3 步枚举残余）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| active-pointer.ts | CAS 合同冻结（硬不变量）；`resourceIdentityKey` 串构造 = R25-D 身份坐标 + S5-D-5 CSE 已拒；`assertExpectedActive` 生产调用方在位（registry.ts:598） | 无新候选 |
| approval-profile.ts | canAutoPromote 预算/计数整数重验 = R35-D §0 记录 1 维持；约束列表几何 = R30-D（禁移植）；includes 链 + 嵌套 includes（表长 ≤10）= S1-D-8 本体 + S4-D-4 已拒；构造器每调用新对象享元化 = S7-B-5/S6-B-2 类身份可观察；`isAutoAdaptEnabled` env 读 = process-environment coordinate（R25-I 面） | 无新候选 |
| candidate.ts | hash32 锚带内（9.2–10.6 µs）；`assertSingleResourceBoundary` 廉价探针 = S12-D-1 永冻（原形读码复核，trim 首字符卫在位）；`assertAcyclicLineage` Set 走链 = R23-D 谱系；JSON.parse 位点 = REPRX 计数即止；trim 空串卫群 = R35-D §0 记录 4 镜像面维持 | 无新候选 |
| eval-routing.ts | 双 parseTaskId = S2-D-3；四遍 O(E) = S12-D-2；catalogCost find = S1-D-4；S9-D-4 站点健全形态在位；roots 构造 = S3-D-3/S9-D-2；episode 形状 = S9-D-1/S8-D-5；prior 陆器哨兵宿主（§3，未触发）；报告尾链 = S7-D-2；mkdir 每 eval = R33-D §0 记录 2 维持；载入重叠 = S4-D-3；全 UNOBSERVED 短路 = S5-D-4；parseEpisode/loadDataset 映射闭包 = §0 记录 1 镜像家族同腿（once-per-command parse 腿已整体定价） | 无新候选 |
| monitor.ts | R22-D 属地格宿主——三条重开腿哨兵全部未触发（§3）；W-M 见证连续性复核五次全过（§1(iii)）；每 observe 基线重建（含双 slice 分配）= X2-5 本体；**freezeBaseline 四处 `new Set(window.map(...))` = §0 换名记录 2 拒列**；**`Object.values(axes).filter` 布尔计数物化 = §0 换名记录 4 拒列**；死 emptyAxes = S3-D-5；`Math.*` ×3 = BINDX 计数即止；requireNonEmpty 空串卫 = R35-D §0 记录 4 镜像面；mode() Map 计数 = 休眠零流量 | 无新候选（属地维持） |
| mutate.ts | instruction 双 trim = R35-D §0 记录 2 维持；休眠（R22-D 定价 8.8–15.3 µs）；regex 分派 = 既有 S 族；`escapeRegExp` 每调用铸 RegExp = 零流量 + X0-6 邻域 | 无新候选 |
| pareto.ts | 休眠（R22-D：生产 n 个位数 1.9 µs）；O(n²) skyline = S1-D-6；尾 spread = S3-D-2 | 无新候选 |
| promotion-rules.ts | `isPromotableStatus` includes（3 元表）= S1-D-8；promote 链 hash 三算 = R33-D §0 记录 1 维持（S1-D-7 本体）；production-labeled 拒收原文在位（L125–129，硬不变量证人）；evalReport kind 门 = R24-D 权威面；provisional 双读 = 同函数同裁决；**validateChangeNote 双 for 循环融合 = §0 换名记录 3 拒列** | 无新候选 |
| promotion.ts | parse/save 腿 = R29-D + R16-D；load 链融合 = S11-D-2；fsync = S8-D-3 禁去（L230 在位）；临时名 UUID = S7-D-3；条件 spread = S4-D-1/S8-D-5 族；美化 JSON = S4-G-6 族数据面；锁 retryMs = R18-D；再导出别名 = R22-D；mkdir 每写 = S10-D-1；asRecord/asArray 私有重复 = X0-5；parsePromotionReview 双层校验 = R34-D §0 记录 2 维持；loadOrNew 正则探针提升 = R34-D §0 记录 3 维持；**asStringArray/parseRegistrySnapshot 每调用映射闭包 = §0 换名记录 1 拒列** | 无新候选 |
| reflection.ts | partitionEvidence 复合串键换嵌套 Map = R35-D §0 记录 3 维持；休眠（R22-D 定价 proposeCandidates）；`evaluateProposalShadow` = experiments 属地（历轮同判）；`isSelfSupported` unique 线性去重 = 个位数评估者 + S1-D-8 族；`Math.min` ×1 = BINDX 计数 | 无新候选 |
| registry.ts | addVersion 复制追加 = S1-D-1/R21-D；`candidatesFor` 线扫 = S1-D-2；`Array.from(values())`（含 snapshot 的 entries+map 两遍形）= S1-D-5；两相 begin/commit = R23-D；restore 再哈希 = S8-D-1 禁去；restore 头部预清除 = S8-D-4 禁去；getActiveContent 读路径再哈希 = S8-D-2 禁去；putContent 重存 = S7-D-1 禁去；ledger() 拷贝 = S3-D-1；重复 review.candidateId 检查 = S9-D-3 防御纵深保留；pendingByIntent 在飞线扫 = 个位数 + S1-D-8 族；rollback CAS 后冗余 get = S2-D-2；rollback knownTargets.some 死检查 = R34-D §0 记录 1 维持；幂等 last() 探针 = S7-D-4 毗邻原形保留 | 无新候选 |
| resource.ts | kind 闭枚举（10 值）；NON_AUTO_PROMOTABLE_KINDS includes = S1-D-8；`identityEquals` / `scopeEquals` = R25-D 身份坐标 | 无新候选 |
| retirement.ts | 休眠三件套（R22-D 17–359 ns 已定价） | 无新候选 |
| rollback.ts | user CAS 锚带内（3.0–3.3 µs）；载入链双拷贝 = S4-D-2 禁去；外层再拷贝 = S3-D-4；`list()` 每读防御拷贝 = R33-D §0 记录 3 维持（X4-2/S1-F-3 族）；evidence 防御拷贝群 = R36-A 记录 2 D 镜像面（既有已点名家族复命中，§0）；`validateIdentity` 与 parseIdentity 重复 = X0-5；asStringArray 谓词闭包 = §0 记录 1 同腿；restore 验证弱化 = fail-open 家族明文冻结 | 无新候选 |

## 5. 候选总表

**空。** 本轮零候选：元闭合审计独立重推（§2）35 行无一松动；哨兵复核（§3）按定义不产出候选（不计时、不剂量）；十四文件枚举（§4）全部命中既有 ID 本体、姊妹归档面或 plan-ban 轴名；4 项新站点级换名记录（§0）全部拒列。本轮无任何计时新臂 ⇒ 无可定价候选面。

## 6. CHECKS

- 分支恰 `cursor/r36-d-adaptation-thirty-sixth-pass-83a1`，基线 `fead867`；提交前先后检测到两次纯 docs 快进 `fead867` → `83c3429`（经 `bf0a146`，恰 3 docs 文件，`-- src/` 0 行）与 `83c3429` → `a0fd7ed`（恰 3 docs 文件，`-- src/` 0 行），均已按令纯快进对齐（`git merge --ff-only`）并各重验冻结；R36-B / R36-C 已先后补读折入，R36-E / R36-F 在飞零触碰。
- 冻结核对：`git diff 82bef36 -- src/adaptation/` 派单 tip / 测量后 / 两次快进后 / 提交前**五点均 0 行**；`wc -l` 合计 3294（14 文件逐一列出核对，与 R33-D / R34-D / R35-D 逐数一致）；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**（含两段快进）。
- 地板脚本官方五次运行 `ALL CHECKS GREEN`（0 FAIL；要求 ≥3）；脚本与 R31-D 归档件 sha256 逐字符同一（`0862c6b6…43e41`，498 行）；五运行全部带内自报 `node v22.22.2`，零弃置批次（§1(ii)）。
- 生产调用方普查（第三十六次）：五休眠模块零切片外调用方；adaptation importers 恰 4 生产文件 + tracking/analysis.ts（type-only）；裸词宽口径误中已逐行核查为注释/路径字符串非 import（§3）。
- 聚焦测试：`npx tsx --test test/unit/adaptation/*.test.ts` = **99/99 pass**（suites 10）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿**——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D…R35-D / R36-A / R36-B / R36-C 逐字同计数）；退出码 0；Node v22.22.2；测量与门禁在 `fead867` 工作树完成，三 tip 间 `src/ test/ scripts/ package` 逐字节相同 ⇒ 结果原样携带。
- 仓库改动恰本报告一个文件（`git diff cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：无阈值/权限/数据面/公开签名改动；fail-open 家族保持关闭；S9-D-4 / S12-D-1 未重开（原形读码复核）；双 LCB + 双归因在位；proposal-first 在位；fsync 在位（promotion.ts:230）；分析未变更任何在飞 run；不开 PR。

## 7. 重开条件

本轮无新定价轴，不加行。R22-D §5 三腿（含 R31-D 显式扩展的配置平面覆盖）、S12-B-2 条件陆器、S13-B-1 loser 裁决、R30-D 几何面、R29-D 载荷面等既有重开条件全额维持原文；本轮 §3 哨兵直证全部未触发。本 VM 相对 R34-D 高偏置窗持续回落、相对 R35-D 轻度偏快且残余高尾收窄（§1(i)）不构成重开触发——冻结 0 行、引擎位级同一、脚本逐字节同一、锚间全序五次保持、R33-B/R33-C/R34-D/R35-D 同型归档在案；偏置方向不松动任何裁决。

## 8. 新增排除 ID

**无。** 空枚举轮不铸 S36-D-\* ID；不取绰号、不作轴名、不加 EXCLUSIONS 行。换名检查 4 项新站点级记录见 §0（1–4），归档备后续轮次引用，无需并入 EXCLUSIONS.md。

## 附录：仿真出处与运行摘录

本轮**未新建**仿真脚本：官方地板脚本为 R31-D 附录归档件的逐字节重跑（从 `round-31/R31-D.md` 附录 L176–L673 提取（`sed -n '176,673p'`），`sha256 = 0862c6b6f96c7734d340ce931621fc6e5be227917806ff1a9bfde65145a43e41`，498 行，与 R31-D…R35-D 归档值逐字符相同；全文见 R31-D 附录，此处不重复内联）。运行：`npx tsx /tmp/r36d-floor.mts`（nvm Node v22.22.2，PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开 `/exec-daemon` 系统 Node v22.14.0）。临时文件已从磁盘删除。

官方五运行关键行摘录（每 run 首行带内自报 `node v22.22.2`）：

```text
run 1:
S0: load=0.13 save=0.52 | eval(toy)=4.27ms
S0c: load=0.62 save=1.52 | eval=3.50ms   S0d: 40.9us / 3.0us / 9.2us
S-late: load=0.61 eval=3.20ms

run 2:
S0: load=0.10 save=0.53 | eval(toy)=4.18ms
S0c: load=0.57 save=1.46 | eval=3.73ms   S0d: 45.5us / 3.3us / 9.2us
S-late: load=0.59 eval=3.50ms

run 3:
S0: load=0.12 save=0.54 | eval(toy)=4.24ms
S0c: load=0.61 save=1.61 | eval=3.83ms   S0d: 45.4us / 3.1us / 10.6us
S-late: load=0.66 eval=3.45ms

run 4:
S0: load=0.11 save=0.64 | eval(toy)=4.28ms
S0c: load=0.62 save=1.64 | eval=3.57ms   S0d: 45.8us / 3.2us / 9.2us
S-late: load=0.65 eval=3.45ms

run 5:
S0: load=0.12 save=0.56 | eval(toy)=4.15ms
S0c: load=0.57 save=1.39 | eval=3.48ms   S0d: 42.0us / 3.2us / 9.6us
S-late: load=0.65 eval=3.30ms
```

MORE_OPTIMA=no
BRANCH=cursor/r36-d-adaptation-thirty-sixth-pass-83a1
