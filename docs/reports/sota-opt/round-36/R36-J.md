MODEL_SLUG=claude-fable-5-thinking-xhigh

# R36-J：persist / 数据面切片第三十六遍复查报告（Round 36）

**战役:** 全库持久 SOTA 优化 Round 36 / R36-J（R1-J…R35-J 同区三十五遍之后的第三十六遍）
**基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `c5fe985`（派单 tip，R36-G 空枚举摄取 + 本 R36-J 派单）。冻结核对、29 文件从零重读、生产调用面 rg 普查、重锚 pass-1 与 J1 sim 在 `c5fe985` 树完成；随后 fetch 检测到 exclusive **一次纯 docs 快进** `c5fe985` → `c92d647`（两笔提交 `f56d7fc` R36-H 摄取 + `c92d647` R36-I 摄取——`--stat` 恰 4 个 docs 文件（PROGRESS.md / round-36 PLAN.md / R36-H.md / R36-I.md，366 插入 15 删除），对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接 diff **0 行**），按令 `git merge --ff-only` 纯快进对齐（零冲突）并重验冻结（29 文件 vs `fb41417` 与最强冻结 `89f0455..HEAD -- src/` 均 **0 行**）；聚焦套件、`pnpm gate` 与重锚 pass-2/pass-3 在 `c92d647` 树完成——两 tip `src/`+`test/` 逐字节相同 ⇒ 全部测量位级可比、原样并表。快进带入的 **R36-H** 与 **R36-I** 已全文实读并折入非移植清单（§0.3）；派单要求预读的七份姊妹报告 **R36-A…R36-G**（七份均诚实空枚举）已全文通读并折入。工作期间按令未等待、未触碰任何姊妹分支。
**分支:** `cursor/r36-j-persist-thirty-sixth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（R28-J…R35-J 同引擎约定，带可比；本 VM 系统 PATH 暴露 Node v22.14.0——开工即 PATH 前置 `~/.nvm/versions/node/v22.22.2/bin`，每个计时批前在同 shell `which node` + `node --version` + `process.versions.v8` 三重复核，正批全程 v22.22.2，**v22.14.0 上零批次、零弃置批**；`pnpm install --frozen-lockfile` 干净完成，锁文件未改）。

## 结论

**诚实的空枚举：第三十六组没有可立 ID 的新角度，不铸任何 S36-J-\* ID，不铸新 face 名，不取 ALLCAPS 绰号，不写入 EXCLUSIONS 表行，生产代码零改动，切片维持关闭（MORE_OPTIMA=no）。** 本轮按派单纪律收口 J 区：从零重读 29 个切片文件（3655 行，§0.1）、在本 VM 以 R12-J 附录 A 同源 harness 重锚配置态 I/O 地板矩阵九格 ×3 遍（§1.1）、重跑 J1 等价 sim 全绿（**2468 项逐位检查**，§1.2）、对每个「看起来新」的残余位点做换名拒绝检查（§2.2）。结果是**零新站点级换名记录**——本轮从零重推出的全部候选角度经查证均为既有 ID / 既有记录 / 姊妹属地的复命中：R35-J §2.2 十二项重赛角度按令逐一确认原裁决在位、不重铸（`track/loop.ts` `assignments.find` = **S1-J-5 排除行本体**（L106 逐字复核原样）；`context/packet.ts` L122 首个 `omissions.sort` = **S1-J-7 排除行本体**；waitForClarification 双 `readAll` = **S2-J-11**；`pickCanonical` = **S7-J-1**；`stripForbidden` = **S3-J-1 / S9-J-1..4**；context 跨模块 token 估算孪生 = **R3-J / X1-1 公开形状禁区**；episode append promise 链 = R7-J 顺序契约 + S0b 地板支配；`reduceEpisodeEvents` fold/`markFailed` = R17-J ln 域 + R18-J 空 fold 定价；jsonl fsync open/sync/close = **fsync 不变量本体**；`queryPacketGrounding` = test-only 面（第三十六次 re-grep 零生产调用方）；file-lock `retryMs ?? 10` = D/E 已计价 + J 零生产调用方；R34-J 四项记录原形在位）；本轮重读另核对的三处「看起来新」角度（preferences/materialize `Object.entries` 拷贝环、precedence 5 元素表线性 `find`、readFeedback 双 `.filter` 链）全部落在既往轮命令类裁决 / 既有删除面族价签之内（§2.2），无一达到「从未在任何 J 报告点名」的新站点门槛。重锚九格全部落在历史带内或与之交叠，若干格快缘低于历史快缘 ≤7%（本 VM 轻度 host-fast 窗，与 R36-I 同波注记；冻结 0 行按派单原文不构成发现，§1.1 判读）。空枚举与 R32-J…R35-J、R36-A…R36-I 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 29 文件自 J1 落地（`fb41417`）**逐字节零变化**——派单 tip `c5fe985`、快进对齐 `c92d647` 后、测量后与提交前核对 `git diff fb41417 -- <29 文件>` 均 **0 行**；`git log fb41417..HEAD -- <切片八目录>` **零提交**（最后触碰切片的提交仍为 `fb41417` J1 本体）；最强冻结 `git diff 89f0455..HEAD -- src/` = **0 行**（R10 以来 docs-only；快进段亦纯 docs）⇒ 生产调用图对本切片**可证不变（最强形式，第二十八连）**。`wc -l` 合计 **3655**（29 文件逐一实测，§0.1）。重锚九格与历史带关系（§1.1）：九格全部与 R34-J/R35-J 带交叠或全含；saveToDisk 50/500、jsonl fsync=false、级联双格、plan randomUUID 六格快缘低于历史快缘 1.6–7.0%（同批同向 = 本 VM 轻度 host-fast 刻度，R36-I version 地板 50.8–51.2 对 R35-I 54.2–58.7 同波互证）；jsonl fsync=true 本轮无 R35-J 型 399–801 极端慢窗（union 240.5–380.2，pass-1 首采样 380.2 为温和设备侧慢样，其余八采样 240.5–340.3 落 R34-J 带 227.1–338.2 内）。**I/O 支配判据第三十六次成立**（persist 地板数百 µs vs CPU 格 1–2.6 µs）。J1 落地代码本轮 `scripts/r1j-equivalence-sim.ts` 重跑全绿（**2468 项逐位检查，reference 9997.7 ms → current 3.5 ms = 2823.6×**——比率落派单记载 2760.9×、R31-J 2836.9×、R35-J 2948.5× 的宿主偏移带内，current 腿 3.5–3.6 ms 跨轮稳定；J1 锁不重铸，fold 形状锁定原样）。聚焦切片套件 **91/91**（8 目录 4 suites，`c92d647` 树）；`pnpm gate` **exit 0**（tests **1169** / suites 78 / pass **1168** / fail **0** / skipped **1**，与 R14-E…R36-I 历史计数逐字相同）。哨兵静态普查 0 命中（§2.4）。硬性不变量逐条零触碰（§3）。仓库变更仅本报告一个文件；重锚 harness 按纪律不入库（与 R12-J 附录 A 归档件同源，除头注释与临时目录前缀 `r12j-`→`r36j-` 外逐字同源；每个 gate 运行前均已从仓库树删除，附录 A 存原始输出）。不开 PR。

## 0. 冻结、快进处置与划界

### 0.1 冻结与快进

- `git diff fb41417 -- <29 文件>` = **0 行**（派单 tip `c5fe985`、快进对齐 `c92d647` 后、测量后、提交前核对一致，四点覆盖全程）。
- 最强冻结：`git diff 89f0455..HEAD -- src/` = **0 行**（R10 以来 `src/` docs-only 不变；快进段 `c5fe985..c92d647` 对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接 diff **0 行**）。
- `git log fb41417..HEAD -- src/cluster src/privacy src/preferences src/episode src/persist src/track src/context src/feedback` = **零提交**。
- `wc -l`（本轮逐文件实测）：cluster **345**（host 200 / mailbox 91 / spawn 54）+ privacy **416**（deletion 126 / record-classes 261 / state-layout 29）+ preferences **707**（export 65 / loop-eval 180 / materialize 36 / precedence 40 / service 65 / store 288 / types 33）+ episode **396**（closure 49 / events 44 / manager 207 / replay 49 / store 47）+ persist **102**（file-lock 49 / jsonl 53）+ track **518**（clarify 65 / loop 279 / plan 122 / primary-split 52）+ context **941**（index 418 / packet 523）+ feedback **230**（redaction 121 / store 85 / types 24）= **3655**，文件数恰 **29**（3+3+7+5+2+4+2+3 逐目录确认），与 R32-J/R33-J/R34-J/R35-J 记录逐一相同。
- 快进处置：冻结、重读、调用面普查、重锚 pass-1 与 J1 sim 在 `c5fe985` 树完成；fetch 见一笔快进 `c5fe985..c92d647`（两笔提交 R36-H / R36-I 摄取，`--stat` 恰 4 个 docs 文件 366 插入 15 删除），`git merge --ff-only` 零冲突对齐，冻结重验 0 行；聚焦套件、`pnpm gate` 与重锚 pass-2/3 在 `c92d647` 树完成——两 tip `src/`+`test/` 位级同一 ⇒ 三遍矩阵与 sim/gate 结果原样并表，无需重测；R36-H / R36-I 实读折入 §0.3。推送前再次 fetch 复核 exclusive 仍在 `c92d647`，无第二笔快进。

### 0.2 生产调用面复核（本 tip rg 实测，第三十六次）

- `withExclusiveFileLock` 调用方恰 4 个切片外文件：`pi-adapter/file-credential-store.ts`（:43/:54，I 属地）、`cli/episode.ts`（:90，I 属地）、`learning/bandit-store.ts`（:44，E 属地）、`adaptation/promotion.ts`（:247，D 属地）——J 属地仅定义位点（file-lock.ts:11），**零 J 侧生产调用方**（派单不变量原文维持：`retryMs ?? 10` 已由 D/E 计价，J 无可铸对象）。
- `queryPacketGrounding` = `src/` 内仅 `context/packet.ts:498` 定义位点，零生产调用方（test-only 面第三十六次确认）。
- `loadLearnedRouting` 生产调用位点恰 3 处不变（cli/main.ts:708、run/flowchart-run.ts:712、track/loop.ts:88）+ 1 定义（learned-routing.ts:115）——**S8-E-1 零去重**维持（E 属地，J 侧 loop.ts:88 为调用方侧坐标，零触碰；与 R36-I §5 同轮独立普查逐位点互证同值）。
- `track/loop.ts:80` `applyPrecedence(contract, "user-first")` = R31-H **D1 停驻门**的切片外规则字面量普查坐标（H 属地，R36-H §2.4 本轮直证过）——J 侧零触碰、不代跑。
- fsync 面在位直证：`persist/jsonl.ts:16-19`（fsync 腿 open/sync/close）与 run 侧 `pause-controller.ts:32` / `checkpoint-store.ts:23` 的 `handle.sync()` 原形在位，零去除。

### 0.3 姊妹面划界与非移植清单（R36-A…R36-G 派单折入 + 快进带入 R36-H / R36-I）

派单非移植项全数持有：命名轴 FITQ NAMESHAPE SCALEX XPROC YMIX SCHEDWIN KFAN NVG SEEDX TERMCLASS SHAPEK ORDX CKMIX CALLB OCCX JB RUNLIM VALCLASS CG AGEX COTARG PRICEX RTL DELINV PROBX RVL SIDEC PWL ZREP PAIRX CSPELL REPRX **LINKTOP BINDX MODEX**；R32-J 六残余坐标（inode 时间戳 / extent 布局 / owner 字 / ACL-xattr / dirent 序 / 线程池宽度——已三方败）；R30-J 链接拓扑（LINKTOP）与 R31-J inode 模式拓扑（MODEX）耐久工件面；R31-E I/O text-codec topology / per-run transcode-byte manifest；R32-A 哨兵方法 / R32-B 闭合稳定性方法仅作卫生应用不铸「我又查了一遍」；R34-J 四项记录 / R35-J 十二项重赛角度按令不重铸（§2.2）。round-36 姊妹九份逐项划界：

- **R36-A**（tracking 空枚举）：三项站点级换名检查拒列（types parse 回调闭包提升 / from-child `artifactIds` 防御拷贝省略 / roller `keptMandatory` 中间映射数组消除），全部 tracking 物理对象；预算重锚 13.8–17.6 µs/gate——J 零共享物理对象，无可移植轴。
- **R36-B**（live 路由空枚举）：零新站点级换名记录；天花板重锚 M=2 10.18–11.68 ms/eval、replay-faithful M=2 8.38–8.53 ms；S12-B-2 / S13-B-1 重开条件经其重取证未触发（属地哨兵，J 不代跑）——零共享。
- **R36-C**（离线路由空枚举）：一项换名检查（`routeR1` cheapest-`reduce` 折叠成本携带）= X1-4 具名成本池属地拒列；生产中位带 667.9–681.0 ms/report——C 属地，零共享。
- **R36-D**（adaptation 空枚举）：四项站点级换名检查拒列（parse/load 回调闭包提升镜像 / monitor `freezeBaseline` `new Set(map)` 中间数组 / `validateChangeNote` 双循环融合 / `Object.values(axes).filter` 布尔计数）；eval 地板重锚 S0c 3.48–3.83 ms——全部 adaptation 物理对象。其记录家族的 J 侧镜像面划界：家族 2（`new Set(x.map(...))` 中间数组）J 切片内无对应新站点（本轮读码直证：deletion.ts 墓碑构造为 `new Set(ids)` 直集非 map 中间态、loop-eval inferredCounts 为 Map 增量维护）；家族 3（同谓词双遍融合）J 侧 readFeedback 双 `.filter` 链 = 既有删除面族价签复命中（§2.2 角度 c）；家族 4（filter 布尔计数）J 侧无对应计数站点。零新记录。
- **R36-E**（learning 空枚举）：零新站点级换名记录，六项候选全部复命中；SLICE-CPU cmp25 重锚 14.9–15.4 µs/run；S8-E-1 三调用位点直证均为 E 属地（J 侧 loop.ts:88 调用方坐标 §0.2 互证）——零共享 J 物理对象。
- **R36-F**（experiments 空枚举）：零新站点级换名记录；r5f/r6f/r7f/r1f 锚点全落历史包络；S7-F-1 ≠ S6-F-5 确认——15 文件全在 `src/experiments/`，零共享。
- **R36-G**（runtime/graph/domain 空枚举）：恰一项新站点级换名记录三重拒列（`domain/contract.ts` 溯源域中间数组物化，G 属地物理对象）；计算顶 0.292–0.295 ms/run vs I/O 地板 93.6–96.9 ms/run；digest `06cbcf92c098c8f0` 第二十七次同一——**G 未铸 digest ID，J 按令不重铸 digest 轴**；G 侧 run 消费面是 J 的调用方侧非 J 属地——零共享本体。
- **快进带入 R36-H**（evaluation/requirement/review/rubric 空枚举）：零新站点级换名记录（候选与 R35-H §2.2 八项重赛角度逐一重合复命中）；锚点全落历史带 / ns 级两侧擦边（其判读同为「偏暖窗 + 冻结 0 行非发现」）；S5-H-1 前置守卫在位；其 §0.2 生产调用面普查含 J 侧三个调用方文件（clarify.ts:4 `extractHeuristicContract` / loop.ts:38 `applyPrecedence` / plan.ts:5 `shouldScout`）均为 **H 导出的 J 调用方侧坐标**——H 属地本体、J 零可铸对象；D1 门坐标 track/loop.ts:80 划界见 §0.2。零共享物理对象。
- **快进带入 R36-I**（cli/pi-adapter/config/telemetry 空枚举）：零新站点级换名记录、零残余候选（R35-I 唯一残余 `assertKnownCatalogId` 当场指认 S7-I-4 本体即止）；六类 custom−builtin 带与世系带族全部相交；本 VM 整体轻度 **host-fast**（version 50.8–51.2 对 R35-I 54.2–58.7）如实注记不构成发现——与本轮 J 矩阵若干格快缘低于历史快缘同波互证（§1.1）；r4i/r5i/r7i 仿真 68/119/80 checks 全 0 失败；S8-I-1 文件级封锁复验维持、npm `.bin` symlink no-op 按令未「修复」——25 文件全在 cli/pi-adapter/config/telemetry，与 J **零共享物理对象**（其 S8-E-1 哨兵普查含 track/loop.ts:88 = J 文件内的 E 属地调用方侧坐标；file-lock 调用方 file-credential-store.ts / cli/episode.ts 为 I 侧计价面，§0.2 互证），无可移植轴。

## 1. 重锚（历史仪器，I/O 地板矩阵 ×3 遍 + J1 sim）

### 1.1 I/O 地板矩阵（R12-J 附录 A 同源 harness，三遍 × 各 3 次运行，nvm v22.22.2）

| 格 | 本轮三遍并集 | R35-J 记载带 | R34-J 记载带 | 裁决 |
|---|---|---|---|---|
| saveToDisk 50 obs | 132.0–267.2 µs/写 | 146.3–253.7（稳态；暖窗并集至 397.2） | 141.9–234.4（稳态；暖窗并集至 252.7） | 带重叠（146.3–253.7 共域）；快缘 132.0 低于 R34-J 快缘 −7.0% = host-fast 窗 |
| saveToDisk 500 obs | 412.4–534.9 µs/写 | 438.7–509.9 | 429.1–533.4 | 带重叠（慢缘 534.9 对 R34-J 慢缘 533.4 逐位邻域）；快缘 −3.9% host-fast |
| jsonl append fsync=false | 59.6–65.1 µs | 69.7–76.9 | 61.2–69.4 | 与 R34-J 带交叠（61.2–65.1 共域）；快缘 59.6 −2.6% host-fast（R35-J 慢侧窗本轮未再现） |
| jsonl append fsync=true | 240.5–380.2 µs | pass-2 290.0–326.3；pass-1/3 慢窗 399.5–801.5 | 227.1–338.2 | 八采样 240.5–340.3 落 R34-J 带内；pass-1 首采样 380.2 为温和设备侧慢样（+12.4% 对带顶）——R35-J 型 399–801 极端 flush 竞争窗本轮未再现 |
| 级联 MATCH（N=200，20 hits） | 623.0–717.0 µs | 739.9–897.6 | 636.0–857.7（并集） | 与 R34-J 带交叠（636.0–717.0 共域）；快缘 623.0 −2.0% host-fast |
| 级联 NO-MATCH | 254.1–302.9 µs | 285.5–344.0 | 260.3–335.6 | 带重叠；快缘 254.1 −2.4% host-fast |
| context index（F=40 + 30 syms） | 40.3–41.0 µs | 37.7–38.4（快侧窗） | 40.4–41.9 | 与 R34-J 带交叠（40.4–41.0 共域）——R35-J 快侧窗回正 |
| track plan 计数器 | 1.02–1.12 µs | 0.97–1.08 | 1.01–1.11 | 带重叠（1.02–1.08/1.01–1.11 共域） |
| track plan randomUUID | 2.48–2.63 µs | 2.52–2.74 | 2.52–2.70 | 带重叠；快缘 2.48 −1.6% host-fast |

判读：九格无一为结构迁移——同批冻结差分 0 行 + 九格全部与历史带交叠 + 六格快缘同向低于历史快缘 1.6–7.0%（同向齐移 = 宿主刻度特征而非位点性变化，与 R36-I 同 VM 世代 host-fast 注记互证）+ fsync 格极端慢窗消失（设备侧窗口性最直接证据：窗口可来可去而代码不变）。按派单原文「host-bias windows with freeze 0 lines are NOT findings」不作新发现申报、不改带。**I/O 支配判据第三十六次成立**：persist 地板（saveToDisk 132–535 µs、jsonl 60–380 µs、级联 254–717 µs）比切片 CPU 格（plan 1.0–2.6 µs、context 40.3–41.0 µs）高 1–2 个数量级，任何纯 CPU 换名的收益上界被同路径 I/O 地板吞没。

### 1.2 J1 等价 sim（`scripts/r1j-equivalence-sim.ts`，不重铸）

场景 1 确定性边角 **64 checks / 16 cases** + 场景 2 随机化 **2400 checks / 600 cases** + perf fixture（N=6000 多主体 ~25% tombstoned）= **ALL EQUIVALENCE CHECKS PASSED（2468 bitwise checks）**。reference **9997.7 ms** → current **3.5 ms** = **2823.6×**（reference 逐元素访问 92,590,419 次不变）；对照派单 2760.9×、R31-J 2836.9×、R32-J 2713.8×、R35-J 2948.5×——本轮比率落宿主偏移带内（reference 腿 9997.7 介于 R34-J 9912.2 与 R35-J 10400.5 之间，current 腿 3.5–3.6 ms 跨轮稳定）。J1 fold 形状锁定原样，J1 锁不重铸。

## 2. 第三十六遍枚举（诚实结果：空；零新站点级换名记录）

### 2.1 逐文件重读与裁决（29 文件从零重读，零未点名站点）

| 文件（行数） | 站点归属（全数指到既有裁决） | 新候选 |
|---|---|---|
| `cluster/host.ts`（200） | 广播扇出 `body.trim()` + `targets.slice(1)` 提升 = **R34-J 记录 3** 原形在位不重铸（纯内存 ns、零持久坐标） | 无 |
| `cluster/mailbox.ts`（91） | mailbox n 三连 = **S5-J-4**；冗余 `byRole.set` = R15-J 拒列原文（R12-J 位点 B 已裁） | 无 |
| `cluster/spawn.ts`（54） | 组装命令类；双 `objective.trim()` = R12-J 位点 A 已裁；公开签名 X0-4 禁区 | 无 |
| `privacy/deletion.ts`（126） | 级联读序 `.catch(() => [])` 错误面 = R5-J §3 契约；`[...tombstones].sort()`/`cascaded.sort()` = R10-J µs 拒列；双文件串行 = **S4-J-3**（并行化竞态拒）；O(记录数) 整读改写 = 删除正确性契约本体（**DELINV** 域，§2.3） | 无 |
| `privacy/state-layout.ts`（29） | 路径构造命令类 | 无 |
| `privacy/record-classes.ts`（261） | `durableRecordClassById` 零生产调用方（R12-J §1.2 起逐轮复证）；表驻留命令类 | 无 |
| `preferences/loop-eval.ts`（180） | J1 落地本体（fold 形状锁定）；两次 `tombstones.has` = **S2-J-1** 原样（L153/L159） | 无 |
| `preferences/store.ts`（288） | `rebuildViews` 条件 set = **S7-J-4** / `nowIso` 提升 = **S3-J-5**；`applyObservation` 全量 filter = **S1-J-2 / S2-J-6**（saveToDisk 支配）；每写 `mkdirSync` 自愈 = **S7-J-6 保留**；地板本轮 132–535 µs 直测（§1.1） | 无 |
| `preferences/export.ts`（65） | scopes includes / listObservations 拷贝 = **S3-J-6 / S5-J-1**；`exportForDataset` 零生产调用方；`JSON.stringify(…, null, 2)` 美化 = once-per-export 命令类 | 无 |
| `preferences/precedence.ts`（40）/ `materialize.ts`（36）/ `service.ts`（65）/ `types.ts`（33） | 既往轮命令类裁决原样（本轮两处「看起来新」角度归属见 §2.2 角度 a/b）；service 纯转发；types 纯类型 | 无 |
| `episode/manager.ts`（207） | 事件构造恒写可选键 / `runIds.includes` = R11-J §2.3 own-property 面 + **S1-J-3**；reduce fold + `markFailed` 粘性拒绝面 = **R17-J ln 域 + R18-J 空 fold 定价**——本轮候选复命中（§2.2 重赛角度 8） | 无 |
| `episode/replay.ts`（49） | 两导出零生产调用方（R12-J §1.2 起复证）；单遍 parse 支配 = S3-J-4 域 | 无 |
| `episode/events.ts`（44） | 纯类型 | 无 |
| `episode/store.ts`（47） | append 队列 promise 链 = **R7-J 顺序契约** + R10-J §2.3-4 / R11-J **S0b 地板支配**——本轮候选复命中（§2.2 重赛角度 7） | 无 |
| `episode/closure.ts`（49） | L32 legacy-match 模板串 = **R34-J 记录 4** 原形在位不重铸（once-per-close + legacy 兼容契约面）；acceptanceEvidence 结构扫描命令类 | 无 |
| `persist/file-lock.ts`（49） | `retryMs ?? 10` = D/E 已计价；J 零生产调用方本轮 rg 直证（§0.2）——整面等价重写生产收益恒 0；owner-token 回读 = 锁释放正确性契约 | 无 |
| `persist/jsonl.ts`（53） | fsync 腿 open/sync/close = **fsync 不变量本体**（不可去）+ 地板格直测（§1.1）；`readJsonlObjects` split 单遍 = S2-J 读地板族 | 无 |
| `track/primary-split.ts`（52） | 纯组装转发；公开形状 X0-4 | 无 |
| `track/plan.ts`（122） | L41 `/\b(tests?\|coverage)\b/i` + L112 `/\btests?\b/i` 行内正则 = **R34-J 记录 1** 原形在位不重铸（once-per-plan 1.0–2.6 µs 整调用直测内的 ns 份额） | 无 |
| `track/clarify.ts`（65） | H 消费面（`extractHeuristicContract` 调用方侧，H 属地）；spread 合并命令类 | 无 |
| `track/loop.ts`（279） | `assignments.find` in `planned.map`（L106）= **S1-J-5 排除行本体**（本轮逐字复核，C ≤ ~6）；waitForClarification 双 `readAll`（L261/L266）= **S2-J-11**；catalog∥learned = **S4-J-2**；`resolvedQuestionIds`/`catalogIds.includes` = X3-1 域；L80 `"user-first"` = R31-H D1 门坐标（H 属地零触碰，§0.2）；L88 = S8-E-1 站点（E 属地零触碰） | 无 |
| `context/index.ts`（418） | `estimateCodeMapTokens` 跨模块孪生 = **R3-J / X1-1 公开形状禁区**（S5-J-2 注原文）；`isUnderGenerated` O(d×g) 已计价（context 地板 40.3–41.0 µs 直测整含）；排序/去重命令类 | 无 |
| `context/packet.ts`（523） | L122 首个 `omissions.sort` = **S1-J-7 排除行本体**（本轮逐字复核在位；R1-J S17 比较器全字段覆盖 ⇒ 双排结构等价论证维持）；`pickCanonical` 单值组 spread+sort = **S7-J-1**；`estimateCodeMapEntry` 孪生 = 同 R3-J/X1-1；`queryPacketGrounding` = test-only 面（L498 唯一位点，§0.2） | 无 |
| `feedback/types.ts`（24） | 纯类型 | 无 |
| `feedback/store.ts`（85） | `readFeedback` 串行两读 = **R34-J 记录 2** 原形在位不重铸（**S5-J-3 / S6-J-1** 删除面串行形态族）；双 `.filter` 链 = 同族价签内份额（§2.2 角度 c）；`isFeedbackRecord` 结构卫命令类 | 无 |
| `feedback/redaction.ts`（121） | `stripForbidden` 顺序 split/join = **S3-J-1 / S9-J-1..4**；needles 每调用长度降序重排序 = R3-J 已显式计价（n=4 小常数，I/O 支配）；`copyFeedback` 显式字段拷贝 = 既往裁决 | 无 |

### 2.2 换名拒绝检查：本轮零新站点级记录

从零重推（假装排除表不存在重读 3655 行）枚举出的候选角度经对表逐一查证，**全部为已裁决站点 / 已点名属地的复命中，无一达到「从未在任何 J 报告点名」的新站点门槛**——本轮不新增站点级换名记录（J 区经三十五遍逐格归属后已无此类站点，零是更强的收口——R35-J / R36-B / R36-E / R36-F / R36-H / R36-I 同款判读）。处置分两组：

**第一组：R35-J §2.2 十二项重赛角度**（派单明令不重铸）——本轮从零重读中十二项角度逐一再浮出，每项在 §2.1 对应文件行内以属主行复核收口（站点原形在位读码直证），不重列细节、不铸任何新记录：`assignments.find` = S1-J-5 本体；L122 `omissions.sort` = S1-J-7 本体；双 `readAll` = S2-J-11；`pickCanonical` = S7-J-1；`stripForbidden` = S3-J-1 / S9-J-1..4；token 估算孪生 = R3-J / X1-1；episode append promise 链 = R7-J 顺序契约 + S0b 地板；`reduceEpisodeEvents` fold/`markFailed` = R17-J + R18-J；jsonl fsync 腿 = fsync 不变量 + 地板格；`queryPacketGrounding` = test-only 面（§0.2 第三十六次 re-grep）；file-lock 整面 = D/E 计价 + 零 J 调用方；R34-J 四项记录（plan 行内正则 / readFeedback 串行双读 / host 广播 trim/slice / closure legacy 模板）裁决逐字维持。

**第二组：本轮重读中另核对的三处「看起来新」角度**（逐项拒收，均为既往裁决域内份额，不铸记录）：

- **a. `preferences/materialize.ts` `Object.entries(view.aggregates)` 拷贝环**（L16-19/L30-33 两处）——materialize 面 = 既往轮命令类裁决原样（§2.1 R35-J 同行原文）；`effectiveKeys` 为 `MaterializedPreference` 公开返回形状成员（X0-4 公开形状域），拷贝即防御性输出隔离；聚合键数个位数规模 ns 级 + habitsFromPreferences 调用面 once-per-clarify。复命中 + µs/ns 拒收类即止。
- **b. `preferences/precedence.ts` `SCOPE_PRECEDENCE.find` 每调用线性扫描**（L17）——5 元素常量表，Map 化收益 ns 负数量级；`selectHighestPriority`/`explicitOverridesInferred` 调用面零生产热通量（既往轮命令类裁决）。拒收即止。
- **c. `feedback/store.ts` `readFeedback` 双 `.filter` 链**（L73 `values.filter(isFeedbackRecord).filter(tombstone)`）——单遍融合候选 = R34-J 记录 2（串行两读）同一删除面族的族内份额（**S5-J-3 / S6-J-1** 价签整含）；N=200 档 ns 级中间数组 vs 同函数 jsonl 读地板 60–380 µs（§1.1 直测）差 2–3 个数量级。族价签复命中即止。

### 2.3 放大因子收口（引用 R33-J §2.2 方法作卫生核对，不铸面）

切片每个持久 I/O 位点的每命令倍率均为 O(1) 或 O(记录数)：preferences 每观察 1 次全量 stringify+write（地板 132–535 µs）、episode/feedback 每事件 1 次 jsonl 追加（60–380 µs 本轮带）、删除级联每次 2 读 + 命中时 1 改写（254–717 µs）。与实测地板相乘，真实规模下每命令持久成本 **< 1 ms**——距 tens-of-ms 落地线仍 **1.5–2 个数量级**；唯一 O(记录数) 整读改写（删除级联）是删除正确性契约本体（**DELINV** 域，不可折叠）。核对非发现，不铸面。

### 2.4 静态哨兵与在案条件核对（卫生应用，不铸门审计 face）

- **静态划界哨兵**：`chmod|umask|readdir|xattr|\blink\(|symlink|chown` 对切片八目录一次 rg = **0 命中**（空匹配直证）——**MODEX / LINKTOP 在 J 切片代码内无物理对象**，耐久工件面维持 R30-J/R31-J plan-ban 划界，不铸「我又查了门」。
- **fsync / 完整性面**：jsonl fsync 腿 + run 侧 `handle.sync()` 双位点在位直证（§0.2），零去除。
- **R32-J 六残余坐标**（inode 时间戳 / extent 布局 / owner 字 / ACL-xattr / dirent 序 / 线程池宽度）：已三方败，本轮零重提零代跑。
- **S8-E-1 / D1 门坐标**：J 调用方侧位点在位核对（§0.2），属地在 E/H，零触碰。
- **R32-A 哨兵方法 / R32-B 闭合稳定性方法**：作为方法应用于上列核对，不铸「我检查过」新面。

### 2.5 枚举收口

零未点名站点（§2.1 29/29 文件全归属）；零新站点级换名记录（§2.2 第一组十二项按令复命中即止、第二组三角度全拒收，R34-J 四项记录维持不重铸）；放大因子收口 < 1 ms/命令（§2.3）；静态哨兵 0 命中（§2.4）；重锚九格全为冻结 0 行下的宿主窗（§1.1 host-fast 同向齐移判读）；J1 sim 2468 全绿（§1.2）。**没有可立 ID 的新角度。不铸 S36-J-\*，不铸新 face 名，不取绰号，不写 EXCLUSIONS 行。**

## 3. 测试与验证

- 聚焦切片套件（`test/unit/{cluster,privacy,preferences,episode,persist,track,context,feedback}`，node:test via tsx）：**tests 91 / suites 4 / pass 91 / fail 0**（`c92d647` 树，nvm v22.22.2）。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0**（`c92d647` 树）；**tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1**，与 R14-E…R36-I 历史计数逐字相同。临时重锚 harness 在每次 gate 运行前均已从仓库树删除（附录 A 纪律）。
- J1 sim：**2468/2468 逐位通过**（§1.2）。
- 硬性不变量逐条复核：分析不改在途 run；tracking 无指挥权；H/score 不写路由 PASS/FAIL；live = R0 等价（R1 未接线）；双 LCB / 双归因在位；promotion proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；CAS / 凭证 / 数据面合同 / 公开签名零触碰；**无 fsync / 完整性再哈希去除**（§2.4 直证）；fail-open 家族保持关闭；S5-H-1 / S7-F-1≠S6-F-5 / S8-I-1 / S9-D-4 / S12-D-1 / S8-E-1 / S12-B-2 / S13-B-1 未重开未代跑（属地在姊妹区）；catalog 行序契约（R28-B W3）与 R28-I npm `.bin` symlink 静默 no-op 均未触碰；file-lock `retryMs ?? 10` 原样；**J1 fold 形状锁定原样**。
- 仓库变更仅本报告一个文件：`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` = 本文件新增；重锚 harness 未入库（附录 A）。不开 PR。

## 4. 前后对比

无生产代码变更，无前后对比对象。切片 I/O 地板带维持 R34-J/R35-J 记载（本轮九格 ×3 遍复测见 §1.1，本 VM 轻度 host-fast 窗如实注记）。

## 5. MORE_OPTIMA 判定

- 29 文件零未点名站点；R35-J 十二项重赛角度复命中即止 + 本轮三角度全拒收、零新站点级换名记录；R34-J 四项记录 + 既有 S\*-J-\* 全系原形在位；重锚九格全为宿主窗（冻结 0 行 + 同向齐移 + 极端慢窗消失三重证据）；J1 sim 2468 全绿；每命令持久成本上界 < 1 ms，距落地线 1.5–2 个数量级，且其组成已逐段归属既有 ID / 契约面。
- 判定：**MORE_OPTIMA=no**。

## 6. 新增排除 ID：无

不铸 S36-J-\*；不新增 EXCLUSIONS 表行；空枚举轴不取绰号；不重铸 J1 / R34-J 四项记录 / R35-J 十二项重赛角度 / R32-J 六坐标 / LINKTOP / MODEX / digest 轴 / 姊妹空枚举方法。

## 附录 A：重锚原始输出（harness 未入库；与 R12-J 附录 A 除头注释与 tmpdir 前缀 `r12j-`→`r36j-` 外逐字同源）

```text
# pass 1 (c5fe985 树)                              # node v22.22.2
saveToDisk 50-obs seed: 256.8 / 188.0 / 157.4 us/write
saveToDisk 500-obs seed: 511.0 / 442.1 / 416.4 us/write
jsonl fsync=false: 64.2 / 65.1 / 59.6   fsync=true: 380.2 / 242.4 / 240.5 us/append
cascade MATCH: 708.7 / 633.1 / 626.3    NO-MATCH: 275.6 / 271.2 / 302.9 us/cascade
buildProjectContextIndex: 40.8 / 41.0 / 40.9 us/build
plan counter: 1.12 / 1.11 / 1.12        plan randomUUID: 2.63 / 2.62 / 2.62 us/plan

# pass 2 (c92d647 树；src/ 与 pass-1 位级同一)      # node v22.22.2
saveToDisk 50: 267.2 / 181.5 / 133.0    saveToDisk 500: 507.3 / 413.1 / 412.4
jsonl false: 64.8 / 64.5 / 60.6         jsonl true: 246.8 / 271.0 / 252.1
cascade MATCH: 717.0 / 664.9 / 638.9    NO-MATCH: 277.2 / 286.3 / 254.1
context: 40.3 / 40.3 / 40.3             plan: 1.05 / 1.08 / 1.05   uuid: 2.56 / 2.53 / 2.48

# pass 3 (c92d647 树)                              # node v22.22.2
saveToDisk 50: 190.0 / 209.1 / 132.0    saveToDisk 500: 534.9 / 423.7 / 419.7
jsonl false: 64.6 / 65.1 / 61.3         jsonl true: 296.0 / 298.9 / 340.3
cascade MATCH: 716.5 / 657.5 / 623.0    NO-MATCH: 278.8 / 286.3 / 261.8
context: 40.5 / 40.4 / 40.3             plan: 1.02 / 1.03 / 1.02   uuid: 2.59 / 2.58 / 2.52

# J1 sim (scripts/r1j-equivalence-sim.ts, c5fe985 树)
scenario 1 (deterministic edge cases): 64 checks over 16 cases
scenario 2 (randomized): 2400 checks over 600 cases
perf fixture (N=6000, multi-subject, ~25% tombstoned):
  reference 9997.7 ms -> current 3.5 ms (2823.6x)
  reference element visits per evaluation: 92,590,419
ALL EQUIVALENCE CHECKS PASSED (2468 bitwise checks)
```

---

MORE_OPTIMA=no
BRANCH=cursor/r36-j-persist-thirty-sixth-pass-83a1
