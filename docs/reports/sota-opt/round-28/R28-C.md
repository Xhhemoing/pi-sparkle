MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 28 / R28-C：离线路由切片第二十八遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 28 / R28-C（Round 1–27 同区第二十八遍，叠在
  已落地的 S7-C 之上）
- **基线:** 分支创建与全部测量在 `cursor/sota-persistent-opt-83a1` @
  `57c2b8e`（R27-H 空枚举合入 + Round 27 关轮 + R28-C 派单后的独占分支
  tip）；提交前独占分支快进两次至 `5ef5379`（`683b1aa` R28-B 空枚举合入 +
  R28-D 派单，`5ef5379` R28-A 空枚举合入 + R28-E 派单，均 docs-only），
  报告分支已 rebase 通过并在新 tip 上重验冻结。`git log -1 -- src/routing/`
  显示切片最后一次生产改动**仍为 `183df9b`「perf(routing): land S7-C
  support-only eta」**；`git diff 183df9b..HEAD --` 对 9 个切片文件为
  **空**（stat 空输出、exit 0，`57c2b8e` 与 `5ef5379` 两个 tip 上各验一次），
  两次快进均未触及 `src/`，测量形态与提交形态位级同一。R28-A / R28-B
  轴已核对并入非移植清单（§4），未等待、未触碰其分支。
- **分支:** `cursor/r28-c-offline-routing-twenty-eighth-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，wc -l = 170/168/311/585/254/156/88/130/177 = 2,039；含 S7-C 去重分支内支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开现场核对）
- **模型:** `claude-fable-5-thinking-xhigh`

## 结论

**第二十八组诚实枚举为空：不铸任何新 ID，本轮生产代码零改动。**
对 9 个源文件逐行第二十八遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R27-C 的全部无号关闭
探针与轴，含 R27-C 的 PROBX、R26-C 的 PRICEX、R25-C 的 AGEX、R24-C 的
OCCX、R23-C 的 ORDX、R22-C 的 SEEDX、R21-C 的 YMIX、R20-C 的 SCALEX、
R19-C 的 FITQ）后，本轮**从未点名站点数为零**（连续第十遍）——每一个
执行站点都能指到既有 ID、无号探针、零池备忘或池算术覆盖。R26-C/R27-C
已把切片公开输入面的**全部数值坐标**定价完毕（观测行六维 + 货币维 +
概率对 + 验证器报告面），输入值轴域穷尽；本轮新增的完备性轴因此落在
**输入值之外的执行坐标**上，并按派单预授权条件执行（证明了与 R27-H
不同的 C 区对象后才开轴）：

- **execution-substrate face 的 C 区数值内核实例化（新轴一次性收口，
  非候选；不铸 ALLCAPS 绰号、不铸 ID——face 名归 R27-H 所有，本轮只
  在 C 区对象上补一格剂量）**——R1-C..R27-C 的**每一个生产读数**都
  测在唯一一种基片形态上：tsx CLI 装载 `src/*.ts`（esbuild 转译）。
  而真实生产 bin（`pi-sparkle` = `node dist/cli/main.js`）跑的是
  **node + tsc ES2024 emit**。基片坐标对 C 区宿主（201 拟合 / 8,966
  次 solveSymmetric / 3,586,400 行访问 / ~6.45 亿次冻结顺序浮点的
  ~660 ms 热数值内核——**非 R27-H 的 ns 域 eval/requirement emit
  站点**）从未剂量化。本轮三臂剂量（S = tsx+src 正典测量基片；D =
  node+dist 生产基片；SD = tsx+dist 分解臂，装载器与代码形状二因子
  拆开），**runner 逐字节同一文件**，臂只由解释器 + 模块路径选择，
  交错 3 轮 × 3 臂 = 9 个独立进程：
  **位面**——9/9 进程的全报告 SHA-256 摘要（perf + small + 3 电池
  形状 + 确定性重跑，数值按 IEEE-754 位型折叠）**逐字节相同**
  （`63e74db4…a893`）：基片坐标无法移动任何一个输出位（同一 V8，
  ES2024 tsc emit 与 esbuild 转译执行等同算术）。
  **热层**——perf 形状 7-rep 中位：S 660.4 / 658.2 / 656.4，D 663.0 /
  660.4 / 660.7，SD 661.2 / 659.6 / 658.4 ms；同轮差 D−S = +2.6 /
  +2.2 / +4.3 ms（比率 1.003–1.007），SD−S = +0.9 / +1.4 / +2.0，
  D−SD = +1.7 / +0.8 / +2.3——全部与臂内跨轮散布同量级（S 臂自身
  跨轮 4.0 ms），**离 ±35 ms 带 ≥8×**；小形状差符号翻转（−0.27 /
  −0.07 / +0.59 ms）= 纯抖动。装载器腿（SD−S）与 emit 腿（D−SD）
  各自亚噪声，二因子分解无隐藏抵消。
  **冷面**——node+dist 切片子树导入 2.0–2.2 ms（进程墙钟 24–27 ms），
  tsx+src 7.0–8.5 ms（进程墙钟 158–163 ms）⇒ 基片冷差 **+134–137
  ms/进程，落在测量装具一侧**（生产 bin 本就在便宜臂上）——
  once-per-process 派单拒绝类，与 R27-H 冷读数（+174–183 ms/进程）
  同类同向，只记名不立道。
  **收口判定**：热层剂量响应深亚带（对 660 ms 级 JIT 主导数值内核，
  基片比率坍缩到 ~1.00——与 R27-H 在 ns 域站点测到的 1.004–1.239
  形成量纲互补），位面全等，冷面拒绝类 ⇒ **历轮全部 C 区锚点对生产
  基片在噪声内原样有效，无需重锚**；且即使响应越带也无可落地物
  （构建面/平台面冻结，S9-C-2/S6-I-2 族禁区——基片不是 9 文件内的
  代码自由度）。轴一次性收口，非候选。
- **APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠）**
  ——本轮四次读数（1 预演 + 3 正典）：APCX 无计时器独立道 **58.3 /
  58.8 / 61.8 / 58.9 ms**（与 R27-C 带 53.8–59.2 在 [58.3, 59.2]
  交叠），FLOORX 最廉价逐位合法形态地板 **32.6 / 31.6 / 37.5 /
  41.7 ms**（与 R27-C 带 34.0–40.7 交叠）⇒ **组合天花板 = 25.7 /
  27.3 / 24.3 / 17.3 ms < 35**——其中 25.7 / 27.3 两读数高出
  R16-C..R26-C 历史读数域 [12.1, 25.6] 上沿 0.1–1.7 ms（FLOORX 地板
  道本轮跨运行散布 31.6–41.7 驱动），按 R14-C 处置规则**随读重锚为
  本轮带 17.3–27.3**；距 35 ms 带最近读数仍差 **7.7 ms**，组合关闭
  维持。**sink=7.309 与 floorSink=7.309 八读数全同，且与
  R14-C/R16-C..R27-C 打印值逐位同值**；每次运行内
  `Object.is(apcSink, floorSink)` 断言全绿（两道 65 遍扫掠累加双精度
  逐位相等）——派单强制的跨轮确定性旁证成立。

除上述轴与天花板重测外，第二十八遍逐行重读**零新发现**：其余每一个
执行站点都落入 R12-C 确立的四分类（重命名 / 亚带池 / 逐位违例 /
结构交换负优化）+ R18-C 补充的 once-per-fault 拒绝类。生产中位与
地板复核见 §1/§3：本 VM 上生产中位 **660.1–661.3 ms/报告（四次运行
661.3 / 661.2 / 660.3 / 660.1；三次正典 660.1–661.2）**——与 R27-C
带 660.6–668.4 在 [660.6, 661.3] 交叠（贴 R27-C 正典下沿，与本 VM 类
整体微快一致，同 VM 独立旁证：r7c 回归资产锚点 **production
662.3 / 661.3 ms = 1.12× vs 冻结 S6-C ctl 737.9**，方向幅度与 R27-C
读数 658.9/738.3 同构），按 R14-C 处置规则随读重锚为本轮带
660.1–661.3；非切片回归或加速（切片对 `183df9b` 零 diff）。小形状档
整报告中位 **30.0–30.2 ms**（R27-C：32.2–32.9——同向 VM 微快，亚
噪声），整条报告低于噪声带（R7-I 矩阵义务履行）。基片轴的 S 臂中位
（656.4–660.4）与主锚带交叠——两测量装具互证。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB
（Beta vs 正态）与双归因（offline-logit vs offline-prob-add）全部保留
（X0-11，`writeAttributionPair` 两路调用本轮 rg 复核在位），live 面零
文件改动（`src/supervisor/` 对 9 文件导入为零，本轮 rg 复核）。仓库
变更仅本报告一个文件；无生产赢家故未提交 `/tmp/r28c/` 三个临时脚本
（全文进附录 A，sha256 归档后已删除）。

## 0. 范围与约束遵守

- 读序（强制）已履行：README → EXCLUSIONS.md（完整表，含全部
  S\*-C-\* 行与 S12-C-1、S13-B-1、X1-3/X3-4/X3-5/X4-2 posterior/
  propensity/bandit 行）→ PROGRESS.md → round-28/PLAN.md（C 段 +
  全部 plan-ban 轴；独占 tip `57c2b8e` 已含 R27-H execution-substrate
  face 与 Round 27 关轮）→ R27-C.md（空枚举确认，无 S27-C-\*；PROBX
  轴收口与「切片公开输入面全数值坐标定价完毕」裁决域勘定——本轮轴
  因此落在输入值之外的执行坐标上）→ R27-H.md（execution-substrate
  face 判例：宿主 = H 区 eval/requirement emit 热层 ns 域站点 + 冷
  导入面；跨基片热层比率 1.004–1.239、|Δ| 亚 µs；冷 +174–183 ms/
  进程 once-per-process 拒绝——派单预授权条件「证明不同的 C 区对象
  （离线数值内核）」本轮已满足，见 §4）→ R26-C.md（PRICEX）/
  R25-C.md（AGEX）轴定义核对（经 EXCLUSIONS 完整表逐条比对，证本轮
  轴非其重铸：零价格剂量、零时间剂量、零概率剂量——fixture 全程
  冻结在文档化生产形态）→ 9 个切片源文件全量重读 → rebase 后补读
  R28-B.md（catalog-face row-arrangement face）与 R28-A.md（input
  key-set geometry face）合入摘要及更新版 PLAN.md（两轴均已列入
  plan-ban 段与 C 段禁移植名单）。
- 基线漂移检查：本轮测量期起点 = 分支创建点 = `57c2b8e`；测量全程
  独占分支未动，提交前复核发现两次 docs-only 快进（`683b1aa` R28-B
  合入 + R28-D 派单、`5ef5379` R28-A 合入 + R28-E 派单），按派单
  规程 rebase 报告分支通过至 `5ef5379` 并重验冻结。冻结主张三次
  核验：`git diff 183df9b..<tip> --stat` 对 9 个切片文件为空——
  分支创建时（`57c2b8e`）、提交前（`57c2b8e`）、rebase 后
  （`5ef5379`）各一次；两次快进仅触 docs/reports，测量数字无需
  重采。R28-A / R28-B 轴已入 §4 非移植清单。
- 调用图确认（派单强制项，本轮 rg 复核）：`routeR1` 的仓内调用方仅
  `src/routing/r1-shadow-report.ts` 与 `src/experiments/shadow-compare.ts`
  （均离线面）；`fitLogitAdditive` 与 `fitProbabilityAdditive` 均由
  `src/experiments/attribution-report.ts` 的 `writeAttributionPair`
  消费（双归因两路在位）；`src/supervisor/` 对 9 个切片文件的导入为
  **零**——live = R0 等价维持，R1 未接线，
  `src/supervisor/model-router.ts` 零字节未触。
- 基片对象勘定（本轮新轴的对象域）：生产 bin 面 `package.json` 的
  `bin.pi-sparkle = dist/cli/main.js`（node + tsc emit），`build =
  tsc -p tsconfig.build.json`（target ES2024 ⇒ emit 为类型擦除近原样
  输出，switch 直加/循环展开/行引用提升全部原样保留——附录 A 抽查
  dist/routing/offline-logit.js case 5 体逐语句同构）；历轮 C 区测量
  装具 = `tsx`（esbuild 转译 src/*.ts）。两基片之间的热层/位面/冷面
  三响应本轮全部直测（§2）。
- 禁止重开项零触碰：S7-C 本体未回退未重写未另起平行 eta/dot 路径
  （X2-1 一般实值设计维持排除；共享 `dot()` / APC / on-prob 零字节
  改动）；X0-\*..X4-\*、S1-\*..S27-\*（含 S12-C-1，重开条件：无）
  未重提、未立道、未重测；R13-C..R27-C 二十七个无名关闭（PIVCSE/
  FINTAIL/REFZ/TSORT/DEG/SWORD/XF64/IRLSDP/CMPH/STORD/RIDGE/ICOL/
  SFILL/NSQRT/PMV/OSTZ/TAILG/REJX/FITQ/SCALEX/YMIX/SEEDX/ORDX/OCCX/
  AGEX/PRICEX/**PROBX**）未重探未补铸未换址重提——**SCALEX 未重测、
  YMIX 未重测、SEEDX 未重测、ORDX 未重测、OCCX 未重测、AGEX 未重测、
  PRICEX 未重测、PROBX 未重测**（基片轴全部剂量点都在冻结 fixture
  之上：同一 0xbeef/0x51a1/0x5eed 构造字节、同一发射次序、同一
  options、零概率/货币/时间坐标移动——摘要全等本身就是八输入维
  恒等的位级见证；三臂只换解释器与模块路径，fixture 生成代码在
  runner 内逐字节同一）；FITQ 重开条件（bootstrap 抽取数或拟合规模
  位移 ≥1 个数量级）未被触发：options 全程默认值/文档化 PERF 值；
  ITERX/COLDX/AAFLR/GCAX 四轴未补铸（热层读数取 7-rep 中位，首 rep
  的 705–733 ms 冷 JIT 尾巴被中位吸收不进入读数——COLDX 的进程内
  预热份额口径未重测；冷面读数是**跨基片进程级**导入价，业已按
  once-per-process 拒绝类记名）；嵌套序 × 布局组合空间（S4-C-2 +
  S5-C-1..4 + S12-C-1）未重开；CMB 组合未重堆叠（本轮 APCX/FLOORX
  重测系派单强制天花板复测，FLOORX 逐位合法性 59 名额外断言全绿）；
  PLSEG/PMV/TAILG 备忘未重提；R24-I adaptation-state coordinate 邻域
  的 config 坐标（halfLife/minSamples/priors/lcbKind + bandit/shadow
  config）维持 R27-C 诚实划界，零剂量。
- 硬不变量全部满足（生产零 diff 下天然成立）：分析不改在跑 run；
  tracking 无指挥权；H/score 不写 routing PASS/FAIL；live=R0 等价
  （R1 未接线）；双 LCB 与双归因两路一行未删；promotion
  proposal-first；阈值 / 测试 / 公开签名 / CAS / 凭据 / 数据面契约
  不动；X2-3 数值路径未触；单线程逐位审计基底不动；fail-open 族维持
  关闭；无 fsync / 完整性再哈希移除（本切片不涉盘，天然成立）。
- 环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0——回到
  R1-C..R27-C 的处理；VM 默认 22.14.0 未用于任何读数）、pnpm 10.17.1、
  `pnpm install --frozen-lockfile`（`pnpm gate` 全绿，见 §5）。三臂
  基片剂量共享同一 Node 22.22.2 运行时（引擎代坐标冻结——基片轴
  动的是模块管线，不是引擎版本）。

## 1. 生产中位与锚点复核（本 VM 实测，未拷贝历轮数字）

本轮测量装具为**复刻道保真电池 + 未仪表化生产导入直测**：主锚脚本
携带落地管线的逐语句复刻道（buildDesign / computeSupports /
canonicalRowKeys / irls / onProbabilitiesFor /
averagePredictiveComparison / percentile 全文照抄），其忠实性先经
**34 例形状电池 + perf + small 全报告 vs 生产 `fitLogitAdditive`
导入逐位全绿**（Object.is 含符号零/NaN，每运行 2,768 项检查）再
采信；生产中位数与全部基片轴读数均由**未仪表化生产导入**直接测得。
**范围注记（诚实划界）**：本轮未重建 R27-C 型逐池仪表化复刻管线
（派单强制项为生产中位 + APC 天花板重锚，均已履行）；池结构证据
链为（a）切片对 `183df9b` 零 diff ⇒ 池组成不可能移动，（b）站点
计数在本 VM 本轮由回归资产独立重印——r2c 打印 **3,586,400 行访问 /
8,966 次迭代 → 1,249,652 去重 dot**，r7c 打印 **rowVisits=3586400 /
dedupDots=1249652 / dotTerms=74979120 / supportAdds=5563132**，r1c
打印 **design.build 每拟合 358,006 次**——与 R10-C..R27-C 锚定值
逐位相同（跨轮确定性旁证）。

perf 夹具与 R10-C..R27-C 完全一致（fixtureRng(0xbeef)，4 场景 ×
6 模型 × 8 项目，rows=400, passRate=0.6，bootstrap=200,
seed=20260818 → p=60，201 拟合）；小形状档同一 0x51a1 夹具（3×3×3 /
120 行，bootstrap=200）。

本轮共四次全量运行：run 0 为**预演运行**（脚本与正典运行**逐字节
相同**——预演后零改动，四次运行同一脚本，sha256 见附录 A），run A /
run B / run C 为正典运行。四次运行全部计入处置依据，不作剔除；每次
运行 2,768 项逐位检查全绿。**披露**：预演之前另有一次**废弃试跑**
（探针协议错配：小形状夹具误用 0xbeef/2 场景、sink 累加器未按
R14-C 谱系 65 遍扫掠口径），其读数（生产中位 664.5 ms，带内）**不
计入任何处置依据**；协议修正后脚本冻结，run 0..C 零改动。

生产整报告中位（每次运行内 7 测量取中位）：**661.3 / 661.2 /
660.3 / 660.1 ms**——四读数带 660.1–661.3 与 R27-C 带 660.6–668.4
在 [660.6, 661.3] 交叠，贴 R27-C 正典下沿（方向与本 VM 类整体微快
一致，同 VM 独立旁证：r7c 回归资产锚点 **production 662.3 / 661.3
ms = 1.12× vs 冻结 S6-C ctl 737.9**——R27-C：658.9/738.3 同构），
按 R14-C 处置规则**随读重锚为本轮带 660.1–661.3（正典
660.1–661.2）**；判为 VM 微架构漂移带内往返，非切片回归或加速
（切片对 `183df9b` 零 diff）。

各运行 7-rep 明细：

```text
run 0: median 661.3  (reps 663.0 670.4 667.0 661.3 661.0 658.5 660.9; small 30.1)
run A: median 661.2  (reps 661.3 667.5 657.1 660.2 658.3 661.8 661.2; small 30.0)
run B: median 660.3  (reps 661.2 666.9 663.0 656.9 658.4 660.3 658.0; small 30.2)
run C: median 660.1  (reps 661.7 663.9 659.6 657.5 660.1 660.5 659.4; small 30.2)
```

小形状档整报告中位 **30.0–30.2 ms**（R27-C：32.2–32.9——同向微快，
亚噪声）——小配置态整条报告低于噪声带，调用矩阵小态一侧整体收口
维持（R7-I 矩阵义务履行）。生产自确定性复核：3 seed × 各 2 次运行
全字段 Object.is 全绿（每运行含于 2,768 项）。基片轴 S 臂（同装具
异进程）中位 656.4–660.4 与主锚带交叠——双装具互证。

## 2. 第二十八组枚举（诚实结果：空；零新站点 + 一条新轴 + 一次按规天花板重测）

枚举方法：9 文件逐行重读，对每一个执行站点问三问——（1）是否已有
排除条目或其重命名/重选址；（2）若全新，其整池算术上界是否越带；
（3）若上界越带，其机制是否保逐位且实测符号稳定越带。**没有任何
站点三问全过，且本轮从未点名站点数为零**（连续第十遍）：

| 站点/轴 | 关闭方式 | 证据 |
| --- | --- | --- |
| 从未点名站点普查：**零**（连续第十遍） | 逐行三问全覆盖 | 28 遍扫描把 9 文件每一个执行站点指到既有覆盖物：offline-logit 编排头（DEG/BASE）、irls 全站点（S2-C/S3-C/S6-C/S7-C 落地体 + S10-C-1/S9-C-3/S9-C-4/S6-C-5/6/S5-C-7/S4-C-5/6/MUCSE/SWORD/STORD/RIDGE/ICOL/IRLSDP + 收敛尾 S2-C-3/S8-C-3/NSQRT + ITERX/FITQ）、solve 全站点（COPY/S4-C-3/XF64 + PIVCSE/S5-C-6 + PIVSWAP + FZERO + S4-C/S5-C 落地体 + S12-C-1 + S6-C-7 + SFILL/FINTAIL + S8-C-4）、APC（S1-C-1/S2-C-2/S2-C-4/S4-C-4/S8-C-2/S10-C-2 + FLOORX 天花板）、on-prob（S7-C-4/S8-C-1）、采样环（SAMP/S3-C-3/S2-C-5）、抽取效应环（S7-C-1/DMAP）、尾部效应环（TSORT/CMPH/REFZ/TAILG）、posterior（X1-3/X3-4/S1-C-7/OSTZ/S1-C-4 + 落地 memo + AGEX 封顶）、prob-add（S1-C-6/PMV/PLSEG）、propensity（S1-C-8/X3-5 + PROBX 四面封顶）、r1（X1-4/S1-C-10/S1-C-2/3 + PRICEX 封顶）、bandit/shadow（S1-C-9/X4-2 + R27-C 划界读数 ns 域）、r1-shadow-report（X1-5 + REJX + PRICEX selectedCost 封顶）——R18-C「TAILG 是尾部段最后一个从未点名位置」的判定经 R19-C..R27-C 与本遍连续十轮核实成立；观测行六维 + 货币维 + 概率对 + 验证器报告面关死后（R26-C/R27-C 裁决），**切片公开输入面的数值轴域已穷尽，本轮零位移** |
| execution-substrate face 的 C 区数值内核实例化（新轴，非候选；不铸绰号不铸 ID——face 名归 R27-H；派单预授权：「证明不同的 C 区对象（offline numeric kernels）」已满足） | 位面 9/9 摘要全等 + 热层三臂交错直测深亚带 + 装载器/emit 二因子分解各自亚噪声 + 冷面 once-per-process 拒绝类 + 无可落地物（构建面冻结） | **对象**：C 区热数值内核（201 拟合 / 8,966 solve / 3,586,400 行访问 / ~6.45 亿冻结顺序浮点，~660 ms/报告），响应变量 = 整报告中位 + 全输出位型——**非** R27-H 的 H 区 ns 域 emit 站点/每调用比率。**剂量设计**：三臂 {S=tsx+src（R1-C..R27-C 正典测量基片）, D=node+dist（生产 bin 基片，tsc ES2024 emit）, SD=tsx+dist（分解臂）}，runner 逐字节同一（臂仅由解释器+模块路径选择），交错 3 轮 × 3 臂 = 9 进程。**位面**：每进程对 {perf, small, 3 电池形状, perf 确定性重跑} 六报告做 IEEE-754 位型折叠 SHA-256——9/9 摘要 `63e74db4…a893` 全同 ⇒ 基片坐标移不动任何输出位（同 V8；tsc emit 与 esbuild 转译等同算术；摘要全等同时见证 fixture 八输入维恒等与进程内自确定性）。**热层**：S 660.4/658.2/656.4，D 663.0/660.4/660.7，SD 661.2/659.6/658.4 ms；同轮 D−S = +2.6/+2.2/+4.3（比率 1.0039/1.0033/1.0066）、SD−S = +0.9/+1.4/+2.0、D−SD = +1.7/+0.8/+2.3——与臂内跨轮散布（S 4.0 / D 2.6 / SD 2.9 ms）同量级，**离 ±35 ms 带 ≥8×**；小形状差符号翻转（−0.27/−0.07/+0.59）= 抖动；装载器腿与 emit 腿各自亚噪声，无隐藏抵消。**冷面**：dist 子树导入 2.0–2.2 ms / 进程墙钟 24–27 ms vs tsx+src 7.0–8.5 ms / 158–163 ms ⇒ 冷差 +134–137 ms/进程 **落在测量装具一侧**（生产 bin 已在便宜臂）——once-per-process 派单拒绝类（R27-H 冷读数同类同向），只记名。**收口算术**：热层最大同轮差 4.3 ms ⇒ 越带需响应再放大 8×，而三轮方向虽同号（D 微慢）幅度全部落在臂内散布里；即便未来响应越带，基片不是 9 文件内代码自由度（S9-C-2/S6-I-2 族构建/平台面禁区），唯一后果是锚点重标定而非候选。**判定**：历轮 C 锚点对生产基片在噪声内原样有效；轴一次性收口。重开仅当构建管线换代（tsc target 降级 / bundler 引入 / 引擎跨大版本）使热层比率实测越出 [0.95, 1.05]——属构建面事件，不预期 |
| APCX/FLOORX 天花板重测（派单强制项，非候选、非重开、非堆叠） | 天花板仍亚带 + sink 逐位同值 + FLOORX 逐位合法性 59 名断言全绿 | APCX **58.3 / 58.8 / 61.8 / 58.9**（与 R27-C 带 53.8–59.2 在 [58.3, 59.2] 交叠）；FLOORX **32.6 / 31.6 / 37.5 / 41.7** ⇒ 天花板 **25.7 / 27.3 / 24.3 / 17.3 < 35**——25.7/27.3 两读数越出 R16-C..R26-C 历史域 [12.1, 25.6] 上沿 0.1–1.7 ms（FLOORX 地板道散布驱动；APCX 道自身带内），按 R14-C 规则随读重锚为本轮带 17.3–27.3，距 35 带最近读数仍差 7.7 ms，组合关闭维持；**sink=7.309 与 floorSink=7.309 八读数全同，且与 R14-C/R16-C..R27-C 打印值逐位同值**，每运行内 `Object.is(apcSink, floorSink)`（65 遍扫掠累加双精度）全绿——派单强制的 APC 关闭维持条件成立；S8-C-2/S10-C-2/S2-C-4/S4-C-4 与 CMB 禁令全部维持原状 |

以上无一构成候选：从未点名普查结果为零（无新站点可立道）；基片轴
是执行坐标定价不是变换（三臂全部执行同一落地形态，零变体决斗——
其唯一潜在「候选族」= 换构建管线，属构建/平台面禁区且收益上界即
本轮实测 2–4 ms 亚噪声）；APCX/FLOORX 是按规天花板测量不是变换。
**故第二十八组候选集为空，不铸 S28-C-1 及后续编号**，遵循
R11-C..R27-C 无号关闭先例。

**枚举收口：** 除上表外，第二十八遍未再发现任何未点名站点。逐文件
裁决维持 R10-C §3 表格原样（posterior / offline-prob-add / r1 /
r1-shadow-report / propensity / bandit / shadow 七文件每函数亚 ms
级、每报告一次或 M≤10 常数域，全部由既有 ID 或池算术覆盖）。R27-C
「切片公开输入面上不存在从未定价的数值坐标」的裁决本轮零位移地
维持；本轮把定价域第一次扩到**输入值之外的执行坐标**（基片），并
证明该坐标对本切片三个响应面（位/热/冷）全部无越线点。仍不剂量化、
不铸名的坐标（诚实划界，继承 R27-C）：options 坐标（maxIter/
bootstrap——契约面）、config 坐标（halfLife/minSamples/priors/
lcbKind + bandit/shadow 的 seed/epsilon/explorationBudget/
comparisonBudgetUsd/comparisonCostUsd——config 面，R24-I 邻域禁
移植域）、`TaskFeatures.contextTokens/outputTokens`（探索面 ns 域
划界封顶）、字符串身份坐标（IDG 冻结）、引擎版本坐标（本轮三臂
共享 Node 22.22.2——引擎代属 R13-G/R15-\* 引擎轴域，非本切片自由
度）。本轮核实换名再提交拒列：零。

## 3. 三大池地板与组合出路复核

- **solve 池（本轮未逐池重仪表；结构证据 = 零 diff + 站点计数跨轮
  逐位）**：消元主体 ≈ 6.45 亿次冻结顺序浮点乘减/报告，8,966 次调用
  计数由 r2c/r7c 在本 VM 本轮重印逐位相同。R6-C §2.5 七类分解 +
  S10-C-3 + S12-C-1（嵌套序，重开条件：无）+ R11-C COPY + R12-C
  FZERO/PIVSWAP + S5-C-6 + R13-C PIVCSE/FINTAIL + R14-C XF64 +
  R16-C SFILL 之后本轮零新位置。**基片维度对该池零触达**（本轮位面
  见证：solve 输出经由报告位型 9/9 摘要全等；热层差 2–4 ms 若全部
  记在 solve 池头上也不足带宽 1/8）。触碰地板本体的机制只剩 SIMD/
  线程（S9-C-1/2 + 不变量禁止）与数值路径改道（X2-3 冻结）。
- **irls 自身池**：可省项全部已关闭（S2-C/S6-C/S7-C 落地本体之上：
  S10-C-1 决斗带内、S9-C-3 符号翻转、S6-C-6 上界 2–4、S5-C-7 无正
  决斗、S9-C-4、S4-C-5/6、S2-C-3、S8-C-3、R11-C RID/CNT/EXP/ALLOC、
  R12-C MUCSE、R14-C SWORD/IRLSDP、R15-C STORD/RIDGE、R16-C ICOL、
  R17-C NSQRT）。行访问/去重 dot 计数（3,586,400 / 1,249,652）本轮
  重印逐位。零新位置。
- **APC 池**：派单强制天花板重测照常执行（§2 表）：APCX 无计时器
  独立道 **58.3–61.8 ms**（**sink=7.309 与 R14-C..R27-C 逐位同值** =
  跨轮确定性旁证成立）；FLOORX 最廉价逐位合法形态地板 **31.6–41.7
  ms** ⇒ **组合天花板 = 17.3–27.3 ms（四次读数）< 35**。十三轮天花板
  读数 12.1–23.8 / 13.8–21.6 / 12.1–20.9 / 13.9–24.0 / 17.4–21.6 /
  12.5–19.8 / 17.1–21.8 / 14.6–16.0 / 15.9–22.7 / 20.8–25.6 /
  19.5–22.9 / 15.7–21.0 / **17.3–27.3** 稳定亚带（本轮上沿 27.3 系
  FLOORX 道单读数 31.6 拉宽差值所致，同运行 APCX 58.8 带内；四读数
  全部 < 35 且散布覆盖历史域），组合关闭维持。
- **引擎侧三轴（R15-C/R16-C 收口，本轮未重测未补铸）**：COLDX 预热
  份额、GCAX 收集器份额、ALLOC 分配封顶——三轴均无切片内可受理
  变换；本轮全部热层读数为 7-rep 中位稳态口径（首 rep 705–733 ms
  冷尾巴被中位吸收）。**基片轴与 COLDX 正交**：COLDX 是同基片进程
  内预热份额，本轮冷面是跨基片进程级导入价（once-per-process 拒绝
  类，落在装具侧）。
- **拒绝面（R18-C REJX 轴收口，本轮未重测未补铸）**：31 终点
  once-per-fault 定价原样引用；四分类 + once-per-fault 拒绝类继续
  穷尽。
- **跨池组合**：成员全为已关闭 ID（R10-C CMB 实测带级摆幅带内），
  维持关闭。本轮探针无一是稳定越带正项（普查为零、基片轴是执行
  坐标定价、APCX/FLOORX 是天花板测量），无新成员可入组合。

推翻本轮裁决需要：（a）表所有者层级契约变更（公开异步签名 / 构建
面开放 / X2-3 数值路径解冻 / 单线程审计基底变更 / maxIter=50、
tol=1e-8、bootstrap/seed 契约、rows/OutcomeObservation 输入契约
变更——均不预期）；（b）现实规模位移触达 SCALEX 标定的越线点
（今日生产流量在 perf 夹具当量及以下，未触达；八个输入维度经
SCALEX/YMIX/SEEDX/ORDX/OCCX/AGEX/PRICEX/PROBX 证明均无越线点存在，
**执行基片维度经本轮证明热层比率 ~1.00、位面全等**）；（c）构建
管线换代使基片热层比率实测越出 [0.95, 1.05]（构建面事件，非切片
自由度，后果是锚点重标定而非候选）；或（d）指出「重命名 / 亚带 /
逐位违例 / 结构交换负优化 / once-per-fault 拒绝」之外的第六类
成本——二十八遍枚举未再发现。

## 4. 重命名检查（派单强制项）

- **本轮基片轴 vs R27-H execution-substrate face（直接前驱，派单
  预授权条件挂在此处）**：R27-H 的对象是 **H 区 eval/requirement
  emit 热层站点**（ns 域每调用比率 1.004–1.239，|Δ| 亚 µs）+ H 区
  冷导入面（+174–183 ms/进程 once-per-process 拒绝）；本轮对象是
  **C 区离线数值内核**（201 拟合 / 8,966 solve / ~6.45 亿浮点的
  ~660 ms 整报告中位 + 全输出位型摘要 + C 切片子树冷导入价）——
  宿主池、响应变量量纲（整报告 ms vs 每调用 ns）、位面见证物
  （IEEE-754 位型摘要 vs emit 字节）三重不同，恰为派单预授权点名
  的「distinct C-slice object (offline numeric kernels, not
  eval/requirement emit)」。**不铸 ALLCAPS 绰号、不铸 ID**——face
  命名权归 R27-H，本轮只是该 face 在 C 区对象上的一格剂量补测，
  结论（热层亚噪声、冷面拒绝类）与 R27-H 同向且量纲互补。非重铸。
- **基片轴 vs COLDX（R16-C）/ ITERX / AAFLR / GCAX**：COLDX 是
  **同基片进程内**预热份额轴；本轮冷面是**跨基片进程级**导入价且
  按拒绝类记名，热层读数全部取预热后中位（COLDX 口径未重测）。
  ITERX（迭代计数普查）本轮只经 r2c/r7c 重印锚定值，未重测。非
  重铸。
- **基片轴 vs S9-C-2（WASM/SIMD）/ S6-I-2（compile cache）/
  S5-I-\*（CLI 惰性化）**：三者是**改构建/装载管线的变换候选**
  （已否决：构建面 + 平台面 + once-per-process 域）；本轮零管线
  改动——三臂全部是仓库现状自带形态（tsx dev 装具 / tsc build
  产物），轴只定价不变换。非重开。
- **基片轴 vs SCALEX/YMIX/SEEDX/ORDX/OCCX/AGEX/PRICEX/PROBX（八
  输入维，全部派单点名禁重铸）**：八维全部作用在**输入数据**上；
  本轮 fixture 冻结（同构造字节、同 options、同发射次序——runner
  内 fixture 生成代码逐字节同一，且 9/9 摘要全等是八维恒等的位级
  见证），动的是**执行基片坐标**（解释器 + 模块管线）。零输入维
  位移。非重铸，八轴未重测。
- **基片轴 vs FITQ（R19-C）**：FITQ 是同基片下拟合粒度墙钟分布；
  本轮无逐拟合计时器，重开条件未触发。非重铸。
- **基片轴 vs R27-I process output-channel disposition face /
  R26-I argv surface-form face / R25-I process-environment
  coordinate（派单点名禁移植）**：三者的剂量对象是 I 区进程的
  输出通道处置 / argv 表面形 / 环境坐标本身；本轮 runner 的
  R28C_MODULE/R28C_ARM 环境变量与单行 JSON 输出是**装具选择器与
  报告通道**（计时区外各读/写一次，非剂量对象），argv 三臂恒空。
  基片轴的剂量对象是模块管线。非移植。
- **基片轴 vs R27-A scalar numeric value-class face / R27-J SIDEC
  （派单点名禁移植）**：R27-A 是 A 区每标量值类价格普查（本轮零
  数字→字符串移动进入计时区），SIDEC 是 J 区删除面侧车库存——
  宿主与机制均在他区。非移植。
- **APCX/FLOORX 重测 vs S8-C-2 / S10-C-2 / S2-C-4 / S4-C-4 / CMB
  禁令**：本轮重测系**派单强制天花板复测**，产出仅用于天花板差值
  算术与 sink 逐位核对；FLOORX 道的 59 名逐位合法性断言是对既有
  S8-C-2 引理的复核不是新道；不重跑任何已关 ID 决斗、不构建堆叠
  候选、不产出可落地形态。
- **基片轴 vs R28-B catalog-face row-arrangement (permutation) face
  （mid-run 合入，PLAN 点名禁移植）**：R28-B 的剂量对象是 B 区目录
  行排列（replay 贴噪声地板；live 1.1–5.3 µs/call once-per-CLI 拒收；
  其 W3 证排列是契约决策承载）。本切片 9 文件内无目录行结构，
  fitLogitAdditive 的行序敏感面已由 ORDX（R23-C，记录间序）定价关闭，
  本轮 fixture 行序全程冻结、零排列剂量。基片轴动的是模块管线坐标，
  与行排列正交。非移植、非重铸。
- **基片轴 vs R28-A input key-set geometry face（mid-run 合入，PLAN
  点名禁移植）**：R28-A 的剂量对象是 A 区调用方对象的结构类型盈余
  宽度 × 插入序 × 属性 backing（生产在轴原点，盈余斜率 ~225–235
  ns/key，µs/ns 拒收）。本切片输入行对象的键集形状面已由具名
  plan-ban SHAPEK（形状基数/混合、schema 精确键集）覆盖，本轮
  fixture 键集逐字节冻结、零键集剂量。基片轴不动任何输入对象几何。
  非移植、非重铸。
- **换名再提交拒列（本轮核实）**：零。R13-C..R27-C 二十七个无名
  关闭未重探、未补铸、未换址重提；S10-C-1..3 / S12-C-1 / RID / CNT
  零重提；X2-1 重写维持排除（S7-C 的 0/1 形态即落地形态）；COTARG /
  VALCLASS / CALLB / SHAPEK / KFAN / JB / RUNLIM / CKMIX / TERMCLASS /
  SCHEDWIN / XPROC / NAMESHAPE / CG / RTL / DELINV / RVL / SIDEC 与
  全部他区无名轴（M=1 catalog collapse、approval-authority、state
  serialization-boundary provenance、operand-residency、
  adaptation-state、identity-coordinate、plan-header
  identity-geometry、injection command-kind、consumer-stage
  string-representation / operand-realm、injection-seam default-arm、
  input storage-state、edge condition-kind、argv surface-form、
  process-environment、collation-locale、value-domain / string
  content-class、scalar numeric value-class、batch-cardinality
  bottom + prelude intercept、policy-content tree-geometry、input
  access-path mechanics、join-policy、process output-channel
  disposition、catalog-face row-arrangement (permutation)、input
  key-set geometry）零移植。

## 5. 测试与验证

环境：Node 22.22.2（nvm 切换满足 engines ≥22.19.0）、pnpm 10.17.1、
`pnpm install --frozen-lockfile`。生产代码零改动，测试文件零改动。

```bash
pnpm gate   # = typecheck && lint && test && build
            # ✓ 全绿（pass 1168 / fail 0 / skipped 1；exit 0）
            # build 产物 dist/ 即基片轴 D/SD 臂的被测模块

# 本轮测量彩排（临时脚本，未入库——无生产赢家，全文见附录 A）
npx tsx /tmp/r28c/r28c-anchor.mts        # ✓ 2,768 项逐位 × 4 次运行全绿
                                         #   （34 例复刻道保真电池 + perf + small
                                         #    + 3 seed × 2 自确定性
                                         #    + FLOORX 59 名逐位合法性
                                         #    + sink===floorSink 双精度断言
                                         #    + APCX/FLOORX 天花板重测内嵌）
# 基片轴（runner 逐字节同一，臂由解释器 + R28C_MODULE 选择；交错 3 轮）
R28C_ARM=S  R28C_MODULE=src/routing/offline-logit.ts  npx tsx  r28c-substrate-runner.mjs
R28C_ARM=D  R28C_MODULE=dist/routing/offline-logit.js node     r28c-substrate-runner.mjs
R28C_ARM=SD R28C_MODULE=dist/routing/offline-logit.js npx tsx  r28c-substrate-runner.mjs
# ✓ 9/9 摘要 63e74db4ac45eed0f2a219e159b5f2d65b9ea8d962408e66858c7b459dea3893

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项（design.build 358,006/拟合）
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项（3,586,400 → 1,249,652 @ 8,966 迭代）
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项 ×2 次运行
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 662.3 / 661.3 ms（1.12× vs 冻结 S6-C ctl 737.9）——与
# §1 生产正典中位带（660.1–661.2）互证。
```

代表性一次测量彩排全量输出（正典 run A；run B/C 与预演数字见 §1/§2）：

```text
scenario 1 (replica-lane fidelity battery, 34 cases + perf + small, bitwise vs production import): OK
scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is): OK
  shape small (3 models x 3 projects, 120 rows): production median 30.0 ms/report (3 reps)
  shape perf (6 models x 8 projects, 400 rows): production median 661.2 ms/report (7 reps)
  perf reps: 661.3 667.5 657.1 660.2 658.3 661.8 661.2
probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) 0.293 ms/fit -> x201 fits = 58.8 ms/report (base-shape approximation; sink=7.309)
probe FLOORX: cheapest bitwise-legal sweep 0.157 ms/fit -> x201 fits = 31.6 ms/report floor; APC combination ceiling on THIS VM = 27.3 ms (floorSink=7.309)

ALL EQUIVALENCE CHECKS PASSED (2768 bitwise checks)
```

基片轴九进程原始读数（交错序：每轮 S→D→SD；digest 九行全同故列一次）：

```text
digest (all 9 processes): 63e74db4ac45eed0f2a219e159b5f2d65b9ea8d962408e66858c7b459dea3893
round 1: S perf 660.373 (reps 725.6 662.4 659.6 680.0 660.4 659.9 660.1; small 30.838)
         D perf 662.963 (reps 728.8 659.4 659.2 680.8 663.0 661.6 664.8; small 30.572)
         SD perf 661.243 (reps 722.0 659.6 659.5 682.4 654.7 661.2 663.0; small 30.496)
round 2: S perf 658.202 (small 30.314) / D perf 660.370 (small 30.245) / SD perf 659.616 (small 30.257)
round 3: S perf 656.384 (small 30.217) / D perf 660.692 (small 30.808) / SD perf 658.395 (small 30.775)
同轮差: D−S +2.59/+2.17/+4.31 ms; SD−S +0.87/+1.41/+2.01; D−SD +1.72/+0.75/+2.30
比率:   D/S 1.0039/1.0033/1.0066 — 全部深亚带（≥8× 低于 ±35 ms）
冷面 (5 reps each): D import 2.0–2.2 ms / wall 24–27 ms; S import 7.0–8.5 ms / wall 158–163 ms
                    ⇒ 冷差 +134–137 ms/进程（once-per-process 拒绝类，落在测量装具侧）
```

## 6. 新增排除 ID：无

第二十八组候选集为空。从未点名站点普查结果为零（连续第十遍；R18-C
「最后一个从未点名位置」判定经 R19-C..R27-C 与本轮十遍核实）。
execution-substrate face 的 C 区数值内核实例化为执行坐标定价轴
（重开条件：构建管线换代——tsc target 降级 / bundler 引入 / 引擎跨
大版本——使热层比率实测越出 [0.95, 1.05]；属构建面事件，非切片
自由度，后果是锚点重标定而非候选。基片维度本身**无越线点**：位面
9/9 摘要全等、热层同轮差 2.2–4.3 ms 与臂内散布同量级且离带 ≥8×、
冷面 once-per-process 拒绝类落在装具侧；不铸绰号不铸 ID，face 命名
权归 R27-H）。APCX/FLOORX 为按规天花板重测（天花板 17.3–27.3 < 35
稳定亚带，sink=floorSink=7.309 八读数逐位同值）。生产中位随读重锚
660.1–661.3 ms（正典 660.1–661.2），与 R27-C 带交叠。

## 附录 A：测量彩排脚本（完整，可复现；归档后临时文件已删除）

- `/tmp/r28c/r28c-anchor.mts` — sha256
  `07d79f12f3ffe4de24aedd6d93ea80658075717ad727efaf817d0bdfb9386225`
- `/tmp/r28c/r28c-substrate-runner.mjs` — sha256
  `744faf9964d0d78ccb320d619e269254a899842a4487637956ef3e01ea6379a8`
- `/tmp/r28c/r28c-cold.mjs` — sha256
  `94b81b314e72d72d28ec6ae7c75665a0d5d3710e899d0b8bc89526e0b03f8596`
- 另有一次性可行性探针 `probe.mjs`（4 行：动态导入 src/.ts 与
  dist/.js 各一次打印 typeof——验证 tsx/node 解析行为）与原始读数
  暂存 `substrate-results.jsonl`（内容已全文誊入 §5），均已删除。
- 四次主锚运行（1 预演 + 3 正典）所用 `r28c-anchor.mts` 逐字节相同
  （预演后零改动）；预演前的一次废弃试跑（协议错配版本）已在 §1
  披露，其脚本已被冻结版覆盖、读数不计入任何处置依据。

### 主锚 `/tmp/r28c/r28c-anchor.mts`

```ts
/**
 * Round 28 / R28-C measurement re-anchor + twenty-eighth-group closure probes
 * (offline routing slice, twenty-eighth pass). NOT committed — the twenty-
 * eighth-group enumeration carries no candidate lanes that reach the
 * acceptance bar; full source goes into the R28-C report appendix.
 *
 * Contents:
 *  - replica-lane fidelity battery: a verbatim copy of the landed
 *    fitLogitAdditive pipeline (buildDesign / computeSupports /
 *    canonicalRowKeys / irls / onProbabilitiesFor /
 *    averagePredictiveComparison / percentile) compared bitwise
 *    (Object.is, includes signed zero / NaN) against the production import
 *    on a 33-case shape battery + the perf fixture + the small fixture.
 *  - production median re-anchor: perf shape (4 scenarios x 6 models x
 *    8 projects, rows=400, passRate=0.6, bootstrap=200, seed=20260818)
 *    7 reps -> median; small shape (2 x 3 x 3, 120 rows) 3 reps -> median.
 *  - production self-determinism: 3 seeds x 2 runs, all report fields
 *    Object.is.
 *  - APCX probe (dispatch-mandated ceiling re-measure): standalone
 *    timer-free APC sweep (59 non-intercept names x 400 rows, base-fit
 *    coefficients) per-fit cost x 201 fits; sink = sum of all APC values
 *    (cross-round determinism witness, expected print 7.309).
 *  - FLOORX probe: cheapest bitwise-legal sweep (S8-C-2 support-ascending
 *    off-sum form, never landed — X2-1 lemma premises hold: 0/1 design,
 *    +0.0 start, finite beta), per-name values asserted Object.is against
 *    the APCX sweep, then per-fit cost x 201 fits; combination ceiling =
 *    APCX - FLOORX must stay < 35 ms with floorSink === sink bitwise.
 *
 * The substrate-axis arms run out of the byte-identical runner
 * r28c-substrate-runner.mjs (see report appendix), not out of this file.
 */
import { fitLogitAdditive } from "/workspace/src/routing/offline-logit.ts";
import { betaQuantileLcb } from "/workspace/src/routing/posterior.ts";
import { solveSymmetric } from "/workspace/src/routing/lin-alg.ts";
import type { AttributionEffect, AttributionLabel, AttributionReport } from "/workspace/src/routing/offline-types.ts";

let checks = 0;
function assertSame(label: string, a: unknown, b: unknown): void {
  checks += 1;
  if (!Object.is(a, b)) {
    throw new Error(`MISMATCH ${label}: ${String(a)} !== ${String(b)}`);
  }
}
function assertTrue(label: string, v: boolean): void {
  checks += 1;
  if (!v) throw new Error(`ASSERT ${label}`);
}

/* ------------- replica lane: verbatim copy of the landed pipeline ------------- */

const MAX_ITER_DEFAULT = 50;
const TOL = 1e-8;
const BOOTSTRAP_DEFAULT = 200;
const SEED_DEFAULT = 20260818;
const INTERACTION_MIN_N = 3;
const MIN_SUCCESSFUL_DRAWS = 20;
const ATTRIBUTION_EFFECT = 0.1;
const QUALITY_FLOOR = 0.55;

interface Design {
  readonly names: readonly string[];
  readonly columnIndex: ReadonlyMap<string, number>;
  build(row: Row, skip?: string): number[];
  readonly referenceLevels: ReadonlyArray<{ factor: "a" | "u" | "v"; name: string }>;
}
interface Row {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
}
function sigmoid(z: number): number {
  if (z >= 0) return 1 / (1 + Math.exp(-z));
  const e = Math.exp(z);
  return e / (1 + e);
}
function rng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function buildDesign(rows: readonly Row[]): Design {
  const scenarios = [...new Set(rows.map((r) => r.scenarioId))];
  const models = [...new Set(rows.map((r) => r.modelVersion))];
  const projects = [...new Set(rows.map((r) => r.projectId))];
  const dropLast = (levels: readonly string[]): string[] =>
    levels.slice(0, Math.max(0, levels.length - 1));
  const scenarioLevels = dropLast(scenarios);
  const modelLevels = dropLast(models);
  const projectLevels = dropLast(projects);
  const pairCounts = new Map<string, number>();
  for (const row of rows) {
    const key = `${row.modelVersion}|${row.projectId}`;
    pairCounts.set(key, (pairCounts.get(key) ?? 0) + 1);
  }
  const interactionPairs = [...pairCounts.entries()]
    .filter(([, n]) => n >= INTERACTION_MIN_N)
    .map(([key]) => key);
  const names = [
    "intercept",
    ...scenarioLevels.map((s) => `a:${s}`),
    ...modelLevels.map((m) => `u:${m}`),
    ...projectLevels.map((p) => `v:${p}`),
    ...interactionPairs.map((key) => `w:${key}`)
  ];
  const columnIndex = new Map(names.map((name, index) => [name, index] as const));
  const interactionPairSet = new Set(interactionPairs);
  const referenceLevels: Array<{ factor: "a" | "u" | "v"; name: string }> = [];
  const lastModel = models[models.length - 1];
  const lastProject = projects[projects.length - 1];
  if (lastModel !== undefined) referenceLevels.push({ factor: "u", name: lastModel });
  if (lastProject !== undefined) referenceLevels.push({ factor: "v", name: lastProject });
  return {
    names,
    columnIndex,
    referenceLevels,
    build(row: Row, skip?: string): number[] {
      const vec = new Array<number>(names.length).fill(0);
      vec[0] = 1;
      const set = (name: string): void => {
        if (name === skip) return;
        const index = columnIndex.get(name);
        if (index !== undefined && index > 0) vec[index] = 1;
      };
      if (row.scenarioId !== scenarios[scenarios.length - 1]) set(`a:${row.scenarioId}`);
      if (row.modelVersion !== models[models.length - 1]) set(`u:${row.modelVersion}`);
      if (row.projectId !== projects[projects.length - 1]) set(`v:${row.projectId}`);
      const pairKey = `${row.modelVersion}|${row.projectId}`;
      if (interactionPairSet.has(pairKey)) set(`w:${pairKey}`);
      return vec;
    }
  };
}
interface FitResult {
  readonly coefficients: readonly number[] | null;
}
function computeSupports(vectors: readonly number[][]): number[][] {
  return vectors.map((vector) => {
    const active: number[] = [];
    for (let j = 0; j < vector.length; j++) {
      if (vector[j] !== 0) active.push(j);
    }
    return active;
  });
}
function canonicalRowKeys(rows: readonly Row[]): number[] {
  const byScenario = new Map<string, Map<string, Map<string, number>>>();
  const keys = new Array<number>(rows.length);
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i]!;
    let byModel = byScenario.get(row.scenarioId);
    if (byModel === undefined) {
      byModel = new Map();
      byScenario.set(row.scenarioId, byModel);
    }
    let byProject = byModel.get(row.modelVersion);
    if (byProject === undefined) {
      byProject = new Map();
      byModel.set(row.modelVersion, byProject);
    }
    let canonical = byProject.get(row.projectId);
    if (canonical === undefined) {
      canonical = i;
      byProject.set(row.projectId, canonical);
    }
    keys[i] = canonical;
  }
  return keys;
}
function irls(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  supports: readonly (readonly number[])[],
  keys: readonly number[],
  keySpace: number,
  maxIter: number
): FitResult {
  const p = design.names.length;
  const n = rows.length;
  const eta = new Array<number>(n).fill(0);
  const mu = new Array<number>(n).fill(0);
  const xtwx: number[][] = Array.from({ length: p }, () => new Array<number>(p).fill(0));
  const xtwz: number[] = new Array<number>(p).fill(0);
  const stamp = new Int32Array(keySpace);
  const etaByKey = new Float64Array(keySpace);
  const muByKey = new Float64Array(keySpace);
  let beta = new Array<number>(p).fill(0);
  for (let iter = 0; iter < maxIter; iter++) {
    const mark = iter + 1;
    for (let i = 0; i < n; i++) {
      const key = keys[i]!;
      if (stamp[key] !== mark) {
        stamp[key] = mark;
        let value = 0;
        const active = supports[i]!;
        for (let ai = 0; ai < active.length; ai++) value += beta[active[ai]!]!;
        etaByKey[key] = value;
        muByKey[key] = sigmoid(value);
      }
      eta[i] = etaByKey[key]!;
      mu[i] = muByKey[key]!;
    }
    for (let d = 0; d < p; d++) xtwx[d]!.fill(0);
    xtwz.fill(0);
    for (let i = 0; i < n; i++) {
      const w = Math.max(mu[i]! * (1 - mu[i]!)!, 1e-10);
      const z = eta[i]! + ((rows[i]!.y - mu[i]!) / w);
      const wz = w * z;
      const active = supports[i]!;
      switch (active.length) {
        case 5: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          const a4 = active[4]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w; r0[a4] = r0[a4]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w; r1[a4] = r1[a4]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w; r2[a4] = r2[a4]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w; r3[a4] = r3[a4]! + w;
          xtwz[a4] = xtwz[a4]! + wz;
          const r4 = xtwx[a4]!;
          r4[a0] = r4[a0]! + w; r4[a1] = r4[a1]! + w; r4[a2] = r4[a2]! + w; r4[a3] = r4[a3]! + w; r4[a4] = r4[a4]! + w;
          break;
        }
        case 4: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          const a3 = active[3]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w; r0[a3] = r0[a3]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w; r1[a3] = r1[a3]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w; r2[a3] = r2[a3]! + w;
          xtwz[a3] = xtwz[a3]! + wz;
          const r3 = xtwx[a3]!;
          r3[a0] = r3[a0]! + w; r3[a1] = r3[a1]! + w; r3[a2] = r3[a2]! + w; r3[a3] = r3[a3]! + w;
          break;
        }
        case 3: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          const a2 = active[2]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w; r0[a2] = r0[a2]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w; r1[a2] = r1[a2]! + w;
          xtwz[a2] = xtwz[a2]! + wz;
          const r2 = xtwx[a2]!;
          r2[a0] = r2[a0]! + w; r2[a1] = r2[a1]! + w; r2[a2] = r2[a2]! + w;
          break;
        }
        case 2: {
          const a0 = active[0]!;
          const a1 = active[1]!;
          xtwz[a0] = xtwz[a0]! + wz;
          const r0 = xtwx[a0]!;
          r0[a0] = r0[a0]! + w; r0[a1] = r0[a1]! + w;
          xtwz[a1] = xtwz[a1]! + wz;
          const r1 = xtwx[a1]!;
          r1[a0] = r1[a0]! + w; r1[a1] = r1[a1]! + w;
          break;
        }
        default: {
          for (let ai = 0; ai < active.length; ai++) {
            const a = active[ai]!;
            xtwz[a] = xtwz[a]! + wz;
            const rowA = xtwx[a]!;
            for (let bi = 0; bi < active.length; bi++) {
              const b = active[bi]!;
              rowA[b] = rowA[b]! + w;
            }
          }
        }
      }
    }
    for (let d = 0; d < p; d++) xtwx[d]![d] = xtwx[d]![d]! + 1e-6;
    const next = solveSymmetric(xtwx, xtwz);
    if (next === null) return { coefficients: null };
    const delta = next.map((value, index) => value - beta[index]!);
    const l2 = Math.sqrt(delta.reduce((acc, d) => acc + d * d, 0));
    beta = next;
    if (!beta.every(Number.isFinite)) return { coefficients: null };
    if (l2 < TOL) break;
  }
  return { coefficients: beta };
}
function dot(a: readonly number[], b: readonly number[]): number {
  let sum = 0;
  for (let i = 0; i < a.length; i++) sum += a[i]! * b[i]!;
  return sum;
}
function onProbabilitiesFor(
  vectors: readonly number[][],
  coefficients: readonly number[]
): number[] {
  return vectors.map((vector) => sigmoid(dot(coefficients, vector)));
}
function averagePredictiveComparison(
  design: Design,
  rows: readonly Row[],
  vectors: readonly number[][],
  coefficients: readonly number[],
  onProbabilities: readonly number[],
  column: string
): number {
  let sum = 0;
  const columnIdx = design.columnIndex.get(column);
  if (columnIdx !== undefined && columnIdx !== 0) {
    for (let i = 0; i < rows.length; i++) {
      if (vectors[i]![columnIdx] === 0) continue;
      const on = onProbabilities[i]!;
      const offVector = vectors[i]!.slice();
      offVector[columnIdx] = 0;
      const off = sigmoid(dot(coefficients, offVector));
      sum += on - off;
    }
  }
  return rows.length === 0 ? 0 : sum / rows.length;
}
function percentile(sortedValues: readonly number[], q: number): number {
  if (sortedValues.length === 0) return Number.NaN;
  const index = (sortedValues.length - 1) * q;
  const lo = Math.floor(index);
  const hi = Math.ceil(index);
  if (lo === hi) return sortedValues[lo]!;
  return sortedValues[lo]! * (hi - index) + sortedValues[hi]! * (index - lo);
}
type OfflineRowInput = {
  readonly scenarioId: string;
  readonly modelVersion: string;
  readonly projectId: string;
  readonly y: 0 | 1;
  readonly occurredAtMs: number;
};
function fitLogitAdditiveReplica(
  rows: readonly OfflineRowInput[],
  options?: { readonly maxIter?: number; readonly bootstrap?: number; readonly seed?: number }
): AttributionReport {
  const maxIter = options?.maxIter ?? MAX_ITER_DEFAULT;
  const bootstrapDraws = options?.bootstrap ?? BOOTSTRAP_DEFAULT;
  const seed = options?.seed ?? SEED_DEFAULT;
  const baseRows: Row[] = rows.map((r) => ({
    scenarioId: r.scenarioId,
    modelVersion: r.modelVersion,
    projectId: r.projectId,
    y: r.y
  }));
  const effects: AttributionEffect[] = [];
  if (baseRows.length === 0 || baseRows.every((r) => r.y === 0) || baseRows.every((r) => r.y === 1)) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: degenerate or empty design");
  }
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irls(design, baseRows, vectors, supports, keys, baseRows.length, maxIter);
  if (fit.coefficients === null) {
    return uncertainReport(baseRows.length, "INVALID_ESTIMATE: singular or non-finite Hessian");
  }
  const onProbabilities = onProbabilitiesFor(vectors, fit.coefficients);
  const pointEffects = new Map<string, number>();
  for (const name of design.names) {
    if (name === "intercept") continue;
    pointEffects.set(
      name,
      averagePredictiveComparison(design, baseRows, vectors, fit.coefficients, onProbabilities, name)
    );
  }
  for (const ref of design.referenceLevels) {
    if (!pointEffects.has(`${ref.factor}:${ref.name}`)) {
      pointEffects.set(`${ref.factor}:${ref.name}`, 0);
    }
  }
  const random = rng(seed);
  const draws = new Map<string, number[]>();
  let successful = 0;
  for (let draw = 0; draw < bootstrapDraws; draw++) {
    const sample: Row[] = [];
    const sampleVectors: number[][] = [];
    const sampleSupports: number[][] = [];
    const sampleKeys: number[] = [];
    for (let i = 0; i < baseRows.length; i++) {
      const index = Math.floor(random() * baseRows.length);
      sample.push(baseRows[index]!);
      sampleVectors.push(vectors[index]!);
      sampleSupports.push(supports[index]!);
      sampleKeys.push(keys[index]!);
    }
    if (sample.every((r) => r.y === 0) || sample.every((r) => r.y === 1)) continue;
    const bootFit = irls(design, sample, sampleVectors, sampleSupports, sampleKeys, baseRows.length, maxIter);
    if (bootFit.coefficients === null) continue;
    successful += 1;
    const sampleOnProbabilities = onProbabilitiesFor(sampleVectors, bootFit.coefficients);
    for (const [name] of pointEffects.entries()) {
      const value = averagePredictiveComparison(
        design,
        sample,
        sampleVectors,
        bootFit.coefficients,
        sampleOnProbabilities,
        name
      );
      pushValue(draws, name, value);
    }
  }
  for (const [name, point] of pointEffects.entries()) {
    const values = draws.get(name) ?? [];
    if (successful < MIN_SUCCESSFUL_DRAWS || values.length < MIN_SUCCESSFUL_DRAWS) {
      return uncertainReport(baseRows.length, "INVALID_ESTIMATE: fewer than 20 successful bootstrap draws");
    }
    values.sort((a, b) => a - b);
    effects.push({
      name,
      point,
      lcb: percentile(values, 0.025),
      ucb: percentile(values, 0.975)
    });
  }
  const n = baseRows.length;
  const mean = baseRows.reduce((acc, r) => acc + r.y, 0) / n;
  const muPosterior = { alpha: 1 + n * mean, beta: 1 + n * (1 - mean) };
  const muLcb = betaQuantileLcb(muPosterior, 0.05);
  const models = new Set(baseRows.map((r) => r.modelVersion)).size;
  const projects = new Set(baseRows.map((r) => r.projectId)).size;
  const ZERO_EPS = 0.005 * ATTRIBUTION_EFFECT;
  const containsZero = (e: AttributionEffect): boolean =>
    e.lcb <= ZERO_EPS && e.ucb >= -ZERO_EPS;
  const interactionsFor = (prefix: string): AttributionEffect[] =>
    effects.filter((e) => e.name.startsWith(`w:${prefix}`));
  let diagnosis: AttributionLabel = "uncertain";
  const scenarioHard = muLcb < QUALITY_FLOOR && models >= 2 && projects >= 3;
  if (
    effects.some(
      (e) => e.name.startsWith("u:") && e.lcb < -ATTRIBUTION_EFFECT && interactionsFor(modelOf(e.name)).every(containsZero)
    )
  ) {
    diagnosis = "model-problem";
  } else if (
    effects.some(
      (e) =>
        e.name.startsWith("v:") &&
        e.lcb < -ATTRIBUTION_EFFECT &&
        effects
          .filter((w) => w.name.startsWith("w:") && w.name.endsWith(`|${e.name.slice("v:".length)}`))
          .every(containsZero)
    )
  ) {
    diagnosis = "project-problem";
  } else if (effects.some((e) => e.name.startsWith("w:") && e.lcb < -ATTRIBUTION_EFFECT)) {
    diagnosis = "interaction-only";
  } else if (scenarioHard) {
    diagnosis = "scenario-hard";
  }
  return {
    estimator: "logit-additive",
    rowsUsed: baseRows.length,
    effects,
    diagnosis,
    reason: diagnosis === "uncertain" ? "no effect beyond threshold or intervals too wide" : `${diagnosis} beyond the ${ATTRIBUTION_EFFECT} effect threshold`,
    writesActivePointer: false
  };
}
function modelOf(effectName: string): string {
  return effectName.slice("u:".length);
}
function pushValue(map: Map<string, number[]>, key: string, value: number): void {
  const list = map.get(key);
  if (list === undefined) map.set(key, [value]);
  else list.push(value);
}
function uncertainReport(rowsUsed: number, reason: string): AttributionReport {
  return {
    estimator: "logit-additive",
    rowsUsed,
    effects: [],
    diagnosis: "uncertain",
    reason,
    writesActivePointer: false
  };
}

/* ------------------------------ fixtures ------------------------------ */

function fixtureRng(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(r: () => number, items: readonly T[]): T {
  return items[Math.floor(r() * items.length)]!;
}
function randomRows(
  r: () => number,
  options: {
    scenarios: number;
    models: number;
    projects: number;
    rows: number;
    passRate: number;
    pipeInModel?: boolean;
  }
): OfflineRowInput[] {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) =>
    options.pipeInModel === true && i === 0 ? "weird|model-0" : `model-${i}`
  );
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows: OfflineRowInput[] = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
function perfRows(): OfflineRowInput[] {
  const r = fixtureRng(0xbeef);
  return randomRows(r, { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
}
// Same small-shape fixture as R10-C..R27-C (seed 0x51a1, 3 x 3 x 3 / 120).
function smallRows(): OfflineRowInput[] {
  const r = fixtureRng(0x51a1);
  return randomRows(r, { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
}
const PERF_OPTIONS = { bootstrap: 200, seed: SEED_DEFAULT };

/* ------------------------------ comparisons ------------------------------ */

function compareReports(label: string, a: AttributionReport, b: AttributionReport): void {
  assertSame(`${label}.estimator`, a.estimator, b.estimator);
  assertSame(`${label}.rowsUsed`, a.rowsUsed, b.rowsUsed);
  assertSame(`${label}.diagnosis`, a.diagnosis, b.diagnosis);
  assertSame(`${label}.reason`, a.reason, b.reason);
  assertSame(`${label}.writesActivePointer`, a.writesActivePointer, b.writesActivePointer);
  assertSame(`${label}.effects.length`, a.effects.length, b.effects.length);
  for (let i = 0; i < a.effects.length; i++) {
    const ea = a.effects[i]!;
    const eb = b.effects[i]!;
    assertSame(`${label}.effects[${i}].name`, ea.name, eb.name);
    assertSame(`${label}.effects[${i}].point`, ea.point, eb.point);
    assertSame(`${label}.effects[${i}].lcb`, ea.lcb, eb.lcb);
    assertSame(`${label}.effects[${i}].ucb`, ea.ucb, eb.ucb);
  }
}

function batteryCases(): Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> {
  const r = fixtureRng(0x3c01);
  const cases: Array<{ rows: OfflineRowInput[]; options?: { maxIter?: number; bootstrap?: number; seed?: number } }> = [];
  for (let i = 0; i < 24; i++) {
    cases.push({
      rows: randomRows(r, {
        scenarios: 1 + Math.floor(r() * 3),
        models: 1 + Math.floor(r() * 4),
        projects: 1 + Math.floor(r() * 4),
        rows: 10 + Math.floor(r() * 90),
        passRate: 0.3 + r() * 0.6,
      }),
      options: { bootstrap: 25 + Math.floor(r() * 40), seed: 1 + Math.floor(r() * 10_000) },
    });
  }
  cases.push({ rows: [] });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: 2 }) });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 2, projects: 2, rows: 20, passRate: -1 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 30, passRate: 0.6 }),
    options: { bootstrap: 5, seed: 42 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 1, rows: 4, passRate: 0.5 }),
    options: { bootstrap: 30, seed: 7 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 40, passRate: 0.5 }),
    options: { maxIter: 3, bootstrap: 30, seed: 11 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 60, passRate: 0.55, pipeInModel: true }),
    options: { bootstrap: 30, seed: 99 },
  });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 1, projects: 1, rows: 24, passRate: 0.5 }),
    options: { bootstrap: 40, seed: 3 },
  });
  cases.push({ rows: randomRows(r, { scenarios: 2, models: 3, projects: 3, rows: 50, passRate: 0.6 }) });
  cases.push({
    rows: randomRows(r, { scenarios: 1, models: 2, projects: 2, rows: 80, passRate: 0.55 }),
    options: { bootstrap: 40, seed: 21 },
  });
  return cases;
}

function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)]!;
}

/* --------------------------------- main --------------------------------- */

function main(): void {
  // 1. Replica fidelity battery (replica lane vs production import, bitwise).
  const cases = batteryCases();
  for (let i = 0; i < cases.length; i++) {
    const c = cases[i]!;
    compareReports(`battery[${i}]`, fitLogitAdditive(c.rows, c.options), fitLogitAdditiveReplica(c.rows, c.options));
  }
  compareReports("perf-fixture", fitLogitAdditive(perfRows(), PERF_OPTIONS), fitLogitAdditiveReplica(perfRows(), PERF_OPTIONS));
  compareReports("small-fixture", fitLogitAdditive(smallRows(), PERF_OPTIONS), fitLogitAdditiveReplica(smallRows(), PERF_OPTIONS));
  console.log(`scenario 1 (replica-lane fidelity battery, ${cases.length} cases + perf + small, bitwise vs production import): OK`);

  // 2. Production self-determinism: 3 seeds x 2 runs.
  for (const seed of [SEED_DEFAULT, 7_777, 424_242]) {
    const rows = perfRows();
    compareReports(`determinism.seed=${seed}`, fitLogitAdditive(rows, { bootstrap: 200, seed }), fitLogitAdditive(rows, { bootstrap: 200, seed }));
  }
  console.log("scenario 2 (production self-determinism: 3 seeds x 2 runs, all report fields Object.is): OK");

  // 3. Production median re-anchor (perf 7 reps, small 3 reps).
  const prodPerf: number[] = [];
  {
    const rows = perfRows();
    for (let rep = 0; rep < 7; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      prodPerf.push(performance.now() - t0);
    }
  }
  const prodSmall: number[] = [];
  {
    const rows = smallRows();
    for (let rep = 0; rep < 3; rep++) {
      const t0 = performance.now();
      fitLogitAdditive(rows, PERF_OPTIONS);
      prodSmall.push(performance.now() - t0);
    }
  }
  console.log(`  shape small (3 models x 3 projects, 120 rows): production median ${median(prodSmall).toFixed(1)} ms/report (3 reps)`);
  console.log(`  shape perf (6 models x 8 projects, 400 rows): production median ${median(prodPerf).toFixed(1)} ms/report (7 reps)`);
  console.log(`  perf reps: ${prodPerf.map((v) => v.toFixed(1)).join(" ")}`);

  // 4. APCX / FLOORX ceiling re-measure (dispatch-mandated).
  const rows = perfRows();
  const baseRows: Row[] = rows.map((r) => ({ scenarioId: r.scenarioId, modelVersion: r.modelVersion, projectId: r.projectId, y: r.y }));
  const design = buildDesign(baseRows);
  const vectors = baseRows.map((r) => design.build(r));
  const supports = computeSupports(vectors);
  const keys = canonicalRowKeys(baseRows);
  const fit = irls(design, baseRows, vectors, supports, keys, baseRows.length, MAX_ITER_DEFAULT);
  assertTrue("apcx.base-fit-converged", fit.coefficients !== null);
  const coefficients = fit.coefficients!;
  const onProbabilities = onProbabilitiesFor(vectors, coefficients);
  const sweepNames = design.names.filter((name) => name !== "intercept");

  // FLOORX lane bitwise-legality proof, per name (S8-C-2 form; X2-1 lemma
  // premises hold: 0/1 entries, +0.0 start, finite beta; ascending support
  // order = the full dot's nonzero visit order). Outside the sink accumulator.
  for (const name of sweepNames) {
    let sum = 0;
    const columnIdx = design.columnIndex.get(name);
    if (columnIdx !== undefined && columnIdx !== 0) {
      for (let i = 0; i < baseRows.length; i++) {
        if (vectors[i]![columnIdx] === 0) continue;
        const active = supports[i]!;
        let value = 0;
        for (let ai = 0; ai < active.length; ai++) {
          const j = active[ai]!;
          if (j === columnIdx) continue;
          value += coefficients[j]!;
        }
        sum += onProbabilities[i]! - sigmoid(value);
      }
    }
    const floorValue = baseRows.length === 0 ? 0 : sum / baseRows.length;
    assertSame(
      `floorx.bitwise[${name}]`,
      averagePredictiveComparison(design, baseRows, vectors, coefficients, onProbabilities, name),
      floorValue
    );
  }
  // Verbatim R14-C-lineage protocol: 5 warmup sweeps + 60 timed sweeps per
  // lane, sink accumulated across all 65 (expected cross-round print 7.309).
  const names = sweepNames;
  let apcSink = 0;
  const sweepReps = 60;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tSweep0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const on = onProbabilities[i]!;
          const offVector = vectors[i]!.slice();
          offVector[columnIdx] = 0;
          const off = sigmoid(dot(coefficients, offVector));
          sum += on - off;
        }
      }
      apcSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perSweepMs = (performance.now() - tSweep0) / sweepReps;
  console.log(
    `probe APCX: standalone timer-free APC sweep (59 names x 400 rows, base-fit coefficients) ` +
      `${perSweepMs.toFixed(3)} ms/fit -> x201 fits = ${(perSweepMs * 201).toFixed(1)} ms/report ` +
      `(base-shape approximation; sink=${apcSink.toFixed(3)})`
  );

  let floorSink = 0;
  for (let rep = 0; rep < 5; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const tFloor0 = performance.now();
  for (let rep = 0; rep < sweepReps; rep++) {
    for (const name of names) {
      let sum = 0;
      const columnIdx = design.columnIndex.get(name);
      if (columnIdx !== undefined && columnIdx !== 0) {
        for (let i = 0; i < baseRows.length; i++) {
          if (vectors[i]![columnIdx] === 0) continue;
          const active = supports[i]!;
          let value = 0;
          for (let ai = 0; ai < active.length; ai++) {
            const j = active[ai]!;
            if (j === columnIdx) continue;
            value += coefficients[j]!;
          }
          sum += onProbabilities[i]! - sigmoid(value);
        }
      }
      floorSink += baseRows.length === 0 ? 0 : sum / baseRows.length;
    }
  }
  const perFloorMs = (performance.now() - tFloor0) / sweepReps;
  console.log(
    `probe FLOORX: cheapest bitwise-legal sweep ${perFloorMs.toFixed(3)} ms/fit -> x201 fits = ` +
      `${(perFloorMs * 201).toFixed(1)} ms/report floor; APC combination ceiling on THIS VM = ` +
      `${(perSweepMs * 201 - perFloorMs * 201).toFixed(1)} ms (floorSink=${floorSink.toFixed(3)})`
  );
  assertSame("apcx.sink===floorSink", apcSink, floorSink);
  assertTrue("ceiling<35", perSweepMs * 201 - perFloorMs * 201 < 35);

  console.log("");
  console.log(`ALL EQUIVALENCE CHECKS PASSED (${checks} bitwise checks)`);
}

main();
```

### 基片轴 runner `/tmp/r28c/r28c-substrate-runner.mjs`

```js
/**
 * R28-C execution-substrate axis runner (C-slice numeric-kernel object).
 * Plain-JS on purpose: THIS FILE IS BYTE-IDENTICAL ACROSS ALL ARMS. The arm
 * is selected entirely by (a) the interpreter that launches it (node vs tsx)
 * and (b) R28C_MODULE (src .ts vs dist .js production import). NOT committed;
 * full source goes into the R28-C report appendix.
 *
 * Arms (dose points of the substrate coordinate):
 *   S  : tsx  + /workspace/src/routing/offline-logit.ts   (canonical R1-C..R27-C measurement substrate)
 *   D  : node + /workspace/dist/routing/offline-logit.js  (production `pi-sparkle` bin substrate, tsc ES2024 emit)
 *   SD : tsx  + /workspace/dist/routing/offline-logit.js  (decomposition arm: tsx loader on the tsc emit)
 *
 * Emits one JSON line: hot-tier medians (perf 7 reps, small 3 reps), all
 * reps, and a SHA-256 digest over the IEEE-754 bit patterns + strings of the
 * full reports (perf + small + 3 battery shapes + a determinism re-run),
 * so cross-substrate bitwise equality is a digest-equality check.
 */
import { createHash } from "node:crypto";

const modPath = process.env.R28C_MODULE;
const arm = process.env.R28C_ARM ?? "?";
if (!modPath) {
  console.error("R28C_MODULE not set");
  process.exit(1);
}
const { fitLogitAdditive } = await import(modPath);

function fixtureRng(seed) {
  let a = seed >>> 0;
  return () => {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick(r, items) {
  return items[Math.floor(r() * items.length)];
}
function randomRows(r, options) {
  const scenarios = Array.from({ length: options.scenarios }, (_, i) => `fam${i}|role${i % 2}`);
  const models = Array.from({ length: options.models }, (_, i) => `model-${i}`);
  const projects = Array.from({ length: options.projects }, (_, i) => `prj_${i}`);
  const rows = [];
  for (let i = 0; i < options.rows; i++) {
    const modelVersion = pick(r, models);
    const rate = modelVersion === models[0] ? options.passRate * 0.4 : options.passRate;
    rows.push({
      scenarioId: pick(r, scenarios),
      modelVersion,
      projectId: pick(r, projects),
      y: r() < rate ? 1 : 0,
      occurredAtMs: 1_000 + i,
    });
  }
  return rows;
}
const perfRows = () => randomRows(fixtureRng(0xbeef), { scenarios: 4, models: 6, projects: 8, rows: 400, passRate: 0.6 });
const smallRows = () => randomRows(fixtureRng(0x51a1), { scenarios: 3, models: 3, projects: 3, rows: 120, passRate: 0.6 });
const PERF_OPTIONS = { bootstrap: 200, seed: 20260818 };

/** Canonical walk: numbers as IEEE-754 bit-pattern hex, strings/bools/null tagged. */
function foldValue(hash, value) {
  if (typeof value === "number") {
    const buf = new ArrayBuffer(8);
    new Float64Array(buf)[0] = value;
    hash.update("n:");
    hash.update(Buffer.from(buf));
  } else if (typeof value === "string") {
    hash.update(`s:${value.length}:${value};`);
  } else if (typeof value === "boolean") {
    hash.update(`b:${value};`);
  } else if (value === null) {
    hash.update("null;");
  } else if (value === undefined) {
    hash.update("undef;");
  } else if (Array.isArray(value)) {
    hash.update(`a:${value.length}[`);
    for (const item of value) foldValue(hash, item);
    hash.update("]");
  } else {
    const keys = Object.keys(value);
    hash.update(`o:${keys.length}{`);
    for (const key of keys) {
      hash.update(`k:${key}=`);
      foldValue(hash, value[key]);
    }
    hash.update("}");
  }
}
function digestReports(reports) {
  const hash = createHash("sha256");
  for (const report of reports) foldValue(hash, report);
  return hash.digest("hex");
}
function median(values) {
  const sorted = [...values].sort((a, b) => a - b);
  return sorted[Math.floor(sorted.length / 2)];
}

// Hot-tier timing: perf shape 7 reps, small shape 3 reps.
const perfReps = [];
{
  const rows = perfRows();
  for (let rep = 0; rep < 7; rep++) {
    const t0 = performance.now();
    fitLogitAdditive(rows, PERF_OPTIONS);
    perfReps.push(performance.now() - t0);
  }
}
const smallReps = [];
{
  const rows = smallRows();
  for (let rep = 0; rep < 3; rep++) {
    const t0 = performance.now();
    fitLogitAdditive(rows, PERF_OPTIONS);
    smallReps.push(performance.now() - t0);
  }
}

// Cross-substrate bitwise-equality payload: perf + small + 3 battery shapes
// + a determinism re-run of the perf shape (digest equality within one arm
// certifies in-process determinism; digest equality across arms certifies
// substrate-invariance of every output bit).
const r = fixtureRng(0x5eed);
const batteryShapes = [
  { rows: randomRows(r, { scenarios: 2, models: 3, projects: 2, rows: 60, passRate: 0.5 }), options: { bootstrap: 40, seed: 11 } },
  { rows: randomRows(r, { scenarios: 3, models: 4, projects: 4, rows: 150, passRate: 0.55 }), options: { bootstrap: 60, seed: 77 } },
  { rows: randomRows(r, { scenarios: 1, models: 2, projects: 3, rows: 45, passRate: 0.65 }), options: { bootstrap: 30, seed: 5 } },
];
const reports = [
  fitLogitAdditive(perfRows(), PERF_OPTIONS),
  fitLogitAdditive(smallRows(), PERF_OPTIONS),
  ...batteryShapes.map((c) => fitLogitAdditive(c.rows, c.options)),
  fitLogitAdditive(perfRows(), PERF_OPTIONS),
];
const digest = digestReports(reports);

console.log(JSON.stringify({
  arm,
  module: modPath,
  node: process.version,
  perfMedianMs: Number(median(perfReps).toFixed(3)),
  smallMedianMs: Number(median(smallReps).toFixed(3)),
  perfReps: perfReps.map((v) => Number(v.toFixed(1))),
  smallReps: smallReps.map((v) => Number(v.toFixed(1))),
  digest,
}));
```

### 冷面探针 `/tmp/r28c/r28c-cold.mjs`

```js
// R28-C cold-face probe: whole-process module-load cost per substrate arm.
// Byte-identical across arms; interpreter + R28C_MODULE select the arm.
// Once-per-process class (dispatch reject class) — recorded, never a lane.
const t0 = performance.now();
await import(process.env.R28C_MODULE);
console.log(JSON.stringify({ importMs: Number((performance.now() - t0).toFixed(1)) }));
```
