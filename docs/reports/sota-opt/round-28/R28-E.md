MODEL_SLUG=claude-fable-5-thinking-xhigh

# R28-E：`src/learning/` 第二十八遍复查报告（Round 28）

**战役:** 全库持久 SOTA 优化 Round 28 / R28-E
**基线:** `cursor/sota-persistent-opt-83a1` @ `8e13778`（开工基线 `5ef5379`（R28-A 摄取 + 本派单）；运行中独占分支**两次** docs-only 快进：`5ef5379` → `08a1724`（R28-C「C-slice numeric-kernel execution-substrate instantiation」空枚举摄取 + R28-F 派出）→ `8e13778`（R28-D「contents-store population face」空枚举摄取 + R28-G 派出），本分支两次 rebase 跟进并各重验冻结与 PLAN 增量；报告首推**之后**独占第三次快进 `8e13778` → `c146dae`（R28-F「boundary call-protocol face」空枚举摄取 + R28-H 派出）——增量复核 docs-only（`src/` 零行）、冻结在 `c146dae` 下重验仍 0 行、其轴按 §6 补入不移植清单；因分支已推送、按不改写已推历史纪律不作 rebase（merge-base `8e13778` 为 `c146dae` 祖先，`git diff exclusive...HEAD` 仍恰为本报告一个文件）；R28-G / R28-H 在飞未触碰、未等待）
**分支:** `cursor/r28-e-learning-twenty-eighth-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**无可落地的新更优解，本轮生产代码零改动，第二十八组新角度枚举为空
（有理由的空枚举，未铸造任何 S28-E-\* ID，与 R10-E..R27-E 同口径），
切片维持关闭（MORE_OPTIMA=no）。本轮补齐一条 R1-E..R27-E 从未拉伸过
的完整性轴——「proposal 写路径终止腿面（PWL）：提案函数在哪条 return
腿上终止 × 该腿的持久写激活集（registry lock / load / save /
preference 铸造）× W1-vs-W2 存盘不对称作为腿属性 × 稳态腿占用普查」。
它以「一次提案调用从哪条腿返回」为受控变量——这是 28 轮里 propose
写侧唯一从未受控过的坐标：R26-E RTL 控的是 **live 消费面**
（applyLearnedRouting 的返回腿，读侧）；R24-I 控的是 **load 侧**
（registry.json 在场 × 字节数，I 切片接线）；R20-E 给同一路径定过
**并发面**价（锁竞争）；S2-E-1 / X0-3 / S1-D-2 各关闭过单个改写形态
（no-change 存盘跳过、W1/W2 合并、find 扫描索引）——没有任何一轮钉过
提案在哪条腿终止、逐腿写激活集、W1-vs-W2 存盘不对称作为腿属性、或
稳态腿占用。杠杆非变量声明：registry 状态是到达腿的杠杆（R24-I 坐标
不剂量——字节钉在最小态）；SPARKLE_AUTO_ADAPT 环境变量是逐臂钉死的
杠杆（R25-I 坐标不剂量）；策略内容逐写者钉死（R27-D 树几何不剂量）；
outcome 行多重集钉死（R17-E/R19-E 构成/次序不剂量）。5 次套件运行
= 3 × v22.22.2 + 2 × v22.14.0，52/52 sanity ×5 全绿，全部确定性
VERDICT 行跨版本跨运行逐位一致：**

1. **两写者十腿命运矩阵首次成文（确定性，逐腿写激活集钉死）**：
   W2 `proposeRoutingFromOutcomes`（from-episode.ts:94-155）四腿——
   N「no bound taskSuccess outcomes」(:98-100) 为**锁前早退**
   （lock=0 load=0 save=0，且**先于** preference 尾巴 :145-153 返回：
   episodeId 已绑定也不铸偏好——preference 铸造对 N 腿敏感、对
   E/B/C 三条锁腿不敏感）；E「identical candidate already proposed」
   (:115-122) 与 B「observed policy matches the active baseline」
   (:123-129) 均为 lock=1 load=1 **save=0**；C「proposed
   routing-policy candidate」(:130-143) 为 lock=1 load=1 save=1。
   W1 `proposeAndMaybePromote`（auto-loop.ts:150-201，私有，经
   runAutoAdaptLoop failing 分支 :106-124 到达）三腿 C/E/B 全部
   lock=1 load=1 **save=1**（:190/:193 无条件存盘）。W1 上游三条
   编排门腿（kill-switch「auto-adapt disabled; collected only」
   :93-101 / no-failing「no actionable model-project issue」/
   empty「no feedback to learn from」:126-132）均不取锁——但
   kill-switch 腿的 bandit 写激活**不受门控**（collect 侧
   :80-90 照写 learning/ 目录；empty 腿连 bandit 门也不开）。
2. **W1-vs-W2 存盘不对称作为腿属性（inode+mtime 见证，S2-E-1 已关
   余量的腿归因）**：同一「无变化」语义的两条腿写命运相反——W2-E/
   W2-B 存盘跳过（registry.json 的 ino+mtime 逐位不变），W1-E/W1-B
   **照样重写+fsync 一个未变化的 registry**（ino 变化见证 temp+
   fsync+rename 全程走完）。稳态腿占用（固定 failing 剖面连跑 3 次
   同根）：两写者腿序列均为 [C,E,E]，但 W2 存盘 1/3（load-only
   稳态）、W1 存盘 3/3（每次 no-change 运行全额再持久化）。该余量
   （每次 ~0.6–0.8 ms once-per-run I/O）的消除形态 = S2-E-1
   （no-change 存盘跳过）与 X0-3（W1/W2 合并）已关闭条目的换名/
   移植，且 save 实现体在 `src/adaptation/`（切片外属地）——仅记录。
3. **两条可达性定理（生产态钉死）**：(i) W2-C 单次 save 同时持久化
   baseline+candidate（ensureRoutingBaseline 在同一锁内注册、无独立
   存盘——重载断言 active 指针 = 空 avoid 基线、恰 1 候选、内容哈希
   与切片复刻策略逐字节相等）；(ii) W2-B 在生产中**仅**可经已晋升的
   非空 active 指针到达——自动铸造的空 avoid 基线哈希 ≠ 任何 W2
   策略哈希（W2 策略恒 avoid.length≥1，哈希不等式断言），夹具经
   registerBaseline 杠杆到达同一 registry 状态。
4. **逐腿定价（once-per-run 全谱，6 采样 = 4 × v22.22.2 + 2 ×
   v22.14.0）**：切片内 CPU 前奏（content+hash+identity）998–1073
   ns/call；W2-N 零 I/O 腿 540–628 ns/call（地板）；W2-E 稳态
   326.5–412.7 µs/run 与 W2-B 314.5–392.0 µs/run（lock+load、零
   save）；W2-C 一次性 med 1.09–1.34 ms（lock+load+save+fsync，30
   新根）；W1-E 稳态 **950.2–1195.3 µs/run**（no-change 再持久化
   税：比 W2-E 贵 ~0.6–0.8 ms，全在切片外 save）；W1-C 一次性 med
   0.62–1.45 ms。**全部腿 once-per-run**（W1 接线 track/loop.ts:172
   / cli/main.ts:783 / cli/adapt.ts:205 + runAutoAdaptFromEvents
   经 cli/adapt.ts:188，W2 接线 proposeRoutingFromRoutedEvents 经
   cli/adapt.ts:168——每 run / 每命令至多一次）；腿间价差的 I/O 端
   属地在 `src/adaptation/`（lock/load/save 实现体全部切片外），
   切片内可控 CPU 份额 ~0.5–1.1 µs/run。**不产生候选。**

**天花板裁决（频率 × 单价 × 属地三端封死，R20-E..R27-E 同式）**：
频率端——propose 路径每 run/命令至多一次（调用位点普查行号钉死，
无每任务/每事件重入放大器）；单价端——切片内 CPU 全谱 0.5–1.1
µs/run，即使整轴清零也距 ≥10 ms 门槛 4 个数量级；腿间最大 I/O 价差
（W1 no-change 再持久化 ~0.6–0.8 ms）为 once-per-run 拒收带；属地
端——该价差的消除形态 = S2-E-1 / X0-3 已关闭条目，save/lock/load
实现体 = 切片外属地（R24-E「属地拒列」同裁），preference 尾巴与
bandit 门控 = 行为可见的学习证据契约。**不产生候选。**

其余与前二十七轮同构的复核全数成立：切片二十八遍零 diff
（`git diff adb20d7..HEAD -- src/learning` 为 **0 字节 0 提交**，
开工、快进 rebase 后、写报告前各验一次；10 文件 1770 行 `wc -l` 与
R14-E..R27-E 相同）；SLICE-CPU 本夹具（seeds `0xe28e01/02/03`，构成
collected=18 / outcomes=1 / diagnoseGroups=1）**16.7–17.2 µs/run**
（12 次运行 = 6 × v22.22.2 + 6 × v22.14.0，版本差异不可分辨，
582–600× 低于门槛；构成比 R27-E 主夹具 collected=10 重，collect 段
13.8–14.2 即全部差源）；**R25-E 种子对调（cmp25，9 次 = 6 × v22.22.2
+ 3 × v22.14.0）复现 collected=14 / outcomes=1 / diagnoseGroups=2
（VERDICT 行与 R25-E/R26-E/R27-E 夹具行逐字相同），总量 15.0–15.2
µs，9/9 落在派单默认带 14.7–15.2 内（上缘区），分段 collect
11.5–11.8 / outcomes 1.3–1.5 / diagnose 1.32–1.45 / bandit 0.8 全在
R25-E 带内**——默认带沿用不变；配置态 live 面锚点全数在历史带内
（apply(avoid=10,M=10) 261–289 ns/task（257–312 带内）、空策略
113–133（110–140 带内）、load 现实 registry 77.2–120.4 µs
（74.3–134.5 带内 9/9）；load fresh(ENOENT) 31.8–41.9 µs，5/9 采样
高出历史并集上缘（36.8）2.3–5.1 µs——once-per-run µs 级 ENOENT 路径
的本 VM 会话位置噪声，方向单侧、realistic 载入与 apply 面同批全数
带内、零 diff 下 VERDICT 行逐位一致，非切片漂移，只读作价签不改带；
历史 nonempty 格 VERDICT `allowed=5 preferred=m5` 二十八轮不变）；
`pnpm gate` 在 engines 合规 Node v22.22.2 全绿（1169 tests / 78
suites / 1168 pass / 0 fail / 1 skipped，计数与 R14-E..R27-E 逐一
相同）；切片套件 48/48 双版本过。S8-E-1 / S9-E-2 / S13-B-1 未以任何
形态触碰。R27-E RVL、R26-E RTL、R25-E CG、R24-E JB、R22-E NVG 未复铸
（本轮不动裁决格、不触 apply 终止腿、无 TASK_RETRY 链态、join 恒
完整路由、无数值类剂量——PWL 的 outcome 行夹具恒 2 行固定多重集，
仅作到达腿的杠杆）；R23-E GS 未复铸（diagnose 仅作 W1 到达杠杆不
重价）；R28-A input key-set geometry face、R28-B catalog-face
row-arrangement (permutation) face、R28-C C-slice numeric-kernel
execution-substrate instantiation（快进补入）、R28-D contents-store
population face（快进补入）、R27-H execution-substrate face、R27-J
**SIDEC**、R27-A scalar numeric value-class face、R27-I process
output-channel disposition face 均未移植。不声称 Outcome-supported；Checkpoint F-PROD 仍开放
（ADR-005）。**切片关闭，MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/learning/` 全部 10 文件（`attribution` 92 行 /
  `auto-loop` 283 / `bandit-store` 83 / `diagnostics` 78 /
  `from-episode` 298 / `learned-routing` 216 / `patterns` 146 /
  `signals` 439 / `signatures` 66 / `task-success` 69 = 1770 行，
  `wc -l` 与 R14-E..R27-E 逐一相同）。
- 生产最后落地 `adb20d7`；`git diff adb20d7..HEAD -- src/learning/`
  空（开工、快进 rebase 后、写报告前各验一次）。
- 独占分支运行中两次 docs-only 快进：`5ef5379 → 08a1724 → 8e13778`
  （R28-C、R28-D 摄取 + R28-F/G 派出）；`git diff` 复核两次增量仅
  `docs/reports/sota-opt/`，`src/` 零变化——未按生产变更对待，按令
  rebase 跟进并把 R28-C「C-slice numeric-kernel execution-substrate
  instantiation」与 R28-D「contents-store population face」加入
  不移植清单（后者对象为 `src/adaptation/` 的 `contentsByHash` 别名
  重数 µ × 尺寸分区——本轴 registry 字节恒钉最小态、无别名重数剂量，
  无交集）。报告首推后第三次快进 `8e13778 → c146dae`（R28-F
  「boundary call-protocol face」摄取 + R28-H 派出）：增量复核仍
  docs-only（`src/` 零行）、10 文件冻结对 `adb20d7` 在 `c146dae` 下
  重验 0 行、R28-F 轴补入 §6（本套件全部函数直呼、无调用适配器
  剂量，无交集）；分支已推送故不改写历史，merge-base `8e13778` 为
  `c146dae` 祖先，末态 `git diff exclusive...HEAD` 仍恰为本报告一个
  文件。
- 硬不变量全数未触碰：分析不变更在飞 run；双 LCB / 双归因均在；
  提案先行；无阈值改动；无 fsync / 完整性再哈希删除分析立项（本轮
  恰恰把 W1 的「多余」fsync 记为已关闭条目的余量而**不**提议删除）；
  S5-H-1 / S7-F-1≠S6-F-5 / S8-E-1 / S9-E-2 / S9-D-4 / S12-B-2 /
  S12-D-1 / S13-B-1 全部维持（本轮对 `loadLearnedRouting` 零去重
  分析，apply 本体零改动，S13-B-1 重开条件未触发）。
- 未编辑 EXCLUSIONS.md / PROGRESS.md / PLAN.md；未开 PR；未触兄弟
  分支；package-lock.json 未出现。
- 阅读序合规：README → EXCLUSIONS 全表 → PROGRESS → round-28/PLAN
  （E 段）→ R27-E → R26-E → R25-E（另按令读 R28-A / R28-B 结论与
  轴段，快进后补读 R28-C 结论与轴段）。

## 1. 冻结证明与 SLICE-CPU 重锚

```bash
git log --oneline -1 origin/cursor/sota-persistent-opt-83a1
# 08a1724 docs(sota-opt): ingest R28-C (empty enum) and dispatch R28-F
git diff adb20d7..HEAD --stat -- src/learning/   # （空输出，0 文件）
wc -l src/learning/*.ts                          # 1770 total（同 R14-E..R27-E）
```

锚点脚本 `/tmp/r28e-anchor.mts`（构造 = R10-E 附录 A 逐字同构 +
R28-E 置换，见附录 A）共 **12 次主运行**（6 × v22.22.2 + 6 ×
v22.14.0）+ **9 次 cmp25 对调运行**（R25-E 种子 `0xe25e01/02/08` +
R25-E NOW `17:30`，6 + 3），每次 3/3 sanity 全过，确定性 VERDICT 行
各自跨运行跨版本逐位一致。代表性一次主运行（v22.22.2）：

```text
VERDICT fixtures: E=41 collected=18 routedE=41 outcomes=1 S=12 diagnoseGroups=1 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=13.9us outcomes=1.1us diagnose=1.08us bandit-build=0.8us | total in-slice CPU ~16.8us per run vs landing bar >=10000us (594x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=289ns/task | empty policy=117ns/task -> x10 tasks/run = 2.89us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=34.8us | realistic registry (baseline+1 candidate)=120.4us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

cmp25 对调 9 次 VERDICT 行 9/9 逐位一致且与 R25-E / R26-E / R27-E
夹具行逐字相同（`E=41 collected=14 routedE=41 outcomes=1 S=12
diagnoseGroups=2 prevArms=3`）；总量 15.1 / 15.1 / 15.1 / 15.1 /
15.2 / 15.2 | 15.0 / 15.1 / 15.2 µs vs 派单默认带 14.7–15.2：
**9/9 带内（上缘区 15.0–15.2）**，分段 collect 11.5–11.8 / outcomes
1.3–1.5 / diagnose 1.32–1.45 / bandit 0.8 vs R25-E 带 11.3–11.9 /
1.3–1.5 / 1.33–1.46 / 0.8 全数在带（与 R27-E 记录的 2 个 collect
高位噪声点相比，本 VM 会话 collect 段回到带内）。SLICE-CPU 重锚
判定：**默认带沿用 14.7–15.2 µs/run 不变**，本轮主夹具 16.7–17.2
µs 为新构成点（collected=18，比 R25-E 构成重——collect 段 13.8–14.2
vs 11.5–11.8 即全部差源，随 collected 构成单调，无新机制）。

配置态锚点（9 采样 = 6 × v22.22.2 + 3 × v22.14.0）：apply(avoid=10,
M=10) 261–289 ns/task（R13-E..R27-E 带 257–312 内）、空策略 113–133
（110–140 带内）、load 现实 registry 77.2–120.4 µs（R15-E..R27-E 带
74.3–134.5 内 9/9）。**load fresh(ENOENT) 31.8–41.9 µs：5/9 采样
（39.1/41.0/41.9 | 40.2/41.1）高出历史并集上缘 36.8（R24-E）
2.3–5.1 µs**——once-per-run µs 级 ENOENT 探测路径的本 VM 会话位置
噪声（方向单侧；同批 realistic 载入与两个 apply 面全数带内；零
diff 下夹具逐字节相同、VERDICT 行逐位一致；该面自 R24-E 起历史带
本就最宽 25.2–36.8，为 I/O 依赖最深的一格）——非切片漂移，按 R26-E
(b) / R27-E 位置噪声同例只读作价签记录，不改带、不立项。

## 2. 新轴 PWL：proposal 写路径终止腿面

### 2.1 定义与盲区

受控变量：**一次 propose 调用从哪条 return 腿终止**，同时钉死并
定价该腿的持久写激活集（registry lock / load / save / preference
铸造）。写者普查与 R26-E RTL 同源（写侧）——切片内恰两个提案写者：

- **W2** `proposeRoutingFromOutcomes`（`from-episode.ts:94-155`），
  四腿：N「no bound taskSuccess outcomes」(:98-100，锁前早退，
  子腿 N1 usable=0 / N2 usable>0 但零可归罪 FAIL 行，均死于
  `policyFromOutcomes` :67-88 内)；E「identical candidate already
  proposed」(:115-122)；B「observed policy matches the active
  baseline」(:123-129)；C「proposed routing-policy candidate」
  (:130-143)。preference 尾巴 `recordInferredPreference`
  (:145-153) 仅在 episodeId 绑定时执行，且 **N 腿先于它返回**。
- **W1** `proposeAndMaybePromote`（`auto-loop.ts:150-201`，私有，
  经 `runAutoAdaptLoop` failing 分支 :106-124 到达），三腿：
  C (:178-188 创建)、E (:173-176 命中既有候选)、B (:189-192 基线
  匹配)——**三腿全部无条件存盘**（:190 / :193）。
- W1 上游编排门腿（`runAutoAdaptLoop` :74-133）：kill-switch
  (:93-101)、no-failing / empty (:126-132)——均不取 registry 锁。

调用位点频率普查（行号钉死）：W1 ← `track/loop.ts:172`（每 tracked
run 一次）/ `cli/main.ts:783`（每 run 命令一次）/ `cli/adapt.ts:205`
（每 adapt 命令一次）/ `runAutoAdaptFromEvents`
（`auto-loop.ts:247-282` 内转 :275，← `cli/adapt.ts:188`）；W2 ←
`proposeRoutingFromRoutedEvents`（`from-episode.ts:164-185`）←
`cli/adapt.ts:168`（每 adapt propose 命令一次）与
`proposeRoutingFromAssignments`（:158-162，deprecated 垫片）。
**全部 once-per-run / once-per-command，无每任务重入位点。**

盲区证明（为什么 27 轮没人控过这条腿）：R26-E RTL 的受控变量是
`applyLearnedRouting` 的终止腿——**live 消费面（读侧）**，不触
propose；R24-I 控的是 **load 状态坐标**（registry.json 在场 ×
字节数）且在 I 切片接线上；R20-E 给**并发面**定价（同一锁的竞争
序），腿身份从未变动；S2-E-1 关闭的是「no-change 存盘跳过」这一个
**改写形态**（W2 现行 E/B 腿的零 save 即其落地形），X0-3 关闭的是
W1/W2 **合并**形态，S1-D-2 关闭的是 candidatesFor().find() 的
**索引**形态——三者都是单形态裁决，不是轴：没有任何一轮枚举过腿
集合、钉过逐腿写激活集、把 W1-vs-W2 存盘不对称当作腿属性见证过
（inode/mtime 级），也没有任何一轮测过稳态腿占用（固定剖面下第
k 次运行落在哪条腿、存几次盘）。

### 2.2 与近邻轴的区分（换名自查核心）

| 近邻 | 其受控变量 | PWL 钉死之 | 区分 |
| --- | --- | --- | --- |
| **RTL**（R26-E） | apply 终止腿（live 读侧） | 不触 apply（PWL 无路由、无 catalog） | 同为终止腿透镜，读侧 vs 写侧——函数、位点、激活集三不同 |
| **RVL**（R27-E） | TASK_RESULT 裁决格 | outcome 行夹具恒 2 行固定多重集（仅作到达腿杠杆） | 裁决格是 PWL 的上游输入，不是变量 |
| **CG**（R25-E） | TASK_RETRY 链深 × 增长类 | 无 retry 事件 | 链态 vs 终止腿 |
| **JB**（R24-E） | join 命运 × 绑定完整度 | W1 夹具恒完整路由 join-hit | join vs 终止腿 |
| **GS**（R23-E） | diagnose 消费级守卫链 | diagnose 仅作 W1 到达杠杆不重价 | 消费级守卫 vs 写路径腿 |
| R24-I（I 切片） | registry 载入状态坐标（在场 × 字节） | registry 字节钉最小态（0 或 1 基线 + ≤1 候选） | 状态坐标是杠杆非变量；PWL 控腿不控字节 |
| R25-I（I 切片） | 环境变量坐标 | SPARKLE_AUTO_ADAPT 逐臂钉死 | env 是到达 kill-switch 腿的杠杆，不剂量 |
| R20-E | 锁竞争（并发面） | 全程单进程零竞争 | 并发 vs 腿身份 |
| S2-E-1 / X0-3 / S1-D-2 | 单改写形态裁决 | 不重提任何形态 | 形态裁决 ≠ 完整性轴；PWL 引其为逐腿归因先例 |
| R27-D policy-content tree-geometry（兄弟轮） | adaptation 策略内容树几何 | 策略内容逐写者钉死（W1/W2 各一条固定 avoid） | 内容几何不剂量；未移植 |
| R28-A input key-set geometry face（兄弟） | 调用对象键集几何 | 全部 input 字面量 schema-exact | 未移植 |
| R28-B catalog-face row-arrangement（兄弟） | catalog 行排列 | 无 catalog | 未移植 |
| R28-C numeric-kernel execution-substrate（兄弟，快进补入） | 执行基底 | 单基底（tsx-on-src）双 Node 版本 | 未移植（版本对照仅按惯例复测） |
| R28-D contents-store population face（兄弟，快进补入） | `contentsByHash` 别名重数 µ × 尺寸分区 × 引用态 | registry 内容人口恒最小态（≤1 基线 + ≤1 候选，µ 不剂量） | 未移植；人口是杠杆非变量 |
| R17-E / R19-E | 事件构成 / 次序 | 事件与 outcome 多重集逐臂逐字节相同 | 构成/次序不剂量 |

### 2.3 命运矩阵（确定性，5/5 运行跨版本逐位一致）

10 腿 × { reason 串 / lock / load / save / preference 铸造 }，全部由
`/tmp/r28e-pwl.mts` SEGMENT F 断言（52 检查），存盘见证 = registry.json
的 (ino, mtimeMs) 对比（save 走 temp+fsync+rename 必换 inode；零
save 则两者逐位不变）：

```text
VERDICT PWL W2-N1: reason="no bound taskSuccess outcomes" lock=0 load=0 save=0 pref=0 (pre-lock return, from-episode.ts:98-100)
VERDICT PWL W2-N2: PASS-only usable rows die in policyFromOutcomes avoid.length===0 (from-episode.ts:86) -> same leg as N1
VERDICT PWL W2-C: lock=1 load=1 save=1 pref=1 (baseline+candidate persisted in ONE save) | W2-E: lock=1 load=1 save=0 pref=1 (no-save witness: ino+mtime bit-identical)
VERDICT PWL W2-B: lock=1 load=1 save=0 (reachable in production only through a promoted non-empty active pointer; empty-baseline hash != any W2 policy hash)
VERDICT PWL W1-C: lock=1 load=1 save=1 | W1-E: lock=1 load=1 save=1 (X0-3 save-timing asymmetry vs W2-E save=0, witnessed by inode change on an identical no-change run)
VERDICT PWL W1-B: lock=1 load=1 save=1 (saves an unchanged registry; W2-B saves nothing — both no-change legs, opposite write fates)
VERDICT PWL GATE-disabled: kill switch pins the leg BEFORE the failing branch; bandit write activation is kill-switch-INSENSITIVE (env is a lever pinned per arm — R25-I coordinate not dosed)
VERDICT PWL GATE-nofailing: issues exist but none actionable -> no lock, no registry I/O
VERDICT PWL GATE-empty: zero signals -> even the bandit write gate stays closed
VERDICT PWL W2 steady-state occupancy: legs=[C,E,E] saves=1/3 (fixed failing profile -> load-only steady state)
VERDICT PWL W1 steady-state occupancy: legs=[C,E,E] saves=3/3 (every no-change run rewrites+fsyncs an unchanged registry — the S2-E-1-closed margin, now leg-attributed)
```

汇总为腿 × 写激活集矩阵：

| 腿 | reason 串 | lock | load | save | pref 铸造（episodeId 绑定时） |
| --- | --- | --- | --- | --- | --- |
| W2-N (N1/N2) | no bound taskSuccess outcomes | 0 | 0 | 0 | **0（先于尾巴返回）** |
| W2-E | identical candidate already proposed | 1 | 1 | **0** | 1 |
| W2-B | observed policy matches the active baseline | 1 | 1 | **0** | 1 |
| W2-C | proposed routing-policy candidate | 1 | 1 | 1 | 1 |
| W1-C | proposed routing-policy candidate | 1 | 1 | 1 | —（W1 无 pref 尾巴） |
| W1-E | identical candidate already proposed | 1 | 1 | **1（无条件）** | — |
| W1-B | observed policy matches the active baseline | 1 | 1 | **1（无条件）** | — |
| GATE-disabled | auto-adapt disabled; collected only | 0 | 0 | 0 | —（但 bandit 写**不受门控**） |
| GATE-nofailing | no actionable model-project issue | 0 | 0 | 0 | — |
| GATE-empty | no feedback to learn from | 0 | 0 | 0 | —（bandit 门也关闭） |

跨职权观察（非候选、非 ID，仅记录；均为行为可见的语义/属地面）：

- **(a) W1-vs-W2 存盘不对称**：同一「无变化」腿语义（E/B），W2 零
  save（S2-E-1 的落地形），W1 无条件重写+fsync 未变化的 registry
  （auto-loop.ts:190/:193 的 save 在候选查找**之后**、不看 created
  标志）。稳态下（固定 failing 剖面）W1 每次运行支付 ~0.95–1.20 ms
  的再持久化税，W2 只付 lock+load 的 ~0.33–0.41 ms。把 S2-E-1 语义
  移植进 W1 = 已关闭条目（S2-E-1 / X0-3）的换名/移植 + 切片外 save
  属地 + once-per-run ms 级，仅记录。
- **(b) preference 尾巴的腿敏感性**：`recordInferredPreference` 在
  W2 的 N 腿**不**执行（:98-100 先返回）、在 E/B/C 三条锁腿一律执行
  且不看腿身份——「提了案才记偏好」与「无案可提不记偏好」是行为可见
  的偏好证据语义；移动尾巴（前置/腿过滤/去重）= 功能立项非优化，
  preferences 服务本体切片外。
- **(c) bandit 写激活对 kill-switch 不敏感**：SPARKLE_AUTO_ADAPT=0
  只钉死提案腿（先 collect 后返回），collect 侧 bandit store 写
  (:88-90) 照常发生（learning/ 目录出现）；仅 empty 腿（零信号）连
  bandit 门也不开 (:88 some 条件)。收集与提案的门控边界是设计契约
  （kill switch 只关提案权，不关观测），改动 = 行为变更，仅记录。
- **(d) W2-B 的生产可达性**：自动铸造基线恒空 avoid，W2 策略恒
  avoid.length≥1，哈希不等式使 B 腿在纯自动路径上不可达——只有
  人工/晋升流程把非空策略立为 active 指针后才可达。B 腿因此在自动
  稳态中占用为 0（稳态占用 [C,E,E,…]），其零 save 行为与 E 腿一致，
  无独立成本洞。

### 2.4 逐腿定价（6 采样 = 4 × v22.22.2 + 2 × v22.14.0）

```text
切片内 CPU 前奏 (content+hash+identity):  998–1073 ns/call
W2-N（锁前零 I/O 腿，含 promise 机械）:    540–628 ns/call —— 地板
W2-E 稳态（lock+load, save=0）:           326.5–412.7 µs/run
W2-B（lock+load, save=0）:                314.5–392.0 µs/run
W2-C 一次性（lock+load+save+fsync）:      min 0.65–0.88 / med 1.09–1.34 / max 1.52–2.24 ms（30 新根）
W1-E 稳态（复刻，lock+load+SAVE）:        950.2–1195.3 µs/run —— no-change 再持久化税
W1-C 一次性（复刻）:                      min 0.55–0.85 / med 0.62–1.45 / max 1.08–2.23 ms（30 新根）
```

结构判读：(i) 全部腿 once-per-run（§2.1 调用位点普查）；(ii) 腿间
价差的成分全在 I/O 段——lock（open+flock）+ load（read+parse）≈
0.31–0.41 ms、save（serialize+temp+fsync+rename）≈ +0.6–0.8 ms——
两段实现体（`withAdaptationRegistryLock` / `loadAdaptationRegistry
OrNew` / `saveAdaptationRegistry`）全部在 `src/adaptation/
promotion.ts`（切片外属地）；(iii) 切片内可控份额 = 前奏 ~1.0 µs +
分支/对象机械 <0.2 µs ≈ **0.5–1.1 µs/run**，即使整段清零也距 ≥10 ms
门槛 4 个数量级；(iv) W1-E 与 W2-E 的稳态差（~0.6–0.8 ms/run）即
(a) 观察的再持久化税——once-per-run 拒收带 + 已关闭形态 + 切片外
属地三重出局。两 Node 版本交叉不可分辨。

### 2.5 三重收口

- **频率端**：propose 路径每 run / 每 adapt 命令至多一次（W1 三个
  调用位点 + W2 一个 CLI 位点，行号钉死 §2.1）；无每任务/每事件
  重入放大器；稳态腿占用 [C,E,E,…] 说明 save-bearing 腿只在剖面
  变化时出现。
- **单价端**：切片内 CPU 全谱 0.5–1.1 µs/run；腿间最大 I/O 价差
  ~0.6–0.8 ms once-per-run；最贵腿全额（W1-C med ≤1.45 ms）也在
  once-per-run 拒收带内。
- **属地端**：lock/load/save 实现体 = `src/adaptation/`（R24-E
  「属地拒列」同裁）；no-change 存盘跳过 = S2-E-1、W1/W2 统一 =
  X0-3、候选 find 扫描 = S1-D-2（全部已关闭条目）；preference
  尾巴与 bandit 门控边界 = 行为可见的学习证据契约；fsync 删除 =
  硬不变量明令禁止。**不产生候选。**

### 2.6 第二十八组枚举表

方法：本轮代码侧透镜「**谁终止、写什么**」：对第二十八遍全量重读的
10 个源文件逐函数问 (a) 哪些位置的成本/行为随终止腿变动，(b) 变动处
有无等价可削形态；同时按惯例对逐字新点名位置做换名检查（对照
EXCLUSIONS 全表 + 二十七轮 E 报告 + 十七轮空枚举面 + 三条 R28 兄弟
轴）。扫描结果：**零个可立项形态**（均未铸新 ID）：

| 形态（本轮第二十八遍读码实际定位） | 理论收益位 | 裁决 | 对应先例 |
| --- | --- | --- | --- |
| W1 no-change 存盘跳过（把 created/changed 标志接到 :190/:193 的 save 上） | ~0.6–0.8 ms/no-change run（once-per-run） | 换名/移植拒列：S2-E-1（存盘跳过形态已裁）+ X0-3（W1/W2 统一已裁）；save 本体切片外；registry mtime/inode 是外部可观测见证（行为邻接） | S2-E-1 / X0-3 |
| W1/W2 提案体合一（同锁同腿序单实现） | 消一份重复体 | 换名重提拒列：X0-3 逐字维持（两写者 actor 身份、腿序、save 语义有意分立） | X0-3 |
| candidatesFor().find(contentHash) 扫描 → 哈希索引 | ns–µs/腿（候选数最小态） | 换名重提拒列：S1-D-2 find 扫描索引已裁；生产候选基数无增长证据 | S1-D-2 |
| registry 跨调用缓存 / propose 间复用已载入实例 | ~0.31–0.41 ms/稳态 run | 拒列：X1-1 缓存邻域 + S8-E-1 并发晋升新鲜度分歧同式；锁语义要求锁内重载 | X1-1 / S8-E-1 |
| 锁窗收窄（load 出锁 / 仅 save 持锁） | µs 级 | 拒列：TOCTOU——锁内 load+判定+save 是并发正确性契约；R20-E 并发面已价、锁序为契约 | R20-E |
| W2 前奏惰性化（content/hash/identity 后置到锁内首用点） | ≤1 µs/run | 拒收：µs 级 once-per-run；N 腿本就先于前奏返回（:97-100 在 :101-103 之前），已是零支付 | 门槛条款 |
| preference 尾巴移动 / 腿过滤 / 去重 | µs 级 | 行为可见的偏好证据语义功能立项（(b) 观察）；preferences 服务切片外 | X0-2 邻域 |
| bandit 写提前门控（kill-switch 下跳过 collect 写） | ~µs–ms once-per-run | 行为变更：观测权与提案权的门控边界是设计契约（(c) 观察）；env 剂量 = R25-I 坐标 | R25-I + 硬不变量 |
| W2-N 零 I/O 腿特化 | 已在 540–628 ns 地板 | 无可省：guard+return 两步即返回 | — |
| gate 腿 reason 串预制 / 返回对象复用 | ns 级 | 拒收：ns 级 once-per-run；返回对象是公开结果契约 | 门槛条款 |
| 漂移带来的新调用边 / 新写者 / 新热路径 | — | 不存在：切片二十八遍零漂移；调用位点普查行号钉死（§2.1）；快进增量纯 docs | — |

**提议排除 ID：无（空枚举未铸任何 S28-E-\*，亦未向 EXCLUSIONS 新增
轴昵称行——PWL 为 observation-only 轴，按令由父代 plan-ban）。**

## 3. 逐文件复核

| 文件 | 第二十八遍终止腿透镜下的复核 | 结论 |
| --- | --- | --- |
| `from-episode.ts` | W2 四腿命运矩阵 + 两条可达性定理 + preference 尾巴腿敏感性（§2.3）；policyFromOutcomes 的 N 子腿分解（usable=0 vs avoid=0）；环内 try/catch 维持（R17-E A4/A5） | 无候选 |
| `auto-loop.ts` | W1 三腿全 save 语义 + 编排门三腿零锁 + bandit 写门控不对称（§2.3 (c)）；optimizedPolicy 内容逐字复刻验证（哈希相等断言）；主模型保护不对称维持 R26-E (a) 记录 | 无候选 |
| `learned-routing.ts` | ensureRoutingBaseline 在 W2-C 单 save 内联注册（可达性定理 i）；未触 apply/load（RTL / S8-E-1 / S13-B-1 关闭） | 无候选 |
| `bandit-store.ts` | updateProjectBandit 作为 GATE-disabled 腿的不受门控写激活见证（(c)）；store 本体未触（fail-soft 契约） | 无候选 |
| `signals.ts` | collect 侧仅作 W1 到达杠杆（裁决格 RVL 关闭不复铸）；S9-E-2 未触 | 无候选 |
| `diagnostics.ts` | diagnose 仅产 actionable 判定供 failing 分支（GS 关闭不重价）；mode()/unique 维持 | 无候选 |
| `task-success.ts` | V2 语义未触（RVL 关闭）；S2-E-7 / S9-E-3 维持 | 无候选 |
| `patterns.ts` / `attribution.ts` / `signatures.ts` | test-only 面复核仍零生产调用方，自动拒列；X2-6、S1-E-7/8 维持 | 无候选 |

## 4. 测试与验证面

零代码改动下全量 gate 与切片套件基线复核：

```bash
# 本 VM: shell 默认 node v22.22.2（~/.nvm，engines >=22.19.0 合规）；
# v22.14.0 经 /exec-daemon/node 提供（对照批次以
# /exec-daemon/node node_modules/tsx/dist/cli.mjs 调度）。
pnpm gate
# typecheck ✓ + lint ✓ + test ✓ + build ✓ （GATE_EXIT=0）
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（计数与 R14-E..R27-E 逐一相同）

npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0 （v22.22.2 与 v22.14.0 均过）
```

锚点脚本 12 次主运行 + 9 次 cmp25 对调全过（§1）；PWL 轴套件
（`/tmp/r28e-pwl.mts`，全文见附录 A）共 **5 次命运段运行**
（3 × v22.22.2 + 2 × v22.14.0），每次 **52/52 sanity 全过**（腿
reason/返回形状断言 + 存盘 ino/mtime 见证 ×6 + preference 计数见证
×4 + 可达性哈希不等式 + 稳态占用断言），全部 VERDICT 行跨 5 次运行
跨版本逐位一致；定价段 6 采样（4 + 2）区间见 §2.4 与附录 A。

本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交
`scripts/round28-r28e-equivalence-sim.ts`，遵守 R7-F/R8-C/R9-I/
R10-E..R27-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件
（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 仅
`docs/reports/sota-opt/round-28/R28-E.md`）。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2.6 空枚举，逐形态有理由；第二十八遍以「谁
  终止、写什么」透镜普查新点名的全部维度落进换名重提区（S2-E-1 /
  X0-3 / S1-D-2 / X1-1）、属地区（lock/load/save/preferences 本体
  切片外）、契约面（锁窗 / 门控边界 / 偏好语义 / fsync 硬禁）或
  实测清零区（N 腿地板、gate 腿零锁）；新轴本身以「无候选」收口，
  未发现第二十二个机制类）。
- 剩余赢家：无（全部 µs–ms 级余量为 once-per-run I/O 且属地/形态
  双重出局；CPU 温态面即使清零也距门槛 582–600×（本构成）/
  658–665×（cmp25 构成）；切片内提案路径 CPU 份额 0.5–1.1 µs/run
  距门槛 4 个数量级）。
- 矩阵空洞：无（调用位点行号与 R8-E..R27-E 逐一相同；配置态 live
  面与载入面锚点除 load-fresh 5/9 高位噪声点（§1，单侧、只读价签）
  外全数带内；双 Node 版本交叉不可分辨；cmp25 种子对调 9/9 VERDICT
  逐字相同、9/9 带内）。
- 终止腿假说的写侧对偶（「propose 腿里藏着未测的成本洞或语义洞」）：
  成本洞以逐腿定价关闭（写激活集全谱 540 ns–1.45 ms，腿间差全落
  切片外 I/O 段）；语义洞以命运矩阵关闭且**有实洞**——W1-vs-W2
  存盘不对称、preference 尾巴腿敏感性、bandit 写门控不对称、W2-B
  生产可达性约束——全部为行为可见的契约/属地面（记录为 §2.3
  (a)–(d)，非候选）。「终止腿」维度自此读写双侧完成：live 消费腿
  （R26-E RTL）× 提案写腿（本轮）。
- S13-B-1 边界：apply 本体零触碰，重开条件未触发。S8-E-1 零去重
  分析。S9-E-2 未触碰。RVL/RTL/CG/JB/NVG/GS 未复铸（§2.2 逐轴
  区分）。
- 不移植清单（含运行中快进增量）：R28-A input key-set geometry
  face、R28-B catalog-face row-arrangement (permutation) face、
  R28-C C-slice numeric-kernel execution-substrate instantiation
  （快进补入；face 名归 R27-H）、R28-D contents-store population
  face（快进补入；`contentsByHash` 别名重数 µ × 尺寸分区 × 引用态
  ——本轴无内容存储人口剂量）、R28-F boundary call-protocol face
  （首推后快进补入；公开函数调用适配器形态——本套件全部直呼、
  无调用适配器剂量）、R27-A scalar numeric value-class
  face、R27-B batch-cardinality bottom + prelude intercept face、
  R27-C **PROBX**、R27-D policy-content tree-geometry face、R27-F
  input access-path mechanics face、R27-G join-policy face、R27-H
  execution-substrate face、R27-I process output-channel
  disposition face、R27-J **SIDEC**、R26-A..R26-J 与 R25-A..R25-I
  及 R24-B/D/F/H/I 各无名面、以及全部已名轴（FITQ / NAMESHAPE /
  SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX /
  TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM /
  VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX /
  RVL / SIDEC）——逐一定位核过，与本轴对象机制无交集。R28-G /
  R28-H 在飞，未读、未等待、未移植。
- 遗留「maybe later」候选：无。

切片级重开总条件维持 R3-E §7 … R27-E §6（含 R14-E 规模判据、R15-E
分配/GC/噪声地板判据、R16-E 累积状态与载荷形态判据、R17-E 异常面与
构成面判据、R18-E 执行层级与字符宽度判据、R19-E 表示面与次序面判据、
R20-E 并发面判据、R21-E 标识符几何判据、R22-E 数值值类判据、R23-E
消费级结构判据、R24-E 铸造级连接判据、R25-E 同键链态判据、R26-E
终止腿判据、R27-E 裁决格判据），并新增一条可标定判据：**(k) 提案腿
判据**——若 propose 路径获得每任务/每事件级调用位点（脱离
once-per-run 包络，调用位点普查 §2.1 需重证）、或 S2-E-1 / X0-3 的
形态裁决被正式推翻（届时 W1 no-change 存盘跳过按 (a) 观察重价）、
或 lock/load/save 实现体迁入本切片（属地翻转）、或 preference 尾巴
在锁腿间变为腿敏感（偏好证据语义变更需重证 (b)）、或 registry 生产
基数增长使 load 腿重 ≥2 个数量级（与 R24-I 载入面联动），优先复跑
PWL 十腿双版本套件；或任一被引裁决（S2-E-1 / X0-3 / S1-D-2 / X1-1 /
R20-E 并发面）被正式推翻，或切片/调用面出现实际代码漂移。

**MORE_OPTIMA=no——切片关闭。**

---

## 附录 A：本轮测量资产（未提交 scripts/，遵守无赢家纪律）

临时脚本 sha256（测量完成后已从 /tmp 删除，正文与本附录为其存档）：

```text
6c48a9e9afcbcf67cf0c0869a7b42322c1446500cd78dbcfbed9c6b3f13fd1aa  /tmp/r28e-anchor.mts
742da5ec3ff30076e148db3ad249a72bfcb2cc9035117b3b3125d11350f930b0  /tmp/r28e-pwl.mts
```

主锚点脚本全文（`/tmp/r28e-anchor.mts`，`npx tsx /tmp/r28e-anchor.mts`）
与 R10-E 附录 A 脚本同构（夹具生成器逐字镜像），仅以下替换即可从
R10-E 附录 A 逐字重建：头注释轮次改 R28-E；
`NOW = "2026-08-26T00:30:00.000Z"`；SECTION 1 的 seeds
`0xe28e01`（collect）/ `0xe28e02`（routed）/ `0xe28e03`（signals）——
拒绝采样：collect/routed 角首选一次通过（collected=18>3、
outcomes=1>0），signals 角首选 03 即通过（diagnoseGroups=1≥1，
本轮零拒绝）；探针三元组 k=1..12 由 `probe` 开关打印（01:
(18,1,0) / 02: (17,1,1) / 03: (14,6,1) / …）；SECTION 2 的
`mkdtemp` 前缀 `r28e-fresh-`/`r28e-reg-` 与 detector identity
`r28e-anchor`。另加 CLI 开关 `probe` 与 `cmp25`（置换为 R25-E 的
NOW（`2026-08-24T17:30`）与 seeds `0xe25e01/02/08`、前缀
`r28e-cmp25-`、identity `r28e-anchor-cmp25`）。其余逐字同 R10-E
附录 A。

12 次主锚点运行原始区间（6 × v22.22.2 + 6 × v22.14.0）：

```text
SLICE-CPU 总量:
  v22.22.2: 16.8 / 16.7 / 16.8 / 16.7 / 17.2 / 16.8 us
  v22.14.0: 16.9 / 16.9 / 17.0 / 16.8 / 16.9 / 16.9 us
collect:    13.8-14.2 us（collected=18 构成）  outcomes: 1.1 us（outcomes=1 构成）
diagnose:   1.03-1.13 us（diagnoseGroups=1 构成） bandit:   0.7-0.8 us
apply(non): 261-289 ns/task    apply(空): 113-133 ns/task
load fresh: 31.8-41.9 us（5/9 高出历史并集上缘 36.8，§1 位置噪声记录）
load real:  77.2-120.4 us（74.3-134.5 带内 9/9）
VERDICT 行: 12/12 逐位一致；sanity 3/3 x12 全过
```

9 次 R25-E 种子比对运行（cmp25）原始值（6 × v22.22.2 + 3 × v22.14.0）：

```text
总量: 15.1 / 15.1 / 15.1 / 15.1 / 15.2 / 15.2 | 15.0 / 15.1 / 15.2 us
  —— vs 派单默认带 14.7-15.2：9/9 带内（上缘区）
collect 11.5-11.8 / outcomes 1.3-1.5 / diagnose 1.32-1.45 / bandit 0.8
  —— vs R25-E 11.3-11.9 / 1.3-1.5 / 1.33-1.46 / 0.8：全数带内
VERDICT 行: 9/9 逐位一致且与 R25-E / R26-E / R27-E 夹具行逐字相同
```

PWL 轴套件 5 次命运段 + 6 次定价采样原始区间（命运段 3 × v22.22.2 +
2 × v22.14.0，定价 4 + 2；v22.14.0 批次以 `/exec-daemon/node
node_modules/tsx/dist/cli.mjs` 调度）：

```text
prelude:  1015 / 998 / 1044 / 1035 | 1073 / 1005 ns/call
W2-N:     559 / 611 / 628 / 544 | 580 / 540 ns/call
W2-E 稳态: 412.7 / 372.4 / 358.0 / 326.5 | 384.2 / 400.4 us/run
W2-B:     389.4 / 314.5 / 392.0 / 341.2 | 390.5 / 330.1 us/run
W2-C 一次性 med: 1.34 / 1.25 / 1.31 / 1.09 | 1.28 / 1.33 ms（min 0.65-0.88 max 1.52-2.24）
W1-E 稳态: 1027.0 / 988.2 / 1148.7 / 950.2 | 1195.3 / 1181.2 us/run
W1-C 一次性 med: 0.62 / 1.09 / 1.10 / 0.87 | 0.78 / 1.45 ms（min 0.55-0.85 max 1.08-2.23）
VERDICT 行: 5/5 逐位一致；sanity 52/52 x5 全过
```

PWL 轴套件全文（`/tmp/r28e-pwl.mts`，`npx tsx /tmp/r28e-pwl.mts`）：

```ts
/**
 * R28-E new completeness axis: proposal write-path termination-leg face (PWL) —
 * WHICH return leg the post-run proposal path exits on, as the controlled
 * variable, with the per-leg persistent-write activation set (registry lock /
 * load / save / preference mint) pinned and priced at both in-slice writers.
 *
 * The two in-slice proposal writers (same census as R26-E RTL, write side):
 *   W2 = proposeRoutingFromOutcomes (from-episode.ts:94-155), legs:
 *     N  ("no bound taskSuccess outcomes", :98-100)  — PRE-LOCK early return
 *         (sub-legs N1 usable=0 / N2 usable>0 but zero avoidable FAIL rows,
 *          both die inside policyFromOutcomes :67-88);
 *     E  ("identical candidate already proposed", :115-122) — lock+load, NO save;
 *     B  ("observed policy matches the active baseline", :123-129) — lock+load, NO save;
 *     C  ("proposed routing-policy candidate", :130-143) — lock+load+createCandidate+SAVE.
 *     recordInferredPreference tail (:145-153) runs on E/B/C when episodeId
 *     is bound; leg N returns BEFORE it.
 *   W1 = proposeAndMaybePromote (auto-loop.ts:150-201, private; reached via
 *        runAutoAdaptLoop failing branch :106-124), legs:
 *     C  (create, :178-188) — lock+load+SAVE;
 *     E  (identical, :173-176 existing hit) — lock+load+SAVE (:193 saves anyway);
 *     B  (baseline match, :189-192) — lock+load+SAVE (:190 saves anyway).
 *   Orchestrator gate legs upstream of W1 (runAutoAdaptLoop): kill-switch
 *   ("auto-adapt disabled; collected only"), no-failing ("no actionable
 *   model-project issue"), empty ("no feedback to learn from") — none takes
 *   the registry lock.
 *
 * What prior passes did NOT control: R26-E RTL controlled the LIVE face
 * (applyLearnedRouting return legs); R24-I controlled the LOAD side
 * (registry.json presence x bytes on I-slice wiring); R20-E priced the
 * CONCURRENCY face of this path (lock contention); S2-E-1/X0-3/S1-D-2 closed
 * individual rewrite forms (no-change save skip, W1/W2 merge, find-scan
 * index). No pass ever pinned WHICH leg a proposal terminates on, the per-leg
 * write-activation set, the W1-vs-W2 save asymmetry as a leg property, or
 * steady-state leg occupancy.
 *
 * Levers, not variables (non-port declarations): registry state is a lever to
 * reach legs (R24-I's coordinate is not dosed — bytes pinned at minimal);
 * SPARKLE_AUTO_ADAPT env is a lever pinned per arm (R25-I's coordinate is not
 * dosed); policy CONTENT is pinned per writer (R27-D tree-geometry not dosed);
 * outcome-row multiset is pinned (R17-E/R19-E composition/order not dosed).
 *
 * Segments (argv[2] optional filter): F (fate matrix + reachability +
 * asymmetry witnesses) | P (per-leg pricing). Deterministic fixtures, no RNG.
 */
import { mkdtemp, stat, access } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { performance } from "node:perf_hooks";
import { proposeRoutingFromOutcomes } from "/workspace/src/learning/from-episode.js";
import { runAutoAdaptLoop } from "/workspace/src/learning/auto-loop.js";
import {
  ensureRoutingBaseline,
  routingPolicyContent,
  routingPolicyIdentity,
  type LearnedRoutingPolicy
} from "/workspace/src/learning/learned-routing.js";
import { hashCandidateContent } from "/workspace/src/adaptation/candidate.js";
import {
  adaptationRegistryPath,
  loadAdaptationRegistry,
  loadAdaptationRegistryOrNew,
  saveAdaptationRegistry,
  withAdaptationRegistryLock
} from "/workspace/src/adaptation/promotion.js";
import { adaptationRoot } from "/workspace/src/privacy/state-layout.js";
import { parseOutcomeObservation, type OutcomeObservation } from "/workspace/src/routing/outcomes.js";
import { inspectPreferences } from "/workspace/src/preferences/service.js";
import { oneHotDistribution } from "/workspace/src/routing/catalog-model.js";
import type { Event } from "/workspace/src/run/events.js";
import type { ProjectId, EpisodeId } from "/workspace/src/domain/ids.js";
import type { IsoTimestamp } from "/workspace/src/domain/timestamp.js";

const SEG = process.argv[2] ?? "FP";
const NOW = "2026-08-26T00:45:00.000Z" as IsoTimestamp;
const PROJECT_ID = "prj_pwl00001" as ProjectId;
const EPISODE_ID = "epi_pwl00001" as EpisodeId;
const PRIMARY = "m-good";

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}`);
  }
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function exists(path: string): Promise<boolean> {
  try {
    await access(path);
    return true;
  } catch {
    return false;
  }
}
async function saveWitness(path: string): Promise<{ ino: number; mtimeMs: number }> {
  const s = await stat(path);
  return { ino: Number(s.ino), mtimeMs: s.mtimeMs };
}

/* ---------- deterministic outcome fixtures (schema-true via parser) ---------- */
function passRow(i: number): OutcomeObservation {
  return parseOutcomeObservation({
    taskFamily: "edit",
    role: "actor",
    modelId: "m-bad",
    modelVersion: "v1",
    featureVersion: "fv1",
    criterion: "taskSuccess",
    outcome: "PASS",
    occurredAtMs: Date.parse(NOW),
    source: "deterministic-check",
    taskId: `tsk_0000000${i}`,
    runId: "run_pwl00001",
    evidenceIds: ["evd_00000001"]
  });
}
function failRow(i: number): OutcomeObservation {
  return parseOutcomeObservation({
    taskFamily: "edit",
    role: "actor",
    modelId: "m-bad",
    modelVersion: "v1",
    featureVersion: "fv1",
    criterion: "taskSuccess",
    outcome: "FAIL",
    occurredAtMs: Date.parse(NOW),
    source: "deterministic-check",
    failureClass: "model",
    taskId: `tsk_0000000${i}`,
    runId: "run_pwl00001",
    evidenceIds: ["evd_00000001"]
  });
}
const FAIL_OUTCOMES = [failRow(1), failRow(2)];
const PASS_OUTCOMES = [passRow(1), passRow(2)];

/* W2 policy replica of policyFromOutcomes (from-episode.ts:67-88) on
 * FAIL_OUTCOMES — property order matches the avoid.push literal
 * (modelId, family, reason) and the return literal (primaryModelId, avoid,
 * prefer), so routingPolicyContent bytes are identical. */
const W2_POLICY: LearnedRoutingPolicy = {
  primaryModelId: PRIMARY,
  avoid: [
    {
      modelId: "m-bad",
      family: "edit",
      reason: "deterministic-check taskSuccess FAIL (failureClass=model)"
    }
  ],
  prefer: []
};

/* W1 policy replica of optimizedPolicy (auto-loop.ts:135-148) on the failing
 * events fixture below (samples=5, meanScore=0.15, family=edit) — property
 * order matches (modelId, reason, family) / (primaryModelId, avoid, prefer). */
const W1_POLICY: LearnedRoutingPolicy = {
  primaryModelId: PRIMARY,
  avoid: [
    {
      modelId: "m-bad",
      reason: "meanScore 0.15 over 5 samples",
      family: "edit"
    }
  ],
  prefer: [{ family: "edit", modelId: PRIMARY }]
};

/* Failing events fixture for the real W1 path via runAutoAdaptLoop:
 * 5 routed tasks on m-bad + 5 TASK_RESULT FAILURE|FAILED -> one actionable
 * (project, m-bad) issue (samples=5 >= 5, meanScore=0.15 < 0.45,
 * deterministic-only) -> failing branch -> W1. */
function failingEvents(projectRoot: string): Event[] {
  const out: unknown[] = [
    {
      type: "PROJECT_DISCOVERED",
      runId: "run_pwl00001",
      occurredAt: NOW,
      payload: { project: { id: PROJECT_ID, rootPath: projectRoot } }
    }
  ];
  for (let i = 1; i <= 5; i += 1) {
    out.push({
      type: "MODEL_ROUTED",
      runId: "run_pwl00001",
      occurredAt: NOW,
      payload: {
        taskId: `tsk_0000000${i}`,
        model: "m-bad",
        role: "actor",
        eligibleModels: ["m-bad", PRIMARY],
        behaviorDistribution: oneHotDistribution(["m-bad", PRIMARY], "m-bad"),
        family: "edit",
        modelVersion: "v1",
        featureVersion: "fv1",
        agentRole: "worker"
      }
    });
  }
  for (let i = 1; i <= 5; i += 1) {
    out.push({
      type: "CHILD_MESSAGE",
      runId: "run_pwl00001",
      occurredAt: NOW,
      payload: {
        message: {
          type: "TASK_RESULT",
          taskId: `tsk_0000000${i}`,
          runId: "run_pwl00001",
          outcome: "FAILURE",
          verification: { kind: "FAILED" },
          summary: "plain verdict probe body",
          evidenceIds: ["evd_00000001"]
        }
      }
    });
  }
  return out as Event[];
}

function passingEvents(projectRoot: string): Event[] {
  const events = failingEvents(projectRoot) as unknown as Array<Record<string, unknown>>;
  for (const event of events) {
    if (event.type !== "CHILD_MESSAGE") continue;
    const payload = event.payload as { message: Record<string, unknown> };
    payload.message.outcome = "SUCCESS";
    payload.message.verification = { kind: "PASSED" };
  }
  return events as unknown as Event[];
}

/* Verbatim replica of the private W1 body proposeAndMaybePromote
 * (auto-loop.ts:150-201) for pricing isolation (same replica discipline as
 * the R2-E banditBuild reference form). */
const LEARN_EVALUATION_PLAN = {
  stages: ["static", "replay"],
  metrics: ["task-success", "cost"],
  planVersion: 1
} as const;
const AUTO_ACTOR = "pi-sparkle-auto-loop";
async function w1Replica(input: {
  readonly stateRoot: string;
  readonly projectRoot: string;
  readonly policy: LearnedRoutingPolicy;
}): Promise<{ created: boolean; promoted: boolean; candidateId?: string; reason: string }> {
  const content = routingPolicyContent(input.policy);
  const contentHash = hashCandidateContent(content);
  const identity = routingPolicyIdentity(input.projectRoot);
  return withAdaptationRegistryLock(input.stateRoot, async () => {
    const registry = await loadAdaptationRegistryOrNew(input.stateRoot);
    const parent = ensureRoutingBaseline(registry, identity, input.policy.primaryModelId, AUTO_ACTOR);
    const existing = registry
      .candidatesFor(identity)
      .find((candidate) => candidate.contentHash === contentHash);
    let candidateId = existing?.candidateId;
    let created = false;
    if (existing === undefined && parent.contentHash !== contentHash) {
      const candidate = registry.createCandidate({
        identity,
        content,
        parentVersionId: parent.versionId,
        author: { kind: "detector", identity: AUTO_ACTOR },
        evaluationPlan: LEARN_EVALUATION_PLAN
      });
      candidateId = candidate.candidateId;
      created = true;
    }
    if (candidateId === undefined) {
      await saveAdaptationRegistry(input.stateRoot, registry);
      return { created: false, promoted: false, reason: "observed policy matches the active baseline" };
    }
    await saveAdaptationRegistry(input.stateRoot, registry);
    return {
      created,
      promoted: false,
      candidateId,
      reason: created ? "proposed routing-policy candidate" : "identical candidate already proposed"
    };
  });
}

/* ================================================================
 * SEGMENT F: fate matrix + reachability + save-asymmetry witnesses.
 * ================================================================ */
if (SEG.includes("F")) {
  /* ---- W2 leg N1: zero usable rows -> pre-lock early return, zero I/O. ---- */
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-w2n1-"));
    const prefBefore = inspectPreferences().count;
    const res = await proposeRoutingFromOutcomes({
      stateRoot: root,
      projectRoot: "/tmp/proj-pwl",
      projectId: PROJECT_ID,
      episodeId: EPISODE_ID,
      primaryModelId: PRIMARY,
      outcomes: []
    });
    const prefAfter = inspectPreferences().count;
    check("W2-N1 reason", res.reason === "no bound taskSuccess outcomes");
    check("W2-N1 created=false", !res.created);
    check("W2-N1 no candidateId/parentVersionId", res.candidateId === undefined && res.parentVersionId === undefined);
    check("W2-N1 zero write activation (no adaptation dir)", !(await exists(adaptationRoot(root))));
    check("W2-N1 no lock file", !(await exists(`${adaptationRegistryPath(root)}.lock`)));
    check("W2-N1 preference NOT minted despite bound episodeId", prefAfter === prefBefore);
    console.log(
      `VERDICT PWL W2-N1: reason="no bound taskSuccess outcomes" lock=0 load=0 save=0 pref=0 (pre-lock return, from-episode.ts:98-100)`
    );
  }

  /* ---- W2 leg N2: usable PASS rows only -> same leg (avoid empty). ---- */
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-w2n2-"));
    const res = await proposeRoutingFromOutcomes({
      stateRoot: root,
      projectRoot: "/tmp/proj-pwl",
      projectId: PROJECT_ID,
      primaryModelId: PRIMARY,
      outcomes: PASS_OUTCOMES
    });
    check("W2-N2 reason (same string as N1)", res.reason === "no bound taskSuccess outcomes");
    check("W2-N2 zero write activation", !(await exists(adaptationRoot(root))));
    console.log(
      `VERDICT PWL W2-N2: PASS-only usable rows die in policyFromOutcomes avoid.length===0 (from-episode.ts:86) -> same leg as N1`
    );
  }

  /* ---- W2 legs C then E on one root: create saves once; identical never saves. ---- */
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-w2ce-"));
    const prefBefore = inspectPreferences().count;
    const resC = await proposeRoutingFromOutcomes({
      stateRoot: root,
      projectRoot: "/tmp/proj-pwl",
      projectId: PROJECT_ID,
      episodeId: EPISODE_ID,
      primaryModelId: PRIMARY,
      outcomes: FAIL_OUTCOMES
    });
    const prefAfterC = inspectPreferences().count;
    check("W2-C created=true", resC.created);
    check("W2-C reason", resC.reason === "proposed routing-policy candidate");
    check("W2-C candidateId+parentVersionId", resC.candidateId !== undefined && resC.parentVersionId !== undefined);
    check("W2-C registry.json persisted", await exists(adaptationRegistryPath(root)));
    check("W2-C preference minted (episodeId bound)", prefAfterC === prefBefore + 1);

    // One save persisted baseline + candidate together (reachability theorem 1).
    const reloaded = await loadAdaptationRegistry(root);
    const identity = routingPolicyIdentity("/tmp/proj-pwl");
    const active = reloaded.getActiveVersion(identity);
    check("W2-C baseline registered+persisted in same save", active !== undefined);
    check(
      "W2-C baseline content is the empty-avoid baseline",
      active !== undefined &&
        active.contentHash ===
          hashCandidateContent(routingPolicyContent({ primaryModelId: PRIMARY, avoid: [], prefer: [] }))
    );
    check("W2-C exactly one candidate", reloaded.candidatesFor(identity).length === 1);
    check(
      "W2 policy replica hash equals persisted candidate hash",
      reloaded.candidatesFor(identity)[0]!.contentHash === hashCandidateContent(routingPolicyContent(W2_POLICY))
    );

    const before = await saveWitness(adaptationRegistryPath(root));
    const prefBeforeE = inspectPreferences().count;
    const resE = await proposeRoutingFromOutcomes({
      stateRoot: root,
      projectRoot: "/tmp/proj-pwl",
      projectId: PROJECT_ID,
      episodeId: EPISODE_ID,
      primaryModelId: PRIMARY,
      outcomes: FAIL_OUTCOMES
    });
    const after = await saveWitness(adaptationRegistryPath(root));
    check("W2-E created=false", !resE.created);
    check("W2-E reason", resE.reason === "identical candidate already proposed");
    check("W2-E returns existing candidateId", resE.candidateId === resC.candidateId);
    check("W2-E NO save (ino+mtime unchanged)", after.ino === before.ino && after.mtimeMs === before.mtimeMs);
    check("W2-E preference minted again (leg-insensitive among lock legs)", inspectPreferences().count === prefBeforeE + 1);
    console.log(
      `VERDICT PWL W2-C: lock=1 load=1 save=1 pref=1 (baseline+candidate persisted in ONE save) | W2-E: lock=1 load=1 save=0 pref=1 (no-save witness: ino+mtime bit-identical)`
    );
  }

  /* ---- W2 leg B: active content == proposed content -> lock+load, NO save. ----
   * Production reachability: only a PROMOTED non-empty active pointer can
   * match a W2 policy (the ensureRoutingBaseline baseline has avoid=[] and
   * W2 policies always have avoid.length>=1 — hash inequality asserted
   * below). The fixture reaches the same registry state via registerBaseline
   * (state-equivalent lever). */
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-w2b-"));
    const registry = await loadAdaptationRegistryOrNew(root);
    registry.registerBaseline({
      identity: routingPolicyIdentity("/tmp/proj-pwl"),
      content: routingPolicyContent(W2_POLICY),
      author: { kind: "detector", identity: "r28e-pwl" }
    });
    await saveAdaptationRegistry(root, registry);
    const before = await saveWitness(adaptationRegistryPath(root));
    const res = await proposeRoutingFromOutcomes({
      stateRoot: root,
      projectRoot: "/tmp/proj-pwl",
      projectId: PROJECT_ID,
      primaryModelId: PRIMARY,
      outcomes: FAIL_OUTCOMES
    });
    const after = await saveWitness(adaptationRegistryPath(root));
    check("W2-B reason", res.reason === "observed policy matches the active baseline");
    check("W2-B created=false, parentVersionId only", !res.created && res.candidateId === undefined && res.parentVersionId !== undefined);
    check("W2-B NO save (ino+mtime unchanged)", after.ino === before.ino && after.mtimeMs === before.mtimeMs);
    check(
      "W2-B unreachable via empty baseline (hash inequality theorem)",
      hashCandidateContent(routingPolicyContent({ primaryModelId: PRIMARY, avoid: [], prefer: [] })) !==
        hashCandidateContent(routingPolicyContent(W2_POLICY))
    );
    console.log(
      `VERDICT PWL W2-B: lock=1 load=1 save=0 (reachable in production only through a promoted non-empty active pointer; empty-baseline hash != any W2 policy hash)`
    );
  }

  /* ---- W1 legs C, E via the real runAutoAdaptLoop (failing fixture). ---- */
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-w1ce-"));
    const proj = await mkdtemp(join(tmpdir(), "r28e-pwl-proj-"));
    const resC = await runAutoAdaptLoop({
      stateRoot: root,
      projectRoot: proj,
      projectId: PROJECT_ID,
      primaryModelId: PRIMARY,
      events: failingEvents(proj)
    });
    check("W1-C created=true", resC.created);
    check("W1-C reason", resC.reason === "proposed routing-policy candidate");
    check("W1-C collected=5", resC.collected === 5);
    check("W1-C actionable issue present", resC.issues.some((issue) => issue.actionable && issue.modelId === "m-bad"));
    check("W1-C promoted=false (never CAS-promotes)", !resC.promoted);
    check("W1-C registry.json persisted", await exists(adaptationRegistryPath(root)));
    const reloaded = await loadAdaptationRegistry(root);
    check(
      "W1 policy replica hash equals persisted candidate hash",
      reloaded.candidatesFor(routingPolicyIdentity(proj))[0]!.contentHash ===
        hashCandidateContent(routingPolicyContent(W1_POLICY))
    );

    const before = await saveWitness(adaptationRegistryPath(root));
    const resE = await runAutoAdaptLoop({
      stateRoot: root,
      projectRoot: proj,
      projectId: PROJECT_ID,
      primaryModelId: PRIMARY,
      events: failingEvents(proj)
    });
    const after = await saveWitness(adaptationRegistryPath(root));
    check("W1-E created=false", !resE.created);
    check("W1-E reason", resE.reason === "identical candidate already proposed");
    check("W1-E candidateId returned", resE.candidateId !== undefined);
    check("W1-E SAVES ANYWAY (ino changed: temp+fsync+rename)", after.ino !== before.ino);
    console.log(
      `VERDICT PWL W1-C: lock=1 load=1 save=1 | W1-E: lock=1 load=1 save=1 (X0-3 save-timing asymmetry vs W2-E save=0, witnessed by inode change on an identical no-change run)`
    );
  }

  /* ---- W1 leg B via the real loop: active content == optimizedPolicy content. ---- */
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-w1b-"));
    const proj = await mkdtemp(join(tmpdir(), "r28e-pwl-projb-"));
    const registry = await loadAdaptationRegistryOrNew(root);
    registry.registerBaseline({
      identity: routingPolicyIdentity(proj),
      content: routingPolicyContent(W1_POLICY),
      author: { kind: "detector", identity: "r28e-pwl" }
    });
    await saveAdaptationRegistry(root, registry);
    const before = await saveWitness(adaptationRegistryPath(root));
    const res = await runAutoAdaptLoop({
      stateRoot: root,
      projectRoot: proj,
      projectId: PROJECT_ID,
      primaryModelId: PRIMARY,
      events: failingEvents(proj)
    });
    const after = await saveWitness(adaptationRegistryPath(root));
    check("W1-B reason", res.reason === "observed policy matches the active baseline");
    check("W1-B created=false, no candidateId", !res.created && res.candidateId === undefined);
    check("W1-B SAVES ANYWAY (ino changed)", after.ino !== before.ino);
    console.log(
      `VERDICT PWL W1-B: lock=1 load=1 save=1 (saves an unchanged registry; W2-B saves nothing — both no-change legs, opposite write fates)`
    );
  }

  /* ---- Orchestrator gate legs (no registry lock taken). ---- */
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-gate1-"));
    const proj = await mkdtemp(join(tmpdir(), "r28e-pwl-projg1-"));
    process.env.SPARKLE_AUTO_ADAPT = "0";
    const res = await runAutoAdaptLoop({
      stateRoot: root,
      projectRoot: proj,
      projectId: PROJECT_ID,
      primaryModelId: PRIMARY,
      events: failingEvents(proj)
    });
    delete process.env.SPARKLE_AUTO_ADAPT;
    check("GATE-disabled reason", res.reason === "auto-adapt disabled; collected only");
    check("GATE-disabled still collects", res.collected === 5 && res.issues.length === 1);
    check("GATE-disabled no registry.json", !(await exists(adaptationRegistryPath(root))));
    check(
      "GATE-disabled bandit write NOT gated (collect side still writes)",
      await exists(join(adaptationRoot(root), "learning"))
    );
    console.log(
      `VERDICT PWL GATE-disabled: kill switch pins the leg BEFORE the failing branch; bandit write activation is kill-switch-INSENSITIVE (env is a lever pinned per arm — R25-I coordinate not dosed)`
    );
  }
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-gate2-"));
    const proj = await mkdtemp(join(tmpdir(), "r28e-pwl-projg2-"));
    const res = await runAutoAdaptLoop({
      stateRoot: root,
      projectRoot: proj,
      projectId: PROJECT_ID,
      primaryModelId: PRIMARY,
      events: passingEvents(proj)
    });
    check("GATE-nofailing reason", res.reason === "no actionable model-project issue");
    check("GATE-nofailing no registry.json", !(await exists(adaptationRegistryPath(root))));
    console.log(`VERDICT PWL GATE-nofailing: issues exist but none actionable -> no lock, no registry I/O`);
  }
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-gate3-"));
    const proj = await mkdtemp(join(tmpdir(), "r28e-pwl-projg3-"));
    const res = await runAutoAdaptLoop({
      stateRoot: root,
      projectRoot: proj,
      projectId: PROJECT_ID,
      primaryModelId: PRIMARY,
      events: []
    });
    check("GATE-empty reason", res.reason === "no feedback to learn from");
    check("GATE-empty zero write activation (no adaptation dir at all)", !(await exists(adaptationRoot(root))));
    console.log(`VERDICT PWL GATE-empty: zero signals -> even the bandit write gate stays closed`);
  }

  /* ---- Steady-state leg occupancy census (3 consecutive runs, one root). ---- */
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-ss2-"));
    const legs: string[] = [];
    let saves = 0;
    let last: { ino: number; mtimeMs: number } | undefined;
    for (let k = 0; k < 3; k += 1) {
      const res = await proposeRoutingFromOutcomes({
        stateRoot: root,
        projectRoot: "/tmp/proj-pwl",
        projectId: PROJECT_ID,
        primaryModelId: PRIMARY,
        outcomes: FAIL_OUTCOMES
      });
      legs.push(res.created ? "C" : res.reason.startsWith("identical") ? "E" : "?");
      const wit = await saveWitness(adaptationRegistryPath(root));
      if (last === undefined || wit.ino !== last.ino) saves += 1;
      last = wit;
    }
    check("W2 steady-state legs = C,E,E", legs.join(",") === "C,E,E");
    check("W2 steady-state saves = 1 (first run only)", saves === 1);
    console.log(`VERDICT PWL W2 steady-state occupancy: legs=[${legs.join(",")}] saves=1/3 (fixed failing profile -> load-only steady state)`);
  }
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-ss1-"));
    const proj = await mkdtemp(join(tmpdir(), "r28e-pwl-projss-"));
    const legs: string[] = [];
    let saves = 0;
    let last: { ino: number; mtimeMs: number } | undefined;
    for (let k = 0; k < 3; k += 1) {
      const res = await runAutoAdaptLoop({
        stateRoot: root,
        projectRoot: proj,
        projectId: PROJECT_ID,
        primaryModelId: PRIMARY,
        events: failingEvents(proj)
      });
      legs.push(res.created ? "C" : res.reason.startsWith("identical") ? "E" : "?");
      const wit = await saveWitness(adaptationRegistryPath(root));
      if (last === undefined || wit.ino !== last.ino) saves += 1;
      last = wit;
    }
    check("W1 steady-state legs = C,E,E", legs.join(",") === "C,E,E");
    check("W1 steady-state saves = 3 (every run re-persists)", saves === 3);
    console.log(`VERDICT PWL W1 steady-state occupancy: legs=[${legs.join(",")}] saves=3/3 (every no-change run rewrites+fsyncs an unchanged registry — the S2-E-1-closed margin, now leg-attributed)`);
  }
}

/* ================================================================
 * SEGMENT P: per-leg pricing (once-per-run costs; landing bar arithmetic).
 * ================================================================ */
if (SEG.includes("P")) {
  console.log("--- BENCH PWL per-leg prices (once-per-run proposal path) ---");

  // CPU prelude shared by all lock-taking legs (content+hash+identity).
  const prelude = bench(() => {
    void hashCandidateContent(routingPolicyContent(W2_POLICY));
    void routingPolicyIdentity("/tmp/proj-pwl");
  }, 100000);
  console.log(`BENCH PWL prelude (content+hash+identity): ${(prelude * 1e6).toFixed(0)}ns/call`);

  // W2 leg N (pre-lock return; pure CPU + promise machinery).
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-pn-"));
    const cost = await benchAsync(async () => {
      await proposeRoutingFromOutcomes({
        stateRoot: root,
        projectRoot: "/tmp/proj-pwl",
        projectId: PROJECT_ID,
        primaryModelId: PRIMARY,
        outcomes: PASS_OUTCOMES
      });
    }, 5000);
    console.log(`BENCH PWL W2-N: ${(cost * 1e6).toFixed(0)}ns/call (zero-I/O leg)`);
  }

  // W2 leg E steady state (lock+load, no save).
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-pe-"));
    await proposeRoutingFromOutcomes({
      stateRoot: root,
      projectRoot: "/tmp/proj-pwl",
      projectId: PROJECT_ID,
      primaryModelId: PRIMARY,
      outcomes: FAIL_OUTCOMES
    });
    const cost = await benchAsync(async () => {
      await proposeRoutingFromOutcomes({
        stateRoot: root,
        projectRoot: "/tmp/proj-pwl",
        projectId: PROJECT_ID,
        primaryModelId: PRIMARY,
        outcomes: FAIL_OUTCOMES
      });
    }, 300);
    console.log(`BENCH PWL W2-E steady: ${(cost * 1e3).toFixed(1)}us/run (lock+load, save=0)`);
  }

  // W2 leg B (lock+load, no save).
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-pb-"));
    const registry = await loadAdaptationRegistryOrNew(root);
    registry.registerBaseline({
      identity: routingPolicyIdentity("/tmp/proj-pwl"),
      content: routingPolicyContent(W2_POLICY),
      author: { kind: "detector", identity: "r28e-pwl" }
    });
    await saveAdaptationRegistry(root, registry);
    const cost = await benchAsync(async () => {
      await proposeRoutingFromOutcomes({
        stateRoot: root,
        projectRoot: "/tmp/proj-pwl",
        projectId: PROJECT_ID,
        primaryModelId: PRIMARY,
        outcomes: FAIL_OUTCOMES
      });
    }, 300);
    console.log(`BENCH PWL W2-B: ${(cost * 1e3).toFixed(1)}us/run (lock+load, save=0)`);
  }

  // W2 leg C (one-shot per fresh root; save-bearing leg).
  {
    const costs: number[] = [];
    for (let i = 0; i < 30; i += 1) {
      const root = await mkdtemp(join(tmpdir(), "r28e-pwl-pc-"));
      const t0 = performance.now();
      await proposeRoutingFromOutcomes({
        stateRoot: root,
        projectRoot: "/tmp/proj-pwl",
        projectId: PROJECT_ID,
        primaryModelId: PRIMARY,
        outcomes: FAIL_OUTCOMES
      });
      costs.push(performance.now() - t0);
    }
    costs.sort((a, b) => a - b);
    console.log(
      `BENCH PWL W2-C one-shot: min=${costs[0]!.toFixed(2)}ms med=${costs[15]!.toFixed(2)}ms max=${costs[29]!.toFixed(2)}ms (lock+load+save+fsync, 30 fresh roots)`
    );
  }

  // W1 replica leg E steady state (lock+load+save EVERY run).
  {
    const root = await mkdtemp(join(tmpdir(), "r28e-pwl-p1e-"));
    const proj = "/tmp/proj-pwl";
    await w1Replica({ stateRoot: root, projectRoot: proj, policy: W1_POLICY });
    const cost = await benchAsync(async () => {
      await w1Replica({ stateRoot: root, projectRoot: proj, policy: W1_POLICY });
    }, 300);
    console.log(`BENCH PWL W1-E steady (replica): ${(cost * 1e3).toFixed(1)}us/run (lock+load+SAVE — the no-change re-persist tax)`);
  }

  // W1 replica leg C (one-shot per fresh root).
  {
    const costs: number[] = [];
    for (let i = 0; i < 30; i += 1) {
      const root = await mkdtemp(join(tmpdir(), "r28e-pwl-p1c-"));
      const t0 = performance.now();
      await w1Replica({ stateRoot: root, projectRoot: "/tmp/proj-pwl", policy: W1_POLICY });
      costs.push(performance.now() - t0);
    }
    costs.sort((a, b) => a - b);
    console.log(
      `BENCH PWL W1-C one-shot (replica): min=${costs[0]!.toFixed(2)}ms med=${costs[15]!.toFixed(2)}ms max=${costs[29]!.toFixed(2)}ms (30 fresh roots)`
    );
  }
}

console.log(`VERDICT total sanity checks: ${checks}, failures: ${failures}`);
if (failures > 0) {
  console.error(`${failures} SANITY CHECK(S) FAILED`);
  process.exit(1);
}
console.log("ALL PWL SANITY CHECKS PASSED (deterministic, no RNG)");
```

MORE_OPTIMA=no
BRANCH=cursor/r28-e-learning-twenty-eighth-pass-83a1
