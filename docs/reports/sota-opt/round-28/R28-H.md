MODEL_SLUG=claude-fable-5-thinking-xhigh

# R28-H：`src/evaluation/` + `src/requirement/` + `src/review/` + `src/rubric/` 第二十八遍扫描报告——新轴边界实参槽物化面（boundary argument-slot materialization face，战役首创）定价收口——空枚举

- **基线**：`cursor/sota-persistent-opt-83a1` @ `c146dae`（派单时 exclusive tip——R28-F 空枚举收口摄取 + 本 R28-H 派发）；运行中 exclusive **两次** docs-only 快进：`3b3dc84`（R28-E 空枚举收口——新增 **proposal write-path termination-leg face（PWL）** 轴（E 切片两处提案写者的返回腿 × 持久写激活集）——+ R28-I 派发）→ `45aad9d`（R28-G 空枚举收口——新增 **decision-gate resolution face** 轴（G 切片 gate 角色 × 审批要求 × 后继计划几何 × 选择子集）——+ R28-J 派发）。`git diff c146dae..3b3dc84 --stat` 与 `git diff 3b3dc84..45aad9d --stat` 逐次复核均仅触及 `docs/reports/sota-opt/` 三文件（PROGRESS / round-28/PLAN / R28-E.md，第二次为 R28-G.md），`src/` 两次均零变化——按令逐次 rebase 本分支到新 tip、重验 21 文件冻结（两次均仍 0 行）、并把 PWL 与 decision-gate resolution face 加入非移植清单（§3：本切片无提案写者/持久写腿物理对象、无 flowchart 决策门/审批腿物理对象——coverage 门是 fail-closed 断言非决策门解析——两轴零交集）。docs-only 快进未按生产变更对待。全部测量在 `c146dae` 的工作树上完成，两次快进增量 `src/` 零变化 ⇒ 测量在字节相同的切片上完成，全部有效。
- **分支**：`cursor/r28-h-eval-twenty-eighth-pass-83a1`；仓库变更仅本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` = 本文件）。未开 PR。
- **模型**：claude-fable-5-thinking-xhigh。
- **环境**：Node v22.22.2（VM 默认 22.14.0 低于 engines ≥22.19.0，按 R28-F 同处理以 nvm PATH 前置切换）、tsx 4.23.12、TypeScript 5.9.3（target ES2024 / module NodeNext）、pnpm 10.17.1、`pnpm install --frozen-lockfile`。
- **前置阅读**：README、EXCLUSIONS 全表（545 行，至 S13-B-1）、PROGRESS、round-28/PLAN（H 段 + 全部 plan-ban 轴，快进前后三版）、R27-H 全文（execution-substrate face + 附录 C 锚点仪器逐行——本轮锚点仪器的复刻源）、R26-H / R25-H 关键段（realm 面 / 字符串表示普查）、R21-H 全文关键段（异步调度面——本轮候选轴筛选时排除「扩展点决议时序」的依据）、R17-H 关键段（fail-closed 拒绝路径普查——排除「throw 路径/栈捕获」候选轴的依据）、R18-H 关键段（休眠分支普查——本轮设计性排除 populated-options 值臂的依据）、R14-H / R19-H / R20-H 一行结论（引擎底 GC/JIT / 执行史 / 进程态压力——候选轴筛选排除依据）、R28-F 全文（boundary call-protocol face——本轮新轴的最近邻调用面标尺，证正交性 §2.2）、R28-A / R28-B / R28-C / R28-D / R28-E / R28-G 结论段（快进前后）。

## 结论

**空枚举。零候选。不铸任何 S28-H-\* ID。** 这是 R12-H 起第十七个连续 H 空枚举。

1. **新轴（§2，战役首创，已定价收口）**：**边界实参槽物化面（boundary argument-slot materialization face）**——切片公开边界上声明了可选/缺省尾参的五个函数，其**调用方实际物化多少个实参槽**（argc）以及物化时槽里装什么（显式 `undefined` vs 显式缺省字面量）作为受控变量。全部臂都是 TypeScript 合法的 DIRECT 调用（分发适配器按 R28-F 钉死）、操作数逐字节相同、语义逐字节相同。二十七遍 H 的每一个夹具把每个边界钉在恰一个 argc 坐标（门恒 3-arity：显式 `undefined` 或 populated options——R18-H/R27-H 夹具形态）；而**仓库自己的真实调用方横跨该面**：生产 3 个门位点全部 3-arity（逐位点三元式 `undefined`|object）、单元测试 3 处 2-arity 省略、切片内 pairwise L104 2-arity 显式布尔、切片内工厂 L187 1-arity 对象——argc 坐标在真实调用方间双峰分布，却从未被任何一轮作为受控变量。三轮 × 双视图（进程内轮转 + 门边界进程隔离 3 子/臂）：**5 边界 12 臂全平坦**（6/6 判定类内 ×3；奇偶签名 sha256 三轮逐字节同一）。全面唯一的结构化信号是 `habits = {}` 缺省初始化器的每调用空对象分配（X1E 比 X0/X1U 快 4.6–14.0 ns/call，三轮同号）——而生产唯一消费位点（`heuristic.ts` L187 `heuristicExtractor(input.habits ?? {})`）恒传对象、缺省路径的分配只是移到调用方 ⇒ 零池。门的真下应用（G2：argc 2 < 形参数 3——census 见下）进程内三轮同向 +16.2~+40.4 ns，但进程隔离视图符号翻转（−13.7/+7.8/+43.2，地板 1.044–1.221 覆盖）⇒ 无稳定下应用税。算术闭合：最坏跨臂门差 × 每 run 恰 1 次门调用 = **≤0.000043 ms/run**，距 10 ms 保守地板 5+ 个数量级。轴完备定价并收口，夹具 argc 坐标对该维度证明代表性（R7-I 型盲区在此维度补齐后为空格），无候选。
2. **Function.length 普查（新轴副产物，结构定性）**：切片可选边界面横跨**两个 ECMAScript 形参类**——TS `?` 可选参在运行时是**普通形参**（计入 `Function.length`；省略 = 引擎级真下应用 argc < 形参数）：`assertCoverageAllowsStart.length === 3`、`runBlindPairwisePair.length === 3`；`= default` 形参截断计数（省略 → 执行初始化器）：`heuristicExtractor.length === 0`、`blindPairwiseCompare.length === 1`、`classifyDiffScope.length === 2`。两类的省略臂在本面上同判平坦。
3. **锚点全数重测（§1，三轮）**：默认态链 **9006–9483 ns**（三轮全落 R27-H 带 9005–9493 内；成分 extract 8011–8493 + 满门 703–748 + precedence 247–256 与 R12-H 剖面归账同构）；配置 A 7764–8823 ns（两次落 R27-H 带 7097–8241 内、一次 8823 落 R25-H 带 7.55–9.26 µs 内）；配置 B **6393–8312 ns——高出全部历史 B 带（历史顶 R25-H 6.70 µs），诚实 VM 漂移注记见 §1**（同轮次序 default > A > B 三轮全成立，B 的两个门/precedence 成分 542–654 / 115–117 ns 均在带内，涨幅全部来自 extract 成分 5624–7628 ns；默认锚——R7-I 保守口径——三轮全带内 ⇒ 判 B 腿 VM 窗口漂移非结构迁移）；promotion 18.0–21.7 ns；四个零生产调用方命令类 ≤2.47 µs；冷首调 dist 中位 1688–1736 µs（R6-H…R27-H 1.6–1.8 ms 同带，once-per-process 否决类归属不变）。生产调用面与 21 文件冻结零漂移。
4. **勘误一条（§0）**：R27-H 勘误 (b) 的普查行本身需再勘误——其声称「21 文件自身零直接 Date/random 站点（rg `Date.now|new Date|Math.random|randomUUID|crypto|hrtime`：零命中）」与本轮 rg 实测矛盾：`src/rubric/types.ts` L35 `createdAt: new Date().toISOString()`（`createRubric`）是 21 文件内**恰一个**直接时钟站点。`createdAt` 字段族的掩蔽策略（R27-H canon 已掩蔽全部 `createdAt`）本就覆盖该站点 ⇒ 历轮全部测量与奇偶结论不受影响；仅普查句需缩窄为「零 `Date.now`/`Math.random`/`randomUUID`/`crypto`/`hrtime` 站点；`new Date` 恰一站点在 rubric/types.ts L35（其余 createdAt 均经切片外 `nowIso()` 铸造）」。

不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动；无阈值 / 测试 / 公开签名 / 数据面变更。无赢家故未向仓库提交任何脚本资产（两组驱动全文进附录 A/B 并附 sha256，临时文件已删——附录 C）。

## §0 范围、读序、冻结与不变量核查

- **文件普查**：8+7+4+2 = 21 文件在位；`wc -l` 655（evaluation）+712（requirement）+309（review）+74（rubric）= **1750**，与 R25-H…R27-H 逐组一致。与派单「evaluation 是 8 不是 9」吻合。
- **冻结**：`git diff fd437a9..HEAD -- src/evaluation src/requirement src/review src/rubric` 输出为空——在开工 tip `c146dae`、快进 rebase 后 `3b3dc84`、提交前共三次核验，均 0 行。二十八遍 H 未触 `src/`。
- **S5-H-1**：`src/requirement/precedence.ts` L24 `if (fast.length === 0 || slow.length === 0) return [];` 逐字在位。语义三元组在两组驱动内各 attested（fast-only → []、slow-only → []、fast+slow → 恰 1 conflict；锚点驱动 ×3 + 新轴驱动 ×3 全过；新轴驱动按 R27-H 纪律把三元组放在全部计时之后执行——见下协议注记）。
- **生产调用面复核**（rg import 搜索，本 tip 实测）：切片外导入位点与 R3-H…R27-H 恒定——`adaptation/{promotion,promotion-rules}` 仅 import `review/self-review`；`run/{coordinator,flowchart-run,supervisor}` + `track/{clarify,loop,plan,primary-split}` import requirement 模块。`src/evaluation/` 全部 8 文件、`review/{pairwise,reconcile,critic}.ts`、`src/rubric/` 两文件**零生产调用方**（`src/evaluation/evaluator.ts` 与 `src/review/critic.ts` 的 rg 命中为切片内自引用与类型引用，非新生产消费者——逐条打开核验）。
- **门禁**：`pnpm gate` 全绿——typecheck ✓、lint ✓、test ✓（**tests 1169 / suites 78：1168 pass / 1 skipped / 0 fail**）、build ✓（exit 0）。
- **协议注记（锚点仪器次序）**：本轮首次预跑把 S5-H-1 exotic-shape 三元组（mkC 合同与真合同 map 不同）放在锚点**之前**，默认锚读得 9722 ns（高出干净带 2.5–8.0%）——与 R27-H 附录 C「anchors measured FIRST, before ANY exotic object」纪律及其 E0 IC 残留观察（+505–532 ns）同向。该预跑作为协议变体**弃置不计入锚点带**；三次正式运行全部把三元组移到计时之后。
- **勘误核验**：`rg -n "new Date|Date\.now|Math\.random|randomUUID|hrtime" src/{evaluation,requirement,review,rubric}` 恰一命中 `src/rubric/types.ts:35`；`rg "crypto|structuredClone|JSON\.parse|JSON\.stringify"` 同范围零命中。结论第 4 条成立。

## §1 锚点重测（本 VM，历史仪器：tsx/src 基质、进程内、R27-H 附录 C 锚点段逐行复刻，三轮独立运行）

| 锚 | run1 | run2 | run3 | 历史参照带 |
|---|---|---|---|---|
| 默认态链（extract+gate+precedence，Q=0） | 9019 ns | 9483 ns | 9006 ns | R27-H 9005–9493（三轮全带内） |
| —— extract 成分 | 8060 | 8493 | 8011 | R21-H 8169–8601 / R26-H 8236–8774 邻域 |
| —— 满门成分 | 703 | 739 | 748 | R26-H 676–771 带内 |
| —— precedence 成分 | 256 | 251 | 247 | R26-H 246–253 带内/贴顶 |
| 配置 A（habits，Q=1 gated） | 7764 | 8823 | 7938 | R27-H 7097–8241（两次带内）；R25-H 7.55–9.26 µs（8823 带内） |
| 配置 B（vague，Q=3 gated） | 6393 | 8312 | 7421 | R27-H 3927–4733 / R25-H 4.11–6.70 µs——**全部高出历史顶，见下注** |
| promotion 类（生产） | 21.7 ns | 18.0 ns | 19.8 ns | R26-H 17.8–19.9（21.7 贴上沿外 9%，R27-H 已见 21.2/23.7 同型） |
| evaluate 类（零生产调用方） | 1285 ns | 1105 ns | 1209 ns | R25-H 1.146–1.227 µs 跨骑 |
| critique 类（run-start 链内） | 592 ns | 594 ns | 588 ns | R27-H 593–605 带内 |
| pairwise 类（零生产调用方） | 2465 ns | 1979 ns | 2056 ns | R25-H 2.059–2.361 跨骑 |
| reconcile 类（零生产调用方） | 65 ns | 63 ns | 64 ns | R27-H 66–73 邻接/贴底 |
| 冷首调 dist 默认（5 进程中位） | 1736 µs | 1688 µs | 1725 µs | R6-H…R27-H 1.6–1.8 ms 同带（once-per-process 拒收类） |

三轮 CHECKS 12/12 全过 ×3。**配置 B 诚实注记**：6393/8312/7421 ns 全部高出二十七轮 B 腿历史最宽包络（顶 R25-H 6.70 µs），且三轮内部散布 ±13%（B 腿历史即是散布最宽的腿：R25-H 4.11–6.70 = ±24%）。判 VM 窗口漂移非结构迁移的三条依据：(a) 同轮次序 default > A > B 三轮全成立（8312 < 8823、7421 < 7938、6393 < 7764）——腿间结构序无翻转；(b) B 的门（542–654 ns）与 precedence（115–117 ns）成分均在历带内，涨幅全部集中在 extract 成分（5624/7628/6759）——正则/短串路径的窗口敏感性，与 R25-H 单轮 4.11→6.70 的既见幅度同型；(c) 默认锚——R7-I 义务下的保守口径——三轮**全部落死 R27-H 带内**（带宽仅 5.4%），配置态矩阵无隐藏悬崖。四个零生产调用方命令类 ≤2.47 µs——即便被生产接线也不构成候选池。

## §2 新轴：边界实参槽物化面（boundary argument-slot materialization face，战役首创，已定价收口）

### §2.1 定义与真实调用方普查

受控变量：**调用方在切片公开边界物化多少个实参槽、物化的尾槽装什么**。切片公开面上恰有五个函数声明了可选/缺省尾参，构成两个 ECMAScript 形参类（结论第 2 条 Function.length 普查）：

| 边界 | 签名尾参 | 形参类 | 臂 | 生产/测试真实坐标 |
|---|---|---|---|---|
| `assertCoverageAllowsStart(contract, tasks, options?)` | TS `?` 普通形参（length=3） | 真下应用 | G2（省略）/ G3U（显式 undefined） | 生产 3 位点全 3-arity：supervisor.ts L500–509、flowchart-run.ts L759–765、coordinator.ts L226–235，第三槽逐位点三元式 `undefined`\|object；单元测试 coverage-gate.test.ts L63/L76/L86 = 2-arity、L87 = 3-arity object；历轮夹具恒 3-arity |
| `heuristicExtractor(habits = {})` | 缺省字面量（length=0） | 初始化器 | X0（0-arity）/ X1U（1-arity undefined）/ X1E（1-arity 共享 {}） | 切片内唯一消费 heuristic.ts L187 `heuristicExtractor(input.habits ?? {})` 恒 1-arity 对象（X1E 坐标；缺省路径在调用方新建 {}） |
| `blindPairwiseCompare(input, swapOrder = false)` | 缺省字面量（length=1） | 初始化器 | P1 / P2U / P2F（显式 false） | 切片内 pairwise.ts L104 恒 2-arity 显式布尔（P2F 坐标）；零生产调用方 |
| `runBlindPairwisePair(input, rng, options?)` | TS `?` 普通形参（length=3） | 真下应用 | R2 / R3U | 零生产调用方 |
| `classifyDiffScope(context, allChanged, rules = DEFAULT_RULES)` | 缺省标识符（length=2） | 初始化器 | D2 / D3U | 零生产调用方 |

全部臂 TS 合法、DIRECT 分发、操作数共享同一构造位点（own-data / packed / 主 realm / canon 串）、语义逐字节相同（含门的 fail-closed 拒收路：G2/G3U 在 blocked 合同上抛出的错误类 + 消息逐字节相等——奇偶检查项）。**设计性排除的臂**：4-arity 过应用（TS 非法调用方，契约外）；populated options 值臂（R18-H 休眠分支普查已定价的格——本面两臂同走 `options?.resolvedQuestionIds ?? []` 同一内部分支）；实参上任何膜/访问路径剂量（R27-F）；非 DIRECT 适配器（R28-F 八臂矩阵）。

生产通量：门 = 每 run 恰 1 次（三个 run 入口互斥，各门一次——R21-H 频次取证不变）；工厂 = 每 extract 链 1 次；其余零生产流量。

### §2.2 新颖性与正交性（逐条钉死）

- **新颖性**：rg 全库 `docs/reports/sota-opt/`——「arity / 参数槽 / 缺省参数 / argument count」在 H 系零命中作为受控变量；R22-D/R21-D 的「全默认参数」是 D 切片生产形态**取证**非剂量；R21-F 时钟通道定价的是**流经**缺省形参的值（F 切片，值剂量），非槽物化本身。二十七遍 H 夹具恒 3-arity 门（R18-H L642 populated / R27-H L637 显式 undefined）。本轮兄弟 A–F 亦无一碰 argc 维（R28-F 明言全臂「按位置传递」全 arity）。
- **vs R28-F boundary call-protocol face（最近邻，按令禁重铸适配器矩阵）**：R28-F 剂量**控制流如何抵达被调函数**（Call/Apply builtin、bound trampoline、spread 去糖），其八臂全部满 arity；本轮分发适配器全臂钉死 DIRECT，剂量**实参向量几何**（argc × 尾槽载荷）。不是适配器矩阵的重铸——受控变量、物理对象（实参栈槽物化 + 形参缺省初始化器字节码 + argc-mismatch 处理）均不同。
- **vs R28-A input key-set geometry face（按令禁移植）**：那是对象**键集**几何（盈余宽度 × 插入序 × backing）；本轮全部操作数对象键集/形状/backing 逐臂逐字节同一，剂量的是**位置形参槽**——键集面的位置参数镜像，但物理对象（实参适配 vs 属性存储）无交集。
- **vs R18-H 休眠分支普查 / R17-H 拒绝路径普查**：populated-options 值格与 throw 端点定价归 R18/R17-H——本面两臂同分支、同端点，fail-closed 拒收路仅作奇偶见证（消息逐字节相等断言）非剂量。
- **vs R22-H 调用上下文几何**：栈深/try/异步几何全臂钉死（同一平坦同步循环）。**vs R21-H 异步调度面**：无微任务/宏任务剂量（门与工厂全同步；pair 腿的 promise 机器逐臂相同）。**vs R23-H / R24-H / R25-H / R26-H / R27-H**：默认注册表、共享操作数池、canon 串、主 realm、单一 tsx/src 基质。**vs R26-F / R27-F**：packed+extensible、own-data 无膜。**vs SHAPEK**：每臂独立调用位点（显式双克隆函数）、单形状宇宙。**vs CALLB / R27-B**：排程与批基数恒定（恒 1 调用/迭代）。**vs R27-A**：无数值类剂量（P2F 的 false 是布尔缺省字面量，非数值坐标）。**vs R28-E PWL（快进新增）**：本切片无提案写者/持久写腿，零写路径剂量。**vs R28-G decision-gate resolution face（第二次快进新增）**：无 gate 角色/审批要求/后继计划几何/选择子集剂量——本轮剂量的是 coverage 断言边界的实参向量几何，非任何决策门的解析腿。

### §2.3 方法

- **驱动**（附录 B，443 行，单文件双模式）：Function.length 普查（5 断言）→ 共享钉死操作数构造（默认合同 Q=0 C=2 + blocked 合同 Q=3 + tasks T=5 + pairInput + diff 夹具 + 共享 {}）→ **奇偶**（五边界逐臂输出 canon 化比对：门通过路双臂 + 门 fail-closed 双臂错误字节、工厂三臂 extract 全载荷、P 三臂、R 双臂、D 双臂；canon 掩蔽 evt\_ nonce 与 createdAt——R27-H 勘误 (b) + 本轮勘误的同一掩蔽族）→ **进程内轮转热层**：12 臂 × 每臂两个文本独立的调用位点克隆（各自字节码位置/反馈槽；克隆分歧 = 同坐标 A/A 噪声地板）× 5 pass 交错 × GC 屏障，每臂 2000×2 预热 → 逐边界判定：跨臂比 ≤ max(1.10, 1.5×最坏克隆地板) 或 |Δ| < 1 µs → **进程隔离确证**（门边界——唯一有生产流量的边界）：G2/G3U 各 3 个全新子进程（T,D 交错 spawn，`node --expose-gc --import tsx` 子模式仅测本臂），臂内散布为地板 → 算术闭合 → S5-H-1 三元组（计时后）。
- **三次独立运行**（run1/2/3；进程隔离子进程共 18 个）。

### §2.4 结果（三轮全过：CHECKS 27/27 ×3；ADJUDICATIONS 6/6 类内 ×3）

**奇偶**：五边界联合签名 sha256 = `52450a5e69bf58f5196009cb02d9bf03eca47533948c58d5406e37f6ea41d189`，三轮逐字节同一。argc 不泄漏进任何语义输出字节（含门 fail-closed 错误消息逐字节、S5-H-1 三元组）。

**热层**（ns/call；每格 run1/run2/run3；judgment = 跨臂比 vs 最坏克隆 A/A 地板）：

| 边界 | 臂读数 | 跨臂比 | 最坏地板 | max\|Δ\| | 判定 |
|---|---|---|---|---|---|
| gate（进程内） | G2 528.3/509.0/530.8 vs G3U 488.0/492.8/502.7 | 1.083/1.033/1.056 | 1.122/1.274/1.046 | 40.4 ns | 类内 ×3（G2>G3U 三轮同向，见下注） |
| gate（进程隔离） | G2 534.8/536.9/572.5 vs G3U 548.5/529.1/529.3 | 1.026/1.015/1.082 | 1.044/1.130/1.221 | 43.2 ns | 类内 ×3（**符号翻转**：−13.7/+7.8/+43.2） |
| extractor-factory | X0 20.3/20.6/33.4；X1U 23.9/20.7/25.0；X1E 16.1/16.1/19.5 | 1.484/1.284/1.719 | 1.173/1.046/1.240 | 14.0 ns | 类内 ×3（\|Δ\|<1µs 判据；X1E 最快三轮同向） |
| pairwise-compare | P1/P2U/P2F 854–887 窄簇 | 1.011–1.021 | ≤1.022 | 18.2 ns | 类内 ×3 |
| pair-runner | R2 1904–2028 vs R3U 1962–2094 | 1.013–1.058 | 1.063–1.107 | 114.3 ns | 类内 ×3（R3U 略慢三轮同向，地板覆盖；零生产调用方） |
| diff-scope | D2/D3U 318–344 | 1.000–1.029 | ≤1.025 | 9.8 ns | 类内 ×3（符号翻转） |

诚实方向注记两条：(a) 门的真下应用臂 G2 在进程内三轮同向慢 16.2–40.4 ns，但进程隔离视图（每臂全新 IC 宇宙）符号翻转且地板全覆盖 ⇒ 该「下应用税」不随进程复现，判轮转视图窗口伪影而非 argc 机制税；(b) 全面唯一跨视图稳定的结构化信号是工厂缺省初始化器的每调用空对象分配（X1E 比 X0/X1U 快 4.6–14.0 ns/call，三轮同号）——`habits = {}` 初始化器每调用铸一个新 `{}`，传入共享对象则跳过。

**算术闭合**：最坏跨臂门差 43.2 ns × 每 run 恰 1 次门调用 = **0.000043 ms/run**；工厂信号 14.0 ns × 每 run 1 次 = 0.000014 ms/run。距 10 ms 保守地板 5–6 个数量级。

### §2.5 判定

轴已完备定价并收口：**面平坦本身即结论**——argc 维度不藏配置态税，二十七轮 3-arity 夹具坐标对该维度是代表性的，仓库真实调用方的双峰 argc 分布（生产 3-arity / 测试 2-arity）不构成任何测量盲区（R7-I 型盲区补齐后为空格）。且即便有税也无承接位点：(1) 实参槽物化全程执行在**调用方帧与引擎调用机械里**——物化期间切片零行代码运行，切片对自己被以几个槽调用零选择权（R28-F 同款调用方关税归属）；(2) 唯一稳定信号（初始化器 {} 分配 5–14 ns）的生产消费位点已在 X1E 坐标（恒传对象），缺省路径的分配只是移到调用方——「改边界签名省 {}」= 公开签名变更（X0-4 族）换 ~10 ns once-per-run，自败；(3) 闭合 ≤0.000043 ms/run，µs/ns 拒收类。**零候选，不铸 ID。** 按令不把「boundary argument-slot materialization face」写入 EXCLUSIONS 表——父级计划禁令所有权。

## §3 枚举收口（不补铸 / 不移植逐条对照）

- **具名禁令逐条遵守**：FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL（快进新增）——本轮新轴不触及任何一条的受控变量。未重开任何 X\* / S1-\* … S27-\* 条目；S5-H-1 保留并三重 attested；S8-H-1 PairwiseInput 未收窄；S8-H-3 全决议快路径未做任何形态。
- **H 系历轴不补铸不重测**：R27-H execution-substrate face（本轮单一 tsx/src 基质；锚点冷层 dist 探针仅为载具注记，未做基质对照）；R26-H operand-realm face（单 realm）；R25-H string-representation census（canon 串）；R24-H operand-residency census（共享池、身份钉死）；R23-H membership-mix（默认注册表）；R22-H 调用上下文（固定调用几何）；R21-H 异步调度面（无调度剂量）；R20-H 进程态压力 / R19-H 执行史 / R18-H 休眠分支（populated-options 值臂设计性排除即其格）/ R17-H 拒绝路径（fail-closed 仅作奇偶见证）/ R16-H 载荷格 + PATH_RE（重开条件未触发：`src/` 零提交 ⇒ 无程序化/对抗 objective 来源、无 ≥3.2K 无斜杠载荷）/ R15-H A/A 地板（本轮克隆地板法是其调用位点级应用，非重铸）/ R14-H 引擎底轴 / R13-H 规模标定 / R12-H 剖面。
- **同轮兄弟轴不移植**：R28-A input key-set geometry face（正交性 §2.2——全臂键集钉死）；R28-B catalog-face row-arrangement（无目录行剂量）；R28-C C 区执行基质格（face 名归 R27-H，未触）；R28-D contents-store population face（本切片无 contents-store 物理对象，零别名重数/尺寸分区/引用态剂量）；R28-E **PWL** proposal write-path termination-leg face（快进摄取；本切片无提案写者/持久写腿，零交集）；R28-F boundary call-protocol face（正交性 §2.2——全臂 DIRECT，未重铸适配器矩阵）；R28-G decision-gate resolution face（第二次快进摄取；本切片无 flowchart 决策门/审批腿物理对象——`assertCoverageAllowsStart` 是 fail-closed 覆盖断言，非决策门解析，且其 argc 面已由本轮新轴定价——零交集）；R27-J SIDEC（无墓碑/侧车物理对象）。
- 本轮新轴昵称不入 EXCLUSIONS 表（父级所有权）；候选轴筛选期间被历轮档案**排除**而未开工的方向（throw 路径/栈捕获 → R17-H 已定价含 `Error.stackTraceLimit` 想法；扩展点决议时序 → R21-H Y/QM 腿已裁决；JIT 层级/GC → R14-H；输入类跨调用混合 → R19-H；输出保留/堆压 → R20-H）均为读档裁决，无新测量、无 ID、无表行。

## §4 门禁与仓库状态

- `pnpm gate` 全绿（§0）：typecheck ✓ lint ✓ test ✓（1169 tests / 78 suites：1168 pass / 1 skipped / 0 fail）build ✓。
- 仓库变更仅本报告一个文件；`scripts/` 零新增；无赢家故无 production 变更、无测试变更、无阈值变更。S5-H-1 在位且六次运行 attested。
- 驱动脚本仅存 `/tmp` 与本报告附录（A/B），已按附录 C 清单删除。

## 附录 A：锚点驱动 `/tmp/r28h-anchor.mts`（241 行，sha256 `31bb8b47f733faeb1f81fd8bcc92ce71022e64fcf723bb911f860b49998a783a`）

运行：`pnpm build`（或 `pnpm gate`）后 `node --expose-gc --import tsx /tmp/r28h-anchor.mts`（cwd = /workspace）。

```ts
/**
 * R28-H anchor re-measurement (twenty-eighth pass), historical instrument:
 * tsx/src substrate, in-process, mirrors the R27-H appendix-C anchor section
 * (measureChain: extract benchAsync 3000 reps + gate bench 50000 reps +
 * precedence bench 100000 reps, summed; command classes at R9-H..R27-H rep
 * counts; S5-H-1 semantic attestation triple), plus a dist cold-first-call
 * probe (5 fresh child processes, median — once-per-process veto class,
 * re-anchored for completeness only).
 *
 * Run: node --expose-gc --import tsx /tmp/r28h-anchor.mts   (from /workspace)
 */
import { performance } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import { extractHeuristicContract, type HeuristicHabits } from "/workspace/src/requirement/heuristic.ts";
import { assertCoverageAllowsStart, type CoverageTaskRef } from "/workspace/src/requirement/coverage.ts";
import { applyPrecedence, detectConflicts } from "/workspace/src/requirement/precedence.ts";
import { critiqueContract } from "/workspace/src/requirement/critic.ts";
import { assertCanPromoteFromReview } from "/workspace/src/review/self-review.ts";
import { runBlindPairwisePair, type PairwiseInput } from "/workspace/src/review/pairwise.ts";
import { reconcileReviews } from "/workspace/src/review/reconcile.ts";
import { createEvaluationRecord } from "/workspace/src/evaluation/evaluator.ts";
import { createRubric } from "/workspace/src/rubric/types.ts";
import type { RequirementContract } from "/workspace/src/domain/contract.ts";
import type { EpisodeId } from "/workspace/src/domain/ids.ts";

const OBJ = "fix the login retry bug in src/auth/session.ts and keep tests green";
const forceGc = (globalThis as { gc?: () => void }).gc;
function gcBarrier(): void {
  if (forceGc !== undefined) {
    forceGc();
    forceGc();
  }
}
let checks = 0;
let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function bench(fn: () => void, reps: number): number {
  fn();
  gcBarrier();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  gcBarrier();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}
function median(xs: number[]): number {
  const s = [...xs].sort((a, b) => a - b);
  return s[Math.floor(s.length / 2)] ?? Number.NaN;
}
function tasksFor(contract: RequirementContract, count = 5): CoverageTaskRef[] {
  const tasks: { id: string; acceptanceCriteria: { id: string }[] }[] = [];
  for (let i = 0; i < count; i += 1) {
    const acs: { id: string }[] = [];
    for (const criterion of contract.acceptanceCriteria) acs.push({ id: criterion.id });
    tasks.push({ id: `tsk_0000000${i}`, acceptanceCriteria: acs });
  }
  return tasks as unknown as CoverageTaskRef[];
}
function buildArgs(objective: string, habits: HeuristicHabits | undefined): {
  objective: string;
  habits?: HeuristicHabits;
} {
  return habits === undefined ? { objective } : { objective, habits };
}

/* ---- S5-H-1 attestation triple (runs AFTER the anchor block: the anchor
 * tier must be measured first, before any exotic contract shape exists in
 * this process — R27-H appendix-C discipline) ---- */
function attestS5H1(): void {
  const mkC = (checks_: string[]): RequirementContract =>
    ({
      id: "req_00000001",
      rawText: "t",
      goals: [],
      constraints: [],
      acceptanceCriteria: checks_.map((oc, i) => ({
        id: `ac_0000000${i}`,
        description: "d",
        observableCheck: oc,
        sourceRefs: []
      })),
      assumptions: [],
      questions: [],
      provenance: []
    }) as unknown as RequirementContract;
  const fastOnly = detectConflicts(mkC(["response is fast", "check completes < 10ms"]));
  const slowOnly = detectConflicts(mkC(["batch job is slow", "runs > 1000ms"]));
  const both = detectConflicts(mkC(["response is fast", "batch job is slow"]));
  check("S5-H-1 fast-only -> []", fastOnly.length === 0);
  check("S5-H-1 slow-only -> []", slowOnly.length === 0);
  check("S5-H-1 fast+slow -> exactly 1", both.length === 1);
}

/* ---- ANCHOR: hot tier, three states (R27-H appendix-C instrument) ---- */
async function measureChain(
  label: string,
  objective: string,
  habits: HeuristicHabits | undefined
): Promise<number> {
  const args = buildArgs(objective, habits);
  const chain = await benchAsync(async () => {
    await extractHeuristicContract(args);
  }, 3000);
  const prod = await extractHeuristicContract(args);
  const real = prod.contract;
  const tasks = tasksFor(real);
  const options =
    real.questions.length > 0
      ? { resolvedQuestionIds: real.questions.map((question) => question.id) }
      : undefined;
  const gate = bench(() => assertCoverageAllowsStart(real, tasks, options), 50000);
  const precedence = bench(() => void applyPrecedence(real, "user-first"), 100000);
  const total = chain + gate + precedence;
  log(
    `CEILING ${label}: extractHeuristicContract=${(chain * 1e6).toFixed(0)}ns + run-start gate(Q=${real.questions.length}${options !== undefined ? ", all resolved" : ", no options"})=${(gate * 1e6).toFixed(0)}ns + applyPrecedence=${(precedence * 1e6).toFixed(0)}ns = ${(total * 1e6).toFixed(0)}ns once-per-run production total`
  );
  return total;
}
await measureChain("default (9-pass anchor shape, Q=0)", OBJ, undefined);
await measureChain("configured A (habits set, Q=1 gated)", OBJ, {
  requireTests: true,
  preferReview: false,
  askBeforeWrite: true
});
await measureChain("configured B (vague, Q=3 gated)", "fix bug please", {
  askBeforeWrite: true
});

/* ---- Command-class anchors beyond run-start (R9-H..R27-H mirror) ---- */
{
  const promo = bench(
    () => assertCanPromoteFromReview({ reviewerKind: "peer", reviewerId: "rev-1", actorId: "act-1" }),
    200000
  );
  log(`CEILING promotion class: assertCanPromoteFromReview=${(promo * 1e6).toFixed(1)}ns once per promotion (production)`);

  const rubric = createRubric("rub-1", "task", [
    { id: "crit-1", description: "d1", weight: 0.4, observableCheck: "check-1" },
    { id: "crit-2", description: "d2", weight: 0.3, observableCheck: "check-2" },
    { id: "crit-3", description: "d3", weight: 0.3, observableCheck: "check-3" }
  ]);
  const evalInput = {
    episodeId: "ep_00000001" as EpisodeId,
    evaluator: { kind: "deterministic" as const, version: "v1", rubricVersion: "1" },
    rubric,
    evidence: { "crit-1": "evidence-a", "crit-2": "evidence-b" }
  };
  const evaluate = bench(() => void createEvaluationRecord(evalInput), 50000);
  log(`CEILING evaluate class (TEST-ONLY face): createEvaluationRecord(3 criteria)=${(evaluate * 1e6).toFixed(0)}ns/call`);

  const prod = await extractHeuristicContract({ objective: OBJ });
  const critique = bench(() => void critiqueContract(prod.contract), 50000);
  log(`CEILING critique class (inside run-start chain): critiqueContract(real contract)=${(critique * 1e6).toFixed(0)}ns/call`);

  const pairInput: PairwiseInput = {
    episodeId: "ep_00000001" as EpisodeId,
    aId: "cand-a",
    bId: "cand-b",
    aScore: 0.8,
    bScore: 0.7,
    aComment: "solid",
    bComment: "weaker"
  };
  const pairwise = bench(() => void runBlindPairwisePair(pairInput, () => 0.7), 20000);
  const pair = runBlindPairwisePair(pairInput, () => 0.7);
  const two = [pair.first, pair.swapped];
  const reconcile = bench(() => void reconcileReviews(two), 100000);
  log(`CEILING pairwise class (TEST-ONLY face): runBlindPairwisePair=${(pairwise * 1e6).toFixed(0)}ns/call`);
  log(`CEILING reconcile class (TEST-ONLY face): reconcileReviews(n=2)=${(reconcile * 1e6).toFixed(0)}ns/call`);
  check("pairwise identity-stable scores agree across swap", pair.reconciliation.consensus === "a");
  const biased = runBlindPairwisePair(pairInput, () => 0.7, { bindScoresToSlots: true });
  check("pairwise slot-bound scores reconcile to uncertain", biased.reconciliation.consensus === "uncertain");
  const record = createEvaluationRecord(evalInput);
  check("evaluate deterministic FAIL on missing evidence", record.overall === "FAIL");
  check(
    "evaluate per-criterion outcomes",
    record.scores.map((s) => s.outcome).join(",") === "PASS,PASS,FAIL"
  );
}

attestS5H1();

/* ---- COLD first-call (dist, 5 fresh children, median; veto class) ---- */
{
  const script = `
import { performance } from "node:perf_hooks";
const t0 = performance.now();
const { extractHeuristicContract } = await import("/workspace/dist/requirement/heuristic.js");
const { assertCoverageAllowsStart } = await import("/workspace/dist/requirement/coverage.js");
const { applyPrecedence } = await import("/workspace/dist/requirement/precedence.js");
const importMs = performance.now() - t0;
const OBJ = ${JSON.stringify(OBJ)};
const t1 = performance.now();
const prod = await extractHeuristicContract({ objective: OBJ });
const contract = prod.contract;
const tasks = [];
for (let i = 0; i < 5; i += 1) {
  tasks.push({ id: "tsk_0000000" + i, acceptanceCriteria: contract.acceptanceCriteria.map((c) => ({ id: c.id })) });
}
assertCoverageAllowsStart(contract, tasks, undefined);
applyPrecedence(contract, "user-first");
const coldUs = (performance.now() - t1) * 1e3;
process.stdout.write("COLDCHILD " + JSON.stringify({ importMs, coldUs }) + "\\n");
`;
  const colds: number[] = [];
  const imports: number[] = [];
  for (let i = 0; i < 5; i += 1) {
    const res = spawnSync(process.execPath, ["--input-type=module", "-e", script], {
      cwd: "/workspace",
      encoding: "utf8",
      timeout: 60000
    });
    check(`cold child ${i} exit 0`, res.status === 0, String(res.stderr).slice(0, 300));
    const line = (res.stdout ?? "").split("\n").find((l) => l.startsWith("COLDCHILD "));
    if (line !== undefined) {
      const parsed = JSON.parse(line.slice(10)) as { importMs: number; coldUs: number };
      colds.push(parsed.coldUs);
      imports.push(parsed.importMs);
    }
  }
  log(
    `COLD dist first-call (5 fresh children): median=${median(colds).toFixed(0)}us [${Math.min(...colds).toFixed(0)},${Math.max(...colds).toFixed(0)}]; 3-module import median=${median(imports).toFixed(1)}ms — once-per-process veto class`
  );
}

log(`CHECKS ${checks - failures}/${checks} passed`);
if (failures > 0) process.exit(1);
```

## 附录 B：新轴驱动 `/tmp/r28h-arity.mts`（443 行，sha256 `22560ae0e872c317bf7b302daa8afebbfb1832b42e5051b63f96aa69d4cb2268`）

运行：`node --expose-gc --import tsx /tmp/r28h-arity.mts`（cwd = /workspace；进程隔离子进程由驱动自行 spawn，同文件 `R28H_CHILD=G2|G3U` 子模式）。

```ts
/**
 * R28-H NEW AXIS driver: BOUNDARY ARGUMENT-SLOT MATERIALIZATION FACE
 * (twenty-eighth pass — the pass's new completeness axis).
 *
 * Axis: the ACTUAL-ARGUMENT VECTOR GEOMETRY at the slice's public boundaries
 * that declare optional / defaulted trailing parameters: how many argument
 * slots the caller materializes (argc) and what the trailing slot carries
 * when materialized (explicit `undefined` vs the explicit default literal).
 * Every arm is a TypeScript-legal DIRECT call (dispatch adapter pinned per
 * R28-F) on byte-identical operands with byte-identical semantics.
 *
 * 27 prior H passes pinned every boundary at exactly ONE argc coordinate
 * (fixtures: gate always 3-arity with explicit undefined or populated
 * options — R18-H/R27-H; production: all 3 gate sites 3-arity via a
 * per-site ternary; the repo's own unit tests: 2-arity omitted). The argc
 * coordinate is bimodal across the repo's real callers and has never been a
 * controlled variable.
 *
 * Orthogonality pins (every named/unnamed plan-ban axis):
 * - R28-F boundary call-protocol face: dispatch adapter pinned DIRECT in
 *   every arm (the dosed variable there was HOW control reaches the callee;
 *   here it is HOW MANY argument slots are materialized — R28-F pinned all
 *   arms at full positional arity).
 * - R28-A input key-set geometry face: all operand OBJECTS byte- and
 *   shape-identical across arms (no surplus keys, no insertion-order or
 *   backing dose); the dosed slots are positional parameters, not keys.
 * - R22-H call-context geometry: stack depth / try / async geometry pinned
 *   (same flat sync loop for every arm).
 * - R18-H dormant-branch census / R17-H reject paths: the populated-options
 *   VALUE arm is R18-H's cell — excluded by design; both arms of each pair
 *   here take the SAME internal branch.
 * - R21-F clock-channel precedent priced the VALUE flowing through a
 *   default parameter (F slice); here the dosed variable is slot
 *   materialization itself, values pinned.
 * - R26-F storage-state / R27-F access-path / R24-H residency / R25-H
 *   string-representation / R26-H realm / R27-H substrate: operands shared,
 *   own-data, packed, main-realm, canon strings, single construction site,
 *   tsx/src substrate for all arms.
 *
 * Run: node --expose-gc --import tsx /tmp/r28h-arity.mts   (from /workspace)
 */
import { performance } from "node:perf_hooks";
import { spawnSync } from "node:child_process";
import { createHash } from "node:crypto";
import { extractHeuristicContract, heuristicExtractor } from "/workspace/src/requirement/heuristic.ts";
import { assertCoverageAllowsStart, type CoverageTaskRef } from "/workspace/src/requirement/coverage.ts";
import { detectConflicts } from "/workspace/src/requirement/precedence.ts";
import {
  blindPairwiseCompare,
  runBlindPairwisePair,
  type PairwiseInput
} from "/workspace/src/review/pairwise.ts";
import { classifyDiffScope } from "/workspace/src/evaluation/ownership.ts";
import type { AdapterContext } from "/workspace/src/evaluation/adapters.ts";
import type { RequirementContract } from "/workspace/src/domain/contract.ts";
import type { EpisodeId } from "/workspace/src/domain/ids.ts";

const OBJ = "fix the login retry bug in src/auth/session.ts and keep tests green";
const forceGc = (globalThis as { gc?: () => void }).gc;
function gcBarrier(): void {
  if (forceGc !== undefined) {
    forceGc();
    forceGc();
  }
}
let checks = 0;
let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    process.stderr.write(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}\n`);
  }
}
let adjudications = 0;
let breaches = 0;
function adjudicate(name: string, ok: boolean, detail: string): void {
  adjudications += 1;
  if (!ok) breaches += 1;
  process.stdout.write(`ADJUDICATE ${ok ? "in-class" : "BREACH"} ${name}: ${detail}\n`);
}
function log(line: string): void {
  process.stdout.write(`${line}\n`);
}
function median(xs: number[]): number {
  const s = [...xs].sort((a, b) => a - b);
  return s[Math.floor(s.length / 2)] ?? Number.NaN;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return ((performance.now() - t0) / reps) * 1e6; /* ns */
}
/* Deterministic canonical stringify masking the two volatile identity
 * families (evt_ nonce ids, createdAt clocks — R27-H errata (b) policy;
 * NOTE R28-H erratum: one createdAt is minted by a DIRECT in-slice clock
 * site, rubric/types.ts L35 — same mask covers it). */
function canon(v: unknown): string {
  if (v === null || typeof v !== "object") return JSON.stringify(v) ?? "undefined";
  if (Array.isArray(v)) return `[${v.map(canon).join(",")}]`;
  const rec = v as Record<string, unknown>;
  const keys = Object.keys(rec).sort();
  return `{${keys
    .map((k) => {
      const val = rec[k];
      if (k === "createdAt" && typeof val === "string") return `${JSON.stringify(k)}:"MASKED-CLOCK"`;
      if (k === "id" && typeof val === "string" && val.startsWith("evt_")) return `${JSON.stringify(k)}:"MASKED-NONCE"`;
      return `${JSON.stringify(k)}:${canon(val)}`;
    })
    .join(",")}}`;
}
function sha(s: string): string {
  return createHash("sha256").update(s).digest("hex");
}

/* ================= FORMAL-PARAMETER CENSUS (Function.length) ============
 * TS `?`-optionals are PLAIN formals at runtime (counted by length; omitting
 * them is true engine-level under-application argc < formal count), while
 * `= default` formals stop the count (omitting them runs the initializer).
 * The slice's optional surface splits across BOTH classes. */
check("Function.length gate = 3 (options? is a plain formal)", assertCoverageAllowsStart.length === 3);
check("Function.length heuristicExtractor = 0 (habits={} default stops count)", heuristicExtractor.length === 0);
check("Function.length blindPairwiseCompare = 1 (swapOrder=false default stops count)", blindPairwiseCompare.length === 1);
check("Function.length runBlindPairwisePair = 3 (options? is a plain formal)", runBlindPairwisePair.length === 3);
check("Function.length classifyDiffScope = 2 (rules=DEFAULT_RULES default stops count)", classifyDiffScope.length === 2);

/* ================= SHARED PINNED OPERANDS =============================== */
const prod = await extractHeuristicContract({ objective: OBJ });
const contract = prod.contract;
check("canon default shape Q=0 C=2", contract.questions.length === 0 && contract.acceptanceCriteria.length === 2);
const tasks: CoverageTaskRef[] = (() => {
  const out: { id: string; acceptanceCriteria: { id: string }[] }[] = [];
  for (let i = 0; i < 5; i += 1) {
    out.push({
      id: `tsk_0000000${i}`,
      acceptanceCriteria: contract.acceptanceCriteria.map((c) => ({ id: c.id }))
    });
  }
  return out as unknown as CoverageTaskRef[];
})();
const blocked = (await extractHeuristicContract({ objective: "fix bug please", habits: { askBeforeWrite: true } }))
  .contract; /* Q=3 unresolved -> gate throws (fail-closed) */
const pairInput: PairwiseInput = {
  episodeId: "ep_00000001" as EpisodeId,
  aId: "cand-a",
  bId: "cand-b",
  aScore: 0.8,
  bScore: 0.7,
  aComment: "solid",
  bComment: "weaker"
};
const rng = (): number => 0.7;
const diffContext: AdapterContext = {
  episodeId: "ep_00000001" as EpisodeId,
  workingDirectory: "/workspace",
  revision: "rev-1",
  changeSet: ["src/a.ts", "src/b.ts"]
};
const changed = ["src/a.ts", "src/b.ts", "dist/out.js", "node_modules/x/y.js", "docs/readme.md"];
const SHARED_EMPTY_HABITS = {}; /* single construction site, shared identity */

/* ---- child mode: measure ONE gate arm in a fresh process, then exit ---- */
if (process.env.R28H_CHILD !== undefined) {
  const armName = process.env.R28H_CHILD;
  const site: () => void =
    armName === "G2"
      ? () => {
          assertCoverageAllowsStart(contract, tasks);
        }
      : () => {
          assertCoverageAllowsStart(contract, tasks, undefined);
        };
  for (let i = 0; i < 4000; i += 1) site();
  const passesOut: number[] = [];
  for (let p = 0; p < 5; p += 1) {
    gcBarrier();
    passesOut.push(bench(site, 20000));
  }
  process.stdout.write(`CHILDGATE ${median(passesOut).toFixed(2)}\n`);
  process.exit(failures > 0 ? 1 : 0);
}

/* ================= PARITY (semantics identical across arms) ============= */
{
  /* gate pass path: both arms return undefined without throwing */
  let g2ok = true;
  let g3ok = true;
  try {
    assertCoverageAllowsStart(contract, tasks);
  } catch {
    g2ok = false;
  }
  try {
    assertCoverageAllowsStart(contract, tasks, undefined);
  } catch {
    g3ok = false;
  }
  check("gate pass-path parity G2/G3U", g2ok && g3ok);
  /* gate fail-closed path: identical error class + message across arms */
  let m2 = "";
  let m3 = "";
  try {
    assertCoverageAllowsStart(blocked, tasks);
  } catch (e) {
    m2 = `${(e as Error).name}:${(e as Error).message}`;
  }
  try {
    assertCoverageAllowsStart(blocked, tasks, undefined);
  } catch (e) {
    m3 = `${(e as Error).name}:${(e as Error).message}`;
  }
  check("gate fail-closed parity G2/G3U (byte-equal error)", m2 !== "" && m2 === m3, `${m2} vs ${m3}`);

  /* heuristicExtractor arms: X0 / X1U (initializer) / X1E (caller {}) */
  const x0 = heuristicExtractor();
  const x1u = heuristicExtractor(undefined);
  const x1e = heuristicExtractor(SHARED_EMPTY_HABITS);
  const [c0, c1, c2] = await Promise.all([
    x0.extract({ objective: OBJ, sources: [] }),
    x1u.extract({ objective: OBJ, sources: [] }),
    x1e.extract({ objective: OBJ, sources: [] })
  ]);
  const s0 = canon(c0);
  check("extractor parity X0/X1U/X1E", s0 === canon(c1) && s0 === canon(c2));

  /* blindPairwiseCompare arms: P1 / P2U / P2F */
  const p1 = canon(blindPairwiseCompare(pairInput));
  const p2u = canon(blindPairwiseCompare(pairInput, undefined));
  const p2f = canon(blindPairwiseCompare(pairInput, false));
  check("pairwise-compare parity P1/P2U/P2F", p1 === p2u && p1 === p2f);

  /* runBlindPairwisePair arms: R2 / R3U */
  const r2 = canon(runBlindPairwisePair(pairInput, rng));
  const r3 = canon(runBlindPairwisePair(pairInput, rng, undefined));
  check("pair-runner parity R2/R3U", r2 === r3);

  /* classifyDiffScope arms: D2 / D3U */
  const d2 = canon(classifyDiffScope(diffContext, changed));
  const d3 = canon(classifyDiffScope(diffContext, changed, undefined));
  check("diff-scope parity D2/D3U", d2 === d3);
  log(`PARITY sha256(all five boundaries, canon) = ${sha([m2, s0, p1, r2, d2].join("\n"))}`);
}

/* ================= HOT: per-arm distinct call sites ===================== */
/* Each arm gets TWO textually distinct call-site clones (own bytecode
 * positions, own feedback) — clone disagreement is the same-coordinate A/A
 * noise floor. Pass order interleaves arms. */
type Site = () => void;
const sink: unknown[] = [];
let sinkI = 0;
function keep(v: unknown): void {
  sinkI = (sinkI + 1) & 1023;
  sink[sinkI] = v;
}

/* gate G2 (omit) — the repo's own unit-test coordinate */
const G2a: Site = () => {
  assertCoverageAllowsStart(contract, tasks);
};
const G2b: Site = () => {
  assertCoverageAllowsStart(contract, tasks);
};
/* gate G3U (explicit undefined) — 27-round fixture + production-undefined arm */
const G3Ua: Site = () => {
  assertCoverageAllowsStart(contract, tasks, undefined);
};
const G3Ub: Site = () => {
  assertCoverageAllowsStart(contract, tasks, undefined);
};
/* factory X0 / X1U / X1E */
const X0a: Site = () => keep(heuristicExtractor());
const X0b: Site = () => keep(heuristicExtractor());
const X1Ua: Site = () => keep(heuristicExtractor(undefined));
const X1Ub: Site = () => keep(heuristicExtractor(undefined));
const X1Ea: Site = () => keep(heuristicExtractor(SHARED_EMPTY_HABITS));
const X1Eb: Site = () => keep(heuristicExtractor(SHARED_EMPTY_HABITS));
/* blindPairwiseCompare P1 / P2U / P2F */
const P1a: Site = () => keep(blindPairwiseCompare(pairInput));
const P1b: Site = () => keep(blindPairwiseCompare(pairInput));
const P2Ua: Site = () => keep(blindPairwiseCompare(pairInput, undefined));
const P2Ub: Site = () => keep(blindPairwiseCompare(pairInput, undefined));
const P2Fa: Site = () => keep(blindPairwiseCompare(pairInput, false));
const P2Fb: Site = () => keep(blindPairwiseCompare(pairInput, false));
/* runBlindPairwisePair R2 / R3U */
const R2a: Site = () => keep(runBlindPairwisePair(pairInput, rng));
const R2b: Site = () => keep(runBlindPairwisePair(pairInput, rng));
const R3Ua: Site = () => keep(runBlindPairwisePair(pairInput, rng, undefined));
const R3Ub: Site = () => keep(runBlindPairwisePair(pairInput, rng, undefined));
/* classifyDiffScope D2 / D3U */
const D2a: Site = () => keep(classifyDiffScope(diffContext, changed));
const D2b: Site = () => keep(classifyDiffScope(diffContext, changed));
const D3Ua: Site = () => keep(classifyDiffScope(diffContext, changed, undefined));
const D3Ub: Site = () => keep(classifyDiffScope(diffContext, changed, undefined));

interface Arm {
  boundary: string;
  arm: string;
  a: Site;
  b: Site;
  reps: number;
}
const ARMS: Arm[] = [
  { boundary: "gate", arm: "G2", a: G2a, b: G2b, reps: 20000 },
  { boundary: "gate", arm: "G3U", a: G3Ua, b: G3Ub, reps: 20000 },
  { boundary: "extractor-factory", arm: "X0", a: X0a, b: X0b, reps: 200000 },
  { boundary: "extractor-factory", arm: "X1U", a: X1Ua, b: X1Ub, reps: 200000 },
  { boundary: "extractor-factory", arm: "X1E", a: X1Ea, b: X1Eb, reps: 200000 },
  { boundary: "pairwise-compare", arm: "P1", a: P1a, b: P1b, reps: 20000 },
  { boundary: "pairwise-compare", arm: "P2U", a: P2Ua, b: P2Ub, reps: 20000 },
  { boundary: "pairwise-compare", arm: "P2F", a: P2Fa, b: P2Fb, reps: 20000 },
  { boundary: "pair-runner", arm: "R2", a: R2a, b: R2b, reps: 10000 },
  { boundary: "pair-runner", arm: "R3U", a: R3Ua, b: R3Ub, reps: 10000 },
  { boundary: "diff-scope", arm: "D2", a: D2a, b: D2b, reps: 50000 },
  { boundary: "diff-scope", arm: "D3U", a: D3Ua, b: D3Ub, reps: 50000 }
];
/* warm every clone site */
for (const arm of ARMS) {
  for (let i = 0; i < 2000; i += 1) {
    arm.a();
    arm.b();
  }
}
const N_PASSES = 5;
const readings = new Map<string, { a: number[]; b: number[] }>();
for (const arm of ARMS) readings.set(`${arm.boundary}/${arm.arm}`, { a: [], b: [] });
for (let pass = 0; pass < N_PASSES; pass += 1) {
  for (const arm of ARMS) {
    const slot = readings.get(`${arm.boundary}/${arm.arm}`)!;
    gcBarrier();
    slot.a.push(bench(arm.a, arm.reps));
    gcBarrier();
    slot.b.push(bench(arm.b, arm.reps));
  }
}
const armMedian = new Map<string, number>();
log("--- ARG-SLOT FACE: in-process rotation view (ns/call; median over 2 clones x 5 passes) ---");
for (const arm of ARMS) {
  const key = `${arm.boundary}/${arm.arm}`;
  const slot = readings.get(key)!;
  const m = median([...slot.a, ...slot.b]);
  const floor = Math.max(median(slot.a), median(slot.b)) / Math.min(median(slot.a), median(slot.b));
  armMedian.set(key, m);
  log(`HOT ${key}: ${m.toFixed(1)}ns/call (clone-A/A floor ${floor.toFixed(3)})`);
}
/* adjudication per boundary: cross-arm ratio vs worst clone floor */
const BOUNDS: [string, string[]][] = [
  ["gate", ["G2", "G3U"]],
  ["extractor-factory", ["X0", "X1U", "X1E"]],
  ["pairwise-compare", ["P1", "P2U", "P2F"]],
  ["pair-runner", ["R2", "R3U"]],
  ["diff-scope", ["D2", "D3U"]]
];
for (const [boundary, arms] of BOUNDS) {
  const meds = arms.map((a) => armMedian.get(`${boundary}/${a}`)!);
  const hi = Math.max(...meds);
  const lo = Math.min(...meds);
  const ratio = hi / lo;
  const absDelta = hi - lo;
  let worstFloor = 1;
  for (const a of arms) {
    const slot = readings.get(`${boundary}/${a}`)!;
    const f = Math.max(median(slot.a), median(slot.b)) / Math.min(median(slot.a), median(slot.b));
    worstFloor = Math.max(worstFloor, f);
  }
  adjudicate(
    `arg-slot ${boundary}`,
    ratio <= Math.max(1.1, worstFloor * 1.5) || absDelta < 1000,
    `cross-arm=${ratio.toFixed(3)} floor=${worstFloor.toFixed(3)} |delta|=${absDelta.toFixed(1)}ns arms=[${arms
      .map((a) => `${a}=${armMedian.get(`${boundary}/${a}`)!.toFixed(1)}`)
      .join(" ")}]`
  );
}

/* ============ PROCESS-ISOLATED CONFIRMATION (gate boundary only) ========= */
{
  const wall: Record<string, number[]> = { G2: [], G3U: [] };
  const meds: Record<string, number[]> = { G2: [], G3U: [] };
  const order: ("G2" | "G3U")[] = ["G2", "G3U", "G2", "G3U", "G2", "G3U"];
  for (const armName of order) {
    const t0 = performance.now();
    const res = spawnSync(
      process.execPath,
      ["--expose-gc", "--import", "tsx", "/tmp/r28h-arity.mts"],
      {
        cwd: "/workspace",
        env: { ...process.env, R28H_CHILD: armName },
        encoding: "utf8",
        timeout: 180000
      }
    );
    wall[armName]!.push(performance.now() - t0);
    check(`isolated child ${armName} exit 0`, res.status === 0, String(res.stderr).slice(0, 300));
    const line = (res.stdout ?? "").split("\n").find((l) => l.startsWith("CHILDGATE "));
    check(`isolated child ${armName} emitted CHILDGATE`, line !== undefined);
    if (line !== undefined) meds[armName]!.push(Number(line.slice(10)));
  }
  const g2 = median(meds.G2!);
  const g3 = median(meds.G3U!);
  const ratio = Math.max(g2, g3) / Math.min(g2, g3);
  const g2f = Math.max(...meds.G2!) / Math.min(...meds.G2!);
  const g3f = Math.max(...meds.G3U!) / Math.min(...meds.G3U!);
  const floor = Math.max(g2f, g3f);
  log(
    `ISOLATED gate: G2=${g2.toFixed(1)}ns G3U=${g3.toFixed(1)}ns cross=${ratio.toFixed(3)} within-arm-spread floor=${floor.toFixed(3)} (3 fresh processes/arm)`
  );
  adjudicate(
    "arg-slot gate (process-isolated)",
    ratio <= Math.max(1.1, floor * 1.5) || Math.abs(g2 - g3) < 1000,
    `cross=${ratio.toFixed(3)} floor=${floor.toFixed(3)} |delta|=${Math.abs(g2 - g3).toFixed(1)}ns`
  );
  /* arithmetic closure: gate = 1 call/run in production (3 mutually
   * exclusive run entries, each gates once) */
  log(
    `CLOSURE worst cross-arm gate |delta| x 1 call/run = ${(Math.abs(g2 - g3) / 1e6).toFixed(6)}ms/run — reject class by >=4 orders of magnitude`
  );

  /* S5-H-1 attestation (after all timing) */
  const mkC = (checks_: string[]): RequirementContract =>
    ({
      id: "req_00000001",
      rawText: "t",
      goals: [],
      constraints: [],
      acceptanceCriteria: checks_.map((oc, i) => ({
        id: `ac_0000000${i}`,
        description: "d",
        observableCheck: oc,
        sourceRefs: []
      })),
      assumptions: [],
      questions: [],
      provenance: []
    }) as unknown as RequirementContract;
  check("S5-H-1 fast-only -> []", detectConflicts(mkC(["response is fast", "check completes < 10ms"])).length === 0);
  check("S5-H-1 slow-only -> []", detectConflicts(mkC(["batch job is slow", "runs > 1000ms"])).length === 0);
  check(
    "S5-H-1 fast+slow -> exactly 1",
    detectConflicts(mkC(["response is fast", "batch job is slow"])).length === 1
  );
  log(`CHECKS ${checks - failures}/${checks} passed; ADJUDICATIONS ${adjudications - breaches}/${adjudications} in-class (${breaches} BREACHED)`);
  if (failures > 0) process.exit(1);
}
```

## 附录 C：临时资产清单（已删除）

| 路径 | 行数 | sha256 | 处置 |
|---|---|---|---|
| `/tmp/r28h-anchor.mts` | 241 | `31bb8b47f733faeb1f81fd8bcc92ce71022e64fcf723bb911f860b49998a783a` | 全文存档于附录 A 后删除 |
| `/tmp/r28h-arity.mts` | 443 | `22560ae0e872c317bf7b302daa8afebbfb1832b42e5051b63f96aa69d4cb2268` | 全文存档于附录 B 后删除 |
| `/tmp/r28h-gate.log` | — | —（gate 全绿输出，1169/78/1168/1/0） | 删除 |

早期协议变体（S5-H-1 三元组前置的锚点预跑与新轴驱动的 Function.length 断言初版——预期值 2/2 被实测 3/3 纠正，census 教训已写入 §2.1/结论第 2 条）未存档为独立文件：两者均为同一驱动文件的中间编辑态，最终态 sha256 已在上表。

## 尾注

- 未开 PR；分支 `cursor/r28-h-eval-twenty-eighth-pass-83a1` 已推送；`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` = 仅本文件。
- 硬不变量逐条核对（生产零 diff 下天然成立）：分析不改 in-flight run；tracking 无命令权；H/score 不写路由 PASS/FAIL；live = R0 等价（R1 不接线）、稀疏 → 已批准 R0 基线；双 LCB 与双归因都在；提升 proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值变更、无测试改动；CAS/凭据/数据面契约/公开签名未触；无 fsync/完整性再哈希触碰（本切片无该面）；fail-open 家族保持关闭；S5-H-1 保留；PATH_RE 重开条件未触发。

MORE_OPTIMA=no
BRANCH=cursor/r28-h-eval-twenty-eighth-pass-83a1
