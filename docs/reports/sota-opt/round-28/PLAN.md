# Round 28 分区（10 子代理，3-VM 分波）

模型一律 `claude-fable-5-thinking-xhigh`。基线：最新 `cursor/sota-persistent-opt-83a1`。

Round 1–27 已对各区做过二十七遍穷尽裁决（Round 27 已关闭，全部空枚举），并落地 S1-F / J1 / S1-C / S1-I / S2-C / S3-C / S4-C / S4-I / S5-C / S5-F / S5-I-1 / S6-C / S6-F-1 / S7-C / S7-F-1 / S7-F-2 / S7-I-1。本轮只接受排除表未覆盖、且理论+仿真达门槛的新更优解。禁止重开任何 X* / S1-* … S27-* 条目（含已合入的 S13-B-1 与 R12–R27 全部空枚举收口，含刚合入的 R26-A … R26-J / R27-A … R27-J / R28-A / R28-B / R28-C / R28-D / R28-E / R28-F / R28-G / R28-H / R28-I）。S7-C 已落地，不得另起平行实现。S7-F-1 不是 S6-F-5。S5-H-1 必须保留。禁止再编号 **FITQ** / **NAMESHAPE** / **SCALEX** / **XPROC** / **YMIX** / **SCHEDWIN** / **KFAN** / **NVG** / **SEEDX** / **TERMCLASS** / **SHAPEK** / **ORDX** / **CKMIX** / **CALLB** / **OCCX** / **JB** / **RUNLIM** / **VALCLASS** / **CG** / **AGEX** / **COTARG** / **PRICEX** / **RTL** / **DELINV** / **PROBX** / **RVL** / **SIDEC** / **PWL**。空枚举轴「M=1 catalog collapse」「approval-authority face」「state serialization-boundary provenance face」「operand-residency census」「adaptation-state coordinate」「value-domain / string content-class census」「task-face optional-field presence topology」「identity-coordinate face」「plan-header identity-geometry face」「injection command-kind face」「consumer-stage string-representation census」「process-environment coordinate」「collation-locale face」「privacy / provider-policy constraint-leg face」「injection-seam default-arm face」「input storage-state face」「edge condition-kind face」「argv surface-form face」「consumer-stage operand-realm face」「scalar numeric value-class face」「batch-cardinality bottom + prelude intercept face」「policy-content tree-geometry face」「input access-path mechanics face」「join-policy face」「process output-channel disposition face」「execution-substrate face」「catalog-face row-arrangement (permutation) face」「input key-set geometry face」「C-slice numeric-kernel execution-substrate instantiation」「contents-store population face」「boundary call-protocol face」「proposal write-path termination-leg face」「decision-gate resolution face」「boundary argument-slot materialization face」「invocation-identity / path-referent spelling × cwd face」不铸 ID、不写入排除表，但禁止再编号或跨区移植。face 名「execution-substrate face」归 R27-H；R28-C 只是该面在 C 区数值内核上的一格剂量，勿另起绰号。boundary call-protocol face 与 R22-H 调用上下文 / **CALLB** 不同面。**PWL** 是 proposal write-path termination-leg face 的观察绰号，不写入排除表。

R7-I 的教训：默认态夹具会遮蔽配置态主路径。本轮须按「配置态 × 命令类」矩阵复核测量盲区。多个切片已有整片预算收口——先复核再找新角度，不要硬凑。

分区与 Round 1 相同（R28-A … R28-J），报告写入 `docs/reports/sota-opt/round-28/`。

状态：第 1–7 波 A–I 已合入；第 8 波 J 运行中。Round 28 未关轮（仅 J 仍在飞）。Round 29 已开卷（A 运行中、B 本波派出）。

A 切片已合入：空枚举，未铸 S28-A-*。切片 `git diff 7acb666..HEAD` 为空。预算复核 12.2–15.2 µs/gate ⇒ ~61–76 µs/run（与 R24-A…R27-A 带重叠）。本轮新增 input key-set geometry face（调用方对象的结构类型盈余宽度 × 插入序 × 属性backing；生产在轴原点；投影面平坦，盈余斜率 ~225–235 ns/key，最坏合法表几何 +1.0–1.3 µs/gate，属 µs/ns 拒收）。与 **SHAPEK**（形状基数/混合、schema 精确键集）/ **ORDX**（记录间序）/ R27-F input access-path mechanics / R26-F input storage-state / R24-A list-dimension 不同面，不补铸。R27-A scalar numeric value-class face 与 R26-A collation-locale face / R25-A value-domain / string content-class census / R23-A G×E / R22-A 公开导出普查 / R21-A H 输入面 / R20-A openMinors / R25-I process-environment coordinate / R26-I argv surface-form face / R27-I process output-channel disposition face / R27-J **SIDEC** / R26-J **DELINV** / R26-H consumer-stage operand-realm face / R27-H execution-substrate face / R28-B catalog-face row-arrangement (permutation) face 不补铸。基线 `7acb666` 空 diff 再确认。

B 切片已合入：空枚举，未铸 S28-B-*。切片 `git diff 94ed3d9..HEAD` 为空。天花板复核 M=2 8.84–9.53 / M=10 17.62–18.13 ms/eval（历史带重叠）。S12-B-2 / S13-B-1 重开条件未触发。本轮新增 catalog-face row-arrangement (permutation) face（目录行排列；replay 贴噪声地板；live 1.1–5.3 µs/call 属 once-per-CLI 拒收；W3 证排列是契约决策承载，预排序是行为变更）。R27-B batch-cardinality bottom + prelude intercept face 与 R26-B privacy / provider-policy constraint-leg face / R25-B task-face optional-field presence topology / R24-B M=1 catalog collapse / R23-B batch arrangement / R22-B 标识符几何（G×T）不补铸。基线 `94ed3d9` 空 diff 再确认。

C 切片已合入：空枚举，未铸 S28-C-*。切片 `git diff 183df9b..HEAD` 为空。生产中位复核 660.9–667.5 ms（与 R26-C/R27-C 带重叠）。APCX−FLOORX 17.3–27.3 < 35；sink=7.309 逐位。r1c–r7c 8028 / 14420 / 14730 / 24888 / 28555 / 25483 / 6193 全绿。本轮新增 C-slice numeric-kernel execution-substrate instantiation（tsx-on-src / node-on-dist / tsx-on-dist；face 名仍归 R27-H；9/9 摘要全等，热层亚带，冷面 once-per-process 拒收，9 文件内无可落地物）。**PROBX** / **PRICEX** / **AGEX** / **FITQ** / **SCALEX** / **YMIX** / **SEEDX** / **ORDX** / **OCCX** 不补铸。R28-A input key-set geometry face / R28-B catalog-face row-arrangement (permutation) face / R27-H execution-substrate face / R27-J **SIDEC** / R27-A scalar numeric value-class face / R27-I process output-channel disposition face 不要移植。基线 `183df9b` 空 diff 再确认。

D 切片已合入：空枚举，未铸 S28-D-*。切片 `git diff 82bef36..HEAD` 为空。eval 地板复核 3.27–3.44 ms（贴 R26-D 上沿、低于 R27-D，记为 VM 方差）。S9-D-4 / S12-D-1 未重开。本轮新增 contents-store population face（`contentsByHash` 的别名重数 μ × 尺寸分区 × 引用态；夹具已 μ=1.33；写侧 μ 不变，persist/load 只付 unique 字节；分区最坏 ~0.13 ms 属 µs 拒收；成功路径无持久孤儿；10 ms 越线需幻想规模）。R27-D policy-content tree-geometry face 与 R26-D injection-seam default-arm face / R25-D identity-coordinate face / R24-D approval-authority face / R23-D 两相 P-dose / R22-D 公开导出流普查不补铸。R28-C C-slice numeric-kernel execution-substrate instantiation / R28-A input key-set geometry face / R28-B catalog-face row-arrangement (permutation) face / R27-H execution-substrate face / R27-J **SIDEC** / R27-A scalar numeric value-class face / R27-I process output-channel disposition face 不要移植。基线 `82bef36` 空 diff 再确认。

E 切片已合入：空枚举，未铸 S28-E-*。切片 `git diff adb20d7..HEAD` 为空。SLICE-CPU 复核 cmp25 15.0–15.2 µs/run（9/9 落在 14.7–15.2 带）。S8-E-1 / S9-E-2 / S13-B-1 未重开。本轮新增 proposal write-path termination-leg face（**PWL**：两处提案写者的返回腿 × 持久写激活集；片内 CPU 0.5–1.1 µs/run，腿差 0.3–1.2 ms 属 once-per-run I/O 且在 `src/adaptation/` / 已关 ID）。**RVL** / **RTL** / **CG** / **JB** / **NVG** 不补铸。R28-F boundary call-protocol face / R28-D contents-store population face / R28-C C-slice numeric-kernel execution-substrate instantiation / R28-A input key-set geometry face / R28-B catalog-face row-arrangement (permutation) face / R27-H execution-substrate face / R27-J **SIDEC** / R27-A scalar numeric value-class face / R27-I process output-channel disposition face 不要移植。基线 `adb20d7` 空 diff 再确认。

F 切片已合入：空枚举，未铸 S28-F-*。切片 `git diff 519101f..HEAD` 为空。锚点复核 r5f 121.48/120.18、r6f 122.81、r7f 127.06/126.63、r1f 2668/2668（历史带重叠）。S7-F-1 ≠ S6-F-5。本轮新增 boundary call-protocol face（公开函数调用适配器：direct / extracted-local / `.call` / `.apply` / `Reflect.apply` / 预绑定 / spread / rest-spread；面平坦，适配税 +3.9–4.6 ns/call，最坏 0.009 ms/实验，属 ns 拒收）。与 R22-H 调用上下文 / **CALLB** 不同面，不补铸。R27-F input access-path mechanics face 与 R26-F input storage-state face / R25-F plan-header identity-geometry face / R24-F state serialization-boundary provenance face / R23-F plan count-field 联合面 / R22-F membership-topology 不补铸。R28-D contents-store population face / R28-C C-slice numeric-kernel execution-substrate instantiation / R28-A input key-set geometry face / R28-B catalog-face row-arrangement (permutation) face / R27-H execution-substrate face / R27-I process output-channel disposition face / R27-G join-policy face / R27-J **SIDEC** 不要移植。基线 `519101f` 空 diff 再确认。

G 切片已合入：空枚举，未铸 S28-G-*。切片 `git diff 4efee23..HEAD` 为空。计算顶复核 0.286–0.289 vs I/O 89.4–103.1 ms（~309–360×）。digest `06cbcf92c098c8f0` 第十九次逐位相同，勿补铸 digest。本轮新增 decision-gate resolution face（gate 角色 × 审批要求 × 后继计划几何 × 选择子集；`compileChildrenToFlowchart` 内部剂量为零，腿属 once-per-command µs，审批被人类墙钟主导）。R27-G join-policy face 与 R26-G edge condition-kind face / R25-G injection command-kind face / **RUNLIM** / **CKMIX** / **TERMCLASS** / **SCHEDWIN** / **XPROC** / **NAMESHAPE** 不补铸。R28-E proposal write-path termination-leg face / **PWL** / R28-F boundary call-protocol face / R28-D contents-store population face / R28-C C-slice numeric-kernel execution-substrate instantiation / R28-A input key-set geometry face / R28-B catalog-face row-arrangement (permutation) face / R27-F input access-path mechanics face / R27-I process output-channel disposition face / R27-H execution-substrate face / R27-J **SIDEC** 不要移植。基线 `4efee23` 空 diff 再确认。

H 切片已合入：空枚举，未铸 S28-H-*。切片 `git diff fd437a9..HEAD` 为空。热层复核 9006–9483 ns（落在 R27-H 9005–9493 带）。S5-H-1 六次见证保留。本轮新增 boundary argument-slot materialization face（五处公开边界的 argc 坐标 / 末槽显式 `undefined` vs 默认字面；12 臂全平坦，唯一稳定信号是 `habits = {}` 空对象 4.6–14.0 ns，生产调用方已传对象；≤0.000043 ms/run）。R27-H execution-substrate face 与 R26-H consumer-stage operand-realm face / R25-H consumer-stage string-representation census / R24-H operand-residency census / R23-H rubric-registry membership-mix / R22-H 调用上下文 / R26-F input storage-state face 不补铸。R28-G decision-gate resolution face / R28-E proposal write-path termination-leg face / **PWL** / R28-F boundary call-protocol face / R28-D contents-store population face / R28-C C-slice numeric-kernel execution-substrate instantiation / R28-A input key-set geometry face / R28-B catalog-face row-arrangement (permutation) face / R27-J **SIDEC** 不要移植。PATH_RE 仅当目标为程序化/对抗或真实载荷 ≥~3.2K 无斜杠 `[\w.-]` 才重开。基线 `fd437a9` 空 diff 再确认。

I 切片已合入：空枚举，未铸 S28-I-*。切片 `git diff 8dee7fb..HEAD` 为空。custom−builtin 复核 children +46.4~+48.0 / +23.1~+26.3、track +45.9~+50.3 / +23.1~+25.7、flowchart-flat64 +41.0~+45.9 / +16.5~+24.6 ms（与 R27-I 同号同量级）。S8-I-1 未重开。r4i/r5i/r7i 68/119/80 全绿。本轮新增 invocation-identity / path-referent spelling × cwd face（第五条 spawn 输入通道；面平坦，最坏 ~0.7 µs / 天花板 ~0.14 ms/command，属 REJECT）。入口 `import.meta.url` vs `pathToFileURL(argv[1])` 在 npm POSIX `.bin` 符号链接下 silent no-op 已归档，属行为变更、战役外缺陷观察，不在本战役改。R27-I process output-channel disposition face 与 R26-I argv surface-form face / R25-I process-environment coordinate / R24-I adaptation-state coordinate / R23-I `.pi/` 可观测树 / R22-I run-record 回读 / R21-I flowchart 平面 / **NAMESHAPE** 不补铸。R28-H boundary argument-slot materialization face / R28-G decision-gate resolution face / R28-E proposal write-path termination-leg face / **PWL** / R28-F boundary call-protocol face / R28-D contents-store population face / R28-C C-slice numeric-kernel execution-substrate instantiation / R28-A input key-set geometry face / R28-B catalog-face row-arrangement (permutation) face / R27-J **SIDEC** / R27-H execution-substrate face 不要移植。基线 `8dee7fb` 空 diff 再确认。

J 切片 = 29 文件。J1 / S5-J-3 / S6-J-1 / S8-J-2 钉死。禁止去 fsync。R27-J **SIDEC** 与 R26-J **DELINV** / R25-J **COTARG** / R24-J **VALCLASS** / R23-J **CALLB** / R22-J **SHAPEK** / R21-J **KFAN** 不补铸。R28-I invocation-identity / path-referent spelling × cwd face / R28-H boundary argument-slot materialization face / R28-G decision-gate resolution face / R28-E proposal write-path termination-leg face / **PWL** / R28-F boundary call-protocol face / R28-D contents-store population face / R28-C C-slice numeric-kernel execution-substrate instantiation / R28-A input key-set geometry face / R28-B catalog-face row-arrangement (permutation) face / R27-I process output-channel disposition face / R27-G join-policy face / R26-I argv surface-form face / R26-G edge condition-kind face / R25-I process-environment coordinate / R27-H execution-substrate face 不要移植。file-lock retryMs 已由 D/E 定价，勿作为 J ID 补铸。基线 `fb41417` 预期空 diff。运行中。若落地：重跑 J1（2468）+ r28j。I/O 地板 R27-J：preferences 125.8–310.6 / 422.0–533.8 µs；jsonl 60.7–70.2 / 232.2–324.7 µs。
