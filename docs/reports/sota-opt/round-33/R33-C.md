MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 33 / R33-C：离线路由切片第三十三遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 33 / R33-C（Round 1–32 同区三十二遍
  之后的第三十三遍，叠在已落地的 S7-C 之上）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `f0a9d97`
  （R33-A 空枚举摄取——A 账本 44/44 三腿卫生——+ 本 R33-B/C/D 波派单）；
  工作期间独占分支 docs-only 快进**两次**：`f0a9d97` → `d9a4e30`
  （R33-D 空枚举摄取 + R33-E 派单，增量仅触 docs/reports/sota-opt/ 下
  3 文件 PROGRESS / round-33 PLAN / R33-D.md，159 插入 17 删除）→
  `8a29f3a`（R33-B 空枚举摄取 + R33-F 派单，增量同仅触
  docs/reports/sota-opt/ 下 3 文件 PROGRESS / round-33 PLAN /
  R33-B.md，184 插入 15 删除）。两次增量对 `src/ test/ scripts/
  package.json pnpm-lock.yaml` 直接复核均**零行**；按令逐次 rebase
  报告分支至 `d9a4e30`、`8a29f3a`（均纯快进零冲突）并重验冻结：
  `git diff 183df9b -- <9 切片文件>` 在分支创建点（`f0a9d97`）、
  两次快进后（`d9a4e30`、`8a29f3a`）、提交前**四验均 0 行**。最强
  冻结再延一轮：`git diff 89f0455..HEAD -- src/` **完全为空**（R10
  以来全部提交均为 docs）。`git log -1 -- src/routing/` 显示切片
  最后一次生产改动**仍为 `183df9b`「perf(routing): land S7-C
  support-only eta」**。新收口姊妹项 **R33-D 空枚举**（D 区元闭合
  35 行重推 + 哨兵卫生，未铸新面）与 **R33-B 空枚举**（B 区从零重
  枚举 + 天花板重锚落主机偏置窗族，未铸新面）已按两版快进后 PLAN
  并入非移植清单并完成划界（§4）。全部锚点测量在 `f0a9d97` 工作树
  完成——两次快进增量 `src/` 逐字节零变化 ⇒ 测量形态与提交形态位
  级同一，全部有效；聚焦套件 + `pnpm gate` 在三个 tip 各跑一次均绿
  （计数逐字同）。R33-E / R33-F 在飞，按令未等待、未触碰。
- **分支:** `cursor/r33-c-offline-routing-thirty-third-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，`wc -l` 逐文件 170/168/311/585/254/156/88/130/177 = 合计 2,039 行，与 R29-C..R32-C 普查同构；S7-C 支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开、S2-C 规范键 eta/mu 去重、S1-C bootstrap 工件复用全部现场核对在位）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，
  与 R28-C..R32-C 同引擎位级相同；`/exec-daemon` PATH 暴露的系统 Node
  v22.14.0 已显式绕开——PATH 前置 nvm bin 后 `node --version` 在每个
  官方批次前复核 v22.22.2，**零批次**落在 v22.14.0 上，无弃置批次；
  tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**第三十三组诚实枚举为空：不铸任何 S33-C-\* ID、不铸新 face 名、
不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，本轮生产代码零改动。**
对 9 个源文件逐行第三十三遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R32-C 的全部无号关闭
探针与轴，含 R31-C 的 **BINDX**、R30-C 的 REPRX、R29-C 的 PAIRX、
R28-C 的基片实例化、R27-C 的 PROBX、R26-C 的 PRICEX、R25-C 的
AGEX、R19-C 的 FITQ、R31-J 的 **MODEX**，及本轮姊妹 R33-A /
R33-B / R33-D 三次空枚举卫生记录）后，本轮**从未点名站点数为零（连续第十五遍）**
——每一个执行站点都能指到既有 ID、无号探针、零池备忘或池算术覆盖。
R26-C/R27-C 关掉输入数值坐标、R28-C 关掉执行基片坐标、R29-C 关掉
工作负载组成坐标、R30-C 关掉输入表示坐标、R31-C 关掉名字解析坐标、
R32-C 独立重推元坐标系无余项之后，本轮再次独立重推（R32-A §2.3
方法版本 + R33-A/R33-B/R33-D 卫生后缀），**无任何「从未受控 +
独立物理对象 + 真尺度」三条同时成立的剩余坐标**：C 切片是进程内
纯同步数值内核，零 fs 数据路径（MODEX / R31-E codec 面结构性空，§4 计数直证）、
零 `JSON.parse` 输入位点（REPRX 划界维持）、零 OS 边界（R31-I 审计
平面正交）。本轮新增换名检查 **2 项**（§2.2：posterior `lnGamma`
反射分支死码消除；fitLogitAdditive 头部退化结局双 `every` 扫描融合），
逐项裁决为既有 ID 族 + 公开面活契约 / 亚带，不铸 ID。

按派单在本 VM 重锚默认预算（未拷贝历轮数字，装具为 R31-C 附录
A.1 逐字节血统，仅头注更新，§附录）：

- **生产中位带（本轮正典）：670.2–708.7 ms/报告**（四次运行
  665.7 / 670.2 / 708.7 / 679.5；正典三读数 670.2–708.7）——高于
  R32-C 带 659.0–662.0，与 R16-C 历史带 672.8–686.3 交叠
  （672.8–679.5）。**诚实注记（主机干扰窗口）**：正典 2 的 708.7
  是逐 rep 整体抬升（7 rep 中 5 个 ≥ 706，峰 745.7），正典 1 含单个
  728.9 慢 rep，正典 3 则紧致（676.6–684.9）——抬升同现于同一会话
  窗口、层内同向，是 0 行冻结下的主机偏置（本 VM 比 R32-C 宿主更
  嘈杂），非切片回归。**同 VM 独立旁证钉死结构结论**：r7c 回归资产
  锚点 **production 663.9 ms = 1.12× vs 冻结 S6-C ctl 740.9**——
  比率与 R28-C/R29-C/R31-C/R32-C（1.12×）/R30-C（1.13×）逐轮
  基本不动（另一次 r7c 通过打印 665.4 = 1.14×，同带）；切片对
  `183df9b` 零 diff，装具协议逐字节同 R31-C/R32-C。按 R14-C 处置
  规则随读重锚为本轮带。
- **small 带 30.3–30.9 ms**（R32-C 30.0–30.2、R31-C 29.9–30.2，
  带缘相接——small 档对主机窗口远不如 perf 档敏感，与主锚抬升为
  宿主项的判读一致）；整条 small 报告低于 ±35 ms 噪声带（R7-I
  矩阵义务：该档只作确定性与保真见证，不作候选判据）。
- **APCX / FLOORX / 组合天花板**：APCX 无计时器独立道正典
  68.9 / 63.3 / 67.8 ms（R32-C 正典带 60.2–61.7——同主机窗口
  同向抬升）；FLOORX 正典 37.2 / 37.6 / 40.4 ms（与 R32-C 带
  35.1–40.0 交叠）⇒ **组合天花板 = 31.7 / 25.7 / 27.3 ms < 35**
  ——正典带 25.7–31.7 与 R32-C 正典带 20.2–26.6 交叠
  （25.7–26.6）；正典 1 的 31.7 为历轮正典最高读数（由主机抬升的
  APCX 驱动），距 35 带下缘仍差 3.3 ms，且四读数全部 < 35：组合
  出路维持关闭。预演天花板 25.7 同 < 35，按历轮惯例预演不入正典带。
- **sink = floorSink = 7.309 八读数全同**（4 运行 × 2 道，每运行
  内 `Object.is(apcSink, floorSink)` 断言全绿）——与
  R14-C/R16-C..R32-C 打印值逐位同值，跨轮确定性旁证成立：主机
  抬升动不了任何输出位。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等；每运行
  2,768 项位级检查全绿，四运行合计 **11,072 项**。

回归资产与门禁（§5）：r1c–r7c 七脚本全绿，检查数逐一匹配锚定值
**8028 / 14420 / 14730 / 24888 / 28555 / 25483 / 6193**；聚焦
离线路由套件 59/10/59/0；`pnpm gate` 在 `f0a9d97`、`d9a4e30`、
`8a29f3a` 三 tip 各跑一次均 exit 0（tests 1169 / suites 78 /
pass 1168 / fail 0 / skipped 1，三次逐字同计数，与 R32-C/R33-A/
R33-B/R33-D 同计数）。

硬性不变量逐条复核（§0）：双 LCB 在位（posterior.ts L22
`LcbKind = "normal" | "beta-quantile"`，本轮 rg 复核）；双归因
在位（attribution-report.ts L18–19 `logit: fitLogitAdditive(rows),
probAdd: fitProbabilityAdditive(rows)`，现场核对）；live = R0
等价（`src/supervisor/` 对 9 切片文件导入为零，本轮 rg 复核，
R1 未接线）；推广 proposal-first、不声称 Outcome-supported
（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；
CAS/凭证/数据面合同/公开签名零触碰。仓库变更仅本报告一个文件；
无生产赢家故未提交任何临时装具（血统锚定装具指纹见附录，临时
文件归档后已删除）。空枚举与 R8-C..R32-C 同为战役允许的诚实
结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → PROGRESS.md → EXCLUSIONS.md
  （完整表 545 行，含全部 S\*-C-\* 行与 S12-C-1、S13-B-1）→
  round-33/PLAN.md（C 段 + 具名/unnamed 全 ban 榜，快进前后三版）
  → round-32/PLAN.md → R32-C.md 全文（空枚举确认，无 S32-C-\*；
  两项换名检查——pow 强度削减 / TypedArray 数值排序——属主记录
  在案，本轮不重提）→ R33-A.md 全文（空枚举；A 账本 44/44 三腿
  卫生，**方法应用非新面，本轮不在 C 区重演账本分类**）→
  R31-C.md 全文（BINDX 轴定义 + C1 capture-hoist 双重拒绝 +
  附录 A.1 装具血统提取源）→ 9 个切片源文件全量重读 →
  `src/experiments/attribution-report.ts` 消费图现场核对 → 第一次
  快进后补读 R33-D.md 结论（D 区空枚举 + 元闭合 35 行重推卫生，
  与 C 零共享物理对象）与更新版 round-33/PLAN.md → 第二次快进后
  补读 R33-B.md 结论（B 区空枚举 + 从零重枚举 + 天花板重锚主机
  偏置窗，与 C 零共享物理对象）与再更新版 round-33/PLAN.md。
- 基线漂移检查：本轮测量期起点 = 分支创建点 = `f0a9d97`；工作
  期间发现两次 docs-only 快进（`d9a4e30` R33-D 合入 + R33-E
  派单；`8a29f3a` R33-B 合入 + R33-F 派单），每次均以
  `git diff --stat` + 对 `src test scripts package.json
  pnpm-lock.yaml` 的直接 diff = **0 行**逐项核对后按派单规程
  rebase 报告分支（两次均纯快进零冲突）并重验冻结。冻结主张四次
  核验均 **0 行**（切片 9 文件 vs `183df9b`：分支创建时、两次
  快进后、提交前）；最强冻结 `89f0455..HEAD -- src/` 为空。测量
  在字节相同的切片上完成，全部有效。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + 更新版
  PLAN 禁榜）：input object-representation / **REPRX**（切片
  `JSON.parse` 输入位点 0，§4 计数）/ **BINDX**（R31-C；realm 级
  绑定态——本轮零 patch、零剂量、零计时新臂；C1 capture-hoist
  不重提；划界计数见 §4）/ **MODEX**（R31-J——inode 权限模式字
  拓扑，J 区持久工件面；本切片数据路径零 fs 位点，面结构性空，
  §4 计数后声明即止）/ KFAN / COTARG / PROBX / DELINV / PRICEX /
  VALCLASS / PAIRX（工作负载组成——本轮全部测量单内核恒定，姊妹
  估计器零计时）/ **R32-A 重开条件哨兵审计**（方法不重铸——本轮
  不做「再查一遍重开条件」式产出；C 档案在案重开条件均要求 src/
  变更或规模增长，由 0 行冻结与锚点保真电池自然覆盖，不另立审计
  面）/ **R32-B 闭合稳定性审计**（B 区方法记录，不重走、不铸
  「再查一遍闭合稳定性」式新面）/ **R33-A 空枚举**（A 账本 44/44
  三腿分类是方法卫生，非新面——本轮不在 C 区重演 44 行式账本
  分类；C 账本裁决携带由 0 行冻结 + 锚点保真电池 + 本轮逐文件
  重读覆盖）/ **R33-D 空枚举**（第一次快进带入——D 区元闭合重推
  + 静态哨兵卫生，与 C 零共享物理对象，声明即止）/ **R33-B 空
  枚举**（第二次快进带入——B 区从零重枚举 + `ROLE_FAMILY` 死回退
  换名记录三重关拒列 + 天花板重锚主机偏置窗，其 10 文件全在在线
  路由面，与 C 零共享物理对象，声明即止）/ R31-I CLI 进程
  边界门扉闭合审计（本切片零 argv/env/stdin/stdout/子进程/信号/
  退出腿，审计平面正交零交集）/ digest / sink=7.309 位等不另铸轴
  （本轮 sink 八读数只作既有跨轮见证复现）。FITQ / NAMESHAPE /
  SCALEX / XPROC / YMIX / SCHEDWIN / NVG / SEEDX / TERMCLASS /
  SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / CG / AGEX /
  RTL / RVL / SIDEC / PWL / ZREP / CSPELL / LINKTOP 全部未编号
  未移植。R28-C 基片实例化未移植（全部测量锁死单一基片 tsx+src）；
  R30-I 时间段拓扑、R30-J LINKTOP、R31-A/B/D/F/G/H 停驻门与属地格
  未开。X\* / S1-\* … S32-\* 全部未重开；S12-C-1 重开条件未触发
  （重开条件：无）；S6-C-3/S6-C-4 重开条件（支撑分布大改 / 因子数
  增长使支撑上界 ≥ ~8）经锚点夹具形状见证未触发（支撑上界仍 5）；
  未起 S7-C 平行实现（X2-1 引理前提复核成立：0/1 设计、+0.0
  起点、有限 beta——FLOORX 道逐名值对 APCX 道 `Object.is` 断言
  全绿即为可执行见证）。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
  不开 PR。输家脚本不入库（本轮无输家道——除血统锚定装具外零
  计时新臂）。

## 1. 生产中位与锚点复核（本 VM 实测，未拷贝历轮数字）

测量装具与 R31-C 附录 A.1 逐字节血统（replica 保真电池 34 例 +
自决定性 + 中位重锚 + APCX/FLOORX；仅头注更新为 R33-C，diff 即
6 行注释，指纹见附录）；Node v22.22.2，tsx + src 直连（R28-C S
臂 = 历史正典基片）。1 次预演 + 3 次正典独立进程，全部在
`f0a9d97` 工作树完成：

| 运行 | perf 中位 (ms) | small 中位 (ms) | APCX (ms) | FLOORX (ms) | 组合天花板 (ms) | sink / floorSink | 检查 |
|---|---|---|---|---|---|---|---|
| 预演 | 665.7 | 30.4 | 61.5 | 35.8 | 25.7 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 1 | 670.2 | 30.9 | 68.9 | 37.2 | 31.7 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 2 | 708.7 | 30.3 | 63.3 | 37.6 | 25.7 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 3 | 679.5 | 30.7 | 67.8 | 40.4 | 27.3 | 7.309 / 7.309 | 2,768 ✓ |

perf 逐 rep（7 rep/运行）：预演 673.8 670.7 663.1 661.4 666.2
665.7 658.1；正典 1 679.7 670.2 668.5 667.4 668.2 728.9 691.2；
正典 2 708.7 678.7 721.2 706.5 745.7 710.0 671.1；正典 3 678.0
676.6 676.8 684.9 681.6 679.9 679.5。

- **生产中位带（本轮正典）：670.2–708.7 ms**。高于 R32-C 带
  659.0–662.0；与 R16-C 历史带 672.8–686.3 交叠。**主机干扰
  判读（诚实注记）**：正典 2 整轮抬升（5/7 rep ≥ 706）而非单
  慢 rep，预演与正典 3 的 rep 散布（658.1–684.9）与历史带大幅
  交叠——抬升是本 VM 的会话窗口宿主项。三重独立旁证钉死「非
  切片回归」：(i) 切片对 `183df9b` 逐字节零 diff；(ii) r7c 回归
  资产（§5）同 VM 打印 production 663.9 ms = **1.12×** vs 冻结
  S6-C ctl 740.9——与 R28-C/R29-C/R31-C/R32-C 逐轮同比率（另一
  次通过 665.4 = 1.14×，ctl 同向抬升）；(iii) sink 八读数逐位
  同历史值（输出位不动）。跨切片旁证：同轮 R33-B（另一 VM）独立
  记录 +12~+29% 主机偏置窗并裁归宿主——战役本窗口主机嘈杂为跨
  VM 普遍现象（独立 VM，读数不互抄，判读方法各自独立）。按
  R14-C 处置规则**随读重锚为本轮带 670.2–708.7**，绝对带较宽但
  结构结论（层级、比率、天花板、确定性）全部与历轮同构。
- **small 带 30.3–30.9 ms**（R32-C 30.0–30.2 带缘相接）；整条
  small 报告低于 ±35 ms 噪声带，只作确定性与保真见证。
- **APCX 正典带 63.3–68.9**（R32-C 60.2–61.7——主机窗口同向
  抬升）；**FLOORX 正典带 37.2–40.4**（R32-C 35.1–40.0 交叠）；
  **组合天花板正典带 25.7–31.7 < 35**（与 R32-C 20.2–26.6 交叠
  25.7–26.6；正典 1 的 31.7 为历轮正典最高，距带下缘 3.3 ms，
  是主机抬升 APCX 的直接后果——APCX 与 FLOORX 同 VM 同窗对减，
  差值仍全数 < 35，组合出路维持关闭）。预演天花板 25.7 同 < 35，
  预演不入正典带。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等（含
  replica 保真电池 34 例 + perf + small 对生产 import 的全字段
  断言），每运行 2,768 项检查全绿，四运行合计 11,072 项。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is(apcSink, floorSink)`）——与 R14-C/R16-C..R32-C
  打印值同值；X2-1 引理前提（0/1 设计、+0.0 起点、有限 beta）
  由 FLOORX 道逐名 `Object.is` 断言随运行复核成立。

## 2. 第三十三组枚举（诚实结果：空；零新站点 + 两项新换名检查）

### 2.1 逐文件重读与裁决（零未点名站点，连续第十五遍）

| 文件 | 本轮重读检查（全部命中既有 ID / 无号探针 / 零池） | 结论 |
|---|---|---|
| `r1.ts`（170 行） | 每调用 `modelsById` Map 构造、estimates spread、sampled/aboveFloor 双 filter、cheapest reduce = S1-C-5/S1-C-10 本体；costOf/tierIndex 线性 find = X1-4 本体（M≤10 噪声级）；prepared 观测索引 = 已落地 A2/B1；跨 episode 记忆化 = X1-6 | 无新候选 |
| `r1-shadow-report.ts`（168 行） | 每 episode request spread + routeR0 调用（B 区文件不触碰）；merge 路径 = S1-C-2（仓内不可达）+ S1-C-3（公开 ReadonlyMap 形状）；selectedCost find = X1-4 族；与 shadow-compare 合并 = X1-5；sanitizeClaims once-per-report | 无新候选 |
| `posterior.ts`（311 行） | 80 次二分 / Newton / 闭式 = X1-3；lnBeta 每 quantile 一次 = 已落地 C1；estimateFingerprint = S1-C-4；groupObservationsByKey = 已落地；betacf 守卫 Math.abs = BINDX 坐标（计数即止）；updatePosterior pow 站点 = R32-C 换名记录 1 属主（不重提）；`lnGamma` 反射分支（z<0.5）死码消除 = **本轮换名记录 1**（§2.2）；posteriorMean/Variance `total>0` 卫 = S8-C-4 fail-closed 数值卫家族（公开面可达零参先验，非死码）；零池备忘维持 | 无新候选 |
| `offline-logit.ts`（585 行） | S1-C（bootstrap 工件复用）/S2-C（规范键 eta/mu 去重）/S3-C（单位乘法消除）/S6-C（switch 直线化）/S7-C（支撑升序直加）落地形态逐站点在位；头部退化结局双 `every` 扫描融合 = **本轮换名记录 2**（§2.2）；draw 侧塌缩检查融合 = S2-C-5 本体；`beta.every(Number.isFinite)` = S7-C-2/S8-C-3；delta map+reduce = S2-C-3；`xtwx fill(0)` = S5-C-7；dot 展开 = S5-C-5（串行依赖）；onProbabilitiesFor = S7-C-4/S8-C-1；APC off 道全系 = S1-C-1/S2-C-2/S4-C-4/S8-C-2/S10-C-2（本轮组合天花板 25.7–31.7 < 35 维持关闭）；percentile 换 quickselect = X4-3 族；draws 循环 `.entries()` = S7-C-1；draws 预分配/pushValue = S1-C/S3-C-3 分配级；`values.sort` 数值特化 = R32-C 换名记录 2 属主（不重提）；`xtwz[a] = xtwz[a] + w` 换 `+=` 拼写 = 优化后同机器码零效应（S7-C-3 零效应家族，纯拼写不立记录）；尾部 reduce/Set once-per-report 亚噪声；worker/WASM = S9-C-1/S9-C-2；嵌套序交换 = S12-C-1（重开条件：无）；per-fit distinct-key 代表表 = S10-C-1；stamp Int32Array 换窄宽度 = S7-C-3 零效应家族 | 无新候选 |
| `offline-prob-add.ts`（254 行） | X3-4 + R17-C 零池；cell 双算合一 = S1-C-6；站点规范键去重 = S2-C-1；betaInterval 记忆化 = S1-C-7；betaInterval 上分位第二次 80 二分 = X1-3 本体 + S1-C-7 亚 ms 池；diagnose 内 `[...entries()]` 每效应重物化 = once-per-report 零池覆盖；`lastSegment` 死首参裁剪 = 纯文本改动零池（diagnose once-per-report），不立记录 | 无新候选 |
| `propensity.ts`（156 行） | 三遍改单遍 = X3-5 + S1-C-8 本体（weights 双 reduce 融合同属）；R21-C 零池维持 | 无新候选 |
| `lin-alg.ts`（88 行） | S4-C（行引用提升）/S5-C（按四展开）落地形态在位；变体空间 = S4-C-1..3 + S5-C-1..7 + S10-C-3（对称 Schur：部分主元破坏对称）+ S12-C-1 封闭；`stop=n-3` 提升 / `a.some` 手写 = S6-C-7；回代 eps 卫 = S8-C-4（fail-closed 数值卫）；回代串行链 = S5-C-5 | 无新候选 |
| `bandit.ts`（130 行） | recordReward/recordExploration 拷贝追加 = X4-2 readonly 契约；`arms.includes` / meanReward 线性 = S1-C-9（arms 个位数）；selectArm 贪心循环 arm-0 重评（best 初始化后循环仍从 0 起，严格不等式下重评不改选择）= S1-C-9 本体 ns 级，非新站点；validateTaskFeatures includes = S1-C-9 族 | 无新候选 |
| `shadow.ts`（177 行） | decisions 追加拷贝 = X4-2 本体；halted / breached once-per-fault 路径 = R16-C/R18-C 拒绝类；roundUsd/预算扣减 µs 域 = S1-C-9 族 | 无新候选 |

结构位置 × 消除机制满矩阵（死码/常量折叠/CSE/记忆化/数据结构/
融合/早退/免分配/复杂度类/强度削减/惰性/平行路径/形状单态化）
逐格过筛：非空格全部落在上表既有 ID 或 §2.2 两项新记录上。

### 2.2 本轮新换名检查记录（裁决后拒列，不铸 ID）

1. **posterior `lnGamma` 反射分支死码消除**（posterior.ts L118
   `if (z < 0.5) return Math.log(Math.PI / Math.sin(Math.PI * z)) -
   lnGamma(1 - z)`——在锚定生产池内 z ≥ 1 恒成立故分支零流量，
   消除之可省一次每调用比较）：**非死码**——`PosteriorConfig` 的
   priorAlpha/priorBeta 经公开 `R1Input.config`（`Partial<
   PosteriorConfig>`）可取 (0, 0.5) 内 schema 合法值，且公开导出
   `betaQuantileLcb` 仅守 `alpha <= 0 || beta <= 0`，分数先验直达
   反射分支 ⇒ 消除是**公开面 fail-open**（S8-H-3 / S9-D-4 同类
   角落反例结构：默认态死、schema 合法态活）。量级第二重拒绝：
   站点住在 posterior 零池（R31-C 通量普查 Math.log 全切片
   169 次/报告 ⇒ lnGamma 调用每报告数十次，整函数 µs 域），
   X3-4（lnGamma Lanczos 提升无可测收益）同域亚带。双重拒绝，
   拒列。
2. **fitLogitAdditive 头部退化结局双 `every` 扫描融合**
   （offline-logit.ts L428 `baseRows.every((r) => r.y === 0) ||
   baseRows.every((r) => r.y === 1)` 改单遍双旗标扫描）：等价
   成立（纯读谓词无副作用，短路只改访问次数不可观察，布尔结果
   逐位同）——但站点 once-per-report、n=400 元素读，上界亚 µs，
   离 ±35 ms 带 4–5 个数量级；且 S7-C-2 已在同文件实证 V8 对
   builtin 谓词的特化使手写循环替换 `every` 呈负效应，融合形
   预期为零或负。draw 侧同形站点（L481 `sample.every`，×200/
   报告）**是 S2-C-5 本体**（bootstrap 采样循环融合塌缩检查，
   亚噪声已拒），头部站点为其 once-per-report 严格弱化格。双重
   拒绝，拒列。

### 2.3 枚举收口

除 §2.2 两项外，第三十三遍逐文件重读**零未点名站点**。R12-C
四分类（重命名 / 亚带池 / 逐位违例 / 结构交换负优化）+ R18-C
once-per-fault 拒绝类第三十三遍无余项。**第三十三组枚举为空——
比强铸败者 ID 更诚实。**

## 3. 三大池地板与组合出路复核

- **APCX 池**（59 非截距名 × 400 行 APC 扫掠 × 201 fit）：无
  计时器独立道 0.306 / 0.343 / 0.315 / 0.337 ms/fit ⇒ 61.5 /
  68.9 / 63.3 / 67.8 ms/报告——正典带 63.3–68.9，较 R32-C 正典带
  60.2–61.7 同主机窗口同向抬升（判读见 §1）。
- **FLOORX 池**（S8-C-2 支撑升序 off-sum 形态，从未落地——X2-1
  引理前提本轮复核成立；逐名值对 APCX 道 `Object.is` 断言全绿）：
  0.178 / 0.185 / 0.187 / 0.201 ms/fit ⇒ 35.8 / 37.2 / 37.6 /
  40.4 ms/报告地板。
- **组合天花板 = APCX − FLOORX = 25.7 / 31.7 / 25.7 / 27.3 ms
  < 35**——正典带 25.7–31.7 与 R32-C 正典带 20.2–26.6 交叠
  25.7–26.6、与 R30-C 正典带 18.8–26.6 带缘相接；正典 1 的 31.7
  是主机抬升 APCX 的读数（同窗 FLOORX 未等幅抬升所致），距 35
  带下缘 3.3 ms，四读数全部 < 35：组合出路维持关闭。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is`）——与 R14-C/R16-C..R32-C 打印值同值，派单强制的
  跨轮确定性旁证成立。
- 零池备忘维持：posterior/propensity/bandit/shadow 四文件全部
  站点在既有 ID 或无号探针覆盖下，本轮重读新增两项换名记录
  （§2.2）后无余项。

## 4. 姊妹面划界（含两次快进带入的 R33-D / R33-B；计数即止，零剂量零计时）

1. **R33-D 空枚举（本轮第一次快进带入）**——D 区元闭合 35 行
   坐标表独立重推 + 静态哨兵卫生（未铸新面）。与 C 切片零共享
   物理对象（其 BINDX/REPRX/codec 计数全在 `src/adaptation/`
   树内）；其对 R32-A 哨兵方法的卫生性应用先例与本轮 §0 处置
   一致。声明即止，不移植、不重做、不铸新轴。
2. **R33-B 空枚举（本轮第二次快进带入）**——B 区第三十三遍从零
   重枚举（10 文件 1359 行，基线 `94ed3d9` 四验 0 行）+ 恰一处
   站点级换名记录（`ROLE_FAMILY[role] ?? "unknown"` 死回退消除，
   三重关拒列不铸 ID）+ 天花板重锚 M=2 10.75–11.73 / M=10
   21.48–23.74 ms/eval 落主机偏置窗族（W1/W2/W3 行为证人全过，
   偏置归宿主）。与 C 切片零共享物理对象（其 10 文件全在在线
   路由面，与本切片零交集）；其主机偏置判读与本轮 §1 诚实注记
   互为独立 VM 旁证（读数不互抄）。声明即止，不移植、不重做、
   不铸新轴。
3. **R33-A 空枚举 + A 账本 44/44 三腿卫生**——按令是方法应用非
   新面。本轮**不**在 C 区重演 44 行式账本分类：C 账本（X\* +
   S\*-C-\* 全系）的 K 腿（契约/语义/身份）由 0 行冻结与调用面
   最强冻结携带，M 腿（量级）由本轮 ×4 锚点重锚携带（各池上界
   不增；主机抬升只影响绝对刻度不影响池占比），E 腿（引擎实测
   符号）由引擎同一性携带（本轮 canonical 引擎与做出全部 C 区
   实测符号裁决的引擎位级相同：Node v22.22.2 / V8
   12.4.254.21-node.39）——该携带在既有验证流程内自然完成，
   不另立名目、不铸「我也查了一遍账本」式段落。
4. **BINDX（R31-C）**——切片环境内建 token 静态普查（划界计数，
   非剂量）：`Math.*` 恰 **42 token**，其中函数内建 12 个
   （abs 9 / max 7 / log 6 / min 3 / floor 3 / exp 3 / sqrt 2 /
   imul 2 / sin 1 / round 1 / pow 1 / ceil 1）+ 值常量
   `Math.PI` ×3；`Number.isFinite` 恰 **2 站点**（offline-logit
   L339 `beta.every` + lin-alg L85 终检）——与 R31-C/R32-C
   十二内建 + 两站点普查逐名同构，realm 坐标未动、零 patch、
   零计时。C1 capture-hoist 不重提。
5. **REPRX（R30-C）**——切片 `JSON.parse` 输入位点 **0**（全部
   输入经内存对象抵达）——划界维持，声明即止。
6. **MODEX（R31-J）**——9 文件对 `node:fs / readFile / writeFile /
   chmod / umask / mkdir / appendFile` 命中 **0**——切片数据路径
   零持久工件、零铸盘动词，面在 C **结构性空**（R31-E codec 面
   同一计数同一结论）。声明即止。
7. **R32-A 重开条件哨兵审计**——方法不重铸：本轮不产出「重开
   条件清单再验证」式段落；C 档案在案重开条件（S12-C-1 无重开
   条件、S6-C-3/4 支撑分布与因子规模、X2-1 引理前提）凡可执行
   者已由锚点保真电池与 FLOORX `Object.is` 断言在 §1/§3 顺带
   见证，凡要求 src/ 变更者由 0 行冻结判空——不另立审计面、
   不另开断言网格。
8. **R31-I CLI 进程边界门扉闭合审计**——本切片零 OS 进程边界
   腿（无 argv/env/stdin/stdout/spawn/signal/exit），审计平面
   正交零交集，不移植。

## 5. 测试与验证

```
# 聚焦离线路由套件（tsx --test，7 文件）
# 在 f0a9d97 / d9a4e30 / 8a29f3a 三 tip 各跑一次：
#   tests 59 / suites 10 / pass 59 / fail 0  （三次逐字同计数，
#   与 R31-C/R32-C 同计数）

# 全门禁 pnpm gate = typecheck + lint + test + build
# 在 f0a9d97 / d9a4e30 / 8a29f3a 三 tip 各跑一次，均 exit 0：
#   tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#   （三次逐字同计数，与 R32-C/R33-A/R33-B/R33-D 同计数；
#    tsc build 零诊断）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门；
# f0a9d97 工作树，nvm Node v22.22.2）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项（design.build 358,006/拟合）
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项（eta dots 3,586,400 → 1,249,652 去重）
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点（两次通过，检查数两次同为 6,193）：
#   production 663.9 ms = 1.12× vs 冻结 S6-C ctl 740.9（完整块）；
#   另一次通过打印 production 665.4 ms = 1.14×（ctl 同窗抬升）。
# 与 §1 生产正典中位带互证：比率与 R28-C/R29-C/R31-C/R32-C
# （1.12×）/R30-C（1.13×）逐轮基本不动，绝对抬升归宿主。

# 本轮测量装具（临时脚本，未入库——无生产赢家；来源与指纹见附录）
# 主锚 ×4：11,072 项位级检查全绿（2,768 × 4）；
#          sink/floorSink 8/8 = 7.309（进程内 Object.is）
```

冻结四验（§0）；仓库变更 = 本报告一个文件（提交前
`git diff origin/cursor/sota-persistent-opt-83a1...HEAD --stat`
复核仅 `docs/reports/sota-opt/round-33/R33-C.md`）。无生产落地
（派单条件句未触发），未铸新仿真脚本，临时装具已从 /tmp 删除。

## 6. 新增排除 ID：无

- 不铸 S33-C-\*：第三十三组枚举为空；§2.2 两项换名检查分别落
  公开面 fail-open + X3-4 零池与 once-per-report 亚带 + S7-C-2/
  S2-C-5 族，均不越 ±35 ms 带，无任何站点达标。
- 不写入 EXCLUSIONS.md 任何行；不铸新 face 名；不取 ALLCAPS
  绰号（含对 MODEX / BINDX / REPRX 的一切再编号）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态，未重开 S12-C-1
  （重开条件：无），未起 S7-C 平行实现，未重铸 R32-A 哨兵审计
  方法，未重演 R33-A 账本分类、未重走 R32-B 闭合稳定性审计。

## 附录：测量装具来源与指纹（未入库；运行后已删除）

- 装具：`/tmp/r33c/r33c-anchor.mts`，**814 行**，由本仓
  `docs/reports/sota-opt/round-31/R31-C.md` 附录 A.1 代码块
  （L555–L1368）逐字节提取，唯一改动为头部块注释 6 行更新为
  R33-C 说明（协议体、夹具、seed、断言零改动——R31-C A.1 本身
  即 R30-C/R14-C 血统逐字节协议；提取后 diff 复核恰 6 行注释）。
- sha256（归档时点）：
  `d15cef646e127af1b06691ffcfaf2cfbdf1128410366c3cc0e5c9a3d3f1b4925`
- 运行方式（仓库根，nvm Node v22.22.2 + tsx 4.23.12，
  `pnpm install --frozen-lockfile` 后，PATH 前置
  `~/.nvm/versions/node/v22.22.2/bin` 绕开系统 Node v22.14.0）：

```bash
node_modules/.bin/tsx /tmp/r33c/r33c-anchor.mts   # ×4（1 预演 + 3 正典，独立进程）
```

- 输出摘要（四运行）：§1 表格与逐 rep 行为全量记录；每运行尾行
  `ALL EQUIVALENCE CHECKS PASSED (2768 bitwise checks)`。
- 本轮**零新增计时臂**：无绑定/表示/组成/基片/模式剂量，无决斗
  道，无输家脚本——附录无新增源码块（装具全文已在 R31-C 附录
  A.1 归档，本轮仅头注差异，不重复占档）。

MORE_OPTIMA=no
BRANCH=cursor/r33-c-offline-routing-thirty-third-pass-83a1
