MODEL_SLUG=claude-fable-5-thinking-xhigh

# R33-B：live 路由切片 Round 33 三十三搜报告

**战役:** 全库持久 SOTA 优化 Round 33 / R33-B（十区之一，R1-B…R32-B 的第三十三遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `f0a9d97`（派单 tip，R33-A 摄取 + 本 B/C/D 波派单）→ 提交前 rebase 至 **`d9a4e30`**（R33-D 摄取 + R33-E 派单——**纯 docs 快进**，`git diff f0a9d97..d9a4e30 -- src/ package.json pnpm-lock.yaml` = **0 行**）。测量窗口内 exclusive 零快进（分支创建时、测量后两次 `git fetch origin cursor/sota-persistent-opt-83a1` 均确认 tip 仍为 `f0a9d97`），全部测量、聚焦套件与 `pnpm gate` 在 `f0a9d97` 的工作树上完成；提交前第三次 fetch 发现快进 `f0a9d97 → d9a4e30`，按令 rebase 至新 tip、第四点复核 10 文件冻结（0 行、合计 1359）、通读 R33-D 并将其轴并入不移植清单（§0）——src 树两 tip 逐字节同一，测量效力不受影响。
**分支:** `cursor/r33-b-live-routing-thirty-third-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R28-B…R32-B 及 R33-A 引擎位级相同；`/exec-daemon` PATH 暴露的系统 Node v22.14.0 已显式绕开——PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 后 `node --version` 复核 v22.22.2，三次官方运行首行均打印 `engine node=v22.22.2`，且仿真内置 L0 引擎同一性断言 ×3 全过；**零官方批次落在 v22.14.0 上**，无需弃批。tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**诚实空枚举：无达门槛的新更优解，切片零代码改动，未铸任何 S33-B-\* ID、未铸任何新 face 名或 ALLCAPS 外号，EXCLUSIONS.md 零增行；R4-B…R32-B 的整片成本天花板经第三十次复测成立（×3 独立运行），配置态 × 命令类矩阵全六格 + prior 双档格全部复测仍在历史窗内。** 第三十三遍透镜按令**不**重走 R31-B 面格闭合审计（六扇停驻门维持）、**不**重走 R32-B 闭合稳定性审计（54/54 三腿裁决与 D1–D5 残门为停驻记录，「再查一遍闭合稳定性」明令非面）、**不**移植 R32-A 重开条件哨兵审计与 R33-A A 账本 44/44 方法卫生——改做**从零独立重枚举**：重读 10 文件全量 1359 行，重走结构位置 × 消除机制满矩阵（§2d/§4），对每个枚举出的角度执行换名拒绝检查。结果：全部角度落在既有排除 ID 本体、B 区自有封禁面、姊妹区归档面（明令不移植）或 R31-B 停驻门成员上；**恰一处从未在任何 B 报告中作为裁决站点点名过的新站点级换名记录**（`familyOf` 终端 `ROLE_FAMILY[role] ?? "unknown"` 死回退消除，§2d 记录 1）经三重关拒列不铸 ID。派单硬保持项显式重取证：**S12-B-2 重开条件腿 (a) 未触发**（`eval-routing.ts` 全文 `prior` 计数 = 0，:353–354 只传 learned，`assignTasks` 生产调用方图景三处与 R30-B…R32-B 逐条一致无新增）；**S13-B-1 重开条件未触发**（`learning/learned-routing.ts` 对 `94ed3d9` 逐字节 diff = 0 行，本体成本无变化，维持 loser ID）。

关键前提事实：切片 10 个文件（清单逐一核对恰为 10，`wc -l` 合计 **1359** 行，逐文件计数见 §0，与 R32-B 逐数一致）自 R1-B 裁决基线 `94ed3d9` 以来**逐字节零变化**（`git diff 94ed3d9..HEAD -- <切片 10 文件>` 为 0 行，分支创建时、测量后、提交前与 rebase 至 `d9a4e30` 后四点核对；测量窗口内 exclusive 零快进，提交前快进为纯 docs，四点覆盖全程）。R1-B 的结构下界论证、R4-B 的聚合天花板论证与 S1-B-\* … S13-B-1 全部裁决对当前代码原样成立。

天花板本轮实测复核为 **10.75–11.73 ms/eval（M=2）/ 21.48–23.74 ms/eval（M=10）**（×3 独立运行）。本 VM 相对 R32-B canonical 窗（9.04–9.62 / 17.84–18.43）大 N 格整体偏慢 +12~+29%，落在 R31-B 已归档的主机偏置窗族（9.25–11.46 / 21.02–23.17）邻域（M=2 run 1 的 11.73 超 R31-B 上缘 +2.4%、M=10 run 3 的 23.74 超 +2.5%），偏置方向全格一致；小 N 格与单价格保持带内或邻接：live face 49.1–52.3 µs（R14-B 带 47.1–50.4，run 2 的 52.3 +3.8% 同主机方向）、级联探针 plan 531–584 / decide 332–346 ns（decide 侧 332–333 落于历史带 321–345，346 邻接 +0.3%，与 R32-B 同现象）、单拾取 19762–20283 ns（历史带 19.5–21.8 µs 之内）。**回归不可能成立**：切片对 `94ed3d9` 逐字节零 diff（三点冻结），任何 Δ 只能来自主机；W1（双跑逐字节恒等）/ W2（learned 臂仅在 avoid/prefer 命中族上分歧）/ W3（prior 拾取存在性）三组行为证人 ×3 运行全过，语义与 R30-B…R32-B 恒等——R32-B 已裁定该窗族为 VM 特有主机效应，本轮为其第二实例，非漂移趋势。结构性结论逐条成立：全 18 个矩阵格 ×3 运行保持全序 M=2 < M=7 < M=10；learned ≥ no-learned 在 **17/18** 格对成立，唯一例外 run 3 M=10 对（22.27 vs 22.30 ms/eval，learned 侧反快 13.4 µs/调用 = 0.12%，深居 ±5% 抖动带内，同对在 runs 1–2 正常成立且 W1/W2 证人全过——诚实记为主机抖动，非结构翻转）；全部生产规模格低于 30 ms 落地线（holds-below-landing-line=true ×3）；replay-faithful **7.64–8.21 ms/eval**（R22-B 带 6.93–7.10 邻接 +8~+16% 同主机偏置方向，与 R31-B 的 7.37–10.45 交叠）；10× 压力 **125.3–138.0 ms/eval**（历史并集带 94.8–140.9 之内）。配置态矩阵：M=7 configured+learned **17.41–18.57 ms/eval**、M=10 stress+learned **22.25–22.91 ms/eval**。prior 双档格逐格复现（第二十二次跨 VM）：假设 N=2000 档 M=2/7/10 = 36.79–46.47 / 54.05–58.61 / 81.27–88.71 ms/eval（仍为「若有流量即越线」邻接配置，今日流量仍为零）；真实档 live face M=2+prior 192.9–195.3 µs（R32-B 带 189.2–191.8 邻接 +0.6~+1.8%）、M=7+prior+learned 295.8–307.8 µs/批（与 R30-B 带 290.1–296.9 及 R31-B 带 296.5–385.4 相交）。落地线（数十~数百 ms 或复杂度类下降）在现有生产流量下依旧不可达。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- **切片恰 10 文件**（逐文件 `wc -l`）：`r0.ts` 173 / `assign.ts` 129 / `assign-plan.ts` 68 / `policy.ts` 160 / `live-cascade.ts` 118 / `live-selection.ts` 61 / `analyze-task.ts` 108 / `primary-catalog.ts` 101 / `catalog-model.ts` 83 / `supervisor/model-router.ts` 358，合计 1359 行，与 R32-B 逐数一致。`catalog-observed.ts` **不在**本切片（按令未纳入、未触碰、未计时）。
- **冻结四点核对全过**：分支创建时（tip `f0a9d97`）`git diff 94ed3d9..HEAD -- <切片 10 文件>` = **0 行**；测量后复核 0 行；提交前第三验 0 行；rebase 至 `d9a4e30` 后第四验 0 行（`wc -l` 复核合计仍 1359）。`package.json` / `pnpm-lock.yaml` vs `94ed3d9` 亦 **0 行**（rebase 前后各一次）。最后一次生产 live-B 变更维持 `94ed3d9`；最后一次 `src/routing/` 生产变更维持 `183df9b`（S7-C，离线 C 区）。
- **读序（按令完成）**：README → PROGRESS → EXCLUSIONS 全表（545 行，S1-\* … S13-B-1 + X 继承行全部实读）→ round-33 PLAN 全文（B 段 + 具名/不具名禁令）→ round-32 PLAN → R32-B 全文（默认对照带 + 闭合稳定性审计停驻边界）→ R33-A 全文（引擎绕道方法 + 方法卫生边界）→ R31-B 全文（面格六停驻门属主标尺）→ 10 切片文件全量实读（1359 行）。
- **摄取状态**：测量窗口内独占分支零快进（两次 fetch 均 `f0a9d97`）；提交前 fetch 发现纯 docs 快进 `f0a9d97 → d9a4e30`（R33-D 摄取 + R33-E 派单），按令 rebase、复核冻结、通读 R33-D 全文并将其轴并入下条不移植清单；按令未等待 C/E——其自行返回并由父代摄取，姊妹分支零触碰。
- **不移植 / 不重铸清单维持**（枚举时逐条遵守，本轮零触碰）：R30-B scale-cube second-order interaction face（含多任务并发规模立方 R21-B…R29-B 族）、**SCALEX / ORDX / AGEX / YMIX / SHAPEK / REPRX / LINKTOP / BINDX / MODEX** 及全部具名禁令（FITQ、NAMESHAPE、XPROC、SCHEDWIN、KFAN、NVG、SEEDX、TERMCLASS、CKMIX、CALLB、OCCX、JB、RUNLIM、VALCLASS、CG、COTARG、PRICEX、RTL、DELINV、PROBX、RVL、SIDEC、PWL、ZREP、PAIRX、CSPELL）；**R31-B 面格六停驻门**（catalog-row hidden-class / IC-shape、cascade evidence×failure-class lattice、assignTasks cold-onset segmentation、batch class-composition mix、LINKTOP-in-B、prior alias-resolution pathway——全部维持停驻，零重开）；**R32-B 闭合稳定性审计**（54/54 三腿裁决与五扇残门 D1–D5 为停驻记录，本轮**不**重走、**不**铸「再查一遍闭合稳定性」式新面——本报告对 S12-B-2/S13-B-1 的重取证系派单硬保持项明令要求，非腿分类重演）；**R32-A 重开条件哨兵审计（F12a–d）**（A 区方法记录，不移植不重铸）；**R33-A 空枚举**（A 账本 44/44 为方法卫生非新面，不移植）；**R33-D 空枚举**（提交前 rebase 后通读并入：D 区元闭合审计 35 行独立重推 + D 自档案哨兵卫生应用 + 三项站点级换名记录——promote 链三算哈希 / mkdir 每 eval 重付 / `RollbackLog.list()` 防御拷贝——全部为 `src/adaptation/` 文件站点，与本切片零共享物理对象，不移植不重铸；其 S12-B-2 条件陆器哨兵与本报告 §2c 的独立重取证同判未触发，两侧互为佐证不互抄）；R31-A injected-callable 停驻门（R31-B §0 已证本切片公共签名零函数值参数穿缝，本轮 10 文件重读复核仍然成立——全部 `=>` 均为数组组合子内部 lambda）；R31-E I/O text-codec topology（切片 `node:fs` 引入计数 0，面结构性空）；R31-C BINDX（切片 primitive 调 `Math.*` 是同一坐标，不当轴用）；R31-I CLI 进程边界门扉闭合审计；R32-J 六残余坐标（本切片零 fs 位点，无物理对象）。
- **B 区自有封禁面不补铸**：R29-B learned-policy container face / R28-B catalog-face row-arrangement (permutation) face / R27-B batch-cardinality bottom + prelude intercept face / R26-B privacy / provider-policy constraint-leg face / R25-B task-face optional-field presence topology / R24-B M=1 catalog collapse / R20-B objective 长度 L 轴 + K×W 双线性轴 / R19-B 比较器第三腿 + 目录基数 M 轴。**目录行预排序仍是行为变更**（R28-B W3 裁定行序为契约决策载体），本轮未尝试。
- **硬不变量**：切片零代码改动 ⇒ Live=R0 等价（R1 未接线 live；稀疏回退 approved R0 baseline）、双 LCB（Beta+正态）、双归因（logit+prob-add）、proposal-first（Checkpoint F-PROD 开放，ADR-005，不声称 Outcome-supported）、阈值、CAS/凭证/数据面契约/公共签名全部原样保持；live 面不 import R1/bandit/shadow 由 `live-isolation.test.ts` 在聚焦 198 项中复证通过。分析未变更任何在飞 run；tracking 无命令权；H/score 不写 routing PASS/FAIL。无 fsync / 完整性重哈希移除；fail-open 家族保持关闭；未改任何测试。S5-H-1 / S7-F-1 ≠ S6-F-5 / S8-I-1 / S8-E-1 / S9-D-4 / S12-D-1 全部切片外硬保持项零触碰。npm POSIX `.bin` symlink 静默 no-op 未当优化修。
- **EXCLUSIONS.md 未增行**（空枚举记录不入排除表，plan-ban 归属父代）；仓库变更恰为本报告单文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核仅 `docs/reports/sota-opt/round-33/R33-B.md`）。禁止开 PR，未开。

## 1. 第三十三遍搜索方法：从零独立重枚举

R31-B 已证面格闭合（六扇门停驻）、R32-B 已证闭合稳定（54/54 裁决无一重开、五扇残门关闭）——两者均为停驻记录，照抄即空转。第三十三遍据此回到枚举原点：**假装排除表不存在，从零重读 1359 行找出每一个理论上可疑的消除/重构角度，然后逐一对表执行换名拒绝检查**（词根不同 ≠ 面不同；站点不同但同族同向 = 换位点记录不铸 ID）。方法四步：

1. **生产调用方图景重取证**——`rg 'assignTasks\(' src/` 恰 3 个调用方（`cli/main.ts:365` smartChildPlan live face N≤30、`adaptation/eval-routing.ts:353–354` 只传 learned、`track/primary-split.ts:41` C 小批），与 R30-B…R32-B 报告逐条一致，无新调用方、无新可选参数；`assignOne` src 内零生产调用方维持（唯一引用是 `test/unit/routing/assign-plan.test.ts` 等价锁测试）；`effectiveConfidenceThreshold` / `routeTask` / `routeFlowNode` 在 src 内仍只有 `supervisor/flowchart.ts:34–36` barrel 再导出、零消费者（R12-B 零流量取证第三次转移成立）；`routeR0` 唯一调用方仍是 `r1-shadow-report.ts:88`（shadow-only）；`applyCascade` 唯一调用方仍是 `cascade-evidence.ts:102/:108`，而 `applyEvidenceCascade` src 内零调用方（R5-B「生产不可达」维持）；live 级联生产调用方仍是 `track/loop.ts:120`、`cli/main.ts:377`（plan）与 `run/child-coordinator.ts:468/:475`（evidence+decide），每子任务一次。
2. **结构位置 × 消除机制满矩阵**——10 文件 × 13 类消除机制（死码/常量折叠/CSE 去重/记忆化/数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/形状单态化）逐格过筛（§4 按文件汇总）。
3. **换名拒绝检查**——非空格逐一对表；从未被任何 B 报告点名过的站点才允许作新站点级记录（本轮恰 1 项，§2d 记录 1），其余全部命中既有 ID 本体或站点已点名的裁决。
4. **测量侧不铸新剂量网格**——只重锚既有格 ×3 独立运行 + W1/W2/W3 行为证人 + L0 引擎同一性断言（引擎绕道方法承自 R33-A：PATH 前置 nvm bin 绕开 `/exec-daemon` 系统 Node v22.14.0）；harness 为 R32-B 附录 A 的逐行衍生（删 D1 观察探针——该门已关不再复测；增 L0；快照 id 重标），增量见附录 A。

## 2. 天花板复测：R4-B…R32-B 收口第三十次复核成立

引擎 Node v22.22.2 / V8 12.4.254.21-node.39（canonical，L0 断言 ×3）。语料种子 0xb44b01 逐字节复用（R4-B 起原封）。×3 独立进程运行，全部 ALL CHECKS PASSED。单位 ms/eval（×2 调用口径，与 R4-B…R32-B 同）：

| 格 | run 1 | run 2 | run 3 | 历史参照 |
|---|---|---|---|---|
| eval-replay N=2000 M=2 | 11.73 | 10.79 | 10.75 | R32-B 9.04–9.62 / R31-B 主机窗 9.25–11.46（runs 2–3 窗内；run 1 +2.4%） |
| eval-replay N=2000 M=10 | 23.36 | 21.48 | 23.74 | R32-B 17.84–18.43 / R31-B 主机窗 21.02–23.17（runs 1–2 窗内；run 3 +2.5%） |
| analyzeTask 份额 | 2.37 (20%) | 2.30 (21%) | 2.32 (22%) | 恒 ~20–25% |
| replay-faithful M=2 | 8.21 | 7.66 | 7.64 | R22-B 6.93–7.10 邻接；R31-B 7.37–10.45 交叠 |
| 10× 压力 N=20000 M=2 | 132.5 | 138.0 | 125.3 | 历史并集 94.8–140.9 之内 |
| CLI live face N=30 M=2（µs/调用） | 49.1 | 52.3 | 49.8 | R14-B 47.1–50.4（runs 1/3 带内；run 2 +3.8%） |

M=2 全序、M=10 全序、analyzeTask 份额、压力格与 live face 的结构关系逐运行成立；`holds-below-landing-line=true` ×3（M=10 最大 23.74 < 30）。**主机窗定位**：本 VM 大 N 格 +12~+29% 偏慢、方向全格一致，落 R31-B 已归档主机偏置窗族邻域——R32-B 已裁定该窗族为 VM 特有主机效应，本轮为第二实例；W1/W2/W3 行为证人 ×3 全过 + 冻结 0 行 ⇒ 偏置全额归主机，无回归、无行为漂移，天花板收口成立。

## 2b. 配置态 × 命令类矩阵：全六格复测（第二十四次全格复核）

replay N=2000，ms/eval（×2 口径）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| M=2 default, no learned | 7.75 | 7.94 | 7.75 |
| M=2 default, learned | 8.03 | 8.48 | 8.32 |
| M=7 configured, no learned | 15.21 | 16.66 | 16.41 |
| M=7 configured, learned | 17.66 | 18.57 | 17.41 |
| M=10 stress, no learned | 20.29 | 20.78 | 22.30 |
| M=10 stress, learned | 22.91 | 22.25 | 22.27 |

全 18 格保持 M=2 < M=7 < M=10 全序 ×3。learned ≥ no-learned 在 **17/18** 格对成立；**诚实注记**：run 3 M=10 对以 11136.3 vs 11149.7 µs/调用反转（learned 侧反快 0.12% = 13.4 µs/调用），深居 ±5% 主机抖动带内，同对在 runs 1–2 以 +2.62 / +1.47 ms/eval 正常成立，W1（该格双跑逐字节恒等）与 W2（learned 臂仅 review/plan 族分歧）×3 全过——learned 臂的真实增量工作在 M=10 stress 格约为 ~0.7–2.6 ms/eval 档，run 3 的反转是单读数抖动淹没差额，记为主机抖动，不构成结构翻转、不铸坐标。CLI live face M=7 configured+learned = 119.4 / 119.0 / 113.6 µs/调用（R32-B 102.8–107.2、R31-B runs 2–3 109.6–113.5——落 R31-B 主机窗邻域，同偏置方向）。级联探针：`liveCascadePlanFromAssignment` 531–584 ns（历史带 526–577，run 1 的 584 邻接 +1.2%）、`decideLiveCascade` 332–346 ns（332–333 落于历史带 321–345，346 邻接 +0.3%，与 R32-B 同现象）。

## 2c. prior 双档格复测（第二十二次跨 VM；S12-B-2 重开腿 (a) 复核）

真实快照 56 行（R12-B 起逐字节复用，id 重标 `pps_r33b_realistic_v1`）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| 假设档 N=2000 M=2 + prior（ms/eval） | 36.79 | 46.47 | 39.12 |
| 假设档 N=2000 M=7 + prior | 54.05 | 58.61 | 56.26 |
| 假设档 N=2000 M=10 + prior | 81.27 | 83.37 | 88.71 |
| 真实档 live face N=30 M=2+prior（µs/调用） | 195.3 | 192.9 | 193.0 |
| 真实档 N=30 M=7+prior+learned（µs/调用） | 297.9 | 307.8 | 295.8 |
| 单拾取 pickFromPublicPrior（ns） | 19762 | 20283 | 19789 |

假设档三格仍全部越线（>30 ms/eval）——维持「若有流量即越线」邻接配置归档；生产流量本轮重新取证仍为零：`rg -c 'prior' src/adaptation/eval-routing.ts` = 0 匹配（rg exit 1），:353–354 `assignTasks({ catalog, tasks, learned: … })` 只传 learned。**S12-B-2 腿 (a) 未触发**，条件式落地物维持在架，按令不补铸不重扫（R21-B 快照形状轴维持关闭）。W3（三种 M 下 family=edit 拾取存在）×3 全过。真实档 M=7+prior+learned 295.8–307.8 与 R30-B 带 290.1–296.9 及 R31-B 带 296.5–385.4 相交；单拾取 19762–20283 ns 落历史带 19.5–21.8 µs 之内。

## 2d. 第三十三遍枚举：换名拒绝检查记录

从零满矩阵扫描（§4）枚举出的全部角度经对表逐一裁决。**本轮恰 1 项新站点级换名记录**（从未在任何 B 报告中作为裁决站点点名——历史命中仅为附录 harness 中 `familyOf` 复刻体的字面出现，非裁决）：

1. **`familyOf` 终端 `ROLE_FAMILY[role] ?? "unknown"` 死回退消除**（analyze-task.ts:93）。类型域内可证死：`ROLE_FAMILY` 是覆盖闭合 `AgentRole` 联合的全函数 Record，familyOf 的 `role` 形参类型为 `AgentRole` ⇒ 查找永不 undefined，`?? "unknown"` 永不触发。拒列三重关：(i) **运行时 fail-closed 卫**——`AssignableTask.role` 只有类型标注、assign 路径零运行时校验，畸形调用方值直达该查找，删除回退把畸形输出从 `"unknown"`（合法 TaskFamily 兜底）变为 `undefined` 穿透下游 family 消费者（learned avoid/prefer 匹配、prior 拾取、决策记录 family 字段）= fail-open 家族（S9-D-3 零成本防御纵深先例同族）；(ii) **量级**——单终端分支 ~ns，familyOf 住在 analyzeTask 份额内（本轮 2.30–2.37 ms/eval ≈ 20–22%），该份额整体即便归零也低于落地线一个量级；(iii) L 轴与七正则链本体分别归 R20-B（线性收口）与 S4-B-1。拒列，不铸 ID。

其余枚举角度全部落在既有裁决**本体**上（非换位点，无需新记录，代表性清单）：`validateConfig` ROLES.includes / roles Set = R2-B S1-D-8 类一次性构造 + S10-B-3 同族；`assignTasks` 目录 id 双建 = S7-B-6 本体；`planAssignmentPolicy` 双排序拷贝取极值 = S8-B-1 本体；`assignPlanned` 两次防御拷贝 = S8-B-4 / S2-B-1 本体、options 三条件 spread = S9-B-3 / S4-B-4 本体；route 输入骨架 = S5-B-4 / S3-B-6 / S4-B-5 族；`validateInput` 字面量数组 = S1-B-7、per-id trim 卫 = S9-B-2；unknown-model 预扫描下沉 = S9-B-4；per-route allowed Set = S12-B-1（双向封死）；主循环融合 = S11-B-1（镜像封死）；`evaluateLiveCandidate` 空前置失败别名 = S7-B-3、per-model request 字面量 = R1-B §5 契约裁决（fixedCostUsd/fixedLatencyMs 所需）；`toModelDescriptor` 每 (任务,模型) 构造 = R1-B §4.4 架构裁决（三通道分别撞 X1-1 / 公开签名 / 每批预计算已被 R14-B/R15-B 复核维持）；`buildDecision` 构造份额 = R32-B D1 普查（19/20 字段活、池上界 0.64–0.65 ms/eval）；justification 预计算 = S7-B-4；approvalPlan 享元 = S6-B-2；one-hot 载体形 = R32-B D4 三重关；`coldStartRoutingScore` 钳位 = S10-B-2；空 avoid/prefer 跳过 learned = S6-B-4；`cheapFirstTiers` 装饰-排序-还原 / localeCompare / spread = S3-B-2 / S3-B-3 / S1-B-5；`decideLiveCascade` find+findIndex = S1-B-4；`liveRefusalMessage` 双 .some = S8-B-2；`selectLiveModel` 比较器展开 = S8-B-3；analyze-task 正则链 / 死值谓词 / reason join / long 惰性 = S4-B-1 / S5-B-1 / S5-B-3 / S10-B-1 + S1-B-2；`routeR0` 高风险 find = S1-B-6、r0 排序输出 = R1-B 契约；`applyCascade` `{...decision}` 拷贝与 `fallbacks` filter = 生产不可达（R5-B/R9-B 维持，本轮 `applyEvidenceCascade` 零调用方重取证）；`effectiveConfidenceThreshold` candidates 数组 + `Math.max` spread 与 `routeTask`/`routeFlowNode` 包装对象 = R12-B 零流量不铸维持（本轮 barrel census 重证）+ Math.\* 绑定属 BINDX 坐标声明即止；throw 位点 = R17-B 拒绝路径已锚 + R31-F 分解属地（R32-B D2 维持）。

## 3. 候选总表

**空。** 铸 ID 数：**0**。铸 face 名 / ALLCAPS 外号数：**0**。EXCLUSIONS.md 增行数：**0**。本轮唯一新产出为 §2d 记录 1（站点级换名记录，拒列归档备后续轮引用）与三点哨兵重取证（S12-B-2 腿 (a) / S13-B-1 / 调用方图景），均不构成候选。空枚举与 R8-B…R32-B 同为战役允许的诚实结局。

## 4. 结构位置 × 消除机制满矩阵扫描（第三十三遍，按文件汇总）

| 文件 | 本轮新视角检查 | 结论 |
|---|---|---|
| `r0.ts` | `applyCascade` 生产不可达第三十三次重取证（`applyEvidenceCascade` src 零调用方）；routeR0 唯一调用方 r1-shadow-report:88（shadow-only）；S1-B-6/S2-B-3/S3-B-4/S4-B-3 维持；排序输出即契约 | 无候选 |
| `assign.ts` | 调用方图景三处重取证一致（§1）；S8-B-4/S2-B-1 双拷贝、S9-B-3/S4-B-4 options、S3-B-1 批内记忆化、X3-1 维持；S13-B-1 重开门未触发；`flowchartRoleForAgentRole` 属 graph 区（跨切片只记录） | 无候选 |
| `assign-plan.ts` | S8-B-1 维持；`pickPreferredModel` prior 层 = S12-B-2 条件陆器在架未触发；`pickFromPublicPrior` 本体属 `public-prior.ts`（切片外） | 无候选 |
| `policy.ts` | per-model request 字面量 = R1-B 契约裁决；S7-B-3 别名护栏维持；预算/期限 toFixed 串仅失败路径 = R17-B 已锚；COMPLEXITY_RANK 常数查找无池 | 无候选 |
| `live-cascade.ts` | S1-B-4/S1-B-5/S3-B-2/S3-B-3 维持；byId Map 每 plan 一次 M≤10 ns 档；探针 531–584 / 332–346 ns 复锚（§2b） | 无候选 |
| `live-selection.ts` | S8-B-3/S8-B-2 维持；`compareLiveCandidates` Number() 布尔差分已是无分支形态；总序契约（R19-B 第三腿封禁）零触碰 | 无候选 |
| `analyze-task.ts` | **§2d 记录 1**（ROLE_FAMILY 死回退，拒列）；S1-B-1/S1-B-2/S4-B-1/S5-B-1/S5-B-3/S6-B-3/S7-B-1/S10-B-1 维持；份额 20–22% 复锚同带 | 无候选 |
| `primary-catalog.ts` | 单点构造器消灭形状多态（R31-B C1 停驻维持）；modelFor/inferFastId 每配置一次 ns；throw 位点归 R32-B D2 | 无候选 |
| `catalog-model.ts` | `toModelDescriptor` R1-B §4.4 架构裁决维持；oneHotDistribution = R32-B D4 维持；条件 spread 属性存在性可观察（S1-C-10 类）维持 | 无候选 |
| `supervisor/model-router.ts` | `effectiveConfidenceThreshold`/`routeTask`/`routeFlowNode` 零流量重取证（barrel 三行再导出、零消费者）；S12-B-1/S11-B-1 镜像对封死维持；S1-B-7/S9-B-2/S9-B-4/S5-B-4/S10-B-2/S10-B-3/S7-B-6/S6-B-2/S6-B-4/S6-B-5/S7-B-4/S8-B-4 维持；buildDecision = R32-B D1 普查维持（19/20 活）；`createModelRouter` 每 run 一次 CLI 噪声档 | 无候选 |

## 5. 关键裁决细节

- **S12-B-2 换名检查**：假设档 N=2000 三格重测越线依旧（§2c），腿 (a) 重新取证未触发（eval-routing 零 prior、调用方图景三处未变、无新可选参数）。未以任何新名重扫快照形状轴。
- **S13-B-1 换名检查**：`git diff 94ed3d9..HEAD -- src/learning/learned-routing.ts` = 0 行，本体成本无变化，重开条件（applyLearnedRouting 本体成本增 ≥2 个量级；|avoid|≈965–1,063 生产写入方不可达）未触发。learned 容器坐标归 R29-B（封禁），本轮未触碰。
- **目录预排序**：R28-B W3 裁定行序为契约决策载体，预排序 = 行为变更，本轮未尝试。
- **主机窗口**：本 VM 大 N 格 +12~+29% 偏慢，为 R31-B 主机偏置窗族第二实例（R32-B 已裁定 VM 特有）；护栏为 (a) 三点冻结 0 行、(b) W1 双跑逐字节恒等 ×3、(c) W2/W3 语义证人 ×3、(d) 序结构（格间全序、份额占比、越线格集合）与 R30-B…R32-B 完全同构。run 3 M=10 learned 对的 0.12% 反转（§2b 诚实注记）在该护栏下裁定为单读数抖动。
- **引擎纪律**：三次官方运行全部在 nvm Node v22.22.2 产出（L0 断言 + 每次运行前 `node --version` 复核）；系统 Node v22.14.0 上零官方批次，无弃批。

## 6. 前后对比与测试

切片零代码改动，无前后对比。验证（全部在 Node v22.22.2）：

- 聚焦 live 路由单测：`npx tsx --test "test/unit/routing/*.test.ts"` —— **198/198 pass**（18 suites，含 live-isolation / cascade-evidence / failure-class / assign / assign-plan / analyze-task / r0 / live-cascade / live-selection / catalog-eligibility 等），与 R31-B / R32-B 计数逐字相同。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0** —— 1169 项测试 1168 pass / 0 fail / 1 skipped（既有跳过项，非本轮引入），tsc build 通过；计数与 R9-B…R32-B 及 R33-A 逐字相同。
- 仿真 ×3 独立运行 ALL CHECKS PASSED（L0 + W1/W2/W3 逐运行全过）。
- 提交前最终 fetch：独占分支快进 `f0a9d97 → d9a4e30`（R33-D 摄取，纯 docs——`git diff f0a9d97..d9a4e30 -- src/ package.json pnpm-lock.yaml` = 0 行），按令 rebase 至 `d9a4e30`；冻结第四验 0 行；`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 恰为本报告一个新文件。
- 无赢家故 scripts/ 零新增；harness 未入库（增量说明见附录 A，指纹见附录 B，临时文件按纪律核后删除）。

## 7. 新增排除 ID

无。未铸任何 S33-B-\* ID；EXCLUSIONS.md 零增行（§2d 记录 1 为站点级换名拒列记录，归档备后续轮引用，无需并入排除表；plan-ban 归属父代）。

## 附录 A：本轮驱动增量说明

驱动 `/tmp/r33b-sim.mts` 整体为 R32-B 附录 A 脚本的逐行衍生（§0 天花板 / §0b 矩阵 + W1/W2 / §0c prior + W3 三段逻辑零改动），本轮改动仅：

1. 头注释更新为 R33-B 三十三遍测量说明（面格/闭合稳定性审计均为停驻记录不重走）；
2. **删除 R32-B §0d D1 观察式探针块**（决策记录构造份额之门已由 R32-B 普查决定性关闭，本轮不复测不再剂量）；
3. **新增 L0 引擎同一性断言**（不计时；引擎绕道纪律承自 R33-A）：

```typescript
check(
  "L0 engine identity: node v22.22.2 (canonical; system v22.14.0 batches must be discarded)",
  process.version === "v22.22.2",
  process.version
);
```

4. 快照 id 重标 `pps_r33b_realistic_v1`；十模目录 policyVersion 重标 `sim-r33b`。

语料种子 0xb44b01、配置态目录（M=7 alias 行）、真实 prior 快照 56 行、全部 bench 口径与 reps 逐字节同 R32-B 附录 A。

## 附录 B：驱动指纹与运行方式

- 驱动：`/tmp/r33b-sim.mts`，455 行，sha256 `7682fdf9d880fb022326f8e84bfe115bf448f8b07a41e7c8a0a7e7e759514686`（未入库；临时文件已删除）。
- 运行（repo 根，空闲 VM；PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 绕开 `/exec-daemon` 的系统 Node v22.14.0）：

```bash
npx tsx /tmp/r33b-sim.mts   # 独立进程 ×3 = runs 1–3
```

---

**MORE_OPTIMA=no** —— 第三十三遍从零重枚举空收口：天花板 ×3 复锚落 R31-B 主机偏置窗族邻域（M=2 10.75–11.73 / M=10 21.48–23.74 ms/eval，freeze 0 行，W1/W2/W3 证人全过 ⇒ 偏置归主机无回归）；全 18 矩阵格全序成立（learned 对 17/18，唯一 0.12% 反转诚实记为抖动）；prior 双档格 + 单拾取带内；S12-B-2 腿 (a) 与 S13-B-1 重开门显式重取证未触发；满矩阵扫描全部角度命中既有裁决，恰 1 项新站点级换名记录（ROLE_FAMILY 死回退）三重关拒列；不铸 ID、不铸面、不增排除行；生产代码零改动，聚焦 198/198 + `pnpm gate` exit 0（1169/1168/0/1 计数逐字同）。

MORE_OPTIMA=no
BRANCH=cursor/r33-b-live-routing-thirty-third-pass-83a1
