MODEL_SLUG=claude-fable-5-thinking-xhigh

# R33-G：runtime/graph/domain 切片 Round 33 三十三搜报告

**战役:** 全库持久 SOTA 优化 Round 33 / R33-G（十区之一，R1-G…R32-G 的第三十三遍）
**基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `8d31c34`（派单 tip，R33-C 摄取 + 本次派单）；工作期间独占分支 docs-only 快进**一笔**（提交前 fetch 所见）：`8d31c34` → `f163cde`（R33-E 空枚举摄取 + R33-H 派单）。增量核对：`git diff --stat 8d31c34..f163cde` 仅触 `docs/reports/sota-opt/` 下 3 文件（PROGRESS / round-33 PLAN / R33-E.md），对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接复核 **0 行**；按令 rebase 报告分支至 `f163cde`（纯快进零冲突），42 文件冻结在新 tip 复验仍 **0 行**；R33-E 结论（空枚举，零新姊妹轴）已读并补入非移植清单（§0）。全部锚点与测量在 `8d31c34` 工作树完成——快进增量 `src/` 逐字节零变化 ⇒ 测量形态与提交形态位级同一，全部有效；聚焦套件 + `pnpm gate` 在新 tip `f163cde` 复跑一次全绿同计数。R33-F / R33-H 在飞，按令未等待、未触碰。
**分支:** `cursor/r33-g-runtime-thirty-third-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2**（R13-G…R32-G 同引擎线，带可比；tsx 经仓内 node_modules，pnpm 10.17.1，`pnpm install --frozen-lockfile` 干净完成；本轮 PATH 在任何计时前钉死 v22.22.2，**零** v22.14.0 误跑批次需弃置）。

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S33-G-\* ID，不铸新 face 名、不取绰号、不加 EXCLUSIONS 行，切片零代码改动。** 第三十三遍从零重读 42 文件（`wc -l` 合计 **8960** 行与 R32-G 记录一致，清单核对恰 42 文件：run 17 + supervisor 4 + graph 4 + domain 17），逐文件枚举的全部消除角度经换名检查裁决为既有排除 ID 本体、姊妹归档面（禁移植）、plan-ban 轴名或全局计算顶之内的亚噪声——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（§3）。本轮最有信息量的单点：新鲜读码**独立重推导**出 `failurePathCompletedGraph` 共享 `visiting` 在 true 返回路径不清除节点、跨 `failedIds.every` 根携带历史的性质——换名检查裁决其为 **S2-G-2 本体**（R2-G 已录链式恢复 b→a→C 反例：任何「显然的」去重/每根新鲜 visiting 变体翻转可观察终态 FAILED↔COMPLETED，排除表 199 行原文在位）——第三十三遍新鲜眼从代码本身复推出第 2 轮排除行本体，是排除表覆盖真实坐标的直接证据，不作新记录申报（§3.0）。R31-G 的 18 扇聚合门闭合审计按令不重铸。本轮边际动作仅两类，均不构成新面：(a) 缺省预算重锚（计算顶 **0.302–0.309 ms/run**，较 R32-G 带 0.289–0.293 高侧 ~3–6%，逐腿归因宿主干扰窗，§1 诚实注记；I/O 地板 **90.4–104.4 ms/run**，支配比 **~299–338×**，两个数量级支配关系不变）；(b) G 档案在案零剂量停放与重开条件的**不计时静态哨兵复核**（§2——方法属 R32-A 首创并已 plan-ban 重铸，此处按令仅作卫生性应用，不申报为新轴），全部哨兵**未触发**。14 格矩阵摘要 `06cbcf92c098c8f0…` 官方引擎三批 × 进程内 3 迭代 = **9 次逐位一致——第二十四轮同值，按令不铸摘要恒等 ID**。空枚举与 R12-G…R32-G 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 42 文件自生产基线 `4efee23` 逐字节零变化（派单 tip / 快进 rebase 后 / 提交前三点核对 `git diff 4efee23 -- <42 文件>` 均 **0 行**）；最强冻结 `git diff 89f0455..HEAD -- src/` **完全为空**（R10 以来全部提交均 docs，快进一笔亦纯 docs）。聚焦套件 run/supervisor/graph/domain + integration：**tests 386 / pass 385 / fail 0 / skipped 1** 全绿（`8d31c34` 与新 tip `f163cde` 树各一次，逐字同计数，v22.22.2，与 R31-G / R32-G 记录一致）；`pnpm gate`（typecheck + lint + test + build）**全绿两次**（两 tip 各一次）——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A / R32-G / R33-A…R33-E 记录逐字同计数），退出码 0。不开 PR。

## 0. 范围与约束遵守（含不移植清单）

- 切片：`src/run/`（除 A 属地 `child-tracking.ts` / `gate-apply.ts`）17 文件 + `src/supervisor/`（除 B 属地 `model-router.ts`）4 文件 + `src/graph/` 4 文件 + `src/domain/` 17 文件，恰 **42**，全部实际重读全文，未依赖前三十二轮记忆。
- 读序遵令：README → PROGRESS → EXCLUSIONS 全表 → round-33/PLAN → round-32/PLAN → R32-G → R33-A → R33-B → R33-C → R33-D → R31-G，之后才读 42 切片文件并动手；快进后补读 R33-E.md 全文（仅为划界与非移植清单，不据其改动任何测量或结论）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README.md 零触碰。
- **本轮五姊妹空枚举划界（零新轴可移植）**：R33-A（A-ledger 44/44 稳定性卫生审计——A 属地方法与对象，G 侧零共享）；R33-B（`analyze-task.ts` 死 ROLE_FAMILY 回退换名记录已拒——B 属地物理对象，G 切片零 import）；R33-C（`lnGamma` 反射死支 + `fitLogitAdditive` every 融合两项换名检查已拒——C 属地 stats 对象，G 切片零共享）；R33-D（哈希去重 / mkdir 重付 / 回滚日志防御拷贝三项换名记录已拒——D 属地物理对象，G 侧零交集）；R33-E（快进带入，learning 空枚举，零新站点级换名记录——候选角度全为 E 已点名站点复命中，全部 E 属地物理对象；G 切片对 learning 的唯一共享坐标为 `flowchart-run.ts` 的 learned-routing 消费腿——Live=R0 不变量 + S8-E-1 不去重 + E/B 属地边界，零剂量，本清单原有）。五报告均未铸新面，非移植清单无需加行。
- **不移植禁令逐项核对**：M1 亲代 run 子调度格（R30-G）、M2 监督引擎裁决格（R29-G）、decision-gate resolution（R28-G）、join-policy（R27-G）、**RUNLIM / CKMIX / TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / ZREP**——本轮零剂量零重提；**BINDX**（切片 `Math.*` token 恰 **6 个**：hash.ts `Math.abs` ×1、inspection.ts `Math.max` ×3、flowchart-supervisor.ts `Math.max` ×2，与 R32-G 计数逐一同址，计数即止零 patch）；**MODEX**（切片 grep **零** chmod / 零 umask，计数即止）；I/O text-codec topology / per-run transcode-byte manifest（R31-E——切片显式 fs UTF-8 位点恰 **4 个**：checkpoint-store.ts:22/:31、pause-controller.ts:31/:92，与 R32-G 记录逐一同址；event-store / episode-store 经切片外 `persist/jsonl.ts` 走同一坐标——E 属地，本轮零编解码剂量）；R31-G 18 门闭合审计（**不重铸「我又审了一遍门」**；摘要恒等第二十四轮不铸 ID）；R32-A 重开条件哨兵审计与 R32-B 闭合稳定性审计（**方法不重铸为面**——§2 按令仅应用于 G 自档案在案条目，不申报为新轴）；R31-B 六停驻门（cascade 升级臂证据 × 失败类格维持 routing 属地停放）；R31-E/F/H/I 各属地格全额维持。FITQ / SCALEX / YMIX / KFAN / NVG / SEEDX / SHAPEK / ORDX / CALLB / OCCX / JB / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / PAIRX / CSPELL / REPRX / LINKTOP 全部未编号未移植。
- **站位零剂量停放不重铸**：`planTaskTopology` X0-2（读码复核仍无 run loop 调用方，§2 哨兵第三十三次）；`settleSupervisedOutcome` A 属地 no-op（内部调用方 supervisor.ts:601/:711 均不带 assessment，无 assessment 即无门原形在位，§2 哨兵）。
- 未重开任何 X\* / S1-\* … S32-\* 条目。**单变量纪律**：本轮无任何计时新臂——重锚脚本与 R12-G 附录归档件**逐字节同一**（§1、附录），哨兵全部为静态 grep / 读码断言（不计时）。硬不变量零触碰：双 LCB + 双归因在位（切片侧证人：events.ts `isBehaviorDistribution` + flowchart-snapshot.ts `isOneHotDistribution` one-hot 校验原文在位）；Live = R0-equivalent（flowchart-run 的 learned 路由应用面未触碰，B 属地 model-router 未读改）；promotion proposal-first / 无 Outcome-supported 声称（Checkpoint F-PROD 仍开放，ADR-005）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不变（零 diff 天然满足）；fsync / 完整性再哈希在位（event-store 终态 fsync、checkpoint-store `handle.sync()`、pause-controller writeAtomic、episode-store 终态 fsync 全部原文在位）；fail-open 家族保持关闭（events.ts `validateEvent` 追加/重放双侧 fail-closed 原形）；S8-E-1 不去重 loadLearnedRouting；分析不变更在飞 run；tracking 无指挥权；H/score 不写 routing PASS/FAIL。无赢家 ⇒ 零生产改动、零新仿真文件入库。不开 PR。

## 1. CPU vs I/O 重锚（三批次官方运行，nvm Node v22.22.2，seed 仅标注批次）

归档基准从 `docs/reports/sota-opt/round-12/R12-G.md` 原样提取（```ts 围栏内 536 行 → `tmp-r33g-bench.ts`，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`，与 R13-G…R32-G 附录 A 记录值**逐字节一致**；无赢家按纪律不入库，临时文件核后已删除）。三批 seed = 0xa33091 / 0xa33092 / 0xa33093，全部在 `8d31c34` 工作树完成（快进增量纯 docs，两 tip `src/` 逐字节相同 ⇒ 测量形态与提交形态位级同一，全部有效）。

| 探针 | B1 | B2 | B3 |
| --- | --- | --- | --- |
| validateFlowchart 16n join4 | 9.663 µs | 9.432 µs | 9.510 µs |
| → ×32 轮计算顶 | **0.309 ms/run** | **0.302 ms/run** | **0.304 ms/run** |
| DeterministicJudge.decide 8×8 | 0.148 µs | 0.201 µs | 0.154 µs |
| expandTaskTransition | 0.509 µs | 0.491 µs | 0.509 µs |
| compileChildrenToFlowchart 16 子 | 9.420 µs | 8.886 µs | 8.909 µs |
| validateEvent AGENT_EVENT | 0.572 µs | 0.564 µs | 0.562 µs |
| replayRun 300 events | 0.776 µs | 0.653 µs | 0.652 µs |
| validateCheckpoint N=48 含 restore | 299.2 µs | 255.0 µs | 252.1 µs |
| CheckpointStore.write 持久（N=48） | 694.4 µs | 514.3 µs | 642.6 µs |
| → ×66 写/run | 45.8 ms | 33.9 ms | 42.4 ms |
| EventStore.append（无 fsync） | 73.0 µs | 69.2 µs | 66.9 µs |
| → ×300 事件/run | 21.9 ms | 20.8 ms | 20.1 ms |
| EventStore.readAll ×96/run 中期均幅 | 36.7 ms | 35.7 ms | 32.8 ms |
| EpisodeStore.append COMPLETED（fsync） | 298.8 µs | 364.1 µs | 302.8 µs |
| settle-close 终态命令算术 | 0.500 ms | 0.552 ms | 0.493 ms |

计算顶 = **0.309 / 0.302 / 0.304 ms/run**；I/O 地板合成 = **104.4 / 90.4 / 95.3 ms/run**；支配比 **~299–338×**（R32-G ~304–324× / R31-G ~316–363× 同为两个数量级，支配结论不变）。矩阵摘要 `06cbcf92c098c8f0…` 3 批 × 3 迭代 = **9 次逐位一致（第二十四轮同值；14 格配置态 × 命令类矩阵逐格 I/O 计数零变化，不铸摘要轴/ID）**；start→COMPLETED 首跑冷墙钟 19.5 / 23.1 / 17.6 ms，事件尾 [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED] 不变。

**诚实注记**：(i) 计算顶 0.302–0.309 高于 R32-G 带上沿 0.293 约 3–6%（validateFlowchart 单价 9.43–9.66 µs vs R32-G 9.02–9.15 µs），伴随 validateCheckpoint B1 299.2 µs（R32-G 251.7–254.9）与 DeterministicJudge B2 0.201 µs（R32-G 0.146–0.147）两个孤立高点——同轮 R33-B / R33-C 亦各自记录宿主偏置/干扰窗，且冻结 0 行 + 摘要第二十四轮恒等 ⇒ 判宿主代际方差，不作发现；计算顶仍距 ≥10 ms 落地线两个数量级，亚噪声拒收门本轮按实测顶 ~0.30–0.31 ms 执行，结论与 ~0.29 ms 门完全同向。(ii) I/O 地板上沿 104.4 高出 R32-G 带上沿（93.5）——逐腿归因于检查点持久写 B1 694.4 µs 回到 R31-G 曾录高侧带（730.7/716.8 µs，宿主代际偏移族），B2 514.3 µs 仍在历史带内 ⇒ 宿主方差，支配结论不变。(iii) 本轮 PATH 在任何计时前钉死 nvm v22.22.2，全部批次官方引擎完成，**零弃置批次**（对照 R32-G 曾有三批 v22.14.0 误跑弃置）；另有一次同引擎同 seed 的显示性冒烟运行（输出未全量存档，摘要同为 `06cbcf92c098c8f0` ×3），按纪律不计入官方三批。

## 2. G 档案在案零剂量停放 / 重开条件哨兵复核（全部不计时；方法应用，不铸面）

R32-A 首创的哨兵审计方法（plan-ban 重铸为面）此处按令仅作卫生性应用于 **G 自档案**的在案条目。全部哨兵为静态 grep / 读码断言，零计时臂：

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| X0-2（`planTaskTopology`） | run loop 或任何生产文件获得调用方 | `rg "planTaskTopology" src/` 除 supervisor.ts 自身外**空匹配**（第三十三次复核）⇒ **未触发** |
| `settleSupervisedOutcome` A 属地 no-op | 生产侧带 assessment 调用出现 | 调用方仅 supervisor.ts:601/:711（均不带 assessment）；函数原形 assessment 缺席即返回 ⇒ **未触发** |
| R30-G M1 格站位前提 | `startParentRun` 获得 CLI 调用方或 resume 门 | `rg "startParentRun" src/` 除 coordinator.ts 外**空匹配** ⇒ **未触发** |
| R29-G M2 格站位前提 | `startSupervisedRun` 获得 CLI 调用方 | CLI 仅动态 import `resumeSupervisedRun`（main.ts:1021，与历轮记录一致）；`startSupervisedRun` 仍无 CLI 调用方 ⇒ **未触发** |
| 摘要恒等（不铸 ID） | 14 格矩阵任一格 I/O 计数或摘要变化 | 官方三批 9 次逐位同值 `06cbcf92c098c8f0…`（第二十四轮）⇒ **未触发** |
| R31-B cascade 停放门（G 侧宿主） | child-coordinator cascade 臂获得新证据面 | 冻结 0 行 + `maybeCascadeRetry` 原形（:382 调用 / :458 定义）读码复核 ⇒ **未触发**（routing 属地维持） |
| R31-E 编解码坐标（G 侧四位点） | G 存储腿新增编解码剂量 | 显式 utf8 位点恰 4 个与 R32-G 记录逐一同址，零剂量 ⇒ **未触发** |
| BINDX / MODEX 计数哨兵 | 切片 `Math.*` token 数或 chmod/umask 位点变化 | `Math.*` 恰 6 token 逐一同址；chmod/umask **0** 匹配 ⇒ **未触发** |
| 签名/调用图类重开条件全体 | 要求 `src/` 变更 | 最强冻结 `89f0455..HEAD -- src/` **0 行**直接判空 ⇒ **全部未触发** |

## 3. 本轮换名检查与四十二文件检查表（第三十三遍枚举残余）

### 3.0 换名检查三项（全部命中既有属主，非新记录，零候选）

1. **`flowchart-supervisor.ts` `failurePathCompletedGraph` 共享 `visiting` true-路径保留**：新鲜读码独立注意到 `handled` 在 true 返回路径不执行 `visiting.delete`，跨 `failedIds.every` 根携带历史——链式恢复下后续根直接判 false。换名检查 → **S2-G-2 本体**（R2-G 反例 b→a→C 已录：现行共享 visiting 语义是可观察终态契约的一部分，任何去重/每根新鲜 visiting 变体翻转 FAILED↔COMPLETED，公共 API 可达；EXCLUSIONS 199 行原文在位）。第三十三遍新鲜眼从代码复推出第 2 轮排除行本体 = 排除表覆盖真实坐标的直接证据。零剂量、零申报、不重开。
2. **`scheduler.ts` `planRound` 死形参 `_leaseDurationMs`**：换名检查 → **R29-G 落选候选清单本体**（公开导出签名硬禁 + ns 级，R29-G 原文已拒）。非新站点，不重提。
3. **`domain/ids.ts` `createId`/`isId` 的 `prefix === undefined` 防御支 + 前缀/正则双查**：TS 类型下不可达但对未类型化 JS 调用方是 fail-closed 面。换名检查 → 校验平面 VALCLASS 类既有裁决 + **S3-G-5**（isId 前缀提升 ~33 ns/查已排除）+ R26-D fail-closed 纵深族（S6-D-1 同族）。零剂量失败路径，非新站点。

### 3.1 `src/run/`（17 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| coordinator.ts | M0 缺省路径 300-append I/O 地板本体（排除表 G 族行）；M1 `startParentRun` = R30-G 格（站位哨兵未触发，§2）；`toModelRoutedPayload` 路由载荷映射 = 双归因见证不可动 | 无新候选 |
| supervisor.ts | M2 裁决格全维度 = R29-G；`planTaskTopology` X0-2 停放（§2）；`settleSupervisedOutcome` A 属地 no-op（§2）；`reconstructSupervisorState` 重放腿 = RUNLIM/CKMIX 既有属主 | 无新候选 |
| flowchart-run.ts | 每轮全量 readAll 放大 = S1-G-1/S2-J-11/S3-I-6/S6-G-1/S6-G-7 排除行；检查点节奏 = CKMIX；learned 路由应用 = Live=R0 不变量 + B 属地边界（S8-E-1 不去重）；pauseIfRequested = pause token 已锚 | 无新候选 |
| child-coordinator.ts | ConcurrencyGate FIFO 微结构 = 计算顶内亚噪声（R31-G 门）；重试/超时梯 = M1/M2 格已定价；cascade 升级臂 = R31-B 停放（§2 哨兵） | 无新候选 |
| event-store.ts | 终态 fsync = 硬不变量禁去；追加/读双侧 `validateEvent` = fail-closed 读校验契约行 | 无新候选 |
| checkpoint-store.ts | 临时文件 + rename + sync 原子写 = 持久契约；记录组成 = CKMIX；utf8 位点 = R31-E 坐标（§2） | 无新候选 |
| events.ts | EVENT_TYPES 闭表 + validateEvent = 校验平面既有属主；`routingContextFields` / `isBehaviorDistribution` = 双归因 one-hot 见证 | 无新候选 |
| replay.ts | replayRun 0.65–0.78 µs/300 事件（亚噪声）；materializeCheckpoint / validateCheckpoint = CKMIX + S8-G-2 写侧校验 | 无新候选 |
| child-grounding.ts | 包预算/`uniqueArtifacts` 去重一次性 µs 级；context packet 编译 = context 姊妹切片属地 | 无新候选 |
| child-prompt.ts | formatChildPrompt 串接已定价（R30-G [A]）；串形 = NAMESHAPE 禁 | 无新候选 |
| episode-bind.ts | settle-close 终态算术 0.493–0.552 ms 本轮带内已锚（含 `events.some` 三次 O(n) 终态扫描——聚合已定价，融合收益亚噪声）；closure 裁决 = episode 姊妹属地；`hash32` 集中化 = 既有落地 | 无新候选 |
| episode-store.ts | 队列化追加 + 终态 fsync = 硬不变量；readAll 校验 = jsonl 读契约（E 属地坐标） | 无新候选 |
| flowchart-executor.ts | 终态结果投影 = TERMCLASS 禁；薄执行器臂 = R31-G 门（真实支配成本是 executor 秒–分级真跑） | 无新候选 |
| injection.ts | 注入指令种类/校验 = R25-G 面全维度；`unexpectedKeys` 每调 Set 构造 = 校验平面亚噪声（once-per-CLI-inject） | 无新候选 |
| inspection.ts | once-per-run CLI 分析腿 = 拒收类（一次性 CLI 噪声）；findChild 回退线扫（含 Array.from 物化）亚噪声 | 无新候选 |
| pause-controller.ts | writeAtomic fsync = 硬不变量；pretty-print 序列化形态 = pause.json 数据面契约；token 20.0–22.9 / 55.4–59.8 µs 带内已锚 | 无新候选 |
| scheduler.ts | LeaseRegistry / planRound / applyTaskOutcome = R29-G 格 + SCHEDWIN 禁；`_leaseDurationMs` 死形参 = R29-G 落选候选本体（§3.0-2）；expandTaskTransition 0.491–0.509 µs 亚噪声 | 无新候选 |

### 3.2 `src/supervisor/`（4 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| flowchart-supervisor.ts | `propagate()` 不动点 + edgeStatus/joinStatus = join-policy 面（R27-G）；leaseReadyNodes 逐节点 computeStatus 重算 = R28-G 决策门格 + 计算顶内亚噪声；`failurePathCompletedGraph` 共享 visiting = **S2-G-2 本体**（§3.0-1）；structuredClone 防御拷贝面 = R31-G 门；waiter 不变式 = 契约 | 无新候选 |
| flowchart-snapshot.ts | fail-closed 快照结构校验 = CKMIX + S8-G-2 属主（本轮 validateCheckpoint 252–299 µs 含其全量）；`isOneHotDistribution` 每决策 Set 构造 = 双归因见证内亚噪声；`snapshotValidationRouter` throw-only 桩零剂量 | 无新候选 |
| flowchart.ts | 纯再导出垫片，零运行时；模块形 = NAMESHAPE 禁 | 无新候选 |
| ledger.ts | advanceLedgerRound 数组 spread 记账 + 双段 isDuplicateFact 线扫 = 计算顶内亚噪声（R31-G 门；facts 个位数规模） | 无新候选 |

### 3.3 `src/graph/`（4 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| validate.ts | Kahn 拓扑一次性构造（16 节点 µs 级，queue.shift 微形态含于内）；确定序 = ORDX 禁 | 无新候选 |
| readiness.ts | computeReadyTasks 线扫 = R29-G 格；与 planRound 的近重复形 = 调度格属地（去重是重构非优化，零真尺度收益） | 无新候选 |
| judge.ts | DeterministicJudge 0.148–0.201 µs（亚噪声地板级；B2 高点为宿主，§1(i)） | 无新候选 |
| compile-children.ts | compileChildrenToFlowchart 8.9–9.4 µs 一次性已锚；`--children` 编译面历轮已裁 | 无新候选 |

### 3.4 `src/domain/`（17 文件）

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| flowchart.ts | validateFlowchart 9.4–9.7 µs ×32 = 计算顶本体（本轮高侧宿主归因 §1(i)）；条件/join 校验数组字面量 includes = 校验平面既有属主；approval plan/reply 三段校验 = 权威面契约 | 无新候选 |
| state.ts | expandTaskTransition BFS 已定价亚噪声（全表预计算变体 = TERMCLASS 禁 + 亚噪声双拒）；转移表 = TERMCLASS 禁 | 无新候选 |
| ids.ts | 前缀 + 正则双查 + undefined 防御支 = 校验平面（VALCLASS 类 + S3-G-5 + R26-D fail-closed 族，§3.0-3）；branded 类型零运行时 | 无新候选 |
| timestamp.ts | ISO_PATTERN + Date.parse 双查 = 校验平面既有属主 | 无新候选 |
| hash.ts | hash32 集中化 = 既有落地本体（BINDX 计数即止，§0） | 无新候选 |
| task.ts / run.ts / limits.ts / project.ts / evidence.ts / episode.ts / contract.ts | 校验器族 = 校验平面既有排除行；错误串构造仅失败路径 | 无新候选 |
| status.ts / roles.ts / record.ts / errors.ts / index.ts | 闭枚举表 / type-only / 再导出 = 零剂量或亚噪声 | 无新候选 |

## 4. 候选总表

**空。** 本轮零候选：哨兵复核（§2）按定义不产出候选（不计时、不剂量）；换名检查三项（§3.0）全部命中既有属主（S2-G-2 / R29-G 落选清单 / VALCLASS+S3-G-5 族）——无一是新站点；四十二文件枚举（§3.1–3.4）全部命中既有 ID 本体、姊妹归档面、plan-ban 轴名或计算顶内亚噪声。本轮无任何计时新臂 ⇒ 无可定价候选面。全局支配结构不变：计算面全体封顶 0.309 ms/run，距 ≥10 ms 落地线两个数量级；I/O 面三腿 20.1–45.8 ms 全部契约或排除表保护。

## 5. CHECKS

- 分支恰 `cursor/r33-g-runtime-thirty-third-pass-83a1`，基线 `8d31c34` → 快进 rebase `f163cde`（一笔纯 docs，核对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 0 行）。
- 冻结核对：`git diff 4efee23 -- <42 文件>` 派单 tip / 快进 rebase 后 / 提交前均 **0 行**；最强冻结 `git diff 89f0455..HEAD -- src/` **0 行**。
- 切片清单：恰 42 文件（17+4+4+17），`wc -l` 合计 8960 行（与 R32-G 记录一致），全部实读。
- 重锚脚本与 R12-G 附录归档件 sha256 逐字符同一（`9d46a829…b9943`，536 行）；官方三批全部 nvm v22.22.2；本轮零 v22.14.0 误跑（PATH 计时前钉死）。
- 摘要恒等：`06cbcf92c098c8f0…` 官方 9 次逐位一致（第二十四轮同值，不铸 ID）；事件尾不变。
- 哨兵普查（第三十三次）：X0-2 / A 属地 no-op / M1 / M2 站位、cascade 停放、编解码坐标 4 位点、MODEX 零位点、BINDX 6 token 逐一同址——全部未触发（§2）。
- 换名检查三项全部命中既有属主（§3.0）：S2-G-2 独立复推导（排除表覆盖直证）、R29-G 落选清单、VALCLASS/S3-G-5/R26-D 族——零新记录、零重开。
- 姊妹划界（快进带入）：R33-E 零新站点级换名记录、全部 E 属地对象；G↔E 唯一共享坐标为 flowchart-run.ts learned-routing 消费腿（S8-E-1 + Live=R0，本清单原有，零剂量）。
- 聚焦测试：run/supervisor/graph/domain + integration = **386 tests / 385 pass / 0 fail / 1 skipped**（`8d31c34` 与 `f163cde` 树各一次，v22.22.2，与 R31-G / R32-G 逐字同计数）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿两次**（两 tip 各一次）——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A / R32-G / R33-A…R33-E 逐字同计数）；退出码 0。
- 仓库改动恰本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；Live=R0-equivalent 在位；proposal-first 在位；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不变；fsync / 完整性族在位；fail-open 家族保持关闭；S5-H-1 保持；S7-F-1 ≠ S6-F-5 维持；S8-I-1 文件级封锁维持；S8-E-1 不去重维持；分析未变更任何在飞 run；不开 PR。

## 6. 重开条件

本轮无新定价轴，不加行。X0-2、A 属地 no-op、R30-G/R29-G 站位前提、R31-B cascade 停放、R31-E 编解码坐标、CKMIX/S8-G-2、S2-G-2 等既有重开条件全额维持原文；本轮 §2 哨兵直证全部未触发。计算顶高侧 0.302–0.309 与 I/O 上沿 104.4（§1 注记 i/ii）不构成重开触发——逐腿归因宿主代际方差、冻结 0 行、摘要第二十四轮恒等。

## 7. 新增排除 ID

**无。** 空枚举轮不铸 S33-G-\* ID；不取绰号、不作轴名、不加 EXCLUSIONS 行。

## 附录 A：重锚基准出处与关键输出

脚本与 R12-G 附录 A 归档件**逐字节相同**：自 `docs/reports/sota-opt/round-12/R12-G.md`（```ts 围栏内 536 行）原样提取为仓库根 `tmp-r33g-bench.ts` 运行，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`（与 R13-G…R32-G 附录 A 记录值一致，逐字节可复现；无赢家按纪律不入库，临时文件核后已删除）。运行方式：`node node_modules/tsx/dist/cli.mjs tmp-r33g-bench.ts <seed>`（nvm Node v22.22.2），三批 seed = 0xa33091/92/93（seed 仅标注批次，全部数据确定性构造）。关键输出：

```
--- Node v22.22.2 (canonical) ---
validateFlowchart 16n join4: 9.663 / 9.432 / 9.510 us -> x32 = 0.302~0.309 ms/run
validateFlowchart 8n join2: 4.804 / 5.776 / 4.594 us
DeterministicJudge.decide 8x8: 0.148 / 0.201 / 0.154 us
expandTaskTransition PENDING->FAILED: 0.509 / 0.491 / 0.509 us
compileChildrenToFlowchart 16 children: 9.420 / 8.886 / 8.909 us
validateEvent AGENT_EVENT: 0.572 / 0.564 / 0.562 us
replayRun 300 events: 0.776 / 0.653 / 0.652 us
validateCheckpoint full (N=48, incl. restore): 299.2 / 255.0 / 252.1 us
CheckpointStore.write durable (N=48): 694.4 / 514.3 / 642.6 us -> x66 = 45.8 / 33.9 / 42.4 ms/run
EventStore.append AGENT_EVENT (no fsync): 73.0 / 69.2 / 66.9 us -> x300 = 21.9 / 20.8 / 20.1 ms/run
EventStore.readAll 300/100 events: 492.8~560.0 / 189.9~204.5 us -> x96 = 36.7 / 35.7 / 32.8 ms/run
pause token() ENOENT / present: 20.0~22.9 / 55.4~59.8 us
loadLearnedRouting (no snapshot): 25.8 / 22.9 / 23.7 us
EpisodeStore.readAll (2 snapshots): 62.2 / 55.0 / 56.0 us
EpisodeStore.append COMPLETED (fsync): 298.8 / 364.1 / 302.8 us
EpisodeEventStore.append (no fsync): 65.8 / 63.5 / 67.0 us
settle-close arithmetic: 0.500 / 0.552 / 0.493 ms per terminal command
matrix digest (3 proc x 3 iter): 06cbcf92c098c8f0... 九次逐位一致（14 格；与 R10-G..R32-G 同值，第二十四轮）
start->COMPLETED wall-clock: 19.5 / 23.1 / 17.6 ms; returned events tail = [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED]
```

（引擎纪律注记：本轮 PATH 在任何计时前钉死 nvm v22.22.2，零 v22.14.0 弃置批次；另有一次同引擎同 seed 显示性冒烟运行未全量存档，摘要同值，不计入官方三批。）

---

**MORE_OPTIMA=no** —— 第三十三遍复查空枚举收口：计算顶 vs I/O 地板支配比 ~299–338× 两个数量级不变；14 格矩阵摘要第二十四轮同值；G 档案在案停放/重开哨兵全部未触发；换名检查三项全部命中既有属主（含 S2-G-2 的独立复推导直证）；42 文件逐条消除角度全部命中既有属主。无达门槛新更优解。

MORE_OPTIMA=no
BRANCH=cursor/r33-g-runtime-thirty-third-pass-83a1
