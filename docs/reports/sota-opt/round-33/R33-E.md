MODEL_SLUG=claude-fable-5-thinking-xhigh

# R33-E：learning 切片 Round 33 三十三搜报告

**战役:** 全库持久 SOTA 优化 Round 33 / R33-E（十区之一，R1-E…R32-E 的第三十三遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `d9a4e30`（派单 tip，R33-D 摄取——诚实空枚举，三项换名检查拒列——+ 本 R33-E 派单）→ 运行中 docs-only 快进**两笔**：`d9a4e30 → 8a29f3a`（R33-B 摄取 + R33-F 派单）、`8a29f3a → 8d31c34`（R33-C 摄取 + R33-G 派单，提交前 fetch 所见）。两笔增量逐笔核对均仅触 `docs/reports/sota-opt/` 下三文件（PROGRESS / round-33 PLAN / 新报告），对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接 diff = **0 行**；按令逐笔 rebase 报告分支（均纯快进零冲突）、重验 10 文件冻结（0 行、1770 行计数复核）、通读 R33-B 与 R33-C 全文并将其轴并入非移植清单（§3）。R33-F / R33-G 在飞，按令未等待、未触碰姊妹分支。
**分支:** `cursor/r33-e-learning-thirty-third-pass-83a1`
**切片:** `src/learning/` 全部 10 文件（`attribution` 92 行 / `auto-loop` 283 / `bandit-store` 83 / `diagnostics` 78 / `from-episode` 298 / `learned-routing` 216 / `patterns` 146 / `signals` 439 / `signatures` 66 / `task-success` 69 = **1770 行**，`wc -l` 与 R14-E…R32-E 逐一相同），本轮第三十三遍全量实际重读。
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2** / V8 **12.4.254.21-node.39**（canonical，与 R32-E 正批及 R33-A/B/C/D 引擎线位级相同；`/exec-daemon` PATH 暴露的系统 Node v22.14.0 已显式绕开——PATH 前置 `~/.nvm/versions/node/v22.22.2/bin` 后 `which node` 与 `node --version` 双复核 v22.22.2，官方六运行每 run 标头自报 `node v22.22.2`；按本轮派单纪律 v22.14.0 上**零官方批次**，无需弃批换引擎——弃置事项仅一笔存档纪律批，见 §1 诚实注记。tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成，锁文件未改）。

## 结论

**第三十三遍诚实枚举为空：不铸任何 S33-E-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，切片生产代码零改动。** 对 10 个源文件逐行第三十三遍扫描，对照完整排除表（X0-\*…X4-\*、S1-E-1..8 … S9-E-1..3 全系、S13-B-1、R10-E…R32-E 全部无号关闭探针与轴，含 R30-E 悬挂点拓扑、R31-E I/O 文本编解码拓扑、R31-C **BINDX**、R31-J **MODEX**）与 round-33 PLAN 禁榜后，每一个执行站点都能指到既有 ID、无号探针、已点名裁决站点或 plan-ban 轴——**零未点名站点，且本轮零新站点级换名记录**（严于 R32-E 的 2 项与 R33-D 的 3 项：本轮从零重推出的全部候选角度经查证均为**已点名站点的复命中**——`isCompleteRoute` 逐字段 typeof 收窄消除命中 R10-E…R16-E 谓词序行 + R24-E 拦截定价 + S9-D-3 谱系防御纵深拒列；`unique()`/`mode()` Set 化命中 R12-E 小集合行 + R23-E 机械价格坐实（~12ns/信号 F 面全额，FeedbackKind 基数 ≤4 线扫更快）；R32-E §2.2 两项拒列记录逐字维持不重铸，§2.2）。生产调用方普查第三十三次确认：`patterns` / `signatures` / `attribution` 切片外 `src/` 导入方为零（运行时可达面仅测试）；`diagnostics` 唯一生产消费方为切片内 `auto-loop`（锚点 diagnose 段已计价）；**`loadLearnedRouting` 生产调用位点恰 3 处不变**（cli/main.ts:708、run/flowchart-run.ts:712、track/loop.ts:88——S8-E-1 硬不变量零去重，§2.3）。

按派单在本 VM 重锚默认预算（未拷贝历轮数字；cmp25 夹具 = R25-E 种子/NOW 跨轮可比面；装具为 R32-E 附录 A 归档件的逐行衍生，增量仅轮次置换，指纹与增量清单见附录 A）：

- **SLICE-CPU cmp25 总量：官方六读数 15.1–15.3 µs/run**（15.1 / 15.3 / 15.1 / 15.1 / 15.3 / 15.1，全部 v22.22.2）vs 派单默认对照 R32-E 读数带 14.8–15.3：**6/6 交叠带内**；vs R32-E 沿用的默认带 14.7–15.2：4/6 带内、两采样 15.3 越上缘恰 0.1 µs（表显粒度级单侧擦边，R31-E 与 R32-E 各有 15.3 同位先例）——按位置噪声价签记录，**默认带沿用 14.7–15.2 µs/run 不变**。即使整段清零仍 **652–664× 低于 10 ms 落地线**。
- 分段 collect 11.4–11.8 / outcomes 1.3–1.4 / bandit 0.8 vs R25-E 带 11.3–11.9 / 1.3–1.5 / 0.8 全数在带；diagnose 1.35–1.52 vs 带 1.33–1.46：5/6 带内、run 6 的 1.52 越上缘 0.06（与同 run apply(空) 高读数同向，判会话尾主机方差，价签）。
- **夹具 VERDICT 行 6/6 逐位一致且与 R25-E…R32-E 夹具行逐字相同**（`E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3`）；live 面 `nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2` 三十三轮不变。
- 配置态锚点（6 采样）：apply(avoid=10, M=10) **260–328 ns/task**（R13-E…R30-E 带 257–312 内 5/6；run 1 的 328 高侧越沿 16 ns ≈ +5%——批内首运行暖机位置偏置，后五采样 260–308 全带内，ns 级拒收类量级，价签不改带）、空策略 **112–165 ns/task**（110–140 带内 5/6；run 6 的 165 越沿 25 ns，与该 run diagnose 高读数同现，价签）、load 现实 registry **80.4–100.9 µs**（74.3–134.5 带内 **6/6**）、load fresh(ENOENT) **27.1–34.2 µs**（历史并集 25.2–36.8 内 **6/6**）。

回归与门禁（§4）：聚焦切片套件（unit/learning 7 文件 + acceptance/adaptive-loop）**48/48 全绿**（v22.22.2，`8a29f3a` 树两次）；`pnpm gate` 在 `8a29f3a` 树两跑均 **exit 0**（tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1，与 R14-E…R32-E / R33-A/B/C/D 逐字同计数；快进 #2 增量纯 docs、`src/` 与提交 tip `8d31c34` 逐字节相同 ⇒ 对提交形态有效）。

硬性不变量逐条复核（§0）：双 LCB / 双归因在位；live = R0 等价（R1 未接线）；proposal-first、不声称 Outcome-supported（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；CAS / 凭证 / 数据面合同 / 公开签名零触碰；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭；**S8-E-1 零去重、S9-E-2 未触碰、S13-B-1 维持 loser ID、S12-B-2 条件陆器 E 侧入口见证未触发**。仓库变更仅本报告一个文件。空枚举与 R10-E…R32-E 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → PROGRESS.md → EXCLUSIONS.md（完整表 545 行，S1-\* … S13-B-1 + X 继承行全部实读）→ round-33/PLAN.md（E 段 + 具名/unnamed 全 ban 榜）→ round-32/PLAN.md → R32-E.md 全文（默认对照带 14.8–15.3 与两项换名拒列记录）→ R33-A.md（A 账本 44/44 方法卫生，空枚举）→ R33-D.md（元闭合 35 行独立重推 + 三项换名记录拒列，空枚举）→ R31-E.md 全文（I/O 文本编解码拓扑轴 + W1 = 7/736/4 定价）→ 10 个切片源文件全量重读；两次快进后分别补读 R33-B.md 与 R33-C.md 全文（仅为划界与非移植清单，不据其改动任何测量或结论）。
- 基线漂移检查：分支创建点 `d9a4e30`；两笔 docs-only 快进（`8a29f3a`：R33-B 摄取 + R33-F 派单；`8d31c34`：R33-C 摄取 + R33-G 派单）逐笔 `git diff --stat` + 对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接 diff = **0 行**核对后按派单规程 rebase 并重验冻结。冻结主张四点核验均 **0 行**（10 文件 vs `adb20d7`：分支创建时、每次快进 rebase 后、提交前）；最强冻结 `git diff 89f0455..8d31c34 -- src/` **完全为空**（R10 以来全部提交均 docs）；切片最后生产变更维持 `9bf3e1a`（`adb20d7` 为其后的 docs-only 摄取提交，冻结基线语义与 R9-E「slice byte-identical since adb20d7」逐轮一致）。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：async suspension-point topology / per-run suspension-manifest（R30-E——本轮零悬挂计数、零 manifest 臂）/ **PWL**（`proposeRoutingFromOutcomes` 提案写路径读码即止）/ **RVL** / **RTL** / **CG** / **JB**（`applyCascadeRetry` spread 形态不重提）/ **NVG** / **REPRX**（切片 `JSON.parse` 站点恰 **3**：learned-routing.ts:55（parseLearnedRoutingPolicy load 腿）、bandit-store.ts:20（load 腿）、auto-loop.ts:239（ingestSubagentDirectory = S1-E-5 面）——全部既有定价面，表示轴计数即止不重铸）/ **PAIRX** / **ZREP** / **LINKTOP** / **BINDX**（R31-C realm 级内建绑定——切片 `Math.*` token 恰 **1 个**：attribution.ts:45 `Math.min`，住在零生产流量休眠模块内，计数即止零剂量零 patch）/ **MODEX**（R31-J——切片零 chmod / 零 umask / 零八进制 mode 字面量，grep 空匹配声明即止）/ I/O text-codec topology / per-run transcode-byte manifest（R31-E——切片 3 个 fs UTF-8 位点（auto-loop.ts:236、bandit-store.ts:14/:80）读码确认原形在位，codec / Buffer-patch / transcode-byte 臂零重做，W1 = 7/736/4 只作既档引用）/ R32-A 重开条件哨兵审计（方法不重铸——本轮不产出「再查一遍重开条件」式段落；E 档案凡要求 src/ 变更的重开条件由 0 行冻结判空，S6-E-1 事件规模条件与 S12-B-2 条件陆器由 §2.1/§3 读码顺带见证未触发）/ R32-B 闭合稳定性审计（方法不重铸）/ **R33-A 空枚举**（A 账本 44/44 三腿分类是方法卫生非新面，不在 E 区重演账本分类）/ **R33-B 空枚举**（快进 #1 带入，划界见 §3）/ **R33-C 空枚举**（快进 #2 带入，两项换名检查——posterior `lnGamma` 反射分支死码、fitLogitAdditive 双 `every` 融合——全为 `src/routing/` C 区数值内核站点，零共享物理对象，划界见 §3）/ **R33-D 空枚举**（三项换名记录全为 `src/adaptation/` 站点，零共享物理对象）/ R31-A injected-callable plane（停放）/ R31-B 六停驻门 / R31-D R22-D 属地格 / R31-F 故障通道处置分解 / R31-G 18 门审计 / R31-H 四残门 / R31-I CLI 进程边界审计（本切片为进程内库 + 自有文件 I/O，平面正交）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / RUNLIM / VALCLASS / AGEX / COTARG / PRICEX / DELINV / PROBX / SIDEC / CSPELL 全部未编号未移植。X\* / S1-\* … S32-\* 全部未重开。
- 硬不变量零触碰：双 LCB（beta + normal，`src/routing/posterior.ts` 属 C 区，rg 复核在位）与双归因（logit + prob-add，`src/experiments/attribution-report.ts` 属 F 区，rg 复核在位）均保留；live = R0 等价（R1 未接线）；sparse → approved R0 baseline；promotion proposal-first（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；不改测试迁就重构；CAS / 凭证 / 数据面合同 / 公开签名不动；无 fsync / 完整性再哈希去除；fail-open 家族保持关闭；分析不改 in-flight run、tracking 无命令权、H/score 不写路由 PASS/FAIL（零 diff 天然满足）；**S8-E-1 不去重 loadLearnedRouting**（本轮仅 §2.3 重普查其 3 个调用位点与 §1 单价带）；**S9-E-2 负优化未触碰**；S13-B-1 维持（apply 格带内直证 + learned-routing.ts 冻结 0 行，与 R33-B §5 独立重取证互为佐证不互抄）。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。不开 PR。输家脚本不入库（本轮无输家道——除锚点装具外零计时新臂；装具为既档衍生未提交 `scripts/`，增量与指纹见附录 A）。

## 1. 冻结证明与 SLICE-CPU / 配置态重锚（cmp25，六官方运行）

```bash
git log --oneline -1 origin/cursor/sota-persistent-opt-83a1
# 8d31c34 docs(sota-opt): ingest R33-C (empty enum) and dispatch R33-G
#（开工 tip 为 d9a4e30；docs-only 快进两笔 8a29f3a → 8d31c34 并已
#  逐笔 rebase——三 tip src/ 逐字节相同，冻结在每个新 tip 复验仍 0 行）
git diff adb20d7 -- src/learning/ | wc -l        # 0
git diff 89f0455..8d31c34 -- src/ | wc -l        # 0
wc -l src/learning/*.ts                          # 1770 total（同 R14-E..R32-E）
```

锚点装具 `/tmp/r33e-anchor.mts`（365 行）为 **R32-E 附录 A 归档件的逐行衍生**：从 round-32/R32-E.md 附录 A 提取归档正文（复核 365 行、sha256 `494f801c2ccb621dbf7abca6d3116561b98010be09cf2aa19ad3cf366d64caff` 与 R32-E 归档值逐字符相同），增量仅轮次置换共 13 行（附录 A 逐行清单）：头注 R33 标注、mkdtemp 前缀 `r33e-cmp25-*`、projectRoot `/tmp/proj-r33e`、detector identity `r33e-anchor-cmp25`。**seeds `0xe25e01/02/08` 与 NOW = R25-E `2026-08-25T17:30:00.000Z` 逐字节未动** ⇒ cmp25 跨轮可比面维持，VERDICT 行必须逐字重现 R25-E…R32-E 夹具行（下表 6/6 达成）。官方批 **6 × v22.22.2**（本轮派单明令 v22.14.0 批次弃置 ⇒ 不设对照批，全部正批；每 run 标头自报 `node v22.22.2` 六次逐字）。全部在快进 #1 rebase 后（`8a29f3a`）工作树完成核验（两笔快进增量均纯 docs，三 tip `src/` 逐字节相同 ⇒ 测量形态与提交形态位级同一，全部有效）；每次 3/3 sanity 全过。代表性一次运行（run 1）：

```text
VERDICT fixtures: E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=11.7us outcomes=1.4us diagnose=1.35us bandit-build=0.8us | total in-slice CPU ~15.1us per run vs landing bar >=10000us (661x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=328ns/task | empty policy=119ns/task -> x10 tasks/run = 3.28us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=30.7us | realistic registry (baseline+1 candidate)=83.1us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

六官方运行原始区间（全量见附录 A）：

| 格 | 本轮六读数 | 历史带 | 判定 |
|---|---|---|---|
| SLICE-CPU 总量 | 15.1 / 15.3 / 15.1 / 15.1 / 15.3 / 15.1 µs | 派单对照 R32-E 14.8–15.3；默认带 14.7–15.2 | 对照带 **6/6**；默认带 4/6 + 两采样 15.3 越上缘 0.1（R31-E / R32-E 同位 15.3 先例）——价签，**带沿用不变** |
| collect | 11.4–11.8 µs | R25-E 11.3–11.9 | **6/6 带内** |
| outcomes | 1.3–1.4 µs | R25-E 1.3–1.5 | **6/6 带内** |
| diagnose | 1.35 / 1.41 / 1.41 / 1.36 / 1.35 / 1.52 µs | R25-E 1.33–1.46 | 5/6 带内；run 6 的 1.52 越上缘 0.06（注记 (ii)）——价签 |
| bandit-build | 0.8 ×6 | 0.8 | 6/6 |
| apply(avoid=10, M=10) | 328 / 270 / 260 / 274 / 308 / 271 ns/task | 257–312 | 5/6 带内；run 1 的 328 高侧越沿 16 ns（注记 (i)）——价签，不改带 |
| apply(空策略) | 119 / 112 / 113 / 114 / 113 / 165 ns/task | 110–140 | 5/6 带内；run 6 的 165 越沿 25 ns（注记 (ii)）——价签，不改带 |
| load fresh(ENOENT) | 30.7 / 27.8 / 27.1 / 27.9 / 34.2 / 28.3 µs | 并集 25.2–36.8 | **6/6 带内** |
| load real registry | 83.1 / 80.4 / 100.9 / 87.4 / 81.0 / 88.0 µs | 74.3–134.5 | **6/6 带内**（100.9 为带内高侧个体，R31-E 高侧先例同类判读） |

SLICE-CPU 重锚判定：**默认带沿用 14.7–15.2 µs/run 不变**，本轮读数 15.1–15.3 与派单对照 R32-E 14.8–15.3 全交叠；即使整段清零仍 652–664× 低于 ≥10 ms 落地线——派单 µs/ns 拒收类直接适用于切片全部 CPU 面。once-per-run 生产 I/O 位点（load fresh / load real）µs 域、once-per-run CLI 噪声拒收类同样适用。

**诚实注记**：(i) apply(非空) run 1 = 328 ns 为批内首运行高侧个体（+5% 越沿），同 run SLICE-CPU 主锚 15.1 带内、后五采样 260–308 全带内——判批内首运行暖机位置偏置（R30-E 双侧个体同类判读），ns 级拒收类量级，价签不改带。(ii) run 6 的 diagnose 1.52 与 apply(空) 165 同 run 同向高读——判会话尾主机方差个体，主锚同 run 带内，价签不改带。(iii) 补充表征批 ×3（同装具同引擎，附录 A）总量 15.2–15.5：两读数 15.5 越默认带上缘 0.3（collect 11.9/12.0 同向），VERDICT 行逐位一致、sanity 3/3 全过——补充批仅作会话方差表征**不入官方区间**，其高侧方向与注记 (i)(ii) 一致（本 VM 会话尾轻度偏热），结论不受影响。(iv) 存档纪律：快进 #1 rebase 前曾跑过一批补充表征运行（VERDICT 行逐位同上、全过），其 stdout 因会话中断未存档——按纪律**整批弃置不引用**；上表六官方读数与注记 (iii) 补充批全部来自存档件（/tmp 留存核后删除）。

## 2. 第三十三遍枚举（诚实结果：空；零新站点级换名记录）

### 2.1 逐文件重读与裁决（零未点名站点）

| 文件（行数） | 本轮重读检查（全部命中既有 ID / 无号探针 / 已点名站点 / plan-ban 轴） | 结论 |
|---|---|---|
| `auto-loop.ts`（283） | persistSignals evidenceIds.filter = S1-E-4（I/O 支配）；persistSignals∥updateProjectBandit 编排重叠 = S4-E-2（双故障非确定）；ingestSubagentDirectory 顺序读 = S1-E-5；optimizedPolicy filter+map 融合 = S2-E-6（n=10 实测更慢）；runAutoAdaptFromEvents 前置扫描 = S3-E-2 / S5-E-1；无变更跳存 = S2-E-1（X0-3 保存时机）；切片内惰性 import = S5-E-5；mkdir 每写 = S6-E-3；异步悬挂点拓扑 = R30-E 面（plan-ban）；:236 utf8 decode / :239 JSON.parse = R31-E codec 面 + REPRX 计数即止 | 无新候选 |
| `learned-routing.ts`（216） | loadLearnedRouting 去重 = **S8-E-1 硬不变量**（调用位点普查 §2.3）；prefer.find 跨界去重 = S8-E-3；applyLearnedRouting 单次成本 = S13-B-1 维持 loser ID（260–328 ns 采样直证，§1 注记 (i)）；parseLearnedRoutingPolicy 错误信息正则字面量提升 = 冷路径 + S3-E-5 族亚噪声；stableProjectKey 记忆化 = R32-E §2.2 记录 1 拒列维持（X1-1 族 + once-per-run µs 拒收，不重铸）；identity/print/parse 面 = REPRX 划界 | 无新候选 |
| `from-episode.ts`（298） | proposeRoutingFromRoutedEvents 三遍扫描融合 = S2-E-2；提案写路径 = **PWL**（plan-ban，读码即止）；applyCascadeRetry spread / 死 behaviorDistribution 重算 = S9-E-1（**JB** 邻域不重提）；familyFromRouted 小表 includes = S7-E-3 小集合教训族；outcomeKindFromResult 死形参 = R32-E §2.2 记录 2 拒列维持（纯翻搅零收益，不重铸）；**isCompleteRoute 逐字段 typeof 收窄消除 = 已点名站点复命中**（§2.2 复命中 1，拒列）；policyFromOutcomes seen Set 原形健全（个位数 arms 线性域） | 无新候选 |
| `signals.ts`（439） | 两遍结构 = S1-E-1 本体 + **S9-E-2 负优化封死**（未以任何形态触碰）；JUDGE_DECISION 双 Map.get = S1-E-2；verdict 双三元 = S7-E-5；extractAssistant 折叠 = S1-E-3；truncate 恒等快路径 / 流式早退 = S7-E-2 / S2-E-5；scoreUserAnswer 双正则 / trim = S6-E-2 / S7-E-1；baseSignal 条件 spread = S8-E-2（PIC 敏感）；形状单态化 = S4-E-3；ctx 提升 = S5-E-2；`/unknown agent/i` 提升 = S3-E-5；request.agent 探针提升 = S7-E-4；探针复用 = S6-E-5；TASK_RESULT binding 双拷贝 = S2-E-7；五 Map 合一 = S6-E-1（重开条件未触发：事件规模未增 3 个量级且仍被 readAll 支配）；空事件快路径 = S4-E-1 | 无新候选 |
| `bandit-store.ts`（83） | 循环内 arms.includes = 个位数 arms 小表噪声（S2-E-3 邻域）；recordReward 不可变翻搅 = X4-2 readonly 契约（本体属 C 区 `src/routing/bandit.ts`，越切片）；mkdir 每写 = S6-E-3；pretty-print = S6-E-4；无变更跳写 = S2-E-4（write-always 契约）；arms/reward 双扫融合 = S3-E-4；:14/:80 utf8 / :20 JSON.parse = R31-E codec 面 + REPRX 计数即止（W1 既档 7/736/4） | 无新候选 |
| `task-success.ts`（69） | copyDefinedBinding 条件 spread = S5-E-3 族亚噪声；taskSuccessFromResult × scoreTaskResult 分派树合一 = S9-E-3（公开面扩张 / X1-2 复制）；V2 语义零触碰（RVL plan-ban） | 无新候选 |
| `attribution.ts`（92） | attributeToBoundary 排序换单遍 min = S1-E-7（test-only）；findNegativeControlMarker 线扫 = 同一 test-only 可达面；:45 `Math.min` = BINDX 坐标计数即止（切片唯一 Math token）；生产调用方第三十三次为零（§2.3） | 无新候选 |
| `diagnostics.ts`（78） | 组内多遍融合单遍 = S1-E-6（真实规模亚噪声且 10× 更慢）；分组键换嵌套 Map = S5-E-4（平局序发散）；`unique()`/`mode()` Set 化 = **已点名站点复命中**（§2.2 复命中 2，R12-E 行 + R23-E 机械价格，拒列）；唯一生产消费方 = 切片内 auto-loop，锚点 diagnose 段 1.35–1.52 µs 已计价（§1） | 无新候选 |
| `patterns.ts`（146） | detectRepeatedPatterns keys 预取 = S1-E-8（test-only）；相似度对键记忆化 = X2-6（已否决）；computeFeatureSim keySet 物化 = 零生产流量域（signatures 行同判）；O(n²) 聚类 = test-only 零生产调用方（§2.3） | 无新候选 |
| `signatures.ts`（66） | JSON.stringify 哈希入参为内存对象（codec 不在环，R31-E C1 见证既档）；消费方 signals / attribution / patterns 均 type-only 或 test-only 可达；keySet 数组物化 = 同一零生产流量域 | 无新候选 |

结构位置 × 消除机制满矩阵（死码 / 常量折叠 / CSE / 记忆化 / 数据结构 / 融合 / 早退 / 免分配 / 复杂度类 / 强度削减 / 惰性 / 平行路径 / 形状单态化）逐格过筛：非空格全部落在上表既有 ID、plan-ban 轴或 §2.2 已点名站点复命中上。

### 2.2 换名拒绝检查：本轮零新站点级记录

从零重推（假装排除表不存在重读 1770 行）枚举出的候选角度经对表逐一查证，**全部为已点名站点的复命中，无一达到「从未在任何 E 报告点名」的新站点门槛**——本轮不新增站点级换名记录（R32-E 为 2 项、R33-C 为 2 项、R33-D 为 3 项、R33-B 为 1 项；零是更强的收口）：

1. **复命中：`isCompleteRoute` 逐字段 typeof 收窄消除**（from-episode.ts:275——「payload 类型已静态标注 ModelRoutedPayload，五字段 typeof 卫可证冗余」）：站点自 R1-E 起被点名十五轮以上——谓词序 = R10-E…R16-E 拒列行逐字维持；拦截语义 = R24-E 显式定价（不完整路由拦在 join 之外 4–5 ns 档）且 R24-E 已裁定其守卫链语义变更非平凡；重复载荷短路 = R24-E 拒列（S9-D-3 + S9-E-1 谱系）。运行时无校验边界（畸形调用方值直达）⇒ 删卫 = fail-open 家族明文冻结 + S9-D-3 可证死码零成本防御纵深保留。复命中即止，不新增记录。
2. **复命中：`unique()` / `mode()` Set 化 / 单 distinct 早退**（diagnostics.ts:56——「线性 `!seen.includes` 换 Set」）：R12-E 小集合行本体 + R23-E 机械价格坐实（F 面全额 ~12 ns/信号；FeedbackKind 基数 ≤4，线扫免 Set 分配更快）+ R27-E/R28-E「mode()/unique 维持」行。复命中即止，不新增记录。
3. **不重铸：R32-E §2.2 两项拒列记录**（stableProjectKey 记忆化 = X1-1 族 + once-per-run µs 拒收双重命中；outcomeKindFromResult 死形参删除 = 纯翻搅零收益）：裁决逐字维持，站点原形在位读码复核，不以任何新名重提。

### 2.3 生产调用方普查（第三十三次）

- 切片外 `src/` 对 learning 模块导入方恰 7 文件：`run/flowchart-run.ts`（learned-routing）、`cli/adapt.ts`（from-episode + auto-loop）、`cli/main.ts`（learned-routing + auto-loop）、`adaptation/eval-routing.ts`（learned-routing）、`track/primary-split.ts`（type-only，读码复核）、`track/loop.ts`（learned-routing + auto-loop）、`routing/assign.ts`（applyLearnedRouting）——与历轮记录逐文件一致，零新增。
- `patterns` / `signatures` / `attribution` / `diagnostics` 切片外 `src/` 导入方 = **0**；运行时可达面仅 `test/acceptance/adaptive-loop.test.ts` 与 unit 套件（diagnostics 另有切片内 auto-loop 消费，已在锚点计价）。
- `loadLearnedRouting` 生产调用位点恰 3 处：`run/flowchart-run.ts:712`、`track/loop.ts:88`、`cli/main.ts:708`——S8-E-1 的 tracked/children 双载形态原样，零触碰。

### 2.4 枚举收口

第三十三遍逐文件重读**零未点名站点、零新站点级换名记录**。切片全部 CPU 面住在 15.1–15.3 µs/run 总量之内（清零仍 652–664× 低于落地线），全部 I/O 面为 once-per-run µs 域既档定价（load fresh / load real 6/6 带内直证）或 R31-E codec 面既档（W1 = 7/736/4，不重铸）。**第三十三遍枚举为空——比强铸败者 ID 更诚实。**

## 3. 姊妹面划界与非移植清单（含两次快进带入的 R33-B / R33-C）

1. **R33-A**——experiments/账本侧空枚举：A 账本 44/44 三腿卫生分类是**方法卫生**记录（plan-ban 重铸），预算再锚定为 A 属地。E 侧关联：零共享物理对象，无可移植轴；本轮不在 E 区重演账本式分类（E 档案卫生由 §0 哨兵计数 + §2 枚举覆盖，不另立名目）。
2. **R33-B（快进 #1 带入）**——live 路由切片空枚举：从零重枚举恰 1 项新站点级换名记录（`ROLE_FAMILY[role] ?? "unknown"` 死回退消除，analyze-task.ts:93，三重关拒列）；天花板复测落 R31-B 主机偏置窗族邻域；S12-B-2 腿 (a) 与 S13-B-1 显式重取证未触发。E 侧划界：其换名记录站点为 B 区物理对象（`src/routing/analyze-task.ts`），与本切片零共享；其 S13-B-1 哨兵（learned-routing.ts vs `94ed3d9` 冻结 0 行）与本轮 §1 apply 格带内直证**互为佐证不互抄**；fail-open 卫删除拒绝理由（typed-but-unvalidated 边界）与本轮 §2.2 复命中 1 的裁决谱系一致（S9-D-3 族）。无新轴可移植。
3. **R33-C（快进 #2 带入）**——离线路由切片空枚举：从未点名站点连续第十五遍为零；两项换名检查（posterior `lnGamma` 反射分支死码消除；fitLogitAdditive 头部退化结局双 `every` 扫描融合）拒列不铸 ID。E 侧划界：两站点全为 `src/routing/` C 区数值内核物理对象（posterior.ts / offline-logit.ts），本切片零 `lnGamma` 消费、零 fit 链站点；`recordReward` 本体属 C 区 `bandit.ts`（本轮 §2.1 bandit-store 行维持越切片声明即止）；C 侧「零 fs 数据路径 / 零 JSON.parse 位点」结构性空与本切片 3+3 位点计数（§0）互为对照，坐标系一致。无新轴可移植。
4. **R33-D**——adaptation 切片空枚举：元闭合 35 行独立重推无一松动；三项站点级换名记录（promote 链三算哈希 / mkdir 每 eval 重付 / RollbackLog.list() 防御拷贝）全部拒列。E 侧划界：三站点全为 `src/adaptation/` 物理对象，零共享；其 §3 哨兵表对 S12-B-2 / S13-B-1 的记录与本轮 §0/§2 见证同判未触发。无新轴可移植。
5. **R30-E 悬挂点拓扑 / R31-E codec 拓扑 / BINDX / MODEX / R32-A 哨兵方法 / R32-B 闭合稳定性方法 / R31-A/B/D/F/G/H/I 停驻与审计记录**——§0 逐项核对，全部零剂量零重做零重铸。
6. S12-B-2 条件陆器（B 区宿主）E 侧入口读码顺带见证：`adaptation/eval-routing.ts:353–354` 仍只传 `learned` 不传 `prior`（与 round-33/PLAN 记载及 R33-B §2c 独立重取证一致）——未触发，属地在 B/D，声明即止。

## 4. 测试与验证

```bash
# 本 VM: nvm Node v22.22.2（PATH 前置 ~/.nvm/versions/node/v22.22.2/bin
# 绕开 /exec-daemon 系统 Node v22.14.0）。
npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0
#（8a29f3a 树两次同计数；快进 #2 增量纯 docs，src/ 与提交 tip
#  8d31c34 逐字节相同 ⇒ 对提交形态有效）

pnpm gate
# typecheck ✓ + lint ✓ + test ✓ + build ✓ （GATE_EXIT=0）
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（8a29f3a 树两次逐字同计数——快进 #1 rebase 后与提交前终验各一次；
#  快进 #2 增量纯 docs、src/ 逐字节相同 ⇒ 对提交 tip 8d31c34 有效；
#  计数与 R14-E..R32-E / R33-A/B/C/D 逐一相同）
```

锚点装具官方 6 次运行全过（§1，3/3 sanity ×6；确定性 VERDICT 行 6/6 逐位一致且与 R25-E…R32-E 夹具行逐字相同）+ 补充表征批 ×3 全过（不入官方区间）。本轮无候选立项，故无败者仿真；无新增等价性仿真脚本入库（未提交 `scripts/`，遵守 R7-F/R8-C/R10-E…R32-E 无赢家纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 仅 `docs/reports/sota-opt/round-33/R33-E.md`，提交前复核）。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐站点有属主；本轮候选角度全部为已点名站点复命中，零新站点级换名记录）。
- 预算面：SLICE-CPU 默认带 14.7–15.2 µs/run 沿用不变（本轮官方读数 15.1–15.3 与派单对照 R32-E 14.8–15.3 全交叠；两采样 15.3 擦上缘价签），清零仍 652–664× 低于落地线；配置态四格 22/24 官方采样带内、2 高侧价签（首运行暖机 + 会话尾方差，§1 注记 (i)(ii)）——切片无任何达「数十–数百 ms 真尺度非噪声」门槛的剩余面。
- 判定：**MORE_OPTIMA=no**。

## 7. 新增排除 ID：无

- 不铸 S33-E-\*：第三十三遍枚举为空；不取 ALLCAPS 绰号；不写入 EXCLUSIONS.md 任何行（含对空枚举轴外号的一切再编号）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态；S8-E-1 / S9-E-2 / S13-B-1 未重开；未重铸 R32-A 哨兵方法与 R32-B 闭合稳定性方法；未在 E 区重演 R33-A 账本分类。

## 附录 A：锚点装具增量说明、指纹与原始输出（未入库；/tmp 留存核后删除）

- 装具：`/tmp/r33e-anchor.mts`，**365 行**，为 R32-E 附录 A 归档件的**逐行衍生**：先从 `round-32/R32-E.md` 附录 A 提取归档正文并复核指纹（365 行，sha256 `494f801c2ccb621dbf7abca6d3116561b98010be09cf2aa19ad3cf366d64caff`，与 R32-E 归档值逐字符相同），再施加轮次置换共 13 行——头注 R33 标注（L2–5/7/14/93/324 注释行）、mkdtemp 前缀 `r33e-cmp25-fresh-` / `r33e-cmp25-reg-`（L327/332）、projectRoot `/tmp/proj-r33e`（L329/333）、detector identity `r33e-anchor-cmp25`（L336/346）。**seeds `0xe25e01`（collect）/ `0xe25e02`（routed）/ `0xe25e08`（signals）与 NOW = R25-E `2026-08-25T17:30:00.000Z` 逐字节未动**；import 路径为绝对 `/workspace/src/*`，语义同仓库根相对路径。装具全文不重复内联（= R32-E 附录 A 正文 + 上列 13 行置换，任何人可机械重建并核指纹）。
- 本轮装具 sha256：`fc76c820440d80af7d20d8c9aabe4ce04302db934efffc6d7acfbdaea55225b1`
- 运行方式（仓库根，`pnpm install` 后，PATH 前置 nvm bin）：

```bash
node --import tsx /tmp/r33e-anchor.mts   # 独立进程 ×6 = 官方 runs 1–6（每 run 标头打印 node --version 自报 v22.22.2）
```

六官方运行原始值：

```text
SLICE-CPU 总量:  15.1 / 15.3 / 15.1 / 15.1 / 15.3 / 15.1 us
collect:    11.7 / 11.6 / 11.6 / 11.6 / 11.8 / 11.4 us
outcomes:   1.4 / 1.4 / 1.3 / 1.4 / 1.4 / 1.4 us
diagnose:   1.35 / 1.41 / 1.41 / 1.36 / 1.35 / 1.52 us
bandit:     0.8 ×6
apply(non): 328 / 270 / 260 / 274 / 308 / 271 ns/task
apply(空):  119 / 112 / 113 / 114 / 113 / 165 ns/task
load fresh: 30.7 / 27.8 / 27.1 / 27.9 / 34.2 / 28.3 us
load real:  83.1 / 80.4 / 100.9 / 87.4 / 81.0 / 88.0 us
VERDICT 行: 6/6 逐位一致且与 R25-E..R32-E 夹具行逐字相同；
            sanity 3/3 ×6 全过
```

补充表征批 ×3（同装具同引擎；仅作会话方差表征不入官方区间，§1 注记 (iii)）：

```text
SLICE-CPU 总量:  15.5 / 15.2 / 15.5 us
collect:    11.9 / 11.6 / 12.0 us    outcomes: 1.4 / 1.3 / 1.3 us
diagnose:   1.39 / 1.46 / 1.35 us    bandit:   0.8 ×3
apply(non): 273 / 283 / 280 ns/task  apply(空): 115 / 114 / 113 ns/task
load fresh: 29.3 / 26.1 / 29.9 us    load real: 86.2 / 79.5 / 84.7 us
VERDICT 行逐位一致；sanity 3/3 ×3 全过
```

MORE_OPTIMA=no
BRANCH=cursor/r33-e-learning-thirty-third-pass-83a1
