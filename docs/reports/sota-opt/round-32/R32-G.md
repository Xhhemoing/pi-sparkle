MODEL_SLUG=claude-fable-5-thinking-xhigh

# R32-G：runtime/graph/domain 切片 Round 32 三十二搜报告

**战役:** 全库持久 SOTA 优化 Round 32 / R32-G（十区之一，R1-G…R31-G 的第三十二遍）
**基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `f621591`（派单 tip，R32-C 摄取 + 本次派单）；工作期间独占分支 docs-only 快进**两笔**（提交前 fetch 所见）：`f621591` → `41b7a54`（R32-F 空枚举摄取 + R32-H 派单）→ `e17caf8`（R32-E 空枚举摄取 + R32-I 派单）。增量核对：`git diff --stat f621591..e17caf8` 仅触 `docs/reports/sota-opt/` 下 4 文件（PROGRESS / round-32 PLAN / R32-E.md / R32-F.md），对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接复核 **0 行**；按令 rebase 报告分支至 `e17caf8`（纯快进零冲突），42 文件冻结在新 tip 复验仍 **0 行**；R32-F / R32-E 结论（均空枚举，零新姊妹轴）已读并补入非移植清单（§0）。全部锚点与测量在 `f621591` 工作树完成——快进增量 `src/` 逐字节零变化 ⇒ 测量形态与提交形态位级同一，全部有效；聚焦套件 + `pnpm gate` 在新 tip `e17caf8` 复跑一次全绿同计数。R32-H / R32-I 在飞，按令未等待、未触碰。
**分支:** `cursor/r32-g-runtime-thirty-second-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2**（R13-G…R31-G 同引擎线，带可比；tsx 经仓内 node_modules，pnpm 10.17.1，`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S32-G-\* ID，不铸新 face 名、不取绰号、不加 EXCLUSIONS 行，切片零代码改动。** 第三十二遍从零重读 42 文件（`wc -l` 合计 **8960** 行，清单核对恰 42 文件：run 17 + supervisor 4 + graph 4 + domain 17），逐文件枚举的全部消除角度经换名检查裁决为既有排除 ID 本体、姊妹归档面（禁移植）、plan-ban 轴名或全局计算顶之内的亚噪声——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（§3）。R31-G 的 18 扇聚合门闭合审计按令不重铸（每扇属主本轮读码复核**无一松动**，不当边际产出申报）。本轮边际动作仅两类，均不构成新面：(a) 缺省预算重锚（计算顶 **0.289–0.293 ms/run**，与 R31-G 0.284–0.293 带交叠；I/O 地板 **88.2–93.5 ms/run**，支配比 **~304–324×**，两个数量级支配关系不变，§1）；(b) G 档案在案零剂量停放与重开条件的**不计时静态哨兵复核**（§2——方法属 R32-A 首创并已 plan-ban 重铸，此处按令仅作卫生性应用，不申报为新轴），全部哨兵**未触发**。14 格矩阵摘要 `06cbcf92c098c8f0…` 官方引擎三批 × 进程内 3 迭代 = **9 次逐位一致——第二十三轮同值，按令不铸摘要恒等 ID**。空枚举与 R12-G…R31-G 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 42 文件自生产基线 `4efee23` 逐字节零变化（派单 tip / 快进 rebase 后 / 提交前三点核对 `git diff 4efee23 -- <42 文件>` 均 **0 行**）；最强冻结 `git diff 89f0455..e17caf8 -- src/` **完全为空**（R10 以来全部提交均 docs，快进两笔亦纯 docs）。聚焦套件 run/supervisor/graph/domain + integration：**tests 386 / pass 385 / fail 0 / skipped 1** 全绿（`f621591` 与新 tip `e17caf8` 树各一次，逐字同计数，与 R31-G 记录一致，v22.22.2）；`pnpm gate`（typecheck + lint + test + build）**全绿两次**（两 tip 各一次）——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A / R32-D / R32-E / R32-F 记录逐字同计数），退出码 0。不开 PR。

## 0. 范围与约束遵守（含不移植清单）

- 切片：`src/run/`（除 A 属地 `child-tracking.ts` / `gate-apply.ts`）17 文件 + `src/supervisor/`（除 B 属地 `model-router.ts`）4 文件 + `src/graph/` 4 文件 + `src/domain/` 17 文件，恰 **42**，全部实际重读全文，未依赖前三十一轮记忆。
- 读序遵令：README → PROGRESS → EXCLUSIONS 全表 → round-32/PLAN → round-31/PLAN → R31-G → R32-A → R32-B → R32-C（刚摄取）→ R32-D，之后才读 42 切片文件并动手；快进后补读 R32-F.md 与 R32-E.md 全文（仅为划界与非移植清单，不据其改动任何测量或结论）。EXCLUSIONS.md / PROGRESS.md / PLAN.md / README.md 零触碰。
- **快进带入的姊妹结论划界（零新轴可移植）**：R32-F（experiments 空枚举）三项换名检查（stableStringify 仪器形态 / HoldoutVault 注入缝默认臂 / restore 返回拷贝省略）全部为 F 属地物理对象——G 切片 `rg "experiments/"` **零匹配**，零共享对象；R32-E（learning 空枚举）两项换名检查（stableProjectKey 记忆化 / outcomeKindFromResult 死形参）全部为 E 属地物理对象——G 切片对 learning 的唯一 import 为 `flowchart-run.ts` 的 learned-routing 消费腿（§3 已列，Live=R0 不变量 + E/B 属地边界，零剂量）。两报告均未铸新面，非移植清单无需加行。
- **不移植禁令逐项核对**：M1 亲代 run 子调度格（R30-G）、M2 监督引擎裁决格（R29-G）、decision-gate resolution（R28-G）、join-policy（R27-G）、**RUNLIM / CKMIX / TERMCLASS / SCHEDWIN / XPROC / NAMESHAPE / ZREP**——本轮零剂量零重提；**BINDX**（切片 `Math.*` token 恰 **6 个**：hash.ts `Math.abs` ×1、inspection.ts `Math.max` ×3、flowchart-supervisor.ts `Math.max` ×2，同 BINDX 坐标计数即止，零 patch）；**MODEX**（切片 grep **零** chmod / 零 umask / 零八进制 mode 字面量——G 存储四件全部经 umask 默认臂，结构性只有默认臂，计数即止）；I/O text-codec topology / per-run transcode-byte manifest（R31-E——切片显式 fs UTF-8 位点恰 **4 个**：checkpoint-store.ts:22/:31、pause-controller.ts:31/:92；event-store / episode-store 经切片外 `persist/jsonl.ts` 走同一坐标——E 属地，本轮零编解码剂量）；R31-G 18 门闭合审计（**不重铸「我又审了一遍门」**；摘要恒等第二十三轮不铸 ID）；R32-A 重开条件哨兵审计与 R32-B 闭合稳定性审计（**方法不重铸为面**——§2 按令仅应用于 G 自档案在案条目，不申报为新轴）；R31-A injected-callable 停放门；R31-B 六停驻门（cascade 升级臂证据 × 失败类格维持 routing 属地停放）；R31-D R22-D 属地格；R31-F 故障通道处置分解；R31-H 四扇边界参数残门；R31-I CLI 进程边界门扉审计（G 侧 inspection/CLI 腿为 once-per-run 拒收类，平面正交）。FITQ / SCALEX / YMIX / KFAN / NVG / SEEDX / SHAPEK / ORDX / CALLB / OCCX / JB / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / PAIRX / CSPELL / REPRX / LINKTOP 全部未编号未移植。
- **站位零剂量停放不重铸**：`planTaskTopology` X0-2（读码复核仍无 run loop 调用方，§2 哨兵）；`settleSupervisedOutcome` A 属地 no-op（无 assessment 即无门，§2 哨兵）。
- 未重开任何 X\* / S1-\* … S31-\* 条目。**单变量纪律**：本轮无任何计时新臂——重锚脚本与 R12-G 附录归档件**逐字节同一**（§1、附录），哨兵全部为静态 grep / 读码断言（不计时）。硬不变量零触碰：双 LCB + 双归因在位（切片侧证人：events.ts `isBehaviorDistribution` + flowchart-snapshot.ts `isOneHotDistribution` one-hot 校验原文在位）；Live = R0-equivalent（flowchart-run 的 learned 路由应用面未触碰，B 属地 model-router 未读改）；promotion proposal-first / 无 Outcome-supported 声称（Checkpoint F-PROD 仍开放，ADR-005）；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不变（零 diff 天然满足）；fsync / 完整性再哈希在位（event-store 终态 fsync、checkpoint-store `handle.sync()`、pause-controller writeAtomic、episode-store 终态 fsync 全部原文在位）；fail-open 家族保持关闭（events.ts `validateEvent` 追加/重放双侧 fail-closed 原形）；分析不变更在飞 run；tracking 无指挥权；H/score 不写 routing PASS/FAIL。无赢家 ⇒ 零生产改动、零新仿真文件入库。不开 PR。

## 1. CPU vs I/O 重锚（三批次官方运行，nvm Node v22.22.2，seed 仅标注批次）

归档基准从 `docs/reports/sota-opt/round-12/R12-G.md` 原样提取（```ts 围栏内 536 行 → `tmp-r32g-bench.ts`，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`，与 R13-G…R31-G 附录 A 记录值**逐字节一致**；无赢家按纪律不入库，临时文件核后已删除）。三批 seed = 0xa32091 / 0xa32092 / 0xa32093，全部在 `f621591` 工作树完成（快进增量纯 docs，三 tip `src/` 逐字节相同 ⇒ 测量形态与提交形态位级同一，全部有效）。

| 探针 | B1 | B2 | B3 |
| --- | --- | --- | --- |
| validateFlowchart 16n join4 | 9.146 µs | 9.074 µs | 9.017 µs |
| → ×32 轮计算顶 | **0.293 ms/run** | **0.290 ms/run** | **0.289 ms/run** |
| DeterministicJudge.decide 8×8 | 0.146 µs | 0.147 µs | 0.147 µs |
| expandTaskTransition | 0.506 µs | 0.485 µs | 0.506 µs |
| compileChildrenToFlowchart 16 子 | 8.774 µs | 8.897 µs | 8.868 µs |
| validateEvent AGENT_EVENT | 0.560 µs | 0.560 µs | 0.563 µs |
| replayRun 300 events | 0.659 µs | 0.662 µs | 0.642 µs |
| validateCheckpoint N=48 含 restore | 251.7 µs | 251.7 µs | 254.9 µs |
| CheckpointStore.write 持久（N=48） | 585.3 µs | 537.8 µs | 596.8 µs |
| → ×66 写/run | 38.6 ms | 35.5 ms | 39.4 ms |
| EventStore.append（无 fsync） | 70.0 µs | 68.4 µs | 70.1 µs |
| → ×300 事件/run | 21.0 ms | 20.5 ms | 21.0 ms |
| EventStore.readAll ×96/run 中期均幅 | 33.6 ms | 32.2 ms | 33.1 ms |
| EpisodeStore.append COMPLETED（fsync） | 299.3 µs | 298.4 µs | 267.5 µs |
| settle-close 终态命令算术 | 0.491 ms | 0.492 ms | 0.457 ms |

计算顶 = **0.293 / 0.290 / 0.289 ms/run**（R31-G 0.284–0.293 带内，无孤点）；I/O 地板合成 = **93.2 / 88.2 / 93.5 ms/run**；支配比 **~304–324×**（R31-G ~316–363× / R30-G ~309–337× 同为两个数量级，支配结论不变）。矩阵摘要 `06cbcf92c098c8f0…` 3 批 × 3 迭代 = **9 次逐位一致（第二十三轮同值；14 格配置态 × 命令类矩阵逐格 I/O 计数零变化，不铸摘要轴/ID）**；start→COMPLETED 首跑冷墙钟 19.9 / 19.4 / 20.1 ms，事件尾 [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED] 不变。

**诚实注记**：(i) B2 的 I/O 地板下沿 88.2 低于 R30-G 带下沿（88.3）0.1 ms（~0.1%）——逐腿归因于本轮检查点持久写 537.8–596.8 µs 回落历史带内（R31-G 曾录 730.7/716.8 µs 高侧并注记宿主代际偏移，本轮该偏移消退），冻结 0 行 + 计算顶带内 ⇒ 宿主方差，不作发现。(ii) 另有三批完整运行误在系统 Node v22.14.0 上完成（nvm 未穿透默认 PATH）——虽全绿且摘要同为 `06cbcf92c098c8f0` 九次逐位一致，仍按 R13-G…R31-G 引擎纪律**整批弃置不引用其任何数值**；官方三批全部在 nvm v22.22.2 复跑（附录）。(iii) 聚焦套件与 `pnpm gate` 均在 v22.22.2 复核（v22.14.0 上的首次聚焦套件同计数全绿，同样弃置不引用）。

## 2. G 档案在案零剂量停放 / 重开条件哨兵复核（全部不计时；方法应用，不铸面）

R32-A 首创的哨兵审计方法（plan-ban 重铸为面）此处按令仅作卫生性应用于 **G 自档案**的在案条目。全部哨兵为静态 grep / 读码断言，零计时臂：

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| X0-2（`planTaskTopology`） | run loop 或任何生产文件获得调用方 | `rg "planTaskTopology" src/` 除 supervisor.ts 自身外**空匹配**（第三十二次复核）⇒ **未触发** |
| `settleSupervisedOutcome` A 属地 no-op | 生产侧带 assessment 调用出现 | 切片外调用方 **零**；函数原形 assessment 缺席即返回 ⇒ **未触发** |
| R30-G M1 格站位前提 | `startParentRun` 获得 CLI 调用方或 resume 门 | `rg "startParentRun" src/` 除 coordinator.ts 外**空匹配** ⇒ **未触发** |
| R29-G M2 格站位前提 | `startSupervisedRun` 获得 CLI 调用方 | CLI 仅动态 import `resumeSupervisedRun`（main.ts:1021，与历轮记录一致）；`startSupervisedRun` 仍无 CLI 调用方 ⇒ **未触发** |
| 摘要恒等（不铸 ID） | 14 格矩阵任一格 I/O 计数或摘要变化 | 官方三批 9 次逐位同值 `06cbcf92c098c8f0…`（第二十三轮）⇒ **未触发** |
| R31-B cascade 停放门（G 侧宿主） | child-coordinator cascade 臂获得新证据面 | 冻结 0 行 + `maybeCascadeRetry` 原形读码复核 ⇒ **未触发**（routing 属地维持） |
| R31-E 编解码坐标（G 侧四位点） | G 存储腿新增编解码剂量 | 显式 utf8 位点恰 4 个与 R31-G 记录一致，零剂量 ⇒ **未触发** |
| 签名/调用图类重开条件全体 | 要求 `src/` 变更 | 最强冻结 `89f0455..e17caf8 -- src/` **0 行**直接判空 ⇒ **全部未触发** |

## 3. 四十二文件新视角检查表（第三十二遍枚举残余）

**`src/run/`（17 文件）**

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| coordinator.ts | M0 缺省路径 300-append I/O 地板本体（排除表 G 族行）；M1 `startParentRun` = R30-G 格（站位哨兵未触发，§2）；`toModelRoutedPayload` 路由载荷映射 = 双归因见证不可动 | 无新候选 |
| supervisor.ts | M2 裁决格全维度 = R29-G；`planTaskTopology` X0-2 停放（§2）；`settleSupervisedOutcome` A 属地 no-op（§2）；`reconstructSupervisorState` 重放腿 = RUNLIM/CKMIX 既有属主 | 无新候选 |
| flowchart-run.ts | 每轮全量 readAll 放大 = S1-G-1/S2-J-11/S3-I-6/S6-G-1/S6-G-7 排除行；检查点节奏 = CKMIX；learned 路由应用 = Live=R0 不变量 + B 属地边界；pauseIfRequested = pause token 已锚 | 无新候选 |
| child-coordinator.ts | ConcurrencyGate FIFO 微结构 = 0.29 ms 计算顶内亚噪声（R31-G 门）；重试/超时梯 = M1/M2 格已定价；cascade 升级臂 = R31-B 停放（§2 哨兵） | 无新候选 |
| event-store.ts | 终态 fsync = 硬不变量禁去；追加/读双侧 `validateEvent` = fail-closed 读校验契约行 | 无新候选 |
| checkpoint-store.ts | 临时文件 + rename + sync 原子写 = 持久契约；记录组成 = CKMIX | 无新候选 |
| events.ts | EVENT_TYPES 闭表 + validateEvent = 校验平面既有属主；`routingContextFields` / `isBehaviorDistribution` = 双归因 one-hot 见证 | 无新候选 |
| replay.ts | replayRun 0.64–0.66 µs/300 事件（亚噪声）；materializeCheckpoint / validateCheckpoint = CKMIX + S8-G-2 写侧校验 | 无新候选 |
| child-grounding.ts | 包预算/去重一次性 µs 级；context packet 编译 = context 姊妹切片属地 | 无新候选 |
| child-prompt.ts | formatChildPrompt 0.31–0.92 µs 已定价（R30-G [A]）；串形 = NAMESHAPE 禁 | 无新候选 |
| episode-bind.ts | settle-close 终态算术 0.457–0.492 ms 本轮带内已锚；closure 裁决 = episode 姊妹属地；`hash32` 集中化 = 既有落地 | 无新候选 |
| episode-store.ts | 队列化追加 + 终态 fsync = 硬不变量；readAll 校验 = jsonl 读契约 | 无新候选 |
| flowchart-executor.ts | 终态结果投影 = TERMCLASS 禁；薄执行器臂 = R31-G 门（真实支配成本是 executor 秒–分级真跑） | 无新候选 |
| injection.ts | 注入指令种类/校验 = R25-G 面全维度 | 无新候选 |
| inspection.ts | once-per-run CLI 分析腿 = 拒收类（一次性 CLI 噪声）；findChild 回退线扫亚噪声 | 无新候选 |
| pause-controller.ts | writeAtomic fsync = 硬不变量；token ENOENT/present 20.6–23.5 / 54.0–54.6 µs 带内已锚 | 无新候选 |
| scheduler.ts | LeaseRegistry / planRound / applyTaskOutcome = R29-G 格 + SCHEDWIN 禁；expandTaskTransition 0.485–0.506 µs 亚噪声 | 无新候选 |

**`src/supervisor/`（4 文件）**

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| flowchart-supervisor.ts | `propagate()` 不动点 + edgeStatus/joinStatus = join-policy 面（R27-G）；leaseReadyNodes 逐节点 computeStatus 重算 = R28-G 决策门格 + 计算顶内亚噪声；structuredClone 防御拷贝面 = R31-G 门；waiter 不变式 = 契约 | 无新候选 |
| flowchart-snapshot.ts | fail-closed 快照结构校验 = CKMIX + S8-G-2 属主；`isOneHotDistribution` = 双归因见证；`snapshotValidationRouter` throw-only 桩零剂量 | 无新候选 |
| flowchart.ts | 纯再导出垫片，零运行时；模块形 = NAMESHAPE 禁 | 无新候选 |
| ledger.ts | advanceLedgerRound 数组 spread 记账 = 计算顶内亚噪声（R31-G 门）；classifyRoundProgress 线性查重（facts 个位数规模） | 无新候选 |

**`src/graph/`（4 文件）**

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| validate.ts | Kahn 拓扑一次性构造（16 节点 µs 级）；确定序 = ORDX 禁 | 无新候选 |
| readiness.ts | computeReadyTasks 线扫 = R29-G 格 | 无新候选 |
| judge.ts | DeterministicJudge 0.146–0.147 µs（亚噪声地板级） | 无新候选 |
| compile-children.ts | compileChildrenToFlowchart 8.8–8.9 µs 一次性已锚；`--children` 编译面历轮已裁 | 无新候选 |

**`src/domain/`（17 文件）**

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| flowchart.ts | validateFlowchart 9.0–9.1 µs ×32 = 计算顶本体；条件/join 校验 = 校验平面既有属主；approval plan/reply 三段校验 = 权威面契约 | 无新候选 |
| state.ts | expandTaskTransition BFS 已定价亚噪声；转移表 = TERMCLASS 禁 | 无新候选 |
| ids.ts | 前缀 + 正则双查 = 校验平面（VALCLASS 类既有裁决）；branded 类型零运行时 | 无新候选 |
| timestamp.ts | ISO_PATTERN + Date.parse 双查 = 校验平面既有属主 | 无新候选 |
| hash.ts | hash32 集中化 = 既有落地本体（BINDX 计数即止，§0） | 无新候选 |
| task.ts / run.ts / limits.ts / project.ts / evidence.ts / episode.ts / contract.ts | 校验器族 = 校验平面既有排除行；错误串构造仅失败路径 | 无新候选 |
| status.ts / roles.ts / record.ts / errors.ts / index.ts | 闭枚举表 / type-only / 再导出 = 零剂量或亚噪声 | 无新候选 |

## 4. 候选总表

**空。** 本轮零候选：哨兵复核（§2）按定义不产出候选（不计时、不剂量）；四十二文件枚举（§3）全部命中既有 ID 本体、姊妹归档面、plan-ban 轴名或 0.29 ms 计算顶内亚噪声。本轮无任何计时新臂 ⇒ 无可定价候选面。全局支配结构不变：计算面全体封顶 0.293 ms/run，距 ≥10 ms 落地线两个数量级；I/O 面三腿 20.5–39.4 ms 全部契约或排除表保护。

## 5. CHECKS

- 分支恰 `cursor/r32-g-runtime-thirty-second-pass-83a1`，基线 `f621591` → 快进 rebase `41b7a54` → `e17caf8`（两笔均纯 docs，逐笔核对 `src/ test/ scripts/ package.json pnpm-lock.yaml` 0 行）。
- 冻结核对：`git diff 4efee23 -- <42 文件>` 派单 tip / 快进 rebase 后 / 提交前均 **0 行**；最强冻结 `git diff 89f0455..e17caf8 -- src/` **0 行**。
- 切片清单：恰 42 文件（17+4+4+17），`wc -l` 合计 8960 行，全部实读。
- 重锚脚本与 R12-G 附录归档件 sha256 逐字符同一（`9d46a829…b9943`，536 行）；官方三批全部 nvm v22.22.2；v22.14.0 误跑批次已弃置并诚实记录（§1(ii)）。
- 摘要恒等：`06cbcf92c098c8f0…` 官方 9 次逐位一致（第二十三轮同值，不铸 ID）；事件尾不变。
- 哨兵普查（第三十二次）：X0-2 / A 属地 no-op / M1 / M2 站位、cascade 停放、编解码坐标、MODEX 零位点、BINDX 6 token 计数——全部未触发（§2）。
- 姊妹划界（快进带入）：G 切片 `rg "experiments/"` 零匹配（R32-F 三项换名记录零共享对象）；learning 唯一 import 为 flowchart-run.ts 消费腿（R32-E 两项换名记录零交集）。
- 聚焦测试：run/supervisor/graph/domain + integration = **386 tests / 385 pass / 0 fail / 1 skipped**（`f621591` 与 `e17caf8` 树各一次，v22.22.2，与 R31-G 逐字同计数）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿两次**（两 tip 各一次）——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-D / R32-A / R32-D / R32-E / R32-F 逐字同计数）；退出码 0。
- 仓库改动恰本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；Live=R0-equivalent 在位；proposal-first 在位；无阈值变更；CAS / 凭据 / 数据面契约 / 公开签名不变；fsync / 完整性族在位；fail-open 家族保持关闭；分析未变更任何在飞 run；不开 PR。

## 6. 重开条件

本轮无新定价轴，不加行。X0-2、A 属地 no-op、R30-G/R29-G 站位前提、R31-B cascade 停放、R31-E 编解码坐标、CKMIX/S8-G-2 等既有重开条件全额维持原文；本轮 §2 哨兵直证全部未触发。B2 I/O 地板下沿 88.2（§1(i)）不构成重开触发——检查点写腿回落历史带内、冻结 0 行、计算顶带内。

## 7. 新增排除 ID

**无。** 空枚举轮不铸 S32-G-\* ID；不取绰号、不作轴名、不加 EXCLUSIONS 行。

## 附录 A：重锚基准出处与关键输出

脚本与 R12-G 附录 A 归档件**逐字节相同**：自 `docs/reports/sota-opt/round-12/R12-G.md`（```ts 围栏内 536 行）原样提取为仓库根 `tmp-r32g-bench.ts` 运行，sha256 = `9d46a8291a1a50ef16890a9c9a4d7e420a8da3dc8be0553406f0d9f8b7db9943`（与 R13-G…R31-G 附录 A 记录值一致，逐字节可复现；无赢家按纪律不入库，临时文件核后已删除）。运行方式：`node node_modules/tsx/dist/cli.mjs tmp-r32g-bench.ts <seed>`（nvm Node v22.22.2），三批 seed = 0xa32091/92/93（seed 仅标注批次，全部数据确定性构造）。关键输出：

```
--- Node v22.22.2 (canonical) ---
validateFlowchart 16n join4: 9.146 / 9.074 / 9.017 us -> x32 = 0.289~0.293 ms/run
validateFlowchart 8n join2: 4.644 / 4.622 / 4.576 us
DeterministicJudge.decide 8x8: 0.146 / 0.147 / 0.147 us
expandTaskTransition PENDING->FAILED: 0.506 / 0.485 / 0.506 us
compileChildrenToFlowchart 16 children: 8.774 / 8.897 / 8.868 us
validateEvent AGENT_EVENT: 0.560 / 0.560 / 0.563 us
replayRun 300 events: 0.659 / 0.662 / 0.642 us
validateCheckpoint full (N=48, incl. restore): 251.7 / 251.7 / 254.9 us
CheckpointStore.write durable (N=48): 585.3 / 537.8 / 596.8 us -> x66 = 38.6 / 35.5 / 39.4 ms/run
EventStore.append AGENT_EVENT (no fsync): 70.0 / 68.4 / 70.1 us -> x300 = 21.0 / 20.5 / 21.0 ms/run
EventStore.readAll 300/100 events: 485.9~508.8 / 185.6~190.2 us -> x96 = 33.6 / 32.2 / 33.1 ms/run
pause token() ENOENT / present: 20.6~23.5 / 54.0~54.6 us
loadLearnedRouting (no snapshot): 22.4 / 23.2 / 22.9 us
EpisodeStore.readAll (2 snapshots): 57.3 / 55.8 / 57.3 us
EpisodeStore.append COMPLETED (fsync): 299.3 / 298.4 / 267.5 us
EpisodeEventStore.append (no fsync): 64.5 / 69.5 / 62.2 us
settle-close arithmetic: 0.491 / 0.492 / 0.457 ms per terminal command
matrix digest (3 proc x 3 iter): 06cbcf92c098c8f0... 九次逐位一致（14 格；与 R10-G..R31-G 同值，第二十三轮）
start->COMPLETED wall-clock: 19.9 / 19.4 / 20.1 ms; returned events tail = [LEDGER_UPDATED, RUN_COMPLETED, EPISODE_CLOSED]
```

（弃置批次注记：另有三批全绿运行误在系统 Node v22.14.0 上完成——摘要同为 `06cbcf92c098c8f0` 九次逐位一致、计算顶 0.292–0.297——按 R13-G…R31-G 引擎纪律整批弃置，不引用其任何数值。）

---

**MORE_OPTIMA=no** —— 第三十二遍复查空枚举收口：计算顶 vs I/O 地板支配比 ~304–324× 两个数量级不变；14 格矩阵摘要第二十三轮同值；G 档案在案停放/重开哨兵全部未触发；42 文件逐条消除角度全部命中既有属主。无达门槛新更优解。

MORE_OPTIMA=no
BRANCH=cursor/r32-g-runtime-thirty-second-pass-83a1
