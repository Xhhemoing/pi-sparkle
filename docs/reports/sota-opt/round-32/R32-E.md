MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 32 / R32-E：learning 切片第三十二遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 32 / R32-E（Round 1–31 同区三十一遍
  之后的第三十二遍）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `fb106e1`
  （R32-B 空枚举摄取 + 本 R32-E 派单）；工作期间独占分支 docs-only
  快进**三次**：`fb106e1` → `0b8fa0a`（R32-D 空枚举摄取 + R32-F 派单）、
  `0b8fa0a` → `f621591`（R32-C 空枚举摄取 + R32-G 派单）、
  `f621591` → `41b7a54`（R32-F 空枚举摄取 + R32-H 派单，收稿期
  fetch 所见）。三笔增量逐笔核对均仅触 `docs/reports/sota-opt/`
  下文件（PROGRESS / round-32 PLAN / 新报告），对
  `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接复核
  **0 行**；按令逐笔 rebase 报告分支（均纯快进零冲突）并重验冻结：
  `git diff adb20d7 -- src/learning/` 在分支创建点、每次快进后、
  提交前**均 0 行**。最强冻结再延一轮：
  `git diff 89f0455..41b7a54 -- src/` **完全为空**（R10 以来全部提交
  均为 docs）。新收口姊妹结论 R32-D（空枚举，元闭合 35 行无一松动）、
  R32-C（空枚举，两项换名检查拒列）与 R32-F（空枚举，三项换名
  检查拒列）已读并并入非移植清单（§3）。R32-G / R32-H 在飞，按令
  未等待、未触碰。
- **分支:** `cursor/r32-e-learning-thirty-second-pass-83a1`
- **切片:** `src/learning/` 全部 10 文件（`attribution` 92 行 /
  `auto-loop` 283 / `bandit-store` 83 / `diagnostics` 78 /
  `from-episode` 298 / `learned-routing` 216 / `patterns` 146 /
  `signals` 439 / `signatures` 66 / `task-success` 69 = **1770 行**，
  `wc -l` 与 R14-E..R31-E 逐一相同），本轮第三十二遍全量实际重读。
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** 正批 + **v22.14.0** 对照批
  （`/exec-daemon/node node_modules/tsx/dist/cli.mjs` 调度，
  R31-E 同约定）；tsx 4.23.12、pnpm 10.17.1、`pnpm install`
  干净完成（锁文件未改）。

## 结论

**第三十二遍诚实枚举为空：不铸任何 S32-E-\* ID、不铸新 face 名、
不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，本轮生产代码零改动。**
对 10 个源文件逐行第三十二遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-E-1..8 … S9-E-1..3 全系、S13-B-1、R10-E..R31-E 全部无号关闭探针
与轴，含 R30-E 悬挂点拓扑、R31-E I/O 文本编解码拓扑、R31-C
**BINDX**、R31-J **MODEX**）后，每一个执行站点都能指到既有 ID、
无号探针或 plan-ban 轴——**零未点名站点**。本轮新增换名检查 **2 项**
（§2.2：`stableProjectKey` 记忆化；`outcomeKindFromResult` 死形参
删除），逐项裁决为 X1-1 族 + once-per-run µs 拒收类与纯翻搅零收益，
不铸 ID。生产调用方普查第三十二次确认：`patterns` / `signatures` /
`attribution` 运行时可达面仅测试（切片外 `src/` 导入方为零）；
`diagnostics` 唯一生产消费方为切片内 `auto-loop`（锚点 diagnose 段
已计价）；`loadLearnedRouting` 生产调用位点恰 3 处不变（§2.3）。

按派单在本 VM 重锚默认预算（未拷贝历轮数字；cmp25 夹具 = R25-E
种子/NOW，跨轮可比面；装具为 R10-E 附录 A 逐字同构 + R32-E 置换，
全文见附录 A）：

- **SLICE-CPU cmp25 总量：本轮六读数 14.8–15.3 µs/run**
  （15.1 / 14.8 / 15.1 | 14.9 / 15.3 / 15.1；3 × v22.22.2 +
  3 × v22.14.0）vs 默认带 14.7–15.2：**5/6 带内、1 采样 15.3 越
  上缘恰 0.1 µs**（v22.14.0 批第二运行——表显粒度级单侧擦边，
  R31-E 同为 v22.14.0 批 15.3 同位先例；同批 VERDICT 逐位一致、
  零 diff）——按位置噪声价签记录，**默认带沿用 14.7–15.2 µs/run
  不变**。即使整段清零仍 654–674× 低于 10 ms 落地线。
- 分段 collect 11.4–11.8 / outcomes 1.3–1.4 / bandit 0.8 vs R25-E
  带 11.3–11.9 / 1.3–1.5 / 0.8 全数在带；diagnose 1.31–1.39 vs 带
  1.33–1.46：一采样 1.31 低于下缘 0.02（v22.14.0 首运行，R30-E
  diagnose 0.01 同位先例，价签）。
- **夹具 VERDICT 行 6/6 逐位一致且与 R25-E..R31-E 夹具行逐字相同**
  （`E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2
  prevArms=3`）；live 面 `nonempty allowed=5 preferred=m5 | empty
  allowed=10 preferred=m2` 三十二轮不变。
- 配置态锚点（6 采样）：apply(avoid=10, M=10) **271–282 ns/task**
  （R13-E..R30-E 带 257–312 内 **6/6**）、空策略 **113–126 ns/task**
  （110–140 带内 **6/6**）、load 现实 registry **79.3–82.5 µs**
  （74.3–134.5 带内 **6/6**）、load fresh(ENOENT) **24.9–33.6 µs**
  （历史并集 25.2–36.8 内 5/6——**24.9 低位越下缘 0.3 µs**，
  v22.14.0 批第二运行；once-per-run ENOENT µs 级腿的会话位置噪声，
  R31-E 高侧 37.3/37.6/38.8 与 R30-E 双侧个体同类判读，本轮出现在
  低侧——价签记录，不改带）。

回归与门禁（§4）：聚焦切片套件（unit/learning 7 文件 +
acceptance/adaptive-loop）**48/48 双引擎全绿**；`pnpm gate` 在
`f621591` 与提交 tip `41b7a54` 各跑一次均 **exit 0**（tests 1169 /
suites 78 / pass 1168 / fail 0 / skipped 1，两次逐字同计数，与
R14-E..R31-E / R32-A..R32-D 逐一相同）；快进 #1 后的 `0b8fa0a`
树上聚焦套件与 gate 亦各跑一次全绿同计数。

硬性不变量逐条复核（§0）：双 LCB / 双归因在位；live = R0 等价
（R1 未接线）；proposal-first、不声称 Outcome-supported
（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；
CAS / 凭证 / 数据面合同 / 公开签名零触碰；无 fsync / 完整性再哈希
去除；fail-open 家族保持关闭；**S8-E-1 零去重、S9-E-2 未触碰、
S13-B-1 维持 loser ID**。仓库变更仅本报告一个文件。空枚举与
R10-E..R31-E 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → PROGRESS.md → EXCLUSIONS.md
  （完整表，含全部 S\*-E-\* 行与 S12-\*、S13-B-1）→
  round-32/PLAN.md（E 段 + 具名/unnamed 全 ban 榜）→
  round-31/PLAN.md → R31-E.md 全文（空枚举确认，I/O 文本编解码
  拓扑轴 + W1 = 7/736/4 定价，无 S31-E-\*）→ R32-A.md（重开条件
  哨兵审计，方法 plan-ban）→ R32-B.md（闭合稳定性审计，方法
  plan-ban）→ 10 个切片源文件全量重读；三次快进后分别补读
  R32-D.md、R32-C.md 与 R32-F.md 结论（仅为划界与非移植清单，
  不据其改动任何测量或结论）。
- 基线漂移检查：分支创建点 `fb106e1`；三笔 docs-only 快进
  （`0b8fa0a`、`f621591`、`41b7a54`）逐笔 `git diff --stat` + 对
  `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接
  diff = **0 行**核对后按派单规程 rebase 并重验冻结。冻结主张多次
  核验均 **0 行**（10 文件 vs `adb20d7`：分支创建时、每次快进后、
  提交前）；最强冻结 `89f0455..41b7a54 -- src/` 为空；
  `git log -1 -- src/learning/` 最后生产改动仍为 `adb20d7`。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：
  async suspension-point topology / per-run suspension-manifest
  （R30-E——本轮零悬挂计数、零 manifest 臂）/ **PWL**（propose 写
  路径终止腿——`proposeRoutingFromOutcomes` 本轮读码即止）/
  **RVL** / **RTL** / **CG** / **JB**（`applyCascadeRetry` spread
  形态不重提）/ **NVG** / **REPRX**（切片 `JSON.parse` 站点 =
  parseLearnedRoutingPolicy 与 bandit-store 载入两处，均既有定价
  面，表示轴不重铸）/ **PAIRX** / **ZREP** / **LINKTOP** /
  **BINDX**（R31-C realm 级内建绑定——本轮零 patch、零剂量、零
  计时新臂）/ **MODEX**（R31-J inode 模式字拓扑——切片零 chmod /
  零 umask / 零八进制 mode 字面量，声明即止）/ I/O text-codec
  topology / per-run transcode-byte manifest（R31-E——切片 3 个
  fs UTF-8 位点（auto-loop.ts:236、bandit-store.ts:14/:80）读码
  确认原形在位，codec / Buffer-patch / transcode-byte 臂零重做，
  W1 = 7/736/4 只作既档引用）/ R32-A 重开条件哨兵审计（方法不
  重铸——本轮不产出「再查一遍重开条件」式段落；E 档案凡要求
  src/ 变更的重开条件由 0 行冻结判空，S6-E-1 事件规模条件与
  S12-B-2 条件陆器由 §2.1/§3 读码顺带见证未触发）/ R32-B 闭合
  稳定性审计（方法不重铸）/ R31-A injected-callable plane（停放）/
  R31-B 六停驻门 / R31-D R22-D 属地格 / R31-F 故障通道处置分解 /
  R31-G 18 门审计 / R31-H 四残门 / R31-I CLI 进程边界审计（本
  切片为进程内库 + 自有文件 I/O，平面正交）。FITQ / NAMESHAPE /
  SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX /
  TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / RUNLIM /
  VALCLASS / AGEX / COTARG / PRICEX / DELINV / PROBX / SIDEC /
  CSPELL 全部未编号未移植。X\* / S1-\* … S31-\* 全部未重开。
- 硬不变量零触碰：双 LCB（beta + normal，`src/routing/posterior.ts`
  属 C 区，rg 复核在位）与双归因（logit + prob-add，
  `src/experiments/attribution-report.ts` 属 F 区，rg 复核在位）
  均保留；live = R0 等价（R1 未接线）；sparse → approved R0
  baseline；promotion proposal-first（Checkpoint F-PROD 开放，
  ADR-005）；无阈值改动；不改测试迁就重构；CAS / 凭证 / 数据面
  合同 / 公开签名不动；无 fsync / 完整性再哈希去除；fail-open
  家族保持关闭；**S8-E-1 不去重 loadLearnedRouting**（本轮仅
  §2.3 重普查其 3 个调用位点与 §1 单价带）；**S9-E-2 负优化未
  触碰**；S13-B-1 维持。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
  不开 PR。输家脚本不入库（本轮无输家道——除锚点装具外零计时
  新臂；装具全文入附录 A，未提交 `scripts/`）。

## 1. 冻结证明与 SLICE-CPU / 配置态重锚（cmp25）

```bash
git log --oneline -1 origin/cursor/sota-persistent-opt-83a1
# 41b7a54 docs(sota-opt): ingest R32-F (empty enum) and dispatch R32-H
#（开工 tip 为 fb106e1；docs-only 快进三笔 0b8fa0a → f621591 →
#  41b7a54 并已逐笔 rebase——四 tip src/ 逐字节相同，冻结在每个
#  新 tip 复验仍 0 行）
git diff adb20d7 -- src/learning/ | wc -l        # 0
git diff 89f0455..41b7a54 -- src/ | wc -l        # 0
wc -l src/learning/*.ts                          # 1770 total（同 R14-E..R31-E）
```

锚点装具 `/tmp/r32e-anchor.mts`（365 行，构造 = R10-E 附录 A 逐字
同构 + R32-E 置换：头注、NOW = R25-E `2026-08-25T17:30:00.000Z`、
seeds `0xe25e01/02/08`、mkdtemp 前缀 `r32e-cmp25-*`、detector
identity `r32e-anchor-cmp25`——即历轮 `cmp25` 开关的固定化形态；
全文与 sha256 见附录 A）。正批 **3 × v22.22.2 + 对照批
3 × v22.14.0**，全部在 `f621591` rebase 后工作树完成（快进 #3
增量纯 docs，四 tip `src/` 逐字节相同 ⇒ 测量形态与提交形态位级
同一，全部有效）；每次 3/3
sanity 全过，确定性 VERDICT 行**6/6 跨运行跨引擎逐位一致且与
R25-E..R31-E 夹具行逐字相同**。代表性一次运行（v22.22.2 run 2）：

```text
VERDICT fixtures: E=41 collected=14 routedE=41 outcomes=1 S=12 diagnoseGroups=2 prevArms=3
BENCH SLICE-CPU anchor re-verify: collect=11.4us outcomes=1.3us diagnose=1.35us bandit-build=0.8us | total in-slice CPU ~14.8us per run vs landing bar >=10000us (674x below even if zeroed)
VERDICT live-face sanity: nonempty allowed=5 preferred=m5 | empty allowed=10 preferred=m2
BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=273ns/task | empty policy=114ns/task -> x10 tasks/run = 2.73us/run (live face with learned policy loaded)
BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=29.2us | realistic registry (baseline+1 candidate)=82.1us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)
VERDICT total sanity checks: 3, failures: 0
ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)
```

六运行原始区间（全量见附录 A）：

| 格 | 本轮六读数 | 历史带 | 判定 |
|---|---|---|---|
| SLICE-CPU 总量 | 15.1 / 14.8 / 15.1 \| 14.9 / 15.3 / 15.1 µs | 默认带 14.7–15.2 | 5/6 带内；15.3 越上缘 0.1（v22.14.0 批 run 2，R31-E 同引擎 15.3 同位先例）——价签，**带沿用不变** |
| collect | 11.4–11.6 \| 11.4–11.8 µs | R25-E 11.3–11.9 | 6/6 带内 |
| outcomes | 1.3–1.4 \| 1.3 µs | R25-E 1.3–1.5 | 6/6 带内 |
| diagnose | 1.35–1.39 \| 1.31–1.36 µs | R25-E 1.33–1.46 | 5/6 带内；1.31 越下缘 0.02（v22.14.0 run 1，R30-E 0.01 同位先例）——价签 |
| bandit-build | 0.8 ×6 | 0.8 | 6/6 |
| apply(avoid=10, M=10) | 271–273 \| 277–282 ns/task | 257–312 | **6/6 带内** |
| apply(空策略) | 114–126 \| 113–116 ns/task | 110–140 | **6/6 带内** |
| load fresh(ENOENT) | 29.2–33.6 \| 24.9–31.2 µs | 并集 25.2–36.8 | 5/6 带内；**24.9 越下缘 0.3**（v22.14.0 run 2；R31-E 高侧 37.3/37.6/38.8 + R30-E 双侧个体同类判读，本轮低侧）——价签，不改带 |
| load real registry | 79.3–82.5 \| 79.9–82.1 µs | 74.3–134.5 | **6/6 带内** |

SLICE-CPU 重锚判定：**默认带沿用 14.7–15.2 µs/run 不变**，本轮
读数 14.8–15.3（R31-E cmp25 读数 14.9–15.3 交叠）；即使整段清零
仍 654–674× 低于 ≥10 ms 落地线——派单 µs/ns 拒收类直接适用于
切片全部 CPU 面。once-per-run 生产 I/O 位点（load fresh / load
real）µs 域、once-per-run CLI 噪声拒收类同样适用。

诚实注记：快进 #1 后曾在 `0b8fa0a` 树上先行执行过一批锚点运行
（VERDICT 行逐位同上、全过），其 stdout 因会话中断未存档——按
纪律**整批弃置不引用**；上表六读数全部来自 `f621591` 树上的
正式批（三 tip `src/` 逐字节相同，夹具确定性使弃置无信息损失）。

## 2. 第三十二遍枚举（诚实结果：空）

### 2.1 逐文件重读与裁决（零未点名站点）

| 文件（行数） | 本轮重读检查（全部命中既有 ID / 无号探针 / plan-ban 轴） | 结论 |
|---|---|---|
| `auto-loop.ts`（283） | persistSignals evidenceIds.filter = S1-E-4（I/O 支配）；persistSignals∥updateProjectBandit 编排重叠 = S4-E-2（双故障非确定）；ingestSubagentDirectory 顺序读 = S1-E-5；optimizedPolicy filter+map 融合 = S2-E-6（n=10 实测更慢）；runAutoAdaptFromEvents 前置扫描 = S3-E-2 / S5-E-1；无变更跳存 = S2-E-1（X0-3 保存时机）；切片内惰性 import = S5-E-5；mkdir 每写 = S6-E-3；异步悬挂点拓扑 = R30-E 面（plan-ban）；auto-loop.ts:236 utf8 decode = R31-E codec 面（plan-ban） | 无新候选 |
| `learned-routing.ts`（216） | loadLearnedRouting 去重 = **S8-E-1 硬不变量**（调用位点普查 §2.3）；prefer.find 跨界去重 = S8-E-3；applyLearnedRouting 单次成本 = S13-B-1 维持 loser ID（271–282 ns 带内直证）；parseLearnedRoutingPolicy 错误信息正则字面量提升 = 冷路径（每 load 至多一次）+ S3-E-5 族亚噪声；stableProjectKey 记忆化 = **本轮换名检查 1**（§2.2）；identity/print/parse 面 = REPRX 划界（plan-ban，声明即止） | 无新候选 |
| `from-episode.ts`（298） | proposeRoutingFromRoutedEvents 三遍扫描融合 = S2-E-2；proposeRoutingFromOutcomes 提案写路径 = **PWL**（plan-ban，读码即止）；applyCascadeRetry spread 拷贝 / 死 behaviorDistribution 重算 = S9-E-1（**JB** 邻域，不重提）；familyFromRouted 小表 includes = S7-E-3 小集合教训族；outcomeKindFromResult 死形参 = **本轮换名检查 2**（§2.2）；policyFromOutcomes seen Set 原形健全（个位数 arms 线性域） | 无新候选 |
| `signals.ts`（439） | 两遍结构 = S1-E-1 本体 + **S9-E-2 负优化封死**（未以任何形态触碰）；JUDGE_DECISION role/family 双 Map.get = S1-E-2；verdict 双三元 = S7-E-5；extractAssistant 折叠 = S1-E-3；truncate 恒等快路径 / 流式早退 = S7-E-2 / S2-E-5；scoreUserAnswer 双正则 / trim = S6-E-2 / S7-E-1；baseSignal 条件 spread = S8-E-2（PIC 敏感）；形状单态化 = S4-E-3；ctx 提升 = S5-E-2；`/unknown agent/i` 提升 = S3-E-5；request.agent 探针提升 = S7-E-4；探针复用 = S6-E-5；TASK_RESULT binding 双拷贝 = S2-E-7；五 Map 合一 = S6-E-1（重开条件未触发：事件规模未增 3 个量级且仍被 readAll 支配）；空事件快路径 = S4-E-1 | 无新候选 |
| `bandit-store.ts`（83） | 循环内 arms.includes = 个位数 arms 小表噪声（S2-E-3 邻域）；recordReward 不可变逐信号对象翻搅 = X4-2 readonly 契约（且 recordReward 本体属 C 区 `src/routing/bandit.ts`，越切片）；mkdir 每写 = S6-E-3；pretty-print = S6-E-4；无变更跳写 = S2-E-4（write-always 契约）；arms/reward 双扫融合 = S3-E-4；:14 utf8 decode / :80 utf8 encode = R31-E codec 面（plan-ban，W1 既档 7/736/4） | 无新候选 |
| `task-success.ts`（69） | copyDefinedBinding 条件 spread = S5-E-3 族亚噪声；taskSuccessFromResult × scoreTaskResult 分派树合一 = S9-E-3（公开面扩张 / X1-2 复制）；V2 语义零触碰（RVL plan-ban） | 无新候选 |
| `attribution.ts`（92） | attributeToBoundary 排序换单遍 min = S1-E-7（test-only）；findNegativeControlMarker 线扫 = 同一 test-only 可达面；生产调用方第三十二次为零（§2.3） | 无新候选 |
| `diagnostics.ts`（78） | 组内多遍融合单遍 = S1-E-6（真实规模亚噪声且 10× 更慢）；分组键换嵌套 Map = S5-E-4（平局序发散）；唯一生产消费方 = 切片内 auto-loop，锚点 diagnose 段 1.31–1.39 µs 已计价（§1） | 无新候选 |
| `patterns.ts`（146） | detectRepeatedPatterns keys 预取 = S1-E-8（test-only）；相似度对键记忆化 = X2-6（已否决）；O(n²) 聚类 = test-only 零生产调用方（§2.3） | 无新候选 |
| `signatures.ts`（66） | JSON.stringify 哈希入参为内存对象（codec 不在环，R31-E C1 见证既档）；消费方 signals / attribution / patterns 均 type-only 或 test-only 可达；keySet 数组物化 = 同一零生产流量域 | 无新候选 |

结构位置 × 消除机制满矩阵（死码 / 常量折叠 / CSE / 记忆化 / 数据
结构 / 融合 / 早退 / 免分配 / 复杂度类 / 强度削减 / 惰性 / 平行
路径 / 形状单态化）逐格过筛：非空格全部落在上表既有 ID、plan-ban
轴或 §2.2 两项新记录上。

### 2.2 本轮换名检查记录（裁决后拒列，不铸 ID）

1. **`stableProjectKey` 记忆化**（learned-routing.ts L32–35：每
   调用两次 regex replace + toLowerCase + hash32(9 字节域)）：
   调用方为 `routingPolicyIdentity` 与 `learnedRoutingPath`，
   全部住在 once-per-run load 腿（applyLearnedRouting 每任务路径
   不经过它）；单价亚 µs、每 run 常数次。模块级缓存形态 =
   **X1-1 隐藏缓存族**（陈旧缓存风险裁决原文适用），且收益量级
   属派单 once-per-run µs 拒收类。双重命中，拒列。
2. **`outcomeKindFromResult` 死形参删除**（from-episode.ts
   L291–297：形参 `outcome` 在函数体内零读取，仅 `verification`
   被消费；调用点 L216 实参 `message.outcome` 为已求值属性读）：
   删除是**纯翻搅零收益**——私有函数、实参无副作用、V8 对未用
   实参无逐调用成本可省；且 `message.outcome` 在 L221
   classifyTaskFailure 路径继续被消费，删形参不消灭任何读取。
   语义防御纵深（签名与 taxonomy 对齐）保留原形。拒列。

### 2.3 生产调用方普查（第三十二次）

- 切片外 `src/` 对 learning 模块导入方恰 7 文件：
  `run/flowchart-run.ts`（learned-routing）、`cli/adapt.ts`
  （from-episode + auto-loop）、`cli/main.ts`（learned-routing +
  auto-loop）、`adaptation/eval-routing.ts`（learned-routing）、
  `track/primary-split.ts`（type-only）、`track/loop.ts`
  （learned-routing + auto-loop）、`routing/assign.ts`
  （applyLearnedRouting）——与历轮记录逐文件一致，零新增。
- `patterns` / `signatures` / `attribution` / `diagnostics` 切片外
  `src/` 导入方 = **0**；运行时可达面仅
  `test/acceptance/adaptive-loop.test.ts` 与 unit 套件
  （diagnostics 另有切片内 auto-loop 消费，已在锚点计价）。
- `loadLearnedRouting` 生产调用位点恰 3 处：
  `run/flowchart-run.ts:712`、`track/loop.ts:88`、
  `cli/main.ts:708`——S8-E-1 的 tracked/children 双载形态原样，
  零触碰。

### 2.4 枚举收口

除 §2.2 两项外，第三十二遍逐文件重读**零未点名站点**。切片全部
CPU 面住在 14.8–15.3 µs/run 总量之内（清零仍 654–674× 低于落地
线），全部 I/O 面为 once-per-run µs 域既档定价（load fresh /
load real 带内直证）或 R31-E codec 面既档（W1 = 7/736/4，不重
铸）。**第三十二遍枚举为空——比强铸败者 ID 更诚实。**

## 3. 姊妹面划界与非移植清单（含三次快进带入的 R32-D / R32-C / R32-F）

1. **R32-D（快进 #1 带入）**——adaptation 切片空枚举：R31-D 元
   闭合 35 行坐标表从零重推无一松动；R22-D 属地格前提（monitor
   零生产调用方）第三十二次成立；哨兵复核为 R32-A 方法的卫生性
   应用（不铸面）。E 侧关联：其 §3 对 **S13-B-1** 的哨兵
   （applyLearnedRouting 单次成本增长 ≥2 个量级）由本轮 §1
   apply 格 271–282 ns 带内直证继续未触发。无新轴可移植。
2. **R32-C（快进 #2 带入）**——离线路由切片空枚举：从未点名站点
   连续第十四遍为零；两项换名检查（updatePosterior pow 强度
   削减 → BINDX 属地 + 零池；bootstrap values.sort TypedArray
   数值特化 → 逐位违例 + X4-3 族）拒列不铸 ID。E 侧划界：本
   切片无 `Math.pow` 站点、无 bootstrap 排序池，两记录零交集。
   无新轴可移植。
3. **R32-F（快进 #3 带入）**——experiments 切片空枚举：R31-F 面格
   属主表独立重推无一格松动；三项换名检查（stableStringify 仪器
   形态 → 字节契约 + X1-1 族；HoldoutVault 注入缝默认臂 → R31-A
   停放 + 零生产流量；restore 返回拷贝省略 → R29-A 别名拓扑 +
   X3-3 契约）拒列不铸 ID，全部 F 属地物理对象。E 侧划界：本
   切片零 stableStringify 消费、零 vault 链、restore 面属 D/F。
   无新轴可移植。
4. **R30-E 悬挂点拓扑 / R31-E codec 拓扑 / BINDX / MODEX /
   R32-A 哨兵方法 / R32-B 闭合稳定性方法 / R31-A/B/D/F/G/H/I
   停驻与审计记录**——§0 逐项核对，全部零剂量零重做零重铸。
5. S12-B-2 条件陆器（B 区宿主）E 侧入口读码顺带见证：
   `adaptation/eval-routing.ts` 仍只传 `learned` 不传 `prior`
   （与 round-32/PLAN 记载一致）——未触发，属地在 B/D，声明
   即止。

## 4. 测试与验证

```bash
# 本 VM: nvm Node v22.22.2 正批；v22.14.0 对照批以
# /exec-daemon/node node_modules/tsx/dist/cli.mjs 调度。
npx tsx --test "test/unit/learning/"*.test.ts "test/acceptance/adaptive-loop.test.ts"
# tests 48 / suites 2 / pass 48 / fail 0
#（f621591 树 v22.22.2 与 v22.14.0 均过；提交 tip 41b7a54 树
#  v22.22.2 再跑一次同计数；快进 #1 后 0b8fa0a 树亦跑一次同计数）

pnpm gate
# typecheck ✓ + lint ✓ + test ✓ + build ✓ （GATE_EXIT=0）
# test: tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#（f621591 与提交 tip 41b7a54 各跑一次逐字同计数，与
#  R14-E..R31-E / R32-A..R32-D 逐一相同；0b8fa0a 树亦跑一次
#  同计数）
```

锚点装具 6 次运行全过（§1，3/3 sanity ×6；确定性 VERDICT 行
6/6 逐位一致）。本轮无候选立项，故无败者仿真；无新增等价性仿真
脚本入库（未提交 `scripts/`，遵守 R7-F/R8-C/R10-E..R31-E 无赢家
纪律）。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件
（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 仅
`docs/reports/sota-opt/round-32/R32-E.md`）。

## 6. MORE_OPTIMA 判定

- 结构性新角度：无（§2 空枚举，逐站点有属主；§2.2 两项换名检查
  分别落 X1-1 + once-per-run µs 拒收类与纯翻搅零收益）。
- 预算面：SLICE-CPU 默认带 14.7–15.2 µs/run 沿用不变（本轮读数
  14.8–15.3，5/6 带内 + 1 擦边价签），清零仍 654–674× 低于落地
  线；配置态四格 23/24 采样带内、1 低侧价签——切片无任何达
  「数十–数百 ms 真尺度非噪声」门槛的剩余面。
- 判定：**MORE_OPTIMA=no**。

## 7. 新增排除 ID：无

- 不铸 S32-E-\*：第三十二遍枚举为空；不取 ALLCAPS 绰号；不写入
  EXCLUSIONS.md 任何行（含对空枚举轴外号的一切再编号）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态；S8-E-1 / S9-E-2 /
  S13-B-1 未重开；未重铸 R32-A 哨兵方法与 R32-B 闭合稳定性方法。

## 附录 A：锚点装具全文、指纹与原始输出（未入库；运行后 /tmp 留存）

- 装具：`/tmp/r32e-anchor.mts`，**365 行**，构造 = R10-E 附录 A
  逐字同构 + R32-E 置换（头注、NOW = R25-E
  `2026-08-25T17:30:00.000Z`、seeds `0xe25e01/02/08`、mkdtemp
  前缀 `r32e-cmp25-*`、identity `r32e-anchor-cmp25`；import 路径
  为绝对 `/workspace/src/*`，语义同仓库根相对路径）。
- sha256：
  `494f801c2ccb621dbf7abca6d3116561b98010be09cf2aa19ad3cf366d64caff`
- 运行方式（仓库根，`pnpm install` 后）：

```bash
node_modules/.bin/tsx /tmp/r32e-anchor.mts                            # ×3（v22.22.2 正批）
/exec-daemon/node node_modules/tsx/dist/cli.mjs /tmp/r32e-anchor.mts  # ×3（v22.14.0 对照批）
```

六运行原始值：

```text
SLICE-CPU 总量:  v22.22.2: 15.1 / 14.8 / 15.1 us
                 v22.14.0: 14.9 / 15.3 / 15.1 us
collect:    11.4 / 11.4 / 11.6 | 11.4 / 11.8 / 11.6 us
outcomes:   1.4 / 1.3 / 1.4    | 1.3 / 1.3 / 1.3 us
diagnose:   1.39 / 1.35 / 1.35 | 1.31 / 1.36 / 1.35 us
bandit:     0.8 ×6
apply(non): 273 / 273 / 271    | 282 / 278 / 277 ns/task
apply(空):  121 / 114 / 126    | 113 / 113 / 116 ns/task
load fresh: 33.6 / 29.2 / 29.8 | 31.2 / 24.9 / 25.8 us
load real:  79.3 / 82.1 / 82.5 | 79.9 / 82.1 / 80.4 us
VERDICT 行: 6/6 逐位一致且与 R25-E..R31-E 夹具行逐字相同；
            sanity 3/3 ×6 全过
```

装具全文：

```typescript
/**
 * R32-E deterministic anchor re-measurement (thirty-second pass over src/learning/).
 * No fresh candidates survived enumeration (empty thirty-second-group enumeration,
 * same verdict class as R10-E..R31-E), so this script only re-anchors on THIS VM:
 *   (1) the SLICE-CPU total upper-bound anchor (R3-E..R31-E lineage), on the
 *       cmp25 cross-round-comparable fixture (R25-E seeds/NOW; VERDICT lines
 *       must reproduce the R25-E..R31-E fixture lines verbatim), and
 *   (2) the configured-state x command-class matrix cells named by the
 *       dispatch: applyLearnedRouting nonempty vs empty policy, and
 *       loadLearnedRouting fresh root (ENOENT) vs realistic registry.
 * Structure is the R10-E appendix A script verbatim with only these
 * substitutions: header comment, NOW = R25-E's 2026-08-25T17:30:00.000Z,
 * seeds 0xe25e01 (collect) / 0xe25e02 (routed) / 0xe25e08 (signals),
 * mkdtemp prefixes r32e-cmp25-*, detector identity r32e-anchor-cmp25.
 * Seeded PRNG (mulberry32) -> fully reproducible. VERDICT lines are
 * deterministic (bit-identical across runs); BENCH lines carry timing.
 */
import { mkdtemp } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { performance } from "node:perf_hooks";
import {
  collectSignalsFromEvents,
  type ObservedSignal
} from "/workspace/src/learning/signals.js";
import { diagnoseModelProjectIssues } from "/workspace/src/learning/diagnostics.js";
import { outcomesFromRoutedRun } from "/workspace/src/learning/from-episode.js";
import {
  applyLearnedRouting,
  ensureRoutingBaseline,
  loadLearnedRouting,
  routingPolicyContent,
  routingPolicyIdentity,
  type LearnedRoutingPolicy
} from "/workspace/src/learning/learned-routing.js";
import { createBanditState, recordReward, type BanditState } from "/workspace/src/routing/bandit.js";
import { oneHotDistribution } from "/workspace/src/routing/catalog-model.js";
import {
  loadAdaptationRegistryOrNew,
  saveAdaptationRegistry
} from "/workspace/src/adaptation/promotion.js";
import { AGENT_ROLES } from "/workspace/src/domain/roles.js";
import type { ProjectId } from "/workspace/src/domain/ids.js";
import type { Event } from "/workspace/src/run/events.js";
import type { TaskOutcome, VerificationKind } from "/workspace/src/protocol/v1.js";
import type { IsoTimestamp } from "/workspace/src/domain/timestamp.js";
import type { TaskFamily } from "/workspace/src/task/taxonomy.js";

let failures = 0;
let checks = 0;
function check(name: string, ok: boolean): void {
  checks += 1;
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}`);
  }
}
function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}
async function benchAsync(fn: () => Promise<void>, reps: number): Promise<number> {
  await fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) await fn();
  return (performance.now() - t0) / reps;
}

const NOW = "2026-08-25T17:30:00.000Z" as IsoTimestamp;
const FAMILIES_LOCAL: readonly TaskFamily[] = [
  "edit", "test", "review", "plan", "research", "refactor", "deploy", "unknown"
];
const OUTCOMES: readonly TaskOutcome[] = ["SUCCESS", "PARTIAL", "FAILURE", "CANCELLED"];
const VERIFS: readonly VerificationKind[] = ["PASSED", "FAILED", "UNOBSERVED"];
const ROLES = ["actor", "critic", "tester", "planner", "scout", "reviewer"] as const;
const ANSWERS = ["lgtm", "no, revert this", "please also add coverage", "可以", "不行 错误", "hmm"];

/* R1-A composition fixture (E~41): mirrors R8-E..R31-E generators. */
function genCollectEvents(rng: () => number, length: number): Event[] {
  const out: unknown[] = [];
  const taskIds = Array.from({ length: 10 }, (_, i) => `tsk_${i}0000000`);
  out.push({ type: "PROJECT_DISCOVERED", payload: { project: { id: "prj_simsim01", rootPath: "/tmp/x" } } });
  for (let i = 0; i < length; i += 1) {
    const roll = rng();
    const taskId = pick(rng, taskIds);
    if (roll < 0.25) {
      out.push({
        type: "MODEL_ROUTED",
        payload: {
          taskId,
          model: pick(rng, ["cheap", "premium", "mid"]),
          role: pick(rng, ROLES),
          ...(rng() < 0.8 ? { family: pick(rng, FAMILIES_LOCAL) } : {}),
          ...(rng() < 0.8 ? { modelVersion: "v1" } : {}),
          ...(rng() < 0.8 ? { featureVersion: "fv1" } : {})
        }
      });
    } else if (roll < 0.5) {
      out.push({
        type: "CHILD_MESSAGE",
        payload: {
          message: {
            type: "TASK_RESULT",
            taskId,
            runId: "run_simsim01",
            outcome: pick(rng, OUTCOMES),
            verification: { kind: pick(rng, VERIFS) },
            summary: pick(rng, ["tests passed", "did the work\n  with details", "failed to compile", ""]),
            evidenceIds: rng() < 0.6 ? ["evd_00000001"] : []
          }
        }
      });
    } else if (roll < 0.6) {
      out.push({ type: "USER_ANSWER", runId: "run_simsim01", payload: { answer: pick(rng, ANSWERS) } });
    } else if (roll < 0.7) {
      out.push({
        type: "JUDGE_DECISION",
        runId: "run_simsim01",
        payload: {
          taskId,
          verdict: pick(rng, ["APPROVED", "REJECTED", "NEEDS_USER_DECISION"] as const),
          evidenceIds: rng() < 0.5 ? ["evd_00000002"] : []
        }
      });
    } else {
      out.push({ type: pick(rng, ["LEDGER_UPDATED", "TASK_STATUS_CHANGED", "RUN_STARTED"] as const), payload: {} });
    }
  }
  return out as Event[];
}

function genRoutedEvents(rng: () => number, length: number): Event[] {
  const out: unknown[] = [];
  const taskIds = Array.from({ length: 10 }, (_, i) => `tsk_${i}0000000`);
  const models = ["cheap", "premium", "mid"];
  out.push({
    type: "PROJECT_DISCOVERED",
    runId: "run_simsim01",
    occurredAt: NOW,
    payload: { project: { id: "prj_simsim01", rootPath: "/tmp/proj-a" } }
  });
  for (let i = 0; i < length; i += 1) {
    const roll = rng();
    const taskId = pick(rng, taskIds);
    if (roll < 0.28) {
      const model = pick(rng, models);
      const complete = rng() < 0.85;
      out.push({
        type: "MODEL_ROUTED",
        runId: "run_simsim01",
        occurredAt: NOW,
        payload: {
          taskId,
          model,
          role: pick(rng, ["actor", ...AGENT_ROLES]),
          eligibleModels: models,
          behaviorDistribution: oneHotDistribution(models, model),
          ...(complete ? { family: pick(rng, FAMILIES_LOCAL) } : {}),
          ...(complete ? { featureVersion: "fv1" } : {}),
          ...(complete ? { modelVersion: "v1" } : {}),
          ...(complete ? { agentRole: pick(rng, [...AGENT_ROLES]) } : {})
        }
      });
    } else if (roll < 0.36) {
      out.push({
        type: "TASK_RETRY",
        runId: "run_simsim01",
        occurredAt: NOW,
        taskId: rng() < 0.8 ? taskId : undefined,
        payload: { nextModel: rng() < 0.85 ? pick(rng, [...models, "fresh"]) : undefined }
      });
    } else if (roll < 0.62) {
      out.push({
        type: "CHILD_MESSAGE",
        runId: "run_simsim01",
        occurredAt: NOW,
        payload: {
          message: {
            type: "TASK_RESULT",
            taskId,
            runId: "run_simsim01",
            outcome: pick(rng, OUTCOMES),
            verification: { kind: pick(rng, VERIFS) },
            summary: "tests passed",
            evidenceIds: rng() < 0.6 ? ["evd_00000001"] : []
          }
        }
      });
    } else {
      out.push({
        type: pick(rng, ["USER_ANSWER", "LEDGER_UPDATED", "RUN_STARTED"] as const),
        runId: "run_simsim01",
        occurredAt: NOW,
        payload: { answer: "lgtm" }
      });
    }
  }
  return out as Event[];
}

function genSignal(rng: () => number): ObservedSignal {
  const criterion = pick(rng, ["taskSuccess", "userAcceptance", "policyCompliance", undefined] as const);
  return {
    source: pick(rng, ["user", "subagent", "deterministic"] as const),
    kind: pick(rng, ["human", "peer", "judge", "deterministic"] as const),
    projectId: pick(rng, ["prj_a0000000", "prj_b0000000"]) as ProjectId,
    score: Math.floor(rng() * 101),
    boundary: "execution",
    summary: "s",
    createdAt: NOW,
    evidenceIds: [],
    ...(rng() < 0.85 ? { modelId: pick(rng, ["m1", "m2", "m3"]) } : {}),
    ...(criterion !== undefined ? { criterion } : {}),
    ...(rng() < 0.7 ? { family: pick(rng, ["edit", "test", "review"]) } : {}),
    ...(rng() < 0.8 ? { outcomeKind: pick(rng, ["PASS", "FAIL"] as const) } : {})
  };
}

/* Verbatim in-lock bandit build replica (R2-E S2-E-3 reference form). */
function banditBuild(previous: BanditState | undefined, signals: readonly ObservedSignal[]): BanditState {
  const arms = new Set(previous?.arms ?? []);
  for (const signal of signals) {
    if (signal.modelId !== undefined && signal.modelId.trim() !== "") arms.add(signal.modelId);
  }
  const armList = [...arms];
  let state = createBanditState(armList);
  if (previous !== undefined) {
    const pulls: Record<string, number> = {};
    const rewardSum: Record<string, number> = {};
    for (const arm of armList) {
      pulls[arm] = previous.pulls[arm] ?? 0;
      rewardSum[arm] = previous.rewardSum[arm] ?? 0;
    }
    state = {
      arms: armList,
      pulls,
      rewardSum,
      explorationsUsed: previous.explorationsUsed,
      highRiskExplorations: previous.highRiskExplorations
    };
  }
  for (const signal of signals) {
    if (signal.criterion !== "taskSuccess") continue;
    if (signal.source === "user" || signal.kind === "human" || signal.kind !== "deterministic") continue;
    if (signal.modelId === undefined || !state.arms.includes(signal.modelId)) continue;
    if (signal.outcomeKind === "PASS") state = recordReward(state, signal.modelId, 1);
    else if (signal.outcomeKind === "FAIL") state = recordReward(state, signal.modelId, 0);
  }
  return state;
}

/* ================================================================
 * SECTION 1: SLICE-CPU anchor + live-face configured-state cells.
 * ================================================================ */
{
  const events = genCollectEvents(mulberry32(0xe25e01), 40);
  const routed = genRoutedEvents(mulberry32(0xe25e02), 40);
  const sigRng = mulberry32(0xe25e08);
  const signals = Array.from({ length: 12 }, () => genSignal(sigRng));
  const models10 = Array.from({ length: 10 }, (_, i) => `m${i}`);
  const prevSeed = Array.from({ length: 30 }, () => genSignal(sigRng));
  const previous = banditBuild(undefined, prevSeed);

  const collected = collectSignalsFromEvents(events, {});
  const outs = outcomesFromRoutedRun(routed);
  const diag = diagnoseModelProjectIssues(signals);
  console.log(
    `VERDICT fixtures: E=${events.length} collected=${collected.length} routedE=${routed.length} outcomes=${outs.length} S=${signals.length} diagnoseGroups=${diag.length} prevArms=${previous.arms.length}`
  );
  check("collect fixture non-trivial", collected.length > 3);
  check("outcomes fixture non-trivial", outs.length > 0);

  const collect = bench(() => void collectSignalsFromEvents(events, {}), 20000);
  const outcomes = bench(() => void outcomesFromRoutedRun(routed), 20000);
  const diagnose = bench(() => void diagnoseModelProjectIssues(signals), 40000);
  const bandit = bench(() => void banditBuild(previous, signals), 40000);
  const total = collect + outcomes + diagnose + bandit;
  console.log(
    `BENCH SLICE-CPU anchor re-verify: collect=${(collect * 1e3).toFixed(1)}us outcomes=${(outcomes * 1e3).toFixed(1)}us diagnose=${(diagnose * 1e3).toFixed(2)}us bandit-build=${(bandit * 1e3).toFixed(1)}us | total in-slice CPU ~${(total * 1e3).toFixed(1)}us per run vs landing bar >=10000us (${Math.round(10 / total)}x below even if zeroed)`
  );

  // Configured-state live face: nonempty learned policy vs empty policy.
  const catalog10 = models10;
  const learned10: LearnedRoutingPolicy = {
    primaryModelId: "m0",
    avoid: Array.from({ length: 10 }, (_, i) => ({
      modelId: `m${i % 5}`,
      reason: "r",
      ...(i % 2 === 0 ? { family: ["edit", "test", "review", "plan", "research"][i % 5]! } : {})
    })),
    prefer: [{ family: "edit", modelId: "m1" }]
  };
  const learnedEmpty: LearnedRoutingPolicy = { primaryModelId: "m0", avoid: [], prefer: [] };
  const appliedNon = applyLearnedRouting("edit", catalog10, "m2", learned10);
  const appliedEmpty = applyLearnedRouting("edit", catalog10, "m2", learnedEmpty);
  console.log(
    `VERDICT live-face sanity: nonempty allowed=${appliedNon.allowedModels.length} preferred=${appliedNon.preferredModel} | empty allowed=${appliedEmpty.allowedModels.length} preferred=${appliedEmpty.preferredModel}`
  );
  const applyCost = bench(() => void applyLearnedRouting("edit", catalog10, "m2", learned10), 100000);
  const applyEmptyCost = bench(() => void applyLearnedRouting("edit", catalog10, "m2", learnedEmpty), 100000);
  console.log(
    `BENCH configured-state anchor: applyLearnedRouting(avoid=10, M=10)=${(applyCost * 1e6).toFixed(0)}ns/task | empty policy=${(applyEmptyCost * 1e6).toFixed(0)}ns/task -> x10 tasks/run = ${(applyCost * 10 * 1e3).toFixed(2)}us/run (live face with learned policy loaded)`
  );
}

/* ================================================================
 * SECTION 2: configured-state cells — loadLearnedRouting fresh vs
 * realistic registry (production call-site absolute anchor; same
 * fixture as R8-E S8-E-1 / R9-E..R31-E).
 * ================================================================ */
{
  const freshRoot = await mkdtemp(join(tmpdir(), "r32e-cmp25-fresh-"));
  const freshCost = await benchAsync(async () => {
    await loadLearnedRouting(freshRoot, "/tmp/proj-r32e");
  }, 200);

  const stateRoot = await mkdtemp(join(tmpdir(), "r32e-cmp25-reg-"));
  const projectRoot = "/tmp/proj-r32e";
  const registry = await loadAdaptationRegistryOrNew(stateRoot);
  const identity = routingPolicyIdentity(projectRoot);
  const parent = ensureRoutingBaseline(registry, identity, "premium", "r32e-anchor-cmp25");
  const policy: LearnedRoutingPolicy = {
    primaryModelId: "premium",
    avoid: [{ modelId: "cheap", family: "edit", reason: "meanScore 0.30 over 6 samples" }],
    prefer: [{ family: "edit", modelId: "premium" }]
  };
  registry.createCandidate({
    identity,
    content: routingPolicyContent(policy),
    parentVersionId: parent.versionId,
    author: { kind: "detector", identity: "r32e-anchor-cmp25" },
    evaluationPlan: { stages: ["static", "replay"], metrics: ["task-success", "cost"], planVersion: 1 }
  });
  await saveAdaptationRegistry(stateRoot, registry);
  const loaded = await loadLearnedRouting(stateRoot, projectRoot);
  check("matrix registry fixture yields an active policy", loaded !== undefined);
  const realCost = await benchAsync(async () => {
    await loadLearnedRouting(stateRoot, projectRoot);
  }, 200);
  console.log(
    `BENCH configured-state anchor: loadLearnedRouting fresh-root(ENOENT)=${(freshCost * 1e3).toFixed(1)}us | realistic registry (baseline+1 candidate)=${(realCost * 1e3).toFixed(1)}us (once-per-run production call-site cost; the S8-E-1 duplicate second load per tracked/children run costs exactly one of these)`
  );
}

console.log(`VERDICT total sanity checks: ${checks}, failures: ${failures}`);
if (failures > 0) {
  console.error(`${failures} SANITY CHECK(S) FAILED`);
  process.exit(1);
}
console.log("ALL ANCHOR SANITY CHECKS PASSED (deterministic, seeded)");
```

MORE_OPTIMA=no
BRANCH=cursor/r32-e-learning-thirty-second-pass-83a1
