MODEL_SLUG=claude-fable-5-thinking-xhigh

# Round 32 / R32-C：离线路由切片第三十二遍复查报告

- **战役:** 全库持久 SOTA 优化 Round 32 / R32-C（Round 1–31 同区三十一遍
  之后的第三十二遍，叠在已落地的 S7-C 之上）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `337b24d`
  （R32-A 空枚举摄取 + 本 R32-C 派单）；工作期间独占分支 docs-only
  快进**一次**：`337b24d` → `8593dc4`（R31-J 空枚举摄取——铸造
  **MODEX**，Round 31 10/10 收口——+ R32-D 派单，增量仅触
  docs/reports/sota-opt/ 下 4 文件 PROGRESS / round-31 PLAN /
  R31-J.md / round-32 PLAN，1,440 插入 33 删除）。增量对
  `src/ test/ scripts/ package.json pnpm-lock.yaml` 直接复核
  **零行**；按令快进报告分支至 `8593dc4`（纯快进零冲突）并重验冻结：
  `git diff 183df9b -- <9 切片文件>` 在分支创建点（`337b24d`）、
  快进后（`8593dc4`）、提交前**三验均 0 行**。最强冻结再延一轮：
  `git diff 89f0455..8593dc4 -- src/` **完全为空**（R10 以来全部提交
  均为 docs）。`git log -1 -- src/routing/` 显示切片最后一次生产改动
  **仍为 `183df9b`「perf(routing): land S7-C support-only eta」**。
  新收口姊妹轴 **MODEX**（R31-J）已按更新版 round-32/PLAN 并入非移植
  清单并完成划界（§4）。全部锚点/回归测量在 `337b24d` 工作树完成
  ——快进增量 `src/` 逐字节零变化 ⇒ 测量形态与提交形态位级同一，
  全部有效；聚焦套件 + `pnpm gate` 在两 tip 各跑一次均绿
  （计数逐字同）。R32-B / R32-D 在飞，按令未等待、未触碰。
- **分支:** `cursor/r32-c-offline-routing-thirty-second-pass-83a1`
- **切片:** `src/routing/{r1,r1-shadow-report,posterior,offline-logit,offline-prob-add,propensity,lin-alg,bandit,shadow}.ts`（9 文件全量重读确认，`wc -l` 合计 2,039 行，与 R29-C/R30-C/R31-C 普查同构；S7-C 支撑升序直加站点、S6-C switch 累加、S4-C/S5-C solve 提升+按四展开、S2-C 规范键 eta/mu 去重、S1-C bootstrap 工件复用全部现场核对在位）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2**（R28-C…R31-C 同引擎约定，带可比）；tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成。

## 结论

**第三十二组诚实枚举为空：不铸任何 S32-C-\* ID、不铸新 face 名、
不取 ALLCAPS 绰号、不写入 EXCLUSIONS 表行，本轮生产代码零改动。**
对 9 个源文件逐行第三十二遍扫描，对照完整排除表（X0-\*..X4-\*、
S1-C-1..10 … S10-C-1..3、S12-C-1 全系、R11-C..R31-C 的全部无号关闭
探针与轴，含 R31-C 的 **BINDX**、R30-C 的 REPRX、R29-C 的 PAIRX、
R28-C 的基片实例化、R27-C 的 PROBX、R26-C 的 PRICEX、R25-C 的
AGEX、R19-C 的 FITQ 及本波新增的 R31-J **MODEX**）后，本轮**从未
点名站点数为零（连续第十四遍）**——每一个执行站点都能指到既有
ID、无号探针、零池备忘或池算术覆盖。R26-C/R27-C 关掉输入数值坐标、
R28-C 关掉执行基片坐标、R29-C 关掉工作负载组成坐标、R30-C 关掉
输入表示坐标、R31-C 关掉名字解析坐标（BINDX——生产占位于轴最优点，
C1 capture-hoist 实尺度决斗符号翻转拒收）之后，本轮独立重推元
坐标系（R32-A §2.3 版本 + R31-J MODEX 后缀），**无任何「从未受控 +
独立物理对象 + 真尺度」三条同时成立的剩余坐标**：C 切片是进程内
纯同步数值内核，零 fs 数据路径（MODEX / R31-E codec 面结构性空，
§4 计数直证）、零 `JSON.parse` 输入位点（REPRX 划界维持）、
零 OS 边界（R31-I 审计平面正交）。本轮新增换名检查 **2 项**
（§2.2：`updatePosterior` pow 站点强度削减；bootstrap 区间
`values.sort` 数值特化排序），逐项裁决为既有 ID 族 + 亚带 /
逐位违例，不铸 ID。

按派单在本 VM 重锚默认预算（未拷贝历轮数字，装具为 R31-C 附录
A.1 逐字节血统，仅头注更新，§附录）：

- **生产中位带（本轮正典）：659.0–662.0 ms/报告**（四次运行
  660.1 / 659.0 / 659.4 / 662.0；正典三读数 659.0–662.0）——与
  R31-C 带 658.1–660.1 交叠（659.0–660.1），与 R30-C 带
  658.7–663.0 交叠；按 R14-C 处置规则随读重锚为本轮带。同 VM
  独立旁证：r7c 回归资产锚点 **production 661.0 ms = 1.12× vs
  冻结 S6-C ctl 737.8**——比率与 R28-C/R29-C/R31-C（1.12×）/
  R30-C（1.13×）逐轮基本不动。非切片回归或加速：切片对
  `183df9b` 零 diff，装具协议逐字节同 R31-C。
- **small 带 30.0–30.2 ms**（R31-C 29.9–30.2、R30-C 30.1–30.2，
  三轮带互相交叠）；整条 small 报告低于 ±35 ms 噪声带（R7-I
  矩阵义务：该档只作确定性与保真见证，不作候选判据）。
- **APCX / FLOORX / 组合天花板**：APCX 无计时器独立道正典
  60.2 / 61.7 / 61.6 ms（与 R31-C 正典带 60.4–61.8 带缘相接）；
  FLOORX 正典 40.0 / 35.1 / 36.0 ms（与 R31-C 带 33.0–41.1
  交叠）⇒ **组合天花板 = 20.2 / 26.6 / 25.6 ms < 35**——正典带
  20.2–26.6 与 R31-C 正典带 20.7–25.1、R30-C 正典带 18.8–26.6
  两两交叠，组合出路维持关闭。预演读数 29.9 ms（FLOORX 预演
  31.8 偏低所致）仍 < 35，按历轮惯例预演不入正典带，诚实注记。
- **sink = floorSink = 7.309 八读数全同**（4 运行 × 2 道，每运行
  内 `Object.is(apcSink, floorSink)` 断言全绿）——与
  R14-C/R16-C..R31-C 打印值逐位同值，跨轮确定性旁证成立。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等；每运行
  2,768 项位级检查全绿，四运行合计 **11,072 项**。

回归资产与门禁（§5）：r1c–r7c 七脚本全绿，检查数逐一匹配锚定值
**8028 / 14420 / 14730 / 24888 / 28555 / 25483 / 6193**；聚焦
离线路由套件 59/10/59/0；`pnpm gate` 在 `337b24d` 与 `8593dc4`
两 tip 各跑一次均 exit 0（tests 1169 / suites 78 / pass 1168 /
fail 0 / skipped 1，两次逐字同计数，与 R31-C/R32-A 同计数）。

硬性不变量逐条复核（§0）：双 LCB 在位（posterior.ts L22
`LcbKind = "normal" | "beta-quantile"`，本轮 rg 复核）；双归因
在位（attribution-report.ts L18–19 `logit: fitLogitAdditive(rows),
probAdd: fitProbabilityAdditive(rows)`，现场核对）；live = R0
等价（`src/supervisor/` 对 9 切片文件导入为零，本轮 rg 复核，
R1 未接线）；推广 proposal-first、不声称 Outcome-supported
（Checkpoint F-PROD 开放，ADR-005）；无阈值改动；无测试改动；
CAS/凭证/数据面合同/公开签名零触碰。仓库变更仅本报告一个文件；
无生产赢家故未提交任何临时装具（提取自 R31-C 附录的锚定装具
指纹见附录，临时文件归档后已删除）。空枚举与 R8-C..R31-C 同为
战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 读序（强制）已履行：README.md → PROGRESS.md → EXCLUSIONS.md
  （完整表，含全部 S\*-C-\* 行与 S12-C-1、S13-B-1）→
  round-32/PLAN.md（C 段 + 具名/unnamed 全 ban 榜，快进前后两版）
  → round-31/PLAN.md → R31-C.md 全文（空枚举确认，无 S31-C-\*；
  BINDX 轴定义 + C1 capture-hoist 双重拒绝 + 「名字解析坐标关闭」
  裁决域勘定）→ R32-A.md（空枚举；重开条件哨兵审计 F12a–d 按
  方法归档，**本轮不重铸该方法**）→ 9 个切片源文件全量重读 →
  `src/experiments/attribution-report.ts` 消费图现场核对 → 快进
  后补读 R31-J.md 结论（MODEX 铸造 + 三重收口）与更新版
  round-32/PLAN.md（C 段非移植清单已补 **MODEX**）。
- 基线漂移检查：本轮测量期起点 = 分支创建点 = `337b24d`；工作
  期间发现一次 docs-only 快进（`8593dc4` R31-J 合入 + R32-D
  派单），`git diff --stat` + 对 `src test scripts package.json
  pnpm-lock.yaml` 的直接 diff = **0 行**逐项核对后按派单规程
  快进报告分支并重验冻结。冻结主张三次核验均 **0 行**（切片
  9 文件 vs `183df9b`：分支创建时、快进后、提交前）；最强冻结
  `89f0455..8593dc4 -- src/` 为空。测量在字节相同的切片上完成，
  全部有效。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + 更新版
  PLAN 禁榜）：input object-representation / **REPRX**（切片
  `JSON.parse` 输入位点 0，§4 计数）/ **BINDX**（R31-C；realm 级
  绑定态——本轮零 patch、零剂量、零计时新臂；C1 capture-hoist
  不重提，其 0 ± 3.4 ms 决斗裁决在 §2.2 记录 1 作属主引用即止）/
  **MODEX**（R31-J，快进带入的最新收口——inode 权限模式字拓扑，
  J 区持久工件面；本切片数据路径零 fs 位点，面结构性空，§4
  划界后声明即止，不移植不重做）/ KFAN / COTARG / PROBX /
  DELINV / PRICEX / VALCLASS / PAIRX（工作负载组成——本轮全部
  测量单内核恒定，姊妹估计器零计时）/ R32-A 重开条件哨兵审计
  （**方法不重铸**——本轮不做「再查一遍重开条件」式产出；C 档案
  在案重开条件均要求 src/ 变更或规模增长，由 0 行冻结与锚点
  保真电池自然覆盖，不另立审计面）/ R31-I CLI 进程边界门扉闭合
  审计（本切片零 argv/env/stdin/stdout/子进程/信号/退出腿，
  审计平面正交零交集）/ digest / sink=7.309 位等不另铸轴（本轮
  sink 八读数只作既有跨轮见证复现）。FITQ / NAMESHAPE / SCALEX /
  XPROC / YMIX / SCHEDWIN / NVG / SEEDX / TERMCLASS / SHAPEK /
  ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / CG / AGEX / RTL /
  RVL / SIDEC / PWL / ZREP / CSPELL / LINKTOP 全部未编号未移植。
  R28-C 基片实例化未移植（全部测量锁死单一基片 tsx+src）；
  R30-I 时间段拓扑、R30-J LINKTOP、R31-A/B/D/F/G/H 停驻门与
  属地格未开。X\* / S1-\* … S31-\* 全部未重开；S12-C-1 重开条件
  未触发（重开条件：无）；S6-C-3/S6-C-4 重开条件（支撑分布
  大改 / 因子数增长使支撑上界 ≥ ~8）经锚点夹具形状见证未触发
  （支撑上界仍 5）；未起 S7-C 平行实现（X2-1 引理前提复核成立：
  0/1 设计、+0.0 起点、有限 beta——FLOORX 道逐名值对 APCX 道
  `Object.is` 断言全绿即为可执行见证）。
- EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
  不开 PR。输家脚本不入库（本轮无输家道——除血统锚定装具外
  零计时新臂）。

## 1. 生产中位与锚点复核（本 VM 实测，未拷贝历轮数字）

测量装具与 R31-C 附录 A.1 逐字节血统（replica 保真电池 34 例 +
自决定性 + 中位重锚 + APCX/FLOORX；仅头注更新为 R32-C，diff
即 6 行注释，指纹见附录）；Node v22.22.2，tsx + src 直连
（R28-C S 臂 = 历史正典基片）。1 次预演 + 3 次正典独立进程，
全部在 `337b24d` 工作树完成：

| 运行 | perf 中位 (ms) | small 中位 (ms) | APCX (ms) | FLOORX (ms) | 组合天花板 (ms) | sink / floorSink | 检查 |
|---|---|---|---|---|---|---|---|
| 预演 | 660.1 | 30.2 | 61.8 | 31.8 | 29.9 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 1 | 659.0 | 30.0 | 60.2 | 40.0 | 20.2 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 2 | 659.4 | 30.1 | 61.7 | 35.1 | 26.6 | 7.309 / 7.309 | 2,768 ✓ |
| 正典 3 | 662.0 | 30.2 | 61.6 | 36.0 | 25.6 | 7.309 / 7.309 | 2,768 ✓ |

perf 逐 rep（7 rep/运行）：预演 661.3 660.1 662.6 660.1 660.1
663.9 658.3；正典 1 662.6 670.6 657.5 658.1 658.5 659.5 659.0；
正典 2 661.5 660.7 659.4 656.9 659.2 658.4 660.5；正典 3 663.0
675.8 662.0 657.2 662.0 658.5 659.3。

- **生产中位带（本轮正典）：659.0–662.0 ms**。与 R31-C 带
  658.1–660.1 交叠 659.0–660.1、与 R30-C 带 658.7–663.0 交叠；
  正典 3 的 662.0 高于 R31-C 带缘 1.9 ms（正典 1/2 落 R31-C 带
  内；rep 级散布同量级——正典 3 含一个 675.8 的慢 rep，中位
  稳健），按 R14-C 处置规则**随读重锚为本轮带 659.0–662.0**。
  同 VM 独立旁证：r7c 回归资产（§5）打印 production 661.0 ms =
  1.12× vs 冻结 S6-C ctl 737.8——比率与 R28-C/R29-C/R31-C
  （1.12×）/R30-C（1.13×）逐轮基本不动。
- **small 带 30.0–30.2 ms**（R31-C 29.9–30.2 交叠）；整条 small
  报告低于 ±35 ms 噪声带，只作确定性与保真见证。
- **APCX 正典带 60.2–61.7**（R31-C 60.4–61.8 带缘相接）；
  **FLOORX 正典带 35.1–40.0**（R31-C 33.0–41.1 交叠）；
  **组合天花板正典带 20.2–26.6 < 35**（R31-C 20.7–25.1 /
  R30-C 18.8–26.6 两两交叠）。预演天花板 29.9 仍 < 35（预演
  FLOORX 31.8 偏低是单读数散布，与 R31-C 预演 FLOORX 33.0 同
  处带下缘现象族），预演不入正典带。
- 自决定性：3 seeds × 2 runs 全字段 `Object.is` 逐位相等（含
  replica 保真电池 34 例 + perf + small 对生产 import 的全字段
  断言），每运行 2,768 项检查全绿，四运行合计 11,072 项。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is(apcSink, floorSink)`）——与 R14-C/R16-C..R31-C
  打印值同值；X2-1 引理前提（0/1 设计、+0.0 起点、有限 beta）
  由 FLOORX 道逐名 `Object.is` 断言随运行复核成立。

## 2. 第三十二组枚举（诚实结果：空；零新站点 + 两项新换名检查）

### 2.1 逐文件重读与裁决（零未点名站点，连续第十四遍）

| 文件 | 本轮重读检查（全部命中既有 ID / 无号探针 / 零池） | 结论 |
|---|---|---|
| `r1.ts`（170 行） | 每调用 `modelsById` Map 构造、estimates spread、sampled/aboveFloor 双 filter、cheapest reduce = S1-C-5/S1-C-10 本体；costOf/tierIndex 线性 find = X1-4 本体（M≤10 噪声级）；prepared 观测索引 = 已落地 A2/B1；跨 episode 记忆化 = X1-6 | 无新候选 |
| `r1-shadow-report.ts`（168 行） | 每 episode request spread + routeR0 调用（B 区文件不触碰）；merge 路径 = S1-C-2（仓内不可达）+ S1-C-3（公开 ReadonlyMap 形状）；selectedCost find = X1-4 族；与 shadow-compare 合并 = X1-5；sanitizeClaims once-per-report | 无新候选 |
| `posterior.ts`（311 行） | 80 次二分 / Newton / 闭式 = X1-3；lnBeta 每 quantile 一次 = 已落地 C1；estimateFingerprint = S1-C-4；groupObservationsByKey = 已落地；betacf 守卫 Math.abs = BINDX 坐标（计数即止）；updatePosterior pow 站点 = **本轮换名记录 1**（§2.2）；零池备忘维持 | 无新候选 |
| `offline-logit.ts`（585 行） | S1-C（bootstrap 工件复用）/S2-C（规范键 eta/mu 去重）/S3-C（单位乘法消除）/S6-C（switch 直线化）/S7-C（支撑升序直加）落地形态逐站点在位；`beta.every(Number.isFinite)` = S7-C-2/S8-C-3；delta map+reduce = S2-C-3；`xtwx fill(0)` = S5-C-7；dot 展开 = S5-C-5（串行依赖）；onProbabilitiesFor = S7-C-4/S8-C-1；APC off 道全系 = S1-C-1/S2-C-2/S4-C-4/S8-C-2/S10-C-2（本轮组合天花板 20.2–26.6 < 35 维持关闭）；percentile 换 quickselect = X4-3 族；draws 循环 `.entries()` = S7-C-1；draws 预分配/pushValue = S1-C/S3-C-3 分配级；`values.sort` 数值特化 = **本轮换名记录 2**（§2.2）；尾部 reduce/Set once-per-report 亚噪声；worker/WASM = S9-C-1/S9-C-2；嵌套序交换 = S12-C-1（重开条件：无）；per-fit distinct-key 代表表 = S10-C-1 | 无新候选 |
| `offline-prob-add.ts`（254 行） | X3-4 + R17-C 零池；cell 双算合一 = S1-C-6；站点规范键去重 = S2-C-1；betaInterval 记忆化 = S1-C-7；diagnose 内 `[...entries()]` 每效应重物化 = once-per-report 零池覆盖 | 无新候选 |
| `propensity.ts`（156 行） | 三遍改单遍 = X3-5 + S1-C-8 本体；R21-C 零池维持 | 无新候选 |
| `lin-alg.ts`（88 行) | S4-C（行引用提升）/S5-C（按四展开）落地形态在位；变体空间 = S4-C-1..3 + S5-C-1..7 + S10-C-3（对称 Schur：部分主元破坏对称）+ S12-C-1 封闭；`stop=n-3` 提升 / `a.some` 手写 = S6-C-7；回代 eps 卫 = S8-C-4（fail-closed 数值卫）；回代串行链 = S5-C-5 | 无新候选 |
| `bandit.ts`（130 行） | recordReward/recordExploration 拷贝追加 = X4-2 readonly 契约；`arms.includes` / meanReward 线性 = S1-C-9（arms 个位数）；validateTaskFeatures includes = S1-C-9 族 | 无新候选 |
| `shadow.ts`（177 行） | decisions 追加拷贝 = X4-2 本体；halted / breached once-per-fault 路径 = R16-C/R18-C 拒绝类；roundUsd/预算扣减 µs 域 = S1-C-9 族 | 无新候选 |

结构位置 × 消除机制满矩阵（死码/常量折叠/CSE/记忆化/数据结构/
融合/早退/免分配/复杂度类/强度削减/惰性/平行路径/形状单态化）
逐格过筛：非空格全部落在上表既有 ID 或 §2.2 两项新记录上。

### 2.2 本轮新换名检查记录（裁决后拒列，不铸 ID）

1. **`updatePosterior` 衰减权重站点强度削减**（posterior.ts L48
   `Math.pow(2, -ageMs / config.halfLifeMs)`——预除 `1/halfLifeMs`
   为乘法、或换 `Math.exp(-LN2 * …)`、或换 `2 ** x` 运算符形态）：
   预除与换基两形态是**非逐位数值路径变更**（X2-3 族——除法→
   倒数乘法与 pow→exp∘log 均不保位）；`2 ** x` 形态逐位同值
   （V8 同一 exponentiate 下沉）但唯一收益源是删一次 ambient
   `Math.pow` 载入 = **BINDX 属地坐标**——R31-C C1 决斗已在
   23.47M 调用/报告的真实热池上把 ambient 载入定价为 **0 ±
   3.4 ms**（TurboFan 优化帧折叠），而本站点住在 R1/posterior
   零池（updatePosterior 不在 658 ms logit 报告池内，每 estimate
   一次、每报告 ~10² 次量级），比 C1 的定价域还低 5 个数量级。
   双重亚带 + 属地已有主，拒列。
2. **bootstrap 区间组装排序数值特化**（offline-logit.ts L504
   `values.sort((a, b) => a - b)` 换 Float64Array /
   `%TypedArray%.sort` 数值排序）：%TypedArray%.sort 把 **−0
   确定性排在 +0 之前**，而 `a - b` 比较器对 ±0 对返回 0（稳定
   序保留输入次序）；APC 抽样值含精确 ±0.0（参考级与无列名
   效应的贡献恒为 +0.0，抽样扰动可产生 −0.0 端点），percentile
   直读排序后端点 ⇒ 输出位可观察发散 = **R12-C 第 3 类（逐位
   违例）**。忠实形态（拷贝入 TypedArray 后按位回读并保留
   比较器语义）只省比较器调用开销：池上界 ~60 名 × ≤200 draw
   × 1 sort/报告 = µs 域，**X4-3（quickselect）同族亚带**。
   双重拒绝，拒列。

### 2.3 枚举收口

除 §2.2 两项外，第三十二遍逐文件重读**零未点名站点**。R12-C
四分类（重命名 / 亚带池 / 逐位违例 / 结构交换负优化）+ R18-C
once-per-fault 拒绝类第三十二遍无余项。**第三十二组枚举为空——
比强铸败者 ID 更诚实。**

## 3. 三大池地板与组合出路复核

- **APCX 池**（59 非截距名 × 400 行 APC 扫掠 × 201 fit）：无
  计时器独立道 0.307 / 0.300 / 0.307 / 0.307 ms/fit ⇒ 61.8 /
  60.2 / 61.7 / 61.6 ms/报告——正典带 60.2–61.7 与 R31-C 正典带
  60.4–61.8 带缘相接。
- **FLOORX 池**（S8-C-2 支撑升序 off-sum 形态，从未落地——X2-1
  引理前提本轮复核成立；逐名值对 APCX 道 `Object.is` 断言全绿）：
  0.158 / 0.199 / 0.174 / 0.179 ms/fit ⇒ 31.8 / 40.0 / 35.1 /
  36.0 ms/报告地板。
- **组合天花板 = APCX − FLOORX = 29.9 / 20.2 / 26.6 / 25.6 ms
  < 35**——正典带 20.2–26.6 与 R31-C 正典带 20.7–25.1、R30-C
  正典带 18.8–26.6 两两交叠，组合出路维持关闭；预演 29.9 距 35
  带下缘仍差 5.1 ms 且不入正典带。
- **sink = floorSink = 7.309 逐位**（8 读数 + 每运行内
  `Object.is`）——与 R14-C/R16-C..R31-C 打印值同值，派单强制的
  跨轮确定性旁证成立。
- 零池备忘维持：posterior/propensity/bandit/shadow 四文件全部
  站点在既有 ID 或无号探针覆盖下，本轮重读无新增。

## 4. 姊妹面划界（含快进带入的 MODEX；计数即止，零剂量零计时）

1. **MODEX（R31-J，本轮快进带入）**——J 区持久工件 inode 权限
   模式字拓扑面。C 切片划界计数：9 文件对
   `node:fs / readFile / writeFile / chmod / umask / mkdir /
   appendFile` 命中 **0**——切片数据路径零持久工件、零铸盘动词，
   面在 C **结构性空**（R31-E codec 面同一计数同一结论）。
   声明即止，不移植、不重做、不铸新轴。
2. **BINDX（R31-C）**——切片环境内建 token 静态普查（划界计数，
   非剂量）：`Math.*` 恰 **42 token**，其中函数内建 12 个
   （abs 9 / max 7 / log 6 / min 3 / floor 3 / exp 3 / sqrt 2 /
   imul 2 / sin 1 / round 1 / pow 1 / ceil 1）+ 值常量
   `Math.PI` ×3；`Number.isFinite` 恰 **2 站点**（offline-logit
   `beta.every` + lin-alg 终检）——与 R31-C 十二内建 + 两站点
   普查逐名同构，realm 坐标未动、零 patch、零计时。C1
   capture-hoist 不重提（§2.2 记录 1 仅作属主引用）。
3. **REPRX（R30-C）**——切片 `JSON.parse` 输入位点 **0**（全部
   输入经内存对象抵达）——划界维持，声明即止。
4. **R32-A 重开条件哨兵审计**——方法不重铸：本轮不产出「重开
   条件清单再验证」式段落；C 档案在案重开条件（S12-C-1 无重开
   条件、S6-C-3/4 支撑分布与因子规模、X2-1 引理前提）凡可执行
   者已由锚点保真电池与 FLOORX `Object.is` 断言在 §1/§3 顺带
   见证，凡要求 src/ 变更者由 0 行冻结判空——不另立审计面、
   不另开断言网格。
5. **R31-I CLI 进程边界门扉闭合审计**——本切片零 OS 进程边界
   腿（无 argv/env/stdin/stdout/spawn/signal/exit），审计平面
   正交零交集，不移植。

## 5. 测试与验证

```
# 聚焦离线路由套件（tsx --test，7 文件）
# 在 337b24d / 8593dc4 两 tip 各跑一次：
#   tests 59 / suites 10 / pass 59 / fail 0  （两次逐字同计数，
#   与 R31-C 三跑同计数）

# 全门禁 pnpm gate = typecheck + lint + test + build
# 在 337b24d / 8593dc4 两 tip 各跑一次，均 exit 0：
#   tests 1169 / suites 78 / pass 1168 / fail 0 / skipped 1
#   （两次逐字同计数，与 R31-C/R32-A 同计数；tsc build 零诊断）

# 既有回归资产全量复跑（本 VM 锚点，落地零改动仍作回归门；
# 337b24d 工作树）
npx tsx scripts/round01-r1c-equivalence-sim.ts   # ✓ 8,028 项（design.build 358,006/拟合）
npx tsx scripts/round02-r2c-equivalence-sim.ts   # ✓ 14,420 项（eta dots 3,586,400 → 1,249,652 去重）
npx tsx scripts/round03-r3c-equivalence-sim.ts   # ✓ 14,730 项
npx tsx scripts/round04-r4c-equivalence-sim.ts   # ✓ 24,888 项
npx tsx scripts/round05-r5c-equivalence-sim.ts   # ✓ 28,555 项
npx tsx scripts/round06-r6c-equivalence-sim.ts   # ✓ 25,483 项
npx tsx scripts/round07-r7c-equivalence-sim.ts   # ✓ 6,193 项
# 七脚本检查数逐一匹配锚定值 8028/14420/14730/24888/28555/25483/6193。
# r7c 锚点：production 661.0 ms（1.12× vs 冻结 S6-C ctl 737.8）——与
# §1 生产正典中位带（659.0–662.0）互证，比率与 R28-C/R29-C/R31-C
# （1.12×）/R30-C（1.13×）逐轮基本不动。

# 本轮测量装具（临时脚本，未入库——无生产赢家；来源与指纹见附录）
# 主锚 ×4：11,072 项位级检查全绿（2,768 × 4）；
#          sink/floorSink 8/8 = 7.309（进程内 Object.is）
```

冻结三验（§0）；仓库变更 = 本报告一个文件（提交前
`git diff origin/cursor/sota-persistent-opt-83a1...HEAD --stat`
复核仅 `docs/reports/sota-opt/round-32/R32-C.md`）。无生产落地
（派单条件句未触发），未铸新仿真脚本，临时装具已从 /tmp 删除。

## 6. 新增排除 ID：无

- 不铸 S32-C-\*：第三十二组枚举为空；§2.2 两项换名检查分别落
  X2-3+BINDX 属地+零池与 R12-C 第 3 类+X4-3 族，均不越 ±35 ms
  带，无任何站点达标。
- 不写入 EXCLUSIONS.md 任何行；不铸新 face 名；不取 ALLCAPS
  绰号（含对 MODEX / BINDX 的一切再编号）。
- 复检确认：本轮未触碰任何 X\* / S\* 条目状态，未重开 S12-C-1
  （重开条件：无），未起 S7-C 平行实现，未重铸 R32-A 哨兵审计
  方法。

## 附录：测量装具来源与指纹（未入库；运行后已删除）

- 装具：`/tmp/r32c/r32c-anchor.mts`，**814 行**，由本仓
  `docs/reports/sota-opt/round-31/R31-C.md` 附录 A.1 代码块
  （L555–L1368）逐字节提取，唯一改动为头部块注释 6 行更新为
  R32-C 说明（协议体、夹具、seed、断言零改动——R31-C A.1 本身
  即 R30-C/R14-C 血统逐字节协议）。
- sha256（归档时点）：
  `904bca66fc07882c4afb7bf91791e66061cdd415a416e5018ad07dc7e7c944f1`
- 运行方式（仓库根，nvm Node v22.22.2 + tsx 4.23.12，
  `pnpm install --frozen-lockfile` 后）：

```bash
node_modules/.bin/tsx /tmp/r32c/r32c-anchor.mts   # ×4（1 预演 + 3 正典，独立进程）
```

- 输出摘要（四运行）：§1 表格与逐 rep 行为全量记录；每运行尾行
  `ALL EQUIVALENCE CHECKS PASSED (2768 bitwise checks)`。
- 本轮**零新增计时臂**：无绑定/表示/组成/基片/模式剂量，无决斗
  道，无输家脚本——附录无新增源码块（装具全文已在 R31-C 附录
  A.1 归档，本轮仅头注差异，不重复占档）。

MORE_OPTIMA=no
BRANCH=cursor/r32-c-offline-routing-thirty-second-pass-83a1
