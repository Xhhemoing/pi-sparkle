MODEL_SLUG=claude-fable-5-thinking-xhigh

# R32-B：live 路由切片 Round 32 三十二搜报告

**战役:** 全库持久 SOTA 优化 Round 32 / R32-B（十区之一，R1-B…R31-B 的第三十二遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `b9ceed0`（派单 tip，R31-I 摄取 + 本次派单）→ 推送前快进摄取至 `8593dc4`。全部测量、聚焦测试与 `pnpm gate` 在 `b9ceed0` 工作树上完成（测量窗口内两次 `git fetch origin cursor/sota-persistent-opt-83a1` 均确认 tip 仍为 `b9ceed0`）；报告草成后、推送前最终 fetch 发现独占分支快进两笔至 `8593dc4`（`337b24d` 摄取 R32-A 空枚举 + 派单 R32-C；`8593dc4` 摄取 R31-J 空枚举（铸造 **MODEX**）+ 派单 R32-D），两笔均**纯 docs**（仅 `docs/reports/sota-opt/`，`src/` 零行），本分支按令 docs-only rebase 到 `8593dc4`，切片 10 文件冻结在新 tip 上重验仍 **0 行**——`src/` 工作树与测量树位级同一，全部测量数字原样成立；新铸姊妹轴 **MODEX** 与 R32-A 重开条件哨兵审计已补入 §0 非再移植清单。
**分支:** `cursor/r32-b-live-routing-thirty-second-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`

## 结论

**空枚举：无达门槛的新更优解，切片零代码改动，未铸任何 S32-B-\* ID、未铸任何新
face 名或 ALLCAPS 外号；R4-B…R31-B 的整片成本天花板经第二十九次复测成立（×3
独立运行），配置态 × 命令类矩阵全六格 + prior 双档格全部复测仍带内，且本 VM
窗口回落至 R30-B canonical 邻域（R31-B 的 +10–27% host offset 在本机不存在）。
第三十二遍透镜不重复 R31-B 的面格闭合审计（它已证缝清单闭合），改问闭合的
「稳定性」问题：一个逐字节冻结的切片里，新最优解只可能来自两处——(i) 面格
里仍有从未受控的坐标（R31-B 已证无），或 (ii) 某个已关闭裁决的成立前提随
环境漂移而失效（重开条件被静默触发）。本轮据此执行**闭合稳定性审计
（closure-stability audit）**：把切片全部 54 行既有排除裁决（S1-B-1…S13-B-1
共 53 行 + X3-1）按其成立依据分解为三类腿——契约/语义/身份腿（K，环境无关）、
量级/噪声腿（M，由天花板界定）、引擎实测符号腿（E，V8 版本契变）——并逐类
验证当前环境下至少一条腿仍然成立：K 腿由四点冻结证据直接携带；M 腿由本轮
×3 天花板复锚携带（全部格仍带内或更快，收益池无一变大）；E 腿由引擎同一性
携带（本轮 canonical 引擎 Node 22.22.2 / V8 12.4.254.21-node.39 与做出这些
裁决的 R30-B/R31-B 及近轮 canonical 完全相同）。54/54 行全部至少一腿成立，
无一裁决重开（§2e）。审计同时枚举出五扇此前从未作为独立问题问过的残门
（§2d D1–D5），全部按归属关闭：其中唯一携真实流量的新问题 D1（决策记录
字段活性普查）以源码普查决定性闭合——RoutingDecision 20 个字段中 19 个在
MODEL_ROUTED 事件序列化边界（`run/events.ts` `toModelRoutedPayload` +
`routingContextFields` 全字段拷贝 + 双侧验证器逐字段断言）或 supervisor 活
控制流（justification→rationale、statusAfterRoute/approvalPlan 门控、
eligibleModels→级联层级、modelVersion→learning 信号）被消费，恰一个生产死
字段 `preferredConstraint`（test-asserted 公开可选字段，构造成本为一次条件
spread ~ns 级）；观察式定价把**整个**决策记录构造份额（justification 模板 +
makeApprovalPlan + oneHotDistribution + 两次条件 spread）界定在 158.9–163.3
ns/任务 = 0.64–0.65 ms/eval@N=2000×2——即便全部字段皆死也低于落地线约 46×，
而实际死份额只有一次 ~ns 条件 spread；对「消费者读哪些字段」做剂量则是
R30-F boundary output-consumption topology 的跨区移植（明令不移植）。**

关键前提事实：切片 10 个文件（清单逐一核对恰为 10，`wc -l` 合计 1359 行，
逐文件计数见 §0）自 R1-B 的裁决基线 `94ed3d9` 以来**逐字节零变化**
（`git diff 94ed3d9..HEAD -- <切片 10 文件>` 为 0 行，派单 tip `b9ceed0`、
报告草成时与 rebase 到 `8593dc4` 后三点核对；期间快进两笔均纯 docs，
`src/` 零行，故三点覆盖全程）。R1-B 的结构下界论证、R4-B 的聚合天花板
论证与 S1-B-\* … S13-B-1 全部裁决对当前代码原样成立。

天花板本轮实测复核为 **9.04–9.62 ms/eval（M=2）/ 17.84–18.43 ms/eval
（M=10）**（引擎 Node 22.22.2 / V8 12.4.254.21-node.39，×3 独立运行）。本 VM
相对 R31-B 窗（9.25–11.46 / 21.02–23.17）整体回落：M=2 run 2 的 9.04 落回
R30-B canonical 带（8.87–9.13）之内，M=10 runs 2–3（18.09 / 17.84）亦在
R30-B 带（17.34–18.23）之内，run 1 的 18.43 仅高出带上缘 +1.1%——R31-B 记录
的 +10–27% 主机偏置为该 VM 特有，本机不存在。**回归不可能成立**：切片对
`94ed3d9` 逐字节零 diff，任何 Δ 只能来自主机；W1（双跑逐字节恒等）/ W2
（learned 臂仅在 avoid/prefer 命中族上分歧）/ W3（prior 拾取存在性）三组行为
证人 ×3 运行全过，语义与 R30-B/R31-B 恒等。结构性结论逐条成立：全 18 个矩阵
格 ×3 运行保持全序 M=2 < M=7 < M=10、learned ≥ no-learned 逐对成立、全部
生产规模格低于 30 ms 落地线（holds-below-landing-line=true ×3）、
replay-faithful **6.89–7.09 ms/eval**（与 R22-B 带 6.93–7.10 相交）、10×
压力 **96.1–98.9 ms/eval**（历史并集带 94.8–140.9 之内）、live face
**47.2–50.5 µs**（R14-B 带 47.1–50.4，run 1 的 50.5 邻接 +0.2%）。配置态
矩阵：M=7 configured+learned **14.19–14.53 ms/eval**、M=10 stress+learned
**18.20–18.76 ms/eval**。prior 双档格逐格复现（第二十一次跨 VM）：假设
N=2000 档 M=2/7/10 = 36.12–36.54 / 52.10–55.18 / 78.06–80.09 ms/eval（仍为
「若有流量即越线」邻接配置，今日流量仍为零——`eval-routing.ts` 全文 `prior`
计数本轮重新取证为 0，:353–354 只传 learned）；真实档 live face M=2+prior
189.2–191.8 µs、M=7+prior+learned 289.6–299.2 µs/批（与 R30-B 带 290.1–296.9
相交）；单拾取单价 19648–19881 ns（历史带 19.5–21.8 µs 之内）。级联探针
526–538 / 335–346 ns（decide 侧 335–345 落于历史带 321–345，346 邻接
+0.3%）。**S12-B-2 重开条件腿 (a)（任何 N≥10³ 调用方传 prior）本轮重新取证
未触发；腿 (b) 维持 R21-B 定价，按指令不补铸不重扫——条件式落地物维持在架。
S13-B-1 重开条件（applyLearnedRouting 本体成本增 ≥2 个量级）未触发——
`learning/learned-routing.ts` 本轮对 `94ed3d9` 逐字节 diff 为空（0 行）。**
落地线（数十~数百 ms 或复杂度类下降）在现有生产流量下依旧不可达。

## 0. 范围与约束遵守

- **切片恰 10 文件**（逐文件 `wc -l`）：`r0.ts` 173 / `assign.ts` 129 /
  `assign-plan.ts` 68 / `policy.ts` 160 / `live-cascade.ts` 118 /
  `live-selection.ts` 61 / `analyze-task.ts` 108 / `primary-catalog.ts` 101 /
  `catalog-model.ts` 83 / `supervisor/model-router.ts` 358，合计 1359 行。
  `catalog-observed.ts` **不在**本切片（按令未触碰、未计时）。
- **冻结核对全过**：派单 tip `b9ceed0` 上
  `git diff 94ed3d9..HEAD -- <切片 10 文件>` = **0 行**；报告草成时再次
  fetch 同 diff 仍 0 行；推送前 rebase 到快进后 tip `8593dc4` 上第三次
  核对仍 **0 行**（快进两笔 `337b24d` / `8593dc4` 逐一核对为纯
  `docs/reports/sota-opt/` 变更，`src/` 零行）。`package.json` /
  `pnpm-lock.yaml` vs `94ed3d9` 亦 **0 行**。最后一次生产 live-B 变更维持
  `94ed3d9`；最后一次 `src/routing/` 生产变更维持 `183df9b`（S7-C，离线 C）。
- **读序（按令完成）**：README → PROGRESS → EXCLUSIONS 全表 → round-32
  PLAN 全文（B 段 + 具名/不具名禁令）→ round-31 PLAN + R31-B + R31-I 全文
  （闭合审计方法钉）→ 10 切片文件全量实读（1359 行）。R30-B scale-cube
  方法钉经 R31-B §2d C4 复述取用（δ_ML 符号翻转、可分性证人已定价）。
- **摄取状态**：测量窗口内独占分支零快进（两次 fetch 均 `b9ceed0`）；
  推送前最终 fetch 发现快进两笔至 `8593dc4`——R32-A 已回（空枚举；重开
  条件哨兵审计 F12a–d，未铸面）、R31-J 已回（空枚举；铸造 **MODEX** /
  durable-artifact access-authority / inode-metadata mode topology），并
  派单 R32-C / R32-D。两笔均纯 docs，本分支 docs-only rebase 干净落位，
  冻结三验 0 行；新姊妹轴 **MODEX** 与 R32-A 哨兵审计已补入下方非再移植
  清单。按令未等待 J/A——其自行返回并由父代摄取。
- **既有不移植清单维持**（枚举时逐条遵守，本轮零触碰）：R30-B scale-cube
  second-order interaction face、多任务并发规模立方（R21-B…R29-B 族）、
  **SCALEX / ORDX / AGEX / YMIX / SHAPEK / REPRX / LINKTOP / BINDX** 及全部
  具名禁令（FITQ、NAMESHAPE、XPROC、SCHEDWIN、KFAN、NVG、SEEDX、TERMCLASS、
  CKMIX、CALLB、OCCX、JB、RUNLIM、VALCLASS、CG、COTARG、PRICEX、RTL、
  DELINV、PROBX、RVL、SIDEC、PWL、ZREP、PAIRX、CSPELL）；R31-B 六扇停驻门
  （catalog-row hidden-class / IC-shape、cascade evidence×failure-class
  lattice、assignTasks cold-onset segmentation、batch class-composition
  mix、LINKTOP-in-B、prior alias-resolution pathway——本轮 D4/D5 的裁决
  显式引用而不重开）；R31-C **BINDX**（切片 primitive 调 Math.\* 是同一
  坐标，不当轴用）；R31-E I/O text-codec topology / per-run transcode-byte
  manifest（切片无 fs 缝，R31-B §0 已证 `node:fs` 引入计数 0）；R31-A
  injected-callable 停驻门（切片公共签名只收数据值，R31-B §0 已证零函数值
  参数穿缝）；R31-D residual monitor 配置平面（R22-D 属地）；R31-F 故障
  通道处置残格（分解归属——本轮 D2 显式引用）；R31-G / R31-H 审计记录；
  **R31-I CLI 进程边界门扉闭合审计**（本切片纯内存计算零 spawn/stdin/
  signal 位点，无「谈 git / 开子进程」坐标可重铸）；**R31-J MODEX**
  （durable-artifact access-authority / inode-metadata mode topology——
  快进摄取后补入：本切片零 fs 写入位点、不铸盘任何持久工件（`node:fs`
  引入计数 0，R31-B §0 已证），无权限模式字坐标可重铸）；**R32-A 重开
  条件哨兵审计（F12a–d）**（快进摄取后补入：不把「再查一遍重开条件」铸成
  新面——本报告 §2e 的闭合稳定性审计为 B 局部裁决腿分类（K/M/E 三类
  54 行逐行验腿），其中 S12-B-2 / S13-B-1 哨兵复核系本次派单硬保持项
  明令要求；两者均未铸面、未铸 ID，与 A 区方法记录并行且互不引用为面）。
- **B 区自有封禁面不补铸**：R29-B learned-policy container face / R28-B
  catalog-face row-arrangement (permutation) face / R27-B batch-cardinality
  bottom + prelude intercept face / R26-B privacy / provider-policy
  constraint-leg face / R25-B task-face optional-field presence topology /
  R24-B M=1 catalog collapse。目录行预排序仍是行为变更（R28-B W3 契约决策
  承载），本轮未尝试。
- **硬不变量**：切片零代码改动 ⇒ Live=R0 等价（R1 未接线 live；稀疏回退
  approved R0 baseline）、双 LCB（Beta+正态）、双归因（logit+prob-add）、
  proposal-first（Checkpoint F-PROD 开放，ADR-005，不声称
  Outcome-supported）、阈值、CAS/凭证/数据面契约/公共签名全部原样保持；
  live 面不 import R1/bandit/shadow 由 `live-isolation.test.ts` 在聚焦
  198 项中复证通过。S5-H-1 / S7-F-1 ≠ S6-F-5 / S8-I-1 / S9-D-4 / S12-D-1 /
  S8-E-1 / J1 全部未触碰。npm POSIX `.bin` symlink 静默 no-op 未当优化修。
  分析未变更任何在飞 run；tracking 无命令权；H/score 不写 routing
  PASS/FAIL。
- **EXCLUSIONS.md 未增行**（空枚举轴昵称按令不入排除表，plan-ban 归属
  父代）；仓库变更恰为本报告单文件（`git diff
  origin/cursor/sota-persistent-opt-83a1...HEAD` 复核仅
  `docs/reports/sota-opt/round-32/R32-B.md`）。

## 1. 第三十二遍搜索方法：闭合稳定性审计

R31-B 的面格闭合审计已证：输入面（任务/目录/learned/prior）、引擎态、调用
拓扑与规模立方全部有主，剩余未填格全部是封禁面换名、姊妹移植或已锚定格的
仿射派生。在切片逐字节冻结的前提下，第三十二遍如果照抄同一审计只会得到同
一张表。本轮据此换问题：**闭合结论本身依赖哪些环境前提？这些前提在当前
tip / 当前引擎 / 当前调用方图景下是否仍然全部成立？** 方法为：

1. 重列生产调用方图景——`rg 'assignTasks\(' src/` 恰 3 个调用方
   （`cli/main.ts:365` smartChildPlan live face N≤30、
   `adaptation/eval-routing.ts:353–354` 只传 learned、
   `track/primary-split.ts:41` C 小批），与 R30-B/R31-B 报告逐条一致，
   无新调用方、无新可选参数（S12-B-2 腿 (a) 未触发的取证同源）。
2. 把 54 行既有裁决（S1-B-1…S13-B-1 + X3-1）按成立依据分解为 K/M/E 三类
   腿，逐类给出携带证据（§2e）；一行裁决只有当**所有**腿同时失效才重开，
   本轮无一行发生。
3. 对「从未作为独立问题问过」的残余角落做第三十二遍枚举——得五扇门
   （§2d D1–D5），每扇按 R31-B 三问（是否已受控 / 是否有切片可控独立物理
   身位 / 是否有数十 ms 真实规模潜力）裁决，全部关闭，零幸存。
4. 测量侧不铸新剂量网格，只重锚既有格 ×3 独立运行 + W1/W2/W3 行为证人；
   本轮唯一新增测量是 D1 的**观察式**构造份额定价（复刻 buildDecision 的
   构造位点定价其总池上界，无任何剂量变量，不扫消费者拓扑）。

## 2. 天花板复测：R4-B…R31-B 收口第二十九次复核成立

引擎 Node v22.22.2 / V8 12.4.254.21-node.39（canonical，nvm 显式切换）。
语料种子 0xb44b01 逐字节复用（R4-B 起原封）。×3 独立进程运行，全部
ALL CHECKS PASSED。单位 ms/eval（×2 调用口径，与 R4-B…R31-B 同）：

| 格 | run 1 | run 2 | run 3 | 历史参照 |
|---|---|---|---|---|
| eval-replay N=2000 M=2 | 9.62 | 9.04 | 9.47 | R30-B 8.87–9.13（run 2 带内）/ R31-B 9.25–11.46 |
| eval-replay N=2000 M=10 | 18.43 | 18.09 | 17.84 | R30-B 17.34–18.23（runs 2–3 带内；run 1 +1.1%） |
| analyzeTask 份额 | 2.20 (23%) | 2.23 (25%) | 2.22 (23%) | 恒 ~20–25% |
| replay-faithful M=2 | 7.02 | 6.89 | 7.09 | R22-B 6.93–7.10 相交 |
| 10× 压力 N=20000 M=2 | 98.9 | 96.1 | 97.9 | 历史并集 94.8–140.9 之内 |
| CLI live face N=30 M=2（µs/调用） | 50.5 | 47.2 | 48.9 | R14-B 47.1–50.4（runs 2–3 带内；run 1 +0.2%） |

M=2 全序、M=10 全序、analyzeTask 份额、压力格与 live face 的结构关系逐运行
成立；`holds-below-landing-line=true` ×3。R31-B 的 +10–27% 主机偏置在本机
不存在（窗口回落 canonical 邻域）；W1/W2/W3 行为证人 ×3 全过 ⇒ 输出语义与
R30-B/R31-B 逐字节同构。

## 2b. 配置态 × 命令类矩阵：全六格复测（第二十三次全格复核）

replay N=2000，ms/eval（×2 口径）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| M=2 default, no learned | 7.53 | 7.11 | 7.33 |
| M=2 default, learned | 7.53 | 7.48 | 7.62 |
| M=7 configured, no learned | 13.86 | 13.39 | 14.48 |
| M=7 configured, learned | 14.41 | 14.19 | 14.53 |
| M=10 stress, no learned | 17.46 | 17.35 | 17.63 |
| M=10 stress, learned | 18.76 | 18.20 | 18.44 |

全 18 格保持 M=2 < M=7 < M=10 全序与 learned ≥ no-learned 逐对关系（run 1
M=2 对以 3764.8 vs 3765.6 µs/调用分辨，仍为 learned ≥）。CLI live face
M=7 configured+learned = 107.2 / 105.3 / 102.8 µs/调用（R31-B runs 2–3
109.6/113.5 邻域偏快，同主机回落方向）。级联探针：
`liveCascadePlanFromAssignment` 526–538 ns、`decideLiveCascade` 335–346 ns
（335–345 落于历史带 321–345，346 邻接）。W1 双跑（N=2000, M=10, learned）
逐字节恒等 ×3；W2（learned 臂仅在 avoid/prefer 命中族 review/plan 上分歧、
其余族模型逐项恒等）×3 全过。

## 2c. prior 双档格复测（第二十一次跨 VM；S12-B-2 重开腿 (a) 复核）

真实快照 56 行（R12-B 起逐字节复用，id 重标 `pps_r32b_realistic_v1`）：

| 格 | run 1 | run 2 | run 3 |
|---|---|---|---|
| 假设档 N=2000 M=2 + prior（ms/eval） | 36.15 | 36.12 | 36.54 |
| 假设档 N=2000 M=7 + prior | 52.72 | 52.10 | 55.18 |
| 假设档 N=2000 M=10 + prior | 80.09 | 79.74 | 78.06 |
| 真实档 live face N=30 M=2+prior（µs/调用） | 189.2 | 190.4 | 191.8 |
| 真实档 N=30 M=7+prior+learned（µs/调用） | 289.6 | 292.1 | 299.2 |
| 单拾取 pickFromPublicPrior（ns） | 19648 | 19881 | 19807 |

假设档三格仍全部越线（>30 ms/eval）——维持「若有流量即越线」邻接配置归档；
生产流量本轮重新取证仍为零：`rg -c 'prior' src/adaptation/eval-routing.ts`
= 0 匹配，:353–354 `assignTasks({ catalog, tasks, learned: … })` 只传
learned。**S12-B-2 腿 (a) 未触发**，条件式落地物维持在架，按令不补铸不重扫
（R21-B 快照形状轴维持关闭）。W3（三种 M 下 family=edit 拾取存在）×3 全过。
真实档 M=7+prior+learned 与 R30-B 带 290.1–296.9 相交。

## 2d. 第三十二遍枚举：五扇残门逐扇关闭

**D1. 决策记录字段活性普查（decision-record field-liveness census）**——
本轮唯一从未问过且携真实流量的新问题，普查后决定性关闭（无铸）。问题：
`buildDecision` 每 route 构造 20 个字段（含两条模板串、approvalPlan 字面量、
one-hot Record、两次条件 spread）——其中有没有生产从不读取的死构造池？
普查裁决三条：(i) **19/20 字段活**——`run/flowchart-run.ts:138–153`
`toModelRoutedPayload` + `run/events.ts:372–406` `routingContextFields` 把
taskId/role/complexity/model/justification/confidence/approvalPlan/
statusAfterRoute/policyVersion/estimatedCostUsd/estimatedDurationMs/family/
featureVersion/modelVersion/highRisk/eligibleModels/rejections/
behaviorDistribution/coldStartRoutingScore（+agentRole）**全部**拷入持久化
MODEL_ROUTED payload（`flowchart-run.ts:601/786`、`coordinator.ts:298`
三个 append 位点），且 `events.ts:609–673` 与 `flowchart-snapshot.ts:
142–193` 双侧验证器逐字段断言（justification 非空、statusAfterRoute 枚举、
behaviorDistribution one-hot over eligibleModels…）——序列化边界即活性
边界；活控制流侧另有 `flowchart-supervisor.ts:665/709/750–751`
（justification→rationale、statusAfterRoute/approvalPlan 门控）、
`live-cascade.ts:37`（eligibleModels→级联层级）、`from-episode.ts:261–282`
与 `signals.ts:144–151`（modelVersion/eligibleModels→learning 信号）。
(ii) 恰一个生产死字段：`preferredConstraint`（`model-router.ts:308` 产，
src/ 零消费者，仅 `test/unit/routing/catalog-eligibility.test.ts` 断言）
——公开可选字段 + test-asserted 契约，删除 = 公开类型收窄（X0-4 /
S8-A-3 族），构造成本为一次条件 spread ~ns。(iii) 量级：观察式复刻定价
（附录 A §0d，无剂量）把**整个**构造份额界定在 **158.9–163.3 ns/任务 =
0.64–0.65 ms/eval@N=2000×2**——即便 20 字段全死也距 30 ms 落地线 ~46×，
实际死份额仅 ~ns。且任何「按消费者读集分层」的剂量设计正是 R30-F boundary
output-consumption / snapshot-readback topology 的物理对象（不移植）。
**普查闭合 + 属地 + 量级三重关，不铸面。**

**D2. 拒绝/故障对象通道（RoutingRefusalError / DomainValidationError 构造
与处置）**——关（既有属地 + 零锚定流量）。切片 throw 位点普查：
`model-router.ts` 8 处 DomainValidationError + 1 处 RoutingRefusalError、
`primary-catalog.ts` 2 处、`catalog-model.ts` 1 处。拒绝路径单价与尾部
分布是 **R17-B** 已锚定格；抛出后调用方处置分解是 **R31-F** 已按
R17-F + R30-F + R29-F + 引擎面归属的残格（明拒重开）；锚定语料三运行
零 throw（refusal 记录数组属被定价天花板本体，非故障通道）。无新坐标。

**D3. 每调用比较器/闭包分配（sort 内联箭头、cheapFirstTiers 三连箭头）**
——关（既有排除族 + ns 档）。装饰-排序-还原与比较器展开已由 S3-B-2
（39–201ns）/ S8-B-3（~10ns + 总序契约双份）/ S1-B-5 关闭；闭包分配本体
是这些裁决定价的组成部分，M≤10 下无独立池。

**D4. behaviorDistribution 载体形状（Record → null-proto / Map）**——关
（契约 + 停驻门 + 先例）。one-hot Record 是被 `isBehaviorDistribution`
（events.ts:352–370）与 `isOneHotDistribution`（flowchart-snapshot）双侧
Object.keys 遍历断言的公开载体形状；改载体 = 数据面字节/形状发散（S4-G-6
族）；对其做隐藏类/表示剂量 = R31-B C1 停驻门 + REPRX 移植；null-proto
先例 S6-F-4 稳定负优化（慢 38–45%）。三重关。

**D5. ESM live-binding 间接层（切片 9 条跨模块 import 边的绑定单元读）**
——关（BINDX 证据连带 + once-per-process）。R31-C 的实尺度决斗已证
TurboFan 在优化帧折叠 ambient/模块装载（capture-hoist 决斗符号翻转），
ESM 绑定单元读在热路径同理被折叠；模块解析/实例化成本是 once-per-process
CLI 噪声段（S5-I-1 属地，且 R11-G 已证 B 切片边被 main.ts 静态钉死）；
对 realm 绑定态做剂量 = **BINDX**（明令禁）。零切片可控独立对象。

五扇门全关 ⇒ 第三十二遍枚举为空。

## 2e. 闭合稳定性审计：54 行裁决三腿分解与携带验证

54 行 = S1-B-1..8、S2-B-1..4、S3-B-1..6、S4-B-1..5、S5-B-1..4、S6-B-1..5、
S7-B-1..6、S8-B-1..4、S9-B-1..4、S10-B-1..3、S11-B-1、S12-B-1..2、
S13-B-1（53）+ X3-1（assign.ts catalog 重过滤换索引）。按**主导腿**分类
（混合行以最强腿计，任一腿成立即裁决维持）：

| 腿 | 行数 | 成员 | 本轮携带证据 |
|---|---|---|---|
| K 契约/语义/身份（环境无关） | 17 | S1-B-8、S2-B-1、S3-B-3、S3-B-6、S4-B-3、S4-B-5、S6-B-2、S6-B-4、S6-B-5、S7-B-1、S7-B-3、S7-B-5、S8-B-4、S9-B-4、S12-B-2、S13-B-1、X3-1 | 冻结 0 行 ⇒ 语义/契约/身份论证原样成立；S12-B-2 流量门与 S13-B-1 重开门本轮显式重取证未触发（§2c、§0） |
| M 量级/噪声（天花板界定） | 24 | S1-B-1..5、S2-B-2、S2-B-4、S3-B-2、S3-B-4、S4-B-2、S4-B-4、S5-B-1..4、S6-B-1、S7-B-6、S8-B-1、S8-B-3、S9-B-1、S9-B-2、S10-B-1..3 | ×3 天花板复锚全部格带内或更快（§2/§2b/§2c）⇒ 各池上界不增；µs/ns 拒绝线未动 |
| E 引擎实测符号/负优化（引擎契变） | 13 | S1-B-6、S1-B-7、S2-B-3、S3-B-1、S3-B-5、S4-B-1、S6-B-3、S7-B-2、S7-B-4、S8-B-2、S9-B-3、S11-B-1、S12-B-1 | 本轮 canonical 引擎 = V8 12.4.254.21-node.39，与做出 R9-B…R31-B 全部近期 E 类裁决的引擎位级相同 ⇒ 符号裁决直接携带；早期 E 行（S1-B-6/S1-B-7/S2-B-3）的结构已被现代引擎上的 S12-B-1 M 网格决斗（Set vs includes 双向封死）与 S8-B-2/S3-B-5 builtin 反例系列重见证 |

**结论：54/54 行至少一腿成立，无一裁决重开。** 特别地：R14-B/R15-B 已把
「引擎代际」当受控轴归档——若未来 canonical 引擎升级，E 腿需按该轴重验，
这是本审计显式留给后续轮的唯一环境敏感点（当前未触发）。

## 3. 候选总表

| # | 候选 | 裁决 | 类别 |
|---|---|---|---|
| D1 | 决策记录字段活性普查 | 关 | 19/20 字段活于序列化边界；1 死字段 ~ns + 契约；剂量化 = R30-F 移植；池上界 0.64–0.65 ms/eval |
| D2 | 拒绝/故障对象通道 | 关 | R17-B 已锚 + R31-F 分解属地 + 锚定流零 throw |
| D3 | 每调用比较器/闭包分配 | 关 | S3-B-2 / S8-B-3 / S1-B-5 族既有定价，ns 档 |
| D4 | behaviorDistribution 载体形状 | 关 | one-hot 双侧契约 + R31-B C1 停驻 / REPRX + S6-F-4 负先例 |
| D5 | ESM live-binding 间接层 | 关 | BINDX 折叠证据连带 + once-per-process + realm 剂量明禁 |
| — | 54 行裁决稳定性 | 全部维持 | K/M/E 三腿分解，54/54 至少一腿成立（§2e） |

铸 ID 数：**0**。铸 face 名 / ALLCAPS 外号数：**0**。EXCLUSIONS.md 增行数：
**0**。

## 4. 关键裁决细节

- **S12-B-2 换名检查**：假设档 N=2000 三格重测越线依旧（§2c），腿 (a)
  重新取证未触发（eval-routing 零 prior、调用方图景三处未变）。未以任何
  新名重扫快照形状轴。
- **S13-B-1 换名检查**：`git diff 94ed3d9..HEAD --
  src/learning/learned-routing.ts` = 0 行，本体成本无变化，重开条件
  （≥2 量级）未触发。learned 容器坐标归 R29-B（封禁），本轮未触碰。
- **目录预排序**：R28-B W3 裁定行序为契约决策载体，预排序 = 行为变更，
  本轮未尝试；D4 的载体形状裁决与其相互独立、互不重开。
- **D1 与 R25-B 划界**：R25-B 是**输入**任务面可选字段存在拓扑；D1 问的
  是**输出**决策记录字段的消费活性——普查证明活性边界与序列化边界重合，
  故不存在可剂量的输出侧存在拓扑；若强行按消费者读集分层则落 R30-F
  （移植，拒）。两面无重叠、无重开。
- **主机窗口**：本 VM 回落 canonical 邻域（M=2 run 2 带内、M=10 runs 2–3
  带内），R31-B 的 +10–27% 偏置确认为该 VM 特有主机效应而非漂移趋势——
  这本身是 §2e E 腿携带论证的又一佐证（同引擎跨主机，符号与结构不变）。

## 5. 逐文件收口（第三十二遍透镜下的残余检查）

- `r0.ts` / `policy.ts` / `live-selection.ts`：纯函数约束-排序核，throw
  位点归 D2（关）；R1-B 结构下界原样成立。
- `assign.ts` / `assign-plan.ts`：调用方图景三处未变（§1）；learned/prior
  注入点维持 R31-B C4/C6 停驻裁决。
- `live-cascade.ts`：ns 档（§2b 探针带内）；R31-B C2 裁决维持。
- `analyze-task.ts`：份额稳定 23–25%，L 轴归 R20-B（线性收口）。
- `primary-catalog.ts` / `catalog-model.ts`：单点构造器消灭形状多态
  （R31-B C1）；throw 位点归 D2。
- `supervisor/model-router.ts`：D1 普查主对象——buildDecision 构造份额
  0.64–0.65 ms/eval 上界，19/20 字段活性证毕；`createModelRouter` 每 run
  一次（CLI 噪声档），无热路径残余。

## 6. 前后对比与测试

切片零代码改动，无前后对比。验证：

- 聚焦 live 路由单测：`test/unit/routing/*.test.ts` —— **198/198 pass**
  （18 suites，含 live-isolation / cascade-evidence / failure-class /
  assign / assign-plan / analyze-task / r0 / live-cascade /
  live-selection / catalog-eligibility 等），与 R31-B 计数逐字相同。
- `pnpm gate`（typecheck + lint + test + build）：**exit 0** —— 1169 项
  测试 1168 pass / 0 fail / 1 skipped（既有跳过项，非本轮引入），tsc
  build 通过；计数与 R9-B…R31-B 逐字相同。
- 仿真 ×3 独立运行 ALL CHECKS PASSED（W1/W2/W3 逐运行全过）。
- 报告提交前最终 fetch：独占分支快进两笔至 `8593dc4`（均纯 docs，
  `src/` 零行）；docs-only rebase 干净落位，新 tip 上冻结第三验 0 行，
  `git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 恰为本报告
  一个新文件。
- 无赢家故 scripts/ 零新增；harness 全文进附录 A（sha256
  `b72e7f07b31bf61c954b44132dac6216e2f557ed2b1dbf1563ee7426fff22efe`），
  临时文件按纪律核后删除。

## 7. 新增排除 ID

无。未铸任何 S32-B-\* ID；EXCLUSIONS.md 零增行（五扇门与稳定性审计属
空枚举记录，昵称不入排除表，plan-ban 归属父代）。

## 附录 A：测量仿真脚本（完整，可复现）

`/tmp/r32b-sim.mts`（不提交仓库；`node --import tsx /tmp/r32b-sim.mts`
于 repo 根、空闲 VM、Node 22.22.2 运行）：

```ts
/**
 * R32-B deterministic measurement simulation (thirty-second pass).
 * No fresh candidate cleared the rename-reject audit this round (the closure-
 * stability audit found no independent physical object left and no reopened
 * verdict — see report §2d/§2e), so this is a measurement-only harness plus
 * fidelity assertions:
 * 1) Re-anchors the R4-B..R31-B aggregate slice ceiling (corpus seed 0xb44b01
 *    reused verbatim), replay-faithful token-free, the FULL configured-state x
 *    command-class matrix ({M=2 default, M=7 configured, M=10 stress} x
 *    {no learned, learned}), CLI live face, per-child cascade probes, and the
 *    R12-B prior cells (real N=30 + hypothetical N=2000 + per-pick unit cost;
 *    S12-B-2 reopen-condition leg (a) re-anchor).
 * 2) NO new dosed grid: the thirty-second-pass audit is a closure-STABILITY
 *    audit (R31-B closed the face lattice; this pass verifies no closed
 *    verdict has silently reopened via engine/caller/flow drift, and closes
 *    five residual doors — report §2d). The only additions over R31-B are:
 *      (W1) double-run byte identity at production scale (N=2000, M=10,
 *           learned) — standard determinism contract witness;
 *      (W2) matrix-cell output consistency: the "learned" arm differs from the
 *           "no learned" arm ONLY where applyLearnedRouting can act (family
 *           avoid/prefer hits); eligible sets stay within catalog order;
 *      (W3) prior-cell pick existence for family=edit at every M (R12-B
 *           anchor precondition);
 *      (D1 probe) an OBSERVATIONAL replica pricing of the decision-record
 *           construction share (justification template + makeApprovalPlan +
 *           oneHotDistribution + preferredConstraint conditional spread),
 *           shaped exactly like buildDecision's construction sites, to bound
 *           the field-liveness door pool quantitatively (nothing is dosed;
 *           consumer-read topology is R30-F territory and is NOT swept).
 * Seeded PRNG (mulberry32) -> corpora fully reproducible.
 * Run: node --import tsx /tmp/r32b-sim.mts   (from the repo root, idle VM)
 */
import { performance } from "node:perf_hooks";
import { analyzeTask } from "/workspace/src/routing/analyze-task.js";
import { assignTasks, type AssignableTask } from "/workspace/src/routing/assign.js";
import { type LearnedRoutingPolicy } from "/workspace/src/learning/learned-routing.js";
import { catalogFromPrimary } from "/workspace/src/routing/primary-catalog.js";
import { type CatalogModelInput } from "/workspace/src/routing/catalog-model.js";
import {
  liveCascadePlanFromAssignment,
  decideLiveCascade
} from "/workspace/src/routing/live-cascade.js";
import { planAssignmentPolicy } from "/workspace/src/routing/assign-plan.js";
import {
  pickFromPublicPrior,
  type PublicPriorSnapshot
} from "/workspace/src/routing/public-prior.js";
import {
  createModelRouter,
  type ModelRouterConfig
} from "/workspace/src/supervisor/model-router.js";

let failures = 0;
function check(name: string, ok: boolean, detail?: string): void {
  if (!ok) {
    failures += 1;
    console.error(`FAIL ${name}${detail === undefined ? "" : `: ${detail}`}`);
  }
}
function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) >>> 0;
    let t = a;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
function pick<T>(rng: () => number, items: readonly T[]): T {
  return items[Math.floor(rng() * items.length)] as T;
}
function bench(fn: () => void, reps: number): number {
  fn();
  const t0 = performance.now();
  for (let i = 0; i < reps; i += 1) fn();
  return (performance.now() - t0) / reps;
}

const ROLES = [
  "worker", "scout", "planner", "implementer", "reviewer", "tester", "debugger"
] as const;
const OBJECTIVE_TEMPLATES = [
  "Survey the payment module",
  "Plan the checkout migration",
  "Run the unit tests",
  "Deploy payment credentials to production",
  "Implement retry logic for the ledger sync",
  "Review the audit log formatting nits",
  "Refactor and rename the tracking roller",
  "Fix the flaky spec for gate transitions",
  "Research and compare vector store options",
  "Investigate why the drop table migration failed in prod",
  "验证路由细节并补充测试",
  ""
];
const FRAGMENTS = [
  "deploy", "production", "credentials", "review", "tests", "coverage", "plan",
  "design", "survey", "research", "refactor", "implement", "add ", "fix ",
  "migrate", "verify", "\n", "the module", "碎片", " "
];
function genObjective(rng: () => number): string {
  if (rng() < 0.5) return pick(rng, OBJECTIVE_TEMPLATES);
  let text = "";
  const parts = 1 + Math.floor(rng() * 8);
  for (let i = 0; i < parts; i += 1) text += pick(rng, FRAGMENTS) + " ";
  return text;
}
function genTasks(rng: () => number, n: number): AssignableTask[] {
  return Array.from({ length: n }, (_, i) => ({
    taskId: `tsk_${i.toString().padStart(6, "0")}` as AssignableTask["taskId"],
    role: pick(rng, ROLES),
    objective: genObjective(rng),
    ...(rng() < 0.1 ? { contractRisk: rng() < 0.5 } : {}),
    ...(rng() < 0.3 ? { contextTokens: Math.floor(rng() * 100000) } : {}),
    ...(rng() < 0.3 ? { outputTokens: Math.floor(rng() * 4000) } : {})
  }));
}
/** Replay-faithful shape: every production caller maps only taskId/role/objective. */
function genReplayTasks(rng: () => number, n: number): AssignableTask[] {
  return Array.from({ length: n }, (_, i) => ({
    taskId: `tsk_${i.toString().padStart(6, "0")}` as AssignableTask["taskId"],
    role: pick(rng, ROLES),
    objective: genObjective(rng)
  }));
}
function tenModelCatalog(): ModelRouterConfig {
  const models: CatalogModelInput[] = Array.from({ length: 10 }, (_, i) => ({
    id: `m${i}`,
    version: `m${i}-v1`,
    roles: ["actor", "critic"] as const,
    maxComplexity: (i >= 7 ? "HIGH" : "MEDIUM") as CatalogModelInput["maxComplexity"],
    estimatedCostUsd: 0.05 * (i + 1),
    estimatedDurationMs: 500 * (i + 1),
    approvedForHighRisk: i >= 8
  }));
  return { policyVersion: "sim-r32b", models };
}
/** Configured-state catalog, byte-identical to R8-B..R31-B's (M=7, alias rows). */
function configuredCatalog(): ModelRouterConfig {
  const enabled: CatalogModelInput[] = [
    {
      id: "acme/fast-mini", version: "fast-mini-2026-01", providerId: "acme",
      roles: ["actor", "critic"] as const, maxComplexity: "MEDIUM",
      estimatedCostUsd: 0.0009, estimatedDurationMs: 1_500,
      inputCostPerMTok: 0.4, outputCostPerMTok: 1.0,
      contextWindow: 200_000, maxOutputTokens: 16_000,
      capabilities: ["tool-use", "vision"], approvedForHighRisk: false
    },
    {
      id: "acme/std", version: "std-2026-02", providerId: "acme",
      roles: ["actor", "critic"] as const, maxComplexity: "MEDIUM",
      estimatedCostUsd: 0.004, estimatedDurationMs: 1_500,
      inputCostPerMTok: 2.0, outputCostPerMTok: 4.0,
      contextWindow: 200_000, maxOutputTokens: 32_000,
      capabilities: ["tool-use"], approvedForHighRisk: false
    },
    {
      id: "orion/coder", version: "coder-9", providerId: "orion",
      roles: ["actor", "critic"] as const, maxComplexity: "MEDIUM",
      estimatedCostUsd: 0.0035, estimatedDurationMs: 1_500,
      inputCostPerMTok: 1.5, outputCostPerMTok: 4.0,
      contextWindow: 128_000, maxOutputTokens: 8_000,
      capabilities: ["tool-use"], approvedForHighRisk: false
    },
    {
      id: "orion/lite", version: "lite-3", providerId: "orion",
      roles: ["actor", "critic"] as const, maxComplexity: "MEDIUM",
      estimatedCostUsd: 0.0006, estimatedDurationMs: 1_500,
      inputCostPerMTok: 0.25, outputCostPerMTok: 0.7,
      contextWindow: 64_000, maxOutputTokens: 8_000,
      capabilities: ["tool-use"], approvedForHighRisk: false
    },
    {
      id: "acme/frontier", version: "frontier-2026-03", providerId: "acme",
      roles: ["actor", "critic", "judge", "router"] as const, maxComplexity: "HIGH",
      estimatedCostUsd: 0.0125, estimatedDurationMs: 4_000,
      inputCostPerMTok: 5.0, outputCostPerMTok: 15.0,
      contextWindow: 400_000, maxOutputTokens: 64_000,
      capabilities: ["tool-use", "vision"], approvedForHighRisk: true
    }
  ];
  const fastRow = enabled[3]!;
  const primaryRow = enabled[4]!;
  return {
    policyVersion: "router-v1-live",
    models: [...enabled, { ...fastRow, id: "cheap" }, { ...primaryRow, id: "premium" }]
  };
}
function configuredLearned(): LearnedRoutingPolicy {
  return {
    primaryModelId: "acme/frontier",
    avoid: [{ modelId: "orion/coder", reason: "sim: weak review outcomes", family: "review" }],
    prefer: [{ family: "plan", modelId: "acme/frontier" }]
  };
}
function tenModelLearned(): LearnedRoutingPolicy {
  return {
    primaryModelId: "m9",
    avoid: [{ modelId: "m2", reason: "sim", family: "review" }],
    prefer: [{ family: "plan", modelId: "m9" }]
  };
}
function learnedM2(): LearnedRoutingPolicy {
  return {
    primaryModelId: "premium",
    avoid: [{ modelId: "cheap", reason: "sim", family: "review" }],
    prefer: [{ family: "plan", modelId: "premium" }]
  };
}

/* Realistic public-prior snapshot, byte-identical to R12-B..R31-B's (id retagged). */
const TS = "2026-08-19T00:00:00.000Z";
function realisticPrior(): PublicPriorSnapshot {
  const scores: {
    sourceId: string; modelAliases: string[]; raw: number; unit: string;
    fetchedAt: string; sourceUrl: string;
  }[] = [];
  const push = (sourceId: string, alias: string, raw: number, unit = "pass_rate", url = "https://example.com/board"): void => {
    scores.push({ sourceId, modelAliases: [alias, `${alias} (latest)`], raw, unit, fetchedAt: TS, sourceUrl: url });
  };
  push("aider-polyglot", "cheap", 0.41);
  push("aider-polyglot", "premium", 0.88);
  push("swe-bench-verified-mini", "cheap", 0.32);
  push("swe-bench-verified-mini", "premium", 0.74);
  push("terminal-bench-2.1-fixed-harness", "cheap", 0.5);
  push("terminal-bench-2.1-fixed-harness", "premium", 0.8);
  push("arena-coding", "cheap", 1180, "elo");
  push("arena-coding", "premium", 1367, "elo");
  push("aider-polyglot", "Acme Fast Mini 2026-01", 0.44);
  push("aider-polyglot", "acme/std", 0.61);
  push("aider-polyglot", "Orion Coder 9", 0.58);
  push("aider-polyglot", "orion/lite", 0.35);
  push("aider-polyglot", "Acme Frontier (2026-03)", 0.86);
  push("swe-bench-verified-mini", "Acme Fast Mini 2026-01", 0.38);
  push("swe-bench-verified-mini", "acme/std", 0.55);
  push("swe-bench-verified-mini", "Orion Coder 9", 0.51);
  push("swe-bench-verified-mini", "orion/lite", 0.28);
  push("swe-bench-verified-mini", "Acme Frontier (2026-03)", 0.79);
  push("terminal-bench-2.1-fixed-harness", "Acme Fast Mini 2026-01", 0.42);
  push("terminal-bench-2.1-fixed-harness", "acme/std", 0.57);
  push("terminal-bench-2.1-fixed-harness", "Acme Frontier (2026-03)", 0.82);
  push("arena-coding", "acme/std", 1240, "elo");
  push("arena-coding", "Orion Coder 9", 1215, "elo");
  push("arena-coding", "Acme Frontier (2026-03)", 1360, "elo");
  for (let i = 0; i < 10; i += 1) push("aider-polyglot", `m${i}`, 0.3 + i * 0.055);
  for (let i = 0; i < 10; i += 1) push("swe-bench-verified-mini", `m${i}`, 0.25 + i * 0.05);
  const rivals = ["rival/alpha", "rival/beta", "rival/gamma", "zephyr-long-name/model-x", "labs/quill", "labs/scribe"];
  for (const [i, id] of rivals.entries()) {
    push("aider-polyglot", id, 0.45 + i * 0.05);
    push("arena-coding", id, 1200 + i * 21, "elo");
  }
  return {
    schemaVersion: 1,
    snapshotId: "pps_r32b_realistic_v1",
    createdAt: TS,
    qualityBar: 0.55,
    scores,
    aliasMap: [
      { canonicalId: "acme/fast-mini", alias: "Acme Fast Mini 2026-01" },
      { canonicalId: "orion/coder", alias: "Orion Coder 9" },
      { canonicalId: "acme/frontier", alias: "Acme Frontier (2026-03)" }
    ]
  } as unknown as PublicPriorSnapshot;
}

const conclusions: string[] = [];
console.log(`engine node=${process.version} v8=${process.versions.v8}`);

/* ============================================================
 * §0 Ceiling re-anchor (R4-B..R31-B methodology, corpus seed 0xb44b01
 * reused verbatim) + replay-faithful token-free corpus (R5-B).
 * ============================================================ */
{
  const catalog2 = catalogFromPrimary({ primaryModelId: "premium" });
  const catalog10 = tenModelCatalog();
  const tasks = genTasks(mulberry32(0xb44b01), 2000);
  const totalM2 = bench(() => assignTasks({ catalog: catalog2, tasks }), 30);
  const totalM10 = bench(() => assignTasks({ catalog: catalog10, tasks }), 30);
  const analyzeOnly = bench(() => {
    for (const task of tasks) analyzeTask(task.objective, task.role);
  }, 30);
  console.log(
    `ceiling eval-replay N=2000: assignTasks M=2 ${(totalM2 * 1e3).toFixed(1)}us | M=10 ${(totalM10 * 1e3).toFixed(1)}us | analyzeTask share ${(analyzeOnly * 1e3).toFixed(1)}us (${((analyzeOnly / totalM2) * 100).toFixed(0)}%)`
  );
  console.log(
    `ceiling per eval run (x2 calls): M=2 total=${(totalM2 * 2).toFixed(2)}ms | M=10 total=${(totalM10 * 2).toFixed(2)}ms | analyzeTask total=${(analyzeOnly * 2).toFixed(2)}ms`
  );
  const replayTasks = genReplayTasks(mulberry32(0xb44b01), 2000);
  const replayM2 = bench(() => assignTasks({ catalog: catalog2, tasks: replayTasks }), 30);
  console.log(
    `ceiling replay-faithful (token-free) N=2000: assignTasks M=2 ${(replayM2 * 1e3).toFixed(1)}us per call (${(replayM2 * 2).toFixed(2)}ms per eval x2)`
  );
  const stress = genTasks(mulberry32(0xb44b01), 20000);
  const stressM2 = bench(() => assignTasks({ catalog: catalog2, tasks: stress }), 5);
  console.log(
    `ceiling 10x stress N=20000: assignTasks M=2 ${stressM2.toFixed(1)}ms per call (${(stressM2 * 2).toFixed(1)}ms per eval x2)`
  );
  const cli = genTasks(mulberry32(0xb44b01), 30);
  const cliM2 = bench(() => assignTasks({ catalog: catalog2, tasks: cli }), 2000);
  console.log(`ceiling CLI live face N=30: assignTasks M=2 ${(cliM2 * 1e3).toFixed(1)}us per call`);
  conclusions.push(
    `ceiling M=2 per-eval=${(totalM2 * 2).toFixed(1)}ms M=10 per-eval=${(totalM10 * 2).toFixed(1)}ms replay-faithful=${(replayM2 * 2).toFixed(1)}ms (holds-below-landing-line=${totalM10 * 2 < 30})`
  );
}

/* ============================================================
 * §0b Configured-state x command-class matrix (R7-I lesson; R8-B..R31-B
 * anchors). Full grid + CLI live face + per-child cascade probes.
 * ============================================================ */
const anchoredCells: readonly [string, ModelRouterConfig, LearnedRoutingPolicy | undefined][] = [
  ["M=2 default, no learned", catalogFromPrimary({ primaryModelId: "premium" }), undefined],
  ["M=2 default, learned", catalogFromPrimary({ primaryModelId: "premium" }), learnedM2()],
  ["M=7 configured, no learned", configuredCatalog(), undefined],
  ["M=7 configured, learned", configuredCatalog(), configuredLearned()],
  ["M=10 stress, no learned", tenModelCatalog(), undefined],
  ["M=10 stress, learned", tenModelCatalog(), tenModelLearned()]
];
{
  const replayTasks = genReplayTasks(mulberry32(0xb44b01), 2000);
  for (const [label, catalog, learned] of anchoredCells) {
    const cost = bench(
      () => assignTasks({ catalog, tasks: replayTasks, ...(learned !== undefined ? { learned } : {}) }),
      30
    );
    console.log(
      `matrix replay N=2000 [${label}]: ${(cost * 1e3).toFixed(1)}us per call (${(cost * 2).toFixed(2)}ms per eval x2)`
    );
    if (label === "M=7 configured, learned") {
      conclusions.push(
        `configured-state M=7+learned replay per-eval=${(cost * 2).toFixed(1)}ms (still-below-landing-line=${cost * 2 < 30})`
      );
    }
    if (label === "M=10 stress, learned") {
      conclusions.push(
        `M=10+learned replay per-eval=${(cost * 2).toFixed(1)}ms (still-below-landing-line=${cost * 2 < 30})`
      );
    }
  }

  // W1: double-run byte identity at production scale (N=2000, M=10, learned).
  {
    const catalog10 = tenModelCatalog();
    const learned10 = tenModelLearned();
    const first = assignTasks({ catalog: catalog10, tasks: replayTasks, learned: learned10 });
    const second = assignTasks({ catalog: catalog10, tasks: replayTasks, learned: learned10 });
    check(
      "W1: double run (N=2000, M=10, learned) is byte-identical",
      JSON.stringify(first) === JSON.stringify(second)
    );
    // W2: learned-arm consistency vs no-learned arm — eligible sets keep
    // catalog order and only avoid/prefer families may diverge.
    const plain = assignTasks({ catalog: catalog10, tasks: replayTasks });
    let consistent = true;
    let detail = "";
    for (let i = 0; i < plain.length; i += 1) {
      const a = plain[i]!;
      const b = first[i]!;
      if (a.analysis.family !== b.analysis.family) {
        consistent = false;
        detail = `${a.taskId}: family diverged`;
        break;
      }
      const touched = b.analysis.family === "review" || b.analysis.family === "plan";
      if (!touched && a.decision.model !== b.decision.model) {
        consistent = false;
        detail = `${a.taskId}: untouched family ${a.analysis.family} model diverged ${a.decision.model} -> ${b.decision.model}`;
        break;
      }
    }
    check("W2: learned arm diverges only on avoid/prefer families (review/plan)", consistent, detail);
  }

  const cli = genTasks(mulberry32(0xb44b01), 30);
  const learned = configuredLearned();
  const catalogCfg = configuredCatalog();
  const cfgCli = bench(() => assignTasks({ catalog: catalogCfg, tasks: cli, learned }), 2000);
  console.log(`matrix CLI live face N=30: M=7 configured+learned ${(cfgCli * 1e3).toFixed(1)}us per call`);
  conclusions.push(`configured-state live-face=${(cfgCli * 1e3).toFixed(1)}us`);
  const assignments = assignTasks({ catalog: catalogCfg, tasks: cli, learned });
  const first = assignments[0]!;
  const planCost = bench(() => liveCascadePlanFromAssignment(first, { models: catalogCfg.models as never }), 40000);
  const plan = liveCascadePlanFromAssignment(first, { models: catalogCfg.models as never });
  const decideCost = bench(
    () =>
      decideLiveCascade({
        plan,
        previousModelId: first.decision.model,
        evidence: { source: "deterministic-check", kind: "FAIL" },
        failureClass: "model"
      }),
    40000
  );
  console.log(
    `matrix per-child cascade M=7: liveCascadePlanFromAssignment=${(planCost * 1e6).toFixed(0)}ns decideLiveCascade=${(decideCost * 1e6).toFixed(0)}ns`
  );
}

/* ============================================================
 * §0c Prior cells (R12-B §2c .. R31-B §2c re-anchor, twenty-first pass).
 * Production callers passing prior: cli/main smartChildPlan (live face
 * N<=30) and track loop / primary-split (C small). adaptation/eval-routing
 * does NOT pass prior (re-verified by grep this round: eval-routing.ts has
 * zero occurrences of "prior"; :353-354 passes learned only), so N=2000
 * stays a HYPOTHETICAL-scale documentation cell and the S12-B-2 reopen
 * condition leg (a) stays untriggered. The R21-B snapshot-shape axis
 * (R x A x pad) is NOT re-swept -- per instructions it is closed.
 * ============================================================ */
{
  const catalog2 = catalogFromPrimary({ primaryModelId: "premium" });
  const catalogCfg = configuredCatalog();
  const catalog10 = tenModelCatalog();
  const prior = realisticPrior();
  const replayTasks = genReplayTasks(mulberry32(0xb44b01), 2000);
  const cli = genTasks(mulberry32(0xb44b01), 30);
  for (const [label, catalog] of [["M=2", catalog2], ["M=7", catalogCfg], ["M=10", catalog10]] as const) {
    const models = createModelRouter(catalog).config.models;
    const planNow = planAssignmentPolicy(models, catalog.models.map((m) => m.id));
    const picked = pickFromPublicPrior(prior, "edit", planNow.assignableModels);
    check(`W3 prior cell live [${label}]: pickFromPublicPrior returns a pick for family=edit`, picked !== undefined, label);
  }
  const p2 = bench(() => assignTasks({ catalog: catalog2, tasks: replayTasks, prior }), 10);
  const p7 = bench(() => assignTasks({ catalog: catalogCfg, tasks: replayTasks, prior }), 10);
  const p10 = bench(() => assignTasks({ catalog: catalog10, tasks: replayTasks, prior }), 10);
  console.log(
    `matrix prior HYPOTHETICAL replay N=2000 [M=2 default, prior]: ${(p2 * 1e3).toFixed(1)}us per call (${(p2 * 2).toFixed(2)}ms per eval x2)`
  );
  console.log(
    `matrix prior HYPOTHETICAL replay N=2000 [M=7 configured, prior]: ${(p7 * 1e3).toFixed(1)}us per call (${(p7 * 2).toFixed(2)}ms per eval x2)`
  );
  console.log(
    `matrix prior HYPOTHETICAL replay N=2000 [M=10 stress, prior]: ${(p10 * 1e3).toFixed(1)}us per call (${(p10 * 2).toFixed(2)}ms per eval x2)`
  );
  const cli2 = bench(() => assignTasks({ catalog: catalog2, tasks: cli, prior }), 2000);
  const cli7 = bench(() => assignTasks({ catalog: catalogCfg, tasks: cli, prior, learned: configuredLearned() }), 2000);
  console.log(
    `matrix prior REAL-SCALE live face N=30: M=2+prior ${(cli2 * 1e3).toFixed(1)}us | M=7+prior+learned ${(cli7 * 1e3).toFixed(1)}us per call`
  );
  const models7 = createModelRouter(catalogCfg).config.models;
  const plan7 = planAssignmentPolicy(models7, catalogCfg.models.map((m) => m.id));
  const perPick = bench(() => {
    pickFromPublicPrior(prior, "edit", plan7.assignableModels);
  }, 20000);
  console.log(`prior unit cost: pickFromPublicPrior(edit, M=7 pool, ${prior.scores.length} rows) = ${(perPick * 1e6).toFixed(0)}ns per call`);
  conclusions.push(
    `prior-cells real-scale-live-face M=7+prior+learned=${(cli7 * 1e3).toFixed(1)}us hypothetical-N2000 M=7+prior per-eval=${(p7 * 2).toFixed(1)}ms per-pick=${(perPick * 1e6).toFixed(0)}ns`
  );
}

/* ============================================================
 * §0d D1 door probe (OBSERVATIONAL, thirty-second pass only).
 * The field-liveness census (report §2d D1) found 19/20 RoutingDecision
 * fields consumed at the MODEL_ROUTED serialization boundary or by live
 * supervisor control flow, and exactly one production-dead field
 * (preferredConstraint — test-asserted public contract, conditional
 * spread). This probe prices a replica of buildDecision's construction
 * sites (justification template + makeApprovalPlan literal +
 * oneHotDistribution loop + the two conditional spreads) to bound the
 * WHOLE door pool: even if every constructed byte were dead, the pool is
 * orders below the landing line. Nothing is dosed; no consumer topology
 * is swept (that is R30-F territory).
 * ============================================================ */
{
  const selected = { id: "acme/frontier", version: "frontier-2026-03", estimatedCostUsd: 0.0125, estimatedDurationMs: 4000 };
  const eligibleIds = ["acme/fast-mini", "acme/std", "orion/coder", "orion/lite", "acme/frontier"];
  const role = "actor";
  const complexity = "MEDIUM";
  const preferredModel: string | undefined = "acme/frontier";
  const agentRole: string | undefined = "planner";
  const taskId = "tsk_000123";
  const N = 1e6;
  let sink = 0;
  const t0 = performance.now();
  for (let i = 0; i < N; i += 1) {
    const preferred = selected.id === preferredModel;
    const preferredNote = preferred ? `; preferred constraint ${preferredModel}` : "";
    const justification =
      `${selected.id} is allowed for role ${role} and ${complexity} complexity; ` +
      `estimated cost ${selected.estimatedCostUsd} USD and duration ${selected.estimatedDurationMs} ms fit remaining limits` +
      preferredNote;
    const approvalPlan = {
      id: `approval:${taskId}:${selected.id}`,
      items: [
        { id: `route:${selected.id}`, label: `Use ${selected.id}`, selectable: true, defaultSelected: true },
        { id: "route:cancel", label: "Do not run this task", selectable: true, defaultSelected: false }
      ]
    };
    const out: Record<string, number> = {};
    for (const id of eligibleIds) out[id] = id === selected.id ? 1 : 0;
    const tail = {
      ...(agentRole !== undefined ? { agentRole } : {}),
      ...(preferred && preferredModel !== undefined ? { preferredConstraint: preferredModel } : {})
    };
    sink += justification.length + approvalPlan.items.length + out[selected.id]! + Object.keys(tail).length;
  }
  const perCall = ((performance.now() - t0) / N) * 1e6;
  const perEvalMs = (perCall * 2000 * 2) / 1e6;
  console.log(
    `D1 probe: decision-record construction replica (justification + approvalPlan + oneHot + conditional spreads) = ${perCall.toFixed(1)} ns/task -> ${perEvalMs.toFixed(2)} ms per eval at N=2000 x2 (sink=${sink})`
  );
  conclusions.push(
    `D1-door pool bound=${perEvalMs.toFixed(2)}ms/eval (whole construction share; actual dead share is one conditional spread ~ns)`
  );
}

console.log(`\nCONCLUSIONS: ${conclusions.join(" | ")}`);
if (failures > 0) {
  console.error(`\n${failures} check(s) FAILED`);
  process.exit(1);
}
console.log("ALL CHECKS PASSED (measurement harness + determinism/consistency witnesses; corpora seeded + deterministic)");
```

---

**MORE_OPTIMA=no** —— 第三十二遍复查空枚举收口：天花板 ×3 复锚回落
canonical 邻域（M=2 9.04–9.62 / M=10 17.84–18.43 ms/eval，freeze 0 行）；
闭合稳定性审计 54/54 行裁决至少一腿成立、无一重开（S12-B-2 腿 (a) /
S13-B-1 重开门显式重取证未触发）；五扇残门（决策记录字段活性、故障对象
通道、每调用闭包分配、one-hot 载体形状、ESM 绑定间接层）全部按普查/属地/
量级关闭；不铸新 face 名、不铸 ID、不新增排除行；生产代码零改动，聚焦
198/198 + `pnpm gate` exit 0（1169/1168/0/1 计数逐字同）。

MORE_OPTIMA=no
BRANCH=cursor/r32-b-live-routing-thirty-second-pass-83a1
