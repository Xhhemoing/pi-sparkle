MODEL_SLUG=claude-fable-5-thinking-xhigh

# R32-F：`src/experiments/` 第三十二遍复查报告（S7-F-1 + S7-F-2 之上，全新第三十二组枚举）

- **战役:** 全库持久 SOTA 优化 Round 32 / R32-F（R1-F…R31-F 同区三十一遍之后的第三十二遍）
- **基线:** 分支创建于 `cursor/sota-persistent-opt-83a1` @ `0b8fa0a`（R32-D 空枚举摄取 + 本 R32-F 派单）；工作期间独占分支 docs-only 快进**一次**：`0b8fa0a` → `f621591`（摄取 R32-C「离线路由第三十二遍诚实空枚举——未铸新面、无 S32-C-\* ID、连续第十四遍零从未点名站点」+ 派出 R32-G）。增量核对：`git diff --stat 0b8fa0a..f621591` 仅触及 `docs/reports/sota-opt/` 下 3 个文件（PROGRESS / round-32/PLAN / R32-C.md，381 插入 14 删除），对 `src test scripts package.json pnpm-lock.yaml` 直接复核**零行**；按令 rebase 到新 tip（纯快进零冲突），15 文件冻结在新 tip 复验 **0 行**。全部锚点与静态普查在派单 tip `0b8fa0a` 工作树完成——快进增量 `src/` 逐字节零变化 ⇒ 测量形态与提交形态位级同一，全部有效；聚焦套件 + `pnpm gate` 在两 tip 各跑一次均绿（计数逐字同）。R32-E 在飞，按令未等待、未触碰。
- **分支:** `cursor/r32-f-experiments-thirty-second-pass-83a1`
- **切片:** 15 文件 `src/experiments/`（全量重读确认，`wc -l` 合计 **2550** 行，与 R29-F/R30-F/R31-F 普查同数，无缺无余）
- **模型:** `claude-fable-5-thinking-xhigh`
- **引擎:** nvm Node **v22.22.2** / V8 12.4.254.21-node.39（R24-F…R31-F 同引擎约定，带可比）；tsx 4.23.12、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成。本 VM 系统默认 node 为 v22.14.0（`/exec-daemon` PATH 前缀遮蔽 nvm）——开工即显式钉 PATH 到 nvm bin 并在正式批次前 `node --version` 复核 v22.22.2，**零弃置批次**（R32-D §1(iii) 教训前置规避）。

## 结论

**诚实的空枚举：第三十二组没有可立 ID 的新角度，不铸任何 S32-F-\* ID、不铸新 face 名、不取 ALLCAPS 绰号、不写 EXCLUSIONS 行，生产代码零改动（R12-F…R31-F 二十连空之后的第二十一连空）。** R31-F 已把 F 系三十轮建成的边界 I/O 面格逐格归属完毕并把唯一残格（边界故障通道处置）按分解并入 R17-F + R30-F + R29-F + 引擎面；R31-G/R31-H/R31-I/R31-J 与 R32-A…R32-D 收口后姊妹侧亦无新增未审计平面泄入 F。本轮第三十二遍从零重读 15 个源文件与全部四个生产消费文件、在本 VM 重建全部锚点（r5f/r6f/r7f 各**三次**独立运行 + r1f 一次，§1）、对 R31-F §2 面格属主表做独立重推（无一格松动，含 R31-J **MODEX** 后缀行与 R31-E codec 行的切片结构性空直证，§2）、并按 R32-A 首创且 plan-ban 重铸的哨兵方法对 **F 自档案**在案重开条件做**不计时静态卫生复核**（§3——与 R32-D 同款，仅作卫生性应用、不当边际产出申报、不铸「我又查了一遍重开条件」式新面），全部哨兵**未触发**。逐文件枚举的全部消除角度经换名检查裁决为既有排除 ID 本体、姊妹归档面（禁移植）或停驻门成员——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（本轮新换名检查 3 项见 §0；R12-F…R31-F 全部既有换名记录复核维持）。空枚举与 R12-F…R31-F 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

关键前提事实：切片 15 文件自 `519101f` 逐字节零变化（派单 tip / rebase 后 / 提交前**三验** `git diff 519101f..HEAD -- src/experiments/` 均 **0 行**；`git log -1 -- src/experiments/` 显示切片最后一次生产改动仍为 `519101f`）；最强冻结 `git diff 89f0455..f621591 -- src/` **完全为空**（R10 以来全部提交均为 docs）。锚点复测（各 ×3，§1 诚实注记）：**r5f C2 119.46 / 120.27 / 125.31 ms**；**r6f partD 124.25 / 119.48 / 119.70 ms**；**r7f partE 125.62 / 129.79 / 129.50 ms**；r6f 三次序 + r7f 四次序 restore 省额**三轮全正无符号翻转**（S7-F-1 ≠ S6-F-5 复核成立）；r1f 2668 项逐位检查全过（参考侧成员比较 **167,167,000 次**与历轮锚定值逐位相同）。聚焦实验测试 **75/75 绿**（两 tip 各一次）；`pnpm gate`（typecheck + lint + test + build）**全绿两次**——test 1169 / pass 1168 / fail 0 / skipped 1（与 R31-F / R32-A…R32-D 逐字同计数）。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。双 LCB 与双归因未动（X0-11——PAIRX 编排位点 `attribution-report.ts` 本轮零字节未触、本轮零计时装具且无任何脚本 import 两估计器）；无阈值/测试/公开签名/数据面变更。仓库变更仅本报告一个文件。不开 PR。

## 0. 冻结、快进处置与非移植清单（方法第 1、4 步）

- 分支按令创建于派单 tip `0b8fa0a`；快进一次（`f621591`）按令 rebase（纯快进零冲突）。冻结三点核对：派单 tip / rebase 后 / 提交前，`git diff 519101f..HEAD -- src/experiments/` 均 **0 行**。
- 快进内容复核：`git diff --stat 0b8fa0a..f621591` = PROGRESS.md + round-32/PLAN.md + R32-C.md（纯 docs，`src/ test/ scripts/ package.json pnpm-lock.yaml` 直接 diff 零行）。R32-C 结论已实读：诚实空枚举、未铸新面、无新 ALLCAPS 号——**无新姊妹轴需移植**，其对 BINDX / REPRX / MODEX / R31-E codec 的划界计数方法与本报告 §2 划界行同构。
- **非移植清单全额维持并逐项核对**（派单 DO NOT REMINT 全清单 + round-32/PLAN F 段）：R30-F 边界输出消费/快照读回拓扑面、R29-F 输出保留/被替换快照存活面、R28-F 边界调用协议面、R27-F 输入访问路径机制面、R26-F 输入存储状态面、R31-F 故障通道处置残格（propagate/swallow/read-message/materialize-stack/retain/wrap-rethrow 已分解归 R17-F + R30-F + R29-F + 引擎面——本轮不重铸、不新停驻、零计时臂）、**PAIRX**（R29-C——编排位点共驻不当新 F ID）、**BINDX**（R31-C——切片 ambient `Math.*` token 恰 **9 个于 8 行**：gated-comparison.ts:92 ×2、comparison-report.ts:120/185/190/195/200、replay.ts:69/70——同 BINDX 坐标划界计数即止，零 patch 零剂量）、**MODEX**（R31-J——切片 grep **零** chmod / 零 umask / 零八进制 mode 字面量、零 fs 写入位点，权限模式字坐标在 F 结构性空，本轮首次显式划界声明即止）、I/O text-codec topology / per-run transcode-byte manifest（R31-E——`rg "node:fs|readFile|writeFile|Buffer" src/experiments/` 本轮首手复核**零匹配**，公开边界全同步纯内存，面结构性空）、REPRX（R30-C——切片 `JSON.parse` 输入位点 **0**）、R32-A 重开条件哨兵审计（**方法不重铸为面**——§3 按令仅作卫生性应用于 F 自档案）、R32-B 闭合稳定性审计（**方法不重铸为面**——本轮未重走 54 行腿分类，F 侧裁决有效性由 §3 哨兵 + 0 行冻结携带）、R31-A injected-callable 停放门（HoldoutVault 注入缝见 §0 换名记录 2）、R31-B 六停驻门、R31-D 的 R22-D 属地格、R31-G 18 门审计、R31-H 四扇边界参数残门、R31-I CLI 进程边界门扉闭合审计（F 是进程内库，零 argv/stdin/信号/spawn/exit 腿，平面正交零交集——切片仅 `node:path`（isolation.ts）与 `node:crypto` randomUUID（holdout.ts）两个 node: 引入）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / CSPELL / LINKTOP 全部未编号未移植。S7-F-1 ≠ S6-F-5 维持（§1 次序符号复核）。
- **换名重提检查**（本轮枚举全部新角度的逐项裁决，一律不铸 S32-F-\* ID；以下 **3 项为本轮新记录**，其余全部命中既有 ID 本体见 §4）：
  1. **`stableStringify` 内部形态改造**（`parts` map+join 换增量 `+=` / 键序预缓存 / 特化平面对象快路径）= 字节契约仪器改形——它是 manifestHash / sealedManifestHash / rerunHash / replayCacheKey 摘要链与 R30-F/R31-F 决定性见证（账本摘要恒等）的公共仪器；生产流量恰 `replayCacheKey`（4 键平面小对象，once-per-eval，ns–µs 域，S7-D-2 报告尾链邻域）+ `manifestHash` once-per-manifest；大输入腿（`rerunHash` over actions）**零生产流量**（S2-F-2 普查本轮复核成立，§3）；串构建形态变体属 S1-I-7 / S2-I-3 家族（V8 rope/join 对称性实测符号翻转域），键序缓存属 X1-1 模块级隐藏缓存禁区。µs once-per-run 拒收类，不铸；
  2. **HoldoutVault 注入缝默认臂**（`options.now ?? nowIso` / `options.generateId ?? (() => \`ha_${randomUUID()}\`)`）= 注入可调用物身份平面 —— R31-A 停放门成员（对其计时重剂量 R28-F/R30-A 调用面）；默认臂本体 = R26-D「注入缝默认臂」面的 F 区同类非独立对象；且整链**零生产流量**——vault 仅经 `simulation-holdout.ts` `auditHoldoutAccess` 到达，该链零 `src/` 切片外消费者（§3 普查直证）。不铸；
  3. **restore 返回值防御拷贝省略**（`assignments`/`outcomes` 别名直传调用方）= 可观察别名拓扑改变 —— R29-A 输入引用身份/别名拓扑面 + X4-2 / S3-G-1 隔离降级家族本体；且该拷贝是 X3-3 冻结的 fail-closed Ω(P+A) restore 契约组成部分（S1-F-3 同域）。不铸。
- 未重开任何 X\* / S1-\* … S31-\* 条目。**单变量纪律**：本轮**无任何计时新臂**——三个锚点脚本均为 `scripts/round0{5,6,7}-*.ts` 入库血统资产逐字节原样重跑（零改动零参数），r1f 同；§2/§3 全部为静态 rg / 读码断言（不计时）。S9-D-4 / S12-D-1 / S8-I-1 / S13-B-1 / S12-B-2 等姊妹区维持项零触碰（其哨兵属地在 A/B/D/I，本轮不代跑）。

## 1. 锚点重测（r5f/r6f/r7f 各三次独立运行，r1f 一次；本 VM 实测，未拷贝历轮数字；nvm Node v22.22.2）

| 档位 | 第 1 次 | 第 2 次 | 第 3 次 | R31-F 记载带 |
| --- | --- | --- | --- | --- |
| r5f C2 全实验（P=2000/A=1000） | 119.46 ms | 120.27 ms | 125.31 ms | 119.97 / 121.51 / 122.37 ms |
| 2A-validate 组件 | 99.24 ms（83.1%） | 99.41 ms（82.7%） | 104.61 ms（83.5%） | 99.87–101.75 ms（82.7–83.2%） |
| r5f C1 has+add → add+size 省额 | +30.19 ms（20.7%） | +31.85 ms（21.7%） | +30.72 ms（20.4%） | +30.17~+31.33 ms（20.6–21.2%） |
| r6f partD 全实验 | 124.25 ms | 119.48 ms | 119.70 ms | 119.70 / 119.52 / 117.11 ms |
| r7f partE 全实验 | 125.62 ms | 129.79 ms | 129.50 ms | 127.81 / 129.00 / 130.55 ms |
| r7f validate 卫（×2000） | +8.34 ms | +7.17 ms | +10.70 ms | +7.70/+8.25/+7.12 ms |

**次序符号（committed 回归脚本原生视图，全部为「落地生产形态 vs 冻结参考」省额）**：r7f partE 四次序三轮全正——prefix(aligned) +44.80/+45.41/+46.08、half-aligned +26.01/+28.12/+27.24、scattered +4.92/+5.61/+3.76、reversed +5.67/+6.84/+6.89 ms，无符号翻转；r6f partD 三次序三轮全正——prefix +124.49/+123.18/+123.90、scattered +30.82/+29.92/+31.04、reversed +30.95/+29.52/+28.96 ms。**S7-F-1 ≠ S6-F-5 复核成立**：落地代码零漂移（切片零 diff），同下标哈希相等 + 后缀回退形态在全部次序下三轮同号为正。r5f C4 索引环形式 −3.18/−1.45/−1.81 ms 维持 S5-F-3 抖动带记载（R31-F −1.97/−2.95/−3.97）；C5 dataset 镜像 14.2–16.0 µs（U=2000）/ 125.6–135.1 µs（U=20000）维持 S5-F-1 记载（R31-F 13.3–15.1 / 123.0–141.4 带交叠）。r1f 2668 项逐位检查全过（生产侧 62.6 ms / 参考侧 626.8 ms，10.0×；参考侧成员比较 **167,167,000 次**——与历轮锚定值逐位相同；参考侧壁钟非锚点义务，钉死量是比较计数与逐位等价）。

**诚实注记**：(i) r5f C2 run3 125.31 高出 R31-F 带顶（122.37）2.94 ms——居历史读数并集（119.31–132.69）与 R15-F 最宽包络内；同窗 validate 组件比例 83.5% 居历轮 81.1–84.5% 带内，C1 省额比例 20.4% 居 R26-F…R31-F 紧邻带（19.9–23.5%）内 ⇒ 判 VM 窗口方差非结构漂移（切片零 diff，r1f 计数逐位同历轮）；run1 119.46 居 R29-F/R30-F 微快邻域（119.31–119.66）。(ii) r6f run1 124.25 高出 R31-F 带顶（119.70）但完全落在 R24-F…R29-F 带（117.9–126.9）内，runs 2–3 与 R31-F/R30-F 带交叠。(iii) r7f run1 125.62 低于 R28-F…R30-F 包络（126.63–132.69）下缘 1.01 ms——与 R30-F r5f / R31-F r6f 微快注记同向同判（VM 窗口性微快个体）；runs 2–3 全落 R31-F 带（127.81–130.55）内。(iv) r7f validate 卫 run3 +10.70 高出 R30-F/R31-F 并集顶（+8.42）2.28 ms——三轮符号稳定为正、落地 S7-F-2 省额方向不变，量级个体属同款 VM 窗口注记，不构成任何重开触发（§7）。

## 2. 调用面复核 + 面格属主表独立重推（元闭合，方法第 3 步前置）

生产消费图 rg 全仓复核（与 R30-F §1.2 / R31-F §2 逐条一致）：切片外 `src/` importers 恰四个文件，import 行逐条核对——`adaptation/reflection.ts`（validateExperimentPlan + createShadowRunner + 类型：唯一热交错消费者）、`adaptation/eval-routing.ts`（comparison-report 类型与函数 + gatedComparisonReport + createIsolationGuard + stableStringify + replayCacheKey）、`adaptation/promotion-rules.ts`（validateComparisonReport 直呼）、`routing/r1-shadow-report.ts`（comparison-report + gated-comparison）。**故障通道处置普查复核**：四消费文件 catch 位点合计 4 处、全部在 eval-routing.ts（L219/L249/L259 包 `JSON.parse`/`readFile`，非切片物），**恰一处**包切片调用——L331–339 `assertReplayIsolated` 包 `createIsolationGuard`（MESSAGE + WRAP-RETHROW 坐标，once-per-eval 冷通道）；`reflection.ts` / `promotion-rules.ts` / `r1-shadow-report.ts` 零 catch ⇒ 热通道生产坐标仍 = PROPAGATE。切片故障源普查：142 处 `throw new DomainValidationError`（逐文件计数与 R31-F §4 全同：shadow 27 / plan 22 / dataset 19 / canary 14 / manifest 14 / holdout 11 / simulation-holdout 10 / comparison-report 8 / evaluation-card 7 / isolation 6 / replay 3 / gated-comparison 1 / 其余三文件 0），切片零 `throw new TypeError/Error` 直抛点——故障通道单类维持。

R31-F §2 面格属主表本轮从零独立重推（(o) = F 区属主轴；(s) = 姊妹区收口面，明令禁移植；每格属主裁决**无一松动**）：输入存储状态 (o) R26-F / 访问路径 (o) R27-F / 对象表示 (s) REPRX / 引用身份·别名 (s) R29-A / 流组成 (o) R20-F / 计划标头身份几何·计数·成员拓扑 (o) R25-F/R23-F/R22-F / 规模与二阶联角 (o) R14-F 域 + (s) R30-B / 调用协议 (o) R28-F / 排程·实参物化·调用上下文 (s) CALLB/R28-H/R22-H / 调用流次序·分派历史 (s) R30-A / 时钟来源 (s) R29-E / 执行基片·名字解析 (s) R27-H 族 / BINDX / 引擎态 (o) R15-F 轴 3 + R19-F 域 / 输出成功通道保留 (o) R29-F / 输出成功通道消费·读回 (o) R30-F / 输出故障通道制造·单价 (o) R17-F / **输出故障通道调用方处置 = R31-F 分解归属（R17-F + R30-F + R29-F + 引擎面），本轮零计时零重铸维持** / 双估计器共驻 (s) PAIRX（X0-11 禁区）。本轮补两行结构性空直证：**I/O 编解码拓扑 (s) R31-E——切片 fs/codec 位点 0**；**持久工件权限模式字 (s) R31-J MODEX——切片 chmod/umask/mode 位点 0、零盘上工件**。第三十二遍枚举在该坐标系内**无剩余未审计平面**。

## 3. F 档案在案重开条件哨兵复核（全部不计时；R32-A 方法卫生性应用，不铸面、不当边际产出）

| 条目 | 重开 / 触发条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| R31-F §7 (a) | 任何 `src/` 生产调用方在热交错内 catch 边界故障并做非平凡处置 | §2 catch 普查：热消费者 reflection.ts 零 catch；唯一切片 catch 仍为 eval-routing L331–339 once-per-eval ⇒ **未触发** |
| R31-F §7 (b) | 故障对象携带非字符串 own 载荷 | `src/domain/errors.ts` 在最强冻结内（`89f0455..f621591 -- src/` 0 行）⇒ **未触发** |
| R31-F §7 (c) | 故障通道契约（消息文本/单类不变量）解冻 | 快进增量纯 docs 且无解冻声明；切片零 diff ⇒ **未触发** |
| S6-F-2 重开 | delete+size 形态实测稳定反超 >5% | 无任何反超证据；本轮落地形态三次序省额三轮全正（§1）——按纪律**不重计时被拒形态**（重计时即再剂量）⇒ **未触发** |
| S6-F-5 重开 | assignment 次序被契约化为 population 子序列 | 冻结 0 行 + PLAN 重申 S7-F-1 ≠ S6-F-5 ⇒ **未触发** |
| S5-F-1 / S4-F-1 前提 | dataset/simulation-holdout 链获得生产消费者 | `rg 'experiments/(dataset|simulation-holdout|holdout|shadow-compare|threshold-calibration|attribution-report)' src/`（切片外）唯一命中为 eval-routing 的 `experiments/replay.js`（replayCacheKey，模式串重叠命中）⇒ 六链零生产消费者 ⇒ **未触发** |
| S2-F-2 / S4-F-2 前提 | `replayPolicy` 获得生产调用方 | eval-routing 对 replay.ts 的 import 恰 `replayCacheKey` 一项（import 行直证）⇒ **成立维持** |
| S2-F-1 前提 | shadowDecisionAt 占比脱离 ~1% 域 | 切片零 diff + 锚点带内 ⇒ **成立维持** |
| X0-11 / PAIRX | 双路估计器拆分/重排/省略 | attribution-report.ts 零字节未触；本轮零装具 import 估计器 ⇒ **冻结在位** |
| 签名/调用图类（S1-F-6、S1-F-8、X3-3、S3-F-2 等） | 要求 `src/` 变更 | 最强冻结 0 行 ⇒ **全体判空** |

全部哨兵未触发。凡姊妹属地哨兵（S12-B-2 / S13-B-1 / S8-I-1 / S9-D-4 / S12-D-1）本轮不代跑——其属主轮 R32-B / R32-C / R32-D 已各自复核未触发。

## 4. 逐文件收口（R1-F…R31-F 收口之上的本轮检查点）

| 文件 | 本轮新视角检查（+ 维持项） | 结论 |
| --- | --- | --- |
| `shadow.ts` | 27 抛点单类维持；S7-F-1 落地形态零漂移、四次序省额三轮全正（§1）；shadowDecisionAt O(index) 重放 = S2-F-1；restore→assign Set 复用 = S2-F-4/S9-F-1（assign 站点三机制收口）；recordOutcome 末元素见证 = S9-F-2、查重/成本增量 = S1-F-8；空 assignments 跳 Set = S3-F-3;计划引用同一跳验 = S3-F-2；返回值拷贝省略 = §0 换名记录 3（R29-A/X4-2/X3-3）；spread 分配 = R15-F 轴 3 引擎面 | 无候选 |
| `plan.ts` | 22 抛点；S5-F 单探针 + S7-F-2 首字符卫现场在位零漂移；批量 Set = S5-F-2、null-proto = S6-F-4、批快路径 = S6-F-3、长度 1 快路径 = S8-F-3、scratch 共享 = S9-F-3、返回 Set = S1-F-6；r5f C1/C2、r7f validate 卫三轮带内 | 无候选 |
| `canary.ts` | 14 抛点；同构对齐环 + S6-F-1 回退零漂移；reversibleScopes Set = S1-F-7；exposure 派生为契约本体 | 无候选 |
| `gated-comparison.ts` | 1 抛点；strip-retry 复用 = S1-F-1；pairedEvaluationCard 多遍融合 = S2-F-6；mean/SE 双遍 = 同族 | 无候选 |
| `replay.ts` | 3 抛点；双 manifestHash = S2-F-2（零生产调用方本轮直证）；空 exclusions 免建 = S4-F-2；selected propensityFor 去重 = S1-F-2；exclusions Set 化已落地（H1）；replayCacheKey 唯一生产腿 = once-per-eval µs（S7-D-2 邻域） | 无候选 |
| `comparison-report.ts` | 8 抛点；六遍改单遍 = X3-2；`Array.from(families.entries())` = S1-F-5；delta CSE = S4-F-3；四均值 reduce 融合 = X3-2 族；validateComparisonReport 生产消费者（promotion-rules）零 catch = PROPAGATE 维持 | 无候选 |
| `threshold-calibration.ts` | 零抛点；三遍并单遍 = S1-F-4（一次性 informational） | 无候选 |
| `shadow-compare.ts` | 零抛点；薄封装维持 X1-5 | 无候选 |
| `isolation.ts` | 6 抛点；resolve(outputRoot) 提升 = S3-F-4；per-root 双 isInside 的 resolve 重复 = 同族 + once-per-eval µs；**全生产普查唯一被 catch 的切片故障源**（eval-routing L331–339，once-per-eval 冷通道，§2） | 无候选 |
| `simulation-holdout.ts` | 10 抛点；探针去重镜像 = S5-F-1（链零生产消费者本轮直证）；尾部双验 = S2-F-5（fail-closed 纵深）；toFrozenEpisode 投影 = S3-F-5；auditHoldoutAccess try/register = once-per-run | 无候选 |
| `holdout.ts` | 11 抛点；审计追加拷贝 = S1-F-3/X4-2；注入缝默认臂 nowIso/randomUUID = §0 换名记录 2（R31-A 停放 + R26-D 同类 + 零生产流量）；Map 重 set 不可变态 = X4-2 族 | 无候选 |
| `manifest.ts` | 14 抛点；stableStringify 形态改造 = §0 换名记录 1（字节契约仪器 + 零热流量 + S1-I-7/X1-1 族）；validateManifest 每建 Set = once-per-creation | 无候选 |
| `evaluation-card.ts` | 7 抛点；validate 数组字面量 = S1-B-7 域 | 无候选 |
| `dataset.ts` | 19 抛点；seen Set 复用 universe = S4-F-1；has+add 原形（前 S5-F 形态）镜像去重 = S5-F-1（test-only 链维持）；rotateHoldout 双验 = S2-F-5 族 once-per-rotation | 无候选 |
| `attribution-report.ts` | 零抛点；**PAIRX 编排位点**——零字节未触、X0-11 双路对未拆分/未重排/未省略 | 无候选（共驻剂量归 R29-C） |

## 5. 候选总表

**空。** 本轮零候选：面格属主表独立重推（§2）无一格松动且无新残格；哨兵复核（§3）按定义不产出候选（不计时、不剂量）；十五文件枚举（§4）全部命中既有 ID 本体、姊妹归档面、停驻门成员或本轮 §0 换名记录。本轮无任何计时新臂 ⇒ 无可定价候选面。

## 6. CHECKS

- 分支恰 `cursor/r32-f-experiments-thirty-second-pass-83a1`，基线 `0b8fa0a` → 快进 rebase `f621591`。
- 冻结三点核对全 **0 行**（§0）；15 文件普查 2550 行与 R29-F/R30-F/R31-F 同数；最强冻结 `git diff 89f0455..f621591 -- src/` **0 行**。
- 锚点 r5f/r6f/r7f 各三次独立运行 + r1f 一次全部 0 失败（224 / 27 / 169 / 2668 项，§1）；四次序 + 三次序省额三轮全正，无符号翻转；全部在 nvm Node v22.22.2 上执行（正式批次前 `node --version` 在同 shell 复核，零弃置批次）。
- 生产消费普查：4 个 importer 文件（import 行逐条核对）；catch 位点恰 1 处包切片调用（冷通道 once-per-eval）；热消费者零 catch；142 抛点逐文件计数与 R31-F 全同；切片零直抛 Error/TypeError。
- 姊妹面划界计数：BINDX `Math.*` 9 token 于 8 行；R31-E codec 位点 0；REPRX `JSON.parse` 位点 0；MODEX chmod/umask/mode 位点 0；node: 引入恰 2（isolation.ts node:path、holdout.ts node:crypto）。
- 聚焦测试：`npx tsx --test test/unit/experiments/*.test.ts` = **75/75 pass**（10 suites，两 tip 各一次）。
- 全仓门禁：`pnpm gate`（typecheck + lint + test + build）**全绿两次**（两 tip 各一次）——test 1169 / pass 1168 / fail 0 / skipped 1（既有 provider-smoke 凭据跳过，与 R31-F / R32-A…R32-D 记录同计数）；Node v22.22.2。
- 仓库改动恰本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核）。
- 硬不变量零触碰：双 LCB + 双归因在位；live = R0 等价（R1 未接线）；promotion proposal-first；无 Outcome-supported 主张（Checkpoint F-PROD 开放，ADR-005）；无阈值/测试/权限/数据面/公开签名改动；CAS/凭据契约原样；无 fsync/完整性重哈希移除；fail-open 家族保持关闭；分析未变更任何 in-flight run；EXCLUSIONS / PROGRESS / PLAN / README 未编辑；不开 PR。

## 7. 重开条件

本轮无新定价轴，不加行。R31-F 故障通道处置分解的三条显式重开触发（(a) 热交错内非平凡 catch 处置；(b) 非字符串 own 载荷；(c) 通道契约解冻）经 §3 哨兵直证全部未触发，原文全额维持；R30-F（输出消费）、R29-F（输出保留）、R17-F（拒绝终端）、S6-F-2 / S6-F-5 / S5-F-1 / S2-F-1 / S2-F-2 既有重开条件同样全额维持。锚点微快/微慢个体与 r7f validate 卫高侧个体（§1 注记）不构成重开触发——带并集交叠、比例组件全部带内、省额符号三轮稳定。

## 8. 新增排除 ID

**无。** 空枚举轮不铸 S32-F-\* ID、不铸 face 名、不取绰号、不加 EXCLUSIONS 行。换名检查 3 项新记录见 §0（stableStringify 形态 / HoldoutVault 注入缝默认臂 / restore 返回拷贝省略），归档备后续轮次引用，无需并入 EXCLUSIONS.md。

## 附录：仿真出处与运行摘录

本轮**未新建**任何仿真/见证脚本：全部计时证据来自入库血统资产的逐字节原样重跑（`scripts/round01-r1f-equivalence-sim.ts`、`scripts/round05-r5f-equivalence-sim.ts`、`scripts/round06-r6f-equivalence-sim.ts`、`scripts/round07-r7f-equivalence-sim.ts`，零改动零参数）；§2/§3 哨兵为只读 rg / 读码断言（不计时、无临时文件）。运行：`npx tsx scripts/round0X-*.ts`（nvm Node v22.22.2）。

三次官方运行关键行摘录（r7f；r5f/r6f 对应行见 §1 表）：

```text
run 1: part E prefix saving 44.80ms | half 26.01 | scattered 4.92 | reversed 5.67
       validate x2000 saving 8.34ms | full experiment 125.62ms | 169 checks, 0 failures
run 2: part E prefix saving 45.41ms | half 28.12 | scattered 5.61 | reversed 6.84
       validate x2000 saving 7.17ms | full experiment 129.79ms | 169 checks, 0 failures
run 3: part E prefix saving 46.08ms | half 27.24 | scattered 3.76 | reversed 6.89
       validate x2000 saving 10.70ms | full experiment 129.50ms | 169 checks, 0 failures
```

MORE_OPTIMA=no
BRANCH=cursor/r32-f-experiments-thirty-second-pass-83a1
