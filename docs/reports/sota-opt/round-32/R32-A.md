MODEL_SLUG=claude-fable-5-thinking-xhigh

# R32-A：`src/tracking/` + `src/run/{child-tracking,gate-apply}.ts` 第三十二遍搜查报告

**战役:** 全库持久 SOTA 优化 Round 32 / R32-A（Round 1–31 同区第三十二遍）
**基线:** `cursor/sota-persistent-opt-83a1` @ `d97e119`（R31-H 空枚举摄取 + Round 32 开卷 + 本 R32-A 派发）。运行中 exclusive docs-only 快进**一次**：`d97e119` → `b9ceed0`（R31-I 空枚举摄取——**未铸新面、无 S31-I-\* ID**，CLI 进程边界门扉闭合审计穷尽归属——+ R32-B 派发）。增量核对：`git diff --stat d97e119..b9ceed0` **仅触及 docs/reports/sota-opt/ 下 4 个文件（PROGRESS / round-31/PLAN / R31-I.md / round-32/PLAN，635 插入 24 删除）**，对 `src test scripts package.json pnpm-lock.yaml` **零行**（直接复核）；按令快进本分支至 `b9ceed0`（纯快进零冲突），14 文件冻结在新 tip 复验 **0 行**；R31-I 结论已读（空枚举，不铸面），其「CLI 进程边界门扉闭合审计」按更新版 round-32/PLAN 补入 §0 非移植清单并完成划界。三次官方仿真与聚焦套件在 `d97e119` 的工作树完成——快进增量 `src/` 零变化 ⇒ 测量在字节相同的切片上完成，全部有效；`pnpm gate` 与聚焦套件在两 tip 各跑一次均绿（计数逐字同）。R31-J 仍在飞，按令未等待。
**分支:** `cursor/r32-a-tracking-thirty-second-pass-83a1`
**模型:** `claude-fable-5-thinking-xhigh`
**引擎:** nvm Node **v22.22.2**（R28-A…R31-A 同引擎约定，带可比；tsx 4.x、pnpm 10.17.1、`pnpm install --frozen-lockfile` 干净完成）。

## 结论

**诚实空枚举：无达门槛的新更优解，未铸任何 S32-A-\* ID，不铸新 face 名、不取绰号，切片零代码改动；整片预算收口第三十次复核成立（本 VM 重锚 13.5–15.7 µs/gate ⇒ ~68–79 µs/run，与 R31-A 68–84 / R30-A 64–77 带交叠）。** R31-A 已把「在一个 Node 进程内调用一个纯同步函数」的元坐标系逐面归属完毕（喂什么 / 喂多少什么混合 / 什么结局 / 什么环境基质 realm / 什么次序进程史温度 / 经什么协议抵达 / 经什么可调用物注出 / 输出往哪去 / 时间从哪来 / 进程生命期怎么分段 / 持久工件怎么指称 / 溯源覆盖什么形 / 调度裁决什么格——本轮从零独立重推该表，逐面复核无一松动，见 §2.3）；R31-G 的 18 扇聚合门与 R31-H 的边界参数平面四门分解把姊妹侧余项也全部关净。本轮第三十二遍独立枚举从零重读 14 文件、重走 18 结构位置 × 13 消除机制满矩阵（§4），枚举出的全部角度经换名检查逐一裁决为既有排除 ID 本体、姊妹归档面（禁移植）或 R31-A 停靠门成员——**没有一个「从未受控 + 独立物理对象 + 真尺度」三条同时成立的坐标**（3 项新换名记录见 §0；R12-A…R31-A 共 102 项换名检查全部复核维持）。

本轮不铸新轴，边际产出是一次**重开条件哨兵审计（reopen-condition sentinel audit）**——A 档案三十一轮积累的全部在案重开条件第一次被系统性汇编并可执行验证（§2）：R31-A 停靠门三条件 (a)/(b)/(c)（F11a 生产普查逐字重跑 11 项断言 + F12a 全 src/ 调用面普查 4 项断言——生产在 injected-callable 平面仍恰占一个坐标、readers 仍零流量、四个公开入口的引用文件集与 R31-A 期完全一致零新增调用方）；C 轴第一优先重开（R15-A ~2.5 个量级——F12c 生产 requiredChecks/constraints 基数形未变）；X0-7 阈值冻结（F12d DEFAULT_TRACKING_CONFIG 四元组可执行见证）；BINDX / R31-E codec / REPRX 三面在本切片的划界计数（F12b：恰 6 个 `Math.*` 环境内建 token 于 5 行——同 BINDX 坐标不铸不剂量；fs 编解码位点 0——R31-E 面在 A 结构性空；`JSON.parse` 输入位点 0——REPRX 空）。**全部哨兵故意不计时**（R31-F 纪律；对可调用物 / 内建绑定 / 分派历史计时即 R28-F + R29-A + R30-A + BINDX 再剂量），32 项断言 × 3 次官方运行全过，**没有任何一条在案重开条件被触发**。凡要求 `src/` 变更的重开条件（S8-A-1/S11-A-2 签名族、S6-H-2 型热环出现、停靠门 (c) megamorphic 化）由 0 行冻结直接判空。

切片 14 文件自 R1-A 基线（`7acb666`）经 R2-A … R31-A 至本轮两 tip（`d97e119` / `b9ceed0`）**逐字节未变**（`git diff 7acb666 -- <切片>` 在派发 tip、快进 tip、提交前**三验均 0 行**，三十二遍全程零 diff；清单核对恰 14 文件（12 + 2）并全部实际重读，`wc -l` 合计 **1796** 行与 R31-A / R30-A 逐数一致）。调用面维持最强冻结再延一轮：`git diff 89f0455..b9ceed0 -- src/` **完全为空**（R10-A 基线以来跨 R10…R32 共二十三段全部提交均为 docs，含本轮快进带入的 R31-I 收口）。生产调用方经本轮全库 grep + F12a 普查复核仍为 `src/run/supervisor.ts:483/:487`（applyTrackingGate / nextTrackingSeq）、`src/run/coordinator.ts:444`、`src/run/flowchart-run.ts:320`（applyChildThreeLine），每子结果一次（~5 次/run），事件表几十级（E=41）。稳态下即使把整个切片优化到零成本，节省上界 ~0.068–0.079 ms/run，仍比落地线（数十~数百 ms）低约**三个量级**——第三十次支配论证成立。聚焦套件 72/72 绿；`pnpm gate`（typecheck + lint + test + build）全绿（test 1169 / pass 1168 / fail 0 / skipped 1，与 R31-A 逐字同计数；exit 0）。空枚举与 R12-A…R31-A 同为战役允许的诚实结局。**MORE_OPTIMA=no。**

## 0. 范围与约束遵守

- 切片：`src/tracking/`（12 文件：analysis / combined-score / config / from-child / gates / human-score / index / isolation / prescore / roller / turn / types）+ `src/run/child-tracking.ts` + `src/run/gate-apply.ts`，**清单核对恰 14 文件，全部实际重读**（`wc -l` 合计 1796 行）。F11a/F12a 普查对 coordinator.ts / flowchart-run.ts / supervisor.ts 及全 `src/` 树做**只读**静态见证（零编辑）。未触碰其余切片、未触碰在飞的 R31-I / R31-J。
- 先读并遵守（顺序强制）：README.md / PROGRESS.md / EXCLUSIONS.md（全表 545 行）/ round-32/PLAN.md（A 段 + 具名/unnamed 全 ban 榜，快进前后两版）/ round-31/PLAN.md + R31-A…R31-H 全部八份报告（结论 + 停驻门：R31-A injected-callable 停靠三条件、R31-B 六停驻门、R31-C BINDX、R31-D R22-D 属地格、R31-E codec 面、R31-F 故障处置分解、R31-G 18 门穷尽归属、R31-H 四扇边界参数残门）/ R31-I 结论与裁决段（快进带入后按令补读补划界）。之后才读 14 切片文件并动手。EXCLUSIONS.md / PROGRESS.md / 各 PLAN.md / README.md 零触碰。
- **不移植禁令逐项核对**（派单 DO NOT REMINT 全清单 + PLAN 禁榜）：R30-A call-stream ordering / dispatch-history（不重铸——本轮零次序臂零计时新臂）/ R29-A input reference-identity / aliasing-topology（不重铸——锚点夹具沿用血统引用结构钉死）/ R28-A key-set geometry / R27-A scalar numeric value-class / R26-A collation-locale / R31-A injected-callable 平面（**不重铸**——本轮仅以不计时哨兵复核其三条重开条件，见 §2.1）/ **BINDX**（R31-C——切片 6 个 `Math.*` token 是同 BINDX 坐标，F12b 划界计数即止，零剂量零 patch）/ **I/O text-codec topology / per-run transcode-byte manifest**（R31-E——切片 fs 编解码位点 0，面结构性空，声明即止）/ R31-B 六停驻门（catalog-row hidden-class/IC-shape 门在 §0 换名记录 2 作属主引用即止）/ R31-D R22-D 属地格 / R31-F 故障通道处置残格（本切片 DomainValidationError 抛出后的调用方处置属其分解，零重铸）/ R31-G 门扉闭合审计（无新名；其第 4 类「零生产流量停放」法条在 §0 记录 3 引用）/ R31-H 四扇边界参数残门（**A 侧边界参数普查不重做**——A 的每个边界参数已各有属主：config=R22-A 双臂+X0-7、maxItems=S2-A-1、readers+generateEventId=R31-A、humanInput=R21-A H 面、gateFacts=锚点格承载、policyVersion/nowIso=常量输入串，无「新独立物理对象 + 真实生产流量」者，声明即止）/ R28-F boundary call-protocol（含 R29-H/R30-H 停放维持）/ R29-F/R30-F 输出保留/消费 / R29-E 时钟拓扑 / R30-I 进程生命期分段 / R28-J ZREP + R30-J LINKTOP（切片数据路径零磁盘 I/O 零文件指称）/ R30-G M1 / R29-G M2 / R30-H / R29-H / R29-C PAIRX / R29-J CSPELL / R30-C REPRX（F12b：切片零 `JSON.parse` 输入位点）/ **R31-I CLI 进程边界门扉闭合审计**（快进带入的最新收口兄弟项，结论实读后划界：其审计对象是 CLI **进程**与外界的接触面——argv/env/stdin/stdout/子进程/信号/退出；本切片是进程内纯同步函数库，零 OS spawn、零 stdin/stdout 消费、零信号处置、零退出腿——审计平面正交零交集，不移植、不重做）。FITQ / NAMESHAPE / SCALEX / XPROC / YMIX / SCHEDWIN / KFAN / NVG / SEEDX / TERMCLASS / SHAPEK / ORDX / CKMIX / CALLB / OCCX / JB / RUNLIM / VALCLASS / CG / AGEX / COTARG / PRICEX / RTL / DELINV / PROBX / RVL / SIDEC / PWL / ZREP / PAIRX / CSPELL / REPRX / LINKTOP / **BINDX** 全部未编号未移植。`settleSupervisedOutcome` A 属地 no-op（R31-G 停放）不作「我注意到了 no-op」式重铸——F11a 第 9 断言仅作哨兵复核（第二十三次复验 O(1) no-op 早退）。npm POSIX `.bin` symlink 静默 no-op（R28-I）不当优化修。catalog 行序契约（R28-B W3）零触碰。
- **换名重提检查**（本轮枚举全部新角度的逐项裁决，一律不铸 S32-A-\* ID。R12-A…R31-A 共 102 项换名检查全部复核维持；以下 **3 项为本轮新记录**）：
  1. **wait_user 腿外向注入回调**（gate-apply.ts:153 `createMessageId(() => waitingId.slice("evt_".length))`——切片向 domain 函数注出可调用物，是 R31-A 停靠平面的**被调侧反向镜像**）= 同一 injected-callable 坐标类（R28-F BIND/WRAP 近旁 + R31-A 停放）+ **生产零流量**（from-child 生产路径 humanInput ≡ `{}` ⇒ userRejectStop 恒 false、maxItems 恒 undefined ⇒ failClosed 恒 false ⇒ askUser 恒 false；mapGateDirective 对 hard 非 user-reject-stop 恒走 queue_analysis，wait_user 腿在 from-child 生产流上不可达）+ ns 级，拒列；
  2. **输入对象完整性级/原型链/访问器属性作受控变量**（frozen/sealed 输入、null-proto、accessor-vs-data）= 对象形状/hidden-class/IC 机器本体 —— SHAPEK 属地 + R31-B 停驻的 catalog-row hidden-class/IC-shape 门同类非独立对象；且访问器本身就是经输入图注入的可调用物 = R31-A 停放平面成员；生产恒对象字面量单坐标，拒列；
  3. **调用方栈深/内联预算平面**（边界函数在不同调用栈深度被呼叫）= 价只能归因到引擎内联/IC 态 —— R30-A dispatch-history + R28-F call-protocol 的再剂量通道；生产每站点恒定单一栈形（R31-G 第 4 类零剂量法条），拒列。
- 其余枚举角度全部落在既有 ID **本体**上（非换位点，无需新记录）：extractTenPoint 每调用 `new RegExp(source,"gi")` 提升 = **X0-6 本体**（/g lastIndex 状态风险）+ humanInput ≡ `{}` 零生产流量；输入数组 elements-kind / 字符串 rope-flatness = REPRX 本体；for...of 迭代器协议 / iterator-protector 态 = BINDX 本体（R31-C 已实测 protector 失效 +2.47 s 响应，坐标已有主）；prevSummaryHash 跨轮链深 = R20-A 多轮驻留链面本体（且 from-child 生产 window.previous 恒 undefined 零流量）；幂等双 find/死扫描 = S8-A-1/S11-A-2；五扫描逐条 = X0-4/X2-4/S1-A-1/S1-A-9/R1-A 清单；写侧 validateEvent = S4-G-2 族；第二次 prescore = S1-A-2；turn 恒定输入常量折叠 = R12-A §0-11 伞；executionAuthority / bindExecutionContext / evidenceWeight 死导出 = R22-A 公开导出普查本体；skip/replay/hashSummary/H 面/分析-隔离面/G×E/组成/增长/键集/别名/值类/内容类/collation/调用流时序 = R13-A…R31-A 各 §0 维持。
- 未重开任何 X\* / S1-\* … S31-\* 条目。**单变量纪律**：本轮无计时新臂——锚点矩阵与分解全部沿用血统 E=41 冻结夹具（组成/增长/键集几何/别名/值类/内容类/locale/温度稳态热/单 realm 单基质/humanInput ≡ `{}`/M=0/K=0 逐轴钉死）；F11a/F12 纯静态只读或纯语义断言（不计时）；F11b 纯语义证人（不计时）。S11-A-1 / S11-A-2 / S10-A-1 / S9-A-1 / S8-A-1..3 / S7-A-1..4 / X0-4/X2-4 / X0-6 / S6-A-1 / S5-H-1（切片外，未提议移除）专项复核：本轮零触碰零重提，全部维持关闭。
- **配置态 × 命令类矩阵复核（R7-I 教训）**：命令类由锚点五格（gate-outcome × contract × max-codes）覆盖；配置态逐面复核——(a) settle 带/不带 assessment：生产两调用点不带（O(1) no-op 由最强冻结覆盖，F11a 第二十三次复验断言）；(b) trackingConfig 覆写/applyUserThreshold：生产恒 DEFAULT（R22-A 双臂已价，X0-7 维持，F12d 四元组可执行见证）；(c) maxItems 截断态 = S2-A-1 已拒面；(d) 注入可调用物坐标 = R31-A 停放（F11a 哨兵复核）。无测量盲区。
- 公开 API、版本化阈值（softThreshold 0.55 / hardFailCap 0.3 / minorPDip 0.03）、哈希契约、事件 schema、CAS/幂等键格式全部不变——本轮零 diff，天然满足。三线规格（分析不改 in-flight、Tracking 无命令权、H/score 不写路由 PASS/FAIL、双 LCB 双归因保留、提升 proposal-first、无阈值变更、无 fsync/完整性重哈希移除、fail-open 家族保持关闭、未改任何测试）同样天然满足——本轮只经公开 applyChildThreeLine / applyTrackingGate / nextTrackingSeq / hashAssessment / parseTrackingAssessment / validateEvent / computePrescore / runTrackingTurn / assessChildObservation / observationFromChild / prescoreInputFromObservation 驱动。不声称 Outcome-supported；Checkpoint F-PROD 仍开放（ADR-005）。无赢家 ⇒ 零生产改动，未提交任何仿真脚本（按令不建 repo scripts；输家/驱动脚本只进本报告附录）。不开 PR。

## 1. 预算支配论证复核（方法第 1、2 步）

R3-A §3 … R31-A §1 的整片预算论证两前提本轮逐一复核：

1. **切片代码未变**：`git diff 7acb666 -- <切片>` 在派发 tip `d97e119` 与提交前两验均为空，逐字节一致（三十二遍全程零 diff）——R1-A…R31-A 全部测量与排除按令转移，但数值在本 VM 重新锚定，未抄写旧数。
2. **调用面未变（最强冻结跨二十三段）**：`git diff 89f0455..d97e119 -- src/` **完全为空**。生产调用方三点位经全库 grep + F12a 引用文件集普查复验，无新增调用方。

### 1.1 锚点矩阵（本 VM 重锚，三次官方运行，E=41，~5 gate/run，含 R14-A 第五格 max-codes）

```text
                                   run1      run2      run3
PASSED x no-contract               15.6      15.7      15.2  µs/gate (+1 event)
PASSED x contract(3)               14.1      13.8      13.5  µs/gate (+1 event)
FAILED x no-contract               15.7      15.5      15.6  µs/gate (+3 events)
FAILED x contract(3)               14.0      14.0      13.9  µs/gate (+3 events)
FAILED x contract(3) x 2-code      14.8      14.9      14.8  µs/gate (+3 events)
=> 13.5–15.7 µs/gate ⇒ ~68–79 µs/run（稳态热；无孤点、无悬崖）
```

与 R31-A（13.6–16.9 ⇒ 68–84）/ R30-A（12.8–15.3 ⇒ 64–77）/ R29-A（61–80）带交叠，窗口相对 R31-A 上缘回落 ~7%（同 VM 家族不同会话）。**层级保序三 run 全成立**：contract 层（13.5–14.1）< max-codes 层（14.8–14.9）< no-contract 层（15.2–15.7），与历轮结构同构；层内 P/F 次序本轮摆动 ≤0.2 µs（run1 F_C 14.0 < P_C 14.1、run3 P_NC 15.2 < F_NC 15.6，run2 恢复 R31-A 序）——属 R11-A 起在案的 no-contract 格 PIC/基准次序现象族（R30-A 已以受控臂定价该族为 µs 拒收类，卫生注记不入价）。稳态下即使切片零成本，节省上界 ~0.068–0.079 ms/run，仍比战役落地线低**约三个量级**。**支配论证（稳态口径）第三十次复核成立**；生产冷态口径维持 R17-A 轴 6b 裁决（[2.7, 8.5] ms/run once-per-run，不重测）。

### 1.2 合同地板分解复核（R12-A 方法学，PASSED×contract(3) 生产格，三次官方运行区间；括号内 R31-A 区间对照）

| 组件 | 本轮实测/gate | R31-A | 锁定裁决 |
| --- | --- | --- | --- |
| `hashAssessment` × 3（caller + CAS + 写侧再验） | 1933–1954 ns 每次 ⇒ **5.80–5.86 µs** | 5.69–5.80 µs | R1-A CAS fail-closed；S4-G-2 写侧；X1-1/S8-G-2 memo 禁（贴带顶 +0.3–1.1%，与锚点窗口同向） |
| `validateEvent`(TRACKING_ASSESSMENT) 整体 | **3.00–3.02 µs** | 3.01–3.06 µs | S4-G-2 族 schema 屏障；实现在 events.ts（G 切片）；带内或低带底 0.4% |
| `runTrackingTurn` 完整 | **1.64–1.82 µs** | 1.64–1.74 µs | S9-A-1/S7-A-1/S10-A-1/S11-A-1 平行路径拒绝结构（run2/3 带内；run1 1.82 高带顶 4.4%，注记见下） |
| `computePrescore` × 2 | 666–788 ns 每次 ⇒ **1.33–1.58 µs** | 1.32–1.34 µs | S1-A-2（run2/3 逐 ns 落带；run1 788 高 +18%，注记见下） |
| `parseTrackingAssessment` 单测 | **333–363 ns** | 336–361 ns | 贴带（333 低带底 0.9%） |
| O(E) 五扫描 + 拷贝 | **277–281 ns**（⇒ 6.76–6.86 ns/event @E=41） | 276–280 ns | X0-4/X2-4/S1-A-1/S1-A-9/S8-A-1/R1-A 不可变拷贝下界（带内） |
| `applyTrackingGate` 整体隔离 | **5.84–5.89 µs** | 5.87–5.96 µs | 带内或低带底 0.5% |

**诚实注记**：run1 的 prescore（788 ns）/ turn（1817 ns）两个子组件高出 R31-A 带 +18% / +4.4%，run2/3 两组件双双回落带内（666–667 / 1643–1667）且**同 run 的五格整门锚点与 applyTrackingGate 整体全部带内**——单组件基准段相位抖动，非结构变化（分解组件是诊断口径，整门口径三 run 无异常）。

**R18-A skip / R19-A replay 定价抽测复现**（20k 次/格，三次官方运行区间）：guard-3 skip（FAILED 零 evidence refs）**2000–2080 ns = 14.6–14.8% of apply**（R31-A 1983–2044 = 14.0–15.0%，交叠）；replay find#1（GT idempotencyKey 命中，throwing generator 实证重放臂零 id 铸造）**3087–3142 ns = 22.2–22.8% of apply**（R31-A 3104–3148 = 22.2–22.9%，交叠）——组成天花板 {apply, skip, replay} 继续成立（次序半边由 R30-A 19d 收口，本轮不重测）。

## 2. 重开条件哨兵审计（本轮边际产出；全部不计时）

**方法与定位。** 本轮不铸新轴、不开新剂量网格：R31-A 元闭合 + R31-G/R31-H 姊妹审计之后，A 档案里唯一还没被系统性盘点过的「账目」是**在案重开条件本身**——三十一轮停靠/关闭裁决各自附带的重开条件从未被汇编成清单并逐条可执行验证。本轮做第一次全量哨兵过检。这不是新面（不给任何平面计时、不当受控变量拉伸——那会再剂量 R28-F/R29-A/R30-A/BINDX），是对既有裁决有效性的可执行审计。仪器 `/tmp/r32a-sim.mts`（704 行，附录 A/B），**32 项断言 × 3 次官方运行全过**。

### 2.1 R31-A 停靠门三条件（F11a 逐字重跑 + F12a 全 src/ 调用面普查）

| 重开条件 | 哨兵 | 裁决 |
| --- | --- | --- |
| (a) 生产在可调用物平面获得第二坐标（.bind/包装器/共享长寿生成器） | F11a 11 项断言逐字重跑：coordinator:444 / flowchart-run:320 各恰 1 站点每调用新铸箭头；child-tracking / supervisor 门腿纯透传；supervisor 恰 2 个 settle 站点均新铸箭头且不带 trackingAssessment（O(1) no-op 第二十三次复验）；全平面零 `.bind` 零包装工厂 | **未触发**（生产仍单坐标） |
| (b) readers 获得生产流量（expandDetail 接线） | F11a：from-child 生产 turn 调用零 readers、humanInput ≡ `{}` | **未触发**（零流量） |
| (c) 内部调用位点 megamorphic 化（>2 种可调用物种类共存） | 静态充分条件：需 src/ 出现新调用方或新可调用物种类——F12a 全 src/ 树引用文件集普查：`applyChildThreeLine` 恰 {child-tracking(def), coordinator, flowchart-run}；`applyTrackingGate` / `nextTrackingSeq` 恰 {gate-apply(def), child-tracking, supervisor}；`runTrackingTurn` 恰 {turn(def), from-child, tracking/index(re-export)}——与 R31-A 期集合逐文件一致 | **未触发**（零新调用方 + 冻结） |

### 2.2 其余在案重开条件

| 条目 | 重开条件 | 哨兵 / 裁决 |
| --- | --- | --- |
| C 轴第一优先（R15-A；S1-A-4/X4-7 家族 `every×includes` 站点 Set 化） | 生产规模增长 ~2.5 个量级 | F12c：observationFromChild 生产 requiredChecks 形未变（tester + 1 acceptance ⇒ 长度 1；contract(3) ⇒ 3 条 mandatory constraint）；产形站点冻结 ⇒ **未触发**。可操作重开第一优先地位不变 |
| X0-7 阈值冻结 | 规格解冻 | F12d：DEFAULT_TRACKING_CONFIG 四元组 (0.55/0.3/0.54/0.03, version 1) 可执行见证 ⇒ **冻结在位** |
| S8-A-1 / S11-A-2 签名族、S6-H-2 型「热环出现」、S2-A-1 截断态、S13-A/R12-A 常量折叠伞 | 均要求 src/ 变更（公开签名/调用图变化） | `git diff 89f0455..d97e119 -- src/` = 0 行 ⇒ **全体判空** |
| F11b 语义证人连续性 | （非重开条件，连续性见证） | 新铸箭头 / 共享闭包 / 预绑定三种身份 3 臂 × 100 调用 result + 追加字节逐位一致 ×3 run——门仍当不透明 id 源消费，**不计时** |

### 2.3 姊妹面划界计数（F12b，声明即止）

- **BINDX（R31-C）**：切片环境内建 `Math.*` token 恰 **6 个于 5 行**（turn.ts:64 `Math.max`；prescore.ts:66 `Math.max`、:72 `Math.min`；combined-score.ts:24 `Math.min`+`Math.max`；gate-apply.ts:232 `Math.max`）——realm 级内建绑定态的**同 BINDX 坐标**，非新轴，零触碰零剂量零 patch（`Number.*`/`JSON.stringify`/`Array.isArray`/for...of 迭代器协议同属该面，计数即止）。
- **R31-E I/O text-codec 面**：切片 `node:fs|readFile|writeFile` 命中 **0**——面在 A 无物理对象，声明后移步。
- **REPRX（R30-C）**：切片 `JSON.parse` 输入位点 **0**（parseTrackingAssessment 收内存对象非字节流）——面空，声明即止。
- **元闭合审计表独立重推**：R31-A §1.3 十三行逐面属主本轮从零重推，无一面因 R31-I/J 前的状态而松动；R31-G/R31-H 后无新增未审计平面。第三十二遍枚举在该坐标系内**无剩余未审计平面，也无剩余未验证重开条件**。

## 3. 候选总表

**空。** 本轮枚举的全部角度（§0 新记录换名检查 3 项 + R12-A…R31-A 102 项复核维持 + 本体命中若干）均为既有排除 ID 的换名/换位点、姊妹归档面或停靠门成员；哨兵审计按定义不产出候选（不计时、不剂量）。空枚举与 R12-A…R31-A 同为战役允许的诚实结局。

## 4. 结构位置 × 消除机制满矩阵扫描（第三十二遍）

行 = 结构位置（18 个），列 = 消除机制（死码/常量折叠/CSE 去重/记忆化/数据结构/融合/早退重排/免分配/复杂度类/强度削减/惰性/平行路径特化/形状单态化，13 类）。全矩阵 234 格逐一过筛，非空格全部命中既有 ID 或本轮 §0 换名检查；按文件汇总：

| 文件 | 本轮新视角检查 | 结论 |
| --- | --- | --- |
| `gate-apply.ts` | wait_user 腿外向回调 = §0 换名记录 1（生产不可达 + R31-A 平面成员）；nextTrackingSeq `Math.max` = BINDX 坐标（F12b 计数）；replay find#1 3087–3142 ns 复现（throwing generator 实证零 id 铸造）；O(E) 清单 / S11-A-2 / S8-A-1 / S2-A-4 维持 | 无新候选 |
| `child-tracking.ts` | 锚点/分解宿主复测同带（五格 13.5–15.7 µs/gate）；F11a 透传断言宿主；guard-3 skip 2000–2080 ns 复现 | 无新候选 |
| `turn.ts` | readers 零流量哨兵宿主（重开条件 (b) 未触发）；mergeOpenMinors `Math.max` = BINDX 坐标；runTrackingTurn 1.64–1.82 µs（run1 注记 §1.2）；死参裁剪/守卫折叠 = R31-A §0-4/5 维持；R13-A 八子组件 / R12-A §0-11 / R16-A §0-2 维持 | 无新候选 |
| `types.ts` | hash×3 地板 5.80–5.86 µs（贴 R31-A 带顶）；parse 333–363 ns；输入完整性级/原型链/访问器 = §0 换名记录 2 拒列；X1-1/S8-G-2 memo 禁维持 | 无新候选 |
| `from-child.ts` | F11a readers 零流量断言宿主 + F12c 基数形哨兵宿主；S1-A-2 / S2-A-2/6 / S3-A-3 / S5-A-3 / S6-A-2/3 / S7-A-1 / S9-A-1 / S10-A-1 / S11-A-1 十二层收口维持 | 无新候选 |
| `prescore.ts` | C 轴哨兵对象（`every×includes` 两站点；重开未触发，第一优先地位不变）；S1-A-3 / S1-A-4/X4-7 / S6-A-1 / S8-A-3 维持 | 无新候选 |
| `combined-score.ts` | `Math.min/max` = BINDX 坐标（F12b）；S6-A-1 / S7-A-4 / R13-A §0-1 维持 | 无新候选 |
| `gates.ts` | 分支面维持（重排 = R31-A §0-5 族）；X0-7 哨兵 F12d 见证；S1-A-6 / S7-A-3 / S8-A-3 维持 | 无新候选 |
| `roller.ts` | prevSummaryHash 链深 = R20-A 本体 + from-child 零流量（§0）；S2-A-1 / S3-A-1 / S7-A-2 / S9-A-1 / S1-A-5 维持 | 无新候选 |
| `human-score.ts` | extractTenPoint 每调用 RegExp 铸造 = X0-6 本体 + 生产零流量（humanInput ≡ `{}`）；S1-A-3 / S7-A-1 / R14-A §0-2 / R21-A 维持 | 无新候选 |
| `config.ts` | 生产恒 DEFAULT（F12d 可执行见证宿主）；X0-7 维持 | 无新候选 |
| `analysis.ts` | R22-A axis 11 整面维持（零 src/ 生产调用方）；S8-A-2 维持 | 无新候选 |
| `isolation.ts` / `index.ts` | R29-A F9c(iv) 契约角维持；index.ts 纯再导出零逻辑（F12a 见证其 runTrackingTurn 再导出行在案） | 无新候选 |

**对 R31-A 声明的裁决**：「元坐标系逐面有主、无剩余未审计平面」经本轮独立重推成立；本轮把审计对象从「平面」推进到「重开条件账目」，产出全部为哨兵归档，无可落地候选。收口的预言力第三十二遍成立。

## 5. 前后对比

无代码 diff。仓库变更仅本报告一个文件（`git diff origin/cursor/sota-persistent-opt-83a1...HEAD` 提交前复核 = 恰本文件）。仿真为临时脚本未入库（无赢家不落仿真文件；驱动全文关键新增块见附录 A，整驱动 sha256 见附录 B，临时文件已删除），未触碰任何测试。

## 6. 测试

零代码改动下相关套件基线复核，全绿（与 R1-A…R31-A 同套件同计数）：

```bash
npx tsx --test "test/unit/tracking/**/*.test.ts" \
  "test/unit/run/gate-apply.test.ts" \
  "test/integration/track/**/*.test.ts"
# tests 72 / suites 13 / pass 72 / fail 0
```

全仓门禁复核：`pnpm gate`（typecheck + lint + test + build）在派发 tip `d97e119` 全绿（Node v22.22.2；test 计数 1169 / pass 1168 / fail 0 / skipped 1，与 R31-A 三跑逐字相同；退出码 0）。

仿真（临时脚本；seed 参数化可复现，三次官方运行摘要；每 run 32 项 check——F11a 11 + F11b 3 + F12 10 + 锚点 5 + 分解/抽测夹具 3——三 run 共 96 项全过）：

```text
run 1 (seed 0x32a001):
F11a: 11/11 | F11b: 3/3 | F12: 10/10（哨兵全过，无重开条件触发）
anchor: 15.6 / 14.1 / 15.7 / 14.0 / 14.8 µs/gate => ~70–79 µs/run
decomposition: hash×3=5800ns | parse=333ns | validateEvent=3004ns
    | prescore×2=1577ns | turn=1817ns | 5 scans+copy=280ns
    (6.83 ns/event) | applyTrackingGate=5.89µs
spot: skip guard-3 2080ns (14.8%) | replay find#1 3123ns (22.2%)
ALL 32 CHECKS PASSED (deterministic, seeded)

run 2 (seed 0x32a002):
F11a: 11/11 | F11b: 3/3 | F12: 10/10
anchor: 15.7 / 13.8 / 15.5 / 14.0 / 14.9 µs/gate => ~69–79 µs/run
decomposition: hash×3=5862ns | parse=363ns | validateEvent=3019ns
    | prescore×2=1334ns | turn=1667ns | 5 scans+copy=281ns
    (6.86 ns/event) | applyTrackingGate=5.87µs
spot: skip guard-3 2025ns (14.6%) | replay find#1 3142ns (22.7%)
ALL 32 CHECKS PASSED (deterministic, seeded)

run 3 (seed 0x32a003):
F11a: 11/11 | F11b: 3/3 | F12: 10/10
anchor: 15.2 / 13.5 / 15.6 / 13.9 / 14.8 µs/gate => ~68–78 µs/run
decomposition: hash×3=5849ns | parse=358ns | validateEvent=2997ns
    | prescore×2=1333ns | turn=1643ns | 5 scans+copy=277ns
    (6.76 ns/event) | applyTrackingGate=5.84µs
spot: skip guard-3 2000ns (14.8%) | replay find#1 3087ns (22.8%)
ALL 32 CHECKS PASSED (deterministic, seeded)
```

三次官方仿真 F11a/F12 普查行与 F11b 证人行逐字节一致；锚点五格三次同带、层级保序、无悬崖、无孤点。空枚举裁决成立。

## 7. 新增排除 ID

**无**（空枚举，不铸 S32-A-\*、不铸 face 名、不取绰号）。换名检查 3 项新记录见 §0（1–3），归档备后续轮次引用，无需并入 EXCLUSIONS.md。

哨兵审计归档一条：

**A 档案重开条件哨兵审计（第一次全量）**：R31-A 停靠门 (a)/(b)/(c)、C 轴 ~2.5 量级规模条件、X0-7 阈值冻结、全部签名/调用图类条件——经 F11a（11 断言）+ F12a–d（10 断言）+ 最强冻结（`89f0455..d97e119` src/ 0 行）逐条验证，**无一触发**。BINDX / R31-E codec / REPRX 三面在 A 的划界计数（6 Math token / 0 fs 位点 / 0 JSON.parse 位点）随附归档。后续 A 轮可直接引用本清单做增量哨兵（任一条件触发时按对应属主轴的授权方重开，A 不单方解除姊妹停放）。**可操作重开第一优先不变**：C 轴（~2.5 个量级，R15-A；S1-A-4/X4-7 家族 `every×includes` 站点 Set 化）。

## 附录 A：本轮新增哨兵块（F12）源码

驱动整体为 R31-A 附录 A 脚本的逐行复刻（F11a/F11b/warm-up/锚点/分解/抽测六段零改动），改动仅：头注释更新为 R32-A 哨兵审计说明、warm-up 种子异或常量 `0x77a1`→`0x77a2`、check 计数器与收尾输出、`DEFAULT_TRACKING_CONFIG` import、以及下列 **F12 新增块**（插于 F11b 之后、warm-up 之前）：

```typescript
/* ============================================================
 * F12: NEW THIS ROUND -- reopen-condition sentinel audit (all UNTIMED).
 * ============================================================ */
{
  /* F12a: full-src caller census for the four production entry points.
   * Reopen conditions tied to call-surface growth (parked-door (a)/(c),
   * S8-A-1/S11-A-2 signature families, S6-H-2-style "hot loop appears")
   * require a NEW caller; assert the referencing file sets are exactly the
   * R31-A-era sets. */
  const srcFiles: string[] = [];
  const walk = (dir: string): void => {
    for (const entry of readdirSync(dir, { withFileTypes: true })) {
      const full = join(dir, entry.name);
      if (entry.isDirectory()) walk(full);
      else if (entry.name.endsWith(".ts")) srcFiles.push(full);
    }
  };
  walk("/workspace/src");
  const referencing = (symbol: string): string[] =>
    srcFiles.filter((file) => readFileSync(file, "utf8").includes(symbol)).sort();
  const rel = (files: string[]): string[] => files.map((f) => f.replace("/workspace/", ""));
  const setEq = (actual: string[], expected: string[]): boolean =>
    actual.length === expected.length && actual.every((v, i) => v === expected[i]);
  check(
    "F12a applyChildThreeLine referenced only by {child-tracking(def), coordinator, flowchart-run}",
    setEq(rel(referencing("applyChildThreeLine")), [
      "src/run/child-tracking.ts",
      "src/run/coordinator.ts",
      "src/run/flowchart-run.ts"
    ]),
    rel(referencing("applyChildThreeLine")).join(",")
  );
  check(
    "F12a applyTrackingGate referenced only by {gate-apply(def), child-tracking, supervisor}",
    setEq(rel(referencing("applyTrackingGate")), [
      "src/run/child-tracking.ts",
      "src/run/gate-apply.ts",
      "src/run/supervisor.ts"
    ]),
    rel(referencing("applyTrackingGate")).join(",")
  );
  check(
    "F12a nextTrackingSeq referenced only by {gate-apply(def), child-tracking, supervisor}",
    setEq(rel(referencing("nextTrackingSeq")), [
      "src/run/child-tracking.ts",
      "src/run/gate-apply.ts",
      "src/run/supervisor.ts"
    ]),
    rel(referencing("nextTrackingSeq")).join(",")
  );
  check(
    "F12a runTrackingTurn referenced only by {turn(def), from-child, tracking/index(re-export)}",
    setEq(rel(referencing("runTrackingTurn")), [
      "src/tracking/from-child.ts",
      "src/tracking/index.ts",
      "src/tracking/turn.ts"
    ]),
    rel(referencing("runTrackingTurn")).join(",")
  );

  /* F12b: sibling-face demarcation counts on the 14 slice files.
   * BINDX (R31-C): ambient Math.* intrinsic tokens -- same BINDX coordinate,
   * counted for demarcation only, never dosed. R31-E codec face: zero fs
   * sites => face void here. REPRX (R30-C): zero JSON.parse input sites. */
  const SLICE_FILES = [
    "src/tracking/analysis.ts",
    "src/tracking/combined-score.ts",
    "src/tracking/config.ts",
    "src/tracking/from-child.ts",
    "src/tracking/gates.ts",
    "src/tracking/human-score.ts",
    "src/tracking/index.ts",
    "src/tracking/isolation.ts",
    "src/tracking/prescore.ts",
    "src/tracking/roller.ts",
    "src/tracking/turn.ts",
    "src/tracking/types.ts",
    "src/run/child-tracking.ts",
    "src/run/gate-apply.ts"
  ];
  const sliceTexts = SLICE_FILES.map((f) => readFileSync(join("/workspace", f), "utf8"));
  const countAll = (re: RegExp): number =>
    sliceTexts.reduce((sum, text) => sum + [...text.matchAll(re)].length, 0);
  const mathTokens = countAll(/Math\./g);
  check(
    "F12b BINDX demarcation: exactly 6 ambient Math.* tokens on 5 slice lines (turn:64, prescore:66/72, combined-score:24 x2, gate-apply:232)",
    mathTokens === 6,
    String(mathTokens)
  );
  check(
    "F12b R31-E codec demarcation: zero fs codec sites in the slice (face void here)",
    countAll(/node:fs|readFile|writeFile/g) === 0
  );
  check(
    "F12b REPRX demarcation: zero JSON.parse input sites in the slice (parseTrackingAssessment takes in-memory objects)",
    countAll(/JSON\.parse/g) === 0
  );

  /* F12c: C-axis first-priority reopen sentinel (R15-A: prescore
   * every-x-includes Set-ization, needs ~2.5 orders of magnitude scale
   * growth). Production shape witness: observationFromChild caps
   * requiredChecks at the acceptance-criteria list (or ["test"]) and the
   * anchor spec yields exactly 1 required check; production constraint
   * lists remain contract-sized. */
  const mkS = mkChild("PASSED");
  const obsS = observationFromChild(mkS.child, mkS.spec, mkS.contract);
  check(
    "F12c production requiredChecks shape unchanged (tester + 1 acceptance criterion => length 1)",
    obsS.requiredChecks.length === 1 && obsS.requiredChecks[0] === "chk_0"
  );
  check(
    "F12c production constraints shape unchanged (contract(3) => 3 mandatory constraint records)",
    obsS.constraints.length === 3 && obsS.constraints.every((c) => c.mandatory && c.kind === "constraint")
  );

  /* F12d: X0-7 threshold-freeze sentinel (executable). */
  check(
    "F12d DEFAULT_TRACKING_CONFIG quadruple unchanged (0.55 / 0.3 / 0.54 / 0.03, version 1)",
    DEFAULT_TRACKING_CONFIG.version === 1 &&
      DEFAULT_TRACKING_CONFIG.softThreshold === 0.55 &&
      DEFAULT_TRACKING_CONFIG.hardFailCap === 0.3 &&
      DEFAULT_TRACKING_CONFIG.unobservedHighCap === 0.54 &&
      DEFAULT_TRACKING_CONFIG.minorPDip === 0.03
  );
}
```

## 附录 B：驱动指纹与运行方式

- 驱动：`/tmp/r32a-sim.mts`，704 行，sha256 `9c4a43976a21a23a56ea62484da57da197d63f94ecbb547d8c2f4131226ac0eb`（未入库；临时文件已删除）。
- 运行（repo 根，Node v22.22.2 + tsx）：

```bash
npx tsx /tmp/r32a-sim.mts --seed 32a001   # run 1
npx tsx /tmp/r32a-sim.mts --seed 32a002   # run 2
npx tsx /tmp/r32a-sim.mts --seed 32a003   # run 3
```

- 除附录 A 所列 F12 块与头注释/计数器差异外，其余段落与 R31-A 附录 A 逐行一致（F11a 11 断言、F11b 3 断言、3000 轮 seeded warm-up、五格锚点、合同地板分解、skip/replay 抽测）。

MORE_OPTIMA=no
BRANCH=cursor/r32-a-tracking-thirty-second-pass-83a1
