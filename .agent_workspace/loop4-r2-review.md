[Model: claude-fable-5-thinking-xhigh]

# Loop 4 · Round 2 — SOTA review (fable slot)

- **Reviewer:** claude-fable-5-thinking-xhigh
- **Scope:** R2-1…R2-10 as committed on `agent/opt-continuous` (range `c3fee5c`…`749fbe9`, one commit per slot), reviewed against the actual diffs and re-measured on this VM (Node v22.14.0, pnpm 10.17.1). Nothing below is taken from the slot reports on trust alone.
- **Verdict summary: 8 ACCEPT, 2 ACCEPT-WITH-NITS, 0 ROLLBACK.** 38 files, 3,855 insertions / 154 deletions; +42 tests (1508 → 1550). Both P1s landed (flowchart abort now fires; typed lock timeout + measured lock perf), the episode-delete race is closed, and the fuzzer found a real live decoder crash (§4). Unlike Round 1, the parent's integrated commits needed **zero reviewer fixes** — the per-slot "scoped eslint + whole-tree tsc" requirement did its job.

## 0. Integrated verification (what I ran)

| Check | Result |
|---|---|
| `pnpm gate` (typecheck + lint + test + build) | **GREEN, exit 0**, no reviewer intervention. 1550 tests / 1548 pass / 0 fail / 2 skipped. Skip #1 is the pre-existing one; skip #2 is R2-7's **named skip** on the unowned invocation-row TypeError (§4) — deliberate and correct. |
| `node scripts/bench-runtime.mjs` (quiet machine, before the gate) | `{"jsonlAppendMs":44.927,"jsonlAppendFsyncMs":222.654,"jsonlReadMs":0.485,"lockSerialMs":162.402,"lockContendedMs":176.052}` |
| R2-2's ≥5% bar vs the Round 1 review baseline (189.745 / 199.955) | **Independently met:** `lockSerialMs` −14.4%, `lockContendedMs` −12.0%. Consistent with the slot's own same-VM before/after (−12.66% / −12.50%). `jsonlAppendFsyncMs` reads 222.7 vs Round 1's 198.8 — `src/persist/jsonl.ts` has an **empty diff** this round and the jsonl append takes no lock, so this is fsync variance on this VM, not a regression (R2-2's own pre-change run measured 204.9). |
| `node scripts/crash-probe.mjs` | `ok: true` — all **6** cases (3 original + R2-6's `feedback-cascade-strip-before-tombstone`, `episode-settle-stale-lock-recovery`, `atomic-write-stale-unique-temp`) × 3 iterations. |
| Resume (for §5) | `test/integration/m2/{resume,supervisor,scheduler}.test.ts` + `test/integration/m2.5/resume.test.ts` → **16/16 pass**, run explicitly and separately from the gate. |

## 1. Shared-API freeze and forbidden list: **clean**

- `appendJsonlLine(filePath, line, fsync)` / `readJsonlObjects(filePath, corrupt)` — `src/persist/jsonl.ts` has an **empty diff** over the whole round; signatures byte-identical.
- `writeFileAtomic(path, contents, options?)` — `src/persist/atomic-file.ts` untouched. R2-6's probe injects `rename`/`uniqueSuffix`, and I verified both are **pre-existing** T3 seams on `AtomicWriteOptions`, not a new src edit.
- `episodeLockPath` symmetry — `src/run/episode-bind.ts` untouched; `src/cli/episode.ts:91` still builds the identical path; R2-3 **imports** `episodeLockPath` (its test #6 pins the import and forbids a rebuilt template).
- Invocation-sink wiring — `src/cli/main.ts` untouched; both `runCommand` `createExecutor` sites (lines 642–647 and 689–694) still pass `onInvocation: invocationSink`; the source pin is intact.
- `withExclusiveFileLock` — `git log c3fee5c..HEAD -- src/persist/file-lock.ts` shows **exactly one commit, R2-2's** (`8a5288c`). The one-slot unfreeze held.
- Forbidden list — no live R1/bandit/topology on the execution path (the only learning-plane edit, R2-4's `auto-loop.ts`, changes persistence disclosure, not learning behavior; `autoPromote` handling unchanged). Zero Outcome-supported claims in the diff, ADR-006 (`docs/decisions/0006-…`) untouched, no `package.json` edits, no history rewrites.
- Saturation respected — no re-work in `persist/jsonl` (empty diff, `test/unit/persist/jsonl.test.ts` untouched) or protocol parse (`test/unit/protocol/fuzz.test.ts` untouched; `src/protocol/v1.ts` changes are doc comments only — `assertAtMostOneTerminal`'s body and every validator are byte-identical).
- Ownership disjointness — the 28 non-workspace files across the 10 commits partition exactly along the brief's ownership lines; no slot's commit touches another's files.

## 2. Per-slot verdicts

### R2-1 — flowchart run-level abort: **ACCEPT**
The P1 is genuinely closed. Verified in `src/run/flowchart-run.ts`: module-private `RunAbortScope` (controller + live-handle set + `cancelAndSettle()` that re-cancels until nothing is in flight, because an unwinding child can spawn peers); teardown fires at `persistFailed` (**before** the `RUN_FAILED` append — stop paying, then record), `pauseIfRequested` (before `PAUSE_REQUESTED`), `finish` (every terminal status), and `withRunTeardown` (escaping errors, wrapping both `startFlowchartRun` and the resume path); `onSpawn` refuses new peers once aborted and `track`s the rest; `executeRemainingRunningNodes` finally passes `signal: ctx.abort.signal` instead of letting the thin-executor helper mint an unfireable controller. The resume-tail extraction into `resumeRestoredRun` is mechanical (`deps.*`→`ctx.*`; ordering unchanged). The negative control is the strongest in the round: with HEAD's old file restored, all 4 unit tests fail **and the integration file times out at 20 s because the orphaned peer keeps the event loop alive — the money leak observed directly**. Disclosures are honest, including the two it hands to Round 3: an escaping-error death still writes no `RUN_FAILED`, and a child whose `runTask` throws writes no terminal child event (§6 R3-5).

### R2-2 — typed LOCK_TIMEOUT + lock perf: **ACCEPT** (metric bar independently confirmed)
Verified in `src/persist/file-lock.ts`: `FileLockTimeoutError extends DomainValidationError` with `readonly code = "LOCK_TIMEOUT"` (`LOCK_TIMEOUT_CODE` exported), thrown message byte-identical to before, `withExclusiveFileLock` signature unchanged. Perf: happy-path `mkdir` removed (ENOENT-lazy, T7's exact pattern) plus a `setImmediate` first-contention retry and `min(retryMs, remainingMs)` cadence. The invocation sink's `isLockTimeout` now checks the code, not the message, and the new negative test proves it: a `DomainValidationError` carrying the **exact timeout message but no code** is not retried. My bench beat the 5% bar on both metrics (§0). Two residuals, neither R2-2's fault: `feedback/store.ts` still string-matches (R2-4 built it concurrently and deliberately uncoupled — fold the migration into Round 3, §6 R3-2), and the ENOENT→mkdir loop has no bound (same accepted posture as T7's append; contrived to trigger).

### R2-3 — episode delete under the episode lock: **ACCEPT**
Verified in `src/privacy/deletion.ts`: cascade (unchanged, still first, still fail-closed) → new `unlinkEpisodeFiles` that bounds-acquires `episodeLockPath(stateRoot, episodeId)` via `withExclusiveFileLock`, reads the episode text **inside** the lock, unlinks the two record files, and never hand-unlinks the lock (release removes it; it is correctly dropped from `removedPaths` — a file the delete created is not a file it removed). No-op stays lock-free and directory-free; a records-free lock is treated as a live writer and waited on (pinned by test #5, the anti-"nothing found" case). The two locks are sequential, never nested, with the half-way state (text stripped, shells intact) disclosed and pinned. The deliberate behavior change — a stale lock now **fails the delete closed** instead of being silently stolen — is the no-steal posture applied consistently, and R2-6's `episode-settle-stale-lock-recovery` probe exercises exactly this recovery on the integrated tree. Mutation check (`# fail 3` with the lock call bypassed) is credible. Two doc artifacts it flagged in other slots' files remain stale (§6, folded into R3-10).

### R2-4 — feedback append drop-window parity: **ACCEPT-WITH-NITS**
Verified: `appendFeedback` gains only an optional `FileLockOptions` third argument (defaults unchanged, T1 contracts untouched); `appendFeedbackWithRetry` returns the two-state `FeedbackAppendOutcome` (no third state to mistake), retries only lock timeouts with a bounded budget, reports drops through a throw-guarded `onDrop`, and chains a per-path `retryQueues` so a retrying row lands ahead of later rows. The warn-don't-fail decision for the auto-adapt persist is well-argued (the run is over, the diagnosis is in memory, the only terminal-drop cause is a long cascade) and honestly bounded: only the lock-timeout class degrades; EISDIR/EACCES/validation still reject the iteration (pinned). Disclosure is three-way (`feedbackPersisted`/`feedbackDropped`/`feedbackDropReasons` plus the count folded into `reason` on **all four** return sites — verified in the diff).
Nits: (1) it lands a **new** exact-message `isLockTimeout` in `store.ts` in the same round the typed discriminator shipped — justified at the time (R2-2 might not have landed; the classifier survives R2-2's change because the message is unchanged and `FileLockTimeoutError` is a `DomainValidationError` subclass — I verified compatibility), but it is immediate, self-documented debt; migrate to `LOCK_TIMEOUT_CODE` in Round 3 (§6 R3-2). (2) `feedbackDropReasons` unbounded per call — bounded in practice by in-memory signals, disclosed.

### R2-5 — `maxCostUsd` honesty + O(n) terminal check: **ACCEPT**
Half (a): the enforce-vs-document investigation is evidence-complete (usage stream is token-only; `InvocationPricing` is declared and never populated anywhere in `src/`; the experiments-plane gate reads its own thresholds, never `ChildRunLimits`; no producer even sets the field) and the documented non-enforcement now lives at **both** reader surfaces (`protocol/v1.ts` field doc + `ChildTaskInput.limits` doc) with two pins: a source pin that goes red if anyone wires a `limits.maxCostUsd` read without rewriting the doc, and the behavioral "maxCostUsd is inert" test (10M tokens under a $0.000001 ceiling still settles SUCCESS). A declared limit now either works or says it doesn't. Half (b): `AttemptTranscript` with a `sawTerminal` flag replaces `assertAtMostOneTerminal([...seen, message])`; per-attempt semantics preserved (cascade-escalation test untouched and green); rejection still precedes recording and the parent `CHILD_MESSAGE` append; the duplicate-terminal wording is byte-identical at both throw sites (verified: `child-coordinator.ts:179` ≡ `protocol/v1.ts:363`) and pinned by a comparison test. The validation-count proxy test (980 → 120 getter reads for 40 messages, mutation-checked against the restored old line) is honest about being a proxy. `assertAtMostOneTerminal` itself untouched — the fuzz target is intact.

### R2-6 — crash-probe extension: **ACCEPT**
Re-ran on this VM: 6 cases × 3 iterations, `ok:true`, rerunnable. The three new cases assert real invariants, not implementation details: the cascade case pins the kill window deterministically with a **FIFO at the tombstone destination** (strip visible, tombstone unpublishable) and its verdict accepts only complete-old or complete-stripped, explicitly failing tombstone-before-strip; the settle case kills the child **while it holds the episode lock** after the terminal append, then proves the waiter fails closed, manual lock removal recovers, and exactly one terminal snapshot/event survives — which is also the operational recovery story for R2-3's new fail-closed delete; the atomic-write case kills at the public `rename` seam (pre-existing T3 injection seam, verified) and proves old-or-new plus stale-temp inertness. One boundary honestly noted, no action for this slot: the cascade case cannot kill **inside** `writeFeedbackRecords`' plain `writeFile` (no seam there), and that unprobed window hides a real defect I've ranked for Round 3 (§6 R3-2: the rewrite is not crash-atomic).

### R2-7 — persistence-row fuzz: **ACCEPT**
`test/unit/persist/row-fuzz.test.ts`: deterministic xorshift32 at seed `0x4f320007`, dependency-free, 5 s caps, <1 s scoped runtime; covers all four `validateEpisodeEvent` variants, pause-token parse via the public controller, checkpoint parse + `validateCheckpoint` (with the documented `SyntaxError` carve-out), feedback-row loading, and invocation-record validation; accepted values are round-tripped. It found a real defect (§4) and handled the ownership boundary exactly right: a **named skip** (`skipUnowned`) carrying seed and iteration, instead of an unowned fix or a red gate. The two fix-authorized modules needed no fixes; no src edit was made. This is the correct precedent for cross-slot defect handoff.

### R2-8 — mailbox starvation honesty: **ACCEPT**
Verified in `src/cluster/mailbox.ts`: sender-skips now count per mail; the attempt exceeding `maxRoleRequeues` (default 3, validated non-negative integer, injectable `now`) drops the mail into an observable `deadLetters()` snapshot (role-filterable, copies not live references) with `requeueCount()` exposing progress toward the bound; delivery clears the tally; non-starvation semantics byte-equivalent (survivors list preserves order; `{...mail, to}` copy unchanged). The doc block discloses the three deliberate limits — sender-only starvation is now bounded, the bound counts **claims not time** (a never-claimed role queue still sits forever — pinned by its own test so nobody mistakes the bound for a TTL), and no durability. Isolated-worktree typecheck plus 20/20 including the downstream `cluster-tools` consumer is thorough attribution work. Residual, disclosed and correctly deferred: nothing in production reads `deadLetters()` — wiring belongs to `host.ts`'s next owner (§6 R3-7).

### R2-9 — scheduler lease dead code: **ACCEPT**
The best epistemics of the round: it **corrected the brief's evidence** (mine — ROUND1-BRIEF §4 said `expired()` *and* `restore()` were caller-less; `restore()` has a live caller at `supervisor.ts:210` on the `resumeRun` path) instead of trusting the injection, then removed only what a full caller census proved dead (`expired()`, `isExpired()`), rewrote the class contract to state what is true (leases do **not** expire; `planRound` ignores `expiresAt`; bounding lives in the child coordinator), and pinned the previously-implied behavior as assertions (a lease 60 s past `expiresAt` is still active and still excludes its task). The supervisor source pin (comment-stripped, requiring `leases.restore(` and the recovery loop, forbidding `.expired(`/`.isExpired(` from reappearing) was mutation-checked five ways against in-memory copies — the right technique in a shared tree. See §5 for my independent resume confirmation. Handoffs disclosed, not silently widened: `applySkipped` dead code, the unread `_leaseDurationMs` parameter, and the stale docs line (§6 R3-8/R3-10).

### R2-10 — docs truth-up: **ACCEPT-WITH-NITS**
Verified against source: the unique temp naming (`pause.json.<pid>.<random>.tmp`, checkpoint likewise, wx-open, never adopted), the other rename sidecars, the six-entry lock inventory (all six paths confirmed real, including the no-steal/manual-cleanup sentence that now also matches R2-3's delete behavior), and the architecture spec's enforced-vs-validated child-limits split (consistent with R2-5's disclosure and T6's wall-time semantics, including "a terminal that still arrives keeps its own outcome").
Nits — both slot-timing artifacts, not negligence: (1) `docs/specs/m0-m2-architecture.md:347` still claims the checkpoint contains "active leases"; I verified the checkpoint schema has no lease field and leases are rebuilt purely from `TASK_LEASED` events — R2-9's handoff was written after R2-10's commit. (2) The two lines R2-3 flagged (`docs/data-dictionary.md` "the episode `.lock` previously survived deletion" reading as though the hand-unlink is the fix; `src/privacy/record-classes.ts:86-89` "all removed by delete --episode: … `<id>.lock`") still describe the pre-R2-3 mechanism. Folded into Round 3 (§6 R3-10).

## 3. Cross-slot conflict check: clean, with two deliberate couplings verified

- **R2-2 × R2-4:** the feedback store's message classifier still matches after the typed error landed — `FileLockTimeoutError` keeps the exact message and is a `DomainValidationError` subclass. Verified live, and both slots disclosed the intended joint migration.
- **R2-3 × R2-6:** the delete's new fail-closed stale-lock behavior and the probe's `episode-settle-stale-lock-recovery` case assert the same posture from two sides on the integrated tree.
- **R2-1 × T5/T6:** teardown uses both halves T5/T6 built — the composed signal (unit test 4 verifies `AbortSignal.any` composition actually observes the abort) and `handle.cancel()` for the no-attempt windows.
- **R2-5 × T8:** `assertAtMostOneTerminal` unmodified; fuzz seed `0x4f310008` green in the gate; the duplicate-terminal wording parity is test-enforced across the two throw sites.
- Shared-tree process: three slots hit transient whole-tree `tsc` errors from siblings' in-flight edits, and all three attributed them correctly (file-level, `git grep` against HEAD) rather than "fixing" unowned files. The Round 1 process requirement worked.

## 4. Known leftover verified: invocation-row TypeError is real, live, and worse than reported — **top Round 3 rank**

Reproduced at HEAD (my own script, valid branded ids so the id checks pass):

- `validateInvocation({...valid, config: null})` → `TypeError: Cannot read properties of null (reading 'provider')` (`model-invocation.ts:85-86` destructures then dereferences `config` unguarded);
- `config` **missing** → the same TypeError via `undefined.provider`;
- `pricing: null` → `TypeError … (reading 'catalogVersion')` (`model-invocation.ts:128` — `!== undefined` passes `null` through);
- `isInvocation({...valid, config: null})` **throws** instead of returning false.

Blast radius beyond the fuzzer: `loadInvocationsFromStateRoot` (`routing/cost-calibration.ts:143`) — whose doc says "Malformed or invalid rows are skipped" — calls `isInvocation` per row with no catch, and it feeds `calibrateCatalogFromState` → `createCalibratedCliModelRouter`, which is on the **live `pi run`/`resume` startup path**. One JSON-valid row with plausible ids and a null/missing `config` in `runtime/invocations.jsonl` (schema drift, partial hand-edit) crashes the CLI with an unhandled TypeError instead of a skip or a `DomainValidationError`. Writer-side is contained (the sink catches everything), read-side is not. **Ranked P1 for Round 3** (ROUND2-BRIEF R3-1), with R2-7's named skip becoming the regression test the day it's fixed.

## 5. R2-9 / resume: confirmed unbroken

Three independent confirmations: (1) the diff removes only `expired()`/`isExpired()` — `restore()`, `lease()`, `release()`, `active()`, `list()` untouched; (2) the live chain is intact in source: `resumeRun` → `reconstructSupervisorState` (`supervisor.ts:207-219`, `leases.restore(` at 210) → `runSupervisorRounds`' unconditional recovery loop (`supervisor.ts:255-265`), which never used expiry; (3) `test/integration/m2/{resume,supervisor,scheduler}.test.ts` + `test/integration/m2.5/resume.test.ts` → 16/16 pass, plus the whole gate green. The brief's "and `.restore()`" was wrong; R2-9's correction stands and the source pin now prevents the same misreading from causing damage later.

## 6. Round 3 ranking (injection form in `ROUND2-BRIEF.md`)

P1: invocation-row decoders fail closed (§4 — live CLI crash from one bad row). P1/P2: privacy rewrites are not crash-atomic — `writeFeedbackRecords` (`store.ts:282`) and `writeInvocationRecords` (`invocation-log.ts:260`) are plain truncate-then-write `writeFile` under their locks; a SIGKILL mid-rewrite loses unrelated rows and can tear a line, and R2-6's probe deliberately couldn't reach that window; `writeFileAtomic` already exists. P2: `delete --run` resurrection race (no lock between `deleteRunRecords`' `rm -rf` and a live run's `EventStore.append`, whose ENOENT→mkdir retry **recreates the deleted directory** — the run-side twin of what R2-3 just closed); run-event decoder discipline + fuzz (`EventStore.readAll`'s corrupt-line callback throws bare `Error`, pre-T4-style, and `validateEvent`/`replayRun` are unfuzzed); terminal-event honesty on escaping errors (R2-1's disclosure); resume-path invocation sink hole (`main.ts:1130` and `:1167` call `createExecutor` with **no hooks** — resume runs record zero invocations, the same class T2 closed for `runCommand`); stale-lock diagnosability under no-steal (lock files carry `{ownerToken, pid, acquiredAt}` and nothing reads them; a read-only doctor surface makes manual recovery informed without stealing). P3: dead-letter wiring into `ClusterHost`; scheduler leftovers (`applySkipped`, `_leaseDurationMs`, stale supervisor comment); docs/comment truth-up (§2 R2-10 nits). Saturated and off-limits: `persist/jsonl`, `protocol/v1` parse, and now `file-lock` acquisition perf and mailbox starvation semantics.

## 7. Actions for the parent

1. Nothing to commit from me: the gate is green as committed; no reviewer fixes were needed this round.
2. `.agent_workspace/PROGRESS.md` is the only working-tree modification (yours, mid-edit at review time).
3. Round 3 slot instructions: carry forward the scoped-eslint + whole-tree-tsc requirement (it worked); add R2-7's **named-skip protocol** for unowned defects as the standard (skip with seed/iteration + report, never fix across the boundary); and note that R2-2's typed `LOCK_TIMEOUT_CODE` is now the canonical classifier — new lock-timeout handling must not string-match.
