[Model: claude-fable-5-thinking-xhigh]

# Loop 4 · Round 11 — SOTA review at `2767321`

Reviewer ran independently on this VM (Node v22.14.0, engine warning only), on `agent/opt-continuous`, working tree clean at HEAD `2767321`. Every verdict below is against the actual diff `6c60ba6..2767321` — the ten slot landings, the two parent joints (`6926592` test, `df2c395` docs), and the seven orchestrator PROGRESS commits (`be21a05`, `ad9e785`, `5712f4d`, `98c2194`, `f6cc185`, `b7c9d00`, `2767321`, all `.agent_workspace/**`-only) — verified per-commit with `git show --stat`, not taken from slot reports. No file outside `.agent_workspace/` was changed by this review; scratch outputs lived in `/tmp` (`/tmp/r11-gate.log`, `/tmp/r11-crash-probe.log`, `/tmp/routes-{base,head}.txt`, `/tmp/cov-{base,head}.txt`).

## 1. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R11-1 | **ACCEPT** (disclosed partial) | Option (a) shipped exactly as R8-4 §4 designed, not §9's silent no-op: optional non-empty unique-id `VerificationResult.criteria` with FAILED-cites-evidence at both the protocol validator and the tool; a required `GateInput.criterionUnmet` supplied from `unmetCriteriaOf` (reported FAILED only — UNOBSERVED and absence both stay open, never-ran stays unknown-not-unmet with no special case), ordered exactly behind `deterministic-fail`/`ownership-escape`/`claimed-verification-without-checks`; durable `taskCriteria` validated fail-closed on the R9-1 seam; per-criterion reporting riding the one verdict call with absence byte-identical (conditional spread). All nine pin obligations replaced in place; `coverageOutcome` body **byte-identical** (extracted and diffed); `prescore.ts` diff mechanically comment-only; `independentEvidence` still exactly one `void` dereference (re-swept — the field is not mentioned in the diff). The one disclosed gap: **`taskCriteria` has no `src` writer** — `persistCheckpoint` is R11-4's file this round, so the seam is declared-and-validated, not a working durability guarantee; prominently disclosed (§3.3/§7/§9), writer hunk prescribed, and the gate correctly does not depend on it. The red assertion it could not fix lived in R11-3's file — prescribed exactly, shipped verbatim by the parent joint |
| R11-2 | **ACCEPT** | The eleventh probe case is real: a separate child process runs the **unmodified** production `unblockFlowchartRun` with `discardExecuted: true` (the child mode is R9-5's, extended by one conditional spread), 16 MiB reason widens the post-append read, the parent observes the complete newline-terminated row and sends **external SIGKILL**, then asserts signal-death, exactly one decoded `RUN_UNBLOCKED_WITH_DISCARD` row recording only the executed summary, byte-identical BLOCKED checkpoint, no checkpoint temp, lock naming the reaped PID; documented stale-lock removal, then real `resumeFlowchartRun` re-executes each rewound node exactly once to COMPLETED, one authorization, zero anomalies. Setup earns a real gate block with an executed descendant (scout COMPLETED / analysis FAILED / any-join summary COMPLETED). Original ten names and order unchanged; pin update is the two-line disclosure in `test/integration/persist/crash-recovery.test.ts` (the `persist/` path, as required). The mid-flight R11-4 signature transient was attributed by census, not worked around |
| R11-3 | **ACCEPT** (disclosed PARTIAL) | The signed-off src shape landed exactly (+20/−1): `TrackRunInput.pause?: PauseController` (type-only import), the conditional-spread forward in `startTrackedRun`, `runCommand` supplying `createFilePauseController(stateRoot)`. The pure-CLI *mid-run* pause STOP is source-justified, all three facts re-verified at HEAD by this review: `compileChildrenToFlowchart` sets `approvalRequired: false` on every node (`compile-children.ts:114`); the track path always falls back to `fake-children` whose children always pass (`main.ts:801-806`), so no BLOCKED; the clarification checkpoint carries no flowchart payload and `restoreFlowchartSession` refuses it (`flowchart-run.ts:1481`) — so a tracked CLI run is always terminal in one process and the run id prints only at the end. No race primitive anywhere in the tests (no polling, no `fs.watch`, no timers), no process kill, no new fs primitive; the deterministic hook is the pre-existing `TogglePause` (present at `6c60ba6:573`) seeded one level up at `startTrackedRun` — the entry point `runCommand` itself calls — with `pause`/`resume` both driven by `main([...])` and zero `startFlowchartRun` in the proof loop. The control (no controller → COMPLETED, 5 children; with → PAUSED after one child) fails behaviourally if the forwarding is ever dropped. Fold correctly declined per instruction; the pause leg's call-site pin is honestly labelled a source pin, and the early-run-id option was designed, declined for the right reason (needed R11-4's file), and recorded as a Round 12 candidate |
| R11-4 | **ACCEPT** | Exactly the signed-off change: `applyClearingEvent` gains the log as a parameter and its discard arm runs `assertDiscardAuditMatchesLog` **after** the consequence-set recompute (set before sums — R10-1's pinned wrong-set message stays dominant, verified in source ordering); the sole caller `restoreCheckpointedSupervisor` forwards `input.events` it already held; no export-only, no edit to `replay.ts` (commit stat confirms), payload/producer/`chargedAttempts` untouched. The control-first test plus four refusals each drive a hand-appended schema-valid row through real `resumeFlowchartRun` and assert the exact producer messages (character-matched against source by this review) plus fail-closed properties (nothing spent, nothing written, checkpoint still describes the block). The three previously-unreachable refusal arms now have honest public paths. Non-vacuity is triple-corroborated: the slot's documented mutation (`void events;` → all four refusals fail with `Missing expected rejection`), R11-8's AST guard which **failed at base** before this landed, and the new `replay.test.ts` boundary pin (replay deliberately records no anomaly for unsupported charges — this class must stop a resume, not annotate it). The completeness limit (a subset-citing, internally-consistent authorization passes) is disclosed in-source with the re-block shape as the reason |
| R11-5 | **ACCEPT** | Docs only (four files + report); ADR-006 Proposed (line read at HEAD); no ADR edit. Round 10 landings anchored to their real commits (`54cf5e5`/`2399346`/`d4b52b1`); eleven-case probe recorded with the `persist/` pin path and no phantom `m2/` file; five timestamped censuses (23:56:47→23:59:44) that never assigned an uncommitted sibling a commit id. The standing docs-race cost recurred at larger scale than Round 10: R11-1 (00:16:54) and R11-3 (00:10:32) landed 10–16 minutes after the docs commit (00:00:52), leaving **four claims false at HEAD** — "The tool does not carry per-criterion results" (`data-dictionary.md:188`), "that later protocol/gate work remains unimplemented" (`m0-m2-architecture.md:652`), and "tracked start supplies no pause dependency" twice (`status-matrix.md:63-65`, `m0-m2-architecture.md:388-390`). The parent docs joint fixed only the status-matrix coverage-gate row. Per the R9-4(b)/R10-3 precedent this is the honestly-dated race, not a miss — Round 12's docs slot owns the four spots (§6.3) |
| R11-6 | **ACCEPT** (report-only) | The right-sized no-op: the census claim is verified — `git diff 6c60ba6..HEAD -- test/unit/routing/live-isolation.test.ts` is **zero lines**, so the file is byte-identical to Round 10 close; R11-1's sole src import is type-only `AcceptanceCriterion` in `replay.ts` (erased at runtime, confirmed in the diff), and the existing transitive-closure walk plus the 215-module census already pin everything the mandate named (re-swept independently: `loadProjectBanditByKey` store+doctor only, `selectArm` definition+`shadow.ts` only, bare `loadProjectBandit` zero). Declining to add a duplicate assertion is the saturation discipline working, not a shirk; 9/9 run against the final R11-1 tree |
| R11-7 | **ACCEPT** | Two additive freezes, both with teeth: `inspection.test.ts` proves a **rich-child** fixture (child + terminal-result + verification state) cannot grow a fifth `INSPECT_SUMMARY` key (whole-object `deepEqual`, not just key names); `blocked-next.test.ts` pins the four ordinary lines as an ordered prefix directly at the formatter, counts **exactly three** `next:` lines, asserts the discard line starts `note:`, and proves the reorder and `note:`→`next:` promotion mutants fail. No src, no fifth `next:`, both handed paths existed |
| R11-8 | **ACCEPT** | The additive AST guard walks `flowchart-run.ts` and requires `assertDiscardAuditMatchesLog` to be called from inside `applyClearingEvent` or `restoreCheckpointedSupervisor`, then reapplies the unchanged whole-file `assertNoSchedulerRetry` — genuinely non-vacuous (its own census shows it failing before R11-4's edit existed) and correctly sequenced (landed 00:09:00, three seconds after R11-4's 00:08:57, written against the landed call). R8-3/R10-8 inherited assertions untouched; scheduler files diff-empty |
| R11-9 | **ACCEPT** | One additive test that discovers the full non-terminal vocabulary from `RUN_STATUSES` (census-non-vacuity asserted), proves the terminal intersection is exactly the signed-off trio, and holds every other status out of `TERMINAL_REPLAY_STATUSES` — the discovered-not-enumerated complement to R10-10's exact-trio pin, landed before option (a) so it guarded the round in flight. No src, deterministic, correctly exempted from 3× |
| R11-10 | **ACCEPT** | The never-synthesize census now covers R11-1's landed field: `taskCriteria`'s exact spelling joins the forbidden synthesis set alongside a semantic per-task/child-criteria rule that catches equivalent respellings; `ProjectEpisode` pinned to acquire no such sibling; the mutation proof (assigning `episode.acceptance` into `taskCriteria[].acceptanceCriteria`) is rejected by the same whole-source census. Correctly sequenced (00:17:17, after R11-1's 00:16:54). One letter-vs-spirit note (§5): the commit deletes 5 helper lines against an "additive only" mandate — verified to be the centralization of the three authority names into `ORIGINAL_RUN_AUTHORITY_FIELDS` with **all names preserved** and the test count strictly grown (4→5); no pin deleted or weakened |

**10 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK — but this round is not joint-free.** Two parent joints (Round 10 had zero): `6926592` (spent R9-1's reserved pin in `resume.test.ts` with R11-1's prescribed hunk, verbatim) and `df2c395` (one-row docs truth-up). And one **red-tree commit point** in history (Round 10 had zero): at `6096da6` (00:16:54), `resume.test.ts:408` still asserts `/Reserved: per-task acceptance criteria/` while `replay.ts` no longer contains it (verified: 0 matches at that commit) — one failing integration test for 20 seconds until `6926592` (00:17:14). Root cause is a brief-construction defect, not a slot failure: the parent's R11-1 sign-off ordered "replace R9-1's reserved-unimplemented assertion in the same diff" while R11-1's ownership row excluded the file that assertion lives in (R11-3 owned it). Both R11-3 (§6.1) and R11-4 (§5) flagged the gap in real time; R11-1 prescribed the exact hunk; R11-3's refusal to edit into R11-1's active window was the correct lost-update call. The fix for Round 12 is procedural (§6.6).

## 2. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0: 1938 tests / 1937 pass / 0 fail / 1 skipped** — matches the parent's recorded numbers exactly. The one skip is the standing `PI_SMOKE` real-provider gate (`PiAgentExecutor completes a run against a real provider # SKIP`); no slot introduced a second skip (0 other `# SKIP` lines in the TAP output). Engine warning only (`>=22.19.0` wanted, v22.14.0 present).
- **`node scripts/crash-probe.mjs` → `ok: true`, 11 cases × 3 iterations, exit 0** — matches. The original ten names and their order are unchanged from Round 10; `unblock-discard-append-before-checkpoint-sigkill` is appended last.
- **The +23 over Round 10's 1915/1914/1 closes per-slot.** Static test-registration deltas per changed test file, base `6c60ba6` vs HEAD: R11-1 **+8** (`option-a-preconditions` 6→7, `protocol/v1` 19→20, `report-task-result` 17→20, `event-row-fuzz` 7→9, `tracking/gates` 7→8; `criteria-are-guidance` 10→10 and `acceptance` 6→6 are edits inside existing tests) · R11-2 **0** (pin-line edit) · R11-3 **+4** (`m2.5/resume` 16→19, `cli-contract-honesty` 1→2) · R11-4 **+6** (`unblock-flow` 13→18, `replay` 23→24) · R11-5 **0** (docs) · R11-6 **0** (report-only) · R11-7 **+2** (`inspection` 13→14, `blocked-next` 11→12) · R11-8 **+1** (3→4) · R11-9 **+1** (2→3) · R11-10 **+1** (4→5) · parent joint **0** (edits inside an existing test). Sum: exactly 23.
- **Commit hygiene:** each landing touches only its `OWNERSHIP.md` files plus its own report — verified per-commit with `git show --stat`; no two slots share a file; the range's `src` diff is exactly the eleven signed-off files (R11-1's eight, R11-3's two, R11-4's one). Load-bearing sequencing held: R11-8 landed 3 s after R11-4; R11-10 landed after R11-1; the joint landed 20 s after R11-1 (the red window counted above). `package.json`/`pnpm-lock.yaml` diff-empty.
- **Bookkeeping nit:** `PROGRESS.md`'s Round 11 table records R11-2's dispatch id as `bc-3be62857-82a5-5103-8de6-3a5d15860354` — a chimera of R11-2's prefix and R11-7's suffix; `OWNERSHIP.md` has the real id (`…82a1-5523-9ef1-cae7b3339bb7`). One-line parent fix whenever PROGRESS is next touched.

## 3. Freeze check

All verified against the actual range diff and the tree at HEAD, not report claims:

- **Isolation:** reviewer's own sweep — `loadProjectBanditByKey` exactly store declaration (`bandit-store.ts:172`) + doctor import/call (`doctor.ts:17,467`); `selectArm` exactly `routing/bandit.ts` (definition) + `routing/shadow.ts`; `\bloadProjectBandit\b` zero `src` matches. ADR-006 **Proposed** (`0006…md:5`); `docs/decisions/` diff-empty across the range.
- **`independentEvidence`:** whole-`src` census at HEAD — declaration (`prescore.ts:29`), the single load-bearing `void` (`prescore.ts:89`), the object-literal write (`from-child.ts:228`). Exactly one dereference, and it is the `void`. R11-1 gave it no reader; R10-5's pin file untouched and green in the gate.
- **`TERMINAL_REPLAY_STATUSES`:** read at HEAD (`replay.ts:326-329`) — still exactly COMPLETED/FAILED/BLOCKED; R10-10's two-level freeze plus R11-9's non-terminal census both green. No `RUN_UNBLOCKED*` is a status.
- **`RUN_UNBLOCKED`:** exact-keyed `{ blockedEventId, reason, retryNodeId }` (`events.ts:853-860`, allowed-list read directly); `RUN_UNBLOCKED_WITH_DISCARD` still its own exact-keyed four-field validator (`events.ts:875-883`). `events.ts` diff-empty across the round.
- **`applyRetry`:** definition in `scheduler.ts`, production import/call only in `run/supervisor.ts` (swept); both files diff-empty; `flowchart-run.ts` has zero `scheduler` references and `flowchart-supervisor.ts`'s single mention is the pre-existing two-state-machines docstring; R8-3/R10-8/R11-8 pins green.
- **Gate `runStatus`:** zero `src` readers re-swept — identifier only in `gate-apply.ts` (declaration/producers/docstrings) and `child-tracking.ts`'s single object-literal write; neither file changed this round.
- **Five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT`:** extracted at base and HEAD and **diffed character-exact — identical**, through R11-3's `main.ts` edit; R9-7's AST freeze green in the independent gate.
- **Loopback supervised-resume stderr byte-pin:** `test/integration/pi-adapter/loopback-cli-resume.test.ts` diff-empty across the range.
- **`INSPECT_SUMMARY`:** literal read directly at `main.ts:1138-1145` — four keys; now also held by R11-7's rich-fixture pin. **BLOCKED four lines:** prefix-pinned at the formatter with the three-`next:` ceiling (R11-7); the discard disclosure is still a `note:`.
- **Episode boundary:** `episode-bind.ts` diff-empty; R11-10's census extends the forbidden set to `taskCriteria` with the three original authority names preserved (verified in the diff) and a live mutation proof.
- **Writer carriage:** exactly two flowchart-payload `materializeCheckpoint` call sites at HEAD (`flowchart-run.ts:689` via `persistCheckpoint`, `:2002` via the reopen), both carrying `contract` (read directly); R10-4's property pin green. **No writer carries `taskCriteria` — confirmed: zero `src` writers; the identifier exists only as `replay.ts`'s declaration + validator and a `prescore.ts` docstring pointer.** The parent residual is accurate.
- **Verdict-producer freezes after R11-1's `criteria` parameter:** `report-task-result.test.ts`'s diff is insert-only (numstat: 0 deletions), so R10-6's four pins are byte-untouched and green; the emit path keeps the lease-stamped identity and single `reported` guard, criteria ride the same single call, and an omitted parameter emits a byte-identical verification object (conditional spread, read in the diff). FAILED-cites-evidence now applies one level down as well (protocol validator + `criterionList` both refuse an unevidenced FAILED criterion).
- **jsonl / `writeFileAtomic` / no new private tmp+rename:** the round's entire `src` diff adds **zero** raw fs write primitives (grep of added lines for `writeFile`/`appendFile`/`rename`/`createWriteStream` + Sync variants: empty). No dependency changes.
- **`coverageOutcome`:** function body extracted at base and HEAD and **diffed byte-identical**; the whole `prescore.ts` diff is mechanically comment-only (zero non-comment changed lines). R6-2's FAIL-unreachable tripwire green; the 270-cell sweep intact at 270 with its census pin still resolving to the sole production path.
- **Criteria-are-guidance:** the sweep's assertions unchanged; the record-pin regex moved with the source sentence it pins and gained the required where-did-the-gate-go companion (`unmet-acceptance-criterion` named in `prescore.ts` prose) — replacement, not deletion.

## 4. Requested target verifications

### 4.1 Option (a) — one-diff shape vs R8-4 §4

Verified clause by clause against the design doc and the diff: the shipped mechanism is §4's **recommended** shape — a dedicated `unmet-acceptance-criterion` in `AnomalyCode`/`ANOMALY_CODES`, driven by an explicit **required** `GateInput.criterionUnmet` that `assessChildObservation` supplies from `unmetCriteriaOf(observation)` (the child's *reported* FAILED criteria, never a recomputed score, never a dimension outcome), placed exactly after `claimed-verification-without-checks` and before `repeated-no-progress`, firing on observed FAILED only. It is not §9's silent no-op (the criteria-shaped dimension FAIL): `coverageOutcome` gained no FAIL (byte-identical body). All three rejected alternatives stay rejected, and the `derivedClaimedVerificationWithoutChecks` question R8-4 §9 demanded a decision on was decided (unchanged) with the reasoning in-source. Never-ran stays unknown-not-unmet **by construction**: the gate reads only reported outcomes, a never-ran node reports nothing, an UNOBSERVED criterion and an absent array both leave the gate open — carried entirely by the gate logic, deliberately not by the unwritten `taskCriteria` field. Pins: R8-4's 1/4/5 replaced with strengthened successors, pin 3 kept with its unchanged 54-of-270 measurement, R10-2's meta-pin re-pointed at the four post-landing titles (still enforcing rename-in-the-open), R10-2's two `criteria` pins flipped to their positive forms (`? true : never` compile check + a `criteria\?` optionality regex — read at HEAD), R9-1's reserved assertion replaced by the new 7th pin in a file R11-1 owns *plus* the parent joint's integration flip. All replacements, no deletions.

### 4.2 The probe's eleventh case

Real, end to end (§1 R11-2 row): unmodified production producer in a separate process, external SIGKILL from the parent after the observed complete append, no crash callback/sentinel/self-kill in the child path (the `unblock-flowchart` child mode is R9-5's, extended by one conditional spread for the flag), and the recovery leg exercises the recompute-matches restore path — which, at HEAD, also runs R11-4's charge audit over the recorded payload, so the standing probe now covers producer-append, external kill, and audited restore in one case. Ten original names/order unchanged (JSON output compared).

### 4.3 R11-3's STOP

Source-justified — all three structural facts re-verified at HEAD by this review (§1 R11-3 row). They did not race (no polling/watchers/timers anywhere in the four new tests), did not invent an fs primitive (src diff is the type + two conditional spreads + one call-site line), did not kill a process. The hook is the signed-off class: the parent asked for "a deterministic hook of the same class as the untracked flip test, with the CLI driving", and the shipped test seeds the pre-existing `TogglePause` at `startTrackedRun` — the exact function `runCommand` calls, made pausable only by this slot's own src change — with pause and resume both `main([...])` and zero `startFlowchartRun` in the loop. The honest residual is correctly labelled: the pure-CLI arc's *pause request* leg is a call-site source pin, because making it behavioural needs early run-id disclosure, which needs `flowchart-run.ts` (R11-4's file this round) — declined for exactly the right reason and recorded as the Round 12 candidate it is.

### 4.4 R11-4 — the refusals bite, and ordering holds

The four refusal messages in the tests were character-matched against `assertDiscardAuditMatchesLog`'s throws by this review; each test drives a hand-appended schema-valid row through the real resume and additionally asserts nothing was spent, appended, or checkpointed. That the refusals would not fire without the change is established three independent ways: the slot's documented `void events;` mutation (all four fail with `Missing expected rejection` — i.e. the inflated-sum authorization resumed cleanly at base, which is precisely review §6.4(b)); R11-8's AST guard, which its own census shows red at base; and the gate at HEAD, green with the call in place. Ordering: in source, the consequence-set mismatch throw precedes the audit call inside `applyClearingEvent`, so a payload wrong both ways still reports R10-1's exact wrong-set message — the R10-1 behavioural pin is unaffected (green in the gate). This review did not perform its own live src mutation (the no-src-edit rule binds the reviewer too); the three-way corroboration above is the evidence.

### 4.5 R11-6 — byte-identity

`git diff 6c60ba6..HEAD -- test/unit/routing/live-isolation.test.ts` is empty (0 lines). The claim holds exactly: no change, and none was needed — verified independently by the §3 isolation sweep.

## 5. Per-slot notes

- **R11-1** (opus): The strongest option-(a)-shaped landing this loop could have produced under its ownership constraints. The three-decision gate design (required-not-optional, hard-not-soft, supplied-not-derived) is stated in-source at the point of each decision; the `UNTRUSTED_TEXT` reasoning for leaving `derivedClaimedVerificationWithoutChecks` alone is the right epistemics; `evidenceRefsOf` carrying criterion evidence keeps the anomaly auditable without widening the persisted `TrackingAssessment`. The declared-not-written seam was the only honest option available — the alternative (a drive-by `flowchart-run.ts` edit) would have collided with R11-4 mid-flight.
- **R11-2** (gpt-sol): The R9-5 technique transferred cleanly; earning the executed-descendant block from a real three-node any-join run rather than mutating a snapshot is the house discipline. The negative assertion that no plain `RUN_UNBLOCKED` accompanies the stronger event quietly re-proves single-append at the probe level.
- **R11-3** (opus): The five-option determinism table with a stated reason per declination is a model stop-and-report; the set-difference identification of resumed turns (by name, not count) and the `--children` `NOT_APPLICABLE` discriminator keep `constraint-retention: PASS` from being a free default. Disclosure 6.1's lost-update reasoning (do not edit into a sibling's active write window) is the correct shared-tree call and is why the joint exists instead of a lost slot.
- **R11-4** (opus): Small diff, complete argument. Choosing to let replay stay silent (with a test asserting the silence and the reason) rather than adding an anomaly is the right boundary: anomalies are stepped over, and this class must stop the resume. The completeness limit is documented where the next reader will look.
- **R11-5 / R11-6 / R11-7 / R11-8 / R11-9 / R11-10** (gpt-sol): Right-sized. R11-6's decision *not* to add a test is as reviewable as a diff and passes review. R11-7's direct-formatter pin closes the gap between the e2e list assertion and the formatter's own contract. R11-10's helper centralization crossed the letter of "additive only" (5 deleted lines) while strictly strengthening the census — all three authority names verified preserved in `ORIGINAL_RUN_AUTHORITY_FIELDS`, test count 4→5, mutation proof added; recorded here so the letter-vs-spirit call is on the record, below the verdict-qualifier bar.

**Nits recorded, none rising to a qualifier:** the R11-10 letter deviation (above); the PROGRESS.md R11-2 dispatch-id chimera (§2); the four stale docs spots (§6.3 — process cost, not a slot failure).

## 6. New findings for Round 12

1. **The `taskCriteria` writer is the round's one unfinished durability obligation, and it is subtler than the prescribed hunk.** R11-1 §7.1 prescribes mirroring the contract line in `persistCheckpoint` with criteria "accumulated from the `TASK_REQUEST`s the run dispatches". Verified in source: `ctx.childByTaskId` already holds every dispatched task's `acceptanceCriteria`, *but on a resume that map is rebuilt by `childTasksFromLog`, which gives a never-logged request `acceptanceCriteria: []` (`flowchart-run.ts:411`)* — so a naive writer would launder the very unknown the field exists to preserve. The writer must derive from logged `TASK_REQUEST`s (or first-write-wins against the checkpoint's existing record), and the reader half must thread the checkpoint's `taskCriteria` into `childTasksFromLog`'s substitution path (sole call site `flowchart-run.ts:1346`) so a resumed never-ran node is re-dispatched with its original criteria rather than a silent empty list. This changes what a resumed node is asked to check — parent sign-off required, exactly as the residual says.
2. **Early run-id disclosure is both a real operator gap and the missing determinism seam** (R11-3 §6.6, reviewer-verified): an operator cannot pause a live tracked run because the id prints only after the outcome; a `onRunStarted`-style callback on `FlowchartRunDeps`, fired once the run directory exists and before round 1's pause poll, would close the gap and make the pure-CLI pause leg behaviourally provable. It needs `flowchart-run.ts` — **the same file as finding 1**, so the two cannot be separate concurrent slots.
3. **Docs at HEAD carry four false claims** (all true when written, superseded within 16 minutes): the two per-criterion "does not exist" statements (`data-dictionary.md:188`, `m0-m2-architecture.md:652`) and the two "tracked start supplies no pause dependency" statements (`status-matrix.md:63-65`, `m0-m2-architecture.md:388-390`). The parent joint fixed only the status-matrix coverage-gate row. Round 12 docs slot owns all four, plus recording Round 11's landings as commits.
4. **The new gate has no whole-run production-reachability proof.** The channel is covered at the unit level (gate ordering, all-roles blocking via `assessChildObservation` — the real production path), the producer level (criteria ride the terminal through `PiAgentExecutor`), and the persistence level (fuzz seeds for `TRACKING_ASSESSMENT`/`GATE_TRANSITION`). What does not exist is R9-2's measured-reachability analogue for this channel: one integration run where a child reports PASSED-with-one-FAILED-criterion and the *run itself* lands BLOCKED with `reasonCode: unmet-acceptance-criterion`, then exits through `unblock`. Cheap to build (a test executor whose terminal verification carries criteria flows through `observationFromChild`'s wholesale spread — no loopback server needed).
5. **Recorded decisions, not reopened without new evidence:** the discard audit's completeness limit (subset-citing authorizations pass — re-block shape, documented in-source); the criterion-id-nobody-asked-for acceptance (no new authority — a child can already FAILED itself); the role-gate C4 asymmetry (only a tester's asked-for criteria become `requiredChecks` — now visible and pinned, a design question needing its own appetite); `derivedClaimedVerificationWithoutChecks` kept-not-widened (removal is a different question).
6. **Brief-construction rule for Round 12** (the round's one process defect): every "replace X in the same diff" obligation must be cross-checked against the ownership table before dispatch — R11-1's sign-off pointed at an assertion in R11-3's file, which produced the round's two flagged disclosures, the parent joint, and the 20-second red-tree commit point. Either the obligation's file joins the obligated slot's ownership, or the joint is planned (and folded into the landing commit, not appended after it).

## 7. Handoff

- Gate GREEN at `2767321`: 1938/1937/0/1 (`PI_SMOKE` only), crash-probe 11×3 `ok: true` — both re-verified independently on this VM and matching the parent's record exactly. The +23 closes per-slot (§2).
- The Round 12 injection brief is at `.agent_workspace/ROUND11-BRIEF.md`: **four real ranked candidates** (fewer than ten — stated explicitly; the freeze surface is saturated and the brief does not manufacture busywork), the grown frozen-contract list (option (a)'s schema as shipped; the restore-side discard audit; the 11-case probe; the tracked pause controller; `taskCriteria` declared-not-written — confirmed), the file-contention map (findings 1 and 2 share `flowchart-run.ts` and must be one slot or sequenced), and the standing prohibitions.
- Nothing is left red at HEAD. The two parent joints are counted, explained, and correct in content; the red-tree commit point and the sign-off/ownership cross-check rule are recorded so Round 12 does not repeat them.
