[Model: claude-fable-5-thinking]

# Loop 4 · Round 5 — SOTA review (R5-1 … R5-10)

Reviewer: Round 5 SOTA reviewer, evidence-based, on `agent/opt-continuous` at `a770a24` (all ten slots committed as `6b759ae`…`ddf56bc` after the dispatch-id commit `c8e2524`, plus the parent's cross-slot joint fix `a770a24` — treated like `74daff3`, and verified to be exactly the two-line remedy R5-2's report prescribed). Every verdict was checked against the actual diff, not the slot report. Independent gate and crash probe run on this VM (Node v22.14.0). Working tree at review time: clean apart from the parent-owned `.agent_workspace/PROGRESS.md`. Nothing committed by this reviewer; no `src/**` or `docs/**` file edited.

## 0. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R5-1 run lifecycle lock | **ACCEPT** | `withRunLifecycleLock` verified on all four CLI-reachable lifecycles (`startRun` beyond the brief, justified); pause-on-live is explicit — stated on the helper's docstring *and* pinned by a real cross-process `requestPause`; bench honest about its own noise floor, with the noise-independent 0.148 ms acquisition carried; crash-terminal swap taken with a source pin; `deletion.ts`/`event-store.ts` edits verified comment/message-only with the pinned substrings preserved |
| R5-2 crash-terminal + supervised settle | **ACCEPT-WITH-NITS** | Shared module byte-compatible (reason strings, in-flight-only guard, both planes' guards named inside the module); settle-on-crash verified with the two-swallow split pinned in both directions; the R5-8 joint disclosed prominently with the exact fix the parent applied — but `startSupervisedRun` acquires the lock *before* `discoverProject`/`validateTaskGraph`, deviating from R5-1's published rule 1 without disclosure |
| R5-3 bandit store fail-closed | **ACCEPT** | `writeFileAtomic` + `BANDIT_STATE_UNREADABLE` verified in source; empty file is damage, ENOENT the only silent path; skew keys load and are dropped at the read boundary; the structural gate is the old one with defect strings, not a tightened one; kill-switch collect-only pinned from the bandit side; negative controls reproduce both the reset and the tear |
| R5-4 private temp+rename copies deleted | **ACCEPT** | All three copies gone, verified byte-shape preserved (`JSON.stringify(x, null, 2) + "\n"` in all three), chmod 0o600 after publish kept, and the shared writer's `RENAME_FALLBACK_CODES` covers the EPERM/EEXIST/EACCES fallback the deleted copies had; no `persist/` edit, probe untouched with the duplication argument stated |
| R5-5 resume-time adoption decision | **ACCEPT** | Commit verified test-only; the NO is evidenced: measured window (0.0008–0.03 % wall), a prototype run through the real gate at five crash budgets with a control, the cascade counter-example reproduced, and the risk asymmetry argued; four additive pins landed with mutation controls; its §4.4 out-of-slot anomaly **reproduced by this reviewer at HEAD** (finding 1) |
| R5-6 dead letters reachable | **ACCEPT** | The 4-line behavioral diff is exactly as claimed (`roleHolders.has(mail.from)` on the claimed role); the deleted delivery-branch `requeues.delete` is provably unreachable by holder-set monotonicity; `deadLetterReport()`/`onDeadLetter`/`ClusterDeadLetter` shapes and the pinned line format byte-untouched (`host.ts` doc-only); the production-shaped run (fresh id per attempt) reaches `dead-lettered=1` in a real `startParentRun`; deleted R2-8 pin disclosed with the trade priced |
| R5-7 docs truth-up | **ACCEPT-WITH-NITS** | Scoped R4 corrections verified against source; correct timestamped-snapshot method for in-flight R5-1/2/6 — but all three landed hours later, so at HEAD the run-lock, crash-settle, and `dead-lettered=`-unreachable snapshots understate the tree, and Round 5 surfaces from later-landing slots (`BANDIT_STATE_UNREADABLE`, doctor `next:` routing, typed checkpoint error) are undocumented (follow-through, not error) |
| R5-8 wire witness | **ACCEPT** | The loopback server's *received* request is asserted: `model: "loopback-1"` against a distinct configured default, `reasoning_effort: "high"`, `stream: true`; flag-free baseline pins `reasoning_effort` absent; supervised resume witnessed too; the stderr-pin update disclosed per the `74daff3` lesson, and the flag-free `warning:` wording stays pinned in `resume-executor-config.test.ts`, so nothing was unpinned |
| R5-9 error → doctor routing | **ACCEPT** | Routing keyed on the two *imported* frozen code constants (a rename is a compile error, not a dead route); depth-bounded `cause` walk; state-root scanned not parsed, with the reason argued; generic `next:` character-identical; the negative control (verbatim message, no code → generic) is the code-discrimination proof; honest disclosure that `RUN_RECORDS_SURVIVED` has no portable end-to-end producer |
| R5-10 dead code + decode doctrine | **ACCEPT** | `episode/replay.ts` deleted with a census pin requiring a live caller to return; `CheckpointStore.read` throws `DomainValidationError` naming the file, ENOENT and valid-file behavior byte-unchanged, `write` and its decision pin untouched; the `flowchart-io.ts` "bare parse" claim in the Round 4 brief verified **stale** — the file already threw typed errors, and the slot said so instead of editing it |

8 ACCEPT, 2 ACCEPT-WITH-NITS, 0 ROLLBACK. No lint or test blocker found (independent full gate GREEN), so there is nothing to commit from this review beyond this file and `ROUND5-BRIEF.md`.

## 1. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0**: **1725 tests / 1724 pass / 0 fail / 1 skipped** — matching the parent's numbers exactly. The sole skip confirmed by name: `PiAgentExecutor completes a run against a real provider # SKIP set PI_SMOKE=1 …`. **No slot introduced a new named skip** (census over `test/**`: the only `skip:` option anywhere is the `PI_SMOKE` gate; the other grep hit is a variable named `skip` in `coverage-gate.test.ts`). Delta vs the Round 4 baseline (1680/1679/1): **+45 tests**.
- **`node scripts/crash-probe.mjs` → `ok: true`, 8 cases × 3 iterations**, case set unchanged (R5-4 argued its unit pins subsume a probe case rather than padding the census; the argument holds — the shared writer's rename-seam case already exists as `atomic-write-stale-unique-temp`).
- **The `a770a24` parent fix**: verified as exactly R5-2's §3.3 prescription — `rmSync(runLockPath(...), { force: true })` after `waitForTaskRunning`, with a comment naming the process-death fiction. The commit touches only R5-8's test file. Same class as `74daff3`: a joint between two accepted behaviors (R5-2's supervised lock mirror × R5-8's abandon-a-live-handle fixture), not a product defect. Unlike Round 4's version of this story, the slot that caused the break (R5-2) disclosed it prominently in its own report with the exact remedy, and R5-1 independently attributed it by bisection — the process improvement the R4 brief asked for actually happened.

### Freeze check — held, no exceptions this round

- `git diff c8e2524..HEAD` over `src/persist/`, `package.json`, `pnpm-lock.yaml`, `src/run/episode-bind.ts`, `src/cli/episode.ts`, `src/telemetry/model-invocation.ts` is **empty** (also empty over the wider `74daff3..HEAD` range for persist/package). `writeFileAtomic`/`writeFileAtomicSync`, jsonl, `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE` all byte-identical.
- `src/run/event-store.ts`: comment-only diff verified line-by-line — `runLockPath` and `append` bodies untouched; the docstring edits correct statements R5-1's landing made false, which is the right reason to touch an unowned file and was disclosed.
- **The two decision pins hold**: `append does not block on the run lock` (`event-store.test.ts:119`) and `checkpoint writes do not block on the run lock` (`checkpoint-store.test.ts:169`) both present and green; neither writer's body changed.
- `recordCrashTerminal` contract: the shared module reproduces the in-flight-only guard (`PLANNING`/`RUNNING` replay) and the child plane's terminal-type guard verbatim; the two entry points deliberately prevent a caller-supplied predicate from widening in-flight-only. `applyRetry` appears **nowhere** in the supervisor diff. `preserveResumableState` and `RESUMABLE_CRASH_STATUSES` untouched (R5-1's file, no semantic edit).
- `deadLetterReport()`/`onDeadLetter`/`ClusterDeadLetter`/`ClusterMailReport` and the pinned `warning: cluster role-cast mail undelivered: …` line: shapes and format byte-identical; R5-6's `host.ts` edit is a doc paragraph.
- `deleteRunRecords`: double verify and `RUN_RECORDS_SURVIVED` code untouched; the message edit preserves the pinned substrings (`refusing to report the delete as successful`, `Stop or cancel the run before deleting it again`, the `<runDir>.lock` mention) — verified in the diff.
- Sink-wiring and resume-disclosure pins green through R5-9's `main.ts` edit (no `createExecutor` token added; `resumeCommand` untouched). Doctor's frozen-additive JSON: `doctor.ts` not edited this round.
- Sole-ownership held: `flowchart-run.ts` edited only by R5-1's commit, `main.ts` only by R5-9's, `supervisor.ts` only by R5-2's. `crash-terminal.ts` was committed under R5-1's commit with R5-2's 2-line follow-up — both reports account for this (R5-2 authored it, R5-1 swapped onto it; the parent's commit packaging is the only oddity, and it is cosmetic).

## 2. Requested target verifications

### 1. R5-1's pause-on-live is explicit, not incidental — VERIFIED
The trade is stated in three places and enforced in one. `withRunLifecycleLock`'s docstring has a "What it costs, stated rather than discovered" section that names `requestPause` from another process as a blocked writer, with the remedy (`doctor` names the holder; stop the process you own). The pin `a cross-process pause of a live run fails closed rather than racing it` (`run-lifecycle-lock.test.ts:276`) drives a **real** `createFilePauseController.requestPause` against a live run from inside its executor, asserts the rejection is `LOCK_TIMEOUT_CODE` (code, not message), asserts `pause.json` was **not** written, and asserts the run completed undisturbed. The report's §3 additionally argues why the old behavior was never sound (`pauseFlowchartRun` ends in `finish`, which settles the episode and checkpoint under a process still driving them). This is a deliberate posture with the parent's sign-off, pinned in the direction that hurts.

### 2. R5-5's NO is evidenced — VERIFIED
The commit (`c1bf164`) is test-only, matching the ownership rule. The decision rests on numbers and a demonstration, not taste: the crash-budget sweep reproduces R4-4's table with the two columns that matter (result-on-parent-log vs child-log-closed vs accepted); the wall-time window is measured across four run shapes; the prototype runs the **real** `applyChildThreeLine`/`restoreFlowchartSupervisor`/`resumeFlowchartRun` at five budgets against an uncrashed control and lands byte-identical at budget 2; the cascade counter-example (one child run, two `TASK_RESULT`s, `TASK_RETRY` between) is reproduced and pinned, which is what makes "the child log's terminal is the precondition" a fact rather than a preference. The risk asymmetry (detection runs on every resume, payoff fires once in ~10⁴ crashes; the failure mode of drift is a silent false success, which is on the standing forbidden list) is the strongest argument in the round. The flip conditions are stated and falsifiable. The four pins landed and their mutation controls discriminate. I also reproduced its §4.4 out-of-slot observation myself — see finding 1.

### 3. R5-6 does not reshape `deadLetterReport` — VERIFIED
The entire behavioral diff is inside `claimRole`: a `holdersByRole` map fed by claims, and the skip predicate `mail.from === agentId` → `roleHolders.has(mail.from)`. `ClusterDeadLetter`, `ClusterDeadLetterReason`, `MailboxOptions`, `deadLetters()`, `requeueCount()` are untouched; `host.ts` (where `deadLetterReport()`/`onDeadLetter` live) is doc-comment-only in the diff; the CLI line format is asserted byte-identical in the new integration test. The one deleted line (`requeues.delete(mail.id)` in the delivery branch) is correctly argued unreachable: a tally exists only after a skip, the skip predicate is monotone in the ever-growing holder set, so a tallied mail can never reach delivery. The production-shaped run — five scouts chained through `dependsOn`, each attempt registering a fresh `createAgentInstanceId`, exactly the shape R4-2's census said could never drop — completes with `deadLettered: 1` and the exact pinned line. The negative control (old predicate restored → the run reports *nothing*, both counters zero) also validates R5-6's sharpened census finding: multi-registration runs used to lose the starvation signal entirely by handing stale mail to an unrelated later instance.

### 4. `persist/` freeze — HELD
Empty diff over `src/persist/**` for the round (and since `74daff3`). All three R5-4 call sites now import the frozen writer; a post-change census over the three files finds no `open(`, `rename(`, `.tmp` construction. R5-3's source pin (`bandit-store-atomic.test.ts`) forbids `writeFile(`/`rename(`/`.tmp` from reappearing in the bandit store. No new private temp+rename copy anywhere in the round's diff.

### 5. No new named skip besides `PI_SMOKE` — VERIFIED
Census over `test/**` (`skip:`, `.skip(`, `test.skip`, `it.skip`): one hit, the `PI_SMOKE` gate in `provider-smoke.test.ts`. Gate TAP skip count exactly 1, confirmed by name.

## 3. Per-slot notes beyond the targets

- **R5-1 (ACCEPT).** The census correction that reframes the slot is verified and important: R4-1's "refuse" was actually *remove-the-subtree-then-refuse* — the delete destroyed part of a live run's records on its way to failing closed. That is now in the `deleteRunRecords` docstring, so the strongest argument for the change is recorded where a future round can challenge it. The bench is the most honest of the loop: five sets per configuration published, the A/A noise floor shown to be as large as the A/B signal, and the claim rested on the noise-independent per-acquisition cost (0.141–0.161 ms) with both predictions (+0.6 %, +2.0 %) inside the observed spread. `startRun` beyond the brief is correctly justified (plain `run --objective` reaches it; leaving it unlocked would make the new error message lie). The three mirroring rules published for R5-2 are each backed by a named test that caught the author. Disclosures 4 (resume of a nonexistent run id leaves an empty `runtime/runs/`, chosen over a privacy hole) and 6 (the track-loop clarification path is now the one unlocked CLI-reachable embedder — **verified**: `track/loop.ts:198-204` mints a run id and writes run records with no lifecycle acquisition) are both real; 6 is a Round 6 candidate.
- **R5-2 (ACCEPT-WITH-NITS).** The extraction is genuinely byte-compatible (the exact-string reason pins in three suites pass untouched) and the settle-on-crash design is right: re-reading the status from the log makes the settle follow the crash terminal when there is one and the honest recorded state when there is not — the in-flight-only rule deciding, not being widened. The `afterTerminal(events) === ["EPISODE_CLOSED"]` sharpening of the two changed asserts is disclosed and strictly stronger than what it replaced. The disclosed-joint handling is the model the R4 brief asked for. **The nit:** `startSupervisedRun` wraps `discoverProject` and `validateTaskGraph` *inside* the lock, where R5-1's published rule 1 — quoted in R5-2's own report — hoists everything that can refuse the run. A supervised start refused at discovery or graph validation now creates `runtime/runs/` (the lock's `mkdir -p`) for a run that never happened; the flowchart/parent/M0 planes all keep the strict contract and M0 pins it (`a missing project root rejects without persisting a run directory`), while the supervised plane has no such pin and now silently differs from its three siblings. Harmless in degree (an empty directory; no records, no leftover lock file), undisclosed in kind. Residuals 1–4 are real and honestly stated; residual 3 (the abandoned-handle hazard) is now a standing trap two suites have already hit — Round 6 material.
- **R5-3 (ACCEPT).** The census additions beyond the brief all check out and improve the slot: the `O_TRUNC` observation (a torn read is an *empty file*, so the old `raw === ""` → `undefined` branch was itself the defect's front door) is verified in the new code, which makes empty-file damage and ENOENT distinct; the blast-radius census of the throw (caught and disclosed under `run --children`, propagated to `stage: "validation"` elsewhere) means the fail-closed change was priced at its operator surface, not just at the API. The skew half rebuilds from validated fields only, so unknown keys drop at the boundary and the next publish carries exactly the five known keys — pinned. The one rewritten pre-existing test asserted the defect as the contract ("fails closed" while asserting `undefined` plus a successful overwrite); rewriting it was necessary and is disclosed as the slot's only behavioral pin change.
- **R5-4 (ACCEPT).** Tersest report of the round, everything verified in the diff. The two fixed-`${path}.tmp` `"w"` copies and the leak-on-write-failure copy are gone; the callers' bytes are pinned; the credential store's `chmod` remains post-publish with a source pin on the ordering. Deliberately not padding `scripts/crash-probe.mjs` with a duplicate case is the right call and the reasoning is stated.
- **R5-7 (ACCEPT-WITH-NITS).** The R4 truth-ups verified accurate against source at its census time. The nits are the same follow-through shape as R4-8's, now affecting more text: R5-1, R5-2, and R5-6 all landed after the 19:34 UTC snapshot, so at HEAD the status-matrix and architecture-spec snapshot blocks assert an instance-level mailbox skip and an unlocked run lifecycle that no longer exist, and the standing prose above them ("`pending=` is the production-reachable signal", "bounds sender-only role-cast requeues") now understates/mis-frames R5-6's role-level semantics. Also undocumented: `BANDIT_STATE_UNREADABLE` (R5-3 explicitly handed R5-7 the wording, but landed after the docs commit), R5-9's `next:` routing, R5-10's typed checkpoint error. All follow-through for the Round 6 docs slot, none of it an error at the time it was written.
- **R5-8 (ACCEPT).** The witness is real: the fixture's configured default (`loopback-2`) is distinct from the requested model, so `body.model === "loopback-1"` proves selection, not coincidence; `reasoning_effort` is pinned present-with-value on the flagged resume and absent on the flag-free start, which closes R4-6's disclosure #4 at the wire. The supervised witness is honestly scoped (BLOCKED/exit 1 by design — the loopback's plain text cannot satisfy the deterministic judge, and the report says why that does not weaken the witness). On the joint: R5-8 committed third; the lock landed sixth and tenth. Its fixture used the abandon-a-live-handle fiction that was valid at HEAD when written, so the break belongs to the round's ordering, not to this slot — and the round's disclosure machinery (R5-2's report, R5-1's bisection, the parent's fix) handled it exactly as the R4-10 lesson prescribed.
- **R5-9 (ACCEPT).** The two structural decisions are both right and both argued: routing keyed on imported constants (rename → compile error) and message-matching disproven by a negative control rather than asserted. The end-to-end case drives the shipped `delete` against a real foreign lock holder and then runs the exact remedy command the `next:` names, asserting doctor's inventory answers it — the route lands on a surface that works, which is the whole point of the slot. The disclosed limitation (no portable end-to-end `RUN_RECORDS_SURVIVED` producer; a flaky pin is worth less than an honest gap) is the right trade. Only-two-codes-routed is the correct discipline: a route needs a doctor surface that answers it first (Round 6 candidate).
- **R5-10 (ACCEPT).** Census verified: `replayFromLog`/`replayEpisodeEvents` had no production caller; the live path (`episode/store.ts` through `validateEpisodeEvent`) already carries the pins the dead module's self-test duplicated. The replacement census pin (module absent; returning requires a live caller) is the R3-8 pattern. The checkpoint change is surgically scoped: ENOENT branch byte-identical, valid-file behavior unchanged, `write` and the frozen decision pin untouched. Reporting the brief's `flowchart-io.ts` claim as stale — with history checked — instead of "fixing" a compliant file is exactly the census-first discipline the brief asks for; verified: `readJsonFile` has thrown `DomainValidationError("Invalid <label> <path>: …")` all along.

## 4. New reviewer findings (evidence for Round 6)

1. **Verified and reproduced: an ordinary verification-failed clustered node produces a double-terminal log that the tree's own replay flags as anomalous.** R5-5's §4.4 flag checks out at HEAD with an unmodified `startFlowchartRun`: a child returning `outcome: SUCCESS` with `verification: { kind: "FAILED" }` drives the three-line gate to `TRACKING_ASSESSMENT` → `GATE_TRANSITION` → `RUN_BLOCKED` (queue_analysis: "terminal BLOCKED until an explicit unblock", per `gate-apply.ts`'s own comment), after which the flowchart loop appends `RUN_FAILED` (`persistFailed` guards only on a prior `RUN_FAILED`), and `replayRun` reports `anomalies: ["multiple terminal events"]` because `RUN_BLOCKED` sets `sawTerminal` (`replay.ts:124-129`). The two subsystems disagree about what a verification-failed node means: the gate says operator-actionable BLOCKED, the loop says terminal FAILED, and the FAILED buries a state `RESUMABLE_CRASH_STATUSES` treats as resumable. Not a false success (fail direction both ways), but a semantic collision on a production-ordinary log, and several suites assert `anomalies` deep-equals `[]` on their own logs. → R6-1.
2. **A resumed run's children do not run under the specs the run started with.** `childTasksFromDefinition` (`flowchart-run.ts:234-253`) rebuilds every child spec from the checkpointed flowchart definition with **invented** limits (`maxAttempts: 2, timeoutMs: 60_000, maxWallTimeMs: 3_600_000`), **empty** `acceptanceCriteria` and `inputArtifactIds`, and **no** `assignedModel` or `cascade` — while production sets a cascade plan on every routed child and the CLI builds children with real acceptance criteria. R5-5's census stated the cascade/assignedModel half; the acceptance-criteria half is sharper, because the three-line gate assesses against `spec.acceptanceCriteria` — a resumed node may be gated more weakly than the node the run started. The parent log already carries the original `TASK_REQUEST` (limits, criteria) and `MODEL_ROUTED`, so reconstruction may not need schema. This is R4-6's "resume is a re-specification" problem one layer down, un-disclosed and unpinned. → R6-2.
3. **The lifecycle lock has two unlocked/inconsistent corners left.** (a) `track/loop.ts`'s `waitForClarification` mints a run id and writes a whole run's records with no lifecycle acquisition (R5-1's disclosure 6, verified) — the one CLI-reachable embedder the new `RunRecordsSurvivedError` message clause still truly describes. (b) R5-2's supervised pre-flight sits inside the lock (§3 nit), so a refused supervised start persists an empty `runtime/runs/` where its three siblings persist nothing, unpinned. → R6-3.
4. **The three learned/derived-state error codes route nowhere.** R5-9's map deliberately routes only codes doctor can answer; `BANDIT_STATE_UNREADABLE`, `PREFERENCE_SNAPSHOT_UNREADABLE`, and `CATALOG_OBSERVED_CORRUPT` all name manual remedies in their messages but have no doctor inventory that names the damaged file, its plane (learned vs rebuildable), and the remedy. The R3-6→R4-5→R5-9 line stops one surface short of the adaptation plane. → R6-4 (surface), R6-5 (routing).
5. **The abandon-a-live-handle fiction is now a standing test trap.** Two suites hit it in one round (R5-2's own, R5-8's); the remedy exists in two inline copies (`simulateProcessDeath` in `m2/resume.test.ts`, the parent's inline `rmSync` in the loopback suite) with the safety argument (owner-token release) duplicated. Any future slot that starts a run and abandons the handle will hit it again. A shared helper is cheap insurance. → folded into R6-6.
6. **R5-1's bounded-wait default is a CLI-surface decision nobody owns yet.** A long-running production run still times out `delete --run` at `withExclusiveFileLock`'s 5 s default; `deleteCommand`/`pause` do not parameterize the wait. R5-1 disclosure 7 named it out of scope; R5-9's `next:` line softens it. Worth deciding once, cheaply. → folded into R6-5.

## 5. Handoff

- `ROUND5-BRIEF.md` (same directory) carries the Round 6 injection context: what landed, the new baseline (gate 1725/1724/0/1-skip; probe 8×3; frozen list grown), and ten mutually exclusive candidates ranked with the two verified semantic collisions at the top.
- Nothing was committed by this reviewer, no `src/**` or `docs/**` file was edited, and no fix was needed anywhere. The parent should commit `.agent_workspace/loop4-r5-review.md` and `.agent_workspace/ROUND5-BRIEF.md`. Reviewer scratch (the §4.1 repro scripts) lives under `/tmp/r5-review`, not in the repo.
