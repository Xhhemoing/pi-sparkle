[Model: claude-fable-5-thinking-xhigh]

# Loop 4 · Round 17 — SOTA review at `6b8124d`

Reviewer ran independently on this VM (Node v22.14.0, engine warning only), on `agent/opt-continuous`, working tree clean at HEAD `6b8124d`. Every verdict below is against the actual range `5ed437a..HEAD`: two landings (`16a471d` R17-2, `223e3dd` R17-1) plus three orchestrator commits (`9c7cb3e`, `c3ef49e`, `6b8124d` — all `.agent_workspace/**`-only, verified per-commit with `git show --stat`; `6b8124d` touches only `PROGRESS.md`). Commit chronology (UTC 2026-08-25): `9c7cb3e` 03:33 close R16/open R17 → `c3ef49e` 03:34 dispatch ids → **`16a471d` 03:37:29 R17-2 → `223e3dd` 04:13:27 R17-1** → `6b8124d` 04:14:48 (PROGRESS-only). The range's `src` diff is exactly one file, `src/learning/from-episode.ts` (net deletion + one docstring); `src/cli/adapt.ts`, `src/preferences/store.ts`, `src/preferences/service.ts`, `docs/**` (including `docs/decisions/`), `README.md`, `USAGE.md`, `scripts/` (including `crash-probe.mjs`), `test/integration/persist/crash-recovery.test.ts`, `test/unit/privacy/deletion.test.ts`, and `package.json` are all **diff-empty** across the range (scoped `git diff`, 0 lines). No file outside `.agent_workspace/` was changed by this review; all mutation runs happened in full copies under `/tmp` with `node_modules` symlinked, deleted afterwards and verified gone; gate log at `/tmp/r17-gate.log`, probe JSON at `/tmp/r17-probe.json`.

## 1. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R17-2 (`16a471d`) | **ACCEPT** | Exactly the one prescribed test, zero `src`; verified at source that it exercises the **fallback arm** (the racer fires from the temp-name hook, the injected EPERM skips the primary `link`, so the fallback `COPYFILE_EXCL` is the publish that must refuse); reviewer's own out-of-tree clobbering-fallback mutant — the one that survived all 23 tests in Round 16 — is now killed with the new test as the **sole** red (19/20, §4.2) |
| R17-1 (`223e3dd`) | **ACCEPT** | Direction (b) exactly per sign-off: call, import and full `episodeId` plumb gone; in-source record restates the R16-1 writer contract at the former call site; pins genuinely bind the store; reviewer's M1 revert killed by two owned pins plus plane-boundary as a third layer; the outside-ownership allowlist edit is a **required joint**, proven mandatory by reviewer's own M2 (§4.1) |
| Parent | Landing hygiene clean: both landings are slot-files-plus-report only, zero PROGRESS ticks, zero overlapping files; gate and probe numbers match this review's independent runs exactly; the (b) sign-off was executed without binding, locking, or persisting — nothing to relitigate |
| Joints | One: R17-1's two-entry shrink of `test/unit/privacy/plane-boundary.test.ts` (outside its granted set). Disclosed in its own report section, zero logic change, proven necessary (leaving the entries = red tree at the commit point), privacy posture strictly narrower. Scored as required joint, not a nit; dispatch-side note in §5 |

**2 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK.** Zero red-tree commit points: the two landings share no files, R17-2 landed first and is test-only, and R17-1's joint edit is precisely what kept its own commit green.

## 2. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0: 1981 tests / 1980 pass / 0 fail / 0 cancelled / 1 skipped, 112 suites** — matches the parent's recorded numbers exactly. Exactly one `# SKIP` line in the TAP output (`grep -c "# SKIP"` = 1), the standing `PI_SMOKE` real-provider gate ("PiAgentExecutor completes a run against a real provider"). `tsc --noEmit`, `eslint .`, and `tsc -p tsconfig.build.json` all ran in the same gate, exit 0.
- **`node scripts/crash-probe.mjs` → `ok: true`, 11 cases × 3 iterations, exit 0** — full JSON compared name-by-name against the Round 16 record: all eleven names and their order unchanged (`jsonl-truncated-tail`, `checkpoint-old-then-next-write`, `stale-lock-no-steal`, `sigkill-run-lock-operator-recovery`, `feedback-cascade-strip-before-tombstone`, `feedback-rewrite-kill-before-rename`, `invocation-rewrite-kill-before-rename`, `episode-settle-stale-lock-recovery`, `atomic-write-stale-unique-temp`, `unblock-append-before-checkpoint-sigkill`, `unblock-discard-append-before-checkpoint-sigkill` last). **No 12th case**; `scripts/crash-probe.mjs` diff-empty across the range.
- **Test delta vs Round 16 (+4 tests, +0 suites) decomposed exactly:** R17-1 +3 (gate TAP `ok 361` the CLI pin, `ok 715` the direct-outcomes pin, `ok 716` the routed-events pin), R17-2 +1 (nested `ok 7 - never overwrites a destination that appears during the exclusive-copy fallback`). 1977 + 4 = 1981. ✓
- **Owned files re-run at HEAD by this review:** `migrate-legacy.test.ts` 20/20; `from-episode.test.ts` + `adapt.test.ts` + `plane-boundary.test.ts` 30/30 (6 + 19 + 6, wait-free — none of the four added tests uses locks, sleeps, or spawned processes, so the 3× rule does not bind; R17-2's report shows its own 3× anyway).
- **Commit hygiene:** verified per-commit (§ header). Each landing = its owned files + its own `.agent_workspace/loop4-r17-tN.md`, nothing else, zero PROGRESS ticks.

## 3. Freeze check

Everything outside the five touched files is byte-identical to `5ed437a`, which Round 16 verified in full — so every run-plane freeze holds structurally. The requested spot-checks were still read directly at HEAD:

- **Isolation:** `loadProjectBanditByKey` exactly `learning/bandit-store.ts` + `cli/doctor.ts`; `selectArm` exactly `routing/bandit.ts` + `routing/shadow.ts`; bare `loadProjectBandit` zero `src` matches (ripgrep with `ByKey` filtered out, empty).
- **ADR-006 Proposed** (`0006-pi-extension-reverse-adapter.md:5` read directly); `docs/decisions/` diff-empty.
- **`independentEvidence`:** exactly one `void` (`prescore.ts:89`); only the `tracking/from-child.ts:228` writer plus `prescore.ts` mention it.
- **`RunStatus`:** exactly eight members (`domain/status.ts:1-12`, read directly).
- **`DOCTOR_ROUTED_NEXT`:** exactly five routes (`LOCK_TIMEOUT`, `RUN_RECORDS_SURVIVED`, `BANDIT_STATE_UNREADABLE`, `PREFERENCE_SNAPSHOT_UNREADABLE`, `CATALOG_OBSERVED_CORRUPT`) at `main.ts:2051-2077`, message text character-exact against the Round 16 record; `GENERIC_FAILURE_NEXT` untouched.
- **`INSPECT_SUMMARY`** four keys (`main.ts:1163-1173` — `type`, `runId`, `status`, `requiredEvidence`), untouched region.
- **`EventStore.append` / `CheckpointStore.write` remain unlocked:** the sole `withExclusiveFileLock` match in `event-store.ts` is the standing doc comment that records *why* the per-step writers do not take it; `CheckpointStore.write` is a bare `writeFileAtomic` (`checkpoint-store.ts:40-42`). Both files diff-empty.
- **R16-1 writer contract holds:** `withPreferenceSnapshotLock` has exactly two call sites, inside `prefCorrect` and `prefDelete`; `src/preferences/store.ts`/`service.ts` diff-empty; `adapt.ts` has zero matches for `lock-wait-ms`/`lockWait`/`bindPreferenceStore`/`configurePreferencePersistence` — R17-1 added **no** lock and **no** third writer (it removed a would-be one).
- **Probe pin** `crash-recovery.test.ts` diff-empty; **`deletion.test.ts`** diff-empty and its pin green in this review's gate (`ok 862 - deleting an episode does not delete preferences learned from it`).
- **Raw-write sweep re-run at HEAD:** whole-`src` `writeFile`/`writeFileSync` outside `persist/atomic-file.ts` finds only doctor's transient write-probe (`doctor.ts:200`) and the lock owner record (`file-lock.ts:79`) — identical to the Round 16 sweep. No new tmp+rename helper, no new publish shape.
- **No live R1 on the execution path, no Outcome-supported claims, no auto-promote, no dependency edits, no history rewrites, no 12th probe case** — structurally excluded: the range's only `src` change deletes code and edits a docstring.

## 4. Per-slot verification

### 4.1 R17-1 (P3) — remove the persistence-dead inferred-preference recording, direction (b)

Everything the sign-off specified, verified at source, not from the report:

- **The removal is complete.** `recordInferredPreference` call and its `../preferences/service.js` import gone from `from-episode.ts`; the `episodeId` field gone from `LearnFromOutcomesInput`; the `episodeIdFromEvents(read.events)` call, its `../run/episode-bind.js` import, and the `EpisodeId` type import all gone. Whole-tree census at HEAD: `recordInferredPreference` remains only as the `service.ts` definition, the `service-export.test.ts` embedder test (untouched, green in the gate), and the comment-form mention in the new docstring; `episodeIdFromEvents` importers are now exactly `episode-bind.ts` (definition), `child-tracking.ts`, `cli/main.ts`, `track/loop.ts` — the learning-plane edge is gone.
- **The in-source record is the right one.** `proposeRoutingFromOutcomes`'s docstring (`from-episode.ts:88-101`) states the candidate is the only write, why the old call could never survive a one-shot CLI process (`MIN_INFERRED_RECURRENCE_DEFAULT` unreachable), that re-adding the call would not make the plane live, that `recordInferredPreference` stays an embedder API — and restates the R16-1 contract (a host that binds the store is a snapshot writer owing `preferenceSnapshotLockPath` across bind+mutate+persist) exactly where a would-be re-adder will read it.
- **The pins genuinely bind the store.** Verified in `store.ts` that `recordInferredPreference` → `recordPreference` → `applyObservation` pushes into the in-memory `observations` array **and** calls `saveToDisk()` immediately — so under `withBoundPreferenceStore` (reset + `configurePreferencePersistence`) a leftover call is visible both as a non-empty `listObservations()` and as a written `adaptation/preferences.json`; both asserts are load-bearing. The routed-events pin seeds a real log through `EventStore.append` (every event validates) carrying `RUN_ATTACHED` — the exact former trigger — and asserts `created === true`, proving execution passed the former call site. The CLI pin drives `adaptCommand(["learn", "--run", ...])` end-to-end: exit 0, `proposed routing-policy candidate` on stdout, `adaptation/registry.json` present (the advertised durable write survives), `preferences.json` absent, `listObservations()` empty — candidate persists, preference does not, exactly the sign-off's shape.
- **M1 re-proven (reviewer's own copy, `/tmp/r17-rev-mut1`, deleted):** restored the pre-removal `from-episode.ts` wholesale (call, field, plumb, both imports). Across the three touched test files: **4 fail / 26 pass** — `not ok 18 - adapt learn persists a routing-policy candidate and no preference snapshot` (CLI pin), `not ok 24 - the routed-events learn path records no inferred preference for an episode-bound run` (the named mutant killer), plus two plane-boundary tests (`adaptation plane does not import runtime modules outside the allowlist`, `adaptation value-import closure enters runtime only through sanctioned readers`) as a third independent layer. Matches the slot's M1 exactly.
- **The disclosed pin-1 survival is honest and structural:** the direct-outcomes pin drives `proposeRoutingFromAssignments`, which never passed `episodeId` even pre-removal (confirmed in the pre-removal source: the call fired only under `if (input.episodeId !== undefined)`), so it survives M1 by construction and could not be written otherwise against the shipped tree — post-removal the field does not typecheck. Its job is pinning the direct API preference-free; it does that.
- **M2 re-proven (reviewer's own copy, `/tmp/r17-rev-mut1` reused with shipped `src`, deleted):** the two removed allowlist entries put back redden exactly `not ok 3 - every allowlisted exception still exists and type-only ones stay erased` and `not ok 4 - adaptation value-import closure enters runtime only through sanctioned readers` (2/6). So the guard fails closed on stale entries in **both** directions, and the outside-ownership edit was **mandatory**: shipping R17-1's `src` without it commits a red tree. A pure two-entry allowlist shrink, zero logic change; the sanctioned `from-episode` pipe now enters the runtime plane through `run/event-store.ts` alone — privacy posture strictly improves. Required joint, not a nit; the dispatch-side lesson goes to §5.
- **Nothing forbidden happened:** `preferences/store.ts` untouched, no lock anywhere in the diff, no `--lock-wait-ms` on adapt, no bind (grep zero on all four symbols in `adapt.ts`). `src/cli/adapt.ts` diff-empty as claimed — ownership granted, no change warranted, and the in-source record legitimately lives at the call site instead.
- **Census terminator honored:** no dictionary note, and none owed — inferred preferences were never durable from the CLI, `README.md:168` already advertised `adapt learn` as candidate-only, and this review confirmed `docs/**`/`README.md`/`USAGE.md` diff-empty across the range. The slot's five-surface staleness read (report §4) spot-checked and accurate.
- **Residuals, decided:** (1) `LearnFromOutcomesInput.projectId` now unread inside `proposeRoutingFromOutcomes` (verified: the body reads only `stateRoot`/`projectRoot`/`primaryModelId`/`outcomes`) — **kept correctly**. It is a required member of an exported input type; removing it is a public-API break the sign-off did not authorize; it is not a persistence hook; and it is still live one level up, where `proposeRoutingFromRoutedEvents` reads it from `PROJECT_DISCOVERED` to drive the `run has no project snapshot` refusal (`from-episode.ts:192`) and passes it through (`:199`). Recorded as a trivial residual with the slot's own contained prescription, for whichever future slot next owns the file. Not a nit. (2) The `const result = await withAdaptationRegistryLock(...); … return result;` leftover (`:112-167`) — cosmetic, eslint- and tsc-clean, recorded as trivial. (3) One hygiene item the report slightly misattributed: the slot's disclosed interrupted shell left `/tmp/pi-sparkle-adapt-learn-vxDA0x` (03:38:24 UTC — inside the slot's session window), which carries the slot's **own** new-test prefix, not just "other suites'" — a killed process where the `finally` could not run. The shipped cleanup is correct: this review's gate and mutation runs (04:15+) leaked nothing under any new-test prefix. Remnant removed by this review; no tree contact. None of the three affects the verdict.

### 4.2 R17-2 (test-only) — the migrate-legacy fallback never-overwrite pin

Exactly the Round 16 prescription, verified at source rather than trusted:

- One test added, combining the two pre-existing seams: `link` injected to reject `EPERM` **and** `uniqueSuffix: racerWriting(destination, live)` with divergent bytes. The seam order makes this a genuine fallback-arm proof: `uniqueSuffix` fires inside `copyToUniqueTemp` (temp-name construction, before any publish), so the divergent destination exists before publish; the injected `EPERM` means the primary `link` never runs to completion, so the **fallback** `copyFile(temp, destination, COPYFILE_EXCL)` (`migrate-legacy.ts:262`) is the operation that must refuse — its `EEXIST` propagates to the caller's pre-existing digest branch, `sameContent` mismatches, and the command fails. Asserts: exit 1, `could not copy feedback/records.jsonl` on stderr, destination bytes byte-equal to the racer's `live` content, `tempsBeside(destination)` empty. This is distinct from the pre-existing primary-arm racer test (no `link` injection) and from the pre-existing fallback test (absent destination) — the exact hole Round 16 proved.
- **Mutation re-proven (reviewer's own copy, `/tmp/r17-rev-mut2`, deleted):** fallback changed to a clobbering `copyFile(tempPath, destination)`. Owned file: **19 pass / 1 fail**, and the sole red subtest is `not ok 7 - never overwrites a destination that appears during the exclusive-copy fallback` — the mutant that survived all 23 tests at Round 16 now dies. Matches the slot's transcript verbatim.
- Zero `src`; `scripts/crash-probe.mjs` diff-empty; owned file 20/20 at HEAD in this review's own run and in the gate; the slot's 3× record stands. The documented fallback crash window and inert orphan-temp posture correctly untouched.

## 5. Process notes

- **Landing hygiene:** both landings are slot-files-plus-report only, zero PROGRESS ticks (Round 12 model held, verified per-commit). Zero overlapping files. R17-2 landed into a clean tree; R17-1's report discloses starting from `9c7cb3e` with only untracked orchestrator files.
- **Dispatch-side note (no verdict impact):** R17-1's ownership grant omitted `test/unit/privacy/plane-boundary.test.ts`, whose allowlist pins the exact import edge the sign-off ordered deleted — making a consequential edit outside the granted set inevitable. The slot handled it the best available way (censused that no sibling owned it, shipped rather than committing a red tree, disclosed in a dedicated section, and proved necessity with its own M2 — independently reconfirmed here). Future dispatches that change a `src` import edge on the adaptation plane should census the plane-boundary allowlist into the ownership grant up front.
- **Sign-off compliance:** the parent's (b)-remove decision was executed without relitigating — the landing did not bind the store, took no lock, persisted nothing (all three verified at source and by grep), so there is nothing to reopen.
- **Slot-report accuracy:** both reports check out claim-by-claim against source, git, and this review's independent re-runs; the only correction is the trivial `/tmp` remnant attribution in §4.1(3). R17-1's disclosed environment failure (shell death mid-sweep) cost nothing evidentiary — everything cited in its §5 predates it, and this review's full gate covers the lost sweep and more.

## 6. Round 18 disposition

**Zero candidates.** Round 16 proved exactly two remaining holes on the retarget plane (I/O, races, protocol, DR) and Round 17 closed both. This review re-swept for anything new and found nothing: the range added no `src` behavior (net deletion — no new seam can have landed), the raw-write sweep at HEAD is unchanged (doctor probe + lock owner record only), the last unlocked read-modify-write writer was locked in Round 16, the last unpinned publish arm is now pinned, probe coverage is settled (11 cases, 12th declined twice), and every USAGE/docs surface this round could stale was read and is accurate. The honest round is zero slots; `ROUND18-BRIEF.md` §4 records the exhaustive valid reasons to dispatch later instead of manufactured work. Known non-slots (the `doctor.ts:507` path fold-in, the `projectId` residual, the `return result` cosmetic, `MAX_LOCK_WAIT_MS` wording) are carried in §5 of the brief with owners-on-next-touch.

## 7. Handoff

- Gate GREEN at `6b8124d`: **1981 / 1980 / 0 / 1 skipped** (`PI_SMOKE` only, exactly one `# SKIP`), 112 suites; crash-probe **11×3 `ok: true`**, names verified one-by-one, `unblock-discard-append-before-checkpoint-sigkill` last — both run independently on this VM and matching the parent's record exactly. Delta +4 tests / +0 suites fully decomposed to the two landings.
- **2 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK.** Both Round 16-proven holes closed; R17-1's outside-ownership allowlist edit scored as a required joint (M2-proven mandatory), with a dispatch-side ownership-census note; all three residuals trivial and recorded.
- `ROUND18-BRIEF.md` carries **zero** candidates with the valid-dispatch-reasons list, the Rounds 1–16 freeze verbatim, and the two new Round 17 contracts (the CLI inferred-preference plane is not live and pinned so; the migrate-legacy fallback never-overwrite is pinned on both arms).
