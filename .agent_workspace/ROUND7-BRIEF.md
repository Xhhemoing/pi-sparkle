# ROUND 7 BRIEF — injection context for Loop 4 · Round 8

Provenance: written by the Round 7 SOTA reviewer (claude-fable-5-thinking) after evidence-based review of R7-1…R7-10 at `bdc4cb9`; verdicts and verifications in `.agent_workspace/loop4-r7-review.md` (9 ACCEPT, 1 ACCEPT-WITH-NITS, 0 ROLLBACK — the cleanest round yet: zero parent fix-joints, zero breaks left at HEAD). Every candidate below cites evidence the reviewer verified at HEAD, not report hearsay. Nothing is in flight: all ten slots are committed (`e427dd9`…`9464fe1`). **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 7 (all committed)

| Slot | Landed | Key symbols (do not re-implement) |
|---|---|---|
| R7-1 | **A resumed flowchart node runs under the spec the parent log recorded for it.** `childTasksFromLog(events, definition, registry, catalog)` replaced `childTasksFromDefinition` at the single resume call site: objective, artifacts, criteria, budget from the last `TASK_REQUEST` per task; the true `AgentRole` + model + regenerated cascade from the `MODEL_ROUTED` **with `agentRole !== undefined`** (the two-producer trap, handled by filter); `dependsOn` from the checkpointed edges. A never-ran node keeps empty criteria/artifacts and gets an authorized budget: earliest logged sibling → the run's own `RunLimits` (with named `FALLBACK_CHILD_TIMEOUT_MS = 60_000`, the one invented number). Rebuild is a fixed point (last request per task; repeated resumes converge — pinned). Behavior change, disclosed and pinned: resumed children now honour *tighter* caller limits; the one loosening is no-sibling `maxAttempts` 2→3 (the run's own declared limit). `FlowchartContinuation.contract` is a new optional seam, honoured when supplied — **and no production caller can supply it**: the contract is on no record a run id can reach (reviewer-verified: `replay.ts`/`checkpoint-store.ts` have no `contract`; `episode-bind.ts` copies only `acceptanceCriteria`) | `childTasksFromLog`, `assignedRoutes`, `fallbackChildLimits`, `loggedTaskRequests` in `run/flowchart-run.ts`; the rewritten rebuilt-spec pin + reconstruction pin + cascade-escalation pin in `flowchart-run-abort.test.ts`; the substitution pin + **the flip-pin** `a resume that is handed no contract assesses its children against none` in `m2.5/resume.test.ts` |
| R7-2 | Decision recorded, comment-only src (option (b), parent-signed): **acceptance criteria are prompt guidance plus a plan-time coverage obligation; the deterministic verifier is the sole gate.** New verified finding that changes option (a)'s scope: it is **three changes, not one** — `coverageOutcome` gaining FAIL would be a gate no-op because `gates.ts` has no unmet-criterion anomaly code and `turn.ts` feeds `evaluateGates`/`combineScore` the **uncapped** `P`; `cappedByHardFail` caps `displayPrescore` only (all reviewer-verified in source). R6-2's `/tmp` sweep is now a re-runnable 270-cell in-tree test | `test/unit/tracking/criteria-are-guidance.test.ts` (10): the 270-cell directive-invariance sweep; `check-coverage` no-FAIL range pin; `constraint-retention` producer-blocked pin; the display-cap-vs-gate pin; three record-keeping pins (contract prose in both files; tripwire must exist; **sole-production-path census** — `child-tracking.ts` is the only tracking-behaviour importer outside `src/tracking/`; if that pin goes red, re-derive the sweep for the new entry point, do not delete) |
| R7-3 | Investigation + pins, no `src/**`, no schema (as signed off): **BLOCKED is absorbing, now pinned precisely** — all three flowchart recorders' `alreadyTerminal(ctx)` refusal source-pinned; four substitute signals (re-`RUN_STARTED`, injection, `TASK_STATUS_CHANGED` READY, `GATE_TRANSITION` to RUNNING) proven unable to clear replay or the latch; the vocabulary pinned to contain **no `RUN_UNBLOCKED`** (a deliberate Round 8 tripwire: **replace, don't weaken**). Full Round 8 design delivered in `.agent_workspace/loop4-r7-t3.md`: `RUN_UNBLOCKED { blockedEventId, reason, retryNodeId? }`, a dedicated locked `unblock` command (not a fourth injection kind), replay latch-clearing keyed to the active block, checkpoint restore for both block shapes, `applyRetry` pin untouched | 2 pins in `test/unit/run/replay.test.ts`; the design + consumer census in the slot report |
| R7-4 | **The parent plane got one terminal recorder with the shared definition of "terminal".** All three terminal appends in `runParentRun` (crash catch-all *and* the loop's COMPLETED/FAILED exits — disclosed widening, R6-1 precedent) route through `recordTerminal` → `loggedTerminalStatus` → `replayedTerminalStatus`; on refusal the settle/checkpoint/`RunOutcome` take the replayed status, so a BLOCKED-then-crash run stays BLOCKED with `EPISODE_WAITING` and `anomalies: []`; an ordinary crash still records `RUN_FAILED` with the raw un-prefixed message. Chose replay over `recordCrashTerminal` because that module's frozen contract says the caller always rethrows and this plane's catch deliberately settles. Honest scope: a *guard* (no in-loop path can produce the pair today; the log has other writers and R7-3's producer is coming), and `startParentRun` has no production caller (M1 embedder; seven test consumers). Residuals flagged, not fixed: crash over WAITING_FOR_USER still buries the wait (terminal-keyed refusal); `RUN_CANCEL_REQUESTED` unguarded (deliberate — operator fact, not status claim) | `recordTerminal` / `loggedTerminalStatus` (private) in `run/coordinator.ts`; 7 pins in `test/unit/run/parent-terminal-refusal.test.ts` incl. the shared-definition source pin and mutation-checked refusals |
| R7-5 | **A BLOCKED `run` finally routes the operator**: both flowchart-outcome sites (`--flowchart`, `--children`) print the newest `RUN_BLOCKED.payload`'s recorded reason + required evidence (`(none recorded)` when empty), `next:` lines for `inspect` and `inject`, and a `note:` that resume cannot unblock ("no event clears a BLOCKED log today … replays BLOCKED until an unblock ships" — both clauses pinned, so the unblock slot goes red rather than the line rotting). stderr-only prelude; `flowchartExitCode` untouched at both sites (pinned + mutation-checked); zero existing lines modified (50 insertions, 0 deletions). Deliberately not wired: `resumeCommand`/`answerCommand` — the loopback pin byte-pins `resumed.err` on a BLOCKED supervised resume; prescription in the slot report §3 | `formatBlockedRunReport` (exported) + `reportBlockedRun` in `cli/main.ts`; 8 pins in `test/integration/cli/blocked-next.test.ts` incl. the gate's `ANALYSIS_QUEUED` payload rendered verbatim end-to-end |
| R7-6 | Docs truth-up for Round 6 (first-replayed-terminal contract, clarification lock, refused-start/refused-resume postures, pre-rounds settle, `learnedState` + three routes, `--lock-wait-ms` + pause posture, cascade wire witness, probe case 9), ADR-006 untouched and Proposed. **Now stale at HEAD for R7-1/2/4/5/8's landings** (all landed 21:08–21:16, after the 21:04 commit), and the in-flight disclosure names R7-1/2/3 only while R7-4/5/8 were also mid-flight — see R8-5 | `docs/data-dictionary.md`, `docs/status-matrix.md`, `docs/specs/m0-m2-architecture.md` |
| R7-7 | `settleSupervisedOutcome` deleted (helper, always-no-op call, gate-only imports) after an exact census: no producer, no caller passing an assessment, no barrel/dynamic consumer; the supervised plane has no gate producer by design. Absence pinned by namespace membership, stronger than a grep | `test/unit/run/supervisor-dead-branch.test.ts` (1) |
| R7-8 | **The doctor exception got narrower**: `loadProjectBanditByKey` + exported `projectBanditPath` (single spelling of the layout) replace the base-31 hash preimage; pure `readPreferenceSnapshot` (validates without binding; `loadFromDisk` now expressed through it) replaces the bind/unbind. Isolation pin's `assert.match` moved to the new symbol in the same diff (parent sign-off recorded in `OWNERSHIP.md`), `because` unchanged, **readers sweep widened to `loadProjectBandit(?:ByKey)?`** so the new symbol cannot become a hole. `learnedState` JSON byte-identical for all producible inputs; two writer-unreachable corrections pinned (foreign-magnitude key now classified; vanished `preferences.json` now `absent`). Root-keyed `loadProjectBandit` survives with **no `src` caller outside its own module** — see R8-9 | `loadProjectBanditByKey`, `projectBanditPath` in `learning/bandit-store.ts`; `readPreferenceSnapshot`, exported `PreferenceSnapshot` in `preferences/store.ts`; 2 new pins in `test/unit/cli/doctor.test.ts`; the amended pin in `live-isolation.test.ts` |
| R7-9 | `validateTaskGraph([])` now throws `DomainValidationError("Task graph must contain at least one task")` in pre-flight — before the lock and every write; pinned persist-nothing (`readdir(stateRoot)` empty; executor never reached). The R6-3 pre-rounds crash pin re-seeded in the same diff onto the episode-store variant (failed bind → guarded `RUN_FAILED`, **no invented episode closure**, FAILED checkpoint, read-only terminal resume). Consumer consequence disclosed: empty input is now a synchronous validation error, not an asynchronously settled FAILED run; the handle still publishes its `runId` with no record behind it | the guard in `graph/validate.ts`; `test/unit/graph/empty-graph.test.ts` (2); the reseeded seed in `m2/supervisor-crash.test.ts` |
| R7-10 | **STOP, justified and reviewer-confirmed**: `CATALOG_OBSERVED_CORRUPT` has no CLI producer — `loadCatalogObservedSnapshot`'s only `src` callers are its own module and doctor, and doctor *absorbs* the typed error into a `damaged`/`derived` inventory entry rather than throwing it into `main`, so no real command can select the frozen catalog route. The bandit half is de-risked: a real `adapt auto` over truncated bytes at the true `stableProjectKey` path was reproduced end-to-end (exit 1, `stage: "validation"`, the exact routed `next:`, the damaged path in the error). No one-sided test added, per the R5-8/R5-9 precedent | evidence in `.agent_workspace/loop4-r7-t10.md`; no tree change |

## 2. Current baseline (this VM, Node v22.14.0)

- **`pnpm gate` GREEN, exit 0**: **1804 tests / 1803 pass / 0 fail / 1 skipped**. The only skip is the `PI_SMOKE=1` real-provider gate. Any slot whose report shows a second skip introduced one and must say so. Delta vs Round 6: +35 tests, accounting closed per-slot in the review §1.
- `node scripts/crash-probe.mjs` → `ok: true`, **9 cases × 3 iterations** — case set unchanged (no Round 7 slot touched the probe). `crash-recovery.test.ts`'s reduced-probe pin still lists all nine by name; anyone changing the case set owns updating that pin in the same slot, with disclosure.
- Perf: no fresh whole-tree bench needed. Round 7 added one `readAll()` per parent-run *terminal* (R7-4, on a path that already reads the log twice within a few statements) and per-resume log parsing that reuses the resume's existing single read (R7-1). No per-step writer changed; no lock added. Any Round 8 perf claim records its **own** same-VM before/after with an unchanged-arm control; ≥5 % end-to-end regression or rollback.

## 3. Forbidden / frozen for Round 8

Global forbidden list, unchanged: live R1/bandit/topology on the execution path (doctor's `loadProjectBanditByKey` inventory read is the **only** exception, signed off, read-only, pinned — do not widen it); Outcome-supported claims; **ADR-006 stays Proposed** (Round 7 kept the discipline; keep it); auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit; no `git checkout` of other branches.

Frozen contracts (Rounds 1–6 set, all held through Round 7, plus new):
- Rounds 1–6 carry-overs, verbatim from the Round 6 brief §3: jsonl signatures; `writeFileAtomic`(+Sync), no private temp+rename; `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE`; `episodeLockPath` symmetry; `runCommand` sink wiring + resume-site pin; `deleteRunRecords` lock-plus-double-verify + `RUN_RECORDS_SURVIVED`; the two decision pins (`append`/`checkpoint write` take no run lock — re-verified present and green); `recordCrashTerminal` in-flight-only + best-effort + **the caller always rethrows** (R7-4 leaned on that clause; a rethrow-free variant is a decision for that module's owner with sign-off, see R8-7); `applyRetry` sole BLOCKED→READY producer at the scheduler (R7-3's design explicitly preserves it); `preserveResumableState`; `ClusterMailReport` + line; resume flags + the four disclosure wordings (the loopback byte-pin on `resumed.err` is why R7-5 left resume alone); `writeFeedbackTombstones`; the three adaptation error codes + bind-after-load; doctor's frozen-additive JSON incl. `learnedState` field names; feedback fuzz hard-fails; `withRunLifecycleLock` rules; supervised settle-on-crash + the rounds-window `afterTerminal === ["EPISODE_CLOSED"]` pins; `commandFailureNext` code-discrimination; `CheckpointStore.read` typed error; loopback stderr pins; one-definition-of-terminal (`TERMINAL_REPLAY_STATUSES`/`replayedTerminalStatus`); the five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT` character-exact; `--lock-wait-ms` semantics; `process-death.ts`; R6-9's absence pins.
- **New from Round 7:**
  - **The reconstruction contract**: a resumed node whose `TASK_REQUEST` is on the log runs under the caller's spec, field for field, role restored through the `agentRole`-carrying `MODEL_ROUTED`, cascade regenerated through `cheapFirstTiers` against the resuming process's catalog; a never-ran node gets empty criteria/artifacts and an authorized budget (sibling → run limits), never the retired `{2, 60_000, 3_600_000}`; the rebuild is a fixed point. All pinned in `flowchart-run-abort.test.ts` + `m2.5/resume.test.ts`. **Two load-bearing tripwires**: the R6-2 FAIL-unreachable tripwire still stands (whoever makes check-coverage real owns the never-ran-node decision — its docstring names `childTasksFromLog`); and `a resume that is handed no contract assesses its children against none` is the pin the contract-durability slot flips **with disclosure**.
  - **The criteria-are-guidance record**: contract prose must stay in `prescore.ts` and `from-child.ts` (comment-rewrap-safe matching); `prescore.ts` must keep naming the tripwire file; the sole-production-path census (only `child-tracking.ts` imports tracking behaviour from outside) must stay true — a new tracking entry point means re-deriving the 270-cell sweep for it, not deleting the pin.
  - **Absorbing BLOCKED, pinned**: no writer may append a terminal over a BLOCKED log on either plane (flowchart's three recorders + the parent plane's `recordTerminal`); the vocabulary-has-no-`RUN_UNBLOCKED` pin in `replay.test.ts` is a **replace-not-weaken** tripwire owned by the unblock slot.
  - **The parent-plane recorder**: any new terminal append in `runParentRun` must route through `recordTerminal`; `loggedTerminalStatus` is the single place that plane decides "the log already ended" (test 7 pins the consultation; review is what catches a bypass).
  - **The BLOCKED operator block**: exactly three routed lines (`inspect`, `inject`, resume-note), the `note:`'s two honesty clauses, `(none recorded)` for empty evidence, newest-`RUN_BLOCKED`-wins — all pinned in `blocked-next.test.ts`; the unblock slot owns changing the note and its pin together.
  - **Empty graphs are refused in pre-flight**, persist-nothing pinned; the pre-rounds crash window's pin now rides the episode-store seed.
  - **The narrowed doctor exception**: the isolation pin requires `loadProjectBanditByKey` in doctor and its widened sweep forbids any other live reader of either bandit-read symbol; `readPreferenceSnapshot` is the only way to inventory preferences without binding. Changing what doctor calls again means amending the signed-off guard again (parent sign-off, explicit `because`).
  - **R7-7's absence pin**: `settleSupervisedOutcome` must not reappear as a supervisor export without a live producer and caller.
- Saturated — do not spend a slot: everything in the Round 6 saturated list, plus: resume re-specification honesty (R7-1 closed it; only contract durability remains, and it is a schema decision); the option-(b) record (R7-2 emptied the decision; only option (a) itself remains, redesigned per R8-4); absorbing-BLOCKED *pinning* (R7-3 emptied it — what remains is the implementation); parent-plane terminal coherence (R7-4 closed it; only the two flagged residuals remain, R8-7); the BLOCKED block on `run` (R7-5 closed it; only resume/answer remain, R8-3); the dead supervised settle seam (R7-7 deleted it); doctor seam hygiene (R7-8 emptied R6's finding 5 — all three seams shipped).

Process requirements per slot (carried forward — Round 7 executed all of them cleanly; keep the streak): census first; scoped `eslint` + whole-tree `tsc --noEmit` before reporting; census your consumers and ship or prescribe their updates in your own diff (R7-1/R7-8/R7-9 are this round's models); run `live-isolation.test.ts` if you add an import inside the live closure (R7-1/R7-4/R7-8 all did, R7-5 over-satisfied); attribute shared-tree transients to files and re-verify after the owner lands (R7-2 §6 and R7-7's exact reruns are the models); timing-sensitive owned tests 3×; full gate is the parent's job; no scratch files at report time; a "did not land" verdict is only true as of the minute taken. **New this round:** docs slots census the *working tree* for in-flight sibling edits rather than trusting the dispatch's in-flight list (R7-6 named 3 of 6 — the wording came from dispatch, but the tree was checkable).

## 4. Ranked Round 8 candidates (mutually exclusive ownership)

The round's tilt: Round 7 pinned BLOCKED as absorbing on both planes and put an honest "until an unblock ships" promise in front of operators — Round 8's spine is shipping that unblock against R7-3's signed-off design. **Hard file contention at the top:** the unblock (R8-1) and contract durability (R8-2) both need `src/run/replay.ts` and `src/run/flowchart-run.ts`, and R8-1 also needs `main.ts`, which R8-3 wants. **R8-1 and R8-2 cannot run in the same round-half; if both are wanted, sequence them with R8-2 second, re-dispatched after R8-1 lands.** R8-3 folds its note-line obligation into R8-1 regardless (the pin goes red the moment the unblock exists).

File-contention note: `src/run/replay.ts` + `src/run/flowchart-run.ts` + `src/run/events.ts` + `src/run/gate-apply.ts` + `src/cli/main.ts` + `src/supervisor/flowchart-supervisor.ts` belong to **R8-1 alone** (or to R8-2 for the replay/flowchart pair if the parent defers R8-1); `src/tracking/**` + `src/protocol/v1.ts` design surface to **R8-4** (design-only unless signed off); `docs/**` to **R8-5**; `src/routing/catalog-observed.ts` + whatever command surface is chosen to **R8-6**; `src/run/crash-terminal.ts` + `src/run/coordinator.ts` to **R8-7**; `src/learning/bandit-store.ts` to **R8-9**; `test/integration/m2/supervisor-crash.test.ts` to **R8-10**.

### R8-1 (P1, the round's spine) — Ship `RUN_UNBLOCKED` (schema sign-off required)
**Evidence (R7-3 design, premises reviewer-verified; R7-5's operator promise):** BLOCKED is absorbing and pinned so; the design in `loop4-r7-t3.md` is complete: event `{ blockedEventId, reason, retryNodeId? }` with strict validation; a dedicated locked `unblock` command (inspect → unblock → resume; not an injection kind — injection deliberately takes no lifecycle lock); replay clears the latch only for a matched active block (`replayedTerminalStatus` → `undefined`, so **all four existing recorders open with zero per-writer edits** — the seam R6-1 and R7-4 built is the integration point); checkpoint restore for both block shapes (gate-block with FAILED-node reopen, fail-closed on executed descendants; stall-block ledger clear) with the event-first crash window made idempotent.
**Change:** implement the design as written. Mandatory consumer updates in the same diff, censused by R7-3 and the reviewer: `events.ts` (constant, union, validation), `event-row-fuzz.test.ts` (exact-keyed seeds), `gate-apply.ts::currentGateStatus` (a valid unblock reads as RUNNING), replay tests (**replace** the schema-absence tripwire with matched/stale/wrong-terminal/repeated-cycle/post-unblock-terminal cases), `formatBlockedRunReport`'s `note:` line **and** `blocked-next.test.ts` pin 2 (both clauses currently promise no unblock exists), the flowchart reopen operation (distinct name; a negative pin that it never calls `applyRetry` — the scheduler pin stays byte-identical). Integration proof: R6-1's verification-failed seed, blocked → `unblock --retry-node` → resume → the node really re-executes and the run can end COMPLETED with no anomaly.
**Ownership:** `src/run/events.ts`, `src/run/replay.ts`, `src/run/gate-apply.ts`, `src/run/flowchart-run.ts`, `src/supervisor/flowchart-supervisor.ts`, `src/cli/main.ts` (all sole), `test/unit/run/replay.test.ts`, `test/unit/run/event-row-fuzz.test.ts`, `test/integration/cli/blocked-next.test.ts` (the one pin), additive integration tests.

### R8-2 (P1 alternative, contends with R8-1 — do not co-schedule) — Durable run contract on resume
**Evidence (R7-1 disclosure 3, reviewer-verified structurally):** `FlowchartContinuation.contract` is honoured but no production caller can supply it — the constraints live on no record a run id can reach (not the log, not the checkpoint, not the episode). The flip-pin is landed and named.
**Change:** R7-1's prescribed cheapest shape: `contract?` on `FlowchartCheckpointState` (`replay.ts` + `validateCheckpoint`), written at start, read at CLI resume, threaded as the continuation already accepts. Persisted-schema change — needs explicit parent sign-off. Flip `a resume that is handed no contract assesses its children against none` **with disclosure** (a CLI resume of a contract-ful run then reports `constraint-retention: PASS` across the boundary). Do not invent a contract from the episode's acceptance criteria (R7-1's stated reason: an empty constraint set presented as the run's is the class of lie the slot removed).
**Ownership:** `src/run/replay.ts`, `src/run/flowchart-run.ts`, `src/cli/main.ts` resume path (sole — hence the contention), `test/integration/m2.5/resume.test.ts`.

### R8-3 (P2, operator surface) — BLOCKED block on `resume`/`answer`
**Evidence (R7-5 §3, reviewer-verified):** both commands still print the status only for BLOCKED; the supervised-resume stderr is byte-pinned equal to the executor-config disclosure in `loopback-cli-resume.test.ts:457-460`.
**Change:** R7-5's prescription verbatim: wire `formatBlockedRunReport` at the flowchart-resume and `answer` sites; leave the supervised branch alone **or** update the loopback byte-pin in the same diff with disclosure (it is a frozen resume-disclosure pin — sign-off needed to amend). If R8-1 runs, this waits or folds into it (same file).
**Ownership:** `src/cli/main.ts` (only if R8-1 is not scheduled), `test/integration/cli/` additive, possibly the one loopback pin with sign-off.

### R8-4 (P2, design-first) — Option (a), rescoped: criteria that can gate
**Evidence (R7-2 §4, reviewer-verified in source):** three changes, not one — (1) a child-side per-criterion outcome channel (the protocol carries one verdict per task today: a `protocol/v1` schema decision needing sign-off); (2) a gate path (`gates.ts` has no unmet-criterion code; `turn.ts` scores the uncapped `P`; the cap is display-only); (3) the never-ran-node decision R7-1's tripwire docstring assigns to `childTasksFromLog`. The reconstruction prerequisite **has landed**, so sequencing no longer blocks it — scope does.
**Change:** design + decision only, no `src/**` unless the parent signs off all three shapes at once: the protocol field, the gate mechanism (new anomaly code vs cap-to-gate — note R7-2 §7.3's separate question about `cappedByHardFail` affecting the *real* dimensions), and the never-ran-node posture. Anyone implementing must re-derive R7-2's 270-cell sweep under the new semantics and expect the R6-2 tripwire to go red by design.
**Ownership:** report + additive pins in `test/unit/tracking/` at most.

### R8-5 (P3, docs) — Truth-up for Round 7's landings
**Evidence (reviewer-verified stale at HEAD):** undocumented: reconstruction + the contract seam (R7-1 §7 wrote the paragraph), criteria-are-guidance + the display-cap fact (R7-2 §7.4 wrote the two lines; `docs/superpowers/plans/2026-08-18-phase-a-tracking-supervisor.md` mentions the dimensions), the parent-plane recorder (R7-4 §8), the BLOCKED `next:` runbook line (R7-5 §7), the empty-graph refusal, and **`docs/status-matrix.md:63` still names `loadProjectBandit`** where doctor calls `loadProjectBanditByKey` (R7-8 wrote the replacement sentence; `docs/decisions/0005…md:110` has the same staleness but is an ADR body — flag, don't edit). `src/run/supervisor.ts:586`'s comment still names the retired empty-list seed — a `src` comment, so either its owner takes it or the docs slot makes a disclosed comment-only edit under the R5-1/R6-3 precedent.
**Change:** doc-only truth-up; census the working tree for in-flight Round 8 edits rather than trusting the dispatch list (the R7-6 lesson); timestamped end-of-round sync; no ADR status line.
**Ownership:** `docs/**`; optionally the one `supervisor.ts` comment with disclosure.

### R8-6 (P3, decision) — The catalog route: producer or posture
**Evidence (R7-10, reviewer-confirmed):** no real command can select `CATALOG_OBSERVED_CORRUPT`'s routed `next:` — doctor absorbs the typed error into inventory; the route is dead code on the operator surface. The route string itself is frozen (five routes character-exact), so *dropping* it amends a frozen contract.
**Change:** parent decision first: (a) give the snapshot a command-path reader (smallest viable surface: a `models`-plane read or an `adapt` pre-check that loads the observed catalog and propagates the typed error), then land R7-10's deferred end-to-end test plus the already-reproduced bandit producer test; or (b) record the posture that the route is defense-in-depth for a future producer, in-source at `DOCTOR_ROUTED_NEXT`, with the bandit test landed alone. Either way the bandit half (reproduced, exit 1, `stage: "validation"`, exact route) is ready to pin now.
**Ownership:** (a) the chosen command file + `src/routing/catalog-observed.ts` + `test/integration/cli/command-error-doctor.test.ts`; (b) `cli/main.ts` comment + the same test file. R7-10's report notes the test's `configurePreferencePersistence(undefined)` repair in a `finally` is now unnecessary post-R7-8 — drop it while in the file.

### R8-7 (P3, decision) — Parent-plane crash residuals
**Evidence (R7-4 disclosures 3–4, reviewer-verified):** a crash over a log replaying WAITING_FOR_USER still records `RUN_FAILED` (the refusal is terminal-keyed; `recordCrashTerminal`'s in-flight rule is broader, but reusing it would falsify its rethrow clause); `RUN_CANCEL_REQUESTED` is unguarded on both planes (replay flags the post-terminal case as an anomaly).
**Change:** decisions before diffs, exactly as R7-4 framed them: either a rethrow-free variant inside `crash-terminal.ts` (its owner, with sign-off — it amends a frozen module's contract) or an owned decision that the parent plane's `waiting` flag suffices; and a separate decision on whether a cancel request is an operator fact (leave unguarded, both planes agree today) or a status claim. Neither is worth a slot alone; both fit one slot that opens `crash-terminal.ts`. Fold in the cosmetic `failureReason` scope residue at `coordinator.ts:608` (review finding 3).
**Ownership:** `src/run/crash-terminal.ts`, `src/run/coordinator.ts` (sole), `test/unit/run/` additive.

### R8-9 (P3, small census) — Disposition of root-keyed `loadProjectBandit`
**Evidence (reviewer-verified at HEAD):** after R7-8, `loadProjectBandit` has no `src` caller outside its own module; three test callers remain. R7-8 explicitly left the decision ("the natural symbol for the adaptation loop; deleting it is not this slot's call").
**Change:** R6-9's pattern: census-and-delete with an absence pin (migrating the three test callers to `loadProjectBanditByKey` + `stableProjectKey`), or an owned in-source disposition that it is the adaptation loop's intended read seam. Small slot; do not grow it.
**Ownership:** `src/learning/bandit-store.ts` (sole), `test/unit/learning/`.

### R8-10 (P3, test-only) — Re-seed the bound-episode pre-rounds settle
**Evidence (review finding 2):** R7-9's reseed moved the only pre-rounds crash pin onto a seed that fails the bind itself, so "a death after a successful bind and before rounds open settles the bound episode" (`afterTerminal === ["EPISODE_CLOSED"]` in the pre-rounds window) is exercised nowhere — the empty-list seed that used to reach it is now refused in pre-flight. A seed exists: fail the `TASK_GRAPH_ACCEPTED` append after a successful bind (the armable-id-exhaustion pattern `flowchart-run-abort.test.ts` already uses).
**Change:** one additive test in the pre-rounds section driving that seed to the guarded `RUN_FAILED` + `["EPISODE_CLOSED"]` + FAILED checkpoint shape. Test-only; if the seed proves unreachable without `src` changes, stop and report per the R5-8/R7-10 precedent.
**Ownership:** `test/integration/m2/supervisor-crash.test.ts` (additive; otherwise unowned).

*(No tenth candidate is manufactured to round out the list: R8-8 was considered — R7-2 §7.3's `cappedByHardFail`-to-gate question — but it is a semantics change to `computePrescore` inside R8-4's decision space and should be decided there, not in parallel.)*

## 5. Explicitly NOT for Round 8 (unchanged or newly settled)

Live R1/bandit/topology (standing; the keyed diagnostic read is the ceiling); jsonl/lock/append perf and per-step writer locking (saturated; decision pins are the record); protocol/v1 parse and event fuzz (saturated — but note R8-1 *must* extend the fuzz seeds for its one new event, and R8-4's channel design will name a protocol change for sign-off rather than landing one); mailbox/dead-letter/cluster work (saturated since R6-10); lock stealing (no-steal is deliberate, probed, doctor-guided); resume-time adoption of unaccepted child results (R5-5's NO stands); `maxCostUsd` enforcement (no pricing source); `TASK_LEASE_EXPIRED` rename (persisted schema, no benefit); executor-config persistence in the event log (R4-6 stands — and R7-1 proved child *specs* needed no such thing); a non-terminal `RUN_CRASHED` marker (R4-4 stands; R7-4 §7.2 re-affirmed it by refusing to invent one for the refused-crash reason); same-role late mailbox delivery (traded away in R5-6); rewriting append-only run logs for privacy (disclosure + `delete --run` is the contract); `pause --lock-wait-ms` (posture decided in R6-5); ADR-006 status changes (frozen forever); re-litigating option (b) (the record is in-source and pinned — option (a) goes through R8-4's design gate or not at all); a second unblock surface via injection (R7-3 §producer decided against — reopen the decision, not the surface).
