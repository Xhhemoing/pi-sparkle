[Model: claude-fable-5-thinking-xhigh]

# ROUND 14 BRIEF — injection context for Loop 4 · Round 15

Provenance: written by the Round 14 SOTA reviewer (claude-fable-5-thinking-xhigh) after evidence-based review of R14-1 and R14-2 at `3b39353`; verdicts and verifications in `.agent_workspace/loop4-r14-review.md` (**2 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK; zero joints; zero red-tree commit points; zero parent process nits — both Round 13 nits verified fixed**). Every claim below was verified at HEAD against the actual diff `16212ba..3b39353`, not report hearsay. All landings are committed. **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 14 (all committed)

| Slot | Landed | Key facts (do not re-implement) |
|---|---|---|
| R14-2 (`25a3c2f`) | The `replay.ts:85-93` laundering narrative scoped — **the last honesty-debt item on the seam is closed.** One coda paragraph (now `:95-101`) after the byte-identical mechanics: the chain plays out only for a node *neither* source records; a recorded node's substituted spec is restored before the resumed node runs (deliberately scoped to **substituted** specs — `withRecordedCriteria:550` leaves logged requests alone, so "record always wins" would be false); an unvouched logged-empty is detectable as unknown, not the caller's known-none. **Mechanically comment-only, re-proven by the reviewer with the sound parser-driven leaf-token walk** (`replay.ts` 2392/2392; test file 4050/4050; raw-scanner cross-check 1055/1055 matching Round 13). Ride-along: the spent `loop4-r11-t1.md` pointer at `option-a-preconditions.test.ts` replaced with the landed fact (`resume.test.ts:825`, attributed to `81f5b81` = R12-1 — reviewer verified the attribution with `git log -S`). Pinned phrases kept (`never *synthesized*` `:72`/`:106`, `not from the bound episode` `:106`); region pin re-run, matches `:63-124` | `src/run/replay.ts` +8 comment lines; `test/unit/tracking/option-a-preconditions.test.ts` comment swap; conditional pin files correctly byte-untouched |
| R14-1 (`a1ea5f2`) | Round 13 docs truth-up: both stale spots closed (the "operator gap remains open" copies recorded against `1e78220`; the three census notes replaced with a Round 14 census at 01:56:46 recording `f6e4c04`/`e7d018c`/`1e78220`/`8faf8f4` as commits). The `taskCriteria` behavioural closure recorded with the visible legacy cost stated as a cost. Five timestamped censuses; never assigned uncommitted sibling work a commit id (reviewer-grepped: no `25a3c2f` in `docs/**`); ADR-006 Proposed; `docs/decisions/**` diff-empty | `docs/status-matrix.md`, `docs/data-dictionary.md`, `docs/specs/m0-m2-architecture.md`, `docs/superpowers/plans/2026-08-18-phase-a-tracking-supervisor.md` |
| Parent | Committed R14-2 **before** R14-1 (source truth before docs, as two briefs asked); landing commits free of PROGRESS ticks (Round 12's pure model restored); freeze extras not re-dispatched. Gate GREEN recorded at `3b39353`, numbers matching the reviewer's | — |

## 2. Current baseline (this VM, Node v22.14.0, engine warning only)

- **`pnpm gate` GREEN, exit 0**: **1951 tests / 1950 pass / 0 fail / 1 skipped / 0 cancelled**. The only skip is the `PI_SMOKE=1` real-provider gate; exactly one `# SKIP` line. Delta vs Round 13: **+0**, closed structurally — the round's only test-file diff is token-identical, which cannot change a registration. Any Round 15 slot whose report shows a different total introduced the change and must say so.
- `node scripts/crash-probe.mjs` → `ok: true`, **11 cases × 3 iterations**; original ten names and order unchanged, `unblock-discard-append-before-checkpoint-sigkill` last. The name-list pin in `test/integration/persist/crash-recovery.test.ts` is diff-empty and green. Anyone changing the case set owns that pin in the same slot, with disclosure.
- Perf: no fresh bench needed — Round 14's `src` diff is token-invisible (comments only); zero I/O, lock, writer, or fs primitive changed. Any Round 15 perf claim records its own same-VM before/after with an unchanged-arm control; ≥5% end-to-end or it is not a candidate.
- Parent bookkeeping: clean and matching the reviewer's independent numbers.

## 3. Forbidden / frozen for Round 15

Global forbidden list, unchanged: live R1/bandit/topology on the execution path (doctor's `loadProjectBanditByKey` inventory read is the **only** exception — re-swept at HEAD: store + doctor only; `selectArm` definition + `shadow.ts` only; bare `loadProjectBandit` zero `src` matches); Outcome-supported claims; **ADR-006 stays Proposed** (fourteen rounds; keep it); auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit; no `git checkout` of other branches. `independentEvidence` is never read as corroboration (exactly the one `void` at HEAD; the flowchart spine pinned to zero mentions). No new `RunStatus` (exact eight-member freeze). No fourth key on `RUN_UNBLOCKED`; `RUN_UNBLOCKED_WITH_DISCARD` stays exact-keyed.

Frozen contracts (Rounds 1–13 set, all held through Round 14, plus new):
- **Rounds 1–13 carry-overs, verbatim from the Round 13 brief §3** — every one re-verified or gate-green this round, including: the whole R1–R11 block (jsonl/atomic-write/lock/delete/crash-terminal/`applyRetry`/resume-disclosure/doctor/routes/`INSPECT_SUMMARY`/BLOCKED-prefix/episode-boundary/option (a)/discard-audit/probe/verdict-producer freezes); the `taskCriteria` writer as shipped (three sources, first-write-wins, empty logged requests ignored, absence = unknown forever, reader fills only substituted specs at the sole rebuild call site, no `continuation.taskCriteria`, never synthesized, carriage + writer-existence guards, **both behavioural arms pinned**); `onRunStarted` as shipped (throws swallowed, one-way, clarification path silent); the uniform three-path early-id disclosure and its sequence pins; the exact eight-member `RunStatus` vocabulary.
- **New from Round 14:**
  - **The scoped laundering coda is part of the citable source prose** (`replay.ts:95-101`, alongside the Round 13 docstrings at `:107-113` — note the coda shifted those by +8): the hazard narrative is bounded to nodes neither source records; docs and future comments cite the scoped form, not the pre-coda reading. The counterfactual clause at `:89-91` stays as-is — twice ratified (slot §6.4, review §6) as motivation prose, true as counterfactual, bounded by the coda.
  - **Comparator soundness rule (process, binding):** a comment-only proof on any file containing template literals with substitutions must use the parser-driven leaf-token walk (JSDoc subtrees and EOF excluded) or an equivalently template-aware method — **never a bare `createScanner` `skipTrivia` loop as sole proof**. The raw scanner provably reports false differences on such files (R14-2 found it; the reviewer reproduced it: 539 vs 528 tokens on a token-identical file). The raw scanner remains a fine cross-check on template-free files.
- Saturated — do not spend a slot: everything in the Round 13 saturated list, plus: the `FlowchartCheckpointState` docstring in its entirety (every paragraph re-read against the writers this round; all six hold verbatim, the seam's prose debt is zero); the spent-pointer class in `option-a-preconditions.test.ts` (closed); the six freeze-extra files (still six-for-six declinations; not re-dispatched this round, correctly).

Process requirements per slot (carried forward, all held this round — keep them): census first, against the working tree; verify handed paths exist before writing; scoped `eslint` + whole-tree `tsc --noEmit` before reporting (docs-only slots: `git diff --check` minimum, and prefer showing your stale-claim search); census your consumers and ship or prescribe their updates in your own diff; timing-sensitive owned tests 3×; full gate is the parent's job; no scratch files at report time; mutations out-of-tree, full copy under `/tmp` with `node_modules` symlinked, then deleted. Parent: keep the R14 model — source-truth commits before docs-truth commits, landing commits free of PROGRESS ticks.

## 4. Ranked Round 15 candidates (mutually exclusive ownership)

**There are ZERO real code candidates — stated per protocol.** Round 14 closed the last two items the Round 13 brief identified, both prose. Every behavioural seam is pinned (the `taskCriteria` surface carries eleven-plus pins with both arms behavioural; the disclosure sequence is pinned on all three paths; six freeze extras hold with six consecutive correct declinations), the source prose is debt-free (the reviewer's stale-claim sweep found nothing beyond correctly-dated reports and the out-of-scope phase-B plan), and the gate is green at +0. **The loop is prose-saturated. This brief manufactures nothing.**

Exactly one conditional candidate exists, and its validity depends on it being alone:

### R15-1 (P4, docs, conditional — valid ONLY as the round's sole landing) — Terminate the census-note treadmill
**Evidence (review §5):** the standing docs race has now recurred six times: a census note describing sibling state is superseded by the sibling's landing in the same round, every round. At HEAD the three embedded notes (`status-matrix.md:14`, `m0-m2-architecture.md:14`, `data-dictionary.md:270`) describe the `replay.ts` paragraph pre-coda ("then lines 85–93") — dated, hedged, true as census statements, superseded five seconds before their own commit. The treadmill terminates only when a docs truth-up is the round's **sole** landing, and Round 15 is the first round where that condition is available (zero code candidates). The slot records Round 14's two landings as commits (`25a3c2f`, `a1ea5f2`): the scoped coda at `replay.ts:95-101`, the ride-along pointer retirement, and the docs truth-up itself; keeps the five-census pattern; ADR-006 stays Proposed; `docs/decisions/**` untouched. With no sibling in flight, the census can finally be current at HEAD — say so in the note, and say that subsequent rounds need a new census note only when a landing changes what the surfaces describe.
**Ownership:** `docs/**` only. **Condition:** if the parent dispatches ANY other landing slot in Round 15, this candidate is void — running it alongside a sibling reproduces the race it exists to end, converting the slot into treadmill busywork.
**Alternative, equally honest:** decline it. The notes are dated and hedged; no reader is misled who reads the timestamp. If the parent declines, Round 15 dispatches nothing and the loop idles until genuinely new schema arrives (a new seam, a behavioural gap surfaced by usage, a probe failure). Do not fill the round with freeze-census extras — six-for-six declinations across two rounds have proven those slots idempotent.

*(No further candidate exists or is manufactured. Everything else surfaced in Rounds 13–14 is saturated (§3), twice-ratified as stays-as-is (the `:89-91` counterfactual), or a recorded decision from the Round 13 brief's §4–5 lists: the non-empty-entry carry fixture, per-path `advanceTaskCriteria` measurement, per-path liveness pins, the `--children` routing-block position — all still correctly not-candidates for the reasons recorded there.)*

## 5. Explicitly NOT for Round 15 (unchanged or newly settled)

Everything in the Round 13 brief §5, verbatim — live R1/bandit/topology; reading `independentEvidence` as corroboration; any new `RunStatus`; a fourth `RUN_UNBLOCKED` key; re-litigating or re-pinning any part of the `taskCriteria` surface; overloading `onRunStarted`; per-path liveness/pause proofs; a third `Run <id>: <word>` line; synthesizing `contract`/`taskCriteria`; a writer that drops either field; re-litigating option (a), the discard audit, the unblock fail-closed default, the gate-ledger posture, or set-before-sums; protocol-layer criterion correlation; per-criterion `UNOBSERVED`; manufactured pauses; jsonl/lock perf; mailbox/cluster work; lock stealing; resume-time adoption; `maxCostUsd` enforcement; non-terminal `RUN_CRASHED`; rewriting append-only logs; ADR-006 status changes; P0 sign-off; dependency bumps; in-tree mutation testing — **plus, new this round:** editing the `replay.ts` docstring again in any form (the seam's prose is complete: mechanics + coda + writer paragraphs all verified true at HEAD); re-proving comment-only identity of Round 14's files (done twice, two methods); bare-`createScanner` comment-only proofs on template-bearing files (§3, binding); re-dispatching the six freeze-extra censuses absent a diff touching their subject; and any Round 15 docs slot running concurrently with another landing (the §4 condition — the treadmill only ends solo).
