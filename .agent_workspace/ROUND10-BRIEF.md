# ROUND 10 BRIEF — injection context for Loop 4 · Round 11

Provenance: written by the Round 10 SOTA reviewer (claude-fable-5-thinking-xhigh) after evidence-based review of R10-1…R10-10 at `6c60ba6`; verdicts and verifications in `.agent_workspace/loop4-r10-review.md` (10 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK; zero parent fix-joints; nothing left red). Every candidate below cites evidence the reviewer verified at HEAD against the actual diff `8e933a7..6c60ba6`, not report hearsay. All ten slots are committed (`d4741e6`…`66edccb`); parent commits touch only `.agent_workspace/**`. **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 10 (all committed)

| Slot | Landed | Key symbols (do not re-implement) |
|---|---|---|
| R10-1 | **The executed-descendant discard shipped, as R9-3 designed and the parent signed off.** Distinct exact-keyed `RUN_UNBLOCKED_WITH_DISCARD` — never a fourth `RUN_UNBLOCKED` key (still `{ blockedEventId, reason, retryNodeId? }`, refusing `rewoundDescendants` by exact message), never a two-event sequence. Payload: required `retryNodeId` + non-empty `rewoundDescendants[]`, each entry `{ nodeId, taskId, previousState, modelRouteEventIds, childRunIds, chargedEstimatedCostUsd, chargedEstimatedDurationMs }`; strictly-ascending `nodeId` (uniqueness in the same comparison); retry target excluded; ≥1 executed prior state; `READY`/`SKIPPED` entries must carry no references and zero charges (absence-is-not-zero telemetry honesty stated in-source). CLI `unblock --discard-executed` (boolean; parse-refused without `--retry-node`); set **computed** under the lifecycle lock by `reopenAfterUnblockWithDiscard` (a separate plan-returning transform — the ordinary reopen's guard is byte-untouched); refused on a targetless stall and when nothing executed, both by exact message, both proven to write nothing. Charged estimates re-derived from the cited `MODEL_ROUTED` rows **before the append** (`assertDiscardAuditMatchesLog`: missing row / wrong task / sum mismatch / uncited child run all refuse). Transform → one append → checkpoint; `unappliedUnblock` returns a `ClearingEvent` union and every restore funnels through one `applyClearingEvent`, which for the stronger event **recomputes the consequence set and fails closed** on a hand-edited list (exact message, behaviourally pinned). Evidence/events/budgets survive; `success`/`confidence` and active routes clear; a `WAITING_FOR_USER` rewind releases its approval; no refund. The discard-written, resumed, and crash-recovered checkpoints all carry `contract`. Replay/gate treat both clearing events uniformly (anomalies name `${event.type}`; a discard-unblocked run re-blocks `from: "RUNNING"` and the second block needs its own authorization — per-block permission, not a mode). Four ordinary BLOCKED lines byte-preserved and now prefix-pinned; the disclosure is an appended unconditional `note:` (self-conditioning wording; `formatBlockedRunReport` has no checkpoint — reviewer evaluated and accepted the judgement); the CLI catches the exact ordinary-refusal message and appends a `next:` naming the flag, message untouched | the event + validator in `run/events.ts`; `reopenAfterUnblockWithDiscard`/`reopenBlockedFlowchartSnapshotWithDiscard` in `supervisor/flowchart-supervisor.ts`; `discardAuthorization`/`applyClearingEvent`/`assertDiscardAuditMatchesLog`/`chargedAttempts` in `run/flowchart-run.ts`; the flag + `unblockRun` routing + `discardedDescendants` in `cli/main.ts`; one condition in `gate-apply.ts`; tests across `event-row-fuzz` (type freeze both levels + all refusals), `replay.test.ts` (matched/stale/doubled/post-terminal/interleaved), `unblock-flow` (5 e2e), `cli/unblock`, `blocked-next`, `flowchart-supervisor.test.ts` |
| R10-2 | Option (a) frozen unimplemented: `VerificationResult` has no `criteria` (compile-level `keyof` check + source regex on the declared interface); R8-4 pins 1/3/4/5 meta-pinned by exact title (pin 2 correctly unprotected — already through its R9-2 replacement). **These pins are designed to be replaced in the option-(a) diff itself** | the meta-pin in `option-a-preconditions.test.ts`; the criteria pin in `test/unit/protocol/v1.test.ts` |
| R10-3 | Round 9 docs truth-up: ten-case probe line fixed, `persist/` pin path on all three surfaces, R9 decisions recorded (durable contract, verdict producer + measured reachability, `runStatus` ledger, discard decision), discard described accurately against the *shipped* payload. The 23:31 UTC "not yet a HEAD commit" note for R10-1/8/9 was superseded by the parent's 23:32 commits — honest dated census, Round 11 docs slot records them as landed | `docs/status-matrix.md`, `docs/data-dictionary.md`, `docs/specs/m0-m2-architecture.md`, the tracking plan |
| R10-4 | **PARTIAL, correctly stopped.** Landed: the structural writer-carriage pin — an AST census of every `materializeCheckpoint` call with a flowchart payload across all of `src` (initializer-resolving, vacuity-guarded) requiring `contract` on each; a **property, not a count** — R10-1's writer is included automatically (there are exactly two call sites at HEAD; the discard reuses the unblock writer's). STOPPED: the pure-CLI `--track`→pause→resume arc is unreachable — `startTrackedRun` passes no `pause` dep (`track/loop.ts:157-175`), `pauseIfRequested` no-ops without one (`flowchart-run.ts:849`), so an offline tracked run is COMPLETED before `pause` can mean anything (probe: `error: cannot pause a COMPLETED run`). Source-verified by the reviewer; candidate 3 below is the fix | the census pin in `test/integration/m2.5/resume.test.ts` (16 tests) |
| R10-5 | `independentEvidence` posture recorded, **not renamed** (parent-signed): comment-only src (+23/−0, mechanically verified) at the declaration, the write, and the load-bearing `void`; the `{@link GateApplyResult}` pointer above `applyChildThreeLine`. Pin file: whole-`src` AST *dereference* census `deepEqual` to exactly the one `void` (a first read anywhere fails — the option-(a) implementer inherits this deliberately), rewrap-safe prose pins proven on the real sources, producer/consumer inertness (144-cell) honestly scoped to "today" | `test/unit/tracking/independent-evidence-posture.test.ts` (5) |
| R10-6 | Verdict-producer standing rules frozen (producer untouched): adversarial identity (model-supplied `from`/`runId`/`taskId` cannot displace the lease), explicitly-empty FAILED `evidenceIds` refused emitting nothing, repeated *identical* verdict refused (one call per attempt), and an AST pin that the tool is a direct `tools`-array element — an in-memory opt-in-flag mutant proves the pin bites. Opt-in gating remains a `runtime.ts`/`main.ts` parent decision, now mechanically loud | 4 new tests in `report-task-result.test.ts` (17) |
| R10-7 | Episode/contract authority boundary frozen: `openEpisode` projection pinned to metadata + `acceptance: contract.acceptanceCriteria`; `ProjectEpisode` pinned to have no `contract`/`constraints`/`acceptanceCriteria` field; a 215-module census (which names its own found readers — anti-vacuous) rejects any episode reader constructing contract-shaped output, with a `constraints: []` mutant proof; behavioural pin on the persisted episode | `test/unit/run/episode-contract-boundary.test.ts` (4) |
| R10-8 | R8-3 `applyRetry` absence pin proven live over the discard implementation: the new assertion **requires** `/discard|reopenAfterUnblock/i` markers in both flowchart sources (fails at base — genuinely written against landed R10-1) then reapplies the unchanged whole-file `assertNoSchedulerRetry` | the third test in `flowchart-applyretry-absence.test.ts` |
| R10-9 | Gate ledger posture extended to the new event: a real `RUN_UNBLOCKED_WITH_DISCARD` through the actual gate apply — matched clears the next transition to `from: "RUNNING"`, unmatched stays `from: "BLOCKED"`; observes only the transition record; `runStatus`/`applied`/`transitionId` still have zero readers | the ninth test in `gate-status-posture.test.ts` |
| R10-10 | `TERMINAL_REPLAY_STATUSES` frozen at runtime (exactly COMPLETED/FAILED/BLOCKED) and the `RUN_UNBLOCKED*` event family — **discovered from `EVENT_TYPES`, not enumerated** — pinned outside `RunStatus` and the terminal set, plus a type-level `Extract` disjointness that a future overlapping literal fails at `tsc` | `test/unit/run/terminal-replay-statuses-freeze.test.ts` (2) |

## 2. Current baseline (this VM, Node v22.14.0)

- **`pnpm gate` GREEN, exit 0**: **1915 tests / 1914 pass / 0 fail / 1 skipped**. The only skip is the `PI_SMOKE=1` real-provider gate. Any slot whose report shows a second skip introduced one and must say so. Delta vs Round 9: +35, accounting closed per-slot in the review §2.
- `node scripts/crash-probe.mjs` → `ok: true`, **10 cases × 3 iterations**; names and order unchanged from Round 9. The name-list pin lives in `test/integration/persist/crash-recovery.test.ts` (**not** `test/integration/m2/` — that file does not exist; the directory does). Anyone changing the case set owns updating that pin in the same slot, with disclosure — candidate 2 below is exactly that shape.
- Perf: no fresh whole-tree bench needed. Round 10's only hot-path deltas: replay/gate each gained one case-label disjunct per event scan; the entire discard path runs only inside the operator `unblock` command; no per-step writer changed, no lock added, no fs primitive added (verified — zero raw fs writes in the round's src diff). Any Round 11 perf claim records its **own** same-VM before/after with an unchanged-arm control; ≥5% end-to-end or it is not a candidate.

## 3. Forbidden / frozen for Round 11

Global forbidden list, unchanged: live R1/bandit/topology on the execution path (doctor's `loadProjectBanditByKey` inventory read is the **only** exception — re-swept at HEAD: store + doctor only; `selectArm` definition + `shadow.ts` only; bare `loadProjectBandit` zero `src` matches); Outcome-supported claims ("Nothing in this repo is Outcome-supported" retained); **ADR-006 stays Proposed** (ten rounds; keep it); auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit; no `git checkout` of other branches.

Frozen contracts (Rounds 1–9 set, all held through Round 10, plus new):
- Rounds 1–9 carry-overs, verbatim from the Round 9 brief §3: jsonl signatures; `writeFileAtomic`(+Sync), no private temp+rename (Round 10's whole src diff added zero raw fs primitives — verified); `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE`; `episodeLockPath` symmetry; `runCommand` sink wiring; `deleteRunRecords` lock-plus-double-verify; append/checkpoint take no run lock; `recordCrashTerminal` in-flight-only + caller always rethrows; `applyRetry` sole scheduler BLOCKED→READY producer + R8-3 AST pin (now marker-gated over the discard sources by R10-8); `preserveResumableState`; `ClusterMailReport`; resume flags + disclosure wordings + the supervised loopback stderr byte-pin (file diff-empty again this round); `writeFeedbackTombstones`; adaptation error codes + bind-after-load; doctor frozen-additive JSON incl. `learnedState`; feedback fuzz hard-fails; `withRunLifecycleLock` rules; supervised settle-on-crash + rounds-window pins; `commandFailureNext` code-discrimination; `CheckpointStore.read` typed error; one-definition-of-terminal (`TERMINAL_REPLAY_STATUSES` = COMPLETED/FAILED/BLOCKED — now runtime- and type-frozen by R10-10); five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT` character-exact (reviewer re-extracted base-vs-HEAD through R10-1's `main.ts` edit: identical); `--lock-wait-ms`; `process-death.ts`; R6-9 absence pins; reconstruction `childTasksFromLog` + FAIL-unreachable tripwire; criteria-are-guidance + 270-cell sweep; parent-plane `recordTerminal`; empty-graph pre-flight; `settleSupervisedOutcome` absence (re-swept: zero); the Round 8 block (`RUN_UNBLOCKED` three-key semantics + type freeze; four-line BLOCKED block; `INSPECT_SUMMARY` additive freeze; catalog posture (b); parent-plane residual decisions); the Round 9 block (durable-contract semantics — never synthesized, not from the episode (R10-7 census), not as `{ constraints: [] }`; precedence `continuation.contract ?? checkpoint.flowchart.contract`; verdict-producer rules — now six pins deep after R10-6; gate `runStatus` ledger, zero `src` readers on both planes; the ten-case probe; the ordinary executed-descendant refusal **exact message** — byte-identical at HEAD, and now also the CLI's routing trigger, so changing it breaks two things deliberately).
- **New from Round 10:**
  - **The `RUN_UNBLOCKED_WITH_DISCARD` schema is exact-keyed and frozen as shipped**: required `retryNodeId`; non-empty, strictly-nodeId-ascending, retry-target-excluding `rewoundDescendants` with ≥1 executed prior state; `READY`/`SKIPPED` entries reference-free and zero-charged; charged estimates are sums over the cited `MODEL_ROUTED` rows only — **never invocation telemetry, never invented zeros** (absence-is-not-zero). Type-frozen bidirectionally at both the envelope and the entry; runtime-refused key-exactly at both levels.
  - **Both clearing events answer to identical replay/gate matching rules** (stale/unmatched/post-terminal clear nothing; anomalies name the event type; `currentGateStatus` counts both). The stronger event authorizes a wider *transform*, never a wider replay. Neither is a status.
  - **The single-append discipline**: one operator act, one event, one crash window; restore recomputes the consequence set from durables and fails closed on mismatch. The discard authorization is **per-block permission**, not a run mode.
  - **Every flowchart-payload `materializeCheckpoint` call carries `contract`** — now a structural property pin (R10-4), not a census count; a new writer that drops the field fails loudly by location.
  - **The verdict tool is unconditionally surfaced** (AST-pinned with a mutant); identity comes from the lease even against adversarial arguments; empty-array FAILED evidence refuses; one verdict per attempt even for identical repeats.
  - **`independentEvidence` is a self-report posture, recorded not renamed**; the whole-`src` dereference census fails on the first reader. Giving it a reader (or renaming it) is a parent decision; the option-(a) implementer inherits the pin deliberately.
  - **The episode boundary census**: no episode reader constructs `contract`/`constraints`/`acceptanceCriteria` or references `RequirementContract`.
  - **The BLOCKED report's ordinary four lines are a pinned prefix**; the discard disclosure is a `note:` (not a fifth `next:`) and unconditional-by-design until `formatBlockedRunReport` is given the checkpoint.
- Saturated — do not spend a slot: everything in the Round 9 saturated list, plus: freeze/pin work on the discard surface (held at the validator, the type level, the fuzz suite, replay, the gate ledger, the supervisor transform, and the scheduler-absence pin — re-pinning it is busywork); the `TERMINAL_REPLAY_STATUSES` freeze (two levels); the verdict-producer freeze (six pins); the episode boundary; the `independentEvidence` posture; the writer-carriage property; the Round 9 docs truth-up (done; Round 10's own staleness is candidate 5).

Process requirements per slot (carried forward — Round 10 executed them cleanly): census first, against the working tree; scoped `eslint` + whole-tree `tsc --noEmit` before reporting; census your consumers and ship or prescribe their updates in your own diff; **verify that a path the brief hands you actually exists before writing to it**; run `live-isolation.test.ts` if you add an import inside the live closure; attribute shared-tree transients to files with timestamps (R10-5 §5 and R10-9's two-census pattern are this round's models); timing-sensitive owned tests 3×; full gate is the parent's job; no scratch files at report time; a "did not land" claim is only true as of the minute taken.

## 4. Ranked Round 11 candidates (mutually exclusive ownership)

The round's tilt: Round 10 closed the operator dead end — a production pi run can block on a real FAILED verdict and every block shape now has a sanctioned exit. What remains is exactly what the parent already knows about: option (a) is the last designed-and-signed-off-shaped implementation, twice deferred on scheduling alone and now **uncontended**; everything else is consolidation of the discard (its probe case, its audit-integrity residual) and the one STOP Round 10 hit (tracked pause). **There are five real candidates, not ten — stated per protocol; the parent fills freeze extras only against *new* Round 11 schema, because §3's surface is freeze-saturated.**

File-contention map (no overlaps by construction): `src/protocol/v1.ts` + `src/tracking/**` + `src/pi-adapter/pi-executor.ts` + `src/run/replay.ts` + their unit tests to **R11-1 alone**; `scripts/crash-probe.mjs` + `test/integration/persist/crash-recovery.test.ts` to **R11-2**; `src/track/loop.ts` + `src/cli/main.ts` + `test/integration/m2.5/**` to **R11-3**; `src/run/flowchart-run.ts` + `test/integration/run/unblock-flow.test.ts` + `test/unit/run/replay.test.ts` to **R11-4**; `docs/**` to **R11-5**. Note R11-1 and R11-4 both produce `replay`-adjacent work: R11-1 owns `src/run/replay.ts` (TASK_RESULT-side anomaly plumbing) and R11-4 owns `src/run/flowchart-run.ts` (restore-side validation); if the parent schedules both, confirm at sign-off that option (a)'s diff really needs `replay.ts` and R11-4's really does not — the reviewer's reading of both designs says this split holds.

### R11-1 (P1, the round's spine — parent sign-off required) — Implement option (a)
**Evidence (R8-4 §9's ship order, every precondition verified landed; deferred at R9 and R10 on scheduling):** producer (R9-2, freeze-hardened by R10-6), checkpoint seam (R9-1, reserved in prose and pinned unimplemented by R9-1 + R10-2), unblock for the resulting blocks (R8-1 + R10-1 — both strengths now exist). The one-diff shape stands as designed: optional nested `VerificationResult.criteria` (additive-compatible), a new `unmet-acceptance-criterion` anomaly fed by an explicit producer fact on **observed FAILED only** (never-ran nodes stay unknown-not-unmet), durable per-task criteria riding R9-1's reserved checkpoint seam, and the per-criterion reporting surface on `sparkle_report_task_result`.
**Obligations accumulated across three rounds — replace, in the same diff:** R8-4 pins 1/3/4/5 **and** R10-2's meta-pin (it freezes their exact titles) **and** R10-2's two `VerificationResult.criteria` pins (type + source) **and** R9-1's reserved-unimplemented schema assertion. Inherit deliberately: R10-5's `independentEvidence` dereference census (fails on the first read — if the design gives the flag a per-criterion meaning, that is the decision point, parent's call); R10-6's producer freezes (per-criterion reporting must not weaken lease identity, evidence-cited FAILED, or one-verdict-per-attempt); the 270-cell criteria-are-guidance sweep (its replacement rules were written in R8-4). New event/payload shapes ride `event-row-fuzz` seeds as R10-1's did.
**Ownership:** `src/protocol/v1.ts`, `src/tracking/**`, `src/pi-adapter/pi-executor.ts`, `src/run/replay.ts` (all sole); `test/unit/protocol/v1.test.ts`, `test/unit/tracking/**` (incl. `option-a-preconditions.test.ts`, `independent-evidence-posture.test.ts` if the design touches the flag), `test/unit/pi-adapter/**`, `test/unit/run/event-row-fuzz.test.ts`.

### R11-2 (P2, probe — producer landed, sequencing constraint gone) — The discard crash window joins the standing probe
**Evidence (R9-5's technique; R10-1's landed producer; review §6.2):** `unblock-flow` proves the append-durable/checkpoint-lost recovery **in-process**; the standing probe still has no external-SIGKILL, real-process case for `RUN_UNBLOCKED_WITH_DISCARD`. The R9-5 recipe transfers directly: real unmodified `unblockFlowchartRun` with `discardExecuted: true` in the child, a 16 MiB reason widening the producer's post-append read, parent observes the complete newline-terminated row, external SIGKILL, assert signal-death + exactly one decoded row + byte-identical BLOCKED checkpoint + no temp file, then stale-lock removal + real resume applying the discard exactly once (the recompute-matches path) to COMPLETED with zero anomalies. One real difference from the tenth case: the child setup needs a **gate-blocked run with an executed descendant** (the `blockedWithExecutedDescendant` shape from `unblock-flow`), not a stall block — budget the setup accordingly. Owns the ten→eleven name-list pin update with disclosure; original ten names and order unchanged.
**Ownership:** `scripts/crash-probe.mjs`, `test/integration/persist/crash-recovery.test.ts` (name-list pin only).

### R11-3 (P2, src — parent sign-off required) — Tracked-run pause controller, closing R10-4's STOP
**Evidence (R10-4's probe, reviewer re-verified in source at HEAD):** `startTrackedRun` passes no `pause` dep into `startFlowchartRun` (`src/track/loop.ts:157-175`), `pauseIfRequested` no-ops without one, so the pure-CLI `run --track` → `pause` → `resume` contract-retention arc — the last non-embedder leg, and the arc R10-4 was dispatched to prove — is unreachable today. The fix is small and uncontended: `TrackRunInput` gains an optional `pause: PauseController`, `startTrackedRun` forwards it, `runCommand` supplies `createFilePauseController(stateRoot)` (the same controller every other command path builds). Then the e2e: offline `--executor fake` tracked run (R10-4 proved it extracts and persists `c-smallest`/`c-tests` with no live model), pause, resume, `constraint-retention: PASS` off the log, contract round-trip asserted.
**The sign-off must decide the determinism seam:** the pause token is polled at round boundaries and keyed by a run id the pure CLI prints only at the end, so the test cannot pre-seed it and must not race it. Options for the slot to put in front of the parent: poll the state root for the run directory and pause mid-run against a flowchart with enough rounds (racy — against house discipline), or give the tracked path the same deterministic hook the untracked flip test used at the API boundary with the CLI doing the driving. Do not manufacture a pause by killing the process. If the seam cannot be made deterministic without disproportionate src surface, stop and report per the R5-8/R7-10/R10-4 precedent.
**Fold (optional, same owner — `cli/main.ts`):** R10-1 disclosure 7's one-line narrowing — give `formatBlockedRunReport` checkpoint access so the discard `note:` appears only when a descendant actually executed; the prefix pin already protects the ordinary four during that change. Only if the parent wants it; the unconditional wording is honest as-is.
**Ownership:** `src/track/loop.ts`, `src/cli/main.ts` (sole); `test/integration/m2.5/resume.test.ts`, `test/integration/m2.5/cli-contract-honesty.test.ts`, `test/integration/cli/blocked-next.test.ts` (only if the fold runs).

### R11-4 (P3, src, small — parent sign-off required) — Discard audit-integrity hardening
**Evidence (review §6.4, both halves reviewer-verified in source):** (a) `assertDiscardAuditMatchesLog`'s three refusal arms (missing row / wrong task / sum mismatch) are module-private and unreachable through any public path — the producer derives the sums from the same rows it validates, as the function's own docstring discloses — so the fail-closed behaviour the parent ordered is drift insurance with zero test coverage. (b) Restore's recompute compares only `nodeId:taskId:previousState`, so a schema-valid hand-edited row citing real routes with **inflated charge sums** replays cleanly: the state transition stays correct, but the durable audit record lies about money — the exact mismatch class the in-payload decision accepted in exchange for self-containedness, caught at the producer and nowhere else. The fix is small and already has its inputs in scope: `restoreCheckpointedSupervisor` holds `input.events`, so `applyClearingEvent` (or its caller) can run the same `assertDiscardAuditMatchesLog` over the recorded payload at restore; that also gives the three refusal arms their first honest consumers and test path (hand-edited log + resume, the `unblock-flow` fail-closed pattern). Alternative shape if the parent prefers no restore-path change: export the tripwire for direct unit test and record the restore gap as accepted. Either way it is a decision, not a drive-by.
**Ownership:** `src/run/flowchart-run.ts` (sole); `test/integration/run/unblock-flow.test.ts`, `test/unit/run/replay.test.ts` (additive).

### R11-5 (P3, docs) — Round 10 truth-up
**Evidence (reviewer-verified at HEAD, review §4.5/§6.5):** the 23:31 UTC sync note's "the `RUN_UNBLOCKED_WITH_DISCARD` implementation … not yet a HEAD commit" is superseded (`54cf5e5`/`2399346`/`d4b52b1` landed 23:32); undocumented Round 10 facts: the discard as shipped (the docs describe it accurately but from the observed tree — re-anchor to commits), the writer-carriage property pin, the verdict-producer freeze additions, the episode boundary census, the `independentEvidence` posture, and — if any of R11-1…R11-4 land first — their outcomes. `docs/decisions/0005…md:110` region stays flag-only; no ADR status line; ADR-006 stays Proposed.
**Ownership:** `docs/**` only; timestamp-census the working tree (R10-3's pattern, including its sharpened directory-vs-file correction discipline).

*(No further candidate is manufactured: there are **five** real candidates. Everything else surfaced by Round 10 is either saturated (§3), folded above (the disclosure narrowing into R11-3, the `independentEvidence` decision point into R11-1), or a recorded decision whose reopening needs new evidence — the always-on verdict tool (now AST-pinned), the in-payload charged-estimates decision (made at R10-1's sign-off; R11-4 hardens its checking, not the decision), the unconditional disclosure wording (honest as shipped), and the do-not-rename posture on `independentEvidence` (parent-signed). If fewer slots run, the parent fills freeze extras per protocol — against new Round 11 schema only.)*

## 5. Explicitly NOT for Round 11 (unchanged or newly settled)

Live R1/bandit/topology (standing; the keyed diagnostic read is the ceiling, census-pinned across `src`); a fourth key on `RUN_UNBLOCKED` (type-frozen, runtime-frozen, design-rejected, and now the shipped alternative exists); widening ordinary `unblock` to rewind executed work (the refusal is byte-pinned and is the CLI's routing trigger — `--discard-executed` is the only sanctioned path); making the discard a run mode rather than per-block permission (pinned by the re-block test); refunding budgets on discard, or folding invocation telemetry into charged estimates (absence-is-not-zero, stated in the payload docstring and the data dictionary); an operator-supplied rewind list (computed-set rule is in the flag's docstring and the validator's ordering rules); two-append authorization shapes (single-append discipline reasoned in-source); implementing per-criterion anything outside option (a)'s one signed-off diff (R10-2's pins make a drive-by fail at `tsc`); giving gate `runStatus`/`applied`/`transitionId` a consumer without its own justification (posture pinned; R10-9 extended it to the new event); reading `independentEvidence` as corroboration or renaming it as a drive-by (whole-`src` dereference census); synthesizing a contract from the episode or anywhere else (R10-7's census + the standing behavioural pins); opt-in gating of the verdict tool as a drive-by (now AST-pinned with a mutant; it is a `runtime.ts`/`main.ts` parent decision); a new flowchart checkpoint writer that drops `contract` (R10-4's property pin fails by location); jsonl/lock/append perf and per-step writer locking (saturated); mailbox/dead-letter/cluster work; lock stealing; resume-time adoption of unaccepted child results (R5-5's NO stands); `maxCostUsd` enforcement; a non-terminal `RUN_CRASHED` marker; rewriting append-only run logs for privacy; ADR-006 status changes (frozen forever); re-litigating option (b), the catalog posture, the parent-plane residual decisions, the unblock's fail-closed default, the gate-ledger posture, or the R10-1 payload decisions (each recorded and pinned); P0 privacy sign-off (human); `package.json`/dependency bumps.
