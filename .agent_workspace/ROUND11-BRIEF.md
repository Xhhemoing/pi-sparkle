[Model: claude-fable-5-thinking-xhigh]

# ROUND 11 BRIEF — injection context for Loop 4 · Round 12

Provenance: written by the Round 11 SOTA reviewer (claude-fable-5-thinking-xhigh) after evidence-based review of R11-1…R11-10 at `2767321`; verdicts and verifications in `.agent_workspace/loop4-r11-review.md` (10 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK; **two parent joints and one red-tree commit point — counted, explained, procedural fix in §3**). Every candidate below cites evidence the reviewer verified at HEAD against the actual diff `6c60ba6..2767321`, not report hearsay. All ten slots plus the joints are committed. **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 11 (all committed)

| Slot | Landed | Key symbols (do not re-implement) |
|---|---|---|
| R11-1 (`6096da6`) | **Option (a) shipped as R8-4 §4 designed — acceptance criteria can now gate a run.** Optional nested `VerificationResult.criteria` (`CriterionVerification { id, kind, evidenceIds }`): non-empty when present, unique ids, FAILED cites ≥1 evidence id, no `PROTOCOL_VERSION` bump (additive-compatible, asserted by round-trip). New hard `unmet-acceptance-criterion` in `AnomalyCode`/`ANOMALY_CODES`, driven by **required** `GateInput.criterionUnmet` — supplied by the caller from `unmetCriteriaOf(observation)` (the child's *reported* FAILED criteria), never derived from a score or dimension; ordered after `claimed-verification-without-checks`, before `repeated-no-progress`; a reported FAILED criterion blocks **all seven roles** even when the whole-task verdict is PASSED. Never-ran stays unknown-not-unmet by construction (gate reads reported outcomes only; UNOBSERVED and absence both leave it open). `coverageOutcome` body **byte-identical** (reviewer-diffed); the 270-cell sweep intact with its framing truthed up and the record pin strengthened (source must name where the gate went). Durable `taskCriteria?: TaskAcceptanceCriteria[]` on the R9-1 checkpoint seam: validated fail-closed (non-empty when present, ascending `taskId`, entry may carry an empty list = known-none, absence = unknown forever), never synthesized — **and declared-not-written: zero `src` writers at HEAD (reviewer-confirmed), candidate 1 below.** Per-criterion reporting on `sparkle_report_task_result` (`criterionList` refuses whole, not trims; empty array refused — omit to say nothing; FAILED criterion must cite evidence; omitted parameter emits a byte-identical verification object). Ids deliberately not correlated against the request at protocol/tool layer — the tracking layer ignores an unasked id. `derivedClaimedVerificationWithoutChecks` deliberately unchanged (untrusted-prose reasoning in-source). Nine pins replaced in place: R8-4 pins 1/4/5 strengthened, pin 3 kept (count unchanged), R10-2 meta-pin re-pointed at the four landed titles, R10-2's two `criteria` pins flipped positive, R9-1's reserved assertion → new 7th pin + the parent joint | `CriterionVerification`/`isVerificationResult` in `protocol/v1.ts`; `unmetCriteriaOf`/`CriterionObservation` in `tracking/from-child.ts`; `criterionUnmet` in `tracking/gates.ts` + `turn.ts`; `taskCriteria`/`TaskAcceptanceCriteria`/`validateTaskCriteria` in `run/replay.ts`; `criterionList` in `pi-adapter/pi-executor.ts`; pins in `option-a-preconditions.test.ts` (7), `protocol/v1.test.ts` (20), `report-task-result.test.ts` (20), `event-row-fuzz.test.ts` (9), `tracking/gates.test.ts` (8) |
| R11-2 (`db38b21`) | Eleventh standing probe case `unblock-discard-append-before-checkpoint-sigkill`: real gate-blocked run with an executed descendant (scout COMPLETED / analysis FAILED / any-join summary COMPLETED), child process runs **unmodified** `unblockFlowchartRun` with `discardExecuted: true`, 16 MiB reason widens the post-append read, parent observes the complete row and sends external SIGKILL; asserts signal-death, exactly one decoded `RUN_UNBLOCKED_WITH_DISCARD` row, byte-identical BLOCKED checkpoint, no temp, lock names the reaped PID; stale-lock removal → real resume takes the recompute-matches path (which at HEAD also runs the R11-4 charge audit), re-executes each rewound node exactly once, COMPLETED, one authorization, zero anomalies. Ten original names/order unchanged; pin disclosure in `test/integration/persist/crash-recovery.test.ts` | the case + the `discardExecuted` spread in `scripts/crash-probe.mjs`; the eleven-name pin |
| R11-3 (`ac3faa3`) | Tracked-run pause controller wired (+20/−1): `TrackRunInput.pause?: PauseController`, `startTrackedRun` forwards it, `runCommand` supplies `createFilePauseController(stateRoot)`. Control pin: same run, no controller → COMPLETED (5 children); with controller → PAUSED after one child — fails behaviourally if forwarding drops. The arc: tracked run + pre-existing `TogglePause` at the `startTrackedRun` boundary → PAUSED; CLI `pause` → contract byte-equal through the checkpoint rewrite; CLI `resume --unpause --executor fake` → COMPLETED; every assessed turn `constraint-retention: PASS` with resumed turns identified by name; pure-CLI `run --track` proof that the extracted contract is the one children are assessed against, with a `--children` `NOT_APPLICABLE` discriminator. **Disclosed PARTIAL, source-justified (reviewer re-verified all three facts):** a pure-CLI tracked run is always terminal in one process (`approvalRequired: false` on every compiled node; track path always gets a passing fake executor; the clarification checkpoint has no flowchart payload), and the run id prints only at the end — so the pure-CLI *mid-run pause request* leg is a call-site source pin, honestly labelled. Five determinism options designed; races and process-kills declined per sign-off; early run-id disclosure declined **only because it needed `flowchart-run.ts` (R11-4's file)** — candidate 2 below. Fold (checkpoint into `formatBlockedRunReport`) declined per instruction | the pause field + forward in `track/loop.ts`; the controller line in `cli/main.ts`; tests in `m2.5/resume.test.ts` (19), `m2.5/cli-contract-honesty.test.ts` (2) |
| R11-4 (`9663294`) | Restore-side discard audit: `applyClearingEvent` takes the log and its `RUN_UNBLOCKED_WITH_DISCARD` arm runs `assertDiscardAuditMatchesLog` **after** the consequence-set recompute (set before sums — R10-1's wrong-set message stays dominant, pinned); sole caller forwards `input.events`. A schema-valid hand-edited authorization naming the right set but inflating charges no longer resumes; the three previously-unreachable refusal arms (+ the fourth) each have a public path and a control-first test with exact messages (reviewer character-matched). Log growth cannot change the verdict (reads only cited rows). **Disclosed limit, recorded not reopened:** a subset-citing internally-consistent authorization passes — completeness is a producer concern (`chargedAttempts` takes every row) and demanding it would refuse the re-block shape. Replay deliberately records **no** anomaly for unsupported charges (anomalies are stepped over; this class must stop the resume) — pinned with a source-boundary check. Did not edit `replay.ts` | the third parameter + audit call in `run/flowchart-run.ts`; 5 refusal/control tests in `unblock-flow.test.ts` (18); the boundary pin in `replay.test.ts` (24); R11-8's AST guard holding the call in place |
| R11-5 (`9efc715`) | Round 10 docs truth-up: landings anchored to real commits, eleven-case probe with the `persist/` pin path, R10-4's STOP, R10-5/6/7 postures recorded; five timestamped censuses that never assigned an uncommitted sibling a commit id. **Docs-race cost at HEAD (not a slot failure): four claims went false when R11-1/R11-3 landed 10–16 min later — candidate 4 owns them** | `docs/status-matrix.md`, `docs/data-dictionary.md`, `docs/specs/m0-m2-architecture.md`, the tracking plan |
| R11-6 (report-only) | Live-isolation census: R11-1's only src import is type-only `AcceptanceCriterion` in `replay.ts` (erased at runtime); the existing 9-test transitive-closure + 215-module census already covers it; **no change made — file byte-identical to Round 10 close (reviewer: 0-line diff)**. The saturation discipline working | `test/unit/routing/live-isolation.test.ts`, untouched |
| R11-7 (`3bbb8dc`) | `INSPECT_SUMMARY` frozen against a rich-child fixture (whole-object `deepEqual`, exactly four keys); BLOCKED four-line prefix pinned **directly at the formatter** with an exactly-three-`next:` ceiling, discard stays `note:`, reorder and `note:`→`next:` mutants proven to fail | additive tests in `inspection.test.ts` (14), `blocked-next.test.ts` (12) |
| R11-8 (`39c97c3`) | AST guard: `assertDiscardAuditMatchesLog` must be called from `applyClearingEvent` or `restoreCheckpointedSupervisor`, then the unchanged whole-file `assertNoSchedulerRetry` reapplies — failed at base before R11-4 landed (non-vacuous), landed 3 s after it (correct sequencing) | the fourth test in `flowchart-applyretry-absence.test.ts` |
| R11-9 (`330466a`) | Option (a) cannot add a fourth terminal `RunStatus`: discovers the non-terminal vocabulary from `RUN_STATUSES` (census non-vacuity asserted) and holds every status outside the signed-off trio out of `TERMINAL_REPLAY_STATUSES` — the discovered-not-enumerated complement to R10-10, landed before R11-1 so it guarded the round in flight | the third test in `terminal-replay-statuses-freeze.test.ts` |
| R11-10 (`f99a0c8`) | Never-synthesize census extended to the landed field: `taskCriteria`'s exact spelling joins the forbidden set alongside a semantic per-task/child-criteria rule; `ProjectEpisode` pinned to acquire no such sibling; mutation proof (`episode.acceptance` → `taskCriteria[].acceptanceCriteria`) rejected by the whole-source census. Helper deletions verified name-preserving (`ORIGINAL_RUN_AUTHORITY_FIELDS` keeps all three originals) | `episode-contract-boundary.test.ts` (5) |
| Parent joints | `6926592` spent R9-1's reserved pin in `m2.5/resume.test.ts` with R11-1's prescribed hunk verbatim (R11-3 owned the file and did not edit those lines); `df2c395` corrected the status-matrix coverage-gate row (the per-criterion channel now exists). Both correct in content. **Process defect recorded:** the R11-1 sign-off ordered replacing an assertion living in a file outside R11-1's ownership — flagged in real time by R11-3 §6.1 and R11-4 §5, resolved by the joint, leaving one 20-second red-tree commit point at `6096da6`. Round 12 rule in §3 | — |

## 2. Current baseline (this VM, Node v22.14.0, engine warning only)

- **`pnpm gate` GREEN, exit 0**: **1938 tests / 1937 pass / 0 fail / 1 skipped**. The only skip is the `PI_SMOKE=1` real-provider gate. Any slot whose report shows a second skip introduced one and must say so. Delta vs Round 10: +23, accounting closed per-slot in the review §2.
- `node scripts/crash-probe.mjs` → `ok: true`, **11 cases × 3 iterations**; original ten names and order unchanged, `unblock-discard-append-before-checkpoint-sigkill` last. The name-list pin lives in `test/integration/persist/crash-recovery.test.ts` (**not** `test/integration/m2/`). Anyone changing the case set owns updating that pin in the same slot, with disclosure.
- Perf: no fresh whole-tree bench needed. Round 11's only hot-path deltas: one boolean gate check per tracking turn (R11-1); a pause-controller poll at round boundaries only when a controller is supplied (R11-3); R11-4's audit runs once per restore, only in the discard crash window, O(cited rows), no I/O. No writer, lock, or fs primitive changed (verified — zero raw fs writes in the round's src diff). Any Round 12 perf claim records its **own** same-VM before/after with an unchanged-arm control; ≥5% end-to-end or it is not a candidate.
- Parent bookkeeping: `PROGRESS.md`'s Round 11 table has a chimera dispatch id for R11-2 (`…82a5-5103-8de6-3a5d15860354`; the real id per `OWNERSHIP.md` is `…82a1-5523-9ef1-cae7b3339bb7`) — one-line fix whenever PROGRESS is next touched.

## 3. Forbidden / frozen for Round 12

Global forbidden list, unchanged: live R1/bandit/topology on the execution path (doctor's `loadProjectBanditByKey` inventory read is the **only** exception — re-swept at HEAD: store + doctor only; `selectArm` definition + `shadow.ts` only; bare `loadProjectBandit` zero `src` matches); Outcome-supported claims; **ADR-006 stays Proposed** (eleven rounds; keep it); auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit; no `git checkout` of other branches. `independentEvidence` is never read as corroboration (whole-`src` dereference census: exactly the one `void` at HEAD, R11-1 inherited it cleanly). No fourth `RunStatus` (two-level freeze + R11-9's census). No fourth key on `RUN_UNBLOCKED` (exact-key validator, read at HEAD: `{ blockedEventId, reason, retryNodeId }`).

Frozen contracts (Rounds 1–10 set, all held through Round 11, plus new):
- Rounds 1–10 carry-overs, verbatim from the Round 10 brief §3 — every one re-verified or gate-green this round: jsonl signatures; `writeFileAtomic`(+Sync), no private temp+rename (Round 11's whole src diff added zero raw fs primitives — verified); `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE`; `episodeLockPath` symmetry; `runCommand` sink wiring; `deleteRunRecords` lock-plus-double-verify; append/checkpoint take no run lock; `recordCrashTerminal` in-flight-only + caller always rethrows; `applyRetry` sole scheduler BLOCKED→READY producer + R8-3/R10-8/R11-8 AST pins; `preserveResumableState`; `ClusterMailReport`; resume flags + disclosure wordings + the supervised loopback stderr byte-pin (file diff-empty again this round); `writeFeedbackTombstones`; adaptation error codes + bind-after-load; doctor frozen-additive JSON incl. `learnedState`; feedback fuzz hard-fails; `withRunLifecycleLock` rules; supervised settle-on-crash + rounds-window pins; `commandFailureNext` code-discrimination; `CheckpointStore.read` typed error; one-definition-of-terminal (`TERMINAL_REPLAY_STATUSES` = COMPLETED/FAILED/BLOCKED, read at HEAD); five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT` character-exact (reviewer re-extracted base-vs-HEAD through R11-3's `main.ts` edit: identical); `--lock-wait-ms`; `process-death.ts`; R6-9 absence pins; reconstruction `childTasksFromLog` + FAIL-unreachable tripwire; parent-plane `recordTerminal`; empty-graph pre-flight; `settleSupervisedOutcome` absence; the Round 8 block (`RUN_UNBLOCKED` three-key semantics + type freeze; four-line BLOCKED block; `INSPECT_SUMMARY` additive freeze; catalog posture (b); parent-plane residual decisions); the Round 9 block (durable contract never synthesized; precedence `continuation.contract ?? checkpoint.flowchart.contract`; verdict-producer rules; gate `runStatus` ledger — zero `src` readers re-swept; the ordinary executed-descendant refusal **exact message**); the Round 10 block (`RUN_UNBLOCKED_WITH_DISCARD` exact-key schema as shipped; uniform clearing semantics; single-append discipline; per-block permission; every flowchart-payload `materializeCheckpoint` carries `contract` — exactly two call sites at HEAD, both verified; verdict tool unconditionally surfaced + lease identity + evidence-cited FAILED + one verdict per attempt — all four pins byte-untouched through R11-1's additive diff; episode boundary census; `independentEvidence` posture; BLOCKED prefix + `note:` disclosure).
- **New from Round 11:**
  - **Option (a)'s schema is frozen as shipped.** `VerificationResult.criteria` optional, non-empty when present, unique ids, FAILED-cites-evidence at both the protocol validator and `criterionList`; absence keeps its pre-existing meaning and emits a byte-identical verification object; per-criterion `UNOBSERVED` is expressible at the protocol layer and deliberately **not** offered by the tool; no `PROTOCOL_VERSION` bump. Criterion ids are not correlated against the request at protocol/tool layer — the tracking layer ignores an unasked id (recorded decision).
  - **The gate mechanism is frozen:** `unmet-acceptance-criterion` is hard, fed only by the required, supplied-never-derived `GateInput.criterionUnmet`, ordered after `claimed-verification-without-checks` and before `repeated-no-progress`, firing on a reported FAILED only. Never-ran/UNOBSERVED/silent all stay open — **unknown is not unmet**. `coverageOutcome` still has no FAIL in its range; the criteria a caller asks for still move records and prescore, never the directive; `cappedByHardFail` stays display-only.
  - **The `taskCriteria` seam schema is frozen; the field is declared-not-written** (reviewer-confirmed: zero `src` writers). Absence = unknown, forever valid; an entry's empty list = known-none; ascending `taskId`; never synthesized — from the episode (R11-10's census, exact spelling + semantic rule), the flowchart definition, or the run contract. **Filling it is candidate 1, sign-off required.**
  - **The restore-side discard audit is frozen:** both clearing events funnel through the one `applyClearingEvent`, which for the stronger event checks the consequence set first (R10-1's exact message dominant) and the charged sums second, over the recorded payload against the cited rows; replay deliberately records no anomaly for this class (it must stop a resume, not annotate it); the completeness limit (subset-citing authorizations pass) is a recorded decision. AST-pinned to stay in `applyClearingEvent`/`restoreCheckpointedSupervisor` under whole-file `applyRetry` absence.
  - **The 11-case probe** (grows only with disclosure in the same slot; `persist/` pin path).
  - **The tracked pause controller is present and frozen:** `TrackRunInput.pause` forwarded by `startTrackedRun`, supplied by `runCommand`; the no-controller/with-controller behavioural control pin. The run id still prints only after the outcome — the recorded gap candidate 2 closes.
  - **The BLOCKED prefix and `INSPECT_SUMMARY` freezes are now direct** (formatter-level prefix + three-`next:` ceiling; rich-fixture four-key `deepEqual`).
  - **The R10-2 meta-pin lives on re-pointed:** weakening any of the four landed option-(a) pin titles requires renaming it in the open.
- Saturated — do not spend a slot: everything in the Round 10 saturated list, plus: freeze/pin work on the shipped option (a) surface (held at the protocol validator, the tool, the gate test, the fuzz seeds, the 7-pin file, the meta-pin, and the terminal-status census — re-pinning is busywork); the restore-audit surface (producer + restore + AST guard + five behavioural tests); the live-isolation file; the episode boundary incl. `taskCriteria`; `INSPECT_SUMMARY`/BLOCKED prefix; the Round 10 docs truth-up (done; Round 11's own staleness is candidate 4).

Process requirements per slot (carried forward, plus one new): census first, against the working tree; scoped `eslint` + whole-tree `tsc --noEmit` before reporting; census your consumers and ship or prescribe their updates in your own diff; **verify that a path the brief hands you actually exists before writing to it**; run `live-isolation.test.ts` if you add an import inside the live closure; attribute shared-tree transients to files with timestamps (R11-4 §5 and R11-2's failed-then-attributed probe run are this round's models); timing-sensitive owned tests 3×; full gate is the parent's job; no scratch files at report time; a "did not land" claim is only true as of the minute taken. **New, for the parent at dispatch: cross-check every "replace X in the same diff" obligation against the ownership table — if the obligation's file belongs to another slot, either move the file into the obligated slot's ownership or plan the joint and fold it into the landing commit, not a follow-up commit.** Round 11's one red-tree commit point (`6096da6`, 20 seconds) came from exactly this gap.

## 4. Ranked Round 12 candidates (mutually exclusive ownership)

The round's tilt: option (a) shipped and every block shape still has a sanctioned exit; what remains is the one durability obligation option (a) could not complete under its ownership (the `taskCriteria` writer), the one operator gap R11-3 surfaced and correctly declined to drive through a sibling's file (early run-id disclosure), the reachability proof the new gate deserves, and the docs truth-up. **There are four real candidates, not ten — stated per protocol; the freeze surface is saturated and this brief does not manufacture busywork. If fewer slots run, the parent fills extras only against new Round 12 schema.**

File-contention map: `src/run/flowchart-run.ts` is needed by **both** candidates 1 and 2 — **they cannot be separate concurrent slots.** Recommended resolution: one slot (the round's spine) owns both mandates, or candidate 2 defers to Round 13. Remaining ownership is disjoint by construction: `src/run/flowchart-run.ts` + `src/track/loop.ts` + `src/cli/main.ts` + `test/integration/m2.5/resume.test.ts` + `test/unit/tracking/option-a-preconditions.test.ts` to the spine slot; the new `test/integration/run/criteria-gate.test.ts` (directory verified to exist) to R12-3; `docs/**` to R12-4. Nobody else touches `unblock-flow.test.ts`, `replay.test.ts`, or any pin file this round.

### R12-1 (P1, the round's spine — parent sign-off required) — Fill the `taskCriteria` seam: writer + `childTasksFromLog` reader
**Evidence (R11-1 §7.1/§9.1; reviewer verified the laundering path in source):** the field exists, validated, with zero writers. The prescribed writer hunk mirrors the contract line in `persistCheckpoint` — but the prescription's data source needs one correction the reviewer verified in source: `ctx.childByTaskId` holds every dispatched task's `acceptanceCriteria`, **and on a resume that map is rebuilt by `childTasksFromLog`, which gives a never-logged request `acceptanceCriteria: []` (`flowchart-run.ts:411`)** — a naive writer would launder the very unknown the field exists to preserve. The writer must derive from logged `TASK_REQUEST`s (`loggedTaskRequests` already exists) or first-write-wins against the checkpoint's existing `taskCriteria`, ascending `taskId`. The reader half: thread the checkpoint's `taskCriteria` into `childTasksFromLog`'s substitution path (sole call site `flowchart-run.ts:1346`) so a resumed never-ran node is re-dispatched with its original criteria rather than a silent empty list. **This changes what a resumed node is asked to check — the sign-off must approve that semantic before any code.** Obligations: R11-1's 7th pin and R11-10's census must stay green (recording dispatched log facts is not synthesis — the writer must not read the episode, the definition, or the run contract); the R10-4 writer-carriage property must keep finding `contract` on both call sites; behavioural proof is a real crashed-or-paused run with criteria whose resume re-dispatches the original criteria (and whose never-logged node stays unknown), the `unblock-flow`/probe fail-closed pattern. A malformed hand-edited `taskCriteria` already fails closed (R11-1's validator) — do not re-pin it.
**Ownership:** `src/run/flowchart-run.ts` (sole); `test/integration/m2.5/resume.test.ts`; `test/unit/tracking/option-a-preconditions.test.ts` (7th-pin truth-up if its no-writer language needs it).

### R12-2 (P2, src, small — parent sign-off required; **same slot as R12-1 or defer**) — Early run-id disclosure, closing R11-3's declined option (d)
**Evidence (R11-3 §3/§6.6, reviewer re-verified):** an operator cannot pause a live tracked run — the id prints only after the awaited outcome, and the pause token is keyed by run id. The clean shape R11-3 designed and correctly declined to build through a sibling's file: an optional started-run callback on `FlowchartRunDeps` (e.g. `onRunStarted?: (runId: RunId) => void`), fired once the run directory and lifecycle lock exist and **before** round 1's pause poll; `startTrackedRun` forwards it; `runCommand` prints the id early on the track path. Payoff beyond the operator gap: the pure-CLI pause leg becomes provable deterministically — a synchronous `io.stdout` handler seeds the token before the first poll, upgrading R11-3's call-site source pin to its behavioural successor. Do **not** overload the pause controller as a notification channel (R11-3's stated reason for declining the wrapper). Consumers to census: every `startFlowchartRun` deps literal, `blocked-next`'s `runCommand` output censuses, `doctor-routed-next-freeze` (routes must stay character-exact through any `main.ts` edit).
**Ownership:** `src/run/flowchart-run.ts` + `src/track/loop.ts` + `src/cli/main.ts` (all shared with R12-1's slot by the contention rule); `test/integration/m2.5/resume.test.ts`, `test/integration/m2.5/cli-contract-honesty.test.ts`.

### R12-3 (P2, test-only) — Production-reachability proof for the new gate
**Evidence (review §6.4):** `unmet-acceptance-criterion` is covered at the unit level (gate ordering; all-roles blocking through `assessChildObservation`, the real production path), the producer level (criteria ride the child's terminal through `PiAgentExecutor`), and the persistence level (fuzz seeds). What does not exist is R9-2's measured-reachability analogue: one integration run where a child reports PASSED-with-one-FAILED-criterion and the **run itself** lands BLOCKED with `reasonCode: unmet-acceptance-criterion` on the `GATE_TRANSITION`, then exits through the sanctioned `unblock` path to COMPLETED. Cheap and deterministic: a test executor whose `ChildRunOutcome.terminalResult.verification` carries `criteria` flows through `observationFromChild`'s wholesale spread (`child-tracking.ts:80`) — no loopback server, no live provider. Assert the control too: the same run shape with the criterion PASSED (or the array omitted) completes — that is what makes the block attributable to the channel. Do not edit `unblock-flow.test.ts` (R11-4's surface, saturated); a new file keeps ownership clean.
**Ownership:** new `test/integration/run/criteria-gate.test.ts` (directory exists — verify before writing, per the standing rule).

### R12-4 (P3, docs) — Round 11 truth-up
**Evidence (reviewer-verified at HEAD, review §6.3):** four claims are false at HEAD — "The tool does not carry per-criterion results" (`docs/data-dictionary.md:188`); "The tool still carries no per-criterion results; that later protocol/gate work remains unimplemented" (`docs/specs/m0-m2-architecture.md:651-652`); "tracked start supplies no pause dependency" (`docs/status-matrix.md:63-65` **and** `docs/specs/m0-m2-architecture.md:388-390`). All four were true at R11-5's 00:00:52 landing and were superseded by R11-1 (00:16:54) and R11-3 (00:10:32). Record Round 11's landings as commits (`6096da6`, `db38b21`, `ac3faa3`, `9663294`, `9efc715`, `3bbb8dc`, `39c97c3`, `330466a`, `f99a0c8`, joints `6926592`/`df2c395`): option (a) as shipped (including unknown-not-unmet and the no-writer residual — or its closure if R12-1 lands first), the restore-side audit and its recorded completeness limit, the tracked pause controller and the run-id-at-end gap (or its closure if R12-2 lands), the eleven-case probe (already recorded — keep). The dated 23:56–23:59 census notes are superseded the same way Round 10's were. `docs/decisions/**` untouched; no ADR status line; ADR-006 stays Proposed.
**Ownership:** `docs/**` only; timestamp-census the working tree (R11-5's five-census pattern is the model).

*(No further candidate is manufactured: there are **four**. Everything else surfaced by Round 11 is either saturated (§3), folded above (the pure-CLI pause behavioural proof into R12-2), or a recorded decision whose reopening needs new evidence — the audit completeness limit, the unasked-criterion-id acceptance, the role-gate C4 asymmetry (only a tester's asked-for criteria become `requiredChecks` — now visible and pinned; widening `check-coverage`'s roles is a design question with no current evidence of harm), and `derivedClaimedVerificationWithoutChecks` kept-not-widened (removal is a different question with its own evidence).)*

## 5. Explicitly NOT for Round 12 (unchanged or newly settled)

Live R1/bandit/topology (standing; the keyed diagnostic read is the ceiling); reading `independentEvidence` as corroboration or renaming it (whole-`src` dereference census; the option-(a) diff inherited it cleanly — the posture held); a fourth terminal `RunStatus` (two-level freeze + R11-9); a fourth key on `RUN_UNBLOCKED` or any field addition to it (exact-key validator; the stronger event exists); re-litigating option (a)'s mechanism — the criteria-shaped dimension FAIL (measured no-op), cap-to-gate (measured 54-cell non-criteria blast), riding `claimed-verification-without-checks` (untrusted-prose gating), a soft-only code (same directive, vaguer reason), and a `PROTOCOL_VERSION` bump (breaks every persisted row for an additive field) all stay rejected with their evidence; correlating criterion ids against the request at the protocol/tool layer (recorded decision — the tracking layer ignores unasked ids; a slip must not destroy a result); offering per-criterion `UNOBSERVED` on the tool (omission already says it); widening the discard audit to completeness (refuses the re-block shape — recorded); refunding budgets on discard or folding invocation telemetry into charges (absence-is-not-zero); an operator-supplied rewind list; two-append authorization shapes; making the discard a run mode; giving gate `runStatus`/`applied`/`transitionId` a consumer; synthesizing `contract` **or `taskCriteria`** from the episode or anywhere else (R11-10's census names the exact spelling and the semantic rule); a new flowchart checkpoint writer that drops `contract` (property pin fails by location); manufacturing a pause by killing a process, or racing the pause token (standing); overloading the pause controller as a run-started notification (R11-3's recorded reason); opt-in gating of the verdict tool as a drive-by; jsonl/lock/append perf and per-step writer locking (saturated); mailbox/dead-letter/cluster work; lock stealing; resume-time adoption of unaccepted child results; `maxCostUsd` enforcement; a non-terminal `RUN_CRASHED` marker; rewriting append-only run logs for privacy; ADR-006 status changes (frozen); re-litigating option (b), the catalog posture, the parent-plane residual decisions, the unblock fail-closed default, the gate-ledger posture, the R10-1 payload decisions, or the R11-4 set-before-sums ordering (each recorded and pinned); P0 privacy sign-off (human); `package.json`/dependency bumps.
