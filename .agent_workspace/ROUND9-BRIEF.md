# ROUND 9 BRIEF — injection context for Loop 4 · Round 10

Provenance: written by the Round 9 SOTA reviewer (claude-fable-5-thinking-xhigh) after evidence-based review of R9-1…R9-10 at `8e933a7`; verdicts and verifications in `.agent_workspace/loop4-r9-review.md` (9 ACCEPT, 1 ACCEPT-WITH-NITS, 0 ROLLBACK; zero parent fix-joints; nothing left red). Every candidate below cites evidence the reviewer verified at HEAD against the actual diff, not report hearsay. All ten slots are committed (`5970a2f`…`aeb14dc`); parent commits touch only `.agent_workspace/**`. **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 9 (all committed)

| Slot | Landed | Key symbols (do not re-implement) |
|---|---|---|
| R9-1 | **The run contract is durable.** `contract?: RequirementContract` on `FlowchartCheckpointState` at the unchanged `schemaVersion: 1`; absence stays valid; validation via `validateRequirementContract` wrapped as `Invalid RunCheckpoint: flowchart.contract: …`; **never synthesized from the episode** (the episode pin is now a standing rule, load-bearing). All **three** flowchart checkpoint writers carry it: `persistCheckpoint` (every checkpoint, so pause/inject via `restoreFlowchartSession` cannot strip it), and — the find of the round, missed by R8-2's census — `unblockLockedFlowchartRun`, which rebuilds the payload from parts and was silently erasing the contract from every unblocked run. CLI `flowchartContinuation` projects `checkpoint.flowchart.contract` (covers `resumeCommand` and `answerCommand`); runner precedence `continuation.contract ?? checkpoint.flowchart.contract`, behaviourally proved (an explicit constraint-free continuation outranks a two-constraint stored one). The R8-2 flip-pin is flipped **through the production CLI boundary** — `main(["resume",…])` reads `constraint-retention: PASS` off the log for the leg the log never saw run; seeding stays API-side and disclosed (no CLI command accepts a contract — see candidate 4). R8-2's region-regex nit fixed (`^\}$` anchors + `doesNotMatch(/\bunblockCommand\b/)` drift guard). `INSPECT_SUMMARY` producer one-liner landed at the `main.ts` literal. Per-task acceptance criteria are **reserved in prose only** on the field's docstring, and the schema pin asserts the sibling field was not implemented | `FlowchartCheckpointState.contract` + `validateFlowchartCheckpointState` in `run/replay.ts`; the three writers in `run/flowchart-run.ts`; `flowchartContinuation` in `cli/main.ts`; positive schema/writer/CLI pins + flip + precedence + no-contract + unblock-carry + pause-preservation tests in `m2.5/resume.test.ts` (15); valid/malformed/absent trio in `flowchart-snapshot.test.ts`; contract-bearing `CHECKPOINT_SEEDS` in `persist/row-fuzz.test.ts` |
| R9-2 | **The tracking gate has a live producer — option (a) precondition 0 closed.** `sparkle_report_task_result` (`REPORT_TASK_RESULT_TOOL`, `createTaskResultTool`) built per leased attempt beside `createClusterTools`; emits one protocol-v1 `TASK_RESULT` with identity stamped from the request, never the model. Producer rules, each justified in-source: PASSED/FAILED only (UNOBSERVED is what silence means); non-empty summary; CANCELLED refused (parent's fact); malformed `evd_`/`art_` refs refuse the whole call; **FAILED must cite ≥1 evidenceId** (`from-child.ts:76-79` discards unreferenced FAIL dimensions — the verdict would vanish); one verdict per attempt, first wins; a failed attempt's verdict never leaks into the retry. `finish` now really prefers a child-authored `TASK_RESULT` over synthesized UNOBSERVED (the guard was dead; now live, mutation-proved). No protocol schema change. R8-4 pin 2 re-derived in place (census catches runtime-valued kinds; message count 1→2; behavioural half runs the shipped tool into the gate); pins 1/3/4/5 byte-identical — reviewer diffed them. **Measured reachability for `--executor pi`:** PASSED opens (swept floor 0.750 vs softThreshold 0.55, zero of 360 cells below), FAILED hard-blocks with `deterministic-fail` leading (180/180) — a pi run can now really be BLOCKED, and R8-1's `unblock` is what reopens it. Still unreachable, with in-source reasons: `soft-threshold`, `ownership-escape`, `repeated-no-progress`, `user-reject-stop`, `permission-security-reject`, `minor-escalated`, `wait_user`. C6 holds exactly as R8-4 predicted: `claimed-verification-without-checks` fires only on success-claiming FAILED prose with non-empty checks, never as the leading code — pinned with a real reported message | the tool + rules in `pi-adapter/pi-executor.ts`; 13 tests in `test/unit/pi-adapter/report-task-result.test.ts`; rewritten pin 2 in `option-a-preconditions.test.ts` |
| R9-3 | **Executed-descendant discard: designed, decided, fail-closed pinned.** Decision: a distinct exact-keyed event, provisionally `RUN_UNBLOCKED_WITH_DISCARD`, behind CLI `unblock --discard-executed` — **not** a fourth `RUN_UNBLOCKED` key (breaks the exact-key freeze, multi-strength event), **not** a two-event sequence (half-authorization crash window). Flag is boolean opt-in to a set **computed** under the lifecycle lock (never operator-listed); requires a gate block + exact `--retry-node`; refused when nothing executed; no budget refund (charged estimates recorded, absence-is-not-zero telemetry honesty); evidence and events survive, control-state outcome fields clear; restore recomputes the consequence set and fails closed on mismatch; transform → single append → checkpoint, recovered via the `unappliedUnblock` discipline. Payload sketch: `{ blockedEventId, reason, retryNodeId, rewoundDescendants[]: { nodeId, taskId, previousState, modelRouteEventIds, childRunIds, chargedEstimated* } }`. Reviewer evaluated all three shapes and **recommends the distinct event for Round 10 sign-off** with one payload question decided (candidate 1). Additive unit pin landed: ordinary reopen refuses two COMPLETED descendants by exact message and leaves the snapshot untouched | design in `.agent_workspace/loop4-r9-t3.md` §2–7 (incl. full consumer census + test plan); the refusal pin in `flowchart-supervisor.test.ts` |
| R9-4 | Docs truth-up for Round 8 + live Round 9 sync, timestamped to the minute with sibling-transient attribution; ADR-006 untouched and Proposed; `0005:110` stays flag-only. Corrected: `ba0b2ce` landed (not in-flight); `05051ac` pins landed; parent-plane decisions, catalog posture, and R8-4's fourth precondition recorded — then updated live when R9-2 closed it. **Stale at HEAD (Round 10 docs slot):** `m0-m2-architecture.md:737` still says **nine** probe cases (the ten-case probe was committed before the final census — the round's one nit); the sync note's "neither is represented as a HEAD commit" (R9-1/R9-2 landed after R9-4 committed — honest dated census, superseded) | `docs/status-matrix.md`, `docs/data-dictionary.md`, `docs/specs/m0-m2-architecture.md`, the tracking plan |
| R9-5 | **Tenth standing probe case: `unblock-append-before-checkpoint-sigkill`.** The child runs the real, unmodified `unblockFlowchartRun` (no seams, sentinels, or self-kill); a 16 MiB reason widens the producer's own post-append read so the parent can observe the complete newline-terminated `RUN_UNBLOCKED` row and send an **external** SIGKILL, asserting the child died by signal, exactly one row decodes, the checkpoint is byte-identical pre-unblock, and no checkpoint temp exists; then documented stale-lock removal + real `resumeFlowchartRun` recovers the reopen exactly once to COMPLETED with zero anomalies. Original nine cases and order unchanged. Name-list pin updated **with disclosure** in the real consumer `test/integration/persist/crash-recovery.test.ts` — the Round 8 brief's `m2/` path does not exist; the slot disclosed the brief error (future briefs: verify paths) | the case in `scripts/crash-probe.mjs`; the ten-name pin |
| R9-6 | **Gate `runStatus` is a ledger, not a control input — recorded and mechanical.** `gate-apply.ts` comment-only (mechanically verified): posture docstrings on `GateApplyResult` and `currentGateStatus`, citing R8-4 C7 as settled. Census one step stronger than R8-1 §11.3: `runStatus` has **zero readers on both planes** (the coordinator reads `directive` only); `applied`/`transitionId` same position. 8-test pin file: rewrap-safe AST prose pins (proven by reflowing the real file), an all-215-module no-reader sweep, a flowchart-plane events-only pin (exactly one `applyChildThreeLine` call site), real-source mutation checks, ledger identity (`runStatus === transition.payload.to`), and a unit-level observer for `currentGateStatus`'s matched-unblock-reads-RUNNING contract. Ownership deviation disclosed: `applyChildThreeLine` lives in unowned `child-tracking.ts`; the posture lands on its declared return type; a one-line pointer is prescribed for a future owner of that file | the two docstrings in `run/gate-apply.ts`; `test/unit/run/gate-status-posture.test.ts` (8) |
| R9-7 | Five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT` frozen **character-exact at the AST level**: five ordered code-key/wording tuples, exactly one `${doctor}` interpolation each, `deepEqual` against frozen text; a deletion mutant (catalog tuple removed by AST span, in memory) proves the freeze throws. Reviewer independently diffed the extracted routes base-vs-HEAD through R9-1's `main.ts` edit: identical | `test/unit/cli/doctor-routed-next-freeze.test.ts` (2) |
| R9-8 | Reconstruction freeze: `resumeLockedFlowchartRun` pinned to exactly one `childTasksFromLog(read.events, definition, registry, …)` call feeding `childByTaskId`; the R6-2 FAIL-unreachable tripwire frozen by reading this test's own source (title, seam name, both verification kinds, all five outcomes, the FAIL-unreachable assertion) — deleting the tripwire cannot leave only the wiring pin green | the new test in `flowchart-run-abort.test.ts` |
| R9-9 | Post-R8-9 isolation freeze: a recursive 215-module `src` census pins `selectArm`'s only non-definition occurrence to `routing/shadow.ts` and `\bloadProjectBandit\b` to zero `src` occurrences (trailing boundary spares the keyed reader). Existing doctor `assert.match`, widened readers sweep, and the signed-off "read-only inventory, never a selector" wording untouched (+37/−0) | the new test in `live-isolation.test.ts` |
| R9-10 | `RUN_UNBLOCKED` payload **type-frozen**: bidirectional conditional-type equality pins `keyof` the payload to exactly `blockedEventId | reason | retryNodeId` — a fourth typed key fails whole-tree `tsc`. Complements R8-1's runtime exact-key refusals (untouched) | the type pin in `event-row-fuzz.test.ts` |

## 2. Current baseline (this VM, Node v22.14.0)

- **`pnpm gate` GREEN, exit 0**: **1880 tests / 1879 pass / 0 fail / 1 skipped**. The only skip is the `PI_SMOKE=1` real-provider gate. Any slot whose report shows a second skip introduced one and must say so. Delta vs Round 8: +35, accounting closed per-slot in the review §2.
- `node scripts/crash-probe.mjs` → `ok: true`, **10 cases × 3 iterations** (R9-5 added `unblock-append-before-checkpoint-sigkill`; original nine order unchanged). The name-list pin lives in `test/integration/persist/crash-recovery.test.ts` — **not** `test/integration/m2/` (that path does not exist). Anyone changing the case set owns updating that pin in the same slot, with disclosure — candidate 6 below is the next case of that shape, and it sequences *after* candidate 1 lands.
- Perf: no fresh whole-tree bench needed. Round 9's only hot-path deltas are one optional field on checkpoint write/read/validate (spread + one validation call when present) and one tool object built per pi attempt; no per-step writer changed, no lock added, no fs primitive added (verified). Any Round 10 perf claim records its **own** same-VM before/after with an unchanged-arm control; ≥5% end-to-end or it is not a candidate.

## 3. Forbidden / frozen for Round 10

Global forbidden list, unchanged: live R1/bandit/topology on the execution path (doctor's `loadProjectBanditByKey` inventory read is the **only** exception, signed off, read-only, pinned — now doubly held by R9-9's recursive census); Outcome-supported claims; **ADR-006 stays Proposed** (nine rounds; keep it); auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit; no `git checkout` of other branches.

Frozen contracts (Rounds 1–8 set, all held through Round 9, plus new):
- Rounds 1–8 carry-overs, verbatim from the Round 8 brief §3: jsonl signatures; `writeFileAtomic`(+Sync), no private temp+rename (Round 9's whole `src` diff added zero raw fs primitives — verified); `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE`; `episodeLockPath` symmetry; `runCommand` sink wiring; `deleteRunRecords` lock-plus-double-verify; append/checkpoint take no run lock; `recordCrashTerminal` in-flight-only + caller always rethrows; `applyRetry` sole scheduler BLOCKED→READY producer + R8-3's AST pin over both flowchart sources; `preserveResumableState`; `ClusterMailReport`; resume flags + disclosure wordings + the supervised loopback stderr byte-pin (file diff-empty again this round); `writeFeedbackTombstones`; adaptation error codes + bind-after-load; doctor frozen-additive JSON incl. `learnedState`; feedback fuzz hard-fails; `withRunLifecycleLock` rules; supervised settle-on-crash + rounds-window pins (R8-10's seed joins the frozen set); `commandFailureNext` code-discrimination; `CheckpointStore.read` typed error; one-definition-of-terminal (`TERMINAL_REPLAY_STATUSES` = COMPLETED/FAILED/BLOCKED, unchanged); five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT` character-exact (now AST-frozen by R9-7); `--lock-wait-ms`; `process-death.ts`; R6-9 absence pins; reconstruction `childTasksFromLog` + FAIL-unreachable tripwire (now structurally frozen by R9-8); criteria-are-guidance + 270-cell sweep; parent-plane `recordTerminal` (mechanical); empty-graph pre-flight; `settleSupervisedOutcome` absence; the whole Round 8 block (`RUN_UNBLOCKED` semantics + type freeze; four-line BLOCKED block + four-site wiring; `INSPECT_SUMMARY` additive freeze — the producer literal now carries R9-1's comment; `loadProjectBandit` absence — now a whole-`src` census pin; catalog posture (b); parent-plane residual decisions).
- **New from Round 9:**
  - **The durable-contract semantics**: `contract?` is optional at `schemaVersion: 1` and absence is valid forever (old checkpoints, contract-less runs); it is validated through `validateRequirementContract` with the `flowchart.contract:` error prefix; **it is never synthesized — not from the episode, not from acceptance criteria, not as `{ constraints: [] }`**; precedence is `continuation.contract ?? checkpoint.flowchart.contract` (an explicit continuation always wins); **every writer of a flowchart checkpoint payload carries it** — there are exactly three today (`persistCheckpoint`, `restoreFlowchartSession`→`persistCheckpoint`, `unblockLockedFlowchartRun`) and R9-1's schema pin names them; a new writer that drops the field is the exact bug R9-1 fixed. The per-task-criteria sibling field stays **reserved in prose, pinned unimplemented** until option (a)'s signed-off diff.
  - **The verdict-producer rules** (`sparkle_report_task_result`): identity from the lease, never the model; PASSED/FAILED only; FAILED cites ≥1 evidenceId; CANCELLED is not a child claim; one verdict per attempt, refused not double-emitted; a failed attempt's verdict never leaks into the retry; `finish` synthesizes UNOBSERVED **only** for silence. The tool is always surfaced (a capability of the request, the cluster-tools posture) — making it opt-in is a `runtime.ts`/`main.ts` decision the parent may take, not a drive-by. R8-4 pins 1/3/4/5 still carry their replace-in-same-diff obligation to the option-(a) diff; pin 2 has been through its replacement.
  - **Gate `runStatus` is a ledger, not a control input** (parent-signed, comment-only, zero `src` readers on both planes, AST-pinned across all of `src/`): giving it — or `applied`/`transitionId` — a consumer is a decision with its own justification, and R9-6's pin 2 makes it mechanically loud. The flowchart plane reaches `applyChildThreeLine` exactly once and takes only `events` (pin 3): a fifth flowchart command or new gate directive inherits that pin deliberately.
  - **The ten-case probe** with its name-list pin at `test/integration/persist/crash-recovery.test.ts`; the original nine names and order are load-bearing.
  - **The executed-descendant refusal is now unit-pinned by exact message** (ordinary unblock lacks discard authorization; snapshot untouched). Candidate 1 adds a separate positive path; the refusal pin must stay green byte-for-byte.
- Saturated — do not spend a slot: everything in the Round 8 saturated list, plus: freeze/pin work on the existing contract surface (R9-7/8/9/10 closed the four freezes the Round 8 review asked for; the routes, reconstruction seam, isolation census, and unblock payload are each now held at two levels — re-pinning them again is busywork); the gate-reconstruction posture (recorded, pinned, settled); the R8-4 census (pin 2 replaced; the remaining pins transfer as one bundle to the option-(a) diff and to nothing else); the unblock crash window (in the standing probe); Round 8 docs truth-up (done; Round 9's own staleness is candidate 3).

Process requirements per slot (carried forward — Round 9 executed them cleanly again): census first, against the working tree; scoped `eslint` + whole-tree `tsc --noEmit` before reporting; census your consumers and ship or prescribe their updates in your own diff; **verify that a path the brief hands you actually exists before writing to it** (R9-5's lesson — the brief was wrong, the slot checked); run `live-isolation.test.ts` if you add an import inside the live closure; attribute shared-tree transients to files with timestamps (R9-4 §census and R9-6 §7 are this round's models); timing-sensitive owned tests 3×; full gate is the parent's job; no scratch files at report time; a "did not land" claim is only true as of the minute taken.

## 4. Ranked Round 10 candidates (mutually exclusive ownership)

The round's tilt: Round 9 closed the durability arc (the contract survives every operator action) and gave the gate its first live producer — which means a production pi run can now block on a real FAILED verdict, and the one block shape `unblock` cannot fix (executed descendants) is no longer hypothetical. Both remaining implementations — the discard and option (a) — are fully designed with all preconditions closed, **and they contend on `src/run/replay.ts`**. Exactly one can be Round 10's implementation spine; the reviewer recommends the discard (smaller blast radius, complete consumer census, operator-facing dead end that R9-2 just made reachable), with option (a) as Round 11's spine unless the parent decides otherwise.

File-contention map: `src/run/events.ts` + `src/run/replay.ts` + `src/run/flowchart-run.ts` + `src/supervisor/flowchart-supervisor.ts` + `src/run/gate-apply.ts` + `src/cli/main.ts` to **R10-1 alone** (if it runs); `docs/**` to **R10-3**; `test/integration/m2.5/resume.test.ts` to **R10-4**; `src/tracking/**` to **R10-5 only if option (a) does not run this round**.

### R10-1 (P1, the round's spine — schema sign-off required) — Implement the executed-descendant discard
**Evidence (R9-3's design, reviewer-evaluated; R8-1 §11.1; R9-2 §5):** the reopen fails closed on executed descendants — now unit-pinned by exact message — and a real pi FAILED verdict can now produce exactly this block shape in production. The design is complete: distinct exact-keyed `RUN_UNBLOCKED_WITH_DISCARD` (reviewer concurs — a payload field breaks the exact-key freeze, a two-event sequence has a half-authorization crash window), CLI `unblock --discard-executed`, computed consequence set, gate-block-only, refused-when-nothing-executed, no budget refund, evidence survives / control-state outcome fields clear, restore recomputes and fails closed, single append with `unappliedUnblock`-style recovery.
**Sign-off must decide:** (a) the event name; (b) **charged estimates in-payload vs references-only** — the design duplicates derivable `chargedEstimated*` sums into the authorization record (self-contained audit, validated against `MODEL_ROUTED`), which creates a mismatch class references-only would not have; the reviewer leans keeping them *with* fail-closed validation, but it is the parent's call.
**Obligations beyond the design (from Round 9's own findings):** the discard transform is a **fourth flowchart checkpoint writer and must carry `contract` forward** (R9-1's bug class — update R9-1's writer-census schema pin in the same diff); extend R9-10's type freeze and the row-fuzz seeds for the new event; keep the ordinary-refusal pin (R9-3) green byte-for-byte; keep R9-7's route freeze green (`main.ts` owner); replay/`unappliedUnblock`/`currentGateStatus` treat both clearing events uniformly; the full test plan is already written at `loop4-r9-t3.md` §7 — execute it, do not redesign it. `RUN_UNBLOCKED` itself stays byte-frozen.
**Ownership:** `src/run/events.ts`, `src/run/replay.ts`, `src/run/flowchart-run.ts`, `src/supervisor/flowchart-supervisor.ts`, `src/run/gate-apply.ts`, `src/cli/main.ts` (all sole); `test/unit/run/event-row-fuzz.test.ts`, `test/unit/run/replay.test.ts`, `test/integration/run/unblock-flow.test.ts`, `test/integration/cli/unblock.test.ts`, `test/integration/cli/blocked-next.test.ts`, `test/unit/supervisor/flowchart-supervisor.test.ts` (additive), `test/integration/m2.5/resume.test.ts` **only if** R10-4 is not scheduled (else coordinate: R10-4 owns that file).

### R10-2 (P1, mutually exclusive with R10-1 — schedule exactly one) — Option (a), all preconditions now closed
**Evidence (R8-4 §9's ship order, every step verified landed):** producer (R9-2), checkpoint seam (R9-1, reserved in prose and pinned unimplemented), unblock (R8-1). **The parent has not signed this off; closing precondition 0 does not make it this round's schema by default.** The one-diff shape stands as R8-4 designed it: optional nested `VerificationResult.criteria` (additive-compatible), a new `unmet-acceptance-criterion` anomaly fed by an explicit producer fact on **observed FAILED only** (never-ran nodes stay unknown-not-unmet), durable per-task criteria riding R9-1's reserved checkpoint seam, plus the per-criterion reporting surface on `sparkle_report_task_result`. Replaces R8-4 pins 1/3/4/5 and R9-1's reserved-unimplemented assertion in the same diff; folds candidate R10-5.
**Contention:** owns `src/protocol/v1.ts`, `src/tracking/**`, `src/pi-adapter/pi-executor.ts`, `src/run/replay.ts` — collides with R10-1 on `replay.ts` and with R10-5 entirely. If the parent picks this as the spine, R10-1 defers to Round 11 and its design stays frozen as written.
**Ownership (if chosen):** the files above (sole), `test/unit/tracking/**`, `test/unit/pi-adapter/**`, `test/unit/run/event-row-fuzz.test.ts` (TASK_RESULT seed), checkpoint tests.

### R10-3 (P3, docs) — Round 9 truth-up
**Evidence (reviewer-verified stale at HEAD, review §4.4/§5):** `docs/specs/m0-m2-architecture.md:737` still says the probe has **nine** cases (it has ten, committed `25a57d9` — the round's one nit); the status-matrix sync note's "neither is represented as a HEAD commit" is superseded (`dff71f1`/`aeb14dc` landed); undocumented Round 9 facts: the durable contract and its never-synthesize rule, the verdict-producer rules and measured pi reachability (a pi run can now BLOCK), the `runStatus` ledger posture, the discard design decision, the ten-case probe. `docs/decisions/0005…md:110` stays flag-only (ADR body). No ADR status line.
**Ownership:** `docs/**` only; timestamp-census the working tree (the R9-4 pattern, including its miss: sweep owned files for facts committed *during* the round, not only racing working-tree state).

### R10-4 (P3, test-only) — Close the last non-CLI leg of contract retention
**Evidence (R9-1 disclosure 2, premise reviewer-verified):** the flip test seeds through `startFlowchartRun` because no CLI command accepts a contract — but `--track` extracts one and passes it to `startFlowchartRun` (`src/track/loop.ts:157-171`, `contract` at the call), so after R9-1 a tracked run's contract is durable. Nothing yet proves the pure-CLI arc: `run --track` (offline, `--executor fake` — the extraction path `cli-contract-honesty.test.ts` already exercises) → `pause` → `resume`, asserting the resumed leg reports `constraint-retention: PASS` and the stored contract round-trips, with zero embedder API in the loop. **Fold:** a structural writer-census pin (review §6.6) — exactly two `materializeCheckpoint` call sites pass a flowchart payload today and both carry `contract`; pin the count and the carriage so R10-1's third writer fails loudly if it forgets (coordinate wording with R10-1 if both run: the pin should assert "every flowchart-payload writer carries contract", not a frozen count). If the offline `--track` path cannot produce a contract-ful run without a live model, stop and report per the R5-8/R7-10 precedent.
**Ownership:** `test/integration/m2.5/resume.test.ts` (or a new `m2.5/` file), `test/integration/m2.5/cli-contract-honesty.test.ts` (additive). No `src/**`.

### R10-5 (P3, small decision — only if option (a) does not run) — Tracking-plane posture pair
**Evidence (R9-2 §5, reviewer-confirmed in source; R9-6 §4):** (1) `independentEvidence` (`from-child.ts:167`) is now set from a *self*-report and is `void`-ed inside `computePrescore` — nothing reads it, and a future consumer reading it as third-party verification would be wrong; record the posture (or rename) with a pin, the parent's call. (2) The one-line pointer R9-6 prescribed above `applyChildThreeLine` in `child-tracking.ts` (ledger-not-control, `{@link GateApplyResult}`). Both are comments + pins, no behaviour.
**Ownership:** `src/tracking/from-child.ts` and/or `prescore.ts` (comment-only), `src/run/child-tracking.ts` (one comment line), additive pins. **Collides with R10-2 — cancel this slot if option (a) runs.**

### R10-6 (P3, probe — sequenced after R10-1 lands, or next round) — The discard crash window joins the standing probe
**Evidence (R9-5 precedent):** if R10-1 lands, `RUN_UNBLOCKED_WITH_DISCARD` has its own append-before-checkpoint window; the R9-5 technique (real producer, widened append, external SIGKILL) transfers directly. The probe needs the landed producer, so this cannot start until R10-1 is committed — schedule late in the round or hold for Round 11. Owns the ten→eleven name-list pin update with disclosure.
**Ownership:** `scripts/crash-probe.mjs`, `test/integration/persist/crash-recovery.test.ts` (name-list pin only).

*(No further candidate is manufactured: everything else surfaced by Round 9 is either saturated (§3), folded above (the writer-census pin into R10-4, the `child-tracking.ts` pointer into R10-5, the `independentEvidence` hazard into R10-2/R10-5), or a recorded decision whose reopening needs new evidence — the always-on posture of the verdict tool, the charged-estimates question (decided at R10-1's sign-off), and the raw-value `validateCheckpoint` return (disclosed unchanged behaviour, R9-1 §5.3, no consumer harmed). If fewer slots run, the parent fills freeze-only extras per protocol — but note §3: the existing contract surface is freeze-saturated, so freeze extras should target only *new* Round 10 schema, not re-pin Round 9's.)*

## 5. Explicitly NOT for Round 10 (unchanged or newly settled)

Live R1/bandit/topology (standing; the keyed diagnostic read is the ceiling, now census-pinned across all of `src/`); **running both R10-1 and R10-2 in the same round** (they contend on `replay.ts`; one is the spine, the other waits); implementing per-criterion anything outside option (a)'s one signed-off diff (R9-1's reserved-field pin makes a drive-by fail); a fourth key on `RUN_UNBLOCKED` (type-frozen, runtime-frozen, and the design decision explicitly rejected it); widening ordinary `unblock` to rewind executed work (the refusal is unit-pinned by exact message — R10-1's distinct event is the only sanctioned path); giving gate `runStatus`/`applied`/`transitionId` a consumer without its own justification (parent-signed posture, AST-pinned); synthesizing a contract from the episode or anywhere else (standing rule, load-bearing pin); making the verdict tool report anything but the child's own claim, or reading `independentEvidence` as third-party verification; opt-in gating of the verdict tool as a drive-by (it is a `runtime.ts`/`main.ts` parent decision); jsonl/lock/append perf and per-step writer locking (saturated); protocol/v1 parse and event fuzz rule changes (new-event seeds ride R10-1); mailbox/dead-letter/cluster work; lock stealing; resume-time adoption of unaccepted child results (R5-5's NO stands); `maxCostUsd` enforcement; a non-terminal `RUN_CRASHED` marker; rewriting append-only run logs for privacy; ADR-006 status changes (frozen forever); re-litigating option (b), the catalog posture, the parent-plane residual decisions, the unblock's fail-closed default, or the gate-ledger posture (each recorded and pinned); P0 privacy sign-off (human); `package.json`/dependency bumps.
