[Model: claude-fable-5-thinking-xhigh]

# Loop 4 · Round 12 — SOTA review at `aa7282f`

Reviewer ran independently on this VM (Node v22.14.0, engine warning only), on `agent/opt-continuous`, working tree clean at HEAD `aa7282f`. Every verdict below is against the actual diff `2767321..aa7282f` — the six substantive landings (`81f5b81`, `95a2b25`, `b8f784f`, `d1b451c`, `b65a8b1`, `d592f8c`), the parent successor joint (`0e61063`), and the fourteen orchestrator commits (`d694de1`, `7b5b7cc`, `79991d6`, `10ceecf`, `95c759c`, `80fbdcf`, `c434745`, `472767f`, `7329f3e`, `bbb840f`, `03f4b74`, `9b2d79f`, `88d4c3e`, `aa7282f`, all `.agent_workspace/**`-only) — verified per-commit with `git show --stat`, not taken from slot reports. No file outside `.agent_workspace/` was changed by this review; scratch outputs lived in `/tmp` (`/tmp/r12-gate.log`, `/tmp/r12-crash-probe.log`, `/tmp/routes-{base,head}.txt`).

## 1. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R12-1 | **ACCEPT** (two disclosed judgement calls, both ratified) | Both signed-off mandates landed in one diff, exactly at the prescribed seams and with the substance the sign-off demanded. **Mandate A:** the writer records from three sources, monotone first-write-wins — (1) the caller's own `input.childTasks` on the start path (`plannedTaskCriteria`, empty caller lists recorded as known-none), (2) non-empty logged `TASK_REQUEST`s via `advanceTaskCriteria` on every checkpoint write, (3) the checkpoint's existing record on every restore — and the reader (`withRecordedCriteria`) fills **only** the specs `childTasksFromLog` had to substitute for, at its sole call site, leaving any logged request's answer alone. The sign-off's prohibition held: `ctx.childByTaskId` is never a record source (start seeds from caller specs, resume seeds only from the durable checkpoint — read in the diff). The empty-logged-request guard closes the resume-side laundering **behaviourally**: the new resume test's final assertion proves a node that ran under a substituted empty list and logged it is *still absent* from the record after the resume's own checkpoint write. `plannedTaskCriteria` is a dispatch fact recorded early, not synthesis — the arrays are byte-identical to what `buildTaskRequest` copies verbatim into each `TASK_REQUEST` (`child-coordinator.ts:392`, reviewer-verified), nothing reads the episode/definition/contract, and R11-10's census discriminator (constructs **and** `readsEpisode`) passes 5/5 at HEAD (reviewer-run). **Mandate B:** `onRunStarted` fires at the exact signed-off moment — inside `withRunLifecycleLock` (wrap at `flowchart-run.ts:1225`), immediately after the `RUN_CREATED` append (`:1285`), before the loop's first pause poll (`:1009`, reached only after `persistCheckpoint`); not overloaded onto `PauseController` (one-way, nothing read back); the behavioural pure-CLI pause seeds the token from a synchronous `stdout` handler with zero races/kills/timers. Judgement call 1 (§4.4): the abort-test joint folded into the landing commit — content- and process-correct. Judgement call 2 (§4.1): the third writer source beyond the sign-off's two — necessary (the two named sources are provably disjoint from the reader's domain) and boundary-respecting. Both call-site pins in `flowchart-run-abort.test.ts` (`childTasksFromLog(…) : []`, `childByTaskId = childTaskMap(rebuilt)`) match **byte-for-byte** through the `fromLog`/`rebuilt` renaming (reviewer re-matched). Honest residuals: the unblock carry-forward is a source pin, not behavioural (§6.2); two stale source docstrings prescribed, not edited (§6.1) |
| R12-2 | **ACCEPT** | Additive 25-line successor pin, right target: names `src/run/flowchart-run.ts` explicitly, proves it sits inside the recursive whole-`src` census, requires zero `independentEvidence` mentions in the spine (reads, writes, and prose drive-bys), and mutation-proves the census bites by injecting an in-memory reader into the real spine source and asserting the exact-one-`void` check rejects it. Landed 00:48:30 — *before* R12-1's spine edit, so it guarded the round in flight. Whole-`src` census re-run by this review at HEAD: declaration (`prescore.ts:29`), the one `void` (`:89`), the sole write (`from-child.ts:228`) — exactly one dereference, and it is the `void` |
| R12-3 | **ACCEPT** | The measured-reachability analogue review §6.4 asked for, delivered stronger than the brief predicted. One new file, +4 tests: a child reports the whole task PASSED with one evidence-backed FAILED criterion → the node lands **COMPLETED** and the run lands **BLOCKED** with exactly one hard `TRACKING_ASSESSMENT` whose `gate.codes` deep-equals `["unmet-acceptance-criterion"]` (whole-array, ruling out any earlier-ordered code doing the work), exactly one `GATE_TRANSITION` carrying the code, `queue_analysis`, the child's `turnId`, and the **criterion's own** evidence id → the no-retry `unblock` (a retry request is refused with the exact producer message and writes nothing) → resume to COMPLETED with the completed node not re-driven. Three controls (PASSED / UNOBSERVED / omitted), each asserting the gate *assessed* (`kind: "none"`, `codes: []`, zero transitions) rather than merely that the run completed — unknown-not-unmet measured from production. Non-vacuity mutation-proved out of tree (`/tmp` copy, deleted): severing the channel at `child-tracking.ts`'s wholesale spread turns the block arm COMPLETED while all three controls stay green. Deliberate fixture choices (implementer role, empty request criteria, no success-boast summary) leave exactly one variable. Bonus finding worth its record: the block-behind-a-COMPLETED-node shape measures the "gate wins" rule from the direction the suite could not reach, and establishes that `--discard-executed` is structurally unavailable for this block class (no failed node to name) — a consequence, correctly not a choice |
| R12-4 | **ACCEPT** | Docs only (four files + report); ADR-006 Proposed (line read at HEAD); `docs/decisions/` diff-empty across the range. All four Round-11 false-at-HEAD claims fixed (per-criterion channel ×2, pause dependency ×2, exact-stale-claim census zero matches — spot-checked by this review), Round 11 landings anchored to their real commits, the run-id-at-end gap and no-writer residual recorded as they truly were at the 00:48:38 census, with five timestamped censuses that never assigned uncommitted sibling work a commit id. The standing docs-race recurred on schedule: R12-1 landed at 01:06, sixteen minutes after the docs commit (00:50), leaving the spots in §6.4 stale at HEAD — per the R9-4(b)/R10-3/R11-5 precedent this is the honestly-dated race, not a miss; Round 13's docs slot owns them |
| R12-5 | **ACCEPT** (report-only) | The right-sized no-op with the right reason: `assertNoSchedulerRetry` already parses and visits the **whole** ASTs of both `FLOWCHART_SOURCES` files, so R12-1's new paths were covered automatically the moment they appeared — an assertion naming `taskCriteria`/`onRunStarted`/`loggedTaskRequests` would duplicate the whole-file guarantee and break on implementation-preserving renames. Census taken against R12-1's in-flight diff (the honest tree, not the stale HEAD); owned test 3× green post-diff. Reviewer re-swept at HEAD: zero `applyRetry`/`scheduler.js` matches in `flowchart-run.ts`; the audit called from `discardAuthorization` (producer) and `applyClearingEvent` (restore); `scheduler.ts`/`supervisor.ts` diff-empty |
| R12-6 | **ACCEPT** (report-only) | Correct declination with the discriminator argument verified by this review in the census source: a scope is flagged only when it constructs a per-task criteria field **and** reads the episode (`if (!signals.readsEpisode) continue;`), so R12-1's approved logged-request/caller-spec provenance passes and episode provenance fails — the pin was *already built* to adjudicate exactly this round's writer. Exact spelling + semantic-respelling rule + `ProjectEpisode` sibling pin + mutation proof all intact; `ORIGINAL_RUN_AUTHORITY_FIELDS` keeps all three originals. 5/5 green at HEAD **after** R12-1's diff (reviewer-run — closing the timing caveat the report honestly flagged) |
| R12-7 | **ACCEPT** (report-only) | Right declination: R12-1 added no route string and changed no existing output line, so the five-route AST census, the `INSPECT_SUMMARY` rich-fixture four-key `deepEqual`, and the BLOCKED prefix/three-`next:`/`note:` pins already cover everything the mandate named. Reviewer extracted `DOCTOR_ROUTED_NEXT` + `GENERIC_FAILURE_NEXT` at base and HEAD and **diffed character-exact — identical** through R12-1's `main.ts` edit; `INSPECT_SUMMARY` read directly at `main.ts:1147-1151` — four keys; `blocked-next.test.ts` diff-empty across the range. Four timestamped censuses correctly recorded that R12-1's edit was not yet present — the report's conclusion ("no successor needed unless R12-1 changes a frozen value") was then validated by the parent's green gate at HEAD |
| R12-8 | **ACCEPT** (report-only) | The saturation discipline again, with the strongest evidence form available: the final census ran against HEAD **plus R12-1's working-tree diff** (not the stale commit), found the only new `src` import is type-only `TaskAcceptanceCriteria` on an existing in-closure edge, closure steady at 165 of 215 modules, and the file byte-identical by blob id. Reviewer's own sweep at HEAD concurs: `loadProjectBanditByKey` store + doctor only; `selectArm` definition + `shadow.ts` only; bare `loadProjectBandit` zero `src` matches; signed-off `because` unweakened. 9/9 ×3 |
| R12-9 | **ACCEPT** | One additive exact-vocabulary assertion that closes the real gap it names: R11-9 held the *terminal* trio but allowed an arbitrary new non-terminal member of `RUN_STATUSES`; this freezes all eight signed-off members (`PLANNING`/`RUNNING`/`WAITING_FOR_USER`/`PAUSED`/`BLOCKED`/`COMPLETED`/`FAILED`/`CANCELLED`, read at HEAD by this review), landed 00:48:30 — before R12-1, guarding in flight. R10-10/R11-9 inherited assertions untouched |
| R12-10 | **ACCEPT** | The property census done properly: recursive whole-`src` AST walk for every `materializeCheckpoint` call with a flowchart payload, identifier payloads resolved to their nearest preceding same-function initializer (so the R10-4 *property* survives, not a writer count), vacuity-guarded (`writers.length > 0`), `contract` required on every payload, and the pre-landing `taskCriteria` posture handled with the mention-implies-carriage rule — a payload that names the field must carry it as a property, so R12-1 could not drop a field it adds, while a not-yet-written payload stayed valid. Correctly identified the one thing a source-only conditional cannot do (remember a deleted optional writer), prescribed the exact one-line successor, and stop-and-reported it to the parent rather than squatting on R12-1's file. Landed 00:49:25, before R12-1; green on both sides of the landing by construction |
| Parent joints | **Both correct, both counted.** (1) The abort-test joint **folded into `81f5b81`** — `test/unit/run/flowchart-run-abort.test.ts` was owned by no Round 12 slot and not on R12-1's do-not-edit list, its `[]`-criteria assertion was made false by design by Mandate A, and R12-1 flipped it in the landing commit exactly as Round 11's procedural rule prescribes ("fold into the landing commit, not a follow-up"), with prominent §2 disclosure and a self-contained revertible hunk. Content verified: the fixture's caller spec really carries `crit-integration`/`crit-regression`, the flip restores rather than invents, and the artifacts half (`inputArtifactIds` stays `[]`) is deliberately kept as the restoration/invention discriminator; the R6-2 tripwire and both call-site wiring pins in the same file are byte-untouched. (2) `0e61063` — R12-10's prescribed vacuity-guard successor, applied verbatim (modulo line-wrapping), 7 seconds after `81f5b81`, in the only correct window. **Zero red-tree commit points this round** (Round 11 had one): the fold and the held-successor pattern are precisely the Round 11 §6.6 fix working |

**10 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK.** Two joints — one folded per the rule, one held-and-applied per the prescription — both counted; no red-tree window. Round 11's PROGRESS chimera dispatch id for R11-2 was fixed (`…82a1-5523-9ef1-cae7b3339bb7` now recorded — verified).

## 2. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0: 1947 tests / 1946 pass / 0 fail / 1 skipped** — matches the parent's recorded numbers exactly. The one skip is the standing `PI_SMOKE` real-provider gate (`PiAgentExecutor completes a run against a real provider # SKIP`); grep of the TAP output finds exactly one `# SKIP` line — no slot introduced a second. Engine warning only (`>=22.19.0` wanted, v22.14.0 present).
- **`node scripts/crash-probe.mjs` → `ok: true`, 11 cases × 3 iterations, exit 0** — matches. The original ten names and their order are unchanged; `unblock-discard-append-before-checkpoint-sigkill` last. Nobody changed the case set this round; the `persist/` pin file is diff-empty.
- **The +9 over Round 11's 1938/1937/1 closes per-slot.** Static test-registration deltas per changed test file, base `2767321` vs HEAD: R12-1 **+2** (`m2.5/resume` 19→21; `flowchart-run-abort` 23→23, in-place edit) · R12-2 **+1** (`independent-evidence-posture` 5→6) · R12-3 **+4** (new `criteria-gate.test.ts`: one block test + three loop-registered control arms, 4/4 in its 3× runs) · R12-4 **0** (docs) · R12-5/6/7/8 **0** (report-only; R12-8 byte-identical by blob id) · R12-9 **+1** (`terminal-replay-statuses-freeze` 3→4) · R12-10 **+1** (new `checkpoint-writer-carriage.test.ts`, 1) · parent successor **0** (assertion inside the existing test). Sum: exactly 9.
- **Commit hygiene:** each landing touches only its owned files plus its own report — verified per-commit with `git show --stat`. The one cross-ownership edit is the disclosed abort-test fold inside `81f5b81` (§1 joints row). The range's entire `src` diff is exactly R12-1's three signed-off files (`flowchart-run.ts` +187/−9, `main.ts` +7, `track/loop.ts` +13/−1). Load-bearing sequencing held: R12-2/R12-9/R12-10 all landed *before* R12-1 and guarded it in flight; the successor landed 7 s after. `package.json`/`pnpm-lock.yaml` diff-empty.

## 3. Freeze check

All verified against the actual range diff and the tree at HEAD, not report claims:

- **Isolation:** reviewer's own sweep — `loadProjectBanditByKey` exactly store declaration (`bandit-store.ts:172`) + doctor import/call (`doctor.ts:17,467`); `selectArm` exactly `routing/bandit.ts:76` + `routing/shadow.ts:3,96`; `\bloadProjectBandit\b` zero `src` matches. ADR-006 **Proposed** (`0006…md:5` read directly); `docs/decisions/` diff-empty across the range. R12-8's 165-module closure census corroborates against the final tree.
- **`independentEvidence`:** whole-`src` census at HEAD — declaration (`prescore.ts:29`), the single load-bearing `void` (`prescore.ts:89`), the object-literal write (`from-child.ts:228`). Exactly one dereference, and it is the `void`. `flowchart-run.ts` contains zero mentions, now pinned directly by R12-2 with an injected-reader mutation proof.
- **`TERMINAL_REPLAY_STATUSES`:** read at HEAD (`replay.ts:326-330`) — still exactly COMPLETED/FAILED/BLOCKED; the vocabulary above it is now frozen at the exact eight members (R12-9). No `RUN_UNBLOCKED*` is a status.
- **`RUN_UNBLOCKED`:** exact-keyed `{ blockedEventId, reason, retryNodeId }` (`events.ts:856`, allowed-list read directly); `RUN_UNBLOCKED_WITH_DISCARD` still its own exact-keyed four-field validator (`events.ts:880`). `events.ts` diff-empty across the round. R12-3's block test additionally deep-equals a live two-key authorization payload.
- **`applyRetry`:** definition in `scheduler.ts`, production import/call only in `run/supervisor.ts`; both diff-empty; zero `applyRetry`/`scheduler.js` matches in `flowchart-run.ts` after its 187-line diff; R8-3/R10-8/R11-8 pins green in the independent gate.
- **Restore-side discard audit:** read in source at HEAD — `applyClearingEvent`'s consequence-set recompute and R10-1's exact wrong-set throw *precede* the `assertDiscardAuditMatchesLog` call (set before sums); the audit is called from `discardAuthorization` (producer) and `applyClearingEvent` (restore), matching the AST pin's allowed set.
- **Five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT`:** extracted at base and HEAD and **diffed character-exact — identical**, through R12-1's `main.ts` edit; R9-7's AST freeze green in the independent gate.
- **`INSPECT_SUMMARY`:** literal read directly at `main.ts:1147-1151` — four keys. **BLOCKED four lines:** `blocked-next.test.ts` diff-empty across the range; formatter prefix + three-`next:` ceiling + `note:` discard disclosure green in the gate. `onRunStarted` added one *new* stdout line on the track path only (`main.ts:876` is the sole wiring, verified) — no existing output line changed.
- **Episode boundary:** `episode-bind.ts` diff-empty; R11-10's census re-run by this review at HEAD after R12-1's writer — 5/5 green; the discriminator verified in census source (flags construct-**and**-read-episode, so logged-request/caller-spec provenance passes by design, not by accident).
- **Writer carriage:** both flowchart-payload `materializeCheckpoint` call sites at HEAD carry `contract` (read in the diff: `persistCheckpoint`'s spread and the unblock reopen's spread are byte-identical to Round 11), and both now carry `taskCriteria` behind the same conditional-spread shape; R12-10's property census requires `contract` everywhere, requires carriage wherever `taskCriteria` is mentioned, and — since `0e61063` — requires that at least one `taskCriteria` writer exists, so the field can no longer silently lose its last writer.
- **Verdict-producer freezes:** `report-task-result.test.ts`, `unblock-flow.test.ts`, `replay.test.ts`, `option-a-preconditions.test.ts`, `loopback-cli-resume.test.ts` (stderr byte-pin) — all diff-empty across the range, all green in the independent gate. The 7th pin needed no truth-up: it never claimed there was no writer (zero "writer" matches in the file, reviewer-grepped), which is exactly why R12-1's declination to edit it was correct.
- **jsonl / `writeFileAtomic` / no new private tmp+rename:** the round's entire `src` diff adds **zero** raw fs write primitives (grep of added lines for `writeFile`/`appendFile`/`rename`/`createWriteStream` + Sync variants: empty). The new test's direct `pause.json` write is test-side, satisfies the production reader (`PauseController.token` throws on malformed bytes), and is justified in-source by two verified properties of the shipped code (async `requestPause`; the run's own cooperative lock). No dependency changes.
- **`coverageOutcome` / prescore:** `prescore.ts` diff-empty this round (its stale comment is §6.1's debt, not a change); FAIL-unreachable tripwire green; R12-1's residual 4 (resumed tester nodes can now move `check-coverage` PASS→UNOBSERVED because they are re-asked for criteria) moves records and the numeric prescore only, never the directive — the intended consequence of the approved semantic, and no run changed status anywhere in the gate.

## 4. Requested target verifications

### 4.1 `plannedTaskCriteria` — dispatch fact or synthesis, and the third-source judgement call

**Dispatch fact, and the judgement call is ratified.** The sign-off named two writer sources (logged `TASK_REQUEST`s; first-write-wins against the checkpoint record). R12-1 built both and then proved — in source, and the reviewer re-verified each step — that those two alone make the reader a no-op: a `TASK_REQUEST` reaches the log only at attempt start (`child-coordinator.ts:645-651`, the `CHILD_MESSAGE` append immediately after `AGENT_STARTED`), so on any one log "the record knows task T" implies "the log has a request for T", while the reader consults the record only in the substitution branch — exactly the tasks the log has *no* request for. Disjoint sets; zero reads, forever. Meanwhile the bug is real on two verified paths (pause at round 1 polls before any dispatch; the abort test's crash-before-acceptance fixture). The third source — the caller's `input.childTasks` on the start path only — records the identical arrays `buildTaskRequest` copies verbatim into each `TASK_REQUEST` (`child-coordinator.ts:392`), i.e. a dispatch fact captured when the run accepts the specs instead of when a child starts. It reads nothing from the episode, the flowchart definition, or the run contract (whole-diff read; R11-10's census green at HEAD). The forbidden thing — writing `ctx.childByTaskId` substitution empties — was **not done**: the start path derives from `plannedChildren` directly, and the resume path seeds the context's record *only* from `checkpoint.flowchart.taskCriteria`, never from the rebuild. The report flagged the extension for the parent's eye rather than burying it (§3), which is the disclosure discipline working; the parent committed it with the mechanism named in the commit message.

### 4.2 The empty-logged-request guard — laundering prevention, proven behaviourally

Yes, and this is the subtlest correct decision in the diff. On the log, a caller who genuinely asked for no criteria and a node the rebuild substituted for are byte-identical (both log `acceptanceCriteria: []`). Without the guard, the resume's own `persistCheckpoint` would absorb the substituted node's empty list as a durable known-none — the laundering one step removed, through the writer the field gained to *stop* laundering. With it, only the caller's spec can say known-none (recorded empty-list-included by `plannedTaskCriteria`), and the unrecorded node stays unknown forever. The proof is behavioural, not just mutation-claimed: the new resume test runs `tsk_unspecified` (a flowchart node with no caller spec) through a real pause → resume → COMPLETED, confirms it *dispatched* with `[]` and *logged* that, then asserts the post-resume checkpoint **still omits it**. The slot's mutation table row (drop the guard → the node is absorbed as known-none) matches what that assertion would catch. One accepted precision cost, correctly in the fail-toward-unknown direction: a dynamically spawned child (hard-coded `acceptanceCriteria: []` at `flowchart-run.ts:594`) is genuinely dispatched with none but stays *unknown* rather than known-none, because no caller spec vouches for it. Never inventing beats occasionally under-claiming.

### 4.3 `onRunStarted` — ordering, separation, and the swallow

**Ordering verified in source:** `startFlowchartRun` wraps `startLockedFlowchartRun` in `withRunLifecycleLock` (`flowchart-run.ts:1225-1228`), so when the callback fires at `:1285` — immediately after the `PROJECT_DISCOVERED`/`RUN_CREATED` appends that create the run directory — the lock is held; the loop's first pause poll (`pauseIfRequested`, `:1009`) runs only after `persistCheckpoint` (`:1360`) inside `runFlowchartLoop` (`:1362`). After dir + lock + RUN_CREATED, before round 1's poll, exactly as signed off. **Not overloaded onto `PauseController`:** one-way notification, no return value, nothing read back; stopping still goes only through the controller. **The swallow is fail-closed honest** — for the state, which is the thing that must fail closed: if the throw propagated, it would kill a run that has appended records but written no checkpoint, and all three restore paths refuse exactly that state (`no durable checkpoint; refusing to invent state` — three sites read at HEAD), making the orphan unrecoverable by any command. Swallowing degrades only the *notification*: an operator whose sink throws misses the early id and is exactly where every operator was before this round — no worse, and the run stays pausable, resumable, and honest. The reasoning is stated in-source at the call site. The behavioural test needs no race because the run is suspended inside the synchronous callback — the token write is strictly ordered ahead of the first poll by construction, and the pause is then confirmed *read* (no `CHILD_MESSAGE`, a `PAUSE_REQUESTED` row, exit 1, `PAUSED` in stdout).

### 4.4 The folded abort-test joint — content and process

**Content-correct:** the fixture's caller spec really dispatches both nodes with `crit-integration`/`crit-regression` (read at HEAD, `flowchart-run-abort.test.ts:886-888`); node b was unstarted at the crash, so pre-R12-1 its resume attempts honestly asserted `[]`, and post-R12-1 the durable record — written when the caller's specs were accepted — re-asks for exactly those two ids. The flip is a restoration, and the kept `inputArtifactIds: []` assertion (artifacts are deliberately not on the record) is what proves nothing beyond the record came back. The strengthened messages and the 20-line comment disclose the flip with its round tag in-place. The same file's R6-2 tripwire, budget-substitution pins, and both character-exact wiring pins are byte-untouched — R12-1's `fromLog`→`rebuilt` plumbing exists specifically so `const childByTaskId = childTaskMap(rebuilt);` and the sole-call-site regex still match, and the reviewer re-matched both. **Process-correct:** Round 11's rule was "either the obligation's file joins the obligated slot's ownership, or the joint is planned and folded into the landing commit, not appended after it." The file had no Round 12 owner, was not on R12-1's do-not-edit list, and the flip landed *atomically inside* `81f5b81` — which is why this round has zero red-tree commit points where Round 11 had one. The report's §2 offer to split the hunk out was the right courtesy; the parent's commit answered it.

### 4.5 R12-1's residual — the unblock carry-forward

Accurately labelled: the reopen writer's `...(taskCriteria !== undefined ? { taskCriteria } : {})` is held by a character-exact source pin in `resume.test.ts` and by R12-10's AST property (mention-implies-carriage on that payload), but no test drives a BLOCKED-with-recorded-criteria run through `unblock` and reads the record off the rewritten checkpoint. This is the same posture `contract` had before R10-4 gave it a behavioural pin, and the closure is now cheap: R12-3's new block fixture *is* a BLOCKED run whose checkpoint carries a `taskCriteria` record (the caller spec's known-none entry), so one storedTaskCriteria assertion pair around its existing unblock+resume would do it. Round 13 candidate (§6.2), correctly not smuggled into either slot's diff this round.

## 5. Per-slot notes

- **R12-1** (opus): The best landing of the loop so far, and the model for what "spine slot" should mean. The disjointness proof in §3 of its report is the reasoning that separates a mandate executed from a mandate understood: it found that the sign-off as written would have shipped a validated no-op, said so before anything else, built the correction inside the same never-synthesize boundary the sign-off was protecting, and asked to be checked rather than believed. Nine mutation checks with per-mutation red-test counts, honest per-file transient attribution, and a residual list that names its own weakest seam. The `fromLog`/`rebuilt` rename to keep another file's byte-exact pins green without touching them is shared-tree craftsmanship.
- **R12-3** (opus): Reading the source end-to-end *before* writing (predicting the shape rather than discovering it) shows in the result: the COMPLETED-node/BLOCKED-run disagreement was found, not stumbled on, and the whole-array `gate.codes` equality plus the assessed-not-skipped control assertions close the two classic vacuity holes in reachability tests. Out-of-tree mutation with the controls staying green is the correct shared-tree etiquette.
- **R12-4** (gpt-sol): The five-census pattern held; retaining the no-writer residual because R12-1 had not landed *at census time* is exactly right, and exactly why the docs are stale again at HEAD — the recurring structural cost of docs-in-parallel, priced in and owned by Round 13.
- **R12-2 / R12-5 / R12-6 / R12-7 / R12-8 / R12-9 / R12-10** (gpt-sol): Six of seven were guards for a spine landing mid-flight, and the discipline shows: three landed *before* the spine and guarded it live (R12-2, R12-9, R12-10), three verified against the spine's in-flight working diff rather than a stale HEAD (R12-5, R12-6, R12-8), and every declination names the existing property that makes the addition a duplicate. R12-10's mention-implies-carriage conditional — green before the writer, strict after it, with the successor prescribed rather than squatted — is the cleanest cross-slot handoff this loop has produced.

**Nits recorded, none rising to a qualifier:** the docs re-staleness (§6.4 — process cost, priced); R12-1's report placing the folded-joint hunk offer on the parent (answered by the commit); `advanceTaskCriteria`'s log-derived merge arm currently has no behavioural discriminator in a fresh-tree run (§6.3).

## 6. New findings for Round 13

1. **Two source docstrings are now false, and only prose — no pin asserts them (reviewer-grepped the test tree: zero matches).** `src/run/replay.ts:107-110` ("No `src` writer fills this yet…") and `src/tracking/prescore.ts:174` ("…declared and validated but has no writer yet", plus obligation 1's "resumed child specs are re-synthesised with empty criteria", now true only of unrecorded nodes). R12-1's report §8 prescribes replacement text that keeps every pinned sentence (`never *synthesized*`, `not from the bound episode`) intact. Comment-only `src` edits: they need a slot that owns those two files with a pins-first census (the option-(a) pins match `replay.ts` prose; the criteria-are-guidance record pin matches `prescore.ts` prose).
2. **The unblock carry-forward deserves its behavioural pin, and the fixture now exists.** §4.5: R12-3's criteria-gate block is a BLOCKED run whose checkpoint records `taskCriteria` (the caller's known-none entry); assert the record on the checkpoint after its unblock and after the resume. Two assertions, one existing file, closes the last source-pin-only seam on the field.
3. **`advanceTaskCriteria`'s log-derived arm (the sign-off's source 1) has no behavioural discriminator in a fresh-tree run.** In current code the start seed always pre-records every caller task and spawned children log empty (ignored), so the request-merge arm adds an entry only for a run whose checkpoint predates the writer — the legacy-recovery path. One test that hand-strips `taskCriteria` from a checkpoint (schema-valid — absence is unknown), resumes, and asserts the record recovers the logged non-empty requests would prove the arm and document the recovery semantics for pre-Round-12 runs.
4. **Docs at HEAD carry the recurring race staleness** (all true when written at 00:50, superseded by R12-1 at 01:06): `docs/status-matrix.md:36` ("A node never requested keeps empty criteria/artifacts" — now false for recorded criteria; and the row's "At the Round 11 close no `src` writer populates it" reads as current state), `:60-65` (no-writer residual + run-id-at-end gap, both closed for the track path), `docs/specs/m0-m2-architecture.md:367` and `:553` (same no-writer claims). Plus Round 12's own landings to record as commits.
5. **`--flowchart` and `--children` still print their run id only at the end** — the same operator gap `onRunStarted` closed for `--track`, deliberately out of R12-1's scope because those paths' stdout is censused by `blocked-next.test.ts` (R12-7's file this round). One line per path plus the output-census updates, in one slot that owns `main.ts` *and* `blocked-next.test.ts` together.
6. **Recorded decisions, not reopened without new evidence:** the fail-toward-unknown spawned-child precision cost (§4.2); no `continuation.taskCriteria` (a caller may not re-answer a dispatch record — negatively pinned); the onRunStarted swallow (state fail-closed, notification best-effort — reasoned in-source); check-coverage PASS→UNOBSERVED movement for resumed testers (intended consequence, prescore-only); `--discard-executed` unavailable for criterion blocks behind COMPLETED nodes (structural, recorded by R12-3); the audit completeness limit, the unasked-criterion-id acceptance, the role-gate C4 asymmetry, and `derivedClaimedVerificationWithoutChecks` kept-not-widened (all standing).

## 7. Handoff

- Gate GREEN at `aa7282f`: 1947/1946/0/1 (`PI_SMOKE` only), crash-probe 11×3 `ok: true` — both re-run independently on this VM and matching the parent's record exactly. The +9 closes per-slot (§2).
- The Round 13 injection brief is at `.agent_workspace/ROUND12-BRIEF.md`: **four real ranked candidates** (fewer than ten — stated explicitly; the freeze surface remains saturated and the brief does not manufacture busywork), the grown frozen-contract list (the `taskCriteria` writer as shipped — three sources, first-write-wins, empty-logged ignored, substitutions-only reader; `onRunStarted` as shipped; the eight-status vocabulary; the writer-existence guard), the file-contention map, and the standing prohibitions.
- Nothing is left red at HEAD. Both joints are counted, explained, and correct in content; the round executed Round 11's procedural fix (fold, don't follow up; hold the successor, don't pre-land it) and produced zero red-tree commit points.
