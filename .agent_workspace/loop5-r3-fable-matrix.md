# Loop 5 · Round 3 — Fable-status-matrix: proposed rows for `list` / `validate` / `init` / `commits` / `adapt show`+`dataset`

Slot: Fable-status-matrix (claude-fable-5-thinking-xhigh). Analysis + proposal only; no `src/` edits, no `docs/status-matrix.md` edit, no commit by this agent.

Proposed at HEAD `07ffa00` (`docs(agent): record Loop 5 Round 2 closeout and Round 3 dispatch`) on `cursor/pi-sparkle-sota-opt-0da8`. Method: direct reads of `src/cli/{list,validate,init-examples,commits,adapt,main}.ts`, `src/run/inventory.ts`, `src/learning/eval-dataset.ts`, `src/tools/decision-commit.ts`, `src/privacy/record-classes.ts` (routing-eval-dataset entry), the seven relevant test files (test names enumerated, not just globbed), `.github/workflows/ci.yml` at HEAD, README's command table, `docs/data-dictionary.md`, and the R2 aux report (`loop5-r2-fable-aux.md` §3, which drafted three of these rows at `826a44a`). This report supersedes that draft: it adds the `commits` row in full, adds the now-landed `adapt show`/`dataset` row (R2 `3d9f013`), and refreshes every claim against Round 2's landings.

---

## 1. Why this report does not edit `docs/status-matrix.md`

The matrix opens with the Round 15 sole-docs-slot census note (lines 14–26), whose terminator is a standing contract: *"Subsequent rounds need a new census note only when a landing changes what these surfaces describe."* Adding five capability rows is exactly such a landing, so the diff that lands them must also replace the census note — and a census note is only honest when its author can attest **HEAD, a clean tree, and no sibling landing in flight**, and can align every file the new rows implicate. None of that is ownable from this slot right now:

1. **Round 3 siblings are in flight** (Opus-auth, Opus-gate, Opus-review-verdict, Fable-windows-ci at minimum), several of which edit surfaces these rows describe (blocked-run report, promotion review, CI smoke). Any census I stamped would be stale before it merged.
2. **PR #12 is still OPEN and edits `docs/status-matrix.md`** (re-verified via `gh` at this HEAD). The R1 review and R2 aux both set the merge-order rule: matrix edits sequence after #12 or are reconciled by whoever merges second. That reconciliation belongs to the parent, not a parallel slot.
3. **The rows create alignment obligations in files other slots own this round** (§4): README's stale `adapt eval` sentence, the top-level USAGE's missing `adapt show`/`dataset` lines, and the CI-smoke clause that Fable-windows-ci may invalidate mid-round.

So: exact copy-paste-ready rows below, plus the census replacement text and the alignment checklist the landing diff must carry.

## 2. Proposed rows

### 2.1 Runtime line (M0–M2.5) — four new rows

Placement: insert as a block **after the `doctor` row and before `Retention bounds`** (they are operator tooling of the same family as doctor; `Retention bounds` stays the section's only `no`-Present row and keeps its position as the closing caveat). Order below is the brief's: list, validate, init, commits.

| Capability | Present | Wired | Exercised | Outcome-supported | Notes |
|---|---|---|---|---|---|
| `list` (runs + episodes) | yes | yes | unit (`test/unit/cli/list.test.ts`, `test/unit/run/inventory.test.ts`) + dispatch pins (`test/unit/cli/main-dispatch.test.ts`); Linux CI only — the Windows `cli-smoke` job does not run these verbs | no | Read-only inventory of a state root (Loop 5 R1, `679553e`). Runs: every `runtime/runs/<id>/events.jsonl` replayed through the shipped `replayRun`, so all eight `RunStatus` values are listable — including the terminal runs doctor's PLANNING/RUNNING `runStates` inventory deliberately omits; rows carry status, last-event time, and the bound episode id. Episodes: each `runtime/episodes/<epId>.jsonl` at its latest validated snapshot (event logs and `.lock` sidecars are not episode records and are skipped by name). Ordered by id; empty or missing directories are an empty inventory with exit 0 (a fresh install is not an error), while any other scan failure throws. Unreadable records are reported, never silently skipped and never fatal: one stderr `warning: list incomplete: …` plus an `errors[]` listing, readable rows still print, exit stays 0 — an operator listing a state root is usually looking *for* the broken run. `--status` filters runs by replayed status and is **refused** with `--episodes` (an episode carries an `EpisodeStatus`; silently listing everything would be worse), as is `--runs --episodes`. `--json` prints exactly one frozen-additive `RUN_LIST`/`EPISODE_LIST` object (`preview: true`; not a domain `Event` — no `id`, type outside the `Event` union), same posture as doctor `--json` / `INSPECT_SUMMARY` / `VALIDATE_OK`. The run-not-found remedies of `inspect`/`resume`/`answer` route here; `commits`' does not yet (R2 aux C3, queued). |
| `validate` (children + flowchart) | yes | yes | unit (`test/unit/cli/validate.test.ts`), incl. a no-write guard and `VALIDATE_OK` exact-shape pins; Linux CI only (the no-write guard's `HOME` redirect proves nothing on Windows — R2 aux §2.2) | no | Spec check without a run (Loop 5 R1; live-catalog parity `42b4c6c` after GPT challenge). Exactly one of `--children`/`--flowchart`. Creates no run, writes nothing under any state root, opens no provider connection, executes no task: it runs the same parsers the run path runs and reports what they said. `--children` is parsed exactly as `run --children` parses it (`parseChildSpec`) then compiled with `compileChildrenToFlowchart`, so a cycle, self-dependency, or missing dependency is refused here; the compile uses the default cheap/premium policy and its USAGE says honestly that this checks no catalog and no model availability. `--flowchart` runs the flowchart validator **plus the same live-catalog check `run --flowchart` applies**, against the catalog built from the providers config under `--state-root` (default `~/.pi-sparkle`) — a static list here would refuse flowcharts a run accepts and accept ones a run refuses. Reading that config creates nothing; a broken catalog is reported as a catalog problem (`models list` remedy), never as a spec problem. Success `--json` is one frozen-additive `VALIDATE_OK` object (`type`, `preview`, `kind`, `path`, `taskCount`, `nodeCount`, `edgeCount`, `flowchartId`, `catalogSource`, `stateRoot`; the last two present for `--flowchart` only, and `catalogSource` is only ever `"live"`); failure prints the stderr error report and exits 1 — never a half-shaped `VALIDATE_OK`. |
| `init` example scaffold | yes | yes | unit (`test/unit/cli/init-examples.test.ts`), incl. the `examples/` byte-identity pin and a `--json` shape pin; Linux CI only (the byte pin is CRLF-fragile on a Windows checkout — no `.gitattributes` yet, R2 aux §2.2) | no | Writes `sparkle-children.example.json` and `sparkle-flowchart.example.json` into `--dir` (default cwd; a missing directory is created) from **embedded source constants** — the published package ships `dist` only, so a runtime read of `examples/` would work from a checkout and fail from an install; the repo's `examples/` holds the same bytes for browsers and the unit test pins the two copies together. Both targets are checked before either is written: existing files refuse without `--force` and a partial write cannot occur. No state root is read or written and no run starts; prose output ends with the `next: pi-sparkle validate --children …` handoff, so the shipped on-ramp is init → validate → run. `--json` prints an `INIT_EXAMPLES` object (`preview: true`) that is currently the CLI's only pretty-printed machine surface — compaction to one line is queued with the `commits --json` typing fix (R1 review §2.3 / R2 aux C2). D6 decision: KEEP as an example scaffold, not a project cookiecutter. |
| `commits` (decision ledger → git) | yes | yes | integration (`test/integration/cli/commits.test.ts`: real temp git repos, "preview creates no commits" pin, preview-`--json`→`apply --file` round-trip, non-work-tree refusal) + unit (`test/unit/tools/decision-commit.test.ts`); Linux CI only | no | Predates Loop 5 (M3-T15, `b4a7078`) — the row is new, the capability is not. `preview` reads a flowchart run's event log **and durable checkpoint** and emits one conventional proposal per COMPLETED node: `feat`/`fix`/`chore` by node role, `(scope)` from the node, subject from the objective's first line, and `Evidence:`/`Confidence:`/`Run:`/`Model:` trailers from the ledger. Fails closed on a run with no durable checkpoint, a non-flowchart checkpoint, or zero completed nodes; `--nodes` selects and orders a subset and refuses unknown or empty ids. `apply` writes each proposal with `git commit --allow-empty -F -` (`-S` only with `--sign`) into `--repo` or the checkpoint's recorded `project.rootPath`, refusing a non-work-tree with git's own detail; `--file` takes an operator-edited proposal JSON, validated field-by-field before any commit. Apply is sequential and stops at the first git failure: commits already made are real and stay (each printed `Committed …`); no rollback is attempted and no "applied N of M" summary is printed yet (R2 aux C4). Recorded dialect gaps, not hidden: arg/not-found/unknown-subcommand failures are raw stderr without the structured `cliFail` report, and `preview --json` is pretty-printed with no `type`/`preview` keys — both queued as the G4 batch opener (R2 aux C1/C2; safe because `parseDecisionCommitFile` tolerates additive keys). |

### 2.2 Adaptive library line (M3–M6) — one new row, one optional amendment

Placement: insert **after `Promotion CAS + rollback`** — `show` exists to feed `promote`, and `dataset` feeds the `adapt eval` report that a routing-policy promote requires.

| Capability | Present | Wired | Exercised | Outcome-supported | Notes |
|---|---|---|---|---|---|
| `adapt show` / `adapt dataset` (promote inputs + eval-dataset export) | yes | `adapt show --candidate` / `adapt dataset --run` (Loop 5 R2, `3d9f013`; spellings in `pi-sparkle adapt help`) | unit (`test/unit/cli/adapt.test.ts`: dispatch pins, fail-closed cases, and the full dataset → eval → show → promote chain; `test/unit/learning/eval-dataset.test.ts`: determinism, redaction, isolation); Linux CI only | no | Both are read-only where it counts: `show` loads the registry and never writes it, `dataset` writes only the dataset directory — no registry mutation, no bandit file, no policy. `show` prints the exact promote inputs nothing else printed: the stored candidate content (`--content-file` dumps the bytes `promote --content-file` must hash-match) and the review `actorId` the promotion review must carry, plus the active version (`promote --expected`); a candidate whose content the snapshot no longer stores is refused as unpromotable. `show --json` is one frozen-additive `ADAPT_CANDIDATE` object (`preview: true`, not a domain `Event`). `dataset` exports the replay dataset `adapt eval --dataset` consumes from one run's event log to `adaptation/eval-datasets/<runId>/` (or `--dir`): task ids, agent role, task family, deterministic PASS/FAIL, and a ≤500-char `redactSensitiveText`-scrubbed objective excerpt; prompts, tool payloads, model output, and bandit state never reach it, and the live policy is never consulted. Fails closed rather than inventing rows (no project snapshot, no routed PASS/FAIL, no recorded objective), counts skipped-objective tasks on stderr instead of fabricating them, exports a retried task once at its final recorded outcome, re-exports byte-identically, and refuses a dataset dir inside the recorded workspace. New `routing-eval-dataset` privacy record class: the objective excerpt is the only user text and arrives redacted; `delete --run` does **not** cascade into an exported dataset — the record class says so rather than claiming a cascade the tooling does not perform. This closes the "eval still needs a dataset the auto-loop does not yet export" gap (see §4 riders). |

Optional one-clause amendment to the existing `Promotion CAS + rollback` row's Notes (currently just "Proposal-first."), same diff: `Proposal-first. Since R2, adapt show prints the required review actorId and content, and adapt dataset + adapt eval produce the --eval-file a routing-policy promote requires — the promote workflow is completable without hand-assembled blobs.`

## 3. Census note replacement the landing diff must carry

The Round 15 note's closing sentence obligates a new census when these rows land. Proposed replacement for the whole blockquote (placeholders in `<>` to be filled by the landing agent at their verified HEAD):

> Loop 5 docs-slot working-tree census (<UTC timestamp>): HEAD was `<sha>`, with a clean tree and no sibling landing in flight. This census supersedes Round 15's (Loop 4): the Round 14 landings it recorded remain committed and unchanged. Loop 5 added operator verbs without touching frozen honesty contracts: `list`/`init` (`679553e`), `validate` with run-parity live-catalog checking (`2c23a2c` + `42b4c6c`), track-clarification inspect and refuse-answer (`ac9e02a`), delete two-half disclosure (`9872d93`), and `adapt show`/`dataset` (`3d9f013`); `commits` (M3-T15, `b4a7078`) is rowed for the first time. This census is current at HEAD because no sibling is in flight. Subsequent rounds need a new census note only when a landing changes what these surfaces describe.

The `<sha>`/timestamp discipline is the point: whoever lands must re-verify "no sibling in flight" at that moment, which is exactly what this slot cannot do mid-Round-3.

## 4. Alignment obligations riding the landing diff (files this slot cannot own)

1. **README command table, line 181**: "`adapt eval` / `adapt rollback` … eval still needs a dataset the auto-loop does not yet export" is **stale since `3d9f013`** — `adapt dataset` exports it. The row should gain `adapt show` / `adapt dataset` entries and drop the "does not yet export" clause. (PR #12 ships `readme-command-parity.test.ts`; check whether it fails on the missing verbs post-merge.)
2. **Top-level USAGE in `src/cli/main.ts`** lists `adapt status|learn|auto|eval|promote|rollback` but not `show`/`dataset`; they exist only in `ADAPT_USAGE`. Either add the two lines (src edit — not this slot) or keep the matrix row's "spellings in `pi-sparkle adapt help`" hedge, which is written to stay honest in both worlds.
3. **"Linux CI only" clauses**: verified true at this HEAD (`ci.yml` `cli-smoke` runs only `version`+`help` plus `cli-children`/`doctor` tests on Windows). Fable-windows-ci's Round 3 slot may land the R2 aux §2.3 smoke lines; if it merges first, drop the clause from `list`/`validate`/`init` in the same diff (the clause is per-row deliberate: Exercised states where the proof ran).
4. **Sequencing**: PR #12 (OPEN) edits `docs/status-matrix.md`; land after it merges or reconcile explicitly.
5. Not required but adjacent: `docs/data-dictionary.md` already carries the `routing-eval-dataset` row (landed with `3d9f013`) — no action; cited here so the census author doesn't re-derive it.

## 5. Fact base (what each nonobvious row claim was verified against)

- `list`: `src/cli/list.ts` (frozen `ListJson`, `--status`+`--episodes` refusal, exit-0-with-errors), `src/run/inventory.ts` (ENOENT-as-empty vs throw, `.events`/`.lock` filtering, empty-log skip); 13 unit tests + 6 inventory tests + 5 dispatch tests enumerated. `missingRun` in `main.ts` routes inspect/resume/answer not-found to `list`.
- `validate`: `src/cli/validate.ts` (exactly-one refusal, catalog-vs-spec error split, `catalogSource: "live"` only, no-VALIDATE_OK-on-failure comment); 12 unit tests enumerated incl. no-write guard, cross-state-root accept/refuse pair, and exact-shape pins.
- `init`: `src/cli/init-examples.ts` (embedded constants rationale, check-both-before-write, `next:` handoff); 8 unit tests enumerated incl. byte-identity and `--json` pins.
- `commits`: `src/cli/commits.ts` + `src/tools/decision-commit.ts` (`gitCommitArgs` → `--allow-empty -F -`, `-S` gated on sign; `formatCommitMessage` trailers; `validateDecisionCommitProposal` field checks; COMPLETED-only node filter; no-flowchart/no-checkpoint/no-completed-nodes refusals); 4 integration + 8 unit tests enumerated. History: sole prior commit `b4a7078`.
- `adapt show`/`dataset`: `src/cli/adapt.ts` (read-only comments, `AdaptShowJson`, unpromotable-content refusal, skipped-objective stderr), `src/learning/eval-dataset.ts` (OBJECTIVE_MAX_CHARS 500, redaction-class recording, fail-closed triple, isolation guard, superseded-attempt counting), `src/privacy/record-classes.ts:238-257` (no-cascade comment); adapt tests incl. `main routes adapt show and adapt dataset` and the chain test; 7 eval-dataset tests enumerated.
- CI: `.github/workflows/ci.yml` at HEAD — `quality` ubuntu-only, `cli-smoke` two steps, neither touching the five surfaces.

No src, test, or `docs/status-matrix.md` edits were made. This file is the slot's sole output.
