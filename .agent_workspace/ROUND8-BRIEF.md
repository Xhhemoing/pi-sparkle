# ROUND 8 BRIEF — injection context for Loop 4 · Round 9

Provenance: written by the Round 8 SOTA reviewer (claude-fable-5-thinking-xhigh) after evidence-based review of R8-1…R8-10 at `b65ad06`; verdicts and verifications in `.agent_workspace/loop4-r8-review.md` (9 ACCEPT, 1 ACCEPT-WITH-NITS, 0 ROLLBACK; zero parent fix-joints; nothing left red). Every candidate below cites evidence the reviewer verified at HEAD against the actual diff, not report hearsay. All ten slots are committed (`05051ac`…`c8d7d15`); parent commits after that touch only `PROGRESS.md`. **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 8 (all committed)

| Slot | Landed | Key symbols (do not re-implement) |
|---|---|---|
| R8-1 | **`RUN_UNBLOCKED` shipped — a BLOCKED run can now be ended, and the reopened work really runs.** Persisted schema exactly as R7-3 designed: `{ blockedEventId, reason, retryNodeId? }`, exact-keyed validation, no evidence field. Replay retains the active terminal and, for BLOCKED, the opening event id; a **matched** unblock clears `sawTerminal` and re-derives status from the pre-terminal ladder (an unblocked run with an outstanding pause replays PAUSED), so `replayedTerminalStatus` goes `undefined` and **every recorder on both planes reopens with zero per-writer edits** — the R6-1/R7-4 seam did its job. Stale / wrong-terminal / unmatched unblocks are three distinct anomalies with the terminal untouched. `TERMINAL_REPLAY_STATUSES` unchanged; `RUN_UNBLOCKED` is not a status. Dedicated **locked** `pi-sparkle unblock` command (`withRunLifecycleLock`, not a fourth injection kind; the three reasons in-source), refusing blank reasons, spent authorizations, and — for a gate block that names a FAILED node — any `--retry-node` other than exactly that node (`resolveRetryTarget`/`gateBlockedFailedNode`; `retryNodeId` is optional by necessity: the stall shape has no node). Producer order transform → append → checkpoint; refusals write nothing; the append-first crash window is recovered idempotently by `unappliedUnblock` (log position vs `checkpoint.lastEventId`) through `restoreCheckpointedSupervisor`, shared by resume **and** pause/inject restore. The flowchart reopen is its own state machine operation, `reopenAfterUnblock`/`reopenBlockedFlowchartSnapshot`: clears the ledger latch for both shapes, rewinds only join-required downstream consequences that are READY/SKIPPED, **fails closed on executed descendants** (disclosed residual, see candidate 3), never touches `applyRetry`/`scheduler.js` (AST-pinned by R8-3; `src/run/scheduler.ts` byte-identical). `gate-apply.ts::currentGateStatus` reads a matched unblock as RUNNING by the same block-id rule. Operator surface: the BLOCKED block is now four routed lines (inspect / inject / **unblock** `next:` + the rewritten resume `note:`), `deepEqual`-pinned with the retired clauses pinned absent; `reportBlockedRun` wired at flowchart `resume` and `answer` with a four-site wiring pin; supervised branch untouched (`loopback-cli-resume.test.ts` byte-identical). Headline integration on the R6-1 seed: BLOCKED → unblock → resume → the node re-executes exactly once → COMPLETED, `["RUN_BLOCKED","RUN_COMPLETED"]`, zero anomalies; unblock spends nothing (`taskIds` `[]` until resume) | `RunUnblockedPayload` in `run/events.ts`; `activeBlockedEventId`/`clearingUnblockEventId` on `ReconstructedRun`; `unblockFlowchartRun`, `unappliedUnblock`, `restoreCheckpointedSupervisor`, `resolveRetryTarget` in `run/flowchart-run.ts`; `reopenAfterUnblock`, `reopenBlockedFlowchartSnapshot`, `FlowchartReopenRequest` in `supervisor/flowchart-supervisor.ts`; `unblockCommand` + the four-line `formatBlockedRunReport` in `cli/main.ts`; 5 replay tests + fuzz seed with 7 named refusals + `unblock-flow.test.ts` (8) + `cli/unblock.test.ts` (6) |
| R8-2 | Test-only half under contention, as signed off: three **current-absence** tripwires in `m2.5/resume.test.ts` (checkpoint/validator/writer/restorer carry no contract; episode binding projects acceptance only; CLI continuation projects no contract) plus an implementation-grade design for the durable contract. The no-contract flip-pin is byte-untouched. Known nit: the third pin's region regex now silently includes R8-1's `unblockCommand` — the implementing slot fixes the boundary when it replaces the pins | the three pins in `test/integration/m2.5/resume.test.ts`; design in `.agent_workspace/loop4-r8-t2.md` §3–4 (writers, restorers, CLI projection, `continuation.contract ?? checkpoint.flowchart.contract` precedence, never-synthesize-from-episode, the `row-fuzz` seed gap) |
| R8-3 | AST-level negative pin: neither `flowchart-run.ts` nor `flowchart-supervisor.ts` may reference `applyRetry` or import `scheduler.js` (static, dynamic, namespace, re-export, computed-property). Mutation-proved in-test against two synthetic regressions. Green at HEAD **with the reopen present in both files** | `test/unit/run/flowchart-applyretry-absence.test.ts` (2) |
| R8-4 | Option (a) design, no `src/**`: it is **four preconditions, not three**, and the fourth dominates — **no shipped executor can produce the verdict the gate admits on** (reviewer re-verified in source: `translatePiEvent` never emits `MESSAGE`; `finish` always synthesizes `UNOBSERVED`; the only `src` producers of scorable PASSED/FAILED are the two fakes, both PASSED; `assessChildObservation` refuses everything else). So the tracking gate has exactly two production-reachable states: not running (`--executor pi`) or always-opening (`--executor fake`). Also measured: cap-to-gate would move 54 of 270 cells, none about criteria (all `progress-vs-stall` FAILs on PASSED children) — `cappedByHardFail` stays display-only, answered with a number. Recommended shapes on file: optional `VerificationResult.criteria` (nested, additive-compatible — a `criteria`-bearing `TASK_RESULT` validates today); a new `unmet-acceptance-criterion` anomaly code fed by an explicit producer fact, firing on observed FAILED only; never-ran nodes stay unknown-not-unmet with durable per-task criteria riding the contract checkpoint seam. Five replace-in-same-diff pins landed | `test/unit/tracking/option-a-preconditions.test.ts` (5); the ship order in `.agent_workspace/loop4-r8-t4.md` §9: producer → checkpoint seam → (unblock, done) → option (a) as one diff |
| R8-5 | Docs truth-up for Round 7 + the unblock contract, censused three times against the working tree with timestamps; ADR-006 untouched and Proposed; one authorized comment-only `supervisor.ts` docstring fix (retired empty-list seed). **Now stale at HEAD** (flagged, not fixed): `status-matrix.md:66` calls R8-9's landed deletion an "in-flight working-tree edit"; the 21:50 UTC sync notes say R8-1's pins "were still being rewritten" (they landed in `05051ac`); `decisions/0005…md:110` still names the deleted `loadProjectBandit` (ADR body — stays flag-only) | `docs/data-dictionary.md`, `docs/status-matrix.md`, `docs/specs/m0-m2-architecture.md`, the tracking plan |
| R8-6 | Option (b) recorded in-source beside `CatalogObservedCorruptError`: the catalog route is defense-in-depth for a future command producer; doctor absorbs the typed error into inventory. The five `DOCTOR_ROUTED_NEXT` routes are character-exact vs baseline (reviewer diffed the extracted map). The bandit half is now pinned through the **real** producer: `adapt auto` end-to-end over truncated bytes at `projectBanditPath(stateRoot, stableProjectKey(projectRoot))` — exit 1, `stage: "validation"`, the complete routed `next:` verbatim, doctor's damaged/learned inventory entry. Obsolete `configurePreferencePersistence` repair dropped | the posture comment in `routing/catalog-observed.ts`; the rewritten producer test in `command-error-doctor.test.ts` |
| R8-7 | The parent plane's two crash residuals **decided and recorded in source with behaviour pins**: (1) the refusal stays terminal-keyed — a crash over a log replaying WAITING_FOR_USER records `RUN_FAILED`, the `waiting` flag is the plane's own wait, the answering channel dies with the process, and the cost (an out-of-band `USER_ANSWER` can no longer clear the buried wait) is stated next to the decision; both widenings named and refused (`recordCrashTerminal` reuse would falsify its frozen rethrow clause). (2) `RUN_CANCEL_REQUESTED` stays unguarded — operator fact, not status claim; cross-plane evidence corrected (supervised `recordCancel`, no flowchart producer exists). `failureReason` write-only residue removed, payload byte-identical. Test 4 makes the recorder-routing obligation mechanical: no `make("RUN_COMPLETED"/"RUN_FAILED")` inside `runParentRun`'s slice outside `recordTerminal` | the two docstrings in `run/coordinator.ts`; `test/unit/run/parent-crash-residuals.test.ts` (4, each mutation-checked) |
| R8-8 | `INSPECT_SUMMARY` **frozen additive, declared and enforced**: exactly `type`/`runId`/`status`/`requiredEvidence`, no `id`, exact-key + `deepEqual` pins on both branches (stalled and clean), a new pin that the type is outside `EVENT_TYPES` and refused by `validateEvent`, `--json` purity untouched. No `src/**` edit; the freeze lives in the test-file header because the producer is an inline literal in R8-1's sole file | `SUMMARY_CONTRACT_KEYS` + 3 strengthened/new pins in `test/unit/run/inspection.test.ts` |
| R8-9 | Root-keyed `loadProjectBandit` **deleted** (no `src` caller existed); eleven test call sites migrated to `loadProjectBanditByKey` + `stableProjectKey`; namespace absence pin (`"loadProjectBandit" in banditStore === false`); `banditPath` retained (live writer still takes `projectRoot`); isolation pin and its widened `loadProjectBandit(?:ByKey)?` sweep untouched; `learnedState` JSON unchanged | the absence pin in `test/unit/learning/bandit-store.test.ts` |
| R8-10 | The bound-episode pre-rounds settle is exercised again: an armable nine-id budget lets the bind succeed and fails the `TASK_GRAPH_ACCEPTED` append; the pin proves `RUN_ATTACHED` present, no graph acceptance, exactly one guarded `RUN_FAILED`, **`afterTerminal === ["EPISODE_CLOSED"]`**, replay/episode/checkpoint FAILED, zero anomalies. R7-9's failed-bind seed untouched | the new test in `test/integration/m2/supervisor-crash.test.ts` |

## 2. Current baseline (this VM, Node v22.14.0)

- **`pnpm gate` GREEN, exit 0**: **1845 tests / 1844 pass / 0 fail / 1 skipped**. The only skip is the `PI_SMOKE=1` real-provider gate. Any slot whose report shows a second skip introduced one and must say so. Delta vs Round 7: +41, accounting closed per-slot in the review §2.
- `node scripts/crash-probe.mjs` → `ok: true`, **9 cases × 3 iterations** — case set unchanged (no Round 8 slot touched the probe). `crash-recovery.test.ts`'s reduced-probe pin still lists all nine by name; anyone changing the case set owns updating that pin in the same slot, with disclosure — candidate 5 below is exactly that shape.
- Perf: no fresh whole-tree bench needed. Round 8's only hot-path deltas are inside the unblock/resume restore path (one `eventIndex` scan per restore, reusing the resume's existing read) and a per-event branch in `replayRun`/`currentGateStatus`; no per-step writer changed, no lock added. Any Round 9 perf claim records its **own** same-VM before/after with an unchanged-arm control; ≥5 % end-to-end regression or rollback.

## 3. Forbidden / frozen for Round 9

Global forbidden list, unchanged: live R1/bandit/topology on the execution path (doctor's `loadProjectBanditByKey` inventory read is the **only** exception, signed off, read-only, pinned — do not widen it); Outcome-supported claims; **ADR-006 stays Proposed** (eight rounds of discipline; keep it); auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit; no `git checkout` of other branches.

Frozen contracts (Rounds 1–7 set, all held through Round 8, plus new):
- Rounds 1–7 carry-overs, verbatim from the Round 7 brief §3: jsonl signatures; `writeFileAtomic`(+Sync), no private temp+rename (Round 8's whole `src` diff added zero raw fs primitives — verified); `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE`; `episodeLockPath` symmetry; `runCommand` sink wiring; `deleteRunRecords` lock-plus-double-verify + `RUN_RECORDS_SURVIVED`; append/checkpoint take no run lock; `recordCrashTerminal` in-flight-only + best-effort + **the caller always rethrows** (now cited by name from `coordinator.ts`); `applyRetry` sole DAG BLOCKED→READY producer at `src/run/scheduler.ts` — now doubly held by the byte-identical scheduler pins **and** R8-3's AST absence pin over both flowchart sources; `preserveResumableState`; `ClusterMailReport`; resume flags + the four disclosure wordings + the supervised loopback stderr byte-pin (byte-identical through R8-1, still frozen); `writeFeedbackTombstones`; the three adaptation error codes + bind-after-load; doctor's frozen-additive JSON incl. `learnedState`; feedback fuzz hard-fails; `withRunLifecycleLock` rules; supervised settle-on-crash + rounds-window pins; `commandFailureNext` code-discrimination; `CheckpointStore.read` typed error; one-definition-of-terminal (`TERMINAL_REPLAY_STATUSES`/`replayedTerminalStatus`); the five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT` character-exact; `--lock-wait-ms`; `process-death.ts`; R6-9's absence pins; the reconstruction contract (`childTasksFromLog`, untouched in Round 8) + the FAIL-unreachable tripwire; the criteria-are-guidance record + 270-cell sweep + sole-production-path census; the parent-plane recorder (`recordTerminal` — its routing obligation is now **mechanical**, test 4 of `parent-crash-residuals.test.ts`); empty-graph pre-flight; the narrowed doctor exception; `settleSupervisedOutcome` absence.
- **New from Round 8:**
  - **The `RUN_UNBLOCKED` contract**: payload exactly `{ blockedEventId, reason, retryNodeId? }`, exact-keyed; only an unblock naming the **active** block clears the latch; stale/unmatched/post-terminal unblocks are anomalies with the terminal untouched; `RUN_UNBLOCKED` is not a status and `TERMINAL_REPLAY_STATUSES` is unchanged; the clear reopens every recorder through `replayedTerminalStatus` with **zero per-writer special cases** — a new writer that special-cases unblock is a defect. Producer order transform → append → checkpoint; refusals write nothing; crash recovery idempotent vs `checkpoint.lastEventId` and shared by resume/pause/inject restore. `unblock` is a dedicated **locked** command, never an injection kind. The reopen fails closed on executed descendants (changing that is candidate 3's own authorization contract, not a widening). All pinned in `replay.test.ts` (5), `event-row-fuzz.test.ts` (seed + 7 refusals), `unblock-flow.test.ts` (8), `cli/unblock.test.ts` (6), and the R8-3 AST pin.
  - **The BLOCKED operator block, v2**: exactly four routed lines (`inspect`/`inject`/`unblock` `next:` + the resume `note:`), `deepEqual`-pinned verbatim with the retired Round 7 clauses pinned absent by string; wired at all four flowchart exits (`run --flowchart`, `run --children`, `resume`, `answer`) with a four-site source pin on `flowchartExitCode` — a fifth flowchart command must route BLOCKED too.
  - **The `INSPECT_SUMMARY` freeze**: frozen additive like doctor `--json` — the four keys never change name/type/meaning or disappear; a new key lands only with `SUMMARY_CONTRACT_KEYS` + both exact-shape pins updated in the same diff; the type stays outside the `Event` union (pinned against `EVENT_TYPES` and `validateEvent`); `--json` stays a pure event NDJSON stream.
  - **`loadProjectBandit` absence**: the root-keyed reader must not reappear as a bandit-store export without a live production caller (namespace pin); doctor's `loadProjectBanditByKey` remains the sole live read exception under the unchanged isolation pin.
  - **The catalog posture (option b)**: `CATALOG_OBSERVED_CORRUPT` stays in the frozen five-route map as defense-in-depth; the posture is recorded in-source at `catalog-observed.ts`; inventing a fake producer to test the route remains refused; a *real* command-path reader is a future parent decision that reopens the posture comment, not just the code.
  - **The parent-plane residual decisions**: the crash refusal is terminal-keyed by decision (WAITING_FOR_USER crash → `RUN_FAILED`, cost stated in-source); `RUN_CANCEL_REQUESTED` stays unguarded by decision (operator fact). Changing either flips a named behaviour pin plus a prose pin — a decision, not a refactor.
  - **Replace-in-same-diff obligations now standing**: R8-4's five option-(a) precondition pins (pin 2 goes to whoever ships an executor verdict producer; the rest to the option-(a) diff); R8-2's three contract-absence pins (to the durable-contract slot, which also fixes the region-regex nit); R8-10's bound-episode settle pin joins the pre-rounds frozen set.
- Saturated — do not spend a slot: everything in the Round 7 saturated list, plus: the unblock itself (R8-1 closed R7-3's whole arc; what remains is the executed-descendant discard, candidate 3); BLOCKED operator routing (all four sites wired; only new commands owe it); the option-(b)-vs-(a) record (R8-4 emptied the design space — what remains is precondition 0 and then the one signed-off diff); parent-plane crash coherence (R8-7 turned both residuals into recorded decisions with stated costs — reopening one is a decision change, not a finding); `INSPECT_SUMMARY` stability (declared); root-keyed bandit reader disposition (deleted); the catalog route posture (recorded).

Process requirements per slot (carried forward — Round 8 executed them cleanly again): census first, against the working tree; scoped `eslint` + whole-tree `tsc --noEmit` before reporting; census your consumers and ship or prescribe their updates in your own diff; run `live-isolation.test.ts` if you add an import inside the live closure; attribute shared-tree transients to files and re-verify after the owner lands (R8-2 §5 and R8-7 §6 are this round's models); timing-sensitive owned tests 3×; full gate is the parent's job; no scratch files at report time; docs slots census the working tree with timestamps (R8-5 executed the R7-6 lesson — keep it); a "did not land" claim is only true as of the minute taken.

## 4. Ranked Round 9 candidates (mutually exclusive ownership)

The round's tilt: Round 8 shipped the unblock and turned option (a) into a sequenced plan whose honest first step needs no schema. Round 9's spine is **durability** — the run contract that R7-1 disclosed, R8-2 designed, and R8-1 has now un-blocked by releasing `replay.ts`/`flowchart-run.ts`/`main.ts`. **File contention at the top:** the durable contract (R9-1) owns those three files plus `resume.test.ts`; the executed-descendant discard (R9-3) would need two of them, so it is design-only unless the parent sequences it after R9-1 lands.

File-contention map: `src/run/replay.ts` + `src/run/flowchart-run.ts` + `src/cli/main.ts` + `test/integration/m2.5/resume.test.ts` to **R9-1 alone**; `src/pi-adapter/pi-executor.ts` to **R9-2**; `docs/**` to **R9-4**; `scripts/crash-probe.mjs` + `test/integration/m2/crash-recovery.test.ts` to **R9-5**; `src/run/gate-apply.ts` to **R9-6**.

### R9-1 (P1, the round's spine) — Durable run contract on resume (schema sign-off required)
**Evidence (R8-2's design, premises reviewer-verified at HEAD; R7-1 disclosure 3):** `FlowchartContinuation.contract` is honoured but no production caller can supply it; the constraints live on no record a run id can reach. All three absence pins are landed and honest; the flip-pin is landed and named. The files no longer collide with anything.
**Change:** implement R8-2's design as written: optional `contract?: RequirementContract` on `FlowchartCheckpointState` (same `schemaVersion: 1`, absence stays valid), validated via `validateRequirementContract` wrapped as `Invalid RunCheckpoint: flowchart.contract: …`; written by `persistCheckpoint` when `ctx.contract` is defined; restored by `restoreFlowchartSession` so pause/inject cannot strip it; projected by CLI `flowchartContinuation` (one change covers `resumeCommand` and `answerCommand`); precedence `continuation.contract ?? checkpoint.flowchart.contract`. **Never synthesize from the episode** (R7-1's stated reason stands). Mandatory in-diff consumers, censused by R8-2 §4: replace the three absence pins with positive schema/writer/CLI pins (and fix the region-regex boundary while there — review §5 nit 1); **flip the no-contract pin with disclosure**, proving a contract-ful run resumed through the *production CLI boundary* reports `constraint-retention: PASS` (not another direct call that hand-feeds the contract); `flowchart-snapshot.test.ts` valid/malformed/absent trio; a contract-bearing flowchart-checkpoint seed for `test/unit/persist/row-fuzz.test.ts` (its sole seed is non-flowchart and would never mutate the field); a pause-or-inject preservation assertion. Sign-off note: R8-4 §5.3 wants per-task acceptance criteria on the same checkpoint later — decide now whether this sign-off names that as a planned sibling field, but do **not** implement it (option (a) stays design-gated).
**Ownership:** `src/run/replay.ts`, `src/run/flowchart-run.ts`, `src/cli/main.ts` (all sole), `test/integration/m2.5/resume.test.ts`, `test/unit/supervisor/flowchart-snapshot.test.ts`, `test/unit/persist/row-fuzz.test.ts`.

### R9-2 (P2, the honest first step of option (a)) — An executor that can report a verdict
**Evidence (R8-4 C5, reviewer re-verified in source):** `PiAgentExecutor` cannot emit a child-authored `TASK_RESULT` at all — `translatePiEvent` never maps to `MESSAGE`, so `finish` always synthesizes `UNOBSERVED`, which `assessChildObservation` refuses. The gate is producer-blocked for the only real executor; the fakes are hard-coded PASSED. **Because this precondition holds, Round 9 must not implement the per-criterion protocol field — it would be fillable only by fakes, the exact defect R7-2 recorded for `constraint-retention`.**
**Change:** give the real executor a way to report a whole-task verdict: a `report_task_result` tool surfaced through the existing per-request tool seam (`PiExecutorOptions.tools` + the `createClusterTools(request.cluster)` precedent), emitting a real protocol-v1 `TASK_RESULT` with `verification.kind` PASSED/FAILED and evidence ids into the transcript so `finish` stops synthesizing. No protocol schema change — the existing message carries it. Replace R8-4's pin 2 in the same diff (its message says re-derive the producer census, not delete). Mind C6 while there: a real FAILED/PASSED verdict makes `deterministic-fail` and the existing gate real for `--executor pi` for the first time — the slot owns disclosing exactly which gate outcomes become reachable, and R8-4's C6 note on `derivedClaimedVerificationWithoutChecks` (prose-conditioned) should be re-checked against real inputs, not decided silently.
**Ownership:** `src/pi-adapter/pi-executor.ts` (sole), `test/unit/pi-adapter/`, `test/unit/tracking/option-a-preconditions.test.ts` (pin 2 only).

### R9-3 (P2, design-first — contends with R9-1, do not co-schedule an implementation) — Executed-descendant discard
**Evidence (R8-1 §11.1, reviewer-verified in the diff):** the reopen fails closed when a FAILED node's join-required downstream work already executed; such a gate block is still unfixable by `unblock` alone. R8-1's stated position: this needs its own authorization contract (an explicit "discard this work" flag with its own audit record), not a widening of the unblock.
**Change:** design + decision this round: the flag's shape (`unblock --discard-executed`? a second event vs a payload field on `RUN_UNBLOCKED`? — note the payload is exact-key frozen, so a new field is a schema sign-off), what the audit record must carry (which nodes, what spend was discarded), and whether rewound COMPLETED work leaves its events untouched (R8-1's evidence-survives rule suggests yes). Implementation lands a later round in R8-1's files, after R9-1.
**Ownership:** report + at most additive pins in `test/unit/supervisor/`; no `src/**`.

### R9-4 (P3, docs) — Round 8 truth-up
**Evidence (reviewer-verified stale at HEAD, review §4.4):** `docs/status-matrix.md:66` still frames R8-9's committed deletion as an "in-flight working-tree edit"; the three 21:50 UTC sync notes say R8-1's pins "were still being rewritten" (they landed in `05051ac`); undocumented Round 8 facts: the parent-plane residual decisions (R8-7), the catalog posture (R8-6), R8-4's fourth precondition (the docs describe the gate without saying no real executor can reach it). `docs/decisions/0005…md:110` stays flag-only (ADR body). The `main.ts` producer-side `INSPECT_SUMMARY` one-liner R8-8 suggested belongs to `main.ts`'s owner (R9-1) or nobody — not the docs slot.
**Change:** doc-only truth-up; census the working tree with timestamps (the R8-5 pattern); end-of-round sync; no ADR status line.
**Ownership:** `docs/**`.

### R9-5 (P3, probe) — The unblock crash window joins the standing probe
**Evidence (review §6.6):** the append-before-checkpoint window is proven by integration tests that simulate the crash by rolling the checkpoint back; the nine SIGKILL cases in `scripts/crash-probe.mjs` predate the unblock and none kills a real process inside the new window.
**Change:** one new probe case — SIGKILL between the `RUN_UNBLOCKED` append and the checkpoint write, then verify resume recovers the reopen exactly once and the run can complete. The case set is pinned by name in `crash-recovery.test.ts`'s reduced-probe pin: update it in the same slot, with disclosure (the standing rule from the Round 6 brief). If the window cannot be hit deterministically from outside the process, stop and report per the R5-8/R7-10 precedent.
**Ownership:** `scripts/crash-probe.mjs`, `test/integration/m2/crash-recovery.test.ts` (the name-list pin only).

### R9-6 (P3, small decision) — The gate's near-write-only reconstruction
**Evidence (R8-1 §11.3, reviewer-confirmed):** `applyChildThreeLine`'s `GateApplyResult.runStatus` is discarded on the flowchart path; `currentGateStatus` is observable only through the `from` field of the *next* written transition, which the happy path never writes — exercised end-to-end by exactly one test (R8-1's re-block cycle).
**Change:** a decision, not a refactor: either record the posture in-source at `applyChildThreeLine`/`currentGateStatus` (this is a consistency ledger for the transition record, deliberately not a control input on this plane), or give `runStatus` a consumer with its own justification. Do not silently grow the gate's authority — R8-4's C7 (soft and hard both block) is adjacent and settled.
**Ownership:** `src/run/gate-apply.ts` (sole), `test/unit/run/` additive.

*(No further candidate is manufactured: everything else surfaced by Round 8 is either saturated (§3), folded into R9-1 (the R8-2 pin nit, the `main.ts` one-liner), or a stated-cost decision whose reopening needs new evidence, not a slot.)*

## 5. Explicitly NOT for Round 9 (unchanged or newly settled)

Live R1/bandit/topology (standing; the keyed diagnostic read is the ceiling); **the per-criterion protocol field** (R8-4's fourth precondition holds — verified — so the channel would be fillable only by fakes; precondition 0 (R9-2) comes first, and option (a) itself remains one later signed-off diff of all three shapes at once); jsonl/lock/append perf and per-step writer locking (saturated); protocol/v1 parse and event fuzz (saturated — R9-1 owes the checkpoint fuzz seed, not a rule change); mailbox/dead-letter/cluster work; lock stealing; resume-time adoption of unaccepted child results (R5-5's NO stands); `maxCostUsd` enforcement; `TASK_LEASE_EXPIRED` rename; executor-config persistence in the event log (R4-6 stands); a non-terminal `RUN_CRASHED` marker (R4-4 stands, re-affirmed by R8-7's prose pin `doesNotMatch(/RUN_CRASHED/)`); same-role late mailbox delivery; rewriting append-only run logs for privacy; `pause --lock-wait-ms` (decided R6-5); ADR-006 status changes (frozen forever); re-litigating option (b), the catalog posture, the parent-plane residual decisions, or the unblock's fail-closed default (each is a recorded, pinned decision — candidate 3 is the sanctioned path for the last one); a second unblock surface via injection (R7-3's producer decision stands, now doubly held by the locked-command contract); widening `unblock` to rewind executed work without its own authorization contract (R8-1's refusal is load-bearing and pinned).
