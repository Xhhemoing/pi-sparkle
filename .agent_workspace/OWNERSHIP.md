# File ownership — Loop 4 Round 13 (`agent/opt-continuous`)

Parent owns `.agent_workspace/PROGRESS.md`. Subagents never git commit. **Stay on branch `agent/opt-continuous`. Do not `git checkout` another branch.**

Lists are binding. **Hard contention:** R13-1 (comment-only `replay.ts` + `prescore.ts`) and R13-4 (docs) truth-up the same facts — they stay separate; R13-4 timestamp-censuses and never assigns an uncommitted sibling a commit id. R13-3 owns `main.ts` **and** both output-census files together. Nobody edits `flowchart-run.ts` writer/reader region, `unblock-flow.test.ts`, or `replay.test.ts`.

**Dispatch cross-check:** every "replace X in the same diff" obligation below lives in that slot's owned files. Fold unowned-file joints into the landing commit, not a follow-up.

Injection brief: `.agent_workspace/ROUND12-BRIEF.md`. Review: `.agent_workspace/loop4-r12-review.md`.

| Slot | Model | Owns |
|---|---|---|
| R13-1 | opus | Comment-only `src/run/replay.ts` + `src/tracking/prescore.ts`. `test/unit/tracking/option-a-preconditions.test.ts` and `test/unit/tracking/criteria-are-guidance.test.ts` **only if** a pinned sentence must move with its prose (disclose; replacement not deletion). Retire the two false "no writer" docstrings. Whole diff mechanically comment-only (zero non-comment changed lines). `coverageOutcome` body byte-identical. Keep every pinned sentence (`never *synthesized*`, `not from the bound episode`). **No behavioural src.** |
| R13-2 | opus | Additive only in `test/integration/run/criteria-gate.test.ts` and/or `test/integration/m2.5/resume.test.ts`. **No `src/**`.** Behavioural pins: (a) unblock carry-forward of `taskCriteria` on R12-3's BLOCKED fixture; (b) `advanceTaskCriteria` log-derived arm via hand-stripped checkpoint. Do not touch the writer. Do not edit `unblock-flow.test.ts`. |
| R13-3 | opus | `src/cli/main.ts` (sole src); `test/integration/cli/blocked-next.test.ts`; `test/unit/cli/doctor-routed-next-freeze.test.ts`; `test/integration/m2.5/cli-contract-honesty.test.ts` only if its regexes need widening (disclose). Early `onRunStarted` on `--flowchart` and `--children`. Census every stdout assertion before writing the line. Routes character-exact. `INSPECT_SUMMARY` untouched. If a byte-pinned output shape cannot absorb the line additively, **stop-and-report with the census**. **Do not** edit `flowchart-run.ts`. **Do not** race or kill a process. |
| R13-4 | gpt-sol | `docs/**` only. Timestamp-census the working tree. **Do not touch any ADR status line.** ADR-006 stays Proposed. Own the stale Round 12 spots in the brief §4 R13-4. Record Round 12 landings as commits. Never assign an uncommitted sibling a commit id. |
| R13-5 | gpt-sol | Additive only in `test/unit/routing/live-isolation.test.ts`. **No `src/**`.** R13-3 edits `main.ts`. Keep doctor `loadProjectBanditByKey`, `selectArm` shadow-only, bare `loadProjectBandit` absent. Report-only is acceptable. |
| R13-6 | gpt-sol | Additive only in `test/unit/run/flowchart-applyretry-absence.test.ts`. **No `src/**`.** Keep R8-3/R10-8/R11-8: no `applyRetry`/`scheduler.js`; restore-side `assertDiscardAuditMatchesLog` stays in `applyClearingEvent` or `restoreCheckpointedSupervisor`. Report-only is acceptable. |
| R13-7 | gpt-sol | Additive only in `test/unit/run/episode-contract-boundary.test.ts`. **No `src/**`.** Keep never-synthesize of `taskCriteria` from the episode. Report-only is acceptable. |
| R13-8 | gpt-sol | Additive only in `test/unit/tracking/independent-evidence-posture.test.ts`. **No `src/**`.** Keep whole-`src` one `void` and the flowchart-spine zero-mention pin. Report-only is acceptable. |
| R13-9 | gpt-sol | Additive only in `test/unit/run/terminal-replay-statuses-freeze.test.ts`. **No `src/**`.** Keep exact eight `RunStatus` members and terminal trio. Report-only is acceptable. |
| R13-10 | gpt-sol | Additive only in `test/unit/run/checkpoint-writer-carriage.test.ts`. **No `src/**`.** Keep `contract` on every flowchart payload and at least one `taskCriteria` writer. Report-only is acceptable. |

**Parent sign-off**
- **R13-1 YES — comment-only truth-up of the two false "no writer" docstrings** in `replay.ts` and `prescore.ts`. Use R12-1 §8 replacement text (keep pinned sentences). Mechanically comment-only; `coverageOutcome` body byte-identical. If a pin must move with prose, replace in the same diff (you own those test files only for that).
- **R13-3 YES — wire `onRunStarted` on `--flowchart` and `--children`** the same way the track path already does (`Run <id>: started`). Own `main.ts` and both output-census files together. Census stdout assertions first. Routes stay character-exact. Stop-and-report if a byte-pinned shape cannot absorb the line additively.
- **R13-2 / R13-4:** no extra sign-off. Docs: no ADR flip.

Frozen (do not break): `taskCriteria` writer as shipped (three sources, empty logged requests ignored, no `continuation.taskCriteria`); `onRunStarted` as shipped (throw swallow, not a control channel); exact eight `RunStatus` members; option (a) schema; restore-side discard audit; 11-case probe; tracked pause controller; `INSPECT_SUMMARY` four keys; BLOCKED prefix + `note:`; `RUN_UNBLOCKED` three keys; `RUN_UNBLOCKED_WITH_DISCARD` exact-key; `independentEvidence` one `void` + spine zero-mention; episode never-synthesize; every flowchart-payload writer carries `contract` and `taskCriteria`; verdict-producer rules; gate `runStatus` ledger; R8-3 AST pin; `loadProjectBandit` absence; catalog posture (b); parent-plane residuals; reconstruction `childTasksFromLog` + FAIL-unreachable; `coverageOutcome` no FAIL; `recordCrashTerminal` rethrow; `applyRetry` sole scheduler producer; jsonl; `writeFileAtomic`(+Sync); no new private tmp+rename; `withRunLifecycleLock`; append/checkpoint unlocked; five `DOCTOR_ROUTED_NEXT` routes; loopback supervised resume; empty-graph pre-flight; `settleSupervisedOutcome` absence; no live R1 / Outcome-supported / ADR-006 Accepted / auto-promote / `package.json`.

Every slot: census first against the working tree; **verify that a path the brief hands you actually exists before writing to it**; scoped eslint + whole-tree `tsc --noEmit`; 3× timing-sensitive tests; report `.agent_workspace/loop4-r13-tN.md`. Census consumers. Run `live-isolation.test.ts` if you add an import inside the live closure. No full gate. No scratch files. Docs slots census the working tree with timestamps.
