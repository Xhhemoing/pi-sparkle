# File ownership — Loop 4 Round 10 (`agent/opt-continuous`)

Parent owns `.agent_workspace/PROGRESS.md`. Subagents never git commit. **Stay on branch `agent/opt-continuous`. Do not `git checkout` another branch.**

Lists are binding. **Hard contention:** R10-1 owns `events.ts` + `replay.ts` + `flowchart-run.ts` + `flowchart-supervisor.ts` + `gate-apply.ts` + `main.ts`. R10-2 (option (a)) is **not scheduled**. R10-4 owns `resume.test.ts` — R10-1 must not touch it. R10-6 (discard crash-probe) is **not scheduled** (needs the landed producer).

Injection brief: `.agent_workspace/ROUND9-BRIEF.md`. Review: `.agent_workspace/loop4-r9-review.md`. Discard design: `.agent_workspace/loop4-r9-t3.md`. Option-(a) design stays frozen at `.agent_workspace/loop4-r8-t4.md` (Round 11).

Dispatch ids: pending first commit of this file; parent records them immediately after launch.

| Slot | Model | Owns |
|---|---|---|
| R10-1 | opus | `src/run/events.ts`, `src/run/replay.ts`, `src/run/flowchart-run.ts`, `src/supervisor/flowchart-supervisor.ts`, `src/run/gate-apply.ts`, `src/cli/main.ts` (all sole); `test/unit/run/event-row-fuzz.test.ts`, `test/unit/run/replay.test.ts`, `test/integration/run/unblock-flow.test.ts`, `test/integration/cli/unblock.test.ts`, `test/integration/cli/blocked-next.test.ts`, `test/unit/supervisor/flowchart-supervisor.test.ts` (additive). Ship `RUN_UNBLOCKED_WITH_DISCARD`. **Do not** edit `test/integration/m2.5/resume.test.ts` (R10-4). **Do not** add a field to `RUN_UNBLOCKED`. |
| R10-2 | gpt-sol | Additive only in `test/unit/tracking/option-a-preconditions.test.ts` (pins 1/3/4/5 must remain; do not replace pin 2) and/or new `test/unit/protocol/` pin. **No `src/**`.** Freeze: option (a) is deferred — `VerificationResult` has no `criteria` field; R8-4 pins 1/3/4/5 still present. Do not implement option (a). |
| R10-3 | gpt-sol | `docs/**` only. Timestamp-census the working tree. **Do not touch any ADR status line.** ADR body `0005` stays flag-only. Fix `m0-m2-architecture.md` nine-case probe line (now ten). |
| R10-4 | gpt-sol | `test/integration/m2.5/resume.test.ts` (sole vs R10-1); additive `test/integration/m2.5/cli-contract-honesty.test.ts`. **No `src/**`.** Pure-CLI `--track` → pause → resume `constraint-retention: PASS`. Writer-census pin: **every** flowchart-payload `materializeCheckpoint` site carries `contract` (property, not a frozen count of two — R10-1 adds a writer). If offline `--track` cannot produce a contract-ful run without a live model, stop and report. |
| R10-5 | opus | `src/tracking/from-child.ts` and/or `src/tracking/prescore.ts` (comment-only); `src/run/child-tracking.ts` (one comment line above `applyChildThreeLine`); additive pins in `test/unit/tracking/` and/or `test/unit/run/`. **Record the `independentEvidence` posture; do not rename.** Do not give `runStatus` a consumer. |
| R10-6 | gpt-sol | Additive `test/unit/pi-adapter/` (existing `report-task-result.test.ts` or new file). **No `src/**`. Do not edit `scripts/crash-probe.mjs`.** Discard crash-probe is deferred. This slot freezes R9-2 verdict-producer standing rules (identity from the lease, FAILED cites evidence, one verdict per attempt, tool always surfaced). |
| R10-7 | gpt-sol | Additive tests under `test/unit/run/` or `test/unit/tracking/` targeting `src/run/episode-bind.ts` readers. **No `src/**`.** Freeze the never-synthesize rule: episode binding copies `acceptanceCriteria` only; nothing constructs a `RequirementContract` from the episode as the run's constraints. |
| R10-8 | gpt-sol | Additive only in `test/unit/run/flowchart-applyretry-absence.test.ts`. **No `src/**`.** Keep R8-3's AST pin; prove it still covers both flowchart sources after R10-1's new discard path (the pin is whole-file — do not weaken; add a named assertion that a `WITH_DISCARD` / discard-transform identifier still cannot reference `applyRetry`/`scheduler.js`). |
| R10-9 | gpt-sol | Additive only in `test/unit/run/gate-status-posture.test.ts`. **No `src/**`.** Keep the ledger posture: `runStatus` has zero `src` readers. R10-1 edits `gate-apply.ts` and must keep this green — do not give the field a consumer; do not edit `gate-apply.ts`. |
| R10-10 | gpt-sol | New `test/unit/run/terminal-replay-statuses-freeze.test.ts` (or additive in a file R10-1 does **not** own). **No `src/**`. Do not edit `replay.test.ts` (R10-1).** Freeze `TERMINAL_REPLAY_STATUSES` = `COMPLETED`/`FAILED`/`BLOCKED` and that neither `RUN_UNBLOCKED` nor a future `RUN_UNBLOCKED_WITH_DISCARD` is a status. |

**Parent sign-off**
- **R10-1: YES — persisted schema** `RUN_UNBLOCKED_WITH_DISCARD` as `.agent_workspace/loop4-r9-t3.md` designed. CLI `unblock --discard-executed`. Distinct exact-keyed event; **not** a fourth `RUN_UNBLOCKED` key; **not** a two-event sequence. Charged estimates **in-payload** (`chargedEstimatedCostUsd` / `chargedEstimatedDurationMs` per rewound descendant) with **fail-closed validation against referenced `MODEL_ROUTED` rows**. Do not invent zeros; mismatch refuses and writes nothing. The discard transform is a **fourth flowchart checkpoint writer and must carry `contract` forward**. Extend R9-10's type freeze and row-fuzz seeds for the new event; keep R9-3's ordinary-refusal pin green **byte-for-byte**; keep R9-7's route freeze green; replay / `unappliedUnblock` / `currentGateStatus` treat both clearing events uniformly. `RUN_UNBLOCKED` stays byte-frozen. Execute the test plan at `loop4-r9-t3.md` §7 — do not redesign it. `TERMINAL_REPLAY_STATUSES` unchanged; the new event is not a status.
- **R10-2: NO — option (a) deferred to Round 11.** Closing precondition 0 does not make it this round's schema. This slot is a freeze that the remaining R8-4 pins and the protocol field stay unimplemented.
- **R10-5: record the posture** — `independentEvidence` is set from a self-report and is not third-party verification; do not rename this round. One-line `{@link GateApplyResult}` pointer above `applyChildThreeLine`. Comment-only src.
- **R10-6 probe: DEFERRED** until R10-1 is committed. This numbered slot is the verdict-producer freeze, not the eleventh crash-probe case.

Frozen: durable-contract semantics (never synthesize; every flowchart checkpoint writer carries `contract`); verdict-producer rules; gate `runStatus` ledger (zero `src` readers); ten-case probe; executed-descendant ordinary-refusal exact message; `RUN_UNBLOCKED` three keys + type freeze; four-line BLOCKED block; R8-3 AST pin; `INSPECT_SUMMARY` additive freeze; `loadProjectBandit` absence; catalog posture (b); parent-plane residual decisions; reconstruction `childTasksFromLog` + FAIL-unreachable tripwire; criteria-are-guidance + 270-cell + sole-production-path; `recordCrashTerminal` rethrow; `applyRetry` sole scheduler producer; jsonl; `writeFileAtomic`(+Sync); no new private tmp+rename; `withRunLifecycleLock`; append/checkpoint unlocked; five `DOCTOR_ROUTED_NEXT` routes character-exact; loopback supervised resume stderr pin; empty-graph pre-flight; `settleSupervisedOutcome` absence; one-definition-of-terminal; no live R1 / Outcome-supported / ADR-006 Accepted / auto-promote / `package.json`.

Every slot: census first against the working tree; **verify that a path the brief hands you actually exists before writing to it**; scoped eslint + whole-tree `tsc --noEmit`; 3× timing-sensitive tests; report `.agent_workspace/loop4-r10-tN.md`. Census consumers. Run `live-isolation.test.ts` if you add an import inside the live closure. No full gate. No scratch files. Docs slots census the working tree with timestamps.
