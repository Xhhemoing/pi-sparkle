# File ownership — Loop 4 (`agent/opt-continuous`)

Parent owns `.agent_workspace/PROGRESS.md`. Subagents never git commit.

Round 1 slots will be filled after the fable audit. Exclusive file lists are binding: do not touch another slot’s files. If two slots need `src/cli/main.ts`, stay inside the named function region only.
