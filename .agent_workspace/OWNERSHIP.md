# File ownership — Loop 2 Round 1

Parent owns `.agent_workspace/PROGRESS.md`. Subagents never git commit.

If two slots need `src/cli/main.ts`, stay inside the function region listed above. Do not reformat the rest of the file.
