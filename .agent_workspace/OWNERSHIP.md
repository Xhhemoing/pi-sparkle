# File ownership — Loop 4 Round 11 (`agent/opt-continuous`)

Parent owns `.agent_workspace/PROGRESS.md`. Subagents never git commit. **Stay on branch `agent/opt-continuous`. Do not `git checkout` another branch.**

Lists are binding. **Hard contention:** R11-1 owns `protocol/v1.ts` + `tracking/**` + `pi-executor.ts` + `replay.ts`. R11-4 owns `flowchart-run.ts` and must **not** edit `replay.ts`. R11-3 owns `track/loop.ts` + `main.ts`. Confirm at sign-off: option (a) needs `replay.ts` (checkpoint sibling field); R11-4 does not.

Injection brief: `.agent_workspace/ROUND10-BRIEF.md`. Review: `.agent_workspace/loop4-r10-review.md`. Option-(a) design: `.agent_workspace/loop4-r8-t4.md`. Discard design: `.agent_workspace/loop4-r9-t3.md`.

Dispatch ids: pending first commit of this file; parent records them immediately after launch.

| Slot | Model | Owns |
|---|---|---|
| R11-1 | opus | `src/protocol/v1.ts`, `src/tracking/**`, `src/pi-adapter/pi-executor.ts`, `src/run/replay.ts` (all sole); `test/unit/protocol/v1.test.ts`, `test/unit/tracking/**` (incl. `option-a-preconditions.test.ts` — replace pins 1/3/4/5 **and** the R10-2 meta-pin), `test/unit/pi-adapter/**`, `test/unit/run/event-row-fuzz.test.ts`. Implement option (a). **Do not** give `independentEvidence` a reader or rename it. **Do not** edit `flowchart-run.ts`. |
| R11-2 | gpt-sol | `scripts/crash-probe.mjs`, `test/integration/persist/crash-recovery.test.ts` (name-list pin only). Verify that path exists; `test/integration/m2/crash-recovery.test.ts` does **not**. Eleventh probe case: SIGKILL in the `RUN_UNBLOCKED_WITH_DISCARD` append-before-checkpoint window. Original ten names and order unchanged. |
| R11-3 | opus | `src/track/loop.ts`, `src/cli/main.ts` (sole); `test/integration/m2.5/resume.test.ts`, `test/integration/m2.5/cli-contract-honesty.test.ts`. Wire tracked-run pause controller. **Do not** edit `blocked-next.test.ts` (fold declined). **Do not** race the pause token; **do not** kill a process to manufacture a pause. |
| R11-4 | opus | `src/run/flowchart-run.ts` (sole); additive `test/integration/run/unblock-flow.test.ts`, `test/unit/run/replay.test.ts`. Restore-path `assertDiscardAuditMatchesLog`. **Do not** edit `src/run/replay.ts`. **Do not** change the in-payload charged-estimates decision. |
| R11-5 | gpt-sol | `docs/**` only. Timestamp-census the working tree. **Do not touch any ADR status line.** ADR body `0005` stays flag-only. Record R10-1/8/9 as landed commits. |
| R11-6 | gpt-sol | Additive only in `test/unit/routing/live-isolation.test.ts`. **No `src/**`.** R11-1 may add live-closure imports. Keep doctor `loadProjectBanditByKey`, `selectArm` shadow-only, bare `loadProjectBandit` absent. Do not weaken the signed-off `because`. |
| R11-7 | gpt-sol | Additive in `test/unit/run/inspection.test.ts` and/or `test/integration/cli/blocked-next.test.ts`. **No `src/**`.** R11-3 edits `main.ts`. Keep `INSPECT_SUMMARY` four keys and the four ordinary BLOCKED lines as a pinned prefix. Do not add a fifth `next:`. |
| R11-8 | gpt-sol | Additive only in `test/unit/run/flowchart-applyretry-absence.test.ts`. **No `src/**`.** R11-4 edits `flowchart-run.ts`. Keep R8-3/R10-8 AST pin; discard/reopen markers still must not reference `applyRetry`/`scheduler.js`. |
| R11-9 | gpt-sol | Additive only in `test/unit/run/terminal-replay-statuses-freeze.test.ts`. **No `src/**`.** R11-1 edits `replay.ts`. `TERMINAL_REPLAY_STATUSES` stays COMPLETED/FAILED/BLOCKED; no `RUN_UNBLOCKED*` event is a status. |
| R11-10 | gpt-sol | Additive only in `test/unit/run/episode-contract-boundary.test.ts`. **No `src/**`.** R11-1 adds per-task criteria on the checkpoint — those must still not be synthesized from the episode. Keep the never-synthesize census. |

**Parent sign-off**
- **R11-1: YES — implement option (a)** as `.agent_workspace/loop4-r8-t4.md` designed, one diff. Optional nested `VerificationResult.criteria` (additive-compatible); new `unmet-acceptance-criterion` anomaly on **observed FAILED only** (never-ran stays unknown-not-unmet); durable per-task criteria on R9-1's reserved checkpoint seam; per-criterion reporting on `sparkle_report_task_result`. Replace in the same diff: R8-4 pins 1/3/4/5, R10-2's title meta-pin, R10-2's two `criteria` pins, R9-1's reserved-unimplemented assertion. Inherit R10-5: **do not read `independentEvidence`**; keep the `void`; do not rename. Inherit R10-6 producer freezes (lease identity, FAILED cites evidence, one verdict per attempt, tool always surfaced). Re-derive the 270-cell sweep if the sole-production-path changes; do not delete it. New shapes ride `event-row-fuzz` seeds.
- **R11-3: YES — wire the pause controller.** `TrackRunInput` gains optional `pause: PauseController`; `startTrackedRun` forwards it; `runCommand` supplies `createFilePauseController(stateRoot)`. Determinism: **do not race** the pause token; prefer a deterministic hook (same class as the untracked flip test) with the CLI driving. If the seam cannot be made deterministic without disproportionate src, stop and report. **Fold declined:** do not give `formatBlockedRunReport` the checkpoint this round (unconditional `note:` accepted).
- **R11-4: YES — validate charged estimates at restore.** Run `assertDiscardAuditMatchesLog` from `applyClearingEvent` (or its caller) over the recorded payload so inflated sums fail closed; give the three producer-side refusal arms an honest test path (hand-edited log + resume). Do not export-only without restore checking. Do not refund budgets or fold invocation telemetry into charges.

Frozen: `RUN_UNBLOCKED_WITH_DISCARD` exact-key schema + uniform clearing + single-append + per-block permission; every flowchart-payload writer carries `contract`; verdict-producer rules; `independentEvidence` self-report (no reader); episode never-synthesize; four-line BLOCKED prefix; durable-contract semantics; `RUN_UNBLOCKED` three keys; gate `runStatus` ledger; ten-case probe (grows only with disclosure); ordinary executed-descendant refusal exact message; R8-3 AST pin; `INSPECT_SUMMARY`; `loadProjectBandit` absence; catalog posture (b); parent-plane residuals; reconstruction `childTasksFromLog` + FAIL-unreachable; criteria-are-guidance until R11-1 replaces the pins; `recordCrashTerminal` rethrow; `applyRetry` sole scheduler producer; jsonl; `writeFileAtomic`(+Sync); no new private tmp+rename; `withRunLifecycleLock`; append/checkpoint unlocked; five `DOCTOR_ROUTED_NEXT` routes; loopback supervised resume; empty-graph pre-flight; `settleSupervisedOutcome` absence; one-definition-of-terminal; no live R1 / Outcome-supported / ADR-006 Accepted / auto-promote / `package.json`.

Every slot: census first against the working tree; **verify that a path the brief hands you actually exists before writing to it**; scoped eslint + whole-tree `tsc --noEmit`; 3× timing-sensitive tests; report `.agent_workspace/loop4-r11-tN.md`. Census consumers. Run `live-isolation.test.ts` if you add an import inside the live closure. No full gate. No scratch files. Docs slots census the working tree with timestamps.
