# File ownership — Loop 4 Round 12 (`agent/opt-continuous`)

Parent owns `.agent_workspace/PROGRESS.md`. Subagents never git commit. **Stay on branch `agent/opt-continuous`. Do not `git checkout` another branch.**

Lists are binding. **Hard contention:** brief candidates 1 and 2 both need `src/run/flowchart-run.ts`. They are **one slot (R12-1)**, not concurrent. Nobody else edits `flowchart-run.ts`, `track/loop.ts`, `cli/main.ts`, `test/integration/m2.5/**`, or `test/unit/tracking/option-a-preconditions.test.ts`.

**Dispatch cross-check (Round 11 defect):** every "replace X in the same diff" obligation below lives in that slot's owned files. Do not order a slot to replace an assertion in another slot's file.

Injection brief: `.agent_workspace/ROUND11-BRIEF.md`. Review: `.agent_workspace/loop4-r11-review.md`.

| Slot | Model | Owns |
|---|---|---|
| R12-1 | opus | **Spine: brief R12-1 + R12-2 in one diff.** `src/run/flowchart-run.ts`, `src/track/loop.ts`, `src/cli/main.ts` (all sole); `test/integration/m2.5/resume.test.ts`, `test/integration/m2.5/cli-contract-honesty.test.ts`; `test/unit/tracking/option-a-preconditions.test.ts` (7th-pin truth-up only if its no-writer language needs it). **Do not** edit `unblock-flow.test.ts`, `replay.ts`, `replay.test.ts`, `blocked-next.test.ts`, `docs/**`. **Do not** race the pause token or kill a process. **Do not** overload the pause controller as a notification channel. **Do not** synthesize `taskCriteria` from the episode, the flowchart definition, or the run contract. |
| R12-2 | gpt-sol | Additive only in `test/unit/tracking/independent-evidence-posture.test.ts`. **No `src/**`.** Keep the whole-`src` dereference census at exactly one `void`. Do not rename. R12-1 does not touch this field; this slot exists so a drive-by cannot. |
| R12-3 | opus | New `test/integration/run/criteria-gate.test.ts` only (directory exists; `unblock-flow.test.ts` is a sibling — **do not edit it**). Production-reachability: child reports PASSED with one FAILED criterion → run BLOCKED `reasonCode: unmet-acceptance-criterion` → sanctioned `unblock` → COMPLETED. Control: same shape with criterion PASSED or array omitted completes. No loopback server, no live provider. **No `src/**`.** |
| R12-4 | gpt-sol | `docs/**` only. Timestamp-census the working tree (R11-5 five-census pattern). **Do not touch any ADR status line.** ADR-006 stays Proposed. `0005` body stays flag-only. Own the four false-at-HEAD claims in the brief §4 R12-4. Record Round 11 landings as commits. |
| R12-5 | gpt-sol | Additive only in `test/unit/run/flowchart-applyretry-absence.test.ts`. **No `src/**`.** R12-1 edits `flowchart-run.ts`. Keep R8-3/R10-8/R11-8: no `applyRetry`/`scheduler.js`; restore-side `assertDiscardAuditMatchesLog` stays in `applyClearingEvent` or `restoreCheckpointedSupervisor`. |
| R12-6 | gpt-sol | Additive only in `test/unit/run/episode-contract-boundary.test.ts`. **No `src/**`.** R12-1 writes `taskCriteria` from logged `TASK_REQUEST`s — that must still not be synthesis from the episode. Keep the never-synthesize census (exact spelling + semantic rule). |
| R12-7 | gpt-sol | Additive in `test/integration/cli/blocked-next.test.ts` and/or `test/unit/cli/doctor-routed-next-freeze.test.ts` (verify path exists) and/or `test/unit/run/inspection.test.ts`. **No `src/**`.** R12-1 edits `main.ts`. Keep five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT` character-exact; `INSPECT_SUMMARY` four keys; BLOCKED four-line prefix / exactly three `next:`; discard stays `note:`. |
| R12-8 | gpt-sol | Additive only in `test/unit/routing/live-isolation.test.ts`. **No `src/**`.** R12-1 may add live-closure imports. Keep doctor `loadProjectBanditByKey`, `selectArm` shadow-only, bare `loadProjectBandit` absent. Do not weaken the signed-off `because`. |
| R12-9 | gpt-sol | Additive only in `test/unit/run/terminal-replay-statuses-freeze.test.ts`. **No `src/**`.** `TERMINAL_REPLAY_STATUSES` stays COMPLETED/FAILED/BLOCKED; no `RUN_UNBLOCKED*` event is a status; no fourth `RunStatus`. |
| R12-10 | gpt-sol | Additive only if needed to keep the R10-4 writer-carriage **property** green without owning `resume.test.ts` (R12-1 owns that file). Prefer a new `test/unit/run/checkpoint-writer-carriage.test.ts` that AST-censuses every flowchart-payload `materializeCheckpoint` for `contract` (and, after R12-1, `taskCriteria` only where a writer exists). **No `src/**`.** Do not convert the property into a count. If the existing pin in `resume.test.ts` already covers `contract` and you cannot add `taskCriteria` without that file, report-only and prescribe the one-line successor for R12-1. |

**Parent sign-off**
- **R12-1 YES — fill the `taskCriteria` seam.** Writer derives from logged `TASK_REQUEST`s (`loggedTaskRequests`) or first-write-wins against the checkpoint's existing `taskCriteria`, ascending `taskId`. **Do not** write `ctx.childByTaskId` substitution empties — that launders unknown into known-none. Reader: thread checkpoint `taskCriteria` into `childTasksFromLog`'s substitution path (sole call site `flowchart-run.ts:1346`) so a resumed never-ran node is re-dispatched with original criteria; absence stays unknown. Semantic approved: this changes what a resumed node is asked to check, and that is the field's reason to exist. Behavioural proof: crashed-or-paused run with criteria whose resume re-dispatches the originals; a never-logged node stays unknown. Keep `contract` on every flowchart-payload `materializeCheckpoint`. R11-1's 7th pin and R11-10's census stay green (recording dispatched log facts is not synthesis).
- **R12-1 YES — early run-id disclosure, same diff.** Optional `onRunStarted?: (runId: RunId) => void` on `FlowchartRunDeps`, fired once the run directory and lifecycle lock exist and **before** round 1's pause poll. `startTrackedRun` forwards it. `runCommand` prints the id early on the track path. Payoff: a synchronous `io.stdout` handler can seed the pause token before the first poll, upgrading R11-3's call-site pin to a behavioural pure-CLI pause. Census every `startFlowchartRun` deps literal. Routes must stay character-exact (prescribe `blocked-next` / doctor-freeze updates; do not edit those files — R12-7 owns them). If this half cannot be made deterministic without disproportionate src, **land the writer half and stop-and-report this half**.
- **R12-3 / R12-4:** no extra sign-off. Docs: no ADR flip.

Frozen (do not break): option (a) schema as shipped; `unmet-acceptance-criterion` supplied-never-derived, observed-FAILED only, unknown-not-unmet; restore-side discard audit (set before sums, completeness limit recorded); 11-case probe; tracked pause controller; `INSPECT_SUMMARY` four keys; BLOCKED prefix + `note:`; `RUN_UNBLOCKED` three keys; `RUN_UNBLOCKED_WITH_DISCARD` exact-key; `TERMINAL_REPLAY_STATUSES`; `independentEvidence` one `void`; episode never-synthesize incl. `taskCriteria`; every flowchart-payload writer carries `contract`; verdict-producer rules; gate `runStatus` ledger; R8-3 AST pin; `loadProjectBandit` absence; catalog posture (b); parent-plane residuals; reconstruction `childTasksFromLog` + FAIL-unreachable; `coverageOutcome` no FAIL; `recordCrashTerminal` rethrow; `applyRetry` sole scheduler producer; jsonl; `writeFileAtomic`(+Sync); no new private tmp+rename; `withRunLifecycleLock`; append/checkpoint unlocked; five `DOCTOR_ROUTED_NEXT` routes; loopback supervised resume; empty-graph pre-flight; `settleSupervisedOutcome` absence; no live R1 / Outcome-supported / ADR-006 Accepted / auto-promote / `package.json`.

Every slot: census first against the working tree; **verify that a path the brief hands you actually exists before writing to it**; scoped eslint + whole-tree `tsc --noEmit`; 3× timing-sensitive tests; report `.agent_workspace/loop4-r12-tN.md`. Census consumers. Run `live-isolation.test.ts` if you add an import inside the live closure. No full gate. No scratch files. Docs slots census the working tree with timestamps.
