[Model: claude-fable-5-thinking]

# Loop 4 · Round 7 — SOTA review (R7-1 … R7-10)

Reviewer: Round 7 SOTA reviewer, evidence-based, on `agent/opt-continuous` at `bdc4cb9` (all ten slots committed as `e427dd9`…`9464fe1` after the dispatch-id commit `a05a054`; the interleaved parent commits touch only `.agent_workspace/PROGRESS.md` — **no parent fix-joint was needed this round**, against three in Round 6). Every verdict was checked against the actual diff, not the slot report. Independent gate and crash probe run on this VM (Node v22.14.0). Working tree at review time: clean apart from the parent-owned `.agent_workspace/PROGRESS.md`. Nothing committed by this reviewer; no `src/**` or `docs/**` file edited; no scratch files (gate/probe logs live under `/tmp`, not the repo).

## 0. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R7-1 reconstruct child specs from the log | **ACCEPT** | `childTasksFromLog` implements R6-2's design at the single call site with the `MODEL_ROUTED` trap handled where the reviewer verified it exists (`assignedRoutes` filters `agentRole !== undefined`; `routing/assign.ts:115` supplies the field, the supervisor's per-node route does not); the rebuilt-spec pin is consciously rewritten with a `notDeepEqual` against the retired `SYNTHESISED_LIMITS`, the FAIL-unreachable tripwire is present with its order updated not weakened; the contract disclosure is honest and reviewer-verified structurally (`replay.ts`/`checkpoint-store.ts` carry no `contract`; `episode-bind.ts:78` copies only `acceptanceCriteria`); the tighter-limits behavior change and the one loosening (no-sibling `maxAttempts` 2→3, from `defaultRunLimits().maxAttemptsPerTask = 3` — verified) are both disclosed rather than smoothed over |
| R7-2 criteria are prompt-guidance (option b) | **ACCEPT** | `src/tracking` diff verified mechanically comment-only (67 insertions, 0 deletions, zero non-comment added lines); `coverageOutcome` (`prescore.ts:158-162`) still has no `FAIL` in its range; the R6-2 tripwire is green at HEAD (gate test 1115); the §4 finding — option (a) is three changes, because `evaluateGates` has no criterion code and `turn.ts:89/94-95` feeds the gate the **uncapped** `P` while `cappedByHardFail` caps `displayPrescore` only — reviewer-verified in source; R6-2's non-re-runnable `/tmp` sweep is now a 270-cell in-tree test, closing the Round 6 review's complaint |
| R7-3 absorbing-BLOCKED pins + unblock design | **ACCEPT** | Commit verified test-plus-report only (no `src/**`); the two pins hold the currently-unreachable `persistCompleted` refusal by source region and prove four plausible substitute signals (`RUN_STARTED`, injection, `TASK_STATUS_CHANGED` READY, `GATE_TRANSITION` to RUNNING) all leave replay BLOCKED with the latch set; the schema-absence assertion on `EVENT_TYPES` is an explicit Round 8 tripwire; `RUN_UNBLOCKED` design covers event id targeting, a dedicated locked `unblock` command, replay clearing, checkpoint restore for both block shapes, and keeps the `applyRetry` sole-producer pin untouched — the sign-off boundary respected exactly |
| R7-4 parent-plane terminal refusal | **ACCEPT** | All three parent-plane terminal appends (not just the briefed catch-all) now route through one `recordTerminal` fed by `loggedTerminalStatus` → `replayedTerminalStatus` — the widening disclosed with the R6-1 precedent and covered by tests 3–5; choosing replay over `recordCrashTerminal` is argued from that module's frozen rethrow clause, not taste; the honest reachability census ("a guard, not a fix for an in-loop defect — the log has other writers") is the round's best example of not over-crediting a change; ordinary crash still records `RUN_FAILED` with the raw un-prefixed reason (test 2), BLOCKED-then-crash refuses the second terminal (test 1), both re-verified green here |
| R7-5 BLOCKED `next:` | **ACCEPT** | `main.ts` diff verified purely additive (50 insertions, 0 deletions — every existing pinned string byte-identical by construction); both flowchart-outcome sites guarded, `flowchartExitCode` still the return; the reason is read off the newest `RUN_BLOCKED.payload` (`findLast`, matching `inspection.ts`'s last-writer rule) so all four producers render truthfully; the `note:` honesty clause is driven through the CLI (pin 4 resumes and gets BLOCKED back) not narrated; the `--children` offline-unreachability wall is answered the way `undelivered-mail.test.ts` answered the same wall, with a mutation check keeping the wiring pin non-vacuous |
| R7-6 Round 6 docs truth-up | **ACCEPT-WITH-NITS** | The landed content verified accurate to Round 6 source (first-replayed-terminal contract, clarification lock, refused-start/refused-resume postures, `learnedState` shape and the three routes, `--lock-wait-ms` bounds and the pause posture, cascade wire witness, probe case 9); ADR-006 untouched and Proposed at HEAD with every reference agreeing (verified) — the R6-7 failure not repeated; **but** the in-flight disclosure names R7-1/2/3 only, while R7-4/5/8 were also mid-flight at 21:01 (their edits landed 21:08–21:16) — the wording traces to the dispatch instruction, so the fault is shared, but a census of the working tree would have caught it; and the status-matrix R1 row's `loadProjectBandit` naming was accurate at 21:04 and stale four minutes later when R7-8 landed — standard follow-through staleness, Round 8 docs material |
| R7-7 delete `settleSupervisedOutcome` | **ACCEPT** | Deletion verified exact: the helper, its always-no-op call in `finishSupervisedRun`, and its gate-only imports (`applyTrackingGate`, `nextTrackingSeq`, `hashAssessment`, `TrackingAssessment`, `EventId`) all gone, settle-tail comment truthed; the absence pin is a namespace-export check in a new 7-line file so reintroduction under the old name goes red; R6-3's lock/pre-flight/settle structures untouched (diff inspected); shared-tree transients attributed to R7-8/R7-9 mid-edit and confirmed self-resolved |
| R7-8 keyed bandit read + pure preference reader | **ACCEPT** | All five files verified: `projectBanditPath` is now the single spelling of the layout with `banditPath` delegating; `loadProjectBanditByKey` carries the same contract; `readPreferenceSnapshot` is a pure reader `loadFromDisk` is now expressed through (one snapshot read path, binder posture unchanged); doctor deletes the base-31 preimage and the bind/unbind pair; the signed-off pin's `assert.match` moves to the new symbol in the same diff with the `because` intact **and the readers sweep widened to `loadProjectBandit(?:ByKey)?`** so the new symbol cannot become a hole in the guard; the two behavioral deltas (foreign-magnitude keys now classified; vanished `preferences.json` now `absent` not `readable`) are corrections in writer-unreachable corners, both pinned with claimed baseline-fail verification |
| R7-9 reject empty task graphs | **ACCEPT** | The O(1) guard verified first in `validateTaskGraph`; the persist-nothing refusal pinned with `readdir(stateRoot)` empty; the R6-3 pre-rounds crash pin re-seeded on the disclosed episode-store variant in the same diff exactly as the brief ordered, with the settle-shape change (`afterTerminal` `["EPISODE_CLOSED"]` → `[]`, since the failed bind exposes no episode) disclosed as "no invented closure"; the stale reproduction comment in `supervisor.ts` (R7-7's sole file) flagged for its owner rather than edited — ownership discipline exactly right; one coverage note in §4.2, not rollback material |
| R7-10 STOP — no catalog CLI producer | **ACCEPT** | The stop is correct and reviewer-reproduced at the census level: `loadCatalogObservedSnapshot`'s complete `src/**` caller set is its own module plus `src/cli/doctor.ts` (verified by grep), and doctor **absorbs** `CatalogObservedCorruptError` into a `damaged`/`derived` inventory entry (`doctor.ts:590-601`, verified) rather than throwing it into `main` — so no real command can select the frozen catalog route; the bandit half was reproduced through a real `adapt auto` before stopping, so Round 8 inherits a half-verified candidate, not a guess; no one-sided test was added, per the R5-8/R5-9 precedent and the slot's explicit instruction |

9 ACCEPT, 1 ACCEPT-WITH-NITS, 0 ROLLBACK. No lint or test blocker found (independent full gate GREEN), so there is nothing to commit from this review beyond this file and `ROUND7-BRIEF.md`.

## 1. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0**: **1804 tests / 1803 pass / 0 fail / 1 skipped** — matching the parent's numbers exactly. The sole skip confirmed by census: the only `skip:` option anywhere in `test/**` is the `PI_SMOKE=1` gate in `provider-smoke.test.ts`; no `.skip(`/`test.skip`/`it.skip` anywhere. **No slot introduced a new named skip.** Delta vs the Round 6 baseline (1769/1768/1): **+35 tests**, and the accounting closes exactly: R7-1 +3 (abort 20→22, resume 6→7), R7-2 +10, R7-3 +2 (replay 12→14), R7-4 +7 (new file), R7-5 +8 (new file), R7-7 +1 (new file), R7-8 +2 (doctor 15→17), R7-9 +2 (new file; the supervisor-crash reseed is count-neutral at 11).
- **`node scripts/crash-probe.mjs` → `ok: true`, 9 cases × 3 iterations** — case set unchanged from Round 6 (no slot touched the probe, so `crash-recovery.test.ts`'s reduced-probe pin needed no edit and got none).
- Key new suites re-run scoped after the gate (`parent-terminal-refusal`, `blocked-next`): 15/15. The specific requested pins confirmed green inside the gate by name: `check-coverage cannot fail…` (1115), `a node re-executed on resume runs under the spec the log recorded for it` (1112), `a crash after the run is blocked reports the block…` (1146), `an ordinary crash still records RUN_FAILED…` (1147), `all three flowchart terminal recorders refuse while replay names BLOCKED` (1169), `current operator and scheduler signals cannot clear RUN_BLOCKED…` (1170), `a criteria-shaped FAIL caps the displayed prescore and leaves the gate open`.

### Freeze check — held, no undisclosed exceptions

- `git diff a05a054..HEAD` over `src/persist/`, `package.json`, `pnpm-lock.yaml`, `src/run/episode-bind.ts`, `src/cli/episode.ts`, `src/telemetry/model-invocation.ts`, `src/run/gate-apply.ts`, `src/run/checkpoint-store.ts`, `src/run/event-store.ts`, `src/run/scheduler.ts`, `src/run/crash-terminal.ts`, `src/run/events.ts`, `src/run/replay.ts`, `src/cluster/**`, `src/supervisor/**`, `src/routing/**`, `src/track/**` is **empty**. `writeFileAtomic`(+Sync), jsonl, `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE` byte-identical. No dependency edit.
- **No new private temp+rename**: zero `.tmp`/`writeFileAtomic` matches in the whole round's `src` diff.
- **The two decision pins hold**: `append does not block on the run lock` (`event-store.test.ts:119`) and `checkpoint writes do not block on the run lock` (`checkpoint-store.test.ts:156`) present and gate-green; both stores untouched. R7-4's guard adds one `readAll()` per run *terminal* (not per step) with no lock — the pins are unthreatened.
- **`applyRetry` sole BLOCKED→READY producer**: `scheduler.ts` untouched; R7-3's design explicitly preserves the pin and prescribes a distinct flowchart reopen operation plus a negative pin for Round 8.
- **One definition of run-terminal**: nothing re-derives "terminal" locally. R7-4 *consumes* `replayedTerminalStatus` (its test 7 pins the consultation and forbids a private terminal set); R7-3's pin holds all three flowchart recorders to `alreadyTerminal(ctx)` delegating to the same function; `TERMINAL_REPLAY_STATUSES` untouched.
- **No new persisted schema**: `events.ts` diff empty; R7-3's pin additionally asserts `EVENT_TYPES` contains no `RUN_UNBLOCKED`.
- **ADR-006 Proposed** at HEAD (`docs/decisions/0006…md` §Status verified; `how-to-adapt-to-pi.md:14,229` and `m0-m2-architecture.md:365` agree), nothing Outcome-supported (status-matrix retains the sentence), no live R1/bandit/topology, no `package.json` edit — all verified.
- **Live isolation**: `src/**` readers of stored bandit state are exactly `bandit-store.ts` (itself) and `src/cli/doctor.ts` via `loadProjectBanditByKey` (the sign-off followed the symbol; the pin moved with it in the same diff, per the recorded parent sign-off in `OWNERSHIP.md`); `selectArm`'s only `src` caller remains `routing/shadow.ts`, outside the live closure. The root-keyed `loadProjectBandit` now has **no `src` caller outside its own module** (three test callers survive) — see finding 4.
- Sole-ownership held everywhere: `flowchart-run.ts` edited only by `e34dddb`, `coordinator.ts` only by `9464fe1`, `main.ts` only by `59984ca`, `supervisor.ts` only by `12726ed`, `prescore.ts`/`from-child.ts` only by `b32584b` (comment-only), `doctor.ts`/`bandit-store.ts`/`preferences/store.ts` only by `5785ee2`, `graph/validate.ts` only by `33ea5c0`, `docs/**` only by `b3c996d`. The round's only out-of-sole-list test touch is R7-9's authorized `supervisor-crash.test.ts` reseed, confined to the one seed/pin as claimed (22 changed lines, inspected). No slot committed; the parent committed each landing.

## 2. Requested target verifications

### 1. R7-1 reconstruction — VERIFIED
`assignedRoutes` (`flowchart-run.ts`) filters `event.payload.agentRole === undefined` **before** inserting, so neither first nor last wins by accident — and the two-producer premise itself was re-verified: `routing/assign.ts:115` writes `agentRole: task.role` while `flowchart-supervisor.ts` contains no `agentRole` at all. The rebuilt-spec pin was **rewritten, not deleted**: same three-leg harness, same field-by-field discipline, the new budget asserted both as a literal and as `deepEqual(original.limits)`, plus an explicit `notDeepEqual` against the named `SYNTHESISED_LIMITS` constant so the tightening is asserted, and a new fixed-point assertion (`requestedSpec(rebuilt[1]) === requestedSpec(rebuilt[0])`). The FAIL-unreachable tripwire is **present and green** (gate 1115); its docstring is rewritten to the true post-R7-1 order (never-ran nodes are the remaining exposure; `childTasksFromLog` is where the decision lands) without weakening the instruction. The contract disclosure is **honest and structurally verified by this reviewer**: `startFlowchartRun` takes the contract as input, `bindEpisodeToRun` copies only `contract.acceptanceCriteria` onto the episode (`episode-bind.ts:76-79`), and neither `replay.ts` nor `checkpoint-store.ts` contains the string `contract` — so `FlowchartContinuation.contract` is a seam no production caller can fill, exactly as disclosed, with the flip-pin (`a resume that is handed no contract assesses its children against none`) landed for Round 8. The one loosening (no-sibling fallback `maxAttempts` 3 vs the old hard-coded 2) checks against `defaultRunLimits()` and is disclosed instead of hidden under "uniformly tightening".

### 2. R7-2 option (b) — VERIFIED
The `src/tracking` diff is comment-only by mechanical check: 67 inserted lines, 0 deleted, and filtering the added lines for anything that is not a comment or blank leaves **nothing**. `coverageOutcome`'s body is byte-identical to the pre-round source and returns only `NOT_APPLICABLE`/`PASS`/`UNOBSERVED` (`prescore.ts:158-162`). The R6-2 tripwire is green. The §4 finding was verified in source, not taken from the report: `turn.ts:89` (`combineScore({ P: prescore.P, … })`) and `:94-95` (`evaluateGates({ P: prescore.P, … })`) both take the **uncapped** `P`; `cappedByHardFail` reaches only `displayPrescore` (`prescore.ts:73-74`), which `from-child.ts:103` copies into the recorded turn — so the hard-fail cap is a display value and the obvious form of option (a) really would be a gate no-op. The sole-production-path pin's premise re-verified at HEAD: `src/run/child-tracking.ts` is the only importer of any tracking behavior module outside `src/tracking/`.

### 3. R7-3 — VERIFIED
No `src/**` in the commit (`replay.test.ts` + report only). The absorbing pins are exactly as reported: pin 1 covers the unreachable `persistCompleted` direction by bounded source-region match on all three recorders plus the `alreadyTerminal` delegation shape; pin 2 first asserts the vocabulary has no `RUN_UNBLOCKED` (the deliberate Round 8 tripwire, marked replace-not-weaken), then drives all four substitute signals and proves the latch survives each, including that a subsequent `RUN_FAILED` is still the multiple-terminal anomaly — the latch proven, not inferred from the displayed status. No new schema anywhere in the round (§1).

### 4. R7-4 — VERIFIED, both arms
Test 2 (`an ordinary crash still records RUN_FAILED naming the escaping error`) and test 1 (`a crash after the run is blocked reports the block instead of burying it`: BLOCKED status + checkpoint, `terminals === ["RUN_BLOCKED"]`, no `RUN_FAILED`, `anomalies: []`, `EPISODE_WAITING` present, `EPISODE_CLOSED` absent) both green in the gate and in a scoped re-run. The code path verified in the diff: the crash catch routes through the same `recordTerminal` as the loop's two exits, the settle/checkpoint/return all take the replayed status on a refusal so log, episode, checkpoint and `RunOutcome` agree. The disclosed widening beyond the briefed catch-all is real, tested (3–5), and precedented. Residuals 3 (WAITING_FOR_USER burial — the refusal is terminal-keyed, and `replayedTerminalStatus` correctly excludes WAITING_FOR_USER, so the disclosure is accurate) and 4 (`RUN_CANCEL_REQUESTED` unguarded) verified as stated. One cosmetic note in §4.3.

### 5. Freeze — HELD (§1). Nothing beyond the disclosed, authorized edits.

### 6. Isolation — VERIFIED
`rg loadProjectBandit src/` → `bandit-store.ts` (definition + delegation) and `doctor.ts` calling `loadProjectBanditByKey` only; the amended pin requires the new symbol and its widened readers sweep forbids any other live reader of either symbol; `selectArm` has no live-closure caller (`shadow.ts` only). `live-isolation.test.ts` green in the gate at HEAD.

### 7. No new named skip — VERIFIED (§1 census; gate skip count exactly 1).

### 8. R7-10 STOP — JUSTIFIED, reviewer-reproduced at the census level
`loadCatalogObservedSnapshot` has exactly two `src` references: its definition and `doctor.ts:578`, where the call sits inside a try whose catch converts `CatalogObservedCorruptError` into a `damaged` inventory entry with `scanErrors` untouched (`doctor.ts:590-601`) — the error is absorbed as advisory inventory, never thrown into `main`, so `commandFailureNext` can never select the frozen catalog route from a real command. Building a producer is a `src/**` decision outside R7-10's ownership; stopping was the correct move and the report hands Round 8 a reproduced bandit-side producer (`adapt auto` over truncated bytes at the real key path) so half the original candidate is already de-risked.

## 3. Per-slot notes beyond the targets

- **R7-1 (ACCEPT).** The census-first correction to R6-2's own table ("8 of 9, not 9 of 9" — the contract was never a `ChildTaskInput` field and is on no durable record) is what a census is for: it changed the design honestly instead of forcing the brief's wording. The three named properties all verified in the diff: the trap filter, the fixed point (last `TASK_REQUEST` per task, so repeated resumes converge — pinned), and the catalog honesty note on `CascadeCatalog`. The choice *not* to adopt the supervisor's own per-node route when no assignment exists ("adopting it would be inventing a spec, which is the defect, not the fix") is the right call and the kind of reasoning that keeps this fix from quietly widening. The cascade pin is behavioral where inspection is impossible (no `TASK_REQUEST` carries a cascade): escalation `cheap → premium` with the executor's `modelIds` as the wire-adjacent witness. Disclosure 5 (`FlowchartContinuation` gained an optional field; `main.ts` unaffected) checks — R7-5's diff builds continuations without `contract` and compiles clean.
- **R7-2 (ACCEPT).** The right slot shape for a decision that was already made: the record lives on the functions that own it, and three record-keeping pins keep it from rotting (prose in both files, the named tripwire must exist, the sole-path census must stay true). The census corrections beyond the brief are each verified: criteria are *not* decorative everywhere — they reach the child prompt and the plan-time coverage gate — so the recorded statement is the narrower true one ("decorative at the gate"); and `derivedClaimedVerificationWithoutChecks` is a second copy-fed consumer that can add an anomaly code but never the leading one (`mapGateDirective` reads `codes[0]`), pinned. The mutation-check methodology (temporarily make `coverageOutcome` FAIL, watch the tripwire go red and the directive sweep stay green) is how the three-changes finding was *measured*, and it upgraded the Round 8 scope for option (a) from folklore to source-verified fact.
- **R7-3 (ACCEPT).** The design answers every question the Round 6 brief posed: the event (with `blockedEventId` targeting so a stale unblock cannot clear a later block), the producer (a dedicated locked `unblock` command, with three stated reasons it is not a fourth injection kind — the lock incompatibility argument is the load-bearing one), replay clearing (latch + active-terminal interval, so the existing recorders open with zero per-writer edits — the integration seam R6-1 and R7-4 built is used, not bypassed), checkpoint restore for both block shapes with the event-first crash window handled, and the `applyRetry` pin explicitly preserved with a prescribed negative pin. The consumer census for Round 8 (`events.ts`, `replay.ts`, `gate-apply.ts`'s separate gate-status reconstruction, `event-row-fuzz`'s exact-keyed seeds) is accurate as far as this reviewer checked and one entry short — see finding 1.
- **R7-4 (ACCEPT).** The refusal-vs-`recordCrashTerminal` argument is the round's best frozen-contract reasoning: the helper's docstring promises "the caller always rethrows", this plane's catch deliberately settles, and falsifying a frozen module's contract to reuse it would have been worse than a parallel consumer of the shared definition. The reachability honesty (§4: no in-loop path can produce the pair today; the guard is real because `ChildCoordinator` writes the same log and `append` takes no lock, and R7-3 is designing a producer) prevents the report from claiming a fixed defect that does not exist — the same discipline R6-1's `persistCompleted` note showed. The multi-run/same-id-stream trap recorded in test 5's history will save a future slot an afternoon.
- **R7-5 (ACCEPT).** The formatter reads the payload rather than assuming the gate's reason — verified necessary, since the stall producers write a different `reason` into the same shape — and the `(none recorded)` choice is information-preserving. Not reusing `cliFail` is correct for the stated reason (a BLOCKED run is not a thrown command error; a second `ok:false` envelope on that path would surprise `parseCliErrorJson` consumers) and costs nothing (exit code identical). The scope call on `resume`/`answer` is properly argued: the loopback pin byte-pins `resumed.err` **equal** to the executor-config disclosure on a BLOCKED supervised resume, so wiring resume means amending a frozen pin — left with a precise prescription instead. The mutation check (pin 7) that deliberately deletes the `--children` guard to prove pin 6 non-vacuous is the honest answer to a branch offline tests cannot reach.
- **R7-6 (ACCEPT-WITH-NITS).** Content spot-verified against Round 6 landed source: the terminal contract paragraph, the clarification-lock and refused-start/refused-resume prose, the `--lock-wait-ms` bounds with the pause posture reasoning, the SIGKILL/probe-case-9 paragraph, the amended isolation wording ("never feeds selection"), and the cascade wire witness in the Pi executor row — all accurate as of 21:04. The nits: (1) the in-flight disclosure understates the round — R7-4/5/8 were also mid-flight and their edits were visible in the shared tree; the slot followed the dispatch wording ("disclose in-flight R7-1/2/3") instead of censusing, which is the same class of instruction-trust R6-7's lesson warned about, at far lower stakes; (2) five slots landed after the snapshot, so the docs now understate the tree (no BLOCKED `next:` block, no reconstruction, no parent-plane recorder, `loadProjectBandit` naming) — correct method, known cost, Round 8 material. Neither nit touches a forbidden item; the ADR discipline was kept.
- **R7-7 (ACCEPT).** Small slot kept small, exactly as the brief ordered. The census is the right kind (exact symbol search, import census, barrel/dynamic-import audit, consumer-pin audit including the observation that removing gate-only imports adds nothing to the live closure) and the delete-not-wire decision follows the gathered evidence (no assessment producer exists on the supervised plane — wiring would be an unrequested feature plus a sign-off). The absence pin's namespace-membership form is stronger than a source grep. Transient attribution to R7-8/R7-9 mid-edits, with exact reruns after their owners finished, is the shared-tree process working as designed.
- **R7-8 (ACCEPT).** The preimage post-mortem is the report's centerpiece and it is correct: the base-31 round trip held for every producible key, so the deleted code was *right* — and load-bearing on the hash algorithm never changing, which is precisely why it had to go. Both behavioral deltas are honestly classified as corrections in corners the shipped writers cannot reach (a foreign-magnitude key like `pffffffff`; a TOCTOU disappearance of `preferences.json`), and both got pins verified against the restored old doctor (claimed baseline-fail; consistent with the deltas' direction). Leaving `command-error-doctor.test.ts`'s now-unnecessary `finally` repair alone because it is another slot's file — with a note that R7-10 may drop it — is ownership discipline done right. Three new exports, all with live callers, honoring R6-9's rule.
- **R7-9 (ACCEPT).** Tighten was the evidenced choice and the guard sits exactly where the brief wanted the refusal: pre-flight, before the lock, before any write — pinned as persist-nothing with the M0-style `readdir` assertion. The reseed obligation from R6-3's disclosure 4 was honored in the same diff, and the shape difference of the new seed (the failed bind exposes no episode, so no `EPISODE_CLOSED` follows the terminal) is asserted rather than papered over. The disclosed handle nuance (the synchronous `runId` is still published; no record is created for it) is the honest residue of R6-3's hoist architecture. See finding 2 for the one thing the reseed narrows.
- **R7-10 (ACCEPT).** A stop that produced value: the bandit route's real producer is reproduced end-to-end (command, stage, routed `next:`, damaged path in the error) and ready to be pinned by whoever picks it up, and the catalog route's unreachability is established by a complete caller census plus a live `doctor --json` reproduction of the absorption behavior. The one improvement available — the report could have named the smallest viable producer surface for Round 8 — is optional; the brief's own instruction was to stop and report, and it did.

## 4. New reviewer findings (evidence for Round 8)

1. **Round 8's two P1 candidates collide on files, and the collision must be resolved at dispatch.** The unblock implementation (R7-3's design) needs `src/run/replay.ts` (latch clearing) and `src/run/flowchart-run.ts` (locked producer + checkpoint restore) plus `events.ts`/`gate-apply.ts`/`main.ts`; contract durability (R7-1's disclosure 3) needs `replay.ts` (`FlowchartCheckpointState.contract` + `validateCheckpoint`) and `flowchart-run.ts` (write at start, read at resume). Two P1s, same two core files — they cannot be separate slots in one round-half. One additional consumer R7-3's Round 8 map does not list: `formatBlockedRunReport`'s `note:` line promises "no event clears a BLOCKED log today", and R7-5's pin 2 asserts both of its clauses — the unblock slot owns that string and that pin going red (R7-5's handoff says so; the unblock implementer must own `main.ts` anyway for the `unblock` command, so the contention is triple).
2. **The pre-rounds "settle closes the bound episode" shape now has no seed.** R7-9's reseed (correctly) moved the pre-rounds crash pin onto the episode-store variant — but that seed fails the *bind itself*, so the pre-rounds window's settle now runs against no bound episode (`afterTerminal === []`) in the only pre-rounds pin. The rounds-window `EPISODE_CLOSED` pins survive (`supervisor-crash.test.ts:179/250/416`), but "a death after a successful bind and before rounds open settles the episode" is exercised nowhere: the empty-list seed that used to reach it is now refused in pre-flight. A seed still exists (fail the `TASK_GRAPH_ACCEPTED` append after a successful bind — the armable-id-exhaustion pattern R7-1's new harness uses). Test-only, small. → R8-10.
3. **`coordinator.ts:608`'s `failureReason` is now write-only outside the child-failure branch** — the crash path passes the reason straight into the recorder's payload without setting the variable, and nothing reads it after the try/finally. Harmless (the reason is recorded; nothing consumed the variable), purely cosmetic scope residue of R7-4's refactor. Fold into whichever slot next opens `coordinator.ts`; not worth a slot.
4. **The root-keyed `loadProjectBandit` now has no `src` caller outside its own module.** After R7-8, its callers are `bandit-store.ts` itself (delegation) and three test files. R7-8 explicitly declined to delete it ("the natural symbol for the adaptation loop… not this slot's call"). It is now exactly the shape R6-9's census dispositioned case-by-case: either it is a contract seam worth keeping (the adaptation loop's future read path) or a census-and-delete with an absence pin. One-slot decision, evidence already gathered. → R8-9.
5. **Docs at HEAD understate the tree in five specific places, all with hand-off wording already written:** the BLOCKED `next:` block (R7-5 §7 wrote the runbook line), reconstruction and the contract seam (R7-1 §7 wrote the paragraph), the parent-plane recorder (R7-4 §8), criteria-are-guidance and the display-cap fact (R7-2 §7.4 wrote the two lines and named the one doc file mentioning the dimensions), and the status-matrix R1 row still naming `loadProjectBandit` where doctor now calls `loadProjectBanditByKey` (R7-8 §census wrote the replacement sentence; `docs/decisions/0005…md:110` has the same stale claim but is an ADR body — flag, not edit, per the standing rule). `src/run/supervisor.ts:586`'s reproduction comment also still names the now-refused empty-list seed (R7-9 flagged it; it is a `src` comment, so it needs its owner or a disclosed comment-only edit with the R5-1/R6-3 precedent, not a docs slot). → R8-5.
6. **Option (a)'s true scope is now three subsystems and at least one protocol decision.** R7-2's verified finding means "make check-coverage real" requires: a child-side per-criterion outcome channel (the protocol today carries one verdict per task — a `protocol/v1` schema decision), a gate path (a new anomaly code in `gates.ts` or routing the cap into what `turn.ts` scores — a semantics change to real dimensions too, see the `cappedByHardFail` question R7-2 §7.3 separated out), and the never-ran-node decision R7-1's tripwire docstring now assigns to `childTasksFromLog`. Round 8 should treat it as design-first with parent sign-off on the protocol shape, not an implementation slot. → R8-4.
7. **This round's process record is the cleanest yet and worth naming:** zero parent fix-joints (Round 6 needed three), zero caught-by-others breaks left at HEAD, every consumer-census obligation honored in-diff (R7-1 rewrote R6-2's pins, R7-8 moved the signed-off pin, R7-9 reseeded the crash pin), and every shared-tree transient attributed to its owner with exact post-landing reruns (R7-2→R7-1's in-progress pin, R7-7→R7-8/R7-9 mid-edit, R7-10→R7-9's momentary `tsc` break). The Round 6 brief's three new process rules all show up executed. The one soft spot is R7-6's instruction-trust nit (§3).

## 5. Handoff

- `ROUND7-BRIEF.md` (same directory) carries the Round 8 injection context: what landed, the new baseline (gate 1804/1803/0/1-skip; probe 9×3; frozen list grown with the Round 7 contracts), and ten ranked candidates with the unblock/contract-durability file collision stated up front and the option-(a) scope correction folded in.
- Nothing in flight at review time: all ten slots are committed; the working tree holds only the parent-owned `PROGRESS.md` edit.
- Nothing was committed by this reviewer, no `src/**` or `docs/**` file was edited, and no fix was needed anywhere. The parent should commit `.agent_workspace/loop4-r7-review.md` and `.agent_workspace/ROUND7-BRIEF.md`.
