[Model: claude-fable-5-thinking-xhigh]

# Loop 4 · Round 10 — SOTA review at `6c60ba6`

Reviewer ran independently on this VM (Node v22.14.0), on `agent/opt-continuous`, working tree clean at HEAD `6c60ba6`. Every verdict below is against the actual diff `8e933a7..6c60ba6` — the ten exclusive landings (`d4741e6`…`66edccb`) plus the two parent commits (`fdb46e4`, `6c60ba6`), which touch only `.agent_workspace/**` — verified per-commit with `git show --stat`, not taken from slot reports. No file outside `.agent_workspace/` was changed by this review; scratch outputs lived in `/tmp` (`/tmp/r10-gate.log`, `/tmp/r10-crash-probe.log`, `/tmp/routes-{base,head}.txt`, `/tmp/pin-{base,head}.txt`).

## 1. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R10-1 | **ACCEPT** | `RUN_UNBLOCKED_WITH_DISCARD` shipped exactly as signed off: distinct exact-keyed event with in-payload charged estimates fail-close-validated against cited `MODEL_ROUTED` rows before the append; one append per operator act; restore recomputes the consequence set and refuses a hand-edited list by exact message; `RUN_UNBLOCKED` byte-frozen at three keys and rejecting `rewoundDescendants`; R9-3 refusal pin byte-identical; routes freeze character-exact through the `main.ts` edit; four BLOCKED lines byte-preserved under a new prefix pin; contract carried on the discard-written, resumed, and crash-recovered checkpoints (§4.1) |
| R10-2 | **ACCEPT** | No `src/**`; pins 1/3/4/5 untouched (diff is insert-only) and now meta-pinned by exact title; pin 2 untouched; `VerificationResult.criteria` frozen unimplemented at both the type level (`"criteria" extends keyof …` compile check) and the source level |
| R10-3 | **ACCEPT** | Docs only; ADR-006 (`0006-pi-extension-reverse-adapter.md`) still Proposed; `0005:110` region stays flag-only; the nine→ten probe line fixed (`m0-m2-architecture.md:776`); all three surfaces name the real pin path `test/integration/persist/crash-recovery.test.ts`; the discard is described accurately against the shipped payload; the 23:31 UTC sync note's "not yet a HEAD commit" for R10-1/8/9 went stale on the parent's 23:32 commits — the unavoidable docs-race cost, honestly dated (R9-4(b) precedent), not a miss |
| R10-4 | **ACCEPT** (disclosed PARTIAL) | The writer-census pin is a genuine **property** — an AST census of every `materializeCheckpoint` call with a flowchart payload across all of `src`, initializer-resolving, with a `writers.length > 0` vacuity guard — green with R10-1's writer in place; the pure-CLI `--track`→pause STOP is justified in source (§4.2), explicitly reported, and correctly not papered over with an embedder seed or a manufactured pause |
| R10-5 | **ACCEPT** | Mechanically verified comment-only: 0 non-comment added lines, 0 removed lines across `from-child.ts`(+10)/`prescore.ts`(+12)/`child-tracking.ts`(+1); field not renamed; `runStatus` still has zero `src` readers (re-swept); the 5-test pin file holds the posture, the whole-`src` dereference absence, rewrap-safety on the real sources, and the flag's producer/consumer inertness |
| R10-6 | **ACCEPT** | Four additive standing-rule pins, each closing a named gap: adversarial identity (model-supplied `from`/`runId`/`taskId` cannot displace the lease), explicitly-empty FAILED `evidenceIds` refused with nothing emitted, repeated *identical* verdict refused (one call per attempt, not one per value), and an AST pin that the verdict tool is a direct `tools`-array element — proven non-vacuous by an in-memory opt-in-flag mutant the pin rejects |
| R10-7 | **ACCEPT** | Additive only; the `openEpisode` projection pinned to episode metadata + `acceptance: contract.acceptanceCriteria`; a recursive census over all 215 `src` modules that names its own found readers (anti-vacuous) and rejects any episode reader constructing contract-shaped output; a mutation guard proves the census rejects `constraints: []`; a behavioural pin proves a persisted episode carries the criterion but no contract field |
| R10-8 | **ACCEPT** | Additive only; not vacuously green — the new assertion *requires* discard/`reopenAfterUnblock` identifiers to exist in both flowchart sources (it fails at base, before R10-1) and then reapplies the existing whole-file `assertNoSchedulerRetry`; committed at 23:32:24, after R10-1's 23:32:18, so it was written against the landed implementation; not over-fitted (loose case-insensitive marker regex, whole-file check unchanged) |
| R10-9 | **ACCEPT** | Additive only; the ninth test drives a real `RUN_UNBLOCKED_WITH_DISCARD` through the actual gate apply and asserts both identity cases — matched clears to `from: "RUNNING"`, unmatched stays `from: "BLOCKED"` — observing only the next transition record; `runStatus`/`applied`/`transitionId` gain no reader; fails at base (event absent), so not vacuous either |
| R10-10 | **ACCEPT** | New freeze file; the runtime pin discovers every `EVENT_TYPES` entry starting `RUN_UNBLOCKED` (two at HEAD) and proves each absent from `RUN_STATUSES` and `TERMINAL_REPLAY_STATUSES`; the type-level `Extract` intersection makes any current or future `RUN_UNBLOCKED*` literal overlapping `RunStatus` a whole-tree `tsc` failure — green before R10-1 landed and automatically covering the new event after |

**10 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK.** Zero parent fix-joints; nothing left red at HEAD. (R10-4 is a disclosed, source-justified PARTIAL executed per the R5-8/R7-10 stop-and-report precedent — the deliverable that could land landed.)

## 2. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0: 1915 tests / 1914 pass / 0 fail / 1 skipped** — matches the parent's recorded numbers exactly. The one skip is the standing `PI_SMOKE` real-provider gate (`PiAgentExecutor completes a run against a real provider # SKIP`); no slot introduced a second skip.
- **`node scripts/crash-probe.mjs` → `ok: true`, 10 cases × 3 iterations** — matches. The ten names and their order are unchanged from Round 9; `unblock-append-before-checkpoint-sigkill` is still last.
- **The +35 over Round 9's 1880/1879/1 closes per-slot.** Static test-registration deltas per changed test file, base `8e933a7` vs HEAD: R10-1 **+15** (`cli/unblock` 6→9, `unblock-flow` 8→13, `event-row-fuzz` 5→7, `replay` 19→23, `flowchart-supervisor` 25→26, `blocked-next` 11→11 — that file's delta is edits inside existing tests) · R10-2 **+2** (`protocol/v1` 18→19, `option-a-preconditions` 5→6) · R10-3 **0** (docs) · R10-4 **+1** (`resume` 15→16) · R10-5 **+5** (new `independent-evidence-posture.test.ts`) · R10-6 **+4** (`report-task-result` 13→17) · R10-7 **+4** (new `episode-contract-boundary.test.ts`) · R10-8 **+1** (2→3) · R10-9 **+1** (8→9) · R10-10 **+2** (new freeze file). Sum: exactly 35.
- **Commit hygiene:** each of the ten landings touches only its `OWNERSHIP.md` files plus its own report — verified per-commit with `git show --stat`; no two slots share a file. `fdb46e4`/`6c60ba6` touch only `.agent_workspace/**`. Commit order is load-bearing and correct: R10-1 landed at 23:32:18 and the three commits that reference or race its symbols (R10-8, R10-9, R10-3) all landed at 23:32:24, after it — every commit point is a green tree. The dirty-tree snapshot visible at dispatch was the pre-`6c60ba6` shared working tree; everything in it is inside the ten commits.

## 3. Freeze check

All verified against the actual range diff and the tree at HEAD, not report claims:

- **Allowed-to-change set honoured; no undisclosed extras.** `src/**` changes are exactly: R10-1's six signed-off files (`run/events.ts`, `run/replay.ts`, `run/flowchart-run.ts`, `supervisor/flowchart-supervisor.ts`, `run/gate-apply.ts`, `cli/main.ts`) and R10-5's three comment-only files — the latter mechanically verified (filtering added lines through a comment pattern leaves zero; zero removed lines). Diff-empty across the round: `package.json`, `pnpm-lock.yaml`, `scripts/**`, `src/run/scheduler.ts`, `src/run/crash-terminal.ts`, `src/run/episode-bind.ts`, `src/persist/**`, `src/protocol/**`, `src/track/**`, `src/routing/**`, `src/pi-adapter/**`, `docs/decisions/**`, `test/integration/pi-adapter/loopback-cli-resume.test.ts`.
- **jsonl / `writeFileAtomic` / no new private tmp+rename:** the round's entire `src` diff adds **zero** raw fs write primitives (grep of added lines for `writeFile`/`appendFile`/`rename`/`createWriteStream` and Sync variants: empty). The discard path writes through the existing `eventStore.append` + `CheckpointStore.write`, in that order, under the existing `withRunLifecycleLock`.
- **`RUN_UNBLOCKED`:** payload at HEAD read directly (`events.ts:252-256`) — exactly `{ blockedEventId, reason, retryNodeId? }`; its validator hunk is untouched (the events diff is insert-only); the fuzz suite additionally asserts it refuses `rewoundDescendants` by exact message. The stronger authorization is a **distinct** event with its own exact-key validator; R9-10's type freeze is untouched and the new event has its own bidirectional type-equality pin over both the envelope and the nested entry.
- **`TERMINAL_REPLAY_STATUSES`:** unchanged — the `replay.ts` hunk touches only the docstring; the set is still `COMPLETED`/`FAILED`/`BLOCKED`; R10-10 now pins it at runtime and at the type level, with the `RUN_UNBLOCKED*` family discovered rather than enumerated.
- **Five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT`:** extracted at base and HEAD and **diffed character-exact — identical**, despite R10-1's `main.ts` edit; R9-7's AST freeze is green in the independent gate.
- **`applyRetry` sole scheduler producer:** definition in `scheduler.ts`, production import/call only in `run/supervisor.ts` (independently swept); both files diff-empty; R8-3's whole-file AST pin green, and R10-8 re-applies it over the discard-bearing sources with a marker requirement that proves it ran against them.
- **Isolation:** reviewer's own sweep at HEAD — `\bloadProjectBandit\b` zero `src` matches; `loadProjectBanditByKey` exactly store declaration + doctor import/call; `selectArm` exactly definition + `shadow.ts`. ADR-006 **Proposed** (`0006-pi-extension-reverse-adapter.md:5`); `docs/decisions/` diff-empty; `docs/status-matrix.md` still records "Nothing in this repo is Outcome-supported".
- **Gate `runStatus` ledger:** zero `src` readers re-swept — the identifier appears only in `gate-apply.ts` (declaration/producers/docstrings) and `child-tracking.ts`'s single object-literal write; R10-1's `gate-apply.ts` edit is one condition plus docstring; R10-9's new test observes only the next transition record.
- **Durable contract:** never synthesized (`episode-bind.ts` diff-empty; R10-7's census makes constructing a contract from episode data fail across all 215 modules); both `materializeCheckpoint` flowchart-payload call sites carry `contract` (independently grepped), now held by R10-4's structural property pin; the ordinary-refusal exact message is character-identical at base and HEAD in source, and the R9-3 pin body is **byte-identical** (extracted and diffed).
- **`recordCrashTerminal` rethrow, `withRunLifecycleLock` rules, append/checkpoint unlocked, empty-graph pre-flight, `settleSupervisedOutcome` absence, reconstruction `childTasksFromLog` + FAIL-unreachable tripwire, criteria-are-guidance + 270-cell, parent-plane residuals, `INSPECT_SUMMARY` four keys, loopback supervised-resume stderr pin, catalog posture (b):** owning files diff-empty; all pins green in the independent gate; `INSPECT_SUMMARY` literal read directly — four keys.
- **Verdict-producer rules:** `pi-executor.ts` diff-empty this round; R10-6 strengthened the freeze without touching the producer; `PiExecutorOptions` still exposes no verdict-tool flag (pinned by AST + mutant).

## 4. Requested target verifications

### 4.1 R10-1 — every requested property, against the diff

- **Distinct event, exact-keyed:** `RUN_UNBLOCKED_WITH_DISCARD` joins `EVENT_TYPES` and the `Event` union; payload exactly `{ blockedEventId, reason, retryNodeId, rewoundDescendants[] }` with `retryNodeId` **required**; every entry exactly `{ nodeId, taskId, previousState, modelRouteEventIds, childRunIds, chargedEstimatedCostUsd, chargedEstimatedDurationMs }`. The validator refuses: unknown keys at both levels; bad ids; blank `reason`/`retryNodeId`; empty set; negative/non-finite charges; a `READY`/`SKIPPED` entry carrying references or non-zero charges; an entry repeating the retry target; non-strictly-ascending `nodeId` order (which settles uniqueness in the same comparison); a set with no executed prior state. Every refusal is exercised in `event-row-fuzz` ("refused unless every claim it makes is auditable"), and the closing assertion proves `RUN_UNBLOCKED` still refuses `rewoundDescendants`.
- **CLI:** boolean `--discard-executed`, refused at parse time without `--retry-node` (exact message, tested pre-log); the set is **computed** — the supervisor's `reopenAfterUnblockWithDiscard` walks `downstreamConsequences(retryNodeId).toSorted()` under the lifecycle lock; a gate block still demands the exact `--retry-node` (`resolveRetryTarget`, logic unchanged through a signature refactor); nothing-executed refused by exact message; targetless stall refused by exact message ("this block names no failed node…") with a test proving the refusal wrote nothing.
- **Charged estimates fail-closed:** `discardAuditRecords` reads references and sums off the run's own `MODEL_ROUTED`/`CHILD_RUN_CREATED` rows (never invocation telemetry — absence-is-not-zero is stated in the payload docstring); `assertDiscardAuditMatchesLog` re-derives every entry's sums from the cited rows **before the append** and refuses on a missing row, a row that routed a different task, a sum mismatch, or an uncited child run — so a refusal writes nothing. One honest caveat the function itself discloses: because the producer derives the sums from the same rows it cites, the three refusal arms are unreachable through any public path and are untested — they are refactor-drift insurance, not a live gate (§6.4 carries the residual).
- **Order and recovery:** transform → **one** append → checkpoint, with the two-appends-half-authorization rejection reasoned in-source; `unappliedUnblock` widened to a `ClearingEvent` union and every restore path funnels through one `applyClearingEvent`; for the stronger event, restore recomputes the consequence set from the durable definition + blocked checkpoint and refuses a mismatch by exact message — behaviourally proved with a schema-valid hand-edited row naming an unrelated COMPLETED node (`SCOUT`), refused with `authorized rewinding SCOUT:…, but this checkpoint's consequence set is SUMMARY:…`. The in-process crash test (append durable, checkpoint rolled back) recovers exactly once to COMPLETED with zero anomalies.
- **Semantics:** evidence counts, `CHILD_MESSAGE` rows, ledger facts, routed model and `parallelGroup` survive; `success`/`confidence` and the active route clear; a `WAITING_FOR_USER` rewind releases its pending approval (waiter invariant); neither remaining budget moves — all pinned in the supervisor positive test and end-to-end in `unblock-flow`.
- **Contract:** the discard flows through `unblockLockedFlowchartRun`'s single `materializeCheckpoint` call, which carries `...(contract !== undefined ? { contract } : {})` with the reason in-source. To be precise about the "fourth writer" language: there is no fourth *call site* — the fourth logical writer path (the discard transform) shares the third's call, and R10-4's property census therefore still finds exactly two flowchart-payload sites, both carrying contract. Behaviourally the obligation is proved three ways in `unblock-flow`: the discard-written, the resumed, and the crash-recovered checkpoints all carry `investigationContract()`.
- **Did not edit `resume.test.ts`** (absent from the commit stat). **R9-3 refusal pin byte-identical** (extracted and diffed; now also reached through the CLI). **`TERMINAL_REPLAY_STATUSES` unchanged**; the new event is not a status (R10-10 doubly holds it). **`currentGateStatus` uniform:** one added disjunct; matched/stale/unmatched/post-terminal handling shared with the ordinary event in `replay.ts`'s single case with `${event.type}`-naming anomalies; the re-block test proves a second failure after a discard unblock records `from: "RUNNING"` and needs its own authorization (per-block permission, not a mode). **Routes character-exact** (independent extraction). **R8-3 AST pin holds** (gate + R10-8 + independent sweep).
- **Four BLOCKED lines:** byte-identical, same order, now pinned as a *prefix* in addition to the whole-list assertion; the disclosure is a `note:` (asserted not to start with `next:`), so the ordinary remedy list is not weakened or extended.
- **The unconditional-disclosure judgement — evaluated, accepted.** The premise is source-true: `formatBlockedRunReport(runId, stateRoot, events)` receives no checkpoint, and the block's executed-descendant fact lives only there. The chosen wording is self-conditioning ("**if** that unblock is refused because a descendant … already executed") — it states the precondition rather than implying applicability, so a stall-blocked operator reads a conditional that never fires rather than a wrong instruction. The alternatives are both worse: plumbing the checkpoint into a guidance formatter couples it to a second data source for one line, and keying on the `RUN_BLOCKED` reason string couples operator guidance to a gate reason code. The prefix pin makes future narrowing safe. Additionally, the dead-end is closed at the surface that *does* know: the CLI catches the exact ordinary-refusal message and appends a `next:` naming the flag, leaving the state machine's message untouched.

### 4.2 R10-4 — the property pin and the STOP

- **The census is a property, not a count.** The new test recursively parses all `src/**/*.ts`, finds every `materializeCheckpoint` call with a third argument, resolves a local payload initializer when the argument is an identifier (latest declaration before the call, function-scoped), and requires a `contract` property on each — with `assert.ok(writers.length > 0)` against vacuity and per-writer failure messages. It is green at HEAD *with* R10-1's writer in the tree (independent gate), which is exactly the coordination the brief demanded: R10-1's addition was included, not rejected for changing a count.
- **The STOP is source-justified, re-verified at HEAD:** `startTrackedRun` calls `startFlowchartRun` with no `pause` dependency (`src/track/loop.ts:157-175`; zero `pause` matches in the file); `pauseIfRequested` returns immediately when `ctx.pause === undefined` (`flowchart-run.ts:849`); the pause dep is injected only where `deps.pause` exists (resume/unblock/inject paths). So an offline tracked run completes before `pause` can mean anything, and the shipped `pause` correctly refuses a COMPLETED run — the slot's probe output shows exactly that. The report is an explicit PARTIAL with the production fix prescribed; it did not seed through the embedder API, kill a process to fake a pause, or add a live provider. Not a silent pass.
- **Recommendation for Round 11: yes, wire it** — brief candidate 3 (§6.3). The src surface is small and outside every other candidate's files: `TrackRunInput` gains an optional `pause`, `startTrackedRun` forwards it into `startFlowchartRun`'s deps, and `runCommand` passes `createFilePauseController(stateRoot)`. One genuine design obligation: the e2e must make the pause request land deterministically while the tracked loop is live (the token is polled at round boundaries and is keyed by a run id the pure CLI only prints at the end), so the slot has to design that seam rather than race it — this is the part that needs the parent's sign-off along with the src change.

### 4.3 R10-5 — comment-only, posture recorded

Mechanically verified: `git diff -U0` added lines filtered through a comment pattern leave **zero** non-comment lines; **zero** removed lines (per file: +10/+12/+1). The field is not renamed (declaration, write, and `void` all present at HEAD under their old names); `runStatus` gained no consumer (re-swept). The `{@link GateApplyResult}` pointer above `applyChildThreeLine` is the exact line R9-6 prescribed, with no new import (the type was already imported). The pin file's absence sweep is the strong half: an AST dereference census over all of `src` that `deepEqual`s to exactly the one `void input.independentEvidence` — so both "somebody read it" and "somebody kept the read but dropped the `void`" fail — proven against in-memory mutants of the real sources, rewrap-safe at two widths. Pins 4/5 keep the claim honestly scoped: the flag is a function of `verification.kind` alone at the producer and inert across a 144-cell `computePrescore` sweep, which is evidence for "harmless *today*", with pin 2 owning the future.

### 4.4 R10-2 / R10-6 / R10-7 / R10-8 / R10-9 / R10-10 — freeze slots

No `src/**` in any of them (commit stats: own test file(s) + report only; the round's only non-R10-1 src lines are R10-5's comments). Details in the scoreboard; the two the brief specifically flagged:

- **R10-8 is not vacuously green and not over-fitted.** The new assertion *requires* a `/discard|reopenAfterUnblock/i` identifier or string literal in **both** flowchart sources before reapplying the whole-file scheduler-absence check — at base it fails (no discard marker in `flowchart-run.ts`), so its green at HEAD proves it ran against R10-1's landed code; the marker regex is deliberately loose and the absence check is the unchanged R8-3 one, so nothing is fitted to R10-1's exact identifiers. The existing mutation test (scheduler import + reopen-helper-calls-`applyRetry` mutants) was correctly left as-is rather than duplicated.
- **R10-9 is not vacuous either:** `makeEvent("RUN_UNBLOCKED_WITH_DISCARD", …)` fails to even typecheck at base (both R10-2 and R10-5 observed exactly that transient mid-round); at HEAD it drives the real gate apply and pins both identity cases, asserting only on the next transition's `from` — no ledger field gains a reader.

R10-2's meta-pin freezes the four protected R8-4 titles (pin 2, already through its R9-2 replacement, is correctly not protected); its one "deletion" is an import-line rewrite. R10-6's four pins each close a real gap its census named (ordinary-input identity only, absent-but-not-empty evidence, conflicting-but-not-identical duplicate, behavioural-but-not-structural surfacing) — and the AST pin's opt-in mutant is applied to the real source in memory. R10-7's census asserts it found its own known readers before asserting the absence, which is what keeps a 215-module sweep honest.

### 4.5 R10-3 — docs

Docs-only (four files, no ADR line, no `PROGRESS.md`). Fixed both Round 9 nits: the probe line now says **ten** cases, and the three "not HEAD commits" notes are superseded by a 23:31 UTC census naming `aeb14dc`/`dff71f1`/`25a57d9` as landed. The discard is described accurately against the *shipped* payload (distinct exact-keyed event, computed set, charged estimates validated against cited rows, no refund, evidence survives) — a real risk for a docs slot writing against an in-flight sibling, avoided here. All three surfaces name the `persist/` pin path; the report also sharpened the Round 8 brief-error record (the `m2/` *directory* exists; the proposed *file* did not). The one claim that went stale at HEAD — "the `RUN_UNBLOCKED_WITH_DISCARD` implementation … not yet a HEAD commit at this timestamp" — was true at 23:31:00 and superseded by the parent's 23:32:18-24 commit batch; that is the standing docs-race cost (R9-4(b) precedent), honestly timestamped, and unlike R9-4(a) there is no committed-before-census fact the sweep missed.

## 5. Per-slot notes

- **R10-1** (opus): The strongest implementation slot of the loop so far. The audit-record discipline is coherent end to end: canonical ordering settles uniqueness inside the validator, READY/SKIPPED zeroes are by-construction rather than by-lookup (with the reason — a pre-run route row must not be claimed by a state that never held it), and the telemetry-honesty rule from R9-3 survives into both docstrings. Turning the ordinary refusal into routing *at the CLI* while leaving the state machine's message untouched is the right seam. The `unblock-flow` scenario construction (scout completes; analysis and an any-join summary lease together; summary passes, analysis fails) earns the block instead of mutating a snapshot into shape.
- **R10-2 / R10-8 / R10-9 / R10-10** (gpt-sol): Right-sized freezes again; R10-8's marker-gated re-check and R10-10's discovered-not-enumerated event family are both stronger than the literal instruction asked for. R10-9's census timestamps (23:04 initial, 23:12 final) with the R10-1 sequencing transient attributed is the shared-tree discipline working.
- **R10-3** (gpt-sol): Clean truth-up; the correction *of the prior correction* (m2/ directory vs file) is the right level of care.
- **R10-4** (gpt-sol): The initializer-resolving census is a solid piece of AST work, and the STOP is the honest outcome — the probe JSON showing `pauseError: "error: cannot pause a COMPLETED run"` is exactly the artifact a stop-and-report should carry.
- **R10-5** (opus): The three-site posture (declaration first, where a future consumer actually looks) and the dereference-vs-write distinction in the sweep are careful; disclosure 3's refusal to let the inertness pin imply more than it proves is the right epistemics.
- **R10-6 / R10-7** (gpt-sol): Both censuses name their own positive findings before asserting absences, which keeps them falsifiable.

**Nits:** none rising to a verdict qualifier this round. Two observations recorded as findings instead: the producer tripwire's unreachable refusal arms (§6.4) and the restore-side charge-sum gap (§6.4) — both are Round 11 candidates, not slot failures, because the sign-off and the R9-3 design were executed as written.

## 6. New findings for Round 11

1. **Option (a) is now uncontended.** The discard shipped, so nothing competes for its files; every precondition has been closed since Round 9 (producer R9-2, checkpoint seam R9-1, unblock R8-1), and the R10-2 freeze pins are *designed* to be replaced in the same diff that implements it. It is the natural Round 11 spine — **still requires parent sign-off**, which has now been deferred twice on scheduling, not on merit.
2. **The discard crash window has an in-process test but no standing probe case.** `unblock-flow` proves append-durable/checkpoint-lost recovery in-process; the external-SIGKILL, real-process version (R9-5's exact technique — widened post-append read, parent-observed complete row, byte-identical checkpoint) is the missing eleventh case. The producer is landed, so the sequencing constraint that deferred R10-6's probe is gone. Note the probe child needs a gate-blocked run with an executed descendant — a more elaborate setup than the tenth case's stall block.
3. **The tracked-run pause controller is the last leg of contract retention** (R10-4's STOP, §4.2): small src surface (`track/loop.ts`, `cli/main.ts`), no contention with candidates 1–2, needs sign-off plus a deterministic-pause design decision.
4. **Discard audit-integrity residual, two halves:** (a) `assertDiscardAuditMatchesLog`'s three refusal arms are unreachable through any public path and untested — module-private, self-consistent by construction; (b) restore recomputes and compares only `nodeId:taskId:previousState`, so a schema-valid hand-edited row citing real routes with **inflated charge sums** replays cleanly — the state transition stays correct, but the durable audit record can lie about money. The design scoped restore's recompute to the consequence set, so this is a hardening decision, not a bug: `restoreCheckpointedSupervisor` already holds `input.events`, so validating the cited sums at restore (or exporting the tripwire for direct test) is a small, signed-off src change in `flowchart-run.ts`.
5. **Docs staleness at HEAD** is limited to the superseded 23:31 "not yet a HEAD commit" note — the Round 11 docs slot records R10-1/8/9 as landed commits, plus this round's decisions (the discard semantics as shipped, the writer-carriage property pin, the tracked-pause blocker is already recorded).
6. **Brief hygiene held:** every path the Round 10 briefs handed out existed; R10-4/R10-7 verified theirs before writing, per the standing rule.

## 7. Handoff

- Gate GREEN at `6c60ba6`: 1915/1914/0/1 (`PI_SMOKE` only), crash-probe 10×3 `ok: true` — both re-verified independently on this VM and matching the parent's record exactly. The +35 closes per-slot (§2).
- The Round 11 injection brief is at `.agent_workspace/ROUND10-BRIEF.md`: five real ranked candidates (fewer than ten — stated explicitly; the surviving surface is freeze-saturated and the brief does not manufacture busywork), the grown frozen-contract list (the discard event's exact-key schema and uniform clearing semantics, the writer-carriage property, the verdict-producer standing pins, the episode boundary census, the `independentEvidence` posture), and the standing prohibitions.
- Nothing is left red, nothing needs a parent fix-joint. The two audit-integrity observations (§6.4) are candidates with sign-off requirements, not defects at HEAD.
