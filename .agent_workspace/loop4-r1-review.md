[Model: claude-fable-5-thinking-xhigh]

# Loop 4 · Round 1 — SOTA review (fable slot)

- **Reviewer:** claude-fable-5-thinking-xhigh
- **Scope:** T1–T10 as committed on `agent/opt-continuous` (`0302d3e`…`197f486`), reviewed against `origin/main` @ `2a921ee`. Every claim below was checked against the actual diff or re-measured on this VM (Node v22.14.0, pnpm 10.17.1); nothing is taken from the slot reports on trust alone.
- **Verdict summary: 7 ACCEPT, 3 ACCEPT-WITH-NITS, 0 ROLLBACK.** The round is real: 5,068 insertions, +73 tests (1435 → 1508), two P1 privacy/correctness holes closed, one measured perf win independently re-verified.

## 0. Integrated verification (what I ran)

| Check | Result |
|---|---|
| `pnpm gate` (typecheck + lint + test + build) | **GREEN, exit 0** — but only after two working-tree fixes, see §1. 1508 tests / 1507 pass / 0 fail / 1 skip. The 2 doctor host-Node baseline failures are **gone** (T9's acceptance met on this 22.14.0 VM). |
| `node scripts/bench-runtime.mjs` (current tree) | `jsonlAppendMs 45.58`, `jsonlAppendFsyncMs 198.806` |
| Same bench in a worktree at pre-T7 commit `9b07f07` | `jsonlAppendMs 66.512`; with T7's new bench file run against the **old** jsonl impl: `jsonlAppendFsyncMs 278.287` |
| `node scripts/crash-probe.mjs` | `{"ok":true}` — all 3 cases × 3 iterations |
| Shared-API freeze | `git diff origin/main...HEAD -- src/persist/file-lock.ts docs/ package.json` is **empty**. `appendJsonlLine(filePath, line, fsync)` and `readJsonlObjects(filePath, corrupt)` signatures byte-identical (T7's diff touches only the body). |
| Forbidden list | No live R1/bandit/topology change reaches the execution path (T9's new transitive closure now *proves* this, not just first-hop). Zero Outcome-supported claims, zero ADR-006 status changes, zero auto-promote, zero `package.json` edits. Every "Outcome-supported" string in the diff is a compliance re-affirmation inside `.agent_workspace/`. |

## 1. Two integration defects the slot reports could not see (both now remediated)

Slots were instructed not to run the full gate, so these only surfaced when I ran it integrated:

1. **T8's commit was typecheck-red.** `test/unit/protocol/v1.test.ts:302` read `.evidenceIds` off the un-narrowed `AgentMessage` union (the exact error T3/T4/T5 reported seeing in flight). The parent committed the narrowing fix as `197f486` mid-review; HEAD is now typecheck-green. No action left.
2. **T9's commit was lint-red and broke `pnpm gate`.** `test/unit/cli/doctor.test.ts:66` used two literal leading spaces in the `^  FAIL` regex → eslint `no-regex-spaces` error. I applied the one-character fix (`^ {2}FAIL`) in the working tree — a test file, not `src/` — and re-ran the full gate to green. **The parent must commit this uncommitted one-line change** (`git status` shows it as the only modification).

Neither defect is behavioral; both are "acceptance said `pnpm gate` green, slot verified only its own files" process gaps. Round 2 tasks should require at minimum a scoped `eslint` + a whole-tree `tsc --noEmit` from every slot (both are cheap and tree-shared-safe, unlike the test suite).

## 2. Per-slot verdicts

### T1 — feedback lock + fail-closed cascade: **ACCEPT**
Verified in `src/feedback/store.ts`: `feedbackLogLockPath` (`records.jsonl.lock`), `withFeedbackLogLock` over the untouched `withExclusiveFileLock`, in-process `appendQueues`, `appendFeedback` appending under the lock with its signature unchanged. Verified in `src/privacy/deletion.ts`: `readFeedbackRecordsRaw(...).catch(() => [])` is **gone** — the fail-open privacy defect (corrupt log → "successful" delete with text left on disk) is closed; read→filter→rewrite→tombstone all inside the lock; ENOENT stays a lock-free no-op via `statExists`; strip-before-tombstone ordering kept. The one scope extension — cascade now runs **before** episode-file unlink in `deleteEpisodeRecords` — is disclosed, matches the run-delete doctrine already in the module, and is pinned by `"a corrupt feedback line fails the episode delete closed, before anything is unlinked"`. All 5 required destructive tests present in the two owned test files (byte-identical-on-timeout, corrupt-middle-line, interleaved append survival, late-append non-resurrection, ENOENT no-op).
Honest residual (T1 disclosed it): `appendFeedback` rejects after the 5 s lock default and its caller `src/learning/auto-loop.ts:251` awaits it — ranked for Round 2 (§4 R2-4).

### T2 — invocation sink + flowchart wiring: **ACCEPT-WITH-NITS**
Verified: `createInvocationSink` in `src/telemetry/invocation-log.ts` retries **only** the exact lock-timeout message for this log's lock path, never rejects, reports terminal drops via `onDrop` (guarded against throwing reporters), and adds an outer `sinkQueues` chain so a retrying row cannot land out of order. Both `createExecutor` call sites inside `runCommand` (`src/cli/main.ts:645-651` flowchart, `:693-695` children/track) now pass the shared sink — the flowchart branch's `hooks = undefined` hole is closed and pinned by a comment-blanking source test (`test/unit/telemetry/invocation-log.test.ts:470`) whose non-vacuousness T2 demonstrated by reverting in a scratch copy. `appendInvocationRecord` signature unchanged; readers lock-free.
Nits: (1) `isLockTimeout` string-matches `withExclusiveFileLock`'s message — pinned by tests so a wording change goes red rather than silent, but the durable fix is a typed error on the lock itself (Round 2, §4 R2-2). (2) Ownership was "only the two call sites and their hooks argument"; the import line and the single sink construction were also touched — necessary and disclosed, noted for the record.

### T3 — shared atomic writer: **ACCEPT**
Verified `src/persist/atomic-file.ts`: `<file>.<pid>.<random>.tmp` opened `"wx"` (a stale temp is refused, never truncated or adopted; EEXIST retries a fresh name, bounded at 3), write+fsync+close, rename with the EPERM/EEXIST/EACCES unlink-retry fallback promoted from pause-controller, self-temp-only cleanup on failure. `CheckpointStore.write` and `requestPause` are one-line delegations; both private tmp+rename copies are deleted; published bytes byte-identical (exact-bytes assertions in both test files). The fixed-tmp concurrent-torn-write hole (fable §1c) is closed and the negative control (9/21 tests fail with the old naming restored) is credible.
Confirmed residual: `docs/data-dictionary.md:181` still names `pause.json.tmp` literally — the classification statement holds but the literal name is stale, and `checkpoint.json` temps are likewise unique-named now. Docs were outside every slot's ownership; ranked for Round 2 (§4 R2-10). Minor note, no action: during the (Windows-only in practice) rename fallback there is an instant where the destination is unlinked before the retry rename — pre-existing pause behavior, now shared by checkpoint; strictly better than checkpoint's old no-fallback throw.

### T4 — episode validation + settle lock: **ACCEPT**
Verified: `validateEpisodeEvent` (`src/episode/events.ts`) is a fail-closed decoder over exactly the four shapes; unknown `type` throws with a 40-char-truncated label; only known keys are copied (JSON-parsed own `__proto__` dropped); `EPISODE_CLOSED.outcomeId` omitted-not-undefined so rows round-trip byte-identically. `EpisodeEventStore.readAll` validates every row and its corrupt-JSON callback is upgraded from bare `Error` to `DomainValidationError` — `episode events --json` can no longer emit a row that fails validation. `settleBoundEpisode` runs read-decide-append inside `withExclusiveFileLock` on `episodeLockPath`, which I confirmed is byte-identical to the CLI's template (`src/cli/episode.ts:91`) — and the two-way source pin means either side moving its lock file goes red. Re-read-inside-the-lock is structural (`settleLockedEpisode` only runs with the lock held). `bindEpisodeToRun` unlocked with the required rationale comment. All 6 required behaviors pinned.
Disclosed caveats accepted as-is: validation line number is record-index (drifts only if a log ever contains blank lines, which `appendJsonlLine` never writes); unknown forward keys are dropped on read (strict posture, internal-only writers).

### T5 — pre-aborted executor short-circuit: **ACCEPT**
Verified all three windows in `src/pi-adapter/pi-executor.ts`: (a) `execute` checks `signal.aborted` first and yields `EXECUTION_FINISHED/CANCELLED` plus a `callOutcome:"cancelled"` invocation via `resolveIdentity` — no `resolveModel`, no `new Agent`, no stream call; (b) `runWithRetry` checks before **every** attempt with a `latest` accumulator so an early return reports the attempt that actually ran; (c) post-registration re-check calls `agent.abort()` exactly once. Retry classification, usage honesty, and event translation untouched. The `finish()`/`reportInvocation()` extraction is **not** cosmetic — both the normal and short-circuit paths flow through it, which is what keeps the cancelled event shape identical for the coordinator. The negative controls (guards disabled → provider actually called) and the honest disclosure that window (c) is closed by construction rather than test-driven are exactly the right epistemics. One added assertion in the retry suite pins zero spend after cancellation.

### T6 — durable cancel + `maxWallTimeMs`: **ACCEPT**
Verified in `src/run/child-coordinator.ts`: `cancelledChildren: Set<RunId>` consulted at the top of every attempt iteration (first iteration = right after gate admission, covering the queued window), entries dropped on settle, `cancel()` after settle a no-op. Wall deadline: one timer per child run through the injectable `schedule`, racing the per-attempt timer (`min(timeoutMs, remaining)` by construction); non-finite/non-positive budgets fail closed as already-exhausted; values above the `setTimeout` range clamped; timer cancelled in `finally`. Wall expiry mid-attempt is reported as the wall limit only when the attempt produced neither a terminal result nor a protocol violation — a terminal that actually arrived keeps its honest outcome, which is the right reading and interacts correctly with T5's CANCELLED executor shape (the wall check precedes the `executorOutcome === "CANCELLED"` branch). No protocol shape change, no new event types. The negative control (pre-change coordinator in an isolated /tmp copy: cancel tests fail with the documented wrong outcomes, wall tests hang) is the strongest evidence in the round, and the generous-wall regression pin passing identically on pre-change code satisfies the byte-identical requirement.
Disclosed and accepted: `maxCostUsd` still unread at the child level (ranked §4 R2-5); an executor that ignores its abort and returns a terminal result settles with that result.

### T7 — jsonl append fast path: **ACCEPT** (metric bar independently confirmed)
The ≥5%-or-ROLLBACK gate is **met by my own measurement**, not just the slot's: non-fsync 66.512 → 45.58 ms/1000 (**−31.5%**), fsync (new bench against old impl in the pre-T7 worktree) 278.287 → 198.806 ms/1000 (**−28.6%**). The slot correctly added the `jsonlAppendFsyncMs` bench *first* and recorded its own same-VM before/after (70.355/308.655 → 45.003/204.972 — consistent with mine). Implementation verified: fsync path writes and syncs through **one** append-mode handle; the unconditional `mkdir` is replaced by append-first with a single ENOENT→`mkdir`→retry (non-ENOENT errors propagate untouched); exported signatures byte-identical; truncated-tail recovery untouched. The 5 required tests are present in `test/unit/persist/jsonl.test.ts`.

### T8 — protocol fuzz: **ACCEPT-WITH-NITS**
The fuzzer is deterministic (fixed `DEFAULT_SEED = 0x4f31_0008`, in-test xorshift, no new dependencies, 5 s timeout caps), covers the three surfaces with the required explicit cases (proto-pollution inert, duplicate terminal first/last, 10,000-entry arrays), and asserts `error.constructor === DomainValidationError` — subclass-proof. It found a real pre-existing defect: `assertAtMostOneTerminal([null])` escaped a `TypeError`, which would have been a coordinator **crash** (not a rejection) at the `child-coordinator.ts` catch that expects `DomainValidationError`. The fix (validate each entry before the terminal check) is minimal and fail-closed, with a named regression.
Nits: (1) the committed test file was typecheck-red (§1 item 1 — now remediated by the parent's `197f486`, but the slot's own acceptance said gate-green); (2) the fix makes the coordinator's per-message call `assertAtMostOneTerminal([...seen, message])` (`child-coordinator.ts:721`) re-validate the whole prefix — O(n²) validation per transcript. Bounded and harmless at real transcript sizes; fold the cheap fix (validate only the incoming message, track a terminal flag) into the Round 2 coordinator slot (§4 R2-5).

### T9 — doctor hermeticity + transitive closure: **ACCEPT-WITH-NITS**
Verified: `DoctorOptions.nodeVersion` seam with `process.versions.node` default (real CLI behavior unchanged; `--json` contract untouched); the two host-dependent tests inject 22.19.0; a 22.18.9 test pins the exact `FAIL node` line and exit 1. The full suite is now green on this 22.14.0 VM — the round's masking hazard is gone. The closure walker follows relative static/dynamic/re-export **value** edges transitively, excludes `import type`, fails closed on import-shaped comments, and pins exactly three justified runtime ingresses; the negative-case test proves a synthetic `adaptation → routing → run` chain and a commented import both go red without touching `src/`. This closes the Loop-3 "adaptation-plane transitive import closure" residual for real.
Nits: (1) the committed file carried the gate-breaking lint error (§1 item 2 — reviewer-fixed, parent must commit); (2) the two rewritten tests now call `doctorCommand` directly instead of `main(["doctor", …])`, dropping CLI-dispatch coverage from those two paths — acceptable because other doctor tests still go through `main`, but worth knowing.

### T10 — crash probe: **ACCEPT**
Re-ran on this VM: `ok:true`, 3/3 cases × 3 iterations, rerunnable, `/tmp` state removed (cleanup failure would itself fail the verdict via the `temporary-state-cleanup` case). Design constraints honored: sentinel-file synchronization (no timing races), the checkpoint case plants its **own** `interrupted-<uuid>.tmp` and never assumes the store's naming (so it held both before and after T3 landed — exactly the required invariance), the lock case asserts the documented no-steal posture (`DomainValidationError` naming the lock path, manual removal recovers). Integration test bounded (one-iteration form, 10 s ceiling). One honest observation, no action needed: in the jsonl case the partial line is durable before the SIGKILL, so the kill proves post-mortem recovery rather than interrupting the write itself — that is the strongest deterministic claim available without fault injection, and the checkpoint/lock cases do interrupt real multi-step protocols.

## 3. Cross-slot conflict check: clean

- T2's sink wraps T1's-pattern lock via the unchanged `appendInvocationRecord`; the two logs' locks are independent files.
- T5's cancelled-executor event shape (`MESSAGE` + `EXECUTION_FINISHED/CANCELLED`) is exactly what T6's coordinator branches on, and T6's wall check correctly outranks the executor-cancelled reading when the deadline timer caused the abort.
- T3's unique temps satisfy T10's naming-agnostic probe constraint by construction; the probe passed on the integrated tree.
- T7's jsonl body changes are invisible to T1/T4's locked appends (signatures and semantics frozen; ENOENT-retry preserves directory creation).
- T4's `episodeLockPath` equals the CLI's path byte-for-byte, pinned from both sides.
- Ownership disjointness held: no slot's diff touches another's files (T2's import/sink lines in `main.ts` are the one disclosed, necessary stretch).

## 4. Round 2 ranking (see `ROUND1-BRIEF.md` for the injection form)

P1: flowchart run-level abort is **created three times and aborted zero times** (`flowchart-run.ts:790,891,1044`; `rg '\.abort\(' src/run/flowchart-run.ts` → empty) — now that T5/T6 made cancel effective, nothing on the flowchart path ever calls it. P1/P2: typed lock-timeout error + lock perf (sanctioned `file-lock.ts` unfreeze). P2: delete-vs-episode-lock, feedback-append drop parity, `maxCostUsd` honesty + O(n²) fix, crash-probe extension over the round's new writers, persistence-row fuzz. P3: mailbox TTL/dead-letter, scheduler dead code, docs accuracy. Saturated: `persist/jsonl` (post-T7 wins would be <2% absent retention-growth evidence) and `protocol/v1` (fuzz-clean after the T8 fix) — per the standing rule, Round 2 tilts to I/O, races, and disaster recovery.

## 5. Actions for the parent

1. **Commit the one-line lint fix** in `test/unit/cli/doctor.test.ts` (currently the only working-tree modification). Without it, `pnpm gate` is red at lint.
2. `197f486` (v1.test.ts narrowing) is already committed — nothing further there.
3. Round 2 slot instructions should add: scoped `eslint` on owned files **plus a whole-tree `tsc --noEmit`** as mandatory per-slot checks (both are safe in a shared tree; this round's two integration breaks were exactly in that gap), and note that the "2 known doctor failures" baseline allowance is **retired** — the suite is fully green on this VM now.
