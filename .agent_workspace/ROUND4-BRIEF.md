# ROUND 4 BRIEF — injection context for Loop 4 · Round 5

Provenance: written by the Round 4 SOTA reviewer (claude-fable-5-thinking) after evidence-based review of R4-1…R4-10 at `74daff3`; verdicts and measurements in `.agent_workspace/loop4-r4-review.md` (8 ACCEPT, 2 ACCEPT-WITH-NITS, 0 ROLLBACK). Every candidate below cites evidence the reviewer reproduced at HEAD, not report hearsay. **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 4 (all committed, `cf554b3`…`74daff3`)

| Slot | Landed | Key symbols (do not re-implement) |
|---|---|---|
| R4-1 | Run-scoped cooperative lock: `runtime/runs/<id>.lock`, held by `deleteRunRecords` across rm+verify **and re-verified after release** (the second verify, not the lock, closes the leak — 0/120 adversarial leaks vs 12/120 at baseline); `requestPause` + track-questions take it; the two per-step writers do **not** (measured +22.5%/+17.5% e2e, rolled back per the 5% bar, decision-pinned); track-questions now atomic | `runLockPath(stateRoot, runId)` in `run/event-store.ts`; `deleteRunRecords(…, options?)`; decision pins "append/write does not block on the run lock" |
| R4-2 | Undelivered cluster mail reaches the operator: `summarizeClusterMail` (pull, at run end) on both embedders, `clusterMail?` on both outcomes, one stderr line via `formatUndeliveredClusterMail`/`warnUndeliveredClusterMail` (two call sites, source-pinned). **Census: production cannot produce a dead letter today** — `claimRole` requeue-counts only `mail.from === agentId` and every attempt registers a fresh agent id, so `dead-lettered=` reads 0 in real runs; `pending=` carries real starvation | `ClusterMailReport`, `summarizeClusterMail` in `run/coordinator.ts`; `formatUndeliveredClusterMail` in `cli/main.ts`; host/mailbox untouched |
| R4-3 | Supervised-plane crash honesty: `runSupervisorRounds` is a wrapper that records an in-flight-only `RUN_FAILED` and rethrows; round settlement via `Promise.allSettled` (no post-terminal appends, no unawaited children); both BLOCKED→READY sites wired through `applyRetry` with the *recorded* status (behavior-neutral, source-pinned) | supervised `recordCrashTerminal`/`crashReason` (module-private) in `run/supervisor.ts`; `test/integration/m2/supervisor-crash.test.ts` |
| R4-4 | Paused/waiting/blocked crash contract decided: no terminal (unchanged) **plus** a best-effort checkpoint flush in teardown and around `injectFlowchartRun`'s tail, so the resume point is never behind the log; investigation overturned R3-5's lossless claim (measured budget sweep); in-flight nodes stay at-least-once with the retry visible and pinned; child duplicate-terminal guard kept (reachable via published `childRunId`) and pinned; **no schema addition** | `RESUMABLE_CRASH_STATUSES`, `preserveResumableState` in `run/flowchart-run.ts`; `recordCrashTerminal` byte-unchanged |
| R4-5 | Doctor: per-lock additive `remediation` (dead-recorded-PID ⇒ "inspect and remove manually; never automatic"; everything else conservative) and additive `runStates` inventory (PLANNING/RUNNING logs via real `EventStore`+`replayRun`, age, inspect/resume/delete guidance, advisory `run-state-inventory` check failing only on scan errors); read-only, `file-lock.ts`/`main.ts` untouched | `DoctorRunStateInventory`, `DoctorLockEntry.remediation` in `cli/doctor.ts` |
| R4-6 | Resume accepts `--primary-model`/`--thinking`, both `createExecutor` sites forward them, and `describeResumeExecutorConfig` prints one stderr disclosure **before** the build (four cases; flag-free pi resume always warns; wording never implies restore). Persistence refused with evidence: the checkpoint is derived, so honest persistence is an event-schema change — not taken. Resume now refuses an unusable ambient `PI_THINKING_LEVEL` (parity with `run`, pinned) | `describeResumeExecutorConfig` in `cli/main.ts`; extended sink-wiring pin + 6 mutants |
| R4-7 | Feedback tombstones crash-atomic: `writeFeedbackTombstones(stateRoot, tombstones, options?)` via `writeFileAtomic`; reader throws exactly `DomainValidationError` on unparseable JSON; probe case reworked onto the kill-before-rename seam (FIFO hack deleted); feedback fuzz `skipUnowned` → `failFuzz` (helper deleted — no skip mechanism remains) | `writeFeedbackTombstones` in `feedback/store.ts`; R4-1 swapped the `deletion.ts` call site |
| R4-8 | Docs truth-up for R3 (delete verification, episode-lock sidecar, DAG-skip and lease-expiry corrections, doctor lock inventory, host dead-letter surfaces) with timestamped coordination snapshots for then-in-flight R4-1/2/6 — **now stale at HEAD, see R5-7** | `docs/data-dictionary.md`, `docs/status-matrix.md`, `docs/specs/m0-m2-architecture.md` |
| R4-9 | `catalog-observed.json` atomic + torn read → `CatalogObservedCorruptError`/`CATALOG_OBSERVED_CORRUPT` (derived: message names the rebuild; ENOENT stays the one silent path; shape-skew still degrades). `preferences.json` atomic via new **additive** `writeFileAtomicSync` + torn read → `PreferenceSnapshotUnreadableError`/`PREFERENCE_SNAPSHOT_UNREADABLE` with **bind-after-successful-load** (silent reset of learned state closed, reproduced). Frozen async `writeFileAtomic` byte-identical | `writeFileAtomicSync`/`AtomicWriteSyncOptions` in `persist/atomic-file.ts` (the one disclosed freeze exception, reviewer-verified additive) |
| R4-10 | Test-only loopback OpenAI SSE provider (`customProviders[].baseUrl`) driving the exported CLI: `run --executor pi` → approval gate → `resume --executor pi` → two invocation rows → production calibration reader decodes both. Executes R3-9's pinned joint offline. Parent's `74daff3` pins R4-6's resume warning in its stderr assert (cross-slot joint, not a defect) | `test/helpers/loopback-openai-provider.ts` (records requests, validates model); `test/integration/pi-adapter/loopback-cli-resume.test.ts` |

## 2. Current baseline (this VM, Node v22.14.0, pnpm 10.17.1)

- **`pnpm gate` GREEN, exit 0**: **1680 tests / 1679 pass / 0 fail / 1 skipped**. The only skip is the `PI_SMOKE=1` real-provider gate (`test/integration/pi-adapter/provider-smoke.test.ts`). There are no named defect skips and no remaining `skipUnowned` mechanism anywhere. Any slot whose report shows a second skip introduced one and must say so.
- `node scripts/crash-probe.mjs` → `ok: true`, **8 cases × 3 iterations** (case count unchanged; the tombstone case now proves the rename-seam property).
- Perf: no fresh whole-tree bench needed. R4-1's decision pins carry its measurements (locked append +372%/op, +22.5% e2e; locked checkpoint +62%/op, +17.5% e2e; both rolled back). Any Round 5 perf claim records its **own** same-VM before/after with an unchanged-arm control; ≥5% end-to-end regression or rollback. **R5-1 in particular must bench the once-per-run acquisition.**

## 3. Forbidden / frozen for Round 5

Global forbidden list, unchanged: live R1/bandit/topology on the execution path; Outcome-supported claims; ADR-006 stays Proposed; auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit.

Frozen contracts (Rounds 1–3 set, all held through Round 4, plus new):
- Rounds 1–3 carry-overs: `appendJsonlLine`/`readJsonlObjects` signatures; `writeFileAtomic(path, contents, options?)`; `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE` (code-discrimination only); `episodeLockPath` ⇔ `cli/episode.ts` symmetry; `runCommand` sink wiring; `deleteEpisodeRecords` semantics; `appendFeedbackWithRetry`/`FeedbackAppendOutcome`; `invocationError(unknown)` totality + `isInvocation` never-throws; `writeFeedbackRecords`/`writeInvocationRecords` atomic posture; `EventStore.readAll`'s `DomainValidationError` posture; `deadLetterReport()`/`onDeadLetter` shapes; `planRound` 4-arg arity pin + no-skip-transition pin; R2-5 `maxCostUsd` source pin; R2-9 `restore()` pins; doctor's frozen-additive JSON.
- **New from Round 4:**
  - `writeFileAtomicSync(path, contents, options?)` joins its async sibling in the freeze; no further `persist/` edits without a disclosed justification of this round's caliber. **No private temp+rename copies may be *added*; R5-4 deletes the surviving pre-freeze ones.**
  - `runLockPath(stateRoot, runId)` template and no-steal posture; `deleteRunRecords`' lock-plus-double-verify with `RunRecordsSurvivedError`/`RUN_RECORDS_SURVIVED` (R5-1 may add lifecycle acquisition for clean deletes but must keep the verify, the code, and the fail-closed direction); the two decision pins that `EventStore.append` and `CheckpointStore.write` do **not** take the run lock — falsifiable via a new same-VM bench only, never silently.
  - `recordCrashTerminal`'s in-flight-only + best-effort contract, now on **three** planes (R5-2 owns the extraction into one module; the contract itself does not change); `preserveResumableState`'s flush-only-for-`RESUMABLE_CRASH_STATUSES` and the no-terminal rule for paused/waiting/blocked logs; exactly-one-terminal pins including the shared-child-log case.
  - `applyRetry` as the sole BLOCKED→READY producer (source-pinned; no status literals).
  - `ClusterMailReport` shape and the `warning: cluster role-cast mail undelivered: …` line shape (pinned exactly); `summarizeClusterMail` consumed pull-at-run-end.
  - Resume flags + `describeResumeExecutorConfig`'s four disclosure cases (wording pinned); the extended sink-wiring pin (no unhooked `createExecutor` in `main.ts` **and** both resume sites carry the config) must stay green through any `main.ts` edit.
  - `writeFeedbackTombstones` signature + the tombstone reader's `DomainValidationError`; `CATALOG_OBSERVED_CORRUPT` and `PREFERENCE_SNAPSHOT_UNREADABLE` codes + bind-after-load; the feedback fuzz hard-fails (do not reintroduce a skip path).
  - The loopback fixture's pinned resume-warning stderr assert (`74daff3`): a slot that changes resume's stderr owns updating that pin **with disclosure**, the R4-6×R4-10 lesson.
- Saturated — do not spend a slot: jsonl internals/perf; protocol/v1 parse; event-log corrupt-line discipline and event fuzz; lock acquisition perf; invocation-row decoding; run-delete verification semantics (R4-1 emptied it — only the lifecycle-acquisition convenience remains, and that is R5-1); feedback-plane decode/atomicity (R4-7 emptied it); mailbox TTL/durability (accepted non-goals — R5-6 is a *reachability decision*, not a durability slot); doctor read-only inventories (R4-5 emptied them — only error-path routing remains, R5-9).

Process requirements per slot (carried forward — they worked): scoped `eslint` on owned files + whole-tree `tsc --noEmit` before reporting; attribute shared-tree transients to files, never edit unowned files to "fix" them; timing-sensitive owned tests run 3×; census before trusting this brief; full gate is the parent's job; no scratch files left at report time. **New this round:** before pinning "no stderr/stdout beyond X" on a shared CLI path, check the round's ownership table for slots adding output to that path and disclose the joint (R4-10's miss).

## 4. Ranked Round 5 candidates (mutually exclusive ownership)

The round's tilt: decide the two postures Round 4 priced but did not buy (a delete that *waits* for a live run; dead letters that can actually happen), finish the crash-teardown story (settle tails, one shared helper), and close the last silent-reset/private-copy stragglers in the atomic-write doctrine. No P0 exists: nothing in the tree reports success it cannot prove, and every closed window failed closed first.

File-contention note: `src/run/flowchart-run.ts` belongs to **R5-1 alone** this round (R5-2 and R5-5 coordinate through exports and tests, R4-1×R4-7 pattern); `src/cli/main.ts` belongs to **R5-9 alone**; `src/run/supervisor.ts` to **R5-2 alone**.

### R5-1 (P1, privacy/operability posture) — The run lifecycle takes the run lock
**Evidence (reviewer-verified):** R4-1's shipped posture is 0-clean/120-refused: a `delete --run` racing a live run now always throws `RunRecordsSurvivedError` (correct, fail-closed) where locking the per-step writers would make all 120 clean — at +27.1% e2e, rejected. R4-1's own disclosure names the cheap alternative: **one acquisition per run**, taken at run start and released at teardown, so a delete *waits* for a live run (bounded) instead of refusing, at ~one lock cost per run instead of per write.
**Change:** acquire `runLockPath` in the run lifecycle of both CLI-reachable embedders (`startFlowchartRun`/`resumeFlowchartRun` in `flowchart-run.ts`, `startParentRun` in `coordinator.ts`), released in the same teardown that already runs `cancelAndSettle`/finish; keep `EventStore.append`/`CheckpointStore.write` lock-free (their decision pins must stay green). Bench the once-per-run acquisition same-VM with an unchanged-arm control, ≥5% e2e bar. **Posture decision to state and pin, with parent sign-off:** a SIGKILLed run now leaves a lock that blocks pause/track-writes/delete until an operator clears it (doctor already inventories it with age+PID+remediation) — that trade must be explicit, not incidental. Keep the delete's double verify and error code (frozen). Update the `RunRecordsSurvivedError` remedy wording if the lock changes what survivors mean. While owning `flowchart-run.ts`: swap its `recordCrashTerminal` copy to R5-2's shared helper when that export lands (one-line, disclosed coupling), and leave the teardown/flush contract byte-compatible.
**Ownership:** `src/run/flowchart-run.ts` (sole owner), `src/run/coordinator.ts`, `test/unit/privacy/deletion.test.ts` (the delete-waits case), `test/integration/cli/delete.test.ts`, `test/unit/run/flowchart-run-abort.test.ts` additions, plus a lifecycle-lock unit test. The supervised embedders (`supervisor.ts`) are R5-2's — publish the acquisition pattern in your report so R5-2 can mirror it.

### R5-2 (P2, crash honesty) — Crashed-run settle tails + one `recordCrashTerminal`
**Evidence (R4-3's verified disclosure):** a crashed supervised run leaves its episode bound and its checkpoint at the last pre-crash status — the settle tail (`settleBoundEpisode`, `settleSupervisedOutcome`, `checkpointStore.write`) lives after the `runSupervisorRounds` call in both embedders, so the rethrow skips it. The flowchart plane shares the shape (its `finish` does the settle; `withRunTeardown` records terminal+flush only). Separately: `crashReason`/`recordCrashTerminal` now exist in **three** files with deliberately identical contracts — the extraction point R4-3 named.
**Change:** (a) extract the helper into a shared module (suggest `src/run/crash-terminal.ts`), migrate `supervisor.ts` and `child-coordinator.ts` (the `child run crashed:` prefix stays a parameter), export for R5-1's one-line flowchart swap; contract byte-compatible, existing pins stay green. (b) Best-effort settle on the crash path of the supervised embedders: episode settled, terminal checkpoint written, both swallowing their own failures, error always rethrown; pin that a crashed supervised run's episode is closed and its checkpoint reads `FAILED`. (c) Mirror R5-1's run-lock acquisition in `startSupervisedRun`/`resumeSupervisedRun` per its published pattern (disclosed coupling; skip if R5-1 slips — census first). Do not widen in-flight-only anywhere.
**Ownership:** `src/run/supervisor.ts`, `src/run/child-coordinator.ts`, new `src/run/crash-terminal.ts`, `test/integration/m2/` (supervisor-crash + resume), `test/integration/m2.5/children-flowchart.test.ts`.

### R5-3 (P2, learned-state atomicity) — The bandit store silently resets on damage
**Evidence (reviewer-verified at HEAD):** `updateProjectBandit` writes pretty multi-line JSON with plain `writeFile` (`learning/bandit-store.ts:126`; the lock serializes writers, it does not make the write atomic), and `readBanditFile` (`:58-70`) converts **any** parse failure or shape mismatch to `undefined` — a torn or damaged file reads as "no bandit", and the next update overwrites the learned pulls/rewards for good. This is byte-for-byte the preference-store defect class R4-9 closed, one plane over.
**Change:** write through `writeFileAtomic`; decide the read posture per R4-9's doctrine — bandit state is learned and not recomputable from any log, so torn should fail closed with a typed, code-discriminated error (suggest `BANDIT_STATE_UNREADABLE`), while `isBanditState`-shaped-but-unknown content may keep degrading if argued as version skew (document the distinction, pin both halves). Negative control: restore the old writer/reader and show the reset. Adaptation-plane only — no live-routing wiring, no reward-semantics change.
**Ownership:** `src/learning/bandit-store.ts`, `test/unit/learning/` (bandit tests).

### R5-4 (P2, atomic-writer doctrine) — Delete the three surviving private temp+rename copies
**Evidence (reviewer-verified at HEAD, predates Round 4):** `writeAtomicJson` (`config/providers-config.ts:189`) and `FileCredentialStore.save` (`pi-adapter/file-credential-store.ts:85`) publish through a **fixed** `${path}.tmp` opened `"w"`: a crashed writer's stale temp is silently adopted/truncated rather than refused, and two concurrent writers collide on one temp name — the exact failures `writeFileAtomic`'s `"wx"` unique-temp protocol and the `atomic-write-stale-unique-temp` probe case exist to prevent. `saveAdaptationRegistry` (`adaptation/promotion.ts:324-344`) uses unique names but leaks its temp when the write itself fails (cleanup wraps only the rename).
**Change:** route all three through `writeFileAtomic` (the credential store keeps its post-publish `chmod 0o600`; preserve on-disk bytes); delete the private copies; add a crash-probe case only if it proves something the unit pins cannot (probe file is this slot's, sole owner). No `persist/` edit — consume the frozen writer as published.
**Ownership:** `src/config/providers-config.ts`, `src/pi-adapter/file-credential-store.ts`, `src/adaptation/promotion.ts`, their unit tests, `scripts/crash-probe.mjs` (sole owner this round).

### R5-5 (P2/P3, disaster-recovery investigation) — Should resume adopt recorded-but-unaccepted child results?
**Evidence (R4-4's verified disclosure):** budgets 0–2 of its crash sweep — a child's `TASK_RESULT` (and even the child's own `RUN_COMPLETED`) can be on the log while the supervisor never accepted it; resume re-executes the node. R4-4 made the retry *visible and pinned* (two `CHILD_RUN_CREATED`, distinct child runs, closed child logs) and argued that teardown-time adoption would skip the three-line gate. The remaining question is whether **resume-time** adoption (replaying the recorded result through the real acceptance path in `resumeRestoredRun`/`executeRemainingRunningNodes`) is sound.
**Change:** investigation-first, **no `src/run/flowchart-run.ts` edits this round** (R5-1 owns the file): measure how often the window matters at realistic cadences, decide whether resume-time adoption through the real gate is behavior R6 should buy, and deliver the decided contract as pins + a written design (files, seams, risks) if yes, or a documented accepted-cost posture if no. Tests that sharpen the current visible-retry pins are in scope; a src prototype belongs in the report, not the tree.
**Ownership:** `test/integration/m2.5/resume.test.ts`, `test/unit/run/flowchart-run-abort.test.ts` (additive pins only), the report.

### R5-6 (P2/P3, cluster semantics decision) — Make dead letters reachable, or say they are API-only
**Evidence (R4-2's census, reviewer-verified):** `claimRole` counts a requeue only when `mail.from === agentId` (`mailbox.ts:122`) and `runAttempt` registers a fresh `createAgentInstanceId` per attempt (`child-coordinator.ts:639-641`), so no production run can cross `DEFAULT_MAX_ROLE_REQUEUES`: the operator line's `dead-lettered=` is structurally 0 and only `pending=` carries information.
**Change:** decision-first, parent sign-off required for any semantic change to the frozen-adjacent mailbox. Options in rough order of invasiveness: (a) document dead letters as API-only and adjust the line/docs accordingly (no src); (b) count a requeue whenever a role claim delivers nothing and the queue's only sender is the claimant-role's own cast — i.e. make the bound mean "this mail survived N claim opportunities" (mailbox change, `deadLetterReport` shape frozen); (c) stable per-task agent identity across attempts (largest, touches R5-2's file — do not take without coordination). Whatever is decided: pin it, and keep `deadLetterReport()`/`onDeadLetter` shapes and the pinned line format intact.
**Ownership:** `src/cluster/mailbox.ts` (only with sign-off), `src/cluster/host.ts` (doc comments), `test/unit/cluster/`, `test/integration/cluster/undelivered-mail.test.ts` (additive).

### R5-7 (P3, docs) — Truth-up for Round 4's landings
**Evidence (reviewer-verified stale at HEAD):** three R4-8 coordination snapshots now understate the tree — `docs/data-dictionary.md` and `docs/status-matrix.md` say no run-plane lock exists (R4-1 landed one, with double verify), the status-matrix dead-letter row says nothing reaches a CLI operator (R4-2's line does), and the resume row says `--primary-model`/`--thinking` don't exist on resume (R4-6 landed them, with disclosure semantics). Undocumented new surfaces: `CATALOG_OBSERVED_CORRUPT` (rebuildable) vs `PREFERENCE_SNAPSHOT_UNREADABLE` (not), doctor's `runStates`+`remediation`, the supervised crash terminal, `preserveResumableState`'s flush contract, the undelivered-mail line (including that `dead-lettered=` is production-unreachable today — R4-2's census belongs in operator docs), and the loopback fixture as the offline verification path.
**Change:** doc-only truth-up; repeat the timestamped end-of-round sync for R5-1/2/6 rather than predicting them.
**Ownership:** `docs/**`.

### R5-8 (P3, verification infrastructure) — Wire-level witness for resume executor config
**Evidence:** R4-6's forwarding is pinned structurally only (its disclosure #4: no offline test could observe `modelOverride`/`thinkingLevel` arriving at the provider). R4-10's loopback fixture already records every incoming request and validates the model id — the witness exists, it just isn't asked.
**Change:** test-only: extend the loopback suite with a resume passing `--primary-model loopback/loopback-1` (and a distinct second configured model to prove selection, if the fixture supports two) and assert the **request the server received** carries the requested model; cover the supervised resume path too if reachable. Assert the R4-6 "note:" disclosure line for the flagged case (you own updating the pinned stderr expectations — disclose, per the `74daff3` lesson). If thinking level is not observable in the wire protocol, say so and pin what is.
**Ownership:** `test/integration/pi-adapter/`, `test/helpers/` (no `src/**`; if the protocol needs adapter changes, stop and report — Round 6 evidence).

### R5-9 (P3, operability routing) — From a refused command to the doctor remedy
**Evidence:** after R4-1, `delete --run` on a live run always refuses (`RUN_RECORDS_SURVIVED`), and no-steal lock timeouts (`LOCK_TIMEOUT_CODE`) block pause/track/delete after a kill — while R4-5's doctor now carries exactly the inventories that answer both (`locks` with per-entry remediation; `runStates` with inspect/resume/delete guidance). Nothing routes an operator from the failing command to that surface; the R3-6→R4-5 line stops one hop short, the same last-hop shape R4-2 just closed for mail.
**Change:** in the CLI's error surface (`cliFail`/error classification region of `main.ts`, and `cli/errors.ts` if needed): when a command fails with `LOCK_TIMEOUT_CODE` or `RUN_RECORDS_SURVIVED`, append a stable `next:` naming `pi-sparkle doctor` (and `--json` fields `locks`/`runStates`). Code-discriminated only — never message-matching (frozen doctrine). Keep every existing pinned error string intact; the sink-wiring and resume-disclosure pins in `main.ts` must stay green.
**Ownership:** `src/cli/main.ts` (sole owner this round), `src/cli/errors.ts`, `test/integration/cli/` (delete/pause failure-path tests).

### R5-10 (P3, decode doctrine + dead code) — Episode replay dead code and the last bare parses
**Evidence (reviewer-verified):** `episode/replay.ts` has **no production caller** (census: only its own unit test; the live read path is `episode/store.ts:46` through `validateEpisodeEvent`) and is off-doctrine twice — per-line `JSON.parse(line) as EpisodeEvent` with no validation, and a bare `Error` on a corrupt middle line. `CheckpointStore.read` (`checkpoint-store.ts:52`) parses uncaught: crash-tear is unreachable (atomic writer) but external damage surfaces as a bare `SyntaxError` on every resume. `cli/flowchart-io.ts:15` likewise parses CLI input files bare.
**Change:** R3-8 census-first discipline: delete `replayFromLog`/`replayEpisodeEvents` with a pin (or wire them through the validated path if a caller is imminent — the census decides); give the checkpoint and flowchart-io readers typed `DomainValidationError`s naming the file (checkpoint read posture must not change resume semantics for a *valid* file, and the ENOENT path stays). Small slot; do not grow it.
**Ownership:** `src/episode/replay.ts`, `src/run/checkpoint-store.ts` (read method only — its write side and decision pin are frozen), `src/cli/flowchart-io.ts`, `test/unit/episode/replay.test.ts`, `test/unit/run/checkpoint-store.test.ts` (additive).

## 5. Explicitly NOT for Round 5 (unchanged or newly settled)

Live R1/bandit/topology (standing; R5-3 hardens the bandit *store*, it does not wire the bandit); jsonl anything and lock perf (saturated); protocol/v1 parse and event fuzz (saturated); per-step writer locking (measured and rejected — the decision pins are the record; only a new same-VM bench reopens it); mailbox TTL/durability/drain-ack (R5-6 is a reachability decision, not a durability slot); lock stealing in any form (no-steal is deliberate, probed, doctor-guided); `maxCostUsd` enforcement (still blocked on a real pricing source); `TASK_LEASE_EXPIRED` rename (persisted schema; disclosed in-source); executor-config persistence in the event log (R4-6 established it needs schema + parent sign-off; resume re-specification with disclosure is the standing contract until a schema round); a non-terminal `RUN_CRASHED` marker event (R4-4 decided against it on evidence — reopen only with parent sign-off and a changed operator action); getter-throw hardening in validators (`JSON.parse` output is the blast radius); rewriting append-only run logs for privacy (disclosure + `delete --run` is the contract).
