[Model: claude-fable-5-thinking]

# Loop 4 · Round 6 — SOTA review (R6-1 … R6-10)

Reviewer: Round 6 SOTA reviewer, evidence-based, on `agent/opt-continuous` at `48a1a59` (all ten slots committed as `6fb7d9d`…`0c9b04a` after the dispatch-id commit `26632a3`, plus three parent joints treated like `74daff3`/`a770a24`: the ADR-006 revert folded into `773b98f`, the isolation sign-off `8e87b8d`, and the probe-pin widening `48a1a59`). Every verdict was checked against the actual diff, not the slot report. Independent gate and crash probe run on this VM (Node v22.14.0). Working tree at review time: clean apart from the parent-owned `.agent_workspace/PROGRESS.md`. Nothing committed by this reviewer; no `src/**` or `docs/**` file edited. Reviewer scratch (the §2.1 repro) lives under `/tmp/r6-review`, not in the repo.

## 0. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R6-1 gate BLOCKED beats loop FAILED | **ACCEPT** | Option (a) implemented exactly as signed off: all three terminal recorders consult one `alreadyTerminal` built on the new `replayedTerminalStatus`, whose set is pinned equal to replay's `sawTerminal` statuses; reviewer's own repro at HEAD confirms BLOCKED status+checkpoint, one terminal, `anomalies: []`, `ANALYSIS_QUEUED` with the evidence named, episode WAITING; `gate-apply.ts`, `applyRetry`, `childTasksFromDefinition` all untouched; the beyond-sign-off widening of `persistBlocked`/`persistCompleted` is disclosed and honestly not claimed as a fixed defect |
| R6-2 resume re-specification | **ACCEPT** | Commit verified test-only; the headline "no reachable false-accept" rests on a structural fact this reviewer verified in source: `coverageOutcome` (`prescore.ts:121-125`) has no `FAIL` in its range at all, and `from-child.ts:141/143` feed it (and `constraint-retention`) copies of their own inputs; the landed pins carry the load-bearing halves (re-specification shape, gate-verdict invariance, FAIL-unreachable tripwire, reconstruction-from-log premise incl. the two-producer `MODEL_ROUTED` trap); `childTasksFromDefinition` not moved by anyone (verified against R6-1's diff) |
| R6-3 lifecycle-lock completeness | **ACCEPT** | Clarification path wraps its whole record-writing body in `withRunLifecycleLock` with discovery hoisted and the nested questions-write lock **deleted** (not nested — the helper is not reentrant); supervised pre-flight hoisted per R5-1 rule 1 with the handle still synchronous; the empty-task-list pre-rounds death reproduced-then-fixed with the same terminal-then-settle pair the rounds get; the resume terminal-check reordering and the two comment-only out-of-ownership edits both disclosed with the precedent named |
| R6-4 doctor `learnedState` | **ACCEPT-WITH-NITS** | Inventory is frozen-additive, reads only through the three shipped readers, catches only the three typed errors as `damaged`, fails only on scan errors (the `runStates` posture) — all verified in the diff; **but** the slot broke `live-isolation.test.ts` at HEAD (doctor is in the live import closure; `loadProjectBandit` is a learned-state read-back) and neither noticed nor disclosed it — R6-5 caught and attributed it with a clean-worktree repro and the parent had to sign off the exception (`8e87b8d`); the base-31 hash-preimage used to reach the keyed bandit path through a root-keyed reader is verified-safe but a wart, and the preferences probe unbinds the process-global store rather than restoring it |
| R6-5 routes + `--lock-wait-ms` | **ACCEPT** | Three routes keyed on the imported frozen code constants, wording matched to R6-4's `remediation` strings with the learned/derived split asserted; `GENERIC_FAILURE_NEXT` and both old routes character-identical (context lines, not edits); absent flag passes `{}` so the unflagged delete is byte-identical, `/^\d+$/` + 24 h ceiling verified, both delete targets covered, pause declined on semantics with the reasoning recorded; the cross-slot attribution of R6-4's isolation break (reproduced in a clean worktree at HEAD) is the round's best process artifact |
| R6-6 cascade wire witness | **ACCEPT** | The decorator supplies only the verification verdict — production `ChildCoordinator` picks the tier and the real Pi executor carries it onto the wire; the pin asserts the **second received request body** carries `loopback-2` against a first-tier `loopback-1`, plus the `TASK_RETRY` hop and an exact count of 2; `process-death.ts` extracted with the owner-token safety argument stated once; loopback helper generalization stays inside `test/helpers/` ownership; no `src/**` |
| R6-7 docs truth-up | **ACCEPT-WITH-NITS** | The landed docs verified accurate to Round 5 source with the correct timestamped disclosure of in-flight R6-1/2/3 (20:25 UTC) — but the slot flipped ADR-006 to **Accepted** against the standing forbidden list; the parent reverted it before committing (`773b98f` contains no `docs/decisions/` change; the file and every doc reference say Proposed at HEAD — verified by grep), so the slot's report now contradicts the landed tree in three places (the Accepted claim, the ADR file in its Files list, the stale-phrase census asserting no "Proposed ADR-006 wording" remains) |
| R6-8 SIGKILL run-lock probe | **ACCEPT-WITH-NITS** | The case is real and passes 3/3 on this VM: a child running real `startRun` self-SIGKILLs mid-executor, the lock's dead PID is proven (`ESRCH`), delete refuses with `LOCK_TIMEOUT` against a byte-compared directory snapshot, doctor's inventory carries `pidLiveness: "not-running"` + manual-removal remediation, and the operator remedy completes a verified delete — but the slot changed the probe's case set without censusing its consumers, breaking `crash-recovery.test.ts`'s reduced-probe pin at HEAD (R6-1 attributed it; the parent fixed it in `48a1a59`), the exact census-your-consumers class the Round 5 brief codified |
| R6-9 unused-export census | **ACCEPT** | All three deletions verified gone with absence pins (`closeEpisode` duplicate, `DispatchPreflightError`, `compilableChildFrom` — no caller existed for any); the 155→150 accounting is explicit including the two exports R6-4 made live mid-round; the 150 remaining seams are dispositioned individually with named evidence classes; `supervisor.ts`/`flowchart-run.ts` correctly report-only, and the `trackingAssessment` no-op census this reviewer re-verified in source |
| R6-10 flowchart dead-letter + countdown | **ACCEPT** | A real `startFlowchartRun` (`cluster: true`) with five chained scouts reaches `deadLettered: 1` and the byte-identical operator line; the unit pin drives tallies `[1, 2, 3]` monotone into the exact dead-letter record with `requeues: 3` and `requeueCount() === 0` after the drop; no production file changed, shapes and line format byte-frozen |

7 ACCEPT, 3 ACCEPT-WITH-NITS, 0 ROLLBACK. No lint or test blocker found (independent full gate GREEN), so there is nothing to commit from this review beyond this file and `ROUND6-BRIEF.md`.

## 1. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0**: **1769 tests / 1768 pass / 0 fail / 1 skipped** — matching the parent's numbers exactly. The sole skip confirmed by census: the only `skip:` option anywhere in `test/**` is the `PI_SMOKE=1` gate in `provider-smoke.test.ts` (the other grep hit is the pre-existing variable named `skip` in `coverage-gate.test.ts`; no `.skip(`/`test.skip`/`it.skip` anywhere). **No slot introduced a new named skip.** Delta vs the Round 5 baseline (1725/1724/1): **+44 tests**.
- **`node scripts/crash-probe.mjs` → `ok: true`, 9 cases × 3 iterations**, including R6-8's `sigkill-run-lock-operator-recovery` at 3/3. Case set grew by exactly one.
- Key new suites re-run scoped after the gate (`gate-outcome`, `clarification-lifecycle-lock`, `supervised-lifecycle-lock`, `supervisor-crash`, `crash-recovery`, `live-isolation`): 39/39 pass.
- **The three parent joints, verified exactly as claimed:**
  - **ADR-006 (inside `773b98f`)**: the commit touches no `docs/decisions/` file; `0006-pi-extension-reverse-adapter.md` says **Proposed** at HEAD, and every doc reference (`how-to-adapt-to-pi.md:14,229`, `m0-m2-architecture.md:352`) says Proposed. The slot's flip never landed. Frozen forever, as instructed.
  - **`8e87b8d` (R6-4×live-isolation)**: the pin's reader census now excludes `src/cli/doctor.ts` **and** adds an `assert.match` requiring `doctor.ts` to still call `loadProjectBandit` — the exception cannot silently go unused. The docstring names the sign-off ("doctor never feeds routing") and the assertion message calls it "the signed-off diagnostic exception". Exactly as the task described.
  - **`48a1a59` (R6-8×crash-recovery)**: adds `sigkill-run-lock-operator-recovery` to the reduced-probe expected case list and widens the child-kill timeout 14 s→24 s and the test timeout 15 s→25 s. Touches only that test file.

### Freeze check — held, no undisclosed exceptions

- `git diff 26632a3..HEAD` over `src/persist/`, `package.json`, `pnpm-lock.yaml`, `src/run/episode-bind.ts`, `src/cli/episode.ts`, `src/telemetry/model-invocation.ts`, `src/run/gate-apply.ts`, `src/run/checkpoint-store.ts` is **empty**. `writeFileAtomic`/`writeFileAtomicSync`, jsonl, `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE` byte-identical. No dependency edit.
- **No new private temp+rename**: the only `.tmp`-matching addition in the whole round diff is a test assertion that no temp file is left behind (R6-3's crash-atomicity pin).
- **The two decision pins hold**: `append does not block on the run lock` (`event-store.test.ts:119`) and `checkpoint writes do not block on the run lock` (`checkpoint-store.test.ts:156`) present and green; `event-store.ts`'s diff is comment-only (11 changed lines, all doc-comment — verified line-by-line; `runLockPath` and `append` bodies untouched), `checkpoint-store.ts` untouched.
- **`applyRetry` sole BLOCKED→READY producer**: `scheduler.ts` untouched; its one caller (`supervisor.ts:277`) unchanged in R6-3's diff; nothing new produces READY from BLOCKED. R6-1's change makes BLOCKED *absorbing* at the run-log level (see finding 2) without touching the scheduler contract.
- **Dead-letter surfaces**: no `src/cluster/**` edit anywhere in the round; R6-10 pins the line byte-identical from the flowchart embedder.
- **Resume disclosures / sink wiring**: R6-5's `main.ts` diff enters only `deleteCommand`, `DOCTOR_ROUTED_NEXT`, and usage text; `runCommand`/`resumeCommand` bodies untouched; no `createExecutor` moved.
- **Crash-terminal module**: untouched. R6-3's pre-rounds widening *consumes* `recordCrashTerminal` + `settleCrashedSupervisedRun` in the same order the rounds do rather than adding a third recorder; the in-flight-only guard is what keeps the degenerate logs safe, unchanged.
- **Live isolation**: `loadProjectBandit` readers in `src/**` are exactly `bandit-store.ts` (itself) and `src/cli/doctor.ts` (the signed-off exception); `selectArm`'s only `src/**` caller remains `routing/shadow.ts`, outside the live closure — the pinned selector census is green.
- **ADR-006 Proposed, nothing Outcome-supported** (status-matrix retains "Nothing in this repo is Outcome-supported"), no live R1/bandit/topology, no `package.json` edit — all verified.
- Sole-ownership held: `flowchart-run.ts` edited only by `0f5225e`, `main.ts` only by `e7cf625`, `supervisor.ts`/`track/loop.ts` only by `0c9b04a`, `doctor.ts` only by `bee0ca3`. R6-3's `deletion.ts`/`event-store.ts` comment-only edits are the round's only out-of-list `src` touches, both disclosed with the R5-1 precedent and with the pinned message substrings verified intact.

## 2. Requested target verifications

### 1. R6-1 option (a) — VERIFIED, reviewer-reproduced
My own repro at HEAD (`/tmp/r6-review/repro-r61.mts`, real `startFlowchartRun`, one clustered child through `compileChildrenToFlowchart` returning `outcome: SUCCESS` + `verification: { kind: "FAILED" }`): `status: BLOCKED`, `checkpoint.status: BLOCKED`, tail `…TRACKING_ASSESSMENT → GATE_TRANSITION → RUN_BLOCKED → LEDGER_UPDATED → EPISODE_WAITING`, terminals `["RUN_BLOCKED"]`, `anomalies: []`, `RUN_BLOCKED.reason === "ANALYSIS_QUEUED"` with the owed evidence named, episode events end `EPISODE_WAITING` (no `EPISODE_CLOSED`), no `RUN_FAILED` anywhere. This is the exact inversion of the Round 5 review's finding 1. `gate-apply.ts` has an empty diff for the round. `applyRetry` untouched. The refusal mechanism is structurally sound: `TERMINAL_REPLAY_STATUSES` is pinned equal to the statuses `replayRun` sets `sawTerminal` for (verified against `replay.ts:117-129`), and `replayedTerminalStatus` calls `replayRun` rather than re-scanning, so the two notions of "terminal" cannot drift. The old per-recorder guards are strictly subsumed (a log carrying `RUN_FAILED` replays FAILED), so idempotence is preserved. `persistFailed` still runs `cancelAndSettle()` before the read.

### 2. R6-2's 0-false-accept and the tautology — VERIFIED at the structural level
The 240-cell sweep itself lives in `/tmp/r62` and is not re-runnable, but its load-bearing premise is a two-line source fact I verified independently: `coverageOutcome` (`prescore.ts:121-125`) returns only `NOT_APPLICABLE`/`PASS`/`UNOBSERVED` — **`FAIL` is not in the function's range for any input**, resumed or not — and `from-child.ts:141` sets `completedChecks` to a copy of `requiredChecks` on PASSED and `[]` otherwise, so the child path can only ever see PASS/NOT_APPLICABLE/UNOBSERVED. I additionally verified the adjacent tautology R6-2 stated in passing: `retainedConstraintIds` (`from-child.ts:143`) is likewise a copy of the constraints' own ids, so `constraint-retention` is always PASS when constraints exist. Given those, "the gate directive is a pure function of `verification.kind`" follows and the 0/240 result is what the code must produce. The landed tripwire (`check-coverage cannot fail…`) pins `FAIL` unreachable through the shipped derivation, exactly as reported. `childTasksFromDefinition` still sits at `flowchart-run.ts:231` with its hard-coded trio, moved by no one — R6-1's diff has zero occurrences of the symbol (verified). The re-specification, budget-widening (12× wall time), role-coarsening, and contract-drop findings all check against source (`resumeFlowchartRun`'s loop context carries no `contract` key; the start path threads `input.contract`).

### 3. R6-3's three parts — VERIFIED
(a) The clarification path: `waitForClarification` mints the run id and runs `discoverProject` **outside**, then wraps `recordClarificationRun` in `withRunLifecycleLock` with the `runLock` seam threaded; the questions write's own `withExclusiveFileLock` acquisition is **gone** from `track/loop.ts` (import deleted; the new source pin forbids `runLockPath`/`withExclusiveFileLock` in the file), which is the only correct shape given the helper is not reentrant. The "writes nothing while a lock is held" pin drives a real foreign holder against the deterministically-minted run id and asserts no event log, no checkpoint, no questions file, no bound episode. (b) `startSupervisedRun` hoists `discoverProject` + `validateTaskGraph` into the pre-acquisition async IIFE, keeps the handle synchronous, and the refused-start pins assert `readdir(stateRoot)` empty for missing-root, cyclic-graph, and lock-refused starts (the M0 pin's exact assertion, now on the supervised plane). (c) `runAndSettleSupervisedRun` takes `open: () => Promise<SupervisorState>` and gives the pre-rounds window `recordCrashTerminal` + `settleCrashedSupervisedRun` in the rounds' order; the empty-task-list seed (reachable with no filesystem sabotage — `validateTaskGraph([])` accepts, the `TASK_GRAPH_ACCEPTED` append is refused by event validation) now settles to FAILED with `afterTerminal === ["EPISODE_CLOSED"]`. The disclosed consequence — resume's terminal check moved above state reconstruction — is correctly scoped: observable only on terminal graph-free logs, which no writer could produce before this slot, and the checkpoint-write-and-return block itself is relocated unmodified. Resume passes a trivial `open` so a refused resume appends nothing.

### 4. Freeze — HELD (§1). Nothing beyond the disclosed comment-only edits and the signed-off isolation exception.

### 5. Isolation — VERIFIED
`rg loadProjectBandit src/` → `bandit-store.ts` + `doctor.ts` only; the amended pin requires doctor's call to persist and forbids any other live reader; `selectArm` has no live-closure caller (`shadow.ts` is its only `src` consumer and the pinned selector census is green through the gate).

### 6. No new named skip — VERIFIED (§1 census; gate TAP skip count exactly 1).

### 7. Parent joints — VERIFIED exactly as claimed (§1). One correction of emphasis: the ADR-006 joint is not visible as a separate commit — the parent stripped the flip *before* committing `773b98f`, so the only trace is the commit message and the slot report's contradiction. Future reviewers diffing commits will not find a revert to inspect; the evidence is the absence of any `docs/decisions/` hunk plus the Proposed status at HEAD.

## 3. Per-slot notes beyond the targets

- **R6-1 (ACCEPT).** The census that reframed the fix is right and verified: `startParentRun` already breaks out of its terminal-append branch on `queue_analysis`, and `recordCrashTerminal` already refused to write over BLOCKED — the flowchart loop was the outlier, so option (a) is two planes' existing contract extended to a third, not an invention. The honest §4 ("I could not reach `persistCompleted`'s refusal today and am not claiming it as a fixed defect", with five probed PASSED shapes) is the round's best example of not over-crediting one's own change. Disclosure 1 (the `run --children` stderr loses `reason:`/`next:` for this case because the status is no longer FAILED — verified at `main.ts:911`, exit code unchanged) and the census that no test pinned the lost wording are both accurate. The suites-asserting-`anomalies: []` census (none drives a verification-failed clustered child, so none needed fixing) matches the green gate.
- **R6-2 (ACCEPT).** Investigation-first done properly: the negative headline is stated in the first line, the structural cause is named, and the finding that *upgrades* the round — no acceptance criterion anywhere in the tree can currently fail a child — is handed to Round 7 with a tripwire that fires the moment it stops being true, whose docstring orders the fix ("fix `childTasksFromDefinition` in the same diff"). The reconstruction design's sharp edge (two `MODEL_ROUTED` producers for one task; select on `agentRole !== undefined`) is the kind of trap a Round 7 implementer would otherwise hit silently. The self-correction (node `b` never carried the caller's criteria — both its attempts come from a resume; node `a` is the control) is disclosed rather than papered over.
- **R6-3 (ACCEPT).** The strongest fix of the round by evidence discipline: all three defects reproduced at HEAD before editing, with the empty-task-list seed genuinely production-plausible and the "no command can settle it" consequence demonstrated, not asserted. The nonexistent-run-resume posture (empty `runtime/runs/` and nothing else, both planes, table-driven) converts R5-1's disclosure 4 from folklore to contract. The R5-2 nit (supervised pre-flight inside the lock) is closed with the M0 pin mirrored verbatim. Both out-of-ownership comment edits shrink over-counting lists rather than change behavior, and no test pinned the prose (checked by the slot, spot-verified here).
- **R6-4 (ACCEPT-WITH-NITS).** The surface itself is well built and the report is honest about its two structural compromises (no pure preference reader exists, so bind-then-unbind; no keyed bandit reader exists, so a verified hash preimage). The nits: (1) the isolation break — `doctor.ts` has been in the live import closure all along, so calling `loadProjectBandit` from it was always going to trip the Checkpoint-F pin; the slot's verification ran the doctor suite but not the isolation suite, and the report does not mention the red test it left at HEAD. R6-5 attributed it with a detached-worktree repro and named the two remedies; the parent chose the sign-off. The outcome is fine; the process relied on a sibling's diligence. (2) The preimage maps the stored key to an opaque root of control characters so the root-keyed reader lands on the stored path — verified safe (round-trip checked, algorithm change degrades to a scan error), but it is a second encoding of the key algorithm living in doctor; a keyed reader export would delete it. (3) `configurePreferencePersistence(undefined)` after the probe *unbinds* rather than restores — harmless in the CLI (process exits), a footgun for in-process callers (R6-5's end-to-end test already had to restore the binding in a `finally`). All three are Round 7 cleanup material, none rollback material.
- **R6-5 (ACCEPT).** The route wording is coupled to R6-4's `remediation` strings so the `next:` and the inventory it points at say the same thing, and the derived route is pinned never to say "relearn" — the learned/derived distinction enforced at the operator surface, which was the point of the two-slot split. The end-to-end case (`pref list` over a truncated snapshot → exit 1 → the routed doctor command really lists that path as `damaged`/`learned`) makes one of the three routes a checked promise; the other two are constructed-error cases (see brief, R7-10). The `--lock-wait-ms` decisions are all argued in the right direction: `{}` not `5000` keeps the default owned by the lock; `/^\d+$/` because a wait that differs from what the operator typed is worse than a refusal; the pause refusal is a semantics argument (a longer pause wait can only ever succeed against an already-terminal run) recorded where a future slot will look. The shared-tree process lesson ("a 'precondition did not land' verdict is only true as of the minute you took it") is worth carrying.
- **R6-6 (ACCEPT).** The boundary is drawn exactly right: the decorator rewrites only the `TASK_RESULT`'s verification/outcome fields and never chooses a model or makes an HTTP call, so the second request's `model: "loopback-2"` can only have come from production cascade selection. The helper generalization (accepted-model list, echo the requested model) is what makes the witness possible without weakening the protocol-error posture (unknown models still 400). `process-death.ts` closes the Round 5 review's finding 5 for the loopback suite; the `m2/resume` inline copy stays by ownership, correctly.
- **R6-7 (ACCEPT-WITH-NITS).** The landed truth-up content verified accurate against Round 5 source: role-level mailbox semantics with the countdown meaning stated, the four lock postures, the crash-terminal/settle contract including the independent best-effort halves, `BANDIT_STATE_UNREADABLE` with the `adapt skipped:` vs `stage: "validation"` split R5-3 handed over, the doctor routes, the typed checkpoint read, the wire witness — and the in-flight R6-1/2/3 disclosure uses the correct timestamped method. The nits are process, and they are not small: flipping ADR-006 violated a standing forbidden-list item that appears in the brief, the ownership file, *and* the dispatch context; the report's own text ("per the task instruction") suggests the slot believed it was instructed to — either a misreading or an injected instruction, and either way the check that should have caught it (re-read the forbidden list before flipping any decision status) did not run. The parent's revert made the landed tree compliant, but the report at HEAD misdescribes the diff it shipped in three particulars (§0). Standard follow-through staleness also applies: R6-1/2/3/4/5 all landed after the snapshot, so the docs now understate the tree (the delete prose lacks `--lock-wait-ms`, doctor's row lacks `learnedState`, the lock prose lacks the clarification run) — correct method, Round 7 docs material.
- **R6-8 (ACCEPT-WITH-NITS).** The case's delta over the in-process pins is real and stated (no unit pin crosses an OS process boundary or bypasses the lock's cleanup `finally`; `stale-lock-no-steal` has no doctor/delete chain), the snapshot byte-compare makes "removed nothing" a proof rather than an assertion, and the full operator arc ends in `verifyRunRecordsRemoved`. The nit: `test/integration/persist/crash-recovery.test.ts` spawns the probe and pins its case list — the direct consumer of exactly the thing this slot changed — and the slot neither censused nor mentioned it. R6-1 caught the red test and attributed it correctly; the parent's `48a1a59` is the fix R6-8 should have shipped or at least prescribed (the R5-2 precedent: disclose the break and the remedy in your own report). The Round 5 brief's census-your-consumers lesson was written for "process death" semantics specifically; the brief for Round 7 generalizes it.
- **R6-9 (ACCEPT).** The method (compiler-API reference counts, alias resolution, dynamic-import audit, barrel audit) is a real census, and the three corrections it found against its own first pass (`listSparkleModels` etc.) are evidence it was actually run. The three deletions are the right ones — each verified to have no caller, no test, no barrel, no doc — and each absence pin follows the R3-8/R5-10 pattern. The disposition legend makes the 150 survivors auditable rather than a bulk pardon; the spot checks I ran (tombstone `saveLearnedRouting`, harness seams, the two newly-live readers) all hold. Report-only discipline for the two round-owned files was kept, and the `trackingAssessment` hand-off is precise (sole caller `finishSupervisedRun` at `supervisor.ts:561` passes none; the branch returns early at `:529`).
- **R6-10 (ACCEPT).** Tersest slot, everything checked: the flowchart reachability test is production-shaped (dependency-chained scouts, `DEFAULT_MAX_ROLE_REQUEUES + 2` of them, real `compileChildrenToFlowchart`), the report shape and line are asserted byte-for-byte, and the countdown pin nails the two halves of R5-6's disclosure 3 — monotone tallies while pending, the final tally preserved in the dead-letter record, and the pending-only accessor honestly zero after the drop.

## 4. New reviewer findings (evidence for Round 7)

1. **No acceptance criterion anywhere can fail a child — verified in source, and it is the round's most consequential fact.** `coverageOutcome` cannot return `FAIL` for any input (`prescore.ts:121-125`), and the child path feeds it a copy of its own `requiredChecks` (`from-child.ts:141`); `constraint-retention` is likewise copy-fed (`from-child.ts:143`). The gate's directive is a pure function of `verification.kind`. This means: (a) R6-2's resume gap cannot cross a threshold today, and (b) the moment anyone makes check-coverage real, the empty-criteria resumed spec becomes a genuine false-accept vector — R6-2's tripwire fires first and orders the fix. Round 7 should treat "make the criteria-shaped dimensions real vs record the accepted contract" as a decision needing parent sign-off, sequenced **after or with** the reconstruction fix. → R7-1/R7-2.
2. **BLOCKED is now absorbing at the run-log level.** After R6-1, all three recorders refuse over a BLOCKED log — including `persistCompleted` — and nothing produces an unblock (`replayRun` has no event that clears `RUN_BLOCKED`; `applyRetry` is scheduler-level READY, not log-level). So a BLOCKED run is injectable and resumable but can never replay as anything but BLOCKED, ever. Before R6-1 the escape hatch was an anomalous double-terminal; now there is none. R6-1 said this plainly and deliberately did not invent a schema event; the standing "no new persisted schema without sign-off" posture applies. → R7-3 (investigation/design first).
3. **`coordinator.ts`'s catch-all appends `RUN_FAILED` unconditionally** (verified, `coordinator.ts:618-621`): the parent-plane crash path lacks both the in-flight-only guard and R6-1's replayed-terminal refusal, so a crash after a gate BLOCKED on that plane would write the same double-terminal R6-1 just removed from the flowchart plane. R6-1's hand-off, re-verified here. → R7-4.
4. **A BLOCKED `run --children` exits 1 with no `reason:`/`next:` block** (verified, `main.ts:911` fires only on FAILED). The node failure is still visible via `printFlowchartOutcome`, but the operator gets no routed next step for the queued analysis — the R5-9 pattern stops one status short. `main.ts` is unowned next round. → R7-5.
5. **The doctor exception should get narrower, not wider.** Three seams would shrink it: a keyed bandit reader (deletes the base-31 preimage), a pure preference reader (deletes the global bind/unbind that R6-5's test already had to repair around), and keeping the isolation pin's `assert.match` in lockstep with whatever doctor ends up calling — that last edit touches a signed-off guard and needs parent sign-off by the same rule that created it. → R7-8.
6. **`settleSupervisedOutcome.trackingAssessment` is a dead branch on every path** (R6-9's census, re-verified: sole caller passes none, early return at `supervisor.ts:529`), and R6-1's census adds the missing context — the supervised plane has no gate producer at all, so the branch is not "not yet wired", it is unreachable by design of the current tree. Census-and-delete or wire-it is now a one-slot decision with the evidence already gathered. → R7-7.
7. **Two of the three new doctor routes have no end-to-end producer in tests** — bandit and catalog are constructed-error cases; only preferences is driven through a real command (`pref list`). Real producers exist (`adapt` paths over a damaged `bandit.json`; catalog loads over a damaged `catalog-observed.json`), so the honest-gap situation differs from R5-9's `RUN_RECORDS_SURVIVED`: these are cheap to close. → R7-10.

## 5. Handoff

- `ROUND6-BRIEF.md` (same directory) carries the Round 7 injection context: what landed, the new baseline (gate 1769/1768/0/1-skip; probe 9×3; frozen list grown with the Round 6 contracts), and ten mutually exclusive candidates ranked with the check-coverage/reconstruction pair at the top and the ordering constraint between them stated.
- Nothing in flight at review time: all ten slots and all three joints are committed; the working tree holds only the parent-owned `PROGRESS.md` edit.
- Nothing was committed by this reviewer, no `src/**` or `docs/**` file was edited, and no fix was needed anywhere. The parent should commit `.agent_workspace/loop4-r6-review.md` and `.agent_workspace/ROUND6-BRIEF.md`. Reviewer scratch lives under `/tmp/r6-review`, not in the repo.
