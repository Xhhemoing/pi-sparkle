[Model: claude-fable-5-thinking-xhigh]

# Loop 4 · Round 9 — SOTA review at `8e933a7`

Reviewer ran independently on this VM (Node v22.14.0), on `agent/opt-continuous`, working tree clean at HEAD `8e933a7`. Every verdict below is against the actual diff `b65ad06..8e933a7` — the ten exclusive landings (`5970a2f`…`aeb14dc`) plus the three parent commits (`54d3131`, `c5ab9bf`, `8e933a7`), which touch only `.agent_workspace/**` — verified per-commit with `git show --stat`, not taken from slot reports. No file outside `.agent_workspace/` was changed by this review; scratch outputs lived in `/tmp` (`/tmp/r9-gate.log`, `/tmp/r9-crash-probe.log`, `/tmp/routes-{b65ad06,HEAD}.txt`, `/tmp/pin-{base,head}.txt`).

## 1. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R9-1 | **ACCEPT** | Every signed-off property verified in the diff: optional `contract?` at `schemaVersion: 1`, wrapped validation, all **three** writers carry it (including the census-missed `unblockLockedFlowchartRun` — real in the diff, §4.1), flip-pin through `main(["resume",…])`, region-regex nit fixed with a drift guard, `INSPECT_SUMMARY` one-liner landed, per-task criteria reserved in prose only; routes/reconstruction/AST/loopback freezes all held |
| R9-2 | **ACCEPT** | `sparkle_report_task_result` built per leased attempt with request-stamped identity; PASSED/FAILED only, FAILED must cite evidence (the `from-child.ts:76-79` discard re-verified in source); pin 2 replaced in place, pins 1/3/4/5 **byte-identical** (diffed body-by-body); reachability disclosure re-checked against `gates.ts`/`prescoreInputFromObservation`, not rubber-stamped (§4.2); no protocol schema change |
| R9-3 | **ACCEPT** | No `src/**`; the additive fail-closed pin names both executed descendants and proves the snapshot untouched; the `RUN_UNBLOCKED_WITH_DISCARD` design is the right call of the three shapes (§4.3) — recommend Round 10 sign it off with one payload question decided (§6.1) |
| R9-4 | **ACCEPT-WITH-NITS** | Docs-only, no ADR file opened, ADR-006 Proposed, `0005:110` flag-only, timestamped census; nit: `m0-m2-architecture.md:737` still says the probe has **nine** cases — R9-5's tenth case was committed at 22:32:42, four minutes before R9-4's final 22:36:16 census, in a fact-class the sync covered (§5 nit 1) |
| R9-5 | **ACCEPT** | Tenth case `unblock-append-before-checkpoint-sigkill` appended after the unchanged original nine; the kill is genuinely external (parent polls for the complete newline-terminated append of a 16 MiB reason, then SIGKILLs; asserts `signal === "SIGKILL"`, checkpoint byte-identical, no temp file); real consumer `test/integration/persist/crash-recovery.test.ts` pinned with disclosure — the brief's `m2/` path does not exist and the slot said so |
| R9-6 | **ACCEPT** | `gate-apply.ts` diff mechanically verified comment-only (every added line matches a comment pattern; zero removed lines); `runStatus` has zero `src` readers — re-swept independently, the identifier appears only in `gate-apply.ts` and one object-literal write in `child-tracking.ts`; the `applyChildThreeLine` ownership deviation is disclosed, ownership-forced, and lands the posture on the declared return type |
| R9-7 | **ACCEPT** | AST-level character-exact freeze of `GENERIC_FAILURE_NEXT` + all five ordered route tuples, with a deletion-mutant proof; reviewer independently extracted and diffed the routes base-vs-HEAD — identical through R9-1's `main.ts` edit |
| R9-8 | **ACCEPT** | Additive only (+56/−0); call-site pin requires exactly one `childTasksFromLog(read.events, definition, registry, …)` in `resumeLockedFlowchartRun` and the child map built from it; the R6-2 tripwire is frozen by reading this test's own source (title, seam name, both kinds, all five outcomes, FAIL-unreachable assertion) |
| R9-9 | **ACCEPT** | Additive only (+37/−0); recursive 215-module census pins `selectArm` to `shadow.ts` alone and `\bloadProjectBandit\b` to zero `src` occurrences; the signed-off doctor `assert.match` and the widened readers sweep are untouched; reviewer's own sweep confirms all three facts at HEAD |
| R9-10 | **ACCEPT** | Bidirectional type-level equality freezes `keyof` the `RUN_UNBLOCKED` payload to exactly the three keys — a fourth typed key fails whole-tree `tsc`; fuzz seed and runtime exact-key refusals untouched; payload at HEAD read directly: `{ blockedEventId, reason, retryNodeId? }` |

**9 ACCEPT, 1 ACCEPT-WITH-NITS (R9-4), 0 ROLLBACK.** Zero parent fix-joints; nothing left red at HEAD.

## 2. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0: 1880 tests / 1879 pass / 0 fail / 1 skipped** — matches the parent's recorded numbers exactly. The one skip is the `PI_SMOKE` real-provider gate (`PiAgentExecutor completes a run against a real provider # SKIP`); no slot introduced a second skip.
- **`node scripts/crash-probe.mjs` → `ok: true`, 10 cases × 3 iterations** — matches. The tenth case is `unblock-append-before-checkpoint-sigkill`, appended last; the original nine names and their order are unchanged in the probe output.
- **The +35 over Round 8's 1845/1844/1 closes per-slot.** Static test-registration deltas per changed test file, base `b65ad06` vs HEAD: R9-1 **+8** (resume 10→15, flowchart-snapshot 7→10, row-fuzz 5→5) · R9-2 **+13** (new `report-task-result.test.ts` 13; `option-a-preconditions` 5→5, pin 2 replaced in place) · R9-3 **+1** (flowchart-supervisor 24→25) · R9-4 **0** (docs) · R9-5 **0** (crash-recovery 1→1, name-list only) · R9-6 **+8** (new `gate-status-posture.test.ts`) · R9-7 **+2** (new freeze file) · R9-8 **+1** (22→23) · R9-9 **+1** (8→9) · R9-10 **+1** (event-row-fuzz 4→5). Sum: exactly 35.
- **Commit hygiene:** each of the ten landings touches only its `OWNERSHIP.md` files plus its own report — verified per-commit with `git show --stat`; no two slots share a file. `54d3131`/`c5ab9bf`/`8e933a7` touch only `.agent_workspace/**` (review, brief, ownership, PROGRESS). The dirty-tree snapshot some observers saw at dispatch was R9-1's pre-commit state; everything in it is inside `aeb14dc`.

## 3. Freeze check

All verified against the actual range diff and the tree at HEAD, not report claims:

- **Allowed-to-change set honoured; no undisclosed extras.** `src/**` changes are exactly: `cli/main.ts` + `run/flowchart-run.ts` + `run/replay.ts` (R9-1, signed off), `pi-adapter/pi-executor.ts` (R9-2, signed off), `run/gate-apply.ts` (R9-6, comment-only — mechanically verified). Diff-empty across the round: `package.json`, `pnpm-lock.yaml`, `src/run/events.ts`, `src/run/scheduler.ts`, `src/run/crash-terminal.ts`, `src/run/child-tracking.ts`, `src/run/episode-bind.ts`, `src/persist/**`, `src/protocol/**`, `src/tracking/**`, `src/graph/**`, `docs/decisions/**`, `test/integration/pi-adapter/loopback-cli-resume.test.ts`.
- **jsonl signatures / `writeFileAtomic` / no new private tmp+rename:** the round's entire `src/**` diff adds **zero** raw fs write primitives (grep of added lines for `writeFile`/`appendFile`/`rename`/`createWriteStream` and Sync variants: empty). The contract writes go through the existing `CheckpointStore.write`; the verdict tool writes nothing — it emits into the attempt's in-memory event array.
- **`RUN_UNBLOCKED`:** payload at HEAD is exactly `{ blockedEventId: EventId; reason: string; retryNodeId?: string }` (read directly in `events.ts:251-255`); no fourth key anywhere in the round (R9-3 designed a **distinct** event instead, R9-10 type-froze the three keys); `TERMINAL_REPLAY_STATUSES` unchanged (`COMPLETED`/`FAILED`/`BLOCKED`, read at `replay.ts:276-279`); producer order and the locked `unblock` command untouched (`flowchart-run.ts`'s only unblock hunk is the contract carry-forward).
- **Five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT`:** extracted at base and HEAD and **diffed character-exact — identical**, despite R9-1 editing `main.ts`; now additionally AST-frozen by R9-7 with a deletion-mutant check; `CATALOG_OBSERVED_CORRUPT_CODE` still the fifth entry.
- **Reconstruction contract:** `childTasksFromLog` untouched (no hunk names it); the resume call site is now source-pinned (R9-8); the R6-2 FAIL-unreachable tripwire present and itself frozen by R9-8's self-source pin.
- **`applyRetry` sole scheduler producer:** `scheduler.ts` and both scheduler test files byte-identical; R8-3's AST pin green in the gate with R9-1's `flowchart-run.ts` edits in place.
- **Isolation:** reviewer's own sweep at HEAD — `\bloadProjectBandit\b` zero `src` matches; `loadProjectBanditByKey` exactly store declaration + doctor import/call; `selectArm` exactly definition + `shadow.ts`. R9-9 added the recursive census without touching the signed-off `because` (+37/−0). ADR-006 **Proposed** (`docs/decisions/0006…md:3-5`); `docs/decisions/` diff-empty for the entire round. `docs/status-matrix.md` still records "Nothing in this repo is Outcome-supported".
- **Parent-plane residuals, `recordCrashTerminal` rethrow, empty-graph pre-flight, `settleSupervisedOutcome` absence, `withRunLifecycleLock` rules, append/checkpoint unlocked, `--lock-wait-ms`, `process-death.ts`, loopback supervised-resume stderr byte-pin, four-line BLOCKED block:** all owning files diff-empty; all pins green in the independent gate.
- **`INSPECT_SUMMARY`:** frozen-additive pins untouched; R9-1 added only the producer-side comment the parent ordered — the literal's four keys unchanged in the diff.
- **Gate `runStatus`:** now a recorded ledger posture (comment-only) with zero `src` readers — independently swept, and R9-6's pin 2 makes a future reader fail across all 215 `src` modules.

## 4. Requested target verifications

### 4.1 R9-1 — every requested property, against the diff

- **Field optional, absence valid:** `contract?: RequirementContract` on `FlowchartCheckpointState`; the validator returns the exact three-key object when absent (`if (value.contract === undefined) return { definition, snapshot, limits }`) — no empty contract invented. Validation runs `validateRequirementContract` and wraps as `Invalid RunCheckpoint: flowchart.contract: …` — both in the diff.
- **Writers/restorers/projection/precedence:** `persistCheckpoint` spreads `ctx.contract` on every checkpoint; `restoreFlowchartSession` restores `checkpoint.flowchart.contract` into the context (pause/inject cannot strip); `flowchartContinuation` projects `opts.checkpoint?.flowchart?.contract` (one change covers `resumeCommand` and `answerCommand`, both pinned to feed it the checkpoint); `resumeLockedFlowchartRun` uses `continuation.contract ?? checkpoint.flowchart.contract` — precedence proved behaviourally by the constraint-free-continuation test (NOT_APPLICABLE while the checkpoint carries two, which distinguishes `??` from both `||` and the reverse).
- **Never synthesized from the episode:** `episode-bind.ts` diff-empty; the episode pin is kept and reframed from "currently" to the standing rule, with the load-bearing reason in the test (a recovery reading constraints off the episode would present `[]` as the run's own).
- **The flip is through the production CLI boundary:** `main(["resume","--run",…,"--executor","fake","--unpause"])` with the verdict read off the log (`constraint-retention: PASS` on `tsk_second`), stored contract asserted before and after. The API-side *seeding* is disclosed in the docstring with the correct reason — no CLI command accepts a contract. Both companion tests landed: the no-contract CLI resume invents none (both legs `NOT_APPLICABLE`, checkpoint still `undefined`), and the retired pin's exact call shape flipped (direct resume handed nothing recovers the durable value).
- **The unblock carry is real in the diff:** `unblockLockedFlowchartRun` now destructures `contract` off the validated checkpoint and spreads it into the `materializeCheckpoint` payload — this writer bypasses `persistCheckpoint` and rebuilds `{ definition, snapshot: reopened, limits }` from parts, so before this diff it silently erased the contract from any run an operator unblocked, with both restorers still correct. R8-2's census missed it; R9-1's found it and pinned it in source and behaviour (BLOCKED stall run → `unblockFlowchartRun` → contract still stored). The best find of the round.
- **Region-regex nit fixed, with a drift guard:** every region now ends on the function's own `^\}$` (the old `^\}` alone captured only `flowchartContinuation`'s signature — the report explains why R8-2's `doesNotMatch` passed vacuously), and the `resumeCommand` region asserts `doesNotMatch(/\bunblockCommand\b/)` so the boundary cannot silently drift again.
- **`INSPECT_SUMMARY` one-liner:** a three-line comment at the `main.ts` producer literal naming the freeze and the pin file; the four keys and the emit are unchanged.
- **Per-task criteria reserved in comment only:** the `Reserved:` prose is on the field's docstring, and the schema pin asserts `doesNotMatch(/acceptanceCriteria\??:/)` on the interface — the reservation is itself pinned unimplemented.
- **Kept green:** R9-7's route freeze (reviewer diffed the routes independently — identical), R9-8's reconstruction pins, R8-3's AST pin, the four-line BLOCKED block, the loopback supervised-resume pin (file diff-empty), and no fourth `RUN_UNBLOCKED` key — all green in the independent gate at HEAD.
- Row-fuzz gained a genuine contract-bearing flowchart checkpoint seed (`CHECKPOINT_SEEDS`, cycled per the file's idiom, `contractsMutated > 0` asserted — the arm proves it is no longer vacuous); flowchart-snapshot gained the valid/malformed(4 shapes)/absent trio with `"contract" in … === false` on absence.

### 4.2 R9-2 — producer, pins, and the reachability disclosure re-checked

- **Tool per leased attempt, identity from the request:** `createTaskResultTool(request, emit)` built inside `runAttempt` beside `createClusterTools`, appended to the same `tools` array; `runId`/`taskId`/`from: request.agentInstanceId` and id/clock from adapter helpers — the model supplies verdict, prose, references, nothing else. The retry-isolation reason (fresh tool over the attempt's own `events`; `runWithRetry` surfaces only the last attempt) is in-source and pinned (`drops a verdict reported by an attempt that then failed`).
- **PASSED/FAILED only; FAILED must cite evidence:** both rules in the diff with their reasons; the evidence rule's justification re-verified in source — `from-child.ts:76-79` returns `{ apply: false }` when FAIL dimensions carry no evidence refs, so an unreferenced FAILED verdict really would vanish between the transcript and the gate. CANCELLED refused (parent's fact); one verdict per attempt, refused at the producer with the standing verdict named.
- **`finish` prefers the child's terminal:** behaviour unchanged per input — the previously-dead `collected.some(… TASK_RESULT)` guard is now the live path; the diff touches only its docstring; mutation check 2 (guard flipped to `PROGRESS`) proves the preference pin is not vacuous.
- **No protocol schema change:** `src/protocol/v1.ts` diff-empty; the executor imports two existing types from it.
- **Pin 2 re-derived, pins 1/3/4/5 byte-untouched:** reviewer extracted each of the four other pins' bodies (registration to next `it(`) at base and HEAD — **byte-identical all four**. Pin 2 is stronger than its predecessor: the census regex now also catches runtime-valued kinds (`<runtime>`), the pi message count assertion goes 1→2 with both named, and the behavioural half runs the shipped tool through `validateAgentMessage` into `assessChildObservation`.
- **Reachability disclosure, re-checked in source:** PASSED opens (swept floor 0.750 > `softThreshold` 0.55, `config.ts:13`); FAILED hard-blocks with `deterministic-fail` leading — `gates.ts:20` pushes it first and `mapGateDirective` stamps `codes[0]`. Still-unreachable list confirmed against `prescoreInputFromObservation`/`assessChildObservation`: `escaped: false` hard-coded (`from-child.ts:144`) → no `ownership-escape`; `stalledTurns: 0` (`:166`) → no `repeated-no-progress`; `humanInput: {}` (`:91`) → no `user-reject-stop`/`permission-security-reject`; `openMinors: []` (`:86`) → no `minor-escalated`; the soft floor kills `soft-threshold`. The parent's recorded list is exactly right.
- **C6 re-checked against real reported messages:** on PASSED the `completedChecks` echo (`from-child.ts:162`) keeps the gap closed; on FAILED it fires only with success-claiming prose + non-empty `requiredChecks`, and is never the leading code — pinned with a real reported terminal asserting `["deterministic-fail", "claimed-verification-without-checks"]` and its neutral-prose sibling. R8-4's prose prediction held exactly.
- Worth carrying forward (§6.4): `independentEvidence` is now set from a *self*-report and is still `void`-ed in `computePrescore` — R9-2 flagged the naming hazard rather than deciding it silently, which is correct.

### 4.3 R9-3 — design evaluated, not just verified

No `src/**` (commit touches the test + report only). No fourth `RUN_UNBLOCKED` key anywhere. The additive pin is production-shaped: a FAILED node fans into two branches, both complete, `reopenBlockedFlowchartSnapshot` refuses with the exact message naming `left, right`, and the source snapshot is deep-equal untouched after the refusal.

**The three shapes, evaluated:** (1) a `discardExecuted` field on `RUN_UNBLOCKED` breaks the exact-key freeze, forces every existing refusal pin to be rewritten, and turns one event into a multi-strength authorization whose validator must branch on a flag — rejected correctly. (2) A discard event followed by an ordinary `RUN_UNBLOCKED` creates a two-append crash window where half an authorization survives, needing cross-event pairing/orphan rules for one operator act — rejected correctly. (3) The distinct `RUN_UNBLOCKED_WITH_DISCARD` keeps the ordinary event byte-frozen, gives the stronger authorization its own exact-key schema and replay matching, and reuses the single-append `unappliedUnblock`-style recovery. **The distinct event is the right call — recommend Round 10 sign off the schema** (§6.1), with one payload question decided at sign-off: the design duplicates derivable facts (`chargedEstimatedCostUsd`/`chargedEstimatedDurationMs`) into the authorization record for audit self-containedness, validated against the referenced `MODEL_ROUTED` rows — defensible, but duplication creates a mismatch class that references-only would not have. The design's other decisions are sound: computed set not operator-listed; flag refused when nothing executed; gate-block-only (targetless stalls refused); no budget refund; evidence and events survive while control-state outcome fields clear; restore recomputes and compares, failing closed on a hand-edited list. One implementation obligation the design does not name because it predates R9-1's find: **the discard transform is a fourth flowchart checkpoint writer and must carry `contract` forward** — the exact bug class R9-1 just fixed in the third one.

### 4.4 R9-4 — docs, and the one miss

Docs-only (five files, no ADR opened, no `PROGRESS.md`); ADR-006 Proposed; `0005:110` still names `loadProjectBandit` — flag-only as ordered. The three stale 21:50 UTC notes are replaced; `status-matrix.md:66`'s in-flight framing corrected to `ba0b2ce`; the parent-plane decisions, catalog posture, R8-4's fourth precondition (updated live when R9-2 closed it), the `INSPECT_SUMMARY` freeze, and R9-1's durability are all recorded. The census is honestly timestamped with per-minute attribution of sibling mutation-check transients — the R8-5 pattern executed well. **Two stale-at-HEAD facts:** (a) `m0-m2-architecture.md:737` still says the probe exercises **nine** cases — R9-5's tenth landed in `25a57d9` at 22:32:42, before R9-4's final 22:36:16 census, in a file R9-4 owned and edited; this is a miss, not a race (§5 nit 1). (b) The sync note's "neither is represented as a HEAD commit" (about R9-1/R9-2) went stale when `dff71f1`/`aeb14dc` landed *after* R9-4's commit — that one is the unavoidable docs-race cost, honestly dated, not held against the slot (R8-5 precedent). Both go to the Round 10 docs slot.

### 4.5 R9-5

Case set is 10; the pin lives in the **real** consumer `test/integration/persist/crash-recovery.test.ts` (the brief's `test/integration/m2/crash-recovery.test.ts` does not exist — verified by listing the directory; the slot disclosed the brief error instead of creating a file at the wrong path). The pin diff appends the tenth name after the unchanged nine with a disclosure comment. The window is hit deterministically from outside: the child runs the real, unmodified `unblockFlowchartRun` (imported via `tsImport`, no seams); the 16 MiB reason widens the producer's existing post-append `readAll` enough for the parent to observe the complete newline-terminated row and send an external SIGKILL; the parent asserts the child died by `SIGKILL` (not a normal exit), exactly one complete `RUN_UNBLOCKED` decodes, `checkpoint.json` is byte-identical to the pre-unblock BLOCKED bytes, and no checkpoint temp exists. Recovery: documented manual stale-lock removal, then real `resumeFlowchartRun` — node executes exactly once, one `RUN_UNBLOCKED` in the log, `["RUN_BLOCKED","RUN_COMPLETED"]`, zero anomalies. Reviewer re-ran the probe: `ok: true`, 10×3.

### 4.6 R9-6

`gate-apply.ts` comment-only — verified mechanically: filtering the diff's added lines through a comment-pattern match leaves nothing, and there are zero removed lines. Zero `src` readers of `runStatus` — reviewer's independent word-boundary sweep finds the identifier only in `gate-apply.ts` (declaration, producers, docstrings) and `child-tracking.ts`'s single object-literal *write*. `child-tracking.ts` not edited (diff-empty). The slot's census is a step stronger than R8-1 §11.3: the parent DAG plane discards `runStatus` too (the coordinator reads `directive` only), so the field has zero readers on *both* planes — `applied` and `transitionId` are in the same position. The 8-test pin file is high quality: rewrap-safe by construction (AST comment ranges, whitespace-collapsed substring), proven by reflowing the real file in memory; mutation checks against the real sources for the reader/consumer/dropped-record arms; and pin 8 gives `currentGateStatus`'s matched-unblock-reads-RUNNING contract a unit-level observer where it previously had exactly one integration observer. The ownership deviation (parent sign-off said "at `applyChildThreeLine`", which lives in unowned `child-tracking.ts`) is disclosed, the posture lands on the return type that symbol declares, and the one-line pointer is prescribed for a future owner — the right resolution of an ownership/sign-off conflict.

### 4.7 R9-7 / R9-8 / R9-9 / R9-10

All four: no `src/**` (commit stats: own test file + report only). R9-7's freeze is AST-shaped, not a grep: it requires `DOCTOR_ROUTED_NEXT` to remain a five-entry `Map` of code-key/renderer tuples, reconstructs each wording as head + `${doctor}` + tail with exactly one `doctor` interpolation, and `deepEqual`s the ordered tuples against frozen expected text — which matches the reviewer's independent base-vs-HEAD extraction; the second test deletes the catalog tuple by AST span in memory and proves the freeze throws. R9-8 pins the resume call site (exactly one `childTasksFromLog(read.events, …)` and `childByTaskId = childTaskMap(rebuilt)`) and freezes the R6-2 tripwire by reading its own module source — deleting or renaming the executable tripwire cannot leave only the wiring pin green. R9-9's recursive census is additive over the existing closure sweep (which deliberately excludes store/doctor and so could not express whole-`src` absence); both new pins verified independently at HEAD; the signed-off `because` untouched. R9-10's bidirectional conditional-type equality makes a fourth typed payload key a whole-tree `tsc` failure — the type-level complement to R8-1's runtime exact-key refusals, which are untouched.

## 5. Per-slot notes

- **R9-1** (opus): The unblock contract-drop find is exactly what the census-first discipline is for — a third writer nobody's design named, found by sweeping `materializeCheckpoint` call sites rather than trusting R8-2 §4, fixed and pinned in the same diff. The precedence test's constraint-free-continuation construction (distinguishing `??` from `||` behaviourally) and the vacuous-capture analysis of R8-2's old regex are both careful work. Disclosure 1 (CLI projection and runner default are mutually redundant, each mutation-checked, only source pins notice a single removal) is the honest framing of signed-off defense-in-depth.
- **R9-2** (opus): Measured, not argued — the 360-cell reachability sweep and the 180-cell FAILED sweep convert the disclosure obligation into numbers, and the C6 probe pins a real reported message rather than prose. The five producer rules each carry their downstream justification in-source; the `independentEvidence` hazard flag is the right instinct.
- **R9-3** (gpt-sol): A design that starts from the crash discipline (why one event, not two) rather than the operator surface is the right order. The census (§1) is accurate against the diff, including the telemetry honesty point that forbids an unconditional actual-spend claim.
- **R9-4** (gpt-sol): The minute-by-minute attribution of sibling mutation-check transients (22:33–22:34) is model census work. The one miss (§4.4a) is real but one line.
- **R9-5** (gpt-sol): Widening the producer's own post-append read with a 16 MiB reason — instead of a sentinel, callback, or self-kill — keeps the child production-pure; the "must still be alive after publishing the append" assertion is what makes the external kill provably inside the window. Correct handling of the brief's wrong path.
- **R9-6** (opus): The rewrap-safety construction (and proving it by reflowing the real file) sets the standard for prose pins. The two-plane census upgrade is a finding, not just a verification.
- **R9-7/8/9/10** (gpt-sol): Right-sized freezes, each strictly stronger than a grep would have been (AST tuples, self-source tripwire freeze, recursive module census, type-level key equality). None weakened an existing pin; all additive.

**Nits (the ACCEPT-WITH-NITS basis):**
1. **R9-4 (docs nit):** `docs/specs/m0-m2-architecture.md:737` still reads "exercises nine crash/recovery cases" at HEAD. The ten-case probe was committed (`25a57d9`, 22:32:42) before R9-4's final census (22:36:16) and the line sits in a file this slot edited this round; the end-of-round sync recorded later-landing facts (R9-1/R9-2 working-tree state) but missed this committed one. One line; Round 10 docs slot fixes it.
2. **R9-4 (inherent, not held against the slot):** the status-matrix sync note's "neither is represented as a HEAD commit" was true at commit time and went stale when `dff71f1`/`aeb14dc` landed minutes later — the standing docs-race cost, honestly dated.

## 6. New findings for Round 10

1. **The executed-descendant discard is implementation-ready** — R9-3's design is complete with a consumer census and test plan, the fail-closed refusal is now unit-pinned, and R9-2 just made the gap production-reachable for the first time (a real pi child's FAILED verdict can hard-block a run whose failure already fanned into completed work). Needs the `RUN_UNBLOCKED_WITH_DISCARD` schema sign-off, with one payload question decided (charged estimates in-payload vs references-only, §4.3) and one obligation added (the new checkpoint writer carries `contract`).
2. **Option (a) is unblocked — every precondition is now closed** (producer: R9-2; checkpoint seam: R9-1; unblock: R8-1) — but it is **not** automatically Round 10's schema: the parent has not signed it off, and its one-diff shape (optional `VerificationResult.criteria` + `unmet-acceptance-criterion` on observed FAILED only + durable per-task criteria on the reserved checkpoint seam) contends with candidate 1 on `replay.ts`. Exactly one of the two can be a Round 10 implementation.
3. **Docs staleness at HEAD** (§4.4): the nine-case probe line; the "neither is a HEAD commit" sync note; plus Round 9 decisions not yet in docs (the `runStatus` ledger posture, the discard design decision, the verdict-producer rules).
4. **`independentEvidence` naming hazard** (R9-2 §5): now set from a self-report, still `void`-ed, reads nowhere — a future consumer must not read it as third-party verification. A posture comment or rename is a small decision; it lives in option (a)'s files, so it folds there if option (a) runs.
5. **The last non-CLI leg of contract retention:** R9-1's flip test seeds via the embedder API because no CLI command accepts a contract — but `--track` extracts one and passes it to `startFlowchartRun` (`src/track/loop.ts:157-171`), so a pure-CLI seed→pause→resume retention proof is now possible, test-only.
6. **A structural writer-census pin is cheap insurance:** R9-1's find generalizes — exactly two `materializeCheckpoint` call sites pass a flowchart payload today, and both carry `contract`; a pin counting and checking them makes the discard diff (which adds a third) fail loudly instead of silently dropping the field.
7. **The discard crash window will need a probe case** once candidate 1 lands — same shape as R9-5, next round, not co-scheduled (the probe needs the landed producer).
8. **Brief hygiene:** the Round 8 brief named a nonexistent path (`test/integration/m2/crash-recovery.test.ts`). R9-5 handled it correctly; future briefs should verify consumer paths against the tree.

## 7. Handoff

- Gate GREEN at `8e933a7`: 1880/1879/0/1 (`PI_SMOKE` only), crash-probe 10×3 `ok: true` — both re-verified independently on this VM and matching the parent's record exactly. The +35 closes per-slot (§2).
- The Round 10 injection brief is at `.agent_workspace/ROUND9-BRIEF.md`: ranked mutually exclusive candidates from the verified findings above, the grown frozen-contract list (the durable-contract semantics, the verdict-producer rules, the `runStatus` ledger posture, the ten-case probe, the unit-pinned executed-descendant refusal), and the standing prohibitions (ADR-006 Proposed, no live R1, no Outcome-supported claims).
- Nothing is left red, nothing needs a parent fix-joint. The only open nit is one stale docs line (§5 nit 1), owned by the Round 10 docs slot by construction.
