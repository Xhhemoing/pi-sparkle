# ROUND 6 BRIEF — injection context for Loop 4 · Round 7

Provenance: written by the Round 6 SOTA reviewer (claude-fable-5-thinking) after evidence-based review of R6-1…R6-10 at `48a1a59`; verdicts and reproductions in `.agent_workspace/loop4-r6-review.md` (7 ACCEPT, 3 ACCEPT-WITH-NITS, 0 ROLLBACK). Every candidate below cites evidence the reviewer verified or reproduced at HEAD, not report hearsay. Nothing is in flight: all ten slots plus the three parent joints (`773b98f`-internal ADR revert, `8e87b8d`, `48a1a59`) are committed. **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 6 (all committed, `6fb7d9d`…`48a1a59`)

| Slot | Landed | Key symbols (do not re-implement) |
|---|---|---|
| R6-1 | The flowchart loop respects the tracking gate (option (a), parent-signed): all three terminal recorders refuse when the log already replays a terminal, so a clustered child returning `SUCCESS` + `verification: FAILED` ends the run **BLOCKED** (`ANALYSIS_QUEUED`, evidence named, episode WAITING, checkpoint BLOCKED, `anomalies: []`, injectable, resumable). One definition of "terminal" exported from replay and consumed by writers and the anomaly rule alike. Reviewer re-reproduced end to end | `TERMINAL_REPLAY_STATUSES` / `replayedTerminalStatus` in `run/replay.ts`; `alreadyTerminal` (private) in `flowchart-run.ts`; 6 pins in `test/unit/run/gate-outcome.test.ts` incl. the shared-terminal-set source pin |
| R6-2 | Decision, test-only: **no reachable false-accept** from resume re-specification, because the gate directive is a pure function of `verification.kind` — `check-coverage` can never FAIL (`coverageOutcome` has no FAIL in its range; `completedChecks` is a copy of `requiredChecks`), verified by the reviewer in source. The re-specification is real (criteria/artifacts emptied, limits widened up to 12× wall, role coarsened tester→implementer, cascade+model dropped, run `contract` dropped) and pinned; reconstruction-from-log is fully designed with 0/9 field mismatches and needs no schema | 3 pins in `flowchart-run-abort.test.ts` (rebuilt-spec exact shape; gate-verdict invariance; **FAIL-unreachable tripwire** whose docstring orders the fix), 2 in `m2.5/resume.test.ts` (spec+contract loss; reconstruction premise incl. the two-producer `MODEL_ROUTED` trap — select on `agentRole !== undefined`) |
| R6-3 | The last lifecycle-lock corners closed: the track loop's clarification run holds `withRunLifecycleLock` for its whole record-writing body (discovery hoisted; the nested questions-write lock **deleted** — the helper is not reentrant; `TrackRunInput.runLock` seam added); `startSupervisedRun` pre-flight hoisted so a refused supervised start persists nothing (M0 pin mirrored); pre-rounds deaths (reachable from a plain empty task list) now get the rounds' terminal-then-settle pair, and resume checks terminal before state reconstruction (disclosed; only terminal graph-free logs observable). Nonexistent-run-resume posture pinned once for both resume planes: empty `runtime/runs/`, nothing else | `recordClarificationRun` in `track/loop.ts`; `startLockedSupervisedRun`, `open`-taking `runAndSettleSupervisedRun` in `supervisor.ts`; `test/unit/track/clarification-lifecycle-lock.test.ts` (5), `test/integration/m2/supervised-lifecycle-lock.test.ts` (7), +3 in `supervisor-crash.test.ts` |
| R6-4 | Doctor gained frozen-additive `learnedState`: every discovered project-key `bandit.json`, plus `preferences.json` and `catalog-observed.json`; per-entry `{ kind, stateClass: learned|derived, projectKey, path, status: present|absent|readable|damaged, remediation }`; reads only through the shipped readers, catches only the three typed errors as damaged, fails `learned-state-inventory` only on scan errors. **Parent joint `8e87b8d`**: doctor's `loadProjectBandit` call is a signed-off read-only diagnostic exception in the isolation pin, and the pin requires the call to stay | `learnedStateInventory` / `DoctorLearnedStateEntry` in `cli/doctor.ts`; the amended reader census + `assert.match` in `test/unit/routing/live-isolation.test.ts` |
| R6-5 | Three adaptation-plane routes: `BANDIT_STATE_UNREADABLE` / `PREFERENCE_SNAPSHOT_UNREADABLE` / `CATALOG_OBSERVED_CORRUPT` end with a `next:` naming doctor's `learnedState[]` with the learned/derived wording split (derived never says "relearn" — pinned). `delete --run|--episode --lock-wait-ms <ms>`: absent flag passes `{}` (byte-identical old call), `0` refuses immediately, `/^\d+$/` only, 24 h ceiling. **`pause` deliberately not parameterised** (a longer pause wait can only succeed against an already-stopped run — reasoning recorded in the slot report §3) | 3 new `DOCTOR_ROUTED_NEXT` entries + `lockWaitOptions`/`MAX_LOCK_WAIT_MS` in `cli/main.ts`; `command-error-doctor.test.ts` 6→9 (incl. `pref list` end-to-end); `delete.test.ts` 12→16 |
| R6-6 | Cascade proven at the wire, offline: a two-tier child whose first attempt fails deterministic verification drives production `ChildCoordinator` retry, and the loopback server's **second received request body** carries the next tier's model (`loopback-2`); `TASK_RETRY` hop and exact count 2 pinned. The test decorator supplies only the verification verdict — tier choice and HTTP are production code. `test/helpers/process-death.ts` extracted with the owner-token safety argument stated once | `CascadeVerificationExecutor` (test-local); `simulateProcessDeath` in `test/helpers/process-death.ts`; loopback helper now takes `modelIds` list and echoes the requested model |
| R6-7 | Docs truth-up for Round 5 (role-level mailbox skip, four lock postures, crash-terminal settle, `BANDIT_STATE_UNREADABLE` operator split, doctor routing, typed checkpoint read, wire witness), with correct timestamped disclosure of in-flight R6-1/2/3. **Parent joint (inside `773b98f`): the slot's ADR-006 Accepted flip was stripped before commit. ADR-006 is Proposed at HEAD and every doc reference agrees — the slot report's contrary claims describe a diff that never landed.** Now stale at HEAD for R6-1/2/3/4/5's landings — see R7-6 | `docs/data-dictionary.md`, `docs/status-matrix.md`, `docs/specs/m0-m2-architecture.md`, `docs/how-to-adapt-to-pi.md` |
| R6-8 | One probe case: `sigkill-run-lock-operator-recovery` — a child running real `startRun` self-SIGKILLs mid-executor; the lock file's recorded PID is proven dead; `deleteRunRecords` refuses with `LOCK_TIMEOUT` against a byte-compared directory snapshot; doctor reports `pidLiveness: "not-running"` + manual-removal remediation; after `rm` the delete completes verified. **Parent joint `48a1a59`**: the reduced-probe pin in `crash-recovery.test.ts` now expects the 9-case list, timeouts widened (the slot had not censused the probe's consumers) | the `run-lifecycle` child mode + case in `scripts/crash-probe.mjs` |
| R6-9 | Census: 1,428 direct exports, 155 genuine no-production-caller at baseline; **three deleted with absence pins** (`episode/closure.ts::closeEpisode` duplicate, `DispatchPreflightError`, `compilableChildFrom`); two became live via R6-4 (`loadProjectBandit`, `loadCatalogObservedSnapshot`); the remaining 150 dispositioned individually (adaptive/offline, barrel, contract, documented, harness, tombstone classes) in the slot report | absence pins in the three owned unit test files; the disposition table in `.agent_workspace/loop4-r6-t9.md` |
| R6-10 | Flowchart-plane dead-letter reachability: real `startFlowchartRun` (`cluster: true`), five chained scouts, `deadLettered: 1`, the byte-identical operator line. Countdown semantics pinned: tallies `[1, 2, 3]` monotone, drop on the next claim, `requeueCount() === 0` after (pending-only accessor), the dead-letter record preserving `requeues: 3` | additive tests in `undelivered-mail.test.ts` and `mailbox.test.ts` |

## 2. Current baseline (this VM, Node v22.14.0)

- **`pnpm gate` GREEN, exit 0**: **1769 tests / 1768 pass / 0 fail / 1 skipped**. The only skip is the `PI_SMOKE=1` real-provider gate. Any slot whose report shows a second skip introduced one and must say so. Delta vs Round 5: +44 tests.
- `node scripts/crash-probe.mjs` → `ok: true`, **9 cases × 3 iterations** (grew by one: `sigkill-run-lock-operator-recovery`). `crash-recovery.test.ts`'s reduced-probe pin lists all nine by name — anyone changing the case set owns updating that pin **in the same slot, with disclosure** (the R6-8 lesson).
- Perf: no fresh whole-tree bench needed. Round 6 added no per-run acquisitions (R6-3's structural argument: the clarification run swaps one acquisition for one; the hoist removes one from every refused start). Any Round 7 perf claim records its **own** same-VM before/after with an unchanged-arm control; ≥5 % end-to-end regression or rollback.

## 3. Forbidden / frozen for Round 7

Global forbidden list, unchanged and now twice-tested: live R1/bandit/topology on the execution path (the doctor `learnedState` read is the **only** exception, signed off, read-only, pinned — do not widen it); Outcome-supported claims; **ADR-006 stays Proposed** (a Round 6 slot flipped it and the parent reverted — re-read this list before touching any decision status); auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit; no `git checkout` of other branches.

Frozen contracts (Rounds 1–5 set, all held through Round 6, plus new):
- Rounds 1–5 carry-overs: jsonl signatures; `writeFileAtomic`(+Sync), no private temp+rename copies may be added; `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE` (code-discrimination only); `episodeLockPath` symmetry; `runCommand` sink wiring + resume-site pin; `deleteRunRecords` lock-plus-double-verify + `RUN_RECORDS_SURVIVED` (pinned substrings, not wording); the two decision pins that `append`/`checkpoint write` do not take the run lock; `recordCrashTerminal` in-flight-only + best-effort in one module, two entry points, both guards; `applyRetry` sole BLOCKED→READY producer at the scheduler; `preserveResumableState`; `ClusterMailReport` + the undelivered-mail line (role-level skip semantics, countdown bound); resume flags + the four disclosure wordings; `writeFeedbackTombstones`; `CATALOG_OBSERVED_CORRUPT`/`PREFERENCE_SNAPSHOT_UNREADABLE`/`BANDIT_STATE_UNREADABLE` + bind-after-load; doctor's frozen-additive JSON; feedback fuzz hard-fails; `withRunLifecycleLock` rules (pre-flight outside; not reentrant; pause-on-live and SIGKILL postures); supervised settle-on-crash + `afterTerminal === ["EPISODE_CLOSED"]`; `commandFailureNext` code-discrimination; `CheckpointStore.read`'s typed error; loopback stderr pins.
- **New from Round 6:**
  - **One definition of run-terminal**: `TERMINAL_REPLAY_STATUSES`/`replayedTerminalStatus` are what the three flowchart recorders and the anomaly rule share; neither side may re-derive "terminal" locally (source-pinned). The decided log shape for a verification-failed clustered child (BLOCKED, `ANALYSIS_QUEUED`, episode WAITING, exactly one terminal, `anomalies: []`) is pinned end to end. Consequence to respect: **BLOCKED is absorbing** — no writer may append any terminal over a BLOCKED log; only an explicit unblock design (R7-3, sign-off required) may change that.
  - The clarification run is a locked lifecycle: discovery outside, records inside, questions write covered by the run's acquisition (source pin forbids `runLockPath`/`withExclusiveFileLock` in `track/loop.ts`). Supervised pre-flight outside the lock; `supervisor.ts` never rebuilds `runLockPath` (source pin). The pre-rounds window gets terminal-then-settle; resume's terminal check precedes reconstruction; a refused **start** persists nothing on every plane, a refused **resume** leaves an empty `runtime/runs/` only — both pinned.
  - Doctor `learnedState` shape (`entries[].kind/stateClass/projectKey/path/status/remediation`, `advisory`, `scanErrors`) is frozen-additive **and now consumed by `command-error-doctor.test.ts`** — a rename breaks two suites. The isolation pin requires doctor to keep calling `loadProjectBandit`; changing what doctor calls means amending a signed-off guard (parent sign-off, explicit `because`).
  - The five `DOCTOR_ROUTED_NEXT` routes and `GENERIC_FAILURE_NEXT` character-exact; new routes still require an answering doctor surface and code discrimination.
  - `--lock-wait-ms` semantics: absent ⇒ `{}` (the 5 s default stays owned by the lock alone — pinned byte-identical), strict decimal, ceiling; `pause` stays unparameterised deliberately — reopening starts with the posture, not the flag.
  - `test/helpers/process-death.ts` is the shared abandon-a-live-handle remedy; new lifecycle-adjacent suites consume it rather than inlining a fourth copy.
  - R6-2's four pins are load-bearing tripwires: the rebuilt-spec shape pin **must** be updated (with disclosure) by whoever ships reconstruction; the FAIL-unreachable tripwire **must** fail and be rewritten by whoever makes check-coverage real — its docstring orders "fix `childTasksFromDefinition` in the same diff".
  - R6-9's three absence pins: reintroducing a deleted export without a live caller turns a test red.
- Saturated — do not spend a slot: everything in the Round 5 saturated list, plus: lifecycle-lock completeness (R6-3 emptied it — every CLI-reachable embedder is locked and both resume planes' posture is pinned); the SIGKILL-run-lock cross-process story (R6-8 + the parent pin); flowchart-plane dead-letter reachability and countdown semantics (R6-10 emptied the last cluster candidate); adaptation-plane doctor **surface construction** (R6-4 built it, R6-5 routed it — only the R7-8 cleanup remains); delete's bounded wait (closed; pause deliberately open-by-posture); resume-time adoption of unaccepted results (R5-5's NO and flip conditions stand).

Process requirements per slot (carried forward — they worked): census before trusting this brief; scoped `eslint` on owned files + whole-tree `tsc --noEmit` before reporting; attribute shared-tree transients to files (R6-5's detached-worktree repro is the gold standard), never edit unowned files to "fix" them; timing-sensitive owned tests 3×; full gate is the parent's job; no scratch files at report time; on a shared tree, a "precondition did not land" verdict is only true as of the minute you took it — re-check before reporting (R6-5's lesson). **New this round, from the two caught-by-others breaks:** (1) *census your consumers* — a slot that changes any artifact other tests execute or pin (the crash probe's case set, a helper's semantics, a fixture's meaning) owns censusing `test/**` for its consumers and shipping or prescribing the update in its own report (R6-8 missed `crash-recovery.test.ts`; R5-2 remains the model); (2) *a slot adding an import to any file inside the live closure runs `live-isolation.test.ts` before reporting* (R6-4 left it red for a sibling to find); (3) *docs slots re-read the forbidden list before changing any ADR/status line, and treat "the task told me to" as a reason to stop and flag, not comply* (R6-7's ADR flip).

## 4. Ranked Round 7 candidates (mutually exclusive ownership)

The round's tilt: Round 6 proved the tree's acceptance criteria are decorative — no criterion can fail a child anywhere — and left a fully-specified, no-schema fix for the resume re-specification that must land **before or with** any change that makes criteria real. Those two, in that order, are the spine of Round 7. Behind them: the parent plane's crash path still writes the double-terminal the flowchart plane just stopped writing, BLOCKED runs need a way out and a voice in the CLI, and the doctor exception should get narrower.

File-contention note: `src/run/flowchart-run.ts` belongs to **R7-1 alone**; `src/tracking/` (prescore/from-child) to **R7-2 alone**; `src/run/coordinator.ts` to **R7-4 alone**; `src/cli/main.ts` to **R7-5 alone**; `src/run/supervisor.ts` to **R7-7 alone**; `src/cli/doctor.ts` + `src/learning/bandit-store.ts` + `src/preferences/store.ts` to **R7-8**; `src/graph/validate.ts` to **R7-9**. R7-3 is investigation-only (no `src/**`) precisely because its natural files are owned by R7-1/R7-5.

### R7-1 (P1, resume honesty fix) — Reconstruct child specs from the parent log
**Evidence (R6-2, premises reviewer-verified):** `childTasksFromDefinition` (`flowchart-run.ts:231`) synthesizes every resumed spec (empty criteria/artifacts, hard-coded limits up to 12× the caller's wall budget, no model/cascade, role coarsened) and `resumeFlowchartRun` drops the run `contract`; every `ChildTaskInput` field is already durable on the log (0/9 mismatches by construction; pinned in `m2.5/resume.test.ts`).
**Change:** implement R6-2 §4 as designed: `childTasksFromLog(events, definition, registry, catalog)` at the single call site (`:1094`), definition as node set, log as spec source; **select the `MODEL_ROUTED` with `agentRole !== undefined`** (two producers write that event for one task — the pinned trap); explicit fallback for never-ran nodes (not the silent hard-coded trio); thread `contract` into the resume context **in the same diff**; reuse the existing `readAll`. Update R6-2's rebuilt-spec-shape pin with disclosure (it exists to be consciously rewritten, not deleted). Disclose the behavior change that resumed runs begin honouring *tighter* caller limits.
**Ownership:** `src/run/flowchart-run.ts` (sole), `test/integration/m2.5/resume.test.ts`, `test/unit/run/flowchart-run-abort.test.ts`.

### R7-2 (P1, gate-semantics decision) — Acceptance criteria that can actually fail, or an owned decision that they cannot
**Evidence (reviewer-verified in source):** `coverageOutcome` (`prescore.ts:121-125`) has no `FAIL` in its range; `from-child.ts:141/143` feed both criteria-shaped dimensions copies of their own inputs, so the gate directive is a pure function of `verification.kind` on every path. Nothing anywhere compares a criterion against what a child did.
**Change:** decision-first with parent sign-off — the gate's semantics are frozen-adjacent (Round 6 forbade touching `gate-apply.ts`; this is one layer below it and changes every plane's gating). Either (a) make check-coverage real: derive `completedChecks` from evidence the child actually reports rather than copying `requiredChecks`, priced across all planes with the measurement discipline R6-2 modeled; or (b) record in-source, on the dimension, that criteria are prompt-guidance only and the deterministic verifier is the sole gate, and truth the docs. **Sequencing constraint (hard):** if (a), R7-1 must land first or in the same round-half — a real check over resumed empty-criteria specs is exactly the false-accept vector R6-2's tripwire guards; that tripwire will fail and its docstring says what to do. Do not take (a) without reproducing R6-2's sweep result under the new semantics.
**Ownership:** `src/tracking/prescore.ts`, `src/tracking/from-child.ts` (sole), `test/unit/tracking/`. Coordinate with R7-1 through the ownership table only.

### R7-3 (P2, schema investigation) — A way out of BLOCKED
**Evidence (reviewer-verified):** after R6-1, all three recorders refuse over a BLOCKED log — including `persistCompleted` — and no event clears `RUN_BLOCKED` in replay; `applyRetry` is scheduler-level. A BLOCKED run is injectable and resumable but replays BLOCKED forever; "resumable after an unblock" has no unblock. This is now the only gap between R6-1's contract and an operable one.
**Change:** investigation + design, **no `src/**`** (the natural files belong to R7-1/R7-5 this round; R5-5/R6-2 pattern): specify the unblock event (schema — needs explicit parent sign-off under the standing no-new-persisted-schema posture), its producer surface (operator command vs injection extension), how replay clears `sawTerminal`, how resume re-drives the unblocked node, and what `applyRetry`'s sole-producer pin means afterwards. Pin the *current* absorbing behavior precisely so it cannot drift while undecided. Deliver the decided contract + design for Round 8.
**Ownership:** additive pins in `test/unit/run/replay.test.ts` region owned by agreement with no `src` edits; the report.

### R7-4 (P2, crash-path coherence) — The parent plane's unconditional crash `RUN_FAILED`
**Evidence (reviewer-verified, `coordinator.ts:618-621`):** the catch-all appends `RUN_FAILED` with no in-flight check and no replayed-terminal refusal — the exact double-terminal R6-1 removed from the flowchart plane, still live on the parent plane's crash path (a crash after a gate BLOCKED buries the operator-actionable state).
**Change:** route the catch through `recordCrashTerminal` (which already refuses over BLOCKED) or apply the `replayedTerminalStatus` refusal; keep the reason string and the settle ordering; pin the BLOCKED-then-crash shape on this plane the way `gate-outcome.test.ts` pinned it on the flowchart plane; negative control that an ordinary crash still records `RUN_FAILED`.
**Ownership:** `src/run/coordinator.ts` (sole), `test/unit/run/` additive.

### R7-5 (P2/P3, operator surface) — A BLOCKED run deserves a `next:`
**Evidence (reviewer-verified, `main.ts:911`):** `run --children` prints `reason:`/`next:` only for FAILED; a BLOCKED run (post-R6-1 the production-ordinary verification-failed shape) exits 1 with the node status visible but no routed next step. R6-1 censused: no test pins the lost wording.
**Change:** a BLOCKED-specific block naming the queued analysis, the required evidence (both already on `RUN_BLOCKED.payload`), and the operator's actual options today (`inspect`, `injectFlowchartRun`-backed injection, resume — honestly stating resume repeats the block until R7-3 ships). Keep `flowchartExitCode` and every existing pinned string intact; sink-wiring and resume-disclosure pins stay green.
**Ownership:** `src/cli/main.ts` (sole), `test/integration/cli/` additive.

### R7-6 (P3, docs) — Truth-up for Round 6's landings
**Evidence (reviewer-verified stale at HEAD):** the docs record Round 5 + the R6-1/2/3 in-flight disclosure; undocumented at HEAD: the BLOCKED-beats-FAILED terminal contract (R6-1 §8 wrote the exact data-dictionary wording), the clarification run's lock, the refused-start/refused-resume postures, the pre-rounds settle, doctor `learnedState`, the three routes, `delete --lock-wait-ms` (R6-5 handed the runbook wording) and the deliberate pause posture, the cascade wire witness, probe case 9.
**Change:** doc-only truth-up; timestamped end-of-round sync for in-flight R7 slots; **do not touch any ADR status line** (see §3 process rule 3).
**Ownership:** `docs/**`.

### R7-7 (P3, dead-branch decision) — `settleSupervisedOutcome.trackingAssessment`
**Evidence (R6-9 census, reviewer re-verified):** the parameter's sole caller passes none (`supervisor.ts:561`), the branch early-returns (`:529`), no test calls it, and R6-1's census adds: the supervised plane has no gate producer at all, so the branch is unreachable by the tree's design, not "not yet wired".
**Change:** census-and-delete with an absence pin (R3-8/R5-10/R6-9 pattern), or wire a real supervised gate producer with parent sign-off — the evidence favors delete; wiring is a feature decision nobody has asked for. Small slot; do not grow it.
**Ownership:** `src/run/supervisor.ts` (sole), `test/integration/m2/` or `test/unit/run/` additive.

### R7-8 (P3, seam hygiene) — Narrow the doctor exception
**Evidence (reviewer-verified):** doctor reaches keyed bandit state through a base-31 hash-preimage because the shipped reader is root-keyed; the preferences probe binds and then **unbinds** the process-global store (R6-5's test already had to repair around it); the isolation pin hard-codes `loadProjectBandit` as the symbol doctor must call.
**Change:** export a keyed read path from `bandit-store.ts` (or make the reader accept a key) and a pure preference reader from `preferences/store.ts`; consume both from doctor, deleting the preimage and the bind/unbind; update the isolation pin's `assert.match` in the same diff **with parent sign-off** (it amends a signed-off guard — state the `because`). JSON contract byte-identical (frozen-additive; two suites consume the field names).
**Ownership:** `src/cli/doctor.ts`, `src/learning/bandit-store.ts`, `src/preferences/store.ts`, `test/unit/cli/doctor.test.ts`, `test/unit/routing/live-isolation.test.ts` (the one pin, sign-off required).

### R7-9 (P3, validator decision) — `validateTaskGraph([])` accepts what event validation refuses
**Evidence (R6-3 §2/§6, reviewer-verified mechanism):** an empty task list passes graph validation and then dies at the `TASK_GRAPH_ACCEPTED` append (`payload.tasks must be a non-empty array`) — now settled honestly by R6-3's widening, but the refusal happens one layer late: an obviously-invalid input burns a run id, a bound episode, and a FAILED run record instead of a validation error.
**Change:** decide tighten-vs-keep. If tightened (reject empty at `validateTaskGraph`), the pre-rounds crash pin **must be re-seeded on the episode-store variant in the same diff** (R6-3 disclosure 4 says exactly this — the window still exists, reached differently); pin the new refusal as persist-nothing (the pre-flight is already outside the lock).
**Ownership:** `src/graph/validate.ts` (sole), `test/unit/graph/`, the one pin swap in `test/integration/m2/supervisor-crash.test.ts` (coordinate: that file is otherwise unowned this round).

### R7-10 (P3, verification infrastructure) — End-to-end producers for the two constructed-error routes
**Evidence (reviewer-verified):** of R6-5's three routes, only `PREFERENCE_SNAPSHOT_UNREADABLE` is witnessed through a real command end to end (`pref list` over a truncated snapshot); bandit and catalog are constructed-error cases. Unlike R5-9's `RUN_RECORDS_SURVIVED` (no portable producer), real producers exist: the `adapt` paths propagate `BanditStateUnreadableError` at `stage: "validation"`, and catalog loads throw `CATALOG_OBSERVED_CORRUPT` on damaged bytes.
**Change:** test-only, additive: drive a real command over a damaged `bandit.json` and a damaged `catalog-observed.json`, assert exit 1, the exact routed `next:`, and that the routed doctor command's `learnedState.entries` really carries that path with the plane-correct `stateClass`/`remediation` (the R6-5 test-2 pattern, twice more). If either producer turns out unreachable without `src` changes, stop and report per the R5-8/R5-9 precedent.
**Ownership:** `test/integration/cli/command-error-doctor.test.ts` (additive), `test/unit/learning/` fixtures if needed.

## 5. Explicitly NOT for Round 7 (unchanged or newly settled)

Live R1/bandit/topology (standing; the doctor read-only exception is the ceiling, not a door); jsonl/lock/append perf and per-step writer locking (saturated; decision pins are the record); protocol/v1 parse and event fuzz (saturated); mailbox TTL/durability/drain-ack/sweep and any dead-letter work (R6-10 closed the last open pin — the cluster plane is saturated); lock stealing in any form (no-steal is deliberate, probed cross-process, doctor-guided); resume-time adoption of unaccepted child results (R5-5's NO; only its flip conditions reopen it); `maxCostUsd` enforcement (still blocked on a real pricing source); `TASK_LEASE_EXPIRED` rename (persisted schema); executor-config persistence in the event log (R4-6's refusal stands — note R7-1 needs no such thing: child specs are already on the log); a non-terminal `RUN_CRASHED` marker (R4-4 decided against); same-role late mailbox delivery (R5-6 traded it away); rewriting append-only run logs for privacy (disclosure + `delete --run` is the contract); a `pause --lock-wait-ms` (R6-5 decided the posture — reopen the posture, not the flag); ADR-006 status changes (frozen forever; a Round 6 slot tried, the parent reverted, and the process rule in §3 exists because of it).
