[Model: claude-fable-5-thinking-xhigh]

# Loop 4 · Round 18 — SOTA review at `0c948ba`

Reviewer ran independently on this VM (Node v22.14.0, engines `>=22.19.0` warning only), on `cursor/opt-r18-postmerge-42b1`, working tree clean at review start and at report time. HEAD advanced from the dispatch's `a5535fb` to `0c948ba` mid-review; both are `.agent_workspace/PROGRESS.md`-only (verified with `git show --name-status`), so the code tree under review is exactly `4412fac`'s. Every verdict below is against the actual range `101e4bb..HEAD`: two landings (`daea498` R18-2, `4412fac` R18-1) plus two orchestrator commits (`a5535fb`, `0c948ba`). Commit chronology (UTC 2026-08-25): **`daea498` 05:37:55 R18-2 → `4412fac` 05:43:25 R18-1** → `a5535fb` 05:45:16 → `0c948ba` 05:46:13 — note R18-2 landed *before* R18-1, the reverse of slot numbering; both slot reports disclosed the shared-tree overlap correctly and their file sets are disjoint. The range's `src`/`test`/`docs`/`scripts` diff is exactly five files (`src/pi-adapter/pi-executor.ts`, `test/integration/pi-adapter/steer-retry.test.ts` new, `src/cli/main.ts`, `test/integration/m1/cli-children.test.ts`, `docs/specs/m0-m2-architecture.md`), so everything else in those trees is diff-empty across the range — verified explicitly (0 diff lines each) for `src/run/coordinator.ts`, `src/run/events.ts`, `scripts/crash-probe.mjs`, `package.json`, `src/run/child-coordinator.ts`, `src/protocol/v1.ts`, `src/domain/limits.ts`, `src/pi-adapter/kernel.ts`, `src/pi-adapter/cost-gate.ts`, `src/execution/contract.ts`, both privacy guard tests, and the three pre-existing steer suites. No file outside `.agent_workspace/` was changed by this review; all mutation runs happened in a full `git archive HEAD` copy under `/tmp/r18-rev/tree` with `node_modules` symlinked, deleted afterwards and verified gone. No scratch remains at report time; the leaked suite state roots under `/tmp/pi-sparkle-*` (see §5) were removed as well.

## 1. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R18-1 (`4412fac`) | **ACCEPT** | Direction (a) exactly per sign-off: `acceptedSteers` opens and closes with `runWithRetry` (identity-guarded `finally`), per-attempt snapshot `[...steers]` before the attempt starts, replay latched to the attempt's first `TURN_FINISHED` after `gate.recordTurn`, record-after-accept in `steerText`; zero new event types (`coordinator.ts`/`events.ts` 0 diff lines); all 10 pre-existing steer pins plus the 2 new pins green 3×; reviewer's own three out-of-tree mutants each killed 2/2 — M1 reproduces the audit hole verbatim, M2 fails placement and double-apply, M3 fails outcome (§4.1) |
| R18-2 (`daea498`) | **ACCEPT** | `parseChildCostCeiling` copies a positive finite `maxCostUsd`, refuses any other non-`undefined` value with `DomainValidationError` naming the task, absent stays absent; carriage proven on disk (`TASK_REQUEST.limits` + child `RUN_CREATED.limits` both 0.25) and refusal writes no run (`runtime/runs` empty), both re-run by this review; `main.ts` byte-identical outside the parse region (the whole range diff of the file is the one new function + one call + one conditional spread); spec lines rewritten to match the `ChildRunLimits` disclosure word-for-substance; reviewer's own two mutants each redden exactly one new pin and nothing else (§4.2) |
| Parent | Landing hygiene clean: each landing = its slot files + its own `loop4-r18-tN.md`, nothing else, zero PROGRESS ticks (verified per-commit); file sets disjoint; the parent's recorded gate and probe numbers match this review's independent runs exactly; the (a) sign-off was executed without a new event type, route, or `RunStatus` contact — nothing to relitigate |
| Joints | None. Neither landing touched a file outside its granted set; no privacy-allowlist edit was needed (neither diff adds or removes a `src` import — verified in both diffs), exactly as the dispatch predicted |

**2 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK.** Zero red-tree commit points, structurally: the landings share no files; at `daea498` the tree was HEAD's minus R18-1's two files (the pre-fix `pi-executor.ts` passed every suite that existed there — the audit's gate at `7d8b7a3` proves it — and `steer-retry.test.ts` did not exist yet); at `4412fac` the tree equals HEAD's code, which this review's gate proves green.

## 2. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0: 2042 tests / 2041 pass / 0 fail / 0 cancelled / 1 skipped, 120 suites** — matches the parent's recorded numbers exactly. All four gate steps ran (typecheck, lint, test, build). Exactly one `# SKIP` line in the TAP output (`grep -c` = 1): `ok 296 - PiAgentExecutor completes a run against a real provider # SKIP` — the standing `PI_SMOKE` real-provider gate.
- **`node scripts/crash-probe.mjs` → `ok: true`, 11 cases × 3 iterations, exit 0** — full JSON parsed and compared name-by-name against the Round 16/17/18-audit record: all eleven names and their order unchanged (`jsonl-truncated-tail`, `checkpoint-old-then-next-write`, `stale-lock-no-steal`, `sigkill-run-lock-operator-recovery`, `feedback-cascade-strip-before-tombstone`, `feedback-rewrite-kill-before-rename`, `invocation-rewrite-kill-before-rename`, `episode-settle-stale-lock-recovery`, `atomic-write-stale-unique-temp`, `unblock-append-before-checkpoint-sigkill`, `unblock-discard-append-before-checkpoint-sigkill` last). **No 12th case**; `scripts/crash-probe.mjs` diff-empty across the range.
- **Test delta vs the post-merge audit baseline 2038 (+4 tests, +0 suites) decomposed exactly:** R18-2 +2 (gate TAP `ok 161` the carriage pin, `ok 162` the refusal pin), R18-1 +2 (`ok 299` the single-retry pin, `ok 300` the double-retry pin). 2038 + 4 = 2042. ✓
- **Owned suites re-run at HEAD by this review, 3× (timing-sensitive — blocked tools, retries):** `steer-retry.test.ts` + `m0/steer.test.ts` + `steer-blocked-tool.test.ts` + `steer-inflight.test.ts` together = **12/12 pass, three identical runs** — the 10 pre-existing steer pins green untouched plus the 2 new. `cli-children.test.ts` 10/10 in the out-of-tree control (and in the gate).
- **Both privacy guards green standalone: 11/11** — including the stale-entry asserts in both directions, so green is bidirectional proof the allowlists equal the real import graph. Consistent with neither diff touching an import.
- **Commit hygiene:** verified per-commit (§ header). `daea498` = `main.ts` + `cli-children.test.ts` + the spec doc + `loop4-r18-t2.md`; `4412fac` = `pi-executor.ts` + `steer-retry.test.ts` (new) + `loop4-r18-t1.md`; zero PROGRESS ticks in either.

## 3. Freeze check

Everything outside the five touched files is byte-identical to `101e4bb`, whose code tree the Round 18 post-merge audit verified in full — so every frozen contract holds structurally. The requested spot-checks were still read directly at HEAD:

- **ADR-006 Proposed** (`0006-pi-extension-reverse-adapter.md:5` read directly); `docs/decisions/` diff-empty.
- **`RunStatus` exactly eight members** (`domain/status.ts:1-12`, read directly).
- **`independentEvidence` exactly one `void`** (`prescore.ts:89`); only the `tracking/from-child.ts:228` writer plus `prescore.ts` mention it.
- **Thinking stays bytes-only outside `src/pi-adapter/**`:** whole-`src` sweep — the only non-adapter mentions are the `bytes: number` payload in `execution/contract.ts:30`, the `thinking delta (N bytes)` summaries in `coordinator.ts:384-385` and `child-coordinator.ts:771-780`, and the `events.ts:362` docstring forbidding thinking-derived text in `STEER_INJECTED`. R18-1's replay reads only caller-supplied steer strings; `translatePiEvent` unchanged.
- **Bandit isolation:** `loadProjectBanditByKey` exactly `learning/bandit-store.ts` (definition) + `cli/doctor.ts`; `selectArm` exactly `routing/bandit.ts` (definition) + `routing/shadow.ts`.
- **Three cap-forward paths honest, absent stays absent:** `coordinator.ts:375` (root execution request) and `:705` (ChildCoordinator dep), `supervisor.ts:384` — all conditional spreads; `child-coordinator.ts` `costCapFor` diff-empty; and now `main.ts:443` (`parseChildSpec`) the same conditional-spread shape. No path invents a cap.
- **Five doctor routes and `INSPECT_SUMMARY` four keys:** the range diff of `main.ts` is confined to the parse region, so both surfaces are byte-identical to the audited merge tree; direct reads confirm the five route codes wired at `main.ts:2067-2077` and the four frozen-additive keys (`type`, `runId`, `status`, `requiredEvidence`) in `run/inspection.ts:56-59` with the frozen-contract comment at `main.ts:1184`.
- **Preference lock intact:** `preferenceSnapshotLockPath(stateRoot)` still held in `main.ts:1697`; `onRunStarted` and `runCommand` inside the untouched region.
- **Kernel-reuse contracts untouched:** `kernel.ts`, `cost-gate.ts`, `contract.ts` diff-empty; the replay emits no `ExecutionEvent` and rides the existing subscription, so `streamPrefixOpen`/`streamedCount`/per-attempt verdict buffering (`77e5d42`) are byte-identical; `gate.recordTurn` still runs first on every `TURN_FINISHED` and the replay never calls the latching `requestStopIfExceeded`.
- **No live R1 on the execution path, no Outcome-supported claims, no auto-promote, no dependency edits, no history rewrites, no 12th probe case, no fourth `RUN_UNBLOCKED` key, no new `RunStatus`** — structurally excluded by the five-file range diff plus the spot-reads above.

## 4. Per-slot verification

### 4.1 R18-1 (`4412fac`) — re-deliver accepted steers into retry kernels, direction (a)

Everything the sign-off specified, verified at source, not from the report:

- **Lifetime is the execution.** `runWithRetry` creates the array and registers it under `request.agentInstanceId` before the attempt loop (`pi-executor.ts:655-656`) and deletes it in a `finally` guarded by array identity (`:688-690`), the same defensive shape `liveKernels` uses (`:612-614`); nothing is cleared on attempt success, so a third attempt still replays what attempt 1 was told. Because `runWithRetry` is the whole post-resolution body of `execute()`, the log's scope is exactly the execution.
- **Snapshot before each attempt.** `runAttempt` receives `[...steers]` taken before the attempt starts (`:661`), so a steer accepted live *during* the attempt is delivered once by the kernel and is absent from that attempt's replay — and present in the next attempt's snapshot if the attempt is retried.
- **Replay on the first `TURN_FINISHED`, not the opening prompt.** Inside the one existing kernel subscription — no second subscribe — `replayPending` (initialized `replay.length > 0`) latches off on the attempt's first `TURN_FINISHED`, *after* `gate.recordTurn`, and enqueues each text via `kernel.steerText` (`:568-584`). That places the re-delivery in exactly the steering-queue poll a live steer would have hit, preserving "picked up after its current turn". A ceiling stop still wins: the loop's own `stopAfterTurn` runs between the enqueue and the poll.
- **Record-after-accept.** `steerText` iterates `liveKernels.entries()` to know which execution accepted, calls `kernel.steerText(text)` first, and appends to the log only after it returns (`:705-719`). All three `DomainValidationError` refusal messages byte-identical.
- **No new event type, no coordinator contact:** `src/run/coordinator.ts` and `src/run/events.ts` are 0 diff lines across the range; the replay emits no `ExecutionEvent`; no import added or removed in the diff.
- **Pins:** the 10 pre-existing steer pins plus the 2 new integration pins green 3× in this review's own runs (12/12 × 3, §2). The new tests are the audit's proof shape turned regression: scripted faux provider, blocking tool to make the steer window observable, `429` thrown mid-attempt, retry `sleep` stubbed; test 1 asserts the discarded attempt carried the steer, the retry's first call repeats the prompt *alone*, exactly one surviving call carries the steer, exactly four provider calls, terminal `SUCCESS`; test 2 kills attempt 2 on its opening call so attempt 3 must be handed the same text again, exactly once across five calls.
- **Reviewer's own mutants (out-of-tree copy `/tmp/r18-rev/tree`, control 2/2 green, deleted):**
  - **M1 — re-delivery dropped** (`for (const text of replay) kernel.steerText(text)` removed): **0/2** — `the surviving attempt must see the steer exactly once, got [[prompt alone]]` — the audit's hole reproduced verbatim at HEAD; matches the slot's M1.
  - **M2 — delivered before the retry's prompt** (enqueued at kernel construction, latch pre-cleared): **0/2** — test 1 red on `the retry's first call must repeat the original prompt alone, got [prompt, steer]` (the steer folded into the opening request), test 2 red on double-apply (`expected: 1 actual: 2` — both post-retry attempts carried it). The placement clause is genuinely load-bearing.
  - **M3 — `replayPending` never latched**: **0/2**, both on outcome `expected: 'SUCCESS' actual: 'FAILURE'` — the re-queue loop starves the scripted run. Matches the slot's M3.
- **Residuals, judged (none is a Round 19 slot):**
  1. *Record-after-accept unreachable through Pi* — correct as disclosed: `Agent.steer` only enqueues, so `SparkleKernel.steerText` cannot refuse against a real kernel, and `PiAgentExecutor` has no kernel-injection seam; adding one purely to pin the ordering would put a test hook in production code. The analogous ordering is pinned one layer up (`steer-inflight` test: "a steer refused by the kernel is not recorded as if the agent had received it", green here). Keep unpinned and disclosed.
  2. *Live+replayed order swap on a retry's first turn* — both texts are delivered exactly once each; only relative order can differ, no contract or record names an ordering across a retry boundary, and constructing the case needs a steer inside a blocked tool of a post-retry attempt. Not an honesty hole; nothing in the log becomes false. Recorded, not a slot.
  3. *Refuse-between-attempts unchanged* — correct: during backoff no kernel is live and `steerText` refuses loudly, which is the existing pinned contract; the slot repaired dropped delivery, not the steerable window.
  4. *Ceiling can leave a replayed steer unconsumed* — the ceiling winning is the frozen precedence, the stop is disclosed in the transcript summary, and the same shape already exists for a *live* steer enqueued just before a ceiling stop, retry or no retry — so this is not a new hole R18-1 introduced, and "fixing" it would mean the ceiling not winning. Not a slot.
  5. *Leaked `mkdtemp` roots from unrelated suites* — real and re-measured by this review: 128 `pi-sparkle-*` state roots present in `/tmp` at review time (dominant prefixes `pi-sparkle-pause`, `-eval-state`, `-eval-frozen`, `-eval-ds`, ~20 each after several full-suite runs), consistent with the slot's ~138-per-full-suite figure. Test hygiene in suites neither slot owns, not a behavioural honesty hole; removed by this review, recorded in the brief §5 for whoever next owns those suites.

### 4.2 R18-2 (`daea498`) — carry a declared per-child `maxCostUsd` through `parseChildSpec`

Exactly the brief's prescription, verified at source rather than trusted:

- **The parse is right.** `parseChildCostCeiling(taskId, value)` (`main.ts:366-374`): `undefined` → `undefined` (absent stays absent — no invented cap); anything that is not a positive finite number (`typeof !== "number"`, `!Number.isFinite` — which also rejects `NaN`/`Infinity` — or `<= 0`) throws `DomainValidationError` naming the task (`Child task <id>: limits.maxCostUsd must be a positive finite number`); a valid value is copied via conditional spread into the built `limits` (`:443`). One call site, inside the task mapper. The whole `main.ts` range diff is this function + call + spread, which *is* the proof that `runCommand`, the doctor routes, `INSPECT_SUMMARY`, `onRunStarted`, and the preference lock are byte-identical outside the parse region (§3 adds the direct reads).
- **Carriage proven on disk.** The new carriage pin runs real `main()` end-to-end with the default fake-children executor and reads the state root back: exactly one `TASK_REQUEST` with `limits.maxCostUsd === 0.25` in the parent's `events.jsonl`, exactly one child `RUN_CREATED` with `limits.maxCostUsd === 0.25`. Green in this review's gate (`ok 161`) and out-of-tree control.
- **Refusal proven loud and clean.** The refusal pin declares `0`, `-1`, `"0.25"`, `null` (JSON cannot carry `NaN`/`Infinity`, so `null` is the honest not-a-number shape), each in a fresh state root: exit non-zero, stderr names `tsk_capped` and `maxCostUsd`, CLI error JSON `ok:false`, and `runtime/runs` absent-or-empty — the refusal writes no run. Green (`ok 162`).
- **Reviewer's own mutants (same out-of-tree copy, control 10/10, deleted):**
  - **MA — carriage drop restored** (spread deleted, refusal kept): exactly `not ok 1` (`strictEqual` expected `0.25`), the other nine green — the carriage pin alone owns the defect. Matches the slot's Mutant A.
  - **MB — invalid silently copied** (`parseChildCostCeiling` returns `value as number`, no validation): exactly `not ok 2`, failing on the `match` operator with an **empty** actual stderr message — independently confirming the slot's observation that a copied `0` dies far away at protocol validation with no operator-facing message naming the file or task, which is precisely why the refusal must live at the parse and the pin must assert the message, not just the exit code. Matches the slot's Mutant B.
- **The spec rewrite is faithful.** `docs/specs/m0-m2-architecture.md:359-366` now says: forwards the tighter of per-task and run-level to the selected executor, stamps the effective cap into the child's `RUN_CREATED.limits`, `PiAgentExecutor` prices observed usage from the resolved catalog and stops before another turn, an executor that cannot price leaves it unenforced rather than inventing a figure — best-effort per-execution cap, not a cross-child ledger. Compared clause-by-clause against the `159630e` `ChildRunLimits` docstring (`protocol/v1.ts:79-88`): same substance, no overclaim. The false "does not currently read usage or enforce this ceiling" is gone.
- **The pinned non-enforcement stands.** `src/testing/fake-executor.ts` diff-empty; the fake-children executor still ignores the forwarded cap, and nothing in the diff asserts enforcement — carriage only, as granted.
- **Residuals, judged (none is a Round 19 slot):** `onCostGate` stays unwired — ROUND18-BRIEF §5 explicitly excludes it as a standalone slot, and although R18-2 created the first CLI path that can declare a cap, the disclosure already names enforcement executor-dependent and a real ceiling stop is transcript-visible; no honesty hole proven, frozen posture carried. No CLI flag for the run-level cap — capability, nothing claims the flag exists. `docs/kernel-reuse.md:107-114` — read at HEAD: it is a dated round-status journal whose claim ("both coordinator paths forward `run.limits.maxCostUsd`", claim gate `rg maxCostUsd src/run/coordinator.ts` = two hits) is still exactly true (`:375`, `:705`); leaving it untouched was right.

## 5. Process notes

- **Landing hygiene:** both landings are slot-files-plus-report only, zero PROGRESS ticks, verified per-commit; file sets disjoint. R18-2 landed first (05:37:55) from a shared tree that also carried R18-1's then-uncommitted edits — its report censused the overlap and touched none of it; R18-1's report likewise disclosed landing after R18-2. The dispatch-order inversion cost nothing: no shared file, no red-tree commit point.
- **Slot-report accuracy:** both reports check out claim-by-claim against source, git, and this review's independent re-runs — every mutant transcript reproduced within expected variance (M1/M2/M3, MA/MB all redden exactly the claimed pins with the claimed failure shapes). One count refinement, not a correction: t1's "~138 leaked `/tmp` roots" measured 128 at review time; same phenomenon, same prefixes, same judgment (hygiene, out of slot scope). Both slots' `/tmp` proof copies verified gone.
- **HEAD moved during review:** `0c948ba` (record SOTA review dispatch, PROGRESS-only) landed after the dispatch message was written; code tree unaffected. The dispatch's git-status snapshot predated the parent's landing commits and showed R18-1's files uncommitted; the actual tree was clean from this review's first command onward.
- **Sign-off compliance:** the parent's (a) decision was executed without a correcting event type, without touching `coordinator.ts`/`events.ts`, and without widening the steerable window — verified at source; nothing to reopen.
- **Hygiene at report time:** `/tmp/r18-rev` (mutation copy), the two pristine-file staging copies, the gate log, and the probe JSON all deleted; the 128 pre-existing leaked `pi-sparkle-*` suite roots (an unattributable mix of the parent's, the slots', and this review's full-suite runs) also removed.

## 6. Round 19 disposition

**Zero candidates.** The Round 18 post-merge audit swept the entire merge seam (nine surfaces recorded clean with reasons) and found exactly two holes; both are now closed at HEAD with this review's own out-of-tree mutants confirming each fix is load-bearing in every clause. This review re-judged every residual either slot surfaced — the unreachable kernel-refusal ordering, the cross-retry steer ordering, the between-attempts refusal, the ceiling-beats-steer precedence, the `/tmp` suite-root leak, the unwired `onCostGate`, the missing run-level-cap flag, and the `kernel-reuse.md` journal — and none is a behavioural honesty hole at HEAD; each has a recorded owner-on-next-touch or a frozen posture. The range added two tightly-scoped behaviours, both born pinned (each new pin proven to be the *sole* killer of its defect by this review's single-red mutants), so no landing stale-ifies any recorded surface beyond the spec lines R18-2 itself fixed. The honest round is zero slots; `ROUND19-BRIEF.md` §4 records that and the standing valid reasons to dispatch later.

## 7. Handoff

- Gate GREEN at `0c948ba` (code tree `4412fac`): **2042 / 2041 / 0 / 1 skipped** (`PI_SMOKE` only, exactly one `# SKIP`), 120 suites; crash-probe **11 × 3 `ok: true`**, names verified one-by-one, `unblock-discard-append-before-checkpoint-sigkill` last, no 12th case — both run independently on this VM, matching the parent's record exactly. Delta vs the 2038 audit baseline: +4, fully decomposed (R18-2 `ok 161`/`ok 162`, R18-1 `ok 299`/`ok 300`).
- **2 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK.** Both audit-proven holes closed; zero joints; zero red-tree commit points; both slot reports accurate.
- `ROUND19-BRIEF.md` carries **zero** candidates with the valid-dispatch-reasons list, the Rounds 1–18 freeze carried forward, and the two new Round 18 contracts (steer re-delivery across retries as shipped and pinned; per-child cap carriage + parse-time refusal as shipped and pinned).
