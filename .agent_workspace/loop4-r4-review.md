[Model: claude-fable-5-thinking]

# Loop 4 · Round 4 — SOTA review (R4-1 … R4-10)

Reviewer: Round 4 SOTA reviewer, evidence-based, on `agent/opt-continuous` at `74daff3` (all ten slots committed as `cf554b3`…`71bf74d`, plus the parent's cross-slot joint fix `74daff3`; range reviewed: `623058d..74daff3`). Every verdict was checked against the actual diff, not the slot report. During the review the parent landed `39dcee4` (`.agent_workspace/PROGRESS.md` only — verified to touch nothing reviewed). Independent gate and crash probe run on this VM (Node v22.14.0, pnpm 10.17.1). Working tree at review time: clean. Nothing committed by this reviewer; no `src/**` or `docs/**` file edited.

## 0. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R4-1 run-scoped delete lock | **ACCEPT** | Lock + double-verify verified in source; the report credits the *second verify*, not the lock, for the 0/120 leak — and the code comments say the same; per-step writers unlocked with the measurement carried in falsifiable decision pins; `RunRecordsSurvivedError` kept |
| R4-2 undelivered mail reaches an operator | **ACCEPT** | Both embedders wired, formatter + two CLI call sites verified; real E2E starvation tests print the exact line; the census finding (production cannot dead-letter) is verified true and prominently disclosed |
| R4-3 supervised crash terminal + `applyRetry` | **ACCEPT** | Wrapper/`allSettled`/rethrow verified; in-flight-only contract byte-equivalent to the other planes; both retry sites now go through `applyRetry` with the *recorded* status; R2-9/R3-8 pins re-checked |
| R4-4 paused-crash semantics decided | **ACCEPT** | Investigation genuinely overturned R3-5's lossless claim with a measured crash-point sweep; `preserveResumableState` verified as a guarded sibling, `recordCrashTerminal` byte-unchanged; no schema; child guard kept and pinned |
| R4-5 doctor remediation + run inventory | **ACCEPT** | Additive `remediation` and `runStates` verified in source; genuinely read-only (only `EventStore.readAll` through frozen `readJsonlObjects`); `file-lock.ts`/`main.ts` untouched |
| R4-6 resume executor config | **ACCEPT** | Flags, both forwarded call sites, disclosure-before-build all verified; wording never implies restore; no event/checkpoint schema change; sink pin intact and extended with mutants |
| R4-7 crash-atomic tombstones | **ACCEPT** | `writeFeedbackTombstones` via `writeFileAtomic` verified; reader throws exactly `DomainValidationError`; probe case reworked onto the rename seam (FIFO hack gone); feedback fuzz `skipUnowned` → `failFuzz` |
| R4-8 docs truth-up | **ACCEPT-WITH-NITS** | All scoped R3 corrections accurate against source; in-flight R4-1/2/6 timestamp-disclosed, not predicted — correct method, but those three landed hours later, so three coordination snapshots are now stale at HEAD (follow-through, not error) |
| R4-9 derived/learned JSON stragglers | **ACCEPT** | Both writers atomic, both readers code-discriminated; preference silent-reset closed by bind-after-load (reproduced defect in the report checks out against the old code shape); the `persist/` freeze exception is disclosed, additive, and justified |
| R4-10 offline loopback `--executor pi` | **ACCEPT-WITH-NITS** | Fixture + E2E genuinely test-only and execute R3-9's pinned joint; but its empty-stderr assert collided with R4-6's disclosed new warning on the same path — foreseeable from the brief, undisclosed in its report, fixed by the parent (`74daff3`), not the slot |

8 ACCEPT, 2 ACCEPT-WITH-NITS, 0 ROLLBACK. No lint or test blocker found (independent full gate GREEN), so there is nothing to commit from this review beyond this file and `ROUND4-BRIEF.md`.

## 1. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0**: **1680 tests / 1679 pass / 0 fail / 1 skipped** — matching the parent's numbers exactly. The sole skip was confirmed by name: `PiAgentExecutor completes a run against a real provider # SKIP set PI_SMOKE=1 …` (`test/integration/pi-adapter/provider-smoke.test.ts`). **No slot introduced a new named skip** (verified by census: the only `skip:` option in `test/**` is the `PI_SMOKE` gate). Delta vs the Round 3 baseline (1604/1603/1): **+76 tests**.
- **`node scripts/crash-probe.mjs` → `ok: true`, 8 cases × 3 iterations.** The case set is unchanged in count; R4-7 reworked `feedback-cascade-strip-before-tombstone` from the FIFO-blocking hack onto the atomic writer's deterministic kill-before-rename seam (verified in the diff: `createFifo` deleted, the child now stages the complete tombstone update and self-SIGKILLs before rename, the parent proves old tombstones remain readable and only the unique temp holds the candidate).
- **Key new pins spot-confirmed green in the gate log by name**: the starvation E2E pair (R4-2), the supervised crash pair (R4-3), the paused-resume-point and shared-child-log pins (R4-4), the run-delete adversarial pair (R4-1), the resume-config wiring pin (R4-6), and the loopback run→resume→calibration joint (R4-10).
- **The `74daff3` cross-slot joint**: verified as exactly what the parent described — R4-10's loopback resume test asserted empty stderr; R4-6's flag-free `--executor pi` resume now prints one defaults-rebuild warning on that path; the commit pins the exact warning string and still fails on any other stderr. A joint between two accepted behaviors, not a product defect in either.

### Freeze check — held, with one disclosed and justified exception

- `git diff 623058d..HEAD` over `src/persist/jsonl.ts`, `src/persist/file-lock.ts`, `src/run/episode-bind.ts`, `src/cli/episode.ts`, `src/telemetry/model-invocation.ts`, `src/cluster/host.ts`, `src/cluster/mailbox.ts`, `package.json`, `pnpm-lock.yaml` is **empty**. jsonl signatures, `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE`, `episodeLockPath` symmetry, `isInvocation`'s never-throws contract, and the `deadLetterReport`/`onDeadLetter` shapes are all byte-identical. No dependency edits anywhere in the round.
- **`src/persist/atomic-file.ts` is no longer an empty diff** — the R4-9 exception. Verified line-by-line: the frozen `writeFileAtomic(path, contents, options?)` signature and body are untouched; the only change inside pre-existing code is a behavior-identical one-line extraction of the temp-name template into a shared `tempName` helper (produces the same `${path}.${pid}.${suffix}.tmp` string). The additions — `writeFileAtomicSync`, `AtomicWriteSyncOptions`, `openUniqueTempSync` — mirror the async protocol exactly (`"wx"` never-adopt, fsync, rename with the same `RENAME_FALLBACK_CODES`, unpublished-temp cleanup). The justification holds: the preference store's API is synchronous and may not change shape, and an inline temp+rename copy in `preferences/store.ts` is precisely what the R1/T3 freeze forbids. A sibling in the one blessed module is the only move that satisfies both rules.
- `runCommand`'s sink construction and its hooked call sites: untouched (the only `runCommand` edit is R4-2's one added `warnUndeliveredClusterMail` line in the `--track` summary block). The invocation-sink-wiring pin file keeps its original pin and mutants verbatim and extends additively; both it and the whole gate are green.
- `RunRecordsSurvivedError` / `RUN_RECORDS_SURVIVED` kept (message wording updated to name the lock and the remedy — the message is not the contract, the code is). `planRound` still 4-arg with its arity pin re-mutation-checked by R4-3. `recordCrashTerminal` in-flight-only on both existing planes: the flowchart copy is **byte-for-byte unchanged** in the diff; the child copy got a comment-only edit; the new supervised copy reproduces the two limits without widening either existing one.
- Region discipline: `main.ts` was three-way shared (R4-2 summary output / R4-6 `resumeCommand` / nobody else) and the diff shows no overlap; R4-6's two USAGE-line touches sit outside its named region and were disclosed, with no collision. `flowchart-run.ts` was two-way shared (R4-2 cluster options+outcome / R4-4 teardown) and the diff separates cleanly. R4-9 edited `test/unit/persist/atomic-file.test.ts` (unowned but unclaimed by any slot) and disclosed it.

## 2. Requested target verifications

### 1. R4-1's honesty — VERIFIED, EXEMPLARY
- **The second verify, not the lock, closes the leak — and both the report and the shipped source say so.** `deleteRunRecords` (`deletion.ts`) holds `runLockPath` across `removeRunSubtree` + `verifyRunRecordsRemoved`, then, after release, re-verifies (`if (removed.length > 0) await verifyRunRecordsRemoved(...)`) with an inline comment explaining that releasing a lock is two I/O turns an unlocked appender can use. `EventStore.append`'s class doc states plainly that against a writer that does not take the lock, the lock cannot prevent resurrection — verification is what stops the delete from lying. The report's §"120/120 leak-free" paragraph makes the identical attribution. No credit inflation anywhere.
- **Per-step writers unlocked, with the evidence carried where it can be challenged.** The diff confirms `append` and `write` bodies are byte-identical to baseline (doc comments plus the `runLockPath` export are the entire src delta on those two files), so the shipped tree has zero hot-path cost by construction. The rolled-back numbers (+372%/append, +62%/write, +22.5%/+17.5% e2e against the 5% bar) live in throwaway harnesses I could not re-run, but they are recorded in the class docstrings and enforced by falsifiable decision pins (verified: `append does not block on the run lock` acquires the lock and appends inside it; same shape for the checkpoint store) — a future re-introduction must argue with a red test and a stale measurement, which is the right durability for a bench-based decision.
- **`RunRecordsSurvivedError` kept**, `runRecordsSurvived` now also names the lock path, and the remedy ("stop or cancel the run") is unchanged. `writeFeedbackTombstones` call-site swap landed exactly as the R4-1×R4-7 coordination planned; `track-questions.json` now goes through `writeFileAtomic` under the run lock; `clearPause`/`token`/`read` deliberately unlocked with reasons stated at each site.
- **Real behavior change, honestly priced:** a delete racing a live run now *always* refuses (0 clean / 120 attempts under the adversarial probe) where it used to sometimes succeed and sometimes lie. Fail-closed is the correct direction; the convenience fix (one lock acquisition per run in the run lifecycle) is disclosed as a Round 5 posture decision, not smuggled in. → R5-1.

### 2. R4-2 — operator line and the `dead-lettered=0` truth — VERIFIED
- **The line actually prints for sender-only starvation**, proven end-to-end through both embedders: `test/integration/cluster/undelivered-mail.test.ts` drives a real `startParentRun` and a real `startFlowchartRun` with an executor that role-casts to its own role, and asserts both the outcome report (`pending: 1, scout=1`) and the exact formatted line. The CLI hop is a source pin (exactly two `warnUndeliveredClusterMail` sites, one inside `printFlowchartOutcome`) because `ChildFakeExecutor` — built in a `main.ts` region owned elsewhere — never sends peer mail; the limitation is disclosed and the split matches the R3-9 precedent.
- **The census finding is true and load-bearing.** Verified at HEAD: `claimRole` (`mailbox.ts:117-137`) counts a requeue only when `mail.from === agentId`, and `ChildCoordinator.runAttempt` mints a fresh `createAgentInstanceId` for every attempt (`child-coordinator.ts:639`) before its only production `register` call (`:641`). So no mail can cross `DEFAULT_MAX_ROLE_REQUEUES` in a real run and `dead-lettered=` will read 0 in production until mailbox semantics change. **Disclosed?** Yes — prominently in the report (it reshaped the slot), in the test file's doc comment on the host-driven dead-letter case, and structurally in the design (the line leads with `pending=`, the field that carries real starvation). Nit, not verdict-changing: the disclosure lives in report+tests, not in operator-facing docs — folded into the Round 5 docs slot, and the reachability *decision* is R5-6.
- Consumed `deadLetterReport()` exactly as published, pull-not-push with the correct register-lifecycle argument; `cluster/host.ts` and `mailbox.ts` untouched.

### 3. R4-4 — no silent lost work in the closed window; the remaining window disclosed — VERIFIED
- The investigation genuinely overturned R3-5's residual claim: the budget-sweep tables identify the exact crash point (parent's `LEDGER_UPDATED` landed, `RUN_COMPLETED` not yet) where a paused run's checkpoint sat one node behind its log and resume silently re-paid for a finished node. The fix is verified in source: `preserveResumableState` re-reads the log, flushes the checkpoint **only** when replay is `PAUSED`/`WAITING_FOR_USER`/`BLOCKED`, swallows its own failures, and runs *after* `recordCrashTerminal` so a newly-terminal run is no longer resumable when the flush re-reads. `injectFlowchartRun`'s tail is wrapped with the same flush and — correctly — never a terminal (inject can target a run another process drives). `recordCrashTerminal` itself is byte-unchanged; the in-flight-only rule is not widened.
- **The remaining window is disclosed, not papered**: budgets 0–2 (child result on the log, not yet accepted by the supervisor) stay at-least-once, with the principled reason recorded (preserving them would apply a result while skipping its three-line gate) and the retry made *visible and pinned* (two `CHILD_RUN_CREATED` for the same task, distinct child run ids, each child log closed). The real fix (`resumeRestoredRun` adopting recorded results) is named as out-of-region Round 5 material. → R5-5.
- The child-side duplicate guard: correctly re-classified from "defensive" to reachable-via-published-`childRunId` (census of the four production call sites verified — none passes it), kept, comment-documented, and pinned by the new shared-log children-flowchart test (green in the gate). Six new abort-suite tests all present, each asserting the events on the log at its crash point so a sequence shift fails loudly. No new event type anywhere in the diff.

### 4. R4-6 — no schema change; disclosure does not imply restore — VERIFIED
- **No event or checkpoint schema change**: `src/run/events.ts`, `src/run/replay.ts`, and every payload validator are absent from the round's diff. The census argument for *why* the persistence option was refused is independently sound — `materializeCheckpoint(replayRun(events), …)` rebuilds `checkpoint.json` from replayed state, so a config field written there would be dropped by the first plain resume; honest persistence requires the event log, i.e. schema with parent sign-off. Not taken, and said so.
- **The disclosure is scrupulous about not implying restore.** All four `describeResumeExecutorConfig` cases verified in source: the flag-free pi case says "the run's own `--primary-model`/`--thinking` are **not recorded**, so pass them again if it did not start on defaults"; the flagged case says "this is what you asked for now, **not what it started with**". Disclosure is emitted before `createExecutor`, so a failed build has still said what it attempted. Both call sites forward `modelOverride`/`thinkingLevel` as 4th/5th args with the hooks object untouched.
- The frozen sink pin is verbatim-intact and the additions are genuinely additive (new `assertResumeExecutorConfigWiring` + six mutants, all rejected). Deliberate behavior change disclosed and pinned: resume now refuses an unusable ambient `PI_THINKING_LEVEL` (parity with `run`). The two USAGE-line edits outside the named region: disclosed, verified non-overlapping with R4-2's region.

### 5. R4-9 — `persist/` additive only; preference silent-reset closed — VERIFIED
- Freeze exception verified in §1: async writer untouched in signature and behavior; the sync sibling is a faithful mirror; the addition is the only shape that satisfies both the no-API-change rule on the preference store and the no-private-tmp-rename rule. A source pin in the owned preferences test keeps the store on the shared writer.
- **The silent reset is genuinely closed, and the old defect was worse than the brief said** — the report's reproduction (bind-before-load meant a swallowed `SyntaxError` left the singleton bound to the damaged file, and the next observation overwrote learned history) matches the old code shape in the diff (`persistFile = filePath; if (filePath !== undefined) loadFromDisk();`). The fix is bind-after-successful-load with parse-and-validate before any in-memory mutation; damaged bytes now throw `PreferenceSnapshotUnreadableError` (`PREFERENCE_SNAPSHOT_UNREADABLE`) and nothing can be written over them. Structural gate scoped correctly: `{}` and empty histories stay valid; only damage shapes reject.
- `catalog-observed.json`: `writeFileAtomic` + `CatalogObservedCorruptError` (`CATALOG_OBSERVED_CORRUPT`), ENOENT stays the one silent path, parseable-but-skewed shapes still degrade (version skew ≠ damage, and a test pins the distinction). The choice of throw-over-absent for a derived file is argued convincingly (absent has a meaning: "no observations") and the message names the rebuild. Byte-shape of both files preserved (the deletion suite's byte-identity pin depends on it, and holds).

### 6. No new named skip — VERIFIED
Census over `test/**`: the only skip of any kind is the `PI_SMOKE` gate in `provider-smoke.test.ts` (confirmed in the gate TAP output, skip count exactly 1). R4-7's fuzz flip *removed* the last unowned escape hatch — both feedback-plane `skipUnowned` paths are now `failFuzz`, and the helper itself is deleted, so there is no remaining mechanism for that fuzz file to skip.

## 3. Per-slot notes beyond the targets

- **R4-3 (ACCEPT).** The census correction matters and is verified: an *executor* throw cannot escape (`runAttempt` converts it to `executorOutcome: "FAILURE"`); the honest triggers are judge throws, rejected appends, unmapped roles, and the child coordinator's out-of-attempt appends. The `Promise.allSettled` settlement is the supervised analog of `cancelAndSettle` and is separately pinned (round-mate's `COMPLETED` lands before `RUN_FAILED`, which is last). Not aborting the controller is argued correctly — an abort would record `RUN_CANCEL_REQUESTED` and re-classify the crash as a cancellation nobody requested. The `applyRetry` wiring passes the *recorded* status, which is what makes the rule's guard non-vacuous; behavior-neutrality is evidenced the right way (mutation flips only the source pin). Disclosed residuals (crashed run leaves episode bound + checkpoint stale; the crash unwind waits for the round; three `recordCrashTerminal` copies) are real and correctly deferred. → R5-2.
- **R4-5 (ACCEPT).** Tersest report of the round; every claim verified in the diff. `remediation` is per-entry and conservative (only dead-recorded-PID gets "inspect and remove manually; never automatic"; running/unknown/missing PID cases explicitly refuse to treat age as proof). `runStates` scans only `runtime/runs/<id>/events.jsonl`, replays through the real `EventStore`/`replayRun`, lists only `PLANNING`/`RUNNING`, and its check fails only on scan errors — candidate presence is advisory, corrupt logs are fail-closed into `scanErrors`. Read-only confirmed: doctor's only write remains its pre-existing state-root probe. What it does not do (route operators here from the failing commands themselves) is the follow-through. → R5-9.
- **R4-7 (ACCEPT).** The exact scoped change, verified: writer atomic with the `AtomicWriteOptions` seam, reader throws exactly `DomainValidationError` on unparseable JSON (existing typed error kept for wrong shapes), probe case honest about what it proves (complete old tombstones readable, target not tombstoned, stripped feedback survives, candidate only in the unique temp). Keeping the established case name kept the probe census green. The R4-1 coupling worked exactly as the R3-2×R3-3 pattern prescribes.
- **R4-8 (ACCEPT-WITH-NITS).** The R3 truth-ups are accurate against source (delete verification + `RUN_RECORDS_SURVIVED`; episode `.lock` re-described as an owned-release sidecar, not a deleted record; the DAG-skip and wall-clock-lease-expiry claims replaced with the implemented contracts; doctor lock inventory and host dead-letter surfaces documented). The timestamped coordination snapshots (18:40 UTC) were the correct method and are explicitly non-predictive. Nits, all follow-through: R4-1, R4-2, and R4-6 landed after the snapshot, so at HEAD three passages now *understate* the tree — `data-dictionary.md` and `status-matrix.md` say no run-plane lock exists (one now does), the dead-letter row says nothing reaches a CLI operator (the undelivered-mail line now does), and the resume row says the flags don't exist (they now do). Also not yet documented: R4-9's two error codes, R4-5's `runStates`, R4-3's supervised crash terminal, R4-4's flush contract. → R5-7.
- **R4-10 (ACCEPT-WITH-NITS).** The fixture is a real loopback HTTP server speaking the OpenAI SSE protocol (validates model and `stream: true`, emits usage and `[DONE]`, records requests), and the test drives the exported CLI through `providers.json` `customProviders[].baseUrl` — run, gated resume, second invocation row, production calibration reader decoding both. That executes R3-9's pinned joint and R3-1's loader over real transport with no credentials and **no `src/**` edits** (commit verified: test files only). The child-run-id scope note is honest. The nit: its `assert.equal(resumed.err.join(""), "")` sat on the exact path R4-6 — a slot named in the same brief — was making noisy by design; the collision failed the parent's first gate and was closed by the parent's `74daff3`, and R4-10's own report neither anticipates nor mentions it. Every other slot that saw the red test attributed it correctly, so the round's process absorbed it, but joint-awareness was this slot's to own.

## 4. New reviewer findings (evidence for Round 5)

1. **The bandit store has the exact silent-reset defect R4-9 closed for preferences.** `updateProjectBandit` writes pretty-printed multi-line JSON with plain `writeFile` (`learning/bandit-store.ts:126`, under the lock — which serializes writers but does not make the write crash-atomic), and `readBanditFile` (`:58-70`) turns *any* parse failure or shape mismatch into `undefined`: a torn file reads as "no bandit yet", `createBanditState` starts fresh, and the next update overwrites the learned state for good. Adaptation-plane only (live routing reads the promoted policy), which caps severity — but it is learned, behavior-bearing state destroyed silently. → R5-3.
2. **Three private temp+rename copies of the atomic writer survive in `src/`, two of them defective.** `writeAtomicJson` (`config/providers-config.ts:189`) and `FileCredentialStore.save` (`pi-adapter/file-credential-store.ts:85`) both use a **fixed** `${path}.tmp` opened `"w"` — a stale temp from a crashed writer is silently adopted/truncated rather than refused, and two concurrent writers collide on the same temp name (exactly the failure modes `writeFileAtomic`'s `"wx"`-unique-temp protocol and the `atomic-write-stale-unique-temp` probe case exist to prevent). `saveAdaptationRegistry` (`adaptation/promotion.ts:324`) uses the unique-name protocol but leaks its temp when the write itself fails (cleanup only wraps the rename). All three predate Round 4; none is in any slot's Round 4 diff. Consolidating them onto `writeFileAtomic` deletes the last inline copies the R1/T3 doctrine forbids. → R5-4.
3. **`episode/replay.ts` is dead code with off-doctrine error discipline.** `replayFromLog`/`replayEpisodeEvents` have no production caller (census: only their own unit test); the live episode read path is `episode/store.ts:46`, which validates through `validateEpisodeEvent`. The dead copy casts each parsed line to `EpisodeEvent` unvalidated and throws a bare `Error` on a corrupt middle line. R3-8-style removal with a pin (or wiring through the validated path) is a small, clean slot component. Same slot can align `CheckpointStore.read`'s bare `JSON.parse` (`checkpoint-store.ts:52` — crash-tear unreachable since the writer is atomic, but external damage surfaces as a bare `SyntaxError` on every resume) and `cli/flowchart-io.ts:15` with the typed-error doctrine. → R5-10.
4. **A refused delete now needs a route to the remedy.** After R4-1, `delete --run` against a live run refuses *every* time (by design), and lock-timeout failures (`LOCK_TIMEOUT_CODE`) on the episode/run planes are more visible than ever — but the CLI error output still names no next step, while R4-5's doctor now carries exactly the inventories (`locks` with per-entry remediation, `runStates` with resume/delete guidance) that answer them. Connecting the failing commands' error surface to doctor is the last hop of the R3-6→R4-5 line. → R5-9.
5. **The dead-letter bound needs a decision, not more plumbing.** R4-2's verified census (finding 2 above, §2.2) means the mailbox's requeue-limit is production-unreachable. Either the bound should count something real (e.g. claims by the sender when the sender is the only registrant, or stable per-task agent identity across attempts — both semantic changes to frozen-adjacent surfaces needing parent sign-off) or the dead-letter half should be documented as API-only. Leaving it undecided leaves an operator line whose second field is structurally zero. → R5-6.
6. **R4-6's forwarding still has no wire-level witness.** Only `--executor pi` reads `modelOverride`/`thinkingLevel`, and R4-6 could pin the forwarding only structurally. R4-10's loopback fixture already records incoming requests and validates the model — extending its test to resume with `--primary-model loopback/loopback-1` and assert the request the server *received* closes R4-6's disclosure #4 entirely test-side. → R5-8.

## 5. Handoff

- `ROUND4-BRIEF.md` (same directory) carries the Round 5 injection context: what landed, the new baseline (gate 1680/1679/0/1-skip; probe 8×3; frozen list grown), saturated modules, and ten mutually exclusive candidates ranked with the run-lifecycle lock posture at the top.
- Nothing was committed by this reviewer, no `src/**` or `docs/**` file was edited, and no fix was needed anywhere. The parent should commit `.agent_workspace/loop4-r4-review.md` and `.agent_workspace/ROUND4-BRIEF.md`.
