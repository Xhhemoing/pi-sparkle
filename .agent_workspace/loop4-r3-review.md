[Model: claude-fable-5-thinking-xhigh]

# Loop 4 · Round 3 — SOTA review (R3-1 … R3-10)

Reviewer: Round 3 SOTA reviewer, evidence-based, on `agent/opt-continuous` at `62cb3ff` (all ten slots committed, `50fbc15`…`62cb3ff`). Every verdict below was checked against the actual diff (`3473253..HEAD`), not just the slot report. Independent gate and crash probe run on this VM (Node v22.14.0, pnpm 10.17.1). Working tree at review time: only `.agent_workspace/PROGRESS.md` modified (parent's file, untouched).

## 0. Scoreboard

| Slot | Verdict | One-line basis |
|---|---|---|
| R3-1 invocation decoders fail closed | **ACCEPT** | Verified total `invocationError(unknown)`; `isInvocation` cannot throw; loader skips bad rows; fuzz un-skipped and hard-fails |
| R3-2 atomic privacy rewrites | **ACCEPT** | Both rewrites through `writeFileAtomic`; `isLockTimeout` now code-based; probe 8 cases green; `persist/` untouched |
| R3-3 `delete --run` resurrection | **ACCEPT** | Fail-closed `RunRecordsSurvivedError` verified; option analysis sound; residual window quantified and disclosed, not hidden |
| R3-4 event decoder discipline + fuzz | **ACCEPT** | 2-line src fix exactly as scoped; 601-line seeded fuzz over all 34 event types; found no further defect (credible: discipline was already typed) |
| R3-5 terminal events on escaping errors | **ACCEPT** | Both planes fixed; duplicate/paused guards verified in source and pinned; negative controls + mutation checks reported and spot-verified |
| R3-6 stale-lock diagnosability | **ACCEPT** | Read-only confirmed (the `writeFile`/`unlink` in doctor is the pre-existing state-root write probe); `file-lock.ts` untouched |
| R3-7 dead letters reach an operator | **ACCEPT-WITH-NITS** | Host surface excellent and mutation-checked, but the slot title is only half-true: no production embedder consumes it — an operator still sees nothing |
| R3-8 scheduler/supervisor leftovers | **ACCEPT** | Census-first discipline; `applySkipped` removal + arity pin verified; new `applyRetry` divergence finding verified at `supervisor.ts:266,423` |
| R3-9 resume sink wiring | **ACCEPT** | Both resume `createExecutor` sites hooked through one shared sink (`main.ts:1120,1139,1180`); `runCommand` wiring untouched; honest about what cannot be executed offline |
| R3-10 docs accuracy | **ACCEPT-WITH-NITS** | Checkpoint/lease and episode-lock corrections verified; but `delete --run` docs not reconciled with R3-3 (disclosed timing gap) and R3-8's flagged doc lines not picked up |

8 ACCEPT, 2 ACCEPT-WITH-NITS, 0 ROLLBACK. No lint blocker found (the gate's `eslint .` passed inside a green gate), so no test-file fix was applied and there is nothing for the parent to commit from this review beyond this file and `ROUND3-BRIEF.md`.

## 1. Independent verification (this VM)

- **`pnpm gate` GREEN, exit 0**: 1604 tests / 1603 pass / 0 fail / **1 skipped**. The skip count dropped 2 → 1 exactly as R3-1 predicts, and the survivor was confirmed by name: `PiAgentExecutor completes a run against a real provider # SKIP set PI_SMOKE=1 …` (`test/integration/pi-adapter/provider-smoke.test.ts`). R2-7's named invocation skip is gone — the un-skipped fuzz case is now a live regression test. Delta vs the Round 2 baseline (1550/1548/2): +54 tests.
- **`node scripts/crash-probe.mjs` → `ok: true`, 8 cases × 3 iterations** — the six Round 2 cases plus R3-2's `feedback-rewrite-kill-before-rename` and `invocation-rewrite-kill-before-rename`, both proving the destination keeps its complete pre-rewrite bytes (unrelated row included) while the candidate rewrite exists only in the unique temp.
- **Freeze held completely.** `git diff 3473253..HEAD` over `src/persist/` (jsonl.ts, atomic-file.ts, file-lock.ts) is **empty** — jsonl signatures, `writeFileAtomic`, and the re-frozen `withExclusiveFileLock` are byte-identical. `src/run/episode-bind.ts` (`episodeLockPath`) and `src/cli/episode.ts` unchanged, symmetry intact. `runCommand`'s sink construction (`main.ts:627`) and both of its hooked call sites (`:642`, `:689`) are untouched; R3-9 added the resume side without moving them. No `package.json`/dependency edits anywhere in the round.
- One correction to my own process, disclosed: my first freeze diff used the wrong filename (`atomic-write.ts`); re-checked against the real `src/persist/atomic-file.ts` — unchanged.

## 2. Requested target verifications

### R3-1: `isInvocation` never throws; loader skips bad rows; fuzz un-skipped — VERIFIED
- `invocationError` (`model-invocation.ts:96`) is total over `unknown`: root `isRecord` guard, `config` guard, non-string `modelVersion` guard, `pricing` guard, `typeof`-first numeric narrowing, and a `describe()` renderer that never coerces objects. `isInvocation` (`:199`) is a pure delegation — there is no throw path for any JSON-expressible value. The disclosed limit (a hand-built object with a throwing getter can still escape) is correct and correctly out of scope: `JSON.parse` output is the blast radius.
- `loadInvocationsFromStateRoot` (`cost-calibration.ts:143`) calls `isInvocation(parsed)` per row; with the predicate total, one bad row in `runtime/invocations.jsonl` no longer crashes `pi run`/`resume` startup. The new loader contract test writes ten bad rows ahead of one good row and drives the real `calibrateCatalogFromState` path.
- The fuzz case is genuinely un-skipped: both former `skipUnowned` calls in the invocation case are now `failFuzz` (`row-fuzz.test.ts:565,576,579,589`), and the case additionally pins that `isInvocation` neither throws nor disagrees with `validateInvocation` — the predicate, which is the surface production actually calls, is covered directly. The report's failing-direction check (HEAD source + fixed test ⇒ fails at seed `0x4f320007` iteration 11) matches the mechanism. The widened `invocationError` signature is disclosed, not on any frozen list, and is what makes the predicate honest.
- Note: the feedback-row fuzz case still carries `skipUnowned` (correct — that decoder was unowned this round, and it currently finds nothing, so no skip fires). Round 4 candidate R4-7 claims that plane and flips it.

### R3-3 residual: post-delete resurrection window — VERIFIED, RANKED HONESTLY
- The fix is fail-closed detection, and it is real: `removeRunSubtree` catches an `rm` that lost the race mid-walk (writer recreation ⇒ `ENOTEMPTY`, attached as `cause`) and follows a clean `rm` with `verifyRunRecordsRemoved`; both shapes raise `RunRecordsSurvivedError` (`RUN_RECORDS_SURVIVED` code, per R2-2's code-not-message doctrine), while an I/O failure that left nothing behind is rethrown unchanged. The p50 aggregate invalidation moved into the invocation rewrite so a failed delete cannot leave a stale aggregate — verified at `deletion.ts:746`.
- **The window is still open, and the slot said so.** The check is point-in-time: a writer whose append lands after `verifyRunRecordsRemoved` recreates the directory *after* the delete has already returned success. The slot measured it (~6/30 deletes under an adversarial tight-loop writer; far rarer at human append cadence, not zero) and disclosed it in the source docstring, not just the report. The slot's option analysis was independently verified: there is genuinely no run-plane lock today (the six `withExclusiveFileLock` call sites are episodes/feedback/invocations/bandit/credentials/adaptation-registry), and `EventStore.append`, `checkpoint-store.ts`, `pause-controller.ts`, and `track/loop.ts` all write under `runtime/runs/<id>/` uncoordinated while `appendJsonlLine` recreates a missing directory by design.
- **Ranking: this is the top Round 4 candidate (R4-1).** Closing it needs a writer-side run-scoped lock acquired by all four writers plus the delete — a four-writer change with a hot-path cost that must be measured, which is exactly why it was out of a single Round 3 slot's reach. What landed is the correct intermediate posture: a delete can no longer report success it cannot prove, and the error message tells the operator the actual remedy.

### R3-7: host surfaces dead letters, CLI embedders still don't — VERIFIED, RANKED AS LEFTOVER
- The host surface is complete and well-built: `deadLetterReport()` computes from `mailbox.deadLetters()` on every call (cannot drift, catches out-of-band drops), `onDeadLetter` fires from `register` with a watermark for exactly-once push, observer throws are tallied without failing registration, and all of it is mutation-checked in a new 6-test file.
- **But verified at HEAD: neither production embedder consumes it.** `createClusterHost` at `src/run/coordinator.ts:322` and `src/run/flowchart-run.ts:267` still pass only `onSpawn`; a repo-wide search finds no production caller of `deadLetterReport` and no `onDeadLetter` outside the host and its tests. A starved role-cast is now observable to *an embedder that asks* — no operator sees anything yet. The slot disclosed this precisely (the two call sites are in files it did not own, one being R3-5's file that round), so this is ACCEPT-WITH-NITS, not a rollback: the seam that was missing now exists and is tested. The last hop is Round 4 candidate R4-2, and the brief makes it end-to-end this time — embedder option plus an operator-visible line — so the same "reaches an operator" title cannot be half-true twice.

### R3-9: resume sink wiring — VERIFIED
- One `createInvocationSink(stateRoot, { onDrop })` is built in `resumeCommand` (`main.ts:1120`) with the same stderr disclosure string as `runCommand`; both resume `createExecutor` sites pass `onInvocation` through it (supervised `:1139`, flowchart `--executor` `:1180`). One shared sink is correct — the sink's per-log-path queue is the ordering guarantee, and two sinks over one log would regress it.
- The new pin file (`invocation-sink-wiring.test.ts`, 396 lines, 5 tests + mutation controls) forbids any unhooked `createExecutor` call in `main.ts` — the whole defect class, not just these two sites. The honest-scope disclosures check out: `createExecutor` forwards `onInvocation` only on the `pi` branch, so both behavioral tests correctly assert zero rows for fake executors, and the resumed-`pi`-persists-a-row joint is pinned, not executed (offline drive of `--executor pi` is structurally impossible today — see R4-10).
- Adjacent finding for Round 4 (not a defect in this slot): `resumeCommand` accepts neither `--primary-model` nor `--thinking` (`parseArgs` options at `main.ts:1082-1095`), so a resumed run silently rebuilds its executor with default model config regardless of how the run was started (R4-6).

### R3-5: terminal events don't duplicate on already-terminal/paused runs — VERIFIED
- Run plane: `recordCrashTerminal` (`flowchart-run.ts:729`) appends `RUN_FAILED` **only** when the persisted log replays as `PLANNING` or `RUNNING` — an already-terminal log gets no duplicate, and a `PAUSED`/`WAITING_FOR_USER`/`CANCELLED` log is never converted to dead. The whole body is best-effort (`try { … } catch {}`), called after `cancelAndSettle()` and before the rethrow, so the escaping error is never masked.
- Child plane: `recordCrashTerminal` (`child-coordinator.ts:378`) checks the child's own log for existing terminal types (`RUN_COMPLETED`/`RUN_FAILED`/`RUN_CANCEL_REQUESTED`) before appending, with the same swallow.
- Pins confirmed present and green in the gate: exactly-one-terminal asserts at `flowchart-run-abort.test.ts:312` and `children-flowchart.test.ts:321`; the paused-crash tests assert the log stays `PAUSED` and terminal-free (`:357,:370`); the escaping-error and torn-log rethrow cases exist as named tests. The slot's own mutation table (dropping the status guard or the swallow flips named tests red) is consistent with the code shape.
- Disclosed residual stands: a crash while tearing down a paused/waiting state still leaves a log that "just stops" in that state — deliberate, and Round 4 candidate R4-4 owns deciding whether that needs more than the current honesty.

### Freeze — VERIFIED (detailed in §1)
`appendJsonlLine`/`readJsonlObjects` signatures, `writeFileAtomic`, `withExclusiveFileLock` + `FileLockTimeoutError`/`LOCK_TIMEOUT_CODE`, `episodeLockPath` ⇔ `cli/episode.ts` symmetry, `runCommand` sink wiring: all byte-identical or untouched. The two slots that consumed lock timeouts (R3-2's feedback retry) migrated to the code-based discriminator as the frozen contract requires — the message-matching classifier is gone from `feedback/store.ts`.

## 3. Per-slot notes beyond the targets

- **R3-2 (ACCEPT).** Verified diff: `writeFeedbackRecords` and `writeInvocationRecords` are now `writeFileAtomic` with the same bytes and same caller-held locks; the `AtomicWriteOptions` parameter is additive and exists as the deterministic crash seam the probe uses. The bundled `isLockTimeout` migration closes the disclosed R2-2/R2-4 joint residual in the file the slot owned. The report is the tersest of the ten; every claim in it checked out.
- **R3-4 (ACCEPT).** `EventStore.readAll` now throws `DomainValidationError` for a corrupt middle line — a 1-line src change, exactly as scoped, with the frozen `readJsonlObjects` signature untouched. The new fuzz file is substantial (seed `0x4f33_0004`, 120 mutations × 34 event types + 180 corrupted-file iterations, accepted rows re-validated and replayed). Finding no defect in `validateEvent`/`replayRun` is credible — that plane already had typed discipline — and leaving `src/run/events.ts` unedited was the right non-action. Event-log parse discipline is now saturated.
- **R3-6 (ACCEPT).** The inventory is genuinely read-only over lock files (verified: doctor's only writes are its pre-existing state-root write probe), parses lock metadata locally without touching frozen `file-lock.ts`, reports the empty-metadata kill-window case, and carries the PID-reuse advisory disclaimer in the output itself. Tests pin byte-for-byte lock-file survival after a doctor run.
- **R3-8 (ACCEPT).** Diff verified line-for-line: `applySkipped` removed with a constraint-stating note, `planRound` down to 4 args with the arity pin calibrated against runtime behavior (the `leases?` optional still counts in `Function.length` — the slot caught its own wrong first draft), stale supervisor comment now *discloses* the `TASK_LEASE_EXPIRED` name-vs-trigger mismatch instead of hiding it, and R2-9's pins re-mutation-checked. The new `applyRetry` finding is real — verified: production performs BLOCKED→READY inline at `supervisor.ts:266` and `:423` and never calls the declared rule, so editing `applyRetry` changes nothing. Correctly disclosed rather than widened; now R4-3.
- **R3-10 (ACCEPT-WITH-NITS).** The three scoped corrections are accurate against source (checkpoint carries no lease registry; leases rebuild from `TASK_LEASED`; episode lock is a sidecar released, not a record hand-unlinked). Nits, all follow-through rather than error: (a) `docs/data-dictionary.md` §"Deletion tooling" and `docs/status-matrix.md`'s privacy row still describe `delete --run` as an unverified subtree removal — R3-3 landed after this slot's final coordination check, which the slot disclosed with a timestamp; (b) `m0-m2-architecture.md:169` still claims dependencies can be "explicitly `SKIPPED` by a declared transition rule" (no such rule exists after R3-8) and that "Lease expiry converts a running task to `BLOCKED`" (false since R2-9), with the `-> SKIPPED` edge still in the `:159` diagram; (c) `status-matrix.md` still says `delete --episode` removes `.lock`. All folded into R4-8.

## 4. New reviewer findings (evidence for Round 4)

1. **Supervised DAG plane has R3-5's exact gap.** `runSupervisorRounds` (`supervisor.ts:222`) contains no `try`/`catch` anywhere: an executor throw, a judge failure, or a rejected append escapes with no `RUN_FAILED`, and the supervised run's log replays `RUNNING` forever. R3-5 fixed the flowchart and child planes only (its ownership). → R4-3 (same files as the `applyRetry` wiring).
2. **`tombstones.json` is written non-atomically and read untyped.** `cascadeFeedbackTombstones` writes it with plain `writeFile` (`deletion.ts:674`); `readFeedbackTombstoneIds` (`store.ts:79`) does an uncaught `JSON.parse`, so a SIGKILL mid-write tears the file and every subsequent `readFeedback` throws a bare `SyntaxError` until manual repair. Fail-closed in direction (nothing resurrects) but the wrong error class and a read-side outage. → R4-7.
3. **`catalog-observed.json` and `preferences.json` have the same tear window.** `persistCatalogObserved` (`catalog-observed.ts:102`) and the preference store (`preferences/store.ts:35`) write pretty-printed multi-line JSON with plain `writeFile`/`writeFileSync`; `loadCatalogObservedSnapshot` (`:115`) parses uncaught. `track-questions.json` (`track/loop.ts:254`) likewise. Checkpoint and pause writers already use `writeFileAtomic` — these are the stragglers. → R4-9 (and `track/loop.ts` folded into R4-1, which owns that file anyway).
4. **Lock-timeout failures don't route the operator to the new doctor surface.** `FileLockTimeoutError`'s message (frozen) names the path but no remedy; the no-steal recovery is now *diagnosable* via R3-6's inventory, but nothing connects a failing `delete --episode`/settle to it. → R4-5.
5. **Resume flag parity** (§2, R3-9 notes). → R4-6.
6. **The one remaining skip is structural.** `--executor pi` cannot be driven offline because `createConfiguredPiExecutor` always passes real provider models — yet `providers.json` already supports `customProviders[].baseUrl`, so a loopback HTTP provider fixture could close the run/resume/invocation-row joint end-to-end without touching src. → R4-10.

## 5. Handoff

- `ROUND3-BRIEF.md` (same directory) carries the Round 4 injection context: what landed, the new baseline (gate 1604/1603/0/1-skip; probe 8×3; frozen list grown), and ten mutually exclusive candidates with the parent's five priorities ranked at the top.
- Nothing was committed by this reviewer, no `src/` file was edited, and no lint fix was needed. The parent should commit `.agent_workspace/loop4-r3-review.md` and `.agent_workspace/ROUND3-BRIEF.md`.
