[Model: claude-fable-5-thinking-xhigh]

# ROUND 12 BRIEF — injection context for Loop 4 · Round 13

Provenance: written by the Round 12 SOTA reviewer (claude-fable-5-thinking-xhigh) after evidence-based review of R12-1…R12-10 at `aa7282f`; verdicts and verifications in `.agent_workspace/loop4-r12-review.md` (10 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK; **two joints — one folded into the landing commit, one held-and-applied successor — counted; zero red-tree commit points**). Every candidate below cites evidence the reviewer verified at HEAD against the actual diff `2767321..aa7282f`, not report hearsay. All landings are committed. **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 12 (all committed)

| Slot | Landed | Key symbols (do not re-implement) |
|---|---|---|
| R12-1 (`81f5b81`) | **The `taskCriteria` seam is filled, and tracked runs disclose their id early.** Writer: three sources, monotone first-write-wins, never revised, never dropped — (1) the caller's `input.childTasks` on the start path (`plannedTaskCriteria`; the arrays are byte-identical to what `buildTaskRequest` copies into each `TASK_REQUEST`, so this is a dispatch fact recorded early; an empty caller list is recorded as the durable known-none; duplicate ids resolve last-wins matching `childTaskMap`); (2) non-empty logged `TASK_REQUEST`s merged on every checkpoint write (`advanceTaskCriteria`; **a logged request with empty criteria is deliberately ignored** — on the log it is indistinguishable from a substituted re-dispatch, so only the caller's own spec can say known-none); (3) the checkpoint's existing record on every restore (resume, pause/inject session, unblock reopen all carry it forward beside `contract`). Reader: `withRecordedCriteria` at `childTasksFromLog`'s **sole** call site fills only the specs the log has no request for; a task the log knows keeps the log's answer; a task neither source names keeps its empty list. **No `continuation.taskCriteria`** — a dispatch record is not a knob a caller may re-answer (negatively pinned). The forbidden `ctx.childByTaskId` substitution empties were never written (reviewer-verified: start seeds from caller specs, resume seeds only from the checkpoint). Behavioural proofs: a paused run's resume re-dispatches recorded criteria while `tsk_unspecified` (no caller spec, never logged) stays **absent — unknown, not known-none — even after the resume's own checkpoint write**; the crash-path fixture in `flowchart-run-abort.test.ts` re-asks its caller's two criteria (the folded joint, content-verified). `onRunStarted?: (runId: RunId) => void` on `FlowchartRunDeps`: fires inside `withRunLifecycleLock`, immediately after the `RUN_CREATED` append, before round 1's pause poll; throws swallowed (state fail-closed: a propagated throw would orphan a run with records but no checkpoint, which every restore refuses); not a control channel — stopping still goes only through `PauseController`. `startTrackedRun` forwards it; `runCommand`'s track path prints `Run <id>: started`; the behavioural pure-CLI tracked pause (synchronous stdout handler seeds the token before the first poll — no race, no kill) replaces R11-3's call-site-only posture. Nine mutation checks recorded with per-mutation red counts | `plannedTaskCriteria` / `advanceTaskCriteria` / `withRecordedCriteria` / `FlowchartLoopContext.taskCriteria` in `run/flowchart-run.ts`; `onRunStarted` in `flowchart-run.ts` + `track/loop.ts` + `cli/main.ts:876`; tests in `m2.5/resume.test.ts` (21), the flipped pin + wiring pins in `flowchart-run-abort.test.ts` |
| R12-2 (`95a2b25`) | Additive spine pin: `flowchart-run.ts` proven inside the whole-`src` census, required to contain zero `independentEvidence` mentions, with an injected-reader mutation proof that the exact-one-`void` assertion bites. Landed before R12-1 and guarded it in flight | 6th test in `independent-evidence-posture.test.ts` |
| R12-3 (`b8f784f`) | Production reachability for `unmet-acceptance-criterion`, +4 tests: child reports task PASSED + one evidence-backed FAILED criterion → node **COMPLETED**, run **BLOCKED**, exactly one hard assessment with `gate.codes` deep-equal `["unmet-acceptance-criterion"]` (whole-array), one `GATE_TRANSITION` carrying the code/`queue_analysis`/the criterion's own evidence id → retry request refused with the exact producer message (writes nothing) → no-retry `unblock` (two-key payload deep-equal) → resume COMPLETED, completed node not re-driven. Three controls (PASSED/UNOBSERVED/omitted) each assert the gate *assessed* (`kind: "none"`, `codes: []`, zero transitions) — unknown-not-unmet from production. Out-of-tree mutations: severing the channel at `child-tracking.ts`'s wholesale spread turns the block arm COMPLETED, controls stay green. Recorded consequences: the "gate wins" rule measured from the disagreeing direction; `--discard-executed` structurally unavailable for criterion blocks behind COMPLETED nodes (no failed node to name) | `test/integration/run/criteria-gate.test.ts` — **also the natural fixture for candidate 2 below** |
| R12-4 (`d1b451c`) | Round 11 docs truth-up: the four false-at-HEAD claims fixed, landings anchored to real commits, five timestamped censuses that never assigned uncommitted work a commit id, ADR-006 Proposed. **Stale again at HEAD (the standing race, not a miss): R12-1 landed 16 minutes later — candidate 4 owns the spots** | `docs/status-matrix.md`, `docs/data-dictionary.md`, `docs/specs/m0-m2-architecture.md`, the tracking plan |
| R12-5 (report-only, `bbb840f`) | Correct declination: `assertNoSchedulerRetry` already whole-file-AST-walks both `FLOWCHART_SOURCES`, so R12-1's new paths were covered automatically; a symbol-naming assertion would be brittle duplication. Censused against R12-1's in-flight diff | `flowchart-applyretry-absence.test.ts`, untouched |
| R12-6 (report-only, `80fbdcf`) | Correct declination with the load-bearing argument: the census discriminator flags a scope only when it constructs a per-task criteria field **and** reads the episode, so it was already built to accept logged-request/caller-spec provenance and reject episode provenance. Reviewer re-ran 5/5 at HEAD after R12-1's writer | `episode-contract-boundary.test.ts`, untouched |
| R12-7 (report-only, `79991d6`) | Correct declination: R12-1 added no route string and changed no output line. Reviewer independently diffed `DOCTOR_ROUTED_NEXT` + `GENERIC_FAILURE_NEXT` base-vs-HEAD: character-exact identical; `INSPECT_SUMMARY` four keys read directly; `blocked-next.test.ts` diff-empty | routes/inspect/BLOCKED pins, untouched |
| R12-8 (report-only, `9b2d79f`) | Live-isolation census against HEAD **plus R12-1's working diff**: sole new `src` import is type-only `TaskAcceptanceCriteria` on an existing in-closure edge; closure steady at 165/215; file byte-identical by blob id. Reviewer's sweep concurs | `live-isolation.test.ts`, untouched |
| R12-9 (`b65a8b1`) | The exact eight-member `RunStatus` vocabulary frozen (`PLANNING`/`RUNNING`/`WAITING_FOR_USER`/`PAUSED`/`BLOCKED`/`COMPLETED`/`FAILED`/`CANCELLED`) — closes R11-9's gap (an arbitrary new non-terminal was previously allowed). Landed before R12-1, guarded in flight | 4th test in `terminal-replay-statuses-freeze.test.ts` |
| R12-10 (`d592f8c`) | New AST property census: every `materializeCheckpoint` call with a flowchart payload (identifier payloads resolved to nearest same-function initializer — a property, never a writer count), vacuity-guarded, `contract` required everywhere, `taskCriteria` required wherever the payload mentions it (green before the writer, strict after). Prescribed the exact successor rather than squatting on R12-1's file | `checkpoint-writer-carriage.test.ts` |
| Parent joints (`0e61063` + the fold in `81f5b81`) | `0e61063` applied R12-10's prescribed writer-existence guard verbatim, 7 s after R12-1 — the field can no longer silently lose its last writer. The abort-test flip folded **inside** `81f5b81` per Round 11's rule (the file had no Round 12 owner; content verified). **Zero red-tree commit points** — Round 11's procedural fix, executed | — |

## 2. Current baseline (this VM, Node v22.14.0, engine warning only)

- **`pnpm gate` GREEN, exit 0**: **1947 tests / 1946 pass / 0 fail / 1 skipped**. The only skip is the `PI_SMOKE=1` real-provider gate; exactly one `# SKIP` line in the TAP output. Any slot whose report shows a second skip introduced one and must say so. Delta vs Round 11: +9, accounting closed per-slot in the review §2.
- `node scripts/crash-probe.mjs` → `ok: true`, **11 cases × 3 iterations**; original ten names and order unchanged, `unblock-discard-append-before-checkpoint-sigkill` last. The name-list pin lives in `test/integration/persist/crash-recovery.test.ts` (diff-empty this round). Anyone changing the case set owns updating that pin in the same slot, with disclosure.
- Perf: no fresh bench needed. Round 12's only hot-path deltas: `persistCheckpoint` gains one in-memory pass over the event array it already replays (plus one more on a resume); `onRunStarted` is one synchronous callback per run start. No I/O, lock, writer, or fs primitive changed (verified: zero raw fs writes in the round's `src` diff). Any Round 13 perf claim records its own same-VM before/after with an unchanged-arm control; ≥5% end-to-end or it is not a candidate.
- Parent bookkeeping: clean — the Round 11 chimera dispatch id was fixed in `PROGRESS.md` (verified).

## 3. Forbidden / frozen for Round 13

Global forbidden list, unchanged: live R1/bandit/topology on the execution path (doctor's `loadProjectBanditByKey` inventory read is the **only** exception — re-swept at HEAD: store + doctor only; `selectArm` definition + `shadow.ts` only; bare `loadProjectBandit` zero `src` matches); Outcome-supported claims; **ADR-006 stays Proposed** (twelve rounds; keep it); auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit; no `git checkout` of other branches. `independentEvidence` is never read as corroboration (whole-`src` dereference census: exactly the one `void` at HEAD; the flowchart spine is now directly pinned to contain zero mentions). No new `RunStatus` at all (the exact eight-member vocabulary is frozen — stronger than the old no-fourth-terminal rule). No fourth key on `RUN_UNBLOCKED` (exact-key validator, read at HEAD).

Frozen contracts (Rounds 1–11 set, all held through Round 12, plus new):
- Rounds 1–11 carry-overs, verbatim from the Round 11 brief §3 — every one re-verified or gate-green this round: jsonl signatures; `writeFileAtomic`(+Sync), no private temp+rename (Round 12's whole `src` diff added zero raw fs primitives — verified); `withExclusiveFileLock` + `LOCK_TIMEOUT_CODE`; `episodeLockPath` symmetry; `runCommand` sink wiring; `deleteRunRecords` lock-plus-double-verify; append/checkpoint take no run lock; `recordCrashTerminal` in-flight-only + caller always rethrows; `applyRetry` sole scheduler BLOCKED→READY producer + R8-3/R10-8/R11-8 AST pins; `preserveResumableState`; `ClusterMailReport`; resume flags + disclosure wordings + the supervised loopback stderr byte-pin (file diff-empty again); `writeFeedbackTombstones`; adaptation error codes + bind-after-load; doctor frozen-additive JSON incl. `learnedState`; feedback fuzz hard-fails; `withRunLifecycleLock` rules; supervised settle-on-crash + rounds-window pins; `commandFailureNext` code-discrimination; `CheckpointStore.read` typed error; one-definition-of-terminal (`TERMINAL_REPLAY_STATUSES` = COMPLETED/FAILED/BLOCKED, read at HEAD); five `DOCTOR_ROUTED_NEXT` routes + `GENERIC_FAILURE_NEXT` character-exact (reviewer re-extracted base-vs-HEAD through R12-1's `main.ts` edit: identical); `--lock-wait-ms`; `process-death.ts`; R6-9 absence pins; reconstruction `childTasksFromLog` + FAIL-unreachable tripwire (both wiring pins re-matched byte-exact through R12-1's rename); parent-plane `recordTerminal`; empty-graph pre-flight; `settleSupervisedOutcome` absence; the Round 8 block (`RUN_UNBLOCKED` three-key; four-line BLOCKED; `INSPECT_SUMMARY` additive; catalog posture (b); parent-plane residuals); the Round 9 block (durable contract never synthesized; precedence `continuation.contract ?? checkpoint.flowchart.contract`; verdict-producer rules; gate `runStatus` ledger; the executed-descendant refusal exact message); the Round 10 block (`RUN_UNBLOCKED_WITH_DISCARD` exact-key; uniform clearing; single-append; per-block permission; every flowchart-payload `materializeCheckpoint` carries `contract`; verdict tool freezes; episode boundary census; `independentEvidence` posture; BLOCKED prefix + `note:`); the Round 11 block (option (a) schema as shipped; `unmet-acceptance-criterion` supplied-never-derived, observed-FAILED only, unknown-not-unmet; the `taskCriteria` seam schema; the restore-side discard audit, set before sums, completeness limit recorded; the 11-case probe; the tracked pause controller; direct formatter/inspect freezes; the R10-2 meta-pin).
- **New from Round 12:**
  - **The `taskCriteria` writer is frozen as shipped.** Three sources exactly — caller specs at start (`plannedTaskCriteria`), non-empty logged `TASK_REQUEST`s (`advanceTaskCriteria`), the checkpoint's existing record on restore — first-write-wins, monotone, **empty logged requests ignored** (only the caller's spec may say known-none; a substituted re-dispatch cannot launder unknown into known-none, proven behaviourally), absence = unknown forever. The reader fills **only** substituted specs, at `childTasksFromLog`'s sole call site; a logged request's answer is never overridden. **No `continuation.taskCriteria`** (negatively pinned). Every flowchart-payload writer carries it beside `contract` (four per-seam source pins + R12-10's property census + `0e61063`'s writer-existence guard). It is never synthesized from the episode, the definition, or the run contract (R11-10's census, discriminator verified).
  - **`onRunStarted` is frozen as shipped:** optional on `FlowchartRunDeps`, fired after run dir + lifecycle lock + `RUN_CREATED`, before round 1's pause poll; throws swallowed (state fail-closed, notification best-effort — reasoned in-source); one-way, never a control channel; forwarded by `startTrackedRun`; the track path prints `Run <id>: started` while the run is still pausable; the clarification path deliberately does not fire it. The behavioural pure-CLI tracked pause rests on this ordering.
  - **The `RunStatus` vocabulary is frozen at the exact eight members** (R12-9) — any new status, terminal or not, must rename that pin in the open.
  - **The criteria-gate reachability facts are recorded:** a reported FAILED criterion blocks a run whose node COMPLETED (the "gate wins" rule from the disagreeing direction); the sanctioned exit for that class is the no-retry unblock; `--discard-executed` is structurally unavailable for it.
  - **The flowchart spine cannot mention `independentEvidence`** (R12-2's census-membership + mutation pin).
- Saturated — do not spend a slot: everything in the Round 11 saturated list, plus: freeze/pin work on the `taskCriteria` surface (writer sources, reader, carriage, episode boundary, vocabulary — held at nine distinct pins; re-pinning is busywork, the two named residuals in §4 are the only real gaps); the `onRunStarted` wiring pins; the criteria-gate block/control surface; the live-isolation file; routes/`INSPECT_SUMMARY`/BLOCKED prefix; the Round 11 docs truth-up (done; Round 12's own staleness is candidate 4).

Process requirements per slot (carried forward, all held this round — keep them): census first, against the working tree; **verify that a path the brief hands you actually exists before writing to it**; scoped `eslint` + whole-tree `tsc --noEmit` before reporting; census your consumers and ship or prescribe their updates in your own diff; run `live-isolation.test.ts` if you add an import inside the live closure; attribute shared-tree transients to files with timestamps; timing-sensitive owned tests 3×; full gate is the parent's job; no scratch files at report time (out-of-tree mutation copies in `/tmp`, deleted — R12-3's model); a "did not land" claim is only true as of the minute taken. Dispatch cross-check (Round 11's rule, validated this round): every obligation's file must live in the obligated slot's ownership, or the joint is planned and **folded into the landing commit** — R12-1's fold and the held `0e61063` successor are the two working patterns; reuse them.

## 4. Ranked Round 13 candidates (mutually exclusive ownership)

The round's tilt: the two durability obligations Round 11 surfaced are both closed; what remains is honesty debt (stale source docstrings and re-staled docs), two cheap behavioural closures on the new writer's least-exercised arms, and the last copy of the run-id-at-end gap. **There are four real candidates, not ten — stated per protocol; the freeze surface is saturated and this brief does not manufacture busywork. If fewer slots run, the parent fills extras only against new Round 13 schema.**

File-contention map: candidate 1 (comment-only `src` docstrings) and candidate 4 (docs) are disjoint by construction but *both truth-up the same facts* — run them as one slot or sequence 1 before 4 so the docs can cite the corrected source. Candidate 2 needs `test/integration/run/criteria-gate.test.ts` and/or `test/integration/m2.5/resume.test.ts`; candidate 3 needs `src/cli/main.ts` **and** `test/integration/cli/blocked-next.test.ts` **and** `test/unit/cli/doctor-routed-next-freeze.test.ts` in one slot (the output census lives there — this is exactly the joint Round 12's R12-1/R12-7 split correctly refused to improvise). Candidates 2 and 3 are disjoint from everything. Nobody touches `flowchart-run.ts`'s writer/reader region, `unblock-flow.test.ts`, `replay.test.ts`, or any pin file without a mandate naming it.

### R13-1 (P1, comment-only src — parent sign-off required for the two files, not the semantics) — Retire the two false "no writer" docstrings
**Evidence (review §6.1, reviewer-verified at HEAD):** `src/run/replay.ts:107-110` ("No `src` writer fills this yet… the writer is prescribed in `.agent_workspace/loop4-r11-t1.md`") and `src/tracking/prescore.ts:174` ("…has no writer yet"; obligation 1's "resumed child specs are re-synthesised with empty criteria" is now true only of unrecorded nodes) are false since `81f5b81`. No test asserts the phrases (whole-tree grep: zero matches), so this is honesty debt, not a red risk — but it is *load-bearing* honesty debt: both comments are exactly where the next reader of the seam will look first. R12-1's report §8.1 supplies replacement text that keeps every pinned sentence intact (`never *synthesized*`, `not from the bound episode`). Obligations: pins-first census — the option-(a) pins match `replay.ts` prose and the criteria-are-guidance record pin matches `prescore.ts` prose (it moved with its sentence once before, R11); the whole diff must be mechanically comment-only (zero non-comment changed lines, R11-1's `prescore.ts` precedent); `coverageOutcome` body byte-identical.
**Ownership:** `src/run/replay.ts` + `src/tracking/prescore.ts` (comment-only); `test/unit/tracking/option-a-preconditions.test.ts` and `test/unit/tracking/criteria-are-guidance.test.ts` only if a pinned sentence must move with its prose — disclose, replacement not deletion.

### R13-2 (P2, test-only) — Behavioural pins for the writer's two unexercised arms
**Evidence (review §4.5/§6.2/§6.3):** (a) The unblock carry-forward is the last source-pin-only seam on `taskCriteria` — the same posture `contract` had before R10-4. The fixture now exists: R12-3's block test is a BLOCKED run whose checkpoint records the caller's known-none entry; read `checkpoint.flowchart.taskCriteria` before the unblock, after the unblock's reopen rewrite, and after the resume — three assertions in an existing file. (b) `advanceTaskCriteria`'s log-derived arm (the sign-off's source 1) has no behavioural discriminator in a fresh-tree run: the start seed pre-records every caller task and spawned children log empty (ignored), so the merge arm only fires for a checkpoint that predates the writer. One test that hand-strips `taskCriteria` from a real checkpoint (schema-valid — absence is unknown), resumes, and asserts the rewritten checkpoint recovered the logged non-empty requests proves the arm and documents the legacy-recovery semantics. Do not touch the writer itself; do not re-pin what §3 lists as held.
**Ownership:** `test/integration/run/criteria-gate.test.ts` (additive) and/or `test/integration/m2.5/resume.test.ts` (additive). No `src/**`.

### R13-3 (P2, src, small — parent sign-off required) — Early run-id for `--flowchart` and `--children`
**Evidence (R12-1 residual 2, reviewer-verified: `onRunStarted` wiring exists at exactly one `main.ts` call site, the track path):** the same operator gap `onRunStarted` closed for `--track` persists on the other two public run paths — their ids print only at the end, so a live `--flowchart` run cannot be paused from its own output. The dep and the guarantee already exist; the change is one `onRunStarted` handler per path in `runCommand`. The reason this was correctly not done in Round 12: those paths' stdout is censused by `blocked-next.test.ts`, which R12-7 owned — so this slot must own `main.ts` **and** both output-census files together (the contention rule, not a suggestion). Census every stdout assertion on those paths before writing the line; routes must stay character-exact; `INSPECT_SUMMARY` untouched (inspect is a different command). If a census shows the new line breaks a byte-pinned output shape that cannot absorb it additively, stop-and-report with the census.
**Ownership:** `src/cli/main.ts` (sole); `test/integration/cli/blocked-next.test.ts`; `test/unit/cli/doctor-routed-next-freeze.test.ts`; `test/integration/m2.5/cli-contract-honesty.test.ts` if its regexes need widening (disclose).

### R13-4 (P3, docs) — Round 12 truth-up
**Evidence (reviewer-verified at HEAD, review §6.4):** stale spots — `docs/status-matrix.md:36` ("A node never requested keeps empty criteria/artifacts" — false for criteria a record names; the row's "At the Round 11 close no `src` writer populates it, so the seam is not yet a durability guarantee" reads as current state and the guarantee now exists), `docs/status-matrix.md:60-65` (no-writer residual + run-id-at-end gap, both closed for the track path), `docs/specs/m0-m2-architecture.md:367` and `:553` (same no-writer claims). Record Round 12's landings as commits (`81f5b81`, `95a2b25`, `b8f784f`, `d1b451c`, `b65a8b1`, `d592f8c`, successor `0e61063`, the folded abort-test joint inside `81f5b81`): the writer's three sources and the unknown/known-none rule, the empty-logged-request guard, `onRunStarted` and the track path's early id (and the `--flowchart`/`--children` gap — or its closure if R13-3 lands first), the criteria-gate reachability and the no-retry exit for COMPLETED-node blocks, the eight-status vocabulary. Sequence after R13-1 (or same slot) so the docs cite corrected source. `docs/decisions/**` untouched; ADR-006 stays Proposed.
**Ownership:** `docs/**` only; timestamp-census the working tree (the five-census pattern is the model).

*(No further candidate is manufactured: there are **four**. Everything else surfaced by Round 12 is either saturated (§3), folded above, or a recorded decision whose reopening needs new evidence — the spawned-child fail-toward-unknown precision cost, the no-`continuation.taskCriteria` rule, the onRunStarted swallow, the check-coverage PASS→UNOBSERVED movement for resumed testers, the discard unavailability for criterion blocks behind COMPLETED nodes, the audit completeness limit, the unasked-criterion-id acceptance, the role-gate C4 asymmetry, and `derivedClaimedVerificationWithoutChecks` kept-not-widened.)*

## 5. Explicitly NOT for Round 13 (unchanged or newly settled)

Live R1/bandit/topology (standing; the keyed diagnostic read is the ceiling); reading `independentEvidence` as corroboration or renaming it (whole-`src` census + the new spine pin); **any** new `RunStatus` (exact eight-member freeze); a fourth key on `RUN_UNBLOCKED` (exact-key validator; the stronger event exists); re-litigating the `taskCriteria` writer's shape — last-request-wins (launders, the exact bug), writing substitution empties (forbidden and proven unnecessary), recording spawned children as known-none (no caller spec vouches), a `continuation.taskCriteria` (a caller re-answering a dispatch record is the invention the seam prevents), or recording empty logged requests (indistinguishable from substitutions on the log) — all rejected with evidence in `81f5b81`'s in-source reasoning and the review §4.1–4.2; overloading `onRunStarted` into a control channel or making its throws fatal (state fail-closed, notification best-effort — recorded); firing `onRunStarted` on the clarification path (nothing to pause); synthesizing `contract` **or `taskCriteria`** from the episode/definition/contract (census + discriminator verified); a flowchart checkpoint writer that drops `contract` or `taskCriteria` (property census + writer-existence guard fail by location); re-litigating option (a)'s mechanism, the discard audit's completeness, the unblock fail-closed default, the gate-ledger posture, the R10-1 payload decisions, or the R11-4 set-before-sums ordering (each recorded and pinned); correlating criterion ids against the request at the protocol/tool layer; offering per-criterion `UNOBSERVED` on the tool; manufacturing a pause by killing a process or racing the token (standing; the synchronous-callback pattern is the sanctioned determinism seam); jsonl/lock/append perf and per-step writer locking (saturated); mailbox/dead-letter/cluster work; lock stealing; resume-time adoption of unaccepted child results; `maxCostUsd` enforcement; a non-terminal `RUN_CRASHED` marker; rewriting append-only run logs for privacy; ADR-006 status changes (frozen); P0 privacy sign-off (human); `package.json`/dependency bumps.
