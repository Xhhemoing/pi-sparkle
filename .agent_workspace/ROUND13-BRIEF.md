[Model: claude-fable-5-thinking-xhigh]

# ROUND 13 BRIEF — injection context for Loop 4 · Round 14

Provenance: written by the Round 13 SOTA reviewer (claude-fable-5-thinking-xhigh) after evidence-based review of R13-1…R13-10 at `16212ba`; verdicts and verifications in `.agent_workspace/loop4-r13-review.md` (10 ACCEPT, 0 ACCEPT-WITH-NITS, 0 ROLLBACK; **one joint — the mandated `main.ts`+output-census fold inside `1e78220` — counted; zero red-tree commit points; three process nits recorded for the parent, none content-bearing**). Every candidate below cites evidence the reviewer verified at HEAD against the actual diff `aa7282f..16212ba`, not report hearsay. All landings are committed. **Stay on `agent/opt-continuous`.**

## 1. What landed in Round 13 (all committed)

| Slot | Landed | Key facts (do not re-implement) |
|---|---|---|
| R13-1 (`f6e4c04`) | The two false "no writer" docstrings retired. **Mechanically comment-only, token-stream-proven** (reviewer re-proved with the TypeScript scanner: zero non-comment tokens changed in either file). `replay.ts`'s `taskCriteria` docstring now states the shipped writer (three sources, first-write-wins, empty logged ignored, absence-is-unknown); `prescore.ts`'s obligation 1 now scopes the empty-criteria re-synthesis to nodes nobody recorded. Every pinned sentence kept in place: `never *synthesized*`, `not from the bound episode`, the asked-for-is-not-evidence sentence, the `` `unmet-acceptance-criterion` anomaly`` phrase, and the FAIL-unreachable tripwire reference (whose only occurrence sat inside the rewritten paragraph — the near-miss was caught by the slot's pins-first census). The spent `loop4-r11-t1.md` pointer dropped. `coverageOutcome` body unchanged | `src/run/replay.ts:107-113`, `src/tracking/prescore.ts:165-182`; both conditional pin files byte-untouched |
| R13-2 (`e7d018c`) | **The `taskCriteria` seam is now fully behaviourally pinned — the two R12 residuals are closed.** Pin (a): the unblock reopen's carry-forward measured off disk (`CheckpointStore.read` + `validateCheckpoint`, never the outcome object) at three moments — before unblock, after the reopen rewrite, after the resume's own checkpoint write — whole-array deep-equal against the caller's known-none entry, the one statement no later writer can rebuild. Pin (b): the `advanceTaskCriteria` log-derived arm proven on its real domain (a pre-writer checkpoint, reproduced by hand-stripping the field from a real paused run's checkpoint — schema-valid, absence is unknown): the resume recovers `tsk_first` from its logged non-empty request onto dispatch and record, while substituted `tsk_second` logs `[]` and **stays absent** — legacy recovery restores what the log can prove and declines to launder the rest. Reviewer independently reproduced the load-bearing mutation out-of-tree: severing the merge arm reds exactly pin (b) (26/27) — the arm had no prior discriminator. No `src/**` | new test in `test/integration/run/criteria-gate.test.ts` (TAP 4→5); new test in `test/integration/m2.5/resume.test.ts` (21→22) |
| R13-3 (`1e78220`) | **All three public run paths now disclose their id early.** Two `onRunStarted` handlers in `runCommand` (`main.ts:784` `--flowchart`, `:972` `--children`), copied verbatim from the track path's shape (`Run <id>: started` on stdout), zero imports added, `flowchart-run.ts`/`track/loop.ts` untouched. Two behavioural sequence pins in `blocked-next.test.ts` (12→14): output read as an ordered `Run <id>: <word>` sequence — `["started","BLOCKED"]` / `["started","COMPLETED"]` — same id on both lines, and the file's own first-match extraction proven to still name the settled run. Nothing races, nothing is killed; both pins read settled output. Routes reviewer-re-extracted base-vs-HEAD: character-exact; `INSPECT_SUMMARY` four keys untouched; the BLOCKED block is stderr, the disclosure stdout. `doctor-routed-next-freeze.test.ts` and `cli-contract-honesty.test.ts` owned, censused, deliberately diff-empty (the honesty file's extractor is first-match; widening would have been busywork) | `src/cli/main.ts` +15/−1; `test/integration/cli/blocked-next.test.ts` +2 tests |
| R13-4 (`8faf8f4`) | Round 12 docs truth-up: the writer's three sources and unknown/known-none rule, `onRunStarted` + the track path's early id, the criteria-gate reachability and no-retry exit, the eight-status freeze — all recorded against real commits only; five timestamped censuses; never assigned an uncommitted sibling a commit id; ADR-006 Proposed, `docs/decisions/**` diff-empty. **Stale again at HEAD, doubled (the standing race, not a miss):** committed 60 s before R13-1 and 173 s before R13-3, so the census-anchored "gap remains open" spots and the still-false-comments census notes are superseded — candidate 1 owns them | `docs/status-matrix.md`, `docs/data-dictionary.md`, `docs/specs/m0-m2-architecture.md`, `docs/superpowers/plans/2026-08-18-phase-a-tracking-supervisor.md` |
| R13-5…R13-10 (report-only, `3862b10`) | Six correct declinations, each naming the structural property that makes an addition duplicative: live-isolation closure 165/215 with zero new import edges (R13-3's callbacks are import-free — reviewer-verified at final HEAD, closing the slots' honest before-the-landings census timing); whole-file AST `applyRetry` absence; construct-and-reads-episode discriminator; exact-one-`void` + zero-spine-mention; exact eight statuses; writer carriage + writer existence. Reviewer re-verified every underlying property at final HEAD; no missed gap | six pin files, all untouched |
| Parent | Committed all slots; gate GREEN recorded at `16212ba` (numbers match the reviewer's). Process nits (review §5, none content-bearing): R13-4 sequenced before R13-1 against the brief's recommendation (race window doubled); landing commits carry one-line PROGRESS ticks (R12's pure-commit model preferred); R13-3's mutation ran in-tree for a disclosed, sha-verified 4.2 s window with a justification R13-2's same-round `/tmp` full-copy model disproves | — |

## 2. Current baseline (this VM, Node v22.14.0, engine warning only)

- **`pnpm gate` GREEN, exit 0**: **1951 tests / 1950 pass / 0 fail / 1 skipped**. The only skip is the `PI_SMOKE=1` real-provider gate; exactly one `# SKIP` line in the TAP output. Any slot whose report shows a second skip introduced one and must say so. Delta vs Round 12: +4, closed per-file in the review §2 (criteria-gate +1, resume +1, blocked-next +2).
- `node scripts/crash-probe.mjs` → `ok: true`, **11 cases × 3 iterations**; original ten names and order unchanged, `unblock-discard-append-before-checkpoint-sigkill` last. The name-list pin in `test/integration/persist/crash-recovery.test.ts` is diff-empty this round. Anyone changing the case set owns updating that pin in the same slot, with disclosure.
- Perf: no fresh bench needed. Round 13's only hot-path delta: one synchronous stdout callback per `--flowchart`/`--children` run start (the same shape `--track` has carried since Round 12). Comments are token-invisible; zero I/O, lock, writer, or fs primitive changed (verified: zero raw fs writes in the round's `src` diff). Any Round 14 perf claim records its own same-VM before/after with an unchanged-arm control; ≥5% end-to-end or it is not a candidate.
- Parent bookkeeping: clean and matching the reviewer's independent numbers.

## 3. Forbidden / frozen for Round 14

Global forbidden list, unchanged: live R1/bandit/topology on the execution path (doctor's `loadProjectBanditByKey` inventory read is the **only** exception — re-swept at HEAD: store + doctor only; `selectArm` definition + `shadow.ts` only; bare `loadProjectBandit` zero `src` matches); Outcome-supported claims; **ADR-006 stays Proposed** (thirteen rounds; keep it); auto-promote; P0 privacy sign-off stays human; `package.json`/dependency edits; git history rewrites; subagents do not commit; no `git checkout` of other branches. `independentEvidence` is never read as corroboration (whole-`src` dereference census: exactly the one `void` at HEAD; the flowchart spine pinned to zero mentions). No new `RunStatus` (exact eight-member freeze). No fourth key on `RUN_UNBLOCKED`; `RUN_UNBLOCKED_WITH_DISCARD` stays exact-keyed.

Frozen contracts (Rounds 1–12 set, all held through Round 13, plus new):
- **Rounds 1–12 carry-overs, verbatim from the Round 12 brief §3** — every one re-verified or gate-green this round, including: the whole R1–R11 block (jsonl/atomic-write/lock/delete/crash-terminal/`applyRetry`/resume-disclosure/doctor/routes/`INSPECT_SUMMARY`/BLOCKED-prefix/episode-boundary/option (a)/discard-audit/probe/verdict-producer freezes); the `taskCriteria` writer as shipped (three sources, first-write-wins, **empty logged requests ignored**, absence = unknown forever, reader fills only substituted specs at the sole rebuild call site, **no `continuation.taskCriteria`**, never synthesized from episode/definition/contract, carriage + writer-existence guards); `onRunStarted` as shipped (fired after run dir + lifecycle lock + `RUN_CREATED`, before round 1's poll; throws swallowed; one-way, never a control channel; clarification path silent); the exact eight-member `RunStatus` vocabulary; the criteria-gate reachability facts; the flowchart spine's zero `independentEvidence` mentions.
- **New from Round 13:**
  - **The early-id disclosure is uniform and frozen as shipped:** all three public run paths print `Run <id>: started` on stdout before the run settles, from the one `flowchart-run.ts:1285` firing site; the terminal `Run <id>: <status>` line keeps its place and shape. The output is pinned **as a sequence** (`blocked-next.test.ts`'s `runLines`): disclosure first, terminal second, same id, and every first-match `/Run (run_…):/` extraction in the tree stays valid. Anyone adding a third `Run <id>: <word>` stdout line to these paths owns the sequence pins in the same slot.
  - **The reopen carry and the legacy log-merge are behavioural guarantees now, not source pins:** a known-none record survives unblock reopen and resume byte-for-byte (measured off disk); a pre-writer checkpoint's record is recovered from non-empty logged requests only, and a substituted node's logged empty is never absorbed. Mutation-verified (slot table + independent reviewer reproduction).
  - **The corrected docstrings are the citable source prose** for the seam (`replay.ts:107-113`, `prescore.ts` obligation 1); docs and future comments cite them, not the retired text.
- Saturated — do not spend a slot: everything in the Round 12 saturated list, plus: the entire `taskCriteria` surface (writer sources, reader, carriage, vocabulary, episode boundary, **and now both behavioural arms** — eleven distinct pins; the only remaining item is the §4 candidate 2 comment); the `onRunStarted` wiring on all three paths and its sequence pins; routes/`INSPECT_SUMMARY`/BLOCKED prefix (re-proven character-exact through a `main.ts` edit for the second consecutive round); all six freeze-extra files (six correct declinations in a row — the census-only dispatch of these slots should be questioned before Round 14 repeats it, see §4).

Process requirements per slot (carried forward, all held this round — keep them): census first, against the working tree; verify handed paths exist before writing; scoped `eslint` + whole-tree `tsc --noEmit` before reporting; census your consumers and ship or prescribe their updates in your own diff; run `live-isolation.test.ts` if you add an import inside the live closure; attribute shared-tree transients with timestamps; timing-sensitive owned tests 3×; full gate is the parent's job; no scratch files at report time. **Mutations run out-of-tree, full copy under `/tmp` with `node_modules` symlinked, then deleted — R13-2's model. The in-tree window R13-3 used is not to be repeated: its "relative imports cannot resolve from `/tmp`" justification is disproven by R13-2's same-round model** (review §5 nit 1). Parent: sequence source-truth commits before docs-truth commits when the brief orders them (nit 2), and keep landing commits free of PROGRESS ticks (nit 3).

## 4. Ranked Round 14 candidates (mutually exclusive ownership)

**The freeze surface is saturated.** Round 13 closed the last two behavioural gaps on the newest seam and the last copy of the operator gap; what remains is exactly two honesty-debt items, both prose. **There are two real candidates, not ten — stated per protocol. This brief does not manufacture busywork.** If more slots run, the parent fills extras only against genuinely new Round 14 schema — and given six-for-six correct declinations this round on the freeze-extra files, re-dispatching those six as census slots is itself now busywork unless a Round 14 diff touches their subject matter.

File-contention map: candidate 1 (docs) and candidate 2 (comment-only `src`) are disjoint by construction but truth-up adjacent facts — **sequence 2 before 1, and commit in that order** (the standing race has now cost two consecutive rounds; the parent owns the ordering, review §5 nit 2). Candidate 2's conditional pin files are the same two R13-1 owned (`option-a-preconditions.test.ts`, `criteria-are-guidance.test.ts`) — same rule: touch only if a pinned sentence must move, replacement not deletion, disclose. Nobody touches `flowchart-run.ts`, `main.ts`, `blocked-next.test.ts`, `unblock-flow.test.ts`, `replay.test.ts`, or any pin file without a mandate naming it.

### R14-1 (P2, docs) — Round 13 truth-up
**Evidence (review §6.1, reviewer-verified at HEAD):** the stale spots are `docs/status-matrix.md:69-70` and `docs/specs/m0-m2-architecture.md:411-413` (the census-anchored "`--flowchart`/`--children`… operator gap remains open" — closed by `1e78220`), plus the three dated census notes (`status-matrix.md:22`, `m0-m2-architecture.md:22`, `data-dictionary.md:271`) describing the source comments as still-false (corrected by `f6e4c04`). Record Round 13's landings as commits (`f6e4c04`, `e7d018c`, `1e78220`, `8faf8f4`): the uniform three-path early-id disclosure and its sequence pins, the behavioural closure of the reopen carry and legacy log-merge (including the visible legacy cost: a stripped-record node re-dispatches with nothing, recorded not hidden), and the docstring truth-up. Keep the five-census pattern; never assign uncommitted sibling work a commit id; `docs/decisions/**` untouched; ADR-006 stays Proposed.
**Ownership:** `docs/**` only.

### R14-2 (P3, comment-only src — parent sign-off required for the file, not the semantics) — Scope the `replay.ts:85-93` laundering narrative
**Evidence (review §4.1, reviewer-verified against `childTasksFromLog:509` and `withRecordedCriteria:550` at HEAD):** the paragraph's mechanics are still literally true — an unlogged node is substituted `[]`, logs it, and the log's answer stays authoritative — but only for a node **neither** source records; for a recorded node the writer restores the spec before the node runs, and an unvouched logged-empty is now detectable against the record. The paragraph reads as current-state hazard and over-states the residue. One or two sentences scoping it, keeping the mechanics sentences and every pinned phrase (`never *synthesized*` and `not from the bound episode` live at `:72`/`:98`, **outside** this paragraph but inside the same interface region the pin regexes — re-run the region pin after editing). Same bar as R13-1: mechanically comment-only, token-stream-proven, pins-first census. Ride-along with disclosure: `option-a-preconditions.test.ts:618`'s comment cites the spent `loop4-r11-t1.md` prescription — if this slot is granted that file (comment-only), replace the pointer with the landed fact (`resume.test.ts`, R12-1); it is a comment, no pin reads it.
**Ownership:** `src/run/replay.ts` (comment-only); optionally `test/unit/tracking/option-a-preconditions.test.ts:618` (comment-only, disclose); the two conditional pin files only if a pinned sentence must move.

*(No further candidate is manufactured: there are **two**, both prose. Everything else surfaced by Round 13 is either saturated (§3), a recorded decision (§5), or a residual the owning slot itself correctly sized as not-a-candidate: the non-empty-entry carry fixture (the carry is one conditional spread with no per-entry logic — R13-2 mutation 1's finding), per-path `advanceTaskCriteria` measurement on pause/inject (shared code), liveness pins for `--flowchart`/`--children` pause (the ordering guarantee is the shared `flowchart-run.ts:1285` site's, already behaviourally proven by the tracked pause), and the `--children` routing-block position (planning precedes the run).)*

## 5. Explicitly NOT for Round 14 (unchanged or newly settled)

Live R1/bandit/topology (standing; the keyed diagnostic read is the ceiling); reading `independentEvidence` as corroboration or renaming it; **any** new `RunStatus`; a fourth key on `RUN_UNBLOCKED`; re-litigating the `taskCriteria` writer's shape (all rejected alternatives recorded in `81f5b81`'s in-source reasoning and the R12 review §4.1–4.2 — last-request-wins, substitution empties, spawned-children-as-known-none, `continuation.taskCriteria`, recording empty logged requests); re-pinning any part of the `taskCriteria` surface (eleven pins; both arms now behavioural); overloading `onRunStarted` into a control channel, making its throws fatal, or firing it on the clarification path; adding a liveness/pause proof per CLI path (the shared-call-site proof exists; a per-path copy is duplication); a third `Run <id>: <word>` line or reordering the `--children` routing block (cosmetic, breaks a pinned sequence); re-running the six freeze-extra censuses absent a diff touching their subject (six-for-six declinations this round); synthesizing `contract` or `taskCriteria` from episode/definition/contract; a flowchart checkpoint writer that drops `contract` or `taskCriteria`; re-litigating option (a)'s mechanism, the discard audit's completeness, the unblock fail-closed default, the gate-ledger posture, or the set-before-sums ordering; correlating criterion ids at the protocol/tool layer; per-criterion `UNOBSERVED` on the tool; manufacturing a pause by killing a process or racing the token; jsonl/lock/append perf and per-step writer locking; mailbox/dead-letter/cluster work; lock stealing; resume-time adoption of unaccepted child results; `maxCostUsd` enforcement; a non-terminal `RUN_CRASHED` marker; rewriting append-only run logs for privacy; ADR-006 status changes; P0 privacy sign-off (human); `package.json`/dependency bumps; in-tree mutation testing (out-of-tree full-copy model only, §3).
